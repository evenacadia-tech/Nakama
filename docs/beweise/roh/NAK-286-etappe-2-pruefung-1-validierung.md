# NAK-286 Etappe 2 — Quellvalidierung der Erstprüfung 1 (Dirigent, 14.09.2026)

Urteil des Prüfers: `NAK-286-etappe-2-pruefung-1-de1821af.txt` (Codex `gpt-6-astra` max, lesend, Thread `01a09fc4-2716-7422-8ff0-b6a855a49c28`, Prüfbereich `f059503d...a2887462`, HEAD `de1821af` vor und nach dem Lauf). Je Befund ein lesender Opus-Agent an der Quelle (HEAD `de1821af`, Code der 21 Ticketpfade gleich `a2887462`). Einordnung durch den Dirigenten in `docs/beweise/NAK-286.md` §23.

## Befund 1 (P1) — Timer-Lebensdauer vor der Kernkopie, `Briefkasten.cpp:866-869`

**Codelage (Zitate am HEAD).**
- `eq-copilot/plugin/core/diagnose/Briefkasten.cpp:866-870`: `void Briefkasten::timerCallback() { const auto k = kern; Kern::takt (k); }` — Zeile 868 liest den Member `kern` (`std::shared_ptr<Kern>`, `Briefkasten.h:308`) über `this`.
- `Briefkasten.cpp:849-858` `stoppe()`: `gestoppt.exchange(true)`, `hakenBeimStopp`, `stopTimer()` (:856), `k.schleuse.schliessen()` (:857). `Briefkasten.cpp:730-733`: `~Briefkasten() { stoppe(); }`.
- Schleuse `ipc::CallbackSchleuse` (`Briefkasten.cpp:476`; `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:121-165`): atomarer Zähler; `schliessen()` wartet nur, solange `aktivZaehler != 0`. Betreten erst in `Kern::takt`, `Briefkasten.cpp:688`; der Testhaken `hakenVorSchleuse` liegt bei :684-685 — hinter der Kopie in :868.
- Destruktoren ohne Message-Thread-Sicherung: `eq-copilot/plugin/src/PluginProcessor.cpp:457-461`, `eq-copilot/plugin/sonde/SondeProcessor.cpp:212-216` (`briefkasten.stoppe()` als erster Schritt; kein `MessageManagerLock`, kein Assert).
- JUCE 8 (`eq-copilot/build/_deps/juce-src/modules/juce_events/timers/juce_Timer.cpp`): `stopTimer()` :395-402 → `removeTimer` :207-224 nimmt nur das Listen-Lock und wartet nicht auf einen laufenden Callback; `callTimers` :161-171 ruft `timer->timerCallback()` mit freigegebenem Lock über einen rohen Zeiger. `Timer::~Timer` :359-370 verlangt im Kommentar: „If you're destroying a timer on a background thread, make sure the timer has been stopped before execution reaches this point" (der `jassert` schweigt hier, weil `isTimerRunning()` nach `stopTimer()` schon false ist).

**Interleaving (real).** (1) Message-Thread: `callTimers` liest `timer`, gibt das Lock frei, springt nach `Briefkasten::timerCallback` (:866). (2) Fremdthread: `~Prozessor()` → `stoppe()` → `stopTimer()` kehrt sofort zurück → `schliessen()` sieht `aktivZaehler == 0` (Schleuse bei :688 noch unbetreten) und kehrt ohne Warten zurück → `~Briefkasten` zerstört `kern`, der Prozessor wird zerstört. (3) Message-Thread führt :868 `const auto k = kern;` aus — Lesen des zerstörten Members über einen toten `this`. Fenster: Eintritt in `timerCallback` bis Abschluss der Kopie in :868; ab :869 hält die lokale Kopie den Kern, und `Kern::takt` fasst `this` nicht mehr an.

**Zusagen.** F-12 (`docs/beweise/NAK-286.md:374`): „Stopp als **erster** Schritt des Destruktors …, danach eine Schleuse gegen einen Takt, der bei einer Zerstörung von einem Fremdthread noch läuft (Muster `callbackSchleuse` …; JUCE-Vertrag zu `stopTimer`)." M-34 (`:548`): Lage „ein Takt läuft auf dem Message-Thread und hält am Testhaken (Barriere)", Zusage „der angehaltene Takt fasst nach der Freigabe keinen zerstörten Zustand an". §5.5 Punkt 1 (`:749`): „Schleuse für den Destruktor". `Briefkasten.h:34-37` (Selbstbeschreibung): „Ein Takt, der bei einer Zerstoerung von einem Fremdthread noch vor der Schleuse steht, haelt den Kernzustand ueber einen `shared_ptr` am Leben und wird an der Schleuse abgewiesen" — im Prolog-Fenster falsch. `CLAUDE.md:84-86` („starten↔stoppen … in denselben Änderungssatz") bricht am Wortlaut nicht.

**Test M-34** (`eq-copilot/plugin/tests/BriefkastenTestMain.cpp:2585-2632`): Haken über `setzeHakenVorSchleuse` (:2597), Halt bei `Briefkasten.cpp:684-685`, Destruktor auf zweitem Thread (:2608-2615), Prüfung `abgewieseneTakte == 1` und Kanarienwert (:2626-2629). Zum Zeitpunkt der Freigabe existiert die lokale Kopie `k` bereits; das Prolog-Fenster :866-868 erzwingt der Test nicht.

**Agent: BESTÄTIGT.** Für Defekt: F-12 verspricht die Schleuse gegen jeden „noch laufenden" Takt und `Briefkasten.h:34-37` behauptet den `shared_ptr`-Schutz vor der Schleuse; beides gilt im Prolog nicht. Für Härtung: keine Matrixzeile benennt das Prolog-Fenster, M-34 definiert die Lage „hält am Testhaken", CLAUDE.md:84-86 bricht nicht.

**Dirigent: DEFEKT** (§23.1) — der Takt im Prolog ist ein Takt, „der bei einer Zerstörung von einem Fremdthread noch läuft" (F-12 wörtlich); die Zusage ist nicht auf die Lage des Testhakens beschränkt, und der Code behauptet in seiner Selbstbeschreibung einen Schutz, den er nicht herstellt (Use-after-free ohne Anfrage, Gate Schicht (3) „ohne Datei geschieht nichts", R-286-2). Schließende Regel P-12 in §23.2.

## Befund 2 (P2) — Rahmenzähler außerhalb der Kopiersperre, `Analyse.cpp:1361-1362`, `SondeProcessor.cpp:947`

**Codelage (Zitate am HEAD).**
- Gen `eq-copilot/plugin/src/prozessor/Analyse.cpp:1344-1366` (`EqCopilotProcessor::diagnoseAntwort`): Sperrblock :1351-1361 über `externerAnalyseSteuerZug()` (`PluginProcessor.h:1466-1481`, `std::unique_lock<std::mutex> zug (analyseSteuerMutex)`) kopiert Snapshot, `merkmale.frame()`, beide Summen, offenes Fenster, `material.festgehalten()`, `bloeckeMax()`; Zeile :1362 `auszug.framesGebaut = merkmalFrames.load();` steht **hinter** dem `}`. Kommentar :1346-1347 behauptet „Snapshot, Rahmen, Zaehler und Materialzeit unter DERSELBEN Steuersperre … kopiert".
- Gen, Erhöhung: `Analyse.cpp:309-310` `if (merkmale.nimmBlock (…)) merkmalFrames.fetch_add (1);` im Worker unter `analyseSteuerMutex` (:160, Sperrbereich :159-362). Feld `PluginProcessor.h:1658`.
- Probeeq `eq-copilot/plugin/sonde/SondeProcessor.cpp:919-951`: Sperrblock :936-946 (`std::lock_guard<std::mutex> l (analyseSchloss)`) kopiert dieselben Größen; :947 `auszug.framesGebaut = framesGebaut.load();` hinter dem Block. Erhöhung :636 `framesGebaut.fetch_add (1, …)` im Worker unter `analyseSchloss` (:549, Sperrbereich :548-697). Feld `SondeProcessor.h:466`.

**Interleaving (real).** Kopierblock verlassen → Worker nimmt die Sperre, `nimmBlock` liefert true, Zähler n+1, Rahmen n+1 → Antwortthread lädt n+1. Antwort: Rahmen n, Summen n, Materialzeit n, `frames_gebaut` n+1. Die Erhöhung läuft nur unter derselben Sperre; das Laden im Sperrblock schließt das Fenster vollständig.

**Zusagen.** F-15 (`NAK-286.md:377`): „Daten werden unter der bestehenden Sperre kopiert (Gen: Analyse-Steuersperre wie `merkmalFrame()`; Probeeq: `analyseSchloss` …); Serialisierung und Datei-I/O laufen ohne Sperre." F-6 (`:359`): „`frames_gebaut` (Gen: `merkmalFrames` …; Probeeq: neuer atomarer Zähler am Rahmenbau im Worker …, nie genullt)"; „Alle Materialzeitfelder werden unter derselben Sperre kopiert wie Snapshot und Rahmen (F-15), damit sie denselben Stand beschreiben." §5.5 Punkt 3 (`:751`, tragend für Gen): „Rahmen über `merkmalFrame()`, **`merkmalFrames`**, die beiden Summen (M-79) und das offene Fenster unter der Steuersperre kopiert". §5.5 Punkt 5 (`:753`, Probeeq): nennt `framesGebaut` nicht in der Sperrliste. M-32 (`:543`): „`frame` aus `merkmale.frame()` unter `analyseSchloss` mit `frames_gebaut` = Zahl der gebauten Rahmen des Sampleplans (kumulativ, F-6)". M-54 (`:585`) regelt nur I/O außerhalb. M-71/F-23: nur Δ über zwei Antworten. Schema `nakama-diagnose-antwort-v1.schema.json:28`: „kumulativ seit Instanzstart, faellt nie" (keine Kohärenzaussage). `SondeProcessor.h:368-369`: „M-32: die Sequenz des zuletzt gebauten Rahmens … der unabhaengige Zeuge fuer `frames_gebaut`. Unter `analyseSchloss`." Test `BriefkastenTestMain.cpp:1789-1790` prüft `frames_gebaut == gebaut && == sequenzOhneNak29` (grün nur, weil die Queue vorher geleert ist, :1764-1767). `CLAUDE.md:186-187`: „Engine kennt keine Optik. Sie liefert kohärente Mess-Snapshots".

**Agent: BESTÄTIGT.** Defekt für Gen (§5.5 Punkt 3 wörtlich, Kommentar :1346-1347); Probeeq eher Lücke mit Härtungsanteil (§5.5 Punkt 5 und F-15 nennen den Zähler nicht; Kohärenz nur in `SondeProcessor.h:368-369` und im Test behauptet).

**Dirigent: DEFEKT für beide Rollen** (§23.1) — Gen bricht §5.5 Punkt 3 wörtlich; Probeeq bricht M-32 („`frame` … unter `analyseSchloss` mit `frames_gebaut` = Zahl der gebauten Rahmen" koppelt Zähler und Rahmen unter derselben Sperre) und die Kohärenzzusage, die Code (`SondeProcessor.h:368-369`) und Test (:1789-1790) selbst tragen; die Ursache ist in beiden Rollen dieselbe. Schließende Regel P-13 in §23.2.

## Gemeinsame Ursache (§23.3)

(j) Der Schutzbereich (Schleuse, Sperre) beginnt oder endet nicht an der Grenze der Zugriffe, die er schützen soll: bei Befund 1 beginnt der Schutz erst nach der ersten Berührung von `this` (Kernkopie vor der Schleuse), bei Befund 2 endet die Sperre vor dem letzten Feld desselben Standes. Der Auftrag prüft jeden Schleusen- und Sperrbereich der Etappe (Briefkasten, Antwortpfade beider Rollen, Testzugänge) auf dieses Muster.
