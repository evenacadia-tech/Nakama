# G3 — Phasengate P3 · Passive Landkarte (T3)
<!-- NAKAMA-URTEIL: T3 NEEDS_WORK 2026-09-03 offen -->

**Datum:** 02./03.09.2026 · **Prüfstufe:** T3 · **Gate-Text (wörtlich, `docs/bauaufteilung-sonden.md:385`):** „**Gate:** `/rust-review` + Codex + 60-min-Soak. Falsifikation: Gate 7" · **Session:** eigene (nakama-dirigent `489e27c7`), hat P3 nicht gebaut und keine Zeile Produktcode, Schema, Test, Fixture oder Runner angefasst. **Vorbild:** `docs/beweise/G2.md`.
**Gate-Stand:** `ad5b2d1b4729e09ea0cd58b6f22dba3f10624bed` (= `origin/master`, Worktree sauber). Seit dem Kanon-Stand `d52fd15` (GRÜN 40/40, `docs/beweise/roh/G3-SOAK-d52fd15.md`) liegen nur `docs/**`-Commits; die 40 Beine gelten für den Code unverändert.

---

## 1. Urteil

**NEEDS_WORK.**

Gate 7 **hält** — von zwei Modellen unabhängig auf 21 beziehungsweise 11 durchgespielten Wegen „kein Pfad gefunden" (§4). Die Exit-Gate-Klauseln Save/Reload, Duplicate, Bridge/PID-Wechsel und zwei offene Projekte **halten** an der Quelle und sind je durch einen benannten Test gemessen (§5). Der Rust-Review kam ohne HIGH oder CRITICAL zurück (§7). Das Urteil trägt **ein bestätigter Defekt**, den der Soak selbst sichtbar gemacht hat:

**D1 — Nach einem Brokerneustart bleibt bei 32 Sonden ein Teil der Sonden endgültig getrennt.** Der 30-Minuten-Lauf mit 32 Sonden und drei Neustarts (`docs/beweise/roh/G3-soak-n32-30min-ad5b2d1.txt`, Bericht `….json`) ist ROT: nach jedem der drei Neustarts wird der `session_snapshot` nie wieder vollständig (S11 `bereit_bis_vollstaendig_ms = -1`, Reconnect p95 = 60 001 ms = Fristmarke), die Mitgliedschaft driftet (S03 22 925 von 82 572 Übernahmen vollständig; laufende Abtastung 4 482 von 17 922), und der K-S5-Zähler zeigt, dass 7 der 33 Control-Clients in der 21-s-Totzeit von Neustart 2 **keinen einzigen Verbindungsversuch** machten (`backoff_deckel_erreicht 26, erwartet 33`). Stale- und Eviction-Zähler bleiben 0, P0-ACK p95 25,5 ms — die Fehlenden waren am neuen Broker **nie angemeldet**, sie wurden nicht evictiert. Mit 1, 4, 8 (je 2 min, 1 Neustart) und 16 Sonden (60 min, 3 Neustarte; Kanon-Kurzform 2 min) ist dasselbe Bein grün — die 60-Minuten-Hälfte des §49.3-Soakbudgets ist damit gemessen, die 32-Sonden-Hälfte fällt.

Ursache, an der Quelle nachgelesen: Beim Neustart stürmen 2·(N+1) = 66 Verbindungen auf zwei scharfe Pipe-Instanzen (`broker/src/transport/server_v3.rs:1684-1700`). `IpcVerbindung::oeffnen` versucht 20 × `WaitNamedPipeW(200 ms)` (`eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:67-89`); wer 20-mal verliert, endet mit `ERROR_PIPE_BUSY`. `IpcVerbindung.cpp:90-99` klassifiziert **jeden** Fehler außer `ERROR_FILE_NOT_FOUND` als `ServerPruefStatus::belegtAberUnverifiziert` — laut `IpcVerbindung.h:40-42` „ein Sicherheitsfehler und kein Livenessfehler". `ControlClient.cpp:994-1012` parkt den Verbindungsthread daraufhin dauerhaft („kein wiederholtes Anklopfen"), nur `reconnect()`/`stop()` löst ihn; `TelemetryClient.cpp:407-424` hat dieselbe Sperre. Die NAK-123-Sicherheitsregel hat damit den reinen Kontentionsfall verschluckt. Gebrochen sind der P3-Exit-Gate-Satz „Brokerneustart und 60-Minuten-Soak erhalten richtige Mitgliedschaft" (Entwurf `:4262-4264`) und §49.3 „mindestens 30 min mit 32 Sonden" (`:3586`). **Produktwirkung:** startet der Broker in einer Session mit vielen Sonden neu, bleibt ein Teil der Sonden bis zum Neuladen der Instanz getrennt.

Register **NAK-134**; Nacharbeit mit Verhaltensmatrix vor Code in `docs/beweise/NAK-134.md` (noch nicht angelegt, entsteht mit dem Bau; §12). Zwei Messlücken des Werkzeugs A24, die den Defekt verdeckten, gehören zur selben Nacharbeit: S04 verwirft abgelehnte P0-Einreihungen still (`SessionSoakMain.cpp:777-785`) und `reconnect_paare` ist die Vektorlänge, kein Messwert (`:1064,1108,1270`).

---

## 2. Bruchaufträge — wörtlich

Beide Modelle bekamen sie mit erzwungener Antwortform (konkreter Pfad **oder** wörtlich „kein Pfad gefunden"):

> **Gate 7** (Entwurf §49.2 Nr. 7: „Eine Standard-Insertprobe wird als exakter Mastersummenbeitrag bezeichnet."): „Finde einen Pfad, auf dem eine Standard-Insertprobe als **exakter Summenbeitrag** bezeichnet wird. Antworte mit dem konkreten Pfad oder mit ‚kein Pfad gefunden' — nicht mit einer Einschätzung."

> **P3-Exit-Gate** (Entwurf `:4262-4264`): „Finde einen Pfad, auf dem Save/Reload, Duplicate, ein Bridge-/PID-Wechsel, zwei offene Projekte oder ein Brokerneustart eine falsche Mitgliedschaft oder eine falsche Steueradresse hinterlassen — im Broker-Store, im `session_snapshot`, im Main-State oder im Gen-Quellenmodell. Antworte je Klausel mit dem konkreten Pfad oder mit ‚kein Pfad gefunden'." Dazu je Klausel der messende Test und die §49.3-Zeilen 3577/3578/3582/3586 mit messendem Bein.

Ausschlüsse: `docs/**` keine Befundfläche; im Register datierte Punkte NAK-118, NAK-124, NAK-125, NAK-126, NAK-132, NAK-133; same-user-Szenarien (Entwurf §48.4); XRun-Hälfte des Soaks (FL-Termin, `docs/beweise/G3-SOAK.md` §2.2). Vollständiger Wortlaut: `docs/beweise/roh/G3-codex-falsifikation-2-ad5b2d1.txt` (Urteil) beziehungsweise Prompt im Temp-Ordner der Session.

---

## 3. Wer geprüft hat

| # | Prüfer | Modell / Effort | Fläche | Ergebnis |
|---|---|---|---|---|
| 1 | Codex, Falsifikation (Fassung 2) | `gpt-5.6-sol`, max, read-only, Thread `01a06416-2bbe-7b13-ae50-4b99c09b9c0e` | Verträge, Fixtures, C++, Rust, State, Store, Snapshot, Modell | Gate 7 **Pfad behauptet** (G7-1, an der Quelle: Härtung, §8) · Exit-Gate: Save/Reload, zwei Projekte, Brokerneustart **kein Pfad**; Duplicate und Bridge/PID **Pfad behauptet** (an der Quelle: Matrix C-10/L-03 deckt Duplicate, Bridge ist Lücke, §8) · 11 + 24 Wege |
| 2 | eigener Falsifikationslauf | Opus, 1 Agent, 65 Werkzeugaufrufe, 692 s | dieselbe Fläche; A5 selbst gefahren (407/407, 250 Fixtures) | Gate 7 **kein Pfad** (21 Wege) · alle fünf Exit-Klauseln **kein Pfad**, je mit messendem Test (§5) · 7 Lücken/Härtungen (§8) |
| 3 | `/rust-review:rust-review` | Opus-Worker `fab505e0` (max), 21 Cluster-Worker + 2 Richter, LOCAL_UNPRIVILEGED, Filter medium | `broker/` | 28 Primaries → **14 MEDIUM, 13 LOW, 1 LIKELY_FP, 0 HIGH/CRITICAL**; 8 neu gegenüber G2 (§7) |
| 4 | Soak A24, Skalierungsreihe | `pruefe_session_soak.py` gegen den echten Coordinator auf der Probe-Pipe | 1 · 4 · 8 (2 min, 1 Neustart), 32 (30 min, 3), 16 (60 min, 3) | 1/4/8 **GRÜN**, 32 **ROT** (D1), 16/60 min: §6 |
| 5 | Ursachenanalyse D1 | Opus, 1 Agent, lesend, 55 Werkzeugaufrufe | Bericht-JSON, A24-Quellen, Broker- und Client-IPC | Ursachenkette §1; vom Dirigenten an der Quelle bestätigt (`IpcVerbindung.cpp:90-99`, `IpcVerbindung.h:42`, `ControlClient.cpp:996-1012`) |

**Werkzeugausfälle, protokolliert (kein Codebase-Befund):**
- Codex-Falsifikation Fassung 1 (Thread `01a06405-9ae8-7a53-b2b7-07b73ea33e3d`, 23:28–23:44) endete mit `turn.failed` „flagged for possible cybersecurity risk", als der Thread einen Probe-Broker auf einer Probe-Pipe starten wollte; kein Urteil. Fassung 2 mit umformuliertem Auftrag (Falsifikation statt Bruch, ausdrücklich ohne Prozessstarts) lief durch. In der Codex-Sandbox startete `py -3.13` nicht (WindowsApps-Launcher, „Anmeldesitzung ist nicht vorhanden"); der Vertragsprüfer A5 wurde deshalb vom eigenen Lauf (2) gefahren.
- `claude --allowed-tools` ist variadisch: ein danach stehender Auftrag wurde zum Werkzeugeintrag, der erste Review-Worker `ad42f989` startete ohne Auftrag (idle). Neustart mit dem Auftrag als erstem Positionsargument (`fab505e0`).

---

## 4. Falsifikation Gate 7 — gehalten

Beide Modelle prüften die G1-Nacharbeiten am HEAD als intakt: `insert|pre|post` pinnen `aussageklasse` per `const` auf `beobachtend` (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json:600-601`), `$defs/capabilities_beitrag` bindet `contribution_aux` auf `supported` (`:143`, `:691`), der Beitragszweig ist mit `plugin_kind` `maxLength: 0` (`:686`) strukturell unerfüllbar; die persistierte Projektion wird beim Laden zweimal gegen den vollen Vertrag validiert (`broker/src/coordinator.rs:1047`, `:1071`); Host-State sperrt `post_fader_contribution` doppelt (`state/NakamaState.cpp:361,395,413-416`), Migration und v2-Rückweg kennen die Position nicht (`:895,901-905`); FlatBuffers trägt die Achse nicht (`nakama_telemetry_v1.fbs:47-50,61`); das Gen-Quellenmodell verwirft einen Descriptor mit `aussageklasse != beobachtend` samt Snapshot (`SourcesModel.cpp:200`); die Anzeige druckt nur `unclassified|insert|pre|post` (`PluginEditor.cpp:1267`). Referenzbein A5 am HEAD: 407/407, `const` 13/13, `enum` 17/17.

Codex' behaupteter Pfad G7-1 (`Coordinator::descriptor_setzen`, `coordinator.rs:1758-1785`, prüft nur die Adresse) hat am HEAD **keinen Produktionsaufrufer** — `rg descriptor_setzen` trifft ausschließlich `broker/tests/store_crash_matrix.rs` (Zeilen 748, 753, 2046, 2136, 2140, 2176, 2183, 2190, 2313). Ohne Eingabepfad ist keine Insertprobe „bezeichnet"; die Stelle ist eine Härtung (Register NAK-137, beide Modelle nannten sie).

---

## 5. P3-Exit-Gate je Klausel — an der Quelle

| Klausel | Befund | Gemessen durch |
|---|---|---|
| Save/Reload | kein Pfad | B14 `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:343-352` (`reload_preserves_identity_label_membership_but_not_live_truth`: Mitglied bestätigt, `control == getrennt`, `runtimeNonce` leer) und `:357-359` (frischer `subscribe_session`); der State trägt keine `session_epoch` (`state/NakamaState.h:49-57`) |
| Duplicate | kein Pfad; Matrix C-10 / ENTSCHIEDEN L-03 (`docs/beweise/SONDE-011.md:516,624`): neue Nonce verdrängt sofort, ein erneuter Report der verdrängten Nonce sperrt beide fail-closed bis zur Auflösung nach C-03/C-07 — genau das beschrieb Codex als „Pfad" | B2 `StateMigrationTestMain.cpp:1266-1272`; A4 `coordinator_model::neue_nonce_verdraengt_alte_sofort`, `verdraengte_nonce_report_sperrt_beide`; A4-SI `nonce_verdraengung_cleanup_vor_altem_report`. **Lücke:** kein Ende-zu-Ende-Test über Hello → Verdrängung → Konfliktriegel → Snapshot → Main (NAK-138) |
| Bridge-/PID-Wechsel | kein falscher Endzustand; **Lücke** im Fenster bis zum ersten Heartbeat der neuen Nonce (≤ 1 s): `control_hello_registrieren` endet `:875` ohne `dirty_sessions.insert`, `control_ende` `:904-910` markiert nur bei `current_link == geschlossener Link`, erst `heartbeat_kontakt` `:1655/:1661` pusht. Ein Befehl mit der alten Nonce ist in diesem Fenster nonce-fail-closed, nicht fehlgeleitet; Fernsteuerung gibt es erst P6/P7 | A4 `broker/tests/sonde012_sources_slice.rs:1023` (`bridge_pid_change_preserves_membership_but_never_old_control_address`), B13 `bridge_pid_change_has_no_false_member_or_address`. Regel NAK-136 |
| Zwei offene Projekte | kein Pfad | `broker/tests/coordinator_model.rs:311-338` (zwei Mains desselben Projekts: Kandidat bleibt in eigener Sitzung, `!bestaetigt`, `beitritt_bestaetigung_noetig`), `coordinator.rs:669-694`. **Härtung:** `zwei_projekte_bleiben_getrennt` `:453-454` prüft nur die Anzahl (NAK-139) |
| Brokerneustart | Semantik hält (alte Epoche kommt nie wieder, keine fremde Adresse, `fuehrendes_main` immer richtig, Subscription endet sichtbar); **Vollständigkeit bricht bei 32 Sonden — D1** | A24 `pruefe_session_soak.py:776-780,915-917,940-942`; `SessionSoakMain.cpp:606-631`; `store_crash_matrix::brokerneustart_sendet_keine_laufgebundenen_felder_der_alten_projektion`, `coordinator_model::brokerneustart_behaelt_session_epoch`, `sonde012_sources_slice::restart_has_no_old_live_or_measurement_truth` |
| 60-Minuten-Soak | §6 | A24 |

---

## 6. Soak-Läufe (A24, Probe-Pipe, echter Coordinator, Gate-Stand)

| Lauf | Parameter | Dauer | Urteil | Kernzähler | Roh |
|---|---|---|---|---|---|
| n1 | `--sonden 1 --minuten 2 --neustarts 1` | 149 s | GRÜN | Neustart vollständig nach 2 516 ms; P0 p95 83,7 ms; 0 Drops | `roh/G3-soak-n1-2min-ad5b2d1.txt` |
| n4 | `--sonden 4 --minuten 2 --neustarts 1` | 148 s | GRÜN | — | `roh/G3-soak-n4-2min-ad5b2d1.txt` |
| n8 | `--sonden 8 --minuten 2 --neustarts 1` | 149 s | GRÜN | — | `roh/G3-soak-n8-2min-ad5b2d1.txt` |
| n32 | `--sonden 32 --minuten 30 --neustarts 3` | 2 131 s | **ROT (D1)** | S03 22 925/82 572 · Abtastung 4 482/17 922 · S13 13 440 (= 17 922 − 4 482) · S11 −1/−1/−1, Reconnect p95 60 001 ms · k_s5 26/33 · stale 0/0 · P0 56 673 gesendet, p95 25,5 ms, 0 verloren außerhalb · Speicher Client +4,0 MB/1 800 s, Broker je Generation ≤ +0,9 MB · Audio 6 389 888 Blöcke, 0 Drops · k_s1–k_s4 getroffen | `roh/G3-soak-n32-30min-ad5b2d1.txt`, `.json` |
| n16 | `--sonden 16 --minuten 60 --neustarts 3` | 3 757 s | **GRÜN** | Neustart 1/2/3 vollständig nach 2 501 / 2 542 / 593 ms (17/17 Paare) · k_s5 17/17 (jeder Client kam bis zum Backoff-Deckel und weiter) · k_s1–k_s4 getroffen · P0 p95 21,9 ms (max 2 235 ms), 0 verloren außerhalb · Speicher Client +3,1 MB/3 600 s, Broker je Generation ≤ +1,8 MB/900 s · Audio 5 633 376 Blöcke, 0 Drops, 0 Kontinuitätsbrüche | `roh/G3-soak-n16-60min-ad5b2d1.txt`, `.json` |

Warmup 20 s (< 30 min) beziehungsweise 120 s; Neustart *k* bei *k/(neustarts+1)* des Messfensters; die Messuhr steht im Neustartfenster (`docs/beweise/G3-SOAK.md` §9). XRun bleibt FL-Termin (§2.2 dort).

---

## 7. Rust-Review `broker/` — Härtungen, kein Gate-Defekt

`.rust-review-results/20260902T213108Z/` (Kopie im Repo: `docs/beweise/roh/G3-rust-review-20260902T213108Z/` mit `REPORT.md`, `fp-summary.md`, `delta-vs-20260901T104834Z.md`, `run-summary.md`, `dedup-summary.md`, `findings/`). Alle 21 Cluster-Tasks `completed`, `validate_artifacts.py` OK. 28 Primaries: **20 bekannt** (G2, Register NAK-124), **8 neu**: MEDIUM `BUFFLUSH-001` (`server_v3.rs:678`, v3-Handle ohne `FlushFileBuffers` geschlossen, während der Store die Wiederzustellungsschuld schon kompaktiert hat) und `NONDET-001` (`coordinator.rs:2648`, P0-Zielverbindung bei zwei Links derselben Adresse aus der HashMap-Reihenfolge); LOW `SAFETYDOC-001` (`server_v3.rs:2115`, 17 undokumentierte `unsafe`-Blöcke in `client_sid_authentisieren`, NAK-123-Code), `STRCMP-001` (`pipetoken.rs:38`), `UNWRAP-001` (`coordinator.rs:3096`), `DROPSKIP-001` (`lib.rs:997`), `BUFFLUSH-002` (`server_v3.rs:2217`); `STRSLICE-001` LIKELY_FP. Zwei G2-Befunde sind durch NAK-123 verschwunden (u. a. `RAWFD-001` über den RAII-Typ `ListenerInstanz`), sechs wurden nicht wiedergefunden und gelten weiter (NAK-124). Der FP-Richter deckelt wie in G2 auf MEDIUM: jeder Weg braucht einen Prozess desselben Desktop-Users; nach Entwurf §48.4 außerhalb des zugesagten Bedrohungsmodells → Härtung, Register **NAK-142**, Einarbeitung mit NAK-121/NAK-124 in S19b.

---

## 8. Befundklassen — an der Quelle eingeordnet

- **Defekt:** D1 (§1) → **NAK-134**.
- **Lücken mit Dirigentenregel:** NAK-136 (nach Nonce-Verdrängung Session dirty + Snapshot-Push im Hello-Zyklus; Matrix C-10 ergänzen; Umsetzung S19b) · NAK-138 (Duplicate-Kette Ende-zu-Ende testen) · NAK-140 (Widerspruch Entwurf `:3582` „höchstens ein aktueller Liveframe je Probe" ↔ `broker/src/transport/warteschlange.rs:23` Cap 2 replace-oldest nach §53.9 — Vertrag und Code gehen vor, Entwurfszeile versioniert nachziehen).
- **Härtungen:** NAK-135 (Broker: nur zwei scharfe Listener-Instanzen `server_v3.rs:1684-1700` bei 2·(N+1) Ansturm, `PIPE_INSTANZEN = 98` erlaubt mehr) · NAK-137 (`descriptor_setzen` ohne v3-Prüfung, kein Aufrufer) · NAK-139 (`zwei_projekte_bleiben_getrennt` nur Anzahl) · NAK-141 (`SourcesModel.cpp:198-200` Klasse↔Position nur für `pre|post` + `main` redundant) · NAK-142 (Rust-Review G3).
- **Kein Befund:** Codex P3-1 Duplicate (Matrix C-10/L-03 beschreibt genau das Verhalten) · Codex G7-1 als Gate-7-Pfad (kein Aufrufer).

---

## 9. §49.3 — welches Bein welche Zeile misst

| Zeile | Budget | Bein |
|---|---|---|
| `:3577` Livekarte 16 Sonden < 300 ms p95 | A23 `pruefe_sonde012_sources_latency.py` / `Sonde012SourcesLatencyTest.cpp:329-336` (N = 16 und 32) | gemessen |
| `:3578` Bassframe 16 384 < 750 ms; Alter wird gezeigt; 10-Hz-Livepfad | A23 (`:333`); Alter B13/B15 (`SourcesModel.h:68`, `PluginEditor.cpp:1131,1169`); die Pfadaussage ist strukturell erfüllt (`FeatureEngine.h:425,444-445,612`), von keinem Bein als Aussage geprüft | teilweise |
| `:3582` höchstens ein aktueller Liveframe je Probe | A22 und A24 S06 messen die §53.9-Politik Cap 2 mit replace-oldest — Wortlaut ≠ Code (NAK-140) | Politik gemessen, Wortlaut nicht |
| `:3586` Soak ≥ 60 min/16, ≥ 30 min/32 ohne ungegrenztes Wachstum | A24 §6 — 32/30 min ROT (D1); XRun FL-Termin | offen bis NAK-134 |

---

## 10. Kanon

Kein Bau-Quellcode zwischen `d52fd15` (GRÜN 40/40, 2026-09-02 18:15) und `ad5b2d1`: `git diff --stat d52fd15..HEAD -- . ':!docs'` ist leer. Binaries frisch gegen Quellen (`EqCopSessionSoak.exe` 16:49:50 > `SessionSoakMain.cpp` 16:49:26; Release-Probe-Broker 07:49:37 > `broker/src` 02:17:48). Ein voller Kanonlauf ist mangels Binäränderung nicht nötig; der Abschluss-Kanon läuft auf dem NAK-134-Endstand.

---

## 11. Was das Urteil nicht deckt

- Kein FL-Lauf, kein XRun-Maß, kein installierter Stand.
- Codex fuhr weder `cargo test` noch A23/A24; die Test-Exes `EqCopStateMigrationTest` und `EqCopSonde012ProjectReloadTest` blieben in der Sandbox ungestartet (sie öffnen Pipes). Der eigene Lauf fuhr A5; alle übrigen Beine gelten über den Kanon `d52fd15`.
- Die sechs in G3 nicht wiedergefundenen G2-Rust-Befunde wurden nicht neu verifiziert; sie bleiben in NAK-124.
- `eq-copilot/plugin/tests/**` war keine Prüffläche des Rust-Reviews (C++), wie in G2.

---

## 12. Nacharbeit — was ein PASS braucht

1. **NAK-134** (`docs/beweise/NAK-134.md`, frischer Opus-Worker, Aufsicht ENG, Spezifikation vor Code): Regel R1 — ein erschöpfter `ERROR_PIPE_BUSY` ist ein Liveness-Ausgang mit eigenem `ServerPruefStatus`, normalem Backoff, ohne Broker-Startpfad und ohne Zählung als Serverprüfung; `belegtAberUnverifiziert` bleibt dem gescheiterten Identitätsbeweis und fremden Besitzern (`ERROR_ACCESS_DENIED` u. a.) vorbehalten. R2 — Control- und Telemetrie-Client identisch, Gegenpfade symmetrisch. R3 — A24 zählt abgelehnte P0-Einreihungen und misst `reconnect_paare` statt die Vektorlänge auszugeben. Beweise: deterministischer Klassifikationstest (vor dem Fix rot), Rotbeweis am echten System (`--sonden 32 --minuten 4 --neustarts 2` vor dem Fix ROT, danach GRÜN), Gate-Form `32/30/3` GRÜN, Kanon-Kurzform GRÜN, Kanon abgekoppelt.
2. Wiederprüfung des Fixdiffs (Vorlage B, frischer Codex-Thread, Sol max) und die Falsifikation Gate 7 bleibt vom Fixdiff unberührt (keine Vertrags-, State- oder Klassenfläche).
3. Danach Nachtrag in diesem Manifest (§13) mit Marke `T3 PASS`, Planstand, Register.

Die Lücken und Härtungen NAK-135 bis NAK-142 tragen das Urteil nicht; sie gehen datiert ins Register und werden in S19b (NAK-121/NAK-124, gleiche Dateien) beziehungsweise als Testhärtung eingearbeitet.
