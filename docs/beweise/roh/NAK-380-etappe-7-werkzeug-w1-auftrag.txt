Nacharbeit Werkzeug W-1, Ticket NAK-380 / S25q, Etappe 7 (Abschluss) — Auftrag des Dirigenten, 26.09.2026

GEGENSTAND (ein Defekt, ein Werkzeug, kein Produktcode)
Der Laufzeit-Arm (tools/fl/laufzeit.ps1 -Ticket NAK-380 -Basis c58a2714 -Beenden, Lauf 1, 14:06 bis 14:19 Uhr, docs/beweise/roh/NAK-380-laufzeit-3e5d3660-lauf1.md) verfehlte das Szenario snapshot-runde01 (Schritt 13 lokal.umlauf, Exit 4) mit dem Kurztext:
  gen: Vergleichsbasis verschieden: metrics_version 'm4.3-2026-09-26' statt m4.1-2026-08-15; probeeq: Vergleichsbasis verschieden: metrics_version 'm4.3-2026-09-26' statt m4.1-2026-08-15
Ursache an der Quelle: tools/fl/szenario.py:116 haelt METRICS_VERSION = "m4.1-2026-08-15" als Kopie (Kommentar „AnalyseEngine.h:60“); das Produkt traegt seit NAK-380 Etappe 6 kMetricsVersion = "m4.3-2026-09-26" (eq-copilot/plugin/src/AnalyseEngine.h:65, Kommentar :60-64). vorbedingung() (szenario.py:1160-1165) lehnt jeden Snapshot mit anderer Fassung als „Vergleichsbasis verschieden“ ab. Alle F-28-Bandwerte des Laufs treffen ihre Anker (Rohzeilen im Rohbeleg); das Produkt ist intakt, der Laeufer veraltet. Derselbe Fehler wuerde bei jedem kuenftigen Fassungsschritt wiederkehren.

REGEL DES DIRIGENTEN (R-380-17, Technik im Pruefwerkzeug)
Der Szenarienlaeufer haelt keine Kopie der Messfassung. Er liest die erwartete metrics_version aus der Quelle eq-copilot/plugin/src/AnalyseEngine.h (die Zeile `inline constexpr const char* kMetricsVersion = "<wert>";`, per regulaerem Ausdruck, relativ zum Repo-Wurzelpfad, den szenario.py heute schon kennt oder aus seiner eigenen Lage ableitet). Fail-closed: fehlt die Datei, fehlt die Zeile oder gibt es mehr als einen Treffer, endet der Schritt lokal.umlauf mit Szenario-Exit 5 (VORAUSSETZUNG) und Klartext, welcher Pfad und welches Muster nicht passte — nie ein Standardwert, nie die alte Kopie. Der Vergleich gegen den Snapshot bleibt wie heute („Vergleichsbasis verschieden: metrics_version <ist> statt <soll>“). Die Attrappen des Selbsttests (attrappe_snapshot, Standardwert des Parameters metrics) nutzen die gelesene Fassung; der Selbsttest (py -3.13 tools/fl/szenario.py --selbsttest, Bein A35 ueber tools/fl/selbsttest.py) bekommt drei neue Faelle mit Rotbeweis an der Zusagezeile:
  (a) Lesen: gegen eine Attrappe der Headerdatei mit bekanntem Wert liefert der Leser genau diesen Wert (Mutation: Muster auf einen anderen Bezeichnernamen -> rot).
  (b) Fail-closed: Attrappe ohne die Zeile und Attrappe mit zwei Zeilen -> VORAUSSETZUNG mit Klartext (Mutation: Rueckfall auf einen Standardwert -> rot).
  (c) Vergleichsbasis: ein Snapshot mit anderer Fassung ergibt weiterhin genau den Mangeltext „Vergleichsbasis verschieden“ (Mutation: Vergleich entfernt -> rot).
Je Matrixzeile faellt der Rotbeweis an der Zeile, die die Zusage traegt, nicht an einem Nebeneffekt. Protokolle der Mutationen als docs/beweise/roh/NAK-380-e7w1-mutation-<fall>.txt (Befehl, Ausgabe, Ruecknahme).

GRENZE
- Dateien: tools/fl/szenario.py, bei Bedarf tools/fl/selbsttest.py und eine Zeile in tools/fl/LIES-MICH.md (dann py -3.13 tools/plan/dokuriegel.py tools/fl/LIES-MICH.md mit Exit 0). Kein Produktcode (eq-copilot/**, broker/** bleiben unberuehrt), keine Szenariodatei (docs/gesundheit/szenarien/**), kein tools/beweise.ps1.
- Kein FL Studio, kein Laufzeit-Arm, kein Installer, kein Kanon, kein Bau: den Laufzeit-Arm faehrt der Dirigent nach deinem Push. Landmine: im sichtbaren Checkout laeuft bis zur Zeile EXIT= im Log $env:TEMP\nakama-nak380-laufzeit-2.log noch der zweite Laufzeit-Lauf, der szenario.py je Szenario neu startet — warte mit jeder Aenderung an tools/fl/**, bis diese Zeile da ist (Get-Content -Tail 1), und pruefe vorher, dass kein Prozess FL64 oder laufzeit.ps1 mehr lebt.
- Zeilenenden: tools/fl/szenario.py, selbsttest.py und LIES-MICH.md sind im Arbeitsbaum CRLF (git ls-files --eol) — so zurueckschreiben; UTF-8 ohne BOM.
- Beine, die du faehrst: py -3.13 tools/fl/selbsttest.py (A35, Exit 0, aus dem Repo-Wurzelpfad) und einzeln py -3.13 tools/fl/szenario.py --selbsttest; Logvergleich vorher/nachher (nur die neuen Faelle verschieden).
- Git: nur Pathspec deiner Dateien und der Mutationsprotokolle, Nachricht aus einer Datei mit Betreff „NAK-380 Etappe 7 Werkzeug W-1: szenario.py liest kMetricsVersion aus AnalyseEngine.h (R-380-17), Selbsttest drei Faelle“, dann git pull --rebase (nur bei sauberem Baum ausser den fremden Eintraegen briefing-hub/, nimbalyst-local/ und dem Broker-Pin eq-copilot/install/nakama-installer-v1.json, den du nicht anfasst) und git push. Kein Sammel-Add, kein Umschreiben von Historie. Fremde Dateien nie anfassen.
- Manifest schreibt der Dirigent (§62); du schreibst kein docs/beweise/NAK-380.md.

PRUEFLISTE (tools/dirigent/pruefliste.md gilt): Diff adversarial lesen (Regex auf genau eine Zeile, Pfadaufloesung relativ zum Repo, kein stiller Rueckfall, Fehlertext nennt Pfad und Muster), Zeilenenden, Selbsttest gruen, Mutationen dokumentiert und zurueckgenommen.

SCHLUSSMELDUNG (letzte Nachricht, genau so): FERTIG W-1 NAK-380, Commit <sha7> gepusht <ja/nein>, A35 Exit <n>, szenario.py --selbsttest <gruen/rot, Faelle n>, Mutationen <n> (rot: <liste>), Abweichungen vom Auftrag: <keine | Liste mit Grund>.
