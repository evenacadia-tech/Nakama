# NAK-309 — Matrixprüfung 1: Urteil des Prüfers (Rohbericht, wörtlich)

Prüfer: frischer, lesender Opus-Thread (Agent der Dirigentensession, Modell
Opus; Codex-Woche 94 %, Skill §3.6), 18.09.2026, 18:0x bis 18:2x Uhr. Auftrag
`docs/beweise/roh/NAK-309-matrixpruefung-1-auftrag.txt`, Prüfbereich `git diff
efb20bb4...f2320ba3 -- docs/beweise/NAK-309.md`. HEAD vor und nach dem Lauf
`589051fa65b047be5e6f6dd9737b3b816a407bcc` (gegenüber dem Auftrag nur ein
gerechnetes `docs/PLAN-STAND.md` mehr). 96 Werkzeugaufrufe, nichts geschrieben.
Einordnung durch den Dirigenten: Manifest §10.

---

URTEIL: PASS — kein DEFEKT (eine Lücke und fünf Härtungen benannt).

## DEFEKT

Keiner. Alle sechs Regeln R‑309‑1 (mit R‑309‑1'), R‑309‑2 bis R‑309‑6 sind je von Zeilen mit messendem Test und einem Rotbeweis an der Zusagezeile gedeckt; alle 13 Befunde tragen ihr Gegenbeispiel deterministisch; kein Pfad des Bauplans überschreitet die Grenze aus §3 und §9; keine Etappe braucht etwas, das erst eine spätere liefert; `istProbePipename`, Identität, Nulltest und Passthrough bleiben unberührt.

## LÜCKE

**L‑1 — „heute rot" ohne Mutation nach dem Bau (M‑09, M‑29).** §5 definiert das Etikett so: „**heute rot**: … die Zeile nennt die Gegenprobe (Datei:Zeile oder Rohbeleg) und die Mutation, die nach dem Bau an der neuen Zusagezeile rot macht", und §6.1 verschärft: „Für „heute rot" ist die Mutation nach dem Bau Pflicht". M‑09 nennt als Rotbeweis nur „derselbe Lauf endet am Basis-SHA `GRUEN` (§1; Teil A Stationen 1 bis 4); nach dem Bau ist dieser Lauf der Beleg", M‑29 nur „derselbe Aufruf ohne drittes Argument in derselben Rohdatei → Exit 1". Der Bauplan bestätigt das ausdrücklich (§6.3: „M‑09 über die zwei Runnerläufe"; §6.4: „M‑29 über den Handlauf ohne drittes Argument"). Beide Belege fallen an der Zusage, nicht an einem Nebeneffekt, und beide sind deterministisch — aber die Matrix entscheidet nirgends, ob ein Lauf am Basisstand die verlangte Mutation ersetzt. Der Dirigent sollte die Regel für diese zwei Zeilen präzisieren, sonst steht der Bauer der Etappen 2 und 3 vor einer Pflicht ohne benannte Mutation.

## HÄRTUNG

**H‑1 — §5 zählt die berührten Produktquellen um eins zu niedrig.** §5, Regeln dieser Matrix: „Produktquellen ändern sich nur an zwei Stellen, beide ohne Verhaltensänderung für den User: der Rückfall `PipeClient.cpp:221` entfällt, und das Produkt nennt an `PluginProcessor.cpp:151` den Namen ausdrücklich (M‑23, M‑24)." §6.4 ändert eine dritte: „`eq-copilot/plugin/src/PipeClient.h` (LF): ein lesender Zugriff auf den Pipenamen (Feld `:154`) für Tests und Wachen (M‑23)"; §8.4 A‑3 nennt sie, §9 genehmigt sie („der lesende Zugriff für M‑23 ändert keinen Zustand"). Kein Grenzbruch, nur der Satz in §5 ist nicht mehr wahr.

**H‑2 — §6.3 plant eine Änderung, die am ZIEL schon existiert.** §6.3: „[4d] die Attrappenfälle aus M‑12 bis M‑15 … wofür `hashen()` das Zielmanifest als Parameter bekommt." Gemessen `tools/eq-copilot/pruefe_installer_manifest.py:1117`: `def hashen(manifest: dict, ziel: pathlib.Path | None = None) -> int:` — der Parameter ist da, `:1122` `ziel = ziel or MANIFEST`, `:1163` `ziel.write_text(...)`. M‑12 selbst schreibt korrekt `hashen(…, ziel=…)`. Nur der Bauplansatz überzeichnet die Arbeit.

**H‑3 — „sechs Fremdprüfungen" sind fünf.** §1 (Gate‑Bezug, append‑only): „Der Runner prüft an sechs Stellen vor dem Beenden auf Fremdprojekte". Gemessen sind fünf Aufrufstellen von `Ist-Fremd` in `tools/fl/laufzeit.ps1`: `:403` (`Installiere`), `:443` (`Pruefe-Controller`), `:564` (`Rendere`), `:697` (`Frischer-Start`), `:807` (`Fahre-Lauf`); die sechste Stelle `Fahre-Rueckweg` (`:712-716`: `Beende-Diagnose-FL` / `Beende-Restprozesse` ohne jede Fremdprüfung) hat keine. Die Matrix ist hier genauer als §1: M‑30 listet exakt diese fünf Bereiche, und M‑39 trägt den Rückweg als eigene, heute rote Zeile („`Fahre-Rueckweg` hat keine Fremdprüfung … `:715` beendet das Titel-Doppel"). Kein Befund gegen die Matrix, nur gegen die Zahl in §1.

**H‑4 — M‑24 misst nur die eine Hälfte seiner Zusage direkt.** Zusage: „Kein Produktverhalten geändert: das Produkt nennt `kPipeName` ausdrücklich; Ziel und Verhalten seiner v2-Verbindung bleiben gleich." Der genannte Test ist die Quelltextwache `produkt_nennt_v2_pipe_ausdruecklich` plus „A1, B14, B8 als Regressionswachen des Prozessors". Die Wache belegt das Literal, nicht die Verhaltensgleichheit; die tragen nur die bestehenden Beine mit. Von keiner Zusage verlangt (§9 genehmigt A‑3 mit genau dieser Grenze), wünschenswert wäre eine Messung am gebauten Client.

**H‑5 — Abschnittszuordnung in M‑62.** M‑62 nennt als Ort des Testservers „`IpcTestMain.cpp`, Abschnitte G7 ab `:4681` und G10 ab `:4866`" für den **Control**-Handschlag. Gemessen ist `:4681` „G7 · der TelemetryClient prueft sein welcome vollstaendig", `:4866` „G10 · beide Clients pruefen das welcome gegen den VOLLSTAENDIGEN Vertrag"; die Schalterinfrastruktur liegt wie behauptet an `:4686-4688` und `:4874-4876` (§6.5 zitiert sie exakt). Der Control-Fall gehört zu G10, nicht zu G7. Zusage und Rotbeweis (an `Verbindung.cpp:804-811`) bleiben richtig.

## geprüft

`git rev-parse HEAD` zu Beginn und am Ende: beide `589051fa65b047be5e6f6dd9737b3b816a407bcc`, `git status --short` nur `briefing-hub/` und `nimbalyst-local/` — der Lauf ist gültig. Gelesen: der Diff `efb20bb4...f2320ba3 -- docs/beweise/NAK-309.md` (1 339 Zeilen, nur diese Datei) sowie §4 bis §8 vollständig und §0 bis §3 und §9 als Gate‑Bezug.

Selbst am ZIEL nachgemessen (Code seit `e008811e` unverändert, deshalb am Arbeitsbaum): **Frische** — `tools/beweise.ps1:969-984` (`$quellOrte` ohne `dsp`), `:985-996` (ein globaler `$neuesteQuelle`), `:1005-1025` mit `-lt` an `:1017`, `:1007` Stillgelegt-Filter, `:1003` `$bauBestaetigt`, `:956` „JEDE Quelle", `:37-39`, Baustandtabelle `:1347-1366`, Urteilsblock `:1199-1214` (`$rot` vor `$fehlendeVoraussetzung` vor `$veraltet`), Grün-Zählung `:1190`, Vorrang `:88`, A4 `:493` und A4‑SI `:501` ohne `--nocapture`, A35 `:618` und B22 `:725` wörtlich wie in §4 zitiert, A12 `:552`, A14 `:570`, Beinnamen B6=`EqCopDspGoldenTest`, B7=`EqCopTransactionTest`, A15 mit `Stillgelegt`. **Bauordnung** — `:886` (`cmake --build`) steht vor `:895-901` (`cargo build --release`); `tools/eq-copilot/pruefe_installer_manifest.py:1650-1665` verlässt `_lauf` mit `return hashen(manifest)` vor `[4c]` (`:1352-1391`), `:1381-1383` und `:1353-1357` und `:36-38` wortgleich zu §4.2, `[3d]`-Muster `:701-721`. **CMake** — 41 Vorkommen `nakama_kern_anbinden`: Definition `:277`, Meldungstext `:1378`, 36 direkte Aufrufe und 3 Hilfsfunktionen (`:365`, `:412`, `:444`) für vier Ziele (`:390`, `:421`, `:447`, `:449`) = **40 gebundene Ziele** (§8.2 Nr. 1 und A‑9 bestätigt); `NAKAMA_KERN_QUELLEN:198-220` mit genau 21 Quellen einschließlich `dsp/DspKern.cpp`; `:41-42` `CMAKE_CONFIGURE_DEPENDS`; `:88-89` `configure_file`; Schleife `:1373-1386`. Am Baubaum: `EqCopDsp.4CC300A8.tlog`, `generate.stamp.depend` mit Manifest, Identitätsdatei und zwei `.h.in`; `generate.stamp` 15.09. 18:24:34 gegen Manifest 18:47:19 — §8.2 Nr. 10 und F‑4 exakt. **Laufzeit-Arm** — `:350` `Ist-Diagnose` als Teilstringtest, `:351` dessen exakte Negation, `:353-360` und `:361-369` beenden ohne Besitzprüfung, `:250` `Stop-Process -Force`, Kopf `:14-15` und `:37-41`, `:99` `ProjektOrdner`, `:90` Repo-Vorgabe, `:289-296`, `:308-313`/`:325` (`uv run`), `:324`, `:410`/`:791`, `:446` `SetupLocal` in `Pruefe-Controller`, `:642-648`, `:681-684`, `:712-722`, `:771`/`:828`, `:782-795`, `:806-810`, `:820`. **Szenarien** — `szenario.py:178/:182` (nur `success`), `:241`, `:615-628`, `:2415-2434`, `TestVerbindung:2196-2249` mit `send_command:2217-2243`, `BEREITSCHAFT_13_09:2698-2712`, `M-58:2715-2726` mit „14 von 14"; `bereitschaft.json:16-17` ohne Echo-Erwartung, `:9` `index`, `:11` `mode`. **Probe** — `PipeProbeMain.cpp:112-113` wählt ohne Argument `kPipeName`, `:5-9`, `:55-59`, `:117`, `:187-194` wartet nur auf `! konflikt`; `PipeClient.cpp:221` Rückfall, `:346-357`, `:624-630`; `PipeToken.h:52-70` unverändert; `EqCopilotIds.h:18`; `PluginProcessor.cpp:151` (`{}`) und `:437-438` (nur v3 abgeschaltet — NB‑309‑1 an der Quelle bestätigt); A4b = `EqCopPipeClientTest` aus `tests/PipeClientLifecycleTestMain.cpp`, Wachenmuster `:1408-1452`; `README.md:55/:61/:62`; `NAK-123.md:634-636`. **Beweislücken** — `store_crash_matrix.rs:3220`, `:3229-3244`, `:3346`, `:3364-3367`, `:3434-3450`; `briefkasten.rs:63` (2 s), `:559-562`, `:859-892`, `:887-889`, `:1054-1058`, `:1791`; `Sonde013FingerprintGoldenTest.cpp:263-272` mit `pruefe (true, …)`; `StateMigrationTestMain.cpp:386-389/:993-996/:1013/:1083`; `erzeuge_state_fixtures.py:1074-1085`; `NakamaState.cpp:712-713`, `:824-825`, `:1512-1517`, `:2079` (§8.2 Nr. 3 exakt); `Verbindung.cpp:628-671` (kein Minor im Welcome) und `:804-811`; `TelemetryClient.cpp:531` ungedeckelt und `:662-667`; `WireEnvelope.h:81` (5) und `TelemetryClient.h:48` (2); `IpcTestMain.cpp:4681/:4686-4688/:4866/:4874-4876`; `abdeckungskarte.md:135-137` und `KONZEPT.md:141` („133 von 136"); `.gitattributes:49`. **Nachbarrepo** (nur lesend, HEAD `01f72386`, sauber): `utils/__init__.py:4` importiert `fl_trigger`; `device_FLStudioMCP.py:134-160` baut die Antwort ohne Kennung, `:95` `SCRIPT_VERSION`, Echo `track`/`name` in `handle_mixer_get_peaks`; `.gitignore:3` `uv.lock`, nicht getrackt. **Sonstiges** — `pruefliste.md:52/:62/:69-70/:82-84/:90` decken jedes in §4, §6 und §7 daraus gezogene Zitat; `planstand.py:102` liest `UNVOLLSTAENDIG\s*[-—]+\s*\d+\s+gruen`, ein Nachsatz bricht die Regex nicht; `NAK-286.md:411/:497/:510/:514/:520/:532/:533/:535/:575` tragen jede zitierte Zeile; alle Zeilenendenangaben aus §6.2 stichprobenweise mit `git ls-files --eol` bestätigt (CRLF: `beweise.ps1`, `laufzeit.ps1`, `PipeClient.cpp`, `PluginProcessor.cpp`, `Sonde013FingerprintGoldenTest.cpp`, `nakama-installer-v1.json`; LF: `CMakeLists.txt`, `PipeProbeMain.cpp`, `PipeClient.h`, `EqCopilotIds.h`, `TelemetryClient.cpp`, `StateMigrationTestMain.cpp`, `briefkasten.rs`); alle sieben neuen Pfade aus §6.0 und `tests/test_request_id.py` im Nachbarrepo existieren am ZIEL nicht; F‑1s Zahl „48 relative Includes" nachgezählt (48, Beispiel `TelemetryClient.cpp:9` wörtlich).

Zählung §5.9 zweimal unabhängig nachgerechnet: 73 Zeilen; Etiketten heute rot 31, heute nicht messbar 16, Regressionswache 16, erlaubtes Verhalten gemessen 6, zwei Hälften 4 (M‑46, M‑63, M‑69, M‑71) — Summe 73, jede Zahl trifft. Gegenfallzeilen 23 verlangte + 25 weitere = 48, beide Listen ausgezählt. Zahlenränder 11. Befundzuordnung deckt alle 73 Zeilen ohne Doppelung. Die vier „zwei Hälften"-Zeilen sind keine Umgehung: jede Hälfte nennt ihr eigenes Etikett und ihre eigene Mutation. Bauplan §6 gegen §5 abgeglichen: jedes in §5 genannte Bein steht in der Etappe seiner Zeile (Etappe 2 A36/A17/A14/B8/B10/A18, Etappe 3 A4b/A35 plus A1/B14/B8 nach A‑3, Etappe 4 B2/A12/B10/A4/A4‑SI/B22/A36); keine Etappe zieht eine spätere heran (A36 entsteht in Etappe 2 und wächst in Etappe 4 um `--nicht-gelaufen`, A‑6 genehmigt); Doppelberührungen von `tools/beweise.ps1` (Bauschritt/Baustand in 2, nur `:618` in 3, NOT RUN und `:725` in 4) und `docs/plugin-wissen.md` (`:872-877`/`:914` in 2, `:879-886` in 3) sind disjunkt; voller Kanon steht am Ende der Etappe 4 und im Abschluss (§6.6), der Laufzeit-Arm erst nach Etappe 3. §8.4 gegen §9 geprüft: keine der elf Abweichungen überschreitet ihre Grenze — `--broker-pin` schreibt laut F‑8 und M‑15 nur das Feld `sha256` des Broker-Artefakts und nur bei Abweichung; A‑3 hält Gens v2-Namen und Verhalten; A‑7 lässt die Nakama-Seite mit einem alten Controller nicht still falsch zuordnen, sondern über M‑42 `UNGEMESSEN` mit Szenario-Exit 5 und Runner-Exit 3; A‑10 bleibt über §6.1 Schritt 5 aus jedem Commit.

## nicht geprüft

Nicht ausgeführt und deshalb nur gelesen: alles, was einen Lauf verlangt — kein `cmake`, `cargo`, `pytest`, `py`, kein Kanon, kein Selbsttest, kein FL und kein Broker; ob die geplanten Tests wirklich rot werden, ob die vorgeschlagene Ableitung aus `CL.read`/`link.read` in der Praxis für alle 40 Ziele trägt und ob die Rotbeweis-Mutationen bytegleich zurücknehmbar sind, kann erst die Erstprüfung je Etappe messen. Vom Baubaum habe ich nur die Existenz des `tlog`-Ordners, die `generate.stamp.depend`-Einträge und die zwei Zeitstempel gelesen, nicht die Inhalte der Tracking-Logs (die 21 `^`-Zeilen, `NAKAMAKERN.LIB` in `link.read.1.tlog` von B6, `Lib-link.write.1.tlog`) — diese Belege aus §7.1 sind übernommen, nicht nachgemessen. Nicht Prüfgegenstand und daher nur als Kontext gelesen: die Quellvalidierung Teil A und Teil B, `BEFUNDE.md` samt `roh/phase-13-bauhostkette.md`, Register und fremde Manifeste über die zitierten Zeilen hinaus, M‑74 und NB‑309‑1/NAK‑317 (§9 nimmt sie ausdrücklich aus), die Sicherheitsbefunde T3‑12‑*, sowie Prosa, Historie und Zeilenverweise in `docs/**` außerhalb der inhaltlichen Zusagen (pruefliste E: „Kein Prüfer erhebt darüber Befunde"). Im Nachbarrepo habe ich nur die vier im Auftrag genannten Dateien und `.gitignore` gelesen, keine Testläufe.
