URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).

# NAK-312 Etappe 4 — Erstprüfung (S25k, Prüfstufe T2)

Frische, lesende Prüfsession (Claude Opus 5), 21.09.2026, Bericht geschrieben ab 13:33 Uhr (`date`). Auftrag `docs/beweise/roh/NAK-312-etappe-4-erstpruefung-auftrag.txt`. Prüfbereich `git diff a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c...33ef53fc64499d80d198c734eef7811f5ebd7927` über die sieben Pfade des Auftrags: 7 Dateien, +1082/-27 (`git diff --stat`), vollständig gelesen. Der Code am HEAD ist der des Zielstands: `git diff --stat 33ef53fc HEAD` zeigt allein den Prüfauftrag, `git diff --stat HEAD -- eq-copilot/plugin tools/beweise.ps1` ist leer. Alle Zeilenangaben gelten am Zielstand. Referenz: Manifest §2 (R-312-2, R-312-7), §5.0, §5.5, §5.6, §6 Kopf und §6.4, §7.0, §7.1, §7.4, §9.2, §11.3, §14.4, §27; als Kontext gelesen: Rotbeweise `NAK-312-rot-M-31/-M-34/-M-35/-M-36/-M-37/-M-38/-NAK-345.txt`, Gegenprobe `NAK-312-etappe4-gegenprobe.txt`, Kanon-Rohlauf `NAK-312-553b70a-dirty.md`, Register NAK-345 in `docs/offene-punkte.md`.

## Befunde

**DEFEKT: keiner.**

### L-1 (LÜCKE) — unbestätigter Text im Popover, wenn das Editorfenster vor dem Popover schließt (§27.9)

Wirkung am Code: `MesspunktPanel::uebernehmen()` kehrt ohne Zugriff zurück, sobald `editor.getComponent()` null ist (`eq-copilot/plugin/src/PluginEditor.cpp:730-732`); der Panel-Destruktor ruft es (`:710`), ebenso der Fokusverlust (`:746`). Schließt der Host das Editorfenster mit offenem Popover, nimmt `~Component` des Editors die CallOutBox als Kind heraus, `ModalItem::componentBeingDeleted` bricht den Modalzustand ab, und Box samt Panel sterben erst in `ModalComponentManager::handleAsyncUpdate` (JUCE-Quelle im Bauverzeichnis: `juce_Component.cpp:275-280`, `juce_ModalComponentManager.cpp:70-90` und `:196-210`, `juce_CallOutBox.cpp:45` und `:62-93`). Auch `textEditorFocusLost` kommt nur über eine gepostete Nachricht (`juce_TextEditor.cpp:1978-1990`, `:2038-2040`). An beiden Stellen ist der Editor schon fort: ein getippter, weder mit Enter noch mit Fokuswechsel bestätigter Name oder Paarname wird verworfen, auch wenn der Prozessor noch lebt. Am BASIS übernahm `proz.setzeBindung` ihn in diesem Fall; bei schon zerstörtem Prozessor war derselbe Weg der Befund T3-04-03.

Einordnung als LÜCKE, nicht als DEFEKT:

- R-312-2 Satz 2 verlangt genau dieses Verhalten: der Rückruf „des Editors" ist „nach dem Ende seines Eigentümers … wirkungslos: kein Zugriff, keine Mutation, keine Dirty-Meldung"; Eigentümer des Panels ist der Editor (`PluginEditor.cpp:643-647`). Der Bauplan §7.4 schreibt dieselbe Form vor („kehrt ohne jeden Zugriff zurück, wenn der Editor weg ist").
- Den letzten Satz „Der Handgriff bleibt derselbe" setzt die Matrix in M-36 (Quelle „R-312-2 (letzter Satz)") und M-37 um: Ereignis „Panel schließen, DANN Editor, DANN Prozessor", Frist „`uebernehmen()` läuft im Destruktor des Panels, solange der Editor lebt". Dieser Weg ist unverändert (`:742` ruft dasselbe `setzeBindung` wie am BASIS) und grün (B15: eine Host-Dirty-Meldung mit, keine ohne Änderung). Rollenwahl, Enter und Fokuswechsel bei lebendem Editor übernehmen wie bisher.
- Den Fall „Editor fort, Prozessor lebt, Eingabe unbestätigt" regeln weder Gate noch Matrix noch Entwurf; M-35 deckt nur den Fall mit zerstörtem Prozessor. Ob FL beim Schließen des Fensters vorher den Fokus abgibt (dann übernähme der Fokusverlust noch bei lebendem Editor), ist nicht gemessen.

Produktwirkung: Wer im Messpunkt-Popover einen Namen tippt und dann ohne Enter oder Klick daneben das Pluginfenster schließt, verliert diesen Namen möglicherweise; bisher wurde er übernommen. Ob das eine Produktkarte braucht, entscheidet der Dirigent. Keine Nacharbeit.

### H-1 (HÄRTUNG) — B31-Behauptung „beide stop() liegen vor dem Schliessen": die Telemetriehälfte ist nicht sicher gemessen

`tools/beweise.ps1:749`. Sicher misst 312/M-34 (`eq-copilot/plugin/tests/SondeLebenslaufTestMain.cpp:639-646`) nur `controlV3.stop()` vor dem Schließen: `stopFristUeberschritten` Control = 1 setzt voraus, dass die Schranke erst nach Ablauf der Frist freikam. Für `telemetryV3.stop()` bleibt allein `abgewiesen == 0`, und das fängt eine Vertauschung nicht zuverlässig. Beleg: im Rotlauf M-34 (`docs/beweise/roh/NAK-312-rot-M-34.txt`, M-30-Lauf unter der Mutante) schloss die Schleuse vor `telemetryV3.stop()`, und der Abbauende-Haken sah trotzdem `abgewiesen 0`. Am Zielstand stimmt die Reihenfolge (gelesen: `eq-copilot/plugin/sonde/SondeProcessor.cpp:333` und `:334` vor `:343`), die Matrixmutation von M-34 fällt, und eine Vertauschung hätte keine schädliche Produktfolge (ein abgewiesenes Telemetrie-Hello ist neutral, `eq-copilot/plugin/core/ipc/TelemetryClient.cpp:397-404` und `:755-764`). Denkbar: die Behauptung auf „controlV3.stop() gemessen, telemetryV3.stop() nach Zeilenfolge" fassen oder den Telemetrieprovider ebenfalls anhaltbar machen. Keine Nacharbeit.

### H-2 (HÄRTUNG) — Hello- und Telemetrie-Hello-Provider sind nur gelesen, nicht gemessen

R-312-7 gilt allen Provider-Rückrufen; alle drei betreten die Schleuse vor dem ersten Besitzerzugriff (`SondeProcessor.cpp:191-193`, `:207-218`, `:224-226`). Schranke und Marke sitzen nach M-30 nur im Statusprovider (`:202-217`). Ein entferntes `betreten()` im Hello- oder im Telemetrie-Hello-Lambda bliebe in B31 und B7 grün: in keiner Geometrie der Beine läuft einer dieser beiden Provider nach dem Ende des Eigentümers, und `betreten` steigt in 312/M-33 und 312/M-38 schon durch den Heartbeat des Statusproviders. Von keiner Zusage verlangt (die Matrix legt die Schranke ausdrücklich in `v3Status()`). Keine Nacharbeit.

## Prüfpunkte des Auftrags

### (a) Besitzschleuse der Sonde

- **Deklaration.** `callbackSchleuse` steht in `eq-copilot/plugin/sonde/SondeProcessor.h:700`, `providerHaken` (Testbau) in `:704`, `controlV3` in `:706`, `telemetryV3` in `:707`, `dspKern`, `dspAusfuehrung` und `transaktion` in `:718-720`. Die Schleuse steht damit vor beiden Clients und vor den drei Heap-Mitgliedern, die §7.4 nennt. Früher deklariert sind weitere Mitglieder, die die Provider lesen (`zustand` `:603`, `zustandSchloss` `:605`, `hostKontextSchloss` `:609`, `hostBusName` `:611`, `analyseQueue` `:617`, `v3LogonSid` bis `v3Channels` `:682-687`), dazu die JUCE-Basis (Callback-Lock, `isSuspended`). Sie entstehen vor der Schleuse und sterben nach ihr. Das ist unschädlich: die Schleuse schließt im Destruktorrumpf vor jeder Mitgliederzerstörung, und ein Provider läuft erst nach `start()` (Produkt: am Ende des Konstruktorrumpfs, `SondeProcessor.cpp:299-300`, nach `transaktion` in `:235`; Test: `v3StartFuerTest()`).
- **Initialisierungsreihenfolge.** Die Liste `SondeProcessor.cpp:176-229` folgt der Deklarationsreihenfolge (`v3LogonSid`, `v3PipeName`, `v3RuntimeNonce`, `callbackSchleuse`, `providerHaken`, `controlV3`, `telemetryV3`, `parameterBaum`). Die Lambdas kopieren `s = callbackSchleuse` und `h = providerHaken` beim Bau von `controlV3` und `telemetryV3`, also nach deren Aufbau; keines fängt einen ungebauten `shared_ptr`. `callbackSchleuse` wird nur in `:185` gesetzt und nie neu zugewiesen (`git grep`).
- **Eintritt vor dem Besitzerzugriff.** Hello `:191-193`, Status `:207-218` (vor `betreten()` läuft nur der Testhaken `vorEintritt` auf eigenem `shared_ptr`), Telemetrie-Hello `:224-226`. `v3TelemetryHello()` fasst `controlV3.kopplung` (`:1433`) innerhalb des Zugs an. Der Zug lebt, bis der Rückgabewert gebaut ist.
- **Neutrale Werte beim Client.** Die Schleuse schließt erst nach beiden `stop()`, und `stop()` setzt `laeuft = false` als Erstes (`eq-copilot/plugin/core/ipc/ControlClient.cpp:102`, `TelemetryClient.cpp:295`). Ein abgewiesenes Hello endet an `sollAbbrechen` (`eq-copilot/plugin/core/ipc/controlclient/Verbindung.cpp:514-526`; sonst fiele es an `adresseGueltig`, `:527-535`, ohne Pipe). Ein Status-Default endet an `sollAbbrechen` mit `break` vor jedem Senden (`:851-855`). Ein Telemetrie-Hello ohne Kopplung endet an `! laeuft` (`TelemetryClient.cpp:397-404`) oder im Sendelauf am Kopplungsvergleich (`:755-764`: Zähler `kopplungswechsel`, Abbruch). Alles bleibt in der geteilten Laufzeit des Clients; kein Zugriff auf den Prozessor, keine Pipe, kein Senden.
- **Weitere Rückrufe.** Die leeren Argumente sind `beiAntwort`, `beiLinkStatus` und `beiVersionierterAntwort` (`ControlClient.h:356-363`) sowie `beiFrame` (`TelemetryClient.h:109-113`). Die Sonde setzt keinen weiteren Haken (`setzeP0Rueckmeldung`, `setzeReplayBeginHook`, `setzeKonfliktWiederholungHook`, `setzeAuftragAbgeschlossenHook` kommen unter `eq-copilot/plugin/sonde/` nicht vor). Es gibt genau die drei Rückrufe, alle durch die Schleuse.

### (b) Destruktor

- **Folge.** `SondeProcessor.cpp:321` Briefkasten, `:326-327` Listener, `:329-332` Worker-Join, `:333` `telemetryV3.stop()`, `:334` `controlV3.stop()`, `:343` `schliessen()`, `:344-347` Abbauende-Haken (Testbau); erst danach sterben die Mitglieder. M-34 hält.
- **Warten.** `schliessen()` wartet ohne Zeitgrenze, aber nur solange `aktiv > 0` (`controlclient/Schleuse.h:145-165`, selbst nicht Prüfgegenstand). Ein Sonde-Provider nimmt `zustandSchloss`, den Callback-Lock, `hostKontextSchloss` und über `kopplung` den `zustandMutex` des ControlClients. Der zerstörende Thread hält an `:343` keine davon, der Worker ist gejoint, und der JUCE-VST3-Wrapper nimmt den Callback-Lock nur in `process()` (`juce_audio_plugin_client_VST3.cpp:3890`), nie beim Zerstören. Die Wartezeit ist damit die Restdauer des laufenden Providers, wie M-32 zusagt. Die Stoppfristen sind unverändert (`eq-copilot/plugin/core` zwischen BASIS und ZIEL unverändert, gemessen); der begrenzte Stop bleibt begrenzt.
- **Reconnect.** Ladeweg `:1320-1321` und `prepareToPlay` `:414-415` rufen nur `reconnect()`. Die Schleuse kennt kein Öffnen (`Schleuse.h` setzt `geschlossen` nur auf true). M-33 hält; seine Vorbedingung „Schleuse offen" misst B31, „öffnet keine geschlossene" ist baulich ausgeschlossen (gelesen).

### (c) Callbackpfad und Audio-Thread

Im Callbackpfad kommen nur `betreten()` und das Verlassen des Zugs hinzu: Atomics, `notify_all` ohne Nutzersperre, keine Allokation. Warten gibt es allein in `schliessen()` auf dem zerstörenden Thread. `processBlock` ist unverändert; die Hunks in `SondeProcessor.cpp` betreffen nur Initialisierungsliste und Destruktor. Die Lebendprüfung des Panels läuft auf dem Nachrichtenthread; Gens Audiopfad ist unverändert (`eq-copilot/plugin/src/PluginProcessor.*` und `src/prozessor/` zwischen BASIS und ZIEL unverändert, gemessen). 312/M-38 misst die Sonde (A-45); die Gen-Hälfte seiner Zusage ist gelesen, nicht gemessen, denn B7 baut keinen Gen. Die Schleusenhälfte trägt nach E-312-11 keine eigene Mutation.

### (d) Lebendprüfung des Panels

- **Zugriffe.** Konstruktor `PluginEditor.cpp:658` (`editor->processor`): synchron in `zeigeMesspunkt()` aus dem eigenen Knopf, der Editor lebt. `uebernehmen()`: zuerst die Prüfung (`:730-732`), danach nur Panelmitglieder und die Testmarke (`:738-741`), dann `ed->processor.setzeBindung` (`:742`) und `geaendert` mit eigener Prüfung (`:777-781`). `comboBoxChanged` `:745` (`paarSichtbarkeit` fasst nur Panelmitglieder an, `:712-718`), `textEditorFocusLost` `:746`, `textEditorReturnKeyPressed` `:747` (nur Fokusabgabe), Destruktor `:710`, `resized` nur Mitglieder. Die Prüfung steht vor jedem Zugriff auf Prozessor und Editor.
- **„Lebender Editor ist lebender Prozessor".** Trägt für alle drei Aufrufer (Destruktor, Combo-Weg, Fokusweg): `AudioProcessor` verlangt das Ende des Editors vor seinem eigenen (`juce_AudioProcessor.cpp:65-66`), und im VST3-Wrapper hält die Editorsicht den Controller und dieser den Prozessor (`juce_audio_plugin_client_VST3.cpp:2521`, `:1599`, `:774`). In B15 stirbt der Editor vor dem Prozessor.
- **`SafePointer::getComponent()`.** Es ist `dynamic_cast<ComponentType*> (weakRef.get())` (`juce_Component.h:2407`). Im Rumpf und beim Mitgliederabbau von `~EqCopilotEditor` ist der dynamische Typ noch `EqCopilotEditor`: nicht null, der Prozessor lebt. In `~AudioProcessorEditor` und `~Component` liefert der Cast null, obwohl `masterReference.clear()` erst nach dem Kinderabbau folgt (`juce_Component.cpp:277-280`). Der Kommentar `:723-727` stimmt, ebenso der berichtigte Kommentar am Übergabepunkt `:766-772` (Box stirbt in der nächsten Runde; JUCE-Stellen wie in L-1).
- **Normaler Handgriff.** Unverändert: M-36 eine Host-Dirty-Meldung, M-37 keine Mutation und keine Dirty-Meldung (Kanon grün, gelesen). Die Wirkung aus §27.9 ist L-1.

### (e) Tests

- **Ordnung.** B31 ordnet über beobachtbare Zustände: Schranke gehalten, Schleusenstand `geschlossen` und `aktiv >= 1`, `lebt`, Rückkehr des zerstörenden Threads, `use_count`, Threadende (`SondeLebenslaufTestMain.cpp:412-592`). Zeitgrenzen sind nur Obergrenzen gegen einen hängenden Lauf; läuft eine ab, wird der Fall rot, nie grün. Die 2-s-Frist ist nur in 312/M-32 Bedingung (`:630-637`), der Zeile, die genau sie zusagt. Die Ungleichung `gewartetMs * 1000 <= destruktorUs - 2 s + 1 ms` folgt aus der Aufrundung in `Schleuse.h:161-164` und der monotonen Uhr in `stop()`; sie flattert nicht.
- **Kein Lauf in undefiniertem Verhalten.** Die Marke steht unmittelbar vor `v3Status()` (A-37; dessen erste Besitzerzeile ist `zustandSchloss`, `SondeProcessor.cpp:1355`) bzw. vor `ed->processor` (`PluginEditor.cpp:738-742`). In Gegenprobe und Mutanten kehrt der Rückruf nach dem Zählen ohne Zugriff zurück. M-31 hält vor `betreten()` (A-40), damit die Mitglied-Mutante nicht in das Verlassen des Zugs läuft. Der Fallschalter (`SondeLebenslaufTestMain.cpp:785-808`) greift nur mit Argument; der Kanon fährt ohne (`tools/beweise.ps1:749`, `Argumente=@()`), und eine Auswahl ohne Treffer endet rot (`geprueft > 0`, `:811`).
- **Umgeformte Mutationen.** A-40: Mitglied statt `shared_ptr`, Marke vor der Mitglied-Schleuse, 20 von 20 Zyklen an der Marke. A-41: vorgezogenes `schliessen()` gibt Frist 0 statt 1 an der Zusage „steigt um 1". A-42: `schliessen()` an den Anfang gibt Frist 0, die Matrixfolge „abgewiesen statt gewartet" zeigt M-39 Fall 3. A-43: die Matrixmutation fällt einfädig nicht, der entfernte Frühausstieg gibt Dirty 1. A-44: zusätzliches `isShowing()` verliert die Änderung, Dirty 0. Jede fällt an der Zeile, die die Zusage trägt. A-45: „mindestens 4000 Blöcke" deckt „4000 Blöcke" und verlangt zusätzlich einen Schleuseneintritt im Messfenster (`TransactionTestMain.cpp:5056-5083`); M-38 sagt damit nicht weniger zu als die Matrixzeile.
- **Testzugänge (gemessen an den Präprozessorzeilen).** Innerhalb `#if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` liegen `callbackSchleuseFuerTest` (`SondeProcessor.h:317-318`), `ProviderHakenFuerTest` mit `setzeProviderHakenFuerTest` (`:329-335`) und `setzeAbbauEndeHakenFuerTest` (`:340`), alle im Block `:300-522`; dazu `abbauEndeHakenFuerTest` (`:640-644`), `providerHaken` (`:701-705`), dessen Aufbau, Fang und Aufrufe (`SondeProcessor.cpp:186-188`, `:197-199`, `:202-206`, `:209-217`), der Abbauende-Aufruf (`:344-347`) und `testzugang::messpunktMarkeFuerTest` samt Abfrage (`PluginEditor.cpp:611-625`, `:738-741`). Außerhalb liegt nur das Produktmitglied `callbackSchleuse` selbst. Die Produktziele `EqCopilot` und `NakamaProbeeq` setzen das Define nicht (`eq-copilot/plugin/CMakeLists.txt:149-155`, `:350-357`); EqCopShot, B31 und B7 setzen es (`:1331-1338`, `:437-439`, `:1179-1181`). Der Fallschalter liegt allein im Testprogramm.
- **Produktions-Pipe.** 312/NAK-345 baut die Sonde aus dem Produktkonstruktor; im Testbau fehlt deren Startzweig (`SondeProcessor.cpp:295-304`), der Fall startet nichts und verlangt null Versuche beider Clients (`SondeLebenslaufTestMain.cpp:298-309`). Die übrigen Fälle prüfen den Probe-Namensraum vor Server und Start (`:418-430`, `TransactionTestMain.cpp:4992-4997`).
- **Heap.** Jede Sonde und jeder Gen liegt auf dem Heap (`SondeLebenslaufTestMain.cpp:298`, `:424`; `ShotTestMain.cpp:352`; `TransactionTestMain.cpp:5005`).

### (f) Behauptungen in `tools/beweise.ps1`

- B7 (`:728`): mindestens 4000 Blöcke wechselnder Größe, beide v3-Clients verbunden, Provider nachweislich durch die Schleuse, beide RtWache-Zähler 0 — jeder Teil hat seine Bedingung in 312/M-38 (`TransactionTestMain.cpp:5076-5077`).
- B15 (`:752`): Marke 0, Bindung bis zum Prozessorende unverändert, 0 Host-Dirty; eine Änderung mit genau einer Dirty-Meldung, ohne Änderung keine — deckt sich mit `ShotTestMain.cpp:427-452`.
- B31 (`:749`): deckt sich mit den Fällen, bis auf die Telemetriehälfte von „beide stop()" (H-1).

## Punkt 3 — Produktverhalten seit BASIS

Selbst gemessen (`git diff --stat` und `--name-only` BASIS..ZIEL): geändert sind allein die sieben Pfade des Prüfbereichs und die Installer-Hashes. Unverändert sind `eq-copilot/identity`, `eq-copilot/schemas`, `eq-copilot/plugin/state`, `eq-copilot/fixtures`, `eq-copilot/plugin/dsp`, `eq-copilot/plugin/core` (Clients, Stoppfristen, `Schleuse.h`), `broker`, `eq-copilot/plugin/CMakeLists.txt`, `src/PluginProcessor.*` und `src/prozessor/`. In `SondeProcessor.cpp` ändern die Hunks nur Initialisierungsliste und Destruktor; `processBlock`, Parameterlayout, Laden und Speichern sowie `v3Hello`, `v3Status` und `v3TelemetryHello` sind unverändert. Parameter-IDs, Reihenfolge, Bereiche, Identität, Stateversion und Audio-Thread bleiben damit unberührt. Gelesen: bei offener Schleuse liefern die Provider dieselben Werte wie am BASIS, Verbindungsaufbau und Reconnect sind unverändert; das Popover hat dieselben Bauteile, Texte und Maße (`PluginEditor.cpp:641-764` ändert nur Mitgliedstyp, Zugriffsweg und Kommentare). Nur gelesen, nicht nachgemessen: Kanon GRÜN 70/70 an `553b70ae` (B7 226 geprüft, 0 Fehler; B15 Panel 3 geprüft, 0 Fehler; B31 17 geprüft, 0 Fehler; A1, A3, A16, B2, B14 OK), die Rotbeweise, die Gegenprobe samt Bildgleichheit des Popovers und der Laufzeit-Arm an `dc2665ba` (Nulltest im Host bitidentisch). Die einzige Verhaltensänderung für den User ist L-1.

## Geprüft / nicht geprüft

**Geprüft:** der ganze Diff der sieben Pfade; am Zielstand die Deklarations- und Initialisierungsreihenfolge der Sonde, alle drei Provider, der Destruktor, Lade- und Vorbereitungsweg, die Verarbeitung der neutralen Werte in `ControlClient` und `TelemetryClient`, die Sperren eines Providers gegen den Destruktorthread, das Messpunkt-Panel mit jedem Zugriffsweg, das Verhalten von `SafePointer`, CallOutBox, ModalComponentManager und TextEditor an der JUCE-Quelle unter `eq-copilot/build/_deps/juce-src`, die Fälle 312/M-30 bis 312/M-39 und 312/NAK-345 in B31, B15 und B7 gegen Matrix §6.4 und die Abweichungen A-37 bis A-47, die drei Behauptungszeilen, die Lage jedes Testzugangs zum Testdefine und die Defines der Produktziele in `CMakeLists.txt`. Selbst gemessen habe ich nur mit Lesekommandos: HEAD, Diffs und Diffstatistiken, Präprozessorgrenzen und `git grep`.

**Nicht geprüft:** Ich habe nichts gebaut, keinen Test und keinen Kanon gefahren, FL nicht gestartet; Kanon, Einzelbeine, Rotbeweise, Gegenprobe, Bildgleichheit und Laufzeit-Arm stehen hier nur als gelesene Rohdaten. `controlclient/Schleuse.h`, Gens Schleuse, der Briefkasten und der JUCE-VST3-Wrapperpatch sind nicht Gegenstand; Schleuse.h und Wrapper habe ich nur gelesen, um Wartezeit und Sperren einzuordnen. Die übrigen CallOutBox-Rückrufe des Editors (Hinweisliste, Kennungskonflikt) gehören nicht zu T3-04-03 und sind nicht bewertet. Ob FL beim Schließen des Pluginfensters den Tastaturfokus vorher abgibt (L-1), ist nicht gemessen. Nicht Prüfgegenstand nach Auftrag: `docs/**`, die Installer-Hashes, die Etappen 5 bis 7, die Karten und die datierten Härtungen im Register. Fremde Ordner nicht berührt; im Repo ist dieser Bericht die einzige neue Datei; nichts gestagt, committet oder gepusht. Lange Diffausgaben hat die Werkzeugumgebung außerhalb des Repos zwischengespeichert.

HEAD zu Beginn: `9e01ecac12b7fd7da65d71a8a241c67ed4494f6e`. HEAD am Ende: `9e01ecac12b7fd7da65d71a8a241c67ed4494f6e` (13:33:33 vor und 13:35:16 nach dem Schreiben dieses Berichts, gleich). Beide mit `git rev-parse HEAD`; HEAD trägt über ZIEL hinaus nur den Prüfauftrag unter `docs/beweise/roh/`.

FERTIG Erstprüfung Etappe 4, PASS, 0 Defekte, 1 Lücken, 2 Härtungen.
