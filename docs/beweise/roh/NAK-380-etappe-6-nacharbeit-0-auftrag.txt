NAK-380 — Etappe 6, Nacharbeit 0 nach Regel R-380-14 (M-43 bei 96 kHz auf den Etappe-6-Stand; die Messfassung reist mit dem gespeicherten Fingerprint, M-122). Auftrag des Dirigenten an den Bauer (frischer Opus-5.5-Worker, Effort xhigh nach NAK-404), 26.09.2026. Runde 0, zählt nicht zum Rundenbudget.

ROLLE. Du bist der Bauer der Nacharbeit 0 der Etappe 6 des Tickets NAK-380 (Planschritt S25q) im Repo C:\Users\phili\Projekte\Nakama (Branch master, sichtbarer Checkout, Windows 11; Shell pwsh 7 über `pwsh -NoProfile -Command`, kein cmd, kein PowerShell 5.1). Du schreibst nur unter dem Repo-Wurzelverzeichnis; du stagest, committest und pushst NIE (kein git add, kein git commit, kein git push, kein git stash, kein git checkout/restore/reset auf geänderte Dateien, kein Verwerfen fremder oder eigener Änderungen über git); der Dirigent committet deinen Stand. Die fremden untracked Einträge briefing-hub/ und nimbalyst-local/ fasst du nie an. Etappe 6 ist gebaut und als Zwischenstand committet (Manifest docs/beweise/NAK-380.md §54 Bauer, §55 Dirigent mit der Regel R-380-14, Einordnung §55.1 und Matrixfassung §55.2). Du baust genau das, was §55.2 R-380-14 (i) und (ii) nennen, nichts sonst. Du kompilierst, fährst die genannten Beine einzeln und die Rotbeweise selbst; den vollen Kanon fährt der Dirigent; ein frischer Prüfer prüft danach; nie du selbst.

WÄCHTER (NAK-354). Jeder Bau- oder Testlauf über zwei Minuten läuft als Hintergrundbefehl mit Log-Datei unter eq-copilot/build/ (nicht im Repo-Index, nie im Repo-Root); du setzt einen Wächter auf das Log (Monitor oder Bash run_in_background mit until-Schleife auf das Endkennzeichen). Läuft ein Wächter ab, setzt du sofort einen neuen. Bei jeder Rückkehr aus dem Warten liest du zuerst die letzten Zeilen des Logs, bevor du irgendetwas anderes tust.

VERBINDLICHE QUELLEN, in dieser Reihenfolge lesen (abschnittsweise mit Zeilenbereichen, nie ganz):
1. docs/beweise/NAK-380.md: §55 vollständig (ab Zeile 3317: Messung, §55.1 Einordnung, §55.2 Regel R-380-14 (i) und (ii) mit Matrixfassung, §55.3 Ticketpfade und Beine), §54.13 Punkte 1 und 2 (ab 3276) und §54.11 Punkt 4 (ab 3158), §6.3 Zeile M-43 (933) und §6.5 Zeile M-122 (1048), §8.1 (Rotbeweis-Ablauf, 1476-1556), §2 R-380-9 über die Verlaufsdatei §12 (docs/beweise/NAK-380-verlauf.md ab Zeile 68, E-380-10), Kopf Zeilen 1-25 (Gate wörtlich, Grenze). Lehren: Verlaufsdatei §24 (ab 1466), §31 (ab 2335), §50.1 im Manifest (2220): Rotbeweis an der Zusagezeile, Doppelmutation bei doppelt getragener Zusage, Wache ohne Trennschärfe ist ein Defekt.
2. CLAUDE.md (Invarianten: State bleibt verlustfrei — unbekannte Felder erhalten, Altstände laden; Maschinen-Landminen: NAK-230 Zeitstempel, NAK-300 vctip.exe, Zeilenenden je Datei nach `git ls-files --eol`, keine Backticks in Shell-Befehlen, typografische Anführungszeichen in Rust-Strings als U+201C, Pipe-Zeichen in Markdown-Tabellenzellen als \|) und tools/dirigent/pruefliste.md (Arbeitsliste, nicht ändern).
3. Code am HEAD (vom Dirigenten gemessen): eq-copilot/plugin/tests/Sonde013DynamicsTest.cpp (M-43-Fall ab :1047: Soll-Tafel :1054 `{ 44100.0, 3, 1666 }, { 48000.0, 3, 1530 }, { 96000.0, 2, 765 }`, Kommentar :1052 „96 kHz: Delta f = 23,4375 Hz, k = ceil(1,295) = 2 bis 766, K = 765“, Ringtafel :1071-1081 `{ 96000.0, 766, 58, 118u }`, Prüfzeilen :1064, :1090-1098, Allokationswache :1134; Helfer :285-300); broker/src/coordinator/experiment_verdrahtung.rs (`fingerprint_json` :660-667 mit version, band_energie, chroma, onset; Belegschreiber „fingerprint“/„passage_fingerprint“/„upstream_fingerprint“ :679-686; `beurteile`-Aufruf :1233 mit `beleg (…)`; `fingerprint_aus_wert` :1333 (Leitung, experiment_begin/candidate); `fingerprint_aus_gespeichertem` :1604; Passage/Referenz aus gespeichertem :1626-1645; der Kommentar zur Projektion `experiments.state_jcs` :640-658); broker/src/coordinator/invalidierung.rs (`material_wechsel` mit `let fassung = super::vergleichbarkeit::METRICS_VERSION;` auf beiden Seiten, `material_urteil (Option<(&Fingerprintwerte, u32)>, …)`, `Materialurteil::{Gleich, Wechsel, NichtVergleichbar}`); broker/src/coordinator/vergleichbarkeit.rs (`Passagenbeleg` :101-118 ohne Fassung, `beurteile (a, b)` → `beurteile_versioniert (a, METRICS_VERSION, b, METRICS_VERSION)`, `Herabstufungsgrund::MessfassungVerschieden`, `METRICS_VERSION = 20260929` :149); broker/src/telemetrie.rs (`fingerprint_vergleich (a, fassung_a, b, fassung_b)` → `FingerprintVergleich::{Aehnlichkeit, NichtVergleichbar}`; Modultest `nak380_m122_fingerprint_nur_gleiche_metrics_version`); tools/beweise.ps1 B18 :766, A4 :518. Miss jede Stelle am HEAD nach, bevor du änderst (die Zeilen gelten zum Zwischenstand-Commit dieser Nacharbeit).

TICKETGRENZE. Du änderst nur:
- eq-copilot/plugin/tests/Sonde013DynamicsTest.cpp: die 96-kHz-Hälfte von M-43 nach R-380-14 (i): Soll `{ 96000.0, 3, 1530 }`, Ring `{ 96000.0, 1532, 115, 232u }`, Kommentare (Δf 11,71875 Hz aus 8 192 Punkten, k = ceil(30,36/11,71875) = 3 bis 1532, w_max = ceil(1532·(2^(125/1200) − 1)) = 115, 2·115 + 2 = 232; Speicher 2·1530·8 = 24 480 B ≤ 26 656 B als eigene Prüfung, falls der Fall sie nicht schon trägt); die Sätze zu 44,1 und 48 kHz bleiben wörtlich; kein anderer Fall.
- broker/src/coordinator/experiment_verdrahtung.rs nach R-380-14 (ii): Schreiber trägt neben jedem gespeicherten Fingerprint die Messfassung (`metrics_version` = `vergleichbarkeit::METRICS_VERSION` des schreibenden Brokers; additives Feld im gespeicherten JSON, Ort und Name sind dein Technikentscheid — im Fingerprint-Objekt oder daneben im Beleg —, begründet in §56; kein Feld auf der v3-Leitung: `fingerprint_aus_wert` für Leitungsnachrichten bleibt, ein frisch angekommener Fingerprint trägt `METRICS_VERSION`); Leser liefert die gespeicherte Fassung als `Option<u32>` (fehlt das Feld → `None` = unbekannt; kein Rückfall auf `METRICS_VERSION`); die zwei Aufrufer (`beurteile`-Aufruf :1233 und der Weg zu `material_wechsel`/`material_urteil`) übergeben je Seite die Fassung des Belegs (gespeichert → gespeicherte Fassung; laufend → `METRICS_VERSION`); unbekannte Fassung auf einer Seite → „nicht vergleichbar“ fail-closed (kein Materialwechsel, nie `MaterialVerschieden`; Grund `MessfassungVerschieden` oder ein eigener benannter Grund „Messfassung unbekannt“ — Technikentscheid, begründet). Modultests `nak380_m122_gespeicherte_fassung_*` nach §55.2 (a) bis (d).
- broker/src/coordinator/invalidierung.rs und broker/src/coordinator/vergleichbarkeit.rs nur, wenn (ii) eine Signatur (`Option<u32>` je Seite) oder einen benannten Grund braucht; `METRICS_VERSION` bleibt 20260929, kein weiterer Versionsschritt (die Projektion ist kein Vertrag).
- broker/src/telemetrie.rs nur Kommentar zu `fingerprint_vergleich`, falls die Herkunft der Fassung dort beschrieben ist.
- tools/beweise.ps1 nur die Behauptungszeilen B18 :766 und A4 :518, je ein Satz mit gemessenen Zahlen (CRLF mit BOM erhalten).
- Rohdateien: docs/beweise/roh/NAK-380-rot-M-43.txt und NAK-380-rot-M-122.txt append-only (neuer Abschnitt „Nacharbeit 0 der Etappe 6 (e6n0)“), Mutationsprotokolle docs/beweise/roh/NAK-380-e6n0-mutation-<Kürzel>.txt (m43-untergrenze, m43-ring2w, m122-leser-rueckfall, m122-aufrufer-fassung, m122-schreiber-ohne-feld, dazu Doppelmutationen und Gegenproben, wo eine Zusage zwei Riegel trägt).
- docs/beweise/NAK-380.md append-only als §56 (Startmessung, Schließung je Punkt (i) und (ii) mit Datei:Zeile, Änderungen je Datei mit Zeilenzahlen vorher/nachher, Tabelle je berührter Matrixzeile M-43 und M-122 nach der Auftragsregel: Zusagesatz → Prüfzeile → Mutation → rot JA/NEIN → Rohdatei, je Satz und Hälfte eine Zeile; Bewegungen bestehender Tests mit Zahl (etwa exakte JCS-Vergleiche im Experimentmodul); einzeln gefahrene Beine mit Exit, Zählerstand und Binaryzeitstempel; Logvergleich B18 vorher/nachher (Prüfzeilen zeichengleich außer M-43 bei 96 kHz); Abweichungen begründet; Selbstaudit; offene Punkte); Kopf: Zeile Etappe bekommt als letzten Fettsatz „Nacharbeit 0 gebaut; Kanon und Erstprüfung 6 stehen aus“ (höchstens 70 Zeichen; die übrigen Sätze bleiben). Manifest LF, Pipe-Zeichen in Tabellenzellen als \|. Dokuriegel `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-380.md` ohne Befund (Rohdateien vor dem Dokuriegel anlegen).
Nicht angefasst: jeder andere Produkt- oder Testpfad (insbesondere FeatureEngine.h, Spektrum.h, AnalyseEngine.*, die Etappe-6-Tests in B5/A2/B30/B15, Nak380Pruefsignale.h, metriken-v1.json, festhalten-referenz.json, der Entwurf), eq-copilot/fixtures/** (v3-Fixtures bleiben bytegleich, die Leitung ändert sich nicht), eq-copilot/schemas/**, broker/src/store/** (keine Migration; der Store hält JSON, unbekannte Felder bleiben erhalten), broker/tests, .claude/**, design/**, docs/PLAN-STAND.md, docs/offene-punkte.md, docs/plan/**, docs/beweise/NAK-380-verlauf.md, die fremden Einträge. Kein FL Studio, keine Installation, kein Laufzeit-Arm, keine Produktions-Pipe, kein voller Kanon, kein paralleler zweiter Kanon (NAK-290).

PRÄZISIERUNGEN DES DIRIGENTEN (Lehren §24, §31, §36.1, §39.1, §42.1, §47.1, §50.1 gelten für jede Zeile):
- Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Eine Zusage, die zwei Riegel tragen (etwa Schreiber und Leser für „die Fassung reist mit“; Riegel im Leser und Riegel im Aufrufer für „unbekannt → nicht vergleichbar“), bekommt eine Doppelmutation als Rotbeweis und je Einzelmutation eine Gegenprobe; beide Riegel im Testkommentar benannt (Lehre §50.1).
- Vorbedingung hergestellt und verriegelt: der Test baut den gespeicherten Beleg selbst (JSON mit und ohne Feld, Fassung 20260928 als Altstand, 20260929 als laufende Fassung), verriegelt vor der Prüfung, dass die zwei Fingerprints numerisch unähnlich sind (Cosinus unter dem Gate, wie im bestehenden M-122-Test), damit „nicht vergleichbar“ von „gleich“ trennscharf unterscheidbar ist; jede Zahl mit Herleitung im Kommentar; nie ein Wert aus der Engine als Referenz.
- Keine Wache ohne Trennschärfe: (c) gleiche Fassung ergibt bei den unähnlichen Fingerprints Materialwechsel bzw. `MaterialVerschieden` (Wache); (a) und (b) ergeben „nicht vergleichbar“; der Unterschied ist in jedem Fall am Ergebniszustand gemessen, nicht an einem Nebeneffekt.
- Zusage je Element: alle drei gespeicherten Fingerprintfelder (passage, passage_fingerprint, upstream_fingerprint) tragen die Fassung und werden gelesen; beide Aufrufpfade (Vergleichbarkeit und Invalidierung) übergeben sie.
- Rotbeweise formgültig (keine Panik, kein Index außerhalb); Mutation im Produktcode (Leser, Aufrufer, Schreiber), nicht im Test; Rücknahme per SHA-256 mit Sicherung unter eq-copilot/build/nak380-sicherung/ (nie über git), `(Get-Item <datei>).LastWriteTime = Get-Date` nach dem Zurückkopieren (NAK-230). Rust: `cargo test --manifest-path broker/Cargo.toml <filter>` für den Rotbeweis, voller A4 am Ende (`--color never`), `git diff --check` ohne Befund, `cargo fmt --check` ist kein Befund.
- Keine neue Zusage über die Regel hinaus: keine Persistenz der Fassung auf der Leitung, kein Schemafeld, keine Store-Migration, kein Versionsschritt.
- Bauen C++: & 'C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' --build eq-copilot/build --config Release --target EqCopSonde013DynamicsTest (B18); Binaryzeitstempel gegen die Quelle prüfen (NAK-230); B5, A1, A16, B1, B4 nur fahren (Binaries vom Etappenendbau, jünger als ihre Quellen; falls nicht, neu bauen). Prozessprobe vor dem ersten Bau (`Get-Process cmake, MSBuild, cl, link, cargo, rustc, EqCop*, eqcop-broker, FL64, vctip -ErrorAction SilentlyContinue`). Bleibt ein Bauschritt ohne Kindprozess stehen, beende vctip.exe (NAK-300).
- Zeilenenden: Sonde013DynamicsTest.cpp, prepost.rs, vergleichbarkeit.rs, telemetrie.rs, tools/beweise.ps1 CRLF (w/crlf); invalidierung.rs, experiment_verdrahtung.rs (`git ls-files --eol` prüfen), Manifest und Rohdateien LF. Jede Datei mit genau den Zeilenenden ihrer Arbeitskopie zurückschreiben.

ABLAUF.
1. Start: `Get-Date -Format 'yyyy-MM-dd HH:mm:ss'`; `git rev-parse HEAD`; `git status --short` (nur die fremden Einträge); Prozessprobe; Startmessung in §56.
2. (i) M-43: Soll- und Ringtafel für 96 kHz nachziehen, Kommentare berichtigen, B18 bauen und fahren (127/0 erwartet: 125 + 2), Rotbeweise `m43-untergrenze` (FeatureEngine.h, Untergrenze k = 0 → K = 1533 bei 48 und 96 kHz → die K-Prüfungen beider Raten rot) und `m43-ring2w` (Ringgröße 2·w_max statt 2·w_max + 2 → Ringprüfung bei 96 kHz rot) nach dem Muster der Etappe-4-Protokolle `NAK-380-e4-n0-mutation-m43-*.txt` (lesen), Rücknahme per SHA-256, B18 grün; Logvergleich B18 vorher (Etappenendlauf, `eq-copilot/build/nak380-e6/` falls vorhanden, sonst frischer Lauf am Zwischenstand vor deiner Änderung) gegen nachher.
3. (ii) Fassung im gespeicherten Fingerprint: Schreiber, Leser, Aufrufer; Modultests (a) bis (d); `cargo test` grün; Rotbeweise nach §55.2 (Leser-Rückfall, Aufrufer mit `METRICS_VERSION`, Schreiber ohne Feld; Doppelmutationen und Gegenproben, wo zwei Riegel tragen); voller A4; bestehende Experimenttests: bewegen sich exakte JSON-Vergleiche der Projektion durch das additive Feld, ziehst du sie nach und nennst jede Bewegung mit Zahl in §56 (die Projektion ist kein Vertrag); bewegt sich ein v3-Fixture oder ein Test der Leitung, ist das ein Befund an deiner Änderung.
4. Beine einzeln: B18, A4 (voll), B5, A1, A16, B1, B4; Exitcodes, Zählerstände, Binaryzeitstempel in §56.
5. Selbstaudit: den eigenen Diff (`git diff --stat` und `git diff` gegen HEAD, ohne zu stagen) adversarial lesen — keine Änderung außerhalb der Ticketpfade, kein Leitungsfeld, kein Rückfall auf `METRICS_VERSION` im Leser, beide Aufrufpfade, alle drei Fingerprintfelder, Altstand ohne Feld lädt weiter (State verlustfrei) und ist „nicht vergleichbar“, gleiche Fassung unverändert, 44,1/48-kHz-Sätze von M-43 wörtlich, Zeilenenden. Prüfliste abhaken (Datei nicht ändern).

SCHLUSSMELDUNG, höchstens zwölf Zeilen: HEAD, Uhrzeit Start und Ende, geänderte Dateien (`git status --short`, uncommittet), je Punkt (i) und (ii) „JA“ oder „NEIN“ an der Zusagezeile mit Prüfzeile, Beine mit Exit und Zählerstand, Bewegungen bestehender Tests mit Zahl, Abweichungen, offene Punkte, Dokuriegel-Ergebnis. Beende dich mit einer Zeile: FERTIG Nacharbeit 0 Etappe 6, Beine <alle grün oder Liste>, uncommittet.
