# NAK-380 — Matrixprüfung 2, Validierung der vier Befunde an der Quelle (Codex gpt-6-astra, Effort high nach NAK-396, lesend)

**Thread:** 01a0d6f6-a77e-7720-ba3d-e4cb5ebd45b6 · **HEAD vor und nach dem Lauf:** d86a84fa88b6ca7c3917f44c2c3efa9a008ac530 (Matrixstand cf33f554; darüber §14, Prüfauftrag 2, Rohurteil 2 und dieser Auftrag) · **Lauf:** 25.09.2026, 07:08:03 bis 07:16:14 Uhr (+02:00, aus der Startdatei des Laufs `tools/dirigent/codex-lauf.ps1`, Kennung `nak380-m2v`, EXIT=0; Modell und Effort aus der Startzeile des Laufs, der Bericht selbst nennt die Modellkennung nicht) · **Gegenstand:** die vier Befunde B1 bis B4 aus `docs/beweise/roh/NAK-380-matrixpruefung-2-urteil.md` (Thread 01a0d6ec) gegen `docs/beweise/NAK-380.md` §5 bis §9 und den Code an HEAD · **Auftrag:** `docs/beweise/roh/NAK-380-matrixpruefung-2-validierung-auftrag.txt` · **Ablage:** letzte Nachricht des Validierers (read-only-Sandbox), unverändert vom Dirigenten abgelegt; 16 lesende Kommandos. Ergebnis: 3 bestätigt, 1 präzisiert (B2), 0 widerlegt. Die genannte untracked Datei `.claude/settings.local.json` ist global ignoriert (siehe Validierung 1); kein Befund.

---
NAK-380, 25.09.2026, Codex (genaue Modellkennung nicht verfügbar), lesende Quellvalidierung des Diffs 8d6444c7..cf33f554: HEAD zu Beginn und Ende d86a84fa88b6ca7c3917f44c2c3efa9a008ac530 auf master; git status --short beide Male unverändert mit ausschließlich ?? .claude/settings.local.json, ?? briefing-hub/ und ?? nimbalyst-local/; Git meldete zusätzlich fehlenden Zugriff auf die globale Ignore-Datei, keine Dateien wurden geändert. Die Nacharbeit muss Ursache (c) behandeln: B1/B2 übernehmen ein Wache-Etikett ohne passenden Basisbeleg, B3/B4 vermischen Erstaufnahme, unveränderte Referenz und erlaubten Referenznachzug; weitere nachweislich rote Wachen wurden nicht geprüft, bei den zusätzlich gelesenen Golden-Hälften M-12, M-28(a), M-86 und M-118 wurde keine weitere fehlende Ausgangsreferenz oder unzulässige M-28-Ausnahme festgestellt, Builds, Tests, Skripte, Kanon und FL Studio wurden auftragsgemäß nicht ausgeführt. FERTIG Validierung Matrixprüfung 2, 3 bestätigt, 1 präzisiert, 0 widerlegt.

Full review comments:

- [P2] Ordne beide Leistungszusagen von M-08 als heute rot ein — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-380.md:871-871
  B1 — BESTÄTIGT, DEFEKT; kein Ausschluss des Prüfauftrags. M-08 verlangt „(b) Trägerrandwache: Gruppe 35 = −3,01 dBFS ± 1,0 dB“ und etikettiert diese Hälfte als „Regressionswache“. Das Livegitter `nakama_log64_v1.json:225-228` nennt `"fein_von": 120` und `"fein_bis_exkl": 124`. Zuständig ist die Hauptstufe mit N = 4096 (`FeatureEngine.h:378,383,469-472`), also Δf = 11,71875 Hz. Aus den Gitterkanten und `Zeit.h:139-143` folgen die Binbereiche 83–85, 86–87, 88–90 und 91–93: 3/2/3/3 Bins; der Ton liegt in Feinband 122. Das periodische Hann hat Σw² = 1536 (`FeatureEngine.h:1118-1120`); `Spektrum.h:426` verwendet `const double norm = 2.0 / (s.fs * s.fensterEnergie);`, anschließend gilt `energie /= (double) (bis - von);` (:494). Die analytischen Banddichtemittel sind ungefähr 2,7·10⁻⁷, 0,004795, 0,011025 und 1,8·10⁻⁷. `Frame.h:347` addiert sie mit `summe += liveAkku[(std::size_t) b].summe / n;`: ungefähr 0,01582, logarithmiert −18,01, auf der Leitung −18,0 dB statt erlaubter −4,01 bis −2,01 dB. Die Berichtsschranke −13,70 dB ist ebenfalls konservativ gültig, aber nicht der tatsächliche Näherungswert. Verletzt wird „Regressionswache: heute grün“ ([§6](docs/beweise/NAK-380.md#L781-L790)). Schließende Regel: Beide Leistungswerte gehören zur roten Hälfte, nur Bereich, Bits, Sättigung und Lesbarkeit zur Wache; §8.1/§8.2 mitziehen. Bei weiterhin zwei Hälften bleibt die M-08-Zählung in §6.6 unverändert.

- [P2] Trenne D7s roten Basisfall von der Nachher-Binmutation — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-380.md:990-990
  B2 — PRÄZISIERT, DEFEKT; kein Ausschluss. M-81 verlangt „Präsenzbit gesetzt“, behauptet aber „Regressionswache: der bestehende Phasepfad ist grün für bekannte Verzögerung“. Die Kanten 15544,511… und 16000 Hz ergeben mit `Zeit.h:139-143` die Bins 1327–1365, also 39; der nächste Mittenbin ist 1346. Damit stimmen 19 Bins Abstand und Δφ = 2π·19·48/4096 = 1,39899 rad. Die heutige Bandsumme (`Stereo.h:75-79,87-90,229-230`) ergibt bei gleichen Binleistungen C = [sin(39θ/2)/(39·sin(θ/2))]² ≈ 0,4765 mit θ = 2π·48/4096. Das ist eine analytische Näherung, kein exakt nachgewiesener Wert des endlichen D7-Saatlaufs; Hann-Fensterung und Stichprobengewichte sind darin nicht vollständig enthalten. Entscheidend ist `if (koh > kKohaerenzSchwellePhase)` (:236), bei `kKohaerenzSchwellePhase = 0.8` (`FeatureEngine.h:186`): Das Phasenbit bleibt im Modell aus. Präzisierung der Bits: `basisGesetzt = true` (:203) bleibt möglich; das Kohärenzbit wird bei mindestens acht Frames auch unter 0,8 gesetzt (:225-234). Mit unverändertem Standardintervall 0,25 s und Block 512 hat der letzte Snapshot vor 10 s sogar nur sieben Frames: ⌊476160/2048⌋ − ⌊460800/2048⌋ = 7, also weder Kohärenz- noch Phasenbit. Verletzt werden „Regressionswache: heute grün“ (§6:786) und der erforderliche Vorher-Beleg für rote Fälle ([§8.1](docs/beweise/NAK-380.md#L1491-L1497)). Schließende Regel: D7 als heute rot führen, den Basisfall dokumentieren und die Erstbinmutation separat nach dem Bau behalten; D7 (§7.2), §8.5 und §6.6 mitziehen, letztere dann 68/20/43 statt 67/20/44.

- [P2] Sichere M-68s Ausgangsbytes vor dem Etappenumbau — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-380.md:1521-1523
  B3 — BESTÄTIGT, DEFEKT; kein Ausschluss. Der neue Satz lautet „die bestehenden Fingerprint-Bytes von M-68 bleiben als Klasse-(i)-Wache unverändert und werden nicht erzeugt“. Eine eingefrorene Hexreferenz für F1, den 30-s-Akkord, wurde in den untersuchten Quellen nicht gefunden. `Sonde013FingerprintGoldenTest.cpp:186-187` berechnet sowohl `a1` als auch `a2` durch `fingerprintVon (akkord (0.4, 220.0))`; :188 prüft `a1.gesetzt && a2.gesetzt && a1 == a2`. Ebenso wird `f` in :227 frisch berechnet und in :283-284 mit dem frisch berechneten Vorzeichenbild verglichen: `umgekehrt.gesetzt && umgekehrt == f`. Diese Fälle verwenden 400 Blöcke à 512 Samples, also 4,2667 s, nicht 30 s. Die vorhandenen synthetischen Wire-Fingerprints sind keine F1-Ausgangsreferenz. Der Diff `8d6444c7..cf33f554` entfernt ausdrücklich „am Etappenstart mit dem unveränderten Code erzeugten Golden (hex im Test, Erzeugung im Etappenabschnitt)“ aus M-68 und die entsprechende Erfassung aus §8.1. Damit fehlt der zugesagten Bytegleichheitswache ihr Vergleichsanker; zwei Berechnungen mit demselben veränderten Code erkennen gemeinsame Änderungen nicht. [E-380-17](docs/beweise/NAK-380.md#L2290-L2296) verlangt „Golden oder Wert bleibt unverändert“, nicht den Verzicht auf dessen erstmalige Sicherung. Schließende Regel: Ausgangsbytes einmal vor dem Umbau erfassen und danach unverändert vergleichen; nur der Nachzug bleibt verboten, die Mutation bleibt Rotbeleg. Folgestellen: M-68, F1, §7.5, §8.1 und §8.4.

- [P2] Begrenze die LRA-Toleranzausnahme auf M-28(b) — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-380.md:1680-1685
  B4 — BESTÄTIGT, DEFEKT; kein Ausschluss. §8.3 erklärt „M-28(b) und M-39 sind Klasse (ii): Nur wenn der dokumentierte Altwert die neue Toleranz verfehlt“ sei Rotlauf und Erzeuger nötig. M-39 verlangt dagegen ausdrücklich „die neue Referenz trägt die neue metrics_version“ (:914). Am Ziel liegt der Bytevergleich tatsächlich in `BriefkastenTestMain.cpp:444-445`, nicht :442-443: `soll.getSize() == e.maskiert.size()` und `std::memcmp (soll.getData(), e.maskiert.data(), e.maskiert.size()) == 0`. :442 prüft das Entfernen der Testdatei; :443 ist leer. Maskiert wird ausschließlich `created_utc` (:199-210). `DiagnoseAntwort.cpp:65` schreibt `wurzel->setProperty ("metrics_version", kMetricsVersion);`; die alte Referenz enthält `"m4.1-2026-08-15"` (`festhalten-referenz.json:3`). Schon der vorgeschriebene Versionswechsel erzwingt daher unterschiedliche Bytes, unabhängig von einer LRA-Abweichung. Verletzt werden M-39, §8.1:1504-1511 und [E-380-17](docs/beweise/NAK-380.md#L2293-L2296): „neuer Code gegen alten Golden rot (Rohdatei), dann Erzeuger“. Schließende Regel: Die ±0,2-LU-Ausnahme gilt ausschließlich M-28(b); für M-39 bleiben alter Referenzinhalt, Rotlauf und anschließender B30-Erzeugerlauf verpflichtend. Folgestellen: §8.3, M-28, M-39 und die Klassenzuordnung in §8.1.
