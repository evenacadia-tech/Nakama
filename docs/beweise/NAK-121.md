# NAK-121 — Broker-Modulaufteilung und G2/G3-Haertung (S19b)

| Feld | Wert |
|---|---|
| Ticket | NAK-121 (Modulaufteilung) und NAK-124 (G2-Haertung), Planschritt S19b, Stufe T2. Mitgefuehrt: NAK-135, NAK-136, NAK-137, NAK-142 — alle vier tragen im Register dieselbe Klasse S19b |
| Basis-SHA | `85fb5f28054851831976db95760171cd5c239370`, gemessen mit `git rev-parse HEAD` am 2026-09-03; Worktree sauber (`git status --short` leer) |
| Phase | **Etappe 1, Nacharbeit Runde 1 nach Matrixpruefung 1.** Kein Produktcode, kein Test, kein Schema. Die acht Defekte der Matrixpruefung 1 sind in Paragraph 3, 4, 5 und 5.8 geschlossen; die Regeln des Dirigenten und die geaenderten Zeilen stehen woertlich in Paragraph 9. Der Aenderungssatz der Etappe sind genau zwei Dateien. Die Modulaufteilung und die Haertungen baut ein Folge-Worker auf dem Schnittplan aus Paragraph 5 und der Verhaltensmatrix aus Paragraph 4 |
| Urteil | **NOT RUN — offen fuer den Dirigenten** |
| Kanon vorher | **GRUEN 40/40, Exit 0**, Lauf 2026-09-03 07:06, Rohausgabe `docs/beweise/roh/NAK-134-eef6ebb-dirty.md`. Gemessen in Paragraph 6: `git diff --stat d3bf936..85fb5f2 -- broker eq-copilot tools` ist leer, der Lauf steht also auf demselben Codestand wie dieses Manifest. Kein neuer Kanon in dieser Etappe |
| Aenderungssatz | `docs/beweise/NAK-121.md` (neu) und `docs/plan/plan.json` (nur das Feld `beleg` des Schrittes S19b). **Nicht angefasst:** `broker/`, `eq-copilot/`, `tools/`, `docs/offene-punkte.md`, `docs/PLAN-STAND.md` und jede weitere Datei |
| Pruefung Etappe 1 (Matrixpruefung 1) | Codex `gpt-5.6-sol` (high), lesend, Thread `01a065ea-8425-77d2-ae92-cb3b8129cef0`, HEAD vor und nach dem Lauf `591fabf` — Codex lieferte keine URTEIL-Zeile, sondern sechs P1- und zwei P2-Kommentare; der Dirigent wertet das als **URTEIL NEEDS_WORK** mit **acht Defekten** (alle an der Quelle bestaetigt, Einordnung und Regeln in §9). Vollstaendiges Urteil in `docs/beweise/roh/NAK-121-matrixpruefung-1-591fabf.txt` |

## 1. Gate-Text — woertlich

Aus `docs/plan/plan.json`, Schritt S19b, Ticket NAK-121, Stufe T2:

> ANGELEGT 02.09.2026 (PR2 Runde 1, Codex-Befund D7; Gate-Satz von PR2: „NAK-121
> Modul-Aufteilung und NAK-124 bekommen einen Ort im Plan"). Ein Ort im Register
> genuegte dafuer nicht — hier ist er. INHALT, ein einziger verhaltensneutraler
> Aenderungssatz: (1) MODULAUFTEILUNG der drei gewachsenen Broker-Dateien
> `server_v3.rs`, `coordinator.rs` und `store.rs` je Fachbereich, mit erhaltener
> Historie (Wortlaut und Schnittvorschlag in Register NAK-121). (2) Die **25
> G2-Haertungen aus NAK-124** (14 MEDIUM und 11 LOW aus `docs/beweise/G2.md`
> Paragraph 4.2) werden im selben Satz eingearbeitet, weil sie dieselben Dateien
> anfassen und eine zweite Runde durch dieselben Zeilen die Aufteilung sonst
> erneut aufreisst. GRENZE: **kein Wire-, State- oder Testvertrag wird
> beruehrt** — keine Schemas, keine Fixtures, keine Nachrichtenform, keine
> oeffentliche Signatur, die ein anderer Verbraucher liest. Wer eine
> Vertragsaenderung braucht, gehoert nicht in diesen Schritt. BEWEIS: der Kanon
> laeuft **vor und nach** der Aufteilung auf demselben Stand und ist beide Male
> gruen; die Zahl der Beine ist vorher wie nachher gleich. PRUEFUNG: frischer
> Codex-Thread, lesend, aus `tools/dirigent/pruefauftrag-vorlage.md`. ZEITPUNKT:
> nach dem G2-PASS und vor P4, also direkt nach G3 — spaeter waere die
> Aufteilung teurer, weil P4 bis P7 in dieselben Dateien schreiben. Register
> NAK-121 und NAK-124. BELEG-FELD 02.09.2026 (Dirigent, vor dem Gate-Lauf G3):
> Das Manifest docs/beweise/NAK-121.md ist noch nicht angelegt; es entsteht mit
> dem Bau, und erst dann wird das Feld beleg gesetzt, wie bei allen anderen
> offenen Schritten. Bis dahin meldete der Dokuriegel den gerechneten Planstand
> als Verweis ins Leere (Befund vom 02.09.2026, behoben).

## 2. Umfang und Quellen

Sechs Registerzeilen tragen die Klasse S19b. Was jede in diesen Schritt einbringt:

| ID | Was sie einbringt |
|---|---|
| **NAK-121** | Die Modulaufteilung selbst, mit dem Schnittvorschlag Transport-Framing, Session/Coordinator, Store/Outbox und der Auflage, die Historie zu erhalten. Verfeinert in Paragraph 5 aus dem Code |
| **NAK-124** | Die 25 G2-Primaries (14 MEDIUM, 11 LOW). Same-user, nach Entwurf Paragraph 48.4 ausserhalb des IPC-Bedrohungsmodells, aber reale Defekte in DoS-, Verfuegbarkeits- oder Soundness-Form |
| **NAK-135** | Der v3-Listener haelt nur zwei Pipe-Instanzen scharf, obwohl `PIPE_INSTANZEN` 98 erlaubt. Kein Ursachenbefund zu NAK-134, aber der Ausloeser des Ansturms |
| **NAK-136** | Dirigentenregel vom 03.09.2026: nach einer Nonce-Verdraengung markiert der Coordinator die Session dirty und flusht den Snapshot noch im Hello-Zyklus. Matrixzeile C-10 wird um diese Frist ergaenzt, ein `coordinator_model`-Test misst sie |
| **NAK-137** | Regel: v3-Pruefung im Setter oder `cfg(test)`-Zaun fuer `Coordinator::descriptor_setzen`. Paragraph 3 entscheidet die Alternative technisch |
| **NAK-142** | Der Rust-Review G3: 28 Primaries, davon 20 bekannt und 8 neu, plus die Feststellung, dass zwei G2-Befunde durch NAK-123 verschwunden sind |

Berichte und Belege:

* G2, Lauf 20260901T104834Z: `.rust-review-results/20260901T104834Z/REPORT.md`.
  Diese Mappe ist gitignoriert und nur lokal vorhanden — deshalb macht die
  Befundtabelle in Paragraph 3 jede Zeile selbsttragend: ID, Fundstelle am
  heutigen Stand, Fix und messender Test stehen dort, nicht nur der Verweis.
* G3, Lauf 20260902T213108Z, committet:
  `docs/beweise/roh/G3-rust-review-20260902T213108Z/REPORT.md` und
  `docs/beweise/roh/G3-rust-review-20260902T213108Z/delta-vs-20260901T104834Z.md`.
* Gate-Manifeste: `docs/beweise/G2.md` Paragraph 4.2, `docs/beweise/G3.md`
  Paragraph 4 und Paragraph 5.
* Matrixform und die beruehrten Zusagen: `docs/beweise/SONDE-011.md` Abschnitt
  B.2 (Coordinator und Session, Zeilen C-01 bis C-10), B.3 (Liveness und
  Eviction, E-01 bis E-06) und B.5 (Outbox und Prioritaeten, O-01 bis O-04).
* Entwurf `docs/FL-Nakama-Sonden-Design-Entwurf.md`: Paragraph 48.3
  Broker-Lifecycle, Paragraph 48.4 Sicherheit und Bedrohungsmodell same-user,
  Paragraph 53.4 Zielmodule im Repository, Paragraph 53.9 IPC-, Coordinator- und
  Storebindung. Die heutige Modulkarte des Transports steht im Kopfkommentar von
  `broker/src/transport/mod.rs`.
* Arbeitsliste: `tools/dirigent/pruefliste.md`, abgehakt in Paragraph 7.

**Zwei ID-Fallen, die dieses Manifest ausdruecklich aufloest.** Erstens vergeben
G2 und G3 dieselbe ID an **verschiedene** Defekte: `RAWFD-001`, `STRCMP-001`,
`BUFFLUSH-001`, `SAFETYDOC-001` und die ganze `RESEXHAUST`-Nummerierung meinen in
den beiden Laeufen nicht dasselbe. Jede Zeile in Paragraph 3 traegt deshalb ein
Praefix `G2-` oder `G3-`; eine unpraefigierte ID ist in diesem Ticket ungueltig.
Zweitens ist ein Befund erst erledigt, wenn er am Code widerlegt oder behoben
ist — nicht schon dann, wenn ein spaeterer Lauf ihn nicht erneut meldet; sechs
G2-Primaries hat G3 nicht reproduziert, obwohl der Defekt nachweislich
fortbesteht.

**Nicht in diesem Schritt.** `docs/beweise/G2.md` Paragraph 4.3 fuehrt zwei
weitere Matrixfragen (Register NAK-125 und NAK-126). Beide tragen **nicht** die
Klasse S19b und werden hier weder umgesetzt noch entschieden.

**Zahlen am HEAD gemessen, nicht abgeschrieben.** Der Auftrag nennt 4752, 3286
und 1662 Zeilen; `wc -l` am Stand `85fb5f2` liefert **5095** fuer
`broker/src/transport/server_v3.rs`, **3482** fuer `broker/src/coordinator.rs`
und **1774** fuer `broker/src/store.rs`. Der Schnittplan rechnet mit den
gemessenen Zahlen. Auch die Registerzeile NAK-121 nennt aeltere Werte (rund 4100
und 2300); sie stammt vom 01.09.2026.

## 3. Befundtabelle

Alle 25 G2-Primaries, alle 8 G3-Neubefunde und die drei Registerregeln, je eine
Zeile. **Fundstelle heute** ist am Stand `85fb5f2` nachgeprueft; die G3-Zeilen
gelten woertlich, weil der Diff der G3-Reviewbasis gegen HEAD auf `broker/` leer
ist (Paragraph 6). **Zielmodul** nennt den Zielpfad nach der Aufteilung relativ
zu `broker/src/`; diese Zielpfade sind noch nicht angelegt und entstehen erst im
Folge-Worker nach Paragraph 5.

**Einordnung.** `A` = innerhalb der Grenze, wird im Folge-Worker umgesetzt.
`B` = braucht eine Vertragsaenderung und wird **nicht** umgesetzt.
`C` = am HEAD nicht mehr vorhanden oder Fehlbefund, mit Beleg.

Das Gate verlangt „kein Testvertrag wird beruehrt": bestehende Tests behalten
ihre Aussage. Neue Tests sind erlaubt und gehen in die vorhandenen
Testbinaries, die das Thema schon besitzen — es entsteht keine neue Testdatei.

### 3.1 G2-MEDIUM (14 Primaries, 15 Zeilen: G2-STRCMP-001 ist in eine A- und eine B-Haelfte geteilt)

| ID | Herkunft | Fundstelle heute | Zielmodul | Einordnung | Fix-Zusage | Test |
|---|---|---|---|---|---|---|
| G2-RESEXHAUST-004 | G2 MEDIUM | `broker/src/server.rs:696-697`, `verbindung_bedienen`, Gate auf `r.sensoren.len() >= max_sensor_ids`; eine Suche nach `sensoren.remove`, `sensoren.retain` oder `sensoren.clear` trifft nirgends im Crate | `server.rs` bleibt, kein Ziel dieser Aufteilung | A | Der v2-Sensor-ID-Pool bekommt einen Rueckgabeweg mit **benannter Frist**: `SENSOR_ID_RUECKGABE_MS = 60_000` neben `MAX_SENSOR_IDS = 128` (`broker/src/server.rs:53`). Der Wert ist nicht frei gewaehlt, sondern der Leerlaufhorizont, den der Broker fuer sich selbst schon fuehrt (`BROKER_IDLE_ENDE_MS = 60_000`, `broker/src/lib.rs:39`): wer laenger fort ist, als der Broker ohne jeden Sensor weiterlaeuft, hat keinen Anspruch mehr auf einen Platz. **Einen v2-Tick gibt es nicht** — eine Suche nach periodischen Schleifen in `broker/src/server.rs` trifft nur Accept- und Flushwartezeiten (`:291`, `:526`, `:555`, `:895`). Der Sweep laeuft deshalb **im Gate selbst**, unter demselben Lock wie die Grenzpruefung bei `:688-701` und vor ihr. Die Grenze ist **inklusiv**: frei wird, wessen `getrennt_seit_ms` (`broker/src/lib.rs:120`, UTC-ms, gesetzt bei `:458`) `jetzt - getrennt_seit_ms >= SENSOR_ID_RUECKGABE_MS` erfuellt; am Gate wird danach der am laengsten getrennte Eintrag verdraengt statt hart abgewiesen. Der harte Reject bleibt genau dann, wenn alle 128 Plaetze wirklich verbunden sind. Matrixzeile H-13 | neu inline in `broker/src/server.rs`, das die v2-Poolproben bereits traegt: `v2_sensorpool_gibt_getrennte_ids_zurueck` misst ueber `Register::getrennte_ids_freigeben_zu(jetzt)` — dem `_zu`-Zeitseam von `sensoren_snapshot_zu` (`broker/src/lib.rs:500`) — die drei Kanten **Frist minus eins** (Eintrag bleibt), **exakt Frist** (Eintrag faellt) und **128 verbundene Eintraege** (Reject bleibt) |
| G2-RESEXHAUST-001 | G2 MEDIUM | `broker/src/telemetrie.rs:308`, `pruefe`, pusht `eintraege_zu_viele` und faellt in die Schleife ab `:316`, statt wie die drei Wachen darueber zurueckzukehren | `telemetrie.rs` bleibt | A | **Nur der Rust-Leser.** Nach dem Push von `eintraege_zu_viele` kehrt `pruefe` sofort kanonisch zurueck, damit die Verstosszahl nicht mit der Eintragszahl waechst. **Die Abweisungsentscheidung bleibt identisch** zu heute und zum C++-Leser, gemessen am Cross-Language-Korpus `eq-copilot/fixtures/v3/flatbuffers/MANIFEST.json`: 100 Fixtures, hoechstens zwei Verstoesse je Fixture, und das einzige Fixture oberhalb der Grenze — `ungueltig/eintraege-33.bin` — traegt im Manifest genau **einen** Verstoss `/eintraege eintraege_zu_viele`. Die fruehe Rueckkehr liefert dieselbe Menge; der C++-Leser liefert sie heute schon. **Nach aussen sichtbar ist die Verstossliste nicht:** `p2` verwirft bei `broker/src/coordinator.rs:3082-3086` das `Err(_)` und meldet den festen Grund `P2RejectGrund::FeatureBatchUngueltig`; eine Nachrichtenform oder ein Telemetriezaehler ueber Verstosszahlen existiert nicht. Sichtbar ist sie allein als Rueckgabe der `pub fn pruefe` an `broker/tests/`, und dort bleibt sie in Form und Wert unveraendert. Die C++-Spiegelung der fruehen Rueckkehr ist Haertung ausserhalb dieses Tickets, siehe den Absatz unter Paragraph 3.4. Matrixzeile H-20 | `broker/src/telemetrie.rs` traegt heute **null** Inlinetests (Zaehlung der Testattribute am Stand `85fb5f2`). Die Wache ist der vorhandene `broker/tests/contract_cross_language.rs`, Test `fb_korpus_klassifiziert_wie_das_manifest`: er faehrt `pruefe` ueber alle 100 Fixtures und haelt Urteil **und vollstaendige Verstossmenge** gegen das handgeschriebene Manifest. Neu im selben Test eine Grenzprobe, die einen Batch mit `MAX_EINTRAEGE + 1` Eintraegen im Speicher baut (`FlatBufferBuilder` aus der vorhandenen `flatbuffers`-Abhaengigkeit, **keine** neue Fixture-Datei) und die Verstossmenge auf genau `/eintraege eintraege_zu_viele` prueft |
| G2-RESEXHAUST-002 | G2 MEDIUM | `broker/src/transport/warteschlange.rs:354`, `IngressWarteschlange::einreihen`, deckelt nur die Slotzahl; Aufrufstelle `broker/src/transport/server_v3.rs:2770` | `transport/warteschlange.rs` bleibt; Aufrufstelle nach `transport/server_v3/verbindung.rs` | A | Neben das Slotbudget tritt ein Bytebudget `CAP_INGRESS_BYTES`. Ueberschreitung wirkt genau wie Slot-voll: erst P2 verwerfen, dann P0 und P1 zum Trennen. Matrixzeile H-11 | neu in `broker/tests/transport_fuzz.rs`: `ingress_bytebudget_verwirft_p2_zuerst`, gemessen am Budget und am Budget plus eins |
| G2-RESEXHAUST-005 | G2 MEDIUM | `broker/src/instance_alias.rs:163-164` und `:180`, `registriere_wire_zuordnung`; eine Suche nach `quarantaene.remove`, `quarantaene.retain` oder `quarantaene.clear` trifft nirgends. Persistente Haelfte in `broker/src/store.rs`, Tabelle `conflict_guards` ohne Deckel | `instance_alias.rs` bleibt; Storehaelfte nach `store/writer.rs` | A | Beide Speicher bekommen eine Obergrenze; ist sie erreicht, wird die Verbindung abgewiesen, was ohnehin der fail-closed-Ausgang ist. Die Aufloesung eines Konfliktriegels raeumt zusaetzlich den passenden Aliaseintrag, was sie heute nicht tut. Die abgenommene Zusage C-07 bleibt unveraendert: keine zeitbasierte Freigabe, Aufloesung nur ueber explizite Neu-ID. Matrixzeile H-14 | neu in `broker/tests/store_crash_matrix.rs`: `quarantaene_deckel_weist_ab_statt_zu_wachsen` und `guard_aufloesung_raeumt_den_aliaseintrag` |
| G2-RESEXHAUST-003 | G2 MEDIUM | `broker/src/coordinator.rs:833` und `:1987`, beide Male ein Einfuegen in `stand.sessions`; eine Suche nach `sessions.remove`, `sessions.retain` oder `sessions.clear` trifft in der Datei nur `dirty_sessions.remove` bei `:2193`, eine andere Map | `coordinator/liveness.rs` | A | Eine Session wird fallengelassen, sobald sie keinen Client mehr hat — in `client_eviktieren_locked` und im Liveness-Sweep. Zusaetzlich ein `GLOBAL_SESSION_CAP` neben `GLOBAL_CLIENT_CAP`, damit die Schranke ein spaeteres Einfuegen ohne Entfernen ueberlebt. Matrixzeile H-12 | neu in `broker/tests/coordinator_model.rs`: `session_faellt_mit_dem_letzten_client` und `global_session_cap` am Cap und am Cap plus eins |
| G2-RAWFD-003 | G2 MEDIUM | `broker/src/transport/server_v3.rs:1979`, `v3_server_starten_intern`, Spawn-Fehlerpfad: das Handle geht vor dem Registeraustrag zu. In G3 als OPAQUEPTR-001 wiedergefunden, das dort G3-UAF-001, G3-RAWFD-002, G3-UNSAFESYNC-001 und G3-SAFETYDOC-002 absorbiert | `transport/server_v3/win_handles.rs` | A | `Verbindungsgriff` uebernimmt Registereintrag **und** Handle. Ein Destruktor traegt unter derselben Sperre aus und schliesst erst danach; `HandleEintrag` entfaellt. Der Abbruchaufruf bekommt eine Rueckgabewertpruefung, damit ein unerwartetes `ERROR_INVALID_HANDLE` gezaehlt statt verschluckt wird. Matrixzeile H-01 | neu in `broker/tests/security_vectors.rs`: `registeraustrag_geht_dem_close_voraus` und `cancel_auf_totes_handle_wird_gezaehlt` |
| G2-UAF-001 | G2 MEDIUM | `broker/src/transport/server_v3.rs:1289`, `alle_io_abbrechen`, speist den Abbruch mit dem Inhalt des Handleregisters ohne Generationszaehler; das zugehoerige `unsafe impl Send` steht bei `:975` | `transport/server_v3/win_handles.rs` | A | Senkenseite desselben Defekts, **ein** Arbeitsstueck mit G2-RAWFD-003. Sobald der Eintrag den geteilten Verbindungsgriff besitzt, beschreibt das `unsafe impl Send` eine Invariante, die der Compiler haelt. Matrixzeile H-01 | wie G2-RAWFD-003 |
| G2-RAWFD-001 | G2 MEDIUM | **Nicht mehr vorhanden.** Das Erst-Instanz-Flag wird heute in `broker/src/transport/server_v3.rs:388` innerhalb des RAII-Typs `ListenerInstanz` gebunden, der bei `:370` beginnt | entfaellt | C | Behoben durch NAK-123, und zwar genau wie G2 empfohlen hatte: das Handle wird bei der Erzeugung gebunden. Beleg: eine Suche nach `struct ListenerInstanz` in `broker/src/transport/server_v3.rs` trifft `:370`, der zugehoerige Destruktor steht bei `:478` | schon vorhanden: `broker/tests/security_vectors.rs`, `startfehler_nach_erstem_handle_raeumt_handles_threads_und_namen_vor_publish` und zwei Schwestertests |
| G2-RAWFD-002 | G2 MEDIUM | `broker/src/transport/server_v3.rs:2908`, `verbindung_bedienen`, zaehlt `schreiber_abgeloest`; `join_mit_frist` bei `:1314` laesst den Joinhandle fallen, der Schreiberthread haelt seinen geteilten Verbindungsgriff weiter, der Registereintrag verschwindet mit dem Funktionsende. In G3 als RAWFD-001 und dort LOW | `transport/server_v3/win_handles.rs` und `transport/server_v3/verbindung.rs` | A | Zwei Haelften. Erstens dieselbe Ein-Besitzer-Aenderung wie H-01: der Registereintrag lebt, solange irgendein Thread einen geteilten Verbindungsgriff haelt. Zweitens — und das ist der Teil, der heute fehlt — ein **ausdruecklicher Abbruchweg**. `HandleRegister` (`broker/src/transport/server_v3.rs:966-969`) bekommt neben `offen` eine zweite Menge `abgeloest: HashSet<u64>`; die Ablosung bei `:2907-2910` traegt die Verbindungs-ID dort ein. Der vorhandene Wachhund bricht heute bei `:1725-1743` **nur** faellige Bootstrap-IDs ab — `BootstrapFrist` hat die ID bei `:2379` laengst ausgetragen, deshalb erreicht ihn ein abgeloester etablierter Schreiber nie. Kuenftig bricht dieselbe Schleife auch jede ID aus `abgeloest` ab, bei **jedem** Tick erneut, bis der Thread endet und der Griff faellt; dessen Destruktor traegt die ID aus beiden Mengen aus. `alle_io_abbrechen` (`:1283`) deckt sie beim Serverstopp ohnehin. Reihenfolge: Ablosung → Eintrag in `abgeloest` → Wachhund-Abbruch → Threadende → Handle frei. Matrixzeile H-02 | neu in `broker/tests/security_vectors.rs`: `abgeloester_schreiber_gibt_instanz_und_handle_zurueck`, erzwingt `schreiber_abgeloest` groesser null, wartet **einen** Wachhundtick ab und prueft danach `V3Griff::gehaltene_handles() == 0` (`:1168`) bei weiterhin annehmendem Listener |
| G2-RAWPTR-001 | G2 MEDIUM | **Nicht mehr vorhanden.** Die Funktion `warten_auf_verbindung` ist geloescht | entfaellt | C | Beleg: eine Suche nach `warten_auf_verbindung` ueber `broker/src` und `broker/tests` liefert keinen Treffer; nur veraltete Build-Artefakte unter `broker/target/` matchen und sind kein Quellstand. Die drei verbliebenen Abbruchaufrufe mit Stack-Overlapped stehen bei `:488`, `:609` und `:649` und haben unmittelbar danach ein `GetOverlappedResult` bei `:490`, `:610` und `:650`; `:1289`, `:1301` und `:1740` uebergeben einen Nullzeiger statt einer Overlapped-Struktur und sind nicht betroffen | keiner noetig; die Wache ist die Abwesenheit des Musters |
| G2-CHANSTARVE-001 | G2 MEDIUM | `broker/src/coordinator.rs:2184`, `flush_session`, ruft je Abonnent sequentiell; die Kette endet in `broker/src/transport/server_v3.rs:1046` mit einem Empfang unter `SENKE_FRIST` | `coordinator/flush.rs` und `transport/server_v3/griff.rs` | A | **Genau ein Zustellpfad.** `SessionPush::snapshot_schreiben` reiht den Snapshot ein und kehrt sofort zurueck, wie `messframe_schreiben` bei `broker/src/transport/server_v3.rs:1049-1061`; das synchrone `antwort.recv_timeout(SENKE_FRIST)` bei `:1046` entfaellt ersatzlos. **Die Alternative mit einem Wanduhrbudget ueber die Zielschleife wird gestrichen.** Die Buchhaltung bleibt trotzdem ehrlich: der `geschrieben`-Kanal des Ausgangseintrags (`:826`) traegt kuenftig die Kompaktierung selbst, sodass `snapshot_schuld_kompaktieren` weiterhin **nach vollstaendigem Write** laeuft und die abgenommene Zusage O-03 unberuehrt bleibt. Die Senkenfrist wechselt nur den Ort, nicht den Wert: `ov_schreiben` wartet bei `:648` heute `INFINITE` und bekommt `SENKE_FRIST`; laeuft sie ab, wird abgebrochen, im neuen Zaehler `snapshot_frist_abgelaufen` gezaehlt und getrennt — derselbe Weg, den der Schreiber bei `:2622-2628` schon geht. Eviction laeuft **weiterhin** vor der Zustellphase; das ist keine Verschiebung, sondern eine gehaltene Reihenfolge: `client_eviktieren_locked` steht bei `broker/src/coordinator.rs:1528` im gesperrten Block, die Zustellschleife bei `:1545` danach. Matrixzeile H-06 | neu in `broker/tests/coordinator_model.rs`: `nicht_lesender_abonnent_haelt_den_liveness_tick_nicht_auf` mit **vier benannten Assertionen im selben Test** — (1) Tickdauer unter dem Budget, (2) die lesenden Abonnenten haben ihren Snapshot, (3) der nicht lesende ist getrennt und in `snapshot_frist_abgelaufen` gezaehlt, (4) ein in diesem Tick eviktierter Client steht in keiner Zielliste, der Reihenfolgebeweis Eviction vor Zustellung |
| G2-STRCMP-001 | G2 MEDIUM | `broker/src/coordinator.rs:1151`, `push_ziel_noch_gueltig`, fragt den Konfliktriegel gross- und kleinschreibungsempfindlich ab. Am HEAD gemessen sind es **elf** solche Abfragen: `:1004`, `:1151`, `:1796`, `:1849`, `:1864`, `:1982`, `:2027`, `:2208`, `:2491`, `:2505` und `:2708` — der G3-Deltabericht nennt nur sieben davon. Dazu kommen **drei** schreibende Stellen `:606`, `:795` und `:1578` und **zwei** loeschende `:2320` und `:2323`. `adresse_pruefen` in `broker/src/transport/bootstrap.rs:150-151` prueft `logon_sid` nur auf Laenge 1 bis 184, waehrend die vier Geschwisterfelder hex32 sein muessen | `coordinator/flush.rs` | A | **Nur die fail-open-Haelfte, und ohne die gespeicherte Form zu aendern.** Alle elf Guard-Abfragen vergleichen kuenftig ueber einen zusaetzlichen, gefalteten Index, sodass eine anders geschriebene SID den dauerhaften Konfliktriegel nicht mehr abschuettelt. Die drei schreibenden und die zwei loeschenden Stellen pflegen denselben Index mit, sonst liefen Lesen und Schreiben auseinander. Die Bytes in der Spalte `effective_address` bleiben exakt wie heute, damit Altstaende unveraendert laden. Die zweite Haelfte — Kanonisierung der SID an der Vertrauensgrenze — ist Einordnung B und steht als eigene Zeile darunter. Matrixzeile H-21 | neu in `broker/tests/store_crash_matrix.rs`: `konfliktriegel_greift_auch_bei_anderer_sid_schreibweise` |
| G2-DROPPANIC-001 | G2 MEDIUM | `broker/src/server.rs:341-350`, der Destruktor von `RegistrierteVerbindung`; behandelt das Register defensiv, ruft dann aber bei `:347` die Senke, die in `broker/src/coordinator.rs:2430` mit `expect` auf einem vergiftbaren Mutex sperrt | `coordinator/intervention.rs`; der Aufrufer bleibt in `server.rs` | A | Zwei Aenderungen. Der Senkenaufruf im Destruktor wird gegen Panik gefasst, damit eine panische Senke den Destruktor nicht verlaesst. Und die Ursache faellt: die Sperrgriffe des Coordinators werden vergiftungstolerant — das Muster, das dieselbe Datei bereits vierzehnmal verwendet. Matrixzeile H-05 | neu in `broker/tests/coordinator_model.rs`: `vergifteter_stand_beendet_den_prozess_nicht`, vergiftet den Lock absichtlich und prueft, dass Trennung und Hoermarkierung weiterlaufen |
| G2-TOCTOU-002 | G2 MEDIUM | `broker/src/store.rs:1524`, `store_pfad_ist_remote`, geht aufwaerts, bis eine Komponente existiert, und klassifiziert diesen Vorfahren; weder ein Reparse-Punkt-Flag noch eine Handle-Rueckaufloesung kommt in `broker/src/` vor | `store/pfad.rs` | A | Klassifiziert wird das geoeffnete Objekt, nicht ein Vorfahre: jede fehlende Komponente wird einzeln erzeugt, sodass ein bereits vorhandener Eintrag sichtbar bleibt; eine vorhandene Komponente wird mit Backup-Semantik und Reparse-Punkt-Flag geoeffnet und bei gesetztem Reparse-Attribut abgewiesen; die Volumenklassifikation laeuft danach auf dem Handle der geoeffneten Datenbank | neu in `broker/tests/store_crash_matrix.rs`: `store_weist_reparse_punkt_im_pfad_ab` |
| G2-STRCMP-001-REST | G2 MEDIUM, zweite Haelfte | dieselbe Fundstelle; zusaetzlich `broker/src/coordinator.rs:3203`, `effektive_adresse`, das die rohe SID einbettet | entfaellt | B | **Wird nicht umgesetzt.** Die SID an der Vertrauensgrenze zu kanonisieren aendert den Wert von `effektive_adresse` und damit den persistierten Schluessel in der Tabelle `conflict_guards`: nach der Umstellung faenden Altstaende ihre eigenen Riegel nicht wieder. Der G2-Bericht verlangt zusaetzlich ausdruecklich, dieselbe Regel im v3-Schema zu spiegeln, damit das C++-Bein zustimmt — also State- **und** Wire-Vertrag. Genau das schliesst der Gate-Satz aus. Der Rest gehoert datiert nach `docs/offene-punkte.md` in ein Ticket mit versionierter Migration und Cross-Language-Vertragstest; der Folge-Worker traegt die Zeile ein | keiner in S19b |

### 3.2 G2-LOW (11)

| ID | Herkunft | Fundstelle heute | Zielmodul | Einordnung | Fix-Zusage | Test |
|---|---|---|---|---|---|---|
| G2-CARGOLINT-001 | G2 LOW | `broker/Cargo.toml`, keine `lints`-Tabelle; die Lints gegen undokumentierte `unsafe`-Bloecke bleiben ueber alle `unsafe`-Stellen des Crates aus | `broker/Cargo.toml` | A | **Drei Teile, weil der Clippy-Lint im Kanon gar nicht laeuft.** `tools/beweise.ps1` faehrt in Bein A4 (`:457-460`), in A4-SI (`:465-468`) und im Release-Bau (`:775`) ausschliesslich `cargo test` und `cargo build`; eine Suche nach `clippy` im ganzen Skript trifft **nirgends**, und `undocumented_unsafe_blocks` ist ein reiner Clippy-Lint, den `rustc` nicht kennt. Also: **(a)** `[lints.rust] unsafe_op_in_unsafe_fn = "deny"` wirkt im Bau und damit im bestehenden Bein — die `[lints]`-Tabelle gilt ab Cargo 1.74, gemessen `cargo 1.93.1`; sie greift auf `broker/src/store.rs:1589`, die einzige `unsafe fn` des Crates. **(b)** Eine **Quellwache** als Test misst die eigentliche Zusage dauerhaft, siehe Testspalte. **(c)** `[lints.clippy] undocumented_unsafe_blocks = "deny"` steht zusaetzlich in derselben Tabelle, **beweist im Kanon aber nichts**, solange kein Bein Clippy faehrt; die Clippy-Verdrahtung ist benannte Haertung ausserhalb dieses Tickets. `tools/` wird hier nicht angefasst, die Beinzahl bleibt 40. Die Aufteilung geht voraus, damit der Lint nicht ueber verschobene Zeilen streut. Matrixzeile H-22 | neu in `broker/tests/security_vectors.rs`: `jeder_unsafe_block_traegt_einen_safety_kommentar` liest `broker/src/**/*.rs` ohne `generiert/` ueber `std::fs`, wie `contract_cross_language.rs:19-25` es fuer Fixtures schon tut, und verlangt vor jeder Zeile mit `unsafe impl` oder `unsafe {` einen mit `// SAFETY:` beginnenden Kommentar in den **drei vorangehenden nicht-leeren Zeilen**. Am Stand `85fb5f2` gemessen: **100** solche Stellen, **59** erfuellen das Fenster, **41** nicht. Der Test ist erst gruen, wenn alle 100 ihren Kommentar tragen; er misst damit G2-SAFETYDOC-001 und G3-SAFETYDOC-001 und laeuft im Kanon-Bein A4 (`broker`) |
| G2-MSRV-001 | G2 LOW | `broker/Cargo.toml`, kein `rust-version`, obwohl eine benutzte Standardmethode einen Compilerboden setzt | `broker/Cargo.toml` | A | `rust-version` wird auf den tatsaechlich noetigen Boden gesetzt und damit zur pruefbaren Zusage neben dem bereits gepinnten SQLite | vorhandener Inlinetest `rusqlite_version_0_40_2_ist_exakt_gepinnt` in `broker/src/store.rs` bekommt einen Zwilling fuer den Compilerboden |
| G2-SAFETYDOC-001 | G2 LOW | `broker/src/server.rs:889-890`, der Spawn mit dem Puffer-Flush. Der G2-Bericht nennt ihn als einzigen Block der Datei ohne Sicherheitskommentar; das Fenster aus der Testspalte von G2-CARGOLINT-001 misst am HEAD **sieben** Stellen ohne `// SAFETY:` in `server.rs` (`:174`, `:177`, `:182`, `:183`, `:285`, `:869`, `:889`) | `server.rs` bleibt | A | Der Sicherheitskommentar an `:889` wird geschrieben und benennt das Cancel-und-Join-Protokoll, auf dem die Soundness ruht. Weil die Quellwache aus G2-CARGOLINT-001 die **ganze** Quelle misst, bekommen im selben Arbeitsstueck alle 41 gemessenen Stellen ihren Kommentar; anders waere der Test nicht gruen. Matrixzeile H-22 | die Quellwache aus G2-CARGOLINT-001, nicht der Clippy-Lint |
| G2-REENTRANT-001 | G2 LOW, in G3 MEDIUM | `broker/src/coordinator.rs:3191`, in der Senkenimplementierung: der globale Standlock wird ueber den fremden Push-Aufruf gehalten | `coordinator/senke.rs` | A | Die Zielliste wird unter dem Lock gesammelt, der Lock vor dem Push freigegeben, der Push danach ohne Lock gefahren — genau das Muster, das `flush_session` bei `broker/src/coordinator.rs:2271` schon anwendet und in seinem Kommentar bei `:2267-2270` als Invariante beschreibt. Matrixzeile H-03 | neu in `broker/tests/coordinator_model.rs`: `senke_haelt_den_standlock_nicht_ueber_den_push`, mit einer Senke, die im Push reentrant in den Coordinator greift |
| G2-REENTRANT-002 | G2 LOW, in G3 MEDIUM | `broker/src/coordinator.rs:1124`, `messframes_an_subscriber_push`: der bei `:1097` genommene Standlock steht noch, waehrend die bereits kopierte Frameliste verschickt wird | `coordinator/subscription.rs` | A | Der Lock endet nach dem Sammeln, also vor der Schleife; die Liste ist ohnehin schon eine Kopie. Matrixzeile H-03 | wie G2-REENTRANT-001 |
| G2-TOCTOU-001 | G2 LOW | `broker/src/store.rs:1259`, vier namensbasierte Aufloesungen ohne mitgefuehrtes Handle; der Rest, der wirklich zaehlt, ist die Reihenfolge: die Migration laeuft vor dem Pruefsummenvergleich | `store/migration.rs` | A | Der Pruefsummenvergleich wandert vor die Migration, sodass ein fremdes Schema nicht erst angefasst und dann beurteilt wird | vorhandene Migrationstests in `broker/tests/store_crash_matrix.rs`, erweitert um `pruefsumme_entscheidet_vor_der_migration` |
| G2-LOSSYSTR-001 | G2 LOW | `broker/src/store.rs:1504`, verlustbehaftete UTF-16-Wandlung auf einem Pfad, der danach geoeffnet und sicherheitsgeprueft wird | `store/pfad.rs` | A | Die verlustfreie Wandlung, die das Crate an anderer Stelle schon verwendet, ersetzt die verlustbehaftete. Damit ist der geprueft Pfad byteweise derselbe wie der geoeffnete | neu in `broker/tests/store_crash_matrix.rs`: `store_pfad_bleibt_bei_nicht_utf8_zeichen_unveraendert` |
| G2-FLOATEDGE-001 | G2 LOW | `broker/src/coordinator.rs:1263`, die Fensterbreite wird aus Samplezahl geteilt durch Samplerate gerechnet; eine Suche nach einer Wache auf `sample_rate` im ganzen Crate bleibt leer | `coordinator/sicht.rs` | A | Nicht-endliche und nicht-positive Samplerate ergibt kein Fenster, sondern `None`, und wird gezaehlt. Das ist die NaN-Ehrlichkeit aus `CLAUDE.md`, angewandt auf eine abgeleitete Groesse statt nur auf den Eingang | neu in `broker/tests/coordinator_model.rs`: `subnormale_samplerate_ergibt_kein_fenster`, mit subnormalem, null und negativem Wert |
| G2-BUFFLUSH-001 | G2 LOW | `broker/src/server.rs:863-864`, der v2-Abbau schliesst ohne wirksamen Flush; der beschraenkte Flush-Helfer bei `:875` haengt weiterhin nur an den Reject-Pfaden | `server.rs` bleibt | A | Der v2-Abbau geht denselben Weg wie die Reject-Pfade: beschraenkter Flush mit harter Frist, dann trennen, dann schliessen. Matrixzeile H-08 | neu in `broker/tests/security_vectors.rs`: `v2_abbau_flusht_die_letzte_antwort` |
| G2-RESDISC-001 | G2 LOW | `broker/src/store.rs:784`, das Ergebnis des Leerlauf-Checkpoints wird verworfen **und** der Erfolg wird direkt danach gemerkt, sodass ein gescheiterter Checkpoint nie wiederholt wird, waehrend die Storesicht weiter gesund meldet | `store/writer.rs` | A | Das Ergebnis wird ausgewertet: nur ein erfolgreicher Checkpoint merkt sich den Erfolg, ein gescheiterter wird gezaehlt und beim naechsten Leerlauf erneut versucht. Matrixzeile H-18 | neu in `broker/tests/store_crash_matrix.rs`: `gescheiterter_leerlauf_checkpoint_wird_wiederholt` |
| G2-RESDISC-002 | G2 LOW | `broker/src/transport/server_v3.rs:1426`, `fertige_ernten`, verwirft jedes Join-Ergebnis; es gibt keinen Panikzaehler, waehrend das v2-Ende genau das auswertet | `transport/server_v3/griff.rs` | A | Das Join-Ergebnis wird ausgewertet und eine Panik in einem Verbindungsthread in `V3Statistik` gezaehlt. Der Befund entfernt sonst genau das Erkennungssignal fuer die Fehlerklasse, die dieser Review jagt, und zwar am Endpunkt, der angreiferkontrollierte Bytes parst. Matrixzeile H-19 | neu in `broker/tests/transport_fuzz.rs`: `panik_im_verbindungsthread_wird_gezaehlt` |

### 3.3 G3-Neubefunde (8)

| ID | Herkunft | Fundstelle heute | Zielmodul | Einordnung | Fix-Zusage | Test |
|---|---|---|---|---|---|---|
| G3-BUFFLUSH-001 | G3 MEDIUM, neu | `broker/src/transport/server_v3.rs:678`, der Destruktor von `Verbindungsgriff` schliesst das Handle bar; ein Puffer-Flush kommt in `broker/src/transport/` nirgends vor, waehrend `broker/src/coordinator.rs:2280` die Snapshotschuld auf die Schreiber-Wahrheit hin kompaktiert | `transport/server_v3/win_handles.rs` | A | **Entscheidung an O-02 und O-03, nicht an der Berichtsempfehlung.** Die abgenommene Zusage O-03 erlaubt woertlich, die Schuld nach vollstaendigem Write zu kompaktieren, „ohne Empfaengerwirkung zu behaupten", und O-02 sagt, ein Pipe-Write bleibe keine Zustellwahrheit und jeder Subscribe liefere den aktuellen absoluten Snapshot. Es gibt also keine verlorene Wiederzustellpflicht. Der reale Rest ist der fehlende Abfluss beim Schliessen: der Verbindungsgriff bekommt einen beschraenkten Flush mit harter Frist nach dem v2-Muster, danach trennen, danach schliessen, und ein Zaehler `flush_abgelaufen` macht den Verlust sichtbar. **Der Coordinator wird nicht angefasst**; deshalb behalten alle 70 Store-Crash-Tests ihre Aussage. Matrixzeile H-07 | neu in `broker/tests/security_vectors.rs`: `verbindungsgriff_flusht_vor_dem_schliessen_und_zaehlt_die_frist` |
| G3-BUFFLUSH-002 | G3 LOW, neu | `broker/src/transport/server_v3.rs:2217`, `bootstrap_reject_schreiben`, schreibt den Ablehnungsrahmen und laesst ihn liegen; der Peer sieht eine abgerissene Pipe statt des Grundes | `transport/server_v3/verbindung.rs` | A | Der Ablehnungsrahmen laeuft ueber denselben beschraenkten Flush wie H-07, damit der Grund den Peer erreicht. Genau dafuer existiert das v2-Gegenstueck bereits. Matrixzeile H-08 | neu in `broker/tests/security_vectors.rs`: `bootstrap_reject_erreicht_den_peer` |
| G3-SAFETYDOC-001 | G3 LOW, neu | `broker/src/transport/server_v3.rs:2115`, in `client_sid_authentisieren` — gemessen ueber `:2070-2179` genau **18** `unsafe`-Vorkommen und **null** Sicherheitskommentare, darunter ein typengepunkter Deref und ein unaligned Write, im Authentisierungstor selbst | `transport/server_v3/auth.rs` | A | Jeder `unsafe`-Block bekommt seinen Sicherheitskommentar. Die Aufteilung geht voraus, weil die Funktion dadurch ein eigenes Modul mit genau einer Verantwortung wird und der Lint aus G2-CARGOLINT-001 die Zusage danach haelt. Die Ausrichtungszusicherung bei `broker/src/transport/server_v3.rs:2027` wandert mit. Matrixzeile H-22 | die Quellwache aus G2-CARGOLINT-001, nicht der Clippy-Lint |
| G3-STRCMP-001 | G3 LOW, neu | `broker/src/transport/pipetoken.rs:38`, `ist_probe_pipename`, ist ein roher Praefixvergleich auf einem nicht normalisierten Pipe-Pfad | `transport/pipetoken.rs` bleibt | A | Der Name wird vor dem Vergleich normalisiert: Praefixform vereinheitlichen und Gross- und Kleinschreibung nach ASCII falten, bevor die Erlaubnis entschieden wird. Die Erlaubnisform bleibt, was der Kommentar zusagt — eine Erlaubnis, keine Sperrliste | vorhandene `pipetoken`-Inlinetests, erweitert um Namensvarianten, die heute durchrutschen oder faelschlich abgewiesen werden |
| G3-STRSLICE-001 | G3, neu, LIKELY_FP | `broker/src/vertrag.rs:612`, `aufloesen`, schneidet den Referenznamen mit einem festen Byteindex ab | entfaellt | C | **An der Quelle als Fehlbefund entschieden.** `aufloesen` ist privat und nur ueber ein bereits geladenes Schema erreichbar; `Schema` wird ausschliesslich in `Schema::laden` bei `broker/src/vertrag.rs:475` gebaut, und `laden` prueft den ganzen Baum. Die Ladepruefung bei `broker/src/vertrag.rs:559-560` weist jede Referenz ab, die nicht mit dem lokalen Praefix beginnt, und bei `:569-571` zusaetzlich jede haengende Referenz. Das Praefix ist reines ASCII, der Byteindex liegt also immer auf einer Zeichengrenze. Die Schemawurzel ist ausserdem kein Peer-Eingang, sondern die eingebettete Vertragsdatei, siehe `broker/src/coordinator.rs:43-48` | keiner noetig; die vorhandenen Ladepruefungstests in `broker/src/vertrag.rs` sind die Wache |
| G3-NONDET-001 | G3 MEDIUM, neu | `broker/src/coordinator.rs:2648`, `persistenz_p0`, sucht das Befehlsziel mit einer Suche ueber die Linkmap allein nach Adresse, Session und Trennflag; die Verdraengung bei `broker/src/coordinator.rs:766-767` greift nur bei **abweichender** Nonce, sodass zwei lebende Links dieselbe Adresse tragen koennen | `coordinator/befehl.rs` und `coordinator/link.rs` | A | **Entscheidung an C-10.** C-10 sagt Verdraengung fuer die **neue** Nonce zu und schweigt zum Fall identischer Nonce — eine Luecke, keine gebrochene Zusage. Zwei Haelften: erstens wird das Ziel ueber die Linkidentitaet des Clients aufgeloest und die Adresse danach verifiziert, statt die Linkmap zu durchsuchen; das ist die bindende Zusage, weil die Reproduzierbarkeit des append-only Logs daran haengt. Zweitens verdraengt auch ein Hello mit identischer Nonce den aelteren Link, wodurch die Voraussetzung ganz entfaellt. **Falle fuer den Folge-Worker:** der Aliasbesitzer ist Instanz-ID und Nonce zusammengesetzt, siehe `broker/src/coordinator.rs:741`; bei identischer Nonce ist er fuer beide Links derselbe, der Aliaseintrag darf in diesem Zweig deshalb **nicht** entfernt werden, sonst zieht die Verdraengung dem ueberlebenden Link seine Wireadresse weg. Matrixzeile H-10 | neu in `broker/tests/coordinator_model.rs`: `p0_ziel_ist_bei_zwei_links_derselben_adresse_deterministisch`, mit permutierter Einfuegereihenfolge, und `hello_mit_identischer_nonce_verdraengt_den_alten_link_ohne_den_alias_zu_ziehen` |
| G3-UNWRAP-001 | G3 LOW, neu | `broker/src/coordinator.rs:3096`, in der Senkenimplementierung, sperrt den Standlock mit `expect`; die Datei traegt 54 solcher Stellen gegen nur 14 vergiftungstolerante | `coordinator/senke.rs` und alle Coordinator-Module | A | Dieselbe Aenderung wie G2-DROPPANIC-001, hier als eigener Primary gefuehrt: alle Sperrgriffe des Coordinators werden vergiftungstolerant. Damit macht eine einzelne Panik keinen dauerhaften Brokerausfall mehr. Matrixzeile H-04 | wie G2-DROPPANIC-001, plus eine Quellwache im Test, die zaehlt, dass kein `expect` auf dem Standlock mehr uebrig ist |
| G3-DROPSKIP-001 | G3 LOW, neu | `broker/src/lib.rs:990-997`, der geordnete Stopp nimmt beide Serverhandles und laesst sie fallen, ruehrt den Storeschreiber aber nicht an; er liegt bei `broker/src/lib.rs:847` im Brokerlauf und stirbt am Prozessende ohne geordnetes Schliessen | `lib.rs` bleibt | A | Der geordnete Stopp stoppt den Storeschreiber, bevor der Prozess endet: Reihenfolge erst Serverhandles, dann Store, damit kein Schreiber mehr Auftraege bekommt, wenn der Store zumacht. Das ist die Regel starten-gegen-stoppen aus `CLAUDE.md` im selben Aenderungssatz. **Falle fuer den Folge-Worker:** das Feld liegt bei `broker/src/lib.rs:847` blank im Brokerlauf, waehrend `StoreWriter::stoppen` bei `broker/src/store.rs:704` eine exklusive Referenz verlangt und der Brokerlauf nur geteilt aus einer statischen Zelle erreichbar ist. Das Feld bekommt deshalb dieselbe Huelle wie seine drei Geschwister im selben Typ, wird beim Stopp entnommen und fallengelassen; der vorhandene Destruktor bei `broker/src/store.rs:715` fuehrt den Stopp dann selbst aus. `broker_store_sicht` bei `broker/src/lib.rs:1074` muss den entnommenen Zustand ehrlich melden statt zu panisieren. Matrixzeile H-09 | neu in `broker/tests/broker_idle.rs`: `geordneter_stopp_schliesst_den_store` |

### 3.4 Registerregeln (3)

| ID | Herkunft | Fundstelle heute | Zielmodul | Einordnung | Fix-Zusage | Test |
|---|---|---|---|---|---|---|
| NAK-135 | Register, Gate-Lauf G3 | `broker/src/transport/server_v3.rs:1684-1700`, der Start bewaffnet genau zwei Listenerinstanzen; `PIPE_INSTANZEN` bei `:91` erlaubt 98, naemlich 96 Worker plus zwei Besitzlistener | `transport/server_v3/listener.rs` | A | **Dynamisch statt fest — nur so bleiben Deckel und Testvertrag unangetastet.** `PIPE_INSTANZEN = 98` bleibt, und beide bestehenden Tests bleiben **woertlich** unveraendert: `zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz` (`broker/tests/security_vectors.rs:327-356`, Assertion `bewaffnete_listener == 2` bei 96 Workern) und `ersatzlistener_fehler_behaelt_besitz_und_meldet_nicht_bereit` (`:299-325`, Assertion `== 1`). Die Zusage lautet deshalb: die Zahl der bewaffneten Instanzen ist eine **Zielgroesse** `min(LISTENER_RESERVE_MAX, PIPE_INSTANZEN - aktive_worker)`, und der Acceptor stellt sie **ausschliesslich im Annahmepfad** her, nie proaktiv beim Schleifeneintritt — proaktives Nachruesten braeche den zweiten Test, der nach genau einem fehlgeschlagenen Ersatz eine bewaffnete Instanz sieht. Bei 96 aktiven Workern ist die Zielgroesse `98 - 96 = 2`, genau der Stand, den der erste Test misst; bei wenigen Workern ist sie `LISTENER_RESERVE_MAX`. Die Invariante `aktive Worker + bewaffnete Listener <= PIPE_INSTANZEN` haelt damit jederzeit. `LISTENER_RESERVE_MAX = 16` ist die benannte Obergrenze; hart gedeckelt ist sie durch `WaitForMultipleObjects`, das hoechstens `MAXIMUM_WAIT_OBJECTS = 64` Handles annimmt (`windows-sys 0.60.2`, `Win32/System/SystemServices`), und der Acceptor wartet auf die Listenerereignisse **plus** sein Stopereignis (`broker/src/transport/server_v3.rs:1787-1800`) — also sind hoechstens 63 moeglich. Das feste Feld `[Some(erster), Some(zweiter)]` bei `:1780` wird dafuer ein `Vec`; das ist modulintern und beruehrt keine Signatur. Der Zaehler `bewaffnete_listener` behaelt seine Aussage. Der Backoff bleibt der Wiederherstellungsweg, mehr Instanzen verkuerzen ihn nur. Matrixzeile H-15 | die beiden genannten Startpfadtests bleiben **unveraendert** und sind der Beweis, dass die Zielgroesse an ihren beiden gemessenen Punkten dieselben Zahlen liefert; neu `ansturm_nach_neustart_findet_eine_scharfe_instanz` in `broker/tests/broker_idle.rs`, misst nach der ersten Annahme `bewaffnete_listener == LISTENER_RESERVE_MAX` und ueber den ganzen Ansturm die Invariante `aktive_worker + bewaffnete_listener <= PIPE_INSTANZEN` |
| NAK-136 | Register, Dirigentenregel 03.09.2026 | `broker/src/coordinator.rs:766-779` verdraengt den alten Link, ohne die Session dirty zu markieren; `control_hello_registrieren` endet bei `:875`, der Lock faellt schon bei `:864`. `control_ende` markiert bei `:904-910` nur, wenn der aktuelle Link noch der geschlossene ist — bei Verdraengung ist er bereits umgehaengt. Erst der Heartbeat bei `:1654-1655` und `:1660-1661` pusht | `coordinator/link.rs` | A | **Praezisiert die Registerregel vom 03.09.2026** (dort: Flush „noch im Hello-Zyklus"). Am Code ist diese Form nicht haltbar: `control_hello_registrieren` gibt die zu schliessenden Links bei `:864-875` erst an den Transport zurueck, und `broker/src/transport/server_v3.rs:2465-2468` beginnt ihren Abbau **danach** — ein Push vor der Rueckkehr liefe vor Ingress-Schluss, Kopplungsloesung, Joins und Trenncallbacks und verletzte C-06. Also: der Verdraengungszweig markiert die betroffene Session unter dem Lock nur als **dirty**; der Push haengt am Trennzyklus des verdraengten Links. Traeger ist `control_ende` (`:881-921`), das heute bei `:904-910` **nicht** markiert, weil `current_link` bei Verdraengung bereits umgehaengt ist — es flusht kuenftig am Ende, nach der Lockfreigabe, die schon dirty markierte Session. An dieser Stelle ist der coordinatorseitige Teil von C-06 vollstaendig: Ingress ist geschlossen (`server_v3.rs:2873`), Linkbesitz, Subscription, Telemetrieplatz und Interventionen sind unter demselben Lock entfernt. Die danach folgenden Transportschritte aendern **keinen** Zustand, den `snapshot_locked` liest: `control_getrennt` ist im Coordinator ein leerer Rumpf (`:3031`), `telemetrie_getrennt` beruehrt allein `telemetry_links` (`:3046-3052`), und `telemetry_links` kommt in `snapshot_locked` (`:2107-2183`) nicht vor. Die Frist steht in Matrixzeile H-16 und ergaenzt C-10 | neu in `broker/tests/coordinator_model.rs`: `verdraengung_flusht_den_snapshot_im_trennzyklus` — nach `control_hello` der neuen Nonce ist der Push-Zaehler **noch nicht** erhoeht, nach `control_ende` des alten Links ist er erhoeht, beides **ohne** Uhrenschritt |
| NAK-137 | Register, Gate-Lauf G3 | `broker/src/coordinator.rs:1758-1785`, `descriptor_setzen` ist `pub`, prueft nur die Adresse und hat am HEAD keinen Produktionsaufrufer; die Haertung auf `beobachtend` sitzt allein in `descriptor_aus_heartbeat` bei `:1743` | `coordinator/liveness.rs` | A | **Von den beiden Registeralternativen bleibt nur eine.** Ein `cfg(test)`-Zaun scheidet aus: `descriptor_setzen` wird von `broker/tests/store_crash_matrix.rs` an neun Stellen gerufen, und eine Integrationstestdatei bindet die Bibliothek ohne Testkonfiguration — der Zaun braeche also den Testbau und damit den Testvertrag, den das Gate schuetzt. Also die andere: der Setter validiert den Deskriptor gegen den v3-Vertrag, bevor er ihn uebernimmt. Der Vertrag pinnt die Aussageklasse per Konstante auf `beobachtend`, ein Beitragsdeskriptor faellt damit ab. **Vertraeglich mit allen neun Aufrufstellen**, weil die Tests ihren Deskriptor aus dem Snapshot des Coordinators selbst entnehmen und nur das Label aendern — geprueft an `broker/tests/store_crash_matrix.rs:2044-2046`. Matrixzeile H-17 | neu in `broker/tests/store_crash_matrix.rs`: `descriptor_setzen_weist_beitragsklasse_ab`; die neun vorhandenen Aufrufe bleiben unveraendert gruen |

**Haertung ausserhalb dieses Tickets.** `eq-copilot/plugin/vertrag/NakamaTelemetrie.cpp:558-568`
meldet `eintraege_zu_viele` und laeuft danach weiter ueber alle Eintraege — dieselbe
Arbeit, die H-20 auf der Rustseite abschneidet. Das ist kein Vertragsbruch: die
Verstossmenge des Cross-Language-Korpus bleibt auf beiden Seiten dieselbe. Es ist
aber dieselbe Erschoepfungskante im C++-Bein und gehoert in ein eigenes Ticket; die
Registerzeile zieht der Dirigent nach.

## 4. Verhaltensmatrix der A-Befunde

Form wie `docs/beweise/SONDE-011.md` Abschnitt B.2. Neue Zeilen tragen das
Praefix `H-`. Eine Zeile, die eine bestehende Zusage beruehrt, nennt sie beim
Namen; nur H-16 **ergaenzt** eine bestehende Zeile (C-10), alle uebrigen
schliessen eine Luecke, ohne eine abgenommene Zusage umzuschreiben.

Zwei A-Befunde stehen bewusst nicht in dieser Matrix, weil sie weder
Nebenlaeufigkeit noch Lebenszyklus, Rueckstau, Handle-Besitz oder
Sperrreihenfolge beruehren: G2-MSRV-001 und G3-STRCMP-001. Ihre Zusage steht
vollstaendig in Paragraph 3. G2-CARGOLINT-001, G2-SAFETYDOC-001 und
G3-SAFETYDOC-001 standen bis zur Nacharbeit Runde 1 ebenfalls hier; sie tragen
seither die gemeinsame Matrixzeile H-22, weil ihre Zusage erst durch eine
benannte Messung im Kanon-Bein pruefbar wird.

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| H-01 | Verbindungshandle ist im Abbruchregister veroeffentlicht, ein Thread haelt seinen Verbindungsgriff | Spawn-Fehler, Verbindungsende oder Serverstopp raeumt beide Fakten ab | **ZUSAGE:** Registereintrag und Handle haben **einen** Besitzer. Reihenfolge in genau einem Destruktor: unter der Registersperre austragen, danach schliessen. Das Register veroeffentlicht damit nie ein geschlossenes Handle, und der `unsafe`-Sendevertrag des Registers beschreibt eine Invariante, die der Compiler haelt. Der Abbruchaufruf wertet seinen Rueckgabewert aus; ein unerwartetes ungueltiges Handle wird gezaehlt, nicht verschluckt | Kein Zeitbudget; die Grenze ist die Reihenfolge unter derselben Sperre. `MAX_VERBINDUNGEN = 96` bleibt unveraendert | `broker/src/transport/server_v3.rs:1979`, `:1289`, `:975`, `:665-681`; G2-Befunde RAWFD-003 und UAF-001; G3-Befund OPAQUEPTR-001 | neu `broker/tests/security_vectors.rs`: `registeraustrag_geht_dem_close_voraus`, `cancel_auf_totes_handle_wird_gezaehlt` |
| H-02 | Ein Schreiberthread haengt in einem Write, der Joinversuch hat seine Frist verpasst | Der Verbraucher gibt den Joinhandle auf und verlaesst die Verbindungsfunktion | **ZUSAGE:** Der Registereintrag ueberlebt den abgeloesten Schreiber, **und ein ausdruecklicher Abbruchweg beendet ihn.** Reihenfolge: Ablosung (`schreiber_abgeloest` bei `:2907-2910`) → Eintrag der Verbindungs-ID in die neue Menge `HandleRegister::abgeloest` → der vorhandene Wachhund bricht sie bei **jedem** Tick erneut ab, so wie er heute nur faellige Bootstrap-IDs abbricht (`:1725-1743`) → der Schreiberthread endet → der letzte geteilte Verbindungsgriff faellt, sein Destruktor traegt die ID aus `offen` **und** `abgeloest` aus und schliesst erst danach. Ohne diesen Weg bliebe nach `BootstrapFrist`-Austrag bei `:2379` nur der Serverstopp. Eine abgeloeste Verbindung verbrennt damit keine Pipeinstanz dauerhaft. Der Zaehler `schreiber_abgeloest` behaelt seine heutige Aussage | Joinfrist unveraendert (`SENKE_FRIST`). Der Abbruch faellt spaetestens im naechsten Wachhundtick; der Takt ist heute das nackte `Duration::from_millis(100)` bei `:1721` und wird zur benannten Konstante `WACHHUND_TAKT = 100 ms` — modulintern, keine oeffentliche Signatur. Nach diesem Tick muessen die gehaltenen Handles auf null zurueckgehen | `broker/src/transport/server_v3.rs:2907-2910`, `:1314`, `:966-969`, `:1721`, `:1725-1743`, `:2379`, `:1283`; G2-Befund RAWFD-002, in G3 RAWFD-001 | neu `broker/tests/security_vectors.rs`: `abgeloester_schreiber_gibt_instanz_und_handle_zurueck` — erzwingt `schreiber_abgeloest` groesser null, wartet einen `WACHHUND_TAKT` ab und misst danach `V3Griff::gehaltene_handles() == 0` (`:1168`) bei weiterhin annehmendem Listener |
| H-03 | Der globale Standlock des Coordinators ist genommen, eine Zielliste steht bereits als Kopie | Push an eine fremde Senke | **ZUSAGE:** Fremde Senkenarbeit laeuft nie unter dem Standlock. Reihenfolge: unter dem Lock sammeln, Lock freigeben, danach pushen. Das ist die Invariante, die `flush_session` in seinem eigenen Kommentar bereits fuehrt und einhaelt; die beiden anderen Stellen ziehen nach. Eine reentrante Senke kann den Broker damit nicht auf dem globalen Lock verklemmen | Keine neue Frist; die Grenze ist die Sperrreihenfolge | `broker/src/coordinator.rs:1097` und `:1124`, `:3191`, Invariantenkommentar `:2267-2270`; G2-Befunde REENTRANT-001 und REENTRANT-002 | neu `broker/tests/coordinator_model.rs`: `senke_haelt_den_standlock_nicht_ueber_den_push` mit einer reentrant zugreifenden Senke |
| H-04 | Der Standlock des Coordinators ist vergiftet, weil ein Thread unter ihm panisch geworden ist | Beliebiger weiterer Zugriff auf den Coordinator | **ZUSAGE:** Eine einzelne Panik macht keinen dauerhaften Brokerausfall. Jeder Sperrgriff auf den Coordinatorstand ist vergiftungstolerant und arbeitet auf dem inneren Wert weiter — das Muster, das dieselbe Datei heute schon vierzehnmal fuehrt. Fachlich sichtbare Zustaende bleiben, was sie waren; es wird nichts stillschweigend zurueckgesetzt | Keine Frist; die Grenze ist, dass nach der Umstellung kein `expect` auf dem Standlock uebrig ist | `broker/src/coordinator.rs:3096`, `:2430`; G3-Befund UNWRAP-001 | neu `broker/tests/coordinator_model.rs`: `vergifteter_stand_beendet_den_prozess_nicht`, plus eine Quellwache, die die Zahl der `expect`-Stellen auf dem Standlock auf null prueft |
| H-05 | Eine v2-Verbindung wird abgebaut, ihr Registrierungsobjekt faellt | Der Destruktor ruft die Interventionssenke | **ZUSAGE:** Ein panischer Senkenaufruf verlaesst den Destruktor nicht. Reihenfolge: Register defensiv abraeumen, dann den Senkenaufruf gegen Panik gefasst fahren. Zusammen mit H-04 entsteht damit keine doppelte Panik im Abwickeln und kein Prozessabbruch, der den geordneten Stopp uebergeht | Keine Frist; die Grenze ist, dass der Destruktor unter keinem Zustand des Coordinators panisch endet | `broker/src/server.rs:341-350`, `:347`; `broker/src/coordinator.rs:2430`; G2-Befund DROPPANIC-001 | wie H-04, plus `vergifteter_stand_beendet_den_prozess_nicht` mit einem Abbau ueber den v2-Weg |
| H-06 | Eine Session hat mehrere Abonnenten, mindestens einer liest seine Pipe nicht | Snapshotzustellung aus dem Liveness-Tick | **ZUSAGE, genau EIN Zustellpfad:** Ein untrusted Peer bestimmt nicht den Takt des Coordinators. Snapshotzustellung wird eingereiht und kehrt sofort zurueck, wie die Messframe-Zustellung bei `:1049-1061`; das synchrone `recv_timeout(SENKE_FRIST)` bei `:1046` entfaellt ersatzlos. **Die Alternative mit einem Gesamtbudget ueber die Zielschleife ist gestrichen.** Die Buchhaltung loest sich asynchron auf: der `geschrieben`-Kanal des Ausgangseintrags (`:826`) traegt die Kompaktierung, sodass `snapshot_schuld_kompaktieren` weiter **nach vollstaendigem Write** laeuft und O-03 unberuehrt bleibt. Ein Ziel, das nicht liest, wird nach derselben Senkenfrist getrennt und gezaehlt. Die Reihenfolge im Tick bleibt, wie sie ist: Eviction im gesperrten Block (`coordinator.rs:1528`), Zustellung danach (`:1545`) — H-06 **haelt** diese Reihenfolge fest, statt sie zu verschieben | `SENKE_FRIST = 2000 ms` behaelt ihren Wert und wandert vom Coordinator zum Schreiber: `ov_schreiben` wartet bei `:648` heute `INFINITE` und bekommt diese Frist; Ablauf heisst `CancelIoEx`, neuer Zaehler `snapshot_frist_abgelaufen` und Trennen ueber den vorhandenen Weg bei `:2622-2628`. Der Coordinator-Tick hat danach **kein** Zustellbudget mehr, weil er nicht mehr wartet | `broker/src/coordinator.rs:2184`, `:2268-2282`, `:1528`, `:1545`; `broker/src/transport/server_v3.rs:1028-1046`, `:1049-1061`, `:826`, `:648`, `:2622-2628`; SONDE-011 C-06, E-03, O-03; G2-Befund CHANSTARVE-001 | neu `broker/tests/coordinator_model.rs`: `nicht_lesender_abonnent_haelt_den_liveness_tick_nicht_auf` mit vier benannten Assertionen — Tickdauer unter Budget; die lesenden Abonnenten haben ihren Snapshot; der nicht lesende ist getrennt und in `snapshot_frist_abgelaufen` gezaehlt; ein im selben Tick eviktierter Client steht in keiner Zielliste (Reihenfolgebeweis) |
| H-07 | Ein Snapshot ist in den Pipe-Ausgabepuffer geschrieben, der Peer hat ihn noch nicht abgeholt | Verbindungsende, Verdraengung oder Serverstopp schliesst das Handle | **ZUSAGE:** Vor dem Schliessen laeuft ein beschraenkter Abfluss, danach wird getrennt, danach geschlossen. Verpasst der Abfluss seine Frist, wird das gezaehlt und ist damit sichtbar, statt still zu verschwinden. **Was ausdruecklich NICHT geaendert wird:** die Bedeutung eines erfolgreichen Writes und die Reaktion des Coordinators darauf. O-03 erlaubt woertlich, die Snapshotschuld nach vollstaendigem Write zu kompaktieren, „ohne Empfaengerwirkung zu behaupten", und O-02 sagt, ein Pipe-Write sei keine Zustellwahrheit und jeder Subscribe liefere den aktuellen absoluten Snapshot aus der Projektion. Eine Wiederzustellpflicht geht also nicht verloren; der reale Rest ist der fehlende Abfluss und der fehlende Zaehler | Der Abfluss laeuft im eigenen Thread mit harter Frist nach dem v2-Muster; hoechstens ein aufgegebener Thread je nicht lesendem Peer und Verbindung, also durch `MAX_VERBINDUNGEN = 96` gedeckelt. Der Abfluss laeuft **nicht** im Coordinator-Cleanup, weil C-06 dort I/O verbietet | `broker/src/transport/server_v3.rs:678`; `broker/src/coordinator.rs:2280`; `broker/src/server.rs:875-892`; SONDE-011 O-02, O-03 und C-06; G3-Befund BUFFLUSH-001 | neu `broker/tests/security_vectors.rs`: `verbindungsgriff_flusht_vor_dem_schliessen_und_zaehlt_die_frist` |
| H-08 | Ein Bootstrap wird abgelehnt, der Ablehnungsrahmen ist geschrieben; auf der v2-Seite dasselbe beim Abbau | Die Verbindung wird geschlossen | **ZUSAGE:** Ein Peer, dem der Broker einen Grund geschrieben hat, bekommt ihn auch. Beide Wege gehen ueber denselben beschraenkten Abfluss aus H-07: schreiben, abfliessen lassen, trennen, schliessen. Auf der v2-Seite bekommt der Abbau denselben Weg, den die Reject-Pfade dort bereits gehen | Dieselbe harte Frist wie H-07 | `broker/src/transport/server_v3.rs:2217`; `broker/src/server.rs:863-864` und `:875`; G3-Befund BUFFLUSH-002, G2-Befund BUFFLUSH-001 | neu `broker/tests/security_vectors.rs`: `bootstrap_reject_erreicht_den_peer`, `v2_abbau_flusht_die_letzte_antwort` |
| H-09 | Broker laeuft, der Storeschreiberthread haelt die einzige Schreibverbindung | Geordneter Stopp | **ZUSAGE:** Der geordnete Stopp beendet den Storeschreiber, bevor der Prozess endet. Reihenfolge: Supervisor, v2-Handle, v3-Handle, **dann** Store — damit kein Weg mehr Auftraege einreicht, wenn der Store zumacht. Die Storesicht meldet den entnommenen Zustand ehrlich, statt zu panisieren | Kein neues Budget; die vorhandene Stoppreihenfolge wird nur um ein Glied verlaengert | `broker/src/lib.rs:847`, `:990-997`, `:1074`; `broker/src/store.rs:704`, `:715`; G3-Befund DROPSKIP-001 | neu `broker/tests/broker_idle.rs`: `geordneter_stopp_schliesst_den_store` |
| H-10 | Zwei lebende Control-Links tragen dieselbe effektive Adresse in derselben Session, keiner ist auf Trennen gesetzt | Ein P0-Befehl nennt diese Adresse als Ziel | **ZUSAGE, LUECKE ZU C-10 GESCHLOSSEN:** Erstens wird das Befehlsziel ueber die Linkidentitaet des Zielclients aufgeloest und die Adresse danach verifiziert; die Linkmap wird nicht mehr nach der ersten Adressuebereinstimmung durchsucht. Damit erzeugt gleiches Clientverhalten gleichen persistierten Zustand, und das append-only Log bleibt reproduzierbar. Zweitens verdraengt auch ein Hello mit **identischer** Nonce den aelteren Link genau wie eines mit abweichender, sodass die Voraussetzung gar nicht mehr entsteht. Reihenfolge im zweiten Teil wie in C-10 und C-06: alten Link auf Trennen setzen, Subscription entfernen, Link schliessen. **Ausnahme, die den Fall von C-10 unterscheidet:** bei identischer Nonce ist der Aliasbesitzer beider Links derselbe Schluessel; der Aliaseintrag wird in diesem Zweig deshalb nicht entfernt, sonst verloere der ueberlebende Link seine Wireadresse | Verdraengung ohne stale-Wartefrist, wie C-10 sie bereits fuehrt | `broker/src/coordinator.rs:2648`, `:766-779`, `:741`; SONDE-011 C-10 und C-06; G3-Befund NONDET-001 | neu `broker/tests/coordinator_model.rs`: `p0_ziel_ist_bei_zwei_links_derselben_adresse_deterministisch` mit permutierter Einfuegereihenfolge, `hello_mit_identischer_nonce_verdraengt_den_alten_link_ohne_den_alias_zu_ziehen` |
| H-11 | Die Ingressqueue einer Verbindung ist unterhalb ihrer Slotgrenze, traegt aber grosse Frames | Ein weiterer Frame trifft ein | **ZUSAGE:** Rueckstau wird in Slots **und** Bytes gemessen. Ueberschreitet das Bytebudget, wirkt das genau wie Slot-voll und in derselben Reihenfolge: erst den aeltesten P2 verwerfen, dann bei P0 oder P1 den Client trennen. P1 faellt nie still. Damit gilt die Politik aus Entwurf Paragraph 53.9 auch fuer die Byteachse, die die Schreiberseite laengst kennt | `CAP_INGRESS = 256` bleibt; neu `CAP_INGRESS_BYTES`, gemessen am Budget und am Budget plus eins | `broker/src/transport/warteschlange.rs:354`; `broker/src/transport/server_v3.rs:2770`; SONDE-011 B.5 Klassentabelle, Zeile Broker-Ingress; G2-Befund RESEXHAUST-002 | neu `broker/tests/transport_fuzz.rs`: `ingress_bytebudget_verwirft_p2_zuerst` |
| H-12 | Eine Session im Coordinatorstand hat ihren letzten Client verloren | Eviction oder Liveness-Sweep | **ZUSAGE:** verbinden gegen trennen gilt auch fuer die Sessionmap. Eine Session ohne verbleibenden Client wird beim selben Vorgang entfernt, der ihren letzten Client entfernt — in der Eviction und im Sweep. Zusaetzlich deckelt eine globale Sessiongrenze die Map, damit die Schranke ein spaeteres Einfuegen ohne Entfernen ueberlebt. E-03 bleibt unveraendert: dauerhafte Konfliktriegel gehen dabei nicht verloren | Neu `GLOBAL_SESSION_CAP` neben `GLOBAL_CLIENT_CAP = 128`, gemessen am Cap und am Cap plus eins | `broker/src/coordinator.rs:833`, `:1987`, `:1441`, `:1502`; SONDE-011 E-03 und E-04; G2-Befund RESEXHAUST-003 | neu `broker/tests/coordinator_model.rs`: `session_faellt_mit_dem_letzten_client`, `global_session_cap` |
| H-13 | Der v2-Sensor-ID-Pool ist voll, ein Teil der Eintraege ist laengst getrennt | Ein neues v2-Hello | **ZUSAGE:** Der Pool hat einen Rueckgabeweg mit **benannter Frist**. Einen periodischen v2-Tick gibt es nicht, deshalb laeuft der Sweep im Gate selbst, unter demselben Lock wie die Grenzpruefung und **vor** ihr: freigegeben wird jeder Eintrag, dessen Trennzeitpunkt die Frist erreicht hat; am Gate wird danach der am laengsten getrennte Eintrag verdraengt. Der harte Reject bleibt genau dann, wenn alle Plaetze wirklich verbunden sind — dieselbe stale-first-Ordnung, die E-04 fuer den v3-Weg bereits zusagt | Poolgrenze `MAX_SENSOR_IDS = 128` unveraendert. Neu `SENSOR_ID_RUECKGABE_MS = 60_000`, gleich dem Leerlaufhorizont `BROKER_IDLE_ENDE_MS`, den der Broker fuer sich selbst fuehrt. Grenze **inklusiv**: frei ab `jetzt - getrennt_seit_ms >= SENSOR_ID_RUECKGABE_MS`. Verdraengungsordnung stale-first, dann aeltester Trennzeitpunkt | `broker/src/server.rs:688-701`, `:53`; `broker/src/lib.rs:39`, `:120`, `:457-458`, `:500`; SONDE-011 E-04; G2-Befund RESEXHAUST-004 | neu inline in `broker/src/server.rs`: `v2_sensorpool_gibt_getrennte_ids_zurueck` misst ueber `Register::getrennte_ids_freigeben_zu(jetzt)` die drei Kanten **Frist minus eins** (Eintrag bleibt), **exakt Frist** (Eintrag faellt) und **alle 128 verbunden** (Reject bleibt) |
| H-14 | Zwei Hellos kollidieren auf einem Aliasbesitzer; Quarantaene und dauerhafter Konfliktriegel entstehen | Weitere Kollisionen desselben Angreifers | **ZUSAGE:** Beide Speicher sind gedeckelt. Ist der Deckel erreicht, wird die Verbindung abgewiesen — der fail-closed-Ausgang, den C-07 ohnehin fordert. Die Aufloesung eines Riegels raeumt zusaetzlich den passenden Aliaseintrag, sodass Riegel und Alias nicht auseinanderlaufen. **Unveraendert bleibt C-07 im Kern:** keine zeitbasierte Freigabe, Aufloesung ausschliesslich ueber die explizite Neu-ID, kein Routing vor erfolgreichem Riegel-Restore | Neuer Deckel auf Quarantaene und auf der persistenten Riegeltabelle, gemessen am Deckel und am Deckel plus eins | `broker/src/instance_alias.rs:163-164`, `:180`; `broker/src/store.rs`, Tabelle der Konfliktriegel; SONDE-011 C-07 und E-03; G2-Befund RESEXHAUST-005 | neu `broker/tests/store_crash_matrix.rs`: `quarantaene_deckel_weist_ab_statt_zu_wachsen`, `guard_aufloesung_raeumt_den_aliaseintrag` |
| H-15 | Der v3-Listener ist gestartet und haelt zwei bewaffnete Instanzen | Ein Ansturm vieler Clients unmittelbar nach dem Brokerneustart | **ZUSAGE:** Die Zahl der bewaffneten Instanzen ist **dynamisch** und folgt der Zielgroesse `min(LISTENER_RESERVE_MAX, PIPE_INSTANZEN - aktive_worker)`. Der Acceptor stellt sie **ausschliesslich im Annahmepfad** her, nie proaktiv beim Schleifeneintritt. Damit findet ein Ansturm nach dem Neustart eine scharfe Instanz, statt reihum auf besetzte Pipes zu laufen; bei 96 aktiven Workern sind es weiterhin genau zwei. Der Backoff des Clients bleibt der Wiederherstellungsweg — mehr Instanzen verkuerzen ihn nur. Der Zaehler der bewaffneten Listener behaelt seine Aussage und misst die Zielgroesse | Obergrenze bleibt `PIPE_INSTANZEN = MAX_VERBINDUNGEN + 2 = 98`; alle Instanzen desselben Namens muessen denselben Wert nennen. **Invariante jederzeit:** `aktive_worker + bewaffnete_listener <= PIPE_INSTANZEN`. Neu `LISTENER_RESERVE_MAX = 16`, hart gedeckelt durch `MAXIMUM_WAIT_OBJECTS = 64` minus dem Stopereignis, also hoechstens 63 | `broker/src/transport/server_v3.rs:1684-1705`, `:1780`, `:1787-1800`, `:1834-1876`, `:87-91`; `broker/tests/security_vectors.rs:299-325`, `:327-356`; Register NAK-135; Entwurf Paragraph 48.3 | die beiden vorhandenen Startpfadtests in `broker/tests/security_vectors.rs` bleiben **woertlich unveraendert** und messen die Zielgroesse an ihren beiden Punkten (1 und 2); neu `broker/tests/broker_idle.rs`: `ansturm_nach_neustart_findet_eine_scharfe_instanz` misst nach der ersten Annahme `LISTENER_RESERVE_MAX` und ueber den Ansturm die Invariante |
| H-16 | Dieselbe Instanz-ID und Session-Epoche hat eine aktive alte Nonce mit laufender Subscription | Eine neue Nonce registriert sich und verdraengt die alte Verbindung | **ERGAENZUNG ZU C-10:** C-10 sagt heute Verdraengung, vollstaendiges Ende der alten Subscription nach C-06 und Tombstone des alten Clients zu, nennt aber **keine Frist fuer den Snapshot-Push**. Sie wird ergaenzt, **und zwar hinter dem Cleanup, nicht davor**: der Coordinator markiert die betroffene Session in `control_hello_registrieren` unter demselben Lock nur als **dirty**; geflusht wird sie im Trennzyklus des verdraengten Links, ausgeloest am Ende von `control_ende` nach der Lockfreigabe. Ein Push noch vor der Rueckkehr aus dem Hello — die urspruengliche Registerform — liefe vor Ingress-Schluss, Kopplungsloesung, Joins und Trenncallbacks und verletzte C-06, weil `server_v3.rs:2465-2468` den Abbau erst **nach** der Rueckgabe beginnt. An der neuen Stelle ist der coordinatorseitige Teil von C-06 vollstaendig, und die verbleibenden Transportschritte beruehren keinen Zustand, den `snapshot_locked` liest. Alles Uebrige an C-10 bleibt woertlich, insbesondere der fail-closed-Duplikatkonflikt, wenn die verdraengte Nonce erneut meldet | Push innerhalb des Trennzyklus des verdraengten Links, also **ohne** Uhrenschritt und ohne Heartbeat-Takt; bisher bis zu `HEARTBEAT_INTERVAL_MS = 1000` spaeter. Bleibt `control_ende` aus, weil kein Transportcallback kommt, bleibt der Heartbeat der Rueckfall — die Session ist dirty markiert und geht nicht verloren. Der Push selbst laeuft ohne Coordinator-Lock, wie C-09 es verlangt | `broker/src/coordinator.rs:766-779`, `:864`, `:875`, `:881-921`, `:904-910`, `:1654-1661`, `:2107-2183`, `:3031`, `:3046-3052`; `broker/src/transport/server_v3.rs:2465-2468`, `:2873`, `:2880`; SONDE-011 C-10, C-09 und C-06; `docs/beweise/G3.md` Paragraph 5, Zeile Bridge- und PID-Wechsel; Register NAK-136 | neu `broker/tests/coordinator_model.rs`: `verdraengung_flusht_den_snapshot_im_trennzyklus` — nach `control_hello` der neuen Nonce ist der Push-Zaehler **noch nicht** erhoeht, nach `control_ende` des alten Links ist er erhoeht, beides ohne Uhrenschritt |
| H-17 | Ein Link ist angemeldet; ein Aufrufer setzt einen Sondendeskriptor direkt | Der Setter uebernimmt den Deskriptor in Client, Snapshot, Store und Push | **ZUSAGE:** Der Setter prueft den Deskriptor gegen den v3-Vertrag, bevor er ihn uebernimmt — nicht nur die Adresse. Damit gilt fuer jeden Weg dieselbe Haertung, die `descriptor_aus_heartbeat` heute allein traegt, und kein spaeterer Aufrufer kann sie umgehen. Der Vertrag pinnt die Aussageklasse per Konstante auf `beobachtend`; ein Beitragsdeskriptor faellt ab, bevor er in den Snapshot geraet | Keine Frist; die Grenze ist der v3-Vertrag selbst. Ein `cfg(test)`-Zaun scheidet aus, weil er den Bau der Integrationstests braeche und damit den Testvertrag, den das Gate schuetzt | `broker/src/coordinator.rs:1758-1785`, `:1712-1756`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json`, Aussageklasse als Konstante; `docs/beweise/G3.md` Paragraph 4; Register NAK-137 | neu `broker/tests/store_crash_matrix.rs`: `descriptor_setzen_weist_beitragsklasse_ab`; die neun vorhandenen Aufrufe bleiben unveraendert gruen |
| H-18 | Der Store ist im Leerlauf, das Schreibprotokoll ist ueber der Schwelle | Der Leerlauf-Checkpoint laeuft | **ZUSAGE:** Nur ein erfolgreicher Checkpoint merkt sich den Erfolg. Ein gescheiterter wird gezaehlt und beim naechsten Leerlauf erneut versucht; die Storesicht meldet den Fehlschlag, statt weiter gesund zu behaupten. Das ist die Regel aus der Pruefliste, dass Rueckgabewerte einer Politik ausgewertet und nicht ignoriert werden | Schwellen unveraendert; die erwartete Zahl belegter Checkpoints bleibt ihre heutige Konstante | `broker/src/store.rs:778-786`; SONDE-011 B.4; G2-Befund RESDISC-001 | neu `broker/tests/store_crash_matrix.rs`: `gescheiterter_leerlauf_checkpoint_wird_wiederholt` |
| H-19 | Ein v3-Verbindungsthread ist beendet, sein Joinergebnis liegt vor | Der Acceptor erntet fertige Threads | **ZUSAGE:** Das Joinergebnis wird ausgewertet. Eine Panik in einem Verbindungsthread erhoeht einen eigenen Zaehler und ist damit sichtbar — so wie das v2-Ende es bereits tut. Ohne diesen Zaehler entfernt der Broker genau das Erkennungssignal fuer die Fehlerklasse, die dieser Review jagt, und zwar am Endpunkt, der angreiferkontrollierte Bytes parst | Der Zaehler muss im Normalbetrieb strukturell null sein; ein Test, der ihn absichtlich hebt, belegt, dass er ueberhaupt zaehlt | `broker/src/transport/server_v3.rs:1411-1428`, `:1426`; `tools/dirigent/pruefliste.md`, Abschnitt A; G2-Befund RESDISC-002 | neu `broker/tests/transport_fuzz.rs`: `panik_im_verbindungsthread_wird_gezaehlt` |
| H-20 | Ein Telemetrieframe traegt mehr Eintraege, als die Grenze erlaubt | Die Vertragspruefung laeuft | **ZUSAGE, nur der Rust-Leser:** Die Eintragsgrenze ist ein Tor, kein Etikett. Ist sie ueberschritten, kehrt `pruefe` sofort kanonisch zurueck, statt danach noch jeden Eintrag einzeln zu bewerten. Die Verstosszahl waechst damit nicht mehr mit der Eintragszahl, und ein einzelner Frame kann keinen Ingressthread mehr auslasten. **Die Abweisungsentscheidung bleibt identisch** zu heute und zum C++-Leser, gemessen am bestehenden Cross-Language-Korpus. Nach aussen ist die Verstossliste nicht sichtbar: `p2` verwirft das `Err(_)` und meldet den festen Grund `FeatureBatchUngueltig`; eine Nachrichtenform oder ein Telemetriezaehler ueber Verstosszahlen existiert nicht. **Der Satz zum C++-Bein entfaellt**; die dortige Spiegelung steht als benannte Haertung ausserhalb dieses Tickets unter Paragraph 3.4 | Eintragsgrenze `MAX_EINTRAEGE = 32` unveraendert, gemessen an der Grenze und an der Grenze plus eins. Der zusaetzliche Deckel auf die Gesamtzahl der Verstoesse **entfaellt**: die fruehe Rueckkehr bindet die Arbeit bereits, und ein zweiter, an keinem Fixture messbarer Deckel waere genau die unbelegte Zusage, die diese Runde schliesst | `broker/src/telemetrie.rs:278-316`, `:81`, `:342`; `broker/src/coordinator.rs:3082-3086`; `eq-copilot/fixtures/v3/flatbuffers/MANIFEST.json` (100 Fixtures, `ungueltig/eintraege-33.bin`); `eq-copilot/plugin/vertrag/NakamaTelemetrie.cpp:558-568`; `tools/dirigent/pruefliste.md`, Abschnitte A und C; G2-Befund RESEXHAUST-001 | der vorhandene Test `fb_korpus_klassifiziert_wie_das_manifest` in `broker/tests/contract_cross_language.rs` haelt Urteil und vollstaendige Verstossmenge aller 100 Fixtures; neu im selben Test eine Grenzprobe, die einen Batch mit `MAX_EINTRAEGE + 1` Eintraegen im Speicher baut und genau `/eintraege eintraege_zu_viele` erwartet. `broker/src/telemetrie.rs` traegt heute null Inlinetests |
| H-21 | Ein dauerhafter Konfliktriegel steht auf einer effektiven Adresse | Push-, Dispatch- oder Registrierungspruefung schlaegt die Adresse nach | **ZUSAGE:** Die Riegelabfrage ist unempfindlich gegen die Schreibweise der SID. Ein Peer, der dieselbe SID anders schreibt, schuettelt den dauerhaften fail-closed-Riegel nicht mehr ab; alle elf Abfragestellen gehen ueber denselben gefalteten Index, und die drei schreibenden sowie die zwei loeschenden Stellen pflegen ihn mit. **Was ausdruecklich unveraendert bleibt:** die gespeicherte Form der effektiven Adresse. Altstaende finden ihre eigenen Riegel damit weiterhin, und weder Wire- noch State-Vertrag wird beruehrt. Die Kanonisierung an der Vertrauensgrenze ist die getrennte B-Zeile aus Paragraph 3.1 | Keine Frist; die Grenze ist, dass die gespeicherten Bytes gleich bleiben | Lesend `broker/src/coordinator.rs:1004`, `:1151`, `:1796`, `:1849`, `:1864`, `:1982`, `:2027`, `:2208`, `:2491`, `:2505`, `:2708`; schreibend `:606`, `:795`, `:1578`; loeschend `:2320`, `:2323`; Adressbildung `:3203`; `broker/src/transport/bootstrap.rs:150-151`; SONDE-011 C-07; G2-Befund STRCMP-001 | neu `broker/tests/store_crash_matrix.rs`: `konfliktriegel_greift_auch_bei_anderer_sid_schreibweise` |
| H-22 | Ein `unsafe`-Block oder ein `unsafe impl` steht in `broker/src/**` | Jede Uebersetzung im Kanon-Bein A4 | **ZUSAGE:** Jede `unsafe`-Stelle traegt ihre Begruendung, und eine Wache haelt das dauerhaft — im vorhandenen Bein, ohne ein neues. Drei Teile: `[lints.rust] unsafe_op_in_unsafe_fn = "deny"` wirkt im Bau und greift auf die einzige `unsafe fn` des Crates; eine **Quellwache als Test** misst den Sicherheitskommentar an jeder Stelle; `[lints.clippy] undocumented_unsafe_blocks = "deny"` steht in derselben Tabelle, **beweist im Kanon aber nichts**, weil kein Bein Clippy faehrt. Die Clippy-Verdrahtung ist benannte Haertung ausserhalb dieses Tickets: `tools/` bleibt unangetastet und die Beinzahl bleibt 40 | Fenster der Wache: ein mit `// SAFETY:` beginnender Kommentar in den **drei** vorangehenden nicht-leeren Zeilen. Am Stand `85fb5f2` gemessen: **100** Stellen, **59** erfuellt, **41** offen — der Test ist erst mit 100 von 100 gruen. `[lints]` gilt ab Cargo 1.74, gemessen `cargo 1.93.1` | `broker/Cargo.toml`; `broker/src/store.rs:1589`; `broker/src/server.rs:889-890`; `broker/src/transport/server_v3.rs:2070-2179`; `tools/beweise.ps1:457-460`, `:465-468`, `:775`; G2-Befunde CARGOLINT-001 und SAFETYDOC-001; G3-Befund SAFETYDOC-001 | neu `broker/tests/security_vectors.rs`: `jeder_unsafe_block_traegt_einen_safety_kommentar` |

## 5. Schnittplan

Grundlage sind der Schnittvorschlag aus Register NAK-121 (Transport-Framing,
Session und Coordinator, Store und Outbox), Entwurf Paragraph 53.4 und 53.9 und
der heutige Code. Die Verantwortungsgrenzen aus Paragraph 53.4 sind verbindlich,
die Namen duerfen praezisiert werden — das steht dort woertlich.

**Alle Zielpfade unten sind relativ zu `broker/src/` und noch nicht angelegt.**

### 5.1 Gemessener Ausgangsstand

| Datei | Zeilen gesamt | davon Produktion | davon `mod tests` | Inline-Tests |
|---|---:|---:|---:|---:|
| `broker/src/transport/server_v3.rs` | 5095 | 3125 | 1970 | 31 |
| `broker/src/coordinator.rs` | 3482 | 3217 | 265 | 14 |
| `broker/src/store.rs` | 1774 | 1558 | 216 | 3 |

Gemessen mit `wc -l` und einer Zaehlung der Testattribute am Stand `85fb5f2`.
Der groesste einzelne Block ist der Testmodul von `server_v3.rs`; die groesste
einzelne Produktionseinheit ist der Block `impl Coordinator` mit rund 2430
Zeilen und 84 Methoden.

### 5.2 Die zwei Techniken, auf denen der Schnitt ruht

**Privates Untermodul plus Re-Export.** Jedes Ziel wird als `mod x;` **privat**
deklariert, und der uebergeordnete `mod.rs` traegt `pub use x::{...}`. Damit gibt
es weiterhin genau **einen** oeffentlichen Pfad je Element. Waeren die Module
`pub mod`, entstuende ein zweiter gueltiger Pfad — das waere eine Erweiterung der
oeffentlichen Flaeche und damit genau die Vertragsaenderung, die der Gate-Satz
ausschliesst. Ausnahme: `broker/src/transport/mod.rs` fuehrt `server_v3` bereits
als `pub mod`; das bleibt, wie es ist.

**Geschwistermodule brauchen `pub(super)`.** In Rust ist ein privates Element im
definierenden Modul **und in allen Nachfahren** sichtbar — in den **Geschwistern
nicht**. Solange `Stand`, `ClientStand` und `LinkStand` in `coordinator/mod.rs`
blieben, saehen `link.rs`, `liveness.rs` und `flush.rs` sie; sobald sie nach
Paragraph 5.4 in `coordinator/zustand.rs` wandern, sind sie Geschwister, und der
Zugriff faellt mit E0603 beziehungsweise E0616. Die Aufteilungscommits waeren
dann nicht mehr einzeln uebersetzbar. Deshalb gilt: **jedes heute private
Element, das seinen Elternmodul verlaesst, wird `pub(super)`** — der Typ, seine
Felder, seine Methoden und, bei Tupelstrukturen, auch das Tupelfeld, denn
`pub(super)` auf dem Typ nimmt die Felder nicht mit. `pub(super)` ist
`pub(in super)` und macht das Element im **ganzen Teilbaum** des Elternmoduls
sichtbar, also fuer alle Geschwister. Trait-Implementierungen brauchen nichts:
sie sind ueberall sichtbar, wo Trait und Typ es sind. Und jedes Untermodul darf
weiterhin einen eigenen Block `impl Coordinator` tragen; Rust erlaubt beliebig
viele Blocks fuer denselben Typ innerhalb eines Crates.

`pub(crate)` wird **nirgends** gebraucht. Jedes Element, das heute privat ist,
ist heute schon nur innerhalb seiner eigenen Datei erreichbar — kein anderes
Crate-Modul kann es benutzen, sonst waere es nicht privat. Nach der Aufteilung
genuegt daher ausnahmslos `pub(super)`.

Weder `pub(super)` noch `pub(crate)` ist eine oeffentliche Signatur, die ein
anderer Verbraucher liest: kein solches Element ist aus `lib.rs` heraus
erreichbar, und genau dort verlaeuft die Grenze des Gate-Textes. Neue
uneingeschraenkte `pub`-Items entstehen ausschliesslich als `pub use`-Re-Export
im jeweiligen `mod.rs`, und zwar nur fuer Elemente, die vorher schon `pub`
waren. Paragraph 5.8 misst genau das.

Die Richtung dieses Eingriffs ist sicher: ein **fehlendes** `pub(super)` ist ein
Uebersetzungsfehler und faellt sofort auf, ein **ueberfluessiges** ist folgenlos.
Ein Element, das nach der Verschiebung nur noch in seinem eigenen Modul
verwendet wird, bleibt privat.

**Wie die Anker in 5.3 bis 5.5 zu lesen sind.** Eine Zeilennummer zeigt auf die
**erste** Zeile des Elements einschliesslich seines Attributs; bei einem Typ mit
`#[derive(..)]` ist das die Attributzeile, nicht die `struct`-Zeile. Und wer
einen Typ nennt, nimmt alles mit, was zu ihm gehoert: seine `impl`-Bloecke, sein
`Drop` und seine `unsafe impl`-Markierungen. Sie sind deshalb nicht einzeln
aufgezaehlt. Innerhalb der drei Tabellen ist kein Anker zweimal vergeben; das ist
maschinell gegengelesen.

### 5.3 `transport/server_v3.rs` in neun Module

| Zielmodul | Verantwortung | Was dorthin wandert |
|---|---|---|
| `transport/server_v3/mod.rs` | Modulkopf, Modulliste, Re-Exports, die oeffentlichen Konstanten | Der heutige Kopfkommentar `:1-45`; `MAX_VERBINDUNGEN` `:87`, `PIPE_INSTANZEN` `:91`, `BOOTSTRAP_FRIST` `:164`, `RATE_PRO_SEKUNDE` `:168`, `CAP_WRITER` `:173`, `SENKE_FRIST` `:195`; die drei Schema-Minor-Konstanten `:178-180` und `schema_minor_bekannt` `:182` |
| `transport/server_v3/senke.rs` | Die schmale Schnittstelle zwischen Transport und Coordinator | `Senke` `:233`, `ZaehlSenke` `:269` mit ihrem Impl, `ControlAnmeldung` `:198` mit ihrem Impl |
| `transport/server_v3/win_handles.rs` | Win32-Besitz und ueberlappende I/O — **der Ort von H-01, H-02 und H-07** | `Ereignis` `:338`, `ListenerInstanz` `:370`, `EndeSignal` `:505`, `Verbindungsgriff` `:665`, `SicherheitsSpur` `:686`, `HandleRegister` `:966`, `HandleEintrag` `:2221` (entfaellt mit H-01), `TokenGriff` `:2029`, `leeres_overlapped` `:548`, `IoAusgang` `:557`, `io_fehler_deuten` `:566`, `ov_lesen` `:578`, `ov_schreiben` `:627`, `alle_io_abbrechen` `:1283`, `io_abbrechen` `:1295` |
| `transport/server_v3/auth.rs` | Das Authentisierungstor — **der Ort von G3-SAFETYDOC-001** | `client_sid_authentisieren` `:2070`, `VerbindungsSicherheitsCleanup` `:2039`, die Ausrichtungszusicherung `:2027` |
| `transport/server_v3/queues.rs` | Ein- und Ausgang je Verbindung — **der Ort von H-11** | `Eingang` `:724`, `Ausgang` `:811`, `Ausgangsart` `:816`, `AusgangEintrag` `:823` |
| `transport/server_v3/griff.rs` | Serverhandle, Sender, Statistik, Workerplaetze — **der Ort von H-06 und H-19** | `V3Griff` `:977` mit Impl `:1150` und Drop `:1277`, `V3Closer` `:1002`, `V3Sender` `:1014` mit dem `SessionPush`-Impl `:1064`, `V3Statistik` `:1075`, `join_mit_frist` `:1314`, `fertige_ernten` `:1411`, `WorkerPlatz` `:1485`, `WorkerPlatzProbe` `:1498`, `TestHilfsthread` `:1500`, `worker_reservieren` `:1535` |
| `transport/server_v3/listener.rs` | Start, Acceptor, Instanzverwaltung — **der Ort von H-15** | Die vier Startfunktionen `:1569`, `:1577`, `:1592`, `:1614`, `v3_server_starten_intern` `:1637`, `naechste_instanz` `:1438`, die sieben Cancel-Testmarken `:1557-1563`, `V3StartTestFehler` `:98`, `V3AuthTestFehler` `:109`, `V3UebergabeBarriere` `:122`, `V3SecurityTestOptionen` `:150` |
| `transport/server_v3/verbindung.rs` | Der Verbindungsweg vom Bootstrap bis zum letzten Frame — **der Ort von H-08** | `verbindung_bedienen` `:2252`, `BootstrapRahmenStand` `:2180`, `bootstrap_rahmenstand` `:2186`, `bootstrap_reject_schreiben` `:2202`, `BootstrapFrist` `:2234`, `Senkenruf` `:1344` |
| `transport/server_v3/trennung.rs` | Die Trennreihenfolge aus C-06 als eigene Einheit | `TrennStand` `:2944`, `TrennMelder` `:2970`, `TrennRegister` `:2979`, die vier Melderfunktionen `:2981`, `:2991`, `:3008`, `:3020`, `auf_telemetrie_getrennt_warten` `:3033`, `kopplung_loesen` `:3080`, `melden_getrennt` `:3115` |

**Was `pub(super)` bekommt.** Ueber die Anker der Tabelle gezaehlt, am Stand
`85fb5f2`, ohne die `impl`-Bloecke (die tragen keine eigene Sichtbarkeit):
`win_handles.rs` **15 von 15**, `trennung.rs` **10 von 10**, `verbindung.rs`
**6 von 6**, `queues.rs` **4 von 4**, `auth.rs` **3 von 3**, `griff.rs` **5**
(`join_mit_frist`, `fertige_ernten`, `WorkerPlatz`, `TestHilfsthread`,
`worker_reservieren`), `listener.rs` **9 in drei Ankergruppen** (`naechste_instanz`,
`v3_server_starten_intern` und die sieben Konstanten `CANCEL_VOR_READ_*` bei
`:1557-1563`, die der Schreiberthread in `verbindung.rs` mitliest),
`senke.rs` **0**.
`mod.rs` bleibt unveraendert privat, weil es der Elternmodul ist und seine
Kinder es ohnehin sehen. In `win_handles.rs` sind es namentlich `Ereignis`,
`ListenerInstanz`, `EndeSignal`, `Verbindungsgriff`, `SicherheitsSpur`,
`HandleRegister`, `HandleEintrag`, `TokenGriff`, `IoAusgang`,
`leeres_overlapped`, `io_fehler_deuten`, `ov_lesen`, `ov_schreiben`,
`alle_io_abbrechen` und `io_abbrechen` — die drei Tupelstrukturen `Ereignis`,
`EndeSignal` und `TokenGriff` zusaetzlich in ihrem Tupelfeld. Keines dieser
Elemente ist heute aus `lib.rs` erreichbar; keines wird es dadurch.

Die 31 Inline-Tests wandern mit dem Element, das sie messen; ihre Summe bleibt
31. Kein Test wird umbenannt oder inhaltlich geaendert. **Eine benannte
Ausnahme:** der Inline-Test `bootstrap_und_senkenfrist_minus_eins_und_exakt_sind_inklusive`
liest bei `broker/src/transport/server_v3.rs:3146` seinen eigenen Quelltext ueber
`include_str!("server_v3.rs")`. Nach der Umbenennung liegt er in
`transport/server_v3/mod.rs`, und der relative Pfad muss `"mod.rs"` heissen —
sonst uebersetzt schon der Umbenennungscommit nicht. Paragraph 5.6 fuehrt diese
und die uebrigen Pfadmakros vollstaendig.

**Oeffentliche Flaeche, die unveraendert bleiben muss.** Aus `broker/tests/` und
`broker/src/bin/` werden heute genau diese Elemente unter dem Pfad
`eqcop_broker::transport::server_v3::` gelesen: `PIPE_INSTANZEN`,
`MAX_VERBINDUNGEN`, `Senke`, `ZaehlSenke`, `ControlAnmeldung`, `V3Griff`,
`V3Sender`, `V3AuthTestFehler`, `V3SecurityTestOptionen`, `V3StartTestFehler`,
`V3UebergabeBarriere`, `v3_server_starten`,
`v3_server_starten_mit_epoch_und_sender` und
`v3_server_starten_fuer_security_vectors`. Dazu kommen aus `broker/src/lib.rs`
`V3Griff`, `V3Sender` und `v3_server_starten_mit_epoch_und_sender`. Alle
erscheinen als `pub use` im neuen `mod.rs`; die uebrigen heute oeffentlichen
Elemente (`v3_server_starten_mit_epoch`, `V3Closer`, `V3Statistik`,
`BOOTSTRAP_FRIST`, `RATE_PRO_SEKUNDE`, `CAP_WRITER`, `SENKE_FRIST`,
`WorkerPlatzProbe`) ebenfalls, damit die Flaeche weder waechst noch schrumpft.

### 5.4 `coordinator.rs` in dreizehn Module

| Zielmodul | Verantwortung | Was dorthin wandert |
|---|---|---|
| `coordinator/mod.rs` | Modulkopf, Modulliste, Re-Exports, die oeffentlichen Konstanten, der Typ selbst und sein Aufbau | Kopfkommentar `:1-13`; die Konstanten `:27-39`; `Coordinator` `:469`, `Default` `:563`, `SESSION_FLUSH_SCHLOSS_ANZAHL` `:484`, `CoordinatorFlushTestHaken` `:219`; die Konstruktoren `mit_uhr` `:579`, `mit_store` `:595`, `instant_mit_store` `:626`, `session_push_setzen` `:634`, `flush_test_haken_setzen` `:639`; `SessionPush` `:201`; `effektive_adresse` `:3203`, `persistenz_utc_ms` `:3210` |
| `coordinator/zustand.rs` | Der fluechtige Sessiongraph als Datenmodell | `ClientKey` `:251`, `SessionKey` `:277`, `ClientStand` `:283`, `SessionStand` `:305`, `LinkStand` `:310`, `Subscription` `:327`, `Intervention` `:333`, `LiveMessframe` `:338`, `Messfehler` `:348`, `P2RejectGrund` `:355`, `Lautheitsstand` `:386`, `Stand` `:393` mit `Default` `:429`, `SessionCommandWirkung` `:422`, `JSON_SAFE_INTEGER_MAX` `:376`, `P2_REJECT_KATALOG` `:378` |
| `coordinator/uhr.rs` | Monotone Zeit, getrennt von der Anzeigezeit | `MonotonicClock` `:154`, `InstantClock` `:159`, `ManualClock` `:181` |
| `coordinator/schema.rs` | Der v3-Vertrag als Leser, ohne Sessionwissen | `v3_schema_wurzel` `:43`, `v3_schema_minor_0_wurzel` `:50`, `v3_schema` `:97`, `v3_nachricht_lesen_mit_minor` `:113`, `v3_nachricht_lesen_beliebig_mit_minor` `:122`, `v3_nachricht_lesen` `:128`, `projektion_mit_aktuellem_lauf` `:132`, `JSON_SCHEMA_MINOR_AKTIV` `:41` |
| `coordinator/sicht.rs` | Was der Coordinator nach aussen zeigt — **der Ort von G2-FLOATEDGE-001** | `ControlRegistrierung` `:487`, `ClientModellSicht` `:512`, `Lautheitszustand` `:523`, `MessframeSicht` `:533`, `SessionModellSicht` `:555`, `Interventionssicht` `:462`; `messsicht` `:1220`, `modell_sicht` `:2057`, `session_snapshot_json` `:2098`, `snapshot_locked` `:2107`; die neun Zaehlerabfragen `:1199`, `:1206`, `:1213`, `:1285`, `:1290`, `:1300`, `:1308`, `:1315`, `:1322` |
| `coordinator/link.rs` | Control-Link an und ab — **der Ort von H-16 und der Verdraengungshaelfte von H-10** | `verbindung_soll_trennen` `:1183`, `control_registrieren` `:647`, `control_hello_registrieren` `:716`, `control_ende` `:881`, `ist_ungebundene_probe` `:665`, `main_sessions_fuer_probe_locked` `:670`, `eindeutige_main_session_locked` `:688`, `ungebundene_links_mit_eindeutigem_main_locked` `:697`, `adresse_des_links_passt` `:1330`, `aktueller_telemetrie_client_locked` `:1337` |
| `coordinator/subscription.rs` | Subscription und Messframe-Push — **der Ort von G2-REENTRANT-002** | `subscription_abweisen` `:926`, `subscribe_json` `:931`, `subscribe_json_mit_minor` `:935`, `resubscribe_snapshot_push` `:991`, `messframes_an_subscriber_push` `:1094`, `push_ziel_noch_gueltig` `:1131`, `session_push_ziele` `:1162`, `subscription_entfernen_locked` `:1373`, `messframe_abweisen_locked` `:1351`, `messframe_abweisen` `:1368` |
| `coordinator/liveness.rs` | Frische, Caps, Eviction, Heartbeat, Deskriptor — **der Ort von H-12 und H-17** | `platz_schaffen_locked` `:1387`, `stales_opfer` `:1419`, `client_eviktieren_locked` `:1441`, `stale_aktualisieren_locked` `:1488`, `liveness_tick` `:1502`, `heartbeat_kontakt` `:1551`, `state_report_json` `:1670`, `state_report_json_mit_minor` `:1674`, `descriptor_aus_heartbeat` `:1712`, `descriptor_setzen` `:1758` |
| `coordinator/mitgliedschaft.rs` | Beitritt und Fuehrung | `auto_join_locked` `:1788`, `fuehrung_neu_bewerten_locked` `:1836`, `beitritt_bestaetigen` `:1878`, `beitritt_bestaetigen_locked` `:1902`, `beitritt_aufheben` `:1922`, `beitritt_aufheben_locked` `:1946`, `fuehrung_uebergeben` `:1963`, `main_darf_schreiben` `:1998`, `beitritt_noetig_locked` `:2016` |
| `coordinator/flush.rs` | Snapshotzustellung, Routing, Konfliktriegel — **der Ort von H-06 und H-21** | `flush_session` `:2184`, `session_flush_shard` `:2286`, `guards_persistieren` `:2298`, `konflikt_guard_aufloesen` `:2310`, `routing_fail_closed` `:1379`, `routing_bereit` `:1192`, `store_degradiert` `:573`, `store_verweigert_fuer_link` `:2564`, `alias_quarantaenisiert` `:2499`, `dispatch_fuer_link_erlaubt` `:2478`, `dispatch_fuer_link_erlaubt_locked` `:2486` |
| `coordinator/intervention.rs` | Der eine fail-closed Interventionszustand aus C-08 | `sequenz_annehmen` `:2331`, `intervention_begin` `:2346`, `intervention_end` `:2375`, `hoermarkierung_v2` `:2410`, `hoermarkierung_v2_getrennt` `:2429`, `intervention_overflow` `:2437`, `tail_fortschritt` `:2444`, `neutral_resync` `:2453`, `interventionssicht` `:2465`, `evidence_dispatch` `:2514`, der v2-Senkenimpl `:3001-3010` |
| `coordinator/befehl.rs` | P0-Befehle und ihre Persistenz — **der Ort der Zielaufloesung aus H-10** | `command_ack` `:2527`, `persistierte_command_wirkung` `:2552`, `persistenz_p0` `:2572`, `session_command` `:2796`, `p0_json` `:2932`, `p0_json_mit_minor` `:2936` |
| `coordinator/senke.rs` | Der Transportkontakt — **der Ort von G2-REENTRANT-001 und H-04** | Der Block `impl Senke for Coordinator` `:3012-3201` |

**Was `pub(super)` bekommt.** Ueber die Anker der Tabelle gezaehlt, am Stand
`85fb5f2`, ohne die `impl`-Bloecke: `zustand.rs` **15 Elemente** — `ClientKey`,
`SessionKey`, `ClientStand`, `SessionStand`, `LinkStand`, `Subscription`,
`Intervention`, `LiveMessframe`, `Messfehler`, `P2RejectGrund`,
`Lautheitsstand`, `Stand`, `SessionCommandWirkung`, `JSON_SAFE_INTEGER_MAX`,
`P2_REJECT_KATALOG` — **und die 77 Felder ihrer zwoelf Strukturen**, denn
`pub(super)` auf dem Typ nimmt die Felder nicht mit; allein `Stand` traegt 25
und `ClientStand` 18. `P2RejectGrund` ist ein Enum, dessen Varianten die
Sichtbarkeit des Typs erben.
Weiter: `schema.rs` **8**, `subscription.rs` **8**, `flush.rs` **8**,
`link.rs` **6**, `liveness.rs` **6**, `befehl.rs` **6**,
`mitgliedschaft.rs` **5**, `uhr.rs` **1** (`InstantClock`),
`sicht.rs` **1** (`snapshot_locked`), `intervention.rs` **1**
(`sequenz_annehmen`; der v2-Senkenimpl ist ein Trait-Impl und braucht nichts),
`senke.rs` **0** (nur ein Trait-Impl). `mod.rs` bleibt privat wie bisher.
Kein einziges dieser Elemente ist heute aus `lib.rs` erreichbar.

Die 14 Inline-Tests wandern mit ihrem Gegenstand; ihre Summe bleibt 14.

**Oeffentliche Flaeche, die unveraendert bleiben muss.** Aus `broker/tests/` und
`broker/src/bin/` werden unter `eqcop_broker::coordinator::` gelesen:
`Coordinator`, `ManualClock`, `MonotonicClock`, `SessionPush`,
`CoordinatorFlushTestHaken`, `Lautheitszustand` und die elf Konstanten
`FUEHRENDE_MAINS_PRO_SESSION`, `GLOBAL_CLIENT_CAP`, `HEARTBEAT_INTERVAL_MS`,
`LAST_SONDEN`, `SESSION_CLIENT_CAP`, `SESSION_SUBSCRIPTION_EVENT_REPLAY_MAX`,
`SICHTBARE_SONDEN_NORMAL`, `STALE_JITTER_MS`, `STALE_NACH_MS`,
`STALE_VERPASSTE_INTERVALLE` und `TOMBSTONE_MS`. Dazu die uebrigen heute
oeffentlichen Sichttypen. Alle erscheinen als `pub use` im neuen `mod.rs`.

### 5.5 `store.rs` in fuenf Module

| Zielmodul | Verantwortung | Was dorthin wandert |
|---|---|---|
| `store/mod.rs` | Modulkopf, Modulliste, Re-Exports, Konstanten, Werte und die reinen Ausloeserfunktionen | Kopfkommentar `:1-7`; die Konstanten `:22-34`; `StoreFehler` `:169`, `ConflictGuard` `:198`, `SnapshotZiel` `:205`, `StoreEvent` `:213`, `AppendAusgang` `:254`, `CheckpointErgebnis` `:278`, `StorePragmas` `:285`, `StoreSicht` `:300`; `commit_ausloesen` `:1223`, `checkpoint_ausloesen` `:1227`, `recovery_testgrenze_bestanden` `:1232`, `busy_timeout_abgelaufen` `:1236` |
| `store/migration.rs` | Schema, Migration und Pruefsumme — **der Ort von G2-TOCTOU-001** | `MIGRATION_1_SQL` `:36`, `migration_1_checksum` `:1240`, `store_vorbereiten` `:1246`, `pragmas_setzen` `:1291`, `pragmas_lesen` `:1305`, `migration_1` `:1315`, `bekanntes_schema_readonly` `:1349`, `konflikt_guards_lesen` `:1368`, `projektionen_neu_bauen` `:1384` |
| `store/pfad.rs` | Wo die Datenbank liegen darf — **der Ort von G2-TOCTOU-002 und G2-LOSSYSTR-001** | `store_pfad_unter` `:1469`, `standard_store_pfad` `:1474` und `:1514`, `store_pfad_ist_remote` `:1524` und `:1555`, `wal_pfad` `:1459`, `wal_groesse` `:1465` |
| `store/handle.rs` | Der Zugang fuer alle fremden Threads | `StoreHandle` `:413` mit Impl `:420`, `StoreKonfiguration` `:365`, `StoreStartBarriere` `:336`, `StoreTestHaken` `:395`, `kurze_leseconnection` `:1431`, `degradiere` `:1443`, `utc_ms_i64` `:1452` |
| `store/writer.rs` | Der eine besitzende Schreiberthread — **der Ort von H-18** | `StoreWriter` `:595` mit Impl `:601` und Drop `:715`, `WriterBefehl` `:721`, `AppendStand` `:747`, `CommitJob` `:753`, `writer_lauf` `:757`, `append_gruppe` `:958`, `append_eins` `:988`, `projektionen_anwenden` `:1052`, `kompaktieren` `:1147`, `konflikt_guard_schreiben` `:1175`, `konflikt_guard_loeschen` `:1193`, `checkpoint` `:1207` |

**Was `pub(super)` bekommt.** Ueber die Anker der Tabelle gezaehlt, am Stand
`85fb5f2`, ohne die `impl`-Bloecke: `writer.rs` **11** (`WriterBefehl`,
`AppendStand`, `CommitJob`, `writer_lauf`, `append_gruppe`, `append_eins`,
`projektionen_anwenden`, `kompaktieren`, `konflikt_guard_schreiben`,
`konflikt_guard_loeschen`, `checkpoint`), `migration.rs` **7**,
`handle.rs` **3** (`kurze_leseconnection`, `degradiere`, `utc_ms_i64`),
`pfad.rs` **2** (`wal_pfad`, `wal_groesse`). `mod.rs` bleibt privat wie bisher.
`StoreWriter` selbst ist bereits `pub` und bleibt es.

Die 3 Inline-Tests wandern mit ihrem Gegenstand; ihre Summe bleibt 3.

**Oeffentliche Flaeche, die unveraendert bleiben muss.** `broker/tests/store_crash_matrix.rs`
liest 28 Elemente unter `eqcop_broker::store::`, `broker/src/bin/eqcop-store-crash-worker.rs`
sieben und `broker/tests/sonde012_sources_slice.rs` eines. Alle erscheinen als
`pub use` im neuen `mod.rs`. Diese Datei hat damit die breiteste
Importflaeche der drei und wird deshalb zuerst geschnitten: sie prueft die
Re-Export-Technik am haertesten Fall, bevor die beiden groesseren Dateien folgen.

### 5.6 Historie erhalten

Der erste Commit je Datei ist **nur** eine Umbenennung ohne jede
Inhaltsaenderung:

* `git mv broker/src/store.rs broker/src/store/mod.rs`
* `git mv broker/src/coordinator.rs broker/src/coordinator/mod.rs`
* `git mv broker/src/transport/server_v3.rs broker/src/transport/server_v3/mod.rs`

Eine reine Umbenennung ist eine Aehnlichkeit von 100 Prozent; `git log --follow`
auf den neuen Pfad zeigt danach die ganze Geschichte bis SONDE-010
beziehungsweise SONDE-011 zurueck, ohne von einem Schwellenwert abzuhaengen.
Das Ziel `mod.rs` ist zum Zeitpunkt der Umbenennung zugleich das groesste
Zielmodul, weil es noch die ganze Datei traegt. Die Form folgt der
Hausordnung: `broker/src/transport/mod.rs` gibt es bereits. Die uebrigen Module
werden danach in eigenen Commits **aus** `mod.rs` herausgeloest, sodass die
Reihenfolge der Commits die Herkunft jedes Moduls belegt.

Rust laesst `foo.rs` und `foo/` nicht nebeneinander bestehen; die Umbenennung
muss deshalb der erste Schritt sein, nicht ein spaeterer.

**Eine Inhaltsaenderung ist dabei unvermeidlich, und sie ist vollstaendig
aufgezaehlt.** Ein `include_str!` loest seinen relativen Pfad gegen die Datei auf,
in der es steht; die Umbenennung schiebt jede der drei Dateien eine Ebene tiefer
und macht damit **jedes** relative Pfadmakro darin falsch. Am Stand `85fb5f2`
sind das genau sieben Stellen: `broker/src/coordinator.rs:44` und `:3224`,
`broker/src/store.rs:1715` (`../Cargo.toml` wird `../../Cargo.toml`),
`broker/src/transport/server_v3.rs:3146` (`server_v3.rs` wird `mod.rs`),
`:4837`, `:4841` und `:4976` (je eine Ebene mehr `../`). Der Umbenennungscommit
passt genau diese Pfadliterale an, sonst uebersetzt er nicht; keine Aussage,
kein Testname und keine Assertion aendert sich dabei. Gemessen mit einer Suche
nach `include_str!`, `include_bytes!` und `file!()` ueber `broker/src` ohne
`generiert/`.

### 5.7 Commit-Reihenfolge

| Nr | Commit | Inhalt |
|---:|---|---|
| 0 | Etappe 1 | Dieses Manifest und das Belegfeld in `docs/plan/plan.json` |
| 1 | Store, Umbenennung | Nur `git mv` auf `store/mod.rs` |
| 2 bis 5 | Store, vier Herausloesungen | Je ein Commit fuer `migration.rs`, `pfad.rs`, `handle.rs`, `writer.rs`, jeder fuer sich uebersetzbar und gruen |
| 6 | Coordinator, Umbenennung | Nur `git mv` auf `coordinator/mod.rs` |
| 7 bis 18 | Coordinator, zwoelf Herausloesungen | Reihenfolge von unten nach oben: `uhr.rs`, `schema.rs`, `zustand.rs`, `sicht.rs`, `senke.rs`, `intervention.rs`, `mitgliedschaft.rs`, `subscription.rs`, `flush.rs`, `liveness.rs`, `befehl.rs`, `link.rs` |
| 19 | Server_v3, Umbenennung | Nur `git mv` auf `transport/server_v3/mod.rs` |
| 20 bis 27 | Server_v3, acht Herausloesungen | Reihenfolge von unten nach oben: `senke.rs`, `win_handles.rs`, `queues.rs`, `auth.rs`, `griff.rs`, `trennung.rs`, `verbindung.rs`, `listener.rs` |
| 28 | Kanon nach der Aufteilung | Kein Code; Rohausgabe und Manifestkopf |
| 29 ff. | Haertungen | Je ein Commit pro Arbeitsstueck aus Paragraph 4, gruppiert nach Zielmodul. H-01 und H-02 sind **ein** Commit, weil sie eine Aenderung sind; H-04 und H-05 ebenfalls; H-07 und H-08 ebenfalls |

Die Aufteilung geht den Haertungen voraus, damit jede Haertung in genau einem
Modul landet und der Prueferblick nicht zwischen Umzug und Inhaltsaenderung
unterscheiden muss. Die Reihenfolge von unten nach oben heisst: zuerst die
Module, die nichts aus ihren Geschwistern brauchen.

### 5.8 Nachweise der Verhaltensneutralitaet

| Nachweis | Wert vorher, gemessen am Stand `85fb5f2` | Anforderung nachher |
|---|---|---|
| Testanzahl `cargo test --manifest-path broker/Cargo.toml -- --list` | **362**, davon 190 Bibliothek, 4 `broker_idle`, 9 `contract_cross_language`, 34 `coordinator_model`, 20 `security_vectors`, 26 `sonde012_sources_slice`, 70 `store_crash_matrix`, 9 `transport_fuzz`; 0 Benchmarks, 0 Doctests; die vier Binaries und `main.rs` tragen je 0 | Nach den Aufteilungscommits exakt dieselben 362 mit derselben Verteilung. Nach den Haertungscommits waechst nur die Zahl der neuen Tests aus Paragraph 4; keine bestehende faellt weg |
| Inline-Tests je aufgeteilter Datei | 31 in `server_v3.rs`, 14 in `coordinator.rs`, 3 in `store.rs` | Summe je Modulfamilie unveraendert |
| Compilerwarnungen im selben Lauf | **0** | 0 |
| Oeffentliche Flaeche: uneingeschraenkte `pub`-Deklarationen unter `broker/src` ohne `generiert/`, gemessen mit `rg -N --no-heading -g '*.rs' '^\s*pub\s+(fn\|struct\|enum\|const\|static\|trait\|type\|mod\|use\|unsafe\|extern)\b' broker/src`, gefiltert gegen `generiert`, der Dateipfad abgeschnitten, sortiert | **404 Zeilen, davon 378 verschieden; 17 davon `pub mod`, 1 `pub use`** | Dieselbe sortierte Liste, **erweitert ausschliesslich um die neuen `pub use`-Zeilen** der drei neuen `mod.rs`; jede davon nennt ein Element, das in der Liste vorher schon steht. Die Zahl der `pub mod` bleibt **17** — die neuen Untermodule werden als privates `mod x;` deklariert und vom Muster nicht getroffen. Kein `pub(super)` und kein `pub(crate)` erscheint in dieser Liste; genau das ist der Beweis, dass die Sichtbarkeitsanhebungen aus Paragraph 5.2 die oeffentliche Flaeche nicht beruehren |
| Aenderungen unter `broker/tests/` und `broker/src/bin/` in den Aufteilungscommits | entfaellt | **Genau eine Datei, genau ein Grund, und der ist kein oeffentlicher Pfad.** `broker/tests/coordinator_model.rs:473` liest die Coordinatorquelle ueber `include_str!("../src/coordinator.rs")` und schneidet sie zwischen `fn stale_aktualisieren_locked` und `pub fn heartbeat_kontakt`; beide wandern nach Paragraph 5.4 in `coordinator/liveness.rs`. Der Umbenennungscommit setzt den Pfad auf `../src/coordinator/mod.rs`, der Extraktionscommit von `liveness.rs` auf `../src/coordinator/liveness.rs`. Assertion und Testname bleiben unveraendert. **Sonst exakt null Zeilen** — und diese Abwesenheit ist der mechanische Beweis, dass kein oeffentlicher Pfad sich bewegt hat, weil eine Quelltextwache kein Pfad ist |
| Kanon | GRUEN 40/40, Exit 0, Lauf 2026-09-03 07:06, siehe Paragraph 6 | Derselbe Lauf nach der Aufteilung: 40 Beine, gruen, Exit 0 |

## 6. Kanon vorher

Der juengste gruene Kanon ist **GRUEN 40/40, Exit 0**, Lauf am 2026-09-03 um
07:06, Rohausgabe `docs/beweise/roh/NAK-134-eef6ebb-dirty.md`. Er lief auf dem
Codestand, der als `d3bf936` committet wurde. Die Frage, ob dieser Lauf der
„Kanon vorher" fuer S19b ist, entscheidet ein Diff:

```text
$ git rev-parse HEAD
85fb5f28054851831976db95760171cd5c239370

$ git status --short
(leer)

$ git diff --stat d3bf936..85fb5f2 -- broker eq-copilot tools
(leere Ausgabe, Exitcode 0)
```

**Die Ausgabe ist leer.** Zwischen `d3bf936` und dem HEAD dieses Manifests haben
sich `broker/`, `eq-copilot/` und `tools/` um kein Zeichen geaendert; die beiden
Commits dazwischen (`25173ab` Gate-Abnahme G3 und `85fb5f2` neu gerechneter
Planstand) fassen nur `docs/` an. Der Lauf vom 07:06 ist damit der **Kanon
vorher** auf demselben Codestand. In dieser Etappe wird **kein** Kanon gestartet.

Ein zweiter Diff bindet die Fundstellen der Befundtabelle:

```text
$ git diff --stat ad5b2d1..85fb5f2 -- broker/
(leere Ausgabe, Exitcode 0)
```

`ad5b2d1` ist die Basis des G3-Rust-Reviews. Weil `broker/` seither unveraendert
ist, gelten **alle Zeilennummern des G3-Berichts am HEAD woertlich**; nur die
G2-Zeilennummern mussten ueber Funktion und Konstrukt uebersetzt werden, weil
NAK-123 zwischen G2 (`e77ebf9`) und G3 in acht Dateien 1957 Zeilen eingefuegt und
202 geloescht hat.

## 7. Pruefliste

Aus `tools/dirigent/pruefliste.md`, die Zeilen, die diese Etappe betreffen, je
mit dem Ort der Messung. Die Liste ist Arbeitsmittel des Workers, keine
Anforderungsquelle.

| Zeile | Wo gemessen |
|---|---|
| **E — Zahlen sind gemessen, nicht abgeschrieben** | Paragraph 5.1 und 5.8: Zeilenzahlen aus `wc -l`, Inline-Tests aus einer Zaehlung der Testattribute, 362 Tests und ihre Verteilung aus `cargo test -- --list`, 0 Warnungen aus demselben Lauf. Paragraph 2 nennt ausdruecklich, dass der Auftrag 4752, 3286 und 1662 sagt und die Messung 5095, 3482 und 1774 ergibt |
| **E — Positionen als Symbol oder Anker** | Paragraph 3 und 4 nennen zu jeder Zeilennummer die Funktion oder das Konstrukt; Paragraph 6 belegt mit einem Diff, warum die G3-Zeilen am HEAD woertlich gelten und die G2-Zeilen uebersetzt werden mussten |
| **E — Behauptung hoechstens so gross wie die Messung** | Die drei `C`-Zeilen in Paragraph 3 tragen jede ihr Kommando und dessen Ergebnis. G3-STRSLICE-001 wird nicht als „wahrscheinlich harmlos" gefuehrt, sondern an der Ladepruefung und am Konstruktionsweg des Schemas widerlegt |
| **E — der lebende Kopf wird beim Abschluss nachgezogen** | Die Feldtabelle in Paragraph 0 traegt `NOT RUN`; Urteil und Pruefung setzt der Dirigent |
| **A — Rueckgabewerte und Zaehler werden ausgewertet** | H-18 und H-19 machen genau das zur Zusage; H-07 fuehrt den Zaehler `flush_abgelaufen` ein |
| **A — je Klasse steht die Politik bei voll ausdruecklich** | H-11 uebertraegt die vorhandene Politik auf die Byteachse, ohne die Reihenfolge zu aendern |
| **B — Reihenfolge beim Verbinden und Trennen ist festgelegt und gemessen** | H-01, H-02, H-07, H-08, H-09, H-16 nennen je die Reihenfolge; H-16 ergaenzt C-10 um die fehlende Frist |
| **B — jeder Join hat eine Frist** | H-02 und H-07 halten die vorhandenen Fristen und fuegen keine unbeschraenkte hinzu |
| **F — verbinden gegen trennen, starten gegen stoppen im selben Commit** | H-09 (Store starten gegen stoppen), H-12 (Session anlegen gegen entfernen), H-13 (Sensor-ID vergeben gegen zurueckgeben), H-14 (Quarantaene setzen gegen aufloesen) — jede dieser Zeilen ist ein Commit mit beiden Haelften |

## 8. Was diese Etappe ausdruecklich nicht tut

* Kein Produktcode, kein Test, kein Schema, keine Fixture.
* Kein Kanonlauf; der Stand aus Paragraph 6 genuegt und ein neuer waere ohne
  Codeaenderung nur eine Wiederholung.
* Keine Zeile in `docs/offene-punkte.md`. Der B-Rest aus Paragraph 3.1
  (Kanonisierung der SID an der Vertrauensgrenze) gehoert dorthin und wird vom
  Folge-Worker datiert eingetragen, zusammen mit dem Aenderungssatz, der ihn
  ausloest.
* Keine Entscheidung ueber NAK-125 und NAK-126. Beide stehen in
  `docs/beweise/G2.md` Paragraph 4.3, tragen aber nicht die Klasse S19b.

## 9. Nacharbeit Runde 1 (Matrixpruefung 1)

Die Matrixpruefung 1 (Codex `gpt-5.6-sol`, lesend, Stand `591fabf`, Rohurteil
`docs/beweise/roh/NAK-121-matrixpruefung-1-591fabf.txt`) meldete sechs P1- und
zwei P2-Kommentare. Der Dirigent hat alle acht an der Quelle bestaetigt und je
Befund die Regel festgelegt, die ihn schliesst. Dieser Abschnitt ist
**append-only**: er fuehrt je Defekt die Regel des Dirigenten woertlich und die
geaenderte Zeile. Die Paragraphen 6 bis 8 sind unveraendert.

### 9.1 Defekt 1 — Paragraph 5.2 und 5.4, Geschwistermodule

> Paragraph 5.2 wird korrigiert. Elemente, die Geschwistermodule brauchen,
> erhalten `pub(super)` (innerhalb einer Modulfamilie) oder `pub(crate)` (wenn
> ein anderes Crate-Modul sie braucht). Beides ist keine oeffentliche Signatur,
> die ein anderer Verbraucher liest — die oeffentliche Flaeche des Crates
> (`pub` ohne Einschraenkung, erreichbar aus `lib.rs`) bleibt unveraendert;
> genau das ist die Grenze des Gate-Textes. Neue uneingeschraenkte `pub`-Items
> entstehen ausschliesslich als `pub use`-Re-Export im jeweiligen mod.rs fuer
> Elemente, die vorher schon `pub` waren. Trage in Paragraph 5.8 einen Nachweis
> ein, der das misst (z. B. die Menge der aus `lib.rs` erreichbaren
> `pub`-Pfade vorher = nachher, gemessen mit einem benannten Kommando). Pruefe
> die drei Tabellen 5.3 bis 5.5 auf weitere Geschwister-Abhaengigkeiten (Typen
> in `zustand.rs`, `win_handles.rs`, `handle.rs`) und benenne je Modul, welche
> Elemente `pub(super)` bekommen.

Geaendert: Paragraph 5.2 (zweite Technik vollstaendig neu), die drei Nachtraege
unter den Tabellen 5.3, 5.4 und 5.5, sowie die neue erste Zeile in Paragraph
5.8. **`pub(crate)` wird nirgends gebraucht**, weil jedes heute private Element
schon heute nur dateilokal erreichbar ist; `pub(super)` genuegt ausnahmslos.
Gemessene Zahlen: 404 uneingeschraenkte `pub`-Deklarationen (378 verschieden,
17 `pub mod`, 1 `pub use`) als Vorher-Wert der oeffentlichen Flaeche; 77 Felder
allein in den zwoelf Strukturen von `coordinator/zustand.rs`.

**Zwei Nachbarzeilen mussten mit, weil sie am Code widerlegt sind.** Beim
Eintragen des 5.8-Nachweises gemessen: sechs relative Pfadmakros in den drei
Dateien brechen bei der reinen Umbenennung (Paragraph 5.6 traegt sie jetzt
namentlich), und `broker/tests/coordinator_model.rs:473` liest die
Coordinatorquelle per `include_str!` und braucht deshalb zwei Pfadanpassungen —
die frueher zugesagten „exakt null Zeilen" unter `broker/tests/` waren nicht
haltbar. Beide Zeilen nennen jetzt den genauen Umfang; keine Aussage und kein
Testname aendert sich dadurch.

### 9.2 Defekt 2 — Paragraph 3.4 NAK-135 und H-15, Listener-Deckel

> Bestehende Tests bleiben woertlich unveraendert (Testvertrag),
> `PIPE_INSTANZEN` bleibt 98. NAK-135 bleibt nur dann A, wenn die Zusage
> lautet: die Zahl der bewaffneten Listener ist dynamisch und haelt stets
> `aktive Worker + bewaffnete Listener <= PIPE_INSTANZEN`; bei 96 aktiven
> Workern sind es weiterhin genau zwei (der bestehende Test misst genau das und
> bleibt gruen), bei weniger Workern mehr, mit einer benannten Obergrenze als
> Konstante. Kannst du diese Form am Code nicht belegen, stufst du NAK-135 auf
> B (vertagt, Grund: Deckel und bestehender Test) und streichst H-15. Der Satz
> „messen dieselbe Zusage an der Konstante statt an der Zahl zwei" entfaellt in
> jedem Fall.

Geaendert: Registerzeile NAK-135 in Paragraph 3.4 und Matrixzeile H-15.
**NAK-135 bleibt A**, weil die dynamische Form am Code belegbar ist: die
Zielgroesse `min(LISTENER_RESERVE_MAX, PIPE_INSTANZEN - aktive_worker)` wird
ausschliesslich im Annahmepfad hergestellt und landet bei 96 Workern auf genau
zwei. Beide bestehenden Tests bleiben woertlich; der zweite
(`ersatzlistener_fehler_behaelt_besitz_und_meldet_nicht_bereit`, Assertion `1`)
ist der Grund, warum proaktives Nachruesten beim Schleifeneintritt
ausgeschlossen ist. `LISTENER_RESERVE_MAX = 16` ist hart gedeckelt durch
`MAXIMUM_WAIT_OBJECTS = 64` minus dem Stopereignis. Der Satz „messen dieselbe
Zusage an der Konstante statt an der Zahl zwei" ist an beiden Stellen entfernt.

### 9.3 Defekt 3 — H-16 gegen C-06

> (ersetzt die Dirigentenregel aus Register NAK-136 vom 03.09.2026, die den
> Push „noch im Hello-Zyklus" verlangte): Bei der Verdraengung markiert
> `control_hello_registrieren` die Session unter dem Lock als dirty. Der Push
> erfolgt erst nach Abschluss des C-06-Cleanups des verdraengten Links,
> ausgeloest im `control_ende`-Pfad des alten Links (heute
> `coordinator.rs:904-910`, der bei Verdraengung nicht mehr markiert, weil
> `current_link` bereits umgehaengt ist — dieser Pfad flusht kuenftig die
> bereits dirty markierte Session), also ohne Uhrenschritt und ohne
> Heartbeat-Takt. Frist: Push innerhalb des Trennzyklus des verdraengten Links.
> Test in `broker/tests/coordinator_model.rs`: nach `control_hello` (neu) ist
> der Push-Zaehler noch nicht erhoeht; nach `control_ende` (alt) ist er erhoeht,
> ohne Uhrenschritt. Bleibt `control_ende` aus (kein Transportcallback), bleibt
> der Heartbeat der Rueckfall — als eigener Satz in der Zeile. Schreibe H-16
> entsprechend um und vermerke in der Zeile, dass sie die NAK-136-Regel
> praezisiert.

Geaendert: Registerzeile NAK-136 in Paragraph 3.4 und Matrixzeile H-16; der
Testname heisst jetzt `verdraengung_flusht_den_snapshot_im_trennzyklus`. Am Code
gemessen und in der Zeile benannt: `control_ende` laeuft bei
`server_v3.rs:2880`, also nach dem Ingress-Schluss bei `:2873` und vor
`kopplung_loesen`, Joins und Trenncallbacks. Dass diese Restschritte keinen
Zustand beruehren, den `snapshot_locked` liest, ist gemessen —
`control_getrennt` ist im Coordinator ein leerer Rumpf, `telemetrie_getrennt`
beruehrt allein `telemetry_links`, und `telemetry_links` kommt in
`snapshot_locked` nicht vor.

### 9.4 Defekt 4 — H-02, abgeloester Schreiber

> H-02 nennt den Abbruchweg ausdruecklich: ein abgeloester Schreiber wird in ein
> Abloese-Register (oder als markierter Eintrag im bestehenden Handle-Register)
> eingetragen, und der vorhandene Wachhund-Tick ruft fuer diese Eintraege
> `CancelIoEx` wiederholt, bis der Thread endet und der Griff faellt;
> `alle_io_abbrechen` beim Serverstopp deckt sie ebenfalls. Die Zeile nennt
> Reihenfolge (Ablosung → Eintrag → Wachhund-Abbruch → Threadende → Handle frei)
> und Frist (naechster Wachhund-Tick, mit der heutigen Tickkonstante als Wert).
> Der Test beobachtet nach dem Wachhund-Tick null gehaltene Handles bei weiter
> annehmendem Listener. Technik (eigenes Register oder Markierung) entscheidest
> du am Code und benennst sie.

Geaendert: Zeile G2-RAWFD-002 in Paragraph 3.1 und Matrixzeile H-02. **Technik
am Code entschieden:** eine zweite Menge `abgeloest: HashSet<u64>` im
vorhandenen `HandleRegister` (`server_v3.rs:966-969`) statt eines eigenen
Registers — der Wachhund haelt das Register bereits gesperrt und iteriert
ohnehin ueber `offen`, und der Destruktor des Verbindungsgriffs traegt unter
derselben Sperre aus beiden Mengen aus. Der Wachhundtakt ist heute das nackte
`Duration::from_millis(100)` bei `:1721` und wird zur benannten Konstante
`WACHHUND_TAKT`.

### 9.5 Defekt 5 — H-06, zwei Zustellpfade

> Genau ein Pfad: Snapshotzustellung wird eingereiht und kehrt sofort zurueck,
> wie die Messframe-Zustellung heute; die Alternative mit synchronem
> Gesamtbudget wird gestrichen. Jede Nebenwirkung, die die Zeile zusagt
> (Eviction laeuft vor der Zustellphase; ein Ziel, das nicht liest, wird nach
> der bestehenden Senkenfrist getrennt; Uebersprung-/Trennzaehler), bekommt
> einen benannten Test oder eine benannte Assertion im selben Test: Tickdauer
> unter Budget UND lesende Abonnenten erhalten den Snapshot weiterhin UND der
> nicht lesende Abonnent wird getrennt und gezaehlt UND Eviction vor Zustellung
> (Reihenfolgebeweis).

Geaendert: Zeile G2-CHANSTARVE-001 in Paragraph 3.1 und Matrixzeile H-06. Am
Code gemessen und in die Zeile aufgenommen: Eviction laeuft **schon heute** vor
der Zustellphase (`coordinator.rs:1528` im gesperrten Block gegen `:1545`
danach) — H-06 haelt diese Reihenfolge fest, statt sie zu verschieben. Die
Senkenfrist wechselt nur den Ort: `ov_schreiben` wartet heute `INFINITE`
(`server_v3.rs:648`) und bekommt `SENKE_FRIST`. Damit `snapshot_schuld_kompaktieren`
weiter nach vollstaendigem Write laeuft (O-03), traegt der schon vorhandene
`geschrieben`-Kanal des Ausgangseintrags die Kompaktierung.

### 9.6 Defekt 6 — H-20, C++-Leser

> H-20 wird auf den Rust-Leser begrenzt; der Satz „Dieselbe Klassifikation gilt
> im C++-Bein" entfaellt. Zusage neu: die Abweisungsentscheidung (welcher Frame
> abgewiesen wird) bleibt identisch zu heute und zum C++-Leser, gemessen an den
> bestehenden Cross-Language-Fixtures; nur die Verstosszaehlung und die Schleife
> aendern sich. Pruefe und benenne in der Zeile, ob die Verstosszahl oder die
> Verstossliste irgendwo nach aussen sichtbar ist (Nachrichtenform,
> Reject-Grund, Telemetriezaehler); ist sie es, bleibt sie in Form und Wert
> unveraendert (sonst Vertragsberuehrung → B). Die C++-Spiegelung der fruehen
> Rueckkehr wird in Paragraph 3 als Haertung ausserhalb dieses Tickets benannt
> (das Register zieht der Dirigent nach).

Geaendert: Zeile G2-RESEXHAUST-001 in Paragraph 3.1, Matrixzeile H-20 und ein
neuer Absatz „Haertung ausserhalb dieses Tickets" am Ende von Paragraph 3.
**Sichtbarkeit gemessen:** `p2` verwirft das `Err(_)` und meldet den festen
Reject-Grund `FeatureBatchUngueltig` (`coordinator.rs:3082-3086`); es gibt
keinen Verstosszaehler in einer Nachrichtenform. Sichtbar ist die Liste allein
als Rueckgabe der `pub fn pruefe` an `broker/tests/`, und dort bleibt sie
gleich: das einzige Korpus-Fixture oberhalb der Grenze,
`ungueltig/eintraege-33.bin`, traegt im Manifest genau einen Verstoss, den die
fruehe Rueckkehr ebenfalls liefert. **Zwei Korrekturen kamen mit:** der Deckel
auf die Gesamtzahl der Verstoesse entfaellt (an keinem Fixture messbar, also
genau die unbelegte Zusage, die diese Runde schliesst), und die Testspalte nennt
nicht mehr „vorhandene Telemetrie-Inlinetests" — `broker/src/telemetrie.rs`
traegt null davon; die Wache ist der vorhandene Korpustest.

### 9.7 Defekt 7 — G2-CARGOLINT-001, Clippy-Lint ohne Messung

> Kein neues Kanon-Bein (die Beinzahl muss gleich bleiben) und tools/ wird nicht
> angefasst. Die Zusage wird so gemessen: (a) `[lints.rust]
> unsafe_op_in_unsafe_fn = "deny"` wirkt im Build und damit im bestehenden Bein;
> (b) ein neuer Test in broker/tests (Quellwache) liest broker/src/** und
> verlangt vor jedem `unsafe {`-Block und jedem `unsafe impl` einen
> `// SAFETY:`-Kommentar in einem benannten Zeilenfenster — er misst
> G2-SAFETYDOC-001 und G3-SAFETYDOC-001 dauerhaft im Kanon-Bein `broker`; (c)
> `[lints.clippy] undocumented_unsafe_blocks = "deny"` darf zusaetzlich stehen,
> die Clippy-Verdrahtung in den Kanon ist Haertung ausserhalb dieses Tickets.
> Trage Test und Fenster in Paragraph 3 (Zeile CARGOLINT, SAFETYDOC-001 G2 und
> G3) und als Matrixzeile in Paragraph 4 ein.

Geaendert: Zeilen G2-CARGOLINT-001 und G2-SAFETYDOC-001 in Paragraph 3.2,
G3-SAFETYDOC-001 in Paragraph 3.3, der Vorspann von Paragraph 4 (bisher fuenf
Befunde ausserhalb der Matrix, jetzt zwei) und die neue Matrixzeile **H-22**.
Fenster: `// SAFETY:` in den drei vorangehenden nicht-leeren Zeilen. Am Stand
`85fb5f2` gemessen: **100 unsafe-Stellen in `broker/src` ohne `generiert/`, 59
erfuellen das Fenster, 41 nicht** — der Test ist erst mit 100 von 100 gruen,
also gehoeren alle 41 in denselben Aenderungssatz, nicht nur die zwei aus den
Berichten. In `server.rs` allein sind es sieben, nicht die eine, die G2 nannte;
die Fundstelle von G2-SAFETYDOC-001 sagt das jetzt.

### 9.8 Defekt 8 — H-13, Frist ohne Wert

> H-13 nennt eine benannte Konstante mit Wert (du legst ihn am Code fest und
> begruendest ihn; „sofort bei Trennung" ist zulaessig, dann entfaellt die Frist
> und die Zeile sagt das), die Inklusivitaet der Grenze, und der Test misst die
> Kanten Frist minus eins und exakt Frist sowie den Fall „alle Plaetze verbunden
> → Reject bleibt".

Geaendert: Zeile G2-RESEXHAUST-004 in Paragraph 3.1 und Matrixzeile H-13.
**Wert am Code festgelegt:** `SENSOR_ID_RUECKGABE_MS = 60_000`, gleich dem
Leerlaufhorizont `BROKER_IDLE_ENDE_MS` (`broker/src/lib.rs:39`), den der Broker
fuer sich selbst fuehrt — wer laenger fort ist, als der Broker ohne jeden Sensor
weiterlaeuft, hat keinen Anspruch mehr auf einen Platz. „Sofort bei Trennung"
scheidet aus, weil ein getrennter Eintrag Label, Heartbeatzahl und
Konfliktwahrheit traegt und in der Sensorliste sichtbar bleibt
(`sensoren_snapshot_zu`, `broker/src/lib.rs:500`). Grenze **inklusiv**. Der
frueher zugesagte „Sweep auf dem vorhandenen Tick" ist gestrichen: einen
periodischen v2-Tick gibt es in `broker/src/server.rs` nicht; der Sweep laeuft
im Gate, unter demselben Lock und vor der Grenzpruefung. Der Test wandert
deshalb inline nach `broker/src/server.rs`, wo das Zeitseam
`Register::getrennte_ids_freigeben_zu(jetzt)` nach dem Muster von
`sensoren_snapshot_zu` deterministisch messbar ist.
