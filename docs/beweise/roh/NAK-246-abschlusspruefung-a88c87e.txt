URTEIL: NEEDS_WORK — zwei Nebenläufigkeitsdefekte im Persistenzabschluss.

Geprüft: die 40 Ticketpfade, Matrix-Zusagezeilen, Wechselwirkungen und vorhandene Rohbelege lesend; M-05 und Schema-Diff leer, HEAD vor/nach unverändert a88c87e8ba8f664ede6480d511b2a8492134aa76.

Nicht geprüft: dynamische Ausführung; keine Builds, Tests oder Produktprozesse gestartet, die grünen Nulltest-, Migrations-, Vertrags- und Golden-Ergebnisse einschließlich 64/64 stammen ausschließlich aus den vorhandenen Rohbelegen.

Full review comments:

- [P1] Serialisiere den Reload mit bereits abgeholten Sources-Befehlen — C:/Users/phili/Projekte/Nakama/eq-copilot/plugin/src/prozessor/Ipc.cpp:1421-1422
  Beim Reload derselben Projektbindung kann der neue Worker einen alten Befehl nachträglich auf den geladenen State anwenden. Reproduktion mit dem vorhandenen Drain-Haken: Mitglied speichern, bestätigtes `unbind_probe` nach dem Swap anhalten, gespeicherten State in dieselbe Instanz laden, Worker freigeben. Das Mitglied wird anschließend wieder gelöscht. `setStateInformation` leert nur die gemeinsamen Listen; der lokale Batch überlebt, und die konstante `v3SessionEpoch` lässt ihn bei gleicher Bindung passieren. Das verletzt M-31 („persistente Mitglieder aus dem State“) und [State-Verlustfreiheit](CLAUDE.md#L194-L196). Der Reload muss auch bereits abgeholte Befehle ausschließen. Lesebeleg: `git show a88c87e8:eq-copilot/plugin/src/prozessor/State.cpp`; dynamisch hier nicht ausgeführt.

- [P2] Schütze die Modellnachführung vor überholten Zustandskopien — C:/Users/phili/Projekte/Nakama/eq-copilot/plugin/src/prozessor/Ipc.cpp:1403-1406
  Auch bei unterschiedlicher Projektbindung kann die nachgelagerte Modellnachführung den Reload überschreiben: Worker nach dem Kopieren der alten Mitglieder anhalten, einen read-only State laden und `projektReload({})` abschließen lassen, anschließend den Worker fortsetzen. `setzePersistenteMitglieder(kopie)` trägt dann die alten Mitglieder wieder als bestätigt ins geleerte Modell ein. Beide Prozessorsperren sind bereits frei; eine Bindungs- oder Versionsprüfung fehlt, und weitere leere Drains korrigieren das nicht. Das verletzt M-31s Mitgliedschaft aus dem geladenen State und betrifft den gemeinsam zu behandelnden [Save/Load-Lebenszyklus](AGENTS.md#L48-L50). Die Modellpublikation benötigt eine Ordnungsabsicherung, nicht nur das vorherige Kopieren unter `bindungMutex`. Lesebeleg: `git show a88c87e8:eq-copilot/plugin/src/prozessor/Ipc.cpp`; dynamisch hier nicht ausgeführt.