NAK-246 Etappe 4 - Rotbeweis M-12 (Manifest docs/beweise/NAK-246.md §3.3): dirty_und_revision_genau_einmal_je_bestaetigtem_befehl (B14)
Stand: 2026-09-11 05:30 (PC-Lokalzeit); Basis-SHA 9174d272e927279bdc8d71ac7ae64dc455853892; git status vor der Etappe leer.
Muster: docs/beweise/roh/NAK-246-rot-M-06.txt (endgueltige Tests gegen die Fixfassung ohne den Riegel, §3.3 M-12 und §5.3 Feinheit 3).

WAS GEFAHREN WURDE
  Bein B14 EqCopSonde012ProjectReloadTest, Fall M-12 allein (`--nur m12`), mit dem ENDGUELTIGEN Test
  (Sonde012ProjectReloadTest.cpp, nak246d3::dirty_und_revision_genau_einmal_je_bestaetigtem_befehl) gegen
  GENAU die Mutation, die §5.3 Feinheit 3 und M-12 festlegen: `sourcesDrainMutex` - die einzige Klammer ueber
  Swap UND Anwendung - ist im Rahmen `wendeBestaetigteSourcesCommandsAn` entfernt (Ipc.cpp, Diff unten).
  Interleaving per Haken, deterministisch: Drain A (Workerzug) haelt am Testhaken zwischen Swap [confirm_join]
  und Anwendung; der Test legt danach [unbind_probe] bestaetigt in die Liste und laesst Drain B (Editor-Tick,
  eigener Faden) laufen; nach 300 ms wird A freigegeben.
  Am Basis-SHA ist die Zeile nicht messbar (ein einziger Drain ueberholt sich nicht, §3.3); der Rotbeweis faellt
  an der Zusage "in ACK-Reihenfolge" gegen die Fixfassung ohne den Riegel.

BAU UND LAUF
  cmake --build eq-copilot/build --config Release --target EqCopSonde012ProjectReloadTest: Exit 0, 7 s.
  Quelle (Rotstand) Ipc.cpp 2026-09-11 05:30:20; Binary EqCopSonde012ProjectReloadTest.exe 05:30:39 (juenger: ja).
  Lauf: eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe --nur m12
        -> Exit 1, 3 s, 8 Pruefungen, 4 Fehler.
  Ruecknahme: git checkout -- eq-copilot/plugin/src/prozessor/Ipc.cpp aus dem gestageten Endstand (05:30:42).

DER KIPP (M-12: "Genau einmal je Befehl, in ACK-Reihenfolge")
  Ohne Riegel swappt B [unbind_probe] und wendet ihn an, waehrend A noch haelt - ein No-op, das Mitglied ist noch
  nicht da. Danach wendet A [confirm_join] an: das Mitglied steht im State, obwohl die ACK-Reihenfolge
  Join -> Unbind es entfernen muesste. Deterministisch, weil der Haken A haelt, bis B fertig ist (B: 0 ms).
  FEHLER  M-12: dirty_und_revision_genau_einmal_je_bestaetigtem_befehl - Join, dann Unbind desselben Mitglieds in ACK-Reihenfolge endet OHNE Mitglied  [1 Mitglied(er)]
      -> die Zeile, die die Zusage traegt.
  FEHLER  M-12: Dirty-Zaehler == Zahl der State-aendernden Befehle (2)  [1]
      -> nur der Join aenderte den State; der Unbind war ein No-op.
  FEHLER  M-12: Revision-Delta == 2  [1]
  FEHLER  M-12: der zweite Drain wartete auf den ersten - `sourcesDrainMutex` klammert Swap UND Anwendung  [0 ms Editor-Tick]
      -> B lief in 0 ms durch, statt auf A zu warten.
  Gruen bleiben: A steht am Haken; waehrend A haelt ist nichts angewandt; beide Swaps liefen durch den Haken (2).

DIFF DES ROTSTANDS (git diff -- Ipc.cpp, Rotlaufstand gegen den gestageten Endstand)
diff --git a/eq-copilot/plugin/src/prozessor/Ipc.cpp b/eq-copilot/plugin/src/prozessor/Ipc.cpp
index 42914f95..fed5639b 100644
--- a/eq-copilot/plugin/src/prozessor/Ipc.cpp
+++ b/eq-copilot/plugin/src/prozessor/Ipc.cpp
@@ -1404,7 +1404,9 @@ void EqCopilotProcessor::wendeBestaetigteSourcesCommandsAn()
 {
     std::size_t geaendert = 0;
     {
-        std::lock_guard<std::mutex> drain (sourcesDrainMutex);
+        // ROTLAUF NAK-246 M-12 (Mutation nach Paragraph 5.3/M-12): die einzige Klammer
+        // ueber Swap und Anwendung ist entfernt.
+        // std::lock_guard<std::mutex> drain (sourcesDrainMutex);
         const auto befehle = bestaetigteSourcesCommandsAbholen();
         if (befehle.empty())
             return;

VOLLSTAENDIGE ROHAUSGABE: EqCopSonde012ProjectReloadTest.exe --nur m12
== NAK-246 M-12 dirty_und_revision_genau_einmal_je_bestaetigtem_befehl ==
  ok      NAK-246 D3: der Prozessor ist ein Main mit Bindung
  ok      M-12: Drain A (Workerzug) steht am Haken - zwischen Swap [confirm_join] und Anwendung
  ok      M-12: waehrend A haelt, ist nichts angewandt und nichts gemeldet
  FEHLER  M-12: dirty_und_revision_genau_einmal_je_bestaetigtem_befehl - Join, dann Unbind desselben Mitglieds in ACK-Reihenfolge endet OHNE Mitglied  [1 Mitglied(er)]
  FEHLER  M-12: Dirty-Zaehler == Zahl der State-aendernden Befehle (2)  [1]
  FEHLER  M-12: Revision-Delta == 2  [1]
  FEHLER  M-12: der zweite Drain wartete auf den ersten - `sourcesDrainMutex` klammert Swap UND Anwendung  [0 ms Editor-Tick]
  ok      M-12: beide Swaps liefen durch den Haken  [2]
SONDE-012 ProjectReload (nur m12): 4/8 gruen

GRUEN (Endstand, Binary 05:42:42, voller Lauf): alle 8 Zeilen ok - 0 Mitglieder, Dirty 2, Revision +2, Editor-Tick wartete 300 ms; B14 gesamt 92/92, Exit 0.
