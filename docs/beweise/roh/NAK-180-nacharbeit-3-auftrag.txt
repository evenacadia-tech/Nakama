NAK-180 — Etappe 1, Nacharbeit 3 (letzte Spezifikationsrunde, Budget Runde 3 von 3). Zusatzauftrag des Dirigenten (2026-09-05).

Du bist der Bauer dieses Tickets (Session-Fortsetzung). Basis = `git rev-parse HEAD` beim Start (der Dirigent hat §5.3 mit Matrixprüfung 3 committet). Git-Regeln und Grenzen wie in docs/beweise/roh/NAK-180-auftrag.txt: Commits nur mit explizitem Pathspec auf docs/beweise/NAK-180.md, Push nach `git pull --rebase` bei sauberem Worktree, keine Codezeile, keine Schemaänderung, kein Anfassen fremder Dateien.

DEFEKT (Matrixprüfung 3, wörtlich aus docs/beweise/roh/NAK-180-matrixpruefung-3-fd1074d.txt):
„[P1] Serialisiere positive und negative Linkcallbacks — docs/beweise/NAK-180.md:194. Bei `reconnect()` oder `stop()` während eines laufenden positiven Callbacks kann `v3ControlLink(false)` dessen Zustand zuerst löschen und der noch laufende `v3ControlLink(true)` anschließend `replayFaellig` bzw. den Bericht erneut setzen: Der negative Callback läuft ausdrücklich auf dem Aufruferthread (`ControlClient.h:239-243`), während `meldeLinkStatus` nur das Statusbit, nicht die Callback-Ausführung serialisiert (`ControlClient.cpp:988-995`). Weil danach wegen des bereits falschen Status kein zweiter Ende-Callback folgt, übernimmt der nächste Link den veralteten Bericht. Damit reicht die hier zugesagte sterbende Generation nicht aus und N-23/verbinden↔trennen bleibt gebrochen; erforderlich ist eine Generationsprüfung beziehungsweise Serialisierung samt Test dieses true/false-Interleavings. Befundklasse: DEFEKT."

Vom Dirigenten an der Quelle bestätigt (docs/beweise/NAK-180.md §5.3, Spalte Quelle): `reconnect()` (`ControlClient.cpp:752-768`) joint nicht und ruft `meldeLinkStatus(false)` auf dem Aufruferthread; `stop()` ruft es in `:708` vor dem Join `:743`; `eineVerbindung` (`:1559`) löst keinen zweiten Callback aus, weil das Bit schon `false` ist; `v3ControlLink(true)` ruft in `PluginProcessor.cpp:1226-1227` selbst `controlV3.reconnect()` — der negative Callback ist reentrant aus dem positiven erreichbar. Der nach `kStopFristMs` detachte Thread (`:727-742`) ist NAK-184 und nicht Teil dieses Tickets.

REGEL, die den Defekt schließt (Technikentscheid des Dirigenten; Ursache, nicht Punkt):
R11 — Callback-Wirkungen sind generationsgebunden; überlappende Callbacks prägen keinen späteren Link. Wortlaut in docs/beweise/NAK-180.md §5.3, Spalte Regel. Kern: jede Wirkung von `v3ControlLink(true)` (Aufbau-Flags, `replayFaellig`, offener Bericht, Weckung) trägt die `wireGeneration` ihres Links; Verbraucher werten nur die aktuelle Generation; eine nach dem negativen Callback geschriebene Wirkung eines älteren positiven Callbacks ist für den nächsten Link inert; `v3ControlLink(false)` löscht für die sterbende Generation; das Replay leitet sich zur Ausführungszeit aus dem aktuellen lokalen Zustand ab (E7), nie aus einem Schnappschuss; Serialisierung im ControlClient nur zusätzlich und nur verklemmungsfrei für den reentranten Pfad und für `stop()` aus dem Client-Thread (B-CC-11).

WAS DU EINARBEITEST (nur docs/beweise/NAK-180.md):
1. §2.0: neuer Entscheid E10 mit dem gewählten Weg und der Begründung (warum Generationsbindung statt oder zusätzlich zu Serialisierung; was mit dem reentranten Pfad `:1226-1227` passiert; was mit `stop()` aus dem Client-Thread).
2. §2.1: die Linkende-Zusage so umschreiben, dass sie überlappende Callbacks ausdrücklich abdeckt (welche Wirkung wann inert wird; kein zweiter Ende-Callback nötig). Prüfe dabei jede Zeile der Aufbaufolge (Schritt 1 bis 6) darauf, ob sie unter einem gleichzeitig laufenden negativen Callback noch trägt, und schreibe das Ergebnis hin.
3. §2.2: neue Zeile N-34 (erzwungenes true/false-Interleaving, beide Fälle `reconnect()` und `stop()`+`start()`, Messgrößen und Rotbeweis wie in §5.3), Nachzug von N-23, N-26, N-28 (und jeder weiteren Zeile, die Aufbau-Flags, `replayFaellig` oder den Bericht nennt) um die Generationsbindung.
4. §2.4: Abgrenzung zu NAK-184 (detachter Thread) als ausdrücklich nicht zugesagt.
5. §3: die Quellzeilen aus §5.3 (ControlClient.h:239-243, ControlClient.cpp:695-768, :988-996, :1559, PluginProcessor.cpp:1172-1231).
6. §4.1/§4.3/§4.4/§4.6: Bauetappe (B3b oder eigene), Beine, Rotbeweis „Generationsbindung entfernt", Abweichung vom Ticketauftrag benennen, Prüflistenzeile verbinden↔trennen ehrlich stellen.
7. §5.3.1 „Nacharbeit 3 — was eingearbeitet wurde" mit Tabelle wie §5.2.1 und einem Selbstaudit-Absatz: Lies deinen Diff adversarial genau auf die Frage, ob eine NEUE Zusage entsteht, die unter Nebenläufigkeit nicht trägt — das war das Muster der Runden 2 und 3. Was du nicht zusagen kannst, sagst du in §2.4 ausdrücklich nicht zu.

Danach: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-180.md` grün, Commit nur dieser Datei mit Pathspec, `git pull --rebase`, Push, und deinen Turn mit der letzten Zeile „ETAPPE 1 NACHARBEIT 3 ABGESCHLOSSEN" beenden. Kein Bau, keine Nachfrage an den User.