Wiederprüfung Etappe 1 (Matrixprüfung 4) / Ticket NAK-181 (G4-Nacharbeit 2), Runde 3 — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, keinen Compiler, keinen Cargo-Lauf und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 525ac18064df281633749992218bff2788f776b6...1f2ed1012d30c3009ef1abb80cc0e468a8c53ba4 -- docs/beweise/NAK-181.md`
(ZIEL = 1f2ed1012d30c3009ef1abb80cc0e468a8c53ba4 = HEAD, vor und nach deinem Lauf identisch; eine uncommittete Datei docs/beweise/roh/NAK-181-matrixpruefung-4-auftrag.txt im Worktree ist dieser Auftrag selbst). Der übrige Ticketbereich — die Matrix vom Stand 20fc256 — wurde in Matrixprüfung 1 bis 3 bereits geprüft und ist hier nicht Prüfgegenstand. In dieser Etappe gibt es keinen Produktcode: Prüfgegenstand ist die Verhaltensmatrix in docs/beweise/NAK-181.md §2 (E4, N-18, N-18b, N-18c, N-18d, §2.3, §2.4), §3.3 und §4 (B5, §4.2, §4.4 Nr. 17/18); der Änderungsbericht des Workers steht in §5.5. Prüfstufe T3-Nacharbeit zum Gate G4.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus Matrixprüfung 3, docs/beweise/roh/NAK-181-matrixpruefung-3-20fc256.txt; Einordnung des Dirigenten mit der Regel je Befund in docs/beweise/NAK-181.md §5.4 und im Nacharbeitsauftrag docs/beweise/roh/NAK-181-matrixnacharbeit-3-auftrag.txt):
MP3-1 [P2] „Beziehe beide Sampleraten in den R4b-Fehlerweg ein — DEFEKT, MP2-3/R4b bleibt offen: Bei `sample_rate = 1e-308` beziehungsweise `hello.samplerate = 1e-308` lassen `NakamaEvidenz.cpp:162–184` und `ControlClient.cpp:464–472,1667,1740` den endlichen Wert durch; `broker/src/vertrag.rs:183–185` und `tools/eq-copilot/pruefe_v3_vertrag.py:203–205` lehnen ihn dennoch ab. Die behauptete Unerreichbarkeit stimmt deshalb für beide Sampleratenzeilen nicht: Nach dem vorgesehenen Helferwechsel bliebe `null` in Pflicht-Zahlenfeldern erreichbar, wodurch Snapshot beziehungsweise Hello schemaungültig würden (`eq-ipc-v3.schema.json:159,358`). B5 muss auch diese Vorprüfungen erweitern und Snapshot beziehungsweise Handschlag bei Verweigerung abbrechen." — Regel R4d: beide Sampleratenzeilen erreichbar; B5 erweitert die Vorprüfungen (Snapshot verworfen, kein Hello); N-18d misst beides; die übrigen „unerreichbar"-Zeilen mit derselben Frage nachgeprüft.
MP3-2 [P2] „Formuliere die Untergrenze anhand des Dezimalexponenten — DEFEKT, neuer Bruch von E4/R4c: Der zugesagte Bereich enthält beispielsweise `2e-308`, obwohl beide Riegel dafür `dez = -308` berechnen und ablehnen (`broker/src/vertrag.rs:181–185`, `tools/eq-copilot/pruefe_v3_vertrag.py:202–205`). Die Bedingung `dez > -308` bedeutet für nichtnullige Dezimalliterale einen Betrag von mindestens `1e-307`, nicht lediglich mehr als `1e-308`. Damit verspricht diese neue Bereichsbeschreibung die Übertragung von Zahlen, die der bestehende Leser verweigert. Übernimm hier die tatsächliche lexikalische `dez`-Bedingung des gerundeten Wiretexts." — Regel R4e: Untergrenze am Dezimalexponenten des gerundeten Wiretexts (`dez > −308`, Betrag ab `1e-307`); N-18b bekommt `2e-308` und `9.99e-308`, N-18 `1e-307`; jeder Randwert trägt seinen `dez`; Obergrenze mit derselben Rechnung.

GATE-BEZUG, wörtlich: P4-Exit-Gate (docs/FL-Nakama-Sonden-Design-Entwurf.md:4287-4289) Satz 2: „Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage." §49.2 Gate 6 (:3557): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." Invarianten aus CLAUDE.md: Schemas sind Verträge (v3 eingefroren; Writer, Reader, Fixtures, Cross-Language-Verbraucher im selben Änderungssatz); Audio-Thread ohne Sperren, Allokationen, Datei-, Pipe-, Netzzugriff und Logging; Nulltest bitidentisch; NaN-Ehrlichkeit; State verlustfrei.

Du beantwortest genau zwei Fragen:
1. Ist jeder der zwei Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Zeilen-ID, Manifestzeile, Quelle Datei:Zeile am ZIEL)? Prüfe an der Quelle: R4d gegen `NakamaEvidenz.cpp:162-164` und `ControlClient.cpp:464-472`, `:1667-1669` (Ausgänge existieren), `FeatureEngine.h:153` und `ControlClient.h:121` (Typ `double`), und die Behauptung, dass genau zwei Aufrufer (`staerke_mad`, `band_zentrum_hz`) am C++-Typ `float` unerreichbar bleiben (kleinster positiver `float` `1.4e-45`, `dez = −45`; prüfe den Feldtyp an der Quelle und ob ein `float`-Feld je in `double` gewandelt mit kleinerem Betrag entstehen kann); R4e gegen `vertrag.rs:181-185` und `pruefe_v3_vertrag.py:202-205` — rechne die Randwerttabelle in E4 selbst nach (`1e-307`, `2e-308`, `9.99e-308`, `9.9999999999999999e-308`, `1e308`, `1e300`, `1e-300`), einschließlich der Behauptung, dass `9.9999999999999999e-308` als Text `1e-307` reist und angenommen wird.
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile, einen Entscheid E1 bis E9, eine Zusage aus NAK-180, einen bestehenden Vertrag, einen Satz des Gate-Bezugs oder eine Invariante aus CLAUDE.md? Besonders: bleibt N-16 (bytegleich zwischen Locales) und N-19 (State-Hashes unverändert) mit der berichtigten Untergrenze verträglich; ist die vierte Klassenregel in E4 mit den drei bestehenden konsistent; führt die erweiterte Vorprüfung von `audioGueltig` zu einem neuen Verhalten für heute gültige Sampleraten (44100 bis 768000)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1 bis 3: DEFEKT (bricht Gate, Matrix, Regel, Entwurf, Vertrag, Code am ZIEL oder Invariante — je Defekt Zeilen-ID, Datei:Zeile, welcher Satz bricht), LÜCKE, HÄRTUNG. Ausschlüsse: docs/** außerhalb von §2 bis §5.5 dieses Manifests (Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände; docs/beweise/G4.md, NAK-180.md, offene-punkte.md); im Register datierte Härtungen und Lücken NAK-155 bis NAK-179 und NAK-183 bis NAK-192; Nebenbefunde §4.7 Nr. 1 (`seitenanteil_db`, M-45) und Nr. 3 (`$defs/cycle` Pflichtfelder, in B5 mitgezogen) sowie die `epochwechsel`-Benennung des Kanalwechsels (NAK-194) liegen außerhalb; tools/dirigent/pruefliste.md ist keine Anforderungsquelle; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; Ticketfremdes (NAK-180, NAK-182, Anzeigefläche, v2-Telemetriebaum) nein; Kosmetik, Stil nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID oder Manifestzeile, Quelle Datei:Zeile, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
