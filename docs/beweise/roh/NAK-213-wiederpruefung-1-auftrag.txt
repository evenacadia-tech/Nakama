Wiederprüfung G5-Nacharbeit 2 / Ticket NAK-213, Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff c7514fe2d5b573699ab2015bb099eb344bb50f89...91ad6f2058642bcc0c69c4dd6c469674dddb7255 -- broker/src/coordinator/hypothese.rs broker/src/coordinator/hypothese_verdrahtung.rs broker/src/coordinator/invalidierung_verdrahtung.rs broker/src/coordinator/liveness.rs broker/src/coordinator/mod.rs broker/tests/sonde014_gegenbeispiele.rs broker/tests/sonde014_verdrahtung.rs`
(ZIEL = 91ad6f2058642bcc0c69c4dd6c469674dddb7255 = HEAD, vor und nach deinem Lauf identisch; VORHER = c7514fe, der Stand vor der Runde — gegenüber dem in der Erstprüfung geprüften Stand 0491c6a nur drei Rohdateien unter docs/beweise/roh/, kein Codeunterschied). Der übrige Ticketbereich wurde in der Erstprüfung (Thread 01a07fb5-4029-7602-a198-e1a0d3561661, Stand 0491c6a) bereits geprüft und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); keine Prozesse des Produkts, kein cargo, kein Python-Interpreter; du schreibst nichts.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus dem vorigen Urteil, docs/beweise/roh/NAK-213-erstpruefung-1-0491c6a.txt; je Befund die Regel des Dirigenten aus docs/beweise/roh/NAK-213-nacharbeit-1-auftrag.txt):
1. „[P2] Bewahre Main-Belege auch ohne eindeutige Führung — broker/src/coordinator/hypothese_verdrahtung.rs:282-284. DEFEKT: Vollständigkeitsmarke setzen, Main A gültige Fenster liefern lassen, anschließend ein zweites Main derselben Sitzung automatisch beitreten lassen und A erneut messen lassen; Sonden liefern noch nichts. Bei `fuehrendes_main = None` werden hier sämtliche Main-Profile verworfen. `enthaltung_ohne_ort` sammelt anschließend nur Kandidaten- und Masterfenster und liefert trotz gültiger Sitzungsbelege `None`: Die Sitzung schweigt. Das verletzt R4 und die sitzungsweite Belegsammlung aus E7. Die Main-Belege müssen unabhängig von der Führungswahl erhalten bleiben." Regel: Die Aufnahme trägt die Profile ALLER Main-Clients der Sitzung; die Enthaltung ohne Ort sammelt `evidence_ids` über alle Quellen einschließlich aller Mains; nur ohne jeden gültigen Beleg bleibt es bei R8 (kein Befund). Eine Zeile misst die Eingabefolge des Prüfers mit Rotbeweis (laut Manifest K-54); K-31/K-35/K-38 bleiben grün.
2. „[P2] Halte Kanalwechsel bei Store-Fehlern konsistent — broker/src/coordinator/liveness.rs:513-518. DEFEKT bei verweigertem Store-Append: Main und Sonde liefern zwölf gedeckte, ausgerichtete Fenster mit positivem Zusammenhang, die Sonde zunächst ohne Mixerindex; danach Store stoppen und einen Heartbeat mit Kanal 9 senden. Der Deskriptor ist bereits ersetzt, wenn `invalidierung_anwenden` scheitert. Dessen Fehlerzweig nimmt die Evidenzausschlüsse zurück und berechnet sofort neue Befunde: Die alten Fenster zählen nun unter Kanal 9, bis hin zu `hoch`. Das anschließende Setzen von `link.trennen` verhindert diese Neuberechnung nicht. Damit bricht R5: Fenster vor dem Wechsel dürfen nicht zum Routing danach zählen. Deskriptorwechsel und fehlgeschlagene Invalidierung benötigen einen konsistenten Rückweg." Regel: Ein Kanalwechsel wird erst wirksam, wenn seine Invalidierung angenommen ist; scheitert der Append, wird der Deskriptorersatz für den Kanal zurückgenommen, BEVOR der Fehlerzweig neu rechnet, und der Link gilt als storeverweigert; kein Fenster von vor dem Wechsel zählt je unter dem neuen Kanal. Positionswechsel unverändert (§2.9 Nr. 4). Eine Zeile misst die Eingabefolge des Prüfers mit verweigerndem Store und Rotbeweis (laut Manifest K-55).
3. „[P2] Fahre K-49 mit einem echten Store — broker/tests/sonde014_verdrahtung.rs:1805-1808. DEFEKT im zugesagten Prüfaufbau: `coordinator()` erzeugt `Coordinator::mit_uhr` ohne Store. Dieser Test erreicht deshalb ausschließlich den `Ok(None)`-Zweig der Invalidierung, nicht deren Persistierung und Zustellung über den Store. K-49 verlangt ausdrücklich ‚Der Broker läuft mit Store'. Der Test muss einen Store verwenden und die Invalidierung der acht Belege mit `messpunkt_wechsel` auf diesem Weg bestätigen." Regel: `kanalwechsel_ueber_den_heartbeat_befehl` läuft mit Store und bestätigt die Invalidierung der acht Belege mit `messpunkt_wechsel` über den Store-Weg (persistiert und zugestellt), zusätzlich zum Zähler; K-49 nennt genau das, was gemessen ist.
4. „[P2] Prüfe F4 auch ohne weitere Messung der gewechselten Quelle — broker/tests/sonde014_gegenbeispiele.rs:1231-1235. DEFEKT gegenüber K-44: Die Kanalwechselfolge sendet anschließend immer auch ein neues Fenster von C. Der ausdrücklich verlangte zweite Ablauf fehlt: C wechselt per Heartbeat von Kanal 7 auf 9, danach messen nur Main und A weiter. Die Prüfung `stark(&nachher) == 0` ist bereits wegen der konstanten Pegel durch NAK-212 R1 erfüllt und belegt die zusätzliche Konkurrenzregel nicht. Ergänzt werden muss der zweite Ablauf mit dem sichtbaren Ausschluss von C als `evidenz_zurueckgenommen` und der zugesagten Begrenzung von A." Regel: Der Test fährt beide Lagen; in Lage 2 trägt der führende Befund C als `(…, evidenz_zurueckgenommen)` sichtbar und A höchstens `mittel` mit `Konkurrenzlage::Messausschluss` als gemessenem Grund; in Lage 1 wird As Grund ebenso gemessen; die bedingte Prüfung `if let Some(f) = c_befund` wird zu einer messenden Aussage.

GATE-TEXT, wörtlich (docs/bauaufteilung-sonden.md:395-400): „**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — „G5 ist das ungewöhnlichste Gate: Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer." Regeln R1 bis R8 wie in der Erstprüfung; Matrix docs/beweise/NAK-213.md §2 (jetzt K-01 bis K-55) ist Referenz; laut Manifest §5 „Nacharbeit 1": A4 GRÜN 712 Tests über 29 Binaries, A31 17/0, `sonde014_verdrahtung` 35/0 (gelesen, nicht nachgemessen).

Du beantwortest genau zwei Fragen:
1. Ist jeder der vier Befunde am Zielstand geschlossen (je Befund: ja/nein, mit Beleg Datei:Zeile; bei 1 und 2 auch: ist die Regel des Dirigenten so umgesetzt, wie sie oben steht, und ist die Eingabefolge des Prüfers jetzt durch einen Test mit Rotbeweis gedeckt)?
2. Hat der Fixdiff etwas gebrochen — einen Test, eine Matrixzeile oder einen Entscheid (insbesondere K-19, K-31, K-35, K-38, K-40 bis K-44, K-49, K-53; E7, E8), einen Satz des Gate-Textes, eine Regel R1 bis R8, eine unverändert geltende Referenzzeile (M-24, M-25, M-27, M-28, M-86, M-87; NAK-212 E6, N-43; SONDE-013 R24 Positionswechsel unverändert), eine Invariante aus CLAUDE.md? Achte besonders auf: die Reihenfolge Deskriptorersatz ↔ Invalidierung ↔ Fehlerzweig (kein zweiter Lock, kein Deadlock, kein doppelter Ausschluss beim wiederholten Heartbeat), Determinismus der Belegsammlung über mehrere Mains (M-25), und ob ein Fassung-3-Consumer oder der Vertrag berührt ist (eq-copilot/ darf in dieser Runde unberührt sein — bestätige das).

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen und Ausschlüsse wie in der Erstprüfung (docs/** und CLAUDE.md ausgeschlossen; Härtungen NAK-191 bis NAK-211 und NAK-215 bis NAK-222; NAK-214 und S31b ticketfremd; Prüfliste keine Anforderung; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung).

URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
