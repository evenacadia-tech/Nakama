# NAK-314 — S25m: Tiefenaudit 3 abarbeiten, Teil 6: Broker (W13, W14, W15, W16, W17, W25, W28)

| Merkmal | Wert |
|---|---|
| Ticket | NAK-314 (Register `docs/offene-punkte.md`), Planschritt **S25m** (`docs/plan/plan.json`), Prüfstufe **T2**. Quelle: Tiefenaudit 3 (`docs/audits/2026-09-15-tiefenaudit/BEFUNDE.md`: Phasen 08, 10, 14, 15, 16; Arbeitsübersicht Phase 16 W13, W14, W15, W16, W17, W25, W28). |
| Gate | Gate-Text S25m (`docs/plan/plan.json`), wörtlich: „INHALT: 10 Befunde im Broker — T3-08-01 (Domain-Atomarität am 64er-Batchrand; NAK-157), T3-08-02 und T3-08-03 (Rücknahme vor Erstpersistenz nach Restore wieder gültig, Commit- und Publikationsordnung; NAK-269), T3-15-12 und T3-15-13 (degradierter Store trennt Clients im Sekundentakt, transienter Lesefehler lässt Routing fail-closed ohne Anzeige), T3-16-02 (verspätetes Ende einer alten Telemetrieverbindung entkoppelt die angenommene Nachfolgerin), T3-15-15 und T3-14-03 (Klonkonflikt ohne Löser mit dauerhaften Sperren, Unknown nach reinem Sonde-Abbruch ohne Rückweg), T3-16-03 (erfolgreich geschriebene P1-Evidenz geht beim gewöhnlichen Linkende verloren), T3-08-05 (vollständiger Loop-Wrap invalidiert gültige Evidenz entgegen M-53). Produktentscheide als Karten: Klon-Identität und Sperrenende, Empfangs-ACK oder gezählter Verlust. GRENZE: ACK erst nach vollständigem Commit, Replay idempotent, keine Callbacks auf abgemeldeter Sitzung, Konfliktrestore fail-closed, keine Sicherheitsneubewertung (S25i). BEWEIS: docs/beweise/NAK-314.md mit Matrix, Rotbeweisen in A4, A4-SI (store_crash_matrix), A22, B8, vollem Kanon GRÜN und Laufzeit-Arm." |
| Klasse | **10 von 10 Befunden an der Quelle bestätigt oder präzisiert, keiner widerlegt** (Validierung 26.09.2026, §0: Teil A 2/2/0, Teil B 2/1/0, Teil C 2/1/0 bestätigt/präzisiert/widerlegt). Kategorievorschläge der Validierer: 8 DEFEKT (T3-08-01, T3-08-02, T3-08-03, T3-08-05, T3-15-12, T3-15-13 [Audit: HÄRTUNG], T3-16-02, T3-15-15), 2 LÜCKE mit Technikregel (T3-14-03, T3-16-03); Produktentscheid nur für die Klon-Wegwahl (Karte U50, Text zu präzisieren). Einordnung durch den Dirigenten (§1) steht aus. |
| Etappe | **Etappe 0 von 7 angehalten 26.09.2026** (vorläufige Zählung; der Etappenplan §4 entsteht nach der Einordnung): Quellvalidierung abgeschlossen, Einordnung §1, Regeln §2, Karten §3 stehen aus. Halt auf User-Wort 26.09.2026 („stoppe den bau nach dem nächsten worker ende"): kein Matrix- oder Bau-Worker gestartet. **Halt: Einordnung §1 folgt in einer neuen Session.** |
| Urteil | offen — kein Prüferurteil zum Ticket (T2 folgt nach der letzten Etappe). |
| Prüfstufe | **T2 gefordert** (Muster NAK-311 bis NAK-313, NAK-380). |
| Prüfmodell | Codex `gpt-6-astra`, Effort xhigh (NAK-362), lesend, für Matrixprüfung, Erstprüfung je Etappe und Abschlussprüfung; nie der Bauer. Bei Codex-Woche ≥ 95 % (Cockpit-Telemetrie 26.09.2026, 15:10 Uhr: 95 %) prüft ein frischer Opus-5.5-Thread, Effort xhigh (Skill §3.6, NAK-404; Muster NAK-380 §62). Bauer: frischer Opus-5.5-Worker (`--model opus`), Effort xhigh (NAK-404). Validierer der Etappe 0: lesende Opus-5.5-Agenten, Effort xhigh. |
| Aufsicht | **ENG** (Store, Nebenläufigkeit, Lebenszyklus, Vertrag, Cross-Language). |
| Basis-SHA | `4ee4c40752845d98fb451f550ddf8743079153be` (= `git rev-parse HEAD` = `origin/master` beim Start der Validierer, 26.09.2026, 15:21 Uhr; `git status --short` zeigte nur die fremden Ordner `briefing-hub/` und `nimbalyst-local/`). |
| Kanon vorher | Geerbt, solange kein Produktpfad wandert: NAK-380 Abschlusskanon **GRÜN 70/70** an `3e5d3660` (26.09.2026, `docs/beweise/NAK-380.md` §62); `git diff --stat 3e5d3660 4ee4c407 -- eq-copilot broker tools` berührt nur `tools/fl/szenario.py`, `tools/fl/LIES-MICH.md` und die Installer-Hashes, keinen Produktpfad. |
| Kanon nachher | steht aus. |
| Laufzeit-Arm | steht aus (Abschluss, `tools/fl/laufzeit.ps1 -Ticket NAK-314 -Basis 4ee4c407 -Beenden`). |
| Ticketpfade | Vorläufig, präzisiert im Bauplan der Etappe 1: `broker/src/store/` (writer, handle), `broker/src/coordinator/` (befehl, evidenz, invalidierung, invalidierung_verdrahtung, flush, liveness, link, senke, subscription, mitgliedschaft, mod), `broker/src/instance_alias.rs`, `broker/src/lebenslauf.rs`, `broker/src/transport/server_v3/` (queues, verbindung, senke), `broker/src/transport/bootstrap.rs` (nur Kopplung und Backoff), `broker/tests/` (store_crash_matrix, sonde013_verdrahtung, sonde013_taint, coordinator_model, contract_cross_language); C++-Anschluss nur, soweit Vertrag oder Verbraucher betroffen: `eq-copilot/plugin/src/SourcesModel.cpp`, `eq-copilot/plugin/core/ipc/controlclient/`, `eq-copilot/plugin/core/ipc/TelemetryClient.cpp`, `eq-copilot/plugin/sonde/SondeProcessor.*`, `eq-copilot/plugin/src/prozessor/Ipc.cpp`; `tools/beweise.ps1` (Behauptungszeilen A4, A4-SI, A22, B8); Vertragstexte unter `eq-copilot/schemas/v3/` nur mit Matrixzeile. |
| Grenze | Gate wörtlich: ACK erst nach vollständigem Commit, Replay idempotent, keine Callbacks auf abgemeldeter Sitzung, Konfliktrestore fail-closed, keine Sicherheitsneubewertung (S25i ruht nach NAK-378; Sicherheitsbefunde T3-12-* sind nicht Gegenstand; Bootstrap-Tor und Pipe-Autorisierung gehören NAK-310). Invarianten `CLAUDE.md`: Audio bleibt echtzeitfest; Schemas sind Verträge (für v3 sind Discriminator, Zieladresse, Revision und Capability nicht additiv erweiterbar); State bleibt verlustfrei; keine toten UI-Elemente; Designteil geparkt (die sichtbare Form eines Konflikts auf der Probeeq bleibt geparkte Produktfrage; hier nur Diagnosezustand, Vertrag und Rückweg). Keine Legacy-Umbenennung, keine Neben-Refactors. Produktentscheide (Karten) werden nicht vor der Antwort gebaut. |
| Fremde Dateien | `briefing-hub/`, `nimbalyst-local/` (untracked, fremd, nie anfassen). |

**Alles unter diesem Kopf ist append-only Verlauf.** Der lebende Kopf wird beim
Abschluss nachgezogen; Positionen im Kopf stehen als Symbol oder Anker, im
Verlauf gilt eine Zeilennummer zum Stand des Abschnitts, der sie trägt
(`tools/dirigent/pruefliste.md`, Abschnitt E).

---

## 0. Übernahme (Dirigent, 26.09.2026, 15:20 Uhr)

**Reihenfolge im Plan.** `py -3.13 tools/plan/planstand.py` am Sessionstart
(Quellstand `c0c3eeb9`, 39 abgenommen, 3 gebaut, 59 gesamt): „Als Nächstes“
nennt S25m (NAK-314). Die drei Schritte ohne Urteil bleiben stehen: S25i
(NAK-310) ruht seit 23.09.2026 nach ausgeschöpftem Rundenbudget der
Matrixprüfung (NAK-378); S25l (NAK-313) wartet mit Etappe 8 auf die Antwort
zu Karte U65 und mit dem Laufzeit-Arm auf den Abschluss; S25g (NAK-289)
bleibt hinter den Audit-Tickets (`docs/beweise/NAK-311.md` §0). Keine
Änderung an dieser Reihenfolge.

**Vorprüfung §3.1.** `git status --short`: nur die fremden Ordner. `claude
agents --json --all`: kein arbeitender oder eingabebedürftiger Agent; die
Sitzung `nakama-1f` ist interaktiv und idle, kein Schreiber. HEAD `4ee4c407`
= `origin/master`. `CronList`: leer. Cockpit-Telemetrie (15:10 Uhr):
Kontext 11 %, Claude 5 Stunden 21 %, Claude-Woche 30 %, Codex-Woche 95 %
(„Kontingentgrenze erreicht“; Folge: Prüfer sind frische Opus-5.5-Threads
xhigh, Skill §3.6); zweite Meldung „Änderungen ohne sichtbaren Worker“ =
die fremden Ordner. Ticketanker gesetzt: `cockpit.ps1 -Anker -Ticket
NAK-314` (15:14 Uhr). Zeitfenster für Produktfragen offen (15:21 Uhr).

**Gegenstand.** Die 10 Befund-IDs des Gates, gebündelt nach der
Arbeitsübersicht Phase 16: W13 (T3-08-01), W14 (T3-08-02, T3-08-03), W28
(T3-08-05), W15 (T3-15-12, T3-15-13), W16 (T3-16-02), W17 (T3-15-15,
T3-14-03), W25 (T3-16-03). Ursachenbündel des Audits: B14 (Domain-Atomarität
im Store), B15 (Commit, Invalidierung und Veröffentlichung), B22
(Transportepoche versus gültiger Loop). Offene Karte: U50 (Klon-Identität,
seit 18.09.2026, mit dem Zusatz „Ohne Antwort baut der Dirigent Weg 1“, der
gegen die Regel „Produktentscheide nicht vor der Antwort bauen“ steht und in
§3 entschieden wird). Zur zweiten im Register genannten Karte
(Empfangs-ACK oder gezählter Verlust, W25) trägt `docs/plan/fragen.json`
keine Karte; ob es eine Produktwahl ist, prüft Teil C.

**Codeänderungen seit dem Audit.** Die Auditzeilen wurden am Stand
`aff2d818` (18.09.2026) oder älter gemessen; seitdem änderten 21 Commits
`broker/` (NAK-309 Etappe 4; NAK-313 Etappen 4 bis 7; NAK-380 Etappen 2 bis
6). Die Validierer lesen jede Zeilennummer am HEAD nach.

**Quellvalidierung (Etappe 0).** Drei lesende Opus-5.5-Agenten, Effort
xhigh, Hintergrundprozesse `nakama-nak314-4ee4c40-validA/B/C` (Kennungen
`f6d00246`, `9300e8ab`, `93e99e08`), am HEAD `4ee4c407`, Start 26.09.2026,
15:21 Uhr, Werkzeuge Read, Grep, Glob, Bash (Git lesend), Write (nur die
eigene Berichtsdatei): Teil A (W13, W14, W28: T3-08-01, T3-08-02, T3-08-03,
T3-08-05; Regelfragen atomare Domänentransaktion, Ordnung von Commit und
Publikation, Loop-Wrap-Grenze) nach
`docs/beweise/roh/NAK-314-quellvalidierung-teil-A.md`; Teil B (W15, W16:
T3-15-12, T3-15-13, T3-16-02; Regelfragen Livestand ohne Commit bei
degradiertem Store, ehrliche Diagnose bei fail-closed Routing,
Telemetriegeneration) nach
`docs/beweise/roh/NAK-314-quellvalidierung-teil-B.md`; Teil C (W17, W25:
T3-15-15, T3-14-03, T3-16-03; Karte U50, Regelfragen Unknown-Rückweg,
Empfangs-ACK oder gezählter Verlust, Grenzen zu Teil A und B) nach
`docs/beweise/roh/NAK-314-quellvalidierung-teil-C.md`. Aufträge (Zeiger
im Startbefehl, Volltext im Repo):
`docs/beweise/roh/NAK-314-quellvalidierung-teil-A-auftrag.txt`,
`docs/beweise/roh/NAK-314-quellvalidierung-teil-B-auftrag.txt`,
`docs/beweise/roh/NAK-314-quellvalidierung-teil-C-auftrag.txt`. Je Befund:
Quellkette zitiert, Zusage wörtlich, heutige Abdeckung, Urteil,
Kategorievorschlag, kleinster Fix, Ort des Rotbeweises; dazu gemeinsame
Ursachen, Regelfragen mit Trennung Technik und Produkt, Reihenfolge,
Nichtgeprüftes. Ausschließlich Quellenlektüre: kein Bau, kein Lauf, kein
Host. Startbeleg: `claude agents --json` zeigte alle drei `working`.

**Ergebnis der Quellvalidierung (gemessen 26.09.2026, 15:54 Uhr).** Alle drei
Prozesse `done`; Ende laut Dateizeit der Berichte: Teil A 15:47, Teil B
15:49, Teil C 15:53 Uhr. HEAD blieb `4ee4c407`; `git status --short` zeigte
danach nur die drei Berichte, die drei Aufträge, dieses Manifest und die
fremden Ordner. Schlusszeilen wörtlich: „FERTIG Teil A, 2 bestätigt, 2
präzisiert, 0 widerlegt“, „FERTIG Teil B, 2 bestätigt, 1 präzisiert, 0
widerlegt“, „FERTIG Teil C, 2 bestätigt, 1 präzisiert, 0 widerlegt“.
Übersicht aus den Berichtsköpfen (Kategorie und Schwere sind Vorschläge der
Validierer; Zeilennummern gelten zum Stand `4ee4c407`; die Einordnung des
Dirigenten §1 steht aus):

| ID | Urteil des Validierers | Vorschlag | Schwere | Produktentscheid |
|---|---|---|---|---|
| T3-08-01 (W13) | BESTÄTIGT; auch ein SQLite-Fehler im zweiten Abschnitt führt ohne Absturz zum falschen `idempotent_wiederholt`; der Test B14 unterscheidet Teilung und Ganzheit nicht; die Teilung ist eine SONDE-011-Bauregel | DEFEKT | high | nein, Technikregel |
| T3-08-02 (W14) | BESTÄTIGT; der serielle Fall tritt bei jedem Rücksprung mit neuer Epoche ein, also bei jedem Loop-Wrap; dort irrt der Cache, nicht der Store | DEFEKT | medium | nein, Technikregel |
| T3-08-03 (W14) | PRÄZISIERT: Umordnung vor der Queue trägt; die Verbraucherwirkung ist global, fail-closed und heilt mit dem nächsten Snapshot; zweite, umgekehrte Lücke (Erfassung vor Markierung, Commit danach), „nach `event_ord` sortieren“ allein ist kein Fix | DEFEKT | medium | nein, Technikregel |
| T3-08-05 (W28) | PRÄZISIERT: breiter als behauptet; jeder Loop-Wrap, mit oder ohne Passage, nimmt alle Evidenz aller Quellen der Sitzung vor dem alten Frameende zurück; die gebundene Passage kennt der Broker nur nach `experiment_begin`, das heute keinen Bedienweg hat | DEFEKT (Passagenfall gegen M-53, allgemeiner Loopfall gegen Entwurf §32.3) | high | nein, Technikregel |
| T3-15-12 (W15) | BESTÄTIGT; P2 fällt nach dem ersten Heartbeat still statt mit `routing_nicht_freigegeben`; ein voller Storekanal trennt auch bei gesundem Store jeden Heartbeat-Absender; das A4-Bein `degradierter_store_verweigert_annahme_und_tick_trennt_verursacher` schreibt den Defekt als gewollt fest; der Heartbeat-ACK meldet im Startfall jedem Link `duplicate_instance_id: true` | DEFEKT | high | nein, Technikregel |
| T3-15-13 (W15) | PRÄZISIERT: die falsche Anzeige bricht `CLAUDE.md` („Keine toten UI-Elemente“) und SONDE-012 L15 wörtlich (DEFEKT statt HÄRTUNG); nach dem Latch liest der Resubscribe nie wieder; der Sperrgrund wird verworfen (`_grund`) | DEFEKT | low | nein, Technikregel |
| T3-16-02 (W16) | BESTÄTIGT; die Zusagenbasis ist ein Matrixwiderspruch (C-LS-04 „höchstens einmal je `link_id`“ gegen B-TC-10 und den Transport, der Neukopplung vorsieht); `telemetrie_einmalig_gekoppelt` schreibt die `link_id`-Semantik fest; nach jeder Neukopplung bleibt die C-LS-06-Reihenfolge am späteren Control-Ende ungeprüft | DEFEKT | medium | nein, Technikregel |
| T3-15-15 (W17) | PRÄZISIERT: Mechanik trägt vollständig; die Sperre überlebt auch den Brokerneustart und endet erst mit einer neuen Sitzungsepoche; jeder Verdrängungswechsel setzt zusätzlich sticky Unknown für die ganze Sitzung (Kopplung an T3-14-03); der 512er-Deckel sperrt jedes neue Hello, auch Gen | DEFEKT | high | **ja**, Karte U50 mit präzisiertem Text; der Zusatz „Ohne Antwort baut der Dirigent Weg 1“ ist nicht haltbar (Weg 1 ändert den Vertragstext `nakama-state-v2.md` und den Entwurf und steht in Spannung zu SONDE-011 C-07); Empfehlung des Validierers bleibt Weg 1 |
| T3-14-03 (W17) | BESTÄTIGT; ein Sonden-Neuaufbau löst die Sperre nicht (Gegenprüfung Phase 15 K3 irrt); die Probeeq trägt keinen Interventionsstrom; gesperrte Evidenz geht ungezählt verloren; die Sperre entsteht schon beim gewöhnlichen Probeeq-Reconnect (`prepareToPlay`, `setStateInformation`) | LÜCKE, Technikregel | high, vorbehaltlich Messung | nein |
| T3-16-03 (W25) | BESTÄTIGT; SONDE-010 hat die At-least-once-Zustellung der Clienthälfte an SONDE-011 verschoben, SONDE-011 hat sie für Sonde→Broker nie aufgenommen; die Begründung von A-IN-04 setzt eine Senderwiederholung voraus, die es nach erfolgreichem Write nicht gibt; der Verwurf am Ingress ist ungezählt | LÜCKE, Technikregel | medium | nein; „Empfangs-ACK oder gezählter Verlust“ ist keine echte Wahl (Register NAK-314 nennt sie als Karte; keine Karte anlegen) |

Regelfragen (Kurzform; Volltext in den Berichten): Teil A sieht alle drei
Fragen als Technikregeln; Teil B ebenso, die ehrliche Diagnose bei
fail-closed Routing ist Technik, keine Oberfläche; Teil C: U50 bleibt eine
echte Produktwahl mit neuem Kartentext (Teil C, Regelfragen, Frage 1), der
Unknown-Rückweg und die Annahmeregel am Linkende sind Technikregeln.

**Halt (User-Wort 26.09.2026, 15:2x Uhr: „stoppe den bau nach dem nächsten
worker ende“).** Nach dem Ende der drei Validierer startet kein weiterer
Worker. Stand beim Halt: Etappe 0 mit abgeschlossener Quellvalidierung; die
Einordnung je Befund (§1), die Regeln (§2), die Karten (§3; U50-Text
präzisieren, dann im Zeitfenster 9 bis 23 Uhr stellen) und der Etappenplan
(§4) stehen aus und beginnen in einer neuen Session mit dem Lesen der drei
Berichte. Aufräumen: die drei Agenten per `claude rm` entfernt, Cron
`9ff4b6cc` gelöscht, `CronList` leer; der Ticketanker bleibt auf NAK-314.
Hygiene im Abschlussfenster (gemessen 15:5x Uhr): `gesundheit.py` Exit 4
mit den Maßen 4/33/39 wie beim Abschluss von NAK-380 (kein Produktpfad seit
`3e5d3660` geändert; Register NAK-413); `zielbild.py pruefen` Exit 4 (Karte
U66 fehlte im Text) → U66 in `docs/ZIELBILD.md` eingetragen, `abgleichen`
Exit 0, `pruefen` Exit 0; `dokuriegel.py` auf CLAUDE.md und Skill 0
Befunde; A33 (`tidy.py`) nicht neu gefahren, weil seit dem Abschlusskanon
von NAK-380 (A33 Exit 0, 58/58 an `3e5d3660`) kein C++-Pfad geändert ist.
