Matrixprüfung 1 NAK-181 / G4-Nacharbeit 2 — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, keinen Compiler, keinen Cargo-Lauf und keinen Python-Interpreter; du schreibst nichts. Prüfgegenstand ist ausschließlich die Verhaltensmatrix der Etappe 1 dieses Tickets:
`git diff 38573ed98e2d1c0d2c893cf877a1cdb89707db62...548bf9d4294918618e3b36a01973d1fc1f002663 -- docs/beweise/NAK-181.md`
mit BASIS = 38573ed98e2d1c0d2c893cf877a1cdb89707db62 und ZIEL = 548bf9d4294918618e3b36a01973d1fc1f002663 (HEAD; er muss vor und nach deinem Lauf identisch sein). Eine uncommittete Datei docs/beweise/roh/NAK-181-matrixpruefung-1-auftrag.txt im Worktree ist dieser Auftrag selbst, kein Prüfgegenstand.
In dieser Etappe gibt es keinen Produktcode: das Ticket berührt Vergleichs- und Evidenzverträge, Projektwechsel, Wire-Zahlform, Präsenzbits und Broker-Urteilssemantik, deshalb entstand zuerst die Matrix (docs/beweise/NAK-181.md §2 mit den Entscheiden E1 bis E9 in §2.0, den Zuständen und Threads in §2.1, der Matrix N-01 bis N-40 in §2.2, Zahlen §2.3, Nicht-Zusagen §2.4), die Ist-Messung am ZIEL (§3) und der Bauplan mit den Abweichungen vom Ticketauftrag (§4, besonders §4.4 Nr. 1 bis 8). Prüfstufe T3-Nacharbeit zum Gate G4.

GATE-BEZUG, wörtlich: P4-Exit-Gate (docs/FL-Nakama-Sonden-Design-Entwurf.md:4287-4289) Satz 2: „Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage." §49.2 Gate 6 (:3557): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." Entwurf §43.1 (:2837, :2842-2844; Match-Gain vorab, eingefroren; Neustartreihenfolge), §43.3 (:2876-2888; fünf Aussagen), §38.3/§38.5 (:2509-2526, :2560; zeitvariable Ketten, Match-Gain), §40.1/§40.3 (:2628-2669; Stereometrik, Präsenzbits, fail-closed), §15 Vergleichbarkeitsregeln (:1093-1098), §48.1 (:3413-3417; Kontinuitätsbruch), §32.4 (:1969; eine Passage, eine Epoche), §53.5 (State verlustfrei). Invarianten aus CLAUDE.md: Schemas sind Verträge (v3 eingefroren: Discriminator, Zieladresse, Revision, Capability nie; Save und Load gemeinsam testen); Audio-Thread ohne Sperren, Allokationen, Datei-, Pipe-, Netzzugriff und Logging; Passthrough im Nulltest bitidentisch; NaN-Ehrlichkeit (ohne genügend endliche Nachbarn keine Basislinie); State bleibt verlustfrei (unbekannte Major-Versionen read-only mit Originalbytes); Zeit ist aktive Musikzeit.

DEFEKTE, die die Matrix schließen muss (an der Quelle bestätigt, docs/beweise/G4.md §1, §5, §6 Satz 2, §7, §8, §11 Nr. 2; Quellenchecks docs/beweise/roh/G4-quellencheck-leser-9f30008.md V01, V02, V03, V05, V06, V09, V10 und docs/beweise/roh/G4-quellencheck-codex-9f30008.md C3, C4, C5; Zeilenangaben dort gelten für 9f30008 — §3 des Manifests verortet sie am ZIEL neu, prüfe diese Verortung): V01 lebender Match-Gain und Nichtendlich-Zähler im `experiment_candidate` (M-43, M-07); V02 `versuchspegelSpeist` nach „zu wenig Material" tot (entschiedene Lücke, retryfähig); V03 `setStateInformation` lässt Versuch/Passage stehen, alte `experiment_id` unter neuer `project_binding_id` (M-50, M-13); V05 `std::to_string(double)` in NakamaEvidenz.cpp und ControlClient.cpp locale-abhängig (Vertrag); V06 `korrelationKurz`/`persistenz` als gemessene 0,0 ohne Kurzfenster (M-11, M-08); V09/V10 Gen-Empfänger ohne geschlossene Enums, `pair_id`-Länge, `ausschluss` fail-open (M-46, M-22); C3 Probeeq-Engine-Reset verschluckt den Quarantänebruch (M-53, Exit-Gate Satz 2); C4 `Stationaer` bei hartem Ausschluss (M-18, M-22); C5 Aktivzeit durch Projektspanne überschrieben (M-23).
REGELN R1 bis R9 des Dirigenten, wörtlich: docs/beweise/roh/NAK-181-auftrag.txt (Abschnitt REGELN). Die Matrix darf von den Regeln nur mit benannter Begründung in §4.4 abweichen; §4.4 nennt acht Abweichungen.
Referenzmatrix: docs/beweise/SONDE-013.md §3.1 (M-07, M-08, M-11: Zeilen 476-480), §3.2 (M-18, M-22, M-23: 493-498), §3.5 (M-43, M-46, M-50: 533-540), §3.6 (M-53: 548). Verträge: eq-copilot/schemas/v3/eq-ipc-v3.schema.json (`$defs/session_experiment`, `$defs/session_paar`, `$defs/pair_id`, `$defs/stereo_evidenz`/`stereo_bandwerte` mit `gueltig_bitmap`, `experiment_candidate`, `experiment_abort` samt description, `evidence_snapshot`), eq-copilot/schemas/v3/README.md; die ES6-Zahlnorm in eq-copilot/plugin/state/NakamaKanon.h/.cpp und ihre Fixtures.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: eine Matrixzeile N-01 bis N-40 oder ein Entscheid E1 bis E9 widerspricht einem Satz des Gate-Bezugs oben, dem Entwurf, einer Zeile der SONDE-013-Referenzmatrix, einem bestehenden Vertrag (Schema, Fixture, Rust-Leser), dem gemessenen Code am ZIEL oder einer Invariante aus CLAUDE.md; ODER eine Regel R1 bis R9 ist von keiner Zeile mit messendem Test gedeckt und §4.4 begründet die Auslassung nicht; ODER ein Defekt V01 bis C5 bleibt nach der Matrix auf einem erreichbaren Pfad offen; ODER die Matrix lässt einen Weg, auf dem eine starke Cross-Probe-, PRE/POST- oder Experimentaussage aus unvergleichbarem, unbekannt-zeitlichem oder ungemessenem Material entsteht; ODER ein Zustand wird aus dem Audiothread unter Lock, mit Allokation oder ohne definierte Speicherordnung berührt; ODER eine Zusage ändert ein Schemafeld, eine Fixture oder eine State-Hash, ohne dass §2.4/§4.2 es benennen und alle Cross-Language-Verbraucher im Änderungssatz liegen. Je Defekt: Zeilen-ID (N-xx oder Ex), Quelle Datei:Zeile, welcher Satz bricht.
- LÜCKE: Gate, Matrix, Regeln und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, kein NEEDS_WORK-Grund):
1. Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände in docs/**; der Manifestkopf; docs/beweise/G4.md; docs/beweise/NAK-180.md; docs/offene-punkte.md. Nur §2, §3 und §4 von docs/beweise/NAK-181.md sind Prüfgegenstand.
2. Im Register datierte Härtungen und Lücken NAK-155 bis NAK-179 und NAK-183 bis NAK-192 (NAK-177 Kurzfenster über die Snapshotgrenze; NAK-186 `Diagnose::incompatible`). Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung. Der Nebenbefund §4.7 Nr. 1 (`seitenanteil_db` erreicht den Broker nie, M-45) liegt außerhalb dieses Tickets und wird vom Dirigenten ins Register gezogen — er ist kein Defekt dieser Matrix.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. Ticketfremdes (NAK-180 Taint-Resync, Hörmarker, Pipe-Frist; NAK-182 Korpus, Goldens, A26, B19-Blockgrößen; v2-Telemetriebaum `setprecision(10)`; eine Anzeigefläche für `Sicht::experimente`/`paare`; ein Rust-Konsument für `korrelation_kurz`/`persistenz`): nein. Kosmetik, Stil: nein.

WAS DU TUST:
1. Lies das Manifest §2 bis §4 vollständig, dann die Regeln R1 bis R9, dann die zitierten Quellen am ZIEL und die Referenzmatrix.
2. Prüfe je Regel R1 bis R9: Welche Zeile(n) der Matrix decken sie, welcher Test misst jede Zeile (bestehend mit Datei::Fall oder NEU mit Bein), und nennt §4.1/§4.3 einen Rotbeweis, der genau an dieser Zusage fällt?
3. Prüfe E1/N-01 bis N-05 gegen PluginProcessor.cpp am ZIEL (`versuchReferenzJson`, `beginneVersuch`, `erfasseKandidat`, `bindePassagenfensterMitEpoche`, `prepareToPlay`) und Blindvergleich.h: Ist die Kopie im Blindvergleich wirklich zum Zeitpunkt von `beginneVersuch` vollständig (Gain UND Nichtendlich-Zähler)? Stimmt die Behauptung aus §4.4 Nr. 2, dass ein offener Versuch am ZIEL seinen eingefrorenen Gain auf keinem Weg verlieren kann — gibt es einen Pfad (Passage lösen, zweites Binden, `prepareToPlay`, Reload), auf dem ein Kandidat/Urteil ohne eingefrorenen Abgleich entsteht?
4. Prüfe E2/N-06 bis N-08 gegen Vergleichspegel.h `friereEin()` (drei Ausgänge) und `PluginProcessor.cpp:506-507`-Nachfolger: Ist „Speisung folgt dem Einfrierzustand" auf dem Audiothread ohne Lock lesbar, und bleibt N-08 (nichtendlich = Endzustand) mit M-07 verträglich?
5. Prüfe E3/N-09 bis N-15 gegen `setStateInformation` (Vollrestore und `nurLesen`-Zweig), `experiment_verdrahtung.rs:74-93` und `experiment_abort.description` im Schema: Ist der Brokerriegel wirklich vorhanden und misst N-11 ihn ohne Änderung? Bleibt der Versuch beim Broker offen, ohne dass M-50 („offenen Zustand anzeigen, keine stillschweigende Fortsetzung") gebrochen wird — gibt es nach dem Reload einen Weg, auf dem Gen den alten Versuch fortsetzt (z. B. über `laufenderVersuch` aus einem Broker-Snapshot, `Sicht::experimente`)? Ist die Reihenfolge Leeren → `pipe.stop()`/`reconnect()` so, dass kein P0-Byte des alten Versuchs reist (N-09 „kein einziges P0-Byte")? Sind die zwei Mutexe (`versuchMutex`, `passagenBindungMutex`) nachweislich nie aus `processBlock` genommen (N-15)?
6. Prüfe E4/N-16 bis N-20: Ist die ES6-Zahlform aus NakamaKanon für den v3-Draht zulässig (Schema-Zahlen, Rust `serde_json`, Python-Prüfer A5 lesen sie identisch)? Ändert die Extraktion in `core/ipc/WireZahl.h` (JUCE-frei) das Ergebnis von `zahlAlsEs6` für den bestehenden Fixture- und State-Korpus (N-19)? Welche v3-Fixtures ändern ihre Bytes (§4.2), und deckt der Erzeuger `tools/eq-copilot/erzeuge_v3_fixtures.py` genau diese — bleibt A8 `--pruefen` beweisfähig? Bricht `heartbeat-wire-v1.json` (NAK-180 N-33)?
7. Prüfe E5/N-21 bis N-25 gegen FeatureEngine.h (`stereoAuswerten`, `stereoLeeren`, `evidenzLeeren`, `kKorrelationKurzFrames`) und NakamaEvidenz.cpp `stereoBandsatzJson`/`stereoJson`: Trägt jeder Bandsatz heute schon eine eigene `gueltig_bitmap` im Schema (N-24 „Schema bleibt unverändert") — oder teilen sich die Felder heute eine Bitmap, sodass ein eigenes Bit doch ein neues Feld wäre? Ist `korrelation_mittel` (§4.4 Nr. 3) am ZIEL wirklich derselbe Fehler? Liest `broker/src/coordinator/evidenz.rs` die Bitmaps je Bandsatz (N-25) — oder eine gemeinsame?
8. Prüfe E6/N-26 bis N-30 gegen SourcesModel.cpp und das Schema: Sind es genau die acht Enumfelder, stimmen die Wertemengen mit `$defs` überein, und liefert `ungueltig` denselben Ausgang wie die übrigen Typvergehen (kein Teilstand)?
9. Prüfe E7/N-31 bis N-35 gegen SondeProcessor.cpp `analyseZug`, FeatureEngine.h (`zuruecksetzen`, `grenzeZwischen`, `grenzeZiehen`, `vorigerBlock`, `habeVorigen`) und StampedAudioQueue.h: Erkennt die Engine nach dem Wegfall von `merkmale.zuruecksetzen()` die Grenze am nächsten Deskriptor in JEDEM Auslöser (Drop, Seek, Beweislagewechsel, Kanal-/Tap-Wechsel), oder gibt es einen Bruch, den nur der Reset abfing? Leert `grenzeZiehen` denselben Fenstersatz (N-35) — welche Zähler beweisen es? Ist der Grund je Auslöser mit M-53 (Drop → `sequenzluecke`, sonst `epochwechsel`) deckungsgleich?
10. Prüfe E8/N-36, N-37 und E9/N-38 bis N-40 gegen prepost.rs und prepost_verdrahtung.rs: Deckt N-36 alle zehn Ausschlüsse (acht über `beurteile_paar`, zwei über `bilde_paare`)? Hält die Trennung `aktiv_s`/`spanne_s` `schaetze_restlag` und `capture_s` Zeichen für Zeichen gleich (N-40), und ist der Testhelfer `haelfte` so umgestellt, dass die bestehenden Fälle nicht still ihr Ziel wechseln?
11. Prüfe §4.4 Nr. 1 bis 8: Ist jede Abweichung an der Quelle begründet und bricht keine Regel; ist §4.4 Nr. 5 (`NakamaKanon.cpp` Extraktion) wirklich verhaltensneutral; enthält §4.1 eine Etappe, die ohne offenen Entscheid nicht ehrlich fortfahren kann?

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Zeilen-ID oder Manifestzeile, Quelle Datei:Zeile, welcher Satz von Gate, Regel, Entwurf, Vertrag oder Invariante bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
