# NAK-309 Erstprüfung 4 — Rohbericht des Prüfers (wörtlich)

Prüfer: frischer, lesender Opus-Thread (Agent der Dirigentensession), 19.09.2026; Auftrag `docs/beweise/roh/NAK-309-erstpruefung-4-auftrag.txt`; per Skript aus dem Transkript gezogen, nicht abgetippt; 84 Werkzeugaufrufe, 958 Sekunden, nichts geschrieben.

---

URTEIL: PASS — kein DEFEKT (zwei LÜCKEN, drei HÄRTUNGEN benannt).

**geprüft.** Der vollständige Änderungssatz `git diff 8d573e9f...49dab04d` über die 13 Auftragsdateien, Datei für Datei (12 Dateien tatsächlich berührt, +883/−71). Am Zielstand selbst nachgelesen: in `tools/beweise.ps1` der Kopf, `Fuehre-Aus` samt neuem `-Umgebung`-Parameter, die Beinschleife inklusive Meldeordner-Anlage und Klassifikationsblock, die Grünzählung, der ganze Urteilsblock, `$gemesseneZiele`, `$zuBauen`, die Baustandübergabe und die Behauptungszeilen A4, A4-SI, A4b, A36, B22; in `tools/eq-copilot/pruefe_beweisrunner.py` `baustand()` (Populationsbildung, Urteilsschleife, Kreuzprobe, Inventar, Exitabbildung), `nicht_gelaufen()`, `nicht_gelaufen_cli()`, `gemessene_im_runner()`, `urteilsvorrang()`, `URTEILSFOLGE`, die drei neuen Selbsttestfälle und die Fallliste; in `broker/tests/store_crash_matrix.rs` `junction_legen`, die zwei Skip-Stellen, `melde_nicht_gelaufen`, `nicht_gelaufen`, der neue Test und die Testattribute; in `broker/src/briefkasten.rs` der neue Test plus `JOIN_FRIST` (:63), `stoppen_mit` (:859-892), `beim_schreiben` (:1025) und `#[cfg(test)] mod tests` (:953); in `IpcTestMain.cpp` die Abschnittsmarken und beide neuen Blöcke, in `V3TestServer.h` die zwei Schalter und ihre Vorgaben; in `StateMigrationTestMain.cpp` der Block `main-binding-v1` samt Abschnittsmarke G7 und der Gegenfall `:1013`; in `Sonde013FingerprintGoldenTest.cpp` der ganze M-26-Abschnitt; in `erzeuge_state_fixtures.py` `baue()`-Goldenliste und `main()`-Waisenprüfung. Zusätzlich am Zielstand gelesen, weil die Prüffrage es verlangte: `tools/plan/planstand.py` (`KANON`-Regex :102-112), `Verbindung.cpp` Welcome-Schleife :625-676 und Minorriegel :804-811, `TelemetryClient.cpp` :525-531 und :655-700, `WireEnvelope.h:81` (`kJsonSchemaMinor = 5`), `TelemetryClient.h:48` (`kFeatureBatchSchemaMinor = 2`), `Vertrag.h:332-358` (`Fingerprint`, `operator== = default`), `pruefe_installer_manifest.py` (`broker_pin_gegen_datei`, `[4c+]`), `pruefe_ipc_last.py`/`pruefe_sonde012_sources_latency.py`/`pruefe_session_soak.py` (Brokerpfade, Frischeprüfung), `PipeClientLifecycleTestMain.cpp` (M-18 bis M-28), `broker/Cargo.toml`. Selbst gemessen (lesende Kommandos): HEAD vorher/nachher, `git status --short`, `git diff --stat` über den ganzen Änderungssatz und über `broker/`, `plugin/src|dsp|state|vertrag`, `git log` je Datei, `git ls-files eq-copilot/fixtures/state/schema2/`, `git check-attr`/`git ls-files --eol`, `sha256sum` und Größe des neuen Goldens, Inhaltsmarken des Goldens. Extern verifiziert: `Start-Process -Environment` überschreibt einzelne Variablen in der geerbten Umgebung (kein `-UseNewEnvironment`) und existiert ab PowerShell 7.4 (Microsoft-Learn-Referenz zu `Start-Process`, Stand 7.6).

**nicht geprüft.** Nichts gebaut, kein Test, kein Kanon, kein Python-/PowerShell-Werkzeug, kein cargo/cmake, kein FL. Alle Laufzeitaussagen zu Beinergebnissen (Kanon nachher GRÜN 69/69 auf `dca46403`, Beleglauf UNVOLLSTAENDIG/Exit 3 mit zwei `[NOT RUN]`-Beinen, Einzelbeine, Rotbeweise M-57 bis M-72/M-75, A17-Ergebnis) habe ich nur den Rohdateien und Manifestabschnitten entnommen, nicht nachgemessen; sie stützen meine Codeaussagen, tragen sie nicht. Ob der neu gepinnte Broker-SHA-256 wirklich zu einem aus diesem Baum gebauten `eqcop-broker.exe` gehört, ist lesend nicht entscheidbar — ich habe nur gelesen, dass A17 `[4c]`/`[4c+]` den Header-Pin hart gegen die gebaute Datei hält und im grünen Kanon `[OK]` war. Die Bytegleichheit von `MANIFEST.json` zur Neuerzeugung (A12) habe ich nicht nachgerechnet; nur SHA-256 und Länge des neuen Goldens gegen den Manifesteintrag geprüft. `docs/**` und die im Auftrag ausgeschlossenen registrierten Härtungen sind nicht Gegenstand. Die Etappen 1 bis 3 und alles außerhalb des Auftragsdiffs habe ich nicht neu geprüft.

**Befunde.**

L-1 — LÜCKE — `tools/beweise.ps1:798-843` (`$gemesseneZiele`), `tools/beweise.ps1:924-928` (`cargo build --release`), `tools/eq-copilot/pruefe_beweisrunner.py:569-575`.
R-309-9 beginnt mit „Jede Binärdatei, die ein Kanon-Bein ausführt oder misst"; die Population der neuen Messung sind aber ausschließlich die sechs CMake-Ziele aus `$gemesseneZiele` — der Zieldateiweg führt über den CMake-Export (`export["ziele"][ziel]["datei"]`), den ein cargo-Artefakt nie erreicht. Die drei Release-Rust-Binaries, die derselbe Runner mit `-Bauen` baut, stehen damit in keinem Zeitvergleich: `broker/target/release/eqcop-broker-v3probe.exe` (A22, `pruefe_ipc_last.py:45`), `broker/target/release/eqcop-broker-sonde012-probe.exe` (A23 `pruefe_sonde012_sources_latency.py:14`, A24 `pruefe_session_soak.py:119`) und `broker/target/release/eqcop-broker.exe` (A17 über den Pin). Selbst nachgemessen: in diesen drei Prüfskripten gibt es keinen einzigen Treffer auf `mtime`/`st_mtime`/„frisch"/„aelter" — ohne `-Bauen` misst A22/A23/A24 ein möglicherweise altes Artefakt, ohne dass irgendein Riegel es meldet. Kein Satz bricht: M-75 fasst seine Vorbedingung ausdrücklich als „Die sechs Ziele aus `$gemesseneZiele`", A36 behauptet wörtlich „jedes Ziel aus $gemesseneZiele, das der Runner baut", und der Gate-Text S25h nennt nur T3-09-01 („Frischeriegel kennt nicht alle DSP-Quellabhängigkeiten"). Regel, Matrixzeile und Behauptung schweigen also zu den cargo-Zielen → LÜCKE, keine Nacharbeit. (Antwort auf Prüffrage (b) unten.)

L-2 — LÜCKE — `tools/beweise.ps1:1142` (`Feldwert $eintrag 'Meldeweg' $false`), nur erreichbar im Zweig `$eintrag.Art -eq 'cargo'` (`:1127-1148`); gesetzt an genau zwei Einträgen (`:517`, `:526`).
R-309-4 spricht allgemein von „Eine fehlende Testvoraussetzung (Junction-Recht, fehlendes Werkzeug)". Der gebaute Meldeweg existiert aber nur für cargo-Beine mit `Meldeweg = $true`; Beine der Art `plugin` und `python` haben keinen Meldeordner, und das Feld würde dort auch stumm ignoriert. M-67 bis M-70 nennen ihrerseits nur A4 und A4-SI, der Gate-Text nur „A4/A4-SI: Skip als NOT RUN" — die Etappe hält also genau ihre Zusage. Ich benenne nur den strukturellen Zuschnitt; der eine bekannte C++-Fall ist laut Auftrag bereits registriert und daher hier kein Befund.

H-1 — HÄRTUNG — `tools/eq-copilot/pruefe_beweisrunner.py:1353` (`r"(?:else)?if \(\$fehlendeVoraussetzung -gt 0\) \{"` in `URTEILSFOLGE`), Wache `:363-388`.
Die Wache erzwingt die Reihenfolge der Urteilszweige und verlangt für den `$rot`-Zweig ausdrücklich ein führendes `if ` (`:378`), lässt für den Voraussetzungszweig aber `if` wie `elseif` zu. Würde `elseif ($fehlendeVoraussetzung -gt 0) {` (`tools/beweise.ps1:1336`) versehentlich zu `if (...) {`, liefe ein Kanon mit einem roten UND einem NOT-RUN-Bein als `UNVOLLSTAENDIG` statt `ROT` aus, während `urteilsvorrang()` grün bliebe. M-70 verlangt wörtlich nur „der Zweig `$rot` steht vor dem Zweig der fehlenden Voraussetzung; `[NOT RUN]` fehlt in der Grün-Zählung" — das misst die Wache vollständig, deshalb HÄRTUNG und kein Defekt.

H-2 — HÄRTUNG — `tools/beweise.ps1:1266-1293`.
Der Klassifikationsaufruf `Fuehre-Aus -Datei 'py' …` läuft ohne die Vorprüfung, die der python-Zweig hat (`:1157-1164`, `Get-Command py`). Fehlt `py` auf dem Rechner — oder ist die PowerShell älter als 7.4, so dass `$start.Environment` in `Fuehre-Aus` (`:340`) die Parameterbindung wirft —, kommt ExitCode −1 zurück und A4/A4-SI werden `[ROT] … NOT-RUN-Meldeweg unlesbar`. Das ist fail-closed und damit strenger als nötig, benennt die Ursache aber als roten Lauf statt als fehlende Voraussetzung. Keine Zusage bricht.

H-3 — HÄRTUNG — `tools/eq-copilot/pruefe_beweisrunner.py:581-583` in Verbindung mit `tools/beweise.ps1:1073-1086`.
Für ein gemessenes Ziel ohne Eintrag im CMake-Export liefert das Werkzeug `"binary": ""`; der Runner fällt für die Anzeigetabelle dann auf die Namensregel `Pruefbinaer $b.name` zurück (`:1076`) — genau die Regel, die M-75 für gemessene Ziele ausschließt („die Zieldatei kommt aus dem CMake-Export, nie aus einer Namensregel"). Folgenlos, weil das Urteil ausschließlich aus Werkzeug-Exitcode und `BAUSTAND-JSON` kommt und der Fallbackpfad in diesem Fall nicht existiert; die Tabellenzeile entfällt dann einfach. Reine Anzeigehärtung.

**(a) NOT RUN (R-309-4, M-67 bis M-70).** Ja, eine fehlende Junction-Voraussetzung kann am Zielstand nicht als GRÜN enden — an der Quelle nachgelesen. `junction_legen` (`store_crash_matrix.rs:3428-3457`) gibt `Result<(), String>` zurück und kennt den deterministischen Schalter (`:3436-3438`); beide vormaligen Skip-Stellen rufen jetzt `nicht_gelaufen(...)` (`:3229-3235` für `store_weist_reparse_punkt_im_pfad_ab`, `:3357-3362` für `volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen`), das ohne Meldeweg `panic!` macht (`:3494-3502`); ein `eprintln!`-Skip ist in `broker/tests/*.rs` nicht mehr auffindbar (selbst gegrept, einziger Treffer ist die Erfolgsmeldung `:3500`). Der Runner legt je cargo-Bein mit `Meldeweg` einen frischen Ordner an und reicht ihn nur über `Start-Process -Environment` weiter (`tools/beweise.ps1:1141-1147`, `:316-318`, `:340`) — die Runnerumgebung bleibt unberührt (kein `$env:NAKAMA…` im Skript), und `NAKAMA_TEST_JUNCTION_VERWEIGERN` setzt der Runner überhaupt nie; es wird ausschließlich in `store_crash_matrix.rs:3436` gelesen. Die Klassifikation macht `pruefe_beweisrunner.py --nicht-gelaufen` (`:156-209`): leer → 0, Marke `<Test>.nicht-gelaufen` mit Zeilen `<Test>: <Grund>` → 3, alles andere (fehlender Ordner, fremde Datei, falscher Testname in der Zeile, leerer Grund, kein UTF-8, CRLF) → 2. Der Runner bildet das fail-closed ab (E4-5, `:1275-1296`): nur `Exit 3 && marken>0` ist NOT RUN, jeder andere Werkzeug-Exit ≠ 0 (auch Exit 3 ohne lesbare Marken) ist ROT. Bilanz und Urteil: `[NOT RUN]` mit Testname und Grund (`:1282-1285`), `$fehlendeVoraussetzung++`, Nachsatz „k Bein(e) NOT RUN" (`:1323`), Urteilszeile `UNVOLLSTAENDIG - n gruen, …` mit Exit 3 (`:1336-1339`); ROT geht vor, sowohl lokal am Bein (`:1278-1280` lässt ein rotes Bein rot und hängt die Marken nur an den Status) als auch im Urteilsblock (`$rot` vor `$fehlendeVoraussetzung` vor `$veraltet`, `:1332-1346`). E4-6 hält: die neue Zählung `$_.Symbol -in @('[OK]','[HINWEIS]')` (`:1314`) ist für jedes andere Bein deckungsgleich mit der alten Exitcode-Zählung, weil `ExitCode -eq 0` auf genau einem Pfad gesetzt wird und dort immer `[OK]` erzeugt (`:1239-1240`), `[HINWEIS]` unverändert zählt und der python-Exit-3-Fall `[FEHLT]` mit ExitCode 3 unter beiden Regeln nicht grün ist (`:1184-1197`); geändert wird ausschließlich das Bein mit Meldeordner. Das Präfix bleibt für `planstand.py` lesbar: die `KANON`-Regex (`tools/plan/planstand.py:102-112`) greift nur bis „gruen" bzw. „Kanon", der Nachsatz hängt dahinter, und die Änderung an `$veraltetText` (`tools/beweise.ps1:1036`) steht im NICHT-BEGLAUBIGT-Text erst hinter „gruen, aber".

**(b) R-309-9 / M-75.** Am Zielstand gehen alle Ziele aus `$gemesseneZiele` mit eigenem Frischebaum in den Zeitvergleich: `$zuBauen` nimmt sie auf (`tools/beweise.ps1:899`), `bau_ziele = @($zuBauen)` geht an die Frischeprüfung (`:1049`), und `baustand()` bildet aus `bau_ziele` minus Beinnamen die Zusatzpopulation mit der Zieldatei aus dem CMake-Export (`pruefe_beweisrunner.py:569-575`). VERALTET verweigert die Beglaubigung (`:771-772`, Exit 4 → Runner `:1065-1068`, `:1340-1343`), „nicht ableitbar" ist nie grün (`:769-770`, Exit 3 → `:1069-1072`, `$fehlendeVoraussetzung`), und ein Ziel ohne Exporteintrag ist ausdrücklich nicht ableitbar (`:581-583`) — die Reihenfolge der Zweige wurde dafür getauscht (`:614-625`), was für Prüfbinaries verhaltensneutral ist, weil `gruende` dort nur für existierende Dateien gefüllt wird (`:584-594`). Der A36-Fall `_fall_m75` fällt an der Zeile, die die Population bildet: (a) VERALTET/Exit 4 bei frischen Prüfbinaries, (b) fehlendes CL-Leselog → NICHT ABLEITBAR/Exit 3, (c) fehlender Exporteintrag → NICHT ABLEITBAR, (d) fehlende Zieldatei → genannt, nicht beurteilt, (e) Quelltextwache `gemessene_im_runner()` über `$gemesseneZiele → $zuBauen → bau_ziele`, mit Gegenprobe ohne die Zuführungszeile (`:1373-1397`). Zu den drei Rust-Binaries: LÜCKE, nicht DEFEKT — begründet am Wortlaut. R-309-9 nennt zwar allgemein „Jede Binärdatei, die ein Kanon-Bein ausführt oder misst", operationalisiert sich aber im selben Satz auf „die Ziele aus `$gemesseneZiele`"; M-75 fasst seine Vorbedingung auf „Die sechs Ziele aus `$gemesseneZiele`" und seine Zusage ausdrücklich auf Ziele, „die der Runner baut und ein Bein ausführt oder misst" im Mechanismus des CMake-Exports, der cargo-Artefakte gar nicht kennt; die A36-Behauptung sagt „jedes Ziel aus $gemesseneZiele". Ein cargo-Binary in `$gemesseneZiele` aufzunehmen wäre technisch unmöglich gewesen, ohne einen zweiten Ableitungsweg (`broker/target/release/<bin>.d`) zu bauen — den keine Zeile verlangt. Siehe L-1.

**(c) Messung ohne Produktänderung (M-57 bis M-66).** Der Block `main-binding-v1` (`StateMigrationTestMain.cpp:1132-1186`) liegt in Abschnitt G7 (`:993-995`, nächster Abschnitt G8 `:1524`) und trennt Leser und Schreiber: M-57 lädt ausschließlich `platte` von der Platte (`:1157-1169`), M-58 vergleicht die Plattenbytes gegen den heutigen Writer und gegen Save-nach-Load (`:1172-1186`); bricht der Writer, bleibt M-57 grün und umgekehrt. Selbst gemessen: das Golden ist 256 Byte, SHA-256 `c77ecec2a3a5fe87a0377128d9cf6b374d3866f7656c4d1d48e59bbde24b2f21` — identisch mit dem neuen `MANIFEST.json`-Eintrag —, trägt `-text` (`git check-attr`, `git ls-files --eol`) und enthält tatsächlich `project_binding_id` mit `0123456789abcdef0123456789abcdef`, dazu `main`, `MainProject`, `Leitstand`. Die erweiterte Waisenprüfung (E4-1, `erzeuge_state_fixtures.py:1142-1143`, `:1153`) bricht keine bestehende Fixture: `git ls-files` zeigt in `eq-copilot/fixtures/state/schema2/` genau neun Dateien, und die Registrierliste (`:1087-1088`) nennt genau dieselben neun. Die zwei Welcome-Fälle stehen im richtigen Abschnitt (H-5): Telemetrie (M-64/M-65) bei `IpcTestMain.cpp:4732-4793` in G7 (`:4681`, G8 ab `:4795`), Control (M-62/M-63) bei `:4991-5057` in G10 (`:4927`, G11 ab `:5060`). Sie messen die erlaubte Grenze wirklich: `kJsonSchemaMinor = 5` (`WireEnvelope.h:81`), beide Welcome-Schleifen prüfen Familie und Vertrag, nie den Minor (`Verbindung.cpp:628-671`, `TelemetryClient.cpp:655-700`), der Control-Eingang schließt erst am ersten Frame mit Minor 6 (`Verbindung.cpp:804-811`, Prefixvergleich im Test endet vor dem Geviertstrich), und `beiFrame` bekommt `e.kopf.schemaMinor` unverändert (`TelemetryClient.cpp:531`), also 3 bei `kFeatureBatchSchemaMinor = 2`. `V3TestServer.h` bleibt für alle vier Nutzer verhaltensgleich: `welcomeMinor { 0 }` reproduziert den vorherigen Literalwert 0, `p2NachWelcomeMinor { -1 }` überspringt den neuen Block. Der Briefkastentest liegt vollständig in `#[cfg(test)] mod tests` (Modul ab `briefkasten.rs:953-954`, Hunk ab `:1805`), wartet die echte Join-Frist ab — `JOIN_FRIST = 2 s` (`:63`) ist unverändert Produktkonstante, und `stoppen_mit` zählt `join_frist_verfehlt` erst nach deren Ablauf (`:876-889`), was der Test als `verfehlt == 1` misst — und kann bei rotem Verlauf nicht hängen: die Schranke öffnet spätestens nach 30 s von selbst, `Oeffner` wird vor `b` deklariert und öffnet sie beim Abwickeln, alle Wartepunkte sind befristet (10 s), und ein zweites `stoppen()` aus `Drop` kehrt sofort zurück, weil `self.thread` bereits genommen ist (`:871-873`). Produktzeilen: im ganzen Änderungssatz BASIS→ZIEL ist außer dem Testmodul von `briefkasten.rs` keine Datei unter `broker/src`, `eq-copilot/plugin/src|dsp|state|vertrag` berührt — selbst mit `git diff --stat` über genau diese Pfade gemessen (Ergebnis: nur `briefkasten.rs` und `store_crash_matrix.rs`). Der geänderte Release-Brokerhash ist am Diff erklärbar: `3b2d182c` (Testmodul `briefkasten.rs`) liegt chronologisch vor `8d5e5bb8`, dem einzigen Commit der Etappe, der `nakama-installer-v1.json` anfasst; die Quelldatei zu ändern zwingt `cargo build --release` zum Neubau des Crates, und der Bauschritt `--broker-pin` schreibt den SHA-256 des neu gelinkten `eqcop-broker.exe` „nur bei Abweichung" ins Manifest (`tools/beweise.ps1:924-935`) — genau die Bauordnung Broker→Manifest→Plugin, die die Gate-GRENZE erlaubt. Dass ein `#[cfg(test)]`-Block keinen Produktcode erzeugt, der Link aber trotzdem andere Bytes liefert, ist die erwartete Nicht-Reproduzierbarkeit eines MSVC-Links; nach `8d5e5bb8` berührt kein Commit der Etappe `broker/` mehr (selbst gemessen). Ein Nicht-Test-Diff unter `broker/src` existiert also wirklich nicht.

**(d) Behauptung ≤ Messung.** A4 und A4-SI ergänzen je einen Konditionalsatz über NOT RUN mit dem konkreten Testnamen; im grünen Lauf misst das Bein den Vordersatz selbst (leerer Meldeordner heißt: der Test lief wirklich), den Nachsatz messen `_fall_m67`/`_fall_m70` in A36 und der einmalige Beleglauf. A4b nennt die M-18-bis-M-28-Regeln des Probewerkzeugs; jede davon hat ihre Prüfung in `PipeClientLifecycleTestMain.cpp` (`:1524` Namenswahl ohne Argument, `:1564` Produktionsname in beiden Schreibweisen/leer/fremd, `:1589` zugelassene v2- und v3-Probenamen, `:1606-1632` Quelltextwache Reihenfolge und Exit 64, `:1636` leerer Name ohne Verbindungsversuch, `:1663` Produkt nennt seine v2-Pipe, `:1716-1774` `konfliktEndeBestaetigt` samt Abbau/Neuaufbau und Quelltextwache). A36 sagt nichts, was `SELBSTTEST_FAELLE` nicht fährt: „jedes **gebaute** gebundene Kernziel" entspricht der Kreuzprobe, die Ziele außerhalb von `bau_ziele` als „nicht im Kanon, nicht beurteilt" führt (`pruefe_beweisrunner.py:683-684`); die R-309-9-Sätze deckt `_fall_m75` (a)–(e), die R-309-4-Sätze `_fall_m67` (leer/Marke/zwei Marken/fremde Marke plus sieben Unlesbar-Varianten) und `_fall_m70` (Reihenfolge, Grünzählung, zwei Gegenproben). B22 misst M-71 und M-72 wirklich: `umgekehrt == f` ist der vom Compiler erzeugte Vergleich über `gesetzt`, `version`, `bandEnergie[32]`, `chroma[12]`, `onset[32]` und `fenster` (`Vertrag.h:344-357`), also exakt „alle 76 Byte und der Fensterzähler"; `f` und `umgekehrt` stammen aus demselben Material `akkord (0.4, 220.0)` über 400 Blöcke, einmal mit `-material(n)`. Die Trennschärfe M-72 vergleicht denselben Bytesatz gegen `sinus` (`:246`, gleicher Block) mit `verschiedeneBytes >= 1`. Das alte `pruefe (true, …)` ist ersatzlos entfallen; in `Sonde013FingerprintGoldenTest.cpp` existiert kein `pruefe (true` mehr (selbst gegrept). Die Behauptungszeile sagt jetzt „der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich" statt der früheren, unmessbaren Phasenaussage — und behauptet die zusätzlich gemessenen Ränder (Stille mit −0,0, begrenzte Vollaussteuerung) gar nicht erst.

**Produktverhalten seit BASIS (Identität, Passthrough/Nulltest, State, Audio-Thread).** Selbst gemessen: `git diff --stat 8d573e9f...49dab04d` über `broker/`, `eq-copilot/plugin/src`, `dsp`, `state`, `vertrag` berührt ausschließlich das Testmodul von `briefkasten.rs` und `broker/tests/store_crash_matrix.rs`; `eq-copilot/identity/**` und `eq-copilot/schemas/**` sind im gesamten Änderungssatz nicht enthalten. Die einzigen Nicht-Testartefakte sind das neue eingefrorene Fixture (additiv, `-text`, Hash und Länge im MANIFEST bestätigt) und der Broker-Pin im Installer-Manifest (Bauordnung, gate-gedeckt). Kein Audio-Thread-Pfad, keine persistente Schemaänderung, keine Class-ID. Nur gelesen, nicht nachgemessen: dass A1, A14, A16 und B1 im Kanon nachher grün standen.

**HEAD vorher:** `4e53b1137c9c131b710fe3794f4f46a5cf1f8edf`
**HEAD nachher:** `4e53b1137c9c131b710fe3794f4f46a5cf1f8edf` (`git status --short` unverändert: nur die zwei fremden, unversionierten Ordner `briefing-hub/` und `nimbalyst-local/`; im Prüfbereich nichts angelegt, geändert oder gelöscht)
