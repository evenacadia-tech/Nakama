NAK-246 Etappe 4 — Nacharbeit 1 (Runde 1 von 3): drei Testsynchronisations-Defekte aus der Erstprüfung. Auftrag des Dirigenten (2026-09-11).

ANLASS. Die Erstprüfung der Etappe 4 (Codex gpt-6-astra, Effort max, lesend, Thread 01a090ca-cec4-7171-90b2-78d77616ac2f, Urteil docs/beweise/roh/NAK-246-etappe-4-erstpruefung-201f2eb.txt) endete NEEDS_WORK mit drei Befunden, alle in Tests; ein lesender Validierungsagent hat jeden am HEAD an der Quelle bestätigt (Manifest §10.10). Dieser Auftrag enthält ausschließlich diese drei Defekte — nichts sonst. Was hier nicht steht, wird nicht geändert; Produktcode wird nicht angefasst.

MODELL DES BAUERS: claude-opus (Effort max), --permission-mode dontAsk (Skill §3.2). DU BIST der frische Worker im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = git rev-parse HEAD beim Start; git status --short muss leer sein, sonst nichts anfassen und als letzte Zeile „NAK-246 HALT <grund>" melden. Aufsicht ENG. Keine Rückfrage an den User; technische Entscheidungen triffst du selbst an der Quelle und begründest sie im Manifest. Lies zuerst: das Urteil, Manifest §3.3 (M-12), §3.4 (M-17), §5.3 Feinheit 3, §10.3, §10.10, tools/dirigent/pruefliste.md; die Quellen eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp (:365-393 Schranke, :936-1014 M-12, :1280-1330 Altfall), eq-copilot/plugin/src/prozessor/Ipc.cpp:1396-1432 (Drain, Haken, Freigabe, Dirty), eq-copilot/plugin/src/prozessor/Analyse.cpp:345-346 (Worker-Drain), eq-copilot/plugin/tests/IpcTestMain.cpp:3045-3130 (M-17) und eq-copilot/plugin/tests/V3TestServer.h:600-670 (Protokollierung vor commandAckArt).

GRUNDSATZ: Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt — und er fällt deterministisch: durch erzwungene Ordnung, nie durch ein Schlafintervall.

DEFEKTE (Titel wörtlich aus dem Urteil) und REGELN DES DIRIGENTEN:

D-E4-1 „[P2] Erzwinge das M-12-Interleaving im Mutationslauf" — Sonde012ProjectReloadTest.cpp:981-984: A wird nach 300 ms freigegeben, unabhängig davon, ob B überhaupt sourcesTick() betreten hat; `!bFertig` beweist keinen blockierten Drain; im Rotbau ohne sourcesDrainMutex kann B erst nach der Freigabe laufen, der Unbind kommt zuletzt, alle Zeilen werden grün.
REGEL R-E4-1: Der M-12-Test erzwingt die Ordnung „A hält am Haken → B hat sourcesTick() betreten und steht am Riegel → A wird freigegeben" über Ereignisse, nicht über Zeit: B meldet nachweisbar, dass es den Drain betreten hat und vor dem Riegel steht (ein zweiter Testhaken oder Zähler im Drain-Eintritt VOR dem lock, z. B. im Testblock des Prozessors, oder eine Schranke für B), und A wird erst nach diesem Signal freigegeben; `bWartete` misst dann „B stand am Riegel, während A hielt" statt „B war noch nicht fertig". Im Rotbau (sourcesDrainMutex entfernt) ist dieselbe Ordnung erzwungen: B betritt den Drain, läuft — weil kein Riegel klammert — bis nach seiner Anwendung durch, BEVOR A freigegeben wird; damit kippt die ACK-Reihenfolge deterministisch (Unbind vor Join → Mitglied bleibt), und die Zusagezeilen fallen aus genau diesem Grund. Ein Haken im Produktpfad ist im Produktbau inert (Zeiger nullptr), wie sourcesDrainHakenFuerTest. Rotbeweis neu fahren und docs/beweise/roh/NAK-246-rot-M-12.txt ersetzen (Rotbau-Diff in der Datei); Grünlauf B14 --nur m12 und im Normallauf.

D-E4-2 „[P2] Synchronisiere den Dirty-Abschluss im bestehenden B14-Fall" — Sonde012ProjectReloadTest.cpp:1287-1289 legt den Prozessor ohne setzeWorkerDrainFuerTest (false) an; der Worker drainiert alle 50 ms (Analyse.cpp:345-346) und meldet Dirty nach der Riegelfreigabe (Ipc.cpp:1429-1431); die synchrone Prüfung dirtyVor.nonParam == 2 (:1312-1324) hat kein warteAuf → 1 statt 2 möglich, obwohl das Produkt korrekt nachmeldet.
REGEL R-E4-2: Der Altfall confirmed_join_ack_and_name_each_mark_host_dirty misst dieselbe Zusage wie vor der Etappe, aber ohne Rennen mit dem Worker-Drain: entweder wird der Worker-Drain in diesem Fall abgeschaltet (setzeWorkerDrainFuerTest (false), wie in den D3-Fällen), sodass der Test-Tick der einzige Drain bleibt, oder die Prüfung wartet auf den vollständigen Nachlauf (warteAuf auf dirtyVor.nonParam == 2 und die Mitgliedschaft) — du wählst an der Quelle; der Fall bleibt sonst unverändert (keine geänderte Zusage). Nachweis: der Fall ist im Normallauf von B14 grün; ein Rotbeweis ist hier nicht verlangt (Regressionswache), aber im Manifest wird benannt, welches Fenster geschlossen ist.

D-E4-3 „[P2] Beende die ursprüngliche Verbindung vor der ACK-Freigabe" — IpcTestMain.cpp:3111-3112: serverHat() beweist nur p0Texte.push_back (V3TestServer.h:622), der Server liest commandAckArt erst danach (:663); ein verdrängter Serverfaden kann das ACK des 64. nach commandAckArt.store (1) noch über die Altverbindung senden → alle 64 erfolgreich, nur 63 nachgespielt, :3118 fällt bei korrektem Client.
REGEL R-E4-3: Vor commandAckArt.store (1) ist nachweislich kein ACK der Altverbindung mehr möglich: entweder ist die Altverbindung vollständig beendet (Client getrennt und Serverseite hat die Trennung verarbeitet, belegt über einen Serverzähler oder -zustand), oder der Test wartet auf ein Serversignal, dass der 64. Auftrag über das Lesen von commandAckArt hinaus verarbeitet ist (ein Zähler im Testserver, der NACH dem Lesen von commandAckArt erhöht wird — Testinfrastruktur in V3TestServer.h, begründet). Erst danach der Reconnect und die Replay-Phase. Die Zusage von M-17 („jeder der 64 wird später nachgespielt und bestätigt") bleibt unverändert; Rotbeweis M-17 neu fahren (Rotbau ohne Deckel wie in rot-M-17.txt) und die Rohdatei ersetzen; Grünlauf B10 --nak246-d4 und Normallauf.

WAS DU ÄNDERST: eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp (D-E4-1, D-E4-2), eq-copilot/plugin/tests/IpcTestMain.cpp (D-E4-3), eq-copilot/plugin/tests/V3TestServer.h nur, falls R-E4-3 einen Serverzähler braucht (begründet in 10.11), eq-copilot/plugin/src/PluginProcessor.h und eq-copilot/plugin/src/prozessor/Ipc.cpp NUR für einen zusätzlichen, im Produktbau inerten Testhaken oder -zähler nach R-E4-1 (kein Produktverhalten), docs/beweise/roh/NAK-246-rot-M-12.txt und -M-17.txt (ersetzt), docs/beweise/roh/NAK-246-etappe4-beine.txt (Abschnitt „Nacharbeit 1": B14, B10 einzeln aus pwsh, Exit, Zeitstempel; dazu A1 und A16 als Nulltest-Wache, falls PluginProcessor.h/Ipc.cpp berührt wurden), docs/beweise/NAK-246.md: neuer Abschnitt §10.11 „Nacharbeit 1" (je Defekt die geänderte Stelle Datei:Zeile, die Regel, ein Satz, warum die Ordnung jetzt erzwungen ist; Messabdeckung für M-12 und M-17 nachgezogen; Prüfliste abgehakt; Selbstaudit). Nichts sonst; kein Umschreiben von §10.0 bis §10.10, keine Kopfzeile (macht der Dirigent), keine neuen Regeln, keine neuen Befunde, keine Härtungen.

BEINE: B14 EqCopSonde012ProjectReloadTest und B10 EqCopIpcTest einzeln aus pwsh (nie aus Bash — cmake liegt dort nicht im PATH); Landminen NAK-230 (Zeitstempel der Quelle vor dem Bau auf jetzt setzen, Binary-Zeitstempel prüfen) und NAK-175 (Heap). Der M-12-Grünlauf wird dreimal hintereinander gefahren, der M-17-Grünlauf ebenso (Nachweis gegen Flake; Rohausgabe der drei Läufe).

GIT. Nie git add -A, nie --amend, fremde uncommittete Dateien nie anfassen; git status --short zuerst lesen. Commits nur mit explizitem Pathspec; Commitnachrichten über git commit -F <datei>, nie -m mit Backticks; Push nach git pull --rebase bei sauberem Worktree als eigener Schritt. Vor dem Commit: py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-246.md ohne Befund (Pipe-Zeichen in Tabellenzellen als \|; Zeilenbereiche mit ASCII-Bindestrich). Kleine Werkzeugaufrufe (je Edit rund 250 Zeilen). Selbstaudit vor dem Commit (kein Schlafintervall als Ordnungsbeweis; Testhaken im Produkt inert; keine geänderte Zusage).

ENDE: Letzte Zeile deines Turns „NAK-246 ETAPPE 4 NACHARBEIT 1 ABGESCHLOSSEN <voller sha>". Keine Rückfrage an den User.
