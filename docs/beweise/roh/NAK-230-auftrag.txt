NAK-230 — Defekt · Vertrag · C++-Leser (vor S26–28): `fehlercodeHaeltVertrag` kennt nur die zwölf Fehlercodes der Fassung 1; ein `command_ack` mit einem Fassung-2-Code fällt beim Leser, obwohl der Vertrag „unbekannter Code = generischer Fehler, nie Erfolg“ verlangt. Ticketauftrag des Dirigenten (2026-09-09; Registerzeile NAK-230, Quelle NB-3 in docs/beweise/NAK-214.md §4).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-230.md ein (Manifest neu anlegen; Muster Kopftabelle docs/beweise/NAK-224.md: Ticket, Phase, Prüfstufe, Basis-SHA, Kanon vorher, Kanon nachher, Ticketpfade, Nicht angefasst, Änderungssatz, Prüfmodell — darunter §1 Registerzeile und Vertragstext wörtlich, §2 Verhaltensmatrix, §3 Riegelkarte, §4 Prüfliste, §5 Nebenbefunde, §6 Verlauf; ein lebender Kopf, darunter append-only). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie Backticks in `-m`. Keine Neben-Refactors, keine Legacy-Umbenennung (NAK-30), nichts außer diesem Defekt. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §4 nennen, wo jede Zeile gemessen ist. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder dem rekursiven Unix-Löschbefehl; selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. `cmake` liegt NICHT im Bash-PATH: C++-Bauten nur aus pwsh, Baubefehl und Zielnamen aus tools/beweise.ps1 ableiten, Zeitstempel des Binaries nach dem Bau prüfen. Beende deinen Turn nicht, solange ein von dir gestarteter Lauf läuft; die letzte Logzeile `EXIT=` ist das Ende. Je Zeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

REGISTERZEILE, wörtlich (docs/offene-punkte.md NAK-230): „`fehlercodeHaeltVertrag` kennt nur die zwölf Fehlercodes der Fassung 1. `ControlClient.cpp`:74–81 führt `protocol_mismatch` bis `internal`; die sechs Codes der Fassung 2 (`abdeckung_zu_gering`, `schon_terminal`, `ohne_lautheitsabgleich`, `ohne_resultatmessung`, `blindreihenfolge_widerspruch`, `reihenfolge_nicht_gebunden`) fehlen, und ein `command_ack` mit einem davon fällt beim Leser (`:228`), obwohl der Broker ihn seit SONDE-013 sendet und der Schemakommentar „ein unbekannter Code wird als generischer Fehler behandelt, nie als Erfolg“ verlangt — nicht „die Nachricht wird verworfen“. Der Leser ist strenger als der Vertrag. Außerhalb der G5-Grenze (Experimentfamilie); nicht mit E7 (`unknown_target`, Fassung 1) verwechseln.“ Seit NAK-225 (09.09.2026) liegt die Funktion in `eq-copilot/plugin/core/ipc/controlclient/Intern.h`:92–, der Leser prüft sie in `Intern.h`:247. Vertragstext: `eq-copilot/schemas/v3/eq-ipc-v3.schema.json`:815 (description von `code`) und die Enum-Liste darunter (:816–834); Broker-Seite `broker/src/coordinator/schema.rs`:144 und `vergleichbarkeit.rs`:89.

REGEL (Dirigent, Technik, an der Quelle entschieden): Der Leser ist genau so streng wie der Vertrag. (1) Ein `code`, der keine Zeichenkette ist, bleibt ein Strukturfehler der Nachricht (wie heute). (2) Ein Zeichenketten-`code` macht die Nachricht NIE ungültig: die achtzehn Codes aus dem Schema (zwölf der Fassung 1 plus die sechs der Fassung 2) sind bekannt und werden wie bisher verarbeitet; jeder andere Zeichenketten-Code wird als generischer Fehler geführt — der Text bleibt erhalten (für Anzeige und Log), das Ergebnis ist nie `ok`. (3) `fehlercodeHaeltVertrag` verliert damit die Rolle des Gültigkeitsriegels; wenn sie bleibt, heißt ihre Antwort „bekannt“, nicht „gültig“, und ihr Kommentar sagt das. (4) Die Liste der bekannten Codes wird aus GENAU EINER Quelle gepflegt — wenn der Bestand eine generierte oder geteilte Codeliste kennt (B3c-Schemaprüfung, `WireZahl.h`, Vertragsköpfe), nutze sie; sonst steht die Liste einmal in `Intern.h` mit Verweis auf die Schemazeilen, und ein Test hält sie gegen die Enum-Liste des Schemas (Riegel: jede Enum-Zeichenkette des Schemas ist dem Leser bekannt — die nächste Vertragsfassung fällt dann im Test statt im Feld). Kein Verhalten für Fassung-1-Codes ändert sich (E7 `unknown_target` bleibt, wie es ist).

VERHALTENSMATRIX (§2 des Manifests, VOR dem Fix schreiben; Abweichung vom Skill §3.2 mit Begründung „sechs Zeilen, Matrix und Bau in einem Lauf, die Erstprüfung prüft beides“ im Manifest festhalten): Zeilen = `code` fehlt · `code` keine Zeichenkette · Fassung-1-Code · Fassung-2-Code · unbekannte Zeichenkette · leere Zeichenkette; Spalten = Nachricht gültig? · Ergebnis (ok/fehler/generischer fehler) · Text erhalten? · Test, der die Zeile misst · Rotbeweis. Jede Zeile hat genau einen Test in `eq-copilot/plugin/tests/IpcTestMain.cpp` (Bein B10 `EqCopIpcTest`), der am Stand vor dem Fix rot ist, wo die Zeile heute bricht (Fassung-2-Code, unbekannte Zeichenkette), und grün bleibt, wo sie heute schon hält (Fassung-1, kein String, fehlt). Die Rotbeweise stehen als Rohausgabe in docs/beweise/roh/NAK-230-rot.txt (Test gegen das alte Binary rot, gegen das neue grün, mit Zeitstempeln der Binaries).

TICKETPFADE (nichts sonst): `eq-copilot/plugin/core/ipc/controlclient/Intern.h` (und, falls der Leser selbst angepasst wird, die betroffene Übersetzungseinheit unter `core/ipc/controlclient/`), `eq-copilot/plugin/tests/IpcTestMain.cpp`, `docs/beweise/NAK-230.md`, `docs/beweise/roh/NAK-230-*`. NICHT anfassen: Schemas, Fixtures, `broker/`, `eq-copilot/identity/`, `PluginProcessor*`, `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`. Braucht der Bestand zwingend eine weitere Zeile (etwa eine geteilte Codeliste), ist das eine begründete Pfadänderung im Manifest.

RIEGELKARTE (§3): R1 Kanon vorher GRÜN 62/62 auf `f4d497b3` (docs/beweise/roh/NAK-225-f4d497b.md); `git diff --stat f4d497b3..<Basis> -- eq-copilot broker tools` ist leer — das messen und als „Kanon vorher“ belegen. Nachher: voller abgekoppelter Kanon auf dem Endstand, GRÜN 62/62 (gleiche Beinzahl), B10 mit der neuen Prüfungszahl (vorher 381). R2 Matrix: sechs Zeilen, sechs Tests, zwei Rotbeweise. R3 Schema-Riegel: der Test, der die bekannte Liste gegen die Enum-Liste des Schemas hält, liest das Schema aus dem Repo (Pfad relativ zur Wurzel wie in IpcTestMain.cpp üblich). R4 Diff-Riegel: nur Ticketpfade. R5 Warnungen: `EqCopIpcTest` und `EqCopilot_VST3` bauen ohne neue `warning C`.

ABLAUF: (a) Iststand und Matrix ins Manifest, committen und pushen. (b) Tests schreiben, gegen das alte Binary laufen lassen (Rotbeweis festhalten), Fix, neu bauen, grün; ein Commit für Fix plus Tests (sie gehören zusammen). (c) Voller Kanon abgekoppelt:
  $log = Join-Path $env:TEMP 'nakama-nak230-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-230.md -Anhaengen -Titel NAK-230 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl
(einfache Anführungszeichen um EXIT= — CLAUDE.md-Landmine). Warten, bis die letzte Logzeile mit `EXIT=` beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); ein einmaliges „Voraussetzung fehlt“ an zwei flatc-Beinen nach frischem Bau einmal wiederholen. Ergebnis ins Manifest; Urteilsmarke `<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->` erst nach grünem Kanon; T2 setzt der Dirigent nach der Codex-Prüfung. Rundenbilanz `py -3.13 tools/dirigent/rundenbilanz.py <Basis>..HEAD` ins Manifest.

SELBSTAUDIT vor jedem Commit: keine Fassung-1-Zeile ändert ihr Verhalten; ein unbekannter Code ist nie `ok`; der Text des Codes wird nirgends abgeschnitten oder umgeschrieben; kein Test prüft nur einen Nebeneffekt; die Matrix nennt je Zeile den Test mit Namen; keine Behauptung im Manifest ohne Lauf aus dieser Session.

ABSCHLUSS: Manifest vollständig, alles committet und gepusht, Worktree sauber. Melde dich erst, wenn `git status --short` leer ist und `git rev-parse HEAD` gleich `git rev-parse origin/master` ist.
