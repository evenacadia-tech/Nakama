URTEIL: PASS — der Befund ist geschlossen, nichts gebrochen.

Geprüft: ausschließlich der gebundene Fixdiff; `tools/plan/gesundheit.py:577–581` erfüllt R-D1b; die originale Prüfer-Reproduktion liefert am ZIEL unter Python 3.14.2 exakt `[('t.rs', 1, 'union')]`; die Selbsttestfälle `:1113–1151` bestätigen `struct union where→union`, `fn default→default`, `default fn→helfer`, `union Foo→Foo`, `static mut→ZAEHLER`, `const fn→name`, `const NAME→NAME`, `pub const extern "C" fn→helfer`, sämtliche Vorrundenformen sowie separat `pub(in crate::a)`, `static mut NAME` und `extern "C" fn`, jeweils ohne/mit Verwendung; 104/104 rein lesende Selbsttestfälle bestanden, Mutation C im Speicher 100/104 und Exit 2, Rücknahme 104/104 und Exit 0, entsprechend dem gelesenen Rotbeweis `docs/beweise/roh/NAK-223-rot-deadcode.txt:168–188`; übrige Messlogik, Clippy-Pfad, Tabellenform und sämtliche Schwellen sind per AST-Vergleich unverändert, virtuelle Exit-0/2/4-Proben korrekt und wiederholte Berichte bytegleich; HEAD vor/nach `a75bcee7`, Status unverändert mit fremder unversionierter `.claude/settings.local.json`, nichts geschrieben.

Nicht geprüft: Python 3.13 und dessen vollständiger Selbsttest wegen Windows-Anmeldesitzungsfehler; beim Ersatzlauf unter 3.14 wurden zwölf temporär schreibende Selbsttestfälle ausgelassen; reale Repo-Gesamtläufe, Cargo/Clippy, CMake und Kanon wurden nicht ausgeführt.