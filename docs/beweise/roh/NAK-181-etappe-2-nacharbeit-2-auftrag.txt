NAK-181 — Etappe 2, Nacharbeit 2 (Runde 2 von 3). Auftrag des Dirigenten (2026-09-06).

DU BIST ein frischer Opus-Bauer (max) im sichtbaren Checkout C:/Users/phili/Projekte/Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach `git pull --rebase` bei sauberem Worktree. Keine Neben-Refactors, keine Legacy-Umbenennung, keine Schemaänderung, kein Produktcode außer neuen Testzugängen (WN-02). Landminen: MSVC-Stack 1 MiB (Engines auf den Heap); kein Lösch-Cmdlet im eigenen Befehlstext (Logs mit `*>` überschreiben, notfalls `[System.IO.File]::Delete` auf exakte, selbst angelegte Pfade); Pipe-Zeichen in Tabellenzellen reißen den Dokuriegel.

QUELLEN, die du zuerst liest:
1. docs/beweise/NAK-181.md — Kopftabelle, §2.2 Zeilen N-05, N-17, N-35 (wörtlich), §7 (Erstprüfung 1, Regeln NR-05, NR-08, NR-09), §7.1 (was Nacharbeit 1 eingearbeitet hat), **§7.2 (Wiederprüfung 1: die drei offenen Befunde WP1-1 bis WP1-3 mit je der Regel WN-01 bis WN-03 — das ist dein Auftrag)**.
2. docs/beweise/roh/NAK-181-wiederpruefung-1-ed09b1f.txt — die Befunde wörtlich (Datei:Zeile, Begründung).
3. tools/dirigent/pruefliste.md — vor dem Commit abhaken, im Manifest je Zeile den Messort nennen.

AUFTRAG: Schließe genau die drei Befunde aus §7.2 mit den Regeln WN-01 bis WN-03. Nicht weniger, nicht mehr (keine zusätzlichen Umbauten, keine neuen Zusagen ohne Test). Je Regel ein Rotbeweis als Rohdatei docs/beweise/roh/NAK-181-rot-n2-<kennung>.txt (Kopf: Rücknahme, Bein, rote Zeilen, Bilanz — Muster docs/beweise/roh/NAK-181-rot-n1-nr09.txt); Rotbeweis nr09 wird nach WN-01 wiederholt.
- WN-01 (SchemaTestMain.cpp, N-17): den erfolgreichen Locale-Namen vor dem Rückwechsel als `std::string` sichern; jedes erneute `setlocale` auf `!= nullptr` prüfen (eigene `pruefe`-Zeile), bevor Heartbeat und Hello unter Komma gemessen werden. Kein Zugriff mehr auf einen von `setlocale` zurückgegebenen Zeiger nach einem weiteren `setlocale`.
- WN-02 (Sonde013EventWireTest.cpp, N-35 `grenze_leert_die_fenster_und_laesst_den_ereignisring`): nach der Grenze jeden zuvor befüllten Fensterträger einzeln prüfen — Rahmen (Haupt, Bass), Loudness (Zelle, Kurz-Loudness), Spektrum (Bandakku), Fingerprint, Stereo — jeweils auf höchstens den Block nach der Grenze, ohne `||` zwischen zwei Trägern; fehlende Zählerzugänge in FeatureEngine.h als Testzugang anlegen (lesend, kein Verhalten). Die Vorbedingung befüllt alle fünf Träger nachweislich (je Träger eine Zeile, nicht eine Disjunktion). Rotbeweis: `grenzeZiehen` lässt genau einen Träger (z. B. Kurz-Loudness) stehen → genau diese Zeile rot, die anderen grün.
- WN-03 (Sonde013PassageStateTest.cpp, N-05): vor `fahreBisPegel` `pruefe (! p->beginneVersuch (a), …)` und danach `letzterVersuchP0FuerTest()` unverändert (leer); erst dann die Gegenprobe. Rotbeweis: benenne die Rücknahme, die diese Zeile fällt; kann keine einzelne Rücknahme sie fällen, steht das im Manifest als mehrfach gesicherte Wache — nicht behaupten.

REIHENFOLGE (ein Commit mit Pathspec, betroffene Beine grün vor dem Commit):
1. WN-01, WN-02, WN-03 in den drei Tests (plus Testzugänge WN-02).
2. Betroffene Beine einzeln fahren (kein voller Kanon in dieser Runde): B3c EqCopSchemaTest, B16 EqCopSonde013EventWireTest, B23 EqCopSonde013PassageStateTest; Bauziele und Aufrufe stehen in tools/beweise.ps1. Rohausgabe der grünen Läufe als docs/beweise/roh/NAK-181-nacharbeit-2-beine-<sha>.txt.
3. Manifest: neuer Abschnitt §7.3 „Nacharbeit 2 — was eingearbeitet wurde" (Tabelle WP1-xx → Stelle → Test → Rotbeweis-Datei → Zeile im Rotbeweis); §2.2 N-35 als datierter Nachtrag nur, wenn neue Zählerzugänge den Wortlaut präzisieren; §6.7 Prüfliste nachziehen; Kopfzeile Phase voranstellen: „Etappe 2: Nacharbeit 2 abgeschlossen (Basis <basis-sha>, Ende <end-sha>), betroffene Beine grün, Wiederprüfung 2 offen" — die Urteilsmarke `NAKAMA-URTEIL` bleibt unverändert (die setzt der Dirigent). Dokuriegel: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-181.md` vor dem Commit.
4. Commit (Tests, Testzugänge, Manifest, Rohdateien) mit Pathspec, `git pull --rebase`, Push. Kein Anfassen von docs/beweise/G4.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/, docs/NEXT-SESSION.md. Turn mit der letzten Zeile „ETAPPE 2 NACHARBEIT 2 ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.

SELBSTAUDIT vor dem Commit (adversarial): Fällt jeder Rotbeweis an genau der Zeile, die die Zusage trägt? Liest der N-17-Test noch irgendwo einen `setlocale`-Zeiger nach einem weiteren `setlocale`? Steht in N-35 noch ein `||` zwischen zwei Fensterträgern? Nulltest unberührt (kein Produktcode außer lesenden Testzugängen), kein Schemafeld. Keine Prüfzeile trägt einen Zusagenamen, den sie nicht misst.
