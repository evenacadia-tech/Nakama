# NAK-134 — PIPE_BUSY ist Liveness, kein Sicherheitsurteil

| Feld | Wert |
|---|---|
| Ticket | NAK-134 — Nacharbeit zum Phasengate G3 |
| Basis-SHA Phase 1 | `4789f2a8a3cace3d71809b6f9326b6d4aaf7389a` |
| Basis-SHA Phase 1b | `8e6a2acee873864d017c6bdfa504808bface4a1e` |
| Phase | **Phase 1b — Matrix-Nacharbeit nach der Codex-Matrixpruefung 1.** Diese Fassung traegt Kopf, Defekt, Regeln R1–R5 und die ueberarbeiteten Verhaltensmatrizen. Es ist **kein Produktcode und kein Werkzeugcode geaendert**. |
| Urteil | `NOT RUN` — offen fuer den Dirigenten. Der Wert gilt fuer die Spezifikationsphase und bleibt hier im Verlauf stehen, wenn Phase 2 ihn ersetzt. |
| Kanon | `NOT RUN` (Phase 2) |
| Aenderungssatz Phase 1 | nur `docs/beweise/NAK-134.md` |
| Aenderungssatz Phase 1b | `docs/beweise/NAK-134.md`, `docs/beweise/roh/NAK-134-matrixpruefung-1-8e6a2ac.txt` |
| Pruefung Phase 1 | Codex `gpt-5.6-sol` (high), lesend, Thread `01a06478-49e9-76b2-baa9-a3e8e4c202de` — **URTEIL NEEDS_WORK**, vollstaendiges Urteil in `docs/beweise/roh/NAK-134-matrixpruefung-1-8e6a2ac.txt` |

## 1. Gate-Text — woertlich

`docs/FL-Nakama-Sonden-Design-Entwurf.md:4262-4264` (P3-Exit-Gate):

> **Exit-Gate:** Save/Reload, Duplicate, Bridge/PID-Wechsel, zwei offene Projekte, Brokerneustart und
> 60-Minuten-Soak erhalten richtige Mitgliedschaft ohne falsche Steueradresse. Sichtbarer Zustand
> erreicht das Budget aus Abschnitt 49. `R1 · Passive Alpha` ist erreicht.

`docs/FL-Nakama-Sonden-Design-Entwurf.md:3586` (Abschnitt 49.3, Budgettabelle):

> | Soak | mindestens 60 min mit 16, mindestens 30 min mit 32 Sonden ohne XRun/ungegrenztes Wachstum |

Gebrochene Matrixzeilen des Werkzeugtickets: `docs/beweise/G3-SOAK.md` §5, Zeilen **S03**
(„Die Mitgliedschaft driftet ueber die ganze Laufzeit nicht") und **S11** („Innerhalb der Frist aus
§3.1 (**60 s ab `BEREIT`**) je Clientpaar: der Main subscribt neu, alle N Sonden reporten neu, und
der Snapshot ist wieder vollstaendig"). Zusaetzlich faellt **S13** („Ausserhalb der Neustartfenster
wird **kein** Mitglied stale (Grenze > 2.500 ms) und keines evicted (10.000 ms)").

## 2. Defekt — woertlich aus der Ursachenanalyse des Gate-Laufs

> Nach einem Brokerneustart mit 32 Sonden (66 Verbindungen auf zwei scharfe Pipe-Instanzen,
> `broker/src/transport/server_v3.rs:1684-1700`) verlieren einige Clients 20-mal das Rennen um
> `WaitNamedPipeW(200 ms)` (`eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:67-89`) und enden mit
> `ERROR_PIPE_BUSY`. `IpcVerbindung.cpp:90-99` klassifiziert JEDEN Fehler ausser
> `ERROR_FILE_NOT_FOUND` als `ServerPruefStatus::belegtAberUnverifiziert` — laut
> `IpcVerbindung.h:40-42` „ein Sicherheitsfehler und kein Livenessfehler".
> `ControlClient.cpp:994-1012` parkt den Verbindungsthread daraufhin dauerhaft auf einer
> Condition-Variable („kein wiederholtes Anklopfen"), nur `reconnect()`/`stop()` loest ihn;
> `TelemetryClient.cpp:407-424` hat dieselbe Sperre. Folge im Produkt: nach einem Brokerneustart in
> einer Session mit vielen Sonden bleibt ein Teil der Sonden endgueltig getrennt (kein Backoff, kein
> Versuch, kein stale, kein Zaehler), bis die Instanz neu geladen wird.

### 2.1 An der Quelle nachgeprueft (Stand `4789f2a`, in Phase 1b auf `8e6a2ac` erneut gelesen)

| Behauptung | Quelle | Befund |
|---|---|---|
| 20 Runden `WaitNamedPipeW(200)`, danach Abbruch mit dem letzten Fehler | `IpcVerbindung.cpp:67-89` | bestaetigt; `letzterFehler` traegt `ERROR_PIPE_BUSY` |
| jeder Fehler ausser `FILE_NOT_FOUND` wird `belegtAberUnverifiziert`/`pipeOeffnen` | `IpcVerbindung.cpp:90-99` | bestaetigt |
| Control parkt unbefristet auf der Condition-Variable | `ControlClient.cpp:994-1012` | bestaetigt; nur `laeuft == false` oder ein Generationswechsel loest |
| Telemetrie hat dieselbe Sperre | `TelemetryClient.cpp:407-421` | bestaetigt |
| `belegtAberUnverifiziert` zaehlt als Serverpruefung | `ControlClient.cpp:1101-1103`, `TelemetryClient.cpp:554-556` | bestaetigt — und zwar **statusgesteuert**: die Bedingung ist die Positivliste `verifiziert` oder `belegtAberUnverifiziert` |
| der v2-`PipeClient` klassifiziert identisch | `PipeClient.cpp:391-405` | bestaetigt; derselbe Defekt, derselbe Parkpfad (`PipeClient.cpp:305-312`) |
| der v2-`PipeClient` zaehlt `serverPruefungen` **positionsgesteuert** | `PipeClient.cpp:418` | Phase 1b, neu: das `++` steht unbedingt hinter einem erfolgreichen `CreateFileW` — ohne Statusbedingung. Gleicher Zaehlername, andere Semantik als v3 |
| `BrokerLifecycle` haelt bei `belegtAberUnverifiziert` an | `BrokerLifecycle.cpp:734-747` | bestaetigt: Phase `blockiertUnverifiziert`, kein Reconnect, kein Spawn — solange der Status steht, und er steht, weil der Clientthread parkt |
| die Anzeige nennt den Zustand `Server not verified` | `SourcesModel.cpp:410-412,1070` | bestaetigt |
| `belegtAberUnverifiziert` ist **nicht** persistiert und **kein** Wire-Feld | `git grep` ueber `schemas/`, `state/`, `*.json`, `*.py`, `*.ps1` | bestaetigt: reiner Laufzeit-Snapshot; kein Schema-, State- oder Migrationsvertrag beruehrt |

### 2.2 Messung aus dem Gate-Lauf G3 (A24, 32 Sonden, 30 min, 3 Neustarts)

Rohbericht `docs/beweise/roh/G3-soak-n32-30min-ad5b2d1.json`, Laufprotokoll
`docs/beweise/roh/G3-soak-n32-30min-ad5b2d1.txt`.

| Groesse | Wert | Lesart |
|---|---|---|
| `mitgliedschaft.vollstaendig` / `snapshot_pruefungen` | 22.925 / 82.572 | S03 ROT — 72 % aller Snapshots zeigten nicht alle 32 Sonden |
| `mitgliedschaft.abtast_vollstaendig` / `abtast_pruefungen` | 4.482 / 17.922 | dieselbe Drift in der 250-ms-Abtastung |
| `mitgliedschaft.ungueltig`, `fremde_adresse`, `fuehrendes_main_falsch` | 0 / 0 / 0 | die Sicht ist nicht falsch, sie ist **unvollstaendig** |
| `neustart[1..3].bereit_bis_vollstaendig_ms` | −1, −1, −1 | S11 ROT bei **allen drei** Neustarts |
| `neustart[1..3].reconnect_ms.max` | 60.001,0 ms je Neustart | Frist Z2 (60 s) gerissen; Fuellwert „Frist + 1" fuer nicht zurueckgekehrte Paare |
| `neustart[1..3].reconnect_ms.min` | 392,6 / 2.531,3 / 521,8 ms | die **gewinnenden** Paare kamen weit innerhalb der Schranke zurueck |
| `kill.k_s5.backoff_deckel_erreicht` / `erwartet` | 26 / 33 | **7 Control-Clients machten in der Totzeit keine 5 Versuche** — sie versuchten gar nicht mehr |
| `liveness.stale_ausserhalb_neustart` / `stale_im_neustartfenster` | 0 / 0 | kein Mitglied wurde je „stale": es kam nie zurueck, um zu veralten |
| `liveness.evicted_ausserhalb_neustart` | **13.440** | S13 ROT — die verlorenen Sonden wurden ausserhalb der Fenster dauerhaft geraeumt |
| `p0.gesendet` / `beantwortet` | 56.673 / 56.161 | Die nominale Obergrenze aus den Laufparametern (`clientpaare` 33 x 1 Hz x 1.800 s) ist **59.400**; es fehlen **2.727** Heartbeats, die geparkte Clients nicht mehr einreihten. Selbst gerechnet, nicht uebernommen: die Kurzfassung des Defekts nannte hier rund 69.000, was aus den Laufparametern nicht folgt |
| `p0.verloren_ausserhalb_neustart` | 0 | **kein** ACK ging verloren — die verlorenen Heartbeats wurden nie eingereiht und deshalb nie erwartet |

Die letzte Zeile ist der Grund fuer Regel R3(a): das Werkzeug hat den Ausfall **nicht gesehen**,
weil `SessionSoakMain.cpp:777-785` eine abgewiesene Einreihung still zuruecknimmt
(`gesendet.pop_back()`). Der Defekt war nur an `p0.gesendet` gegen eine Kopfrechnung erkennbar.

## 3. Die Regeln, die den Defekt schliessen — woertlich (Dirigent, Technikentscheid)

> **R1.** Ein erschoepfter `ERROR_PIPE_BUSY` (die Pipe existiert, alle Instanzen sind belegt, der
> Server wurde nie erreicht) ist ein LIVENESS-Ausgang: eigener `ServerPruefStatus`-Wert
> (z. B. `belegtNichtErreicht`) mit eigenem `ServerPruefFehler`, der den normalen Backoff
> 500 bis 8000 ms ausloest (`IpcVerbindung.h:34-35`) und NIE den Broker-Startpfad oeffnet (nur
> `nichtDa` darf das, `IpcVerbindung.h:41`) und NICHT als Serverpruefung zaehlt
> (`ControlClient.cpp:1101-1103`). `belegtAberUnverifiziert` bleibt ausschliesslich dem Fall
> vorbehalten, in dem ein Handle geoeffnet wurde und `namedPipeServerAuthentisieren`
> (`IpcVerbindung.cpp:106-113`) den Identitaetsbeweis nicht bestand, sowie jedem Oeffnungsfehler,
> der auf einen fremden Besitzer des Namens deutet (`ERROR_ACCESS_DENIED` und alle uebrigen Fehler
> ausser `PIPE_BUSY` und `FILE_NOT_FOUND`) — die NAK-123-Sicherheitszusage (kein wiederholtes
> Anklopfen an einen belegten, unverifizierten Namen) bleibt fuer diese Faelle unveraendert.
>
> **R2.** Control- und Telemetrie-Client behandeln den neuen Status identisch (Backoff, kein Parken);
> Gegenpfade verbinden↔trennen und parken↔loesen bleiben symmetrisch (`reconnect()`/`stop()` loesen
> weiterhin jede Sperre).
>
> **R3.** Werkzeug A24 muss den Defekt sehen koennen: (a) `SessionSoakMain.cpp:777-785` verwirft eine
> abgelehnte P0-Einreihung still (`gesendet.pop_back()`) — abgelehnte Einreihungen bekommen einen
> eigenen Zaehler im Bericht (`p0.einreihung_abgelehnt`), und `pruefe_session_soak.py` wertet ihn in
> S04 (Wert muss ausserhalb der Neustartfenster 0 sein); (b) `reconnect_paare`
> (`SessionSoakMain.cpp:1064,1108,1270`) ist heute die Vektorlaenge `anzahl+1` — es wird die Zahl der
> Paare, deren Control UND Telemetrie nach dem Neustart tatsaechlich wieder verbunden waren; der
> Pruefer vergleicht sie mit erwartet und meldet ROT bei Abweichung. Beide Aenderungen sind
> Pruefwerkzeug, kein Produktcode; die Matrix in `docs/beweise/G3-SOAK.md` §5/§7 wird um die zwei
> Zaehler ergaenzt (append-only Nachtrag, datiert).

### 3.1 Nachtrag Phase 1b — Regeln R4 und R5 (Dirigent, 03.09.2026)

Beide schliessen Luecken, die die Matrixpruefung 1 an der Quelle gefunden hat.

> **R4.** Scheitert nach bestandenem Identitaetsbeweis ein lokaler Ressourcenschritt, ist das ein
> Liveness-Ausgang — Handle schliessen, normaler Backoff, kein Parken, kein Broker-Startpfad, keine
> zusaetzliche Serverpruefungszaehlung; der Bericht traegt den Win32-Fehler.
>
> **R5.** Ein Abbruch (`stop()`/`reconnect()`) wirkt spaetestens nach dem laufenden
> `WaitNamedPipeW(200 ms)` plus einer `CreateFileW`-Runde; trifft er nach einem erfolgreichen
> `CreateFileW` ein, wird der lokale Handle ohne Hello geschlossen, eine eventuell laufende
> Authentisierung ist zulaessig, ihr Ergebnis wird aber nicht veroeffentlicht (kein Statuswechsel,
> keine Zaehlung).

**Was der Code heute davon schon erfuellt** (Phase 1b an der Quelle gelesen, `8e6a2ac`):

| Regel | Heutiger Stand | Offen fuer Phase 2 |
|---|---|---|
| R4 | `IpcVerbindung.cpp:115-122` schliesst bei `CreateEventW == nullptr` den Handle, setzt `bericht.status = nichtGeprueft` und gibt `false` zurueck. Damit faellt der Ausgang aus der Zaehlbedingung (`ControlClient.cpp:1101-1103` verlangt `verifiziert` oder `belegtAberUnverifiziert`), `authBlockiert` bleibt `false` (`:1099-1012` prueft auf `belegtAberUnverifiziert`) und der Thread nimmt den normalen Backoff. Kein Startpfad, weil der Status nicht `nichtDa` ist (`BrokerLifecycle.cpp:857-861`). **Vier der fuenf R4-Zusagen halten ohne Codeaenderung.** | **Die fuenfte nicht:** `bericht.win32Fehler` bleibt auf dem Wert, den die bestandene Authentisierung gesetzt hat — der `CreateEventW`-Fehler steht nur in der Fehlerzeichenkette (`"CreateEvent Win32 <n>"`), nicht im Bericht. Phase 2 setzt `bericht.win32Fehler = GetLastError()` vor `CloseHandle` |
| R5 | Der Abbruch wirkt in v3 ueber `abbruch.load()` am Schleifenkopf (`IpcVerbindung.cpp:69-70`) **und** ueber die Generation (`ControlClient.cpp:1097`), in v2 ausschliesslich ueber die Generation (`PipeClient.cpp:373-374`); `PipeClient::aktiveIoAbbrechen` (`:803-808`) setzt kein Abbruchflag, sondern cancelt nur ein bereits veroeffentlichtes Handle. Zwischen erfolgreichem `CreateFileW` und `namedPipeServerAuthentisieren` steht auf **keinem** der drei Pfade eine Abbruchpruefung (`IpcVerbindung.cpp:100-106`, `PipeClient.cpp:405-411`) — die Authentisierung laeuft also zu Ende. Ihr Ergebnis wird verworfen: v3 `ControlClient.cpp:1122-1131` schliesst und gibt `false`, v2 `PipeClient.cpp:443-449` schliesst und gibt `false`; die Zaehlung steht in beiden Faellen unter `if (! veralteteGeneration)` | **Nichts am Verhalten** — R5 beschreibt den heutigen Code. Offen ist allein die **Messung**: keine der bestehenden Proben trennt „Abbruch vor `CreateFileW`-Erfolg" von „Abbruch danach". Phase 2 baut die Proben D-A01 bis D-A12 |

## 4. Benennung und Verortung — Entscheide dieser Phase

Technische Wege entscheidet der Worker; jede Wahl steht hier mit ihrem Grund.

| # | Entscheid | Grund |
|---|---|---|
| E1 | Neuer Statuswert heisst `ServerPruefStatus::belegtNichtErreicht` | R1 nennt ihn als Vorschlag; er sagt genau das Gemessene — der Name ist belegt, der Server wurde nicht erreicht. Kein Urteil ueber Identitaet. |
| E2 | Neuer Fehlerwert heisst `ServerPruefFehler::pipeBelegt` | Parallel zu `pipeFehlt` (FILE_NOT_FOUND) und `pipeOeffnen` (alles Uebrige). `pipeOeffnen` behaelt seine Bedeutung „Oeffnen scheiterte auf eine Art, die auf einen fremden Besitzer deuten kann". |
| E3 | `serverPruefFehlerName` (`BrokerLifecycle.cpp:432-460`) bekommt den Fall `pipeBelegt` | Die Funktion ist die einzige Namensquelle; sie faellt sonst auf `"unbekannt"` und der Fehlertext der Anzeige loege. Es gibt **keine** Namensfunktion fuer `ServerPruefStatus` — der Status wird nirgends als Text ausgegeben, nur verglichen (`git grep`: kein `switch` ueber den Status im Produktcode). |
| E4 | **Der v2-`PipeClient` gehoert in den Aenderungssatz**, obwohl der Auftrag ihn nicht auffuehrt | `docs/beweise/NAK-123.md` Zeile **C-06** bindet: „Alle drei Pfade verwenden denselben Serverauth-Helfer" — und NAK-123 begruendet den v2-Retry selbst mit „C-06 verlangt fuer alle drei Connectpfade dieselbe Regel" (`NAK-123.md:313`). Wuerde nur v3 umklassifizieren, brechen zwei Zusagen: die drei Pfade wichen im selben Fehlerfall auseinander, und der v2-Produktpfad behielte exakt den Defekt, dessen Symptom NAK-123 dort schon einmal gesehen hat. Das ist kein Neben-Refactor, sondern dieselbe Regel auf der dritten Kante desselben Vertrags. `PipeClient.cpp` fuehrt seine eigene Oeffnungsschleife (`:371-405`) und erbt nichts automatisch. |
| E5 | `BrokerLifecycle.cpp`, `SourcesModel.cpp` und `PluginProcessor.cpp` werden **nicht** geaendert | Beide Konsumenten fuehren den neuen Status ohne Aenderung korrekt: der Spawnzweig verlangt `nichtDa` (`BrokerLifecycle.cpp:857-861`), die Anzeige faellt in ihren Else-Zweig `brokerUnavailable` **mit** Handgriff (`SourcesModel.cpp:410-418`). Beide Zeilen sind trotzdem Matrixzeilen mit Test — eine Zusage ohne Messung ist keine. |
| E6 | `namedPipeErreichbar` (`IpcVerbindung.cpp:19-30`) bleibt unveraendert | Sie meldet fuer `ERROR_PIPE_BUSY`/`ERROR_SEM_TIMEOUT` bereits „erreichbar" und steht damit schon auf der Livenessseite; R1 macht die Klassifikation im Oeffnungspfad nur gleichlautend. |
| E7 | Kein Schema, kein State, keine Fixture, kein Wire-Feld | Status und Fehler sind reine Laufzeitfelder des Client-Snapshots. Ein `git grep` ueber `eq-copilot/schemas/`, `eq-copilot/plugin/state/` und alle `*.json`/`*.py`/`*.ps1` findet keinen Verbraucher. Damit entfaellt die Versionierungspflicht aus `CLAUDE.md` („Schemas sind Vertraege"). |
| **E8** | **Phase 1b: `serverPruefungen` braucht auf keinem der drei Pfade eine Codeaenderung** | v3 zaehlt statusgesteuert gegen die **Positivliste** `verifiziert` oder `belegtAberUnverifiziert` (`ControlClient.cpp:1101-1103`, `TelemetryClient.cpp:554-556`); ein neuer Enum-Wert faellt automatisch heraus. v2 zaehlt positionsgesteuert erst **hinter** einem erfolgreichen `CreateFileW` (`PipeClient.cpp:418`); ein Oeffnungsfehler erreicht die Stelle nie. Der Aenderungssatz in §12 wird entsprechend berichtigt — Phase 1 hatte dort eine Aenderung an `ControlClient.cpp:1101-1103` angekuendigt, die es nicht braucht. |
| **E9** | **Phase 1b: Matrix D wird faktorisiert, und die Faktorisierung ist belegt, nicht behauptet** | `backoffMs` ist eine **lokale** Variable von `ControlClient::Laufzeit::threadLauf` (`ControlClient.cpp:984`), `TelemetryClient::Laufzeit::threadLauf` (`TelemetryClient.cpp:370`) und `PipeClient::threadLauf` (`PipeClient.cpp:290`). Sie wird `eineVerbindung` nie uebergeben und ist im Oeffnungspfad (`IpcVerbindung::oeffnen`, `PipeClient::eineVerbindung`) nicht sichtbar. Die Klassifikation eines Oeffnungsausgangs ist damit nachweislich unabhaengig von der Verbindungsphase; die Phase wirkt ausschliesslich auf das, was `threadLauf` **nach** der Rueckkehr tut. Deshalb steht D als **D-K** (Klassifikation: Ausgang x Pfad), **D-P** (Phasenwirkung: Ausgang x Phase, je Pfad benannt) und **D-A** (Abbruch: Zeitpunkt x Vorgang x Pfad) — jede Kombination genau einmal, ohne 81 wiederholende Zeilen. |

### 4.1 Zwei Win32-Zusagen, an der Primaerquelle geprueft

Beide tragen den Testaufbau in Phase 2 und sind deshalb hier belegt, nicht angenommen.

| Zusage | Quelle | Wortlaut |
|---|---|---|
| `WaitNamedPipeW` laeuft bei belegter, aber existierender Instanz in sein Zeitlimit | Microsoft Learn, `WaitNamedPipeW` (Doku-Stand 2024-02-22) | „If no instances of the specified named pipe exist, the **WaitNamedPipe** function returns immediately, regardless of the time-out value." / „If the time-out interval expires, the **WaitNamedPipe** function will fail with the error **ERROR_SEM_TIMEOUT**." |
| Ein Client, der eine vom Servermodus nicht erlaubte Zugriffsart verlangt, bekommt `ERROR_ACCESS_DENIED` | Microsoft Learn, `CreateNamedPipeA` (Doku-Stand 2025-07-01) | „**PIPE_ACCESS_INBOUND** … The client must specify **GENERIC_WRITE** access when connecting to the pipe." |

Daraus folgen die zwei deterministischen Testaufbauten ohne echten Broker:

* **`PIPE_BUSY` erschoepft:** Testserver mit `nMaxInstances = 1`, die eine Instanz bereits durch
  einen eigenen Client belegt, **kein** ausstehendes `ConnectNamedPipe`. Der Pruefling faehrt seine
  20 Runden `WaitNamedPipeW(200)` leer (je Runde `ERROR_SEM_TIMEOUT`) und endet mit
  `ERROR_PIPE_BUSY`. Dauer je Fall **mindestens 4.000 ms** (untere Schranke, siehe §7).
* **`ACCESS_DENIED`:** Testserver mit `PIPE_ACCESS_INBOUND`. Beide Oeffnungspfade verlangen fest
  `GENERIC_READ | GENERIC_WRITE` (`IpcVerbindung.cpp:72`, `PipeClient.cpp:376`) und werden
  sofort abgewiesen. Dauer je Fall unter 50 ms.

Beide Tests pruefen den **beobachteten** `win32Fehler` mit, nicht nur die Klassifikation: faellt die
Mechanik anders aus als hier belegt, wird der Test rot statt still gruen.

## 5. Verhaltensmatrix D

**Ausgangszustand fuer D-K und D-P:** Clientthread laeuft (`laeuft == true`), die
Verbindungsgeneration ist aktuell (`sollAbbrechen(generation) == false`), Hello und Adresse haben
den v3-Vertrag bereits bestanden. Pfade: **V3C** = `ControlClient`, **V3T** = `TelemetryClient`,
**V2** = `PipeClient` (Produktpfad v2). Die Faktorisierung in D-K, D-P und D-A ist in E9 belegt.

### 5.1 D-K — Klassifikation: Oeffnungsausgang x Pfad

Spalte „Status/Fehler" nennt `heute → nach R1`; ein `=` heisst: R1 aendert diese Zelle nicht.
Spalte „Zaehler" nennt nur, was sich **gegenueber dem Vorzustand aendert**, ebenfalls
`heute → nach R1`. `verbindungsVersuche` steht auf **allen drei Pfaden** in der Eintrittssperre
und ist damit fuer jede D-K-Zeile bereits +1, bevor irgendein Oeffnungsversuch laeuft
(`ControlClient.cpp:1048`, `TelemetryClient.cpp:531`, `PipeClient.cpp:347`).
Spalte „Rueckgabe" nennt den Wert von `eineVerbindung`, der allein den Backoff-Reset steuert
(`ControlClient.cpp:988`, `TelemetryClient.cpp:401`, `PipeClient.cpp:302`).
Spalte „Startpfad" beantwortet: darf `BrokerLifecycle` daraus einen Spawn ableiten?
Spalte „C" nennt die NAK-123-Zeile, die diese Zeile **erweitert** — keine wird ersetzt.

| ID | Oeffnungsausgang | Pfad | Status / Fehler (heute → nach R1) | Zaehler (heute → nach R1) | Handle | Rueckgabe | Startpfad | C | Test |
|---|---|---|---|---|---|---|---|---|---|
| D-K01 | Handle offen, verifiziert, Hello geschrieben, Welcome vertragsgemaess | V3C | `verifiziert` / `keiner` → `=` | `verbindungsVersuche` +1, `serverPruefungen` +1 → `=` | veroeffentlicht, bis die Verbindung endet | `true` — aber **erst beim Verbindungsende** (`ControlClient.cpp:1512`) | nein (`verifiziert`, Startmutex frei) | C-02, C-06 | bestehend `IpcTestMain.cpp/serverauth_gueltiger_pid_sid_datei_hash_und_signer_vor_hello` (misst `verbunden`, `verifiziert`, `keiner`, `serverPruefungen == 1` und null Bytes vor der Verifikation) |
| D-K02 | wie D-K01 | V3T | `verifiziert` / `keiner` → `=` | wie D-K01 → `=` | wie D-K01 | `true` beim Verbindungsende (`TelemetryClient.cpp:774`) | nein (nur der Control-Snapshot speist den Lifecycle-Hook, `PluginProcessor.cpp:110-122`) | C-06 | bestehend `IpcTestMain.cpp/control_und_telemetry_authentisieren_jedes_handle` (misst beide Pfade `verifiziert` mit je `serverPruefungen == 1`) |
| D-K03 | wie D-K01 | V2 | `verifiziert` / `keiner` → `=` | `verbindungsVersuche` +1, `serverPruefungen` +1 → `=` | veroeffentlicht in `aktivesHandle` (`PipeClient.cpp:439`) | `true` beim Verbindungsende | nein (v2 speist den v3-Lifecycle nicht) | C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `verifiziert`. Der in Phase 1 genannte `pipeclient_sendet_vor_serverauth_keine_bytes` misst **nicht** diesen Fall: er injiziert `hashFalsch` (`:306-343`) und gehoert zu D-K09 |
| D-K04 | Handle offen, verifiziert, aber Hello-Schreiben oder Welcome scheitert (Frist, `reject`, Vertragsbruch, Envelope-Verstoss) | V3C | `verifiziert` waehrend der Pruefung → **`nichtGeprueft`** beim Scheitern (`ControlClient.cpp:1232-1241`) → `=` | `verbindungsVersuche` +1, `serverPruefungen` +1 (die Pruefung fand statt) → `=` | geoeffnet und wieder geschlossen | **`false`** — **kein** Backoff-Reset | nein (`nichtGeprueft` trifft weder Spawn- noch Sperrzweig) | C-02 | **NEU** `IpcTestMain.cpp/verifiziert_ohne_welcome_setzt_backoff_nicht_zurueck` (Testserver authentisiert, antwortet dann nicht) |
| D-K05 | wie D-K04 | V3T | wie D-K04 (`TelemetryClient.cpp:689-698`) | wie D-K04 | wie D-K04 | **`false`** | nein | C-06 | **NEU** derselbe Test, Pfad Telemetrie |
| D-K06 | wie D-K04 | V2 | `verifiziert` waehrend der Pruefung → **`nichtGeprueft`** beim Scheitern — v2 hat dafuer einen gemeinsamen Aufraeumpfad, den **jeder** Rueckkehrweg mit offenem Handle durchlaeuft (`PipeClient.cpp:604-622`) → `=` | `verbindungsVersuche` +1, `serverPruefungen` +1 → `=` | geoeffnet, `aktivesHandle` gesetzt, im Aufraeumpfad geloescht und geschlossen (`:604-609`) | **`false`** — `return welcomeKam` (`:622`) | nein | C-06 | **NEU** derselbe v2-Test, Fall `welcome_bleibt_aus` |
| D-K07 | Handle offen, `namedPipeServerAuthentisieren` faellt an einer Identitaetskante (`serverPidFalsch`, `serverSidFalsch`, `dateiidentitaetFalsch`, `hashFalsch`, `signaturFehltOderUngueltig`, `signerFalsch`, die Token-/Prozess-Oeffnungsfehler) | V3C | `belegtAberUnverifiziert` / die konkrete Klasse → `=` | `verbindungsVersuche` +1, `serverPruefungen` +1 → `=` | geoeffnet, sofort geschlossen (`IpcVerbindung.cpp:107-113`) | `false` | **nein** (`blockiertUnverifiziert`, `BrokerLifecycle.cpp:734-747`) | C-03, C-04, C-07 | bestehend `IpcTestMain.cpp/serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` und `/serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed` |
| D-K08 | wie D-K07 | V3T | wie D-K07 | wie D-K07 | wie D-K07 | `false` | **nein** | C-06 | **NEU** `IpcTestMain.cpp/telemetrie_authfehler_parkt_wie_control` (Telemetrie mit `erwartung.testFehler`). Der in Phase 1 genannte `control_und_telemetry_authentisieren_jedes_handle` misst nur den **Erfolgsfall** (`:1078-1115`) und gehoert zu D-K02 |
| D-K09 | wie D-K07 | V2 | wie D-K07 | `verbindungsVersuche` +1, `serverPruefungen` +1 (`PipeClient.cpp:418`, unbedingt hinter dem offenen Handle) → `=` | geoeffnet, geschlossen (`PipeClient.cpp:443-449`) | `false` | nein | C-06, C-09 | bestehend `PipeClientLifecycleTestMain.cpp/pipeclient_sendet_vor_serverauth_keine_bytes` (`hashFalsch`, misst `serverPruefungen == 1` und `heartbeatsGesendet == 0`) |
| D-K10 | Handle offen, Authentisierung bricht **vor** der ersten Handle-Abfrage ab: `erwartungUngueltig` (`BrokerLifecycle.cpp:462-475`) | V3C, V3T, V2 | `belegtAberUnverifiziert` / `erwartungUngueltig` → `=` — `authFehler` setzt den Status **immer** auf `belegtAberUnverifiziert` (`BrokerLifecycle.cpp:391-400`) | wie D-K07 bzw. D-K09 | geoeffnet, geschlossen | `false` | **nein** | C-03 | **NEU** je Pfad ein Fall in der Fallschleife (leerer `absoluterBrokerPfad`) — in Phase 1 fehlte der Ausgang ganz |
| D-K11 | Handle offen, `GetNamedPipeServerProcessId` liefert nichts: `serverPidNichtErmittelbar` (`BrokerLifecycle.cpp:477-482`) | V3C, V3T, V2 | `belegtAberUnverifiziert` / `serverPidNichtErmittelbar` → `=` | wie D-K10 | geoeffnet, geschlossen | `false` | **nein** | C-03 | **NEU** je Pfad ein Fall (`erwartung.testFehler = serverPidNichtErmittelbar`, der Injektionspfad existiert bereits) — in Phase 1 fehlte der Ausgang ganz |
| D-K12 | Handle offen, Authentisierung **bestanden**, danach faellt `CreateEventW` (Luecke L1, R4) | V3C | `verifiziert` → **`nichtGeprueft`** (`IpcVerbindung.cpp:120`) → `=` | `verbindungsVersuche` +1; `serverPruefungen` **unveraendert** (`nichtGeprueft` faellt aus der Positivliste) → `=` | geoeffnet, geschlossen, **nie veroeffentlicht** | `false` | **nein** | erweitert C-02 | **NEU** `IpcTestMain.cpp/ressourcenfehler_nach_authbeweis_ist_liveness`. Ist `CreateEventW` nicht injizierbar, faellt die Zeile in die Klasse **Regressionswache durch Lesen** (§10) |
| D-K13 | wie D-K12 | V3T | wie D-K12 | wie D-K12 | wie D-K12 | `false` | **nein** | erweitert C-06 | **NEU** derselbe Test, Pfad Telemetrie |
| D-K14 | wie D-K12 | V2 | **entfaellt** — v2 legt im Oeffnungspfad kein Ereignisobjekt an; `CreateEventW` steht erst im E/A-Pfad hinter der Auth (`PipeClient.cpp:690`) | — | — | — | — | — | **Codepruefung** statt Test: `git grep -n "CreateEventW" eq-copilot/plugin/src/PipeClient.cpp` trifft nur `:690`. Die Zeile behauptet nichts, was ein Test messen koennte |
| D-K15 | `CreateFileW` liefert `ERROR_FILE_NOT_FOUND` (2) | V3C | `nichtDa` / `pipeFehlt` → `=` | `verbindungsVersuche` +1; `serverPruefungen` **unveraendert** → `=` | kein Handle | `false` | **ja** — einzige Zeile, die ihn oeffnet | C-01 | bestehend `IpcTestMain.cpp/fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` (misst `nichtDa`, `pipeFehlt`, `brokerPipeFehlt`, `serverPruefungen == 0`) |
| D-K16 | wie D-K15 | V3T | `nichtDa` / `pipeFehlt` → `=` | wie D-K15 → `=` | kein Handle | `false` | nein (nur der Control-Snapshot speist den Lifecycle-Hook) | C-01 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `file_not_found` x Telemetrie |
| D-K17 | wie D-K15 | V2 | `nichtDa` / `pipeFehlt` → `=` | `verbindungsVersuche` +1; `serverPruefungen` unveraendert (die Stelle `:418` wird nie erreicht) → `=` | kein Handle | `false` | nein | C-01 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `file_not_found` |
| D-K18 | 20 Runden `WaitNamedPipeW(200)` leer, `CreateFileW` liefert `ERROR_PIPE_BUSY` (231) | V3C | `belegtAberUnverifiziert` / `pipeOeffnen` → **`belegtNichtErreicht` / `pipeBelegt`** | `verbindungsVersuche` +1; `serverPruefungen` **+1 → unveraendert** (der neue Wert faellt aus der Positivliste, E8) | kein Handle | `false` | **nein** (heute und nach R1) | erweitert C-01, C-05 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `pipe_busy` x Control; prueft `win32Fehler == ERROR_PIPE_BUSY` mit |
| D-K19 | wie D-K18 | V3T | wie D-K18 | wie D-K18 | kein Handle | `false` | **nein** | erweitert C-06 | **NEU** derselbe Test, Fall `pipe_busy` x Telemetrie |
| D-K20 | wie D-K18 | V2 | wie D-K18 | `verbindungsVersuche` +1; `serverPruefungen` unveraendert **heute wie nach R1** (die Stelle `:418` wird nie erreicht) | kein Handle | `false` | nein | erweitert C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `pipe_busy` |
| D-K21 | `CreateFileW` liefert `ERROR_ACCESS_DENIED` (5) | V3C | `belegtAberUnverifiziert` / `pipeOeffnen` → `=` | `verbindungsVersuche` +1; `serverPruefungen` **+1 → +1** — der Zaehler steigt hier **heute schon** und **bleibt** steigen, obwohl gar kein Handle geoeffnet wurde: die Bedingung ist der Status, nicht die Position (`ControlClient.cpp:1101-1103`). Phase 1 behauptete an dieser Stelle „unveraendert (kein Handle, keine Pruefung)" — das war falsch | kein Handle | `false` | **nein** | C-01, C-07 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `access_denied` x Control — misst `serverPruefungen == 1` als **Regressionswache auf den heutigen Wert** |
| D-K22 | wie D-K21 | V3T | wie D-K21 | wie D-K21 (`TelemetryClient.cpp:554-556`) | kein Handle | `false` | **nein** | C-06 | **NEU** derselbe Test, Fall `access_denied` x Telemetrie |
| D-K23 | wie D-K21 | V2 | wie D-K21 | `verbindungsVersuche` +1; `serverPruefungen` unveraendert — v2 zaehlt positionsgesteuert, der Oeffnungsfehler erreicht `:418` nie. **Die drei Pfade weichen hier auseinander**, heute wie nach R1 | kein Handle | `false` | nein | C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `access_denied` — misst `serverPruefungen == 0` |
| D-K24 | `CreateFileW` liefert einen sonstigen Win32-Fehler (weder 2 noch 5 noch 231; deterministisch ueber `ERROR_INVALID_NAME`) | V3C | `belegtAberUnverifiziert` / `pipeOeffnen` → `=` | wie D-K21 | kein Handle | `false` | **nein** | C-01 | **NEU** derselbe Test, Fall `sonstiger_fehler` x Control |
| D-K25 | wie D-K24 | V3T | wie D-K24 | wie D-K21 | kein Handle | `false` | **nein** | C-06 | **NEU** derselbe Test, Fall `sonstiger_fehler` x Telemetrie |
| D-K26 | wie D-K24 | V2 | wie D-K24 | wie D-K23 | kein Handle | `false` | nein | C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/...`, Fall `sonstiger_fehler` |

**Lesart von D-K21 bis D-K26:** sie aendern die **Klassifikation** nicht. Sie stehen in der Matrix,
weil R1 den Geltungsbereich von `belegtAberUnverifiziert` **verengt** — und eine Verengung ist erst
dann eine Zusage, wenn gemessen ist, was **nicht** mitgewandert ist (Pruefliste E). Ihre
Zaehlerspalte aendert sich **nicht** und ist deshalb Regressionswache auf einen heute schon
inkonsistenten Wert: v3 zaehlt eine „Serverpruefung", die nie stattgefunden hat. Diese
Inkonsistenz ist ausserhalb von R1 und geht als Zeile nach `docs/offene-punkte.md`.

### 5.2 D-P — Phasenwirkung: Oeffnungsausgang x Verbindungsphase

Die Phase ist der Stand von `backoffMs` beim Eintritt in `threadLauf`. **P-E** = Erstverbindung
(`backoffMs == kBackoffStartMs`, 500 ms). **P-R** = automatischer Reconnect (`backoffMs` zwischen
500 und 8.000 ms, Verdopplung nach jedem gescheiterten Durchlauf). **P-D** = Backoff-Deckel erreicht
(`backoffMs == kBackoffMaxMs`, 8.000 ms).

Weil `backoffMs = std::min (backoffMs * 2, kBackoffMaxMs)` **nach** dem `wait_for` steht
(`ControlClient.cpp:1025`, `TelemetryClient.cpp:435`, `PipeClient.cpp:334`), ist die
**beobachtbare Wartefolge** 500 → 1.000 → 2.000 → 4.000 → 8.000 → 8.000 … Der Deckel ist nach der
fuenften Wartezeit erreicht; das ist genau die Schwelle, die A24 als
`kill.k_s5.backoff_deckel_erreicht` misst.

| ID | Oeffnungsausgang (D-K-Bezug) | P-E Erstverbindung | P-R automatischer Reconnect | P-D Deckel erreicht | Pfade | Test |
|---|---|---|---|---|---|---|
| D-P01 | Verbindung stand und endete regulaer (D-K01 bis D-K03) | Rueckgabe `true` → `backoffMs` bleibt 500 | Rueckgabe `true` → **Ruecksetzung auf 500** | Rueckgabe `true` → **Ruecksetzung von 8.000 auf 500** | V3C, V3T, V2 | **NEU** `IpcTestMain.cpp/backoff_folge_und_deckel_sind_beobachtbar`, Fall `stehende_verbindung_setzt_zurueck` — misst die Versuchsstempel, nicht eine interne Variable |
| D-P02 | verifiziert, aber Welcome blieb aus (D-K04 bis D-K06) | Rueckgabe `false` → 500, dann 1.000 | Rueckgabe `false` → **keine** Ruecksetzung, naechste Verdopplung | Rueckgabe `false` → bleibt auf 8.000 | V3C, V3T, V2 | **NEU** derselbe Test, Fall `verifiziert_ohne_welcome` — der Fall, den die Phase-1-Matrix mit „die Verbindung steht, der Backoff faellt auf 500 ms zurueck" falsch beschrieben hat |
| D-P03 | Authentisierung gescheitert (D-K07 bis D-K11) | `authBlockiert == true` → **unbefristetes** `warte.wait`, kein Backoff | wie P-E | wie P-E — und ein **zuvor** erreichter Deckel wird beim Loesen der Sperre auf 500 zurueckgesetzt (`ControlClient.cpp:1010`, `TelemetryClient.cpp:420`, `PipeClient.cpp:318`). Ein **neuer** Deckel entsteht hier nie, weil zwischen den Sperren nicht gewartet wird | V3C, V3T, V2 | bestehend, siehe D-K07/D-K09; **NEU** die Phasenaussage in `.../parken_uebergeht_den_backoff` |
| D-P04 | lokaler Ressourcenfehler nach Authbeweis (D-K12, D-K13) | `authBlockiert == false` (`nichtGeprueft`) → normaler Backoff 500 | Verdopplung | gedeckelt bei 8.000 | V3C, V3T | **NEU** im Test aus D-K12 mitgemessen |
| D-P05 | `FILE_NOT_FOUND` (D-K15 bis D-K17) | normaler Backoff 500 | Verdopplung | gedeckelt bei 8.000 | V3C, V3T, V2 | **NEU** `backoff_folge_und_deckel_sind_beobachtbar`, Fall `file_not_found` |
| D-P06 | `PIPE_BUSY` erschoepft (D-K18 bis D-K20) | **heute:** `authBlockiert == true` → unbefristetes Parken, **kein** Versuch mehr. **Nach R1:** normaler Backoff 500 | **heute:** Parken. **Nach R1:** Verdopplung | **heute:** Parken, und ein zuvor erreichter Deckel faellt beim Loesen auf 500; `backoff_deckel_erreicht` bleibt aus — genau die 7 fehlenden Clients aus §2.2. **Nach R1:** gedeckelt bei 8.000, der Deckelzaehler steigt | V3C, V3T, V2 | **NEU** derselbe Test, Fall `pipe_busy`; **Beleg**, weil er vor dem Fix am Parken faellt |
| D-P07 | `ACCESS_DENIED` und sonstige (D-K21 bis D-K26) | `authBlockiert == true` → unbefristetes Parken | wie P-E | wie D-P03: ein zuvor erreichter Deckel faellt beim Loesen auf 500, ein neuer entsteht nicht | V3C, V3T, V2 | **NEU** derselbe Test, Fall `access_denied` — Regressionswache |
| D-P08 | Telemetrie ohne gueltige Kopplung (`linkId`/`challenge` nicht hex32) | Status `wartetAufKopplung`, Wartezeit `kBackoffStartMs / 10 + 1` ms, **`backoffMs` bleibt unberuehrt** (`TelemetryClient.cpp:383-398`) | wie P-E — die Kopplungswarte greift **vor** `eineVerbindung` und faelscht den Backoff-Stand nicht | wie P-E; ein zuvor erreichter Deckel bleibt stehen | V3T | **NEU** `IpcTestMain.cpp/kopplungswarte_faelscht_den_backoff_nicht` — in Phase 1 fehlte dieser Vorzustand ganz |

### 5.3 D-A — Abbruch: Zeitpunkt x Vorgang x Pfad

`stop()` und `reconnect()` sind **nicht** derselbe Vorgang und werden deshalb getrennt gefuehrt:
`stop()` setzt `laeuft = false` und beendet den Thread, `reconnect()` laesst ihn laufen und fordert
sofort eine neue Generation an. Der Zeitpunkt trennt „vor einem erfolgreichen `CreateFileW`" von
„danach", weil zwischen `CreateFileW`-Erfolg und `namedPipeServerAuthentisieren` auf keinem Pfad
eine Abbruchpruefung steht (R5, §3.1).

Die Aussage der Phase-1-Fassung, „D-07 bis D-21 brechen vor `namedPipeServerAuthentisieren` ab",
ist damit **zurueckgenommen**: sie gilt fuer D-K15 bis D-K26 (kein Handle) und fuer D-A01 bis
D-A06, aber **nicht** fuer D-A07 bis D-A12.

Die Wirklatenz ist eine **untere Schranke**, keine Sofortzusage: `ioAbbrechen()` beendet ein
laufendes `WaitNamedPipeW(200)` nicht (`IpcVerbindung.cpp:149-155` cancelt nur ein veroeffentlichtes
Handle; `PipeClient::aktiveIoAbbrechen` `:803-808` ebenso und ohne Abbruchflag). Der Abbruch wirkt
deshalb fruehestens nach der laufenden Wartezeit plus einer `CreateFileW`-Runde.

| ID | Zeitpunkt | Vorgang | Pfad | Was passiert | Zaehler | Status danach | Test |
|---|---|---|---|---|---|---|---|
| D-A01 | vor `CreateFileW`-Erfolg | `stop()` | V3C | `abbruch = true` und Generation +1; die Schleife bricht am Kopf ab (`IpcVerbindung.cpp:69-70`), `oeffnen` gibt `false` mit dem Default-Bericht (`nichtGeprueft`/`keiner`). `veralteteGeneration == true` → `eineVerbindung` schreibt **keinen** Zustand und gibt `false`. Danach `laeuft == false` → Thread endet | `verbindungsVersuche` +1 (Eintritt), `serverPruefungen` unveraendert | `stop()` setzt nach dem Join `getrennt` / `nichtGeprueft` / `keiner` / PID 0 (`ControlClient.cpp:729-735`). **Im Fristpfad (`detach`) setzt Control nur `stopFristUeberschritten` +1 und `getrennt`** — die Auth-Felder bleiben stehen (`:718-725`) | **NEU** `IpcTestMain.cpp/abbruch_vor_und_nach_createfile`, Fall `stop_vor_open` |
| D-A02 | vor `CreateFileW`-Erfolg | `stop()` | V3T | wie D-A01 | wie D-A01 | Telemetrie setzt die Auth-Felder **auch im Fristpfad** zurueck (`TelemetryClient.cpp:295-307`) — die einzige belegte Asymmetrie zu Control | **NEU** derselbe Test, Pfad Telemetrie; die Asymmetrie wird gemessen, nicht geaendert |
| D-A03 | vor `CreateFileW`-Erfolg | `stop()` | V2 | Generation +1; `aktiveIoAbbrechen()` findet **kein** veroeffentlichtes Handle und tut nichts. Die Schleife bricht ueber `sollAbbrechen(generation)` ab (`PipeClient.cpp:373-374`) | `verbindungsVersuche` +1, `serverPruefungen` unveraendert | `stop()` joint **ohne Frist** (`PipeClient.cpp:249-260`) und setzt **keine** Zustandsfelder zurueck; `status` bleibt auf `verbindet` | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_abbruch_vor_und_nach_createfile`, Fall `stop_vor_open` |
| D-A04 | vor `CreateFileW`-Erfolg | `reconnect()` | V3C | Generation +1 **unter** `zustandMutex` zusammen mit dem Reset von `serverPruefstatus`/`serverPrueffehler`/`serverPid`/`brokerPipeFehlt`, dann `ioAbbrechen()`, `notify_all()`, `meldeLinkStatus(false)` (`ControlClient.cpp:738-754`). `laeuft` bleibt `true` | `verbindungsVersuche` +1, `serverPruefungen` unveraendert | `nichtGeprueft` / `keiner` / PID 0, `brokerPipeFehlt == false`; `threadLauf` sieht die neue Generation, setzt `backoffMs` auf 500 und beginnt **sofort ohne Wartezeit** eine neue Runde (`:1020-1023`) | **NEU** derselbe Test, Fall `reconnect_vor_open` |
| D-A05 | vor `CreateFileW`-Erfolg | `reconnect()` | V3T | wie D-A04, ohne `meldeLinkStatus` (Telemetrie hat keinen Link-Callback) und ohne `brokerPipeFehlt` | wie D-A04 | wie D-A04 | **NEU** derselbe Test, Pfad Telemetrie |
| D-A06 | vor `CreateFileW`-Erfolg | `reconnect()` | V2 | Generation +1 unter `zustandMutex` mit Statusreset, `notify_all()`, `aktiveIoAbbrechen()` (`PipeClient.cpp:263-280`) | wie D-A04 | `nichtGeprueft` / `keiner` / PID 0; neue Runde ohne Wartezeit | **NEU** `PipeClientLifecycleTestMain.cpp/...`, Fall `reconnect_vor_open` |
| D-A07 | **nach** `CreateFileW`-Erfolg | `stop()` | V3C | Die Authentisierung laeuft **vollstaendig** durch (keine Abbruchpruefung, `IpcVerbindung.cpp:100-113`); bei Erfolg legt `oeffnen` sogar Ereignis und Handle an und gibt `true`. `eineVerbindung` findet `veralteteGeneration == true`, ruft `verbindung.schliessen()` und gibt `false` (`ControlClient.cpp:1122-1131`) | `verbindungsVersuche` +1; **`serverPruefungen` unveraendert** — der ganze Zaehlblock steht unter `if (! veralteteGeneration)` | kein Statuswechsel aus diesem Lauf; danach der `stop()`-Zustand wie D-A01. **Kein Hello, kein Byte** | **NEU** derselbe Test, Fall `stop_nach_open` — misst, dass der Testserver ein `ConnectNamedPipe` sah, aber **kein** Hello las |
| D-A08 | nach `CreateFileW`-Erfolg | `stop()` | V3T | wie D-A07 (`TelemetryClient.cpp:518-590`) | wie D-A07 | wie D-A07, Fristpfad wie D-A02 | **NEU** derselbe Test, Pfad Telemetrie |
| D-A09 | nach `CreateFileW`-Erfolg | `stop()` | V2 | `namedPipeServerAuthentisieren` laeuft vollstaendig (`PipeClient.cpp:411`), dann `veralteteGeneration == true` → `CloseHandle(h)`, `return false` (`:443-449`). `aktivesHandle` wurde nie gesetzt, weil die Veroeffentlichung im `else`-Zweig unter derselben Bedingung steht (`:429-440`) | wie D-A07 | kein Statuswechsel; `stop()` joint ohne Frist und setzt nichts zurueck | **NEU** `PipeClientLifecycleTestMain.cpp/...`, Fall `stop_nach_open` |
| D-A10 | nach `CreateFileW`-Erfolg | `reconnect()` | V3C | wie D-A07, aber `laeuft` bleibt `true`: der Thread nimmt die neue Generation und beginnt sofort eine neue Runde. Der Reset in `reconnect()` steht **vor** dem verworfenen Urteil und wird von ihm nicht ueberschrieben (die Schreibung liegt unter `if (! veralteteGeneration)`) | wie D-A07 | `nichtGeprueft` / `keiner` / PID 0; naechste Runde ohne Wartezeit | **NEU** derselbe Test, Fall `reconnect_nach_open`; deckt zugleich die Zusage aus C-09 („Reconnect verwirft die Serverfreigabe") auf dem v3-Pfad |
| D-A11 | nach `CreateFileW`-Erfolg | `reconnect()` | V3T | wie D-A10 | wie D-A07 | wie D-A10 | **NEU** derselbe Test, Pfad Telemetrie |
| D-A12 | nach `CreateFileW`-Erfolg | `reconnect()` | V2 | wie D-A10 auf dem v2-Pfad | wie D-A07 | wie D-A10 | bestehend `PipeClientLifecycleTestMain.cpp/reconnectGeneration` **plus NEU** der Fall `reconnect_nach_open`. Der bestehende Test misst zwei **erfolgreiche** V2-Verbindungen ueber einen Reconnect (`:242-296`) und damit die Generationsgrenze, **nicht** den Abbruch waehrend der Authentisierung |

## 6. Verhaltensmatrix E

### 6.1 E-Z — Zaehlervertraege: heute und nach R1

Jede Zeile nennt den Zaehler, die Stelle, an der er entsteht, was der heutige Code tut, was nach R1
gilt und die Probe, die genau das misst. „heute = nach R1" heisst: R1 aendert diesen Vertrag nicht,
die Zeile ist Regressionswache.

| ID | Zaehler | Entsteht in | Heute | Nach R1 | Codeaenderung noetig? | Test |
|---|---|---|---|---|---|---|
| E-Z01 | `verbindungsVersuche` (v3 Control) | `ControlClient.cpp:1048`, in der Eintrittssperre von `eineVerbindung` | +1 bei **jedem** Eintritt, in dem die Generation aktuell ist — unabhaengig vom spaeteren Ausgang; bei bereits veralteter Generation gar nicht | identisch | **nein** | **NEU** `IpcTestMain.cpp/zaehlervertrag_verbindungsversuche_und_serverpruefungen`, Fall Control |
| E-Z02 | `verbindungsVersuche` (v3 Telemetrie) | `TelemetryClient.cpp:531` | wie E-Z01; die Kopplungswarte (D-P08) laeuft **vor** dem Eintritt und zaehlt nicht mit | identisch | **nein** | **NEU** derselbe Test, Fall Telemetrie |
| E-Z03 | `verbindungsVersuche` (v2) | `PipeClient.cpp:347` | +1 bei jedem Eintritt — **die Phase-1-Matrix trug fuer alle v2-Zeilen „keiner" ein; das war falsch** | identisch | **nein** | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_zaehlervertrag` |
| E-Z04 | `serverPruefungen` (v3, beide Clients) | `ControlClient.cpp:1101-1103`, `TelemetryClient.cpp:554-556` | +1 gdw. `serverBericht.status` in der Positivliste `{verifiziert, belegtAberUnverifiziert}` liegt **und** die Generation aktuell ist. Damit zaehlt auch ein reiner Oeffnungsfehler ohne Handle (`ACCESS_DENIED`, `PIPE_BUSY`, sonstige) als „Serverpruefung" | `belegtNichtErreicht` faellt **automatisch** aus der Positivliste; `PIPE_BUSY` zaehlt nicht mehr. `ACCESS_DENIED` und sonstige zaehlen unveraendert weiter | **nein** (E8) | **NEU** derselbe Test, Faelle `pipe_busy` (Wert sinkt gegenueber heute) und `access_denied` (Wert bleibt) |
| E-Z05 | `serverPruefungen` (v2) | `PipeClient.cpp:418` | +1 unbedingt, sobald ein Handle offen ist und die Generation aktuell ist — **positionsgesteuert, ohne Statusbedingung**. Ein Oeffnungsfehler erreicht die Stelle nie | identisch — der neue Status entsteht im Oeffnungsfehlerzweig davor | **nein** (E8) | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_zaehlervertrag`, Faelle `pipe_busy` (0) und `verifiziert` (1) |
| E-Z06 | `stopFristUeberschritten` | `ControlClient.cpp:721`, `TelemetryClient.cpp:299` | +1, wenn `stop()` den Thread nach `kStopFristMs` abloest statt zu joinen | identisch | **nein** | bestehend `EqCopLebenslaufTest` (Stop-Fristen) **plus NEU** der Fall „Stop loest die Auth-Sperre" (E-V05) |

### 6.2 E-V — Folgeverbraucher des neuen Status

| ID | Ausgangszustand x Ereignis | Zusage | Startpfad | Test |
|---|---|---|---|---|
| E-V01 | `BrokerLifecycle` in `wartetAufConnect`, Control meldet `belegtNichtErreicht` x `tick()` | Kein Spawn, kein `blockiertUnverifiziert`, `zustand.serverNichtVerifiziert` bleibt **false**, kein `cooldowns`-Zuwachs, kein `startMutexNehmen`. Der Spawnzweig verlangt `nichtDa` (`BrokerLifecycle.cpp:857-861`), der Sperrzweig verlangt `belegtAberUnverifiziert` (`:733-747`). Der neue Wert trifft keinen von beiden und faellt in den `return` | **nein** | **NEU** `IpcTestMain.cpp/belegt_nicht_erreicht_spawnt_nicht_und_blockiert_nicht` |
| E-V02 | `BrokerLifecycle` in `wartetAufServerpruefung`, Control meldet `belegtNichtErreicht` x `tick()` | Die Phase geht nach `wartetAufConnect` und `letzterServerPruefstatus` traegt den neuen Wert (`:802-813`); die Vorbedingung `anfangsStatus == nichtGeprueft` trifft nicht zu. Auch danach kein Spawn, solange der Status nicht `nichtDa` wird | **nein** | **NEU** derselbe Test, zweiter Fall |
| E-V03 | `SourcesModel` hat eine Sicht, Control meldet `getrennt` + `belegtNichtErreicht` x `setzeControlTransport` | Diagnose `brokerUnavailable` (Text `Broker unavailable`) **mit** Handgriff (`diagnoseHatHandgriff == true`), **nicht** `serverUnverified`. Das ist die ehrliche Aussage: der Broker ist gerade nicht erreichbar und der User hat einen Handgriff, statt einer Sicherheitswarnung, die nichts behauptet | — | **NEU** `Sonde012SourcesModelTest.cpp/belegt_nicht_erreicht_zeigt_broker_unavailable_mit_handgriff` |
| E-V04 | Geparkter Client (D-K07 bis D-K11, D-K21 bis D-K26) x `reconnect()` | Die Sperre loest ueber den Generationswechsel (`ControlClient.cpp:1000-1010`), `backoffMs` faellt auf `kBackoffStartMs`, ein neuer Oeffnungsversuch laeuft. Gegenpfad parken↔loesen bleibt vollstaendig | — | **NEU** `IpcTestMain.cpp/parken_loest_ueber_reconnect_und_stop`, Fall `reconnect`. **Berichtigung zu Phase 1:** der dort genannte `unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht` (`:1152-1186`) ist ein reiner `BrokerLifecycle`-Hooktest — er misst `spawns == 0` und `serverNichtVerifiziert`, aber **nie** die Condition-Variable des Clients |
| E-V05 | Geparkter Client x `stop()` | Die Sperre loest, der Thread endet, `fertig` wird gesetzt, `join` kehrt innerhalb `kStopFristMs` zurueck; wird die Frist gerissen, steigt `stopFristUeberschritten` und der Thread wird abgeloest. Gegenpfad starten↔stoppen bleibt vollstaendig | — | **NEU** derselbe Test, Fall `stop` **plus** die bestehende Fristmessung in `EqCopLebenslaufTest` |
| E-V06 | Client im Backoff nach `belegtNichtErreicht` x `stop()`/`reconnect()` | Das `wait_for` endet an derselben Bedingung wie heute (`! laeuft` oder Generationswechsel); kein neuer Wartepfad, keine zweite Sperre | — | **NEU** im Test aus D-P06 mitgemessen |
| E-V07 | Beliebig x Audiocallback laeuft waehrend Klassifikation, Backoff und Reconnect | Keine neue Sperre, Allokation, Datei-, Pipe- oder Prozessoperation im `processBlock`. Der neue Statuswert existiert nur auf Client- und Lifecycle-Threads | — | bestehend `LebenslaufTestMain.cpp/processblock_quellschnitt_{datei,pipe,prozess}operationen_null`, `/broker_lifecycle_aufrufe_im_audiothread_null` |
| E-V08 | Beliebig x Save/Load, Migration, Wire | Kein persistentes Feld, kein Wire-Feld, keine Fixture beruehrt; `ServerPruefStatus` und `ServerPruefFehler` bleiben reine Laufzeitgroessen | — | **Dateiliste des Aenderungssatzes**, nicht ein Test: `git diff --stat 8e6a2ac..<Ende Phase 2>` zeigt **keine** Datei unter `eq-copilot/schemas/`, `eq-copilot/plugin/state/`, `eq-copilot/fixtures/` oder `broker/`. Die Phase-1-Fassung berief sich hier auf „unveraendert gruene" `StateMigrationTestMain`/`SchemaTestMain` — grüne Tests dieser Beine belegen nur, dass die bestehenden Vertraege halten, **nicht**, dass der neue Laufzeitstatus nirgends verdrahtet wurde. Die Dateiliste belegt genau das und wird in §13 als eigener Beleg gefuehrt |

## 7. Callback-Reihenfolge und Fristen

Die Reihenfolge im Oeffnungspfad ist unveraendert und bleibt die NAK-123-Reihenfolge:

```
CreateFileW  <  namedPipeServerAuthentisieren  <  Handle veroeffentlichen  <  Hello schreiben
             <  Welcome lesen  <  beiLinkStatus(true)
```

**Wo die Kette abbricht** — die pauschale Aussage der Phase-1-Fassung ist zurueckgenommen:

* D-K15 bis D-K26 (kein Handle) und D-A01 bis D-A06 (Abbruch vor dem `CreateFileW`-Erfolg) brechen
  **vor** `namedPipeServerAuthentisieren` ab. Kein Handle, kein Byte, kein `beiLinkStatus(true)`.
* D-A07 bis D-A12 (Abbruch nach dem `CreateFileW`-Erfolg) brechen **nach** der vollstaendig
  gelaufenen Authentisierung ab — es gibt zwischen beiden Schritten keine Abbruchpruefung.
  Auch hier fliesst **kein** Byte und `beiLinkStatus(true)` wird nie gerufen; das Urteil wird
  verworfen.
* D-K12/D-K13 (`CreateEventW`) brechen **nach** der bestandenen Authentisierung und **vor** der
  Handle-Veroeffentlichung ab.

`ControlClient.cpp:1252` ist der einzige `true`-Aufrufer von `beiLinkStatus` und liegt hinter dem
Welcome. Ein zuvor gemeldetes `true` ist bereits beim Verbindungsende auf `false` gefallen
(`ControlClient.cpp:1510`); die `exchange`-Wache in `meldeLinkStatus` (`:970-978`) verhindert den
doppelten Ende-Callback.

**Fristen als untere Schranken.** Die Phase-1-Fassung nannte fuer `PIPE_BUSY` „20 x 200 ms =
4.000 ms obere Schranke". Das ist falsch: 4.000 ms ist die Summe der **angeforderten**
Wait-Zeitlimits. Der `CreateFileW`-Aufwand je Runde und das Scheduling kommen hinzu, und
`WaitNamedPipeW` garantiert nur, dass es **nicht frueher** als das Zeitlimit mit `ERROR_SEM_TIMEOUT`
zurueckkehrt. Gemessen wird deshalb die untere Schranke plus der beobachtete Win32-Fehler.

| Ausgang | Frist bis zur Klassifikation | Frist bis zum naechsten Versuch |
|---|---|---|
| D-K15 bis D-K17 (`FILE_NOT_FOUND`) | die Schleife bricht in Runde 1 ab (kein `PIPE_BUSY`, also kein `WaitNamedPipeW`) | 500 ms, verdoppelt bis 8.000 ms |
| D-K18 bis D-K20 (`PIPE_BUSY` erschoepft) | **untere Schranke: mindestens 4.000 ms** (20 Runden x 200 ms angefordertes Zeitlimit, `IpcVerbindung.cpp:67-89`, `PipeClient.cpp:371-389`). Eine Obergrenze wird **nicht** zugesagt. Der Test misst `dauer >= 4.000 ms` **und** `win32Fehler == ERROR_PIPE_BUSY` | **nach R1** 500 ms, verdoppelt bis 8.000 ms; **heute** keiner: Parken |
| D-K21 bis D-K26 (`ACCESS_DENIED`, sonstige) | die Schleife bricht in Runde 1 ab | keiner: Parken bis `reconnect()`/`stop()` |
| D-K12, D-K13 (`CreateEventW`) | `CreateFileW` plus vollstaendige Authentisierung | 500 ms, verdoppelt bis 8.000 ms |
| D-A01 bis D-A06 (Abbruch vor Oeffnungserfolg) | **untere Schranke:** ein gegebenenfalls laufendes `WaitNamedPipeW(200 ms)` laeuft aus (es laeuft nur, wenn die Runde zuvor `ERROR_PIPE_BUSY` sah), danach greift die Abbruchpruefung am Schleifenkopf. Wirklatenz also mindestens die Restzeit dieser Wartezeit plus eine `CreateFileW`-Runde; eine Obergrenze wird nicht zugesagt | `stop()`: keiner (Thread endet). `reconnect()`: sofort, ohne Wartezeit (Generationswechsel setzt `backoffMs` auf 500 und `continue`) |
| D-A07 bis D-A12 (Abbruch nach Oeffnungserfolg) | die vollstaendige Authentisierung laeuft noch zu Ende; danach wird das Urteil verworfen | wie D-A01 bis D-A06 |
| D-K01 bis D-K11 | `CreateFileW` plus voller Serverbeweis | — |
| D-P08 (Telemetrie ohne Kopplung) | keine Klassifikation — es wird nicht geoeffnet | `kBackoffStartMs / 10 + 1` ms, ohne Wirkung auf `backoffMs` |

Die Schranke aus `docs/beweise/G3-SOAK.md` §3.1 (45.051 ms je Clientpaar) bleibt **unveraendert
gueltig**: sie rechnet bereits mit `8.000 ms Backoff + 4.000 ms WaitNamedPipe-Schleife + 2 x 5.000 ms
Bootstrap-E/A` je Runde und Client. R1 aendert keine dieser Konstanten. Was R1 aendert, ist, dass die
Rechnung fuer einen `PIPE_BUSY`-Verlierer ueberhaupt **weiterlaeuft**, statt an dieser Stelle
abzubrechen.

## 8. Was R1 **nicht** zusagt

R1 beseitigt das dauerhafte Parken. R1 sagt **nicht** zu, dass alle 33 Clientpaare nach einem
Neustart innerhalb der Z2-Frist von 60 s zurueck sind. §3.1 haelt das ausdruecklich fest: „weder
`IpcVerbindung::oeffnen` noch die Annahmeschlange des Listeners geben eine Fairnessgarantie, also
kann ein einzelnes Paar Rennen wiederholt verlieren, waehrend andere durchkommen."

Daraus folgt die Falsifikation fuer Phase 2, vor dem Lauf festgelegt:

* Wird Lauf **(c)** (`--sonden 32 --minuten 30 --neustarts 3`) gruen, ist der Defekt geschlossen und
  die Fairness des Brokers reicht fuer N = 32.
* Bleibt er rot **mit** Backoff-Verkehr (`k_s5.backoff_deckel_erreicht == 33`, `reconnect_paare`
  kleiner als 33), ist R1 wirksam, aber die verbleibende Ursache liegt in der Zahl der scharfen
  Listener-Instanzen — das ist **Register NAK-135** und ausdruecklich nicht dieses Ticket. Dieses
  Manifest traegt den Befund dann als Zeile, nicht als stillen Fehlschlag.

Die Unterscheidung ist genau der Grund fuer R3(b): ohne einen ehrlich gemessenen `reconnect_paare`
laesst sich „geparkt" nicht von „hat das Rennen wieder verloren" trennen.

## 9. Verhaltensmatrix W — Werkzeug A24 (R3)

Alle Zeilen sind **Pruefwerkzeug**. Sie aendern kein Produktverhalten und keinen Bericht des
Produkts.

**Warum je Zaehler zwei Proben.** Eine Mutante, die den **fertigen** Bericht veraendert
(`pruefe_session_soak.py:663-665` und der geplante neue Mutant), beweist den **Pruefer** — nicht die
C++-Erzeugung des Zaehlers. Ein `reconnect_paare`, das versehentlich weiterhin aus
`reconnectMs.size()` entsteht, bliebe damit unentdeckt, selbst wenn der Gesamt-Soak aus anderem
Grund rot ist. Deshalb traegt jeder Zaehler ab Phase 1b **einen Erzeugertest auf der C++-Seite**
(`SessionSoakMain`) **und** eine Berichtsmutante.

**Wie der Erzeugertest laeuft.** `EqCopSessionSoak` nimmt heute ausschliesslich Positionsargumente
und verlangt in `argv[1]` einen Probe-Pipenamen (`SessionSoakMain.cpp:1429-1441`). Der Erzeugertest
bekommt deshalb einen eigenen, ausdruecklich benannten Modus, der den Probenamen-Riegel behaelt,
aber weder Broker noch Sonden braucht: die Zaehlerbildung wird in je eine reine Funktion gezogen
und in diesem Modus gegen deterministische Eingaben geprueft. Damit misst der Test genau die
Erzeugung und nicht den Lauf.

| ID | Heute | Zusage nach R3 | Berichtsfeld | Pruefer | Erzeugertest C++ (NEU) | Berichtsmutante |
|---|---|---|---|---|---|---|
| W-01 | `SessionSoakMain.cpp:777-785` nimmt eine abgewiesene P0-Einreihung still zurueck (`gesendet.pop_back()`); der Heartbeat gilt als nie eingereiht und faellt aus jeder Statistik | Die Ruecknahme bleibt (sie ist richtig: nie eingereiht heisst nie erwartet), aber sie wird **gezaehlt** — getrennt nach innerhalb/ausserhalb der Neustartfenster, mit derselben Fensterlogik wie `imFensterGesendet` (`:765`) | `p0.einreihung_abgelehnt` (Summe, Pflichtfeld) und `p0.einreihung_abgelehnt_ausserhalb_neustart` | S04: `einreihung_abgelehnt_ausserhalb_neustart == 0`. Die Summe steht als Pflichtfeld daneben; ein fehlendes Feld ist ROT, nie uebersprungen (§7 G3-SOAK) | **NEU** `SessionSoakMain` Modus `selbsttest-zaehler`, Fall `p0_ablehnung`: ein `ControlClient` ohne Gegenstelle bekommt `kCapP0 == 64` Einreihungen (`IpcQueues.h:34`); die 65. gibt `false` (`ControlClient.cpp:798-816`). Der Test prueft, dass **genau eine** Ablehnung gezaehlt wird, dass `gesendet` wieder die Laenge vor dem Versuch hat und dass die Fensterzuordnung dem Stand von `imNeustartfenster` folgt | **NEU** `--mutant p0_einreihung`: setzt `einreihung_abgelehnt_ausserhalb_neustart` auf 1 — S04 muss fallen |
| W-02 | `reconnect_paare` (`SessionSoakMain.cpp:1270`) ist `n.reconnectMs.size()`, also strukturell `anzahl+1`. Der Pruefer vergleicht ihn in `pruefe_session_soak.py:920` gegen `args.sonden + 1` — eine Tautologie, dieselbe Klasse wie der `k_s5.erwartet`-Defekt aus §16 | `reconnect_paare` ist die **gemessene** Zahl der Paare, deren Control **und** Telemetrie am Ende des Fristfensters wieder `verbunden` waren (Main-Paar plus N Sonden). Bewusst **ohne** die Mitgliedschaftsbedingung, die `proPaar` zusaetzlich verlangt (`:1077-1091`): damit trennt der Bericht „Transport steht wieder" von „Mitglied ist wieder in der Sicht" — genau die Unterscheidung aus §8 | `reconnect_paare` (Bedeutung geaendert, Feldname gleich) | S11 (`pruefe_session_soak.py:920`) bleibt wortgleich `== args.sonden + 1` und hoert damit auf, tautologisch zu sein | **NEU** `SessionSoakMain` Modus `selbsttest-zaehler`, Fall `reconnect_paare`: die neue reine Funktion bekommt eine Liste von Paarstaenden, in der ein Paar transportseitig getrennt und ein anderes verbunden, aber nicht Mitglied ist. Erwartet wird `anzahl` (nicht `anzahl+1`) — der Test faellt, solange die Zahl aus einer Vektorlaenge stammt | bestehend `--mutant s11_paare` (`pruefe_session_soak.py:663-665`) trifft genau dieses Feld; er wird nach der Umstellung erneut gefahren und im Manifest mit Exitcode belegt |
| W-03 | — | `docs/beweise/G3-SOAK.md` §5 (Zeilen S04, S11) und §7 (Zaehlerkarte) tragen beide Zaehler mit ihrer Bedeutung; **append-only Nachtrag mit Datum**, der lebende Kopf wird nachgezogen, der Verlauf nie umgeschrieben (Pruefliste E) | — | — | — | — |

### 9.1 Haertungen aus der Matrixpruefung 1 (aufgenommen)

Der Pruefer hat sie als „keine Pflicht" gefuehrt. Sie werden aufgenommen und sind hier als
Haertung gekennzeichnet, damit sie nicht mit den Pflichtzeilen W-01 bis W-03 verwechselt werden.

| ID | Haertung | Warum | Wo gemessen |
|---|---|---|---|
| W-H1 | W-01 mit **deterministisch gefuellter 64er-P0-Queue** und Pruefung **beider** Fensterzaehler (`einreihung_abgelehnt` und `einreihung_abgelehnt_ausserhalb_neustart`) | Ohne die volle Queue haengt der Rotbeweis an einem Zufall der Laufzeit; ohne den zweiten Zaehler bliebe eine falsche Fensterzuordnung unsichtbar | Erzeugertest W-01, Fall `p0_ablehnung` (die 64er-Kapazitaet ist die Konstante `kCapP0`, nicht abgeschrieben) |
| W-H2 | W-02 mit einem **Berichtstest**, bei dem genau ein Paar transportseitig getrennt und ein anderes verbunden, aber noch nicht Mitglied ist | Genau diese zwei Faelle trennen den neuen Zaehler vom alten und von `proPaar`; ein Test mit nur einem Fall koennte beide Definitionen erfuellen | Erzeugertest W-02, Fall `reconnect_paare` |
| W-H3 | Backoff zusaetzlich ueber **beobachtete Versuchsstempel** pruefen, damit 500 → 1.000 → 2.000 → 4.000 → 8.000 und der Deckel **getrennt** belegt sind | `backoffMs` ist eine lokale Variable und von aussen nicht lesbar (E9). Nur die Abstaende zwischen beobachteten Oeffnungsversuchen belegen die Folge; nur ein sechster Versuch im selben Abstand belegt den Deckel | **NEU** `IpcTestMain.cpp/backoff_folge_und_deckel_sind_beobachtbar` (D-P01, D-P02, D-P05, D-P06) |

## 10. Testkarte — was neu ist und wie es rot wird

Pruefliste E verlangt: „Jede neue Prüfung wurde einmal absichtlich gebrochen … Eine Probe, die auch
ohne den Fix rot ist, heißt Regressionswache, nicht Beleg." Die Klasse steht deshalb je Test. Kein
Verweis in dieser Karte zeigt auf einen Test, der etwas anderes misst als die Zeile, die ihn nennt —
die drei Fehlverweise der Phase-1-Fassung (D-03, D-05, D-06 alt) sind in §5 und §6 benannt und
ersetzt.

| Test | Bein | Misst | Neu? | Rotbeweis-Klasse | Wie er rot wird |
|---|---|---|---|---|---|
| `oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Faelle `pipe_busy` x {Control, Telemetrie} | `EqCopIpcTest` (B10) | D-K18, D-K19 | **NEU** | **Beleg** — rot **vor** dem Produktfix | Am alten Code meldet der Fall `belegtAberUnverifiziert`/`pipeOeffnen` und der Thread parkt. Lauf und Exitcode stehen in Phase 2 unter „Rotbeweis vor dem Fix" |
| dieselbe Funktion, Faelle `file_not_found`, `access_denied`, `sonstiger_fehler`, je x {Control, Telemetrie} | `EqCopIpcTest` (B10) | D-K16, D-K21, D-K22, D-K24, D-K25 | **NEU** | **Regressionswache** — vor und nach dem Fix gruen | Sie messen, was **nicht** wandert, samt der heutigen `serverPruefungen`-Werte. Rot werden sie ueber die Fixmutante (Klassifikation auf `belegtNichtErreicht` ausgeweitet) |
| dieselbe Funktion, Faelle `erwartung_ungueltig`, `server_pid_nicht_ermittelbar` | `EqCopIpcTest` (B10) | D-K10, D-K11 | **NEU** | **Regressionswache** | Beide Ausgaenge fehlten in Phase 1 ganz. Rot ueber eine Mutante, die `authFehler` einen anderen Status geben liesse |
| `verifiziert_ohne_welcome_setzt_backoff_nicht_zurueck` | `EqCopIpcTest` (B10) | D-K04, D-K05, D-P02 | **NEU** | **Regressionswache** | Misst den heutigen Code. Rot ueber eine Mutante, die `backoffMs` schon beim Auth-Erfolg zuruecksetzt |
| `telemetrie_authfehler_parkt_wie_control` | `EqCopIpcTest` (B10) | D-K08 | **NEU** | **Regressionswache** | Ersetzt den Fehlverweis auf `control_und_telemetry_authentisieren_jedes_handle`, der nur den Erfolgsfall misst |
| `ressourcenfehler_nach_authbeweis_ist_liveness` | `EqCopIpcTest` (B10) | D-K12, D-K13, D-P04, R4 | **NEU**, sofern injizierbar | **Regressionswache**, und **Beleg** fuer die eine offene R4-Zusage | Vier der fuenf R4-Zusagen halten heute schon (§3.1); der `win32Fehler`-Teil ist rot vor dem Fix. Laesst sich `CreateEventW` nicht injizieren, faellt die Zeile in die Klasse **Regressionswache durch Lesen**: `IpcVerbindung.cpp:115-122` wird im Manifest zitiert und die Zeile traegt ausdruecklich „kein Test moeglich, Codepruefung" statt einer Testbehauptung |
| `abbruch_vor_und_nach_createfile`, Faelle `stop_vor_open`, `reconnect_vor_open`, `stop_nach_open`, `reconnect_nach_open`, je x {Control, Telemetrie} | `EqCopIpcTest` (B10) | D-A01, D-A02, D-A04, D-A05, D-A07, D-A08, D-A10, D-A11 | **NEU** | **Regressionswache** | Messen den heutigen Code (R5 beschreibt ihn). Rot ueber eine Mutante, die das Auth-Urteil einer veralteten Generation veroeffentlicht |
| `backoff_folge_und_deckel_sind_beobachtbar` | `EqCopIpcTest` (B10) | D-P01, D-P02, D-P05, D-P06, W-H3 | **NEU** | **gemischt**: Fall `pipe_busy` ist **Beleg** (vor dem Fix gibt es gar keine Folge, weil geparkt wird), die uebrigen Faelle sind **Regressionswache** | Der `pipe_busy`-Fall sieht vor dem Fix keinen zweiten Versuch und faellt |
| `kopplungswarte_faelscht_den_backoff_nicht` | `EqCopIpcTest` (B10) | D-P08 | **NEU** | **Regressionswache** | Rot ueber eine Mutante, die die Kopplungswarte `backoffMs` verdoppeln liesse |
| `zaehlervertrag_verbindungsversuche_und_serverpruefungen` | `EqCopIpcTest` (B10) | E-Z01, E-Z02, E-Z04 | **NEU** | **gemischt**: der Fall `pipe_busy` ist **Beleg** (der Wert sinkt durch R1), die uebrigen sind **Regressionswache** | Vor dem Fix zaehlt `pipe_busy` als Serverpruefung; der Test erwartet danach 0 und faellt vorher |
| `belegt_nicht_erreicht_spawnt_nicht_und_blockiert_nicht` | `EqCopIpcTest` (B10) | E-V01, E-V02 | **NEU** | **Mutante** | Vor dem Fix laesst sich die Zeile nicht ausdruecken — den Statuswert gibt es nicht. Der Rotbeweis ist deshalb eine Mutante **nach** dem Fix: `BrokerLifecycle` behandelt `belegtNichtErreicht` wie `nichtDa`; der Test muss den Spawn sehen und fallen. Das steht hier offen, statt einen Vor-Fix-Rotlauf zu behaupten, den es nicht geben kann |
| `parken_loest_ueber_reconnect_und_stop` | `EqCopIpcTest` (B10) | E-V04, E-V05, E-V06 | **NEU** | **Regressionswache** | Misst den Gegenpfad parken↔loesen am **Client**, den bisher kein Test beruehrt hat |
| `pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `pipe_busy` | `EqCopPipeClientTest` (A4b) | D-K20 | **NEU** | **Beleg** — rot vor dem Fix | wie oben, auf dem v2-Pfad |
| dieselbe Funktion, Faelle `verifiziert`, `welcome_bleibt_aus`, `file_not_found`, `access_denied`, `sonstiger_fehler`, `erwartung_ungueltig`, `server_pid_nicht_ermittelbar` | `EqCopPipeClientTest` (A4b) | D-K03, D-K06, D-K10, D-K11, D-K17, D-K23, D-K26 | **NEU** | **Regressionswache** | wie oben |
| `pipeclient_zaehlervertrag` | `EqCopPipeClientTest` (A4b) | E-Z03, E-Z05 | **NEU** | **Regressionswache** | Misst `verbindungsVersuche` +1 je Eintritt und die positionsgesteuerte `serverPruefungen`-Zaehlung. Rot ueber eine Mutante, die `:418` unter eine Statusbedingung stellte |
| `pipeclient_abbruch_vor_und_nach_createfile` | `EqCopPipeClientTest` (A4b) | D-A03, D-A06, D-A09, D-A12 | **NEU** | **Regressionswache** | wie der v3-Zwilling |
| `belegt_nicht_erreicht_zeigt_broker_unavailable_mit_handgriff` | `EqCopSonde012SourcesModelTest` (B13) | E-V03 | **NEU** | **Mutante** | Vor dem Fix nicht ausdrueckbar; Mutante: `SourcesModel` bildet den neuen Status auf `serverUnverified` ab |
| Fall „Stop loest die Auth-Sperre" (E-V05) | `EqCopLebenslaufTest` (B8) | E-Z06, E-V05 | **NEU** (Erweiterung) | **Regressionswache** | Gegenpfad starten↔stoppen mit der Fristmessung |
| `SessionSoakMain` Modus `selbsttest-zaehler`, Fall `p0_ablehnung` | `EqCopSessionSoak` | W-01, W-H1 | **NEU** | **Beleg** — vor der Werkzeugaenderung existiert der Zaehler nicht | Der Test verlangt genau eine gezaehlte Ablehnung bei 65 Einreihungen in eine 64er-Queue |
| `SessionSoakMain` Modus `selbsttest-zaehler`, Fall `reconnect_paare` | `EqCopSessionSoak` | W-02, W-H2 | **NEU** | **Beleg** — rot, solange die Zahl aus `reconnectMs.size()` stammt | Zwei Paare mit unterschiedlichem Transport-/Mitgliedschaftsstand; erwartet `anzahl`, die Vektorlaenge liefert `anzahl+1` |
| `--mutant p0_einreihung` (W-01) | `pruefe_session_soak.py` | Pruefer zu W-01 | **NEU** | **Mutante** an einem gespeicherten Bericht | Pflicht nach G3-SOAK §14/§15.3 |
| `--mutant s11_paare` (W-02) | `pruefe_session_soak.py` | Pruefer zu W-02 | bestehend | **Mutante**, nach der Umstellung erneut gefahren | Pflicht nach G3-SOAK §14 |

**Ein Test, viele Faelle — mit Absicht.** Die Zeilen D-K10 bis D-K26 liegen in genau zwei
Funktionen (v3 und v2), je als Fallschleife ueber `{Ausgang, Pfad}`. Vorbild sind
`serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed` und
`serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` (`IpcTestMain.cpp:974-1049`), die
ihre Fehlerkanten ebenso einzeln durchfahren und je Kante einzeln melden. Jede Zeile behaelt damit
ihren eigenen sichtbaren Pruefpunkt, ohne siebzehn fast gleiche Funktionen.

## 11. Pruefliste `tools/dirigent/pruefliste.md` — Stand Phase 1b

Phase 1b aendert keinen Code; die Zeilen stehen mit dem Ort, an dem Phase 2 sie messen **wird**, und
mit dem, was die Spezifikation bereits belegt hat.

| Zeile | Stand Phase 1b | Messort |
|---|---|---|
| A — Politik bei *voll* steht ausdruecklich | offen fuer Phase 2 | W-01: die P0-Ablehnung bekommt einen Zaehler statt einer stillen Ruecknahme; `SessionSoakMain.cpp:777-785`. Die Kapazitaet ist `kCapP0` (`IpcQueues.h:34`), aus der Quelle gelesen |
| A — Rueckgabewerte und Zaehler werden ausgewertet, ein strukturell-0-Zaehler ist eine Wache mit Test | **verschaerft in Phase 1b** | W-01 und W-02 tragen je einen **Erzeugertest in C++** neben der Berichtsmutante (§9); die Zaehlervertraege selbst stehen als E-Z01 bis E-Z06 mit je einer Probe |
| A — dieselbe Regel in allen Sprachen | **erfuellt in Phase 1 als Abgrenzung** | R1 beruehrt nur die C++-Clientseite; die Broker-Rust-Seite (Zahl scharfer Listener) ist Register NAK-135, §8 |
| B — Reihenfolge beim Verbinden festgelegt und gemessen | **erfuellt in Phase 1b, praeziser als in Phase 1** | §7: die NAK-123-Kette bleibt woertlich; **welche** Zeilen vor und welche nach der Authentisierung abbrechen, steht jetzt einzeln — die pauschale Aussage der Phase-1-Fassung ist zurueckgenommen |
| B — Gegenpfade, jeder Join hat eine Frist | **spezifiziert**, Messung Phase 2 | E-V04, E-V05, E-V06 und D-A01 bis D-A12. Die Fristunterschiede zwischen den drei `stop()`-Fassungen (Control mit Frist und ohne Feldreset im Abloesepfad, Telemetrie mit Frist und mit Reset, v2 ohne Frist) stehen als eigene Zeilen |
| C — Vertraege und Laengen | **nicht anwendbar, begruendet** | E7 in §4: kein Schema-, State-, Fixture- oder Wire-Feld beruehrt. Der Beleg ist ab Phase 1b die **Dateiliste** des Aenderungssatzes (E-V08), nicht ein gruener Test |
| D — Riegel fail-closed ohne Rohtextheuristik | **erfuellt in Phase 1b** | D-K24 bis D-K26: jeder **unbekannte** Win32-Fehler bleibt auf der Sicherheitsseite. R1 verengt nur um **zwei namentlich genannte** Codes; das Unbekannte bleibt fail-closed |
| E — Behauptung ist nie groesser als die Messung | **erfuellt in Phase 1b** | §8 nennt, was R1 **nicht** zusagt; §10 nennt je Test die Rotbeweis-Klasse und je Zeile den Test, der genau sie misst; §7 nennt untere Schranken statt Obergrenzen; E-V08 ersetzt eine Testbehauptung durch die Dateiliste |
| E — Zahlen sind gemessen, nicht abgeschrieben | **erfuellt** | §2.2: alle Zahlen aus `roh/G3-soak-n32-30min-ad5b2d1.json` gelesen. Alle Konstanten dieser Fassung aus der Quelle: `kBackoffStartMs`/`kBackoffMaxMs` (`IpcVerbindung.h:34-35`), `kCapP0` (`IpcQueues.h:34`), 20 Runden x 200 ms (`IpcVerbindung.cpp:67-89`), `kSchrankeMs`/`kFristMs` (`SessionSoakMain.cpp:76-78`) |
| E — geaenderte Zusage: drei Stellen | offen fuer Phase 2 | W-03: `G3-SOAK.md` §5/§7 als datierter append-only Nachtrag; Skriptkopf `pruefe_session_soak.py`; lebender Kopf dieses Manifests |
| F — Aenderungssatz vollstaendig | **spezifiziert** | verbinden↔trennen (E-V04, D-A), starten↔stoppen (E-V05), parken↔loesen (E-V04/E-V05) liegen im selben Satz wie die Klassifikation; alle drei Connectpfade ebenfalls (E4 in §4) |

## 12. Aenderungssatz Phase 2 — vorab benannt

| Datei | Aenderung |
|---|---|
| `eq-copilot/plugin/core/ipc/IpcVerbindung.h` | `ServerPruefStatus::belegtNichtErreicht`, `ServerPruefFehler::pipeBelegt`, Kommentar an `:41-42` auf die neue Dreiteilung |
| `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp` | Klassifikation `:90-99` dreiwertig; zusaetzlich `bericht.win32Fehler = GetLastError()` im `CreateEventW`-Zweig `:115-127` (die eine offene R4-Zusage, §3.1) |
| `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp` | **nur** `serverPruefFehlerName` um `pipeBelegt` (E3); die `tick`-Logik bleibt unangetastet (E5) |
| `eq-copilot/plugin/core/ipc/ControlClient.cpp` | **keine Aenderung noetig** (E8, berichtigt gegenueber Phase 1): `serverPruefungen` prueft eine Positivliste (`:1101-1103`), aus der der neue Wert automatisch faellt; `authBlockiert` bleibt an `belegtAberUnverifiziert` gebunden (`:995-997`). Die Datei bleibt trotzdem im Blick des Selbstaudits, weil E-Z01/E-Z04 sie messen |
| `eq-copilot/plugin/core/ipc/TelemetryClient.cpp` | dasselbe (`:554-556`, `:407-421`) — **keine Aenderung noetig** |
| `eq-copilot/plugin/src/PipeClient.cpp` | dieselbe Klassifikation (`:391-405`), Begruendung E4. Der Zaehler `:418` bleibt unberuehrt (E8) |
| `eq-copilot/plugin/tests/IpcTestMain.cpp` | D-K04, D-K05, D-K08, D-K10 bis D-K13, D-K16, D-K18, D-K19, D-K21, D-K22, D-K24, D-K25; D-P01 bis D-P08; D-A01, D-A02, D-A04, D-A05, D-A07, D-A08, D-A10, D-A11; E-Z01, E-Z02, E-Z04; E-V01, E-V02, E-V04 bis E-V06 |
| `eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp` | D-K03, D-K06, D-K10, D-K11, D-K17, D-K20, D-K23, D-K26; D-A03, D-A06, D-A09, D-A12; E-Z03, E-Z05 |
| `eq-copilot/plugin/tests/Sonde012SourcesModelTest.cpp` | E-V03 |
| `eq-copilot/plugin/tests/LebenslaufTestMain.cpp` | E-V05, E-Z06 |
| `eq-copilot/plugin/tests/SessionSoakMain.cpp` | W-01, W-02 und der Modus `selbsttest-zaehler` (die zwei reinen Zaehlerfunktionen samt Erzeugertests, §9) |
| `tools/eq-copilot/pruefe_session_soak.py` | W-01 (S04-Zeile, Mutant `p0_einreihung`) |
| `docs/beweise/G3-SOAK.md` | W-03, datierter append-only Nachtrag |
| `docs/offene-punkte.md` | zwei datierte Zeilen, beide **ausserhalb** von R1 und deshalb nicht mitgefixt: (1) v3 zaehlt `serverPruefungen` fuer Oeffnungsfehler **ohne** Handle als Serverpruefung (D-K21 bis D-K26, `ControlClient.cpp:1101-1103`), v2 nicht (`PipeClient.cpp:418`) — derselbe Feldname traegt auf den drei Pfaden zwei Bedeutungen; (2) `PipeClient::stop()` (`:249-260`) joint ohne Frist und setzt keine Zustandsfelder zurueck, sodass der Snapshot nach einem Stop waehrend der Oeffnungsschleife auf `verbindet` stehen bleibt (D-A03) — beide v3-Clients setzen dort `getrennt` |

**Nicht angefasst:** `broker/**` (NAK-135), Schemas, Fixtures, State, Audio-Thread-Code,
`docs/beweise/G3.md`, `docs/PLAN-STAND.md`. Kein Produktions-Pipename in irgendeinem Test.

## 13. Beweislaeufe Phase 2 — offen

| Lauf | Form | Erwartung | Ergebnis |
|---|---|---|---|
| (a) | `py -3.13 tools/eq-copilot/pruefe_session_soak.py --sonden 32 --minuten 4 --neustarts 2`, **vor** dem Produktfix, **mit** dem neuen Werkzeug | **ROT** (`k_s5.backoff_deckel_erreicht` unter 33; `reconnect_paare` unter 33) | `NOT RUN` |
| (b) | dieselbe Form **nach** dem Fix | GRUEN | `NOT RUN` |
| (c) | `--sonden 32 --minuten 30 --neustarts 3` (Gate-Form) | GRUEN, sonst §8 | `NOT RUN` |
| (d) | `--sonden 16 --minuten 2 --neustarts 1` (Kanon-Kurzform) | GRUEN | `NOT RUN` |
| Rotbeweis Test | `EqCopIpcTest` und `EqCopPipeClientTest` vor und nach dem Fix, beide Exitcodes | vorher rot an `pipe_busy`, nachher gruen | `NOT RUN` |
| Erzeugertest Werkzeug | `EqCopSessionSoak` Modus `selbsttest-zaehler`, vor und nach der Werkzeugaenderung | vorher rot an `reconnect_paare`, nachher gruen | `NOT RUN` |
| Berichtsmutanten | `--mutant p0_einreihung` und `--mutant s11_paare`, je mit Exitcode | beide ROT | `NOT RUN` |
| Beleg E-V08 | `git diff --stat 8e6a2ac..<Ende Phase 2>` | **keine** Datei unter `eq-copilot/schemas/`, `eq-copilot/plugin/state/`, `eq-copilot/fixtures/`, `broker/` | `NOT RUN` |
| Kanon | `tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-134.md -Anhaengen -Titel NAK-134` | GRUEN | `NOT RUN` |

Rohausgaben nach `docs/beweise/roh/NAK-134-<lauf>.txt`, Berichte als `.json` daneben.

## 14. Verlauf

### Phase 1 — Verhaltensmatrix vor Code, 2026-09-03

Basis `4789f2a`. Kein Code geaendert. Alle Quellenangaben in Abschnitt 2.1 an der Quelle
nachgelesen; die Zahlen in Abschnitt 2.2 aus dem Rohbericht gerechnet. Die zwei Win32-Zusagen in
Abschnitt 4.1 gegen die aktuelle Microsoft-Learn-Primaerquelle geprueft.

Matrixzeilen: **21** in Matrix D (Oeffnungsergebnis x Connectpfad), **8** in Matrix E
(Folgeverbraucher), **3** in Matrix W (Werkzeug) — zusammen **32**.

Drei Entscheide dieser Phase, die ueber den Auftragstext hinausgehen und dort begruendet stehen:
**E4** (der v2-`PipeClient` gehoert in den Aenderungssatz, sonst bricht NAK-123 C-06), **E5**
(`BrokerLifecycle` und `SourcesModel` brauchen keine Aenderung, bekommen aber je eine Matrixzeile
mit Test) und die Rotbeweis-Klassifizierung in Abschnitt 10 (drei der neuen Tests koennen
konstruktiv **nicht** vor dem Fix rot laufen; sie werden als Mutante belegt, nicht als
Vor-Fix-Rotlauf behauptet).

### Phase 1b — Matrix-Nacharbeit nach der Codex-Matrixpruefung 1, 2026-09-03

Basis `8e6a2ac`. Kein Code geaendert; Aenderungssatz sind `docs/beweise/NAK-134.md` und
`docs/beweise/roh/NAK-134-matrixpruefung-1-8e6a2ac.txt`.

Ausloeser ist das vollstaendige Urteil des lesenden Codex-Threads `gpt-5.6-sol` (high,
`01a06478-49e9-76b2-baa9-a3e8e4c202de`), **URTEIL NEEDS_WORK**, roh abgelegt unter
`docs/beweise/roh/NAK-134-matrixpruefung-1-8e6a2ac.txt`. Jede Datei-/Zeilenangabe des Urteils wurde
am HEAD `8e6a2ac` nachgelesen; keine Angabe war falsch.

**Was die neun Befunde geschlossen hat:**

| Befund | Geschlossen durch |
|---|---|
| 1 — D kreuzt nicht mit Erstverbindung, Reconnect, Deckel; `stop()`/`reconnect()` doppelt belegt | D ist in **D-K** (Klassifikation), **D-P** (Phasenwirkung, Ausgang x P-E/P-R/P-D) und **D-A** (Abbruch, Zeitpunkt x Vorgang x Pfad, 12 Zeilen) geteilt. Die Faktorisierung ist als **E9** am Code belegt: `backoffMs` ist lokal in `threadLauf` und im Oeffnungspfad nicht sichtbar |
| 2 — „offen und verifiziert" garantiert weder stehende Verbindung noch Backoff-Ruecksetzung | Neue Ausgangsklasse D-K04 bis D-K06 („verifiziert, Welcome bleibt aus") mit Rueckgabe `false`; die Backoff-Ruecksetzung haengt nachweislich an der Rueckgabe von `eineVerbindung`, nicht am Auth-Erfolg (D-P01, D-P02). Neue Spalte „Rueckgabe" in D-K |
| 3 — V2 erhoeht `verbindungsVersuche` bei jedem Eintritt, die Matrix liess das aus | E-Z03 als eigener Zaehlervertrag; alle v2-Zeilen in D-K tragen jetzt `verbindungsVersuche` +1 mit Quelle `PipeClient.cpp:347` |
| 4 — „unveraenderte `serverPruefungen`" ist fuer D-13..D-18 falsch | D-K21 bis D-K26 sagen jetzt, dass der Zaehler **heute schon steigt** und **weiter steigen wird**; E-Z04/E-Z05 fuehren die beiden verschiedenen Zaehlsemantiken (statusgesteuert v3, positionsgesteuert v2) getrennt. Die Aussage „D-13 bis D-18 aendern nichts" ist ersetzt |
| 5 — drei Testverweise messen etwas anderes; `erwartungUngueltig`/`serverPidNichtErmittelbar` fehlen; Backoff-Deckel ungemessen | Alle drei Fehlverweise sind in D-K03, D-K08 und D-A12 benannt und durch neue Tests ersetzt. D-K10 und D-K11 tragen die zwei fehlenden Ausgaenge (`authFehler` setzt immer `belegtAberUnverifiziert`, `BrokerLifecycle.cpp:391-400`). D-P und W-H3 messen den Deckel ueber beobachtete Versuchsstempel |
| 6 — W-Mutanten beweisen den Pruefer, nicht die C++-Erzeugung | §9 traegt je Zaehler **Erzeugertest C++ + Berichtsmutante**; der Erzeugertest laeuft im neuen `SessionSoakMain`-Modus `selbsttest-zaehler` gegen deterministische Eingaben, ohne Broker |
| 7 — „20 x 200 ms" ist keine harte Obergrenze | §7 nennt fuer `PIPE_BUSY` und fuer den Abbruch **untere Schranken**; die Tests messen `dauer >= 4.000 ms` plus den beobachteten Win32-Fehler, nicht eine Obergrenze |
| 8 — „sofort" und „bricht vor `namedPipeServerAuthentisieren` ab" sind falsch | §7 nennt einzeln, welche Zeilen vor und welche nach der Authentisierung abbrechen; D-A07 bis D-A12 halten fest, dass die Authentisierung nach einem erfolgreichen `CreateFileW` vollstaendig durchlaeuft und ihr Urteil danach verworfen wird. Die Wirklatenz von `ioAbbrechen()` ist als untere Schranke beschrieben |
| 9 — E-08 nur durch Scope-Inspektion gestuetzt | E-V08 belegt die Zusage durch die **Dateiliste** des Aenderungssatzes (`git diff --stat`) und nicht mehr durch gruene State-/Schema-Tests; der Lauf steht als eigener Beleg in §13 |

**Die zwei Luecken** sind als **R4** (lokaler Ressourcenfehler nach bestandenem Identitaetsbeweis)
und **R5** (Abbruch zwischen `CreateFileW`-Erfolg und Authentisierung) in §3.1 aufgenommen, je mit
dem am Code gelesenen Stand: R4 haelt heute in vier von fuenf Punkten, offen ist allein
`bericht.win32Fehler`; R5 beschreibt den heutigen Code vollstaendig, offen ist allein die Messung.
Die drei **Haertungen** des Pruefers sind als W-H1 bis W-H3 aufgenommen und als Haertung markiert.

Matrixzeilen dieser Fassung: **26** in D-K, **8** in D-P, **12** in D-A, **6** in E-Z, **8** in E-V,
**3** in W plus **3** Haertungen — zusammen **66**. Testkarte §10: **22** Zeilen.

Zwei Befunde dieser Fassung liegen **ausserhalb** von R1 und werden deshalb nicht mitgefixt,
sondern gehen datiert nach `docs/offene-punkte.md` (§12): die v3/v2-Asymmetrie bei
`serverPruefungen` fuer Oeffnungsfehler ohne Handle, und der fristlose `PipeClient::stop()` ohne
Zustandsreset, der den Snapshot nach einem Stop in der Oeffnungsschleife auf `verbindet` stehen
laesst.

Ein Zwischenstand dieser Fassung behauptete zusaetzlich, v2 lasse `serverPruefstatus` nach einem
ausgebliebenen Welcome auf `verifiziert` stehen. Das Selbstaudit vor dem Commit hat die Stelle
gelesen: v2 hat dafuer einen gemeinsamen Aufraeumpfad (`PipeClient.cpp:604-622`), der wie v3 auf
`nichtGeprueft` zuruecksetzt. Die Behauptung ist zurueckgenommen, D-K06 traegt den gemessenen Stand,
und aus dem geplanten Doppeleintrag in `docs/offene-punkte.md` wurde ein einzelner.
