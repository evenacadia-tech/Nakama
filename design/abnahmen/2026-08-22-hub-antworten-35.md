# 2026-08-22 — 35 Antworten des Users aus dem Hub

> ## Abnahme (bindend)
>
> Der User hat am 22.08.2026 zwischen 17:24 und 18:21 auf der Briefing-Seite
> **35 Fragen beantwortet** (Karten U2 · U5 · U6 · U9 · U10). Jede Antwort ist
> User-Wort. Wo er „Vorschlag übernehmen" gewählt hat, ist der im Hub
> vorgelegte Entwurfstext bindend; wo er „Alternative wählen" gewählt hat, ist
> der Entwurf **verworfen** — bindend ist dann nur so viel, wie der
> Alternativtext eindeutig sagt.
>
> Sein Wort zur Sache selbst (22.08., `U2.16`):
>
> > „Dein Wort vom 22.08.2026: Nakama-Design ist das gemeinsame verbindliche Depot."

Quelle: `docs/hub/hub.json` (`antworten`), abgeholt mit
`tools/hub/hub_sync.py holen`. Diese Datei ist die Übersetzung ins Design —
sie erfindet nichts dazu.

---

## 1. Drei Stellen, die nicht glatt abzuhaken sind

Diese drei zuerst, weil sie andere Punkte überschreiben:

### 1.1 `U2.1` schlägt `U6.1` — ein Zeichen für die Familie

Der User hat beide Fragen beantwortet, **sechs Minuten auseinander**, und die
Antworten zeigen in verschiedene Richtungen:

| | Zeit | Wahl | Bedeutet |
|---|---|---|---|
| `U6.1` | 17:53 | Vorschlag übernehmen | Apertur-Logik wie Probeeq, „der Widerspruch zu Gens Karte **bleibt**" |
| `U2.1` | 17:59 | Alternative wählen | „**Ein Zeichen für die ganze Familie**" — der Widerspruch wird aufgelöst |

**Auslegung:** `U2.1` ist später gegeben und ist die speziellere Aussage zur
Sache selbst (Gens Karte gegen Apertur). Es gilt: **ein Zeichen für die ganze
Familie.** Der in `U6.1` mitgenommene Halbsatz „der Widerspruch bleibt, Suna
vergrößert ihn nicht" ist damit überholt; die Apertur-Grammatik als solche
(gefüllt + Saum / gefüllt / hohl) bleibt bestätigt.

**Offen bleibt**, welches der beiden Zeichensysteme sich beugt: bekommt Gens
Karte ein neues Zeichen für „veraltet", oder ändert die Apertur ihre Bedeutung?
Das ist eine Design-Frage und geht als Folgefrage zurück auf die Seite.

### 1.2 `U9.5` überholt die Abnahme vom 20.08.

Wahl: **„Figma gilt"**. Damit ist das **Quellen×Band-Gitter** aus dem
Gen-Stand die Übersicht von Gen. Die Abnahme vom 20.08. („Overview + Detail
ohne Heatmap") ist **an dieser Stelle überholt** — nicht als Ganzes, aber in
dem Punkt, der das Gitter ausschloss.

### 1.3 `U9.1` setzt eine Rangfolge, die über U9 hinausreicht

Wörtlich (18:18):

> „figma ist keine technikreferenz, der implementplan ist es"

Das ist **keine Antwort auf Stelle 1 allein**, sondern eine allgemeine Regel:
Bei einer **technischen** Frage entscheidet der technische Entwurf
(`docs/FL-Nakama-Sonden-Design-Entwurf.md`), nicht der Figma-Stand. Für das
**Aussehen** bleibt Figma die Quelle (Designvertrag 21.08.) — die beiden Sätze
widersprechen sich nicht, sie teilen die Zuständigkeit.

Gehört ins Register in `CLAUDE.md`.

**Folge für Stelle 1 (Rollen 5 oder 7):** technisch gelten die **sieben Rollen**
des Entwurfs (§9). Die Oberfläche zeigt heute fünf Wörter (`U6.3`, `U2.9`
bestätigt als Vokabular). Ob fünf von sieben angezeigt werden oder die
Oberfläche auf sieben wächst, ist **nicht entschieden** — Folgefrage.

---

## 2. Gen — Studie 04 (Karte `U2`, 18 Antworten)

### Bindend übernommen (Wahl „Vorschlag übernehmen")

| Punkt | Was jetzt gilt |
|---|---|
| `U2.0` | **Entscheidungstor offen: JA.** Studie 04 wird weiterverfolgt; die Übersetzung ins lebende Blatt beginnt gegen den Figma-Export des Users. |
| `U2.3` | Befundort ist der **Ring mit Cyan-Saum** um die betroffenen Zellen. Die markierte Zeile existiert **nur, solange das Vorhören gehalten wird**. |
| `U2.5` | Wortlaut der Aktionen: **HOLD TO AUDITION · APPLY · REJECT** — ein Wortschatz für die Familie, aus Probeeqs Draft-Kasten. |
| `U2.7` | **SOURCES** im Kinn ist die Tür zu Quellenliste, Rollen, Suche, Pinning, Fokusgruppen. Die Tür ist gezeichnet, der Raum dahinter nicht. |
| `U2.10` | Energie der Zellen als **Ein-Farb-Rampe** der Probeeq-Kurvenfüllung: `#7E3D3E`, 0–55 % Deckkraft über dem Glas, dunkel → hell. |
| `U2.11` | Drei Sorten Zustand an drei Orten: degradierter Hinweis **oben rechts im Glas** · Messung **im Kinn links** · Ausnahmen je Quelle **im Kartenkopf rechts**. |
| `U2.12` | **Keine** FOCUS/BASS/180–260 HZ-Zeile; der Ort steht als Ring und in der PRIORITY-Zeile. Spalten in Kleinschreibung: **1k 2k 4k 8k**. |
| `U2.13` | Befund-Navigation läuft über **OBSERVE**; das Detail einer Quelle lebt im Schritt **Urteilen** — keine eigene Detailansicht daneben. |
| `U2.15` | Die **Shader-Füllung der Schale entfällt** (Lichtstreifen). Der Gen-Klon trägt sie nicht. |

### Bindend gedreht (Wahl „Alternative wählen") — eindeutig

| Punkt | Was jetzt gilt |
|---|---|
| `U2.2` | Die vier Ebenen (OBSERVE · ADVISE · AUDITION · APPLY) sitzen **als Tasten im Kinn rechts**, neben SOURCES und UNDO. Das Glas beginnt damit — wie bei Probeeq — **mit einer Identitätszeile**. |
| `U2.14` | Bei **16 Quellen keine Namen** in der Zeile (wie Abnahme 20.08.). Namen nur im Regelfall (einstellige Quellenzahl). |

### Gedreht, aber die Form ist offen

Hier hat der User den Entwurf verworfen; der Alternativtext bot **zwei**
Möglichkeiten. Verworfen ist bindend, die Form nicht — sie geht als Folgefrage
zurück auf die Seite. **Nichts davon wird hier geraten.**

| Punkt | Verworfen ist | Offen ist |
|---|---|---|
| `U2.4` | Kartenkopf nennt **nicht** Zahl **und** Name („1 STALE · SYNTH PAD") | nur die Zahl („1 STALE") — oder die Angabe ganz weg |
| `U2.6` | CERTAINTY **nicht** im Befundkopf rechts, OTHER CAUSE **nicht** als sechste gedimmte Zeile | anderer Ort auf der Fläche — oder eine der beiden Angaben einen Schritt entfernt |
| `U2.8` | **kein** schlichter Einsatz „UNDO · n" ohne Verlauf | ein echter Verlauf der angewandten Änderungen (wie Probeeqs Undo) — oder der Zähler an anderer Stelle |
| `U2.9` | Rollenwörter ersetzen **nicht** LIVE in der Zeile | LIVE behalten — oder gar keine Rolle in der Zeile (Rolle nur hinter der SOURCES-Tür) |

### Geklärt ohne Entwurfswahl

| Punkt | Was jetzt gilt |
|---|---|
| `U2.16` | Ein gemeinsames Figma-Depot **Nakama-Design** — steht seit 22.08. im Register. |
| `U2.17` | Skalierungsstufen: **100 %, 125 %, 150 % und 200 %.** Gebackene Teile werden in diesen vier Stufen aus dem Depot geholt. |

---

## 3. Suna (Karte `U6`, 10 Antworten)

### Bindend übernommen

| Punkt | Was jetzt gilt |
|---|---|
| `U6.1` | **Apertur-Logik wie Probeeq**: gefüllt + Leuchtsaum = verbunden und liefert · gefüllt ohne Saum = verbunden, Messung veraltet · hohl = kein Kontakt. (Der Zusatz „der Widerspruch bleibt" ist durch `U2.1` überholt — siehe 1.1.) |
| `U6.2` | Messposition als Dreistellung **PRE · INSERT · POST**. |
| `U6.3` | Rollenwörter **LEADS · CARRIES · SUPPORTS · PROTECTED · MERGED** (führt / trägt / begleitet / geschützt / bewusst verschmolzen). |
| `U6.7` | **Feste Spalten**: BUS 84 · ROLE 56 · POSITION 43 — Rolle und Position stehen über viele Kacheln untereinander. Der Preis (Lücke hinter kurzen Namen) ist angenommen. |
| `U6.10` | **Kein** Zustand „Name unbestätigt". Er ist nirgends beschrieben, und erfunden wird nichts. |

### Bindend gedreht

| Punkt | Verworfen ist | Offen ist |
|---|---|---|
| `U6.4` | **nicht** „MEASURING · STALE · 40 s" mit Alter als Zahl | Alters-Abstufung in Worten (seit 10 s / seit 10 min) — oder ganz ohne Alter |
| `U6.6` | Kopfzeile **PASSIVE PROBE fällt weg** (bei zwölf gestapelten Kacheln zwölfmal dasselbe) | wo der Hinweis dann steht — die Kopfzeile trug ihn rechts |

### Eigene Worte des Users

**`U6.5` — Hinweis-Wortlaut** (17:56):

> „so kurz wie möglich"

Richtung bindend: der Wortlaut wird gekürzt. Die vorgelegten Fassungen
(NOT DELIVERING TO GEN · DUPLICATE INSTANCE) sind damit **zu lang**. Der
konkrete kurze Wortlaut ist noch nicht gesetzt — Folgefrage mit einem knappen
Vorschlag (Produktsprache Englisch).

**`U6.8` — Schwebeschatten halbiert** (18:16):

> „macht kein sinn die frage"

**Kein Entscheid.** Die Frage wird verworfen, nicht die Sache. Sie war
schlecht gestellt: gefragt war, ob Sunas Schatten halb so groß sein darf wie
Probeeqs, weil Probeeqs Schatten sonst so groß wäre wie die Kachel selbst.
Wird neu und verständlich gestellt.

**`U6.9` — Wortmarke** (18:17):

> „alle wortmarken haben den stil wie ich ihn aus figma exportiert habe"

**Bindend und über Suna hinaus:** Maßgeblich für die Wortmarke ist **der
Figma-Export**, für alle drei Apps. Die Frage „Shader-Fassung oder flache
Fassung" erübrigt sich damit — es gilt, was im Export steht. Claude wählt hier
nicht aus, sondern holt.

---

## 4. Figma gegen technischen Entwurf (Karte `U9`, 5 Antworten)

| Punkt | Antwort | Was jetzt gilt |
|---|---|---|
| `U9.1` | „figma ist keine technikreferenz, der implementplan ist es" | **Rangfolge gesetzt** (siehe 1.3). Technisch: sieben Rollen. Anzeige offen. |
| `U9.2` | „das ist eine technische frage. wenn es kein problem ist dann ja" | **Bedingtes Ja** zur bedienbaren Suna-Kachel (Bus, Rolle, Position). Die Bedingung ist eine technische Bewertung, die noch **aussteht** — sie gehört zu NAK-38. Zu prüfen ist insbesondere, ob bedienbare Felder Suna zu Hostparametern zwingen und ob das mit „Suna berät nur" verträglich ist (Zuordnung ist kein Audio-Eingriff). |
| `U9.3` | „Später" · „frage nicht nachvollziehbar" | **Kein Entscheid.** Messzustände 5 gegen 2 — neu und verständlich stellen. |
| `U9.4` | „Später" · „verstehe frage nicht" | **Kein Entscheid.** Arbeitsschritte 3 gegen 5 — neu und verständlich stellen. |
| `U9.5` | „Figma gilt" | **Quellen×Band-Gitter ist die Gen-Übersicht** (siehe 1.2). |

---

## 5. Einzelne Karten

| Punkt | Antwort | Was jetzt gilt |
|---|---|---|
| `U5` | „Später" | Sidechain-Quelle je Band in Probeeq (sichtbar / Untermenü / unsichtbar) bleibt **offen**. Der gespeicherte Zustand trägt seit 22.08. alle dreizehn Parameter — unsichtbar kostet nichts, es eilt nicht. Bleibt NAK-33. |
| `U10` | **„Nein, nur mit Signal"** | **Technischer Entscheid mit Code-Folge:** Die Hör-Markierung ist **nicht** mehr erlaubt, wenn FL keine Transport-Information liefert. Das heutige „fail-open" im Code (`¬hatTransport` erlaubt die Markierung) wird auf die Zielfassung des Entwurfs gezogen: verlangt wird ein **gültiges „spielt"**. Betrifft NAK-35 und NAK-24, umzusetzen mit den Tickets S10–S13 (Messkern). |

---

## 6. Was daraus als Folgefrage zurück auf die Seite geht

Nicht geraten, sondern gefragt — je Punkt genau die eine offene Form:

1. `U2.1` — welches Zeichensystem beugt sich: neues „veraltet"-Zeichen in Gens Karte, oder neue Bedeutung der Apertur?
2. `U2.4` — nur die Zahl, oder die Angabe ganz weg?
3. `U2.6` — welcher andere Ort für CERTAINTY und OTHER CAUSE?
4. `U2.8` — echter Undo-Verlauf, oder nur der Zähler woanders?
5. `U2.9` — LIVE behalten, oder gar keine Rolle in der Zeile?
6. `U6.4` — Alter in Worten, oder ganz ohne Alter?
7. `U6.5` — kurzer Wortlaut für die zwei Hinweise (mit Vorschlag).
8. `U6.6` — wo steht der Hinweis, wenn die Kopfzeile wegfällt?
9. `U6.8` — Schwebeschatten, neu und verständlich gestellt.
10. `U9.1`-Folge — zeigt die Oberfläche fünf von sieben Rollen, oder wächst sie auf sieben?
11. `U9.3` — Messzustände, neu gestellt.
12. `U9.4` — Arbeitsschritte, neu gestellt.

## Bezug

- `abnahmen/2026-08-21-gen-auftrag-figma.md` — die 18 Lesarten (hier beantwortet)
- `abnahmen/2026-08-21-suna-auftrag-figma.md` — die 10 Lesarten (hier beantwortet)
- `abnahmen/2026-08-22-figma-depot.md` — Depot-Wort, auf das `U2.16`/`U2.17` aufsetzen
- `Nakama/docs/hub/hub.json` — Quelle der Antworten (`antworten`)
- `Nakama/docs/offene-punkte.md` — NAK-33 (`U5`), NAK-35/NAK-24 (`U10`), NAK-38 (`U9`)
