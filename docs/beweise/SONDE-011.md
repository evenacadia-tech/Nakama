# SONDE-011 — Phase A: v3-Vertragsrunde vor R0

| Merkmal | Wert |
|---|---|
| Ticket | S16–17 `SONDE-011` |
| Lieferumfang | Phase A — v3-Vertragsrunde vor R0 |
| Datum | 2026-08-31 |
| Basis | `b75ea06` |
| Prüfstufe | T1+T2 |
| Stand | Phase B: Matrix nach Prüferrunde 2 revidiert, in Wiederprüfung |

Dieses Manifest ist noch kein Umsetzungs- oder Abnahmeurteil. Die mit
**ZUSAGE** bezeichneten Regeln und die in §9 zusammengefassten
**ENTSCHIEDEN**-Festlegungen sind die bindende Referenz für Bauer und Prüfer.
Auftrag und Eigentum stehen in `docs/plan/plan.json:192-197`, insbesondere im
vollständigen S16–17-Text in `:195`.

## 1. Maß für Vertragsevolution und Beweis

`CLAUDE.md:123-125` setzt das Maß: Altstände müssen laden, Save und Load werden
gemeinsam geprüft, und Discriminator, Zieladresse, Revision und Capability sind
in v3 **nicht additiv** erweiterbar. Der v3-Vertrag ist standardmäßig strikt;
nur `zaehler`, `konfidenz` und `verteilung` sind ausdrücklich additive Objekte
(`eq-copilot/schemas/v3/README.md:298-315`). Deshalb bedeuten die Kürzel in den
Matrizen:

- **additiv**: alte Leser dürfen den neuen Anteil ignorieren und neue Leser den
  alten Anteil weiterhin lesen; ein neues FlatBuffers-Feld braucht trotzdem
  eine neue, nie wiederverwendete Feld-ID und eine höhere `schema_minor`.
- **Versionierung**: kein Minor-Nachtrag. Vor R0 darf der Dirigent die Änderung
  als neu gefassten anfänglichen v3-Vertrag abnehmen; nach R0 ist eine neue
  Major-Version nötig. „Vor R0 ersetzt“ heißt ausdrücklich nicht „additiv“.
- **keine Wire-Änderung**: bestehender Vertrag wird nur durch eine
  Lebenszyklusregel oder einen bereits vorhandenen Pfad präzisiert.

Die Vor-R0-Ausnahme ist hier real: Der C++-Client wartet noch auf Coordinator und
Landkarte (`eq-copilot/plugin/core/ipc/ControlClient.h:16-18`), und der heutige
v3-Server nennt den Coordinator ausdrücklich noch nicht gebaut
(`broker/src/transport/server_v3.rs:3-6,42`). Sie darf nach dem R0-Freeze nicht
als Begründung wiederverwendet werden.

### Verbindliche Beweisbein-Namen

| Bein | Was es misst |
|---|---|
| **A5** | Python-Referenz gegen JSON Schema, Engine-Teilmenge, Abdeckung und je Definition mindestens ein Negativfixture (`tools/beweise.ps1:307`). |
| **A8** | Fixture-Korpus und `MANIFEST.json` sind bytegleich regenerierbar und ohne verwaiste Datei (`tools/beweise.ps1:310`). |
| **B3c** | Der handgeschriebene C++-Leser `EqCopSchemaTest` klassifiziert Urteil **und** Verletzungsmenge wie das Manifest (`tools/beweise.ps1:422-424`). |
| **A4-Rust** | `cargo test`, insbesondere `broker/tests/contract_cross_language.rs`, klassifiziert denselben Korpus im Rust-Leser (`tools/beweise.ps1:285-290`; `eq-copilot/schemas/v3/flatbuffers/README.md:21`). |
| **A4-SI** | Neuer Rust-Server-Integrationstest unter A4 auf einem Probe-Pipenamen; misst Linkbesitz, Fremdadressablehnung und atomaren Cleanup. Diese Laufzeitbindung können A5/A8/B3c/A4-Rust-Fixtures nicht beweisen. |
| **A9/A10** | `flatc`-Codegen-/Driftwache sowie regenerierbarer FlatBuffers-Korpus (`tools/beweise.ps1:312-318`). |
| **B5** | Der bestehende FeatureEngine-Golden misst die sechs NAK-29-Erzeugergründe getrennt (`eq-copilot/plugin/core/analysis/FeatureEngine.h:185-246`). |
| **Q1** | Quelleninventar dieser Spezifikationsrunde: `plan.json`, `PR1.md`, Entwurf, Register und aktuelle Implementierung werden auf der Basis im Kopf zeilengenau gegeneinander gelesen. |

Ein Empfänger-Nachtest allein erfüllt keine Schema-Zusage. Jede aktive
JSON-Vertragsänderung muss in A5, A8, B3c und A4-Rust dieselbe Klassifikation
erreichen.

## 2. NAK-28 — Gegenpfad zu `subscribe_session`

Ausgangslage: §33.3 zählt derzeit 25 Familien
(`docs/FL-Nakama-Sonden-Design-Entwurf.md:1842-1859`), das Schema kennt
`subscribe_session` (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json:739-748`),
aber keinen Gegenpfad (`docs/offene-punkte.md:54`). Im aktuellen Server gibt es
noch keine Subscribe-Semantik; die Control-Verbindung besitzt jedoch bereits
die Kopplung. Ihr Ende entfernt den Registereintrag und bricht die gekoppelte
Telemetrie ab (`broker/src/transport/server_v3.rs:1579-1588,1770-1793`).

| Entscheid | ZUSAGE und technische Abwägung | Vertrags-/Entwurfsstellen | Evolution | Test/Beweisbein |
|---|---|---|---|---|
| **28-B — ENTSCHIEDEN: Verbindungsende** | Die Lebensdauer einer Subscription ist exakt die Lebensdauer ihres erfolgreichen `subscribe_session` auf **demselben** Control-Link und auf dessen gebundener Session/Adresse. Ein Control-Link darf weder für eine fremde Session noch für eine fremde effektive Adresse abonnieren. Jedes Control-Ende — EOF, Protokollfehler, Timeout, Writefehler oder Serverstopp — entfernt nur die diesem Link gehörenden Subscriptions atomar **vor** einem weiteren Push; Reconnect verlangt ein neues Subscribe. Das kostet keine Wire-Nachricht und wahrt verbinden↔trennen als einen Lebenszyklus. Selektives Abmelden verlangt dafür einen Link-Neuaufbau. | Bindender Satz für Entwurf §33.3 (`:1842-1859`); effektive Adresse §32.1 (`:1620-1630`) und `eq-ipc-v3.schema.json:62-77`; heutiger Besitz-/Cleanup-Pfad `server_v3.rs:1572-1588,1770-1808`; gekoppelte Lebenszyklen `CLAUDE.md:68`. | **Keine Wire-Änderung** und keine Familie `unsubscribe_session`. Mit `reference_match` endet §33.3 bei **26** Familien: 17 definiert + 9 reserviert. | A4-SI `subscription_ist_an_eigenen_control_link_gebunden`: Control-Link A sendet `subscribe_session` für Session/Adresse B und wird abgelehnt; gültige Subscription A bleibt beim Ende von B erhalten und wird ausschließlich beim Ende von A genau einmal und vor jedem späteren Push entfernt. A4-SI `subscription_cleanup_vor_weiterem_push` parametrisiert EOF, Protokollfehler, Timeout, Writefehler und Serverstopp. Q1 prüft, dass Schema und Reservierung kein `unsubscribe_session` enthalten. **A5/A8/B3c/A4-Rust-Fixtures messen diese Linkbindung ausdrücklich nicht.** |
| 28-A — VERWORFEN | `unsubscribe_session` hätte selektives Abmelden ermöglicht, aber einen nicht additiven Top-Level-Discriminator, eine 27. Familie und einen zweiten Cleanup-Auslöser geschaffen. Es ist keine Bauoption mehr. | Entwurf §33.3 (`:1842-1859`); Root-`oneOf` `eq-ipc-v3.schema.json:1-27`. | Nicht übernommen. | Q1 `kein_unsubscribe_session_name` schützt den Entscheid; es entstehen keine Positivfixtures dieser Familie. |

## 3. NAK-29 — bedingte Transportpflichten im Schema

### 3.1 Schemaform

**ZUSAGE:** `$defs.transportstempel` wird über `time_basis` und `$defs.cycle`
über `bounds_valid` als strikte diskriminierte Union formuliert. Die sechs
untenstehenden Relationen werden im Schema selbst abgelehnt, nicht erst in
einem Empfänger. Positive Gegenstücke bleiben gültig. Die bestehenden
Erzeugerriegel 1–6 bleiben bestehen (`FeatureEngine.h:209-232`). Fall 7 des
Erzeugers ist nicht Teil dieser Wire-Zusage; siehe §10.

Die gemeinsame Engine akzeptiert als Discriminatorwert der Instanz heute nur
einen String (`eq-copilot/schemas/v3/README.md:228-235`), während
`cycle.bounds_valid` ein Boolean ist (`eq-ipc-v3.schema.json:199-214`). Der
Bauer muss die Auswertung in Python, C++ und Rust deshalb auf String **und
Boolean** erweitern; fehlend oder ein anderer Typ ergibt weiterhin genau eine
`oneOf`-Verletzung. Für die beiden Beziehungen von `validity` zu Geschwistern
im Transportobjekt bezeichnet ein mit `/` beginnender
`x-nakama-discriminator` einen RFC-6901-Pfad, konkret
`/validity/cycle_bounds` und `/validity/continuous_time`. Bestehende einfache
Eigenschaftsnamen behalten ihre Bedeutung. Verschachtelte, nötigenfalls
duplizierte strikte Zweige drücken dann die Beziehungen aus; ein nur
handgeschriebener Nachtest außerhalb des Schemas erfüllt die Zusage nicht.

Die Änderung schränkt Dokumente ein, die der heutige Vertrag annimmt
(`docs/offene-punkte.md:55`), und braucht deshalb in jeder Zeile
**Versionierung** beziehungsweise die ausdrückliche Vor-R0-Neufassung.

### 3.2 Eine Aussage in JSON und FlatBuffers

**ZUSAGE:** Jede der sechs Relationen gilt nicht nur für das JSON-Schema,
sondern ebenso für `Transportstempel`/`Schleife` im Binärvertrag und wird in
**beiden** handgeschriebenen Binärlesern durchgesetzt. Das ist keine zweite
Semantik: `flatbuffers/README.md:174` verlangt ausdrücklich, dass die beiden
Darstellungen eine Aussage sind. Die heutigen Prüfstellen
`NakamaTelemetrie.cpp:181-250` und `broker/src/telemetrie.rs:355-420` prüfen die
sechs Kopplungen noch nicht; der Änderungssatz ergänzt beide und friert jede
Relation als regenerierbares A10-Binärfixture mit gleichem Urteil und
Verletzungspfad in B3c/A4-Rust ein. Die Binärfelder stehen in
`nakama_telemetry_v1.fbs:139-181`.

| Fall | ZUSAGE | Vertrags-/Entwurfsstellen | Additiv/Version | Test/Beweisbein |
|---|---|---|---|---|
| 29-1 Projektzeit | `time_basis=project_samples` verlangt `validity.project_time=true` **und** `project_sample_start`; binär gilt dasselbe für `zeitbasis`, das `project_time`-Bit in `gueltigkeit` und `project_sample_start`. | Entwurf §32.3 `:1677-1717`; JSON-Schema `:216-256`; FBS `:150-181`; beide Binärleser `NakamaTelemetrie.cpp:181-250`, `telemetrie.rs:355-420`; Erzeugerriegel 1 `FeatureEngine.h:211-215`. | **Versionierung** im JSON-Vertrag; im bestehenden Binärfeldsatz semantische Verschärfung und deshalb dieselbe Vor-R0-Neufassung. | A5/A8/B3c/A4-Rust: `ungueltig/transport-project-samples-ohne-project-sample-start.json`, `ungueltig/transport-project-samples-ohne-project-time.json`, positiv `gueltig/transport-project-samples.json`; A10/B3c/A4-Rust: `ungueltig/transport-project-samples-ohne-project-sample-start.bin` und `ungueltig/transport-project-samples-ohne-project-time-bit.bin`; B5 Grund 1. |
| 29-2 lokale Zeit | `time_basis=local_monotonic` verlangt `validity.project_time=false`; `project_sample_start` ist in JSON und Binärzweig verboten. | Entwurf §32.3 `:1712-1717`; FBS `nakama_telemetry_v1.fbs:150-181`; Binärleser `NakamaTelemetrie.cpp:181-250`, `broker/src/telemetrie.rs:355-420`; Erzeugerriegel 2 `FeatureEngine.h:216-217`; heutiger Erzeuger `:1819-1836`. | **Versionierung**: JSON-Einschränkung und binäre Semantikverschärfung brauchen dieselbe Vor-R0-Neufassung; nach R0 neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/transport-local-monotonic-mit-project-time.json`, `ungueltig/transport-local-monotonic-mit-project-sample-start.json`, positiv `gueltig/transport-local-monotonic.json`; A10/B3c/A4-Rust: `ungueltig/transport-local-monotonic-mit-project-time-bit.bin` und `ungueltig/transport-local-monotonic-mit-project-sample-start.bin`; B5 Grund 2. |
| 29-3 gültige Cycle-Grenzen | `cycle.bounds_valid=true` verlangt sowohl `start_ppq` als auch `end_ppq`; binär gilt das identisch für `schleife.bounds_valid`. Eine halbe Grenze ist nie gültig. | Entwurf §32.3 `:1692-1701,1741-1751`; JSON-Schema `eq-ipc-v3.schema.json:199-214`; FBS `nakama_telemetry_v1.fbs:139-148`; Binärleser `NakamaTelemetrie.cpp:181-250`, `broker/src/telemetrie.rs:355-420`; Erzeugerriegel 3 `FeatureEngine.h:218-220`. | **Versionierung**: JSON-Einschränkung und binäre Semantikverschärfung brauchen dieselbe Vor-R0-Neufassung; nach R0 neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/cycle-bounds-valid-ohne-start-ppq.json`, `ungueltig/cycle-bounds-valid-ohne-end-ppq.json`, positiv `gueltig/evidence-cycle-validiert.json`; A10/B3c/A4-Rust: `ungueltig/schleife-bounds-valid-ohne-start-ppq.bin` und `ungueltig/schleife-bounds-valid-ohne-end-ppq.bin`; B5 Grund 3. |
| 29-4 unbewiesene Ableitung | `cycle.bounds_valid=false` darf keine `derived_sample_bounds.derivation=validated_block_mapping` behaupten; binär darf `schleife.bounds_valid=false` keine `abgeleitete_grenzen.herleitung=validated_block_mapping` behaupten. Rohes PPQ darf getrennt davon vorliegen. | Entwurf §32.3 `:1728-1751`; JSON-Schema `eq-ipc-v3.schema.json:187-214`; FBS `nakama_telemetry_v1.fbs:79-83,129-148`; Binärleser `NakamaTelemetrie.cpp:181-250`, `broker/src/telemetrie.rs:355-420`; Erzeugerriegel 4 `FeatureEngine.h:221-222`. | **Versionierung**: JSON-Einschränkung und binäre Semantikverschärfung brauchen dieselbe Vor-R0-Neufassung; nach R0 neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/cycle-bounds-invalid-mit-validated-block-mapping.json`, positiv `gueltig/evidence-cycle-nur-ppq.json`; A10/B3c/A4-Rust: `ungueltig/schleife-bounds-invalid-mit-validated-block-mapping.bin`; B5 Grund 4. |
| 29-5 PPQ-Gültigkeitsbit | `validity.cycle_bounds=true` verlangt `cycle.start_ppq` und `cycle.end_ppq`; binär verlangt das `cycle_bounds`-Bit in `gueltigkeit` dieselben beiden `schleife`-Werte. Das Bit bescheinigt rohe PPQ-Werte, nicht die Sampleableitung. | JSON-Schema `eq-ipc-v3.schema.json:160-185`; Entwurf §32.3 `:1728-1746`; FBS `nakama_telemetry_v1.fbs:85-112,139-181`; Binärleser `NakamaTelemetrie.cpp:181-250`, `broker/src/telemetrie.rs:355-420`; Erzeugerriegel 5 `FeatureEngine.h:224-230`. | **Versionierung**: JSON-Einschränkung und binäre Semantikverschärfung brauchen dieselbe Vor-R0-Neufassung; nach R0 neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/validity-cycle-bounds-ohne-start-ppq.json`, `ungueltig/validity-cycle-bounds-ohne-end-ppq.json`, positiv `gueltig/validity-cycle-bounds-mit-ppq.json`; A10/B3c/A4-Rust: `ungueltig/validity-cycle-bounds-ohne-start-ppq.bin` und `ungueltig/validity-cycle-bounds-ohne-end-ppq.bin`; B5 Grund 5. |
| 29-6 kontinuierliche Zeit | `validity.continuous_time=true` verlangt `continuous_time_samples`; binär verlangt das `continuous_time`-Bit in `gueltigkeit` dasselbe optionale Feld. | Entwurf §32.3 `:1728-1746`; JSON-Schema `eq-ipc-v3.schema.json:160-185,216-256`; FBS `nakama_telemetry_v1.fbs:85-112,157-181`; Binärleser `NakamaTelemetrie.cpp:181-250`, `broker/src/telemetrie.rs:355-420`; Erzeugerriegel 6 `FeatureEngine.h:231-232`. | **Versionierung**: JSON-Einschränkung und binäre Semantikverschärfung brauchen dieselbe Vor-R0-Neufassung; nach R0 neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/validity-continuous-time-ohne-wert.json`, positiv `gueltig/validity-continuous-time-mit-wert.json`; A10/B3c/A4-Rust: `ungueltig/validity-continuous-time-ohne-wert.bin`; B5 Grund 6. |
| 29-E Enginekante | `x-nakama-discriminator` wertet `true` und `false` als zwei verschiedene Const-Zweige; Boolean wird nicht zu String oder Zahl konvertiert. Ein RFC-6901-Pfad wird segmentweise aufgelöst; fehlendes Segment ist dieselbe eine `oneOf`-Verletzung wie ein fehlender direkter Discriminator. | Enginevertrag `README.md:228-235`; Cycle-Feld `eq-ipc-v3.schema.json:202-208`; drei Verbraucher laut `README.md:15-26`. | Boolean-/Pfad-Unterstützung der Engine ist additiv; ihre Verwendung in den Zeilen 29-1 bis 29-6 bleibt versionierungspflichtig. | Neue direkte Tests `discriminator_boolean_true_false`, `discriminator_boolean_falscher_typ`, `discriminator_boolean_fehlt`, `discriminator_json_pointer_boolean` und `discriminator_json_pointer_segment_fehlt` in A5, B3c und A4-Rust; A8 friert die Fälle im gemeinsamen Manifest ein. |

## 3a. NAK-114 — `plugin_kind` × `measurement_position`

| ID | ZUSAGE | Vertrags-/Entwurfsstellen | Additiv/Version | Test/Beweisbein |
|---|---|---|---|---|
| 114-1 Klassenmatrix | Der v3-Vertrag übernimmt exakt die State-v2-Matrix: `main` darf nur `insert`; `legacy`, `passive_probe` und `active_probe` dürfen `insert`, `pre` und `post`; `post_fader_contribution` darf heute **keine** der vier Klassen führen. Die äußere, über `measurement_position` diskriminierte Union und ihre vier strikten Objektzweige bleiben erhalten. Innerhalb der Engine-Teilmenge wird `plugin_kind` je Zweig inline gekoppelt: `insert` erhält `{"type":"string","enum":["main","passive_probe","active_probe","legacy"]}`; `pre` und `post` erhalten `{"type":"string","enum":["passive_probe","active_probe","legacy"]}`; der Beitragszweig erhält `{"type":"string","enum":["main","passive_probe","active_probe","legacy"],"maxLength":0}`. Weil alle vier eingefrorenen Namen nichtleer sind, ist dieser Zweig für den heutigen Wortschatz bewusst unerfüllbar, ohne einen fünften Namen zu erfinden. `allOf`, `not`, eine leere `enum` oder ein unbekanntes Schlüsselwort werden nicht benutzt. | Eigentum/Befund `docs/offene-punkte.md:17`; State-Matrix `nakama-state-v2.md:45-54`; heutige freien `$ref` in `eq-ipc-v3.schema.json:476-574`; Engine-Schlüsselwörter und nichtleere `enum` `README.md:162-198`; Entwurf §32.2 `:1632-1667`. | **Versionierung**: die erlaubte Kombination zweier Vertragsachsen und der strikte, diskriminierte Descriptor sind nicht additiv. Vor R0 ersetzt dies die v3-Baseline; nach R0 braucht es eine neue Major-Version. | Der heutige Korpus enthält laut NAK-114 nur `passive_probe`. A5/A8/B3c/A4-Rust ergänzen `ungueltig/beitrag-plugin-kind-main.json`, `ungueltig/beitrag-plugin-kind-legacy.json`, `ungueltig/beitrag-plugin-kind-passive-probe.json`, `ungueltig/beitrag-plugin-kind-active-probe.json`, jeweils ansonsten vollständig und mit `contribution_aux=supported`, damit genau der Beitrags-Klassenriegel fällt; `ungueltig/probe-descriptor-main-pre.json` und `ungueltig/probe-descriptor-main-post.json` messen den übrigen `main`-Riegel. Positive Korpuslücke schließen mit `gueltig/session-main-insert.json`, `gueltig/session-active-probe-pre.json`, `gueltig/session-legacy-post.json`; `gueltig/session_snapshot.json` bleibt der positive `passive_probe`-Fall. Das heutige `gueltig/session-beitragsklasse.json` wird als `ungueltig/beitrag-plugin-kind-passive-probe.json` umklassifiziert, nicht als Positivbeleg stehen gelassen. |
| 114-2 Beitragszweig und Riegel | `probe_descriptor_beitrag` wird **nicht entfernt**. Der berechtigte Kern des Prüferbefunds wird präzise umgesetzt: Alle vier heutigen `plugin_kind` fallen in **diesem** Zweig an der State-v2-Matrix, der Zweig selbst bleibt aber Ausdrucksmittel des Vertrags für `post_fader_contribution`/`beitrag` nach §32.2. Zusätzlich bleibt D1 unabhängig scharf: der Zweig verweist auf `capabilities_beitrag` und verlangt `contribution_aux=supported`. Auch beide State-Riegel **innerhalb** `positionErlaubt` bleiben zu: Riegel 1 ist die `false`-Konstante `kContributionAuxVerfuegbar`, Riegel 2 der für alle vier Klassen geschlossene Beitragspfad im Klassenswitch. Im Strukturprüfer wird die handgeschriebene Klassenmatrix ergänzt: Er verlangt genau vier Root-Zweige, ihre `additionalProperties:false`, die exakten branchenspezifischen `plugin_kind`-Schemas und den unerfüllbaren Beitragsriegel. Beim Gleichheitsvergleich wird zusätzlich `plugin_kind` aus dem gemeinsamen Rumpf genommen; alle übrigen Pflichtfelder/Feldschemas bleiben identisch. Die Gegenprobe muss sowohl eine verdorbene Klassenmenge als auch einen gelockerten Beitragsriegel erkennen. | Bindender Ticketwortlaut `docs/plan/plan.json:195`; Wortschatz bleibt ausdrücklich `nakama-state-v2.md:76-90`; Beitragszweig/Capability `eq-ipc-v3.schema.json:119-145,465-574`; heutiger Zweigriegel `pruefe_v3_vertrag.py:506-598`; State-Riegel `NakamaState.cpp:342-358,387-415`; Entwurf §32.2 `:1662-1675`. | Derselbe **nicht additive** Vor-R0-Vertrag wie 114-1. Das Beibehalten eines heute unerreichbaren Zweigs ist keine neue Annahme und keine Capability-Erweiterung. Eine künftig erlaubte Trägerklasse braucht einen eigenen dokumentierten Vertragsentscheid und Versionierung. | A5-Strukturtests `probe_descriptor_plugin_kind_matrix_ist_exakt`, `probe_descriptor_beitragszweig_bleibt_vorhanden`, `beitragszweig_hat_keine_heutige_traegerklasse`, `beitragszweig_verlangt_contribution_aux_supported` sowie zwei Gegenproben `plugin_kind_matrix_drift_faellt` und `beitragsriegel_gelockert_faellt`. A5/A8/B3c/A4-Rust behalten `ungueltig/beitrag-ohne-contribution-aux.json`; nach 114-1 erwartet dessen Manifest **beide** unabhängigen Verletzungen (Klassenriegel und D1), damit D1 nicht von der Unerreichbarkeit verdeckt wird. C++-State-Test `post_fader_contribution_bleibt_fuer_alle_sondenklassen_gesperrt` misst beide State-Riegel. |

## 4. NAK-40 — nicht-hex32-State-ID auf die v3-Adresse abbilden

Der Host-State bewahrt eine Schema-1-`sensor_id` als `instance_id` bytegleich,
auch wenn sie kein hex32 ist (`eq-copilot/schemas/state/nakama-state-v2.md:96-114`);
der bestehende Golden enthält etwa
`11111111-2222-3333-4444-555555555555`
(`eq-copilot/plugin/tests/StateMigrationTestMain.cpp:139,630`). Die v3-Adresse
fordert dagegen hex32 (`eq-ipc-v3.schema.json:31-37,62-77`), ebenso der
Rust-Bootstrap (`broker/src/transport/bootstrap.rs:144-163`) und der C++-Client
(`eq-copilot/plugin/core/ipc/ControlClient.h:35-55`;
`ControlClient.cpp:121-135,471-476`). Der Telemetrieclient verwendet dieselbe
Adresse und denselben Riegel (`TelemetryClient.h:36-42`;
`TelemetryClient.cpp:348-369`).

| Richtung/Fall | ENTSCHIEDENE ZUSAGE | Vertrags-/Entwurfsstellen | Additiv/Version | Test/Beweisbein |
|---|---|---|---|---|
| State → Wire, bereits hex32 | Entspricht der gespeicherte Wert exakt `[0-9a-f]{32}`, ist die Wire-`instance_id` bytegleich derselbe Wert. Keine Kleinschreibung oder sonstige Normalisierung. | State-Vertrag `nakama-state-v2.md:17,109`; Adresse `eq-ipc-v3.schema.json:31-37,74`. | **Versionierung**: Bedeutung der Zieladresse ist nicht additiv; vor R0 als v3-Baseline, danach neue Major-Version. | Neuer gemeinsamer Vektorkorpus `eq-copilot/fixtures/v3/instance-address-alias-v1.json`; Vektor `0123456789abcdef0123456789abcdef → identisch`; B3c und A4-Rust. |
| State → Wire, sonst | `alias = lower_hex(first_16_bytes(SHA-256(concat(UTF8("evenacadia.nakama.v3.instance-address.v1"), 0x00, u64be(len(exact_utf8(instance_id))), exact_utf8(instance_id)))))`. Keine Unicode-Normalisierung, kein Case-Folding. Eingefrorener Vektor: `11111111-2222-3333-4444-555555555555 → 63de6caeedaa39f91a6e35a64de7fd7d`. Ein leerer Schema-1-Wert nimmt weiter den vorhandenen Neu-UUID-Pfad und wird nicht gehasht. | NAK-40 `docs/offene-punkte.md:66`; bytegleiche Migration und Leerwertregel `nakama-state-v2.md:109-112`; C++-/Rust-Adressprüfer oben. | Versionierung wie vorige Zeile. Die Abbildung ändert **nicht** das State-Schema. | B3c/A4-Rust `instance_address_alias_vectors`; Vektoren für Bindestrich-UUID, Groß-/Kleinschreibung und nicht-ASCII sowie `instance_address_empty_uses_fresh_uuid`. A5/A8 prüfen weiterhin, dass auf dem Wire nur hex32 akzeptiert wird. |
| Wire → State/Instanz | Der Alias ist nicht reversibel und wird außerhalb der besitzenden Plugininstanz nicht als State-ID ausgegeben. Broker und Gen führen die Wireadresse; der Zielclient berechnet sie aus seiner eigenen gespeicherten `instance_id` erneut und akzeptiert nur die exakte Übereinstimmung. Niemand dekodiert oder errät eine Original-ID, und der Alias wird nie in den Host-State zurückgeschrieben. Das ist der dokumentierte Rückweg: Vergleich mit der lokalen Originalwahrheit, keine Hash-Umkehr. | Effektive Adresse Entwurf §32.1 `:1606-1630`; State-Lade-/Roundtrip-Regel `nakama-state-v2.md:10,109-114`; C++-Adressgrenze `ControlClient.cpp:121-135,471-476`. | Versionierung der Adresssemantik; Save+Load gemeinsam. | Neue Tests `instance_alias_target_recomputed_and_matched`, `instance_alias_unknown_fail_closed` und `legacy_instance_id_save_load_bytegleich`. Der C++-State-Test lädt, adressiert, speichert, lädt erneut und vergleicht die Originalbytes; A4-Rust beweist, dass ein unbekannter Alias keinen Dispatch erhält. |
| Kollision | Treffen native hex32 und Alias oder zwei verschiedene Originalwerte im selben Adressraum auf denselben Wirewert, wird **für beide Seiten keine Zuordnung gewählt**. Die Kollisionsregistrierung quarantänisiert unter demselben Register-Lock atomar sowohl den neu eintreffenden als auch den zuerst registrierten Eintrag, entfernt eine schon veröffentlichte Aliasauflösung vor Rückkehr aus dem Registrierungsschritt und sperrt Dispatch, Telemetrie und Session-Push für beide. Der erste Eintrag darf also nicht weiterlaufen, nur weil er zeitlich zuerst kam. Der sichtbare Konflikt bleibt bis zur expliziten Neu-ID-Auflösung bestehen; Reihenfolge und Ursprungstyp ändern das Urteil nicht. | Dirigentenentscheid dieser Matrixnacharbeit; Duplikatregel `nakama-state-v2.md:154-156`; bestehender Duplicate-Pfad `StateMigrationTestMain.cpp:1157-1172`; Adressform Entwurf §32.1 `:1606-1630`. | Keine weitere Formatänderung; Teil derselben **versionierten, fail-closed Adresssemantik**. | Deterministisch injizierte A4-Rust-Registertests `instance_alias_collision_native_dann_abgeleitet_quarantaenisiert_beide` und `instance_alias_collision_abgeleitet_dann_native_quarantaenisiert_beide`; zusätzlich parametrisiert `instance_alias_collision_zwei_ableitungen_quarantaenisiert_beide` für A→B und B→A. Jeder Test prüft nach dem zweiten Registerschritt: kein Aliasindexeintrag, beide Identitäten sichtbar quarantänisiert und kein Dispatch/Push an die zuerst oder zuletzt registrierte Seite. |

Die Original-ID bleibt damit die Save-/Load-Wahrheit; der hex32-Alias ist nur
eine abgeleitete Wireadresse. Ein zusätzlich persistiertes Aliasfeld wird nicht
eingeführt und kann daher nicht gegen die Original-ID driften.

## 5. NAK-59 — Band-Stereo

Die FeatureEngine liefert bereits den Seitenanteil `S/(M+S)` je Live-Band samt
Bitmap (`eq-copilot/plugin/core/analysis/FeatureEngine.h:343-366`), während
`Frame` nur die Felder mit IDs 0–9 besitzt
(`eq-copilot/schemas/v3/flatbuffers/nakama_telemetry_v1.fbs:216-226`;
`FELD-IDS.json:74-87`). Die beiden handgeschriebenen Leser sind
`eq-copilot/plugin/vertrag/NakamaTelemetrie.cpp` und
`broker/src/telemetrie.rs` (`flatbuffers/README.md:15-23`), konkret die
Frameprüfungen in `NakamaTelemetrie.cpp:253-384` und
`broker/src/telemetrie.rs:316-353`; der offene Vertragspunkt steht in
`docs/offene-punkte.md:43`.

| Entscheid | ZUSAGE und Abwägung | Vertrags-/Entwurfsstellen | Evolution | Test/Beweisbein |
|---|---|---|---|---|
| **59-A — ENTSCHIEDEN: Wirefeld** | Neues optionales Frame-Feld `band_stereo:Bandwerte (id: 10)`. Wenn vorhanden: Gitter `nakama_log64_v1`, Encoding `float32`, genau 64 Werte, passendes 8-Byte-Bitmap, kein `werte_i16`, jeder gesetzte Wert endlich und in `[0,1]`, `saturated=false`. Neue Sender senden es mit der erhöhten Vertrags-Minor-Version; neue Leser akzeptieren alte Frames ohne Feld. | Entwurf §31.2 `:1546-1554`, §56 `:3879-3898`; FBS `:60-70,189-205,216-226`; ID-Disziplin `flatbuffers/README.md:31-49`; FeatureEngine `:350-366`; Envelope-Schreiber `broker/src/transport/v3.rs:1-12,324-350`. | **Additiv**, weil optional am Tabellenende; `schema_minor` steigt, ID 10 wird in `FELD-IDS.json` eingefroren und nie umgewidmet. FBS, ID-Register, `flatc`-Codegen, **beide** handgeschriebenen Leser und Sender-Minor gehören in denselben Änderungssatz. | A9 `flatc`- und ID-Drift. A10 positiv: `gueltig/band-stereo-fehlt-altframe.bin`, `gueltig/band-stereo-bitmap-alle-werte-ungueltig.bin`, `gueltig/band-stereo-wert-0.bin`, `gueltig/band-stereo-wert-1.bin`; A10 negativ: `ungueltig/band-stereo-nan.bin`, `ungueltig/band-stereo-unter-0.bin`, `ungueltig/band-stereo-ueber-1.bin`, **`ungueltig/band-stereo-saturated-true.bin`**, `ungueltig/band-stereo-falsches-gitter.bin`, `ungueltig/band-stereo-falsches-encoding.bin`, `ungueltig/band-stereo-werte-i16-gesetzt.bin`, `ungueltig/band-stereo-laenge-falsch.bin`, `ungueltig/band-stereo-bitmap-laenge-falsch.bin`. B3c/A4-Rust liefern gleiches Urteil und Verletzungspfad. C++-Roundtrip `band_stereo_featureframe_to_flatbuffer`; Sender-Integrationstest `band_stereo_sender_emittiert_erhoehten_schema_minor` erzeugt über den echten Sendepfad einen Frame, liest im vollständigen Wireframe Offset 7 (Offset 3 des 16-Byte-Kopfs nach `frame_len`) zurück und vergleicht ihn mit der für ID 10 festgelegten Minor-Version — ein bloßer Direktaufruf von `envelope_schreiben` mit einem Testwert genügt nicht. |
| 59-B — VERWORFEN | Die Festhaltung „bleibt rein lokal“ hätte null Wirekosten, aber die bereits berechnete Größe Broker und Gen vorenthalten. Sie ist keine Bauoption mehr. | Entwurf §56 `:3889`; FeatureEngine `:350-364`; heutiger Frame `fbs:216-226`. | Nicht übernommen. | Q1 stellt sicher, dass §56 nach Umsetzung nicht „rein lokal“ behauptet; A9 muss ID 10 sehen. |

## 6. NAK-10 — `hoermarkierung` wird gelesen

Die ältere Entwurfsnotiz behauptet noch, Rust übernehme das Flag nicht
(`docs/FL-Nakama-Sonden-Design-Entwurf.md:1570-1572`). Der aktuelle Code hat
diesen Teil bereits überholt: `MessStand` des v2-Eingangs trägt
`hoermarkierung` (`broker/src/protokoll.rs:128-135`), und das Register wertet es
mit einer bis zum beobachteten Reset fail-closed bleibenden Sperre aus
(`broker/src/lib.rs:306-380`; bestehende Tests `:1150-1220,1247-1293`). Der
aktuelle Codebeleg geht der älteren beschreibenden Notiz und dem entsprechend
veralteten Registertext (`docs/offene-punkte.md:48`) vor.

| ID | ZUSAGE | Vertrags-/Entwurfsstellen | Additiv/Version | Test/Beweisbein |
|---|---|---|---|---|
| 10-1 | Der SONDE-011-Coordinator führt genau **einen** Interventionszustand: eine Menge sauber verfolgter aktiver `intervention_id` plus das sticky Bit `intervention_state_unknown`. Legacy-v2 `measurement.hoermarkierung=true/false` und v3 `audible_intervention_begin/end` mit `art=hoermarkierung` speisen ihn vor jedem Evidenzdispatch. Ein passendes End beendet nur seine eigene sauber verfolgte ID; bei Überlappung bleibt gesperrt, bis alle IDs beendet und ihre `tail_samples` abgelaufen sind. Ein End mit fremder ID ändert nichts. **Ring-Overflow, Control-Disconnect oder eine Eventsequenzlücke setzen sticky `intervention_state_unknown`**, quarantänisieren/invalidieren den betroffenen Timelinebereich und sperren starke Evidenz. Danach entsperren weder ein passend aussehendes End noch v2-`false` noch ein Session-Ende: ausschließlich ein **expliziter Neutral-/Sequenz-Resync** mit bestätigter neuer Sequenzbasis darf das Sticky-Bit löschen. Ein verlorenes Begin oder End kann so nie eine saubere Baseline vortäuschen. | Entwurf §31.2 `:1546-1572`, **§34.2 `:1968-1981`**, §56 `:3879-3898`; Heartbeat-Sticky-Semantik `eq-ipc-v3.schema.json:706-721`; v3 Begin/End, IDs, Sequenz und Nachlauf `:819-854`; heutiger v2-Pfad `protokoll.rs:128-135`, `lib.rs:306-380`. | **Keine neue Wire-Form**: v2-Flag, v3-Begin/End und das optionale Heartbeat-Bit existieren. Der explizite Resync ist eine Coordinator-Lebenszyklusoperation und darf nicht aus einem normalen End/`false` inferiert werden; diese Matrix erfindet dafür keine neue Familie. Eine spätere neue Wirefamilie oder ein neues striktes Objektfeld wäre versionierungspflichtig. | A5/A8/B3c/A4-Rust halten Begin/End samt Begin-`art` gültig und unbekannte Arten ungültig. A4-Rust: `hoermarkierung_v2_v3_gleicher_zustand`, `hoermarkierung_vor_evidence_dispatch`, `intervention_overflow_setzt_sticky_unknown`, `intervention_control_disconnect_setzt_sticky_unknown`, **`intervention_sequenzluecke_setzt_sticky_unknown`**, **`intervention_neutral_resync_entsperrt`**, **`intervention_ueberlappung_bleibt_bis_alle_end`**, **`intervention_falsche_end_id_bleibt_gesperrt`**, `hoermarkierung_tail_sperrt_bis_ende`. Jeder Sticky-Test sendet danach zusätzlich ein passendes End beziehungsweise v2-`false` und beweist, dass dies **nicht** entsperrt; nur der Resync-Test darf starke Evidenz wieder zulassen. |

## 7. Fünf Namensreservierungen ohne Nutzlast

Die bindende Reservierungsregel aus
`eq-copilot/schemas/v3/reservierte-nachrichten-v1.json:5` lautet wörtlich:

> Ihre NAMEN sind trotzdem jetzt vergeben. Ein reservierter Discriminator wird vom v3-Parser ABGELEHNT (er steht nicht im oneOf des Schemas) und darf von keinem spaeteren Ticket fuer etwas anderes verwendet werden. Genau das ist der Vertragsanteil, der spaeter bricht, wenn man ihn heute offen laesst.

Heute sind 17 Familien definiert und 8 reserviert, Summe 25
(`reservierte-nachrichten-v1.json:4,16-77`). Die folgende Zählung ist kumulativ
in Gruppenfolge (a) bis (e). Feldnamen ändern die Familienzahl nicht;
`reference_match` erhöht die reservierten Familien auf 9 und die Summe auf 26.
Eine Reservierung fügt **keine** Nutzlast und keine Annahme ins aktive Schema
ein. A5 muss zusätzlich prüfen, dass jeder reservierte Feldname am strikten
Elternobjekt und jeder reservierte Familienname am Root heute abgelehnt wird.

| Gruppe / konkreter Name | ZUSAGE der Reservierung | Betroffene Stellen | Familienzahl nach Zeile | Additiv/Version bei späterer Einlösung | Riegel/Test |
|---|---|---|---:|---|---|
| (a) `session_snapshot.contribution_inputs` | Reservierter Feldname für später genau einen Deskriptor je diskretem Gen-Eingang. Er ist eine Gen-Eingangsachse, **kein** `probe_descriptor.measurement_position` einer Sondeninstanz. Empfänger ist Gen; Nutzlast und Aux-Bus gehören SONDE-018. Der bestehende `probe_descriptor_beitrag` bleibt daneben als Ausdrucksmittel des v3-Vertrags erhalten, ist nach NAK-114 aber von keiner heutigen Klasse führbar. | Plan S16–17 `plan.json:195`; Entwurf §32.2 `:1632-1675`; `session_snapshot` `eq-ipc-v3.schema.json:750-770`; NAK-79 `docs/offene-punkte.md:34`; NAK-114 §3a. Die Riegel bleiben zu: `capabilities_beitrag`, Capabilitykonstante `NakamaState.cpp:342-358` und Klassenmatrix `:387-415`. | 25 | Reservierung selbst ohne Wirewirkung. Einlösung im strikten `session_snapshot` ist **nicht additiv**: vor R0 einbauen oder neue Major-Version. Sie öffnet den Instanzzweig nicht. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-session-snapshot-contribution-inputs.json`. A5-Strukturriegel und die vier NAK-114-Negativfixtures beweisen, dass der Descriptorzweig vorhanden, aber für jede heutige Klasse geschlossen ist. C++-State-Test `post_fader_contribution_bleibt_fuer_alle_sondenklassen_gesperrt` prüft beide State-Riegel. |
| (b1) `state_report.dsp` | Reservierter Feldname für den bestätigten DSP samt Schutzgrenzen. Nur der Name; DTO/Nutzlast entsteht S26–28. | Entwurf §33.5 `:1892-1920`; heutiges striktes `state_report` `eq-ipc-v3.schema.json:1023-1046`; NAK-110 `docs/offene-punkte.md:23`; Plan `plan.json:195,280`. | 25 | Einlösung ist im strikten Objekt **nicht additiv** und gemeinsam mit DSP-State-Save/Load zu versionieren. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-state-report-dsp.json`; A5-Riegel `reserviertes_feld_hat_keine_nutzlast`. Späterer Eigentümertest: Reload/Reconnect liest erst `state_report.dsp`, nie blind Broker-Cache. |
| (b2) `command_ack.applied_dsp` | Reservierter Bestätigungsname für die **tatsächlich** angewandten Werte einer `apply_transaction`. Später nur in den Erfolgszweigen `angewandt` und `idempotent_wiederholt`; keine Nutzlast wird hier vorweggenommen. | Entwurf §33.4 `:1861-1890`; `command_ack`-Union `eq-ipc-v3.schema.json:951-1021`; reservierte Familie `apply_transaction` `reservierte-nachrichten-v1.json:46-50`; Plan `plan.json:195`. | 25 | Einlösung ändert die strikten, über `ergebnis` diskriminierten ACK-Zweige und ist **nicht additiv**; vor R0 oder neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-command-ack-applied-dsp.json`; später positive Erfolgsfixtures und negative Ablehnungs-/Konfliktfixtures `command-ack-applied-dsp-bei-nichterfolg`. |
| (c) `state_report.eq_enabled` | Reservierter Feldname für den Betriebszustand des Sonden-EQ. Eigentümer S29–31. Der Name verspricht ausdrücklich **keine** Anzeige auf Gens Landkarte. | Plan S16–17 `plan.json:195`, Berichtigung S18–19 `:218`; heutiges `state_report` `eq-ipc-v3.schema.json:1023-1046`; NAK-107 `docs/offene-punkte.md:26`. | 25 | Einlösung im strikten Objekt ist **nicht additiv**; vor R0 oder neue Major-Version. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-state-report-eq-enabled.json`; A5 prüft Scope-Kollision mit `state_report.dsp`. Späterer Eigentümertest `eq_enabled_roundtrip_ohne_ui_zusage` misst nur Fernsteuerzustand. |
| (d1) `probe_descriptor.host_bus_name` | Reservierter optionaler Feldname. Fehlt er später, lautet die einzige Aussage „der Host liefert keinen Wert“; `label` bleibt gespeichertes, untrusted User-Wort. Typ, Länge und FL-Befüllung werden nicht vorweggenommen. | Plan S16–17 `plan.json:195`, S18–19 `:218`; strikte Descriptor-Union `eq-ipc-v3.schema.json:465-574`; NAK-106 `docs/offene-punkte.md:25`. | 25 | Die Reservierung verhindert Umwidmung, macht die spätere Einlösung aber **nicht additiv**: alle vier strikten Descriptorzweige und alle Leser brauchen eine versionierte Änderung. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-probe-descriptor-host-bus-name.json`; positives bestehendes Descriptorfixture ohne Hostfelder bleibt gültig. S18–19 misst separat `fl_track_properties_bus_name_present_or_absent`. |
| (d2) `probe_descriptor.host_mixer_index` | Reservierter optionaler Feldname. Fehlen heißt „der Host liefert keinen Wert“; daraus darf weder Verbindungsreihenfolge noch eine erfundene Mixernummer abgeleitet werden. Typ/Bereich und FL-Befüllung bleiben S18–19. | Dieselben Descriptor-/Planstellen wie (d1), insbesondere `plan.json:218`. | 25 | Wie (d1): Reservierung ja, spätere Einlösung **nicht additiv** und versionierungspflichtig. | A5/A8/B3c/A4-Rust: `ungueltig/reserviertes-feld-probe-descriptor-host-mixer-index.json`; positives Fixture ohne Wert; S18–19 `fl_track_properties_mixer_index_present_or_absent`. |
| (e) `reference_match` | Reservierter Top-Level-Familienname für Referenz-Nachbilden/EQ-Match. Nur Zweck und Name; kein Request-/Result-/Payloadmodell. Ausbau bleibt laut U19 hinten. | Plan S16–17 `plan.json:195`; Entwurf §47.5 `:3044-3059`; Root-Discriminator `eq-ipc-v3.schema.json:1-27`. | **26** = 17 definiert + 9 reserviert | Die Reservierung selbst bleibt abgelehnt. Spätere Aktivierung eines Top-Level-Discriminators ist **nicht additiv** und braucht eine neue Major-Version, sofern sie nicht vor R0 vollständig definiert wird. | A5 prüft disjunkte Namen und neue Summe 26; A8 erzeugt `ungueltig/reservierter-typ-reference-match.json`; B3c/A4-Rust müssen ihn mit `oneOf` ablehnen. Test `reservierter_name_reference_match_wird_nicht_umgewidmet`. |

Damit gilt nach dem Entscheid 28-B verbindlich: Das Verbindungsende ist der
Gegenpfad, `unsubscribe_session` entsteht nicht und die Gesamtsumme ist mit
`reference_match` **26**. Der bisherige A5-Riegel „Summe 25“
(`reservierte-nachrichten-v1.json:6`) muss bei Umsetzung auf 26 geändert werden;
eine bloße Änderung der Zahl ohne Disjunktheits-, Ablehnungs- und Fixturebeweis
genügt nicht.

## 8. Abschlussprüfung gegen PR1 und Plan

| Prüfung | Ergebnis/ZUSAGE | Fundstellen | Additiv/Version | Test/Beweisbein |
|---|---|---|---|---|
| Zusätzlicher zugewiesener Vertragspunkt | **NAK-114 gefunden und in §3a aufgenommen.** Das Register weist die fehlende Kopplung `plugin_kind` × `measurement_position` ausdrücklich S16–17 zu. Sie ist keine Begründung, `probe_descriptor_beitrag` zu entfernen, sondern schließt dessen heutige Trägerklassen fail-closed nach der bereits bindenden State-v2-Matrix. | `docs/offene-punkte.md:17`; `nakama-state-v2.md:45-54,76-90`; bindender Beitragswortlaut `docs/plan/plan.json:195`; Matrixgruppe §3a dieses Manifests. | **Versionierung**, weil der strikte diskriminierte Descriptor und die erlaubten Achsenkombinationen geändert werden. | Q1 plus sämtliche in §3a benannten A5/A8/B3c/A4-Rust-Struktur-, Positiv- und Negativbeine. |
| Weitere ausdrücklich dieser Runde zugewiesene Vertragspunkte | **Geprüft, nach Aufnahme von NAK-114 keine weiteren unbedingten Punkte.** PR1 weist NAK-10/28/29/40/59 der Runde zu und seine damalige Reservierungstabelle enthält (a) bis (d); der jüngere S16–17-Text ergänzt (e). Der laufende Überdeckungswert aus U16 ist **noch kein reservierbarer Vertragsname**: S23–25 entscheidet erst am Datenweg, ob überhaupt ein eigenes v3-Feld nötig ist. Nur falls dort „ja“ belegt wird, muss der Name vor R0 in diese Vertragsrunde zurückkehren; er darf nicht still später angefügt werden. | `docs/beweise/PR1.md:254-258,575-583,898-904`; `docs/plan/plan.json:195,254-259`, besonders die Bedingung in `:257`; Registerprüfung `docs/offene-punkte.md:11-29`. | Heute keine weitere Wire-Änderung. Ein später bejahter Name wird nach seinem Ort mit dem strikten Elternobjekt beziehungsweise Discriminator klassifiziert und ist nicht automatisch additiv. | Q1 auf Basis `b75ea06`. Kein A5/A8/B3c/A4-Rust-Fixture für U16, solange Quelle, Ort und Name nicht entschieden sind; ein erfundenes Fixture würde gerade die offene Datenwegentscheidung vorwegnehmen. |

## 9. Gefallene Dirigentenentscheidungen

1. **NAK-28 — ENTSCHIEDEN 28-B:** Das Verbindungsende ist der einzige
   Gegenpfad von `subscribe_session`; keine Familie `unsubscribe_session`.
   Linkbesitz wird im Server-Integrationstest bewiesen. Endsumme mit
   `reference_match`: **26**.
2. **NAK-59 — ENTSCHIEDEN 59-A:** `band_stereo` wird optionales
   FlatBuffers-Framefeld mit ID 10 und erhöhter Minor-Version. Schema,
   ID-Register, `flatc`-Codegen, beide Leser, Sender und Drift-/Binärbeweise
   bilden einen Änderungssatz.
3. **NAK-40 — ENTSCHIEDEN:** Gültiges hex32 bleibt identisch; sonst gilt die
   domain-separierte, längengebundene SHA-256/128-Abbildung ohne
   Normalisierung. Unbekanntes und jede Kollision sind fail-closed; eine
   Kollision quarantänisiert atomar **beide** Seiten, einschließlich der zuerst
   registrierten, in beiden Registrierungsreihenfolgen.
4. **Beitragszweig — ENTSCHIEDEN:** `probe_descriptor_beitrag` bleibt als
   Ausdrucksmittel des Vertrags. NAK-114 koppelt die vier heutigen
   `plugin_kind` an die State-v2-Positionsmatrix, sodass keine heutige Klasse
   `post_fader_contribution` führen kann; `capabilities_beitrag` und beide
   State-Riegel bleiben zusätzlich geschlossen.

## 10. Bewusst nicht aufgenommen

- **Broker-Autostart/Beenden/Installer-Rückweg** steht zwar im selben S16–17-Text
  (`plan.json:195`), ist aber Phase B und kein Vertragsmatrix-Anteil dieses
  ausdrücklich auf Phase A begrenzten Spezifikationsschritts.
- **Nutzlasten** der sieben konkreten Reservierungsnamen sind nicht erfunden:
  Contribution gehört SONDE-018, DSP S26–28/S29–31, Hostfelder S18–19,
  EQ-Match in den späteren Ausbau. Reservierung heißt bis dahin Ablehnung.
- **Das Entfernen von `probe_descriptor_beitrag`** ist bewusst nicht
  aufgenommen. `plan.json:195` hält das Instanzfeld fail-closed, und
  `nakama-state-v2.md:76-80` verlangt, dass der Vertrag die Position weiter
  ausdrücken kann. Die vier heutigen `plugin_kind` werden deshalb im
  Beitragszweig abgelehnt, ohne den Zweig zu löschen: Gesperrt ist die heutige
  Trägerbehauptung, nicht das Wort.
- **Eine künftige Trägerklasse für `post_fader_contribution`** wird nicht
  erfunden. Welche Klasse die Position bei real vorhandenem diskretem Bus führen
  darf, bleibt laut `nakama-state-v2.md:86-90` eine eigene Produktentscheidung
  und spätere Versionierung.
- **NAK-115** erhält keine eigene v3-Matrixzeile: Er betrifft den v2-Rollen-
  Rückweg in `NakamaState.cpp` und ist im Register primär S7/SONDE-006
  zugeordnet (`docs/offene-punkte.md:18`). Solange keine heutige Klasse den
  Beitragszweig führen darf, bleibt dieser Rückweg unerreichbar; ein späterer
  Trägerentscheid muss ihn mit Save+Load gemeinsam lösen.
- **Die verworfenen Wege** `unsubscribe_session` und „Band-Stereo bleibt rein
  lokal“ sind keine offenen Baualternativen und erhalten weder Nutzlast noch
  Positivfixture.
- **Ein Capabilityname für FL-Hostfelder** ist nicht ergänzt. S18–19 besitzt die
  echte Hostmessung und das Capability-Ergebnis (`plan.json:218`). Da Capability
  nicht additiv ist, muss ein dort nötiger Name vor R0 zur Abnahme zurück oder
  eine neue Major-Version erhalten; diese Matrix darf ihn ohne Messung nicht
  erfinden.
- **Ein v3-Feld für die Überdeckungsanzeige** ist aus dem in §8 genannten
  bedingten Grund nicht reserviert.
- **NAK-29-Erzeugerfall 7** (`sample_count==0`, über 1 048 576 oder
  `start+count`-Überlauf) bleibt ein FeatureEngine-Publikationsvertrag. Der
  allgemeine Wirevertrag kann laut Quellenkommentar einen leeren Frame
  darstellen (`FeatureEngine.h:234-244`); ihn hier zu verbieten wäre ein neuer,
  nicht zugewiesener Wireentscheid.
- **Produktcode, Schema, Fixtures und Reservierungsdatei** werden in diesem
  Spezifikationsschritt nicht geändert. Ihre späteren Änderungen sind genau die
  in jeder Matrixzeile benannten Bau- und Beweisaufträge.

---

## Kanon-Lauf - SONDE-011 Phase A

**Lauf:** 2026-08-31 05:09 | **Runner:** `tools/beweise.ps1` | **Urteil:** ROT - 1 von 33 Kanon-Laeufen fehlgeschlagen | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 2 | **Rohausgabe:** [roh/SONDE-011-b327c1d.md](roh/SONDE-011-b327c1d.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-08-31 05:09:39 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | b327c1d SONDE-011 Phase A Runde 2: Cancel-vor-Read-Fenster strukturell geschlossen |
| Commit (voll) | b327c1d503ffd4ca73da144486ecef1973e99214 |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,11 s | [A1](roh/SONDE-011-b327c1d.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 10,00 s | [A2](roh/SONDE-011-b327c1d.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 6,70 s | [A3](roh/SONDE-011-b327c1d.md#a3) |
| A4 | Broker-Vertragstests gruen (Framing, Protokoll, Bindung, Aggregat, Server, v3-Coordinator, NAK-40-Alias/Kollisionsquarantaene und sticky Interventionszustand). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 4,37 s | [A4](roh/SONDE-011-b327c1d.md#a4) |
| A4-SI | SONDE-011/28-B Server-Integration auf Probe-Pipenamen: subscribe_session ist an Adresse, Session und eigenen Control-Link gebunden; Fremdadressen fallen; EOF, Protokoll-/Writefehler, Timeout-Hook und Serverstopp entfernen nur den eigenen Besitz atomar vor jedem spaeteren Push. | `cargo test --manifest-path broker/Cargo.toml --color never subscription_` | [OK] Exit 0 | 0,81 s | [A4-SI](roh/SONDE-011-b327c1d.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 1,08 s | [A4b](roh/SONDE-011-b327c1d.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,28 s | [A5](roh/SONDE-011-b327c1d.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,17 s | [A6](roh/SONDE-011-b327c1d.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,14 s | [A7](roh/SONDE-011-b327c1d.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,20 s | [A8](roh/SONDE-011-b327c1d.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,48 s | [A9](roh/SONDE-011-b327c1d.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen und band_stereo ID 10 samt saturated-/Grenzfaellen sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,00 s | [A10](roh/SONDE-011-b327c1d.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,31 s | [A11](roh/SONDE-011-b327c1d.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,38 s | [A12](roh/SONDE-011-b327c1d.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,28 s | [A13](roh/SONDE-011-b327c1d.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [ROT] Exit 2 | 6,07 s | [A14](roh/SONDE-011-b327c1d.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen (Zielkollision, state_schema-Drift, Stilllegung umgangen, Marke unlesbar, ungueltige ziel_id-Typen, Pfadtraversal) fallen einzeln. [4] vergleicht im Kanon weich (Abweichung nach Relink = Hinweis, fehlendes Artefakt oder nicht bildbarer Ordner-Hash = Fehler), hart nur mit --release; [4b] berichtet den installierten Stand ohne Urteil, ok nur bei Journalstatus OK; [5] Ordner-Hash v1 bytegleich in Python und PowerShell, Nicht-ASCII bricht beide ab. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft, jede unerwartete Ausnahme endet als Klartext mit Exit 2 statt als Traceback. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,12 s | [A17](roh/SONDE-011-b327c1d.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,38 s | [A18](roh/SONDE-011-b327c1d.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,15 s | [A19](roh/SONDE-011-b327c1d.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A20](roh/SONDE-011-b327c1d.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,31 s | [A21](roh/SONDE-011-b327c1d.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,54 s | [A22](roh/SONDE-011-b327c1d.md#a22) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,10 s | [A16](roh/SONDE-011-b327c1d.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,14 s | [B1](roh/SONDE-011-b327c1d.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 0,33 s | [B2](roh/SONDE-011-b327c1d.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,09 s | [B3](roh/SONDE-011-b327c1d.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,12 s | [B3b](roh/SONDE-011-b327c1d.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,26 s | [B3c](roh/SONDE-011-b327c1d.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten, und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,53 s | [B4](roh/SONDE-011-b327c1d.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,18 s | [B9](roh/SONDE-011-b327c1d.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle 61 Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen - waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, aber kein unbewiesenes Projektintervall oder FFT-Event erzeugen; Frame-Stempel umfassen den echten ueberlappenden FFT-Support und bleiben an int64/u32-Grenzen darstellbar; Drop zaehlt als Segment, alles andere als Epoche; alle sieben Erzeuger-Stempelregeln fallen einzeln und mit eigener Nummer; kein spektraler Fluss ueberbrueckt eine Grenze, der Ereignisring ist fest gedeckelt; LUFS-S trifft die analytisch gerechnete Erwartung unter 0,1 LU; die Nyquist-Kappe greift bei 22,05 kHz wirklich; verdrahtet: alle sieben Gueltigkeitsbits kommen ueber die Hostbruecke durch, der Playhead-Rueckfallweg nachweislich nur zwei. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 2,12 s | [B5](roh/SONDE-011-b327c1d.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | v3-Envelope in C++ klassifiziert den Envelope-Korpus wie das Manifest (Urteil UND Verstossmenge, alle 14 Regeln mit Negativfixture); CRC32C trifft die RFC-3720-Vektoren, P0/P1 tragen CRC exakt 0, P2 die Pflichtsumme ueber genau die Payloadbytes; 40 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und 7671 angenommene EINBIT-Mutanten gueltiger Frames halten jede Kopfregel (reiner Zufall wird praktisch immer abgewiesen - die Invariante braucht deshalb die Mutanten, sonst spraeche sie ueber eine leere Menge), 3000 gekippte P2-Bits fallen einzeln, byteweise Zustellung liefert dieselben 40 Frames und ein kaputter Frame beendet den Strom; Pipetoken trifft das Golden aus §48.3 samt SHA-256- und RFC-4648-Vektoren; P0 verwirft nichts und meldet den 65. Eintrag, P1 koalesziert an der Position und haelt Ereignisse fuer den Reconnect vor, die P2-Schleuse ersetzt den aeltesten ungesendeten Frame, uebergibt 100 000 Frames mit 0 Allokationen (mit Gegenprobe am selben Zaehler) und liefert unter Flut keinen zerrissenen Frame; verdrahtet: Control koppelt Telemetry ueber link_id + challenge, ein ungekoppelter Telemetry-Connect wird geschlossen, der Client verbindet nach Serverneustart von selbst wieder, ein kaputter Envelope vom Server schliesst die Verbindung, und ein P0-Ueberlauf WAEHREND einer stehenden Verbindung schliesst sie ebenfalls statt still zu kuerzen. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 51,12 s | [B10](roh/SONDE-011-b327c1d.md#b10) |
| B8 | Lifecycle-Klassifikation §53.5: unclassified beim Laden und audio-neutral; Schema-1 sensor\|pre\|post -> legacy (immer passiv), hub bzw. bestaetigter Schema-2-Main-State -> main; ein Scannerlauf klassifiziert nicht; read-only nimmt die Klassifikation zurueck; Brokerstart nur fuer main mit offenem Editor; die Sondenbundles bleiben bis gueltigem State neutral und werden nie main. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,13 s | [B8](roh/SONDE-011-b327c1d.md#b8) |


---

## Kanon-Lauf - SONDE-011 Phase A

**Lauf:** 2026-08-31 06:16 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 33/33 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/SONDE-011-db8b1f0-dirty.md](roh/SONDE-011-db8b1f0-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-08-31 06:16:44 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | db8b1f0 SONDE-011 Phase A Runde 3: Kern identitaetsfrei — Domain-Tag und FlatBuffers-Namespace ohne Herstellernadel |
| Commit (voll) | db8b1f0eb9828e80993d2ee44ccac3327b53a387 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,09 s | [A1](roh/SONDE-011-db8b1f0-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,64 s | [A2](roh/SONDE-011-db8b1f0-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 6,70 s | [A3](roh/SONDE-011-db8b1f0-dirty.md#a3) |
| A4 | Broker-Vertragstests gruen (Framing, Protokoll, Bindung, Aggregat, Server, v3-Coordinator, NAK-40-Alias/Kollisionsquarantaene und sticky Interventionszustand). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 3,16 s | [A4](roh/SONDE-011-db8b1f0-dirty.md#a4) |
| A4-SI | SONDE-011/28-B Server-Integration auf Probe-Pipenamen: subscribe_session ist an Adresse, Session und eigenen Control-Link gebunden; Fremdadressen fallen; EOF, Protokoll-/Writefehler, Timeout-Hook und Serverstopp entfernen nur den eigenen Besitz atomar vor jedem spaeteren Push. | `cargo test --manifest-path broker/Cargo.toml --color never subscription_` | [OK] Exit 0 | 0,73 s | [A4-SI](roh/SONDE-011-db8b1f0-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 1,07 s | [A4b](roh/SONDE-011-db8b1f0-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 1,80 s | [A5](roh/SONDE-011-db8b1f0-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,13 s | [A6](roh/SONDE-011-db8b1f0-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/SONDE-011-db8b1f0-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,19 s | [A8](roh/SONDE-011-db8b1f0-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,26 s | [A9](roh/SONDE-011-db8b1f0-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen und band_stereo ID 10 samt saturated-/Grenzfaellen sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 1,49 s | [A10](roh/SONDE-011-db8b1f0-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,25 s | [A11](roh/SONDE-011-db8b1f0-dirty.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,25 s | [A12](roh/SONDE-011-db8b1f0-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,22 s | [A13](roh/SONDE-011-db8b1f0-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 5,42 s | [A14](roh/SONDE-011-db8b1f0-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen (Zielkollision, state_schema-Drift, Stilllegung umgangen, Marke unlesbar, ungueltige ziel_id-Typen, Pfadtraversal) fallen einzeln. [4] vergleicht im Kanon weich (Abweichung nach Relink = Hinweis, fehlendes Artefakt oder nicht bildbarer Ordner-Hash = Fehler), hart nur mit --release; [4b] berichtet den installierten Stand ohne Urteil, ok nur bei Journalstatus OK; [5] Ordner-Hash v1 bytegleich in Python und PowerShell, Nicht-ASCII bricht beide ab. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft, jede unerwartete Ausnahme endet als Klartext mit Exit 2 statt als Traceback. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,00 s | [A17](roh/SONDE-011-db8b1f0-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 34,96 s | [A18](roh/SONDE-011-db8b1f0-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/SONDE-011-db8b1f0-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A20](roh/SONDE-011-db8b1f0-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,33 s | [A21](roh/SONDE-011-db8b1f0-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,50 s | [A22](roh/SONDE-011-db8b1f0-dirty.md#a22) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,09 s | [A16](roh/SONDE-011-db8b1f0-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,10 s | [B1](roh/SONDE-011-db8b1f0-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 0,32 s | [B2](roh/SONDE-011-db8b1f0-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,05 s | [B3](roh/SONDE-011-db8b1f0-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/SONDE-011-db8b1f0-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,25 s | [B3c](roh/SONDE-011-db8b1f0-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten, und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,45 s | [B4](roh/SONDE-011-db8b1f0-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,13 s | [B9](roh/SONDE-011-db8b1f0-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle 61 Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen - waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, aber kein unbewiesenes Projektintervall oder FFT-Event erzeugen; Frame-Stempel umfassen den echten ueberlappenden FFT-Support und bleiben an int64/u32-Grenzen darstellbar; Drop zaehlt als Segment, alles andere als Epoche; alle sieben Erzeuger-Stempelregeln fallen einzeln und mit eigener Nummer; kein spektraler Fluss ueberbrueckt eine Grenze, der Ereignisring ist fest gedeckelt; LUFS-S trifft die analytisch gerechnete Erwartung unter 0,1 LU; die Nyquist-Kappe greift bei 22,05 kHz wirklich; verdrahtet: alle sieben Gueltigkeitsbits kommen ueber die Hostbruecke durch, der Playhead-Rueckfallweg nachweislich nur zwei. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 2,11 s | [B5](roh/SONDE-011-db8b1f0-dirty.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | v3-Envelope in C++ klassifiziert den Envelope-Korpus wie das Manifest (Urteil UND Verstossmenge, alle 14 Regeln mit Negativfixture); CRC32C trifft die RFC-3720-Vektoren, P0/P1 tragen CRC exakt 0, P2 die Pflichtsumme ueber genau die Payloadbytes; 40 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und 7671 angenommene EINBIT-Mutanten gueltiger Frames halten jede Kopfregel (reiner Zufall wird praktisch immer abgewiesen - die Invariante braucht deshalb die Mutanten, sonst spraeche sie ueber eine leere Menge), 3000 gekippte P2-Bits fallen einzeln, byteweise Zustellung liefert dieselben 40 Frames und ein kaputter Frame beendet den Strom; Pipetoken trifft das Golden aus §48.3 samt SHA-256- und RFC-4648-Vektoren; P0 verwirft nichts und meldet den 65. Eintrag, P1 koalesziert an der Position und haelt Ereignisse fuer den Reconnect vor, die P2-Schleuse ersetzt den aeltesten ungesendeten Frame, uebergibt 100 000 Frames mit 0 Allokationen (mit Gegenprobe am selben Zaehler) und liefert unter Flut keinen zerrissenen Frame; verdrahtet: Control koppelt Telemetry ueber link_id + challenge, ein ungekoppelter Telemetry-Connect wird geschlossen, der Client verbindet nach Serverneustart von selbst wieder, ein kaputter Envelope vom Server schliesst die Verbindung, und ein P0-Ueberlauf WAEHREND einer stehenden Verbindung schliesst sie ebenfalls statt still zu kuerzen. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 51,19 s | [B10](roh/SONDE-011-db8b1f0-dirty.md#b10) |
| B8 | Lifecycle-Klassifikation §53.5: unclassified beim Laden und audio-neutral; Schema-1 sensor\|pre\|post -> legacy (immer passiv), hub bzw. bestaetigter Schema-2-Main-State -> main; ein Scannerlauf klassifiziert nicht; read-only nimmt die Klassifikation zurueck; Brokerstart nur fuer main mit offenem Editor; die Sondenbundles bleiben bis gueltigem State neutral und werden nie main. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,12 s | [B8](roh/SONDE-011-db8b1f0-dirty.md#b8) |


---

# Phase A — Bau, Prüfrunden und Kanon (2026-08-31, Dirigent, Session b428f3a9)

**Phase A (v3-Vertragsrunde vor R0) ist gebaut, dreifach nachgearbeitet und
vom frischen Prüfer mit PASS abgeschlossen.** Das Ticketurteil S16–17 bleibt
offen, bis Phase B (Koordinator/Eviction, SQLite-Migration, Outbox,
Broker-Autostart) gebaut und geprüft ist — deshalb trägt diese Datei noch
keine NAKAMA-URTEIL-Marke.

**Arbeitsmodus:** Codex-first (§3.6). Spezifikation und Bau: Codex-Thread
`01a054e9-ab19-7cc0-9767-bca640bad4cf` (`gpt-5.6-sol`, max, workspace-write).
Alle Prüfungen: frische Sol-max-Threads, read-only. Der Dirigent hat nur
gemessen, committet, gepusht.

## Runden

| Runde | Basis → Ende | Inhalt | Urteil des frischen Prüfers |
|---|---|---|---|
| Matrix | `b75ea06` → `67c49f8` | Vertragsmatrix (Spezifikation vor Code) | NACHARBEIT → **TRAGFÄHIG** |
| Bau | `67c49f8` → `01f6194` | alle Matrixzeilen (95 Dateien, +5076/−114) | Erstprüfung: **NEEDS_WORK** (4 Defekte) |
| 1 | `01f6194` → `329a54a` | Alias-Namespacing, Heartbeat-Quarantäneflag, Writer-Fehlerpfad, NAK-29-Sender beidseitig | **NEEDS_WORK** (Befund 3 offen: Cancel-Fenster) |
| 2 | `329a54a` → `b327c1d` | Ende-Event + WaitForMultipleObjects in `ov_lesen`, deterministische Testnaht; Mutation rot, 20/20 Wiederholungen | **PASS** |
| 3 | `b327c1d` → `db8b1f0` | Kern identitätsfrei: Domain-Tag `nakama.v3.instance-address.v1`, .fbs-Namespace `nakama.v3` (Wire bytegleich, A14 25/0) | **PASS** |

**Rundenbilanz** (`rundenbilanz.py --runden 67c49f8 01f6194 329a54a b327c1d db8b1f0`):
Bau Produkt 87 Datei(en) +4255/−91, Tests 3 +258/−2, Prüfwerkzeug 4 +549/−20;
R1 Produkt 4 +310/−92, Tests 3 +34/−0; R2 Produkt 1 +182/−24;
R3 Produkt 9 +83/−89, Tests 3 +3/−3. Kein Konvergenz-Signal.

## Kanonläufe dieser Phase

- `b327c1d`: ROT 1/33 (A14, Identitätsnadel im Kern → Runde 3), Rohausgabe
  `docs/beweise/roh/SONDE-011-b327c1d.md`.
- `db8b1f0`: **GRÜN 33/33** (Kanonzahl 32 → 33 durch das neue A4-SI-Bein),
  Rohausgabe `docs/beweise/roh/SONDE-011-db8b1f0-dirty.md`; „dirty"
  bezeichnet die zum Laufzeitpunkt uncommitteten Manifest-Anhänge unter
  `docs/beweise/`, keine Quelländerung.

## Vom Dirigenten selbst gemessene Schlusszeilen (auf `db8b1f0`)

A5 333/0 · A8 222 bytegleich · A9 Drift 0 · A10 87 bytegleich · A14 25/0 ·
Rust 175+9+9/0 · B3c 71/0 · B2 176/0 · B10 211/0 · B5 237/0 ·
A4-SI-Zielfall 5× in Folge grün.

# Phase B — Verhaltensmatrix (2026-08-31)

Alle mit **ZUSAGE** bezeichneten Phase-A-Zeilen sowie §9 bleiben unverändert
bindend (`docs/beweise/SONDE-011.md:13-248`), insbesondere 28-B, 10-1 und
NAK-40. Gate und Ticketgrenze stehen in `docs/plan/plan.json:192-197` und
`docs/bauaufteilung-sonden.md:332-341`. Dieser Abschnitt ist Spezifikation vor
Code; er enthält keinen Bau- oder Testnachweis. Die Dirigentenentscheide vom
2026-08-31 stehen in B.9; jede dort festgelegte Zahl erscheint in den
betroffenen Matrixzeilen als benannte Konstante mit Grenztest.

## B.1 Bestandsaufnahme

| Bereich | Heute vorhanden | Für Phase B fehlt |
|---|---|---|
| Coordinator | Phase-A-Kern für linkgebundene Subscription, NAK-40-Alias/Quarantäne und den gemeinsamen sticky Interventionszustand (`broker/src/coordinator.rs:1-66,68-188,252-404`). | Sessiongraph, Join, Führung, Liveness, Tombstones, Eviction, Store- und Outboxanbindung. |
| Transport/Prioritäten | Gekoppelter v3-Probe-Listener, frühe Cleanup-Hooks und die vier begrenzten Queuepolitiken (`broker/src/transport/server_v3.rs:36-44,120-138,1751-1806`; `broker/src/transport/warteschlange.rs:1-24`). | Produktive v3-Verdrahtung: `eqcop-broker.exe` startet heute nur den v2-Server (`broker/src/lib.rs:795-824`); Store/Outbox sind ausdrücklich noch nicht Transportaufgabe (`broker/src/transport/mod.rs:12-15`). |
| Store | Haltbare atomare JSON-Dateiveröffentlichung für Bindungen/Snapshots (`broker/src/persistenz.rs:1-6,135-186`). | `rusqlite = "=0.40.2"` mit `bundled`, lokale Migration 1, Tabellen/Schlüssel aus B.4, Eventlog, Projektionen, Single-Writer, WAL, Outbox und dauerhafter Konfliktriegel; `broker/Cargo.toml:11-34` enthält heute noch keine SQLite-Abhängigkeit. |
| Plugin-Lifecycle | `state::Lebenslauf::darfBrokerStarten()` ist exakt `main && editor` und B8 misst die Erlaubnis (`eq-copilot/plugin/state/NakamaLebenslauf.h:117-127`; `tools/beweise.ps1:461-462`). `ControlClient` verbindet nur und spawnt nie (`eq-copilot/plugin/core/ipc/ControlClient.h:8-18`). | Prozessstart, idempotente Mehrfachstart-Sperre, Prozessstopp/Idle-Ende, produktive v3-Verbindung und semantisches In-Flight-Register oberhalb der P0-Queue. |
| Installer/Rückweg | Broker liegt als gehashtes Artefakt im geschützten Ziel; vorhandener Stand wird vor Tausch gesichert und über `-Rueckweg` restauriert (`eq-copilot/install/nakama-installer-v1.json:8-17,48-64`; `eq-copilot/install/Install-Nakama.ps1:24-39,55-68`). | Manifest-Hashprüfung, WinVerifyTrust-/Thumbprint-Prüfkette, Prozessstart und Broker-Selbstende; bewusst kein Installer-/Boot-Autostartartefakt. Das Codesigning-Zertifikat und der befüllte Thumbprint bleiben NAK-119/S34–35. |

## B.2 Verhaltensmatrix — Coordinator und Session

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| C-01 | Kein Control-Link | Authentisiertes v3-Control-Hello | **ZUSAGE:** Bootstrap prüfen → Link/Adresse/Alias beim Coordinator anmelden → `control_verbunden` vollständig abschließen → erst dann `welcome` freigeben. Kein Sessionzustand wird vor Authentisierung sichtbar. | `MAX_BOOTSTRAP_BYTES = 16 * 1024 = 16384`; `BOOTSTRAP_FRIST = 5000 ms`; `SENKE_FRIST = 2000 ms`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3767-3775`; `broker/src/transport/v3.rs:49`; `broker/src/transport/server_v3.rs:89-107,1360-1407`. | A4-Rust `hello_ueber_16_kib_faellt_vor_dem_parser` bei `MAX_BOOTSTRAP_BYTES`/`MAX_BOOTSTRAP_BYTES + 1`, `coordinator_model::control_vor_welcome`, `welcome_folgt_dem_abgeschlossenen_control_verbunden` und Timeoutgrenzen bei Konstante minus eins/exakt; B10. |
| C-02 | Angemeldeter Control-Link, Telemetrie frei | Telemetry-Hello mit identischem `runtime_nonce`, `link_id` und Challenge | **ZUSAGE:** genau einmal koppeln → Telemetry-Welcome senden → Trennpflicht setzen → `telemetrie_gekoppelt` höchstens einmal aufrufen. Der Callback läuft nie vor `control_verbunden`; ein fremdes Merkmal, ungekoppelter oder zweiter Telemetry-Link wird ohne zweiten Callback geschlossen. | `BOOTSTRAP_FRIST = 5000 ms`; `SENKE_FRIST = 2000 ms`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3771-3777`; `broker/src/transport/server_v3.rs:1429-1479`; bestehende Matrix `docs/beweise/SONDE-010.md:254`. | A4-Rust `coordinator_model::telemetrie_einmalig_gekoppelt`, `connect_callbacks_je_kopplung_genau_einmal` und Grenzfall bei exakt `SENKE_FRIST`; B10. |
| C-03 | Gültiger Report, noch kein bestätigtes Mitglied | Join-Kandidat | **ZUSAGE:** automatischer Beitritt nur bei genau einer eindeutigen Main-Sitzung im selben Host; Bridge, zwei offene Projekte oder doppelte ID setzen `beitritt_bestaetigung_noetig`, niemals heuristische Steuerbarkeit. Phase B hält Kandidat, Bestätigungsbedarf und Führung ausschließlich intern im Coordinator und veröffentlicht sie über das bestehende `session_snapshot`; keine neue Familie und kein neuer Wire-Weg. | Interner Modellstand bis SONDE-012; bestätigender User-Akt auf Wire erst SONDE-012/S29–31. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1628-1630`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:856-874`; `docs/beweise/SONDE-011.md:608` (L-01). | A4-Rust `coordinator_model::join_nur_bei_eindeutigem_main` mit eindeutiger, Bridge-, Zwei-Projekte- und Duplicate-Variante; A4-SI `session_snapshot_zeigt_join_kandidat_ohne_neue_familie`; Q1 hält die Familienmenge unverändert. |
| C-04 | Sitzung ohne oder mit führendem Main | bestätigter Beitritt, stale/Eviction, Reconnect, Brokerneustart oder User-Übergabe | **ZUSAGE — REVIDIERT 2026-08-31:** Führung ist Userintent im `MainProjectState`, aber Phase B besitzt dafür weder Ingress noch neuen Wire-Weg. Innerhalb desselben Brokerlaufs gelten weiterhin Serialisierung, Exklusivität, stale bis Eviction und Rückgabe an dieselbe `instance_id`/Session-Epoche ohne bestätigte Übergabe. Nach Brokerneustart ist Führung frei; sie fällt automatisch nur an ein dann einziges bestätigtes Main, sonst `beitritt_bestaetigung_noetig`. Rekonstruktion aus `MainProjectState` gehört SONDE-012/S29–31. Bei Übergabe verliert das alte Main Schreibfähigkeit vor Freigabe des neuen. | `FUEHRENDE_MAINS_PRO_SESSION = 1`; kein beobachtbarer Doppel-Führungszustand und kein Phase-B-State-Ingress. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1454-1455,1907-1911,3925-3939`; `docs/beweise/SONDE-011.md:609` (L-02); `docs/offene-punkte.md:15` (NAK-120). | A4-Rust `coordinator_model::erstes_einziges_main_fuehrt_sonst_bestaetigung`, `fuehrung_stale_bis_eviction`, `reconnect_im_selben_brokerlauf_gibt_unuebergebene_fuehrung_zurueck`, `brokerneustart_ohne_mainprojectstate_ingress_gibt_fuehrung_frei`, `fuehrung_exklusiv_und_uebergabe`. |
| C-05 | Zwei Projektkopien oder zwei Projekte | Reports/Reconnect/Brokerneustart | **ZUSAGE:** `project_binding_id + session_epoch` trennt Sitzungen; `session_epoch` stammt vom führenden Main, überlebt den Brokerneustart und wird nicht aus Ähnlichkeit oder `host_pid` ersetzt. `broker_epoch` ändert nur die Brokerlaufgrenze. | `FUEHRENDE_MAINS_PRO_SESSION = 1`; `SICHTBARE_SONDEN_NORMAL = 16`; `LAST_SONDEN = 32`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1454-1456,1610-1626`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:62-76,782-797`. | A4-Rust `coordinator_model::zwei_projekte_bleiben_getrennt`, `brokerneustart_behaelt_session_epoch` und Normalgrenze bei `SICHTBARE_SONDEN_NORMAL`; A22 bei exakt `LAST_SONDEN`. |
| C-06 | Link mit gültiger Subscription | EOF, Protokoll-/Writefehler, Timeout, Serverstopp, Nonce-Verdrängung oder stale-bedingtes Linkende | **ZUSAGE 28-B:** Ingress schließen → unter dem Coordinator-Lock nur Linkbesitz, Subscription und gekoppelte Semantik entfernen → Kopplung lösen → fristbegrenzte Joins → `telemetrie_getrennt` vor `control_getrennt`. Cleanup liegt vor jedem später gestarteten Push; Reconnect muss neu abonnieren. | Cleanup ohne I/O; jeder Join/Callback höchstens `SENKE_FRIST = 2000 ms`. | `docs/beweise/SONDE-011.md:69-72`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:1854-1860`; `broker/src/transport/server_v3.rs:120-131,1751-1806`; `docs/beweise/SONDE-011.md:610` (L-03). | Vorhandene A4-SI `subscription_ist_an_eigenen_control_link_gebunden`, `subscription_cleanup_vor_weiterem_push`; A4-Rust `coordinator_model::trennen_bereinigt_vor_join` mit Grenzfall bei exakt `SENKE_FRIST`. |
| C-07 | Eindeutige oder kollidierende effektive Adresse | Registrierung/Dispatch/Push/Eviction/Brokerstart | **ZUSAGE NAK-40 — REVIDIERT 2026-08-31:** Alias ist nur Wireadresse. Kollision quarantänisiert atomar beide Seiten und sperrt Dispatch, Telemetrie sowie Session-Push; Registrierungsreihenfolge ändert das Urteil nicht. Der clientunabhängige, nicht routbare Konfliktindex wird in `conflict_guards` dauerhaft gespeichert, überlebt Eviction und Brokerneustart und wird beim Start vor jeglichem Routing restauriert. Misslingt Persistenz oder Restore, bleibt Routing fail-closed. Auflösung ausschließlich über explizite Neu-ID im Phase-A-Weg. | Wireadresse exakt hex32; keine zeitbasierte Freigabe; kein Routing vor erfolgreichem Guard-Restore. | `docs/beweise/SONDE-011.md:146-151,239-243`; `broker/src/instance_alias.rs:122-212,214-257`; `docs/beweise/SONDE-011.md:613` (L-06). | Vorhandene A4-Rust-Kollisionstests; neu `coordinator_model::eviction_haelt_dauerhaften_konfliktriegel`, `store_crash_matrix::konfliktriegel_ueberlebt_neustart_mit_nur_einem_partner`, `routing_bleibt_zu_bis_konfliktriegel_restauriert`. |
| C-08 | Interventionszustand sauber oder sticky unknown | v2-Hörmarkierung, v3 Begin/End, Overflow, Sequenzlücke, Disconnect, Resync | **ZUSAGE 10-1:** v2 und v3 speisen vor Evidenzdispatch genau einen Zustand. Overflow/Lücke/Disconnect setzen sticky unknown; End, v2-`false`, Sessionende, stale oder Eviction löschen es nicht. Nur bestätigter Neutral-/Sequenz-Resync entsperrt; Überlappung und Tail laufen vollständig aus. | `MAX_AKTIVE_INTERVENTIONEN = 64`; Tail in Samples, nicht Wanduhr. | `docs/beweise/SONDE-011.md:186-188`; `broker/src/coordinator.rs:19,235-369,391-404`. | Vorhandene A4-Rust-Tests aus 10-1 prüfen `MAX_AKTIVE_INTERVENTIONEN`/`MAX_AKTIVE_INTERVENTIONEN + 1`; zusätzlich `coordinator_model::eviction_loescht_intervention_unknown_nicht`. |
| C-09 | I/O-Frame validiert | Übergabe an Domäne/Store/Antwort | **ZUSAGE:** I/O-Worker decodieren nur Envelope, Grenzen und Authentisierung; nur der Coordinator mutiert den Sessiongraphen und erzeugt Storeevents/Outboxaufträge. Blockierende Pipe-I/O hält weder Coordinator- noch Storelock. | `CAP_INGRESS = 256`; `CAP_WRITER = 256`; keine ungebundene Domänenqueue. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3566-3569,3787-3790`; `broker/src/transport/server_v3.rs:98-107,1543-1599`. | A4-Rust `coordinator_model::io_worker_mutiert_keinen_sessiongraph`; A4-SI `blockierter_writer_haelt_coordinator_und_store_nicht`; Grenztests jeweils bei `CAP`/`CAP + 1`; B10/A22. |
| C-10 | Dieselbe `instance_id`/Session-Epoche hat eine aktive alte Nonce | neue Nonce registriert sich / alte Nonce sendet erneut | **ZUSAGE:** Die neue Nonce verdrängt die alte Verbindung sofort; alte Subscription endet vollständig nach C-06 und der alte Client wird tombstoned. Meldet die verdrängte Nonce danach erneut, entsteht der fail-closed-Duplikatkonflikt: beide Seiten sind bis zur expliziten Auflösung nach C-03/C-07 gesperrt. | Verdrängung ohne stale-Wartefrist; alter Report ist ab dem ersten Frame fail-closed. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1612-1626`; `docs/beweise/SONDE-011.md:610` (L-03). | A4-Rust `coordinator_model::neue_nonce_verdraengt_alte_sofort`, `verdraengte_nonce_report_sperrt_beide`; A4-SI `nonce_verdraengung_cleanup_vor_altem_report`. |

## B.3 Verhaltensmatrix — monotone Liveness und Eviction

| ID | Zustand vor Ereignis | Ereignis | Zusage | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| E-01 | `fresh` | gültiger Heartbeat/Report | **ZUSAGE:** `last_seen` und jede Liveness-Deadline werden ausschließlich aus Rust `Instant` geführt; `SystemTime` darf nur Anzeige-/Persistenzzeit liefern. Ein Wallclock-Sprung ändert kein Liveness-Urteil. `letzter_kontakt_ms` ist das monotone Alter seit dem letzten Kontakt, kein Zeitpunkt. | `HEARTBEAT_INTERVAL_MS = 1000`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1830-1840,3207-3212`; `docs/beweise/SONDE-011.md:611` (L-04). | A4-Rust `coordinator_model::liveness_nur_instant` mit Vor-/Rücksprung der simulierten Wanduhr und Grenzschritten von exakt `HEARTBEAT_INTERVAL_MS`. |
| E-02 | `fresh` oder `stale`, noch nicht evicted | monotones Alter wächst / gültiger Heartbeat oder Report | **ZUSAGE:** `stale` gilt ausschließlich bei Alter `> STALE_NACH_MS`; bis einschließlich der Grenze bleibt die Quelle fresh. Jeder gültige Kontakt vor Eviction macht sie sofort fresh und setzt den Tombstone-Timer zurück. Audio bleibt unberührt; stale bleibt sichtbarer Zustand, kein erfundener Messwert. | `STALE_VERPASSTE_INTERVALLE = 2`; `STALE_JITTER_MS = 500`; `STALE_NACH_MS = STALE_VERPASSTE_INTERVALLE * HEARTBEAT_INTERVAL_MS + STALE_JITTER_MS = 2500`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:2082-2087`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:393-401`; `docs/beweise/SONDE-011.md:611` (L-04). | A4-Rust `coordinator_model::stale_konstanten_ableitung` und `stale_grenze_exklusiv_und_kontakt_setzt_zurueck`: Alter `STALE_NACH_MS` fresh, `STALE_NACH_MS + 1` stale, Kontakt sofort fresh und neue volle Tombstone-Frist; Fake-Clock, kein Sleep. |
| E-03 | `stale`/getrennt, Tombstone sichtbar | Tombstone-Frist läuft ab | **ZUSAGE — REVIDIERT 2026-08-31:** Bei Ablauf verschwindet der Client atomar aus Sessiongraph, Register, Nonce-, Aliasindizes, Subscriptions sowie allen Push-/Dispatchzielen; persistente Projektidentität und Plugin-State bleiben. Konfliktwahrheit bleibt clientunabhängig in `conflict_guards` und wird durch Eviction weder gelöscht noch routbar. | `TOMBSTONE_MS = 10000`; bei `TOMBSTONE_MS - 1` bleibt der Client sichtbar, bei exakt `TOMBSTONE_MS` wird evicted. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3207-3212,3937-3939`; `docs/beweise/SONDE-011.md:146-151,611,613` (L-04/L-06). | A4-Rust `coordinator_model::tombstone_grenze_entfernt_alle_fluechtigen_sichten_aber_keinen_dauerhaften_konfliktriegel`; A4-SI prüft nach exakt `TOMBSTONE_MS` leere Pushziele und weiterhin gesperrte Guard-Adresse. |
| E-04 | Session-/Globalcap erreicht | neuer Client/Eintrag | **ZUSAGE:** stale-first, dann ältestes `last_seen`, dann lexikographisch vollständige effektive Adresse. HashMap-/Einfügereihenfolge ändert das Opfer nicht. Gibt es ausschließlich fresh Clients, wird der neue Client ehrlich und gezählt abgewiesen; kein fresh Client fällt. | `SESSION_CLIENT_CAP = 64`; `GLOBAL_CLIENT_CAP = 128`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1454-1456,3209-3212`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:856-874`; `docs/beweise/SONDE-011.md:612` (L-05). | A4-Rust `coordinator_model::session_und_global_cap`: jeweiliges `CAP` akzeptiert; `CAP + 1` evicted das deterministische stale-Opfer oder wird bei nur fresh gezählt abgewiesen; permutierte Einfügereihenfolge. |
| E-05 | Stale Client besitzt Subscription/Intervention | Eviction | **ZUSAGE:** zuerst C-06/C-08 ausführen: Subscription vor Push entfernen, aktive ID entfernen und sticky unknown setzen; Eviction ist kein Resync und wäscht weder Hör- noch Alias-Konfliktwahrheit. | Keine zusätzliche Frist. | `docs/beweise/SONDE-011.md:69-72,146-151,186-188`; `tools/dirigent/pruefliste.md:32-38`. | A4-Rust `coordinator_model::eviction_haelt_phase_a_riegel`; A4-SI `eviction_cleanup_vor_push`. |
| E-06 | Broker neu gestartet | Guard-Restore, Storeevents und neue Reports treffen ein | **ZUSAGE — REVIDIERT 2026-08-31:** zuerst dauerhafte Konfliktriegel restaurieren, erst danach Routing öffnen; übrigen flüchtigen Graphen aus Store/Reports neu bilden. Phase B hat keinen `MainProjectState`-Ingress: Führung startet frei und fällt nur an ein dann einziges bestätigtes Main automatisch, sonst `beitritt_bestaetigung_noetig`. Brokerneustart verändert `broker_epoch`, nicht Projektbindung oder lokale Identität. | Nur Testgrenze `STORE_RECOVERY_TEST_MAX_MS = 5000`; kein hartes Wire-Budget. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1620-1626,1907-1916,3929-3935`; `docs/beweise/SONDE-011.md:609,613,616` (L-02/L-06/L-09); `docs/offene-punkte.md:15` (NAK-120). | A4-Rust `coordinator_model::brokerneustart_ohne_mainprojectstate_ingress`, `store_crash_matrix::guard_restore_vor_routing`, `projektion_rebuild_nach_neustart_unter_testgrenze`; die Zeitprobe besteht bei `STORE_RECOVERY_TEST_MAX_MS - 1`, injizierte Gleichheit fällt. |

## B.4 Verhaltensmatrix — SQLite-Store

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| S-01 | Keine DB, älteres/neueres Schema oder aufgelöster Remote-Pfad | Storestart/Migration 1 | **ZUSAGE — REVIDIERT 2026-08-31:** `STORE_SCHEMA_MAJOR = 1`. DB/WAL liegen unter dem per `FOLDERID_LocalAppData` aufgelösten lokalen Root plus `STORE_RELATIVPFAD = "evenacadia\\nakama-broker"` und `STORE_DATEINAME = "nakama-broker.sqlite3"`, nie neben der Roaming-Bindungsdatei. Remote-Volume, neueres Schema oder Storefehler öffnen/schreiben nichts und führen sichtbar in L-08-Degradation; Routing bleibt bis zum Konfliktriegel-Restore fail-closed, P2 darf weiterlaufen. Kein Downgrade, Import oder Ändern von `%APPDATA%\evenacadia\nakama\eq-copilot-bindungen.json`. | Kein Wire-Startbudget; Testgrenze `STORE_RECOVERY_TEST_MAX_MS = 5000`; Netzspeicher ist unzulässig. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1903-1916,3799-3816,4131,4173`; `broker/src/main.rs:6-20`; `docs/beweise/SONDE-011.md:614-616` (L-07/L-09). | A4-Rust `store_crash_matrix::migration_1_frisch_und_idempotent`, `store_pfad_nutzt_folderid_localappdata`, `remote_volume_degradiert_ohne_db_oder_wal`, `schema_major_1_schreibt_und_major_plus_1_degradiert_read_only`; Recovery jeweils unter `STORE_RECOVERY_TEST_MAX_MS`; K-08/K-09. |
| S-02 | Writer bereit; neue oder bereits bekannte `command_id` | Coordinator akzeptiert persistenzpflichtigen Befehl/Storeevent | **ZUSAGE — REVIDIERT 2026-08-31:** allein der Store-Writer appendet Eventwahrheit, aktualisiert Projektion, schreibt die koaleszierte Snapshot-Schuld in die Outbox und bindet bei Befehlen intern `command_id → event_uuid` gemeinsam in einer `BEGIN IMMEDIATE`-Transaktion. Erst der Commit einer neuen `command_id` erlaubt `command_ack(ergebnis = angewandt)`; ein Retry derselben ID liest die haltbare Zuordnung, erzeugt kein zweites Event und antwortet `ergebnis = idempotent_wiederholt`. Das ACK referenziert nur `command_id` und die schemafesten Zustandsfelder, nie die interne Event-UUID. Eventfelder und `(broker_epoch, sequence)` bleiben wie festgelegt eindeutig. | Annahme erst nach Commit; `command_id` ist für Befehle persistent unique; kein Einzel-fsync je Event. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1863-1866,3566-3569,3804-3811`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/beweise/SONDE-011.md:618` (L-11); ANGENOMMEN V-03 in `docs/beweise/SONDE-011.md:598`. | A4-Rust `store_crash_matrix::event_projection_snapshotschuld_command_id_atomare_transaktion`, `command_id_mappt_intern_auf_genau_eine_event_uuid`, `event_log_append_only_und_sequence_eindeutig`; A4-SI `verlorenes_command_ack_retry_liefert_idempotent_wiederholt_ohne_wire_uuid`; K-01 bis K-03. |
| S-03 | I/O-/Coordinatorereignis liegt an | Schreiben/Antwort oder Storedegradation | **ZUSAGE:** Reihenfolge I/O → Coordinator → begrenzter Storekanal → alleinige Writer-Connection. I/O hält weder Coordinator- noch Storelock. Bei vollem Kanal oder degradiertem Store wird die Annahme persistenzpflichtiger Ereignisse verweigert und der Verursacher getrennt; nichts Unhaltbares wird bestätigt. Disk-full, I/O-Fehler, beschädigte/neue DB stoppen nicht den Broker: sichtbarer gezählter Betrieb ohne Historie, P2 läuft weiter. | `STORE_KANAL_CAP = 256`; Einträge bis einschließlich `STORE_KANAL_CAP` zulässig, `STORE_KANAL_CAP + 1` fail-closed. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1903-1916,3556-3569,3787-3790`; `CLAUDE.md:39-40,128-130`; `docs/beweise/SONDE-011.md:615` (L-08). | A4-Rust `store_crash_matrix::store_kanal_cap_und_naechster_trennt`, `disk_full_io_kaputt_neues_schema_degradieren_ohne_brokerstop`; A4-SI `blockierter_store_writer_bestaetigt_nichts_und_p2_laeuft`. |
| S-04 | Eventlog vorhanden, Projektion fehlt/ist alt | Projektion-Rebuild | **ZUSAGE:** Domänentabellen sind neu aufbaubare Projektionen oder immutable Artefakte; Rebuild verändert `event_log` nicht und liefert denselben fachlichen Endstand wie Live-Projektion. Kein hartes Wire-Budget. | Testgrenze `STORE_RECOVERY_TEST_MAX_MS = 5000` für Migration plus Recovery des Testkorpus. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3804-3807,4173`; `docs/beweise/SONDE-011.md:616` (L-09). | A4-Rust `store_crash_matrix::projektion_rebuild_ist_deterministisch_und_unter_testgrenze` besteht bei `STORE_RECOVERY_TEST_MAX_MS - 1`; injizierte Gleichheit fällt. |
| S-05 | Writer startet / Batch offen | PRAGMA-Konfiguration, Event oder Commitgrenze | **ZUSAGE:** `foreign_keys=ON`, `journal_mode=WAL`, `synchronous=FULL`, `busy_timeout=BUSY_TIMEOUT_MS`, `wal_autocheckpoint=0`. Group-Commit erfolgt beim ersten Auslöser: monotones Fenster oder Batchcap; nie Einzel-fsync je Event. | `BUSY_TIMEOUT_MS = 2000`; `COMMIT_FENSTER_MS = 50`; `COMMIT_BATCH_MAX = 64`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1834-1837,3808-3811`; `docs/beweise/SONDE-011.md:599,616` (V-04/L-09). | A4-Rust `store_crash_matrix::pragmas_exakt`, `commit_bei_fenster_oder_batch`: bei `COMMIT_FENSTER_MS - 1` und `COMMIT_BATCH_MAX - 1` noch offen, bei exakt einer Grenze committed; `busy_timeout_grenze` löst vor `BUSY_TIMEOUT_MS` nicht aus und an der Grenze aus. |
| S-06 | Leser/aktive Capture-Session/WAL | Lesen, externe Arbeit oder Checkpoint | **ZUSAGE — REVIDIERT 2026-08-31:** Leser halten nur kurze Snapshots und keine Transaktion über Pipe-, UI- oder sonstige externe Arbeit. `PASSIVE` bei Store-Idle oder WAL-Größe; `TRUNCATE` nur ohne Leser und ohne aktive Capture-Session; DB/WAL ausschließlich lokal. Idle heißt zugleich keine Events über die Frist und keine aktive Capture-Session. | `WAL_SCHWELLE_BYTES = 4 * 1024 * 1024 = 4194304`; `STORE_IDLE_MS = 5000`; `CHECKPOINT_BUSY_ERWARTET = 0`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3568-3569,3812-3816`; `docs/beweise/SONDE-011.md:616` (L-09). | A4-Rust `store_crash_matrix::passive_checkpoint_an_dualer_grenze`: bei `WAL_SCHWELLE_BYTES - 1`/`STORE_IDLE_MS - 1` keiner, an einer exakten Grenze PASSIVE; `truncate_nur_in_ruhelage`; `store_crash_matrix::leser_haelt_keine_transaktion_ueber_externe_arbeit` blockiert simulierte externe Arbeit an einer Barriere und verlangt währenddessen `wal_checkpoint(PASSIVE)` mit `busy = CHECKPOINT_BUSY_ERWARTET` und `log_frames = checkpointed_frames`. Die Mutation mit absichtlich über der Barriere offener Lesertransaktion muss rot sein, weil die WAL nicht vollständig eingezogen wird; ein Writer-Commit allein ist ausdrücklich kein Beweis. |
| S-07 | Store fehlt/ist älter als Plugin-State | Reload/Reconnect | **ZUSAGE:** lokaler Plugin-State bleibt Wahrheit für Identität und bestätigten DSP. Fehlende DB kostet nur Historie/Ranking; ein alter Storeeintrag überschreibt nie neueren Plugin-State. | `AUDIO_THREAD_STORE_WAIT_MS_MAX = 0`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:1458-1462,1903-1916`; `CLAUDE.md:128-130`. | B2 für State-Roundtrip; A4-Rust `store_crash_matrix::alter_store_ueberschreibt_plugin_state_nicht`; A4-SI prüft exakt `AUDIO_THREAD_STORE_WAIT_MS_MAX`. |

### Migration 1 — Spalten und Schlüssel

Diese vom Bauer nach angenommenem V-03 festgelegte Minimalform ist vor dem Bau
bindend; zusätzliche fachliche Spalten verlangen eine erneute Matrixänderung.

| Tabelle | Spalten | Schlüssel/Regel | Grenztest |
|---|---|---|---|
| `schema_migrations`<br>`conflict_guards` | Migration: `major INTEGER`, `checksum_sha256 TEXT`, `applied_utc_ms INTEGER`, jeweils nicht null.<br>Guard: `effective_address TEXT`, `derived_id TEXT`, `created_utc_ms INTEGER`, jeweils nicht null. | Migration-Primärschlüssel `major`, genau `STORE_SCHEMA_MAJOR = 1`.<br>Guard-Primärschlüssel `(effective_address, derived_id)`; eigenes dauerhaftes Sicherheitsregister, ausdrücklich keine Eventwahrheit; Löschung nur durch explizite Neu-ID-Auflösung. | `store_crash_matrix::migration_1_spalten_und_schluessel_exakt`; `STORE_SCHEMA_MAJOR + 1` degradiert. `konfliktriegel_ueberlebt_neustart_mit_nur_einem_partner`; Restore vor Routing, Eviction löscht nicht. |
| `event_log` | `event_ord INTEGER`, `event_uuid TEXT`, `command_id TEXT` nullable, `project_binding_id TEXT`, `session_epoch TEXT`, `utc_ms INTEGER`, `broker_epoch TEXT`, `sequence INTEGER`, `event_type TEXT`, `schema_major INTEGER`, `schema_minor INTEGER`, `payload_jcs BLOB`; außer `command_id` alles nicht null | Primärschlüssel `event_ord`; `event_uuid` unique; `command_id` unique, wenn gesetzt und damit der gewählte interne Idempotenzschlüssel; `(broker_epoch, sequence)` unique; Index `(project_binding_id, session_epoch, event_ord)`; append-only. Die Event-UUID bleibt Storewahrheit und wird weder in `command_ack` noch in `session_snapshot` serialisiert. | `store_crash_matrix::event_log_keys_und_append_only`; Retry derselben `command_id` liefert intern dieselbe `event_uuid` ohne zweiten Satz; Duplicate von UUID oder `(broker_epoch, sequence)` fällt; Q1/A4-SI `event_uuid_bleibt_aus_ack_und_session_snapshot_wire_fern`. |
| `projects` | `project_binding_id TEXT`, `last_event_ord INTEGER`, `state_jcs BLOB`, jeweils nicht null | Primärschlüssel `project_binding_id`; `last_event_ord` referenziert `event_log`. | `store_crash_matrix::projects_projection_rebuild_und_fk`. |
| `sessions` | `project_binding_id TEXT`, `session_epoch TEXT`, `last_event_ord INTEGER`, `state_jcs BLOB`, jeweils nicht null | Primärschlüssel `(project_binding_id, session_epoch)`; `last_event_ord` referenziert `event_log`; Phase B speichert/restauriert hier keine Führung. | `store_crash_matrix::sessions_projection_composite_key_ohne_fuehrungsrestore`; A4-Rust `brokerneustart_ohne_mainprojectstate_ingress_gibt_fuehrung_frei`. |
| `passages`, `evidence`, `findings`, `proposals`, `transactions`, `experiments`, `user_verdicts` | jeweilige `*_id TEXT`, `project_binding_id TEXT`, `session_epoch TEXT`, `last_event_ord INTEGER`, `state_jcs BLOB`, jeweils nicht null | Primärschlüssel jeweilige `*_id`; `last_event_ord` referenziert `event_log`; Index `(project_binding_id, session_epoch, last_event_ord)`. | Parametrisierter A4-Rust `store_crash_matrix::domainprojektionen_spalten_keys_und_rebuild`. |
| `experiment_events` | `experiment_id TEXT`, `event_uuid TEXT`, beide nicht null | Primärschlüssel `(experiment_id, event_uuid)`; beide Fremdschlüssel auf `experiments` beziehungsweise `event_log`; immutable Relation. | `store_crash_matrix::experiment_events_composite_key_und_fks`. |
| `outbox` | `target_project_binding_id TEXT`, `target_session_epoch TEXT`, `target_instance_id TEXT`, `object_key TEXT`, `snapshot_event_ord INTEGER`, `write_attempts INTEGER`, jeweils nicht null | Primärschlüssel `(target_project_binding_id, target_session_epoch, target_instance_id, object_key)`; `snapshot_event_ord` referenziert `event_log(event_ord)`. UPSERT ersetzt je Ziel/Objektschlüssel nur durch einen neueren geschuldeten Snapshot-Stand; weder Event-UUID noch `confirmed`-/ACK-Zustand. Nach vollständigem Snapshot-Write darf die Schuld kompaktieren, weil Reconnect stets aus der Projektion den aktuellen absoluten Snapshot bildet. | `store_crash_matrix::outbox_koalesziert_letzten_snapshotstand_pro_ziel_und_objekt`, `snapshot_outbox_kompaktierung_verliert_committete_wirkung_nicht`; A4-SI `session_subscription_hat_nur_snapshot_push`. |

## B.5 Verhaltensmatrix — Outbox und Prioritäten

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| O-01 | Persistenzpflichtiger P0-Befehl mit `command_id` | Wire-Write, Store-Commit, `command_ack` oder Verbindungsverlust | **ZUSAGE — REVIDIERT 2026-08-31:** Vor dem Senden übernimmt ein clientseitiges In-Flight-Register oberhalb der Queue den logischen Auftrag. A-P0-05 gibt nach erfolgreichem Wire-Write nur den Queueplatz frei. `command_ack(ergebnis = angewandt)` gibt das Register als Erfolg frei; nach verlorenem ACK gibt der persistente Idempotenzschlüssel beim Retry mit `ergebnis = idempotent_wiederholt` ebenfalls als Erfolg frei. `abgelehnt`, `konflikt` und `abgelaufen` geben endgültig beantwortet, aber nicht erfolgreich frei. Verbindungsverlust vor einem ACK reiht dieselbe `command_id` erneut ein. Das ACK referenziert `command_id`; die zugehörige Event-UUID bleibt rein intern und erscheint nie auf dem Wire. Voller/degradierter Store verweigert Annahme und trennt den Verursacher; P2 bleibt aktiv. | `STORE_KANAL_CAP = 256`; bis `STORE_KANAL_CAP` zulässig, `STORE_KANAL_CAP + 1` fail-closed. | `docs/beweise/SONDE-010.md:136-140`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:1863-1866,3782-3784,3804-3819`; `docs/beweise/SONDE-011.md:615,618` (L-08/L-11). | A4-SI mit echtem C++-Client `command_ack_angewandt_und_idempotent_wiederholt_geben_inflight_als_erfolg_frei`, `command_ack_endgueltige_fehler_geben_ohne_erfolg_frei`, `brokerkill_vor_ack_reiht_dieselbe_command_id_wieder_ein`, `command_ack_traegt_keine_event_uuid`; A4-Rust `store_crash_matrix::command_id_mappt_intern_auf_eine_event_uuid`; K-01 bis K-03. |
| O-02 | `subscribe_session` ohne Cursor oder UUID-Feld | Erst-/Reconnect-Subscribe, Snapshot-Write oder Subscriber-Kill | **ZUSAGE — REVIDIERT 2026-08-31:** Einziger Wire-Abfluss dieser Subscription ist der aktuelle `session_snapshot`-Push. Es gibt weder Event-Replay an Subscriber noch Empfänger-Dedupe oder Event-UUID auf diesem Wire-Weg. Jeder Subscribe/Reconnect liefert den aktuellen absoluten Snapshot; letzter Snapshot gewinnt, absolutes Setzen ist idempotent. Dadurch geht eine committete Wirkung nicht verloren und wiederholte Snapshots wenden sie nicht doppelt an. Ein Pipe-Write bleibt keine Zustell-/Wirkungswahrheit. | `SESSION_SUBSCRIPTION_EVENT_REPLAY_MAX = 0`; der Vertragstest verlangt exakt null Event-Replay-Nachrichten. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3804-3819,4131`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-SI `session_subscription_sendet_nur_aktuellen_absoluten_snapshot`, `wiederholter_snapshot_letzter_gewinnt_idempotent`, `reconnect_snapshot_enthaelt_committete_wirkung_genau_einmal`; Q1/A4-Rust `session_subscription_hat_keinen_event_uuid_wire_traeger`; K-04 bis K-07. |
| O-03 | Koaleszierte Snapshot-Schuld und Projektion vorhanden | neuer Projektionsstand, Snapshot-Push, Recovery oder Kompaktierung | **ZUSAGE — REVIDIERT 2026-08-31:** Die Outbox hält je Ziel und Objektschlüssel ausschließlich den neuesten geschuldeten `snapshot_event_ord`; ein neuerer Stand ersetzt den älteren atomar. Der Broker rekonstruiert daraus beziehungsweise bei jedem Reconnect den aktuellen absoluten Snapshot aus der Projektion. Nach vollständigem Write eines deckenden Snapshots darf die Schuld kompaktieren, ohne Empfängerwirkung zu behaupten; Reconnect bleibt unabhängig von der Outboxzeile verlustfrei. Kein Timer-Retry in derselben Verbindung und keine Event-FIFO-Zusage an Subscriber. Zielidentität bleibt `project_binding_id + session_epoch + instance_id`, nie Nonce. | `STORE_RECOVERY_TEST_MAX_MS = 5000` ist nur Testgrenze; Recovery des Testkorpus strikt darunter. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3785,3816-3819,4131,4173`; `docs/beweise/SONDE-011.md:616-617` (L-09/L-10). | A4-Rust `store_crash_matrix::outbox_koalesziert_letzten_snapshotstand_pro_ziel_und_objekt`, `snapshot_outbox_kompaktierung_verliert_committete_wirkung_nicht`, `recovery_unter_testgrenze`; A4-SI `jeder_resubscribe_liefert_aktuellen_absoluten_snapshot`. |
| O-04 | Snapshot-Schuld liegt in Store-/Zwischenpuffer | gleicher Objektschlüssel, Rückstau oder Writefehler | **ZUSAGE — REVIDIERT 2026-08-31:** Eventlog und Projektion bleiben haltbar; Snapshotbytes werden daraus rekonstruiert. Snapshot-Schuld und P1-Zwischenpuffer koaleszieren nach Objektschlüssel. Normaler P1-Rückstau darf ohne Reconnect abfließen; nach Writefehler liefert der nächste Subscribe wieder den aktuellen Vollsnapshot. Nicht koaleszierbare Events bleiben interne Store-Wahrheit: In Phase B besitzen sie keinen Produkt-Consumer und keine Wire-Zustellzusage; Evidenz an Main und weitere Consumer-Träger gehören S18–19/S29–31. | `CAP_P1 = 128`; `CAP_P1` zulässig, `CAP_P1 + 1` folgt Koaleszenz oder fail-closed statt stillem Verlust. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3785`; `broker/src/transport/warteschlange.rs:21-24,90-112,156-229`; `tools/dirigent/pruefliste.md:12-24`; `docs/beweise/SONDE-011.md:617` (L-10). | Vorhandene A4-Rust-P1-Tests; B10; A4-SI `p1_snapshot_abfluss_ohne_reconnect_aber_writefehler_liefert_aktuellen_snapshot`, `phase_b_hat_keinen_wire_consumer_fuer_nicht_koaleszierbare_events`; Grenztest bei `CAP_P1`/`CAP_P1 + 1`. |

| Klasse | Zustand bei voll | Verbindliche Wirkung | Quelle | Beweisbein |
|---|---|---|---|---|
| P0 Control, `CAP_P0 = 64` | kein Platz / persistenter Befehl geschrieben, aber semantisch unbestätigt | Nichts verwerfen; bei vollem P0 Verbindung schließen, Preview lokal auslaufen lassen. A-P0-05 gibt nach Wire-Write nur den Queueplatz frei. Das In-Flight-Register endet erfolgreich bei `angewandt`/`idempotent_wiederholt`, endgültig ohne Erfolg bei `abgelehnt`/`konflikt`/`abgelaufen`; Verbindungsverlust davor reiht dieselbe `command_id` wieder ein. | `docs/beweise/SONDE-010.md:136-140`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3785`; `broker/src/transport/warteschlange.rs:21-24`; `tools/dirigent/pruefliste.md:20-26`. | A4-Rust `p0_verwirft_nie_und_meldet_ueberlauf` bei `CAP_P0`/`CAP_P0 + 1`; A4-SI `queueplatz_frei_aber_semantischer_auftrag_bleibt_inflight`, `alle_command_ack_ergebnisse_beenden_inflight_korrekt`; B10. |
| P1 Zustand/Evidenz, `CAP_P1 = 128` | gleicher Snapshot / anderes Ereignis | Session-Snapshot nach Objektschlüssel an seiner Position ersetzen. Nicht koaleszierbare Events bleiben in Phase B interne Store-Wahrheit ohne Produkt-Consumer/Wire-Träger; S18–19/S29–31 besitzen ihre spätere Zustellung. Ist am Ingress kein P2 zum Opfern vorhanden, nie still droppen; heutige fail-closed-Politik trennt. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3785`; `broker/src/transport/warteschlange.rs:21-24,90-112,156-229`; `tools/dirigent/pruefliste.md:14-24`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-Rust P1-Tests bei `CAP_P1`/`CAP_P1 + 1`, `session_snapshot_koalesziert_nach_objektschluessel` und `ingress_voll_ohne_p2_trennt_auch_bei_p1`; B10. |
| P2 Live pro Probe, `CAP_P2_JE_SONDE = 2` | kein Platz | Ältesten ungesendeten Frame ersetzen; Analyse degradiert, nie Control. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3785`; `broker/src/transport/warteschlange.rs:21-24`. | A4-Rust `p2_ersetzt_den_aeltesten_ungesendeten` bei `CAP_P2_JE_SONDE`/`CAP_P2_JE_SONDE + 1`; B10/A22. |
| Broker-Ingress pro Verbindung, `CAP_INGRESS = 256` | kein Platz | Ältesten P2 zuerst droppen; P0-Überlauf trennt. Getrennter P0-Verbraucher beantwortet weiter, während P1/P2 blockiert. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3780-3790`; `broker/src/transport/warteschlange.rs:21-24`; `tools/dirigent/pruefliste.md:20-26`. | A4-Rust `ingress_droppt_p2_zuerst_und_trennt_bei_p0` bei `CAP_INGRESS`/`CAP_INGRESS + 1`, `p0_wird_beantwortet_waehrend_p1_die_senke_blockiert`; A22. |

## B.6 Verhaltensmatrix — Broker-Autostart (NAK-13)

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Frist/Grenze | Quelle | Test/Beweisbein |
|---|---|---|---|---|---|---|
| A-01 | Beliebige Instanz, Brokerstatus unbekannt | v3-Verbindungsversuch | **ZUSAGE:** immer zuerst Connect-without-spawn auf der SID-Pipe. Fehlender Broker ist notwendige, nie hinreichende Startbedingung; `ControlClient` selbst startet keinen Prozess. Der normale Reconnectpfad bleibt vom kürzeren Post-Spawn-Retry getrennt. | Bestehend `kBackoffStartMs = 500`, `kBackoffMaxMs = 8000`; erster Versuch an der Startgrenze, Sättigung an der Maximalgrenze. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3191-3199`; `eq-copilot/plugin/core/ipc/ControlClient.h:8-18`; `eq-copilot/plugin/core/ipc/IpcVerbindung.h:34-36`. | B10 `connect_without_spawn_kommt_zuerst`, `normaler_reconnect_backoff_start_und_max`; B8 `launcher_bleibt_vor_gate_unberuehrt`. |
| A-02 | Connect scheitert | Lifecycle-Gate wird geprüft | **ZUSAGE:** nur `state::Lebenslauf::darfBrokerStarten()` darf freigeben: State-Restore/Klassifikation abgeschlossen, positiv `main`, Editor offen. Scanner, Offline-Render, Legacy, Probe, unclassified und Main mit geschlossenem Editor spawnen nie. | Prüfung/Spawn außerhalb Audiothread; keine Audiowartezeit. | `docs/plan/plan.json:195`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3193-3201,3628-3640`; `eq-copilot/plugin/state/NakamaLebenslauf.h:117-127`. | Vorhandenes B8 plus `brokerstart_gate_alle_negativzustaende`; B10 Prozess-Gegenprobe. |
| A-03 | Gate offen, Broker fehlt | Startanforderung | **ZUSAGE — REVIDIERT 2026-08-31:** Vor jedem Spawn stimmt SHA-256 des installierten Broker-Binaries mit dem Installer-Manifest überein. Ist `authenticode_thumbprint` gesetzt, müssen zusätzlich `WinVerifyTrust` erfolgreich sein und der Signer-Thumbprint exakt passen; fehlende, ungültige oder fremde Signatur sperrt den Start. Ist das Feld `null`, erlaubt Phase B sichtbar gezählt den SHA-256-Mindestschutz, weil Zertifikat/Befüllung NAK-119/S34–35 gehören. Erst danach startet der Lifecycle-Worker den absoluten Pfad per `CreateProcessW` ohne Shell, verborgen und per-User-Mutex-idempotent. | `SPAWN_CONNECT_BACKOFF_START_MS = 250`; `SPAWN_BEREIT_TIMEOUT_MS = 10000`; `SPAWN_COOLDOWN_MS = 30000`; Grenzen jeweils Konstante minus eins/exakt. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3193-3201`; `eq-copilot/install/nakama-installer-v1.json:8-17,48-57`; `docs/beweise/SONDE-011.md:619` (L-12); `docs/offene-punkte.md:16` (NAK-119). | B10 `autostart_hash_vor_signatur_vor_spawn`, `thumbprint_gesetzt_verlangt_winverifytrust_und_exakten_signer`, `thumbprint_null_erlaubt_nur_passenden_hash`, `autostart_parallel_startet_genau_einen_prozess`, `spawn_retry_bereit_timeout_und_cooldown_grenzen`; A17 mit Testzertifikat: gültig, fehlend, falsch. |
| A-04 | Broker läuft; weitere Mains/Probes verbinden | weitere Startanforderung | **ZUSAGE:** kein zweiter Broker; Verlierer des Mutex-/Pipe-Rennens verbindet zum vorhandenen Prozess. Sonden bleiben reine Connectoren. | `BROKER_PRO_USER_MAX = 1`; keine globale Maschineninstanz. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3184-3201`; `broker/src/main.rs:6-10`. | B10 `autostart_mehrere_mains_ein_broker` mit `BROKER_PRO_USER_MAX` und konkurrierendem Zusatzstarter; B8 beweist, dass Probes nie anfordern. |
| A-05 | Letzter Client geht; Broker hat keine Clients | Idle-Selbstende / neuer Client | **ZUSAGE:** Start und Stopp entstehen im selben Änderungssatz, aber nur der Broker beendet sich selbst. Das Plugin beendet nie einen fremden Broker. Ein neuer Client setzt die Idlefrist zurück; aktive Clients verhindern das Ende. Kein Stopp entsteht im Audiothread. | `BROKER_IDLE_ENDE_MS = 60000`; bei `BROKER_IDLE_ENDE_MS - 1` kein Ende, bei exakt `BROKER_IDLE_ENDE_MS` ohne Clients Selbstende. | `docs/plan/plan.json:195`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3199-3201`; `CLAUDE.md:68-70`; `tools/dirigent/pruefliste.md:28-38`; `docs/beweise/SONDE-011.md:619` (L-12). | B8 `brokerstop_nie_im_processblock`; B10 `letzter_client_idle_stop_an_grenze`, `aktiver_fremdclient_verhindert_stop`, `client_resetet_idlefrist`, `stop_waehrend_spawn`. |
| A-06 | Phase-B-Paket installiert | `Install-Nakama.ps1 -Rueckweg` | **ZUSAGE:** NAK-13 erzeugt kein Installer-/Boot-Autostartartefakt. Der vorhandene Installer installiert den Broker bewusst und sein vorhandener Rückweg restauriert dessen vorige Bytes; es gibt kein zusätzliches Startartefakt zu entfernen. | `AUTOSTART_ARTEFAKTE_PHASE_B = 0`; Installation bleibt bewusster Admin-Schritt. | `docs/plan/plan.json:195`; `CLAUDE.md:68-70`; `eq-copilot/install/Install-Nakama.ps1:24-39,55-68`; `eq-copilot/install/nakama-installer-v1.json:58-64`; `docs/beweise/SONDE-011.md:619` (L-12). | A17 Manifeststruktur; A18 Sandbox-Rückweg; A-06 `kein_installer_oder_boot_autostartartefakt` prüft exakt `AUTOSTART_ARTEFAKTE_PHASE_B`. |
| A-07 | Audiocallback läuft, auch während Start/Stop/Fehler | beliebig viele Audioblöcke | **ZUSAGE:** keine Sperre, Allokation, Datei-, Pipe-, Prozess- oder Logoperation; Audio bleibt sampleidentisch und wartet nie auf Broker/Store. | `AUDIO_THREAD_BROKER_OPERATIONEN_MAX = 0`. | `CLAUDE.md:36-40,128-130`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3196-3201,3561-3565`. | B8 `broker_lifecycle_aufrufe_im_audiothread_null` prüft exakt `AUDIO_THREAD_BROKER_OPERATIONEN_MAX` mit Gegenprobe; A1/B4 bleiben bitgleich und allokationsfrei. |

## B.7 Killmatrix — Outbox, Migration und WAL

Persistenzgrenzen laufen als echte Child-Prozess-Kills in `store_crash_matrix`;
ein bloß zurückgegebener Fehler ersetzt den Kill nicht. K-01 bis K-03 nutzen
zusätzlich in A4-SI den echten C++-Client und die fünf schemafesten ACK-Werte.
K-04 bis K-07 prüfen Commit vor Snapshot-Push, Subscriber-Kill vor/nach
absoluter Anwendung und Kill vor/nach Snapshot-Outbox-Kompaktierung. Es gibt
keinen Event-Replay oder UUID-Träger auf diesem Wire-Weg; Quellen: `docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4131,4173`
und `docs/beweise/SONDE-011.md:617-618`.

| Killpunkt | Haltbarer Stand beim Kill | Erwartung nach Neustart | Genau-einmal-Beweis | Quelle | Test |
|---|---|---|---|---|---|
| K-01 vor Persist/Store-Commit | weder interne `command_id`-Zuordnung noch Event/Projektion/Snapshot-Schuld haltbar; C++-Auftrag In-Flight | Kein ACK ging nach außen. Broker-Kill trennt den echten C++-Client; er reiht dieselbe `command_id` wieder ein. Der erste erfolgreiche Commit erzeugt genau eine interne Event-UUID und antwortet `ergebnis = angewandt`. | Vor Retry kein committed Datensatz; danach eine `command_id`, eine interne Event-UUID, eine Wirkung; ACK ohne UUID. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4131`; `docs/beweise/SONDE-010.md:136-140`; `docs/beweise/SONDE-011.md:618` (L-11). | A4-SI `store_crash_matrix::kill_vor_persist_mit_echtem_cpp_client_antwortet_angewandt_ohne_wire_uuid`. |
| K-02 während `BEGIN IMMEDIATE`, vor Commit | Zuordnung/Event/Projektion/Snapshot-Schuld uncommitted; C++-Auftrag In-Flight | Recovery verwirft die ganze Transaktion. Der echte C++-Client wiederholt dieselbe `command_id`; der spätere Commit antwortet `ergebnis = angewandt`, ohne Teilsatz oder zweite Wirkung. | Alle vier Storeteile gemeinsam abwesend oder committed; danach genau eine interne Event-UUID, ACK ohne UUID. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3804-3819,4131`; `docs/beweise/SONDE-011.md:618` (L-11). | A4-SI plus A4-Rust `store_crash_matrix::kill_waehrend_commit_mit_echtem_cpp_client_antwortet_angewandt`. |
| K-03 nach Commit, vor `command_ack` | interne `command_id → event_uuid`, Event, Projektion und Snapshot-Schuld committed; C++-Auftrag noch In-Flight | Das verlorene `ergebnis = angewandt` führt zum Retry derselben `command_id`; der Broker findet den Idempotenzschlüssel und antwortet `ergebnis = idempotent_wiederholt`, ohne zweites Event. Beide ACK-Varianten referenzieren nur `command_id`, nie die Event-UUID. | Zwei Befehlsversuche, eine interne Zuordnung/Event-UUID und eine Wirkung; `idempotent_wiederholt` gibt In-Flight frei. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:1863-1866,3804-3819`; `docs/beweise/SONDE-011.md:618` (L-11). | A4-SI `store_crash_matrix::kill_nach_commit_vor_command_ack_retry_antwortet_idempotent_wiederholt_ohne_wire_uuid`. |
| K-04 Kill nach Store-Commit, vor Snapshot-Push | Event, Projektion und koaleszierte Snapshot-Schuld committed; noch kein Snapshot geschrieben | Nach Brokerneustart oder Reconnect wird aus der Projektion der aktuelle absolute Snapshot gepusht; er enthält die committete Wirkung genau einmal. | Commit bleibt haltbar; vor Kill null Snapshot-Writes, danach enthält der erste empfangene Snapshot die Wirkung einmal. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4131`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-Rust/A4-SI `store_crash_matrix::kill_nach_store_commit_vor_snapshot_push`. |
| K-05 Subscriber-Kill vor oder nach Snapshot-Anwendung | aktueller absoluter Snapshot geschrieben; Empfänger hat ihn je Variante noch nicht oder bereits angewandt | Reconnect liefert erneut den aktuellen absoluten Snapshot. Vorher nicht angewandt wird die Wirkung hergestellt; vorher schon angewandt bleibt sie durch absolutes Setzen unverändert. | Beide Varianten enden mit genau einer Zustandswirkung; kein Event-Replay und kein Dedupefenster. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-SI `store_crash_matrix::subscriberkill_vor_und_nach_snapshot_anwendung_bleibt_idempotent`. |
| K-06 Kill vor Outbox-Kompaktierung | deckender Snapshot vollständig geschrieben; koaleszierte Snapshot-Schuld noch vorhanden | Nach Neustart darf die verbliebene Schuld erneut einen aktuellen Snapshot auslösen; Wiederholung setzt denselben absoluten Zustand, danach darf die Zeile ohne Verlust kompaktieren. | Snapshot vor/nach Kill enthält die committete Wirkung; wiederholtes Setzen erzeugt keine Doppelwirkung. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-Rust/A4-SI `store_crash_matrix::kill_vor_snapshot_outbox_kompaktierung`. |
| K-07 Kill nach Outbox-Kompaktierung | Projektion enthält die Wirkung; gedeckte Snapshot-Schuld ist gelöscht | Recovery/Reconnect rekonstruiert unabhängig von der gelöschten Outboxzeile den aktuellen absoluten Snapshot mit der Wirkung. | Erster empfangener Snapshot enthält die Wirkung genau einmal; keine verlorene Wirkung und keine Doppelanwendung. | `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4131`; `docs/beweise/SONDE-011.md:617` (L-10). | A4-Rust/A4-SI `store_crash_matrix::kill_nach_snapshot_outbox_kompaktierung_snapshot_traegt_wirkung`. |
| K-08 während Migration 1 | keine haltbare Migration-1-Markierung oder uncommittierte Migration zu `STORE_SCHEMA_MAJOR = 1` | Recovery sieht vollständig den Vormigrationsstand oder vollständig `STORE_SCHEMA_MAJOR`, rollt zurück oder führt Migration idempotent fort und verarbeitet danach pending Events genau einmal. | Kein Teilschema; `schema_migrations.major` ist vor der Migration abwesend, danach exakt `STORE_SCHEMA_MAJOR`; Recovery unter `STORE_RECOVERY_TEST_MAX_MS = 5000`. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3799-3803,4173`; `docs/beweise/SONDE-011.md:614,616` (L-07/L-09). | A4-Rust `store_crash_matrix::kill_waehrend_migration_1` mit beiden Schema- und Zeitgrenzen. |
| K-09 während WAL-Replay/Recovery | letzter FULL-Commit plus WAL | Recovery stellt genau committed Transaktionen her; uncommittete fehlen. Projektionen, `command_id`-Zuordnungen und Outbox werden atomar versöhnt; Replay beginnt mit Snapshot-Cut statt Writestatus. | Integritätscheck; Migration plus Recovery `< STORE_RECOVERY_TEST_MAX_MS = 5000`, injizierte Gleichheit fällt. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3804-3819,4173`; `docs/beweise/SONDE-011.md:616-618` (L-09/L-10/L-11). | A4-Rust `store_crash_matrix::kill_waehrend_wal_replay`. |

## B.8 Angenommene Technikvorschläge — Dirigentenentscheid 2026-08-31

| ID | Angenommene Technik | Begründung / gebundene Grenze und Test |
|---|---|---|
| **ANGENOMMEN V-01** | `RUSQLITE_VERSION = "0.40.2"`; Cargo pinnt exakt `rusqlite = { version = "=0.40.2", features = ["bundled"] }`. Der Broker protokolliert beim Storestart die aktive SQLite-Version. | Vermeidet Laufzeit-DLL-Suche und bindet reproduzierbare WAL-/PRAGMA-Semantik. A4-Rust `rusqlite_version_0_40_2_ist_exakt_gepinnt` prüft Cargo-Metadaten gegen `RUSQLITE_VERSION`; `store_start_protokolliert_sqlite_version` prüft den Laufzeitwert ([docs.rs `rusqlite` 0.40.2](https://docs.rs/crate/rusqlite/0.40.2); `docs/FL-Nakama-Sonden-Design-Entwurf.md:3566-3569,3808-3816`; `broker/Cargo.toml:11-34`). |
| **ANGENOMMEN V-02 — REVIDIERT 2026-08-31** | Ein eigener `StoreWriter`-Thread besitzt die Write-Connection und den begrenzten `sync_channel`; Befehle sind `AppendBatch`, `CompactSnapshotDebt`, `PersistConflictGuard`, `ResolveConflictGuard`, `Checkpoint`, `Shutdown`. Leser verwenden nur kurze eigene Snapshots. Coordinator und I/O halten beim Senden weder Coordinator- noch Storelock. | Macht Single-Writer, dauerhaften Konfliktriegel und fehlenden Subscriber-ACK-Zustand strukturell prüfbar. `STORE_KANAL_CAP = 256`; A4-Rust `store_writer_ist_alleiniger_write_connection_besitzer`, `store_kanal_cap_und_naechster_trennt` und S-06 prüfen Besitz sowie `STORE_KANAL_CAP`/`STORE_KANAL_CAP + 1` (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3556-3569,3787-3790,3811-3816`). |
| **ANGENOMMEN V-03 — REVIDIERT 2026-08-31** | Interne `command_id`-Zuordnung, `event_log`-Append, Projektion und UPSERT der neuesten Snapshot-Schuld laufen in einer `BEGIN IMMEDIATE`-Transaktion nach B.4. Es gibt keinen Subscriber-Event-/`confirmed`-/ACK-Stand. Outbox-Kompaktierung ist eine eigene Writer-Transaktion nach vollständigem Snapshot-Write; die Projektion bleibt Rekonstruktionswahrheit. `conflict_guards` bleiben ein eigenes Sicherheitsregister außerhalb der Eventwahrheit. | Kleinste atomare Form für K-02 bis K-07. A4-Rust `event_projection_snapshotschuld_command_id_atomare_transaktion` killt unmittelbar vor/nach Commit und `snapshot_outbox_kompaktierung_verliert_committete_wirkung_nicht` verriegelt Cleanup; A4-SI misst ausschließlich absoluten, idempotenten Snapshot-Resync ohne UUID-Dedupe (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3804-3819`; `docs/beweise/SONDE-011.md:617-618`, L-10/L-11). |
| **ANGENOMMEN V-04** | Group-Commit löst bei `COMMIT_FENSTER_MS = 50` **oder** `COMMIT_BATCH_MAX = 64` aus. PASSIVE-Checkpoint löst bei `WAL_SCHWELLE_BYTES = 4 * 1024 * 1024 = 4194304` **oder** `STORE_IDLE_MS = 5000` ohne Events und ohne aktive Capture-Session aus; TRUNCATE nur in echter Ruhe. | Duale Trigger verhindern Daueraufschub. Fake-Clock-/Dateigrößentests prüfen jeweils Konstante minus eins/exakt sowie beim Idle-Trigger Capture aktiv/inaktiv (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3808-3816`; `docs/beweise/SONDE-011.md:616`, L-09). |
| **ANGENOMMEN V-05** | Injizierbare `MonotonicClock` nur für Tests; Produktion kapselt ausschließlich Rust `Instant`. Opferwahl mit `SESSION_CLIENT_CAP = 64` und `GLOBAL_CLIENT_CAP = 128`: stale vor fresh, dann ältestes `last_seen`, dann lexikographisch vollständige effektive Adresse; bei ausschließlich fresh Clients wird der neue abgewiesen. | Schlaflose, deterministische Grenztests. `coordinator_model::caps_stale_first_deterministisch` prüft je Cap die Konstante und `CAP + 1` über permutierte Einfügereihenfolge (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3207-3212`; `docs/beweise/SONDE-011.md:612`, L-05). |
| **ANGENOMMEN V-06 — REVIDIERT 2026-08-31** | Ein pluginseitiger Lifecycle-Worker prüft immer manifestgebundenes SHA-256. Bei gesetztem `authenticode_thumbprint` verlangt er zusätzlich erfolgreiche `WinVerifyTrust`-Prüfung und exakt passenden Signer-Thumbprint; bei `null` bleibt SHA-256 der Mindestschutz. Erst danach startet er per `CreateProcessW` mit absolutem Pfad/argv und verborgenem Fenster; ein benannter per-User-Mutex schützt den Start. Nur der Broker beendet sich selbst, das Plugin nie einen fremden Broker. | Zusammen mit L-12: `SPAWN_CONNECT_BACKOFF_START_MS = 250`, `SPAWN_BEREIT_TIMEOUT_MS = 10000`, `SPAWN_COOLDOWN_MS = 30000`, `BROKER_IDLE_ENDE_MS = 60000`. B10 prüft jede Konstante minus eins/exakt sowie Hashfehler und konkurrierende Starter; A17 prüft mit Testzertifikat gültig/fehlend/falsch (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3193-3201`; `docs/beweise/SONDE-011.md:619`, L-12). |
| **ANGENOMMEN V-07 — REVIDIERT 2026-08-31** | `store_crash_matrix` startet einen kleinen Store-Worker als Child, synchronisiert Store-Killpunkte über test-only Barrieren/Marker, killt hart und öffnet dieselbe DB neu. K-01 bis K-03 koppeln die Commitseiten in A4-SI an den echten C++-Client und die schemafesten ACK-Werte; K-04 bis K-07 prüfen Commit vor Snapshot-Push, Subscriber vor/nach absoluter Anwendung sowie Outbox vor/nach Kompaktierung; K-08/K-09 prüfen Migration/WAL. | Ein simulierter `Err` beweist weder WAL-Recovery noch Prozess-/Client-Killfenster. Alle K-Zeilen müssen in ihrem jeweils benannten A4-Rust-/A4-SI-Bein grün werden (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4173`). |

## B.9 Dirigentenentscheide (2026-08-31)

| ID | Ausgangslücke | Entscheid |
|---|---|---|
| **ENTSCHIEDEN L-01** | Der v3-Vertrag hat keine Join-/Führungsfamilie (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-874`; `docs/beweise/SONDE-011.md:229-248`). | Phase B führt Join-Kandidaten, `beitritt_bestaetigung_noetig` und Führung ausschließlich im Coordinator und macht sie über den bestehenden `session_snapshot` sichtbar. Es entsteht keine neue Familie und kein neuer Wire-Weg. Der bestätigende User-Akt auf Wire bleibt Eigentum von SONDE-012/S29–31. Q1 und A4-SI `session_snapshot_zeigt_join_kandidat_ohne_neue_familie` verriegeln die Grenze. |
| **ENTSCHIEDEN L-02 — REVIDIERT 2026-08-31** | Initialwahl, stale Führung und Wiedergewinn waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:1454,1907-1911,3925-3939`). | **Ersetzt:** „Phase B rekonstruiert Führung beim Brokerneustart aus `MainProjectState`.“ **Neue Regel:** `MainProjectState` bleibt Userintent, erreicht den Broker in Phase B aber mangels Ingress/Wire-Weg nicht. Im selben Brokerlauf serialisiert der Coordinator: der erste bestätigt beigetretene Main führt automatisch nur als einziges Main; stale behält bis Eviction, danach automatische Vergabe nur an ein einziges bestätigtes Main, sonst `beitritt_bestaetigung_noetig`; Reconnect derselben `instance_id`/Session-Epoche erhält unübergebene Führung zurück. Nach Brokerneustart ist Führung frei und folgt derselben Einzigkeitsregel ohne State-Rekonstruktion. Diese Rekonstruktion gehört SONDE-012/S29–31 und NAK-120 (`docs/offene-punkte.md:15`). `FUEHRENDE_MAINS_PRO_SESSION = 1`; `coordinator_model::brokerneustart_ohne_mainprojectstate_ingress_gibt_fuehrung_frei` und `fuehrung_exklusiv_und_reconnect_im_selben_lauf` prüfen null/eins/mehr als ein Main. |
| **ENTSCHIEDEN L-03** | Konkurrenz alter und neuer Nonce derselben `instance_id` war offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:1612-1626`). | Die neue Nonce verdrängt die alte Verbindung sofort; Tombstone und Subscription-Cleanup folgen C-06. Sendet die verdrängte Verbindung danach einen Report, greift fail-closed wie C-03/C-07 und beide bleiben bis expliziter Auflösung gesperrt. `coordinator_model::neue_nonce_verdraengt_alte_sofort` und `verdraengte_nonce_report_sperrt_beide` messen beide Grenzen. |
| **ENTSCHIEDEN L-04** | Stale-, Tombstone- und Altersgrenzen waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2085-2087,3207-3212`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:393-401`). | `HEARTBEAT_INTERVAL_MS = 1000`, `STALE_VERPASSTE_INTERVALLE = 2`, `STALE_JITTER_MS = 500`, daraus `STALE_NACH_MS = STALE_VERPASSTE_INTERVALLE * HEARTBEAT_INTERVAL_MS + STALE_JITTER_MS = 2500`; stale gilt erst bei Alter `> STALE_NACH_MS`. `TOMBSTONE_MS = 10000`. Jeder gültige Report/Heartbeat vor Eviction macht sofort fresh und setzt den Tombstone-Timer zurück. `letzter_kontakt_ms` ist monotones Kontaktalter, kein Wallclock-Zeitpunkt. `stale_konstanten_ableitung` sowie Fake-Clock-Tests prüfen die Ableitung, `STALE_NACH_MS`/`STALE_NACH_MS + 1` und `TOMBSTONE_MS - 1`/`TOMBSTONE_MS`. |
| **ENTSCHIEDEN L-05** | Caps, Tie-Break und Nur-fresh-Fall waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3209-3212`). | `SESSION_CLIENT_CAP = 64`, `GLOBAL_CLIENT_CAP = 128`, oberhalb `FUEHRENDE_MAINS_PRO_SESSION = 1`, `SICHTBARE_SONDEN_NORMAL = 16` und `LAST_SONDEN = 32`. Opferwahl: stale vor fresh, dann ältestes `last_seen`, dann lexikographisch vollständige effektive Adresse. Bei ausschließlich fresh Clients wird der neue Client ehrlich und gezählt abgewiesen; kein fresh fällt. `coordinator_model::caps_stale_first_deterministisch` prüft jeweils `CAP`/`CAP + 1` und permutierte Reihenfolge. |
| **ENTSCHIEDEN L-06 — REVIDIERT 2026-08-31** | Eviction durfte Phase-A-Konfliktwahrheit nicht waschen (`broker/src/instance_alias.rs:189-212`; `docs/beweise/SONDE-011.md:146-151`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3208-3212,3937-3939`). | **Ersetzt:** „Der Konfliktindex überlebt Eviction, aber nicht den Brokerneustart.“ **Neue Regel:** Der clientunabhängige, nicht routbare Riegel wird in `conflict_guards` dauerhaft außerhalb der Eventwahrheit gespeichert und beim Brokerstart vor jeglichem Routing restauriert; Eviction und Neustart löschen ihn nicht. Persist-/Restorefehler halten betroffenes Routing fail-closed. Auflösung bleibt ausschließlich die explizite Neu-ID über `broker/src/instance_alias.rs:189-212`. A4-Rust `coordinator_model::konfliktriegel_ueberlebt_eviction` und `store_crash_matrix::konfliktriegel_ueberlebt_neustart_mit_nur_einem_partner` beweisen, dass auch nur ein zurückkehrender Kollisionspartner gesperrt bleibt. |
| **ENTSCHIEDEN L-07 — REVIDIERT 2026-08-31** | Storepfad, Schemafelder/-schlüssel und Forward-/Downgradeverhalten waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3799-3816`; `broker/src/bindung.rs:1-43`; `broker/src/main.rs:6-20`). | **Ersetzt:** „DB im per-User-Datenordner neben der Roaming-JSON-Bindungsdatei.“ **Neue Regel:** DB/WAL liegen unter dem per `FOLDERID_LocalAppData` aufgelösten Root in `STORE_RELATIVPFAD = "evenacadia\\nakama-broker"` mit `STORE_DATEINAME = "nakama-broker.sqlite3"`. Liegt der aufgelöste Pfad auf einem Remote-Volume, öffnet der Store dort nichts und startet sichtbar fail-closed degradiert nach L-08. Migration/Keys stehen in B.4; `STORE_SCHEMA_MAJOR = 1`, kein Schreiben/Migrieren neuerer Major, kein Downgrade. `%APPDATA%\evenacadia\nakama\eq-copilot-bindungen.json` bleibt am heutigen Ort unverändert und ohne Import. `store_crash_matrix::store_pfad_nutzt_folderid_localappdata`, `remote_volume_degradiert_ohne_db_oder_wal` und `schema_major_1_schreibt_und_major_plus_1_degradiert_read_only` prüfen die Grenzen. |
| **ENTSCHIEDEN L-08** | Storekanal und Fehlerbetrieb waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:1903-1916,3780-3819`). | `STORE_KANAL_CAP = 256`. Bei vollem Kanal oder degradiertem Store wird die Annahme persistenzpflichtiger Ereignisse verweigert und die Verursacherverbindung getrennt; Wiederholung kommt vom Sender-Reconnect. Disk-full, I/O-Fehler oder beschädigte DB stoppen den Broker nicht: sichtbar gezählter Betrieb ohne Historie, keine falsche Annahmebestätigung, P2 läuft weiter. Es entsteht kein zusätzlicher flüchtiger Outboxkanal. A4-Rust/A4-SI prüfen `STORE_KANAL_CAP`/`STORE_KANAL_CAP + 1` sowie jeden Degradationsgrund. |
| **ENTSCHIEDEN L-09** | SQLite-Fristen und Triggerwerte waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3808-3816`). | `BUSY_TIMEOUT_MS = 2000`; Group-Commit bei `COMMIT_FENSTER_MS = 50` oder `COMMIT_BATCH_MAX = 64`; PASSIVE-Checkpoint bei `WAL_SCHWELLE_BYTES = 4 * 1024 * 1024 = 4194304` oder `STORE_IDLE_MS = 5000`, wobei Idle keine Events und keine aktive Capture-Session bedeutet. Kein hartes Wire-Start-/Rebuildbudget; `STORE_RECOVERY_TEST_MAX_MS = 5000` verlangt Migration plus Recovery des Testkorpus strikt darunter. Fake-Clock-/Lock-/Dateigrößentests prüfen jede Schwelle bei Konstante minus eins/exakt; injizierte Recovery bei exakt `STORE_RECOVERY_TEST_MAX_MS` fällt. |
| **ENTSCHIEDEN L-10 — REVIDIERT 2026-08-31** | Outboxziel, Idempotenz, ACK, Retry, Ordnung und Snapshotpersistenz waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819,4131`; `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:845-876`). | **Ersetzt:** „Nach dem Snapshot folgen Subscriber-Events mit UUID-Dedupe.“ **Neue Regel:** `subscribe_session` und `session_snapshot` tragen keine Event-UUID. Einziger Phase-B-Wire-Abfluss der Session-Subscription ist der aktuelle absolute, idempotente `session_snapshot`: letzter Snapshot gewinnt; jeder Reconnect liefert den aktuellen Stand, daher geht keine committete Wirkung verloren und wiederholtes absolutes Setzen wirkt nicht doppelt. Es gibt keinen Event-Replay und kein Empfänger-Dedupe. `event_log` und die je Ziel/Objektschlüssel auf den neuesten geschuldeten Snapshot-Stand koaleszierte Outbox bleiben interne Store-Wahrheit; nach vollständigem Snapshot-Write darf die Schuld kompaktieren, Reconnect rekonstruiert weiter aus der Projektion. Nicht koaleszierbare Events haben in Phase B keinen Produkt-Consumer/Wire-Träger; Evidenz an Main und weitere Consumer gehören S18–19/S29–31. Dafür entsteht kein neuer Registerpunkt; NAK-120 (`docs/offene-punkte.md:15`) deckt die Rekonstruktionsseite. K-04 bis K-07 prüfen Commit vor Push, Subscriber vor/nach Anwendung und Outbox vor/nach Kompaktierung. |
| **ENTSCHIEDEN L-11 — REVIDIERT 2026-08-31** | Vor Persist fehlte die durable Quelle (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3816-3819`; `docs/beweise/SONDE-010.md:136-140,158-161,219-220`). | **Ersetzt:** „Ein nicht im Schema vorhandener Erfolgswert gab In-Flight frei und wurde beim Retry wiederholt.“ **Neue Regel:** SONDE-010 A-P0-05 bleibt wahr: Wire-Write gibt nur den Queueplatz frei. Das darüberliegende In-Flight-Register endet erfolgreich bei `command_ack(ergebnis = angewandt)` sowie nach Retry bei `ergebnis = idempotent_wiederholt`; `abgelehnt`, `konflikt` und `abgelaufen` beenden es endgültig ohne Erfolg. Verbindungsverlust vor einem ACK reiht dieselbe `command_id` erneut ein. Annahmegrenze bleibt der `BEGIN IMMEDIATE`-Commit; davor kein Erfolgs-ACK. Der Broker speichert `command_id → Event-UUID` persistent eindeutig, aber rein intern. Das ACK referenziert nur `command_id` und enthält nirgends die Event-UUID (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1057-1125`). K-01/K-02 laufen mit echtem C++-Client; K-03 beweist nach verlorenem `angewandt` die Antwort `idempotent_wiederholt` ohne zweite Wirkung oder Wire-UUID. |
| **ENTSCHIEDEN L-12 — REVIDIERT 2026-08-31** | Startverifikation, Fristen, Stoppbesitz und Installer-Autostart waren offen (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3193-3201`; `eq-copilot/install/nakama-installer-v1.json:14-17`). | **Ersetzt:** „Phase B prüft nur SHA-256; Authenticode liegt vollständig in S34–35.“ **Neue Regel:** Phase B baut die vollständige Prüfkette: manifestgebundenes SHA-256 immer zuerst; bei gesetztem `authenticode_thumbprint` danach gültige `WinVerifyTrust`-Signatur plus exakter Thumbprint, sonst Start fail-closed. Bei heutigem `null` bleibt SHA-256 der Mindestschutz, weil S34–35/NAK-119 Zertifikat und befüllten Thumbprint liefern (`docs/offene-punkte.md:16`). Testzertifikat deckt gültig/fehlend/falsch ab. `SPAWN_CONNECT_BACKOFF_START_MS = 250`, `SPAWN_BEREIT_TIMEOUT_MS = 10000`, `SPAWN_COOLDOWN_MS = 30000`, `BROKER_IDLE_ENDE_MS = 60000`; B10 prüft jede Zeitgrenze minus eins/exakt. Stopp nur als Broker-Selbstende; kein fremder Brokerkill. `AUTOSTART_ARTEFAKTE_PHASE_B = 0`; A-06/A17/A18 prüfen null Installer-/Boot-Autostartartefakte, Prüfkette und Rückweg. |
