SONDE-014 / S23–25 — Etappe 2, Nacharbeit 1 (Runde 1 von 3). Auftrag des Dirigenten (2026-09-07) an einen frischen Opus-Bauer (max).

LAGE
Repo C:/Users/phili/Projekte/Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start (der Dirigent hat den Stand nach dce6970 gemessen: Worktree sauber, Etappe 2 abgeschlossen mit T1 PASS, Abschlusskanon GRÜN 60/60 auf c053be8, docs/beweise/roh/SONDE-014-c053be8.md). Ticket-Basis bleibt f90abf5. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec (neue Dateien zuerst `git add -- <pfad>`), Commit-Nachrichten über Temp-Datei und `git commit -F`, Push nach origin master nach `git pull --rebase` bei sauberem Worktree. Kein Löschbefehl und kein Lösch-Cmdlet (blockiert; Logs per `*>` überschreiben). Keine Nachfrage an den User. Keine Änderung an docs/offene-punkte.md, docs/PLAN-STAND.md, docs/NEXT-SESSION.md, docs/plan/, design/, eq-copilot/plugin/sonde/, probe/, core/analysis/, hostbridge/, eq-copilot/identity/, eq-copilot/install/. Engines in Tests auf den Heap (NAK-175). Jeder Rotbeweis fällt an genau der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt (Regel aus NAK-181/182: das häufigste Prüferurteil war „der Rotbeweis fällt nicht an der Zeile, die die Zusage trägt").

REFERENZ
Die Verhaltensmatrix docs/beweise/SONDE-014.md §3 in der jüngsten Fassung (Etappe 1b, Matrixnacharbeit 1; Matrixprüfung 2 PASS), Entscheide E-01 bis E-12 (docs/beweise/roh/SONDE-014-etappe-1-entscheid-auftrag.txt, -matrixnacharbeit-1-auftrag.txt, -etappe-2-fortsetzung-1-auftrag.txt), Regeln R1 bis R4, der Bau in §7.1 bis §7.12, die Bauregeln aus docs/beweise/roh/SONDE-014-etappe-2-auftrag.txt (Rolle, zwei Testebenen, Rotbeweise, Vertragsregeln, Landminen) gelten unverändert. Die Erstprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a079da-2aab-7540-8173-c6733c7739ca, HEAD dce6970 vor und nach dem Lauf, 31 min) steht wörtlich in docs/beweise/roh/SONDE-014-erstpruefung-1-dce6970.txt, ihr Auftrag in docs/beweise/roh/SONDE-014-erstpruefung-1-auftrag.txt. Urteil: NEEDS_WORK, 14 Befunde. Der Dirigent hat jeden an der Quelle geprüft: 13 DEFEKTE und eine LÜCKE, die er in dieser Runde entscheidet (E-13). Lies zuerst diesen Auftrag, dann das Rohurteil vollständig, dann die genannten Matrixzeilen und Quellstellen.

DEFEKTE (Kernsatz wörtlich aus dem Urteil; Datei:Zeile zum Stand dce6970) und die REGEL, die jeden schließt:

EP-01 — „DEFEKT gegen M-86: Bei `intent == None` ist diese Bedingung falsch; ohne jede Vollständigkeitsmeldung entstehen daher bereits Findings und Proposals. Derselbe Fehler steht in `hypothesen()`. … Der vorhandene Sperrtest prüft dagegen nur einen vorhandenen, unvollständigen Bestand beziehungsweise die nicht verwendete Hilfsfunktion `darf_rechnen()`." (broker/src/coordinator/hypothese_verdrahtung.rs:139-142; hypothese.rs:1057; darf_rechnen() in intent.rs:541-550 ohne Produktaufrufer)
  Regel NR-01: Die Sperre ist fail-closed: ohne Vollständigkeitsmarke (auch bei `intent == None`) rechnet der Broker weder Hypothese noch Proposal — genau EIN Produktriegel (`darf_rechnen()` oder die eine Bedingung `!intent.as_ref().is_some_and(|i| i.vollstaendig)`), eingesetzt in `aufnahmen_sammeln`/`hypothesen()`; die zweite Kopie verschwindet. Der A4-Fall `keine_rechnung_vor_der_vollstaendigkeitsmarke` bekommt einen ersten Abschnitt OHNE jede `intent_update`-Nachricht: Evidenz einreihen → `befunde_sicht(...)` leer und `draft_offers_zaehler() == 0`; erst nach dem Vollbestand mit Marke entsteht ein Befund. Alle bestehenden Tests, die ohne Intent Befunde erwarten (z. B. `causehypothesis_traegt_die_zehn_felder`, die Fälle in sonde014_befund/maskierung/proposal/p5_korpus und der P5-Korpuslauf), senden fortan den Vollbestand mit Marke — die Regel wird nicht aufgeweicht, die Tests werden nachgezogen. Rotbeweis: Riegel auf `is_some_and(|i| !i.vollstaendig)` zurückdrehen → der neue Abschnitt fällt.

EP-02 — „DEFEKT gegen M-85: Der Sender koalesziert je Quelle/Scope, während diese Ablehnung die globale Bestandsrevision verwendet. Reprofolge: Teilupdate A/Revision 1, B/Revision 2, anschließend A/Revision 3 vor dem Abfluss einreihen. `P1Warteschlange::einreihen()` ersetzt A an seiner bisherigen Position, sodass A/3 vor B/2 ankommt; B/2 wird hier vollständig verworfen." (broker/src/coordinator/intent.rs:404-405; eq-copilot/plugin/core/ipc/IpcQueues.h:349-360)
  Regel NR-02: Die Revisionsordnung „ältere überschreibt nie eine jüngere" gilt JE KOALESZIERUNGSOBJEKT `(quelle_id, scope)`: ein Teilupdate wird nur abgewiesen, wenn seine Revision kleiner ist als die zuletzt für DIESES Objekt übernommene; die Sitzungsrevision `bestand.revision` wird als Maximum fortgeschrieben und ordnet nur Vollberichte (`vollstaendig: true` ersetzt den Bestand vollständig). Neuer A4-Fall `teilupdate_fremden_schluessels_ueberlebt_eine_koaleszierung`: Ankunft A/1, B/2, A/3 → danach stehen beide Objekte mit ihren Rollen in `intent_sicht`, und A/2 nach A/3 wird weiterhin mit `AeltereRevision` abgewiesen. Rotbeweis: Ordnung wieder global → B/2 fehlt.

EP-03 — „DEFEKT gegen M-10/M-24: `hypothesen_bilden()` kopiert den Eingang unter dem Lock, rechnet außerhalb und trägt anschließend bedingungslos ein. Repro-Interleaving: Aufnahme mit Intent-Revision 1 sammeln, während der Rechnung Revision 2 empfangen und den Befund invalidieren, danach das alte Ergebnis hier eintragen. Dadurch überschreibt ein alter READY-Befund den bereits auf STALE gesetzten Bestand." (broker/src/coordinator/hypothese_verdrahtung.rs:297-302)
  Regel NR-03: `befunde_eintragen` prüft unter DEMSELBEN Lock vor dem Einsetzen: (a) die Intent-Revision des Ergebnisses ist gleich der aktuellen Sitzungsrevision — sonst wird das Ergebnis verworfen und die Sitzung bleibt dirty (Neurechnung), nichts wird eingetragen; (b) jede `evidence_id` des Ergebnisses ist in der aktuellen Evidenzhistorie vorhanden und nicht ausgeschlossen — sonst wie (a). Ein bereits sichtbarer STALE- oder invalidierter Befund wird nie durch ein älteres Ergebnis ersetzt. Gemessen mit A4-Fall `veraltetes_rechenergebnis_wird_nicht_veroeffentlicht` über einen Testhaken zwischen `aufnahmen_sammeln` und `befunde_eintragen` (cfg(test)/Feature-Haken oder zwei Threads mit Barriere): Revision während der Rechnung erhöhen → kein READY-Befund mit der alten Revision im Snapshot; zweiter Abschnitt: Evidenz während der Rechnung zurücknehmen → kein Befund mit der zurückgenommenen ID. Rotbeweis: Prüfung (a) entfernen → Fall fällt.

EP-04 — „DEFEKT gegen M-28: Der sichtbare Snapshot serialisiert ausschließlich den Cache; auch die Erzeugung filtert lediglich dessen `ausschlussgrund`, ohne die referenzierten Store-Zeilen zu prüfen. … Der vorhandene M-28-Test verwendet keinen Store und prüft nur eine Teilinvalidierung im Speicher; er misst die ausdrücklich zugesagte Prüfung gegen SQLite nicht." (broker/src/coordinator/sicht.rs:635-640; hypothese_verdrahtung.rs:242-251; broker/tests/sonde014_hypothese.rs:955-979)
  Regel NR-04: „Existent" ist eine Prüfung gegen die Tabelle `evidence` des Stores (M-28 wörtlich): beim Eintragen eines Befunds UND vor jeder Serialisierung in `session_snapshot_json` werden alle referenzierten `evidence_ids` mit EINER Existenzabfrage gegen den Store gehalten (außerhalb des Standlocks, Ergebnis unter dem Lock angewandt); fehlt eine Zeile oder ist sie ausgeschlossen, wird der Befund unsichtbar (alle IDs fehlen) beziehungsweise STALE (Teilverlust) — nie ohne Beleg gezeigt. Ohne Store (Testkoordinator `mit_uhr`) gilt die Speicherprüfung ausdrücklich als Fallback und wird als solcher benannt. Neuer A4-Storefall `befund_mit_geloeschter_evidenzzeile_wird_unsichtbar`: Store-Coordinator, Befund erzeugen, `DELETE FROM evidence` für seine IDs, `session_snapshot_json` → Befund nicht mehr in `findings`. Rotbeweis: Store-Abfrage entfernen → Fall fällt.

EP-05 — „DEFEKT gegen M-23: `fenster` zählt ausschließlich die Kandidatenfenster; auch das vorgelagerte Passagengate prüft die Mindestzahl nur beim Kandidaten. … Verlangt sind dagegen mindestens acht unabhängige Fenster je beteiligter Quelle innerhalb der Passage, andernfalls MORE DATA mit `passage_zu_kurz`." (broker/src/coordinator/hypothese.rs:792-815 und 1314, 1328-1336)
  Regel NR-05: Passagengate und Klassenbildung halten `min(unabhaengige_fenster(kandidat_in_passage), unabhaengige_fenster(master_in_passage)) >= GATE_MINDEST_FENSTER`; unterschritten → `Ausschlussgrund::PassageZuKurz` beziehungsweise höchstens `Mittel`. Neuer Fall im `EqCopSonde014HypotheseTest`-Bereich (Rust, A4/sonde014_hypothese): acht unabhängige Kandidatenfenster gegen ein einziges überlappendes Masterfenster → `MORE DATA` mit `passage_zu_kurz`, nicht `READY TO SEND`. Rotbeweis: Master aus dem Minimum nehmen → Fall fällt. Die Kalibrierung §7.8 (M-31-Nachtrag) wird danach neu gemessen und im Manifest fortgeschrieben, nicht abgeschrieben.

EP-06 — „DEFEKT im Frequenzdatenweg von M-18/M-19: Die Rückabbildung passt nicht zur Gruppierung `band * 64 / 221`. … `python -B -c "b=100;g=b*64//221;print(g*221//64,(g+1)*221//64)"` liefert `96 100`. Eine Anomalie in Band 100 wird damit dem halboffenen Intervall `[96,100)` zugeordnet, das dieses Band gerade ausschließt." (broker/src/coordinator/hypothese.rs:740-742, 1229-1230; Dirigent nachgerechnet: 63 von 221 Bändern liegen außerhalb des Intervalls ihrer eigenen Gruppe; Bruch am Gate-Text §1.5 „Die Zone zeigt nur, was der Befund belegt hat" und an M-16 Ort/Beobachtung)
  Regel NR-06: Die Rückabbildung ist die echte Umkehrung der Gruppierung: `von = ceil(g*221/64) = (g*221 + 63)/64`, `bis = ((g+1)*221 + 63)/64` (ganzzahlig), so dass für jedes Band `b` gilt `von(gruppe_von_band(b)) <= b < bis(gruppe_von_band(b))`. Neuer Fall `jedes_band_liegt_im_intervall_seiner_gruppe` über alle 221 Bänder; dazu in `masteranomalie` die Behauptung, dass das Band der Beobachtung im zurückgegebenen Intervall liegt (debug_assert und Testfall). Rotbeweis: floor-Inverse zurück → 63 Verstöße. Folgen in Maskierung (`maskierung.rs` liest `band.von..band.bis`), `band_hz` und `parameters.frequency_hz` werden mit denselben Fällen nachgezogen; Goldens/Fixtures, die sich dadurch ändern, werden neu erzeugt und ihre Änderung im Manifest begründet.

EP-07 — „DEFEKT gegen das Exit-Gate und M-43/M-46: `passage_id` fehlt in `required`. Die lesende Gegenprobe hat das Feld jeweils aus `draft_offer`, `draft-offer-mehr-daten` und `draft-offer-keine-aenderung` entfernt; `Draft202012Validator.is_valid()` blieb bei allen drei Objekten wahr. … Writer, Reader und Fixtures müssen gemeinsam korrigiert werden." (eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1989-1993; broker/tests/sonde014_proposal.rs:202-229 überspringt das Feld)
  Regel NR-07: `passage_id` wird in `$defs/proposal.required` aufgenommen; Writer und Reader in Rust (`proposal.rs`, `dto`/Leser) und C++ (Sonde014-Leser/Writer) ziehen im selben Änderungssatz nach (M-77 Sprachparität); Negativfixture `eq-copilot/fixtures/v3/ungueltig/proposal-ohne-passage.json` über `erzeuge_v3_fixtures.py` (A8 bytegleich), A5 `pruefe_v3_vertrag.py --abdeckung` lehnt sie ab; ein Proposal ohne Passage entsteht auf dem Wire nicht (Erzeuger liefert erst mit benannter Passage ein Objekt). Der Sprung `if feld == "passage_id" { continue; }` in `sonde014_proposal.rs` entfällt: die Bühne führt eine benannte Passage, und `sechs_gate_felder_sind_pflicht` prüft alle sechs Felder. Rotbeweis: `passage_id` aus `required` nehmen → A5 fällt an der Negativfixture.

EP-08 — „DEFEKT gegen M-59/M-88/M-89: Der Sender liest den separaten Schatten `assistentSchritt`, aber `setStateInformation()` restauriert ausschließlich `zustand.assistent`. Reprofolge im vorhandenen B29-Aufbau: `getStateInformation()` → neuer Prozessor → `setStateInformation()` → `v3AssistantStepFuerTest()`. Der persistente Schritt ist vorhanden, die Wire-Nachricht bleibt jedoch leer; bei Reload im selben Prozessor kann stattdessen der vorherige Schritt gesendet werden. Zudem zählt der Schatten seine Revision unabhängig vom gespeicherten Zustand." (eq-copilot/plugin/src/PluginProcessor.cpp:1941-1946, 2040-2054, 2445-2508; `v3AssistantStepFuerTest()` ohne Aufrufer)
  Regel NR-08: Es gibt EINE Wahrheit des Schritts im Main: `v3AssistantStepJson()` liest `zustand.assistent` (unter dem Zustandsmutex) mit dessen Revision; der Schatten `assistentSchritt` samt eigener Revision entfällt (oder wird in `setStateInformation` und in jedem Schreibpfad ausschließlich aus `zustand` abgeleitet — keine zweite Zählung). Neuer Fall in `Sonde014AssistentTest.cpp`: nach `getStateInformation` → neuer Prozessor → `setStateInformation` liefert `v3AssistantStepFuerTest()` den restaurierten Schritt (`step_id`, `revision` aus dem MainProject, nicht leer); zweiter Abschnitt: Reload im selben Prozessor sendet nie den Vorgängerschritt. A4-Gegenprobe: nach `intent_update{vollstaendig:true}` folgt `assistant_step_update` mit dem aktuellen Schritt und der Broker-Spiegel trägt ihn (M-88). Rotbeweis: Sender wieder auf den Schatten → Fall fällt.

EP-09 — LÜCKE, in dieser Runde entschieden — „`experiment_json()` speichert das neue Ziel nicht, und die Wiederherstellung setzt es hier ausdrücklich auf `None`. Reprofolge: `experiment_begin` mit Ziel- und Schutzbändern → Brokerneustart auf demselben Store → manueller Experimentabschluss. Der weiterhin abschließbare Versuch benutzt anschließend die Heuristik statt der übergebenen Bereiche." (broker/src/coordinator/experiment_verdrahtung.rs:743-806, 1728-1732). M-48 sagt zum Neustart nichts; M-71 ordnet „Experimente" dem Store zu.
  Entscheid E-13 (Dirigent, 07.09.2026): Das Experimentziel ist Teil des persistierten Experimentobjekts (M-71 „Experimente gehören in den lokalen SQLite-Experimentstore" meint das ganze Objekt; CLAUDE.md „State bleibt verlustfrei", Arbeitsregel speichern↔laden im selben Änderungssatz). Ein mit `ziel` begonnenes Experiment behält sein Ziel über den Brokerneustart; `ziel_geraten` bleibt dem Versuch vorbehalten, der ohne `ziel` begonnen wurde. M-48 erhält diesen Satz als Nachtrag „E-13 (Nacharbeit 1, 07.09.2026)".
  Regel NR-09: `experiment_json` schreibt `ziel` (band_von, band_bis, geschuetzte_baender, proposal_id), `experiment_aus_gespeichertem` liest es zurück; neuer A4-Storefall `experimentziel_ueberlebt_den_brokerneustart`: `experiment_begin` mit Ziel → Neustart auf demselben Store → manueller Abschluss → Resultat mit den übergebenen Bereichen und `ziel_geraten == false`; Gegenprobe ohne Ziel → `ziel_geraten == true`. Rotbeweis: Rücklesen entfernen → Fall fällt.

EP-10 — „DEFEKT gegen M-73: Dieser grüne Test bekommt seinen ACK vom Testserver, nicht vom Coordinator. Repro am tatsächlichen Empfänger: das gültige `user_verdict.json` als P0 an `Coordinator::p0_mit_minor()` übergeben. Der Schema-Leser akzeptiert die neue Familie, aber `p0_json_mit_minor()` in `befehl.rs` hat keinen entsprechenden Zweig und endet in `_ => None`; weder Persistenz noch ACK entstehen. Auch `assistentAntwort()` sendet kein `user_verdict`." (broker/src/coordinator/befehl.rs:530-805; eq-copilot/plugin/tests/IpcTestMain.cpp:5634-5644; PluginProcessor.cpp:3579-3591; store/writer.rs:576 Projektion `user_verdicts` ohne Produzenten)
  Regel NR-10: `user_verdict` hat einen Produktpfad auf beiden Seiten: der Broker nimmt es in `p0_json_mit_minor` als persistenzpflichtigen P0 an (derselbe Weg wie die Experimentfamilien: Append mit `event_type = "user_verdict"`, Projektion `user_verdicts`/`user_verdict_id`, ACK erst nach dem Append, idempotente Wiederholung unter derselben `command_id`); das Plugin sendet das Urteil in `assistentAntwort` über `sendePersistenzP0` (E-09), zusätzlich zum `assistant_step_update`. Gemessen mit A4-Fall `user_verdict_wird_persistiert_und_bestaetigt` (Zeile in `user_verdicts` nach `p0_mit_minor`, ACK-Antwort), A4-SI-Fall `user_verdict_wiederholt_sich_idempotent_ueber_den_brokerkill` (dieselbe `command_id` nach Kill/Neustart → genau eine Zeile) und B10-Erweiterung: `assistentAntwort` erzeugt genau ein `user_verdict` in der P0-Queue (Inhalt aus dem Urteil, keine Koaleszierung). Rotbeweis: Zweig in `befehl.rs` entfernen → A4-Fall fällt (kein ACK, keine Zeile).

EP-11 — „DEFEKT gegen M-71: Angenommene `intent_update`-Nachrichten ändern ausschließlich `stand.intent`, invalidieren gegebenenfalls Befunde und kehren erfolgreich zurück. Anders als beim AssistantStep gibt es für diesen Bestand keinen Store-Append. Nach einem Vollbericht beziehungsweise einer Rollenänderung enthält SQLite daher keinen versionierten SourceIntent-Spiegel für Suche und Crashdiagnose." (broker/src/coordinator/intent.rs:486-493; Vorbild assistent.rs:148-182)
  Regel NR-11: Nach jeder übernommenen `intent_update` (Voll- und Teilbericht) spiegelt der Broker den Bestand versioniert in den Store über denselben Ein-Writer-Append wie `assistent.rs:148-182` (append-only `event_log`, `event_type = "intent_update"` oder gleichwertig, Revision als Version, außerhalb des Standlocks; kein neuer Writer, keine neue Tabelle). Der Spiegel ist nie autoritativ (M-89-Analogie). Neuer A4-Storefall `intent_bestand_wird_versioniert_gespiegelt`: zwei `intent_update` → zwei Ereigniszeilen mit steigender Revision; nach dem Neustart überschreibt der Spiegel den neueren Main-Bestand nicht (Main meldet Vollbestand → `intent_sicht` zeigt den Main-Stand). Rotbeweis: Append entfernen → Fall fällt.

EP-12 — „DEFEKT gegen M-31/M-66: Nach dem Absenken der Schwelle bewertet `_passt()` weiterhin die ursprüngliche Sicherheitsklasse statt der nun zugelassenen Handlung. Ein falscher Distraktor mit `mittel` gilt dadurch auch dann als richtig, wenn die Kandidatenschwelle ihn handelbar macht. … Schon `mittel` erklärt sechs Befunde einschließlich Distraktor und Parent-Duplikat für handelbar, dennoch steht `davon_falsch=0`; `unklar` lässt sogar alle acht passieren." (tools/eq-copilot/pruefe_p5_korpus.py:219-226, 234-240)
  Regel NR-12: `schwelle_suchen` bewertet je Kandidatenschwelle die NEU ZUGELASSENE Handlung: wer bei dieser Schwelle handelbar wäre, muss die wahre Ursache sein (Enthaltungen `daten_reichen_nicht` ausgenommen); `falsch = [f for f in handelbar if not f["ist_wahre_ursache"]]`, zusätzlich zur Deckelprüfung `sicherheit_hoechstens`. Neuer `--selbsttest`-Fall (A30): ein Distraktor mit `sicherheit == sicherheit_hoechstens == mittel`, `ist_wahre_ursache=False` → Stufe `mittel` hält nicht; Gegenprobe mit wahrer Ursache → hält. Die ausgegebene Schwelle (A29) und der M-31-Nachtrag in §7.8 werden danach neu gemessen und im Manifest fortgeschrieben. Rotbeweis: alte `_passt`-Logik → Selbsttest fällt.

EP-13 — „DEFEKT gegen M-64: Die Ursachenklasse wird ungeprüft aus der Produktausgabe übernommen; als Wahrheit zählt lediglich die Quell-ID beziehungsweise eine Enthaltung. … A29 blieb in dieser Session grün und meldete auch für die falsche Klasse Precision/Recall 1,000. Zusätzlich entsteht der Recall-Nenner nur aus ausgegebenen Befunden, sodass ausgelassene Sollbefunde verschwinden." (tools/eq-copilot/pruefe_p5_korpus.py:354-361, 409-411; das Sollabel `ursachenklasse` steht bereits je Sitzung in eq-copilot/fixtures/p5-korpus/sitzungen.json und wird nie gelesen)
  Regel NR-13: Die Klassenmetrik rechnet gegen die unabhängige Wahrheit des Korpus: `_faelle_bilden` führt `soll_klasse = s["ursachenklasse"]`, Precision/Recall werden je `soll_klasse` ausgewiesen, eine Abweichung `b["ursachenklasse"] != soll_klasse` (außer Enthaltung) ist ein eigener roter Riegel mit Sitzungsname, und der Recall-Nenner entsteht aus den Sollsitzungen (erwartete starke Befunde), nicht aus den ausgegebenen. Neuer `--selbsttest`-Fall: Klassenvertauschung einer Sitzung → genau eine rote Zeile; ausgelassener Sollbefund → Recall < 1; Gegenprobe ohne Vertauschung grün. Rotbeweis: Gruppierung zurück auf die Ausgabe → Selbsttest fällt.

EP-14 — „DEFEKT gegen M-65/NAK-190: Gezählt wird nur die Länge beliebiger `alternatives`-Listen. Ob eine ID einen existenten, anderen Befund bezeichnet und dieser tatsächlich zur bekannten Distraktorquelle gehört, bleibt ungeprüft. … In der geladenen Ausgabe von `korrelierter_distraktor` sämtliche Alternativverweise durch `'f' * 32` ersetzen … keine Probleme, die Sitzung gilt weiterhin als messender NAK-190-Nachweis." (tools/eq-copilot/pruefe_p5_korpus.py:353, 377-384)
  Regel NR-14: Der NAK-190-Nachweis gilt nur, wenn jede `alternatives`-ID auf einen existenten anderen Befund DESSELBEN Sitzungslaufs auflöst (`finding_id`-Menge) und dessen `candidate_source` die im Korpus als Distraktor deklarierte Quelle ist; `messende` wird nur dann gesetzt, nicht auflösbare oder fremde IDs sind eine rote Zeile. Der Korpus (sitzungen.json) deklariert die Distraktorquelle je Fall ausdrücklich, falls noch nicht geschehen (A28 bytegleich). Neuer `--selbsttest`-Fall: Verweis auf eine nicht existente ID → rot; Verweis auf einen Befund einer anderen Quelle → rot; Verweis auf den echten Distraktorbefund → grün. Rotbeweis: Auflösung entfernen → Selbsttest fällt.

GEMEINSAME URSACHEN — der Auftrag ist die Ursache, nicht die Punktkorrektur: (1) Sperr- und Revisionslogik am Broker (NR-01, NR-02, NR-03) — fail-closed, Ordnung je Objekt, Prüfung unter dem Lock vor der Veröffentlichung; (2) Store-Anbindung, die die Matrix zusagt und die im Bau fehlte (NR-04, NR-09, NR-10, NR-11) — derselbe Ein-Writer-Pfad, Save↔Load in demselben Änderungssatz; (3) zwei Rechenfehler im Frequenz- und Fenstergate (NR-05, NR-06); (4) Vertrag (NR-07); (5) zweite Wahrheit im Main (NR-08); (6) Riegel des Prüfwerkzeugs ohne Weg zu fallen (NR-12, NR-13, NR-14). Baue je Ursache einen logischen Commit.

BETROFFENE BEINE (diese Runde fährt nur sie; kein voller Kanon — der läuft beim Abschluss des Tickets): A4 `cargo test --manifest-path broker/Cargo.toml` (alle sonde014_*-, sonde013_verdrahtung- und Store-Tests), A4-SI, A5 `py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py --abdeckung`, A8 `erzeuge_v3_fixtures.py --pruefen`, A28 `erzeuge_p5_korpus.py --pruefen`, A29 `pruefe_p5_korpus.py`, A30 `pruefe_p5_korpus.py --selbsttest`, B3c Schema, B10 `EqCopIpcTest`, B13 `EqCopSonde012SourcesModelTest`, B27 `EqCopSonde014IntentTest`, B28 `EqCopSonde014BefundTest`, B29 `EqCopSonde014AssistentTest`, B2 `EqCopStateMigrationTest` und B14, falls NakamaState oder ProjectReload berührt sind; dazu jedes weitere Bein, dessen Quelle dein Diff berührt (aus `tools/beweise.ps1` ablesen). Beinnamen und Aufrufe aus `tools/beweise.ps1` lesen, nicht raten. Jede Regel hat ihren Rotbeweis als Datei `docs/beweise/roh/SONDE-014-rot-NR-nn.txt` (Rücknahme, roter Lauf mit der fallenden Zeile, Wiederherstellung, grüner Lauf).

MANIFEST docs/beweise/SONDE-014.md: neuer Abschnitt „§8 Erstprüfung 1 und Nacharbeit 1" (append-only unter §7.12): Kopf der Erstprüfung 1 (Modell, Effort, Thread, HEAD, Dauer, Urteil, Rohdatei, Auftrag), Einordnung des Dirigenten (13 Defekte, 1 Lücke → E-13), Zuordnung EP-01 bis EP-14 → NR-01 bis NR-14 → geänderte Zeile/Test/Rotbeweis, gemessene Beinläufe mit Zahlen, die neu gefassten oder ergänzten Matrixzeilen (M-48 Nachtrag E-13; M-23, M-28, M-31/M-66 Nachträge — alte Fassung bleibt Verlauf, die neue steht als „Nacharbeit 1" markiert darunter), die neu gemessene Kalibrierung (NR-05, NR-12), Prüflisten-Abhakung (tools/dirigent/pruefliste.md) und der lebende Kopf: Zeile „Phase" auf „Etappe 2: Nacharbeit 1 abgeschlossen, Wiederprüfung 1 offen", Zeile „Urteil" um „Erstprüfung 1 (Astra max): NEEDS_WORK, 14 Befunde — 13 Defekte, 1 Lücke (E-13)" ergänzt, Kurztabelle der Runden fortgeschrieben, Ticketpfade um neue Dateien ergänzt. Nebenbefunde in §7.11 fortschreiben (N-26 ff.), nicht ins Register. `py -3.13 tools/plan/dokuriegel.py docs/beweise/SONDE-014.md` grün vor dem Commit.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen — Sperre wirklich fail-closed (None sperrt), Ordnung je Objekt wirklich getestet mit vertauschter Ankunft, Eintragung wirklich unter dem Lock geprüft, Store-Abfrage wirklich im Sichtbarkeitspfad, ceil-Inverse über alle 221 Bänder, Schema+Writer+Reader+Fixture in einem Satz, Schatten wirklich weg, Save↔Load des Ziels symmetrisch, user_verdict auf beiden Seiten, Intent-Spiegel ohne zweiten Writer, jeder Prüfwerkzeug-Riegel mit nachgewiesenem Fallweg; keine Umbenennung, kein Neben-Refactor, keine Änderung an Identität, Audio-Pfad oder Installer. Commits mit Pathspec je Ursache, pushen; beende den Turn nie mit uncommittetem Stand oder laufendem Bein. Letzte Zeile deiner Ausgabe: „NACHARBEIT 1 ABGESCHLOSSEN <end-sha>".