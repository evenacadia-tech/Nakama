# unit-02 — core\analysis\FeatureEngine.h (Zeilen 1002–3551, 17 Einheiten)
Gelesen: 17 von 17 Einheiten vollständig · Zeilen: 2550

Repo `C:\Users\phili\Projekte\Nakama`, HEAD `9f3000881df632f4969d78551fb0a516b61288cb`.
Bedrohungsmodell LOCAL_UNPRIVILEGED. Nur gelesen; keine Repo-Datei geändert.

## Befunde

- **[MEDIUM]** `core/analysis/FeatureEngine.h:2999-3000` und `:3025-3028` (Ursache `:2908`) · logik/protokoll + NaN-/Nullehrlichkeit · **`StereoBandwert::korrelationKurz` und `::persistenz` werden bei der Standard-Evidenzkadenz in JEDEM Snapshot als gemessene `0.0` veröffentlicht, obwohl sie nie berechnet wurden.** · Auslöser: Normalbetrieb bei 44,1/48 kHz mit dem Vorgabewert `evidenzIntervallS = kEvidenzIntervallMinS = 0,25 s` (`:4176`, Deklaration `double evidenzIntervallS { kEvidenzIntervallMinS }`); es genügt beliebiges Stereomaterial über dem Aktivgate. Rechnung: ein Evidenzfenster von 0,25 s sind bei 48 kHz 12000 Samples, die Hauptstufe hat `kHauptHop = 2048`, also höchstens 5–6 aktive Welch-Frames je Fenster. `stereoSchritt` schließt ein Kurzfenster aber erst bei `++stereoKurzFrames >= kKorrelationKurzFrames` (= 8, `:2908`), und `evidenzLeeren()` ruft je Snapshot `stereoLeeren()` (`:3572`), das `stereoKurzFrames`, `stereoKurzfenster` und `stereoKorrKurzGesetzt` wieder nullt (`:3080-3083`). Damit ist `stereoKurzfenster` dauerhaft 0 und `stereoKorrKurzGesetzt[b]` dauerhaft 0. · Wirkung: In `stereoAuswerten` bleibt `e.korrelationKurz` auf dem `StereoBandwert{}`-Startwert `0.0f` (der Zuweisungszweig `:2999-3000` läuft nie) und `e.persistenz` ebenso (`if (stereoKurzfenster > 0)`, `:3025`), während `e.basisGesetzt = true` gesetzt wird (`:2986`). Beide Felder haben **kein eigenes Präsenzbit**; der Vertragskommentar an `StereoBandwert` (`:469-471`, „Ein Band ohne dieses Bit hat in KEINEM der fuenf Felder einen Wert") macht `basisGesetzt` ausdrücklich zum Präsenzbit auch für `korrelationKurz`, und der Kommentar an `persistenz` (`:485-489`) sagt „0 heisst … ‚in keinem Kurzfenster kohaerent'". Ein Empfänger liest daraus „Band vollständig dekorreliert" und „nie stabil" — eine Phasenaussage, die niemand gemessen hat. Das ist genau der von CLAUDE.md verbotene Fall „gemeldete 0 ≠ nie gesagt". Die Gegenprobe im selben Block zeigt, dass es anders geht: `kohaerenz`/`phaseRad` und die Zeitperzentile tragen eigene Bits und schweigen im selben Lauf korrekt. · Erreichbar: ja. Aufruferkette `SondeProcessor.cpp:350 nimmBlock` → `FeatureEngine::baueFrame` → `stereoAuswerten()` (`:3532` im Frameaufbau) → `SondeProcessor.cpp:550 stereo.baender = &merkmale.stereoBand(0)` → `vertrag/NakamaEvidenz.cpp:313-315` (`"korrelation_kurz"`, Gate `b[i].basisGesetzt`) und `:325-327` (`"persistenz"`, Gate `b[i].basisGesetzt`) → Evidenz-JSON auf der Leitung. Bei `kEvidenzIntervallMaxS` (1 s) oder 96 kHz schließen 1–2 Kurzfenster, dort tritt der Fehler nicht auf — er hängt an der Vorgabekadenz, nicht an einer Sonderlage. · Beleg: 2905–2930 (Kurzfensterlogik, nur Hauptstufe zählt), 2996–3000, 3024–3028, 3074–3086, 3564–3582. · Register: neu (`rg -n "korrelation_kurz|korrelationKurz|Kurzfenster|persistenz" docs/offene-punkte.md` und `docs/beweise/` → keine passende Zeile; `rg -n "NaN" docs/offene-punkte.md` → 0 Treffer).

- **[LOW]** `core/analysis/FeatureEngine.h:1784` · konvertierung/UB (Vergleich mit NaN) · **`grenzeZwischen()` vergleicht die Kontext-Samplerate mit `!=`; bei einem nicht-endlichen Hostwert ist der Vergleich in jedem Blockpaar wahr und die Analyse verstummt dauerhaft.** · Auslöser: Der Host liefert `processContext.sampleRate = NaN` bei gesetztem `processContextPresent`. Der Wert wird ungeprüft übernommen (`hostbridge/NakamaHostBridge.h:301`, Feldkommentar `:128` „Kontext-Samplerate, **roh**"), und das Gültigkeitsbit hängt nur am Kontext, nicht am Wert (`src/PluginProcessor.cpp:656`, `sonde/SondeProcessor.cpp:245`: `sampleRateGueltig = processContextPresent && k.sampleRate.gueltig`). Der Rückfall auf die Prozessorrate greift nur bei `! sampleRateGueltig` (`src/PluginProcessor.cpp:328-334`) und wirkt hier nicht. · Wirkung: `beideSr` ist wahr, `NaN != NaN` ist wahr → `Grenzgrund::sampleratewechsel` bei **jedem** Block. Folge je Block: `grenzeZiehen()` (`:1911-2114`) leert FFT-Ringe, alle drei Bandakkus, Verteilungsring, Stereoevidenz, Loudnesszellen, LRA-Histogramm und Fingerprint, erhöht `transportEpoche`; der Aufrufer ruft zusätzlich `analyseEngine.zuruecksetzen()` (`SondeProcessor.cpp:353`, `PluginProcessor.cpp:930`). Ergebnis ist ein vollständiger, stiller und dauerhafter Telemetrieausfall, solange der Zustand anhält — genau die Todesart, die der Kommentar bei `moeglicherStraddleIn` (`:1876-1880`) ausdrücklich als falsch verwirft. Kein Speicherfehler, kein Audioeingriff. Der fehlende Riegel ist eine Inkonsistenz im selben Blatt: `baueStempel` prüft dieselbe Klasse Hostdoubles fail-closed mit `std::isfinite` (`:3704-3708`). · Erreichbar: eingeschränkt — nur über einen fehlerhaften oder feindlichen Host-Prozesskontext; ein fremder unprivilegierter Prozess kann `ProcessContext` nicht schreiben. Kette: Host → `NakamaHostBridge::kontextAus` → `stempelAusKontext` → `StampedAudioQueue` → `FeatureEngine::nimmBlock:1150` → `grenzeZwischen:1784`. · Beleg: 1782–1784, gegen 3703–3708. · Register: neu.

- **[LOW]** `core/analysis/FeatureEngine.h:1893` (Nebenstelle `:1851`) · logik/protokoll + NaN · **Nicht-endliche oder verdrehte PPQ-Werte lassen den Straddle-Riegel fail-open laufen, obwohl der Erzeugerriegel dieselben Werte fail-closed abweist.** · Auslöser: Host meldet `kCycleValid` bzw. `getLoopPoints()` mit nicht-endlichem `cycleEndePpq`/`ppqPosition` oder `endePpq < startPpq`; Übernahme ungeprüft in `hostbridge/NakamaHostBridge.h:311,318-321` und `src/PluginProcessor.cpp:303-317`. · Wirkung: `b.cycleEndePpq > von && b.cycleEndePpq < bis` (`:1893`) ist mit NaN immer false → `moeglicherStraddleIn` meldet keinen Straddle, obwohl §32.3 an dieser Stelle „als ungueltig markiert" verlangt; `istLoopWrap` (`:1851-1852`) benennt die Grenze danach als `zeitSprung` statt `loopWrap`. Der Schaden ist begrenzt und deshalb LOW: das Ergebnis ist identisch mit dem im Code ausdrücklich gewollten Pfad „PPQ fehlt ganz" (`:1872-1880`), und `grenzeZwischen()` trennt den Wrap spätestens im Folgeblock über den Zeitsprung. Bleibt die Inkonsistenz, dass `baueStempel` dieselben Werte mit `std::isfinite` und `endePpq >= startPpq` verwirft (`:3704-3708`) und der Frame `cycle_bounds_valid = false` meldet, während die Grenzentscheidung sie benutzt hat. · Erreichbar: eingeschränkt, wie beim vorigen Befund nur über den Hostkontext. · Beleg: 1838–1856, 1876–1894, gegen 3703–3708. · Register: neu (NAK-70 betrifft `grenzeZiehen()`-Auslassungen bei `loopWrap`/`moeglicherStraddle`, nicht diesen Riegel).

Kein Befund, hier nur benannt, weil der Vertrag heute hält: `stereoBand(int)` (`:1215-1216`) und `ereignis(int)` (`:1347-1352`) indizieren ohne Bereichsriegel, anders als das Schwestermitglied `grenzenMitGrund()` (`:1378-1384`), das genau dafür nach T2-3 einen bekam. Alle heutigen Aufrufer halten den Vertrag ein (`SondeProcessor.cpp:499-503` mit `anzahl = ereignisAnzahlJetzt() <= kEreignisPlaetze`; `:550` mit fest `0` hinter `stereoHatInhalt()`), es gibt also keinen Eingabepfad. Ebenso ohne Eingabepfad: `++grundZaehler[(std::size_t) grund]` (`:1934`) ohne Bereichsriegel — alle Aufrufstellen (`:1161`, `:1171`) übergeben nur echte Gründe.

## Gate-5- und Invariantenbeleg

1. **Audio-Thread ohne Sperren, Allokationen, Datei-/Pipe-/Netzzugriff und Logging; Überlast verwirft Analyseframes, nie Audio — hält.** Meine Einheiten laufen ausschließlich im Analyse-Worker (`sonde/SondeProcessor.cpp:350`, `src/PluginProcessor.cpp:924`). Aller Speicher entsteht in `vorbereiten()` (`:969-1031`) und `Stufe::vorbereiten()` (`:1564-1601`) auf dem Nachrichtenthread. Im heißen Pfad `:1150-3551` gibt es kein `assign`/`resize`/`new`/`malloc`, keine Sperre, kein Logging und kein `throw` (Textsuche über den ganzen Bereich); `std::sort` läuft nur über feste Puffer (`flussSortiert` `:2794`, `float folge[64]` `:2963`) und alloziert nicht. Verworfen wird ausschließlich Analysematerial: `moeglicherStraddleIn`-Zweig `:1170-1178`, `grenzeZiehen()` `:1911-2114`, `zEreignisseVerworfen` `:2806-2807`.
2. **Passthrough bitidentisch, kein Hostpuffer angefasst — hält.** `verarbeiteSamples` nimmt `const float* daten` und liest nur (`:2283-2284`); in meinen Einheiten steht kein einziger Schreibzugriff auf einen Hostpuffer, keine Latenzmeldung und kein Ausfade.
3. **Gate 5 (kein Telemetrie-/Steuerframe steuert Gain oder erzeugt ein PRE/POST-Delta) — hält.** Die einzigen Steuereingänge meiner Einheiten sind `evidenzIntervallSetzen()` (`:1447-1454`, klemmt nur die Snapshot-Kadenz) und `setzePassagenfenster()`/`loeschePassagenfenster()` (`:1249-1310`, `:1314-1326`, verschieben nur Messfenster). Keiner davon berührt einen Gain, einen Filterkoeffizienten des Produktpfads oder einen Ausgabepuffer.
4. **NaN-Ehrlichkeit — teils gebrochen.** Hält auf Sampleebene: nicht-endliche Eingangssamples werden vor jeder Rechnung durch 0 ersetzt, verriegelt und gezählt (`:2297-2306`); der Verteilungsring nimmt nur `plausibel(db)` (`:2590-2592`, Riegel in `BandGrid.h:236-239`/`:252-257`, fängt NaN und ±inf); die Nyquist-Kappe sitzt in `zuordnung()` (`:1713-1737`) und wirkt über `bandBis <= bandVon` bis in Stereo- und Fingerprintpfad durch; die Stereosummen haben einen eigenen `isfinite`-Riegel beim Erzeugen (`:2868-2872`); ohne genügend Frames oder Werte gibt es keine Basislinie (`:3003-3021` Kohärenz/Phase, `:3030-3037` Perzentile ab vier Werten, `kurzLufs`/`momentanLufs` `:3325-3359`, `lraLu` `:3413-3419`). **Gebrochen** an drei Stellen: veröffentlichte 0 ohne Messung bei `korrelationKurz`/`persistenz` (Befund 1, `:2999-3000`, `:3025-3028`) und fehlender Endlichkeitsriegel im Entscheidungspfad für Hostdoubles (Befunde 2 und 3, `:1784`, `:1893`).
5. **State bleibt verlustfrei — nicht berührt.** In meinen Einheiten liegt kein State-Blob, keine Versionsprüfung und kein Dirty-Pfad.

## Ledger

- `core\analysis\FeatureEngine.h:1002-1151` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:1152-1301` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:1302-1451` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:1452-1601` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:1602-1751` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:1752-1901` · Befund 2 (`:1784`), Befund 3 (`:1893`, `:1851`)
- `core\analysis\FeatureEngine.h:1902-2051` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2052-2201` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2202-2351` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2352-2501` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2502-2651` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2652-2801` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:2802-2951` · Befund 1 (Ursache `:2908`)
- `core\analysis\FeatureEngine.h:2952-3101` · Befund 1 (`:2999-3000`, `:3025-3028`)
- `core\analysis\FeatureEngine.h:3102-3251` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:3252-3401` · Befund 0 (clean)
- `core\analysis\FeatureEngine.h:3402-3551` · Befund 0 (clean)

Zu den Pflichtfragen der Zuweisung, geprüft und ohne Befund:
- **bounds**: Alle Schreib- und Leseindizes in meinen Einheiten sind gedeckelt. `s.ringM/ringS/ringProjekt*/ringContinuous*[s.gefuellt]` (`:2429-2437`) bleibt unter `s.punkte`, weil `gefuellt` bei Gleichheit sofort auf `punkte - hop` zurückfällt (`:2439`, `:2545`). `bandVon/bandBis` sind in `zuordnung()` auf `[0, punkte/2+1]` geklemmt (`:1734-1736`), damit liegen alle `psd[k]`, `fftM/fftS.leistung/realTeil/imagTeil(k)` innerhalb der in `Fft::vorbereiten` angelegten `n` Elemente. `evidenzKante(b+1)` erreicht Index 221 in einem 222er-Feld (`BandGridZahlen.h:25`). Ringe mit Modulo (`kurzStand`, `flussStand`, `ereignisStand`, `fpOnsetStand`) sind vor jedem Zugriff normiert; `momentanLufs` (`:3351`) addiert `kKurzZellen * 2` vor dem Modulo und bleibt damit nichtnegativ. `folge[kVerteilungPlaetze]` bekommt aus `ringInZeitfolge` (`:3818-3825`) höchstens `gefuellt <= 64` Werte (`:3818-3825`) und wird nur bis `n` gelesen.
- **integer**: `const int n = (int) block.sampleCount` (`:2196`) ist sicher, weil die Queue `frames <= 0` und `frames > Layout::maxBlockFrames` verwirft (`StampedAudioQueue.h:380`, `:421`). Der Passagenausschnitt `passVon/passBis` (`:2218-2247`) kann `int` nicht sprengen: die Sättigung `bEnde = kMax` tritt nur ein, wenn `kMax - b0 < n`, dann ist auch `bis - b0 < n`. Alle Projektzeitadditionen laufen über `projektVorwaerts()` mit Überlaufprüfung (`:1629-1639`) oder über eine explizite Sättigung (`:1808-1812`, `:2237-2239`, `blockProjektSpanneGueltig` `:2117-2125`). Die Fensterzähler sättigen statt umzulaufen (`:2545-2546`). `fpOnsetProStelle *= 2` (`:3162`) erreicht den `int`-Überlauf erst nach rund 1,7e10 Analysefenstern (≈ 23 Jahre bei 48 kHz) und wird an jeder Grenze zurückgesetzt — kein realer Pfad. Divisionen prüfen ihren Nenner (`zellenSamples >= 1` `:993-994`; `bis - von > 0`; `mad > 0.0` `:2758`; `nenner > 0.0`; `gesamt > 0.0`; `spanne = max(…, 1e-9)` `:3218`).
- **return-values**: Jeder Rückgabewert, dessen Fehlschlag zählt, wird geprüft — `projektVorwaerts` (`:1657-1667`, `:2148-2170`), `kurzLufs`/`kurzTruePeak` (`:3295`, `:3384`), `lraBin` (`:3297`), `frameversuch().verstoss` (`:3499-3512`), `moeglicherStraddleIn`/`grenzeZwischen` (`:1157-1178`), `setzePassagenfenster` beim Aufrufer (`src/PluginProcessor.cpp:850-853`). Die eine bewusst verworfene Rückgabe ist `(void) tp.nachlauf()` in `setzePassagenfenster` (`:1281`) und dort begründet.

## Nicht gelesen

keine

## Außerhalb meiner Einheiten

- `eq-copilot/plugin/hostbridge/NakamaHostBridge.h:301, 308, 311, 318-321` — `sampleRate`, `tempo`, `ppqPosition`, `cycleStartPpq`/`cycleEndePpq` werden roh aus dem VST3-ProcessContext übernommen; es gibt auf diesem Weg keine Endlichkeitsprüfung.
- `eq-copilot/plugin/src/PluginProcessor.cpp:303-317` — derselbe Rohübernahmepfad über den JUCE-Playhead (`getPpqPosition`, `getBpm`, `getLoopPoints`).
- `eq-copilot/plugin/vertrag/NakamaEvidenz.cpp:313-315` und `:325-327` — `korrelation_kurz` und `persistenz` reisen am Präsenzbit `basisGesetzt`, während `kohaerenz`, `phase_rad` und die Zeitperzentile daneben eigene Bits tragen.
- `eq-copilot/plugin/core/analysis/Fft.h:114-134` — `leistung()`, `realTeil()`, `imagTeil()` indizieren `re`/`im` ohne eigenen Bereichsriegel; die Deckelung liegt allein beim Aufrufer.
- `eq-copilot/plugin/core/analysis/FeatureEngine.h:3958` — `abstand` der Konvergenzrechnung, nur im Vorbeigehen gesehen, nicht bewertet.
