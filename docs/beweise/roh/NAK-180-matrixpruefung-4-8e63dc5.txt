URTEIL: NEEDS_WORK — MP3-1 ist wegen nicht atomarer Generationsprüfung und Bereinigung nicht geschlossen; außerdem ist der Stop/Start-Beleg von N-34 an den ausgeschlossenen Detach-Pfad gebunden. Geprüft wurden der gebundene Fixdiff, E10/N-34 und die genannten C++-/Rust-Quellen bei unverändertem HEAD 8e63dc578b1176792bc22190b8570b8b91709e21. Nicht geprüft wurden der übrige Matrixbereich und ausführbare Produktbeweise; gemäß Auftrag wurden keine Tests oder sonstigen Prozesse gestartet.

Full review comments:

- [P1] Binde den End-Versand atomar an die Generation — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:122-122
  Klasse: **DEFEKT**. Nachdem der Worker für Zustand 3 `zustellGeneration == wireGeneration.load()` festgestellt hat, kann vor dem Einreihen des `end` bereits Link G+1 aufgebaut und dessen Filter abgeschlossen werden. Das danach eingereihte Ereignis überlebt gemäß E6 und kann auf G+1 vor dem Replay reisen; `intervention.rs:214-219` setzt dann `unknown`, womit N-29, R11 und der Gate-Satz brechen. Vergleich und Einreihen müssen gegenüber dem Generationswechsel atomar gekoppelt oder vor dem Commit erneut geprüft und mit diesem Interleaving getestet werden (`AGENTS.md:48-50`).

- [P1] Lösche nur die tatsächlich beobachtete alte Generation — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:215-215
  Klasse: **DEFEKT**. Das beschriebene Aufräumen per Setzen auf `0` ist nicht generationsbedingt atomar: Ein Verbraucher kann den alten Wert G sehen, der positive Callback schreibt anschließend G+1, und der verspätete Cleanup löscht dann den aktuellen Wert; ebenso kann ein alter negativer Callback nach dem positiven Callback von G+1 aufräumen. Dadurch können `replayFaellig`, `berichtOffen` oder das Aufbauurteil des neuen Links verloren gehen und N-23/N-34 bleibt verletzt. Das Aufräumen muss den beobachteten Wert per CAS entfernen, und `v3ControlLink(false)` darf nur Zustände seiner sterbenden Generation löschen (`AGENTS.md:48-50`).

- [P1] Trenne N-34 vom ausgeschlossenen Detach-Pfad — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:300-300
  Klasse: **LÜCKE**. Ein `stop()` auf dem zweiten Thread kann nicht vollständig zurückkehren, während der positive Callback weiter festgehalten wird, ohne nach `kStopFristMs` den Client-Thread in `ControlClient.cpp:724-743` abzutrennen; genau dieser abgelöste Pfad wird in §2.4 ausdrücklich weder zugesagt noch gemessen. Wird der Callback vorher freigegeben, entsteht das von N-34 verlangte Schreiben nach `stop()`/`start()` nicht mehr. Der Test muss entweder den Detach-Pfad in den Umfang aufnehmen oder den Stop/Start-Fall so formulieren, dass er ohne diese ausgeschlossene Voraussetzung tatsächlich ausführbar ist.