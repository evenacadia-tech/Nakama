Die Matrix lässt mehrere nachweisbare R2-Fenster und Reconnect-Fehler offen, insbesondere durch die verbindungsübergreifende P0-Queue und vor dem ersten Heartbeat. Außerdem schwächt der Bauplan den von R3a verlangten Bytevergleich ab.

Full review comments:

- [P1] Binde eingereihte P0-Nachrichten an die Linkgeneration — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:33-33
  DEFEKT (E2/N-23): `ControlClient::Laufzeit::sendeP0` reiht nur ein (`ControlClient.cpp:799-832`); scheitert der spätere Wire-Write, legt der Client dasselbe JSON in die verbindungsübergreifende P0-Queue zurück (`:1454-1463`). Das `exchange(false)` hat das Aufbau-Flag dann bereits verbraucht, und `v3ControlLink(false)` löscht zwar Flags, aber nicht den alten Heartbeat. Auf dem nächsten Link kann so ein altes `false` den R1-Zweig auslösen oder ein altes Begin mit dem neuen Replay dieselbe ID doppeln. Das bricht R2/N-23 und M-39; die Matrix braucht Linkgenerationsbindung und einen Test mit Abbruch zwischen Enqueue und Wire-Commit.

- [P1] Decke ein abgewiesenes Marker-Begin im R2-Weg ab — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:110-110
  DEFEKT (N-05/R2): N-05 setzt voraus, dass das Begin gesendet wurde, obwohl R2 für jeden beim Aufbau hörbaren Marker gilt. Heute entnimmt `interventionenSenden` das Ereignis vor `sendeP0`; bei dessen Fehlschlag bleibt nur das Sticky-Bit (`PluginProcessor.cpp:1077-1087`). Nach E1 existiert dann kein „offenes gesendetes Begin“, also folgen auf dem neuen Link zwar `true`, aber kein Replay; das normale End trifft beim Broker auf kein Begin (`intervention.rs:214-219`), startet keinen Nachlauf und verwirkt den Bericht, sodass der reguläre Pfad nicht nullt. Dieser Sendefehler braucht eine eigene Matrixzeile samt Rotbeweis und einen erhaltenen oder rekonstruierten Begin/End-Pfad.

- [P1] Sperre Evidenz bereits zwischen Hello und erstem Heartbeat — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:112-112
  DEFEKT (N-07/R2): Auf einem frischen Broker legt `control_hello_registrieren` Link und Sitzung zunächst ohne Taint an (`broker/src/coordinator/link.rs:238-278`); Unknown entsteht erst beim später verarbeiteten `true`-Heartbeat (`befehl.rs:550-557`), während `evidence_dispatch_locked` eine Sitzung ohne Taint als erlaubt behandelt (`intervention.rs:577-586`). Eine Sonde kann daher zwischen angenommenem Main-Hello und erstem Heartbeat starke Evidenz committen, obwohl der lokale Marker läuft. N-07 injiziert erst nach dem Heartbeat und misst dieses Fenster nicht, womit die ausdrückliche R2-Zusage „kein Fenster“ unerfüllt bleibt.

- [P2] Vergleiche den vollständigen Heartbeat bytegleich — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-180.md:215-215
  DEFEKT (R3a/§4.2): R3a verlangt ausdrücklich, dass der Plugin-Test `heartbeatAlsJson` bytegleich gegen die erzeugte Fixture prüft; §4.2 beschränkt den Bytevergleich jedoch auf das einzelne Feldsegment und vergleicht alle übrigen Felder nur nach dem Parsen. Dadurch bleiben Wire-Regressions bei Reihenfolge, Escaping oder Auslassung außerhalb dieses Segments unentdeckt. Diese Abweichung ist zudem nicht in §4.4 begründet; erforderlich ist eine vollständig bytegleiche Wire-Fixture oder ein entsprechend vollständig reproduzierbarer Erwartungstext.