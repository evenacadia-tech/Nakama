# G4 — Phasengate P4 · Vergleichsevidenz und manueller Versuch (T3)
<!-- NAKAMA-URTEIL: T3 NEEDS_WORK 2026-09-05 offen -->

**Datum:** 05.09.2026 · **Prüfstufe:** T3 · **Gate-Text (wörtlich, `docs/bauaufteilung-sonden.md:393`):** „**Gate:** `/c-review` (DSP) + Codex. Falsifikation: Gate 5, 6" · **Session:** eigene (nakama-dirigent `3d3f130c`), hat P4 nicht gebaut und keine Zeile Produktcode, Schema, Test, Fixture oder Runner angefasst. **Vorbild:** `docs/beweise/G3.md`.
**Gate-Stand:** `9f3000881df632f4969d78551fb0a516b61288cb` (= `origin/master`, Worktree sauber). Seit dem Kanon-Stand `043e98e` (GRÜN 53/53, `docs/beweise/roh/SONDE-013-043e98e.md`) liegen nur `docs/**`-Commits — `git diff --stat 043e98e..9f30008 -- broker eq-copilot tools` ist leer; die 53 Beine gelten für den Code unverändert. Phasenänderungssatz: `ed9bbf7...043e98e` (SONDE-013, S20–22, T2 PASS 2026-09-05).

---

## 1. Urteil

**NEEDS_WORK.**

Gate 5 **hält** — von zwei Modellen unabhängig „kein Pfad gefunden" (Codex 10 Wege, eigener Lauf 10 Wege, §4). Gate 6 **hält** im Produktpfad (§5), aber sein Prüfwerkzeug nicht: A26 würde einen Gate-6-Verstoß im Korpus als grün ausweisen (C6). Das P4-Exit-Gate **fällt in allen drei Sätzen**, je an der Quelle bestätigt:

- **Satz 3 („Hörmarker- und Experimentende schließen alle Taintintervalle; der reguläre Pfad nullt danach wieder") — D-01, von beiden Modellen unabhängig gefunden.** Der einzige Löscher des sticky `intervention_state_unknown` ist `neutral_resync` (`broker/src/coordinator/intervention.rs:370-399`); sein einziger Produktaufrufer verlangt im Heartbeat ein ausdrückliches `"intervention_state_unknown": false` (`broker/src/coordinator/befehl.rs:550-575`), und der einzige Heartbeat-Erzeuger lässt das Feld bei `false` weg (`eq-copilot/plugin/core/ipc/ControlClient.cpp:404-411`). `control_ende` setzt `unknown` für jede Sitzung, die noch Clients hat (`broker/src/coordinator/link.rs:444-453`). Nach dem ersten Control-Reconnect von Gen — jedes `prepareToPlay` (`PluginProcessor.cpp:219`), `setzeBindung`, Broker-Neustart, Pipeabbruch — bleibt `starke_evidenz_erlaubt` für die Sitzung dauerhaft `false`, und jeder `evidence_dispatch` wird als `evidence_gesperrt` gezählt. M-60/M-61 sind unerreichbar. Richtung fail-closed: keine falsche starke Aussage, aber der Evidenzpfad der Sitzung verstummt — genau die Sorte Fehler, die SONDE-013 R01/B16 schließen wollte (`PluginProcessor.cpp:1188-1189`). Kein Bein fällt, weil die Rust-Beine `neutral_resync`/`resync_bestaetigen` direkt rufen, B23 `v3LinkFuerTest` direkt ruft (NAK-156) und B04 nur den JSON-Text prüft. Dazu **C2**: startet der Broker, während ein Marker hörbar ist, kennt der neue Broker den laufenden Marker nicht (kein Begin-Replay, `PluginProcessor.cpp:1199` überspringt den Resync wegen `hoerbar()`, setzt aber nichts) — bis zum `end` gilt Sondenevidenz als sauber (M-62/M-63).
- **Satz 2 („Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage") — C3, C4, C5.** Probeeq setzt nach einem Quarantänebruch die Engine ganz zurück (`SondeProcessor.cpp:341` `merkmale.zuruecksetzen()`), der gehaltene Block läuft mit `habeVorigen=false`, der Bruch erreicht Epoche/Segment nicht (M-53; Gen macht es in `PluginProcessor.cpp:904-912` richtig). `prepost.rs:926` serialisiert für alle zehn harten Ausschlüsse den Kettenbefund `Stationaer` statt `NichtBeurteilbar` (M-18/M-22). `prepost_verdrahtung.rs:274-277` überschreibt die gemessene Aktivzeit mit der Projektspanne, der zweite der „genau drei" M-23-Gründe ist im Produktpfad tot.
- **Satz 1 („Referenzkorpus, Loop-/Seek-/PDC-Goldens und adversariale Vergleichsfixtures bestehen") — C6, C7, C8, C9, C10, C11.** `pruefe_p4_korpus.py:139` lässt starke Aussagen bei `unvergleichbar`/`nicht_kausal`/`nicht_exakt` durch (M-79, Gate 6/7 im Korpus blind). Nur 8 von 22 Korpus-Fallnamen existieren wörtlich, einer ist substanzlos (`B18::lufs_integrated_matches_reference`; die ±0,1-LU-Prüfung liegt in B9; M-81). M-82 (Stereo über alle Blockgrößen: B19 fest auf 512), M-83 Satz 2 (gekoppelter 0,01-dB-Evidenzpfad), M-84 (Impuls-Sweep über Blockgrößen/Sampleraten) und M-85 Ziel 1 (korrelierter nichtkausaler Distraktor: der Begriff kommt in keinem Code, Test oder Fixture vor) sind **ungemessen**, obwohl die Matrix sie als belegt führt.

Dazu drei bestätigte Defekte des C++-Reviews außerhalb der Falsifikationsziele: der Hörmarker latcht ein nicht-endliches Hostsample dauerhaft in seinen Filterzustand und schreibt danach NaN in den Hostpuffer (§7, Invariante NaN-Ehrlichkeit); `WaitNamedPipeW(…, 0)` übernimmt die Serverfrist eines fremden Pipe-Besitzers, und der Destruktor joint den Lebenslaufthread fristlos (§7, §48.4); der Kandidat eines Experiments liest den lebenden statt des eingefrorenen Match-Gains (M-43). Alle Defekte sind nach Ursache in **drei Nacharbeitstickets** gebündelt (§11, Register NAK-180 bis NAK-182); Lücken und Härtungen stehen datiert im Register (§8).

---

## 2. Bruchaufträge — wörtlich

Beide Modelle bekamen denselben Auftrag (`docs/beweise/roh/G4-codex-falsifikation-auftrag.txt`) mit erzwungener Antwortform (konkreter Pfad **oder** wörtlich „kein Pfad gefunden", je Auftrag die Zahl der durchgespielten Wege und die messenden Tests):

> **B1 — Gate 5** (Entwurf §49.2 Nr. 5, `docs/FL-Nakama-Sonden-Design-Entwurf.md:3556`: „Telemetrie steuert samplegenauen Gain oder erzeugt hörbares PRE/POST-Delta."): „Finde einen Pfad, auf dem ein Telemetrie- oder Steuerframe … samplegenauen Gain im Audiothread steuert oder ein hörbares PRE/POST-Delta erzeugt — in Gen (PluginProcessor.cpp) oder in Probeeq (SondeProcessor.cpp). Die einzige zulässige hörbare Ausnahme ist der lokale, useraktivierte Hörmarker (HoerMarkierung.h) mit den vier fail-closed Termen aus M-33 und dem Ausfade aus M-34 bis M-36."

> **B2 — Gate 6** (`:3557`: „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil."): „Finde einen Pfad, auf dem ein Experiment ohne starke Vergleichbarkeit (M-28/M-29 …, M-31, M-30, M-63, unbekannte Zeit, fehlender oder nicht eingefrorener Match-Gain M-43) trotzdem eine starke Aussage aus M-46 als Siegerurteil erhält oder persistiert … Kann ein Wire-Client (Gen) ein starkes Urteil selbst setzen, das der Broker ungeprüft persistiert?"

> **B3/B4/B5 — P4-Exit-Gate** (Entwurf §58, `:4287-4289`): Satz 2 (unbekannter Zeitpfad → starke Cross-Probe-/PRE/POST-Aussage), Satz 3 (Marker-/Experimentende schließen alle Taintintervalle, regulärer Pfad nullt), Satz 1 (je Zusage M-79 bis M-86 der messende Test oder „ungemessen").

Ausschlüsse: `docs/**` keine Befundfläche; Register NAK-135 bis NAK-154 (03.09.2026) und NAK-155 bis NAK-179 (05.09.2026); same-user-Szenarien (Entwurf §48.4); FL-Termine; `tools/dirigent/pruefliste.md` keine Anforderungsquelle; Sabotage gegen Prüfwerkzeuge = Härtung.

---

## 3. Wer geprüft hat

| # | Prüfer | Modell / Effort | Fläche | Ergebnis |
|---|---|---|---|---|
| 1 | Codex, Falsifikation | `gpt-5.6-sol`, max, read-only, elevated Sandbox, Thread `01a06edb-42f2-7973-938f-4c9700d22426`, 01:57–02:51 | Prüfbereich aus dem Auftrag (DSP-Header, Prozessoren, Hörmarker, IPC-Clients, Vertrag, Coordinator, Store, Korpus-Skripte) | Gate 5 **kein Pfad** · Gate 6 im Produkt **kein Pfad**, im Prüfer **Pfad** (C6) · Exit-Gate: **11 Befunde** (10 P1, 1 P2), alle an der Quelle bestätigt (§8); Urteil `docs/beweise/roh/G4-codex-falsifikation-9f30008.txt` |
| 2 | eigener Falsifikationslauf | Opus, 1 Agent, lesend, 104 Werkzeugaufrufe, 986 s; 15 Test-Exes vom Kanonstand gefahren (alle grün) | dieselbe Fläche | B1 **kein Pfad** (10 Wege) · B2 **kein Pfad** (10) · B3 **kein Pfad** (10) · B4 **Pfad** (D-01, 12 Wege) · B5: M-82/M-84 ungemessen, Korpus-Fallnamen ungebunden; 1 Defekt, 2 Härtungen, 3 Lücken (`roh/G4-eigener-lauf-9f30008.md`) |
| 3 | `/c-review` (DSP), Ersatzform | Trail-of-Bits-Workflow 2.0.1 (Detect) + **14 Opus-Klartext-Leser** über dieselbe Standortpartition, `LOCAL_UNPRIVILEGED`, Severity `all` | `eq-copilot/plugin/` ohne `tests/` und `vertrag/generiert/`: 76 Dateien, 915 Einheiten, 34 585 Zeilen; Ledger je Einheit, alle 14 Leser 100 % gelesen | **3 HIGH, 14 MEDIUM, 35 LOW**; HIGH/MEDIUM alle an der Quelle geprüft: 17 bestätigt, 1 widerlegt (§7); Parts `roh/G4-creview-parts-9f30008/`, Leserauftrag `roh/G4-creview-leser-auftrag.md` |
| 4 | Quellencheck der Befunde | Dirigent selbst (D-01, NaN-Latch, Pipe-Wait, L-01 bis L-03, H-01/H-02, C7-Fallname) + zwei lesende Opus-Prüfagenten (V01–V15: `roh/G4-quellencheck-leser-9f30008.md`; C2–C11: `roh/G4-quellencheck-codex-9f30008.md`) | jeder HIGH/MEDIUM/P1/P2-Befund gegen Quelle, Matrix, Gate-Text, §48.4, Invarianten, Register | Einordnung in §8 |

**Werkzeugausfälle, protokolliert (kein Codebase-Befund):**
- `/c-review` 2.0.1 trägt den G2-Windows-Bug weiter: `scripts/enumerate_units.py::_within` vergleicht mit `/`, `Path.resolve()` liefert unter Windows `\` — jede Unterdatei gilt als „outside the scope root". Der Detect-Lauf fuhr über eine Kopie des Plugins im Session-Scratchpad mit gepatchtem `_within` (`os.sep`-tolerant) und `workflows/c-review.js` auf LF (der Freigabedialog des `Workflow`-Werkzeugs lehnt CR-Bytes als „control characters" ab). Detect: 76 Dateien, 915 Einheiten, `refused: []`, `parse_degraded_files: 50`, 14 Zuweisungen (`.c-review-results/20260904T235509Z/`, gitignoriert).
- Danach blockierte der Safety-Classifier **alle 16** Review-/Sweep-Agenten des Workflows („output schema too large to classify safely"), Run `wf_0049a738-b61`, 0 Parts — dieselbe Form wie in G2. Ersatz nach G2-Vorbild: 14 Opus-Klartext-Leser mit derselben Partition, Fragenkatalog und Bedrohungsmodell, ohne großes Ausgabeschema. Kein Richter lief; jede Schwere ist das Urteil des Lesers, die Einordnung hat der Dirigent an der Quelle nachgeholt (§7, §8).
- Der erste Codex-Start lief als sitzungsgebundenes Hintergrundkommando und wurde nach zwei Minuten bewusst gestoppt (10-Minuten-Werkzeuglimit); der gewertete Lauf ist der abgekoppelte Start (PID 21884, 01:57:37) mit Logdatei im Temp-Ordner, HEAD vor und nach dem Lauf `9f30008`.

---

## 4. Falsifikation Gate 5 — gehalten

Beide Modelle unabhängig „kein Pfad gefunden" (je 10 Wege). Kein Wert, der über `ControlClient`/`TelemetryClient` hereinkommt, erreicht Verstärkung, Filter, Match-Gain oder Ausfade: Probeeq hat keine Inbound-Handler und schreibt nie in den Hostpuffer; in Gen liegen die vier M-33-Terme vollständig in `PluginProcessor.cpp:470-479`, der Marker wird ausschließlich aus dem Editor gesetzt, und `HoerMarkierung.h` liest keinen Wire-Zustand. Messende Beine: `EqCopNullTest` (`NULLTEST OK`), A16 `EqCopProbeeqNullTest` (87/0), `EqCopMarkierungTest` (OK), B21 `EqCopSonde013InterventionRingTest` (22/0) — vom eigenen Lauf gefahren. 14 Leser bestätigen Invariante 3 in ihren Einheiten mit Zeilenbeleg (Parts).

## 5. Falsifikation Gate 6 — im Produkt gehalten, im Prüfer gefallen

Produktpfad: `experiment_manual_result` kennt kein Urteils- oder Vergleichbarkeitsfeld (`additionalProperties:false`), das Urteil entsteht nur in `sicht.rs:413-417` aus brokereigener Rechnung, die Blindreihenfolge (M-44) wird erst mit dem Terminal aufgedeckt; ein Wire-Client kann kein starkes Urteil setzen. Beide Modelle „kein Pfad" (je 10 Wege). **Aber:** der Gen-Empfänger nimmt für `experimente`/`paare` jeden String an (V09/V10, M-46/M-22), und das Exit-Gate-Bein A26 lässt einen Korpusfall mit `wahrheit` in {`unvergleichbar`, `nicht_kausal`, `nicht_exakt`} und `aussage=stark` als grün durch (C6, `pruefe_p4_korpus.py:139` gegen `moegliche_starke:91`) — der Prüfer würde einen Gate-6-Verstoß nicht sehen. Beides Nacharbeit (§11).

## 6. P4-Exit-Gate je Satz — an der Quelle

| Satz | Stand | Befunde |
|---|---|---|
| 1 Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures bestehen | **fällt** | C6 `_passt`; C7 Fallnamen (8/22 wörtlich, `B18::lufs_integrated_matches_reference` substanzlos); C8/L-01 M-82 Blockgrößen; C9 M-83 Satz 2; C10/L-02 M-84 Sweep; C11 M-85 Ziel 1; L-03 A26 prüft nur das Bein |
| 2 Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage | **fällt** | C3 Quarantänebruch in Probeeq (M-53); C4 `Stationaer` bei Ausschluss (M-18/M-22); C5 Aktivzeit (M-23). `presentation_validiert` ist im Produktpfad hart `false` (`prepost_verdrahtung.rs:292`), `FeatureAligned` unerreichbar — B3 des eigenen Laufs „kein Pfad" bezieht sich darauf; die drei Codex-Befunde liegen daneben und halten |
| 3 Hörmarker- und Experimentende schließen alle Taintintervalle; der reguläre Pfad nullt danach wieder | **fällt** | D-01 (= C1), C2 |

Neun saubere Wege zu Satz 3 stehen im eigenen Lauf (`roh/G4-eigener-lauf-9f30008.md` B4): normales Markerende, Ende ohne Projektzeit, Nachlauf, M-60-Sicht, Ringüberlauf ohne `drop-oldest`, P0-Sendefehler, Experimentterminal in jeder Sitzung, Abbruch ohne Terminal über den Bestandsdeckel, Control-Disconnect.

## 7. C++-Review `eq-copilot/plugin/` (DSP) — HIGH und MEDIUM, an der Quelle

| Part | Stelle | Befund (Kurz) | Einordnung des Dirigenten |
|---|---|---|---|
| 09 HIGH | `HoerMarkierung.h:548-560`, `:405`, `:468`, `:518-522` | ±Inf/NaN im Hostpuffer latcht den TDF2-Zustand des Wet-Pfads; danach schreibt `:468` NaN in den Hostpuffer, solange der Marker engagiert ist; Reset erst bei `fade<=0 && !zielAn`; kein Riegel, kein Zähler | **Defekt** (Invariante NaN-Ehrlichkeit) → NAK-180 |
| 05 HIGH | `IpcVerbindung.cpp:27`, `BrokerLifecycle.cpp:784/660-661`, `PluginProcessor.cpp:167` | `WaitNamedPipeW(name, 0)` = Serverfrist; ein fremder Pipe-Besitzer mit `nDefaultTimeOut≈∞` hält den Lebenslaufthread, `stop()` joint fristlos aus dem Destruktor → Host-Message-Thread hängt | **Defekt** (§48.4 „schützt vor fremden Windows-Usern") → NAK-180 |
| 11 HIGH | `PluginProcessor.cpp:2090` (+`:1937/:1946`, `:2026`) | `erfasseKandidat` liest den lebenden Vergleichspegel ohne `gainGesetzt()`-Riegel; `vorbereiten()` zwischen begin und candidate → `match_gain_db:0` | **Defekt** (M-43, M-07) → NAK-181 |
| 09 MEDIUM | `HoerMarkierung.h:252-264` | `vorbereiten()` ohne `endete` → Intervall bleibt offen, 64 Wechsel kippen die Sitzung | **widerlegt wie beschrieben**: `prepareToPlay` ruft `controlV3.reconnect()` (`:219`), `control_ende` entfernt die Intervalle des Links (`link.rs:450-452`); der Endzustand ist D-01 (sticky unknown), nicht 64 offene Intervalle → in NAK-180 als Matrixzeile |
| 11 MEDIUM | `PluginProcessor.cpp:2022-2027` | `versuchspegelSpeist` nach „zu wenig Material" nie wieder an | **Lücke**, Regel in derselben Runde entschieden (retryfähig) → NAK-181 |
| 11 MEDIUM | `PluginProcessor.cpp:1395-1446` | `setStateInformation` leert Experiment-/Passagenzustand nicht → meldet unter neuer `project_binding_id` | **Defekt** (M-50) → NAK-181 |
| 05 MEDIUM | `ControlClient.cpp:1412-1414` | `sendeP1`-Ergebnis verworfen, `abgewiesen`/`zuGross` nie wiederholt (heilt beim Reconnect) | **Härtung** → NAK-183 |
| 13 MEDIUM | `NakamaEvidenz.cpp:78` (+`ControlClient.cpp:374`) | `std::to_string(double)` locale-abhängig (MSVC /MD); Komma-Locale → ungültiges JSON oder 442 statt 221 Werte | **Defekt** (Invariante „Schemas sind Verträge"; Gegenprobe `PluginProcessor.cpp:1752-1760`) → NAK-181 |
| 02 MEDIUM | `FeatureEngine.h:2999-3028` (Ursache `:2908`), `NakamaEvidenz.cpp:313-327` | `korrelationKurz`/`persistenz` ohne Präsenzbit; bei 0,25 s reichen die Frames nie für 8 → in jedem Snapshot als gemessene 0,0 | **Defekt** (M-11, M-08) → NAK-181 |
| 04 MEDIUM | `ControlClient.cpp:1023-1027` | verlorene Weckung, unbefristetes `warte.wait` | **Härtung** (Nebenläufigkeit, G6) → NAK-184 |
| 04 MEDIUM | `ControlClient.cpp:727-742` | nach Fristablauf detachter Thread ruft Callbacks in den Prozessor (UAF bei Callback > 2 s) | **Härtung** (G6) → NAK-184 |
| 12 MEDIUM | `SourcesModel.cpp:530-612` | Enums und `pair_id maxLength` aus dem Schema nicht durchgesetzt | **Defekt** (M-46, M-22) → NAK-181 |
| 12 MEDIUM | `SourcesModel.cpp:607-609` | nicht-String `ausschluss` still verworfen = fail-open | **Defekt** (M-22) → NAK-181 |
| 12 MEDIUM | `SourcesModel.cpp:429-431` | `Diagnose::incompatible` nach ~33 ms vom Editor-Timer gelöscht | **Lücke** (UI, S31b) → NAK-186 |
| 10 MEDIUM | `PluginProcessor.cpp:611` | `tailSamples` aus ungeprüfter `getSampleRate()` statt `samplerateAtomic`; nicht-endliche Rate → Tail 1 Sample | **Defekt** (Invariante NaN-Ehrlichkeit) → NAK-180 |
| 14 MEDIUM | `NakamaVertrag.cpp:487` | `teil()` Θ(k²) — 262-KB-Token blockiert den Control-Thread (nur authentisierter Peer) | **Härtung** → NAK-185 |
| 14 MEDIUM | `NakamaVertrag.cpp:474` | kein Tiefenriegel vor `juce::JSON::parse` (nur authentisierter Peer) | **Härtung** → NAK-185 |
| 14 MEDIUM | `NakamaTelemetrie.cpp:621` | NAK-150 hat seit SONDE-012/013 einen Produktionsaufrufer | **Register-Nachtrag** NAK-150 |

35 LOW-Befunde stehen in den Parts (je mit Datei:Zeile, Auslöser, Erreichbarkeit); sie sind nicht einzeln an der Quelle geprüft und gehen als Sammelzeile ins Register (NAK-189). Zwei Parts fanden bereits registrierte Punkte wieder (NAK-143, NAK-144, NAK-154, NAK-78, NAK-141) und führen sie nicht neu. Leser 01 meldet Registerdrift an NAK-71 (b)/(c) (`neustartWunsch` ist seit `2f3ee24` ein release/acquire-Paar) — Register nachgezogen.

## 8. Befundklassen — an der Quelle eingeordnet

**Defekt** (Satz bricht; Nacharbeit): D-01/C1, C2, NaN-Latch, Pipe-Wait, V12 → **NAK-180**; V01, V03, V05, V06, V09, V10, C3, C4, C5 (+ entschiedene Lücke V02) → **NAK-181**; C6, C7/L-03, C8/L-01, C9, C10/L-02, C11 → **NAK-182**.
**Lücke** (Gate, Matrix, Entwurf sagen nichts; datiert ins Register): V11 (NAK-186); M-85 Ziel 1 als P5-Gegenstand (NAK-190, Entscheid unten).
**Härtung** (von keiner Zusage verlangt): V04 (NAK-183), V07+V08 (NAK-184), V13+V14 (NAK-185), H-01 (NAK-187), H-02 (NAK-188, löst sich mit NAK-180), LOW-Sammlung (NAK-189), V15 (Nachtrag NAK-150).
**Widerlegt:** Leser-09-MEDIUM „64 offene Intervalle" (§7, Zeile 4) — `prepareToPlay` führt über `reconnect()` zu `control_ende`, die Intervalle des Links werden entfernt; der wahre Endzustand ist D-01.

**Entscheid des Dirigenten zu C11/M-85 (Technik/Plan, 05.09.2026):** „Ein korrelierter, aber nicht kausaler Distraktor wird als Alternative gezeigt" setzt Ursachen und Alternativen voraus — das ist Lieferumfang von P5 (Entwurf §59, S23–25/SONDE-014) und Falsifikationsziel von **G5** („adversariale Gegenbeispiele … falsche starke Ursachenbehauptung provozieren"). P4 gibt keine Ursachenaussage ab; die P4-Fassung von M-85 Ziel 1 ist deshalb nur, dass der Korpus nichts behauptet, was B24 nicht misst (C11-Korrektur in NAK-182), und dass kein P4-Datenweg einen „sicheren Auslöser" benennt. Das vollständige Ziel geht datiert als NAK-190 in Register und SONDE-014-Ticketquelle. Kein Produktentscheid: Bedienlogik und sichtbare Priorität ändern sich nicht.

## 9. Kanon

Kein Code-Diff seit dem beglaubigten Kanon `043e98e` (GRÜN 53/53, `docs/beweise/roh/SONDE-013-043e98e.md`); die 53 Beine gelten für den Gate-Stand `9f30008` unverändert. Realtime-Guard- und Blockgrößen-Stresslauf (T3-Tabelle, Bauaufteilung §3): B4 `EqCopQueueStressTest` (116/0 im eigenen Lauf), B6 `EqCopDspGoldenTest`, B19 `EqCopSonde013StereoGoldenTest` (57/0, aber fest 512 Frames — C8), B24 `EqCopSonde013PrePostGoldenTest` (30/0), A16 und `EqCopNullTest` sind Teil des Kanons. Der Kanon läuft neu auf dem Endstand jedes Nacharbeitstickets und beim Gate-Abschluss.

## 10. Was das Urteil nicht deckt

- Kein FL-Lauf, kein Hörtest, kein installierter Stand.
- `eq-copilot/plugin/tests/**` und `vertrag/generiert/**` waren keine Fläche des C++-Reviews (`exclude`); Tests sind für ein DSP-Gate keine Angriffsfläche, der generierte FlatBuffers-Header wird durch A9 `pruefe_flatc_drift.py` gegen den `.fbs` gehalten.
- Kein Richter im C++-Review: 35 LOW-Befunde sind Leserurteile ohne Quellencheck des Dirigenten (Sammelzeile NAK-189).
- Codex fuhr weder Test-Exes noch Python; der eigene Lauf fuhr 15 Plugin-Exes, keine Rust- und Python-Beine.
- `broker/` war keine Fläche eines Rust-Reviews (G3 hat ihn zuletzt gelesen; G7 ist das nächste Rust-Gate).

## 11. Nacharbeit — was ein PASS braucht

Drei Tickets, je frischer Opus-Worker (max), Aufsicht ENG, **Spezifikation vor Code** (Verhaltensmatrix im Ticketmanifest, lesende Codex-Matrixprüfung, dann Bau), betroffene Beine je Runde, voller Kanon abgekoppelt am Ticketende, Wiederprüfung nach Vorlage B mit frischem Codex-Thread (Sol, Effort unverändert). Reihenfolge nach Risiko:

1. **NAK-180 — Plugin-Lebenszyklus: Taint-Resync, Hörmarker, Broker-Wartefrist** (`docs/beweise/NAK-180.md`). R1 Der bestätigte Neuaufbau reist ausdrücklich (erster Heartbeat eines neuen Links trägt `intervention_state_unknown:false` genau dann, wenn der Prozessor Neutralität festgestellt hat; kein Selbstheilen, `befehl.rs:572` bleibt der Riegel). R2 Läuft beim Neuaufbau ein Marker oder ist der Ring nicht leer, muss der Brokerzustand der Sitzung nach dem Neuaufbau dem wahren lokalen Zustand entsprechen (offenes Intervall mit derselben `intervention_id`, das das normale `end` schließt; Backlog in Reihenfolge; kein Fenster, in dem fremde Evidenz als stark durchgeht — gemessen mit injizierter Sondenevidenz zwischen den Aufbaunachrichten); die Matrix entscheidet Reihenfolge und Mittel ohne Schemaänderung. R3 Die Naht wird ende-zu-ende gemessen: Fixture aus dem echten Serializer, Broker-Test durch `p0_json_mit_minor`, B23 `r01Resync` über den echten ControlClient (NAK-156 erste Hälfte), B04 misst beide Zusagen. R4 Der Wet-Pfad des Hörmarkers latcht nie einen nicht-endlichen Zustand: nicht-endliche Eingangssamples gehen als 0 in das Filter, der Zustand bleibt endlich, das Ereignis wird gezählt und ist im Status lesbar; Rotbeweis mit ±Inf-Block. R5 `tailSamples` rechnet mit der geprüften Samplerate (`samplerateAtomic`). R6 `namedPipeErreichbar` wartet mit expliziter kleiner Frist, nie mit der Serverfrist; kein Aufruf im Lebenslaufthread blockiert unbegrenzt, `stop()` kehrt in gemessener Frist zurück — Test mit besetztem Pipenamen und `nDefaultTimeOut=0xFFFFFFFE`.
2. **NAK-181 — Vergleichsevidenz-Semantik** (`docs/beweise/NAK-181.md`). Match-Gain eingefroren und geriegelt (V01, M-43); Experiment-/Passagenzustand fällt mit dem Projektwechsel (V03, M-50); JSON-Zahlen locale-unabhängig in allen Serialisierern (V05, `NakamaEvidenz.cpp:78`, `ControlClient.cpp:374`); Präsenzbits für `korrelationKurz`/`persistenz` oder Kadenz, die sie erreichbar macht (V06, M-11); Gen-Empfänger setzt geschlossene Enums und Längen durch, `ausschluss` fail-closed (V09/V10, M-46/M-22); Probeeq zieht die Kontinuitätsgrenze wie Gen (C3, M-53); `NichtBeurteilbar` bei Ausschluss (C4, M-18/M-22); Aktivzeit bleibt gemessen (C5, M-23); entschiedene Lücke V02: `versuchspegelSpeist` retryfähig, „zu wenig Material" ist kein Endzustand.
3. **NAK-182 — Korpus und Goldens** (`docs/beweise/NAK-182.md`). `_passt` fängt alle Wahrheiten außer der passenden, mit Negativtest des Prüfers (C6, M-79); jeder Korpus-Fallname existiert wörtlich in der genannten Testquelle und A26 prüft das (C7/L-03); die LUFS-I-Referenz zeigt auf B9 oder bekommt einen echten B18-Fall (M-81); B19 fährt die fünf Stereo-Klassen über alle unterstützten Blockgrößen (C8, M-82); der gekoppelte 0,01-dB-Evidenzpfad Sender → Dekodierung → PRE/POST ±0,1 dB samt Intervall wird gemessen (C9, M-83); Impuls-Sweep über Blockgrößen und Sampleraten (C10, M-84); der Korpus behauptet zu M-85 nur, was gemessen wird (C11, Entscheid §8).

Nach dem dritten PASS: Nachtrag hier (§12) mit Marke `T3 PASS`, Kanon auf dem Endstand, Planstand, Register.

## 12. Nachtrag (2026-09-05, Dirigent): NAK-180 abgenommen — T3 PASS

Nacharbeitsticket 1 von 3 (`docs/beweise/NAK-180.md`, Urteilsmarke im Kopf, §7.6) ist mit Endstand `44c623d` abgenommen: Satz 3 des Exit-Gates (D-01/C1, C2) und die drei C++-Review-Defekte (NaN-Latch, Tail-Samplerate, Pipe-Wartefrist) sind geschlossen; Verhaltensmatrix E1 bis E11 und N-01 bis N-37 gemessen, Kanon GRÜN 53/53 auf `5083eb6`. Prüfer: Codex Sol (Matrixprüfungen, Erstprüfung, Wiederprüfung 1) und seit 05.09.2026 Codex Astra max (Wiederprüfungen 2 und 3); Reste als Härtungen NAK-191 und NAK-192. **G4 bleibt NEEDS_WORK**, bis NAK-181 (Vergleichsevidenz-Semantik) und NAK-182 (Korpus und Goldens) abgenommen sind; Reihenfolge nach §11.
