SONDE-013 Etappe 2 Nacharbeit 1 - Rotbeweis B04
============================================================

Datei:     eq-copilot/plugin/src/PluginProcessor.cpp
Bein:      EqCopMarkierungTest
Warum:     v3Status() liest das Sticky-Bit des RT-Control-Rings nicht mehr. Ein verlorenes letztes Ereignis erzeugt dann keine Sequenzluecke und keine Meldung - der Broker bleibt scheinbar sauber (M-39).

Eingebauter Fehler (alt -> neu):
--- ALT ---
    s.interventionStateUnknown = interventionsRingUeberlauf.load (std::memory_order_relaxed)
                              || interventionsRing.ueberlaufGesehen();
--- NEU ---
    s.interventionStateUnknown = false;

=== LAUF 1: MIT dem Fehler (erwartet: ROT) ===
  ok      B04: als Main klassifiziert
  ok      B04: frisch ist der Interventionszustand BEKANNT - ein Bit, das immer stuende, sagte nichts
  ok      B04: der Ring nimmt seine Kapazitaet auf  [32]
  FEHLER  B04: ring_overflow_reist_im_heartbeat - der Ueberlauf des RT-Control-Rings erreicht `v3Status()` (M-39)
  FEHLER  B04: und steht im Heartbeat-JSON
  ok      B04: ein sauberer Zustand schreibt das Feld GAR NICHT - ein `false` in jedem Takt waere die Behauptung 'Zustand bekannt'
EXIT=1

=== LAUF 2: OHNE den Fehler (erwartet: GRUEN) ===
  ok      B04: als Main klassifiziert
  ok      B04: frisch ist der Interventionszustand BEKANNT - ein Bit, das immer stuende, sagte nichts
  ok      B04: der Ring nimmt seine Kapazitaet auf  [32]
  ok      B04: ring_overflow_reist_im_heartbeat - der Ueberlauf des RT-Control-Rings erreicht `v3Status()` (M-39)
  ok      B04: und steht im Heartbeat-JSON
  ok      B04: ein sauberer Zustand schreibt das Feld GAR NICHT - ein `false` in jedem Takt waere die Behauptung 'Zustand bekannt'
EXIT=0

URTEIL: BEWIESEN - der Test faellt am eingebauten Fehler und steht ohne ihn.
