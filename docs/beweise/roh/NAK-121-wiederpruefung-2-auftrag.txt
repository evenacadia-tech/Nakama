Wiederpruefung S19b / Ticket NAK-121, Runde 2 — gebundener Pruefauftrag des Dirigenten (2026-09-03), Variante B der Vorlage tools/dirigent/pruefauftrag-vorlage.md.

Du bist der frische, lesende Pruefer dieser Runde. Pruefbereich ist ausschliesslich der Fixdiff der Runde:
`git diff ae58c745d677bbf59d661a0a543d08af2d1dc329...28068a43b4f05ed939c6b12ef526dd4949c9f58f -- broker/ docs/beweise/NAK-121.md docs/beweise/roh/`
(ZIEL = HEAD = 28068a43b4f05ed939c6b12ef526dd4949c9f58f, vor und nach deinem Lauf identisch). Der uebrige Ticketbereich wurde bereits geprueft (Abschlusspruefung 1, Thread 01a06727-def6-7ab3-835a-4db9bba4d951; Wiederpruefung 1, Thread 01a0683f-4fde-7352-8956-e1fcd37c7bbf, Rohurteil docs/beweise/roh/NAK-121-wiederpruefung-1-956e86e.txt) und ist hier nicht Pruefgegenstand. Referenz als Kontext, nicht als Pruefgegenstand: docs/beweise/NAK-121.md Paragraph 4 (Matrix H-01 bis H-22), Paragraph 13 (Nacharbeit Runde 1), Paragraph 14 (Nacharbeit Runde 2: je Defekt Regel, Commit, Test, Rotbeweis), docs/beweise/roh/NAK-121-nacharbeit-2-auftrag.txt (der Auftrag mit den Regeln), SONDE-011 B.2 (C-06, C-07, C-09, C-10).

BEFUNDE, die diese Runde schliessen sollte (woertlich aus dem vorigen Urteil), mit der Regel des Dirigenten (03.09.2026):

R2-1 = D4 [P2] G2-FLOATEDGE-001, broker/src/coordinator/sicht.rs:165 — „Nach einem einzigen subnormalen Frame ruft jede weitere `messsicht`-Abfrage erneut `fenster_ms_bilden` auf und erhoeht den Zaehler. Im neuen Test werden 0, negativ, inf und NaN am Eingang abgewiesen; die vier anschliessenden Abfragen zaehlen lediglich denselben alten subnormalen Frame erneut. Damit misst der Zaehler Lesefrequenz statt die geforderten Grenzfaelle und verletzt die NaN-Zaehlzusage aus CLAUDE.md; ein zweiter Aufruf ohne neuen Frame reproduziert die Fehlzaehlung." Regel: Gezaehlt wird das Ereignis, nicht das Lesen — Fensterbildung genau einmal je angenommenem Frame bei der Aufnahme in den Stand (senke.rs, `messframes.insert`), Ergebnis mit dem Frame gespeichert, `messsicht` liest nur; Test: ein subnormaler Frame → 1, fuenf Lesungen ohne neuen Frame → bleibt 1, zweiter subnormaler Frame → 2, normaler Frame → unveraendert; Mutante (Fensterbildung zurueck in die Sicht) rot.

R2-2 = D6 [P2] H-14, broker/src/coordinator/liveness.rs (Codex nannte link.rs:229-231 als Vergleichsstelle) — „Startet der Coordinator mit 1023 oder 1024 restaurierten persistenten Guards und leerem Aliasregister, werden alter und neuer Nonce-Link zunaechst angenommen; die Kollision entsteht erst beim erneuten Report des verdraengten alten Links in `heartbeat_kontakt`, wo zwei Guards ohne Deckelpruefung eingetragen werden. Dieser Check laeuft nur bei einer Kollision waehrend des Hello, sodass der Store erst nach dem Welcome scheitert und lediglich Routing abschaltet; D6/H-14 sowie der geforderte Test am unabhaengigen persistenten Deckel bleiben offen." Regel: H-14 gilt fuer jeden Kollisionspfad; die Deckelpruefung (Aliasreservierung `len + 2 > MAX_QUARANTAENE` UND persistenter Index `guard_anzahl() + neue_riegel > MAX_KONFLIKT_GUARDS`) wird EINE crate-interne Funktion fuer Hello- und Reportpfad; der Reportpfad wertet das Ergebnis von `registriere_wire_zuordnung` aus; am Deckel kein Eintrag in die Riegeltabelle, kein Store-Schreibauftrag, `cap_abweisungen` erhoeht, meldender Link bleibt fail-closed getrennt; unter dem Deckel unveraendert. Test mit 1023 restaurierten Riegeln (Deckel) und 1022 (Gegenprobe: beide Riegel eingetragen und persistiert); Mutante ohne Deckelpruefung im Reportpfad rot. Der Worker benennt in Paragraph 14.1 eine Grenze: die Quarantaene der Kollision selbst bleibt auch am Riegeldeckel bestehen (sie hat ihren eigenen Deckel MAX_QUARANTAENE), damit der Konflikt nicht gewaschen wird — pruefe, ob das mit H-14 und C-07 vereinbar ist.

R2-3 = D10 [P2] G2-TOCTOU-002, broker/src/store/migration.rs:205-206 — „`Connection::open_with_flags` oeffnet zuerst die eigentliche Datenbank, anschliessend oeffnet `geoeffnete_db_volume(&db_pfad)` denselben Namen ueber ein zweites `CreateFileW`. Wird der Pfad oder eine Junction zwischen diesen Opens umgehaengt, haelt SQLite Objekt A, waehrend die Volumenentscheidung Objekt B klassifiziert; der urspruengliche TOCTOU bleibt bestehen. Test und Rotbeweis pruefen ebenfalls nur den separaten Helferhandle und belegen daher keine Entscheidung am Handle der SQLite-Datenbank." Regel: Die Entscheidung faellt auf dem Handle, das SQLite selbst haelt (`rusqlite::ffi::sqlite3_file_control` mit `SQLITE_FCNTL_WIN32_GET_HANDLE`, Handle gehoert SQLite und wird nicht geschlossen); Klassifikation als Funktion ueber ein HANDLE; `store_vorbereiten` weist auf dem SQLite-Handle ab, bevor `pragmas_setzen` laeuft; Test zieht Name und Objekt ueber eine Verzeichnis-Junction auseinander und misst, dass die Entscheidung dem SQLite-Objekt folgt; Mutante (zweites CreateFileW nach Namen) rot.

R2-4 = NEU [P2] Lebenszyklus, broker/src/transport/server_v3/listener.rs:384-385 — „Sobald ein Writer abgeloest wurde, legt der Wachhund einen Eintrag in `abgeloest_abbrueche` an; `Verbindungsgriff::drop` entfernt spaeter nur `offen` und `abgeloest`, nie diesen neuen HashMap-Eintrag. Bei wiederholten H-02-Abloesungen waechst die Map ueber die gesamte Broker-Laufzeit unbegrenzt, obwohl nur 96 Verbindungen gleichzeitig erlaubt sind; damit bleibt der gekoppelte Verbinden/Trennen-Lebenszyklus aus AGENTS.md:48-50 unvollstaendig." Regel: Der Destruktor entfernt `abgeloest_abbrueche[id]` im selben gesperrten Block wie `offen` und `abgeloest`; die D14-Naht bleibt wirksam (der abgeloeste Schreiber liest den Zaehler, solange er seinen geteilten Griff haelt; der letzte Griff faellt nach dem Threadende); Test misst nach dem H-02-Lauf alle drei Mengen leer; Mutante ohne Abraeumen rot.

R2-5 = D11 [P2] H-01, broker/src/transport/server_v3/win_handles.rs:671 — „Der neue Test bezeichnet diesen Block als Serverstopp, ruft aber `alle_io_abbrechen` direkt auf; auch die Mutante aendert nur den gemeinsamen Zaehlhelfer. Entfernt oder entkoppelt man den Aufruf aus `V3Griff::stoppen`, bleiben dieser Test und `NAK-121-rot-D11-r1.txt` gruen, obwohl der geforderte Stopp-Pfad wieder ungeprueft ist; D11 verlangt ausdruecklich einen Rotbeweis ueber diesen Pfad." Regel: Der Beweis geht durch `V3Griff::stoppen`: Server mit totem Handle NUR in `offen` (Naht-Variante, nicht in `abgeloest`, sonst zaehlt der Wachhund), Zaehler vor dem Stopp 0 auch nach mindestens einem Wachhundtick, nach `stoppen()` >= 1; Rotbeweis: Stopp-Pfad reicht die Statistik nicht durch → rot.

R2-6 = D15 [P2] H-07-Test, broker/tests/security_vectors.rs:1058 — „Im lesenden H-07-Lauf ist `frame_lesen` vollstaendig abgeschlossen, bevor `drop(client)` den Verbindungsabbau ausloest; beim Flush ist somit kein Broker-Write mehr ausstehend. Diese Haelfte bleibt ohne `abfliessen_mit_frist` gruen, waehrend der vorhandene Rotbeweis ausschliesslich wegen des Timeoutzaehlers im nicht lesenden Lauf scheitert; die geforderte Aussage, dass ein ausstehender Write den lesenden Peer vor dem Close erreicht, bleibt unbelegt." Regel: In Haelfte (a) liegt das Welcome beim Beginn des Abbaus noch ungelesen im Puffer und der Abbau beginnt brokerseitig (`griff.stoppen()`); der Peer liest erst nach dem Marker `register_austrag`; Welcome kommt vollstaendig an, danach EOF, `flush_abgelaufen == 0`, Reihenfolge `register_austrag` → `flush` → `close`; kein Sleep als Synchronisation; Mutante (`abfliessen_mit_frist` sofort true) rot.

AUSDRUECKLICH NICHT GEGENSTAND (Einordnung des Dirigenten, 03.09.2026, an der Quelle):
- P1 „Halte die neuen Pruef-APIs aus der oeffentlichen Oberflaeche" (coordinator/mod.rs:121, `CoordinatorFlushTestHaken::panik_unter_standlock_scharf`, `Registrierung::DeckelErreicht`, `store::geoeffnete_db_volume`, `store::pfad_aus_utf16`, `Coordinator::standlock_vergiftet`/`fenster_nicht_endlich`, Konstanten) ist HAERTUNG, kein Defekt: der Gate-Satz verbietet eine „oeffentliche Signatur, die ein anderer Verbraucher liest"; die Bibliothek `eqcop_broker` hat keinen anderen Verbraucher als ihre eigenen Binaries und Tests (kein zweites Cargo.toml im Repo referenziert sie), Paragraph 5.8 des Manifests laesst Beobachtungs-APIs zu H-Zeilen ausdruecklich zu, Paragraph 12.4b und die Abschlusspruefung 1 haben dasselbe Muster angenommen, H-17 haelt fest, dass ein `cfg(test)`-Zaun den Bau der Integrationstests braeche. Der Feature-Zaun geht datiert ins Register. Dasselbe gilt fuer die in Paragraph 14.2 benannten drei neuen `pub`-Namen dieser Runde (`store::volume_am_sqlite_handle`, `V3Griff::registermengen`, `V3TotesHandleNaht`), die demselben Muster folgen — sie sind kein Befund, es sei denn, du belegst einen anderen Verbraucher als Binaries und Tests des Crates.
- H-03/C-06 (coordinator/senke.rs, Re-Validierung nach Lockfreigabe): Haertung, Register.
- Die in Paragraph 12.7 benannten offenen A-Zusagen (H-15 auf B umgestuft, H-13 ohne fristlose Verdraengung, H-06 ohne den einen Zustellpfad): vertagt, kein Befund.

GATE-TEXT, der Kern woertlich (docs/plan/plan.json, Schritt S19b; vollstaendig in docs/beweise/NAK-121.md Paragraph 1): „INHALT, ein einziger verhaltensneutraler Aenderungssatz: (1) MODULAUFTEILUNG der drei gewachsenen Broker-Dateien […] mit erhaltener Historie […]. (2) Die **25 G2-Haertungen aus NAK-124** […] werden im selben Satz eingearbeitet […]. GRENZE: **kein Wire-, State- oder Testvertrag wird beruehrt** — keine Schemas, keine Fixtures, keine Nachrichtenform, keine oeffentliche Signatur, die ein anderer Verbraucher liest. Wer eine Vertragsaenderung braucht, gehoert nicht in diesen Schritt. BEWEIS: der Kanon laeuft **vor und nach** der Aufteilung auf demselben Stand und ist beide Male gruen; die Zahl der Beine ist vorher wie nachher gleich."

Du beantwortest genau zwei Fragen:
1. Ist jeder der sechs Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Datei:Zeile, Test, Rotbeweis unter docs/beweise/roh/NAK-121-rot-R2-*-r2.txt)?
2. Hat der Fixdiff etwas gebrochen — einen bestehenden Test, eine Zeile der Matrix (H-Zeilen, SONDE-011 C-06/C-07/C-09/C-10), einen Satz des Gate-Textes (insbesondere die GRENZE: Schema, Fixture, Nachrichtenform, oeffentliche Signatur, die ein anderer Verbraucher liest), eine Invariante aus CLAUDE.md?

Neue Befunde ausserhalb dieser zwei Fragen erhebst du nicht. Befundklassen: Defekt (verletzt Matrix, Gate-Text, Entwurf oder CLAUDE.md-Invariante), Luecke (Matrix und Entwurf sagen nichts — benennen, nicht als Defekt fuehren), Haertung (wuenschenswert, von keiner Zusage verlangt). Ausschluesse wie in Variante A: docs/** ist keine Befundflaeche (Manifeste sind Kontext, keine Befunde ueber Prosa, Zeilenverweise, Trefferzahlen); tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle; Sabotage-Szenarien gegen Pruefwerkzeuge sind Haertung; same-user-Angriffe ausserhalb Entwurf Paragraph 48.4 sind kein Gate-Bruch; Paragraph 2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lueckenlos") gilt fuer Gate-Audits, nicht fuer diese Wiederpruefung. Du liest und faehrst lesende Kommandos (git, rg); cargo kann in deiner Sandbox nicht schreiben — nenne, was du deshalb nicht messen konntest (der eingecheckte Kanon: GRUEN 40/40 auf b2bf568, Rohausgabe docs/beweise/roh/NAK-121-b2bf568.md; der Codestand von 28068a43 unter broker/ ist identisch mit b2bf568). Du schreibst nichts.

URTEIL — genau eine der drei Formen, als erste Zeile deiner Antwort:
URTEIL: PASS — alle sechs Befunde geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz bricht.
URTEIL: BLOCKED — Pruefbereich nicht lesbar.
Danach: geprueft / nicht geprueft, je ein Absatz.
