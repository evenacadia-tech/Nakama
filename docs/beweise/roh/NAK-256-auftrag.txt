NAK-256 — Cockpit: Gesamtplan dauerhaft im zweiten Tab und Stand-Zeile in der Statuszeile; Modellwerte des Beobachters. Werkzeugticket des Dirigenten (2026-09-11).

ANLASS UND ENTSCHEID. User-Wort 11.09.2026, 17:1x (Dirigentensession): „wäre es sinnvoll für mich wenn dort immer eine übersicht ist wo wir stehen, was als nächstes ansteht. also eine übersicht vom gesamtplan und wo wir sind. dann hab ich das immer vor mir und muss dich nicht fragen". Entscheid 17:3x: „gesamtplan stand dauerhaft im zweiten tab + statuszeile". Register docs/offene-punkte.md NAK-256. Der User ist Projektleiter und Musiker, kein Programmierer: Was er sieht, muss ohne Erklärung lesbar sein (Leitungsnamen wie in cockpit.ps1 -Plan, kein Git-Jargon).

MODELL DES BAUERS: claude-opus (Effort max), --permission-mode dontAsk (Skill §3.2). DU BIST der frische Worker im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = git rev-parse HEAD beim Start; git status --short muss leer sein, sonst nichts anfassen und als letzte Zeile „NAK-256 HALT <grund>" melden. Aufsicht NORMAL. Keine Rückfrage an den User; technische Entscheidungen triffst du selbst und begründest sie im Manifest. Lies zuerst: tools/dirigent/cockpit.ps1 (Parameter, -StatusLine, -Plan, -WatchWorker; ValidateSet Zeilen 28 und 32), tools/dirigent/start-dirigent.ps1 (Terminalprofil, Starter-Ablauf, Marker-Neustart), .claude/settings.json (statusLine-Eintrag), docs/PLAN-STAND.md (Kopf und die Zeile „Als Nächstes"), docs/beweise/NAK-246.md Kopftabelle (Zeile „Etappe" als Beispiel eines lebenden Manifestkopfs), docs/beweise/NAK-230.md (Manifestmuster), tools/dirigent/pruefliste.md, CLAUDE.md „Maschinen-Landminen" (Start-Process-Quoting, Backtick-Umbruch, kein Lösch-Cmdlet im Befehlstext).

WAS DU BAUST (drei Teile, ein Ticket, verhaltensneutral für alles Bestehende):
1. STAND-Zeile in der Statuszeile (cockpit.ps1 -StatusLine): eine zusätzliche Zeile `STAND`, die zeigt: das laufende Ticket mit Leitungsname (aus docs/PLAN-STAND.md „Als Nächstes" bzw. dem Arbeitsanker -CurrentStep, falls gesetzt), die Etappe „n von m" und den laufenden Schritt aus der Kopfzeile „Etappe" des zugehörigen Manifests docs/beweise/<TICKET>.md (die Zeile ist Prosa mit fett markierten Fortschrittssätzen; du liest robust: das letzte fett markierte Segment, oder eine klar definierte Konvention, die du im Manifest-Muster dokumentierst — der Dirigent hält sie dann ein), und die nächsten zwei Schritte aus dem Plan (die zwei ersten offenen Schritte nach dem laufenden in docs/plan/plan.json, Leitungsname). Bei fehlender Quelle steht ehrlich „nicht lesbar", nie eine Erfindung (CLAUDE.md: keine toten Elemente, ehrlicher Zustand). Das Manifest des laufenden Tickets ergibt sich aus dem Ticketnamen in „Als Nächstes"; läuft ein Registerpunkt (wie NAK-246) statt eines Planschritts, kommt die Quelle aus einem kleinen, vom Dirigenten gesetzten Anker: lege dafür in cockpit.ps1 einen Parameter `-Ticket <NAK-nnn>` für -StatusLine/-Plan vor oder einen Eintrag im vorhandenen Telemetrie-Cache unter $env:TEMP (nakama-dirigent-<session-id>-*.json), wie es heute -CurrentStep tut — du entscheidest an der Quelle, was der Cache schon trägt, und dokumentierst, wie der Dirigent den Anker setzt (ein Kommando).
2. Gesamtplan dauerhaft im zweiten Tab: ein neues Skript tools/dirigent/plan-tab.ps1, das in einer Schleife (Takt 30 s, Parameter -TaktSekunden, -Einmal für einen einzigen Durchlauf) den Bildschirm neu zeichnet: oben die STAND-Zeile wie in Teil 1, darunter die Ausgabe von cockpit.ps1 -Plan (Gesamtplan mit ✓/○ und Phasen), unten Uhrzeit des letzten Zeichnens und Kontingent/Kontext aus dem Telemetrie-Cache, falls vorhanden. Der Starter tools/dirigent/start-dirigent.ps1 öffnet diesen Tab bei jedem Dirigentenstart automatisch im selben Windows-Terminal-Fenster (Windows Terminal: `wt -w 0 new-tab --title "Nakama · Plan" pwsh -NoProfile -File tools/dirigent/plan-tab.ps1`; Landmine: Argumente mit Leerzeichen selbst quotieren; ohne Windows Terminal ein normales zweites Fenster) — genau ein Plan-Tab, kein zweiter bei Neustart über die Markerdatei (prüfe, ob schon einer läuft, z. B. über einen Fenstertitel oder eine PID-Datei unter $env:TEMP; nichts im Repo). Der Tab ist rein lesend und schreibt keine Projektdatei.
3. Beobachter-Härtung: -WatchWorker liest -StartModel und -StartEffort ohne ValidateSet (freier String) oder aus claude agents --json, falls das Modell dort steht; Anzeige bleibt wie heute.

BEINE (pwsh, einzeln, Rohausgabe nach docs/beweise/roh/NAK-256-beine.txt): (a) Statuszeile-Probe: cockpit.ps1 -StatusLine mit einem Beispiel-JSON auf stdin (so wie Claude Code es liefert; ein Beispiel liegt vielleicht schon in tools/dirigent/ oder in docs/beweise/roh/NAK-2xx-*; sonst baue eines aus der Doku code.claude.com/docs/en/statusline) — die Ausgabe zeigt die STAND-Zeile mit echten Werten (NAK-246, Etappe 4 von 6, „Wiederprüfung 1 steht aus", nächste Schritte); (b) cockpit.ps1 -Plan vor und nach dem Umbau bytegleich (die bestehende Ansicht bleibt unverändert); (c) plan-tab.ps1 -Einmal liefert Exit 0 und die Ausgabe (Rohdatei); (d) start-dirigent.ps1 nicht scharf starten (er startet Claude); belege den Tab-Start mit einem Trockenlauf-Parameter (-NurPlanTab o. ä.), der nur den Tab öffnet, und einem zweiten Aufruf, der keinen zweiten Tab öffnet; (e) -WatchWorker -StartModel Fable -StartEffort xhigh startet ohne Validierungsfehler (gegen eine nicht existierende Worker-ID: die erste Meldung genügt, dann beenden). Rotbeweis je Zusage: die STAND-Zeile fehlt bzw. sagt „nicht lesbar" am Basis-SHA; der ValidateSet-Fehler am Basis-SHA (bereits als Monitor-Ereignis vom 11.09. belegt, in NAK-246 §7.8).

MANIFEST docs/beweise/NAK-256.md (neu, Muster docs/beweise/NAK-230.md): Kopftabelle (Ticket, Klasse Werkzeug, Urteil offen, Prüfstufe T1 mit Selbstaudit, Basis-SHA, Ticketpfade), §1 User-Wortlaut (oben, wörtlich), §2 Was gebaut wurde (je Teil Datei:Zeile; die Konvention, wie die STAND-Zeile den Manifestkopf liest; das Kommando, mit dem der Dirigent den Ticketanker setzt), §3 Beine mit Rohausgabe, §4 Prüfliste abgehakt, §5 Abweichungen, §6 Selbstaudit (Quoting-Landminen, Verhalten ohne Windows Terminal, kein zweiter Tab beim Marker-Neustart, keine Projektdatei durch den Tab). Dazu ein Absatz in .claude/skills/dirigent/SKILL.md §0 (Start und Cockpit): drei bis fünf Zeilen, wie der Plan-Tab entsteht und wie der Dirigent den Ticketanker setzt — nicht mehr, der Skill ist über der Byte-Grenze (NAK-255).

TICKETGRENZE: tools/dirigent/cockpit.ps1, tools/dirigent/start-dirigent.ps1, tools/dirigent/plan-tab.ps1 (neu), .claude/skills/dirigent/SKILL.md (nur der eine Absatz in §0), docs/beweise/NAK-256.md, docs/beweise/roh/NAK-256-*. NICHT ANFASSEN: .claude/settings.json (kann nur der User), docs/plan/**, docs/PLAN-STAND.md, docs/offene-punkte.md, tools/plan/**, alles unter eq-copilot/ und broker/, andere Manifeste.

GIT. Nie git add -A, nie --amend, fremde uncommittete Dateien nie anfassen; git status --short zuerst lesen. Commits nur mit explizitem Pathspec; Commitnachrichten über git commit -F <datei>; Push nach git pull --rebase bei sauberem Worktree als eigener Schritt. Vor dem Commit: py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-256.md .claude/skills/dirigent/SKILL.md ohne Befund. Kleine Werkzeugaufrufe. Selbstaudit vor dem Commit.

ENDE: Letzte Zeile deines Turns „NAK-256 ABGESCHLOSSEN <voller sha>". Keine Rückfrage an den User.
