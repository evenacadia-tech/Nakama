Wiederprüfung 1 NAK-214 / G5-Nacharbeit 3 (Ticket NAK-214, Etappe 2), Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 8c5c6b017d14b5475f8f17b7bc4b9ab74b01068f...8261bc179d5a8b98ba898705e2fc406e00430922 -- broker/tests/sonde014_verdrahtung.rs broker/src/coordinator/befehl.rs`
(ZIEL = 8261bc179d5a8b98ba898705e2fc406e00430922 = HEAD, vor und nach deinem Lauf identisch; VORHER = 8c5c6b01, der Stand vor der Runde — er enthält gegenüber dem geprüften Stand 184fe645 nur drei Rohdateien unter docs/beweise/roh/, keinen Code). Der übrige Ticketbereich wurde in der Erstprüfung (Thread 01a081dd-842a-7f93-8c48-194b95d95887, docs/beweise/roh/NAK-214-erstpruefung-184fe64.txt) bereits geprüft und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); kein cargo, kein cmake, kein Python-Interpreter; du schreibst nichts. Enthält der Fixdiff außer der Testdatei weitere Dateien, prüfe sie nur darauf, ob die Änderung zur Schließung der zwei Befunde nötig war und nichts anderes ändert.

BEFUNDE, die diese Runde schließen sollte — wörtlich aus dem Urteil der Erstprüfung, mit der Regel, die der Dirigent je Befund entschieden hat (docs/beweise/roh/NAK-214-nacharbeit-1-auftrag.txt):
1. DEFEKT V-32: „Die Bühne liefert frisch berechnete Befunde über `zustand_aus_sicherheit(..., false)`; dieser Filter erfasst daher nur `MoreData`, niemals `Stale`. Damit bleibt der vorgeschriebene Kontrollfall V-32 ungemessen: Ein versehentliches Zurückweisen ausschließlich veralteter Befunde würde nicht auffallen. Der Zustand ist produktiv erreichbar: Befund F erzeugen → wirksames `intent_update` ohne Folge-Evidenz → `Stale` ausdrücklich prüfen → `user_verdict` mit neuer `command_id` senden und Annahme samt Persistenz prüfen." Regel: eigener Teilfall oder Test über den produktiven Pfad — Befund F wird über den bestehenden Weg veraltet (Muster `rollenaenderung_macht_den_vorschlag_stale`), der Test prüft ausdrücklich `zustand == Befundzustand::Stale`, dann `user_verdict` mit neuer `command_id` → `angewandt`, persistiertes Domänenereignis, `state_hash`; Rotbeweis `NAK-214-rot-V-32.txt` (Zustandsbedingung eingezogen → rot); Manifest V-32 und §6-Präzisierung auf die Messung umgeschrieben.
2. DEFEKT V-34: „Dieser Negativtest setzt ausdrücklich vorhandene Befunde voraus und prüft anschließend nur eine darin unbekannte ID. Der in V-34 verlangte zweite Teilfall mit fehlendem Sitzungseintrag fehlt. V-35 ersetzt ihn nicht […]. Die fehlende Eingabefolge lautet: Main anmelden und State melden, ohne Befunde zu erzeugen → neue `command_id` mit wohlgeformter unbekannter `finding_id` senden → `abgelehnt/unknown_target` und keinerlei Persistenz prüfen." Regel: zweiter Teilfall über den produktiven Pfad — Main angemeldet, State gemeldet, `stand.befunde` ohne Eintrag für die Sitzung (im Test geprüft), dann `user_verdict` mit neuer `command_id` und wohlgeformter unbekannter `finding_id` → `abgelehnt`/`unknown_target`, `store.command_event_lesen(command_id)` `None`, kein `user_verdict`-Domänenereignis, kein `internal_p0_command`; Rotbeweis `NAK-214-rot-V-34.txt`; Manifest V-34 und §6 Zeile „derselbe Zweig" auf die Messung umgeschrieben.

GATE-TEXT, wörtlich (docs/bauaufteilung-sonden.md:395-400): „**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — „Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer." P5-Exit-Gate Satz 1 (Entwurf §59, Zeilen 4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs". Regel R4 (docs/beweise/G5.md §11 Nr. 3): „`user_verdict` mit einer `finding_id`, die im Broker nicht existiert, wird abgewiesen (`reject`), nicht persistiert." Entscheid E8 (docs/beweise/NAK-214.md §2.0): Riegel hinter dem Idempotenzcheck; der Zustand des Befunds spielt keine Rolle. Matrixzeilen V-30 bis V-36 (§2.4).

Du beantwortest genau zwei Fragen:
1. Ist jeder der zwei Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Testname, die Zeilen, die `Stale` bzw. den fehlenden Sitzungseintrag herstellen und prüfen, die Zusicherungen zu Antwort und Persistenz; Datei:Zeile)? Kommt `Stale` über den produktiven Weg zustande (kein direktes Setzen des Feldes, keine Testhilfe im Produktcode)? Prüft V-34 wirklich den fehlenden Sitzungseintrag (`stand.befunde.get(&session)` ist `None`) und nicht eine leere Liste? Sind die Rotbeweise als Dateien vorhanden und beschreiben sie eine Mutation, die den jeweiligen Test rot macht?
2. Hat der Fixdiff etwas gebrochen — einen bestehenden Test derselben Datei (Kollision von `command_id`s, geteilte Bühne, Store-Ordner), eine Matrixzeile V-30 bis V-36, E8, R4, einen Satz des Gate-Textes oder eine Invariante aus CLAUDE.md? Wurde Produktcode geändert, und wenn ja: war das für die Schließung nötig und im Manifest als Ursache benannt?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in der Erstprüfung (DEFEKT / LÜCKE / HÄRTUNG); Ausschlüsse wie dort: docs/**, CLAUDE.md und .claude/** sind kein Prüfgegenstand (Manifest nur Kontext); Register NAK-191 bis NAK-211 und NAK-215 bis NAK-227; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; tools/dirigent/pruefliste.md ist keine Anforderungsquelle; Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — beide Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz von Gate, Matrix oder Regel bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
