NAK-225 — S25d: Plugin-Kern verhaltensneutral aufteilen (`eq-copilot/plugin/src/PluginProcessor.cpp`, `core/analysis/FeatureEngine.h`, `core/ipc/ControlClient.cpp`). Ticketauftrag des Dirigenten (2026-09-09, Pflegeschritt 2 nach G5; Muster S25c/NAK-224 und S19b/NAK-121).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-225.md ein (Manifest neu anlegen; Muster ist docs/beweise/NAK-224.md: Kopftabelle mit Ticket, Phase, Prüfstufe, Basis-SHA, Kanon vorher, Kanon nachher, Testbestand, Ticketpfade, Nicht angefasst, Änderungssatz, Prüfmodell — darunter §1 Ticket- und Registerbezug wörtlich, §2 Schnittplan, §3 Riegelkarte, §4 Prüfliste, §5 Nebenbefunde, §6 Verlauf; ein lebender Kopf, darunter append-only). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (neue Dateien vorher `git add -- <pfad>`); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie Backticks in `-m`. Keine Neben-Refactors, keine Legacy-Umbenennung (NAK-30: `EqCop*`, `Eqcp`, `EqCopilotProcessor` bleiben), kein neues Verhalten, keine neue Optik. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §4 nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits, Manifest und Dateiköpfe Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder dem rekursiven Unix-Löschbefehl (Ask-Regel, der Auto-Modus hält an); selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. `cmake` liegt NICHT im Bash-PATH dieses Rechners: C++-Bauten nur aus pwsh, Baubefehl, Konfiguration und Zielnamen aus tools/beweise.ps1 ableiten (die Funktion, die die Ziele baut), nie raten; nach jedem Bau den Zeitstempel des Binaries gegen den Fix prüfen (CLAUDE.md-Landmine). Keine GPU-Batch-Render-Schleifen auf der Arc A770: EqCopShot nur über den Runner-Aufruf oder einzeln. Beende deinen Turn nicht, solange ein von dir gestarteter Lauf (Kanon) läuft; die letzte Logzeile `EXIT=` ist das Ende.

TICKET, wörtlich (docs/plan/plan.json, Schritt S25d, Ticket NAK-225, Stufe T2; für §1 des Manifests direkt aus plan.json kopieren, nicht aus diesem Auftrag): „ANGELEGT 08.09.2026 (Pflegeschritt 2 nach G5, Register NAK-225, vor S26–28, damit der EQ-Bau auf geteilten Dateien beginnt). Gemessen am 08.09.2026: `eq-copilot/plugin/src/PluginProcessor.cpp` 4 161 Zeilen, `core/analysis/FeatureEngine.h` 3 969, `core/ipc/ControlClient.cpp` 2 307, `src/SourcesModel.cpp` 1 797. INHALT: je Datei nach Fachbereich teilen (Prozessor: Hostbrücke, State, IPC-Anbindung, Analyse-Anstoß; FeatureEngine: Messkern je Merkmalsfamilie; ControlClient: Verbindung, Nachrichten, Fehlercodes), Identität, Audio-Thread und State-Pfade unberührt. BEWEIS: Nulltest bitidentisch, Passthrough sampleidentisch, Kanon vorher = nachher grün, `EqCopShot`-Sichtprüfung unverändert, Beweismanifest mit den Beinzahlen. GRENZE: keine Legacy-Umbenennung (NAK-30), keine neue Optik, keine Funktionsänderung.“
REGISTERZEILEN (docs/offene-punkte.md, wörtlich dort nachlesen und in §1 zitieren): NAK-225 (der Schritt selbst). NICHT Teil dieses Tickets — Entscheid des Dirigenten (Technik), im Manifest so zu nennen: `src/SourcesModel.cpp` (1 864), `src/PluginEditor.cpp` (1 665), `src/PluginProcessor.h` (1 524) liegen unter der Grenze 2 000 und bleiben in NAK-238 für einen späteren Pflegeschritt; NAK-235 (keine lange Funktion wird zerlegt: `processBlock` 394, `interventionenSenden` 341, `workerLauf` 251, `schreibeSnapshotDatei` 215, `ControlClient::Laufzeit::eineVerbindung` 580, `verarbeiteSamples` 226 bleiben je EINE Funktion, keine wird länger); NAK-236 außerhalb der drei Dateien; NAK-237.

ISTSTAND am Basis-SHA (Dirigent, 09.09.2026, `py -3.13 tools/plan/gesundheit.py`; du misst ihn selbst erneut und trägst die Tabelle in §2 ein): `PluginProcessor.cpp` 4 389 Zeilen (44 Memberdefinitionen von `EqCopilotProcessor`, drei anonyme Namensräume mit Helfern: Zeilen 21–79, 1846–1857, 2946–2984; alles in `namespace eqcop`), `FeatureEngine.h` 4 390 Zeilen (Header-only: Zeit-, Event- und Bandvertrag mit `Transportstempel`, `Ereignis`, `Fingerprint`, `Bandsatz`, `FeatureFrame` und Konstanten bis Zeile 921; `class FeatureEngine` 922–4390 mit `public:` 924–1591 und `private:` ab 1592, darin die verschachtelten `Stufe` 1595, `Support` 1676, `Akku`/`Breite` 4186, `StereoAkku` 4209, `Passagenfenster` 4293), `ControlClient.cpp` 2 475 Zeilen (anonymer Namensraum 19–399, Pimpl `Laufzeit`, Memberdefinitionen ab 876; `namespace nakama::ipc`). Einbindung: `src/PluginProcessor.cpp` steht 14-mal wörtlich in `eq-copilot/plugin/CMakeLists.txt` (Hauptziel und 13 Testziele), `core/ipc/ControlClient.cpp` einmal in `NAKAMA_KERN_QUELLEN` (Zeile 174), `FeatureEngine.h` wird von 16 Dateien direkt eingebunden. Kanon vorher: GRÜN 62/62 auf `41f4984` (Abschlusskanon NAK-224, docs/beweise/roh/NAK-224-41f4984.md); `git diff --stat 41f4984..HEAD -- eq-copilot broker tools` ist leer — das in der Kopftabelle als „Kanon vorher“ mit genau dieser Messung belegen. Ratschen in gesundheit.py: Funktionen über 200 Zeilen 28, Kommentar-Bezeichner 32, clippy 91; Pflegeticket-Liste dort nennt genau die drei Dateien dieses Tickets.

SCHNITT — Technik, entschieden vom Dirigenten; die Feinheit leitest du aus dem Code ab und schreibst sie VOR dem ersten Schnitt als Schnittplan in §2 (Tabelle: jedes Item alt — Funktion, Helfer, Konstante, verschachtelter Typ — mit Zieldatei; die Regel, nach der du geschnitten hast; jede Sichtbarkeits- oder Bindungsänderung einzeln):
(1) `PluginProcessor.cpp` bleibt als Wurzeldatei bestehen (kein `git mv`; sie behält Konstruktor, Destruktor, `processBlock` und alles, was der Audio-Thread ruft, damit die Historie des Audio-Pfads auf der Datei bleibt). Je Fachbereich aus dem Plan — Hostbrücke, State (Save/Load, Dirty, Migration), IPC-Anbindung (Broker, Control, Interventionen), Analyse-Anstoß (Worker, Snapshots, Assistent) — wird in eigenen Commits eine Übersetzungseinheit `src/prozessor/<Fachbereich>.cpp` AUS der Wurzel herausgelöst; die Reihenfolge der Commits belegt die Herkunft. Helfer der anonymen Namensräume, die mehrere Teile brauchen, gehen GENAU EINMAL in einen internen Kopf `src/prozessor/Intern.h` (`inline`, eigener Namensraum `eqcop::prozessor_intern` oder als private statische Member — du entscheidest im Schnittplan); nichts wird dupliziert, keine ODR-Verletzung, kein Helfer wird breiter sichtbar als nötig. `PluginProcessor.h` bleibt unangetastet bis auf das, was der Compiler für die Aufteilung zwingend braucht (jede Zeile im Schnittplan begründet). CMake: die 14 wörtlichen Einträge `src/PluginProcessor.cpp` werden durch EINE Quellliste ersetzt (`set(NAKAMA_PROZESSOR_QUELLEN …)` neben `NAKAMA_KERN_QUELLEN`), im ersten Commit des Tickets, damit jeder spätere Teil an genau einer Stelle hinzukommt; jedes Ziel, das vorher die Wurzel kompilierte, kompiliert danach dieselbe Menge Teile. Die Zielmenge (`cmake --build … --target help` oder die Zielliste aus dem Runner) ist vorher wie nachher gleich.
(2) `FeatureEngine.h` bleibt der einzige Kopf, den die 16 Einbinder kennen: `#include "…/FeatureEngine.h"` ändert sich in keiner Datei (0 Zeilen in `tests/`, `sonde/`, `src/`). Die Klassendeklaration `class FeatureEngine` bleibt in `FeatureEngine.h`; die Definitionen ihrer Memberfunktionen werden je Merkmalsfamilie (etwa Zeit- und Transportvertrag, Spektrum/Bänder, Lautheit/Pegel, Stereo, Fingerprint/Passage, Verteilung/Konvergenz — du leitest die Familien aus dem Code ab) als `inline`-Definitionen außerhalb der Klasse in Teile `core/analysis/featureengine/<Familie>.h` verschoben, die `FeatureEngine.h` am Ende einbindet (Muster Header-only-Bibliothek: Deklaration oben, Definitionen in Teilköpfen; `#pragma once` überall; die Teilköpfe sind nur über `FeatureEngine.h` einbindbar und sagen das im Kopf). Die Vertragsstrukturen vor Zeile 922 (`Transportstempel`, `Ereignis`, `Fingerprint`, `FeatureFrame`, Konstanten) dürfen in einen eigenen Teilkopf, wenn der Schnittplan das begründet. Funktionskörper textgleich; `noexcept`, `constexpr`, Standardargumente und Reihenfolge der Deklarationen unverändert (Reihenfolge ist bei Header-only-Klassen ABI- und Overload-relevant). Keine Definition wandert in eine `.cpp` — das würde die 16 Einbinder und alle Testziele berühren.
(3) `ControlClient.cpp` bleibt Wurzel (Konstruktor, `start`/`stop`/`reconnect`, `Laufzeit`-Grundgerüst); je Fachbereich aus dem Plan — Verbindung (Aufbau, Hello, Generationen, Abbau), Nachrichten (Senden, Zustellen, Snapshot), Fehlercodes (Abbildung, Zählung) — eine Übersetzungseinheit `core/ipc/controlclient/<Fachbereich>.cpp`, herausgelöst in eigenen Commits; die Helfer des anonymen Namensraums (19–399) wie unter (1) genau einmal in einem internen Kopf. `ControlClient.h` unangetastet außer zwingenden Zeilen (begründet); die Pimpl-Klasse `Laufzeit` darf in einen internen Kopf `core/ipc/controlclient/Laufzeit.h`, wenn die Teile sie brauchen. CMake: die neuen Teile kommen in `NAKAMA_KERN_QUELLEN`.
(4) Dateiköpfe wahr: jede neue Datei trägt einen Kopf, der sagt, was darin steht und welche Regel oder Invariante es bindet (Audio-Thread: keine Sperren, Allokationen, Datei-, Pipe- oder Netzzugriffe, kein Logging — steht bei dem Teil, der `processBlock` und seine Aufrufe trägt); die Wurzelköpfe werden nicht kopiert, sondern verteilt und um den Verweis auf die Teile ergänzt.
(5) Lügende Kommentare in den drei Dateien (A32-Liste der Kommentar-Bezeichner ohne Entsprechung, soweit sie in diesen Dateien liegen) werden wahr gemacht; Fremdnamen und Heuristikfehler bleiben mit Begründung in §5. Kommentare außerhalb der drei Dateien bleiben (NAK-236). Kommentare in ANDEREN Dateien, die auf `PluginProcessor.cpp:<Zeile>` oder `ControlClient.cpp:<Zeile>` zeigen (z. B. `sonde/SondeProcessor.cpp`:354, `SondeProcessor.h`:177, `core/StampedAudioQueue.h`:5, `tests/IpcTestMain.cpp`:1607), bleiben unberührt — sie sind Verlauf und kein Ticketpfad; sie werden in §5 als Nebenbefund gelistet.
(6) `tools/plan/gesundheit.py`: die Pflegeticket-Liste muss nach dem Schnitt wahr sein (die drei NAK-225-Einträge entfallen, die Liste darf leer sein, das Werkzeug muss damit umgehen); die Ratschen werden auf den gemessenen neuen Iststand GESENKT, nie erhöht; der Selbsttest bleibt grün. Erwartung: „Quelldateien über 2000 Zeilen“ = 0, A32 endet mit Exit 0 — dann stimmt die A32-Behauptung in `tools/beweise.ps1` („reisst heute bei … drei … NAK-225“) nicht mehr und wird nach der Prüflisten-Regel „geänderte Zusage, drei Stellen“ (Runnerkopf, Skriptkopf, Manifest) nachgezogen; kein neues Modul über dem Ziel 1 500 Zeilen — liegt eines darüber, steht die Begründung im Schnittplan und der Dirigent entscheidet.

NICHT ANFASSEN: `eq-copilot/identity/` (Identität eingefroren, `EqCopIdentityTest` bleibt grün), `eq-copilot/schemas/`, `eq-copilot/fixtures/`, `eq-copilot/plugin/tests/` (0 Zeilen; eine zwingende `#include`-Pfadänderung wäre je Zeile zu begründen — sie darf nach (2) nicht nötig sein), `eq-copilot/plugin/hostbridge/`, `third_party/patches/`, `broker/`, `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md`, `.claude/`, `design/`. Ticketpfade sind ausschließlich: `eq-copilot/plugin/src/PluginProcessor.cpp` (+ neue `src/prozessor/`), `eq-copilot/plugin/core/analysis/FeatureEngine.h` (+ neue `core/analysis/featureengine/`), `eq-copilot/plugin/core/ipc/ControlClient.cpp` (+ neue `core/ipc/controlclient/`), `eq-copilot/plugin/CMakeLists.txt` (nur Quelllisten), `tools/plan/gesundheit.py`, `tools/beweise.ps1` (nur der Behauptungstext von A32), `docs/beweise/NAK-225.md`, `docs/beweise/roh/NAK-225-*`. Zwingende Zeilen in `PluginProcessor.h` oder `ControlClient.h` sind Pfadänderungen: erlaubt, aber je Zeile im Manifest genannt.

RIEGELKARTE (§3 des Manifests; je Zeile Befehl, Wert VORHER am Basis-SHA und NACHHER am Endstand, aus dieser Session):
R1 Kanon: vorher GRÜN 62/62 auf `41f4984` mit leerem Codediff zum Basis-SHA; nachher voller abgekoppelter Kanon auf dem Endstand, GRÜN mit 62 Beinen. Zusätzlich je Bein die vom Bein gemeldete Prüfungszahl (aus der Rohausgabe) vorher = nachher — als Tabelle in docs/beweise/roh/NAK-225-beinzahlen.txt.
R2 Nulltest/Passthrough: A1 `EqCopNullTest`, A16 `EqCopProbeeqNullTest`, B3b `EqCopHostProbeTest` (0 Allokationen im Audio-Thread) grün vorher und nachher, Ausgabe zitiert.
R3 Sicht: B15 `EqCopShot --sonde012-suite eq-copilot/build/sonde012-shots` — SHA-256 jedes gerenderten Bildes vorher und nachher, Liste in docs/beweise/roh/NAK-225-shots-vorher.txt und -nachher.txt, Differenz leer. Zuerst am Basis-SHA ZWEIMAL rendern: weichen die Hashes schon dort zwischen zwei Läufen ab, ist die Zusage die Übereinstimmung mit einem der beiden Basis-Läufe je Bild (beide Listen aufheben) und die Abweichung steht in §5; ein Bild, das nach dem Schnitt von beiden Basis-Läufen abweicht, ist ein Defekt.
R4 Testbestand: die Menge der CMake-Ziele vorher = nachher (Liste); jedes Testziel baut; keine Prüfung fällt still weg (R1-Prüfungszahlen).
R5 Diff-Riegel: `git diff --stat <Basis>..HEAD` — `eq-copilot/plugin/tests/` 0, `eq-copilot/identity/` 0, `eq-copilot/schemas/` 0, `eq-copilot/fixtures/` 0, `hostbridge/` 0, `third_party/` 0, `broker/` 0; jede Abweichung je Zeile begründet.
R6 Historie: die drei Wurzeldateien behalten Namen und Pfad (`git log --follow` trivial); `git log --oneline <Basis>..HEAD` zeigt je herausgelöstem Teil einen eigenen Commit.
R7 Dateiköpfe wahr: jede neue Datei geprüft gegen ihren Inhalt, Liste in §3.
R8 Audio-Thread: der Text von `processBlock` und aller Funktionen, die es (transitiv, innerhalb des Prozessors) ruft, ist textgleich (Diff je Funktion gegen den Basisstand, Liste in §3); keine neue Indirektion, kein neuer virtueller Aufruf, keine Allokation auf dem Pfad; B3b misst es.
R9 gesundheit.py: Tabelle vorher/nachher; über 2 000 = 0; Pflegeticket-Liste wahr; kein neues Modul über 1 500; Ratschen nur gesenkt; Selbsttest grün; A32-Behauptung im Runner nachgezogen; `--clippy` unberührt (Broker nicht angefasst).
R10 Bau je Commit: jeder Commit baut das Hauptziel `EqCopilot_VST3` und die betroffenen Testziele (mindestens A1, A16, B3b, `EqCopIdentityTest`, `EqCopStateMigrationTest`, `EqCopIpcTest`, `EqCopSonde014BefundTest`, `EqCopLebenslaufTest`, B15) grün; Anzahl der MSVC-Warnungen (`warning C`) im Baulog vorher = nachher, gezählt je Ziel.

ABLAUF: (a) Iststand messen (R1 bis R4, R9 vorher, Shot-Hashes zweimal) und ins Manifest; Manifest mit Schnittplan committen und pushen (erster Commit, nur docs/beweise/NAK-225.md und docs/beweise/roh/NAK-225-*). (b) CMake-Quellliste (1 Commit). (c) `PluginProcessor.cpp`: je Fachbereich ein Commit, danach Bau und Beine aus R10. (d) `FeatureEngine.h`: je Familie ein Commit. (e) `ControlClient.cpp`: je Fachbereich ein Commit. (f) Kommentare (5) in einem Commit. (g) gesundheit.py und A32-Behauptung (6) in einem Commit. (h) Riegelkarte nachher messen, dann der volle Kanon abgekoppelt auf sauberem Baum:
  $log = Join-Path $env:TEMP 'nakama-nak225-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-225.md -Anhaengen -Titel NAK-225 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl
(einfache Anführungszeichen um EXIT= — CLAUDE.md-Landmine vom 08.09.2026). Warten, bis die letzte Logzeile mit `EXIT=` beginnt; höchstens alle 15 Minuten `Get-Content $log -Tail 3`. Ein einmaliges „Voraussetzung fehlt“ an zwei flatc-Beinen direkt nach einem frischen Bau ist bekannt: einmal wiederholen, bevor du einen Befund erhebst. Ergebnis (Kopfzeile, Beinzahl, Rohausgabe docs/beweise/roh/NAK-225-<sha>.md) ins Manifest; Urteilsmarke `<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->` erst nach grünem Kanon mit 62 Beinen; T2 setzt der Dirigent nach der Codex-Prüfung. Rundenbilanz: `py -3.13 tools/dirigent/rundenbilanz.py <Basis>..HEAD` ins Manifest.

SELBSTAUDIT vor jedem Commit (adversarial): kein Verhalten geändert (Diff je Funktion gegen den Ursprungstext — nur Verschiebung, `#include`, Sichtbarkeit, Dateikopf), keine Konstante umbenannt oder umgewertet, keine Deklarationsreihenfolge in `FeatureEngine.h` geändert, kein Helfer dupliziert (ODR), kein `static` in einem Kopf ohne `inline`, keine neue Abhängigkeit zwischen Teilen, die vorher nicht bestand (Include-Zyklen), Audio-Pfad textgleich, Identität und State-Pfade unberührt, kein Test still weggefallen (R1-Prüfzahlen, R4), keine Behauptung im Manifest ohne Lauf aus dieser Session. Beziehungen: Quellliste ↔ Teil ↔ Ziel in einem Änderungssatz; Ratsche ↔ Iststand ↔ Pflegeticket-Liste ↔ A32-Behauptung gemeinsam.

ABSCHLUSS: Manifest vollständig (Kopftabelle mit Basis- und Endstand, Schnittplan, Riegelkarte R1–R10 mit beiden Werten, Prüfliste, Nebenbefunde nummeriert, Kanon-Kopfzeile, Rundenbilanz), alles committet und gepusht, Worktree sauber. Melde dich erst, wenn `git status --short` leer ist und `git rev-parse HEAD` gleich `git rev-parse origin/master` ist.
