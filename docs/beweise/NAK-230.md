# NAK-230 — Fehlercode-Leser genau so streng wie der Vertrag

| Merkmal | Wert |
|---|---|
| Ticket | NAK-230 (Register `docs/offene-punkte.md` Zeile 245), **Defekt · Vertrag · C++-Leser**, eingeordnet **vor S26–28**. Quelle: NB-3 aus `docs/beweise/NAK-214.md` §4 (08.09.2026), Ticketauftrag des Dirigenten 09.09.2026. Prüfstufe **T1** (Erbauer), **T2** setzt der Dirigent nach der Codex-Prüfung. |
| Phase | **In Arbeit 09.09.2026.** §1 und §2 stehen VOR dem Fix; der Verlauf in §6 trägt jede Runde. |
| Prüfstufe | T1 durch den Erbauer (Selbstaudit je Commit, Rotbeweis je neuer Zusage, voller abgekoppelter Kanon am Ende). T2 durch Codex nach dem Dirigenten. |
| Basis-SHA | `819d54d0fe1998bba83ec82f47410d2e6630228d` (= `git rev-parse HEAD` beim Start; `git status --short` leer, HEAD = `origin/master`) |
| Kanon vorher | `f4d497b3` **GRÜN 62/62, Exit 0** (Abschlusskanon NAK-225, Rohausgabe `docs/beweise/roh/NAK-225-f4d497b.md`, Lauf 2026-09-09 20:45). `git diff --stat f4d497b3..819d54d0 -- eq-copilot broker tools` ist **leer** (in dieser Session gemessen, §3 R1) — der grüne Lauf steht auf demselben Prüfstand wie der Basis-SHA. |
| Kanon nachher | *(offen — voller abgekoppelter Kanon auf dem Endstand, GRÜN 62/62 bei gleicher Beinzahl, B10 mit neuer Prüfungszahl)* |
| Ticketpfade | `eq-copilot/plugin/core/ipc/controlclient/Intern.h`, `eq-copilot/plugin/tests/IpcTestMain.cpp`, `docs/beweise/NAK-230.md`, `docs/beweise/roh/NAK-230-*` |
| Nicht angefasst (Grenze) | Schemas (`eq-copilot/schemas/**`), Fixtures, `broker/**`, `eq-copilot/identity/**`, `PluginProcessor*`, `eq-copilot/plugin/tests/V3TestServer.h` (der Vertragsbruch-Fall 3 dort wird WIEDERVERWENDET, nicht geändert), `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md`, `design/`. Keine Legacy-Umbenennung (NAK-30), keine Neben-Refactors. |
| Änderungssatz | Ein Defektfix in zwei Commits: (a) Iststand und Matrix ins Manifest; (b) Tests **und** Fix gemeinsam — Leser (`Intern.h`) und die Prüfzeilen, die seine Zusage tragen (`IpcTestMain.cpp`), gehören in denselben Satz, weil der Bestandstest `nur_schemafestes_command_ack_gibt_inflight_frei` die alte, vertragswidrige Zusage als Fall 3 festschreibt. |
| Prüfmodell | T1 Erbauer; Prüfer Codex `gpt-6-astra` (max, lesend). Abweichung vom Dirigenten-Skill §3.2 (Matrix und Bau in getrennten Läufen) mit Begründung des Ticketauftrags: **sechs Zeilen, Matrix und Bau in einem Lauf, die Erstprüfung prüft beides.** |

---

## 1. Registerzeile und Vertragstext, wörtlich

**Register** (`docs/offene-punkte.md`, Zeile 245), wörtlich:

> **NAK-230** | 09-08, NAK-214-Abschluss (NB-3, `docs/beweise/NAK-214.md` §4;
> Dirigent an der Quelle) | **[Defekt · Vertrag · C++-Leser · vor S26–28]**
> **`fehlercodeHaeltVertrag` kennt nur die zwölf Fehlercodes der Fassung 1.**
> `ControlClient.cpp`:74–81 führt `protocol_mismatch` bis `internal`; die sechs
> Codes der Fassung 2 (`abdeckung_zu_gering`, `schon_terminal`,
> `ohne_lautheitsabgleich`, `ohne_resultatmessung`,
> `blindreihenfolge_widerspruch`, `reihenfolge_nicht_gebunden`) fehlen, und ein
> `command_ack` mit einem davon fällt beim Leser (`:228`), obwohl der Broker ihn
> seit SONDE-013 sendet und der Schemakommentar „ein unbekannter Code wird als
> generischer Fehler behandelt, nie als Erfolg“ verlangt — nicht „die Nachricht
> wird verworfen“. Der Leser ist strenger als der Vertrag. Außerhalb der
> G5-Grenze (Experimentfamilie); nicht mit E7 (`unknown_target`, Fassung 1)
> verwechseln. Wortlaut NB-3

**Vertragstext** (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json`, `$defs/fehlercode`
ab Zeile 814, `description` Zeile 815), der tragende Satz wörtlich:

> Maschinenlesbar (§33.3). Ein neuer Code ist eine Minor-Aenderung; **ein
> unbekannter Code wird als generischer Fehler behandelt, nie als Erfolg.**

Die Enum-Liste darunter (Zeilen 816–835, achtzehn Werte in den Zeilen 817–834):
zwölf der Fassung 1 (`protocol_mismatch`, `unknown_message`, `schema_violation`,
`unauthorized`, `unknown_target`, `revision_conflict`, `capability_missing`,
`record_state_unknown`, `recording_active`, `lease_expired`, `rate_limited`,
`internal`) und sechs der Fassung 2 (`abdeckung_zu_gering`, `schon_terminal`,
`ohne_lautheitsabgleich`, `ohne_resultatmessung`, `blindreihenfolge_widerspruch`,
`reihenfolge_nicht_gebunden`).

**Regel des Dirigenten** (Technik, an der Quelle entschieden, 09.09.2026),
wörtlich:

> Der Leser ist genau so streng wie der Vertrag. (1) Ein `code`, der keine
> Zeichenkette ist, bleibt ein Strukturfehler der Nachricht (wie heute). (2) Ein
> Zeichenketten-`code` macht die Nachricht NIE ungültig: die achtzehn Codes aus
> dem Schema (zwölf der Fassung 1 plus die sechs der Fassung 2) sind bekannt und
> werden wie bisher verarbeitet; jeder andere Zeichenketten-Code wird als
> generischer Fehler geführt — der Text bleibt erhalten (für Anzeige und Log),
> das Ergebnis ist nie `ok`. (3) `fehlercodeHaeltVertrag` verliert damit die
> Rolle des Gültigkeitsriegels; wenn sie bleibt, heißt ihre Antwort „bekannt“,
> nicht „gültig“, und ihr Kommentar sagt das. (4) Die Liste der bekannten Codes
> wird aus GENAU EINER Quelle gepflegt […] Kein Verhalten für Fassung-1-Codes
> ändert sich (E7 `unknown_target` bleibt, wie es ist).

### 1.1 Iststand am Basis-SHA, gemessen

Der Leser liegt seit NAK-225 (09.09.2026) nicht mehr in `ControlClient.cpp`,
sondern in `eq-copilot/plugin/core/ipc/controlclient/Intern.h` (benannter
Namensraum `nakama::ipc::controlclient_intern`). Gemessen am Basis-SHA:

| Ort | Stand |
|---|---|
| `Intern.h`:92–100 | `fehlercodeHaeltVertrag` — zwölf Codes der Fassung 1, keiner der Fassung 2, ohne Verweis auf die Schemazeilen |
| `Intern.h`:246–248 | `if (code != nullptr && (! code->istString ‖ ! fehlercodeHaeltVertrag (code->wert))) return CommandAckArt::keinAck;` — **hier fällt die Nachricht** |
| Aufrufer 1 | `controlclient/Vertrag.cpp`:52 (`commandAckHaeltVertrag`) — liefert `false`, das ACK gilt als kein ACK |
| Aufrufer 2 | `controlclient/Nachrichten.cpp`:274 (`Laufzeit::inFlightAck`) — kehrt bei `keinAck` sofort um; der In-Flight-Eintrag bleibt stehen und läuft in seine Frist |
| Rohtext der Antwort | `controlclient/Verbindung.cpp`:832–841 — geht an `inFlightAck` **vorbei** immer an `beiVersionierterAntwort`/`beiAntwort`. Der `code`-Text erreicht Anzeige und Log also unabhängig vom Leserurteil |
| Gegenstelle | `broker/src/coordinator/befehl.rs`:41–62 baut jedes ACK; **alle** `angewandt`- und `idempotent_wiederholt`-Aufrufe (`:442`, `:612`, `:94`, `:195`, `:458`, `:533`) übergeben `code = None`. Ein Erfolgs-ACK mit `code` sendet der Broker heute nicht |
| Bestandstest | `IpcTestMain.cpp`:4961–5003, Fall 3 `{ 3, 2, 'a', "unbekannter-fehlercode" }` schreibt die **alte** Zusage fest: ein ACK mit `"code":"nicht_im_schema"` darf das In-Flight-Register NICHT freigeben |
| Testserver | `tests/V3TestServer.h`:123–125, `:682–683` — `commandAckVertragsbruch == 3` hängt `,"code":"nicht_im_schema"` an. Bleibt unverändert und trägt nach dem Fix die neue Zusage |
| geteilte Codeliste | **keine.** `git grep protocol_mismatch -- '*.h' '*.cpp'` trifft genau `Intern.h`:94. Weder `WireZahl.h` noch ein Vertragskopf führt die Codes; Regel (4) greift damit in der zweiten Fassung: Liste einmal in `Intern.h`, Riegel im Test gegen die Schema-Enum |

**Schaden, präzise:** kein Datenverlust (der Rohtext läuft am Leser vorbei),
sondern ein Liveness-Fehler — ein `command_ack` mit einem Fassung-2-Code lässt
den Auftrag im In-Flight-Register stehen, bis seine Frist läuft, obwohl der
Broker ihn final beantwortet hat.

---

## 2. Verhaltensmatrix (vor dem Fix geschrieben)

`Nachricht gültig?` = liefert `commandAckArtLesen` etwas anderes als
`CommandAckArt::keinAck`. `Ergebnis` = was der Aufrufer daraus als Ausgang des
Befehls sieht. `Text erhalten?` = erreicht der `code`-Wortlaut unverändert
Anzeige und Log (Weg `Verbindung.cpp`:836–841).

| # | `code` im `command_ack` | Nachricht gültig? | Ergebnis | Text erhalten? | Test (B10 `EqCopIpcTest`) | Rotbeweis |
|---|---|---|---|---|---|---|
| 1 | fehlt | ja (heute wie nachher) | aus `ergebnis`: ok bei angewandt/idempotent, sonst fehler | — (kein Code) | `nak230_ack_ohne_code_bleibt_wie_bisher` | **grün vor dem Fix** — Regressionswache, die Zeile hält heute schon |
| 2 | keine Zeichenkette (z. B. `17`) | **nein** (heute wie nachher) | keins — Strukturfehler der Nachricht | — | `nak230_code_ohne_zeichenkette_bleibt_strukturfehler` | **grün vor dem Fix** — Regressionswache |
| 3 | Fassung-1-Code (`unknown_target`) | ja (heute wie nachher) | aus `ergebnis`, unverändert; E7 bleibt wie er ist | ja | `nak230_fassung_1_code_bleibt_unveraendert` | **grün vor dem Fix** — Regressionswache |
| 4 | Fassung-2-Code (`abdeckung_zu_gering` … `reihenfolge_nicht_gebunden`, alle sechs) | heute **nein** → nachher **ja** | fehler aus `ergebnis` (der Code ist bekannt) | ja | `nak230_fassung_2_codes_sind_dem_leser_bekannt` | **ROT vor dem Fix** (`docs/beweise/roh/NAK-230-rot.txt`) |
| 5 | unbekannte Zeichenkette (`nicht_im_schema`) | heute **nein** → nachher **ja** | **generischer Fehler, nie ok** — auch wenn `ergebnis` einen Erfolg behauptet | ja | `nak230_unbekannter_code_ist_generischer_fehler_nie_erfolg` | **ROT vor dem Fix** (`docs/beweise/roh/NAK-230-rot.txt`) |
| 6 | leere Zeichenkette (`""`) | heute **nein** → nachher **ja** | generischer Fehler, nie ok (wie Zeile 5) | ja | `nak230_leerer_code_ist_generischer_fehler_und_gueltig` | **ROT vor dem Fix** (`docs/beweise/roh/NAK-230-rot.txt`) |

**Zusatzzeilen ohne eigene Matrixzeile**, beide im selben Änderungssatz gemessen:

| Zusage | Test |
|---|---|
| R3-Riegel: die Liste in `Intern.h` deckt die Enum-Liste des Schemas **genau** — in beide Richtungen, gelesen aus `eq-copilot/schemas/v3/eq-ipc-v3.schema.json` im Repo | `nak230_bekannte_fehlercodes_decken_die_schema_enum_genau` |
| Ende zu Ende: derselbe unbekannte Code am echten `ControlClient` gibt den In-Flight-Eintrag **endgültig ohne Erfolg** frei (statt ihn stehen zu lassen) | `nak230_unbekannter_fehlercode_gibt_inflight_endgueltig_ohne_erfolg_frei` |

### 2.1 Warum Zeile 5 auch ein `ergebnis: angewandt` herabstuft

Der Vertrag sagt „nie als Erfolg“, nicht „nie als Erfolgscode“. Ein ACK, das
Erfolg meldet **und** einen Fehlercode trägt, den der Leser nicht versteht, ist
in sich widersprüchlich; die Erfolgsbehandlung würde seinen `state_hash` als
bestätigten Wirkungsschnitt buchen. Der Leser stuft deshalb auf einen
endgültigen Fehler herab: final beendet, kein Erfolg, In-Flight-Eintrag frei.
Das kostet keine echte Nachricht — die einzige Gegenstelle sendet `code` bei
`angewandt`/`idempotent_wiederholt` nachweislich nie (§1.1, Zeile „Gegenstelle“).
Ohne diese Herabstufung hätte die Kenntnisunterscheidung im Produkt keinerlei
Wirkung und wäre totes Beiwerk, das nur ein Test liest.

---

## 3. Riegelkarte

| Riegel | Zusage | Stand |
|---|---|---|
| **R1** | Kanon vorher GRÜN 62/62 auf `f4d497b3`; `git diff --stat f4d497b3..819d54d0 -- eq-copilot broker tools` leer | **erfüllt** — Diff leer gemessen, Rohausgabe `docs/beweise/roh/NAK-225-f4d497b.md` (Urteilszeile „GRUEN - 62/62 … Exitcode: 0“) |
| **R1b** | Kanon nachher: voller abgekoppelter Lauf auf dem Endstand, GRÜN 62/62 (gleiche Beinzahl), B10 mit neuer Prüfungszahl (vorher 381) | *(offen)* |
| **R2** | Matrix: sechs Zeilen, sechs Tests, mindestens zwei Rotbeweise | *(offen)* |
| **R3** | Schema-Riegel: der Test liest `eq-copilot/schemas/v3/eq-ipc-v3.schema.json` aus dem Repo und hält die bekannte Liste gegen die Enum-Liste | *(offen)* |
| **R4** | Diff-Riegel: nur Ticketpfade | *(offen)* |
| **R5** | `EqCopIpcTest` und `EqCopilot_VST3` bauen ohne neue `warning C` | *(offen)* |

---

## 4. Prüfliste (`tools/dirigent/pruefliste.md`)

*(wird vor jedem Commit abgehakt; je Zeile steht hier, wo sie gemessen ist)*

---

## 5. Nebenbefunde

*(nummeriert, nie stumm behoben)*

---

## 6. Verlauf (append-only)

### Runde 0 — Iststand und Matrix (09.09.2026)

Quellen gelesen, nichts geändert: `Intern.h`, `Vertrag.cpp`, `Nachrichten.cpp`,
`Verbindung.cpp`, `ControlClient.h`, `IpcTestMain.cpp`, `V3TestServer.h`,
`eq-ipc-v3.schema.json`, `broker/src/coordinator/befehl.rs`,
`broker/src/coordinator/schema.rs`, `tools/beweise.ps1`. R1 gemessen (Diff gegen
`f4d497b3` leer). §1 und §2 stehen damit vor dem Fix.
