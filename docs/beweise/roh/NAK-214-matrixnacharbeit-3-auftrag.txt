NAK-214 — Matrixnacharbeit 3 (Etappe 1, Runde 3). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (docs/beweise/NAK-214.md); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Kein Produktcode, kein Test, keine Fixture — diese Runde ändert ausschließlich die Verhaltensmatrix in docs/beweise/NAK-214.md. tools/dirigent/pruefliste.md ist deine Arbeitsliste. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks.

LAGE: Matrixprüfung 3 (Codex gpt-6-astra max, lesend, Thread 01a080f3-cc76-7fd3-81fe-ceeca7f7ea69, Stand 87ee76f5) bestätigt Befund 1 der Matrixprüfung 2 (Terminal bei laufendem Nachlauf, V-49) als geschlossen und meldet nichts Gebrochenes; genau ein Befund bleibt offen — Urteil wörtlich in docs/beweise/roh/NAK-214-matrixpruefung-3-87ee76f.txt, Prüfauftrag in docs/beweise/roh/NAK-214-matrixpruefung-3-auftrag.txt. Der Dirigent hat ihn an der Quelle bestätigt. Du schließt genau diesen einen, sonst nichts. Zeilen-IDs V-01 bis V-49 bleiben stabil; eine neue Zeile bekäme V-50. Nichts, was der Prüfer nicht erhoben hat, kommt dazu.

DER BEFUND UND DIE REGEL, DIE IHN SCHLIESST:

DEFEKT V-12 — die Ausrichtung der erzeugten Paare ist ausdrücklich ungemessen. Prüfer: „Wird der erste oder mittlere Befund herausgefiltert, kann eine anschließende `zip`-Paarbildung die Befundhälften verschieben, während sämtliche beschriebenen Bestandsprüfungen grün bleiben: `neue` übernimmt ausschließlich die Proposalhälfte (`proposal_verdrahtung.rs:58–62`), und `angebot` bleibt wegen der festen Capability-Sperre unabhängig davon `None` (`:86–89`, `:122`; `proposal.rs:928`). Die Zeile erreicht jetzt den Coordinator-Pfad, schließt dessen Paarprüfung aber ausdrücklich aus; der Schlussriegel-Rotbeweis misst lediglich drei statt zwei Vorschläge. […] Benenne eine Prüfung der tatsächlich erzeugten `(Befund, Proposal)`-Tupel, die eine verschobene Zuordnung erkennt, ohne die P5-Zustellsperre aufzuweichen." Bestätigt: V-12 sagt an HEAD selbst „Was die Zeile ausdrücklich NICHT misst: die Ausrichtung innerhalb von `paare` … eine `zip`-Mutation der Paarbildung bliebe grün"; `capability_vorhanden` ist fest `false` (`proposal_verdrahtung.rs:122`, `darf_draft_offer` `proposal.rs:916–928`), `paare` ist eine lokale Variable von `vorschlaege_bilden` (`:58–61`) und an keinem Bestand sichtbar. Die Zusage aus WN-04 („Die Paare bleiben zusammen: `angebot` braucht den Befund ZU seinem Vorschlag, und ein `zip` über zwei unterschiedlich lange Listen hängte den falschen an", `:55–57`) ist damit seit SONDE-014 Nacharbeit 2 eine Zusage ohne Messung. REGEL (Dirigent, 08.09.2026): Die Paarbildung wird als eigene, testbare Funktion in `proposal_verdrahtung.rs` geführt — eine reine Verschiebung der bestehenden `filter_map`-Zeilen (`:58–61`) in eine Funktion mit der Signatur „Befunde und Lage hinein, `Vec<(CauseHypothesis, Proposal)>` heraus", die `vorschlaege_bilden` an derselben Stelle ruft; kein Verhaltenswechsel, keine neue Sichtbarkeit nach außen (Sichtbarkeit `pub(super)` oder enger), `darf_draft_offer`, `capability_vorhanden` und die P5-Zustellsperre bleiben unberührt (§2.9 Nr. 3, M-52). V-12 (a) misst zusätzlich am Rückgabewert dieser Funktion je Tupel `befund.finding_id == vorschlag.finding_id`, mit dem herausgefilterten Befund an erster, mittlerer und letzter Position (drei Teilfälle), und die Länge der Tupelliste gleich der Zahl der übrigen Befunde; der Bestand (`stand.vorschlaege`, `vorschlaege_sicht`, `angebot == None`) wird weiter wie bisher über den Coordinator gemessen. Rotbeweis (neu, `NAK-214-rot-V-12.txt`): die Paarbildung als `zip` über Befunde und gefilterte Vorschläge — die Tupel verschieben sich, der Test wird rot; die Bestandsprüfung allein bliebe grün, und genau das dokumentiert der Rotbeweis. Der Satz „Was die Zeile ausdrücklich NICHT misst" entfällt; stattdessen sagt die Zeile, dass die Tupelprüfung die WN-04-Zusage misst, die in P5 an keinem Bestand sichtbar ist und in P6 (`draft_offer` mit `capability_vorhanden`) verhaltenswirksam wird. E1 (Aufrufkettentabelle) nennt die neue Funktion in der Kette `vorschlaege_bilden` → Paarbildung → `stand.vorschlaege.insert`; §2.7 M-52 und die Kopftabelle „Ticketpfade" bleiben wahr (`proposal_verdrahtung.rs` ist bereits Ticketpfad). Die Extraktion ist kein Neben-Refactor, sondern das Mittel, mit dem die Zusage messbar wird — das steht so in §2.0 E1 und in §5.

VERLAUF: Trage im Manifest §5 (append-only) „Matrixprüfung 3 — NEEDS_WORK (08.09.2026, Stand 87ee76f5)" mit Thread, Modell, Effort, Rohdatei und der Zuordnung (V-49 geschlossen, V-12 offen) ein, und darunter „Matrixnacharbeit 3" mit den geänderten Zeilen. Kopftabelle: Zeile „Phase" auf „Etappe 1 — Matrixnacharbeit 3 nach Matrixprüfung 3 (NEEDS_WORK, ein Restbefund)"; Regeln-Zeile um die V-12-Regel (Quelle: diese Auftragsdatei docs/beweise/roh/NAK-214-matrixnacharbeit-3-auftrag.txt). Der lebende Kopf und §2 werden umgeschrieben, §5 nur angehängt.

SELBSTAUDIT vor dem Commit: die geänderte Zeile gegen den Code an HEAD lesen (herausgefilterter Befund an Position 1, 2, 3 von 3; zwei von zwei herausgefiltert → leere Tupelliste, `stand.vorschlaege.remove`; keiner herausgefiltert → drei Tupel); die Sichtbarkeit der neuen Funktion; keine Zusage ohne Test und Rotbeweis; keine Manifestsätze, die mehr behaupten als der Test misst. Dann `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-214.md` grün, Commit mit Pathspec docs/beweise/NAK-214.md, Betreff „NAK-214 Matrixnacharbeit 3: …", pull --rebase, push, STOPP. Die Matrixprüfung 4 setzt der Dirigent.
