Matrixprüfung 4 (Wiederprüfung nach Matrixnacharbeit 3) NAK-214 / G5-Nacharbeit 3 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, kein cargo und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde an der Verhaltensmatrix:
`git diff ba3de43e118ba887936ab6c9d527a67b099039d4...c387891532e1aa9c7421424f2056e23444eb17a9 -- docs/beweise/NAK-214.md`
(ZIEL = c387891532e1aa9c7421424f2056e23444eb17a9 = HEAD, vor und nach deinem Lauf identisch; VORHER = ba3de43e, der Stand vor der Runde — er enthält gegenüber dem geprüften Stand 87ee76f5 nur drei Rohdateien unter docs/beweise/roh/, keine Manifeständerung). Der Codestand unter broker/, eq-copilot/, tools/ ist an VORHER und ZIEL identisch mit dem Kanonstand d3c6709 (Abschlusskanon NAK-213, GRÜN 61/61). Der übrige Matrixbereich wurde in den Matrixprüfungen 1 bis 3 (Threads 01a080a4, 01a080d5, 01a080f3-cc76-7fd3-81fe-ceeca7f7ea69; Rohdateien docs/beweise/roh/NAK-214-matrixpruefung-{1-d0f2f30,2-b204bbc,3-87ee76f}.txt) bereits geprüft und ist hier nicht Prüfgegenstand.

DER BEFUND, den diese Runde schließen sollte — wörtlich aus dem Urteil der Matrixprüfung 3, mit der Regel, die der Dirigent entschieden hat (docs/beweise/roh/NAK-214-matrixnacharbeit-3-auftrag.txt):
DEFEKT V-12: „Wird der erste oder mittlere Befund herausgefiltert, kann eine anschließende `zip`-Paarbildung die Befundhälften verschieben, während sämtliche beschriebenen Bestandsprüfungen grün bleiben: `neue` übernimmt ausschließlich die Proposalhälfte (`proposal_verdrahtung.rs:58–62`), und `angebot` bleibt wegen der festen Capability-Sperre unabhängig davon `None` (`:86–89`, `:122`; `proposal.rs:928`). Die Zeile erreicht jetzt den Coordinator-Pfad, schließt dessen Paarprüfung aber ausdrücklich aus […]. Benenne eine Prüfung der tatsächlich erzeugten `(Befund, Proposal)`-Tupel, die eine verschobene Zuordnung erkennt, ohne die P5-Zustellsperre aufzuweichen." Regel: Die Paarbildung wird als eigene, testbare Funktion in `proposal_verdrahtung.rs` geführt — reine Verschiebung der bestehenden `filter_map`-Zeilen (`:58–61`), Signatur „Befunde und Lage hinein, `Vec<(CauseHypothesis, Proposal)>` heraus", von `vorschlaege_bilden` an derselben Stelle gerufen; kein Verhaltenswechsel, Sichtbarkeit `pub(super)` oder enger; `darf_draft_offer`, `capability_vorhanden` und die P5-Zustellsperre unberührt (§2.9 Nr. 3, M-52). V-12 (a) misst zusätzlich am Rückgabewert je Tupel `befund.finding_id == vorschlag.finding_id` mit dem herausgefilterten Befund an erster, mittlerer und letzter Position und die Tupelzahl gleich der Zahl der übrigen Befunde; der Bestand (`stand.vorschlaege`, `vorschlaege_sicht`, `angebot == None`) bleibt über den Coordinator gemessen. Rotbeweis `NAK-214-rot-V-12.txt`: Paarbildung als `zip` → Tupel verschoben, Test rot, Bestandsprüfung allein bliebe grün. Der Satz „Was die Zeile ausdrücklich NICHT misst" entfällt; die Zeile sagt, dass die Tupelprüfung die WN-04-Zusage misst, die in P5 an keinem Bestand sichtbar ist und in P6 verhaltenswirksam wird. E1 nennt die neue Funktion in der Aufrufkette.

Der Worker hat die Änderungen an V-12, E1 und im Kopf/§5 eingetragen (Verlauf „Matrixprüfung 3" und „Matrixnacharbeit 3").

GATE-BEZUG, wörtlich: P5-Exit-Gate (Entwurf §59, docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung." SONDE-014 M-25 (Determinismus), M-46, M-52 (`darf_draft_offer` unverändert), M-57 (höchstens ein Angebot je Sitzung), WN-04 (Paare bleiben zusammen, `proposal_verdrahtung.rs:47–57`). Invarianten aus CLAUDE.md: „Schemas sind Verträge", „Keine toten UI-Elemente", keine Neben-Refactors (die Extraktion ist als Mittel der Messung in E1 begründet — prüfe, ob die Begründung trägt und der Umfang auf die Verschiebung der Paarbildung beschränkt ist).

Du beantwortest genau zwei Fragen:
1. Ist der Befund am Zielstand geschlossen — ja/nein, mit Beleg (Zeilen-ID, Entscheid, Satz der Matrix; und ob die Zusage am Code an ZIEL herstellbar ist, mit Datei:Zeile)? Insbesondere: Erkennt die benannte Tupelprüfung eine `zip`-Verschiebung bei erstem, mittlerem und letztem herausgefilterten Befund; ist die Extraktion so spezifiziert, dass `vorschlaege_bilden` verhaltensgleich bleibt (dieselbe `filter_map`, dieselbe Reihenfolge, `neue` und `angebot` aus denselben Tupeln) und die P5-Sperre unberührt ist; hat die Zeile Test (Datei::Fall) und Rotbeweis; widerspricht sie keiner anderen Zeile (V-05, V-08, V-11, V-13)?
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile oder einen Entscheid, einen Satz des Gate-Bezugs, eine unverändert geltende Referenzzeile, §2.8 („kein Fassungsschritt"), §2.9 oder eine Invariante aus CLAUDE.md? Hat eine geänderte Zeile ihren Test oder ihren Rotbeweis verloren?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1: DEFEKT (Widerspruch zu Gate, Regel, Entwurf, Vertrag, Referenzmatrix, Code an ZIEL oder Invariante; oder Regel ohne messende Zeile mit Test und Rotbeweis), LÜCKE (benennen, keine Nacharbeit), HÄRTUNG (benennen, keine Nacharbeit).
AUSDRÜCKLICH AUSGESCHLOSSEN: Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände in docs/**; der Manifestkopf; §3, §4, §5 von docs/beweise/NAK-214.md (nur §1 und §2 sind Prüfgegenstand; §5 liest du als Kontext); docs/beweise/G5.md, NAK-212.md, NAK-213.md, docs/offene-punkte.md; im Register datierte Härtungen NAK-191 bis NAK-211 und NAK-215 bis NAK-222; Sabotage-Szenarien gegen Prüfwerkzeuge (= Härtung); tools/dirigent/pruefliste.md ist keine Anforderungsquelle; die vom Dirigenten getragenen Lesarten E2, E6, E7, E9, die Regeln R6 bis R8 und die V-12-Regel sind keine Wortlautdefekte; Ticketfremdes nur insoweit, als die Matrix dorthin abgrenzt. Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — der Befund ist geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID (V-nn oder E-n) oder Manifestzeile, Quelle Datei:Zeile, welcher Satz von Gate, Regel, Entwurf, Vertrag, Referenzmatrix oder Invariante bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
