Nacharbeit 1 der Abschlussprüfung / Ticket NAK-312, Planschritt S25k (Etappe 8, Ticketabschluss) — Auftrag des Dirigenten an einen frischen Opus-Worker (2026-09-23)

ARBEITSORT UND GRENZE. Du arbeitest im sichtbaren Checkout C:\Users\phili\Projekte\Nakama (Branch master; miss `git rev-parse HEAD` zu Beginn und nenne ihn). Ticketgrenze dieser Runde: genau ein bestätigter Defekt der Abschlussprüfung, sonst nichts. Du änderst eq-copilot/plugin/tests/TransactionTestMain.cpp an genau einer Stelle und hängst deinen Bericht als Abschnitt „## 65. Nacharbeit 1 der Abschlussprüfung — D-1 (Bauer, 23.09.2026)" an docs/beweise/NAK-312.md an (der Kopf des Manifests bleibt Sache des Dirigenten; nicht anfassen). Keine weitere Datei; kein Produktcode; keine Behauptungszeile in tools/beweise.ps1 (sie bleibt richtig, Validierung Punkt 4); keine Legacy-Umbenennung; fremde Dateien (briefing-hub/, nimbalyst-local/) nie anfassen. Parallel arbeitet ein zweiter Worker in der Arbeitskopie C:\Users\phili\nk-doku an docs/beweise/NAK-310.md und pusht auf origin/master; deshalb vor dem Push `git pull --rebase` (Worktree sauber), und du berührst NAK-310-Dateien nie.

DER DEFEKT, wörtlich aus dem Rohurteil docs/beweise/roh/NAK-312-abschlusspruefung-urteil.md (Codex gpt-6-astra xhigh, Thread 01a0cdcc-8b86-76b2-b9da-28617470cccb), an der Quelle bestätigt (docs/beweise/roh/NAK-312-abschlusspruefung-validierung.md, lesender Opus-Thread c3fb5e3f, BESTÄTIGT, Klassenvorschlag DEFEKT; Einordnung des Dirigenten docs/beweise/NAK-312.md §64: DEFEKT):
„[P2] Halte den Testzustand über beide Prozessoraufbauten am Leben — eq-copilot/plugin/tests/TransactionTestMain.cpp:4655-4657: Im regulären B7-Lauf liefert blockStaende() ein temporäres std::array. Dessen überladener operator[] liefert eine Referenz und verlängert die Lebensdauer des Arrays nicht: Am Semikolon wird es zerstört, sodass beide folgenden blockPruefling(blk, mitBand) einen bereits zerstörten Zustand lesen. Damit enthält der neue M-20-NaN/Inf-Test undefiniertes Verhalten und liefert keinen belastbaren, compilerunabhängigen Nachweis. Halte den Zustand als eigene Kopie oder das vollständige Array in einer lokalen Variablen."

SCHLIESSENDE REGEL (Dirigent, §64): Ein Test, der einen Rotbeweis trägt, steht auf definiertem Verhalten; eine Referenz bindet nie an ein Unterobjekt, das über einen Funktionsaufruf (auch einen überladenen operator[]) aus einem Temporary gewonnen wird. Kleinster Fix nach der Validierung: in Zeile 4655 das Array in einer lokalen Variablen halten (`const auto staende = blockStaende(); const auto& mitBand = staende[1].satz;`) oder den Satz kopieren (`const auto mitBand = blockStaende()[1].satz;`); Erwartung (Ausgang endlich, 0 abweichend, 12 Blockrandziele), Behauptungstext des Tests (Zeilen 4672-4676) und die B7-Behauptung in tools/beweise.ps1 bleiben unverändert.

WAS DU TUST:
1. Lies docs/beweise/roh/NAK-312-abschlusspruefung-validierung.md ganz (Quelle, Ablauf, Zusagenlage, kleinster Fix) und docs/beweise/NAK-312.md §21.5 bis §21.11 (Etappe 3b, Teilfall „nicht endliche Hostwerte am Blockrand", Rotbeweis Teil b und Gegenprobebau NAK312_GEGENPROBE_OHNE_BLOCKRAND) sowie §64.
2. Baue den kleinsten Fix in eq-copilot/plugin/tests/TransactionTestMain.cpp (eine Stelle, Zeile 4655; Zeilenenden CRLF im Worktree beibehalten, `git ls-files --eol`). Prüfe mit `git grep -n "blockStaende()\["` und einer Sichtung der hinzugefügten Zeilen des Tickets (`git diff 090d0fa1...HEAD -- eq-copilot/plugin/tests/TransactionTestMain.cpp`), dass dasselbe Muster (Referenzbindung an `f(…)[…]`, `f(…).g(…)` aus einem Temporary) nirgends sonst in dieser Datei steht; die Validierung hat den Ticketdiff der Tests bereits durchsucht (Punkt 8 dort) und nur diese Stelle gefunden — bestätige das für TransactionTestMain.cpp und ändere nichts anderes.
3. Baue und fahre nur das betroffene Bein B7 (EqCopTransactionTest) aus dem Release-Baum: `cmake --build eq-copilot/build --config Release --target EqCopTransactionTest` (cmake liegt nicht im Bash-PATH: aus pwsh bauen; tools/beweise.ps1 zeigt den Aufruf des Beins; prüfe den Binary-Zeitstempel gegen die Quelle, Landmine Copy-Item/LastWriteTime in CLAUDE.md) und den Test; Ergebnis grün mit der Zeile „312/M-20 Teilfall nicht_endliche_hostwerte_am_blockrand" als bestanden. Wiederhole danach den Rotbeweis Teil b des Teilfalls einmal auf definiertem Verhalten (so, wie §21 ihn beschreibt: Gegenprobebau mit NAK312_GEGENPROBE_OHNE_BLOCKRAND oder der dort dokumentierte Weg), lege die Rohausgabe unter docs/beweise/roh/NAK-312-abschluss-nacharbeit-1-rot-M-20-teil-b.txt ab und stelle den Release-Baum danach wieder auf den regulären Bau (Binary-Zeitstempel und ein grüner regulärer Lauf von B7 danach). Kein voller Kanon (der Dirigent fährt ihn beim Abschluss); keine anderen Beine.
4. Bericht §65 im Manifest: HEAD zu Beginn, die Änderung (Zeile vorher wörtlich, nachher wörtlich), die Musterprüfung aus Punkt 2 (Kommando, Treffer), Bau- und Laufbelege von B7 (Zeitstempel des Binaries, Prüfzeilen-Zählung, die Zeile des Teilfalls), der wiederholte Rotbeweis Teil b (Kommando, rote Zeile, Rohdatei), die Rückstellung des Baus, Abweichungen von diesem Auftrag mit Begründung, Selbstaudit gegen tools/dirigent/pruefliste.md (Abschnitte, die für eine Testkorrektur gelten), Hinweise an den Dirigenten. Uhrzeiten nur aus `Get-Date`. In Markdown-Tabellenzellen ein Pipe als \|.
5. WÄCHTER (NAK-354): Beim Warten auf einen Bau oder Lauf setzt du einen abgelaufenen Wächter neu und liest bei jeder Rückkehr zuerst das Log; du meldest dich nie mit „ich warte auf die Benachrichtigung" und stehst dann.

GIT-REGELN (Skill §6, CLAUDE.md): Commit nur mit explizitem Pathspec (`git add -- eq-copilot/plugin/tests/TransactionTestMain.cpp docs/beweise/NAK-312.md docs/beweise/roh/NAK-312-abschluss-nacharbeit-1-rot-M-20-teil-b.txt`), nie `git add -A`, nie `--amend`, nie `git reset --hard`, nie fremde uncommittete Dateien anfassen (im Worktree liegen untracked Dateien des Dirigenten unter docs/beweise/roh/ — nicht stagen). Mehrzeilige Commitnachricht über `git commit -F <datei>`; sie endet mit `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Betreff: „NAK-312 Abschluss Nacharbeit 1: D-1 Lebensdauer des Blockstands im Teilfall 312/M-20 (B7), Rotbeweis Teil b wiederholt". Vor dem Commit `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` (sauber = Exit 0 oder nur „benannt"). Dann `git pull --rebase`, dann `git push` als eigener Schritt. Beende dich mit einer Zeile: FERTIG Nacharbeit 1 Abschlussprüfung NAK-312, B7 <grün/rot>, Rotbeweis Teil b <rot/nicht gefahren>, HEAD <sha>.
