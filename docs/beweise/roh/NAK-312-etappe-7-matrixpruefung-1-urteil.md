# NAK-312 — Matrixprüfung der Etappe 7, Runde 1: Rohurteil (Codex gpt-6-astra, Effort xhigh, lesend)

Abgelegt vom Dirigenten am 23.09.2026, 00:52 Uhr. Der Prüfer konnte in der schreibgeschützten Sandbox keine Berichtsdatei anlegen; dieser Text ist sein Schlussurteil aus `codex review --json -o` (Datei `%TEMP%\nakama-nak312-e7-matrix1-6ea8ec526537ece6505b6d379516d435b4069316-last.txt`), unverändert. Thread `01a0cb3f-9523-72a2-affc-7fc5458a5a87`; Lauf 00:32:16 bis 00:49:40 Uhr (Startlog); HEAD vor und nach dem Lauf `6ea8ec526537ece6505b6d379516d435b4069316`. Auftrag: `docs/beweise/roh/NAK-312-etappe-7-matrixpruefung-1-auftrag.txt`. Die neun Befunde führt der Dirigent als D1 bis D9 in der Reihenfolge dieses Textes.

---

NEEDS_WORK: neun Defekte in Matrix und Bauplan, keine Lücken oder Härtungen. Ausschließlich lesend geprüft; keine Testläufe und wegen der schreibgeschützten Sandbox keine Berichtdatei angelegt. HEAD zu Beginn und am Ende: 6ea8ec526537ece6505b6d379516d435b4069316.

Full review comments:

- [P2] Ziehe die bestehenden 32-Quellen-Tests in Satz 1 nach — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9455-9459
  DEFEKT §47.2: Die geplante Begrenzung lässt bestehende Tests scheitern, deren Anpassung fehlt. B13 verlangt in `Sonde012SourcesModelTest.cpp:809–817` weiterhin 32 sichtbare Quellen und liefert andernfalls eine Fehlerlatenz. A23 wartet in `Sonde012SourcesLatencyTest.cpp:216–219,320–327` ebenfalls auf sämtliche 32 Quellen und bricht nach Zeitablauf ab. Beide verwenden echte Snapshots, nicht den ausgenommenen Fixturezugang. Satz 1 muss diese Erwartungen samt A23-Prüfweg auf 20 angenommene und 12 abgewiesene Quellen umstellen; andernfalls kann der zugesagte abschließende Kanon nicht grün werden.

- [P2] Plane das Leeren der Live-Sicht beim Rollenwechsel ausdrücklich — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9750-9754
  DEFEKT §47.4/M-110: Bei bereits empfangenen Live-Quellen erzeugt die leere Mitgliederkopie keine leere Sicht. `SourcesModel.cpp:430–440` behält Einträge mit `fluechtigImSnapshot` ausdrücklich als unklassifiziert; auch `controlEnde()` entfernt sie nicht (`:576–595`). Die vorgelagerte C-1-Regel behält solche Snapshot-Kandidaten ebenfalls. Damit bleibt beim Wechsel Main→Legacy mindestens eine Zeile stehen, entgegen M-110 und M-120. Plane eine rollenabhängige Stilllegung der Live-Sicht und einen Test mit echten Snapshot-Einträgen, statt nur persistenten Mitgliedern; dies betrifft die gemeinsam zu prüfenden Übergänge aus [CLAUDE.md:77–79](../../CLAUDE.md#L77-L79).

- [P2] Definiere den K-A-Tap während der Eintrittsblende konsistent — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9045-9045
  DEFEKT M-96, K-A-Hälfte: Die Zusage verlangt den Eingang als Tap-Inhalt auch im ersten Bypassblock. §47.3 beschreibt dagegen einen Wechsel des Committed-Pfads wie beim eigenen Schalter: zunächst 256 Samples Blende, danach Passthrough. Am ZIEL entsteht `postCommitted` aus dem bereits gemischten Committed-Ausgang (`DspKern.cpp:1317–1319,1342`); beim HB-Aufbau mit +6 dB ist dieser während der Blende nicht der Eingang. Lege für K-A den Übergangsinhalt ausdrücklich fest und gleiche Test und Bauplan ab, sonst widersprechen sich Messoracle und geplanter Mechanismus bereits im ersten Bypassblock.

- [P2] Trenne die noch rote Analysehälfte von M-99 und M-107 — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9048-9048
  DEFEKT M-99/M-107: Beide Zeilen sind vollständig als Regressionswache bezeichnet, versprechen aber zusätzlich Analyse beziehungsweise Analyseverbuchung über beide Eintritte. Am ZIEL erledigt die geerbte JUCE-Basis ausschließlich die Prüfung und das Leeren zusätzlicher Ausgangskanäle (`juce_AudioProcessor.cpp:592–606`); sie erreicht weder Veröffentlichung noch `verwirfOhneAudio` aus `SondeProcessor.cpp:643–665`. Nur die Audiohälfte gilt bereits. Kennzeichne die Analysehälfte separat als noch nicht erfüllte Zusage und gib ihr einen passenden Rotbeweis; die angegebene Double-Rückkopiermutation prüft ausschließlich die Audiohälfte.

- [P2] Miss die Ruhegrenze vor der Rückkehr aus dem Hostbypass — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9052-9052
  DEFEKT M-103: Bei der bereits vor der Folge laufenden Geste zählen die 40 normalen Blöcke davor und danach zusammen 20.480 Samples, also mehr als die Ruhegrenze von 12.000 Samples bei 48 kHz. Auch wenn der Mutant sämtliche Bypass-Samples unterschlägt, schließt ein anschließender Kontrolltakt deshalb die Epoche (`SondeProcessor.cpp:1834–1839`). Die beschriebene Vorher/Nachher-Messung liefert nicht zuverlässig das behauptete Rot „Epoche bleibt offen“. Prüfe die Grenze während des Bypassabschnitts mit festgelegtem Gestenbeginn und expliziten Kontrolltakten, bevor normale Blöcke den fehlenden Fortschritt verdecken.

- [P2] Ergänze den verlangten Zählerüberlauf im Bypass-Prüfweg — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9052-9052
  DEFEKT §46/M-103: Der ausdrücklich verlangte Gegenfall „Überlauf von Zählern“ fehlt für den hier berührten Samplezähler. M-103 prüft nur eine kurze Folge; M-121 hält eine Revision am Zahlenrand, und M-124 begrenzt Kandidatenzahlen, aber keiner dieser Fälle führt einen Zähler über seinen Rand. Gerade die Ruheprüfung verwendet die Differenz zweier `uint64_t`-Samplestände (`SondeProcessor.h:776–777`, `SondeProcessor.cpp:1837–1838`). Ergänze einen deterministischen Bypass-Fall über den Wrap hinweg samt Rotbeweis; das ist eine ausdrückliche Anforderung aus `NAK-312-etappe-7a-auftrag.txt:41`, keine optionale Härtung.

- [P2] Ersetze den wirkungslosen Offline-Mutanten in M-104 — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9053-9053
  DEFEKT M-104: Die erste Mutationsalternative, `setzeOfflineRiegel(true)` im Bypasseintritt aufzurufen, bewirkt nicht das behauptete „B schneidet, A blendet“: Beide Prozessoren durchlaufen diesen Eintritt und erhalten dieselbe Mutation. Aufbau HB enthält keine Vorschau; das harte Schalten betrifft nach `DspKern.cpp:1225–1236` nur die bereits auf Processed stehende Hörmatrix, während die neue Bypassblende ausdrücklich entkoppelt sein soll. A und B können daher weiterhin bitgleich bleiben. Verwende die tatsächlich wirksame Entkopplungsmutation oder prüfe zusätzlich unmittelbar, dass der Echtzeit-Bypasseintritt den Offline-Riegel nicht setzt.

- [P2] Lass den Read-only-Rotbeweis die tatsächliche Mutation beobachten — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9088-9088
  DEFEKT M-117: Wird die Wache unmittelbar hinter `zustand.common = neu` verschoben (`State.cpp:338`), kehrt sie weiterhin vor der Publikation und Dirty-Meldung an `:376–377` mit `false` zurück. Auch die gespeicherten Bytes bleiben gleich, weil `NakamaState.cpp:2165–2168` im Read-only-Fall ausschließlich `originalBytes` ausgibt. Der beschriebene Mutant erzeugt somit kein Host-Dirty und kann alle genannten Beobachtungen bestehen. Prüfe zusätzlich die unveränderte In-Memory-Bindung oder wähle eine Mutation, die an der zugesagten Verweigerung tatsächlich scheitert; damit wird die Read-only-Invariante aus [CLAUDE.md:193–195](../../CLAUDE.md#L193-L195) messend abgesichert.

- [P2] Klassifiziere die kapazitätsabhängigen Zusagen nicht als reine Wachen — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-312.md:9116-9117
  DEFEKT M-127/M-128: Bei den ausdrücklich verwendeten echten Snapshots gelten die vollständigen Zusagen am ZIEL noch nicht. M-127 erhält bei 21/32/64 Quellen ebenso viele Sichtzeilen, aber nur 20 Rechtecke (`SourcesModel.cpp:1670`, `PluginEditor.cpp:1144–1160`). In M-128 ist X aus dem 21er-Snapshot heute zudem ein bekannter Eintrag; passende P2-Frames und Findings werden daher berücksichtigt (`SourcesModel.cpp:1576–1581,1716–1717`). Das heutige Überspringen völlig unbekannter IDs beweist diesen Fall nicht. Trenne die bereits grüne Ersatzzielhälfte von der roten Kapazitätshälfte und plane Gegenproben für die tatsächliche 21. Quelle statt ausschließlich nachträglicher Wachmutationen.