NAK-312 / Planschritt S25k — Nacharbeit 1 der Etappe 7b, Teil 1 (Sätze 1 und 3): Bauauftrag des Dirigenten (2026-09-23)

Du bist ein frischer Opus-Worker (Bauer) im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master, `--permission-mode dontAsk`. Du baust GENAU die drei Punkte unten, nicht mehr. Ticketmanifest: docs/beweise/NAK-312.md (Kopf; Regeln §2 und §50.4; Zusagen §45.2 bis §45.4; Matrix §46 in der Fassung nach §51, Zeilen M-110 bis M-132; Bauplan §47.2, §47.4, §47.5; Bauerbericht der Etappe 7b Teil 1 §54; Messung §55; Erstprüfung §56.1; Validierung §56.2; Einordnung des Dirigenten §56.3 — sie ist dein Auftrag im Wortlaut, samt der Regeln R-312-32 bis R-312-34 und der Matrixzeile M-133). Prüfliste: tools/dirigent/pruefliste.md (Arbeitsliste, keine Anforderungsquelle). Rohurteil und Validierung: docs/beweise/roh/NAK-312-etappe-7b-teil1-erstpruefung-urteil.md (D-1 bis D-3 wörtlich), docs/beweise/roh/NAK-312-etappe-7b-teil1-erstpruefung-validierung.md (je Befund Quelle, Ablauf, Zusagenlage; für D-1 der Rotbeweis mit vorhandenen Zugängen, für D-2 das Muster 312/M-86, für D-3 der Umfang der Pixelvergleiche). Basis deiner Runde ist HEAD beim Start (`git rev-parse HEAD`; nenne ihn im Manifest).

Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

PUNKT 1 — DEFEKT D-1 (R-312-32, neue Matrixzeile M-133): Befundzähler nach dem Nachrücken über den lokalen Eintritt.
Befund wörtlich (Erstprüfung): „Aktualisiere den Befundzähler nach dem Nachrücken — eq-copilot/plugin/src/SourcesModel.cpp:476-477. DEFEKT gegen die ehrliche Zustandsanzeige aus CLAUDE.md:44–45: Bei 21 gespeicherten Quellen kann X zunächst warten, während eine angenommene Quelle Y offline ist. Enthält der Snapshot bereits einen offenen Befund für X und wird Y anschließend lokal entfernt, rückt X hier mit `findingsOffen == 0` nach. Wartende Quellen wurden zuvor absichtlich nicht gezählt; nach dem Einfügen fehlt jedoch `zaehleOffeneFindings()`, und auch `tick()` korrigiert den Zähler nicht. Bei Auswahl von X behauptet der Editor deshalb bis zum nächsten Snapshot „no findings yet“, obwohl der Befund vorhanden ist. Nach dem Nachrücken die Zähler erneut ableiten." Validiert (Bericht D-1): Nachrückschleife in `setzePersistenteMitglieder` (`:461-477`); `zaehleOffeneFindings()` (`:1771-1790`) wird nur in `uebernehmeSessionSnapshot` (`:1441`) und über `setzeAlleBefundeStale` (`:1804`) gerufen; `befunde` hält den Befund für das wartende X; der wartende Eintrag trägt `findingsOffen` 0 (Default); `tick()`, `revidiere()`, `stelleZielSicher()` korrigieren nicht; die ACK-Nachführung (`Ipc.cpp:1553`) läuft durch dieselbe Schleife; Anzeige `PluginEditor.cpp:1529-1531` und `:1383-1384`.
Schließende Regel R-312-32 (§56.3): Jeder Eintritt, der Einträge anlegt oder nachrücken lässt — `uebernehmeSessionSnapshot`, `setzePersistenteMitglieder` und damit die ACK-Nachführung —, leitet die Befundzähler danach aus `befunde` neu ab (`zaehleOffeneFindings()` unter demselben `mutex`); zu jedem Zeitpunkt entspricht `Zeile::findingsOffen` der Zahl der nicht als `stale` markierten Befunde mit dieser `candidate_source`. Eine nachgerückte Quelle trägt ihren Stand aus dem jüngsten Snapshot einschließlich der Befunde.
Bau: In `SourcesModel::setzePersistenteMitglieder` nach der Nachrückschleife (`:461-477`) und vor `stelleZielSicher()` die Zähler neu ableiten (`zaehleOffeneFindings()`; die Funktion hält den `mutex` bereits — prüfe das am Funktionskopf, keine zweite Sperre, keine Sperrenordnungsänderung). Prüfe, ob `projektReload` betroffen ist (laut Validierung nicht: es leert Wartende und Befunde, `:528`, `:562`); wenn doch, gilt dieselbe Regel. Kein Editor-Code: die Anzeige liest `findingsOffen` aus `sicht()`.
Matrixzeile M-133 in §46 nach M-130 eintragen, Wortlaut aus §56.3, Spalten wie die Nachbarzeilen (Kennung, Befund/Zusage, Zustand, Ereignis, Zusage fett, Reihenfolge, Test, Rotbeweis). Test: B13 `312/M-133 nachruecken_zaehlt_befunde` in eq-copilot/plugin/tests/Sonde012SourcesModelTest.cpp nach dem Rotbeweismuster der Validierung: Testform `setzePersistenteMitglieder (…, 0, 1)` (`SourcesModel.h:300-310`, benutzt an `:980`), `beginneSubscription`, `uebernehme (m, snapshot (…, { befundJson (…, x.id, "ready_to_send") }), t)` (Muster `:1052-1055`, Helfer `:141-158`, `:160-192`, `:215-220`), dann `setzePersistenteMitglieder (G ohne G0, 0, 2)`. Erwartet: X ist Zeile, `nichtAngenommen` 0, `sicht().befunde.size() == 1`, Zeile X `findingsOffen == 1`; danach `controlEnde` → Befund `stale`, Zeile X zählt 0. Zweiter Fall (ACK-Nachführung): wenn im B13-Test ohne Prozessor erreichbar, messen; sonst am Code feststellen und in der Matrixzeile so nennen, dass sie dieselbe Funktion ruft (Zeile zitieren).
Gegenprobe am Stand vor deinem Bau: der neue Test ist rot (0 statt 1); Ausgabe in docs/beweise/roh/NAK-312-etappe7b-nacharbeit1-gegenprobe-M-133.txt. Rotbeweis nach dem Bau: `zaehleOffeneFindings()` nach der Schleife entfernen → rot → zurück → grün; Ausgabe in docs/beweise/roh/NAK-312-rot-M-133.txt. B13-Behauptungszeile in tools/beweise.ps1 um M-133 ergänzen (nur, was der Test misst; R-312-34).

PUNKT 2 — DEFEKT D-2 (R-312-33; nur Test, Rotbeweis, Behauptung, Matrix — kein Produktcode): Prüfdeckung von M-130 „beim ersten erlaubten Block".
Befund wörtlich (Erstprüfung): „Prüfe M-130 bereits nach dem ersten erlaubten Block — eq-copilot/plugin/tests/MarkierungTestMain.cpp:2298-2300. DEFEKT in der Prüfdeckung von M-130: Nach `prepareToPlay` verarbeitet der Test zunächst 40 erlaubte Blöcke und verlangt anschließend lediglich `hoerbar > 0` sowie ein neues `begin`. Ein versehentlich erst im zweiten oder späteren Block beginnender Auftrag erfüllt sämtliche Bedingungen ebenfalls, obwohl M-130 ausdrücklich den ersten erlaubten Block zusagt. Der vorhandene Rotbeweis prüft nur den Verlust des Auftrags, nicht diesen Zeitpunkt. Den ersten Block separat verarbeiten und dessen Beginn beobachten, bevor die restlichen Blöcke folgen." Validiert (Bericht D-2): `bloecke (n)` (`:1546-1578`) liefert Summen, `ernte()` (`:1582-1599`) die Ereignisse seit der letzten Ernte; Muster `312/M-86` (`:2117-2119`, `:2128-2136`) beobachtet den ersten Block einzeln; das Produkt hält den Zeitpunkt (`HoerMarkierung.h:533-539`, `:630`, `:658-663`; `erlaubt` `PluginProcessor.cpp:877-884`); der Rotbeweis docs/beweise/roh/NAK-312-rot-M-130.txt fällt an „Auftrag bleibt".
Schließende Regel R-312-33 (§56.3): Eine Zusage über den Zeitpunkt eines Ereignisses wird am Block gemessen, nicht an der Summe; der Test beobachtet den ersten Block getrennt (Muster 312/M-86), und der Rotbeweis verzögert das Ereignis um einen Block.
Bau: Im Zweig `Weg::release` (`MarkierungTestMain.cpp:2268-2275`) nach `prepareToPlay` zuerst `s.bloecke (1)` und `s.ernte()` — im ersten erlaubten Block: genau ein neues `begin` (Kennung ≠ altes `begin`), kein `end`, `hoerbar == 1` bzw. `abweichend == 1` (Muster M-86) —, dann `s.bloecke (39)` und die bisherigen Gesamtbedingungen (`begins == 1`, `ends == 0`, `markierungHoerbar()`). Die Prüfung `312/M-130` (`:2298-2304`) so fassen, dass ihr Text sagt, was sie misst. Der Zweig `Weg::reset` (`312/M-63`, Zusage „beim naechsten erlaubten Block NEU") liegt außerhalb des Prüfbereichs dieser Etappe und bleibt unverändert — vermerke die gleiche Schwäche als Hinweis im Manifest (kein Bau). Rotbeweis am Zeitpunkt: eine Testmutation, die den Neubeginn um genau einen Block verzögert (etwa im Markierungspfad nach `prepareToPlay` den ersten erlaubten Block überspringen — Ort und Form entscheidest du, im Produktcode nur als vorübergehende Mutation, danach bytegleich zurück und Zeitstempel setzen, NAK-230), → `312/M-130` rot an der Erster-Block-Bedingung → zurück → grün; Ausgabe in docs/beweise/roh/NAK-312-rot-M-130-erster-block.txt. Der bestehende Rotbeweis (`reicheAus()`) bleibt gültig. A3-Behauptungszeile in tools/beweise.ps1 (`:509`) um den gemessenen Zeitpunkt ergänzen; M-130 in §46 (Test- und Rotbeweis-Spalte) berichtigen.

PUNKT 3 — DEFEKT D-3 (R-312-34; Runner-Text und Matrix, kein Testcode): B15-Behauptungszeile „die bisherigen Bilder bleiben bytegleich".
Befund wörtlich (Erstprüfung): „Beschränke die B15-Behauptung auf den tatsächlichen Messumfang — tools/beweise.ps1:752. DEFEKT gegen Prüfauftrag (d): Der neue Nachsatz „die bisherigen Bilder bleiben bytegleich“ wird von B15 nicht gemessen. `schreibeShot` überschreibt die PNGs und prüft lediglich erfolgreiches Schreiben sowie Fenstermaße; die neuen Pixelvergleiche betreffen Diagnosefeld und Sources-Spalte, nicht die bisherigen Bilder gegen eine Referenz. Eine versehentliche Änderung etwa des Titeltexts könnte daher unbemerkt bleiben. Der einmalige Bildvergleich des Bauers ersetzt keinen Vergleich im jeweiligen Kanonlauf. Den Nachsatz entfernen oder einen echten Referenzvergleich anschließen." Validiert (Bericht D-3): kein Referenzvergleich im Bein (`ShotTestMain.cpp:120-140`, `:274-279`; keine SHA-Liste, keine Referenzbilder); Bytegleichheit einmalig vom Bauer gemessen (§54.5, §54.8, §54.14, §54.19); M-129 Test-Spalte weist den Teil B15 zu; M-131 Rotbeweis-Spalte setzt den Vergleich voraus.
Schließende Regel R-312-34 (§56.3): Eine Behauptungszeile trägt nur, was das Bein im Kanonlauf misst; einmalige Etappenmessungen stehen mit Verfahren und Werten im Manifest und in der Matrix als Etappenmessung, nie in der Behauptung. Der Dirigent hat den Weg entschieden: streichen, nicht Riegel bauen (ein Bildregressionsriegel ist Härtung NAK-372).
Bau: In tools/beweise.ps1:752 den Nachsatz „; die bisherigen Bilder bleiben bytegleich" streichen (der Rest der Zeile bleibt; danach `[Management.Automation.Language.Parser]::ParseFile` ohne Fehler, Landmine: ein `'` im Behauptungstext bricht den Parser). In §46 die Test-Spalte von M-129 und die Rotbeweis-Spalte von M-131 berichtigen: die Bytegleichheit der bestehenden Bilder ist eine einmalige Etappenmessung des Bauers gegen den Basisstand (SHA-256, §54.5, §54.8, §54.14, §54.19), nicht Teil von B15 im Kanon; der M-131-Rotbeweis „Sichtsatz weicht ab" ist an M-123 und M-124 gefallen (§54.19). Keine Änderung an ShotTestMain.cpp.

REGELN: R-312-32 bis R-312-34 aus §56.3 wörtlich in §2 des Manifests nachtragen (Muster R-312-24).

GRENZE: nur eq-copilot/plugin/src/SourcesModel.cpp (Punkt 1), eq-copilot/plugin/tests/Sonde012SourcesModelTest.cpp (Punkt 1), eq-copilot/plugin/tests/MarkierungTestMain.cpp (Punkt 2), tools/beweise.ps1 (B13-, A3-, B15-Behauptungszeilen), docs/beweise/NAK-312.md, die genannten Rohdateien unter docs/beweise/roh/. Produktcode außer Punkt 1 nur als vorübergehende Rotbeweismutation, bytegleich zurück. Audio-Thread unberührt; State unberührt (keine Bytes, keine Goldens); keine neue Oberfläche; keine Legacy-Umbenennung; Identität eingefroren; Sperrenordnung unverändert (sourcesDrainMutex vor bindungMutex; Modell-mutex wie heute). Fremde uncommittete Dateien (briefing-hub/, nimbalyst-local/) nie anfassen.

BEWEISLAUF: (1) Gegenprobe am Stand vor deinem Bau: M-133 rot (Ausgabe in die Gegenprobedatei). (2) Bau der drei Punkte. (3) Einzelbeine über tools/beweise.ps1 — B13 (Sonde012SourcesModelTest), A3 (MarkierungTest), B15 (EqCopShot --sonde012-suite) — grün; Einzelbefehle stehen im Skript; ein Bein niemals als Session-Hintergrundbefehl, sondern abgekoppelt mit Log und `EXIT=`-Zeile (Muster Skill §3.5), und beim Warten auf ein Bein einen abgelaufenen Wächter neu setzen und bei jeder Rückkehr zuerst das Log lesen (NAK-354); Turn nie beenden, solange ein Bein läuft. (4) Rotbeweise: M-133 (Neuzählung entfernen) und M-130 (Neubeginn um einen Block verzögert) je rot → zurück → grün, Ausgaben in die Rotbeweisdateien; nach jedem Zurückspielen `LastWriteTime` der Quelle setzen und den Binary-Zeitstempel prüfen (NAK-230). (5) Kein voller Kanon in dieser Nacharbeit (der Abschluss der Etappe fährt ihn). Übergib nie NOT RUN.

MANIFEST: neuer Abschnitt „## 57. Nacharbeit 1 der Etappe 7b, Teil 1 — D-1 (M-133, R-312-32), D-2 (M-130, R-312-33), D-3 (R-312-34) (Bauer, 23.09.2026)" nach dem Muster von §42: Gebaut je Punkt (Diff-Beschreibung mit Datei:Zeile), Gegenprobe, Matrixzeile M-133 und die berichtigten Zeilen M-129, M-130, M-131 (Tabelle wie §46), Einzelbeine mit Kopfzeilen, Rotbeweise mit Rohdateien, Abweichungen begründet (A-123 ff.), Prüfliste abgehakt, Selbstaudit (Zahlenränder, stale Closures, Sperrenordnung, UI-Texte), Hinweise an den Dirigenten mit Produktwirkung. Kopfzeile „Etappe" des Manifests: letzten Fettsatz auf „**Etappe 7b Teil 1: Nacharbeit 1 gebaut, Wiederprüfung folgt**" setzen (≤ 70 Zeichen). Vor dem Commit: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` ohne Befund; Pipe-Zeichen in Tabellenzellen als \|; Zeilenenden je Datei mit `git ls-files --eol` lesen und gleich zurückschreiben (Manifest: Index LF, Arbeitskopie CRLF — so lassen, wie du sie vorfindest; Rohdateien LF).

GIT: nie `git add -A`, nie `--amend`, nie `git reset --hard`, fremde uncommittete Dateien nie anfassen; ein Commit je Änderungssatz mit explizitem Pathspec (Code+Tests+Rohdateien+Manifest zusammen ist in Ordnung), Betreff „NAK-312 Etappe 7b Teil 1 Nacharbeit 1: …", Commitnachricht über `git commit -F <datei>` mit der Zeile „Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>" am Ende; vor dem Push `git pull --rebase` bei sauberem Worktree, dann `git push` als eigener Schritt. Zeiten nur aus `Get-Date` (nie geschätzt). Am Ende: Baum sauber (bis auf die zwei fremden Ordner), HEAD = origin/master, und deine letzte Ausgabe nennt Basis-SHA, End-SHA und die Kopfzeilen der gefahrenen Beine.
