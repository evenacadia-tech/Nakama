# NAK-309 Abschlussprüfung T2 — Rohbericht des Prüfers (wörtlich)

Prüfer: frischer, lesender Opus-Thread (Agent der Dirigentensession), 19.09.2026; Auftrag `docs/beweise/roh/NAK-309-abschlusspruefung-auftrag.txt`; per Skript aus dem Transkript gezogen, nicht abgetippt; 78 Werkzeugaufrufe, 686 Sekunden, nichts geschrieben.

---

URTEIL: PASS — kein DEFEKT (Lücken und Härtungen sind unten benannt).

**geprüft.** Der volle Ticketdiff `4c1c7f3c…4a7c08e0` über die 32 Repo-Pfade (`git diff --stat` selbst gefahren; die 28 Nicht-`docs`-Pfade des Änderungssatzes sind deckungsgleich mit der Auftragsliste, es gibt seit BASIS keine weitere geänderte Nicht-`docs`-Datei) und der Nachbarrepo-Diff `01f72386…15b3b1a1` über die vier Pfade. Am Zielstand im Zusammenhang gelesen: `tools/beweise.ps1` (Bauschritt, Baustandblock, Meldeweg-Block, Urteilsblock, Rohausgabe), `tools/eq-copilot/pruefe_beweisrunner.py` (Kopfvertrag, `Ableitung`, `konfigurationsstand`, `_lies_export`, `baustand`, `baustand_cli`, `nicht_gelaufen`, `gemessene_im_runner`, `urteilsvorrang`, `_fall_m67/m70/m75`, `selbsttest`, `main`), `tools/fl/laufzeit.ps1` (Besitzblock 397–563, `Neue-Umgebung`, `Rendere`, `Fahre-Lauf`, `Pruefe-McpStand`, Einstieg) und `tools/fl/szenario.py` (`lade_mcp_modul`, `_nimmt_kennung`, `Umgebung.verbindung`, `_sende`, `_zuordnen`, `_antwort_zelle`). Vollständig gelesen: `ProbePipeRegel.h`, die Diffs von `PipeClient.cpp/.h`, `PluginProcessor.cpp/.h`, `CMakeLists.txt`, `PipeProbeMain.cpp`, `PipeClientLifecycleTestMain.cpp`, `LebenslaufTestMain.cpp`, `IpcTestMain.cpp`, `V3TestServer.h`, `ShotTestMain.cpp`, `Sonde013FingerprintGoldenTest.cpp`, `StateMigrationTestMain.cpp`, `briefkasten.rs`, `store_crash_matrix.rs`, `erzeuge_state_fixtures.py`, `pruefe_installer_manifest.py` (Bauordnungsriegel, `hashen_und_binden`, `broker_pin`, Attrappe), die vier Szenariodateien, `mcp-stand.json`, `README.md`, `nakama-installer-v1.json`, `fixtures/state/MANIFEST.json`. Selbst gemessen mit lesenden Kommandos: M-73 (leer), die Vollständigkeit der Testschalter-Zielmenge im CMakeLists, die `git log`-Probe, dass seit `789f305a` nur `eq-copilot/install/nakama-installer-v1.json` geändert wurde, der MCP-Pin gegen `git rev-parse` und die vier Blob-Hashes im Nachbarrepo, die Konstanten `kJsonSchemaMinor=5` und `kFeatureBatchSchemaMinor=2`, die Echo-Felder im Controller (`index`, `track`, `mode`), alle `V3Verdrahtung`- und `PipeClient`-Konstruktionsstellen, alle Prozess-Beendigungsstellen in `laufzeit.ps1`, HEAD vor und nach dem Lauf.

**nicht geprüft.** Nichts gebaut, kein Test, kein Kanon, kein Werkzeug des Prüfbereichs, kein FL Studio, keine Installation — das Urteil über Laufverhalten stützt sich für diese Teile auf die Rohausgaben (`docs/beweise/roh/NAK-309-dca4640-dirty.md`: Urteilszeile `GRUEN - 69/69`, Exit 0, `Baustand: FRISCH (Exit 0)`, Kreuzprobe 38 gedeckt/0 nicht gedeckt, Inventar 0 Lücken, A4/A4-SI/A4b/A12/A17/A35/A36/B2/B8/B10/B22/B28/B30/A33 je `[OK] Exit 0`, A32 `[HINWEIS] Exit 4`; `NAK-309-laufzeit-1d41b460.md` über §21) — das habe ich gelesen, nicht nachgemessen. Die vier Etappenprüfungen (§10, §13, §16, §20) habe ich nicht Zeile für Zeile wiederholt; einzelne Matrixzeilen ohne Bezug zu (a)–(d) (M-29 bis M-56 im Detail, M-59 bis M-61 Feldzählung) habe ich nur an der tragenden Codestelle, nicht in allen Gegenfällen nachvollzogen. `docs/**` außer den vier Szenariodateien war ausgeschlossen; `tools/fl/LIES-MICH.md` habe ich nur überflogen (Prosa). Ich konnte nicht messen, ob `eq-copilot/plugin/probe/PipeProbeMain.cpp` übersetzt — der Kanon baut das Ziel nicht (siehe L-1); die Kopfdatei `ProbePipeRegel.h` selbst ist über A4b und B8 gebaut.

**Befunde**

**L-1 (LÜCKE) — `eq-copilot/plugin/probe/PipeProbeMain.cpp:119-140,213-228`, `tools/eq-copilot/pruefe_beweisrunner.py:145`.** Das Probewerkzeug `EqCopPipeProbe` steht nicht in `$zuBauen`; die Kreuzprobe führt es als „nicht im Kanon, nicht beurteilt" und das Inventar nimmt `eq-copilot/plugin/probe/` mit Grund aus (`"nur EqCopPipeProbe, ein Handwerkzeug ausserhalb des Kanons"`, im Rohlauf so ausgegeben). Die Einlösung von R-309-5 und R-309-3 *in `main`* ist damit nur durch die Quelltextwachen `probe_main_prueft_pipenamen_vor_dem_client` und `probe_main_wartet_auf_konfliktende_praedikat` (PipeClientLifecycleTestMain.cpp:1568-1596, 1774-1795) gehalten — sie lesen den Text, übersetzen ihn nicht. Ein Übersetzungsbruch in `PipeProbeMain.cpp` bliebe im Kanon unsichtbar. Gate, Matrix und Entwurf sagen dazu nichts (der Zustand ist als NAK-289 NB-1 und in §1 T3-13-01 ausdrücklich notiert), deshalb Lücke, keine Nacharbeit.

**H-1 (HÄRTUNG) — `tools/beweise.ps1:608`.** Die Behauptungszeile A17 nennt `[4]`, `[4b]`, `[5]`, `[6]`, aber nicht die Prüfungen, die das Bein seit diesem Ticket zusätzlich fährt: `[3e]` Bauordnung im Runner (`pruefe_installer_manifest.py:793-813`), `[4c+]` Brokerpin gegen die gebaute Datei und `[4d]` die Attrappe für `--hashen`/`--broker-pin`. Die Zeile bleibt unverändert, K5 („Behauptung ≤ Messung") hält also — sie behauptet weniger als sie misst. Wer nur die Übersichtstabelle eines Manifests liest, sieht die gemessene Bauordnung dort nicht. Keine Zusage verlangt die Ergänzung.

**H-2 (HÄRTUNG) — `docs/gesundheit/szenarien/snapshot-runde01.json:14`, `docs/gesundheit/szenarien/u40-aktivitaetsgate.json:10`; Beleg `C:\Users\phili\Projekte\fl-studio-mcp\fl_controller\device_FLStudioMCP.py:417,424`.** Der Controller antwortet auf `transport.setLoopMode` mit `{"mode": mode}`, wobei `mode = params.get("mode", "pattern")` — also mit dem *angeforderten* Wert, nicht mit dem gelesenen Zustand. Die neue Zeile `"erwarte": {"mode": {"gleich": "song"}}` ist damit ein Korrelationsbeleg im Sinn von R-309-2 („Szenariodateien fragen das Echo ab"), keine Zustandsmessung. In `bereitschaft.json:17` steht zusätzlich `transport.getStatus → loop_mode gleich song`, dort ist der Zustand belegt; in den beiden anderen Dateien nicht. R-309-2 verlangt nur das Echo, deshalb Härtung.

**H-3 (HÄRTUNG) — `tools/eq-copilot/pruefe_beweisrunner.py:631-642`.** Mit `--bau-bestaetigt` steht der Zweig `if bau_bestaetigt: eintrag["stand"] = STAND_BAU` **vor** `elif konf.ausstehend` und `elif fehlend`: eine ausstehende Konfiguration und eine fehlende Eingabe führen dann nicht zu `VERALTET`. Das ist die im Werkzeugkopf (Zeile 86-89) und im Runner (`tools/beweise.ps1:1031-1035`) ausdrücklich zugesagte Regel („der Zeitvergleich folgt dem Urteil des Buildsystems"), Kreuzprobe und Inventar laufen weiter und können in derselben Lage noch Exit 3 oder 4 erzwingen. Keine Zusage verlangt mehr.

**H-4 (HÄRTUNG) — `tools/fl/mcp-stand.json:8-19`.** Der Pin führt zehn Laufzeitdateien mit Blob-Hash; `tests/test_request_id.py` — die Datei des Nachbarrepos, die R-309-2 dort misst — steht nicht darunter. Sie ist nur über den `git status`-Riegel (`tools/fl/laufzeit.ps1:1014-1017`) gegen unbemerkte Änderung gedeckt, nicht über einen Hash. T3-13-06 verlangt den Pin für den Erstcode des MCP-Repos; die Tests nennt die Zusage nicht.

**Antworten auf die Prüffragen**

**(a) Zusammenspiel der Etappen.** Alle vier Zusagen der Etappe 2 halten am Endstand.
- *Frischeurteil je Prüfbinary aus den Tracking-Logs:* `pruefe_beweisrunner.py:343-363` (`fuer_binary`: Link-Schreiblog, CL- und Link-Leselog erforderlich), `:319-328` (Erzeuger einer Bibliothek), `:600-643` (Urteil je Eintrag gegen den **eigenen** Baum, `vergleich_ns = juengste_ns`). Übergeben wird die Population aus `tools/beweise.ps1:924-935`.
- *VERALTET verweigert die Beglaubigung:* `pruefe_beweisrunner.py:769-775` (Exit 4 bei `veraltet and not bau_bestaetigt` oder Lücken) → `tools/beweise.ps1:1046-1049` (`$veraltet = $true`) → `:1340-1343` (`$exitcode = 4`, „NICHT BEGLAUBIGT").
- *Nicht ableitbar ist nie grün:* `pruefe_beweisrunner.py:769-770` (Exit 3 vor Exit 4), `:557-558`, `:581-583`, `:590-594`, `:681-682`, `:690-692`, `:740-742` (jeder Ableitungsbruch setzt `nicht_ableitbar`) → `tools/beweise.ps1:1043-1045, 1050-1053` (`$baustandFehlt`) → `:1324-1327` (`$fehlendeVoraussetzung++`) → `:1335-1338` (Exit 3, „UNVOLLSTAENDIG"). Auch ein Werkzeug-Exit außerhalb {0,3,4} oder eine fehlende JSON-Zeile landet dort (`:1043-1046`).
- *Bauordnung Release-Broker → `--broker-pin` → C++-Bau:* `tools/beweise.ps1:907-919` (cargo `build --release` mit Abbruch, dann `pruefe_installer_manifest.py --broker-pin` mit Abbruch), `:945-948` (`cmake --build`). Gemessen von A17 `[3e]` mit zwei eingebauten Rotmutationen: `pruefe_installer_manifest.py:771-813`. `--hashen` fährt `[4c]`/`[4c+]` jetzt gegen die *geschriebene* Datei (`:1264-1292`).
- *Vorrang ROT → fehlende Voraussetzung → VERALTET:* `tools/beweise.ps1:1330-1346`, festgehalten von der Quelltextwache `urteilsvorrang` (`pruefe_beweisrunner.py:1455-1496`) mit zwei Gegenproben (`:1506-1513`). NOT RUN zählt nie als grün: die Grünzählung nimmt am Symbol (`tools/beweise.ps1:1313`, `$_.Symbol -in @('[OK]','[HINWEIS]')`), ein Bein mit NOT RUN endet mit Exit 0 und wird auf `[NOT RUN]` + `$fehlendeVoraussetzung++` gesetzt (`:1284-1290`); ein rotes Bein bleibt rot und nennt die Marken (`:1281-1283`); ein unlesbarer Meldeweg ist rot (`:1291-1296`).
- *Widerspricht sich eine Behauptungszeile mit ihrem Bein?* **Nein.** Geprüft an den in diesem Ticket geänderten Zeilen: A4 (`:493`) und A4-SI (`:501`) gegen `store_crash_matrix.rs:3229-3235, 3357-3363` (NOT-RUN-Marke mit Grund, `Meldeweg=$true` gesetzt) — passt; A4b (`:535`) gegen PipeClientLifecycleTestMain.cpp:1500-1792 — jede Teilzusage hat ihren Fall; A35 (`:642`) gegen `laufzeit.ps1` (Besitz 397-563, MCP-Pin 993-1026) und `szenario.py` (`_sende`/`_zuordnen` 691-724, Attrappenpaket-Fälle 3856-3901) — passt; A36 (`:645`) gegen `pruefe_beweisrunner.py` Fälle M-01…M-08, M-10, `--bau-bestaetigt`, M-67, M-70, M-75 (`:1517-1532`) — passt; B8 (`:1063`) gegen LebenslaufTestMain.cpp:323-381 — passt; B22 (`:749`): die alte Formel „alle drei Verlaeufe sind Energien ohne Phase" (die der Test mit `pruefe(true,…)` quittierte) ist ersetzt durch „der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich", und genau das misst Sonde013FingerprintGoldenTest.cpp:264-319 samt Gegenfall (M-72) und zwei Rändern — der K5-Bruch T3-09-05 ist damit geschlossen. Einzige Abweichung in die unkritische Richtung: A17 (siehe H-1).

**(b) Gate Satz für Satz — je Befund die tragende Stelle am Endstand**

- **T3-09-01** (DEFEKT, geschlossen): `tools/eq-copilot/pruefe_beweisrunner.py:343-363` + `eq-copilot/plugin/CMakeLists.txt:1388-1434` (CMake-Export) + `tools/beweise.ps1:1004-1070` (handgepflegte `$quellOrte` entfernt); Bein A36 `tools/beweise.ps1:645`.
- **T3-05-03** (DEFEKT, geschlossen): `tools/beweise.ps1:907-919` (Broker → Pin → Bau) und `tools/eq-copilot/pruefe_installer_manifest.py:1264-1292` (`--hashen` fährt `[4c]`/`[4c+]` gegen die geschriebene Datei), `:1294-1330` (`--broker-pin`, idempotent, nur das Feld `sha256`).
- **T3-13-01** (DEFEKT, geschlossen): `eq-copilot/plugin/src/ProbePipeRegel.h:57-86` (Erlaubnisliste, Produktionsname case-insensitiv, Trenner hinter `\\.\pipe\` = fremd), `eq-copilot/plugin/probe/PipeProbeMain.cpp:126-137` (Urteil vor Servererwartung und erstem Client, Exit 64), `eq-copilot/plugin/src/PipeClient.cpp:221` (Rückfall auf `kPipeName` entfernt) und `:237-244` (leerer Name startet keinen Thread). Nebenbefund NB-1: `eq-copilot/README.md:57-70`.
- **T3-13-02** (DEFEKT, geschlossen): `tools/fl/laufzeit.ps1:498-512` (`Besitz-Urteil`: PID **und** UTC-Startzeit **und** Befehlszeile aus `Win32_Process`, dazu Projekt der Arbeitskopie und Titelprobe) und `:528-535` (`Beende-Eigenen` — die Prüfung sitzt am Beenden selbst); `:276-284` liest Startzeit und Befehlszeile, `:473-485` räumt veraltete Einträge ab.
- **T3-13-03** (LÜCKE, geschlossen): `tools/fl/szenario.py:691-724` (`_sende`/`_zuordnen`: neue Kennung je Versuch, jede fremde oder fehlende Kennung = UNGEMESSEN und gezählt) und `:317-319` (Bibliothek ohne `request_id` = Voraussetzung fehlt); Gegenstück im Nachbarrepo `src/fl_studio_mcp/utils/midi_connection.py:281-327` (absolute Frist, verspätete Antwort wird übersprungen ohne die Datei zu löschen) und `fl_controller/device_FLStudioMCP.py:154,167-168` (Echo in Erfolg und Fehler).
- **T3-13-04** (LÜCKE, geschlossen): `eq-copilot/plugin/src/ProbePipeRegel.h:91-99` (`konfliktEndeBestaetigt`: gleiche Versuchszahl, gestiegener ACK-Zähler, kein Konflikt) und `eq-copilot/plugin/probe/PipeProbeMain.cpp:213-228`.
- **T3-13-05** (DEFEKT, geschlossen): `tools/fl/szenario.py:254-279` (`lade_mcp_modul`: Stellvertreter mit echtem `__path__`, danach Wache über `sys.modules`) und `:306-310`; gemessen über ein Attrappenpaket in `:3856-3901`.
- **T3-13-06** (HÄRTUNG, erledigt): `tools/fl/mcp-stand.json` (Revision `15b3b1a1…`, zehn Blob-Hashes) gelesen in `tools/fl/laufzeit.ps1:993-1026` vor jedem Lauf, Abweichung = Exit 3. Selbst nachgemessen: Revision und die drei geänderten Blob-Hashes stimmen mit dem Nachbarrepo-HEAD überein.
- **T3-02-07** (HÄRTUNG, als Beweis nachgezogen): `eq-copilot/fixtures/state/schema2/main-binding-v1.bin` (neu, 256 B), `eq-copilot/fixtures/state/MANIFEST.json:426-430`, `eq-copilot/plugin/tests/StateMigrationTestMain.cpp:1132-1186` (Leser M-57 am Bytebild von der Platte, Schreiber M-58 getrennt), `tools/eq-copilot/erzeuge_state_fixtures.py:1084-1090` und `:1137-1154` (Waisenprüfung nun auch über `schema2/`).
- **T3-03-11** (HÄRTUNG, erlaubtes Verhalten gemessen): `eq-copilot/plugin/tests/IpcTestMain.cpp:4991-5057` (Control M-62/M-63: Minor 5 verbindet, Minor 6 verbindet auch, der erste Frame danach schließt mit `envelopeAbweisungen +1`) und `:4732-4792` (Telemetrie M-64/M-65: Minor 6 koppelt, der P2-Frame erreicht `beiFrame` unverändert); Attrappenseite `eq-copilot/plugin/tests/V3TestServer.h:72-79, 427-429, 470-485`.
- **T3-04-04** (HÄRTUNG, erlaubtes Verhalten gemessen): `broker/src/briefkasten.rs:1808-1895` (`nach_join_frist_endet_der_begonnene_schreibschritt_genau_einmal`: `join_frist_verfehlt == 1`, genau ein fertiger Takt, 0 Existenzprüfungen nach dem Stopp, genau eine Antwortdatei).
- **T3-09-04** (DEFEKT, geschlossen): `broker/tests/store_crash_matrix.rs:3448-3533` (`melde_nicht_gelaufen`/`nicht_gelaufen` mit eigenem Test M-68), `:3229-3235` und `:3357-3363` (die zwei Skips melden jetzt); Runnerseite `tools/beweise.ps1:1137-1147` (frischer Meldeordner nur in der Kindumgebung) und `:1261-1299`; Leser `tools/eq-copilot/pruefe_beweisrunner.py:817-886`.
- **T3-09-05** (DEFEKT, geschlossen): `eq-copilot/plugin/tests/Sonde013FingerprintGoldenTest.cpp:264-319` (`pruefe(true,…)` ersetzt durch M-71 Vorzeichenumkehr bytegleich, M-72 Gegenfall, zwei Ränder) und die berichtigte Behauptungszeile `tools/beweise.ps1:749`.

**(c) GRENZE des Gates.** Kein Produktverhalten außer der Bauordnung.
- *Produktbau nennt genau den Produktionsnamen:* `eq-copilot/plugin/src/PluginProcessor.cpp:86-98` — der `#else`-Zweig liefert `juce::String (juce::CharPointer_UTF16 (kPipeName))`, byte-für-byte das, was der entfernte Rückfall in `PipeClient.cpp:221` lieferte. Gesetzt in `produktVerdrahtung()` (`:110-118`), durchgereicht an den v2-Client (`:171-178`).
- *Probe-Name hängt nur am Testschalter:* die Wahl ist eine Übersetzungszeitentscheidung `#if defined(NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` (`:95`), keine Laufzeitbedingung. Ich habe alle Konstruktionsstellen von `V3Verdrahtung` aufgezählt (nur `PluginProcessor.cpp:110` und `:128`, beide setzen `v2PipeName`), der private Konstruktor `:151` ist die einzige Senke, `EqCopilotProcessor(V3Verdrahtung)` ist privat.
- *Kein Test/Probewerkzeug/Selbsttest öffnet die Produktions-Pipe:* `${NAKAMA_PROZESSOR_QUELLEN}` steht an 15 Stellen in `eq-copilot/plugin/CMakeLists.txt` (111, 135, 460, 531, 566, 630, 661, 681, 698, 763, 784, 848, 883, 915, 944, 1123); außer `EqCopilot` (:134) trägt jedes dieser Ziele `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3=1` — 14 über die Schleife `:1322-1329`, `EqCopSonde013PassageStateTest` zusätzlich `:869`, die Sondenfunktionen `:409/:439`, `EqCopSessionSoak` `:495`, `EqCopTransactionTest` `:1173`; `EqCopilot` steht in keiner dieser Listen. Ich habe das selbst nachgezählt; dieselbe Aussage hält die Quelltextwache `LebenslaufTestMain.cpp:258-310, 353-380` am Anker `CMakeLists.txt:1318`. Der v2-Client wird im Konstruktor unbedingt gestartet (`PluginProcessor.cpp:463`), die v3-Clients und der Briefkasten nur im Produktzweig (`:464-470`) — im Testbau bekommt der v2-Client `\\.\pipe\evenacadia.eq-copilot.m2probe.testbau`, den `probePipeUrteil` als `zugelassen` einstuft und auf dem kein Server lauscht. Das Probewerkzeug lehnt jeden Nicht-Probenamen vor Servererwartung und erstem Client mit Exit 64 ab (`PipeProbeMain.cpp:126-137`); die Erlaubnisliste ist dicht — keiner der zugelassenen Namen kann unter der Win32-Pfadnormalisierung auf den Produktionsnamen zurückfallen, weil Trenner ausgeschlossen sind (`ProbePipeRegel.h:70-72`) und die erlaubte Suffixmenge keinen Punkt enthält (`:81`). A36 arbeitet nur auf Temp-Attrappen, der Selbsttest von `laufzeit.ps1` verlässt den Einstieg vor `Neue-Umgebung` (`:2256-2259` gegen `:2267`) und benutzt die Attrappenumgebung mit eigenem `BeendeProzess` (`:1155`, `:1276`).
- *Beendet `laufzeit.ps1` an jedem Ausgang nur Eigenes?* **Ja.** Im ganzen Skript gibt es genau ein `Stop-Process` (`:275`), erreichbar nur über `& $script:U.BeendeProzess` an einer einzigen Stelle: `Beende-Eigenen` (`:533`), und dort steht `Besitz-Urteil` davor (`:531-532`). Alle drei Aufrufer (`Beende-Diagnose-FL` :549, `Beende-Restprozesse` :556, `Rendere` :780) gehen darüber; das Urteil vergleicht PID, UTC-Startzeit *und* Befehlszeile gegen die Besitzliste und verlangt zusätzlich ein Projekt der Arbeitskopie dieses Laufs sowie (bei Zweck `fl`) einen passenden Fenstertitel. Eine unlesbare Besitzliste gilt als leer — nichts ist eigen (`:424-434`).

**(d) M-73.** Selbst gemessen: `git diff --stat 4c1c7f3c8cb5145de0e545aebe6305e450f00f7c...HEAD -- eq-copilot/identity eq-copilot/schemas eq-copilot/plugin/dsp eq-copilot/plugin/core/ipc/PipeToken.h eq-copilot/plugin/core/ipc/PipeToken.cpp` → **leere Ausgabe**. Ergänzend selbst gemessen: der vollständige Nicht-`docs`-Diff seit BASIS umfasst exakt die 28 Auftragspfade, es gibt also auch außerhalb der genannten fünf Orte keine ungenannte Produktänderung. Identität, Schemas, DSP-Kern und der Pipetoken sind unberührt; Passthrough/Nulltest, State-Verlustfreiheit und Audio-Thread habe ich **nicht selbst gemessen** — A1, A14, A16, B1, B2 stehen im Rohkanon `NAK-309-dca4640-dirty.md` mit `[OK] Exit 0`, der Host-Nulltest `BITIDENTISCH` steht in §21 des Manifests (beides gelesen, nicht nachgemessen).

HEAD vor dem Lauf: `4a7c08e0371f25869fc3e4e41b28b4b01ff3c7bb`
HEAD nach dem Lauf: `4a7c08e0371f25869fc3e4e41b28b4b01ff3c7bb`
