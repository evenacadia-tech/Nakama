# NAK-289 — S25g: C++-Statikanalyse: die clang-tidy-Fundstellen abbauen (Ratsche auf 0)

| Merkmal | Wert |
|---|---|
| Ticket | NAK-289 (Register `docs/offene-punkte.md`), Planschritt **S25g** (`docs/plan/plan.json`), Prüfstufe **T2**. Verhaltensneutraler Pflegeschritt nach Muster S25c/S25d (NAK-224, NAK-225; Riegelkarte `docs/beweise/NAK-225.md` §3). |
| Etappe | Etappe 1 von 3 (Kern- und Werkzeugpfad: `bugprone-exception-escape`, `clang-analyzer-core.NullDereference`, `clang-analyzer-core.NonNullParamChecker`, `clang-analyzer-deadcode.DeadStores`, `clang-analyzer-optin.core.EnumCastOutOfRange`, `bugprone-incorrect-roundings`, `cert-err34-c`, `bugprone-inc-dec-in-conditions`, `performance-avoid-endl`; Auftrag `docs/beweise/roh/NAK-289-etappe-1-auftrag.txt`). **Kanon vorher läuft auf dem Basisstand 432f209c.** |
| Phase | Etappe 1 im Bau (Worker, 15.09.2026). Die Etappen 2 und 3 folgen je als eigener Worker; dazwischen misst der Dirigent. |
| Prüfstufe | T2 am Ticketende durch den Dirigenten. In dieser Etappe kein Urteil und keine Urteilsmarke. |
| Basis-SHA | `432f209caf787d818790b6483bc769d54dd3912d` (= `git rev-parse HEAD` = `origin/master` beim Start, in dieser Session gemessen; `git status --short` zeigte nur die fremden Ordner `briefing-hub/`, `nimbalyst-local/` und die Auftragsdatei; kein fremder Bau- oder Testprozess). Der Codestand weicht vom letzten grünen Kanon `d6382571` ab (`git diff --stat d6382571..432f209c -- eq-copilot broker tools`: 8 Dateien, +725/−39 — drei FL-Fixtures unter `eq-copilot/fixtures/fl/`, `eq-copilot/install/nakama-installer-v1.json`, vier Dateien unter `tools/fl/`), deshalb kein geerbter Kanon. |
| Kanon vorher | läuft (§3 R1). |
| Kanon nachher | steht aus (§3 R1). |
| Testbestand | steht aus: Beinzahl und Prüfungszahlen je Bein vorher gegen nachher (§3 R4). |
| Ticketpfade | Die Quelldateien, in denen die frische Messung Fundstellen der Etappe-1-Checks meldet (Iststand 12.09.2026: `eq-copilot/plugin/core/analysis/TruePeak.h`, `core/ipc/controlclient/Vertrag.cpp`, `core/ipc/IpcQueues.h`, `core/ipc/PipeToken.h`, `core/ipc/WireEnvelope.cpp`, `core/ipc/WireZahl.h`, `dsp/DspKern.cpp`, `probe/PipeProbeMain.cpp`, `vertrag/NakamaTelemetrie.cpp`, `vertrag/NakamaUtf8.h`, `vertrag/NakamaVertrag.cpp`); `tools/plan/tidy.py` (nur `TIDY_GRENZE`, ihr Kommentar und „HERKUNFT DER SCHWELLE"); `eq-copilot/plugin/tests/**` nur für Regressionstests nach Auftrag Punkt 4; `docs/beweise/NAK-289.md`; `docs/beweise/roh/NAK-289-*`. Wie gebaut: steht aus. |
| Nicht angefasst (Grenze) | `eq-copilot/plugin/.clang-tidy`, `tools/beweise.ps1`, `eq-copilot/identity/**`, `eq-copilot/schemas/**`, `eq-copilot/fixtures/**`, `eq-copilot/install/**`, `eq-copilot/plugin/CMakeLists.txt`, `broker/**`, `tools/**` außer `tidy.py`, `docs/**` außer Manifest und Rohdaten, `.claude/**`, `design/**`, die fremden untracked Ordner. Fundstellen der Etappen 2 und 3 bleiben unberührt, auch in denselben Dateien. Gemessen: steht aus (§3 R5). |
| Änderungssatz | Verhaltensneutral; je Fundklasse a) bis h) ein Commit mit Pathspec; Ratsche ↔ Vollmessung ↔ Manifest im Commit nach der Vollmessung (Auftrag Punkt 5). |
| Prüfmodell | T1 durch den Erbauer (Teilmessung und Beine je Klasse, Rotbeweis je Klasse mit Verhaltensrisiko, Vollmessung vorher und nachher, Kanon vorher und nachher, Selbstaudit nach `tools/dirigent/pruefliste.md`); Messung der Etappe durch den Dirigenten; T2 am Ticketende. |
| Urteil | steht aus (Etappe 1 im Bau). |

---

## 1. Ticket und Register, wörtlich

**Ticket** (`docs/plan/plan.json`, Schritt S25g, Ticket NAK-289, Stufe T2), Feld `text`:

> ANGELEGT 13.09.2026 (Register NAK-289; Übergabe der NAK-288-Session vom
> 13.09.2026 an den Dirigenten: „eigener verhaltensneutraler Pflegeschritt im
> Plan, kein Rest dieses Auftrags“; Codebase-Gesundheit ist Planbestandteil,
> User 08.09.2026). INHALT: die 83 Fundstellen des Beins A33
> (`tools/plan/tidy.py`, Iststand `docs/beweise/roh/NAK-288-tidy-iststand-83.txt`,
> Regelsatz `eq-copilot/plugin/.clang-tidy`) verhaltensneutral abbauen und die
> Ratsche in Teilstücken senken, bis sie auf 0 steht; je Teilstück voller Kanon
> vorher und nachher (Muster S25c/S25d), Nulltest, Goldens und State-Fixtures
> bytegleich, Identität unberührt. GRENZE: keine Regel im `.clang-tidy` zum
> Wegdrücken einer Fundstelle ohne datierten Grund im Manifest, kein
> Produktverhalten, keine Legacy-Umbenennung (NAK-30), keine Vermischung mit
> NAK-292 (Gesundheitsrisse aus dem Quellinventar, S31c). BEWEIS:
> `docs/beweise/NAK-289.md` mit Fundstellen je Teilstück vorher/nachher,
> A33-Rohausgaben, Kanon vorher und nachher GRÜN.

**Register** (`docs/offene-punkte.md`, Zeile NAK-289), Inhaltszelle:

> **[Lücke · Pflege · S25f]** **83 clang-tidy-Fundstellen im Plugin (Ratsche 83
> in `tools/plan/tidy.py`, Ziel 0).** Verteilung: 17
> `bugprone-misplaced-widening-cast`, 17
> `performance-unnecessary-copy-initialization`, 9 `performance-avoid-endl`, 7
> `bugprone-exception-escape` (noexcept-Funktionen mit werfenden Aufrufen, u. a.
> `TruePeak.h:96`, `IpcQueues.h:414`), 7
> `bugprone-implicit-widening-of-multiplication-result`, 5
> `bugprone-suspicious-memory-comparison` (`memcmp` über `double` in
> `NakamaParameter.cpp`, `NakamaTransaktion.cpp`), 5
> `cppcoreguidelines-pro-type-member-init`, 3 `performance-move-const-arg`, je 2
> `bugprone-branch-clone`, `clang-analyzer-core.NonNullParamChecker`,
> `clang-analyzer-optin.core.EnumCastOutOfRange`,
> `performance-unnecessary-value-param`, je 1 `bugprone-inc-dec-in-conditions`,
> `bugprone-incorrect-roundings`, `cert-err34-c`,
> `clang-analyzer-core.NullDereference` (`dsp/DspKern.cpp:466`, durch den
> double-Vergleich `t < 1.0` faktisch geschützt, Härtung: expliziter Nullcheck)
> und `clang-analyzer-deadcode.DeadStores`. Dateien: `state/NakamaState.cpp` 19,
> `probe/PipeProbeMain.cpp` 10, `core/analysis/featureengine/Spektrum.h` 6.
> Bearbeitung als verhaltensneutraler Pflegeschritt (Muster S25c/S25d: Nulltest
> bitidentisch, Passthrough sampleidentisch, Kanon vorher = nachher, Ratsche im
> selben Ticket senken); die `exception-escape`-Stellen auf dem Audio-Pfad
> zuerst. Geschlossen, wenn die Ratsche 0 steht.

**Auftrag der Etappe 1** (Dirigent, 15.09.2026): `docs/beweise/roh/NAK-289-etappe-1-auftrag.txt`
— Rolle, Teilstücke, Ticketpfade, Regeln je Fundklasse a) bis h), Invarianten,
Baufolge 1 bis 8.

## 2. Teilstückplan

Technikentscheid des Dirigenten (Auftrag, Abschnitt TEILSTÜCKE): drei Etappen,
je eine ein eigener Worker, dazwischen misst der Dirigent. Zahlen nach dem
Iststand vom 12.09.2026 (`docs/beweise/roh/NAK-288-tidy-iststand-83.txt`;
Zeilennummern dort sind Verlauf, jede Etappe misst frisch).

| Etappe | Checks (Fundstellen) | Summe | Ratsche danach (Erwartung) |
|---|---|---:|---:|
| 1 (diese) | `bugprone-exception-escape` 7, `clang-analyzer-core.NullDereference` 1, `clang-analyzer-core.NonNullParamChecker` 2, `clang-analyzer-deadcode.DeadStores` 1, `clang-analyzer-optin.core.EnumCastOutOfRange` 2, `bugprone-incorrect-roundings` 1, `cert-err34-c` 1, `bugprone-inc-dec-in-conditions` 1, `performance-avoid-endl` 9 | 25 | 58 |
| 2 | `bugprone-misplaced-widening-cast` 17, `bugprone-implicit-widening-of-multiplication-result` 7 | 24 | 34 |
| 3 | `bugprone-suspicious-memory-comparison` 5, `cppcoreguidelines-pro-type-member-init` 5, `performance-unnecessary-copy-initialization` 17, `performance-move-const-arg` 3, `performance-unnecessary-value-param` 2, `bugprone-branch-clone` 2 | 34 | 0 |

Regeln der Aufteilung: Eine Fundstelle einer anderen Etappe wird nicht
angefasst, auch wenn sie in derselben Datei liegt. Meldet die frische Messung
eine Fundstelle eines Etappe-1-Checks, die im Iststand vom 12.09. fehlt, gehört
sie zu Etappe 1; meldet sie einen Check, der in keiner Etappe steht, wird er
hier als offener Punkt geführt und liegen gelassen. Reihenfolge in Etappe 1:
Fundklassen a) bis h) des Auftrags, je Klasse ein Commit.

## 3. Riegelkarte R1–R10

Jede Zeile mit Befehl, Wert am Basis-SHA und Wert am Endstand der Etappe; jeder
Wert aus einem Lauf dieser Session, jeder Rohbeleg unter `docs/beweise/roh/`.

| Riegel | Befehl | Vorher (Basis `432f209c`) | Nachher (Endstand Etappe 1) |
|---|---|---|---|
| R1 Kanon vorher auf dem Basisstand | `pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-289.md -Anhaengen -Titel NAK-289-Kanon-vorher` (abgekoppelt; nachher Titel `NAK-289-Etappe-1`) | läuft | steht aus |
| R2 Nulltest bitidentisch, Passthrough sampleidentisch | Beine A1 `EqCopNullTest`, A16 `EqCopProbeeqNullTest`, B3b `EqCopHostProbeTest`, B4 `EqCopQueueStressTest` (verdrahteter Passthrough), B6 `EqCopDspGoldenTest` (ausgeschaltet und hard-bypassed kein Sample geschrieben, 0 Allokationen und 0 Sperren im Callback) — Exit und Prüfungszahl aus den Kanon-Rohausgaben | steht aus | steht aus |
| R3 Goldens und State-Fixtures bytegleich | Beine A2 `EqCopGoldenTest`, A5 `pruefe_v3_vertrag.py`, A8 `erzeuge_v3_fixtures.py --pruefen`, A10 `erzeuge_fb_fixtures.py --pruefen`, A12 `erzeuge_state_fixtures.py --pruefen`, A20 `erzeuge_envelope_fixtures.py --pruefen`, B2 `EqCopStateMigrationTest`, B5 `EqCopAnalysisGoldenTest`, B6, B17 `EqCopSonde013TruePeakGoldenTest`; dazu `git diff --stat 432f209c..HEAD -- eq-copilot/fixtures eq-copilot/schemas` | steht aus | steht aus |
| R4 Zielmenge und Beinzahlen unverändert | Beinzahl und ok-Zeilen je Bein aus den Kanon-Rohausgaben; `git diff --stat 432f209c..HEAD -- eq-copilot/plugin/CMakeLists.txt` (kein neues Ziel) | steht aus | steht aus |
| R5 nicht angefasste Pfade | `git diff --stat 432f209c..HEAD -- <Bereich>` je Bereich der Kopfzeile „Nicht angefasst" | — | steht aus |
| R6 A33 je Check und je Datei | `py -3.13 tools/plan/tidy.py` (Rohdateien `roh/NAK-289-tidy-vorher-432f209c.txt`, `roh/NAK-289-tidy-etappe1-nachher.txt`) | steht aus (Iststand 12.09.2026: 83) | steht aus |
| R7 `.clang-tidy` unverändert | `git diff 432f209c..HEAD -- eq-copilot/plugin/.clang-tidy` | — | steht aus (Soll: leer) |
| R8 NOLINT-Ausnahmen | `git grep -n -I NOLINT <stand> -- eq-copilot/plugin` | **0 Zeilen** (15.09.2026 gemessen) | steht aus (Soll: 0, jede Ausnahme mit Manifestzeile) |
| R9 `identity/` unberührt | `git diff --stat 432f209c..HEAD -- eq-copilot/identity` | — | steht aus (Soll: leer) |
| R10 keine neue Fundstelle eines anderen Checks | Vollmessung nachher ohne Etappe-1-Checks gegen vorher, zugeordnet über Datei und Check (Zeilen verschieben sich durch den Umbau) | steht aus | steht aus |

## 4. Etappe 1

Im Bau. Folgt: Vollmessung vorher, Einordnung je Fundstelle, Behebung je
Fundklasse a) bis h), Beine, Rotbeweise, Vollmessung nachher, NOLINT-Liste,
Abweichungen, Selbstaudit, Nebenbefunde, offene Punkte für Etappe 2 und 3.
