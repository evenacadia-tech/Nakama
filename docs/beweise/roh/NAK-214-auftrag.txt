NAK-214 — G5-Nacharbeit 3: Vertragsvollständigkeit im Datenweg. Ticketauftrag des Dirigenten (vorbereitet 2026-09-08 nach NAK-213 T3 PASS, zu starten von der nächsten Dirigenten-Session).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-214.md ein. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors, keine Legacy-Umbenennung. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits und Manifest Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks (CLAUDE.md, Maschinen-Landminen).

TICKETGRENZE — genau die Befunde des Gates G5 (docs/beweise/G5.md §1, §7, §8, §11 Nr. 3), die §11 Nr. 3 diesem Ticket zuordnet: E-D5 (= A6), E-D6, E-L2, E-L5, A5, A6. Ihre gemeinsame Ursache: der Datenweg vom Befund zum Proposal, zur Gen-Anzeige und zurück (user_verdict) setzt Vertragszusagen nicht produktiv durch — Exit-Gate Satz 2 (Entwurf §59, docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324: „jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg"), M-17 (SONDE-014 §3: „Aus paralleler Telemetrie allein entsteht nie Klasse 2 oder 3 … 2. Wirkungsbeleg (PRE/POST-Paar mit reproduzierbarer Veränderung)"), Exit-Gate Satz 1 auf der Gen-Seite („Jede sichtbare Behauptung referenziert existente Evidenz-IDs"), und die Invariante „Schemas sind Verträge". Konkret (Stand bf92891, seither verschoben — an der Quelle nachlesen): `proposal_verdrahtung.rs` nimmt `unwrap_or_default()` auf `fuehrendes_main` (heute :119), ein Proposal trägt `target: ""` und wird persistiert; `gate_felder_vollstaendig` (`proposal.rs`:360) hat keinen Produktivaufrufer; `hypothese_verdrahtung.rs` setzt `prepost_paar` aus `ergebnis.is_some()` (heute :243–260), und `prepost.rs` liefert `Some` auch für `Probable`, sodass `claim_class = wirkungsbeleg` ohne `AudioAligned`-Paar entsteht; Gen (`SourcesModel.cpp` `controlEnde` :472 und die Befundzustände) behält nach einer Rücknahme ohne Folge-Snapshot (Pipe-Abbruch) einen READY-Befund; `befehl.rs` persistiert `user_verdict` mit einer `finding_id`, die im Broker nicht existiert (heute um :562); `experiment.rs` `juengste_passage_im_projekt` wählt die jüngste Passage ohne Filter (zurückgenommen, getaintet, Vergleichbarkeit unbekannt).
ACHTUNG: NAK-212 (T3 PASS 08.09.2026, docs/beweise/NAK-212.md) und NAK-213 (T3 PASS 08.09.2026, docs/beweise/NAK-213.md) haben `hypothese.rs`, `hypothese_verdrahtung.rs`, `invalidierung_verdrahtung.rs`, `liveness.rs`, den Vertrag (Fassung 4: `screening_ueberboten`, `master_duplikat`, `ausschluesse.maxItems` = 64, fassungsabhängiger C++-Leser) und den Korpus umgebaut. Lies zuerst NAK-213 §2 (E1–E9, K-01 bis K-55, R6–R8) und NAK-212 §2, dann die aktuellen Zeilen an der Quelle. Der Master der Aufnahme ist seit NAK-213 das führende Main (`fuehrendes_main`), ohne eindeutige Führung entsteht eine Enthaltung ohne Ort — R1 dieses Tickets („ohne führendes Main kein Proposal") baut darauf auf. Die in NAK-212/213 als NAK-214 gedruckten Lücken (broker/tests/sonde014_gegenbeispiele.rs:30 und :470, tools/eq-copilot/pruefe_p5_korpus.py:335 `offene_luecken`, eq-copilot/fixtures/p5-korpus/MANIFEST.json `nicht_gemessen` mit ticket NAK-214) sind deine Abnahmefälle: nach diesem Ticket geben sie die erwartete Ausgabe aus und keine Lücke mehr; `offene_luecken` meldet für NAK-214 eine leere Liste.

ROHBELEGE (lesen, nicht abschreiben): docs/beweise/roh/G5-eigener-lauf-bf92891.md (D5, D6, L2, L5, B6), docs/beweise/roh/G5-codex-astra-falsifikation-bf92891.txt (A5, A6), Quellencheck-Einordnung in docs/beweise/G5.md §8 (Zeilen 98–101) und die Gate-Satz-Tabelle §7 (Zeilen 59–60). Referenz: docs/beweise/SONDE-014.md §3 (M-17, M-29 bis M-35 Befundzustände, M-42 Proposal-Feldmenge, M-46, M-77), docs/beweise/SONDE-013.md (PRE/POST-Klassen `AudioAligned`/`Probable`, Experimente, Taint), design/abnahmen (Abnahme U21: Befundzustände, Alternativen als eigene Befunde), Entwurf §36.1 (Aussageklassen), §59 (Exit-Gate, Zeilen 4322–4324). Vertrag: eq-copilot/schemas/v3/eq-ipc-v3.schema.json ($defs/proposal — `target` hex32, die sechs Gate-Felder; session_finding; user_verdict; evidence_invalidate), eq-copilot/schemas/v3/README.md (Fassungsleiter; nach NAK-213 steht `aktuell` auf 4), tools/eq-copilot/pruefe_v3_vertrag.py, erzeuge_v3_fixtures.py.

BETROFFENE PFADE: broker/src/coordinator/proposal_verdrahtung.rs und proposal.rs (R1: `gate_felder_vollstaendig` als produktiver Riegel vor Persistenz und Zustellung; kein Proposal ohne führendes Main), broker/src/coordinator/hypothese_verdrahtung.rs NUR die Paarnaht `prepost_paar` (R2), broker/src/coordinator/prepost.rs nur lesend oder für die Klassenmeldung (R2), broker/src/coordinator/befehl.rs nur `user_verdict` (R4), broker/src/coordinator/experiment.rs nur `juengste_passage_im_projekt` und die Passagenwahl der Aufnahme (R5), eq-copilot/plugin/src/SourcesModel.cpp (R3: `controlEnde`, Sitzungswechsel, Rücknahme ohne Folge-Snapshot → alle Befunde `stale`, nicht handelbar) samt Header, broker/tests/sonde014_proposal.rs, sonde014_befund.rs, sonde014_gegenbeispiele.rs, sonde013_prepost.rs und sonde013_experiment.rs (nur neue Fälle), eq-copilot/plugin/tests/Sonde014BefundTest.cpp und Sonde012SourcesModelTest.cpp, tools/eq-copilot/pruefe_p5_korpus.py und erzeuge_p5_korpus.py sowie eq-copilot/fixtures/p5-korpus/ nur für die NAK-214-Lücken, eq-copilot/fixtures/v3/ und die Vertragswerkzeuge nur, falls R4 oder R1 eine Vertragsfassung braucht (dann versioniert, Fassungsleiter, Rückbauprobe, alle Spiegel — Muster NAK-213 E9/R7), tools/beweise.ps1 nur bei neuem Bein. NICHT anfassen: Klassenwahl, Zusammenhang, Screening, Deckel, Konkurrenzlage, Master-Wahl, Kanaltafel, Kanalwechsel (NAK-212/213 — nur nutzen); mitgliedschaft.rs; Audio- und State-Pfade des Plugins; die Anzeige-Gestaltung in Gen (S31b — hier nur der Zustand `stale`/nicht handelbar, keine neue Optik); design/, docs/plan/, docs/offene-punkte.md, docs/PLAN-STAND.md. Nebenbefunde außerhalb der Grenze nummeriert ins Manifest, nie ins Register.

REGELN, die die Befunde schließen (Technikentscheid des Dirigenten vom 07.09.2026, wörtlich aus docs/beweise/G5.md §11 Nr. 3; Feinheiten entscheidet deine Verhaltensmatrix, die Codex vor dem Bau prüft):
R1 — Kein Proposal entsteht oder wird persistiert ohne die sechs Felder des Exit-Gates; `gate_felder_vollstaendig` ist produktiver Riegel vor Persistenz und Zustellung, und ohne führendes Main gibt es kein Proposal.
R2 — `claim_class = wirkungsbeleg` entsteht nur aus einem PRE/POST-Paar der Klasse `AudioAligned` mit gemessener Wirkung (M-17); ein Paar darunter bleibt Klasse 1, und `next_test` verlangt das Paar weiterhin.
R3 — Verbindungsende (`controlEnde`) und Sitzungswechsel setzen in Gen alle Befunde auf `stale` (nicht handelbar); eine Rücknahme ohne Folge-Snapshot lässt keinen READY-Befund stehen.
R4 — `user_verdict` mit einer `finding_id`, die im Broker nicht existiert, wird abgewiesen (`reject`), nicht persistiert.
R5 — Die Passage einer Aufnahme ist die jüngste Passage des Projekts, deren Experiment nicht zurückgenommen und nicht getaintet ist und deren Vergleichbarkeit nicht unbekannt ist; sonst rechnet die Aufnahme ohne Passage.

SPEZIFIKATION VOR CODE (Etappe 1): Zuerst die Verhaltensmatrix in docs/beweise/NAK-214.md §2: Zustände × Ereignisse × Zusage, je Zeile der Test (Datei::Fall) und der Rotbeweis — mindestens: Proposal bei eindeutigem Main, bei zwei Mains (`fuehrendes_main = None`, seit NAK-213 Enthaltung ohne Ort — was tut der Proposal-Weg mit einer Enthaltung?), bei Main-Rückzug zwischen Befund und Proposal, mit jedem der sechs Gate-Felder einzeln fehlend (Ziel, Passage, Grenzen, Hörziel, Stopbedingung, Rückweg) — wo greift der Riegel, was wird nicht persistiert, was nicht zugestellt, was sieht Gen; Klassenwahl `wirkungsbeleg` aus `AudioAligned` mit Wirkung, aus `AudioAligned` ohne gemessene Wirkung, aus `Probable`, ohne Paar — je Klasse und `next_test`; Gen bei `controlEnde`, bei Sitzungswechsel (neue `session_epoch`), bei `evidence_invalidate` ohne Folge-Snapshot, bei Reconnect mit Folge-Snapshot (Rückweg aus `stale`); `user_verdict` mit existenter, mit nie existenter, mit zurückgenommener und mit `stale` `finding_id` — Antwort (`reject`/Grund), Persistenz, Idempotenz; Passagenwahl mit jüngster Passage gültig, zurückgenommen, getaintet, Vergleichbarkeit unbekannt, ohne Passage — welche Passage rechnet, wann rechnet die Aufnahme ohne Passage, Zusammenspiel mit NAK-212 E5/N-43 (Master-Belege vor der Passage); die NAK-214-Lücken aus Korpus und A31 mit erwarteter Ausgabe; welche Zeilen aus SONDE-014 §3, NAK-212 §2 und NAK-213 §2 unverändert gelten und welche dieses Ticket präzisiert; ob und welche Vertragsfassung nötig ist (R4-Antwortform, R1-Riegelmeldung) mit Fassungsleiter, Fixtures, Negativfixture und den Spiegeln in C++ und Python (M-77). Was die Matrix nicht spezifiziert, wird nicht gebaut. Kopftabelle wie in docs/beweise/NAK-213.md (Ticket, Phase, Basis-SHA, Kanon vorher = Abschlusskanon von NAK-213 GRÜN 61/61 auf `d3c6709` mit `git diff --stat d3c6709..HEAD -- broker eq-copilot tools` leer, Ticketpfade, Grenze, Prüfmodell Codex gpt-6-astra max). Etappe 1 endet mit dem Commit des Manifests (Pathspec docs/beweise/NAK-214.md) und Push; danach STOPP — die Matrixprüfung durch Codex und die Fortsetzung setzt der Dirigent. Kein Produktcode, kein Test, keine Fixture in Etappe 1.

BAU (Etappe 2, erst nach Fortsetzung durch den Dirigenten): Broker, Gen-Leser, Vertrag falls nötig, Fixtures, Erzeuger, Prüfer, Tests nach der geprüften Matrix; je Matrixzeile ein Rotbeweis unter docs/beweise/roh/NAK-214-rot-<zeile>.txt; Manifestabschnitte Bauetappen, Rotbeweise, Abweichungen, Nebenbefunde. Betroffene Beine je Etappe grün (A4, A5 `pruefe_v3_vertrag.py --abdeckung` und A8 bei Vertragsberührung, A28–A31, B3c/B10/B13/B28 bei Gen-Berührung; C++-Beine aus pwsh bauen, cmake fehlt im Bash-PATH); am Ende der volle Kanon abgekoppelt auf sauberem Baum (Muster docs/beweise/roh/NAK-213-etappe-2-auftrag.txt, Log nakama-nak214-kanon.log; Prüfaufträge und Urteile vorher committen). Kein Stand mit NOT RUN wird übergeben. Nach grünem Kanon Urteilsmarke T1 PASS in den Manifestkopf, Kopftabelle nachziehen, Commit mit Pathspec, pull --rebase, push, STOPP. T2/T3 setzt der Dirigent nach der Erstprüfung.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen — Zahlenränder (leerer `target`, sechs Felder einzeln leer, zwei Mains, kein Main, Paar mit Wirkung 0, `finding_id` mit 32 Hexzeichen aber unbekannt, Passage mit gleichem Zeitstempel), Determinismus (M-25), Vertragssymmetrie (jede neue Antwortform oder Feldregel in Schema, Rust, C++ und Python; Fixture ↔ Erzeuger ↔ Prüfer bytegleich; alte Consumer lesen die Fassung), Save/Load nicht betroffen, Audio-Thread nicht betroffen, keine irreführenden Kommentare oder Manifestsätze, keine sichtbare Behauptung ohne Beleg (Gen: `stale` ist ein ehrlicher Zustand, kein totes Element). Beziehungen: Befund ↔ Proposal ↔ Rückweg, verbinden ↔ trennen (`controlEnde` ↔ Reconnect), Rücknahme ↔ Folge-Snapshot, `user_verdict` ↔ Existenz des Befunds, Passage ↔ Taint/Rücknahme.
