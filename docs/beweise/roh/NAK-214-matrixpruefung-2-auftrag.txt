Matrixprüfung 2 (Wiederprüfung nach Matrixnacharbeit 1) NAK-214 / G5-Nacharbeit 3 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, kein cargo und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde an der Verhaltensmatrix:
`git diff 41a3fca081feee31bf804ecfce29f23b63747bba...b204bbcbde356fceb5bbd3aa685fec2cd427e0f1 -- docs/beweise/NAK-214.md`
(ZIEL = b204bbcbde356fceb5bbd3aa685fec2cd427e0f1 = HEAD, vor und nach deinem Lauf identisch; VORHER = 41a3fca0, der Stand vor der Runde — er enthält gegenüber dem geprüften Stand d0f2f304 nur drei Rohdateien unter docs/beweise/roh/, keine Manifeständerung). Der Codestand unter broker/, eq-copilot/, tools/ ist an VORHER und ZIEL identisch mit dem Kanonstand d3c6709 (Abschlusskanon NAK-213, GRÜN 61/61). Der übrige Matrixbereich wurde in Matrixprüfung 1 (Thread 01a080a4-8e6d-7fe0-9fc6-53bfce5d210e, docs/beweise/roh/NAK-214-matrixpruefung-1-d0f2f30.txt) bereits geprüft und ist hier nicht Prüfgegenstand.

BEFUNDE, die diese Runde schließen sollte — wörtlich aus dem Urteil der Matrixprüfung 1, mit der Regel, die der Dirigent je Befund entschieden hat (docs/beweise/roh/NAK-214-matrixnacharbeit-1-auftrag.txt; R6, R7, R8 sind neue Dirigentenregeln, R1 bis R5 stehen in docs/beweise/G5.md §11 Nr. 3):
1. DEFEKT E3/V-14: „Auch vollständig identische PRE/POST-Signale erfüllen das Prädikat: gültige Bänder, ausgerichtetes Delta null, `match_gain_db = 0` und `wirkung = Some("Kette wirkt breitbandig gleichmäßig")` (`prepost.rs:824–843, 883–884`). Hier existiert weder eine Spektral- noch eine Pegelveränderung. Trotzdem verlangt V-14 `wirkungsbeleg` und `effektkette_pre_post`." Regel R6: vierter Punkt im Strukturprädikat, schwellenfrei — mindestens ein gültiges Band des ausgerichteten Deltas ist von exakt 0.0 verschieden ODER `match_gain_db` ist von exakt 0.0 verschieden; das exakt-null-Paar bleibt Klasse 1 mit `next_test = PrePostPaarMessen`; kein Textvergleich, keine dB-Schwelle, keine Bandbindung; V-14 geteilt, das Nullpaar als eigene Zeile mit Test und Rotbeweis (vierter Punkt entfernt → `wirkungsbeleg`).
2. DEFEKT (Nachlauf) / LÜCKE (`unknown`) E9/V-39: „Nach einem Interventionsende wird die zugehörige ID entfernt, während `tail_samples_offen` weiterhin positiv sein kann (`intervention.rs:230–236`). Der geplante Filter würde die Experimentpassage dann wieder zulassen, obwohl `Taintstand::erlaubt()` weiterhin falsch ist; dasselbe gilt bei `unknown` ohne aktive ID (`zustand.rs:194–197`)." Regel R7: „getaintet" umfasst den Nachlauf der Intervention dieses Experiments (M-58); die Zuordnung wird beim Interventionsende gerettet (internes Feld am `Taintstand`, kein Leitungsfeld, kein Fassungsschritt), geleert bei `tail_samples_offen == 0` oder `neutral_resync` (M-61); der Filter liest offene Interventionen UND die Nachlaufmenge über alle Taintstände; `intervention.rs` (nur Retten/Leeren) und `zustand.rs` (nur das Feld) werden Ticketpfade, §2.9 Nr. 11 entsprechend präzisiert; Nachlauffall als eigene Zeile mit Rotbeweis. Lücke `unknown`, Entscheid: sitzungsweit ohne Experimentbezug, kein Kriterium der Passagenwahl; Kontrollzeile: `unknown = true` ohne offene Intervention und ohne Nachlauf → Passagenwahl unverändert, Material der Sitzung über den bestehenden Taint gesperrt.
3. DEFEKT E1/V-02, V-05–V-10: „Der beschriebene gemeinsame Rotbeweis kann nicht entstehen: Ohne Schlussriegel stoppen weiterhin die neue Zielvorbedingung und die bestehende Passagenvorbedingung (`proposal.rs:656`); umgekehrt bleibt V-02 beim alleinigen Entfernen der Zielvorbedingung am Schlussriegel hängen. Außerdem lässt sich `stop_if` nicht durch eine handgebaute Lage leeren, weil `proposal()` es selbst erzeugt (`proposal.rs:665, 732, 855`); Eingriffsgrenzen werden ebenfalls intern gebildet." Regel: EIN Riegel — die Zielvorbedingung entfällt, `gate_felder_vollstaendig` am Ende von `proposal()` erfüllt R1 wörtlich; V-02 misst über den Schlussriegel (Rotbeweis: Schlussriegel entfernt → Proposal mit `target: ""` in Sicht und Store); Sechserprüfung an zwei Stellen: (a) Funktion am `Proposal`-Objekt, sechs Teilfälle direkt gebaut (Rotbeweis: Funktion konstant wahr → sechs Teilfälle rot), (b) produktiver Aufruf über den Erzeuger mit den zwei erreichbaren Feldern `target` (V-05) und `listen_for` (V-08), Rotbeweis: Aufruf entfernt → Objekt entsteht; V-06 nur am Objekt (WN-04 bestehend); V-07, V-09, V-10 über den Erzeuger strukturell nicht erreichbar, nur an der Funktion gemessen und so benannt; E1, §2.7 M-46-Nachtrag und Aufrufkettentabelle auf den einen Riegel umgeschrieben.
4. DEFEKT V-12: „Innerhalb einer Sitzung kann nicht genau einem Befund das Proposal-Ziel fehlen. `vorschlaege_bilden` ermittelt eine gemeinsame `Proposallage` und übergibt sie jedem Befund (`proposal_verdrahtung.rs:38–60`); jedes Proposal übernimmt dieselbe `lage.ziel_instanz` (`proposal.rs:719`)." Regel: V-12 auf das befundeigene Feld `listen_for` umgestellt (genau ein Befund mit leerem `listen_for`, von Hand gebaut); die übrigen entstehen, `paare` bleibt paarweise; Kontrollfall.
5. DEFEKT E4/V-17: „E4 erhält `prepost_paar` ausdrücklich mit seiner bisherigen Bedeutung `ergebnis.is_some()` (`hypothese_verdrahtung.rs:243–253`). Genau diese Zuweisung ‚wieder' herzustellen verändert nach dem vorgesehenen Bau nichts […]. Der Rotbeweis bliebe grün." Regel: Der Rotbeweis V-17 setzt `prepost_wirkungsbeleg` aus `ergebnis.is_some()` statt aus dem Strukturprädikat; `prepost_paar` bleibt unberührt.
6. DEFEKT E6/V-23, V-24: „Für die neuen lokalen Zustandswechsel fehlt die gekoppelte Aktualisierung von `findingsOffen`. Der Zähler wird nur beim Snapshot neu berechnet und schließt `stale` aus (`SourcesModel.cpp:1260, 1588–1605`). […] `PluginEditor.cpp:1318–1321` zeigt weiterhin ‚Findings: 2 open'." Regel R8: Jeder lokale Übergang auf `stale` (`controlEnde`, Rücknahme ohne Folge-Snapshot) rechnet `findingsOffen` über `zaehleOffeneFindings()` neu, danach `findingsOffen == 0`; V-23/V-24 messen den Zähler mit (Rotbeweis: Neuberechnung entfernt → Zähler bleibt 2); §2.7 bekommt M-84.
7. DEFEKT E6/V-25, V-27: „Auch beim Reconnect derselben Sitzung ruft der Produktpfad `beginneSubscription` auf (`PluginProcessor.cpp:2408–2410`). Diese Funktion leert `befunde` unbedingt, ohne Epochenvergleich (`SourcesModel.cpp:439–455`). Bleibt der Snapshot aus, existiert eine leere Liste statt der zugesagten erhaltenen stale-Befunde." Regel: Produktpfad bleibt, kein Epochenvergleich; zwischen Verbindungsende und Reconnect sind die Befunde `stale` und sichtbar (V-23); jeder Reconnect ruft `beginneSubscription` und leert `befunde`, `experimente`, `paare`, `findingsOffen` — bei neuer wie bei gleicher `session_epoch` (V-25 auf beide Fälle erweitert); ohne Folgesnapshot leere Liste, `findingsOffen` 0, nichts handelbar (V-27 umgeschrieben, misst den Produktpfad); mit Folgesnapshot füllt der Broker (V-26); der E6-Satz „Die Liste bleibt stehen" gilt nur bis zum Reconnect.
8. DEFEKT V-44: „‚Terminal' umfasst auch `experiment_abort`: Dieser setzt `Terminal::Abgebrochen` (`experiment.rs:1210–1223`) und schließt die zugehörigen Taintintervalle (`experiment_verdrahtung.rs:637–641`). Genau dieser Zustand muss nach R5, E9 und V-38 ausgeschlossen bleiben, während V-44 seine Passage wieder freigibt." Regel: V-44 nach Terminalart geteilt: (a) `neutral_resync` auf offenem Versuch oder Ablauf des Nachlaufs (R7) → wieder wählbar, sofern die übrigen Eignungsbedingungen gelten; (b) `Terminal::Ergebnis` mit bekannter Vergleichbarkeit → wählbar (V-41), mit `None` → nicht (V-40); (c) `Terminal::Abgebrochen` → nie wieder wählbar (V-38, R5); (c) ist Kontrollfall zu V-38.

Der Worker hat die Änderungen als Zeilen V-45 bis V-48 (neu) und Umschreibungen an E1, E3, E6, E9, V-02, V-05 bis V-12, V-14, V-17, V-23 bis V-27, V-39, V-44, §2.7, §2.9 und der Kopftabelle eingetragen (Verlauf §5 „Matrixprüfung 1" und „Matrixnacharbeit 1").

GATE-BEZUG, wörtlich: P5-Exit-Gate (Entwurf §59, docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung." Entwurf §36.1 (Zeile 2359): „Wirkungsbeleg: Ein PRE/POST-Paar zeigt eine reproduzierbare Veränderung durch eine Kette." SONDE-014 M-17; SONDE-013 M-58 („Das Ende allein genügt nicht: der Nachlauf muss abgelaufen sein"), M-61, M-62; SONDE-014 M-84 (der Befundzähler fällt mit seiner Quelle), M-25 (Determinismus), M-30, M-46, M-77; NAK-212 N-43; NAK-213 E7/R4, R8. Invarianten aus CLAUDE.md: „Schemas sind Verträge", „Keine toten UI-Elemente", „Begriffe nicht vermischen", „State bleibt verlustfrei".

Du beantwortest genau zwei Fragen:
1. Ist jeder der acht Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Zeilen-ID, Entscheid, Satz der Matrix; und ob die neue Zusage am Code an ZIEL und am gebauten Vertrag wahr ist, mit Datei:Zeile)? Für die drei Dirigentenregeln R6, R7, R8 prüfst du, ob die Matrix sie so umsetzt, wie sie oben stehen, und ob die Umsetzung mit Code an ZIEL, Vertrag und den unverändert geltenden Zeilen (M-17, M-25, M-30, M-46, M-58, M-61, M-77, M-84, N-43) widerspruchsfrei ist. Insbesondere: Ist R7 (Zuordnung Nachlauf→Experiment) an `intervention.rs:230–236`, `zustand.rs:169–197` und `intervention.rs:446–459` so spezifiziert, dass Setzen, Leeren bei `tail_samples_offen == 0`, Leeren bei `neutral_resync` und das Verhalten bei `taint_intervalle_des_experiments_schliessen` (Terminal) je eine Zeile mit Test haben und kein Zustand zwei Ausgaben zulässt? Ist R6 exakt-null gegen nicht-null ohne neue Zahl formuliert, und bleibt V-15 (kein gültiges Band) davon getrennt? Ist der eine Riegel (Befund 3) mit V-02 bis V-13 und §2.7 M-46 widerspruchsfrei — hat jede Zeile ihren erreichbaren Rotbeweis oder ihre ehrliche Kennzeichnung?
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile oder einen Entscheid, einen Satz des Gate-Bezugs, eine unverändert geltende Referenzzeile, einen bestehenden Vertrag, §2.8 („kein Fassungsschritt") oder eine Invariante aus CLAUDE.md? Hat eine geänderte Zeile ihren Test (Datei::Fall) oder ihren Rotbeweis verloren, oder widersprechen sich zwei Zeilen nach der Änderung (Nichtdeterminismus, M-25)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1: DEFEKT (Widerspruch zu Gate, Regel, Entwurf, Vertrag, Referenzmatrix, Code an ZIEL oder Invariante; oder Regel ohne messende Zeile mit Test und Rotbeweis), LÜCKE (benennen, keine Nacharbeit), HÄRTUNG (benennen, keine Nacharbeit).
AUSDRÜCKLICH AUSGESCHLOSSEN: Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände in docs/**; der Manifestkopf; §3, §4, §5 von docs/beweise/NAK-214.md (nur §1 und §2 sind Prüfgegenstand; §5 liest du als Kontext); docs/beweise/G5.md, NAK-212.md, NAK-213.md, docs/offene-punkte.md; im Register datierte Härtungen NAK-191 bis NAK-211 und NAK-215 bis NAK-222; Sabotage-Szenarien gegen Prüfwerkzeuge (= Härtung); tools/dirigent/pruefliste.md ist keine Anforderungsquelle; die vier vom Dirigenten getragenen Lesarten E2 (Rangschwelle), E6 (`clear()`), E7 (`unknown_target`), E9 (offen tauglich, unvergleichbar ist nicht unbekannt) sind keine Wortlautdefekte; Ticketfremdes (NAK-212/213: Sicherheitsklasse, Screening, Deckel, Konkurrenzlage, Master-Wahl, Kanaltafel, Kanalwechsel; S31b Anzeige) nur insoweit, als die Matrix dorthin abgrenzt. Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — alle acht Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID (V-nn oder E-n) oder Manifestzeile, Quelle Datei:Zeile, welcher Satz von Gate, Regel, Entwurf, Vertrag, Referenzmatrix oder Invariante bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
