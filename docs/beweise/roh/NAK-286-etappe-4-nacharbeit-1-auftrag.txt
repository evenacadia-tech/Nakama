NAK-286 — Etappe 4, Nacharbeit 1 (Runde 1 von 3) nach der Erstprüfung 1. Auftrag des Dirigenten, 15.09.2026.

ROLLE. Du bist der Bauer der Nacharbeit 1 der Etappe 4 des Tickets NAK-286 (Plan S25e, Laufzeit-Arm) im Repo C:\Users\phili\Projekte\Nakama (Branch master, sichtbarer Checkout, Windows 11; Runner in PowerShell 7 und Python 3.13 unter tools/fl/, Szenarien unter docs/gesundheit/szenarien/, dazu das MCP-Repo C:\Users\phili\Projekte\fl-studio-mcp mit eigenem Git, Zweig evenacadia-local, Tests mit pytest über `uv run`, Python 3.12). Du schließt genau die drei bestätigten Defekte der Erstprüfung 1 im Code, beweist jeden mit Test und Rotbeweis, prüfst die gemeinsame Ursache (m) über die ganze Etappe, fährst die betroffenen Beine und einen Laufzeitlauf selbst und übergibst nie „NOT RUN". Mehr baust du nicht. Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

VERBINDLICHE QUELLEN (in dieser Reihenfolge lesen, bevor du eine Datei anfasst): (1) docs/beweise/roh/NAK-286-etappe-4-erstpruefung-1-5f76e90a.txt — das Urteil wörtlich mit den drei Befunden; (2) docs/beweise/roh/NAK-286-etappe-4-pruefung-1-validierung.md — Quellzitate mit Zeilen, Regelwortlaute, kleinste schließende Änderungen der drei Validierer; (3) docs/beweise/NAK-286.md: Kopf (Gate wörtlich, Grenze), §2 (R-286-1 bis R-286-5, Feinheiten F-16 bis F-28, besonders F-22, F-23, F-24, F-28 samt datierter Ergänzung), §4 (Regeln am Beginn; §4.1 M-01 bis M-13, besonders M-03; §4.2 M-14 bis M-22, besonders M-21; §4.7 bis §4.10, besonders M-56, M-70, M-71; M-75, M-78, Runner-Hälften M-53 und M-81), §5.0 bis §5.4, §5.7 (Punkte 1, 2, 9, Tests), §5.9, §7.1 (T-1 bis T-37, besonders T-11, T-17), §7.3, §10.2 P-1 bis P-6, §13.2 P-7 bis P-9, §16.2 P-9 präzisiert und P-10, §20 P-11, §23.2 P-12 und P-13, §26.2 P-12 präzisiert und P-14, §33.2 P-15, §37 (Bau der Etappe 4, besonders 37.1, 37.2 Rotbeweise, 37.6 Laufzeitläufe, 37.9 Abweichungen), §38 (Messung, Regeln P-16 bis P-18, Lücken L-286-1 und L-286-2), §39 (Einordnung §39.1, Regeln P-19 und P-20 §39.2, Ursache (m) und NAK-302 §39.3, Start §39.4); (4) den Code an HEAD, den Befunde und Validierung nennen: tools/fl/szenario.py (:328-338 `_python313`; :353-356 `Umgebung.fenster`; :528-538 `sammle`; :802-819 `lokal_fenster`; :1133 `F23_FELDER`; :1323-1327 `stellen_zeile`; :1350-1352 `stellen_durchlauf`; :1440-1448 und :1466-1475 `messe_stelle`; :2805 `fall_stelle_zwei_durchlaeufe`; alle Selbsttestfälle), tools/fl/laufzeit.ps1 (:112-131 `Stop-Process`-Regel; :303-310 `FahreSzenario`; :496-540 `Rendere` mit :506-507, :511-512, :516-518; :700-707 Fremdprüfung vor dem FL-Start; :987-1001 Selbsttestfall M-03; Selbsttestabschnitt), im MCP-Repo src/fl_studio_mcp/utils/fenster.py (:179 `PrintWindow`, :224-245 `capture_process_window`) und tests/test_fenster.py, docs/beweise/roh/NAK-286-rotskript-etappe4.ps1 (:85-91 Mutation M-03; das ganze Skript als Muster), docs/beweise/roh/NAK-286-rot-M-03.txt, NAK-286-rot-M-21.txt, NAK-286-rot-M-70.txt, NAK-286-rot-M-71.txt, docs/beweise/roh/NAK-286-laufzeit-34bdf159.md (:1501-1502, :1670-1673 U40-Tabellen). Dazu CLAUDE.md (Wahrheitskern, Invarianten, Maschinen-Landminen — Start-Process -ArgumentList quotiert nicht; Pipes verdecken Exitcodes; Backticks nie in einem Bash-Befehlstext; ein Pipe-Zeichen in einer Tabellenzelle als \|), tools/dirigent/pruefliste.md (Arbeitsliste, Selbstaudit A bis F).

TICKETPFADE (nichts anderes wird angefasst): tools/fl/szenario.py, tools/fl/laufzeit.ps1; im MCP-Repo nur src/fl_studio_mcp/utils/fenster.py und tests/test_fenster.py, und nur, falls die Unterprozess-Erfassung dort einen Einstieg braucht (ein `__main__`-Einstieg oder eine kleine Funktion; kein anderes Verhalten; Commit mit Pathspec auf evenacadia-local, Push origin evenacadia-local; nie main, nie upstream; die unversionierten tests/__pycache__/*.pyc nie anfassen). Dazu docs/beweise/NAK-286.md — änderbar sind: §4.1 die Zeile M-03 (Rotbeweisspalte: die Mutation umfasst die redundante Schranke `$rest`, der Träger ist `start /R` im Protokoll), §4.2 die Zeile M-21 (Testspalte: Unterprozess mit Frist, Selbsttestfall mit blockierender Attrappe; Rotbeweisspalte: Rotbeweis an der `timeout`-Übergabe), §4.10 die Zeile M-71 (Rohzeile trägt `durchlauf` und Kombinationen nach P-16); jede geänderte Zeile behält ihre ID, keine neue Zeile, §4.12 unverändert; §5.7 nur, wenn ein Punkt durch die Änderung faktisch anders lautet (dann als datierte Ergänzung, ohne den Wortlaut zu streichen); ein neuer Abschnitt „## 40. Nacharbeit 1 der Etappe 4 (Bauer, 15.09.2026)"; die Kopfzeile „Etappe" nur im letzten Fettsatz. Alles andere im Manifest bleibt unverändert (auch §37, §38 und §39). Rohdaten unter docs/beweise/roh/: NAK-286-rot-M-03-nach1.txt, NAK-286-rot-M-21-nach1.txt, NAK-286-rot-M-71-nach1.txt, weitere NAK-286-rot-M-<nn>-nach1.txt nur, falls die Ursache (m) eine Zeile neu beweist, NAK-286-etappe4-nacharbeit1-beine.txt, NAK-286-etappe4-nacharbeit1-mcp-tests.txt (nur bei MCP-Änderung), NAK-286-rotskript-etappe4-nacharbeit1.ps1, NAK-286-laufzeit-<sha>.md (schreibt der Runner; <sha> ist der HEAD deines Laufs). Nicht angefasst: tools/fl/nulltest.py, tools/fl/selbsttest.py, tools/fl/LIES-MICH.md, docs/gesundheit/szenarien/** (die vier Szenarien und LIES-MICH), tools/beweise.ps1, eq-copilot/** (Installer, Schemas, Fixtures — auch eq-copilot/fixtures/fl/Nakama-Diagnose.flp, kein FL-Projekt wird gespeichert; Identität, Plugin), broker/**, tools/plan/**, tools/eq-copilot/**, tools/dirigent/**, docs/gesundheit/KONZEPT.md, docs/PLAN-STAND.md, docs/offene-punkte.md, docs/plan/**, .claude/**, im MCP-Repo alles außer den zwei genannten Dateien, fremde uncommittete Dateien (die untracked Ordner briefing-hub/ und nimbalyst-local/). Nie git add -A, nie --amend, nie git reset --hard, nie git clean; Commit nur mit Pathspec; neue Dateien einzeln mit `git add -- <Pfad>` stagen.

INVARIANTEN (CLAUDE.md, Gate, R-286-1 bis R-286-5, P-1 bis P-20): der Runner startet nie eqcop-broker.exe und nie einen Testbroker; kein mutierter Bau wird installiert (T-11); kein FL mit fremdem Projekt wird beendet, ein fremdes Projekt heißt begründetes Überspringen mit Exit 0 (R-286-5, M-02, M-03 — das Produktverhalten von M-03 ändert sich nicht, nur sein Rotbeweis); das Diagnoseprojekt wird nie gespeichert (M-12); der Inhalt einer Antwortdatei wird nie als Pfad oder Parameter übernommen (M-53 (c), M-56, M-57); Exitcodes 0, 3, 4, Urteilswörter, Kopfzeile und Aufbau der Rohdatei bleiben (M-11, F-24) — die neuen Spalten `durchlauf` und `kombinationen` kommen in die U40-Tabellen (M-71), nicht in die Kopfzeile des Laufs; Szenario-Exit 5 lässt die Folge weiterlaufen (P-3); die Fenstererfassung bleibt ohne SetForegroundWindow, keybd_event oder pynput (M-21); keine Legacy-Umbenennung (NAK-30); vor dem Schreiben jeder bestehenden Datei `git ls-files --eol` lesen und dieselben Zeilenenden zurückschreiben (Index LF für tools/fl/szenario.py, tools/fl/laufzeit.ps1 und das Manifest; die Arbeitskopie zeigt seit dem Rebase w/crlf — schreibe so, dass `git diff` nur deine Änderung zeigt, nie die ganze Datei; im MCP-Repo die Zeilenenden der jeweiligen Datei); Python ohne den LastWriteTime-Schritt; die Selbsttests importieren fl_studio_mcp nicht und laufen unter `py -3.13` ohne MCP-Repo; die MCP-Tests laufen mit `uv run pytest` im MCP-Repo mit PYTHONDONTWRITEBYTECODE=1. Prüfe vor dem ersten Lauf, dass kein Kanon und kein anderer Bau- oder Testprozess im Checkout läuft (NAK-290); das offene Diagnose-FL des Bauers (PID 28220, Titel „Nakama-Diagnose.flp - FL Studio 2026") beendet dein Runner beim Laufzeitlauf selbst; vorher fasst du es nicht an.

DIE DREI DEFEKTE, WÖRTLICH, UND DIE SCHLIESSENDEN REGELN. Bindend sind §39.1 und die Regeln P-16 (§38.2), P-19 und P-20 (§39.2) wörtlich (P-1 bis P-18 gelten weiter):

1. „[P2] Begrenze die Fenstererfassung tatsächlich auf fünf Sekunden — tools\fl\szenario.py:802-807. DEFEKT: Hängt FL während `PrintWindow`, blockiert dieser synchrone Aufruf den Szenarioprozess unbegrenzt. Die Dauerprüfung erfolgt erst nach seiner Rückkehr; auch `FahreSzenario` wartet ohne Prozessfrist. Damit wird die zugesagte „Frist 5 s“ aus M-21 nicht durchgesetzt. Lesender Nachweis: `rg -n 'umg.fenster|FENSTER_FRIST_S' tools/fl/szenario.py`, zusammen mit dem synchronen `PrintWindow` in `utils/fenster.py:179`. Die Erfassung benötigt eine abbrechbare Ausführung statt lediglich einer nachträglichen Dauerwertung." — Schließende Regel P-19 (§39.2): Eine Frist der Matrix ist eine erzwungene Obergrenze, keine Messgröße: der Schritt endet spätestens mit Ablauf der Frist als VERFEHLT mit der Meldung „Frist <n> s ueberschritten", auch wenn der darunterliegende Aufruf nicht zurückkehrt. Für `lokal.fenster`: die Erfassung läuft nicht in-process, sondern als Unterprozess über den vorhandenen Weg `_python313(..., FENSTER_FRIST_S)` mit `subprocess.run(timeout=FENSTER_FRIST_S)`, der bei Ablauf den Unterprozess beendet; die Frist bleibt 5 s (M-21); Pfad, Breite, Höhe, SHA-256 und `einfarbig` kommen als JSON aus dem Unterprozess zurück (kein Pfad aus der Antwort wird als Parameter übernommen). Der Unterprozess läuft unter dem MCP-Python (`uv run --directory <MCP-Repo> --python 3.12 python …`, wie der Runner heute `fl_studio_mcp` erreicht) — brauchst du dafür einen Einstieg in `utils/fenster.py`, ist er der einzige MCP-Grund dieser Runde. Test: Selbsttestfall mit einer Attrappe, deren Erfassung länger als die Frist blockiert (Unterprozess, der schläft) → Schritt VERFEHLT nach höchstens Frist plus Aufräumzeit, der Szenarioprozess läuft weiter, der Unterprozess ist beendet; Rotbeweis an der `timeout`-Übergabe (Mutation: `timeout` entfernt oder auf None → der blockierende Fall kehrt nicht innerhalb der Frist zurück → rot; der Rotlauf braucht selbst eine Obergrenze im Rotskript, damit er endet). Die äußere Prozessobergrenze in `FahreSzenario` ist Härtung NAK-302 und nicht Teil dieser Runde.

2. „[P2] Trage Durchlauf und Kombinationszahl in jede U40-Rohzeile ein — tools\fl\szenario.py:1323-1327. DEFEKT: Die erzeugten Messzeilen enthalten weder `durchlauf` (`gemeinsam`/`getrennt`) noch die Zahl der getragenen Kombinationen. Diese Informationen erscheinen ausschließlich im Begleittext und werden nicht an den Zeilenschreiber weitergegeben. Das verletzt P-16, das beide Angaben ausdrücklich je Rohzeile verlangt; insbesondere verlieren extrahierte Zeilen ihren Durchlaufkontext. Lesend nachvollziehbar mit `rg -n '^\| Stelle \|' docs/beweise/roh/NAK-286-laufzeit-34bdf159.md`: Alle drei Tabellen fehlen diese Spalten. Die Planmetadaten müssen bis zur einzelnen Messzeile weitergereicht werden." — Schließende Regel P-16 (§38.2, wörtlich): „jede Rohzeile trägt `durchlauf` (`gemeinsam` oder `getrennt`) und die Zahl der getragenen Kombinationen". Umsetzung nach Validierer B: `stellen_zeile` bekommt `durchlauf` und `kombinationen` (Form `<getragen>/<alle>`, z. B. `16/16`) als zwei Spalten direkt hinter `Rolle`; Kopf in `messe_stelle` entsprechend; `stellen_durchlauf` gibt `(tragend, alle)` zurück statt nur zu texten; der Begleittext bleibt. Test: `fall_stelle_zwei_durchlaeufe` prüft die Zeile selbst (`| S3 | probeeq | getrennt | 4/16 |` oder die Werte der Attrappe), nicht den Gesamttext; Rotbeweis: die zwei Spalten aus `stellen_zeile` entfernt → rot an der Zeile.

3. „[P2] Weise im M-03-Rotfall den tatsächlichen Renderstart nach — tools\fl\laufzeit.ps1:998-999. DEFEKT: Bei der dokumentierten M-03-Mutation verhindern die verbleibende `$rest.Count`-Prüfung und der spätere Fremdprojektabbruch weiterhin den Renderstart; der Lauf endet mit Exit 0/UEBERSPRUNGEN. Rot werden hier stattdessen der erwartete Meldungswortlaut und die Existenz von `render.json`, obwohl kein Renderprozess gestartet wurde. Nachvollziehbar mit `Get-Content docs/beweise/roh/NAK-286-rot-M-03.txt` und `Rendere()`. Damit fehlt der von M-03 verlangte Nachweis „Attrappe mit fremdem Titel erreicht den Renderaufruf“: Der Negativaufbau muss die tatsächliche Renderfreigabe prüfen, nicht ersatzweise Meldung oder Statusdatei." — Schließende Regel P-20 (§39.2): Ein Rotbeweis gilt nur, wenn die Mutation die Zusage der Zeile falsifiziert: der Selbsttest prüft die Zusage an ihrem Träger (für M-03 die Renderfreigabe, sichtbar als `start /R` im Protokoll), Meldungstext und Statusdateien sind getrennte, nachrangige Prüfungen mit eigenem Wortlaut; hält nach der Mutation eine redundante Schranke die Zusage aufrecht, gehört sie zur Mutation (für M-03 zusätzlich `$rest = @(FL-Instanzen)` → `Where-Object { -not (Ist-Fremd $_) }`), und das Trägermuster des Rotskripts nennt den Träger, nicht die Meldung. Umsetzung nach Validierer C: im Selbsttestfall M-03 (`laufzeit.ps1:987-1001`) die Sammelprüfung trennen — eigene Prüfung `(-not (T-Hat 'start /R'))` mit dem Wortlaut „Renderprozess trotz fremdem FL gestartet" als Träger, `render.json` und Meldungstext getrennt und nachrangig; im Rotskript die M-03-Mutation um die zweite Ersetzung an `:511` ergänzen und das Trägermuster auf den neuen Wortlaut setzen; Rotbeweis: unter der vollständigen Mutation startet die Attrappe `FL64 /R` (Attrappe des Prozessstarts, kein echter Render), die Trägerprüfung wird rot und nennt den Träger. Das Produktverhalten von M-03 (`:506-507`, `:511-512`) ändert sich nicht.

GEMEINSAME URSACHE (m) (§39.3; §10.3 (a) bis (f), §13.3 (g) bis (i), §23.3 (j), §26 (k), §33.3 (l) gelten weiter) — die Ursache ist der Auftrag, nicht die Punktkorrektur: Der Zeuge steht nicht am Träger der Zusage. Prüfe jede Stelle der Etappe an dieser Frage und schreibe je Stelle Fund oder Nichtfund mit Zeile in §40: (i) jede Frist in szenario.py und laufzeit.ps1 (`_python313`, `sammle`, `tasklist`, Antwort 10 s, Ping 150 s, Render 600 s, Aufgabe 900 s, Boot-Marke, jede `Warte`-Schleife): vorab erzwungen (Abbruch bei Ablauf) oder nur gemessen? Nur gemessen ist ein Fund und wird nach P-19 umgebaut, mit Rotbeweis; (ii) jeder Rotbeweis der Etappe (§37.2, Rotskript): falsifiziert die Mutation die Zusage der Zeile, oder hält eine redundante Schranke sie aufrecht, sodass Meldung, Statusdatei oder Wortlaut rot werden statt der Träger? Prüfe das lesend am Rotskript und an den Rohdateien; jeder Fund wird nach P-20 neu gefahren und als `-nach1` abgelegt; (iii) jede Rohzeilenzusage (P-16, F-23, F-24, F-28, M-71, M-11): steht jede verlangte Angabe in der Zeile selbst, und prüft der Selbsttest die Zeile statt den Gesamttext? Jeder Fund wird umgebaut mit Rotbeweis an der Zeile.

BAUFOLGE.
1. Vorher messen: `git rev-parse HEAD` (muss docs/beweise/NAK-286.md mit dem Abschnitt „## 39." tragen — fehlt er, stoppst du und meldest es), `git status --short` (nur die zwei fremden Ordner), `git ls-files --eol` der zu ändernden Dateien; im MCP-Repo `git status --short` (nur tests/__pycache__/*.pyc), `git rev-parse HEAD` (muss 01f72386 sein), Zweig evenacadia-local; kein anderer Bau- oder Testprozess (NAK-290); A35 am Basisstand grün (Rohzeile in die Beinedatei). Den Basisstand jedes Defekts festhalten (Rotlauf vor der Änderung, Rohzeilen in die Beinedatei): für Defekt 1 der neue Selbsttestfall mit blockierender Attrappe gegen den unveränderten Runner (er darf dort nicht hängen — der Rotlauf selbst braucht eine Obergrenze im Rotskript; endet der Fall am Zeitlimit des Rotskripts, ist das der rote Beleg), für Defekt 2 der neue zeilengenaue Test gegen die unveränderte `stellen_zeile`, für Defekt 3 die vollständige Mutation samt Trägerprüfung gegen den unveränderten Selbsttest (Nachweis, dass die alte Prüfung `start /R` nicht als Träger sah).
2. Bau nach P-19, P-16 und P-20 in der beschriebenen Form; Ursache (m) über die genannten Stellen.
3. Je betroffener Matrixzeile (M-21, M-71, M-03, sowie jede Zeile, die die Ursache (m) berührt) der Testfall grün UND der Rotbeweis nach §5.1: SHA-256 der unveränderten Quelle, Mutation genau an der Zeile, die die Zusage trägt (bei M-03 samt redundanter Schranke), Bein einzeln (A35 oder der einzelne Selbsttest, dann mit dem Beinnamen in der Rohdatei) mit Rohausgabe (Exit ungleich 0 und eine Zeile, die den Träger der Zusage nennt), bytegleiche Rücknahme aus den gesicherten Originalbytes (SHA-256 gleich, Schreiben mit Wiederholung wie im Rotskript der Etappe 4), Bein grün; Skript dazu docs/beweise/roh/NAK-286-rotskript-etappe4-nacharbeit1.ps1. Die bestehenden Rotbeweise der Etappe 4 (§37.2) wiederholst du nicht, außer die Zeile ist vom Umbau oder von Ursache (m) berührt (dann neu fahren und als -nach1 ablegen).
4. Beine einzeln aus pwsh (Einzelbefehle in tools/beweise.ps1): A35 (`py -3.13 tools\fl\selbsttest.py`), bei MCP-Änderung MCP-pytest (`uv run --directory C:\Users\phili\Projekte\fl-studio-mcp pytest -p no:cacheprovider --color=no -v`, Rohausgabe docs/beweise/roh/NAK-286-etappe4-nacharbeit1-mcp-tests.txt, MCP-SHA in §40; erst dann Commit mit Pathspec und Push auf evenacadia-local; danach `setup-local.ps1`, damit der Controller in FL aktuell bleibt), A32 als Hinweis (bekannter Exit 4, NAK-292; keine neue Fundstelle aus der Runde). Rohausgabe docs/beweise/roh/NAK-286-etappe4-nacharbeit1-beine.txt. Kein voller Kanon in dieser Runde (Skill §3.4; der volle Kanon läuft beim Abschluss der Etappe 5); A17 (Exit 2, außerhalb der Etappe, §38.3) und A18 sind nicht berührt und laufen nicht.
5. Laufzeitlauf (§5.4; nie parallel zu einem Kanon; vorher kein Bau- oder Testprozess): `pwsh -NoProfile -File tools/fl/laufzeit.ps1 -Ticket NAK-286 -Basis <Basis-SHA dieser Runde> -Erzwingen` (der Diff dieser Runde trägt keinen Produktpfad, ohne den Schalter überspränge der Runner begründet; §37.9 Punkt 3). Erwartet: `fenster` 0 über den Unterprozess (das Bild bleibt 67 × 31, P-17 kommt erst in Etappe 5), `u40-aktivitaetsgate` 0 mit den Spalten `durchlauf` und `kombinationen` in jeder Zeile der drei Tabellen, `bereitschaft` 0, `snapshot-runde01` 0, `nulltest-host` 5 (Karte U43, Gesamturteil VORAUSSETZUNG, kein Fehler dieser Runde). Exit 4 ist Arbeit, kein Übergabegrund: Ursache benennen, beheben (nur in den Ticketpfaden), Lauf wiederholen. Rohdatei docs/beweise/roh/NAK-286-laufzeit-<sha>.md; Kopfzeile wörtlich in §40; die vom Runner nachgezogenen Hashzeilen in eq-copilot/install/nakama-installer-v1.json gehören zum Commit (einzige zulässige Änderung dort).
6. Manifest: M-03, M-21, M-71 wie oben; neuer Abschnitt „## 40. Nacharbeit 1 der Etappe 4 (Bauer, 15.09.2026)" mit: Kopftabelle (Basis-SHA, MCP-Basis- und MCP-End-SHA, Ticketpfade wie gebaut, Vorher-Messung mit SHA-256 und Zeilenenden, die roten Erstläufe der drei neuen Prüfungen gegen den unveränderten Stand); je Defekt: die geänderten Zeilen (was vorher stand, was jetzt steht, warum es den Befund schließt); je Matrixzeile Test, Rotbeweis (beide SHA-256, beide Exitcodes, Rohdatei), Ergebnis; Ursache (m) je Stelle Fund oder Nichtfund mit Zeile (Fristen, Rotbeweise, Rohzeilen); Beine mit Exitcodes; MCP-Tests, falls geändert; Laufzeitlauf mit Kopfzeile und je Szenario Urteil (U40: die drei Tabellen mit den neuen Spalten, je Stelle und Rolle `durchlauf` und `kombinationen`); Selbstaudit nach tools/dirigent/pruefliste.md A bis F; datierte Abweichungen von P-16, P-19, P-20 mit Grund (Technik innerhalb der Grenze; eine Abweichung außerhalb der Ticketpfade oder der Invarianten ist keine Abweichung, sondern ein Halt: dann stoppst du und meldest); eigene Messungen; was die Runde nicht geändert hat. Kopfzeile „Etappe": die bisherige Aussage bleibt davor stehen; der letzte Fettsatz (höchstens 70 Zeichen) lautet „**Nacharbeit 1 der Etappe 4 gebaut am 15.09.2026; Wiederprüfung steht aus.**". Keine neuen Deckel oder Zahlen ohne Quelle — jede Zahl trägt Datei:Zeile oder ihre Rechnung. Pipe-Zeichen in Tabellenzellen als \|; Zeilenenden des Manifests LF. Vor dem Commit `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-286.md` (0 Befunde).
7. Abschluss: `git status --short` zeigt nur Ticketpfade, Rohdaten, die Hashzeilen und die zwei fremden Ordner. Commit nur mit Pathspec über alle geänderten Ticketpfade, Rohdaten und Hashzeilen (`git commit -F <Nachrichtendatei unter $env:TEMP> -- <Pfade>`; neue Dateien vorher mit `git add -- <Pfad>` einzeln stagen; Betreff „NAK-286 Etappe 4 Nacharbeit 1: drei Defekte der Erstpruefung 1 geschlossen (M-21 Frist als Unterprozess P-19, U40-Rohzeile mit durchlauf/kombinationen P-16, Rotbeweis M-03 am Traeger P-20), Ursache m geprueft, A35 gruen, Laufzeitlauf"; die Nachricht endet mit „Co-Authored-By: Claude Opus <noreply@anthropic.com>"). Dann `git pull --rebase` nur bei sauberem Baum und `git push origin master`. docs/PLAN-STAND.md fasst du nicht an; planstand.py läuft nicht; docs/offene-punkte.md fasst du nicht an. Produktfragen beantwortest du nicht (U40 und U43 sind Karten). Melde zum Schluss in höchstens zwölf Zeilen: Commit-SHA (Nakama) und MCP-SHA (falls geändert), die geänderten Zeilen und IDs, je Matrixzeile Test grün und Rotbeweis rot mit Rohdatei, Ursache (m) Funde je Stelle, Beine mit Exit, Laufzeit-Kopfzeile und je Szenario das Urteil, datierte Abweichungen, offene Punkte.
