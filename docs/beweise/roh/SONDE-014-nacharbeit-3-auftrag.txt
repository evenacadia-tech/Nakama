SONDE-014 / S23–25 — Etappe 2, Nacharbeit 3 (Runde 3 von 3, letzte Runde des Budgets). Auftrag des Dirigenten (2026-09-07) an einen frischen Opus-Bauer (max).

LAGE
Repo C:/Users/phili/Projekte/Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start (der Dirigent hat den Stand nach d09a6d7 gemessen: Worktree sauber, Nacharbeit 2 abgeschlossen, betroffene Beine grün, docs/beweise/roh/SONDE-014-nacharbeit-2-b49dc69.txt). Ticket-Basis bleibt f90abf5. Aufsicht ENG. Es gelten unverändert die Rahmen- und Bauregeln aus docs/beweise/roh/SONDE-014-nacharbeit-2-auftrag.txt, -nacharbeit-1-auftrag.txt und -etappe-2-auftrag.txt (Git nur mit Pathspec, nie `git add -A`, nie `--amend`, Commit-Nachricht über Temp-Datei und `git commit -F`, `git pull --rebase` bei sauberem Worktree vor dem Push; kein Löschbefehl und kein Lösch-Cmdlet; keine Änderung an docs/offene-punkte.md, docs/PLAN-STAND.md, docs/NEXT-SESSION.md, docs/plan/, design/, Audio-/Sonden-/Hostbridge-/Identity-/Installer-Pfaden; Engines in Tests auf den Heap; jeder Rotbeweis fällt an genau der Zeile, die die Zusage trägt; keine Nachfrage an den User; beende den Turn nie mit uncommittetem Stand oder laufendem Bein). Landmine N-29: `cmake` liegt nicht im Bash-PATH — C++-Beine aus pwsh bauen und nach dem Bau prüfen, dass das gemessene Binary jünger als der Fix ist.

REFERENZ
Verhaltensmatrix docs/beweise/SONDE-014.md §3 mit den Nachträgen aus §8.6 und §9.6; Entscheide E-01 bis E-14; Regeln NR-01 bis NR-14 (§8) und WN-01 bis WN-05 (§9). Die Wiederprüfung 2 (Codex gpt-6-astra max, lesend, Thread 01a07b4a-c459-7363-83c9-b8ece4f1d006, HEAD d09a6d7 vor und nach dem Lauf, 14 min, Fixdiff 1a9ccbd...d09a6d7) steht wörtlich in docs/beweise/roh/SONDE-014-wiederpruefung-2-d09a6d7.txt, ihr Auftrag in docs/beweise/roh/SONDE-014-wiederpruefung-2-auftrag.txt. Urteil: NEEDS_WORK, zwei Befunde WP2-1 und WP2-2; WP1-2 bis WP1-5 sind geschlossen. Der Dirigent hat beide an der Quelle geprüft: zwei DEFEKTE. Lies zuerst diesen Auftrag, dann das Rohurteil vollständig, dann §9 des Manifests und die genannten Quellstellen.

DEFEKTE (Kernsatz wörtlich aus dem Urteil; Datei:Zeile zum Stand d09a6d7) und die REGEL, die jeden schließt:

WP2-1 — „WP1-1/WN-01 bleibt bei mehreren ausstehenden Urteilen offen: Zwei `assistentAntwort(..., &urteil, ...)` vor dem ersten ACK überschreiben hier den Mitschnitt des ersten Auftrags; der Assistentenschritt bleibt dabei offen. Kommt anschließend dessen `konflikt`-ACK, liefert `urteilMitFrischemKopf()` wegen der abweichenden Kennung einen leeren Text und `ControlClient.cpp:1350–1351` entfernt das unpersistierte Urteil endgültig. Das verletzt M-73 trotz unveränderter `command_id`-Politik." (eq-copilot/plugin/src/PluginProcessor.h:1235-1246 `Urteilmitschnitt letztesUrteil` als Einzelwert; PluginProcessor.cpp:2027-2032, 2060-2068; ControlClient.cpp:1324-1327, 1350-1351; die Annahme „zwei gleichzeitig offene gibt es nicht" ist nirgends erzwungen — `setzeAssistentenergebnis` lässt `offen` stehen, der P0 geht unabhängig von `veraendert`; zweiter Auslöser: ein nach WN-05 bei voller Queue zurückgehaltenes Urteil plus ein zweites vor dem Reconnect)
  Regel WN3-01: Der Mitschnitt für die Konfliktwiederholung wird JE `command_id` gehalten (kleine Abbildung, gedeckelt auf die 64 P0-Plätze; Eintrag beim Erzeugen, Löschung erst bei `angewandt`, `idempotentWiederholt` oder endgültigem Fehlschlag nach dem Versuchsdeckel) — kein Einzelwert, keine Annahme über die Zahl offener Urteile. Der Nachtrag zu M-73 (§9.6 „ein `konflikt`-ACK ist kein endgültiger Verlust") gilt für jedes ausstehende Urteil. Gemessen mit einem neuen B29-Abschnitt UND einem B10-Fall über den echten Hook (`urteilMitFrischemKopf`, nicht eine Test-Lambda): zwei Urteile vor dem ersten ACK → `konflikt`-ACK auf die ERSTE `command_id` → das erste Urteil wird unter seiner eigenen Kennung mit frischem Kopf wiederholt, das zweite bleibt unberührt; Gegenprobe: Löschung des Eintrags nach `angewandt`. Rotbeweis: Abbildung wieder auf einen Einzelwert → Fall fällt.

WP2-2 — „Die neue Rundung garantiert keinen vertragsgültigen Wiretext für kleine Rangkomponenten: Für `2.7e-11` wird der Faktor `1e25`; `round(2.7e-11 * 1e25) / 1e25` ergibt `2.6999999999999997e-11`. … der echte Referenz-Textriegel nimmt den ursprünglichen Text an und verwirft das Ergebnis. Auch Rust und C++ begrenzen echte Brüche auf 15 signifikante Stellen. Über `befund_json()` gelangt das Ergebnis in den Snapshot, dessen Re-Subscribe deshalb wieder `routing_fail_closed()` auslöst. … erforderlich ist eine Begrenzung der tatsächlich serialisierten Dezimaldarstellung." (broker/src/coordinator/hypothese_verdrahtung.rs:913-918 `zahl()`, 937-956 `auf_wirestellen`; Dirigent in Rust nachgerechnet: `10f64.powi(14 - exponent)` ist ab `exponent <= -9` selbst ungenau — `2.7e-11` → 17 Stellen, `5.55e-12` → 17 Stellen, und von 100 000 vorher gültigen Kleinwerten `1e-15…1e-9` werden 34 535 vertragswidrig; Kleinstwerte sind im Rechenweg normal (`bandpassung` über lineare Leistungen, Pearson-`koinzidenz`, `uplift`); Wirkkette `befund_json` → `sicht.rs` `serde_json::to_vec` (kürzeste Roundtrip-Form, bis 17 Stellen) → `subscription.rs:162-181` `v3_nachricht_lesen` → `vertrag.rs:222-227` `signifikante_stellen > 15` → `routing_fail_closed`; C++ deckelt den TEXT (`eq-copilot/plugin/core/ipc/WireZahl.h:34, 72, 196` über `to_chars` scientific mit 14 Nachkommastellen), Python ebenso (`erzeuge_v3_fixtures.py:3396`), Rust rundet numerisch — dieselbe Grenze, zwei Regeln (M-77); `rg auf_wirestellen broker/` trifft keinen Test)
  Regel WN3-02: Die 15-Stellen-Grenze greift in Rust an der SERIALISIERTEN DEZIMALDARSTELLUNG, spiegelbildlich zu `nakama::wire::wireZahl`: eine Funktion `wire_zahl(f64) -> f64` (Ort neben `vertrag.rs`, damit Vertrag und Deckel beieinander liegen) formatiert mit höchstens 15 signifikanten Stellen (`format!("{:.14e}")` oder gleichwertig) und parst zurück, so dass `serde_json` daraus einen Text mit höchstens 15 signifikanten Stellen erzeugt; Ganzzahligkeit, `±(2^53−1)` und der Exponentbereich werden wie in der C++-Fassung am fertigen Text entschieden; `auf_wirestellen` entfällt, `zahl()` und ALLE f64-Felder des Befunds, der Maskierung und des Proposals laufen durch `wire_zahl` (Inventar im Manifest). Gemessen mit (a) einem Rust-Property-Test: Zufalls-f64 aus `[0,1]`, `[-200,200]` und dem Dekadenband `1e-15…1e-9`, Zusage `textriegel_bytes(serde_json::to_vec(&wire_zahl(x)))` ist Ok; (b) einem Tabellenfall mit `2.7e-11`, `5.55e-12`, `9.038084803672431e-11`, `-3.3f32 as f64`, `0.1+0.2`; (c) einem A4-Fall, der einen `session_snapshot` mit einem Befund kleiner Rangkomponenten (≈1e-10) baut, durch `v3_nachricht_lesen` schickt und über `resubscribe_snapshot_push` ohne `routing_fail_closed` ausliefert (heute rot); (d) derselben Wertetabelle als gemeinsame Fixture beidseitig nach M-77: im Python-Textriegel (`tools/eq-copilot/pruefe_v3_vertrag.py`, A5) und im C++-Bein gegen `nakama::wire::wireZahl` (B10 oder B3c), Erzeugung über `erzeuge_v3_fixtures.py` (A8 bytegleich). Rotbeweis: `wire_zahl` durch die alte numerische Rundung ersetzen → (b) und (c) fallen.

BETROFFENE BEINE (nur diese; kein voller Kanon — der läuft beim Abschluss): A4 `cargo test --manifest-path broker/Cargo.toml` (sonde014_nacharbeit2, sonde014_nacharbeit1, sonde014_verdrahtung, contract_cross_language, neue Fälle), A5, A8, B10 `EqCopIpcTest`, B29 `EqCopSonde014AssistentTest`, B3c falls berührt; dazu jedes weitere Bein, dessen Quelle dein Diff berührt. Rotbeweise `docs/beweise/roh/SONDE-014-rot-WN3-01.txt` und `-WN3-02.txt`; Beinlauf auf dem Endstand als `docs/beweise/roh/SONDE-014-nacharbeit-3-<sha>.txt`.

MANIFEST docs/beweise/SONDE-014.md: neuer Abschnitt „§10 Wiederprüfung 2 und Nacharbeit 3" (append-only unter §9.8): Kopf der Wiederprüfung 2 (Modell, Effort, Thread, HEAD, Dauer, Urteil, Rohdatei, Auftrag; WP1-2 bis WP1-5 geschlossen), Einordnung des Dirigenten (2 Defekte), Zuordnung WP2-1/WP2-2 → WN3-01/WN3-02 → geänderte Stelle → Rotbeweis, das f64-Feldinventar aus WN3-02, gemessene Beinläufe mit Zahlen, Matrixnachträge (M-73 „Nacharbeit 3": Mitschnitt je command_id; M-77 „Nacharbeit 3": Textdeckel in allen drei Sprachen; N-30 fortschreiben), Prüflisten-Abhakung; lebender Kopf: „Phase" auf „Etappe 2: Nacharbeit 3 abgeschlossen, Wiederprüfung 3 offen", „Urteil" um „Wiederprüfung 2 (Astra max): NEEDS_WORK, 2 Befunde — 2 Defekte" ergänzt, Kurztabelle der Runden fortgeschrieben, Ticketpfade um neue Dateien ergänzt; Nebenbefunde in §7.11 fortschreiben. `py -3.13 tools/plan/dokuriegel.py docs/beweise/SONDE-014.md` grün vor dem Commit.

SELBSTAUDIT vor jedem Commit: Mitschnitt wirklich je Kennung und wirklich über den echten Hook gemessen, Deckel wirklich am Text (Property-Test über das Dekadenband fällt mit der alten Rundung), alle f64-Felder erfasst (Inventar gegen `rg 'zahl\(|as_f64|f64' broker/src/coordinator/hypothese_verdrahtung.rs sicht.rs proposal_verdrahtung.rs maskierung.rs`), drei Sprachen mit derselben Tabelle; keine Umbenennung, kein Neben-Refactor. Commits mit Pathspec je Ursache, pushen. Letzte Zeile deiner Ausgabe: „NACHARBEIT 3 ABGESCHLOSSEN <end-sha>".