HEAD vorher `60581ec3c27bfd92277306d47ca12201953f47f8` · HEAD nachher `60581ec3c27bfd92277306d47ca12201953f47f8`

# NAK-312 S25k, Etappe 6 — Quellvalidierung der Erstprüfung

- Validierer: frischer, lesender Opus-5.5-Thread, Effort max, 22.09.2026; weder Bauer der Etappe 6a oder 6b noch Prüfer `23eb5a93` noch Thread einer früheren Runde.
- Gegenstand: Rohurteil `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-urteil.md` (PASS; L-1 bis L-6, H-1 bis H-6, A-78, A-81) nach `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-validierung-auftrag.txt`; Klassen, Ausschlüsse und die Sonderregel für die drei Registervorschläge aus §38.28 nach `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-auftrag.txt`.
- Stand: `git diff --stat 86c31f46 HEAD` und `git diff --stat HEAD` über die 13 Pfade sind leer (Arbeitsbaum = HEAD = ZIEL). `eq-copilot/plugin/src/prozessor/Ipc.cpp` (Blob `aa02e6a6`) und `eq-copilot/plugin/core/ipc/InterventionsRing.h` (Blob `4ef48c6e`) sind an BASIS `fd287d3c`, ZIEL `86c31f46` und HEAD blobgleich. JUCE und das mitgelieferte VST3-SDK unter `eq-copilot/build/_deps/juce-src` gelesen; Pfade darin im Folgenden relativ zu `modules/`.
- Arbeitsweise: nur `git rev-parse`, `status`, `log`, `show`, `diff`, `ls-files`, `check-attr` und Lesewerkzeuge; kein Compiler, kein Test, kein Python, kein FL. Einzige geschriebene Datei: dieser Bericht. Nicht gelesen: die Brokerseite (Rust); FLs Hostverhalten ist hier nicht messbar.

## L-1 — `reset()` gleichzeitig zu `process()`

1. **Quelle.** Alle Stellen stimmen am Zielstand. `prozessor/Hostbruecke.cpp:190-224` `markierungAbbrechen() noexcept`, Kommentar `:198-199` „Nie gleichzeitig mit processBlock - dieselbe Annahme, unter der prepareToPlay die Markierung zuruecksetzt."; `HoerMarkierung.h:337-351` `brichAb()` (`hartAus()`, `warHoerbar = false`, `hoerbareSamples = 0`). Wrapper `juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp:3612-3618`: `if (! state) getPluginInstance().reset();` ohne Sperre. `FLStudioDIYSpecificationEnforcementLock` `:3815-3828` (nur bei `isFruityLoops()`), genommen in `setActive` `:2811`, `activateBus` `:3316`, `setBusArrangements` `:3461`, `process` `:3716`; nicht in `setProcessing`, `setupProcessing` (`:3592-3610`) und `terminate` (`:2758-2762`). `getCallbackLock()` umschließt nur `processBlock` (`:3889-3910`). Ring `core/ipc/InterventionsRing.h:162-176`: `kopf.load (std::memory_order_relaxed)`, Schreiben in `plaetze[k & …]`, `kopf.store (k + 1, …)`.
   Detail: „In FL laufen prepareToPlay und releaseResources … unter dieser Sperre" gilt für `releaseResources` nur auf dem Weg über `setActive (false)` (`:2837`); über `terminate()` (`:2760`) läuft es ohne FL-Sperre.
2. **Ablauf.** Nachgerechnet. `verarbeite` rampt aus einer lokalen Kopie von `fade` (`HoerMarkierung.h:626`), schreibt `fade = fEnde` zurück (`:641`) und bildet die Übergänge aus `warHoerbar` (`:658-663`, `:682`). Kreuzt `brichAb` die Mischschleife, überschreibt der Block `fade = 0`, sieht `warHoerbar == false` und meldet `begann`: der Klang endet nicht (M-59 bricht in diesem Lauf), `brichAb` hat für A `endete` gemeldet. In dieser Verschränkung liest `markierungAbbrechen` die Nummer (`Hostbruecke.cpp:206`) wenige Befehle nach `brichAb`, der Block zieht B erst nach der Mischschleife (`PluginProcessor.cpp:1001`) — die Paarung `end:A begin:B` bleibt im Regelfall richtig. Treffen beide Schreiber in `schreibe` zusammen, lesen sie dasselbe `kopf` und schreiben denselben Platz: ein Ereignis fehlt, beide Sequenzen sind gezogen, die Lücke setzt nach `InterventionsRing.h:22-24` und Entwurf §34.2 (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2236-2241`) sticky unknown. Zwei weitere Verschränkungen, gerechnet, im Rohurteil nicht genannt: (i) `brichAb` liest `warHoerbar == false` unmittelbar vor dem ersten hörbaren Block und löscht es erst nach dessen `begin:B` → der nächste hörbare Block meldet `begin:C`, B bekommt nie ein end (der Sender überschreibt `offenesBegin`, `Ipc.cpp:362`); (ii) liest `markierungAbbrechen` die Nummer erst nach dem `fetch_add` des Blocks, trägt das end die Nummer des neuen Eingriffs (A ohne end, B mit vorzeitigem end). Ob der Broker (i) und (ii) fail-closed wertet, ist ohne Brokerlektüre offen; „fail-closed, keine scheinbar saubere Baseline" ist nur für die Kollision belegt.
   Vertrag, VST3-SDK `juce_audio_processors/format_types/VST3_SDK/pluginterfaces/vst/ivstaudioprocessor.h:310-318`: „This will be called before any process calls start with true and after with false. … this function could be called in the UI or in Processing Thread … (no memory allocation …)". Ein `reset()` neben `process` liegt damit außerhalb des Vertrags. Zum vorgeschlagenen Ausweg „Anforderung, die der nächste Block einlöst": nach derselben Stelle folgt auf `setProcessing (false)` bis zum nächsten `setProcessing (true)` kein `process` — das end reiste erst beim Neustart, das Intervall bliebe so lange offen (fail-closed).
3. **Zusagenlage.** Gate-GRENZE „Audio-Thread ohne Sperren und Allokationen": vom gebauten Weg gehalten (A-78); eine Sperre zur Serialisierung stünde ihr entgegen. R-312-5 („mit genau einem `end` je `begin`") und M-86 sagen zu gleichzeitigen Hostaufrufen nichts; im Entwurf kein Treffer zu `setProcessing` oder `reset()`, §34.2 regelt nur die Folge eines Verlusts. Die Annahme ist benannt: §36.14 A-78 („Annahme wie bei `prepareToPlay`: `reset()` und `releaseResources()` laufen nie gleichzeitig mit `processBlock`"), §36.16 („`reset()` kommt aus `setProcessing (false)`, das JUCE nicht sperrt … Nicht gemessen, wie FL es hält"), §36.17 („ob noch ein `process` danach oder gleichzeitig kommt, ist nicht gemessen"), im Code `Hostbruecke.cpp:198-199` und `HoerMarkierung.h:323`.
4. **Urteil: PRÄZISIERT** — Kern stimmt; abweichend: `releaseResources` über `terminate()` ohne FL-Sperre; zwei weitere Verschränkungen mit fehlendem oder falsch zugeordnetem end; fail-closed nur für die Kollision belegt. **Klassenvorschlag: HÄRTUNG** — dieselbe Klasse wie §34.3 H-1 (gleichzeitige `setNonRealtime`-Aufrufe, Ausschluss nur über den VST3-Vertrag, JUCE sperrt nicht, FL ungemessen → HÄRTUNG, NAK-355 (1)). Eine Messung von Thread und Reihenfolge von `setProcessing` im Laufzeit-Arm beantwortete zugleich die Frage aus NAK-357. Die LÜCKE des Prüfers ist vertretbar.

## L-2 — A-78 und A-79 gegen den Wortlaut von M-59, M-62, M-64

1. **Quelle.** §6.7, wörtlich: M-59 Frist „derselbe Abschlussweg wie `prepareToPlay` (`eq-copilot/plugin/src/prozessor/Hostbruecke.cpp:97-142`), aufgerufen aus der neuen `reset`-Override"; M-62 Zustand „zwei offene Intervalle erzwungen (Marker aktiv, ein Übergang wartet bereits)", Zusage „`interventionsRingUeberlauf` wird true, und es wird KEINE Sequenznummer gezogen, die nicht reist"; M-64 Zusage „(b) erzeugt dasselbe `end` wie heute — der Fix verwendet den vorhandenen Weg wieder, statt einen zweiten zu bauen". Gebaut: `markierungAbbrechen` über den RT-Ring (`Hostbruecke.cpp:190-224`); der Block unter `sendeZustandMutex` (`:102-158`) bleibt allein `prepareToPlay`.
2. **Ablauf.** Nachgerechnet. Das end aus `reset()` steht mit seiner Sequenz im Ring; der wartende Übergang aus `prepareToPlay` wird vor das erste Ringereignis höherer Sequenz oder ans Ringende gestellt (`Ipc.cpp:353-357`, `:395-401`) — kein Überlauf, gemessen „begin@1 end@2 begin@3 end@4, kein Ueberlauf" (§36.12; `docs/beweise/roh/NAK-312-rot-M-60.txt:67`). Der Bauplanweg legte das end in `offenesBegin.totesEnde` oder `ausstehenderTotUebergang` (`Hostbruecke.cpp:135-157`), nie in den Ring, und `interventionenSenden` beginnt bei leerem Ring ohne Replay und Bericht keinen Zug (`Ipc.cpp:126-130`) — M-59s Zusage „im Interventionsring steht genau EIN `end`" war auf ihm unerfüllbar. Ergänzung: M-59 widersprach sich damit selbst (Zusagespalte Ring, Fristspalte Sendezustand); gebaut ist die Zusagespalte.
3. **Zusagenlage.** Die Verhaltenszusagen halten: M-59 Zusage, M-60, M-64 (a) und „dasselbe `end`" (Kennung, `project_sample_end` null, tail 45 760 für reset, release und prepare), M-62 (b) mit vollem Ring, M-86. Wörtlich ungedeckt: M-59 Frist, M-62 (a), M-64 (b) zweiter Halbsatz — zwei davon stehen in der Zusagespalte. Code nach dem Wortlaut bräche R-312-5 (M-62 (a): Überlauf hieße, ein berechtigtes end nicht einzureihen; ein gesetztes Bit wäre ein falsches unknown) bzw. die GRENZE (M-64 (b): der Block nimmt `sendeZustandMutex`, `Hostbruecke.cpp:104`). A-78 und A-79 sind begründet (§36.14) und in §37.2 vorgemerkt.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: LÜCKE** (veralteter Matrixwortlaut; Nachzug der drei Stellen als Matrixnachtrag, kein Codeweg). Wer die Zusagespalten von M-62 (a) und M-64 (b) wörtlich liest, sieht einen Matrixbruch — die Korrektur gehört auch dann in die Matrix.

## L-3 — M-84 unter Produkttakt

1. **Quelle.** `Ipc.cpp:139-147`: `if (h.pluginKind != "main" || ! nakama::ipc::adresseGueltig (h.adresse)) { … while (interventionsRing.lies (weg)) {} return; }` — geleert und verworfen. `h.pluginKind = nakama::state::wort (zustand.common.klasse)` (`:473`); `"sensor"` → `Klasse::legacy` (`eq-copilot/plugin/state/NakamaState.cpp:2190`). Replay: `:1006-1023` (`setzeGeneration (replayFaellig, meineGeneration)` nur bei nicht neutralem Aufbau), Replay-Entscheid `:213-222`, `:312-321`. BASIS: `git show fd287d3c:eq-copilot/plugin/src/prozessor/Ipc.cpp` zeigt `:118-147` und `:1006-1023` wortgleich; `Ipc.cpp` ist keiner der 13 Pfade. Test `eq-copilot/plugin/tests/MarkierungTestMain.cpp:1796-1817`: `setzeBindung ("sensor")`, 16 Blöcke, `setzeBindung ("hub")`, 40 Blöcke, erst dann `s.ernte()` (`:1817`), bei bestätigt ruhendem Sender (A-75).
2. **Ablauf.** Nachgerechnet wie beschrieben. Ergänzt: `offenesBegin` und `sendeBeginOffen` räumt allein `sendeEnde` (`Ipc.cpp:290-291`); nach dem Verwerfen stehen beide, bis ein neuer Markierungszyklus sein end sendet oder die Instanz neu entsteht. Jeder spätere Aufbau als Main ist deshalb nicht neutral (`:956-959`), meldet ausdrücklich unknown und replayt das alte Begin (`:1006-1023`); der Neuaufbau-Bericht schließt nie (`:413-433` verlangt `! offenesBegin.gueltig`). Folge: dauerhaft gesperrte starke Evidenz — die Klasse, die der N-10-Kommentar `Hostbruecke.cpp:93-101` beschreibt („die Sitzung dauerhaft gesperrt"), fail-closed, keine scheinbar saubere Baseline. BASIS: `spiegleKlassifikation` bestand nur aus dem Store (`git show fd287d3c:…/State.cpp:262-273`); weg von Main fiel `erlaubt`, die Rampe lief aus, das end kam in den Ring und wurde vom selben Sender verworfen — keine Regression der Etappe. Erreichbarkeit bestätigt: ein hörbarer Marker in Main entsteht nur über den Befundlisten-Popover im Restfall NAK-341; solange diese modale Box offen ist, erreicht kein Klick ein anderes Element (`juce_gui_basics/components/juce_Component.cpp:2232-2247`), und die Main-Fläche zeigt keinen Rollenwähler (`messpunktKnopf` in der Legacy-Liste, `PluginEditor.cpp:956-962`) — „weg von Main" bleibt `setStateInformation`.
3. **Zusagenlage.** M-84 Zusage „Genau ein `begin` und genau ein `end` dazu, mit der gezählten Hördauer …", Frist „… und das `end` entsteht am Fadeende"; R-312-8 (E-312-8) „Ein hörbarer Marker endet über den weichen Ausfade mit genau einem `end` zu seinem `begin`" — beide über die Entstehung; der Test misst die Leitung nur in der günstigen Folge (Ernte nach der Rückkehr zu hub). Detail zu „Gate und Matrix sagen … nichts": der Entwurf spricht über das verlorene End (§34.2, `:2240-2241`: „Ein verlorenes Begin **oder** End darf damit niemals eine scheinbar saubere Baseline erzeugen.") — diese Zusage hält hier (Control-Disconnect beim Rollenwechsel, nicht neutraler Aufbau nach der Rückkehr).
4. **Urteil: PRÄZISIERT** — Folge ist dauerhafte, nicht einmalige Nicht-Neutralität; der Entwurf schweigt nicht, seine Zusage hält. **Klassenvorschlag: LÜCKE.** Liest der Dirigent M-84 auf Leitungsebene, bräche die Zeile unter Produkttakt mit ihrem eigenen Ereignis — vorbestehend seit BASIS, außerhalb des Änderungssatzes.

## L-4 — abgewiesener Entwurf im Umordnungsfall (Registervorschlag §38.28 (1))

1. **Quelle.** `PluginEditor.cpp:1046-1047` `if (! hatZiel && ! vorherigesZiel.empty()) uebernehmeSourcesLabel();`; `:1017-1022` Fehlerbit und „Label update failed - write handover or target changed."; `:1048-1050` Ziel leer, `sourcesSchreibfehlerAktiv = false`; `:1062-1064` „Main target is outside the drawn list - select a drawn source."; `:1082-1086` Kennung leer; beim Wiedereintritt `:1087-1093` `setText (it->userLabel, …)`. „Dieselbe Funktion" meint `aktualisiereSourcesSteuerung`.
2. **Ablauf.** Nachgerechnet. `benenneSourcesHauptziel` scheitert (`Ipc.cpp:1208-1223`) ohne Schreibrecht, ohne Mitglied, bei `nurLesen` oder Klasse ≠ main; die letzten beiden verlassen die Main-Fläche. Ohne Schreibrecht ist das Feld gesperrt (`PluginEditor.cpp:1058-1059`) — ein Entwurf entsteht nur mit Schreibrecht, die Reichweite stimmt.
3. **Zusagenlage.** M-85 setzt den erfolgreichen Abschluss voraus; R-312-9 und M-69 regeln den abgewiesenen Abschluss nicht; CLAUDE.md „meldet ehrlich einen Zustand" hält (der gezeigte Status ist wahr). Alle schweigen.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: LÜCKE.**

## L-5 — Editorabbau innerhalb des Konfliktrückrufs (Registervorschlag §38.28 (2))

1. **Quelle.** `PluginEditor.cpp:621` `SafePointer`, `:633-635` die einzige Prüfung, `:640-643` `ed->statusMeldung = ed->processor.neueSensorId() ? … : …; ed->statusMeldungBisMs = …;`. Der Kommentar `:617` sagt „prueft ihn VOR jedem Zugriff".
2. **Ablauf.** Bis zum Host nachgerechnet: `neueSensorId` (`prozessor/State.cpp:54-67`) → `meldeHostDirty` (`Hostbruecke.cpp:237-240`) → `updateHostDisplay` ruft die Listener synchron (`juce_audio_processors/processors/juce_AudioProcessor.cpp:431-436`) → `audioProcessorChanged` (`juce_audio_plugin_client_VST3.cpp:1502-1559`) → `ComponentRestarter::restart` ruft auf dem Nachrichtenthread `handleAsyncUpdate` direkt (`juce_audio_processors/format_types/juce_VST3Common.h:1639-1650`) → `setDirty (true)` und `restartComponent (flags)` (`juce_audio_plugin_client_VST3.cpp:1613-1622`). Danach zwei Schreibzugriffe auf `ed` (die rechte Seite ist vor der linken sequenziert). Der read-only-Zweig ruft den Host nicht (`State.cpp:58-59`). Einen Hostweg, der dort abbaut, belegt nichts (`docs/beweise/roh/NAK-349-quellvalidierung.md:139`: „Nicht gemessen; die Quelle gibt dafür keinen Anhalt."); der VST3-Header verbietet ihn nicht (`…/pluginterfaces/vst/ivsteditcontroller.h:230-232`, `:303-305`). Ohne Host baubar wäre ein Rotbeweis über einen Listener, der den Editor in `audioProcessorChanged` abbaut.
3. **Zusagenlage.** R-312-2 Satz 2 spricht: „nach dem Ende seines Eigentümers ist er wirkungslos: kein Zugriff, keine Mutation, keine Dirty-Meldung" — für diesen Fall nicht erfüllt. M-91: Ereignis „danach Editor zerstören … DANN die Nachrichtenschleife pumpen" (Abbau vor der Zustellung), Frist „Lebendprüfung über `SafePointer` VOR jedem Zugriff auf Editor und Prozessor" — zeitlich erfüllt, gegen einen Abbau mitten im Rückruf wirkungslos; die Zeile misst den Fall nicht. Gate und CLAUDE.md schweigen.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: LÜCKE** nach der Sonderregel des Erstprüfauftrags für die §38.28-Vorschläge (DEFEKT nur bei Matrixzeile, Gate-Satz oder CLAUDE.md-Invariante; keine bricht). „Alle schweigen" trifft wegen R-312-2 nicht zu: nach der allgemeinen Klassendefinition (Regel zählt) und der Präzedenz E-312-17 (§35.2, derselbe Rückruf, ungemessener Hostweg, DEFEKT gegen R-312-2) wäre es DEFEKT gegen R-312-2 Satz 2, niedrige Schwere, nur bei ungemessenem Hostverhalten erreichbar. Kleinster Nachzug wie vom Prüfer genannt.

## L-6 — M-56 „der Grund steht im Status" in der Main-Fläche

1. **Quelle.** `markierungBeenden` setzt `statusMeldung` (`PluginEditor.cpp:919-927`); gezeichnet nur im Legacy-Zweig (`:1819-1844`), `paint` kehrt in Main nach `paintMainFlaeche` zurück (`:1515-1519`), diese zeichnet `sourcesBedienstatus` (`:1428-1434`); das Bein liest die Variable (`PluginEditor.h:79`, `MarkierungTestMain.cpp:1751`). `markierungAusKnopf` steht in der Legacy-Liste (`PluginEditor.cpp:956-962`).
2. **Ablauf.** Bestätigt. Detail zu „kein sichtbares Element behauptet etwas Falsches": im Popover-Restfall von NAK-341 setzt `BefundListe::zeigeZustand` den Toggle von Solo/Puls nur beim Aufbau und beim Klick (`:386-391`, `:474`, `:491`); beendet ein Netz den Latch, bleibt der Knopf „an", und ein Klick darauf schaltet wieder ein statt aus (`schalteMarkierung` `:843-850`, `:894-902`). Der Code ist seit BASIS unverändert (kein Hunk in `:361-509`); in Legacy bestand dieselbe Veraltung vorher, in Main ist sie neu erreichbar, weil die Netze dort jetzt ticken. Das liegt im ausgeschlossenen Restfall NAK-341 und ist kein neuer Befund.
3. **Zusagenlage.** M-56 Zusage „… und der Grund steht im Status"; §6.7 Vorspann „Die Fälle messen **tatsächlichen Ausgang und UI-Zustand gemeinsam**" — gemessen am Editorzustand, Sichtbarkeit in der Main-Fläche verlangt die Zeile nicht. CLAUDE.md „jedes sichtbare Element führt einen Handgriff aus oder meldet ehrlich einen Zustand": ein nicht gezeichneter Status ist kein sichtbares Element — die Regel verbietet tote Elemente, sie verlangt keine; der veraltete Toggle führt einen Handgriff aus und erfüllt sie dem Wortlaut nach. NAK-341 (`docs/offene-punkte.md:344`) nennt Auslöser und Aus-Knopf, nicht die Statusanzeige.
4. **Urteil: PRÄZISIERT** — der Satz „kein sichtbares Element behauptet etwas Falsches" gilt im Popover-Restfall nicht; NAK-341 deckt die Statusanzeige nur der Sache nach. **Klassenvorschlag: LÜCKE** (Produktlücke der Klasse NAK-341; Nachtrag dort um Statusanzeige und veralteten Toggle).

## H-1 — der Allokationszähler von M-60 und B7

1. **Quelle.** `MarkierungTestMain.cpp:42-65`: `thread_local` Zähler, ersetzt nur `operator new (std::size_t)` und `operator new[] (std::size_t)`; Zählfenster allein um `s.p->reset()` (`:1983-1986`). B7 gleich gebaut (`TransactionTestMain.cpp:154-165`). `HeapBlock` teilt mit `std::calloc` zu (Kommentar `HoerMarkierung.h:739-742`). Rohdatei `NAK-312-rot-M-60.txt:36`: „FEHLER 312/M-60 … [0 Allokationen, Zuteilungen 1 -> 2]" — unter „vorbereiten statt brichAb" meldet der Zähler 0, rot wird die Zeile über den Wet-Zuteilungszähler; mitgefallen M-61 (`:35`, `:85`).
2. **Ablauf.** Bestätigt. `blockpufferFuerTest()` = `versuchTrocken.capacity() + pufferKapazitaet()` (`PluginProcessor.h:683-684`): ein `assign` gleicher Größe ruft kein `operator new` und ändert keine Kapazität — unsichtbar. Den Wet-Puffer teilt allein `vorbereiten` zu (`HoerMarkierung.h:305-307`), der Zuteilungszähler deckt ihn also vollständig. Falsch rot ausgeschlossen (thread-lokal, Fenster nur um den Aufruf). Der Weg ist nach Lesen allokationsfrei (A-78).
3. **Zusagenlage.** M-60 „der Allokationszähler steigt über den Aufruf um 0; weder `wet.calloc` noch `versuchTrocken.assign` laufen" — hält nach Lesen, die `assign`-Hälfte ist bei gleicher Größe nicht messbar. A3 (`tools/beweise.ps1:509`: „… 0 Allokationen") und B7 (`:728`: „(seit Etappe 6a gemessen) 0 Allokationen") nennen die Zählart nicht; Präzedenz §34.3 H-4 (eine Behauptungszeile, die weiter reicht als ihr Bein, zog der Dirigent als Pflichtzusatz nach).
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## H-2 — Testzähler im Produktpfad von `HoerMarkierung.h`

1. **Quelle.** `HoerMarkierung.h:307` `++zuteilungen;` (in `vorbereiten`), `:855` Mitglied, `:743-744` Leser — ohne Makro. Unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`: `PluginEditor.h:60-122`, `:196-198`; `PluginProcessor.h:539-1071` (darin `:671-686`); `PluginEditor.cpp:551-566`, `:636-639`, `:1105-1110`. Unbedingte Testhaken derselben Datei: `:394-395`, `:417-450`, `:518-519`.
2. **Ablauf.** Inkrement im Kontext von `prepareToPlay`, nicht im Audiothread, ohne Sperre und Zuteilung; im Produktbau nie gelesen (einziger Leser `PluginProcessor.h:685-686` unter dem Makro).
3. **Zusagenlage.** GRENZE nicht berührt. §7.0 und §7.6 verlangen das Makro für die dort genannten Zugänge (Tick-Testzugang in `PluginEditor.h`; die Sondenzugänge in §7.2), keine allgemeine Regel „jeder Testzugang unter dem Makro"; A-83 (§36.14) nennt den Zähler ausdrücklich außerhalb des Defines. Stil, keine Zusage.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## H-3 — M-68 (b): das Ereignis der Matrixzeile

1. **Quelle.** `ShotTestMain.cpp:702-714`: Tippen, `klicke (*b.ed, 1)` (Klick in Zeile B der Liste, zweimal `mouseDown`, `:612-630`), Tick, `sourcesLabelFokusVerlustFuerTest()`. M-68 Ereignis: „(b) Fokusverlust durch Klick außerhalb der Liste".
2. **Ablauf.** Bestätigt. `mouseDown` bestätigt A (`PluginEditor.cpp:1158-1159`), der Tick lädt Bs Label mit Kennung B (`:1087-1093`), der Fokusverlust trifft Text gleich Bs Label und kehrt früh zurück (`:1010-1011`). Der Accessor ruft den Rückruf nur, wenn er verdrahtet ist (`PluginEditor.h:110-114`); einziger Aufrufer `ShotTestMain.cpp:707`. Ohne `PluginEditor.cpp:210` bleibt jedes Bein grün; ein ohne Enter verlassener Entwurf würde dann im nächsten Tick durch das gespeicherte Label ersetzt (`:1087-1093`). Die Rotbeweise von (b) fallen am Kennungsvergleich und an der Bestätigung im Klick (`NAK-312-rot-M-68.txt`, Teil 1 und 2); nur unter der Mutation aus Teil 1 schreibt der Fokusverlust selbst (auf B).
3. **Zusagenlage.** M-68 (b) Zusage hält nach Lesen; der geforderte Rotbeweis „Mutation wie M-66" ist erfüllt. Kein Satz verlangt einen Rotbeweis an `:210`.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## H-4 — die Anzeigeräumung fragt nur zum Tick

1. **Quelle.** `PluginEditor.cpp:942-949`.
2. **Ablauf.** Bestätigt. Main → Legacy nimmt den Auftrag zurück (`State.cpp:287-289`), `zielGesetzt` fällt (`HoerMarkierung.h:406-407`); Legacy → Main publiziert nichts; der Tick sieht Main und lässt `markModus` stehen. Heilt beim nächsten Netz oder Klassenwechsel. Zwei Wechsel innerhalb eines Ticks bei gesetztem Latch nur über `setStateInformation`: ein Latch entsteht nur in Main (`:894-901`), dort über den Befundlisten-Popover, und dort gibt es keinen Rollenwähler (siehe L-3).
3. **Zusagenlage.** M-57 misst den Rückwechsel über Ticks; zu zwei Wechseln innerhalb eines Ticks schweigen Gate und Matrix.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## H-5 — Meldung für einen still verfallenden Entwurf (Registervorschlag §38.28 (3))

1. **Quelle.** M-69 Zusage „Der Entwurf verfällt ohne Mutation: weder A noch B werden geschrieben, und es entsteht kein Host-Dirty"; R-312-9 letzter Satz „existiert die Startquelle nicht mehr, verfällt der Entwurf ohne Mutation"; Code `PluginEditor.cpp:1000-1008`, `:1087-1093`. Detail: „ohne Meldung" steht nicht in M-69 — die Zeile schweigt zu einer Meldung.
2. **Ablauf.** Bestätigt (M-69 (a) bis (c), §38.4).
3. **Zusagenlage.** M-69 und R-312-9 verlangen keine Meldung und verbieten keine; das Feld zeigt danach den gespeicherten Namen, also einen wahren Zustand. Eine Meldung wäre neuer Text im geparkten Designteil.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## H-6 — der Publisher-Aufruf in `spiegleKlassifikation`

1. **Quelle.** `State.cpp:286-290` (`if (main != istMainKlassifiziert.load (…) && markierung.zielGesetzt()) markierung.reicheAus(); istMainKlassifiziert.store (main);`), gerufen aus `setStateInformation` (`:192`, `:215`) und `setzeBindung` (`:362`); `reicheEin` schreibt `auftraege[schreibIndex]` und `schreibIndex` nicht atomar (`HoerMarkierung.h:385-408`, Mitglied `:825-826` „Nur Message-Thread"); Kommentar `State.cpp:364-366`. BASIS: `spiegleKlassifikation` bestand allein aus dem Store — „bisher nur Atomics" stimmt.
2. **Ablauf.** Liefe `setStateInformation` auf einem fremden Thread neben einem `reicheEin` des Editors, schrieben zwei Publisher `schreibIndex`, und die Besitzpermutation des Tripelpuffers (`HoerMarkierung.h:362-373`) bräche. Vertrag: der Wrapper vermerkt „The VST3 spec requires that this function is called from the UI thread" und prüft es mit `assertHostMessageThread()` (`juce_audio_plugin_client_VST3.cpp:3042-3046`, nur im Debugbau).
3. **Zusagenlage.** Keine Zusage zu `setState` von fremden Threads; GRENZE nicht berührt.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: HÄRTUNG.**

## A-78 — Abschluss aus `reset()` und `releaseResources()` über den RT-Ring

1. **Quelle.** `Hostbruecke.cpp:190-224`: `brichAb()` (`HoerMarkierung.h:337-351`, `noexcept`; `hartAus` `:807-812` und `resetZustaende` `:800-806` setzen nur Mitglieder); `nakama::ipc::Interventionsereignis e;` auf dem Stack (`InterventionsRing.h:100-138`: `bool` und 64-Bit-Ganzzahlen, trivial kopierbar, kein Heap — „POD" im strengen Sinn nicht, weil die Standard-Memberinitialisierer den Defaultkonstruktor nicht-trivial machen; ohne Belang); Atomics `letzteGueltigeSamplerate`, `interventionsSequenz`, `interventionsNummer`, `interventionsRingUeberlauf` (`PluginProcessor.h:1426`, `:1718`, `:1721`, `:1724`), Lock-Freiheit für `std::uint64_t` und `double` auf dieser Plattform per `static_assert` belegt (`eq-copilot/plugin/core/StampedAudioQueue.h:650`, `eq-copilot/plugin/dsp/DspKern.cpp:16`); `tailSamplesFuer` rein und `noexcept` (`InterventionsRing.h:78-90`); `fuellstand` und `schreibe` nur Atomics und `std::array<Interventionsereignis, kPlaetze> plaetze` (`:162-176`, `:207-212`, `:225-229`). Kein JUCE-Basisaufruf: `void AudioProcessor::reset() {}` (`juce_audio_processors/processors/juce_AudioProcessor.cpp:589`), `virtual void releaseResources() = 0;` (`…/juce_AudioProcessor.h:142`); die Overrides (`Hostbruecke.cpp:165-188`) rufen keine Basis. Sequenz nur bei Platz `:216-221`; Sequenz 0 nur bei Neutralität `Ipc.cpp:956-971`.
2. **Ablauf.** Bestätigt. Im Fenster zwischen Ringeintrag und Abholung ist der Prozessor nicht neutral: der Ring hält das end, und `hoerbarAtomic` bleibt nach `brichAb` stehen (`HoerMarkierung.h:348-349`); `endete` setzt `warHoerbar` voraus, und der Block setzt `warHoerbar` und `hoerbarAtomic` gemeinsam (`:682`, `:689`). Das end gleicht dem aus `prepareToPlay` (`Hostbruecke.cpp:204-211` gegen `:132-146`; gemessen M-64: je ein end, null, 45 760). Der Bauplanweg nähme `sendeZustandMutex` (`:104`) und ließe das end liegen, bis ein Zug läuft (`Ipc.cpp:126-130`). `releaseResources` gibt danach frei (`Hostbruecke.cpp:186-187`) — Freigabe, keine Zuteilung, auf den Wegen `setActive (false)` und `terminate()`, nicht über `setProcessing`.
3. **Zusagenlage.** GRENZE „Audio-Thread ohne Sperren und Allokationen" und R-312-5 „`reset()` allokiert nicht" gehalten; M-59 Zusage, M-60, M-64 und M-86 grün gemessen (§36.12). Wortlautfolgen: L-2; Nebenläufigkeit: L-1.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: kein Befund** — technische Entscheidung innerhalb der GRENZE.

## A-81 — Eingriffsnummer in `prepareToPlay`

1. **Quelle.** `Hostbruecke.cpp:118-120` (`anLebendes = offenesBegin.gueltig && ! offenesBegin.tot && offenesBegin.ereignis.nummer == interventionsNummer.load (…) - 1`), `:121-157`; `PluginProcessor.cpp:1000-1003`; `Ipc.cpp:353-357`, `:395-401`.
2. **Ablauf.** Nachgerechnet für M-86: Ring `end:A` (s1), `begin:B` (s2); `prepareToPlay` mit offenem Sender-Begin A → A ≠ B → Wartender mit s3 > s2; der Sender sendet end:A, begin:B und am Ringende `totEndeSenden` → Leitung `end:A begin:B end:B` (gemessen, §36.12; ohne Prüfung `end:A end:A begin:B`, §36.14). Ein berechtigtes end wird nicht unterdrückt: `anLebendes` falsch führt auf den Wartepfad (`:148-157`), nur bei schon wartendem Übergang auf den vorbestehenden Überlauf ohne gezogene Sequenz (`:121-131`). Die Nummer des offenen Intervalls ist die zuletzt gezogene: jedes begin zieht (`PluginProcessor.cpp:1001`), ein neues begin setzt das end des vorigen voraus (`HoerMarkierung.h:659-681`). Die Prüfung läuft unter `sendeZustandMutex` in `prepareToPlay`, nicht im Audiothread.
3. **Zusagenlage.** Trägt M-86 („das abschließende `prepareToPlay` schließt genau dieses eine offene Intervall. Ereigniszahl am Ende: zwei `begin` und zwei `end`") und R-312-5; GRENZE unberührt.
4. **Urteil: BESTÄTIGT.** **Klassenvorschlag: kein Befund** — technische Entscheidung innerhalb der GRENZE.

## Übersicht

| Befund | Urteil | Klassenvorschlag | tragender Satz |
|---|---|---|---|
| L-1 | PRÄZISIERT | HÄRTUNG (Präzedenz §34.3 H-1, NAK-355 (1)) | keiner; VST3 `ivstaudioprocessor.h:310-311` schließt den Fall aus („after with false"); GRENZE gehalten |
| L-2 | BESTÄTIGT | LÜCKE (Matrixwortlaut nachziehen) | M-59 Frist, M-62 (a), M-64 (b) beschreiben den ersetzten Weg; die Verhaltenszusagen halten |
| L-3 | PRÄZISIERT | LÜCKE | M-84 und R-312-8 regeln die Entstehung, nicht die Zustellung; Entwurf §34.2 (verlorenes End) hält |
| L-4 | BESTÄTIGT | LÜCKE | keiner (M-85 setzt den Erfolg voraus) |
| L-5 | BESTÄTIGT | LÜCKE nach Sonderregel §38.28; nach Präzedenz E-312-17 DEFEKT | R-312-2 Satz 2 „nach dem Ende seines Eigentümers … kein Zugriff" |
| L-6 | PRÄZISIERT | LÜCKE (Nachtrag NAK-341) | keiner; M-56 „Grund steht im Status" = Editorzustand; „Keine toten UI-Elemente" nicht berührt |
| H-1 | BESTÄTIGT | HÄRTUNG | keiner; A3 und B7 nennen die Zählart nicht |
| H-2 | BESTÄTIGT | HÄRTUNG | keiner; §7.0 und §7.6 binden das Makro nur an die dort genannten Zugänge |
| H-3 | BESTÄTIGT | HÄRTUNG | keiner; der Rotbeweis „Mutation wie M-66" ist erfüllt |
| H-4 | BESTÄTIGT | HÄRTUNG | keiner |
| H-5 | BESTÄTIGT | HÄRTUNG | keiner; M-69 und R-312-9 schweigen zur Meldung |
| H-6 | BESTÄTIGT | HÄRTUNG | keiner; VST3 laut Wrapper: `setState` im UI-Thread |
| A-78 | BESTÄTIGT | kein Befund | GRENZE und R-312-5 gehalten; M-59, M-60, M-64, M-86 |
| A-81 | BESTÄTIGT | kein Befund | M-86, R-312-5 |

FERTIG Validierung Etappe 6, 11 bestätigt, 3 präzisiert, 0 widerlegt
