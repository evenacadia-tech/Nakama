# NAK-134 — PIPE_BUSY ist Liveness, kein Sicherheitsurteil

| Feld | Wert |
|---|---|
| Ticket | NAK-134 — Nacharbeit zum Phasengate G3 |
| Basis-SHA | `4789f2a8a3cace3d71809b6f9326b6d4aaf7389a` |
| Phase | **Phase 1 — Spezifikation vor Code.** Diese Fassung traegt Kopf, Defekt, Regeln und Verhaltensmatrix. Es ist **kein Produktcode und kein Werkzeugcode geaendert**. |
| Urteil | `NOT RUN` — offen fuer den Dirigenten. Der Wert gilt fuer die Spezifikationsphase und bleibt hier im Verlauf stehen, wenn Phase 2 ihn ersetzt. |
| Kanon | `NOT RUN` (Phase 2) |
| Aenderungssatz Phase 1 | nur `docs/beweise/NAK-134.md` |

## 1. Gate-Text — woertlich

`docs/FL-Nakama-Sonden-Design-Entwurf.md:4262-4264` (P3-Exit-Gate):

> **Exit-Gate:** Save/Reload, Duplicate, Bridge/PID-Wechsel, zwei offene Projekte, Brokerneustart und
> 60-Minuten-Soak erhalten richtige Mitgliedschaft ohne falsche Steueradresse. Sichtbarer Zustand
> erreicht das Budget aus Abschnitt 49. `R1 · Passive Alpha` ist erreicht.

`docs/FL-Nakama-Sonden-Design-Entwurf.md:3586` (Abschnitt 49.3, Budgettabelle):

> | Soak | mindestens 60 min mit 16, mindestens 30 min mit 32 Sonden ohne XRun/ungegrenztes Wachstum |

Gebrochene Matrixzeilen des Werkzeugtickets: `docs/beweise/G3-SOAK.md` §5, Zeilen **S03**
(„Die Mitgliedschaft driftet ueber die ganze Laufzeit nicht") und **S11** („Innerhalb der Frist aus
§3.1 (**60 s ab `BEREIT`**) je Clientpaar: der Main subscribt neu, alle N Sonden reporten neu, und
der Snapshot ist wieder vollstaendig"). Zusaetzlich faellt **S13** („Ausserhalb der Neustartfenster
wird **kein** Mitglied stale (Grenze > 2.500 ms) und keines evicted (10.000 ms)").

## 2. Defekt — woertlich aus der Ursachenanalyse des Gate-Laufs

> Nach einem Brokerneustart mit 32 Sonden (66 Verbindungen auf zwei scharfe Pipe-Instanzen,
> `broker/src/transport/server_v3.rs:1684-1700`) verlieren einige Clients 20-mal das Rennen um
> `WaitNamedPipeW(200 ms)` (`eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:67-89`) und enden mit
> `ERROR_PIPE_BUSY`. `IpcVerbindung.cpp:90-99` klassifiziert JEDEN Fehler ausser
> `ERROR_FILE_NOT_FOUND` als `ServerPruefStatus::belegtAberUnverifiziert` — laut
> `IpcVerbindung.h:40-42` „ein Sicherheitsfehler und kein Livenessfehler".
> `ControlClient.cpp:994-1012` parkt den Verbindungsthread daraufhin dauerhaft auf einer
> Condition-Variable („kein wiederholtes Anklopfen"), nur `reconnect()`/`stop()` loest ihn;
> `TelemetryClient.cpp:407-424` hat dieselbe Sperre. Folge im Produkt: nach einem Brokerneustart in
> einer Session mit vielen Sonden bleibt ein Teil der Sonden endgueltig getrennt (kein Backoff, kein
> Versuch, kein stale, kein Zaehler), bis die Instanz neu geladen wird.

### 2.1 An der Quelle nachgeprueft (Stand `4789f2a`)

| Behauptung | Quelle | Befund |
|---|---|---|
| 20 Runden `WaitNamedPipeW(200)`, danach Abbruch mit dem letzten Fehler | `IpcVerbindung.cpp:67-89` | bestaetigt; `letzterFehler` traegt `ERROR_PIPE_BUSY` |
| jeder Fehler ausser `FILE_NOT_FOUND` wird `belegtAberUnverifiziert`/`pipeOeffnen` | `IpcVerbindung.cpp:90-99` | bestaetigt |
| Control parkt unbefristet auf der Condition-Variable | `ControlClient.cpp:994-1012` | bestaetigt; nur `laeuft == false` oder ein Generationswechsel loest |
| Telemetrie hat dieselbe Sperre | `TelemetryClient.cpp:407-421` | bestaetigt |
| `belegtAberUnverifiziert` zaehlt als Serverpruefung | `ControlClient.cpp:1101-1103`, `TelemetryClient.cpp:554-556` | bestaetigt |
| der v2-`PipeClient` klassifiziert identisch | `PipeClient.cpp:391-405` | bestaetigt; derselbe Defekt, derselbe Parkpfad (`PipeClient.cpp:305-312`) |
| `BrokerLifecycle` haelt bei `belegtAberUnverifiziert` an | `BrokerLifecycle.cpp:734-747` | bestaetigt: Phase `blockiertUnverifiziert`, kein Reconnect, kein Spawn — solange der Status steht, und er steht, weil der Clientthread parkt |
| die Anzeige nennt den Zustand `Server not verified` | `SourcesModel.cpp:410-412,1070` | bestaetigt |
| `belegtAberUnverifiziert` ist **nicht** persistiert und **kein** Wire-Feld | `git grep` ueber `schemas/`, `state/`, `*.json`, `*.py`, `*.ps1` | bestaetigt: reiner Laufzeit-Snapshot; kein Schema-, State- oder Migrationsvertrag beruehrt |

### 2.2 Messung aus dem Gate-Lauf G3 (A24, 32 Sonden, 30 min, 3 Neustarts)

Rohbericht `docs/beweise/roh/G3-soak-n32-30min-ad5b2d1.json`, Laufprotokoll
`docs/beweise/roh/G3-soak-n32-30min-ad5b2d1.txt`.

| Groesse | Wert | Lesart |
|---|---|---|
| `mitgliedschaft.vollstaendig` / `snapshot_pruefungen` | 22.925 / 82.572 | S03 ROT — 72 % aller Snapshots zeigten nicht alle 32 Sonden |
| `mitgliedschaft.abtast_vollstaendig` / `abtast_pruefungen` | 4.482 / 17.922 | dieselbe Drift in der 250-ms-Abtastung |
| `mitgliedschaft.ungueltig`, `fremde_adresse`, `fuehrendes_main_falsch` | 0 / 0 / 0 | die Sicht ist nicht falsch, sie ist **unvollstaendig** |
| `neustart[1..3].bereit_bis_vollstaendig_ms` | −1, −1, −1 | S11 ROT bei **allen drei** Neustarts |
| `neustart[1..3].reconnect_ms.max` | 60.001,0 ms je Neustart | Frist Z2 (60 s) gerissen; Fuellwert „Frist + 1" fuer nicht zurueckgekehrte Paare |
| `neustart[1..3].reconnect_ms.min` | 392,6 / 2.531,3 / 521,8 ms | die **gewinnenden** Paare kamen weit innerhalb der Schranke zurueck |
| `kill.k_s5.backoff_deckel_erreicht` / `erwartet` | 26 / 33 | **7 Control-Clients machten in der Totzeit keine 5 Versuche** — sie versuchten gar nicht mehr |
| `liveness.stale_ausserhalb_neustart` / `stale_im_neustartfenster` | 0 / 0 | kein Mitglied wurde je „stale": es kam nie zurueck, um zu veralten |
| `liveness.evicted_ausserhalb_neustart` | **13.440** | S13 ROT — die verlorenen Sonden wurden ausserhalb der Fenster dauerhaft geraeumt |
| `p0.gesendet` / `beantwortet` | 56.673 / 56.161 | Die nominale Obergrenze aus den Laufparametern (`clientpaare` 33 x 1 Hz x 1.800 s) ist **59.400**; es fehlen **2.727** Heartbeats, die geparkte Clients nicht mehr einreihten. Selbst gerechnet, nicht uebernommen: die Kurzfassung des Defekts nannte hier rund 69.000, was aus den Laufparametern nicht folgt |
| `p0.verloren_ausserhalb_neustart` | 0 | **kein** ACK ging verloren — die verlorenen Heartbeats wurden nie eingereiht und deshalb nie erwartet |

Die letzte Zeile ist der Grund fuer Regel R3(a): das Werkzeug hat den Ausfall **nicht gesehen**,
weil `SessionSoakMain.cpp:777-785` eine abgewiesene Einreihung still zuruecknimmt
(`gesendet.pop_back()`). Der Defekt war nur an `p0.gesendet` gegen eine Kopfrechnung erkennbar.

## 3. Die Regeln, die den Defekt schliessen — woertlich (Dirigent, Technikentscheid)

> **R1.** Ein erschoepfter `ERROR_PIPE_BUSY` (die Pipe existiert, alle Instanzen sind belegt, der
> Server wurde nie erreicht) ist ein LIVENESS-Ausgang: eigener `ServerPruefStatus`-Wert
> (z. B. `belegtNichtErreicht`) mit eigenem `ServerPruefFehler`, der den normalen Backoff
> 500 bis 8000 ms ausloest (`IpcVerbindung.h:34-35`) und NIE den Broker-Startpfad oeffnet (nur
> `nichtDa` darf das, `IpcVerbindung.h:41`) und NICHT als Serverpruefung zaehlt
> (`ControlClient.cpp:1101-1103`). `belegtAberUnverifiziert` bleibt ausschliesslich dem Fall
> vorbehalten, in dem ein Handle geoeffnet wurde und `namedPipeServerAuthentisieren`
> (`IpcVerbindung.cpp:106-113`) den Identitaetsbeweis nicht bestand, sowie jedem Oeffnungsfehler,
> der auf einen fremden Besitzer des Namens deutet (`ERROR_ACCESS_DENIED` und alle uebrigen Fehler
> ausser `PIPE_BUSY` und `FILE_NOT_FOUND`) — die NAK-123-Sicherheitszusage (kein wiederholtes
> Anklopfen an einen belegten, unverifizierten Namen) bleibt fuer diese Faelle unveraendert.
>
> **R2.** Control- und Telemetrie-Client behandeln den neuen Status identisch (Backoff, kein Parken);
> Gegenpfade verbinden↔trennen und parken↔loesen bleiben symmetrisch (`reconnect()`/`stop()` loesen
> weiterhin jede Sperre).
>
> **R3.** Werkzeug A24 muss den Defekt sehen koennen: (a) `SessionSoakMain.cpp:777-785` verwirft eine
> abgelehnte P0-Einreihung still (`gesendet.pop_back()`) — abgelehnte Einreihungen bekommen einen
> eigenen Zaehler im Bericht (`p0.einreihung_abgelehnt`), und `pruefe_session_soak.py` wertet ihn in
> S04 (Wert muss ausserhalb der Neustartfenster 0 sein); (b) `reconnect_paare`
> (`SessionSoakMain.cpp:1064,1108,1270`) ist heute die Vektorlaenge `anzahl+1` — es wird die Zahl der
> Paare, deren Control UND Telemetrie nach dem Neustart tatsaechlich wieder verbunden waren; der
> Pruefer vergleicht sie mit erwartet und meldet ROT bei Abweichung. Beide Aenderungen sind
> Pruefwerkzeug, kein Produktcode; die Matrix in `docs/beweise/G3-SOAK.md` §5/§7 wird um die zwei
> Zaehler ergaenzt (append-only Nachtrag, datiert).

## 4. Benennung und Verortung — Entscheide dieser Phase

Technische Wege entscheidet der Worker; jede Wahl steht hier mit ihrem Grund.

| # | Entscheid | Grund |
|---|---|---|
| E1 | Neuer Statuswert heisst `ServerPruefStatus::belegtNichtErreicht` | R1 nennt ihn als Vorschlag; er sagt genau das Gemessene — der Name ist belegt, der Server wurde nicht erreicht. Kein Urteil ueber Identitaet. |
| E2 | Neuer Fehlerwert heisst `ServerPruefFehler::pipeBelegt` | Parallel zu `pipeFehlt` (FILE_NOT_FOUND) und `pipeOeffnen` (alles Uebrige). `pipeOeffnen` behaelt seine Bedeutung „Oeffnen scheiterte auf eine Art, die auf einen fremden Besitzer deuten kann". |
| E3 | `serverPruefFehlerName` (`BrokerLifecycle.cpp:432-460`) bekommt den Fall `pipeBelegt` | Die Funktion ist die einzige Namensquelle; sie faellt sonst auf `"unbekannt"` und der Fehlertext der Anzeige loege. Es gibt **keine** Namensfunktion fuer `ServerPruefStatus` — der Status wird nirgends als Text ausgegeben, nur verglichen (`git grep`: kein `switch` ueber den Status im Produktcode). |
| E4 | **Der v2-`PipeClient` gehoert in den Aenderungssatz**, obwohl der Auftrag ihn nicht auffuehrt | `docs/beweise/NAK-123.md` Zeile **C-06** bindet: „Alle drei Pfade verwenden denselben Serverauth-Helfer" — und NAK-123 begruendet den v2-Retry selbst mit „C-06 verlangt fuer alle drei Connectpfade dieselbe Regel" (`NAK-123.md:313`). Wuerde nur v3 umklassifizieren, brechen zwei Zusagen: die drei Pfade wichen im selben Fehlerfall auseinander, und der v2-Produktpfad behielte exakt den Defekt, dessen Symptom NAK-123 dort schon einmal gesehen hat. Das ist kein Neben-Refactor, sondern dieselbe Regel auf der dritten Kante desselben Vertrags. `PipeClient.cpp` fuehrt seine eigene Oeffnungsschleife (`:371-405`) und erbt nichts automatisch. |
| E5 | `BrokerLifecycle.cpp`, `SourcesModel.cpp` und `PluginProcessor.cpp` werden **nicht** geaendert | Beide Konsumenten fuehren den neuen Status ohne Aenderung korrekt: der Spawnzweig verlangt `nichtDa` (`BrokerLifecycle.cpp:857-861`), die Anzeige faellt in ihren Else-Zweig `brokerUnavailable` **mit** Handgriff (`SourcesModel.cpp:410-418`). Beide Zeilen sind trotzdem Matrixzeilen mit Test — eine Zusage ohne Messung ist keine. |
| E6 | `namedPipeErreichbar` (`IpcVerbindung.cpp:19-30`) bleibt unveraendert | Sie meldet fuer `ERROR_PIPE_BUSY`/`ERROR_SEM_TIMEOUT` bereits „erreichbar" und steht damit schon auf der Livenessseite; R1 macht die Klassifikation im Oeffnungspfad nur gleichlautend. |
| E7 | Kein Schema, kein State, keine Fixture, kein Wire-Feld | Status und Fehler sind reine Laufzeitfelder des Client-Snapshots. Ein `git grep` ueber `eq-copilot/schemas/`, `eq-copilot/plugin/state/` und alle `*.json`/`*.py`/`*.ps1` findet keinen Verbraucher. Damit entfaellt die Versionierungspflicht aus `CLAUDE.md` („Schemas sind Vertraege"). |

### 4.1 Zwei Win32-Zusagen, an der Primaerquelle geprueft

Beide tragen den Testaufbau in Phase 2 und sind deshalb hier belegt, nicht angenommen.

| Zusage | Quelle | Wortlaut |
|---|---|---|
| `WaitNamedPipeW` laeuft bei belegter, aber existierender Instanz in sein Zeitlimit | Microsoft Learn, `WaitNamedPipeW` (Doku-Stand 2024-02-22) | „If no instances of the specified named pipe exist, the **WaitNamedPipe** function returns immediately, regardless of the time-out value." / „If the time-out interval expires, the **WaitNamedPipe** function will fail with the error **ERROR_SEM_TIMEOUT**." |
| Ein Client, der eine vom Servermodus nicht erlaubte Zugriffsart verlangt, bekommt `ERROR_ACCESS_DENIED` | Microsoft Learn, `CreateNamedPipeA` (Doku-Stand 2025-07-01) | „**PIPE_ACCESS_INBOUND** … The client must specify **GENERIC_WRITE** access when connecting to the pipe." |

Daraus folgen die zwei deterministischen Testaufbauten ohne echten Broker:

* **`PIPE_BUSY` erschoepft:** Testserver mit `nMaxInstances = 1`, die eine Instanz bereits durch
  einen eigenen Client belegt, **kein** ausstehendes `ConnectNamedPipe`. Der Pruefling faehrt seine
  20 Runden `WaitNamedPipeW(200)` leer (je Runde `ERROR_SEM_TIMEOUT`) und endet mit
  `ERROR_PIPE_BUSY`. Dauer je Fall rund **4.000 ms**, deterministisch.
* **`ACCESS_DENIED`:** Testserver mit `PIPE_ACCESS_INBOUND`. Beide Oeffnungspfade verlangen fest
  `GENERIC_READ | GENERIC_WRITE` (`IpcVerbindung.cpp:72`, `PipeClient.cpp:376`) und werden
  sofort abgewiesen. Dauer je Fall unter 50 ms.

Beide Tests pruefen den **beobachteten** `win32Fehler` mit, nicht nur die Klassifikation: faellt die
Mechanik anders aus als hier belegt, wird der Test rot statt still gruen.

## 5. Verhaltensmatrix D — Oeffnungsergebnis x Connectpfad

**Ausgangszustand fuer D-01 bis D-21:** Clientthread laeuft (`laeuft == true`), die
Verbindungsgeneration ist aktuell (`sollAbbrechen(generation) == false`), Hello und Adresse haben
den v3-Vertrag bereits bestanden. Pfade: **V3C** = `ControlClient`, **V3T** = `TelemetryClient`,
**V2** = `PipeClient` (Produktpfad v2).

Spalte „Zaehler" nennt nur, was sich **gegenueber dem Vorzustand aendert**.
Spalte „Startpfad" beantwortet: darf `BrokerLifecycle` daraus einen Spawn ableiten?
Spalte „C-Zeile" nennt die NAK-123-Zeile, die diese Zeile **erweitert** — keine wird ersetzt.

| ID | Oeffnungsergebnis | Pfad | Status | Fehler | Zaehler | Backoff oder Parken | Startpfad | C-Zeile | Test |
|---|---|---|---|---|---|---|---|---|---|
| D-01 | Handle offen **und** verifiziert | V3C | `verifiziert` | `keiner` | `verbindungsVersuche` +1, `serverPruefungen` +1 | weder noch: die Verbindung steht, der Backoff faellt auf 500 ms zurueck | nein (`bereit`, Startmutex frei) | C-02, C-06 | bestehend `IpcTestMain.cpp/serverauth_gueltiger_pid_sid_datei_hash_und_signer_vor_hello` |
| D-02 | Handle offen **und** verifiziert | V3T | `verifiziert` | `keiner` | `verbindungsVersuche` +1, `serverPruefungen` +1 | wie D-01 | nein | C-06 | bestehend `IpcTestMain.cpp/control_und_telemetry_authentisieren_jedes_handle` |
| D-03 | Handle offen **und** verifiziert | V2 | `verifiziert` | `keiner` | `serverPruefungen` +1 | wie D-01 | nein | C-06 | bestehend `PipeClientLifecycleTestMain.cpp/pipeclient_sendet_vor_serverauth_keine_bytes` |
| D-04 | Handle offen, `namedPipeServerAuthentisieren` faellt (PID/SID/Bild/Hash/Signer) | V3C | `belegtAberUnverifiziert` | die konkrete Klasse (`serverPidFalsch` bis `signerFalsch`) | `verbindungsVersuche` +1, `serverPruefungen` +1 | **Parken** — unbefristet auf `warte`, nur `reconnect()`/`stop()` loest | **nein** (`blockiertUnverifiziert`) | C-03, C-04, C-07 | bestehend `IpcTestMain.cpp/serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes`, `/serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed` |
| D-05 | wie D-04 | V3T | `belegtAberUnverifiziert` | wie D-04 | wie D-04 | **Parken** | **nein** | C-06 | bestehend `IpcTestMain.cpp/control_und_telemetry_authentisieren_jedes_handle` |
| D-06 | wie D-04 | V2 | `belegtAberUnverifiziert` | wie D-04 | `serverPruefungen` +1 | **Parken** | **nein** | C-06, C-09 | bestehend `PipeClientLifecycleTestMain.cpp/pipeclient_reconnect_verwirft_serverfreigabe` |
| D-07 | `CreateFileW` liefert `ERROR_FILE_NOT_FOUND` (2) | V3C | `nichtDa` | `pipeFehlt` | `verbindungsVersuche` +1; `serverPruefungen` **unveraendert** | Backoff 500 bis 8000 ms | **ja** — einzige Zeile, die ihn oeffnet | C-01 | bestehend `IpcTestMain.cpp/fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` |
| D-08 | wie D-07 | V3T | `nichtDa` | `pipeFehlt` | `verbindungsVersuche` +1 | Backoff 500 bis 8000 ms | nein (nur der Control-Snapshot speist den Lifecycle-Hook, `PluginProcessor.cpp:110-122`) | C-01 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `file_not_found`, Pfad Telemetrie) |
| D-09 | wie D-07 | V2 | `nichtDa` | `pipeFehlt` | keiner | Backoff 500 bis 8000 ms | nein (v2 speist den v3-Lifecycle nicht) | C-01 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `file_not_found`) |
| D-10 | 20 Runden `WaitNamedPipeW(200)` leer, `CreateFileW` liefert `ERROR_PIPE_BUSY` (231) | V3C | **`belegtNichtErreicht`** (neu) | **`pipeBelegt`** (neu) | `verbindungsVersuche` +1; `serverPruefungen` **unveraendert** | **Backoff 500 bis 8000 ms, kein Parken** | **nein** | erweitert C-01, C-05 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `pipe_busy`, Pfad Control) |
| D-11 | wie D-10 | V3T | **`belegtNichtErreicht`** | **`pipeBelegt`** | `verbindungsVersuche` +1; `serverPruefungen` unveraendert | **Backoff, kein Parken** | **nein** | erweitert C-06 | **NEU** derselbe Test, Pfad Telemetrie |
| D-12 | wie D-10 | V2 | **`belegtNichtErreicht`** | **`pipeBelegt`** | keiner | **Backoff, kein Parken** | nein | erweitert C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `pipe_busy`) |
| D-13 | `CreateFileW` liefert `ERROR_ACCESS_DENIED` (5) | V3C | `belegtAberUnverifiziert` | `pipeOeffnen` | `verbindungsVersuche` +1; `serverPruefungen` **unveraendert** (kein Handle, keine Pruefung) | **Parken** — unveraendert gegenueber heute | **nein** | C-01, C-07 | **NEU** `IpcTestMain.cpp/oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `access_denied`, Pfad Control) — Regressionswache |
| D-14 | wie D-13 | V3T | `belegtAberUnverifiziert` | `pipeOeffnen` | wie D-13 | **Parken** | **nein** | C-06 | **NEU** derselbe Test, Pfad Telemetrie — Regressionswache |
| D-15 | wie D-13 | V2 | `belegtAberUnverifiziert` | `pipeOeffnen` | keiner | **Parken** | nein | C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `access_denied`) — Regressionswache |
| D-16 | `CreateFileW` liefert einen sonstigen Win32-Fehler (weder 2 noch 5 noch 231) | V3C | `belegtAberUnverifiziert` | `pipeOeffnen` | wie D-13 | **Parken** | **nein** | C-01 | **NEU** derselbe Test, Fall `sonstiger_fehler` (deterministisch ueber einen Namen mit unzulaessiger Syntax, `ERROR_INVALID_NAME`) |
| D-17 | wie D-16 | V3T | `belegtAberUnverifiziert` | `pipeOeffnen` | wie D-13 | **Parken** | **nein** | C-06 | **NEU** derselbe Test, Pfad Telemetrie |
| D-18 | wie D-16 | V2 | `belegtAberUnverifiziert` | `pipeOeffnen` | keiner | **Parken** | nein | C-06 | **NEU** `PipeClientLifecycleTestMain.cpp/pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit` (Fall `sonstiger_fehler`) |
| D-19 | `stop()`/`reconnect()` waehrend der Oeffnungsschleife (`abbruch` gesetzt) | V3C | `nichtGeprueft` | `keiner` | `verbindungsVersuche` +1 | weder noch: der Thread sieht die neue Generation und startet **sofort ohne Backoff** neu bzw. endet | **nein** | C-09 | **NEU** derselbe Test, Fall `abbruch` |
| D-20 | wie D-19 | V3T | `nichtGeprueft` | `keiner` | `verbindungsVersuche` +1 | wie D-19 | **nein** | C-09 | **NEU** derselbe Test, Pfad Telemetrie |
| D-21 | wie D-19 | V2 | `nichtGeprueft` | `keiner` | keiner | wie D-19 | nein | C-09 | bestehend `PipeClientLifecycleTestMain.cpp/reconnectGeneration`, um den Fall erweitert |

**Lesart der Zeilen D-13 bis D-18:** sie aendern nichts. Sie stehen in der Matrix, weil R1 den
Geltungsbereich von `belegtAberUnverifiziert` **verengt** — und eine Verengung ist erst dann eine
Zusage, wenn gemessen ist, was **nicht** mitgewandert ist. Ohne diese sechs Zeilen waere „nur
`PIPE_BUSY` wechselt die Klasse" eine Behauptung ohne Messung (Pruefliste E).

## 6. Verhaltensmatrix E — Folgeverbraucher des neuen Status

| ID | Ausgangszustand x Ereignis | Zusage | Startpfad | Test |
|---|---|---|---|---|
| E-01 | `BrokerLifecycle` in `wartetAufConnect`, Control meldet `belegtNichtErreicht` x `tick()` | Kein Spawn, kein `blockiertUnverifiziert`, `zustand.serverNichtVerifiziert` bleibt **false**, kein `cooldowns`-Zuwachs, kein `startMutexNehmen`. Der Spawnzweig verlangt `nichtDa` (`BrokerLifecycle.cpp:857-861`), der Sperrzweig verlangt `belegtAberUnverifiziert` (`:733`). Der neue Wert trifft keinen von beiden und faellt in den `return`. | **nein** | **NEU** `IpcTestMain.cpp/belegt_nicht_erreicht_spawnt_nicht_und_blockiert_nicht` |
| E-02 | `BrokerLifecycle` in `wartetAufServerpruefung`, Control meldet `belegtNichtErreicht` x `tick()` | Die Phase geht nach `wartetAufConnect` und `letzterServerPruefstatus` traegt den neuen Wert (`:802-813`). Auch danach kein Spawn, solange der Status nicht `nichtDa` wird. | **nein** | **NEU** derselbe Test, zweiter Fall |
| E-03 | `SourcesModel` hat eine Sicht, Control meldet `getrennt` + `belegtNichtErreicht` x `setzeControlTransport` | Diagnose `brokerUnavailable` (Text `Broker unavailable`) **mit** Handgriff (`diagnoseHatHandgriff == true`), **nicht** `serverUnverified`. Das ist die ehrliche Aussage: der Broker ist gerade nicht erreichbar und der User hat einen Handgriff, statt einer Sicherheitswarnung, die nichts behauptet. | — | **NEU** `Sonde012SourcesModelTest.cpp/belegt_nicht_erreicht_zeigt_broker_unavailable_mit_handgriff` |
| E-04 | Geparkter Client (D-04/D-13) x `reconnect()` | Die Sperre loest, die Generation wechselt, `backoffMs` faellt auf 500 ms, ein neuer Oeffnungsversuch laeuft. Gegenpfad parken↔loesen bleibt vollstaendig. | — | bestehend `IpcTestMain.cpp/unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht` (Sperrseite) **plus NEU** Loesungsseite im selben Test |
| E-05 | Geparkter Client (D-04/D-13) x `stop()` | Die Sperre loest, der Thread endet, `fertig` wird gesetzt, `join` kehrt innerhalb seiner Frist zurueck. Gegenpfad starten↔stoppen bleibt vollstaendig. | — | bestehend `LebenslaufTestMain.cpp` (Stop-Fristen) **plus NEU** Fall „Stop loest die Auth-Sperre" |
| E-06 | Client im Backoff nach `belegtNichtErreicht` x `stop()`/`reconnect()` | Das `wait_for` endet an derselben Bedingung; kein neuer Wartepfad, keine zweite Sperre. | — | **NEU** im Test aus D-10 mitgemessen |
| E-07 | Beliebig x Audiocallback laeuft waehrend Klassifikation, Backoff und Reconnect | Keine neue Sperre, Allokation, Datei-, Pipe- oder Prozessoperation im `processBlock`. Der neue Statuswert existiert nur auf Client- und Lifecycle-Threads. | — | bestehend `LebenslaufTestMain.cpp/processblock_quellschnitt_{datei,pipe,prozess}operationen_null`, `/broker_lifecycle_aufrufe_im_audiothread_null` |
| E-08 | Beliebig x Save/Load, Migration, Wire | Kein persistentes Feld, kein Wire-Feld, keine Fixture beruehrt; `ServerPruefStatus` und `ServerPruefFehler` bleiben reine Laufzeitgroessen. | — | bestehend `StateMigrationTestMain.cpp`, `SchemaTestMain.cpp` — unveraendert gruen ist hier die Zusage |

## 7. Callback-Reihenfolge und Fristen

Die Reihenfolge im Oeffnungspfad ist unveraendert und bleibt die NAK-123-Reihenfolge:

```
CreateFileW  <  namedPipeServerAuthentisieren  <  Handle veroeffentlichen  <  Hello schreiben
             <  Welcome lesen  <  beiLinkStatus(true)
```

Fuer **jede** Zeile D-07 bis D-21 gilt: die Kette bricht vor `namedPipeServerAuthentisieren` ab.
Damit wird **kein** Handle veroeffentlicht, **kein** Byte gesendet und `beiLinkStatus(true)` **nie**
gerufen (`ControlClient.cpp:1252` ist der einzige `true`-Aufrufer und liegt hinter dem Welcome).
Ein zuvor gemeldetes `true` ist bereits beim Verbindungsende auf `false` gefallen
(`ControlClient.cpp:1510`); die `exchange`-Wache in `meldeLinkStatus` (`:970-977`) verhindert den
doppelten Ende-Callback.

| Ausgang | Frist bis zur Klassifikation | Frist bis zum naechsten Versuch |
|---|---|---|
| D-07 bis D-09 (`FILE_NOT_FOUND`) | sofort — die Schleife bricht in Runde 1 ab | 500 ms, verdoppelt bis 8.000 ms |
| D-10 bis D-12 (`PIPE_BUSY` erschoepft) | **20 x 200 ms = 4.000 ms** obere Schranke (`IpcVerbindung.cpp:67-89`) | 500 ms, verdoppelt bis 8.000 ms — **neu**; heute: nie |
| D-13 bis D-18 (`ACCESS_DENIED`, sonstige) | sofort — die Schleife bricht in Runde 1 ab | keiner: Parken bis `reconnect()`/`stop()` |
| D-19 bis D-21 (`abbruch`) | sofort | sofort, ohne Backoff (Generationswechsel) |
| D-01 bis D-06 | `CreateFileW` plus voller Serverbeweis | — |

Die Schranke aus `docs/beweise/G3-SOAK.md` §3.1 (45.051 ms je Clientpaar) bleibt **unveraendert
gueltig**: sie rechnet bereits mit `8.000 ms Backoff + 4.000 ms WaitNamedPipe-Schleife + 2 x 5.000 ms
Bootstrap-E/A` je Runde und Client. R1 aendert keine dieser Konstanten. Was R1 aendert, ist, dass die
Rechnung fuer einen `PIPE_BUSY`-Verlierer ueberhaupt **weiterlaeuft**, statt an dieser Stelle
abzubrechen.

## 8. Was R1 **nicht** zusagt

R1 beseitigt das dauerhafte Parken. R1 sagt **nicht** zu, dass alle 33 Clientpaare nach einem
Neustart innerhalb der Z2-Frist von 60 s zurueck sind. §3.1 haelt das ausdruecklich fest: „weder
`IpcVerbindung::oeffnen` noch die Annahmeschlange des Listeners geben eine Fairnessgarantie, also
kann ein einzelnes Paar Rennen wiederholt verlieren, waehrend andere durchkommen."

Daraus folgt die Falsifikation fuer Phase 2, vor dem Lauf festgelegt:

* Wird Lauf **(c)** (`--sonden 32 --minuten 30 --neustarts 3`) gruen, ist der Defekt geschlossen und
  die Fairness des Brokers reicht fuer N = 32.
* Bleibt er rot **mit** Backoff-Verkehr (`k_s5.backoff_deckel_erreicht == 33`, `reconnect_paare`
  kleiner als 33), ist R1 wirksam, aber die verbleibende Ursache liegt in der Zahl der scharfen
  Listener-Instanzen — das ist **Register NAK-135** und ausdruecklich nicht dieses Ticket. Dieses
  Manifest traegt den Befund dann als Zeile, nicht als stillen Fehlschlag.

Die Unterscheidung ist genau der Grund fuer R3(b): ohne einen ehrlich gemessenen `reconnect_paare`
laesst sich „geparkt" nicht von „hat das Rennen wieder verloren" trennen.

## 9. Verhaltensmatrix W — Werkzeug A24 (R3)

Beide Zeilen sind **Pruefwerkzeug**. Sie aendern kein Produktverhalten und keinen Bericht des
Produkts.

| ID | Heute | Zusage nach R3 | Berichtsfeld | Pruefer | Rotmutante |
|---|---|---|---|---|---|
| W-01 | `SessionSoakMain.cpp:777-785` nimmt eine abgewiesene P0-Einreihung still zurueck (`gesendet.pop_back()`); der Heartbeat gilt als nie eingereiht und faellt aus jeder Statistik | Die Ruecknahme bleibt (sie ist richtig: nie eingereiht heisst nie erwartet), aber sie wird **gezaehlt** — getrennt nach innerhalb/ausserhalb der Neustartfenster, mit derselben Fensterlogik wie `imFensterGesendet` | `p0.einreihung_abgelehnt` (Summe, Pflichtfeld) und `p0.einreihung_abgelehnt_ausserhalb_neustart` | S04: `einreihung_abgelehnt_ausserhalb_neustart == 0`. Die Summe steht als Pflichtfeld daneben; ein fehlendes Feld ist ROT, nie uebersprungen (§7). | **NEU** `--mutant p0_einreihung`: setzt `einreihung_abgelehnt_ausserhalb_neustart` auf 1 — S04 muss fallen |
| W-02 | `reconnect_paare` (`SessionSoakMain.cpp:1270`) ist `n.reconnectMs.size()`, also strukturell `anzahl+1`. Der Pruefer vergleicht ihn in `pruefe_session_soak.py:920` gegen `args.sonden + 1` — eine Tautologie, dieselbe Klasse wie der `k_s5.erwartet`-Defekt aus §16 | `reconnect_paare` ist die **gemessene** Zahl der Paare, deren Control **und** Telemetrie am Ende des Fristfensters wieder `verbunden` waren (Main-Paar plus N Sonden). Bewusst **ohne** die Mitgliedschaftsbedingung aus `proPaar`: damit trennt der Bericht „Transport steht wieder" von „Mitglied ist wieder in der Sicht" — genau die Unterscheidung aus Abschnitt 8 | `reconnect_paare` (Bedeutung geaendert, Feldname gleich) | S11 (`pruefe_session_soak.py:920`) bleibt wortgleich `== args.sonden + 1` und hoert damit auf, tautologisch zu sein | bestehend `--mutant s11_paare` (`pruefe_session_soak.py:665`) trifft genau dieses Feld; er wird nach der Umstellung erneut gefahren und im Manifest mit Exitcode belegt |
| W-03 | — | `docs/beweise/G3-SOAK.md` §5 (Zeilen S04, S11) und §7 (Zaehlerkarte) tragen beide Zaehler mit ihrer Bedeutung; **append-only Nachtrag mit Datum**, der lebende Kopf wird nachgezogen, der Verlauf nie umgeschrieben (Pruefliste E) | — | — | — |

## 10. Testkarte — was neu ist und wie es rot wird

Pruefliste E verlangt: „Jede neue Pruefung wurde einmal absichtlich gebrochen … Eine Probe, die auch
ohne den Fix rot ist, heisst Regressionswache, nicht Beleg." Die Klasse steht deshalb je Test.

| Test | Bein | Neu? | Rotbeweis-Klasse | Wie er rot wird |
|---|---|---|---|---|
| `oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Faelle `pipe_busy` x {Control, Telemetrie} | `EqCopIpcTest` (B10) | **NEU** | **Beleg** — rot **vor** dem Produktfix | Am alten Code meldet der Fall `belegtAberUnverifiziert`/`pipeOeffnen` und der Thread parkt. Lauf und Exitcode stehen in Phase 2 unter „Rotbeweis vor dem Fix". |
| dieselbe Funktion, Faelle `access_denied`, `sonstiger_fehler`, `file_not_found`, `abbruch` | `EqCopIpcTest` (B10) | **NEU** | **Regressionswache** — vor und nach dem Fix gruen | Sie messen, was **nicht** wandert. Rot werden sie nur ueber die Fixmutante (Klassifikation auf `belegtNichtErreicht` ausgeweitet). |
| `pipeclient_oeffnungsausgaenge_sind_liveness_oder_sicherheit`, Fall `pipe_busy` | `EqCopPipeClientTest` | **NEU** | **Beleg** — rot vor dem Fix | wie oben, auf dem v2-Pfad |
| dieselbe Funktion, uebrige Faelle | `EqCopPipeClientTest` | **NEU** | **Regressionswache** | wie oben |
| `belegt_nicht_erreicht_spawnt_nicht_und_blockiert_nicht` | `EqCopIpcTest` (B10) | **NEU** | **Mutante** | Vor dem Fix laesst sich die Zeile nicht ausdruecken — den Statuswert gibt es nicht. Der Rotbeweis ist deshalb eine Mutante **nach** dem Fix: `BrokerLifecycle` behandelt `belegtNichtErreicht` wie `nichtDa`; der Test muss den Spawn sehen und fallen. Das steht hier offen, statt einen Vor-Fix-Rotlauf zu behaupten, den es nicht geben kann. |
| `belegt_nicht_erreicht_zeigt_broker_unavailable_mit_handgriff` | `EqCopSonde012SourcesModelTest` (B13) | **NEU** | **Mutante** | wie oben; Mutante: `SourcesModel` bildet den neuen Status auf `serverUnverified` ab |
| Loesungsseite in `unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht` (E-04) | `EqCopIpcTest` (B10) | **NEU** (Erweiterung eines bestehenden Tests) | **Regressionswache** | misst den Gegenpfad parken↔loesen, der heute schon haelt |
| Stop loest die Auth-Sperre (E-05) | `EqCopLebenslaufTest` | **NEU** | **Regressionswache** | Gegenpfad starten↔stoppen |
| `--mutant p0_einreihung` (W-01) | `pruefe_session_soak.py` | **NEU** | **Mutante** an einem gespeicherten Bericht | Pflicht nach G3-SOAK §14/§15.3 |
| `--mutant s11_paare` (W-02) | `pruefe_session_soak.py` | bestehend | **Mutante**, nach der Umstellung erneut gefahren | Pflicht nach G3-SOAK §14 |

**Ein Test, viele Faelle — mit Absicht.** Die 15 Zeilen D-07 bis D-21 liegen in genau zwei
Funktionen (v3 und v2), je als Fallschleife ueber `{Ausgang, Pfad}`. Vorbild sind
`serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed` und
`serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` (`IpcTestMain.cpp:974-1049`), die
ihre Fehlerkanten ebenso einzeln durchfahren und je Kante einzeln melden. Jede Zeile behaelt damit
ihren eigenen sichtbaren Pruefpunkt, ohne 15 fast gleiche Funktionen.

## 11. Pruefliste `tools/dirigent/pruefliste.md` — Stand Phase 1

Phase 1 aendert keinen Code; die Zeilen stehen mit dem Ort, an dem Phase 2 sie messen **wird**, und
mit dem, was Phase 1 bereits belegt hat.

| Zeile | Stand Phase 1 | Messort |
|---|---|---|
| A — Politik bei *voll* steht ausdruecklich | offen fuer Phase 2 | W-01: die P0-Ablehnung bekommt einen Zaehler statt einer stillen Ruecknahme; `SessionSoakMain.cpp:777-785` |
| A — Rueckgabewerte und Zaehler werden ausgewertet, ein strukturell-0-Zaehler ist eine Wache mit Test | offen fuer Phase 2 | W-01 (`einreihung_abgelehnt_ausserhalb_neustart == 0` mit Mutante `p0_einreihung`) und W-02 (`reconnect_paare` hoert auf, tautologisch zu sein) |
| A — dieselbe Regel in allen Sprachen | **erfuellt in Phase 1 als Abgrenzung** | R1 beruehrt nur die C++-Clientseite; die Broker-Rust-Seite (Zahl scharfer Listener) ist Register NAK-135, Abschnitt 8 |
| B — Reihenfolge beim Verbinden festgelegt und gemessen | **erfuellt in Phase 1** (Spezifikation) | Abschnitt 7: die NAK-123-Kette bleibt woertlich; jede neue Zeile bricht **vor** dem Serverbeweis ab |
| B — Gegenpfade, jeder Join hat eine Frist | **spezifiziert**, Messung Phase 2 | E-04, E-05, E-06 |
| C — Vertraege und Laengen | **nicht anwendbar, begruendet** | E7 in Abschnitt 4: kein Schema-, State-, Fixture- oder Wire-Feld beruehrt |
| D — Riegel fail-closed ohne Rohtextheuristik | **erfuellt in Phase 1** (Spezifikation) | D-16 bis D-18: jeder **unbekannte** Win32-Fehler bleibt auf der Sicherheitsseite. R1 verengt nur um **zwei namentlich genannte** Codes; das Unbekannte bleibt fail-closed |
| E — Behauptung ist nie groesser als die Messung | **erfuellt in Phase 1** | Abschnitt 8 nennt, was R1 **nicht** zusagt; Abschnitt 10 nennt je Test die Rotbeweis-Klasse, statt fuer alle „rot vor dem Fix" zu behaupten |
| E — Zahlen sind gemessen, nicht abgeschrieben | **erfuellt in Phase 1** | Abschnitt 2.2: alle Zahlen aus `roh/G3-soak-n32-30min-ad5b2d1.json` gelesen, nicht aus dem Gate-Manifest uebernommen. Dabei zeigte sich `evicted_ausserhalb_neustart = 13.440`, das in der Kurzfassung des Defekts nicht vorkam |
| E — geaenderte Zusage: drei Stellen | offen fuer Phase 2 | W-03: `G3-SOAK.md` §5/§7 als datierter append-only Nachtrag; Skriptkopf `pruefe_session_soak.py`; lebender Kopf dieses Manifests |
| F — Aenderungssatz vollstaendig | **spezifiziert** | verbinden↔trennen (E-04), starten↔stoppen (E-05) und parken↔loesen (E-04/E-05) liegen im selben Satz wie die Klassifikation; alle drei Connectpfade ebenfalls (E4 in Abschnitt 4) |

## 12. Aenderungssatz Phase 2 — vorab benannt

| Datei | Aenderung |
|---|---|
| `eq-copilot/plugin/core/ipc/IpcVerbindung.h` | `ServerPruefStatus::belegtNichtErreicht`, `ServerPruefFehler::pipeBelegt`, Kommentar an `:41-42` auf die neue Dreiteilung |
| `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp` | Klassifikation `:90-99` dreiwertig |
| `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp` | **nur** `serverPruefFehlerName` um `pipeBelegt` (E3); die `tick`-Logik bleibt unangetastet (E5) |
| `eq-copilot/plugin/core/ipc/ControlClient.cpp` | `serverPruefungen` zaehlt `belegtNichtErreicht` nicht (`:1101-1103`); `authBlockiert` bleibt an `belegtAberUnverifiziert` gebunden (`:994-998`), dort also keine Aenderung |
| `eq-copilot/plugin/core/ipc/TelemetryClient.cpp` | dasselbe (`:554-556`, `:407-421`) |
| `eq-copilot/plugin/src/PipeClient.cpp` | dieselbe Klassifikation (`:398-405`), Begruendung E4 |
| `eq-copilot/plugin/tests/IpcTestMain.cpp` | Tests D-07 bis D-21 (v3), E-01, E-02, E-04 |
| `eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp` | Tests D-09 bis D-21 (v2) |
| `eq-copilot/plugin/tests/Sonde012SourcesModelTest.cpp` | Test E-03 |
| `eq-copilot/plugin/tests/LebenslaufTestMain.cpp` | Test E-05 |
| `eq-copilot/plugin/tests/SessionSoakMain.cpp` | W-01, W-02 |
| `tools/eq-copilot/pruefe_session_soak.py` | W-01 (S04-Zeile, Mutant `p0_einreihung`) |
| `docs/beweise/G3-SOAK.md` | W-03, datierter append-only Nachtrag |

**Nicht angefasst:** `broker/**` (NAK-135), Schemas, Fixtures, State, Audio-Thread-Code,
`docs/beweise/G3.md`, `docs/PLAN-STAND.md`. Kein Produktions-Pipename in irgendeinem Test.

## 13. Beweislaeufe Phase 2 — offen

| Lauf | Form | Erwartung | Ergebnis |
|---|---|---|---|
| (a) | `py -3.13 tools/eq-copilot/pruefe_session_soak.py --sonden 32 --minuten 4 --neustarts 2`, **vor** dem Produktfix, **mit** dem neuen Werkzeug | **ROT** (`k_s5.backoff_deckel_erreicht` unter 33; `reconnect_paare` unter 33) | `NOT RUN` |
| (b) | dieselbe Form **nach** dem Fix | GRUEN | `NOT RUN` |
| (c) | `--sonden 32 --minuten 30 --neustarts 3` (Gate-Form) | GRUEN, sonst Abschnitt 8 | `NOT RUN` |
| (d) | `--sonden 16 --minuten 2 --neustarts 1` (Kanon-Kurzform) | GRUEN | `NOT RUN` |
| Rotbeweis Test | `EqCopIpcTest` und `EqCopPipeClientTest` vor und nach dem Fix, beide Exitcodes | vorher rot an `pipe_busy`, nachher gruen | `NOT RUN` |
| Kanon | `tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-134.md -Anhaengen -Titel NAK-134` | GRUEN | `NOT RUN` |

Rohausgaben nach `docs/beweise/roh/NAK-134-<lauf>.txt`, Berichte als `.json` daneben.

## 14. Verlauf

### Phase 1 — Verhaltensmatrix vor Code, 2026-09-03

Basis `4789f2a`. Kein Code geaendert. Alle Quellenangaben in Abschnitt 2.1 an der Quelle
nachgelesen; die Zahlen in Abschnitt 2.2 aus dem Rohbericht gerechnet. Die zwei Win32-Zusagen in
Abschnitt 4.1 gegen die aktuelle Microsoft-Learn-Primaerquelle geprueft.

Matrixzeilen: **21** in Matrix D (Oeffnungsergebnis x Connectpfad), **8** in Matrix E
(Folgeverbraucher), **3** in Matrix W (Werkzeug) — zusammen **32**.

Drei Entscheide dieser Phase, die ueber den Auftragstext hinausgehen und dort begruendet stehen:
**E4** (der v2-`PipeClient` gehoert in den Aenderungssatz, sonst bricht NAK-123 C-06), **E5**
(`BrokerLifecycle` und `SourcesModel` brauchen keine Aenderung, bekommen aber je eine Matrixzeile
mit Test) und die Rotbeweis-Klassifizierung in Abschnitt 10 (drei der neuen Tests koennen
konstruktiv **nicht** vor dem Fix rot laufen; sie werden als Mutante belegt, nicht als
Vor-Fix-Rotlauf behauptet).
