SONDE-013 / Planschritt S20–22 — ETAPPE 1, NACHARBEIT RUNDE 1: sieben Defekte der Matrixprüfung 1 schließen. Kein Produktcode.

ROLLE UND ORT
Du bist der Nacharbeits-Worker (Opus) im sichtbaren Checkout C:/Users/phili/Projekte/Nakama, Branch master. Stand vor dieser Runde: 367a0ea732c9e883df12f2bfc5abb2f67df6da5d (Ticket-Basis bleibt ed9bbf7). Der Dirigent (Fable) misst nur am Repo. Lies zuerst CLAUDE.md, dann docs/beweise/SONDE-013.md vollständig (Kopf, §1 Gate-Text, §2 Ist-Stand, §3 Matrix, §4 offene Punkte, §5 Bauplan, §7 Dirigentenentscheide) und das Rohurteil docs/beweise/roh/SONDE-013-matrixpruefung-1-367a0ea.txt (Codex gpt-5.6-sol, high, lesend, Thread 01a06922-fd1d-7222-b180-fcddb1fdfc2d). Die Prüfung ergab NEEDS_WORK mit sieben Befunden. Der Dirigent hat alle sieben an der Quelle als Defekt bestätigt und je Befund die Regel festgelegt. Du setzt genau diese sieben Regeln im Manifest um, nicht mehr. Drei Regeln ersetzen Entscheide des Dirigenten in §7 (E-02, E-03, E-04); der neue Wortlaut steht unten und gilt.

DIE SIEBEN DEFEKTE — je Befund wörtlich (Codex), dann die Regel des Dirigenten (03.09.2026)

D1 — E-02, Manifestzeile um 1004 (§7.1). Codex: „E-02 belegt `experiment_result` für SONDE-013, obwohl die bestehende Reservierung die gesamte Familie SONDE-017 zuweist (eq-copilot/schemas/v3/reservierte-nachrichten-v1.json:156-158). Der Vertrag kennt nur reservierte Familien, keine teilreservierten Discriminator-Zweige (tools/eq-copilot/pruefe_v3_vertrag.py:1063-1079): Ein `active_probe`-Zweig im `oneOf` wäre bereits definiert, ein fehlender Zweig lediglich unbekannt. Damit greift E-02 einem fremden Ticket vor und widerspricht zugleich M-73."
REGEL (ersetzt E-02): `experiment_result` bleibt reserviert bei SONDE-017; SONDE-013 belegt weder einen fremden Namen noch einen Teilzweig. SONDE-013 definiert stattdessen die NEUE Familie `experiment_manual_result` (Eigentümer SONDE-013, direkt in `definiert`; `gesamt_erwartet` steigt von 27 auf 28, die Summenprüfung in pruefe_v3_vertrag.py zieht mit). Nutzlast: `experiment_id`, Zieladresse, `base_revision`, `ttl_ms`, Schema-/Capability-Version wie jede steuernde Nachricht (M-66), dazu Hörurteil, aufgedeckte Blindreihenfolge, Usernotiz und optionaler Werkzeugname; keine State-Hashes, keine Candidate-Revision, kein Match-Gain. Der Broker rechnet Zielmetrik, Guardrail-Deltas und Effektstabilität aus Baseline- und Resultatevidenz im Store und schreibt das terminale `result`-Ereignis mit `execution_mode = manual_external` append-only in `experiment_events`; der Rückweg zu Gen läuft über den bestehenden Outbox-/Snapshot-Pfad. Versionsschritt: eine neue Fassung 2 des `wire_envelope_schema_minor` im Register (aktuell 1 → 2), deren `fassungen."2"`-Eintrag die drei neu definierten Familien nennt (`experiment_begin`, `experiment_abort`, `experiment_manual_result`); ein Leser der Fassung 1 lehnt sie ab. Nachziehen: §7.1 E-02 (Wortlaut ersetzen, alter Wortlaut als durchgestrichen oder in §8 als Verlauf), M-49 (Wirefamilie `experiment_manual_result`, nicht „Store-Ereignis statt Wirefamilie"), M-66 (drei Familien: zwei aus `reserviert`, eine neu; Summe 28, Aufteilung 21/7), M-73 (unverändert: `experiment_result` und `user_verdict` bleiben fremd), §3.8, §5.2, §5.4. Tests: A5-Fälle `experiment_manual_result_definiert`, `experiment_result_bleibt_reserviert_fuer_sonde017`, `summe_ist_28`; B10 EqCopIpcTest für die C++-Hälfte; sonde013_experiment.rs `manual_result_writes_terminal_event_and_deltas`.

D2 — E-04, Manifestzeile um 1006 (§7.1). Codex: „E-04 verlangt den Versionsschritt im `steuerkopf`, aber `evidence_invalidate` besitzt keinen solchen Kopf, sondern nur `type`, `grund` und `umfang` (eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1085-1094). Die geltende Fassungswahl erfolgt über `message_family` und `schema_minor` des Wire-Envelopes (reservierte-nachrichten-v1.json:7-11); die aktuelle Regel würde daher entweder einen unbegründeten Steuerkopf erfinden oder die strikte Enum ohne wirksame Fassungswahl ändern."
REGEL (ersetzt E-04): Kein Steuerkopf. Die zwei neuen Gründe `material_wechsel` und `messpunkt_wechsel` kommen mit derselben Fassung 2 des `wire_envelope_schema_minor` (Eintrag in `fassungen."2"`, etwa `evidence_invalidate_grund_erweitert: ["material_wechsel", "messpunkt_wechsel"]`); der Empfänger wählt die Schemafassung anhand `message_family` und `schema_minor` im Wire-Envelope, im Payload entsteht kein weiteres Versionsfeld. Ein Leser der Fassung 1 lehnt die neuen Gründe ab (Negativfixture), Leser beider Sprachen (A5/A8 Rust, B3c C++) tragen die Fassung 2; A9 ist nur berührt, wenn ein .fbs angefasst wird — hier nicht. Nachziehen: §7.1 E-04, M-54, §3.8, §5.2.

D3 — M-23, Manifestzeile um 465. Codex: „Für ein Messfenster mit Seek ordnet M-23 nur `probable` an. §58 verlangt dagegen ausdrücklich Invalidierung bei Seek (docs/FL-Nakama-Sonden-Design-Entwurf.md:4285), und M-53 selbst sagt, dass der Bruch die alte Zeitreihe beendet und kein Fenster über die Grenze fortgeführt wird. Der geplante Test würde somit genau das Weiterverwenden von Evidenz festschreiben, das das Gate ausschließt."
REGEL: Der Herabstufungsgrund „Loop-/Seek-Sprünge im Messfenster" entfällt im v3-Pfad; er ist v2-Erbe (broker/src/lib.rs:809-826, Plan §5.7) und bleibt als Vorläufer nur benannt. Im v3-Pfad gilt: Eine Passage bindet an genau eine Transportepoche (§32.4). Ein Seek, ein Loop-Wrap innerhalb der Passage (Loop kürzer als die Passage) oder ein Drop während der laufenden Passagenmessung invalidiert diese Messung (M-53, Grund `epochwechsel` beziehungsweise `sequenzluecke`); die Messung beginnt mit der nächsten gültigen Epoche neu, und kein Frame, der eine Grenze überspannt, geht in eine Paarauswertung ein (B5-Garantie aus SONDE-009). Ein Loop-Wrap an der Passagengrenze (Loop deckt die Passage vollständig) beendet einen Durchlauf und beginnt den nächsten; das ist keine Herabstufung. Die drei übrigen Herabstufungsgründe (Überdeckung unter 80 % des kürzeren Fensters, aktive Messzeit um mehr als 10 % verschieden, verschiedene Host-PIDs) bleiben `probable`. Nachziehen: M-23 (drei Gründe), Test `each_downgrade_reason_falls_alone` auf drei Fälle; NEU sonde013_taint.rs `seek_inside_passage_invalidates_instead_of_downgrading` und `loop_wrap_at_passage_edge_starts_next_pass`.

D4 — M-47, Manifestzeile um 504. Codex: „M-47 verlangt samt Test, dass ein Sitzungsende jedes offene Experiment abbricht, während der bindende Entscheid E-03 ausdrücklich festlegt, dass Sitzungsende, Reconnect und UI-Neustart nicht abbrechen und `restart_keeps_open_experiment` bestehen muss. Beide Anforderungen landen in Etappe G und können nicht gleichzeitig erfüllt werden; zudem verlangt §43.1 die Rekonstruierbarkeit nach Reconnect und UI-Neustart (docs/FL-Nakama-Sonden-Design-Entwurf.md:2839-2845)."
REGEL: E-03 gewinnt. Auslöser eines terminalen `aborted` sind genau: expliziter Userabbruch (`experiment_abort`) und Verdrängung durch einen Deckel (D5). Sitzungsende, Projektwechsel, Reconnect, UI-Neustart und Brokerneustart brechen nicht ab; ein offenes Experiment überdauert sie und ist danach rekonstruierbar (§43.1). Nachziehen: M-47 (Auslöserliste), Test `abort_writes_terminal_event_for_each_trigger` misst genau diese zwei Auslöserklassen und zusätzlich negativ, dass Sitzungsende und Neustart KEIN Ereignis schreiben; `restart_keeps_open_experiment` bleibt.

D5 — E-03, Manifestzeile um 1005 (§7.1). Codex: „Der Deckel E-03 gilt nur je Projektbindung. Werden in fortlaufend neuen Projekten jeweils bis zu `N` Experimente geöffnet, wächst der globale Bestand offener Zeilen weiterhin unbegrenzt, weil weder Wanduhr noch Sitzungsende schließen. Das verletzt die verbindliche Zusage „offene Zeilen sammeln sich nicht unbegrenzt an" (docs/FL-Nakama-Sonden-Design-Entwurf.md:2842-2845); erforderlich ist zusätzlich eine projektübergreifende Grenze oder ein anderer terminaler Retentionspfad."
REGEL (ergänzt E-03): Zwei Deckel, beide benannte Konstanten des Stores, kein Literal im Pfad, vom Bauer begründet gewählt: `N_projekt` je Projektbindung (mindestens 2) und `N_global` über alle Projektbindungen (mindestens `N_projekt`). Beim Überschreiten eines Deckels erhält das älteste offene Experiment des jeweiligen Bereichs ein terminales `aborted` mit Grund `verdraengt`, bevor das neue angelegt wird; keine alte Zeile wird geändert. Terminale und benannte Experimente bleiben, bis der User sie löscht (Roadmap 15). Nachziehen: §7.1 E-03, M-48; Tests `open_cap_per_project_at_n_and_n_plus_one` und `open_cap_global_at_n_and_n_plus_one` (Grenztests, Muster C-09).

D6 — M-05, Manifestzeile um 442. Codex: „M-05 behandelt den lokalen `DynamicsEvent`-Strom als bereits vollständig und plant nur dessen Transport. Tatsächlich erzeugt `FeatureEngine` ein Ereignis ausschließlich bei `fluss > schwelle`, setzt dabei `qualitaetFluss` immer auf `true` und führt den Peakpfad nur als Zusatzbit (FeatureEngine.h:1605-1621); Peaksteigung wird gar nicht verwendet. Damit fehlen der eigenständige Gegenbeleg für sehr kurze Impulse und ein Test dafür, obwohl §39.1 den Detektor aus spektralem Fluss, Peaksteigung und Crest ausdrücklich verlangt (docs/FL-Nakama-Sonden-Design-Entwurf.md:2592-2599)." (Dirigent an der Quelle bestätigt: FeatureEngine.h:1607-1621.)
REGEL: §2.1 wird korrigiert (Ereignis nur bei Flussüberschreitung, `qualitaetFluss` konstant true, Peaksteigung unbenutzt). M-05 bleibt der Transport; eine NEUE Zeile (nächste freie ID) legt den eigenständigen Peakpfad fest: Peaksteigung (Anstieg des Rahmenpeaks gegenüber dem Vorrahmen in dB je Rahmen) und Crest erzeugen ein Ereignis auch ohne Flussüberschreitung, mit `qualitaetFluss = false` und `qualitaetPeak = true`; Flussereignisse tragen `qualitaetFluss = true`; lösen beide Pfade im selben Rahmen aus, wird genau ein Ereignis mit beiden Bits abgelegt; Ring-Deckel 64 und Verlustzähler bleiben; die Schwellen der Peaksteigung wählt der Bauer begründet und misst sie am Golden. Tests: B5 NEU `short_impulse_triggers_peak_path_only`, `flux_event_carries_flux_quality`, `both_paths_yield_one_event`. Nachziehen: §3.1, §3.11, §5.2.

D7 — M-11, Manifestzeile um 448. Codex: „M-11 lässt die im Entwurf vorgeschriebene Interchannel-Phase vollständig aus und bindet auch Fensterdauer und Freiheitsgrade nicht an die Evidenz. §40.1 verlangt beides ausdrücklich und erlaubt die Phaseninterpretation nur in ausreichend kohärenten Bändern (docs/FL-Nakama-Sonden-Design-Entwurf.md:2632-2645); keiner der genannten Tests misst diese Felder oder deren fail-closed Auswertung."
REGEL: M-11 nimmt auf: bandweise Interchannel-Phase, ausgewertet nur in Bändern, deren Kohärenz eine benannte Schwelle überschreitet, sonst `null`; Fensterdauer (ms) und Freiheitsgrade (Zahl der gemittelten gültigen Welch-Frames) als Felder der Evidenz je Band; Kohärenz `null` bei zu wenig Energie oder weniger als acht Frames. Die Felder reisen auf dem Evidenzpfad (E-05) und werden in §3.8 als von SONDE-013 belegte Felder des `evidence_snapshot` geführt. Tests: `phase_only_in_coherent_bands`, `coherence_carries_window_and_dof`, `coherence_is_null_below_eight_frames` (ersetzt `coherence_needs_eight_welch_frames`).

MANIFESTFORM
Die Matrix (§3) und die Entscheidtabelle (§7.1) sind lebende Referenz und werden in place nachgezogen; §3.11 (Zählung: M-36, M-48, M-49, M-54 als ENTSCHIEDEN, neue Zeilen), §4.1 (E-02/E-03/E-04 mit Hinweis „ersetzt durch §7.1 nach Nacharbeit 1"), §5.2 und §5.4 ebenfalls. Darunter hängst du einen neuen Paragraphen „§8 Nacharbeit 1 nach Matrixprüfung 1 (2026-09-03)" an: die sieben Befunde mit Codex-Wortlaut, je Befund Regel und die geänderten Zeilen/Abschnitte; der alte Wortlaut von E-02/E-03/E-04 aus §7.1 wird dort als Verlauf zitiert. Kopf: Zeile „Phase" auf „Etappe 1 — Verhaltensmatrix, Nacharbeit 1 abgeschlossen, Wiederprüfung 1 offen", und eine Zeile „Matrixprüfung 1" mit Codex-Thread, Effort, HEAD 367a0ea, Urteil NEEDS_WORK (7 Defekte) und Verweis auf die Rohdatei. Keine NAKAMA-URTEIL-Marke, kein Belegfeld in plan.json.

GRENZE
Geschrieben werden nur docs/beweise/SONDE-013.md und nichts sonst. Kein Produkt-, Test-, Schema-, Fixture- oder Werkzeugcode; nicht docs/plan/, docs/offene-punkte.md, docs/PLAN-STAND.md. Lesende Befehle sind erlaubt. Drei bereits vorhandene, noch nicht committete Dateien des Dirigenten committest du mit: docs/beweise/roh/SONDE-013-matrixpruefung-1-auftrag.txt, docs/beweise/roh/SONDE-013-matrixpruefung-1-367a0ea.txt und diesen Auftrag docs/beweise/roh/SONDE-013-nacharbeit-1-auftrag.txt — unverändert.

GIT
Nie git add -A, nie --amend, nie git reset --hard, nie git clean. Vorher git status --short lesen: die drei genannten roh-Dateien sind erwartet, alles andere Fremde bleibt unberührt. Vor dem Commit py -3.13 tools/plan/dokuriegel.py docs/beweise/SONDE-013.md fahren (Tabellen: kein rohes `|` in einer Zelle); jeder Befund wird behoben. Commit nur mit explizitem Pathspec: git commit -m "SONDE-013 Etappe 1 Nacharbeit 1: sieben Defekte der Matrixprüfung geschlossen (E-02/E-03/E-04 ersetzt, M-05/M-11/M-23/M-47 nachgezogen)" -- docs/beweise/SONDE-013.md docs/beweise/roh/SONDE-013-matrixpruefung-1-auftrag.txt docs/beweise/roh/SONDE-013-matrixpruefung-1-367a0ea.txt docs/beweise/roh/SONDE-013-nacharbeit-1-auftrag.txt. Vor dem Push bei sauberem Worktree git pull --rebase, dann git push. Der Push ist Teil des Auftrags.

ABSCHLUSS
Fertig heißt: Commit auf origin/master, Worktree sauber. Letzte Meldung: Commit-SHA, je Defekt eine Zeile „D<n> geschlossen: <Zeilen/Abschnitte>", neue Zählung nach Belegklasse.
