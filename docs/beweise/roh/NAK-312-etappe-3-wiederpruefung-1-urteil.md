URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen.

# NAK-312 Etappe 3 — Wiederprüfung 1 (S25k, Fixdiff der Nacharbeit 1)

Frische, lesende Prüfsession (Claude Opus 5), 21.09.2026, 10:14 bis 10:27 Uhr (`date`). Auftrag `docs/beweise/roh/NAK-312-etappe-3-wiederpruefung-1-auftrag.txt`. Prüfbereich `git diff 529b824200e49db95a3ea24560b270f7b710c1a0...bda6362923b070629bce6dfcd8ec40d0763c6459` über die sechs Pfade des Auftrags: 6 Dateien, +289/-22 (`git diff --shortstat`), vollständig gelesen. Der Produktcode am HEAD ist der des Zielstands (`git diff --stat bda63629 HEAD -- . ':!docs'` ist leer); alle Zeilenangaben gelten dort. Referenz: `docs/beweise/NAK-312.md` §23.3, §23.4 (E-312-13, R-312-16, M-87, R-312-18) und §24, Wortlaut und Nachmessung aus `docs/beweise/roh/NAK-312-etappe-3-erstpruefung-urteil.md` und `docs/beweise/roh/NAK-312-etappe-3-erstpruefung-validierung.md`.

## Frage 1 — Ist jeder Befund am Zielstand geschlossen?

| Befund | geschlossen | Beleg |
|---|---|---|
| L-1 | ja | `std::atomic<bool> blockrandNurLesen` (`eq-copilot/plugin/sonde/SondeProcessor.h:690`); Speicherung `SondeProcessor.cpp:1209` vor dem Tausch `:1222` und der Zählerlesung `:1225`; Lesung `:524` nach dem Zähler `:509`, bei gesetztem Flag `neu` ohne `wert`; die Speicherordnung trägt die Zusage (Punkt 3); 312/M-87 misst deterministisch an den Zusagezeilen (Punkt 6) |
| N-1 | ja | `eq-copilot/plugin/dsp/DspKern.h:65-74`, `DspKern.cpp:550-555` und `:681-690` (dazu `:676`, `:1136-1142`, `DspKern.h:241-247`) sagen: im schreibbaren Stand kann der Blockrand im Block, in dem eine ENDE-Blende beginnt, ein Ziel setzen, das die ausblendende Bank formt; im read-only-Stand keines. Die Hunks in `DspKern.*` ändern nur Kommentare; `:676` behält `continue;` |
| N-2 | ja | `SondeProcessor.cpp:1654-1663` nennt je Zeitpunkt, was gilt: vor der Prüfung der ersten Schleife oder nach ihrem Schreiben zeigt der Regler den Hostwert, der Klang den bestätigten Wert; nur zwischen Prüfung und Schreiben bleibt der geschriebene bestätigte Wert. Stimmt mit `:1642-1650` und `:1664-1672` überein; der Hunk ist nur Kommentar |
| N-3 | ja | `tools/beweise.ps1:728`: der Wortdiff ändert nur den Geltungsbereich des 3b-Satzes und hängt den Satz zu 312/M-87 an; jede Bedingung hat ihren Fall (Abschnitt „N-3 gegen die Fälle") |
| N-4 | ja | Die A-19-Zeile `schreibBis = std::min (schreibBis, std::max ({ cUebergangsEnde, cRampenEnde, …` steht an BASIS in `DspKern.cpp:1437` und am Ziel in `:1448` wortgleich (`git grep` an beiden Commits); kein Hunk an dieser Stelle. `docs/beweise/roh/NAK-312-rot-A-19.txt` meldet die Rücknahme mit gleichem SHA-256 |

### L-1 im Einzelnen

1. **Speicherung vor Tausch und Zählerlesung.** In `setStateInformation` folgen unter dem Zustandsschloss (`:1172`) aufeinander: beide Ladestartzweige (`:1188-1197`), `blockrandNurLesen.store (transaktion->nurLesen(), std::memory_order_seq_cst);` (`:1209`), `hostEreignisOffen.exchange (false);` (`:1222`) und die Zählerlesung der Quittierung `hostEreignis[(size_t) i].load (std::memory_order_seq_cst)` (`:1225`). Dazwischen steht nur Kommentar, kein Rücksprung.

2. **Lesung nach dem Zähler.** `processBlock` liest je Platz den Zähler mit `seq_cst` (`:509`), vergleicht mit dem Blockrandstand (`:510`), setzt `neu` und `stand` (`:513-514`), prüft die Quittung (`:517`) und liest erst danach das Flag (`:524`). Bei gesetztem Flag folgt `continue` vor `h.wert = true` (`:526`): `neu` ohne `wert`. Der Kern setzt dann nur `blockStand` und `blockZielGueltig = false` (`DspKern.cpp:673-676`), der Blockrandstand zieht nach, kein Ziel. Die Mailbox wird vor `blockrand` übernommen (`DspKern.cpp:1159-1165`), also gilt das auch im ersten Block einer ENDE-Blende und während eines beim Laden laufenden Übergangs. Neu im Audiothread ist allein die Lesung eines `std::atomic<bool>`: kein Schloss, keine Allokation, kein Warten. `DspKern` ändert kein Verhalten.

3. **Ablauf „Hostwert unmittelbar nach der Quittierung", gerechnet nach [atomics.order] Absatz 3 und 4** (Normtext im aktuellen Arbeitsentwurf unter eel.is/c++draft/atomics.order am 21.09.2026 nachgelesen). Ladestart L auf dem Nachrichtenthread: S = Flagspeicherung `:1209`, R = Zählerlesung `:1225`, R liest q. Host H: `fetch_add (1, release)` (`:1505`) schreibt q+1, in der Änderungsordnung des Zählers nach dem Wert, den R gelesen hat. Audiothread, jeder spätere Block: Z = Zählerlesung `:509`, F = Flaglesung `:524`.
   - Liest Z q+1 oder einen späteren Stand: R liest den Wert einer Modifikation, die in der Änderungsordnung vor dem `fetch_add` liegt, also ist R kohärenzgeordnet vor dem `fetch_add` (Abs. 3, dritter Punkt); Z liest dessen Wert oder den einer späteren Modifikation, also ist R kohärenzgeordnet vor Z (Abs. 3, erster, zweiter und vierter Punkt). R und Z sind `seq_cst`, also steht R in S vor Z (Abs. 4, erste Kohärenzbedingung). S ist vor R sequenziert und Z vor F, also steht S vor R und Z vor F in S (Abs. 4, erster Satz). Damit steht S vor F. Läse F eine Speicherung, die in der Änderungsordnung des Flags vor S liegt, wäre F kohärenzgeordnet vor S (Abs. 3, dritter Punkt) und stünde in S vor S — Widerspruch; den Anfangswert aus dem Konstruktor kann F ebenso wenig lesen (er geschieht vor S; Abs. 5, Anmerkung 2). F liest also `true` oder die Speicherung eines späteren Ladestarts. Ohne späteren schreibbaren Ladestart: `continue`, kein Ziel. Die Quittungslesung `:517` (`acquire`) ändert das Ergebnis nicht: gleicht die gelesene Quittung n, folgt `continue`, sonst folgt F.
   - Liest Z noch einen Stand bis q: gleich dem Blockrandstand heißt nichts zu tun; sonst ist es ein Wert von vor der Quittierung, zweiter Ablauf.
   - Die Zusage von R-312-16 hält damit für jeden Hostwert, dessen Zählerstand die Quittierung nicht mehr gesehen hat, im Speichermodell und nicht nur auf x64. Die Rechnung braucht `seq_cst` an allen vier Zugriffen S, R, Z und F; genau so steht es im Code (A-30). Der Listener darf `release` bleiben; eine `seq_cst`-Lesung ist zugleich eine acquire-Lesung, die Paarung von `hostWert` (`:1504`, `:527`) mit dem Zähler bleibt (A-21).

4. **Ablauf „Hostwert unmittelbar vor dem Ladestart".** H schreibt vor L; R liest diesen Stand q (oder einen späteren), die Quittierung speichert ihn als `blockrandQuittung` (`:1234-1236`, `release`). Erster Block nach dem Laden in regulärer Folge: Z liest q ungleich dem Blockrandstand, also `neu`; die Quittungslesung `:517` liefert q gleich n, also `continue` ohne Ziel (W02, T3-05-01). Das Flag wird dafür nicht gebraucht, und die ENDE-Blende klingt mit den Gains der Bank aus. Ein Ziel aus diesem Wert entsteht nur, wenn die Lesungen des Blockrands vor L liegen (im Speichermodell: Quittung und Flag veraltet gelesen, Z und F in S vor S). Dann gehört der Block zum Stand vor dem Laden, und R-312-16 („ab dem ersten Block") ist nicht berührt. Anders nur, wenn derselbe Block in `uebernehmeBereiten` (`DspKern.cpp:535`) schon die ENDE-Publikation von L nimmt: dann muss der ganze Abschnitt von L ab der Flagspeicherung `:1209` bis zur Publikation `:1246` zwischen `:524` und `DspKern.cpp:535` desselben Blocks liegen, also bei angehaltenem Audiothread. Das ist das Restfenster aus der Validierung (Teil 2 Punkt 1, von §23.2 übernommen) und aus §24.9, das E-312-13 ausdrücklich offen lässt; es betrifft Werte von vor dem Laden. Der Fix verengt es: vorher genügte der Abschnitt ab der Quittungsspeicherung `:1234`, jetzt muss es der Abschnitt ab `:1209` sein. Kein Befund (Entscheid E-312-13; theoretischer Randfall ohne Zusage).

5. **Wechsel nur bei erfolgreichem Laden, in beide Richtungen.** Gespeichert wird `transaktion->nurLesen()`. `nurLesenStand` setzt allein `Transaktionskern::ladestart`, unmittelbar vor `return true` (`eq-copilot/plugin/state/NakamaTransaktion.cpp:272-273`); jede Ablehnung kehrt vorher mit `false` zurück (`:239-251`). `ladestart` hat im Produktcode genau die zwei Aufrufer `SondeProcessor.cpp:1189` und `:1193`; der Kern entsteht einmal im Konstruktor (`:189`), Flag und `nurLesenStand` beginnen beide mit `false`. Bei `LadeErgebnis::ignoriert` kehrt `setStateInformation` vor dem Schloss zurück (`:1166-1167`), das Flag bleibt. Schreibbar zu read-only speichert `true` (Hauptfall, Teilfall (i)), read-only zu schreibbar speichert `false` (Teilfall (ii)). Andere Schreiber gibt es nicht: `git grep blockrandNurLesen` trifft nur `SondeProcessor.cpp:524`, `:1209` und `SondeProcessor.h:690`.

6. **312/M-87 misst deterministisch an der Zusagezeile.** `eq-copilot/plugin/tests/TransactionTestMain.cpp:4749-4901`. Jeder Lauf steht ganz in `mitAngehaltenemTaktFuerTest` (`:4789`), das `zustandSchloss` hält (`SondeProcessor.h:383-387`); der Worker kann nicht takten, Laden, Hostwerte und Blöcke laufen auf einem Thread, die Saaten sind fest (3871, 3872 + Blockgröße, 3879, Aufbau 3200). Bestehensbedingungen: bitweiser Vergleich je Float (`abweichungsbild`, `memcmp`), `blockrandZiele()` des Kerns, den B7 selbst liest (E-312-11), dazu Aufbauprüfungen (Fixture gelesen, read-only, Commit angenommen, Übergang lief, Rückweg schreibbar). Keine Wandzeit, kein Warten; `groessterFaktorDb` steht nur im Meldungstext. Der Fall deckt §23.4 wörtlich: A und C mit eq an und Output-Trim +3 dB eingeschwungen, fremdes Major in beiden, nur A vier Hostwerte (`blockParameter()`: +24 dB, +24 dB, 2, 0; `:4219-4227`); Hauptfall 4 Blöcke zu 256 (`:4812-4835`); (i) Commit +6 dB und genau ein Block zu 64 vor dem Laden, danach Blöcke zu 185 und 512 (`:4837-4860`); (ii) fremdes Major, dann eigener Stand mit eq an, Output-Trim +6 dB vor Block 10 ohne Takt gegen einen Lauf mit Takt nach jedem Block, am Ende 10^(6/20) auf 1e-5 (`:4862-4900`). Zusagezeilen: die Flaglesung `:524` (Mutation Teil a: Hauptfall und (i) rot, 256, 626 und 768 Samples je Kanal, Ziele +4) und die Speicherung `:1209` (Teil b, nur setzen, nie löschen: allein (ii) rot, A/B 1664, am Ende 1,0). Die Gegenprobe am Stand vor dem Fix ist mit denselben Zahlen rot (`docs/beweise/roh/NAK-312-rot-M-87.txt`, `docs/beweise/roh/NAK-312-etappe3-nacharbeit1-gegenprobe.txt`, beide gelesen). Der Fall steht wie 312/M-79 und M-82 unter `#if ! defined (NAK312_GEGENPROBE_OHNE_BLOCKRAND)`; keine Bauquelle setzt den Schalter (`git grep` außerhalb von `docs/` trifft nur die Testdatei).

### N-3 gegen die Fälle

„alle vier in beiden Ständen bei Blockgröße 1, 64, 185, 240, 256 und 512 in Echtzeit": 312/M-20 (`mengeLauf (blk, 3201, false)`, `TransactionTestMain.cpp:4366`) und 312/M-22 (`{ 1, 64, 185, 240, 256, 512 }`, `:4437`), jeweils über `blockStaende()` und `blockParameter()`. „bei 64 offline": 312/M-21 (`:4420`). „für Output-Trim im neutralen Stand zusätzlich bei 4096": 312/M-27 (`:4520-4526`, `mitEq (true)`, `folgeM20 (kOutTrim, …)`). „über eine Rampe von 200 Blöcken": 312/M-23 (`:4459-4468`, Output-Trim, neutral). „über den Zählerüberlauf": 312/M-82 (`:4716-4731`, Output-Trim, neutral). Der Satz zu 312/M-87 nennt Aufbau, Hostwerte, Blöcke zu 256 sowie 185 und 512 bei laufendem Übergang, Ziele +0 und den Rückweg ohne Kontrolltakt mit 10^(6/20) — die drei gefahrenen Fälle, nicht mehr. `git diff --word-diff` zeigt in der Zeile keine weitere Änderung.

## Frage 2 — Hat der Fixdiff etwas gebrochen?

Nein.

- **Verhalten außerhalb des read-only-Stands.** Code ändern nur `SondeProcessor.cpp:509` (acquire zu seq_cst), `:524-525` (Flaglesung), `:1209` (Speicherung), `:1225` (relaxed zu seq_cst) und das Mitglied `SondeProcessor.h:690`; alle übrigen Hunks sind Kommentar (`git diff -U0`). In jedem schreibbaren Stand ist das Flag `false` (Konstruktor, jeder schreibbare Ladestart), der Blockrand verhält sich wie am Stand 3b; die stärkeren Ordnungen ändern in einem Thread kein Ergebnis. `git diff --stat 529b8242 bda63629` ist leer über `eq-copilot/identity`, `eq-copilot/schemas`, `eq-copilot/fixtures`, `eq-copilot/plugin/state`, `DspProgramm.*`, `DspBankPool.*`, `eq-copilot/plugin/src`, `eq-copilot/plugin/hostbridge`, `eq-copilot/plugin/CMakeLists.txt` und `broker`; über `eq-copilot`, `tools` und `broker` stehen genau die sechs Dateien und die nachgezogenen Hashes in `eq-copilot/install/nakama-installer-v1.json`.
- **Tests und Matrixzeilen.** 312/M-10 bis M-14, M-19, M-78 und M-29 laden eigene, schreibbare Bytes (`plusDrei`, `TransactionTestMain.cpp:3816-3822`; M-29 `:4576`, `:4591`), das Flag bleibt `false`. M-15 und M-18 laden read-only und messen am ruhenden Pfad (`:3943-3945`, `:4022-4023`), an dem der Blockrand schon vorher kein Ziel setzte (`DspKern.cpp:691`); das Ergebnis bleibt, M-15 prüft weiter `zurueck == fremd`. M-16 hängt am Abgleich, nicht am Blockrand. M-17 lädt fremd, dann eigen (`:3981-3983`); danach ist das Flag `false`. M-20 bis M-28 und M-79 bis M-82 laden keinen read-only-Stand. In `docs/beweise/roh/NAK-312-rot-M-87.txt` bleiben unter beiden Mutationen alle übrigen NAK-312-Fälle grün, nach der Rücknahme B7 225 geprüft, 0 Fehler; `docs/beweise/roh/NAK-312-rot-A-19.txt` meldet nach der Rücknahme B7 225/0 und B6 389/0.
- **Gate-Text.** T3-05-02 ist jetzt auch am Blockrand erfüllt, T3-05-01 unverändert (Quittierung, `:517`). GRENZE: Audiothread ohne Sperre und Allokation (eine Atomic-Lesung mehr; 312/M-26 grün laut Rohdatei); State verlustfrei, fremde Bytes bytegleich (kein Statefeld, `getStateInformation` unberührt); keine Oberfläche; keine Legacy-Umbenennung. BEWEIS: neue Rotbeweise an B7 (M-87 Teil a und b, A-19); der volle Kanon ist nach §23.5 für das Ende der nächsten Etappe angesetzt — der Fixdiff bricht keinen Satz.
- **Invarianten aus CLAUDE.md.** Audio echtzeitfest: siehe oben. Nulltest bitidentisch: der ausgeschaltete Pfad ist unberührt, das Flag nimmt nur Ziele weg; der Laufzeit-Arm an 32813efd meldet `lokal.nulltest` „BITIDENTISCH v=0 g=1.0 Abweichungen=0" (`docs/beweise/roh/NAK-312-laufzeit-32813efd.md`, gelesen). State verlustfrei: siehe oben. Identität eingefroren: `eq-copilot/identity` unberührt.
- **Rückweg (312/M-17, Teilfall (ii)).** Automation wirkt am Blockrand wieder: der schreibbare Ladestart speichert `false` vor seiner Quittierung (`:1209`), ein Hostwert danach sieht nach der Rechnung in Punkt 3 dieses `false` und setzt `wert` und Ziel. (ii) misst das ohne Kontrolltakt (A/B 0, am Ende 10^(6/20)); Teil b der Rotbeweise zeigt, dass (ii) an der Speicherzeile hängt. 312/M-17 wacht zusätzlich über den Weg mit Takt und ist grün.

## Geprüft

Vollständig gelesen: der Fixdiff über die sechs Pfade (`git diff`, `-U0`, `--word-diff`, `--shortstat`) und am Zielstand der Code darum — `processBlock`, `setStateInformation`, `parameterValueChanged`, `hostParameterAbgleichen`, Taktsperre und Mitglieder in `SondeProcessor.*`, `Transaktionskern::ladestart`, `DspKern::blockrand`, `uebernimmBlockrandHostwerte`, `setzeProgrammziel` und `verarbeite` samt Kopfkommentaren, in B7 die Helfer `fahreFolge`, `blockPruefling`, `blockParameter`, `abweichend`, `eingangVon` und jeder Ladeaufruf mit fremdem oder eigenem Stand. Mit lesenden Git-Befehlen (`rev-parse`, `status`, `log`, `diff`, `grep`, `ls-files --eol`) nachgemessen: die Hunks, die unveränderte A-19-Zeile an BASIS und Ziel, die unberührten Identitäts-, Schema-, Fixture-, State- und Brokerpfade, die Aufrufer von `ladestart` und alle Zugriffe auf das Flag; die sechs Dateien liegen im Index weiter als LF. Gelesen als Beleg: §23 bis §25 des Manifests, Erstprüfung und Validierung, die Rohdateien Gegenprobe, Rotbeweis M-87, Rotbeweis A-19 (Mutation, Rücknahme, Summen) und Laufzeit-Arm 32813efd (Kopf, Protokoll, Nulltestzeile), dazu der Normtext von [atomics.order] Abs. 3 bis 5. Die Abläufe unter Frage 1 Punkt 3 und 4 sind am Code und am Normtext gerechnet.

## Nicht geprüft

Nichts gefahren: kein Compiler, kein Test, kein Kanon, kein FL, kein Python; jedes Laufergebnis stammt aus den Rohdateien oder, für A1, A16, B2, B11 bis B14, B30 und B31, allein aus §24.7 (dazu keine Rohdatei gelesen). SHA-256 der Quellen sind nicht nachgerechnet (kein zulässiges Werkzeug); dass Rotläufe und Beinläufe die committeten Bytes fuhren, stützt sich auf die Rohdateien (gleiche `git diff --stat` über die sechs Dateien) und §24. Die Befehlsangaben zur MSVC-STL aus §24.3 und die clang-tidy-Ratsche sind nicht nachgelesen. Das Restfenster für Hostwerte von vor dem Laden (Punkt 4) ist nach E-312-13 nicht Gegenstand der Schließung. Nicht Prüfgegenstand nach Auftrag: `docs/**` (darunter §6 ohne M-87 und die Matrixzeile M-27, A-36), die Wertung „Exit 3 allein wegen U43" des Laufzeit-Arms (`nulltest-host.json` Exit 5 laut Kopfzeile) aus §25, der übrige Ticketbereich, die datierten Härtungen im Register und die Entscheide des Dirigenten. Fremde Ordner nicht berührt. Im Repo ist dieser Bericht die einzige neue Datei; nichts gestagt, committet oder gepusht. Eine lange Diffausgabe hat die Werkzeugumgebung außerhalb des Repos zwischengespeichert.

HEAD zu Beginn: `d1b206830a97ee7a14c74087483df023bdf459cc` (10:14:46). HEAD am Ende: `d1b206830a97ee7a14c74087483df023bdf459cc` (10:27:03 vor und 10:30:05 nach dem Schreiben dieses Berichts, gleich). Alle Werte mit `git rev-parse HEAD`; HEAD trägt über ZIEL hinaus nur den Prüfauftrag unter `docs/beweise/roh/`.

FERTIG Wiederprüfung 1 Etappe 3, PASS, 0 offene Befunde, 0 Brüche.
