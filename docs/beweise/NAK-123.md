# NAK-123 — Sicherheits-Härtung Broker-Impersonation: Verhaltensmatrix (Phase 1)

| Feld | Wert |
|---|---|
| Datum | 2026-09-01 |
| Bezug | G2 `NEEDS_WORK`; Register NAK-123, NAK-90 und NAK-116 |
| Basis-SHA | `e0a0dd67000b646c5fa03992b69c03be5913424a` |
| Auftrag | Spezifikation vor Code: Broker besitzt den v3-Pipenamen durchgehend und authentisiert jeden Client über die verbindliche Impersonation-Kette; Plugin authentisiert den tatsächlich verbundenen Server vor jeder Adoption. |
| Beweisstatus | Stand 2026-09-02: alle Matrixzeilen **A-01 bis C-11 `PASS`**, gemessen im Abschnitt „Bau- und Beweislauf 2026-09-02". Jede dort benannte neue Prüfung wurde einmal absichtlich gebrochen (Prüflistenzeile E-5, 19 Fälle, alle rot). Kanon `tools/beweise.ps1` **GRÜN, 39/39 Beine, Exitcode 0** auf Commit `d8676e0` (Lauf 2026-09-02 07:48, Rohausgabe `roh/NAK-123-d8676e0-dirty.md`). Die zwei Befunde der G2-Wiederprüfung Runde 1 sind geschlossen und belegt (Abschnitt „Nacharbeitsrunde 1 2026-09-02“, Rohausgabe `roh/NAK-123-r1-d8676e0.md`). Offen bleibt Prüflistenzeile **D-3** (Installer-Hash des neu gelinkten Brokers) — ausdrücklich außerhalb dieses Änderungssatzes. Der Phase-1-Wert `NOT RUN` galt für die Spezifikationsphase und steht dort im Verlauf unverändert. |

## 0. Verbindliche Grenze und Realitätsanker

Das Bedrohungsmodell lautet wörtlich: „Das schützt vor fremden Windows-Usern, Verwechslung und zufälligen lokalen Clients. Code, der bereits unter demselben User in FL injiziert wurde, liegt außerhalb des IPC-Threat-Models; dafür bleiben Signaturprüfung, Installationspfad und Betriebssystemschutz maßgeblich.“ (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3243-3245`).

Die serverseitige Reihenfolge ist ebenfalls bindend: `ConnectNamedPipe` → Bootstrap-Hello lesen → `ImpersonateNamedPipeClient` → Token-User-SID gegen die erwartete SID → `RevertToSelf` in jedem Pfad → erst danach Hello annehmen oder ablehnen. Ein fehlgeschlagenes `ImpersonateNamedPipeClient` schließt die Verbindung; es wird keine Clientanforderung im Serverkontext ausgeführt (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3216-3225`). Der dafür verlangte Negativtest heißt `security_vectors` (`docs/FL-Nakama-Sonden-Design-Entwurf.md:4168-4177`).

| Aktueller Befund | Nachgeprüfte Fundstelle |
|---|---|
| G2 wird von genau diesem live gewordenen Defekt getragen und bleibt `NEEDS_WORK`. | `docs/beweise/G2.md:12-55,136-141` |
| NAK-90 verlangt die fehlende v3-Impersonation; NAK-116 benennt das nicht durchgehende Pipename-Pinning; NAK-123 koppelt beides mit der fehlenden Clientprüfung. | `docs/offene-punkte.md:22,100,151` |
| Der produktive SID-gebundene v3-Endpunkt ist heute geöffnet. | `broker/src/lib.rs:898-906` |
| Nur das erste Handle trägt `FILE_FLAG_FIRST_PIPE_INSTANCE`; Folgeinstanzen nicht. Der Acceptor übergibt das verbundene Handle und legt die nächste Instanz erst danach an. | `broker/src/transport/server_v3.rs:1143-1189,1287-1306,1370-1486` |
| Der aktuelle v3-Pfad liest und klassifiziert das Bootstrap-Hello, besitzt danach aber keine Impersonation-/SID-Stufe. | `broker/src/transport/server_v3.rs:1561-1665,1667-1715` |
| Die 60-s-Leerlaufkonstante und Selbstendeentscheidung liegen aktuell nicht in `coordinator.rs`, sondern in `lib.rs`. Der Coordinator liefert nur die aktive Clientzahl. | `broker/src/lib.rs:39,947-984`; `broker/src/coordinator.rs:1290-1297` |
| `IpcVerbindung` und der v2-`PipeClient` veröffentlichen ihr `CreateFileW`-Handle ohne Prüfung des Serverprozesses. | `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:44-103`; `eq-copilot/plugin/src/PipeClient.cpp:312-347` |
| Die im Auftrag als `plugin/src/BrokerLifecycle.cpp` bezeichnete Datei liegt aktuell unter `plugin/core/ipc/`. Eine erreichbare Pipe wird vor der Binärprüfung übernommen; diese Prüfung schützt nur den späteren `CreateProcessW`-Pfad. | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:288-364,458-603` |
| Connect, Binärprüfung und Spawn laufen auf Client-/Lifecycle-Threads. `processBlock` darf weiterhin keinerlei Sperre, Allokation, Datei-, Pipe-, Prozess- oder Logoperation enthalten. | `CLAUDE.md:37-40,132-135`; `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:566-637` |

## A. Verhaltensmatrix — Broker-Pipename-Besitz über den Lebenszyklus

**Flächenzusage A:** Der veröffentlichte Broker hält stets mindestens eine eigene v3-Serverinstanz und im Normalzustand zwei gleichzeitig mit `ConnectNamedPipe` bewaffnete Listener. Nimmt einer an, bleiben der verbundene Serverhandle und der zweite Listener im Brokerbesitz, bis ein Ersatzlistener bewaffnet ist; erst danach darf der verbundene Handle an einen Worker gehen. Solange ein Plugin eine vorhandene Pipe adoptieren könnte, gilt daher genau eine der beiden Aussagen: Der echte Broker besitzt den Namen lückenlos, oder die Clientprüfung aus Fläche C erkennt und verweigert den anderen Besitzer. Crash, Leerlauf-Selbstende und Shutdown können den Namen legitim freigeben; sie werden ausdrücklich nicht als unbesetzbare Zeiträume behauptet.

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Quelle | Messender Test in Phase 3 |
|---|---|---|---|---|---|
| A-01 | Broker aus; v3-Name frei | Brokerstart; Erfolg oder injizierter Frühfehler nach erfolgreichem ersten Handle | Die erste RAII-besessene Instanz entsteht mit aktueller User-DACL, `PIPE_REJECT_REMOTE_CLIENTS` und `FILE_FLAG_FIRST_PIPE_INSTANCE`; eine zweite entsteht ohne First-Flag. Auf beiden läuft `ConnectNamedPipe`, und erst dann wird der v3-Servergriff veröffentlicht. Für jede Fehlerinjektionskante nach dem ersten Handle — direkt danach, zweite Instanz/Armierung sowie Wachhund- oder Acceptor-Spawn vor Veröffentlichung — liefert der Start einen Fehler, schließt per RAII alle erzeugten Handles, stoppt und joint bereits angelegte Threads und veröffentlicht keinen Griff. Eine unabhängige Probe muss den Namen unmittelbar wieder mit `FILE_FLAG_FIRST_PIPE_INSTANCE` beanspruchen können. | Bestätigter Pinning-Befund `docs/offene-punkte.md:22`; heutige Frühpfade `broker/src/transport/server_v3.rs:1262-1306,1308-1347,1356-1500`; DACL `broker/src/server.rs:181-201` | Neu `broker/tests/security_vectors.rs::start_freier_name_bewaffnet_zwei_listener_vor_veroeffentlichung` plus parametrisiert `startfehler_nach_erstem_handle_raeumt_handles_threads_und_namen_vor_publish`; jede Fehlerkante verlangt `Err`, keinen Griff/lebenden Hilfsthread und erfolgreichen First-Instance-Reacquire |
| A-02 | Broker aus; Name bereits durch einen anderen Server belegt | Brokerstart | Die erste Instanz mit `FILE_FLAG_FIRST_PIPE_INSTANCE` scheitert sichtbar; kein zweiter Broker, kein Fallback-Erstlistener ohne First-Flag. Ein Plugin darf die vorhandene Pipe nur nach C-07 übernehmen; ein unverifizierter Besitzer bleibt belegt, aber unbenutzt. | `broker/src/transport/server_v3.rs:1287-1306`; `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:470-484,537-576` | Neu `security_vectors::start_belegter_name_scheitert_ohne_zweiten_broker` plus `IpcTestMain.cpp`/`vorhandene_fremdpipe_wird_nicht_adoptiert` |
| A-03 | Zwei Listener bewaffnet | einer der beiden `ConnectNamedPipe`-Aufrufe nimmt einen Client an | Bei freier Kapazität bleibt der verbundene Serverhandle im Acceptorbesitz und der andere Listener bewaffnet. Ein Ersatzlistener wird angelegt und bewaffnet, bevor der verbundene Handle an den Worker geht. Der Name hat daher weder zwischen Accept und Worker-Übergabe noch zwischen zwei Accept-Runden ein besitzerloses Fenster. | `broker/src/transport/server_v3.rs:1356-1486,1502-1544` | Neu `server_v3::accept_bewaffnet_nachfolger_vor_worker_uebergabe_bei_zwei_listenern` mit deterministischer Barriere |
| A-04 | Ein Listener bleibt bewaffnet; ein angenommener Handle wartet noch im Acceptor | paralleler Ersatzlistener wird erzeugt und bewaffnet oder dieser Schritt scheitert | Erst Erfolg erlaubt die Worker-Übergabe. Bei `ERROR_PIPE_BUSY` oder anderem Fehler bleiben mindestens der Restlistener und der angenommene Serverhandle im Brokerbesitz; begrenzte Wiederholungen dürfen Verfügbarkeit kosten, nie den Namen. Ein irreparabler Fehler wird sichtbar und führt fail-closed in den geordneten Stopp, dessen letzter Besitzhandle erst am Ende schließt. | `broker/src/transport/server_v3.rs:1135-1189,1477-1486` | Neu `server_v3::ersatzlistener_fehler_behaelt_besitz_und_meldet_nicht_bereit` |
| A-05 | Zwei Listener bewaffnet; `MAX_VERBINDUNGEN` Worker-Verbindungen aktiv | weiterer Client oder fremder First-Instance-Versuch | Der Worker-Cap bleibt 96; zwei Listener sind zusätzliche Reserven. Alle Instanzen nennen konsistent `MAX_VERBINDUNGEN + 2 = 98` als `nMaxInstances`. Verbindet ein 97. Client einen Listener, wird dieser Handle nur geschlossen, während der andere Serverhandle den Namen hält, und danach ersetzt; er erreicht keinen Worker. Ein konkurrierender First-Instance-Server erhält den Namen nicht. | `broker/src/transport/server_v3.rs:83,1143-1166,1290-1300` | Neu `security_vectors::zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz` |
| A-06 | Keine aktiven v2-/v3-Clients; zwei Listener bewaffnet | monotone Leerlaufzeit erreicht 60 s | Bis einschließlich der Selbstendeentscheidung bleiben die Besitzhandles offen. Beim geordneten Selbstende werden Accept-I/O und Verbindungen beendet; der letzte Listener schließt als letzter Pipebesitz unmittelbar vor Prozessende. Ab dann trägt C-08 die Schutzwirkung gegen einen neuen fremden Besitzer. | `broker/src/lib.rs:39,947-984`; `broker/src/transport/server_v3.rs:923-962` | Erweitert `broker/tests/broker_idle.rs::echter_brokerprozess_beendet_sich_nach_letztem_client_selbst` um `idle_exit_schliesst_besitzlistener_zuletzt`; C++-Gegenbein `serverauth_nach_idle_exit_verweigert_fremde_pipe` |
| A-07 | Broker lebt; mindestens ein Besitzhandle offen | harter Prozesscrash/Kill | Der Prozess kann kein Handle weiterbesitzen; der Name wird frei. Es gibt hier bewusst keine lückenlose Brokerzusage. Jeder folgende Connect authentisiert deshalb den neu verbundenen Server nach C-08; weder alter PID-Cache noch bloße Erreichbarkeit gelten. | `docs/beweise/G2.md:22-47`; `docs/FL-Nakama-Sonden-Design-Entwurf.md:3243-3245` | Neu `security_vectors::prozesskill_gibt_name_frei` plus `IpcTestMain.cpp`/`serverauth_nach_brokercrash_verweigert_pidwechsel_und_fremdbild` |
| A-08 | Alter Broker beendet; Name frei oder inzwischen fremd belegt | Brokerneustart/Reconnect | Bei freiem Namen beginnt A-01 erneut. Bei belegtem Namen greift A-02. Ein Plugin sendet auf keiner inzwischen belegten Pipe ein Hello, Telemetrie oder P0/P1/P2, bevor C den konkreten Handleinhaber verifiziert hat. | `broker/src/lib.rs:867-906`; `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:44-103` | Neu `security_vectors::neustart_besetzt_name_vor_bereit` plus `IpcTestMain.cpp`/`reconnect_sendet_erst_nach_neuer_serverauth` |
| A-09 | Servergriff aktiv | expliziter Shutdown/`Drop` | Stop setzen → beide Accepts wecken → Verbindungs-I/O abbrechen → Acceptor/Wachhund/Worker joinen → letzten Besitzlistener zuletzt schließen. Mehrfacher Stop bleibt idempotent. Nach der Freigabe gilt dieselbe C-Kopplung wie bei Crash und Idle. | `broker/src/transport/server_v3.rs:923-962,1112-1133` | Neu `server_v3::stoppen_schliesst_besitzlistener_zuletzt_und_neustart_ist_sofort_moeglich` |

## B. Verhaltensmatrix — v3-Listener-Impersonationskette je Verbindung

**Flächenzusage B:** Jeder von `ConnectNamedPipe` angenommene Control- und Telemetry-Pfad besitzt einen Cleanup-Guard, der vor jedem Ausgang `RevertToSelf` ausführt. Kein `welcome`, kein Coordinator-/Store-/Senkenaufruf und keine weitere Clientanforderung läuft vor erfolgreicher SID-Gleichheit und erfolgreichem Revert.

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Quelle | Messender Test in Phase 3 |
|---|---|---|---|---|---|
| B-01 | Verbindung angenommen; vollständiges, syntaktisch gültiges Bootstrap-Hello gelesen | `ImpersonateNamedPipeClient` gelingt; Token-User-SID entspricht exakt der beim DACL-Bau verwendeten SID | `ConnectNamedPipe` → Hello lesen → impersonieren → Threadtoken öffnen und `TokenUser` lesen → binäre SID mit `EqualSid` vergleichen → Token schließen → `RevertToSelf` erfolgreich → erst dann Hello fachlich annehmen und gegebenenfalls `welcome` senden. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3216-3225`; heutiger Einfügepunkt `broker/src/transport/server_v3.rs:1617-1671` | Neu `broker/tests/security_vectors.rs::gleiche_sid_wird_erst_nach_revert_angenommen`; echte Probe-Pipe, Senkenreihenfolge und `OpenThreadToken`-Gegenprobe |
| B-02 | Gültiges Hello gelesen; Guard aktiv | `ImpersonateNamedPipeClient` liefert `FALSE` | Fehler sichern → `RevertToSelf` über denselben Cleanupweg → Pipe schließen → Verbindungsthread beenden. Null Welcome, null Senken-/Coordinator-/Storeaufrufe, null Verarbeitung weiterer Clientbytes im Serverkontext. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3217-3225,4174-4176` | Verbindlicher Negativtest neu `security_vectors::impersonation_fehlschlag_schliesst_ohne_serverkontext` |
| B-03 | Impersonation aktiv | `OpenThreadToken`, Größenabfrage, `GetTokenInformation(TokenUser)` oder SID-Validierung schlägt fehl | Kein Vergleich wird geraten. Token schließen, erfolgreich reverten, Verbindung schließen, Thread beenden; kein fachlicher Callback. | Vorhandenes alignment- und fehlerpfadsicheres Tokenmuster `broker/src/server.rs:104-178` | Neu `security_vectors::tokenabfragefehler_revertiert_und_schliesst_ohne_senke`, je API-Kante einzeln injiziert |
| B-04 | Impersonation aktiv; Token-User-SID lesbar | SID ist fremd | Exakter binärer Vergleich fällt → Token schließen → `RevertToSelf` → Pipe schließen. Kein Welcome und keine Anmeldung. Ein Test setzt als erwartete SID absichtlich eine andere gültige SID und nutzt den echten Clienttoken; er braucht kein zweites Benutzerkonto. | `docs/FL-Nakama-Sonden-Design-Entwurf.md:3218-3225,3243-3245` | Neu `security_vectors::fremde_sid_revertiert_und_erreicht_keinen_coordinator` |
| B-05 | Verbindung angenommen; Guard aktiv | Hello-Read endet mit vollständig gerahmtem, aber abzulehnendem Hello oder ohne vollständigen Frame | Bei einem vollständigen Frame darf der Parser den Ablehnungsgrund nur vormerken: Auch ungültiges JSON, falscher Typ/`connection_kind` und Protokollmismatch durchlaufen zwingend Read → `ImpersonateNamedPipeClient` → Token-User-SID-Vergleich → `RevertToSelf`; erst danach wird Reject geschrieben und die Pipe geschlossen. Impersonation-, SID- oder Revertfehler fallen in B-02/B-04/B-07 und senden keinen Reject. Nur wenn wegen Prefix, Übergröße, EOF oder Timeout kein vollständiger Frame vorliegt, greift der gemeinsame Readfehler-Cleanup aus B-06. Kein Zweig erreicht Welcome, Coordinator, Store oder Senke. | Verbindliche Reihenfolge `docs/FL-Nakama-Sonden-Design-Entwurf.md:3217-3225`; heutige Read-/Ablehnstellen `broker/src/transport/server_v3.rs:1614-1665,1671-1685` | Neu `security_vectors::vollstaendiges_abzulehnendes_hello_impersoniert_prueft_sid_und_revertiert_vor_reject`; Call-Trace verlangt `read < impersonate < sid < revert < reject < close` für ungültiges JSON, falschen Typ/Kind und Protokollmismatch, alle Fachzähler bleiben null; Prefix/Übergröße/EOF/Timeout misst B-06 getrennt |
| B-06 | Verbindung angenommen; Guard aktiv; Hello unvollständig | Bootstrap-Timeout, EOF, Cancel oder Lesefehler | Wachhund/Readpfad beendet die I/O; Cleanup ruft `RevertToSelf` und schließt. Kein Timeoutpfad gelangt in SID-/Hello-Annahme oder Serverfachlogik. | `broker/src/transport/server_v3.rs:1308-1342,1604-1663` | Neu `security_vectors::bootstrap_timeout_und_lesefehler_revertieren_jeweils_ohne_serverweiterlauf` |
| B-07 | Cleanup muss zum Self-Kontext zurückkehren | `RevertToSelf` liefert `FALSE` | Keine Fachlogik und kein normaler Cleanup laufen mehr: Der Broker beendet sich unmittelbar fail-fast; Windows schließt damit auch den Verbindungshandle. Nur das Ende des betroffenen Threads oder ein geordneter Stopp reichen nicht, weil deren weiterer Code unter einem potenziell fremden Threadtoken liefe. Der Wiederanlauf fällt unter A-07/C-08. | Bindende „jedem Pfad“-Regel `docs/FL-Nakama-Sonden-Design-Entwurf.md:3217-3223` | Neu `security_vectors::revert_fehlschlag_beendet_testbrokerprozess_fail_fast` mit injizierter API-Kante und Child-Prozess-Beleg |
| B-08 | Control oder Telemetry verbindet separat | jeweiliges Bootstrap-Hello | Beide Verbindungstypen durchlaufen B-01 bis B-07 unabhängig. Eine bereits authentisierte Control-Verbindung autorisiert kein zweites Telemetry-Handle und umgekehrt. | Heutige getrennte Bootstrapzweige `broker/src/transport/server_v3.rs:1671-1846` | Neu `security_vectors::control_und_telemetry_durchlaufen_die_identische_sid_kette` |
| B-09 | Erfolgreiche SID-Prüfung und Revert abgeschlossen | späterer Envelope-, Rate-, Queue-, Write- oder Trennfehler | Es wird nicht erneut unter Clienttoken gearbeitet. Der Verbindungsthread bleibt im Self-Kontext; jeder spätere Rückweg schließt normal. Die Authentisierung wird nur für einen neuen Pipe-Handle wiederholt. | `broker/src/transport/server_v3.rs:1848-2050` | Neu `security_vectors::nach_revert_bleiben_envelope_und_trennpfade_im_self_kontext` |

## C. Verhaltensmatrix — clientseitige Server-Authentisierung beim Connect

**Flächenzusage C:** `WaitNamedPipeW` beweist nur Erreichbarkeit. Vertrauen entsteht ausschließlich am tatsächlich durch `CreateFileW` verbundenen Handle und vor dem ersten ausgehenden Byte. Geprüft werden zusammen: Server-PID, Server-Token-User-SID, tatsächliches Prozessimage/Dateiidentität sowie der bereits manifestgebundene SHA-256- und gegebenenfalls Authenticode-/Thumbprint-Vertrag. PID und Pfad allein bleiben ausdrücklich keine Identität.

| ID | Zustand vor Ereignis | Ereignis | Zusage und Reihenfolge | Quelle | Messender Test in Phase 3 |
|---|---|---|---|---|---|
| C-01 | Kein Handle offen | `CreateFileW` endet mit `ERROR_FILE_NOT_FOUND` | Zustand `getrennt` und `brokerPipeFehlt=true`. Nur dieser Fall darf den Lifecycle-Spawnweg eröffnen; `ERROR_PIPE_BUSY`, Zugriff verweigert und Authfehler gelten nicht als „Broker fehlt“. | `eq-copilot/plugin/core/ipc/ControlClient.cpp:79-86`; `eq-copilot/plugin/tests/IpcTestMain.cpp:3421-3451` | Erweitert `IpcTestMain.cpp`/`fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` |
| C-02 | `CreateFileW` liefert einen Handle | echter Broker desselben Kontos und erwarteten Images | Vor Veröffentlichung des Handles und vor Hello: `GetNamedPipeServerProcessId` → Prozesshandle mit `PROCESS_QUERY_LIMITED_INFORMATION` halten → Prozess-`TokenUser` per `EqualSid` gegen aktuelle Plugin-User-SID → `QueryFullProcessImageNameW` → tatsächliche Dateiidentität gegen manifestgebundenen absoluten Brokerpfad → vorhandene SHA-256-Prüfung und bei gesetztem Thumbprint zusätzlich `WinVerifyTrust` plus exakter Signer. Erst das Gesamtergebnis verifiziert den Handle. | Aktuelle ungeschützte Veröffentlichungsstellen `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:67-103` und `eq-copilot/plugin/src/PipeClient.cpp:327-347`; vorhandene Binärkette `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:288-337` | Neu `IpcTestMain.cpp`/`serverauth_gueltiger_pid_sid_datei_hash_und_signer_vor_hello` |
| C-03 | Pipehandle offen; PID ermittelt | Prozess kann nicht geöffnet werden, Tokenabfrage scheitert oder Server-User-SID ist fremd | Fail-closed: Pipehandle schließen, kein Byte senden, `status=getrennt`, `brokerPipeFehlt=false` und konkrete interne Fehlerklasse „Server nicht verifiziert“. Fehlende Abfragerechte sind kein Grund, dem Prozess zu vertrauen. | Aktuelle SID-Ermittlungsmuster `eq-copilot/plugin/core/ipc/PipeToken.cpp:147-178`; fehlende Serverprüfung `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:67-103` | Neu `IpcTestMain.cpp`/`serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` |
| C-04 | PID und User-SID passen | Prozessimage fehlt, Dateiidentität/Pfad weicht ab, SHA-256 weicht ab, Signatur fehlt/ist ungültig oder Signer weicht ab | Jede Kante schließt den Handle vor Hello. Es gibt keinen Fallback auf eine nur per Pfad, PID, Welcome oder Pipe-Token plausible Pipe. Beim heutigen Manifest-`null` bleibt der passende SHA-256 die Mindestbedingung; ein künftig gesetzter Thumbprint verschärft, ersetzt den Hash aber nicht. | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:288-337`; `eq-copilot/install/nakama-installer-v1.json:14-17` | Neu `IpcTestMain.cpp`/`serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed`, jede Fehlerklasse einzeln |
| C-05 | `WaitNamedPipeW` meldete einen Server | Besitzer wechselt vor `CreateFileW` oder ein anderer Listener gewinnt die konkrete Instanz | Das Vorprüfergebnis wird verworfen. Ausschließlich PID/SID/Image des tatsächlich geöffneten Handles werden geprüft. Ein Namenswechsel wird daher als anderer Prozess erkannt, bevor der Client schreibt. | `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:19-30,55-90` | Neu `IpcTestMain.cpp`/`waitnamedpipe_toctou_wird_am_verbundenen_handle_erkannt` mit deterministischer Besitzerwechsel-Barriere |
| C-06 | Mehrere Produkt-Connectpfade | v3-Control, v3-Telemetry oder v2-`PipeClient` öffnet je ein Handle | Alle drei Pfade verwenden denselben Serverauth-Helfer; jedes Handle wird einzeln geprüft. Ein verifiziertes Control-Handle autorisiert weder Telemetry noch v2. Es gibt keinen unverifizierten Legacy-Fallback. | v3-Aufrufer `eq-copilot/plugin/core/ipc/ControlClient.cpp:1059-1067` und `eq-copilot/plugin/core/ipc/TelemetryClient.cpp:498-506`; v2-Pfad `eq-copilot/plugin/src/PipeClient.cpp:312-347` | Neu `IpcTestMain.cpp`/`control_und_telemetry_authentisieren_jedes_handle` plus `PipeClientLifecycleTestMain.cpp`/`pipeclient_sendet_vor_serverauth_keine_bytes` |
| C-07 | Lifecycle hält Startmutex; Controlcache noch getrennt; Pipe ist erreichbar | `vorhandenePipeUebernehmen` vor oder nach Binärprüfung fordert einen Control-Connect an | `WaitNamedPipeW` stößt höchstens den Versuch an. Der `ControlClient` verifiziert den tatsächlich zu übernehmenden `CreateFileW`-Handle und meldet `nicht da / verifiziert / belegt aber unverifiziert` zurück; ein getrenntes Prüfhandle darf keinen späteren Handle autorisieren. Nur `verifiziert` veröffentlicht den Handle, gibt das Mutex frei und überspringt den Spawn. `belegt aber unverifiziert` meldet den Sicherheitsfehler, reconnectet nicht weiter, spawnt nicht gegen den belegten Namen und wird nie als bereit/adoptiert gezählt. | Defektreihenfolge `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:470-484,537-576` | Neu `IpcTestMain.cpp`/`vorhandene_pipe_wird_nur_mit_dem_authentisierten_handle_uebernommen` und `unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht` |
| C-08 | Pipe fehlt; Startgate erlaubt; Startmutex gewonnen | Binärprüfung und `CreateProcessW` | Vor Spawn bleibt die bestehende manifestgebundene Hash-/Signaturprüfung. Der Kindprozess oder sein zurückgegebener PID gewährt aber kein dauerhaftes Vertrauen: jeder nachfolgende Connect prüft C-02 erneut. Erscheint zwischen Prüfung und Reconnect eine fremde Pipe, fällt C-03/C-04; sie wird nicht als das gestartete Child angenommen. | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:288-364,548-599`; Produkt-Hooks `eq-copilot/plugin/src/PluginProcessor.cpp:104-127` | Neu `IpcTestMain.cpp`/`spawn_pid_ersetzt_serverauth_nicht_und_fremdpipe_im_bereitfenster_faellt` |
| C-09 | Früherer Server war verifiziert | Reconnect nach EOF, Crash, Idle, Shutdown oder PID-/Broker-Epoch-Wechsel | Keine Freigabe überlebt den Handle. Der neue `CreateFileW`-Handle durchläuft C-02 vollständig; alter PID, alte Dateiidentität oder altes Welcome sind kein Cache-Schlüssel. Bei Fehler bleibt der Client getrennt und sendet keine wartenden Frames an den neuen Besitzer. | Reconnectpfade `eq-copilot/plugin/src/PipeClient.cpp:261-269,285-307`; `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:486-513` | Neu `IpcTestMain.cpp`/`reconnect_prueft_pid_sid_und_bild_ohne_altfreigabe` plus `PipeClientLifecycleTestMain.cpp`/`pipeclient_reconnect_verwirft_serverfreigabe` |
| C-10 | Serverauth schlägt fehl; ein passendes `confirm_join` ist ausstehend | unverifizierter Server schreibt vor Abschluss der Prüfung ein erfolgreiches gefälschtes `command_ack`; danach lesen Lifecycle/UI und der Persistenz-Tick den Zustand | Ehrliche Wahrheit: getrennt, Pipe nicht fehlend, Server nicht verifiziert, benannte Fehlerklasse; kein „wartet auf Broker“, „bereit“ oder Fallback. Das ACK erreicht den Produktcallback nicht und bleibt wirkungslos: ausstehender Auftrag unverbraucht, `mainProjectMitglieder`, serialisierte Statebytes, Host-Dirty-Zähler und `v3StateRevision` gegenüber dem Vorzustand exakt unverändert; Telemetrie sowie P0/P1/P2 bleiben ebenfalls aus. | Konkreter G2-Angriff `docs/beweise/G2.md:38-46`; heutige v3-Open-/Callbackstellen `eq-copilot/plugin/core/ipc/ControlClient.cpp:1059-1067,1284-1298`; ACK-zu-Persistenz-Pfad `eq-copilot/plugin/src/PluginProcessor.cpp:850-864,1311-1353`; vorhandene Zustandsbeobachtung `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:119-149`; Lifecycle-Snapshot `eq-copilot/plugin/core/ipc/BrokerLifecycle.h:79-107` | Neu gekoppelter Test `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp::gefaelschtes_command_ack_vor_serverauth_mutiert_keinen_persistenten_projektzustand`: echter `ControlClient` gegen unverifizierte Testpipe, ACK mit exakt ausstehender `command_id`, Callbackzähler null; vor/nach `sourcesTick()` bytegleicher Save-State, gleiche Mitglieder, Dirty- und Revisionzähler sowie ehrlicher Serverauth-Fehlerstatus |
| C-11 | Audiocallback läuft während Connect, Prüfung, Spawn oder Reconnect | beliebig viele Audioblöcke | Sämtliche PID-, Token-, Pfad-, Datei-, Hash-, Authenticode-, Pipe- und Lifecycle-Arbeit bleibt auf den vorhandenen Client-/Lifecycle-Threads. `processBlock` enthält weiterhin null Sperren und null Datei-/Pipe-/Prozess-I/O; Audio bleibt sampleidentisch. | `CLAUDE.md:37-40,132-135`; `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:566-637` | Bestehende Riegel erweitert um den neuen Helfernamen: `LebenslaufTestMain.cpp`/`broker_lifecycle_aufrufe_im_audiothread_null` und `processblock_quellschnitt_{datei,pipe,prozess}operationen_null` |

## Technische Wahl

### T-01 — Zwei bewaffnete Listener und Nachfolger vor Übergabe

`FILE_FLAG_FIRST_PIPE_INSTANCE` beansprucht nur die erste Instanz; Folgeinstanzen verwenden es nicht. Ein nie acceptender „Wächter“ scheidet aus, weil ein Client eine mit `CreateNamedPipeW` erzeugte Instanz bereits verbinden kann. Gewählt werden deshalb zwei echte, gleichzeitig mit `ConnectNamedPipe` bewaffnete Listener. Nach einem Accept bleibt der verbundene Handle im Acceptor, bis ein Ersatzlistener bewaffnet ist; bei vollem Cap wird ein überschüssig verbundener Listener nur geschlossen, solange der zweite Besitzhandle offen bleibt. Alle Instanzen verwenden konsistent `MAX_VERBINDUNGEN + 2 = 98` als `nMaxInstances`, sodass 96 Worker-Verbindungen und zwei Listenerreserven gleichzeitig bestehen; 98 liegt unter Windows' Grenze 255. Weil Windows damit nicht mehr selbst bei 96 Verbindungen begrenzt, prüft der Acceptor den Worker-Cap separat und atomar zur Übergabe; eine 97. Verbindung erreicht keinen Worker. Grundlage im Repo: `broker/src/transport/server_v3.rs:83,1143-1189,1287-1306` und `broker/src/server.rs:58-76,181-201`. API-Basis: [CreateNamedPipeW](https://learn.microsoft.com/en-us/windows/win32/api/namedpipeapi/nf-namedpipeapi-createnamedpipew).

### T-02 — Eine SID-Wahrheit und ein fail-closed Impersonation-Guard

Die beim Serverstart einmal gelesene binäre Current-User-SID speist sowohl DACL als auch erwarteten Tokenvergleich; es entstehen keine zwei unabhängig normalisierten SID-Strings. Verglichen wird nach Validierung mit `EqualSid`. Ein Guard entsteht unmittelbar nach erfolgreichem `ConnectNamedPipe` und ruft auf jedem Ausgang `RevertToSelf`. Im Erfolgsweg wird vor jeder Fachlogik explizit revertiert. Schlägt Impersonation fehl, werden keine Clientanforderungen ausgeführt; schlägt Revert fehl, beendet sich der Prozess unmittelbar fail-fast statt einen normalen Cleanup unter dem unbekannten Threadtoken fortzusetzen. Grundlage im Repo: `broker/src/server.rs:104-201` und der Einfügepunkt `broker/src/transport/server_v3.rs:1561-1671`. API-Basis: [ImpersonateNamedPipeClient](https://learn.microsoft.com/en-us/windows/win32/api/namedpipeapi/nf-namedpipeapi-impersonatenamedpipeclient), [RevertToSelf](https://learn.microsoft.com/en-us/windows/win32/api/securitybaseapi/nf-securitybaseapi-reverttoself), [EqualSid](https://learn.microsoft.com/en-us/windows/win32/api/securitybaseapi/nf-securitybaseapi-equalsid).

### T-03 — Serverbeweis am konkreten Clienthandle

Gewählt wird ein gemeinsamer C++-Helfer für `IpcVerbindung` und `PipeClient`. Er nimmt den noch unveröffentlichten Pipehandle und die manifestgebundene Erwartung entgegen. `GetNamedPipeServerProcessId` bindet die Prüfung an die konkrete Instanz; ein gehaltenes Prozesshandle verhindert, dass die Prüfung nur auf einer wiederverwendbaren PID beruht. `TokenUser` muss dem Plugin-User entsprechen. `QueryFullProcessImageNameW` liefert das Image; dessen geöffnete Dateiidentität muss dem manifestgebundenen absoluten Brokerpfad entsprechen. Danach wird die vorhandene `brokerBinaryPruefen`-Kette auf dieses Image angewendet. PID und Pfad sind damit nur Bindeglieder, nie alleinige Identität — entsprechend `docs/FL-Nakama-Sonden-Design-Entwurf.md:3223-3225`. API-Basis: [GetNamedPipeServerProcessId](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-getnamedpipeserverprocessid), [QueryFullProcessImageNameW](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-queryfullprocessimagenamew).

### T-04 — Lifecycle braucht drei Ergebnisse, keinen Reachability-Bool

`namedPipeErreichbar` bleibt höchstens eine günstige Livenessprobe und kann nie `verifiziert` liefern. Der Lifecycle erhält stattdessen vom `ControlClient` das Ergebnis des tatsächlichen, noch unveröffentlichten Handles: `nicht da`, `verifiziert` oder `belegt aber unverifiziert`. `vorhandenePipeUebernehmen` geht nach dem Anstoß in einen nicht blockierenden Wartezustand; nur `verifiziert` darf genau diesen Handle veröffentlichen, das Mutex freigeben und den Spawn überspringen. Vor und nach `brokerBinaryPruefen` sowie nach `CreateProcessW` gilt dieselbe Handleprüfung; ein Probehandle, ein früheres Ergebnis oder ein Spawn sind kein Authentisierungsersatz. Das baut auf der heutigen manifestgebundenen Hash-/Authenticode-Kette auf (`eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:288-364`), korrigiert aber die Adoption davor (`eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:470-484,545-576`).

### T-05 — Keine neue Realtime-Kante

Alle gewählten Windows-Aufrufe, Dateihashes und Vertrauensprüfungen sind blockierende Connect-/Lifecycle-Arbeit und bleiben außerhalb des Audiothreads. Es wird keine Synchronisation aus diesen Helfern in `processBlock` gespiegelt. Die vorhandenen strukturellen und dynamischen Null-Riegel bleiben Teil der Abnahme (`CLAUDE.md:37-40,132-135`; `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:566-637`).

## Bewusst außerhalb

- **Same-user-Angriffe:** Code, der bereits unter demselben User in FL injiziert wurde, bleibt nach §48.4 außerhalb dieses IPC-Bedrohungsmodells (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3243-3245`). NAK-123 darf dafür keine weitergehende Sicherheitszusage behaupten.
- **NAK-124:** Die same-user-auslösbaren Rust-/C++-Härtungen einschließlich `RAWPTR-001`, Handle-/Register-Soundness, Ressourcenerschöpfung und weitere DoS-/Verfügbarkeitsbefunde bleiben ein eigener Auftrag (`docs/offene-punkte.md:152`; `docs/beweise/G2.md:143-154`).
- **NAK-125:** Das `session_command`-ACK bei degradiertem Store und seine Persistenzzusage werden hier weder entschieden noch verändert (`docs/offene-punkte.md:153`).
- **NAK-126:** Die nach Nonce-Verdrängung weiter eingabefähige alte Control-Pipe und deren Kopplungs-/Eviktionssemantik bleiben außerhalb (`docs/offene-punkte.md:154`).

## Bau 2026-09-01

### Bau- und Beweisstatus

Der Fix ist für alle drei Flächen der freigegebenen Matrix implementiert. Die Matrix A-01 bis C-11 oben blieb dabei unverändert. Der Broker hält zwei bewaffnete RAII-Listener, ersetzt einen angenommenen Listener vor der Worker-Übergabe und führt die Impersonation-/SID-/Revert-Kette auf jedem v3-Verbindungspfad aus. Control, Telemetry und der v2-`PipeClient` authentisieren den Server am konkret geöffneten Handle, bevor dieses veröffentlicht oder das erste Byte gesendet wird; der Lifecycle adoptiert nur den so verifizierten Control-Handle.

Auf diesem Rechner wurden gemäß Auftrag weder kompiliert noch Binär- oder Kanontests ausgeführt. Der Laufstatus sämtlicher unten aufgeführter Tests ist deshalb **`NOT RUN`**. Ausgeführt wurden ausschließlich statische Prüfungen: `rustfmt --emit stdout` hat alle geänderten Rust-Dateien geparst; `rustfmt --check` war für `broker/src/transport/server_v3.rs` und `broker/tests/security_vectors.rs` ohne Befund; alle Matrix-Testnamen und ihre Aufrufe in den C++-Test-Einstiegspunkten wurden gefunden; die Quellreihenfolgen `vollständiger Frame → Impersonation → TokenUser/EqualSid → Revert → Parse/Reject`, `CreateFileW → Serverauth → Handleveröffentlichung → Hello` sowie die Abwesenheit der neuen Connect-/Verify-Helfer in `processBlock` wurden im aktuellen Quelltext geprüft. Das ist ein Syntax-/Verdrahtungsbeleg, kein Laufbeleg.

### Matrixzuordnung: Code und messender Test

| ID | Implementierte Fundstelle | Messender Test / Fundstelle (`NOT RUN`) |
|---|---|---|
| A-01 | `broker/src/transport/server_v3.rs:370-499,1608-1679,1969-1975` — RAII-Listener, beide Armierungen vor Rückgabe und alle Frühfehler-Gegenpfade | `broker/tests/security_vectors.rs:206-255` — `start_freier_name_bewaffnet_zwei_listener_vor_veroeffentlichung`, `startfehler_nach_erstem_handle_raeumt_handles_threads_und_namen_vor_publish`; parametrisiert direkt nach erstem Handle, zweite Instanz, nach zweiter Armierung, Wachhund und Acceptor |
| A-02 | `broker/src/transport/server_v3.rs:383-447,1652-1669`; `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:721-813` | `broker/tests/security_vectors.rs:258-268`; `eq-copilot/plugin/tests/IpcTestMain.cpp:1129-1162` — `start_belegter_name_scheitert_ohne_zweiten_broker`, `vorhandene_fremdpipe_wird_nicht_adoptiert` |
| A-03 | `broker/src/transport/server_v3.rs:1751-1858` — angenommener Handle und zweiter Listener bleiben bis zum bewaffneten Ersatz im Acceptor | `broker/tests/security_vectors.rs:271-297` — `accept_bewaffnet_nachfolger_vor_worker_uebergabe_bei_zwei_listenern` mit Übergabebarriere |
| A-04 | `broker/src/transport/server_v3.rs:1419-1463,1830-1967`; `broker/src/lib.rs:1044-1065` — fataler Ersatzfehler behält Restbesitz und wird dem Prozess sichtbar | `broker/tests/security_vectors.rs:300-325` — `ersatzlistener_fehler_behaelt_besitz_und_meldet_nicht_bereit` |
| A-05 | `broker/src/transport/server_v3.rs:87-91,1492-1524,1797-1827` — 96 Worker plus zwei Listenerinstanzen; Cap vor Workerübergabe | `broker/tests/security_vectors.rs:328-367` — `zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz` |
| A-06 | `broker/src/transport/server_v3.rs:1183-1255`; `broker/src/lib.rs:958-1041`; `broker/src/bin/eqcop-broker-v3probe.rs:87-113` — auch unvollständige Bootstraps zählen als aktiv, geordneter Idle-Gegenpfad schließt Listener zuletzt | `broker/tests/broker_idle.rs:99-173`; `eq-copilot/plugin/tests/IpcTestMain.cpp:1374-1378` — echter 60-s-Prozesslauf und `serverauth_nach_idle_exit_verweigert_fremde_pipe` |
| A-07 | OS-Prozessende gibt Handles frei; jeder neue C++-Handle läuft erneut durch `IpcVerbindung.cpp:44-127`, `ControlClient.cpp:738-754,1035-1148` | `broker/tests/security_vectors.rs:370-377`; `eq-copilot/plugin/tests/IpcTestMain.cpp:1351-1371` — `prozesskill_gibt_name_frei`, `serverauth_nach_brokercrash_verweigert_pidwechsel_und_fremdbild` |
| A-08 | `broker/src/transport/server_v3.rs:1608-1676`; Reconnect-Neuprüfung `ControlClient.cpp:738-754,1091-1148` | `broker/tests/security_vectors.rs:380-396`; `eq-copilot/plugin/tests/IpcTestMain.cpp:1214-1316` — `neustart_besetzt_name_vor_bereit`, `reconnect_sendet_erst_nach_neuer_serverauth` |
| A-09 | `broker/src/transport/server_v3.rs:1203-1261`; Produktgegenpfad `broker/src/main.rs:60-73`, `broker/src/lib.rs:958-992` | `broker/tests/security_vectors.rs:399-425` — `stoppen_schliesst_besitzlistener_zuletzt_und_neustart_ist_sofort_moeglich`, einschließlich idempotentem zweiten Stop |
| B-01 | `broker/src/transport/server_v3.rs:2040-2147,2222-2353`; erwartete SID stammt aus `broker/src/server.rs:61-89,121-236` | `broker/tests/security_vectors.rs:428-454` — `gleiche_sid_wird_erst_nach_revert_angenommen`, API-Spur bis `hello_accept` und Threadtoken-Gegenprobe |
| B-02 | `broker/src/transport/server_v3.rs:2009-2049,2328-2333` — gemeinsamer Cleanup revertiert auch nach fehlgeschlagener Impersonation | `broker/tests/security_vectors.rs:457-480` — `impersonation_fehlschlag_schliesst_ohne_serverkontext` |
| B-03 | `broker/src/transport/server_v3.rs:2052-2088` — Tokenhandle-RAII, Größen-, Informations- und SID-Validierungskanten | `broker/tests/security_vectors.rs:483-497` — `tokenabfragefehler_revertiert_und_schliesst_ohne_senke`, alle benannten API-Kanten parametrisiert |
| B-04 | `broker/src/transport/server_v3.rs:2091-2131` — echte gültige GegensID und echter `EqualSid`-Vergleich | `broker/tests/security_vectors.rs:500-506` — `fremde_sid_revertiert_und_erreicht_keinen_coordinator` |
| B-05 | `broker/src/transport/server_v3.rs:2150-2189,2281-2347` — vollständigen Frame nur vormerken; Parse/Reject erst nach SID und Revert | `broker/tests/security_vectors.rs:509-532` — `vollstaendiges_abzulehnendes_hello_impersoniert_prueft_sid_und_revertiert_vor_reject`; vollständiges ungültiges JSON sowie vollständiger falscher Typ, Kind und Protokoll |
| B-06 | `broker/src/transport/server_v3.rs:2009-2038,2246-2321` — gemeinsamer Readfehler-/Timeout-/Cancel-Cleanup | `broker/tests/security_vectors.rs:535-589` — `bootstrap_timeout_und_lesefehler_revertieren_jeweils_ohne_serverweiterlauf`; EOF, Teilpräfix, Übergröße, Timeout und Stop-Cancel |
| B-07 | `broker/src/transport/server_v3.rs:2020-2029`; Child-Naht `broker/src/bin/eqcop-broker-v3probe.rs:47-58` — Revertfehler abortiert den Prozess | `broker/tests/security_vectors.rs:592-610` — `revert_fehlschlag_beendet_testbrokerprozess_fail_fast` |
| B-08 | Gemeinsamer Eintritt `broker/src/transport/server_v3.rs:2222-2353`, getrennte Control-/Telemetry-Annahme danach | `broker/tests/security_vectors.rs:613-672` — `control_und_telemetry_durchlaufen_die_identische_sid_kette`, je Schritt genau zweimal und je Handle geordnet |
| B-09 | `broker/src/transport/server_v3.rs:2132-2147,2352-2890` — Self-Kontext vor sämtlicher Fach-, Envelope-, Queue-, Write- und Trennlogik | `broker/tests/security_vectors.rs:675-696` — `nach_revert_bleiben_envelope_und_trennpfade_im_self_kontext` |
| C-01 | `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:44-100`; Statusübernahme `ControlClient.cpp:1091-1132` | `eq-copilot/plugin/tests/IpcTestMain.cpp:899-913` — `fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` |
| C-02 | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:462-600`; lokale Handlehaltung/Veröffentlichung `IpcVerbindung.cpp:44-127` | `eq-copilot/plugin/tests/IpcTestMain.cpp:916-949` — `serverauth_gueltiger_pid_sid_datei_hash_und_signer_vor_hello`; Signer ist im aktuellen Manifest leer und daher im positiven Pfad vertragsgemäß optional, seine Negativkanten misst C-04 |
| C-03 | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:487-528`; Fail-close `IpcVerbindung.cpp:102-113` | `eq-copilot/plugin/tests/IpcTestMain.cpp:951-992` — `serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` |
| C-04 | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:530-595` — Image, Dateiidentität, Hash und optionaler Authenticode-/Signer-Vertrag | `eq-copilot/plugin/tests/IpcTestMain.cpp:994-1027` — `serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed`, jede Fehlerklasse einzeln |
| C-05 | `eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:59-113`; konkrete PID-Bindung `BrokerLifecycle.cpp:477-493` | `eq-copilot/plugin/tests/IpcTestMain.cpp:1029-1053` — `waitnamedpipe_toctou_wird_am_verbundenen_handle_erkannt` |
| C-06 | v3-Control `ControlClient.cpp:1091-1148`, v3-Telemetry `TelemetryClient.cpp:518-596`, v2 `PipeClient.cpp:340-425` — derselbe Authhelfer, jedes Handle einzeln | `eq-copilot/plugin/tests/IpcTestMain.cpp:1055-1092`; `eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp:288-325` — beide v3-Handles und v2-Nullbytes vor Auth |
| C-07 | `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp:721-813,782-799,870-911` — dreistufiges Urteil; nur `verifiziert` adoptiert, unverifiziert blockiert Spawn und Retry | `eq-copilot/plugin/tests/IpcTestMain.cpp:1095-1162` — `vorhandene_pipe_wird_nur_mit_dem_authentisierten_handle_uebernommen`, `unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht` sowie Aliaszusage `vorhandene_fremdpipe_wird_nicht_adoptiert` |
| C-08 | Spawn bleibt manifestgeprüft in `BrokerLifecycle.cpp:882-938`; Produktverdrahtung `PluginProcessor.cpp:77-136`, `sonde/SondeProcessor.cpp:60-78` authentisiert jeden Folgehandle erneut | `eq-copilot/plugin/tests/IpcTestMain.cpp:1165-1211` — `spawn_pid_ersetzt_serverauth_nicht_und_fremdpipe_im_bereitfenster_faellt` |
| C-09 | Generationsgrenzen `ControlClient.cpp:738-754,1035-1132`, `TelemetryClient.cpp:319-334,518-577`, `PipeClient.cpp:263-280,340-425` | `eq-copilot/plugin/tests/IpcTestMain.cpp:1214-1316`; `eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp:229-285` — Neuprüfung ohne Altfreigabe und `pipeclient_reconnect_verwirft_serverfreigabe` |
| C-10 | Vor-Byte-Sperre `IpcVerbindung.cpp:102-127`, Callback erst nach Auth/Welcome `ControlClient.cpp:1091-1148,1283-1365`; Persistenzpfad `PluginProcessor.cpp:859-873,1320-1360`; ehrlicher Status `SourcesModel.cpp:398-415` | `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:124-252` — echtes vor Auth geschriebenes ACK mit passender `command_id`; Callback, Auftrag, exakte Mitglieder, Statebytes, Dirty, Revision, P0/P1 und Empfang bleiben unverändert/null |
| C-11 | Blockierende Arbeit ausschließlich in Client-/Lifecycle-Threads; Produktkonstruktion `PluginProcessor.cpp:77-136`, Audiofunktion `PluginProcessor.cpp:225-458` enthält keinen neuen Connect-/Verify-Aufruf | `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:566-643` — dynamischer Audio-Operationszähler mit Gegenprobe und Quellschnitte für Datei, Pipe, Prozess, Logging und Store-Wait |

### Abgehakte Prüfliste mit Messort

`[x]` bedeutet hier, dass die Zeile bewertet und einem Messort oder einem begründeten `N/A` zugeordnet wurde. Es bedeutet bei den ausdrücklich mit `NOT RUN` markierten Punkten keinen erfolgreichen Lauf.

| Prüflistenzeile | Ergebnis und Messort |
|---|---|
| [x] A-1 Politik bei voller P0/P1/P2-Klasse ausdrücklich | Von NAK-123 nicht geändert. Bestehende Politiken bleiben in `broker/src/transport/warteschlange.rs:1-26,90-232,308-379` und `ControlClient.cpp:785-958`; bestehende Messungen `IpcTestMain.cpp:1910-2055` und Rust-Queue-Tests ab `warteschlange.rs:421`. |
| [x] A-2 Wiederholpuffer fließt ohne Reconnect | Von NAK-123 nicht geändert; Messort `broker/src/transport/warteschlange.rs:462-477` und `IpcTestMain.cpp:1948-1969`. |
| [x] A-3 Schlüssel überlebt Zwischenpuffer und Koaleszierung | Von NAK-123 nicht geändert; Messort `broker/src/transport/warteschlange.rs:494-509` und `IpcTestMain.cpp:1991-2036`. |
| [x] A-4 höhere Priorität wird trotz niedrigerer Blockade gelesen | Von NAK-123 nicht geändert; Leseschritt nach jedem Send `ControlClient.cpp:1398-1497`, bestehender Testabschnitt `IpcTestMain.cpp:3320-3580`. |
| [x] A-5 Rückgabewerte/Zähler werden im Lastbein ausgewertet | Neue Listener-, Worker- und Authzähler werden in `security_vectors.rs:206-696`, `IpcTestMain.cpp:899-1378` und `broker_idle.rs:99-173` abgefragt; Lauf `NOT RUN`. |
| [x] A-6 Regel in C++ und Rust beidseitig | Lebenszyklus-/Namensbesitz ist mit Rust A-02/A-06/A-07/A-08 und den gekoppelten C++-Tests in `IpcTestMain.cpp:1095-1378` beidseitig belegt; absichtlicher Rotlauf extern ausstehend. |
| [x] B-1 Verbindungsreihenfolge festgelegt und gemessen | Brokerfolge B-01/B-05 in `server_v3.rs:2222-2353`, Clientfolge C-02 in `IpcVerbindung.cpp:44-127`; zugehörige Tests `security_vectors.rs:428-532`, `IpcTestMain.cpp:916-949`, Lauf `NOT RUN`. |
| [x] B-2 beim Trennen zuerst entkoppeln, dann warten/joinen | Bestehender v3-Gegenpfad bleibt in `server_v3.rs:2390-2508,2840-2890`; NAK-123-Stopp hält Listener bis nach allen Joins (`server_v3.rs:1207-1255`), gemessen durch A-09/B-09, Lauf `NOT RUN`. |
| [x] B-3 jeder Join hat Frist oder getesteten Self-Join-Gegenpfad | Bestehende Senken-/Clientfristen bleiben `server_v3.rs:1294-1386,2860-2890` und `ControlClient.cpp:681-735`; A-09 prüft den neuen Listenergegenpfad. Kein neuer Callback-Join eingeführt. |
| [x] B-4 geschlossene Queue liefert nichts mehr | Von NAK-123 nicht geändert; Rust-Messort `server_v3.rs:3914-3940`. |
| [x] B-5 Registrierung im Stoppfenster ist deterministisch | Handle wird vor Spawn registriert (`server_v3.rs:1874-1891`), Stop bricht wiederholt ab (`server_v3.rs:1207-1238`); A-09/B-06 sind die neuen Messorte, Lauf `NOT RUN`. |
| [x] C-1 Textlängen exakt geprüft | `N/A`: NAK-123 ändert kein Schema oder Textfeld; bestehende Bootstrap-/Envelope-Längen bleiben unverändert. |
| [x] C-2 exakte Feldmenge in C++ und Rust | `N/A`: kein Drahtvertrag geändert; bestehender C++-Messort `IpcTestMain.cpp:3774-3781`, Rust-/Bootstrap-Parser unverändert. |
| [x] C-3 Discriminator/Familie/Typ vor Inhalt | B-05 ordnet nur die Sicherheitsprüfung vor die bestehende fachliche Ablehnung; Parser-/Familienvertrag bleibt `server_v3.rs:2334-2370,2620-2675`, bestehende Gegenpfade in `IpcTestMain.cpp:3010-3055`. |
| [x] C-4 NaN/Inf/Grenzwerte für Drahtzahlen | `N/A`: keine Drahtzahl geändert; bestehender Messort `IpcTestMain.cpp:2877-2904`. |
| [x] D-1 Riegel fail-closed ohne Rohtextheuristik | Serverauth und Listenerersatz behandeln jedes unbekannte/fehlende Urteil als unverifiziert/fatal: `BrokerLifecycle.cpp:462-600,721-780`, `server_v3.rs:1419-1463,1830-1855`; C-03/C-04/A-04, Lauf `NOT RUN`. |
| [x] D-2 Frische der Eingaben statt veraltetes Artefakt | Bewertet: Quell- und Testverdrahtung aktuell statisch geprüft; Artefaktfrische kann ohne Build nicht bewiesen werden und bleibt für den externen Kanon `NOT RUN`. |
| [x] D-3 Relink ändert Bundlebytes; Hash ist Auslieferungswert | Bewertet: Brokercode wurde geändert, daher muss der Auslieferungsprozess den Broker neu bauen und den SHA-256 im Installer-Manifest vor dem Produktlauf aktualisieren. `eq-copilot/install/**` liegt ausdrücklich außerhalb dieses Änderungssatzes; kein alter Hash wird hier als passend behauptet. |
| [x] D-4 nur vom Kanon Gebautes darf frisch heißen | Bewertet: kein Build und kein Kanonlauf auf diesem Rechner; deshalb keine Frische- oder Grünbehauptung. |
| [x] E-1 Behauptung höchstens so stark wie Messung/Gate | Dieser Abschnitt trennt Implementierung, statische Prüfung und `NOT RUN`; insbesondere wird weder Kompilierbarkeit noch Laufgrün behauptet. |
| [x] E-2 Zahlen im Manifest gemessen | Konstanten und Testanzahl stammen aus aktuellem Code (`MAX_VERBINDUNGEN=96`, `PIPE_INSTANZEN=98`, `server_v3.rs:87-91`) und der aktuellen Symbolsuche; dynamische Zählerwerte werden nicht vorweggenommen. |
| [x] E-3 lebender Kopf mit Symbol/Anker, Verlauf mit Standzeilen | Matrixzeilen blieben unverändert; der neue Verlauf nennt Symbole und aktuelle Datei:Zeile-Fundstellen zum Stand 2026-09-01. |
| [x] E-4 lebender Kopf nachziehen, darunter append-only | Die freigegebene MATRIX_PASS-Matrix wurde nicht umgeschrieben; dieser Bauabschnitt wurde append-only angehängt. Der Phase-1-Status in Zeile 9 bleibt als Status genau jener Phase historisch wahr. |
| [x] E-5 jede neue Prüfung einmal absichtlich gebrochen | Bewertet, aber **extern ausstehend**: Ohne Kompilier-/Testmöglichkeit wurde kein Rotlauf erzeugt und keine Rohausgabe erfunden. Der externe Kanon muss vor Grün jede neue A/B/C-Prüfung einmal gegen die jeweilige Testnaht brechen. |
| [x] E-6 geänderte Zusage an drei Stellen nachziehen | `N/A`: Die MATRIX_PASS-Zusage, Runnerköpfe und Skriptköpfe wurden nicht geändert; NAK-123 fügt Produktcode und die bereits in der Matrix benannten Tests hinzu. |
| [x] E-7 Writer-Fixtures statt handgeschriebener Writer-Form | `N/A`: NAK-123 ändert keinen persistenten oder Wire-Writervertrag. B-05 nutzt bewusst vollständige mutierte Bootstrap-Hellos; C-10 erzeugt den ACK-Envelope mit dem echten `envelopeSchreiben`. |
| [x] F-1 gekoppelte Lebenszyklen im selben Änderungssatz | Start/Stop/Idle/Fatalfehler liegen gemeinsam in `server_v3.rs`, `lib.rs`, `main.rs`; Öffnen/Schließen/Reconnect/Adoption gemeinsam in `IpcVerbindung`, Control, Telemetry, `PipeClient` und `BrokerLifecycle`. |
| [x] F-2 Writer, Reader, Migration, Fixtures, Cross-Language-Verbraucher gemeinsam | Kein Schema-/Migrationswechsel erforderlich. Die unveränderten Writer/Reader werden durch Rust-`security_vectors` und die C++-Control-/Telemetry-/PipeClient-Tests an derselben v3-/Bootstrapgrenze gekoppelt. |

### Getroffene technische Wahlen

- **Zwei echte Listener statt eines passiven Wächterhandles:** Nur zwei gleichzeitig bewaffnete `ConnectNamedPipe`-Instanzen und der Ersatz vor Workerübergabe erfüllen den lückenlosen Namensbesitz auch während Accept; das spiegelt das vorhandene Overlapped-I/O-Muster in `server_v3.rs`.
- **Eine binäre SID für DACL und Tokenvergleich:** `Sicherheit` hält genau die SID-Kopie, aus der der SDDL-Deskriptor gebaut wurde; dadurch können DACL- und Impersonationsentscheidung nicht durch zwei Normalisierungen auseinanderlaufen.
- **Fail-fast bei fehlgeschlagenem `RevertToSelf`:** Ein normaler Rückweg unter unbekanntem Threadtoken wäre selbst ein Sicherheitsbruch; deshalb beendet nur dieser Fehler den Brokerprozess unmittelbar.
- **Serverbeweis am noch unveröffentlichten Pipehandle:** `GetNamedPipeServerProcessId`, gehaltenes Prozesshandle, binärer User-SID-Vergleich, Prozessimage, Dateiidentität und die vorhandene Hash-/optionale Authenticode-Kette binden die Entscheidung an den tatsächlich verbundenen Server statt an Reachability oder einen PID-Cache.
- **Dreistufiger Lifecycle-Status:** `nicht da`, `verifiziert` und `belegt aber unverifiziert` verhindern, dass ein Sicherheitsfehler als Livenessfehler einen Spawn oder automatischen Fallback auslöst.
- **Reconnect an eine Generation gebunden:** Ein während PID-/SID-/Image-/Hashprüfung ausgelöster Reconnect darf weder das alte Urteil noch dessen Handle in die neue Generation veröffentlichen; dasselbe Muster wird in Control, Telemetry und `PipeClient` verwendet.
- **Idle zählt bereits angenommene Worker:** Auch ein Client im noch unvollständigen Bootstrap verhindert das 60-s-Selbstende; der Coordinator allein kennt diesen Zustand noch nicht.
- **Keine neue Abhängigkeit und keine Audio-Thread-Kante:** Verwendet werden die bereits gelinkten Win32-Bibliotheken und die vorhandene Broker-Hash-/Authenticode-Implementierung; alle neuen Aufrufe bleiben auf Client-, Lifecycle- oder Brokerthreads.
- **Auslieferungshash bleibt Packaging-Aufgabe:** Weil der Broker neu gelinkt werden muss, ist der heutige Installer-Hash kein Nachweis für das neue Binary; die Aktualisierung muss im ausdrücklich nachgelagerten, externen Auslieferungsschritt erfolgen.

## Nachtrag 2026-09-01 spät — Zwischenstand committet, Bauerwechsel

Fable-Sitzung `e0375c12` (User-Gespräch, kein Dirigentenlauf). Der Bau oben lag seit 18:23 Uhr uncommittet im Worktree; kein Dirigent, kein Worker und kein Codex-Thread lief mehr. Damit der nächste Worker ihn übernehmen kann, ohne an der Regel „fremde uncommittete Dateien nie anfassen" zu scheitern, ist er als benannter Zwischenstand committet (Commit-Betreff „NAK-123 Zwischenstand: Codex-Bau der G2-Nacharbeit, nicht kompiliert, Tests NOT RUN"). Das ändert am Laufstatus nichts: alle Tests dieses Abschnitts bleiben `NOT RUN`.

Einzige Messung aus dieser Sitzung: `cargo check --tests` im Broker auf genau diesem Stand endet mit Exit 0 in 4,5 s inkrementell; eine Warnung `dead_code` für das Feld `coordinator` in `BrokerLauf` (`broker/src/lib.rs:839-849`). Die C++-Seite wurde nicht übersetzt.

User-Entscheid 01.09. spät, wörtlich: „opus wieder bauer und codex prüfer . so wie am anfang, habe genug wochen kontigent". Die G2-Nacharbeit wird deshalb von einem Opus-Worker im sichtbaren Checkout fortgesetzt, der kompiliert, die Matrix-Tests A/B/C fährt, jede neue Prüfung einmal absichtlich bricht (Prüfliste E-5) und den Kanon abgekoppelt laufen lässt; die Prüfung bleibt ein frischer Codex-Thread. Offen bleibt aus der Prüfliste D-3: Installer-Hash des neu gelinkten Brokers.

---

## Kanon-Lauf - NAK-123

**Lauf:** 2026-09-02 01:07 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 39/39 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-123-0362720-dirty.md](roh/NAK-123-0362720-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-02 01:07:07 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 0362720 NAK-123: E-5-Bruchproben, 19 Kippungen, jede neue Pruefung einmal rot |
| Commit (voll) | 0362720a7f87af8320f1d966263aeeff5dae7229 |
| Arbeitsbaum | 1 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,10 s | [A1](roh/NAK-123-0362720-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,27 s | [A2](roh/NAK-123-0362720-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 6,49 s | [A3](roh/NAK-123-0362720-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 77,11 s | [A4](roh/NAK-123-0362720-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 7,27 s | [A4-SI](roh/NAK-123-0362720-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 0,60 s | [A4b](roh/NAK-123-0362720-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 2,56 s | [A5](roh/NAK-123-0362720-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,17 s | [A6](roh/NAK-123-0362720-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-123-0362720-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,19 s | [A8](roh/NAK-123-0362720-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,40 s | [A9](roh/NAK-123-0362720-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen und band_stereo ID 10 samt saturated-/Grenzfaellen sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 1,97 s | [A10](roh/NAK-123-0362720-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,27 s | [A11](roh/NAK-123-0362720-dirty.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A12](roh/NAK-123-0362720-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,22 s | [A13](roh/NAK-123-0362720-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 5,91 s | [A14](roh/NAK-123-0362720-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,02 s | [A17](roh/NAK-123-0362720-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 32,35 s | [A18](roh/NAK-123-0362720-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,13 s | [A19](roh/NAK-123-0362720-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A20](roh/NAK-123-0362720-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,27 s | [A21](roh/NAK-123-0362720-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,01 s | [A22](roh/NAK-123-0362720-dirty.md#a22) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,72 s | [A23](roh/NAK-123-0362720-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,10 s | [A16](roh/NAK-123-0362720-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,12 s | [B1](roh/NAK-123-0362720-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,15 s | [B2](roh/NAK-123-0362720-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,08 s | [B3](roh/NAK-123-0362720-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,10 s | [B3b](roh/NAK-123-0362720-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,25 s | [B3c](roh/NAK-123-0362720-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten, und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,43 s | [B4](roh/NAK-123-0362720-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,15 s | [B9](roh/NAK-123-0362720-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle 61 Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen - waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, aber kein unbewiesenes Projektintervall oder FFT-Event erzeugen; Frame-Stempel umfassen den echten ueberlappenden FFT-Support und bleiben an int64/u32-Grenzen darstellbar; Drop zaehlt als Segment, alles andere als Epoche; alle sieben Erzeuger-Stempelregeln fallen einzeln und mit eigener Nummer; kein spektraler Fluss ueberbrueckt eine Grenze, der Ereignisring ist fest gedeckelt; LUFS-S trifft die analytisch gerechnete Erwartung unter 0,1 LU; die Nyquist-Kappe greift bei 22,05 kHz wirklich; verdrahtet: alle sieben Gueltigkeitsbits kommen ueber die Hostbruecke durch, der Playhead-Rueckfallweg nachweislich nur zwei. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 2,04 s | [B5](roh/NAK-123-0362720-dirty.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 59,85 s | [B10](roh/NAK-123-0362720-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 0,11 s | [B11](roh/NAK-123-0362720-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,54 s | [B12](roh/NAK-123-0362720-dirty.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,10 s | [B13](roh/NAK-123-0362720-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,18 s | [B14](roh/NAK-123-0362720-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,03 s | [B15](roh/NAK-123-0362720-dirty.md#b15) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,13 s | [B8](roh/NAK-123-0362720-dirty.md#b8) |


---

## Bau- und Beweislauf 2026-09-02

Opus-Worker im sichtbaren Checkout, Auftrag aus dem Nachtrag 01.09. spät.
Auftragsbasis war `a0be9c80deaa78aba5d13539c0e5500e460b69c6`; während des Laufs
hat eine parallele Design-Sitzung `2efb5b3` gepusht, worauf ohne Überschneidung
rebased wurde. Commits dieses Laufs: `25c20cd` (Broker), `0f92c88` (Plugin),
`0362720` (E-5-Rohausgabe) und der Commit dieses Abschnitts.

Der Stand aus `c197fcd` war **nie kompiliert**. Beide Sprachen sind auf diesem
Rechner jetzt übersetzt, alle Zieltests gefahren, jede neu benannte Prüfung
einmal absichtlich gebrochen und der Kanon abgekoppelt gefahren. Der Laufstatus
`NOT RUN` aus dem Abschnitt „Bau 2026-09-01" ist damit für jede Matrixzeile
ersetzt; jener Abschnitt bleibt als historischer Stand unverändert stehen. Die
freigegebene Matrix A-01..C-11 wurde nicht angefasst.

### Kompilierstand

| Seite | Ergebnis |
|---|---|
| Rust | `cargo test --manifest-path broker/Cargo.toml --no-run` Exit 0. Die `dead_code`-Warnung für `coordinator` in `BrokerLauf` ist behoben (`_coordinator`, reiner Lebensdauerhalter, gleiche Unterstrich-Konvention wie `_supervisor`/`_griff_v2`/`_griff_v3`); der Bau ist warnungsfrei. |
| C++ | `cmake --build eq-copilot/build --config Release` für alle Zieltests und Bundles Exit 0, ohne eine einzige `warning C`-Zeile. |

Der Zwischenstand kompilierte auf beiden Seiten unverändert. Alle Befunde unten
sind **Laufbefunde**, keine Übersetzungsfehler — genau die Klasse, die ein
`cargo check --tests` nicht sehen kann.

### Befunde und Fixes

Drei Befunde, jeweils an der gemeinsamen Ursache behoben statt am Symptom.

| # | Datei | Ursache | Betroffene Matrixzeile | Fix |
|---|---|---|---|---|
| 1 | `eq-copilot/plugin/src/PipeClient.cpp` (Produktcode) | `ERROR_PIPE_BUSY` wurde auf das Sicherheitsurteil `belegtAberUnverifiziert` abgebildet. BUSY heißt aber nur „der Name existiert, gerade horcht keine Instanz" — Liveness, kein Sicherheitsbefund. Weil C-07 dieses Urteil bewusst fail-closed und endgültig macht (kein weiterer Reconnect, kein Spawn), parkte der Clientthread danach dauerhaft im `authBlockiert`-Warten; der Testpeer blieb in `ConnectNamedPipe` stehen und der ganze Testprozess hing statt rot zu werden. | C-06, C-09 (`pipeclient_reconnect_verwirft_serverfreigabe` und die bestehende Prüfung „Reconnect während Welcome verliert keine Konfigurationsgeneration") | Die BUSY-Retryschleife aus dem v3-Pfad `IpcVerbindung::oeffnen` (dort seit NAK-104) in den v2-Pfad gespiegelt, mit Stop- und Generationsprüfung je Runde. C-06 verlangt für alle drei Connectpfade dieselbe Regel. Nur `ERROR_FILE_NOT_FOUND` bleibt `nichtDa`/`pipeFehlt` (C-01). |
| 2 | `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp` | Der Test war nicht rot, sondern **wirkungslos**: ohne `prepareToPlay` trug `v3Hello()` samplerate/blockSize/channels = 0. Der `ControlClient` wies sein eigenes Hello schon vor `CreateFileW` ab („Audiolage hält den v3-Vertrag nicht"), öffnete die Testpipe nie und maß den C-10-Angriff überhaupt nicht. Sichtbar wurde das nur, weil der Peer danach in `ConnectNamedPipe` hängenblieb; ohne diese Zeitüberschreitung wäre der Test als vorhanden durchgegangen. | C-10 | Prozessor wird mit 48000/512 vorbereitet, bevor das Hello gebildet wird. Der Angriff läuft jetzt wirklich: der unverifizierte Server schreibt sein gefälschtes `command_ack` mit exakt passender `command_id`, und der Test misst dessen Wirkungslosigkeit. |
| 3 | `eq-copilot/plugin/tests/IpcTestMain.cpp` | `TestServer::starten()` kehrte zurück, bevor der Acceptorthread seine erste Pipeinstanz angelegt hatte. Für Tests, die über einen `ControlClient` mit Warteschleife verbinden, blieb das folgenlos; die beiden C-07-Tests ticken den Lifecycle dagegen sofort einmal an und sahen den Namen mal, mal nicht. | C-07 | `starten()` wartet auf die erste Instanz und meldet ehrlich, ob sie steht; beide C-07-Tests werten diesen Rückgabewert aus. |

Zusätzlich in `PipeClientLifecycleTestMain.cpp` und `Sonde012ProjectReloadTest.cpp`
ein Weckruf vor `peer.join()`. Eine Prüfung, die im roten Fall nicht rot wird,
sondern den Kanon blockiert, ist keine Prüfung (Prüflistenzeile E-5) — und genau
das war bei den Befunden 1 und 2 die einzige Außenwirkung.

### Laufstatus je Matrixzeile

| ID | Messender Test | Lauf |
|---|---|---|
| A-01 | `security_vectors::start_freier_name_bewaffnet_zwei_listener_vor_veroeffentlichung`, `::startfehler_nach_erstem_handle_raeumt_handles_threads_und_namen_vor_publish` (5 Fehlerkanten) | PASS |
| A-02 | `security_vectors::start_belegter_name_scheitert_ohne_zweiten_broker`; `IpcTestMain.cpp/vorhandene_fremdpipe_wird_nicht_adoptiert` | PASS |
| A-03 | `security_vectors::accept_bewaffnet_nachfolger_vor_worker_uebergabe_bei_zwei_listenern` | PASS |
| A-04 | `security_vectors::ersatzlistener_fehler_behaelt_besitz_und_meldet_nicht_bereit` | PASS |
| A-05 | `security_vectors::zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz` | PASS |
| A-06 | `broker_idle::echter_brokerprozess_beendet_sich_nach_letztem_client_selbst` (echter Prozesslauf, Testdatei 60,59 s); `IpcTestMain.cpp/serverauth_nach_idle_exit_verweigert_fremde_pipe` | PASS |
| A-07 | `security_vectors::prozesskill_gibt_name_frei`; `IpcTestMain.cpp/serverauth_nach_brokercrash_verweigert_pidwechsel_und_fremdbild` | PASS |
| A-08 | `security_vectors::neustart_besetzt_name_vor_bereit`; `IpcTestMain.cpp/reconnect_sendet_erst_nach_neuer_serverauth` | PASS |
| A-09 | `security_vectors::stoppen_schliesst_besitzlistener_zuletzt_und_neustart_ist_sofort_moeglich` | PASS |
| B-01 | `security_vectors::gleiche_sid_wird_erst_nach_revert_angenommen` | PASS |
| B-02 | `security_vectors::impersonation_fehlschlag_schliesst_ohne_serverkontext` | PASS |
| B-03 | `security_vectors::tokenabfragefehler_revertiert_und_schliesst_ohne_senke` (4 API-Kanten) | PASS |
| B-04 | `security_vectors::fremde_sid_revertiert_und_erreicht_keinen_coordinator` | PASS |
| B-05 | `security_vectors::vollstaendiges_abzulehnendes_hello_impersoniert_prueft_sid_und_revertiert_vor_reject` (JSON, Typ, Kind, Protokoll) | PASS |
| B-06 | `security_vectors::bootstrap_timeout_und_lesefehler_revertieren_jeweils_ohne_serverweiterlauf` (EOF, Teilpräfix, Übergröße, Timeout, Stop-Cancel) | PASS |
| B-07 | `security_vectors::revert_fehlschlag_beendet_testbrokerprozess_fail_fast` | PASS |
| B-08 | `security_vectors::control_und_telemetry_durchlaufen_die_identische_sid_kette` | PASS |
| B-09 | `security_vectors::nach_revert_bleiben_envelope_und_trennpfade_im_self_kontext` | PASS |
| C-01 | `IpcTestMain.cpp/fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` | PASS |
| C-02 | `IpcTestMain.cpp/serverauth_gueltiger_pid_sid_datei_hash_und_signer_vor_hello` | PASS |
| C-03 | `IpcTestMain.cpp/serverauth_fremde_sid_und_tokenabfragefehler_senden_null_bytes` | PASS |
| C-04 | `IpcTestMain.cpp/serverauth_bild_dateiid_hash_signatur_und_signer_fail_closed` | PASS |
| C-05 | `IpcTestMain.cpp/waitnamedpipe_toctou_wird_am_verbundenen_handle_erkannt` | PASS |
| C-06 | `IpcTestMain.cpp/control_und_telemetry_authentisieren_jedes_handle`; `PipeClientLifecycleTestMain.cpp/pipeclient_sendet_vor_serverauth_keine_bytes` | PASS |
| C-07 | `IpcTestMain.cpp/vorhandene_pipe_wird_nur_mit_dem_authentisierten_handle_uebernommen`, `/unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht`, `/vorhandene_fremdpipe_wird_nicht_adoptiert` | PASS |
| C-08 | `IpcTestMain.cpp/spawn_pid_ersetzt_serverauth_nicht_und_fremdpipe_im_bereitfenster_faellt` | PASS |
| C-09 | `IpcTestMain.cpp/reconnect_prueft_pid_sid_und_bild_ohne_altfreigabe`, `/reconnect_sendet_erst_nach_neuer_serverauth`; `PipeClientLifecycleTestMain.cpp/pipeclient_reconnect_verwirft_serverfreigabe` | PASS |
| C-10 | `Sonde012ProjectReloadTest.cpp/gefaelschtes_command_ack_vor_serverauth_mutiert_keinen_persistenten_projektzustand` | PASS |
| C-11 | `LebenslaufTestMain.cpp/broker_lifecycle_aufrufe_im_audiothread_null`, `/brokerstop_nie_im_processblock`, `/processblock_quellschnitt_{datei,pipe,prozess,log}operationen_null`, `/processblock_quellschnitt_store_wait_null` | PASS |

Zugehörige Laufzahlen aus den Ausgaben dieses Laufs: `security_vectors` 18/18,
`broker_idle` 4/4, Broker gesamt (`cargo test`) 190 + 4 + 9 + 34 + 18 + 26 + 53
(17 ignoriert) + 9 ohne Fehler; `EqCopIpcTest` 262 Prüfungen 0 Fehler,
`EqCopPipeClientTest` 17 Prüfungen 0 Fehler, `EqCopLebenslaufTest` 77 Prüfungen
0 Fehler, `EqCopSonde012ProjectReloadTest` 10/10, `EqCopProbeeqNullTest` 87
Prüfungen 0 Fehler; A22 Exit 0 (10 048/10 048 P0 bei 35 421 ersetzten
P2-Frames), A23 Exit 0 (p95 48,9/92,7/347,2 ms bei 16 Quellen und
52,5/95,7/351,1 ms bei 32 Quellen).

### E-5: jede neue Prüfung einmal absichtlich gebrochen

19 Fälle, je genau eine gekippte Produktstelle, Rotlauf belegt, Kippung sofort
danach aus einer Sicherung zurückgenommen. Der Grünlauf wurde nach der
Rust-Gruppe unmittelbar erneut belegt (`security_vectors` 18/18) und für beide
Sprachen abschließend durch den Kanon (39/39, Exit 0) auf dem
zurückgenommenen Stand. Kein gekippter Stand wurde committet; `git status` war
vor und nach jedem Fall sauber, geprüft nach dem letzten Fall mit leerem
`git diff`. Vollständige Rohausgabe:
[roh/NAK-123-e5-bruchproben-0f92c88.md](roh/NAK-123-e5-bruchproben-0f92c88.md).

| Fall | Matrix | Gekippte Stelle | Beobachteter Rotbefund |
|---|---|---|---|
| R1 | A-02, A-08 | `server_v3.rs` `ListenerInstanz::neu`: erste Instanz ohne `FILE_FLAG_FIRST_PIPE_INSTANCE` | `start_belegter_name_scheitert_ohne_zweiten_broker` und `neustart_besetzt_name_vor_bereit`: `v3_server_starten_fuer_security_vectors(...).is_err()` schlägt fehl — der Start gelingt auf einem fremd belegten Namen |
| R2 | A-01 | `server_v3.rs`: zweiter Listener wird erzeugt, aber nie bewaffnet | `start_freier_name_bewaffnet_zwei_listener_vor_veroeffentlichung`: `bewaffnete_listener == 2` schlägt fehl |
| R3 | A-01 | `server_v3.rs`: Startfehlerkante `ZweiteInstanz` gibt das erste Handle nicht per RAII frei | `startfehler_nach_erstem_handle_...`: „Fehlerkante ZweiteInstanz liess den Namen besetzt" |
| R4 | A-03 | `server_v3.rs`: Übergabefenster liegt vor der Armierung des Ersatzlisteners | `accept_bewaffnet_nachfolger_...`: `bewaffnete_listener == 2` am Barrierepunkt schlägt fehl |
| R5 | A-04 | `server_v3.rs`: Ersatzlistener-Fehler wird verschluckt (kein `listener_fehler`, kein Stop) | `ersatzlistener_fehler_...`: `listener_fehler == 1` wird binnen 3 s nicht erreicht |
| R6 | A-05 | `server_v3.rs` `worker_reservieren`: Cap auf `MAX_VERBINDUNGEN + 4` angehoben | `zwei_listener_plus_96_worker_...`: `am_worker_cap_abgewiesen == 1` wird nicht erreicht |
| R7 | A-07 | `server_v3.rs`: Broker besitzt `<name>.e5` statt des geforderten Namens | `prozesskill_gibt_name_frei`: `FirstInstance::nehmen(&pipe).is_none()` schlägt fehl, solange der Broker lebt |
| R8 | A-09 | `server_v3.rs` `V3Griff::stoppen`: Besitzlistener fallen vor allen Joins | `stoppen_schliesst_besitzlistener_zuletzt_...`: Reihenfolge `close` < `listeners_close` verletzt |
| R9 | A-06 | `server_v3.rs` `V3Griff::aktive_worker` liefert immer 0 | `echter_brokerprozess_beendet_sich_nach_letztem_client_selbst`: die 60-s-Frist läuft ab Prozessstart statt ab dem letzten Client, `dauer >= BROKER_IDLE_ENDE_MS` schlägt fehl |
| R10 | B-01, B-02, B-03, B-04, B-05, B-08, B-09 | `server_v3.rs` `client_sid_authentisieren` nimmt jede Verbindung ohne Impersonation, SID-Vergleich und Revert an | alle sieben benannten Tests FAILED (0 passed / 7 failed): fehlende Spurschritte `impersonate`/`sid`/`revert`/`self`, Fachlogik trotz Fehlerinjektion erreicht |
| R11 | B-06 | `server_v3.rs`: Impersonation-/SID-Kette läuft vor dem Bootstrap-Read | `bootstrap_timeout_und_lesefehler_...`: `!spur.contains("impersonate")` schlägt fehl — auch der Timeout-/EOF-Pfad impersoniert |
| R12 | B-07 | `server_v3.rs` `VerbindungsSicherheitsCleanup::revertieren` ohne `process::abort` | `revert_fehlschlag_beendet_testbrokerprozess_fail_fast`: „Revert-Fehler liess den Broker weiterlaufen" |
| C1 | gemessen rot: C-02, C-03, C-04, C-05, C-08, C-09, C-10 sowie die C++-Gegenbeine von A-06, A-07, A-08 und `pipeclient_sendet_vor_serverauth_keine_bytes` aus C-06 | `BrokerLifecycle.cpp` `namedPipeServerAuthentisieren` meldet jeden Server sofort als verifiziert | `EqCopIpcTest` 9 Fehler bei 262 Prüfungen (`serverauth_gueltiger_...`, `serverauth_fremde_sid_...`, `serverauth_bild_dateiid_...`, `waitnamedpipe_toctou_...`, `spawn_pid_ersetzt_serverauth_nicht_...`, `reconnect_prueft_pid_sid_und_bild_...`, `reconnect_sendet_erst_nach_neuer_serverauth`, `serverauth_nach_brokercrash_...`, `serverauth_nach_idle_exit_...`); `EqCopPipeClientTest` `pipeclient_sendet_vor_serverauth_keine_bytes`; `EqCopSonde012ProjectReloadTest` `gefaelschtes_command_ack_...` |
| C2 | C-01 | `IpcVerbindung.cpp`: fehlende Pipe wird als `belegtAberUnverifiziert`/`pipeOeffnen` gemeldet | `fehlender_broker_ist_einzige_startbedingung_auch_mit_serverauth` und drei weitere Startbedingungsprüfungen FEHLER |
| C3 | C-11 | `PluginProcessor.cpp`: `processBlock` ruft `brokerLifecycleSnapshot()` | `broker_lifecycle_aufrufe_im_audiothread_null`, `brokerstop_nie_im_processblock` und alle fünf `processblock_quellschnitt_*`-Prüfungen FEHLER |
| C4 | C-06 | `TelemetryClient.cpp`: übernimmt die Control-Freigabe, führt und zählt keine eigene Handleprüfung | `control_und_telemetry_authentisieren_jedes_handle` FEHLER (`t.serverPruefungen == 1` hält nicht) |
| C5 | C-07 (mit dem C++-Gegenbein von A-02) und C-08 | `BrokerLifecycle.cpp`: `belegtAberUnverifiziert` nicht mehr fail-closed | `unverifizierte_belegte_pipe_spawnt_und_reconnectet_nicht`, `vorhandene_fremdpipe_wird_nicht_adoptiert`, `spawn_pid_ersetzt_serverauth_nicht_...` FEHLER |
| C6 | C-07 | `BrokerLifecycle.cpp` `vorhandenePipeUebernehmen`: Erreichbarkeit ersetzt das Handleurteil | `vorhandene_pipe_wird_nur_mit_dem_authentisierten_handle_uebernommen` FEHLER |
| C7 | C-09 und C-06 | `PipeClient.cpp`: eine einmal erteilte Serverfreigabe überlebt den Reconnect | `pipeclient_reconnect_verwirft_serverfreigabe` und `pipeclient_sendet_vor_serverauth_keine_bytes` FEHLER |

Die Spalte „Matrix" nennt hier die **gemessene** Wirkung, nicht die vorab
vermutete. In der Übersichtstabelle der Rohdatei
`roh/NAK-123-e5-bruchproben-0f92c88.md` steht die vorab geplante Zuordnung je
Kippung; maßgeblich sind dort die `FEHLER`-Zeilen der Einzelfälle. Jede in der
Matrix benannte Prüfung ist über die Fälle hinweg mindestens einmal rot
gewesen.

### Kanon

`pwsh -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-123.md -Anhaengen -Titel NAK-123`,
abgekoppelt gestartet, keine Quelldatei während des Laufs geändert.

**GRÜN — 39/39 Kanon-Läufe bestanden, Exitcode 0**, Lauf 2026-09-02 01:07 auf
Commit `0362720`. Kopf und Übersicht stehen im Abschnitt „Kanon-Lauf - NAK-123"
oben, die Rohausgabe in [roh/NAK-123-0362720-dirty.md](roh/NAK-123-0362720-dirty.md).
Die Marke `-dirty` trägt genau eine unbestätigte Datei, den fremden
uncommitteten Ordner `.workflow/ultracode/20260831-084621-sonde011-phaseb-bau/`;
er wurde nicht angefasst und gehört nicht zu diesem Änderungssatz. Die
Runnerzeile „2 geplante Prüfung(en) noch nicht gebaut | 1 stillgelegte(s)
Bein(e)" betrifft B6/B7 (ab P6) und A15 (Suna-Stilllegung 28.08.) und ist von
NAK-123 unberührt.

### Abgehakte Prüflistenzeilen mit Messort

`[x]` heißt: Zeile bewertet und einem Messort dieses Laufs oder einem begründeten
`N/A` zugeordnet. Anders als im Abschnitt vom 01.09. steht hinter jeder Zeile mit
Testbezug jetzt ein tatsächlicher Lauf.

| Prüflistenzeile | Ergebnis und Messort |
|---|---|
| [x] A-1 Politik bei voller P0/P1/P2-Klasse ausdrücklich | `N/A` für NAK-123: keine Queue-Politik geändert. Im selben Kanonlauf gemessen durch A22: 35 421 ersetzte P2-Frames, `0 abgelehnt = 0 zu gross + 0 ohne Platz`, `envelope=0 rate=0 p0=0`. |
| [x] A-2 Wiederholpuffer fließt ohne Reconnect | `N/A` für NAK-123. Messort im selben Lauf: A22, `kein P0-Frame geht verloren (Clientseite) 10048/10048` ohne Reconnect. |
| [x] A-3 Schlüssel überlebt Zwischenpuffer und Koaleszierung | `N/A` für NAK-123. Messort im selben Lauf: A22, `0 neueste verworfen, 0 Positionen uebersprungen` (replace-oldest). |
| [x] A-4 höhere Priorität wird trotz niedrigerer Blockade gelesen | `N/A` für NAK-123. Messort im selben Lauf: A22, `10048 empfangen / 10048 beantwortet` bei 95 943 P2-Frames und nachweislichem Rückstau. |
| [x] A-5 Rückgabewerte/Zähler werden im Lastbein ausgewertet | Erfüllt: `security_vectors` liest `bewaffnete_listener`, `worker_uebergaben`, `aktive_worker`, `am_worker_cap_abgewiesen`, `listener_fehler` und die Sicherheitsspur; `IpcTestMain.cpp` liest `serverPruefungen`, `serverPid`, `p0Gesendet`, `p1Gesendet`, `empfangen`. Jeder dieser Zähler wurde in E-5 einmal rot gesehen (R2, R4, R5, R6, R8, C1, C4, C7). |
| [x] A-6 Regel in C++ und Rust beidseitig | Erfüllt: A-02, A-06, A-07 und A-08 haben je ein Rust- und ein C++-Bein; beide Seiten wurden einzeln rot gesehen (Rust R1/R7/R9, C++ C1). |
| [x] B-1 Verbindungsreihenfolge festgelegt und gemessen | Erfüllt: Brokerfolge `connect < read < impersonate < sid < revert < self < hello_accept` in B-01/B-08; Clientfolge `CreateFileW < Serverauth < Handleveröffentlichung < Hello` in C-02/C-06/C-10. Beide in E-5 rot gesehen (R10, C1). |
| [x] B-2 beim Trennen zuerst entkoppeln, dann warten/joinen | Erfüllt: A-09 misst `close` vor `listeners_close`; B-06 misst denselben Vorrang im Stop-Cancel-Fall. Rot gesehen in R8. |
| [x] B-3 jeder Join hat Frist oder getesteten Self-Join-Gegenpfad | Erfüllt: `EqCopPipeClientTest` misst `stop()` in beiden Blockadefällen unter 1000 ms (gemessen 0 ms und 0 ms). NAK-123 führt keinen neuen Callback-Join ein; die neue BUSY-Retryschleife prüft Stop und Generation je Runde und blieb in denselben Läufen unter der Frist. |
| [x] B-4 geschlossene Queue liefert nichts mehr | `N/A` für NAK-123; von diesem Änderungssatz nicht berührt. Rust-Messort unverändert im Kanonbein A4 (Exit 0, 77,11 s). |
| [x] B-5 Registrierung im Stoppfenster ist deterministisch | Erfüllt: das angenommene Handle wird vor dem Threadspawn registriert; A-09 und der Stop-Cancel-Fall aus B-06 messen den Gegenpfad. Rot gesehen in R8. |
| [x] C-1 Textlängen exakt geprüft | `N/A`: NAK-123 ändert kein Schema und kein Textfeld. |
| [x] C-2 exakte Feldmenge in C++ und Rust | `N/A`: kein Drahtvertrag geändert. |
| [x] C-3 Discriminator/Familie/Typ vor Inhalt | Erfüllt: B-05 ordnet die Sicherheitsprüfung vor die bestehende fachliche Ablehnung und misst das für ungültiges JSON, falschen Typ, falsches `connection_kind` und Protokollmismatch. Rot gesehen in R10. |
| [x] C-4 NaN/Inf/Grenzwerte für Drahtzahlen | `N/A`: keine Drahtzahl geändert. |
| [x] D-1 Riegel fail-closed ohne Rohtextheuristik | Erfüllt: unbekannte oder fehlende Urteile gelten als unverifiziert beziehungsweise fatal (C-03, C-04, A-04). Die Gegenprobe C5 zeigt, was ohne diese Regel passiert: der Lifecycle spawnt gegen den belegten Namen. |
| [x] D-2 Frische der Eingaben statt veraltetes Artefakt | Erfüllt durch den Kanonlauf: `tools/beweise.ps1 -Bauen` baut alle Kanonziele und die gemessenen Ziele vor dem Lauf neu; die Tabelle „Baustand der Prüfbinaries" in der Rohausgabe weist jedes Binary als `frisch (Bau bestaetigt)` aus. |
| [x] D-3 Relink ändert Bundlebytes; Hash ist Auslieferungswert | **Offen und ausdrücklich außerhalb dieses Änderungssatzes.** Der Brokercode wurde geändert, also trägt `eqcop-broker.exe` neue Bytes; der SHA-256 im Installer-Manifest ist für dieses neue Binary kein Nachweis mehr. `eq-copilot/install/**` liegt laut Ticketgrenze außerhalb. Das Kanonbein A17 prüft die Manifeststruktur und den Journalvertrag, nicht den Auslieferungshash gegen ein frisch gebautes Binary — hier wird kein alter Hash als passend behauptet. |
| [x] D-4 nur vom Kanon Gebautes darf frisch heißen | Erfüllt: die Frischebehauptung dieses Abschnitts stützt sich auf den Kanonlauf, nicht auf die Einzelläufe während der Fehlersuche. |
| [x] E-1 Behauptung höchstens so stark wie Messung/Gate | Erfüllt: Kompilierstand, Einzelläufe, E-5-Rotläufe und Kanon sind getrennt ausgewiesen. Für D-3 wird ausdrücklich nichts behauptet. |
| [x] E-2 Zahlen im Manifest gemessen | Erfüllt: alle Zahlen stammen aus den Laufausgaben dieses Abschnitts (Testanzahlen, Sekunden, p95-Werte, Frameanzahlen, Kanonzahl 39/39). |
| [x] E-3 lebender Kopf mit Symbol/Anker, Verlauf mit Standzeilen | Erfüllt: die freigegebene Matrix blieb wörtlich unverändert; dieser Verlaufsabschnitt nennt Testsymbole und Dateinamen statt Zeilennummern fremder Stände. |
| [x] E-4 lebender Kopf nachziehen, darunter append-only | Erfüllt: nachgezogen wurde ausschließlich die Kopfzeile `Beweisstatus`; alles darunter blieb unverändert, dieser Abschnitt ist angehängt. |
| [x] E-5 jede neue Prüfung einmal absichtlich gebrochen | Erfüllt: 19 Fälle, alle ROT, Tabelle oben, Rohausgabe `roh/NAK-123-e5-bruchproben-0f92c88.md`. |
| [x] E-6 geänderte Zusage an drei Stellen nachziehen | `N/A`: keine Runner- oder Skriptzusage geändert. `tools/beweise.ps1` und alle Beinbehauptungen sind unverändert; NAK-123 fügt Produktcode und die bereits in der Matrix benannten Tests hinzu. |
| [x] E-7 Writer-Fixtures statt handgeschriebener Writer-Form | Erfüllt: C-10 erzeugt den gefälschten ACK-Envelope mit dem echten `envelopeSchreiben`; B-05 nutzt deklarierte Mutanten vollständiger Bootstrap-Hellos. |
| [x] F-1 gekoppelte Lebenszyklen im selben Änderungssatz | Erfüllt: Start/Stop/Idle/Fatalfehler liegen gemeinsam in `server_v3.rs`, `lib.rs`, `main.rs`; Öffnen/Schließen/Reconnect/Adoption gemeinsam in `IpcVerbindung`, `ControlClient`, `TelemetryClient`, `PipeClient` und `BrokerLifecycle`. Der BUSY-Fix berührt Öffnen und Reconnect im selben Commit. |
| [x] F-2 Writer, Reader, Migration, Fixtures, Cross-Language-Verbraucher gemeinsam | Erfüllt: kein Schema- oder Migrationswechsel nötig. Die unveränderten Writer/Reader sind über `security_vectors` (Rust) und die C++-Control-/Telemetry-/PipeClient-Tests an derselben v3-/Bootstrapgrenze gekoppelt; beide Seiten wurden in E-5 rot gesehen. |

### Offene Punkte

- **D-3 Installer-Hash — offen.** Der neu gelinkte `eqcop-broker.exe` braucht
  einen aktualisierten SHA-256 in `eq-copilot/install/nakama-installer-v1.json`.
  Das liegt außerhalb der Ticketgrenze von NAK-123 und ist hier weder ausgeführt
  noch behauptet.
- **NAK-124, NAK-125, NAK-126** bleiben unverändert außerhalb (siehe Abschnitt
  „Bewusst außerhalb").

---

## Prüfrunden (Dirigent, 2026-09-02)

| Runde | Prüfer | Bereich | Urteil |
|---|---|---|---|
| Bau | Opus-Worker `nakama-nak123-a0be9c8-bau` (Agent `97d2409c`), Effort max, Aufsicht ENG | Basis `a0be9c8` → `05973e0` (Commits `25c20cd`, `0f92c88`, `0362720`, `05973e0`); Kanon 39/39 GRÜN auf `0362720` | Rundenbilanz `a0be9c8..05973e0`: Produkt 2 Dateien +40/−10, Tests 3 Dateien +63/−5, Doku 16 Dateien +6772/−7 (Doku enthält den parallelen Design-Commit `2efb5b3` einer User-Sitzung; er berührt keinen Ticketpfad) |
| Erstprüfung NAK-123 | Codex `gpt-5.6-sol`, Effort max, read-only, Thread `01a05f49-10db-74b0-91bd-f9725867b016`, 1102 s, 118 lesende Kommandos | `e0a0dd6...05973e0`, die 32 Ticketpfade (Prüfauftrag Vorlage A, Gate-Text §48.4 und §66.2 wörtlich) | **PASS** — „kein reproduzierbarer Defekt"; Lücken: keine; Härtungen: keine; nicht geprüft: die Testläufe wurden ausgewertet, nicht wiederholt; HEAD vor/nach Lauf identisch. Das Codex-`review`-Subkommando gab die Urteilszeile erst auf Nachfrage im selben Thread aus (`nakama-05973e0…-review-form.jsonl`). |
| G2-Wiederprüfung Runde 1 | Codex `gpt-5.6-sol`, Effort max, read-only, Thread `01a05f5c-c8ff-71d3-81b5-13153925f266`, 1259 s, 124 Kommandos | Fixdiff `e77ebf9...05973e0` (G2-Gate-Stand → Ticketende), dieselben 32 Pfade (Vorlage B; Befund = G2 §1 wörtlich; Falsifikationsziele 1, 2, 5) | **NEEDS_WORK** — Härtung a–d vorhanden; zwei Befunde P1/P2, beide vom Dirigenten an der Quelle bestätigt (unten); HEAD vor/nach Lauf identisch |

### Befunde der G2-Wiederprüfung Runde 1 — Einordnung an der Quelle

**P1 — DEFEKT gegen Matrixzeile A-06 (Nebenläufigkeit).** Prüfer wörtlich: „Verwende einen einzigen atomaren Workerzähler — `broker/src/transport/server_v3.rs:1494-1498`. Bei zwei gleichzeitig endenden Verbindungen können `zaehler` und `statistik.aktive_worker` dauerhaft auseinanderlaufen: A führt `fetch_sub` 2→1 aus, B 1→0 und speichert 0, danach speichert A den veralteten Wert 1. `broker_soll_idle_enden()` sieht dann trotz null Clients dauerhaft Aktivität; die umgekehrte Reserve/Drop-Reihenfolge kann 0 bei einem lebenden Worker hinterlassen und den Broker nach 60 s stoppen. Das bricht A-06 sowie die gekoppelte Lebenszyklusregel; der aktuelle Ein-Client-Idle-Test misst dieses Interleaving nicht."
An der Quelle bestätigt: `WorkerPlatz::drop` (`server_v3.rs:1492-1500`) schreibt `store(vorher−1)` aus dem Rückgabewert von `fetch_sub`; `worker_reservieren` (`:1502-1524`) schreibt `store(stand+1)` nach dem `compare_exchange`; `V3Griff::aktive_worker` (`:1187-1188`) liest ausschließlich `statistik.aktive_worker`, und genau diesen Wert fragt `broker_soll_idle_enden` (`lib.rs:1033`). Zwei Drops (A `vorher=2`, B `vorher=1`; B speichert 0, dann A speichert 1) lassen 1 stehen → kein Selbstende; Drop gegen Reserve lässt 0 bei lebendem Worker stehen → Selbstende nach 60 s. Beide Richtungen verletzen A-06 („keine aktiven v2-/v3-Clients" als Bedingung des Selbstendes; Wahl „Idle zählt bereits angenommene Worker").
Regel, die ihn schließt: genau **eine** atomare Zählerwahrheit. `V3Griff::aktive_worker` liest denselben `AtomicUsize`, der in `worker_reservieren` reserviert und in `WorkerPlatz::drop` freigegeben wird; eine getrennt gespeicherte Statistik-Kopie entfällt oder wird ausschließlich mit `fetch_add`/`fetch_sub` geführt, nie mit `store` aus einem gelesenen Vorwert. Messender Test: ein neuer `security_vectors`-Test verschränkt deterministisch (Barriere) zwei Drops sowie Drop gegen Reserve und verlangt danach `aktive_worker == 0` bzw. `== 1`; er ist ohne den Fix rot (E-5).

**P2 — DEFEKT: bestehender Ende-zu-Ende-Prüfweg durch den Fixdiff gebrochen.** Prüfer wörtlich: „Authentifiziere die E2E-Probe gegen den Probe-Broker — `eq-copilot/plugin/probe/PipeProbeMain.cpp:56-59`. Beim vorgesehenen Lauf `eqcop-broker-probe.exe 10 <probe>` plus `EqCopPipeProbe.exe <probe> 5` gehört der konkrete Handle dem separaten Probe-Binary. Diese Erwartung verweist jedoch auf das installierte `eqcop-broker.exe`; deshalb endet `namedPipeServerAuthentisieren` stets mit `dateiidentitaetFalsch`, bevor ein Hello gesendet wird, und die bestehende C++↔Rust-Probe scheitert in Stufe 1. Gegen die Produktions-Pipe darf der Test nicht ausweichen."
An der Quelle bestätigt: der Fixdiff (`e0a0dd6...05973e0`) hat `PipeProbeMain.cpp` um die `ServerErwartung { installbindung::brokerPfad, brokerSha256, authenticodeThumbprint }` erweitert (Zeilen 56-59). Der dokumentierte Ablauf (`docs/plugin-wissen.md` §4 und §5: `eqcop-broker-probe.exe [sekunden] [pipe-name]` gegen `EqCopPipeProbe [pipe] [s]`, immer über einen Probe-Namen) verbindet mit einem anderen Binary als dem manifestgebundenen Broker; die Dateiidentitätsprüfung (C-04) scheitert damit deterministisch vor dem ersten Byte. Das Werkzeug ist seit dem Fix funktionsunfähig; `EqCopPipeProbe` ist kein Kanonbein, aber ein im Repo dokumentierter Prüfweg (`CLAUDE.md`: „Broker-Ende-zu-Ende immer über den Probe-Pipenamen testen").
Regel, die ihn schließt: `EqCopPipeProbe` authentisiert den tatsächlich verbundenen Server gegen das Binary, das der Aufruf benennt (zusätzliches Argument mit Pfad des Server-Binaries; SHA-256 daraus zur Laufzeit gerechnet; ohne Argument bleibt der installierte Broker aus `BrokerInstallBinding.h` die Erwartung). Serverauth wird dabei nicht abgeschaltet und die Produktions-Pipe nicht benutzt. Beleg: der dokumentierte Ablauf gegen `eqcop-broker-probe.exe` läuft grün durch alle Stufen, ein Lauf gegen ein falsches Binary bleibt rot (`dateiidentitaetFalsch`); beide Ausgaben in der Rohdatei der Runde; `docs/plugin-wissen.md` §5 nennt das neue Argument.

Nacharbeitsrunde 1: frischer Opus-Worker (Effort max, Aufsicht ENG) mit genau diesen zwei Befunden, den Regeln oben und `tools/dirigent/pruefliste.md`; betroffene Beine `broker` (A4), `broker_idle`, `security_vectors` und der Probe-Ablauf; danach voller Kanon abgekoppelt; anschließend prüft ein frischer Codex-Thread nur den Fixdiff der Runde (Vorlage B).

---

## Kanon-Lauf - NAK-123

**Lauf:** 2026-09-02 07:48 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 39/39 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-123-d8676e0-dirty.md](roh/NAK-123-d8676e0-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-02 07:48:28 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | d8676e0 NAK-123 R1: PipeProbe authentisiert benanntes Server-Binary (P2) |
| Commit (voll) | d8676e01b24ed389a125c8e407230b95089ca533 |
| Arbeitsbaum | 1 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,11 s | [A1](roh/NAK-123-d8676e0-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,29 s | [A2](roh/NAK-123-d8676e0-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 6,21 s | [A3](roh/NAK-123-d8676e0-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 70,68 s | [A4](roh/NAK-123-d8676e0-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 6,88 s | [A4-SI](roh/NAK-123-d8676e0-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 1,09 s | [A4b](roh/NAK-123-d8676e0-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 2,90 s | [A5](roh/NAK-123-d8676e0-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-123-d8676e0-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-123-d8676e0-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,19 s | [A8](roh/NAK-123-d8676e0-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,42 s | [A9](roh/NAK-123-d8676e0-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen und band_stereo ID 10 samt saturated-/Grenzfaellen sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 1,86 s | [A10](roh/NAK-123-d8676e0-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,27 s | [A11](roh/NAK-123-d8676e0-dirty.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A12](roh/NAK-123-d8676e0-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,25 s | [A13](roh/NAK-123-d8676e0-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 4,63 s | [A14](roh/NAK-123-d8676e0-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,03 s | [A17](roh/NAK-123-d8676e0-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 31,66 s | [A18](roh/NAK-123-d8676e0-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,13 s | [A19](roh/NAK-123-d8676e0-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A20](roh/NAK-123-d8676e0-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,27 s | [A21](roh/NAK-123-d8676e0-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,02 s | [A22](roh/NAK-123-d8676e0-dirty.md#a22) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,71 s | [A23](roh/NAK-123-d8676e0-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,10 s | [A16](roh/NAK-123-d8676e0-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,12 s | [B1](roh/NAK-123-d8676e0-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,09 s | [B2](roh/NAK-123-d8676e0-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,07 s | [B3](roh/NAK-123-d8676e0-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-123-d8676e0-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,24 s | [B3c](roh/NAK-123-d8676e0-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten, und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,32 s | [B4](roh/NAK-123-d8676e0-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,13 s | [B9](roh/NAK-123-d8676e0-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle 61 Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen - waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, aber kein unbewiesenes Projektintervall oder FFT-Event erzeugen; Frame-Stempel umfassen den echten ueberlappenden FFT-Support und bleiben an int64/u32-Grenzen darstellbar; Drop zaehlt als Segment, alles andere als Epoche; alle sieben Erzeuger-Stempelregeln fallen einzeln und mit eigener Nummer; kein spektraler Fluss ueberbrueckt eine Grenze, der Ereignisring ist fest gedeckelt; LUFS-S trifft die analytisch gerechnete Erwartung unter 0,1 LU; die Nyquist-Kappe greift bei 22,05 kHz wirklich; verdrahtet: alle sieben Gueltigkeitsbits kommen ueber die Hostbruecke durch, der Playhead-Rueckfallweg nachweislich nur zwei. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 2,01 s | [B5](roh/NAK-123-d8676e0-dirty.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 59,76 s | [B10](roh/NAK-123-d8676e0-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 0,13 s | [B11](roh/NAK-123-d8676e0-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,55 s | [B12](roh/NAK-123-d8676e0-dirty.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,11 s | [B13](roh/NAK-123-d8676e0-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,27 s | [B14](roh/NAK-123-d8676e0-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,08 s | [B15](roh/NAK-123-d8676e0-dirty.md#b15) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,13 s | [B8](roh/NAK-123-d8676e0-dirty.md#b8) |

---

## Nacharbeitsrunde 1 2026-09-02

Auftrag dieser Runde: ausschließlich die zwei Befunde der G2-Wiederprüfung
Runde 1 schließen (Abschnitt „Prüfrunden (Dirigent, 2026-09-02)“ oben), jeder
nach der dort wörtlich festgehaltenen Regel. Kein weiterer Umbau, kein
Refactor, keine Änderung an der Verhaltensmatrix — sie steht unverändert.

| Feld | Wert |
|---|---|
| Basis-SHA | `b02ae62e30e16d246a73d47f046e59679923877c` |
| Commits | `0411073` (P1), `d8676e0` (P2), dieser Manifest-Commit |
| Rohausgabe der Runde | [roh/NAK-123-r1-d8676e0.md](roh/NAK-123-r1-d8676e0.md) |
| Fremde Datei im Baum | `.workflow/ultracode/20260831-084621-sonde011-phaseb-bau/` — nicht angefasst, gehört nicht zu diesem Änderungssatz |

### P1 — eine Zählerwahrheit für `aktive_worker` (Matrixzeile A-06)

**Fix** in `broker/src/transport/server_v3.rs` (Commit `0411073`): es gibt jetzt
genau einen Zähler. `V3Griff` hält den `Arc<AtomicUsize>`, den
`worker_reservieren` per `compare_exchange` besetzt und `WorkerPlatz::drop` per
`fetch_sub` freigibt; `V3Griff::aktive_worker` liest genau diesen. Das Feld
`V3Statistik::aktive_worker` entfällt samt beider `store`-Schreibungen — der
nachlaufende `store(vorher−1)` in `Drop` und der `store(stand+1)` nach dem
geglückten Tausch. Alle bisherigen Leser (die internen Tests in `server_v3.rs`
und `security_vectors`) gehen über denselben Produktlesepfad, den auch
`broker_soll_idle_enden` (`broker/src/lib.rs:1033`) benutzt. `broker/src/lib.rs`
brauchte dafür keine Änderung.

**Messender Test** (`broker/tests/security_vectors.rs`, deterministisch über
eine Condvar-Barriere, kein Sleep): ein Freigabethread wird unmittelbar nach der
Zählerfreigabe angehalten, während der zweite Vorgang vollständig durchläuft.

| Test | Verschränkung | Erwartung |
|---|---|---|
| `zwei_verschraenkte_freigaben_lassen_aktive_worker_auf_null` | Drop A hält nach `fetch_sub` 2→1, Drop B läuft 1→0 komplett durch, dann läuft A zu Ende | `aktive_worker() == 0` |
| `freigabe_gegen_reserve_laesst_den_lebenden_worker_stehen` | Drop A hält nach `fetch_sub` 1→0, eine Reserve belegt 0→1, dann läuft A zu Ende | `aktive_worker() == 1` |

**E-5 Rotlauf gegen den alten Code:** in einem eigenen lokalen Clone (`C:\n123r1`,
Stand `d8676e0`) wurde ausschließlich der Zählmechanismus des Basisstands
zurückgedreht, Testcode und Testnaht blieben gleich. Beide Tests sind dort ROT,
und zwar in genau den zwei Richtungen des Befunds: `left: 1 / right: 0`
(Reststand trotz null Worker) und `left: 0 / right: 1` (Null bei lebendem
Worker). Nach `git checkout` derselben Datei im selben Clone laufen dieselben
Tests grün — Rot und Grün trennt allein der Fix. Beide Ausgaben wörtlich in
[roh/NAK-123-r1-d8676e0.md §1a/§1b](roh/NAK-123-r1-d8676e0.md).

**Grünlauf im sichtbaren Checkout** (Ausgaben in §1c/§1d der Rohdatei):

| Lauf | Ergebnis |
|---|---|
| `cargo test --manifest-path broker/Cargo.toml --test security_vectors` | ok, 20 passed, 0 failed, 5,52 s |
| `cargo test --manifest-path broker/Cargo.toml --test broker_idle` | ok, 4 passed, 0 failed, 60,40 s |
| `cargo test --manifest-path broker/Cargo.toml` (gesamt) | ok, alle Ziele, 0 failed, Exit 0; 0 Zeilen beginnen mit `warning` |

### P2 — `EqCopPipeProbe` authentisiert das benannte Server-Binary

**Fix** in `eq-copilot/plugin/probe/PipeProbeMain.cpp` (Commit `d8676e0`): ein
drittes Argument `[server-binary]` nennt das Binary, dem der Server auf dieser
Pipe gehört. Der SHA-256 kommt zur Laufzeit aus dem vorhandenen Produkthelfer
`nakama::ipc::serverErwartungFuerTestdatei` (`BrokerLifecycle.cpp`) — keine
zweite Hash-Implementierung im Probecode; `erwarteterPid` bleibt 0 und der
Thumbprint leer. Ohne das Argument bleibt der installierte Broker aus
`BrokerInstallBinding.h` die Erwartung. Die Serverauthentisierung wird nicht
abgeschaltet und es gibt keinen Pfad ohne Prüfung: eine leere Erwartung (Datei
fehlt oder nicht lesbar) bricht mit Exit 2 ab, statt permissiv zu werden.
`eq-copilot/plugin/CMakeLists.txt` brauchte keine Änderung — der Helfer liegt
in `NakamaKern`, an dem `EqCopPipeProbe` bereits hängt.
`docs/plugin-wissen.md` §5 nennt das neue Argument.

**Gebaut:** `cmake --build eq-copilot/build --config Release --target
EqCopPipeProbe` (Artefakt
`eq-copilot/build/plugin/EqCopPipeProbe_artefacts/Release/EqCopPipeProbe.exe`)
und `cargo build --manifest-path broker/Cargo.toml --bin eqcop-broker-probe`.

**Läufe**, alle auf dem Probe-Pipenamen
`\\.\pipe\evenacadia.eq-copilot.m2probe.nak123r1`; die Produktions-Pipe wurde
in keinem Lauf geöffnet (Ausgaben wörtlich in
[roh/NAK-123-r1-d8676e0.md §2](roh/NAK-123-r1-d8676e0.md)):

| Lauf | Aufruf | Ergebnis |
|---|---|---|
| 1 — dokumentierter Ablauf | Broker `eqcop-broker-probe.exe 20 <probe>`, dann `EqCopPipeProbe.exe <probe> 5 broker\target\debug\eqcop-broker-probe.exe` | **GRÜN** durch alle drei Stufen: `PROBE OK v2 … heartbeats 2/2 bestaetigt`, `KONFLIKT OK`, `KONFLIKT-ENDE OK`, Exit 0 |
| 2 — Gegenprobe falsches Binary | dritter Parameter `…\eqcop-broker-v3probe.exe` | **ROT**: `Server nicht verifiziert: dateiidentitaetFalsch`, Exit 1 |
| 3 — Gegenprobe ohne Argument | kein dritter Parameter (Default = installierter Broker) | **ROT**: `dateiidentitaetFalsch`, Exit 1 — der Default-Pfad bleibt unverändert fail-closed |

Nach allen Läufen lebt kein Brokerprozess mehr (`RESTPROZESSE: keine`) und der
Probe-Pipename ist wieder frei. Das Sekundenargument beendet den Probe-Broker
selbst; kein Lauf hat einen fremden Prozess beendet.

### Kanon

`pwsh -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-123.md -Anhaengen -Titel NAK-123`,
abgekoppelt gestartet, während des Laufs keine Quelldatei geändert.

**GRÜN — 39/39 Kanon-Läufe bestanden, Exitcode 0**, Lauf 2026-09-02 07:48 auf
Commit `d8676e0`. Kopf und Übersicht stehen im Abschnitt „Kanon-Lauf - NAK-123“
direkt oberhalb, die Rohausgabe in
[roh/NAK-123-d8676e0-dirty.md](roh/NAK-123-d8676e0-dirty.md). Die Marke
`-dirty` trägt genau die eine fremde unbestätigte Datei aus der Kopftabelle
oben; sie wurde nicht angefasst. Die Runnerzeile „2 geplante Prüfung(en) noch
nicht gebaut | 1 stillgelegte(s) Bein(e)“ betrifft unverändert B6/B7 (ab P6)
und A15 (Suna-Stilllegung 28.08.).

### Abgehakte Prüflistenzeilen mit Messort

Bewertet für **diese Runde** (zwei Defekte). `N/A` heißt: von diesem
Änderungssatz nicht berührt; der Messort nennt trotzdem den Lauf, der die Klasse
in diesem Kanon abdeckt.

| Prüflistenzeile | Ergebnis und Messort |
|---|---|
| [x] A-1 Politik bei voller P0/P1/P2-Klasse ausdrücklich | `N/A`: keine Queue-Politik berührt. Im selben Kanonlauf gemessen durch A22 (`pruefe_ipc_last.py`, Exit 0, 10,02 s). |
| [x] A-2 Wiederholpuffer fließt ohne Reconnect | `N/A`: kein Puffer geändert. Messort im selben Lauf: A22. |
| [x] A-3 Schlüssel überlebt Zwischenpuffer und Koaleszierung | `N/A`: kein Zwischenpuffer geändert. Messort im selben Lauf: A22. |
| [x] A-4 höhere Priorität wird trotz niedrigerer Blockade gelesen | `N/A`: kein Lesepfad geändert. Messort im selben Lauf: A22. |
| [x] A-5 Rückgabewerte/Zähler werden im Lastbein ausgewertet | Erfüllt: der Workerzähler ist jetzt genau eine Zahl und wird von `security_vectors` (20 Tests) sowie von `broker_idle` über den Produktlesepfad `V3Griff::aktive_worker` ausgewertet; er wurde in dieser Runde einmal rot gesehen (§1a der Rohdatei). |
| [x] A-6 Regel in C++ und Rust beidseitig | Erfüllt: P1 ist reine Rust-Seite (`security_vectors`, `broker_idle`), P2 die C++-Seite desselben Ende-zu-Ende-Wegs (`EqCopPipeProbe` gegen den Rust-Probe-Broker); beide Seiten wurden in dieser Runde einzeln rot gesehen. |
| [x] B-1 Verbindungsreihenfolge festgelegt und gemessen | Erfüllt für P2: `CreateFileW` → Serverauth → erst dann Hello. Lauf 2 und 3 belegen, dass ohne verifizierten Server kein Byte fließt (`Protokoll 0`, `Versuche 1`, Fehler vor dem Hello). |
| [x] B-2 beim Trennen zuerst entkoppeln, dann warten/joinen | `N/A`: Trennreihenfolge unverändert. Messort im selben Kanonlauf: A4 (`cargo test`, Exit 0, 70,68 s) mit den unveränderten A-09/B-06-Tests. |
| [x] B-3 jeder Join hat Frist oder getesteten Self-Join-Gegenpfad | Erfüllt: der neue Testhaltepunkt läuft nur in `security_vectors` und hat beidseitig eine 10-s-Frist (`wait_timeout`, `assert!(!ablauf.timed_out())`); der Produktpfad trägt ihn als `None` und joint nichts Neues. |
| [x] B-4 geschlossene Queue liefert nichts mehr | `N/A`: keine Queue berührt. Messort im selben Lauf: A4. |
| [x] B-5 Registrierung im Stoppfenster ist deterministisch | Erfüllt: Reservieren und Freigeben laufen unverändert über `worker_reservieren`/`WorkerPlatz::drop`; der neue Test misst genau das Fenster zwischen beiden. `security_vectors` 20/20 grün, darunter A-05 und A-09. |
| [x] C-1 Textlängen exakt geprüft | `N/A`: kein Schema und kein Textfeld geändert. |
| [x] C-2 exakte Feldmenge in C++ und Rust | `N/A`: kein Drahtvertrag geändert. |
| [x] C-3 Discriminator/Familie/Typ vor Inhalt | `N/A`: Bootstrap-Prüfreihenfolge unverändert. Messort im selben Lauf: A4 und A21 (`transport_fuzz`, Exit 0). |
| [x] C-4 NaN/Inf/Grenzwerte für Drahtzahlen | `N/A`: keine Drahtzahl geändert. |
| [x] D-1 Riegel fail-closed ohne Rohtextheuristik | Erfüllt: `EqCopPipeProbe` entscheidet über den Bericht von `namedPipeServerAuthentisieren`, nicht über Text; eine leere Erwartung endet mit Exit 2, ein falsches Binary mit `dateiidentitaetFalsch`. Läufe 2 und 3. |
| [x] D-2 Frische der Eingaben statt veraltetes Artefakt | Erfüllt: der Kanon lief mit `-Bauen`; die Tabelle „Baustand der Prüfbinaries“ der Kanonrohausgabe weist die Binaries als frisch aus. Die P2-Läufe fuhren gegen die unmittelbar davor gebauten Artefakte. |
| [x] D-3 Relink ändert Bundlebytes; Hash ist Auslieferungswert | **Offen, unverändert außerhalb.** Der Brokercode wurde erneut geändert, also trägt `eqcop-broker.exe` neue Bytes; der SHA-256 in `eq-copilot/install/nakama-installer-v1.json` ist dafür kein Nachweis. `eq-copilot/install/**` liegt außerhalb der Ticketgrenze. Hier wird kein alter Hash als passend behauptet — der Punkt bleibt in „Offene Punkte“ stehen. |
| [x] D-4 nur vom Kanon Gebautes darf frisch heißen | Erfüllt: die Frischebehauptung stützt sich auf den Kanonlauf, nicht auf die Einzelläufe. |
| [x] E-1 Behauptung höchstens so stark wie Messung/Gate | Erfüllt: Rotlauf, A/B-Gegenprobe, Einzelbeine, Probe-Läufe und Kanon sind getrennt ausgewiesen; für D-3 wird nichts behauptet. |
| [x] E-2 Zahlen im Manifest gemessen | Erfüllt: 20/20, 4/4, 39/39, die Sekundenwerte und die Exitcodes stammen aus den Läufen dieser Runde (Rohdatei und Kanonrohausgabe). |
| [x] E-3 lebender Kopf mit Symbol/Anker, Verlauf mit Standzeilen | Erfüllt: dieser Abschnitt nennt Testsymbole (`zwei_verschraenkte_freigaben_lassen_aktive_worker_auf_null`, `freigabe_gegen_reserve_laesst_den_lebenden_worker_stehen`) und Dateinamen statt Zeilennummern fremder Stände. |
| [x] E-4 lebender Kopf nachziehen, darunter append-only | Erfüllt: nachgezogen wurde ausschließlich der Kanonsatz in der Kopfzeile `Beweisstatus`; alles darunter blieb unverändert, dieser Abschnitt ist angehängt. Die Verhaltensmatrix A/B/C ist Wort für Wort unberührt. |
| [x] E-5 jede neue Prüfung einmal absichtlich gebrochen | Erfüllt: beide neuen Tests waren gegen den alten Zählmechanismus rot (§1a) und sind mit dem Fix grün (§1b) — im selben Baum, allein durch den Fix getrennt. Für P2 sind die Gegenproben 2 und 3 der Rotbeleg des neuen Prüfwegs. |
| [x] E-6 geänderte Zusage an drei Stellen nachziehen | Erfüllt und gemessen: `git grep -n "EqCopPipeProbe \[pipe\]" -- docs/ tools/` findet die Aufrufzusage an genau zwei Stellen — `docs/plugin-wissen.md:821` (nachgezogen) und das wörtliche Prüferzitat im Verlauf dieses Manifests, das append-only nicht umgeschrieben wird. `tools/beweise.ps1` nennt das Werkzeug nicht (kein Kanonbein); `docs/beweise/SONDE-008.md` und `docs/archiv/**` nennen nur den Zielnamen ohne Aufrufform. Der Skriptkopf von `PipeProbeMain.cpp` ist im selben Commit mitgeändert. |
| [x] E-7 Writer-Fixtures statt handgeschriebener Writer-Form | `N/A`: kein Fixture erzeugt. Die P2-Erwartung wird zur Laufzeit vom Produkthelfer aus der echten Datei gerechnet, nicht von Hand geschrieben. |
| [x] F-1 gekoppelte Lebenszyklen im selben Änderungssatz | Erfüllt: Reservieren↔Freigeben liegen in einem Commit (`worker_reservieren`, `WorkerPlatz::drop`, `V3Griff::aktive_worker`, beide Testrichtungen); Verbinden↔Trennen des Probewegs liegen in `PipeProbeMain.cpp` gemeinsam samt Doku. |
| [x] F-2 Writer, Reader, Migration, Fixtures, Cross-Language-Verbraucher gemeinsam | `N/A`: kein Vertrag geändert. Der einzige sprachübergreifende Verbraucher dieser Runde ist der Probe-Weg C++↔Rust, und beide Hälften wurden zusammen gemessen (Läufe 1–3). |

### Offene Punkte nach dieser Runde

- **D-3 Installer-Hash — weiterhin offen** und ausdrücklich außerhalb der
  Ticketgrenze (`eq-copilot/install/**`).
- Sonst nichts: beide Befunde der G2-Wiederprüfung Runde 1 sind nach ihrer
  Regel geschlossen und belegt.
