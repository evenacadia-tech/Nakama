# G5 — Phasengate P5 · Intent, Ursachen, Proposal und passiver Assistent (T3)
<!-- NAKAMA-URTEIL: T3 PASS 2026-09-09 -->

**Abgenommen: T3 PASS 2026-09-09 (§14; Beweise vom 08.09.2026).** Alle drei Nacharbeitstickets aus §11 sind abgenommen (NAK-212 §12, NAK-213 §13, NAK-214 §14); das Urteil aus §1 (NEEDS_WORK vom 07.09.2026) bleibt als Verlauf stehen.

**Datum:** 07.09.2026 · **Prüfstufe:** T3 · **Gate-Text (wörtlich, `docs/bauaufteilung-sonden.md:395`):** „**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — Erläuterung des Plans (`:397-400`): „G5 ist das ungewöhnlichste Gate: Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer." Gate 8 ist gegenstandslos (`:500-505`). · **Session:** eigene (nakama-dirigent `112f27af`), hat P5 nicht gebaut und keine Zeile Produktcode, Schema, Test, Fixture oder Runner angefasst. **Vorbild:** `docs/beweise/G4.md`.
**Gate-Stand:** `bf92891b17b0b6f216dcf1b10a50ac2d9b756dc4` (= `origin/master`, Worktree sauber). Der Codestand ist identisch mit dem Kanonstand `4df8855` (GRÜN 60/60, `docs/beweise/roh/SONDE-014-4df8855.md`): `git diff --stat 4df8855..bf92891 -- broker eq-copilot tools` ist leer. Phasenänderungssatz: `5e99a13...4df8855` (SONDE-014, S23–25, T2 PASS 2026-09-07, 145 Dateien unter `broker/`, `eq-copilot/`, `tools/`).

---

## 1. Urteil

**NEEDS_WORK.**

Gate 7 **hält** (eigener Lauf B2 „kein Pfad gefunden", 7 Wege; kein Prüfer fand einen Text oder ein Feld, das eine Insertprobe als exakten Summenbeitrag bezeichnet — §5). Gate 6 **hält auf der Experimentseite** (eigener Lauf B1, 9 Wege; Gen kann kein Urteil setzen), **fällt aber an der Naht zur P5-Kette**: ein PRE/POST-Paar unterhalb `AudioAligned` wird zur Aussageklasse `wirkungsbeleg` (D6 des eigenen Laufs, Quellencheck bestätigt, §4). Das P5-Exit-Gate **fällt in Satz 2 und Satz 3**, Satz 1 hält im Broker:

- **Satz 3 („Gegenbeispiele erzwingen nachweislich Enthaltung") — fällt breit, von drei unabhängigen Läufen.** Der Gegenbeispiel-Lauf hat 27 adversariale Eingaben durch den Produktpfad gefahren; **11 erzeugen eine falsche starke Behauptung** (`hoch` / `READY TO SEND`), darunter eine Quelle ohne jede Energie im Befundband, eine gegenläufig schwingende Quelle, eine Quelle im falschen Band, der Distraktor allein im Rennen und der Kandidatendeckel, der die wahre Ursache spurlos abschneidet (§7). Der eigene Lauf fand unabhängig vier Mechanismen (Distraktor mit mehr Material, Alignment-Gate über Spannen statt Überlappung, berührende Fenster als Passagenmaterial, Main als unerkannter Parent); Codex Astra fand unabhängig die Intent-Priorität als falsche Trennung, den Main als Parent, die Anomalie aus Fenstern außerhalb der Passage und den Deckel nach Stufe B. Die Ursache ist gemeinsam: **die Sicherheitsklasse `hoch` entsteht aus drei strukturellen Größen (Fensterzahl, Routing bekannt, kein Parent-Duplikat) und aus keinem Beleg für einen Zusammenhang** (`broker/src/coordinator/hypothese.rs:1387-1396`); ein Einzelüberlebender gilt trivial als „getrennt" (`:1216-1223`), und die Gates davor messen die falsche Größe (`:823-829`, `:1612-1623`).
- **Satz 2 („jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg") — fällt.** Tritt ein zweites Main derselben Sitzung bei, setzt `mitgliedschaft.rs:87-92` `fuehrendes_main = None`; `proposal_verdrahtung.rs:113-119` nimmt `unwrap_or_default()`, das Proposal trägt `target: ""` und wird persistiert. `gate_felder_vollstaendig` (`proposal.rs:360-380`) hat keinen Produktivaufrufer (eigener Lauf D5 gemessen, Astra A6 unabhängig).
- **Satz 1 („Jede sichtbare Behauptung referenziert existente Evidenz-IDs") — hält im Broker** (eigener Lauf B6 „kein Pfad", 10 Wege; Härtung gegen den Store `hypothese_verdrahtung.rs:635`, `:691`). Gen behält nach einer Rücknahme ohne Folge-Snapshot (Pipe-Abbruch) einen READY-Befund (Astra A5) — Lücke, Regel in NAK-214.

Die Befunde sind nach Ursache in **drei Nacharbeitstickets** gebündelt (§11, Register NAK-212 bis NAK-214); Lücken und Härtungen stehen datiert im Register (§8).

---

## 2. Bruchaufträge — wörtlich

Alle Prüfer bekamen denselben Auftrag (`docs/beweise/roh/G5-codex-falsifikation-auftrag.txt`) mit erzwungener Antwortform (konkreter Pfad **oder** wörtlich „kein Pfad gefunden", je Auftrag die Zahl der Wege und die messenden Tests). Die harten Gates wörtlich (Entwurf §49.2, `docs/FL-Nakama-Sonden-Design-Entwurf.md:3571-3572`): 6. „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." 7. „Eine Standard-Insertprobe wird als exakter Mastersummenbeitrag bezeichnet." P5-Exit-Gate wörtlich (§59, `:4322-4324`): „**Exit-Gate:** Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung. `R2 · Passive Beta` ist erreicht."

> **B1 — Gate 6 im P5-Kontext:** nicht vergleichbares Experiment → starkes Siegerurteil oder Speisung einer `hoch`-Hypothese / eines handelbaren Proposals. **B2 — Gate 7:** Insertprobe als exakter Mastersummenbeitrag in Befund, Anzeigezeile, `expected_effect` oder `target`. **B3 — korrelierter Distraktor:** Eingabefolge, bei der ein korrelierter, nicht kausaler Kandidat `hoch`/READY TO SEND erhält oder zwei starke Befunde entstehen. **B4 — Parent-Duplikat:** Parent und Child beide stark oder das Duplikat allein stark. **B5 — verschobene Passage:** verschobene, andere oder zu kurze Passage ergibt `hoch`. **B6 — Exit-Gate Satz 1**, **B7 — Satz 2** (auch `no_change`/`more_data`), **B8 — Satz 3:** je Zusage M-21, M-22, M-23, M-27, M-31, M-64 bis M-69 der messende Test oder „ungemessen"; stehen die drei Korpus-Gegenbeispiele wirklich unter Last; welche Rust-Tests messen die Zeilen, deren Testspalte nicht existierende C++-Ziele nennt.

Ausschlüsse: `docs/**` keine Befundfläche; Register NAK-135 bis NAK-211; same-user-Szenarien (Entwurf §48.4); FL-Termine; P6-Lieferumfang; UI-Gestaltung; `tools/dirigent/pruefliste.md` keine Anforderungsquelle; Sabotage gegen Prüfwerkzeuge = Härtung. „Starke Ursachenbehauptung" = Befund `hoch`/READY TO SEND oder Proposal mit Aktion außer `no_change`/`more_data`; „Enthaltung" = Ursachenklasse `daten_reichen_nicht` (M-27) oder Zustand MORE DATA.

---

## 3. Wer geprüft hat

| # | Prüfer | Modell / Effort | Fläche | Ergebnis |
|---|---|---|---|---|
| 1 | Codex, Falsifikation | `gpt-6-astra`, max, read-only, elevated Sandbox, Thread `01a07c39-79a6-7050-9503-2529792b65bf`, 16:15–17:08, abgekoppelt über `tools/dirigent/codex-lauf.ps1`, HEAD vor und nach dem Lauf `bf92891` | Prüfbereich aus dem Auftrag (Coordinator, Store, Vertrag, Gen-Empfänger, Korpus) | **7 Befunde** (4 P1, 3 P2), alle als DEFEKT erhoben; an der Quelle: 4 Defekte, 2 Lücken, 1 Härtung, davon A2 als eigener Mechanismus widerlegt (§8); Urteil `docs/beweise/roh/G5-codex-astra-falsifikation-bf92891.txt` |
| 2 | Codex, Gegenprüfung | `gpt-5.6-sol`, max, read-only, elevated Sandbox, Thread `01a07c39-858f-7433-b797-1fc8d380e834`, 16:15–18:37, abgekoppelt | dieselbe Fläche, derselbe Auftrag, unabhängig | **abgebrochen ohne Urteil** durch den Dirigenten nach 2 h 22 min: 646 Lesekommandos (620 eindeutig, keine Schleife), keine Urteilsphase erreicht, während Astra in 53 min fertig war und drei Läufe bereits übereinstimmend NEEDS_WORK ergaben; die Gegenprüfung wird bei der Abschlussprüfung des Gates mit engerem Prüfbereich nachgeholt (`docs/beweise/roh/G5-codex-sol-abbruch-bf92891.txt`) |
| 3 | eigener Falsifikationslauf | Opus, 1 Agent, lesend, 91 Werkzeugaufrufe, 2 042 s; 14 Beine vom Kanonstand gefahren (alle grün, §9), dazu eine eigene Messbühne außerhalb des Repos (14 Eingaben durch `Coordinator::mit_uhr` → `c.p1`) | dieselbe Fläche | B1 **kein Pfad** (9 Wege) · B2 **kein Pfad** (7) · B3 **2 Pfade** (12) · B4 **1 Pfad** (8) · B5 **1 Pfad** (9) · B6 **kein Pfad** (10) · B7 **1 Pfad** (8) · B8: keine Zeile ganz ungemessen, vier halb (M-33, M-44, M-69, M-87); 6 Defekte, 7 Lücken, 4 Härtungen (`docs/beweise/roh/G5-eigener-lauf-bf92891.md`) |
| 4 | adversariale Gegenbeispiele, gefahren | Opus, 1 Agent, in eigener lokaler Arbeitskopie (`git clone --local`, kein Produktcode geändert), 65 Werkzeugaufrufe, 1 606 s | 12 neue Korpussitzungen + 15 Eingaben in einem Kettenbein, alle durch den Produktpfad; `pruefe_p5_korpus.py` auf dem erweiterten Korpus | **27 Fälle: 10 × Enthaltung/`mittel` erzwungen, 6 × zulässig stark, 11 × falsche starke Behauptung**; Prüfer-Riegel `falsche_starke` schlägt mit 6 Sitzungen an, Exit 2; 5 Defekte, 4 Lücken, 3 Härtungen (`docs/beweise/roh/G5-gegenbeispiele-bf92891.md`, Patch `docs/beweise/roh/G5-gegenbeispiele-bf92891.patch`, Rohausgaben `docs/beweise/roh/G5-gegenbeispiele-bf92891/`) |
| 5 | Quellencheck der Befunde | Dirigent selbst (G-D1 bis G-D4, E-D1, E-D3, E-D4, E-D5 an der Quelle) + ein lesender Opus-Prüfagent (A1 bis A7, E-D6, E-L2, G-D5, G-L2; Ergebnis im Scratchpad, Einordnung in §8) | jeder Defekt gegen Quelle, Matrix, Gate-Text, Entwurf §36/§59, Invarianten, Register | Einordnung in §8; zwei Abweichungen des Dirigenten von der Agent-Einordnung sind dort begründet |

**Werkzeuglage, protokolliert (kein Codebase-Befund):** Die Matrix-Testspalte nennt `EqCopSonde014HypotheseTest`, `…ProposalTest`, `…MaskierungTest`; diese existieren weder als Quelle noch als CMake-Ziel — die Zusagen sind in `broker/tests/sonde014_hypothese.rs`, `sonde014_proposal.rs`, `sonde014_maskierung.rs` gemessen (eigener Lauf B8 (c), Zeile für Zeile). Das ist Manifestprosa, kein Befund; die Testspalte zieht der Abschluss-Worker nach. Der Kettenbein-Lauf des Korpus schreibt ein Bauartefakt unter `eq-copilot/build/`; die Gegenbeispiele liefen deshalb in einer eigenen Arbeitskopie, der sichtbare Checkout blieb unverändert (`git status --short` je Agent belegt).

---

## 4. Falsifikation Gate 6 — Experimentseite hält, Naht zur Kette fällt

Experimentseite (eigener Lauf B1, 9 Wege): `experiment_manual_result` kennt kein Urteils- oder Vergleichbarkeitsfeld, das Urteil entsteht nur in `sicht.rs` aus brokereigener Rechnung (unverändert seit G4 §5). **Aber:** `hypothese_verdrahtung.rs:193-203` setzt `prepost_paar` allein aus `ergebnis.is_some()`, und `prepost.rs:1110-1130` liefert `Some(…)` auch für `Alignmentklasse::Probable`, bei der `ausgerichtet_db`, `match_gain_db` und `wirkung` `None` sind (`:811-852`). Der Befund trägt dann `claim_class = "wirkungsbeleg"` und `ursachenklasse = "effektkette_pre_post"` — M-17 wörtlich („Wirkungsbeleg (PRE/POST-Paar **mit reproduzierbarer Veränderung**)") bricht, und die SONDE-013-Sperre („Kein unbekannter Zeitpfad erzeugt eine starke … PRE/POST-Aussage", `prepost.rs:896-898`) wird von der P5-Kette aufgehoben. Am Quelltext hergeleitet (eigener Lauf D6), Quellencheck bestätigt; `sonde014_hypothese.rs::parallele_telemetrie_bleibt_klasse_eins` misst nur den Fall ohne Paar. **Defekt → NAK-214.**

## 5. Falsifikation Gate 7 — gehalten

Kein Prüfer fand ein Feld oder einen Text, der eine Insertprobe als exakten Summenbeitrag bezeichnet (eigener Lauf B2, 7 Wege; Astra ohne Eintrag). `beobachtung` eines `session_finding` ist die Anomalie des **Masters** (`hypothese.rs:1284-1288`), `expected_effect` entsteht aus dem Befundband, nicht aus einem Summenbeitrag. **Lücke:** die Kette trägt keinen Feldnamen, der „gemessen an der Sonde" von „Beitrag zur Summe" unterscheidet; heute folgenlos, weil Gen das Feld nicht zeichnet → NAK-215.

## 6. P5-Exit-Gate je Satz — an der Quelle

| Satz | Stand | Befunde |
|---|---|---|
| 1 Jede sichtbare Behauptung referenziert existente Evidenz-IDs | **hält im Broker** | eigener Lauf B6 kein Pfad (10 Wege); Astra A5 (Gen nach Rücknahme ohne Folge-Snapshot) = Lücke mit Regel → NAK-214 R3 |
| 2 Jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg | **fällt** | E-D5 = A6 (leeres Ziel bei zwei Mains, `gate_felder_vollstaendig` ohne Produktivaufrufer) → NAK-214 R1; L6 `allowed_bounds {}` als „genannt" (Lesart M-46) → NAK-217 |
| 3 Gegenbeispiele erzwingen nachweislich Enthaltung | **fällt** | §7: G-D1 bis G-D5, E-D1 bis E-D4, A1, A3, A4, A7; dazu G-L1 (Passagenriegel im committeten Korpus nie unter Last: `sitzung_fahren` legt keine Passage an, `verschobene_passage` fällt am Master-Alignment) → NAK-212 R6 |

## 7. Adversariale Gegenbeispiele — was das Produkt tatsächlich sagt

Aus dem Gegenbeispiel-Lauf (Tabelle in `docs/beweise/roh/G5-gegenbeispiele-bf92891.md`, Abschnitt „Tabelle"); „entscheidender Riegel" ist die Stelle, die den Fall entschieden hat, „keiner" heißt: der Fall lief bis zur Klassenwahl durch.

| Fall | Entscheidender Riegel | Ausgabe | Urteil |
|---|---|---|---|
| Distraktor mit mehr Fenstern, gleicher Verlauf | `hypothese.rs:1210-1228` getrennt | 2 × `mittel` | mittel erzwungen |
| Distraktor lauter (+12 dB, 24 Fenster) | keiner (321 Quanten trennen) | `hoch` auf Distraktor | **falsche starke** |
| Distraktor allein im Rennen (Verursacher fällt am Alignment) | keiner (`:1219` `_ => true`) | `hoch`, Ausschluss im selben Befund | **falsche starke** |
| Quelle ohne jede Energie im Befundband | keiner (`:1387-1396` liest nur 3 Größen) | `hoch` | **falsche starke** |
| Sonde auf dem Masterkanal | keiner (Main nicht in `ids`) | `hoch` | **falsche starke** |
| Parent nicht im Rennen (Alignment / zu wenig Fenster) | `:1113-1121`, `:1136-1141` | `mittel` | mittel erzwungen |
| Parent angemeldet, ohne Evidenz | keiner (`je_kanal` aus Evidenzschlüsseln) | `hoch` | **falsche starke** |
| Fenster genau 8 / 7 | `:1388` | `hoch` / `mittel` | zulässig / mittel erzwungen |
| Kandidatendeckel (6 Kandidaten, Verursacher rangniedrigst) | `:1154` `truncate` ohne Grund | `hoch` auf lautesten Distraktor, Verursacher spurlos | **falsche starke** + M-87 |
| Gleichstand, Verursacher führt | `:1188-1194` | `hoch` + `mittel` | zulässig |
| Passage `[0,12)`, `[0,11)`, `[0,8)` / `[0,7)`, `[12,24)`, gleiche ID verschoben | `:838-854` | `hoch` / `daten_reichen_nicht` | zulässig / **Enthaltung erzwungen** (3 ×) |
| Zwei Master | keiner (`hypothese_verdrahtung.rs:216` letzter gewinnt) | `hoch` im fremden Band | **falsche starke** |
| Master ohne Fenster | `:221` `continue` | nichts | keine Aussage (Lücke) |
| Kanalwechsel vor / nach | `:1136-1141` / keiner | 2 × `mittel` / `hoch` | mittel erzwungen / **falsche starke** (Lücke) |
| Drei Sonden auf einem Kanal | `:1113-1141` | 3 × `mittel` | mittel erzwungen |
| Antikorrelierte Quelle | keiner (`:962-967`, Bootstrap zweiseitig) | `hoch`, Rang 0,4358 | **falsche starke** |
| Quelle im falschen Band | keiner (`gate()` ohne Bandrelevanz) | `hoch`, Bandpassung 0,016 | **falsche starke** |
| Antikorreliert neben korreliert | `:1188-1194` | `hoch` + `mittel` | zulässig |
| Rangdifferenz 2 Quanten | keiner (`:1216-1222` `a > b`) | `hoch` | **falsche starke** |

`pruefe_p5_korpus.py` auf dem erweiterten Korpus: Exit 2, `gesamt: 6 falsche starke Behauptung(en)`, niedrigste haltende Stufe `None`, Zuverlässigkeit `hoch` 0,333 (`docs/beweise/roh/G5-gegenbeispiele-bf92891/pruefe-p5-korpus.txt`). Der committete Korpus (sechs Sitzungen) bleibt grün — er enthält keinen dieser Fälle.

## 8. Befundklassen — an der Quelle eingeordnet

Kennungen: **G-** Gegenbeispiel-Lauf, **E-** eigener Lauf, **A** Codex Astra. Jede Zeile hat der Dirigent an der Quelle gelesen oder vom Quellencheck-Agent belegt bekommen (§3 Nr. 5).

**Defekt** (Satz bricht; Nacharbeit):
- **Ursache A — `hoch` ohne Zusammenhang; Gates messen die falsche Größe → NAK-212:** G-D1 (Quelle ohne Energie im Band, im falschen Band: `hypothese.rs:1387-1396`; Entwurf §36.1 Klasse 1 „Quelle und Masterproblem treten im selben Bereich und Zeitfenster auf" nicht erfüllt, M-69, Gate-Text), E-D2 = G „Distraktor lauter" (M-21 wörtlich), A1 (Intent-Relevanz trennt identische Kandidaten, `:1212-1216` gegen `:535-540`, `:1016-1029`; M-21), E-D4 (Alignment über Spannen: `spanne` min/max `:1563-1574`, `zeitueberdeckung` `:1581-1592`; M-20 „harte Gate-Regel" misst nicht die Überlappung), E-D3 (`fenster_in_passage :1612-1623` zählt Berührung ≥ 1 Sample; M-23 „innerhalb der Passage"), G „Rangdifferenz 2 Quanten" (M-26 „praktisch gleich" mit 1e-6 wirkungslos).
- **Ursache B — unvollständige Konkurrenz und Master-Wahl → NAK-213:** G-D3 = A7 (Deckel `:1154` nach Stufe B und ohne Grund; M-18 Rotbeweis „ein sechster Kandidat erreicht Stufe B", M-87 „**Jeder** Kandidat, der ausscheidet, trägt einen Grund"), G-D2 (Einzelüberlebender neben Messausschluss `:1216-1223`; M-21, M-69), E-D1 = A3 = G „Sonde auf Masterkanal" (Main in `je_kanal`, nicht in `ids` `:1113-1121`; M-69, Gate-Text „Parent-Duplikat … Enthaltung"; **Abweichung vom Quellencheck**, der M-22 „nicht zwei" wörtlich hält: eine Quelle, die das Mastersignal misst, ist nie dessen Ursache — das ist die falsche starke Behauptung, die M-69 auf jedem Gegenbeispiel verbietet), G-D4 (zwei Master: `hypothese_verdrahtung.rs:216` ignoriert `fuehrendes_main`, obwohl `mitgliedschaft.rs:87-92` es bei zwei Mains auf `None` setzt; Entwurf `:1669` „genau ein führendes Main pro aktiver Sitzung").
- **Ursache C — Vertragsvollständigkeit im Datenweg → NAK-214:** E-D5 = A6 (Proposal `target: ""` persistiert; Exit-Gate Satz 2, Schema `$defs/proposal.target` hex32, Invariante „Schemas sind Verträge"), E-D6 (wirkungsbeleg aus Probable-Paar; M-17, §4).

**Lücke** (Gate, Matrix, Entwurf schweigen; Regel in dieser Runde entschieden, Nacharbeit nur über die Regel):
- A4 (Masteranomalie aus `fenster.last()` `:1245-1246` auch außerhalb der Passage; M-23 bindet nur Zeitüberdeckung) → Regel NAK-212 R5. G-D5 (Bootstrap zweiseitig belohnt stabil negativen Uplift `experiment.rs:1542-1552`, `hypothese.rs:947`, `:962-967`; M-19 verlangt nur „Stabilität") → Regel NAK-212 R2. E-L3 (Klasse liest keine Rangkomponente — Lesart M-15) → Regel NAK-212 R1. G-L1 (Passagenriegel im Korpus ungemessen) → NAK-212 R6. A2 (Aufnahme iteriert `stand.evidenz` statt `stand.clients`) — **als eigener Mechanismus widerlegt**: die stumme Quelle fiele an `gate():807-809`, und der Ausschluss senkt den Überlebenden nicht; die Ausprägung ist G-D2 → NAK-213 R2. G-L2 (Kanalwechsel zwischen Fenstern hebt `mittel` auf `hoch`) → NAK-213 R5. G-L4 (Main ohne Fenster: Schweigen) → NAK-213 R4. E-L4 (Parent zurückgenommen/ohne Routing → Child unmarkiert) → NAK-213 R3. A5 (Gen behält READY nach Rücknahme bei Pipe-Abbruch; Broker schiebt sonst nach `invalidierung_verdrahtung.rs:385`) → NAK-214 R3. E-L5 (`user_verdict` mit nicht existenter `finding_id` persistiert, `befehl.rs:545-568`) → NAK-214 R4. E-L2 (`juengste_passage_im_projekt` ohne Filter) → NAK-214 R5. E-L1 (kein Feldname Sonde ↔ Summe) → NAK-215. G-L3 (Großeltern-Kanal nicht darstellbar) → NAK-216. E-L6 (`allowed_bounds {}` gilt als genannt) → NAK-217.

**Härtung** (von keiner Zusage verlangt): E-H1 (Gen prüft `alternatives`/`ausschluesse`/`candidateSource` nicht gegen den Snapshot) → NAK-218. E-H2 (Proposal behält `hoch` nach `stale` des Befunds) → NAK-219. G-H2 (verschobene Passage unter gleicher ID dauerhaft unvergleichbar) → NAK-220. E-L7 halb gemessene Zeilen M-33, M-44, M-87 → NAK-221; M-69 → NAK-212 R6. G-H1 (Duplikatpartner ohne Evidenz unsichtbar) und E-H4 (Deckel stumm) → in NAK-213 R2/R1 aufgegangen. E-H3 (Korpus ohne `mixer: null`) → NAK-212 R6. G-H3 (`bandpassung` als Trennschärfe) → NAK-212 R3.

**Entscheid des Dirigenten (Technik, 07.09.2026):** M-15 („die Klasse wird nicht aus dem Score gerundet") und M-31 („Schwelle kalibriert, nie geraten") bleiben. Sie verbieten eine Score-Schwelle, nicht ein **strukturelles** Zusammenhangsgate. Die Regeln R1 bis R3 in NAK-212 sind Strukturregeln: Energie im Befundband vorhanden, mindestens eine Zusammenhangskomponente positiv belegt, kein Gegenbeleg, Trennung über Zusammenhangskomponenten statt über ein Quantum. Kein Produktentscheid: Bedienlogik und sichtbare Priorität ändern sich nicht; das Produkt sagt in denselben Situationen öfter „mehr Daten nötig", was der Gate-Text ausdrücklich als gewünschtes Ergebnis benennt.

## 9. Kanon

Kein Code-Diff seit dem beglaubigten Kanon `4df8855` (GRÜN 60/60, `docs/beweise/roh/SONDE-014-4df8855.md`); die 60 Beine gelten für den Gate-Stand `bf92891` unverändert. Vom eigenen Lauf auf diesem Stand gefahren, alle grün: cargo `sonde014_hypothese` 19, `sonde014_proposal` 18, `sonde014_verdrahtung` 17, `sonde014_befund` 7, `sonde014_maskierung` 5, `sonde014_p5_korpus` 1, `sonde014_nacharbeit1` 8, `sonde014_nacharbeit2` 4, `sonde014_nacharbeit3` 3, `sonde013_verdrahtung` 78; `EqCopSonde014IntentTest` 210, `EqCopSonde014BefundTest` 60, `EqCopSonde014AssistentTest` 185, `EqCopIpcTest` 381; A28 bytegleich, A29 und A30 grün. Der Kanon läuft neu auf dem Endstand jedes Nacharbeitstickets und beim Gate-Abschluss.

## 10. Was das Urteil nicht deckt

- Kein FL-Lauf, kein Hörtest, kein installierter Stand; kein Material aus echten Sessions — alle Gegenbeispiele sind synthetische Bandpegelreihen im Korpusformat.
- Codex fuhr weder Tests noch Python; der eigene Lauf und der Gegenbeispiel-Lauf fuhren Rust- und Plugin-Beine, keinen Broker auf einer Pipe.
- `eq-copilot/plugin/tests/**` und `vertrag/generiert/**` waren nur Beleg, keine Fläche.
- Gate 7 hält an der heutigen Kette; ob eine spätere Anzeige (S31b) den Messpunkt nennt, ist mit NAK-215 registriert, nicht geprüft.
- Kein `/c-review`, kein `/rust-review`: der Gate-Text verlangt sie für G5 nicht (G4 hat die DSP-Fläche, G3 den Broker zuletzt gelesen; G7 ist das nächste Rust-Gate).
- Kein zweites Codex-Modell hat geurteilt: die Sol-Gegenprüfung wurde ohne Urteil abgebrochen (§3 Nr. 2). Die Unabhängigkeit der Befunde trägt in diesem Lauf das Paar Astra ↔ eigene Opus-Läufe (drei Mechanismen von zwei Seiten unabhängig gefunden: Main als Parent, leeres Proposal-Ziel, Deckel); die Sol-Gegenprüfung ist Teil der Abschlussprüfung.

## 11. Nacharbeit — was ein PASS braucht

Drei Tickets, je frischer Opus-Worker (max), Aufsicht ENG, **Spezifikation vor Code** (Verhaltensmatrix im Ticketmanifest, lesende Codex-Matrixprüfung Astra max, dann Bau), betroffene Beine je Runde, voller Kanon abgekoppelt am Ticketende, Wiederprüfung nach Vorlage B mit frischem Codex-Thread (Astra max; Sol max nur Gegenprüfer). Reihenfolge nach Anteil an Satz 3; die Ticketmanifeste `docs/beweise/NAK-212.md`, `docs/beweise/NAK-213.md` und `docs/beweise/NAK-214.md` sind anzulegen (Zielpfad, legt der Worker mit der Matrix an). Jede Regel ist ein Technikentscheid des Dirigenten (07.09.2026); der Worker setzt sie als Matrixzeilen mit Rotbeweis um und nennt, wo er jede Zeile gemessen hat.

1. **NAK-212 — Sicherheit `hoch` braucht Zusammenhang; Gates messen Überlappung und Passage** (`broker/src/coordinator/hypothese.rs`, Korpus und Prüfer). **R1** Sicherheit `hoch` setzt einen strukturell gemessenen Zusammenhang voraus: der Kandidat trägt Energie im Befundband (Bandpassung nicht null), und mindestens eine Zusammenhangskomponente ist positiv belegt — bedingter Uplift mit Fenstern ohne die Quelle (M-19) oder Onset-Koinzidenz. Ohne Zusammenhang höchstens `mittel`. Das ist eine Strukturregel, keine Score-Schwelle (M-15, M-31 unverändert). **R2** Ein Gegenbeleg — negativer bedingter Uplift oder negative Korrelation — schließt `hoch` aus und führt den Kandidaten als Alternative; Wiederholbarkeit misst die Stabilität eines positiven Effekts (einseitig), nie die eines Gegenbelegs. **R3** „Getrennt" (M-26 „praktisch gleich") ist ein Strukturmerkmal aus den Zusammenhangskomponenten, nicht ein Quantum von 1e-6 und nicht Intent-Relevanz oder Bandpassung allein: zwei Kandidaten, die sich in Uplift, Koinzidenz und Wiederholbarkeit nicht unterscheiden, sind ungetrennt, beide höchstens `mittel`, jeder Alternative des anderen — auch bei `rolle = fuehrt`. **R4** Das Alignment-Gate misst die paarweise Zeitüberlappung der Fenster von Kandidat und Master, nicht die Überlappung ihrer Spannen. **R5** Passagenmaterial zählt nur Fenster, die vollständig innerhalb der Passage liegen (M-23 „innerhalb"); die Masteranomalie wird ausschließlich aus Master-Fenstern innerhalb der benannten Passage bestimmt. **R6** Der Korpus fährt die Gegenbeispiele: die 12 Sitzungen und 15 Ketteneingaben aus `docs/beweise/roh/G5-gegenbeispiele-bf92891.patch` werden als Korpussitzungen beziehungsweise Kettenbein übernommen — samt einer Sitzung mit `mixer: null` — und der Passagenriegel steht im Korpus über `experiment_begin` unter Last; jede Gegenbeispiel-Sitzung trägt die Wahrheit „keine starke Aussage", und `pruefe_p5_korpus.py` prüft „auf jedem Gegenbeispiel unsicher" als eigenen Abschnitt (M-69). Die Kalibrierung (M-31) wird auf dem erweiterten Korpus neu gemessen; bewegt sich die Schwelle, gibt es einen `metrics_version`-Schritt. Zugeordnet: G-D1, G-D5, G-H3, G-L1, E-D2, E-D3, E-D4, E-L3, E-H3, A1, A4, E-L7 (M-69).
2. **NAK-213 — Unvollständige Konkurrenz und Master-Wahl** (`hypothese.rs` Ranking und Deckel, `hypothese_verdrahtung.rs` Aufnahme, Vertrag der Ausschlussgründe). **R1** Der Kandidatendeckel (M-18) greift vor Stufe B — höchstens fünf Kandidaten erreichen `rangkomponenten`, das Screening arbeitet auf dem 64er-Gitter — und jeder am Deckel ausgeschiedene Kandidat trägt einen Grund aus der geschlossenen Menge (M-87); die Menge wächst um genau diesen Grund in einer versionierten Vertragsfassung, gespiegelt in Rust, C++ und Python (M-77). **R2** Ein einziger Überlebender ist nicht „getrennt", wenn ein Konkurrent aus einem Messgrund ausgeschieden ist (`coverage_fehlt`, `alignment_falsch`, `passage_unvergleichbar`, `passage_zu_kurz`, `evidenz_zurueckgenommen`) oder eine angemeldete aktive Quelle der Sitzung noch keine Evidenz geliefert hat: dann höchstens `mittel`, der Konkurrent bleibt als Ausschluss oder Alternative sichtbar. **R3** Eine Quelle auf dem Mixerkanal des Masters misst das Mastersignal; sie ist Duplikat des Masters, nie Ursache, und fällt mit Grund. Parent-Duplikate werden über alle Quellen der Sitzung einschließlich Master, zurückgenommener und stummer Quellen erkannt (M-22). **R4** Ohne eindeutig führendes Main (`fuehrendes_main = None`) rechnet die Kette nicht: das Ergebnis ist Enthaltung mit Grund; der Master der Aufnahme ist stets das führende Main, nie „der letzte"; ein Main ohne Fenster ergibt Enthaltung, nicht Schweigen. **R5** Ein Wechsel des Mixerkanals einer Quelle ist ein Messpunktwechsel: Befunde, die die Quelle tragen, werden invalidiert (`stale`), und Fenster vor dem Wechsel zählen nicht zum Routing nach dem Wechsel. Zugeordnet: G-D2, G-D3, G-D4, G-L2, G-L4, G-H1, E-D1, E-L4, E-H4, A2 (Ausprägung), A3, A7.
3. **NAK-214 — Vertragsvollständigkeit im Datenweg** (`proposal_verdrahtung.rs`, `proposal.rs`, `hypothese_verdrahtung.rs` Paarnaht, `befehl.rs`, `experiment.rs`, `eq-copilot/plugin/src/SourcesModel.cpp`). **R1** Kein Proposal entsteht oder wird persistiert ohne die sechs Felder des Exit-Gates; `gate_felder_vollstaendig` ist produktiver Riegel vor Persistenz und Zustellung, und ohne führendes Main gibt es kein Proposal. **R2** `claim_class = wirkungsbeleg` entsteht nur aus einem PRE/POST-Paar der Klasse `AudioAligned` mit gemessener Wirkung (M-17); ein Paar darunter bleibt Klasse 1, und `next_test` verlangt das Paar weiterhin. **R3** Verbindungsende (`controlEnde`) und Sitzungswechsel setzen in Gen alle Befunde auf `stale` (nicht handelbar); eine Rücknahme ohne Folge-Snapshot lässt keinen READY-Befund stehen. **R4** `user_verdict` mit einer `finding_id`, die im Broker nicht existiert, wird abgewiesen (`reject`), nicht persistiert. **R5** Die Passage einer Aufnahme ist die jüngste Passage des Projekts, deren Experiment nicht zurückgenommen und nicht getaintet ist und deren Vergleichbarkeit nicht unbekannt ist; sonst rechnet die Aufnahme ohne Passage. Zugeordnet: E-D5, E-D6, E-L2, E-L5, A5, A6.

Nach dem dritten PASS: Nachtrag hier (§12 ff.) mit Marke `T3 PASS`, Kanon auf dem Endstand, Planstand, Register.

## 12. Nachtrag (2026-09-08, Dirigent): NAK-212 abgenommen — T3 PASS

Nacharbeitsticket 1 von 3 (`docs/beweise/NAK-212.md`, Urteilsmarke im Kopf) ist mit Endstand `1551dc2` abgenommen: Kanon **GRÜN 61/61** (`docs/beweise/roh/NAK-212-1551dc2-dirty.md`; die Marke `-dirty` steht für zwei untracked Rohdateien des Dirigenten, kein Codeunterschied). Die Regeln R1 bis R6 aus §11 Nr. 1 sind als Verhaltensmatrix E1 bis E9 und N-01 bis N-47 spezifiziert (drei Matrixprüfungen: NEEDS_WORK 6 → NEEDS_WORK 1 → PASS) und gebaut: Sicherheit `hoch` verlangt jetzt einen strukturell belegten Zusammenhang (eigene vorzeichenbehaftete Größe, Gate in der Klassenwahl, kein Score-Gate), ein Gegenbeleg schließt `hoch` aus, die Wiederholbarkeit ist einseitig, „getrennt" verlangt verschiedenen quantisierten Gesamtrang **und** verschiedenen Zusammenhang gegen jeden Überlebenden, das Alignment misst paarweise Überlappung ohne leere Fenster, Passagenmaterial zählt nur ganz innenliegende Fenster und die Masteranomalie nur Passagenfenster (Enthaltung statt Schweigen ohne Masterfenster), der Korpus fährt die 12 Gegenbeispiel-Sitzungen mit je-Fenster-Reihen und das neue Kettenbein A31 die 15 Ketteneingaben durch den Produktpfad mit benannten NAK-213-/NAK-214-Lücken; die Kalibrierung auf dem erweiterten Korpus lässt die Schwelle bei `hoch`, kein `metrics_version`-Schritt. Runden nach dem Bau: Erstprüfung (vier Defekte) → Nacharbeit 1 → Wiederprüfung 1 (Rest von NR-03) → Nacharbeit 2 → Wiederprüfung 2 (WN-02 geschlossen; WN-01-Rest = Store-Banner auf stderr, per Entscheid Härtung NAK-222). Prüfer: Codex Astra max (drei Matrixprüfungen, Erstprüfung, zwei Wiederprüfungen), Bauer Opus max. Aus dem Ticket ins Register: NAK-222. **G5 bleibt T3 NEEDS_WORK**, bis NAK-213 (unvollständige Konkurrenz und Master-Wahl) und NAK-214 (Vertragsvollständigkeit im Datenweg) abgenommen sind; Reihenfolge nach §11.

## 13. Nachtrag (2026-09-08, Dirigent): NAK-213 abgenommen — T3 PASS

Nacharbeitsticket 2 von 3 (`docs/beweise/NAK-213.md`, Urteilsmarke im Kopf) ist mit Endstand `d3c6709` abgenommen: Kanon **GRÜN 61/61** (`docs/beweise/roh/NAK-213-d3c6709.md`, Worktree sauber). Die Regeln R1 bis R5 aus §11 Nr. 2 sind — ergänzt um die Dirigentenregeln R6 (Ausschlussliste nie gekappt, `ausschluesse.maxItems` = `SESSION_CLIENT_CAP` in Fassung 4), R7 (der C++-Leser wählt die Fassung: neue Gründe und Längengrenze nur bei `schema_minor >= 4`) und R8 (Enthaltung ohne Ort setzt einen gültigen Beleg voraus, sonst kein Befund) — als Verhaltensmatrix E1 bis E9 und K-01 bis K-55 spezifiziert (drei Matrixprüfungen: NEEDS_WORK 9 → NEEDS_WORK 1 → PASS) und gebaut: der Kandidatendeckel schneidet vor Stufe B über eine eigene Screeningordnung (Gruppenenergie, Überlappung, Onset-/Peak-Koinzidenz, Priorität) und jeder Ausgeschiedene trägt `screening_ueberboten`; die geschlossene Menge wächst in Vertragsfassung 4 um `screening_ueberboten` und `master_duplikat`, gespiegelt in Schema, Rust, C++ und Python mit Rückbauprobe auf Fassung 3; ein Einzelüberlebender neben einem Messausschluss oder einer stummen angemeldeten Quelle trägt über die `Konkurrenzlage` höchstens `mittel`; Kanaltafel und Parent-Erkennung laufen über alle Quellen einschließlich Master, eine Sonde auf dem Kanal des führenden Mains fällt mit `master_duplikat`; der Master der Aufnahme ist das führende Main, ohne eindeutige Führung oder ohne Masterbeleg entsteht eine Enthaltung ohne Ort statt Schweigen; der Mixerkanalwechsel ist ein Messpunktwechsel über den produktiven Heartbeat-Pfad mit Rückweg bei verweigertem Store; die acht in NAK-212 gedruckten NAK-213-Lücken geben ihre erwartete Ausgabe aus, Korpus und Kettenbein A31 drucken keine NAK-213-Lücke mehr. Runden nach dem Bau: Erstprüfung (vier Defekte: Main-Belege ohne Führung, Kanalwechsel bei Store-Fehler, K-49 ohne Store, K-44 ohne Lage 2) → Nacharbeit 1 → Wiederprüfung 1 (zwei Restbefunde in Tests) → Nacharbeit 2 → Wiederprüfung 2 PASS. Prüfer: Codex Astra max (drei Matrixprüfungen, Erstprüfung, zwei Wiederprüfungen), Bauer Opus max. Aus dem Ticket ins Register: nichts; Nebenbefunde NB-1 bis NB-9 im Manifest §4. **G5 bleibt T3 NEEDS_WORK**, bis NAK-214 (Vertragsvollständigkeit im Datenweg) abgenommen ist.

## 14. Nachtrag (2026-09-09, Dirigent; Beweise vom 08.09.2026): NAK-214 abgenommen — G5 T3 PASS

Nacharbeitsticket 3 von 3 (`docs/beweise/NAK-214.md`, Urteilsmarke im Kopf) ist mit Endstand `89c97f0` abgenommen: Kanon **GRÜN 61/61** (`docs/beweise/roh/NAK-214-89c97f0.md`, Worktree sauber; Codestand identisch mit `3b744695`, dem Commit der Nacharbeit 2). Die Regeln R1 bis R5 aus §11 Nr. 3 sind — ergänzt um die Dirigentenregeln R6 (vierter Punkt des Wirkungsprädikats: mindestens ein gültiges Band des ausgerichteten Deltas oder `match_gain_db` von exakt 0.0 verschieden), R7 (der Nachlauf einer Intervention gehört zum Taint; die Zuordnung Nachlauf→Experiment wird beim Interventionsende gerettet und nur durch Nachlaufende oder bestätigten Resync geleert, ein Terminal lässt sie unberührt) und R8 (jeder lokale `stale`-Übergang in Gen rechnet `findingsOffen` neu) — als Verhaltensmatrix E1 bis E9 und V-01 bis V-49 spezifiziert (vier Matrixprüfungen: NEEDS_WORK → NEEDS_WORK → NEEDS_WORK → PASS) und in sechs Bauetappen gebaut: `gate_felder_vollstaendig` ist produktiver Riegel vor Persistenz und Zustellung, ohne führendes Main entsteht kein Proposal (A); `claim_class = wirkungsbeleg` entsteht nur aus einem PRE/POST-Paar der Klasse `AudioAligned` mit gemessener Wirkung, ein Paar darunter bleibt Klasse 1 und `next_test` verlangt das Paar weiter (B); Gen setzt bei `controlEnde`, Sitzungswechsel und Rücknahme ohne Folge-Snapshot alle Befunde auf `stale` und zählt `findingsOffen` neu (C); `user_verdict` auf eine im Broker nicht existente `finding_id` wird mit `unknown_target` abgewiesen und nicht persistiert (D); die Passage einer Aufnahme ist die jüngste nicht zurückgenommene, nicht getaintete Passage bekannter Vergleichbarkeit, sonst rechnet die Aufnahme ohne Passage (E); die drei NAK-214-Lückenstellen in Korpus und Kettenbein A31 geben ihre erwartete Ausgabe aus (F). 22 Rotbeweise, kein Fassungsschritt (Manifest §2.8). Runden nach dem Bau: zwei rote Kanonläufe (die Bühnen dreier ticketfremder Tests schickten `user_verdict` auf befundlose IDs — nur die Bühne nachgezogen, die Zusagen unverändert) → Kanon GRÜN 61/61 auf `ac20c7a` → Erstprüfung (kein Produktdefekt; zwei Testdefekte der R4-Abdeckung: V-32-Kontrollfall `stale` ungemessen, zweiter Teilfall von V-34 fehlt) → Nacharbeit 1 → Wiederprüfung 1 (V-32 geschlossen; Rest V-34: die Vorbedingung unterscheidet `None` nicht von `Some(vec![])`) → Nacharbeit 2 (Unit-Test im Crate am Bestand `stand.befunde`, kein neues `pub`) → Wiederprüfung 2 **PASS** (Thread `01a0825f-2b4b-71d0-a3b4-b888bc16a9e2`). Prüfer: Codex Astra max (vier Matrixprüfungen, Erstprüfung, zwei Wiederprüfungen), Bauer Opus max. Aus dem Ticket ins Register: NAK-228 bis NAK-233 (Nebenbefunde §4 des Manifests: PRE/POST-Wirkung ohne Bandbindung, drei `next_test`-Fälle mit einem Anzeigetext, C++-Leser kennt die sechs Fehlercodes der Fassung 2 nicht, `pre_post` aus Paarexistenz statt Messpunkt, vor R1 persistierte Proposals mit leerem Ziel, toter `veraltet`-Parameter) und NAK-234 aus der Codebase-Messung des Abschlussfensters.

**Damit ist das P5-Exit-Gate in allen drei Sätzen geschlossen** (Satz 3 mit NAK-212 und NAK-213, Satz 2 mit NAK-214 R1, Satz 1 mit NAK-214 R3 für Gen und R4 für den Broker, §6), Gate 7 hält (§5), und Gate 6 hält jetzt auch an der Naht zur P5-Kette (§4: `wirkungsbeleg` entsteht nur aus einem Paar der Klasse `AudioAligned`); die adversarialen Eingaben des Gegenbeispiel-Laufs (§7) laufen im Korpus (A28, A29) und im Kettenbein A31 mit ihrer erwarteten Ausgabe ohne benannte Lücke. **Urteil: T3 PASS** — Modell und Effort der Nacharbeitsprüfungen: Codex `gpt-6-astra` max, lesend, je frischer Thread; Bauer Opus max. Kein FL-Lauf, kein Hörtest, kein installierter Stand (§10 gilt unverändert). Nächstes Gate: G6 nach S26–28; davor die Pflegeschritte S25b bis S25d (NAK-223 bis NAK-225).
