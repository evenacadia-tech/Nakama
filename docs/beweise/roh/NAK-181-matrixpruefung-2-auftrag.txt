Wiederprüfung Etappe 1 (Matrixprüfung 2) / Ticket NAK-181 (G4-Nacharbeit 2), Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, keinen Compiler, keinen Cargo-Lauf und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 55ef08682c0d33c142796681d133d26b1aa725d8...2f88e4531cdd979fd8084e992981757597a59a87 -- docs/beweise/NAK-181.md`
(ZIEL = 2f88e4531cdd979fd8084e992981757597a59a87 = HEAD, vor und nach deinem Lauf identisch; eine uncommittete Datei docs/beweise/roh/NAK-181-matrixpruefung-2-auftrag.txt im Worktree ist dieser Auftrag selbst). Der übrige Ticketbereich — die Matrix vom Stand 548bf9d — wurde in Matrixprüfung 1 bereits geprüft und ist hier nicht Prüfgegenstand. In dieser Etappe gibt es keinen Produktcode: Prüfgegenstand ist die Verhaltensmatrix in docs/beweise/NAK-181.md §2 (Entscheide E1 bis E9, Zeilen N-01 bis N-42 samt N-18b, §2.3, §2.4), der Ist-Stand §3 und der Bauplan §4 (mit §4.4 Nr. 1 bis 13); der Änderungsbericht des Workers steht in §5.1. Prüfstufe T3-Nacharbeit zum Gate G4.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus Matrixprüfung 1, docs/beweise/roh/NAK-181-matrixpruefung-1-548bf9d.txt; Einordnung des Dirigenten mit der Regel je Befund in docs/beweise/NAK-181.md §5 und im Nacharbeitsauftrag docs/beweise/roh/NAK-181-matrixnacharbeit-1-auftrag.txt):
MP1-1 [P1] „Stimme N-18 auf den v3-Textriegel ab — `wireZahl(1.0/3.0)` erzeugt im gewählten ES6-Format `0.3333333333333333`, doch jeder Broker-v3-Payload läuft vor `serde_json` durch `textriegel_bytes` (`broker/src/coordinator/schema.rs:229`), das nichtganzzahlige Literale mit mehr als 15 signifikanten Stellen ablehnt (`broker/src/vertrag.rs:219-226`; A5 spiegelgleich in `tools/eq-copilot/pruefe_v3_vertrag.py:232-237`). Auch `1e300` und `4.9e-324` verletzen dessen Zahlenränder, sodass N-18 nur C++-Parsebarkeit statt der von R4 verlangten Rust-/A5-Vertragsannahme misst." — Regel R4a: Wire-Zahl hält den Textriegel (≤ 15 signifikante Stellen, Exponent innerhalb der Riegelgrenze); `zahlAlsEs6` für State bytegleich; N-18 misst Annahme durch `textriegel_bytes` UND A5 UND `juce::JSON`; Werte jenseits der Riegelgrenze reisen nie als Zahl, die Matrix sagt, was stattdessen reist.
MP1-2 [P1] „Berücksichtige ausstehende P0-Retries beim Reload — Wenn beim Reload ein Experiment-P0 bereits auf den Draht geschrieben, aber noch nicht bestätigt wurde, bleibt sein Text in `inFlight` (`ControlClient.h:537-542`) und wird nach `controlV3.reconnect()` erneut eingereiht (`ControlClient.cpp:1137-1151`). B9 leert nur den lokalen Vergleichszustand und §2.4 lässt die Queue unangetastet; N-09s Zusage ‚kein einziges P0-Byte' ist damit gerade in diesem erreichbaren Retry-Fall falsch." — Regel R3a: die Matrix legt fest, was mit `inFlight`/Queue des alten Versuchs geschieht, misst es mit injiziertem unbestätigtem Experiment-P0; nichts aus NAK-180 wird verworfen; Weg (a) gezieltes Verwerfen oder (b) ehrliche Zusage „kein angenommenes Byte", auf beiden Seiten gemessen.
MP1-3 [P1] „Binde das Persistenzbit an einen bandweisen Messnachweis — Bei N-23 mit einem stillen Kanal wird `stereoKurzfenster` dennoch global erhöht (`FeatureEngine.h:2908-2934`), während die heutige Persistenzzuweisung allein `stereoKurzfenster > 0` prüft (`:3025-3028`). Wird `persistenzGesetzt` wie E5 verlangt genau an dieser Zuweisungsstelle gesetzt, reist für das stille Band weiterhin eine gemessene 0 statt eines fehlenden Bits." — Regel R5a: Persistenzbit bandweise an einen Messnachweis gebunden; ohne Nachweis kein Bit.
MP1-4 [P1] „Erhalte N-22s Präsenzbits bis zur Serialisierung — Wenn B7 die neuen Bits in `StereoBandwert` wie beschrieben in `stereoLeeren()` nullt, geschieht das über `evidenzLeeren()` noch in `baueFrame()` (`FeatureEngine.h:3555-3572`); `SondeProcessor` liest `stereoBand()` erst anschließend für den JSON-Writer (`SondeProcessor.cpp:542-557`). Damit sind die publizierten Bits in jedem frischen Snapshot bereits gelöscht und N-22 kann nie einen gesetzten Wert beobachten." — Regel R5b: die Bits sind Mitglieder des publizierten `StereoBandwert`, ausschließlich in `stereoAuswerten` bestimmt wie `kohaerenzGesetzt` (`:3010`); `stereoLeeren` fasst das publizierte Array nicht an.
MP1-5 [P1] „Akzeptiere den schema-gültigen null-Wert für pair_id — Ein `session_paar` mit `pair_id: null` ist nach dem eingefrorenen v3-Schema gültig: `$defs/pair_id` erlaubt String oder null (`eq-ipc-v3.schema.json:788-792`), und `session_paar` referenziert diese Definition (`:1030-1037`). E6/N-28 schreibt dagegen zwingend einen nichtleeren String vor." — Regel R6a: `null` gültig („kein Paar", wie `liesDescriptor`), Strings 1 bis 64 Codepoints, sonst ungültig; N-28 mit `null`-Fall.
MP1-6 [P1] „Ergänze E7 um Kanalwechsel als Epochengrenze — Bei einem Kanalwechsel innerhalb derselben `startFolge` verwirft `Blockquarantaene::schliesstAn` den gehaltenen Block (`StampedAudioQueue.h:727-734`), aber `FeatureEngine::grenzeZwischen` vergleicht `kanaele` nicht (`FeatureEngine.h:1773-1824`). Der nächste freigegebene Block erscheint dadurch nur als `lokaleLuecke`/`sequenzluecke`." — Vom Dirigenten als LÜCKE mit entschiedener Regel R7a eingeordnet: eine Matrixzeile für den heutigen Weg (`lokaleLuecke` → `continuity_segment + 1` → `Sequenzluecke`), gemessen; KEIN Umbau von `grenzeZwischen`; `epochwechsel`-Benennung als Härtung NAK-194. Geschlossen heißt hier: die Zeile existiert, misst den heutigen Weg, und §2.4 nennt die Nicht-Zusage.
MP1-7 [P2] „Ergänze die vorgeschriebene Retry-Diagnose — Beim ersten Druck nach 0,2 s liefert der Plan nur `false` und lässt zwei Rohzähler abfragen; ein Produkt-/Editorpfad ruft diese Getter am Ziel jedoch nicht auf." — Vom Dirigenten als KEIN DEFEKT eingeordnet (Begründung in E2 :119-125 gültig; kein Editor-Handgriff existiert, Fläche ist S31b). Für diesen Befund ist keine Änderung verlangt; du prüfst nur, ob die Ergänzung in E2 etwas bricht.
MP1-8 [P2] „Trenne in N-03 lebenden und eingefrorenen Zähler — Nach Beginn lädt `versuchNichtEndlicheSamples()` am Ziel ausschließlich das eingefrorene Atomic `versuchNichtEndlich` (`PluginProcessor.cpp:2997-2999`); neue Audiosamples erhöhen nur den internen Zähler des lebenden `Vergleichspegel`. B8 ändert den Getter nicht, sodass er im Zustand von N-03 weiterhin 0 liefert." — Regel R1a: N-03 berichtigen, lebender Zähler über einen benannten Leser gemessen.
MP1-9 [P2] „Beschreibe N-35s Reset-Unterschiede korrekt — `zuruecksetzen()` leert den Ereignisring (`FeatureEngine.h:1117-1118`), `grenzeZiehen()` berührt ihn dagegen nicht, und `rahmenLeeren()` ordnet ihn ausdrücklich nur dem Reset zu (`:3615-3618`)." — Regel R7b: N-35 benennt die Differenz, E7 begründet, dass kein Ereignis über die Grenze in eine Paarauswertung eingeht.

GATE-BEZUG, wörtlich: P4-Exit-Gate (docs/FL-Nakama-Sonden-Design-Entwurf.md:4287-4289) Satz 2: „Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage." §49.2 Gate 6 (:3557): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." Referenzmatrix docs/beweise/SONDE-013.md M-07, M-08, M-11 (Zeilen 476-480), M-18, M-22, M-23 (493-498), M-43, M-46, M-50 (533-540), M-53 (548). Invarianten aus CLAUDE.md: Schemas sind Verträge (v3 eingefroren); Audio-Thread ohne Sperren, Allokationen, Datei-, Pipe-, Netzzugriff und Logging; Nulltest bitidentisch; NaN-Ehrlichkeit; State verlustfrei; Zeit ist aktive Musikzeit.

Du beantwortest genau zwei Fragen:
1. Ist jeder der genannten Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Zeilen-ID, Manifestzeile, Quelle Datei:Zeile am ZIEL)? Für MP1-6 gilt die Regel R7a des Dirigenten als Maßstab, für MP1-7 „keine Änderung verlangt". Prüfe die Schließungen an der Quelle: R4a gegen `broker/src/vertrag.rs` (Regeln und Grenzen, DEZ_GRENZE) und `tools/eq-copilot/pruefe_v3_vertrag.py`; R3a gegen `broker/src/coordinator/befehl.rs` (`persistenz_p0_intern`, `unknown_target`), `experiment_verdrahtung.rs`, `ControlClient.cpp` `inFlightNachReconnect`; R5a/R5b gegen `FeatureEngine.h` (`stereoAuswerten`, `stereoLeeren`, `stereoErgebnis`, `baueFrame`) und `SondeProcessor.cpp`; R6a gegen das Schema und `SourcesModel.cpp`; R7a gegen `StampedAudioQueue.h`/`FeatureEngine.h`; R1a gegen `PluginProcessor.cpp`, `Vergleichspegel.h` und die Testbindung von B23/B26 (`Sonde013PassageStateTest.cpp`, `Sonde013ExperimentUiTest.cpp`); R7b gegen `grenzeZiehen`, `rahmenLeeren` und `Sonde013EventWireTest.cpp:530-545`.
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile, einen Entscheid E1 bis E9, eine Zeile der SONDE-013-Referenzmatrix, eine Zusage aus NAK-180 (docs/beweise/NAK-180.md §2, Aufbaufolge §2.1, `inFlight`/Zustellgeneration), einen bestehenden Vertrag, einen Satz des Gate-Bezugs oder eine Invariante aus CLAUDE.md? Besonders: Ist der Umhang der R1-/R2-Zeilen von B26 auf B23 (§5.1, §4.4 Nr. 12/13) an der Quelle richtig (bindet B26 wirklich keinen Prozessor)? Ist der neue Leser `vergleichspegelZaehlerstand` audiothreadfrei? Deckt N-41 (`unknown_target` über `persistenz_p0_intern`) den Retry-Fall wirklich, oder kann der Retry einen Link der Sendersitzung finden (z. B. Sonden-Link derselben Sitzung)? Ist die 15-Stellen-Deckelung mit N-16 („bytegleich zur C-Locale-Ausgabe") und mit N-19 (State-Hashes unverändert) verträglich?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1: DEFEKT (bricht Gate, Matrix, Regel, Entwurf, Vertrag, Code am ZIEL oder Invariante — je Defekt Zeilen-ID, Datei:Zeile, welcher Satz bricht), LÜCKE, HÄRTUNG. Ausschlüsse: docs/** außerhalb von §2 bis §5.1 dieses Manifests (Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände; docs/beweise/G4.md, NAK-180.md, offene-punkte.md); im Register datierte Härtungen und Lücken NAK-155 bis NAK-179 und NAK-183 bis NAK-192; Nebenbefund §4.7 Nr. 1 (`seitenanteil_db`, M-45) und die `epochwechsel`-Benennung des Kanalwechsels (NAK-194) liegen außerhalb; tools/dirigent/pruefliste.md ist keine Anforderungsquelle; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; Ticketfremdes (NAK-180, NAK-182, Anzeigefläche, v2-Telemetriebaum) nein; Kosmetik, Stil nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID oder Manifestzeile, Quelle Datei:Zeile, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
