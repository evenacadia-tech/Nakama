URTEIL: NEEDS_WORK — ein Bruch aus Frage 2: §40.3, Absatz vor der zweiten Tabelle, Satz „Die Ebenen ab `prozessor()` (`:1722`) fahren dagegen echtes Audio — ihre Paare stehen in der ersten Tabelle" ist am Code falsch; `abgeschlossener_gestus_ist_eine_revision (M-82)` (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2114-2143`) ist ein Publikationspaar auf der Ebene `prozessor()` OHNE Audio und steht in der ZWEITEN Tabelle derselben Datei (Zeile 6957 von `docs/beweise/NAK-311.md`). Alle Befunde V-1 bis V-3 und alle Regeln R-311-21 bis R-311-23 sind geschlossen; keine Tabellen- oder Listenzeile ist an der Quelle falsch.

# NAK-311 — Matrixprüfung 6 (Wiederprüfung, Etappe 5, Runde 2 von 3)

**HEAD zu Beginn:** `1427d43cd82036736f0245321a6cd18b23452bf5`
Prüfbereich: `git diff 4849572fed391c1a3e3b6a49c75a2c334189eb96...da5ebad6d8837814ffe648285a8d95c0c2fb4730 -- docs/beweise/NAK-311.md`, 33 Hunks, +692/−137, genau eine Datei. Hunkgrenzen selbst gemessen (`git diff -U0 | grep "^@@"`): Hunk 1 in der Kopfzeile „Etappe" (Zeile 8), Hunks 2 bis 32 in alten Zeilen 5932 bis 7523 (§38 bis §41), Hunk 33 als Anhang ab alter Zeile 8346 (§47). `git status --short` zeigt nur `briefing-hub/` und `nimbalyst-local/`; beide unberührt. Geschrieben habe ich genau diese Datei.

---

## Frage 1 — Ist jeder Befund am Zielstand geschlossen?

### V-1 / R-311-21 — JA, geschlossen

**(i) Kein Satz behauptet mehr Vollständigkeit der Bestandsfälle durch Lesen.**
Eigene Suche über §38 bis §41 (Zeilen 5836 bis 7719) nach „vollständig", „Vollständigkeit", „jeden Fall", „kein weiterer", „gibt es … nicht", „die sieben", „alle Rampenfälle", „jede bestehende", „jeder Fall", „alle Fälle", „sämtlich"; jeder Treffer im Zusammenhang gelesen. Die vier Stellen, die den Begriff noch tragen, kehren ihn um: §38.1 (5932-5935) „die größten **bekannten** Bestandsfälle … die Vollständigkeit misst der Änderungssatz C nach R-311-21"; §40.3 (6858, 6887) „Schritt C-0 — die Vollständigkeit wird gemessen, nicht gelesen" und „Sie behaupten keine Vollständigkeit durch Lesen"; §40.4 (7207) „**Vollständigkeit behauptet diese Liste nicht durch Lesen** — sie misst der Änderungssatz D"; §41.2 F-20 (7472) und §41.6 „Gegengelesen" (7679-7681) ebenso. Die vom Auftrag genannten Altsätze sind weg: „Die Bestandsfälle auf dem Rampenweg, vollständig", „Kein weiterer Fall der vier Testdateien publiziert …", „Alle sieben Fälle liegen unter den drei Kriterien", „Weitere Fälle über einem Kriterium gibt es in den vier Testdateien nicht" finden sich im Zielstand nicht mehr (`rg` über den Bereich, null Treffer). Verbliebene Treffer mit anderem Gegenstand, geprüft und ohne Bezug zu den Bestandsfällen: 6551 („setzt vollständig zurück", Slot-Reset), 6482 („sämtlich auf der ungedeckelten Seite", M-119), 7409 („die vollständige Paarung", Beziehungsregel F des Änderungssatzes), 6982 („nennt ihre Tabelle für diese drei Dateien selbst vollständig" — Selbstauskunft der Quellvalidierung, ausdrücklich NICHT als Wache übernommen, 6983-6985).

**(ii) Die zwei Tabellen von §40.3 stimmen an der Quelle.**
*Erste Tabelle („Bekannte Rampenfälle mit Audio über einem Blockrand", Zeilen 6916-6924), alle 9 Zeilen einzeln nachgemessen:*

- Zeile 1-3 `bandwert_<feld>_rampt_ohne_zustandsreset`: `DspGoldenTestMain.cpp:1875` = `{ "gain_db", param::kGainDb, 3.0, 9.0 }`, `:1876` = `1000.0, 1400.0`, `:1877` = `1.0, 3.0`; `pruefe` `:1931`, Prüfname `:1932`, Maß „Residuum < 1e−5" (`:1931`, `:1933`). Werte 6,0 dB / 1,4 / 3,0; Abstände 14,0 dB, Faktor 1,4286 (≈1,43), Faktor 1,333 — selbst nachgerechnet. Alle drei: gleiche Topologie, `belege` `:1885` Bell 1000 Hz, Audio über Blockrand bei Sample 2048 (`:1880`).
- Zeile 4 `null_allokationen_im_callback_samt_programmwechseln`: `:4013` = 6.0, `:4014` = 2.0 → 4,0 dB, Abstand 16,0; `pruefe` `:4075`, Prüfname `:4076`; gelesen werden Allokations- und Sperrzähler und `uebernahmen` (`:4073-4075`).
- Zeile 5 `uebernahme_nur_am_aeusseren_blockrand`: `:4199` = lowShelf 8000 Hz Q 0,707 **+9.0**, `:4205` = dieselbe Topologie **−9.0** → 18,0 dB, Abstand 2,0; Prüfname `:4225`; gelesen wird der Umschaltzeitpunkt (`haken.aufrufe == 3`, `uebernahmen() == vorher + 1`, `:4223-4224`). Die zweite Publikation läuft über `uebernehmeZustand` im Teilstückhaken (`:4211`).
- Zeile 6 `realtime_und_offline_gleich (M-120)`: `:5670` = Bell 250 Hz Q 1,2 **+5.0**, `:5686` = Band 0 `kGainDb` **−2.0** → 7,0 dB, Abstand 13,0; `pruefe` `:5799` und `:5816`, Prüfnamen `:5800` und `:5817`; verglichen werden zwei Läufe desselben Kerns, bitgleich (`gleicheLaenge && ersteAbweichung < 0`).
- Zeile 7 `311/M-94 …`: `:6825` = Bell 1000 Hz Q 2,0 **+12.0**, `:6831` = `kGainDb` **6.0** → 6,0 dB, Abstand 14,0; Prüfname `:6876`; gelesen werden E-31-Spitze am Umschaltsample und Perioden-RMS gegen den Referenzkern (`:6875-6880`).
- Zeile 8 Gegenprobe in `capability_entscheidet_den_automationspfad (M-119)`: `TransactionTestMain.cpp:2069` = `setzeBand (z, 0, 1000.0, 9.0)`, publiziert `:2070`; `:2081` = `hostSchreibt (… kGainDb, -9.0f)`, Kontrolltakt `:2082`; Audio `:2074` (200×128) und `:2083` (50×128); `pruefe` `:2085`, Prüfname der Gegenprobe `:2086`, Prüfname des Tests `:2080`. 18,0 dB, Abstand 2,0. Gelesen wird `gain (wirksamerZustand(), 0) == -9.0 && ! bitgleich (c, d)` — ein Unterschied, keine Zahl (`:2085`). **Rampenkompatibilität selbst geprüft**: die Topologieschreibungen `:2071` (`kType`) und `:2072` (`kChannelMode`) wirken nicht — `:2078-2079` verlangt `enumIndex == 0` für beide; damit bleiben Typ, Modus, `dynamisch`, `nutztSvf`, `quelle` gleich (`DspProgramm.cpp:216-224`), `eqEngagiert` wahr, Samplerate gleich.
- Zeile 9 `automation_erzeugt_keine_revision (M-81, Prozessor)` / `epoche_wechselt_genau_zweimal_je_geste`: `:2091` = 1000 Hz / 3,0 dB, publiziert `:2092`; Schleife `:2095`, `:2097` = `-6.0f + 0.2f*i`, Kontrolltakt `:2098`, Audio `:2099` (1 Block à 480); Prüfnamen `:2109` und `:2111`. 9,0 dB als Paar (3,0 → −6,0 bei i = 0, **vor** dem ersten Blockrand: zwischen `:2092` und der ersten Publikation aus `:2098` läuft kein `fahreAudio`), danach 0,2 dB je Blockrand; Abstände 11,0 und 19,8 — nachgerechnet. Gelesen werden Revision, Undo-Ring (`:2108`) und Automationsepoche (`:2110`). Die Doppelaussage der Zeile entspricht dem Entscheid §48 Nr. 4.

*Zweite Tabelle („Bekannte Publikationspaare ohne Audio über einem Blockrand", Zeilen 6938-6959): 22 Zeilen, nachgezählt; ich habe alle 22 an der Quelle gemessen* (verlangt waren zehn). Einzeln:

- `T1` `:398`→`:400`→`:402`, Prüfname `:409`: 3,0 (aus `grundzustand` `:279`) → 6,0 → −6,0, größter Schritt 12,0 dB, Abstand 8,0, unter.
- `T2` `:417`→`:427`, Prüfname `:422`, Wiederholung `:428` — **Berichtigung bestätigt**: der Lauf an `:421` endet in `konflikt` (`:423` verlangt `Ausgang::konflikt`) und publiziert nicht; erst `:427` committet (`:428`). 3,0 → 6,0 = 3,0 dB, Abstand 17,0, unter.
- `T3` `:435`→`:443`, Prüfname `:438`, Wiederholung `:444` — die 18,0 dB an `:436` sind außerhalb ±12 dB und fallen mit `Ausgang::fehler` (`:439`); publiziert wird 6,0 an `:443`. 3,0 dB, Abstand 17,0, unter.
- `T6` `:476`→`:478`, Prüfname `:481`: 3,0 dB, Abstand 17,0.
- `T7` `:491`→`:499`, Prüfname `:500` — **Berichtigung bestätigt**: `:494` endet in `busyRetry` (`:495`), erst `:499` committet. 3,0 dB, Abstand 17,0.
- `T9` `:524-531`, Prüfname `:532`, 500 Hz aus `zonenVorlauf` `:372-381` Wertzeile `:375`, Commit `:531`: **Frequenz genau 2,0, Abstand 0, AUF dem Kriterium**. Nachverfolgt: `:526` fällt in den Zonenkonflikt, `:529` publiziert 500 Hz (r = 2), `:531` publiziert 1000 Hz (r = 3). Gelesen werden Ausgang, Stufe, Revision (`:532-533`) — weder Weg noch Ausgang.
- `dreiunddreissigCommits` `:570-583`, Schleife `:575`, Wertzeile `:577`, Prüfnamen `:591`/`:601`: `(i%24)−12+0,25`; i = 23 → 11,25, i = 24 → −11,75, Differenz **23,0 dB** (nachgerechnet), Abstand 3,0, **über**.
- `T12` `:609`→`:610`→`:612`, Prüfname `:623`: 12,0 dB, Abstand 8,0. Die 31 Abweisungen `:616-618` publizieren nicht.
- `fehler_hinterlaesst_keinen_registereintrag` `:742`→`:762`, Prüfname `:764`: 3,0 dB, Abstand 17,0; die sieben Einspritzungen `:751-757` lassen alles unverändert.
- `hinter_dem_commit_punkt_wird_nicht_alloziert` `:785-805`, Schleife `:799`, Wertzeile `:801`, Prüfname `:817`: Slot `i%8`, also derselbe Slot bei i und i+8; Gain `(i%12)` → `((i+8)%12)`, Maximum 8,0 dB; Frequenz `(280+10i)/(200+10i)`, Maximum bei i = 0 = **1,4**. Abstände 12,0 dB und Faktor 1,4286 — nachgerechnet.
- dieselbe Prüfung, Rücknahmezweig `:806-811` (Undo, Undo, Redo, Remove, Neutralisieren, Revert — wörtlich diese sechs): höchstens 8,0 dB; Frequenz je Slot 590/510 = **1,1569** (≈1,16), Abstand Faktor 1,73 (≈1,7) — nachgerechnet.
- `apply_erzeugt_genau_eine_revision` `:832`→`:848`, Prüfname `:843` (Namensbildung `std::string (name) + "_erzeugt_genau_eine_revision"`): 3,0 dB, Abstand 17,0.
- Vorlauf Abschnitt E, `abschnitt` `:890`, `:891-897`, Schleife `:894`, Wertzeile `:896`, gespeichert `:900`: Gain `(double) i` von 1 bis 7, größter Schritt 1,0 dB, Abstand 19,0; gelesen wird nichts (erst der Ladestart danach).
- `wiederholung_unter_einer_zone_bekommt_nicht_user_schutz` `:1004`→`:1009`→`:1013`, Prüfname `:1019`: 1000 → 1050 → 500 Hz; größter Schritt 1050/500 = **2,1**, Abstand Faktor 1,05, **über**. Gelesen werden Zonenein-/-austritt, Ausgang, Revision, Hash (`:1015-1019`).
- `register_ist_nicht_teil_des_zustands` `:1042`→`:1043`, Prüfname `:1053`: 3,0 dB, Abstand 17,0.
- `verletztes_band_bleibt_bedienbar (M-69)` `:1273`→`:1282`→`:1284`, Prüfname `:1288`: Gain 3,0 → −4,0 = 7,0 dB; Frequenz 1000 → 950 = 1,0526; Abstände 13,0 dB und Faktor 1,90 — nachgerechnet.
- `zone_deaktivieren_ist_eine_transaktion (M-71)` `:1304`→`:1313`/`:1315`→`:1316`/`:1317`, `pruefe` `:1318`, Prüfname `:1320`: 1000 → 500 → 1000 Hz, **genau 2,0 in beiden Richtungen, Abstand 0, AUF dem Kriterium**; gelesen werden Ausgänge, Revision, verletzte Bänder (`:1318-1319`).
- `undo_ring_haelt_tiefe_32 (M-78)` `:1393-1397`, Schleife `:1394`, Wertzeile `:1396`, Prüfname `:1405`: `((i%24)−12)+0,5`; i = 23 → 11,5, i = 24 → −11,5, **23,0 dB**, Abstand 3,0, **über**.
- `neue_transaktion_schneidet_den_redo_zweig_ab (M-79)` `:1411`→`:1412`→`:1413`→Undo `:1414`→`:1416`, Prüfname `:1421`: 3,0 → 1,0 → 2,0 → (Undo) 1,0 → 9,0; größter Schritt 8,0 dB, Abstand 12,0.
- `abgeschlossener_gestus_ist_eine_revision (M-82)` `:2117`/`:2118` → Gestus `:2124-2135`, Prüfname `:2140`: Endwerte −11,0 dB und 1680 Hz (`:2138-2139`), also 14,0 dB und Faktor 1,68; Abstände 6,0 dB und Faktor 1,19. **Audio: keines** — zwischen `:2114` und `:2143` steht kein `fahreAudio`/`processBlock` (eigene Suche). Die Zeile selbst ist damit richtig eingeordnet; der Einordnungssatz darüber ist es nicht (Frage 2, D-1).
- `speichern_laden_speichern_bytegleich_in_v2` (Abschnitt `:653`): `SondeNullTestMain.cpp:202-203` = 1000 Hz / **12.0** dB, publiziert `:660`; `:662-663` = **−3.0** dB / q **0,7071067811865476**, publiziert `:664`; `pruefe` `:681`, Prüfname `:685`. 15,0 dB und Verhältnis 1,4142; Abstände 5,0 dB und Faktor 2,83 — nachgerechnet. Gelesen werden Bytegleichheit, Revision, Hash (`:681-683`).
- dieselbe Prüfung, Undo-Zweig `:662-663` → Undo `:665-669`: Rückrichtung, gleiche Werte.

Die einleitende Begründung zur Ebene `Stand` habe ich an der Quelle geprüft: `Stand` `:130-144` mit `bereiteVor` an `:139`, `fahre` `:156-161` ruft ausschließlich `st.tk->fuehreAus` und nie `verarbeite` — richtig.

**(iii) Der Absatz zu den zwei Fällen mit Abstand 0** (6961-6972) bindet an M-101 mit dem wörtlichen „**strikt größer** entscheidet: genau auf der Grenze bleibt der Slot auf dem Rampenweg"; M-101 (Zeile 6301) trägt genau diesen Satz. Der Entscheid aus §46.2 (8478-8485) ist inhaltlich vollständig wiedergegeben: Zahl bleibt 2,0, Begründung „kein Audio, Urteil liest weder Weg noch Ausgang", Abgrenzung zum Gainfall aus §43.2, Vorbehalt „zeigt der Messlauf C-0 einen Audiofall mit Abstand 0, entscheidet der Dirigent an der Messung neu". Die Rechnung 1000,0/500,0 = exakt 2,0 in `double` ist richtig (beide Werte exakt darstellbar, Quotient exakt).

**(iv) Schritt C-0 und M-144 tragen R-311-21.**
*Ort der Instrumentierung, an `DspKern.cpp` nachgemessen:* `vergebeKennungen` steht an `:261-292`; die `bleibt`-Bedingung an `:286-287` mit genau fünf Topologievergleichen (`typ`, `modus`, `dynamisch`, `nutztSvf`, `quelle`), Zuweisung `:288`. **Einziger Aufrufer** im ganzen Produktcode: `publiziereVorbau` an `:347` (`grep` über `eq-copilot/plugin/` ohne `tests/`); `publiziereVorbau` steht an `:315-363` (die Angabe des Nachtrags stimmt, §48 Nr. 3), aufgerufen aus `DspKern.cpp:299` (`uebernehmeZustand`) und `NakamaTransaktion.cpp:732`. `DspKern.h:440-444` sagt es wörtlich: „NUR der Worker, in `publiziereVorbau` … VOR der Uebergabe der Bank (M-52)". Kein Audiothread-Pfad erreicht die Stelle. Der Satz des Nachtrags stimmt.
*Bedingung der Zeile:* „die fünf Felder der `bleibt`-Bedingung, `DspKern.cpp:286-287`" — richtig; geändertes `freq_hz`, `q` oder `gain_db` — die drei Werte sind die wirksamen nach der Nyquistkappung (`DspProgramm.cpp:286-288`, geprüft: `b.freqHzWirksam = kappeNyquist (…)` an `:288`).
*Beine:* B6 = `tools/beweise.ps1:723`, B7 = `:728`, A16 = `:688`, B3c = `:702` — alle vier Kürzel an genau diesen Zeilen bestätigt.
*Rohdatei, Entscheidregel, Rücknahme, kein Produktcode:* wörtlich vorhanden (6858-6882, 7029-7031, Zeile M-144 an 6313).
*Rotbeweis — aktiv gegen Grünbleiben geprüft:*
  - Meldet die Instrumentierung **beide** 18,0-dB-Fälle? `uebernahme_nur_am_aeusseren_blockrand`: beide Programme laufen über `uebernehmeZustand` (`:4200` und `:4211` im Teilstückhaken) → `publiziereVorbau` → `vergebeKennungen`; Topologie identisch (lowShelf 8000 Hz, Kanalmodus Default), `m.gueltig` bleibt wahr (kein bankfreier Zustand dazwischen, `DspKern.cpp:319-330`), also `bleibt` wahr und die Zeile entsteht. Gegenprobe M-119: `setze` `:2070` und Kontrolltakt `:2082` publizieren beide über den Transaktionsweg; die Topologieschreibungen dazwischen wirken nicht (`:2078-2079`). Beide Fälle werden gemeldet.
  - Greift die 0,9-Schwelle? Mit mutierter Auswertungsschranke 17,0 ist 0,9·17,0 = 15,3 < 18,0 — beide Zeilen stehen in der Rohdatei.
  - Unterscheidet die Auswertung „mit Audio" von „ohne", und woran? Die Instrumentierungszeile selbst trägt kein Audiomerkmal. Die Unterscheidung leistet die Zuordnung zur ersten Tabelle von §40.3 (das ist die Audio-Tabelle) plus „samt der umgebenden Prüfausgabe" (6875). Das trägt: die Zusage von M-144 lautet „kein Bestandsfall **mit Audio über einem Blockrand** liegt auf oder über einem Kriterium", und mit der Schranke 17,0 liegen genau die zwei Zeilen der ersten Tabelle darüber. Der Rotbeweis fällt damit an der eigenen Zusage der Zeile, nicht an einem Nebeneffekt.
  - Kann die Instrumentierung einen rampenkompatiblen Wertwechsel übersehen, den das spätere Kriterium sähe? Nein: der Vergleichsgegenstand ist derselbe. Das Kriterium wird nach §40.3 („Die Stelle", 6995-7002) in dieselbe `bleibt`-Bedingung eingebaut und vergleicht denselben Merkzettel gegen dasselbe neue Programm. Der Merkzettel wird an `:291` je Publikation fortgeschrieben, auch bei verdrängten Zwischenpublikationen (Bestandsfall `311/M-41`, `DspGoldenTestMain.cpp:5286-5287`); der Candidate-Pfad hat seinen eigenen Merkzettel (`merkzettel[(size_t) p]`, `:240`), und die Zeile trägt den Pfad. Kein Unterschied.
  - Etikett: „heute nicht messbar" trifft §6 (Zeilen 515-517: „der Mechanismus oder das Urteil entsteht erst im Bau") — Messlauf und Auswertung entstehen in C-0. Richtig.
  - Änderungssatz und Reihenfolge: C-0 liegt am Basisstand von Satz C, braucht nur B6/B7/A16/B3c (bestehen alle) und die **vorgeschlagenen** Zahlen aus §40.3; die Konstanten im Code entstehen erst danach — das ist ausdrücklich so gewollt („vor dem Bau des Kriteriums", 6880). Nichts aus einem späteren Satz wird gebraucht. §40.0 Zeile C (6483) nennt C-0 als Satzanfang; „Tests und Beine" (7029-7032) sagt, M-144 bekommt keinen neuen Test im Bein und B3c läuft zusätzlich; „Rotbeweise nach §7.1" (7046-7049) trägt die Auswertungsmutation.
  - **Eine Lücke dabei, siehe L-1 unten**: R-311-21 nennt B7 als Bein, und B7 enthält `hinter_dem_commit_punkt_wird_nicht_alloziert` (`TransactionTestMain.cpp:817`), das ab Stufe S8 **null** Allokationen verlangt (`zaehleAbS8` `:777-779`, Haken gesetzt `:786`); S8 wird an `NakamaTransaktion.cpp:647` gemeldet und `publiziereVorbau` erst an `:701` gerufen — die Instrumentierung liegt also innerhalb des gezählten Fensters. Weder §40.3 noch M-144 sagen, dass sie allokationsfrei sein muss.

**(v) Gestrichene Wache und Blindheitssatz stehen wie in S-V1 (d).** §40.3 „Risiken und Grenze" (7080-7095): „**Abwehr ist der Messlauf C-0**"; der `git grep` „**entfällt als Vollständigkeitswache**" (§48 Nr. 5: ganz); der Zeilenvergleich bleibt „wo er anderes trägt (Muster M-82, M-83), **sieht aber ausdrücklich nicht**, was eine Prüfung ohne Zahlen in ihrer Ausgabe tut: die Gegenprobe an `TransactionTestMain.cpp:2085-2086`". An der Quelle geprüft: `:2085-2086` ist ein `pruefe`-Aufruf mit nur zwei Argumenten, ohne Detailtext — er gibt tatsächlich keine Zahl aus.

### V-2 — JA, geschlossen

**M-99** (Zeile 6299) nennt jetzt `DspGoldenTestMain.cpp:2057` „als Literal", dazu `:2540` mit `rVon` aus `:2536` und `:2652` über `machKern (0.0)` an `:2657`. Selbst nachgemessen, Signatur `machDynamisch (DspSatz&, int slot, double rangeDb, double thresholdDb, …)` an `:183`:

- `:2057` = `machDynamisch (s, 0, 0.0, -60.0);   // Range 0` — Literal, stimmt.
- `:2536` = `const double rVon = richtung == 0 ? -12.0 : 0.0, …`, `:2540` = `machDynamisch (sa, 0, rVon, …)` — für `richtung == 1` Range 0, stimmt.
- `:2652` = `machDynamisch (s, 0, range, …)` im Lambda `machKern` (`:2647`), `:2657` = `auto kRef = machKern (0.0);` — stimmt.
- Der Vermerk stimmt ebenfalls: `:5677` = `machDynamisch (basis, 5, -6.0, …)` (Range −6,0), `:6821` = `machDynamisch (vorher, 0, -12.0, …)` (Range −12,0).

**§40.4** nennt `:2118` (Zeile 7221: „`:2116` und `:2118` (je die `pruefe`-Zeile; der Prüfname der zweiten steht an `:2120`. Berichtigt in der Matrixnacharbeit 4, V-2: `:2119` ist nur die Fortsetzung der Bedingung)") — `:2119` kommt als Fundstelle nicht mehr vor.

**Jede im Fixdiff neu eingeführte Pfad:Zeile-Angabe in §38 bis §41 geprüft.** Ich habe die hinzugefügten Zeilen mit `git diff -U0` ausgelesen und jede Angabe der Form Datei:Zeile sowie jede fortsetzende `:Zeile` in den zwei Tabellen von §40.3 und den neuen Zeilen der zwei Listen von §40.4 an der Quelle gelesen (nicht aus §47.5 übernommen). Ergebnis: **alle stimmen**, mit einer Ausnahme (Härtung H-1):

- `DspKern.cpp`: `:261-292` (`vergebeKennungen`), `:286-287`, `:286-288`, `:315-363` + Aufruf `:347` (`publiziereVorbau`), `:515` (`bankNeu.baender = baenke.bank (alt).baender`), `:690-710` (Steuerratenzweig ab `if (z.schrittRest <= 0)`) — stimmen.
- `DspProgramm.cpp`: `:207-227` (`rampenKompatibel`), `:212` (`eqEngagiert`/`hardBypass`), `:222-223` (nur Topologiefelder), `:318` (`detektorLaeuft … && b.rangeDb != 0.0`), `:286-288` (Nyquistkappung), `:290-300` (statischer Entwurf) — stimmen.
- `tools/beweise.ps1`: `:688` A16, `:702` B3c, `:723` B6, `:728` B7 — stimmen.
- `CLAUDE.md:29-33` — „Grundgesetz: nichts Ungefragtes … kein Logging" steht genau dort.
- `DspGoldenTestMain.cpp`: `:1875`, `:1877`, `:2057`, `:4199`, `:5677`, `:6142-6165`/`:6145-6147` (M-37-Spiegel über die Ruhe), Helfer `machSatz` `:150`, `belege` `:171` — stimmen. Die neuen Listenzeilen `:1624`/`:1625-1626`/`:1606-1628`, `:1610`, `:1620`, `:1648`, `:1649`, `:1652`, `:1654`, `:1658`/`:1659`/`:1636-1660`, `:4277`, `:4375`/`:4376`, `:4387`, `:4399`/`:4401`, `:5274`, `:5278-5280`, `:5297`/`:5298`, `:5481`, `:5628`, `:5631`/`:5632`, `:5643`/`:5644` — alle an der Quelle bestätigt.
- `TransactionTestMain.cpp`: `:130-144`, `:139`, `:156-161`, `:253`, `:266`, `:276`, `:284`, `:398`, `:1114`/`:1115`, `:1179`/`:1181`, `:1541`/`:1542`, `:1585`/`:1587`, `:1722`, `:1795`, `:2069`, `:2085-2086`, `:2091`, `:2181-2196` mit `:2183`–`:2187`, `:2197`/`:2198`, `:2222`/`:2224`, `:2243`, `:2760`/`:2761`, `:2828`, `:3006`/`:3008`, `:3026-3077`, `:3063-3066`, `:3067-3074`, `:3102`/`:3103` — alle bestätigt.
- `SondeNullTestMain.cpp`: `:198` (`setzeHoerbaresBand`) mit „Kommentar `:197`" in der Helferliste — richtig; `:202-203`, `:653`, `:1129` (`mitBand`) — stimmen. `SchemaTestMain.cpp:1424` (`nak311AchtShelves`) — stimmt.
- Die Aussage „`belege` existiert nur in `DspGoldenTestMain.cpp`" (6889) habe ich nachgesucht — trifft zu (einzige Definition).

### V-3 / R-311-22 / R-311-23 — JA, geschlossen

**(i) Die Sammelzeile ist in fünf Zeilen aufgelöst** (7258-7262), je mit Prüfname, `pruefe`-Zeile und eigenem Grund; alle fünf an der Quelle gemessen: `undo_stellt_das_entfernte_band_als_ein_objekt_her (M-61)` `:1114`/`:1115` (Prüfling `:1095-1107`), `freier_slot_verarbeitet_nichts (M-58)` `:1179`/`:1181`, `dynamicwerte_ueberleben_aus_und_ein_bitgleich (M-24)` `:1541`/`:1542`, `klemmliste_erscheint_im_bericht (M-23, E2-7)` `:1585`/`:1587`, `311/M-40 remove_und_belegen_ohne_audio_startet_kalt` `:2760`/`:2761`. **Die zwei berichtigten Gründe stimmen an der Quelle:** `:1179-1180` prüft tatsächlich `gleich` (bitweiser Audiovergleich) **und** `auslenkung[2] == 0.0`, also Audio und Auslenkung statt „Register, Hash, Revision"; `:2760` liest `vorher[0] == -12.0`, also eine Absolutzahl der Dynamik nach gefahrenem Audio (Quadraturton `:2685-2700`-Bereich, Range −12,0 / Threshold −40).

**(ii) Die Zeile zu `311/M-77` und `311/M-78`** (7196) trägt beide Prüfnamen und die Fundstelle; an der Quelle: Namensbildung `:3063-3066` (`recall_in_neue_instanz_innerhalb_der_toleranz_pruefling_` bzw. `recall_nach_preparetoplay_innerhalb_der_toleranz_pruefling_`), `pruefe` `:3067-3074`, Prüfstand `:3026-3077`. Stimmt.

**(iii) `311/M-76` und `311/M-79`** stehen in der zweiten Liste (7263, 7264) mit den Gründen aus R-311-23: M-76 „zwei gleich gebaute Läufe gegeneinander" (Quelle `:3006`: `ausgangGleich && tapGleich` gegen den ununterbrochenen Vergleichskern) plus der Übernahme des `BandZustand` als Ganzes (`DspKern.cpp:515` — bestätigt); M-79 „Plateau am Quadraturton, Fixpunkt bitgenau" (Quelle `:3102`: `vorher == nachher && werte[0] == -12.0`). **Der Satz zu `311/M-80`** (7282-7287) stimmt an der Quelle: Prüfling `:2181-2196` belegt zwei statische Bänder (`:2183`, `:2184`), Width (`:2185`), Auto-Gain (`:2186`) und eine Zone (`:2187`) — kein `machDynamisch`, kein `nak311DynBand`; `pruefe` `:2197`, Prüfname `:2198`.

**(iv) Die acht weiteren Prüfungen** (7250-7257) stehen mit Prüfname, `pruefe`-Zeile und eigenem Grund. Jeden Grund am Testmaterial geprüft:
1. `dynamisches_<typ>_ruhe_gegen_rbj` `:1624`: `Sidechain::none` an `:1610` → `detektorLaeuft` falsch (`DspProgramm.cpp:318`); gemessen wird die Impulsantwort gegen RBJ (`:1613`, `:1620`), Schranken 0,05/0,1 (`:1624`). Grund trägt.
2. `dynamischer_high_shelf_bei_minus_24_db_gegen_rbj` `:1658`: `fahreStereoTon` an `:1648`/`:1649` (0,5 bei 250/1000/4000 Hz), Auslenkung exakt −12,0 (`:1652`), Sollkurve bei −24 dB (`:1654`), `gain_db` = −12 an `:1643`. Grund trägt; die Aussage „−24 dB sind −12 plus Auslenkung −12" ist belegt.
3. `im_regulaeren_betrieb_ist_jede_auslenkung_endlich` `:4375`: `alleEndlich && nichtEndlicheAuslenkungen() == 0`; dynamisches Band Slot 4 Range −6,0 an `:4277`. Grund trägt.
4. `nichtendliche_auslenkung_wird_null_und_gezaehlt` `:4399`: NaN als Range an `:4387`, geprüft `nachher[4] == 0.0 && ! std::signbit (…)` und Zähler `+ bloecke`. Grund trägt.
5. `311/M-41 verdraengte_zwischenpublikation_bricht_den_lebenszyklus` `:5297`: `vorherA[0] == -12.0` nach 48 000 Samples Quadraturton (`:5278-5280`), Range −12,0/Threshold −40 (`:5274`). Grund trägt; die Nebenrechnung „10-ms-Pol, τ = 480 Samples, hundertfach eingeschwungen" stimmt für 48 kHz.
6. `jede Publikation des Messlaufs kam durch (kein busy_retry)` `:5628`: `abgewiesen == 0`, acht dynamische Bänder Range −6,0 (`:5481`). Grund trägt.
7. `worst_case_vier_baenke_laeuft` `:5631`: `vierRechnend == stichproben`. Grund trägt.
8. `die Uebergangslast wurde gemessen, nicht gedeckelt` `:5643`: `mittel > 0.0 && isfinite (mittel) && isfinite (maximum)`. Grund trägt in der Sache (kein Deckel); die Formulierung „Endlichkeit und ‚größer null' von Mittel **und Maximum**" ist minimal weiter als die Quelle — „größer null" steht nur für `mittel`. Kein Befund, Härtung H-2.
**Der Satz über Abschnitt C ist berichtigt** (7272-7281): Abschnitt C ist aus der Sammelaussage herausgenommen, mit beiden Gründen (`:1659` falsch, `:1625` Umschreibung ohne Testnamen); G, H, H2, A16, B3c und B2 bleiben mit Begründung. Die A16-/B3c-Begründung habe ich nachgesucht: `SondeNullTestMain.cpp` und `SchemaTestMain.cpp` tragen je **null** Treffer für `kDynamicEnabled` und `kDynamicRangeDb`.

**(v) Keine Prüfung steht in beiden Listen.** Die erste Liste („Welche Bestandszeilen sich verschieben", 7188-7197) führt `:2432`, `:2434`, `:2510`, `:2356`, `:2291`, `:2630-2632` und `TransactionTestMain.cpp:3067-3074`; keine dieser Fundstellen und keiner dieser Prüfnamen kommt in den 15 + 28 Zeilen der zweiten Liste vor (Zeilenabgleich über alle Fundstellen beider Listen).

**(vi) Der Absatz „Die Messung der zwei Listen"** (7289-7303) und **M-128** (Zeile 6362) tragen R-311-22 wörtlich: vier Beine (B6 `:723`, B7 `:728`, B3c `:702`, A16 `:688` — alle bestätigt), vier Zielpfade (`…-b6-`, `-b7-`, `-b3c-`, `-a16-zeilenvergleich.txt`), Befundregel in beide Richtungen („jede abweichende Zeile gehört zu einer Prüfung der ERSTEN Liste … eine Prüfung der ZWEITEN Liste, deren Zeile abweicht oder die rot wird, ebenso") und der Satz über Prüfungen ohne Zahl in der Ausgabe. M-128 trägt die Messung in der Testspalte und nennt R-311-22 in der Quellspalte; sein Rotbeweis („das Mittel asymmetrisch gemacht → der eingeschwungene Wert wandert über den Fixpunkt hinaus → `kennlinie_im_knie_wirkt_im_audiopfad` reißt seine 0,02 dB") fällt am Gegenstand der Zeile — am Fixpunktargument, das die Liste trägt —, nicht an einem Nebeneffekt.

**(vii) Zählungen — selbst nachgezählt am Text:**
- §39.6: Summe 18 + 8 + 11 + 9 + 1 + 0 = **47**; M-96 bis M-120 (25) plus M-123 bis M-144 (22) = 47. Etiketten: heute rot **9** (M-96, M-97, M-98, M-99, M-112, M-120, M-124, M-132, M-135), heute nicht messbar **20**, Regressionswache **16**, zwei Hälften **2**; 9 + 20 + 16 + 2 = 47. Ich habe die vier Listen zu einer Menge vereinigt: sie deckt M-96 bis M-120 und M-123 bis M-144 **lückenlos und überschneidungsfrei** ab. Je Matrix-ID genau eine Zeile.
- §40.3: erste Tabelle **9** Zeilen (6916-6924), zweite Tabelle **22** Zeilen (6938-6959) — gezählt.
- §40.4: Abschnitt F **15** Zeilen (7217-7231), außerhalb **28** Zeilen (7237-7264) — gezählt; M-128 nennt genau „fünfzehn" und „achtundzwanzig".
- Kopfzeile „Etappe" (Zeile 8) nennt „neue Zeile M-144, 47 Matrixzeilen" — deckungsgleich.

---

## Frage 2 — Hat der Fixdiff etwas gebrochen?

**JA, an einer Stelle.**

### D-1 — §40.3, Absatz vor der zweiten Tabelle (Zeile 6933-6934): der Einordnungssatz zur Ebene `prozessor()` ist am Code falsch

**Quelle:** `eq-copilot/plugin/tests/TransactionTestMain.cpp:2114-2143` (Prüfung `abgeschlossener_gestus_ist_eine_revision (M-82)`), gegen `docs/beweise/NAK-311.md:6933-6934` und `:6957`.

**Welcher Satz bricht:** „Die Ebenen ab `prozessor()` (`:1722`) fahren dagegen echtes Audio — **ihre Paare stehen in der ersten Tabelle**."

**Reproduktion:** Der Prüfling `:2114-2143` legt seinen Prozessor mit `prozessor()` (`:2115`) an, publiziert 1000 Hz / 3,0 dB (`:2117`/`:2118`) und publiziert am Gestenende (`:2135`) den Endzustand −11,0 dB / 1680 Hz (`:2137-2139` verlangen `stateRevision() == r + 1`). Zwischen den beiden Publikationen steht **kein** `fahreAudio` und kein `processBlock` — der Block enthält keinen einzigen Audiolauf. Das Paar steht folgerichtig in der **zweiten** Tabelle (`docs/beweise/NAK-311.md:6957`), also genau dort, wohin der zitierte Satz es zu stellen verbietet. Der Satz ist damit eine durch Lesen behauptete Einordnungsregel, die die eigene Tabelle derselben Fassung widerlegt.

**Umfang:** genau diese eine Teilaussage. Keine Zeile der zwei Tabellen ist davon betroffen, die Zeile `:6957` ist in allen Spalten richtig, und die Entscheidregel des Messlaufs C-0 hängt nicht an dem Satz. Die Zusatzbedingung „fährt ein Fall mit Abstand 0 Audio über einen Blockrand" (6879) ist für dieses Paar folgenlos (14,0 dB und Faktor 1,68, beide unter dem Kriterium).

### Alles Übrige: kein Bruch

- **Gate-Text** (Kopf Zeile 6) und §39-Kopfregel (6270-6279): „Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nie an einem Nebeneffekt" — M-144 erfüllt das (siehe V-1 (iv)), M-128 ebenso.
- **CLAUDE.md-Invarianten:** M-144 verlangt einen Dateizugriff in `vergebeKennungen`; die Stelle liegt nachweislich außerhalb des Audio-Threads (einziger Aufrufer `publiziereVorbau`, S8 des Workers, unter dem Zustandsschloss — `DspKern.h:175-184`, `:440-444`). Kein Bruch von „kein Logging im Audio-Thread"; die Zeile zitiert `CLAUDE.md:29-33` selbst als Quelle. Kein Produktcode bleibt, keine neue Stateversion, keine v3-Vertragsänderung, Identität unberührt; RBJ- und Stereo-Goldens (M-82, M-83) bleiben Bezug (7064-7066). Bitidentität und Passthrough sind nicht berührt (keine Zeile ändert einen Audiopfad).
- **Regeln R-311-1 bis R-311-23:** R-311-13 (M-100, M-101 bis M-103, M-142) unverändert getragen; der Entscheid zu `kSprungGainDb` = 20,0 aus §43.2 steht unverändert in F-20 (7466-7473); der Entscheid §46.2 zum Frequenzkriterium ist richtig wiedergegeben; R-311-8 (M-94 (b)) wird im Absatz „Was nicht Kriterium ist" unverändert weitergeführt (6987-6993).
- **Unberührte Matrixzeilen:** M-100 bis M-104 geprüft (M-101 „strikt größer" trägt den Randfall, M-103 nennt jetzt zwei 18,0-dB-Fälle mit richtigen Fundstellen), M-127, M-128 (erweitert, siehe V-3 (vi)), M-142 (Wertwache über 6,0 dB und Verhältnis 3,0 — an `:1875`/`:1877` nachgemessen). Kein Widerspruch.
- **SONDE-015:** die zitierten Zusagen M-17, M-25, M-26, M-84, M-121 sind in der Quellspalte, nicht als eigene Zeilen (§39 Kopf 6227-6229) — die Regel hält.
- **Änderungssatzfolge:** C-0 braucht nichts aus einem späteren Satz (siehe V-1 (iv)); M-128s Messung liegt in Satz D und vergleicht nur vorher/nachher desselben Satzes. §40.0 (6486-6492) bleibt schlüssig.
- **Je Matrix-ID genau eine Zeile:** geprüft über die Partition in §39.6 (siehe V-3 (vii)).

---

## Lücken

**L-1 — Die Regeln schweigen dazu, ob die temporäre Instrumentierung des Schritts C-0 allokationsfrei sein muss.** R-311-21 nennt B7 als eines der vier zu fahrenden Beine. B7 enthält `hinter_dem_commit_punkt_wird_nicht_alloziert` (`eq-copilot/plugin/tests/TransactionTestMain.cpp:817`), das über 47 Commits **null** Allokationen ab Stufe S8 verlangt; der Zähler wird vom Stufenhaken `zaehleAbS8` (`:777-779`, gesetzt `:786`) eingeschaltet, sobald S8 gemeldet ist (`eq-copilot/plugin/state/NakamaTransaktion.cpp:647`), und `publiziereVorbau` — und damit `vergebeKennungen` (`DspKern.cpp:347`) — läuft danach (`:701`). Eine Instrumentierung, die dort einen String baut oder einen Stream öffnet, macht diese Prüfung im C-0-Lauf rot. §40.3, M-144 und §7.1 sagen dazu nichts; §7.1 verlangt nur, mitgefallene Prüfungen zu protokollieren, was für einen **Messlauf** (nicht Rotbeweis) kein Ersatz ist. Die Zusage von M-144 bleibt tragfähig — die Messzeilen entstehen unabhängig davon —, aber der Lauf ist ohne diese Bedingung nicht sauber. Benannt, keine Nacharbeit.

## Härtungen

**H-1 — `SondeNullTestMain.cpp:197` in der Tabellenzeile.** Die Zeile `speichern_laden_speichern_bytegleich_in_v2` (6958) nennt „über `setzeHoerbaresBand` `:197`"; die Funktion steht an `:198`, `:197` ist ihre Doku-Kommentarzeile. Die Helferliste desselben Abschnitts (6903) nennt es korrekt („`:198`, Kommentar `:197`"). §47.9 Nr. 2 führt `:197` unter den fünf berichtigten Angaben — an dieser einen Stelle ist die Berichtigung nicht angekommen. Alle sachtragenden Fundstellen der Zeile (`:202-203`, `:660`, `:662-663`, `:664`, `:681`, `:685`) stimmen. Von keiner Zusage verlangt, sinngemäß von §48 Nr. 1 gedeckt.

**H-2 — Formulierung des achten neuen Grundes.** Zeile 7257 sagt „geprüft werden Endlichkeit und ‚größer null' von Mittel und Maximum"; an der Quelle (`DspGoldenTestMain.cpp:5643`) gilt „größer null" nur für `lastUebergang.mittel`, das Maximum wird nur auf Endlichkeit geprüft. Die Sache des Grundes (Lastzahl ohne Deckel) stimmt.

**H-3 — Post-mortem des gestrichenen `git grep`.** Zeile 7088-7091 sagt, der Grep habe „das Paar `:1304` → `:1317` **gar nicht**" gefunden. An der Quelle tragen `:1313` und `:1316` beide `param::kFreqHz`; nicht auffindbar ist nur der Ausgangswert aus `grundzustand` (`:1304`), also die erste Richtung 1000 → 500. Für die zweite Richtung 500 → 1000 wäre derselbe Maßstab „nur halb" bis „ganz". Die Wache ist per Entscheid (§48 Nr. 5) ohnehin gestrichen; der Satz ist Historie, keine Zusage.

---

## Geprüft / nicht geprüft

**Selbst an der Quelle nachgemessen** (Datei gelesen, Zeile für Zeile verglichen): alle 9 Zeilen der ersten und alle 22 Zeilen der zweiten Tabelle von §40.3, je auf Fundstelle, Werte, Differenz beziehungsweise Verhältnis, Abstand, Lage, Audio ja/nein und was die Prüfung liest; die Rampenkompatibilität der zwei neuen Audio-Paare gegen `DspProgramm.cpp:207-227`; die Fundstellen von M-99 samt dem Vermerk zu `:5677`/`:6821`; `:2118` in §40.4; alle 5 aufgelösten Sammelzeilen, die 8 neuen `DspGoldenTestMain`-Zeilen, die 2 R-311-23-Zeilen und der `311/M-80`-Satz mit ihren Gründen; die Helferliste aller vier Testdateien; die vier Beinkürzel in `tools/beweise.ps1`; der Aufrufergraph von `vergebeKennungen` und `publiziereVorbau` über den ganzen Produktcode; die Stufenhaken- und Allokationsmechanik von B7; `CLAUDE.md:29-33`; die Abwesenheit dynamischer Bänder in `SondeNullTestMain.cpp` und `SchemaTestMain.cpp`; die Abwesenheit eines Audiolaufs im Gestus-Prüfling.

**Selbst nachgerechnet** (ohne Interpreter, im Kopf/schriftlich): 23,0 dB aus `(i%24)−12+0,25` und aus `((i%24)−12)+0,5` beim Umlauf i = 23 → 24; 1050/500 = 2,1; 280/200 = 1,4; 590/510 ≈ 1,157; 2,0/1,4 ≈ 1,43; 2,0/1,0526 ≈ 1,90; 4,0/1,4142 ≈ 2,83; 2,0/1,68 ≈ 1,19; 1000,0/500,0 = exakt 2,0; alle Abstände der zwei Tabellen; die Summen und die Partition der Etikettenzählung in §39.6; die Zeilenzahlen 9, 22, 15, 28, 47.

**Nur gelesen, nicht nachgemessen:** §47 als Bericht des Bauers (als Wegweiser benutzt, jede darauf gestützte Aussage an der Quelle nachgemessen); die Zahlen aus Phase 15 und Phase 16 (+0,02 dB, +17,41 dB, +22,20 dB) und aus §44 (31,70/31,75/31,83/31,71 ms), die einen Lauf brauchen; die Rohberichte der Matrixprüfung 5 und ihrer Validierung, soweit sie Befundwortlaut liefern; §42, §43, §45, §46, §48 als Entscheidquellen; die Prüfnamenzeilen von Bestandszeilen außerhalb des Fixdiffs.

**Nicht Prüfgegenstand** (Auftrag): unberührte Zeilen und Sätze in §38 bis §41; dass eine Tabelle oder Liste einen weiteren Bestandsfall nicht nennt; H-1 bis H-4 der Matrixprüfung 4 und H-1 bis H-3 der Matrixprüfung 5; §47.9 Nr. 1, 3, 5, 7, 8; pruefe-Zeile gegen Prüfnamenzeile innerhalb desselben Aufrufs; die Zahlen 2,0/4,0/20,0/24,0/10,0 als Vorschläge; der Entscheid zum Frequenzkriterium als solcher; Prosa, Zeilenverweise und Historie außerhalb der berührten Zusagen; §47 selbst als Text; U44 bis U47 und U54; T3-12-*; `tools/dirigent/pruefliste.md`.

**Kein Prozess gestartet:** kein Compiler, kein Test, kein Bein, kein FL Studio, kein Python-Interpreter, kein Repo-Skript. Nur lesende Kommandos (`git rev-parse`, `git status`, `git diff`, `rg`/`grep`, `awk`/`sed` zum Anzeigen von Zeilenbereichen).

**HEAD am Ende:** `1427d43cd82036736f0245321a6cd18b23452bf5` — gleich dem Wert zu Beginn.
