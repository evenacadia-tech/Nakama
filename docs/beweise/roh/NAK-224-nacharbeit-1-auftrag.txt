NAK-224 — S25c: Nacharbeit Runde 1 (Budget: Runde 1 von 3). Auftrag des Dirigenten (2026-09-09) nach der Erstprüfung (Codex gpt-6-astra max, Thread 01a08674-5ab8-7841-8469-eb52429fef6d, HEAD 61c990b3, URTEIL NEEDS_WORK, Volltext docs/beweise/roh/NAK-224-erstpruefung-61c990b.txt).

DU BIST der frische Opus-Bauer (max) dieser Runde. Repo C:\Users\phili\Projekte\Nakama, Branch master. Stand vor der Runde = `git rev-parse HEAD` beim Start; trage ihn in §6 des Manifests docs/beweise/NAK-224.md als Kopf des Abschnitts „Nacharbeit Runde 1“ ein. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; ein Commit je Defekt; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie Backticks in `-m`. Keine Neben-Refactors, nichts außer den zwei Defekten unten. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest nennen, wo jede Zeile gemessen ist. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder dem rekursiven Unix-Löschbefehl; selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. Je Zeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Kein voller Kanon in dieser Runde — nur die betroffenen Beine; der Abschlusskanon folgt nach der Wiederprüfung.

DEFEKT D1 (Prüfer, wörtlich): „[P2] Ziehe A29s Quellliste beim Modulschnitt mit — broker/src/coordinator/hypothese/mod.rs:50-53. DEFEKT gegen R2/A29: Nach einer gewöhnlichen Änderung an einem neuen Hypothesenmodul akzeptiert A29 weiterhin alte P5-Ergebnisse. `tools/eq-copilot/pruefe_p5_korpus.py::QUELLEN` nennt noch die gelöschte `hypothese.rs`; `_voraussetzung()` überspringt sie mittels `is_file()`. Die lesende Zeitstempel-Gegenprobe bestätigt: Eine neuere ursprüngliche Datei wird abgewiesen, eine neuere `hypothese/befund.rs` nicht. Reproduktion der verlorenen Abhängigkeit: `python -B -c "import runpy; m=runpy.run_path('tools/eq-copilot/pruefe_p5_korpus.py'); assert all(p.is_file() for p in m['QUELLEN'])"`. Die Quellenliste muss alle neuen Rechenmodule berücksichtigen, damit der Nachweis weiterhin den aktuellen Stand betrifft.“
Vom Dirigenten an der Quelle bestätigt: `tools/eq-copilot/pruefe_p5_korpus.py`:53 nennt `broker/src/coordinator/hypothese.rs`; `:431-432` prüft `quelle.is_file() and quelle.stat().st_mtime > stand` und lässt eine fehlende Quelle still aus.
REGEL D1: `QUELLEN` nennt statt der gelöschten Datei alle Rust-Quellen unter `broker/src/coordinator/hypothese/` (rekursiv, zur Laufzeit aufgezählt, sortiert) und weiterhin `hypothese_verdrahtung.rs`; eine in `QUELLEN` genannte, aber fehlende Datei ist ab jetzt eine fehlende Voraussetzung des Beins (Exit 3 mit Nennung des Pfads), kein stilles Überspringen — genau der Mechanismus, der den Verlust verdeckt hat. Die Frischeprüfung selbst (Ergebnisdatei jünger als jede Quelle) bleibt unverändert. Kommentarverweise in derselben Datei auf `hypothese.rs`:Zeilen (`:65`, `:303`) dürfen als Pfadänderung auf das neue Modul und seine Zeilen zeigen; Pflicht ist das nicht.
ROTBEWEIS D1 (Datei docs/beweise/roh/NAK-224-rot-d1.txt, Rohausgabe beider Läufe): (a) Ergebnisdatei `eq-copilot/build/p5-korpus-ergebnis.json` frisch erzeugen (`cargo test --manifest-path broker/Cargo.toml --test sonde014_p5_korpus`), A29 grün; dann den Zeitstempel von `broker/src/coordinator/hypothese/befund.rs` auf jetzt setzen (`(Get-Item …).LastWriteTime = Get-Date`, keine Inhaltsänderung) → A29 muss Exit 3 „Voraussetzung fehlt“ melden; Ergebnisdatei erneut erzeugen → grün. (b) Vor dem Fix reproduziert dieselbe Folge das Fehlverhalten (grün trotz jüngerer Quelle) — einmal am Stand vor der Runde messen und zitieren. (c) Der Reproduktionsbefehl des Prüfers (`assert all(p.is_file() …)`) läuft nach dem Fix ohne AssertionError. Betroffene Beine: A28 und A29 über den Runner-Einzelaufruf oder direkt (`py -3.13 tools/eq-copilot/erzeuge_p5_korpus.py --pruefen`, `py -3.13 tools/eq-copilot/pruefe_p5_korpus.py`).

DEFEKT D2 (Prüfer, wörtlich): „[P2] Schütze die neuen server-Imports mit cfg(windows) — broker/src/lebenslauf.rs:21-21. DEFEKT gegen den verhaltensneutralen Schnitt: Auf Nicht-Windows-Zielen wird jetzt bedingungslos `crate::server` importiert, obwohl `lib.rs` dieses Modul ausschließlich unter `#[cfg(windows)]` deklariert. Derselbe Fehler steht in `probe.rs:14`. Damit scheitert bereits der Bibliotheksbau mit E0432, statt den vorhandenen Nicht-Windows-Rückweg von `broker_starten` bereitzustellen. Reproduktion mit installiertem Linux-Target: `cargo check --manifest-path broker/Cargo.toml --lib --target x86_64-unknown-linux-gnu`. Beide `server`-Imports benötigen dieselbe Windows-Bedingung wie zuvor ihre Verwendungen; der Fehler wurde hier statisch geprüft, nicht cross-kompiliert.“
Vom Dirigenten an der Quelle bestätigt: am Basis-SHA stand `#[cfg(windows)] mod server;` in `lib.rs`:18-19, und jede `server::`-Verwendung lag in einem `#[cfg(windows)]`-Block (`:919-948`, `:1200`); der Nicht-Windows-Rückweg stand unter `#[cfg(not(windows))]` (`:989`, `:1088`, `:1123`, `:1144`). Am Endstand importieren `lebenslauf.rs`:21 und `probe.rs`:14 `crate::server` ohne Bedingung.
REGEL D2: beide `use crate::server…`-Zeilen tragen `#[cfg(windows)]` — dieselbe Reichweite, die die Verwendungen vorher hatten; keine weitere Änderung an Rückwegen oder Bedingungen. Prüfe zusätzlich jede andere neue Modulwurzel dieses Tickets (`register.rs`, `sitzung.rs`, `paar.rs`, `zeit.rs`, `testhilfe.rs`, `hypothese/*`, `server_v3/tests_*`) auf `use`-Zeilen zu Items, die im Bestand nur unter einer `cfg`-Bedingung existieren; ein weiterer Treffer wird nach derselben Regel behandelt und im Manifest genannt.
ROTBEWEIS D2 (Datei docs/beweise/roh/NAK-224-rot-d2.txt): Linux-Target hinzufügen (`rustup target add x86_64-unknown-linux-gnu`, nur Standardbibliothek, kein Linker nötig für `cargo check`); dann `cargo check --manifest-path broker/Cargo.toml --lib --target x86_64-unknown-linux-gnu` (a) am Stand vor der Runde: E0432 für `crate::server` in `lebenslauf.rs` und `probe.rs` zitieren; (b) nach dem Fix: dieser Fehler ist fort. Falls der Bibliotheksbau auf dem Linux-Target am Basis-SHA `090954fa` (lokale Arbeitskopie `C:/nk224` steht auf ihm) aus anderen Gründen scheitert, ist die Zusage: die Fehlermenge am Endstand ist gleich der am Basis-SHA — beide Listen (`error[…]`-Zeilen, sortiert) in die Rotbeweisdatei, Differenz leer. Lässt sich das Target nicht installieren (offline), gilt der statische Beweis (grep: jede `server`-Verwendung unter `cfg(windows)`) und der Umstand steht als Nebenbefund im Manifest. Betroffene Beine: `cargo build`, `cargo test --lib` (Windows, weiter 0 Warnungen) und A4 nicht nötig.

MANIFEST: docs/beweise/NAK-224.md — Kopftabelle: Zeile „Phase“ um „Nacharbeit Runde 1 abgeschlossen (Datum): D1, D2 geschlossen — §6“ ergänzen; §6 neuer Abschnitt „Nacharbeit Runde 1“ mit Stand vor der Runde, je Defekt Regel, Umsetzung (Datei:Zeile), Commit, Test und Rotbeweis, dazu die Prüfliste; Riegelkarte R2 um die neue Quellliste (Zeile), R5 um die geänderten Pfade ergänzen. Den lebenden Kopf nicht umschreiben, nur ergänzen; Verlauf append-only. Rundenbilanz `py -3.13 tools/dirigent/rundenbilanz.py <Stand vor der Runde>..HEAD` ins Manifest.

ABSCHLUSS: zwei Commits (D1: `tools/eq-copilot/pruefe_p5_korpus.py` + Rotbeweisdatei; D2: `broker/src/lebenslauf.rs`, `broker/src/probe.rs` [+ weitere Treffer] + Rotbeweisdatei) und ein Manifest-Commit, alles gepusht, Worktree sauber. Melde dich erst, wenn `git status --short` leer ist und `git rev-parse HEAD` gleich `git rev-parse origin/master` ist.
