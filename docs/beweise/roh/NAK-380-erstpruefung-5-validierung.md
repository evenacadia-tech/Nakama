# NAK-380 Erstprüfung 5 — Validierung der Befunde D1 bis D3 an der Quelle

- **Ticket:** NAK-380, Etappe 5 (Kohärenz je Bin über Frames, DSP-23 mit NAK-177), Planschritt S25q.
- **HEAD Beginn:** `8e81885e588d26dfd552735a5f8716468c09bc14` (26.09.2026 04:09:04 Uhr, `git rev-parse HEAD`).
- **HEAD Ende:** `8e81885e588d26dfd552735a5f8716468c09bc14` (26.09.2026 04:22:48 Uhr, gemessen nach dem Schreiben dieser Datei; identisch).
- **Datum:** 26.09.2026.
- **Modell:** Opus 5.5 (`claude-opus-5-5`), Effort xhigh laut Auftrag; frischer lesender Validierer, weder Bauer noch Nacharbeiter noch Prüfer.
- **Gegenstand:** Prüfbericht der Erstprüfung 5 (Codex `gpt-6-astra` xhigh, Thread `01a0db6c-6bf5-7cf3-9e23-14272eb0f30e`, Vorlage A, URTEIL NEEDS_WORK, 3 Defekte), Wortlaut `docs/beweise/roh/NAK-380-erstpruefung-5-urteil.md`, über `git diff c43c1646...8e81885e -- broker eq-copilot tools`.
- **Prüfform:** nur lesend (Read, Grep; `git rev-parse`, `git show`, `git diff`, `git status`). Kein Bau, kein Testlauf, kein Python, kein FL. Laufzahlen stammen aus den genannten Rohdateien und sind als „gelesen“ gekennzeichnet; Code- und Matrixzeilen sind am HEAD gemessen. Während des Laufs erschien `M eq-copilot/install/nakama-installer-v1.json` (Kanon des Dirigenten, nicht Gegenstand, nicht angefasst).

Zeilenangaben ohne Datei im Manifest beziehen sich auf `docs/beweise/NAK-380.md` am HEAD (die Matrix §6.4 steht dort unverschoben in `:964-1007`).

## Teil 1 — je Befund

### D1 — M-75 bis M-79 prüfen nur den letzten Snapshot

**a) Maßstab, wörtlich.**

- M-73 (`:982`), Spalte Ereignis: „jeder Evidenzsnapshot ab dem siebten (Ring beider Stufen voll)“; Zusage: „jedes Band 30 Hz bis 18 kHz mit Kohärenzbit trägt Kohärenz ≥ 0,95 (Referenz ρ_w(48)² = 0,998 bei N = 4096 und 0,9999 bei N = 16 384, nachgerechnet); jedes Band mit Energie trägt ein Bit“; Reihenfolge: „`stereoAuswerten` nach `fuelleEvidenz`“; Test: „C++: B19 NEU `380/M-73 laufzeit_1ms_breitband_48k`“; Rotbeweis: „heute rot: 0,477 bei 15,77 kHz (39 Bins) … nach dem Bau Mutation Kohärenz aus der über Bins summierten Kreuzleistung (`Stereo.h:229-230` alter Weg) → 0,477 → rot“; Etikett heute rot.
- M-74 (`:983`), Ereignis: „je Blockgröße der letzte Snapshot nach 10 s“.
- M-75 (`:984`), Spaltenfolge `| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | …` (`:980`): Vorbedingung „§7 D1 (d = 44), Blöcke 64, 512, 16384“; **Ereignis „wie M-73“**; Zusage „jedes Band mit Bit hat Kohärenz ≥ 0,95 (Referenz ρ_w(44)² = 0,9985 bei N = 4096) und jedes Band mit mindestens zwei Bins Gruppenlaufzeit 1000·44/44 100 = 0,997732 ms ± 0,02 ms, beides nachgerechnet“; Reihenfolge ebenfalls „wie M-73“; Test „C++: B19 NEU `380/M-75 laufzeit_1ms_44k1`“; Rotbeweis „heute rot (bei Blöcken ≤ 512 gar kein Hauptstufenbit, M-82); nach dem Bau Mutation Laufzeitdivisor `fs` → 48 000 …“; Etikett heute rot. „wie M-73“ steht also in **beiden** Spalten, Ereignis und Reihenfolge.
- M-76 bis M-79 (`:985-988`): Ereignis und Reihenfolge je „wie M-73“, Zusage Kohärenz ≥ 0,95 (Referenzen 0,9939 / 0,9928 / 0,9760 / 0,9715 vor Etappe 6) und Gruppenlaufzeit.
- Fassung §47.3 (`:5304`): „M-74 bis M-79 \| Laufzeithälfte: „± 0,02 ms“ → „innerhalb T_B(K_eff, F, C, Δf) nach R-380-13 (iii), aus den Vorbedingungen gerechnet; Bänder mit einem genutzten Bin ohne Laufzeitbit“; **Kohärenzhälfte unverändert**; Rotbeweis der Laufzeithälfte: Δf aus 48 000 statt fs (unverändert)“. Die Ereignisspalte ändert §47.3 nicht; §49.2 (`:5783-5785`) ändert nur M-80 und den Rotbeweis von M-74.
- M-80 (`:989`), Ereignis: „letzter Snapshot jedes Laufs“.
- R-380-13 (iii) (`:5267-5269`): „5σ, weil ein Lauf 70 bis 134 Bänder und die Etappe 33 Läufe prüft (rund 4 200 Bandprüfungen; Bonferroni bei 1 % je Etappe verlangt 4,7σ)“.
- Präzisierung „Zusage je Element“ (`docs/beweise/roh/NAK-380-etappe-5-auftrag.txt:25`): „sagt eine Zeile „jedes Band“, „je Blockgröße“, „alle Bassbänder mit Energie“, „je Rate und Block“ oder „alle Bänder mit Kohärenzbit“ zu, prüft der Test jedes Element, keine Stichprobe (Lehre D2, Z1 aus §24)“; Lehren `docs/beweise/NAK-380-verlauf.md:1505` (D2 „gilt je belegtem Band“) und `:1507` (Z1 „M-02 ‚je Gruppe g‘“).

**b) Nachmessung am HEAD** (`eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp`).

- Der Lauf speichert jeden Snapshot: `:547-557` (`ziel->schnapp.push_back (std::move (s));` im `jeSnapshot`-Rückruf). Die Daten für eine Prüfung ab Snapshot 7 liegen also vor.
- `nak380KohPruefen (const Nak380Rauschlauf& L, int i, Nak380KohBefund& f)` (`:610-644`) nimmt den Snapshotindex: `const auto& s = L.schnapp[(std::size_t) (i - 1)];` (`:612`), Freiheitsgrade je Snapshot aus dem Kadenzmodell `nak380RingFrames (L.snapSoll, i, bf.punkte(), bf.ring())` (`:623`).
- `nak380LaufzeitKohaerenz` (`:827-848`), Kommentar `:822`: „Kohaerenzzusage eines Laufzeitlaufs am letzten Snapshot (M-74 bis M-79)“; Aufruf `:831-832`: `if (! L.schnapp.empty()) nak380KohPruefen (L, (int) L.schnapp.size(), f);`; Prüftext `:835`: „letzter Snapshot - jedes Band mit Energie traegt ein Bit, jedes Band mit Bit Kohaerenz >= 0,95; Freiheitsgrade nach Kadenzmodell“.
- `nak380LaufzeitPruefen` (`:733-797`): `const auto& s = L.schnapp.back();` (`:739`); `nak380LaufzeitWert` (`:850-876`) ruft sie ohne Index (`:853`).
- `nak380Toleranz` (`:692-713`): F nur am letzten Snapshot, `t.f = nak380RingFrames (L.snapSoll, (int) L.snapSoll.size(), bf.punkte(), bf.ring());` (`:699`).
- M-73 (`:878-920`) prüft Snapshots 7 bis n: `for (int i = 7; i <= (int) L.schnapp.size(); ++i) { nak380KohPruefen (L, i, f); ++geprueft; }` (`:886-890`), dazu die Vorbedingung `geprueft + 6 == (int) L.snapSoll.size()` (`:900`). §46.4 (`:4684-4686`, gelesen): „M-73 (48 kHz, Block 512, Snapshots 7 bis 31)“.
- M-74 (`:922-932`): je Blockgröße `nak380LaufzeitKohaerenz` und `nak380LaufzeitWert` — letzter Snapshot, wie die Matrix verlangt.
- M-75 bis M-79 (`nak380Rate`, `:934-943`, aufgerufen `:1562-1566`): dieselben zwei Helfer je Block 64, 512, 16 384 (`:940-941`) — **nur der letzte Snapshot**, für Kohärenz- und Laufzeithälfte.
- M-80 (`:952-978`): `nak380LaufzeitWert` und `nak380LaufzeitPruefen` am letzten Snapshot — deckt sich mit „letzter Snapshot jedes Laufs“ (`:989`); **M-80 trägt die Lücke nicht**.
- §46.5 beschreibt die Prüfzeile der Kohärenzhälfte von M-75 als „`Sonde013StereoGoldenTest.cpp:688` und `:696` „380/M-75 laufzeit_1ms_44k1“ je Block 64, 512, 16 384, B19“ (`:4741`), ohne Snapshotangabe; §48.5 (`:5554-5556`, `:5562`) nennt „je Zeile dieselben fünf Sätze mit denselben Prüfzeilen“, Kohärenz „(`:833`, `:841`)“ — das ist der Prüftext „letzter Snapshot …“ (`:835`).
- **Machbarkeit Kohärenzhälfte ab Snapshot 7:** ohne Produktänderung, wie M-73 (`:886-890`) — Schleife über `nak380KohPruefen (L, i, f)`; die Freiheitsgrade je Snapshot kommen schon aus dem Kadenzmodell (`:623`).
- **Machbarkeit Laufzeithälfte ab Snapshot 7:** technisch ja (`nak380Toleranz` und `nak380LaufzeitPruefen` bekämen einen Snapshotindex; F je Snapshot aus `nak380RingFrames (L.snapSoll, i, …)`), aber die 5σ von R-380-13 (iii) sind auf „rund 4 200 Bandprüfungen“ bemessen (`:5267-5269`). Nachgerechnet aus dem Kadenzmodell (`:320-347`): je Lauf 21 bis 27 Snapshots ab dem siebten (Block 64: 27; Block 512: 25 bzw. 26; Block 16 384: 21 bzw. 23), mit den Bandzahlen aus §46.4 (134 / 97 / 94 / 73 / 70) ergibt die Laufzeithälfte M-75 bis M-79 rund 34 700 Bandprüfungen, zusammen mit M-74 rund 37 000. Bonferroni bei 1 % verlangt dafür rund 5,15σ statt 4,7σ; aufeinanderfolgende Snapshots teilen 2/3 (Hauptstufe) bzw. 6/7 (Bass) ihrer Ringdaten und sind nicht unabhängig, und bei K = 2 und 3 unterschätzt die Formel die Streuung um bis zu 15 % (§49.1, `:5773`). Ob 5σ je Snapshot trägt, ist damit von der Regel nicht hergeleitet.
- Ob die Kohärenz bei 176,4 und 192 kHz an **jedem** Snapshot ab dem siebten ≥ 0,95 hält, ist nicht gemessen (am letzten Snapshot Minimum 0,9625 bis 0,9676 gegen Referenz 0,9760 / 0,9715, §46.4 `:4675-4680`, gelesen).

**c) Was bricht.** Kohärenzhälfte M-75 bis M-79: die Ereignisspalte „wie M-73“ = „jeder Evidenzsnapshot ab dem siebten (Ring beider Stufen voll)“ (`:982`, `:984-988`), von §47.3 ausdrücklich unverändert gelassen (`:5304` „Kohärenzhälfte unverändert“), und die Präzisierung „Zusage je Element … keine Stichprobe“ (`NAK-380-etappe-5-auftrag.txt:25`). Laufzeithälfte: dieselbe Ereignisspalte steht gegen R-380-13 (iii), deren 5σ-Bemessung (`:5267-5269`) einen Snapshot je Lauf voraussetzt; die Regel sagt nicht, welcher Snapshot gilt.

**d) Urteil: PRÄZISIERT.** Kohärenzhälfte **DEFEKT** (Vorbedingung/Ereignis und „je Element“ nicht abgebildet; derselbe Helfer wie M-74 übernimmt dessen Ereignis „letzter Snapshot“). Laufzeithälfte **LÜCKE** (Widerspruch Matrix-Ereignis ↔ Multiplizitätsbasis von R-380-13 (iii); die Regel entscheidet der Dirigent). Kein Ausschluss greift: kein docs-Befund, die Ereignisspalte ist nicht durch §47.3/§49.2 ersetzt, keine angenommene Abweichung in §46.9/§48.9 behandelt die Snapshotwahl.

**e) Schließende Regel.** Die Kohärenzhälfte von M-75 bis M-79 wird wie M-73 an jedem Snapshot ab dem siebten geprüft (jedes Band mit Energie mit Bit, jedes Band mit Bit ≥ 0,95, Freiheitsgrade je Snapshot aus dem Kadenzmodell, Vorbedingung „Snapshots 7 bis n“ verriegelt), die Laufzeithälfte bindet der Dirigent per Matrixfassung entweder an den letzten Snapshot (wie M-74 und M-80, deckungsgleich mit der 5σ-Bemessung) oder an jeden Snapshot ab dem siebten mit einer für diese Prüfzahl neu hergeleiteten σ-Schranke. Folgestellen: `Sonde013StereoGoldenTest.cpp:822-848` (`nak380LaufzeitKohaerenz`), `:934-943` (`nak380Rate`), bei Wahl „jeder Snapshot“ zusätzlich `:692-713` und `:733-797`; Rohdateien `NAK-380-rot-M-75.txt` bis `NAK-380-rot-M-79.txt` und ein Rotbeweis der Kohärenzhälfte an einem Snapshot vor dem letzten (z. B. Mutation, die die Kohärenz nur in frühen Snapshots verfälscht, oder `m73-bandsumme` mit Befund je Snapshot); Matrixzeilen M-75 bis M-79 (Ereignisspalte, Fassung des Dirigenten); Prüfzeilen §46.5 (`:4737-4770`) und §48.5 (`:5554-5568`).

### D2 — M-95: Endlichkeitszusage fällt nur am verlorenen Kohärenzbit

**a) Maßstab, wörtlich.**

- M-95 (`:1004`): Befund „DSP-23 · Zahlenrand: nicht-endliche Kreuzspektren“; Vorbedingung „Testzugang setzt in einem Frame das Kreuzspektrum eines Bins auf NaN“; Ereignis „`stereoSchritt`“; Zusage „der Frame zählt für dieses Band nicht (Riegel je Band wie `Stereo.h:81-84`), die übrigen Bänder zählen; keine NaN in Kohärenz, Phase oder Laufzeit“; Reihenfolge „`stereoSchritt`“; Test „C++: B18 NEU `380/M-95 stereo_nan_riegel_je_bin` (Testzugang)“; Rotbeweis „**Regressionswache: Mutation Riegel weg → NaN in der Kohärenz → rot**“; Quelle „`CLAUDE.md:189-190`“.
- Etikett (`:786-789`): „**Regressionswache**: heute grün und hält, was der Bau nicht ändern darf; sie wird einmal absichtlich gebrochen und ist kein Beleg. … Das Etikett gilt je Satz der Zusage (R-313-12)“.
- Regel (`:75-77`): „je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt“; §6-Kopf (`:796-797`): „Ein Rotbeweis fällt an der Zeile, die die Zusage trägt, nie an einem Nebeneffekt.“
- Präzisierungen (`NAK-380-etappe-5-auftrag.txt:23`): „je Satz und je Hälfte eine eigene Zeile“; (`:26`): „sie ändert die Rechnung so, dass genau die Zusageprüfung fällt … Nennt die Matrix eine Mutation, die am HEAD-Code an einem Riegel endet oder nicht mehr existiert, wählst du eine formgültige Mutation mit derselben Aussage und begründest sie“.
- Lehre Z1 (`NAK-380-verlauf.md:2376`, §31.1): „Wache ohne Trennschärfe … **DEFEKT**“; Anwendung als Rotbeweis an einem Nebeneffekt: §39.1 (`:3671`) „Rotbeweis an einem Nebeneffekt (Regel §2; Lehre Z1 §31)“.

**b) Nachmessung am HEAD.**

- Riegel beim Erzeugen, `Stereo.h:199-202`:
  `if (! (std::isfinite (smm) && std::isfinite (sss) && std::isfinite (sll) && std::isfinite (srr) && std::isfinite (sxyRe) && std::isfinite (sxyIm))) continue; // NaN-Riegel beim ERZEUGEN, je Band und Frame`. Hinter dem Riegel gehen die Bandsummen in `stereoAkku` und `stereoKurz` (`:204-213`), je Bin in Ringslot und Kurzfenster (`:220-234`; der Testzugang setzt dort ebenfalls NaN, `:227-228`) und die Zähler steigen um eins (`:235-237`). Mit Riegel zählt der Frame für das Band nirgends; das Zählen ist die Zählerdifferenz von `stereoRingFrames` (`:108-124`).
- Zweite Sperre in der Auswertung, `Stereo.h:451-465`: `const double n2 = xx * yy; if (n2 > 0.0) { summe += (re * re + im * im) / n2; ++mitEnergie; }` … `const double koh = mitEnergie > 0 ? summe / (double) mitEnergie : 0.0;` **`if (mitEnergie > 0 && std::isfinite (koh))`** (`:465`). Ein NaN im Kreuzspektrum eines Bins macht `summe` und `koh` zu NaN; `:465` sperrt dann Bit und Wert. Phase und Laufzeit liegen innerhalb dieses Zweigs (`:470-506`) und haben eigene Sperren: Phase `if (mitteEnergie && std::isfinite (phi))` (`:473`), Laufzeit je Bin `if (! (n2 > 0.0 && std::isfinite (n2) && std::isfinite (re) && std::isfinite (im))) continue;` (`:491`) und `std::isfinite (tauMs)` (`:504`). Kurzfenster `:308`, Korrelation `:413` ebenfalls gesperrt.
- Die Sperre `:465` ist nicht neu: am BASIS `c43c1646` steht `if (std::isfinite (koh))` in `Stereo.h:231` (gelesen mit `git show`). Die Kette der Matrix „Riegel weg → NaN in der Kohärenz“ trifft damit schon seit der Etappe 1 nicht zu — genau der Fall der Präzisierung `:26` („endet am HEAD-Code an einem Riegel“).
- Test `Sonde013DynamicsTest.cpp` (`nak380StereoNanRiegel`, `:856-964`): drei Zusageprüfungen —
  1. `:939-941` „der Frame mit nicht endlichem Kreuzspektrum zaehlt fuer Band 216 nicht (Riegel je Band)“, `nach216 == vorher[b216]`;
  2. `:942-946` „jedes andere Hauptstufenband mit Bins zaehlt ihn (+1) …“;
  3. `:948-963` **kombiniert**: `pruefe (nichtEndlich == 0 && kohBits > 0 && w216.kohaerenzGesetzt && std::isfinite (w216.kohaerenz), kopf + ": keine nicht endliche Kohaerenz, Phase oder Laufzeit; Band 216 behaelt seine Kohaerenz aus den gueltigen Frames", …)`.
- Rohdatei `NAK-380-e5-mutation-m95-riegel.txt` (gelesen): Mutation `continue;` → `(void) 0;` (`Stereo.h:200` Etappenstand); gefallen `[16 -> 17]` an Prüfung 1 und Prüfung 3 mit „`[148 Baender mit Kohaerenz, nicht endlich 0, Band 216 ohne Bit]`“; das Protokoll führt Prüfung 3 als „mitgefallen (Grund: dieselbe Mutation traegt auch diese Zusage)“. Prüfung 3 fiel also am Teilausdruck `w216.kohaerenzGesetzt`, **`nichtEndlich` blieb 0**.
- Rohdatei `NAK-380-e5-mutation-m95-global.txt` (gelesen): `continue;` → `return;`, gefallen nur Prüfung 2 (`[148 Baender, abweichend 4, …]`) — an ihrer Zusagezeile.
- §46.5 (`:4891`) trägt für „keine NaN in Kohärenz, Phase oder Laufzeit“ `:958` mit `m95-riegel` als „JA“.
- **Doppelmutation.** Riegel weg (`Stereo.h:202` `continue;` → `(void) 0;`) **und** `std::isfinite (koh)` in `:465` weg: `koh` ist NaN, `e.kohaerenzGesetzt = true`, `e.kohaerenz = (float) std::clamp (koh, 0.0, 1.0)` (`:467-468`) — `std::clamp` liefert bei NaN den Eingangswert (beide Vergleiche falsch). Damit steigt `nichtEndlich` in `:953` auf mindestens 1 und die Endlichkeitsprüfung fällt an ihrer eigenen Aussage. Formgültig im Sinn von `:26`: kein Längenriegel, keine Assertion, keine Panik, kein Zugriff außerhalb; NaN in einer Ausgabe ist kein Absturz. Wer jeden Zweifel an der Vorbedingung von `std::clamp` bei NaN ausschließen will, setzt als dritte Zeile `e.kohaerenz = (float) koh;` (Muster der Doppel- und Dreifachmutationen, §46.9 Punkt 13, `:5065-5072`). Phase und Laufzeit bleiben auch unter der Doppelmutation endlich (`koh > kKohaerenzSchwellePhase` ist für NaN falsch, `:470`); die Rotbeweisspalte verlangt nur „NaN in der Kohärenz“.

**c) Was bricht.** Der Rotbeweis des Satzes „keine NaN in Kohärenz, Phase oder Laufzeit“ fällt an einem Nebeneffekt (Bitverlust von Band 216), nicht an der Endlichkeit: Regel `:75-77` und §6-Kopf `:796-797`; die Mutation erzeugt die in der Rotbeweisspalte M-95 (`:1004`) behauptete Folge „NaN in der Kohärenz“ nicht; die Präzisierung `:26` verlangte bei einer Mutation, die „an einem Riegel endet“, eine formgültige Ersatzmutation mit derselben Aussage. Die kombinierte Assertion `:958` bündelt zwei Sätze, deren einer (Endlichkeit) keine eigene fallende Prüfung hat (Präzisierung `:23`, Etikett je Satz `:788-789`).

**d) Urteil: BESTÄTIGT, DEFEKT.** Kein Ausschluss: NAK-407 betrifft den Ort des Testhakens `nanKreuzBin` im Produktpfad, nicht den Rotbeweis; angenommen ist in §46.9 Punkt 13 die Zusatzmutation `m95-global`, nicht `m95-riegel`, und die Annahme gilt laut Prüfauftrag (`NAK-380-erstpruefung-5-auftrag.txt:35`) nur, „sofern sie die Zusagezeile tragen“.

**e) Schließende Regel.** `:948-963` wird in zwei Prüfungen getrennt — (i) „keine nicht endliche Kohärenz, Phase oder Laufzeit“ (`nichtEndlich == 0 && kohBits > 0`) und (ii) „Band 216 behält sein Kohärenzbit mit dem Wert aus den gültigen Frames“ —, und der Rotbeweis von (i) fällt unter der Doppelmutation Riegel weg (`Stereo.h:202`) plus Endlichkeitssperre der Auswertung weg (`Stereo.h:465`, bei Bedarf dritte Zeile `:468` ohne `std::clamp`) mit „nicht endlich ≥ 1“, während `m95-riegel` allein (ii) und die Zählprüfung rot macht. Folgestellen: `Sonde013DynamicsTest.cpp:948-963`; neue Rohdatei `NAK-380-e5-mutation-m95-nan-doppel.txt` (Name frei), `NAK-380-e5-mutation-m95-riegel.txt` neu gefasst, `NAK-380-rot-M-95.txt`; Matrixzeile M-95 Rotbeweisspalte (Fassung: „Riegel weg und Endlichkeitssperre der Auswertung weg → NaN in der Kohärenz → rot“); Prüfzeile §46.5 `:4891`.

### D3 — M-87 (c): der Rotbeweis lässt die Binzuordnung nicht veralten

**a) Maßstab, wörtlich.**

- M-87 (`:996`): Befund „DSP-23 · Ring leert an Grenze, Rücksetzen und Ratenwechsel (starten↔stoppen)“; Vorbedingung „44,1 kHz, Block 512, Evidenzintervall 0,25 s, korrelierter 1-kHz-Sinus L = R; (a) Seek bei 5 s; (b) `zuruecksetzen()`; (c) `vorbereiten (96000)` im Lauf; deterministisch ohne Saat“; Ereignis „erster Snapshot danach“; Zusage „(a) und (b): höchstens die Frames seit der Grenze (höchstens 5, …), Kohärenz `null`; **(c) Binzuordnung für 96 kHz neu, kein Wert der alten Rate überlebt**“; Test „C++: B19 bestehend §32.3-Fall (`:734-785`) erweitert, NEU `380/M-87 ring_leert` mit Beinen `seek`, `reset`, `rate_change`“; Rotbeweis „Regressionswache: der Basisstand leert FFT- und Stereozustand an allen drei Grenzen und berechnet die Binzuordnung beim Ratenwechsel neu (`FeatureEngine.h:451-520,526-535`, `Zeit.h:347-388`); nach dem Bau je eine temporäre Mutation, die `Zeit.h:388` beim Seek, `FeatureEngine.h:534` beim Reset oder **`FeatureEngine.h:468-472,520` beim Ratenwechsel ausnimmt, lässt alte Frames beziehungsweise die alte Binzuordnung überleben → rot**“.
- Regel `:75-77`, §6-Kopf `:796-797`, Etikett je Satz `:788-789` wie bei D2; Präzisierung `NAK-380-etappe-5-auftrag.txt:26` (Ersatz nur, wenn die Matrixmutation „am HEAD-Code an einem Riegel endet oder nicht mehr existiert“, dann „mit derselben Aussage“); `:40` Selbstaudit „Ratenwechsel mit neuer Binzuordnung“; Prüfauftrag `NAK-380-erstpruefung-5-auftrag.txt:29`: „ein Ratenwechsel ohne neue Binzuordnung“ ist DEFEKT.
- Die Matrixstellen am Basis-SHA `b5a0bf9c` (gelesen mit `git show`): `:468` `bass.vorbereiten (kBassPunkte, sr);`, `:469` `haupt.vorbereiten (kHauptPunkte, sr);`, `:471-472` die zwei `zuordnung`-Aufrufe, `:520` `zuruecksetzen();`.

**b) Nachmessung am HEAD** (`eq-copilot/plugin/core/analysis/FeatureEngine.h`, `vorbereiten (double samplerate)` `:582-685`).

- Reihenfolge beim Ratenwechsel: `sr = samplerate;` (`:598`), `bass.vorbereiten (kBassPunkte, sr); haupt.vorbereiten (kHauptPunkte, sr);` (`:599-600`; `Stufe::vorbereiten` setzt `bandVon`/`bandBis` auf 0, `:1313-1314`), `zuordnung (bass, 0, trennIndex()); zuordnung (haupt, trennIndex(), Gitter::evidenzBaender);` (`:602-603`; `Zeit.h:120-145`, `ceil (kante*punkte/fs)` `:139-143`), Ringbemessung aus `stereoBins (b)` (`:641-646`; `stereoBins` liest die Stufen-Zuordnung, `Stereo.h:59-64`), `stereoRing.assign (elemente, StereoBinAkku {});` (`:646`), `vorbereitet = true; zuruecksetzen();` (`:683-684`) mit `stereoLeeren();` (`:698`; `Stereo.h:625-631` nullt jeden Ringeintrag und Stand). Die Matrixstellen existieren am HEAD also verschoben (`:599-603`, `:684`), nicht entfallen.
- Mutation `m87-rate` (Rohdatei gelesen): `stereoRing.assign (elemente, StereoBinAkku {});` → `if (stereoRing.empty()) stereoRing.assign (elemente, StereoBinAkku {});`. Folge am Code: beide `zuordnung`-Aufrufe laufen mit 96 kHz, die Indizes folgen der neuen Zuordnung und liegen im größeren alten Vektor (3 542 von 7 382 Elementen), `zuruecksetzen` → `stereoLeeren` nullt alles. **Neue Binzuordnung richtig, kein alter Wert überlebt; nur die Vektorgröße bleibt 236 224 B.**
- Test `Sonde013StereoGoldenTest.cpp` `nak380M87` (`:1306-1412`): nach dem Ereignis `ringFramesAlle` und `ringBytes` (`:1345-1350`), dann **kombiniert** `pruefe (ringNachEreignis == 0.0 && ringBytesNach == ringBytesSoll, kopf + ": direkt nach dem Ereignis (und dem ersten Block) traegt der Ring keinen Frame, und sein Aufbau folgt der geltenden Binzuordnung (…)", …)` (`:1368-1374`); Frames im ersten und zweiten Snapshot gegen das Kadenzmodell (`:1393-1402`, 13 und 27); Kohärenzbit des 1-kHz-Bands > 0,99 (`:1403-1406`). Eine Prüfung von `bandVon`/`bandBis` je Band nach dem Ratenwechsel gibt es nicht.
- Referenz: `ringBytesSoll = nak380RingBytesSoll (fs2)` (`:1369`) → `nak380RingBytesSoll` (`:1187-1198`) → `nak380Binfenster` (`:297-309`) aus dem Gitter mit eigenen Konstanten (`:273-280`, „bewusst NICHT aus dem Produkt gelesen“). Unabhängig, aber nur als **Summe** über alle Bänder.
- Rohdatei `NAK-380-e5-mutation-m87-rate.txt` (gelesen): gefallen nur „`… traegt der Ring keinen Frame, und sein Aufbau folgt der geltenden Binzuordnung (113344 B)  [Ringframes 0, Ringbytes 236224]`“; grün blieben u. a. „der erste Snapshot danach traegt nur die Frames seit dem Ereignis (13 …)“, „der zweite … (27 …)“ und „1-kHz-Band … Kohaerenzbit (Mono) [1.0000]“. Gefallen ist also der Größenteil der kombinierten Assertion; die Größe gehört zur Zusage von M-85 (Speicher je Rate), nicht zu (c).
- Testzugang für die Binzuordnung je Band: im B19-Freund `FeatureEngineTestzugang` (`:173-234`) **keiner** (`verarbeitet`, `ringBytes`, `ringKapazitaetBytes`, `ringFramesAlle`, `genutzteBins` — letzteres zählt nur Bins mit Energie im Ring). Muster vorhanden in B5: `AnalysisGoldenTestMain.cpp:54-73` (`bandVon`, `bandBis` je Band aus der zuständigen Stufe).
- **Formgültige Mutation mit veralteter Zuordnung:** beim Ratenwechsel `FeatureEngine.h:599-603` auslassen (Matrixmutation, am HEAD vorhanden), etwa `if (! vorbereitet) { … }` um die Stufenvorbereitung und beide `zuordnung`-Aufrufe. Die Stufen behalten N, Puffer und `fs = 44 100`, die Zuordnung der alten Rate überlebt; der Ring wird bei `:646` aus derselben alten Zuordnung bemessen (236 224 B), es gibt keinen Zugriff außerhalb (nach Lesung; im Bau zu bestätigen). Fallen müssten: je Band `bandVon`/`bandBis` gegen `nak380Binfenster (b, 96000)` (neue Prüfung) und — für dieses Ratenpaar trennscharf — die Ringbytes (236 224 ≠ 113 344). Die 1-kHz-Kohärenz fällt dabei nicht (L = R ergibt in jedem Bin mit Energie MSC 1), die Framezahlen ebenfalls nicht (Hop in Samples unverändert).
- **Zweiter Satz „kein Wert der alten Rate überlebt“:** am HEAD doppelt getragen (`assign` `:646` und `stereoLeeren` über `:684`/`:698`); eine Einzelmutation kann ihn nicht verletzen. Formgültig rot wird er unter der Doppelmutation `m87-rate` + `m87-reset` (`zuruecksetzen` ruft `stereoFensterLeeren` statt `stereoLeeren`): der alte Vektorinhalt bleibt, die Zählerköpfe liegen bei unverändertem `trennIndex` an denselben Stellen, „Ringframes 0 direkt nach dem Ereignis“ und „13 Frames im ersten Snapshot“ fallen; die neuen Indizes (3 542 Elemente) liegen im alten Vektor (7 382).

**c) Was bricht.** Regel `:75-77` / §6-Kopf `:796-797`: der Rotbeweis von (c) fällt am Nebeneffekt „Vektorgröße“ (M-85-Größe), nicht an „Binzuordnung für 96 kHz neu“ und nicht an „kein Wert der alten Rate überlebt“; Präzisierung `:26`: Ersatz ohne dieselbe Aussage, obwohl die Matrixmutation am HEAD existiert (die Begründung „Matrixstellen verschoben“, §46.9 Punkt 13 `:5055-5056`, erfüllt die Ersatzbedingung nicht); Etikett je Satz `:788-789`: keiner der zwei Sätze von (c) ist einmal absichtlich gebrochen.

**d) Urteil: BESTÄTIGT, DEFEKT.** Kein Ausschluss: `m87-rate` ist als Ersatzmutation angenommen (§47.1 `:5215`), aber laut Prüfauftrag (`NAK-380-erstpruefung-5-auftrag.txt:35`) nur, „sofern sie die Zusagezeile trägt“ — sie trägt sie nicht.

**e) Schließende Regel.** Nach dem Ratenwechsel prüft der Fall je Band `bandVon`/`bandBis` beider Stufen über einen lesenden B19-Testzugang (Muster `AnalysisGoldenTestMain.cpp:61-73`) gegen `nak380Binfenster (b, 96000)` und trennt Ringframes und Ringbytes in zwei Prüfungen; der Rotbeweis „Binzuordnung neu“ läuft mit der Matrixmutation am HEAD (`FeatureEngine.h:599-603` beim Ratenwechsel ausgelassen) und fällt an der Je-Band-Prüfung, der Rotbeweis „kein Wert überlebt“ mit der Doppelmutation `m87-rate` + `m87-reset` an „Ringframes 0“. Folgestellen: `Sonde013StereoGoldenTest.cpp:173-234` (Testzugang), `:1306-1412` (insbesondere `:1368-1374`); Rohdateien `NAK-380-e5-mutation-m87-rate.txt` (neu gefasst oder ersetzt), neue Protokolle für beide Sätze, `NAK-380-rot-M-87.txt`; Matrixzeile M-87 Rotbeweisspalte (Stellen am HEAD); Prüfzeile §46.5 `:4830`.

## Teil 2 — Gemeinsame Ursache und Durchsicht

**Gemeinsame Wurzel.** Alle drei Befunde sind Fälle von „der Test bildet die Zusage der Matrixzeile nicht vollständig ab, und die Tabelle §46.5 zählt einen Fall als belegt, der an etwas anderem fällt“ (Regel `:75-77`, Lehre Z1 `NAK-380-verlauf.md:2376`, §39.1 `:3671`). Sie teilen sich in zwei Unterursachen, wie in §31.3 und §39.3:

- **(A) Abdeckung je Element** — D1: der Helfer für M-74 („letzter Snapshot“) trägt auch M-75 bis M-79, deren Ereignis „wie M-73“ jeden Snapshot ab dem siebten verlangt.
- **(B) Rotbeweis an einem Nebeneffekt bei doppelt getragener Zusage** — D2 (Riegel `Stereo.h:199-202` plus Endlichkeitssperre `:465`) und D3 (Leeren durch `assign` `FeatureEngine.h:646` plus `stereoLeeren` `:698`; Zuordnung neu, Vektor alt): eine Einzelmutation kann die Zusage nicht verletzen, die kombinierte Assertion fällt an ihrem anderen Teilausdruck, und das Protokoll wertet das als „JA“. Beide Matrixzeilen nennen eine Mutation, deren Kette am Code nicht aufgeht (M-95) bzw. die am HEAD existiert, aber nicht gewählt wurde (M-87 (c)).

**Durchsicht.**

- Ereignisspalte „Snapshots“, „jeder Evidenzsnapshot“ oder „wie M-73“: M-73 prüft 7 bis n (`:886-890`, `:900`); M-83 („Snapshots“, `:992`) prüft jeden Snapshot beiderseits der Grenze (`:1093-1100`, `:1109-1116`); M-93 („Snapshot“, Zustand „wie M-73“, `:1002`) prüft jeden Snapshot (`:1534-1547`); M-82 („erster und zweiter“, `:991`) prüft genau diese (`:1051-1066`); M-84 und M-88 (zehnter bzw. zweiter, `:993`, `:997`) prüfen genau diese (`:1131`); M-74, M-80, M-81 (letzter) prüfen den letzten (`:739`, `:1010`). Außer D1 keine weitere Stelle.
- Rotbeweise „Riegel weg“, „Leeren im …“, Strukturmutation: M-88 `m88-leeren` fällt an der Zusage „5 + 7 = 12“ (`[gemeldet 7, abweichend 150 von 150]`, gelesen); M-92 (a) `m92-energie` fällt am stillen Kanal, (b) `m92-min1` an „7 Frames … kein Bit“ (gelesen); M-85 `m85-alle-bins` fällt an den Ringbytes = Zusage „Speicher“ (gelesen); M-87 (b) `m87-reset` fällt an Ringframes, Frames 5/12 und „Kohärenz null“ (`[1.0000]`, gelesen); M-87 (a) `m87-seek` fällt an den Frames — aber nicht an „Kohärenz null“ (Z1 unten); M-95 und M-87 (c) sind D2 und D3.
- Binzahlen je Rate: M-85 (`:1206-1229`) und M-87 (`:1369`) rechnen gegen `nak380RingBytesSoll` aus dem Gitter (`:1187-1198`, `:297-309`), nicht gegen die Engine — unabhängig. M-95 rechnet Band 216 unabhängig (`Sonde013DynamicsTest.cpp:906-907`, `ceil` aus dem Gitter). Lücke nur die fehlende Je-Band-Prüfung in M-87 (in D3 enthalten).

**Z1 — M-87 (a) Seek: Satz „Kohärenz `null`“ ohne trennscharfe Prüfung.**

- Zusage (`:996`): „(a) und (b): höchstens die Frames seit der Grenze (höchstens 5, …), Kohärenz `null`“; Etikett je Satz (`:788-789`).
- Test: Bein `seek`, `pruefe (zwei && ! danach[0].koh, kopf + ": und die Kohaerenz des ersten ist null (weniger als acht Frames)", …)` (`Sonde013StereoGoldenTest.cpp:1407-1410`). Der erste Snapshot nach dem Seek fällt 512 Samples nach der Grenze (Endstand in `NAK-380-rot-M-87.txt`: „gemeldet 0 nach 512 Samples“; der Kommentar `:1299-1300` nennt noch 684 Samples, Hinweis) und trägt 0 neue Frames; damit ist das Bit schon durch `if (a.frames == 0u) continue;` (`Stereo.h:388-389`) ausgeschlossen, unabhängig vom Ring.
- Beleg: unter `m87-seek` (Ring überlebt) bleibt genau diese Prüfung grün: „`[ok]   380/M-87 ring_leert seek: und die Kohaerenz des ersten ist null (weniger als acht Frames)  [ohne Bit]`“ (`NAK-380-e5-mutation-m87-seek.txt:37`, gelesen). `NAK-380-rot-M-87.txt` führt für (a) die Prüfzeilen „:1203, :1226, :1231 (Bein seek) und :2085, :2089 (§32.3)“ — die Kohärenzprüfung des Beins (`:1241` am Etappenstand) fehlt dort; der §32.3-Fall (`:2253-2257` am HEAD) prüft 24 Frames und „die Kohärenz des neuen Materials“, nicht „null“.
- Einordnung: **DEFEKT (schwach)** — Wache ohne Trennschärfe (Lehre Z1 `NAK-380-verlauf.md:2376`; Präzisierung `NAK-380-etappe-5-auftrag.txt:24` „Keine Wache ohne Trennschärfe“), der Satz „Kohärenz `null`“ von (a) ist nie gebrochen; gleiche Wurzel (B). Nicht vom Prüfer genannt, kein Ausschluss greift.
- Regel: der Seek liegt so, dass der erste Snapshot danach 1 bis 7 neue Frames trägt (Seek unmittelbar nach einem Snapshot, erster Snapshot nach rund 13 824 Samples mit 5 Frames, wie im Bein `reset`), damit `m87-seek` (überlebender Ring, 5 + alte Frames ≥ 8) die Prüfung „Kohärenz null“ rot macht. Folgestellen: `Sonde013StereoGoldenTest.cpp:1319-1329`, `:1360-1367` (Kadenz der Segmente), `:1407-1410`; `NAK-380-e5-mutation-m87-seek.txt`, `NAK-380-rot-M-87.txt`; Prüfzeile §46.5 `:4828`.

Keine weitere Stelle mit derselben Wurzel gefunden.

## Übersicht

| Befund | Urteil | Einordnung | schließende Regel in einem Satz | Folgestellen |
|---|---|---|---|---|
| D1 M-75 bis M-79 nur letzter Snapshot | PRÄZISIERT | Kohärenzhälfte DEFEKT; Laufzeithälfte LÜCKE (Ereignis „wie M-73“ gegen 5σ-Bemessung von R-380-13 (iii)) | Kohärenzhälfte an jedem Snapshot ab dem siebten wie M-73; Laufzeithälfte per Matrixfassung an den letzten Snapshot oder an jeden Snapshot mit neu hergeleiteter σ-Schranke. | `Sonde013StereoGoldenTest.cpp:822-848`, `:934-943` (ggf. `:692-713`, `:733-797`); `NAK-380-rot-M-75.txt` bis `-M-79.txt`; Matrix M-75 bis M-79 Ereignis; §46.5 `:4737-4770`, §48.5 `:5554-5568` |
| D2 M-95 Endlichkeit fällt am Bitverlust | BESTÄTIGT | DEFEKT | Endlichkeit und „Band 216 behält Kohärenz“ getrennt prüfen; Rotbeweis der Endlichkeit mit Riegel weg plus Sperre `Stereo.h:465` weg (ggf. `:468` ohne `std::clamp`). | `Sonde013DynamicsTest.cpp:948-963`; `NAK-380-e5-mutation-m95-riegel.txt`, neues Doppelprotokoll, `NAK-380-rot-M-95.txt`; Matrix M-95 Rotbeweisspalte; §46.5 `:4891` |
| D3 M-87 (c) Rotbeweis an der Vektorgröße | BESTÄTIGT | DEFEKT | Binzuordnung je Band gegen `nak380Binfenster (b, 96000)` prüfen, Ringframes und Ringbytes trennen; Rotbeweise mit veralteter Zuordnung (`FeatureEngine.h:599-603` ausgelassen) und mit `m87-rate` + `m87-reset` für „kein Wert überlebt“. | `Sonde013StereoGoldenTest.cpp:173-234`, `:1306-1412`; `NAK-380-e5-mutation-m87-rate.txt`, neue Protokolle, `NAK-380-rot-M-87.txt`; Matrix M-87 Rotbeweisspalte; §46.5 `:4830` |
| Z1 M-87 (a) „Kohärenz null“ im Bein seek ohne Trennschärfe | weitere Stelle | DEFEKT (schwach) | Seek so legen, dass der erste Snapshot danach 1 bis 7 neue Frames trägt, damit `m87-seek` die Prüfung „Kohärenz null“ rot macht. | `Sonde013StereoGoldenTest.cpp:1319-1329`, `:1360-1367`, `:1407-1410`; `NAK-380-e5-mutation-m87-seek.txt`, `NAK-380-rot-M-87.txt`; §46.5 `:4828` |

## Nicht geprüft

- Kein Lauf: ob die Kohärenzhälfte von M-75 bis M-79 an jedem Snapshot ab dem siebten ≥ 0,95 hält (insbesondere 176,4 und 192 kHz), ob die vorgeschlagenen Mutationen im Bau formgültig laufen und an genau den genannten Prüfungen fallen, ob die Doppelmutation in D2 unter MSVC tatsächlich NaN ausgibt — alles nur am Code gelesen.
- Die Snapshotzahlen je Lauf (21 bis 27 ab dem siebten) und die Summe von rund 37 000 Bandprüfungen sind aus dem Kadenzmodell des Tests nachgerechnet, nicht gezählt.
- Nicht gelesen: der übrige Diff außerhalb der für D1 bis D3 und die Durchsicht nötigen Stellen (Vertrag, Register, Broker, A5-Skript, `Nak380Pruefsignale.h` außer über die Rohdateien), die Nachbauten `NAK-380-etappe-5-laufzeit-nachbau*.txt`, die übrigen 50 Mutationsprotokolle der Etappe außer `m95-riegel`, `m95-global`, `m87-rate`, `m87-seek`, `m87-reset`, `m88-leeren`, `m92-energie`, `m92-min1`, `m85-alle-bins`, und der während des Laufs entstehende Kanon-Abschnitt.
- Die Vorgaben §5.1, §7.2, §7.3, §8.1 wurden nicht vollständig gelesen; zitiert ist nur, was die Befunde tragen.

FERTIG Validierung Erstprüfung 5, 2 bestätigt, 1 präzisiert, 0 widerlegt, 1 weitere Stellen
