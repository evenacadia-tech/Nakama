Validierung der Erstprüfung S25k Etappe 4 / Ticket NAK-312 — gebundener Auftrag des Dirigenten (2026-09-21)

Du bist ein frischer, lesender Validierer (nicht der Bauer, nicht der Prüfer, kein Bauer, Prüfer oder Validierer früherer Runden). Gegenstand ist das Rohurteil docs/beweise/roh/NAK-312-etappe-4-erstpruefung-urteil.md (URTEIL: PASS, 0 Defekte, Lücke L-1, Härtungen H-1 und H-2) über den Änderungssatz `git diff a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c...33ef53fc64499d80d198c734eef7811f5ebd7927` der sieben Pfade aus docs/beweise/roh/NAK-312-etappe-4-erstpruefung-auftrag.txt. Du prüfst jede tragende Aussage des Berichts an der Quelle nach — Zitat mit Datei:Zeile, am Stand HEAD, dessen Code der des Zielstands ist — und urteilst je Aussage BESTÄTIGT, PRÄZISIERT (mit der Berichtigung) oder WIDERLEGT (mit dem Gegenbeleg). Die Einordnung in DEFEKT, LÜCKE oder HÄRTUNG bleibt Sache des Dirigenten; du lieferst die Quelllage, auf der er sie trifft, und sagst, wo die Einordnung des Prüfers an der Quelle nicht trägt.

Du liest und fährst nur lesende Kommandos (git status, git rev-parse, git log, git show, git diff, git blame, git grep, git ls-files und die Lesewerkzeuge; die JUCE-Quelle liegt unter eq-copilot/build/_deps/juce-src); du startest keinen Compiler, keinen Test, kein cargo, kein FL Studio, keinen Python-Interpreter; du schreibst genau eine Datei: docs/beweise/roh/NAK-312-etappe-4-erstpruefung-validierung.md (neu, untracked, LF-Zeilenenden; du stagest, committest und pushst nichts). Fremde Dateien fasst du nie an (briefing-hub/ und nimbalyst-local/ sind fremd). Miss git rev-parse HEAD zu Beginn und am Ende und nenne beide Werte; sie müssen gleich sein.

REFERENZ: docs/beweise/NAK-312.md — Kopf (Gate wörtlich, Grenze), §2 (R-312-2, R-312-7), §5.5 und §5.6, §6 Kopf und §6.4 (M-30 bis M-39), §7.4, §11.3 (E-312-10, R-312-12), §14.4 (E-312-11), §27 (Etappenbericht, Abweichungen A-37 bis A-47, Hinweise §27.9), §28 (Messung). CLAUDE.md (Grundgesetz, keine toten UI-Elemente, Beziehungen öffnen↔schließen, Designteil geparkt: keine neue Oberfläche, nur der nötige Handgriff oder die ehrliche Zustandsmeldung).

ZU VALIDIEREN:
V-1 (L-1, Mechanik). Stimmt die Kette des Berichts an der JUCE-Quelle und am Code: schließt der Host das Editorfenster mit offenem Messpunkt-Popover, sterben Box und Panel erst in einem späteren Durchlauf der Nachrichtenschleife, und kommt auch textEditorFocusLost nur über eine gepostete Nachricht — sodass uebernehmen() in beiden Wegen einen bereits toten Editor sieht und einen getippten, unbestätigten Namen oder Paarnamen verwirft? Gibt es einen Weg, auf dem die Eingabe doch noch bei lebendem Editor übernommen wird (etwa ein synchroner Fokusverlust beim Entfernen vom Desktop oder beim Abbau der Kinder)?
V-2 (L-1, Stand am BASIS). Wurde der unbestätigte Text am BASIS a80f0603 in genau diesem Fall (Editor fort, Prozessor lebt) tatsächlich übernommen — also ist die Wirkung eine durch die Etappe eingeführte Änderung des bisherigen Verhaltens? Zitiere den BASIS-Code (git show a80f0603:eq-copilot/plugin/src/PluginEditor.cpp).
V-3 (L-1, Zusagenlage). Was sagen R-312-2 (alle Sätze), M-35, M-36, M-37, §5.6 und der Bauplan §7.4 wörtlich zu dem Fall „Editor fort, Prozessor lebt, Eingabe unbestätigt"? Trägt die Lesart des Prüfers, dass M-36 den letzten Satz von R-312-2 vollständig umsetzt und der Fall ungeregelt ist, oder deckt ein Satz ihn?
V-4 (L-1, Baubarkeit einer erhaltenden Regel — nur Quelllage, kein Entwurf). Der Dirigent erwägt die Regel: „Schließt das Editorfenster mit offenem Messpunkt-Popover, übernimmt das Panel die offene Eingabe noch bei lebendem Editor — der Editor baut sein Popover in seinem eigenen Abbau synchron ab —; danach bleibt der Rückruf wirkungslos (R-312-2)." Belege an der JUCE-Quelle und am Code, ob das ohne neue Oberfläche und ohne Bruch von R-312-2 baubar ist: Gibt CallOutBox::launchAsynchronously eine Referenz zurück, die der Editor als SafePointer halten kann; ist das Zerstören einer modalen, vom ModalComponentManager gehaltenen CallOutBox durch ihren Aufrufer zulässig und räumt der Manager seinen Eintrag danach ohne Doppelfreigabe ab (autoDelete-Weg); ist der Editor im Rumpf von ~EqCopilotEditor für SafePointer::getComponent() noch lebend (der Bericht sagt ja) und lebt dort der Prozessor; welche anderen CallOutBox-Aufrufe des Editors (Hinweisliste, Kennungskonflikt) berührt das nicht? Nenne Risiken, die du an der Quelle siehst (Reihenfolge im Destruktor des Editors, Timer, Listener), ohne Code vorzuschlagen.
V-5 (H-1). Stimmt, dass 312/M-34 die Lage von telemetryV3.stop() vor dem Schließen nicht sicher misst und die B31-Behauptungszeile in tools/beweise.ps1 („beide stop() liegen vor dem Schliessen") damit an dieser Hälfte mehr sagt, als das Bein misst? Der Auftrag an den Bauer lautete wörtlich: „keine Zeile behauptet mehr, als ihr Bein misst; je Bedingung genau das Gemessene". Zitiere die Bedingungen im Test und den Rotlauf docs/beweise/roh/NAK-312-rot-M-34.txt.
V-6 (H-2). Stimmt, dass ein entferntes betreten() im Hello- oder im Telemetrie-Hello-Lambda in B31 und B7 grün bliebe, und verlangt eine Matrixzeile oder R-312-7 die Messung dieser zwei Provider?
V-7 (tragende Entlastungen). Prüfe nach: (a) es gibt genau drei Rückrufe der v3-Clients in die Sonde und keinen weiteren Haken an der Schleuse vorbei; (b) schliessen() kann am Zielstand nicht unbegrenzt warten, weil der zerstörende Thread keine Sperre hält, die ein Provider braucht; (c) jeder Testzugang liegt innerhalb #if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3), und die Produktziele setzen das Define nicht; (d) eq-copilot/plugin/core, dsp, state, identity, schemas und broker sind zwischen BASIS und ZIEL unverändert.

FORM DES BERICHTS: erste Zeile „VALIDIERUNG: <Zahl> bestätigt, <Zahl> präzisiert, <Zahl> widerlegt". Danach je Punkt V-1 bis V-7 ein Abschnitt: Urteil, Zitate mit Datei:Zeile, bei V-3 die Sätze wörtlich. Danach „Nicht geprüft" und die zwei HEAD-Werte. In Markdown-Tabellenzellen ein Pipe-Zeichen als \| schreiben. Beende dich mit einer Zeile: FERTIG Validierung Erstprüfung Etappe 4, <Zahl> bestätigt, <Zahl> präzisiert, <Zahl> widerlegt.
