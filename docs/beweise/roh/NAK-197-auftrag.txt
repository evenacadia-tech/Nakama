NAK-197 — Härtung am Prüfwerkzeug: die Parken-Tests der IPC-Clients fallen unter Last falsch rot. Auftrag des Dirigenten (2026-09-06), außerhalb von NAK-181, vor dessen Abschlusskanon.

DU BIST ein frischer Opus-Bauer (max) im sichtbaren Checkout C:/Users/phili/Projekte/Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach `git pull --rebase` bei sauberem Worktree. **Kein Produktcode**: geändert werden nur eq-copilot/plugin/tests/IpcTestMain.cpp und eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp (plus die Registerzeile, siehe unten). Landminen: kein Lösch-Cmdlet im eigenen Befehlstext (Logs mit `*>` überschreiben); Test-Pipes nur im Probe-Namensraum. Auf dem Rechner läuft fremde Grundlast (rund 39 % CPU) — genau die Lage, in der die Fälle gefallen sind; sie ist erwünscht.

BEFUND (docs/beweise/NAK-181.md §7.5 Nr. 1; Rohausgaben docs/beweise/roh/NAK-181-1bf41ed.md Abschnitt B10 und docs/beweise/roh/NAK-181-768737f.md Abschnitte B5 und B10): `parken_uebergeht_den_backoff` (IpcTestMain.cpp:1580-1692, vier Fälle control/telemetrie × authfehler/access_denied) und `pipeclient_parken_uebergeht_den_backoff` (PipeClientLifecycleTestMain.cpp:1110-1224) stempeln den dritten Verbindungsversuch, sobald `verbindungsVersuche` 3 zeigt (`versuchsStempel`), und legen sofort die Sperrursache an (Fremdpipe oder Auth-Server). Der Client zählt den Versuch aber VOR dem Öffnen (ControlClient.cpp:1668 `++zustand.verbindungsVersuche`, Öffnen erst :1725; dazwischen Hello-Provider und Audioprüfung). Unter Last trifft deshalb schon der dritte Versuch die Sperre: „geparkt 1 bei Versuch 3" (verlangt ≥ 4), alle übrigen Bedingungen erfüllt. In zwei Kanonläufen fielen drei verschiedene Fälle (control/access_denied, telemetrie/access_denied, pipeclient authfehler); der Kanon war davor mehrfach grün. Der Kommentar :1633-1636 bedenkt nur das Verpassen nach hinten.

REGEL: Die Sperrursache entsteht erst, wenn der dritte gestempelte Versuch ABGESCHLOSSEN ist — beobachtet an einem Snapshot-Feld, das der Client erst nach dem Öffnungsversuch setzt (Fehlausgang, z. B. `status`-Wechsel weg von `verbindet`, `brokerPipeFehlt` oder der Zeitstempel des Fehlausgangs; lies den Client, um das Feld zu wählen, das für ControlClient, TelemetryClient und PipeClient gleich verlässlich ist) —, nie nur am Zählerstand. Die Zusage der Zeile („geparkt bei Versuch ≥ 4, Backoff steht auf 4000 ms, Reconnect löst und der Versuch danach nutzt 500 ms") bleibt unverändert; nur die Vorbedingung wird deterministisch. Passe den Kommentar :1633-1636 an die wirkliche Race-Richtung an. Beide Varianten (Fremdpipe für access_denied, Auth-Server für authfehler) gleich behandeln. Prüfe, ob weitere Fälle in diesen zwei Dateien dieselbe Form „stempeln, dann sofort Ursache anlegen" tragen (rg 'versuchsStempel'), und schließe sie mit demselben Muster — aber nichts sonst.

BELEGE:
1. Zehnfachlauf beider Beine grün — `EqCopIpcTest` und `EqCopPipeClientTest` je zehn Läufe hintereinander, je Exitcode 0 — Bilanzzeilen als docs/beweise/roh/NAK-197-zehnfach-<sha>.txt. Bauziele und Aufrufe stehen in tools/beweise.ps1 (Beine B10 und B5).
2. Ursachenbeleg docs/beweise/roh/NAK-197-ursache.txt: die Zeilen im Client (Zählen vor Öffnen) und im Test (Ursache sofort nach dem Stempel), das gewählte Beobachtungsfeld und warum es erst nach dem Öffnungsversuch gesetzt wird. Ein Rotbeweis ist nur nötig, wenn er ohne Produktänderung möglich ist (z. B. Sperrursache im Test absichtlich sofort nach dem Stempel plus künstliche Verzögerung im Test-Provider `nak123ControlHello`, der ja Testcode ist) — dann als docs/beweise/roh/NAK-197-rot.txt; sonst begründen, nicht behaupten.

REGISTER: In docs/offene-punkte.md trägt der Dirigent die Zeile NAK-197 selbst nach — du fasst sie NICHT an. Du schreibst kein Manifest; die Belege liegen als Rohdateien.

COMMIT (zwei Testdateien, Rohdateien) mit Pathspec, `git pull --rebase`, Push. Kein Anfassen von docs/**, Produktcode, anderen Tests. Turn mit der letzten Zeile „NAK-197 ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.

SELBSTAUDIT: Wartet der Test auf den Abschluss des dritten Versuchs, oder nur länger? Bleibt die Zusage der Zeile (≥ 4, Fenster 350..1200, ohne Reset ≥ 4000) unverändert? Kein Produktcode, keine Schemaänderung, keine anderen Tests berührt.
