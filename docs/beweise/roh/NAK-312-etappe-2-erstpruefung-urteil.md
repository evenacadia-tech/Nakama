URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).

# NAK-312 Etappe 2 — Erstprüfung (S25k, Prüfstufe T2)

Frische, lesende Prüfsession, 21.09.2026. Auftrag `docs/beweise/roh/NAK-312-etappe-2-erstpruefung-auftrag.txt`. Prüfbereich `git diff f96bdd4a3834ef4469e110d2a286df0b86f4ff5d...5a049913067de9a65a1f349c7f6c3bcaf293c1df` über die sechs Pfade des Auftrags (6 Dateien, +587/-12), vollständig gelesen. Zeilenangaben gelten zum Zielstand 5a049913.

## Befunde

DEFEKTE: keine. LÜCKEN: keine.

**H-1 (HÄRTUNG) — Offline-Satz der B7-Behauptung mehrdeutig.** `tools/beweise.ps1:728`: „legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, nimmt jeder Wechsel nach offline genau eine gemeldete Sperre, ohne Wechsel keine". Gemessen ist: jeder Aufruf `setNonRealtime (true)` nimmt genau eine gemeldete Sperre, `setNonRealtime (false)` keine (`eq-copilot/plugin/tests/TransactionTestMain.cpp:3587-3621`; im Kanon 200 Sperren bei 200 Aufrufen, von denen nur der erste den Zustand wechselt). Das Produkt nimmt die Sperre bei jedem Aufruf mit `offline == true` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1371-1377`), und der VST3-Wrapper ruft `setNonRealtime` in jedem Block (`juce_audio_plugin_client_VST3.cpp:3892`, Bauartefakt unter `eq-copilot/build/_deps/juce-src/`). Liest man „Wechsel" als Zustandsübergang — so gebraucht Etappe 5 das Wort („Wechselerkennung", M-40) —, sagt „ohne Wechsel keine" das Gegenteil der Messung: 199 Aufrufe ohne Übergang nahmen je eine Sperre. Der Kopfkommentar von B7 sagt es genau („nimmt je Aufruf eine gemeldete Sperre", `TransactionTestMain.cpp:35`). Kein DEFEKT: die Zusage von M-04 führt denselben Begriff für dieselbe Messung, und in der Lesart „Aufruf mit offline" ist der Satz wahr. Genauer wäre: „nimmt jeder Aufruf setNonRealtime (true), im Offlinebetrieb also jeder Block, genau eine gemeldete Sperre, setNonRealtime (false) keine".

**H-2 (HÄRTUNG) — Produktverdrahtung des Konstruktors ungemessen.** `eq-copilot/plugin/sonde/SondeProcessor.cpp:139-151` und `:172-183`. Die Delegation ist nach dem Lesen verhaltensgleich (Geprüft, Punkt d), gemessen wird sie nicht: in den Tests kommt `v3PipeNameFuerTest` nur in B31 (Testkonstruktor) und in `Sonde012ProjectReloadTest.cpp` (Gen) vor, `pipeNameV3` nur als Funktionsprüfung in `IpcTestMain.cpp:3669` und `:3681`; keine der fünf Szenariodateien unter `docs/gesundheit/szenarien/` nennt Control, Telemetrie, Pipe oder Quellen der Sonde. Die Regressionswache M-09 sieht eine Verschiebung von Name oder Installbindung im Produktkonstruktor damit nur im Diff. Ein Vergleich `v3PipeNameFuerTest() == pipeNameV3 (aktuelleLogonSid())` an einer Sonde aus dem Produktkonstruktor im Testbau startet keinen Client und machte die Namenshälfte messbar; die Servererwartung hat keine Sicht. Etappe 4 baut an derselben Initialisierungsliste weiter (Lambdas der Schleuse, §7.0).

## Geprüft

(a) `const RtWache::Bereich wache;` ist die erste Anweisung von `processBlock` (`SondeProcessor.cpp:417`); die Funktion hat keinen frühen Rücksprung, der Bereich schließt also Stempel, Kern, Analysekopie, Queue und Sicherheitsschleife (`:540-541`) ein. Er erhöht und senkt nur die thread-lokale Tiefe (`eq-copilot/plugin/dsp/DspRtWache.h:36-37`, `:126-130`), schreibt kein Sample, ändert weder Programmzustand noch Latenz und nimmt keine Sperre, alloziert nicht, wartet nicht; außer den Zählfunktionen und B7 liest niemand `imAudiopfad()` (`git grep`), kein Produktzweig hängt an der Tiefe. Der Bereich des Kerns (`DspKern.cpp:1059`) läuft darin auf Tiefe 2 und kehrt auf 1 zurück, `verlasse()` fällt nie unter 0; B6 misst den Kern weiter allein. (b) `setNonRealtime` nimmt `zustandSchloss` über `GemeldetesSchloss` (`lock` ruft `enter`, `unlock` ruft `exit`, `SondeProcessor.h:135-140`) an derselben Stelle und mit derselben Lebensdauer wie zuvor den `juce::ScopedLock`: nach der Rückkehr bei `offline == false`, vor `beendePreview` und `setzeHoermatrix`, gelöst am Funktionsende (`SondeProcessor.cpp:1368-1380`). Neu ist nur `meldeSperre()` vor der Nahme, im Produkt ein thread-lokales Lesen ohne Zählung, weil der Wrapper `setNonRealtime` vor `processBlock` und damit außerhalb jedes Bereichs ruft (`juce_audio_plugin_client_VST3.cpp:3890-3909`). 312/M-01, 312/M-02 und 312/M-04 lesen genau die Zähler ihrer Zusage (`RtWache::sperren()`, `RtWache::allokationen()`; den eigenen Zähler nur als Nachweis, dass der Workerzug allozierte), setzen vor der Messung zurück oder messen Differenzen, nutzen feste Seeds und keine Wanduhr; der mitlaufende Worker kann nichts beitragen, weil `zaehleAllokationen` thread-lokal ist und kein Produktpfad außer `setNonRealtime` eine gemeldete Sperre nimmt. Die Rotbeweise mutieren je die Zusagezeile: M-01 Bereich entfernt (0 statt 200; heute rot mit 0 Sperren bei 200 Playhead-Aufrufen), M-02 Bereich als Mitglied (der Workerzug zählt 1137 Allokationen), M-04 Adapter zurück auf `juce::ScopedLock` (0 statt 200). (c) Beide Operatoren (`TransactionTestMain.cpp:92-109`) haben die Form von B6 (`DspGoldenTestMain.cpp:98-115`, nur mit `dsp::`); `++allokationen` steht weiter vor der Meldung, `meldeAllokation()` alloziert nicht; keine bestehende B7-Prüfung liest `RtWache::allokationen()`, der Diff ändert außer dem Kopfkommentar keine bestehende Prüfung (heute-rot-Lauf 192 von 195 grün, Kanon 195/195). (d) Der Testkonstruktor (`SondeProcessor.h:150-167`, `.cpp:153-170`) übernimmt nur Namen, die `istProbePipename` annimmt (Probe-Präfix plus nicht leerer Rest, `PipeToken.h:59-70`), sonst die leere Zeichenkette; Testkonstruktor, Starthaken und `v3PipeNameFuerTest()` stehen unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` (`.h:284-447`), das Bundleziel `NakamaProbeeq` bekommt das Define nicht (`CMakeLists.txt:350-357`, `:390`). Der Produktkonstruktor ruft `aktuelleLogonSid()` einmal, bildet `pipeNameV3` aus demselben Wert und ruft `brokerServerErwartung()` einmal; beide Clients nehmen Name und Erwartung als Wert (`ControlClient.h:356-363`, `TelemetryClient.h:109-113`), die Erwartung trägt keine Instanzwerte (`IpcVerbindung.h:105-121`), die Kopie ist also inhaltsgleich zu den zwei früheren Aufrufen. Die Mitgliederreihenfolge ist unverändert (`.h:570-586`); verschoben werden nur `logonSid` und `pipeName` in die Mitglieder, danach liest die Initialisierungsliste nur Mitglieder. B31 öffnet die Produktions-Pipe in keinem Fall: kein Fall nutzt den Produktkonstruktor, Server- und Startname gehen vorher durch `istProbePipename`, die fremde Sonde aus M-05 startet nie, und `reconnect()` aus Laden und Vorbereiten startet keinen Thread (`ControlClient.cpp:147-162`). (e) Die B31-Zeile (`tools/beweise.ps1:749`) nennt genau M-05 bis M-08 und nicht die Etappe-4-Messungen (A-2); die B6-Klarstellung (`:723`) ist wahr, B6 übersetzt nur `tests/DspGoldenTestMain.cpp` (`CMakeLists.txt:1156-1162`); A16 liest keinen Wachenzähler (`git grep RtWache` in `SondeNullTestMain.cpp` leer), seine Zeile bleibt zu Recht unverändert (A-1). Das Ziel entsteht über `nakama_sonde012_test` mit `PRODUCT_NAME` gleich Zielname, der Runner baut jedes Plugin-Bein, dessen Name in der CMake-Datei steht (`tools/beweise.ps1:904`, `:953`), und B31 lief im Kanon (Exit 0, 8 geprüft, Binary frisch). Zum Offline-Satz der B7-Zeile siehe H-1. (f) A-4: die Ersatzmutation (Worker im Destruktor nicht gejoint) lässt B31 mit 0xC0000409 enden und bricht „zerstört sie und endet mit Exitcode 0"; die Prüfzeile 312/M-07 selbst wird dabei nicht erreicht, weil schon die erste abgebaute Sonde (M-05) den Prozess beendet, die Hälfte „mit laufenden Clients" fällt aber unter der M-06-Mutation mit („beide Clients vor dem Abbau verbunden NEIN"). A-5: `beendePreview` an der umgebauten Stelle entfernt lässt [SONDE-015] M-120 fallen, die Wache sieht also eine Verhaltensänderung genau dort, wo die Etappe den Produktweg umbaut; die Matrixmutation (Tausch) blieb laut Rohdatei grün, weil M-41 erst in Etappe 5 entsteht. Produktverhalten seit BASIS, selbst nachgemessen mit lesenden Git-Kommandos: `git diff --stat f96bdd4a 5a049913` über `eq-copilot/identity`, `eq-copilot/schemas`, `eq-copilot/plugin/state`, `eq-copilot/fixtures`, `eq-copilot/plugin/dsp`, `eq-copilot/plugin/src`, `eq-copilot/plugin/core`, `eq-copilot/plugin/hostbridge` und `broker` ist leer; `SondeProcessor.h` bekommt nur Zeilen hinzu, `SondeProcessor.cpp` ändert sich nur in Konstruktor, `processBlock` und `setNonRealtime`; die Bundle-Zielfunktion in CMake ist unberührt; `git diff --stat 89432468 5a049913` über den Prüfbereich ist leer, der Kanon an 89432468 gilt also für den Code des Zielstands; alle sechs Dateien liegen im Index als LF. Nur gelesen: Kanon GRÜN 70/70 an 89432468 (A16 131 ok, B6 389/0, B7 195/0 mit 312/M-01 200 Sperren und 0 Allokationen, 312/M-02 1137 eigene Allokationen bei Wachenzählern +0, 312/M-04 200 und 0, B31 8/0), Laufzeit-Arm aa074e42 mit Host-Nulltest BITIDENTISCH, die neun Rotbeweisdateien M-01 bis M-09.

## Nicht geprüft

Nicht gefahren: kein Compiler, kein Test, kein Kanon, kein FL, kein Python; jedes Laufergebnis oben stammt aus den Rohdateien. Ohne Produkthaken nicht messbar und deshalb nur gelesen: dass der Bereich nach `dspKern->verarbeite` bis zum Funktionsende offen bleibt (312/M-01 sondiert nur die Playhead-Stelle davor; Allokationen dahinter sieht B7s eigener Zähler unabhängig vom Bereich) und dass mit dem leeren Pipenamen keine Verbindung zustande kommt (`eq-copilot/plugin/core/ipc/IpcVerbindung.cpp:19-22` und `:66-101`, kein Rückfall auf einen anderen Namen; B31 startet die fremde Sonde nie). Nicht Gegenstand nach Auftrag: docs/**, `DspRtWache.h` (vom Rotbeweis M-03 nur vorübergehend mutiert; `git diff --stat f96bdd4a 5a049913 -- eq-copilot/plugin/dsp` ist leer), die Etappen 3 bis 7, die Produktkarten, der JUCE-VST3-Wrapperpatch, T3-12-* und die datierten Registerhärtungen. Außerhalb des Repos liegen zwei Lesekopien (`git show` von `TransactionTestMain.cpp` und `tools/beweise.ps1` am Zielstand) im Job-Temp-Ordner dieser Session; im Repo ist dieser Bericht die einzige neue Datei, nichts gestagt, committet oder gepusht.

HEAD zu Beginn: `4949f0239db3fcac301b2280953df091e047e1cb`. HEAD am Ende: `4949f0239db3fcac301b2280953df091e047e1cb`. Beide mit `git rev-parse HEAD`; HEAD trägt über ZIEL hinaus nur den Prüfauftrag (`git diff --stat 5a049913 HEAD`: eine Datei unter `docs/beweise/roh/`).

FERTIG Erstprüfung Etappe 2, PASS, 0 Defekte, 0 Lücken, 2 Härtungen.
