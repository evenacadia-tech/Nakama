# Offene Punkte — durables Offen-Set

> Regeln: Offene Einträge werden beim Auffrischen NIE gelöscht. Schließen
> nur mit ID + Commit-SHA (oder expliziter User-Entscheidung). Neue
> Out-of-scope-Funde kommen als datierte Zeile mit ID dazu.
> Nur Nakama-/eq-copilot-Punkte — FL-Studio-Workspace-Themen (MIDI,
> Vorlagen, Plugin-Hub-GUI) bleiben dort.
> Seit 21.08.2026 gibt es eine dritte Tabelle **Geparkt**: Punkte der
> Prisma-Studie, die der User geparkt hat — weder Arbeit noch erledigt.

## Offen (Stand 2026-08-22)

| ID | Seit | Punkt |
|---|---|---|
| NAK-07 | 08-14 | Dauerbetrieb: Loudness inkrementell, Pipe-Deadlines. **Umgewidmet 08-21 (Kontext-Interview):** der Rest des alten Änderungssatzes 2/3 lebt in der Sondenlinie weiter — fixed-memory Loudness ist `SONDE-008` (Entwurf §65), Backpressure/Deadlines der Pipe sind `SONDE-010`. Bleibt offen, bis diese Tickets ihn messbar schließen. |
| NAK-10 | 08-17 | Broker liest das `hoermarkierung`-Heartbeat-Flag noch nicht (serde verwirft still; `protokoll.rs` MessStand ohne Feld) — Aggregat soll laut `eq-ipc.schema.json` während Markierung pausieren/markieren. Bewusst nach dem Harness-Lauf, s. HOER-MARKIERUNG-KONZEPT. Entwurf §31.2 führt es als offenen Vertrag; Zielphase P2 (§56). |
| NAK-13 | 08-18 | Broker-Autostart fehlt: Früher startete die Hub-App den Broker automatisch; jetzt muss `eqcop-broker.exe` von Hand laufen, sonst misst das Plugin nur lokal (verbindet per Backoff, sobald der Broker da ist — kein Datenverlust, aber keine Paare/Aggregate). Klären: Autostart-Eintrag, Task-Scheduler oder Start auf Zuruf. Konzeptantwort im Entwurf §48.3 (Start nur durch klassifiziertes Main mit offenem Editor, `SONDE-007b`) — ungebaut. |
| NAK-25 | 08-21 | **Der Baustand-Riegel von `tools/beweise.ps1` ist zu grob geworden** (Fund beim Abschluss von S3b, out of scope). Er bildet EINEN globalen „neueste Quelle“-Zeitstempel über alle Quellorte und vergleicht ihn gegen JEDE Prüfbinärdatei (`beweise.ps1`, Schleife um `$neuesteQuelle`). Seit `hostprobe/` dazugekommen ist, gilt damit z. B. `EqCopNullTest` (Bau 18.08.) als veraltet, obwohl seine eigenen Quellen unberührt sind — ein Lauf OHNE `-Bauen` endet dadurch immer auf Exitcode 4. Die Richtung stimmt (er beglaubigt nie fälschlich, er verweigert nur zu oft), und mit `-Bauen` schlägt das Urteil des Buildsystems den Zeitstempel — der Normalfall bleibt also grün. **Bewusst NICHT jetzt gefixt:** eine Zuordnung Quelle→Ziel wäre echte Arbeit und würde einen Riegel anfassen, der sich in dieser Session zweimal bewährt hat. Wer es angeht: je Ziel die eigenen Quellorte führen, nicht global vergleichen. |
| NAK-24 | 08-21 | **`hatTransport` ist im VST3-Pfad eine Tautologie** (Fund aus `SONDE-003`, out of scope dieses Tickets). `PluginProcessor.cpp:140` (seit 22.08.; vorher `:135`) setzt `hatTransport = true`, sobald `getPosition()` irgendein `PositionInfo` liefert — und JUCEs VST3-Playhead gibt **nie** `nullopt` zurück (`juce_audio_plugin_client_VST3.cpp`, `getPosition()` endet auf `return info;`). Ein fehlender `ProcessContext` sieht damit aus wie „Transport vorhanden, Position 0". Ebenso wird `projektZeitSamples` nie gelöscht, wenn der Context wegfällt. Die Hostbrücke liefert seit `1e91d54` das ehrliche Bit (`processContextPresent`); der Umbau des Processors darauf gehört zu `SONDE-008/009` (P2) und wird dort mit den zeitgestempelten Featureframes zusammen gemacht — nicht nebenbei. |
| NAK-26 | 08-21 | **`pluginval.exe` liegt nur unter `%TEMP%`** (`C:\Users\phili\AppData\Local\Temp\pluginval.exe`, Fund beim Abschluss von SONDE-005a, out of scope). Der Beweiskanon zitiert es als Host-Härtungsschritt (`--strictness-level 8`), und die Manifeste von SONDE-003/003b/005a stützen sich darauf — aber Windows räumt `%TEMP%` auf, und auf dem Zweitrechner existiert es gar nicht. Wer es angeht: an einen dauerhaften Ort legen (z. B. `tools/extern/`, nicht ins Repo — Fremdbinärdatei) und im Beweis-Runner als eigener Kanon-Eintrag mit „Voraussetzung fehlt"-Zweig führen, damit sein Fehlen sichtbar ist statt stillschweigend übersprungen zu werden. |
| NAK-27 | 08-21 | **`offline_render_detection` ist keine Capability aus §53.6** (Fund in T2-Runde 1 zu SONDE-005a). Das v3-Schema hatte sie selbst erfunden; sie ist entfernt, weil der Capabilityvertrag §53.6 abschliessend ist und das Objekt strikt. Die Faehigkeit selbst wird von `EqCopHostProbe` (S3b) real gemessen. Wer es angeht: entscheiden, ob §53.6 im Entwurf um eine elfte Capability erweitert wird (dann Schema, Fixtures und alle drei Beine nachziehen) — oder ob Offline-Render dauerhaft nur eine Messgroesse ohne Capabilitybit bleibt. Kein Produktcode haengt heute daran. |
| NAK-28 | 08-21 | **`subscribe_session` hat keinen Gegenpfad** (Fund in T2-Runde 1 zu SONDE-005a). Alle anderen Paare sind vollstaendig (`preview_begin`↔`preview_end`, `audible_intervention_begin`↔`end`, `apply`↔`revert`, `experiment_begin`↔`abort`), aber die 25er-Liste aus Entwurf §33.3 kennt kein `unsubscribe_session`. Die CLAUDE.md-Invariante zu Gegenpfaden und §67 Punkt 2 (Unsubscribe) fragen es woertlich ab. Weil die 25 geschlossen ist und ein unbekannter Discriminator abgelehnt wird, ist das spaeter KEIN additiver Nachtrag. Wer es angeht: entweder §33.3 auf 26 erweitern, oder im Entwurf festhalten, dass das Verbindungsende der Gegenpfad ist — dann gehoert dieser Satz in §33.3 und nicht in eine Sitzungsnotiz. |
| NAK-29 | 08-21 | **Bedingte Feldpflichten im Transportstempel sind nicht durchgesetzt** (Fund in T2-Runde 1 zu SONDE-005a, gemessen: alle vier Faelle sind heute gueltig). `time_basis=project_samples` + `validity.project_time=true` OHNE `project_sample_start`; `time_basis=local_monotonic` MIT `validity.project_time=true` (direkter Widerspruch zu §32.3); `cycle.bounds_valid=true` ohne `start_ppq`/`end_ppq`; `cycle.bounds_valid=false` mit `derivation=validated_block_mapping`. Die Mechanik dafuer existiert (diskriminierte Union, in dieser Runde zweimal neu angewandt), aber der Schnitt ist groesser als ein Fix: `transportstempel` muesste ueber `time_basis` diskriminieren und `cycle` ueber `bounds_valid`. Gehoert zu `SONDE-009` (FeatureEngine v2, Zeit- und Validity-Vertraege), wo der Erzeuger dieser Felder entsteht. |
| NAK-30 | 08-21 | **Umbenennung zu „Nakama Studio" / Gen / Probeeq / Suna ist eine Identitätsänderung** (User 21.08.: „Nakama Gen = Main app · aktive sonde = Nakama Probeeq · passive sonde = Nakama Suna · Bundle = Nakama Studio"). `eq-copilot/identity/plugin-identities-v1.json` friert neben den CIDs auch `bundle` (`EQ-Copilot.vst3`) und `produktname` (`EQ-Copilot`) ein; die CIDs leiten sich laut Manifest aus `Evna`+`Eqcp`/`NkPr`/`NkAc` ab, nicht aus Namen. Zu klären, nicht entschieden: Braucht die Umbenennung ein `plugin-identities-v2.json`, und bleiben die CIDs dabei unverändert (dann laden alte FL-Projekte weiter), oder ändert sich mit dem Bundle-Dateinamen etwas, das FL als neues Plugin sieht? Erst messen (Scan-Fixture, `EqCopIdentityTest`), dann umbenennen — nie als Nebenbei-Refactor der `EqCop*`-Namen. |
| NAK-31 | 08-21 | **`tools/analyze-track.py` (Erzeuger der Golden-Referenz) liegt nur im FL-Studio-Repo** (`C:\Users\phili\FL-Studio\tools\analyze-track.py`, dazu `match-reference.py`; im Nakama-Repo 0 Treffer). `erzeuge_fixtures.py` sucht es erst unter `tools/`, dann per dokumentiertem Fallback unter `~/FL-Studio/tools/` (`lade_analyze()`, Z. 144–158); Kalibrier-Protokoll und M1-/CS1-Befund referenzieren es als `tools/…`; der GoldenTest misst gegen seine Achse. Ins Nakama-Repo holen (mit Abhängigkeiten), sonst ist die Golden-Referenz auf dem Zweitrechner nicht regenerierbar. |
| NAK-32 | 08-21 | **`eq-copilot/install/` ist gitignoriert** (`eq-copilot/.gitignore:2`): `Install-EQ-Copilot.ps1`, Staging-Bundle, vier Rollback-Bundles und `install-result.txt` existieren nur auf dem Desktop; CLAUDE.md nennt das Skript als den einzigen Installationsweg. Das Skript versionieren (ohne Bundles und Rollbacks — die bleiben Maschinenartefakte). |
| NAK-33 | 08-21 | **Parameterzahl je Band: 12 (Figma) gegen 13 (Entwurf §53.8).** Der Figma-Stand Probeeq (21.08.) zeigt BAND · TYPE · FREQ · GAIN · Q · MODE \| DYN · THRESH · RANGE · ATTACK · HOLD · RELEASE; §53.8 definiert je Slot zusätzlich `sidechain_source` (`none\|internal\|priority_sidechain`), und `enabled` erscheint im Figma-Stand nur als Griff-Zustand auf der Kurve. Offen, nicht entschieden: ist `sidechain_source` ein sichtbarer Parameter, ein Untermenü von DYN, oder entfällt er im ersten Active-Release? Entwurf-Errata (h). **Nachtrag 08-22 (SONDE-006):** der State trägt alle **13** IDs (`eq-copilot/schemas/state/nakama-parameter-v1.json`) — die mechanisch sichere Obermenge: ein nicht sichtbarer Parameter bleibt auf Default `none` und kostet nichts, ein später nachgeschobener hätte eine neue Layout-Version erzwungen (IDs werden nie umbenannt, §53.8). Offen bleibt damit nur die UI-Frage (sichtbar / Untermenü / unsichtbar) für den Figma-Stand — Entscheid beim User. |
| NAK-34 | 08-21 | **`EqCopHostProbeTest` zählt 89 nur mit PNG-Argument, der Kanon misst 85.** Mit Zielpfad laufen vier zusätzliche Bildbeweis-Prüfungen (`HostProbeTestMain.cpp:727-766`); das zweite Bild (Messzustand) entsteht per `replace("leerzustand","messzustand")` am Dateinamen (`:755-756`) — heißt die Datei nicht „…leerzustand…", überschreibt das Messbild das Leerbild still, und beide Prüfungen bleiben grün. Kanon-Eintrag B3b läuft ohne Argument (85/85 in allen Manifesten seit SONDE-005a). Wer es angeht: zwei feste Zielnamen statt Namensersetzung, und den Bildbeweis in den Kanon holen oder die 89 nirgends mehr als Kanonzahl führen. |
| NAK-35 | 08-21 | **Hör-Markierungs-Verriegelung: Code und bisherige Doku sagen Verschiedenes.** `PluginProcessor.cpp:202-207` (seit 22.08.; vorher `:197-202`): `(echtzeitOk ∨ testEchtzeit) ∧ (spielt ∨ ¬hatTransport) ∧ ¬isNonRealtime ∧ (editorOffen ∨ testEchtzeit)`. Die CLAUDE.md bis 21.08. (`e2b553e^`) beschrieb vier UND-Bedingungen („bewiesene Echtzeit ∧ Editor offen ∧ Transport ∧ !isNonRealtime"); Code ist Wahrheit, die Doku ist nachgezogen. Offen bleibt die Produktfrage: ist `¬hatTransport ⇒ erlaubt` (fail-open ohne Transportinfo) gewollt? Entwurf §0.1 führt den Zweig als „formal fail-open, praktisch tot" (weil `hatTransport` im VST3-Pfad immer wahr wird — NAK-24) und verlangt für die Zielmigration gültiges `playing=true`. Zusammen mit NAK-24 in `SONDE-008/009` entscheiden; dazu der User-Hörtest in FL (vormals NAK-03 §18), sobald Gen seine Oberfläche hat. |
| NAK-36 | 08-21 | **Broker-Release-Binaries sind älter als die Quellen.** `broker/target/release/eqcop-broker.exe` und `eqcop-broker-probe.exe` vom 18.08. 11:43; Quellen zuletzt 21.08. (`Cargo.toml` 17:50, `telemetrie.rs` / `contract_cross_language.rs` 18:26). `cargo test` baut nur `debug`. Vor dem nächsten Betrieb oder Pipe-Ende-zu-Ende-Lauf `cargo build --release --manifest-path broker/Cargo.toml`. |
| NAK-37 | 08-21 | **`EqCopAuxSpikeTest` (41 Prüfungen, `SONDE-004a`) läuft nicht im Kanon** — `tools/beweise.ps1` kennt es weder als „jetzt" noch als „geplant" (grep 0 Treffer), während das Schwester-Messgerät `EqCopHostProbeTest` Kanon-Eintrag B3b ist. Das Gerät trägt die Messung mit dem höchsten Erkenntnisrisiko (Termin A). Entweder als Kanon-Eintrag führen, bis S4 das Gerät entsorgt, oder die Asymmetrie im Runner begründen. |
| NAK-38 | 08-21 | **UI-Rückfluss-Liste Design ↔ Entwurf** (Errata (h)): Rollen 5 (Design-Spez.) vs. 7 (§9) · Suna-Kachel mit Name/Rolle-Bedienung (Design) vs. „null Hostparameter, Bedienung über Main" (§0.4) · Messzustände 5 vs. 2 · Arbeitsschritte 3 vs. 5 · Main-Übersicht: Figma-Stand 21.08. zeigt ein Quellen×Band-Gitter, Abnahme 20.08. sagte Overview+Detail ohne Heatmap — ob das Gitter die Heatmap aus §35.1 ist, klärt die Übersetzung. Rückfluss laut §0.4 Punkt 2 vor dem P3-Gate; Entscheid je Punkt beim User (Figma-Stand = neues User-Wort). | offen |
| NAK-39 | 08-21 | **„12 von 20 Kernfunktionen gewählt" (Entwurf §0.2/§22) ist nicht rekonstruierbar** — weder die Zwanziger-Liste noch die Auswahl existiert in einer Datei (Kontext-Inventur). Gilt als Codex-Annahme der Fassung 0.1 (Errata (m)). Frage an den User, wenn der Kernfunktionen-Satz je beschnitten oder erweitert werden soll. | offen |
| NAK-40 | 08-22 | **`instance_id` im State ist bytegleich zur Schema-1-`sensor_id`, die v3-Adresse verlangt hex32** (Fund in SONDE-006). §32.2 verlangt „`sensor_id` bleibt bytegleich als `instance_id` erhalten"; reale IDs sind `juce::Uuid` (32 Hex-Kleinbuchstaben) und passen, aber `adresse.instance_id` in `schemas/v3/eq-ipc-v3.schema.json` ist `$defs/hex32` (`^[0-9a-f]{32}$`), und die eingefrorenen Schema-1-Goldens tragen eine UUID **mit Bindestrichen**. Der State bleibt bytegleich (Vertrag `nakama-state-v2.md` §3); wer `SONDE-010` baut, legt fest, wie eine nicht-hex32-`instance_id` auf die v3-Adresse kommt (ableiten + im State mitführen, oder den Altstand sichtbar neu kennzeichnen) — nie still. |
| NAK-44 | 08-22 | **Termin A2 — PDC-Golden, Bus-Identität, Kanalreihenfolge, FL-Version** (T2-Befunde zu SONDE-004). Termin A hat einen getrennten, recall-stabilen Sidechain-Weg mit Versatz 0 gemessen, aber **kein Plugin im Aufbau meldete Latenz** — FLs PDC hatte nichts zu kompensieren, der Fall aus Entwurf §32.2 („Fehlreihenfolgen bei latenzbehafteten Instanzen") ist ungemessen; deshalb steht `aux_priority_sidechain` **unsupported** (§53.6 „PDC-synchroner echter Sidechain"). Ebenso unentscheidbar: welche Quelle auf welchem Aux ankam und die L/R-Reihenfolge (dieselbe Impulsdatei, L = R, auf allen drei Spuren); die FL-Version von Termin A ist nicht protokolliert (installiert 25.2.5.5319 und 26.1.4.5589). Termin A2: auf der Sidechain-Quellspur ein latenzmeldendes Plugin, je Spur ein anderer Impulszeitpunkt, einmal L-only/R-only, FL-Version notieren (Stand-Hinweis in `eq-copilot/docs/FL-TERMIN-A-AUX-PDC.md`). Hebt bei Erfolg `aux_priority_sidechain` und `aux_compare_pre` (T2-Runde 2: derselbe Maßstab — §32.2 „Topologie, Kanalreihenfolge und PDC" — gilt für beide Aux-Bits); zusammen mit einem Impulsgolden je Bus (NAK-43) auch `presentation_latency`. |
| NAK-43 | 08-22 | **`presentation_latency`: ein verworfener Wertwechsel ohne Protokoll** (Termin B, `host-probe-20260822-132644.json` `presentation_latency.verworfene_wertwechsel = 1`). Die Host-Probe hält je Richtung den ERSTEN gemeldeten Wert (Eingang 3 924, Ausgang 4 410) und zählt spätere andere Werte nur — welcher Wert wann kam (Render? Projektwechsel?), steht nicht im Bericht. Bevor `SONDE-009` die Presentation-Latency für die Cross-Probe-Ausrichtung nutzt: die Probe um eine kurze Werthistorie (Block, Richtung, alt → neu) erweitern und den Fall in FL reproduzieren. Bis dahin steht das Bit `presentation_latency` auf `unsupported` (T2-Runde 1 zu SONDE-004; `host-capabilities-fl-v1.json` `offen`). |
| NAK-42 | 08-22 | **Rust klassifiziert die ungültigen DTO-Fixtures nicht** (T2-Befund zu SONDE-006). `broker/tests/contract_cross_language.rs` kanonisiert und hasht nur `dto_gueltig`; die 15 `dto/ungueltig/` mit Grund liest nur C++ (`NakamaParameter::ausDtoText`) und Python (`erzeuge_state_fixtures.py`). §65 braucht es für SONDE-006 nicht, aber §55 („C++ und Rust klassifizieren jedes gültige und ungültige Fixture identisch") wird es am P1-Exit (G1) verlangen. Eigentümer: SONDE-016 (der Broker verifiziert dort `apply_transaction`-DTOs) — spätestens vor G1 ein Rust-DTO-Validator gegen `nakama-parameter-v1.json` mit denselben Grundwörtern. |
| NAK-41 | 08-22 | **Schema-2-Stand in einem alten Build (≤ 0.3.0 vom 16.08.)**: der alte `setStateInformation` verlangt den Baumtyp `EqCopilotState` und ignoriert `NakamaState` still — die Instanz bekäme eine frische zufällige `sensor_id` und Rolle `sensor` (kein Absturz, aber stiller Identitätsverlust). §55 „Rollback" meint den Weiterbetrieb des alten Builds mit seinen eigenen Projekten, nicht das Rückladen neuer Projekte. Folge für die Installation: der 0.3.0-Stand mit Schema 2 wird erst installiert, wenn der User das weiß (NAK-32-Skript, UAC-Klick); ein Rollback auf das 16.08.-Bundle nach dem Speichern eines Projekts mit dem neuen Build verliert dessen Messpunkt-Identität. Kein Code-Fix möglich (der alte Build ist ausgeliefert); Gegenmittel wäre ein Legacy-Spiegel im State, den §33.5 („keine zwei stillen Wahrheiten") verbietet. |
| NAK-45 | 08-22 | **Der OpenWiki-Lauf ist nicht abgeschlossen und liegt vollständig uncommittet** (Fund beim Hook-Audit, out of scope). `openwiki/_plan.md` liegt noch im Baum — laut Vertrag des OpenWiki-Servers entfernt erst die Finalisierung diese temporäre Datei; ein liegengebliebenes `_plan.md` heißt also: unterbrochen oder nie finalisiert. Untracked sind außerdem `openwiki/` (sechs Seitenordner), `AGENTS.md` und `.github/workflows/openwiki-update.yml` — letzterer ein täglicher `schedule`-Lauf (08:00 UTC), der nie feuert, solange er nicht im Remote liegt. `CLAUDE.md` trägt dazu einen uncommitteten OpenWiki-Abschnitt, der auf das ebenfalls uncommittete `AGENTS.md` verweist: die Verfassung verweist auf eine Datei, die es im Repo noch nicht gibt. Wer es angeht: den Lauf zu Ende führen oder die Reste entfernen — das ist eine Entscheidung über eine ZWEITE Wissensquelle neben `docs/` und gehört nicht nebenbei gemacht. Die Dateien stammen aus einer fremden Session; bis zur Klärung nicht anfassen. |
| NAK-46 | 08-22 | **Nimbalyst legt Workspace-Stümpfe im Repo-Wurzelverzeichnis an** (Fund beim Hook-Audit). `nakama/`, `Project Nakama/`, `Untitled Workspace/` — je nur ein `README.md` mit „Welcome to your new workspace!", angelegt 22.08. zwischen 17:31 und 17:36 — liegen untracked neben `eq-copilot/` und `design/`. Sie verrauschen `git status` und damit die Uncommitted-Warnung des `nakama-primer`, die vor parallelen Sessions schützt. Nicht gelöscht, weil Löschen unumkehrbar ist und die Ordner vom User stammen können: sobald sein Wort vorliegt, entweder weg oder in `.gitignore`. |
| NAK-47 | 08-22 | **Die Hör-Markierung schneidet hart, statt auszublenden** (Fund aus G0, aus Codex' widerlegtem Gate-1-Pfad verifiziert geborgen; `docs/beweise/G0.md` §3.1). `hartAus()` (`eq-copilot/plugin/src/HoerMarkierung.h:419-424`) setzt `fade = 0.0` sofort. Fällt `erlaubt` weg, während die Markierung hörbar ist (Editor schließt, Transport stoppt, Echtzeitbeweis verfällt), springt das Signal im nächsten Block abrupt vom gefilterten auf den Originalpfad — hörbarer Klick. Kein Bruch von Gate 1 (das Audio wird ja gerade **nicht** mehr verändert), aber ein Bruch des Startbudgets §49.3 „A/B-Zustandswechsel: kein Klick; identischer Zustand nullt innerhalb numerischer Toleranz". Dieselbe harte Kante steht in `HoerMarkierung.h:281` (`n > wetKapazitaet` ⇒ `hartAus()`). Fix wäre ein kurzer erzwungener Ausfade statt des Sofortschnitts — verlangt aber Sorgfalt, weil der Neutralpfad danach **bit-transparent** bleiben muss (MarkierungTest). Eigentümer offen; spätestens wenn die Markierung aus dem Provisorium in die Figma-UI wandert. |
| NAK-48 | 08-22 | **Der gevendorte JUCE-VST3-Wrapper nimmt eine `CriticalSection` im Audiocallback** (Nebenbefund von Codex in G0, **nicht** eigenständig verifiziert — vor jeder Handlung an der Quelle prüfen). Behauptet: `_deps/juce-src/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp:3714-3716, 3815-3823, 4050` sperrt im Callback, während `setActive()` (`:2809-2812`) denselben Mutex von einem anderen Hostthread hält; der Audiothread wartet dann über `juce_ScopedLock.h:79` / `juce_Threads_windows.cpp:62`. Das ist fremder Code außerhalb `eq-copilot/plugin` und damit außerhalb des G0-Scopes, zielt aber genau auf **Gate 2** („ein IPC-, Broker-, Datenbank- oder UI-Fehler blockiert den Audiothread"), das ab G2 scharf ist. Vor G2 prüfen: gilt das für unsere Bus-Konfiguration, und ändert der Bridge-Patch etwas daran? Falls bestätigt, ist es ein Grundgesetz-Bruch, den wir nicht selbst geschrieben haben — Umgang (Patch erweitern oder Bit dokumentieren) gehört dann entschieden, nicht stillschweigend hingenommen. |
| NAK-49 | 08-22 | **22 bestätigte C++-Befunde aus dem G0-Lauf, keiner auf der P0-Fläche** (`/c-review`, 13 Cluster, zwei Judges; Manifest `docs/beweise/G0.md` §4, Rohdaten regenerierbar unter `.c-review-results/`, gitignoriert). Verteilung: **2 HIGH** — `ACCESS-001` (`src/PipeClient.cpp:186`: `CreateFileW` ohne `SECURITY_SQOS_PRESENT \| SECURITY_IDENTIFICATION`, ein lokaler Pipe-Namensbesetzer kann den DAW-Token impersonieren) und `STR-002` (`:336`: `juce::String::fromUTF8` auf Peer-Bytes ohne UTF-8-Prüfung, JUCE validiert nur per `jassert`); beide selbst an der Quelle nachgemessen. **8 MEDIUM** in `PipeClient.cpp` (Handle-Race und verworfener Rückgabewert bei `CancelSynchronousIo` `:130`, unbegrenzte JSON-Rekursion `:211`, kein Lesetimeout `:316`, `:274`), `PluginProcessor.cpp` (kein Exception-Riegel in `setStateInformation` `:427`, UTF-8-Lauf über ein `.flp`-Label `:715`) und `state/NakamaState.cpp:352` (unbegrenzte Allokation/Rekursion in `ValueTree::readFromStream`). **12 LOW**, darunter drei Build-Härtungspunkte (`CMakeLists.txt`: kein CFG, keine gehärtete STL, `/GL /LTCG` nur im Shipping-Target) und vier `jassert`-Prüfungen, die im Release verschwinden. Sachliche Zuordnung: die Pipe-Befunde gehören zu **SONDE-010/011**, wo der IPC-Weg ohnehin neu gebaut wird (§48.4 fordert dort Impersonation + SID-Prüfung, siehe NAK-21); die State-Parserbefunde zu SONDE-006-Nacharbeit; die Build-Härtung zu SONDE-007b (Installer-/Zielmanifest). Nichts davon blockiert G0 oder S8 — aber keiner der 22 darf still verschwinden (Bauaufteilung §6.6). |
| NAK-50 | 08-22 | **Der User will den Hub im Nimbalyst-GUI statt auf der Briefing-Seite** — Wortlaut 22.08. abends: „EIGENTLICH ist die seite unnötig noise, es wäre mir lieber alles hier im nimbalyst GUI zu haben". Das berührt den Entscheid von heute Vormittag („das ist der neue und einzige hub", Register-Zeile 22.08.) und muss vor dem Umbau als Register-Eintrag präzisiert werden — **noch nicht ins Register übernommen**, weil der Wortlaut eine Präferenz („wäre mir lieber") ist und nicht klärt, ob die Seite abgeschaltet oder nur entlastet wird. **Auslöser** war die Erkenntnis, dass nichts die Seite automatisch aktuell hält: Stand der Seite 9/34, lokal 11/34, real 12/34 bei 13 Commits Drift; die Kette `hub.json` → `hub_sync.py senden` → Seite hängt daran, dass eine Session daran denkt, und riss heute, weil `hub.json` uncommittet bei einer parallelen Session lag. **Was die Seite heute leistet und was ersetzt werden muss** (User-Worte 22.08.): (a) anzeigen — Plan, „bei dir", Figma-Stand *mit Bild* („ich muss sehen können um was es geht"), ersetzt durch `docs/PLAN-STAND.md` (erzeugt, seit `2e8141a`) plus ein Bilder-Blatt; (b) **asynchron antworten** — „baue eine antwortfunktion für mich ein, dass ich zu jedem punkt stellung nehmen kann, dann musst du nur noch die seite anschauen um infos zu bekommen": das ist die tragende Funktion, sie darf beim Umbau nicht verlorengehen; Nimbalyst-Ersatz wäre ein Antwort-Blatt im Repo, das der User direkt im Editor beschreibt und das der SessionStart-Hook vorliest — ohne Netz, ohne Deploy, versioniert in git; (c) Bilder hochladen — im Repo trivial, Dateien danebenlegen. **Vorteil des Umbaus:** eine Wahrheit weniger, kein Deploy für neue Fragen oder Bilder, keine Sync-Kette die reißen kann. **Kosten:** `briefing-hub/` (Subtree, Codex-Vorarbeit) wird tot, und CLAUDE.md-Abschnitt „Hub" plus drei SessionStart-Hooks müssen umgeschrieben werden. Nicht nebenbei machen. |
| NAK-51 | 08-22 | **OpenWiki: die taegliche Auffrischung laeuft ins Leere, weil `OPENAI_API_KEY` als Repo-Geheimnis fehlt.** Gemessen 22.08. abends: `gh secret list` ist leer, `gh run list` zeigt keinen Lauf. Der Workflow `.github/workflows/openwiki-update.yml` liegt seit `ba6d6c8` im Hauptzweig und ist damit scharf (`schedule: 0 8 * * *`, taeglich 08:00 UTC, dazu `workflow_dispatch`); er startet also, installiert `openwiki@0.3.3` und bricht beim Schritt „Run OpenWiki" ab, weil `OPENWIKI_PROVIDER: openai` mit `OPENWIKI_MODEL_ID: gpt-5.6-terra` einen Schluessel braucht. **Still** — sichtbar nur im Actions-Bereich auf GitHub. Setzen kann das nur der User: `gh secret set OPENAI_API_KEY` (fragt den Wert interaktiv ab, damit er nirgends im Klartext landet). Zwei weitere Geheimnisse sind optional und betreffen nur die LangSmith-Ablaufverfolgung (`OPENWIKI_LANGSMITH_API_KEY`, `LANGSMITH_API_KEY`) — ohne sie laeuft der Rest. **Zustand sonst gesund:** `openwiki/_plan.md` und `_skeleton.md` sind nicht mehr im Baum, der Initiallauf ist also finalisiert (berichtigt damit NAK-45, das den Lauf noch als unabgeschlossen fuehrte); die Seiten liegen unter `openwiki/` in fuenf Bereichen, `AGENTS.md` und der CLAUDE.md-Verweis sind seit `ba6d6c8` committet. Der Workflow committet nicht direkt, sondern legt einen Pull Request auf Zweig `openwiki/update` an — der Merge bleibt eine bewusste Handlung. **Klarstellung zum Modellvertrauen (User 22.08. abends, Wortlaut):** „gpt IST WAS anderes. das galt nur für gemini. gpt vertraue ich." Der Fremdmodell-Riegel (Register 22.08.) bindet **nur Gemini**; GPT schreibend im OpenWiki-Lauf ist ausdruecklich gewollt. Ebenfalls sein Wort: „das workspace hier baut auf openwiki als kontextsystem auf" — OpenWiki ist damit Grundlage, nicht Beiwerk, und wird nicht als zusaetzliche Wissensquelle gegen `docs/` abgewogen. |
| ~~NAK-52~~ | 08-22, S8 | **GESCHLOSSEN 23.08. mit `8e32baf`** (S9/SONDE-007b Abschnitt 1): `cmake/NakamaIdentitaet.cmake` liest den Manifesteintrag, das Bauskript traegt kein Identitaetsliteral mehr, und `EqCopIdentityTest` ist im selben Aenderungssatz umgestellt — der Quellfrost misst jetzt „steht keine zweite Wahrheit mehr da", nicht „steht das Literal noch da". Beide Class-IDs bytegleich, 93 Pruefungen gruen (`docs/beweise/SONDE-007b.md` B1/B2/B5). Der urspruengliche Text: **Die Identitaet steht als Literal im CMake, nicht aus `plugin-identities-v1.json` gelesen.** Entwurf §53.4 sagt: „Identitaet kommt ausschliesslich aus `plugin-identities-v1.json` ueber die duennen Target-Schichten." S8 hat die eine Haelfte dieses Satzes gebaut und bewiesen — der Kern sieht keine Identitaet (`docs/beweise/SONDE-007a.md`). Die andere Haelfte steht aus: `juce_add_plugin(EqCopilot …)` traegt `COMPANY_NAME "evenacadia"`, `PLUGIN_MANUFACTURER_CODE Evna`, `PLUGIN_CODE Eqcp`, `PRODUCT_NAME "EQ-Copilot"` als Literale in `eq-copilot/plugin/CMakeLists.txt`. **Bewusst nicht in S8 geaendert**, aus zwei Gruenden: (a) „duenne Target-Schichten" (Plural) entstehen erst mit den drei Zielen in `SONDE-007b` — bei genau einem Ziel ist die JSON-Lesung Zeremonie ohne Nutzen; (b) `EqCopIdentityTest` friert die CMake-**Quelle** heute in genau dieser Textform ein (`tests/IdentityTestMain.cpp:257-263`: `zielBlock.contains("PLUGIN_CODE " + …)`) — eine JSON-Lesung wuerde dieses Bein brechen und muss es im selben Aenderungssatz auf den neuen Weg umstellen (dann: liest CMake die JSON, und stimmt das gebaute `moduleinfo.json` weiter mit ihr ueberein). **Eigentuemer: `SONDE-007b` / S9.** Bis dahin ist die eingefrorene Identitaet doppelt niedergelegt (JSON + CMake-Literal) und von B1 gegeneinander gemessen — kein Risiko, aber auch nicht, was §53.4 verlangt. |

## Geparkt mit der Prisma-Studie (User 21.08.2026: „Familie; Prisma nur Studie")

Weder offen im Sinne von Arbeit noch erledigt. Statusblatt:
`eq-copilot/design/prisma-studie/STATUS.md`. Wird nur wieder bewegt, wenn der
User die Studie ausdrücklich reaktiviert.

| ID | Seit | Punkt |
|---|---|---|
| NAK-11 | 08-17 | Unicorn-Studio-Weg: Offline-/Selbsthosting der Szenen-JSON braucht den „Legend"-Plan; SDK-Lizenz („nur für legitime Unicorn-Studio-Projekte") vor einem JUCE-WebView-Einsatz im ausgelieferten Plugin klären. Design-Phase (CDN-Embed) ist unkritisch. **Geparkt 08-21:** die Unicorn-Lichtwelt gehört zur Prisma-Studie; ohne Studie kein WebView-Einsatz, ohne Einsatz keine Lizenzfrage. |
| NAK-14 | 08-18 | Lesbarkeits-Test der Befund-Verkörperung ist bisher nur an EINEM Befund gedacht: Kein Entwurf aus dem /diverge-duo-Lauf (12 Optionen) wurde gegen den Alltagsfall MEHRERE gleichzeitige Befunde (3–4 nebeneinander, ggf. auf verschiedenen Flächen) geprüft — genau der Fall, für den der 2-Sekunden-Massstab des Profis gilt. Ebenso ungenutzt als Träger: die drei Flächen/das Drehen und der Bündel-Modus. Vor dem Bauen einer Verkörperung mitprüfen. **Geparkt 08-21:** Befund-Verkörperung im Glas ist Prisma-Studie; die Plugin-UI kommt aus Figma. |
| NAK-16 | 08-19 | **ST-Map-Live-Refraktion** (User-Freigabe 19.08., ersetzt den NAK-15-Ansatz nach dessen Verwurf). **Pipeline recherche-gehärtet** — verbindlich ist `eq-copilot/design/prisma-studie/docs/2026-08-19-stmap-live-refraktion.md` (seit 21.08.; vorher docs/research/) (5-Worker-Deep-Research, Primärquellen, Blender-Fakten am installierten 5.1.2 verifiziert): Refraction BSDF statt Glass · 32f-EXR statt PNG-Hi/Lo · Box-Filter 0,01 + Denoise aus · Offset-Form → RG16F (LINEAR = WebGL2-Kern) · R/B-Normalisierung (B = Gewicht + TIR-Maske) · Lerp-Composite mit Glanz-Pass · Dispersion per 3-Tap. Erst EIN Probe-Still mit Probe-Checkliste beweisen (Maßstab: Glasinneres ändert sich mit dem Hintergrund — Ähnlichkeit zum alten Frame ist als Metrik VERBOTEN), dann 72er-Sequenz. Einstieg: `docs/NEXT-SESSION.md`. **Stand 19.08. (cba6bd6, 602b20a): Probe-Still GEBAUT und BESTANDEN** — 74,2 % der Glaspixel ändern sich mit dem Hintergrund (Messwerte `renders/stmap/messung.json`, Checkliste komplett beantwortet: B nicht Fresnel-gewichtet, 64 Samples genügen, Half-Float trägt, RGBA16F ok). Offen bleibt: User-Sichtung des lebenden Blatts (`stmap-probe.html`, Optik-Abnahme inkl. Dispersion-Stärke + Glanz-Licht-Rig), dann 72er-Sequenz (~18 min: ST-Maps 4 s + Glanz 11 s je Frame) und Integration in Prototyp/Schlieren-Blatt (dort den getContext-Patch für preserveDrawingBuffer mitnehmen). **Geparkt 08-21:** Pfade meinen den Stand vor dem Umzug nach `eq-copilot/design/prisma-studie/`; die regenerierbaren ST-Map-Daten wurden am 21.08. entfernt (Regenerierung: STATUS.md). |
| NAK-17 | 08-19 | **Prisma-Mesh: Seitenflächen-Normalen zeigen nach INNEN** (Winding in `prisma-material-still.py` `from_pydata`; am evaluierten Depsgraph gemessen). Cycles rendert Glas trotzdem korrekt (Shading-Normalen werden geflippt) — alle abgenommenen Renders entstanden SO. Nicht nebenbei „fixen": ein Flip ändert potenziell MNEE-/Fasen-Nuancen aller Folge-Renders gegen den abgenommenen Stand. Nur zusammen mit einer bewussten Neu-Abnahme drehen. Bis dahin: Spiegelwinkel-/Normalen-Mathe immer am evaluierten Mesh messen, nie aus der Eckenliste herleiten. **Geparkt 08-21** mit der Studie. |
| NAK-18 | 08-19 | **Boden-Kontakt-Ebene widerspricht dem Zielvertrag:** `renders/dreh-boden/` (Taste B im Blatt) ist aus der alten Dark-Field-Studiowelt gebacken; `eq-copilot/design/prisma-studie/docs/visuelles-zielbild-hoerkompass.md` (seit 21.08.; vorher docs/) §6 erlaubt Kontaktlicht/Bodenwirkung nur aus DERSELBEN Lichtwelt wie der lebende Hintergrund. Bei der NAK-16-Integration entscheiden: Kontakt-Ebene aus der lebenden Welt neu herleiten (z. B. gespiegeltes Live-Sampling analog zur ST-Map) oder ersatzlos aus dem Begleiterbild nehmen. Gleiche Prüfung gilt fürs Glanz-Licht-Rig (aktuell statisches Kanten-Paar, kühl-blau — nah an der Nexus-Palette, aber nicht AUS ihr). **Geparkt 08-21:** der Zielvertrag selbst ist „Alles nur Studie" (User 21.08.). |

## Geschlossen

| ID | Geschlossen | Punkt |
|---|---|---|
| NAK-01 | 08-21, Kontext-Interview (User: „alle alten sind alt"; Aufräum-Freigabe Frage 20: alte Design-Richtungen ins Archiv) | **Geschlossen ohne Umsetzung:** das Tiefenfeld-Musterblatt liegt im Archiv (`eq-copilot/design/archive/alt-2026-08/nakama-spectral-field-claude.html`), ein Design-Freeze findet nicht mehr statt — die Plugin-UI kommt aus Figma. Ursprungstext: Design-Freeze ausstehend: User kalibriert das Tiefenfeld-Musterblatt im Browser; „Werte kopieren"-JSON in den Chat = Freeze → JUCE-Voreinstellung. |
| NAK-02 | 08-21, Kontext-Interview (User: „alle alten sind alt"; Archiv) | **Geschlossen ohne Umsetzung:** Bauplan 2.0 liegt im Archiv (`docs/archiv/NAKAMA-SPECTRAL-FIELD-BAUPLAN.md`); der JUCE-Umbau der Hauptansicht nach seinem §13 ist kein Ziel mehr; Gen bekommt seine Oberfläche aus Figma über `design/`. Ursprungstext: JUCE-Umbau der Hauptansicht nach Bauplan 2.0 §13 (12 Schritte) — startet erst NACH NAK-01. |
| NAK-03 | 08-21, Kontext-Interview (Material-Kit-Front: „Nie abgenommen – bleibt Provisorium"; Hub-App seit 18.08. kein Produktteil — CLAUDE.md „Was NICHT mehr gilt") | **Geschlossen:** §4/§11–15 der Testanleitung setzen die gestrichene Hub-App und ihren 🎚-Knopf voraus (Stand-Hinweis in `eq-copilot/docs/FL-TESTANLEITUNG.md`); §16–17 prüfen das Material-Kit-Provisorium, an dem keine Arbeit mehr stattfindet. Die formalen FL-Messungen der Sondenfamilie sind Termin A/B. Rest ohne Nachfolger: der Hörtest der Hör-Markierung (§18) — bei NAK-35 mitgeführt. Ursprungstext: Beim User: FL-TESTANLEITUNG §11–17 (M2-Roundtrip · M3-Karten · Graph/„KURVE STEHT") + §18 Hör-Markierung (Puls/Solo hören, Render-Beweis). Plugin 0.3.0 ist installiert. |
| NAK-04 | 08-21, Kontext-Interview (User: „Termine bald; bis dahin S7") | **Ersetzt** durch die formalen FL-Termine A (`eq-copilot/docs/FL-TERMIN-A-AUX-PDC.md`, Messgerät `EqCopAuxSpike`) und B (`…-B-HOSTZEIT.md`, Messgerät `EqCopHostProbe`) — mit Klickliste, Messgerät und JSON-Bericht statt formloser Meldung. Ursprungstext: 16-Instanzen-/PDC-/Offline-Render-Verhalten = Alltagsbeobachtung, User meldet formlos. |
| NAK-05 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-Roadmap M3b; die Sondenlinie ist seit 20.08. beschlossen: „okay dann fangen wir damit nächste session an") | **Geschlossen:** M3b gehört zur M-Roadmap des alten EQ-Copilot (Benchmark-Studie, Abschnitt „M3b — Lernfenster sichtbar + Korridor"); die Sondenlinie setzt Kalibrierung neu auf — Entwurf §34.3 Konfidenz, §49.4 Evaluationskorpus, P4/P5 (`SONDE-013/014`). Ein „Korridor" als globale Zielkurve widerspricht außerdem der Invariante `ltasReferenzDb ≠ Sollkurve`. Ursprungstext: M3b: Korridor-Kalibration aus even-Bounces (Benchmark-Studie, nächste Stufe). |
| NAK-06 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-Roadmap M3c; Sondenlinie seit 20.08. beschlossen) | **Geschlossen:** die Inhalte sind in der Sondenlinie neu definiert — PRE/POST-Kettenprüfung = Kernfunktion 5 (Entwurf §10, §38), Passagen = §32.4, Masking = Roadmap 3 (§47.1); die M3c-Fassung auf dem alten Plugin wird nicht gebaut. Ursprungstext: M3c: PRE/POST-Karte, Hover-Inspekt, Passagen, Masking (Benchmark-Studie). |
| NAK-08 | 08-21, Kontext-Interview (User: „Nein – raus aus dem Produkt") | **Geschlossen:** keine KI-/Claude-Erklärschicht im Produkt; der Advisor ist regelbasiert (CLAUDE.md Wahrheitskern; Entwurf-Errata (e)). Ursprungstext: Claude-Anbindung: `--bare` scheitert an OAuth → Agent-SDK-/API-Adapter nötig (M4-Thema; Grundgesetz: Aufruf nur auf bewussten Klick). |
| NAK-09 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-M3-Linie; Sondenlinie seit 20.08. beschlossen) | **Geschlossen:** das Kalibrier-Protokoll kalibriert die Befundkarten-Schwellen des alten Plugins („kommt mit M3b-Profil", `eq-copilot/kalibration/KALIBRIER-PROTOKOLL.md`); der Messkern wird als FeatureEngine v2 (`SONDE-009`) mit eigenen Goldens neu gebaut, die Evidenzqualität folgt §49.4. Das Teststück und `verify_testsong.py` bleiben als Fixture-Quelle; `analyze-track.py` ist NAK-31. Ursprungstext: Kalibrier-Protokoll Runde 2 (helle Instrumente ODER EQ-Fehler-Blindtest), `eq-copilot/kalibration/`. |
| NAK-12 | 08-21, Befund nach Kontext-Interview (Hub-App seit 18.08. kein Produktteil — CLAUDE.md „Was NICHT mehr gilt") | **Geschlossen:** der Entwurf kennt die Komponente nicht (grep `Sensorübersicht\|Svelte` 0 Treffer); Profilbindung und Sensor-Status sind in der Sondenlinie Aufgabe von Gen (Entwurf §3.1, §35.1 Quellenübersicht, `SONDE-012`). Die Referenzkopie `broker/sensoruebersicht-referenz/Sensoruebersicht.svelte` bleibt als Verlauf liegen. Ursprungstext: Sensorübersicht heimatlos: Das Svelte-Panel (Profilbindung, Sensor-Status) flog mit der Broker-Herauslösung aus der Hub-App; Referenzkopie in `broker/sensoruebersicht-referenz/`. Braucht ein neues Zuhause (eigene Mini-UI, CLI oder Plugin-Editor) — bis dahin Bindungen nur per JSON-Datei + Broker-Neustart. |
| NAK-15 | 08-19, `7de964d` | **Prisma in der App ohne starren Hintergrund** (User-Anforderung 19.08.). Umgesetzt in der Nacht 19.08.: 72 RGBA-Drehframes `renders/dreh-frei/` (film_transparent, Plate+Boden kameraunsichtbar, Kamera identisch zur bewiesenen JS-Projektion) + 72 Boden-Kontaktframes `renders/dreh-boden/` (Prisma lichtwirksam aber unsichtbar, offline Alpha aus Helligkeit — Shadow Catcher war gemessen der falsche Weg, die Pfützen sind Glanzreflexe); Blatt komponiert transparent (clearRect, `?frames=dreh` Vergleich, Taste B/`?boden=0`), Pixel-Beweis: neben der Silhouette exakt Body-Farbe, Ebene 0 scheint durch. Alte Frames bleiben als Archiv/Prototyp-Quelle. **Nachtrag 19.08.: Ergebnis vom User VERWORFEN** („spiegelung des alten hintergrundes eingebacken … beleuchtung komplett falsch … billig zusammengeklebt") — die Silhouetten-Freistellung stimmt, aber das GLASINNERE trägt weiter die alte Konserve. Nachfolger: **NAK-16** (ST-Map-Live-Refraktion). |
| NAK-19 | 08-20, `ab80522` | Aufgeloest durch **Produktentscheid 20.08.** (Entwurf §0.3): Hoerkompass-Zielvertrag gilt der eigenstaendigen Prisma-App, das Master-Plugin bekommt eine konventionelle Arbeits-UI — Landkarte und Zielvertrag leben in zwei Oberflaechen. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. **Nachtrag 21.08. (Kontext-Interview):** gegenstandslos — der Hörkompass ist „Alles nur Studie" (User), die Plugin-UI kommt aus Figma; die Kollision existiert nicht mehr, auch der Auflösungsgrund „zwei Oberflächen" ist damit Studie (Entwurf-Errata (c)). |
| NAK-20 | 08-20, `ab80522` | In Fassung 0.4 uebernommen: `JUCE_VST3_CAN_REPLACE_VST2=0` ist Teil des eingefrorenen Identitaetsmanifests (§31.2/§44.1/§53.5, `SONDE-001`); die vier abgeleiteten CIDs stehen wortgleich in §31.2. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-21 | 08-20, `ab80522` | In Fassung 0.4 korrigiert: §48.4 traegt die verbindliche Reihenfolge (Connect → Hello lesen → impersonieren → SID pruefen → RevertToSelf → annehmen/ablehnen) samt `FALSE`-Regel; `security_vectors` (§66.2) hat den Negativtest. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-22 | 08-20, `ab80522` | Uebernommen: `SONDE-004` ist die erste Welle (Entwurf §65 + NEXT-SESSION). Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-23 | 08-20, `ab80522` | In Fassung 0.4 erledigt: (a) der widerspruechliche Buendelungssatz ist entfernt, (b) §53.4 haelt fest, dass der gemeinsame Kern keine `JucePlugin_*`-Konstanten sieht, (c) `experiment_abort` ist im Nachrichtenvertrag. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. **Nachtrag 21.08.:** `docs/bauaufteilung-sonden.md` §1.3 trug bis zum 21.08. die Gegenposition („1 Batch = N Frames EINER Quelle + Wrapper") als „Festlegung bleibt gültig" weiter; am 21.08. auf Entwurf §33.1 und das gebaute `.fbs` berichtigt (eine Liste von `(source, frame)`, höchstens ein Frame je Quelle, Regel `quelle_doppelt`). |
