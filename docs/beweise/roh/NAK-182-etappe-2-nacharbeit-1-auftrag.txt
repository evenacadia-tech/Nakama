NAK-182 — Etappe 2, Nacharbeit 1 (Runde 1 von 3). Auftrag des Dirigenten (2026-09-06) an einen frischen Opus-Bauer (max).

Repo C:/Users/phili/Projekte/Nakama, Branch master, Basis = `git rev-parse HEAD` beim Start (der Dirigent hat 054883f gemessen: Worktree sauber, Kanon GRÜN 54/54 auf 3ca500f). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach origin master nach `git pull --rebase` bei sauberem Worktree. Kein Anfassen von docs/beweise/G4.md, docs/beweise/SONDE-013.md, docs/beweise/NAK-180.md, docs/beweise/NAK-181.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/. Vor jedem Commit tools/dirigent/pruefliste.md abhaken und im Manifest nennen, wo jede Zeile gemessen wurde. Landminen: MSVC-Stack 1 MiB — Engines/Prozessoren in Tests auf den Heap; keine GPU-Batchläufe; PowerShell 7 deutet deutsche Anführungszeichen in doppelt zitierten Strings als Begrenzer — Textbausteine nur in wörtlichen Here-Strings oder per Write-Werkzeug; Fixtures mit LF und ohne BOM, wie der Erzeuger sie schreibt; beim Kanon- oder Logstart kein Löschbefehl (die Umleitung `*>` überschreibt; eigene Dateien nur über `[System.IO.File]::Delete` auf exakte, selbst angelegte Pfade).

REFERENZ: die Verhaltensmatrix docs/beweise/NAK-182.md §2 in der jeweils jüngsten Fassung (Etappe 1b, Matrixnacharbeit 1 und 2; Matrixprüfung 3 PASS), der Bau in §6, die Regeln R1 bis R7 (docs/beweise/roh/NAK-182-auftrag.txt), R1a, MN1-1 bis MN1-7, MN2-1 und MN2-2 (docs/beweise/roh/NAK-182-*-auftrag.txt). Die Erstprüfung (Codex Astra max, Thread 01a075e5-6643-71f3-a2a3-a3ea78a0fa17, docs/beweise/roh/NAK-182-erstpruefung-054883f.txt) ist NEEDS_WORK mit fünf Defekten; der Dirigent hat jeden an der Quelle bestätigt. Dein Auftrag sind GENAU diese fünf Defekte mit den Regeln unten — nichts darüber hinaus, keine Neben-Refactors, kein weiterer Produktcode, kein Schemafeld, keine Umbenennung bestehender Bezeichner.

DEFEKTE (wörtlich aus dem Urteil) und die REGEL, die jeden schließt:

EP-01 — „DEFEKT gegen R4/E4b und N-21: Die Gainzeile liest über perzentilDb unmittelbar FeatureFrame-Arrays; PRE und POST werden hier weder serialisiert noch aus eigenem Wire-Text zurückgelesen. Auch die Leiter ruft nur quantisiere16 auf Testzahlen auf, statt die Auflösung erzeugter Snapshots zu messen. Damit fehlt die zugesagte C++-Wirehälfte der gekoppelten Kette." (eq-copilot/plugin/tests/Sonde013EventWireTest.cpp:1658-1662, :1686-1697)
  Regel NR-01: Jede Zahl der C++-Hälfte kommt aus dem WIRE-TEXT: PRE- und POST-Frame der echten FeatureEngine werden mit dem echten Serializer (`nakama::evidenz::evidenceSnapshotAlsJson`, wie N-18d es schon nutzt) zu `evidence_snapshot`-Texten; der Test parst den Text (`juce::JSON`), liest `verteilung.p50` (Ganzzahlen und das Encoding-Wort `q_db_0p01_i16`) und dekodiert mit der Vertragsformel (Wert geteilt durch 100). Gainzeile: je Band mit Bit ist POST minus PRE gleich G aus der Fixture innerhalb ±0,1 dB. Leiterzeile: N = 20 Pegel im Abstand 0,01 dB (Startpegel und Schrittzahl aus der Fixture) laufen je durch Engine und Serializer; die 20 Ganzzahlen aus den Wire-Texten sind streng monoton und benachbarte unterscheiden sich um genau 1 — `quantisiere16` auf Testzahlen ist kein Ersatz. Ist ein 20-facher Enginelauf zu langsam (Zeit messen, in §7 nennen), darf die Leiter die P50-Werte EINES Enginelaufs je Band um k·0,01 dB versetzt durch den echten Serializer schicken — aber gelesen wird immer der Wire-Text. Rotbeweise M-83-aufloesung und M-83-skala neu fahren: Zehnteln der Werte fällt an der Gainzeile, Encoder auf q_db_0p1_i16 fällt an der Leiterzeile (der Schlussteil über die Bereichsprüfung in §6.3 gilt dann nicht mehr — neu messen, nicht übernehmen).

EP-02 — „DEFEKT gegen N-18: Der Sweep speist zwar 300, 900 und 2000 Hz ein, wertet aber ausschließlich das 900-Hz-Band aus. Die Matrixzeile verlangt drei Trägerfrequenzen je Blockgröße; §6.4 begründet nur die Toleranz von 0,25 rad, nicht das Weglassen zweier Träger. … Ein Sample Versatz ergibt bei 2000 Hz 0,2618 rad und überschreitet diese Toleranz, während die ausgewerteten 900 Hz grün bleiben." (Sonde013StereoGoldenTest.cpp:1179-1183)
  Regel NR-02: `sweep_known_delay` wertet je Blockgröße alle drei Träger (300, 900, 2000 Hz) aus — je Träger das Band, in dem er liegt, mit Präsenzbit und Phasenfehler gegen +2·pi·f·tau; die Toleranz steht als EINE Zahl im Test und in N-18 (0,25 rad mit der Begründung aus §6.4 Nr. 6, oder 0,1 rad, wenn die Messung sie trägt — Matrixzeile und Test sagen dasselbe, §7 nennt den gemessenen Fehler je Träger und Blockgröße). Rotbeweis M-82 neu fahren: der Ein-Sample-Versatz fällt jetzt auch an `sweep_known_delay` (2000 Hz), §7 nennt, welche Zeilen fallen.

EP-03 — „DEFEKT gegen N-19: Für unkorrelierte Kanäle verlangt die Matrix zusätzlich „keine Lag- oder Polaritätsempfehlung". Das neue Prädikat prüft ausschließlich Basis, Korrelationsbit und Korrelation; die bereits erhobenen Kohärenz- und Phasenfelder beeinflussen das Ergebnis nicht. Eine fälschlich vorhandene Phase würde deshalb bei niedriger Korrelation als korrekte Antwort bestehen." (Sonde013StereoGoldenTest.cpp:1201-1204)
  Regel NR-03: `sweep_uncorrelated_channels` prüft je Blockgröße zusätzlich, dass keine Phase gesetzt ist (`phaseGesetzt == false`) und keine Polaritätsempfehlung entsteht — dieselben Felder und Schwellen, die der bestehende 512-Abschnitt für M-12 nutzt (an der Quelle nachschlagen, nicht neu erfinden). Rotbeweis: Prädikat ohne die Phasenprüfung und ein Lauf, in dem die Phase gesetzt ist (Manipulation im Test-Speiser: korrelierte Kanäle unter dem Namen des unkorrelierten Falls) → die Zeile fällt; nach Rücknahme grün. Ablage docs/beweise/roh/NAK-182-rot-M-82-empfehlung.txt.

EP-04 — „DEFEKT gegen S-07: Diese Erwartungen prüfen lediglich die Mitgliedschaft zweier Literale in einer lokalen Testmenge. Sie erzeugen keinen ungültigen Korpusfall und erreichen die tatsächliche Ablehnung in main nicht. … A26 weist unbekannte Wahrheiten aktuell korrekt ab, aber der zugesagte Selbsttest misst diese Eigenschaft nicht." (tools/eq-copilot/pruefe_p4_korpus.py:500-504)
  Regel NR-04: Die Wahrheitsvalidierung ist EINE Funktion (z. B. `_pruefe_wahrheiten(faelle, mengen) -> list[str]`), die A26 auf den geladenen Korpus anwendet und die S-07 mit einem synthetischen Fall aufruft: Wahrheit außerhalb `ERLAUBTE_WAHRHEITEN` → genau eine rote Zeile mit dem Fallnamen; Gegenteil (Wahrheit innerhalb) → keine. Kein zweiter Prüfweg, keine lokale Kopie der Menge. Rotbeweis: die Funktion auf „immer leer" → S-07 fällt (A27 rot); zurück → grün. Ablage docs/beweise/roh/NAK-182-rot-A27-wahrheit.txt.

EP-05 — „DEFEKT gegen E4b/Form B: Die generierte Fixture enthält Pegel und Auflösung, aber keine deklarierte Signalfrequenz oder Materialdauer. B16 legt stattdessen Amplitude, 1000 Hz, 48000 Hz und die Laufbegrenzung lokal fest; damit ist das zugesagte Material nicht über die gemeinsame Datei gebunden." (tools/eq-copilot/erzeuge_v3_fixtures.py:3002-3008; Sonde013EventWireTest.cpp:1629-1637)
  Regel NR-05: Die Fixture trägt das Material als Zahlen — Signalfrequenz, Samplerate, Dauer (oder Blockzahl) und Amplitude von PRE — neben Pegel L, Gain G, Auflösung und Leiter; der Erzeuger schreibt sie, A8 `--pruefen` hält sie bytegleich, und B16 liest Frequenz, Samplerate, Dauer und Amplitude AUS der Fixture statt aus lokalen Konstanten (die Rust-Hälfte liest weiterhin die Snapshot-Texte; ändert sich ihr Inhalt durch die Ergänzung nicht, bleibt sie unberührt — sonst mitziehen). Rotbeweis: ein Byte der Fixture geändert → A8 `--pruefen` rot (vorhandener Beweis M-83 wiederholen, falls die Datei sich ändert).

BETROFFENE BEINE (nur diese fahren; kein voller Kanon in dieser Runde — der läuft beim Abschluss): B16 `EqCopSonde013EventWireTest`, B19 `EqCopSonde013StereoGoldenTest`, A26 `py -3.13 tools/eq-copilot/pruefe_p4_korpus.py`, A27 `py -3.13 tools/eq-copilot/pruefe_p4_korpus.py --selbsttest`, A25 `--pruefen`, A8 `py -3.13 tools/eq-copilot/erzeuge_v3_fixtures.py --pruefen`, A5 `py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py`, `cargo test --test sonde013_verdrahtung` (im Ordner broker), dazu `EqCopNullTest`. Rohausgaben in docs/beweise/roh/NAK-182-nacharbeit-1-beine-<sha>.txt; Rotbeweise wie oben benannt (bestehende Dateien nicht überschreiben, neue Fassung als eigene Datei mit Suffix -n1).

MANIFEST docs/beweise/NAK-182.md: neuer Abschnitt „§7.1 Nacharbeit 1" mit der Zuordnung EP-01 bis EP-05 → NR-01 bis NR-05 → geänderte Zeile/Test/Rotbeweis, die gemessenen Laufzeiten, die neu gefassten Matrixzeilen (N-18, N-19, N-21, N-23, S-07 — alte Fassung bleibt als Verlauf, die neue steht als „Nacharbeit 1" markiert darunter), Prüflisten-Abhakung und die Kopftabelle (Zeile „Phase" auf „Etappe 2: Nacharbeit 1 abgeschlossen (Basis <sha>, Ende <sha>), betroffene Beine grün, Wiederprüfung 1 offen"). §7 selbst (Erstprüfung) hat der Dirigent angelegt — nicht umschreiben.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen — jede Zahl der C++-Hälfte aus dem Wire-Text, drei Träger wirklich ausgewertet, Phasenprüfung wirklich wirksam (Rotbeweis fällt), Validierung ein Weg, Fixture bytegleich und von B16 wirklich gelesen, kein Produktcode, keine Umbenennung. Commits mit Pathspec in logischen Schritten, pushen; Turn mit der letzten Zeile „NACHARBEIT 1 ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.
