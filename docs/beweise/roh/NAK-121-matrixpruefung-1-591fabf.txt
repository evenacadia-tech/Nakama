Der abgegrenzte Commit enthält tatsächlich keine Änderungen unter broker/, eq-copilot/ oder tools/, und die vorhandenen Testbinaries listen insgesamt 362 Tests. Die Spezifikation enthält jedoch mehrere nicht umsetzbare oder nicht messbar belegte Zusagen sowie direkte Konflikte mit C-06 und dem unveränderten Testvertrag.

Full review comments:

- [P1] Halte Geschwistertypen im sichtbaren Modulbereich — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:239-246
  Wenn `Stand`, `ClientStand` und `LinkStand` wie in §5.4 nach `coordinator/zustand.rs` wandern, können die geplanten Geschwistermodule `link.rs`, `liveness.rs` und `flush.rs` diese privaten Typen und Felder nicht lesen. Rust gewährt private Kindmodul-Elemente nicht den Geschwistern; `Write-Output 'mod p { mod a { struct S { x:u8 } } mod b { fn f(s:&super::a::S)->u8{s.x} } }' | rustc - --crate-type lib --emit metadata -o NUL` reproduziert E0603/E0616. Damit brechen die als einzeln übersetzbar zugesagten Aufteilungscommits; der Plan muss gemeinsame Elemente im Elternmodul lassen oder mindestens `pub(super)`-Sichtbarkeit vorsehen.

- [P1] Loese den unmoeglichen Listener-Deckel auf — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:166-166
  Bei ausgelasteten 96 Workern lässt `PIPE_INSTANZEN = MAX_VERBINDUNGEN + 2 = 98` exakt zwei bewaffnete Listener zu; eine jederzeit höhere Konstante überschreitet deshalb den unverändert zugesagten Pipe-Deckel. Zudem bindet `broker/tests/security_vectors.rs:327-350` diesen Zustand im Test `zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz` ausdrücklich an zwei Listener (`cargo test --manifest-path broker/Cargo.toml --test security_vectors zwei_listener_plus_96_worker_erhalten_cap_und_namensbesitz -- --exact`). NAK-135 kann daher nicht als A umgesetzt werden, ohne eine Grenze oder die Aussage eines bestehenden Tests zu ändern.

- [P1] Halte den H-16-Push hinter dem C-06-Cleanup — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:200-200
  Bei einer Nonce-Verdrängung muss H-16 den Push vor der Rückkehr aus `control_hello_registrieren` starten, aber `broker/src/coordinator.rs:864-875` gibt die zu schließenden Links erst an den Transport zurück und `broker/src/transport/server_v3.rs:2465-2468` beginnt deren Abbau erst danach. Der Push läuft somit vor Ingress-Schluss, Kopplungslösung, Joins und Trenncallbacks und verletzt C-06: vollständiger Cleanup vor jedem später gestarteten Push. Der deklarierte reine Coordinator-Test misst diese Transportreihenfolge nicht; `rg -n "ControlRegistrierung::angenommen|zu_schliessende_links" broker/src/coordinator.rs broker/src/transport/server_v3.rs` zeigt den Call-Trace.

- [P1] Ergaenze fuer abgeloeste Schreiber einen echten Abbruchweg — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:186-186
  Wenn ein etablierter Writer nach `join_mit_frist` hängen bleibt, hält der vorgeschlagene gemeinsame Griff zwar den Registereintrag, löst aber keinen weiteren Abbruch aus. `BootstrapFrist` entfernt die ID bereits bei `broker/src/transport/server_v3.rs:2379`, während der Wachhund bei `:1725-1743` ausschließlich noch fällige Bootstrap-IDs abbricht; nach dem Ablösen bei `:2907-2909` bleibt nur ein Serverstopp. Damit kann der zugesagte Test nicht zugleich null Handles und einen weiter annehmenden Listener beobachten. `rg -n "frist.erfuellt|faellig.contains|schreiber_abgeloest" broker/src/transport/server_v3.rs` reproduziert die fehlende Verbindung; H-02 benötigt einen expliziten Abbruchweg für abgelöste etablierte Writer.

- [P1] Entscheide H-06 auf genau einen Zustellpfad — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:190-190
  Bei mindestens einem nicht lesenden Abonnenten schreibt H-06 zugleich Fire-and-forget-Zustellung fest und erlaubt alternativ weiterhin synchrone Zustellung mit Gesamtbudget. Der angegebene Test misst nur die Tickdauer und bindet weder asynchrone Outbox-Kompaktierung noch Übersprungzähler, sofortiges Trennen oder die Reihenfolge Eviction vor Zustellung. Dadurch können verschiedene, unvollständige Implementierungen denselben Test bestehen; die Referenzmatrix muss einen Pfad wählen und jede zugesagte Nebenwirkung messbar zuordnen.

- [P1] Beweise H-20 auch im C++-Leser — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:204-204
  Bei einem Batch oberhalb `MAX_EINTRAEGE` kehrt der geplante Rust-Leser sofort zurück, während der C++-Leser in `eq-copilot/plugin/vertrag/NakamaTelemetrie.cpp:558-568` nach `eintraege_zu_viele` weiter über alle Einträge läuft. Die einzige neu benannte Grenzprobe liegt in `broker/tests/contract_cross_language.rs`, das ausschließlich den Rust-Leser ausführt; ohne Fixture- oder C++-Grenztest bleibt die zugesagte identische Verletzungsmenge unbelegt. `rg -n "eintraege_zu_viele|for \(flatbuffers::uoffset_t" broker/src/telemetrie.rs eq-copilot/plugin/vertrag/NakamaTelemetrie.cpp` zeigt beide divergierenden Pfade.

- [P2] Fuehre den zugesagten Clippy-Lint im Beweislauf aus — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:137-137
  Beim kanonischen Beweislauf wird `clippy::undocumented_unsafe_blocks` nicht ausgewertet: `tools/beweise.ps1:460,468,775` führt nur `cargo test` beziehungsweise `cargo build` aus, und `rustc -W help` kennt diesen Clippy-only-Lint nicht. Eine `[lints.clippy]`-Tabelle kann daher vorhanden sein, während alle 40 Beine grün bleiben und undokumentierte Blöcke unbemerkt bleiben; genau diese fehlende Clippy-Verdrahtung nennt auch der zugrunde liegende CARGOLINT-Befund. `rg -n "clippy" tools/beweise.ps1` reproduziert die fehlende Ausführung, sodass der deklarierte Test die A-Zusage nicht misst.

- [P2] Lege die Rueckgabefrist fuer H-13 fest — C:\Users\phili\Projekte\Nakama\docs\beweise\NAK-121.md:197-197
  Wenn der v2-Pool voll ist und getrennte Einträge enthält, fordert H-13 einen Sweep für Einträge „älter als die Frist“, nennt aber weder Wert noch Konstante; im aktuellen `broker/src/server.rs` existiert dafür ebenfalls keine Frist. Der angegebene Test misst keine Kanten bei Frist minus eins und exakt Frist, sodass der Worker beliebige Löschzeitpunkte wählen könnte. Damit fehlt der Matrixzeile die verlangte messbare Grenze und der Zeitpunkt, ab dem eine sichtbare getrennte Sensor-ID verschwinden darf.