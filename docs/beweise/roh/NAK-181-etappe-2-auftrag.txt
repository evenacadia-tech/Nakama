NAK-181 — Etappe 2 (Bau) nach Konvergenzentscheid. Zusatzauftrag des Dirigenten (2026-09-05).

Du bist der Bauer dieses Tickets (Session-Fortsetzung, frischer Opus max). Basis = `git rev-parse HEAD` beim Start (der Dirigent hat §5.6 committet). Es gilt der Abschnitt BAU aus docs/beweise/roh/NAK-181-auftrag.txt vollständig (Ticketgrenze V01, V02, V03, V05, V06, V09, V10, C3, C4, C5; Regeln R1 bis R9; Beine; Kanon abgekoppelt; Git-Regeln; Selbstaudit; Endzeile „ETAPPE 2 ABGESCHLOSSEN <end-sha>"), dazu die Matrix docs/beweise/NAK-181.md §2 bis §4 mit E1 bis E9 und N-01 bis N-42 (samt N-18b, N-18c, N-18d, N-41b; 46 Zeilen) als Referenz, die Regeln der Nacharbeiten R1a, R3a, R3b, R4a bis R4e, R5a, R5b, R6a, R7a, R7b (docs/beweise/roh/NAK-181-matrixnacharbeit-1/2/3-auftrag.txt, eingearbeitet in §2) — und die eine Regel aus dem Konvergenzentscheid §5.6, die du ZUERST in die Matrix einarbeitest und dann baust:

R4f — N-18 Rundungsnachweis: Der Wert `9.9999999999999999e-308` ist als Binary64 bitgleich mit `1e-307` (`0x0031fa182c40c60d`) und belegt die Text-gegen-Wert-Grenze nicht. Ersatz: `std::nextafter (1e-307, 0.0)` (= `9.999999999999997e-308`). Der Test prüft ausdrücklich drei Dinge: die Eingabe ist kleiner als `1e-307` (Wertvergleich), ihr auf 15 Stellen gerundeter Wiretext lautet `1e-307`, und `textriegel_bytes`/A5 nehmen ihn an. Rotbeweis: eine Implementierung, die den WERT statt den Text prüft, verweigert ihn → Test fällt. Zeile N-18 und die Randwerttabelle in E4 werden entsprechend berichtigt (eigener Commit, nur docs/beweise/NAK-181.md: „NAK-181 Etappe 2 Vorspann: R4f in N-18").

REIHENFOLGE:
1. Matrix nachziehen (R4f, nur docs/beweise/NAK-181.md, eigener Commit). Kopfzeile Phase: „Etappe 2 im Bau (Konvergenzentscheid §5.6)".
2. Bau nach §4.1 (Etappen B1 bis B9 in der dort festgelegten Reihenfolge), in allen betroffenen Sprachen: C++ Plugin (PluginProcessor, SourcesModel, FeatureEngine, Blindvergleich, NakamaEvidenz, ControlClient, neuer Kopf core/ipc/WireZahl.h, NakamaKanon-Extraktion, SondeProcessor, CMakeLists), Rust Broker (prepost.rs, prepost_verdrahtung.rs; Tests sonde013_prepost.rs, sonde013_verdrahtung.rs, sonde013_experiment.rs, contract_cross_language.rs), Fixtures + Erzeuger (tools/eq-copilot/erzeuge_v3_fixtures.py, eq-copilot/fixtures/v3/). Keine Schemaänderung (§2.4); `$defs/cycle`-Pflichtfelder `active`/`bounds_valid` werden im Serialisierer geschrieben (B5, §4.7 Nr. 3), nicht im Schema geändert. Landmine: MSVC-Stack 1 MiB — Engines/Prozessoren in Tests auf den Heap (`std::unique_ptr`).
3. Je Matrixzeile N-01 bis N-42 (46 Zeilen) den messenden Test fahren und im Manifest (§6 „Etappe 2 — Bau", Tabelle je Zeile: Test, Ergebnis, Rohdatei) eintragen — KEINE Zeile „gebaut, nicht gemessen"; Rotbeweise je Regel nach docs/beweise/roh/NAK-181-rot-<kennung>.txt (mindestens die in §4.1 benannten: B1 bis B9 je einer); Rohausgaben der Beine nach docs/beweise/roh/NAK-181-<sha>.txt.
4. Beine aus dem BAU-Abschnitt fahren: EqCopSonde013ExperimentUiTest, EqCopSonde013PassageStateTest, EqCopSonde013EventWireTest, EqCopSonde013StereoGoldenTest, EqCopSonde013DynamicsTest, EqCopSonde013PrePostGoldenTest, EqCopSonde012SourcesModelTest, EqCopSonde012ProjectReloadTest, EqCopStateMigrationTest, EqCopSchemaTest, EqCopNullTest, EqCopProbeeqNullTest, EqCopQueueStressTest, cargo test (sonde013_prepost, sonde013_verdrahtung, sonde013_experiment, contract_cross_language, coordinator_model), A5 pruefe_v3_vertrag.py, A8 erzeuge_v3_fixtures.py --pruefen. Dann der volle Kanon abgekoppelt (Befehl im BAU-Abschnitt: $log = nakama-nak181-kanon.log, Start-Process, warten bis EXIT=); kein Stand mit NOT RUN wird übergeben.
5. Manifest: §6 Bauabschnitte (je Etappe B1 bis B9: Ort, Änderung, Test, Rotbeweis), §6.x Messbilanz (46/46 Matrixzeilen gemessen, sonst ehrlich die Zahl), Abweichungen vom Bauplan, Nebenbefunde (NICHT ins Register — der Dirigent zieht nach); tools/dirigent/pruefliste.md abgehakt mit Messort je Zeile (§4.6 fortschreiben). Kopfzeile Phase: „Etappe 2 gebaut, Kanon <GRÜN n/n> auf <sha>, Erstprüfung offen".
6. Commits mit explizitem Pathspec in logischen Schritten (Produkt, Tests, Fixtures, Erzeuger, docs/beweise/NAK-181.md, docs/beweise/roh/NAK-181-*), `git pull --rebase` bei sauberem Worktree, Push nach jedem logischen Schritt. Kein Anfassen von docs/beweise/G4.md, docs/beweise/NAK-180.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/, eq-copilot/schemas/.
7. Turn mit der letzten Zeile „ETAPPE 2 ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.

SELBSTAUDIT vor jedem Commit wie im Ticketauftrag (Zahlenränder, NaN/Inf, `-0`, Locale, stale Closures, Save/Load-Symmetrie, binden↔lösen, beginnen↔abbrechen, Reload↔Reconnect, Kommentare und Header-Zusagen; Audio-Thread-Invarianten; Nulltest bitidentisch; Passthrough sampleidentisch; keine Schemaänderung). Zusätzlich die Fragen der vier Matrixprüfungen: (1) Erreicht irgendein Aufrufer von `zahlJson`/`zahl` den `"null"`-Pfad mit einem Wert, den eine Vorprüfung durchlässt? (2) Wird ein publiziertes Präsenzbit irgendwo genullt, bevor der Leser es sieht? (3) Nimmt `processBlock` eine der zwei neuen Sperren? (4) Reist nach dem Reload irgendein Byte, das eine Wirkung erzeugt, die es vor dem Reload nicht gab? Jede solche Stelle ist ein Defekt vor dem Commit.
