Quellvalidierung der Abschlussprüfung / Ticket NAK-312, Planschritt S25k — gebundener Auftrag des Dirigenten (2026-09-23)

Du bist ein frischer, LESENDER Validierer (kein Bauer einer Etappe, nicht der Prüfer — Codex-Thread 01a0cdcc-8b86-76b2-b9da-28617470cccb, gpt-6-astra xhigh —, kein Prüfer oder Validierer einer früheren Runde). Du prüfst den einen Befund des Rohurteils docs/beweise/roh/NAK-312-abschlusspruefung-urteil.md (NEEDS_WORK; ein Defekt [P2]: „Halte den Testzustand über beide Prozessoraufbauten am Leben — eq-copilot/plugin/tests/TransactionTestMain.cpp:4655-4657: Im regulären B7-Lauf liefert blockStaende() ein temporäres std::array. Dessen überladener operator[] liefert eine Referenz und verlängert die Lebensdauer des Arrays nicht: Am Semikolon wird es zerstört, sodass beide folgenden blockPruefling(blk, mitBand) einen bereits zerstörten Zustand lesen. Damit enthält der neue M-20-NaN/Inf-Test undefiniertes Verhalten und liefert keinen belastbaren, compilerunabhängigen Nachweis.") an der Quelle: existiert die zitierte Stelle, sagt sie das Behauptete, ist der gerechnete Ablauf stimmig und am Code reproduzierbar, und trägt eine Zusage (Gate-Satz, Matrixzeile, Regel R-312-n, Satz des Prüfauftrags, CLAUDE.md-Invariante) den Fall — oder schweigt sie?

Prüfbereich: der Zielstand der Abschlussprüfung e8c6f079f4331755f4a901c2715ce6df2324420a; für eq-copilot/plugin/tests/TransactionTestMain.cpp ist der Arbeitsbaum gleich diesem Stand (HEAD trägt darüber nur docs/**; prüfe das mit git diff --stat e8c6f079 HEAD -- eq-copilot). HEAD misst du zu Beginn und am Ende (git rev-parse HEAD) und nennst beide Werte; du liest nur (git status, git rev-parse, git log, git show, git diff, git blame, git grep, git ls-files und die Lesewerkzeuge), startest keinen Compiler, keinen Test, kein FL Studio, keinen Python-Interpreter, stagest, committest und pushst nichts und schreibst genau eine Datei: docs/beweise/roh/NAK-312-abschlusspruefung-validierung.md (neu, untracked, LF). Fremde Dateien (briefing-hub/, nimbalyst-local/) fasst du nie an.

Quellen für die Zusagenlage: docs/beweise/NAK-312.md — Gate-Text im Kopf (Zeile „Gate": „BEWEIS: docs/beweise/NAK-312.md mit Matrix, Rotbeweisen an B2, B6, B7, B14, B15, RT-Wache vor Parameterverarbeitung, vollem Kanon GRÜN und Laufzeit-Arm"), Matrixzeile M-20 in §6 (hostwerte_sind_blockgebunden, T3-01-05 Teil a, R-312-10 in der Fassung von E-312-5) und ihr Teilfall „nicht endliche Hostwerte am Blockrand" (E4-10, NaN-Ehrlichkeit; Bauerbericht der Etappe 3b in §21.5 bis §21.11, Rotbeweise), Regel R-312-34 (§56.3: die Behauptungszeilen sagen nicht mehr, als der Test misst), der Prüfauftrag docs/beweise/roh/NAK-312-abschlusspruefung-auftrag.txt (Befundklassen: DEFEKT verletzt „einen Test"; Ausschlüsse), CLAUDE.md „Tragende technische Invarianten" (NaN-Ehrlichkeit) und „Fortschritt erst nach Beleg"; im Code eq-copilot/plugin/tests/TransactionTestMain.cpp (blockStaende() um Zeile 4471, die Struktur Blockstand und ihr Feld satz, der Aufruf um Zeile 4610 in der Schleife, die Stelle 4655-4657, blockPruefling, fahreFolge), die Behauptungszeile von B7 in tools/beweise.ps1.

Du lieferst:
1. **Quelle:** jede zitierte Datei:Zeile geöffnet; Zitat der tragenden Zeilen (kurz): Rückgabetyp von blockStaende(), Definition von Blockstand und satz, die Zeile 4655 wörtlich, die Signatur von blockPruefling (nimmt sie eine Referenz auf satz oder kopiert sie?).
2. **Ablauf:** Bindet `const auto& mitBand = blockStaende()[1].satz;` an ein Unterobjekt eines Temporaries, dessen Lebensdauer verlängert wird, oder an das Ergebnis eines Funktionsaufrufs (std::array::operator[] liefert eine Referenz), sodass keine Verlängerung stattfindet (C++20 [class.temporary]/6: Verlängerung nur, wenn die Referenz an ein Temporary oder ein Unterobjekt über Member-Zugriff, Array-Subscript auf ein eingebautes Array, oder bestimmte Casts gebunden wird — ein überladener operator[] ist ein Funktionsaufruf)? Wird der Zustand danach noch gelesen (blockPruefling kopiert oder referenziert?) — Zeilen nennen. Ist der Ablauf des Prüfers damit reproduzierbar, oder liest blockPruefling den Satz sofort und kopiert ihn, sodass der Verweis zum Zeitpunkt des Lesens bereits ungültig ist (das wäre trotzdem UB, weil das Temporary am Ende der Deklarationsanweisung zerstört ist)? Gibt es im Diff des Tickets (git diff 090d0fa1...e8c6f079 -- eq-copilot/plugin/tests/) weitere Stellen mit demselben Muster (`= blockStaende()[`, `const auto& … = …()[…]` auf ein Temporary; grep)? Zeile 4610 (Range-for über blockStaende()) ist nach [stmt.ranged] verlängert — bestätige oder widerlege.
3. **Zusagenlage:** welcher Satz den Fall trägt — der Gate-Satz „Rotbeweisen an … B7", die Matrixzeile M-20 mit ihrem Teilfall, R-312-34, das Defektkriterium „verletzt … einen Test" — wörtlich zitiert; oder die Feststellung, dass alle schweigen.
4. **Urteil:** BESTÄTIGT / PRÄZISIERT / WIDERLEGT, mit Klassenvorschlag DEFEKT (Test mit undefiniertem Verhalten trägt den Rotbeweis nicht belastbar), LÜCKE oder HÄRTUNG, und der kleinste Fix (Kopie statt Referenz, oder das Array in einer lokalen Variablen halten) mit der Angabe, ob der Fix das Verhalten des Tests (Erwartung, Behauptungstext) unverändert lässt. Die Einordnung trifft der Dirigent.

Bericht (Markdown, kompakt; in Tabellenzellen ein Pipe als \| schreiben): Kopfzeile mit HEAD vorher/nachher; die vier Punkte; eine Tabelle Befund · Urteil · Klassenvorschlag · tragender Satz; eine Schlusszeile: FERTIG Validierung Abschlussprüfung NAK-312, <n> bestätigt, <n> präzisiert, <n> widerlegt.
