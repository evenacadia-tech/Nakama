Wiederprüfung S25e Etappe 3 / Ticket NAK-286, Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-14)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, git grep, cat, Get-Content, Get-FileHash); du startest keinen Compiler, kein cargo, keinen Test, keinen Kanon, kein FL Studio und keinen Python-Interpreter außer zum Lesen; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 23390bca92b0c1f9e98157da294336ef0b6a982b...6e305bbbac183e8aa32dc440bdf66b0834b355b7 -- broker/src/briefkasten.rs eq-copilot/plugin/tests/BriefkastenTestMain.cpp`
mit VORHER = 23390bca92b0c1f9e98157da294336ef0b6a982b (Stand vor der Runde; er trägt §33 des Dirigenten mit der Einordnung, der Regel P-15 und der Ursache (l)) und ZIEL = 6e305bbbac183e8aa32dc440bdf66b0834b355b7 (Commit des Bauers „NAK-286 Etappe 3 Nacharbeit 1: Defekt der Erstpruefung 1 geschlossen (Wurzelform der Anfrage P-15, Falltabelle i/j in beiden Sprachen), Ursache l geprueft, Beine gruen"; 2 Dateien außerhalb docs/**, +42/−10). HEAD trägt darüber hinaus nur docs/** (Manifest §35, Rohdaten, diesen Auftrag; kein Prüfgegenstand); der Code der zwei Pfade am HEAD ist der Code des ZIEL. HEAD muss vor und nach deinem Lauf identisch sein. Der übrige Ticketbereich der Etappe 3 (`git diff c8ca4425...8b3bfc3c` über broker/src/briefkasten.rs, broker/src/lib.rs, broker/src/lebenslauf.rs, broker/tests/briefkasten.rs) wurde in der Erstprüfung 1 bereits geprüft und ist hier nicht Prüfgegenstand.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus dem vorigen Urteil, docs/beweise/roh/NAK-286-etappe-3-pruefung-1-03a91fe6.txt, Thread 01a0a191):
1. „[P2] Weise JSON-Arrays als Anfrage zurück — broker/src/briefkasten.rs:112-113. DEFEKT gegen R-286-1/F-2: Bei vorhandenen Briefkastenordnern wird `["nakama.diagnose.anfrage.v1","00000000000000000000000000000000"]` für eine noch unbeantwortete Kennung akzeptiert und beantwortet. Vorgeschrieben ist jedoch „ein JSON-Objekt mit genau den Schlüsseln“; das Schema setzt `type: object`. Serdes abgeleiteter Struct-Deserializer akzeptiert auch Sequenzen; `deny_unknown_fields` verhindert das nicht. Der C++-Leser lehnt denselben Inhalt ab. Die Objektform muss zusätzlich erzwungen und dieser Ablehnungsfall getestet werden."
   Schließende Regel P-15 (docs/beweise/NAK-286.md §33.2, wörtlich): „Der Anfrageleser jeder Rolle prüft die Form des JSON-Wurzelwerts, bevor er Felder liest: nur ein JSON-Objekt ist eine Anfrage; ein Array, eine Zeichenkette, eine Zahl, `true`, `false` oder `null` als Wurzel ist fremd geformt, auch wenn Elemente positionell die Felder `format` und `anfrage_id` ergäben. […] die Objektform wird ausdrücklich erzwungen — zulässig ist ein eigener Visitor, dessen `visit_seq` ablehnt, ein Lesen als `serde_json::Value` mit Zwang auf `Value::Object` vor der Feldprüfung, oder die Prüfung des ersten Nicht-Leerraum-Bytes auf `{` wie im C++-Leser (`Briefkasten.cpp:186`); […] Die Falltabelle M-27/M-43 bekommt in beiden Sprachen die Varianten (i) „JSON-Array `["nakama.diagnose.anfrage.v1","<32 Nullen>"]` mit unbeantworteter Kennung" und (j) „JSON-Zeichenkette `"nakama.diagnose.anfrage.v1"` als Wurzel", Zusage wie (b): keine Antwort, kein Anlegeversuch, kein Logeintrag, Kennung nicht im Ring; die Tabellen bleiben zeichengleich (M-43). Rotbeweis (3) in beiden Sprachen: die Wurzelprüfung entfernt […] → (i) erzeugt eine Antwort → rot. Der C++-Leser ändert sich nicht (er erfüllt P-15 schon); `BriefkastenTestMain.cpp` bekommt nur die zwei Varianten und den Rotbeweis; Bein B30 grün."
   Dazu die gemeinsame Ursache (l) (§33.3): ein abgeleiteter Mechanismus trägt eine Vertragszusage nur so weit, wie sein Ableitungsvertrag reicht; die Nacharbeit prüft jede Stelle der Etappe, an der ein abgeleiteter Leser oder Schreiber eine Zusage aus F-2 bis F-7 trägt (Wurzelform, doppelte Schlüssel, Leerraum, BOM; Serialisierung des Umschlags; `antwort_feldmenge_wie_schema`; Anfragetext der Tests). Der Bauer hat seine Runde in §34 beschrieben (34.1 Defekt, 34.2 Rotbeweise, 34.3 Ursache (l) je Stelle mit einem zweiten Fund: der Anfragetext der Tests nahm `format` aus der Konstante des Lesers statt aus der Vertragsdatei, geändert mit Rotbeweis M-43 (4); 34.4 Beine, 34.5 Selbstaudit, 34.6 fünf datierte Abweichungen, 34.7 Messungen); §34 ist Kontext für dich, kein Prüfgegenstand.

GATE-TEXT, wörtlich (docs/beweise/NAK-286.md, Kopfzeile „Gate"; Quelle docs/plan/plan.json:313; hier die Schicht (3) und die GRENZE, die diese Etappe trägt): „(3) Messkanal ohne Klick, Diagnose-Briefkasten: liegt `%LOCALAPPDATA%\evenacadia\nakama\diagnose\anfrage.json`, schreibt das Plugin auf dem Message-Thread seinen Snapshot (`schreibeSnapshotDatei`) und der Broker sein Aggregat (`aggregat_schreiben`) in einen Antwortordner; ohne Datei geschieht nichts; keine neuen Host-Parameter, Identität unberührt, Audio-Thread unberührt; der Prüfer bestätigt Verhaltensneutralität für Audio und State (Nulltest, Passthrough, State-Fixtures). […] GRENZE: keine Legacy-Umbenennung (NAK-30), keine Produktions-Pipe für Testbroker, kein Speichern des Diagnoseprojekts aus der Automatik, kein Beenden eines FL mit fremdem Projekt." Die Verhaltensmatrix steht in docs/beweise/NAK-286.md §4 (M-27 in §4.3, M-43 in §4.4, mit Varianten (i), (j) und Rotbeweis (3) seit dieser Runde); die Regeln R-286-1 bis R-286-5 (§2) mit P-1 bis P-15; der Technikentscheid T-37 (§7.1). Die Riegel, die es messen: A4 `cargo test --manifest-path broker/Cargo.toml` (`kennungsregeln_wie_plugin`, `anfrage_und_zeitstempel_wie_plugin`, `pfadtraversal_unmoeglich` in broker/src/briefkasten.rs), B30 `EqCopBriefkastenTest` (`fremdeAnfragenWerdenIgnoriert` in eq-copilot/plugin/tests/BriefkastenTestMain.cpp); Beinlauf docs/beweise/roh/NAK-286-etappe3-nacharbeit1-beine.txt; Rotbeweise docs/beweise/roh/NAK-286-rot-M-43-nach1.txt, NAK-286-rot-M-27-nach1.txt, NAK-286-rot-M-48-nach1.txt; Skript docs/beweise/roh/NAK-286-rotskript-etappe3-nacharbeit1.ps1. Jüngster voller Kanon: GRÜN — 67/67, 1 stillgelegt (A15), Exit 0, 14.09.2026 21:18 Uhr auf `8b3bfc3c` (docs/beweise/roh/NAK-286-c8ca442-dirty.md); die Runde fuhr nur die betroffenen Beine (A4 Exit 0, B30 Exit 0 mit 168 Fällen, Clippy 94 wie am Basisstand, A32 Exit 4 als Hinweis).

Du beantwortest genau zwei Fragen:
1. Ist der genannte Befund am Zielstand geschlossen (ja/nein, mit Beleg)? Prüfe dazu: die Wurzelprüfung liegt vor jedem Feldzugriff und vor `serde_json::from_slice` (broker/src/briefkasten.rs:112-120) und überspringt genau den JSON-Leerraum (Leerzeichen, Tab, LF, CR), den serde_json und der C++-Leser überspringen; (i) und (j) stehen in beiden Falltabellen mit denselben Bytes (Rust broker/src/briefkasten.rs um :1669-1670, C++ eq-copilot/plugin/tests/BriefkastenTestMain.cpp:1188-1190) und die Tabellen sind weiterhin zeichengleich; die Zusage „keine Antwort, kein Anlegeversuch, Kennung nicht im Ring" wird gemessen; die Rotbeweise M-43 (3) und M-27 (3) fallen an der Zeile, die die Zusage trägt (Rohdateien, Exit rot ungleich 0 mit Trägerzeile, grün 0, SHA-256 nach der Rücknahme gleich; `git diff -- eq-copilot/plugin/core/diagnose/Briefkasten.cpp` leer); alles Übrige aus F-2 (genau die zwei Schlüssel, je einmal, Kennungsmuster, kein BOM, Größengrenze) bleibt an seiner Stelle und gemessen.
2. Hat der Fixdiff etwas gebrochen — einen Test, eine Matrixzeile (besonders M-25 bis M-29, M-41 bis M-53, M-76), einen Satz des Gate-Textes, eine Regel P-1 bis P-15, eine Invariante aus CLAUDE.md (Grundgesetz „nichts Ungefragtes", Identität eingefroren, State verlustfrei, Schemas sind Verträge, nie ein Testbroker auf der Produktions-Pipe)? Prüfe besonders: `vertrag_format()` liest die Schemadatei nur im Testbau und nur einmal, der Produktteil (bis `mod tests`) ist außer der Wurzelprüfung unverändert; die Änderung an `pfadtraversal_unmoeglich` (Anfragetext über `anfrage_text`) hält die Bytes der Anfragen gleich; die Ringprüfung (`ring_belegt`) in der Falltabelle greift auf keinen Zustand außerhalb der Testsperre zu; der C++-Leser ist unverändert und die zwei C++-Varianten ändern keinen anderen Fall.

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen und Ausschlüsse wie in Variante A (DEFEKT / LÜCKE / HÄRTUNG; docs/** ausgeschlossen; die datierten Abweichungen §7.3, §21.6, §24.7, §27.7, §31.6 und die fünf in §34.6 — insbesondere der C++-Rotbeweis (3) mit einem Arrayzweig statt bloßer Lockerung, weil die bloße Lockerung gemessen grün bleibt (§34.6 Punkt 1), und die Regressionswache (j) ohne eigenen Rotbeweis (§34.5 E) — sind kein Befund; Härtungen NAK-291 bis NAK-299, NAK-276, NAK-255, NAK-289; A32 Exit 4 (NAK-292) und Clippy 94 bei Grenze 91 (NAK-299, keine Fundstelle in einer Datei der Runde); broker/src/briefkasten.rs mit 1 995 Zeilen unter der Grenze 2 000; die Reihenfolge der Umschlagschlüssel zwischen Rust (alphabetisch) und C++ (Schemaliste) ist ein Hinweis für den Runner der Etappe 4 (§34.3 Punkt 2), kein Befund; die Runner-, Szenario- und MCP-Hälften der Etappen 4 und 5; die zwei fremden untracked Ordner briefing-hub/ und nimbalyst-local/; Prüfliste keine Anforderung; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; Kosmetik, Stil, theoretische Randfälle ohne Zusage, Ticketfremdes: nein).

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — der Befund geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz von Gate, Matrix, Regel oder Invariante bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
