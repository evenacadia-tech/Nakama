Wiederprüfung S25b / Ticket NAK-223, Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 1c93d0fce97e341d3acf2e3a6a4a34ca1f8c8dfc...afc2d699bbe68ee9ea46c0800b084d2623c9f6a2 -- tools/plan/gesundheit.py`
(ZIEL = afc2d699bbe68ee9ea46c0800b084d2623c9f6a2, das Ende der Nacharbeit 1; HEAD beim Lauf ist ein Nachfolger von ZIEL, der gegenüber ZIEL ausschließlich diesen Prüfauftrag unter docs/beweise/roh/ trägt — `git diff --stat afc2d699..HEAD -- tools .claude broker eq-copilot` ist leer — und vor wie nach deinem Lauf identisch bleibt; VORHER = 1c93d0fc, der Stand vor der Runde, er enthält gegenüber dem geprüften Stand d6a0bfda nur Rohdateien unter docs/beweise/roh/, keinen Code). Der übrige Ticketbereich wurde in der Erstprüfung (Thread 01a08530-2bfd-7162-a0c8-d3ff058cc551, docs/beweise/roh/NAK-223-erstpruefung-a1722a2.txt) bereits geprüft und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat; `py -3.13 tools/plan/gesundheit.py --selbsttest` und der Messlauf ohne `--clippy` sind lesende Läufe und erlaubt — falls Python 3.13 bei dir nicht startet, wie in der Erstprüfung, nenne das unter „nicht geprüft“); kein cargo, kein cmake, kein Kanon; du schreibst nichts.

BEFUNDE, die diese Runde schließen sollte — wörtlich aus dem Urteil der Erstprüfung, mit der Regel des Dirigenten (docs/beweise/roh/NAK-223-nacharbeit-1-auftrag.txt):

DEFEKT 1: „Erfasse bei const fn den tatsächlichen Helfernamen — tools/plan/gesundheit.py:532-533. Bei einem unbenutzten `pub const fn helfer()` erfasst `ELEMENT` das Schlüsselwort `fn` als Namen. Jede andere Funktionsdefinition gilt anschließend als Verwendung; der aufruferlose Helfer bleibt ungemeldet. Lesende Reproduktion: `py -3.14 -B -c "import runpy; g=runpy.run_path('tools/plan/gesundheit.py'); print(g['finde_dead_code']([('t.rs',None)], {'t.rs':'#[allow(dead_code)]\npub const fn helfer() {}\nfn main() {}\n'}))"` liefert `[]` statt eines Treffers.“ Regel R-D1: Die Namenswahl überspringt alle Sichtbarkeits- und Funktionsqualifizierer (`pub`, `pub(crate)`, `pub(super)`, `pub(in …)`, `const`, `unsafe`, `async`, `extern "…"`, `default`) und liefert den Bezeichner unmittelbar nach dem Elementschlüsselwort; `const NAME: T` bleibt von `const fn name` unterscheidbar, ebenso `unsafe impl`/`unsafe trait`; Selbsttestfälle für `pub const fn`, `pub(crate) unsafe fn`, `pub async fn`, `pub const HELFER: u8`, `unsafe fn` je mit und ohne Verwendung; Rotbeweis `NAK-223-rot-deadcode.txt` erweitert; die Reproduktion des Prüfers liefert genau einen Treffer `helfer`.

DEFEKT 2: „Verwirf Clippy-Ergebnisse eines fehlgeschlagenen Prozesses — tools/plan/gesundheit.py:522-526. Bricht Clippy beispielsweise mit Compilerfehler E0308 und Exit 101 ab, steht die Fehlerdiagnose bereits in `fund`; dadurch überspringt `not fund` die Fehlerbehandlung. Die unvollständige Messung erscheint als gültige Warnungszahl. Reproduktion ohne Cargo-Ausführung: `py -3.14 -B -c "import runpy,pathlib,subprocess,shutil,json; g=runpy.run_path('tools/plan/gesundheit.py'); shutil.which=lambda _: 'cargo'; subprocess.run=lambda *a,**k: subprocess.CompletedProcess([],101,json.dumps(dict(reason='compiler-message',message=dict(level='error',code=dict(code='E0308')))),''); print(g['miss_clippy'](pathlib.Path.cwd()))"` liefert `(1, ['?:0:0 E0308'], '')`; der virtuelle Gesamtmesslauf endet damit sogar mit Exit 0.“ Regel R-D2: Ein Exitcode ≠ 0 des gestarteten `cargo clippy`-Prozesses ist ein Werkzeugfehler, unabhängig von gelesenen Diagnosen: `miss_clippy` liefert keine Zahl, sondern den Fehler (Exitcode, letzte stderr-Zeile), und der Gesamtlauf mit `--clippy` endet mit Exit 2 „Werkzeugfehler: …“, nie mit 0 oder 4; „NICHT MESSBAR“ bleibt dem Lauf ohne `--clippy` und dem Fall „cargo nicht gefunden“ vorbehalten; Selbsttestfälle mit Prozessersatz (Exit 101 mit/ohne Diagnosen → Fehler; Exit 0 mit 3 Warnungen → 3; Exit 0 ohne Diagnosen → 0); Rotbeweis `NAK-223-rot-clippy.txt` erweitert; die Reproduktion des Prüfers liefert keine Zahl mehr.

GATE-TEXT, wörtlich (docs/plan/plan.json S25b): „`tools/plan/gesundheit.py` misst die Codebase (… `cargo clippy`-Warnungen; aufruferlose `#[allow(dead_code)]`-Helfer; …) … druckt eine Tabelle und gibt Exit 4, sobald eine Schwelle gerissen ist. … Die Schwellen entscheidet der Bauer aus der Messung des Iststands am Basis-SHA, nie geraten … GRENZE: kein Produktcode, kein Umbau bestehender Beine.“ Exit-Semantik des Werkzeugs (Docstring): 0 = keine Grenze gerissen, 2 = Werkzeug kaputt, 4 = Befund.

Du beantwortest genau zwei Fragen:
1. Ist jeder der zwei Befunde am Zielstand geschlossen — je Befund ja/nein, mit Beleg (Datei:Zeile der Regex bzw. der Exitcode-Prüfung, die Selbsttestfälle mit Namen, der Rotbeweis-Abschnitt; die beiden Reproduktionen des Prüfers am Zielstand nachgefahren, soweit lesend möglich)?
2. Hat der Fixdiff etwas gebrochen — die übrigen Maße, die Exit-Semantik 0/2/4, die Tabellenform, den Determinismus, die Schwellen aus Manifest §2.3 (28/91/35 Ratschen; 2 000/1 500/200; Kontextgrenzen), das Runner-Bein A32 (`--mit-selbsttest`, Exit 4 → HINWEIS) oder den bestehenden Selbsttest?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen (DEFEKT / LÜCKE / HÄRTUNG) und Ausschlüsse wie in der Erstprüfung: docs/** kein Prüfgegenstand; Register NAK-215 bis NAK-222, NAK-224, NAK-225, NAK-228 bis NAK-234; die Nebenbefunde N-1 bis N-4 aus docs/beweise/NAK-223.md §4; Heuristik-Fehlerklassen aus §2.5 sind erklärte Grenzen; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; tools/dirigent/pruefliste.md keine Anforderungsquelle; Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — beide Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Regel bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
