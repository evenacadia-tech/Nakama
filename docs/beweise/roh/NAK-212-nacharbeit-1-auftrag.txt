NAK-212 — Nacharbeit 1 nach der Erstprüfung. Auftrag des Dirigenten (2026-09-07).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA dieser Runde = ea63a3aaf2efe0bac5374f54fba583f99a66e553 (= `git rev-parse HEAD` beim Start; prüfe das, Worktree sauber). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann push. tools/dirigent/pruefliste.md vor jedem Commit abhaken und im Manifest nennen, wo jede Zeile gemessen ist.

LIES ZUERST: docs/beweise/NAK-212.md vollständig (Kopf, §2 Matrix — sie ist die Spezifikation, von drei Codex-Matrixprüfungen abgenommen —, §3a bis §3c Bauetappen, §4, §5), dann docs/beweise/roh/NAK-212-auftrag.txt (Regeln R1–R6, Ticketgrenze), dann das Urteil der Erstprüfung docs/beweise/roh/NAK-212-erstpruefung-1-ea63a3a.txt (Codex gpt-6-astra max, lesend, Thread 01a07d94-83a1-7ce1-bb41-c061e977cbfb, HEAD vor und nach dem Lauf ea63a3a). Der Dirigent hat alle vier Befunde an der Quelle bestätigt.

TICKETGRENZE — genau diese vier Defekte, mit ihrer Ursache, sonst nichts:

NR-01 (E1/E6, hypothese.rs:1990-1993 `korrelation_gerichtet`): `(r != 0.0).then_some(r)` macht eine messbare Nullkorrelation (beide Reihen mit Streuung, r exakt 0) zu `None`; E1 verlangt `Some(0.0)`. Folge: `zusammenhang_verschieden` (E6) hält ein nach E6 getrenntes Paar für ungetrennt, der Führende bleibt `mittel` statt `hoch`. Regel: `None` nur, wenn die Korrelation nicht messbar ist (zu kurze Reihen, eine Reihe ohne Streuung); bei messbarer Korrelation `Some(r)` einschließlich `Some(0.0)`. Rotbeweis: Onsetreihen `[1,1,3,3]` gegen `[1,3,1,3]` je dreimal wiederholt ergeben `Some(0.0)`, nicht `None`; dazu der E6-Fall aus dem Urteil (gleicher positiver Uplift, verschiedener quantisierter Gesamtrang, Nullkorrelation gegen konstante Onsets → getrennt, Führender `hoch`).

NR-02 (N-42, broker/tests/sonde014_gegenbeispiele.rs:402-483 `d2_fuehrender_wird_gegen_jeden_geprueft`): Der Kandidat `m` hat vier laute und acht leise Fenster; sein Median liegt auf dem leisen Wert, durch `k >= schwelle` landen alle Fenster in `mit`, Uplift und Wiederholbarkeit liefern nichts — `m` rangiert hinter C (A > C > M statt A > M > C). Der Vergleich nur mit dem Zweitplatzierten erzwingt schon Enthaltung; die Zusage „gegen jeden Überlebenden" ist nicht isoliert gemessen. Regel: Der Test baut die Reihenfolge A > M > C tatsächlich (M mit eigenem, von A und C verschiedenem Zusammenhang und höherem quantisierten Rang als C) und prüft die Reihenfolge und die Zusammenhangsverschiedenheit von M gegen A als Vorbedingung (assert), bevor er A = `mittel` wegen C erwartet; Gegenprobe im selben Test: ohne C wird A `hoch`.

NR-03 (R6/N-38, broker/tests/sonde014_gegenbeispiele.rs): Das Kettenbein trägt sieben Fälle; es fehlen aus den 15 vereinbarten Ketteneingaben des Patches docs/beweise/roh/G5-gegenbeispiele-bf92891.patch (broker/tests/g5_gegenbeispiele.rs) mindestens die Passage `[0,11)`, der zweite `experiment_begin` mit gleicher `passage_id` (f5b), zwei Master (f8), Master ohne Fenster (f8b), Kanalwechsel vor/nach (f4), drei Sonden auf einem Kanal (f4b), antikorreliert neben korreliert (eigen5) und der Produktpfadfall „Masteranomalie außerhalb der Passage" (A4). Regel: Jede der 15 Ketteneingaben läuft im Bein A31 durch den Produktpfad und gibt ihr Ergebnis aus; NAK-213-/NAK-214-Fälle (f8, f8b, f4, f4b, „Distraktor allein", „Parent ohne Evidenz", „Kandidatendeckel") werden ausgeführt und mit benannter, gedruckter Lücke ausgegeben (Muster NAK-190 im Prüfer), nicht gelöst und nicht als grün gewertet; §2.6 der Matrix ist die Referenz für die erwartete Ausgabe je Eingabe. Bein A31 bleibt grün; keine Produktänderung für fremde Fälle.

NR-04 (§2.6/N-16-Kontrollfall, tools/eq-copilot/erzeuge_p5_korpus.py:259 `WECHSEL7`): `[9,0,9,0,9,0,9]` liegt gegenläufig zur Masterreihe — der bedingte Uplift ist negativ, der Fall bleibt schon durch R1/R2 `mittel` und misst nicht „nur die Fensterzahl trennt". Regel: Der Sieben-Fenster-Kontrollfall trägt einen positiven, gleichläufigen Zusammenhang zum Master (dieselbe Phase wie `WECHSEL8`), und `pruefe_p5_korpus.py` beziehungsweise das Rustbein prüft als Vorbedingung, dass Uplift und Koinzidenz positiv sind, bevor `mittel` allein aus `fenster < GATE_MINDEST_FENSTER` erwartet wird; Gegenprobe: dieselbe Reihe mit acht Fenstern wird `hoch`. Korpus über den Erzeuger neu erzeugen, bytegleich (A28 `--pruefen`), Manifest-SHA im MANIFEST.json nachziehen.

ARBEITSWEISE: Je Befund ein Rotbeweis unter docs/beweise/roh/NAK-212-rot-NR-0n.txt (Test fällt ohne die Änderung, hält mit ihr — bei Test-/Korpusbefunden: der alte Aufbau erfüllt die Vorbedingung nicht, der neue erfüllt sie). Betroffene Beine grün und im Manifest mit Zahlen: A4 `cargo test --manifest-path broker/Cargo.toml` (mindestens `--test sonde014_hypothese`, `--test sonde014_gegenbeispiele`, `--test sonde014_p5_korpus` und die Einheitstests in hypothese.rs), A28 `py -3.13 tools/eq-copilot/erzeuge_p5_korpus.py --pruefen`, A29 `py -3.13 tools/eq-copilot/pruefe_p5_korpus.py`, A30 `--selbsttest`, A31; Ausgabe des Beinlaufs unter docs/beweise/roh/NAK-212-nacharbeit-1-<sha>.txt. Kein voller Kanon in dieser Runde (der läuft beim Ticketabschluss). Manifest docs/beweise/NAK-212.md: neuer Abschnitt „§6 Nacharbeit 1" (Befunde NR-01 bis NR-04 wörtlich, Regel, Änderung mit Datei:Zeile, Rotbeweis, gemessene Beine), Matrixzeilen präzisieren, wo der Wortlaut sich ändert (alte Fassung kenntlich), Kopftabelle (Änderungssatz, Testanzahl) und §5 Verlauf nachziehen, Prüfliste §3. Committen mit Pathspec (Produkt, Tests, Korpus, Erzeuger, Prüfer, docs/beweise/NAK-212.md, docs/beweise/roh/NAK-212-*), pull --rebase, push, danach STOPP. Nichts außerhalb der vier Befunde ändern; Nebenbefunde nummeriert in §4, nicht ins Register.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen — Zahlenränder (r exakt 0, Reihen der Länge 1 und 2, konstante Reihen, NaN in Reihen), Determinismus (M-25 bytegleich), keine Score-Schwelle (M-15), Erzeuger ↔ Prüfer ↔ Fixture bytegleich, Riegel ↔ Rotbeweis, keine irreführenden Kommentare oder Manifestsätze; eq-copilot/plugin/ bleibt unberührt.
