URTEIL: PASS — Befund geschlossen, nichts gebrochen.

Geprüft: Ja, der Befund ist am ZIEL als Spezifikation geschlossen — `docs/beweise/NAK-246.md:632-635,1262-1290,1488` legt einheitlich fest: Gleichheit wird angenommen beziehungsweise koalesziert, nur kleinere Marken werden auch nach Entnahme verworfen und gezählt; die Consumer-Belege aus `:1326-1342` wurden statisch nachgemessen: `eq-copilot/plugin/src/SourcesModel.cpp:1152-1158,1171-1180,1217-1230,1252-1279` bestätigt Sitzungsprüfung ohne Altersriegel, Erhalt vorhandener Messdaten bei gleicher Laufidentität, bedingte Reject-Aktivierung und Ersetzung statt Aufsummierung, mit den ausdrücklich genannten Empfangszeit-/Revisionsänderungen (`SourcesModel.h:323`, Aufrufer `prozessor/Ipc.cpp:1093-1097`); Resubscribe und Live-Fallback sind durch `broker/src/coordinator/subscription.rs:62-70,120-161`, die unveränderten Projektionsordinale durch `flush.rs:122-123` und `store/handle.rs:256-270` beziehungsweise `store/writer.rs:541-545` belegt; beim Fristfall beendet `transport/server_v3/griff.rs:86-90` nur das Warten, nicht den Queueeintrag, die Schuld bleibt bestehen (`flush.rs:148-151`), und `subscription.rs:219-254,269-292` spielt dieselben Ereignisse nach, auch ohne Linkwechsel (`evidenz.rs:247`), während `session_snapshot` ausdrücklich ausgespart und beim Subscribe geliefert wird; Koaleszierung und Aufnahme entsprechen `queues.rs:161-173,194-200`, die Duplikat-Robustheitslesart dem Entwurf `docs/FL-Nakama-Sonden-Design-Entwurf.md:3551,4248-4249`; die geplante Mutation `<` zu `<=` kippt bei M-22/M-24 genau die zugesagte Gleichheitszustellung zu Ablehnung mit wachsendem Verwerfungszähler, nicht bloß einen Nebeneffekt; zur zweiten Frage: kein eingeführter Bruch an M-21/M-23/M-26 bis M-28, §4, R-D1 bis R-D9 oder den CLAUDE-Invarianten festgestellt, §3.9 nachgerechnet mit 37 Zeilen, 25 BELEGT, 27 ENTSCHIEDEN, davon 12 ausschließlich, 26 BAULÜCKEN und 11 primären Regressionswachen, ausschließlich freigegebene Manifeststellen geändert, §7.1 bis §7.6 unverändert und Produkt-/Test-/Schema-/Werkzeugcode gegenüber f9b82360 unverändert.

Nicht geprüft: Laufzeitverhalten, Builds, tatsächliche Rot-/Grünläufe oder dokuriegel — Aufträge, Gate und geplante Tests wurden gelesen, ihre Quellbezüge statisch geprüft, aber nichts ausgeführt oder geschrieben; HEAD blieb vor/nach identisch bei f61f1c826d6ef55eecd29b725c241e3bba078f10, der Status unverändert mit der fremden ungetrackten `.claude/settings.local.json`.