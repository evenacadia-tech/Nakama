NAK-286 — Nacharbeit 1 der Abschlussprüfung (Runde 1 von 3): Weg R2 nach M-64 bauen (Regel P-21). Auftrag des Dirigenten, 15.09.2026.

ROLLE. Du bist der Bauer der Nacharbeit 1 nach der Abschlussprüfung des Tickets NAK-286 (Plan S25e, Laufzeit-Arm) im Repo C:\Users\phili\Projekte\Nakama (Branch master, sichtbarer Checkout, Windows 11; Runner in PowerShell 7 und Python 3.13 unter tools/fl/, Szenarien unter docs/gesundheit/szenarien/; das MCP-Repo C:\Users\phili\Projekte\fl-studio-mcp wird nicht angefasst). Die Etappen 1 bis 5 sind gebaut und geprüft; die Abschlussprüfung (docs/beweise/NAK-286.md §44, Codex gpt-6-astra max, Thread 01a0a46f) hat genau einen Defekt erhoben, den du behebst. Du beweist jede berührte Matrixzeile mit Test und Rotbeweis, fährst den vollen Kanon und den Laufzeitlauf selbst und übergibst nie „NOT RUN". Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

DER BESTÄTIGTE DEFEKT (wörtlich aus dem Urteil, Quellvalidierung §44.2):
„[P2] Implementiere R2 vor der Abnahme des Gesamt-Gates — tools/fl/szenario.py:804-805. DEFEKT der Gesamt-Gate-Prüfung: Auch bei vorhandenen U43-Projekten liefern beide Referenzschritte bedingungslos Exit 5, ohne Render oder Vergleich. Das Gate (docs/plan/plan.json:313) verlangt ausdrücklich „Render über FL64.exe /R mit und ohne Verarbeitung"; M-64 (docs/beweise/NAK-286.md §4.8) verlangt die entsprechenden Vergleiche bei vorhandenen Projekten. U43 liefert lediglich die Dateien; der Handgriff kann die fehlende Implementierung nicht ersetzen."
Bricht: Matrixzeile M-64 (§4.8: „Render beider Projekte | der Render „Verarbeitung ein" weicht von der Quelle ab (Abweichungszahl größer 0); der Render „ohne Slots" trägt dieselben SHA-256 der Datenbereiche wie der Render im Auslieferungszustand; Urteilsweg: ohne die Projekte aus Karte U43 VORAUSSETZUNG/Szenario (Szenario-Exit 5) für diese Schritte, nie still; weicht ein Render von der Zusage ab, VERFEHLT") und der Gate-Satz „Nulltest im echten Host (Render über `FL64.exe /R` mit und ohne Verarbeitung, Vergleich mit den Offline-Werkzeugen)". Stand am Ziel 614913b8: tools/fl/szenario.py:794-805 endet bei vorhandenem Projekt bedingungslos mit Exit 5; tools/fl/laufzeit.ps1 rendert nur das Diagnoseprojekt (Rendere :543-587, einziger Aufruf :748; Ausgabenamen render.json, ergebnis.json, <Projekt>.wav fest); tools/fl/nulltest.py kennt nur Render gegen Quelle (vergleiche :174-229, EXIT_JE_URTEIL :232-234, CLI :517-524).

SCHLIESSENDE REGEL P-21 (Dirigent, 15.09.2026, §44.3; Technik innerhalb von M-64, F-17 bis F-20, M-65, F-26):
(a) Der Runner rendert vor dem Start des Diagnose-FL jedes kopierte Referenzprojekt wie das Diagnoseprojekt (F-18: nur ohne FL mit Fenster, Frist 600 s je Render, bei Hängen genau diese PID beenden, nie speichern) aus der Arbeitskopie %LOCALAPPDATA%\evenacadia\nakama-laufzeit\projekt\ und schreibt je Projekt einen eigenen Renderstatus (Format nakama.laufzeit.render.v1 wie heute, Feld projekt) und eine eigene Renderdatei, unterscheidbar nach Projektname (eigener Unterordner oder Namenszusatz — deine Wahl, datiert in §45); Ort und Name des Auslieferungsrenders, seines render.json und seiner ergebnis.json bleiben (F-26: snapshot-runde01 liest sie). Ein fehlendes Referenzprojekt bekommt einen Renderstatus mit grund „Referenzprojekt fehlt (Karte U43, K-286-1)" und keinen Render; die Reihenfolge der Renders steht im Protokoll.
(b) lokal.nulltest liest für vergleich verarbeitung_ein und ohne_slots den Renderstatus des in params.projekt genannten Projekts (nicht mehr die Existenz der Datei im Repo-Ordner; REFERENZPROJEKT_ORDNER entfällt oder zeigt auf die Arbeitskopie); ohne Render endet der Schritt mit Szenario-Exit 5 und dem Grund aus dem Renderstatus (fehlt das Projekt: „VORAUSSETZUNG <vergleich>: Referenzprojekt fehlt (Karte U43, K-286-1)" wie heute), nie still (P-3: die Folge läuft weiter).
(c) verarbeitung_ein: derselbe Vergleich gegen die Quelle wie R1 (F-19: fmt-Chunk, N aus der Songlänge, Versatzsuche, Ausrichtung, Abweichungszahl, g); Zusage nach M-64: Abweichungszahl nach Ausrichtung größer 0 → GEMESSEN (Szenario-Exit 0; auch mit v ≠ 0 oder Faktor g, beide in der Rohzeile); 0 Abweichungen (BITIDENTISCH oder VERSATZ ohne Abweichung) → VERFEHLT (Szenario-Exit 4: der Vergleich wäre blind, Rotbeweisspalte M-64); Formatfehler oder kein Render → Szenario-Exit 5.
(d) ohne_slots: SHA-256 des Datenbereichs des Referenzrenders (derselbe Framebereich und dieselbe Float-32-Darstellung, die die ergebnis.json des Auslieferungsrenders als sha256_render_bereich hasht; bei v = 0 die Frames [0, N)) gegen sha256_render_bereich aus dieser ergebnis.json; gleich → GEMESSEN (Szenario-Exit 0); ungleich → VERFEHLT (Szenario-Exit 4) mit beiden SHA-256, der Abweichungszahl zwischen beiden Renders und der ersten Abweichung in der Rohzeile; fehlt die ergebnis.json des Auslieferungsrenders oder endete der Auslieferungsschritt nicht BITIDENTISCH → Szenario-Exit 5 mit Grund (die Zusage vergleicht gegen einen bitidentischen Auslieferungsrender).
(e) Kein Rückweg aus den Referenzschritten (M-65: nur der Auslieferungsrender mit ABWEICHUNG löst ihn aus; verarbeitung_ein und ohne_slots zählen nie).
(f) Rohzeile je Referenzschritt wie M-62 (N, überlappende Frames, v, Abweichungszahl, erste und letzte Abweichung, SHA-256 beider verglichenen Datenbereiche, Stichprobenspitze je Kanal, g in dB) plus Projektname, SHA-256 des Projekts und Renderdauer aus dem Renderstatus; Urteilswort des Schritts GEMESSEN / VERFEHLT / VORAUSSETZUNG wie heute.
(g) Tests gegen Attrappen, nie ein echter Render mit fremdem Projekt: Selbsttestfälle in tools/fl/nulltest.py (Render-gegen-Render nach (d); invertierte Bewertung nach (c)), tools/fl/szenario.py (Referenzschritte aus dem Renderstatus: gemessen, verfehlt, fehlend) und tools/fl/laufzeit.ps1 (drei Renders vor dem FL-Start aus der Arbeitskopie, eigene Status- und Renderdateien, fehlendes Projekt ohne Render mit Rohzeile, Auslieferungsrender an altem Ort); Rotbeweise an der Zusage: verarbeitung_ein mit 0 Abweichungen grün → rot; ohne_slots mit ungleichen SHA-256 grün → rot; Referenzrender nach dem FL-Start oder in die Datei des Auslieferungsrenders → rot; fehlendes Projekt still (ohne Rohzeile oder ohne Exit 5) → rot.

VERBINDLICHE QUELLEN (in dieser Reihenfolge lesen, bevor du eine Datei anfasst): docs/beweise/NAK-286.md — Kopf (Gate wörtlich, Grenze), §2 (R-286-1 bis R-286-5; F-17 bis F-20, F-24, F-26, F-28), §4.8 (M-60 bis M-65, M-75), §4.2 M-21, §4.11 M-74, §5.4, §5.8, §5.9 (Grenze), §7.1 (T-11, T-12, T-17, T-20), §10.2 P-3 und P-4, §13.2 P-7, §38.2 P-17 und P-18, §39.2 P-19 und P-20, §43 (Etappe 5, besonders §43.7, §43.10, §43.11), §44 (Abschlussprüfung, Quellvalidierung, P-21, dieser Auftrag); den Code an HEAD: tools/fl/laufzeit.ps1 (Rendere, Bereite-Projekt, Pruefe-Projekt, Fahre-Lauf, Selbsttestteil), tools/fl/szenario.py (lokal_nulltest, lokal_fenster als Muster für Renderstatus-Lesen, Selbsttestteil), tools/fl/nulltest.py (ganz), tools/fl/selbsttest.py (Sammler A35), docs/gesundheit/szenarien/nulltest-host.json und LIES-MICH.md, tools/fl/LIES-MICH.md; die Rohdaten docs/beweise/roh/NAK-286-laufzeit-7e39db9a.md (:114-143 nulltest-host) und docs/beweise/roh/NAK-286-rotskript-etappe5.ps1 (Muster: Obergrenze, Schreiben mit Wiederholung, bytegleiche Rücknahme); CLAUDE.md (Wahrheitskern, Invarianten, Maschinen-Landminen — Start-Process -ArgumentList quotiert nicht; Pipes verdecken Exitcodes; Backticks nie in einem Bash-Befehlstext; Copy-Item überträgt LastWriteTime; ein Kanon kann einmalig mit zwei flatc-Beinen „Voraussetzung fehlt" enden, dann einmal wiederholen; bleibt der Kanon nach „Baue:" bei 0 % CPU stehen, wartet er auf VCTIP.EXE — Prozess beenden; ein Pipe-Zeichen in einer Tabellenzelle als \|), tools/dirigent/pruefliste.md (Arbeitsliste, Selbstaudit A bis F).

TICKETPFADE (nichts anderes wird angefasst): tools/fl/szenario.py, tools/fl/laufzeit.ps1, tools/fl/nulltest.py, tools/fl/selbsttest.py (nur, falls die Registrierung neuer Fälle dort nötig ist), docs/gesundheit/szenarien/nulltest-host.json (nur, falls die zwei Referenzschritte ein Erwartungsfeld brauchen; sonst unverändert), docs/gesundheit/szenarien/LIES-MICH.md und tools/fl/LIES-MICH.md (Weg R2 gebaut: Sätze „nicht gebaut" ersetzen, P-21 in der Tabelle, Skriptkopf von laufzeit.ps1 nennt die drei Renders), eq-copilot/install/nakama-installer-v1.json (nur die Hashzeilen, die der Laufzeitlauf nachzieht). Dazu docs/beweise/NAK-286.md: §4.8 die Zeile M-64 (Test- und Rotbeweisspalte mit dem gefahrenen Beleg, Quelle P-21; ID bleibt, keine neue Zeile), ein neuer Abschnitt „## 45. Nacharbeit 1 der Abschlussprüfung — Weg R2 (Bauer, <Datum>)" ans Ende, die Kopfzeile „Etappe" nur im letzten Fettsatz. Alles andere im Manifest bleibt unverändert (den lebenden Kopf zieht der Dirigent nach der Wiederprüfung nach). Rohdaten unter docs/beweise/roh/: NAK-286-rot-M-64-nach1.txt, NAK-286-rotskript-abschluss-nacharbeit1.ps1, NAK-286-abschluss-nacharbeit1-beine.txt, NAK-286-<sha>.md (Kanon-Runner), NAK-286-laufzeit-<sha>.md (Laufzeit-Runner). Nicht angefasst: eq-copilot/plugin/**, eq-copilot/schemas/**, eq-copilot/fixtures/** (auch Nakama-Diagnose.flp; kein FL-Projekt wird gespeichert), eq-copilot/identity/**, eq-copilot/install/Install-Nakama.ps1, broker/**, tools/plan/**, tools/eq-copilot/**, tools/dirigent/**, tools/beweise.ps1, das MCP-Repo, .claude/**, docs/PLAN-STAND.md, docs/plan/**, docs/offene-punkte.md, docs/gesundheit/KONZEPT.md, fremde uncommittete Dateien (untracked Ordner briefing-hub/ und nimbalyst-local/; falls vorhanden, vom User neben das Diagnoseprojekt gelegte .flp-Dateien). Nie git add -A, nie --amend, nie git reset --hard, nie git clean; Commit nur mit Pathspec; neue Dateien einzeln mit `git add -- <Pfad>` stagen.

INVARIANTEN (CLAUDE.md, Gate, R-286-1 bis R-286-5, P-1 bis P-21): der Runner startet nie eqcop-broker.exe und nie einen Testbroker auf der Produktions-Pipe; kein mutierter Bau wird installiert (T-11); kein FL mit fremdem Projekt wird beendet (R-286-5); Diagnose- und Referenzprojekte werden nie gespeichert; ein Render läuft nur ohne FL mit Fenster (F-18); Exitcodes 0, 3 und 4 des Runners, Urteilswörter, Kopfzeile und Aufbau der Rohdatei bleiben (M-11, F-24); Szenario-Exit 5 lässt die Folge weiterlaufen (P-3); Fristen sind erzwungene Obergrenzen (P-19); Rotbeweise fallen am Träger (P-20); die Fenstererfassung bleibt unberührt (M-21, P-17); Identität unberührt, keine Legacy-Umbenennung (NAK-30); vor dem Schreiben jeder bestehenden Datei `git ls-files --eol` lesen und dieselben Zeilenenden zurückschreiben (Index LF; Arbeitskopie w/crlf durch core.autocrlf=true, Manifest w/mixed — schreibe so, dass `git diff` nur deine Änderung zeigt); Python ohne den LastWriteTime-Schritt; die Selbsttests laufen unter `py -3.13` ohne MCP-Repo. Prüfe vor dem Kanon und vor dem Laufzeitlauf, dass kein anderer Bau- oder Testprozess im Checkout läuft (NAK-290); ein Laufzeitlauf läuft nie parallel zu einem Kanon; das offene Diagnose-FL aus §43.7 (PID 29068, Runner-eigen) beendet dein Runner beim Laufzeitlauf selbst. Karte U43 ist zugesagt, der Handgriff weder angekündigt noch als erledigt gemeldet; die Referenzprojekte liegen nicht neben dem Diagnoseprojekt — dein Laufzeitlauf endet deshalb für beide Referenzschritte mit „Referenzprojekt fehlt" (Exit 5) und insgesamt VORAUSSETZUNG; das ist kein Fehler und steht so in §45. Meldet der User den Handgriff während deiner Arbeit, sagt es dir der Dirigent per Zusatz; du fragst nicht nach; Produktfragen beantwortest du nicht.

BAUFOLGE.
1. Vorher messen: `git rev-parse HEAD` (Basis der Runde; das Manifest muss §44 tragen — fehlt er, stoppst du und meldest es), `git status --short` (nur die zwei fremden Ordner), `git ls-files --eol` der zu ändernden Dateien, SHA-256 der zu ändernden Dateien; A35 am Basisstand grün (Rohzeile); Prozessliste ohne Bau-, Test- und Renderprozesse.
2. Rotläufe vor der Änderung (§5.1): die neuen Selbsttestfälle nach P-21 (g) gegen den unveränderten Stand rot (Attrappe mit vorhandenem Referenzprojekt: der Schritt endet heute mit Exit 5 „Weg R2 nicht gebaut" statt GEMESSEN; Runner: nur ein Render statt drei) — Rohdatei.
3. Bau nach P-21 (a) bis (f) in tools/fl/laufzeit.ps1, tools/fl/nulltest.py, tools/fl/szenario.py; Doku nach den Ticketpfaden; M-64 im Manifest.
4. Je berührter Matrixzeile (M-64; falls du M-60 bis M-62 oder M-65 im Verhalten berührst, auch diese) der Testfall grün UND der Rotbeweis nach §5.1 (SHA-256 vorher, Mutation am Träger, Bein einzeln mit Rohausgabe, bytegleiche Rücknahme, Bein grün); Skript docs/beweise/roh/NAK-286-rotskript-abschluss-nacharbeit1.ps1 nach dem Muster der Etappe 5 (Obergrenze, Schreiben mit Wiederholung).
5. Beine einzeln aus pwsh: A35 (`py -3.13 tools\fl\selbsttest.py`), `py -3.13 tools\fl\nulltest.py --selbsttest`, A32 als Hinweis (bekannter Exit 4, NAK-292). Rohausgabe docs/beweise/roh/NAK-286-abschluss-nacharbeit1-beine.txt.
6. Kanon auf dem End-Stand: voller Kanon abgekoppelt nach Skill §3.5 auf dem Stand nach deinen Änderungen (dirty zulässig; der Commit muss danach genau diesen Stand tragen): `$log = Join-Path $env:TEMP 'nakama-nak286-nach1-kanon.log'; $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-286.md -Anhaengen -Titel NAK-286 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"; Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl` — fertig, wenn die letzte Logzeile mit EXIT= beginnt; höchstens alle 15 Minuten `Get-Content $log -Tail 3`; bleibt der Lauf nach „Baue:" bei 0 % CPU, VCTIP.EXE beenden (NAK-300); endet er einmalig mit zwei flatc-Beinen „Voraussetzung fehlt", einmal wiederholen (NAK-280). Erwartet: GRÜN, A35 grün, A17 Exit 0, A33 unter der Ratsche. Ein rotes Bein ist Arbeit, kein Übergabegrund — außer es liegt außerhalb der Ticketpfade: dann stoppst du und meldest es. Der Runner hängt den Kanon-Abschnitt an das Manifest an; das bleibt so.
7. Nach grünem Kanon der Laufzeitlauf mit allen Szenarien (nie parallel zum Kanon): `pwsh -NoProfile -File tools/fl/laufzeit.ps1 -Ticket NAK-286 -Basis <Basis-SHA dieser Runde> -Erzwingen` (der Diff trägt keinen Produktpfad; -Erzwingen mit Grund in §45). Erwartet: bereitschaft 0, fenster 5 (eingeklappt, K-286-2), nulltest-host 5 (auslieferung BITIDENTISCH; verarbeitung_ein und ohne_slots „Referenzprojekt fehlt" aus dem Renderstatus, Protokoll mit den zwei Zeilen „kein Render" vor dem FL-Start), snapshot-runde01 0, u40-aktivitaetsgate 0; Gesamturteil VORAUSSETZUNG, Exit 3. Exit 4 ist Arbeit: Ursache benennen, beheben (nur in den Ticketpfaden), Lauf wiederholen. Kopfzeile wörtlich in §45.
8. Manifest: „## 45. Nacharbeit 1 der Abschlussprüfung — Weg R2 (Bauer, <Datum>)" mit Kopftabelle (Basis-SHA, Ticketpfade wie gebaut, Vorher-Messung mit SHA-256 und Zeilenenden, Endstand-SHA-256), je Punkt aus P-21 (a) bis (g) der Beleg (Datei:Zeile), je Matrixzeile Test und Rotbeweis (beide SHA-256, beide Exitcodes, Rohdatei), Beine mit Exitcodes, Kanon mit Kopfzeile und Rohausgabe, Laufzeitlauf mit Kopfzeile und je Szenario Urteil, Selbstaudit nach tools/dirigent/pruefliste.md A bis F, datierte Abweichungen von P-21 mit Grund (Technik innerhalb der Grenze; eine Abweichung außerhalb der Ticketpfade oder Invarianten ist ein Halt), eigene Messungen, offene Punkte (U43/M-64 im Host, K-286-2; ungemessen: ein echter Referenzrender). Kopfzeile „Etappe": die bisherige Aussage bleibt davor stehen; der letzte Fettsatz (höchstens 70 Zeichen) lautet „**Nacharbeit 1 gebaut am <Datum>; Wiederprüfung steht aus.**". Keine neuen Deckel oder Zahlen ohne Quelle. Pipe-Zeichen in Tabellenzellen als \|; Zeilenenden des Manifests LF im Index. Vor dem Commit `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-286.md docs/gesundheit/szenarien/LIES-MICH.md tools/fl/LIES-MICH.md` mit 0 Befunden.
9. Abschluss: `git status --short` zeigt nur Ticketpfade, Rohdaten, Hashzeilen, den vom Kanon-Runner angehängten Manifestabschnitt und die fremden Dateien. Commit nur mit Pathspec (`git commit -F <Nachrichtendatei unter $env:TEMP> -- <Pfade>`; neue Dateien vorher einzeln stagen; Betreff „NAK-286 Abschluss-Nacharbeit 1: Weg R2 nach M-64 gebaut (P-21) - drei Renders vor dem FL-Start, verarbeitung_ein und ohne_slots verglichen, A35 gruen, Kanon GRUEN, Laufzeitlauf"; die Nachricht endet mit „Co-Authored-By: Claude Opus <noreply@anthropic.com>"). Dann `git pull --rebase` nur bei sauberem Baum und `git push origin master`. docs/PLAN-STAND.md fasst du nicht an; planstand.py läuft nicht; docs/offene-punkte.md fasst du nicht an. Melde zum Schluss in höchstens zwölf Zeilen: Commit-SHA, Kanon-Kopfzeile, Laufzeit-Kopfzeile und je Szenario das Urteil, je Matrixzeile Test grün und Rotbeweis rot mit Rohdatei, Beine mit Exit, datierte Abweichungen, offene Punkte.
