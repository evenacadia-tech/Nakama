NAK-246 Matrix-Nacharbeit 1 (Etappe 1, Spezifikation vor Code — KEIN Bau) — Auftrag des Dirigenten (2026-09-11)

ANLASS. Die Matrixprüfung 1 (Codex gpt-6-astra, Effort max, lesend, Thread 01a08da2-55bf-77c0-8f10-3590a4240789, Urteil docs/beweise/roh/NAK-246-matrixpruefung-1-e50dd27.txt) endete NEEDS_WORK mit zehn Befunden. Ein lesender Validierungsagent hat jeden am HEAD 36133a12 an der Quelle bestätigt (Manifest §7.3). Dieser Auftrag enthält ausschließlich die acht Defekte und die drei vom Dirigenten entschiedenen Lücken der Ticketgrenze — nichts sonst. Was hier nicht steht, wird nicht geändert.

MODELL DES BAUERS (Dirigenten-Vermerk 11.09.2026): claude-fable-5-1[1m] mit --effort xhigh bis zum Wochenreset (User-Wort 10.09.2026 abends; User-Wort 11.09.2026, 00:5x: „doch bei 95 einfach weitermachen. wie gesagt in 14 stunden ist der wochenreset. schlimmstenfalls bricht es eben ab und wir müssen nach dem reset weitermachen wo wir aufgehört haben"). --permission-mode dontAsk.

DU BIST der frische Worker im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = git rev-parse HEAD beim Start; git status --short muss leer sein, sonst nichts anfassen und als letzte Zeile „NAK-246 HALT <grund>“ melden. Aufsicht ENG. Keine Rückfrage an den User; technische Entscheidungen triffst du selbst und begründest sie im Manifest. Lies zuerst vollständig: docs/beweise/NAK-246.md (Manifest, dein einziges Werkstück), docs/beweise/roh/NAK-246-auftrag.txt (Ticketauftrag mit R-D1 bis R-D9), docs/beweise/roh/NAK-246-matrixpruefung-1-e50dd27.txt (Urteil; der Text trägt Silbendoppelungen aus der Ausgabe, lies sinngemäß) und Manifest §7.3 (Quellvalidierung mit Zitaten).

GRUNDSATZ, wörtlich in jeder Zeile anzuwenden: Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Deckel und Zahlen kommen nur aus der Quelle (Datei:Zeile), nie aus dem Kopf.

DEFEKTE (Titel wörtlich aus dem Urteil; Quelle aus der Validierung; je Defekt die Regel des Dirigenten, die ihn schließt):

D-M1-1 „[P1] Schleuse auch die nachregistrierten Produkt-Callbacks ein“ — Quelle: PluginProcessor.cpp:164 setzeP0Rueckmeldung (zwei Lambdas: zugestellt, verworfen), :225 setzeReplayBeginHook, :304 setzeKonfliktWiederholungHook; alle vier rohe this, aufgerufen aus der ablösbaren Client-Laufzeit (Verbindung.cpp:315, :331-332, :934-935, :998-999; Nachrichten.cpp:310, :344-345, :356-357; threadLauf :440, ControlClient.cpp:133 thread.detach()). Das Manifest zählt sechs (§2.4, §5.2 Feinheit 2 :1000, M-09 :599).
REGEL R-M1-1: Die Schleuse deckt ALLE Produkt-Callbacks, die controlV3 und telemetryV3 aus ihrer Laufzeit rufen: die sechs Konstruktor-Lambdas (:112-121) und die vier nachregistrierten. §2.4, §5.2 (Feinheiten 2, 5, 8) und die Zeilen M-06, M-07, M-09 nennen zehn Callbacks, je Callback den Setter im Prozessor und die Aufrufstelle im Client mit Zeile. M-09 misst „betreten“ gegen die Summe aller vom Testserver deterministisch ausgelösten Ereignisse einschließlich P0-Rückmeldung und Replay-Begin (welche der Testserver auslöst, legt die Zeile fest). Mindestens einer der Rotbeweise M-06/M-07 hält einen der vier nachregistrierten Callbacks (naheliegend beiP0Verworfen, weil er nach Fristablauf im abgelösten Thread läuft). §5.2 Feinheit 8 („Außerhalb von D2“: pipe, brokerLifecycle) bleibt nur mit Beleg (Datei:Zeile), dass diese beiden keine Prozessor-Callbacks aus einem ablösbaren Thread rufen; fehlt der Beleg, gehören sie in die Menge.

D-M1-2 „[P2] Berücksichtige die bereits laufende Produktverdrahtung in B23“ — Quelle: PluginProcessor.cpp:317-321 klammert nur controlV3.start()/telemetryV3.start()/brokerLifecycle.start(); die Registrierung :112-121 ist unbedingt; Sonde013PassageStateTest.cpp:713-744 startet den Produktclient über v3ProbeGegenstelleFuerTest/v3StartFuerTest (PluginProcessor.h:721-728) gegen einen Testserver. §2.4 (:435) „läuft in keinem Bein“ und §5.2 Feinheit 5 (:1029 „erstmals“) sind am Code falsch.
REGEL R-M1-2: §2.4 sagt, was der Code trägt (Makro unterdrückt nur den Konstruktorstart; B23 fährt die Produkt-Callbacks bereits gegen einen Testserver); die Lücke ist der deterministische Lebensdauertest. §5.2 Feinheit 5 wird entsprechend korrigiert; der Testaufbau aus B23 (:713-744) ist die Vorlage für die B14-Fälle M-06, M-07, M-09, und §4.2 (Etappe 3) sagt, dass er nachgenutzt und nicht neu erfunden wird.

D-M1-3 „[P2] Definiere für M-12 einen tatsächlich wirksamen Rotbeweis“ — Quelle: M-12 (:607) entfernt nur sourcesDrainMutex; die Sperrenordnung §5.3 (:1076-1078: sourcesDrainMutex → bindungMutex → sourcesCommandMutex nur für den Swap → SourcesModel::mutex) und §5.10 Zeile 2 legen bindungMutex über Swap UND Anwendung; heute (Ipc.cpp:1327-1369) wird bindungMutex je Befehl in der Schleife genommen. Hält bindungMutex Swap und Anwendung zusammen, kann ohne sourcesDrainMutex kein zweiter Drain überholen — der Rotbeweis fällt nicht deterministisch.
REGEL R-M1-3: Der Rotbeweis von M-12 fällt an der Zusage „genau einmal je Befehl, in ACK-Reihenfolge“: der Test hält den Workerzug zwischen Swap und Anwendung, und ein zweiter Drain MUSS dann überholen können, also ist die entfernte Sperre diejenige, die Swap und Anwendung tatsächlich zusammenhält. §5.3 legt in EINER widerspruchsfreien Aussage fest, welche Sperre die Klammer ist (Feinheiten 1 bis 3, Sperrenordnung :1076-1078 und §5.10 Zeile 2 passen zueinander): entweder bindungMutex klammert Swap und Anwendung — dann ist sourcesDrainMutex redundant und die Mutation des Rotbeweises ist die Freigabe von bindungMutex zwischen Swap und Anwendung — oder bindungMutex deckt wie heute nur die Anwendung je Befehl und sourcesDrainMutex ist die einzige Klammer — dann ist deren Entfernen die Mutation. Du entscheidest an der Quelle; M-12 nennt die Mutation, das Interleaving (welcher Haken, welche Reihenfolge) und den deterministischen Kipp (welcher Zähler, welche Reihenfolge fällt).

D-M1-4 „[P2] Passe die Aufrufertests von M-20 an die Annahmesemantik an“ — Quelle: Sonde014AssistentTest.cpp:945-951 (WN-05) fordert bei voller P0-Queue ausdrücklich false von assistentAntwort; Analyse.cpp:1190-1191 kehrt bei false um; N-05 (Sonde013PassageStateTest.cpp:3456-3472) misst fehlendes Pegelmaterial vor beginneVersuch, nicht die Wiederholungsannahme. M-20 (:620) führt beide als „bleibt grün“ und zitiert :921-935 statt :949-951.
REGEL R-M1-4: M-20 führt keinen bestehenden Test als „bleibt grün“, der die alte Semantik zusichert. WN-05 wird in Etappe 4 auf die neue Semantik umgeschrieben (bei voller Queue meldet assistentAntwort Erfolg, weil zur Wiederholung angenommen; der Auftrag wird nach dem Reconnect bestätigt; ROT am Basis-SHA: heute false); der Versuch-Aufrufer (Analyse.cpp:706-713) bekommt einen eigenen Fall (beginneVersuch bei voller Queue setzt letzterVersuchP0 und kehrt nicht um); N-05 wird aus M-20 gestrichen; der Zeilenverweis lautet :949-951. Ticketpfad: L-M1-3.

D-M1-5 „[P2] Miss M-23 hinter der echten Writerqueue“ — Quelle: PushProbe (store_crash_matrix.rs:439-459, registriert :476-477) implementiert SessionPush direkt; flush.rs:145-147 ruft das Trait; weder V3Sender (griff.rs:72-91) noch die Ausgang-Queue (queues.rs:155-173) liegen auf dem Weg; der bestehende Test :1197-1206 assertiert gegen den Store.
REGEL R-M1-5: Der Ordnungsbeweis fällt am zuletzt angenommenen Zustand HINTER der echten Ausgang-Queue und dem V3Sender: die beiden Flushes laufen durch V3Sender in einen Ausgang, dessen Zustellfolge der Test misst (In-Process-Listener wie broker/src/transport/server_v3/tests_abonnement.rs oder ein Ausgang mit angehaltenem Consumer, aus dem der Test die Folge entnimmt). Eine Probe, die SessionPush selbst implementiert, misst die Zustellordnung nicht und wird für M-23 nicht als Beleg geführt. Ticketpfad: L-M1-1.

D-M1-6 „[P2] Führe M-26 durch den Sender statt nur durch den Ausgang“ — Quelle: queues.rs:133-145 nimmt den Objektschlüssel als Parameter, :161-173 ersetzt nur bei gleichem Schlüssel; der feste Schlüssel „session_snapshot“ sitzt allein in griff.rs:86. Der Ausgang-direkt-Fall ist am Basis-SHA grün.
REGEL R-M1-6: M-26 misst den Sender: die drei Nachrichten laufen durch V3Sender::snapshot_schreiben mit Objektschlüssel in den Ausgang; ROT am Basis-SHA heißt: alle drei landen unter „session_snapshot“ und koaleszieren (queues.rs:161-173). Der Ausgang-direkt-Fall darf nur als Regressionswache (heute grün) geführt werden.

D-M1-7 „[P2] Erzwinge für M-27 den Queue-Stillstand vor der Entnahme“ — Quelle: verbindung.rs:543-562: der Writer entnimmt zuerst (ausgang.entnehmen(), :543) und blockiert erst beim Pipe-Schreiben (:559); kleine Nachrichten landen im Pipe-Puffer.
REGEL R-M1-7: Der Test weist die Queue-Belegung nach, BEVOR der Writer entnimmt: der Writer wird vor ausgang.entnehmen() deterministisch angehalten (Testhaken oder ein Consumer, der erst auf Freigabe entnimmt), Rücknahme und Vollsnapshot werden über den Sender eingereiht, die Belegung (zwei Einträge, zwei Schlüssel) wird geprüft, dann wird freigegeben und die Empfängerreihenfolge gemessen. Ein „langsamer Leser“ hält keine Nachricht in der Queue und ist kein Rotbeweis; M-27 wird entsprechend umgeschrieben.

D-M1-8 „[P2] Setze fehlende Projektion nicht mit fehlendem Store gleich“ — Quelle: subscription.rs:120-135: projektion ist bei Some(store) None, wenn store_degradiert() gilt oder session_state_lesen Ok(None) liefert; mod.rs:293-299: event_sequence startet auch mit Store bei 0 und ist prozesslokal; flush.rs:110-123 zieht die Marke aus dem Store. §5.5 Feinheit 1 (:1185-1190 „die Räume mischen sich nie“) hält am Code nicht; ein Live-Snapshot nach einem persistenten Snapshot kann eine kleinere Marke tragen und an der Hochwassermarke (M-22) scheitern.
REGEL R-M1-8: Die Ordnungsmarke kommt je Prozess aus genau einem Raum, und ein neuerer Zustand trägt nie eine kleinere Marke als ein bereits zugestellter älterer — auch wenn der Store vorhanden ist und die Projektion None liefert oder der Store degradiert ist. §5.5 Feinheit 1 unterscheidet drei Fälle (kein Store; Store mit Projektion; Store ohne Projektion oder degradiert) und nennt für jeden die Marke und den Beleg an der Quelle, dass die Monotonie gegenüber bereits zugestellten Marken hält. Der Satz „die Räume mischen sich nie“ fällt. M-24 bekommt für den Fall „Store vorhanden, Projektion None nach zugestelltem persistentem Snapshot“ einen Test mit Rotbeweis an der Zusage (ein neuerer Live-Snapshot wird wegen kleinerer Marke verworfen = Defekt). Kein Deckel, keine Zahl ohne Quelle.

VOM DIRIGENTEN ENTSCHIEDENE LÜCKEN DER TICKETGRENZE (11.09.2026; ins Manifest §4.4/§4.5/§4.7, §5.10 Zeile 3 und in die Kopfzeilen „Ticketpfade“ und „Nicht angefasst“ eintragen — genau diese Grenze, nicht mehr):
L-M1-1 (Etappe 5): broker/src/transport/server_v3/tests_abonnement.rs (Erweiterung) ODER eine neue Datei broker/src/transport/server_v3/tests_nak246.rs samt ihrer einen mod-Zeile in broker/src/transport/server_v3/mod.rs. Begründung: die In-Process-Listener-Tests mit steuerbarem Ausgang liegen nur dort (tests_abonnement.rs:14, :25-30); broker/tests/ fährt den externen Brokerprozess (store_crash_matrix.rs:209, :227) ohne steuerbaren Ausgang. Schließt den Befund „[P2] Halte zusätzliche Broker-Testpfade innerhalb der Ticketgrenze“.
L-M1-2 (Etappe 6): eq-copilot/plugin/tests/Sonde013PrePostGoldenTest.cpp (:439, :451, :463, :485) und eq-copilot/plugin/tests/Sonde013ExperimentUiTest.cpp (:82, :298, :366): ausschließlich die sieben Aufrufe von Vergleichspegel::speise auf die neue Form; keine Zusage dieser Tests ändert sich; B24 und B26 fahren in Etappe 6 und stehen in §4.5 als Beine mit Grund. Schließt den Befund „[P2] Plane sämtliche Aufrufer der entfernten speise-Form ein“.
L-M1-3 (Etappe 4): eq-copilot/plugin/tests/Sonde014AssistentTest.cpp: WN-05 (:945-951) auf die Annahmesemantik und die neuen M-20-Fälle; B29 fährt in Etappe 4. Gehört zu D-M1-4.

WAS DU ÄNDERST (nur docs/beweise/NAK-246.md): §2.4 (D-M1-2), §3 Zeilen M-06, M-07, M-09 (D-M1-1), M-12 (D-M1-3), M-20 (D-M1-4), M-23 (D-M1-5), M-24 (D-M1-8), M-26 (D-M1-6), M-27 (D-M1-7) und die Zählung §3.9 neu aus den Zeilen gerechnet; §4.2, §4.3, §4.4, §4.5, §4.7 (Pfade, Beine, Nachnutzung des B23-Aufbaus); §5.2, §5.3, §5.5, §5.8, §5.10 (Feinheiten und Abweichungen, die die Regeln verlangen); die Kopfzeilen „Ticketpfade“ und „Nicht angefasst (Grenze)“ um L-M1-1 bis L-M1-3; ein neuer Abschnitt §7.5 „Matrix-Nacharbeit 1“ mit: je Defekt D-M1-1 bis D-M1-8 und je Lücke L-M1-1 bis L-M1-3 die geänderte Stelle (Zeile am Ende deines Stands), die Regel, ein Satz, warum die Zusage jetzt an der Zeile fällt; Prüfliste tools/dirigent/pruefliste.md abgehakt mit Fundstelle je Zeile; Selbstaudit. Alles andere im Manifest bleibt unverändert; keine Zeile des Kopfes außer den zwei genannten; kein Umschreiben von §7.1 bis §7.4. Keine neuen Regeln, keine neuen Befunde, keine Härtungen. Kein Produkt-, Test-, Schema- oder Werkzeugcode.

GIT. Nie git add -A, nie --amend, fremde uncommittete Dateien nie anfassen; git status --short zuerst lesen. Vor dem Commit: py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-246.md ohne Befund (Pipe-Zeichen in Tabellenzellen als \| schreiben). Commit nur mit explizitem Pathspec docs/beweise/NAK-246.md; Commitnachricht über git commit -F <datei> (Bezeichner enthalten Backticks), nie -m mit Backticks; danach git pull --rebase bei sauberem Worktree, dann git push origin master als eigener Schritt. Kleine Werkzeugaufrufe (je Edit rund 250 Zeilen). Selbstaudit vor dem Commit: jede geänderte Zeile trägt weiter sieben Spalten, jede Matrix-ID genau einmal, jede Regel R-M1-1 bis R-M1-8 an genau benannter Stelle eingelöst, keine Zahl ohne Quelle, §3.9 aus den Zeilen gezählt.

ENDE: Letzte Zeile deines Turns „NAK-246 MATRIX-NACHARBEIT 1 ABGESCHLOSSEN <voller sha>“. Keine Rückfrage an den User.
