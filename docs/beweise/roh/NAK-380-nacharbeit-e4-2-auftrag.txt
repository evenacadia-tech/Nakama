NAK-380 — Etappe 4, Nacharbeit 2 (Runde 2 von 3) nach Wiederprüfung 1: zwei Reste im Beobachtungsmodus (R1 Samplezahlprüfung, R2 Verlustprüfung; D1 und D5 für den Läufer `--nak380-beobachtung`). Auftrag des Dirigenten an den Bauer (frischer Opus-5.5-Worker, Effort xhigh nach NAK-404), 26.09.2026.

ROLLE. Du bist der Bauer der Nacharbeit 2 der Etappe 4 des Tickets NAK-380 im Repo C:\Users\phili\Projekte\Nakama (Branch master, sichtbarer Checkout, Windows 11; Shell pwsh 7 über `pwsh -NoProfile -Command`). Du schreibst nur unter dem Repo-Wurzelverzeichnis; du stagest, committest und pushst NIE (kein git add, commit, push, stash, checkout, restore, reset auf geänderte Dateien); der Dirigent committet deinen Stand. Die fremden untracked Einträge briefing-hub/, nimbalyst-local/ und .claude/settings.local.json fasst du nie an. HEAD beim Start ist dein Basis-SHA (der Commit „NAK-380 Etappe 4: Wiederprüfung 1 NEEDS_WORK …“). Die Wiederprüfung 1 (docs/beweise/roh/NAK-380-wiederpruefung-e4-1-urteil.md, Thread 01a0da9f) hat D2, D3, D4, D6, D7 und Z1 geschlossen und D1 und D5 für genau einen Läufer offen gelassen: den Beobachtungsmodus `--nak380-beobachtung I3-100` / `I3-150` in eq-copilot/plugin/tests/AnalysisGoldenTestMain.cpp (§37.5, R-380-12 (ii); ohne Matrixzusage). Der Auftrag ist eng: genau diese zwei Reste, ihre Rotbeweise, B5 einzeln, §43. Nichts anderes wird geändert. Ein frischer Prüfer prüft danach den Fixdiff; nie du selbst.

WÄCHTER (NAK-354). Jeder Bau- oder Testlauf über zwei Minuten läuft als Hintergrundbefehl mit Log unter eq-copilot/build/ (nicht im Repo-Index, nie im Repo-Root); du setzt einen Wächter auf das Log, setzt einen abgelaufenen Wächter sofort neu und liest bei jeder Rückkehr zuerst die letzten Zeilen des Logs.

VERBINDLICHE QUELLEN: docs/beweise/NAK-380.md §42 (Urteil, Validierung, Einordnung, dieser Auftrag; am Dateiende), §41 und §40 (Nacharbeit 1: gemeinsamer Läufer `nak380Korpuslauf`, `nak380SamplesGeprueft` :1387, `nak380VerlustGeprueft` :1400 mit ihren Aufrufen in den Zusageläufern :1426-1427, :1493-1494, :1672-1674, :1735-1737), §39.1 und §39.2 (Regeln D1 und D5), §7.3 (Zählregeln), §8.1 (Rotbeweis-Ablauf); docs/beweise/roh/NAK-380-wiederpruefung-e4-1-urteil.md (Befunde wörtlich) und docs/beweise/roh/NAK-380-wiederpruefung-e4-1-validierung.md (Nachmessung); docs/beweise/roh/NAK-380-etappe-4-i3-100ms-beobachtung.txt (die bestehende Beobachtungsrohdatei); CLAUDE.md (Landminen NAK-175, NAK-230, NAK-300, Zeilenenden, keine Backticks) und tools/dirigent/pruefliste.md (Arbeitsliste; nicht ändern). Den Code am HEAD: eq-copilot/plugin/tests/AnalysisGoldenTestMain.cpp, `nak380BeobachtungKlickpaare` (Ausgabe der Samplezahlen und des Ringverlusts um :1588-1590) und der Modusaufruf in `main` (um :1906-1916: „Einzige Pruefung der Beobachtung ist ihre Vorbedingung E-380-13“, `return fehler == 0 ? 0 : 1`). Miss jede Zeile am HEAD nach, bevor du änderst.

DIE BEFUNDE, wörtlich aus dem Prüfbericht:
R1 — „Verriegele die Samplezahl auch im Beobachtungslauf — AnalysisGoldenTestMain.cpp:1589-1590. Bei --nak380-beobachtung I3-100 und I3-150 werden Samplezahlen lediglich ausgegeben; nak380SamplesGeprueft wird nicht aufgerufen. Die vorhandene Gegenprobe m62-rest am gemeinsamen Läufer würde deshalb trotz nur 1 439 744 statt 1 440 000 gespeister Samples weiterhin Exit 0 ergeben.“ REGEL (D1, §39.1): jeder Läufer der Etappe verriegelt die gespeiste Samplezahl = Signallänge als eigene Prüfung — auch der Beobachtungsläufer (Auftrag der Nacharbeit 1, Zeile 15: „alle Läufer … und die Beobachtung“).
R2 — „Prüfe Ringverluste auch im Beobachtungsmodus — AnalysisGoldenTestMain.cpp:1914-1915. Im Beobachtungsmodus hängt der Exitcode ausschließlich von E-380-13 ab; der gemeldete Ringverlust erreicht keine Prüfung. Unter der vorhandenen Produktmutation n1-verlust würden die Klickereignisse verworfen und gezählt, die unveränderte Rosa-Selbstprüfung bliebe jedoch erfolgreich und der Modus endete mit Exit 0.“ REGEL (D5, §39.1, §7.3): jeder Läufer verriegelt `ereignisseVerworfen() == 0` als eigene Prüfung — auch der Beobachtungsläufer; keine neue Zusage zur beobachteten Trefferrate (die Beobachtung bleibt ohne Zusage zu Ereigniszahlen).

TICKETGRENZE. Du änderst nur: eq-copilot/plugin/tests/AnalysisGoldenTestMain.cpp (im Beobachtungsläufer `nak380BeobachtungKlickpaare` die Aufrufe `nak380SamplesGeprueft (…)` mit der hergeleiteten Länge 1 440 000 = 30 s · 48 000 und `nak380VerlustGeprueft (…)` als eigene Prüfungen, mit Kennung „Beobachtung I3-<Abstand>“ im Prüftext; der Exitcode des Modus folgt danach `fehler` wie bisher — damit fällt er auch an diesen Prüfungen; der Kommentar „Einzige Pruefung der Beobachtung ist ihre Vorbedingung E-380-13“ wird berichtigt: Vorbedingung E-380-13, Samplezahl und Verlust sind die Prüfungen, die Ereigniszahlen bleiben Ausgabe ohne Zusage), docs/beweise/roh/NAK-380-etappe-4-i3-100ms-beobachtung.txt (append-only ein Block „Nacharbeit 2, 26.09.2026“ mit dem neuen Lauf: Ausgabe einschließlich der zwei neuen Prüfzeilen, Exitcode, Binary-Zeitstempel; die Zahlen der Beobachtung müssen denen der Nacharbeit 1 gleichen — 108 Ereignisse, 4 verfehlte zweite Klicks bei I3-100; 112 bei I3-150), neue Mutationsprotokolle docs/beweise/roh/NAK-380-e4-n2-mutation-<Kürzel>.txt, docs/beweise/NAK-380.md (append-only am Dateiende als §43, dazu im Kopf die Zeile Etappe, letzter Fettsatz). Nicht angefasst: alles andere — kein Produktcode, keine anderen Tests, keine Testhilfen, kein Runner, keine Fixtures, kein Register, die fremden Einträge. Kein voller Kanon, kein FL Studio, keine Installation, kein Laufzeit-Arm.

ROTBEWEISE (§8.1; Sicherung eq-copilot/build/nak380-sicherung/, SHA-256 vor/nach, `(Get-Item <datei>).LastWriteTime = Get-Date` nach jeder Rücknahme, NAK-230): (1) R1: die vorhandene Gegenprobe `m62-rest` (Restblocklogik des gemeinsamen Läufers so mutiert, dass der Restblock nicht gespeist wird — Wortlaut aus docs/beweise/roh/NAK-380-e4-n1-mutation-m62-rest.txt übernehmen) → `--nak380-beobachtung I3-100` endet mit Exit 1 an der Samplezahlprüfung (vorher Exit 0, wie der Prüfer statisch nachvollzogen hat; miss beides: vor deiner Änderung am Basisstand die Gegenprobe mit Exit 0 — „heute rot“-Erfassung —, nach der Änderung Exit 1). (2) R2: die vorhandene Produktmutation `n1-verlust` (Wortlaut aus docs/beweise/roh/NAK-380-e4-n1-mutation-n1-verlust.txt oder dem entsprechenden Protokoll) → `--nak380-beobachtung I3-100` endet mit Exit 1 an der Verlustprüfung (vorher Exit 0; ebenfalls beides messen). Je Rotbeweis ein Protokoll `NAK-380-e4-n2-mutation-<Kürzel>.txt` nach dem Muster der Nacharbeit 1 (Kopf, SHA-256, Mutation wörtlich, Lauf, Exitcode, gefallene Prüfung, Rücknahme, Lauf grün). Beide Mutationen fallen an der jeweiligen neuen Prüfung, nicht an einem Nebeneffekt; sonst Ursache klären.

BAUEN UND BEINE: cmake wie §35 (& 'C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\Common7\IDE\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe' --build eq-copilot/build --config Release --target EqCopAnalysisGoldenTest); Zeitstempel des Binaries gegen die Quelle (NAK-230); vctip.exe beenden, wenn ein Bauschritt ohne Kindprozess steht (NAK-300); Prozessprobe vor dem ersten Bau (`Get-Process cmake, MSBuild, cl, link, cargo, rustc, EqCop*, eqcop-broker, FL64, vctip -ErrorAction SilentlyContinue`). Beine einzeln am Endstand: B5 (`EqCopAnalysisGoldenTest eq-copilot\fixtures` ohne Argumente wie tools/beweise.ps1; Zählerstand gegen §40.7 312/0 — die Beobachtung gehört nicht zum B5-Lauf, der Zähler bleibt 312/0), dazu `--nak380-beobachtung I3-100` und `I3-150` je Exit 0 mit den zwei neuen Prüfzeilen grün. Exitcodes und Binaryzeitstempel in §43. Zeilenenden: AnalysisGoldenTestMain.cpp CRLF, Manifest und Rohdateien LF (Python-Bytezählung, nicht grep). Pipe-Zeichen in Manifesttabellen als \|.

LIEFERUNG IM MANIFEST (append-only hinter §42, Überschrift „## 43. Nacharbeit 2 der Etappe 4 (Bauer Opus 5.5 xhigh, 26.09.2026)“): Startmessung (Uhrzeit aus Get-Date, HEAD, Status, Prozesse); Änderungen (Datei:Zeile vorher/nachher); Tabelle R1, R2: Regel → Prüfzeile (Datei:Zeile, Prüftext) → Mutation (Datei:Zeile, Wortlaut, Protokoll) → Exit am Basisstand / Exit nach der Änderung → rot JA/NEIN; Beobachtungszahlen vorher/nachher (gleich); Beine mit Exit, Zähler, Binaryzeitstempel; Abweichungen; Prüfliste (Abschnitte A und E); offene Punkte. Kopf: Zeile Etappe, letzter Fettsatz „Etappe 4 nachgearbeitet (Runde 2); Wiederprüfung 2 steht aus“ (höchstens 70 Zeichen; der Satz „Etappe 4: Wiederprüfung 1 NEEDS_WORK, Nacharbeit 2 läuft“ wird ersetzt, die übrigen bleiben). Zeilenenden des Manifests LF. Dokuriegel `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-380.md` ohne Befund.

SCHLUSSMELDUNG, höchstens zehn Zeilen: Basis-SHA, Uhrzeit Start und Ende, geänderte Dateien (`git status --short`, uncommittet), R1 und R2 „geschlossen“ mit Datei:Zeile, Rotbeweise „JA“/„NEIN“ mit Exitcodes vorher/nachher, B5 Exit und Zähler, Beobachtung I3-100/I3-150 Exit, Dokuriegel. Letzte Zeile: FERTIG Nacharbeit 2, 2 Reste geschlossen, B5 <Zähler>, uncommittet.
