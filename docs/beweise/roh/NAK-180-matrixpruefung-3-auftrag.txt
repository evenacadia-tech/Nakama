Wiederprüfung NAK-180 / G4-Nacharbeit 1, Matrixprüfung 3 (Runde 2 der Etappe 1) — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 35d1d86afa91fbc72f4cb9c6ba27256363908bae...fd1074dccf7ae972a2f914f4e257ce1fa660e462 -- docs/beweise/NAK-180.md`
(ZIEL = fd1074dccf7ae972a2f914f4e257ce1fa660e462 = HEAD, vor und nach deinem Lauf identisch). Der übrige Matrixbereich wurde in Matrixprüfung 1 und 2 geprüft und ist hier nicht Prüfgegenstand. Es gibt weiterhin keinen Produktcode in diesem Ticket; geprüft wird die Spezifikation (docs/beweise/NAK-180.md §2 bis §4, §5.2.1). Der Absatz „Stand 2026-09-05, 04:2x (Dirigent, Übergabe …)" in §5.2 ist Dirigentenprosa und kein Prüfgegenstand.

BEFUND, den diese Runde schließen sollte (wörtlich aus Matrixprüfung 2, docs/beweise/roh/NAK-180-matrixpruefung-2-f364d5c.txt; Regel R10 des Dirigenten in docs/beweise/NAK-180.md §5.2, Zeile MP2-1):
1. [P1] „Ziehe die Wire-Generation vor den Link-Callback" — Wenn `v3ControlLink(true)` den Worker für ein Replay weckt, wird die neue `wireGeneration` laut der bisherigen Reihenfolge erst nach Rückkehr des Callbacks vergeben (`ControlClient.cpp:1282-1283`). Der Worker kann daher ein zum neuen Link gehörendes Replay noch mit der alten Generation einreihen, worauf der unmittelbar folgende N-26-Filter es verwirft; bei Zustand 1 oder 3 erreicht anschließend das `end` den Broker ohne Begin und setzt wieder `unknown` (`intervention.rs:214-219`). Damit blieben MP1-1 und MP1-2 trotz N-26/N-28 offen: Generation und Filter müssen vor dem Callback liegen oder die neue Generation muss dem Callback übergeben werden, und N-26/N-28 samt B3b/B5-Rotbeweis müssen dieses Interleaving erzwingen. Regel R10: Die neue `wireGeneration` wird vor `meldeLinkStatus(true)` vergeben (oder dem Callback als Parameter übergeben), der Aufbaufilter (N-26) läuft vor dem Wecken des Workers; alles, was der Callback oder der geweckte Worker einreiht, trägt die neue Generation; N-26 und N-28 erzwingen dieses Interleaving im Test (Rotbeweis mit der alten Reihenfolge); §2.1 wird als Zusage mit der neuen Reihenfolge festgeschrieben; spiegelbildlich beim Linkende sieht `v3ControlLink(false)` die sterbende Generation.

Dazu der eigene Selbstauditfund des Workers (§5.2.1, letzter Absatz), den du mitprüfst: (1) der Aufbaufilter nimmt `sendeMutex` (Datenrennen auf der Deque zwischen Filter und gleichzeitig einreihendem Worker, vgl. `ControlClient.cpp:1438-1441`); (2) die Generationsmarke wird beim Einreihen aus `wireGeneration` gelesen, nicht beim Bilden der Nachricht; dazu die Begründung, warum zwischen Schritt 2 (Generation) und Schritt 4 (Callback) der Aufbaufolge kein neuer Bericht entstehen kann.

GATE-BEZUG, wörtlich (docs/FL-Nakama-Sonden-Design-Entwurf.md:4289): „Hörmarker- und Experimentende schließen alle Taintintervalle; der reguläre Pfad nullt danach wieder." Dazu Entwurf §34.2 (Resync als ausdrückliche, bestätigte Lebenszyklusoperation), SONDE-013-Matrix M-33 bis M-39 und M-58 bis M-63 (docs/beweise/SONDE-013.md:460-653), Invarianten aus CLAUDE.md (Audio-Thread ohne Lock/Allokation, Nulltest bitidentisch, NaN-Ehrlichkeit, Schemas eingefroren).

Du beantwortest genau zwei Fragen:
1. Ist der Befund MP2-1 am Zielstand in der Matrix geschlossen (ja/nein, mit Zeilen-IDs E9, N-26, N-28, den Schritten der Aufbaufolge in §2.1, messendem Test und Rotbeweis als Beleg)? Prüfe an der Quelle (`eq-copilot/plugin/core/ipc/ControlClient.cpp`, insbesondere `:1282-1283`, `:620-625`, `:708`, `:767`, `:1559`, `:1438-1441`; `IpcQueues.h`; `PluginProcessor.cpp`; `broker/src/coordinator/intervention.rs:214-219`), ob die festgeschriebene Aufbaufolge (sechs Schritte, spiegelbildliches Linkende) mit dem gemessenen Code zusammenpasst und ob sie das Interleaving tatsächlich ausschließt: Kann ein Replay oder ein Bericht, der während `meldeLinkStatus(true)` oder vom geweckten Worker eingereiht wird, noch eine alte Generation tragen? Kann zwischen Schritt 2 (Generation) und Schritt 4 (Callback, Wecken des Workers) ein neuer Bericht entstehen, und läuft der Aufbaufilter (Schritt 3) tatsächlich, bevor irgendein neuer Einreiher aktiv wird? Hält die spiegelbildliche Ordnung beim Linkende (die drei `false`-Aufrufer `:708`, `:767`, `:1559`) die sterbende Generation? Machen die zwei Zusatzpflichten (sendeMutex, Marke beim Einreihen) R10 vollständig, oder fehlt eine Pflicht, ohne die N-26/N-28 grün sein könnten, während der Produktpfad bricht?
2. Hat der Fixdiff etwas gebrochen — eine ältere Matrixzeile (N-01 bis N-33), einen Entscheid E1 bis E8, eine Zeile der SONDE-013-Matrix, einen Satz des Gate-Bezugs, eine Invariante (insbesondere: kein Lock und keine Allokation im Audio-Thread durch die Aufbaufolge), einen Vertrag (keine neuen Wire-Felder), die Beziehung verbinden↔trennen (§2.1 Linkende gegen `v3ControlLink(false)`)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1 und 2 (DEFEKT / LÜCKE / HÄRTUNG): DEFEKT nur, wenn eine Matrixzeile, R1 bis R10, der Gate-Satz, der Entwurf oder eine Invariante bricht; was Matrix und Entwurf zu einem Fall nicht sagen, ist LÜCKE; wünschenswert, aber von keiner Zusage verlangt, ist HÄRTUNG. Ausgeschlossen: docs/** außerhalb von NAK-180.md §2–§4/§5.2.1, Registerpunkte NAK-155 bis NAK-190, tools/dirigent/pruefliste.md als Anforderungsquelle, Ticketfremdes (NAK-181/182, `IpcVerbindung::oeffnen`-Fristlogik, G6/G7-Punkte NAK-183 bis NAK-185), Kosmetik, Prosa- oder Zeilenverweis-Inkonsistenzen im Manifest.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — MP2-1 geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID, Quelle Datei:Zeile, welcher Satz bricht, Befundklasse.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.