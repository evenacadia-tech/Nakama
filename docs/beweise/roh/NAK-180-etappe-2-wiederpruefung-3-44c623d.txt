URTEIL: NEEDS_WORK — die vier ursprünglichen Befunde sind geschlossen, jedoch bestehen zwei neue DEFEKTE im WN-02-Test, jeweils mögliche Fehlalarme bei korrektem Produktcode.

Geprüft: ausschließlich Fixdiff und notwendige Aufrufbeziehungen bei unverändertem HEAD 44c623de8eaf0c7348e9ed2e21ea1de0168755d0; WA-01 ja: `wn02ZustellungMeldetDieWireGeneration` startet den echten Client (`Sonde013PassageStateTest.cpp:2224`) und misst Serverempfang (`:2343–2354`), `ControlClient.cpp:2129` wird durchlaufen, `docs/beweise/roh/NAK-180-rot-wa01.txt:5,258–259` dokumentiert die alleinige Rücknahme und exakt die unzulässige Folge `begin/1 begin/1 end/2`; WA-02 ja: Workerquittung (`PluginProcessor.cpp:1329`, `.h:382–402`), bestätigte Pause in beiden N37-Messfenstern (`Sonde013PassageStateTest.cpp:2531–2596,2611–2667`), Rotbelege `-rot-wn04.txt:279` und `-rot-wn05.txt:282` fallen exakt durch Überschreiben mit G4 beziehungsweise Löschen von G7; WA-03 ja: Rust-R2 prüft vor `false` Unknown, Evidenzsperre und abgewiesene Einspeisung (`sonde013_verdrahtung.rs:4563–4578`), danach Annahme (`:4581–4596`), `-rot-wa03.txt:79–80` fällt exakt am unerlaubten Entsperren durch den Tick; WA-04 ja: `r01Resync` pausiert bestätigt vor Ringfüllung (`:729–735`), leert unter derselben Pause (`:811`) und gibt erst danach frei (`:829`), kein eigener Rotbeweis für diesen Fehlalarmfix, vorhandener Zehnfachbeleg `NAK-180-wa04-zehnmal.txt` mit jeweils 222/0; keine zusätzliche Produktverhaltensänderung, Audio-, Gate- oder Vertragsverletzung festgestellt, neue Produkthaken ausschließlich außerhalb des Audiothreads und im Produkt inaktiv.

Nicht geprüft: keine Builds, Test-/Binär- oder Python-Läufe gemäß Auftrag; Rotbelege, Zehnfachlauf und Kanon 53/53 ausschließlich gelesen, kein Code-Diff seit 5083eb6 festgestellt, neue Interleavings statisch hergeleitet und nicht ausgeführt, ausgeschlossene Bereiche nicht neu bewertet und Worktree unverändert belassen.

Full review comments:

- [P2] Warte vor der Generationsmessung auf den abgeschlossenen Aufbau — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013PassageStateTest.cpp:2225-2228
  NEU-01, DEFEKT (Testform): Wird der Client zwischen `zustand.status = verbunden` (`ControlClient.cpp:1844–1850`) und `aufbauZug()` (`:1858`) unterbrochen, endet diese Wartephase bereits, obwohl `g1` noch 0 ist. Nach Fortsetzung des Clients fällt deshalb die Prüfung bei Zeile 2251 trotz korrektem Produktcode; beim zweiten Aufbau kann entsprechend `g2 > g1` scheitern. Diese Reihenfolge ist auch ausdrücklich [festgelegt](docs/beweise/NAK-180.md#L179-L183): Der sichtbare Verbindungsstatus bestätigt noch keine Generationsvergabe. Beide Messungen müssen auf den abgeschlossenen Aufbau warten, beispielsweise auf dessen ersten empfangenen Heartbeat.

- [P2] Ordne den Reconnect vor der Freigabe des zweiten Servers an — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013PassageStateTest.cpp:2258-2259
  NEU-02, DEFEKT (Testform): Erhält der automatische Wiederverbindungsversuch des laufenden Clients nach dem Serverstart ausreichend Laufzeit, kann er das wartende Begin bereits zustellen, bevor der Test `v3ReconnectFuerTest()` aufruft. Dieser Aufruf trennt dann den gerade aufgebauten Link erneut. Auf dem folgenden Link ist ein Replay gemäß [N-05](docs/beweise/NAK-180.md#L299) korrekt, aber `server->p0Texte` sammelt Nachrichten beider Verbindungen: Die Prüfungen bei Zeilen 2347–2354 melden dadurch dieselbe Doppelung wie der Rotbeweis bei korrektem Writer. Reproduktionsablauf: Testfaden nach Serverstart bis zum ersten Begin-Empfang zurückstellen, anschließend fortsetzen. Der Test muss genau einen Neuaufbau erzwingen, statt automatische Wiederverbindung und zusätzlichen Reconnect konkurrieren zu lassen.