# NAK-182 — G4-Nacharbeit 3: Korpus und Goldens

| Merkmal | Wert |
|---|---|
| Ticket | NAK-182 (Register `docs/offene-punkte.md`), Nacharbeit zum Phasengate **G4** (`docs/beweise/G4.md` §1, §6 Satz 1, §8 Entscheid zu M-85, §11 Nr. 3) |
| Phase | **Etappe 2: Nacharbeit 1 abgeschlossen 2026-09-06 (Basis `6087684`, Ende `d3d9661`), betroffene Beine grün, Wiederprüfung 1 offen.** Regeln NR-01 bis NR-05 aus `docs/beweise/roh/NAK-182-etappe-2-nacharbeit-1-auftrag.txt`, Zuordnung und Messung in §7.1; alle fünf Defekte EP-01 bis EP-05 geschlossen, sechs Rotbeweise, kein Produktcode berührt. B16, B19, A26, A27, A25, A8, A5, A4 `sonde013_verdrahtung` und `EqCopNullTest` je Exit 0 (`roh/NAK-182-nacharbeit-1-beine-d3d9661.txt`); kein voller Kanon in dieser Runde, so beauftragt. Stand davor: **Erstprüfung 1 NEEDS_WORK 2026-09-06 (Codex Astra max, Thread `01a075e5-6643-71f3-a2a3-a3ea78a0fa17`, Prüfbereich `c4290fb...054883f` über die 15 Ticketpfade ohne `docs/**`; §7: fünf Defekte EP-01 bis EP-05, alle vom Dirigenten an der Quelle bestätigt, 0 Lücken, 0 Härtungen — alle fünf in Tests und Prüfwerkzeug, keine Produktregression: die C++-Hälfte der 0,01-dB-Kette misst Frame-Arrays statt Wire-Text, der Laufzeit-Sweep wertet nur einen von drei Trägern aus, der Unkorreliert-Sweep prüft keine Empfehlungsfreiheit, S-07 erreicht die Validierung nicht, die Fixture trägt kein Material). Nacharbeit 1 beauftragt (Runde 1 von 3, frischer Opus-Worker, `docs/beweise/roh/NAK-182-etappe-2-nacharbeit-1-auftrag.txt`, Regeln NR-01 bis NR-05), danach Wiederprüfung 1 nach Vorlage B über den Fixdiff.** Stand davor: **Etappe 2 (Bau) abgeschlossen 2026-09-06, Worker.** Sechs Bauetappen (`c0f7f8c` bis `c143397`), zwoelf Rotbeweise, voller Kanon abgekoppelt auf dem Endstand; Beine und Laufzeiten in §6.2, Abweichungen in §6.4. Stand davor: **Matrixpruefung 3 PASS 2026-09-06** (Codex Astra max, `docs/beweise/roh/NAK-182-matrixpruefung-3-7a5755c.txt`) — Etappe 1 konvergiert, die Matrix im Stand `7a5755c` ist Referenz fuer Bau und Pruefer. Davor: Angelegt 2026-09-05; Etappe 1 (Spezifikation), Etappe 1b (Entscheid zu E1 und E4), Matrixnacharbeit 1 (MP1-1 bis MP1-7) und Matrixnacharbeit 2 (MP2-1, MP2-2), alle 2026-09-06. |
| Prüfstufe | T3-Nacharbeit: Matrixprüfung vor dem Bau, danach Erst- und Wiederprüfung nach Vorlage B (frischer Codex-Thread, Astra, Effort max — User-Wort 05.09.2026; Sol nur Gegenprüfer), voller Kanon auf dem Endstand. |
| Basis-SHA | `c4290fb15ebea9f30ca19772aeb6ed80a68ad2c7` (Worktree sauber beim Start der Etappe 1) |
| Regeln | R1 bis R7 wörtlich in `docs/beweise/roh/NAK-182-auftrag.txt`. **R1 ist seit Etappe 1b durch R1a ersetzt** (zwei geschlossene Mengen statt einer); Wortlaut in `docs/beweise/roh/NAK-182-etappe-1-entscheid-auftrag.txt`, eingearbeitet in E1b, Paragraph 2.1 bis 2.4, 4.2 und 4.4 |
| Befunde | C6 (M-79), C7/L-03 (M-81, Fallnamen), C8/L-01 (M-82), C9 (M-83 Satz 2), C10/L-02 (M-84), C11 (M-85, Korpus-Ehrlichkeit; Ziel 1 → NAK-190) — Wortlaut in `docs/beweise/roh/G4-codex-falsifikation-9f30008.txt`, `docs/beweise/roh/G4-eigener-lauf-9f30008.md` (B5), `docs/beweise/roh/G4-quellencheck-codex-9f30008.md` |
| Ticketpfade | **Tatsaechlich beruehrt in Etappe 2** (`git diff --name-only c4290fb..HEAD`): `tools/eq-copilot/pruefe_p4_korpus.py`, `tools/eq-copilot/erzeuge_p4_korpus.py`, `tools/eq-copilot/erzeuge_v3_fixtures.py`, `eq-copilot/fixtures/p4-korpus/` (5 Dateien), `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` (neu), `eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp`, `AnalysisGoldenTestMain.cpp`, `Sonde013EventWireTest.cpp`, `Sonde013TruePeakGoldenTest.cpp`, `LoudnessGoldenTestMain.cpp`, `QueueStressTestMain.cpp`, `SondeNullTestMain.cpp`, `broker/tests/sonde013_verdrahtung.rs`, **`broker/src/coordinator/experiment_verdrahtung.rs` — nur lesende Testauskunft** (`resultatmessung_fuer_test`, delegiert ausschliesslich), `tools/beweise.ps1` (Bein-Behauptungen und das neue Bein A27), `docs/beweise/NAK-182.md`, `docs/beweise/roh/NAK-182-*`. **Nicht beruehrt**, obwohl in der Ticketpfadmenge vorgesehen: `eq-copilot/plugin/CMakeLists.txt` (A27 ist ein Python-Bein, kein neues Testziel), `.gitattributes` (die `-text`-Regel fuer `eq-copilot/fixtures/v3/**` steht seit NAK-181), `Sonde013PrePostGoldenTest.cpp`, `Sonde013DynamicsTest.cpp`, `Sonde013FingerprintGoldenTest.cpp`, `broker/tests/sonde013_prepost.rs`, `sonde013_experiment.rs`, `sonde013_passage.rs`, `sonde013_taint.rs` (ihre Bezeichner standen bereits woertlich da). Sonst bleibt Produktcode unter `eq-copilot/plugin/src`, `core`, `sonde`, `vertrag`, `state` und `broker/src` ausgeschlossen. |

## 1. Gate-Bezug, wörtlich

P4-Exit-Gate Satz 1: „Referenzkorpus, Loop-/Seek-/PDC-Goldens und adversariale Vergleichsfixtures bestehen." Matrix SONDE-013 §3.10 M-79 bis M-85.

## 2. Verhaltensmatrix

### 2.0 Der gewählte Weg — ein Entscheid je Regel, mit Begründung

Sechs Entscheide tragen die Matrix. Sie weichen an neun Stellen von der
naheliegenden Lesart des Ticketauftrags ab, weil die Quelle am Basis-SHA
etwas anderes zeigt als der Gate-Stand `9f30008` vermuten ließ; jede
Abweichung steht einzeln in §4.4. Vorbild der Form: `docs/beweise/NAK-181.md`
§2.0.

---

**E1 — R1: `UNSICHERE_WAHRHEITEN` reicht weiter als der Riegel für starke Behauptungen. Zwei bestehende *schwache* Fälle fallen mit, und ihre Wahrheit wird berichtigt, nicht ihre Aussage.**

Der Auftrag verlangt, dass `_passt` und `moegliche_starke` dieselbe benannte
Menge lesen und dass „eine starke ODER schwache Aussage auf einer dieser
Wahrheiten eine falsche Behauptung" ist. Am Basis-SHA liest
`moegliche_starke` (`tools/eq-copilot/pruefe_p4_korpus.py:91-92`) vier Werte,
`_passt` (`:139`) einen. Das Zusammenziehen ist fünf Zeilen — seine Folge ist
es nicht, weil `_passt` **auch** Brier Score und Kalibrierung speist
(`:96-107`).

Gemessen am committeten Korpus (§3.2, Rohlauf
`docs/beweise/roh/NAK-182-ist-stand-c4290fb.txt`): mit der erweiterten Menge
und **sonst unverändertem Korpus** springt `falsche_schwache` von 0 auf **2**
und der Brier Score der Klasse `vergleich` von 0,1413 auf **0,3912** —
schlechter als ein Münzwurf. Die beiden Fälle sind

- `A4::distributed_pair_never_produces_transfer_function`, `wahrheit: nicht_kausal`, `aussage: schwach`,
- `A16::insert_probe_is_never_called_master_sum`, `wahrheit: nicht_exakt`, `aussage: schwach`.

Der Gesamt-Brier bliebe mit 0,0468 unter dem Gate von 0,25, und
`falsche_schwache` ist heute **kein** Riegel — das Gate bliebe also grün und
meldete gleichzeitig zwei falsche Behauptungen. Das ist genau die Sorte
leises Grün, die C6 begründet.

Der Entscheid berichtigt die **Wahrheit**, nicht die Aussage, denn an der
Quelle ist beides keine Unsicherheit:

- `broker/tests/sonde013_prepost.rs:479` baut ein **verteiltes** PRE/POST-Paar
  und misst, dass daraus Banddifferenzen und kein Frequenzgang entstehen. Die
  Wahrheit des Falls ist „verteilt", nicht „nicht kausal"; die schwache
  Aussage trägt sie.
- `eq-copilot/plugin/tests/SondeNullTestMain.cpp:253-270` baut eine
  **Insertprobe** auf einem einzigen Stereo-Bus und misst, dass
  `post_fader_contribution` read-only zurückkommt. Die Wahrheit ist
  „insertprobe"; dass sie **kein** exakter Mastersummenbeitrag ist, ist gerade
  das Sichere an ihr.

Damit sind die vier unsicheren Wahrheiten wieder genau das, was Gate 6 und
Gate 7 meinen: Lagen, in denen **keine** Behauptung getragen wird — weder
stark noch schwach. Der Korpus trägt sie nach der Korrektur nur noch mit
`aussage: unsicher`.

`ERLAUBTE_WAHRHEITEN` wird im Erzeuger geschlossen geführt (wie
`ERLAUBTE_AUSSAGEN`, `erzeuge_p4_korpus.py:148`), und der Prüfer weist eine
Wahrheit außerhalb der Menge ab — sonst entstünde beim nächsten Fall eine
dritte Schreibweise, und `_passt` fiele still auf „passt" zurück.

**Warum `recall <= 1.0` ein eigener Riegel ist und nicht aus dem ersten folgt.**
Gemessen (§3.2): der C6-Rotbeweis an
`gate6_incomparable_never_gets_a_strong_winner` lässt A26 heute grün und
erzeugt `recall = 1,0667`. Die Kennzahl widerlegt sich selbst, und kein Gate
sieht es. Nach E1 wird derselbe Fall rot — der Recall-Riegel ist dann eine
**Wache**, kein Fänger: er hält die Stelle fest, an der Zähler und Nenner
auseinanderlaufen dürften, falls eine spätere Runde `_passt` und
`moegliche_starke` wieder trennt. Dieselbe Bauform wie in NAK-181 E1: ein
Riegel, für den es nach dem Fix keinen Korpusweg mehr gibt und der seinen Weg
deshalb **synthetisch** im Selbsttest bekommt (§2.4 S-03).

**Etappe 1b (Entscheid des Dirigenten, 2026-09-06) — R1a ersetzt die
Einmengenlogik durch ZWEI Mengen; die zwei Umbenennungen sind abgelehnt.**
Wortlaut des Entscheids:
`docs/beweise/roh/NAK-182-etappe-1-entscheid-auftrag.txt`. Die Zeilen darüber
bleiben stehen, damit die Geschichte lesbar bleibt; **bindend ist ab hier
dieser Block.**

*Angenommen aus E1:* `falsche_schwache`, `precision <= 1,0` und
`recall <= 1,0` werden Riegel; der Selbsttest wird ein eigenes Bein A27; der
B24-Fall trägt `zeitvariabel`.

*Abgelehnt aus E1:* `nicht_kausal` → `verteilt` und `nicht_exakt` →
`insertprobe`. Der Grund ist an der Zusage gemessen und trifft: **M-85 Ziel 3
/ §49.2 Gate 7** („Eine Standard-Insertprobe wird als exakter
Mastersummenbeitrag bezeichnet") verlangt, dass eine **starke** Aussage auf
dem A16-Fall eine falsche Behauptung ist — R6 nennt genau diesen Rotbeweis.
Mit `insertprobe` außerhalb der unsicheren Menge ließe `_passt` eine starke
Aussage dort durch, und Gate 7 wäre im Prüfer wieder blind: derselbe Fehler
wie C6, nur eine Ebene weiter. Für
`distributed_pair_never_produces_transfer_function` gilt dasselbe: eine
**starke** Aussage (Frequenzgang aus einem verteilten Paar) ist falsch, die
**schwache** (Banddifferenzen) trägt die Wahrheit.

Der Fehler der Erstfassung lag also nicht in der Beobachtung (§3.2 misst
richtig), sondern in der Folgerung: sie behandelte „keine **starke**
Behauptung" und „**keine** Behauptung" als eine Klasse. Es sind zwei.

**Die zwei Mengen (R1a).** Beide werden im **Erzeuger** geschlossen geführt
**und im MANIFEST mitgeführt**; der Prüfer liest sie von dort — eine Quelle,
keine Kopie im Prüfskript. Ein Prüfer mit eigener Kopie wäre genau die zweite
Wahrheit, an der C6 hing.

| Menge | Werte | Was sie verbietet | Zusage dahinter |
|---|---|---|---|
| `KEINE_BEHAUPTUNG` | `unbekannt`, `unvergleichbar`, `zeitvariabel` | **jede** Behauptung, stark **und** schwach | Gate 6 (§49.2); Exit-Gate Satz 2; M-83 Satz 3 — „keine falsche statische EQ-Behauptung", auch keine schwache |
| `KEINE_STARKE` | `KEINE_BEHAUPTUNG` + `nicht_kausal`, `nicht_exakt` | nur die **starke** Behauptung; die schwache trägt die Wahrheit | Gate 7 (§49.2); verteiltes Paar: Banddifferenzen ja, Frequenzgang nein. Insertprobe: Beitrag ja, exakter Mastersummenbeitrag nein |

Daraus die drei Zeilen des Prüfers:

    _passt:  unsicher -> True
             stark    -> wahrheit nicht in KEINE_STARKE
             schwach  -> wahrheit nicht in KEINE_BEHAUPTUNG
    moegliche_starke = Faelle mit wahrheit nicht in KEINE_STARKE

`ERLAUBTE_WAHRHEITEN` umfasst beide Mengen und die sicheren Wahrheiten; eine
Wahrheit außerhalb ist rot (§2.1).

**Gemessen unter R1a am committeten Korpus** (dieselbe Rechnung wie §3.2, mit
den drei Zeilen oben statt der Einmengenform):

| Klasse | `falsche_starke` | `falsche_schwache` | Precision | Recall | Brier |
|---|---:|---:|---:|---:|---:|
| `referenz` | 0 | 0 | 1,00 | 1,0000 | 0,0010 |
| `stereo` | 0 | 0 | 1,00 | 1,0000 | 0,0009 |
| `vergleich` | 0 | 0 | 1,00 | 1,0000 | 0,1413 |
| `zeitachse` | 0 | 0 | 1,00 | 1,0000 | 0,0007 |
| **gesamt** | **0** | **0** | 1,00 | 1,0000 | **0,0174** |

Kalibrierung 0,0671, Coverage 0,77 — **beide identisch zum heutigen Lauf**,
und der Brier springt nicht (0,0174 heute wie unter R1a). Die Zahlen aus §3.2
(`falsche_schwache = 2`, Klassen-Brier 0,3912) gelten für die **Einmengenform**
und bleiben dort als Messung stehen; sie sind der Beleg dafür, **warum** R1a
zwei Mengen braucht, nicht ein Befund über den Korpus.

Der B24-Fall mit `wahrheit: zeitvariabel` und `aussage: unsicher` (E6) ändert
unter R1a keine Kennzahl: eine Enthaltung passt immer. Gemessen: gesamt
`falsche_starke` 0, `falsche_schwache` 0, Brier 0,0174 — unverändert.

**Der Recall-Riegel wird damit schärfer begründet.** Unter R1a lesen `_passt`
(starker Zweig) und `moegliche_starke` **dieselbe** Menge `KEINE_STARKE`;
`richtige_starke <= moegliche_starke` gilt deshalb strukturell, und
`recall > 1` ist auf keinem Korpusweg mehr erreichbar. Der Riegel ist eine
**Wache** gegen ein künftiges Auseinanderlaufen der beiden Leser — genau der
Riss, den C6 gefunden hat — und bekommt seinen Weg synthetisch im Selbsttest
(§2.4 S-03).

**Was `nicht_kausal` und `nicht_exakt` im Korpus behalten.**
`A4::distributed_pair_never_produces_transfer_function` bleibt
`nicht_kausal`/`schwach`/0,60, `A16::insert_probe_is_never_called_master_sum`
bleibt `nicht_exakt`/`schwach`/0,65 — unverändert gegenüber dem Basis-SHA. Die
Änderungen an §2.3 aus Etappe 1 sind insoweit zurückgenommen (§2.3, Block
Etappe 1b).

---

**E2 — R2: Der Bezeichner ist ein Wort, das im Test steht — nicht die Prosa des Abschnitts. Wo keines steht, wird eines *hinzugefügt*, nie eines umbenannt.**

An der Quelle gibt es drei Bauformen für „Fallname" (§3.3):

1. `abschnitt ("M-08  mono_identity")` — Kürzel plus snake_case-Bezeichner
   (`Sonde013StereoGoldenTest.cpp`, `Sonde013DynamicsTest.cpp`,
   `Sonde013PrePostGoldenTest.cpp`, `Sonde013FingerprintGoldenTest.cpp`).
2. Reine Prosa: `abschnitt ("§40.3  Polaritaetsinvertierung: Korrelation -1, …")`,
   `grenzfall ("G1 Drop (kFlagLueckeDavor)", …)`,
   `std::cout << "== B - Rampe, Rauschen, lange Laufzeit =="`.
3. Rust: der `fn`-Name (`broker/tests/*.rs`).

Ein Prüfer, der die Prosa als Bezeichner nimmt, bindet den Korpus an einen
Satz, den jeder Kommentarumbau bricht. Der Entscheid ist deshalb: **jeder
Korpusfall trägt genau ein snake_case-Wort, das wörtlich in seiner Quelldatei
steht.** Fehlt es, wird es in die bestehende Abschnittszeile eingesetzt
(`abschnitt ("§40.3  polarity_inversion — Korrelation -1, …")`); bestehende
Bezeichner bleiben unangetastet, und kein Testziel, keine Funktion und kein
`fn`-Name wird umbenannt.

Der Prüfer sucht das Wort mit einer wörtlichen Textsuche in der genannten
Datei — dieselbe Bauform wie `_bein_bekannt` (`:62-74`), eine Ebene tiefer:
vom Bein auf den Fall. Zusätzlich prüft er, dass die Datei existiert und der
Pfad relativ zur Repo-Wurzel steht; ein Pfad ins Leere ist rot, kein Hinweis.

**Die substanzlose Zeile.** `B18::lufs_integrated_matches_reference` gibt es
weder wörtlich noch sachlich: `Sonde013DynamicsTest.cpp` führt keinen
LUFS-I-Referenzfall (§3.3). Die ±0,1-LU-Prüfung liegt in **B9**
`EqCopLoudnessGoldenTest`, `LoudnessGoldenTestMain.cpp:217`
(`name + ": LUFS-I innerhalb ±0,1 LU"`, Konstante `kToleranz = 0.1` bei
`:164`). Der Fall zeigt ab hier dorthin, mit `bein: B9`, und
`KLASSENBEINE["referenz"]` führt B9 mit. Kein neuer B18-Fall — das wäre die
Doppelmessung, die der Korpuskopf ausdrücklich verbietet
(`erzeuge_p4_korpus.py:7-12`).

**Und die Zusage wird dabei ehrlich beschrieben, nicht gedehnt.** M-81 sagt
„gegen eine validierte Referenz auf **Standard-Testmaterial**". B9 misst gegen
die **ausgebaute Vektorrechnung** — den Zweitdurchgang, der bis 23.08.2026 in
`AnalyseEngine::finalisiereLoudness` stand (`LoudnessGoldenTestMain.cpp:6-12`),
über eigene Korpora (konstante Pegel, Rampe, Rauschen, eine Stunde, Stille
unter dem absoluten Gate, Oberband). Das ist eine **unabhängige zweite
Implementierung**, kein externes Testset. Der `hinweis` des Falls sagt genau
das. Damit ist die erste Hälfte von M-81 („±0,1 LU gegen eine validierte
Referenz") gemessen und der Zusatz „auf Standard-Testmaterial" für LUFS-I
**nicht** — er ist für True Peak gedeckt (B17 fährt das offizielle
EBU-Tech-3341-Set, Fälle 15 bis 19, `Sonde013TruePeakGoldenTest.cpp:127`).
Diese Teilung steht als Belegspalte in §2.2 und als Nebenbefund in §4.7;
**kein neues Testset in diesem Ticket.**

---

**E3 — R3: Die Blockgrößenmenge kommt aus dem Layout, nicht aus B4s Liste; der Sweep ist ein eigener Abschnitt, und die sechs Stereo-Korpusfälle ziehen auf ihn.**

Die Menge steht dreifach an der Quelle, und die engste gewinnt:

| Quelle | Aussage |
|---|---|
| `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:160` | `block_size`: `minimum` 1, `maximum` 65536 — der Vertrag |
| `eq-copilot/plugin/core/StampedAudioQueue.h:240` | `GenStrom = StreamLayout<1, 131072, 2048, 16384>` — `maxBlockFrames` ist **16384**; ein größerer Hostblock wird als Ganzes nur für die Analyse verworfen (`:40-43`) |
| `eq-copilot/plugin/tests/QueueStressTestMain.cpp:666-667` | die 18 Größen `1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345, 16384`, die B4 §J fährt |

**„Unterstützt" heißt für die Analyse 1 bis 16384** — das ist die
Slotkapazität aus dem Layout, und B4s größter Wert ist genau sie. Die Menge
steht im Test **genau einmal** als benanntes `constexpr`-Feld und trägt einen
Kommentar auf `StampedAudioQueue.h:240`.

**Laufzeitbudget.** B19 speist die `FeatureEngine` direkt (`Speiser::sende`,
`Sonde013StereoGoldenTest.cpp:102-119`), also gilt die Slotkapazität hier
nicht als Filter, wohl aber als Definition von „unterstützt". Ein Sweep über
alle 18 Größen × 6 Klassen wären 108 Läufe bis zum ersten Evidenzfenster. Ein
Evidenzfenster braucht bei `evidenzIntervallSetzen(1.0)` rund eine Sekunde
Audio plus Fensterfüllung; die FFT-Arbeit ist **blockgrößenunabhängig** (sie
hängt an Samples, nicht an Blöcken), der Blockmehraufwand bei Größe 1 sind
rund 48 000 zusätzliche `nimmBlock`-Eintritte je Sekunde Audio.

Der Entscheid: **Teilmenge, und die Bein-Behauptung sagt „über die Menge X",
nie „über alle".** Fünf Größen, jede mit Grund:

| Größe | Grund |
|---:|---|
| 1 | die kleinste; FL zerteilt Puffer bis auf 1 Sample (`StampedAudioQueue.h:42`, Capabilityreport S4) |
| 333 | Nichtteiler des Hauptfensterhops (2048) **und** teilerfremd zur Rahmenkadenz — die Phasenfalle aus `AnalysisGoldenTestMain.cpp:2152-2162` |
| 512 | der heutige Speiserwert, damit der Sweep den bestehenden Befund reproduziert |
| 2048 | genau der Hauptfensterhop `kHauptHop` (`FeatureEngine.h:932`) — der einzige Wert, bei dem Block- und Fenstergrenze zusammenfallen |
| 16384 | die größte; zugleich `kBassPunkte` (`FeatureEngine.h:927`) und die Slotkapazität |

Das sind 30 Läufe statt 108. **Zielbudget: B19 bleibt unter 60 s.** Der
gemessene Wert geht in Etappe 2 in §6; bleibt er unter 20 s, wird die Menge
in derselben Etappe auf alle 18 Größen erweitert und die Behauptung auf
„über alle unterstützten Blockgrößen (1 bis 16384)" gezogen. Wird das Budget
gerissen, bleibt es bei den fünf, und die Behauptung nennt sie einzeln.

**Warum ein eigener Abschnitt und nicht die bestehenden sechs umgebaut.** Die
bestehenden Abschnitte messen ihre Klasse **tief** (Korrelation,
Seitenanteil, Kohärenz, Phase, Folddown, Balance, je mit eigener Zahl) und
brauchen dafür `evidenzIntervallSetzen(1.0)` und eine bestimmte Materialwahl.
Sie 30-fach zu fahren vervielfacht Laufzeit und Ausgabe, ohne eine neue
Zusage zu messen. Der Sweep misst je Klasse und Blockgröße **die eine
analytisch bekannte Antwort**, die M-82 nennt — und zusätzlich, dass je
Blockgröße vergleichbar viele Frames gemittelt wurden.

Die sechs Stereo-Korpusfälle ziehen deshalb auf die **sechs Bezeichner des
Sweeps**, denn dort steht die vollständige Zusage von M-82; die bestehenden
Abschnitte bleiben unverändert und stehen im `hinweis`.

---


**Matrixnacharbeit 1 (MP1-7) — die Bein-Behauptung nennt in BEIDEN Zweigen die
gefahrene Menge; „über alle" fällt weg.**

Matrixprüfung 1 hat den Rückweg aus E3 als Defekt geführt: „Falls das Budget
unter 20 Sekunden bleibt, soll die Bein-Behauptung auf ‚alle unterstützten
Blockgrößen' wechseln, obwohl nur die 18 Werte aus
`QueueStressTestMain.cpp:666-667` gefahren werden … beispielsweise 256 bleibt
im 18er-Sweep ungemessen." Der Befund trifft: die unterstützte Menge ist das
ganze Intervall 1 bis 16384 (`StampedAudioQueue.h:240`), 16384 Werte; die 18
sind eine Auswahl daraus, keine Erschöpfung.

**Regel MN1-7.** Der Zweig bleibt, der Wortlaut nicht:

| Zweig | Bein-Behauptung von B19, wörtlich |
|---|---|
| Budget gerissen (Regelfall) | „über die fünf Blockgrößen 1, 333, 512, 2048, 16384" |
| Budget unter 20 s | „über die 18 Blockgrößen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345, 16384" |

**Nie „über alle".** In keinem Zweig. §2.5 hält fest, dass M-82s „alle
unterstützten Blockgrößen" nicht erschöpfend fahrbar ist und durch die
benannte Menge **repräsentiert** wird; die Auswahl ist begründet (Ränder 1 und
16384, Hop-Teiler 512 und 2048, Hop-Nichtteiler 333 mit der Phasenfalle).
E3, N-15 bis N-20, §4.3 und §4.4 Nr. 5 sagen ab hier dasselbe.

---
**E4 — R4: Der Produktpfad existiert ende-zu-ende. Er liegt im Experimentpfad, nicht im Paarurteil — und deshalb wird ohne Schemaänderung und ohne Produktcode gemessen.**

Die erste Frage der Matrix lautet: **welcher Produktpfad liefert aus
dekodierter 0,01-dB-Evidenz Gain UND Intervall?** Die Antwort steht in §3.1
mit Zeilen; hier der Entscheid, der daraus folgt.

Es gibt **zwei** Wege, und nur einer trägt beides:

| Weg | Gain | Intervall | Urteil |
|---|---|---|---|
| Paarurteil PRE/POST — `Dreifachergebnis` (`broker/src/coordinator/prepost.rs:294-313`) | ja: `match_gain_db`, der Median der Bandrelationen (`:826-828`) | **nein** — die Struktur hat kein Intervallfeld | trägt M-83 Satz 2 **nicht** |
| Experimentpfad — `Achsenrechnung` (`broker/src/coordinator/experiment.rs:199-224`) | ja: `Resultatmessung::band_delta_db`, je Band Resultat minus Baseline (`experiment_verdrahtung.rs:936-943`) | **ja**: `intervall: Option<(f64, f64)>` aus `block_bootstrap` (`experiment.rs:335-341`, `:1366`) | trägt **beides** |

Und beide Größen des zweiten Weges kommen aus **derselben** dekodierten
Evidenz: `Evidenzstand::aus_json` ruft `perzentil_dekodieren`
(`evidenz.rs:232`, `:327-359`), dort ist `q_db_0p01_i16` der Teiler 100,0
(`:335`); das Ergebnis ist `Evidenzstand.p50_db` (`:92`), und genau daraus
baut `Verdrahtung::resultatmessung` (`experiment_verdrahtung.rs:787-975`)
`band_delta_db` und die Zeitreihe `fenster_delta_db`, aus der der Bootstrap
sein Intervall zieht.

**Der Befund lautet also nicht „der Pfad fehlt", sondern „der Pfad ist
ungemessen".** Kein Test fährt Sender → Brokerdekodierung → Auswertung als
**eine** Kette; die Rust-Hüllkurven in `sonde013_prepost.rs:33-104` kommen nie
durch `perzentil_dekodieren`, und `Achsenrechnung.intervall` wird heute nur
aus **von Hand gesetzten** `Resultatmessung`-Feldern gemessen
(`sonde013_verdrahtung.rs:816`, `:2487`; `sonde013_experiment.rs:27-57`). Kein
Produktcode und kein Schemafeld sind nötig.

**Der gewählte Messweg — drei Zeilen, eine Kette.**

1. **C++-Hälfte (B16).** Die echte `FeatureEngine` erzeugt aus bekanntem
   Material einen `evidence_snapshot`; `fuelleVerteilung`
   (`FeatureEngine.h:3925-3930`) setzt `encoding = q_db_0p01_i16` für
   P10/P50/P95, `NakamaEvidenz.cpp:474-478` schreibt `verteilung` auf den
   Draht. Gemessen wird an **ihrem eigenen Wire-Text** mit der Vertragsformel
   (Wert geteilt durch 100): PRE mit bekanntem Bandpegel, POST = PRE + G, und
   die Bandzahl aus `verteilung.p50` gibt G innerhalb ±0,1 dB wieder. Dazu die
   Auflösung selbst: zwei Pegel, die sich um 0,01 dB unterscheiden, erzeugen
   im Wire-Text zwei um genau 1 verschiedene Ganzzahlen — die Zusage
   „0,01-dB-Evidenz" wird gemessen, nicht angenommen.
2. **Rust-Hälfte (A4).** Zwei Snapshots derselben Quelle (Baseline und
   Resultat) mit `encoding: q_db_0p01_i16` gehen durch den echten Wirepfad in
   den Coordinator; ein Experiment wird begonnen, ein Kandidat erfasst und das
   Ergebnis abgeschlossen. Das Terminal trägt `achsen.intervall`, und das
   Intervall enthält G und liegt mit beiden Grenzen innerhalb ±0,1 dB von G.
   Harnisch und Weg stehen schon: `sonde013_verdrahtung.rs:3990-4093`
   (`passagenbelege_kommen_aus_den_vertretenen_quellen`) fährt genau diese
   Kette, nur mit anderer Erwartung.
3. **Die Kopplung.** Beide Hälften messen **dieselbe Datei**, die keine von
   beiden schreibt: `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json`
   (Zielpfad, in Etappe 2 anzulegen), erzeugt von
   `tools/eq-copilot/erzeuge_v3_fixtures.py`, bytegleich geprüft
   von A8 `--pruefen` und vom Textriegel A5 mitgelesen — dieselbe Bauform wie
   `evidenz-zahlen-wire-v1.json` aus NAK-181. Die Datei trägt das PRE/POST-Paar
   mit dem bekannten Bandpegel, dem bekannten G und der 0,01-dB-Auflösung; die
   C++-Hälfte prüft, dass **ihr eigener** Encoder für dasselbe Material
   dieselben Ganzzahlen schreibt, die Rust-Hälfte speist die Datei ein.
   `.gitattributes` bekommt keine neue Zeile: `eq-copilot/fixtures/v3/**` steht
   bereits als `-text` (`.gitattributes:36`).

**Warum nicht der einfachere Weg über `Dreifachergebnis`.** Er läge näher an
M-83s Wortlaut („PRE/POST"), trägt aber kein Intervall; ihn zu messen hieße,
Satz 2 zur Hälfte zu messen und die andere Hälfte offen zu lassen. Der
Experimentpfad misst beide Hälften aus einer Dekodierung — das ist die Kette,
die M-83 Satz 2 verlangt. Dass das Wort „PRE/POST" im Experimentpfad
„Baseline/Resultat" heißt, steht als Belegspalte in §2.2 und als Nebenbefund
in §4.7.

**Etappe 1b (Entscheid des Dirigenten, 2026-09-06) — die Kopplung nimmt Form B:
die Fixture hält ZAHLEN, nicht den Encoderausgang.**

Der Dirigent hat den Widerspruch zwischen E4 Nr. 1 („G innerhalb ±0,1 dB") und
E4 Nr. 3 („dieselben Ganzzahlen") benannt und die Wahl der Matrix überlassen:
Ganzzahl-Gleichheit **nur**, wenn die Matrix mit Zeilen belegt, dass der echte
Encoder für das gewählte Material deterministisch dieselben Ganzzahlen
schreibt. **Die Matrix wählt Form B** — sie kann das nicht belegen, und zwar
aus vier Gründen an der Quelle:

1. **Der Ring ist `float`, die Rechnung `double`.**
   `evidenzVerteilung[b].schiebe((float) db)`
   (`eq-copilot/plugin/core/analysis/FeatureEngine.h:2649`) speichert den
   Bandpegel als `float`; `VerteilungsRing` (`:905-911`) hält ihn so. Die
   spätere Perzentilrechnung und `quantisiere16` laufen wieder in `double`.
   Der Unterschied POST minus PRE ist deshalb G **bis auf die
   float-Rundung**, nicht exakt G — weit innerhalb ±0,1 dB, aber nicht
   bitgleich.
2. **Die Rundungsregel hat eine Kante.**
   `quantisiere16` rechnet `std::round(db * skalierung)` — halbe Werte von
   null weg (`BandGrid.h:148-163`). Ein Bandpegel, dessen wahrer Wert auf
   einem Vielfachen von 0,005 dB liegt, kippt die Ganzzahl bei einem
   einzigen ULP Unterschied. Eine committete Ganzzahl wäre damit an eine
   Fließkommakante gebunden.
3. **Cross-Build-Determinismus ist im Repo nirgends zugesagt.** Die
   bestehenden Bitgleichheitszusagen gelten je **einem** Lauf desselben
   Binärstands: die Zwillingsprobe in B5 vergleicht zwei Engines im selben
   Prozess (`AnalysisGoldenTestMain.cpp:584`), B22 zweimal dasselbe Material
   im selben Lauf. Der User arbeitet an zwei Rechnern (CLAUDE.md); eine
   Fixture, die Encoderganzzahlen festschreibt, wäre die erste Zusage dieser
   Art und gehört nicht in ein Nacharbeitsticket.
4. **Der Absolutpegel ist gar nicht vorhersagbar, die Differenz schon.**
   Der Bandwert entsteht als `energieAlsDb` (`BandGrid.h:252-257`,
   `10*log10` der linear integrierten Energie) über Fensterfunktion,
   FFT-Normierung und Bandaggregation. Welche Zahl dabei herauskommt, ist ohne
   zweite Implementierung nicht zu behaupten. Ein **skalarer Gain** g
   multipliziert die Energie mit g² und verschiebt damit **jedes** Band um
   exakt `20*log10(g)` — die Differenz ist vorhersagbar, der Absolutwert
   nicht. Deshalb misst die C++-Hälfte die **Differenz**.

**Form B im Wortlaut.** E4 Nr. 1 und Nr. 3 lauten ab hier so:

- **Die Fixture trägt Zahlen, nicht Encoderbytes.** Sie hält Material
  (Frequenz, Dauer, Samplerate), den nominalen Bandpegel L, den Gain G, die
  Auflösung 0,01 dB und die zwei fertigen `evidence_snapshot`-Texte (PRE und
  POST), die der **Python-Erzeuger** aus L, G und der Vertragsformel
  `round(db * 100)` baut. Keine der drei Sprachen erzeugt sie; alle drei
  messen gegen sie — dieselbe Bauform und derselbe Kopfkommentar wie
  `evidenz-zahlen-wire-v1.json` aus NAK-181
  (`tools/eq-copilot/erzeuge_v3_fixtures.py:2856`, gelesen von A5
  `pruefe_v3_vertrag.py:439-449`, `SchemaTestMain.cpp:434-449` und
  `contract_cross_language.rs:663-676`).
- **Die C++-Hälfte misst gegen die Zahlen der Fixture, nicht gegen ihre
  Bytes.** Aus dem **eigenen** Wire-Text der echten `FeatureEngine`, dekodiert
  mit der Vertragsformel (Wert geteilt durch 100): je Band **mit Bit** ist
  POST minus PRE gleich G innerhalb **±0,1 dB**. Dazu die Auflösung als eigene
  Zeile: zwei Pegel, die sich um 0,01 dB unterscheiden, ergeben im Wire-Text
  zwei um **genau 1** verschiedene Ganzzahlen.
- **Die Rust-Hälfte speist die zwei Fixture-Texte ein** und misst
  `achsen.intervall` wie in Nr. 2.
- **Die Kopplung** ist damit: beide Hälften sind an **dieselben deklarierten
  Zahlen** (L, G, 0,01 dB) gebunden, und die Fixture ist die dritte Instanz
  zwischen ihnen. Keine Seite prüft die Bytes der anderen.

**Materialwahl, damit die Zeile nicht an einer Kante hängt.** L wird so
gewählt, dass der Bandpegel deutlich innerhalb des Plausibilitätsfensters
liegt (`kPlausibelMinDb = -144,0`, `kPlausibelMaxDb = 24,0`,
`BandGrid.h:233-238`) — ein Wert außerhalb kommt gar nicht erst in den Ring
(`FeatureEngine.h:2648-2649`) und das Band trüge kein Bit. G = **3,00 dB**:
groß genug, dass der Skalen-Rotbeweis (Teiler 10 statt 100) mit 0,30 dB weit
außerhalb ±0,1 dB liegt, und klein genug, dass POST im selben Fenster bleibt.

**Woran der Auflösungs-Rotbeweis fällt — und woran nicht.** Setzt man den
Encoder auf `q_db_0p1_i16`, fällt die **Schrittzeile** deterministisch: 0,01 dB
mal Skalierung 10 ist 0,1, `std::round` macht daraus 0, die zwei Pegel tragen
dieselbe Ganzzahl statt zwei um 1 verschiedene. Die **±0,1-dB-Zeile** fällt
dort **nicht** zwingend: zwei Rundungen zu je höchstens ±0,05 dB lassen die
Differenz gerade noch innerhalb der Toleranz. Genau deshalb misst die Zeile
die Auflösung getrennt — R4 hat diesen Fall vorweggenommen, und §4.2 führt ihn
in der Spalte „fällt nicht an".

---


**Matrixnacharbeit 1 (MP1-3, MP1-4, MP1-5) — drei Berichtigungen an der
0,01-dB-Kette.**

---

**MP1-3 — `achsen.intervall` ist bandblind, und die bandweise Zahl ist von
außen nicht lesbar. Befund in §3.7; die Zeile misst, was ohne Produktcode
messbar ist, und benennt die Lücke.**

Der Befund trifft an der Quelle: `Resultatmessung::achsen`
(`broker/src/coordinator/experiment.rs:327-341`) mittelt **jede Fensterzeile
über die Bänder**, bevor der Bootstrap zieht. Banddeltas von 2 und 4 dB
ergeben dasselbe Intervall wie zweimal 3 dB. `gescannte_baender > 0` ist eine
Zählung und trennt das nicht.

**Der Zugriff auf die bandweise Zahl fehlt** — geprüft, nicht angenommen
(§3.7): `Verdrahtung::resultatmessung` ist `pub(super)`
(`experiment_verdrahtung.rs:787`), `Coordinator::experiment_export` liefert
`Export` ohne Banddeltas (`experiment.rs:1241-1248`), und `sicht.rs:352` hält
ausdrücklich fest, dass Rohdeltas **im Store bleiben**. Die Konstruktion in
`sonde013_verdrahtung.rs:816` ist eine **von Hand gebaute** `Resultatmessung`,
nicht die aus dem Produktpfad. Eine bandweise Prüfung im Sinne von MN1-3 (a)
braucht damit einen neuen, lesenden Zugang im Broker — also Produktcode, den
dieses Ticket ohne Entscheid des Dirigenten nicht anfassen darf.

**Was statt dessen gemessen wird — die Hülle, mit benannter Restlücke.** Vier
Erwartungen aus vorhandenen, produktgebauten Feldern der `Achsenrechnung`,
alle in derselben Kette:

| # | Erwartung | Was sie ausschließt |
|---:|---|---|
| a1 | `guardrail_geschuetzt_db` liegt innerhalb ±0,1 dB von G | Es ist das **zweitgrößte** Banddelta dem Betrag nach (`experiment_verdrahtung.rs:1073-1088`: sortiert absteigend, Zielband ist das größte, gemessen wird das Maximum der übrigen). Der Gegenfall der Prüfung — Bänder mit 2 und 4 dB bei G = 3 — liefert hier **2**, und der Abstand 2 zu 3 = 1 > 0,1: die Zeile **fällt**. Genau das, was `intervall` durchließ |
| a2 | `signifikante_baender == gescannte_baender` | Jedes Band mit Bit hat sich bewegt. `bootstrap_p` (`experiment.rs:1377-1410`) gibt für eine konstante Reihe bei 0 den p-Wert 1 und bei G ungleich 0 den kleinstmöglichen — ein Band, das den Gain nicht trägt, ist nicht signifikant |
| a3 | `gescannte_baender` gleicht der Zahl der Bänder mit Präsenzbit in der Fixture | Ein stiller Ausfall ganzer Bänder |
| b | `achsen.intervall` ist `Some((u, o))` mit `u <= G <= o` und beide Grenzen innerhalb ±0,1 dB von G | Der Mittelwert über die Bänder trifft G |

**Die Restlücke, ausdrücklich:** genau **ein** Band — das mit dem größten
Delta dem Betrag nach — ist von a1 nicht erfasst und geht in b nur mit 1/n ein. Ein
einzelnes davonlaufendes Band bliebe damit unentdeckt. §2.5 führt das; die
Entscheidung, ob das reicht oder ob der Broker einen lesenden Zugang bekommt,
liegt beim Dirigenten (§3.7). **Kein stiller Rückfall auf den Mittelwert.**

Der Rotbeweis „Dekoderskala 10 statt 100" fällt ab hier an **a1** (das
zweitgrößte Banddelta wird 0,30 statt 3,00 dB) **und** an b; §4.2 nennt a1 als
die zuerst fallende Zeile.

---

**MP1-4 — die Auflösung wird als LEITER gemessen, nicht als Paar.**

Der Befund trifft: `BandGrid.h:155` rundet mit
`std::round (db * skalierung)` die **absoluten** Pegel getrennt, nicht ihre
Differenz. Bei −30,051 und −30,041 dB liefert auch Skalierung 10 die
Ganzzahlen −301 und −300 — ein Paar im Abstand 0,01 dB kann also selbst bei
`q_db_0p1_i16` einen Unterschied von genau 1 zeigen. Die Zwei-Pegel-Probe war
rundungsphasenabhängig und damit kein Beleg.

**Regel MN1-4.** N = **20** Pegel im Abstand von genau 0,01 dB; Startpegel und
Schrittzahl stehen als Zahlen in der Fixture. Erwartung mit
`q_db_0p01_i16`: die 20 Ganzzahlen sind **streng monoton**, und je zwei
benachbarte unterscheiden sich um **genau 1**. Das ist
rundungsphasenunabhängig, weil jeder Schritt genau eine Quantisierungsstufe
ist: `round(x·100)` und `round((x+0,01)·100) = round(x·100 + 1)` unterscheiden
sich für jedes x um exakt 1, gleich wo die Rundungsphase liegt.

Rotbeweis „Encoder auf `q_db_0p1_i16`": 20 Schritte zu 0,01 dB sind 0,19 dB
Spanne, also **höchstens 3** verschiedene Ganzzahlen bei Skalierung 10 — die
Leiterzeile fällt an der Auflösung selbst, nicht an einem Nebeneffekt. Die
Gain-Zeile (G innerhalb ±0,1 dB) bleibt davon getrennt; §4.2 benennt, dass sie
im selben Rotlauf ebenfalls fallen **darf**, aber nicht die gewertete Zeile
ist.

---

**MP1-5 — „A5 liest mit" entfällt. Die Kopplung sind A8 plus zwei
Verbraucher, einer je Sprache.**

Der Befund trifft: `tools/eq-copilot/pruefe_v3_vertrag.py:449` öffnet
ausschließlich `evidenz-zahlen-wire-v1.json`; die Funktion darum
(`pruefe_wire_zahlklassen`, `:436-449`) ist auf die Zahlklassentabelle
zugeschnitten. Eine neue Datei wird davon nicht gelesen, und A5 liegt
außerhalb der Ticketpfade.

**Regel MN1-5.** Die Kopplung lautet ab hier:

- **A8** `erzeuge_v3_fixtures.py --pruefen` hält die Datei bytegleich. Nötig
  und hinreichend dafür ist **ein Eintrag in der Dateiliste `alle`**
  (`tools/eq-copilot/erzeuge_v3_fixtures.py:2929-2937`, direkt neben
  `evidenz-zahlen-wire-v1.json` bei `:2935`, mit demselben Grund im
  Kommentar): jede Datei dieser Liste wird auf Existenz und Bytegleichheit
  geprüft (`:2962-2968`).
- Die **Waisenwache** (`:2942-2957`) wird **nicht** angefasst: sie sammelt nur
  `gueltig/`, `ungueltig/` und das eigene `MANIFEST.json`; eine Datei auf der
  obersten Ebene von `fixtures/v3/` taucht dort gar nicht auf und kann deshalb
  weder fälschlich als verwaist gemeldet werden noch unbemerkt liegen bleiben.
- **Zwei Verbraucher, einer je Sprache:** **B16** (C++,
  `Sonde013EventWireTest.cpp`) und **A4** (Rust, `sonde013_verdrahtung.rs`).
  Damit ist M-70 erfüllt — „Ein Fixture ohne Verbraucher in beiden Sprachen
  ist ein Befund" (`docs/beweise/SONDE-013.md:575`).
- **`pruefe_v3_vertrag.py` wird nicht erweitert** und bleibt außerhalb der
  Ticketpfade. Form B braucht keinen Vertragsriegel auf dieser Datei: sie
  trägt Zahlen und zwei `evidence_snapshot`-Texte, deren Vertragsform bereits
  vom bestehenden Fixturekorpus in `gueltig/` gemessen wird.

An der Quelle geprüft: A5 führt **keine** Wache „Fixture ohne Verbraucher" und
**keine** Registrierungsliste, die eine neue Datei rot machen würde. Es ist
also nichts an A5 zu ändern und nichts zu übersehen.

N-23, E4b, §4.1 B6, §4.3 und die Ticketpfade sagen ab hier dasselbe.

---

**Matrixnacharbeit 2 (MP2-2 = MP1-3, offen geblieben) — Entscheid des
Dirigenten: der Broker bekommt eine lesende Testauskunft, und die Rust-Hälfte
misst bandweise am Produkt.**

**Erst die Berichtigung meiner eigenen Zahl.** Die Hülle aus a1 bis a3 und b
lässt **nicht** „genau ein Band" durch, sondern beliebig viele. Der
Gegenfall der Prüfung, an der Quelle nachgerechnet: Banddeltas 4 dB in einem
Band, 2 dB in einem zweiten, 3 dB in allen übrigen, G = 3.

| Erwartung | Wert | Urteil |
|---|---|---|
| a1 `guardrail_geschuetzt_db` | Zielband ist das mit 4 dB, größtes Delta der übrigen ist **3** (`experiment_verdrahtung.rs:1073-1087`) | besteht |
| a2 `signifikante_baender == gescannte_baender` | jedes Band trägt ein Delta ungleich 0, `bootstrap_p` gibt den kleinstmöglichen p-Wert (`experiment.rs:1377-1410`) | besteht |
| a3 Bandzahl | unverändert | besteht |
| b `intervall` | Fenstermittel `(4 + 2 + 3·(n−2))/n = 3` → `[3, 3]` (`experiment.rs:327-366`) | besteht |

**Alle vier bestehen, und trotzdem verletzen zwei Bänder die ±0,1-dB-Zusage.**
Die Hülle bindet den **Mittelwert** und den **zweitgrößten Betrag**; jede
Abweichungsmenge, die den Mittelwert bei G lässt, den zweitgrößten Betrag bei
G lässt und kein Band auf 0 zieht, kommt durch — das sind nicht ein Band,
sondern beliebig viele paarweise auslöschende. Meine Formulierung „Restlücke
von genau einem Band" in §2.5 war **falsch** und hat die Einschränkung
unterschätzt; sie ist gestrichen (§2.5, Block Matrixnacharbeit 2).

**Regel MN2-2 — die kleinste lesende Auskunft, sonst nichts.** Damit N-22 (a)
bandweise **am Produktpfad** messen kann, bekommt der Broker eine lesende
Testauskunft. An der Quelle geprüft, welche Form die kleinste ist:

| Weg | Trägt er? |
|---|---|
| `resultatmessung` von `pub(super)` auf `pub` heben (`experiment_verdrahtung.rs:787`) | **nein, allein nicht** — der zweite Parameter ist `&SessionKey`, und `SessionKey` ist `pub(super)` (`broker/src/coordinator/zustand.rs:36`). Ein `pub fn` mit einem nicht nennbaren Parametertyp ist von `broker/tests/` aus nicht aufrufbar |
| eine **delegierende** `pub fn` an derselben Verdrahtung | **ja** — sie nimmt nur öffentlich nennbare Typen und leitet weiter |

Die Form ist damit festgelegt:

    pub fn resultatmessung_fuer_test(&self, experiment_id: &str,
                                     adresse: &Adresse) -> Resultatmessung

Sie leitet auf `self.resultatmessung(experiment_id, &ClientKey::aus_adresse(adresse).session())`
weiter — **genau die Ableitung, die der Produktpfad selbst benutzt**
(`experiment_verdrahtung.rs:213-216`). Kein neues Feld, kein Schemafeld, keine
Änderung am Terminal, keine Verhaltensänderung: die Funktion ruft auf, was
`experiment_manual_result` ohnehin ruft, und gibt zurück, was dort schon
gerechnet wird.

Warum das reicht, an der Quelle: `experiment_verdrahtung` ist ein privates
Modul, aber die Funktion sitzt auf `impl Coordinator`, und `Coordinator` ist
`pub` (`broker/src/coordinator/mod.rs:166`). Dieselbe Bauform tragen dort
bereits `experiment_sicht` (`:1715`) und `experiment_export` (`:1744`) als
`pub fn`. `Resultatmessung` ist `pub` in `pub mod experiment`
(`experiment.rs:233`, `mod.rs:40`) und damit im Test nennbar; `Adresse`
importiert der Test bereits (`sonde013_verdrahtung.rs:16`). **`mod.rs` braucht
keine Änderung.**

Der Vorbildfall steht im selben Repo: `JSON_SCHEMA_MINOR_AKTIV_FUER_TEST`
(`broker/src/coordinator/mod.rs`, Kommentar „nur fuer Beine sichtbar") ist
genau so eine lesende Auskunft, und NAK-181 hat auf der C++-Seite dasselbe
Muster benutzt (`fingerprintFenster()`, `stereoAkkuBelegteBaender()`).

**N-22 nach MN2-2.** Die gewertete Zeile ist ab hier (a):

- **(a) bandweise, aus der Produktmessung.** Für **jedes** Band mit
  Präsenzbit liegt `band_delta_db[b]` innerhalb ±0,1 dB von G. Gelesen wird
  über `resultatmessung_fuer_test` **nach** `experiment_candidate` und
  `experiment_manual_result` — also die Messung aus dem echten Weg
  Store → `resultatmessung` → Terminal, **nicht** eine von Hand gebaute
  (`sonde013_verdrahtung.rs:815-824` ist genau die von Hand gebaute und
  bleibt, was sie ist: ein Unit-Fall für `achsen()`).
- **(b) `intervall`** ist `Some((u, o))` mit `u <= G <= o`, beide Grenzen
  innerhalb ±0,1 dB von G.
- **a1 bis a3 bleiben als Zusatz stehen** — sie kosten nichts und halten die
  Guardrail-Achse wach —, sind aber **nicht** die gewertete Zeile.

Der Rotbeweis „Dekoderskala 10 statt 100" fällt ab hier an **(a)**: jedes Band
trägt 0,30 statt 3,00 dB, Abweichung 2,70 dB. §4.2 nennt (a) als gewertete
Zeile.

**Ticketgrenze.** `broker/src/coordinator/experiment_verdrahtung.rs` kommt als
Ticketpfad in den Kopf, ausdrücklich mit dem Zusatz **„nur lesende
Testauskunft"**. Nichts anderes unter `broker/src/` wird angefasst.

---
**E5 — R5: Die Zeitlage ist blockgrößenunabhängig *sampleexakt*. Die Zeile misst schärfer als „innerhalb eines Feature-Hops" — und der Peakpfad ist es nicht, deshalb löst der Sweep über den Fluss aus.**

Aus der Quelle abgeleitet, nicht angenommen (§3.5):

- Das Ereignis trägt seine Zeit als `Ereignis.stromSample` — der
  **Fensteranfang** im lokalen monotonen Strom (`FeatureEngine.h:296`, gesetzt
  bei `:2818` aus `s.fensterStromStart`). Dazu `projektSample`, nur wenn
  `projektzeitGesetzt`.
- `fensterStromStart` wird gesetzt, **wenn der Ring leer ist**, aus
  `block.stromVon + sampleOffset` (`:2482`), und nach jedem vollen Fenster um
  genau `s.hop` weitergeschoben (`:2553`). Die Fenstergrenzen liegen damit auf
  einem **Sampleraster**, das am Stromanfang verankert ist und die Blockgröße
  nicht kennt.
- Das Raster ist in **Samples** definiert, nicht in Sekunden:
  `kHauptPunkte = 4096`, `kHauptHop = 2048` (`:929`, `:932`). Über Sampleraten
  bleibt es dasselbe Raster; nur `dauerMs` (`:2830`) rechnet es in
  Millisekunden um.

**Daraus die Zusage der Zeile:** ein Impuls bei bekanntem Stromsample t0
erzeugt ein Ereignis, dessen `stromSample` bei **jeder** Blockgröße derselben
Samplerate **denselben Wert** trägt — nicht „innerhalb eines Hops", sondern
gleich. Und es liegt innerhalb eines Hauptfensterhops vor t0, weil t0 in dem
Fenster liegt, das bei `stromSample` beginnt. Beides wird gemessen:
Gleichheit über die Blockgrößen als exakte Zahl, Nähe zu t0 als
`0 <= t0 - stromSample < 2048`.

Über **Sampleraten** gilt die Gleichheit ebenso in Samples; in Sekunden
verschiebt sich derselbe Wert. Der Sweep fährt die fünf Raten, die B17 fährt
(44,1 / 48 / 88,2 / 96 / 192 kHz, `Sonde013TruePeakGoldenTest.cpp:178`) und
misst je Rate die Gleichheit über die Blockgrößenmenge aus E3 — die
Kombination ist damit 5 × 5 = 25 Läufe.

**Die Phasenfalle, und warum der Sweep über den Fluss auslöst.** Der
**Rahmen** ist nicht sampleexakt: `nimmBlock` prüft die Kadenz **am Blockende**
(`FeatureEngine.h:1221-1223`, `kLiveIntervallS = 0.1` bei `:979`), also fällt
ein Rahmen beim ersten Block **nach** 0,1 s — und wo das liegt, hängt an der
Blockgröße. Der Peakpfad vergleicht gegen `vorigerRahmenPeak` und feuert
höchstens einmal je Rahmen (`:2803-2808`, `:3669`); seine Auslösung ist damit
rahmenphasenabhängig. Genau diese Falle beschreibt
`AnalysisGoldenTestMain.cpp:2152-2162` für
`short_impulse_triggers_peak_path_only` bei einer einzigen Blockgröße.

Der Sweep misst deshalb die **Zeitlage**, nicht die Auslösung: das
Impulsmaterial ist so gewählt, dass der **Flusspfad** sicher auslöst (breiter
Ausschlag auf ruhigem Boden, wie `flux_event_carries_flux_quality` in demselben
Bein), denn der Flusspfad hängt nur am FFT-Fenster und damit am sampleexakten
Raster. Der bestehende Peak-Fall bleibt unverändert daneben stehen; der Sweep
ist sein **Nachbar**, nicht sein Ersatz.

---


**Matrixnacharbeit 1 (MP1-1, MP1-2) — die Nähe-Zusage kommt aus der
Fensterlänge, nicht aus dem Hop; und der Rotbeweis verschiebt um ein ganzes
Fenster.**

---

**MP1-1 — `0 <= t0 - stromSample < 2048` war falsch. Richtig ist 4096.**

Der Befund trifft und ist ein Rechenfehler der Erstfassung:
`FeatureEngine.h:929-932` definiert **4096** Samples Fensterlänge bei **2048**
Samples Hop; `:2818` meldet den **Fensteranfang**. Ein Impuls in der zweiten
Fensterhälfte — etwa bei Offset 3072 — hat einen korrekt gemeldeten Stempel,
verletzt aber `t0 - stromSample < 2048`. Die Zeile hätte richtiges
Produktverhalten abgelehnt. Die Blockgrößenunabhängigkeit des Rasters (E5)
bleibt davon unberührt; sie beweist die zusätzliche Nähe nicht.

**Die Ankerarithmetik, aus der die Zahlen kommen.** Der Anker ist
`block.stromVon + sampleOffset` des ersten Samples in den leeren Ring
(`:2482`); danach schreibt jedes volle Fenster ihn um `kHauptHop` fort
(`:2553`). Aus einem frisch vorbereiteten Lauf ohne Grenze ist der Anker
deshalb **0**, und die Fenster sind

    W_k = [k * 2048, k * 2048 + 4096)   für k = 0, 1, 2, …

Ereignisse entstehen **nur** auf diesem Raster: `flussSchritt` läuft
ausschließlich für die Hauptstufe (`:2682-2690`, `if (&s == &haupt)`), die
Bassstufe mit ihrem 8192er-Hop trägt keine Ereignisse bei. Jedes t0 liegt in
**genau zwei** aufeinanderfolgenden Fenstern (Überlappung 50 %, `:930`).

**Die Fensterphase des Impulses, und warum sie gewählt wird.** Mit
d = t0 (Anker 0), k2 = floor(d / 2048) und o2 = d − k2·2048 liegt t0 in
W_{k2−1} bei Offset o2 + 2048 und in W_{k2} bei Offset o2. Die Fenster sind
Hann-gewichtet (`:930`, `:1608`), also w(o) = sin²(π·o/4096):

    w(o2)        = sin²(π·o2/4096)
    w(o2 + 2048) = cos²(π·o2/4096)

Für o2 > 1024 wiegt **W_{k2}** den Impuls stärker. Gewählt wird
**o2 = 1536**, also

    t0 = k * 2048 + 1536

Dort ist w = sin²(0,375·π) ≈ **0,854** in W_{k2} gegen ≈ **0,146** in
W_{k2−1} — das spätere Fenster trägt rund das 34-fache der Impulsenergie, und
t0 liegt in seiner **ersten Hälfte** (1536 < 2048). Diese Phase ist für jede
Blockgröße dieselbe, weil der Anker am **Stromanfang** liegt und nicht an
einer Blockgrenze.

**Was die Zeile trotzdem NICHT behauptet.** Die Flussschwelle ist adaptiv
(Median/MAD, `:2711` ff.); ob **nur** W_{k2} auslöst oder auch W_{k2−1}, ist
ohne Lauf nicht belegbar. Die Zeile sagt deshalb ehrlich **„erstes oder
zweites enthaltendes Fenster"** und misst:

- **(a) Gleichheit, exakt.** Der Test nimmt je Lauf das Ereignis mit dem
  **größten `stromSample` <= t0** — eine deterministische Auswahl, gleich
  welches der beiden Fenster ausgelöst hat — und verlangt für diesen Wert
  **exakte Gleichheit** über die fünf Blockgrößen, je Samplerate.
- **(b) Nähe, mit 4096.** `0 <= t0 - stromSample < kHauptPunkte` (4096). Keine
  schärfere Zahl ohne Beleg; die gewählte Phase ergäbe 1536 oder 3584, aber
  gemessen wird die Schranke, die aus der Fensterlänge folgt.

---

**MP1-2 — der Rotbeweis verschiebt um 4096, nicht um einen Block.**

Der Befund trifft: bei Blockgröße 1 beziehungsweise 333 verschöbe „um einen
Block" den Impuls nur um 1 beziehungsweise 333 Samples. Beide bleiben kleiner
als der Hop (2048); der Impuls kann im selben auslösenden Fenster bleiben,
`:2552-2553` und `:2818` liefern denselben Anker, und die Gleichheitsprüfung
bliebe **grün trotz Manipulation** — ein Rotbeweis, der nichts beweist.

**Regel MN1-2.** Der Rotbeweis verschiebt den Impuls bei Blockgröße ungleich
512 um **`kHauptPunkte` = 4096 Samples** — eine ganze Fensterlänge und damit
zwei volle Hops. Der Stempel fällt dann bei jeder Blockgröße in ein anderes
Fenster (er wandert um genau 4096), und Zeile **(a) fällt sicher**. Zeile (b)
fällt im selben Lauf ebenfalls, weil t0 der **nominale** Wert der Erwartung
bleibt und `t0 - stromSample` negativ wird; §4.2 nennt **(a)** als die zuerst
fallende Zeile.

Eine kleinere Verschiebung wäre nur zulässig, wenn die Fensterphase aus MP1-1
beweist, dass sie eine Fenstergrenze kreuzt — bei o2 = 1536 wäre das erst ab
512 Samples der Fall, und die Matrix nimmt die belastbare Zahl statt der
knappen.

---

**Matrixnacharbeit 2 (MP2-1 = MP1-2, offen geblieben) — die Ereigniszuordnung
kennt t0 nicht mehr.**

Der Befund trifft, und er trifft meine Auswahlregel, nicht die Verschiebung.
Mit der festgelegten Phase `t0 = k · 2048 + 1536` liegen die zwei
enthaltenden Fenster bei `t0 - 1536` und `t0 - 3584`. Nach der Verschiebung um
4096 wandern sie auf `t0 + 2560` und `t0 + 512` — **beide größer als t0**
(`FeatureEngine.h:929-932`, `:2482`, `:2553`, `:2818`). Die Auswahl „größtes
`stromSample` <= t0" verwirft dann **beide** und greift auf ein älteres
Ereignis oder gar keines zurück. Die Gleichheitszeile misst damit nicht mehr
den verschobenen Impuls, und die Nähezeile kann nach diesem Filter
**strukturell nie** wegen einer negativen Differenz fallen. Ein Filter, der die
Sollzeit kennt, kann eine Zeitabweichung nicht messen — er blendet sie aus.

**Regel MN2-1: die Auswahl ist zeitfrei.** Gemessen wird **das erste Ereignis
des Laufs** — `ereignis(0)`, also der älteste Eintrag im Ring, ohne jeden
Bezug auf t0.

Warum `ereignis(0)` wirklich das erste ist, an der Quelle:
`ereignis(i)` indiziert vom **ältesten** aus
(`erstes = (ereignisStand - ereignisAnzahl + 64) % 64`,
`FeatureEngine.h:1381-1385`), und `ereignisAblegen` überschreibt erst bei
vollem Ring (`:2861-2867`, `kEreignisPlaetze = 64` bei `:992`). Solange
`ereignisAnzahlJetzt() < 64` gilt, ist die Ringreihenfolge die Zeitreihenfolge,
und `ereignis(0)` ist das erste Ereignis des Laufs.

**Warum nicht „genau ein Ereignis".** Die Variante, die Zahl der Ereignisse
auf 1 festzunageln, ist aus der Quelle **nicht** belegbar: der Impuls liegt in
**zwei** überlappenden Fenstern (50 %, `:930`), und ob beide die adaptive
Schwelle `med + 3·MAD` überschreiten (`:2761-2767`), hängt an der
Flusshistorie und ist ohne Lauf nicht entscheidbar. Der nächstgelegene
bestehende Fall, `both_paths_yield_one_event`
(`AnalysisGoldenTestMain.cpp:2216-2255`), belegt das auch nicht — er misst
„kein Zeitpunkt trägt ZWEI Ereignisse", nicht „ein Ausschlag erzeugt EIN
Ereignis", und er fährt mehrere Ausschläge. Die Matrix nimmt deshalb die
belegbare Form.

**Die sechs Erwartungen von N-26, keine davon mit t0 im Filter:**

| Nr. | Erwartung | Was sie sichert |
|---|---|---|
| E-a | `ereignisAnzahlJetzt() >= 1` | Der Impuls hat wirklich ausgelöst — die Auslösung ist gemessen, nicht angenommen |
| E-b | `ereignisAnzahlJetzt() < 64` | Der Ring hat nichts verworfen; Ringreihenfolge ist Zeitreihenfolge, `ereignis(0)` ist das erste Ereignis |
| E-c | `ereignis(0).qualitaetFluss` ist gesetzt | Gemessen wird das Flussereignis des Impulses, nicht ein Peakereignis aus der rahmenphasenabhängigen Spur (E5) |
| E-d | kein Ereignis mit `stromSample < t0 - 4096`, also `ereignis(0).stromSample >= t0 - 4096` | Der Boden erzeugt vor dem Impuls nichts — das ist die **Erwartung**, mit der die Materialwahl belegt wird, kein Auswahlfilter |
| **(a)** | `ereignis(0).stromSample` ist über die fünf Blockgrößen je Samplerate **identisch** (exakte Zahl) | die gewertete Zeile |
| **(b)** | `0 <= t0 - ereignis(0).stromSample < 4096` | Nähe, aus der Fensterlänge (MN1-1) |

**Material und Lage, aus der Quelle abgeleitet.** Ein Impuls auf einem ruhigen,
aber nicht stillen Boden — Vorbild `flux_event_carries_flux_quality`
(`AnalysisGoldenTestMain.cpp:2187-2212`: breitbandiger Ausschlag auf ruhigem
Sinusboden). Der Boden muss Streuung tragen, denn `flussAus` verlangt
ausdrücklich `mad > 0.0` (`FeatureEngine.h:2810`); ein exakt stiller Boden
ergäbe MAD 0 und **gar kein** Ereignis. Und der Impuls muss nach der
Aufwärmphase liegen: die Schwelle greift erst bei voller Flusshistorie,
`kFlussHistorie = 16` (`:994`, `:2766`), das erste Fenster schließt bei 4096
und jedes weitere nach 2048 Samples — die Schwelle steht also ab Sample
`4096 + 15 · 2048 = 34816`. Gewählt wird **k = 100**, also

    t0 = 100 * 2048 + 1536 = 206336

weit hinter der Aufwärmphase und, nach der Verschiebung um 4096, mit
`t0 + 4096 = 210432` ebenso.

**Der Rotbeweis fällt jetzt am Impuls.** Verschiebung um 4096 bei Blockgröße
ungleich 512: `ereignis(0).stromSample` liegt bei den vier verschobenen
Läufen um 4096 höher als bei 512 — **(a) fällt**, und **(b) fällt** in den
verschobenen Läufen mit negativer Differenz (`t0 - stromSample` wird −512 oder
−2560). §4.2 nennt (a) als die gewertete Zeile.

---
**E6 — R6: Der B24-Fall bekommt die Wahrheit, die der Test baut; die Distraktor-Lücke wird gedruckt; und „kein sicherer Auslöser" wird an den geschlossenen Mengen des Vertrags gemessen — in A26, nicht in A5.**

`Sonde013PrePostGoldenTest.cpp:127-272` führt **dasselbe Quellsignal** durch
identische Kette, +15 dB Gain, linearen Tiefpass, Kompressor, Modulation,
Saturation und wechselnde Latenz. Das sind **kausale, zeitvariable** Ketten;
gemessen wird, dass die Kohärenz fällt und deshalb keine statische
EQ-Behauptung entsteht. Die Wahrheit des Falls ist `zeitvariabel`, nicht
`nicht_kausal`. Die Aussage bleibt `unsicher`: Nakama trifft über die Kette
keine statische EQ-Aussage — das ist die Enthaltung, die §49.4 lobt.

Der Wortlaut „Distraktor" verschwindet aus Fällen, Hinweisen und MANIFEST.
Statt dessen führt das MANIFEST eine eigene Zeile, und **A26 druckt sie**:

    in P4 nicht gemessen: M-85 Ziel 1 (korrelierter, nicht kausaler
    Distraktor) -> NAK-190

Sie steht im MANIFEST als Feld `nicht_gemessen` (Liste von Objekten mit
`zusage`, `grund`, `ticket`), damit der Erzeuger sie bytegleich mitführt und
niemand sie als Prosa umformuliert. A26 druckt jede Zeile daraus im Kopf des
Berichts — die Lücke bleibt im Kanon sichtbar, statt still zu fehlen.

**Die zweite Hälfte des Entscheids §8: kein P4-Datenweg benennt einen
„sicheren Auslöser".** Gemessen an den geschlossenen Mengen des Vertrags
(`eq-copilot/schemas/v3/eq-ipc-v3.schema.json`, §3.6), alle fünf gelesen und
alle fünf ohne kausalen Bezeichner:

| Menge | Ort im Schema | Werte |
|---|---|---|
| `urteil` (M-46) | `$defs/session_experiment/properties/urteil` | `ziel_verbessert_guardrails_stabil`, `messbar_anders_urteil_offen`, `keine_belastbare_aenderung`, `ziel_verbessert_geschuetztes_schlechter`, `vergleich_nicht_gueltig` |
| `klasse` | `$defs/alignment_klasse` | `feature_aligned`, `audio_aligned`, `probable`, `unclear` |
| `kettenbefund` | `$defs/session_paar/properties/kettenbefund` | `stationaer`, `zeitvariabel_markiert`, `pegelabhaengig_markiert`, `latenz_wechselt_markiert`, `nicht_beurteilbar` |
| `ausschluss` | `$defs/session_paar/properties/ausschluss` | zehn Gründe, `haelfte_fehlt` bis `sprung_im_fenster` |
| `konfidenz.klasse` | `$defs/konfidenz/properties/klasse` | `stark`, `mittel`, `schwach`, `unbrauchbar` |

**Form: A26, nicht A5.** `pruefe_v3_vertrag.py` (A5) liegt nicht in den
Ticketpfaden, `pruefe_p4_korpus.py` schon; und die Zusage gehört sachlich zum
Korpus („der Korpus behauptet nichts, was nicht gemessen wird"), nicht zum
Vertragsriegel. A26 liest die fünf Mengen aus dem Schema und weist jeden Wert
ab, der ein Wort aus einer **benannten, geschlossenen** Liste kausaler
Bezeichner trägt: `ursache`, `ursaechlich`, `ausloeser`, `ausgeloest`,
`verursacht`, `kausal`, `cause`, `caused`, `trigger`, `grund_fuer`. Fehlt eine
der fünf Mengen im Schema, ist das rot — sonst könnte eine umbenannte
Definition den Riegel still leeren.

### 2.1 Begriffe, Mengen und Zahlen — einmal, hier

| Name | Wert | Quelle |
|---|---|---|
| ~~`UNSICHERE_WAHRHEITEN`~~ | ~~`unbekannt`, `unvergleichbar`, `nicht_kausal`, `nicht_exakt`~~ | **Etappe 1b: aufgehoben**, durch die zwei Mengen unten ersetzt (E1b) |
| `KEINE_BEHAUPTUNG` | `unbekannt`, `unvergleichbar`, `zeitvariabel` | R1a (E1b); jede Behauptung darauf ist falsch |
| `KEINE_STARKE` | `KEINE_BEHAUPTUNG` plus `nicht_kausal`, `nicht_exakt` | R1a (E1b); nur die **starke** Behauptung ist falsch. Gelesen von `_passt` (starker Zweig) **und** `moegliche_starke` — eine Quelle, aus dem MANIFEST |
| `ERLAUBTE_WAHRHEITEN` | `KEINE_STARKE` plus die sicheren Wahrheiten `ueberschreitet`, `innerhalb`, `getrennt`, `segment`, `mono`, `identisch`, `invertiert`, `laufzeit`, `unkorreliert`, `zeitstabil`, `wiedergefunden` | E1b; Erzeuger führt sie geschlossen wie `ERLAUBTE_AUSSAGEN`, MANIFEST trägt sie mit. **Etappe 1b:** `verteilt` und `insertprobe` sind entfallen (Umbenennung abgelehnt), `zeitvariabel` ist in `KEINE_BEHAUPTUNG` gewandert |
| `ERLAUBTE_AUSSAGEN` | `stark`, `schwach`, `unsicher` | unverändert (`erzeuge_p4_korpus.py:148`) |
| Blockgrößenmenge des Sweeps | `1, 333, 512, 2048, 16384` | E3; Obergrenze `maxBlockFrames = 16384` (`StampedAudioQueue.h:240`) |
| Sampleraten des Sweeps | 44100, 48000, 88200, 96000, 192000 Hz | `Sonde013TruePeakGoldenTest.cpp:178` |
| Hauptfenster / Hop | 4096 / 2048 Samples | `FeatureEngine.h:929`, `:932` |
| Folddown-Toleranz | 0,25 dB | Entwurf §40.3; `Sonde013StereoGoldenTest.cpp:255` |
| Gain-Toleranz (M-83 Satz 2) | ±0,1 dB | Entwurf §38.5; M-83 |
| Evidenzauflösung | 0,01 dB je Schritt (`q_db_0p01_i16`, Teiler 100) | `BandGrid.h:140`, `evidenz.rs:335` |
| Loudness-Toleranz | ±0,1 LU | `LoudnessGoldenTestMain.cpp:164` |
| True-Peak-Toleranz | ±0,1 dB | Entwurf §49.3; `Sonde013TruePeakGoldenTest.cpp:127` |
| Laufzeitbudget B19 | Ziel unter 60 s | R3 |
| Gates von A26 | `falsche_starke` 0, `falsche_schwache` 0 (neu), Brier < 0,25, Coverage >= 0,5, Kalibrierung <= 0,15, `precision <= 1,0` (neu), `recall <= 1,0` (neu) | `pruefe_p4_korpus.py:45-49`; die drei neuen aus R1a (E1b) |
| Nähe-Schranke der Impulszeit | `0 <= t0 - stromSample < 4096` (= `kHauptPunkte`) | **Matrixnacharbeit 1 (MP1-1)**; der Stempel ist der Fensteranfang (`FeatureEngine.h:2818`), die Fensterlänge ist 4096 (`:929`). Die frühere Schranke 2048 (Hop) hätte richtiges Verhalten abgelehnt |
| Fensterphase des Impulses | `t0 = k · 2048 + 1536` (Anker 0) | **Matrixnacharbeit 1 (MP1-1)**; Hann-Gewicht 0,854 im späteren gegen 0,146 im früheren der zwei enthaltenden Fenster (`FeatureEngine.h:930`, `:1608`) |
| Verschiebung des M-84-Rotbeweises | 4096 Samples (eine ganze Fensterlänge) | **Matrixnacharbeit 1 (MP1-2)**; „ein Block" wäre bei Blockgröße 1 oder 333 kleiner als der Hop und bliebe im selben Fenster |
| Auflösungsleiter (M-83) | N = 20 Pegel im Abstand 0,01 dB; benachbarte Ganzzahlen unterscheiden sich um genau 1 | **Matrixnacharbeit 1 (MP1-4)**; `BandGrid.h:155` rundet die absoluten Pegel getrennt, ein einzelnes Paar wäre rundungsphasenabhängig |
| Gain der 0,01-dB-Kette | G = 3,00 dB | E4b; groß genug, dass der Skalen-Rotbeweis mit 0,30 dB weit außerhalb ±0,1 dB liegt |

### 2.2 Die Matrix — je Zusage: Wortlaut, messender Fall, Negativfall, Erwartung

Spalte „Fall" nennt Bein, wörtlichen Bezeichner und Datei. Ein Eintrag mit
**neu** entsteht in Etappe 2; alle übrigen stehen am Basis-SHA.

**M-79 — vier Klassen, alle bestehen; Kennzahlen samt „falsche starke Behauptungen = 0"**

| Nr. | Zusage (wörtlich, gekürzt) | Messender Fall | Negativfall / Rotbeweis | Erwartung mit Zahl |
|---|---|---|---|---|
| N-01 | „Alle vier bestehen, sonst ist das Gate rot." | A26 `pruefe_p4_korpus.py`, Pflichtklassen `:191-194` | eine Klassendatei fehlt | Exit 2, Text „Klasse fehlt: …"; vier Klassen `referenz`, `zeitachse`, `vergleich`, `stereo` |
| N-02 | „die Zahl falscher starker Behauptungen" (muss null sein) | A26, `_kennzahlen` gegen `UNSICHERE_WAHRHEITEN` | **neu** `roh/NAK-182-rot-M-79-unvergleichbar.txt`: `gate6_incomparable_never_gets_a_strong_winner` auf `aussage: stark` | heute: `falsche_starke = 0`, Exit 0, `recall = 1,0667` (gemessen §3.2). Nach dem Bau: `falsche_starke = 1`, Exit 2 |
| N-03 | dieselbe Zusage für `nicht_kausal` und `nicht_exakt` | A26 | **neu** je ein Rotbeweis an `nonlinear_chain_never_yields_static_eq_claim` (für den Rotbeweis auf `nicht_kausal` gesetzt) und `insert_probe_is_never_called_master_sum` | je `falsche_starke = 1`, Exit 2 |
| N-04 | „eine starke ODER schwache Aussage auf einer dieser Wahrheiten ist eine falsche Behauptung" (R1) | A26, neuer Riegel `falsche_schwache = 0` | **neu** ein `schwach`-Fall auf `unvergleichbar` | Exit 2. Am unkorrigierten Korpus wären es **2** Fälle mit Klassen-Brier 0,3912 (gemessen §3.2) — deshalb E1 |
| N-05 | Recall ist eine Quote und nie größer als 1 | A26, neuer Riegel `recall <= 1.0` | **Selbsttest** S-03 (§2.4), da es nach E1 keinen Korpusweg mehr dorthin gibt | `recall > 1,0` → Exit 2 |
| N-06 | „Jedes genannte Bein steht wirklich im Kanon-Runner." | A26 `_bein_bekannt:62-74` gegen `tools/beweise.ps1` | erfundenes Kürzel in `KLASSENBEINE` | Exit 2, „Bein … steht nicht im Kanon-Runner" |
| N-07 | Kalibrierung, Brier, Coverage, Enthaltungsrate werden gemeldet | A26, Gesamtzeile `:199-204` | — (Berichtszeile, kein Riegel) | nach der Korrektur: alle vier Werte innerhalb der Gates aus §2.1; Zahlen in §6 |


**Etappe 1b (Entscheid des Dirigenten) — N-02 bis N-05 in der Fassung R1a.**
Die vier Zeilen darüber bleiben in ihrer Etappe-1-Form stehen; **bindend sind
die folgenden.** Geändert ist nur die gelesene Menge: `_passt` prüft im
starken Zweig gegen `KEINE_STARKE`, im schwachen gegen `KEINE_BEHAUPTUNG`
(E1b). Alle Zahlen sind unter R1a am committeten Korpus gemessen.

| Nr. | Zusage (wörtlich, gekürzt) | Messender Fall | Negativfall / Rotbeweis | Erwartung mit Zahl |
|---|---|---|---|---|
| N-02 | „die Zahl falscher starker Behauptungen" (muss null sein) | A26, `_kennzahlen`, starker Zweig gegen `KEINE_STARKE` | **neu** `roh/NAK-182-rot-M-79-unvergleichbar.txt`: `gate6_incomparable_never_gets_a_strong_winner` auf `aussage: stark` | heute (Einmengenform): `falsche_starke = 0`, Exit 0, `recall = 1,0667` (gemessen §3.2). Unter R1a: `falsche_starke = 1`, Exit 2 |
| N-03 | Gate 7 und der verteilte Pfad: eine **starke** Aussage auf `nicht_exakt` bzw. `nicht_kausal` ist falsch | A26, starker Zweig | **neu** `roh/NAK-182-rot-M-85-gate7.txt`: `insert_probe_is_never_called_master_sum` auf `aussage: stark`; dazu `distributed_pair_never_produces_transfer_function` auf `aussage: stark` | je `falsche_starke = 1`, Exit 2 (beide unter R1a gemessen). **Gegenprobe:** dieselben zwei Fälle als `schwach` bleiben grün (`falsche_schwache = 0`) — genau das trennt `KEINE_STARKE` von `KEINE_BEHAUPTUNG` |
| N-04 | „keine falsche statische EQ-Behauptung" (M-83 Satz 3), Gate 6, Exit-Gate Satz 2: auf `KEINE_BEHAUPTUNG` ist **jede** Aussage falsch | A26, neuer Riegel `falsche_schwache = 0`, schwacher Zweig gegen `KEINE_BEHAUPTUNG` | **neu** `roh/NAK-182-rot-M-83-schwach.txt`: `nonlinear_chain_never_yields_static_eq_claim` mit `wahrheit: zeitvariabel` auf `aussage: schwach` | `falsche_schwache = 1`, Exit 2 (unter R1a gemessen). Unter R1a am unveränderten Korpus: `falsche_schwache = 0` — der Sprung auf 2 in §3.2 gehört der Einmengenform |
| N-05 | Recall ist eine Quote und nie größer als 1 | A26, neuer Riegel `recall <= 1.0` | **Selbsttest** S-03 (§2.4) | Unter R1a lesen `_passt` (starker Zweig) und `moegliche_starke` **dieselbe** Menge; `recall > 1` ist auf keinem Korpusweg erreichbar. Der Riegel ist eine Wache gegen ein künftiges Auseinanderlaufen: `recall > 1,0` → Exit 2 |

**M-80 — Korpus bytegleich reproduzierbar**

| Nr. | Zusage | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-08 | „Ein Erzeuger-Skript baut jede Fixture bytegleich neu … meldet jede verwaiste Datei." | A25 `erzeuge_p4_korpus.py --pruefen` | ein Byte im committeten Korpus geändert | Exit 2, „nicht bytegleich: …". Heute grün: 5 Dateien, 22 Fälle (gemessen §3.2) |
| N-09 | Zeilenenden bleiben erhalten | `.gitattributes:61` `eq-copilot/fixtures/p4-korpus/** -text` | — | unverändert; keine neue Regel nötig |

**M-81 — Loudness und True Peak gegen validierte Referenz**

| Nr. | Zusage | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-10 | „Der 8-fach-True-Peak-Pfad besteht das offizielle EBU-Testset" | B17 `true_peak_matches_ebu_test_set` (**Bezeichner neu** in `Sonde013TruePeakGoldenTest.cpp:127`) | Bezeichner aus dem Test entfernt → A26 rot (R2) | Toleranz ±0,1 dB, EBU Tech 3341 Fälle 15 bis 19 |
| N-11 | „plus äquivalente generierte Mehrsampleraten-Goldens" | B17 `eight_times_oversampling_error_bound` (**Bezeichner neu**, `:216`); Sweep `:178` | — | 8-fach bei fs/4: -0,042 dB; 4-fach: -0,17 dB; fünf Raten je fs/4, fs/6, fs/8 innerhalb 0,1 dB |
| N-12 | „Toleranz ±0,1 LU … gegen eine validierte Referenz" | **B9** `lufs_integrated_matches_reference` (**Bezeichner neu** in `LoudnessGoldenTestMain.cpp:217`), `bein: B9`, `KLASSENBEINE["referenz"]` um B9 erweitert | erfundener Fallname im Korpus → A26 rot | `kToleranz = 0.1` LU über alle Korpora; Referenz ist die **ausgebaute Vektorrechnung** (`:6-12`), kein externes Testset — Belegspalte sagt das |
| N-13 | „LRA wird bei zu kurzem oder zu stillem Material nicht numerisch vorgetäuscht." | B18 `lra_below_sixty_seconds_is_not_a_number` (`Sonde013DynamicsTest.cpp:497`, **Korpusname gezogen**) | — | Wahrheit `unbekannt`, Aussage `unsicher` — unverändert |
| N-14 | PSR gegen dasselbe Fenster | B18 `plr_psr_definitions_and_labels` (`:265`, **Korpusname gezogen**) | — | Wahrheit `innerhalb`, Aussage `stark` |

**M-82 — fünf Stereoklassen über die unterstützten Blockgrößen**

Alle sechs Zeilen messen **je Blockgröße aus der Menge in §2.1** dieselbe
analytisch bekannte Antwort und zusätzlich, dass je Blockgröße vergleichbar
viele Frames gemittelt wurden (`kohaerenzFenster` je Lauf innerhalb ±20 % des
Medians über die Menge — kein Fall besteht, weil zu wenig Material floss).

| Nr. | Klasse | Messender Fall (**alle neu**, `Sonde013StereoGoldenTest.cpp`) | Erwartung je Blockgröße |
|---|---|---|---|
| N-15 | Mono | `sweep_mono_identity` | Korrelation > 0,999; Kohärenz > 0,99; Phase < 0,01 rad; Folddown < 0,25 dB |
| N-16 | identisches Stereo | `sweep_identical_stereo` | dieselben vier Werte bei breitbandigem Material auf beiden Kanälen |
| N-17 | Polaritätsinvertierung | `sweep_polarity_inversion` | Korrelation < -0,999 bei Kohärenz > 0,99; Monosumme an der Vertragsgrenze |
| N-18 | bekannte Laufzeit | `sweep_known_delay` | Phase folgt +2·pi·f·tau an drei Trägerfrequenzen, Abweichung < 0,1 rad |
| N-19 | unkorrelierte Kanäle | `sweep_uncorrelated_channels` | Korrelation < 0,2; keine Lag- oder Polaritätsempfehlung |
| N-20 | Folddown | `sweep_folddown_within_0p25db` | angezeigter Monoverlust gegen den wirklich gefalteten Puffer innerhalb 0,25 dB |

**Rotbeweis N-15 bis N-20** (`roh/NAK-182-rot-M-82.txt`): ein Test-Speiser,
der bei Blockgröße ungleich 512 einen Kanal um **ein Sample** versetzt.
Erwartung: `sweep_mono_identity`, `sweep_identical_stereo` und
`sweep_known_delay` fallen **genau bei den vier anderen Größen** und bleiben
bei 512 grün — der Beweis, dass der Sweep Blockgrößen wirklich unterscheidet
und nicht fünfmal denselben Lauf fährt.


**Matrixnacharbeit 1 (MP1-7) — N-15 bis N-20: die Bein-Behauptung nennt die
Menge.** Die sechs Zeilen oben messen unverändert; ergänzt wird nur, was B19
im Runner **sagt**: „über die fünf Blockgrößen 1, 333, 512, 2048, 16384"
beziehungsweise, im 18er-Zweig, die achtzehn Werte einzeln — **nie „über
alle"**. §2.5 führt, dass M-82s „alle unterstützten Blockgrößen" (1 bis 16384,
`StampedAudioQueue.h:240`) nicht erschöpfend fahrbar ist und durch die
benannte Menge repräsentiert wird.

**M-83 — PRE/POST-Goldens, Satz 2**

| Nr. | Zusage (wörtlich) | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-21 | „fokussierte 0,01-dB-Evidenz muss Gain innerhalb ±0,1 dB … wiederfinden" — C++-Hälfte | **neu** B16 `focused_evidence_wire_carries_0p01_db_steps` (`Sonde013EventWireTest.cpp`) | Encoder auf `q_db_0p1_i16` → die Schrittprüfung fällt | Aus dem eigenen Wire-Text mit Teiler 100: POST minus PRE = G innerhalb ±0,1 dB; zwei um 0,01 dB verschiedene Pegel ergeben zwei um genau 1 verschiedene Ganzzahlen |
| N-22 | „… samt statistischem Intervall" — Rust-Hälfte | **neu** A4 `dekodierte_evidenz_findet_gain_mit_intervall` (`broker/tests/sonde013_verdrahtung.rs`) | Dekoderteiler 10 statt 100 → die Rust-Hälfte fällt an ±0,1 dB | `achsen.intervall` ist `Some((u, o))` mit `u <= G <= o`, `abs(u - G) <= 0,1` und `abs(o - G) <= 0,1`; `gescannte_baender > 0` |
| N-23 | die Kopplung beider Sprachen | **neu** `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` aus `erzeuge_v3_fixtures.py`; A8 `--pruefen` bytegleich, A5 liest mit | ein Byte der Fixture geändert → A8 rot | beide Hälften lesen dieselbe Datei; keine von beiden schreibt sie |
| N-24 | „Kompression, Modulation, Saturation und wechselnde Latenz erzeugen keine falsche statische EQ-Behauptung." | B24 `nonlinear_chain_never_yields_static_eq_claim` (`Sonde013PrePostGoldenTest.cpp:273`) | — | unverändert gemessen; **Wahrheit im Korpus von `nicht_kausal` auf `zeitvariabel` berichtigt** (E6) |
| N-25 | „Der verteilte `q_db_0p1_i16`-Livepfad wird **nicht** gegen die 0,05-dB-Grenze geprüft." | — | — | ausdrücklich **keine** Zeile; §2.5 |


**Etappe 1b (Entscheid des Dirigenten) — N-21 und N-23 in der Form B (E4b).**
Die zwei Zeilen oben bleiben in ihrer Etappe-1-Form stehen; **bindend sind die
folgenden.** Die C++-Hälfte misst gegen die **Zahlen** der Fixture, nicht gegen
ihre Bytes — die Begründung mit Zeilen steht in E4b.

| Nr. | Zusage (wörtlich) | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-21 | „fokussierte 0,01-dB-Evidenz muss Gain innerhalb ±0,1 dB … wiederfinden" — C++-Hälfte | **neu** B16 `focused_evidence_wire_carries_0p01_db_steps` (`Sonde013EventWireTest.cpp`) | Encoder auf `q_db_0p1_i16` → die **Schrittzeile** fällt (0,01 · 10 = 0,1, `std::round` → 0, beide Pegel tragen dieselbe Ganzzahl) | Aus dem **eigenen** Wire-Text mit Teiler 100, je Band **mit Bit**: POST minus PRE = G = 3,00 dB innerhalb ±0,1 dB. Zweite Zeile: zwei um 0,01 dB verschiedene Pegel ergeben zwei um **genau 1** verschiedene Ganzzahlen. **Keine** Ganzzahl-Gleichheit mit der Fixture |
| N-23 | die Kopplung beider Sprachen | **neu** `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` aus `erzeuge_v3_fixtures.py`; A8 `--pruefen` bytegleich, A5 liest mit | ein Byte der Fixture geändert → A8 rot | Die Fixture trägt **Zahlen**: Material (Frequenz, Dauer, Samplerate), nominaler Bandpegel L, Gain G = 3,00 dB, Auflösung 0,01 dB, dazu die zwei fertigen `evidence_snapshot`-Texte aus L, G und `round(db · 100)`. Beide Hälften sind an dieselben deklarierten Zahlen gebunden; keine Seite prüft die Bytes der anderen |


**Matrixnacharbeit 1 (MP1-3, MP1-4, MP1-5) — N-21 bis N-23 neu gefasst.**
Die Fassungen aus Etappe 1 und 1b bleiben als Verlauf stehen; **bindend sind
die folgenden.** Begründung mit Zeilen im Block MN1 zu E4b.

| Nr. | Zusage (wörtlich) | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-21 | „fokussierte 0,01-dB-Evidenz muss Gain innerhalb ±0,1 dB … wiederfinden" — C++-Hälfte, zwei getrennte Zeilen | **neu** B16 `focused_evidence_wire_carries_0p01_db_steps` (`Sonde013EventWireTest.cpp`) | Encoder auf `q_db_0p1_i16` → die **Leiterzeile** fällt: 20 Schritte zu 0,01 dB sind 0,19 dB Spanne, bei Skalierung 10 also höchstens **3** verschiedene Ganzzahlen statt 20 | **Gainzeile:** aus dem eigenen Wire-Text mit Teiler 100, je Band mit Bit: POST minus PRE = G = 3,00 dB innerhalb ±0,1 dB. **Leiterzeile:** N = 20 Pegel im Abstand 0,01 dB ergeben 20 streng monotone Ganzzahlen, benachbarte unterscheiden sich um **genau 1** — rundungsphasenunabhängig, weil `round(x·100 + 1)` sich von `round(x·100)` für jedes x um exakt 1 unterscheidet (`BandGrid.h:155`) |
| N-22 | „… samt statistischem Intervall" — Rust-Hälfte, vier Erwartungen in **einer** Kette | **neu** A4 `dekodierte_evidenz_findet_gain_mit_intervall` (`broker/tests/sonde013_verdrahtung.rs`) | Dekoderskala 10 statt 100 → **a1 fällt zuerst** (das zweitgrößte Banddelta wird 0,30 statt 3,00 dB), b fällt mit | **a1** `guardrail_geschuetzt_db` innerhalb ±0,1 dB von G — das ist das **zweitgrößte** Banddelta dem Betrag nach (`experiment_verdrahtung.rs:1073-1088`) und fängt den Fall „Bänder 2 und 4 dB bei G = 3", den `intervall` durchließ. **a2** `signifikante_baender == gescannte_baender`. **a3** `gescannte_baender` gleicht der Zahl der Bänder mit Präsenzbit. **b** `intervall` ist `Some((u, o))` mit `u <= G <= o`, beide Grenzen innerhalb ±0,1 dB. **Restlücke benannt (§2.5):** das eine Band mit dem größten Delta dem Betrag nach ist von a1 nicht erfasst |
| N-23 | die Kopplung beider Sprachen | **neu** `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` (Zielpfad, in Etappe 2 anzulegen), registriert in der Dateiliste `alle` des Erzeugers (`erzeuge_v3_fixtures.py:2929-2937`) | ein Byte der Fixture geändert → A8 `--pruefen` rot (`:2962-2968`) | **A8 hält sie bytegleich; zwei Verbraucher, einer je Sprache: B16 (C++) und A4 (Rust)** — damit ist M-70 erfüllt (`SONDE-013.md:575`). **„A5 liest mit" entfällt** (MP1-5): `pruefe_v3_vertrag.py:449` öffnet nur `evidenz-zahlen-wire-v1.json`, A5 liegt außerhalb der Ticketpfade und wird nicht erweitert. Die Waisenwache (`:2942-2957`) bleibt unangetastet |


**Matrixnacharbeit 2 (MP2-2) — N-22 neu gefasst: bandweise am Produktpfad.**
Die Fassungen darüber bleiben als Verlauf stehen; **bindend ist die folgende.**
Begründung mit Zeilen im Block Matrixnacharbeit 2 zu E4b.

| Nr. | Zusage (wörtlich) | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-22 | „… samt statistischem Intervall" — Rust-Hälfte, bandweise **und** als Intervall | **neu** A4 `dekodierte_evidenz_findet_gain_mit_intervall` (`broker/tests/sonde013_verdrahtung.rs`), liest die Produktmessung über die neue lesende Auskunft `Coordinator::resultatmessung_fuer_test` nach `experiment_candidate` und `experiment_manual_result` | Dekoderskala 10 statt 100 → **(a) fällt**: jedes Band trägt 0,30 statt 3,00 dB, Abweichung 2,70 dB | **(a) — die gewertete Zeile:** für **jedes** Band mit Präsenzbit liegt `band_delta_db[b]` innerhalb ±0,1 dB von G = 3,00 dB, gelesen aus der **Produktmessung** (Store → `resultatmessung` → Terminal), nicht von Hand gebaut. **(b)** `intervall` ist `Some((u, o))` mit `u <= G <= o`, beide Grenzen innerhalb ±0,1 dB. **Zusatz, nicht gewertet:** a1 `guardrail_geschuetzt_db` innerhalb ±0,1 dB, a2 `signifikante_baender == gescannte_baender`, a3 Bandzahl gleich der Zahl der Bänder mit Bit |

**Warum a1 bis a3 allein nicht reichen** (an der Quelle nachgerechnet, MP2-2):
Banddeltas 4 dB, 2 dB und sonst 3 dB bei G = 3 bestehen **alle vier** früheren
Erwartungen — Guardrail 3, jedes Band signifikant, Intervall `[3, 3]` — und
verletzen trotzdem in **zwei** Bändern die ±0,1-dB-Zusage. Die Hülle bindet
Mittelwert und zweitgrößten Betrag, nicht die Bandgenauigkeit.

**M-84 — Loop-, Seek- und PDC-Goldens; Impulszeit stabil**

| Nr. | Zusage | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-26 | „Bekannte Impulsereignisse bleiben über Blockgrößen und Sampleraten zeitlich stabil." | **neu** B5 `impulse_time_is_stable_over_block_sizes_and_sample_rates` (`AnalysisGoldenTestMain.cpp`) | **neu** `roh/NAK-182-rot-M-84.txt`: Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um einen Block | je Samplerate: `stromSample` **identisch** über alle fünf Blockgrößen (exakte Gleichheit, E5); zusätzlich `0 <= t0 - stromSample < 2048` je Lauf; 5 Raten × 5 Größen = 25 Läufe |
| N-27 | „Jede Grenze … trennt jedes offene Fenster." | B5 `no_window_crosses_an_epoch_boundary` (**Bezeichner neu** im `grenzfall`-Helfer, `AnalysisGoldenTestMain.cpp:426-428`) | Bezeichner entfernt → A26 rot | unverändert; deckt G1 bis G9 und G13a bis G13f |
| N-28 | „ein Drop zählt als Segment" | B5 `drop_counts_as_segment_not_epoch` (**Bezeichner neu**, `:445-448`) | — | `segmentwechsel + 1`, `epochenwechsel` unverändert |
| N-29 | „ein Drop erzeugt Zähler, sichtbare Lücke und neue Grenze" | B4 `drop_produces_counter_gap_and_boundary` (**Bezeichner neu**, `QueueStressTestMain.cpp:505-515`) | — | `kontinuitaetsbrueche == 1`, `verworfeneBloecke + 1`, `haeltBlock()` |
| N-30 | Fingerprintfenster überbrückt keine Grenze | B22 `fingerprint_window_never_crosses_epoch_boundary` (`Sonde013FingerprintGoldenTest.cpp:276`) | — | unverändert, Bezeichner steht wörtlich |
| N-31 | Epochenwechsel entwertet offene Evidenz | A4 `epoch_change_invalidates_open_evidence` (`broker/tests/sonde013_taint.rs:106`) | — | unverändert, `fn`-Name steht wörtlich |


**Matrixnacharbeit 1 (MP1-1, MP1-2) — N-26 neu gefasst.** Die Zeile oben
bleibt als Verlauf stehen; **bindend ist die folgende.** Sie ersetzt die
falsche Schranke 2048 durch 4096 und trennt Gleichheit von Nähe.

| Nr. | Zusage | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-26 | „Bekannte Impulsereignisse bleiben über Blockgrößen und Sampleraten zeitlich stabil." | **neu** B5 `impulse_time_is_stable_over_block_sizes_and_sample_rates` (`AnalysisGoldenTestMain.cpp`); Impuls bei `t0 = k · 2048 + 1536` (Anker 0), Auswahl je Lauf: das Ereignis mit dem größten `stromSample <= t0` | **neu** `roh/NAK-182-rot-M-84.txt`: der Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um **4096 Samples** (eine ganze Fensterlänge) | **(a) Gleichheit, exakt:** `stromSample` ist über die fünf Blockgrößen je Samplerate **identisch** (exakte Zahl). **(b) Nähe:** `0 <= t0 - stromSample < 4096` (`kHauptPunkte`, `FeatureEngine.h:929`) — **nicht** 2048, weil der Stempel der Fensteranfang ist und t0 in der zweiten Fensterhälfte liegen darf. Die Zeile sagt „erstes **oder** zweites enthaltendes Fenster" und behauptet die Auslösung nicht schärfer. 5 Raten × 5 Größen = 25 Läufe |

**Rotbeweis, Reihenfolge:** die Verschiebung um 4096 lässt den Stempel bei
jeder Blockgröße um genau 4096 wandern — **(a) fällt zuerst**; (b) fällt im
selben Lauf mit, weil t0 der nominale Erwartungswert bleibt und
`t0 - stromSample` negativ wird. Eine Verschiebung „um einen Block" wäre bei
Blockgröße 1 oder 333 kleiner als der Hop geblieben und hätte die
Gleichheitszeile grün gelassen (MP1-2).

**M-85 — adversariale Vergleichsfixtures**

| Nr. | Ziel | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-32 | Ziel 2: „Ein nicht vergleichbares Experiment erhält kein starkes Siegerurteil." | A4 `gate6_incomparable_never_gets_a_strong_winner` (`broker/tests/sonde013_experiment.rs:576`) | Rotbeweis N-02 | Wahrheit `unvergleichbar`, Aussage `unsicher` |
| N-33 | Ziel 2, zweite Hälfte | A4 `comparability_needs_all_five_and_hash_alone_never_wins` (`broker/tests/sonde013_passage.rs:60`) | — | Wahrheit `unvergleichbar`, Aussage `unsicher` |
| N-34 | Ziel 3: „Eine Standard-Insertprobe wird nie als exakter Mastersummenbeitrag bezeichnet." | A16 `insert_probe_is_never_called_master_sum` (**Bezeichner neu** in `SondeNullTestMain.cpp:253`) | Rotbeweis N-03 | **Wahrheit von `nicht_exakt` auf `insertprobe` berichtigt** (E1), Aussage `schwach` |
| N-35 | Exit-Gate Satz 2: kein unbekannter Zeitpfad erzeugt eine starke Aussage | A4 `unknown_time_path_can_never_reach_strong` (`broker/tests/sonde013_prepost.rs:546`) | — | Wahrheit `unbekannt`, Aussage `unsicher` |
| N-36 | verteiltes Paar liefert keinen Frequenzgang | A4 `distributed_pair_never_produces_transfer_function` (`broker/tests/sonde013_prepost.rs:479`) | — | **Wahrheit von `nicht_kausal` auf `verteilt` berichtigt** (E1), Aussage `schwach` |
| N-37 | Ziel 1: „korrelierter, aber nicht kausaler Distraktor" | **nicht gemessen** — Entscheid G4 §8, NAK-190 | — | MANIFEST-Feld `nicht_gemessen`, von A26 im Kopf gedruckt (E6); der Wortlaut „Distraktor" verschwindet aus Fällen und Hinweisen |
| N-38 | „kein P4-Datenweg benennt einen sicheren Auslöser" | **neu** A26 liest fünf geschlossene Mengen aus `eq-ipc-v3.schema.json` (E6) | **neu** `roh/NAK-182-rot-M-85-ausloeser.txt`: ein Enumwert um `ausloeser` erweitert | Exit 2; fehlt eine der fünf Mengen im Schema, ebenfalls Exit 2 |


**Etappe 1b (Entscheid des Dirigenten) — N-34 und N-36 in der Fassung R1a.**
Die zwei Zeilen oben bleiben in ihrer Etappe-1-Form stehen; **bindend sind die
folgenden.** Die Umbenennung der Wahrheit ist abgelehnt; unter R1a braucht es
sie nicht, weil `nicht_exakt` und `nicht_kausal` nur in `KEINE_STARKE` stehen
und die **schwache** Aussage sie trägt (E1b).

| Nr. | Ziel | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-34 | Ziel 3: „Eine Standard-Insertprobe wird nie als exakter Mastersummenbeitrag bezeichnet." | A16 `insert_probe_is_never_called_master_sum` (**Bezeichner neu** in `SondeNullTestMain.cpp:253`) | **neu** `roh/NAK-182-rot-M-85-gate7.txt`: derselbe Fall auf `aussage: stark` → `falsche_starke = 1`, Exit 2 | Wahrheit `nicht_exakt` **unverändert**, Aussage `schwach`, Konfidenz 0,65. Gegenprobe: als `schwach` bleibt er grün — das ist Gate 7 im Prüfer, gemessen |
| N-36 | verteiltes Paar liefert keinen Frequenzgang | A4 `distributed_pair_never_produces_transfer_function` (`broker/tests/sonde013_prepost.rs:479`) | **neu** `roh/NAK-182-rot-M-85-verteilt.txt`: derselbe Fall auf `aussage: stark` → `falsche_starke = 1`, Exit 2 | Wahrheit `nicht_kausal` **unverändert**, Aussage `schwach`, Konfidenz 0,60. Gegenprobe: als `schwach` bleibt er grün |


**Matrixnacharbeit 2 (MP2-1) — N-26 neu gefasst: die Auswahl kennt t0 nicht.**
Die Fassungen darüber bleiben als Verlauf stehen; **bindend ist die folgende.**
Begründung mit Zeilen im Block Matrixnacharbeit 2 zu E5.

| Nr. | Zusage | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-26 | „Bekannte Impulsereignisse bleiben über Blockgrößen und Sampleraten zeitlich stabil." | **neu** B5 `impulse_time_is_stable_over_block_sizes_and_sample_rates` (`AnalysisGoldenTestMain.cpp`); ein Impuls bei `t0 = 100 · 2048 + 1536 = 206336` auf ruhigem, nicht stillem Boden; gemessen wird **`ereignis(0)`**, das erste Ereignis des Laufs (Ringreihenfolge, `FeatureEngine.h:1381-1385`) — **ohne jeden Bezug auf t0** | **neu** `roh/NAK-182-rot-M-84.txt`: der Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um **4096 Samples** | **E-a** `ereignisAnzahlJetzt() >= 1` (der Impuls hat ausgelöst). **E-b** `ereignisAnzahlJetzt() < 64` (Ring hat nichts verworfen → Ringreihenfolge ist Zeitreihenfolge). **E-c** `ereignis(0).qualitaetFluss` gesetzt. **E-d** `ereignis(0).stromSample >= t0 - 4096` (der Boden erzeugt vor dem Impuls nichts). **(a) — gewertet:** `ereignis(0).stromSample` über die fünf Blockgrößen je Samplerate **identisch**. **(b)** `0 <= t0 - ereignis(0).stromSample < 4096`. 5 Raten × 5 Größen = 25 Läufe |

**Rotbeweis, Reihenfolge:** nach der Verschiebung liegt `ereignis(0)` bei den
vier verschobenen Läufen um 4096 höher als bei 512 — **(a) fällt**; (b) fällt
in denselben Läufen mit negativer Differenz. Die frühere Auswahl „größtes
`stromSample <= t0`" hätte beide verschobenen Fenster verworfen und den
Rotbeweis am Impuls **vorbeimessen** lassen (MP2-1).

### 2.3 Der Korpus nach der Korrektur — alle Fälle

24 Fälle in vier Klassen (heute 22). Spalte „misst wirklich" ist die Kurzform
dessen, was der genannte Bezeichner an seiner Stelle prüft.

**Klasse `referenz` — Beine B17, B18, B9 (neu)**

| bein | fall | quelle (Datei · Bezeichner) | wahrheit | aussage | konf. | misst wirklich |
|---|---|---|---|---|---:|---|
| B17 | `true_peak_matches_ebu_test_set` | `eq-copilot/plugin/tests/Sonde013TruePeakGoldenTest.cpp` · `true_peak_matches_ebu_test_set` **(neu)** | `ueberschreitet` | stark | 0,98 | EBU Tech 3341 Fälle 15–19, fs/4, fs/6, fs/8 bei 0,50 und 1,41 FFS, ±0,1 dB |
| B17 | `eight_times_oversampling_error_bound` | dieselbe Datei · `eight_times_oversampling_error_bound` **(neu)** | `ueberschreitet` | stark | 0,97 | geschlossene Fehlerformel: 8-fach -0,042 dB gegen 4-fach -0,17 dB |
| **B9** | `lufs_integrated_matches_reference` | `eq-copilot/plugin/tests/LoudnessGoldenTestMain.cpp` · `lufs_integrated_matches_reference` **(neu)** | `innerhalb` | stark | 0,99 | LUFS-I gegen die **ausgebaute Vektorrechnung**, ±0,1 LU über sechs Korpora — kein externes Testset |
| B18 | `lra_below_sixty_seconds_is_not_a_number` | `eq-copilot/plugin/tests/Sonde013DynamicsTest.cpp:497` · wörtlich vorhanden | `unbekannt` | unsicher | 0,50 | unter 60 s gegatetem Material gibt es keinen LRA-Wert |
| B18 | `plr_psr_definitions_and_labels` | dieselbe Datei `:265` · wörtlich vorhanden | `innerhalb` | stark | 0,95 | PSR gegen das True-Peak-Maximum **desselben** 3-s-Fensters |

**Klasse `zeitachse` — Beine B4, B5, B22, A4**

| bein | fall | quelle | wahrheit | aussage | konf. | misst wirklich |
|---|---|---|---|---|---:|---|
| B5 | `no_window_crosses_an_epoch_boundary` | `AnalysisGoldenTestMain.cpp` · Bezeichner **neu** im `grenzfall`-Helfer | `getrennt` | stark | 0,99 | nach jeder der neun Grenzen trägt kein Fenster ein Sample von davor |
| B5 | `drop_counts_as_segment_not_epoch` | dieselbe Datei · Bezeichner **neu** | `segment` | stark | 0,98 | ein Drop erhöht `segmentwechsel`, nicht `epochenwechsel` |
| B5 | `impulse_time_is_stable_over_block_sizes_and_sample_rates` **(neu)** | dieselbe Datei · **neu** | `zeitstabil` | stark | 0,97 | `stromSample` eines bekannten Impulses ist über fünf Blockgrößen identisch, bei fünf Sampleraten |
| B4 | `drop_produces_counter_gap_and_boundary` | `QueueStressTestMain.cpp` · Bezeichner **neu** | `getrennt` | stark | 0,97 | Seek erzeugt genau einen Bruch, genau einen verworfenen Block, neue Quarantäne |
| B22 | `fingerprint_window_never_crosses_epoch_boundary` | `Sonde013FingerprintGoldenTest.cpp:276` · wörtlich vorhanden | `getrennt` | stark | 0,96 | auch der Fingerprint fällt unter §32.3 |
| A4 | `epoch_change_invalidates_open_evidence` | `broker/tests/sonde013_taint.rs:106` · wörtlich vorhanden | `getrennt` | stark | 0,98 | der Grund auf dem Draht ist je Auslöser eindeutig (M-53) |

**Klasse `vergleich` — Beine B24, B16, A4, A16**

| bein | fall | quelle | wahrheit | aussage | konf. | misst wirklich |
|---|---|---|---|---|---:|---|
| B24 | `nonlinear_chain_never_yields_static_eq_claim` | `Sonde013PrePostGoldenTest.cpp:273` · wörtlich vorhanden | `zeitvariabel` **(war `nicht_kausal`)** | unsicher | 0,50 | vier zeitvariable/nichtlineare Ketten **desselben** Signals; die Kohärenz fällt, keine statische EQ-Behauptung |
| B16 | `focused_evidence_wire_carries_0p01_db_steps` **(neu)** | `Sonde013EventWireTest.cpp` · **neu** | `wiedergefunden` | stark | 0,96 | der echte Encoder schreibt 0,01-dB-Schritte; aus dem eigenen Wire-Text kommt G ±0,1 dB zurück |
| A4 | `dekodierte_evidenz_findet_gain_mit_intervall` **(neu)** | `broker/tests/sonde013_verdrahtung.rs` · **neu** | `wiedergefunden` | stark | 0,95 | Wirepfad → `perzentil_dekodieren` → `resultatmessung` → `achsen`: Intervall enthält G, beide Grenzen ±0,1 dB |
| A4 | `distributed_pair_never_produces_transfer_function` | `broker/tests/sonde013_prepost.rs:479` · wörtlich vorhanden | `verteilt` **(war `nicht_kausal`)** | schwach | 0,60 | verteilt gibt es Banddifferenzen, keinen Frequenzgang |
| A4 | `gate6_incomparable_never_gets_a_strong_winner` | `broker/tests/sonde013_experiment.rs:576` · wörtlich vorhanden | `unvergleichbar` | unsicher | 0,50 | harter Gate 6 aus §49.2 |
| A4 | `comparability_needs_all_five_and_hash_alone_never_wins` | `broker/tests/sonde013_passage.rs:60` · wörtlich vorhanden | `unvergleichbar` | unsicher | 0,50 | kein einzelner Hash hebt einen Widerspruch auf |
| A4 | `unknown_time_path_can_never_reach_strong` | `broker/tests/sonde013_prepost.rs:546` · wörtlich vorhanden | `unbekannt` | unsicher | 0,50 | Exit-Gate Satz 2 |
| A16 | `insert_probe_is_never_called_master_sum` | `SondeNullTestMain.cpp:253` · Bezeichner **neu** | `insertprobe` **(war `nicht_exakt`)** | schwach | 0,65 | Gate 7: `post_fader_contribution` kommt read-only zurück, die Instanz bleibt neutral |

**Klasse `stereo` — Bein B19**

Alle sechs Fälle zeigen auf den neuen Sweep; die bestehenden Abschnitte
(`mono_identity:160`, `folddown_matches_real_buffer_within_0p25db:255`,
`bandwise_ms_and_correlation:294`, `bekannte Laufzeit:495`,
`Polaritaetsinvertierung:692`) bleiben unverändert und stehen im `hinweis`.

| bein | fall | quelle (`Sonde013StereoGoldenTest.cpp`, alle **neu**) | wahrheit | aussage | konf. |
|---|---|---|---|---|---:|
| B19 | `sweep_mono_identity` | `sweep_mono_identity` | `mono` | stark | 0,99 |
| B19 | `sweep_identical_stereo` | `sweep_identical_stereo` | `identisch` | stark | 0,99 |
| B19 | `sweep_polarity_inversion` | `sweep_polarity_inversion` | `invertiert` | stark | 0,98 |
| B19 | `sweep_known_delay` | `sweep_known_delay` | `laufzeit` | stark | 0,96 |
| B19 | `sweep_uncorrelated_channels` | `sweep_uncorrelated_channels` | `unkorreliert` | stark | 0,95 |
| B19 | `sweep_folddown_within_0p25db` | `sweep_folddown_within_0p25db` | `innerhalb` | stark | 0,97 |

**Bilanz gegen heute:** 22 → 24 Fälle. Neu:
`impulse_time_is_stable_over_block_sizes_and_sample_rates` (zeitachse),
`focused_evidence_wire_carries_0p01_db_steps` und
`dekodierte_evidenz_findet_gain_mit_intervall` (vergleich). Umgezogen:
`lufs_integrated_matches_reference` von B18 auf **B9**. Wahrheit berichtigt:
drei Fälle (E1, E6). Fallname gezogen: `lra_below_sixty_seconds_is_not_a_number`,
`plr_psr_definitions_and_labels` und alle sechs `stereo`-Fälle. Bezeichner im
Test **hinzugefügt** (nie umbenannt): neun Stellen.


**Etappe 1b (Entscheid des Dirigenten) — drei Zeilen der Klasse `vergleich`
zurückgenommen. Bindend ist dieser Block; die Tabelle darüber bleibt in ihrer
Etappe-1-Form stehen, damit die Geschichte lesbar bleibt.**

Die Umbenennung `nicht_kausal` → `verteilt` und `nicht_exakt` → `insertprobe`
ist abgelehnt (E1b): mit `insertprobe` außerhalb der unsicheren Menge ließe
`_passt` eine **starke** Aussage auf dem A16-Fall durch, und Gate 7 wäre im
Prüfer wieder blind. Unter R1a braucht es die Umbenennung nicht: die schwache
Aussage trägt `nicht_kausal` und `nicht_exakt`, weil beide nur in
`KEINE_STARKE` stehen, nicht in `KEINE_BEHAUPTUNG`.

| bein | fall | wahrheit (Etappe 1b) | aussage | konf. | war in Etappe 1 |
|---|---|---|---|---:|---|
| B24 | `nonlinear_chain_never_yields_static_eq_claim` | `zeitvariabel` | unsicher | 0,50 | `zeitvariabel` — **unverändert** (E6 angenommen). Neu ist nur, dass `zeitvariabel` in `KEINE_BEHAUPTUNG` steht: eine schwache Aussage darauf wäre ab jetzt falsch (N-04) |
| A4 | `distributed_pair_never_produces_transfer_function` | `nicht_kausal` | schwach | 0,60 | `verteilt` — **zurückgenommen**, wie am Basis-SHA |
| A16 | `insert_probe_is_never_called_master_sum` | `nicht_exakt` | schwach | 0,65 | `insertprobe` — **zurückgenommen**, wie am Basis-SHA |

Alle übrigen Zeilen der vier Klassen bleiben, wie sie in den Tabellen darüber
stehen. Der Fallbestand bleibt bei **24** (22 am Basis-SHA plus die drei neuen
aus R4/R5, minus keiner); die Klasse `vergleich` hat weiterhin acht Fälle.

**Bilanz gegen heute (Etappe 1b):** 22 → 24 Fälle. Neu:
`impulse_time_is_stable_over_block_sizes_and_sample_rates` (zeitachse),
`focused_evidence_wire_carries_0p01_db_steps` und
`dekodierte_evidenz_findet_gain_mit_intervall` (vergleich). Umgezogen:
`lufs_integrated_matches_reference` von B18 auf **B9**. Wahrheit berichtigt:
**ein** Fall (B24, `nicht_kausal` → `zeitvariabel`, E6) — nicht drei.
Fallname gezogen: `lra_below_sixty_seconds_is_not_a_number`,
`plr_psr_definitions_and_labels` und alle sechs `stereo`-Fälle. Bezeichner im
Test **hinzugefügt** (nie umbenannt): neun Stellen.

### 2.4 Die Selbsttestfälle des Prüfskripts

Form: `pruefe_p4_korpus.py --selbsttest` als **eigenes Kanonbein A27**
(Entscheid: eigenes Bein statt Vorlauf in A26, weil ein Vorlauf im selben Bein
bei rotem Selbsttest nicht mehr unterscheidbar wäre von einem roten Korpus —
und weil die Bein-Behauptung dann zwei verschiedene Sachen sagen müsste, was
Prüfliste E ausdrücklich verbietet). Der Selbsttest läuft **ohne
Repo-Fixture**: er baut seine Fälle im Speicher und schickt sie durch
`_kennzahlen` und die Riegel.

| Nr. | Fall | Erwartung | Gegenteil (im selben Lauf) |
|---|---|---|---|
| S-01a…d | je ein synthetischer Fall mit `aussage: stark` auf `unbekannt`, `unvergleichbar`, `nicht_kausal`, `nicht_exakt` | `falsche_starke == 1` | derselbe Fall mit `aussage: unsicher` → `falsche_starke == 0` |
| S-02a…d | dieselben vier mit `aussage: schwach` | `falsche_schwache == 1` | mit `aussage: unsicher` → `0` |
| S-03 | ein Korpus, in dem `richtige_starke > moegliche_starke` konstruiert ist | `recall > 1,0` wird als rot gemeldet | derselbe Korpus mit einer sicheren Wahrheit mehr → `recall <= 1,0`, grün |
| S-04 | `precision > 1,0` | rot | Gegenprobe grün |
| S-05 | ein Fall mit erfundenem Fallnamen in einer existierenden Datei (R2) | rot, Text nennt Datei und Bezeichner | derselbe Fall mit einem Bezeichner, der in der Datei steht → grün |
| S-06 | ein Fall mit `quelle`-Pfad ins Leere | rot | vorhandener Pfad → grün |
| S-07 | ein Fall mit einer Wahrheit außerhalb `ERLAUBTE_WAHRHEITEN` | rot | Wert aus der Menge → grün |
| S-08 | ein Schema, in dem eine der fünf geschlossenen Mengen fehlt (N-38) | rot | vollständiges Schema → grün |
| S-09 | ein Enumwert mit einem kausalen Bezeichner (N-38) | rot | Vertragsschema → grün |

Jede Zeile prüft ihre Erwartung **und** ihr Gegenteil — sonst wäre der
Selbsttest grün, auch wenn der Riegel immer rot meldete.


**Etappe 1b (Entscheid des Dirigenten) — S-01 und S-02 in der Fassung R1a.**
Die Zeilen S-03 bis S-09 gelten unverändert; S-01 und S-02 der Tabelle darüber
lasen noch eine einzige Menge und bleiben als Verlauf stehen. **Bindend sind
die folgenden drei Zeilen.** Sie messen genau die Trennung, die R1a einführt —
ohne S-02b wäre `KEINE_STARKE` von `KEINE_BEHAUPTUNG` nicht unterscheidbar,
und der Selbsttest wäre auch mit einer einzigen Menge grün.

| Nr. | Fall | Erwartung | Gegenteil (im selben Lauf) |
|---|---|---|---|
| S-01a…e | je ein synthetischer Fall mit `aussage: stark` auf jedem Wert aus `KEINE_STARKE` (`unbekannt`, `unvergleichbar`, `zeitvariabel`, `nicht_kausal`, `nicht_exakt`) | `falsche_starke == 1` | derselbe Fall mit `aussage: unsicher` → `falsche_starke == 0` |
| S-02a…c | je ein Fall mit `aussage: schwach` auf jedem Wert aus `KEINE_BEHAUPTUNG` (`unbekannt`, `unvergleichbar`, `zeitvariabel`) | `falsche_schwache == 1` | derselbe Fall mit `aussage: unsicher` → `falsche_schwache == 0` |
| S-02d…e | je ein Fall mit `aussage: schwach` auf jedem Wert aus `KEINE_STARKE` ohne `KEINE_BEHAUPTUNG` (`nicht_kausal`, `nicht_exakt`) | **`falsche_starke == 0` und `falsche_schwache == 0`** — die schwache Aussage trägt diese Wahrheit | derselbe Fall mit `aussage: stark` → `falsche_starke == 1` |

Jede Zeile prüft ihre Erwartung **und** ihr Gegenteil — sonst wäre der
Selbsttest grün, auch wenn der Riegel immer rot meldete.


**Matrixnacharbeit 1 (MP1-6) — die Quotenwachen bekommen einen ausführbaren
Weg: `_riegel` wird eine eigene Funktion, und A27 speist ihr Kennzahlen direkt
ein.** Die Tabellen oben bleiben stehen; **bindend ist dieser Block.**

Der Befund trifft: der vorgeschriebene Weg „synthetischer Korpus →
`_kennzahlen` → Riegel" kann S-03 und S-04 nach R1a **nicht** erzeugen.
`richtige_starke = len(stark) - len(falsche_starke)` ist strukturell höchstens
`len(stark)` (`pruefe_p4_korpus.py:85-94`), und unter R1a sind die richtigen
starken Fälle eine Teilmenge der möglichen — `recall > 1` und
`precision > 1` sind über den Korpus unerreichbar. Genau das hatte E1b als
„strukturell unerreichbar" beschrieben, ohne die Folge zu ziehen: eine Wache
ohne ausführbaren Negativtest ist keine gemessene Zusage.

**Regel MN1-6.** Die Riegelauswertung wird aus `main` herausgezogen:

    _riegel(kennzahlen: dict, klasse: str) -> list[str]

Sie bekommt ein Kennzahl-Dict und den Klassennamen und gibt die roten Zeilen
zurück (leer = grün). A26 ruft sie auf die **echten** Kennzahlen jeder Klasse
und auf die Gesamtzahlen; A27 speist ihr **synthetische** Dicts direkt ein.
Damit hat jede Wache einen Weg, auf dem sie fällt, ohne dass ein Korpus sie
erzeugen müsste.

**§2.4 nennt ab hier je Selbsttestfall den Weg.**

| Nr. | Weg | Fall | Erwartung | Gegenteil (im selben Lauf) |
|---|---|---|---|---|
| S-01a…e | **Korpusweg** (synth. Korpus → `_kennzahlen` → `_riegel`) | `aussage: stark` auf jedem Wert aus `KEINE_STARKE` | `falsche_starke == 1` | derselbe Fall `unsicher` → 0 |
| S-02a…c | **Korpusweg** | `aussage: schwach` auf jedem Wert aus `KEINE_BEHAUPTUNG` | `falsche_schwache == 1` | derselbe Fall `unsicher` → 0 |
| S-02d…e | **Korpusweg** | `aussage: schwach` auf `nicht_kausal` bzw. `nicht_exakt` | `falsche_starke == 0` **und** `falsche_schwache == 0` | derselbe Fall `stark` → `falsche_starke == 1` |
| S-03 | **Kennzahlweg** (`_riegel` direkt) | `{"recall": 1.07, …}` | eine rote Zeile, die `recall` nennt | `{"recall": 1.00, …}` → keine rote Zeile |
| S-04 | **Kennzahlweg** | `{"precision": 1.02, …}` | eine rote Zeile, die `precision` nennt | `{"precision": 1.00, …}` → keine |
| S-04b | **Kennzahlweg** | `{"falsche_starke": 1, …}` | rote Zeile | `0` → keine |
| S-04c | **Kennzahlweg** | `{"falsche_schwache": 1, …}` | rote Zeile | `0` → keine |
| S-04d | **Kennzahlweg** | `{"brier": 0.26, …}` (über dem Gate 0,25) | rote Zeile | `0.24` → keine |
| S-04e | **Kennzahlweg** | `{"coverage": 0.49, …}` bzw. `{"kalibrierung": 0.16, …}` | je eine rote Zeile | `0.51` bzw. `0.14` → keine |
| S-05 bis S-09 | **Korpusweg** bzw. Schemaweg, unverändert | wie in der Tabelle oben | unverändert | unverändert |

Jede Zeile prüft ihre Erwartung **und** ihr Gegenteil. Der Kennzahlweg ist
kein Umgehen des Produktpfads: A26 fährt dieselbe Funktion auf den echten
Kennzahlen, und ein Riegel, der nur im Selbsttest existierte, wäre die
zweite Wahrheit, an der C6 hing.

### 2.5 Was die Matrix ausdrücklich **nicht** zusagt

- **M-85 Ziel 1** (korrelierter, nicht kausaler Distraktor) wird in P4 nicht
  gemessen. Entscheid G4 §8, Eigentümer NAK-190. Die Lücke steht im MANIFEST
  und wird von A26 gedruckt (N-37).
- **M-81 „Standard-Testmaterial" für LUFS-I** ist nicht gedeckt: B9 misst
  gegen die ausgebaute Vektorrechnung, nicht gegen ein externes Testset (E2,
  N-12). Kein neues Testset in diesem Ticket; Nebenbefund §4.7.
- **Der verteilte `q_db_0p1_i16`-Livepfad** wird nicht gegen die 0,05-dB-Grenze
  des lokalen Audio-Compare geprüft — M-83 verbietet das ausdrücklich (N-25).
- **`Dreifachergebnis` bekommt kein Intervallfeld.** Die Zusage von M-83 Satz 2
  wird im Experimentpfad gemessen (E4); das Paarurteil bleibt unverändert.
  Nebenbefund §4.7.
- **Kein Fractional-Lag-Residual-Golden** (Entwurf §38.5) — nicht Gegenstand
  dieses Tickets.
- **Kein Produktcode, kein Schemafeld, keine Umbenennung** bestehender
  Testbezeichner, Testziele oder `fn`-Namen.
- **B19 fährt fünf Blockgrößen, nicht alle achtzehn**, solange das
  Laufzeitbudget nicht gemessen ist (E3). Die Bein-Behauptung nennt die Menge
  und sagt nie „über alle".


**Matrixnacharbeit 1 — zwei weitere Nicht-Zusagen, ausdrücklich.**

- **M-82 „über alle unterstützten Blockgrößen" ist nicht erschöpfend fahrbar
  (MP1-7).** Die unterstützte Menge ist das ganze Intervall 1 bis 16384
  (`StampedAudioQueue.h:240`), also 16384 Werte; gefahren wird eine benannte
  Auswahl von fünf (oder achtzehn). Die Auswahl ist begründet — Ränder 1 und
  16384, Hop-Teiler 512 und 2048, Hop-Nichtteiler 333 samt Phasenfalle — und
  **repräsentiert** die Zusage, erschöpft sie nicht. Werte wie 256 bleiben in
  beiden Zweigen ungemessen. Die Bein-Behauptung von B19 nennt deshalb in
  jedem Zweig die gefahrene Menge und sagt **nie „über alle"**.
- **Die bandweise Gain-Wiederfindung im Broker wird nur als Hülle gemessen
  (MP1-3).** `guardrail_geschuetzt_db` bindet das **zweitgrößte** Banddelta dem Betrag nach
  an G, `signifikante_baender == gescannte_baender` verlangt, dass jedes Band
  mit Bit sich bewegt hat, und `intervall` bindet den Mittelwert. **Genau ein
  Band — das mit dem größten Delta dem Betrag nach — ist davon nicht erfasst**; es geht in
  den Mittelwert nur mit 1/n ein. Ein einzelnes davonlaufendes Band bliebe
  unentdeckt. Der Grund ist kein Messfehler, sondern eine Sichtbarkeitsgrenze:
  die bandweise Zahl verlässt den Store nicht (§3.7). Ob das reicht oder ob
  der Broker einen lesenden Zugang bekommt, entscheidet der Dirigent.


**Matrixnacharbeit 2 — die Restlücke ist gestrichen, nicht verkleinert.**

Der zweite Punkt des Blocks darüber („Die bandweise Gain-Wiederfindung im
Broker wird nur als Hülle gemessen … genau ein Band") ist **überholt durch
MN2-2** und in zwei Hinsichten falsch:

1. **Die Zahl war falsch.** Nicht ein Band entkommt der Hülle, sondern
   beliebig viele: jede Abweichungsmenge, die den Mittelwert bei G lässt, den
   zweitgrößten Betrag bei G lässt und kein Band auf 0 zieht, besteht a1 bis
   a3 und b. Der Gegenfall der Prüfung — 4 dB, 2 dB, sonst 3 dB bei G = 3 —
   verletzt in **zwei** Bändern die ±0,1-dB-Zusage und besteht trotzdem alle
   vier Erwartungen.
2. **Die Einschränkung besteht nicht mehr.** Mit der lesenden Testauskunft
   `Coordinator::resultatmessung_fuer_test` (MN2-2) misst N-22 (a) jedes Band
   mit Präsenzbit direkt an der Produktmessung. Es gibt keine Restlücke mehr,
   die auszuweisen wäre.

Die Zeile darüber bleibt als Verlauf stehen und trägt diesen Vermerk;
**gültig ist dieser Block.** Die Nicht-Zusage zu M-82 (erste Zeile des Blocks
darüber, MP1-7) bleibt davon unberührt und gilt weiter.

## 3. Ist-Stand an der Quelle

Alle Zeilen sind am Basis-SHA `c4290fb` neu verortet; die Zahlen des
Gate-Stands `9f30008` sind seit NAK-180/NAK-181 teilweise verschoben. Roher
Lauf: `docs/beweise/roh/NAK-182-ist-stand-c4290fb.txt`, Rechnung
`docs/beweise/roh/NAK-182-ist-stand-rechnung.py` (liest nur den committeten
Korpus, mutiert im Speicher).

### 3.1 R4 zuerst — welcher Produktpfad liefert Gain UND Intervall

Die Frage des Auftrags: „den Produktpfad benennen, der aus dekodierter
0,01-dB-Evidenz Gain UND Intervall liefert … Gibt es keinen solchen Pfad
ende-zu-ende, steht das als Befund in §2/§3 mit der Stelle, an der das
Intervall fehlt."

**Es gibt ihn.** Die Kette steht vollständig im Produkt, Zeile für Zeile:

| # | Schritt | Stelle |
|---:|---|---|
| 1 | Der Encoder setzt die Auflösung: `f.evidenzP10/P50/P95` bekommen `encoding = q_db_0p01_i16`, jeder Wert entsteht über `quantisiere16(punkte[i], BandEncoding::q_db_0p01_i16)` | `eq-copilot/plugin/core/analysis/FeatureEngine.h:3925-3930`, `:3961`; Teiler 100,0 in `BandGrid.h:140` |
| 2 | Der Serialisierer schreibt `verteilung.p10/p50/p95` auf den Draht | `eq-copilot/plugin/vertrag/NakamaEvidenz.cpp:474-478`, Bandsatz `:133-139` |
| 3 | Der Broker dekodiert: `Evidenzstand::aus_json` ruft `perzentil_dekodieren`, dort ist `Some("q_db_0p01_i16") => 100.0` | `broker/src/coordinator/evidenz.rs:232-233`, `:327-359`, Teiler `:335` |
| 4 | Das Ergebnis liegt als `p50_db: Vec<f32>` mit Präsenzbits im Evidenzbestand | `evidenz.rs:92` |
| 5 | **Der Gain je Band** entsteht als Resultat minus Baseline über die gebundenen Snapshots derselben Passage | `broker/src/coordinator/experiment_verdrahtung.rs:787-975`, Bandmittel `:901-934`, Differenz `:936-943` |
| 6 | Die **Zeitreihe** je Resultatfenster entsteht daneben (`fenster_delta_db`) | `experiment_verdrahtung.rs:946-960`, Feldbeschreibung `experiment.rs:236-245` |
| 7 | **Das Intervall** entsteht aus dieser Zeitreihe im Block-Bootstrap | `experiment.rs:335-341` (`block_bootstrap` über die Fenstermittel, Ziehungen und Blocklänge als Konstanten `:528-530`), Implementierung `:1366` |
| 8 | Beides reist im Terminalereignis und ist von außen lesbar | `Achsenrechnung.intervall: Option<(f64, f64)>` (`experiment.rs:202`), `Terminal::Ergebnis { achsen }` (`:542-551`), Serialisierung `experiment_verdrahtung.rs:676-697` |

**Wo das Intervall fehlt.** Im **anderen** Pfad, dem Paarurteil:
`Dreifachergebnis` (`broker/src/coordinator/prepost.rs:294-313`) trägt
`roh_db`, `roh_gueltig`, `ausgerichtet_gueltig`, `baender_ohne_messung`,
`ausgerichtet_db`, `match_gain_db` und `wirkung` — **kein** Intervallfeld. Der
Gain ist dort der Median der endlichen Bandrelationen (`:826-828`). Dieser
Pfad kann M-83 Satz 2 nicht tragen, und er bekommt in diesem Ticket auch kein
Feld (§2.5).

**Was heute gemessen wird und was nicht.**

- Gemessen ist die **Grenzwertkonsistenz** der Kodierung zwischen Schema,
  Rust und C++: `broker/tests/contract_cross_language.rs:304-305`, `:339-342`;
  `eq-copilot/plugin/tests/SchemaTestMain.cpp:485-529`, `:751`.
- Gemessen ist der **Bootstrap isoliert**: `EqCopSonde013ExperimentGoldenTest`
  (B25, `tools/beweise.ps1:654`) und
  `broker/tests/sonde013_experiment.rs:434`
  (`block_bootstrap_und_fdr_halten_ihre_zusagen`) sowie `:867`
  (`block_bootstrap_weist_nichtendliches_ab`).
- **Nicht gemessen ist die Kette.** `Achsenrechnung.intervall` wird heute
  ausschließlich aus **von Hand gesetzten** `Resultatmessung`-Feldern gefahren:
  `broker/tests/sonde013_experiment.rs:27-57` (`fn messung()`),
  `broker/tests/sonde013_verdrahtung.rs:816` und `:2487`. Kein Test führt
  `perzentil_dekodieren` in dieselbe Zeile wie `intervall`.
- Die Rust-Hüllkurven in `broker/tests/sonde013_prepost.rs:33-104` (`huelle`,
  `verzoegere`, `haelfte`, `perfekt`) sind **Floats direkt**; sie kommen nie
  durch die Dekodierung.
- Der einzige Produktpfadtest, der die volle Experimentkette fährt, ist
  `broker/tests/sonde013_verdrahtung.rs:3990-4093`
  (`passagenbelege_kommen_aus_den_vertretenen_quellen`) — er prüft
  Vergleichbarkeit und Effektstabilität, **nicht** Gain und Intervall.
- Die Wirefixture, die dieser Harnisch einspeist
  (`eq-copilot/fixtures/v3/gueltig/evidence-snapshot-mit-ereignissen-und-stereo.json`,
  gelesen bei `sonde013_verdrahtung.rs:151-156`), trägt in
  `verteilung.p10/p50/p95` heute `encoding: "q_db_0p1_i16"` — also **nicht** die
  fokussierte Auflösung. Sie ist von Hand geschrieben und nicht die Ausgabe des
  echten Encoders. Deshalb die eigene Fixture aus E4.

**Folge für den Bau:** kein Produktcode, kein Schemafeld. Der Messweg aus E4
benutzt ausschließlich vorhandene öffentliche Wege (`evidence_snapshot_json`,
`experiment_begin`, `experiment_candidate`, `experiment_manual_result`,
`experiment_sicht`) und vorhandene öffentliche Felder.

### 3.2 Prüfskript und Erzeuger — gemessen, nicht gelesen

Baseline am Basis-SHA (roher Lauf, Abschnitte 1 und 2):

    A25 erzeuge_p4_korpus.py --pruefen  -> "bytegleich", EXIT 0
                                           5 Dateien, 22 Faelle in 4 Klassen
    A26 pruefe_p4_korpus.py             -> EXIT 0, 22 Faelle,
                                           0 falsche starke Behauptungen
                                           Brier 0,0174 · Kalibrierung 0,0671
                                           Coverage 0,77 · Enthaltungsrate 0,23

Je Klasse heute: `referenz` 5 Fälle (4 stark, 1 Enthaltung, Brier 0,0010),
`stereo` 6 (6 stark, Brier 0,0009), `vergleich` 6 (0 stark, 4 Enthaltungen,
Brier 0,1413, Coverage 0,33), `zeitachse` 5 (5 stark, Brier 0,0007).

**Der Widerspruch aus C6 an den Zeilen des Basis-SHA:**

- `moegliche_starke` schließt vier Wahrheiten aus:
  `tools/eq-copilot/pruefe_p4_korpus.py:91-92`.
- `_passt` schließt genau eine aus: `:127-139`, entscheidende Zeile `:139`.
- `falsche_starke` liest `_passt`: `:85`; die Gates hängen daran: `:177`,
  `:190`.
- `_passt` speist **auch** Brier (`:100-102`) und Kalibrierung (`:104-106`).

**Die Falsifikation, gemessen (Abschnitt 3 des Rohlaufs):**

| Lauf | `falsche_starke` | Gate | Nebenbefund |
|---|---:|---|---|
| Korpus wie committet, heutiges `_passt` | 0 | grün | — |
| `gate6_incomparable_never_gets_a_strong_winner` auf `stark`, heutiges `_passt` | **0** | **grün** | **`recall = 1,0667`** — eine Quote über 1, auf die kein Riegel reagiert |
| derselbe Fall unter R1 | 1 | rot | — |
| alle sechs `vergleich`-Fälle auf `stark`, heutiges `_passt` | 1 | rot | `precision = 0,83`, `recall = 1,0000` — der Recall bleibt hier gerade unter 1 |

Der bestehende Rotbeweis `docs/beweise/roh/SONDE-013-rot-M-79.txt` trifft
`unknown_time_path_can_never_reach_strong` mit `wahrheit: unbekannt` — genau
den einen Wert, den `_passt` heute fängt. Er erzeugt Vertrauen, das er nicht
deckt.

**Die Nebenwirkung von R1, die den Entscheid E1 auslöst** (Abschnitt 3,
zweiter Block): mit der erweiterten Menge und sonst unverändertem Korpus wird

    falsche_schwache = 2  (distributed_pair_never_produces_transfer_function,
                           insert_probe_is_never_called_master_sum)
    Brier der Klasse vergleich = 0,3912   (heute 0,1413)
    Brier gesamt               = 0,0468   (heute 0,0174) - bleibt unter dem Gate

Ein Prüfer, der nur `falsche_starke` riegelt, meldete also **grün** und
druckte gleichzeitig zwei falsche Behauptungen und einen Klassen-Brier
schlechter als ein Münzwurf. Deshalb bekommt `falsche_schwache` einen Riegel
und die zwei Fälle ihre richtige Wahrheit (E1).

**Kein Testartefakt.** Für `tools/eq-copilot/pruefe_p4_korpus.py` gibt es am
Basis-SHA keinen Test: die einzigen Verweise auf das Skript stehen im Runner
(`tools/beweise.ps1:577`), im Manifest und in Beweisdateien.

### 3.3 Die 22 Fallnamen an der Quelle

`_bein_bekannt` (`pruefe_p4_korpus.py:62-74`) sucht das **Bein** im Runner;
das Feld `fall` wird nirgends geprüft. `baue()`
(`erzeuge_p4_korpus.py:159-174`) validiert Feldmenge, Aussagewert und
Konfidenzbereich — nicht den Namen.

Nachgeprüft am Basis-SHA, Bauform je Datei:

| Datei | Bauform der Fallnamen |
|---|---|
| `Sonde013StereoGoldenTest.cpp` | `abschnitt ("M-08  mono_identity")` — 13 von 16 Abschnitten mit snake_case, drei reine Prosa (`:495`, `:692`, `:733`) |
| `Sonde013DynamicsTest.cpp` | 10 von 12 mit snake_case |
| `Sonde013PrePostGoldenTest.cpp` | alle drei mit snake_case (`:273`, `:410`, `:507`) |
| `Sonde013FingerprintGoldenTest.cpp` | 4 von 5 mit snake_case |
| `Sonde013TruePeakGoldenTest.cpp` | **alle sechs reine Prosa** |
| `Sonde013EventWireTest.cpp` | Buchstabenabschnitte plus NAK-181-Bezeichner |
| `AnalysisGoldenTestMain.cpp` | `grenzfall ("G1 Drop (kFlagLueckeDavor)", …)`; snake_case nur in einzelnen `pruefe`-Texten, u. a. `short_impulse_triggers_peak_path_only` (`:2123`, `:2181`) |
| `LoudnessGoldenTestMain.cpp` | Buchstabenabschnitte als `std::cout`, Fallnamen als deutsche Prosa in `vergleiche(name, …)` |
| `QueueStressTestMain.cpp` | Buchstabenabschnitte A bis T als `std::cout` |
| `broker/tests/*.rs` | durchgehend `fn`-Namen |

**Stand der 22:** 8 stehen wörtlich, 14 nicht (Tabelle im Quellencheck C7, am
Basis-SHA nachgeprüft). Wörtlich vorhanden sind
`fingerprint_window_never_crosses_epoch_boundary`
(`Sonde013FingerprintGoldenTest.cpp:276`),
`epoch_change_invalidates_open_evidence` (`sonde013_taint.rs:106`),
`nonlinear_chain_never_yields_static_eq_claim`
(`Sonde013PrePostGoldenTest.cpp:273`),
`distributed_pair_never_produces_transfer_function`
(`sonde013_prepost.rs:479`), `gate6_incomparable_never_gets_a_strong_winner`
(`sonde013_experiment.rs:576`),
`comparability_needs_all_five_and_hash_alone_never_wins`
(`sonde013_passage.rs:60`), `unknown_time_path_can_never_reach_strong`
(`sonde013_prepost.rs:546`) und `mono_identity`
(`Sonde013StereoGoldenTest.cpp:160`).

**Die substanzlose Zeile, bestätigt.** `Sonde013DynamicsTest.cpp` führt zwölf
Abschnitte (`:159` bis `:675`); keiner davon misst LUFS-I gegen eine Referenz.
Die ±0,1-LU-Prüfung steht in `LoudnessGoldenTestMain.cpp`: Konstante
`kToleranz = 0.1` (`:164`), Prüfzeile
`pruefe (d <= kToleranz, name + ": LUFS-I innerhalb ±0,1 LU", …)` (`:217`),
gefahren über sechs Korpora (`:279` konstante Pegel, `:290` Rampe, Rauschen
und eine Stunde, `:309` Stille unter dem absoluten Gate, `:531` Oberband,
zweimal Naht bei `:559` und `:566`). Der Kopfkommentar `:6-12` benennt die
Referenz als die bis 23.08.2026 ausgebaute Vektorrechnung — **nicht** als
externes Testset.

Sachäquivalente der übrigen dreizehn stehen in §2.3 als Zielspalte.

### 3.4 B19 und die Blockgrößen

- Der Speiser ist fest:
  `struct Speiser { double sr { 48000.0 }; int frames { 512 }; … }`,
  `Sonde013StereoGoldenTest.cpp:74-78`. Die einzige Variation im ganzen Bein
  ist `s.sr = 22050.0` bei `:607` für die Nyquist-Kappe.
- `bisEvidenz` (`:121-130`) läuft **blockgezählt** (`maxBloecke = 900`), nicht
  samplegezählt. Bei Blockgröße 1 wären 900 Blöcke 900 Samples — kein
  Evidenzfenster. **Der Sweep muss die Schranke auf eine Samplezahl
  umrechnen** (`900 * 512 / frames`, mindestens 900); sonst schweigt der Fall
  bei kleinen Größen und meldete trotzdem grün, sobald der Rückgabewert nicht
  geprüft wird. Das ist die erste Falle des Sweeps und steht als eigene
  Erwartung in N-15 bis N-20 (Rückgabe `true` je Lauf).
- Die Bein-Behauptung von B19 (`tools/beweise.ps1:657`) verspricht die
  Blockgrößen **nicht** — nur die Matrixzeile M-82 tut es. Nach dem Bau nennt
  sie die Menge (R7).
- Quellen der Menge: `StampedAudioQueue.h:240`
  (`GenStrom = StreamLayout<1, 131072, 2048, 16384>`, also
  `maxBlockFrames = 16384`), `QueueStressTestMain.cpp:666-667` (18 Größen),
  `eq-ipc-v3.schema.json:160` (`block_size` 1 bis 65536). Die engste wirksame
  Grenze für die Analyse ist 16384.

### 3.5 B5 und die Zeitlage

- `short_impulse_triggers_peak_path_only` steht bei
  `AnalysisGoldenTestMain.cpp:2123` (Kommentar) und `:2181` (Prüftext). Der
  Fall misst die **Qualitätsbits** (`qualitaetPeak` ohne `qualitaetFluss`),
  nicht die Zeit — es gibt heute keine Zeitprüfung für ein Ereignis.
- Er läuft bei genau einer Blockgröße und einer Samplerate: Speiser
  `AnalysisGoldenTestMain.cpp:114-118` (`sr { 48000.0 }`, `frames { 512 }`),
  Impulsabstand 37 Blöcke (`:2158`, `:2167`).
- Die Zeitstelle des Ereignisses: `Ereignis.stromSample`
  (`FeatureEngine.h:296`), gesetzt bei `:2818` aus `s.fensterStromStart`. Dazu
  `projektSample` und `projektzeitGesetzt` (`:297-298`), `epoche` und
  `segment` (`:301-302`), `dauerMs` als Fensterhop (`:306`, gerechnet
  `:2830`).
- `fensterStromStart` wird **nur bei leerem Ring** aus
  `block.stromVon + sampleOffset` gesetzt (`:2482`) und danach je vollem
  Fenster um `s.hop` fortgeschrieben (`:2553`). `schiebeStufe` wird je Sample
  gerufen; das Fensterraster ist damit sampleverankert und blockgrößenblind.
- Fenstermaße: `kBassPunkte = 16384`, `kHauptPunkte = 4096`,
  `kBassHop = 8192`, `kHauptHop = 2048` (`:927-932`) — in **Samples**, also
  über Sampleraten dasselbe Raster.
- **Der Gegenbefund, der die Zeile bindet:** die Rahmenkadenz ist **nicht**
  sampleexakt. `nimmBlock` prüft `liveSamples / sr >= kLiveIntervallS`
  (`:1221-1223`, `kLiveIntervallS = 0.1` bei `:979`) am **Blockende**; der
  Rahmen fällt beim ersten Block nach 0,1 s. Der Peakpfad hängt daran
  (`peakEreignisImRahmen`, `vorigerRahmenPeak`, `:2803-2808`, `:3669`), der
  Flusspfad nicht. Deshalb misst der Sweep über den Fluss (E5).
- Die Phasenfalle ist im Bein bereits beschrieben und dreimal aufgetreten:
  `:2152-2162`.
- B4 fährt 18 Blockgrößen, aber für den **Passthrough**, nicht für die
  Zeitlage: `QueueStressTestMain.cpp:655-700`, Prüfzeile `:692`.
- B17 fährt fünf Sampleraten, aber für True Peak:
  `Sonde013TruePeakGoldenTest.cpp:176-200`.

### 3.6 M-85, der Distraktor und die geschlossenen Mengen

- Der B24-Fall führt **dasselbe Quellsignal** durch fünf Ketten:
  `Sonde013PrePostGoldenTest.cpp:127-272`, Quelle `:143-155`. Kausal und
  zeitvariabel — kein zweites, nur korreliertes Signal ohne kausale
  Verbindung.
- Der Begriff „Distraktor" kommt am Basis-SHA in keinem Code, Test oder
  Fixture vor, ausschließlich in Prosa (Entwurf, Bauaufteilung, SONDE-013).
- Der Entscheid des Dirigenten (G4 §8, 05.09.2026) stellt Ziel 1 als
  P5-Lieferumfang zu NAK-190. Die P4-Fassung ist die Korpus-Ehrlichkeit plus
  „kein P4-Datenweg benennt einen sicheren Auslöser".
- Die fünf geschlossenen Mengen sind aus dem Schema gelesen (Werte in §2.0 E6)
  und tragen **keinen** kausalen Bezeichner. `kettenbefund` ist die schärfste
  Probe: seine vier nichtstationären Werte enden alle auf `_markiert`
  (`zeitvariabel_markiert`, `pegelabhaengig_markiert`,
  `latenz_wechselt_markiert`) — eine Markierung, keine Ursache.
- Der Gen-Empfänger setzt dieselben Mengen seit NAK-181 durch:
  `eq-copilot/plugin/src/SourcesModel.cpp:631-635` (`urteil`), `:684-695`
  (`klasse`, `kettenbefund`). Das Schema bleibt die einzige Quelle, die A26
  liest — zwei Leser derselben Menge wären zwei Wahrheiten.


### 3.7 Matrixnacharbeit 1 — die bandweise Zahl verlässt den Store nicht

Befund zu MP1-3, an der Quelle geprüft. MN1-3 verlangt, dass die Rust-Hälfte
`Resultatmessung::band_delta_db[b]` je Band mit Bit gegen G misst, „gelesen VOR
der Zusammenfassung … kein Produktcode, kein neues Feld". **Diesen Zugang gibt
es am Basis-SHA nicht.**

| Weg | Stelle | Warum er nicht trägt |
|---|---|---|
| `Verdrahtung::resultatmessung` | `broker/src/coordinator/experiment_verdrahtung.rs:787` | `pub(super)` — von `broker/tests/` aus nicht aufrufbar |
| `Coordinator::experiment_export` | `experiment_verdrahtung.rs:1744-1753`, Struktur `experiment.rs:1241-1248` | `Export` trägt `Experiment`, `Passage`, Evidence-IDs und Ereignisse — **keine** Banddeltas |
| `Coordinator::experiment_sicht` → `Terminal::Ergebnis { achsen }` | `experiment_verdrahtung.rs:1715`, `experiment.rs:542-551` | `Achsenrechnung` ist die **Zusammenfassung**: Intervall, Zählungen, Guardrails — keine Bandachse |
| Sitzungssnapshot | `broker/src/coordinator/sicht.rs:352` | hält ausdrücklich fest: „Rohdeltas und die Evidence-IDs bleiben im Store" |
| `Resultatmessung` in `sonde013_verdrahtung.rs:816`, `:2487`; `sonde013_experiment.rs:27-57` | Testquelle | **von Hand gebaut**, nicht die aus dem Produktpfad — misst `achsen()` als reine Funktion, nicht die Kette |

Die Struktur `Resultatmessung` und ihre Felder sind `pub`; unerreichbar ist
allein der **Bauweg** aus dem Evidenzbestand. Ein bandweiser Test bräuchte
deshalb einen neuen, lesenden Zugang im Broker — etwa
`pub fn resultatmessung_fuer_pruefung(&self, experiment_id, session)` oder eine
Sichtstruktur mit den Banddeltas. Das ist **Produktcode unter `broker/src/`**
und nach der Ticketgrenze ohne Entscheid des Dirigenten ausgeschlossen.

**Was ohne Produktcode messbar bleibt** (N-22 a1 bis a3 und b, §2.2): das
zweitgrößte Banddelta dem Betrag nach über `guardrail_geschuetzt_db`
(`experiment_verdrahtung.rs:1073-1088`), die Zahl der bewegten Bänder über
`signifikante_baender` gegen `gescannte_baender` (p-Werte je Band aus
`bootstrap_p`, `experiment.rs:1377-1410`) und der Mittelwert über
`intervall`. Der Gegenfall aus der Prüfung — Banddeltas 2 und 4 dB bei G = 3 —
**fällt** damit an a1 (zweitgrößtes Delta 2, Abweichung 1 dB). Die
verbleibende Lücke ist genau ein Band und steht in §2.5.

**Zur Entscheidung des Dirigenten:** (i) die Hülle aus a1 bis a3 und b
genügt, oder (ii) der Broker bekommt einen lesenden Zugang, und N-22 misst
`band_delta_db[b]` je Band. Die Matrix schlägt nichts still vor und fällt
nicht auf den Mittelwert zurück.


**Matrixnacharbeit 2 — der Befund bleibt, die Folgerung ändert sich: der
Dirigent hat die lesende Testauskunft entschieden.**

Der Befund oben ist bestätigt und bleibt gültig: am Basis-SHA gibt es keinen
Weg, `Resultatmessung` aus dem Produktpfad zu lesen. Der Entscheid nach MN1-3
ist gefallen — **Weg (ii)**, der Broker bekommt eine lesende Auskunft. Der
Grund steht in MP2-2: die Hülle aus a1 bis a3 und b lässt nicht ein Band
durch, sondern beliebig viele, und eine Zusage mit unbekannt großer Lücke ist
keine.

**Die kleinste Form, an der Quelle bestimmt.** Die naheliegende Variante —
`resultatmessung` von `pub(super)` auf `pub` heben
(`experiment_verdrahtung.rs:787`) — reicht **nicht**: der zweite Parameter ist
`&SessionKey`, und `SessionKey` ist `pub(super)`
(`broker/src/coordinator/zustand.rs:36`). Ein `pub fn` mit einem von außen
nicht nennbaren Parametertyp bleibt aus `broker/tests/` unaufrufbar. Es
braucht deshalb die delegierende Form:

    pub fn resultatmessung_fuer_test(&self, experiment_id: &str,
                                     adresse: &Adresse) -> Resultatmessung

Sie leitet weiter auf
`self.resultatmessung(experiment_id, &ClientKey::aus_adresse(adresse).session())`
— dieselbe Ableitung wie der Produktpfad (`experiment_verdrahtung.rs:213-216`).

**Was dafür trägt und was nicht angefasst wird:**

| Prüfung | Ergebnis |
|---|---|
| Ist die Funktion von außen erreichbar? | ja — sie sitzt auf `impl Coordinator`, und `Coordinator` ist `pub` (`mod.rs:166`); dieselbe Bauform wie `experiment_sicht` (`:1715`) und `experiment_export` (`:1744`) im selben privaten Modul |
| Ist `Resultatmessung` im Test nennbar? | ja — `pub struct` (`experiment.rs:233`) in `pub mod experiment` (`mod.rs:40`) |
| Ist `Adresse` im Test nennbar? | ja — der Test importiert sie bereits (`sonde013_verdrahtung.rs:16`) |
| Braucht `mod.rs` eine Änderung? | **nein** — kein neuer Re-Export nötig |
| Neues Feld, Schemafeld, Terminaländerung, Verhaltensänderung? | **nein** — die Funktion ruft, was `experiment_manual_result` ohnehin ruft, und gibt zurück, was dort schon gerechnet wird |
| Vorbild im Repo | `JSON_SCHEMA_MINOR_AKTIV_FUER_TEST` (`broker/src/coordinator/mod.rs:61`, Kommentar „nur fuer Beine sichtbar"); auf der C++-Seite NAK-181 mit `fingerprintFenster()` und `stereoAkkuBelegteBaender()` |

`broker/src/coordinator/experiment_verdrahtung.rs` steht ab hier in den
Ticketpfaden im Kopf, ausdrücklich mit dem Zusatz **„nur lesende
Testauskunft"**. Nichts anderes unter `broker/src/` wird angefasst; die
Ticketgrenze bleibt sonst unverändert.

## 4. Bauplan

### 4.1 Reihenfolge

Sieben Etappen. Jede endet mit einem eigenen Commit mit Pathspec und einem
Rotbeweis, wo die Matrix einen nennt. Die Reihenfolge folgt der Abhängigkeit,
nicht der Nummer der Regel: das Werkzeug zuerst, damit jede spätere Etappe
schon vom neuen Riegel gemessen wird.

| # | Etappe | Ticketpfade | Beine | Rotbeweis |
|---:|---|---|---|---|
| B1 | **Prüfer und Erzeuger** (R1a, R2, R6 zweite Hälfte): `KEINE_BEHAUPTUNG` und `KEINE_STARKE` im Erzeuger UND im MANIFEST, Prüfer liest sie von dort; `ERLAUBTE_WAHRHEITEN`, Riegel `falsche_schwache`, `precision <= 1`, `recall <= 1`, `quelle`-Prüfung, Schema-Enum-Lesung, MANIFEST-Feld `nicht_gemessen` samt Druck, `--selbsttest`; dazu bekommt `docs/beweise/roh/NAK-182-ist-stand-rechnung.py` seine R1a-Spalte, damit die Zahlen aus E1b aus dem Repo nachrechenbar sind | `tools/eq-copilot/pruefe_p4_korpus.py`, `tools/eq-copilot/erzeuge_p4_korpus.py`, `docs/beweise/roh/NAK-182-ist-stand-rechnung.py` | A25, A26, **A27** (neu) | die alte `_passt`-Form → Selbsttest fällt (`roh/NAK-182-rot-A27-passt.txt`) |
| B2 | **Bezeichner in die Tests** (R2): neun Stellen bekommen ein snake_case-Wort in ihrer bestehenden Abschnitts- oder Prüfzeile; nichts wird umbenannt | `Sonde013TruePeakGoldenTest.cpp`, `LoudnessGoldenTestMain.cpp`, `Sonde013StereoGoldenTest.cpp`, `AnalysisGoldenTestMain.cpp`, `QueueStressTestMain.cpp`, `SondeNullTestMain.cpp` | B17, B9, B19, B5, B4, A16 | Bezeichner aus einem Test entfernt → A26 rot (`roh/NAK-182-rot-M-81-bezeichner.txt`) |
| B3 | **Korpus** (R1, R2, R6 erste Hälfte): Fallnamen gezogen, `quelle` je Fall, drei Wahrheiten berichtigt, `lufs_integrated_matches_reference` auf B9, `KLASSENBEINE["referenz"]` um B9, Distraktor-Wortlaut raus, `nicht_gemessen` eingetragen; Fixtures neu erzeugt | `tools/eq-copilot/erzeuge_p4_korpus.py`, `eq-copilot/fixtures/p4-korpus/` | A25 `--pruefen`, A26, A27 | erfundener Fallname → A26 rot (`roh/NAK-182-rot-M-79-fallname.txt`); Gate-6-Fall auf `stark` → A26 rot (`roh/NAK-182-rot-M-79-unvergleichbar.txt`) |
| B4 | **Stereo-Sweep** (R3): sechs neue Bezeichner, Blockgrößenmenge einmal als `constexpr`, Frameszahl je Blockgröße geprüft | `Sonde013StereoGoldenTest.cpp` | B19 | Speiser versetzt einen Kanal bei Blockgröße ungleich 512 um ein Sample (`roh/NAK-182-rot-M-82.txt`) |
| B5 | **Impuls-Sweep** (R5): fünf Blockgrößen × fünf Sampleraten, `stromSample` identisch je Rate | `AnalysisGoldenTestMain.cpp` | B5 | Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um einen Block (`roh/NAK-182-rot-M-84.txt`) |
| B6 | **0,01-dB-Kette** (R4): Fixture aus dem Erzeuger, C++-Hälfte in B16, Rust-Hälfte in `sonde013_verdrahtung.rs` | `tools/eq-copilot/erzeuge_v3_fixtures.py`, `eq-copilot/fixtures/v3/`, `Sonde013EventWireTest.cpp`, `broker/tests/sonde013_verdrahtung.rs`, Korpus | A8 `--pruefen`, A5, B16, A4, A25, A26 | Dekoderskala 10 statt 100 → Rust-Hälfte fällt (`roh/NAK-182-rot-M-83-skala.txt`); Encoder auf `q_db_0p1_i16` → C++-Schrittprüfung fällt (`roh/NAK-182-rot-M-83-aufloesung.txt`) |
| B7 | **Bein-Behauptungen** (R7) und Manifestabschnitte | `tools/beweise.ps1`, `docs/beweise/NAK-182.md`, `docs/beweise/roh/NAK-182-*` | voller Kanon, abgekoppelt | — |

`docs/beweise/SONDE-013.md` wird **nicht** angefasst (R7); den Nachtrag zieht
der Dirigent am Ticketende. Ebenso unangetastet: `docs/beweise/G4.md`,
`NAK-180.md`, `NAK-181.md`, `docs/offene-punkte.md`, `docs/PLAN-STAND.md`,
`docs/plan/`.


**Matrixnacharbeit 1 — vier Bauetappen nachgezogen.** Die Tabelle oben bleibt
stehen; **bindend sind die folgenden Zeilen.**

| # | Was sich ändert | Grund |
|---:|---|---|
| B1 | zusätzlich: die Riegelauswertung wird **eine eigene Funktion** `_riegel(kennzahlen, klasse) -> list[str]`; A26 ruft sie auf die echten Kennzahlen jeder Klasse und auf die Gesamtzahlen, A27 speist ihr synthetische Kennzahl-Dicts direkt ein | MP1-6: über den Korpusweg sind `recall > 1` und `precision > 1` nach R1a strukturell unerreichbar (`pruefe_p4_korpus.py:85-94`) — die Wachen hätten sonst keinen ausführbaren Negativtest |
| B5 | der Rotbeweis verschiebt den Impuls um **4096 Samples** statt „um einen Block"; der Impuls sitzt bei `t0 = k · 2048 + 1536`; die Zeile misst (a) exakte Gleichheit und (b) `< 4096` | MP1-1 und MP1-2 |
| B6 | Registrierung der Fixture **nur** in der Dateiliste `alle` des Erzeugers (`erzeuge_v3_fixtures.py:2929-2937`); die Waisenwache (`:2942-2957`) bleibt unangetastet; **A5 entfällt aus den Beinen dieser Etappe**; die C++-Hälfte misst eine **20-Stufen-Leiter** statt eines Pegelpaars; die Rust-Hälfte misst a1 bis a3 und b (§2.2 N-22) | MP1-3, MP1-4, MP1-5 |
| B7 | die Bein-Behauptung von B19 nennt in beiden Zweigen die gefahrene Blockgrößenmenge einzeln, nie „über alle" | MP1-7 |

**Vor B6 liegt eine Entscheidung des Dirigenten** (§3.7): entweder genügt die
Hülle aus a1 bis a3 und b, oder der Broker bekommt einen lesenden Zugang auf
`Resultatmessung::band_delta_db` — Letzteres wäre Produktcode unter
`broker/src/` und damit außerhalb der heutigen Ticketgrenze.


**Matrixnacharbeit 2 — zwei Bauetappen nachgezogen.** Die Tabellen oben
bleiben stehen; **bindend sind die folgenden Zeilen.**

| # | Was sich ändert | Grund |
|---:|---|---|
| B5 | der Impuls liegt bei `t0 = 100 · 2048 + 1536 = 206336` (hinter der Aufwärmphase der Flusshistorie, `FeatureEngine.h:994`, `:2766`) auf ruhigem, **nicht stillem** Boden (`flussAus` verlangt `mad > 0.0`, `:2810`); gemessen wird **`ereignis(0)`** — das erste Ereignis des Laufs, ohne Bezug auf t0; dazu die Wachen E-a bis E-d aus N-26 | MP2-1: die Auswahl „größtes `stromSample <= t0`" hätte nach der Verschiebung um 4096 **beide** Impulsfenster verworfen und am Rotbeweis vorbeigemessen |
| B6 | zusätzlich: **eine lesende Testauskunft im Broker** — `pub fn resultatmessung_fuer_test(&self, experiment_id: &str, adresse: &Adresse) -> Resultatmessung` an `impl Coordinator` in `experiment_verdrahtung.rs`, die nur auf `resultatmessung` delegiert. Kein neues Feld, kein Schemafeld, keine Terminaländerung, keine Verhaltensänderung, keine Änderung an `mod.rs`. N-22 (a) misst danach **bandweise an der Produktmessung** | MP2-2: die Hülle aus a1 bis a3 und b lässt beliebig viele Bänder durch, nicht eines — der Entscheid nach MN1-3 ist Weg (ii) |

**Reihenfolge innerhalb B6:** erst die lesende Auskunft, dann der Rust-Test —
sonst misst der Test gegen eine Funktion, die es noch nicht gibt. Die Auskunft
ist der einzige Eingriff unter `broker/src/` in diesem Ticket und steht als
solcher in den Ticketpfaden.

### 4.2 Die Rotbeweise — was jeder trifft, und woran er *nicht* fällt

Jeder Rotbeweis wird einzeln gefahren, seine Rohausgabe liegt unter
`docs/beweise/roh/NAK-182-rot-<kennung>.txt`, und die Änderung wird danach
zurückgenommen. Die Spalte „fällt nicht an" ist der Teil, der ihn von einem
Nebeneffekt trennt.

| Kennung | Manipulation | Fällt an | Fällt **nicht** an |
|---|---|---|---|
| `A27-passt` | `_passt` auf die alte Einwertform zurückgesetzt | A27 S-01b/c/d und S-02b/c/d | A26 auf dem korrigierten Korpus (dort gibt es keinen Fall mehr, der die Lücke trifft) — genau deshalb braucht es den Selbsttest |
| `M-79-unvergleichbar` | `gate6_incomparable_never_gets_a_strong_winner` auf `aussage: stark` | A26: `falsche_starke = 1`, Exit 2 | A25 (der Erzeuger baut die Datei bytegleich; der Fall ist inhaltlich falsch, nicht formal) |
| `M-79-nicht-kausal` | `nonlinear_chain_never_yields_static_eq_claim` auf `wahrheit: nicht_kausal`, `aussage: stark` | A26: Exit 2 | B24 selbst — der Test misst weiter dasselbe |
| `M-79-fallname` | ein `fall` im Korpus auf einen erfundenen Namen | A26: Exit 2, Text nennt Datei und Bezeichner | A25 |
| `M-81-bezeichner` | `lufs_integrated_matches_reference` aus `LoudnessGoldenTestMain.cpp:217` entfernt | A26: Exit 2 | B9 selbst — die Messung bleibt grün, nur ihr Name fehlt. Das ist der Punkt von R2 |
| `M-82` | Test-Speiser versetzt bei Blockgröße ungleich 512 einen Kanal um ein Sample | B19: `sweep_mono_identity`, `sweep_identical_stereo`, `sweep_known_delay` fallen bei 1, 333, 2048, 16384 | dieselben drei bei 512; `sweep_uncorrelated_channels` überhaupt nicht (dort ist die Korrelation ohnehin niedrig) — der Beweis, dass der Sweep die Blockgröße unterscheidet |
| `M-83-skala` | `perzentil_dekodieren`-Teiler im Testaufbau auf 10 statt 100 | Rust-Hälfte: der wiedergefundene Gain liegt um Faktor 10 daneben, ±0,1 dB fällt | C++-Hälfte |
| `M-83-aufloesung` | Encoder im Testaufbau auf `q_db_0p1_i16` | C++-Hälfte: die Schrittprüfung (0,01 dB → genau 1 Ganzzahl) fällt | die Gain-Wiederfindung selbst kann bei ±0,1 dB noch halten — deshalb misst die Zeile die **Auflösung getrennt** (R4) |
| `M-84` | Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um einen Block | B5: die Gleichheit von `stromSample` fällt **genau** bei 1, 333, 2048, 16384 | derselbe Lauf bei 512; und der bestehende `short_impulse_triggers_peak_path_only` |
| `M-85-ausloeser` | ein Enumwert im Schema um `ausloeser` erweitert | A26: Exit 2 | A5 (der Vertragsriegel prüft Form, nicht Wortwahl) |


**Etappe 1b (Entscheid des Dirigenten) — drei Rotbeweise dazu, einer
umformuliert.** Die Tabelle oben gilt weiter; die folgenden Zeilen kommen aus
R1a (E1b) und der Form B (E4b). Die Spalte „gemessen" trägt das Ergebnis der
Rechnung unter R1a am committeten Korpus — der Rotbeweis selbst wird in
Etappe 2 als Rohausgabe gefahren.

| Kennung | Manipulation | Fällt an | Fällt **nicht** an | gemessen (R1a, im Speicher) |
|---|---|---|---|---|
| `M-85-gate7` | `insert_probe_is_never_called_master_sum` auf `aussage: stark` | A26: `falsche_starke = 1`, Exit 2 — **Gate 7 im Prüfer** | derselbe Fall als `schwach` (er trägt die Wahrheit; `nicht_exakt` steht nur in `KEINE_STARKE`) | rot: `fs = 1`, `fw = 0`. Gegenprobe `schwach`: grün, `fs = 0`, `fw = 0` |
| `M-85-verteilt` | `distributed_pair_never_produces_transfer_function` auf `aussage: stark` | A26: `falsche_starke = 1`, Exit 2 — ein Frequenzgang aus einem verteilten Paar | derselbe Fall als `schwach` (Banddifferenzen sind die Wahrheit) | rot: `fs = 1`, `fw = 0`. Gegenprobe `schwach`: grün |
| `M-83-schwach` | `nonlinear_chain_never_yields_static_eq_claim` mit `wahrheit: zeitvariabel` auf `aussage: schwach` | A26: `falsche_schwache = 1`, Exit 2 — **M-83 Satz 3**: auch eine schwache statische EQ-Behauptung ist falsch | B24 selbst; und die zwei Zeilen darüber (`nicht_kausal`/`nicht_exakt` als `schwach` bleiben grün) | rot: `fs = 0`, `fw = 1` |
| `M-83-aufloesung` (umformuliert, E4b) | Encoder im Testaufbau auf `q_db_0p1_i16` | die **Schrittzeile** von N-21: 0,01 · 10 = 0,1, `std::round` → 0, beide Pegel tragen dieselbe Ganzzahl statt zwei um 1 verschiedene | die **±0,1-dB-Zeile** — zwei Rundungen zu je höchstens ±0,05 dB lassen die Differenz gerade noch innerhalb der Toleranz. Genau deshalb misst N-21 die Auflösung getrennt | — (Etappe 2) |


**Matrixnacharbeit 1 — drei Rotbeweise neu gefasst.** Die Tabellen oben
bleiben stehen; **bindend sind die folgenden Zeilen.**

| Kennung | Manipulation | Fällt an — und zwar zuerst | Fällt **nicht** an |
|---|---|---|---|
| `M-84` (neu gefasst, MP1-2) | der Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um **4096 Samples** (eine ganze Fensterlänge, `kHauptPunkte`) | **N-26 (a)**, die Gleichheitszeile: der Stempel wandert bei jeder Blockgröße um genau 4096, die fünf Werte sind nicht mehr identisch | derselbe Lauf bei 512; der bestehende `short_impulse_triggers_peak_path_only`. **N-26 (b)** fällt im selben Lauf mit, ist aber nicht die zuerst fallende Zeile. Die frühere Fassung („um einen Block") wäre bei Blockgröße 1 und 333 im selben Fenster geblieben und **grün** — deshalb 4096 |
| `M-83-aufloesung` (neu gefasst, MP1-4) | Encoder im Testaufbau auf `q_db_0p1_i16` | **die Leiterzeile von N-21**: 20 Pegel im Abstand 0,01 dB spannen 0,19 dB, bei Skalierung 10 also höchstens **3** verschiedene Ganzzahlen statt 20 streng monotoner | die **Gainzeile** von N-21 — zwei Rundungen zu je höchstens ±0,05 dB lassen die Differenz gerade noch innerhalb ±0,1 dB. Sie **darf** im selben Lauf fallen, ist aber nicht die gewertete Zeile. Ein einzelnes Pegelpaar wäre hier rundungsphasenabhängig gewesen (`BandGrid.h:155` rundet die absoluten Pegel getrennt) — deshalb die Leiter |
| `M-83-skala` (neu gefasst, MP1-3) | Dekoderskala 10 statt 100 im Testaufbau | **N-22 a1**: `guardrail_geschuetzt_db` wird 0,30 statt 3,00 dB, Abweichung 2,70 dB > 0,1 | die C++-Hälfte. **N-22 b** (`intervall`) fällt mit, ist aber nicht die zuerst fallende Zeile — und sie allein hätte den Fall „Banddeltas 2 und 4 dB bei G = 3" durchgelassen, weil `experiment.rs:327-341` jede Fensterzeile über die Bänder mittelt |


**Matrixnacharbeit 2 — zwei Rotbeweise neu gefasst.** Die Tabellen oben
bleiben stehen; **bindend sind die folgenden Zeilen.**

| Kennung | Manipulation | Fällt an — und zwar zuerst | Fällt **nicht** an |
|---|---|---|---|
| `M-84` (neu gefasst, MP2-1) | der Speiser verschiebt den Impuls bei Blockgröße ungleich 512 um **4096 Samples** | **N-26 (a)**: `ereignis(0).stromSample` liegt bei den vier verschobenen Läufen um 4096 höher als bei 512 — die fünf Werte sind nicht mehr identisch | derselbe Lauf bei 512; der bestehende `short_impulse_triggers_peak_path_only`. **N-26 (b)** fällt in den verschobenen Läufen mit **negativer** Differenz mit, ist aber nicht die gewertete Zeile. Mit der alten Auswahl „größtes `stromSample <= t0`" hätte der Beweis **gar nicht am Impuls** gemessen: beide verschobenen Fenster liegen hinter t0 und wären verworfen worden |
| `M-83-skala` (neu gefasst, MP2-2) | Dekoderskala 10 statt 100 im Testaufbau | **N-22 (a)**, bandweise: jedes Band mit Bit trägt 0,30 statt 3,00 dB, Abweichung 2,70 dB > 0,1 | die C++-Hälfte. **N-22 (b)** und die Zusätze a1 bis a3 fallen mit, sind aber nicht die gewertete Zeile. Die frühere Fassung wertete a1; die trägt den Fall „4 dB, 2 dB, sonst 3 dB" **nicht**, weil sie nur den zweitgrößten Betrag bindet |

### 4.3 Beine, Laufzeiten und die Reihenfolge des Kanons

Zu fahren in Etappe 2, je Etappe die betroffenen zuerst, am Ende der volle
Kanon abgekoppelt:

| Bein | Ziel | erwartete Laufzeit | Grund der Erwartung |
|---|---|---|---|
| A25 | `erzeuge_p4_korpus.py --pruefen` | < 1 s | reines JSON, heute gemessen |
| A26 | `pruefe_p4_korpus.py` | < 2 s | plus Textsuche in bis zu zehn Testdateien und eine Schemalesung |
| **A27** | `pruefe_p4_korpus.py --selbsttest` | < 1 s | rein synthetisch, kein Repo-Fixture |
| B19 | `EqCopSonde013StereoGoldenTest` | **Ziel < 60 s** (§2.1) | heute 57 Prüfungen; der Sweep legt 30 Läufe dazu |
| B5 | `EqCopAnalysisGoldenTest` | heute plus die 25 Sweepläufe | die FFT-Arbeit hängt an Samples, nicht an Blöcken |
| B16 | `EqCopSonde013EventWireTest` | heute plus zwei Engineläufe | PRE und POST |
| B24, B18, B17, B9, B4, A16, `EqCopNullTest` | unverändert | Regressionswache | keine Änderung an ihnen außer Bezeichnern |
| A4 | `cargo test` für `sonde013_prepost`, `sonde013_verdrahtung`, `sonde013_experiment`, `sonde013_passage`, `sonde013_taint`, `contract_cross_language` | heute plus ein Wiretest | ein Coordinator-Harnisch je Test |
| A5, A8 | `pruefe_v3_vertrag.py`, `erzeuge_v3_fixtures.py --pruefen` | < 5 s | wegen der neuen Fixture (R4) |

Danach der volle Kanon abgekoppelt nach dem Wortlaut des Auftrags; fertig,
wenn die letzte Logzeile mit `EXIT=` beginnt. Kein Stand mit Laufstatus
`NOT RUN` wird übergeben.


**Matrixnacharbeit 1 — die Beinzeile für die Fixture berichtigt (MP1-5).** Die
Tabelle oben bleibt stehen; **bindend ist die folgende Zeile.**

| Bein | Ziel | erwartete Laufzeit | Grund der Erwartung |
|---|---|---|---|
| A8 | `erzeuge_v3_fixtures.py --pruefen` | < 5 s | wegen der neuen Fixture (R4). **A5 entfällt hier**: `pruefe_v3_vertrag.py:449` öffnet nur `evidenz-zahlen-wire-v1.json` und wird nicht erweitert; die zwei Verbraucher der neuen Datei sind B16 (C++) und A4 (Rust), damit ist M-70 erfüllt |

A5 bleibt selbstverständlich Teil des vollen Kanons am Ticketende — es ist nur
kein Bein **dieser** Bauetappe und wird von diesem Ticket nicht angefasst.

### 4.4 Abweichungen vom Ticketauftrag — benannt, nicht stillschweigend

1. **R1 trifft zwei bestehende schwache Fälle, und die Matrix berichtigt ihre
   Wahrheit statt ihre Aussage.** Der Auftrag nennt nur die starke Seite als
   Riegel; die Messung (§3.2) zeigt `falsche_schwache = 2` und einen
   Klassen-Brier von 0,3912. Entscheid E1.
2. **`falsche_schwache` wird ein Riegel.** Der Auftrag verlangt ihn nicht
   ausdrücklich; ohne ihn bliebe R1s Satz „eine starke ODER schwache Aussage …
   ist eine falsche Behauptung" ohne Wirkung.
3. **`precision <= 1.0` kommt dazu**, nicht nur `recall <= 1.0`. Dieselbe
   Bauform, dieselbe Kennzahlklasse; einen der beiden zu riegeln und den
   anderen nicht wäre willkürlich.
4. **Der Selbsttest wird ein eigenes Bein A27**, nicht ein Vorlauf in A26. Der
   Auftrag stellt beides zur Wahl. Begründung in §2.4.
5. **B19 fährt fünf statt achtzehn Blockgrößen** (E3), mit dem Rückweg auf
   alle achtzehn, falls das gemessene Budget es trägt. Der Auftrag erlaubt die
   Teilmenge ausdrücklich, verlangt aber ihre Begründung — sie steht in E3.
6. **Die Obergrenze der Menge kommt aus dem Layout (16384), nicht aus B4s
   Liste.** Der Auftrag nennt B4 als Quelle; B4s größter Wert **ist** die
   Slotkapazität, aber die Zahl gehört `StampedAudioQueue.h:240`. Die engere
   Quelle gewinnt (§3.4).
7. **R5 misst schärfer als „innerhalb eines Feature-Hops".** Der Auftrag lässt
   das ausdrücklich zu, falls die Zeitlage blockgrößenunabhängig sampleexakt
   ist. Sie ist es (§3.5), und die Zeile sagt es.
8. **R4 findet den Pfad, statt ihn zu vermissen.** Der Auftrag rechnet damit,
   dass es keinen Ende-zu-Ende-Weg gibt („Gibt es keinen solchen Pfad …"). Es
   gibt ihn — im **Experimentpfad**, nicht im Paarurteil (§3.1). Damit
   entfallen die drei getrennten Ersatzzeilen; gemessen wird die eine Kette.
9. **Zwei neue Korpusfälle statt einem für R4.** Die Kette hat zwei Hälften in
   zwei Sprachen und damit zwei Beine (B16 und A4); ein Fall müsste eines der
   beiden verschweigen.


**Etappe 1b (Entscheid des Dirigenten) — Nr. 1 und Nr. 2 neu gefasst, Nr. 10
neu. Nr. 3 bis 9 sind unverändert angenommen.**

1. **(neu gefasst)** **R1 trifft zwei bestehende schwache Fälle — und die
   Folgerung der Erstfassung war zu grob.** Die Messung (§3.2) steht: unter der
   Einmengenform werden `falsche_schwache = 2` und der Klassen-Brier 0,3912.
   Die Erstfassung schloss daraus auf eine Umbenennung der beiden Wahrheiten;
   der Dirigent hat das abgelehnt, weil `insertprobe` außerhalb der unsicheren
   Menge Gate 7 im Prüfer wieder blind machte. **R1a** trennt statt dessen
   `KEINE_BEHAUPTUNG` von `KEINE_STARKE`; die zwei Fälle behalten
   `nicht_kausal` und `nicht_exakt` und bleiben `schwach`. Unter R1a am
   committeten Korpus: `falsche_starke = 0`, `falsche_schwache = 0`, Brier
   0,0174 — kein Sprung (E1b).
2. **(neu gefasst)** **`falsche_schwache` wird ein Riegel — und er misst jetzt
   eine eigene Zusage.** Unter der Einmengenform wäre er nur die zweite Hälfte
   desselben Satzes gewesen. Unter R1a trägt er M-83 Satz 3 („keine falsche
   statische EQ-Behauptung", auch keine schwache), Gate 6 und Exit-Gate Satz 2,
   und er hat mit `M-83-schwach` einen eigenen Rotbeweis, der an keiner anderen
   Zeile fällt (§4.2).
10. **(neu)** **Die Kopplung der 0,01-dB-Kette nimmt Form B: die Fixture hält
    Zahlen, nicht den Encoderausgang.** Der Auftrag ließ die Wahl offen und
    band Form A an einen Determinismusbeleg. Den kann die Matrix nicht führen —
    der Verteilungsring speichert `float` (`FeatureEngine.h:2649`),
    `quantisiere16` rundet halbe Werte von null weg (`BandGrid.h:148-163`), und
    das Repo sagt Bitgleichheit nirgends über Binärstände hinweg zu. Gemessen
    wird deshalb die **Differenz** POST minus PRE gegen die deklarierten Zahlen
    der Fixture (E4b, N-21, N-23).


**Matrixnacharbeit 1 — Nr. 5 neu gefasst, Nr. 11 bis 14 neu.** Die Nummern 1
bis 4 und 6 bis 10 gelten unverändert.

5. **(neu gefasst)** **B19 fährt fünf Blockgrößen, nicht alle — und die
   Bein-Behauptung sagt das in jedem Zweig.** Der Rückweg auf achtzehn bleibt,
   aber der Wortlaut wechselt nie auf „alle unterstützten Blockgrößen": auch
   die achtzehn sind eine Auswahl aus 1 bis 16384, und 256 bliebe ungemessen
   (MP1-7). §2.5 hält das als Nicht-Zusage fest.
11. **(neu)** **Die Nähe-Schranke der Impulszeit ist 4096, nicht 2048.** Die
    Erstfassung leitete sie aus dem Hop ab; der Stempel ist aber der
    Fensteranfang, und die Fensterlänge ist 4096
    (`FeatureEngine.h:929-932`, `:2818`). Ein Impuls in der zweiten
    Fensterhälfte hätte die Zeile fallen lassen, obwohl das Produkt richtig
    meldet (MP1-1). Die Zeile trennt jetzt (a) exakte Gleichheit von (b) Nähe
    und behauptet die Auslösung nicht schärfer als „erstes oder zweites
    enthaltendes Fenster".
12. **(neu)** **Der M-84-Rotbeweis verschiebt um 4096 Samples statt um einen
    Block.** Bei Blockgröße 1 oder 333 wäre die Verschiebung kleiner als der
    Hop geblieben und die Gleichheitszeile trotz Manipulation grün (MP1-2).
13. **(neu)** **Die Auflösung wird als 20-Stufen-Leiter gemessen, nicht als
    Pegelpaar.** `BandGrid.h:155` rundet die absoluten Pegel getrennt; ein
    einzelnes Paar kann auch bei `q_db_0p1_i16` einen Unterschied von genau 1
    zeigen, und der Rotlauf wäre am ausgeschlossenen Nebeneffekt gescheitert
    (MP1-4).
14. **(neu)** **„A5 liest mit" entfällt, und die bandweise Gain-Prüfung ist
    heute nicht möglich.** Ersteres, weil `pruefe_v3_vertrag.py:449` nur eine
    Datei öffnet und A5 außerhalb der Ticketpfade liegt (MP1-5); Letzteres,
    weil `Resultatmessung` den Store nicht verlässt (§3.7, MP1-3). Für den
    zweiten Punkt liegt die Entscheidung beim Dirigenten; die Matrix misst
    solange die benannte Hülle und führt die Restlücke in §2.5.


**Matrixnacharbeit 2 — Nr. 14 neu gefasst, Nr. 15 neu.** Alle übrigen Nummern
gelten unverändert.

14. **(neu gefasst)** **„A5 liest mit" entfällt — und die bandweise
    Gain-Prüfung ist jetzt möglich.** Der erste Teil bleibt: `pruefe_v3_vertrag.py:449`
    öffnet nur eine Datei, A5 liegt außerhalb der Ticketpfade (MP1-5). Der
    zweite Teil ist **überholt**: der Dirigent hat nach MN1-3 den Weg (ii)
    entschieden — der Broker bekommt eine lesende Testauskunft, und N-22 (a)
    misst bandweise an der Produktmessung (MN2-2, §3.7). Die in §2.5
    ausgewiesene Restlücke entfällt damit; sie war zudem mit „genau ein Band"
    **falsch beziffert** (MP2-2).
15. **(neu)** **Die Ticketgrenze bekommt genau eine Ausnahme: eine lesende
    Testauskunft unter `broker/src/`.** Der Ticketauftrag schloss Produktcode
    unter `broker/src/` „ohne Entscheid des Dirigenten nach der
    Matrixprüfung" aus — der Entscheid liegt jetzt vor.
    `resultatmessung_fuer_test` delegiert ausschließlich und ändert kein
    Verhalten; `broker/src/coordinator/experiment_verdrahtung.rs` steht mit
    dem Zusatz „nur lesende Testauskunft" in den Ticketpfaden. Ebenfalls neu:
    **die Ereignisauswahl in N-26 darf die Sollzeit nicht kennen** — ein
    Filter, der t0 benutzt, kann eine Zeitabweichung nicht messen (MP2-1).

### 4.5 Selbstaudit vor jedem Commit

Vor jedem Commit gelesen, mit dem Diff vor Augen:

- **Zahlenränder:** jede Toleranz steht als Zahl im Test, nicht als Prosa;
  Blockgröße 1 und 16384 werden wirklich gefahren; die Sampleraten wirklich
  gewechselt (`vorbereiten(fs)` je Lauf, nicht einmal am Anfang).
- **NaN/Inf:** der Sweep prüft `std::isfinite` auf jeder gemessenen Größe;
  eine Blockgröße, bei der eine Größe nicht endlich ist, ist rot, nicht
  „übersprungen".
- **Kein Fall besteht durch Materialmangel:** je Lauf wird geprüft, dass ein
  Evidenzfenster wirklich entstand und dass die Zahl der gemittelten Frames
  innerhalb ±20 % des Medians über die Menge liegt (§3.4).
- **Bezeichner wörtlich statt ähnlich:** nach B2 und B3 läuft A26 und meldet
  jeden Fall, dessen Wort nicht in seiner Datei steht.
- **Rotbeweis fällt an der Zusage:** Spalte „fällt nicht an" in §4.2 wird je
  Beweis wirklich gefahren, nicht nur behauptet.
- **Bein-Behauptungen und Korpus-Hinweise sagen die Wahrheit:** B19 sagt „über
  die Menge 1, 333, 512, 2048, 16384", nie „über alle"; der B9-Fall sagt
  „gegen die ausgebaute Vektorrechnung", nie „gegen ein Standardtestset"; der
  B24-Fall sagt „zeitvariabel", nie „nicht kausal".
- **Fixtures bytegleich:** A25 und A8 `--pruefen` laufen nach jeder
  Korpus- und Fixtureänderung.
- **Kein Produktcode, kein Schemafeld, keine Umbenennung:**
  `git diff --stat` gegen die Ticketpfade; kein Treffer unter
  `eq-copilot/plugin/src`, `core`, `sonde`, `vertrag`, `state`, `broker/src`,
  `eq-copilot/schemas/`.
- **Stack:** die neuen Sweeps legen jede `FeatureEngine` auf den Heap
  (`std::unique_ptr`) — Register NAK-175, und der Sweep hält bis zu fünf
  gleichzeitig.
- **Save/Load-Symmetrie und Gegenpaare:** nicht berührt (kein persistenter
  Zustand in diesem Ticket).

### 4.6 Prüfliste `tools/dirigent/pruefliste.md` — Stand Etappe 1

| Abschnitt | Berührt? | Wo gemessen bzw. warum nicht |
|---|---|---|
| A Rückstau und Prioritätsklassen | nein | keine Queue-, IPC- oder Politikänderung |
| B Lebenszyklus | nein | kein Verbinden/Trennen, kein Thread, kein Join |
| C Verträge und Längen | **lesend** | A26 liest fünf geschlossene Mengen aus `eq-ipc-v3.schema.json` (N-38) und weist eine fehlende Menge ab; das Schema selbst bleibt unverändert. NaN/Inf an jeder neu gemessenen Zahl: §4.5 |
| D Bau- und Prüfriegel | **ja** | A26 und A27 sind fail-closed: unbekannte Wahrheit, fehlende Menge, Pfad ins Leere und erfundener Bezeichner sind rot, nicht „unbekannt"; A27 ist ein eigenes Bein mit eigener Behauptung (§2.4) |
| E Behauptung ≤ Messung | **ja** | R7: B19 nennt die Blockgrößenmenge, der B9-Fall nennt seine Referenz, der B24-Fall nennt „zeitvariabel"; die drei nicht gedeckten Zusagen stehen in §2.5. Jede neue Prüfung hat ihren Rotbeweis (§4.2) |
| F Änderungssatz | **ja** | Erzeuger, Prüfer, Fixtures und Cross-Language-Verbraucher der 0,01-dB-Kette liegen in einer Etappe (B6); Bezeichner und Korpusfälle in B2/B3, sodass A26 nach jeder Etappe grün ist |

Der Stand nach Etappe 2 steht in §6.

### 4.7 Nebenbefunde aus Etappe 1 (nicht ins Register — der Dirigent zieht nach)

1. **`Dreifachergebnis` trägt kein Intervall** (`prepost.rs:294-313`). Der
   PRE/POST-Paarpfad kann eine Gainaussage mit Unsicherheit nicht
   ausdrücken; die Unsicherheit lebt nur im Experimentpfad
   (`Achsenrechnung.intervall`). Für M-83 Satz 2 ist das kein Mangel (E4),
   für eine spätere Sicht auf ein Paarurteil könnte es einer werden.
2. **M-81 „Standard-Testmaterial" ist für LUFS-I nicht erfüllt.** B9 misst
   gegen die ausgebaute Vektorrechnung (`LoudnessGoldenTestMain.cpp:6-12`),
   nicht gegen ein externes Set. Für True Peak ist es erfüllt (EBU Tech 3341,
   B17). Die Matrixzeile M-81 sagt beides in einem Satz; die Trennung gehört
   in den Nachtrag.
3. **`falsche_schwache` wurde seit dem Bau des Korpus gerechnet und nie
   ausgewertet** (`pruefe_p4_korpus.py:86-87`). Eine Kennzahl ohne Leser ist
   dieselbe Bauform wie ein toter UI-Knopf.
4. **Der Gesamt-Brier verdeckt Klassen-Brier.** Die Klasse `vergleich` liegt
   heute bei 0,1413 und läge unter der **Einmengenform R1** ohne Korrektur bei
   0,3912, während der Gesamtwert bei 0,0468 bliebe (unter **R1a** tritt der
   Fall nicht ein, §5.1 — der Nebenbefund über den fehlenden Klassenriegel
   bleibt davon unberührt). Ein Klassen-Riegel wäre schärfer als der
   Gesamtriegel; er ist in diesem Ticket **nicht** eingeführt, weil das Gate
   den Gesamtwert nennt (§49.4) und eine Verschärfung ohne Entscheid des
   Dirigenten eine neue Zusage wäre.
5. **`bisEvidenz` zählt Blöcke, nicht Samples** (`Sonde013StereoGoldenTest.cpp:121-122`).
   Bei kleinen Blockgrößen schweigt das Bein, ohne es zu melden, sobald der
   Rückgabewert nicht geprüft wird. Der Sweep prüft ihn (§3.4); die
   bestehenden Abschnitte tun es bereits.
6. **Die Wirefixture des Coordinator-Harnischs ist handgeschrieben** und trägt
   `q_db_0p1_i16` in `verteilung`
   (`evidence-snapshot-mit-ereignissen-und-stereo.json`), während der echte
   Encoder dort `q_db_0p01_i16` schreibt (`FeatureEngine.h:3930`). Die
   Rust-Verdrahtungstests messen damit eine Kodierung, die das Produkt an
   dieser Stelle nicht sendet. NAK-182 legt die eigene Fixture daneben, ersetzt
   die alte aber nicht (das wäre ein Nebenrefactor).

## 5. Matrixprüfungen

*(Etappe 1 abgeschlossen 2026-09-06. Der Dirigent trägt hier „Matrixprüfung 1"
ein.)*

### 5.1 Etappe 1b — Entscheid des Dirigenten, eingearbeitet (2026-09-06)

Auftrag: `docs/beweise/roh/NAK-182-etappe-1-entscheid-auftrag.txt`. Kein
Bau, kein Produktcode; geändert wurde ausschließlich dieses Manifest.

| Punkt | Entscheid | Wo eingearbeitet |
|---|---|---|
| 1 (E1) | `falsche_schwache`, `precision <= 1,0`, `recall <= 1,0` als Riegel; A27 als eigenes Bein; B24 trägt `zeitvariabel` — **angenommen**. Die zwei Umbenennungen `nicht_kausal` → `verteilt` und `nicht_exakt` → `insertprobe` — **abgelehnt**. R1 wird zu **R1a**: zwei geschlossene Mengen `KEINE_BEHAUPTUNG` und `KEINE_STARKE`, im Erzeuger geführt, im MANIFEST mitgeführt, vom Prüfer von dort gelesen | E1b (§2.0), §2.1, §2.2 (N-02 bis N-05), §2.3, §2.4 (S-01, S-02), §4.1 (B1), §4.2, §4.4 Nr. 1 und 2 |
| 2 (E4) | Pfad, Zweiteilung und Fixture-Kopplung — **angenommen**. Der Widerspruch Nr. 1 gegen Nr. 3 wird zugunsten von **Form B** aufgelöst: die Fixture hält Zahlen, nicht den Encoderausgang; die C++-Hälfte misst die Differenz POST minus PRE gegen die deklarierten Zahlen | E4b (§2.0), §2.2 (N-21, N-23), §4.2, §4.4 Nr. 10 |
| — | E2, E3, E5, E6 und §4.4 Nr. 3 bis 9 — **angenommen ohne Änderung** | unverändert |

**Gemessen unter R1a am committeten Korpus** (Basis-SHA `c4290fb`, Rechnung
wie §3.2, mit den drei `_passt`-Zeilen aus E1b): `falsche_starke` 0,
`falsche_schwache` 0, Brier 0,0174, Kalibrierung 0,0671, Coverage 0,77 —
kein Sprung gegenüber dem heutigen Lauf. Die fünf Rotbeweise fallen unter
R1a, die zwei Gegenproben bleiben grün (§4.2). Die R1a-Spalte kommt in
Etappe 2 (B1) in `docs/beweise/roh/NAK-182-ist-stand-rechnung.py`, damit die
Zahlen aus dem Repo nachrechenbar sind statt nur hier zu stehen.

### 5.2 Matrixprüfung 1 — NEEDS_WORK (2026-09-06, Dirigent)

Prüfer: Codex `gpt-6-astra`, Effort max, read-only, elevated Sandbox,
abgekoppelt über `tools/dirigent/codex-lauf.ps1` (Kennung `nak182-mp1`,
08:37–08:57), Thread `01a0756f-5b7f-7153-97fc-a93a38558f7b`. Auftrag nach
Vorlage: `docs/beweise/roh/NAK-182-matrixpruefung-1-auftrag.txt`; Prüfbereich
`git diff c4290fb...5f7ce1e -- docs/beweise/NAK-182.md` (Etappe 1 samt 1b);
HEAD vor und nach dem Lauf `5f7ce1e`. Rohurteil:
`docs/beweise/roh/NAK-182-matrixpruefung-1-5f7ce1e.txt`.

**Urteil: NEEDS_WORK — sieben Defekte, alle vom Dirigenten an der Quelle
bestätigt; keine Lücke, keine Härtung.**

| # | Zeile | Befund (Kurz) | Quelle, geprüft | Einordnung |
|---|---|---|---|---|
| MP1-1 | E5/N-26 | Nähe `t0 - stromSample < 2048` lehnt korrektes Verhalten ab: das Ereignis stempelt den Fensteranfang, das Fenster ist 4096 lang (Hop 2048) | `FeatureEngine.h:927-932` (`kHauptPunkte` 4096, `kHauptHop` 2048), `:2818` (`e.stromSample = s.fensterStromStart`) | **Defekt** → MN1-1 |
| MP1-2 | N-26 Rotbeweis | Verschiebung um einen Block (1 bzw. 333 Samples) bleibt im selben Fenster, der Anker ändert sich nicht — der Rotbeweis bliebe grün | dieselben Zeilen | **Defekt** → MN1-2 |
| MP1-3 | N-22/E4 | `achsen.intervall` bootstrappt die Fenster**mittel** über die Bänder; die bandweise ±0,1-dB-Zusage ist in der Rust-Hälfte nicht gemessen | `broker/src/coordinator/experiment.rs:327-341` | **Defekt** → MN1-3 |
| MP1-4 | E4b/N-21 | Der Encoder rundet absolute Pegel getrennt; ein Paar im Abstand 0,01 dB kann auch mit Skala 10 um genau 1 differieren — der Auflösungs-Rotbeweis fiele am Nebeneffekt, nicht an der Zusage | `BandGrid.h:155` (`std::round (db * skalierung)`) | **Defekt** → MN1-4 |
| MP1-5 | N-23/B6 | „A5 liest mit" ist mit dem Änderungssatz nicht umgesetzt: A5 öffnet nur `evidenz-zahlen-wire-v1.json`, und A5 liegt nicht in den Ticketpfaden | `tools/eq-copilot/pruefe_v3_vertrag.py:449` | **Defekt** → MN1-5 (Behauptung entfällt; Kopplung = A8 + zwei Verbraucher) |
| MP1-6 | S-03/S-04 | Unter R1a ist `recall <= 1` und `precision <= 1` auf dem Korpusweg unerreichbar rot — die Quotenriegel haben keinen ausführbaren Negativtest | `pruefe_p4_korpus.py:85-94` (`richtige_starke = len(stark) - len(falsche_starke)`, Teilmenge von `moegliche_starke`) | **Defekt** → MN1-6 |
| MP1-7 | E3/N-15–N-20 | Der 18er-Zweig soll „über alle unterstützten Blockgrößen" behaupten, fährt aber 18 von 16384 Werten | `QueueStressTestMain.cpp:666-667`, Regel R3 | **Defekt** → MN1-7 |

Nacharbeit: `docs/beweise/roh/NAK-182-matrixnacharbeit-1-auftrag.txt`
(Regeln MN1-1 bis MN1-7, wörtliche Befunde; Runde 1 von 3 des
Etappe-1-Budgets), derselbe Worker per Fortsetzung. Danach Matrixprüfung 2
als Wiederprüfung nach Vorlage B über `5f7ce1e...HEAD -- docs/beweise/NAK-182.md`.

### 5.4 Matrixprüfung 2 — NEEDS_WORK (2026-09-06, Dirigent)

Prüfer: Codex `gpt-6-astra`, Effort max, read-only, abgekoppelt (Kennung
`nak182-mp2`, 09:15–09:26), Thread `01a07592-1ca5-7a91-8c32-0100ac19dceb`.
Auftrag nach Vorlage B: `docs/beweise/roh/NAK-182-matrixpruefung-2-auftrag.txt`;
Prüfbereich `git diff 5f7ce1e...fad2939 -- docs/beweise/NAK-182.md`
(Matrixnacharbeit 1); HEAD vor und nach dem Lauf `fad2939`. Rohurteil:
`docs/beweise/roh/NAK-182-matrixpruefung-2-fad2939.txt`.

**Urteil: NEEDS_WORK — fünf von sieben Befunden geschlossen, zwei offen; kein
neuer Bruch.**

| # | Stand | Beleg des Prüfers | Einordnung des Dirigenten |
|---|---|---|---|
| MP1-1 | geschlossen | N-26: Fensterlänge 4096, Anker und Ereignis (`FeatureEngine.h:929-932`, `:2482`, `:2553`, `:2818`) | — |
| MP1-2 | **offen** → MP2-1 | Auswahl „größtes `stromSample <= t0`" verwirft nach der 4096-Verschiebung beide möglichen Anker (`t0 + 512`, `t0 + 2560`); der Rotbeweis beobachtet den verschobenen Impuls nicht | **Defekt** (Zeile misst ihren eigenen Rotbeweis nicht) → MN2-1: Ereigniszuordnung ohne t0 — genau ein Flussereignis je Lauf, Ereigniszahl ist eigene Erwartung |
| MP1-3 | **offen** → MP2-2 | `guardrail_geschuetzt_db` ist der zweitgrößte Betrag (`experiment_verdrahtung.rs:1073-1088`); Bänder 2 und 4 dB bei G = 3 bestehen alle vier Erwartungen; `resultatmessung` ist `pub(super)` (`:787`), der Test baut `Resultatmessung` von Hand (`sonde013_verdrahtung.rs:815-824`) | **Defekt** (bandweise ±0,1 dB nicht am Produkt gemessen; die vom Worker erbetene Entscheidung nach MN1-3) → MN2-2: lesende Testauskunft im Broker (Sichtbarkeit von `resultatmessung` oder delegierende `_fuer_test`-Funktion, kein Feld, kein Schema, keine Verhaltensänderung; Vorbild NAK-181), `experiment_verdrahtung.rs` (ggf. `mod.rs`) als Ticketpfad „nur lesende Testauskunft" |
| MP1-4 | geschlossen | N-21 Leiter statt Paar (`BandGrid.h:139-155`) | — |
| MP1-5 | geschlossen | N-23/B6: Registrierung und Waisenwache (`erzeuge_v3_fixtures.py:2929-2968`), A5-Leser entfällt (`pruefe_v3_vertrag.py:449`, `:1433-1461`) | — |
| MP1-6 | geschlossen | S-03/S-04/B1: direkte Kennzahleinspeisung (`pruefe_p4_korpus.py:85-94`, `:189-198`) | — |
| MP1-7 | geschlossen | E3/N-15–N-20: Menge benannt (`QueueStressTestMain.cpp:666-667`, `StampedAudioQueue.h:240`) | — |

Nacharbeit: `docs/beweise/roh/NAK-182-matrixnacharbeit-2-auftrag.txt`
(Regeln MN2-1, MN2-2; Runde 2 von 3), derselbe Worker per Fortsetzung. Danach
Matrixprüfung 3 nach Vorlage B über `fad2939...HEAD -- docs/beweise/NAK-182.md`;
bleibt ein Defekt, Konvergenzentscheid nach der dritten Runde.

### 5.6 Matrixprüfung 3 — PASS (2026-09-06, Dirigent)

Prüfer: Codex `gpt-6-astra`, Effort max, read-only, abgekoppelt (Kennung
`nak182-mp3`, 09:38–09:46), Thread `01a075a7-7f27-7882-9c7b-529d4ebe41a4`.
Auftrag nach Vorlage B: `docs/beweise/roh/NAK-182-matrixpruefung-3-auftrag.txt`;
Prüfbereich `git diff fad2939...7a5755c -- docs/beweise/NAK-182.md`
(Matrixnacharbeit 2); HEAD vor und nach dem Lauf `7a5755c`. Rohurteil:
`docs/beweise/roh/NAK-182-matrixpruefung-3-7a5755c.txt`.

**Urteil: PASS — MP2-1 und MP2-2 geschlossen, nichts gebrochen.** Beleg des
Prüfers: N-26 wählt `ereignis(0)` ohne t0-Filter, verlangt 1 bis 63
Ereignisse, das Flussbit und keinen früheren Anker als `t0 - 4096`, danach
exakte Gleichheit und die Nähe-Schranke (`FeatureEngine.h:929-932`,
`:1380-1385`, `:2482`, `:2553`, `:2684-2689`, `:2810-2836`, `:2861-2867`);
N-22 misst bandweise am Produkt nach Kandidat und Terminal, der Zugang
delegiert mit derselben Sessionableitung auf `resultatmessung`
(`experiment_verdrahtung.rs:213-224`, `:787-803`, `:936-960`; `mod.rs:40`,
`:166`; `experiment.rs:233-236`), der Test erreicht die Verdrahtung über
`Senke::p0` (`sonde013_verdrahtung.rs:334-349`, `:4032-4082`); die falsche
Restlücke ist als überholt markiert; die einzige Produktcodeausnahme (lesende
Testauskunft) ist in §4.4 begründet.

**Etappe 1 konvergiert** nach zwei Nacharbeitsrunden (Budget 3). Kurztabelle:

| Runde | Prüfbereich | Urteil | Defekte |
|---|---|---|---|
| Matrixprüfung 1 | `c4290fb...5f7ce1e` | NEEDS_WORK | 7 (MP1-1 bis MP1-7) |
| Matrixprüfung 2 | `5f7ce1e...fad2939` | NEEDS_WORK | 2 offen (MP2-1, MP2-2) |
| Matrixprüfung 3 | `fad2939...7a5755c` | **PASS** | 0 |

Die Matrix im Stand `7a5755c` (§2 mit den bindenden Fassungen aus
Matrixnacharbeit 1 und 2) ist die Referenz für Etappe 2 und für Erst- und
Wiederprüfung. Etappe 2 beauftragt: `docs/beweise/roh/NAK-182-etappe-2-auftrag.txt`.

### 5.3 Matrixnacharbeit 1 — Defekt, Regel, Zeile (2026-09-06)

Auftrag: `docs/beweise/roh/NAK-182-matrixnacharbeit-1-auftrag.txt`. Urteil der
Prüfung: `docs/beweise/roh/NAK-182-matrixpruefung-1-5f7ce1e.txt`. Kein Bau,
kein Produktcode; geändert wurde ausschließlich dieses Manifest. Nur diese
sieben Punkte.

| Defekt | Regel des Dirigenten | Wo eingearbeitet |
|---|---|---|
| **MP1-1** — E5/N-26: `t0 - stromSample < 2048` lehnt richtiges Verhalten ab; der Stempel ist der Fensteranfang, die Fensterlänge 4096 | MN1-1: Schranke aus `kHauptPunkte` (4096); Fensterphase `t0 = k · 2048 + 1536` aus der Ankerarithmetik begründet; zwei getrennte Erwartungen (a) exakte Gleichheit, (b) Nähe < 4096; ohne Auslösungsbeleg „erstes oder zweites Fenster" | MN1-Block zu E5 (§2.0), §2.1 (zwei neue Zeilen), §2.2 (N-26 neu gefasst), §4.1 B5, §4.2, §4.4 Nr. 11 |
| **MP1-2** — der M-84-Rotbeweis verschiebt bei Blockgröße 1/333 nur um 1/333 Samples und bleibt im selben Fenster | MN1-2: Verschiebung um `kHauptPunkte` (4096); §4.2 nennt (a) als die zuerst fallende Zeile | MN1-Block zu E5, §2.1, §2.2 (N-26), §4.1 B5, §4.2, §4.4 Nr. 12 |
| **MP1-3** — `achsen.intervall` mittelt über die Bänder; Banddeltas 2 und 4 dB bestehen bei G = 3 | MN1-3: bandweise Erwartung (a) plus Intervall (b); ist `band_delta_db` nicht ohne Produktcode lesbar, Befund in §3 und Entscheid des Dirigenten — kein stiller Rückfall auf den Mittelwert | **§3.7 (Befund: nicht lesbar, fünf Wege geprüft)**, MN1-Block zu E4b (§2.0), §2.2 (N-22 mit a1 bis a3 und b), §2.5 (Restlücke), §4.1 B6, §4.2, §4.4 Nr. 14 |
| **MP1-4** — `BandGrid.h:155` rundet die absoluten Pegel getrennt; ein Pegelpaar besteht auch bei Skalierung 10 | MN1-4: Leiter aus N ≥ 20 Pegeln im Abstand 0,01 dB; benachbarte Ganzzahlen unterscheiden sich um genau 1; Rotbeweis fällt an der Leiterzeile, die Gainzeile bleibt getrennt | MN1-Block zu E4b, §2.1, §2.2 (N-21), §4.1 B6, §4.2, §4.4 Nr. 13 |
| **MP1-5** — A5 liest die neue Fixture nicht; `pruefe_v3_vertrag.py:449` öffnet nur `evidenz-zahlen-wire-v1.json` | MN1-5: „A5 liest mit" entfällt; Kopplung ist A8 plus zwei Verbraucher (B16, A4) nach M-70; Registrierung nur in der Dateiliste des Erzeugers; A5 wird nicht erweitert | MN1-Block zu E4b, §2.2 (N-23), §4.1 B6, §4.3, §4.4 Nr. 14, Ticketpfade unverändert |
| **MP1-6** — S-03/S-04 sind über den Korpusweg nach R1a unerreichbar; `richtige_starke <= len(stark)` | MN1-6: `_riegel(kennzahlen, klasse) -> list[str]` als eigene Funktion; A26 auf echte Kennzahlen, A27 speist synthetische Dicts ein; §2.4 nennt je Fall den Weg | §2.4 (Wegespalte, S-03 bis S-04e), §4.1 B1, §5.3 |
| **MP1-7** — der 18er-Zweig hieße „alle unterstützten Blockgrößen", obwohl z. B. 256 ungemessen bleibt | MN1-7: beide Zweige nennen die gefahrene Menge einzeln, nie „über alle"; §2.5 hält die Nicht-Erschöpfung mit Begründung der Auswahl fest | MN1-Block zu E3 (§2.0), §2.2 (N-15 bis N-20), §2.5, §4.1 B7, §4.4 Nr. 5 |

**Offen für den Dirigenten (nur ein Punkt):** MP1-3 (i) Hülle a1 bis a3 und b
genügt, oder (ii) der Broker bekommt einen lesenden Zugang auf
`Resultatmessung::band_delta_db`. Alles andere ist ohne Rückfrage geschlossen.

### 5.5 Matrixnacharbeit 2 — Befund, Regel, Zeile (2026-09-06)

Auftrag: `docs/beweise/roh/NAK-182-matrixnacharbeit-2-auftrag.txt`. Urteil der
Prüfung: `docs/beweise/roh/NAK-182-matrixpruefung-2-fad2939.txt`. Kein Bau,
kein Produktcode; geändert wurde ausschließlich dieses Manifest. Nur diese
zwei Punkte. Geschlossen aus Runde 1: MP1-1, MP1-4, MP1-5, MP1-6, MP1-7.

| Befund | Regel des Dirigenten | Wo eingearbeitet |
|---|---|---|
| **MP2-1** (= MP1-2, offen) — nach der Verschiebung um 4096 liegen **beide** Impulsanker bei `t0 + 512` und `t0 + 2560`; die Auswahl `stromSample <= t0` verwirft beide, die Gleichheitszeile misst ein älteres Ereignis oder nichts, und die Nähezeile kann nie wegen negativer Differenz fallen | MN2-1: die Ereigniszuordnung kennt t0 nicht. Gemessen wird `ereignis(0)`, das **erste Ereignis des Laufs** (Ringreihenfolge); dazu die Wachen E-a bis E-d. „Genau ein Ereignis" ist aus der Quelle nicht belegbar (zwei überlappende Fenster, adaptive Schwelle) — die Matrix nimmt die belegbare Form | Block Matrixnacharbeit 2 zu E5 (§2.0), §2.2 (N-26 neu gefasst), §4.1 B5, §4.2, §4.4 Nr. 15 |
| **MP2-2** (= MP1-3, offen) — bei 4 dB, 2 dB und sonst 3 dB bestehen Guardrail, Signifikanz, Bandzahl und Intervall, während **zwei** Bänder die ±0,1-dB-Zusage verletzen; die behauptete Restlücke „genau ein Band" war falsch und unterschätzte die Einschränkung | MN2-2 (Entscheid nach MN1-3): der Broker bekommt eine **lesende Testauskunft** `pub fn resultatmessung_fuer_test(&self, experiment_id, adresse) -> Resultatmessung`, die nur delegiert; N-22 (a) misst bandweise an der Produktmessung, (b) das Intervall; a1 bis a3 bleiben als Zusatz. Die falsche Restlücke wird gestrichen | Block Matrixnacharbeit 2 zu E4b (§2.0), §2.2 (N-22 neu gefasst), §2.5 (Restlücke gestrichen, „überholt durch MN2-2"), §3.7 (Entscheid und kleinste Form), §4.1 B6, §4.2, §4.4 Nr. 14 und 15, Ticketpfade im Kopf |

**Was an der Quelle neu geprüft wurde** (Etappe 1b und Runde 1 hatten es nicht
gebraucht):

- `SessionKey` ist `pub(super)` (`broker/src/coordinator/zustand.rs:36`) —
  deshalb reicht ein `pub`-Lift von `resultatmessung` **nicht**, und die
  delegierende Form ist die kleinste, die trägt.
- `Coordinator` ist `pub` (`mod.rs:166`), `Resultatmessung` ist `pub`
  (`experiment.rs:233`) in `pub mod experiment` (`mod.rs:40`), `Adresse`
  importiert der Test schon (`sonde013_verdrahtung.rs:16`) — **`mod.rs`
  braucht keine Änderung.**
- `ereignis(i)` indiziert vom ältesten aus (`FeatureEngine.h:1381-1385`), und
  `ereignisAblegen` überschreibt erst bei vollem Ring (`:2861-2867`,
  `kEreignisPlaetze = 64` bei `:992`) — `ereignis(0)` ist das erste Ereignis
  des Laufs, solange `ereignisAnzahlJetzt() < 64`.
- `flussAus` verlangt `mad > 0.0` (`:2810`), und die Schwelle greift erst bei
  voller Flusshistorie (`kFlussHistorie = 16`, `:994`, `:2766`) — daraus die
  Materialbedingung und die Lage `t0 = 206336`.
- `both_paths_yield_one_event` (`AnalysisGoldenTestMain.cpp:2216-2255`) misst
  „kein Zeitpunkt trägt zwei Ereignisse", **nicht** „ein Ausschlag erzeugt ein
  Ereignis" — deshalb ist die Variante „genau ein Ereignis" nicht belegbar.

**Offen: nichts.** Beide Punkte sind geschlossen; der einzige Punkt, der nach
Runde 1 beim Dirigenten lag (MP1-3), ist mit MN2-2 entschieden.

---


## 6. Etappe 2 — Bau (2026-09-06, Worker)

Referenz war die Matrix im Stand `7a5755c` (Matrixprüfung 3: PASS). Gebaut
wurde in sechs Commits mit Pathspec; die Rohausgaben liegen in
`docs/beweise/roh/NAK-182-beine.txt` (Beine) und
`docs/beweise/roh/NAK-182-rot-*.txt` (zwölf Rotbeweise).

### 6.1 Bauetappen

| # | Commit | Was gebaut | Gemessene Matrixzeilen (Datei :: Bezeichner) | Rotbeweis |
|---|---|---|---|---|
| B2 | `c0f7f8c` | **sieben** Bezeichner in bestehende Tests eingesetzt, nichts umbenannt | N-10 `Sonde013TruePeakGoldenTest.cpp :: true_peak_matches_ebu_test_set`; N-11 `:: eight_times_oversampling_error_bound`; N-12 `LoudnessGoldenTestMain.cpp :: lufs_integrated_matches_reference`; N-27 `AnalysisGoldenTestMain.cpp :: no_window_crosses_an_epoch_boundary`; N-28 `:: drop_counts_as_segment_not_epoch`; N-29 `QueueStressTestMain.cpp :: drop_produces_counter_gap_and_boundary`; N-34 `SondeNullTestMain.cpp :: insert_probe_is_never_called_master_sum` | `M-81-bezeichner` |
| B4 | `46e5b7a` | Blockgrößen-Sweep in B19, sechs neue Bezeichner, Menge einmal als `constexpr` | N-15 bis N-20, `Sonde013StereoGoldenTest.cpp :: sweep_mono_identity`, `:: sweep_identical_stereo`, `:: sweep_polarity_inversion`, `:: sweep_known_delay`, `:: sweep_uncorrelated_channels`, `:: sweep_folddown_within_0p25db` | `M-82` |
| B5 | `20b2b4d` | Impuls-Sweep über Blockgrößen und Sampleraten | N-26, `AnalysisGoldenTestMain.cpp :: impulse_time_is_stable_over_block_sizes_and_sample_rates` | `M-84` |
| B6 | `00fa14e` | Fixture aus A8, C++-Hälfte in B16, Rust-Hälfte in A4, lesende Testauskunft im Broker | N-21 `Sonde013EventWireTest.cpp :: focused_evidence_wire_carries_0p01_db_steps`; N-22 `broker/tests/sonde013_verdrahtung.rs :: dekodierte_evidenz_findet_gain_mit_intervall`; N-23 `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` | `M-83-aufloesung`, `M-83-skala` |
| B1+B3 | `b08c9f7` | Prüfer, Erzeuger, Korpus (25 Fälle), A27 | N-01 bis N-09, N-13, N-14, N-24, N-30 bis N-38 | `M-79-unvergleichbar`, `M-79-fallname`, `M-83-schwach`, `M-85-gate7`, `M-85-verteilt`, `M-85-ausloeser`, `A27-passt` |
| B7 | `c143397` | Bein-Behauptungen für A25, A26, B19, B5, B16; neues Bein **A27** | R7 | — |

### 6.2 Laufzeiten — gemessen, nicht geschätzt

| Bein | Kommando | Laufzeit | Ergebnis |
|---|---|---:|---|
| A25 | `erzeuge_p4_korpus.py --pruefen` | 0,5 s | bytegleich, 5 Dateien, 25 Fälle |
| A26 | `pruefe_p4_korpus.py` | 0,3 s | Exit 0, 0 falsche Behauptungen |
| **A27** | `pruefe_p4_korpus.py --selbsttest` | 0,1 s | 41 Zusagen, 0 gescheitert |
| A8 | `erzeuge_v3_fixtures.py --pruefen` | 1,5 s | 321 Dateien bytegleich |
| A5 | `pruefe_v3_vertrag.py` | 3,4 s | 512 bestanden, 0 gescheitert |
| **B19** | `EqCopSonde013StereoGoldenTest` | **1,9 s** | 91 bestanden, 0 gescheitert |
| **B5** | `EqCopAnalysisGoldenTest` | 5,3 s | 248 bestanden, 0 gescheitert |
| B16 | `EqCopSonde013EventWireTest` | 0,4 s | 149 bestanden, 0 gescheitert |
| B24 / B18 / B17 / B9 / B4 / B22 / B3c / A16 / A1 | Plugin-Beine | 0,1 bis 4,0 s | alle Exit 0 |
| A4 | `cargo test` × 7 | 0,8 bis 16,8 s | `sonde013_verdrahtung` 74/0, `sonde013_prepost` 18/0, `sonde013_experiment` 19/0, `sonde013_passage` 12/0, `sonde013_taint` 14/0, `contract_cross_language` 10/0, `coordinator_model` 51/0 |

**Der Budgetzweig aus MN1-7 ist entschieden — durch Messung.** B19 läuft mit
dem Sweep in **1,9 s**, weit unter den 20 s, die MN1-7 für den 18er-Zweig
verlangt. Der Sweep fährt deshalb **alle 18 Blockgrößen**, die B4 §J führt
(1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192,
12345, 16384). Die Bein-Behauptung nennt sie **einzeln** und sagt ausdrücklich,
dass es eine Auswahl aus 1 bis 16384 ist — nie „über alle"; 256 bleibt
ungemessen (§2.5).

### 6.3 Was der Bau an der Quelle gelernt hat

**Der Blockgrößen-Sweep zählt Blöcke, nicht Samples.** `bisEvidenz` hat eine
Schranke in **Blöcken** (`maxBloecke = 900`). Bei Blockgröße 1 wären das 900
Samples — kein Evidenzfenster, `kam == false`, und ohne Prüfung des
Rückgabewerts hätte der Fall nichts gemessen und trotzdem bestanden. Die
Schranke rechnet jetzt auf dieselbe **Samplezahl** wie bei 512, und „ein
Evidenzfenster ist wirklich entstanden" ist je Blockgröße eine eigene Zusage.

**Zwei Rauschströme aus einem xorshift32 sind nicht unabhängig.** Der erste
Sweep zog `(n · 2654435761) ^ saat` und mischte danach — zwei Saaten ergaben
Bandkorrelation **0,93** statt nahe null, und `sweep_uncorrelated_channels`
fiel zu Recht. Die Saat muss **vor** die Durchmischung; mit einem
splitmix64-Abschluss liegt die Korrelation bei −0,18.

**Der Ereignisdetektor ist auf keinem Boden still (MP2-1, zweite Runde).** Die
Matrix sah „das erste Ereignis des Laufs" vor, mit der Erwartung, der Boden
erzeuge vor dem Impuls nichts. Diese Voraussetzung hält an dieser Engine
nicht, und der Grund ist strukturell: die Schwelle ist `med + 3·MAD` über die
letzten 16 Fenster, also **relativ zur Streuung des Bodens selbst**.

| Boden | Was passiert | Gemessen |
|---|---|---|
| Rauschen (`r0 · 0,16`) | ein stationäres Rauschen überschreitet `med + 3·MAD` erwartungsgemäß in rund einem Prozent der Fenster; vor dem Impuls liegen rund 85 | E-d 10 von 25, erster Stempel 182272 statt 202752 |
| ein reiner Sinus | der Fluss wird zwischen den Fenstern nahezu konstant, MAD fällt gegen null, die Schwelle wird zum Haarauslöser | E-d 10 von 25, erster Stempel 34816 — das **erste bewaffnete** Fenster |
| Schwebung, Periode 32768 Samples | die Historie ist ab dem ersten bewaffneten Fenster repräsentativ | der Boden löst nicht mehr aus |

Die Schwebung allein reicht trotzdem nicht: bei zwei der fünf Sampleraten
blieb ein Frühzünder. Die **Auswahlregel** ist deshalb auf das **stärkste
Flussereignis** des Laufs gezogen — ebenso t0-frei wie „das erste", und
physikalisch begründet: der Impuls liegt rund 26 dB über dem Boden, ein
Bodenereignis überschreitet die Schwelle definitionsgemäß nur knapp. Dass der
Abstand wirklich groß ist, misst eine eigene Zusage: **mindestens das
123,7-fache** des nächststärksten Flussereignisses. Abweichung Nr. 4 unten.

**Die Ankerarithmetik trägt genau, was E5 vorgerechnet hat.** Der gemeldete
`stromSample` ist bei **allen fünf Sampleraten und allen fünf Blockgrößen**
derselbe Wert **202752** = 99 · 2048, und `t0 − stromSample` ist bei jeder
Kombination **3584**. Das ist der Offset des **früheren** der zwei
enthaltenden Fenster — genau die Lage, für die die Zeile „erstes **oder**
zweites Fenster" sagt und die Schranke aus der **Fensterlänge** (4096) und
nicht aus dem Hop (2048) nimmt. Mit der alten Schranke wäre dieser korrekte
Stempel abgelehnt worden.

**Der naheliegende Skalen-Rotbeweis fällt am Nebeneffekt.** „Dekoderskala 10
statt 100" über `encoding: q_db_0p1_i16` zu erzwingen scheitert **vor** dem
Dekoder: die Wirewerte −3000 liegen außerhalb der q0p1-Grenzen
(`telemetrie.rs`), und der Empfänger weist den Snapshot ab — gemessen fiel
„die PRE-Evidenz der Fixture wird angenommen", nicht die gewertete Zeile. Der
Beweis zehntelt deshalb die **Werte**; der Dekoder teilt weiter durch 100 und
liefert 0,30 statt 3,00 dB. Steht im Kopf der Beweisdatei.

### 6.4 Abweichungen vom Bauplan — benannt, nicht stillschweigend

1. **Reihenfolge: B2, B4, B5, B6, dann B1+B3, dann B7** statt B1 → B7. Grund:
   der Erzeuger validiert seit R2, was der Korpus trägt, und der Korpus zeigt
   auf Bezeichner, die erst B2, B4, B5 und B6 anlegen. Die Reihenfolge aus
   §4.1 hätte A26 zwischen zwei Commits **rot** gelassen. B1 und B3 liegen aus
   demselben Grund in **einem** Änderungssatz (Prüfliste F).
2. **Sieben statt neun Bezeichner** (§2.3 sagte neun). `polarity_inversion`
   und `known_delay` brauchen keinen mehr, weil die sechs Stereo-Korpusfälle
   seit E3 auf den Sweep zeigen.
3. **25 statt 24 Korpusfälle.** 22 am Basis-SHA plus die drei neuen sind 25;
   die Zahl „24" in §2.3 war ein Rechenfehler der Etappe 1. Die Klassen sind
   `referenz` 5, `zeitachse` 6, `vergleich` 8, `stereo` 6 — genau wie die
   Tabellen in §2.3 sie führen.
4. **N-26 wählt das stärkste Flussereignis, nicht das erste.** Begründung und
   Messungen in §6.3. Die Zusage der Zeile ist unverändert und t0-frei; nur
   die Auswahlregel ist die belegbare statt der angenommenen. Der Rotbeweis
   fällt weiterhin an (a).
5. **B19 fährt 18 Blockgrößen, B5 fünf.** MN1-7 kennt den 18er-Zweig nur für
   B19, und das gemessene Budget trägt ihn (1,9 s). N-26 bindet für B5
   ausdrücklich „fünf Blockgrößen je Samplerate, 25 Läufe" — dabei bleibt es.
6. **N-18 misst mit 0,25 rad, nicht 0,1 rad.** Die Matrix nannte 0,1 rad; der
   bestehende Abschnitt `bekannte Laufzeit` misst mit **0,25**, weil die Phase
   ein **Band**wert ist und der Träger in einem Band endlicher Breite sitzt.
   Der Sweep nimmt die Zahl der Quelle. Gemessen ist die Abweichung bei allen
   18 Blockgrößen **0,0000 rad**.
7. **Der M-82-Rotbeweis lässt `sweep_known_delay` grün.** §4.2 sagte, er falle
   mit. Ein Sample sind bei 900 Hz 0,1178 rad und damit innerhalb der 0,25 rad
   aus Nr. 6. Gefallen sind `sweep_mono_identity`, `sweep_identical_stereo`
   und `sweep_polarity_inversion` — bei allen 17 Größen ungleich 512 und bei
   512 grün. Der Beweis zeigt also, was er zeigen soll: der Sweep
   unterscheidet Blockgrößen.
8. **Die Klassenriegel bleiben bei den zwei Behauptungszählern.** `_riegel`
   prüft, was im übergebenen Dict steht; je Klasse übergibt A26
   `falsche_starke` und `falsche_schwache`, für das Gesamt alle Kennzahlen.
   Der erste Bau riegelte alles je Klasse und meldete prompt
   `vergleich: Kalibrierung 0,2100 > 0,15` — eine **neue Zusage**, die §49.4
   nicht verlangt und die §4.7 Nr. 4 ausdrücklich einem eigenen Entscheid
   vorbehält.
9. **`M-85-ausloeser` läuft im Speicher.** Das v3-Schema ist eingefrorener
   Vertrag und wird auch für einen Rotbeweis nicht angefasst; die Manipulation
   liegt in den Selbsttestfällen S-08 und S-09, die eine **Kopie** des Schemas
   verändern. Das ist strenger als eine Dateimanipulation, nicht schwächer.
10. **`.gitattributes` bleibt unberührt.** Die neue Fixture liegt unter
    `eq-copilot/fixtures/v3/**`, und die `-text`-Regel dafür steht seit
    NAK-181 (§4.4 Nr. 10 sagte das voraus).

### 6.5 Messbilanz — was von der Matrix jetzt gemessen ist

| Zusage | Zeilen | Stand |
|---|---|---|
| M-79 | N-01 bis N-07 | gemessen: A26 mit Zweimengenlogik, vier neuen Riegeln und A27 als eigenem Bein |
| M-80 | N-08, N-09 | unverändert gemessen: A25 bytegleich, `.gitattributes` unberührt |
| M-81 | N-10 bis N-14 | gemessen; die Teilung „±0,1 LU gegen die ausgebaute Vektorrechnung, Standard-Testmaterial nur für True Peak" steht im `hinweis` des B9-Falls |
| M-82 | N-15 bis N-20 | gemessen über 18 Blockgrößen; die Nicht-Erschöpfung steht in §2.5 und in der Bein-Behauptung |
| M-83 Satz 2 | N-21 bis N-23 | gemessen als **eine** Kette in zwei Hälften, bandweise und mit Intervall |
| M-83 Satz 3 | N-24 | unverändert gemessen; der Korpus trägt jetzt `zeitvariabel` statt `nicht_kausal` |
| M-84 | N-26 bis N-31 | gemessen; die Zeitlage über 25 Kombinationen ist neu |
| M-85 Ziel 2, 3 | N-32 bis N-36 | gemessen; Gate 6 und Gate 7 haben je einen Rotbeweis **im Prüfer** |
| M-85 Ziel 1 | N-37 | **nicht gemessen** — im MANIFEST geführt, von A26 gedruckt, Eigentümer NAK-190 |
| M-85, „kein sicherer Auslöser" | N-38 | gemessen: fünf geschlossene Vertragsmengen, kein kausaler Bezeichner |

### 6.6 Prüfliste `tools/dirigent/pruefliste.md` — Stand Etappe 2

| Abschnitt | Berührt? | Wo gemessen |
|---|---|---|
| A Rückstau und Prioritätsklassen | nein | keine Queue-, IPC- oder Politikänderung |
| B Lebenszyklus | nein | kein Verbinden/Trennen, kein Thread, kein Join |
| C Verträge und Längen | **lesend** | A26 liest fünf geschlossene Mengen aus `eq-ipc-v3.schema.json` und weist eine fehlende ab (A27 S-08); das Schema selbst ist unverändert. NaN/Inf: der Stereo-Sweep prüft Präsenzbits je Band, der Impuls-Sweep die Ereigniszahl, die 0,01-dB-Kette die Bänder mit Bit |
| D Bau- und Prüfriegel | **ja** | A26 und A25 sind fail-closed: unbekannte Wahrheit, fehlende `quelle`, Pfad ins Leere, erfundener Bezeichner und fehlende Vertragsmenge sind rot, nicht „unbekannt". A27 ist ein eigenes Bein mit eigener Behauptung |
| E Behauptung ≤ Messung | **ja** | B19 nennt die 18 Größen einzeln und sagt nie „über alle"; B5 nennt die Auswahlregel; B16 sagt „gegen die Zahlen der Fixture, nicht gegen ihre Bytes"; der B9-Fall nennt seine Referenz. Jede neue Prüfung hat ihren Rotbeweis (zwölf Dateien), und jeder fällt an der gewerteten Zeile — die eine Ausnahme (`M-83-skala` über das `encoding`) ist erkannt, verworfen und im Beweiskopf begründet |
| F Änderungssatz | **ja** | Erzeuger, Prüfer, Korpus und Fixtures liegen je in einem Commit; die 0,01-dB-Kette liegt mit Fixture, beiden Verbrauchern und der lesenden Auskunft in einem |

### 6.7 Nebenbefunde aus Etappe 2 (nicht ins Register — der Dirigent zieht nach)

1. **`bisEvidenz` zählt Blöcke, nicht Samples** (`Sonde013StereoGoldenTest.cpp`).
   Der Sweep rechnet die Schranke um; die bestehenden Abschnitte bei 512 sind
   nicht betroffen, aber jeder künftige Fall mit kleiner Blockgröße läuft in
   dieselbe Falle.
2. **Die adaptive Flussschwelle hat auf jedem stationären Boden eine
   Fehlalarmrate.** `med + 3·MAD` ist relativ zur Streuung des Bodens: ein
   rauschender Boden zündet gelegentlich, ein streuungsarmer macht die
   Schwelle zum Haarauslöser. Für die Zeitmessung ist das umgangen (stärkstes
   Ereignis); für ein Produkt, das Ereignisse **zählt**, wäre es eine Frage.
3. **`guardrail_loudness_db` und `guardrail_peak_db` sind im Produktpfad hart
   `None`** (`experiment_verdrahtung.rs`), weil `evidence_snapshot` weder
   integrierte Lautheit noch True Peak trägt. Das ist bewusst so (NAK-181
   B11), heißt aber: zwei der fünf M-45-Guardrails sind ohne Vertragsteil
   nicht messbar.
4. **Die alte Wirefixture des Coordinator-Harnischs trägt weiterhin
   `q_db_0p1_i16` in `verteilung`**, während der echte Encoder dort
   `q_db_0p01_i16` schreibt. NAK-182 legt die eigene Fixture daneben und
   ersetzt die alte nicht — das wäre ein Nebenrefactor.
5. **`SondeProcessor opfer;` liegt in `SondeNullTestMain.cpp` auf dem Stack.**
   Bestandscode, heute grün; bei der nächsten Feldergänzung ist es ein
   Kandidat für den 1-MiB-Stack (Register NAK-175).

### 6.8 Voller Kanon auf dem Endstand

Abgekoppelt gefahren nach dem Wortlaut des Auftrags (Start ohne Loeschbefehl,
Zusatz des Dirigenten vom 06.09.2026). **GRUEN - 54/54 Kanon-Laeufe**,
Exitcode 0, Arbeitsbaum beim Lauf sauber, Commit `3ca500f`. Der Runner hat
seinen Abschnitt selbst angehaengt (unten) und die Rohausgabe nach
`docs/beweise/roh/NAK-182-3ca500f.md` gelegt.

53 Beine waren es beim Gate-Stand; das 54. ist **A27**, der Selbsttest des
Pruefers. Kein Bein steht auf `NOT RUN`; die zwei `[GEPLANT]`-Zeilen (B6, B7)
und das eine stillgelegte Bein sind der bekannte Bestand und nicht von diesem
Ticket beruehrt.

## 7. Prüfrunden Etappe 2 (Dirigent)

### 7.0 Erstprüfung 1 — NEEDS_WORK (2026-09-06)

Prüfer: Codex `gpt-6-astra`, Effort max, read-only, abgekoppelt (Kennung
`nak182-ep1`, 10:46–10:57), Thread `01a075e5-6643-71f3-a2a3-a3ea78a0fa17`.
Auftrag nach Vorlage A: `docs/beweise/roh/NAK-182-erstpruefung-auftrag.txt`;
Prüfbereich `git diff c4290fb...054883f` über die 15 Ticketpfade (ohne
`docs/**`); HEAD vor und nach dem Lauf `054883f`. Der Prüfer fuhr A26, A27,
A25 `--pruefen` und A8 `--pruefen` lesend (je Exit 0) und las C++-, Rust- und
Kanonbelege aus den Quellen. Rohurteil:
`docs/beweise/roh/NAK-182-erstpruefung-054883f.txt`. Rundenbilanz
`c4290fb..054883f`: Produkt 7 Dateien +410/−24 · Tests 8 Dateien +963/−7 ·
Prüfwerkzeug 4 Dateien +713/−68 · Doku 28 Dateien +11420/−5.

**Urteil: NEEDS_WORK — fünf Defekte, alle vom Dirigenten an der Quelle
bestätigt; keine Lücke, keine Härtung, keine Produktregression.** Alle fünf
sind Tests oder Prüfwerkzeug, das seine Matrixzeile nicht misst.

| # | Zeile | Befund (Kurz) | Quelle, geprüft | Einordnung |
|---|---|---|---|---|
| EP-01 | N-21/E4b | Die C++-Hälfte liest `perzentilDb` aus `FeatureFrame`-Arrays und die Leiter aus `quantisiere16` auf Testzahlen — kein Wire-Text, kein Serializer | `Sonde013EventWireTest.cpp:1659-1660`, `:1697` | **Defekt** → NR-01 (jede Zahl aus dem Wire-Text des echten Serializers) |
| EP-02 | N-18 | Der Laufzeit-Sweep speist drei Träger, wertet nur 900 Hz mit 0,25 rad aus; ein Sample bei 2000 Hz wären 0,26 rad | `Sonde013StereoGoldenTest.cpp:1179-1183`, `:1159-1161` | **Defekt** → NR-02 (alle drei Träger je Blockgröße, eine Toleranzzahl in Test und Zeile) |
| EP-03 | N-19 | `sweep_uncorrelated_channels` prüft Basis, Korrelationsbit und Korrelation, nicht „keine Lag- oder Polaritätsempfehlung" | `Sonde013StereoGoldenTest.cpp:1201-1204` | **Defekt** → NR-03 (Phase nicht gesetzt, keine Polaritätsempfehlung, wie der 512-Abschnitt für M-12) |
| EP-04 | S-07 | Der Selbsttest prüft Mengenmitgliedschaft zweier Literale, erreicht die Ablehnung in `main` nicht | `pruefe_p4_korpus.py:500-504` | **Defekt** → NR-04 (eine Validierungsfunktion für A26 und S-07) |
| EP-05 | N-23/E4b | Die Fixture trägt `aufloesung`, `pegel`, `leiter`, `pre`, `post`, aber kein Material; B16 setzt 1000 Hz, 48 kHz, Amplitude und Laufbegrenzung lokal | `erzeuge_v3_fixtures.py:3002-3008`, `Sonde013EventWireTest.cpp:1629-1637`, Fixture-Schlüssel | **Defekt** → NR-05 (Material als Zahlen in der Fixture, von B16 gelesen) |

Nacharbeit 1: `docs/beweise/roh/NAK-182-etappe-2-nacharbeit-1-auftrag.txt`
(Regeln NR-01 bis NR-05, wörtliche Befunde; Runde 1 von 3 des
Etappe-2-Budgets), frischer Opus-Worker (max), betroffene Beine B16, B19,
A26, A27, A25, A8, A5, A4 `sonde013_verdrahtung`, `EqCopNullTest`; kein voller
Kanon in dieser Runde. Danach Wiederprüfung 1 nach Vorlage B über den Fixdiff
`054883f...HEAD` mit der Befundliste EP-01 bis EP-05.

### 7.1 Nacharbeit 1 — abgeschlossen (2026-09-06, Worker)

Auftrag: `docs/beweise/roh/NAK-182-etappe-2-nacharbeit-1-auftrag.txt` (Regeln
NR-01 bis NR-05, Runde 1 von 3). Basis `6087684`, Quellstand am Ende
`d3d9661`; fünf Commits mit Pathspec — vier für NR-01 bis NR-05 und die
Bein-Behauptungen, der fünfte für einen Fund des Selbstaudits (letzte Zeile der
Prüflistentabelle). **Alle fünf Defekte sind geschlossen,
jeder mit eigenem Rotbeweis.** Kein Produktcode berührt: der Fixdiff liegt
vollständig in zwei Testdateien, zwei Python-Werkzeugen, einer Fixture und den
Bein-Behauptungen des Runners. Kein Bezeichner umbenannt, kein Schemafeld, kein
Neben-Refactor.

| # | Regel | Geänderte Zeile / Test | Rotbeweis (neu gefahren) |
|---|---|---|---|
| EP-01 | NR-01 | `Sonde013EventWireTest.cpp`: neuer Leser `verteilungAusWire` (Encoding-Wort, Ganzzahlen und Präsenzbits aus der base64-Bitmap des serialisierten `verteilung.p50`); das Lauf-Lambda schickt jeden Frame durch `nakama::evidenz::evidenceSnapshotAlsJson` und parst den Text mit `juce::JSON`. **Gainzeile:** dekodiert mit dem Teiler aus der Fixture, je Band mit Bit in **beiden** Wire-Texten. **Leiterzeile:** 20 eigene Engineläufe im Abstand 0,01 dB, Startversatz `leiter.start_db − pegel.pre_band_db` = −0,004 dB; gelesen werden die Ganzzahlen aus den 20 Wire-Texten. `perzentilDb` und `quantisiere16` kommen in diesem Abschnitt nicht mehr vor | `roh/NAK-182-rot-M-83-skala-n1.txt` — Wirewerte gezehntelt: **nur** die Gainzeile fällt (56 Bänder daneben, größte Abweichung 2,7000 dB, Differenzen 0,300 statt 3,000 dB; Exit 1, 152/1). `roh/NAK-182-rot-M-83-aufloesung-n1.txt` — Encoder auf `q_db_0p1_i16` vor der Serialisierung, nur in den Leiterläufen: die vier Leiterzeilen fallen (Wire-Ganzzahlen −326, −326, −325, −325 …; monoton 2/19, Schritt genau 1 2/19, Encoding-Wort 0/20; Exit 1, 149/4), die Gainzeile bleibt grün |
| EP-02 | NR-02 | `Sonde013StereoGoldenTest.cpp`: `fahre` und `klasse` nehmen eine **Trägerliste**; `Lauf` trägt je Träger Frequenz, Präsenzbits und Bandwerte, die flachen Felder sind der erste Träger (die vier anderen Klassen messen unverändert). `sweep_known_delay` prüft **je Träger** Präsenzbit und Phasenfehler gegen `+2·pi·f·tau`; die Toleranz steht als **eine** Zahl `kPhaseToleranzRad = 0,25` im Test (Begründung §6.4 Nr. 6) und ebenso in der neuen Zeile N-18. Signal und Auswertung kommen aus **derselben** Quelle `quellen` (Frequenz + Startphase), eine zweite Liste kann nicht mehr auseinanderlaufen | `roh/NAK-182-rot-M-82-n1.txt` — derselbe Ein-Sample-Versatz wie in `NAK-182-rot-M-82.txt`: `sweep_known_delay` **fällt jetzt** (300 Hz d = 0,0393 rad, 900 Hz d = 0,1178 rad, 2000 Hz d = 0,2618 rad > 0,25) und bleibt bei Blockgröße 512 grün (d = 0,0000 rad je Träger). Vier statt drei fallende Zeilen; Exit 1, 87/4 |
| EP-03 | NR-03 | `Lauf.baenderMitBasis` und `.baenderMitPhase` werden in `fahre` über **alle** 221 Bänder erhoben — dieselben Felder und dieselbe Schwelle wie der 512er-Abschnitt `width_alone_is_never_a_defect` (M-12): ein Band mit Basis, das eine Phase trägt, ist die Lag-/Polaritätsaussage. `sweep_uncorrelated_channels` verlangt je Blockgröße zusätzlich `baenderMitPhase == 0` | `roh/NAK-182-rot-M-82-empfehlung.txt` — **zwei Läufe.** Lauf 1 (neue Prüfung): gemeinsamer 5-kHz-Ton auf beiden Kanälen; die Korrelation im gemessenen 1-kHz-Band bleibt bei −0,1933 (unter 0,2), und die Zeile fällt allein an „1 von 196 mit Phase" (Exit 1, 90/1). Lauf 2 (dieselbe Manipulation, Prädikat **ohne** die Phasenprüfung, also die Fassung vor der Nacharbeit): Exit 0, 91/0 — damit ist gemessen, dass die neue Prüfung den Unterschied macht und nicht ein Nebeneffekt. Vollständig korrelierte Kanäle wären eine Regressionswache gewesen und kein Beleg (Prüfliste E), weil dort schon die Korrelation fällt |
| EP-04 | NR-04 | `pruefe_p4_korpus.py`: neue Funktion `_pruefe_wahrheiten(faelle, mengen) -> list[str]`, dieselbe Bauform wie `_riegel`. `main` ruft sie je Klasse auf den geladenen Korpus (Meldungstext unverändert, nur die Reihenfolge roter Zeilen ändert sich bei rotem Korpus), S-07 ruft sie mit einem synthetischen Fall und mit dessen Gegenteil. Kein zweiter Prüfweg, keine lokale Kopie der Menge | `roh/NAK-182-rot-A27-wahrheit.txt` — `_pruefe_wahrheiten` auf `return []`: S-07 fällt mit „keine rote Zeile" (Exit 2, 41/1), das Gegenteil bleibt grün. Vor der Nacharbeit wäre derselbe Eingriff unsichtbar geblieben |
| EP-05 | NR-05 | `erzeuge_v3_fixtures.py`: neuer Block `material` mit `signal_hz` 1000,0, `samplerate_hz` 48000,0, `amplitude_pre` 0,25, `blockgroesse` 512, `bloecke_hoechstens` 900 und der daraus **gerechneten** `dauer_s_hoechstens` 9,6. B16 liest Frequenz, Samplerate, Amplitude, Blockgröße und Laufbegrenzung von dort; die fünf lokalen Konstanten sind weg. `pre` und `post` bleiben Byte für Byte unverändert, die Rust-Hälfte damit unberührt | `roh/NAK-182-rot-N-23-fixturebyte.txt` — ein Byte im neuen Materialblock geändert (`signal_hz` 1000,0 → 1001,0): A8 `--pruefen` Exit 2, „weicht ab". Der neue Block fällt damit unter dieselbe Wache wie Pegel, Auflösung und die zwei Schnappschüsse |

**Die betroffenen Beine, gemessen auf `d3d9661`** (Rohausgabe
`roh/NAK-182-nacharbeit-1-beine-d3d9661.txt`, Worktree beim Lauf sauber bis auf
diese Rohdatei; Binaries unmittelbar davor neu gebaut):

| Bein | Befehl | Ergebnis | Laufzeit |
|---|---|---|---|
| B16 | `EqCopSonde013EventWireTest.exe` | Exit 0, 153 bestanden, 0 gescheitert | 0,5 s |
| B19 | `EqCopSonde013StereoGoldenTest.exe` | Exit 0, 91 bestanden, 0 gescheitert | 1,9 s |
| A26 | `py -3.13 tools/eq-copilot/pruefe_p4_korpus.py` | Exit 0, vier Klassen bestehen | 0,1 s |
| A27 | `py -3.13 tools/eq-copilot/pruefe_p4_korpus.py --selbsttest` | Exit 0, 42 bestanden, 0 gescheitert | 0,1 s |
| A25 | `py -3.13 tools/eq-copilot/erzeuge_p4_korpus.py --pruefen` | Exit 0, bytegleich, 5 Dateien, 25 Fälle | 0,1 s |
| A8 | `py -3.13 tools/eq-copilot/erzeuge_v3_fixtures.py --pruefen` | Exit 0, 321 Dateien bytegleich | 0,2 s |
| A5 | `py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py --abdeckung` | Exit 0, 512 Prüfungen bestanden, kein `$defs` ohne Negativfixture | 2,5 s |
| A4 | `cargo test --test sonde013_verdrahtung` (Ordner `broker`) | Exit 0, 74 passed, 0 failed | 11,7 s |
| A1/B2 | `EqCopNullTest.exe` | Exit 0, NULLTEST OK | 0,1 s |

**Der 20-fache Enginelauf der Leiter ist nicht zu langsam.** B16 fährt ab jetzt
22 statt 2 Engineläufe und braucht dafür 0,5 s statt 0,4 s — der im Auftrag
erlaubte Ersatzweg (P50-Werte **eines** Laufs versetzt durch den Serialisierer)
war deshalb nicht nötig, und die Leiter misst die Auflösung wirklich erzeugter
Snapshots.

**Die drei Träger tragen auch 0,1 rad** — gemessen wurden über alle 18
Blockgrößen und alle drei Träger durchweg `d = 0,0000 rad`. Die Zeile bleibt
trotzdem bei **0,25 rad**: das ist die Zahl, die §6.4 Nr. 6 begründet (die
Phase ist ein **Band**wert, und der Träger sitzt in einem Band endlicher
Breite), und dieselbe, die der bestehende 512er-Abschnitt bei `:544` führt.
Zwei verschiedene Zahlen für dieselbe physikalische Aussage im selben Bein
wären die zweite Wahrheit, an der C6 hing. Der Ein-Sample-Versatz fällt mit
0,2618 rad auch an dieser Toleranz.

#### Neu gefasste Matrixzeilen — Fassung „Nacharbeit 1"

Die Fassungen darüber bleiben als Verlauf stehen; **bindend sind die
folgenden fünf.** Begründung je Zeile in der Tabelle oben.

| Nr. | Zusage (wörtlich) | Messender Fall | Negativfall | Erwartung |
|---|---|---|---|---|
| N-18 | „Phase folgt +2·pi·f·tau an drei Trägerfrequenzen" | B19 `sweep_known_delay` (`Sonde013StereoGoldenTest.cpp`) | ein Kanal bei Blockgröße ungleich 512 um **ein Sample** versetzt → die Zeile fällt bei allen Größen außer 512, ausgelöst vom 2000-Hz-Träger (0,2618 rad) | **je Blockgröße und je Träger** (300, 900, 2000 Hz, aus derselben Liste, aus der das Signal entsteht): das Band trägt ein Präsenzbit **und** eine Phase, und der auf (−pi, pi] gewickelte Fehler gegen `+2·pi·f·tau` ist **≤ 0,25 rad**. Dieselbe Zahl steht als `kPhaseToleranzRad` im Test; Begründung §6.4 Nr. 6. Gemessen: `d = 0,0000 rad` in allen 18 × 3 Fällen |
| N-19 | „Korrelation < 0,2; **keine Lag- oder Polaritätsempfehlung**" | B19 `sweep_uncorrelated_channels` | gemeinsamer 5-kHz-Ton auf beiden Kanälen: Korrelation im gemessenen Band bleibt −0,1933, ein Band trägt Phase → die Zeile fällt **allein** an der Empfehlungsprüfung (Gegenlauf ohne sie: grün) | **je Blockgröße:** Basis und Korrelationsbit gesetzt, `korrelationMittel < 0,2` — **und** von den Bändern mit Basis trägt **keines** eine Phase (`phaseGesetzt`, Stufe 2 des fail-closed; ohne sie gibt es weder Lag- noch Polaritätsaussage, §40.2). Dieselben Felder wie der 512er-Abschnitt M-12. Gemessen: 0 von 196 Bändern |
| N-21 | „fokussierte 0,01-dB-Evidenz muss Gain innerhalb ±0,1 dB … wiederfinden" — C++-Hälfte, zwei getrennte Zeilen, **beide am serialisierten Snapshot** | B16 `focused_evidence_wire_carries_0p01_db_steps` | Wirewerte gezehntelt → **Gainzeile** fällt (0,300 statt 3,000 dB). Encoder auf `q_db_0p1_i16` → **Leiterzeilen** fallen (0,19 dB Spanne ergeben bei Skalierung 10 höchstens 3 verschiedene Ganzzahlen statt 20) | PRE und POST laufen durch `evidenceSnapshotAlsJson`; gelesen werden ausschließlich `verteilung.p50.encoding`, `.werte` und `.gueltig_bitmap` **aus dem eigenen Wire-Text**. **Gainzeile:** dekodiert mit dem Teiler der Fixture, je Band mit Bit POST − PRE = G = 3,00 dB innerhalb ±0,1 dB (gemessen: 56 Bänder, Abweichung 0,0000 dB). **Leiterzeile:** N = 20 Pegel im Abstand 0,01 dB laufen **je** durch Engine und Serialisierer; die 20 Ganzzahlen sind streng monoton, benachbarte unterscheiden sich um **genau 1**, und die Schrittfolge ist dieselbe, die die Fixture zusagt. Der absolute Bandpegel bleibt unvorhersagbar — gemessen werden Differenz und Schritte |
| N-23 | die Kopplung beider Sprachen, **einschließlich Material** | `eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json` aus `erzeuge_v3_fixtures.py`, registriert in der Dateiliste `alle` | ein Byte der Fixture geändert → A8 `--pruefen` rot (gemessen im neuen Block `material`) | A8 hält sie bytegleich; zwei Verbraucher, einer je Sprache: B16 (C++) und A4 (Rust). Die Fixture trägt neben Pegel, Gain, Auflösung und Leiter jetzt auch das **Material** als Zahlen — `signal_hz`, `samplerate_hz`, `amplitude_pre`, `blockgroesse`, `bloecke_hoechstens`, `dauer_s_hoechstens` —, und B16 liest sie von dort statt aus lokalen Konstanten. `pre` und `post` bleiben unverändert, die Rust-Hälfte damit unberührt. „A5 liest mit" entfällt weiterhin (MP1-5) |
| S-07 | ein Fall mit einer Wahrheit außerhalb `ERLAUBTE_WAHRHEITEN` | A27, `_pruefe_wahrheiten` — **dieselbe Funktion, die A26 auf den geladenen Korpus anwendet** | die Funktion auf `return []` → S-07 fällt („keine rote Zeile", Exit 2) | ein **synthetischer Korpusfall** mit `wahrheit: erfundene_wahrheit` erzeugt **genau eine** rote Zeile, und die Zeile nennt den Fallnamen und `ERLAUBTE_WAHRHEITEN`. **Gegenteil im selben Lauf:** derselbe Fall mit einem Wert aus der Menge erzeugt keine Zeile |

#### Prüfliste `tools/dirigent/pruefliste.md`, abgehakt

| Zeile | Wo gemessen |
|---|---|
| **C — Verträge und Längen:** „Objekte werden mit exakter Feldmenge geprüft" / Zahlen über den Draht | B16 liest den Bandsatz jetzt vom Draht: 221 Werte, Encoding-Wort und Bitmap werden einzeln geprüft (`Sonde013EventWireTest.cpp`, Abschnitt M-83). Der Fixdiff fügt kein Feld hinzu, das über den Draht ginge |
| **E — Behauptung ≤ Messung** | Die drei Bein-Behauptungen im Runner (B16, B19, A27) sind mit dem Änderungssatz nachgezogen (`tools/beweise.ps1`, Commit `4be4f49`); sie sagen nicht mehr, als die Läufe oben zeigen. Zahlen in §7.1 sind gemessen, nicht abgeschrieben |
| **E — jede neue Prüfung wurde einmal absichtlich gebrochen** | Sechs Rotbeweise, je einer pro Regel plus der wiederholte Fixture-Byte-Beweis. Der Empfehlungsbeweis fährt zusätzlich den Gegenlauf ohne die neue Prüfung (grün) — die Probe ist damit Beleg und nicht Regressionswache |
| **E — geänderte Zusage, drei Stellen** | Runnerkopf `tools/beweise.ps1` (B16, B19, A27), Skriptkopf (`pruefe_p4_korpus.py` Punkt 4, `erzeuge_v3_fixtures.py` Docstring und `_kommentar` der Fixture), lebender Manifestkopf (Kopftabelle, Zeile Phase). Nachweis: `git grep -n verteilungAusWire` trifft 2 Zeilen (`Sonde013EventWireTest.cpp:238`, `:1715`), `git grep -n _pruefe_wahrheiten` 6 (Skriptkopf `:59`, Definition `:179`, A26-Aufruf `:399`, S-07 `:535` und `:541`, Runner-Behauptung A27 `beweise.ps1:578`), `git grep -n kPhaseToleranzRad` 2 und `git grep -n baenderMitPhase` 3 — je Kernbegriff genau die genannten Stellen |
| **F — Änderungssatz** | Writer, Reader und Fixture von NR-05 liegen in **einem** Commit (`3396510`): Erzeuger, Fixture-Bytes und der C++-Verbraucher. Der Rust-Verbraucher liest `pre`/`post`, die sich nicht geändert haben — A4 lief grün gegen dieselbe Datei |
| **A, B, D** | Nicht berührt: der Fixdiff fasst weder Queues noch Lebenszyklus noch Bau-/Prüfriegel an |
| **Selbstaudit vor „fertig“, Zahlenränder** | Beim adversarialen Lesen des Fixdiffs fiel eine **NaN-Lücke** in der neuen Gainzeile auf: ein fehlender `teiler` in der Fixture ergäbe `0/0 = NaN`, und `NaN > toleranz` ist **falsch** — die Zeile wäre still grün geblieben. Behoben in Commit `d3d9661`: nicht-endliche Differenzen zählen als „daneben“, und die Zeile verlangt zusätzlich `teiler > 0` und `toleranz > 0`. Die Leiterzeile liest rohe Ganzzahlen und teilt nicht; `phasenfehler` wickelt nur endliche Bandwerte |

#### Was ausdrücklich **nicht** getan wurde

- Kein voller Kanon in dieser Runde — so beauftragt; er läuft beim Abschluss.
- §7.0 (Erstprüfung) blieb unverändert; §7.1 steht darunter.
- Der bestehende 512er-Abschnitt bei `:544` (drei Träger, 0,25 rad) wurde
  nicht angefasst: er gehört zu M-10, nicht zu N-18, und ein Umbau dort wäre
  ein Neben-Refactor.
- `docs/beweise/G4.md`, `SONDE-013.md`, `NAK-180.md`, `NAK-181.md`,
  `docs/offene-punkte.md`, `docs/PLAN-STAND.md` und `docs/plan/` sind
  unberührt.

## Kanon-Lauf - NAK-182

**Lauf:** 2026-09-06 10:28 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 54/54 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-182-3ca500f.md](roh/NAK-182-3ca500f.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-06 10:28:59 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 3ca500f NAK-182 Etappe 2: Zusatz des Dirigenten — Kanonstart ohne Loeschbefehl (Ask-Regel bleibt, Ersatzweg benannt) |
| Commit (voll) | 3ca500fdd37b0a14a23c4b7f7e66e83599d06990 |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,06 s | [A1](roh/NAK-182-3ca500f.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,58 s | [A2](roh/NAK-182-3ca500f.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,78 s | [A3](roh/NAK-182-3ca500f.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 87,53 s | [A4](roh/NAK-182-3ca500f.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,99 s | [A4-SI](roh/NAK-182-3ca500f.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,73 s | [A4b](roh/NAK-182-3ca500f.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 2,56 s | [A5](roh/NAK-182-3ca500f.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-182-3ca500f.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,14 s | [A7](roh/NAK-182-3ca500f.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A8](roh/NAK-182-3ca500f.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,41 s | [A9](roh/NAK-182-3ca500f.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,06 s | [A10](roh/NAK-182-3ca500f.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,28 s | [A11](roh/NAK-182-3ca500f.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A12](roh/NAK-182-3ca500f.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,25 s | [A13](roh/NAK-182-3ca500f.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 5,84 s | [A14](roh/NAK-182-3ca500f.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,23 s | [A17](roh/NAK-182-3ca500f.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 33,60 s | [A18](roh/NAK-182-3ca500f.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-182-3ca500f.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A20](roh/NAK-182-3ca500f.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,35 s | [A21](roh/NAK-182-3ca500f.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,05 s | [A22](roh/NAK-182-3ca500f.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,73 s | [A24](roh/NAK-182-3ca500f.md#a24) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-182-3ca500f.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,13 s | [A26](roh/NAK-182-3ca500f.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,12 s | [A27](roh/NAK-182-3ca500f.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,53 s | [A23](roh/NAK-182-3ca500f.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,06 s | [A16](roh/NAK-182-3ca500f.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,20 s | [B1](roh/NAK-182-3ca500f.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,33 s | [B2](roh/NAK-182-3ca500f.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,06 s | [B3](roh/NAK-182-3ca500f.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-182-3ca500f.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,34 s | [B3c](roh/NAK-182-3ca500f.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,38 s | [B4](roh/NAK-182-3ca500f.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-182-3ca500f.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,31 s | [B5](roh/NAK-182-3ca500f.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 215,04 s | [B10](roh/NAK-182-3ca500f.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,01 s | [B11](roh/NAK-182-3ca500f.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,60 s | [B12](roh/NAK-182-3ca500f.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,11 s | [B13](roh/NAK-182-3ca500f.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,21 s | [B14](roh/NAK-182-3ca500f.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,20 s | [B15](roh/NAK-182-3ca500f.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,06 s | [B20](roh/NAK-182-3ca500f.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,94 s | [B22](roh/NAK-182-3ca500f.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,49 s | [B23](roh/NAK-182-3ca500f.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 0,99 s | [B24](roh/NAK-182-3ca500f.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-182-3ca500f.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-182-3ca500f.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-182-3ca500f.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,84 s | [B19](roh/NAK-182-3ca500f.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,09 s | [B17](roh/NAK-182-3ca500f.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,03 s | [B18](roh/NAK-182-3ca500f.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2): der echte Encoder schreibt q_db_0p01_i16, aus seinem EIGENEN Wire-Text kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Gemessen wird gegen die ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht gegen ihre Bytes. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,41 s | [B16](roh/NAK-182-3ca500f.md#b16) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,28 s | [B8](roh/NAK-182-3ca500f.md#b8) |

