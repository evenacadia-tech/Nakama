NAK-212 — Nacharbeit 2 nach der Wiederprüfung 1. Auftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA dieser Runde = b36d9e6c53c6b9e965b9b3db27bc0466e259f540 (= `git rev-parse HEAD` beim Start; prüfe das, Worktree sauber). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann push. tools/dirigent/pruefliste.md vor dem Commit abhaken und im Manifest nennen, wo jede Zeile gemessen ist.

LIES ZUERST: docs/beweise/NAK-212.md (Kopf, §2 Matrix — die Spezifikation —, §6 Nacharbeit 1), docs/beweise/roh/NAK-212-nacharbeit-1-auftrag.txt (Regel NR-03), dann das Urteil der Wiederprüfung 1 docs/beweise/roh/NAK-212-wiederpruefung-1-b36d9e6.txt (Codex gpt-6-astra max, lesend, Thread 01a07df9-81c7-7db3-a39f-07e82b548e4f, HEAD vor und nach dem Lauf b36d9e6): NR-01, NR-02 und NR-04 sind vom Prüfer als geschlossen bestätigt; NR-03 ist nicht vollständig geschlossen. Der Dirigent hat beide Restbefunde an der Quelle bestätigt.

TICKETGRENZE — genau diese zwei Befunde (gemeinsame Ursache: die Ausgabe des Kettenbeins A31 kommt im kanonischen Rohbeleg nicht an), sonst nichts:

WN-01 (tools/beweise.ps1:587, Bein A31): Der deklarierte Aufruf `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never` unterdrückt die `println!`-Ausgaben erfolgreicher Tests; im kanonischen Rohbeleg fehlen damit alle Eingabeprotokolle und die benannten NAK-213-/NAK-214-Lücken, die NR-03 ausdrücklich verlangt. Der Beinlauf der Nacharbeit 1 (docs/beweise/roh/NAK-212-nacharbeit-1-41982d1.txt, Zeile ~1050) nutzte `-- --nocapture --test-threads 1`, das Bein selbst nicht. Regel: Der A31-Aufruf in tools/beweise.ps1 gibt die Testausgabe frei (`-- --nocapture --test-threads 1`, gleiche Form wie im Beinlauf), sodass jede der 15 Eingaben mit ihrer Ausgabe und jede benannte Lücke im Rohbeleg des Kanons steht; das Bein bleibt grün und deterministisch (feste Threadzahl, keine Zeitabhängigkeit in der Ausgabe). Prüfe, ob ein weiteres Rust-Bein desselben Runners (A4, A31-Nachbarn) das Muster schon verwendet, und halte dich an die dortige Form.

WN-02 (broker/tests/sonde014_gegenbeispiele.rs:1200-1202, Fall f4b „drei Sonden auf einem Kanal"): Die drei `mittel`-Befunde werden protokolliert, aber der Test endet ohne Lückenkennzeichnung. Die Ausgabe ist nach Matrix §2.6 Nr. 23 korrekt; die Regel NR-03 nennt f4b aber ausdrücklich unter den Fällen, die mit benannter NAK-213-Lücke ausgegeben werden (Parent-Erkennung wird dort neu geregelt: NAK-213 R3). Regel: f4b gibt wie f4 die benannte Lücke aus („Ausgabe heute korrekt; Parent-Erkennung über alle Quellen einschließlich Master, zurückgenommener und stummer Quellen ist NAK-213 R3"), ohne den fremden Produktfall zu lösen und ohne die Erwartung `mittel` zu ändern.

ARBEITSWEISE: Je Befund ein Rotbeweis unter docs/beweise/roh/NAK-212-rot-WN-0n.txt (WN-01: der alte Aufruf liefert einen Rohbeleg ohne die Eingabeprotokolle, der neue mit; WN-02: die alte Ausgabe trägt keine Lückenzeile für f4b, die neue schon). Betroffene Beine grün und im Manifest mit Zahlen: A31 in der Form des Runners (`pwsh -NoProfile -File tools/beweise.ps1` nur für dieses Bein, falls der Runner eine Beinauswahl anbietet — sonst der Aufruf aus Zeile 587 wörtlich) und `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele`; Ausgabe unter docs/beweise/roh/NAK-212-nacharbeit-2-<sha>.txt. Kein voller Kanon in dieser Runde. Manifest docs/beweise/NAK-212.md: neuer Abschnitt „§7 Nacharbeit 2" (WN-01, WN-02 wörtlich, Regel, Änderung mit Datei:Zeile, Rotbeweis, gemessene Beine), Kopftabelle (Änderungssatz) und §5 Verlauf nachziehen, Prüfliste §3. Committen mit Pathspec (tools/beweise.ps1, broker/tests/sonde014_gegenbeispiele.rs, docs/beweise/NAK-212.md, docs/beweise/roh/NAK-212-*), pull --rebase, push, danach STOPP. Nichts außerhalb der zwei Befunde ändern.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen — Ausgabe deterministisch (Threadzahl 1, keine Zeitstempel, keine Pfade mit Laufwerksbuchstaben in der Ausgabe, die der Riegel als Verweis lesen könnte), das Bein bleibt grün, keine Änderung an Produktcode oder Korpus, keine irreführenden Kommentare oder Manifestsätze.
