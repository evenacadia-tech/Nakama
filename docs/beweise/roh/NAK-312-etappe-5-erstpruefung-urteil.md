URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).

# NAK-312 Etappe 5 — Erstprüfung (S25k, Prüfstufe T2)

Frische, lesende Prüfsession (Claude Opus 5), 21.09.2026, Lauf ab 19:08 Uhr, Bericht geschrieben ab 19:32 Uhr (`date`). Auftrag `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-auftrag.txt`. Prüfbereich `git diff 12300f1e429c38354f9d216d7ade395b71f71b70...0014d5f7d2b350539c0bce16b5a455d7648b23e1` über die zehn Pfade des Auftrags: 10 Dateien, +1328/-65 (`git diff --stat`), vollständig gelesen; die Änderungssätze `19ddf5d3`, `be37d10a` und `2c138b2d` liegen darin. Außerhalb von `docs/` hat der Zielstand gegenüber BASIS nur diese Pfade und `eq-copilot/install/nakama-installer-v1.json` geändert (`git diff --stat 12300f1e 0014d5f7 -- eq-copilot broker tools third_party .gitattributes`); `git diff --stat 0014d5f7 HEAD` zeigt allein `docs/PLAN-STAND.md` und den Prüfauftrag, der Code am HEAD ist also der des Zielstands; der Arbeitsbaum ist sauber bis auf die zwei fremden Ordner. Zeilenangaben gelten am Zielstand, wo nicht „BASIS" dabeisteht. Referenz: Manifest am Zielstand §2, §5.0, §5.7, §5.8, §6 Kopf, §6.1 (M-04), §6.5, §6.6, §7.0, §7.1, §7.5, §9.2, §11.3, §14.4, §29.3, §30 bis §33; als Kontext gelesen: Rotbeweise `NAK-312-rot-M-40.txt` bis `-M-54.txt`, `-M-83.txt`, `-M-88.txt`, die Erzeugungsrohdatei des Goldens, Register NAK-345, die JUCE-Quelle samt VST3-SDK unter `eq-copilot/build/_deps/juce-src`, `tools/fl/szenario.py` und lesend `C:\Users\phili\Projekte\fl-studio-mcp\src\fl_studio_mcp\utils\midi_connection.py` (MCP-Repo, Stand `15b3b1a`). Zwischenausgaben dieser Session liegen nur im Job-Temp-Ordner außerhalb des Repos.

## Befunde

**DEFEKT: keiner.**

### L-1 (LÜCKE) — eine während eines laufenden Renders gesetzte Vorschau (Produktwirkung 1 aus §30.14)

Wirkung am Code: `setNonRealtime` führt den Wechsel nur bei geändertem Flag aus und kehrt sonst an `eq-copilot/plugin/sonde/SondeProcessor.cpp:1573-1575` zurück. Am BASIS nahm jeder Aufruf mit `offline == true` das Zustandsschloss und rief `beendePreview` und `setzeHoermatrix (processed)` (BASIS `SondeProcessor.cpp:1541-1553`), also vor jedem Offlineblock. Eine Vorschau, die NACH dem Wechsel über `setzePreview` (`:1506-1510`) kommt, bleibt jetzt als Buchhaltung stehen (`eq-copilot/plugin/state/NakamaTransaktion.cpp:303-311`), belegt bei eingeschaltetem EQ der Vorschau die Candidate-Bank und lässt den Candidate-Pfad in jedem Block mitrechnen (`eq-copilot/plugin/dsp/DspKern.cpp:1257`, `:1321-1326`). Im Render ist sie nicht hörbar, weil der Riegel jeden Wunsch zu Processed macht (`DspKern.cpp:1225`), und die Analyse liest nur `postCommitted` (`SondeProcessor.cpp:616-617`).

Zur Aussage „wird nach dem Render hörbar, wenn der Hörwunsch dann Candidate ist": Der Wechsel stellt den Wunsch auf Processed (`SondeProcessor.cpp:1590`); nach dem Rückweg (`:1583`) gilt `wirksam = hoerwunsch` (`DspKern.cpp:1221-1222`). Die im Render gesetzte Vorschau wird nach dem Render also von selbst NICHT hörbar. Hörbar wird sie nur, wenn nach dem Wechsel jemand den Wunsch auf Candidate stellt — im Render (vom Riegel verdeckt) oder danach —, und dann mit weichem Fade (`DspKern.cpp:1366-1373`). Ein im Render gesetzter Candidate-Wunsch überdauert den Render nur, wenn danach kein `prepareToPlay` kommt: `DspKern::bereiteVor` setzt den Wunsch auf Processed (`DspKern.cpp:146`), `prepareToPlay` publiziert eine noch aktive Vorschau aber neu (`SondeProcessor.cpp:418`). Nach dem VST3-Vertrag verlangt der Wechsel zwischen kOffline und Echtzeit ein `setupProcessing` im deaktivierten Zustand (`ivstaudioprocessor.h:138-139`, `:307`), danach ruft JUCEs `setActive (true)` `prepareToPlay` (`juce_audio_plugin_client_VST3.cpp:2823-2833`). Im Vertragsablauf bleibt die Vorschau nach dem Render damit unhörbare Buchhaltung, bis ein neuer Candidate-Wunsch kommt oder `beendePreview` beziehungsweise ein Ladestart sie beendet. Der Satz aus §30.14 stimmt als Bedingung, beschreibt aber keinen Automatismus. Dasselbe gilt für einen im Render gesetzten Dry- oder Delta-Wunsch: bisher setzte jeder Offlineblock ihn auf Processed zurück, jetzt überdauert er den Render, solange kein `prepareToPlay` folgt.

Einordnung als LÜCKE, nicht als DEFEKT: M-44 spricht über die beim Wechsel aktive Vorschau („`setNonRealtime (false)` belebt die Vorschau NICHT wieder") — eine im Render neu gesetzte ist keine wiederbelebte, und 312/M-44 hält. R-312-3, [SONDE-015] M-120 und die Entwurfssätze `:2137` und `:2748` verlangen Neutralität IM Render und keine Vorschau im Projekt; beides hält (Riegel, Vorschau nie im State). Über eine im Render gesetzte Vorschau und ihren Bestand nach dem Render sagen Gate, Matrix und Entwurf nichts. Erreichbarkeit: am Zielstand ruft kein Produktpfad `setzePreview` oder `setzeHoermatrix` der Sonde; die einzigen Produktaufrufe der Hörmatrix setzen Processed (`SondeProcessor.cpp:1314`, `:1590`; gemessen mit `git grep`). Die Wirkung entsteht erst mit Gens Fernsteuerung der Vorschau. Ob eine im Render gesetzte Vorschau den Render überdauern soll, ist eine Produktfrage für den Dirigenten. Keine Nacharbeit.

### L-2 (LÜCKE) — eine nicht verbuchte Anforderung „Vorschauende" beendet eine nach dem Rückweg neu gesetzte Vorschau

Der Rückweg lässt eine noch nicht verbrauchte Anforderung stehen (`SondeProcessor.cpp:1577-1585`, Kommentar „ein angefordertes Vorschauende bleibt angefordert"). Der nächste Kontrolltakt beendet dann die Vorschau, die in diesem Moment aktiv ist (`:1808-1810` → `NakamaTransaktion.cpp:313-318`), ohne zu unterscheiden, ob es noch dieselbe ist. Gerechneter Ablauf: `setNonRealtime (true)`, `setNonRealtime (false)` vor dem nächsten Takt, dann `setzePreview (neu)` → der Takt beendet die neue Vorschau. Das Fenster reicht bis zum nächsten Takt (frühestens 5 ms Abstand, Wartezeit bis 20 ms: `:753-757`, `:916-918`). Am BASIS endete die Vorschau synchron im Wechsel, eine danach gesetzte blieb. Der Klang bleibt klickfrei (Ende über den regulären Hörhalt). Keine Zusage spricht über eine Vorschau nach einem kurzen Offline-Fenster; E-312-7 legt die Buchhaltung in den Takt, regelt diesen Fall aber nicht. Erreichbarkeit wie L-1: heute kein Produktaufrufer. Keine Nacharbeit.

### H-1 (HÄRTUNG) — der Offline-Übergang ist gegen gleichzeitige, gegensätzliche Aufrufe aus zwei Threads nicht atomar

Wechselerkennung und Ausführung sind zwei Schritte: der Tausch an `SondeProcessor.cpp:1574` entscheidet, wer wechselt, die Wirkung (`:1583` beziehungsweise `:1590-1593`) folgt danach. Bei gleichzeitigen Aufrufen mit GLEICHEM Wert wechselt genau einer (kein doppelter Wechsel). Bei gleichzeitigen Aufrufen mit GEGENSÄTZLICHEN Werten können Flag und Kernzustand auseinanderlaufen, gerechnet:

- `offlineGesehen = false`. Thread 1 ruft `setNonRealtime (true)`, gewinnt den Tausch, steht vor `:1590`. Thread 2 ruft `setNonRealtime (false)`, liest `true`, gewinnt seinen Tausch und löst Riegel und Anforderung (`DspKern.h:349-350`). Thread 1 setzt danach Riegel und Anforderung (`DspKern.h:344-345`). Ergebnis: Flag Echtzeit, Riegel steht; jeder weitere `setNonRealtime (false)` kehrt an `:1573` zurück, und bis zum nächsten Offline-Zyklus wirkt in Echtzeit jeder Hörwunsch als Processed.
- Spiegelbildlich (`offlineGesehen = true`, Rückweg und Hinweg gleichzeitig, der Rückweg speichert zuletzt): Flag offline, Riegel und Anforderung gelöst — der Render liefe ohne Riegel und ohne hartes Schalten, ein Candidate-Hörstand blendete über 256 Samples aus (gegen M-42), ein später gesetzter Wunsch wäre im Render hörbar.
- Das in (a) gefragte Fenster aus A-53: der Audiothread tauscht die Anforderung (`DspKern.cpp:1216-1217`), der Rückweg speichert danach beide `false`, der Audiothread liest den Riegel als `false` (`:1225`) und schneidet ohne Fade auf `hoerwunsch`.

Alle drei setzen voraus, dass `setNonRealtime` auf zwei Threads zugleich läuft. Nach JUCE-Wrapper und VST3-Vertrag geschieht das nicht: im Callback ruft derselbe Thread `setNonRealtime` und danach `processBlock` (`juce_audio_plugin_client_VST3.cpp:3889-3909`), und `setupProcessing` (`:3592-3610`) darf nur im deaktivierten Zustand kommen (`ivstaudioprocessor.h:307`). JUCE vermerkt, dass FLs Patcher `process()` vor oder während `setActive()` ruft, und serialisiert dafür `setActive`, `process`, `activateBus` und `setBusArrangements` (`:3812-3828`, `:2811`, `:3316`, `:3461`, `:3716`), nicht aber `setupProcessing`; ob FL `setupProcessing` neben `process` ruft, ist nicht gemessen. Im Produkt bliebe jede dieser Verschränkungen heute unhörbar: kein Produktpfad setzt einen anderen Hörwunsch als Processed (L-1, Erreichbarkeit), der Schnitt wäre Processed auf Processed. Von keiner Zusage im Vertragsablauf verlangt. Denkbar: Riegel und Anforderung in EIN Atomic mit Vergleichstausch, oder die Anforderung nur unter stehendem Riegel verbrauchen. Keine Nacharbeit.

### H-2 (HÄRTUNG) — Riegel, Rücknahme der Anforderung und Allokationsfreiheit des Offline-Übergangs ohne messenden Fall

Gelesen und gerechnet, nicht gefahren:

- Der Riegel (`DspKern.cpp:1225`): kein Fall setzt einen Hörwunsch ungleich Processed, während die Sonde offline steht. Alle `setNonRealtime (true)` in B7 (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2559`, `:3936`, `:4484`, `:5406`, `:5423`, `:5462`, `:5498`, `:5522`, `:5550`, `:5585`, `:5593`, `:5609`, `:5627`) folgen ohne `setzeHoermatrix`/`setzePreview` bis zum Rückweg; 312/M-43 setzt Candidate erst nach `setNonRealtime (false)`. Eine Mutation, die die Zeile entfernt, bliebe grün. Die Entwurfszusage „Preview, Focus und Delta sind im Offline-Render neutral" (`:2748`) hält damit gelesen, aber ungemessen für einen im Render gesetzten Wunsch.
- Die Rücknahme der Anforderung beim Rückweg (`DspKern.h:349`, A-53): kein Fall kehrt zurück, ohne dass seit dem Hinweg ein Block lief (312/M-04 nach 200 Blöcken, 312/M-43 nach 16, 312/M-44 nach 4). Ohne die Rücknahme schnitte der erste Echtzeitblock nach einem Offline-Fenster ohne Block hart (gegen [SONDE-015] M-55); eine Mutation, die sie entfernt, bliebe grün.
- Allokationen: in 312/M-04, 312/M-40 bis 312/M-47, 312/M-83 und im umgebauten O-Fall ist `zaehleAllokationen` nie scharf; `RtWache::allokationen()` zählt nur über B7s `operator new` bei scharfem Flag (`TransactionTestMain.cpp:141-160`, `eq-copilot/plugin/dsp/DspRtWache.h:41-44`). Gelesen allokiert der Übergang nicht (nur Atomics, (a)). Die Zeile M-40 sagt nur Sperren zu.

Von keiner Matrixzeile verlangt. Denkbar: ein Fall „Wunsch Delta im Render bleibt Processed", ein Fall „Hinweg, Rückweg ohne Block, dann Candidate — Nachbarsprung in der E-31-Schranke", und der Allokationszähler in 312/M-40. Keine Nacharbeit.

### H-3 (HÄRTUNG) — die B2-Hälfte von 312/M-49 ist nie rot gefahren

Die Matrixmutation von M-49 („Parameter ohne Namensraum") lässt M-49 grün; die Rotbeweise tragen Teil c (`getStateInformation`) und Teil d (`addListener`), beide in B7 (A-59; nachvollzogen, sie liegen an den Zeilen der zwei Zusagehälften). B2 blieb unter allen vier Teilen grün (`NAK-312-rot-M-49.txt`), weil es den Schreiber misst, nicht den Prozessor. Rotfähig ist der B2-Fall nur nach seiner Bauart (`eq-copilot/plugin/tests/StateMigrationTestMain.cpp:2526-2553`: Vergleich gegen eingefrorene Bytes und Rundreise über `state::speichere`); gezeigt ist es nicht. Denkbar: ein Rotlauf mit einem veränderten Byte der Bin-Kopie oder einer Mutation im Schreiber. Keine Nacharbeit.

### H-4 (HÄRTUNG) — Textriegel 312/M-88: Schwelle unter dem Bestand, zwei Kopfvorlagen ungelesen

- Schwelle 100 (`TransactionTestMain.cpp:5965`) gegen einen Bestand von 103 Dateien (gemessen: `src` 27, `sonde` 3, `dsp` 9, `state` 13, `vertrag` 8, `core` 42, `hostbridge` 1 mit Endung `.cpp`/`.h`). Ein fehlender Ordner und weniger als 100 Dateien sind rot; 100 bis 102 Dateien blieben grün. „Weniger Dateien als der Bestand ist rot" gilt damit nicht streng (anders als 312/M-50, Schwelle 3 bei 3 Dateien).
- Das Muster `*.cpp;*.h` (`:5950`) liest die zwei Kopfvorlagen `eq-copilot/plugin/core/ipc/BrokerInstallBinding.h.in` und `eq-copilot/plugin/hostbridge/NakamaBrueckeStand.h.in` nicht, aus denen Kopfdateien der Bündel entstehen; die B7-Behauptung sagt „kein Quelltext unter … core und hostbridge" (`tools/beweise.ps1:728`). Ebenso außerhalb: der Nakama-Code im JUCE-Wrapper (`third_party/patches/juce-8.0.9-nakama-vst3-bridge.patch`). Von mir am Zielstand nach allen Wörtern der Liste durchsucht: kein Treffer — die Behauptung ist heute wahr, für diese drei Dateien aber nicht gemessen.
- Die Liste könnte `DestroyWindow`, `WM_DESTROY` und `WM_SYSCOMMAND` aufnehmen; im Pluginquelltext kommen sie heute nicht vor (gemessen).

Die Zeile M-88 in der Fassung von §33.2 verlangt keine Schwelle und keinen Dateityp. Keine Nacharbeit.

## Prüfpunkte des Auftrags

### (a) Offline-Wechsel im Callback

- `setNonRealtime` (`SondeProcessor.cpp:1560-1594`) fasst an: den Basisaufruf `juce::AudioProcessor::setNonRealtime` — ein Store auf `std::atomic<bool> nonRealtime` (`juce_AudioProcessor.cpp:410-413`, `juce_AudioProcessor.h:1603`) —, `offlineGesehen` (Lesung acquire, Tausch acq_rel, `:1573-1575`), `DspKern::setzeHoermatrix` (ein Release-Store, `DspKern.h:308`), `DspKern::setzeOfflineRiegel` (zwei Release-Stores, `DspKern.h:340-352`) und `vorschauEndeAngefordert` (Release-Store, `:1593`). Keine Sperre, keine Allokation, kein Warten, kein Hostaufruf; alle Aufgerufenen sind `noexcept`. `Hoermatrix` ist `enum class` auf `int` (`eq-copilot/plugin/dsp/DspProgramm.h:78`), die Atomics sind auf x64 lock-free. `offlineRiegelAn` und `hartSchalten` sind im Kern ausdrücklich als geteilt dokumentiert (`DspKern.h:736-743`), `offlineGesehen` und `vorschauEndeAngefordert` in der Sonde (`SondeProcessor.h:625-632`); alle Zugriffe (per `git grep`): Riegel und Anforderung nur `DspKern.h:344-353` und `DspKern.cpp:1216-1225`, `vorschauEndeAngefordert` nur `:1593` und `:1808-1809`.
- Aufrufer im Wrapper: `setupProcessing` auf dem Hostthread ohne Callback-Lock (`juce_audio_plugin_client_VST3.cpp:3605`) und `processAudio` unter dem Callback-Lock unmittelbar vor `processBlock` (`:3890-3909`). Wechselerkennung: gleiche Werte gleichzeitig — genau ein Wechsel; gegensätzliche Werte gleichzeitig — H-1.
- Speicherordnung: Hinweg Wunsch, Riegel, Anforderung (je release); der Audiothread liest die Anforderung zuerst (acquire) und verbraucht sie per Tausch; wer `true` liest, synchronisiert mit dem Release-Store und sieht Wunsch Processed und Riegel — das trägt M-41 und M-42. Rückweg Anforderung vor Riegel. Das Fenster „Anforderung verbraucht, Riegel schon gelöst" entsteht als gewöhnliche Verschränkung, verlangt aber einen Rückweg auf einem zweiten Thread während `verarbeiteStueck` und einen Hinweg ohne dazwischenliegenden Block. Nach Wrapper und VST3-Vertrag nicht erreichbar; für FL nicht gemessen; im Produkt heute ohne hörbare Folge. Kein Bruch von M-43 oder [SONDE-015] M-55 im Vertragsablauf — H-1.

### (b) E-312-7: Buchhaltung beim Worker

- Der Audiothread gibt keine Bank frei: seine neuen Zeilen (`DspKern.cpp:1216-1236`) tauschen die Anforderung und setzen `hoerVorher`, `hoerLaufend`, `hoerFadeRest`, die ihm allein gehören (`DspKern.h:745-749`). Im Audiopfad bleibt es bei `meldeAusgedient` (Zustand `verblassend` → `ausgedient` plus ACK, `eq-copilot/plugin/dsp/DspBankPool.cpp:266-280`); frei macht allein `ernteAcks` im Worker (`:164`, `DspBankPool.h:37-39`). Den Merkzettel fassen nur Workerwege an (`DspKern.cpp:288`, `:324`, `:395`, `:445`; `merke` und `vergebeKennungen` hängen an `publiziereVorbau`, `:360`, `:416`).
- `vorschauEndeAngefordert` verbraucht allein `dspKontrollTakt` (`SondeProcessor.cpp:1808-1810`), innerhalb des Zustandsschlosses, das der Takt ab `:1796` hält; `beendePreview` ist idempotent (`NakamaTransaktion.cpp:313-318`).
- Keine Bank bleibt hängen, gerechnet: (1) nie ein Offlineblock — der Rückweg nimmt die Anforderung zurück, der nächste Takt beendet die Vorschau, der nächste Echtzeitblock nimmt die ENDE-Marke bei hörbarem Candidate als Hörhalt, sonst als Ausblende (`DspKern.cpp:562-564`), der Wunsch Processed startet den weichen Fade (`:1366-1373`), an dessen Ende `beendeHoerHalt` die Bank ausdient (`:504-521`, `:1524`), `pflege` gibt sie frei (`SondeProcessor.cpp:1799`); (2) Rückweg vor dem ersten Takt — die Anforderung bleibt (`:1577-1585`), weiter wie (1); (3) ein Hörhalt beim Schalten endet am Ende des ersten Offlinestücks (`DspKern.cpp:1227-1236`, `:1524` beziehungsweise `:1358`); (4) `bereiteVor` und `freigeben` setzen Pool und Hörmatrix zurück (`DspKern.cpp:143-151`, `:184-191`), Riegel und Anforderung bleiben unberührt. Gemessen sind der Hauptfall und der Hörhalt (312/M-46, `TransactionTestMain.cpp:5457-5512`), (1) und (2) nicht (H-2 berührt die Rücknahme).

### (c) Klang und Grundgesetz

- Ab Sample 0: der harte Zweig steht vor jedem Schreiben (`DspKern.cpp:1227-1236` vor der Schleife `:1474-1508`); beim gestückelten Überblock verbraucht das erste Stück die Anforderung (`:1177-1190`); leere Blöcke erreichen den Kern nicht (`SondeProcessor.cpp:515`), die Anforderung wartet auf den nächsten. Blockgröße 1, 64, 256: 312/M-47 (`TransactionTestMain.cpp:5617-5642`), 312/M-42 (O-Fall `:2543-2575`, im Bereich `:5451`).
- Laufender Fade in Gegenrichtung endet mit (`hoerVorher = hoerLaufend = wirksam`, `hoerFadeRest = 0`); ohne Vorschau Processed auf Processed, bitgleich (312/M-45). Rückweg weich: kein hartes Schalten ohne Anforderung, M-55-Fade unverändert (`:1366-1388`; 312/M-43). Offline hin und zurück belebt die Vorschau nicht (312/M-44).
- Grundgesetz: bei `eq_enabled` aus ruht der Committed-Pfad, der Kern schreibt kein Sample (`:1256`, `:1351-1359`); die neuen Zeilen ändern nur Hörmatrixzustand. `setNonRealtime` schreibt kein Audio. Latenz 0 und Tail 0,0 unverändert (`SondeProcessor.h:212`, `SondeProcessor.cpp:368`). Nulltest und A16 laut Rohdateien grün (nicht wiederholt).

### (d) Produktwirkung 1

LÜCKE, siehe L-1, getragen von `SondeProcessor.cpp:1573-1575`, `:1590`, `DspKern.cpp:1225`, `:1221-1222`, `:146`. `setzeHoermatrix (processed)` im Wechsel stellt den Wunsch so, dass die Vorschau nach dem Render NICHT von selbst hörbar wird; die Aussage aus §30.14 trifft nur zu, wenn danach ein Candidate-Wunsch gesetzt wird und kein `prepareToPlay` ihn zurücksetzt.

### (e) Parameteranbindung

- Identität, Zeile für Zeile BASIS `baueParameterLayout` gegen `hostParameterAnlegen` (`SondeProcessor.cpp:94-126`): dieselbe Schleife über `parameter::tabelle()`, `ParameterID { b.id, 1 }` (Versionshinweis 1), Name `b.id`, dieselben Klassen `AudioParameterBool`/`Float`/`Choice`, dieselbe `NormalisableRange` mit `setSkewForCentre` für `.freq_hz`, `.q`, `.attack_ms`, `.release_ms`, dieselben Defaults, keine Einheit, Kategorie Vorgabe; einzig `layout.add` wurde `prozessor.addParameter` in derselben Reihenfolge. Die APVTS registrierte genauso (`addParameterAdapter`, dann `processor.addParameter`, `juce_AudioProcessorValueTreeState.cpp:225-235`), ohne Gruppen (Golden: `gruppen 0`).
- Was die APVTS am BASIS außerdem tat: je Parameter einen `ParameterAdapter` als zweiten Listener mit eigener `CriticalSection` je Wertänderung (`:72-211`, `:148-159`, `:179-204`), einen 10-Hz-Timer und einen ValueTree mit Listener (`:271-276`), Undo `nullptr` (BASIS `SondeProcessor.cpp:229`). Kein Produktweg hing daran: am BASIS nennt der Produktcode `parameterBaum` nur in der Konstruktion (BASIS `SondeProcessor.cpp:86`, `:88`, `:229`, `SondeProcessor.h:716`; `git grep` nach Attachments, `getRawParameterValue`, `copyState`, `replaceState`: nichts); Speichern und Laden laufen über den Transaktionskern (`:1202-1206`, `:1229-1336`); die Sonde hat keinen Editor.
- Konstruktor: Parameter als erste Anweisung des Rumpfs (`:243`). Die Initialisierungsliste (`:182-237`) liest keinen Parameter; die Provider-Lambdas laufen erst nach `start()` (`:313-314` beziehungsweise Starthaken im Testbau); die Konstruktoren von `ControlClient`, `TelemetryClient` und `Briefkasten` starten keinen Thread (`core/ipc/ControlClient.cpp:50-65`, `core/ipc/TelemetryClient.cpp:253-262`, `core/diagnose/Briefkasten.cpp:866-872`); der Worker startet an `:306-307`. Die Mitglieder, die am BASIS nach der APVTS entstanden (`SondeProcessor.h:745-788`), lesen bei ihrer Initialisierung keinen Parameter.
- `addListener` (`:268`) und `removeListener` (`:340-341`) paarweise über `getParameters()`; Indexrand `:1599`; `abgleichTiefe` thread-lokal (`:76-82`, `:1733`); `hostParameterAbgleichen` im Ladeweg nach dem Schloss (`:1333`). Die JUCE-eigene Sperre bleibt (`juce_AudioProcessorParameter.cpp:111-121`) — nach R-312-4 Registerzeile, kein Befund.

### (f) Golden

- Kein Orakel im Sinn von §7.1: das Soll ist die eingefrorene Ausgabe des BASIS-Codes, verglichen mit der Ausgabe desselben Erzeugers am Zielstand — ein Differenzvergleich über jedes erfasste Feld (Index, `getParameterIndex`, ID, Name, Klasse, Auswahl, Versionshinweis, Bereich samt Skew als Bitmuster, Defaults, Wert, Schritte, Flags, Kategorie, Einheit, Hostsicht an drei plus drei Stützstellen; dazu s0, s1, s1 nach der Fahrt, s2, Commits, Revision, Hash, Dirty, Epochen). Stichprobe: Skew von `freq_hz`, `q`, `attack_ms`, `release_ms` ungleich 1,0, Auswahllisten vollständig. Selbst gemessen: die BASIS-Blobs der vier Produktdateien in Arbeitskopie-Zeilenenden (CRLF) haben genau die SHA-256 der Erzeugungsrohdatei (`09DC9F64…`, `F9B1A7B9…`, `E7A53DDC…`, `B0681BCF…`), ebenso die BASIS-Testdatei (`18F63AA9…`); die Bin-Datei hat 5745 Byte und die SHA-256 ihrer `s1`-Zeile (`4ea7c41e…7810`), die Textdatei 29 665 Byte, SHA-256 `98d29988…7bef`, 125 Zeilen (2 Kommentar, `anzahl`, `gruppen`, 112 Parameter, 9 Zustand).
- Fail-closed: B7 (`TransactionTestMain.cpp:5700-5725`) — fehlende Datei, leerer Text (keine Zeilen), weniger oder mehr Zeilen, leere oder fehlende Bin-Datei sind rot. B2 (`StateMigrationTestMain.cpp:2526-2553`) — fehlende oder leere Bin, fehlende `s1`-Zeile rot.
- Zeilenenden: `git check-attr` — Bin `binary: set`, `text: unset`, `diff: unset`, `merge: unset`; Text ohne Attribut. `git ls-files --eol`: Bin `-text`, Text `i/lf w/lf`; `core.autocrlf=true` auf diesem Rechner, ein frischer Checkout kann den Text also mit CRLF schreiben. B7 schneidet `\r` am Zeilenende ab (`goldenZeilen`, `:5653-5665`), B2 teilt mit `StringArray::fromLines` (behandelt `\r\n`, `juce_StringArray.cpp:370-397`) und `trimEnd`; kein Feld endet auf Leerraum. Ein anderer Checkout kann den Vergleich nicht fälschen; die Bin-Datei wird nie umgeschrieben (`.gitattributes`: `*.bin binary`).
- A-48: §7.0 zählt `docs/beweise/roh/NAK-312-*` in jeder Etappe zu den Ticketpfaden; E-312-3 begrenzt neue Quelltextpfade, das Golden ist Testeingabe, kein Quelltext; §7.1 verlangt die Erzeugung über den unveränderten Prozessor am Basis-SHA, keinen Ort. Gedeckt; wird die Datei verschoben, ist das Bein rot. Kein Befund.

### (g) Tests

- Determinismus: `previewAktiv()` und Bankfreigabe werden nur nach ausdrücklichem `kontrollTaktFuerTest()` gefragt (O-Fall `:2572-2574`, 312/M-83 und M-46 `:5457-5484`, Hörhalt-Teilfall `:5486-5512`, 312/M-44 `:5587-5590`). Der mitlaufende Worker kann früher ziehen; er berührt dabei nur den Candidate-Pfad und `pflege`, die verglichenen Ausgänge kommen aus dem Committed-Pfad — die Bitgleichheit hängt nicht am Taktmoment. Die Epochenfolge des Goldens entsteht unter der Taktsperre mit ausdrücklichen Takten (`:1954-2021`). 312/M-53: die Nullzählung gilt für jede Verschränkung; die Bestehensbedingung `geschriebenWaehrend > 0` (`:5843`) hängt an der Zuteilung des zweiten Threads, kann aber nur falsch-rot, nie falsch-grün machen — kein Befund.
- Zähler: 312/M-04, M-40 lesen `RtWache::sperren()` (thread-lokaler Bereich, `DspRtWache.h:36-53`); 312/M-53 zusätzlich `RtWache::allokationen()` und B7s eigenen Zähler bei scharfem Flag (`:5823-5840`).
- Rotbeweise (Rohdateien gelesen, nicht wiederholt): jede Mutation liegt an der Zeile, die die Zusage trägt — M-40 Teil a/b in `setNonRealtime`, M-41 am Anfangswert von `offlineGesehen`, M-42/M-47 und M-45 im harten Zweig, M-43 an seiner Bedingung, M-83/M-46 am Verbrauch im Takt, M-48 an der Tabellenwahl, M-49 Teil c/d, M-50 am Sondenquelltext (Typalias), M-51 bis M-54 an Ladeabgleich, Abmeldeschleife, Blockrandlesung, Indexwache, M-88 per `std::exit` im Pluginquelltext. Die Umformungen tragen ihre Zeile: A-50 (Gleichanteil mit exaktem Fadeschritt), A-51 (beide Wortlaute gefahren), A-52 (belebt die Vorschau beim Rückweg wirklich), A-56 (gemeinsame Mutationen nennt die Matrix selbst), A-59 (Teil c und d an den Zeilen der zwei Hälften; B2-Hälfte H-3), A-60 (Faltung statt Feldzugriff außerhalb, kein undefiniertes Verhalten), A-64 (Typalias, Riegel ist textuell). A-61: die Zählung „vor der Konstruktion und nach der Zerstörung gleich" ist so nicht messbar; gemessen am Ende des Destruktorrumpfs (Prozessor lebt, Worker gejoint): der Zähler des Beins bekommt 112, die Mailbox des Prozessors bleibt, die Gegenprobe vorher erreicht ihn — das trägt „nach dem Destruktor hält kein Parameter mehr einen Listener auf den Prozessor". Kein Rotlauf in undefiniertem Verhalten.
- M-51 lädt in eine frische Instanz (Matrix: „dieselbe Instanz"); in derselben stünden die Regler schon aus dem Commit, ein fehlender Abgleich fiele nicht auf. Die Hälfte „dieselbe Instanz" misst 312/M-10 am Regler (`:4097-4099`), und genau diese Prüfung fiel unter der M-51-Mutation mit. Kein Befund.
- Testzugänge: `hostEreignisFuerTest`, `hostEreignisOffenFuerTest`, `parameterValueChangedFuerTest` stehen im Block `#if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` (`SondeProcessor.h:304-535`, Zugänge `:450-458`); das Sondenziel setzt das Define nicht (`eq-copilot/plugin/CMakeLists.txt:350-357`). A-54: `GemeldetesSchloss` hatte am BASIS genau einen Nutzer (BASIS `SondeProcessor.cpp:1549-1550`), kein Test nennt ihn (`git grep` BASIS und Ziel); die Mutationen von M-40 bringen einen lokalen Adapter mit.

### (h) Behauptungszeilen in `tools/beweise.ps1`

- B7 (`:728`): jeder neue Satz hat seinen Fall — Offlineflag je Aufruf 0 (312/M-04), Vorschau hörbar und 0 Sperren über 200 Blöcke (312/M-40), gewünschte Hörmatrix sofort Processed (M-41), ab Sample 0 bei 1, 64, 256 samt Gegenrichtung und Hörhalt (M-42, M-47, Teilfälle), Buchhaltung nach Takt und Bankfreigabe nach `ceil (kFadeSamples/Blockgröße)+1` Blöcken (M-83, M-46), Rückweg mit Fadeschritt plus 2^-23 (M-43), keine Belebung (M-44), ohne Vorschau unverändert (M-45); Golden mit Hostsicht an den genannten Stützstellen, Statebytes und Epochen (M-48, M-49), kein APVTS unter `sonde/` (M-50), Hostparameter nach dem Laden (M-51), Destruktorrumpf (M-52), Index −1/112 und Abgleich (M-54), 4000 Blöcke mit zweitem Thread samt JUCE-Grenze (M-53). Die M-88-Zeile begrenzt sich ausdrücklich („ob der Host im Betrieb lebt, misst das nicht"); „kein Quelltext unter … core und hostbridge" reicht um die zwei `.in`-Vorlagen weiter als das Muster des Riegels (H-4).
- B2 (`:693`): genau der B2-Fall von M-49 (Länge und SHA-256 zur Zeile, normales Laden, bytegleiche Rückschrift).
- Die zwei Zusätze wie verlangt: NAK-345 Punkt 1 auf „Aufruf" festgelegt („je Aufruf setNonRealtime (true) wie je Aufruf setNonRealtime (false) 0 gemeldete Sperren (bis Etappe 4 je Aufruf setNonRealtime (true) genau eine)"); E-312-15 wörtlich in B31 (`:749`): „controlV3.stop() gemessen vor dem Schliessen, telemetryV3.stop() nach Zeilenfolge".

### (i) M-88

- Kanonteil: die sieben Ordner decken alle `.cpp`/`.h` beider Bündel (Quelllisten `CMakeLists.txt:111-117`, `:134-142`, `:198-220`, `:337-342`); 103 Dateien, kein Treffer laut Kanon. Grenzen: Schwelle 100, zwei `.in`-Vorlagen und der Wrapperpatch ungelesen (H-4, alle drei von mir ohne Treffer durchsucht). `nennt` zählt Kommentare mit (fail-closed nach A-66) und lässt Mitgliedsaufrufe `.exit(`/`->exit(` aus. Die Liste trägt den ohne Host messbaren Teil von R-312-19 (Prozessende, Beenden der Nachrichtenschleife, Schließen des Hauptfensters); „blockiert" ist textuell nicht messbar, und die Behauptungszeile behauptet es nicht. Weitere Wege (`DestroyWindow`, `WM_DESTROY`, `WM_SYSCOMMAND`, `ExitWindowsEx`, `abort`) kommen im Pluginquelltext nicht vor; `std::terminate` bleibt nach NAK-289 erlaubt (A-66).
- Szenario `docs/gesundheit/szenarien/schleife-dauerlauf.json`: nur ein fehlender Ping im ersten Schritt ist VORAUSSETZUNG (`tools/fl/szenario.py:2118`, `:2129-2141`). Verschwindet oder hängt der Host danach, liefert der MCP für jeden späteren FL-Schritt nach der Wiederholung (`:691-712`) einen lokalen Fehler MIT der Anforderungskennung (`midi_connection.py:81-87`, `:317-325`); er gilt als gemessen (`:715-719`), `_pruefe` meldet „Antwort ohne Erfolg", Schritt VERFEHLT, Szenario-Exit 4 (`:2126-2136`). `lokal.briefkasten` ohne Antwort beider Rollen ist ebenfalls VERFEHLT (`:834-843`, `:878-898`). Fünf Umläufe: Songlänge 1 bis 46 000 ms geprüft, dann fünfmal 46 s Warten mit anschließender Prüfung von `is_playing`, Songloop und `frame.spielt` beider Rollen; bei Songlänge höchstens 46 s liegt in jeder Wartezeit mindestens ein Umlauf. Die Position wird nur roh geschrieben, ein Wrap nicht ausdrücklich geprüft — bei durchgehender Wiedergabe folgt er aus Länge und Wartezeit (laut §32.2 je genau ein Wrap bei 45 696 ms).

## Punkt 3 — Produktverhalten seit BASIS

Selbst nachgemessen (nur lesende Kommandos, kein Bau, kein Lauf): HEAD vor und nach; Commits `12300f1e..HEAD`; Umfang des Prüfbereichs; außerhalb von `docs/` nur Prüfbereich und Installer-Hashes geändert; `0014d5f7..HEAD` nur `docs/`; Attribute, Zeilenenden und SHA-256 beider Golden-Dateien, Zeilenzahl des Textes; SHA-256 der BASIS-Blobs gegen die Erzeugungsrohdatei; Dateibestand der Riegelordner und die Suche nach Hostende-Wörtern in Vorlagen, Wrapperpatch und Pluginquelltext; Produktaufrufer von Vorschau und Hörmatrix; APVTS-Nutzung am BASIS; alle Zugriffe auf die vier neuen Atomics.

Nur gelesen: Code, Tests, JUCE- und SDK-Quelle, Runner, Szenario, MCP-Client; Kanon GRUEN 70/70 an `2c138b2d`, A32/A33, Laufzeit-Arm mit Nulltest BITIDENTISCH und alle Rotläufe stehen in den Rohdateien und im Manifest, nicht von mir wiederholt.

Gelesen, kein Bruch: Identität — Parameterklassen, IDs, Reihenfolge, Bereiche wie am BASIS (e), keine Datei unter `eq-copilot/identity/` oder an Class-IDs geändert; Passthrough und Nulltest — der Ruhepfad des Committed-Pfades schreibt weiter kein Sample (c); State und Stateversion — kein Schema-, State- oder Fixturepfad geändert, `getStateInformation` und `setStateInformation` unverändert, Golden-Bytes s1 = s2 wie am BASIS; Audio-Thread — `setNonRealtime` und die neuen Kernzeilen nur Atomics und eigener Zustand, der Callback nimmt `zustandSchloss` nicht mehr; Hostautomation mit Epoche und Ruhegrenze — `parameterValueChanged`, Takt und Ruhegrenze unverändert, Epochenfolge gleich dem BASIS-Golden; Blockbindung der Etappe 3 — `processBlock` und Blockrandlesung unverändert, `hostParameter` ist vor dem ersten Block gefüllt (`:264-269`) und vor den Blockrandgrenzen (`:272-282`); Besitzschleuse der Etappe 4 — Initialisierungsliste bis auf die entfernte APVTS, Destruktor und Provider unverändert.

## Geprüft / nicht geprüft

Geprüft: der vollständige Diff des Prüfbereichs samt der drei Änderungssätze; am Zielstand `setNonRealtime`, `dspKontrollTakt`, `prepareToPlay`, `releaseResources`, `reset`, `processBlock`, `setStateInformation`, `v3Status`, `hostParameterAnlegen`, Konstruktor und Destruktor der Sonde, `DspKern::verarbeite`, `verarbeiteStueck`, `blockrand`, `bereiteVor`, `freigeben`, `beendeAudiohistorie`, `beendeCandidate`, `beendeHoerHalt`, Bankpool-Freigabe; alle neuen und umgebauten Fälle in B7 und B2 mit ihren Hilfen; die Behauptungszeilen B2, B7, B31; die Golden-Dateien und das Szenario; die Wrapperaufrufe und die APVTS in der JUCE-Quelle, der VST3-Vertrag im SDK, der Szenario-Runner und der Zeitüberschreitungsweg des MCP-Clients; als Kontext die Rotbeweise, die Gegenproben-Angaben im Manifest und die Erzeugungsrohdatei.

Nicht geprüft: kein Bau, kein Test, kein Kanon, kein FL, kein Python — die Mutationsüberleber in H-2 sind gerechnet, nicht gefahren; ob FL `setupProcessing` neben `process` ruft und ob FL vor einem Render deaktiviert, ist nicht gemessen; die Ergebnisse von Kanon, Laufzeit-Arm und Rotläufen stammen aus den Rohdateien. Nach dem Auftrag ausgeschlossen: Manifest-, Register- und Planprosa, NAK-340 bis NAK-354, NAK-292, NAK-307, der Auslöser von LZ-1, `tools/fl/laufzeit.ps1`, die Entscheide als solche, Etappen 6 und 7, die Karten U48, U49, U51, U56, U57, Wrapper und Patch als Gegenstand, T3-12-*, der Kommentar in `eq-copilot/plugin/state/NakamaParameter.h`.

HEAD zu Beginn: `6ffc8578269a5cc0b4e7a2d08256d449131a7b95` (19:08:32). HEAD am Ende: `6ffc8578269a5cc0b4e7a2d08256d449131a7b95` (19:32:10 vor dem Schreiben dieses Berichts, nach dem Schreiben erneut gemessen, gleich). Beide mit `git rev-parse HEAD`; HEAD trägt über ZIEL hinaus nur den Prüfauftrag und `docs/PLAN-STAND.md`.

FERTIG Erstprüfung Etappe 5, PASS, 0 Defekte, 2 Lücken, 4 Härtungen.
