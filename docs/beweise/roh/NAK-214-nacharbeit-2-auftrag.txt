NAK-214 — Nacharbeit 2 nach der Wiederprüfung 1 (Etappe 2, Runde 2). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors, keine Legacy-Umbenennung. tools/dirigent/pruefliste.md ist deine Arbeitsliste. Sprache: Docs, Commits, Manifest Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks. Typografische Anführungszeichen in Rust-Strings schließen mit U+201C, nie mit ASCII-`"`.

LAGE: Die Wiederprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a08217-48f9-76e1-bd02-055ce6e596da, Stand 8261bc17) bestätigt V-32 als geschlossen und meldet nichts Gebrochenes; genau ein Rest bleibt — Urteil wörtlich in docs/beweise/roh/NAK-214-wiederpruefung-1-8261bc1.txt, Prüfauftrag in docs/beweise/roh/NAK-214-wiederpruefung-1-auftrag.txt. Der Dirigent hat ihn an der Quelle bestätigt. Du schließt genau diesen einen, sonst nichts. Kein Produktcode, keine Testauskunft im Produktcode.

DER BEFUND UND DIE REGEL, DIE IHN SCHLIESST:

DEFEKT V-34 — die Vorbedingung „kein Sitzungseintrag" ist nicht unterscheidend gemessen. Prüfer: „In `user_verdict_ohne_sitzungsbefunde_wird_abgewiesen` unterscheidet diese Vorbedingung nicht zwischen fehlendem Eintrag und vorhandener leerer Liste: `befunde_der_sitzung` ruft `befunde_sicht` auf, dessen `unwrap_or_default()` beide Fälle zusammenführt (`hypothese_verdrahtung.rs:1037`). Auch `Some(vec![])` würde die Assertion erfüllen. Der aktuelle Aufbau erreicht zwar den richtigen Zweig, aber die bindende Nacharbeitsregel verlangt ausdrücklich die Prüfung des fehlenden Sitzungseintrags im Test." Bestätigt: `broker/tests/sonde014_verdrahtung.rs:3489–3494` prüft `befunde_der_sitzung(&c).is_empty()` und begründet den fehlenden Eintrag im Kommentar statt ihn zu messen; `befunde_sicht` (`hypothese_verdrahtung.rs:1031–1038`) liefert für `None` und für `Some(vec![])` dasselbe; `Stand` ist `pub(super)` (`zustand.rs:286`) und `Coordinator.stand` privat (`mod.rs:193`) — ein Integrationstest unter `broker/tests/` kann `stand.befunde` nicht lesen. REGEL (Dirigent, 08.09.2026): Die unterscheidende Zusicherung wandert in den Crate — wie V-12 (a) seit der Matrixnacharbeit 3 in `proposal_verdrahtung.rs::tests` liegt. Ein Unit-Test im `#[cfg(test)] mod tests` des Moduls, das den `Coordinator` und `stand` erreicht (Muster `proposal_verdrahtung.rs:526`; das Modul wählst du an der Quelle — `befehl.rs` oder `hypothese_verdrahtung.rs`), baut dieselbe Lage (Main angemeldet, State gemeldet, keine Evidenz), prüft VOR dem Urteil ausdrücklich `stand.befunde.get(&session).is_none()` (nicht `is_empty()` auf einer Sicht), sendet dann `user_verdict` mit neuer `command_id` und wohlgeformter unbekannter `finding_id` und prüft `abgelehnt`/`unknown_target` sowie keine Persistenz — mit Store, falls das Muster der Store-Fälle es verlangt (`#[cfg(windows)]` wie im Bestand), sonst über den Rückgabewert und `command_event_lesen`. Der bestehende Integrationstest bleibt als Lauf über den Produktpfad; die Matrixzeile V-34 nennt beide (Datei::Fall) und sagt, welcher die Unterscheidung `None` gegen `Some(vec![])` trägt. Keine öffentliche Testauskunft, kein neues `pub`, keine Änderung an `befunde_sicht`. Rotbeweis: `NAK-214-rot-V-34.txt` bekommt einen zweiten Abschnitt — die Mutation `map_or(true, …)` (oder eine Mutation, die den `None`-Zweig als „existent" behandelt) macht den Unit-Test rot; zusätzlich die Gegenprobe, dass ein von Hand eingetragenes `Some(vec![])` die Vorbedingung des Unit-Tests NICHT erfüllt (die Zusicherung schlägt an) — damit ist die Unterscheidung selbst belegt.

BEINE: Nur A4 (`cargo test --manifest-path broker/Cargo.toml`, gezielt das Modul und danach das ganze Broker-Bein). Kein voller Kanon in dieser Runde; den Abschlusskanon setzt der Dirigent.

MANIFEST docs/beweise/NAK-214.md: §5 Verlauf (append-only) „Wiederprüfung 1 — NEEDS_WORK (08.09.2026, Stand 8261bc17)" mit Thread, Modell, Effort, Rohdatei, Einordnung (V-32 geschlossen, V-34 Rest) und darunter „Nacharbeit 2"; Kopftabelle: Zeile „Phase" auf „Nacharbeit 2 nach der Wiederprüfung 1 (NEEDS_WORK, ein Restbefund) — 08.09.2026", Zeile „Änderungssatz — Nacharbeit 2"; Prüfliste §3 nachziehen; V-34 in §2.4 mit beiden Tests; die Urteilsmarke T1 PASS bleibt.

SELBSTAUDIT vor dem Commit: `is_none()` auf `stand.befunde.get(&session)`, nicht auf einer Kopie; der Unit-Test hält die Sperre nur kurz und nicht während `user_verdict` (Deadlock); `command_id` neu; kein Produktcode; keine Manifestsätze, die mehr behaupten als der Test misst. Dann Commit mit Pathspec (die geänderte Quelldatei des Unit-Testmoduls, broker/tests/sonde014_verdrahtung.rs falls berührt, docs/beweise/NAK-214.md, docs/beweise/roh/NAK-214-rot-V-34.txt), Betreff „NAK-214 Nacharbeit 2: …", pull --rebase, push, STOPP. Die Wiederprüfung 2 setzt der Dirigent.
