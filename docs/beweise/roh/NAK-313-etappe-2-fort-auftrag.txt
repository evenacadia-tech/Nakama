NAK-313 — Etappe 2, Fortsetzung: M-12b (E-313-18, ehrlicher Zustand des Panels nach einer Abweisung). Auftrag des Dirigenten, 24.09.2026.

ROLLE. Du bist der Fortsetzungs-Bauer der Etappe 2 des Tickets NAK-313 (Planschritt S25l) im Repo C:\Users\phili\Projekte\Nakama (Branch master, sichtbarer Checkout, Windows 11, pwsh). Die Etappe 2 ist bis cd9c97a6 gebaut, belegt und im Kanon grün (Manifest docs/beweise/NAK-313.md §19); der Dirigent hat in §20 die Lücke E-313-18 entschieden und die Matrixzeile M-12b in §6.1 ergänzt. Du baust genau M-12b, belegst sie, fährst B15 einzeln und den vollen Kanon nachher auf dem End-Stand selbst und übergibst nie NOT RUN. Ein frischer Prüfer prüft danach den Diff der ganzen Etappe (9bf629bf...HEAD); nie du selbst.

VERBINDLICHE QUELLEN, in dieser Reihenfolge lesen:
1. docs/beweise/NAK-313.md: §20 vollständig (Regel E-313-18 mit Begründung), §6.1 die Zeilen M-12 und M-12b (die neue Zeile ist deine Spezifikation: Vorbedingung, Auslöser, Zusage, Reihenfolge, Test, Rotbeweis je Hälfte), §8.1 (Rotbeweis-Ablauf, Zeilenendenregel), §8.2 (Absatz zur Statuszeile), §19.2 (was die Etappe an PluginEditor.cpp gebaut hat), §19.3 (Muster der Rohdateien und der Tabelle), §19.6 (Abweichungen A-E2-1 bis A-E2-9, besonders A-E2-9 zur A32-Ratsche und A-E2-6 zum Schalter der Gegenprobe), §11.4 R-313-11, R-313-12, R-313-15.
2. docs/beweise/roh/NAK-313-etappe-2-auftrag.txt: alle dort genannten Regeln gelten weiter (Präzisierungen des Dirigenten, Landminen, Zeilenenden, Git, Schlussform); dieser Auftrag verengt nur die Grenze.
3. CLAUDE.md und tools/dirigent/pruefliste.md.
4. Den Code an HEAD: eq-copilot/plugin/src/PluginEditor.cpp (grenzMeldung, MesspunktPanel mit Merkwerten, uebernehmen() mit dem Abweisungszweig, paarSichtbarkeit; Zeilen an HEAD nachmessen), eq-copilot/plugin/tests/ShotTestMain.cpp (panelLauf, die Fälle 313/M-12 als Muster, der Schalter NAK313_GEGENPROBE_BASISSTAND, die Zählung in nak312Messpunktpanel und die Summenzeile), tools/beweise.ps1 (Behauptung B15).

TICKETGRENZE DIESER FORTSETZUNG. Du änderst nur: eq-copilot/plugin/src/PluginEditor.cpp (Abweisungszweig von uebernehmen(); zeigeMesspunkt bleibt unter 200 Zeilen, sonst Helfer außerhalb wie grenzMeldung), eq-copilot/plugin/tests/ShotTestMain.cpp (neuer Fall 313/M-12b und die Fallzählung), tools/beweise.ps1 (nur die Behauptungszeile B15, nur falls der Satz den neuen Fall braucht: „nach einer Abweisung fällt die Rollenauswahl auf die gespeicherte Rolle zurück und die Meldung nennt es“), docs/beweise/NAK-313.md (append-only am Dateiende als §21, dazu die Kopfzeilen Etappe und Kanon nachher), docs/beweise/roh/NAK-313-* (Rohdateien). Nicht angefasst: eq-copilot/plugin/src/PluginProcessor.h und eq-copilot/plugin/src/prozessor/State.cpp (die API bleibt, wie sie ist), eq-copilot/plugin/tests/StateMigrationTestMain.cpp, alles, was der Etappe-2-Auftrag ausschließt. Kein FL Studio, keine Installation, kein Laufzeit-Arm.

PRÄZISIERUNGEN DES DIRIGENTEN:
- Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.
- Der Rückfall geschieht mit juce::dontSendNotification und löst keinen zweiten Aufruf aus; die Zählerdifferenz um die Rollenwahl ist genau 1 (Messung mit testzugang::bindungsaufrufeFuerTest() unmittelbar vor und nach setSelectedId (4, juce::sendNotificationSync)).
- Der Zusatz „The role change was not saved either.“ steht nur, wenn der abgewiesene Aufruf eine Rolle trug; M-12 (nur Label) bleibt wörtlich, wie es ist, und seine Fälle bleiben grün.
- Nach dem Rückfall folgt paarSichtbarkeit() (Paarfeld deaktiviert bei Rolle 1); die Textfelder behalten den Text des Users.
- Die Statuszeile nur bei lebendem Editor über den vorhandenen Weg (statusMeldung, statusMeldungBisMs, uiDirty), wie §19.2 es gebaut hat; kein neues Element (M-16: 7 Kinder bleiben).
- FeatureEngine-Objekte und Prozessoren in Tests auf den Heap (NAK-175).

ABLAUF.
1. Start: git pull --ff-only; git rev-parse HEAD (erwartet cd9c97a6 oder ein Nachfolger nur mit docs/**); git status --short (nur die zwei fremden Ordner); kein fremder Bau- oder Testprozess (Get-Process cmake, MSBuild, cl, link, cargo, EqCop*, FL64; NAK-290). Miss git diff --stat 9bf629bf HEAD -- eq-copilot broker tools und nenne ihn im Manifest.
2. Gegenprobe „heute rot“: schreibe den Fall 313/M-12b zuerst, baue nur EqCopShot, fahre ihn gegen den unveränderten Produktcode (Auswahl bleibt 4, Meldung endet nach „not saved.“) und halte das Ergebnis in docs/beweise/roh/NAK-313-rot-M-12b.txt fest (§8.1; Schalter NAK313_GEGENPROBE_BASISSTAND nach A-E2-6, falls der Fall am Basisstand anders gebaut werden muss).
3. Reparatur nach E-313-18 (§20, M-12b Spalte Reihenfolge). C++ aus pwsh bauen (cmake --build eq-copilot/build --config Release --target EqCopShot), Zeitstempel des Binaries gegen die Quelle prüfen (NAK-230), B15 einzeln fahren (EqCopShot --sonde012-suite <ordner>), Exit und Zählung ins Manifest. Zeilenenden: PluginEditor.cpp und ShotTestMain.cpp LF, tools/beweise.ps1 CRLF, Manifest LF; vor jedem Schreiben git ls-files --eol lesen.
4. Rotbeweise je Hälfte nach §8.1 in derselben Rohdatei (Abschnitt je Hälfte): den Rückfall entfernen → Auswahl bleibt 4 → rot; dontSendNotification durch sendNotificationSync ersetzen → Zählerdifferenz 2 → rot; den Zusatz weglassen → Meldungsvergleich rot; je Mutation SHA-256 vorher, Sicherung außerhalb des Repos, Mutation wörtlich, Kette Mutation → Vektor → Beobachtung, nur der betroffene Fall, gefallene und mitgefallene Prüfungen, bytegleiche Rücknahme mit SHA-256, Gegenlauf grün. Mutationsdateien wie in §19.3 (docs/beweise/roh/NAK-313-e2-mutation-<Kürzel>.txt).
5. Commit mit Pathspec (Reparatur, Test, Behauptung; Rohdateien), Betreff „NAK-313 Etappe 2: M-12b — Rollenrückfall und Meldung nach einer Abweisung (E-313-18)“.
6. Kanon nachher, voll, abgekoppelt, auf dem End-Stand; währenddessen kommt nichts in den Worktree:
   $log = Join-Path $env:TEMP 'nakama-nak313-e2fort-kanon.log'
   $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-313.md -Anhaengen -Titel 'NAK-313 Etappe 2 Fortsetzung' *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
   Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl
   Fertig, wenn die letzte Logzeile mit EXIT= beginnt. Warte mit einem Hintergrundkommando (until-Schleife auf die EXIT=-Zeile, 60 Sekunden Takt), nie mit Gesprächsrunden; beim Warten auf den Kanon einen abgelaufenen Wächter neu setzen und bei jeder Rückkehr zuerst das Log lesen (NAK-354). Landminen wie im Etappe-2-Auftrag (vctip.exe nach „Baue:“, NAK-300; flatc, NAK-280; Exit 124 nach 60 Minuten). Erwartet GRUEN; A32 und A33 nennst du mit Zahlen; A32 darf nicht über den geerbten Stand steigen (33 Funktionen über 200 Zeilen). Den vom Runner angehängten Manifestabschnitt, die Rohausgabe unter docs/beweise/roh/ und eine vom Bauschritt geschriebene Hashzeile in eq-copilot/install/nakama-installer-v1.json committest du mit Pathspec.
7. Selbstaudit: Diff cd9c97a6..HEAD adversarial lesen (Rückfall nur im Abweisungszweig; kein Rückfall bei Erfolg; Merkwerte unverändert bei Abweisung; kein Zugriff auf ed nach dem API-Aufruf außer über den SafePointer; M-12-Fälle grün; Behauptung B15 nicht weiter als die Messung). Prüfliste abhaken.

LIEFERUNG IM MANIFEST (append-only am Dateiende hinter §20, neue Überschrift „## 21. Etappe 2, Fortsetzung — M-12b (Bauer, <Datum>)“; der Kanonabschnitt des Runners steht, wo der Runner ihn anhängt): Startmessung (HEAD, Status, Produktdiff seit 9bf629bf); Änderungen je Datei in Sätzen; Tabelle M-12b (Hälften mit Etikett, Ergebnis am Stand cd9c97a6, Rotbeweis je Hälfte, Rohdatei, „fällt an der Zusagezeile: JA/NEIN“); B15 einzeln mit Exit und Zählung; Kanon nachher (Urteilszeile, Rohdatei, A32/A33-Zahlen); Abweichungen von §20 und M-12b mit Begründung; Prüfliste; offene Punkte. Kopf: Zeile Etappe bekommt als letzten Fettsatz „Etappe 2 von 9 gebaut auf <kurz-SHA>; Erstprüfung 2 steht aus“ (höchstens 70 Zeichen); Zeile Kanon nachher nennt den neuen Lauf zuerst und die zwei Läufe aus §19.8 als Verlauf. Die Zeilen Klasse, Urteil, Basis-SHA, Kanon vorher, Laufzeit-Arm und Ticketpfade bleiben.

GIT. Wie im Etappe-2-Auftrag: nie git add -A, nie --amend, nie reset --hard, nie clean, nie stash; Commits nur mit Pathspec über eine Nachrichtendatei (git commit -F <datei> -- <pfade>); Betreff beginnt mit „NAK-313 Etappe 2: …“, Abschluss „Co-Authored-By: Claude Opus <noreply@anthropic.com>“; keine Backticks in Shell-Befehlen; vor jedem Push git pull --rebase, dann git push origin master als eigener Schritt; docs/PLAN-STAND.md nicht anfassen. Am Ende: git status --short zeigt nur die zwei fremden Ordner, HEAD = origin/master, keine Mutation im Baum, Kanon nachher GRUEN auf HEAD.

SCHLUSSMELDUNG, höchstens zwölf Zeilen: HEAD am Start und End-SHA, Commits, M-12b je Hälfte JA oder NEIN an der Zusagezeile, B15 Exit und Zählung, Kanon nachher (Urteilszeile, Rohdatei, A32/A33), Abweichungen, offene Punkte. Beende dich mit einer Zeile: FERTIG Etappe 2 Fortsetzung, M-12b <JA/NEIN>, Kanon <URTEIL>, Commit <sha>.
