NAK-214 — Etappe 2: Bau nach der geprüften Matrix. Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis dieser Etappe = `git rev-parse HEAD` beim Start (der Commit, der diese Auftragsdatei und die Rohdateien der Matrixprüfung 4 trägt); trage sie in die Kopftabelle von docs/beweise/NAK-214.md ein (Zeile „Etappe 2 — Startstand"). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors, keine Legacy-Umbenennung. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §3 nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits, Manifest Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks. Typografische Anführungszeichen in Rust-Strings schließen mit U+201C, nie mit ASCII-`"`.

LAGE: Die Matrix in docs/beweise/NAK-214.md §2 ist geprüft. Matrixprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a080a4-8e6d-7fe0-9fc6-53bfce5d210e, Stand d0f2f304): NEEDS_WORK, acht Befunde → Matrixnacharbeit 1 (b204bbcb, R6 bis R8). Matrixprüfung 2 (Thread 01a080d5-38de-7961-84fc-5aa8f028602a, Stand b204bbcb): NEEDS_WORK, sechs geschlossen, zwei Restbefunde → Matrixnacharbeit 2 (87ee76f5, V-49, R7-Präzisierung). Matrixprüfung 3 (Thread 01a080f3-cc76-7fd3-81fe-ceeca7f7ea69, Stand 87ee76f5): NEEDS_WORK, ein Restbefund V-12 → Matrixnacharbeit 3 (c3878915, V-12-Regel). Matrixprüfung 4 (Thread 01a08106-d4aa-7113-a024-437746053272, HEAD vor und nach dem Lauf c3878915): URTEIL: PASS — der Befund ist geschlossen, nichts gebrochen (docs/beweise/roh/NAK-214-matrixpruefung-4-c387891.txt). Damit ist Etappe 1 abgenommen: §2 (E1 bis E9, V-01 bis V-49, §2.6 drei Lückenstellen, §2.7, §2.8 „kein Fassungsschritt", §2.9) plus die Dirigentenregeln R6, R7, R8 (docs/beweise/roh/NAK-214-matrixnacharbeit-1-auftrag.txt), die R7-Präzisierung (NAK-214-matrixnacharbeit-2-auftrag.txt) und die V-12-Regel (NAK-214-matrixnacharbeit-3-auftrag.txt) sind ab jetzt die Spezifikation. Was §2 nicht spezifiziert, wird nicht gebaut (§2.9 gilt). ERSTER COMMIT der Etappe 2: Matrixprüfung 4 PASS in §5 Verlauf und in die Kopftabelle eintragen (Phase „Etappe 2 — Bau", Prüfstufe weiter offen bis zum Kanon), Pathspec docs/beweise/NAK-214.md.

BAU: genau nach dem Abschnitt BAU in docs/beweise/roh/NAK-214-auftrag.txt und nach der Matrix. Lies vor dem ersten Codeschritt docs/beweise/NAK-213.md §2 (E7/R4 führendes Main, R8 Enthaltung ohne Ort — nur nutzen, nie ändern), docs/beweise/NAK-212.md §2 (N-43) und die aktuellen Quellzeilen an HEAD. Reihenfolge nach deinem eigenen Bauplan; empfohlen, weil jeder Teil für sich abnehmbar ist: (A) Der Proposal-Riegel — `gate_felder_vollstaendig` als produktiver Schlussriegel am Ende von `proposal()`, die Paarbildung als eigene Funktion in `proposal_verdrahtung.rs` (E1, R1; V-01 bis V-13; Tests `sonde014_proposal.rs`, `sonde014_verdrahtung.rs`, Unit-Testmodul in `proposal_verdrahtung.rs`); (B) Der Wirkungsbeleg — Strukturprädikat mit vier Punkten (E2, E3, R6), `prepost_wirkungsbeleg` neben `prepost_paar` (E4), `next_test` aus der Wirkungsmarke (E5), Klassenmeldung in `prepost.rs` nur lesend (V-14 bis V-22, V-45; `hypothese.rs` nur die Aussageklasse, Ursachenklasse aus der Paarmarke und `next_test`; `sonde014_verdrahtung.rs`, `sonde013_prepost.rs`); (C) Gen — `controlEnde`, Rücknahme ohne Folge-Snapshot → alle Befunde `stale`, `zaehleOffeneFindings()` neu (E6, R3, R8; V-23 bis V-29; `SourcesModel.cpp`/`.h`, Tests `Sonde014BefundTest.cpp` B28 und `Sonde012SourcesModelTest.cpp` B13 — C++-Beine aus pwsh bauen, `cmake` fehlt im Bash-PATH, Binary-Zeitstempel gegen den Fix prüfen); (D) `user_verdict`-Riegel hinter dem Idempotenzcheck mit `unknown_target` (E7, E8, R4; V-30 bis V-36; `befehl.rs` nur `user_verdict_p0` und die Vorbedingung in `persistenz_p0_intern`; `sonde014_verdrahtung.rs`, Store-Fälle `#[cfg(windows)]` wie im Bestand); (E) Die Passage der Aufnahme — Filter vor `max_by_key(folge)` mit drei Ausschlussgründen, Taintmenge aus offenen Interventionen UND der geretteten Nachlaufmenge über alle Taintstände, das interne Feld am `Taintstand` samt Retten beim Interventionsende und Leeren bei Nachlaufende/Resync, Terminal lässt es unverändert (E9, R5, R7; V-37 bis V-44, V-46 bis V-49; `experiment.rs` nur `juengste_passage_im_projekt`, `hypothese_verdrahtung.rs` nur die Passagenwahl, `intervention.rs` nur Retten/Leeren, `zustand.rs` nur das Feld; `sonde013_experiment.rs`, `sonde013_taint.rs`, `sonde014_verdrahtung.rs`); (F) Die drei Lückenstellen aus §2.6 — Kettenbein A31 fährt die drei Fälle (zwei Mains → `Vorschlaege: 0`; `Probable`-Paar → Klasse 1; jüngste Passage untauglich → ältere) mit gedruckter Ausgabe, der aufruferlose Helfer `luecke()` wird gerufen oder entfernt, Runnerkopf/Modulkopf/Manifestkopf nachgezogen („geänderte Zusage: drei Stellen"), `offene_luecken` in `pruefe_p5_korpus.py` bleibt für NAK-214 leer (`sonde014_gegenbeispiele.rs`, `tools/beweise.ps1` nur die A31-Behauptung, `erzeuge_p5_korpus.py`/`pruefe_p5_korpus.py`/`fixtures/p5-korpus/` nur für die NAK-214-Fälle). Je Etappe: betroffene Beine grün, ein logischer Commit mit Pathspec, Manifestabschnitt (Bauetappe, gemessene Beine mit Zahlen, Rotbeweise unter docs/beweise/roh/NAK-214-rot-<zeile>.txt je Matrixzeile mit Rotbeweis, Abweichungen von der Matrix mit Begründung, Nebenbefunde nummeriert ab NB-6 — nie ins Register).

BEINE UND WERKZEUGE: A4 `cargo test --manifest-path broker/Cargo.toml`; A5 `py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py --abdeckung` und A8 `py -3.13 tools/eq-copilot/erzeuge_v3_fixtures.py --pruefen` als Nachweis, dass der Vertrag unberührt bleibt (§2.8: kein Fassungsschritt — die Beine müssen ohne Änderung grün bleiben); A28 `erzeuge_p5_korpus.py --pruefen`, A29/A30 `pruefe_p5_korpus.py`, A31 Kettenbein `sonde014_gegenbeispiele.rs`; B3c, B10, B13, B28 für die C++-Seite. Die genauen Aufrufe stehen in tools/beweise.ps1; nicht aus dem Gedächtnis rekonstruieren. LANDMINEN (CLAUDE.md): `cmake` liegt nicht im Bash-PATH — C++-Beine aus pwsh bauen; MSVC-Stack 1 MiB — große Engine-Objekte in Tests auf den Heap; Bash-Heredocs verändern Backslashes; bytegleiche Fixtures brauchen passende .gitattributes-Regeln. Determinismus M-25; NaN-Ehrlichkeit an der Paarnaht (V-15); exakt-null gegen nicht-null ohne neue Schwelle (R6).

TICKETGRENZE unverändert (docs/beweise/roh/NAK-214-auftrag.txt, Abschnitt BETROFFENE PFADE / NICHT anfassen, plus die in der Matrix ergänzten Pfade `intervention.rs` und `zustand.rs` nur für R7, und die Paarbildungsfunktion in `proposal_verdrahtung.rs`): Sicherheitsklasse, Zusammenhangsgröße, Screening, Deckel, Konkurrenzlage, Master-Wahl, Kanaltafel, Kanalwechsel (NAK-212/213) nur nutzen; `mitgliedschaft.rs`, `invalidierung_verdrahtung.rs`, `liveness.rs` nicht anfassen; `prepost.rs` außer der Klassenmeldung nicht; Audio- und State-Pfade des Plugins nicht; keine neue Optik in Gen (S31b — nur der Zustand `stale` und der Zähler); `eq-copilot/schemas/v3/**` und die Vertragswerkzeuge nicht (§2.8); `design/`, `docs/plan/`, `docs/offene-punkte.md`, `docs/PLAN-STAND.md`: nein.

KANON am Ende, abgekoppelt auf sauberem Baum, nach dem letzten Bau-Commit (alle Rohbelege und Manifestabschnitte vorher committet, sonst trägt der Rohbeleg die -dirty-Marke):
  $log = Join-Path $env:TEMP 'nakama-nak214-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-214.md -Anhaengen -Titel NAK-214 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
Fertig, wenn die letzte Logzeile mit EXIT= beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); die Rohausgabe landet unter docs/beweise/roh/NAK-214-<sha>.md, das Manifest bekommt den Kanonabschnitt. Kein Stand mit NOT RUN wird übergeben. Ist der Kanon rot: Ursache im Manifest benennen, beheben, Kanon erneut — nie einen roten Stand als fertig melden. Nach grünem Kanon: Urteilsmarke `<!-- NAKAMA-URTEIL: T1 PASS 2026-09-08 -->` (oder das tatsächliche Datum) in den Manifestkopf, Kopftabelle nachziehen (Kanon nachher mit Beinzahl, Testanzahl gemessen, Änderungssatz, Prüfliste §3 abgehakt mit Fundstelle, §2.6 mit gemessener Ausgabe je Fall), Commit mit Pathspec (docs/beweise/NAK-214.md, docs/beweise/roh/NAK-214-*), `git pull --rebase` bei sauberem Worktree, push, danach STOPP. T2/T3 setzt der Dirigent nach der Erstprüfung.

SELBSTAUDIT vor jedem Commit (Ticketauftrag): Zahlenränder (leerer `target`, sechs Felder einzeln, zwei Mains, kein Main, Paar mit exakt-null-Delta und exakt-null-Gain, NaN-Band neben Nullband, `finding_id` mit 32 Hexzeichen aber unbekannt, Nachlauf 0 gegen 1 Sample, Terminal während Nachlauf, Zähler 0/1/2, Reconnect mit gleicher gegen neue Epoche, herausgefilterter Befund an Position 1/2/3), Determinismus (M-25), Vertragssymmetrie (kein neues Leitungsfeld; A5/A8 unverändert grün; C++-Leser liest `unknown_target` bereits), Save/Load nicht betroffen, Audio-Thread nicht betroffen, keine irreführenden Kommentare oder Manifestsätze (Modulkopf `sonde014_gegenbeispiele.rs`:30 und Runner-Behauptung nachziehen), keine sichtbare Behauptung ohne Beleg (Gen: `stale` und Zähler 0 sind ehrliche Zustände). Beziehungen: Befund ↔ Proposal ↔ Rückweg, verbinden ↔ trennen (`controlEnde` ↔ Reconnect ↔ Snapshot), Rücknahme ↔ Folge-Snapshot, `user_verdict` ↔ Existenz des Befunds, Passage ↔ Taint/Nachlauf/Rücknahme, Interventionsende ↔ Nachlaufende/Resync.
