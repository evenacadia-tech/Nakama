# NAK-212 — G5-Nacharbeit 1: Sicherheit `hoch` braucht Zusammenhang; Gates messen Überlappung und Passage

| Merkmal | Wert |
|---|---|
| Ticket | NAK-212 (Register `docs/offene-punkte.md`), Nacharbeit zum Phasengate **G5** (`docs/beweise/G5.md` §1, §7, §8, §11 Nr. 1) |
| Phase | **Etappe 2 (07.09.2026) — Bau nach der abgenommenen Matrix, läuft.** Die **Matrixprüfung 3** (Codex `gpt-6-astra`, max, lesend, Thread `01a07d06-735c-7493-8681-02a0d2fc2ecd`, HEAD vor und nach dem Lauf `740de80`, Produktdiff gegen `4df8855` leer) urteilt **PASS** — D7 geschlossen, nichts gebrochen. **Damit ist Etappe 1 abgenommen: §2 (E1 bis E9, N-01 bis N-47, §2.5.1 bis §2.8) ist die Spezifikation dieses Tickets.** Drei Prüfrunden: Matrixprüfung 1 **NEEDS_WORK** (sechs Befunde D1 bis D6) → Nacharbeit 1; Matrixprüfung 2 **NEEDS_WORK** (ein Befund D7) → Nacharbeit 2; Matrixprüfung 3 **PASS**. Stand davor: **Matrixnacharbeit 2 — der eine Defekt der Matrixprüfung 2 geschlossen.** Die Matrixprüfung 2 (Codex `gpt-6-astra`, max, lesend, Thread `01a07cf7-06a2-7331-8e13-6c033d726a59`, HEAD vor und nach dem Lauf `66f578e`) bestätigt **D1 bis D6 als geschlossen** und meldet **NEEDS_WORK** mit genau einem neuen Befund: **D7** — der positive Kontrollfall N-17 widersprach der neuen E6-Regel. Geändert sind E6 (ein Satz zum Kontrollfall), N-17 (neuer Wortlaut) und die neue Grenzfallzeile **N-47**. Nächster Schritt: Matrixprüfung 3. Stand davor: **Matrixnacharbeit 1 — sechs Defekte der Matrixprüfung 1 geschlossen.** Die Matrixprüfung 1 (Codex `gpt-6-astra`, max, lesend, Thread `01a07cd6-8e07-72f2-9d16-f9e1a688c01a`, HEAD vor und nach dem Lauf `2e05a95`) urteilte **NEEDS_WORK** mit sechs Defekten (§5). Geändert sind E4, E5, E6, E8, ein neuer Entscheid E9 und sechs neue Zeilen N-41 bis N-46; die Fassung Etappe 1 ist an jeder geänderten Stelle als solche kenntlich. Nächster Schritt: Matrixprüfung 2. Stand davor: **Etappe 1 — Verhaltensmatrix geschrieben** (8 Entscheide, 40 Zeilen, 27 Korpussitzungen). Keine Zeile Produktcode, kein Test, keine Fixture in beiden Etappen. |
| Prüfstufe | T1 (Bau und Kanon), danach Wiederprüfung nach Vorlage B mit frischem Codex-Thread; T2/T3 setzt der Dirigent. |
| Basis-SHA | `cbc816ad2965a8a666f5ec3c71ba7a1098b409b5` (= `git rev-parse HEAD` beim Start der Etappe 1, Worktree sauber) |
| Etappe 2, Startstand | `740de80171a94e4693173d0428d171c717281f3f` (Commit der Matrixnacharbeit 2, von der Matrixprüfung 3 mit **PASS** abgenommen). Produktdiff gegen den Kanonstand `4df8855` beim Start **leer** — der Bau beginnt auf dem beglaubigten Codestand. Bauauftrag `docs/beweise/roh/NAK-212-etappe-2-auftrag.txt` |
| Kanon vorher | `4df8855` **GRÜN 60/60** (`docs/beweise/roh/SONDE-014-4df8855.md`). `git diff --stat 4df8855..cbc816a -- broker eq-copilot tools` ist **leer** — der gemessene Codestand des Kanons gilt für den Basis-SHA unverändert; die drei Commits dazwischen berühren nur `docs/`. |
| Regeln | R1 bis R6 wörtlich in `docs/beweise/G5.md` §11 Nr. 1; der Ticketauftrag des Dirigenten liegt unter `docs/beweise/roh/NAK-212-auftrag.txt` |
| Befundquellen | `docs/beweise/roh/G5-gegenbeispiele-bf92891.md` (G-D1, G-D5, G-H3, G-L1; 27 gefahrene Fälle), `docs/beweise/roh/G5-gegenbeispiele-bf92891.patch` (12 Korpussitzungen, Kettenbein mit 15 Eingaben — **nicht committet**, Grundlage für R6), `docs/beweise/roh/G5-gegenbeispiele-bf92891/` (Rohausgaben, u. a. `p5-korpus-ergebnis.json`), `docs/beweise/roh/G5-eigener-lauf-bf92891.md` (E-D2, E-D3, E-D4, E-L3, E-H3, E-L7), `docs/beweise/roh/G5-codex-astra-falsifikation-bf92891.txt` (A1, A4) |
| Zugeordnete Befunde (Ticketgrenze) | G-D1, G-D5, G-H3, G-L1, E-D2, E-D3, E-D4, E-L3, E-H3, A1, A4, E-L7 (nur M-69) |
| Ticketpfade | `broker/src/coordinator/hypothese.rs`; `broker/src/coordinator/experiment.rs` (nur `bootstrap_p`-Umfeld, R2); `broker/src/coordinator/hypothese_verdrahtung.rs` (nur R5); `broker/tests/sonde014_hypothese.rs`; `broker/tests/sonde014_p5_korpus.rs`; `broker/tests/sonde014_gegenbeispiele.rs` (**neu, anzulegen**); `tools/eq-copilot/erzeuge_p5_korpus.py`; `tools/eq-copilot/pruefe_p5_korpus.py`; `eq-copilot/fixtures/p5-korpus/` (bytegleich über den Erzeuger, `.gitattributes`); `tools/beweise.ps1` (ein neues Bein A31); **bedingt, nur bei Ausgang A des `metrics_version`-Schritts aus E8 und nur zusammen:** `broker/src/coordinator/vergleichbarkeit.rs`:149, `broker/src/coordinator/prepost.rs`:206, `eq-copilot/plugin/core/analysis/FeatureEngine.h`:783 und `eq-copilot/schemas/v3/metriken-v1.json`. Die drei erstgenannten stehen nicht in der Auftragsliste; A5 prüft alle vier gegen `metriken-v1.json.aktuell`, ein Schritt ohne sie fällt zwingend rot. `FeatureEngine.h`:783 ist die in der Ticketgrenze vorgesehene Plugin-Ausnahme („ein Plugin-Test bricht nachweislich durch eine Vertragsänderung") — berührt wird ausschließlich diese eine Zeile. Begründung und Messung in §2.0 E8 |
| Nicht angefasst (Grenze) | Die geschlossene Menge der Ausschlussgründe, der Kandidatendeckel, die Master-Wahl und die Parent-Erkennung (NAK-213); Proposal, Paarnaht `prepost_paar`, `befehl.rs`, Gen-Seite (NAK-214); `eq-copilot/plugin/`; `design/`, `docs/plan/`, `docs/offene-punkte.md`, `docs/PLAN-STAND.md`, `docs/NEXT-SESSION.md` |
| Prüfmodell | Codex `gpt-6-astra`, Effort max, read-only — Matrixprüfung vor dem Bau, Wiederprüfung nach dem Bau (Sol max nur Gegenprüfer) |

---

## 1. Gate-Bezug, wörtlich

**Gate-Text G5** (`docs/bauaufteilung-sonden.md`:395–400): „**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8." — „G5 ist das ungewöhnlichste Gate: Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer."

**P5-Exit-Gate, Satz 3** (Entwurf §59, `docs/FL-Nakama-Sonden-Design-Entwurf.md`:4322–4324): „Gegenbeispiele erzwingen nachweislich Enthaltung."

**Entwurf §36.1** (`:2354–2365`), Aussageklasse 1: „**Zusammenhang:** Quelle und Masterproblem treten im selben Bereich und Zeitfenster auf." — Das ist der Satz, den dieses Ticket im Datenweg herstellt: „im selben Bereich" ist die Bandpassung, „im selben Zeitfenster" ist die paarweise Überlappung, und der **Zusammenhang** selbst ist der bedingte Uplift beziehungsweise die Onset-Koinzidenz.

**Entwurf §36.2** (`:2367–2386`): „Der Rang wird aus hart gegateter Vergleichbarkeit und getrennten Komponenten gebildet … Keine einzelne hohe Komponente darf fehlende Coverage oder falsches Alignment kompensieren."

**Matrixzeilen** SONDE-014 §3: M-15, M-19, M-20, M-21, M-22, M-23, M-25, M-26, M-31, M-64 bis M-69, M-87 (§2.7 sagt je Zeile, was unverändert gilt und was dieses Ticket präzisiert).

**Invarianten** (`CLAUDE.md`): „NaN-Ehrlichkeit", „Engine kennt keine Optik", „Keine toten UI-Elemente" (hier: keine Sicherheitsklasse, die strukturell unerreichbar oder strukturell trivial ist), „Schemas sind Verträge".

---

## 2. Verhaltensmatrix

### 2.0 Die gewählten Wege — ein Entscheid je Regel, mit Begründung

Die sechs Regeln R1 bis R6 lassen jeweils mehrere Mittel zu. Acht Entscheide legen die Mittel fest; alles Weitere in §2.1 bis §2.6 folgt aus ihnen. **Was hier nicht steht, wird nicht gebaut.**

---

#### E1 — Der Zusammenhang bekommt eine eigene, vorzeichenbehaftete Größe. Die sechs Rangkomponenten bleiben unberührt.

**Der Befund dahinter:** Rang und Klasse lesen heute **disjunkte** Größen. Die sechs Rangkomponenten messen den Zusammenhang und fließen ausschließlich in `confidence.score` — also nur in die Reihenfolge. Die Klassenwahl (`hypothese.rs`:1387–1396) liest davon **keine einzige**.

**Der Weg:** Eine neue, reine Funktion in `hypothese.rs`

```rust
/// Der gemessene Zusammenhang zwischen Kandidat und Master — MIT Vorzeichen.
pub struct Zusammenhangsbeleg {
    /// Bedingter Uplift, normiert wie `uplift()`, aber in [-1, 1].
    /// `None` heisst „nicht messbar" (M-19: keine Fenster ohne die Quelle).
    pub uplift: Option<f64>,
    /// Onset-Korrelation in [-1, 1]. `None` heisst „keine Streuung".
    pub koinzidenz: Option<f64>,
    /// Stabilitaet eines POSITIVEN Uplifts, einseitig, in [0, 1].
    pub wiederholbarkeit: f64,
}

pub fn zusammenhang(kandidat: &Quellprofil, master: &Quellprofil, band: Bandintervall)
    -> Zusammenhangsbeleg;
```

mit zwei Prädikaten:

```rust
impl Zusammenhangsbeleg {
    /// R1: mindestens eine Komponente ist POSITIV belegt.
    pub fn positiv_belegt(&self) -> bool {
        self.uplift.is_some_and(|u| u > 0.0) || self.koinzidenz.is_some_and(|k| k > 0.0)
    }
    /// R2: mindestens eine Komponente ist ein GEGENBELEG.
    pub fn gegenbeleg(&self) -> bool {
        self.uplift.is_some_and(|u| u < 0.0) || self.koinzidenz.is_some_and(|k| k < 0.0)
    }
}
```

**Warum `Option` und nicht 0,0:** `uplift()` gibt heute in drei verschiedenen Lagen 0,0 zurück — „kein Vergleichsfenster", „Differenz nicht positiv" und „Master steht still". Drei Bedeutungen unter einer Zahl sind genau der Fehler, den M-07 („ein Band ohne Bit hat keinen Wert — nie 0, nie NaN") an anderer Stelle schon verboten hat. Für R1 muss „nicht messbar" von „gemessen 0" unterscheidbar sein, für R2 „nicht messbar" von „negativ". `None` trägt diese Unterscheidung; die Rangkomponente bleibt daneben eine Zahl in [0, 1].

**Warum die sechs Rangkomponenten unberührt bleiben:** `RANGKOMPONENTEN` (`hypothese.rs`:330) ist Vertragsreihenfolge **und** Summenreihenfolge (M-25); `Rangkomponenten::werte()` und `rang()` addieren in genau dieser Folge. Eine siebte Komponente oder ein Wertebereich außerhalb [0, 1] änderte die Reduktionsreihenfolge und damit die letzte Bitstelle jedes Rangs — ein Bruch an M-25 ohne Not. `uplift()` und `koinzidenz()` klemmen deshalb weiterhin auf ≥ 0 **für den Rang**; der Gegenbeleg wirkt über die **Klasse**, nicht über die Reihenfolge. Beide lesen dieselbe Rechnung: `uplift()` und `koinzidenz()` werden auf `zusammenhang()` zurückgeführt, damit es die Größe nur einmal gibt.

**Die eine Ausnahme:** `wiederholbarkeit` **ändert** ihren Wert (E2), weil R2 das ausdrücklich verlangt. Sie bleibt Komponente Nummer fünf, in [0, 1], an derselben Stelle der Reduktionsreihenfolge — nur ihre Rechnung wird einseitig. *(Fassung Etappe 1: „trägt den `metrics_version`-Schritt aus E8". Seit der Matrixnacharbeit 1 ist der Schritt **bedingt**: die E-12-Regel knüpft ihn an die **Schwelle**, nicht an den Rechenweg — siehe E8 und N-40.)*

---

#### E2 — Die Wiederholbarkeit wird einseitig, über eine **neue** Bootstrap-Funktion.

**Der Befund dahinter (G-D5):** `bootstrap_p` (`experiment.rs`:1518–1553) zählt `nicht_positiv` und `nicht_negativ` und nimmt das **Minimum**. Eine stabil **negative** Reihe bekommt denselben kleinen p-Wert wie eine stabil positive; `wiederholbarkeit = 1 − p` steht deshalb bei einer antikorrelierten Quelle auf **0,995** — der Gegenbeleg wird nicht ignoriert, sondern **belohnt** (`eigen3`: Rang 0,4358, höher als jede korrekt gemessene Ursache im Bestandskorpus).

**Der Weg:** `experiment.rs` bekommt

```rust
/// Der EINSEITIGE p-Wert: wie oft liegt das Bootstrapmittel NICHT ueber null?
pub fn bootstrap_p_positiv(reihe: &[f64], blocklaenge: usize, ziehungen: usize, saat: u64) -> f64;
```

`hypothese.rs::wiederholbarkeit` ruft ab hier `bootstrap_p_positiv`.

**Warum eine neue Funktion und nicht ein Parameter an `bootstrap_p`:** `bootstrap_p` ist die gemessene Zusage M-45 und hat zwei weitere Aufrufer (`experiment.rs`:410 für die Bandsignifikanz, `sonde013_verdrahtung.rs`:2470/2475 als Zusagetest). Dort ist die Zweiseitigkeit richtig: ein Band, das sich in **beide** Richtungen stabil bewegt hat, ist verändert. Ein Parameter mit Vorgabewert wäre eine stille Verhaltensänderung an einer fremden Zusage; zwei benannte Funktionen sagen an der Aufrufstelle, welche Frage gestellt wird. `bootstrap_p` bleibt Zeile für Zeile unverändert, und beide teilen denselben `Wuerfel`, dieselbe Blockbildung und dieselbe `+1/+1`-Korrektur — die Einseitigkeit ist genau die eine Zeile `let kleiner = nicht_positiv as f64;` statt des Minimums.

**Zahlenrand:** `1.0` heißt weiterhin „kein Beleg", nie 0; die `+1/+1`-Korrektur bleibt, weil eine endliche Zahl Ziehungen „nie" nicht belegen kann. Bei einer stabil negativen Reihe ist `nicht_positiv` = `ziehungen`, also p ≈ 2,0 → auf 1,0 geklemmt → `wiederholbarkeit` = 0. Genau das verlangt R2.

---

#### E3 — Das Zusammenhangsgate steht in der **Klassenwahl**, nicht in `gate()`.

**Der Weg:** `baue_befund` (`hypothese.rs`:1387–1396) bekommt den `Zusammenhangsbeleg` und erweitert die Klassenwahl:

```rust
let klasse = if fenster < GATE_MINDEST_FENSTER
    || !kandidat.routing_bekannt
    || parent_duplikat
    || bandpassung_ist_null            // R1 (a): keine Energie im Befundband
    || !zusammenhang.positiv_belegt()  // R1 (b): kein Beleg fuer einen Zusammenhang
    || zusammenhang.gegenbeleg()       // R2: ein Gegenbeleg schliesst `hoch` aus
    || !getrennt                       // R3, aus `hypothesen()` hereingereicht
{
    Sicherheitsklasse::Mittel
} else {
    Sicherheitsklasse::Hoch
};
```

**Warum nicht in `gate()`:** `gate()` erzeugt **Ausschlüsse** aus einer geschlossenen Menge (M-87), und ein ausgeschiedener Kandidat verlässt das Ranking. R2 verlangt aber ausdrücklich das Gegenteil: der Kandidat mit Gegenbeleg wird „als Alternative geführt", bleibt also sichtbar. Ein Kandidat ohne Zusammenhangsbeleg ebenso — er ist nicht widerlegt, er ist unbelegt (M-27: „mehr Daten nötig" ist ein reguläres Ergebnis). Ein neuer Ausschlussgrund wäre außerdem eine Erweiterung der geschlossenen Menge und läge in NAK-213, nicht hier.

**Warum keine Score-Schwelle (M-15, M-31 bleiben):** Jeder Term der Bedingung ist ein **Prädikat über eine benannte Struktur**, kein Vergleich gegen eine kalibrierte Zahl. `positiv_belegt()` fragt „ist eine Komponente **über null**", nicht „ist sie über 0,3". Die Vorzeichengrenze null ist keine Kalibrierung, sondern die Definition von „positiv"; sie steht nirgends als abstimmbarer Wert und taucht deshalb auch nicht in `metriken-v1.json` auf. Dieselbe Lesart trägt schon heute `parent_duplikat` und `routing_bekannt`.

**`bandpassung_ist_null`** heißt wörtlich `!(bandpassung > 0.0)` — fail-closed gegen NaN, dieselbe negierte Form wie in `gate()`. Auch das ist keine Schwelle: `bandpassung` ist ein Leistungs**anteil**, und null Anteil heißt „im Befundband gar keine gemessene Energie". Ein Kandidat, dessen sämtliche Bänder im Intervall kein Gültigkeitsbit tragen, ist damit gemeint — nicht ein Kandidat mit wenig Energie. Der Fall `eigen4` (Sonde im Band 30..34, Befund in 98..102, `bandpassung` = 0,0161) fällt deshalb **nicht** an diesem Term, sondern an R1 (b): sein Uplift ist nicht belegt und seine Koinzidenz null. Das ist die ehrliche Zuordnung — die Bandpassung allein trennt nicht (G-H3).

---

#### E4 — Alignment: der Anteil der Fenster der **kürzeren** Seite, die einen überlappenden Partner haben.

**Der Befund dahinter (E-D4):** `spanne()` reduziert eine Fensterfolge auf `min/max` und verliert dabei jede Lücke. `zeitueberdeckung` auf zwei solchen Hüllen meldet 1,0, wo **null** Fenster paarweise überlappen (`g5probe::probe::d`: Sondenfenster 20 000 Samples vor und 40 000 nach den Masterfenstern, Alignment 1,0, Klasse `hoch`).

**Der Weg:** Eine neue reine Funktion

```rust
/// Der Anteil der Fenster EINER Seite, die ein Fenster der anderen ueberlappen —
/// normiert auf die KUERZERE Seite, wie `zeitueberdeckung` auf das kuerzere
/// Intervall normiert.
pub fn paarueberdeckung(a: &[Evidenzfenster], b: &[Evidenzfenster]) -> f64;
```

gerechnet als `max(anteil_a_mit_partner, anteil_b_mit_partner)`. `gate()` Schritt 3 ersetzt `zeitueberdeckung(spanne(k), spanne(m))` durch `paarueberdeckung(&k.fenster, &m.fenster)`; die Schwelle bleibt `GATE_ZEITUEBERDECKUNG` = 0,95.

**Das Partnerprädikat steht an EINER Stelle und verlangt eine gemeinsame Spanne GRÖSSER NULL** *(Fassung Matrixnacharbeit 1, D4; die Fassung Etappe 1 nannte `f.projekt_bis > g.projekt_von && f.projekt_von < g.projekt_bis` — exakt das Prädikat, mit dem `gemeinsame_reihen` heute paart)*:

```rust
/// Zwei Fenster derselben Zeitachse mit echter gemeinsamer Spanne.
fn ueberlappt(f: &Evidenzfenster, g: &Evidenzfenster) -> bool {
    f.transport_epoch == g.transport_epoch
        && f.projekt_von.max(g.projekt_von) < f.projekt_bis.min(g.projekt_bis)
}
```

**Warum die alte Form nicht genügt (D4, an der Quelle nachgerechnet):** für `f = [5, 5)` und `g = [0, 10)` sind **beide** Vergleiche wahr (`5 > 0` und `5 < 10`), obwohl **kein einziges Sample** gemeinsam ist — `f` ist leer. Solche Fenster sind erreichbar: `sample_count` hat vertraglich `minimum: 0` (`eq-copilot/schemas/v3/eq-ipc-v3.schema.json`:361), und `fenster_aus_historie` bildet `projekt_bis = von + sample_count`. Die **heutige** Rechnung verwirft den Einzelfensterfall an seiner Länge null (`zeitueberdeckung`: `kuerzer <= 0` → 0,0, `hypothese.rs`:1581–1586); die Paarüberdeckung in der Fassung Etappe 1 hätte ihn **neu durchgelassen** — eine Regression, die R4 gerade verhindern soll. Die Maximum-Minimum-Form ist zugleich die einzige, die `i64`-sicher ist: sie vergleicht, sie subtrahiert nicht.

**Leere Fenster zählen weder im Zähler noch im Nenner.** Ein leeres Fenster hat nie einen Partner; es zählte sonst als Kandidatenfenster ohne Partner und senkte den Anteil, obwohl es gar keine Messung ist. Sind **alle** Fenster einer Seite leer, ist die zählbare Menge leer und `paarueberdeckung` gibt 0,0 — der Kandidat fällt an `CoverageFehlt` oder `AlignmentFalsch` wie heute, nie durch.

**Warum auf die kürzere Seite normiert und nicht „Anteil der Masterfenster":** Der Auftrag lässt beides zu („oder eine gleichwertige paarweise Größe"). „Anteil der **Master**fenster" wäre zu streng und bräche eine bestehende Zusage: `g5_fenster_genau_acht` (Kandidat 8 Fenster, Master 12) käme auf 8/12 = 0,667 und fiele am Alignment — dabei ist genau dieser Fall der gemessene Randwert von M-23 („mindestens acht"). Schlimmer: eine Quelle, die nur in **einem Teil** der Zeit aktiv ist, ist der Normalfall und zugleich die **Voraussetzung** des bedingten Uplifts aus M-19, der Fenster **ohne** die Quelle braucht. Ein Alignment-Gate, das Teilaktivität bestraft, machte R1 unerfüllbar. Die Normierung auf die kürzere Seite ist dieselbe Entscheidung, die `zeitueberdeckung` schon trifft, und sie misst die richtige Frage: „liegen die Fenster der dünneren Seite dort, wo die andere misst?"

**Zahlenränder:** leere Fensterfolge auf einer Seite → 0,0 (nicht 1,0 und keine Division durch null); der Fall ist ohnehin schon in `gate()` Schritt 0 abgefangen. Kein Vergleich rechnet mit `i64`-Differenzen, deshalb kein Überlauf; die Rechnung ist ein Zählen, kein Subtrahieren.

**Aufwand:** naiv O(n·m). Bei `KANDIDATEN_DECKEL` = 5 und Fensterhistorien im Bereich von Dutzenden ist das unkritisch; beide Folgen sind nach `empfangsfolge` sortiert, aber **nicht** nach Projektzeit (eine Sonde darf umsortieren), deshalb steht bewusst der einfache Doppelschleifen-Weg und keine Annahme über Monotonie. Diese Wahl ist eine Zusage: kein Rotbeweis darf an einer Sortierannahme hängen.

---

#### E5 — Passagenmaterial zählt nur **ganz** innenliegende Fenster; die Masteranomalie liest nur Fenster in der Passage.

**Die Befunde dahinter (E-D3, A4):** `fenster_in_passage` (`:1612–1623`) zählt jedes Fenster mit ≥ 1 Sample Überlappung — vier Fenster weit vor der Passage plus zehn mit je einem Sample Überlappung ergeben `hoch`/`ready_to_send`. Und `masteranomalie` (`:1245–1246`) liest `master.fenster.last()`, auch wenn dieses Fenster **außerhalb** der benannten Passage liegt: der veröffentlichte Befund nennt dann die alte Passage und behauptet eine fremde Anomalie.

**Der Weg, zwei Änderungen:**

1. `fenster_in_passage` filtert auf **vollständige** Enthaltung:
   ```rust
   f.transport_epoch == passage.transport_epoch
       && f.projekt_von >= passage.projekt_von
       && f.projekt_bis <= passage.projekt_bis
   ```
   M-23 sagt wörtlich „innerhalb der Passage"; ein Fenster, das zu 99,98 % draußen liegt, ist nicht innerhalb. `passagenmaterial` (min über Kandidat und Master, NR-05) bleibt unverändert darauf aufgesetzt.

2. `masteranomalie` bekommt die Passage als Parameter:
   ```rust
   pub fn masteranomalie(master: &Quellprofil, passage: Option<&Passagenfenster>)
       -> Option<(Zielmetrik, Bandintervall, Beobachtung)>;
   ```
   Mit Passage wird das **letzte vollständig innenliegende** Fenster gelesen (dieselbe Filterbedingung wie oben, also **eine** Definition von „in der Passage"). Ohne Passage bleibt es beim jüngsten Fenster wie heute.

   **Gibt es kein innenliegendes Masterfenster, fällt die Funktion auf das jüngste Fenster ÜBERHAUPT zurück — mit `beobachtung.gueltig = false`** *(Fassung Matrixnacharbeit 1, D3; die Fassung Etappe 1 gab hier `None` zurück und ließ die Rechnung vor den Gates enden)*. Nur wenn der Master **gar keine** verwertbaren Fenster hat, bleibt es bei `None`.

**Warum der Rückfall und nicht `None` (D3, an der Quelle nachgerechnet):** `hypothesen()` bricht bei `None` **vor** allen Gates ab (`hypothese.rs`:1105–1106). Der Fall ist aber nicht exotisch, sondern bereits ein zugesagtes Gegenbeispiel: bei §2.6 Nr. 17 liegen **alle zwölf** Masterfenster vor der Passage `[12,24)`. Die Fassung Etappe 1 hätte dort geschwiegen, statt die zugesagte Enthaltung mit `passage_unvergleichbar` zu liefern — ein Bruch an M-27 („Mehr Daten nötig ist ein reguläres Ergebnis und kein Fehlerzustand") und eine **Regression** gegen den heutigen Stand, der immer eine Anomalie liefert.

**Warum das die Enthaltung strukturell garantiert und A4 trotzdem geschlossen bleibt:** Mit dem Rückfall laufen alle Gates. `fenster_in_passage(master)` ist dann **0**, also ist `passagenmaterial` = `min(kandidat, 0)` = 0 < `GATE_MINDEST_FENSTER` für **jeden** Kandidaten — keiner überlebt, `hypothesen()` baut die Enthaltung (`daten_reichen_nicht`, Sicherheit `unklar`), und jeder Kandidat trägt den Grund, an dem er zuerst gefallen ist: `passage_unvergleichbar`, wenn seine Fenster die Passage zeitlich verfehlen (Nr. 17), sonst `passage_zu_kurz`. Ein **Befund** mit ungültiger Beobachtung kann so gar nicht entstehen. **A4 bleibt geschlossen**, weil der Rückfall nur greift, wenn **kein** Masterfenster innen liegt; im A4-Aufbau (acht Fenster in der Passage, ein neuntes dahinter) liegt der Normalfall vor, und das Außenfenster wird weiterhin nicht gelesen.

**Warum `gueltig: false` und nicht ein erfundener Wert:** `Beobachtung` trägt das Bit genau dafür („Ein Messwert mit seinem Gültigkeitsbit. Ohne das Bit ist die Zahl keine Messung.", `hypothese.rs`:490–497), und es reist bis zu Gen (`hypothese_verdrahtung.rs`:439 schreibt `gueltig` in den Snapshot). Metrik und Band werden weiterhin gebraucht — `finding_id` entsteht aus Sitzung, Metrik, Band, Quelle und Fassung (M-25) —, aber ohne gültige Beobachtung behauptet die Enthaltung keinen Messwert an diesem Ort. Ihr `likely_cause` lautet unverändert „Die Datenlage traegt noch keine Ursachenaussage."

**Was NICHT dazugehört:** ein Main **ganz ohne** Fenster liefert weiter `None` und damit Schweigen. Das ist G-L4 und bleibt **NAK-213 R4** — unverändert gegenüber dem heutigen Stand, also keine neue Lücke.


**Warum die Signatur und nicht die Verdrahtung:** Der Auftrag erlaubt `hypothese_verdrahtung.rs` „nur soweit R5 es verlangt". R5 verlangt nichts dort: `Aufnahme` trägt die Passage bereits, `hypothesen()` ruft `masteranomalie` und kann sie durchreichen. Die Fenster des Masters in der Verdrahtung zu filtern wäre schlechter — dann verlöre `passagenmaterial(master)` die Grundlage, an der es zählt, und zwei Filter an zwei Orten wären zwei Gelegenheiten, sie verschieden zu formulieren (dieselbe Lehre wie NR-01). **`hypothese_verdrahtung.rs` bleibt in diesem Ticket unberührt**; das ist ein Ergebnis der Matrix, keine ausgelassene Arbeit.

**Randfall halboffen:** Das Passagenfenster ist `[von, bis)` — dieselbe Halboffenheit wie ein Evidenzfenster. Ein Fenster, das exakt am Passagenende endet (`projekt_bis == passage.projekt_bis`), liegt **innerhalb**; eines, das exakt am Passagenanfang endet, liegt draußen. Beide Ränder sind eigene Matrixzeilen (N-26, N-27).

---

#### E6 — „Getrennt" heißt **beides zugleich**: verschiedener Gesamtrang **und** verschiedener Zusammenhang — gegen **jeden** Überlebenden.

**Die Befunde dahinter (E-D2, A1, G-H3, `f7`; erweitert um D1 und D2 der Matrixprüfung 1):** Heute vergleicht `getrennt` (`:1216–1222`) die quantisierten Gesamtränge mit `>`. Zwei Quanten genügen — und diese zwei Quanten können aus einem Band stammen, das mit dem Befund nichts zu tun hat (`f7`: +0,1 dB in Band 210 bei Befundband 98..102), aus reiner Materialmenge (E-D2) oder aus der **Intent-Rolle** (A1: `rolle = fuehrt` auf dem Distraktor hebt seine `intent_relevanz` und erklärt messtechnisch identische Kandidaten für getrennt).

**Der Weg** *(Fassung Matrixnacharbeit 1; die Fassung Etappe 1 verglich **nur** die Zusammenhangskomponenten und **nur** die beiden führenden Kandidaten)*:

```rust
/// Sind zwei Kandidaten getrennt? BEIDE Bedingungen muessen halten.
fn getrennt(a: &Kandidat, b: &Kandidat) -> bool {
    rang_quantisiert(a) != rang_quantisiert(b)           // D1: M-25/M-26
        && zusammenhang_verschieden(&a.beleg, &b.beleg)  // R3
}

fn zusammenhang_verschieden(a: &Zusammenhangsbeleg, b: &Zusammenhangsbeleg) -> bool {
    quantisiert_opt(a.uplift)            != quantisiert_opt(b.uplift)
        || quantisiert_opt(a.koinzidenz) != quantisiert_opt(b.koinzidenz)
        || quantisiert(a.wiederholbarkeit) != quantisiert(b.wiederholbarkeit)
}

// D2: der Fuehrende ist nur stark, wenn er von JEDEM anderen getrennt ist.
let getrennt_von_allen = ueberlebende[1..]
    .iter()
    .all(|anderer| getrennt(&ueberlebende[0], anderer));
```

Verglichen wird durchgehend auf `RANG_QUANTUM` quantisiert — dieselbe Auflösung, in der Ränge überhaupt verglichen werden (M-25). `None` ist ein eigener Wert und gleich nur sich selbst: zwei Kandidaten, deren Uplift beide „nicht messbar" ist, sind darin nicht unterschieden.

**Warum der Gesamtrang wieder dazugehört (D1, an der Quelle nachgerechnet):** Verschiedene quantisierte Komponenten können **denselben** quantisierten Gesamtrang ergeben, weil `rang()` durch sechs teilt und erneut quantisiert (`hypothese.rs`:535–545). Bei Bandpassung 0,1, Intent-Relevanz 0,5, Routingqualität 1,0 und sonst null liefern Koinzidenzen von **0,500000 und 0,500001** beide exakt den Rang **0,35** — die Komponenten sind quantisiert verschieden, der Rang ist es nicht. Die Fassung Etappe 1 hätte die beiden für getrennt erklärt; welcher von ihnen dann `hoch` wird, entscheidet die Sortierung über die aufsteigende `candidate_source`. Damit entschiede der **Tie-Break über die Auswahl** statt nur über die Anzeige — genau das verbietet M-26 (`docs/beweise/SONDE-014.md`:745: „Der Tie-Break entscheidet nur die Anzeigereihenfolge, nicht die Auswahl").

**Warum gegen jeden, nicht nur gegen den Zweiten (D2):** Bei drei sichtbaren Kandidaten kann ein Kandidat B durch seine **Intent-Relevanz** zwischen zwei messtechnisch identische Kandidaten A und C rutschen. Der Vergleich nur mit dem Zweitplatzierten liefert dann `getrennt = true`, und A wird `hoch`, obwohl C von ihm ununterscheidbar ist. Hier fehlt **kein** Kandidat durch Messausschluss oder Deckel — NAK-213 trägt den Fall nicht, er gehört hierher. Der Vergleich läuft deshalb über alle Überlebenden **innerhalb des Deckels** (also über die höchstens fünf sichtbaren); ein am Deckel abgeschnittener Kandidat ist NAK-213 R1 und bleibt es.

**Was das für den positiven Kontrollfall heißt** *(Matrixnacharbeit 2, D7)*: Ein positiver Uplift allein macht zwei Kandidaten **nicht** getrennt. `rang()` mittelt sechs Komponenten und quantisiert **erneut** (`hypothese.rs`:535–545), sechstelt also jede Komponentendifferenz — `uplift = Some(0,000001)` gegen `None` ergibt beide Male den Rang **0,266667**, und E6 stuft die beiden korrekt als ungetrennt ein. Wie viele Quanten Komponentenabstand ein Quantum Rangabstand ergeben, hängt zudem von der **Lage** der Summe zur nächsten Quantenmitte ab: nachgemessen zwischen **1 und 5** Quanten je nach Konstellation (bei Bandpassung 0,1 / Intent-Relevanz 0,5 / Routingqualität 1,0 erst ab 5, bei Bandpassung 0,2 schon ab 1). Eine Vorbedingung „mindestens N Quanten Komponentenabstand" wäre deshalb selbst eine geratene Zahl. Jede Zeile, die einen **starken** Befund **neben einem zweiten sichtbaren Kandidaten** zusagt, nennt darum den Rangabstand als ausdrückliche Vorbedingung und **misst** ihn im Test, statt ihn aus der Konstruktion zu erschließen (N-17, N-47; in §2.6 betrifft das allein Zeile 26). Zeilen mit einem **einzigen** Überlebenden brauchen ihn nicht — dort greift `getrennt` gar nicht (`_ => true`), und die Klasse entscheidet allein R1: so bei N-06, N-07 und den Korpussitzungen 15, 19, 20 und 25.

**Was unverändert bleibt:** Ein einziger Überlebender bleibt „getrennt" (der Zweig `_ => true`) — die Lage „allein im Rennen neben einem Messausschluss" ist G-D2 und gehört **NAK-213 R2**. `RANG_QUANTUM`, `TIE_BREAK_KEY` und die Sortierung bleiben Zeile für Zeile unverändert; sie entscheiden weiterhin die **Reihenfolge**, und M-21 Satz 1 (nur der Führende darf stark sein) bleibt der Riegel davor.

**Warum nicht der ganze Rang minus Intent-Relevanz:** Das wäre eine zweite, unbenannte Rangzahl im Pfad — und die Bandpassung bliebe darin, obwohl G-H3 sie ausdrücklich als untaugliche Trennschärfe benennt („zwei Quanten aus einem Band, das mit dem Befund nichts zu tun hat"). Die drei Zusammenhangskomponenten sind genau die Größen, die eine **Ursachen**behauptung tragen; alles andere ist Kontext (Bandpassung, Routing) oder Wille des Users (Intent-Relevanz). Der Gesamtrang steht **zusätzlich** in der Bedingung, nicht statt ihrer — er verhindert nur, dass die Sortierung eine Auswahl trifft.


#### E7 — Der Korpus bekommt je-Fenster-Reihen. Ohne sie wäre `hoch` strukturell unerreichbar.

**Der Befund, gemessen am Rohbeleg:** In der Ergebnisdatei `docs/beweise/roh/G5-gegenbeispiele-bf92891/p5-korpus-ergebnis.json` trägt **jeder** der 18 Fälle einen `confidence_score`, der sich exakt als `(bandpassung + 0 + 0 + 0,5 + 0 + routing)/6` auflöst — `wahrer_kandidat` etwa 0,266635 = (0,099810 + 0 + 0 + 0,5 + 0 + 1,0)/6. **Koinzidenz, Uplift und Wiederholbarkeit sind im gesamten committeten Korpus exakt null**, auch bei der wahren Ursache. Der Grund liegt in der Fixturform: `erzeuge_p5_korpus.py` kennt je Quelle nur eine **konstante** Anhebung über alle Fenster. Daraus folgt zwingend:

* `koinzidenz` = 0 — die Onsetreihe ist konstant, und `korrelation` gibt bei fehlender Streuung 0 zurück (nicht 1,0: „zwei Konstanten sind kein Zusammenhang");
* `uplift` nicht messbar — bei konstantem Pegel liegen **alle** Fenster auf oder über dem eigenen Median, `ohne` bleibt leer, und M-19 verlangt genau diese Vergleichsmenge;
* `wiederholbarkeit` = 0 aus demselben Grund.

**Die Folge, wenn nichts geschähe:** R1 senkte **jeden** Korpusfall auf `mittel`, auch die drei zulässigen Kontrollfälle. Der Riegel `falsche_starke` wäre trivial erfüllt, `recall` fiele auf 0, und die Sicherheit `hoch` wäre im Datenweg ein totes Element — genau der Fehler, vor dem der Quellkommentar in `baue_befund` schon warnt („Wer die Sicherheit `hoch` an ein PRE/POST-Paar knüpfte, machte sie in einer rein passiven Sitzung strukturell unerreichbar").

**Der Weg:** `erzeuge_p5_korpus.py::quelle` und das Master-Feld bekommen zwei optionale Reihen:

```python
quelle(2, anhebung_db=9.0, ...)                     # unveraendert: konstant
quelle(2, pegelreihe_db=[9,0,9,0,9,0,9,0,9,0,9,0])  # je Fenster
quelle(2, onsetreihe=[3.5,1.0,3.5,1.0, ...])        # je Fenster
```

`anhebung_db` bleibt die Kurzform für eine konstante Reihe; im JSON steht immer die **ausgeschriebene Reihe**, damit die Fixture die Wahrheit trägt und nicht eine Kurzform, die zwei Leser verschieden auflösen. `sonde014_p5_korpus.rs` liest sie und setzt sie je Beleg — die Bandanhebung wie heute in `/baender/werte` und `/verteilung/p*/werte`, den Onset über `staerke_mad` der Einträge in `/ereignisse/liste` (das ist die Quelle von `Evidenzstand::onset`, `evidenz.rs`:305–314: die Summe der `staerke_mad`).

**Was das an bestehenden Sitzungen ändert:** nichts an ihrer Konstruktion. `wahrer_kandidat` bleibt eine Quelle mit konstantem Pegel — und ihre **Erwartung** sinkt auf `mittel`, mit gedrucktem Grund: sie **ist** die wahre Ursache, aber es gibt kein Fenster ohne sie, und ohne Vergleichsfenster gibt es nach M-19 keinen Uplift. Das ist die inhaltlich richtige Verschärfung, die der Gate-Text will („Enthaltung ist das gewünschte Ergebnis"), und sie ist zugleich der Rotbeweis dafür, dass R1 keine leere Regel ist. Zwei **neue** Kontrollfälle mit echtem Zusammenhang tragen `hoch` und halten den Recall über null (§2.5).

---

#### E8 — Der `metrics_version`-Schritt ist **bedingt** und **gekoppelt**: vier Codestellen plus Register, oder gar nicht.

**Die Regel** *(Fassung Matrixnacharbeit 1, D5; die Fassung Etappe 1 zog den Schritt unbedingt und nannte nur zwei der vier Stellen)*: Die E-12-Regel aus SONDE-014 knüpft den Schritt an die **Schwelle**, nicht an den Rechenweg. Die Matrix nennt deshalb **beide Ausgänge und den Test, der entscheidet**.

**Der entscheidende Test:** `pruefe_p5_korpus.py` (Bein **A29**), Abschnitt „Die Schwelle als AUSGABE" — `schwelle_suchen()` auf dem erweiterten Korpus. Heute (Sechserkorpus, nach NR-12) ist die niedrigste haltende Stufe **`hoch`**.

**Ausgang B — die Schwelle bleibt `hoch`:** **kein** Schritt. `METRICS_VERSION` bleibt `20260904`, `metriken-v1.json` unberührt, `eq-copilot/plugin/` unberührt. Das ist E-12 Fall 3, derselbe Ausgang wie in SONDE-014 Etappe H und Nacharbeit 1. **Dieser Ausgang ist der erwartete**, weil dieses Ticket keine Schwelle verschiebt, sondern Strukturregeln einzieht.

**Ausgang A — die Schwelle bewegt sich:** der Schritt wird **vollständig gekoppelt** gezogen. Er umfasst — gemessen mit `grep -rn "20260904"` über `*.rs`, `*.h`, `*.cpp`, `*.json`, `*.py`, `*.ps1` ohne `docs/` — **vier Codestellen und das Register**:

| Stelle | heute | Riegel, der sie bindet |
|---|---|---|
| `eq-copilot/schemas/v3/metriken-v1.json`:20 `aktuell` (plus neuer Fassungseintrag) | `20260904` | ist die Wahrheit, gegen die die drei anderen geprüft werden |
| `broker/src/coordinator/vergleichbarkeit.rs`:149 `METRICS_VERSION` | `20260904` | A5 `comparability_schwellen_haengen_an_metrics_version` (`pruefe_v3_vertrag.py`:1945–1951) |
| `broker/src/coordinator/prepost.rs`:206 `METRICS_VERSION` | `20260904` | A5 ebenda, `:1914–1926` — ausdrücklich, „eine zweite Datei mit eigener Fassung koennte sonst unbemerkt auf einer alten Kalibrierung stehenbleiben" |
| `eq-copilot/plugin/core/analysis/FeatureEngine.h`:783 `kFeatureMetricsVersion` | `20260904u` | A5 `metrics_version_bindet_schwellen: Code und Register nennen dieselbe Version` (`pruefe_v3_vertrag.py`:1841–1860) |

Die vier Gate-Schwellen (`GATE_ZEITUEBERDECKUNG`, `GATE_QUELLEN_JACCARD`, `GATE_MATERIAL_COSINE`, `GATE_ABDECKUNG`) und `GATE_MINDEST_FENSTER` **bewegen sich nicht** und werden unverändert in die neue Fassung übernommen; der Riegel verlangt, dass alle vier in der aktuellen Fassung stehen.

**Keine Fixture ist betroffen — gemessen, nicht angenommen.** Die Matrixprüfung 1 vermutet „betroffene Fixtures". `grep -rl "20260904" eq-copilot/fixtures/` liefert **null** Treffer. Die 84 Fixtures mit einem Feld `metrics_version` tragen dort die **Konfidenz**-Metrikversion `1` des Evidenzframes (`erzeuge_v3_fixtures.py`:99, 713, 992) — eine andere Größe mit demselben Feldnamen, wie sie `CLAUDE.md` unter „Begriffe nicht vermischen" meint. Der Regressionslauf des Korpus (A28 bytegleich, A29, A30) läuft in beiden Ausgängen ohnehin.

**Die Plugin-Konstante ist die in der Ticketgrenze vorgesehene Ausnahme.** Der Auftrag schließt `eq-copilot/plugin/` aus, „außer ein Plugin-Test bricht nachweislich durch eine Vertragsänderung — dann Begründung im Manifest". Genau das liegt vor: A5 prüft `kFeatureMetricsVersion` gegen `metriken-v1.json.aktuell`; ein Schritt ohne diese Zeile macht den Vertragslauf **zwingend rot**. Berührt wird **ausschließlich diese eine Zeile**; alles Übrige unter `eq-copilot/plugin/` bleibt unangetastet. Dieselbe Kopplung verlangt `AGENTS.md`:51–52 (Writer und Reader eines Vertrags im selben Änderungssatz) und die Arbeitsregel „Beziehungen mitprüfen" aus `CLAUDE.md`.

**Warum der Rechenweg allein den Schritt nicht zieht:** Die Wiederholbarkeit rechnet ab hier einseitig (E2), und damit ändern sich Rangwerte. Die Fassung Etappe 1 leitete daraus einen unbedingten Schritt ab. Die E-12-Regel ist aber enger gefasst: sie knüpft den Schritt an die **Kalibrierung** — an die Schwelle, die der Korpus ausgibt. Bewegt sie sich nicht, ist der geänderte Rechenweg im Manifest dokumentiert und im Korpuslauf gemessen, ohne dass eine Fassung entsteht, die niemand von der alten unterscheiden könnte. Bewegt sie sich, wird die Fassung gehoben, und alle vier Stellen gehen im **selben** Commit.

---

#### E9 — Der Prüfer ist Teil der Zusage: wechselseitige Alternativen sind der Normalfall, nicht ein Fehlschlag.

**Der Befund dahinter (D6):** R3 verlangt, dass zwei ungetrennte Kandidaten „jeder Alternative des anderen" sind. Heute trägt **nur** der führende Befund Alternativen (`hypothese.rs`:1230 `befunde[0].alternatives = weitere;`), und der Prüfer verlässt sich darauf: `_faelle_bilden` (`pruefe_p5_korpus.py`:425–429) meldet ein Problem, sobald eine Alternativ-ID **nicht** der deklarierten Distraktorquelle gehört. Sobald jeder Befund auf den anderen zeigt, verweist auch der Distraktorbefund auf die wahre Quelle — und `g5_distraktor_mehr_fenster` fiele in A29 **rot**, obwohl beide `mittel`-Befunde genau richtig sind.

**Der Weg:** Die NR-14-Prüfung wird von „**jede** Alternative gehört der Distraktorquelle" auf „**mindestens eine** Alternative im Lauf löst auf einen Befund der Distraktorquelle auf" umgestellt. Das ist der Nachweis, den NAK-190 und SONDE-013 M-85 Ziel 1 wirklich verlangen: *„der korrelierte Distraktor **muss** in `alternatives` erscheinen"* (M-65 wörtlich) — eine Existenzaussage, keine Allaussage. Die beiden anderen Riegel derselben Stelle bleiben **unverändert**: eine Alternativ-ID muss auf einen Befund **desselben Laufs** auflösen, und kein Befund nennt sich selbst.

**Die Gegenprobe in A30** (M-68, Muster A27 — jede Erwartung mit ihrem Gegenteil): (a) zwei `mittel`-Befunde, die sich **wechselseitig** als Alternative nennen, sind **grün**; (b) dieselbe Menge, in der einer der beiden `hoch` trägt, ist **rot** (`falsche_starke`); (c) eine Menge, in der **keine** Alternative auf die Distraktorquelle zeigt, ist **rot** (der NAK-190-Nachweis misst dann nichts); (d) eine Alternativ-ID, die auf **keinen** Befund des Laufs auflöst, bleibt **rot** (NR-14 unverändert).

**Warum das eine Matrixzeile ist und keine Werkzeugnotiz:** Erzeuger, Prüfer und Fixture sind eine **Kette** (der Zweck des P5-Korpus), und `AGENTS.md`:51–52 verlangt Writer und Reader eines Vertrags im selben Änderungssatz. Eine neue Ausgabe des Brokers zu verlangen, ohne den Leser mitzuziehen, hieße, den Bau an einer Stelle raten zu lassen, an der Raten eine zweite Wahrheit erzeugt.


### 2.1 Zusammenhang und Klassenwahl (R1, R2) — Zeilen N-01 bis N-12

Spalten: **Zustand** (der Aufbau) · **Ereignis** (was geschieht) · **Zusage** (was das Produkt sagt) · **Test** (Datei::Fall) · **Rotbeweis** (der Zustand ohne die Regel; Rohausgabe unter `docs/beweise/roh/NAK-212-rot-<Zeile>.txt`).

| # | Zustand | Ereignis | Zusage | Test | Rotbeweis |
|---|---|---|---|---|---|
| **N-01** | Master mit Anomalie in 98..102; eine Sonde, 12 Fenster, bekanntes Routing, **kein einziges Band im Befundintervall trägt ein Gültigkeitsbit** | Der Snapshot wird gelesen | `bandpassung` = 0, Sicherheit **`mittel`**, Zustand `more_data`, `next_test` unverändert aus der bestehenden Kette. Der Befund bleibt sichtbar (M-27) | `sonde014_hypothese.rs::hoch_verlangt_energie_im_befundband` | Ohne den Term `bandpassung_ist_null`: `hoch`/`ready_to_send` bei `bandpassung` = 0 |
| **N-02** | Wie N-01, aber die Sonde trägt Energie **im falschen Band** (30..34, Befund in 98..102), konstanter Pegel | Snapshot | `bandpassung` ≈ 0,016 (**nicht** null, der Term aus N-01 greift nicht); `uplift` = `None`, `koinzidenz` = 0 → **`mittel`**. Die Zeile `likely_cause` nennt weiterhin das Befundband — sie beschreibt den Ort des Masterproblems, nicht die Energie der Quelle | `sonde014_gegenbeispiele.rs::eigen4_quelle_im_falschen_band` | Ohne R1 (b): `hoch`/`ready_to_send` bei fünf von sechs Rangkomponenten auf „kein Zusammenhang" |
| **N-03** | Master und Sonde je 12 Fenster, **konstanter** Pegel, identischer Verlauf, bekanntes Routing, 12 ≥ `GATE_MINDEST_FENSTER` | Snapshot | `uplift` = `None` — **alle** Fenster liegen auf oder über dem eigenen Median, `ohne` ist leer, und M-19 verlangt Fenster ohne die Quelle. `koinzidenz` = 0 (konstante Onsetreihe). → **`mittel`** | `sonde014_hypothese.rs::uplift_ohne_vergleichsfenster_ist_kein_beleg` (Fall a) | Heutiger Stand: `hoch`/`ready_to_send`, gemessen im Korpus als `wahrer_kandidat` |
| **N-04** | Wie N-03, aber der Kandidat hat **nur ein** gemeinsames Fenster mit dem Master | Snapshot | `upliftreihe` gibt `None` (`kandidatenreihe.len() < 2`) → `uplift` = `None` → **`mittel`**; der Fall fällt zusätzlich am Passagen- beziehungsweise Fenstergate, und beide Gründe stehen nebeneinander im Manifest | `sonde014_hypothese.rs::uplift_ohne_vergleichsfenster_ist_kein_beleg` (Fall b) | Ohne die Unterscheidung `None` ↔ 0,0 wäre der Fall von N-05 nicht zu trennen |
| **N-05** | Wie N-03, aber **kein** Band des Kandidaten im Befundintervall trägt ein Bit, während er anderswo misst | Snapshot | `bandmittel` = `None` je Fenster → `endlich` leer → `upliftreihe` = `None` → `uplift` = `None`. Zusammen mit N-01 (Bandpassung null) fällt der Fall doppelt; die Zusage nennt **beide** Gründe | `sonde014_hypothese.rs::uplift_ohne_vergleichsfenster_ist_kein_beleg` (Fall c) | Ohne `Option` läse die Klassenwahl 0,0 und könnte „unbelegt" nicht von „gemessen null" trennen |
| **N-06** | Master alterniert im Befundband (Fenster 0,2,4,… laut, 1,3,5,… leise); die Sonde **gleichläufig** (laut, wenn der Master laut ist), 12 Fenster, bekanntes Routing | Snapshot | `mit` und `ohne` sind je 6 Fenster, `differenz` > 0 → `uplift` > 0 → **`hoch`**/`ready_to_send`, `ursachenklasse = quelle_resonanz`. **Das ist der Fall, auf dem `hoch` erreichbar bleibt** | `sonde014_hypothese.rs::hoch_verlangt_einen_belegten_zusammenhang` (positive Hälfte) · Korpus `zusammenhang_uplift` | Wäre die Regel als Score-Schwelle gebaut, hinge dieser Fall an einer Zahl statt an der Struktur; der Rotbeweis ist der Fall N-03 daneben, der bei **gleichem Score** `mittel` bleibt |
| **N-07** | Wie N-06, aber der Pegel ist konstant und die **Onsetreihe** variiert gleichläufig zum Master | Snapshot | `uplift` = `None`, `koinzidenz` > 0 → **`hoch`**. Eine der beiden Komponenten genügt (R1: „mindestens eine") | `sonde014_hypothese.rs::koinzidenz_allein_traegt_den_zusammenhang` · Korpus `zusammenhang_koinzidenz` | Verlangte die Regel **beide** Komponenten, wäre `hoch` in jeder Sitzung ohne Onsetereignisse unerreichbar |
| **N-08** | Master alterniert; die Sonde **gegenläufig** (laut, wenn der Master leise ist), 12 Fenster, bekanntes Routing | Snapshot | `differenz` < 0 → `uplift` = `Some(negativ)` → `gegenbeleg()` → **`mittel`**, der Kandidat bleibt sichtbar und wird als Alternative geführt | `sonde014_gegenbeispiele.rs::eigen3_antikorrelierte_quelle` · Korpus `gegenbeleg_uplift` | Heutiger Stand: `hoch`/`ready_to_send`, Rang **0,4358** — höher als jede korrekt gemessene Ursache im Bestandskorpus |
| **N-09** | Wie N-08, aber der Gegenlauf liegt in der **Onsetreihe** (Pegel konstant) | Snapshot | `korrelation` < 0 → `koinzidenz` = `Some(negativ)` → **`mittel`** | `sonde014_hypothese.rs::negative_korrelation_ist_ein_gegenbeleg` · Korpus `gegenbeleg_koinzidenz` | Heute klemmt `koinzidenz()` mit `.max(0.0)`: der Gegenbeleg kostet genau so viel wie „keine Angabe" — nichts |
| **N-10** | Die Reihe `mit − mittel(ohne)` ist **stabil negativ** über alle Bootstrapziehungen | `wiederholbarkeit` wird gerechnet | `bootstrap_p_positiv` zählt `nicht_positiv` = `ziehungen` → p auf 1,0 geklemmt → **`wiederholbarkeit` = 0** | `sonde014_hypothese.rs::wiederholbarkeit_belohnt_keinen_gegenbeleg` · Modultest `hypothese.rs::bootstrap_einseitig_trennt_die_richtung` | Heutiger Stand (zweiseitig): `wiederholbarkeit` = **0,995** für dieselbe Reihe |
| **N-11** | Die Reihe ist stabil **positiv** | `wiederholbarkeit` wird gerechnet | `bootstrap_p_positiv` ≈ `bootstrap_p` — derselbe kleine p-Wert, `wiederholbarkeit` nahe 1. **Die Einseitigkeit kostet den richtigen Fall nichts** | `sonde014_hypothese.rs::wiederholbarkeit_belohnt_keinen_gegenbeleg` (Gegenprobe) | Ohne die Gegenprobe wäre „einseitig" von „immer 0" nicht zu unterscheiden |
| **N-12** | `bootstrap_p` wird von `experiment.rs`:410 und `sonde013_verdrahtung.rs`:2470/2475 gerufen | Der Bau ändert die Wiederholbarkeit | `bootstrap_p` bleibt **Zeile für Zeile** unverändert und zweiseitig; die beiden fremden Aufrufer und ihre Zusage M-45 sind unberührt | `sonde013_verdrahtung.rs` (bestehend, muss grün bleiben) · `git diff` zeigt in `experiment.rs` nur die **neue** Funktion | Ein Parameter mit Vorgabewert an `bootstrap_p` hätte M-45 still verändert |

---

### 2.2 Trennung zweier Kandidaten (R3, D1, D2) — Zeilen N-13 bis N-17, N-41, N-42

**Die Definition, gegen die alle Zeilen dieses Abschnitts messen** (E6, Fassung Matrixnacharbeit 1): getrennt ist ein Kandidatenpaar nur, wenn **der quantisierte Gesamtrang verschieden ist UND mindestens eine Zusammenhangskomponente verschieden ist**; der Führende ist nur `hoch`, wenn er von **jedem** anderen Überlebenden in diesem Sinn getrennt ist.

| # | Zustand | Ereignis | Zusage | Test | Rotbeweis |
|---|---|---|---|---|---|
| **N-13** | Zwei Kandidaten, identische Pegelreihe im Befundband, **beide mit belegtem Uplift**; der Master trägt 12 Fenster, ein Kandidat 12, der andere **16** | Snapshot | Die drei Zusammenhangskomponenten sind quantengleich → **ungetrennt** (die zweite Bedingung aus E6 fällt; der Gesamtrang ist hier ebenfalls gleich, es genügt aber schon eine) → beide **`mittel`**, jeder Alternative des anderen. **Warum sie gleich sind, ausdrücklich:** `gemeinsame_reihen` iteriert über die **Kandidaten**fenster und überspringt jedes ohne Masterpartner — bei 16 Kandidaten- und 12 Masterfenstern entstehen 12 Paare, exakt dieselben wie beim 12-Fenster-Kandidaten, und der Median wird über dieselben 12 Werte gebildet. Die vier überzähligen Fenster gehen in keine der drei Größen ein. Die Zeile misst deshalb wirklich „Materialmenge trennt nicht" und nicht einen Zufall der Reihenlänge | `sonde014_hypothese.rs::materialmenge_trennt_nicht` · Korpus `g5_distraktor_mehr_fenster` | Ohne R3 entschiede der quantisierte Gesamtrang; bei ungleicher Bandpassung führte der Lautere |
| **N-14** | Zwei Kandidaten mit **identischem** Uplift, Koinzidenz und Wiederholbarkeit; der Distraktor ist um 0,1 dB lauter (`bandpassung` 0,101736 gegen 0,099810, **321 Quanten** Rangabstand) | Snapshot | Der Gesamtrang ist verschieden (321 Quanten), die Zusammenhangskomponenten sind gleich → die zweite Bedingung aus E6 fällt → **ungetrennt** → beide **`mittel`**. Die Bandpassung sagt, **wo** eine Quelle Energie hat, nicht **ob** sie die Ursache ist (G-H3) | `sonde014_hypothese.rs::bandpassung_allein_trennt_nicht` · Korpus `g5_distraktor_lauter` | Heutiger Stand: Distraktor `hoch`/`ready_to_send` (E-D2, M-21 wörtlich gebrochen) |
| **N-15** | Zwei messtechnisch identische Kandidaten; ein vollständiges `intent_update` setzt für den **Distraktor** `rolle = fuehrt`, für die wahre Quelle `begleitet` | Snapshot | `intent_relevanz` 1,0 gegen 0,5 hebt den Gesamtrang des Distraktors (erste Bedingung hält), aber die Zusammenhangskomponenten sind gleich (zweite fällt) → **ungetrennt** → beide **`mittel`**. Die Reihenfolge folgt weiter dem Rang (der Distraktor steht oben) — **die Reihenfolge ist keine Ursachenbehauptung** | `sonde014_gegenbeispiele.rs::a1_intent_rolle_trennt_identische_kandidaten_nicht` | Heutiger Stand (A1): Distraktor `hoch`/`ready_to_send`, die wahre Quelle wird seine Alternative |
| **N-16** | Zwei Kandidaten, deren quantisierte Ränge sich um **exakt zwei Quanten** unterscheiden, Ursache ist ein Band **außerhalb** des Befundintervalls (Band 210) | Snapshot | Gesamtrang verschieden, Zusammenhang gleich → **ungetrennt** → beide **`mittel`** | `sonde014_gegenbeispiele.rs::f7_rangdifferenz_am_quantum` | Heutiger Stand: `a > b` ist wahr, der Führende bleibt `hoch` |
| **N-17** | Zwei Kandidaten; **nur einer** hat einen belegten, positiven Uplift, der andere `uplift = None`. **Vorbedingung, ausdrücklich:** der Uplift ist groß genug, dass die quantisierten Gesamtränge sich um **mindestens ein Quantum** unterscheiden — die Fixture setzt ihn auf Größenordnung **0,6** (nachgemessen 100 000 Quanten Abstand), nicht auf einen Wert nahe `RANG_QUANTUM` | Snapshot | **Beide** Bedingungen aus E6 halten → **getrennt** → der Führende **`hoch`**, der andere **`mittel`** und seine Alternative. Genau **ein** starker Befund (M-21 Satz 1 bleibt) | `sonde014_hypothese.rs::zusammenhang_trennt_und_erlaubt_genau_eine_starke` · Korpus `getrennt_durch_zusammenhang`. **Der Test prüft den Rangabstand zuerst:** ein `assert` auf `rang_quantisiert(a) != rang_quantisiert(b)` steht **vor** der Erwartung `hoch`. Ohne ihn wäre die Zeile grün, wenn der Aufbau den Abstand verlöre — und sie behauptete etwas anderes, als sie misst (Prüfliste §E) | Ohne diesen Fall wäre die Regel von „nie getrennt" nicht zu unterscheiden. *(Fassung Matrixnacharbeit 1: „der Uplift geht in den Rang ein, der Gesamtrang ist also ebenfalls verschieden" — das ist **nicht** allgemein wahr und war der Defekt D7.)* |
| **N-47** | Zwei Kandidaten wie in N-17, aber der Uplift des einen liegt **unterhalb der Sichtbarkeitsgrenze des Gesamtrangs**: `uplift = Some(0,000001)` gegen `None`, sonst gleich (Bandpassung 0,1, Intent-Relevanz 0,5, Routingqualität 1,0) | Snapshot | Der Zusammenhang ist **verschieden** (`Some` gegen `None`, zweite Bedingung hält) und nach R1 sogar **positiv belegt** — der Gesamtrang ist aber für beide **0,266667** (nachgerechnet), also fällt die erste Bedingung → **ungetrennt** → beide **`mittel`**, jeder Alternative des anderen | `hypothese.rs` Modultest `getrennt_verlangt_rang_und_zusammenhang` (zweiter Fall, neben dem Koinzidenzfall aus N-41) | **Fassung Matrixnacharbeit 1:** N-17 hätte hier `hoch` verlangt, während E6 dieselben Kandidaten ungetrennt nennt — zwei Zusagen, die sich widersprechen (D7). Umgesetzt hätte das erneut den **Tie-Break** über die Auswahl entscheiden lassen und M-26 gebrochen (`docs/beweise/SONDE-014.md`:745). Die Zeile ist damit **der Grenzfall zu N-17**: sie zeigt, dass „positiv belegt" (R1) und „getrennt" (E6) zwei verschiedene Fragen sind — ein Kandidat kann die erste bestehen und an der zweiten scheitern |
| **N-41** | Zwei Kandidaten mit **verschiedenen quantisierten Zusammenhangskomponenten, aber gleichem quantisiertem Gesamtrang**: Bandpassung 0,1, Intent-Relevanz 0,5, Routingqualität 1,0, Uplift und Wiederholbarkeit null, Koinzidenz **0,500000 gegen 0,500001** | Die Klasse wird bestimmt | `rang()` liefert für beide exakt **0,35** (nachgerechnet: `quantisiert(2,100000/6)` und `quantisiert(2,100001/6)` runden beide auf 350 000 Quanten) → erste Bedingung fällt → **ungetrennt** → beide **`mittel`**; die Sortierung über die aufsteigende `candidate_source` entscheidet nur noch die **Reihenfolge** | `hypothese.rs` Modultest `getrennt_verlangt_rang_und_zusammenhang` (auf der reinen Funktion, weil eine Pearson-Korrelation von exakt 0,500001 über die Eingabe nicht darstellbar ist) · Verdrahtungsfall mit **exakt gleichen** Komponenten in `sonde014_hypothese.rs::ungetrennter_erster_platz_ist_nicht_stark` (bestehend, muss grün bleiben) | **Fassung Etappe 1:** nur die Komponenten wurden verglichen → „getrennt", und der **Tie-Break** wählte den einzigen starken Befund. Das bricht M-26 (`docs/beweise/SONDE-014.md`:745) |
| **N-42** | **Drei** sichtbare Kandidaten. A: Uplift 0,60, `rolle = fuehrt` (Intent-Relevanz 1,0). B: Uplift 0,40, `rolle = traegt` (0,75). C: Uplift 0,60, `rolle = begleitet` (0,5). Sonst alle gleich. Beiträge zum Rang: A 1,60 · B 1,15 · C 1,10, also Rangfolge **A > B > C**; A und C sind messtechnisch **identisch**. **Warum `begleitet` und nicht `verschmolzen`:** `rolle = verschmolzen` löst ohne eingetragene `fuehrt_vor`-Beziehung `IntentVetoVerschmolzen` aus (`intent.rs`:175–187) — C fiele am Gate, und der Fall träfe den Defekt gar nicht mehr | Snapshot | A ist von B getrennt (Rang und Uplift verschieden), von **C** aber nicht (Uplift gleich, nur der Rang trennt) → nach D2 ist A **nicht** getrennt von allen → **A `mittel`**; B und C sind nicht führend und tragen über M-21 ebenfalls `mittel`. A und C sind einander Alternative | `sonde014_gegenbeispiele.rs::d2_fuehrender_wird_gegen_jeden_geprueft` | **Fassung Etappe 1:** der Vergleich lief nur gegen den Zweitplatzierten B → `getrennt = true` → **A `hoch`**, obwohl C von A ununterscheidbar ist. Ohne die dritte Quelle ist der Defekt nicht sichtbar — der Fall braucht ausdrücklich **drei** Kandidaten |

---

### 2.3 Alignment (R4, D4) — Zeilen N-18 bis N-22, N-44

| # | Zustand | Ereignis | Zusage | Test | Rotbeweis |
|---|---|---|---|---|---|
| **N-18** | Masterfenster in der Mitte der Projektzeit; Sondenfenster 20 000 Samples **davor** und 40 000 Samples **danach** — die Spanne der Sonde **umklammert** die des Masters, **null** Fenster überlappen paarweise | Snapshot | `paarueberdeckung` = 0,0 < 0,95 → `gate()` Schritt 3 fällt mit `alignment_falsch`; der Kandidat verlässt das Ranking **mit Grund** (M-87). Ergebnis: Enthaltung `daten_reichen_nicht` | `sonde014_gegenbeispiele.rs::d4_umklammernde_spanne_ohne_paarweise_ueberlappung` | Heutiger Stand: `zeitueberdeckung(spanne, spanne)` = **1,0**, Gate hält, Klasse `hoch`/`ready_to_send` |
| **N-19** | Sondenfenster liegen **vollständig vor** den Masterfenstern (disjunkt, keine Umklammerung) | Snapshot | `paarueberdeckung` = 0,0 → `alignment_falsch` | `sonde014_gegenbeispiele.rs::d4b_disjunkte_spanne` | Auch heute schon rot — die Zeile steht als **Regressionswache**, nicht als Beleg; das ist im Manifest so benannt (Prüfliste §E) |
| **N-20** | Kandidat 8 Fenster, Master 12 Fenster, alle 8 überlappen paarweise | Snapshot | Anteil der kürzeren Seite = 8/8 = **1,0** ≥ 0,95 → Gate hält. Der Fall erreicht die Klassenwahl (dort entscheidet N-06/N-03) | `sonde014_hypothese.rs::alignment_misst_paarweise_ueberlappung` (Fall „Teilaktivität hält") · Korpus `g5_fenster_genau_acht` | **Kontrollfall, kein eigener Rotbeweis.** Er ist der Beleg, dass E4 die richtige Normierung gewählt hat: mit „Anteil der **Master**fenster" wäre der Wert 8/12 = 0,667, der Fall fiele am Alignment, und der Randwert 8 aus M-23 wäre nicht mehr messbar. Der Rotbeweis von R4 ist N-18 |
| **N-21** | Kandidat und Master je 12 Fenster, Versatz 6 Fenster | Snapshot | 6 von 12 überlappen → 0,5 < 0,95 → `alignment_falsch`. **Unverändert** zum heutigen Stand, Korpusfall `verschobene_passage` bleibt grün | `sonde014_hypothese.rs::alignment_misst_paarweise_ueberlappung` (Fall „Versatz fällt") | Regressionswache |
| **N-22** | Kandidat und Master überlappen zeitlich, aber in **verschiedenen** `transport_epoch` | Snapshot | Kein Partner → `paarueberdeckung` = 0,0 → `alignment_falsch`. Die Epoche steht **im** Partnerprädikat `ueberlappt()`, nicht daneben — **eine** Definition, von `paarueberdeckung` und `gemeinsame_reihen` gemeinsam genutzt | `sonde014_hypothese.rs::alignment_misst_paarweise_ueberlappung` (Fall „fremde Epoche") | Ohne die Epoche im Prädikat zählte ein Fenster einer anderen Zeitachse als Partner |
| **N-44** | Ein Fenster mit `projekt_bis <= projekt_von` (leer). Vertraglich erreichbar: `sample_count` hat `minimum: 0` (`eq-ipc-v3.schema.json`:361), und `fenster_aus_historie` bildet `projekt_bis = von + sample_count`. Konkret `f = [5, 5)` gegen `g = [0, 10)` | `paarueberdeckung` wird gerechnet | `ueberlappt(f, g)` ist **falsch** — `max(5,0) = 5` ist nicht `< min(5,10) = 5`. Das leere Fenster hat keinen Partner und zählt **weder im Zähler noch im Nenner**; `paarueberdeckung` liefert **0,0**, nicht 1,0. Ein Kandidat, dessen **einziges** Fenster leer ist, fällt an `CoverageFehlt` oder `AlignmentFalsch` wie heute, nie durch | `hypothese.rs` Modultest `leere_fenster_haben_keinen_partner` · `sonde014_hypothese.rs::alignment_misst_paarweise_ueberlappung` (Fall „leeres Fenster") | **Fassung Etappe 1:** `f.projekt_bis > g.projekt_von && f.projekt_von < g.projekt_bis` ist für dieses Paar **beidseitig wahr** → `paarueberdeckung` = 1,0 → das Gate hielte, obwohl kein einziges Sample gemeinsam ist. Die **heutige** Rechnung verwirft den Fall an seiner Länge null (`hypothese.rs`:1581–1586) — die Fassung Etappe 1 wäre damit eine **Regression** |

---

### 2.4 Passage und Masteranomalie (R5, D3) — Zeilen N-23 bis N-30, N-43

| # | Zustand | Ereignis | Zusage | Test | Rotbeweis |
|---|---|---|---|---|---|
| **N-23** | Echte Passage über `experiment_begin`, `[0, 12)` Fenster; Kandidat und Master je 12 Fenster **vollständig** darin | Snapshot | `fenster_in_passage` = 12 je Seite, `passagenmaterial` = 12 ≥ 8 → Gate hält | `sonde014_gegenbeispiele.rs::f5_passagenrandwerte` (Fall `[0,12)`) · Korpus `passage_traegt` | Kontrollfall — ohne ihn wären alle Passagenzeilen trivial erfüllt |
| **N-24** | Passage `[0, 7)`, Kandidat und Master je 12 Fenster | Snapshot | `passagenmaterial` = 7 < 8 → `passage_zu_kurz`, Enthaltung `daten_reichen_nicht` | `sonde014_gegenbeispiele.rs::f5_passagenrandwerte` (Fall `[0,7)`) · Korpus `passage_zu_kurz_echt` | Regressionswache; der Randwert `<` gegen `<=` ist die Gegenprobe zu N-25 |
| **N-25** | Passage `[0, 8)` — der Randwert `GATE_MINDEST_FENSTER` | Snapshot | `passagenmaterial` = 8, `8 < 8` ist falsch → Gate hält. M-23 sagt „**mindestens** acht" | `sonde014_gegenbeispiele.rs::f5_passagenrandwerte` (Fall `[0,8)`) | Ein `<=` an dieser Stelle machte den Randwert unerreichbar |
| **N-26** | Passage `[a, b)`; ein Kandidatenfenster ragt am **Anfang** über die Passage hinaus (`projekt_von < a`, `projekt_bis > a`) | Snapshot | Das Fenster zählt **nicht** (R5: „vollständig innerhalb"). Sinkt das Material dadurch unter 8, folgt `passage_zu_kurz` | `sonde014_gegenbeispiele.rs::d3_beruehrende_fenster_zaehlen_nicht_als_passagenmaterial` (Anfang) | Heutiger Stand: vier Fenster weit vor der Passage plus zehn mit je **einem Sample** Überlappung ergeben `hoch`/`ready_to_send` (E-D3) |
| **N-27** | Wie N-26, aber am **Ende** (`projekt_von < b`, `projekt_bis > b`) | Snapshot | Zählt nicht. Ein Fenster mit `projekt_bis == b` zählt dagegen — die Passage ist halboffen `[von, bis)`, wie jedes Evidenzfenster | `sonde014_gegenbeispiele.rs::d3_beruehrende_fenster_zaehlen_nicht_als_passagenmaterial` (Ende, mit beiden Randwerten) | Ohne den Randwert `projekt_bis == b` wäre „vollständig innerhalb" um ein Fenster zu streng |
| **N-28** | Passage `[T, T+4096)`; Master und Sonde je 8 Fenster darin. Danach liefert **nur der Master** ein neuntes Fenster ab `T+4096` mit einer Anomalie in einem **anderen** Band | Snapshot | `masteranomalie` liest das letzte Fenster **innerhalb** der Passage → Befundband und Beobachtung stammen aus der Passage; das Außenfenster geht in nichts ein | `sonde014_gegenbeispiele.rs::a4_masteranomalie_ausserhalb_der_passage` | Heutiger Stand (A4): `master.fenster.last()` liest genau dieses Außenfenster; der Befund nennt die alte Passage und behauptet eine fremde Anomalie mit `hoch`/`ready_to_send` |
| **N-29** | Passage benannt, der Master hat Fenster, aber **keines liegt vollständig darin** (erreichbar an der Kante: Passage kürzer als ein Evidenzfenster, oder alle Masterfenster ragen über einen Rand) | Snapshot | `masteranomalie` **fällt auf das jüngste Fenster überhaupt zurück**, setzt aber `beobachtung.gueltig = false`. Die Rechnung läuft durch alle Gates; `fenster_in_passage(master)` = 0 macht `passagenmaterial` = 0 < 8 für **jeden** Kandidaten → keiner überlebt → **Enthaltung** `daten_reichen_nicht`, Sicherheit `unklar`, jeder Kandidat mit dem Grund, an dem er zuerst gefallen ist. Ein **Befund** mit ungültiger Beobachtung kann nicht entstehen | `sonde014_gegenbeispiele.rs::d3_passage_ohne_masterfenster_ergibt_enthaltung` | **Fassung Etappe 1:** `masteranomalie` gab `None`, `hypothesen()` endete **vor** den Gates (`hypothese.rs`:1105–1106) und die Sitzung schwieg — ein Bruch an M-27 und eine Regression gegen den heutigen Stand |
| **N-43** | Der bereits zugesagte Gegenbeispielfall §2.6 Nr. 17: Passage `[12,24)`, **alle zwölf** Masterfenster und alle Kandidatenfenster liegen davor (`G5-gegenbeispiele-bf92891.patch`:421–423) | Snapshot | Über den Rückfall aus N-29 laufen die Gates. Kandidat 02 besteht Schritt 3 (Alignment gegen den Master hält, beide Seiten liegen deckungsgleich vor der Passage) und fällt an Schritt 4a mit **`passage_unvergleichbar`** → **Enthaltung** `daten_reichen_nicht` / `more_data`, `ausschluesse = [(02, passage_unvergleichbar)]` — genau die Ausgabe, die §2.6 Nr. 17 seit Etappe 1 zusagt | `sonde014_gegenbeispiele.rs::f5_passagenrandwerte` (Fall `[12,24)`) | **Fassung Etappe 1:** die Sitzung hätte **geschwiegen**; die zugesagte Enthaltung mit `passage_unvergleichbar` wäre nicht entstanden. Der Defekt war in der Matrix selbst nicht sichtbar, weil §2.4 und §2.6 verschiedene Dinge sagten |
| **N-30** | Keine Passage in der Sitzung | Snapshot | `masteranomalie(master, None)` liest das jüngste Fenster wie heute; `baue_befund` zählt `unabhaengige_fenster` über die ganze Historie wie heute | bestehende Fälle in `sonde014_hypothese.rs` müssen grün bleiben | Regressionswache |

---

### 2.5 Korpus, Erzeuger, Prüfer (R6, D5, D6) — Zeilen N-31 bis N-40, N-45, N-46

| # | Zustand | Ereignis | Zusage | Test | Rotbeweis |
|---|---|---|---|---|---|
| **N-31** | Der Erzeuger kennt je Quelle nur eine konstante `anhebung_db` | `erzeuge_p5_korpus.py` läuft | `quelle()` und das Masterfeld nehmen zusätzlich `pegelreihe_db` und `onsetreihe`; im JSON steht **immer die ausgeschriebene Reihe**, nie die Kurzform. `--pruefen` bleibt bytegleich (Bein A28) | `erzeuge_p5_korpus.py --pruefen` (A28) | Eine Kurzform in der Fixture ließe sich von zwei Lesern verschieden auflösen |
| **N-32** | `sonde014_p5_korpus.rs::sitzung_fahren` baut den Coordinator **ohne** Store; `experiment_begin` ist ein persistenzpflichtiger P0 | Eine Sitzung trägt ein `passage`-Feld `{von_offset, bis_offset}` (Samples **relativ zum Basissample** `44 108 200`, nicht Fensterindizes — nur so sind halbe Fensterschritte wie in Nr. 27 darstellbar) | Das Bein baut für **solche** Sitzungen einen Coordinator mit Store (Muster `Buehne::mit_store` aus dem G5-Patch), sendet `state_report` und `experiment_begin` vor der Evidenz, und `juengste_passage_im_projekt` liefert die Passage. Sitzungen **ohne** `passage` laufen unverändert ohne Store — der Bestandsweg bleibt Zeile für Zeile erhalten | `sonde014_p5_korpus.rs::korpus_kette_laeuft_durch_den_produktpfad` | **G-L1:** heute läuft `gate()` Schritt 4 im gesamten Korpus **nie**; `verschobene_passage` fällt am Master-Alignment, nicht an der Passage |
| **N-33** | Der Korpus kennt keine Quelle mit `mixer: null`, obwohl `erzeuge_p5_korpus.py`:178 den Fall vorsieht | Korpuslauf | Eine Sitzung `routing_unbekannt` trägt `mixer: null` → `routing_bekannt` = false → `routingqualitaet` = 0 → **`mittel`**. Wahrheit `routing_unbekannt`, in `keine_starke` | Korpus `routing_unbekannt`, Prüfer A29 | **E-H3:** „Routing unbekannt" ist heute nur im Rustbein und nicht in der Kalibrierung gemessen |
| **N-34** | Die geschlossene Wahrheitsmenge des Korpus trägt fünf Werte in `keine_starke` (`distraktor`, `parent_duplikat`, `verschobene_passage`, `zu_kurze_passage`, `keine_ursache`) | Die Gegenbeispiele kommen dazu | Die Menge wächst um genau **zwei** Werte — **`gegenbeleg`** und **`routing_unbekannt`** — und bleibt geschlossen (M-67 wörtlich: „Die Menge wächst gegenüber P4 um die Ursachenwahrheiten und bleibt geschlossen"). **Kein** Wert `kein_zusammenhang`: eine Quelle ohne belegten Zusammenhang ist entweder `keine_ursache` (sie drängt nicht) oder `distraktor` (sie drängt, ist aber nicht kausal) — ein dritter Name für dieselben zwei Lagen wäre eine Wahrheit ohne eigenen Fall. Die Menge lebt weiter **nur** im `MANIFEST.json` des Korpus — **kein** Vertragsschritt, die Menge der **Ausschlussgründe** bleibt unberührt (NAK-213) | `pruefe_p5_korpus.py` (geschlossene Menge, Muster NAK-182 R1a) · A28 | Ein Wahrheitswert außerhalb der Menge muss rot fallen |
| **N-35** | Der Prüfer weist Kennzahlen je Ursachenklasse aus | Der erweiterte Korpus läuft | Ein **eigener Abschnitt „Gegenbeispiele"** (M-69) prüft: über alle Sitzungen mit einer Wahrheit aus `keine_starke` ist `falsche_starke` = 0 **und** die Zahl der geprüften Gegenbeispiele > 0. Der zweite Teil ist der Riegel gegen die leere Menge — „auf jedem Gegenbeispiel unsicher" ist ohne ein einziges Gegenbeispiel wahr und wertlos | `pruefe_p5_korpus.py` (A29), Selbsttest in A30 mit beiden Richtungen | Ohne die Nichtleere-Bedingung wäre der Abschnitt grün, wenn kein Gegenbeispiel geladen würde |
| **N-36** | Der Selbsttest fährt jede Erwartung mit ihrem Gegenteil (M-68, Muster A27) | Der neue Abschnitt kommt dazu | Auch der Gegenbeispielabschnitt läuft mit Gegenteil: eine synthetische Menge mit einer falschen starken Behauptung fällt, dieselbe ohne sie hält, die leere Menge fällt | `pruefe_p5_korpus.py --selbsttest` (A30) | Eine Riegelerwartung ohne Gegenteil wäre auch dann grün, wenn der Riegel immer rot meldete |
| **N-37** | Fälle, die NAK-212 **nicht** schließt (NAK-213/214) | Korpuslauf | Sie stehen als Korpussitzung mit **gedruckter, benannter Lücke** im `MANIFEST.json` (`nicht_gemessen`: `kennung`, `ticket`, `zusage`, `was_heute_passiert`; Muster NAK-190) und werden vom Prüfer als **eigener Abschnitt** gemeldet: nicht grün, nicht rot, **benannt**. Jede Zeile trägt `kennung`, `ticket`, `zusage` und **`wirkung`** mit genau zwei Werten. **`wirkung: "ausgenommen"`** — die Sitzung liefert **bewusst** eine falsche starke Behauptung, weil ein anderes Ticket sie schließt (Nr. 9, 11, 14 in §2.5.1): sie wird aus `falsche_starke` und aus den Kennzahlen **herausgenommen** und stattdessen als offene Lücke gedruckt. Ohne diese Ausnahme wäre der Kanon auf einem Korpus, der die offenen Fälle absichtlich scharf hält, dauerhaft rot, und der Bau müsste die Fälle entweder entschärfen oder weglassen — beides verbietet R6. **`wirkung: "benannt"`** — die Sitzung ist in ihrer **Sicherheit** korrekt, aber eine **andere** Zusage bleibt ungemessen (Nr. 17: der sechste Kandidat trägt keinen Grund, M-87): sie zählt normal in allen Kennzahlen mit und wird zusätzlich gedruckt. Dazu zwei Regeln, beide mit Gegenteil im Selbsttest: **(a)** Verschwindet eine Zeile aus `nicht_gemessen`, ohne dass ein Ticket sie schließt, ist das **rot** — dieselbe Logik wie `p4_luecke()` (`pruefe_p5_korpus.py`:298–325). **(b)** Liefert eine als `ausgenommen` geführte Sitzung inzwischen ein Ergebnis innerhalb ihrer Wahrheit, meldet der Prüfer das ausdrücklich als „gedruckt, inzwischen gemessen" — die Zeile darf dann weg. Ohne (b) bliebe eine geschlossene Lücke stumm im Manifest stehen. **Kettenbeinfälle** (`f8`, `f8b`, `f4`) stehen **nicht** in `nicht_gemessen`: sie laufen ohne Korpuswahrheit, ihre Lücke steht im Testtext des Beins und in §4 dieses Manifests | `pruefe_p5_korpus.py::offene_luecken` (neu, nach dem Muster von `p4_luecke`), Selbsttest in A30 für beide `wirkung`-Werte und für (a) und (b) | Ohne `wirkung: "ausgenommen"` fällt der Lauf auf den drei offenen Fällen (Nr. 3, 5, 8 in §2.6) mit Exit 2 — das ist die Ausgangslage des Gates und der Grund für die Regel. Ohne (a) wäre eine still verschwindende Lücke eine Zusage ohne Messung |
| **N-38** | Der Kanon nennt Gegenbeispiele nicht als eigene Zeile | Das neue Bein entsteht | `tools/beweise.ps1` bekommt **A31** = `cargo test --test sonde014_gegenbeispiele`. Begründung wie bei A21 (`transport_fuzz`): der Gate-Text von G5 nennt adversariale Gegenbeispiele **namentlich**, und ein namentlich geforderter Beweis, der nur in der Sammelzeile A4 steckt, ist von außen nicht als gefahren zu erkennen | `tools/beweise.ps1` (A31) | — (Runnerzeile, kein Verhalten) |
| **N-39** | Die Kalibrierung (M-31) steht auf `hoch`, gemessen am Sechserkorpus | Der erweiterte Korpus läuft | `schwelle_suchen()` wird **neu gemessen** und das Ergebnis im Manifest ausgewiesen — mit `precision`, `recall`, `brier`, `kalibrierung` je Ursachenklasse und der Zuverlässigkeitstafel. Die Zahl ist **Ausgabe**, nicht Eingabe (M-66) | `pruefe_p5_korpus.py` (A29) | Eine Schwelle, die dem Bein hineingegeben wird, bestätigt ihre eigene Eingabe |
| **N-40** | `METRICS_VERSION` = 20260904 an vier Codestellen, `metriken-v1.json.aktuell` ebenso | Die Kalibrierung läuft auf dem erweiterten Korpus | **Ausgang B (erwartet):** die niedrigste haltende Stufe bleibt `hoch` → **kein** Schritt, alle vier Stellen unverändert, `eq-copilot/plugin/` unberührt (E-12 Fall 3). **Ausgang A:** die Stufe bewegt sich → alle vier Stellen **und** das Register gehen im **selben** Commit auf `20260907` | `pruefe_p5_korpus.py` (A29) entscheidet den Ausgang; `pruefe_v3_vertrag.py` (A5) misst die Kopplung in beiden Ausgängen | **Fassung Etappe 1:** der Schritt war unbedingt und nannte nur zwei der vier Stellen; `kFeatureMetricsVersion` und `prepost.rs` blieben auf `20260904` und hätten A5 **zwingend rot** gemacht |
| **N-45** | Ausgang A ist gezogen | A5 läuft | Alle vier Konstanten nennen dieselbe Fassung wie das Register: `vergleichbarkeit.rs`:149, `prepost.rs`:206, `FeatureEngine.h`:783 und `metriken-v1.json`:20. Die neue Fassung führt alle vier Gate-Schwellen unverändert weiter | `pruefe_v3_vertrag.py` (A5), Prüfungen `metrics_version_bindet_schwellen` und `comparability_schwellen_haengen_an_metrics_version` (beide bestehend) | Jede einzeln ausgelassene Stelle fällt bereits heute rot — das ist die **bestehende** Wache, kein neuer Rotbeweis. Die Zeile steht als **Änderungssatz-Zusage** (Prüfliste §F), nicht als Beleg |
| **N-46** | Zwei ungetrennte Kandidaten nennen sich nach R3 **wechselseitig** als Alternative; die Sitzung deklariert eine Distraktorquelle | `pruefe_p5_korpus.py` liest den Lauf | Die NR-14-Prüfung fragt „**mindestens eine** Alternative im Lauf löst auf einen Befund der Distraktorquelle auf" statt „**jede** Alternative gehört ihr" — das ist der Nachweis, den M-65 wörtlich verlangt („der korrelierte Distraktor **muss** in `alternatives` erscheinen"). Die beiden anderen Riegel derselben Stelle bleiben unverändert: eine Alternativ-ID muss auf einen Befund **desselben Laufs** auflösen, und kein Befund nennt sich selbst | `pruefe_p5_korpus.py::_faelle_bilden` (A29) · Gegenproben in A30: (a) wechselseitige `mittel`-Befunde grün, (b) dieselbe Menge mit einem `hoch` auf dem Distraktor rot, (c) **keine** Alternative auf der Distraktorquelle rot, (d) Alternativ-ID ohne Befund im Lauf rot | **Fassung Etappe 1:** die Prüferzeile war nicht Teil der Zusage. `_faelle_bilden` (`pruefe_p5_korpus.py`:425–429) hätte den korrekten Rückverweis des Distraktorbefunds auf die wahre Quelle als Problem gemeldet, und **`g5_distraktor_mehr_fenster` wäre in A29 rot** — bei zwei völlig richtigen `mittel`-Befunden |

---

### 2.5.1 Der erweiterte Korpus — die vollständige Sitzungsliste

**27 Sitzungen** (heute 6). „Reihe" sagt, ob die Quelle eine je-Fenster-Pegelreihe beziehungsweise Onsetreihe trägt (E7); ohne Angabe ist der Pegel konstant wie heute. „max" ist `erwartet.sicherheit_hoechstens`. Sitzungen mit **offen** stehen zusätzlich in `nicht_gemessen` (N-37).

| # | Kennung | Aufbau | Reihe | Wahrheit | max | Zweck / Matrixzeile |
|---|---|---|---|---|---|---|
| 1 | `wahrer_kandidat` | Bestand, unverändert | — | `wahre_ursache` | **`mittel`** (war `hoch`) | E7: kein Fenster ohne die Quelle → M-19 unbelegt. N-03 |
| 2 | `korrelierter_distraktor` | Bestand, unverändert | — | `distraktor` | `mittel` | unverändert |
| 3 | `parent_duplikat` | Bestand, unverändert | — | `parent_duplikat` | `mittel` | unverändert |
| 4 | `verschobene_passage` | Bestand, unverändert | — | `verschobene_passage` | `unklar` | N-21 |
| 5 | `zu_kurze_passage` | Bestand, unverändert | — | `zu_kurze_passage` | `mittel` | unverändert |
| 6 | `daten_reichen_nicht` | Bestand, unverändert | — | `keine_ursache` | `unklar` | M-27 |
| 7 | `g5_distraktor_mehr_fenster` | 12 gegen 16 Fenster | Pegel, beide gleich | `distraktor` | `mittel` | N-13 |
| 8 | `g5_distraktor_lauter` | +0,1 dB auf dem Distraktor | Pegel, beide gleich | `distraktor` | `mittel` | N-14 |
| 9 | `g5_distraktor_allein_im_rennen` | Verursacher mit Versatz 6 fällt am Alignment | Pegel auf dem Distraktor | `distraktor` | `mittel` | **offen, NAK-213 R2** |
| 10 | `g5_unbeteiligte_quelle` | Sonde mit `anhebung_db` 0,0, 12 Fenster, bekanntes Routing | — | `keine_ursache` | **`mittel`** | N-01/N-03. **Korrektur gegen den G5-Patch**, der hier `unklar` setzt: die Sonde passiert alle fünf Gates (Coverage, Alignment, keine Passage, kein Veto), also überlebt ein Kandidat und `hypothesen()` baut keine Enthaltung. `unklar` ist im Datenweg **ausschließlich** die Klasse von `enthaltung()` (`hypothese.rs`:1313); ein überlebender Kandidat kann sie nicht tragen. Die Ursachenklasse bleibt `quelle_resonanz` — nicht `daten_reichen_nicht` |
| 11 | `g5_sonde_auf_masterkanal` | Sonde auf `mixer: 0` | Pegel | `parent_duplikat` | `mittel` | **offen, NAK-213 R3** |
| 12 | `g5_parent_partner_faellt_aus` | Partner auf Kanal 7 mit Versatz 6 | — | `parent_duplikat` | `mittel` | grün |
| 13 | `g5_parent_partner_zu_wenig_fenster` | Partner mit 2 Fenstern | — | `parent_duplikat` | `mittel` | grün |
| 14 | `g5_parent_partner_ohne_evidenz` | Partner mit 0 Fenstern | Pegel | `parent_duplikat` | `mittel` | **offen, NAK-213 R2** |
| 15 | `g5_fenster_genau_acht` | 8 Kandidaten-, 12 Masterfenster | Pegel | `wahre_ursache` | **`hoch`** | N-20, N-25-Analogon ohne Passage |
| 16 | `g5_fenster_sieben` | 7 Kandidatenfenster | Pegel | `zu_kurze_passage` | `mittel` | Gegenprobe zu 15 — nur die Fensterzahl trennt |
| 17 | `g5_kandidatendeckel` | sechs Kandidaten, Verursacher am leisesten | — | `distraktor` | `mittel` | Klasse geschlossen, **Lücke M-87 offen, NAK-213 R1** |
| 18 | `g5_gleichstand_verursacher_fuehrt` | Verursacher +0,1 dB | — | `distraktor` | **`mittel`** (war `hoch`) | N-14, Verschärfung |
| 19 | `zusammenhang_uplift` | **neu** — Sonde in 6 von 12 Fenstern aktiv, Master folgt | Pegel gleichläufig | `wahre_ursache` | **`hoch`** | N-06, Kontrollfall |
| 20 | `zusammenhang_koinzidenz` | **neu** — Pegel konstant, Onsets variieren gleichläufig | Onset gleichläufig | `wahre_ursache` | **`hoch`** | N-07, Kontrollfall |
| 21 | `gegenbeleg_uplift` | **neu** — Sonde laut, wenn der Master leise ist | Pegel gegenläufig | `gegenbeleg` | `mittel` | N-08, N-10 |
| 22 | `gegenbeleg_koinzidenz` | **neu** — Onsets gegenläufig, Pegel konstant | Onset gegenläufig | `gegenbeleg` | `mittel` | N-09 |
| 23 | `routing_unbekannt` | **neu** — Quelle mit `mixer: null` | Pegel | `routing_unbekannt` | `mittel` | N-33 (E-H3) |
| 24 | `getrennt_durch_zusammenhang` | **neu** — zwei Kandidaten, nur einer mit belegtem Uplift; seine Pegelreihe ist so gewählt, dass der Uplift in der **Größenordnung 0,6** liegt und die quantisierten Gesamtränge sich um rund **100 000 Quanten** unterscheiden (D7) | Pegel auf einem | `wahre_ursache` | **`hoch`** | N-17, Kontrollfall. Der Rangabstand ist Vorbedingung und wird im Test gemessen, nicht angenommen |
| 25 | `passage_traegt` | **neu** — `experiment_begin` mit `{von_offset: 0, bis_offset: 6144}` (= 12 Fensterlängen), 12 Fenster je Seite, alle vollständig innerhalb | Pegel | `wahre_ursache` | **`hoch`** | N-23, N-32 (G-L1) — der Kontrollfall, ohne den jede Passagenzeile trivial erfüllt wäre |
| 26 | `passage_zu_kurz_echt` | **neu** — `{von_offset: 0, bis_offset: 3584}` (= 7 Fensterlängen), 12 Fenster je Seite | Pegel | `zu_kurze_passage` | `unklar` | N-24: Material 7 < 8 → `passage_zu_kurz`, Enthaltung |
| 27 | `passage_teilweise_innerhalb` | **neu** — Master und Kandidat je 12 Fenster à 512 Samples ab `basis` (Fenster *k* = `[basis+512k, basis+512k+512)`). Die Passage läuft von `basis+256` bis `basis+4352`, ist also **8 Fensterlängen lang und um einen halben Fensterschritt versetzt** | Pegel | `zu_kurze_passage` | `unklar` | N-26/N-27 (E-D3). **Gerechnet:** vollständig innerhalb liegen die Fenster *k* = 1..7 → Material **7 < 8** → `passage_zu_kurz`. Nach der heutigen Berührungsregel (`bis > von_p && von < bis_p`) wären es *k* = 0..8 → **9 ≥ 8** → der Fall hielte. Gate 4a hält in beiden Fällen (Zeitüberdeckung der Spannen = 1,0, normiert auf die 4096 Samples der Passage), Gate 3 ebenfalls — **nur** das Materialgate trennt. Der Master trägt dieselben 7 Fenster, also ist `masteranomalie` besetzt und der Fall trifft **nicht** die Lücke N-29 |

**`sicherheit_hoechstens` ist die EXAKT erreichbare Klasse, keine Obergrenze.** Der Prüfer misst beide Richtungen: `falsche_starke` fängt die zu kühne Aussage, `falsche_schwache` (`sicherheit == mittel` und `sicherheit_hoechstens < mittel`) die zu vorsichtige. Eine Sitzung, die `unklar` erwartet und `mittel` liefert, fällt deshalb genauso rot wie umgekehrt. Daraus folgt eine harte Bauregel: **`unklar` steht nur dort, wo `hypothesen()` wirklich die Enthaltung baut** — also wenn **kein** Kandidat das Gate passiert (Nr. 4, 6, 26, 27). Überlebt ein Kandidat, ist die kleinste erreichbare Klasse `mittel`, auch wenn er offensichtlich nicht die Ursache ist.

**Sollfälle für den Recall** (`ist_wahre_ursache` **und** `sicherheit_hoechstens = hoch`): Nr. 15, 19, 20, 24, 25 — **fünf**. Ohne die Zeilen 19 bis 25 wäre der Nenner null und die Kalibrierung eine Aussage über nichts (E7).

**Was die 27 Sitzungen nicht abdecken und deshalb im Kettenbein `sonde014_gegenbeispiele.rs` steht:** eine echte Passage mit **Randwerten** und zweitem `experiment_begin` (`f5`, `f5b`), zwei Master (`f8`), Master ohne Fenster (`f8b`), Kanalwechsel während der Sitzung (`f4`, `f4b`), eine Quelle in einem **anderen Band** als der Befund (`eigen4`), Rücknahme und Intent-Veto nach der Zustellung (`f9`), die Rangdifferenz am Quantum (`f7`), die Intent-Rolle als falsche Trennung (`a1`), umklammernde und disjunkte Spannen (`d4`, `d4b`), die Masteranomalie außerhalb der Passage (`a4`), berührende Passagenfenster (`d3`) und — seit der Matrixnacharbeit 1 — der **Dreierfall** aus D2 (`d2_fuehrender_wird_gegen_jeden_geprueft`, N-42; er braucht drei Rollen aus einem `intent_update`) sowie die **Passage ohne Masterfenster** aus D3 (`d3_passage_ohne_masterfenster_ergibt_enthaltung`, N-29). Das Korpusformat kennt je Quelle nur Anhebung/Reihe, Fensterzahl, Mixerkanal, Versatz und — neu — die Passage; alles Übrige braucht den vollen Produktpfad mit Store.

---

### 2.6 Die 27 Gegenbeispieleingaben — erwartete Ausgabe und Ticketzuordnung

Quelle: `docs/beweise/roh/G5-gegenbeispiele-bf92891.md`, Abschnitt „Tabelle". Spalte **heute** ist die gemessene Ausgabe auf `bf92891`; Spalte **nach NAK-212** ist die Zusage dieser Matrix. „Lücke" heißt: der Fall wird als Korpussitzung beziehungsweise Kettenfall **gefahren und gedruckt**, aber erst ein anderes Ticket schließt ihn (N-37).

| # | Fall | heute | nach NAK-212 | Ursachenklasse / Zustand / Alternativen / Ausschlüsse | schließt | Regel |
|---|---|---|---|---|---|---|
| 1 | `g5_distraktor_mehr_fenster` | 2 × `mittel` | 2 × **`mittel`** | `zwei_quellen_konkurrenz` / `more_data` / je die andere ID / — | **NAK-212** (verschärft: heute Gleichstand, künftig Strukturregel) | R3, N-13 |
| 2 | `g5_distraktor_lauter` | Distraktor `hoch` | 2 × **`mittel`** | `zwei_quellen_konkurrenz` / `more_data` / je die andere ID / — | **NAK-212** | R3, N-14 |
| 3 | `g5_distraktor_allein_im_rennen` | `hoch` | **`mittel`** | `quelle_resonanz` / `more_data` / — / `(02, alignment_falsch)`. **R1 senkt ihn** bei konstantem Pegel; die Sitzung trägt deshalb eine **Pegelreihe mit belegtem Uplift**, damit die Lücke wirklich beißt — Ausgabe bleibt dann **`hoch`**, Wahrheit `distraktor`, Zeile in `nicht_gemessen` mit `wirkung: ausgenommen` | **offen, NAK-213 R2** | R1 + NAK-213 R2 |
| 4 | `g5_unbeteiligte_quelle` | `hoch` | **`mittel`** | `quelle_resonanz` / `more_data` / — / —; Korpuswahrheit `keine_ursache`, `sicherheit_hoechstens` **`mittel`** (nicht `unklar`, siehe §2.5.1 Nr. 10) | **NAK-212** | R1, N-03 |
| 5 | `g5_sonde_auf_masterkanal` | `hoch` | **`hoch`** (Lücke) | `quelle_resonanz` / `ready_to_send` — die Sonde misst das Mastersignal, ihr Uplift gegen den Master ist deshalb **perfekt belegt**; R1 greift nicht. Die Sitzung trägt eine Pegelreihe, damit der Fall scharf bleibt | **NAK-213 R3**, gedruckt | — |
| 6 | `g5_parent_partner_faellt_aus` | `mittel` | **`mittel`** | `quelle_resonanz` / `more_data` / — / `(03, alignment_falsch)` | unverändert grün (Parent-Term) | — |
| 7 | `g5_parent_partner_zu_wenig_fenster` | 2 × `mittel` | 2 × **`mittel`** | `zwei_quellen_konkurrenz` / `more_data` | unverändert grün | — |
| 8 | `g5_parent_partner_ohne_evidenz` | `hoch` | **`hoch`** (Lücke) | `quelle_resonanz` / `ready_to_send`; mit Pegelreihe scharf gehalten | **NAK-213 R2**, gedruckt | — |
| 9 | `g5_fenster_genau_acht` | `hoch` | **`hoch`** | `quelle_resonanz` / `ready_to_send`; die Sitzung bekommt eine **Pegelreihe mit belegtem Uplift**, sonst fiele sie an R1 und der Randwert 8 wäre nicht mehr gemessen | **NAK-212** (Kontrollfall, R4 N-20) | R1, R4 |
| 10 | `g5_fenster_sieben` | `mittel` | **`mittel`** | `quelle_resonanz` / `more_data`; ebenfalls mit Pegelreihe, damit **nur** die Fensterzahl trennt | unverändert grün | — |
| 11 | `g5_kandidatendeckel` | `hoch` auf `..07`, `..02` spurlos | **`mittel`** auf allen fünf sichtbaren; `..02` **weiterhin spurlos** | `zwei_quellen_konkurrenz` / `more_data` / vier IDs / —. **R3 senkt die Klasse** (alle fünf ungetrennt); die **Lücke M-87** (kein Grund für den sechsten) bleibt und steht in `nicht_gemessen` mit `wirkung: benannt` | **teilweise, NAK-213 R1** | R3 + NAK-213 R1 |
| 12 | `g5_gleichstand_verursacher_fuehrt` | `hoch` + `mittel` | 2 × **`mittel`** | `zwei_quellen_konkurrenz` / `more_data`; die Trennung war **nur** Bandpassung (0,1 dB) | **NAK-212** (verschärft) | R3, N-14 |
| 13 | `f5` Passage `[0,12)` | `hoch` | **`hoch`** | mit Pegelreihe; Kontrollfall | **NAK-212** | R5, N-23 |
| 14 | `f5` Passage `[0,11)` | `hoch` | **`hoch`** | Material 11 ≥ 8 | unverändert | R5 |
| 15 | `f5` Passage `[0,8)` | `hoch` | **`hoch`** | Randwert | unverändert | R5, N-25 |
| 16 | `f5` Passage `[0,7)` | `daten_reichen_nicht` | **`daten_reichen_nicht`** | `more_data` / — / `(02, passage_zu_kurz)` | unverändert grün | R5, N-24 |
| 17 | `f5` Passage `[12,24)` | `daten_reichen_nicht` | **`daten_reichen_nicht`** | `more_data` / — / `(02, passage_unvergleichbar)`. **Der Weg dorthin ist seit der Matrixnacharbeit 1 neu:** alle zwölf Masterfenster liegen vor der Passage, also greift der Rückfall aus E5 (ungültige Beobachtung), die Gates laufen, und Kandidat 02 fällt an Schritt 4a. Die **Ausgabe** ist unverändert die seit Etappe 1 zugesagte | unverändert grün | R5, N-29, N-43 |
| 18 | `f5b` gleiche `passage_id`, verschoben | `daten_reichen_nicht` | **`daten_reichen_nicht`** | `(02, passage_unvergleichbar)`; fail-closed | unverändert grün (Härtung G-H2 → NAK-220) | — |
| 19 | `f8` zwei Master | `hoch` im fremden Band | **`mittel`** | `quelle_resonanz` / `more_data` — die Sonde trägt im Band von Master B `bandpassung` 0,0121, aber **`uplift = None`** (konstanter Pegel) → R1. **Lücke bleibt:** der erste Master verschwindet weiterhin lautlos → gedruckt, **NAK-213 R4** | teilweise | R1 + NAK-213 R4 |
| 20 | `f8b` Master ohne Fenster | Schweigen | **Schweigen** (Lücke) | — | **NAK-213 R4**, gedruckt | — |
| 21 | `f4` vor dem Kanalwechsel | 2 × `mittel` | 2 × **`mittel`** | unverändert | — | — |
| 22 | `f4` nach dem Kanalwechsel | `hoch` | **`mittel`** | R1 greift (konstanter Pegel). **Lücke bleibt:** die Deskriptoränderung hebt weiterhin `routingqualitaet` ohne neue Messung → gedruckt, **NAK-213 R5** | teilweise | R1 + NAK-213 R5 |
| 23 | `f4b` drei Sonden auf einem Kanal | 3 × `mittel` | 3 × **`mittel`** | unverändert grün | — | — |
| 24 | `eigen3` antikorreliert | `hoch`, Rang 0,4358 | **`mittel`**, `wiederholbarkeit` **0** statt 0,995 | `quelle_resonanz` / `more_data` / — / — | **NAK-212** | R2, N-08/N-10 |
| 25 | `eigen4` falsches Band | `hoch`, `bandpassung` 0,0161 | **`mittel`** | `quelle_resonanz` / `more_data` | **NAK-212** | R1, N-02 |
| 26 | `eigen5` antikorreliert neben korreliert | `hoch` + `mittel` | **`hoch`** (korreliert) + **`mittel`** (antikorreliert) | getrennt über den Uplift: `Some(positiv)` gegen `Some(negativ)`, im Rang 1,0 gegen 0 (geklemmt) → beide Bedingungen aus E6 halten mit rund **166 000 Quanten** Rangabstand → genau **ein** starker Befund | **NAK-212** (Kontrollfall) | R2, R3, N-17 |
| 27 | `f7` Rangdifferenz 2 Quanten | `hoch` | 2 × **`mittel`** | `zwei_quellen_konkurrenz` / `more_data` | **NAK-212** | R3, N-16 |

**Zur Trennungsdefinition in dieser Tabelle:** seit der Matrixnacharbeit 1 (D1, D2) heißt „getrennt" *verschiedener quantisierter Gesamtrang UND verschiedener Zusammenhang*, geprüft gegen **jeden** Überlebenden. Für die Zeilen 1, 2, 12 und 27 ändert das die Ausgabe nicht — sie waren schon nach der Fassung Etappe 1 ungetrennt, jetzt zusätzlich über die erste Bedingung. Für Zeile 26 (`eigen5`) bleibt es bei **getrennt**: positiver gegen negativen Uplift ist ein Unterschied in beiden Bedingungen. Der Fall, den erst D1/D2 fangen, ist in keinem der 27 Gegenbeispiele enthalten — er steht als N-41 und N-42 mit eigenen Testfällen. **Seit der Matrixnacharbeit 2 (D7)** gilt für jede Zeile dieser Tabelle, die einen **starken** Befund zusagt: der quantisierte Rangabstand ist Vorbedingung und wird im Test gemessen. Betroffen ist hier allein Zeile 26 — die übrigen starken Zeilen (9, 13, 14, 15) tragen **einen einzigen** Kandidaten, und ein einziger Überlebender hat niemanden, von dem er sich abheben müsste (`_ => true`).

**Bilanz der Zusage, gezählt.** Der Gegenbeispiel-Lauf fand **11 falsche starke Behauptungen** (Zeilen 2, 3, 4, 5, 8, 11, 19, 22, 24, 25, 27 dieser Tabelle).

| Ausgang | Zahl | Zeilen |
|---|---|---|
| **vollständig geschlossen** — die Sitzung erreicht `hoch` auch mit belegtem Zusammenhang nicht mehr | **5** | 2, 4, 24, 25, 27 |
| **in der Sicherheitsklasse geschlossen, Restlücke gedruckt** — die falsche starke Behauptung entfällt, ein zweiter Bruch bleibt (M-87 Deckelgrund, erster Master verschwindet, Routingqualität ohne Messung) | **3** | 11 (NAK-213 R1), 19 (NAK-213 R4), 22 (NAK-213 R5) |
| **ausdrücklich offen, scharf gehalten und gedruckt** — die Sitzung trägt bewusst eine Pegelreihe, damit sie `hoch` **bleibt** und die Lücke beißt; Ausnahme vom Riegel nach N-37 (a) | **3** | 3 (NAK-213 R2), 5 (NAK-213 R3), 8 (NAK-213 R2) |

Dazu ein **Schweigen** ohne Ergebnis (Zeile 20, NAK-213 R4), das dieses Ticket nicht anfasst.

**Was das Ticket zusätzlich verschärft, ohne dass es ein Befund war:** Zeile 12 (`g5_gleichstand_verursacher_fuehrt`) galt im Gegenbeispiel-Lauf als **zulässige** starke Aussage — der Verursacher führte um 0,1 dB und wurde `hoch`. Nach R3 ist diese Trennung reine Bandpassung und trägt keine Ursachenbehauptung mehr: **beide fallen auf `mittel`**. Dasselbe gilt für `wahrer_kandidat` im Bestandskorpus (E7). Das ist gewollt und vom Gate-Text gedeckt („Enthaltung ist das gewünschte Ergebnis"), kostet aber zwei bisher starke Fälle — deshalb tragen die Kontrollfälle in §2.5 (`zusammenhang_uplift`, `zusammenhang_koinzidenz`, `getrennt_durch_zusammenhang`, `passage_traegt`, `g5_fenster_genau_acht` mit Pegelreihe, `eigen5`) den Nachweis, dass `hoch` erreichbar **bleibt** und der Recall nicht auf null fällt.

---

### 2.7 SONDE-014 §3 — was unverändert gilt, was dieses Ticket präzisiert

| Zeile | Stand nach NAK-212 | Wortlaut |
|---|---|---|
| **M-15** | **unverändert** | „`confidence.class` und `confidence.score` sind **zwei** Felder; die Klasse wird nicht aus dem Score gerundet." — Jeder neue Term der Klassenwahl ist ein Prädikat über eine benannte Struktur (`positiv_belegt()`, `gegenbeleg()`, `getrennt`), kein Vergleich gegen eine kalibrierte Zahl. Die Vorzeichengrenze null ist die Definition von „positiv", keine Schwelle; sie steht in `metriken-v1.json` nicht und ist nicht abstimmbar. |
| **M-19** | **präzisiert** | Bisher: „vergleicht **bedingten Uplift** (Zielmetrik bei aktiver Quelle gegen ähnliche Fenster ohne diese Aktivität), prüft Stabilität über **Block-Bootstrap**". **Nachtrag NAK-212:** Ohne Vergleichsfenster gibt es **keinen** Uplift — nicht 0, sondern **keinen Wert** (`Option::None`); drei Lagen führen dorthin (weniger als zwei gemeinsame Fenster; kein Band mit Gültigkeitsbit im Befundintervall; alle Fenster auf einer Seite des eigenen Medians). Die Stabilität wird **einseitig** gemessen (`bootstrap_p_positiv`): sie prüft die Wiederholbarkeit eines **positiven** Effekts. Eine stabil negative Reihe trägt `wiederholbarkeit` = 0, nicht 0,995. |
| **M-20** | **präzisiert** | Bisher: „**Keine einzelne hohe Komponente darf fehlende Coverage oder falsches Alignment kompensieren.** Das ist eine harte Gate-Regel vor der Gewichtung." **Nachtrag NAK-212:** Das Alignment-Gate misst die **paarweise** Zeitüberlappung der Fenster (`paarueberdeckung`), nicht die Überlappung ihrer Spannen. Eine Spanne ist ein lückenblindes `min/max`; zwei Fensterfolgen mit **null** gemeinsamen Fenstern erreichten darüber eine Überdeckung von 1,0. Die Schwelle `GATE_ZEITUEBERDECKUNG` = 0,95 bleibt, normiert wird auf die **kürzere** Seite. |
| **M-21** | **unverändert in der Zusage, verschärft in der Wirkung** | „Der Distraktor erzeugt **keine starke** Ursachenbehauptung. Er darf als **Alternative** erscheinen." — Bisher hielt der Satz nur, solange die Ränge quantengleich waren; ab hier trägt ihn die Strukturregel R3. |
| **M-22** | **unverändert** | Routingqualität, Parent-Duplikat und unbekanntes Routing bleiben Zeile für Zeile wie gebaut. Die offenen Fälle (Sonde auf dem Masterkanal, stummer Partner) sind ausdrücklich **NAK-213**. |
| **M-23** | **präzisiert** | Bisher: „gemessene Mindestzahl unabhängiger Evidenzfenster je beteiligter Quelle **innerhalb der Passage**". **Nachtrag NAK-212:** „Innerhalb" heißt **vollständig innerhalb** — dieselbe `transport_epoch` und `passage.von <= f.von && f.bis <= passage.bis`. Ein Fenster mit einem Sample Überlappung liegt nicht innerhalb. Und: die **Masteranomalie** wird ausschließlich aus Masterfenstern innerhalb der benannten Passage bestimmt; ohne Passage aus dem jüngsten Fenster wie bisher. `GATE_MINDEST_FENSTER` = 8 und `passagenmaterial` = min(Kandidat, Master) bleiben. |
| **M-25** | **unverändert** | Bytegleiches Ranking über hundert Läufe; `BOOTSTRAP_SAAT`, `RANGKOMPONENTEN`-Reduktionsreihenfolge, `RANG_QUANTUM` und `TIE_BREAK_KEY` bleiben Zeile für Zeile. *(Fassung Etappe 1: „Fassung gehoben" — der `metrics_version`-Schritt ist seit der Matrixnacharbeit 1 **bedingt**, siehe E8 und N-40.)* Der quantisierte Gesamtrang bekommt mit D1 eine **zweite** Aufgabe: er entscheidet weiterhin die Reihenfolge und zusätzlich mit, ob zwei Kandidaten überhaupt getrennt sind. |
| **M-26** | **präzisiert** | Bisher: „Zwei Kandidaten liegen praktisch gleich → **Beide werden gezeigt.** Der Tie-Break entscheidet nur die Anzeigereihenfolge, nicht die Auswahl." **Nachtrag NAK-212** *(Fassung Matrixnacharbeit 1; die Fassung Etappe 1 nannte nur die Zusammenhangskomponenten)*: „Praktisch gleich" heißt **beides zugleich** — gleicher quantisierter **Gesamtrang** oder gleicher **Zusammenhang** (Uplift, Koinzidenz, Wiederholbarkeit) machen zwei Kandidaten ungetrennt; getrennt sind sie nur, wenn **beide** Größen sich unterscheiden. Beide sind dann höchstens `mittel` und jeder Alternative des anderen — auch wenn Bandpassung oder Intent-Relevanz sie im Rang trennen und auch bei `rolle = fuehrt`. Der Gesamtrang steht in der Bedingung, **weil** verschiedene quantisierte Komponenten denselben Rang ergeben können (`rang()` teilt durch sechs und quantisiert erneut); ohne ihn entschiede der Tie-Break über die **Auswahl** statt über die Anzeige, und genau das verbietet der Satz. Der Führende wird gegen **jeden** anderen Überlebenden geprüft, nicht nur gegen den Zweitplatzierten. |
| **M-31** | **unverändert in der Zusage, neu gemessen** | „Die Schwelle wird **mit dem Prüfkorpus kalibriert, nie in der UI geraten**." Sie wird auf dem erweiterten Korpus neu gemessen und in §2.6 des Bauabschnitts ausgewiesen. |
| **M-64/M-65** | **präzisiert** | Der Korpus wächst um die Gegenbeispielsitzungen und um Sitzungen mit **je-Fenster-Reihen**; ohne sie ist keine Zusammenhangskomponente von null verschieden und die Sicherheit `hoch` strukturell unerreichbar. |
| **M-67** | **unverändert** | Die Wahrheitsmenge wächst um `gegenbeleg` und `routing_unbekannt` und bleibt geschlossen. |
| **M-69** | **erstmals gemessen** | „Gegenbeispiele erzwingen nachweislich Enthaltung. ‚Nachweislich' heißt: der Lauf zeigt, dass auf **jedem** Gegenbeispiel die Aussage `unsicher` ist." — Der Prüfer bekommt dafür einen **eigenen Abschnitt** mit dem Riegel `falsche_starke` **und** einer Nichtleere-Bedingung (N-35). Die Zeile war in `docs/beweise/roh/G5-eigener-lauf-bf92891.md` als halb gemessen geführt (E-L7). |
| **M-87** | **unverändert** | Die geschlossene Menge der Ausschlussgründe wird in diesem Ticket **nicht** angefasst. Der Deckelfall ist NAK-213 R1 und steht als gedruckte Lücke im Korpus. |

---

### 2.8 Was diese Matrix ausdrücklich **nicht** spezifiziert — und deshalb nicht gebaut wird

1. **Kein neuer Ausschlussgrund.** Weder „kein Zusammenhang" noch „Gegenbeleg" noch „am Deckel abgeschnitten" wird zu einem `Ausschlussgrund`. Die Menge ist geschlossen und gehört NAK-213.
2. **Keine Änderung an `KANDIDATEN_DECKEL`, `truncate`, der Master-Wahl oder `ist_parent`/`ids`.** Alle vier sind NAK-213.
3. **Keine Änderung an `hypothese_verdrahtung.rs`.** R5 verlangt sie nicht (E5).
4. **Keine Änderung an Proposal, `prepost_paar`, `befehl.rs` oder der Gen-Seite.** NAK-214.
5. **Keine siebte Rangkomponente und keine Gewichtung.** §36.2 nennt sechs Komponenten und keine Gewichte; eine Gewichtung wäre eine unkalibrierte Zahl im Rechenweg.
6. **Keine numerische Schwelle für „Zusammenhang".** R1 ist eine Vorzeichenfrage, keine Größenfrage (E3).
7. **Keine Änderung an `bootstrap_p`, `GATE_ZEITUEBERDECKUNG`, `GATE_MINDEST_FENSTER`, `RANG_QUANTUM`, `BOOTSTRAP_SAAT`, `TIE_BREAK_KEY`.**
8. **Keine Anzeige.** Ob und wie Gen Zusammenhang, Gegenbeleg oder Ausschlüsse zeigt, ist S31b.
9. **Keine weitere Änderung an `pruefe_p5_korpus.py`** außer den in N-35, N-37 und N-46 benannten: der Gegenbeispielabschnitt, die Lückenbehandlung und die NR-14-Existenzprüfung. Die Kennzahlen, die Schwellensuche und die vier Riegel aus M-64 bleiben Zeile für Zeile.
10. **Keine Änderung an `bootstrap_p`s Aufrufern** `experiment.rs`:410 und `sonde013_verdrahtung.rs`:2470/2475 (N-12) und keine an der Anzeigereihenfolge (M-25, M-26).

---

## 3. Prüfliste `tools/dirigent/pruefliste.md` — wo jede Zeile in dieser Etappe steht

Etappe 1 liefert keinen Code; die Zeilen sind hier als **Bauauftrag an Etappe 2** verankert, damit der Bau sie nicht neu erfinden muss.

| Abschnitt | Bezug in dieser Matrix |
|---|---|
| **A** Rückstau, Prioritätsklassen | nicht berührt — reiner Rechen- und Werkzeugdiff, keine Queue |
| **B** Lebenszyklus | nicht berührt |
| **C** Verträge und Längen | N-34 (geschlossene Wahrheitsmenge, Negativfall), N-40 und N-45 (`metrics_version` an **vier** Codestellen plus Register, beide Ausgänge benannt), Zahlenränder in N-04, N-05, N-18, N-25, N-26, N-27, **N-41** (gleicher Rang bei verschiedenen Komponenten) und **N-44** (leeres Intervall `[5,5)`, `sample_count` `minimum: 0`) |
| **D** Bau- und Prüfriegel | N-31 (A28 bytegleich), N-35/N-36 (A29/A30 fail-closed mit Gegenteil), N-38 (A31 als eigene Zeile) |
| **E** Behauptung ≤ Messung | Jede Zeile in §2.1 bis §2.5 nennt ihren Test **und** ihren Rotbeweis; N-19, N-21 und N-45 sind ausdrücklich als **Regressionswache** beziehungsweise **Änderungssatz-Zusage** benannt, nicht als Beleg. Die Zahlen in §2.6 sind aus `p5-korpus-ergebnis.json` gemessen; der Rang 0,35 in N-41, das leere Intervall in N-44 und die **vier** Codestellen in E8 sind nachgerechnet beziehungsweise mit `grep` gezählt — insbesondere die Aussage „**null** Fixtures tragen `20260904`", die eine Vermutung der Matrixprüfung 1 korrigiert. **Seit der Matrixnacharbeit 2:** N-17 behauptet den Rangabstand nicht mehr, sondern **misst** ihn (`assert` vor der Erwartung `hoch`) — die Zeile sagt damit nicht mehr, als ihr Test prüft; die Sichtbarkeitsgrenze des Gesamtrangs (lageabhängig 1 bis 5 Quanten Komponentenabstand) ist in E6 gemessen und nicht als Konstante gesetzt |
| **F** Änderungssatz | Erzeuger ↔ Prüfer ↔ Fixture bytegleich in **einem** Commit (N-31/N-33/N-34); **alle vier** `metrics_version`-Stellen und das Register in **einem** Commit (N-40, N-45); Broker-Ausgabe ↔ Prüfer-Leser in **einem** Commit (N-46, E9 — `AGENTS.md`:51–52); Riegel ↔ Rotbeweis je Zeile |

---

## 3a. Bauetappe 1 — der Brokerkern (E1 bis E6, R1 bis R5)

**Stand:** `740de80` + dieser Commit. Kein Korpus, kein Erzeuger, kein Prüfer — das ist Bauetappe 2.

### 3a.1 Was gebaut wurde

| Entscheid | Ort | Kern |
|---|---|---|
| **E1** | `hypothese.rs` `Zusammenhangsbeleg`, `zusammenhang()`, `rang_und_beleg()` | Der Zusammenhang wird **einmal** gerechnet und **mit Vorzeichen** geführt (`Option<f64>` für Uplift und Koinzidenz). Die sechs Rangkomponenten bleiben in Zahl, Name, Wertebereich [0, 1] und Reduktionsreihenfolge unverändert — ein Gegenbeleg wird für den **Rang** auf 0 geklemmt und wirkt allein über die **Klasse**. |
| **E2** | `experiment.rs::bootstrap_p_positiv` | Neue Funktion neben `bootstrap_p`; `wiederholbarkeit` ruft ab hier die einseitige. `bootstrap_p` bleibt **Zeile für Zeile** unverändert, seine zwei fremden Aufrufer (`experiment.rs`:410, `sonde013_verdrahtung.rs`:2470/2475) und M-45 sind unberührt. |
| **E3** | `hypothese.rs::baue_befund` | Vier neue Terme in der Klassenwahl, alle Prädikate über eine benannte Struktur: `bandpassung_ist_null`, `!positiv_belegt()`, `gegenbeleg()`, `!getrennt`. Keine Zahl, keine Schwelle — M-15 und M-31 unberührt. |
| **E4** | `hypothese.rs::ueberlappt`, `paarueberdeckung`; `gate()` Schritt 3 | Paarweise Überlappung statt Spannenüberdeckung, normiert auf die kürzere Seite. Das Partnerprädikat steht an **einer** Stelle und verlangt `max(von) < min(bis)`; `gemeinsame_reihen` liest dieselbe Definition. |
| **E5** | `hypothese.rs::fenster_ganz_in_passage`, `masteranomalie(master, passage)` | Passagenmaterial nur vollständig innenliegend; die Masteranomalie liest nur solche Fenster, mit Rückfall auf das jüngste Fenster bei **ungültiger** Beobachtung. `hypothese_verdrahtung.rs` bleibt unberührt, wie E5 es vorsieht. |
| **E6** | `hypothese.rs::getrennt`, `rang_quantisiert`, `zusammenhang_verschieden` | Getrennt heißt verschiedener Gesamtrang **und** verschiedener Zusammenhang, geprüft gegen **jeden** Überlebenden. `getrennt` steht als **benannte** Funktion, damit beide Bedingungen einzeln zurücknehmbar und einzeln messbar sind. |
| **R3** | `hypothese.rs::hypothesen` | Alternativen sind **wechselseitig**: jeder Befund nennt alle anderen. Bis hierher trug nur der führende welche. |

### 3a.2 Gemessene Beine

Zahlen aus dem Lauf auf diesem Stand, nicht abgeschrieben.

| Bein | vorher | nachher | Bühne angepasst? |
|---|---|---|---|
| `cargo test --lib` (Modulebene) | 228 | **233** passed, 0 failed | 5 neue Modultests |
| `--test sonde014_hypothese` | 19 | **24** passed, 0 failed | ja, 5 neue Fälle |
| `--test sonde014_befund` | 7 | **7** passed, 0 failed | ja, 3 Tests |
| `--test sonde014_proposal` | 18 | **18** passed, 0 failed | ja, die gemeinsame Bühne |
| `--test sonde014_nacharbeit1` | 8 | **8** passed, 0 failed | ja, `belege()` |
| `--test sonde014_nacharbeit2` | 4 | **4** passed, 0 failed | ja, `belege()` |
| `--test sonde014_nacharbeit3` | 3 | **3** passed, 0 failed | nein |
| `--test sonde014_maskierung` | 5 | **5** passed, 0 failed | ja, ein Test |
| `--test sonde014_verdrahtung` | 17 | **17** passed, 0 failed | nein |
| `--test sonde013_verdrahtung` | 78 | **78** passed, 0 failed | nein |
| `--test sonde013_experiment` | 19 | **19** passed, 0 failed | nein |
| `--test sonde013_taint` | 14 | **14** passed, 0 failed | nein |
| `--test sonde014_p5_korpus` | 1 | **1** passed, 0 failed | nein (Bauetappe 2) |

**Sieben Bestandstests in fünf Dateien fielen zuerst** — alle aus demselben Grund, und das ist der stärkste Beleg dieser Etappe: **jeder** Test im Bestand, der `hoch` misst, kam bisher mit „genug Fenster + bekanntes Routing" aus. Ihre Bühnen bauen Evidenz mit **konstanter** Bandanhebung; damit gibt es kein Fenster ohne die Quelle, der bedingte Uplift ist nach M-19 nicht messbar, die Onsetreihe ist konstant — kein Zusammenhangsbeleg, nach R1 korrekt `mittel`. Die Sicherheitsklasse `hoch` war im gesamten Bestand erreichbar, ohne dass **eine einzige Zeile Evidenz** einen Zusammenhang zwischen Quelle und Master belegt hätte. Angepasst wurde je die **Bühne**, nie die Zusage.

### 3a.3 Rotbeweise — zehn, jeder fällt

Je Regel eine Rücknahme, Rohausgabe unter `docs/beweise/roh/NAK-212-rot-<zeile>.txt`. Der Baum ist nach jedem Lauf unverändert.

| Datei | Rücknahme | Ergebnis |
|---|---|---|
| `NAK-212-rot-N-01-R1a.txt` | die Klasse liest die Bandpassung nicht mehr | ROT |
| `NAK-212-rot-N-03-R1b.txt` | die Klasse verlangt keinen Zusammenhangsbeleg | ROT |
| `NAK-212-rot-N-08-R2.txt` | ein Gegenbeleg schließt `hoch` nicht mehr aus | ROT |
| `NAK-212-rot-N-10-R2-bootstrap.txt` | der Bootstrap misst wieder zweiseitig | ROT |
| `NAK-212-rot-N-14-R3.txt` | getrennt heißt wieder nur „anderer Rang" | ROT |
| `NAK-212-rot-N-41-D1.txt` | der Gesamtrang geht nicht mehr in die Trennung ein | ROT |
| `NAK-212-rot-N-18-R4.txt` | das Gate misst wieder die Überdeckung der Spannen | ROT |
| `NAK-212-rot-N-44-R4-leer.txt` | das Partnerprädikat lässt leere Fenster durch | ROT |
| `NAK-212-rot-N-26-R5.txt` | jede Berührung ab einem Sample zählt wieder | ROT |
| `NAK-212-rot-N-28-R5-anomalie.txt` | die Masteranomalie liest wieder ohne Passagenbezug | ROT |

**Drei davon waren im ersten Anlauf GRÜN — und das war der wertvollste Teil dieser Etappe.** Eine Probe, die ohne die Regel grün bleibt, misst die Regel nicht:

1. **R1 (a)** — der Testfall fiel **doppelt**: ohne gültige Bänder ist auch der Uplift unmessbar, also griff R1 (b) mit. Der isolierte Fall braucht einen Kandidaten mit **Koinzidenz, aber ohne Bandenergie** — die Onsets leben in `/ereignisse/liste`, nicht in den Bändern. Dabei fielen zwei weitere eigene Fehler auf: ein **leeres** `gueltig_bitmap` lässt den Beleg schon am Vertrag fallen (die Quelle wird gar nicht Kandidat, und der Test maß eine **Enthaltung** statt eines Befunds), und das Befundintervall ist die **Bandgruppe** `[97, 101)`, nicht das angehobene Viererfenster `98..102` — wer nur dieses ausnimmt, lässt Band 97 gültig und misst `bandpassung` = 0,0046 statt 0. Beide Fälle tragen jetzt einen Riegel auf `candidate_source`, damit eine Enthaltung sie nicht still erfüllt.
2. **R2** — der antikorrelierte Kandidat ist auch *nicht positiv belegt*, also fing ihn R1 (b). Der isolierte Fall braucht **positiven Uplift und negative Koinzidenz zugleich**.
3. **D1** — die Rücknahme trifft `hypothesen()`, der Modultest prüfte aber nur die Bausteine einzeln. Konsequenz: die Trennungsregel steht jetzt als **eigene benannte Funktion** `getrennt()`, sonst ist sie nicht isoliert messbar.

### 3a.4 Abweichungen von der Matrix, mit Begründung

1. **Vier Testdateien sind berührt, die die Ticketpfade nicht nennen** — `sonde014_befund.rs`, `sonde014_proposal.rs`, `sonde014_nacharbeit1.rs`, `sonde014_nacharbeit2.rs`, dazu ein Test in `sonde014_maskierung.rs`. In allen Fällen ist die **Bühne** angepasst, nie die Zusage: sie brauchen einen handelbaren Befund und bauten ihn aus konstanter Bandanhebung, die nach M-19 keinen messbaren Uplift trägt. Das ist der Änderungssatz aus Prüfliste §F; keine der Dateien steht in einer Nicht-anfassen-Liste, und `proposal.rs`/`proposal_verdrahtung.rs` — der Produktcode, den die Grenze für NAK-214 reserviert — sind **unberührt** (`git diff --stat` weist sie nicht aus). Zwei Formen kommen vor: „laut gegen still" (`sonde014_hypothese.rs`, `sonde014_befund.rs`, `sonde014_proposal.rs`, mit garantiert lautem letztem Fenster) und „laut gegen leise" mit 9,0 gegen 4,0 dB (die Nacharbeitsbeine) — letztere ist unabhängig von Fensterzahl und Parität, weil `masteranomalie` das Befundband dann in **jedem** Fenster findet.
2. **`broker/src/coordinator/mod.rs` ist berührt**: der Re-Export um `rang_quantisiert` und `Zusammenhangsbeleg` erweitert, damit das Verdrahtungsbein den Rangabstand **messen** kann (D7). Eine Zeile, kein Verhalten.
3. **Ein Kadenztest wurde in der Zeitlage präzisiert:** `sonde014_maskierung.rs::rueckstau_setzt_das_kennzeichen_herabgesetzt` schob ein Sondenfenster acht Fensterlängen hinter das jüngste Masterfenster. Seit das Alignment paarweise misst, hat dieses Fenster keinen Partner — 12 von 13 ergeben 0,923 < `GATE_ZEITUEBERDECKUNG`, der Kandidat schiede mit `alignment_falsch` aus, und der Fall maß die Kadenz nicht mehr. Beide jüngsten Fenster liegen jetzt **zeitgleich**; die Lücke in der Reihe, um die es dem Test geht, ist unverändert.
4. **Ein Modultest wurde in der Konstruktion präzisiert**, nicht in der Zusage: `passagengates_trennen_verschoben_anders_und_zu_kurz` maß „andere Passage" über eine Quelle auf fremder Transportepoche. Seit das Alignment paarweise misst (die Epoche steht **im** Partnerprädikat, N-22), fällt so ein Kandidat schon an Schritt 3 mit `alignment_falsch`. Der Fall trägt jetzt Master **und** Kandidat auf der fremden Epoche und isoliert damit wirklich das Passagengate; die alte Lage steht als eigene Gegenprobe daneben. Beide Gründe liegen in der geschlossenen Menge (M-87), M-23 hält in beiden Formen.

### 3a.5 Was nach dieser Etappe absichtlich noch rot ist

**A29 (`pruefe_p5_korpus.py`) meldet zwei Befunde** — beide sind in §2 spezifizierte Zwischenstände, die Bauetappe 2 schließt:

1. `korrelierter_distraktor: die Alternative … gehoert der Quelle …02, nicht der deklarierten Distraktorquelle (NR-14, NAK-190)` — das ist **D6 am Produktpfad**, exakt wie E9/N-46 es vorhersagt: sobald R3 wechselseitige Alternativen erzeugt, zeigt auch der Distraktorbefund auf die wahre Quelle, und `_faelle_bilden` verwirft den korrekten Rückverweis.
2. `quelle_resonanz: recall 0.000 < 1` — das ist **E7**: `wahrer_kandidat` trägt eine konstante Anhebung, hat damit kein Fenster ohne die Quelle und erreicht `hoch` nach R1 zu Recht nicht mehr. Die je-Fenster-Reihen des Erzeugers stellen den Recall wieder her.

Der Korpuslauf bestätigt damit **beide** Vorhersagen der Matrix am laufenden Produktpfad, bevor eine Zeile am Korpus geändert wurde.

---

## 4. Nebenbefunde außerhalb der Ticketgrenze

Nummeriert, nicht ins Register — das zieht der Dirigent nach.

1. **`docs/beweise/SONDE-014.md` §3 nennt in der Testspalte drei Ziele, die es nicht gibt** (`EqCopSonde014HypotheseTest`, `…ProposalTest`, `…MaskierungTest`). Gemessen wird in `broker/tests/sonde014_hypothese.rs`, `sonde014_proposal.rs`, `sonde014_maskierung.rs`. Bereits in `docs/beweise/G5.md` §3 als Manifestprosa protokolliert; dieses Ticket **ändert die Zeilen nicht**, weil es damit fremde Matrixspalten anfasste. Die betroffenen Zeilen der Präzisierungen in §2.7 nennen die **echten** Dateien.
2. **`uplift()` normiert auf `spannweite(mit ∪ ohne)`.** Steht der Master in beiden Mengen still, ist die Spanne 0 und der Uplift 0 — heute nicht von „kein Vergleichsfenster" unterscheidbar. Diese Matrix führt den Fall als vierte Lage von `None` **nicht** ein: R1 nennt drei Lagen, und eine vierte wäre eine Erweiterung über den Auftrag hinaus. Nach dem Bau lohnt eine eigene Zeile.
3. **`intent_relevanz` geht weiter in den Gesamtrang und damit in die Reihenfolge ein.** Nach R3 trennt sie die Klasse nicht mehr, aber der Distraktor mit `rolle = fuehrt` steht weiterhin **oben** in der Liste (N-15). Ob eine Anzeige das als Ursachenbehauptung liest, ist eine Frage an S31b, nicht an den Datenweg.
4. **Der Korpus fährt jede Sitzung mit genau einem Master und ohne PRE/POST-Paar.** `claim_class = wirkungsbeleg` ist damit im Korpus ungemessen; die zugehörige Naht ist E-D6 → NAK-214 R2.

---

## 5. Verlauf

**Matrixprüfung 3 — PASS (07.09.2026, Stand `740de80`).** Codex `gpt-6-astra`, Effort max, read-only, Thread `01a07d06-735c-7493-8681-02a0d2fc2ecd`; HEAD vor und nach dem Lauf `740de80`, Produktdiff gegen `4df8855` leer; Urteil `docs/beweise/roh/NAK-212-matrixpruefung-3-740de80.txt`. Geprüft wurden N-17 und N-47 gegen `hypothese.rs`:535–540 (Uplift 0,6 gegen `None` → 0,366667 gegen 0,266667, also 100 000 Quanten Abstand; Uplift 0,000001 gegen `None` → zweimal 0,266667), dazu E6, N-41, N-42, R1–R6, die 27 erwarteten Ausgaben, die SONDE-014-Referenzzeilen einschließlich M-15, M-25 und M-26 sowie die Invarianten aus `CLAUDE.md` — **kein bestätigter Bruch**.

**Damit ist Etappe 1 abgenommen.** §2 dieses Manifests — E1 bis E9, N-01 bis N-47, §2.5.1 (27 Korpussitzungen), §2.6 (die 27 Gegenbeispiele), §2.7 (SONDE-014-Zeilen) und §2.8 (was nicht gebaut wird) — ist ab hier die **Spezifikation**. Was dort nicht steht, wird nicht gebaut.

**Die drei Prüfrunden der Spezifikation:**

| Runde | Thread | Urteil | Folge |
|---|---|---|---|
| Matrixprüfung 1 (`2e05a95`) | `01a07cd6-8e07-72f2-9d16-f9e1a688c01a` | **NEEDS_WORK**, sechs Befunde D1–D6 | Matrixnacharbeit 1: E4, E5, E6, E8 neu gefasst, E9 neu, N-41 bis N-46 |
| Matrixprüfung 2 (`66f578e`) | `01a07cf7-06a2-7331-8e13-6c033d726a59` | **NEEDS_WORK**, ein Befund D7 | Matrixnacharbeit 2: E6 ergänzt, N-17 neu gefasst, N-47 |
| Matrixprüfung 3 (`740de80`) | `01a07d06-735c-7493-8681-02a0d2fc2ecd` | **PASS** | Etappe 2 beginnt |

---


**Matrixnacharbeit 2 (07.09.2026, Basis `66f578e`).** Die **Matrixprüfung 2** (Codex `gpt-6-astra`, Effort max, read-only, Thread `01a07cf7-06a2-7331-8e13-6c033d726a59`, HEAD vor und nach dem Lauf `66f578e`, Produktdiff gegen `4df8855` leer, Urteil `docs/beweise/roh/NAK-212-matrixpruefung-2-66f578e.txt`) bestätigt **D1 bis D6 als geschlossen** — je mit dem messenden Test und dem Rotbeweis, den die Matrixnacharbeit 1 benannt hatte — und meldet **NEEDS_WORK** mit genau einem neuen Befund.

| Befund | Kern | Regel | Zeilen |
|---|---|---|---|
| **D7** | Der positive Kontrollfall **N-17** setzte voraus, dass ein positiver Uplift den Gesamtrang bewegt. Das ist nicht allgemein wahr: `rang()` mittelt sechs Komponenten und quantisiert **erneut** (`hypothese.rs`:535–545), sechstelt also jede Differenz. `uplift = Some(0,000001)` gegen `None` ergibt beide Male **0,266667** — E6 stuft die Kandidaten korrekt als ungetrennt ein, N-17 verlangte `hoch`. Zwei Zusagen, die sich widersprechen; umgesetzt hätte der **Tie-Break** erneut über die Auswahl entschieden (M-26, `docs/beweise/SONDE-014.md`:745) | **E6/N-17**: jede Zeile, die einen starken Befund zusagt, nennt den quantisierten Rangabstand als ausdrückliche **Vorbedingung** und **misst** ihn im Test (`assert` vor der Erwartung `hoch`); die Fixture setzt den Uplift auf Größenordnung 0,6 statt nahe `RANG_QUANTUM` | E6 ergänzt, N-17 neu gefasst, **N-47** |

**Der eigene Fund dieser Runde:** Wie viele Quanten Komponentenabstand ein Quantum Rangabstand ergeben, ist **lageabhängig** — nachgemessen zwischen **1 und 5** (bei Bandpassung 0,1 / Intent-Relevanz 0,5 / Routingqualität 1,0 erst ab 5, bei Bandpassung 0,2 schon ab 1; die Obergrenze ist 6, weil die Mittelung sechstelt). Eine Vorbedingung „mindestens N Quanten Komponentenabstand" wäre deshalb selbst eine geratene Zahl — genau das, was M-31 verbietet. Die Zusage steht darum als **gemessene** Bedingung im Test, nicht als Konstante in der Konstruktion. Betroffen ist außer N-17 nur §2.6 Zeile 26 (`eigen5`, rund 166 000 Quanten Abstand); die übrigen starken Zeilen tragen **einen einzigen** Kandidaten, und für den greift `getrennt` gar nicht.

Stand danach: **9 Entscheide, 47 Zeilen**, 27 Korpussitzungen unverändert. Kein Produktcode, kein Test, keine Fixture.

---


**Matrixnacharbeit 1 (07.09.2026, Basis `2e05a95`).** Die **Matrixprüfung 1** (Codex `gpt-6-astra`, Effort max, read-only, Thread `01a07cd6-8e07-72f2-9d16-f9e1a688c01a`, HEAD vor und nach dem Lauf `2e05a95`, Urteil `docs/beweise/roh/NAK-212-matrixpruefung-1-2e05a95.txt`) las §2 mit R1–R6, E1–E8, N-01–N-40 und allen 27 Gegenbeispieleingaben gegen Quellen und Verträge und urteilte **NEEDS_WORK** mit **sechs Defekten**. Jeder ist in dieser Runde an der Quelle nachgerechnet und geschlossen:

| Befund | Kern | Regel | Zeilen |
|---|---|---|---|
| **D1** | Verschiedene quantisierte Komponenten können denselben quantisierten Gesamtrang ergeben (Koinzidenz 0,500000 gegen 0,500001 → beide Rang 0,35, nachgerechnet). Die Fassung Etappe 1 erklärte sie für getrennt, und der **Tie-Break** wählte den einzigen starken Befund — ein Bruch an M-26 | **E6**: getrennt heißt Gesamtrang **und** Zusammenhang verschieden | N-41, N-13 bis N-17 präzisiert |
| **D2** | Der Vergleich lief nur gegen den Zweitplatzierten. Ein Kandidat B kann durch Intent-Relevanz zwischen zwei messtechnisch identische A und C rutschen; A wurde `hoch`, obwohl C ununterscheidbar ist | **E6**: gegen **jeden** Überlebenden im Deckel | N-42 |
| **D3** | Bei einer Passage ohne innenliegendes Masterfenster gab `masteranomalie` `None`, und `hypothesen()` endete **vor** den Gates. Das trifft bereits §2.6 Nr. 17 (alle zwölf Masterfenster vor der Passage `[12,24)`): die dort zugesagte Enthaltung mit `passage_unvergleichbar` wäre nie entstanden — Bruch an M-27 und Regression gegen den heutigen Stand | **E5**: Rückfall auf das jüngste Fenster mit `beobachtung.gueltig = false`; `fenster_in_passage(master)` = 0 lässt jeden Kandidaten fallen und garantiert die Enthaltung strukturell | N-29 neu gefasst, N-43 |
| **D4** | Das Partnerprädikat der Fassung Etappe 1 hält für `f = [5,5)` gegen `g = [0,10)` beidseitig, obwohl kein Sample gemeinsam ist. `sample_count` hat vertraglich `minimum: 0`; die **heutige** Rechnung verwirft den Fall an seiner Länge null — die Paarüberdeckung hätte ihn neu durchgelassen | **E4**: ein Partnerprädikat `ueberlappt()` an **einer** Stelle, mit `max(von) < min(bis)`; leere Fenster zählen weder im Zähler noch im Nenner | N-44, N-22 präzisiert |
| **D5** | Der Schritt war unbedingt und nannte zwei Stellen. A5 prüft die Fassung an **vier** Codestellen: `vergleichbarkeit.rs`:149, `prepost.rs`:206, `FeatureEngine.h`:783 und dem Register — die letzten beiden fehlten und hätten den Vertragslauf zwingend rot gemacht | **E8**: der Schritt ist **bedingt** (E-12-Regel: nur wenn die Schwelle sich bewegt) und **gekoppelt** (alle vier Stellen in einem Commit); beide Ausgänge und der entscheidende Test sind benannt | N-40 neu gefasst, N-45 |
| **D6** | `_faelle_bilden` verlangt, dass **jede** Alternativ-ID der Distraktorquelle gehört. Sobald R3 wechselseitige Alternativen erzeugt, zeigt auch der Distraktorbefund auf die wahre Quelle — `g5_distraktor_mehr_fenster` wäre bei zwei völlig richtigen `mittel`-Befunden **rot** | **E9** (neu): die NR-14-Prüfung wird zur Existenzaussage („mindestens eine Alternative löst auf die Distraktorquelle auf"), wie M-65 sie wörtlich verlangt; vier Gegenproben in A30 | N-46 |

**Zwei eigene Funde dieser Runde, beide gegen die Prüfung präzisiert:**

1. **Eine fünfte Kopplungsstelle**, die der Prüfer nicht nennt: `broker/src/coordinator/prepost.rs`:206 trägt eine **zweite** `METRICS_VERSION`, und `pruefe_v3_vertrag.py`:1914–1926 prüft sie ausdrücklich gegen das Register — „eine zweite Datei mit eigener Fassung koennte sonst unbemerkt auf einer alten Kalibrierung stehenbleiben".
2. **Der Umfang ist an anderer Stelle kleiner als vermutet.** Die Prüfung verlangt den Schritt „einschließlich betroffener Fixtures". `grep -rl "20260904" eq-copilot/fixtures/` liefert **null** Treffer; die 84 Fixtures mit einem Feld `metrics_version` tragen dort die Konfidenz-Metrikversion `1` des Evidenzframes — eine andere Größe unter demselben Feldnamen.

Stand danach: **9 Entscheide, 46 Zeilen**, 27 Korpussitzungen unverändert. Kein Produktcode, kein Test, keine Fixture.

---


**Etappe 1 (07.09.2026, Basis `cbc816a`).** Quellen gelesen (`hypothese.rs` vollständig, `experiment.rs::bootstrap_p`, `hypothese_verdrahtung.rs::aufnahmen_sammeln`/`fenster_aus_historie`, `evidenz.rs`:298–314, `vergleichbarkeit.rs` Konstanten, `erzeuge_p5_korpus.py`, `pruefe_p5_korpus.py`, `sonde014_p5_korpus.rs`, `metriken-v1.json`, `pruefe_v3_vertrag.py`:1930–1960, `tools/beweise.ps1` A4/A28/A29/A30), alle fünf Rohbelege ausgewertet, die Rangkomponenten der 18 gefahrenen Korpussitzungen aus `p5-korpus-ergebnis.json` **nachgerechnet** (Ergebnis: Koinzidenz, Uplift und Wiederholbarkeit sind im gesamten Korpus exakt null — die Grundlage von Entscheid E7). Matrix mit **8 Entscheiden, 40 Zeilen**, einer vollständigen Sitzungsliste des erweiterten Korpus (**27 Sitzungen**), der Zuordnung aller 27 Gegenbeispiele und 13 präzisierten oder bestätigten Matrixzeilen aus SONDE-014 §3. Kein Produktcode, kein Test, keine Fixture.

**Vier Funde des Selbstaudits, alle vor dem Commit eingearbeitet:**

1. **Der Riegel hätte den Kanon blockiert.** R6 verlangt, dass die Fälle für NAK-213 „als Korpussitzung mit gedruckter Lücke geführt werden, **nicht als grüner Fall**". Liefern sie weiterhin `hoch`, zählt `pruefe_p5_korpus.py` sie als `falsche_starke` und der Lauf endet mit Exit 2 — der Kanon wäre nie grün zu bekommen, und der Bau hätte die Fälle entschärfen oder weglassen müssen. N-37 spezifiziert deshalb zwei `wirkung`-Werte in `nicht_gemessen`: `ausgenommen` (die Sitzung verlässt Riegel und Kennzahlen) und `benannt` (sie zählt normal mit, eine andere Zusage bleibt offen).
2. **`sicherheit_hoechstens` ist keine Obergrenze.** `falsche_schwache` bestraft auch die zu vorsichtige Aussage. `g5_unbeteiligte_quelle` trägt im G5-Patch `unklar`, kann aber nur `mittel` erreichen, weil ihr Kandidat alle fünf Gates passiert und `unklar` im Datenweg ausschließlich aus `enthaltung()` stammt (`hypothese.rs`:1313). Ungeprüft übernommen wäre der Korpuslauf rot gewesen und die Ursache hätte wie ein Produktfehler ausgesehen (§2.5.1 Nr. 10).
3. **R5 erzeugt eine neue Lücke.** „Vollständig innerhalb" kann dazu führen, dass **kein** Masterfenster in der Passage liegt — dann gibt `masteranomalie` `None` und der Befund verschwindet ganz. Das ist eine **neue** Lücke, keine bestehende; sie ist als N-29 gedruckt und NAK-213 R4 zugeordnet, samt der Begründung, warum ein zweistufiger Rückfall („sonst überlappend") sie nicht schließen darf. **Nachtrag Matrixnacharbeit 1:** dieser Fund war zur Hälfte falsch. Die Lücke ist echt, aber sie darf nicht gedruckt werden — sie trifft bereits das zugesagte Gegenbeispiel §2.6 Nr. 17 und hätte dort die Enthaltung verhindert (D3). Der Rückfall auf das jüngste Fenster **mit ungültiger Beobachtung** schließt sie, ohne eine zweite Definition von „in der Passage" einzuführen; N-29 ist neu gefasst.
4. **Der Rotbeweis für E-D3 musste neu gerechnet werden.** Ein bloßer Halbfensterversatz genügt nicht: bei einer Passage über die volle Fensterfolge liegen 11 von 12 Fenstern weiterhin vollständig innerhalb, und der Fall hält. Die Sitzung `passage_teilweise_innerhalb` trägt deshalb eine Passage von **8 Fensterlängen mit halbem Versatz** — heute 9 berührende Fenster (hält), nach R5 7 innenliegende (fällt), während Gate 3 und Gate 4a in beiden Fällen halten (§2.5.1 Nr. 27).
