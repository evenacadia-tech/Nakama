NAK-223 (S25b) — Nacharbeit 2 nach der Wiederprüfung 1 (NEEDS_WORK, ein Restbefund: Regression der Namenswahl). Auftrag des Dirigenten, 2026-09-09.

DU BIST der frische Opus-Bauer (max) für diese Nacharbeitsrunde. Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start (Worktree sauber). Aufsicht NORMAL. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`. Kein Produktcode, keine Neben-Refactors. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder `rm -rf`; selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor dem Commit abhaken und im Manifest §3 nachziehen. Der Rotbeweis fällt je Zeile an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

TICKETGRENZE dieser Runde: nur `tools/plan/gesundheit.py` (die Namenswahl `ELEMENT`/`ZWISCHENWORT` und die zugehörigen Selbsttestfälle), der Rotbeweis `docs/beweise/roh/NAK-223-rot-deadcode.txt` (um einen Abschnitt erweitert, nichts Altes umgeschrieben) und das Manifest `docs/beweise/NAK-223.md` (lebender Kopf: Phase-Zeile und Änderungssatz „Nacharbeit 2“; §2.5 dort, wo die Namenswahl beschrieben ist; §5 Verlauf: neuer Abschnitt „5.7 Wiederprüfung 1 — NEEDS_WORK (09.09.2026, Stand `afc2d699`)“ mit Thread `01a08551-373e-7792-bd0e-9983ad9e9ebc`, Urteil wörtlich aus `docs/beweise/roh/NAK-223-wiederpruefung-1-9cef44b.txt`, dann „5.8 Nacharbeit 2“). Nichts sonst: kein Runner, kein Skill, kein Playbook, kein Register, kein Plan, kein Produktcode, keine Änderung an Clippy-Pfad oder Schwellen.

DER BEFUND, wörtlich aus dem Urteil der Wiederprüfung 1 (Codex `gpt-6-astra`, max, read-only, Thread 01a08551, Stand afc2d699; Defekte 1 und 2 der Erstprüfung sind dort als geschlossen bestätigt):
„Überspringe gültige Elementnamen nicht als Qualifizierer — tools/plan/gesundheit.py:572-575. DEFEKT: Beim gültigen Rust-Typnamen `union` überspringt die Wiederholung den tatsächlichen Namen und erfasst stattdessen `where`. Reproduktion: Für `s = '#[allow(dead_code)]\nstruct union where u8: Copy;\nfn x<T>() where T: Copy {}\n'` liefert `finde_dead_code([('t.rs', None)], {'t.rs': s})` am Zielstand `[]`, vorher einen Treffer `union`: Die fremde `where`-Klausel wird als Verwendung gewertet. Bei sonst gehaltenen Grenzen kippt der virtuelle Gesamtlauf dadurch von Exit 4 auf 0. Das verletzt den Namensvertrag aus R-D1 und das Gate-Maß für aufruferlose Helfer; übersprungen werden dürfen nur tatsächliche Qualifizierer.“
Der Dirigent hat den Fall an der Quelle reproduziert (`struct union where` → `[]`); `fn default() -> u8` liefert heute korrekt `default`, `union Foo` korrekt `Foo` — die Regression trifft genau die Lage „ein Wort, das zugleich Schlüsselwort und gültiger Bezeichner ist, steht an Namensposition und wird von einem weiteren Wort gefolgt“.

REGEL R-D1b (Präzisierung von R-D1, Technikentscheid des Dirigenten): Qualifizierer werden ausschließlich VOR dem Elementschlüsselwort übersprungen, und nur dann, wenn auf sie ein Elementschlüsselwort folgt — die Grammatik der Namenswahl ist `[Qualifizierer]* Schlüsselwort [mut]? Name` mit Qualifizierer ∈ {`pub`, `pub(...)`, `const`, `unsafe`, `async`, `default`, `extern "…"`}, Schlüsselwort ∈ {`fn`, `struct`, `enum`, `trait`, `type`, `const`, `static`, `union`, `mod`}, `mut` nur unmittelbar nach `static`. Das erste Wort NACH dem Schlüsselwort (bzw. nach `static mut`) ist der Name, auch wenn es `union`, `default`, `where` oder ein anderes Wort ist, das anderswo Schlüsselwort wäre. `const` ist damit Qualifizierer, wenn ein Schlüsselwort folgt (`const fn name`, `const extern "C" fn name`), sonst selbst das Schlüsselwort (`const NAME: T`). Messung: die Reproduktion des Prüfers liefert genau einen Treffer `union`; Selbsttestfälle im Speicher, je ohne und mit Verwendung, die Erwartung mit ihrem Gegenteil: `struct union where u8: Copy;` → `union`; `fn default() -> u8 { 1 }` → `default`; `default fn helfer() {}` → `helfer`; `union Foo { a: u8 }` → `Foo`; `static mut ZAEHLER: u8 = 0;` → `ZAEHLER`; `const fn name() {}` → `name`; `const NAME: u8 = 1;` → `NAME`; `pub const extern "C" fn helfer() {}` → `helfer`; die Fälle der Nacharbeit 1 bleiben grün. Rotbeweis `NAK-223-rot-deadcode.txt` um einen dritten Abschnitt erweitert (Mutation: die alte Wiederholung nach dem Schlüsselwort wieder eingesetzt → der Selbsttestfall `struct union where` wird rot; Rücknahme → grün).

BEINE dieser Runde: `py -3.13 tools/plan/gesundheit.py --selbsttest` (vollständig, Fallzahl im Manifest — sie muss um die neuen Fälle steigen), `py -3.13 tools/plan/gesundheit.py` zweimal (Messlauf ohne clippy, bytegleich, Tabelle unverändert gegenüber dem Stand der Nacharbeit 1 — der Wert „allow(dead_code) ohne Aufrufer“ muss 0 bleiben, sonst ist es ein Befund, den du nennst, nicht behebst), `py -3.13 tools/plan/gesundheit.py --mit-selbsttest` (so ruft der Runner A32; erwarteter Exit 4). Kein clippy-Lauf nötig (der Clippy-Pfad ist unberührt), kein Kanon — den setzt der Dirigent.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen — die Regex an allen Fällen oben plus `pub(in crate::a) fn helfer`, `unsafe impl Trait for X` (kein Element dieses Maßes; darf nicht als Name `Trait` fehlerhaft gemeldet werden, wenn kein `#[allow(dead_code)]` davor steht), Determinismus (zwei Läufe bytegleich), keine Änderung an Schwellen, Tabellenform, Exit-Semantik oder Clippy-Pfad; `git diff --stat` zeigt nur die drei genannten Dateien.

ABSCHLUSS: ein Commit (Pathspec), gepusht, Worktree sauber; melde dich erst, wenn `git status --short` leer ist und HEAD auf origin/master liegt.
