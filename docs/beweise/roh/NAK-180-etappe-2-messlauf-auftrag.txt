NAK-180 — Etappe 2, Messlauf (Abschluss des Baus vor der Erstprüfung). Zusatzauftrag des Dirigenten (2026-09-05).

Du bist der Bauer dieses Tickets (Session-Fortsetzung). Basis = `git rev-parse HEAD` beim Start. Git-Regeln, Grenzen, Beine und Kanon wie in docs/beweise/roh/NAK-180-auftrag.txt und docs/beweise/roh/NAK-180-etappe-2-auftrag.txt. Landminen: MSVC-Stack 1 MiB (Engines/Prozessoren im Test auf den Heap); Kanon abgekoppelt, Ausgabe mit *> (kein Löschen der Logdatei nötig).

AUSGANGSLAGE (docs/beweise/NAK-180.md §6.3): 21 von 37 Matrixzeilen sind gemessen, 16 sind „gebaut, nicht gemessen". Ein Stand, in dem die Matrix mehr zusagt, als ein Test belegt, ist kein fertiger Bau — die Erstprüfung wird erst gestartet, wenn jede Zeile gemessen ist oder ein an der Quelle belegter technischer Blocker je Zeile im Manifest steht. „Braucht ein erzwungenes Interleaving" ist kein Blocker, sondern die in der Matrix beschriebene Testform (§2.2: Einhängen in `beiLinkStatus`, zweiter Thread).

ZIEL: 37 von 37 Zeilen gemessen, je Zeile Testname und Ergebnis in §6.3; Rotbeweise, wo die Matrix sie verlangt.

SCHRITTE:
1. Nebenbefund §6.6 an der Ursache klären, nicht umgehen: Der Interventionsring liefert im Testrahmen alternierend kein `audible_intervention_end`. Finde die Ursache (Ring `InterventionsRing.h`, Testleser `naechstesInterventionsJsonFuerTest()`, Übergangszähler der Markierung `HoerMarkierung.h`). Liegt sie im Produkt, ist sie ein Defekt im Ticketbereich (Hörmarker/Ring gehören zu R2/R4) und wird behoben und mit Test belegt; liegt sie im Testleser, wird der Leser berichtigt. Ergebnis mit Quellzeilen in §6.6 (wird von „nicht untersucht" zu „Ursache und Behebung").
2. N-04 bis N-12 messen: R2-Weg (Replay des laufenden Markers, Backlog in Reihenfolge, Ausfade), `prepareToPlay`-Paar, Klassentest der Wireaussage, Sendefehler-Rückweg — über den echten ControlClient und einen vollständigen Marker-Zyklus (`EqCopSonde013PassageStateTest`, `EqCopMarkierungTest`, `sonde013_verdrahtung` je nach Zeile). Injizierte Sondenevidenz zwischen den Aufbaunachrichten (R2, N-07-Form), wo die Zeile es verlangt.
3. N-23, N-24, N-28, N-29, N-34, N-35 mit erzwungenem Interleaving messen: Testhaken in `beiLinkStatus` (Matrix §2.2/§2.1), zweiter Thread, deterministische Synchronisation (Latch/Barrier/Condition, kein Sleep als einzige Ordnung), beide N-34-Fälle (`reconnect()`; `stop()` wartet im Join, positiver Callback wird dann freigegeben, `stop()` kehrt vor `kStopFristMs` regulär zurück, `start()`), N-35 (Vergleich → Linkwechsel → Einreihen). Rotbeweise wie in der Matrix: Reihenfolge zurückgedreht (R10), Generationsbindung entfernt (R11), Sperre bzw. Zustellprüfung entfernt (R12), `store(0)` statt CAS (R13); Rohdateien `docs/beweise/roh/NAK-180-rot-<kennung>.txt`.
4. N-20: die Frist von `stop()` selbst als gemessene Zahl, während ein Tick in `namedPipeErreichbar` steht (Test-Pipe mit Probe-Namen, nie der Produktions-Pipename).
5. Betroffene Beine einzeln, dann der volle Kanon abgekoppelt bis EXIT=; kein Stand mit NOT RUN.
6. Manifest: §6.3 als neue Bilanz (jede Zeile: Test, Ergebnis), §6.6 Ursache, neuer Abschnitt §6.8 „Messlauf — was gemessen wurde" (Tabelle, Rotbeweise, Abweichungen), §6.7 Prüfliste nachziehen, Kopfzeile Phase „Etappe 2 gebaut und gemessen, Kanon <GRÜN n/n> auf <sha>, Erstprüfung offen". Bleibt eine Zeile ungemessen, steht dort der technische Blocker mit Quellzeile — nie „braucht Interleaving" oder „Testrahmen".
7. Commits mit explizitem Pathspec in logischen Schritten, `git pull --rebase`, Push. Kein Anfassen von docs/beweise/G4.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/. Turn mit der letzten Zeile „ETAPPE 2 MESSLAUF ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.

SELBSTAUDIT vor jedem Commit wie bisher; zusätzlich: Ist jeder Test grün aus dem richtigen Grund (Rotbeweis dreht genau die Regel zurück, die die Zeile misst)?