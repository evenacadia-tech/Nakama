NAK-121 / Planschritt S19b — ETAPPE 2, NACHARBEIT RUNDE 2: die sechs Defekte der Wiederpruefung 1 schliessen.

ROLLE UND ORT
Du bist der Nacharbeits-Worker (Opus) im sichtbaren Checkout C:/Users/phili/Projekte/Nakama, Branch master. Basis-SHA ist der Commit, der diesen Auftrag enthaelt; er steht im Workernamen und im Startprompt. Der Dirigent (Fable) misst nur am Repo. Lies zuerst CLAUDE.md (Repo-Root), dann docs/beweise/NAK-121.md: Kopf (Urteilsmarken), Paragraph 1 (Gate-Text mit der GRENZE: kein Wire-, State- oder Testvertrag), Paragraph 4 (Matrix H-01 bis H-22), Paragraph 13 (Nacharbeit Runde 1: was jeder der sechzehn Defekte bereits bekommen hat), dazu docs/beweise/roh/NAK-121-wiederpruefung-1-956e86e.txt (Rohurteil) und tools/dirigent/pruefliste.md. Die Wiederpruefung 1 (Codex gpt-5.6-sol, max, Thread 01a0683f-4fde-7352-8956-e1fcd37c7bbf) ergab NEEDS_WORK: elf Defekte geschlossen, fuenf offen, zwei neu. Der Dirigent hat am 03.09.2026 an der Quelle SECHS Befunde als Defekt bestaetigt und je Befund die Regel festgelegt; einen Befund (P1, oeffentliche Pruef-APIs) hat er als HAERTUNG eingeordnet — den fasst du NICHT an (Abschnitt unten). Du setzt genau die sechs Regeln um, nicht mehr. Bestehende Tests behalten ihre Aussage; erlaubt sind neue Tests, Aenderungen an Tests aus Etappe 2 und Nacharbeit 1 sowie neue Testnaehte in den vorhandenen Testnaht-Strukturen (`V3SecurityTestOptionen`, `CoordinatorFlushTestHaken`, `StoreKonfiguration.test_haken`). Neue Sichtbarkeit so eng wie moeglich: `pub(crate)`/`pub(super)` zuerst; eine neue uneingeschraenkte `pub`-Signatur nur, wenn ein Integrationstest sie zwingend braucht, und dann als lesende Beobachtung neben einer vorhandenen gleichartigen (Muster Paragraph 13.2). Jeder Defekt bekommt einen frischen Rotbeweis unter docs/beweise/roh/NAK-121-rot-<ID>-r2.txt (Mutante benennen, Testausgabe rot, danach gruen).

DIE SECHS DEFEKTE — je Befund woertlich (Codex, Wiederpruefung 1), dann die Regel des Dirigenten (03.09.2026)

R2-1 — D4 / G2-FLOATEDGE-001, broker/src/coordinator/sicht.rs:165. Codex: „Nach einem einzigen subnormalen Frame ruft jede weitere `messsicht`-Abfrage erneut `fenster_ms_bilden` auf und erhoeht den Zaehler. Im neuen Test werden 0, negativ, inf und NaN am Eingang abgewiesen; die vier anschliessenden Abfragen zaehlen lediglich denselben alten subnormalen Frame erneut. Damit misst der Zaehler Lesefrequenz statt die geforderten Grenzfaelle und verletzt die NaN-Zaehlzusage aus CLAUDE.md; ein zweiter Aufruf ohne neuen Frame reproduziert die Fehlzaehlung." (Dirigent an der Quelle bestaetigt: sicht.rs:165 ruft `fenster_ms_bilden` in jeder Sicht; coordinator_model.rs:839 erwartet 5 nach vier weiteren Lesungen.)
REGEL: Gezaehlt wird das EREIGNIS, nicht das Lesen. Die Fensterbildung laeuft genau einmal je angenommenem Frame an der Stelle, an der der Frame in den Stand kommt (senke.rs:183, `stand.messframes.insert`): das Ergebnis (`Option<f64>`) wird mit dem Frame gespeichert (Feld in `LiveMessframe`, crate-intern), `fenster_nicht_endlich` erhoeht sich dort genau einmal je Frame mit nicht-normaler/nicht-positiver Samplerate oder nicht-endlichem Ergebnis; `messsicht` liest nur den gespeicherten Wert und zaehlt nichts. Test (coordinator_model, den vorhandenen `subnormale_samplerate_ergibt_kein_fenster` umschreiben): ein subnormaler Frame → Zaehler 1 und `fenster_ms == None`; danach fuenf `messsicht`-Aufrufe ohne neuen Frame → Zaehler bleibt 1; zweiter subnormaler Frame → 2; normaler Frame → Zaehler unveraendert und `fenster_ms` endlich. Die Torfaelle 0, negativ, inf, NaN bleiben im Test (sie fallen in telemetrie.rs und erreichen den Stand nie). Mutante: Fensterbildung zurueck in die Sicht → rot (Zaehler waechst beim Lesen).

R2-2 — D6 / H-14, broker/src/coordinator/liveness.rs:227-247 (Codex nennt link.rs:229-231 als Vergleichsstelle des Hello-Pfads). Codex: „Startet der Coordinator mit 1023 oder 1024 restaurierten persistenten Guards und leerem Aliasregister, werden alter und neuer Nonce-Link zunaechst angenommen; die Kollision entsteht erst beim erneuten Report des verdraengten alten Links in `heartbeat_kontakt`, wo zwei Guards ohne Deckelpruefung eingetragen werden. Dieser Check laeuft nur bei einer Kollision waehrend des Hello, sodass der Store erst nach dem Welcome scheitert und lediglich Routing abschaltet; D6/H-14 sowie der geforderte Test am unabhaengigen persistenten Deckel bleiben offen." (Dirigent an der Quelle bestaetigt: liveness.rs:231 verwirft das Ergebnis von `registriere_wire_zuordnung` mit `let _ =`; :240-247 traegt beide Riegel ohne `guard_anzahl()`-Pruefung ein.)
REGEL: H-14 gilt fuer JEDEN Kollisionspfad, nicht nur fuer das Hello. Die Deckelpruefung (Aliasregister-Reservierung `len + 2 > MAX_QUARANTAENE` UND persistenter Index `guard_anzahl() + neue_riegel > MAX_KONFLIKT_GUARDS`) wird EINE crate-interne Funktion, die Hello-Pfad (link.rs:205-236) und Reportpfad (liveness.rs:227-247) gemeinsam rufen. Im Reportpfad wird das Ergebnis von `registriere_wire_zuordnung` ausgewertet; am Deckel (Alias ODER Riegel) wird in KEINEN der beiden Speicher eingetragen, kein Store-Schreibauftrag erzeugt, `cap_abweisungen` um eins erhoeht, und der meldende Link bleibt fail-closed getrennt (`trennen = true`, Rueckgabe `false`) — der Ausgang, den H-14 „abgewiesen" nennt. Unter dem Deckel bleibt das heutige Verhalten unveraendert (beide Riegel, Persistenz, C-10-Duplikatkonflikt, Session dirty). Test (coordinator_model oder store_crash_matrix; A4-SI, wenn ein Store beteiligt ist): Coordinator mit 1023 restaurierten persistenten Riegeln (ueber den Restore-Weg, den mod.rs:201 faehrt, oder ueber den Store) und leerem Aliasregister; Hello A (Nonce 1), Hello A (Nonce 2) verdraengt; Report des verdraengten Links → Riegelzahl bleibt 1023, kein Riegel persistiert, `cap_abweisungen` positiv, Link getrennt; Gegenprobe mit 1022 Riegeln → beide Riegel eingetragen (1024) und persistiert. Mutante: Deckelpruefung im Reportpfad entfernen → rot.

R2-3 — D10 / G2-TOCTOU-002, broker/src/store/migration.rs:205-206. Codex: „`Connection::open_with_flags` oeffnet zuerst die eigentliche Datenbank, anschliessend oeffnet `geoeffnete_db_volume(&db_pfad)` denselben Namen ueber ein zweites `CreateFileW`. Wird der Pfad oder eine Junction zwischen diesen Opens umgehaengt, haelt SQLite Objekt A, waehrend die Volumenentscheidung Objekt B klassifiziert; der urspruengliche TOCTOU bleibt bestehen. Test und Rotbeweis pruefen ebenfalls nur den separaten Helferhandle und belegen daher keine Entscheidung am Handle der SQLite-Datenbank." (Dirigent an der Quelle bestaetigt.)
REGEL: Die Entscheidung faellt auf dem Handle, das SQLite selbst haelt. Weg, im Repo verifiziert: `rusqlite::ffi::sqlite3_file_control(conn.handle(), c"main".as_ptr(), rusqlite::ffi::SQLITE_FCNTL_WIN32_GET_HANDLE, &mut handle as *mut _ as *mut c_void)` — libsqlite3-sys 0.38.2 (bundled) traegt die Konstante (Wert 29) und die Funktion, `rusqlite::ffi` ist der re-exportierte `libsqlite3_sys`, `Connection::handle()` ist `unsafe` (SAFETY-Kommentar nach H-22, die Wache zaehlt mit). Der Aufruf liefert das HANDLE der geoeffneten Hauptdatenbank; darauf laufen `GetFinalPathNameByHandleW` → `GetVolumePathNameW`/`GetDriveTypeW` wie heute. Die Klassifikation wird eine Funktion ueber ein HANDLE (crate-intern, z. B. `volume_am_handle`); `geoeffnete_db_volume(pfad)` darf als Vorpruefung oder Testhelfer bleiben, ist aber nicht mehr die Entscheidung; `store_vorbereiten` weist auf dem SQLite-Handle ab, bevor `pragmas_setzen` laeuft. Kein Schema, keine gespeicherten Bytes, keine neue Abhaengigkeit. Test (store_crash_matrix, A4-SI): Name und Objekt werden auseinandergezogen — Datenbank ueber eine Verzeichnis-Junction oeffnen (`cmd /c mklink /J`, ohne Adminrechte moeglich; die Junction umgeht den Reparse-Riegel des Stores nicht, deshalb direkt `Connection::open_with_flags` plus die neue Handle-Funktion, nicht `store_vorbereiten`), dann die Junction auf ein zweites Verzeichnis mit einer zweiten Datei desselben Namens umhaengen; die Klassifikation ueber das SQLite-Handle liefert den endgueltigen Pfad des URSPRUENGLICHEN Objekts. Mutante: zweites `CreateFileW` nach Namen → liefert das neue Ziel → rot. `store_weist_reparse_punkt_im_pfad_ab` und `volumenklassifikation_faellt_am_geoeffneten_objekt` bleiben (letzteren bei Bedarf auf die Handle-Funktion umstellen). Temporaere Junctions und Verzeichnisse raeumt der Test selbst ab.

R2-4 — NEU / Lebenszyklus verbinden↔trennen, broker/src/transport/server_v3/listener.rs:384-385 und win_handles.rs:465-469. Codex: „Sobald ein Writer abgeloest wurde, legt der Wachhund einen Eintrag in `abgeloest_abbrueche` an; `Verbindungsgriff::drop` entfernt spaeter nur `offen` und `abgeloest`, nie diesen neuen HashMap-Eintrag. Bei wiederholten H-02-Abloesungen waechst die Map ueber die gesamte Broker-Laufzeit unbegrenzt, obwohl nur 96 Verbindungen gleichzeitig erlaubt sind; damit bleibt der gekoppelte Verbinden/Trennen-Lebenszyklus unvollstaendig." (Dirigent an der Quelle bestaetigt; der Fix D14 aus Runde 1 hat den Eintrag ohne Gegenweg eingefuehrt — CLAUDE.md Arbeitsregel „verbinden↔trennen gehoeren in denselben Aenderungssatz", H-02 „sein Destruktor traegt die ID aus offen UND abgeloest aus".)
REGEL: Der Destruktor des Verbindungsgriffs entfernt `abgeloest_abbrueche[id]` im selben gesperrten Block wie `offen` und `abgeloest` (win_handles.rs:466-469). Die D14-Naht bleibt unveraendert wirksam: der abgeloeste Schreiber liest den Zaehler, solange er seinen geteilten Griff haelt; der letzte Griff faellt erst nach dem Threadende (H-02-Reihenfolge), das Abraeumen hungert die Naht also nicht aus — das misst der Test mit. Test: H-02-Lauf (abgeloester Schreiber, Wachhundabbruch gezaehlt, Threadende, letzter Griff faellt) → danach hat das Register fuer die ID keinen Eintrag mehr in `offen`, `abgeloest` UND `abgeloest_abbrueche` (Beobachtung ueber eine `pub(crate)`-Sicht des Registers oder einen Inline-Test im Modul; `V3Griff::gehaltene_handles()` ist das vorhandene Muster); der D14-Test und sein Rotbeweis bleiben gruen bzw. rot. Mutante: Abraeumen entfernen → rot.

R2-5 — D11 / H-01, broker/src/transport/server_v3/win_handles.rs:671 (Test `stopp_und_verbindungsende_zaehlen_unerwartete_cancel_fehler`). Codex: „Der neue Test bezeichnet diesen Block als Serverstopp, ruft aber `alle_io_abbrechen` direkt auf; auch die Mutante aendert nur den gemeinsamen Zaehlhelfer. Entfernt oder entkoppelt man den Aufruf aus `V3Griff::stoppen`, bleiben dieser Test und NAK-121-rot-D11-r1.txt gruen, obwohl der geforderte Stopp-Pfad wieder ungeprueft ist; D11 verlangt ausdruecklich einen Rotbeweis ueber diesen Pfad." (Dirigent an der Quelle bestaetigt: griff.rs:291 und :309 rufen `alle_io_abbrechen`; der Inline-Test ruft die Funktion selbst.)
REGEL: Der Beweis geht durch `V3Griff::stoppen`. Test (security_vectors, neben dem H-01-Test): Server mit einem toten Handle NUR in `offen` starten — nicht in `abgeloest`, sonst zaehlt bereits der Wachhund und der Stopp-Pfad ist nicht isolierbar; dafuer bekommt die vorhandene Naht `totes_handle_ins_register` (listener.rs:317-330) eine Variante „nur offen" (Enum statt bool oder zweites Feld in `V3SecurityTestOptionen`; Testnaht wie in Paragraph 12.4b). Ablauf: Zaehler vor dem Stopp 0 (nach mindestens einem Wachhundtick weiterhin 0, Gegenprobe), `griff.stoppen()`, danach `cancel_auf_totem_handle >= 1`. Rotbeweis: beide `alle_io_abbrechen`-Aufrufe im Stopp-Pfad (griff.rs:291 und :309) durch eine nicht zaehlende Variante ersetzen bzw. die Statistik dort nicht durchreichen → rot. Der Inline-Test in win_handles.rs darf bleiben, ist aber nicht der Beweis. Die Verbindungsende-Haelfte (`io_abbrechen`) bleibt wie in Runde 1.

R2-6 — D15 / H-07-Test, broker/tests/security_vectors.rs:1058 (`abfluss_vor_dem_schliessen_erreicht_den_peer`, Haelfte a). Codex: „Im lesenden H-07-Lauf ist `frame_lesen` vollstaendig abgeschlossen, bevor `drop(client)` den Verbindungsabbau ausloest; beim Flush ist somit kein Broker-Write mehr ausstehend. Diese Haelfte bleibt ohne `abfliessen_mit_frist` gruen, waehrend der vorhandene Rotbeweis ausschliesslich wegen des Timeoutzaehlers im nicht lesenden Lauf scheitert; die geforderte Aussage, dass ein ausstehender Write den lesenden Peer vor dem Close erreicht, bleibt unbelegt." (Dirigent an der Quelle bestaetigt.)
REGEL: In Haelfte (a) muss das Welcome beim Beginn des Abbaus noch UNGELESEN im Ausgabepuffer liegen, und der Abbau beginnt brokerseitig. Ablauf: Hello schreiben, NICHT lesen, auf `hello_accept` in der Spur warten; brokerseitigen Abbau ausloesen (`griff.stoppen()` in einem Hilfsthread); erst wenn die Spur `register_austrag` traegt (der Marker liegt vor dem Abfluss, win_handles.rs:470), liest der Peer: das Welcome kommt vollstaendig an, danach EOF/Pipe geschlossen; `flush_abgelaufen == 0`; Reihenfolge `register_austrag` → `flush` → `close`. Das ist die Zusage aus H-07 wortgleich: abfliessen, dann trennen, dann schliessen. Mutante: `abfliessen_mit_frist` ohne Warten (sofort `true`) → `DisconnectNamedPipe` verwirft den ungelesenen Puffer, der Peer liest kein Welcome → rot. Haelfte (b) und H-08 bleiben unveraendert. Die 250-ms-Frist (`FLUSH_FRIST`) reicht fuer die Leseaufnahme nach dem Marker; flackert der Lauf, den Peer-Leser VOR dem Stopp starten und auf den Marker warten lassen — kein Sleep als Synchronisation.

AUSDRUECKLICH NICHT ANFASSEN — die Haertung des Dirigenten (03.09.2026)
P1 „Halte die neuen Pruef-APIs aus der oeffentlichen Oberflaeche" (coordinator/mod.rs:121: `CoordinatorFlushTestHaken::panik_unter_standlock_scharf`, `Registrierung::DeckelErreicht`, `store::geoeffnete_db_volume`, `store::pfad_aus_utf16`, `Coordinator::standlock_vergiftet`/`fenster_nicht_endlich`, Konstanten). Einordnung: HAERTUNG, kein Defekt. Der Gate-Satz verbietet eine „oeffentliche Signatur, die ein anderer Verbraucher liest"; die Bibliothek `eqcop_broker` hat keinen anderen Verbraucher als ihre eigenen Binaries und Tests (kein zweites Cargo.toml im Repo referenziert sie), Paragraph 5.8 laesst Beobachtungs-APIs zu H-Zeilen ausdruecklich zu, Paragraph 12.4b und die Abschlusspruefung 1 haben dasselbe Muster angenommen, und H-17 haelt fest, dass ein `cfg(test)`-Zaun den Bau der Integrationstests braeche. Der Feature-Zaun (`testhaken`-Feature mit Selbst-Dev-Dependency) geht datiert ins Register (Ticketabschluss). Du aenderst daran nichts; die Sichtbarkeitsregel oben gilt nur fuer das, was DU neu anlegst.

FORM
- Ein Commit je Defekt, Betreff „NAK-121 Nacharbeit 2 R2-<n>: <Kurz>"; nach jedem Commit `cargo test --manifest-path broker/Cargo.toml` gruen (Bibliothek und betroffene Integrationstests; A4-SI-Tests mit `-- --ignored` fuer die betroffenen Namen), Pathspec-Commit, Push (bei sauberem Worktree `git pull --rebase` vorher).
- Am Ende der Kanon der betroffenen Beine (mindestens broker, A4-SI, A21, A22, B10) ueber `pwsh -NoProfile -File tools/beweise.ps1` — lies im Skript, wie einzelne Beine gewaehlt werden; fehlt eine Auswahl, laeuft der volle Kanon ABGEKOPPELT (Start-Process pwsh -WindowStyle Hidden, Ausgabe mit `*>` in eine Logdatei unter $env:TEMP, z. B. nakama-nak121-kanon-nacharbeit2.log, danach `Add-Content` einer `EXIT=`-Zeile), nie als Hintergrundbefehl der Session; fertig ist er mit der `EXIT=`-Zeile. Rohausgabe (`docs/beweise/roh/NAK-121-<sha>.md`) committen. Kein Stand mit Laufstatus NOT RUN.
- Manifest docs/beweise/NAK-121.md: nur der Kopf (Feld „Phase": Satz „Nacharbeit Runde 2 abgeschlossen (Datum): sechs Defekte der Wiederpruefung 1 geschlossen — Paragraph 14"; Feld „Kanon nachher", wenn ein voller Lauf gefahren wurde; Feld „Testanzahl") und Paragraph 4 nur dort, wo eine Regel eine H-Zeile praezisiert (H-14 Reportpfad, H-02 Abraeumen des Zaehlers, H-07 Beweisablauf); neuer Abschnitt „## 14. Nacharbeit Runde 2 (Wiederpruefung 1)" append-only VOR dem ersten „## Kanon-Lauf"-Abschnitt: je Defekt Codex-Wortlaut gekuerzt, Regel woertlich, Umsetzung, Commit, Test, Rotbeweis; dazu die Pruefliste (tools/dirigent/pruefliste.md) abgehakt mit Messort je Zeile. Nichts unterhalb des Kopfes umschreiben. Dokuriegel: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-121.md`.
- Git: `git status --short` vor jedem Commit; nur explizite Pathspecs; nie `git add -A`, nie `--amend`, nie `reset --hard`; fremde uncommittete Dateien nie anfassen. Eigene temporaere Dateien nur ueber `[System.IO.File]::Delete` bzw. `[System.IO.Directory]::Delete` auf exakte, selbst angelegte Pfade loeschen (Remove-Item ist eine Ask-Regel und bleibt es). Ende: Worktree sauber, HEAD auf origin/master. Wird dein Kontext knapp, committe und pushe den erreichten Stand mit Hinweis im Manifest, welche R2-Nummern offen sind.
- Keine Rueckfragen an den User; technische Entscheidungen triffst du selbst und begruendest sie im Manifest.
