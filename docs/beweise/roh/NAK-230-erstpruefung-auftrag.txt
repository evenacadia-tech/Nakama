Erstprüfung Registerticket NAK-230 (vor S26–28) — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer. Prüfbereich ist ausschließlich der Änderungssatz dieses Tickets:
`git diff 819d54d0fe1998bba83ec82f47410d2e6630228d...e20d815677aab31d023e7ee68dc866d99767ac20 -- eq-copilot/plugin/core/ipc/controlclient/Intern.h eq-copilot/plugin/tests/IpcTestMain.cpp`
mit BASIS = 819d54d0fe1998bba83ec82f47410d2e6630228d und ZIEL = e20d815677aab31d023e7ee68dc866d99767ac20 (HEAD; er muss vor und nach deinem Lauf identisch sein).
Alles andere, was seit BASIS im Repo geändert wurde, gehört zu anderen Tickets und ist hier nicht Prüfgegenstand.
Prüfstufe T2 (Registerticket der Klasse Defekt · Vertrag, Abnahme durch den Dirigenten nach dieser Prüfung).

GATE-TEXT, wörtlich und vollständig (Registerzeile NAK-230 in docs/offene-punkte.md):
„`fehlercodeHaeltVertrag` kennt nur die zwölf Fehlercodes der Fassung 1. `ControlClient.cpp`:74–81 führt `protocol_mismatch` bis `internal`; die sechs Codes der Fassung 2 (`abdeckung_zu_gering`, `schon_terminal`, `ohne_lautheitsabgleich`, `ohne_resultatmessung`, `blindreihenfolge_widerspruch`, `reihenfolge_nicht_gebunden`) fehlen, und ein `command_ack` mit einem davon fällt beim Leser (`:228`), obwohl der Broker ihn seit SONDE-013 sendet und der Schemakommentar „ein unbekannter Code wird als generischer Fehler behandelt, nie als Erfolg“ verlangt — nicht „die Nachricht wird verworfen“. Der Leser ist strenger als der Vertrag. Außerhalb der G5-Grenze (Experimentfamilie); nicht mit E7 (`unknown_target`, Fassung 1) verwechseln.“
Vertragstext dazu: `eq-copilot/schemas/v3/eq-ipc-v3.schema.json`:815 (description von `$defs/fehlercode`) und die Enum-Liste :816–834. Regel des Dirigenten (Ticketauftrag docs/beweise/roh/NAK-230-auftrag.txt): (1) ein `code`, der keine Zeichenkette ist, bleibt Strukturfehler; (2) ein Zeichenketten-`code` macht die Nachricht nie ungültig — bekannte Codes (achtzehn) laufen wie bisher, jeder andere ist generischer Fehler, nie `ok`, Text erhalten; (3) `fehlercodeHaeltVertrag` verliert die Riegelrolle; (4) die bekannte Liste steht genau einmal und wird von einem Test gegen die Enum-Liste des Schemas gehalten; kein Verhalten für Fassung-1-Codes ändert sich.

Mehr verlangt das Gate nicht. Die Verhaltensmatrix steht in docs/beweise/NAK-230.md §2 (sechs Zeilen plus zwei Zusatzzeilen, je mit Test in `IpcTestMain.cpp`, Bein B10 `EqCopIpcTest`); sie ist Referenz. Prüfe sie ZUERST gegen Vertragstext und Regel — insbesondere §2.1: ein `command_ack` mit `ergebnis` = Erfolg UND unbekanntem `code` wird auf endgültigen Fehler herabgestuft (In-Flight frei, nie Erfolg). Wenn du diese Deutung für einen Verstoß gegen den Vertrag hältst, ist das ein Befund gegen die Matrix, mit Zitat des Satzes, der bricht. Die Riegel/Tests, die es messen: die acht Tests `nak230_*` in `eq-copilot/plugin/tests/IpcTestMain.cpp` (Rotbeweise docs/beweise/roh/NAK-230-rot.txt, -rot-runde1.txt, -rot-schemariegel.txt, -gruen.txt), der Bestandstest `nur_schemafestes_command_ack_gibt_inflight_frei` (Fall 3 umgeschrieben, weil er die alte, vertragswidrige Zusage festschrieb), B10 389 Prüfungen (vorher 381). Jüngster Kanon: GRÜN 62/62, Exit 0, auf `1867cac2` (docs/beweise/roh/NAK-230-1867cac.md); der Commit danach berührt nur docs/.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: verletzt einen Satz des Gate-Textes oder Vertragstextes oben, eine Zeile der Verhaltensmatrix, einen Test oder eine Invariante aus CLAUDE.md (Identität eingefroren, Audio echtzeitfest, Nulltest bitidentisch, State verlustfrei). Reproduzierbar, mit Kommando; je Defekt: Datei:Zeile und der Satz, der bricht. Für dieses Ticket heißt DEFEKT vor allem: ein Fassung-1-Code, der sich anders verhält als vorher; ein bekannter Code, der als generischer Fehler geführt wird; ein unbekannter Code, der als Erfolg endet oder die Nachricht verwirft; ein Code-Text, der verloren geht oder umgeschrieben wird; eine Liste, die vom Schema abweicht, ohne dass der Riegeltest fällt; ein Test, der nur einen Nebeneffekt misst.
- LÜCKE: Gate, Matrix und Vertrag sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, keine Erwähnung als NEEDS_WORK-Grund):
1. docs/** — Manifeste, Register, Planstand, Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände, Kommentar-Wortlaute. Manifeste sind Kontext, kein Prüfgegenstand.
2. Im Register datierte Härtungen und die Nebenbefunde des Manifests §5 (NB-1 Schemaleser-Lesart, NB-2 `Copy-Item`-Zeitstempel, NB-3 erste Fassung der Matrixzeilen, NB-4 `commandAckHaeltVertrag` erbt die Herabstufung, NB-5 Schemaebene und Wireleser verschieden streng) — sie sind benannt; nur ein Befund, wenn du zeigst, dass eine Zusage bricht. Ein Befund gegen ein Prüfskript, einen Riegel oder den Runner, der absichtliche Sabotage in repo-eigenen Quellen voraussetzt, ist HÄRTUNG.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. §2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lückenlos“) gilt für Gate-Audits, nicht für diesen Ticketreview. Kosmetik, Stil, optionale Härtung, theoretische Randfälle, Ticketfremdes: nein.

WAS DU TUST:
1. Lies den Diff im Prüfbereich und die Matrix §2.
2. Prüfe am Zielstand, ob Tests und Riegel messen, was Gate und Matrix verlangen: (1) Jede der sechs Matrixzeilen hat genau einen Test, der die Zusage an der Zeile misst, die sie trägt — nicht an einem Nebeneffekt; die Rotbeweise der Zeilen 4 bis 6 sind echt (Test rot am Basisstand, grün am Zielstand). (2) Verhalten der zwölf Fassung-1-Codes unverändert, auch für `unknown_target` (E7 aus NAK-214). (3) Der Schema-Riegeltest liest die Enum-Liste aus dem Repo-Schema und fällt in beide Richtungen (Code im Schema, nicht in der Liste; Code in der Liste, nicht im Schema). (4) Der Text des Codes erreicht Anzeige und Log unverändert (`Verbindung.cpp`, `beiAntwort`). (5) Die Herabstufung in §2.1 trifft nur Erfolgsergebnisse mit unbekanntem Code; ein bekannter Code mit Erfolg bleibt Erfolg. (6) Der umgeschriebene Bestandstest `nur_schemafestes_command_ack_gibt_inflight_frei` misst weiterhin alle Fälle, die er vorher maß, bis auf den vertragswidrigen Fall 3. Du darfst lesen und lesende Kommandos fahren (`git`); du schreibst nichts und baust nichts.
3. Prüfe, ob der Änderungssatz seit BASIS etwas am Produktverhalten gebrochen hat (Identität, Passthrough/Nulltest, State, Audio-Thread; hier vor allem der In-Flight-Lebenszyklus des `ControlClient`: kein Auftrag bleibt endlos stehen, keiner wird doppelt freigegeben) — nenne, was du selbst nachgemessen und was du nur gelesen hast.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Matrix bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
