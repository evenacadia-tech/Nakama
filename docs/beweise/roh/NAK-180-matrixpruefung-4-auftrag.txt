Wiederprüfung NAK-180 / G4-Nacharbeit 1, Matrixprüfung 4 (Runde 3 der Etappe 1, Budgetende) — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff cd8f5a37efeef553369cda089c31fed3b853dc91...8e63dc578b1176792bc22190b8570b8b91709e21 -- docs/beweise/NAK-180.md`
(ZIEL = 8e63dc578b1176792bc22190b8570b8b91709e21 = HEAD, vor und nach deinem Lauf identisch). Der übrige Matrixbereich wurde in Matrixprüfung 1 bis 3 geprüft und ist hier nicht Prüfgegenstand. Es gibt weiterhin keinen Produktcode in diesem Ticket; geprüft wird die Spezifikation (docs/beweise/NAK-180.md §2 bis §4, §5.3.1).

BEFUND, den diese Runde schließen sollte (wörtlich aus Matrixprüfung 3, docs/beweise/roh/NAK-180-matrixpruefung-3-fd1074d.txt; Regel R11 des Dirigenten in docs/beweise/NAK-180.md §5.3, Zeile MP3-1):
1. [P1] „Serialisiere positive und negative Linkcallbacks" — Bei `reconnect()` oder `stop()` während eines laufenden positiven Callbacks kann `v3ControlLink(false)` dessen Zustand zuerst löschen und der noch laufende `v3ControlLink(true)` anschließend `replayFaellig` bzw. den Bericht erneut setzen: Der negative Callback läuft ausdrücklich auf dem Aufruferthread (`ControlClient.h:239-243`), während `meldeLinkStatus` nur das Statusbit, nicht die Callback-Ausführung serialisiert (`ControlClient.cpp:988-995`). Weil danach wegen des bereits falschen Status kein zweiter Ende-Callback folgt, übernimmt der nächste Link den veralteten Bericht. Damit reicht die zugesagte sterbende Generation nicht aus und N-23/verbinden↔trennen bleibt gebrochen; erforderlich ist eine Generationsprüfung beziehungsweise Serialisierung samt Test dieses true/false-Interleavings. Regel R11: Callback-Wirkungen sind generationsgebunden; überlappende Callbacks prägen keinen späteren Link; jede Wirkung von `v3ControlLink(true)` trägt die `wireGeneration` ihres Links, Verbraucher werten nur die aktuelle Generation; das Replay leitet sich zur Ausführungszeit aus dem aktuellen lokalen Zustand ab; Serialisierung nur zusätzlich und nur verklemmungsfrei für den reentranten Pfad (`PluginProcessor.cpp:1226-1227`) und `stop()` aus dem Client-Thread (B-CC-11). Der Worker hat als Weg E10 gewählt: keine Serialisierung, sechs Zustände von Bit auf Generationszahl umgestellt (Zustellstand 3 des offenen lokalen Begins, `neutralerNeuaufbau`, `nichtNeutralerNeuaufbau`, `berichtOffen`, `replayFaellig`, …), `v3ControlLink(false)` löscht nur noch als Hygiene, N-34 misst das erzwungene Interleaving in drei Fällen (`reconnect()` aus zweitem Thread, `stop()`+`start()`, reentrant).

GATE-BEZUG, wörtlich (docs/FL-Nakama-Sonden-Design-Entwurf.md:4289): „Hörmarker- und Experimentende schließen alle Taintintervalle; der reguläre Pfad nullt danach wieder." Dazu Entwurf §34.2 (Resync als ausdrückliche, bestätigte Lebenszyklusoperation), SONDE-013-Matrix M-33 bis M-39 und M-58 bis M-63 (docs/beweise/SONDE-013.md:460-653), Invarianten aus CLAUDE.md (Audio-Thread ohne Lock/Allokation, Nulltest bitidentisch, NaN-Ehrlichkeit, Schemas eingefroren).

Du beantwortest genau zwei Fragen:
1. Ist der Befund MP3-1 am Zielstand in der Matrix geschlossen (ja/nein, mit E10, N-34, der umgeschriebenen Linkende-Zusage und der Schritt-für-Schritt-Tabelle in §2.1, den Zustandsdefinitionen in §2.1, messendem Test und Rotbeweis als Beleg)? Prüfe an der Quelle (`eq-copilot/plugin/core/ipc/ControlClient.cpp`, insbesondere `:695-768`, `:988-996`, `:1282-1283`, `:1559`; `ControlClient.h:239-243`; `eq-copilot/plugin/src/PluginProcessor.cpp:1172-1231`; `broker/src/coordinator/intervention.rs:214-219`), ob die Generationsbindung das Interleaving tatsächlich unschädlich macht: Kann eine Wirkung eines überholten positiven Callbacks (Aufbau-Flag, `replayFaellig`, offener Bericht, Zustellstand, Weckung) auf dem nächsten Link noch etwas auslösen — ein veraltetes `false`, eine doppelte `intervention_id`, ein `end` ohne Begin, ein ausbleibendes Replay? Gibt es einen Verbraucher, der eine Generationszahl liest, ohne sie mit der aktuellen `wireGeneration` zu vergleichen, oder der zwischen Vergleich und Wirkung die Generation wechseln lassen kann (Vergleich und Verbrauch nicht atomar)? Trägt der reentrante Weg (`v3ControlLink(true)` → `controlV3.reconnect()` → `v3ControlLink(false)` auf demselben Thread, danach Rückkehr in den positiven Callback) mit E10? Ist die Aussage in E7.2 (`end` reist nur bei Zustellstand 3 mit `zustellGeneration == wireGeneration.load()` oder Zustand 2, sonst Replay-Begin voran) gegen `intervention.rs:214-219` und gegen die doppelte `intervention_id` (E6) konsistent?
2. Hat der Fixdiff etwas gebrochen — eine ältere Matrixzeile (N-01 bis N-33), einen Entscheid E1 bis E9, eine Zeile der SONDE-013-Matrix, einen Satz des Gate-Bezugs, eine Invariante (insbesondere: kein Lock und keine Allokation im Audio-Thread; die Generationszustände liegen auf Worker-, Nachrichten- und ControlClient-Thread), einen Vertrag (keine neuen Wire-Felder)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1 bis 3 (DEFEKT / LÜCKE / HÄRTUNG): DEFEKT nur, wenn eine Matrixzeile, R1 bis R11, der Gate-Satz, der Entwurf oder eine Invariante bricht; was Matrix und Entwurf zu einem Fall nicht sagen — und was §2.4 ausdrücklich nicht zusagt (Callback-Ordnung, abgelöster Thread NAK-184, Sources-Wirkungen) — ist LÜCKE, nicht Defekt; wünschenswert, aber von keiner Zusage verlangt, ist HÄRTUNG. Ausgeschlossen: docs/** außerhalb von NAK-180.md §2–§4/§5.3.1, Registerpunkte NAK-155 bis NAK-190, tools/dirigent/pruefliste.md als Anforderungsquelle, Ticketfremdes (NAK-181/182, `IpcVerbindung::oeffnen`-Fristlogik, G6/G7-Punkte NAK-183 bis NAK-185), Kosmetik, Prosa- oder Zeilenverweis-Inkonsistenzen im Manifest.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — MP3-1 geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID, Quelle Datei:Zeile, welcher Satz bricht, Befundklasse.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.