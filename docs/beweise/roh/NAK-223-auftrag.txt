NAK-223 — S25b: Gesundheitsmaß für Codebase und Kontext mit Schwellen (`tools/plan/gesundheit.py`). Ticketauftrag des Dirigenten (2026-09-09, nach G5 T3 PASS; erstes Pflegeticket des Plans).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-223.md ein (Manifest neu anlegen; Muster ist die Kopftabelle von docs/beweise/NAK-214.md: Ticket, Phase, Prüfstufe, Basis-SHA, Kanon vorher, Kanon nachher, Ticketpfade, Nicht angefasst, Änderungssatz, Prüfmodell — darunter §1 Ticket- und Regelbezug wörtlich, §2 Iststand und Schwellen, §3 Prüfliste, §4 Nebenbefunde, §5 Verlauf; ein lebender Kopf, darunter append-only). Aufsicht NORMAL. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (neue Dateien vorher `git add -- <pfad>`); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie Backticks in `-m`. Keine Neben-Refactors, keine Legacy-Umbenennung, kein Produktcode. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §3 nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits, Manifest und Ausgabetexte des Werkzeugs Deutsch; Konsolenausgaben ASCII-sicher wie planstand.py (Umlaute nur, wo die Konsole sie sicher trägt); Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder `rm -rf` (Ask-Regel, der Auto-Modus hält an); selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. Beende deinen Turn nicht, solange ein von dir gestarteter Lauf (Kanon) läuft; die letzte Logzeile `EXIT=` ist das Ende.

TICKET, wörtlich (docs/plan/plan.json, Schritt S25b, Ticket NAK-223, Stufe T2): „ANGELEGT 08.09.2026 (User-Wort im Register NAK-223: Codebase-Gesundheit ist Planbestandteil, nicht Nebentätigkeit). INHALT: `tools/plan/gesundheit.py` misst die Codebase (Zeilen je Quelldatei unter broker/src, eq-copilot/plugin/src und eq-copilot/plugin/core mit Grenze 2 000 und Ziel 1 500; Funktionen über 200 Zeilen; `cargo clippy`-Warnungen; aufruferlose `#[allow(dead_code)]`-Helfer; Kommentare, die auf nicht existente Bezeichner verweisen) und den Kontext (Bytes von MEMORY.md, Root-CLAUDE.md und Dirigenten-Skill gegen die Grenzen des Playbooks docs/context-hygiene-playbook.md; Index-Zeilen über 250 Zeichen; Memory-Dateien ohne Index-Link), druckt eine Tabelle und gibt Exit 4, sobald eine Schwelle gerissen ist. Angebunden in der Dirigenten-Skill §3.5 (Abschlussfenster) und als nicht blockierendes Bein in tools/beweise.ps1. Die Schwellen entscheidet der Bauer aus der Messung des Iststands am Basis-SHA, nie geraten; der Iststand steht im Manifest. GRENZE: kein Produktcode, kein Umbau bestehender Beine. Muster für die Anbindung: tools/plan/planstand.py und dokuriegel.py. NACHTRAG 08.09.2026 (Dirigent, Technik): generierte Dateien unter `broker/src/generiert/` (Codegen aus dem .fbs, Bein A9) sind von der Zeilenmessung ausgenommen; die Handmessung beim G5-Abschluss (Register NAK-234) zählt sie nicht."

USER-WORT (Register docs/offene-punkte.md, Zeile NAK-223, 08.09.2026), das den Schritt begründet: „ich möchte dass die codebase selbst regelmäßig auf solche probleme überprüft und verbessert wird. das projekt muss nachhaltig funktionieren und wartbar bleiben. das geht nur wenn die codebase regelmäßig in perfektem shape bleibt. nicht immer nur dann wenn hart an der grenze ist. … ich will nicht dass deine eine kleine nebentätigkeit ist, sondern bestandteil vom plan . da ich kein coder bin, muss ich den code für di KI so wartbar wie möglich halten". Das Werkzeug ist die Messung, die diesen Satz jede Phase prüfbar macht; ein Maß, das nichts misst oder immer grün ist, wäre ein totes Element (CLAUDE.md-Invariante).

GRENZE (Ticketpfade, nichts sonst): `tools/plan/gesundheit.py` (neu); `tools/beweise.ps1` NUR ein zusätzliches, nicht blockierendes Bein — bestehende Beine, Riegel, Bau- und Urteilslogik unverändert; ein Befund von gesundheit.py darf weder das Kanonurteil noch den Exitcode des Runners ändern, muss aber in Übersicht und Rohausgabe sichtbar sein; wie du das minimal im Runner ausdrückst (z. B. ein Feld am Beineintrag, das aus Exit 4 „[HINWEIS]" statt „[ROT]" macht), entscheidest du und begründest es im Manifest; Muster: die bestehenden `Art='python'`-Einträge. `.claude/skills/dirigent/SKILL.md` NUR in §3.5 die Klammer „(bis NAK-223 gebaut ist: … Measure-Object -Line …)“ durch den Aufruf ersetzen — die Datei hat 35 685 Bytes bei einer Grenze von 36 KB, sie darf netto nicht wachsen (vorher/nachher messen und im Manifest nennen). `docs/context-hygiene-playbook.md` nur ein Satz, der das Werkzeug als Messquelle für Phase A/F nennt. `docs/beweise/NAK-223.md` (neu) und `docs/beweise/roh/NAK-223-*.txt`. NICHT anfassen: Produktcode, Tests, Fixtures, Verträge, `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md` (der Dirigent zieht Register, Plan und CLAUDE.md-Regel nach der Abnahme nach; die dortige Klammer „bis es gebaut ist, von Hand“ bleibt bis dahin stehen).

WAS DAS WERKZEUG MISST — Codebase: (1) Zeilen je Quelldatei (*.rs, *.cpp, *.h, *.hpp) unter broker/src, eq-copilot/plugin/src und eq-copilot/plugin/core; Grenze 2 000, Ziel 1 500; `broker/src/generiert/` ausgenommen. (2) Funktionen über 200 Zeilen — Rust `fn`-Blöcke und C++-Funktionsdefinitionen über Klammertiefe; eine Heuristik ist zulässig, aber deterministisch, im Manifest beschrieben und mit ehrlich benannten Fehlerklassen (Makros, Lambdas, Strings mit Klammern). (3) `cargo clippy`-Warnungen für broker/Cargo.toml (Zahl; nur mit Schalter `--clippy`, weil es baut — Laufzeit im Manifest). (4) Aufruferlose `#[allow(dead_code)]`-Helfer: Rust-Bezeichner unmittelbar unter dem Attribut, die außerhalb ihrer Definition im Crate nirgends vorkommen. (5) Kommentare, die auf nicht existente Bezeichner verweisen: Backtick-Bezeichner in `//`-, `///`-, `/* */`-Kommentaren, die im Repo (Rust und C++) nirgends als Definition oder Verwendung vorkommen — Heuristik, Trefferliste und Fehlerrate im Manifest. Kontext: Bytes von `<memory>/MEMORY.md` (Ziel 22 KB, Ceiling 24,4 KB), Root-`CLAUDE.md` (≤ 24 KB), `.claude/skills/dirigent/SKILL.md` (≤ 36 KB); Index-Zeilen in MEMORY.md über 250 Zeichen; Memory-Dateien ohne Index-Link — Grenzen wörtlich aus docs/context-hygiene-playbook.md und Skill §3.5. Der Memory-Pfad ist rechnerlokal (`~/.claude/projects/<slug>/memory/`, Slug aus dem Repo-Pfad; hier `C--Users-phili-Projekte-Nakama`): fehlt er, meldet das Werkzeug „nicht messbar“, nie rot. Ausgabe: eine Tabelle (Maß, Ist, Ziel, Grenze, Status) plus Trefferliste je gerissenem Maß; Exit 0 ohne gerissene Grenze, Exit 4 mit; `--json` für den Runner; nur Standardbibliothek; Laufzeit ohne `--clippy` unter 60 s (messen).

SCHWELLEN AUS DER MESSUNG, NIE GERATEN: Zuerst den Iststand am Basis-SHA messen und als Tabelle ins Manifest §2. Die Zeilengrenzen 2 000 / 1 500 und Funktionen 200 sind durch Register und Plan gesetzt. Für clippy, dead_code und Kommentar-Bezeichner ist der Iststand der Ausgangswert; Grenze so setzen, dass sie heute genau dort reißt, wo ein Pflegeticket bereits angelegt ist (NAK-224 `hypothese.rs`, NAK-225 `PluginProcessor.cpp`/`FeatureEngine.h`/`ControlClient.cpp`, NAK-234 `transport/server_v3/mod.rs`), und sonst nicht — jede weitere gerissene Schwelle ist ein Nebenbefund (§4), kein stiller Fix. Referenzmessung des Dirigenten vom 08./09.09.2026 (Register NAK-223, NAK-234): sechs Dateien über 2 000 Zeilen (eine davon generiert), zwölf über 1 500; MEMORY.md 3,4 KB, CLAUDE.md 22,9 KB, Skill 35,7 KB, 0 Index-Zeilen über 250 nach dem Volllauf vom 09.09.

BEWEIS: Je Maß ein Rotbeweis unter docs/beweise/roh/NAK-223-rot-<maß>.txt — eine synthetische Verletzung (Temp-Kopie des Baums oder ein Testschalter, der eine eingebaute Verletzung einspeist) führt zu Exit 4 mit der richtigen Zeile, die Rücknahme zu Exit 0; Rohausgabe beider Läufe in der Datei. Dazu ein Selbsttest `--selbsttest` (Muster A27/A30: Fälle entstehen im Speicher, laufen durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil), der als Teil des neuen Runner-Beins läuft. Beine dieser Runde: das neue Bein, A9 (unverändert grün, Beweis, dass `generiert/` unberührt ist), und am Ende der volle Kanon abgekoppelt auf sauberem Baum:
  $log = Join-Path $env:TEMP 'nakama-nak223-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-223.md -Anhaengen -Titel NAK-223 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl
(einfache Anführungszeichen um EXIT= — CLAUDE.md-Landmine vom 08.09.2026: doppelte werden von der Argumentzerlegung geschluckt und die Marke fehlt still). Warten bis die letzte Logzeile `EXIT=` ist; Ergebnis ins Manifest; die Rohausgabe schreibt der Runner nach docs/beweise/roh/NAK-223-<sha>.md. Erwartung: GRÜN mit 61 plus deinem neuen Bein; die Beinzahl im Manifest nennen. Urteilsmarke `<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->` erst nach grünem Kanon; T2 setzt der Dirigent nach der Codex-Prüfung.

SELBSTAUDIT vor jedem Commit (adversarial): Zahlenränder (Datei mit exakt 2 000 Zeilen, leere Datei, CRLF und LF, UTF-8 mit BOM, Datei ohne Schlusszeilenumbruch, Funktion mit exakt 200 Zeilen), Determinismus (zwei Läufe bytegleich, sortierte Ausgabe), Pfade ohne Rechnerbezug (Repo-Root aus dem Skriptpfad; Memory-Pfad optional), Exit 4 nur bei gerissener Grenze, keine irreführenden Ausgabetexte („Grenze“ = reißt, „Ziel“ = Wunsch), keine Behauptung im Manifest ohne Lauf aus dieser Session, Runner-Urteil ohne und mit gerissener Schwelle identisch (gemessen, Rotbeweis). Beziehungen: Werkzeug ↔ Skill-Aufruf ↔ Runner-Bein ↔ Playbook-Satz in einem Änderungssatz.

ABSCHLUSS: Manifest vollständig (Kopftabelle mit Basis- und Endstand, Iststand, Schwellen mit Begründung, Rotbeweise, Prüfliste, Nebenbefunde, Kanon-Kopfzeile), alles committet und gepusht, Worktree sauber. Melde dich erst, wenn `git status --short` leer ist und `git rev-parse HEAD` gleich `git rev-parse origin/master` ist.
