Erstprüfung S25d / Ticket NAK-225 — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer. Prüfbereich ist ausschließlich der Änderungssatz dieses Tickets:
`git diff 0ca392b52b484bc3d7463586719d111c3f8bcda9...004b10997d9eaef716aed611838ed8830301fc84 -- eq-copilot/plugin/CMakeLists.txt eq-copilot/plugin/core/analysis/FeatureEngine.h eq-copilot/plugin/core/analysis/featureengine/ eq-copilot/plugin/core/ipc/ControlClient.cpp eq-copilot/plugin/core/ipc/controlclient/ eq-copilot/plugin/src/PluginProcessor.cpp eq-copilot/plugin/src/prozessor/ eq-copilot/plugin/tests/IpcTestMain.cpp tools/beweise.ps1 tools/eq-copilot/pruefe_kern_identitaetsfrei.py tools/plan/gesundheit.py`
mit BASIS = 0ca392b52b484bc3d7463586719d111c3f8bcda9 und ZIEL = 004b10997d9eaef716aed611838ed8830301fc84 (HEAD; er muss vor und nach deinem Lauf identisch sein).
Alles andere, was seit BASIS im Repo geändert wurde, gehört zu anderen Tickets und ist hier nicht Prüfgegenstand.
Prüfstufe T2, verlangt vom Plan für S25d.

GATE-TEXT, wörtlich und vollständig (docs/plan/plan.json, Schritt S25d, Ticket NAK-225):
„ANGELEGT 08.09.2026 (Pflegeschritt 2 nach G5, Register NAK-225, vor S26–28, damit der EQ-Bau auf geteilten Dateien beginnt). Gemessen am 08.09.2026: `eq-copilot/plugin/src/PluginProcessor.cpp` 4 161 Zeilen, `core/analysis/FeatureEngine.h` 3 969, `core/ipc/ControlClient.cpp` 2 307, `src/SourcesModel.cpp` 1 797. INHALT: je Datei nach Fachbereich teilen (Prozessor: Hostbrücke, State, IPC-Anbindung, Analyse-Anstoß; FeatureEngine: Messkern je Merkmalsfamilie; ControlClient: Verbindung, Nachrichten, Fehlercodes), Identität, Audio-Thread und State-Pfade unberührt. BEWEIS: Nulltest bitidentisch, Passthrough sampleidentisch, Kanon vorher = nachher grün, `EqCopShot`-Sichtprüfung unverändert, Beweismanifest mit den Beinzahlen. GRENZE: keine Legacy-Umbenennung (NAK-30), keine neue Optik, keine Funktionsänderung.“

Mehr verlangt das Gate nicht. Das Ticket hat keine Verhaltensmatrix; die Riegelkarte in docs/beweise/NAK-225.md §3 (R1 bis R10) ist die Referenz der Zusagen, der Schnittplan in §2 (§2.3 bis §2.5 Fachbereiche, §2.6 Sichtbarkeits- und Bindungsänderungen einzeln, §2.7 CMake und A14) die Referenz dessen, was wohin verschoben wurde. Entscheide des Dirigenten im Ticketauftrag (docs/beweise/roh/NAK-225-auftrag.txt), keine Lücken des Bauers: `SourcesModel.cpp`, `PluginEditor.cpp` und `PluginProcessor.h` sind nicht Teil des Tickets (unter der Grenze 2 000); keine lange Funktion wird zerlegt (NAK-235); die Wurzeldateien behalten ihre Namen, Teile werden herausgelöst; `FeatureEngine.h` bleibt der einzige Kopf, den Einbinder kennen, Memberdefinitionen wandern als `inline` in Teilköpfe; die 14 CMake-Literale werden durch eine Quellliste ersetzt.
Die Riegel/Tests, die es messen: R1 Kanon `tools/beweise.ps1` (62 Beine, Prüfungszahlen je Bein in docs/beweise/roh/NAK-225-beinzahlen.txt); R2 A1 `EqCopNullTest`, A16 `EqCopProbeeqNullTest`, B3b `EqCopHostProbeTest`; R3 B15 `EqCopShot --sonde012-suite` (25 Bilder, SHA-256 in docs/beweise/roh/NAK-225-shots-vorher.txt, -vorher-b.txt, -nachher.txt); R4 Zielmenge (docs/beweise/roh/NAK-225-ziele-*.txt); R5 Diff-Riegel (docs/beweise/roh/NAK-225-diffriegel.txt); R6 Historie; R7 Dateiköpfe (docs/beweise/roh/NAK-225-dateikoepfe.txt); R8 Audio-Pfad byteweise (docs/beweise/roh/NAK-225-audiopfad.txt); R9 `tools/plan/gesundheit.py` (über 2 000 = 0, Pflegeticket-Liste leer, Ratsche 32 → 30, Selbsttest 121); R10 Bau je Commit, MSVC-Warnungen (docs/beweise/roh/NAK-225-warnungen-*.txt); A14 `tools/eq-copilot/pruefe_kern_identitaetsfrei.py` (Objektliste ↔ `NAKAMA_KERN_QUELLEN`); A5 (Register `eq-copilot/schemas/v3/metriken-v1.json` bindet Datei UND Wert — der erste Vertragsschnitt riss A5, behoben in Commit f4d497b3, Manifest §6 h2). Jüngster Kanon: GRÜN 62/62, Exit 0, auf `f4d497b3` (docs/beweise/roh/NAK-225-f4d497b.md); der Commit danach berührt nur docs/. Davor ROT 1/62 auf `d9e2b6db` (A5), Rohausgabe docs/beweise/roh/NAK-225-d9e2b6d.md.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: verletzt einen Satz des Gate-Textes oben, eine Zeile der Riegelkarte, einen Test oder eine Invariante aus CLAUDE.md (Identität eingefroren, Audio echtzeitfest — keine Sperren, Allokationen, Datei-, Pipe-, Netzzugriffe, kein Logging im Audio-Thread —, Nulltest bitidentisch, State verlustfrei). Reproduzierbar, mit Kommando; je Defekt: Datei:Zeile und der Satz, der bricht. Für dieses Ticket heißt DEFEKT vor allem: eine Verschiebung, die Verhalten ändert (anderer Wert, andere Auswertungsreihenfolge, andere Deklarationsreihenfolge oder Overload-Auflösung in `FeatureEngine.h`, geänderte Bindung mit ODR-Folge, eine Entität, die je Übersetzungseinheit eine eigene Kopie mit eigenem Zustand bekommt, verlorener oder veränderter Test), eine Regel- oder Schwellenänderung, ein Kopf, der lügt, eine gebrochene Historie, ein Ziel, das nicht mehr dieselbe Quellmenge übersetzt, oder ein Riegel (A5, A14, A29-Muster), der nach dem Schnitt still weniger misst als vorher.
- LÜCKE: Gate, Riegelkarte und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, keine Erwähnung als NEEDS_WORK-Grund):
1. docs/** — Manifeste, Register, Planstand, Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände, Kommentar-Wortlaute. Manifeste sind Kontext, kein Prüfgegenstand.
2. Im Register datierte Härtungen und ausgegliederte Punkte: NAK-235 (lange Funktionen), NAK-236 (Kommentar-Bezeichner außerhalb der Ticketdateien; NB-2 `STATUS_STACK_OVERFLOW` ist ein Fremdname), NAK-237, NAK-238 (`SourcesModel.cpp`, `PluginEditor.cpp`, `PluginProcessor.h`), NAK-240, NAK-241. Die Nebenbefunde des Manifests §5 (NB-1 Register bindet Ort, NB-4 Kommentare in anderen Dateien zeigen auf verschobene Zeilen, NB-5 Messkante 215 → 210, NB-6 MSVC-Kopfwarnung fünffach statt einfach) sind benannt und gehen ins Register; sie sind nur dann ein Befund, wenn du zeigst, dass sie eine Zusage brechen. Ein Befund gegen ein Prüfskript, einen Riegel oder den Runner, der absichtliche Sabotage in repo-eigenen Quellen voraussetzt, ist HÄRTUNG — das Bedrohungsmodell der Riegel ist die versehentliche Regression, nicht der Angreifer mit Schreibrecht.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. §2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lückenlos“) gilt für Gate-Audits, nicht für diesen Ticketreview. Kosmetik, Stil, optionale Härtung, theoretische Randfälle, Ticketfremdes: nein.

WAS DU TUST:
1. Lies den Diff im Prüfbereich. Für die Verschiebungen ist der Vergleich alter und neuer Funktionstexte das Werkzeug (`git show 0ca392b5:eq-copilot/plugin/src/PluginProcessor.cpp` usw. gegen die neuen Teile; `git diff -M --color-moved=plain` hilft bei Verschiebungen innerhalb des Prüfbereichs); der Schnittplan §2.3 bis §2.5 sagt, welches Item wohin gehört, §2.6 nennt jede Sichtbarkeits- und Bindungsänderung.
2. Prüfe am Zielstand, ob Tests und Riegel messen, was Gate und Riegelkarte verlangen: (1) Ist jede Verschiebung verhaltensneutral — Funktionskörper, Konstanten, Standardargumente, `noexcept`/`constexpr`, Auswertungs- und Deklarationsreihenfolge textgleich bis auf `#include`, Sichtbarkeit, Bindung und Dateikopf? Prüfe besonders die Bindungsänderungen aus §2.6: die `inline`-Helfer in `src/prozessor/Intern.h` und `core/ipc/controlclient/Intern.h` (eine Entität, kein Zustand je Übersetzungseinheit — etwa `static`-Lokale, Zähler, Mutexe, `thread_local`), die aus der Klasse gezogenen `inline`-Definitionen der 49 privaten `FeatureEngine`-Member (Signatur, Template-/Const-Qualifizierung, `static`), `lraBinMitte`, die Reihenfolge Vertrag → Klasse → Teilköpfe. Nenne jedes Item, das sich inhaltlich unterscheidet. (2) Audio-Thread: `processBlock` und alles, was es innerhalb des Prozessors ruft, ist textgleich und liegt weiterhin ohne neue Indirektion in der Wurzel; keine Allokation, Sperre oder I/O neu auf dem Pfad. (3) CMake: übersetzt jedes Ziel, das vorher `src/PluginProcessor.cpp` übersetzte, jetzt genau `NAKAMA_PROZESSOR_QUELLEN`, und sind alle `controlclient/`-Teile in `NAKAMA_KERN_QUELLEN`; misst A14 nach der Objektlisten-Änderung noch, dass Archiv und Quellliste sich decken (kein stilles Überspringen)? (4) Testbestand: kein Test still weggefallen; die zwei Zeilen in `tests/IpcTestMain.cpp` sind eine reine Pfadänderung einer quelltextlesenden Wache, die weiterhin dasselbe Muster an derselben Logik prüft. (5) `tools/plan/gesundheit.py` und die A32-Zeile in `tools/beweise.ps1`: nur Pflegeticket-Liste (jetzt leer, gegen stilles Veralten gesichert) und Ratsche 32 → 30, keine neue Schwelle, keine geänderte Messlogik jenseits der leeren Liste, Selbsttest 121 grün; die Runner-Behauptung nennt Zahlen, die der Lauf misst. (6) Identität und State: `eq-copilot/identity/` unberührt, Save/Load-Pfade in `prozessor/State.cpp` textgleich, `EqCopIdentityTest` und `EqCopStateMigrationTest` im Kanon grün. Du darfst lesen und lesende Kommandos fahren (`git`, `py -3.13 tools/plan/gesundheit.py`, `py -3.13 tools/eq-copilot/pruefe_kern_identitaetsfrei.py` falls das Archiv im Baum liegt); du schreibst nichts und baust nichts.
3. Prüfe, ob der Änderungssatz seit BASIS etwas am Produktverhalten gebrochen hat (Identität, Passthrough/Nulltest, State, Audio-Thread) — nenne, was du selbst nachgemessen und was du nur gelesen hast.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Riegelkarte bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
