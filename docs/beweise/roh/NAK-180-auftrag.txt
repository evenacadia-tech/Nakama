NAK-180 — Nacharbeit zum Gate G4 (Defekt D-01): Der bestätigte Resync hat im Produkt keinen Auslöser. Ticketauftrag des Dirigenten (2026-09-05), Basis-SHA {{BASIS}}.

DU BIST der frische Opus-Bauer. Repo C:\Users\phili\Projekte\Nakama, Branch master, Basis {{BASIS}} (git rev-parse HEAD beim Start; muss gleich sein, sonst anhalten). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach origin master als eigener Schritt nach `git pull --rebase` bei sauberem Worktree. Keine Neben-Refactors, keine Legacy-Umbenennung. Vor jedem Commit `tools/dirigent/pruefliste.md` abhaken und im Manifest nennen, wo jede Zeile gemessen wurde.

TICKETGRENZE — genau dieser Defekt, mit seiner Ursache, in allen betroffenen Sprachen:
Gate G4 (docs/beweise/G4.md §1, D-01) hat an der Quelle bestätigt: `eq-copilot/plugin/core/ipc/ControlClient.cpp:404-411` schreibt `intervention_state_unknown` NUR wenn es wahr ist; `broker/src/coordinator/befehl.rs:550-575` ruft `resync_bestaetigen` NUR bei ausdrücklichem `Some(false)` im ersten Heartbeat eines Links ohne Ereignissequenz; `broker/src/coordinator/link.rs:444-453` setzt in `control_ende` `taint.unknown = true`, solange die Sitzung noch Clients hat; `broker/src/coordinator/intervention.rs:370-399` (`neutral_resync`) ist der einzige Löscher. Folge: nach jedem Control-Reconnect von Gen (`PluginProcessor.cpp:219` in `prepareToPlay`, `setzeBindung`, Broker-Neustart, Pipeabbruch) bleibt `unknown` für die Sitzung sticky, `interventionssicht().starke_evidenz_erlaubt` ist dauerhaft false, jeder `evidence_dispatch` wird als `evidence_gesperrt` gezählt. Gebrochen: Matrix M-60/M-61 (docs/beweise/SONDE-013.md §3.7) und Exit-Gate §58 Satz 3 („der reguläre Pfad nullt danach wieder"). Richtung fail-closed; die Reparatur darf KEINE Selbstheilung einführen (§34.2: der Resync bleibt eine ausdrückliche, bestätigte Lebenszyklusoperation).

REGELN, die den Defekt schließen (Technikentscheid des Dirigenten, nicht verhandelbar; Feinheiten in der Matrix ausarbeiten):
R1 — Der bestätigte Neuaufbau reist ausdrücklich. Stellt der Prozessor beim Aufbau des Control-Links Neutralität fest (Ring leer und kein Marker hörbar, heute `PluginProcessor.cpp:1199-1210` → `interventionsRing.resync()`), dann trägt GENAU der erste Heartbeat dieses neuen Links `"intervention_state_unknown": false` ausdrücklich. Jeder spätere Heartbeat lässt das Feld weiter weg, wenn der Zustand bekannt ist (M-39: „nur gesetzt reist es"), und setzt `true` nur bei Überlauf/Sendefehler wie heute. Stellt der Prozessor beim Aufbau KEINE Neutralität fest, fehlt das Feld, und das Sticky-Bit bleibt bis zum nächsten bestätigten Neuaufbau. Die Brokerseite (`befehl.rs:572`, nur erster Heartbeat eines Links ohne Ereignissequenz) bleibt der Riegel und wird nicht gelockert. Das Flag „erster Heartbeat nach bestätigtem Neuaufbau" ist Zustand des ControlClients (gesetzt vom Prozessor im Aufbau, verbraucht vom ersten gesendeten Heartbeat, nie aus dem Audiothread) — kein neues Wire-Feld, kein Schemaeingriff (`heartbeat.intervention_state_unknown` ist bereits optional bool).
R2 — Die Naht wird ende-zu-ende gemessen, cross-language. (a) Neue gültige Fixture `eq-copilot/fixtures/v3/gueltig/heartbeat-resync-erster.json` (falls der Erzeuger `tools/eq-copilot/erzeuge_v3_fixtures.py` Fixtures führt: dort erzeugen, bytegleich), die exakt das JSON ist, das `heartbeatAlsJson` im Fall R1 erzeugt; der Plugin-Test prüft die Serialisierung bytegleich gegen die Fixture, der Broker-Test (`broker/tests/sonde013_verdrahtung.rs`) liest DIESELBE Fixture, fährt hello → control_ende (Sitzung behält einen Sonden-Client) → neuer Link → ersten Heartbeat aus der Fixture durch `p0_json_mit_minor` und misst `starke_evidenz_erlaubt == true`, `unknown == false`, danach `evidence_dispatch` angenommen; zweiter Heartbeat ohne Feld ändert nichts, ein späterer Heartbeat mit `false` löscht NICHT (M-61). (b) `EqCopSonde013PassageStateTest::r01Resync` (`Sonde013PassageStateTest.cpp:1178`, Register NAK-156 erste Hälfte) fährt den Handschlag über den echten ControlClient statt `v3LinkFuerTest`. (c) `EqCopMarkierungTest` B04 (`MarkierungTestMain.cpp:744-751`) misst beide Zusagen: Steady-State lässt das Feld weg, erster Heartbeat nach bestätigtem Neuaufbau trägt `false`. Rotbeweis je Test: R1 zurückgenommen → Test fällt (Rohdatei unter docs/beweise/roh/NAK-180-rot-*.txt).
R3 — `prepareToPlay` bei hörbarem Marker: `markierung.vorbereiten` (`HoerMarkierung.h:252-264`) setzt `warHoerbar`/`fade` zurück, das `endete`-Ereignis entfällt. Das ist heute brokerseitig durch `control_ende` gedeckt (Intervall des sterbenden Links wird entfernt). In der Matrix als Zeile festhalten und messen: nach prepareToPlay mit hörbarem Marker → Reconnect → erster Heartbeat → Sitzung nullt (aktive 0, tail 0, unknown false). Kein zusätzlicher Mechanismus, wenn die Messung hält.

SPEZIFIKATION VOR CODE: Zuerst die Verhaltensmatrix in `docs/beweise/NAK-180.md` (neu; Kopf mit Gate-Bezug G4/D-01, Basis-SHA, Prüfstufe T3-Nacharbeit): Zustände × Ereignisse × Zusage, Callback-Reihenfolge, Fristen, je Zeile der Test, der sie misst — mindestens: erster Heartbeat neutral / nicht neutral, zweiter Heartbeat, späteres `false`, Überlauf nach Resync, Reconnect während Ausfade, Broker-Neustart, Sonde-Reconnect (keine Main-Neutralitätsbehauptung), Ring nicht leer beim Aufbau. Danach anhalten und den Dirigenten NICHT fragen — die Matrixprüfung fährt der Dirigent selbst mit einem lesenden Codex-Thread; du erkennst die Freigabe daran, dass im Manifest ein Abschnitt „Matrixprüfung 1: PASS" mit Datum steht. Bis dahin baust du nichts. (Der Dirigent trägt den Abschnitt ein; prüfe das Manifest alle paar Minuten per Read.)

BAU danach: Plugin (ControlClient.h/.cpp, PluginProcessor.cpp), Fixture + Erzeuger, Tests (Plugin und Rust), Manifestabschnitte. Beine fahren: `EqCopMarkierungTest`, `EqCopSonde013PassageStateTest`, `EqCopSonde013EventWireTest`, `EqCopSonde013InterventionRingTest`, `EqCopNullTest`, `EqCopProbeeqNullTest`, `cargo test` für `sonde013_verdrahtung`, `sonde013_taint`, `coordinator_model`, A5 `pruefe_v3_vertrag.py` und A8 `erzeuge_v3_fixtures.py --pruefen` (wenn die Fixture hinzukommt). Rohausgaben nach `docs/beweise/roh/NAK-180-<sha>.txt`. Nach dem Bau der volle Kanon, abgekoppelt (nie als sitzungsgebundener Hintergrundbefehl):
  $log = Join-Path $env:TEMP 'nakama-nak180-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-180.md -Anhaengen -Titel NAK-180 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
Fertig, wenn die letzte Logzeile mit EXIT= beginnt; kein Stand mit Laufstatus NOT RUN wird übergeben. Committen mit Pathspec (Produkt, Tests, Fixtures, docs/beweise/NAK-180.md, docs/beweise/roh/NAK-180-*), pushen. Kein Anfassen von docs/beweise/G4.md, docs/offene-punkte.md, docs/PLAN-STAND.md — das zieht der Dirigent nach.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen (Zahlenränder, NaN/Inf, stale Closures, Save/Load-Symmetrie, irreführende UI- und Kommentartexte), Audio-Thread-Invarianten (kein Lock, keine Allokation, kein Zugriff aus dem Callback auf das neue Flag), Beziehungen mitprüfen (verbinden↔trennen, resync↔Überlauf).
