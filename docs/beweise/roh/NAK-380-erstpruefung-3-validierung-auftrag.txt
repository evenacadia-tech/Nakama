NAK-380 — Validierung der Erstprüfung 3 (Etappe 3, LRA mit 10 Hz und M1-Spanne) an der Quelle. Auftrag des Dirigenten, 25.09.2026.

ROLLE. Du bist ein frischer, lesender Validierer (Opus 5.5, Effort max; nicht der Bauer der Etappe 3, nicht der Nacharbeiter, nicht der Prüfer). Repo C:\Users\phili\Projekte\Nakama, Branch master, HEAD 8308a3d80146f1119203127e927c1f746fcd4ec9. Du prüfst die Befunde des Prüfberichts an der Quelle und lieferst dem Dirigenten die Entscheidungsgrundlage. Du reparierst nichts und änderst keine Repo-Datei.

GRENZE. Nur lesen (Read, Grep, Glob; git nur lesend: status, rev-parse, log, show, diff, grep, ls-files); kein Bau, kein Testlauf, kein cargo, kein Python-Lauf, kein FL Studio; kein git add, commit, push, stash, reset, checkout. Während deines Laufs fährt der Dirigent abgekoppelt den vollen Kanon: Bauartefakte, eine uncommittete Änderung an eq-copilot/install/nakama-installer-v1.json, neue Rohdateien unter docs/beweise/roh/ und ein angehängter Abschnitt in docs/beweise/NAK-380.md können erscheinen; sie sind kein Gegenstand, und du fasst sie nicht an. Fremde Dateien (briefing-hub/, nimbalyst-local/, .claude/settings.local.json) fasst du nie an. Deinen Bericht schreibst du als EINZIGE Schreibaktion in die Datei C:\Users\phili\AppData\Local\Temp\claude\C--Users-phili-Projekte-Nakama\77b31923-3943-4253-8a2f-b849f78c3e63\scratchpad\NAK-380-erstpruefung-3-validierung.md (UTF-8, LF); nichts anderes wird geschrieben. Miss git rev-parse HEAD zu Beginn und am Ende und nenne beides im Kopf.

GEGENSTAND. Der Prüfbericht der Erstprüfung 3 (Codex gpt-6-astra xhigh, Thread 01a0d8e7-d698-7352-84e9-538e3e8fe086, Vorlage A, URTEIL NEEDS_WORK, 4 Defekte, 0 Lücken) über den Änderungssatz git diff b04dfb7c...8308a3d8 -- broker eq-copilot tools (ohne docs). Maßstab: docs/beweise/NAK-380.md §2 (R-380-4, R-380-6, R-380-7, R-380-8), §5.3 (Zusagen), §6.2 (Zeilen M-21 bis M-41, Etikettendefinition im §6-Kopf; M-23 in der Fassung §28), §8.0 (T-380-3, T-380-4, T-380-10, T-380-11), §8.1, §8.3, §19.2, §19.3, §24 (Lehren der Etappe 2: D1 Vorbedingung exakt, D2 Zusage je Element, D3 Bits/Flags am Feld, D4 formgültige Mutationen, D5 je Vertragsstelle eine Prüfung), §28.1 (R-380-11); die Präzisierungen im Etappenauftrag docs/beweise/roh/NAK-380-etappe-3-auftrag.txt; der Bauabschnitt §27 und §29 sind Selbstbericht. Der Prüfauftrag lag als Vorlage A vor (docs/beweise/roh/NAK-380-erstpruefung-3-auftrag.txt liegt noch nicht im Repo; die Ausschlüsse: docs/** kein Gegenstand, NAK-402/398/399/400/177/316/199/401/30, A-1 bis A-8 und B-1 bis B-7 entschieden, Ersatzmutationen Technikentscheid sofern sie die Zusagezeile tragen, Sabotage gegen Prüfwerkzeuge = Härtung, git diff --check und cargo fmt --check kein Befund).

DIE BEFUNDE, wörtlich aus dem Prüfbericht:
D1 — „Prüfe jedes unbelegte Perzentil einzeln auf NaN — eq-copilot/plugin/tests/GoldenTestMain.cpp:610-613. DEFEKT gegen M-38 (‚NaN, nie 0‘): Bei einem leeren Teilblockhistogramm meldet diese Prüfung erst dann einen Fehler, wenn alle drei Perzentile endlich sind. Beispielsweise wird P95 = 0 bei weiterhin NaN in P10/P50 akzeptiert. Eine versehentliche Initialisierung von perzentilP95 mit 0 statt NaN würde den Fall EqCopGoldenTest.exe eq-copilot/fixtures --nak380 M-38 deshalb nicht rot machen (statisch nachvollzogen, nicht ausgeführt). Für unbelegte Bänder muss jedes der drei Felder ausdrücklich NaN sein.“
D2 — „Stelle die vorgeschriebenen M-38-Läufe vollständig her — GoldenTestMain.cpp:571-572. DEFEKT gegen die exakte Vorbedingung von M-38: Gefordert sind W4 über 60 Sekunden und ein zweiter Lauf mit Reset bei 30 Sekunden. Tatsächlich läuft dieser Test nur 30 Sekunden, setzt zurück und speist anschließend eine Sekunde; außerdem wird floor(n/8) nach dem Neustart nicht erneut geprüft. Damit fehlt der zugesagte vollständige Start/Stop-Vergleich, nicht lediglich zusätzliche Härtung.“
D3 — „Prüfe das Gültigkeitsflag auch vor dem ersten Teilblock — GoldenTestMain.cpp:620-623. DEFEKT gegen M-38: ‚perzentileGueltig erst mit mindestens einem Teilblock‘ wird nur auf der bereits gültigen Seite geprüft. Nach der eingespeisten Sekunde besitzen Mitten- und Höhenstufe schon vollständige Teilblöcke; irgendeinBlock ist daher immer true. Selbst ein bedingungslos gesetztes Produktflag würde EqCopGoldenTest.exe eq-copilot/fixtures --nak380 M-38 bestehen (statischer Gegenbeleg, nicht ausgeführt). Ergänze eine Auswertung ohne vollständigen Teilblock, beispielsweise nach 0,1 Sekunden, und fordere dort ausdrücklich perzentileGueltig == false.“
D4 — „Erfasse alle kName-Werte eines Registereintrags — tools/eq-copilot/pruefe_v3_vertrag.py:2389-2393. DEFEKT gegen M-30: Der vorhandene Registereintrag enthält kEreignisPlaetze = 64, kVerteilungPlaetze = 64, aber das verankerte re.match prüft ausschließlich das erste Paar. Eine versehentliche Abweichung beim zweiten Wert bleibt somit unbemerkt; dessen inline constexpr-Definition fehlt zusätzlich im ausschließlich auf static constexpr begrenzten Codeindex. Gegenprobe, nicht ausgeführt: Den zweiten Registerwert auf 65 setzen und py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py --nak380 M-30 aufrufen; keine der vier Prüfungen erfasst diese Abweichung. Beide Paare müssen gegen ihre Codekonstanten geprüft werden.“

TEIL 1 — je Befund D1 bis D4:
a) Die Matrixzeile oder Zusage, wörtlich mit Zeile in docs/beweise/NAK-380.md (M-38 vollständig: Vorbedingung, Ereignis, Zusage, Test, Rotbeweis, Etikett; M-30 vollständig; die Regel „je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt“; §24 Lehren D1 bis D3; die Präzisierung „Vorbedingung exakt“ und „Zusage je Element“ im Etappenauftrag).
b) Nachmessung an der Quelle, Datei:Zeile am HEAD wörtlich zitiert: D1 — die NaN-Prüfung im Fall nak380M38 (GoldenTestMain.cpp um 600-615) und die Initialisierung der Perzentile in AnalyseEngine.cpp berechnePerzentile (fill mit NaN; welche Felder); ist eine Prüfung „alle drei endlich“ oder „je Feld NaN“? D2 — die Läufe im Fall nak380M38 (Dauer, Reset, Speisung; um 560-600) gegen die Vorbedingung von M-38 in §6.2 (W4 60 s; zweiter Lauf mit Reset bei 30 s) und die Zusage floor(n/8) nach dem Neustart; welche Prüfungen bestehen, welche fehlen. D3 — die perzentileGueltig-Prüfung (um 616-625): wird der Zustand vor dem ersten vollständigen Teilblock (perzentileGueltig == false) geprüft? Wie setzt das Produkt perzentileGueltig (AnalyseEngine.cpp berechnePerzentile, irgendeins)? D4 — pruefe_v3_vertrag.py pruefe_nak380_etappe_3, Teil M-30 (um 2380-2400): das re.match und der Codeindex (_konstanten_aus_kern: welche Muster, static constexpr gegen inline constexpr); der Registereintrag in metriken-v1.json (nicht_gefuehrt oder Vergleichbare Stelle mit „kEreignisPlaetze = 64, kVerteilungPlaetze = 64“, Zeile); die Definitionen von kEreignisPlaetze und kVerteilungPlaetze in FeatureEngine.h (inline constexpr?).
c) Welcher Satz bricht wirklich (Matrixzeile, Zusage, Regel, Lehre aus §24, Präzisierung, Invariante NaN-Ehrlichkeit) — wörtlich mit Quelle. Bricht keiner, steht das da.
d) Urteil: BESTÄTIGT, PRÄZISIERT oder WIDERLEGT, und deine Einordnung: DEFEKT, LÜCKE oder HÄRTUNG. Prüfe, ob der Befund unter einen Ausschluss fällt.
e) Die schließende Regel in einem Satz und die Folgestellen (Test, Rohdatei, Prüfskript, Matrixzeile).

TEIL 2 — Gemeinsame Ursache: Teilen D1, D2, D3 die Wurzel „der M-38-Test bildet die Vorbedingung, die Zusage je Feld und die Flagzusage der Matrixzeile nicht vollständig ab“ (dieselbe Ursache A wie in §24 für M-01)? Teilt D4 die Wurzel „je Vertragsstelle/je Wert eine eigene Prüfung“ (Lehre D5)? Gibt es in M-21 bis M-41 weitere Stellen mit derselben Wurzel — prüfe mindestens M-21/M-22/M-29 (Vorbedingung 48 kHz, Block 512, Evidenzintervall 1 s über evidenzIntervallSetzen, Dauer, Saat), M-24 bis M-27 (Referenz T-380-3 je Fall, Normtoleranz und §5-Referenz beide geprüft), M-32 bis M-35 (Zusage je Band: „in jedem 1-Bin-Band“, Median über welche Menge; Toleranzfenster aus der Matrix), M-36 (Schwellen und Texte unverändert: wird der Text geprüft?), M-37 (Abdeckung auf Einzelsegmenten: je Band 221/221?), M-39 (Bytevergleich), M-40 (beide Rust-Konstanten, C++, Register), M-41; nur nennen, was du an der Quelle mit Datei:Zeile belegst; sonst „nicht geprüft“. Was muss ein Nacharbeitsauftrag deshalb als Ursache statt als Punktkorrektur tragen?

LIEFERUNG (in die genannte Datei). Berichtskopf (Ticket, HEAD Beginn/Ende, Datum, Modell, Gegenstand, Prüfform), dann Teil 1 je Befund D1 bis D4, dann Teil 2, dann eine Übersichtstabelle (Befund | Urteil | Einordnung | schließende Regel in einem Satz | Folgestellen), dann „Nicht geprüft“. Ein Urteil ohne zitierte Zeile gilt nicht; schreibe „nicht gefunden“, statt zu raten. In Markdown-Tabellenzellen ein Pipe-Zeichen als \| schreiben. Letzte Zeile der Datei und deiner Schlussmeldung: FERTIG Validierung Erstprüfung 3, <Zahl> bestätigt, <Zahl> präzisiert, <Zahl> widerlegt.
