URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).

# NAK-312 Etappe 3 — Erstprüfung (S25k, Prüfstufe T2)

Frische, lesende Prüfsession, 21.09.2026. Auftrag `docs/beweise/roh/NAK-312-etappe-3-erstpruefung-auftrag.txt`. Prüfbereich `git diff cb473944a12410e17f3205e231b32f35f92805ac...1a91d0d2b0eaefd3b2437561f3b2745e6f1fb228` über die acht Pfade des Auftrags (8 Dateien, +1650/-60), vollständig gelesen; die Änderungssätze c94b8dd9 (3a) und ad18d984 (3b) sind darin enthalten, geurteilt ist über den Zielstand. Mitgelesen am Zielstand, soweit der Diff sie berührt: `DspKern.cpp` (`blockrand`, `verarbeitePfad`, `verarbeiteBank`), `DspProgramm.cpp` (`baueProgramm`, `rampenKompatibel`), `NakamaTransaktion.cpp` (`fuehreAus` S8, `publiziereWirksam`, `baueBericht`), `SondeProcessor.cpp` (`reset`, `gestusAbschliessen`, `zelleAusHost`) und JUCE (`juce_audio_plugin_client_VST3.cpp`, `juce_AudioProcessorParameter.cpp`, Bauartefakte unter `eq-copilot/build/_deps/juce-src/`). Referenz aus dem Manifest: §6.2, §6.3, §7.3, §9.2, §11.3, §14.4, §17, §21. Zeilenangaben gelten zum Zielstand; der Arbeitsbaum ist dort gleich ZIEL (`git diff 1a91d0d2 -- eq-copilot tools` leer).

## Befunde

DEFEKTE: keine.

**L-1 (LÜCKE) — read-only-Ladestart und Blockrand: ein Hostwert nach der Quittierung formt das Ausblenden des vorigen Standes.** `nurLesen` liest der Blockrand nicht; einen read-only-Stand erreicht er nur über die Ruhe des Committed-Pfades (`eq-copilot/plugin/dsp/DspKern.cpp:682-683`). Im ersten Block nach dem Laden ist die Bank des vorigen, eingeschalteten Standes aber noch aktiv: `uebernimmBlockrandHostwerte` läuft vor `blockrand (Pfad::committed)` (`DspKern.cpp:1148-1154`) und setzt das Ziel eines Hostwerts, dessen Zähler ungleich der Quittung ist (`eq-copilot/plugin/sonde/SondeProcessor.cpp:504-515`); erst danach nimmt der Kern die ENDE-Marke der neutralen Publikation, die Rampen behalten ihre Ziele (`DspKern.cpp:541-551`), und die Quellbank der Blende rechnet mit ihnen (`:1054-1063`). Gerechneter Ablauf: Aufbau wie 312/M-15 (eq an, Output-Trim +3 dB committet, eingeschwungen, Block 256), `setStateInformation` mit `fremdes-major-3.bin`, danach `hostSchreibt (kOutTrim, 24.0f)`, dann ein Block. Die Quelle der 256-Samples-Blende rampt von 10^(3/20) auf 10^(24/20); der Ausgang steigt mitten in der Blende auf rund +13,7 dB über den Eingang (Näherung für Output-Trim ohne Band), ohne den Hostwert höchstens +3 dB. Am Stand 3a (c94b8dd9, ohne Blockrand) sind beide Läufe gleich, weil `wirksam()` das Overlay unterdrückt und der Worker nichts publiziert. Läuft beim Laden ein Übergang, reicht das Fenster bis zu dessen Ende plus Blende (höchstens 512 Samples); danach ruht der Pfad, und der read-only-Stand bleibt audio-neutral. Kein DEFEKT: 312/M-15 nimmt die Referenz nach acht Blöcken (`eq-copilot/plugin/tests/TransactionTestMain.cpp:3938-3939`), seine Zusage beginnt am ruhenden Pfad, und es rechnet die Bank des vorigen, eingeschalteten Standes, nicht der read-only-Stand; zu Hostwerten im Ausblenden nach einem read-only-Ladestart sagen Gate, Matrix und Entwurf nichts. Neu gegenüber 3a ist das Fenster trotzdem.

**L-2 (LÜCKE) — die Schreibmaske der Neutralprüfung hängt weiter an Workerübergängen; für Subnormale, NaN und ±Inf im Eingang sagt keine Zeile Echtzeit gleich Offline zu, und 312/M-20 bis M-22 sehen es nicht.** Geschrieben wird, solange ein Programmübergang läuft (`pfadSteht`, `DspKern.cpp:1426`), und seit A-19 bis zum Ende der längsten laufenden Rampe (`:1240-1242`, `:1437`); Übergänge entstehen nur aus dem Takt. Gerechnet an 312/M-20, neutraler Stand, Output-Trim, Block 64: A schreibt die Blöcke 10 bis 16 (die letzte Rampe endet mit Block 16), B nimmt vor Block 15 die Publikation aus dem Takt nach Block 13 und schreibt im Rampenübergang die Blöcke 15 bis 18. In den Blöcken 17 und 18 bleibt A unberührt, B schreibt die Identität zurück: für endliches Rauschen bitgleich (daher A/B 0), für NaN oder ±Inf im Eingang 0,0 statt des Rohwerts (Riegel R9, `:1273-1282`), für Subnormale unter DAZ und FTZ nicht bytegleich (Zusage 3 im Kopf, `DspKern.h:35-49`, nennt genau diese Werte). Offline und Echtzeit verschieben dieselbe Maske über die Arbeitsverteilung des Workers. Die Fälle fahren nur endliches Rauschen in [-0,8; 0,8) und sehen deshalb nur die Richtung „zu wenig geschrieben": ohne A-19 ließe A den Rampenblock 16 unbeschrieben, während B ihn im Übergang schreibt — nach meiner Rechnung rot und deckungsgleich mit den 126 Samples aus A-19; ein eigener Rotlauf für A-19 liegt nicht vor. Ob nach dem Rampenende weitergeschrieben wird, sieht kein Fall. B6 fährt den Kern ohne Hostwerte; dort ist `cRampenEnde` nach Bauart nie größer als `cUebergangsEnde` (Geprüft, d), die Neutral- und Schreibendefälle wachen also nur über den unveränderten Fall mit Programmübergang.

**H-1 (HÄRTUNG) — B7-Behauptung und 312/M-27 reichen weiter als die Messung.** `tools/beweise.ps1:728`: „ein Lauf unter der Taktsperre ohne Kontrolltakt ist fuer diese vier bitgleich zu einem mit Takt nach jedem Block - im neutralen Stand und mit Band, offline wie in Echtzeit, bei Blockgroesse 1, 64, 185, 240, 256, 512 und 4096 (...), bei einer Rampe ueber 200 Bloecke und ueber den Zaehlerueberlauf". Gemessen sind alle vier in beiden Ständen bei 1 bis 512 in Echtzeit (312/M-20, M-22) und offline bei 64 (M-21); 4096 (M-27, `TransactionTestMain.cpp:4461-4479`), die Rampe über 200 Blöcke (M-23) und der Zählerüberlauf (M-82) laufen nur auf Output-Trim im neutralen Stand. Die Matrixzeile M-27 sagt ebenfalls „für die vier abgedeckten Parameter". Nach dem Lesen führt dieselbe Tabelle alle vier durch denselben Weg (`SondeProcessor.cpp:501-516`, `DspKern.cpp:661-707`), und die Blockgröße wirkt nur über die Lage des Rampenendes — kein Verhaltensbefund, eine Wortfassung wie H-1 der Etappe 2. Genauer wäre: „fuer diese vier bei Blockgroesse 1 bis 512, offline bei 64; auf Output-Trim zusaetzlich bei 4096 (...), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf".

**H-2 (HÄRTUNG) — Kommentar der zweiten Prüfung aus R-312-11 stimmt für nicht endliche Hostwerte nicht.** `SondeProcessor.cpp:1629-1633`: „Ein nicht endlicher Hostwert wirkt dort nie (`zelleAusHost`); dann bleibt der geschriebene bestaetigte Wert." Einen Parameter mit neuem Gestus überspringt schon die erste Schleife (`:1619-1620`), geschrieben wird dort nichts; setzt der Host den nicht endlichen Wert erst nach dem Abgleich dieses Parameters, überschreibt er den geschriebenen Wert. In beiden Fällen bleibt der Regler beim Wert des Hosts, der Klang beim bestätigten Wert (E4-10) — dasselbe Verhältnis wie bei jedem nicht endlichen Hostwert außerhalb eines Ladestarts; nur der Kommentar verspricht mehr (R-312-14). Das Fenster misst keine Zeile; A-16 sagt das für die zweite Prüfung selbst.

## Geprüft

(a) W02: Die Quittierung steht im Zustandsschloss von `setStateInformation` nach `ladestart` (`SondeProcessor.cpp:1176-1185`) und vor dem Abgleich (`:1235`): erst das Flag per Tausch (`:1198`), dann je Parameter `hostEreignisGesehen` und der lokale Stand `quittiert` (`:1199-1204`), danach die Quittung des Blockrands mit `release` (`:1210-1212`), die bestätigten Blockwerte (`:1213`) und die Publikation mit dem quittierten Stand (`:1220-1222`). Jeder Vergleich ist Gleichheit — Takt `:1679`, Blockrand `:505` und `:512`, Abgleich `:1615`, Schiedsregel `DspKern.cpp:701` —, also überlaufsicher, gemessen über 0xFFFFFFFF → 0 in 312/M-19 und M-82. Ein Hostwert nach dem Lesepunkt der Quittierung hebt seinen Zähler über den quittierten Stand, setzt das Flag danach neu (der Takt schreibt ihn ins Overlay), ist am Blockrand ungleich der Quittung (Ziel) und bleibt im Abgleich stehen, die zweite Prüfung stellt den Regler auf ihn (312/M-11, M-78); ein Hostwert vor dem Lesepunkt gehört zum Stand vor dem Laden. A-15 hält auf x64: der Tausch ordnet die Leser, und `lock xadd` samt folgender Speicherung bleibt in Programmordnung; im C++-Speichermodell fehlte die Freigabe am Flag (`:1481`, relaxed nach dem Release-Inkrement), ebenso liest `neuerGestus` den Zähler relaxed vor `hostWert` (`:1615`, `:1639`) — auf dem Zielsystem ohne Wirkung, nach Ausschluss 3 kein Befund. `reset` ist unverändert und fasst die Mailbox nicht an (`:374-402`, 312/M-13); `prepareToPlay` publiziert mit `publikationsStandSetzen (false)` den wirksamen Zustand, dessen Overlay der Ladestart geleert hat (`:342-343`, `NakamaTransaktion.cpp:266-267`, 312/M-12). Speichern und Laden: `getStateInformation` ist unberührt, `nurLesenStand` ist Laufzeit ohne Statefeld (`NakamaTransaktion.h:347`), der Ladestart setzt r = r0 = Revision, die Quittierung berührt nur die Mailbox — bestätigter Zustand, Hash, Ring, Cursor und Revision bleiben die der Quelle (312/M-14). (b) read-only: der eigene Stand lädt mit `false` (`SondeProcessor.cpp:1178`), nur der neutrale Rückfall mit `zustand.nurLesen` (`:1181-1182`), gesetzt in beide Richtungen (`NakamaTransaktion.cpp:272`, 312/M-17); `wirksam()` legt dann kein Overlay auf (`:349`), `baueBericht` liest denselben Satz (`:868`). Originalbytes: `gehaltenerStand` gibt den read-only-`zustand` unverändert zurück (`SondeProcessor.cpp:1128-1141`), Transaktionen werden abgewiesen (`:1368-1375`), also kein Dirty (312/M-15). A-17: der Rücksprung läuft nach dem Loslassen des Schlosses (`:1711-1712`) durch `hostParameterAbgleichen` mit `AbgleichHerkunft`; der eigene Listener kehrt auf diesem Thread zurück (`:76-82`, `:1474`), kein Zähler steigt. JUCE reicht die Änderung von einem Nicht-Nachrichtenthread nur in den Parametercache (`juce_audio_plugin_client_VST3.cpp:1480-1483`, `:586-589`), und ein Hostecho mit gleichem Wert meldet nichts (`:843-850`) — keine Rückkopplung in die Mailbox. Im Audiothread entsteht keine Sperre; der Worker hält während der Meldung JUCEs `listenerLock` des Parameters (`juce_AudioProcessorParameter.cpp:111-121`), den der Audiothread bei Hostautomation ohnehin nimmt (T3-01-02, Etappe 5) — im read-only-Fall ein zusätzlicher Mitbewerber, kein neuer Sperrenort. Wie FL den Rücksprung bei laufender Automation wertet, ist ungemessen (A-17, Register NAK-346). Zum Ausblenden nach dem Laden: L-1. (c) Blockrand: `processBlock` liest je Platz einen Zähler mit `acquire`, vergleicht ihn mit dem audiothread-eigenen `blockStand` und lädt nur bei Ungleichheit die Quittung (`acquire`), den Hostwert und in `zelleAmBlockrand` den veröffentlichten bestätigten Wert (beide relaxed) samt konstanter Bereichsgrenzen (`SondeProcessor.cpp:500-516`, `:1566-1582`); die Übergabe sind 96 Byte auf dem Stapel. Der Kern schreibt nur audiothread-eigene Felder und einen relaxed-Zähler (`DspKern.cpp:661-694`); `setzeProgrammziel` liest `bankStand` der gerade genommenen Bank, den der Worker vor der Übergabe schreibt (`:431`) — gedeckt von derselben Release-/Acquire-Übergabe. Keine Sperre, keine Allokation, kein Warten; `hostEreignisOffen` berührt der Callback nicht (`git grep`: Tausch nur `:1198` und `:1671`). Die Zelle folgt `zelleAusHost` in Reihenfolge und Ausdruck (Ausnahme mit `convertTo0to1 ((float) bestaetigt)`, für Gleitkomma gleich `hostWertAus`; Endlichkeit; `jlimit` mit den Tabellengrenzen); alle vier sind Gleitkomma (`eq-copilot/plugin/state/NakamaParameter.cpp:50-52`, `:80`), das Rampenziel rechnet wie `baueProgramm` (`DspProgramm.cpp:259-272`). Dass B dabei bitgleich bleibt, tragen zwei Stellen: der Kern entscheidet die M/S-Stufe am Rampenzustand, nicht an `msStufeAktiv` (`DspKern.cpp:970`), und `rampenKompatibel` kennt die vier nicht (`DspProgramm.cpp:218-241`) — die Programme aus dem Takt laufen als Rampenübergang mit gleichen Bandkoeffizienten. Abdeckung: Tabelle {1, 2, 3, 110} (`DspKern.h:229-234`), nur diese Zähler werden gelesen und auf Rampen abgebildet, Auto-Gain bleibt beim Programm. Speicherordnung: keine zerrissene Lesung (nur Atomics, `atomic<double>` lockfrei nach `DspKern.cpp:16-17`); ein Atomic je Parameter hält Vergleichswert und Rückgabe in einer Publikation (A-18); die Quittung ist eine eigene 64-Bit-Kopie, `kKeineQuittung` trifft keinen 32-Bit-Stand (A-20). Veraltete Lesungen gibt es nur in Wettläufen mit einem Ladestart: `bestaetigtBlock` wird relaxed nach der Quittung gespeichert (`SondeProcessor.cpp:1210-1213`), ein Block kann also die neue Quittung mit dem alten bestätigten Wert sehen (Nanosekunden, trifft nur einen nicht endlichen oder gleichen Hostwert); und die Voraussetzung von E-312-6 Punkt 4 („ein ungleicher Publikationsstand ist immer der ältere") gilt nur, solange Hostwerte im Audiothread eintreffen (VST3-Automation in `process`, `juce_audio_plugin_client_VST3.cpp:3684-3687`). Kommt ein Hostwert von einem anderen Thread (JUCE bei stehendem Transport, `:980-993`) und endet ein ganzer Ladestart zwischen der Zählerlesung (`SondeProcessor.cpp:504`) und `uebernehmeBereiten` (`DspKern.cpp:535`) desselben Blocks, behält `setzeProgrammziel` das Blockrandziel von vor dem Laden, und der nächste Block erkennt den Zähler als quittiert und setzt kein Ziel — der alte Wert bliebe bis zum nächsten Ereignis oder Programm. Beide Fenster verlangen einen im Nanosekunden- bis Mikrosekundenbereich stehenden oder angehaltenen Audiothread; nach Ausschluss 3 theoretische Randfälle, kein Befund. Zahlenränder: NaN und ±Inf (Teilfall zu 312/M-20, zwölf Ziele auf den bestätigten Wert), Überlauf (M-82, drei Ziele über 0xFFFFFFFF → 0 → 1), Blockgröße 1 (M-22, 32 Ziele). (d) A-19: `cRampenEnde` ist das Maximum der fünf Rampenreste am Stückbeginn (`DspKern.cpp:1240-1242`) und wirkt nur im engagierten Zweig `pfadSteht && hoertPfad` (`:1426-1438`). Ausgeschaltet und im Hard-Bypass kehrt `committedRuht` vorher zurück (`:1319-1328`) — Nulltest und Passthrough bleiben unberührt, kein Puffer, keine Latenz, kein Tail. Die Verlängerung schreibt nur, solange eine Rampe läuft, der Gain also nach `ruhtBei` (`DspKern.h:492`) nicht ruht, und endet an ihrem Ende. Ohne Hostwerte (B6 und jeder Aufrufer ohne `hostwerte`) ändern sich Rampenziele nur bei der Übernahme, zusammen mit einem Übergang gleicher Länge (`kFadeSamples` = `kRampeSamples` = 256, `DspProgramm.h:87`, `:91`); während eines Übergangs wird nichts übernommen (E-17), `beendeAudiohistorie` und `ruhe` setzen beide Reste auf 0. Dort ist `cRampenEnde` also höchstens `cUebergangsEnde`, und der Fall mit Programmübergang endet am selben Sample. Abdeckung: L-2. (e) Tests: unter der Taktsperre (`SondeProcessor.h:380-385`) steht der Worker am Zustandsschloss (`SondeProcessor.cpp:1665`), jeder Takt ist ausdrücklich, die Saaten sind fest (`juce::Random`, `std::mt19937 zufall (3261)`), `steady_clock` misst nur in 312/M-21 und 312/Kosten ohne Bestehensbedingung (A-27), der zweite Thread in 312/M-78 wird im Haken gejoint. 312/M-26 zählt `RtWache::sperren()` und `RtWache::allokationen()` ab Callback-Eintritt und B7s eigenen Zähler nur um `processBlock` (A-29); M-28, M-79 und M-82 lesen `blockrandZiele()`, M-28 zusätzlich `uebernahmen()`. Die Rotbeweise mutieren die Zusagezeilen (Blockrandzugriff, Endlichkeitsregel, Zählervergleich, Quittung, Ruhegrenzzähler). A-12: die Ersatzmutation nimmt den Mailboxwert in den geladenen Stand und bricht genau die Hashhälfte. A-24: der Blockzähler erreicht bei 480 Samples je Block die Ruhegrenze nie — rot an „genau zweimal". A-25: der Tausch am Blockrand hält Overlay und Epoche an, rot an 312/M-24; M-25 bleibt zu Recht grün (ohne Overlay keine Revision). A-13: ohne Wiederöffnung verdeckte das Zurücksetzen des Flags den alten Wert allein; mit ihr messen M-10, M-12 und M-19 die Quittierung selbst. A-14: in eigener Instanz bricht `0xFFFFFFFF` den Klang (der erste Takt belebt alle 112 Mailboxwerte, auch die durch den Abgleich veralteten, weil der Listener unter dem Herkunftstag nichts speichert) und `n + 1` schluckt den Gestus — beide an „ein Hostgestus nach dem Recall wirkt". Taktsperre, Ladehaken und Zählerrand-Setter stehen unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` (`SondeProcessor.h:289-483`, Mitglied `:601-604`, Aufruf `.cpp:1224-1227`); `DspKern::setzeBlockrandStandFuerTest` (`DspKern.h:265-267`) steht wie die übrigen B6-Zugänge im definefreien Kernkopf (`:287`, `:332`), einziger Aufrufer ist der Setter der Sonde (`SondeProcessor.h:401`, `git grep`). `NAK312_GEGENPROBE_OHNE_BLOCKRAND` definiert keine Bauquelle (`git grep`). (f) Jeder Satz der neuen B7-Behauptung hat einen gemessenen Fall (312/M-10 bis M-19, M-78; M-20 bis M-29, M-79 bis M-82); zu weit reicht nur der Geltungsbereich, H-1. Produktverhalten seit BASIS, selbst nachgemessen mit lesenden Git-Kommandos: `git diff --stat cb473944 1a91d0d2` ist leer über `eq-copilot/identity`, `schemas`, `fixtures`, `plugin/state/NakamaParameter.*`, `plugin/dsp/DspProgramm.*`, `plugin/dsp/DspBankPool.h`, `plugin/CMakeLists.txt`, `plugin/src`, `plugin/core`, `plugin/hostbridge` und `broker` und zeigt über `eq-copilot/plugin`, `tools` und `broker` genau die acht Dateien; `git diff --stat ad18d984 1a91d0d2 -- eq-copilot tools broker` zeigt nur `eq-copilot/install/nakama-installer-v1.json`, der Kanon an ad18d984 gilt also für den Code des Zielstands, und `dc2aaf1c..1a91d0d2` ist über Plugin, Werkzeuge und Broker leer (Laufzeit-Arm); alle acht Dateien liegen im Index als LF, ihre Blobs an BASIS ohne CR. Parameter-IDs, Reihenfolge und Bereiche kommen unverändert aus `parameter::tabelle()`, `baueParameterLayout` ist unberührt; kein Statefeld, keine Stateversion, `nurLesenStand` ist Laufzeit; im Audiothread neu sind nur Atomics und audiothread-eigener Zustand; Overlay, Epoche und Ruhegrenze bleiben beim Worker, der Blockrand erzeugt keine Revision. Nur gelesen: Kanon GRÜN 70/70 an ad18d984 (A1, A16, B2 OK; B6 389/0; B7 222/0, jede 312-Zeile ok, 312/M-20 A/B 0 bei B/C 894 bis 1551 und Mix im neutralen Stand bauartbedingt 0, M-26 0/0/0, M-80 4480/0, M-81 1187/0), Laufzeit-Arm dc2aaf1c mit Host-Nulltest BITIDENTISCH (v 0, g 1,0, 0 Abweichungen), die Rotbeweise M-14, M-20, M-24 und M-28 an Mutation und Urteil.

## Nicht geprüft

Nicht gefahren: kein Compiler, kein Test, kein Kanon, kein FL, kein Python; jedes Laufergebnis oben stammt aus den Rohdateien. L-1, L-2, die Rechnung zu A-19 und die Wettläufe unter (c) sind gerechnete Abläufe, nicht gefahren; die DAZ-Wirkung auf Subnormale in L-2 ist aus Zusage 3 des Kernkopfs übernommen, nicht gemessen. Die übrigen 21 Rotbeweisdateien und die beiden Gegenproben sind nicht einzeln gelesen, §7.0 und §7.1 des Manifests nur über ihre Anwendung in den Rohdateien. Die Wirkung des Reglerrücksprungs aus dem Worker in FL ist nicht messbar ohne Host (A-17). Nicht Gegenstand nach Auftrag: docs/**, die Etappen 4 bis 7, die Produktkarten, der JUCE-VST3-Wrapperpatch, T3-12-* und die datierten Registerhärtungen. Im Repo ist dieser Bericht die einzige neue Datei; nichts gestagt, committet oder gepusht, keine Lesekopien angelegt.

HEAD zu Beginn: `6dc9d708ffe48d134ab0281c80db113d2e3d37cd`. HEAD am Ende: `6dc9d708ffe48d134ab0281c80db113d2e3d37cd`. Beide mit `git rev-parse HEAD`; HEAD trägt über ZIEL hinaus nur den Prüfauftrag (`git diff --stat 1a91d0d2 HEAD`: eine Datei unter `docs/beweise/roh/`).

FERTIG Erstprüfung Etappe 3, PASS, 0 Defekte, 2 Lücken, 2 Härtungen.
