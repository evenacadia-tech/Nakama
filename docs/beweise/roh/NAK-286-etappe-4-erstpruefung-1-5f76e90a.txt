URTEIL: NEEDS_WORK — drei DEFEKTE. Geprüft wurden beide abgegrenzten Diffs einschließlich Selbsttests und Rotbelegen; Plugin-, Broker-, Identitäts- und Schemaquellen sind unverändert, ebenso beide HEADs (Nakama 5f76e90a, MCP 01f72386). Nicht geprüft wurde die Ausführung von Tests, Kanon oder Hostläufen: auftragsgemäß ausschließlich lesende Prüfung, keine Schreiboperationen.

Full review comments:

- [P2] Begrenze die Fenstererfassung tatsächlich auf fünf Sekunden — C:\Users\phili\Projekte\Nakama\tools\fl\szenario.py:802-807
  DEFEKT: Hängt FL während `PrintWindow`, blockiert dieser synchrone Aufruf den Szenarioprozess unbegrenzt. Die Dauerprüfung erfolgt erst nach seiner Rückkehr; auch `FahreSzenario` wartet ohne Prozessfrist. Damit wird die zugesagte „Frist 5 s“ aus [M-21](C:/Users/phili/Projekte/Nakama/docs/beweise/NAK-286.md#L534) nicht durchgesetzt. Lesender Nachweis: `rg -n 'umg.fenster|FENSTER_FRIST_S' tools/fl/szenario.py`, zusammen mit dem synchronen `PrintWindow` in `utils/fenster.py:179`. Die Erfassung benötigt eine abbrechbare Ausführung statt lediglich einer nachträglichen Dauerwertung.

- [P2] Trage Durchlauf und Kombinationszahl in jede U40-Rohzeile ein — C:\Users\phili\Projekte\Nakama\tools\fl\szenario.py:1323-1327
  DEFEKT: Die erzeugten Messzeilen enthalten weder `durchlauf` (`gemeinsam`/`getrennt`) noch die Zahl der getragenen Kombinationen. Diese Informationen erscheinen ausschließlich im Begleittext und werden nicht an den Zeilenschreiber weitergegeben. Das verletzt [P-16](C:/Users/phili/Projekte/Nakama/docs/beweise/NAK-286.md#L3199), das beide Angaben ausdrücklich je Rohzeile verlangt; insbesondere verlieren extrahierte Zeilen ihren Durchlaufkontext. Lesend nachvollziehbar mit `rg -n '^\| Stelle \|' docs/beweise/roh/NAK-286-laufzeit-34bdf159.md`: Alle drei Tabellen fehlen diese Spalten. Die Planmetadaten müssen bis zur einzelnen Messzeile weitergereicht werden.

- [P2] Weise im M-03-Rotfall den tatsächlichen Renderstart nach — C:\Users\phili\Projekte\Nakama\tools\fl\laufzeit.ps1:998-999
  DEFEKT: Bei der dokumentierten M-03-Mutation verhindern die verbleibende `$rest.Count`-Prüfung und der spätere Fremdprojektabbruch weiterhin den Renderstart; der Lauf endet mit Exit 0/UEBERSPRUNGEN. Rot werden hier stattdessen der erwartete Meldungswortlaut und die Existenz von `render.json`, obwohl kein Renderprozess gestartet wurde. Nachvollziehbar mit `Get-Content docs/beweise/roh/NAK-286-rot-M-03.txt` und `Rendere()`. Damit fehlt der von [M-03](C:/Users/phili/Projekte/Nakama/docs/beweise/NAK-286.md#L510) verlangte Nachweis „Attrappe mit fremdem Titel erreicht den Renderaufruf“: Der Negativaufbau muss die tatsächliche Renderfreigabe prüfen, nicht ersatzweise Meldung oder Statusdatei.