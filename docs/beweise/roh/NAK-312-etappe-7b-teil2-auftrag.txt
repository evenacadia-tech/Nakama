NAK-312 / Planschritt S25k — Etappe 7b, Teil 2 (Satz 2: Hostbypass der Probeeq, T3-01-09, U48 und U58): Bauauftrag des Dirigenten (2026-09-23)

Du bist ein frischer Opus-Worker (Bauer) im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master, `--permission-mode dontAsk`. Du baust GENAU den Satz 2 der Etappe 7b, nicht mehr. Ticketmanifest: docs/beweise/NAK-312.md (Kopf; Regeln §2 und §50.4; Zusagen wörtlich §45.2; Verhaltensmatrix §46 in der Fassung nach §51 und §57.4, Zeilen M-94 bis M-109 und M-132; Bauplan §47.3 — „Der Eintritt — drei geprüfte Wege" (Weg E1 gilt), „Die Blende und die Messung" (Weg K-B gilt), „Änderungen je Datei (E1 mit K-B)", „Wirkung je Frage des Auftrags"; Entscheide §49.2 (E-312-19 mit Nachtrag), Karten U48 und U58 in design/abnahmen/2026-09-21-fl-bypass-weich-messung-laeuft-u48.md und design/abnahmen/2026-09-23-fl-bypass-loslassen-filter-laufen-weiter-u58.md („Was damit festliegt" ist bindend); Bauerberichte der Teile 1 als Muster §54 (Form, Gegenproben, Rotbeweise, Selbstaudit) und §57; Dirigentenabschnitt §60 — er ist dein Auftrag im Wortlaut). Prüfliste: tools/dirigent/pruefliste.md (Arbeitsliste, keine Anforderungsquelle). Basis deiner Etappe ist HEAD beim Start (`git rev-parse HEAD`; nenne ihn im Manifest); Kanon vorher: geerbt vom Kanon der Nacharbeit 1 an 18eec7cf, solange `git diff --stat 18eec7cf HEAD -- eq-copilot broker tools` allein eq-copilot/install/nakama-installer-v1.json zeigt — miss das und halte es fest; ist mehr geändert, fährst du den Kanon vorher selbst.

Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

WAS GILT (Entscheide, nicht neu abwägen):
- Weg E1: `SondeProcessor` überschreibt `processBlockBypassed` (float); beide Eintritte rufen denselben Blockrumpf mit dem Wunsch „Hostbypass ja/nein", den allein der Eintritt setzt; `getBypassParameter()` liefert weiter `nullptr`, der Wrapper behält `byps` mit derselben ID und Persistenz; kein neuer persistenter Parameter, 112 Host-Parameter, Identität NAK-30 unverändert (M-102); State, Hash, Revision und Host-Dirty unberührt (M-103).
- Weg K-B (User-Wort U58 „Filter laufen weiter (Empfohlen)"): der Kern rechnet im Hostbypass weiter wie ohne ihn (Programme, Rampen, Bänke, ACKs, Taps); eine neue Ausgangsstufe hinter Taps und Hörmatrix am Ende von `verarbeiteStueck` blendet den Ausgang in 256 Samples linear auf den Eingang und schreibt danach keinen Sample mehr; zurück blendet sie ebenso; nach dem Austritt kein Einschwingen. Die Analyse misst unverändert `post_committed`. Die K-B-Hälften von M-95, M-96 und M-100 gelten; die K-A-Hälften entfallen (E-312-19, §60).
- Änderungen je Datei nach §47.3: eq-copilot/plugin/sonde/SondeProcessor.h und .cpp (zweiter Eintritt, gemeinsamer Rumpf, RtWache als erste Anweisung in beiden Eintritten, Wunsch an `dspKern->verarbeite`; Hostmailbox-Blockrand, Stempel, Analysekopie und `verarbeiteteSamples` in beiden gleich; prepareToPlay, releaseResources, reset, setNonRealtime unverändert), eq-copilot/plugin/dsp/DspKern.h und .cpp (`verarbeite` bekommt den Wunsch mit Vorgabe „nein", damit B6 und alle bestehenden Aufrufer bitgleich bleiben; neuer Zustand allein des Audiothreads für die Stufe; Blende mit dem Gewicht n/kFadeSamples, Umkehr vom Mischstand (Muster W-5), Schreibende am Blendenende (Muster W-1); hinter einem ruhenden Committed-Pfad übernimmt die Stufe ihr Ziel sofort; das harte Schalten beendet die Blende der Hörmatrix, nicht die der Stufe; Kopfkommentare nennen den zweiten Eintritt und die Stufe). Ein neuer Strom (prepareToPlay, reset, releaseResources) übernimmt den Zustand seines ersten Eintritts ohne Blende (M-105, M-106); unvorbereitet schreibt nichts (M-107); mit `eq_enabled` aus, im Hard-Bypass und unvorbereitet schreibt der Kern in keinem Eintritt (M-99).
- Tests nach §47.3: A16 eq-copilot/plugin/tests/SondeNullTestMain.cpp (M-94 bis M-100, M-105 bis M-107), B6 eq-copilot/plugin/tests/DspGoldenTestMain.cpp (Kernhälften von M-94, M-97, M-98; bestehende Goldens bytegleich — die Vorgabe „nein" hält sie), B7 eq-copilot/plugin/tests/TransactionTestMain.cpp (M-101 bis M-104, M-108, M-132; Muster 312/M-82 und `setzeHostZaehlerFuerTest`), A3 eq-copilot/plugin/tests/MarkierungTestMain.cpp (M-109, Gens Grenzzeile). Je Zeile: Test, Gegenprobe am Stand vor dem Bau, wo die Matrix „heute rot" sagt, Rotbeweis nach dem Bau an der Zusage (Rohdateien docs/beweise/roh/NAK-312-rot-M-<nn>.txt, Gegenproben docs/beweise/roh/NAK-312-etappe7b-gegenprobe-M-<nn>.txt), Behauptungszeilen in tools/beweise.ps1 (A16, B6, B7, A3) um genau das ergänzt, was der Test misst (R-312-34; ein `'` im Behauptungstext bricht den Parser — danach `[Management.Automation.Language.Parser]::ParseFile` ohne Fehler).
- Kosten: der Zusatzaufwand der Stufe je Block gemessen wie in §21.7, Rohdatei docs/beweise/roh/NAK-312-etappe7b-hostbypass-kosten.txt (Zielpfad aus §47.3).
- M-131 (Grundgesetz und Grenze der Etappe 7) für Satz 2 mitmessen: kein Pfad unter eq-copilot/schemas/v3/ im Diff, Parameter-IDs, Reihenfolge, Bereiche und Class-IDs unverändert (B1), keine neue Oberfläche, Legacy-Bezeichner unverändert, im Audio-Thread keine neue Sperre und keine Allokation (M-101), der Nulltest bleibt bitidentisch (A16; Default-Nulltest in beiden Eintritten unberührt).

GRENZE: nur eq-copilot/plugin/sonde/SondeProcessor.h, eq-copilot/plugin/sonde/SondeProcessor.cpp, eq-copilot/plugin/dsp/DspKern.h, eq-copilot/plugin/dsp/DspKern.cpp, die vier Testdateien oben, tools/beweise.ps1, docs/beweise/NAK-312.md, die genannten Rohdateien unter docs/beweise/roh/. Kein Eingriff in Gen (PluginProcessor, PluginEditor), in State, Schemas, Broker, Installer oder Identität; kein neuer persistenter Parameter; keine v3-Vertragsänderung (`state_report.eq_enabled` bleibt reserviert); keine neue Oberfläche (Designteil geparkt); keine Legacy-Umbenennung; Audio-Thread ohne Sperren, Allokationen, Datei-, Pipe- oder Netzzugriffe und ohne Logging; Passthrough außerhalb der 256-Sample-Blende sampleidentisch, Latenz 0, Tail 0. Fremde uncommittete Dateien (briefing-hub/, nimbalyst-local/) nie anfassen. Das Verhalten von FLs Slot-Bypass im Host ist nicht Gegenstand (§47.3 „nicht gemessen", Register NAK-366); du baust kein Laufzeitwerkzeug dafür.

BEWEISLAUF: (1) Gegenproben am Stand vor deinem Bau für jede Zeile, deren Rotbeweis-Spalte „heute rot" sagt. (2) Bau der Änderungen je Datei; danach die Tests. (3) Einzelbeine über tools/beweise.ps1 — A16, B6, B7, A3, dann B1 — grün; ein Bein niemals als Session-Hintergrundbefehl, sondern abgekoppelt mit Log und `EXIT=`-Zeile (Muster Skill §3.5); beim Warten auf ein Bein oder den Kanon einen abgelaufenen Wächter neu setzen und bei jeder Rückkehr zuerst das Log lesen (NAK-354); Turn nie beenden, solange ein Bein oder der Kanon läuft. (4) Rotbeweise je Matrixzeile nach dem Bau: Mutation → rot → zurück → grün; nach jedem Zurückspielen `LastWriteTime` der Quelle setzen und den Binary-Zeitstempel prüfen (NAK-230). (5) Kostenmessung. (6) Voller Kanon auf dem End-Stand deines Satzes, abgekoppelt: `pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312 Etappe 7b Teil 2'` mit `*> <log>; Add-Content <log> ('EXIT=' + $LASTEXITCODE)` (Zeichenketten im Befehlstext einfach quotieren), Log unter `$env:TEMP`; fertig erst mit der `EXIT=`-Zeile; steht das Log nach „Baue:" ohne Bein bei 0 % CPU, ist es die vctip.exe-Landmine (CLAUDE.md) — Prozess beenden; ein einmaliges „Voraussetzung fehlt" an zwei flatc-Beinen einmal wiederholen (NAK-280). Kanon GRUEN ist Pflicht; A32 HINWEIS (Exit 4) ist nicht blockierend, benenne neue Risse. Übergib nie NOT RUN.

MANIFEST: neuer Abschnitt „## 61. Etappe 7b, Teil 2 — Satz 2 Hostbypass (E1 mit K-B): Bauerbericht (Bauer, 23.09.2026)" nach dem Muster von §54 (Unterabschnitte: Start und Basis-SHA, Gegenproben, Gebaut je Datei mit Datei:Zeile, Matrixzeilen-Ergebnistabelle M-94 bis M-109 und M-132 (Test, Ergebnis, Rotbeweis mit Rohdatei), Einzelbeine mit Kopfzeilen, Kosten, Abweichungen begründet (A-131 ff.), Prüfliste abgehakt, Selbstaudit (Zahlenränder NaN/±Inf/Subnormals/Blockgröße 1, Blende über Blockgrenzen, stale Zustände nach prepareToPlay/reset/releaseResources, Sperrenfreiheit im Audiothread, UI-Texte keine), Kanon nachher als Kopfzeile, Hinweise an den Dirigenten mit Produktwirkung und einem Entwurf für docs/plugin-wissen.md zum Hostbypass — du änderst plugin-wissen.md nicht selbst). Kopfzeile „Etappe" des Manifests: letzten Fettsatz während des Baus auf „**Etappe 7b Teil 2 (Satz 2) läuft.**", am Ende auf „**Etappe 7b Teil 2: gebaut, Kanon grün; Laufzeit-Arm folgt.**" setzen (≤ 70 Zeichen); Kopfzeile „Kanon nachher" um deinen Kanon ergänzen (Muster der Etappen). Vor dem Commit: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` ohne Befund; Pipe-Zeichen in Tabellenzellen als \|; Zeilenenden je Datei mit `git ls-files --eol` lesen und gleich zurückschreiben (Manifest: Index LF; Rohdateien LF).

GIT: nie `git add -A`, nie `--amend`, nie `git reset --hard`, fremde uncommittete Dateien nie anfassen; ein Commit je Änderungssatz mit explizitem Pathspec (Code+Tests+Rohdateien+Manifest zusammen ist in Ordnung; der Kanonblock im Manifest samt Rohausgabe unter docs/beweise/roh/ als eigener Commit), Betreff „NAK-312 Etappe 7b Teil 2: …", Commitnachricht über `git commit -F <datei>` mit der Zeile „Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>" am Ende; vor dem Push `git pull --rebase` bei sauberem Worktree, dann `git push` als eigener Schritt. Zeiten nur aus `Get-Date` (nie geschätzt). Am Ende: Baum sauber (bis auf die zwei fremden Ordner), HEAD = origin/master, und deine letzte Ausgabe nennt Basis-SHA, End-SHA, die Kopfzeile des Kanons und die Kopfzeilen der gefahrenen Beine.
