URTEIL: NEEDS_WORK — Mehrere vorgeschriebene Konstellationen werden umgangen, und konkrete Sequenz-, Epochen-, Rollback-, Boundary- und Replayfehler bleiben. Selbst gemessen wurden Passage 127/127, Experiment-UI 52/52, Schema 75/75, Null-/Identitätstests sowie Rust Experiment 19/19, Taint 14/14 und die 29 nicht-storegebundenen Verdrahtungstests; HEAD blieb f1566f5dadd4fd51f349f5ae062645718543be0d. Die betroffenen Store-/Crashfälle und der Codegen-Drift wurden nur gelesen, weil die Storetests am schreibgeschützten Temp-Verzeichnis mit PermissionDenied stoppten und Python nicht startete.

Full review comments:

- [P1] Synchronisiere den Sequenz-Reset mit dem Audiocallback — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\src\PluginProcessor.cpp:1209-1209
  DEFEKT C1: Beginnt ein Audiocallback zwischen Leerprüfung und Reset, reserviert er noch die alte Sequenz oder sein Ringeintrag wird durch `resync()` verworfen; danach springt der Zähler auf 0, sodass die erste Wire-Intervention nicht zuverlässig 1 ist und der Broker `taint.unknown` erneut setzt. Reset und Ring-Resync müssen mit dem Audio-Erzeuger als eine Generation synchronisiert werden (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:48`).

- [P1] Prüfe C1 über den echten Control-Link — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013PassageStateTest.cpp:1178-1178
  DEFEKT C1 bleibt unbelegt: Der Test ruft direkt `v3LinkFuerTest(true)` auf und entnimmt JSON über einen Test-Getter; das Rust-Gegenstück ruft Brokerfunktionen ebenfalls direkt auf. Damit gibt es weder ControlClient↔Broker-Verbindung noch echten Wire-Handschlag, und `taint.unknown` wird nie anhand des vom Plugin gesendeten Frames beobachtet — exakt die von W2 ausgeschlossene Testform (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:48`).

- [P1] Markiere C2 vor dem Seek im Produkttest — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013PassageStateTest.cpp:801-801
  DEFEKT C2 bleibt unbelegt: Der Test führt den Seek vollständig aus und injiziert erst danach mit `passagenfensterWunschFuerTest(..., e0)` eine alte Epoche. Er ruft `merkeManuellePassage` nicht vor dem Seek auf und hält den Worker nicht zwischen Markierung und Verarbeitung an; damit prüft er nur einen künstlichen Epochenvergleich und kann nicht fallen, wenn der Produktpfad die Epoche beim Markieren falsch erfasst (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:46`).

- [P1] Lösche das Pegelfenster schon an der Transportkante — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\src\PluginProcessor.cpp:507-507
  DEFEKT C4: Bei einem Seek oder Loop innerhalb derselben `[start,end)`-Koordinaten meldet `queue.veroeffentliche` die Transportkante bereits im Audiocallback, dort wird aber nur `fensterAktiv`, nicht `pegelFensterAktiv`, gelöscht. Bis der Worker das Bit später aktualisiert, passiert diese Bedingung weiter und nimmt mindestens den ersten Block der falschen Epoche auf; der C4-Test spielt nur weit außerhalb des Fensters und erkennt das nicht (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:46`).

- [P1] Übergib den Vergleichspegel als ganzen Hostblock — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\src\PluginProcessor.cpp:549-553
  DEFEKT C6: `processBlock` ruft `speise` separat je Kanal auf, wobei das neue CAS-Tor nach jedem Aufruf freigegeben wird. `friereEin()` kann daher nach Kanal 0 übernehmen und einen halben Stereo-Hostblock einfrieren; Kanal 1 wird anschließend verworfen. `c6UebergabeAnDerBlockgrenze` ruft nur eine `speise`-Operation auf und nie `friereEin`, sodass er diese Produktkonstellation nicht fährt (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:46`).

- [P1] Rolle das globale Experimentlog nicht zurück — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\experiment.rs:1206-1207
  LÜCKE B3: Bei parallelen Experimentbefehlen verschiedener Sessions liegt `store.append` außerhalb des globalen `stand`-Locks und die Commit-Guards sind nur session-sharded. Scheitert der ältere Append, nachdem der andere Befehl seinen In-Memory-Effekt erzeugt hat und anschließend committet, trunciert dieser Rollback trotzdem dessen Transitionen und setzt die globale Folge zurück. Der B3-Test verwendet nur eine Session seriell und würde auch beim alten Whole-map-Clone bestehen (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:50`).

- [P1] Setze die Kandidatengrenze unter dem Evidenz-Lock — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\experiment_verdrahtung.rs:878-878
  DEFEKT B9: `evidenzfolge` wird vor dem `stand`-Lock gelesen. Gewinnt danach ein Snapshot das Lock, bekommt er genau diesen Wert und wird noch vor dem Kandidaten angenommen; wegen `>=` landet er später dennoch in der Resultathälfte. Der Ein-Snapshot-Test ist seriell und sieht diese normale Main-command/Probe-evidence-Konstellation nicht; Grenze und Kandidatenmutation müssen unter demselben Lock wie die Evidence-Folgevergabe erfolgen (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:52`).

- [P1] Vergleiche auch den Upstream-Fingerprint — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\experiment_verdrahtung.rs:1145-1145
  DEFEKT B10: Der neue Vergleich baut beide Belege ausschließlich aus `passage_fingerprint`; `Experimentreferenz.upstream_fingerprint` wird nie verwendet. Ein Kandidat mit geändertem Upstream-Fingerprint und ansonsten identischen Daten bleibt deshalb stark vergleichbar. Der hinzugefügte B10-Test ändert nur die vertretene Quelle und verwendet dieselbe Referenz, sodass er am vorgeschriebenen Upstream-Fall nicht fallen kann (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:56`).

- [P1] Beweise die Invalidierungsatomarität mit einem Crash — C:\Users\phili\Projekte\Nakama\broker\tests\sonde013_verdrahtung.rs:3137-3141
  DEFEKT B14 bleibt unbelegt: Ein `event_ord`-Abstand von 1 beweist nur, dass keine dritte Zeile zwischen zwei Writes liegt; auch die alte Implementierung mit zwei unmittelbar aufeinanderfolgenden Appends erfüllt diese Assertion. Ohne Kill/Crash zwischen den früheren Appends und Retryprüfung auf `idempotent_wiederholt` plus vorhandene Invalidierung kann der verlangte Crashzustand unentdeckt bleiben; renew/end und Materialwechsel werden ebenfalls nicht so gefahren (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:50`).

- [P1] Antworte auf Append-Fehler mit abgelehnt/internal — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\befehl.rs:736-736
  DEFEKT B16: Bei injiziertem Append-Fehler liefert `persistenz_p0_mit_domaene_und_ords` `None`, und dieser Zweig gibt `None` unverändert zurück; der neue Test verlangt sogar ausdrücklich keine Antwort. W3 fordert in genau diesem Fall einen gezählten `command_ack` mit `ergebnis=abgelehnt` und `code=internal`, damit der Sender eine definitive Ablehnung statt eines Timeouts erhält (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:50`).

- [P1] Rolle Invalidierungen über stabile Evidence-IDs zurück — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\invalidierung_verdrahtung.rs:170-172
  DEFEKT B16: Ist die Evidence-Historie am Retention-Deckel und trifft während des blockierenden Invalidierungs-Appends ein neuer Snapshot ein, verschiebt `pop_front()` alle gespeicherten Indizes. Der Fehler-Rollback löscht dann per altem Index den Ausschluss eines anderen Snapshots oder lässt den eigenen stehen, sodass Memory und Store auseinanderlaufen. Wie der Evidence-Append-Rollback muss auch dieser Weg anhand stabiler `evidence_id`s rückgängig machen (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:50`).

- [P1] Spiele Deltas erst nach dem absoluten Snapshot nach — C:\Users\phili\Projekte\Nakama\broker\src\coordinator\subscription.rs:179-179
  DEFEKT B17: Schlägt der absolute `session_snapshot`-Push etwa durch P1-Backpressure oder Timeout fehl, wird `offene_outbox_nachspielen` trotzdem aufgerufen. Nimmt die Senke den folgenden `evidence_invalidate`-Push an, erhält das reconnectende Modell ein Delta ohne Basisstand und dessen Schuld wird anschließend kompaktisiert; beim `V3Sender` teilen beide Payloads zudem denselben Queue-Key. Replay darf erst nach erfolgreichem absoluten Snapshot beginnen (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:54`).

- [P1] Fahre B17 über Reconnect bis ins Pluginmodell — C:\Users\phili\Projekte\Nakama\broker\tests\sonde013_verdrahtung.rs:3761-3761
  DEFEKT B17 bleibt unbelegt: `h.abonniert()` ruft hier lediglich `subscribe_json` erneut auf demselben Link und derselben `PushProbe` auf; es gibt weder Disconnect/Reconnect noch echten `V3Sender`/ControlClient, und das JSON wird nicht durch `SourcesModel::uebernehmeEvidenzruecknahme` geschickt. Der Test prüft somit nur die Fake-Senke, nicht die verlangte Wirkung und Schuldtilgung im Pluginmodell (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:54`).

- [P1] Ergänze die Kapazitäts-Eviction im B18-Test — C:\Users\phili\Projekte\Nakama\broker\tests\sonde013_verdrahtung.rs:3874-3877
  DEFEKT B18 bleibt unbelegt: Der neue Test deckt ausschließlich `invalidierung_wegen_messpunkt_fuer_link` ab; der bestehende Eviction-Test erzeugt nur stale/Tombstone. Kein Test füllt den Clientdeckel und löst eine Kapazitäts-Eviction aus, obwohl genau dieser Pfad der offene Teil von B18 war und W6 danach eine neu gebildete getrennte Hälfte mit Grund verlangt. Eine Regression in `platz_schaffen_locked` bleibt daher grün (`docs/beweise/roh/SONDE-013-etappe-2-nacharbeit-3-auftrag.txt:56`).