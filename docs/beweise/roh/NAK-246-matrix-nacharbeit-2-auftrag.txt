NAK-246 Matrix-Nacharbeit 2 (Etappe 1, Spezifikation vor Code — KEIN Bau; Runde 2 von 3) — Auftrag des Dirigenten (2026-09-11)

ANLASS. Die Matrixprüfung 2 (Wiederprüfung 1 nach Vorlage B; Codex gpt-6-astra, Effort max, lesend, Thread 01a08df2-aed0-78c2-8c46-6b571c9b238b, Urteil docs/beweise/roh/NAK-246-matrixpruefung-2-55eb4eb.txt) endete NEEDS_WORK: neun der zehn Befunde der Matrixprüfung 1 sind geschlossen, Befund 9 bleibt offen. Der Dirigent hat den Restbefund an den Manifestzeilen selbst bestätigt (Manifest §7.6). Dieser Auftrag enthält ausschließlich diesen einen Defekt — nichts sonst. Was hier nicht steht, wird nicht geändert.

MODELL DES BAUERS (Dirigenten-Vermerk 11.09.2026): claude-fable-5-1[1m] mit --effort xhigh bis zum Wochenreset; --permission-mode dontAsk.

DU BIST der frische Worker im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = git rev-parse HEAD beim Start; git status --short muss leer sein, sonst nichts anfassen und als letzte Zeile „NAK-246 HALT <grund>“ melden. Aufsicht ENG. Keine Rückfrage an den User; technische Entscheidungen triffst du selbst an der Quelle und begründest sie im Manifest. Lies zuerst vollständig: docs/beweise/NAK-246.md §3.5 (M-21 bis M-25), §5.5 (R-D5 und Feinheiten), §7.5 und §7.6; docs/beweise/roh/NAK-246-auftrag.txt (R-D5 wörtlich); docs/beweise/roh/NAK-246-matrix-nacharbeit-1-auftrag.txt (R-M1-8); das Urteil oben.

GRUNDSATZ: Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Deckel und Zahlen kommen nur aus der Quelle (Datei:Zeile).

DEFEKT (Titel wörtlich aus dem Urteil):
D-M2-1 „[P2] Stimme die Fallback-Marke mit dem Gleichheitsriegel ab“ — Urteilstext: „DEFEKT (M-24/§5.5): Hat derselbe Ausgang bereits einen Snapshot mit Marke 12 zugestellt und degradiert danach der Store ohne weiteren Commit (broker/src/coordinator/subscription.rs:120–161), erhält der Live-Snapshot nach dieser Festlegung ebenfalls 12. Die Annahmeregel in §5.5:1277–1286 verwirft jedoch ausdrücklich auch gleiche Marken (marke <= hochwasser), selbst bei leerer Queue. Damit scheitert die Zusage „Livestand kommt an“ aus M-24:635 weiterhin. Fallback und Annahmeregel müssen gemeinsam so festgelegt werden, dass dieser Gleichheitsfall funktioniert, ohne den Nachzügler-Riegel zu umgehen (R-M1-8).“ Vom Dirigenten am Manifest bestätigt: §5.5 Feinheit 1 gibt dem Livestand bei Projektion None die Prozess-Hochwassermarke („≥ jeder bereits zugestellten“), Feinheit 2 verwirft „marke <= hochwasser“ als Duplikat — bei Gleichheit widersprechen sich beide, und M-24 („wird also nie als Nachzügler verworfen“) hält nicht.
REGEL R-M2-1: Fallback-Marke und Annahmeregel werden GEMEINSAM in einer widerspruchsfreien Aussage festgelegt, sodass (a) ein absoluter Livestand bei Projektion None oder degradiertem Store nach einem bereits zugestellten persistenten Snapshot angenommen wird und (b) ein echter Nachzügler — Marke kleiner als das Hochwasser des Schlüssels — weiterhin verworfen und gezählt wird (R-D5). Zwei Wege sind an der Quelle zulässig; du wählst genau einen und belegst die Wahl: (1) Gleichheit gilt nicht als Nachzügler: verworfen wird nur marke < hochwasser; ein Eintrag mit gleicher Marke wird angenommen und ersetzt per Koaleszierung den gleichen Schlüssel (queues.rs:161-173) — dann ist zu belegen (Datei:Zeile), dass der Consumer gegenüber einem inhaltsgleichen oder jüngeren Stand derselben Marke idempotent ist (eq-copilot/plugin/src/SourcesModel.cpp, Übernahme von session_snapshot) und welche Nachrichten heute mit gleicher Marke entstehen können (Nachspiel nach SENKE_FRIST, Resubscribe), und „Drop/Reorder/Duplicate“ (Entwurf :3551) wird als Robustheit gegen Duplikate gelesen, nicht als Verwerfpflicht; oder (2) der Livestand trägt eine strikt größere Marke als das Hochwasser aus einem HEUTE vorhandenen Zähler des Coordinators (keine neue Zahl, R-D5 „keine neue Zahl“) — dann ist zu belegen, welcher Zähler das ist, dass er im degradierten Fall wächst und dass die Monotonie zu späteren Store-Commits (Store wieder gesund) nicht reißt. Welche Wahl auch immer: M-22, M-24, §5.5 Feinheit 1 und Feinheit 2 sagen dasselbe; der Gleichheitsfall steht ausdrücklich in der Zusage, im Test und im Rotbeweis von M-22 und M-24 (der neue A4-SI-Fall aus M-24 „Flush liefert Marke 12; Store degradiert; Resubscribe → der Livestand kommt an“ muss an der gewählten Regel rot werden, wenn sie verletzt ist, und grün mit ihr); §5.10 bekommt eine Zeile, falls die Wahl vom Wortlaut „Gleiche Marke gilt als Duplikat“ abweicht.

WAS DU ÄNDERST (nur docs/beweise/NAK-246.md): §3.5 Zeilen M-22 und M-24 (und M-21/M-23 nur, falls die gewählte Regel dort einen Satz berührt — dann genau diesen), §3.9 nur, falls sich eine Belegklasse ändert; §5.5 Feinheit 1 und Feinheit 2; §5.10 (falls Abweichung); ein neuer Abschnitt §7.7 „Matrix-Nacharbeit 2“ mit: die geänderten Stellen (Zeile am Ende deines Stands), die gewählte Regel mit Quellbelegen, ein Satz, warum der Gleichheitsfall jetzt an der Zusage fällt; Prüfliste tools/dirigent/pruefliste.md abgehakt mit Fundstelle; Selbstaudit. Kein Kopfzeilen-Edit (macht der Dirigent), kein Umschreiben von §7.1 bis §7.6, keine neuen Regeln, keine neuen Befunde, keine Härtungen, kein Produkt-, Test-, Schema- oder Werkzeugcode.

GIT. Nie git add -A, nie --amend, fremde uncommittete Dateien nie anfassen; git status --short zuerst lesen. Vor dem Commit: py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-246.md ohne Befund (Pipe-Zeichen in Tabellenzellen als \| schreiben). Commit nur mit explizitem Pathspec docs/beweise/NAK-246.md; Commitnachricht über git commit -F <datei>, nie -m mit Backticks; danach git pull --rebase bei sauberem Worktree, dann git push origin master als eigener Schritt. Selbstaudit vor dem Commit: jede geänderte Zeile trägt weiter sieben Spalten, jede Matrix-ID genau einmal, Feinheit 1 und 2 sagen dasselbe wie M-22 und M-24, keine Zahl ohne Quelle.

ENDE: Letzte Zeile deines Turns „NAK-246 MATRIX-NACHARBEIT 2 ABGESCHLOSSEN <voller sha>“. Keine Rückfrage an den User.
