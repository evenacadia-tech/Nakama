Matrixprüfung 3 (Wiederprüfung nach Matrixnacharbeit 2) NAK-214 / G5-Nacharbeit 3 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, kein cargo und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde an der Verhaltensmatrix:
`git diff 85be56e6795f600fa10b07133b78bf061c22ddc6...87ee76f579abde3eea01af83e34633d7a8b47d7d -- docs/beweise/NAK-214.md`
(ZIEL = 87ee76f579abde3eea01af83e34633d7a8b47d7d = HEAD, vor und nach deinem Lauf identisch; VORHER = 85be56e6, der Stand vor der Runde — er enthält gegenüber dem geprüften Stand b204bbcb nur drei Rohdateien unter docs/beweise/roh/, keine Manifeständerung). Der Codestand unter broker/, eq-copilot/, tools/ ist an VORHER und ZIEL identisch mit dem Kanonstand d3c6709 (Abschlusskanon NAK-213, GRÜN 61/61). Der übrige Matrixbereich wurde in Matrixprüfung 1 (Thread 01a080a4, docs/beweise/roh/NAK-214-matrixpruefung-1-d0f2f30.txt) und Matrixprüfung 2 (Thread 01a080d5-38de-7961-84fc-5aa8f028602a, docs/beweise/roh/NAK-214-matrixpruefung-2-b204bbc.txt) bereits geprüft und ist hier nicht Prüfgegenstand.

BEFUNDE, die diese Runde schließen sollte — wörtlich aus dem Urteil der Matrixprüfung 2, mit der Regel, die der Dirigent je Befund entschieden hat (docs/beweise/roh/NAK-214-matrixnacharbeit-2-auftrag.txt; R1 bis R5 stehen in docs/beweise/G5.md §11 Nr. 3, R6 bis R8 in docs/beweise/roh/NAK-214-matrixnacharbeit-1-auftrag.txt):
1. DEFEKT E9/V-44/V-46: „Für einen Experimentabschluss bei bereits positivem Restnachlauf fehlt die ausdrücklich verlangte messende Terminalzeile. `intervention.rs:446–459` entfernt aktive Intervalle, lässt den Nachlauf aber bestehen; nach R7 muss deshalb auch dessen gerettete Experimentzuordnung bestehen bleiben. V-46 fährt nur das Interventionsende, V-44 schließt Terminals ausdrücklich aus, und V-48 kann einen Zuordnungsverlust nicht erkennen, weil `Abgebrochen` unabhängig davon ausgeschlossen bleibt. Ein vorzeitiges Löschen der Zuordnung bei `Terminal::Ergebnis` bliebe damit ungemessen und gäbe die Passage entgegen R7/M-58 frei." Regel (Präzisierung von R7): Ein Terminal — `Ergebnis` wie `Abgebrochen` — lässt die gerettete Nachlaufzuordnung unberührt; sie wird ausschließlich durch Nachlaufende (`tail_samples_offen` auf 0) oder bestätigten Resync geleert. `Terminal::Ergebnis` mit bekannter Vergleichbarkeit bei `tail_samples_offen > 0` ist NICHT wählbar, solange der Nachlauf läuft, und wird mit dem Nachlaufende wählbar (dann V-41); `Terminal::Abgebrochen` bleibt dauerhaft ausgeschlossen (V-48). Neue Zeile(n) ab V-49 mit Test und Rotbeweis (Terminal leert die Zuordnung → Passage mitten im Nachlauf gewählt), Freigabe nach Nachlaufende im selben Fall gemessen; die E9-Tabelle „wann die Menge geleert wird" bekommt „Terminal des Experiments: Menge unverändert" (`intervention.rs:446–459`); V-41 und V-44 verweisen auf die neue Zeile.
2. DEFEKT V-12: „Das leere `listen_for` macht den selektiven Fall herstellbar, aber die Verlagerung auf einzelne `proposal()`-Aufrufe verliert die verlangte Messung von `paare`. `proposal.rs:722` kopiert lediglich die eigene `finding_id`; die tatsächlichen Befund-/Proposal-Paare entstehen erst in `proposal_verdrahtung.rs:58–61` und werden bei `:86–89` gemeinsam ausgewertet. Eine nach dem Herausfiltern verschobene Paarzuordnung ließe sämtliche jetzt beschriebenen Einzelaufrufe grün." Regel: V-12 fährt den produktiven Pfad: mehrere Befunde einer Sitzung in `stand.befunde`, genau einer mit leerem `listen_for` (direkt im Stand gesetzt); `vorschlaege_bilden` über den Coordinator; gemessen am Bestand — `stand.vorschlaege` und `vorschlaege_sicht` enthalten genau die Vorschläge der übrigen Befunde, jeder mit der `finding_id` seines Befunds, kein Vorschlag ohne Befund, `angebot` (sofern eines entsteht) gehört zu einem Befund mit Vorschlag; Test in `sonde014_verdrahtung.rs`; Kontrollfall ohne Rotbeweis oder mit benanntem Rotbeweis gegen die Paarbildung.

Der Worker hat die Änderungen als Zeile V-49 (neu) und Umschreibungen an E9 (Leertabelle), V-12, V-41, V-44 sowie im Kopf und §5 eingetragen (Verlauf „Matrixprüfung 2" und „Matrixnacharbeit 2").

GATE-BEZUG, wörtlich: P5-Exit-Gate (Entwurf §59, docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung." SONDE-013 M-58 („Das Ende allein genügt nicht: der Nachlauf muss abgelaufen sein"), M-61, M-62; SONDE-014 M-25 (Determinismus), M-46; NAK-212 N-43. Invarianten aus CLAUDE.md: „Schemas sind Verträge", „Keine toten UI-Elemente", „Begriffe nicht vermischen", Beziehungen aktivieren↔abklingen und verbinden↔trennen im selben Änderungssatz.

Du beantwortest genau zwei Fragen:
1. Ist jeder der zwei Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Zeilen-ID, Entscheid, Satz der Matrix; und ob die neue Zusage am Code an ZIEL wahr ist, mit Datei:Zeile)? Insbesondere: Lässt die Matrix für ein Experiment mit `Terminal::Ergebnis` während laufendem Nachlauf jetzt genau eine Ausgabe zu (V-49 gegen V-41, V-44, V-46, V-48; M-25), und ist die Leertabelle in E9 vollständig (Interventionsende füllt, Nachlaufende und Resync leeren, Terminal lässt unverändert)? Erreicht V-12 den Pfad `vorschlaege_bilden` → `paare` (`proposal_verdrahtung.rs:58–61`) → `angebot` (`:86–89`), und ist die Messung am Bestand (`stand.vorschlaege`, `vorschlaege_sicht`) so benannt, dass eine verschobene Paarzuordnung den Fall rot machte?
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile oder einen Entscheid, einen Satz des Gate-Bezugs, eine unverändert geltende Referenzzeile, §2.8 („kein Fassungsschritt") oder eine Invariante aus CLAUDE.md? Hat eine geänderte Zeile ihren Test (Datei::Fall) oder ihren Rotbeweis verloren, oder widersprechen sich zwei Zeilen nach der Änderung?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1: DEFEKT (Widerspruch zu Gate, Regel, Entwurf, Vertrag, Referenzmatrix, Code an ZIEL oder Invariante; oder Regel ohne messende Zeile mit Test und Rotbeweis), LÜCKE (benennen, keine Nacharbeit), HÄRTUNG (benennen, keine Nacharbeit).
AUSDRÜCKLICH AUSGESCHLOSSEN: Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände in docs/**; der Manifestkopf; §3, §4, §5 von docs/beweise/NAK-214.md (nur §1 und §2 sind Prüfgegenstand; §5 liest du als Kontext); docs/beweise/G5.md, NAK-212.md, NAK-213.md, docs/offene-punkte.md; im Register datierte Härtungen NAK-191 bis NAK-211 und NAK-215 bis NAK-222; Sabotage-Szenarien gegen Prüfwerkzeuge (= Härtung); tools/dirigent/pruefliste.md ist keine Anforderungsquelle; die vom Dirigenten getragenen Lesarten E2, E6, E7, E9 und die Regeln R6 bis R8 sind keine Wortlautdefekte; Ticketfremdes nur insoweit, als die Matrix dorthin abgrenzt. Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — beide Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID (V-nn oder E-n) oder Manifestzeile, Quelle Datei:Zeile, welcher Satz von Gate, Regel, Entwurf, Vertrag, Referenzmatrix oder Invariante bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
