VALIDIERUNG: 3 bestätigt, 5 präzisiert, 0 widerlegt

# NAK-312 Etappe 5 — Validierung der Erstprüfung (S25k, Prüfstufe T2)

Frischer, lesender Validierer (Claude Opus 5), 21.09.2026, Bericht geschrieben ab 20:06 Uhr (`date`). Auftrag `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-validierung-auftrag.txt`. Gegenstand ist das Rohurteil `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-urteil.md` über `git diff 12300f1e…0014d5f7` der zehn Pfade aus `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-auftrag.txt:4`. `git diff --stat 0014d5f7 HEAD` zeigt nur `docs/PLAN-STAND.md` und drei Rohdateien (Prüfauftrag, Rohurteil, Validierungsauftrag): der Code am HEAD ist der des Zielstands. Zeilenangaben gelten am HEAD, wo nicht „BASIS" dabeisteht. JUCE und VST3-SDK stammen aus der gepatchten Baukopie `eq-copilot/build/_deps/juce-src`; abgekürzt: `VST3.cpp` = `modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp`, `ivstaudioprocessor.h` = `modules/juce_audio_processors/format_types/VST3_SDK/pluginterfaces/vst/ivstaudioprocessor.h`, `juce_VST3Common.h` = `modules/juce_audio_processors/format_types/juce_VST3Common.h`. Der Nakama-Patch `third_party/patches/juce-8.0.9-nakama-vst3-bridge.patch` berührt `setupProcessing` nur als Kontextzeile (Patch `:65`), weder `setNonRealtime` noch eine Sperre. Gefahren: nur lesende Kommandos (git, grep, sed, wc, sha256sum).

## V-1 (H-1, Mechanik) — BESTÄTIGT

Quelle:
- `eq-copilot/plugin/sonde/SondeProcessor.cpp:1562` `juce::AudioProcessor::setNonRealtime (offline);` steht vor der Wechselerkennung.
- `SondeProcessor.cpp:1573-1575` `if (offlineGesehen.load (std::memory_order_acquire) == offline || offlineGesehen.exchange (offline, std::memory_order_acq_rel) == offline) return;`
- Rückweg `:1577-1585`, Wirkung allein `:1583` `dspKern->setzeOfflineRiegel (false);`.
- Hinweg `:1590` `dspKern->setzeHoermatrix (nakama::dsp::Hoermatrix::processed);`, `:1591` `dspKern->setzeOfflineRiegel (true);`, `:1593` `vorschauEndeAngefordert.store (true, std::memory_order_release);`.
- `eq-copilot/plugin/dsp/DspKern.h:308` `void setzeHoermatrix (Hoermatrix h) noexcept { hoerwunsch.store (h, std::memory_order_release); }`.
- `DspKern.h:340-352` `setzeOfflineRiegel`: mit `an` `:344` Riegel true, `:345` Anforderung true; ohne `:349` Anforderung false, `:350` Riegel false.
- `eq-copilot/plugin/dsp/DspKern.cpp:1216-1217` `const bool hart = hartSchalten.load (std::memory_order_acquire) && hartSchalten.exchange (false, std::memory_order_acq_rel);`; `:1221-1222` `wirksam` aus `hoerwunsch` mit Candidate-Rückfall; `:1225` `if (offlineRiegelAn.load (std::memory_order_acquire)) wirksam = Hoermatrix::processed;`; harter Zweig `:1227-1236` (`hoerVorher = wirksam; hoerLaufend = wirksam; hoerFadeRest = 0;`); weicher Weg `:1366-1373` (`hoerFadeRest = kFadeSamples`, 256 nach `DspProgramm.h:87`).

Gerechnet:
1. `offlineGesehen = false`. T1 `setNonRealtime (true)`: Lesung false, Tausch liefert false, T1 läuft bis vor `:1590`. T2 `setNonRealtime (false)`: Lesung true, Tausch liefert true, `:1583` → `DspKern.h:349-350`. T1 danach `:1590`, `:1591` → `:344-345`, `:1593`. Ende: `offlineGesehen` false, Riegel true, Anforderung true, Vorschauende angefordert. Jeder weitere `setNonRealtime (false)` liest false und kehrt an `:1575` zurück; der nächste Echtzeitblock verbraucht die Anforderung und schneidet ohne Fade auf Processed (`:1216-1217`, `:1225`, `:1227-1236`); bis zum nächsten Hin- und Rückweg wirkt jeder Wunsch als Processed. Stimmt.
2. `offlineGesehen = true`. Der Rückweg tauscht zuerst (liefert true), der Hinweg liest danach false und tauscht (liefert false); beide führen aus. Speichert der Rückweg zuletzt (`:349-350` nach `:344-345`), bleiben Flag true, Riegel und Anforderung false, Wunsch Processed, Vorschauende angefordert. Weitere `setNonRealtime (true)` kehren früh zurück; ein Candidate-Hörstand blendet über 256 Samples aus, ein später gesetzter Wunsch wäre im Render hörbar. Stimmt.
3. Der Audiothread verbraucht die Anforderung (`:1216-1217`), der Rückweg speichert `:349` und `:350`, der Audiothread liest an `:1225` false und setzt an `:1227-1236` `hoerLaufend = wirksam` ohne Fade. Stimmt.

Zusätze, keine Berichtigung:
- „Flag" meint `offlineGesehen`. JUCEs eigenes `nonRealtime` wird an `:1562` vor der Erkennung gespeichert und folgt der Reihenfolge der zwei Basisaufrufe, nicht der des Tauschs; in (1) kann `isNonRealtime()` true zeigen, während `offlineGesehen` false ist (gelesen an `SondeProcessor.cpp:556` für `stempel.nichtEchtzeit`).
- (2) hält nur, solange weitere Aufrufe `true` bringen; der nächste Aufruf mit `false` fährt den Rückweg regulär und stellt einen stimmigen Zustand her.
- (3) Ziel des Schnitts ist `wirksam`. Der Hinweg hat den Wunsch an `:1590` auf Processed gestellt; ein anderer Hörstand als Processed entsteht nur mit einem dritten Schreiber zwischen `:1590` und `:1221`. Genauer als der H-1-Satz „Alle drei setzen voraus, dass `setNonRealtime` auf zwei Threads zugleich läuft": (3) braucht ein `setNonRealtime (false)` auf einem zweiten Thread, während der Audiothread `verarbeiteStueck` des ersten Blocks nach dem Hinweg fährt. So steht es im Prüfpunkt (a) des Berichts.
- Gleiche Werte gleichzeitig: der zweite Tausch liefert den eigenen Wert und kehrt an `:1575` zurück, genau ein Wechsel. Stimmt.

## V-2 (H-1, Erreichbarkeit) — PRÄZISIERT

JUCE-Quelle, wörtlich:
- `VST3.cpp:3592-3610` `setupProcessing`: `:3594` `ScopedInSetupProcessingSetter inSetupProcessingSetter (juceVST3EditController.get());` (setzt nur ein Flag am Edit-Controller, `:3860-3877`), `:3605` `getPluginInstance().setNonRealtime (newSetup.processMode == Vst::kOffline);`, `:3607` `preparePlugin (processSetup.sampleRate, processSetup.maxSamplesPerBlock, CallPrepareToPlay::no);`. Keine Sperre.
- `VST3.cpp:3714-3716` `process`: `const FLStudioDIYSpecificationEnforcementLock lock (flStudioDIYSpecificationEnforcementMutex);`, danach `:3784-3785` `processAudio<float>` beziehungsweise `processAudio<double>`.
- `VST3.cpp:3889-3909` `processAudio`: `:3890` `const ScopedLock sl (pluginInstance->getCallbackLock());`, `:3892` `pluginInstance->setNonRealtime (data.processMode == Vst::kOffline);`, `:3907` `processBlockBypassed`, `:3909` `processBlock`.
- Kommentar `VST3.cpp:3812-3813`: „FL's Patcher implements the VST3 specification incorrectly, calls process() before/during setActive()." Die Sperre greift nur in FL: `:3820` `static const auto lockRequired = PluginHostType().isFruityLoops();`, `:3822-3823`. Sie steht in `setActive` `:2811`, `activateBus` `:3316`, `setBusArrangements` `:3461`, `process` `:3716`; Mitglied `:4050`. `setupProcessing` und `setProcessing` (`:3612-3618`) nehmen sie nicht. FL meldet sich als `"host": "FruityLoops"` (`docs/beweise/termin-b/host-probe-20260822-130657.json:4`); die Sperre ist in FL also aktiv.
- Der Callback-Lock serialisiert `processAudio` (Flag und `processBlock`) mit `prepareToPlay` (`SondeProcessor.cpp:392`), `releaseResources` (`:441`), `reset` (`:473`) und `v3Status` (`:1419`) der Sonde. `setupProcessing` nimmt keine der beiden Sperren.
- JUCE setzt den Vertrag selbst voraus: `setupProcessing` → `preparePlugin` (`VST3.cpp:3948-3962`) schreibt `bufferMapper` (`juce_VST3Common.h:775-806`, `:808-821`; `:645` `scratchBuffer.setSize (numChannels, blockSize);`, `:646` `channels.reserve (…)`) und leert `midiBuffer` (`VST3.cpp:3957-3958`); `processAudio` benutzt `bufferMapper` (`:3883`). Liefe `setupProcessing` neben `process`, liefe schon der Wrapper in einen Datenwettlauf, unabhängig von der Sonde.

VST3-SDK, wörtlich:
- `ivstaudioprocessor.h:307`: „Called in disable state (setActive not called with true) before setProcessing is called and processing will begin."
- `ivstaudioprocessor.h:138-139`: „Switching between kRealtime (or kPrefetch) and kOffline requires that the host calls IAudioProcessor::setupProcessing in order to inform the plug-in about this mode change."
- `ivstaudioprocessor.h:317-318` (`setProcessing`): „The host has to be sure that it is called only when the plug-in is enable (setActive (true) was called)."
- Der Satz an `:307` beschreibt den Aufrufzeitpunkt. „Darf nur im deaktivierten Zustand kommen" ist die übliche Lesart als Vertrag, kein wörtliches Verbot.

Berichtigung: Der H-1-Satz „Nach JUCE-Wrapper und VST3-Vertrag geschieht das nicht" trägt nur für den Vertrag. Der Wrapper schließt Gleichzeitigkeit allein innerhalb eines Callbacks aus (derselbe Thread ruft `:3892` und `:3909`); `setupProcessing` gegen `process` serialisiert er nicht. Für FL dokumentiert JUCE eine Vertragsabweichung am Nachbarpaar `process`/`setActive` und fängt nur diese ab. Für FL ist der Ausschluss damit eine Annahme, keine Wrappergarantie und keine Messung. Der Code der Etappe nennt den Fall selbst (`SondeProcessor.cpp:1571-1572`): „Der Tausch sorgt dafuer, dass genau EIN Aufruf den Wechsel ausfuehrt, auch wenn Host und Callback zugleich riefen." Das trägt für gleiche Werte; für gegensätzliche Werte laufen beide Wechsel (V-1).

Messung im Repo:
- Gemessen (FL-Termin B, 22.08.2026, FL Studio 2026 26.1.4.5589, `docs/beweise/termin-b/LIES-MICH.md:16`, `:29`): ein Export liefert `offline_an` und `offline_aus` je einmal und 2587 Offline-Blöcke (`host-probe-20260822-130657.json:12`, `:94-95`, Ereignisse `:747` Block 219728 und `:774` Block 222315); ebenso `eq-copilot/identity/host-capabilities-fl-v1.json:47`, `:119`. Messgröße ist `isNonRealtime()` im `processBlock` derselben Instanz (`eq-copilot/plugin/hostprobe/HostProbeProcessor.cpp:153-157`, `:177`, `:184`). Die Probe überschreibt weder `setupProcessing` noch `setNonRealtime`, ihr `prepareToPlay` zählt kein Ereignis (`:97-106`). Belegt ist: FL liefert Exportblöcke mit `processMode == kOffline`, je ein Wechsel hin und zurück.
- Der Laufzeit-Arm rendert im Szenario `nulltest-host` über `FL64.exe /R` und vergleicht nur den Ausgang (`docs/gesundheit/szenarien/LIES-MICH.md:13`).
- Nicht gemessen: ob und wann FL `setupProcessing` ruft, auf welchem Thread, in welchem Aktivierungszustand, ob `setActive (false/true)`, `releaseResources` und `prepareToPlay` den Render rahmen, ob `setupProcessing` neben `process` läuft. `docs/beweise/termin-a/`: kein Treffer zu den Suchwörtern; `docs/plugin-wissen.md:92`, `:105`, `:912`: keine Aufrufpfadmessung; `docs/beweise/SONDE-015.md:3930`, `:4160`, `:4338`, `:4349`: Aussagen zu Wrapper und Sperre, keine FL-Messung. Der Bauer schreibt selbst (`docs/beweise/NAK-312.md:5448-5451`): „welchen Weg FL beim Render nimmt, ist nicht gemessen". R-312-3 (`NAK-312.md:137-139`): „Ob FL vor einem Offline-Render deaktiviert, wird im Laufzeit-Arm beobachtet, wenn das ohne neue Infrastruktur geht; sonst datierte Registerzeile." Eine solche Registerzeile steht am HEAD nicht in `docs/offene-punkte.md` (Treffer zu „Offline-Render" nur NAK-27 und NAK-340).

Was bräche, wenn FL `setupProcessing` neben `process` riefe (ohne Einordnung):
- Verschränkung (1), Riegel und Anforderung in Echtzeit: M-43 („… die harte Umstellung gilt nur für die Offlinerichtung") und [SONDE-015] M-55 („Kein Klick"), weil der nächste Echtzeitblock ohne Fade schneidet; bis zum nächsten Offline-Zyklus [SONDE-015] M-53 (Dry bitidentisch zum Eingang) und M-54 (Delta), weil jeder Wunsch als Processed wirkt.
- Verschränkung (2), Render ohne Riegel: M-42 („Ab Sample 0 tragen die ersten Samples den bestätigten Zustand"), M-47, R-312-3, für einen im Render gesetzten Wunsch [SONDE-015] M-120 Satz 2 und die Entwurfssätze `:2137`, `:2748`.
- Verschränkung (3): M-43 und [SONDE-015] M-55.
- Nicht betroffen: Gate GRENZE „Audio-Thread ohne Sperren und Allokationen" und die Invariante „Audio bleibt echtzeitfest" (jede Verschränkung fasst nur Atomics an); die Beziehungsregel aktivieren↔abklingen ist eine Regel über den Änderungssatz, beide Richtungen stehen in `19ddf5d3`. Heute hörbar ist keiner der Fälle (V-3).

## V-3 (H-1 und L-1, Hörbarkeit heute) — BESTÄTIGT

- `git grep -n "setzePreview\|setzeHoermatrix\|beendePreview" -- eq-copilot/plugin ':!eq-copilot/plugin/tests/**'`: Produktaufrufe der Hörmatrix stehen nur an `SondeProcessor.cpp:1314` (Ladeweg, „R10: nach dem Laden steht die Hoermatrix IMMER auf Processed (M-52).") und `:1590` (Hinweg), beide Processed. `SondeProcessor::setzePreview` (`:1506-1510`, Deklaration `SondeProcessor.h:258`) und `SondeProcessor::setzeHoermatrix` (`SondeProcessor.h:274`) liegen außerhalb des Testblocks und haben außerhalb der Tests keinen Aufrufer; der Block `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` (`SondeProcessor.h:304-535`) ruft keinen der beiden.
- v3-Steuerung: der ControlClient der Sonde entsteht mit `{}` für `beiAntwort`, `beiLinkStatus` und `beiVersionierterAntwort` (`SondeProcessor.cpp:198-230`, Signatur `eq-copilot/plugin/core/ipc/ControlClient.h:356-363`), der TelemetryClient mit `{}` für `beiFrame` (`SondeProcessor.cpp:231-237`, `core/ipc/TelemetryClient.h:109-113`). Keine eingehende Nachricht erreicht Sondencode. `preview` oder `hoermatrix` unter `core/` und `hostbridge/`: nur der Kommentar `core/ipc/InterventionsRing.h:96-98`; unter `core/diagnose`: kein Treffer.
- Ladeweg: `:1314` Processed; `Transaktionskern::ladestart` beendet die Vorschau (`eq-copilot/plugin/state/NakamaTransaktion.cpp:268`).
- Der Broker kennt `preview_begin/renew/end` (`broker/src/coordinator/befehl.rs:864`), das Schema reserviert sie (`eq-copilot/schemas/v3/reservierte-nachrichten-v1.json:271-273`); die Sonde hat keinen Empfangsweg.
- Erster Produktaufrufer: Planschritt S29–31 (`docs/plan/plan.json:437-440`, Ticket SONDE-016/017): „Kopplung Gen↔Sonde als Hauptbedienweg: Pairing, Verschlüsselung, Lease, Anwenden/Zurücknehmen, aktiver Vergleich — dazu die Drei-Stufen-Geste (Halten = hören · 1. Klick = 10-Sekunden-Probe · 2. Klick = fest) …". Entwurf §33.4 (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2108-2109`): „Hold-to-hear bleibt die kurze, regelmäßig erneuerte `preview_begin/renew/end`-Lease mit lokalem Failsafe."; `:2118-2119`: „… Der Vertrag entsteht in S29–31."

## V-4 (L-1, Zusagenlage) — PRÄZISIERT

Die Sätze wörtlich:
- M-44 (`docs/beweise/NAK-312.md:833`): Zustand „Prozessor mit Vorschau, Offline an, Offline aus", Ereignis „zweiter Wechsel nach offline", Zusage „`setNonRealtime (false)` belebt die Vorschau NICHT wieder; ein zweiter Wechsel nach offline findet keine Vorschau mehr vor und ändert am Ausgang nichts", Reihenfolge „Vorschauende ist ein einmaliger Übergang, kein Zustand, der zurückkippt".
- R-312-3 (`NAK-312.md:134-137`): „Die ersten Samples eines Offline-Renders tragen den bestätigten Zustand, nie die Vorschau (M-120), unabhängig davon, ob der Host vorher `prepareToPlay` ruft."
- [SONDE-015] M-120 (`docs/beweise/SONDE-015.md:1224`): „Beide erzeugen denselben Parameterverlauf und damit innerhalb numerischer Toleranz denselben Ausgang, bei gleichem Event- und Blockverlauf. Offline-Render läuft mit dem bestätigten Zustand, nie mit einer Vorschau."
- Entwurf `:2137`: „Preview-Zustand wird nie im Projekt gespeichert und nie in einen Offline-Render übernommen." Entwurf `:2748`: „Preview, Focus und Delta sind im Offline-Render neutral."
- E-312-7 (`NAK-312.md:2079-2082`) bindet die Neutralität an den Riegel: „… ein Offline-Riegel, unter dem jeder Hörwunsch als Processed wirkt (Entwurf: Preview im Offline-Render neutral) …".
- Entwurf §33.4 `:2133-2136`: „Eine gehaltene Preview erhält eine signierte `lease_duration_ms`, die die Probe auf einen festen sicheren Bereich — Startwert höchstens 400 ms — klemmt und ab lokalem QPC-Empfang misst. Main erneuert sie ungefähr alle 100 ms; nur eine neue gültige Renew-ID verlängert sie. Läuft sie ab, fährt die Probe lokal mit einem kurzen Ramp in den gespeicherten Zustand zurück."
- Entwurf §41.3 `:2741-2742`: „Alle neuen hörbaren Preview-/Focus-/Delta-/Markerpfade verlangen Realtime-Modus sowie `play_state.valid=true && playing=true`; unbekannter oder gestoppter Transport ist fail-closed." `:2746-2747`: „Stop, Sidechainfehler, Lease-Ablauf, Prozessorfehler oder Wechsel der Transportepoche blenden zum gespeicherten Zustand zurück."

Lesart des Prüfers: Gate, NAK-312-Matrix und Code sagen zu einer im Render gesetzten Vorschau und ihrem Bestand danach nichts. M-44 spricht von der beim Wechsel aktiven Vorschau, R-312-3 von den ersten Samples, M-120 und `:2137`, `:2748` von der Neutralität im Render, die der Riegel hält (`DspKern.cpp:1225`); die Analyse liest nur `postCommitted` (`SondeProcessor.cpp:616-617`). Insoweit trägt die Lesart.

Berichtigung zu „Über eine im Render gesetzte Vorschau und ihren Bestand nach dem Render sagen Gate, Matrix und Entwurf nichts": Der Entwurf nennt den Fall nicht beim Namen, regelt aber Lebensdauer und Hörbarkeit jeder Vorschau (Lease höchstens 400 ms ohne Erneuerung; hörbar nur im Realtime-Modus bei gültigem, laufendem Transport; Rückkehr bei Stop, Lease-Ablauf und Transportepochenwechsel). Gebaut ist davon nichts (`git grep -w -i lease` unter `sonde/`, `state/`, `dsp/`: kein Treffer); es gehört zu S29–31. Mit diesen Regeln könnte eine im Render gesetzte Vorschau den Render ohne Erneuerung nicht überdauern. Für den Weg über Gen ist die Produktfrage aus L-1 damit im Entwurf vorentschieden; für die heutige Sonde ist sie offen.

Die Kette, Zeilen stimmen:
- Der Wechsel stellt Processed: `SondeProcessor.cpp:1590`.
- Der Rückweg lässt `wirksam = hoerwunsch`: `:1583` fasst nur Riegel und Anforderung an (`DspKern.h:349-350`); danach erzwingt `:1225` nichts mehr, es gilt `DspKern.cpp:1221-1222`.
- `bereiteVor` setzt Processed: `DspKern.cpp:146` (samt `:147-150`), gerufen aus `prepareToPlay` (`SondeProcessor.cpp:399`).
- `prepareToPlay` publiziert eine aktive Vorschau neu: `SondeProcessor.cpp:418`.
- Zusatz: auch `releaseResources` → `DspKern::freigeben` setzt den Wunsch auf Processed (`DspKern.cpp:186`), ebenso der Ladeweg (`SondeProcessor.cpp:1314`). Im Vertragsablauf (`setActive (false)` → `releaseResources`, `VST3.cpp:2837`; `setupProcessing` deaktiviert; `setActive (true)` → `prepareToPlay`, `:2833`) laufen beide um den Wechsel. Die Buchhaltung der Vorschau überlebt beide: `freigeben` setzt nur Pool und Pfade zurück (`DspKern.cpp:184-185`), `prepareToPlay` publiziert die Vorschau neu.

Berichtigung zu „Eine Vorschau, die NACH dem Wechsel über `setzePreview` kommt, bleibt jetzt als Buchhaltung stehen" (L-1): Das gilt erst nach dem ersten Kontrolltakt nach dem Hinweg. Bis dahin steht die Anforderung aus `:1593`; der Takt (`:1808-1810`) ruft `Transaktionskern::beendePreview` (`NakamaTransaktion.cpp:313-318`), das die dann aktive Vorschau beendet, ohne zu prüfen, ob es die beim Wechsel aktive war (`setzePreview` überschreibt `vorschau`, `:303-311`). Eine im ersten Taktintervall des Renders gesetzte Vorschau endet also; erst eine spätere bleibt stehen. Das ist der Mechanismus von L-2.

Vergleich mit BASIS, stimmt: BASIS `SondeProcessor.cpp:1541-1553` lautet `juce::AudioProcessor::setNonRealtime (offline); if (! offline) return; … GemeldetesSchloss gemeldet { zustandSchloss }; const nakama::dsp::RtWache::GemeldeteSperre<GemeldetesSchloss> l (gemeldet); transaktion->beendePreview(); dspKern->setzeHoermatrix (nakama::dsp::Hoermatrix::processed);`. Der Wrapper ruft das vor jedem Block (`VST3.cpp:3892`); jeder Offlineblock beendete die Vorschau und stellte den Wunsch auf Processed.

## V-5 (L-2) — PRÄZISIERT

- Der Ablauf stimmt. Der Hinweg setzt an `:1590-1593` die Anforderung. Der Rückweg `:1577-1585` fasst sie nicht an; Kommentar `:1579-1582`: „Die Vorschau lebt nicht wieder auf; ein angefordertes Vorschauende bleibt angefordert." `setzePreview` (`:1506-1510`, unter `zustandSchloss`) → `NakamaTransaktion.cpp:303-311` (`vorschau.aktiv = true; vorschau.satz = satz;`, Publikation `:307`). Der nächste Takt nimmt das Schloss (`:1796`), verbraucht die Anforderung (`:1808-1810`) und ruft `beendePreview` (`NakamaTransaktion.cpp:313-318`: `if (! vorschau.aktiv) return; vorschau.aktiv = false; aus.beendePreview();`) → `DspKernAusfuehrung::beendePreview` (`:765-768`) → `DspKern::beendeCandidate` (`DspKern.cpp:438-447`, `candidateAktiv` sofort false). `wirksam` fällt auf Processed (`:1222`), der Fade läuft weich (`:1366-1373`), die Bank hält als Hörhalt (`:562-564`) bis `beendeHoerHalt` (`:504-521`). Klickfrei: stimmt.
- Verhaltensänderung der Etappe: ja. BASIS beendete die Vorschau synchron im Aufruf (BASIS `:1551`), der Rückweg tat nichts (BASIS `:1544` `if (! offline) return;`); eine danach gesetzte Vorschau blieb. Eingeführt mit `19ddf5d3`.
- Die Sätze wörtlich:
  - E-312-7 (`NAK-312.md:2079-2088`): „Beim Offline-Wechsel berührt der Callback nur eigene oder geteilte Atomics: hartes Schalten der Hörmatrix, das der Audiothread im eigenen Zustand verbraucht, ein Offline-Riegel, unter dem jeder Hörwunsch als Processed wirkt (Entwurf: Preview im Offline-Render neutral), und ein Atomic „Vorschauende angefordert". Merkzettel, Publikation, `beendeCandidate` und die Bankfreigabe bleiben beim Worker im nächsten Kontrolltakt. …"
  - M-83 (`NAK-312.md:830`), Zustand „wie M-40" (Vorschau vor dem Wechsel gesetzt), Zusage „Die Buchhaltung folgt im nächsten Kontrolltakt, nicht im Block: NACH dem ausdrücklichen Takt ist `previewAktiv()` false, … VOR dem ausdrücklichen Takt sagt die Zeile nichts über `previewAktiv()`".
  - M-46 (`NAK-312.md:835`), Zusage „Es bleibt keine Bank hängen: nach dieser Folge ist die Candidate-Bank wieder frei, und der Hörhalt ist beendet, auch wenn nie ein Fade lief. …"
  - Keiner dieser Sätze, auch M-44 nicht, spricht über eine nach dem Rückweg neu gesetzte Vorschau. Die Rückkehrlisten des Entwurfs (§33.4 `:2136`, §41.3 `:2746-2747`) nennen eine veraltete Offline-Anforderung nicht und sind nicht als abschließend gefasst. Stimmt.
- Berichtigung zum Fenster: Der Takt läuft am Kopf der Workerschleife, wenn `juce::Time::getMillisecondCounterHiRes() >= naechsterKontrollTakt` (`:753`), danach `+ 5.0` ms (`:756`); zwischen den Durchläufen wartet der Worker bis 20 ms (`:916-918`, früher geweckt über `workerWarte`) oder gibt bei Queuerest nur ab (`:911-915`). Der Nennwert ist damit mindestens 5 ms, gewöhnlich bis etwa 20 ms. Eine feste Obergrenze gibt der Code nicht: der Takt wartet auf `zustandSchloss` (`:1796`; gehalten etwa von `setzePreview` `:1508` oder vom Ladeweg), und derselbe Durchlauf fährt den Analyseteil unter `analyseSchloss` (`:759-909`, samt `analyseEngine.auswerten()` auf Anfrage `:899-906`). Hinweg, Rückweg und `setzePreview` müssen vor diesen Takt fallen.
- Erweiterung: Ohne Rückweg wirkt derselbe Mechanismus auf eine im ersten Taktintervall des Renders gesetzte Vorschau (V-4).

## V-6 (H-2, Mutationsüberleber) — PRÄZISIERT

(a) Stimmt. `setNonRealtime (true)` in B7: `eq-copilot/plugin/tests/TransactionTestMain.cpp:2559`, `:3936`, `:4484`, `:5406`, `:5423`, `:5462`, `:5498`, `:5522`, `:5550`, `:5585`, `:5593`, `:5609`, `:5627` (gleich der Liste des Berichts). Hörwünsche setzen die Fälle nur vor dem Hinweg (`:2552`, `:5356` über `offlineAufbau`, `:5518`) oder nach dem Rückweg (`:5558`); die übrigen B7-Aufrufe `:2530` (Delta) und `:2902` (Dry) laufen ohne Offline. Der Offline-Vergleich `:4483-4484` fährt Hostereignisse (`folgeM20`, `:4594-4595`), keinen Wunsch. Weil der Hinweg den Wunsch schon an `SondeProcessor.cpp:1590` auf Processed stellt, bliebe B7 ohne `DspKern.cpp:1225` grün.

(b) In B7 stimmt es: Rückwege an `:3936` (`lauf (false)` nach 200 Offlineblöcken, `:3941-3942`), `:5555` (nach 8 + 8 Blöcken, `:5551`, `:5553`) und `:5588` (nach 4 Blöcken, `:5586`). Außerhalb von B7 nicht: `eq-copilot/plugin/tests/Sonde012HostChannelContextTest.cpp:254-256` fährt `p.setNonRealtime (true);` und `p.setNonRealtime (false);` ohne Block dazwischen, prüft aber nur das Statuswort „offline" (`:255`), kein Audio. Der Schluss „eine Mutation, die die Rücknahme entfernt, bliebe grün" bleibt richtig; „kein Fall" gilt nur für B7.

(c) Stimmt. `zaehleAllokationen` wird nur an `TransactionTestMain.cpp:842`, `:852`, `:3704`, `:3854`, `:3882`, `:4747`, `:5292`, `:5833` scharf, jeweils um `processBlock` oder Transaktionscode, nie um `setNonRealtime`; in 312/M-04 (`:3922-3952`), 312/M-40 bis M-47 und M-83 (`:5402-5641`) und im O-Fall (`:2543-2575`) nie. `RtWache::meldeAllokation` zählt nur im Bereich (`eq-copilot/plugin/dsp/DspRtWache.h:41-44`) und wird nur aus B7s `operator new` bei scharfem Flag gerufen (`TransactionTestMain.cpp:141`, `:151`, `:160`).

Die Matrix wörtlich:
- M-40, Zusage (`NAK-312.md:828`): „`RtWache::sperren()` = 0 über die 200 Blöcke: das Offlineflag nimmt in keinem Block eine Sperre im Callback". Nur Sperren.
- M-41 (`NAK-312.md:829`), Reihenfolge: „`setzeHoermatrix` schreibt ein Atomic (…) und wirkt im selben Callback; dazu ein Offline-Riegel, unter dem jeder Hörwunsch als Processed wirkt, solange er steht". Der Riegel ist dort Mechanismus; die Zusage („Er stellt die Hörmatrix sofort um: `gewuenschteHoermatrix()` ist unmittelbar nach der Rückkehr Processed, und der erste danach gefahrene Block ist ab Sample 0 bitgleich zum bestätigten Lauf (M-42).") verlangt keinen Wunsch unter dem Riegel.
- M-43 (`NAK-312.md:832`), Zustand „wie M-42, danach Offline aus", Zusage „Der Rückweg blendet weiterhin WEICH: der Nachbarsprung am Umschaltsample liegt innerhalb der E-31-Schranke; die harte Umstellung gilt nur für die Offlinerichtung". Der letzte Halbsatz ist die Zusage, der die Rücknahme dient; die Vorbedingung enthält aber Offline-Audio, die Variante ohne Block ist nicht verlangt.
- M-53 (`NAK-312.md:847`) misst `RtWache::allokationen()` = 0 über 4000 Blöcke `processBlock`, ohne `setNonRealtime`.
- E-312-11 (`NAK-312.md:2398-2408`): „… die Zusagen von M-01, M-02, M-26, M-38 und M-53 („`RtWache::allokationen()` = 0" in B7) werden damit messbar …". M-40 fehlt darin.

Berichtigung zu „Von keiner Matrixzeile verlangt" und „Die Zeile M-40 sagt nur Sperren zu": Für die Zusagespalten stimmt das. Der Kopf der Matrix (`NAK-312.md:661-664`) sagt aber: „Keine Zeile verlangt im Audio-Thread eine Sperre, eine Allokation, einen Datei-, Pipe- oder Netzzugriff oder Logging; die Echtzeitzeilen M-09, M-26, M-38, M-40, M-53 und M-60 messen es je Änderungssatz." Er weist M-40 als Echtzeitzeile des Offline-Änderungssatzes aus, die „es" misst, also auch Allokationsfreiheit; Zusage und Fall von M-40 messen nur gemeldete Sperren. Diesen Abstand zwischen Matrixkopf und Zeile nennt der Bericht nicht.

Gate-Satz GRENZE für den Offline-Übergang:
- Sperren in `setNonRealtime`: B7 312/M-40 und 312/M-04 im Bereich wie der Wrapper. Gezählt werden nur Sperren über `RtWache::GemeldeteSperre` (`DspRtWache.h:46-53`, `:78-93`); eine `juce::ScopedLock` bliebe unsichtbar (M-04, Rotbeweisspalte, `NAK-312.md:691`: „den gemeldeten Adapter wieder durch `juce::ScopedLock` ersetzen → Zähler 0 → rot"). Für den Kernteil (`DspKern.h:340-352`, `DspKern.cpp:1216-1236`) schließt der B6-Textriegel `keine_sperre_am_wrapper_vorbei_im_kern` Sperrklassen unter `dsp/` aus (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:6629-6660`).
- Allokationen im Übergang misst kein Bein: B7 schärft den Zähler dort nie (c); B6 fährt `DspKern::verarbeite` mit Zähler ([SONDE-015] M-47), setzt aber nie eine Anforderung (kein Test ruft `setzeOfflineRiegel`). Die Allokationshälfte ist hier nur gelesen (nur Atomics; der Basisaufruf `juce_AudioProcessor.cpp:410-413` ist ein Atomic-Store).

## V-7 (H-3 und H-4) — PRÄZISIERT

(a) M-49, B2-Hälfte:
- Rohdatei: B2 lief unter Teil a (`docs/beweise/roh/NAK-312-rot-M-49.txt:27`, `:31`: „Lauf EqCopStateMigrationTest: Exitcode 0, 1 s, Summe: STATE-MIGRATION-TEST OK - 294 Pruefungen ok, 0 Fehler"), nach dessen Rücknahme (`:110`), unter Teil c (`:226`, `:230`) und nach dessen Rücknahme (`:314`). Teil b (`:132`) und Teil d (`:336`) bauten und fuhren nur `EqCopTransactionTest`. `:100`: „B2 uebersetzt SondeProcessor.cpp nicht (CMake-Ziel EqCopStateMigrationTest), sein Binary bleibt deshalb aelter als die mutierte Quelle - die Zeile frisch NEIN ist dort erwartet, B2 misst den Schreiber und bleibt unter jeder Teilmutation gruen (294/0)." Das Ziel übersetzt `NAKAMA_PROZESSOR_QUELLEN` und `src/…`, nicht `sonde/SondeProcessor.cpp` (`eq-copilot/plugin/CMakeLists.txt:789-798`). Auch die Gegenprobe fuhr B2 grün (`docs/beweise/roh/NAK-312-etappe5b-gegenprobe.txt:10`).
- „Nie rot gefahren" stimmt. Genauer: B2 lief unter zwei der vier Teile, und keine der vier Mutationen lag in einer Quelle, die B2 übersetzt; „unter allen vier Teilen grün" ist für b und d ungemessen. Rotfähig nach Bauart stimmt (`eq-copilot/plugin/tests/StateMigrationTestMain.cpp:2531`, `:2538`, `:2547`: fehlende oder leere Bin-Datei, fehlende oder abweichende `s1`-Zeile, ungleiche Rückschrift sind rot).
- M-49 wörtlich (`NAK-312.md:843`): Zusage „Die Statebytes sind über den Umbau hinweg bytegleich zu denen des Basis-SHA, und eine Automationsfahrt erzeugt dieselbe Epochenfolge wie vorher"; Test „B2 und B7 bestehend, um den Vergleich gegen das Golden des Basis-SHA ergänzt"; Rotbeweis „**Regressionswache**; einmal gebrochen: die Parameter ohne den bisherigen Namensraum anlegen → State weicht ab → rot".
- Matrixkopf (`NAK-312.md:648-650`): „**Regressionswache**: heute grün und hält, was der Bau nicht ändern darf; sie wird einmal absichtlich gebrochen und ist kein Beleg".
- A-59 (`NAK-312.md:5317-5321`): „Den Rotbeweis tragen deshalb die Zeilen der zwei Hälften: Teil c an `getStateInformation` (der Zeile, die die Reihenfolgespalte nennt) und Teil d an der Anmeldung `addListener` (die Anbindung, über die eine Automationsfahrt überhaupt Epochen erzeugt). Die Zusage der Zeile hält; B2 misst den Schreiber und bleibt unter jedem Teil grün."
- Die Zeile verlangt einen Bruch je Zeile, keinen je Bein; einen Rotbeweis an B2 verlangt sie nicht. Stimmt.
- Kontext für die Einordnung: Gate BEWEIS (`NAK-312.md:6`): „BEWEIS: docs/beweise/NAK-312.md mit Matrix, Rotbeweisen an B2, B6, B7, B14, B15, RT-Wache vor Parameterverarbeitung, vollem Kanon GRÜN und Laufzeit-Arm." Bis einschließlich Etappe 5 zeigt keine Rohdatei `docs/beweise/roh/NAK-312-*` einen roten B2-Lauf, und die Matrix plant keinen: M-77 (Etappe 6, `NAK-312.md:884`) nennt B2 nur als Einzelbein, ihr Rotbeweis fällt an B15.

(b) Textriegel 312/M-88:
- Schwelle stimmt: `TransactionTestMain.cpp:5965` `pruefe (alleOrdner && dateien >= 100 && treffer.empty(), …`; Bestand 103 (`git ls-files` und Dateisystem gleich: `src` 27, `sonde` 3, `dsp` 9, `state` 13, `vertrag` 8, `core` 42, `hostbridge` 1). Muster stimmt: `:5950` `dir.findChildFiles (juce::File::findFiles, true, "*.cpp;*.h")`; ungelesen bleiben die getrackten `eq-copilot/plugin/core/ipc/BrokerInstallBinding.h.in` und `eq-copilot/plugin/hostbridge/NakamaBrueckeStand.h.in`. Nachgesucht: kein Wort der Liste in beiden Vorlagen und in den hinzugefügten Zeilen von `third_party/patches/juce-8.0.9-nakama-vst3-bridge.patch`.
- R-312-19 wörtlich (`NAK-312.md:5584-5589`): „Das Plugin beendet oder blockiert seinen Host in keinem Betriebszustand. Gemessen wird das im Laufzeit-Arm über mindestens fünf Songumläufe Echtzeit-Wiedergabe des Diagnoseprojekts; verschwindet der Host oder antwortet er nicht mehr, ist das Szenario verfehlt (Exit 4). Sobald die Ursache bekannt ist, bekommt sie zusätzlich einen Fall im Kanon mit Rotbeweis an der Zeile, die die Zusage trägt."
- M-88 wörtlich (§31.3, `NAK-312.md:5589-5595`): „… Zusage „der Host lebt und antwortet nach jedem Umlauf, und beide Rollen beantworten den Briefkasten", Test neues Laufzeit-Szenario plus der Kanonfall an der Ursache …". §33.2 (`NAK-312.md:6122-6126`): „**M-88 wird berichtigt:** das Etikett „heute rot (Stand `64ece9e1`)" aus §31.3 entfällt — … M-88 ist **Regressionswache**, Rotbeweis wie in §32.7 (reguläres Schließen des runner-eigenen FL → Szenario Exit 4; `std::exit` im Pluginquelltext → Kanonteil rot). R-312-19 bleibt."
- A-66 (`NAK-312.md:5986-5988`): „… fail-closed (fehlt ein Ordner oder sind es weniger als 100 Dateien, ist er rot)."
- Weder M-88 noch R-312-19 verlangt eine Schwelle oder Dateitypen; „weniger Dateien als der Bestand ist rot" ist der Wortlaut von M-50 (`NAK-312.md:844`). Stimmt.
- Behauptungszeile B7 (`tools/beweise.ps1:728`), Schluss wörtlich: „Seit der Nacharbeit 1 der Etappe 5 (R-312-19, Kanonteil von 312/M-88) ruft kein Quelltext unter src, sonde, dsp, state, vertrag, core und hostbridge von eq-copilot/plugin ExitProcess, TerminateProcess, PostQuitMessage, FatalAppExit, exit, _exit, quick_exit, _Exit, Process::terminate oder systemRequestedQuit und keiner nennt WM_CLOSE, WM_QUIT, SC_CLOSE oder JUCEApplicationBase (Textriegel); ob der Host im Betrieb lebt, misst das nicht - das misst der Laufzeit-Arm." Auftrag an den Bauer (`docs/beweise/roh/NAK-312-etappe-5-auftrag.txt:11`): „keine Zeile behauptet mehr, als ihr Bein misst; je Bedingung genau das Gemessene". Die Zeile reicht um genau die zwei getrackten `.h.in`-Dateien unter `core/` und `hostbridge/` weiter als der Riegel; heute ist sie wahr. Den Wrapperpatch behauptet sie nicht, er liegt außerhalb der genannten Ordner.
- Berichtigung zu „… die zwei Kopfvorlagen …, aus denen Kopfdateien der Bündel entstehen": Das gilt für eine der zwei. `BrokerInstallBinding.h.in` wird zu `generated/nakama/BrokerInstallBinding.h` (`eq-copilot/plugin/CMakeLists.txt:88-89`) und ist in beiden Bündeln eingebunden (`sonde/SondeProcessor.cpp:4`, `src/PluginProcessor.cpp:38`). `NakamaBrueckeStand.h.in` wird zu `nakama-generiert/NakamaBrueckeStand.h` (`CMakeLists.txt:1242-1245`) und ist nur im Test eingebunden (`tests/HostContextTestMain.cpp:20`).

## V-8 (tragende Entlastungen) — BESTÄTIGT

(a) Kein Sperren, kein Allokieren, kein Warten; Atomics lock-free:
- `setNonRealtime` (`SondeProcessor.cpp:1560-1594`) fasst an: den Basisaufruf `juce_AudioProcessor.cpp:410-413` (`nonRealtime = newNonRealtime;`) auf `std::atomic<bool> nonRealtime { false };` (`juce_AudioProcessor.h:1603`), `offlineGesehen` (`:1573-1574`), `DspKern::setzeHoermatrix` (`DspKern.h:308`, ein Store), `setzeOfflineRiegel` (`:340-352`, zwei Stores) und `vorschauEndeAngefordert` (`:1593`). Die neuen Kernzeilen `DspKern.cpp:1216-1217`, `:1225`, `:1227-1236` fassen Atomics und drei Mitglieder des Audiothreads an (`DspKern.h:745-749`). Alle Aufgerufenen sind `noexcept` (`SondeProcessor.h:228`, `juce_AudioProcessor.h:989`, `DspKern.h:308`, `:340`).
- Lock-free: alle vier sind `std::atomic<bool>`; gebaut mit Visual Studio 17 2022, x64 (`eq-copilot/build/CMakeCache.txt:416`, `:420`), Compiler 19.44.35222 (`eq-copilot/build/CMakeFiles/3.31.6-msvc6/CMakeCXXCompiler.cmake:4`); MSVC-Kopf `C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Tools\MSVC\14.44.35207\include\atomic:60` `#define ATOMIC_BOOL_LOCK_FREE 2`.
- Zugriffe (`git grep` über `eq-copilot`): `offlineRiegelAn` `DspKern.h:344`, `:350`, `:353`, `DspKern.cpp:1225`; `hartSchalten` `DspKern.h:345`, `:349`, `DspKern.cpp:1216-1217`; `offlineGesehen` `SondeProcessor.cpp:1573-1574`; `vorschauEndeAngefordert` `:1593`, `:1808-1809`; Deklarationen `DspKern.h:742-743`, `SondeProcessor.h:631-632`. Wie berichtet. Randnotiz: der Getter `DspKern::offlineRiegel()` (`DspKern.h:353`) hat weder im Produkt noch in Tests einen Aufrufer.

(b) Parameteridentität:
- BASIS `baueParameterLayout` (BASIS `SondeProcessor.cpp:86-117`) gegen `hostParameterAnlegen` (`:94-126`): dieselbe Schleife über `parameter::tabelle()` bis `kHostParameter`, `const juce::ParameterID id { b.id, 1 };`, dieselben Klassen `AudioParameterBool`, `AudioParameterFloat`, `AudioParameterChoice` mit Name `b.id`, dieselbe `NormalisableRange<float> bereich ((float) b.min, (float) b.max)` mit `setSkewForCentre` für `.freq_hz`, `.q`, `.attack_ms`, `.release_ms`, dieselben Defaults, keine Einheit, keine Kategorie. Einzige Änderung: `layout.add (…)` wurde `neu = …; prozessor.addParameter (neu.release());`. Die APVTS registrierte in derselben Reihenfolge (`juce_AudioProcessorValueTreeState.cpp:225-235`: `addParameterAdapter`, dann `processor.addParameter`) und startete zusätzlich Timer und Baumhörer (`:271-276`).
- Am BASIS nennt der Code `parameterBaum` nur an BASIS `SondeProcessor.cpp:229` und `SondeProcessor.h:716` (`git grep` über `eq-copilot` am BASIS); kein `getRawParameterValue`, `copyState`, `replaceState` oder Attachment unter `sonde/`; `hostParameter` kam am BASIS wie heute über `getParameters()` (BASIS `:248-254`, heute `:262-269`); die Sonde hat keinen Editor (`SondeProcessor.h:196-197`). Randnotiz: die im Bericht genannten BASIS-Zeilen `:86` und `:88` sind die Layoutfunktion, nicht der Name `parameterBaum`.

(c) Golden:
- Erzeugt am BASIS: die SHA-256 der Produktquellen in `docs/beweise/roh/NAK-312-parameter-golden-erzeugung.txt:10-13` gleichen den BASIS-Blobs in Arbeitskopie-Zeilenenden (selbst gemessen: `09dc9f64…`, `f9b1a7b9…`, `e7a53ddc…`, `b0681bcf…`; Testdatei `18f63aa9…`).
- Eingefroren: Text 29 665 Byte, SHA-256 `98d29988…7bef`, 125 Zeilen; Bin 5 745 Byte, SHA-256 `4ea7c41e…7810` gleich der Zeile `s1` (`docs/beweise/roh/NAK-312-parameter-golden.txt:118`); beide einmal committet in `be37d10a`, seither unverändert.
- Fail-closed: B7 `TransactionTestMain.cpp:5702-5724` (`vorhanden`, `gleich = ! a.empty() && a == b` `:5679`, `s1Golden.getSize() > 0`); B2 `StateMigrationTestMain.cpp:2531`, `:2538`, `:2547`.
- Zeilenendenfest: B7 schneidet `\r` ab (`:5660`); B2 teilt mit `StringArray::fromLines` (`juce_StringArray.cpp:370-397`, `:388` `case '\r':  if (*text == '\n') ++text; break;`) und `trimEnd` (`:2535`).
- `git check-attr`: Bin `binary: set`, `diff: unset`, `merge: unset`, `text: unset`; Text alle Attribute `unspecified`. `git ls-files --eol`: Bin `i/-text w/-text attr/-text`, Text `i/lf w/lf`; `core.autocrlf` = true. Wie berichtet.

(d) Testzugänge: die neuen Zugänge `SondeProcessor.h:453-458` liegen im Block `#if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` `:304-535`; alle `…FuerTest` des Kopfs liegen in den Blöcken `:158-175`, `:304-535`, `:667-671`, `:728-732`. Das Define setzen nur Konsolen-Testziele (`eq-copilot/plugin/CMakeLists.txt:409`, `:439`, `:503`, `:519`, `:565`, `:600`, `:877`, `:1181`, Schleife `:1331-1338`); das Sondenziel (`nakama_sonde_ziel`, `juce_add_plugin` `:323`, Definitionen `:350-357`) und `EqCopilot` setzen es nicht.

(e) Umfang: `git diff --stat 12300f1e 0014d5f7 -- . ':(exclude)docs'` zeigt die sieben Code- und Runnerpfade des Prüfbereichs und `eq-copilot/install/nakama-installer-v1.json`, sonst nichts; `git diff --name-status 12300f1e 0014d5f7 -- eq-copilot/identity eq-copilot/schemas broker` ist leer.

## Nicht geprüft

- Kein Bau, kein Test, kein Kanon, kein cargo, kein FL Studio, kein Python-Interpreter; keine Mutation gefahren. Alle Mutationsüberleber (H-2, V-6) sind gerechnet. Ergebnisse von Kanon, Laufzeit-Arm und Rotläufen stammen aus den Rohdateien.
- Wie FL den Render fährt (`setupProcessing`, `setActive`, Thread, Gleichzeitigkeit mit `process`): im Repo nicht gemessen und von mir nicht gemessen.
- JUCE: nur die zitierten Stellen von Wrapper, APVTS, `AudioProcessor`, `StringArray` und `juce_VST3Common.h`; vom SDK nur die Kopfdatei, keine weitere SDK-Dokumentation. Den Wrapperpatch habe ich nur in seinen hinzugefügten Zeilen nach der Wortliste von 312/M-88 durchsucht.
- Manifest-, Register- und Planprosa außerhalb der genannten Referenzen; die Gegenproben (§30.4, §30.7) nur als Kontext; das Szenario `schleife-dauerlauf`; die Etappen 6 und 7.
- Eine Zwischenkopie der BASIS-Datei (`git show 12300f1e:eq-copilot/plugin/sonde/SondeProcessor.cpp`) lag im Job-Temp-Ordner außerhalb des Repos. Im Repo ist dieser Bericht die einzige neue Datei; nichts gestagt, committet oder gepusht.

HEAD zu Beginn: `cf410c58c46f320e53b5ee324ee912da369548d9` (erstes Kommando der Session, `git rev-parse HEAD`). HEAD am Ende: `cf410c58c46f320e53b5ee324ee912da369548d9` (20:06:06 vor dem Schreiben, nach dem Schreiben erneut gemessen, gleich).

FERTIG Validierung Erstprüfung Etappe 5, 3 bestätigt, 5 präzisiert, 0 widerlegt
