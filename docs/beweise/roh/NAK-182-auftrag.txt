NAK-182 — G4-Nacharbeit 3: Korpus und Goldens. Ticketauftrag des Dirigenten (2026-09-06).

DU BIST der frische Opus-Bauer (max). Repo C:/Users/phili/Projekte/Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-182.md ein (Zeile „Basis-SHA"; dazu eine Zeile „Regeln | R1 bis R6 wörtlich in docs/beweise/roh/NAK-182-auftrag.txt" und die Zeile „Ticketpfade" auf die unten genannte Menge nachziehen). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach origin master als eigener Schritt nach `git pull --rebase` bei sauberem Worktree. Keine Neben-Refactors, keine Legacy-Umbenennung, keine Umbenennung bestehender Testbezeichner (der Korpus zieht auf die Tests, nicht umgekehrt). v3-Verträge sind eingefroren: Discriminator, Zieladresse, Revision, Capability und Encodings werden nie angefasst; dieses Ticket erwartet KEIN Schemafeld und KEINEN Produktcode — findet die Matrix eine Zusage, die das Produkt nicht hält, steht das als Befund in §2 und §3, und der Dirigent entscheidet die Regel vor dem Bau. Vor jedem Commit tools/dirigent/pruefliste.md abhaken und im Manifest nennen, wo jede Zeile gemessen wurde. Landminen: der MSVC-Standardstack ist 1 MiB — Engines und Prozessoren in Tests auf den Heap (std::unique_ptr), nie im Rahmen; PowerShell 7 deutet deutsche Anführungszeichen in doppelt zitierten Strings als Begrenzer — Textbausteine nur in wörtlichen Here-Strings (@'…'@) oder per Write-Werkzeug; bytegleich geprüfte Fixtures brauchen passende .gitattributes-Regeln (A25 `erzeuge_p4_korpus.py --pruefen` erzwingt Bytegleichheit samt MANIFEST); keine GPU-Batchläufe.

TICKETGRENZE — genau diese an der Quelle bestätigten Befunde des Gates G4 (docs/beweise/G4.md §1 Satz 1, §5, §6 Satz 1, §8, §11 Nr. 3) mit ihrer Ursache: das Exit-Gate-Bein A26 misst nicht, was M-79 bis M-85 (docs/beweise/SONDE-013.md §3.10, Zeilen 621-627) zusagen. Quellenchecks mit Zeilenbelegen: docs/beweise/roh/G4-quellencheck-codex-9f30008.md (C6, C7, C9, C11 mit Fallnamentabelle), docs/beweise/roh/G4-eigener-lauf-9f30008.md (B5-Tabelle M-79 bis M-86, L-01 bis L-03), docs/beweise/roh/G4-codex-falsifikation-9f30008.txt. Zeilenangaben gelten für den Gate-Stand 9f30008; seit NAK-180/NAK-181 können Testdateien verschoben sein — an der Quelle neu verorten.

C6 (M-79; Gate 6 und 7 im Prüfer) — `tools/eq-copilot/pruefe_p4_korpus.py` `_passt` (:139) kennt nur `unbekannt`; `moegliche_starke` (:91-92) schließt zusätzlich `unvergleichbar`, `nicht_kausal`, `nicht_exakt` aus. Ein Korpusfall mit einer dieser drei Wahrheiten und `aussage=stark` bleibt grün, `recall` kann über 1,0 steigen, und kein Riegel reagiert. Der einzige Beleg ist der Rotbeweis docs/beweise/roh/SONDE-013-rot-M-79.txt, der ausgerechnet den einen Wert manipuliert, den `_passt` fängt (`unbekannt`); dieselbe Manipulation an `gate6_incomparable_never_gets_a_strong_winner` (`unvergleichbar`) bliebe grün. Es gibt kein Testartefakt für das Prüfskript.
C7 / L-03 (M-81, Namensbindung) — `pruefe_p4_korpus.py:161-163` prüft nur die Existenz des BEINS im Runner (`_bein_bekannt`, Regex auf `Kuerzel = '…'`); das Feld `fall` wird nirgends gegen eine Quelle geprüft. Nur 8 von 22 Fallnamen existieren wörtlich (Tabelle im Quellencheck C7: 13 Namensdrift mit Sachäquivalent, einer substanzlos). `B18::lufs_integrated_matches_reference` gibt es weder wörtlich noch sachlich: `Sonde013DynamicsTest.cpp` führt keinen LUFS-I-Referenzfall; die ±0,1-LU-Prüfung liegt in B9 `EqCopLoudnessGoldenTest` (`LoudnessGoldenTestMain.cpp`, Kopfkommentar B: „Integrierte Lautheit gegen die Referenz, Toleranz ±0,1 LU", und die Referenz dort ist die ausgebaute Rechnung, kein externes Testset — Kopfkommentar Zeile 6-10). M-81 sagt „gegen eine validierte Referenz auf Standard-Testmaterial".
C8 / L-01 (M-82) — `Sonde013StereoGoldenTest.cpp` Speiser `frames { 512 }` (:78 am Gate-Stand); kein Fall variiert die Blockgröße. M-82 sagt „über alle unterstützten Blockgrößen"; die Runnerbehauptung von B19 (`tools/beweise.ps1`, Kuerzel B19) verspricht die Blockgrößen nicht, die Matrixzeile tut es. B4 `QueueStressTestMain.cpp` fährt 18 Blockgrößen 1…16384 (Abschnitte I und J) — die Definition, was „unterstützt" heißt, liegt dort oder im Vertrag; die Matrix belegt sie.
C9 (M-83 Satz 2) — „fokussierte 0,01-dB-Evidenz muss Gain innerhalb ±0,1 dB samt statistischem Intervall wiederfinden" ist nirgends gemessen. Gebaut ist die Kodierung (`BandGrid.h`, `FeatureEngine.h`, `broker/src/coordinator/evidenz.rs:335` `q_db_0p01_i16 => 100.0`, `telemetrie.rs`), gemessen nur die Grenzwertkonsistenz (`contract_cross_language.rs`, `SchemaTestMain.cpp`). B24 nutzt +15 dB nur für die Kohärenz, B25 prüft den Block-Bootstrap isoliert ohne dekodierte Bandevidenz, die Rust-Hüllkurven in `sonde013_prepost.rs:33-104` kommen nie durch `p50_dekodieren`. `Dreifachergebnis` (`prepost.rs:294-313`) trägt `roh_db`, `ausgerichtet_db`, `match_gain_db`, `wirkung` — kein Intervallfeld; ob und wo das Produkt aus dekodierter 0,01-dB-Evidenz einen Gain MIT Intervall liefert, ist die erste Frage der Matrix.
C10 / L-02 (M-84) — „Bekannte Impulsereignisse bleiben über Blockgrößen und Sampleraten zeitlich stabil": gemessen ist nur die Grenzhälfte (B5/B4); der kurze Impuls (`AnalysisGoldenTestMain.cpp` `short_impulse_triggers_peak_path_only`, ab :2123 am Gate-Stand) läuft bei EINER Blockgröße und Samplerate. Ein Sweep über Blockgrößen UND Sampleraten für die Zeitlage existiert nicht.
C11 (M-85 Ziel 1; Entscheid G4 §8) — Der Korpusfall `B24::nonlinear_chain_never_yields_static_eq_claim` trägt `wahrheit: nicht_kausal`, der Test fährt aber kausale, zeitvariable Ketten desselben Quellsignals (Kompression, Modulation, Saturation, wechselnde Latenz); ein korrelierter, nicht kausaler Distraktor kommt in keinem Code, Test oder Fixture vor. Entscheid des Dirigenten (G4 §8): Ziel 1 ist P5-Lieferumfang (NAK-190); die P4-Fassung ist nur, dass der Korpus nichts behauptet, was B24/A4 nicht messen, und dass kein P4-Datenweg einen „sicheren Auslöser" benennt.

REGELN, die die Befunde schließen (Technikentscheid des Dirigenten; Form, Reihenfolge und Mittel entscheidet deine Verhaltensmatrix, die Codex vor dem Bau prüft):
R1 (C6, M-79) — `_passt` und `moegliche_starke` lesen dieselbe benannte Menge `UNSICHERE_WAHRHEITEN = ("unbekannt", "unvergleichbar", "nicht_kausal", "nicht_exakt")`; eine starke ODER schwache Aussage auf einer dieser Wahrheiten ist eine falsche Behauptung; `precision <= 1.0` und `recall <= 1.0` sind Riegel (Verletzung = rot, Exit ≠ 0). Die Menge der Wahrheiten wird im Erzeuger geschlossen geführt (`ERLAUBTE_WAHRHEITEN`, wie `ERLAUBTE_AUSSAGEN`), und der Prüfer weist eine Wahrheit außerhalb der Menge ab, damit keine dritte Schreibweise entsteht. Das Prüfskript bekommt einen Selbsttest im Kanon (eigenes Bein `pruefe_p4_korpus.py --selbsttest` oder Vorlauf in A26 — die Matrix entscheidet, die Bein-Behauptung sagt es), der ohne Repo-Fixture je Wert aus `UNSICHERE_WAHRHEITEN` einen synthetischen Fall mit `aussage=stark` durch `_kennzahlen` schickt und `falsche_starke == 1` erwartet, dazu je einen Fall für `recall > 1` und für einen erfundenen Fallnamen (R2) — jede Erwartung mit ihrem Gegenteil. Rotbeweise: die alte `_passt`-Form → Selbsttest fällt; der bestehende Rotbeweis SONDE-013-rot-M-79 wiederholt an `gate6_incomparable_never_gets_a_strong_winner` (`unvergleichbar` → `stark`) → A26 fällt jetzt.
R2 (C7 / L-03, M-81) — Jeder Korpusfall trägt `quelle`: Pfad relativ zur Repo-Wurzel plus wörtlicher Bezeichner (C++: der Abschnitts- oder Fallname, wie er im Test steht; Rust: der `fn`-Name); `pruefe_p4_korpus.py` prüft, dass der Bezeichner wörtlich in der genannten Datei steht, sonst rot. Die 14 driftenden Fallnamen werden im Korpus auf die tatsächlichen Bezeichner gezogen; hat ein Test keinen benennbaren Bezeichner (Sachäquivalent nur als Prosa-Abschnitt), bekommt der Abschnitt im Test einen stabilen Bezeichner, der die Sache trifft — bestehende Bezeichner werden nie umbenannt. `lufs_integrated_matches_reference` zeigt auf das Bein, das ±0,1 LU wirklich misst (nach Quellencheck B9, `LoudnessGoldenTestMain.cpp`, Abschnitt B) mit dessen echtem Bezeichner und `bein: B9`; `KLASSENBEINE["referenz"]` führt B9 dann mit; kein neuer B18-Fall (Doppelmessung). Der `hinweis` dieses Falls sagt, wogegen B9 misst (ausgebaute Rechnung), und die Matrix hält fest, ob damit M-81 „validierte Referenz auf Standard-Testmaterial" gedeckt ist oder ob die Zusage in der Belegspalte ehrlich zu beschreiben ist — kein neues Testset in diesem Ticket, keine Behauptung darüber hinaus. Rotbeweis: erfundener Fallname im Korpus → A26 rot; Bezeichner im Test entfernt → A26 rot.
R3 (C8 / L-01, M-82) — B19 fährt die fünf Klassen (Mono, identisches Stereo, Polaritätsinvertierung, bekannte Laufzeit, unkorrelierte Kanäle) und den Folddown über die Menge der unterstützten Blockgrößen; die Menge steht genau EINMAL im Test und ist an der Quelle belegt, aus der B4 seine Blockgrößen nimmt (oder aus Vertrag/Capabilities, wenn dort eine engere Menge steht — die Matrix zitiert die Stelle). Erwartung je Blockgröße: dieselbe analytisch bekannte Antwort wie heute (Korrelation, Kohärenz, Phase nach +2·pi·f·tau, Folddown innerhalb 0,25 dB, Präsenzbits), und der Test belegt, dass je Blockgröße vergleichbar viele Frames gemittelt wurden (kein Fall besteht, weil zu wenig Material floss). Die Matrix nennt das Laufzeitbudget des Sweeps (Ziel: B19 bleibt unter 60 s; sonst begründete Blockgrößen-Teilmenge mit 1, der kleinsten und der größten Größe, einem Hop-Teiler und einem Hop-Nichtteiler — und die Bein-Behauptung sagt „über die Menge X", nie „über alle", wenn es nicht alle sind). Rotbeweis, der zeigt, dass der Test Blockgrößen unterscheidet (z. B. Test-Speiser, der bei Blockgröße ≠ 512 einen Kanal um ein Sample versetzt → known_delay und mono fallen genau dort) — Form entscheidet die Matrix.
R4 (C9, M-83 Satz 2) — Der gekoppelte 0,01-dB-Evidenzpfad wird als EINE Kette gemessen: echter C++-Encoder (FeatureEngine auf bekanntem Material → `NakamaEvidenz` `evidence_snapshot` mit `encoding: q_db_0p01_i16`; PRE mit bekanntem Bandpegel, POST = PRE + bekannter Gain G) und echter Rust-Decoder (`evidenz.rs` `p50_dekodieren`) samt PRE/POST-Auswertung (`prepost.rs` / `prepost_verdrahtung.rs`): das Ergebnis findet G je Band mit Bit innerhalb ±0,1 dB wieder und trägt ein statistisches Intervall, das G enthält. Erste Aufgabe der Matrix (§3): den Produktpfad benennen, der aus dekodierter 0,01-dB-Evidenz Gain UND Intervall liefert (Kandidaten: `Dreifachergebnis` im Broker ohne Intervallfeld; Block-Bootstrap aus B25 im Plugin auf Experimentseite; `sicht.rs`-Serialisierung). Gibt es keinen solchen Pfad ende-zu-ende, steht das als Befund in §2/§3 mit der Stelle, an der das Intervall fehlt, und die Matrix schlägt den kleinsten Messweg vor, der die Zusage OHNE Schemaänderung ehrlich misst (Gain-Wiederfindung im Broker ±0,1 dB als eine Zeile, Intervall aus dem vorhandenen Bootstrap als zweite Zeile, und die Kopplung beider als dritte) — der Dirigent entscheidet vor dem Bau. Kopplung der Sprachen über eine Datei, die beide Seiten messen und keine schreibt (Muster NAK-181 `eq-copilot/fixtures/v3/evidenz-zahlen-wire-v1.json` aus `erzeuge_v3_fixtures.py`, A8 `--pruefen` bytegleich, A5 liest mit) oder — wenn der echte Encoder nicht bytegleich reproduzierbar ist — die C++-Hälfte dekodiert ihren eigenen Wire-Text mit der Vertragsformel und findet G ±0,1 dB, die Rust-Hälfte dekodiert eine mit derselben Formel erzeugte Fixture und wertet PRE/POST aus; die Matrix wählt und begründet gegen M-83. Rotbeweis: Dekoderskala 10 statt 100 → Rust-Hälfte fällt; Encoder auf `q_db_0p1_i16` → C++-Hälfte fällt an ±0,1 dB nicht zwingend — dann muss die Zeile die Auflösung selbst messen (Schrittweite 0,01 dB im Wire-Text).
R5 (C10 / L-02, M-84) — B5 bekommt einen Sweep: ein bekannter kurzer Impuls bei bekannter Hostzeit t0 (Nachbar von `short_impulse_triggers_peak_path_only`, dessen Phasenfalle im Kommentar zu beachten ist) läuft über die Blockgrößenmenge aus R3 × die Sampleraten, die B17 fährt (44,1/48/88,2/96/192 kHz); die gemeldete Ereigniszeit (Stempel des Ereignisses auf dem Draht oder im FeatureFrame — die Matrix nennt das Feld) liegt bei jeder Kombination innerhalb der Toleranz bei t0, die die Matrix aus dem Entwurf ableitet (§38.5 „innerhalb eines Feature-Hops" bzw. die Hop-Definition der Engine; wenn die Zeitlage blockgrößenunabhängig sampleexakt ist, sagt die Zeile das und misst schärfer), und die Zeitlage ist bei gleicher Samplerate über alle Blockgrößen identisch bis auf die benannte Toleranz. Rotbeweis: Test-Speiser, der den Impuls bei Blockgröße ≠ 512 um einen Block verschiebt → Sweep fällt genau dort.
R6 (C11, M-85; Entscheid G4 §8) — Der Korpus behauptet zu M-85 nur, was gemessen wird: der B24-Fall trägt eine Wahrheit, die benennt, was der Test baut (nichtlineare/zeitvariable Kette desselben Signals → keine statische EQ-Behauptung), nicht „nicht kausal"; der Distraktor-Wortlaut verschwindet aus Fällen, Hinweisen und MANIFEST des P4-Korpus; das MANIFEST führt ausdrücklich „in P4 nicht gemessen: M-85 Ziel 1 (korrelierter, nicht kausaler Distraktor) → NAK-190", und A26 druckt diese Zeile, damit die Lücke im Kanon sichtbar bleibt statt still zu fehlen. Dazu eine Messung, dass kein P4-Datenweg einen „sicheren Auslöser" benennt: die geschlossenen Mengen des Vertrags für `urteil`, `klasse`, `kettenbefund`, `ausschluss` (und die M-46-Aussagen) enthalten keinen kausalen Bezeichner — Form (Schema-Enum-Lesung in A26 oder Testfall in A5) entscheidet die Matrix. Rotbeweis: Distraktor-Fall mit `aussage=stark` in den Korpus → A26 rot (über R1).
R7 (Bein-Behauptungen) — `tools/beweise.ps1` sagt für A26, B19, B5 und jedes von R1 bis R6 berührte Bein (ggf. B9 in der Klasse `referenz`, B16/A4 für die Kettenhälften, ein neues Selbsttestbein) genau, was jetzt gemessen wird — nichts darüber hinaus; docs/beweise/SONDE-013.md wird NICHT angefasst (Nachtrag durch den Dirigenten am Ticketende).

TICKETPFADE: tools/eq-copilot/pruefe_p4_korpus.py, tools/eq-copilot/erzeuge_p4_korpus.py, eq-copilot/fixtures/p4-korpus/, eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp, Sonde013PrePostGoldenTest.cpp, Sonde013DynamicsTest.cpp, Sonde013TruePeakGoldenTest.cpp, Sonde013FingerprintGoldenTest.cpp, Sonde013EventWireTest.cpp, AnalysisGoldenTestMain.cpp, LoudnessGoldenTestMain.cpp, QueueStressTestMain.cpp, SondeNullTestMain.cpp (Bezeichner), eq-copilot/plugin/CMakeLists.txt (nur falls ein neues Testziel nötig ist), broker/tests/sonde013_prepost.rs, broker/tests/sonde013_verdrahtung.rs, broker/tests/sonde013_experiment.rs, sonde013_passage.rs, sonde013_taint.rs (Bezeichner), eq-copilot/fixtures/v3/ und tools/eq-copilot/erzeuge_v3_fixtures.py (nur falls R4 die gemeinsame Datei wählt), .gitattributes (nur falls neue bytegleiche Fixtures), tools/beweise.ps1 (nur Bein-Behauptungen und ggf. ein neues Bein), docs/beweise/NAK-182.md, docs/beweise/roh/NAK-182-*. Kein Produktcode unter eq-copilot/plugin/src, core, sonde, vertrag, state und broker/src ohne Entscheid des Dirigenten nach der Matrixprüfung.

SPEZIFIKATION VOR CODE (Etappe 1): Zuerst die Verhaltensmatrix in docs/beweise/NAK-182.md §2 — je Regel R1 bis R6 ein Entscheid E1 bis E6 mit Begründung (Vorbild docs/beweise/NAK-181.md §2.0), dann je Korpusklasse (referenz, zeitachse, vergleich, stereo) und je Zusage M-79 bis M-85 die Zeilen: Zusage wörtlich × messender Fall (Bein, wörtlicher Bezeichner, Datei) × Negativfall/Rotbeweis × Erwartung mit Zahl (Toleranz, Blockgrößenmenge, Sampleraten, Laufzeitbudget); dazu die vollständige Tabelle aller Korpusfälle nach der Korrektur (bein, fall, quelle, wahrheit, aussage, was der Test wirklich misst) und die Selbsttestfälle des Prüfskripts. §3 „Ist-Stand an der Quelle" mit den Zeilen am aktuellen HEAD, auf die jede Matrixzeile baut — darin als Erstes die Antwort auf R4 (welcher Produktpfad liefert Gain und Intervall aus 0,01-dB-Evidenz; wo fehlt es). §4 „Bauplan" (Reihenfolge, Ticketpfade, Rotbeweise, betroffene Beine, erwartete Laufzeiten). Dann Manifest mit Pathspec committen und pushen (nur docs/beweise/NAK-182.md), und deinen Turn mit der letzten Zeile „ETAPPE 1 ABGESCHLOSSEN" beenden — KEIN Bau vorher, keine Nachfrage an den User. Der Dirigent fährt die lesende Codex-Matrixprüfung, trägt „Matrixprüfung 1" ins Manifest ein und setzt dich mit einem Zusatzauftrag fort.

BAU (Etappe 2, erst nach der Fortsetzung durch den Dirigenten): Prüfskript, Erzeuger, Korpus-Fixtures (neu erzeugt, A25 `--pruefen` grün), Tests (Plugin und Rust), Bein-Behauptungen, Manifestabschnitte (Bauetappen, Rotbeweise, Abweichungen, Nebenbefunde; Nebenbefunde NICHT ins Register — das zieht der Dirigent nach). Beine fahren: A25 `erzeuge_p4_korpus.py --pruefen`, A26 `pruefe_p4_korpus.py` (und der Selbsttest), `EqCopSonde013StereoGoldenTest` (B19), `EqCopAnalysisGoldenTest` (B5), `EqCopSonde013PrePostGoldenTest` (B24), `EqCopSonde013DynamicsTest` (B18), `EqCopSonde013TruePeakGoldenTest` (B17), `EqCopLoudnessGoldenTest` (B9), `EqCopQueueStressTest` (B4), `EqCopSonde013EventWireTest` (B16), `EqCopProbeeqNullTest` (A16), `EqCopNullTest`, `cargo test` für `sonde013_prepost`, `sonde013_verdrahtung`, `sonde013_experiment`, `sonde013_passage`, `sonde013_taint`, `contract_cross_language`, und A5 `pruefe_v3_vertrag.py` plus A8 `erzeuge_v3_fixtures.py --pruefen`, falls R4 die gemeinsame Datei wählt. Rohausgaben nach docs/beweise/roh/NAK-182-<sha>.txt, Rotbeweise nach docs/beweise/roh/NAK-182-rot-<kennung>.txt. Danach der volle Kanon, abgekoppelt (nie als sitzungsgebundener Hintergrundbefehl):
  $log = Join-Path $env:TEMP 'nakama-nak182-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-182.md -Anhaengen -Titel NAK-182 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
Fertig, wenn die letzte Logzeile mit EXIT= beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); kein Stand mit Laufstatus NOT RUN wird übergeben. Committen mit Pathspec (Prüfskript, Erzeuger, Fixtures, Tests, Runner-Behauptungen, docs/beweise/NAK-182.md, docs/beweise/roh/NAK-182-*), pushen. Kein Anfassen von docs/beweise/G4.md, docs/beweise/SONDE-013.md, docs/beweise/NAK-180.md, docs/beweise/NAK-181.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/. Am Ende deinen Turn mit der letzten Zeile „ETAPPE 2 ABGESCHLOSSEN <end-sha>" beenden.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen (Zahlenränder, NaN/Inf, Toleranzen als Zahl statt Prosa, Blockgrößen 1 und 16384 wirklich gefahren, Sampleraten wirklich gewechselt, Bezeichner wörtlich statt ähnlich, Rotbeweis fällt an der Zusage und nicht an einem Nebeneffekt, Bein-Behauptungen und Korpus-Hinweise sagen die Wahrheit), Fixtures bytegleich reproduzierbar, kein Produktcode, kein Schemafeld, keine Umbenennung bestehender Testbezeichner.
