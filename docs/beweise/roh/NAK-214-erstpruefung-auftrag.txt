Erstprüfung NAK-214 / G5-Nacharbeit 3 (Ticket NAK-214, Etappe 2) — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer. Prüfbereich ist ausschließlich der Änderungssatz dieses Tickets:
`git diff 6cfb256c7b9214dcf3531efc79b32b34b488ffc7...184fe645a4404b6f4aeb7c20ca11d5a9a8da9598 -- broker/src/coordinator/befehl.rs broker/src/coordinator/experiment.rs broker/src/coordinator/hypothese.rs broker/src/coordinator/hypothese_verdrahtung.rs broker/src/coordinator/intervention.rs broker/src/coordinator/link.rs broker/src/coordinator/prepost.rs broker/src/coordinator/proposal.rs broker/src/coordinator/proposal_verdrahtung.rs broker/src/coordinator/zustand.rs broker/tests/sonde013_experiment.rs broker/tests/sonde013_prepost.rs broker/tests/sonde014_gegenbeispiele.rs broker/tests/sonde014_nacharbeit1.rs broker/tests/sonde014_nacharbeit2.rs broker/tests/sonde014_proposal.rs broker/tests/sonde014_verdrahtung.rs eq-copilot/plugin/src/SourcesModel.cpp eq-copilot/plugin/src/SourcesModel.h eq-copilot/plugin/tests/Sonde012SourcesModelTest.cpp eq-copilot/plugin/tests/Sonde014BefundTest.cpp tools/beweise.ps1`
mit BASIS = 6cfb256c7b9214dcf3531efc79b32b34b488ffc7 (Codestand identisch mit dem Kanonstand d3c6709, Abschlusskanon NAK-213 GRÜN 61/61) und ZIEL = 184fe645a4404b6f4aeb7c20ca11d5a9a8da9598 (HEAD; er muss vor und nach deinem Lauf identisch sein; Codestand identisch mit ac20c7a, dem Stand des Abschlusskanons — die Commits danach ändern nur docs/, CLAUDE.md, .claude/ und den Planstand). Alles andere, was seit BASIS im Repo geändert wurde, gehört zu anderen Tickets oder zur Kontextpflege und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); keine Prozesse des Produkts, kein cargo, kein cmake, kein Python-Interpreter; du schreibst nichts.
Prüfstufe T3-Nacharbeit (Gate G5, docs/plan/plan.json Schritt G5 "stufe": "T3").

GATE-TEXT, wörtlich und vollständig (docs/bauaufteilung-sonden.md:395-400):
„**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — „G5 ist das ungewöhnlichste Gate: Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer."
P5-Exit-Gate, wörtlich (docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung." Entwurf §36.1 (Zeile 2359): „Wirkungsbeleg: Ein PRE/POST-Paar zeigt eine reproduzierbare Veränderung durch eine Kette." SONDE-014 M-17: „Aus paralleler Telemetrie allein entsteht nie Klasse 2 oder 3 … 2. Wirkungsbeleg (PRE/POST-Paar mit reproduzierbarer Veränderung)." SONDE-013 M-58: „Das Ende allein genügt nicht: der Nachlauf muss abgelaufen sein." Hartes Gate 6 (Entwurf §49.2, Zeile 3571): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil."

Mehr verlangt das Gate nicht. Die Verhaltensmatrix steht in docs/beweise/NAK-214.md §2 (Entscheide E1 bis E9 in §2.0, Zeilen V-01 bis V-49 in §2.1 bis §2.5, die drei Lückenstellen mit erwarteter Ausgabe in §2.6, präzisierte Referenzzeilen in §2.7, „kein Fassungsschritt" in §2.8, Nicht-Zusagen in §2.9); sie ist von vier Codex-Matrixprüfungen abgenommen (PASS, Thread 01a08106-d4aa-7113-a024-437746053272, Stand c387891) und ist Referenz. Die Regeln R1 bis R5 des Dirigenten stehen in docs/beweise/roh/NAK-214-auftrag.txt und docs/beweise/G5.md §11 Nr. 3; R6, R7, R8 in docs/beweise/roh/NAK-214-matrixnacharbeit-1-auftrag.txt, die R7-Präzisierung (Terminal lässt die Nachlaufzuordnung unberührt) in NAK-214-matrixnacharbeit-2-auftrag.txt, die V-12-Regel (Paarbildung als testbare Funktion) in NAK-214-matrixnacharbeit-3-auftrag.txt; die Ticketbefunde des Gates in docs/beweise/G5.md §4, §6, §8, §11 Nr. 3 (E-D5 = A6, E-D6, E-L2, E-L5, A5). Unverändert geltende Referenzzeilen: SONDE-014 §3 M-25 (Determinismus), M-30 (Sperre am Zustand), M-46 (vollständiges Objekt oder keines), M-52 (`darf_draft_offer`), M-57 (ein Angebot je Sitzung), M-77 (dieselbe Regel in allen Sprachen), M-84 (Befundzähler fällt mit seiner Quelle), M-87; SONDE-013 M-58, M-61, M-62; NAK-212 §2 N-43; NAK-213 §2 E7/R4 (führendes Main), R8 (Enthaltung ohne Ort setzt Beleg voraus). Bauetappen A bis F mit gemessenen Beinen, Rotbeweisen und begründeten Abweichungen von der Matrix (fünf, nummeriert) in docs/beweise/NAK-214.md §6; zwei rote Kanonläufe und ihre Ursache (Tests aus NAK-212/213, die die von R4 geschlossene Abweichung festschrieben) im Verlauf §5.
Die Riegel/Tests, die es messen: broker/tests/sonde014_proposal.rs, sonde014_verdrahtung.rs (Bein A4), sonde013_prepost.rs, sonde013_experiment.rs, sonde014_gegenbeispiele.rs (Bein A31, Aufruf in tools/beweise.ps1 — die drei NAK-214-Fälle aus §2.6), Unit-Testmodul in proposal_verdrahtung.rs (Paarbildung); eq-copilot/plugin/tests/Sonde014BefundTest.cpp (B28) und Sonde012SourcesModelTest.cpp (B13); Rotbeweise je Matrixzeile unter docs/beweise/roh/NAK-214-rot-V-*.txt (22). Jüngster Kanon: GRÜN 61/61 auf ac20c7a (docs/beweise/roh/NAK-214-ac20c7a.md; A4 749 bestanden, 0 gescheitert, 22 ignoriert über 29 Testbinaries; B28 80/80; B13 85/85). Laut Manifest §2.8 gibt es keinen Fassungsschritt — die Beine A5 und A8 müssen unverändert grün sein; prüfe, dass eq-copilot/schemas/v3/** und die Vertragswerkzeuge im Prüfbereich nicht vorkommen.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: verletzt einen Satz des Gate-Textes oben, eine Zeile der Verhaltensmatrix (V-01 bis V-49) oder einen Entscheid E1 bis E9, eine Regel R1 bis R8 samt Präzisierungen, einen Test oder eine Invariante aus CLAUDE.md (Identität eingefroren, Audio echtzeitfest, Nulltest bitidentisch, State verlustfrei, Schemas sind Verträge — neue Felder zuerst versionieren, Altstände laden, unbekannte Felder dürfen alte Consumer nicht zerstören; NaN-Ehrlichkeit; Keine toten UI-Elemente). Eine Abweichung von der Matrix in §6, deren Begründung an der Quelle nicht trägt oder die eine Regel bricht, ist DEFEKT. Reproduzierbar, mit Kommando oder Eingabefolge; je Defekt: Datei:Zeile und der Satz, der bricht.
- LÜCKE: Gate, Matrix und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, keine Erwähnung als NEEDS_WORK-Grund):
1. docs/**, CLAUDE.md und .claude/** — Manifeste, Register, Planstand, Skill, Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände, Kommentar-Wortlaute. Manifeste sind Kontext, kein Prüfgegenstand.
2. Im Register datierte Härtungen und Lücken NAK-191 bis NAK-211 und NAK-215 bis NAK-222; die Pflegeschritte NAK-223 bis NAK-227 (Codebase-Aufteilung, Gesundheitsmaß — geplante Arbeit, nicht dieses Ticket); die Anzeige-Gestaltung in Gen (S31b — hier nur der Zustand `stale` und der Zähler); die Sicherheitsklasse und alle NAK-212/213-Terme (Zusammenhang, Screening, Deckel, Konkurrenzlage, Master-Wahl, Kanaltafel, Kanalwechsel — nur genutzt). Ein Befund gegen ein Prüfskript, einen Riegel oder den Runner, der absichtliche Sabotage in repo-eigenen Quellen voraussetzt, ist HÄRTUNG — das Bedrohungsmodell der Riegel ist die versehentliche Regression, nicht der Angreifer mit Schreibrecht.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. §2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lückenlos") gilt für Gate-Audits, nicht für diesen Ticketreview. Kosmetik, Stil, optionale Härtung, theoretische Randfälle ohne Eingabepfad, Ticketfremdes: nein.

WAS DU TUST:
1. Lies den Diff im Prüfbereich.
2. Prüfe am Zielstand, ob Tests und Riegel messen, was Gate und Matrix verlangen: (a) R1 (E1; V-01 bis V-13): ist `gate_felder_vollstaendig` der eine produktive Schlussriegel am Ende von `proposal()`, ohne zweite Zielvorbedingung; deckt er Zustandshaltung, Persistenz (`vorschlag_persistieren`) und Zustellung (`darf_draft_offer`/`draft_offer_ziele`) über die gemessene Aufrufkette; entsteht bei zwei Mains oder Main-Rückzug kein Proposal und kein `event_type = "proposal"` im Store, während die Enthaltung ohne Ort mit `next_test` und `listen_for` sichtbar bleibt; misst die Paarbildungsfunktion je Tupel `finding_id` (V-12) mit `zip`-Rotbeweis; bleibt `darf_draft_offer`/`capability_vorhanden` unverändert (V-13, M-52)? (b) R2, R6 (E2 bis E5; V-14 bis V-22, V-45): ist `prepost_wirkungsbeleg` das Strukturprädikat mit vier Punkten (Klasse `>= AudioAligned`, mindestens ein gültiges Band, `wirkung` `Some`, Delta oder `match_gain_db` von exakt 0.0 verschieden) ohne neue Zahl und ohne Textvergleich; bleibt `prepost_paar` `ergebnis.is_some()` und trägt nur `pre_post`; folgt `next_test` der Wirkungsmarke in unveränderter Reihenfolge; bleiben Vertrag, `aussageklasse`/`ursachenklasse`/`next_test`-Werte und die Sicherheitsklasse unberührt (M-25, zwei Achsen)? (c) R3, R8 (E6; V-23 bis V-29): setzen `controlEnde` und die Rücknahme ohne Folge-Snapshot alle Befunde auf `stale` am `zustand`-String (M-30) und rechnen `findingsOffen` auf 0 (M-84); leert jeder Reconnect über `beginneSubscription` bei gleicher wie neuer Epoche; hebt nur ein Folgesnapshot den Zustand auf; sind B13/B28 ohne FL fahrbar und misst der Rotbeweis V-24 den Befund A5? (d) R4 (E7, E8; V-30 bis V-36): sitzt der Existenzriegel in `persistenz_p0_intern` hinter dem Idempotenzblock; antwortet eine unbekannte `finding_id` mit `abgelehnt`/`unknown_target` ohne Append und ohne Domänenereignis; bleibt ein angenommenes Urteil nach Rücknahme des Befunds `idempotent_wiederholt`; werden `more_data` und `stale` angenommen; ist die Session aus `kopf.ziel` gebildet und kein Fassungsschritt nötig (C++-Leser kennt `unknown_target`)? (e) R5, R7 (E9; V-37 bis V-44, V-46 bis V-49): filtert `juengste_passage_im_projekt` vor `max_by_key(folge)` nach den drei Gründen; kommt die Taintmenge aus offenen Interventionen UND der geretteten Nachlaufmenge über alle Taintstände; wird die Zuordnung beim Interventionsende gesetzt, bei `tail_samples_offen == 0` und bei Resync geleert und vom Terminal unberührt gelassen (`intervention.rs`, `link.rs`, `zustand.rs`); ist `unknown` kein Passagenkriterium; bleibt `Terminal::Abgebrochen` dauerhaft ausgeschlossen, `Ergebnis` mit `vergleichbarkeit == None` ausgeschlossen, `unvergleichbar` wählbar; rechnet die Aufnahme ohne taugliche Passage ohne Passage und entsteht dann kein Proposal (WN-04); ist N-43 unverändert? (f) §2.6 und A31: fahren die drei Fälle (zwei Mains → `Vorschlaege: 0`; `Probable`-Paar → Klasse 1; jüngste Passage untauglich → ältere) mit gedruckter Ausgabe durch den Produktpfad; ist `luecke()` gerufen oder entfernt; sagen Runnerkopf, Modulkopf und Manifestkopf dasselbe; meldet `pruefe_p5_korpus.py` für NAK-214 keine Lücke? (g) Die fünf begründeten Abweichungen in §6 und die Änderungen an `sonde014_nacharbeit1.rs`/`nacharbeit2.rs` (rote Kanonläufe): trägt jede Begründung an der Quelle, oder wurde eine NAK-212/213-Zusage aufgeweicht statt eine festgeschriebene Abweichung korrigiert? Du darfst lesen und lesende Kommandos fahren; du schreibst nichts.
3. Prüfe, ob der Änderungssatz seit BASIS etwas am Produktverhalten gebrochen hat — bestehende Zusagen aus SONDE-013/SONDE-014 §3, die NAK-212/213-Zusagen (nur genutzt), die Determinismuszusage M-25, die Vertragsform von `proposal`, `session_finding`, `user_verdict`, `command_ack` (Felder, Enums, Längen — kein Fassungsschritt), die Audio-Thread-, State- und Nulltest-Invarianten (der Plugin-Diff berührt nur `SourcesModel` und Tests — bestätige, dass kein Audio-Pfad und kein State-Pfad berührt ist) — nenne, was du selbst nachgemessen und was du nur gelesen hast.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Datei:Zeile, Reproduktion, welcher Satz von Gate, Matrix oder Regel bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
