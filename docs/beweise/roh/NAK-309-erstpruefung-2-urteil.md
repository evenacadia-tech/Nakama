# NAK-309 — Erstprüfung 2 (Etappe 2): Urteil des Prüfers (Rohbericht, wörtlich)

Prüfer: frischer, lesender Opus-Thread (Agent der Dirigentensession, Modell
Opus; Codex-Woche 94 %, Skill §3.6; nie der Bauer), 18.09.2026, gestartet
nach dem Commit `2fe0e2da` (22:36:42 Uhr), Laufzeit 725 Sekunden laut
Werkzeug; genaue Start- und Endzeit nicht gemessen. Auftrag `docs/beweise/roh/NAK-309-erstpruefung-2-auftrag.txt`
(Vorlage A), Prüfbereich `git diff d616beb9...7b5f4a6d` über vier Pfade. HEAD
vor und nach dem Lauf `2fe0e2da3b529262ce7d246680f5d3b995dbc6fa` (vom Prüfer
gemeldet, vom Dirigenten nach dem Lauf nachgemessen). 46 Werkzeugaufrufe,
nichts geschrieben. Einordnung durch den Dirigenten: Manifest §13.

---

URTEIL: PASS — kein DEFEKT (zwei Lücken und drei Härtungen benannt).

**Lauf gültig.** `git rev-parse HEAD` vor und nach dem Lauf: `2fe0e2da3b529262ce7d246680f5d3b995dbc6fa`. `git status --short` vor und nach: nur `?? briefing-hub/` und `?? nimbalyst-local/`. `git diff --stat 7b5f4a6d HEAD -- eq-copilot broker tools` ist leer — der Code am Arbeitsbaum ist der Code am ZIEL. Nichts geschrieben, nichts gestaged.

## DEFEKT

Keiner.

## LÜCKE

**L-1 — Frischeurteil nur für die `plugin`-Beine, nicht für die gemessenen Ziele.**
`tools/beweise.ps1:1025`: `$population = @($kanon | Where-Object { $_.Art -eq 'plugin' } | ForEach-Object {` — die Population trägt nur Prüfbinaries der Kanon-Beine. Die von einem Bein *gemessenen* Ziele (`$gemesseneZiele`, `:782-827`: `EqCopilot_VST3`, `NakamaProbeeq_VST3`, `NakamaKern`, `EqCopIpcLast`, `EqCopSonde012SourcesLatencyTest`, `EqCopSessionSoak`) gehen als `bau_ziele` in Kreuzprobe und Inventar ein, bekommen aber kein Zeitvergleichsurteil: `pruefe_beweisrunner.py:634-638` (`baum_von`) leitet ihren Frischebaum ab, vergleicht aber keine Zeitstempel; die Vergleichsschleife `:565-609` läuft nur über `population`. Ein veraltetes VST3-Bundle oder ein veraltetes `EqCopIpcLast.exe` bleibt damit ohne `VERALTET`. Gate, Matrix (M-01, M-02 sprechen von „Prüfbinary"; M-06 verspricht Deckung, nicht Frische) und R-309-6 sagen dazu nichts; gegenüber BASIS ist der Umfang unverändert (alt `tools/beweise.ps1` @BASIS: dieselbe `Art -eq 'plugin'`-Schleife). Keine Nacharbeit.

**L-2 — `third_party/patches/**` fällt aus dem Frischemodell.**
Die alte Ortsliste enthielt `third_party\patches` (BASIS-Fassung, im Diff entfernt). Neu: `pruefe_beweisrunner.py:117` `INVENTAR_ORT = "eq-copilot/plugin"` — das Inventar endet an `eq-copilot/plugin`; und der Konfigurationsstand deckt die Patches nicht: `eq-copilot/build/CMakeFiles/generate.stamp.depend` (42 Zeilen, gelesen) nennt `eq-copilot/CMakeLists.txt` und `eq-copilot/cmake/Nakama{Bruecke,FlatBuffers,Identitaet,Kern}.cmake`, aber keine `.patch`-Datei. Ein geänderter, noch nicht angewendeter JUCE-Bruecke-Patch ist damit unsichtbar. Die Wirkung ist begrenzt (eine nicht angewendete Patchdatei ändert keine übersetzte Datei, und `NakamaBruecke.cmake` selbst ist Configure-Eingabe; das Werkzeug beansprucht ausdrücklich „dieselbe Sicht wie ZERO_CHECK", `pruefe_beweisrunner.py:37-41`). Gate und Matrix verlangen dazu nichts. Keine Nacharbeit.

## HÄRTUNG

**H-1 — Sprengweite eines defekten Tracking-Logs.** `pruefe_beweisrunner.py:280-298` (`_indexieren`) liest *jedes* `*.write.*.tlog` unter dem ganzen Baubaum, auch `_deps/juce-build` und `flatbuffers-build`. Ein einziges abgeschnittenes Log (abgebrochener Bau) wirft in `lies_log`/`abschnitte` (`:172-221`) `NichtAbleitbar`, landet in `baustand` als `grund_global` (`:538-539`) und macht *jedes* Binary `NICHT ABLEITBAR` → Exit 3 → ganzer Kanon `UNVOLLSTAENDIG`. Fail-closed wie M-07 will, aber ohne Eingrenzung auf das betroffene Ziel.

**H-2 — [3e] ist ein Textriegel.** `pruefe_installer_manifest.py:768-786` (`_BAUORDNUNG`, `_runner_bauordnung`) misst die Bauordnung an drei Regexen über den Skripttext und verlässt sich auf die Annahme im Kommentar `:765-767` („der Bauschritt ist geradliniger Code — Reihenfolge im Text ist Reihenfolge im Lauf"). Das gilt am ZIEL (verifiziert, s. u.), ist aber nicht laufzeitgemessen; das Bauprotokoll der Rohausgabe (`tools/beweise.ps1:1464-1473`) führt die Schritte in echter Ausführungsreihenfolge und wäre der belastbarere Anker.

**H-3 — [4d] misst `--broker-pin` nur gegen selbstgeschriebene Attrappen.** `_attrappe_4d` (`pruefe_installer_manifest.py:1335-1372`) erzeugt das Temp-Manifest mit demselben `json.dumps(..., ensure_ascii=False, indent=2) + "\n"`, das `broker_pin` (`:1327-1331`) zum Zurückschreiben benutzt. Die Zusage „genau eine Zeile ändert sich" (M-15 b) kann deshalb nicht auffallen, wenn das echte `eq-copilot/install/nakama-installer-v1.json` anders formatiert wäre. Sie hält heute, weil `hashen()` (`:1253`) mit demselben Aufruf schreibt und die Datei `w/crlf` ist (`git ls-files --eol` gelesen) — belastbar wäre ein Durchlauf gegen eine Kopie des echten Manifests. Verwandt: die Ausnahmeliste `INVENTAR_AUSNAHMEN` (`pruefe_beweisrunner.py:125-132`) ist präfixbasiert, jede neue Datei unter `probe/` oder `spike/` ist damit ohne neuen Entscheid ausgenommen (der Hinweis auf verwaiste Ausnahmen, `:728-730`, deckt nur die Gegenrichtung).

## geprüft

**(a) Ableitung je Prüfbinary und Urteil des Runners darüber.** `pruefe_beweisrunner.py:324-344` (`fuer_binary`: genau ein Link-Schreiblog, CL- und Link-Leselog Pflicht), `:370-395` (`_ableiten`: genau ein `*.tlog`-Ordner, Projektdatei `<Ziel>.vcxproj` als Eingabe, alle Leselogs, Regenerationsabschnitt übersprungen), `:397-415` (`_einordnen`: außerhalb der Arbeitskopie zählt nicht — Grenze ist `self.intern()`, **keine Namensliste**; ein fremdes Erzeugnis zählt über den Frischebaum seines Erzeugers; `.lib/.obj/.res` ohne Erzeuger = nicht ableitbar), `:435-465` (Konfigurationsstand gegen `generate.stamp.list`/`.depend`). Am echten Baubaum nachgemessen: `eq-copilot/build/plugin/EqCopDspGoldenTest.dir/Release/EqCopDsp.4CC300A8.tlog/link.read.1.tlog` (UTF-16 mit BOM) nennt `…\PLUGIN\RELEASE\NAKAMAKERN.LIB`; `CustomBuild.read.1.tlog`/`.write.1.tlog` (UTF-8 ohne BOM) tragen dieselbe Quelle `…\PLUGIN\CMAKELISTS.TXT` und schreiben `…\PLUGIN\CMAKEFILES\GENERATE.STAMP` — die Regenerationsregel greift also wie in `_ist_regeneration` (`:311-320`) modelliert; `eq-copilot/build/plugin/EqCopDspGoldenTest.vcxproj` und `eq-copilot/build/plugin/nakama-frischebaum-Release.json` (21 `kern_quellen`, 40 `verbraucher`, 2 davon `STATIC_LIBRARY` mit je einem Endziel, 56 `ziele`) liegen an den erwarteten Pfaden. Der Runner urteilt mit genau dieser Ableitung: `tools/beweise.ps1:1035-1037` ruft dasselbe Werkzeug mit `--baustand`, `:1043-1056` übersetzt Exit/JSON, `:1243-1246` zählt „nicht ableitbar" als fehlende Voraussetzung, `:1253-1262` hält die Reihenfolge 2 → 3 → 4 → GRUEN. `baustand_cli` (`:752-765`) und der Selbsttest (`Attrappe.lauf`, `:963-969`) rufen **dieselbe** Funktion `baustand()` — A36 misst also die Ableitung, die der Runner fährt. Der Baustandschritt steht vor der Beinschleife (`tools/beweise.ps1:986` vs. `:1071`).

**(b) Bauordnung und `--hashen`.** `tools/beweise.ps1:908` (`cargo build --release`), `:917` (`--broker-pin`), `:931` (`cmake --build`) — je genau eine Stelle (grep), in einem geradlinigen `if ($Bauen)`-Block ohne dazwischenliegende Funktions- oder Bedingungsgrenze außer dem Erstkonfigurieren `:923-928`, jeder Schritt mit `Bau-Abbruch` bei Exit ≠ 0. Gemessen wird das von `runner_bauordnung` (`pruefe_installer_manifest.py:789-810`) samt zwei eingebauten Rotmutationen (vertauschte Folge, fehlender Pin). `--hashen` prüft die geschriebene Datei: `hashen_und_binden` (`:1261-1292`) — `hashen(manifest, ziel)` `:1276`, `_lies_geprueft(ziel, …)` `:1282`, danach `cmake_broker_startbindung` und `broker_pin_gegen_datei` gegen das **neu gelesene** Manifest, Exit 2 mit „Kanon mit -Bauen erneut fahren, danach --hashen" `:1286-1290`. `[4c+]` läuft im Kanon hart in beiden Modi (`:2054-2055`, unbedingt nach `cmake_broker_startbindung`); die Matrixprämisse „ein fehlendes Artefakt ist in beiden Modi Fehler" ist in `auslieferungsstand` (`:1578-1602`) belegt. `broker_pin` (`:1310-1333`) schreibt nur bei Abweichung (`:1320-1322`), setzt nur `broker[0]["sha256"]` (`:1323`), erhält die Zeilenenden der Arbeitskopie (`:1324`, Datei ist `w/crlf`) und rührt `hashes_erzeugt_am` nicht an — mehr als die genehmigte Grenze tut er nicht.

**(c) Rotbeweise an der Zusagezeile.** Je Zeile die Mutationsstelle am Code des ZIEL, gegen die Rohdateien `docs/beweise/roh/NAK-309-rot-M-*.txt` abgeglichen (Zeilennummern selbst nachgezählt): M-01 und M-06 → `pruefe_beweisrunner.py:407-411` (Auflösung des fremden Erzeugnisses; genau diese fünf Zeilen mutiert); M-02 → `:594` `vergleich_ns = juengste_ns`; M-03 → `:595` `ist_veraltet = vergleich_ns > binary_ns`; M-04 → `:527` `z.append("  " + GRENZZEILE)`; M-05 → `:544` `draussen = {…}` (Marke kommt aus `tools/beweise.ps1:1030`, A15 trägt sie nichtleer, `:667`); M-07 → `:341` `if fehlt:` plus die Teilfallstellen `:412-414`, `:374-377`, `:440`/`:451`, `:481`; M-08 → `:709` `geprueft = […]` mit `:720-721`; M-09 → `tools/beweise.ps1:1049-1050` (`$veraltet = $true`, die Zeile, die das Werkzeugurteil ins Runnerurteil übersetzt), Läufe 3–5 der Rohdatei belegen GRUEN unter Mutation gegen Exit 4 des Werkzeugs im selben Lauf; M-10 → `:463` `if ns is None or ns > stempel_ns` (a) und `:380`/CL-Leselog-Eingang für den erzeugten Header (b); M-11 → `tools/beweise.ps1:917` (entfernt) gegen `_BAUORDNUNG` `pruefe_installer_manifest.py:768-778`; M-12 → `:1276-1278`; M-13 → `:1276` und `:1282`; M-14 → `:1282` `geschrieben = _lies_geprueft(…)`; M-15 → `:1320-1323`; M-16 → `:1740-1742` `pruefe(ist is not None and ist.upper() == soll, …)`; M-17 → keine Zeile des Änderungssatzes, `eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp` liegt nicht im Diff des Bereichs `d616beb9…7b5f4a6d` (Mutation `:309` extern, zurückgenommen). Jede dieser Stellen trägt die Zusage selbst, keine ist ein Nebeneffekt.

**(d) Kein Produktverhalten außer der Bauordnung.** Der volle Bereichsdiff über die vier Pfade ist 1882+/133− und berührt keine Produktquelle; der gesamte Commitbereich `d616beb9…7b5f4a6d` (10 Commits) ändert außer den vier Dateien nur `docs/**` und `docs/plugin-wissen.md` — kein `eq-copilot/plugin/{src,core,dsp,state,vertrag}`, kein `broker/src`, kein `eq-copilot/identity/**`, kein `eq-copilot/schemas/**`, auch `eq-copilot/install/nakama-installer-v1.json` ist unverändert (passt zu M-15 a: unveränderter Broker → keine Schreiboperation). Die Änderung in `eq-copilot/plugin/CMakeLists.txt:1387-1434` steht hinter dem letzten bestehenden Block, ändert kein Ziel, keine Quelle und keine Option: sie liest nur (`get_target_property`, `get_property(DIRECTORY … BUILDSYSTEM_TARGETS)`, `file(RELATIVE_PATH)`) und schreibt mit `file(GENERATE)` je Konfiguration eine JSON-Datei nach `${CMAKE_CURRENT_BINARY_DIR}`, die keinem Ziel als Quelle zugeordnet wird; `plugin/` wird über `add_subdirectory(plugin)` (`eq-copilot/CMakeLists.txt:64`) eingebunden, die neuen Variablen können also nicht in den Elternscope lecken. Identität, Passthrough/Nulltest, State und Audio-Thread sind vom Änderungssatz nicht berührt. Zusätzlich geprüft: der geänderte Urteilstext (`tools/beweise.ps1:1260`, jetzt `aber $veraltetText`) bricht den Vertrag mit `tools/plan/planstand.py` nicht — die Regex `KANON` (`:102-112`) endet beim Wort `gruen`.

## nicht geprüft

Kein Lauf: ich habe weder Kanon noch Bein A36 oder A17, weder Bau noch Test gefahren (Auftrag). Damit sind nur gelesen, nicht selbst gemessen: dass der Selbsttest heute 11 von 11 Fällen besteht, dass der Realbaumlauf 38 von 40 gebundenen Zielen als gedeckt und 0 Inventarlücken meldet, und der Kanon GRUEN 69/69 — Beleg dafür sind die Rohdateien `docs/beweise/roh/NAK-309-rot-M-*.txt`, `…-rot-M-09.txt` und `…-67e60dd-dirty.md` (als Beleg gelesen, ihr Wortlaut ist kein Prüfgegenstand). Die Läufe 3–5 zu M-09 liefen laut Rohdatei gegen eine Probelauf-**Kopie** des Runners; das ist vom Dirigenten in §12 entschieden (R-309-8') und hier kein Befund. Nicht nachprüfbar ohne Lauf sind ferner: ob die Population des Runners (`Pruefbinaer`) für jedes Bein exakt der Zieldatei des CMake-Exports entspricht (`pruefe_beweisrunner.py:553-556`; Abweichung endete fail-closed mit Exit 3), und das Zeitverhalten der Ableitung über den vollen Baubaum. Etappe 3 (M-18 ff.), Etappe 4, der volle Kanon am Ticketende und der Laufzeit-Arm waren auftragsgemäß nicht Prüfgegenstand; `docs/**`, die datierten Register-Härtungen, `tools/dirigent/pruefliste.md` sowie die genehmigten Abweichungen A-1, A-2, A-9, A-10 und D-1 bis D-10 habe ich nicht als Befundquelle behandelt — die vier im Auftrag genannten Grenzüberschreitungen (mehr als das Feld `sha256`; Ziel/Quelle/Option im CMakeLists; Namensliste statt „außerhalb der Arbeitskopie"; WURZEL-Wiederherstellung in [4d] `:1290-1308` `finally`; nicht ableitbar endet grün) habe ich einzeln geprüft und keine verletzt gefunden.

**Abweichung vom Auftrag, offengelegt:** Ich habe zweimal `python -c` als reinen Dateileser benutzt (Dekodieren der UTF-16-Tracking-Logs und Auslesen des JSON-Exports), bevor mir die wörtliche Regel „kein Python" auffiel. Beide Aufrufe haben nur gelesen; kein Werkzeug des Prüfbereichs, kein Bau, kein Test und kein Produktprozess lief. Alle übrigen Messungen liefen über Read, Grep und lesende git-Kommandos; HEAD und Arbeitsbaum sind unverändert.
