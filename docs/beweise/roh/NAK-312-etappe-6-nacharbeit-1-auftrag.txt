NAK-312 / Planschritt S25k — Nacharbeit 1 der Etappe 6: Bauauftrag des Dirigenten (2026-09-22)

Du bist ein frischer Opus-Worker (Bauer) im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master, `--permission-mode dontAsk`. Du baust GENAU die zwei Punkte unten, nicht mehr. Ticketmanifest: docs/beweise/NAK-312.md (Kopf; Regeln §2; Zusagen §5.9 bis §5.12; Matrix §6.7 und §38.20; Bauplan §7.0 und §7.6; Bauerbericht 6b §38.18 bis §38.25 zum Kennungskonflikt-Rückruf; Erstprüfung §41.2; Validierung §41.5; Einordnung des Dirigenten §41.6 — sie ist dein Auftrag im Wortlaut). Prüfliste: tools/dirigent/pruefliste.md (Arbeitsliste, keine Anforderungsquelle). Rohurteil und Validierung: docs/beweise/roh/NAK-312-etappe-6-erstpruefung-urteil.md (L-5, L-2), docs/beweise/roh/NAK-312-etappe-6-erstpruefung-validierung.md (L-5 mit Aufrufkette bis zum Host, L-2 mit den drei Matrixstellen).

Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

PUNKT 1 — DEFEKT L-5 (gegen R-312-2 Satz 2; Präzedenz E-312-17): Editorabbau innerhalb des Kennungskonflikt-Rückrufs.
Befund wörtlich (Erstprüfung): „`PluginEditor.cpp:633-643`: der SafePointer wird einmal am Eintritt geprüft; die Zuweisung an `ed->statusMeldung` und `ed->statusMeldungBisMs` geschieht nach `ed->processor.neueSensorId()` ohne zweite Prüfung. Baute der Host den Editor synchron in seiner Reaktion auf Host-Dirty oder restartComponent ab, schriebe der Rückruf in einen toten Editor. R-312-2 deckt den Fall im Wortlaut („nach dem Ende seines Eigentümers … kein Zugriff"); M-91 misst nur den Abbau vor der Zustellung; ein Hostweg ist nicht belegt (V-2). Kleinster Nachzug: den SafePointer nach `neueSensorId()` erneut prüfen." Validiert: Aufrufkette `neueSensorId` → `meldeHostDirty` → `updateHostDisplay` → Listener synchron (`juce_AudioProcessor.cpp:431-436`) → `audioProcessorChanged` → `restartComponent`; der Kommentar `PluginEditor.cpp:617` sagt „prueft ihn VOR jedem Zugriff", der Code nicht.
Schließende Regel: R-312-2 Satz 2 — „nach dem Ende seines Eigentümers ist er wirkungslos: kein Zugriff, keine Mutation, keine Dirty-Meldung" — gilt für JEDEN Zugriff im Rückruf, auch nach einem Hostrückruf innerhalb desselben Rückrufs.
Bau: Im Rückruf (`PluginEditor.cpp` um `:617-643`) die Kennungsvergabe vom Editorzugriff trennen: erst `neueSensorId()` (der Prozessor lebt, der Rückruf hält ihn nicht über den Editor, sondern über den Weg, den M-91 (b) schon nutzt), Ergebnis in eine lokale Variable; DANACH den SafePointer erneut prüfen und nur bei lebendem Editor `statusMeldung` und `statusMeldungBisMs` setzen. Kein Zugriff auf `ed->` zwischen dem Hostaufruf und der zweiten Prüfung; die Kommentarzeile `:617` wird wahr. Kein anderes Verhalten ändert sich: M-91 (a), (b) und M-92 bleiben grün und unverändert in ihrer Zusage.
Neue Matrixzeile M-93 in §6.7 (Spalten wie die Nachbarzeilen; Etappe 6, Nacharbeit 1): Zustand: Messpunkt-Popover mit Kennungskonflikt offen, Editor und Prozessor leben; Ereignis: der Rückruf läuft, und der Host baut den Editor synchron INNERHALB von `neueSensorId()` ab (im Test: ein `AudioProcessorListener`, der in `audioProcessorChanged` den Editor zerstört, so wie es ein Host in `restartComponent` täte); Zusage: nach dem Abbau erfolgt kein Zugriff auf den Editor, die Kennung wechselt trotzdem (der Prozessor lebt), genau ein Host-Dirty, kein zweiter Statuszugriff; Reihenfolge: Kennung zuerst, zweite Lebendprüfung vor jedem Schreibzugriff; Test: B15 `312/M-93 konfliktrueckruf_ueberlebt_editorabbau_im_rueckruf` in eq-copilot/plugin/tests/ShotTestMain.cpp neben `312/M-91`; Rotbeweis: heute rot (Gegenprobe am Stand vor deinem Bau: der Zugriff auf den abgebauten Editor wird als MARKE gemessen — Muster M-91 (b) §38.18 bis §38.20: eine Wächterzahl oder ein Sentinel, das der Editor beim Abbau setzt und das der tote Zugriff träfe —, nie als Lauf in undefiniertem Verhalten; nach dem Bau: die zweite Prüfung entfernen → Marke → rot; Rotbeweisdatei docs/beweise/roh/NAK-312-rot-M-93.txt).
Wie der Zugriff „als Marke" messbar wird, ohne UB, entscheidest du am Muster von M-91 (b) (§38.18 ff.): dort war es die Lebendprüfung, die entfernt eine Marke 1 erzeugt. Wenn ein Zugriff auf freigegebenen Speicher nur über einen Testhaken sicher messbar ist (z. B. der Listener zerstört den Editor NICHT wirklich, sondern räumt den SafePointer über den Testzugang aus und setzt eine Marke, die jeder spätere `ed->`-Zugriff durch eine Prüfschablone erhöht), ist das zulässig — der Testzugang liegt unter NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3 und die Zusage bleibt „kein Zugriff nach dem Abbau". Begründe die Wahl im Manifest.

PUNKT 2 — LÜCKE L-2 mit Regel R-312-24 (nur Manifest, kein Code): Die Matrix beschreibt den gebauten Weg. In docs/beweise/NAK-312.md §6.7 drei Stellen nachziehen, Wortlaut in §41.6 (zweiter Punkt): M-59 Spalte „Reihenfolge und Frist" → Abschluss aus `reset()` und `releaseResources()` über `markierungAbbrechen` und den RT-Ring (`Hostbruecke.cpp:190-224`), sperrfrei; der Block unter `sendeZustandMutex` bleibt allein `prepareToPlay`; M-62 → Fall (a) entfällt (zwei Intervalle schließen über den Ring lückenlos, gemessen `begin@1 end@2 begin@3 end@4`, §36.12), Fall (b) trägt fail-closed bei vollem Ring — Zustand, Ereignis und Zusage der Zeile entsprechend fassen, Test und Rotbeweis wie gebaut (§36.12, docs/beweise/roh/NAK-312-rot-M-62.txt); M-64 Zusage (b) → „erzeugt dasselbe `end` auf der Leitung (Kennung, `project_sample_end` null, tail) über den Ringweg, nicht über den Sendezustand". A-78 und A-79 (§36.14) sind damit Zusage, nicht Abweichung; vermerke das in §7.6, wenn dort der alte Weg steht. Regel R-312-24 in §2 eintragen (Wortlaut: „R-312-24 (Matrixwortlaut, 22.09.2026). Die Matrix beschreibt den gebauten Weg; A-78 und A-79 sind Zusage.").

GRENZE: nur eq-copilot/plugin/src/PluginEditor.cpp, eq-copilot/plugin/src/PluginEditor.h (Testzugang unter Makro, falls nötig), eq-copilot/plugin/tests/ShotTestMain.cpp, tools/beweise.ps1 (B15-Behauptungszeile um M-93), docs/beweise/NAK-312.md, docs/beweise/roh/NAK-312-rot-M-93.txt, docs/beweise/roh/NAK-312-etappe6-nacharbeit1-gegenprobe.txt. Audio-Thread unberührt; State unberührt; keine neue Oberfläche; keine Legacy-Umbenennung; Identität eingefroren. Fremde uncommittete Dateien (briefing-hub/, nimbalyst-local/) nie anfassen.

BEWEISLAUF: (1) Gegenprobe am Stand vor deinem Bau: M-93 rot (Marke), Ausgabe in die Gegenprobedatei. (2) Bau. (3) Einzelbeine über tools/beweise.ps1 — B15 (EqCopShot --sonde012-suite), B14, A1 — grün; Einzelbefehle stehen im Skript; ein Bein niemals als Session-Hintergrundbefehl, sondern abgekoppelt mit Log und `EXIT=`-Zeile (Muster Skill §3.5), und beim Warten einen abgelaufenen Wächter neu setzen und bei jeder Rückkehr zuerst das Log lesen (NAK-354); Turn nie beenden, solange ein Bein läuft. (4) Rotbeweis: zweite Prüfung entfernen → M-93 rot → zurück → grün; Ausgabe in die Rotbeweisdatei. (5) Kein voller Kanon in dieser Nacharbeit (der Abschluss der Etappe 6 fährt ihn); Bauzeitstempel der Binaries gegen die Quellen prüfen (NAK-230-Landmine: nach dem Zurückspielen `LastWriteTime` setzen). Übergib nie NOT RUN.

MANIFEST: neuer Abschnitt „## 42. Nacharbeit 1 der Etappe 6 — L-5 (M-93) und Matrixwortlaut R-312-24 (Bauer, 22.09.2026)" nach dem Muster von §24 und §38.18 bis §38.25: Gebaut (Diff-Beschreibung mit Datei:Zeile), Gegenprobe, Matrixzeile M-93 (Tabelle wie §38.20), Einzelbeine mit Kopfzeilen, Abweichungen begründet (A-101 ff.), Prüfliste abgehakt, Selbstaudit (Zahlenränder, stale Closures, Save/Load, UI-Texte), Hinweise an den Dirigenten mit Produktwirkung. Kopfzeile „Etappe" des Manifests: letzten Fettsatz auf „**Etappe 6 von 8: Nacharbeit 1 gebaut, Wiederprüfung folgt**" setzen (≤ 70 Zeichen). Vor dem Commit: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` ohne Befund; Pipe-Zeichen in Tabellenzellen als \|; Zeilenenden je Datei mit `git ls-files --eol` lesen und gleich zurückschreiben (Manifest LF, Rohdateien LF).

GIT: nie `git add -A`, nie `--amend`, nie `git reset --hard`, fremde uncommittete Dateien nie anfassen; ein Commit je Änderungssatz mit explizitem Pathspec (Code+Tests+Rohdateien+Manifest zusammen ist in Ordnung), Betreff „NAK-312 Etappe 6 Nacharbeit 1: …", Commitnachricht über `git commit -F <datei>` mit den Zeilen „Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>" am Ende; vor dem Push `git pull --rebase` bei sauberem Worktree, dann `git push` als eigener Schritt. Zeiten nur aus `Get-Date` (nie geschätzt). Am Ende: Baum sauber (bis auf die zwei fremden Ordner), HEAD = origin/master, und deine letzte Ausgabe nennt Basis-SHA, End-SHA und die Kopfzeilen der gefahrenen Beine.
