NAK-182 — Etappe 2 (Bau). Zusatzauftrag des Dirigenten (2026-09-06) an denselben Opus-Bauer.

Matrixprüfung 3 ist PASS (Codex Astra max, Thread 01a075a7-7f27-7882-9c7b-529d4ebe41a4, docs/beweise/roh/NAK-182-matrixpruefung-3-7a5755c.txt). Etappe 1 ist damit konvergiert. Die Verhaltensmatrix in docs/beweise/NAK-182.md im Stand 7a5755c — §2 mit den BINDENDEN Fassungen aus Matrixnacharbeit 1 und 2 (E1b, E4b, MN1-1 bis MN1-7, MN2-1, MN2-2; N-01 bis N-38 in ihrer jeweils jüngsten Fassung; Korpus §2.3; Selbsttestfälle §2.4; Nicht-Zusagen §2.5), §3 Ist-Stand und §4 Bauplan (Reihenfolge §4.1, Rotbeweise §4.2, Beine und Laufzeiten §4.3, Abweichungen §4.4) — ist ab jetzt die Referenz für dich und für den Prüfer. Wo eine ältere Fassung einer Zeile mit der jüngsten kollidiert, gilt die jüngste; wo die Matrix und die Quelle beim Bau auseinanderlaufen, wird NICHT still gebaut, sondern die Abweichung in §6 benannt und die Zeile mit Begründung nachgezogen (wie §4.4).

BAU — die Ticketpfade aus der Kopftabelle (Prüfskript, Erzeuger, Korpus-Fixtures, Tests C++ und Rust, Fixture aus A8 samt Erzeuger, Bein-Behauptungen in tools/beweise.ps1, ggf. CMakeLists.txt für A27/neue Ziele). Einzige Produktcodeausnahme: die lesende Testauskunft im Broker nach MN2-2 (broker/src/coordinator/experiment_verdrahtung.rs, ggf. mod.rs) — nur delegierend, kein Feld, kein Schemafeld, keine Änderung des Terminals, keine Verhaltensänderung. Sonst kein Produktcode unter eq-copilot/plugin/src, core, sonde, vertrag, state oder broker/src. Keine Umbenennung bestehender Testbezeichner, Testziele oder fn-Namen; Bezeichner werden nur hinzugefügt (E2). v3-Verträge, Encodings und Discriminatoren bleiben unangetastet. Bytegleiche Fixtures: A25 `erzeuge_p4_korpus.py --pruefen` und A8 `erzeuge_v3_fixtures.py --pruefen` müssen grün sein; .gitattributes nur, wenn eine neue Regel wirklich nötig ist (§4.4 sagt: keine).

Je Bauetappe aus §4.1: bauen, das betroffene Bein fahren, den Rotbeweis aus §4.2 fahren (Rücknahme oder Manipulation, Lauf rot AN DER GEWERTETEN ZEILE, Rücknahme rückgängig, Lauf grün) und die Rohausgabe ablegen: Beine nach docs/beweise/roh/NAK-182-<sha>.txt (Sammeldatei, je Bein Kopfzeile mit Kommando und Ergebniszeile), Rotbeweise nach docs/beweise/roh/NAK-182-rot-<kennung>.txt (Kennung wie in §4.2). Manifest §6 „Bauetappen" (je Etappe: was gebaut, welche Matrixzeilen gemessen mit Datei::Bezeichner, Rotbeweis-Datei, Abweichungen), §6.x „Laufzeiten" (B19-Sweep gemessen: unter 20 s → 18er-Menge, sonst Fünfer-Menge; die Bein-Behauptung nennt die gefahrene Menge, nie „alle" — MN1-7), §6.y „Nebenbefunde" (NICHT ins Register — der Dirigent zieht nach), §6.z Prüfliste tools/dirigent/pruefliste.md abgehakt mit der Stelle, wo jede Zeile gemessen wurde. Kopftabelle: Zeile „Phase" auf den Etappe-2-Stand, „Ticketpfade" auf die tatsächlich berührte Menge.

Beine fahren (alle aus Release-Artefakten des frischen Baus; Ausgabe je Bein in die Sammeldatei): A25 `py -3.13 tools/eq-copilot/erzeuge_p4_korpus.py --pruefen`, A26 `py -3.13 tools/eq-copilot/pruefe_p4_korpus.py`, A27 (Selbsttest, wie in der Matrix beschlossen), A8 `py -3.13 tools/eq-copilot/erzeuge_v3_fixtures.py --pruefen`, A5 `py -3.13 tools/eq-copilot/pruefe_v3_vertrag.py` (unverändert, muss grün bleiben), EqCopSonde013StereoGoldenTest (B19), EqCopAnalysisGoldenTest (B5), EqCopSonde013PrePostGoldenTest (B24), EqCopSonde013DynamicsTest (B18), EqCopSonde013TruePeakGoldenTest (B17), EqCopLoudnessGoldenTest (B9), EqCopQueueStressTest (B4), EqCopSonde013EventWireTest (B16), EqCopProbeeqNullTest (A16), EqCopNullTest, `cargo test` (im Ordner broker) für sonde013_verdrahtung, sonde013_prepost, sonde013_experiment, sonde013_passage, sonde013_taint, contract_cross_language und coordinator_model. Landminen: MSVC-Stack 1 MiB — Engines/Prozessoren in Tests auf den Heap; keine GPU-Batchläufe; PowerShell 7 deutet deutsche Anführungszeichen in doppelt zitierten Strings als Begrenzer — Textbausteine nur in wörtlichen Here-Strings oder per Write-Werkzeug; Fixtures mit LF und ohne BOM, wie der Erzeuger sie schreibt.

Danach der volle Kanon, abgekoppelt (nie als sitzungsgebundener Hintergrundbefehl):
  $log = Join-Path $env:TEMP 'nakama-nak182-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-182.md -Anhaengen -Titel NAK-182 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
Fertig, wenn die letzte Logzeile mit EXIT= beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); kein Stand mit Laufstatus NOT RUN wird übergeben. Der Runner hängt den Kanonabschnitt selbst an das Manifest an und legt die Rohausgabe unter docs/beweise/roh/NAK-182-<sha>.md ab. Fällt ein Bein rot, das dein Diff berührt: Ursache an der Quelle, beheben, betroffenes Bein grün, Kanon erneut; fällt ein ticketfremdes Bein rot: nicht anfassen, in §6.y benennen, Dirigent entscheidet.

Git: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; Commits mit explizitem Pathspec in logischen Schritten (Prüfskript+Erzeuger+Korpus; Tests je Bein; Fixture+Erzeuger; Runner-Behauptungen; Manifest+Roh), Push nach origin master nach `git pull --rebase` bei sauberem Worktree. Kein Anfassen von docs/beweise/G4.md, docs/beweise/SONDE-013.md, docs/beweise/NAK-180.md, docs/beweise/NAK-181.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen — Zahlenränder (Blockgröße 1 und 16384 wirklich gefahren, Sampleraten wirklich gewechselt, Leiter mit 20 Schritten, Toleranzen als Zahl), NaN/Inf, Bezeichner wörtlich statt ähnlich, jeder Rotbeweis fällt an der gewerteten Zeile und nicht an einem Nebeneffekt, Bein-Behauptungen und Korpus-Hinweise sagen die Wahrheit, Fixtures bytegleich reproduzierbar, Testauskunft im Broker nur lesend, kein Schemafeld, keine Umbenennung.

Am Ende deinen Turn mit der letzten Zeile „ETAPPE 2 ABGESCHLOSSEN <end-sha>" beenden — erst nach grünem Kanon, committet und gepusht. Keine Nachfrage an den User.
