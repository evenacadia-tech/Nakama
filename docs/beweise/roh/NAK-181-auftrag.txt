NAK-181 — G4-Nacharbeit 2: Vergleichsevidenz-Semantik. Ticketauftrag des Dirigenten (2026-09-05).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-181.md ein (Zeile „Basis-SHA"; dazu eine Zeile „Regeln | R1 bis R9 wörtlich in docs/beweise/roh/NAK-181-auftrag.txt"). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach origin master als eigener Schritt nach `git pull --rebase` bei sauberem Worktree. Keine Neben-Refactors, keine Legacy-Umbenennung. v3-Verträge sind eingefroren: Discriminator, Zieladresse, Revision und Capability werden nie angefasst; ein additiv-optionales Feld ist nur zulässig, wenn die Matrix es als unumgänglich begründet, und dann liegen Schema, Fixtures, Erzeuger, C++-Writer, Rust-Leser und Python-Prüfer im selben Änderungssatz (M-70). Vor jedem Commit tools/dirigent/pruefliste.md abhaken und im Manifest nennen, wo jede Zeile gemessen wurde. Landminen: der MSVC-Standardstack ist 1 MiB — Engines und Prozessoren in Tests auf den Heap (std::unique_ptr), nie im Rahmen; PowerShell 7 deutet deutsche Anführungszeichen in doppelt zitierten Strings als Begrenzer — Textbausteine nur in wörtlichen Here-Strings (@'…'@) oder per Write-Werkzeug.

TICKETGRENZE — genau diese an der Quelle bestätigten Defekte des Gates G4 (docs/beweise/G4.md §1, §5, §6 Satz 2, §7, §8, §11 Nr. 2) mit ihrer Ursache, in allen betroffenen Sprachen. Quellenchecks mit Zeilenbelegen: docs/beweise/roh/G4-quellencheck-leser-9f30008.md (V01, V02, V03, V05, V06, V09, V10) und docs/beweise/roh/G4-quellencheck-codex-9f30008.md (C3, C4, C5). Zeilenangaben gelten für den Gate-Stand 9f30008; seit NAK-180 kann PluginProcessor.cpp verschoben sein — an der Quelle neu verorten.

V01 (M-43, M-07) — `eq-copilot/plugin/src/PluginProcessor.cpp` `erfasseKandidat` (:2075-2104 am Gate-Stand) hat den Riegel `if (! eingefroren || ! vergleichspegel.gainGesetzt()) return false;` aus `beginneVersuch` (:2026) nicht; `versuchReferenzJson` (:1937) schreibt `match_gain_db` unbedingt aus dem LEBENDEN `vergleichspegel.gainDb()`, `:1946-1947` `nicht_endliche_samples` aus dem LEBENDEN Zähler statt aus dem bei Versuchsbeginn eingefrorenen `versuchNichtEndlich` (:2023). `prepareToPlay` (:211) und `bindePassagenfensterMitEpoche` (:1817) rufen `vergleichspegel.vorbereiten()` → `leerenIntern()` (`core/analysis/Vergleichspegel.h:323-332`): danach reist im `experiment_candidate` ein `match_gain_db:0` als Behauptung „gleich laut" und ein genullter Nichtendlich-Zähler als „nachweislich keines". Der Urteilspfad ist über die Kopie in `Blindvergleich.h:95` nicht betroffen; falsch ist die Referenz auf der Leitung.
V02 (entschiedene Lücke, G4 §7) — `beginneVersuch` (:2022) schaltet `versuchspegelSpeist` ab, bevor `friereEin()` läuft; scheitert der Abgleich an „zu wenig Material" (`Vergleichspegel.h:203-208`, absichtlich retryfähig), kehrt `:2026-2027` ohne Wiederanschalten zurück; der einzige Setzer auf `true` ist `:1819` in `bindePassagenfensterMitEpoche`; der Audiothread speist nur bei `versuchspegelSpeist && pegelFensterAktiv` (:506-507). Der Handgriff ist bei gebundener Passage beim zweiten Druck stumm tot.
V03 (M-50, sekundär M-13) — `setStateInformation` (:1395-1446) leert nur `ausstehendeSourcesCommands`/`bestaetigteSourcesCommands`; `versuchIdAktiv`, `versuchPassageId`, `gebundenePassageId`, `passagenfenster*`, `pegelFensterAktiv`, `versuchspegelSpeist`, `vergleichspegel`, `blindvergleich` bleiben stehen. `versuchKopfJson` (:1953-1963) baut den Kopf aus dem frischen `v3Hello()`: ein danach gesendetes `experiment_candidate`/`experiment_manual_result` trägt die alte `experiment_id` unter der NEUEN `project_binding_id`; `passagenfensterFuehrt(id)` (:1832-1843) behauptet eine Passage, die im geladenen State nicht steht. Gegenbeweis, dass die Reload-Grenze anderswo bewusst gezogen ist: `:2291-2294` vergleicht `projectBindingId` UND `v3SessionEpoch`. State-Bytes sind nicht betroffen.
V05 (Invariante „Schemas sind Verträge") — `eq-copilot/plugin/vertrag/NakamaEvidenz.cpp:78` und `eq-copilot/plugin/core/ipc/ControlClient.cpp:374` `return std::to_string (w);` für double: unter MSVC `%f`, LC_NUMERIC-abhängig und auf sechs Nachkommastellen fixiert. Aufrufer `NakamaEvidenz.cpp:259` (`stereoBandsatzJson`, Array aus 221 Werten: Komma-Locale → zwei Arrayelemente je Wert, `gueltig_bitmap` :262-264 passt nicht mehr), :294, :344, :347, :484, :486, :508, :510. Gegenprobe im selben Produkt: `PluginProcessor.cpp:1752-1760` (`imbue (std::locale::classic())`, `setprecision (10)`) — die einzige Stelle mit `std::locale`. Auslöser ist ein fremdes Modul im FL-Prozess mit `setlocale` (UCRT /MD prozessweit), kein Angreifer.
V06 (M-11, M-08) — `eq-copilot/plugin/core/analysis/FeatureEngine.h:2999-3000` (`korrelationKurz`) und `:3025-3028` (`persistenz`) hängen an `stereoKorrKurzGesetzt[b]` bzw. `stereoKurzfenster > 0`; das Kurzfenster schließt erst bei `++stereoKurzFrames >= kKorrelationKurzFrames` (= 8, `:856`, `:2908`); `evidenzLeeren()` (:3572) ruft bei JEDEM fälligen Snapshot `stereoLeeren()` (:3074-3086), das die Kurzfensterzähler nullt. Rechnung: `kHauptHop` 2048 (:894/:899), `evidenzIntervallS` startet bei 0,25 s (:956/:4176), `evidenzSamples` zählt jeden Sample (:2418) → 5,86 Hops je Fenster, dauerhaft unter 8. `NakamaEvidenz.cpp:313-315` und `:325-327` schreiben beide Felder mit `basisGesetzt` als Präsenzbit auf die Leitung — als gemessene 0,0 in jedem Snapshot bei Vorgabekadenz. `kohaerenz`, `phaseRad` (:3002-3022) und die Zeitperzentile (:3030-3035) tragen eigene Bits und schweigen korrekt.
V09 (M-46, M-22) — `eq-copilot/plugin/src/SourcesModel.cpp:550-573` (`experimente`) prüft nur `id` hex32, `ereignis.isString()`, `offen.isBool()`; die vier optionalen Enumfelder (`hoerurteil`, `blindreihenfolge`, `vergleichbarkeit`, `urteil`) gehen durch `wort()` (:562-567), das jeden String annimmt. `:594-609` (`paare`): `klasse`, `kette` nur `isString()`, keine Längengrenze auf `pair_id`. Schema `eq-copilot/schemas/v3/eq-ipc-v3.schema.json:994ff` (`$defs/session_experiment`), `:1030ff` (`$defs/session_paar`), `:788ff` (`$defs/pair_id`: 1 bis 64 Codepoints) führen alle diese Felder als geschlossene Enums. Gegenprobe in derselben Datei: `liesDescriptor` (:217-224) prüft `pair_id` auf Länge ≤ 64; `measurement_position` (:135-141), `aussageklasse` (:200), `betrieb` (:206-210), `p2_reject.grund` (:247-258), `plugin_kind` (:652-654) werden erzwungen. `SourcesModel.h:93` behauptet zu `urteil` „eine der fünf zulässigen Aussagen aus M-46" — heute unwahr.
V10 (M-22) — `SourcesModel.cpp:607-609`: `ausschluss` wird nur bei `aus.isString()` übernommen; ein vorhandenes Nicht-String-Feld fällt still weg (fail-open), obwohl jedes andere Typvergehen in derselben Funktion `SnapshotErgebnis::ungueltig` liefert (:546-548, :552-556, :586-591, :598-602). `SourcesModel.h:103`: „Leer heißt ‚das Paar trägt eine Aussage'" — aus „trägt keine" wird „trägt eine".
C3 (M-53, Exit-Gate Satz 2) — `eq-copilot/plugin/sonde/SondeProcessor.cpp:336-345`: beim Quarantänebruch (`StampedAudioQueue.h:700-718` verwirft den gehaltenen Block ohne Freigabe) ruft der Worker `analyseEngine.zuruecksetzen(); merkmale.zuruecksetzen(); producerStandLeeren();` — `FeatureEngine::zuruecksetzen()` (:1120-1123) setzt `habeVorigen=false; transportEpoche=0; segmentInEpoche=0`; der nächste Block liefert `Grenzgrund::keine` (:1158), kein `grenzeZiehen` (:2102-2112), der Stempel (:3629-3630) trägt 0/0 → `invalidierung_aus_transportbruch` (`broker/src/coordinator/invalidierung_verdrahtung.rs:433-439`) sieht keine Änderung → keine Invalidierung; stand die Engine vorher > 0, meldet der Broker `Epochwechsel` auch für einen Drop (M-53 verlangt `sequenzluecke`). Gen macht es richtig: `PluginProcessor.cpp:904-912` setzt nur `engine.zuruecksetzen()` und lässt `merkmale` stehen („FeatureEngine erkennt die Grenze am nächsten freigegebenen Deskriptor selbst"); `grenzeZiehen` leert alle Fenster UND zieht Epoche/Segment hoch. Kein Test fährt `SondeProcessor` mit echtem Queue-Drop und liest danach `transport_epoch`/`continuity_segment` auf der Leitung (`QueueStressTestMain.cpp:355-359` misst nur die Quarantäne, `Sonde013DynamicsTest.cpp:631-664` nur die Engine).
C4 (M-18, M-22) — `broker/src/coordinator/prepost.rs:920-931` `fn ausschluss(...)` setzt `kettenbefund: Kettenbefund::Stationaer` (:926) für ALLE zehn harten Ausschlüsse (:947, :950, :953, :956, :959, :964, :970, :975, :1126 Paarkonflikt, :1131 HaelfteFehlt); `prepost.rs:131-137` benennt `NichtBeurteilbar` ausdrücklich als das Gegenteil; `sicht.rs:461-475` serialisiert `kettenbefund` in jedes `session_paar` (Schema :1033 Pflichtfeld, :1039 „nicht_beurteilbar ist ausdrücklich NICHT stationaer"); `broker/tests/sonde013_prepost.rs:582-614` `hard_exclusions_each_name_their_reason` prüft `kettenbefund` nicht.
C5 (M-23, mittelbar Exit-Gate Satz 2) — `broker/src/coordinator/prepost_verdrahtung.rs:254-256` summiert die gemessene Aktivzeit aus `sample_count/sample_rate` je Historieneintrag; `:274-277` überschreibt sie mit der Projektspanne `(bis - von)/rate`, sobald `von`/`bis` vorliegen — und genau die speisen zugleich `projekt_fenster` (:278-283), ohne das der harte Ausschluss `KeineProjektzeit` greift (`prepost.rs:958-960`): jeder Pfad, der die Herabstufung (`prepost.rs:1033-1036`, `AktivzeitKlafft`) erreicht, trägt die Projektspanne; bei gleicher Spanne beider Hälften (Smart Disable, einseitige Stille, Rückstau-Kadenzreduktion `SondeProcessor.cpp:472-478`) fällt der zweite der „genau drei" M-23-Gründe nie. `haelfte_aus_historie` (:186) ist privat und hat außer `paare_bei_bedarf_bilden` keinen Aufrufer; kein Test fährt zwei Hälften mit verschiedener Snapshotdichte über dieselbe Projektspanne.

REGELN, die die Defekte schließen (Technikentscheid des Dirigenten; Reihenfolge, Mittel und Fristen entscheidet deine Verhaltensmatrix, die Codex vor dem Bau prüft):
R1 (V01) — Die Referenz auf der Leitung ist die eingefrorene Referenz. `experiment_candidate` trägt genau den Match-Gain und den Nichtendlich-Zähler, die `beginneVersuch` eingefroren hat (Kopie aus dem Blindvergleich oder aus `versuchNichtEndlich`, nie der lebende `vergleichspegel`); ohne eingefrorenen Abgleich (`gainGesetzt()` falsch, z. B. nach `vorbereiten()` durch `prepareToPlay` oder ein zweites Binden während eines offenen Versuchs) entsteht kein Kandidat — die Matrix legt fest, ob der offene Versuch dann als „Vergleich nicht gültig"/abgebrochen endet oder der Handgriff ehrlich verweigert, und misst beide Wege (M-43: „vorab gemessen und für die Dauer des Versuchs eingefroren"). Test in `EqCopSonde013ExperimentUiTest` (B26-Nachbarschaft), Rotbeweis: Regel zurückgenommen → `match_gain_db:0` reist → Test fällt.
R2 (V02, entschiedene Lücke) — `versuchspegelSpeist` ist retryfähig: scheitert `beginneVersuch` an „zu wenig Material", bleibt die Speisung an (oder wird auf dem Fehlerweg wieder eingeschaltet), die Diagnose meldet ehrlich „zu wenig Material, weiter sammeln", und derselbe Handgriff gelingt, sobald `kMindestSekunden` Material im Fenster liegen — ohne Umweg über Passage lösen/neu binden. Test: erster Druck bei 0,2 s Material scheitert benannt, zweiter Druck nach 0,5 s gelingt; Rotbeweis mit der alten Reihenfolge.
R3 (V03) — Der Projektwechsel beendet den Vergleichszustand. `setStateInformation` (Projektwechsel, Preset-Recall, Copy/Paste der Instanz) bringt `versuchIdAktiv`, `versuchPassageId`, `gebundenePassageId`, Passagenfenster, `pegelFensterAktiv`, `versuchspegelSpeist`, `vergleichspegel` und `blindvergleich` auf den Ausgangszustand; danach reist kein `experiment_candidate`/`experiment_manual_result` der alten `experiment_id` unter der neuen `project_binding_id`, und `passagenfensterFuehrt` behauptet keine Passage aus dem alten Projekt. Ob und wie der beim Broker offene Versuch geschlossen wird (ausdrücklicher Abbruch mit benanntem Grund vor dem Reconnect, oder Broker-Riegel: Kandidat/Ergebnis mit anderer `project_binding_id` als der Beginn wird abgewiesen und der Versuch als abgebrochen geführt — M-50 verlangt „offenen Zustand anzeigen, keine stillschweigende Fortsetzung"), entscheidet die Matrix; sie misst beide Seiten (Gen: kein Senden; Broker: keine Annahme), bevorzugt ohne Schemaänderung. Audiothread-Sicht: alle Flags, die `processBlock` liest, bleiben atomics; das Leeren läuft auf dem Nachrichtenthread. Test in `EqCopSonde013PassageStateTest` oder `EqCopSonde012ProjectReloadTest` (State-Bytes bleiben verlustfrei — beides messen).
R4 (V05) — Jede JSON-Zahl aus `NakamaEvidenz.cpp` und `ControlClient.cpp` ist locale-unabhängig und round-trip-tauglich (ein gemeinsamer Helfer ist zulässig, weil beide Stellen dieselbe Ursache haben; Vorbild `PluginProcessor.cpp:1752-1760` oder `std::to_chars`). Format entscheidet die Matrix; Bedingungen: Punkt als Dezimaltrenner unter jeder Prozess-Locale, `-0`, sehr kleine/große Werte und Nicht-Endliches definiert (nicht-endlich reist nie als Zahl), bestehende Fixtures und Erzeuger bytegleich nachgezogen (`tools/eq-copilot/erzeuge_v3_fixtures.py --pruefen`, A5 `pruefe_v3_vertrag.py`, Rust-Leser). Test: der Testprozess setzt `setlocale(LC_NUMERIC, "de-DE")` (oder `"German_Germany.1252"`), serialisiert einen Stereobandsatz mit 221 Werten und einen Heartbeat/Statusframe und prüft bytegleich gegen die Fixture und Parsebarkeit mit `juce::JSON`; Rotbeweis mit `std::to_string`.
R5 (V06) — `korrelation_kurz` und `persistenz` reisen nie als gemessene Zahl ohne Messung: entweder ein eigenes Präsenzbit je Feld (Mechanik wie `kohaerenz`; bevorzugt, wenn die heutige Wire-Form ein Bit ohne neues Schemafeld hergibt — prüfen, wie `kohaerenz`/`phase` heute ihr `null` transportieren) oder ein Nachweis, dass das Kurzfenster bei jeder zulässigen Kadenz und Samplerate vor dem Snapshot schließt (dann darf `stereoLeeren()` die Kurzfensterzähler nicht mehr nullen oder das Fenster überlebt den Snapshot). Die Matrix legt EINEN Weg fest und begründet ihn gegen M-11 („null bei zu wenig Frames, nie ein geschätzter Wert") und M-08 („Präsenzbit fällt weg, statt 0 oder NaN zu senden"). Test in `EqCopSonde013StereoGoldenTest` (B19) bei Vorgabekadenz 0,25 s und 48 kHz: kein Snapshot trägt `korrelation_kurz`/`persistenz` ohne geschlossenes Kurzfenster; die Wire-Form in `EqCopSonde013EventWireTest`; Rust-Leser (`broker/src/coordinator/evidenz.rs`) liest `null` fail-closed. Rotbeweis: ohne Bit reist 0,0.
R6 (V09, V10) — `SourcesModel::uebernehmeSessionSnapshot` setzt für `experimente` und `paare` die geschlossenen Mengen des Schemas durch (`ereignis`, `hoerurteil`, `blindreihenfolge`, `vergleichbarkeit`, `urteil`, `klasse`, `kettenbefund`, `ausschluss`) und `pair_id` mit 1 bis 64 Codepoints; jede Verletzung und jedes Nicht-String-`ausschluss` liefert `SnapshotErgebnis::ungueltig` wie die übrigen Typvergehen, nie stilles Verwerfen; `SourcesModel.h:93/:103` sagen danach die Wahrheit. Test in `EqCopSonde012SourcesModelTest`: je Feld ein Wert außerhalb der Menge → ungültig, ein Wert innerhalb → übernommen; `pair_id` mit 65 Codepoints → ungültig; Rotbeweis mit `wort()`.
R7 (C3) — Probeeq zieht die Kontinuitätsgrenze wie Gen: beim Quarantänebruch bleibt `merkmale` stehen (nur `analyseEngine.zuruecksetzen()` und `producerStandLeeren()`), die FeatureEngine erkennt die Grenze am nächsten freigegebenen Deskriptor und stempelt `transport_epoch`/`continuity_segment` mit dem je Auslöser eindeutigen Grund (Drop → `sequenzluecke`, Seek und anderer Bruch → `epochwechsel`); die Matrix begründet, dass `grenzeZiehen` dieselbe Fensterleerung leistet wie der bisherige Reset. Test zweiseitig: Plugin — echter `SondeProcessor` mit Queue-Drop, danach `continuity_segment` > 0 im gesendeten Snapshot (Anschluss an `Sonde013EventWireTest.cpp:484-485`); Broker — `broker/tests/sonde013_verdrahtung.rs`: zwei aufeinanderfolgende Snapshots nach echtem Drop invalidieren mit `sequenzluecke`. Rotbeweis: `merkmale.zuruecksetzen()` zurück → Stempel 0/0 → Test fällt.
R8 (C4) — `prepost.rs:926`: `Kettenbefund::NichtBeurteilbar` für jeden harten Ausschluss; `hard_exclusions_each_name_their_reason` prüft `kettenbefund` für alle zehn Gründe, einschließlich `Paarkonflikt` und `HaelfteFehlt` über `bilde_paare`. Rotbeweis mit `Stationaer`.
R9 (C5) — Projektspanne und gemessene Aktivzeit sind zwei Felder: die Projektspanne bleibt für `schaetze_restlag`/`capture_s` (`prepost.rs:1010`), die summierte Messzeit (`sample_count/sample_rate` über nicht zurückgenommene Einträge) speist die M-23-Prüfung (`prepost.rs:1033`). Test in `broker/tests/sonde013_verdrahtung.rs`: zwei Quellen mit gleicher Projektspanne, eine mit halber Snapshotdichte → `AktivzeitKlafft`; die bestehenden `sonde013_prepost.rs`-Fälle bleiben als Einheitsprüfung. Rotbeweis mit der Überschreibung.

SPEZIFIKATION VOR CODE (Etappe 1): Zuerst die Verhaltensmatrix in docs/beweise/NAK-181.md §2: Zustände × Ereignisse × Zusage, je Zeile der Test, der sie misst, und je Regel ein Entscheid mit Begründung (Vorbild docs/beweise/NAK-180.md §2.0 bis §2.x) — mindestens: Kandidat nach `prepareToPlay` während offenem Versuch; Kandidat nach zweitem Binden; Begin bei zu wenig Material und zweiter Druck; Reload bei offenem Versuch (Gen-Seite und Broker-Seite, alte `experiment_id` unter neuer Bindung); Reload bei gebundener Passage ohne Versuch; Reload mit `nurLesen`-State; Komma-Locale für Evidenz-, Status- und Heartbeat-Serialisierer; Stereobandsatz bei 0,25 s/48 kHz und bei 1 s/96 kHz (Kurzfenster geschlossen/nicht geschlossen); jedes Enumfeld außerhalb/innerhalb der Menge, `pair_id` 64/65, `ausschluss` Nicht-String; Quarantänebruch in Probeeq je Auslöser (Drop, Seek, Beweislagewechsel) mit Stempel und Brokergrund; jeder der zehn Ausschlüsse mit `kettenbefund`; Aktivzeit bei gleicher Projektspanne und halber Dichte. Dazu §3 „Ist-Stand an der Quelle" mit den Zeilen, auf die jede Matrixzeile baut (am aktuellen HEAD neu verortet), und §4 „Bauplan" (Reihenfolge, Ticketpfade, Rotbeweise, betroffene Beine). Dann Manifest mit Pathspec committen und pushen (nur docs/beweise/NAK-181.md), und deinen Turn mit der letzten Zeile „ETAPPE 1 ABGESCHLOSSEN" beenden — KEIN Bau vorher, keine Nachfrage an den User. Der Dirigent fährt die lesende Codex-Matrixprüfung, trägt „Matrixprüfung 1" ins Manifest ein und setzt dich mit einem Zusatzauftrag fort.

BAU (Etappe 2, erst nach der Fortsetzung durch den Dirigenten): Plugin, Broker, Tests (Plugin und Rust), Fixtures + Erzeuger soweit R4/R5 sie berühren, Manifestabschnitte (Bauetappen, Rotbeweise, Abweichungen, Nebenbefunde; Nebenbefunde NICHT ins Register — das zieht der Dirigent nach). Beine fahren: `EqCopSonde013ExperimentUiTest`, `EqCopSonde013PassageStateTest`, `EqCopSonde013EventWireTest`, `EqCopSonde013StereoGoldenTest`, `EqCopSonde013DynamicsTest`, `EqCopSonde013PrePostGoldenTest`, `EqCopSonde012SourcesModelTest`, `EqCopSonde012ProjectReloadTest`, `EqCopStateMigrationTest`, `EqCopNullTest`, `EqCopProbeeqNullTest`, `EqCopQueueStressTest`, `cargo test` für `sonde013_prepost`, `sonde013_verdrahtung`, `coordinator_model`, A5 `pruefe_v3_vertrag.py` und A8 `erzeuge_v3_fixtures.py --pruefen`. Rohausgaben nach `docs/beweise/roh/NAK-181-<sha>.txt`, Rotbeweise nach `docs/beweise/roh/NAK-181-rot-<kennung>.txt`. Danach der volle Kanon, abgekoppelt (nie als sitzungsgebundener Hintergrundbefehl):
  $log = Join-Path $env:TEMP 'nakama-nak181-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-181.md -Anhaengen -Titel NAK-181 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
Fertig, wenn die letzte Logzeile mit EXIT= beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); kein Stand mit Laufstatus NOT RUN wird übergeben. Committen mit Pathspec (Produkt, Tests, Fixtures, Erzeuger, Schema nur falls R5 es verlangt, docs/beweise/NAK-181.md, docs/beweise/roh/NAK-181-*), pushen. Kein Anfassen von docs/beweise/G4.md, docs/beweise/NAK-180.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/. Am Ende deinen Turn mit der letzten Zeile „ETAPPE 2 ABGESCHLOSSEN <end-sha>" beenden.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen (Zahlenränder, NaN/Inf, `-0`, Locale, stale Closures, Save/Load-Symmetrie, binden↔lösen, beginnen↔abbrechen, Reload↔Reconnect, irreführende Kommentare und Header-Zusagen), Audio-Thread-Invarianten (kein Lock, keine Allokation, keine Datei/Pipe, kein Logging; Flags nur über atomics), Nulltest bitidentisch, Passthrough sampleidentisch, keine Schemaänderung ohne Matrixbegründung.
