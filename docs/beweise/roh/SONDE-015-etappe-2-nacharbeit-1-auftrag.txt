SONDE-015 — Etappe 2, Nacharbeit 1. Zusatzauftrag des Dirigenten (2026-09-10) an den Bauer nakama-sonde015-2026031-bau2.

Die Erstprüfung der Etappe 2 (Codex gpt-6-astra max, lesend, Thread 01a08939-cb7c-7543-bf7c-ce2733441a1f, Bereich `git diff 2026031f...94a8a336 -- eq-copilot/schemas eq-copilot/fixtures eq-copilot/plugin broker/src broker/tests tools/eq-copilot tools/beweise.ps1`) endete mit URTEIL: NEEDS_WORK — fünf Befunde, alle vom Dirigenten an der Quelle bestätigt und als DEFEKT eingeordnet (Urteil wörtlich: docs/beweise/roh/SONDE-015-etappe-2-erstpruefung-94a8a33.txt; Quellencheck: Manifest §8.8). Dieser Auftrag enthält nur diese fünf Defekte, je Defekt die schließende Regel, und die Prüfliste — sonst nichts. Aufsicht ENG. Git-Regeln wie im Hauptauftrag (nie `git add -A`, nie `--amend`, Pathspec, `git commit -F`, Push nach `git pull --rebase`); keine Neben-Refactors; nur die betroffenen Beine fahren (A5, A8, A10, A12, B2, B3c, B10, A4 und die Broker-Tests), Rohausgabe nach docs/beweise/roh/SONDE-015-etappe2-nacharbeit1-beine.txt; Rotbeweise an der Zusage nach docs/beweise/roh/SONDE-015-rot-<ID>-n1.txt. Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Vor dem Commit tools/dirigent/pruefliste.md abhaken. Manifest: §8.9 „Nacharbeit 1 — was eingearbeitet wurde" (Tabelle B-01…B-05 → Datei:Zeile, Tests, Rotbeweise; Prüfliste). Letzte Zeile deines Turns: „ETAPPE 2 NACHARBEIT 1 ABGESCHLOSSEN <sha>". Keine Nachfrage an den User.

DEFEKTE, wörtlich aus dem Urteil, und die schließende Regel des Dirigenten:

B-01 — [P1] Hebe die P2-Empfängergrenze zusammen mit dem Sender an — eq-copilot/plugin/core/ipc/TelemetryClient.h:48
„DEFEKT (R14/Bestandskompatibilität): Bereits ohne neue Dynamik-Nutzlast sendet `TelemetryClient.cpp:780` jeden Frame mit Minor 2. Der Broker erlaubt in `server_v3/mod.rs:193` weiterhin höchstens P2-Minor 1 und schließt deshalb in `verbindung.rs:665–672` die Telemetrie-Verbindung beim ersten Frame. Lesender Nachweis: `rg -n 'kFeatureBatchSchemaMinor|P2_SCHEMA_MINOR' eq-copilot/plugin/core/ipc broker/src/transport/server_v3`. Empfängergrenze und den weiterhin Minor 2 ablehnenden Test gemeinsam nachziehen."
Quellencheck: am BASIS-Stand 2026031f war `kFeatureBatchSchemaMinor = 1`; Etappe 2 hob den Sender auf 2, `P2_SCHEMA_MINOR` (mod.rs:193) blieb 1; `tests_fristen.rs:126` sichert weiterhin `!schema_minor_bekannt(Familie::P2, 2)` zu; der Koppeltest `transportfassung_und_json_leser_stimmen_ueberein` (tests_fristen.rs:142-148) deckt nur P0/P1. Der Warnkommentar mod.rs:180-187 beschreibt genau diesen Vorfall vom 04.09.2026.
REGEL: Der Fassungsschritt der Familie P2 ist erst vollständig, wenn Sender, Empfänger und Transport ihn kennen: `P2_SCHEMA_MINOR` steigt auf 2; `tests_fristen.rs:126` wird zu `schema_minor_bekannt(Familie::P2, 2)` mit neuer Obergrenze `!…(P2, 3)`; der Koppeltest bindet die P2-Fassung an dieselbe Quelle wie der C++-Sender (die Fassungszahl steht genau einmal in einer Vertrags- oder Registerdatei, von der beide Seiten lesen oder gegen die beide Seiten geprüft werden — keine zweite handgepflegte Kopie). Rotbeweis: mit `P2_SCHEMA_MINOR = 1` fällt der Koppeltest, und ein Broker-Test, der einen Minor-2-Frame durch `verbindung.rs` schickt, sieht die Verbindung geschlossen. Zusätzlich zieht das Manifest §4.2 Punkt 4 die Broker-Seite nach (die Bauanweisung nannte nur den Sender).

B-02 — [P2] Validiere das DSP-DTO nach erfolgreicher Hashprüfung — broker/src/coordinator/liveness.rs:585-588
„DEFEKT (M-105/R13): Ein ansonsten gültiger Bericht mit `dsp.jcs = "{}"` und passendem SHA-256 wird angenommen und als bestätigter DSP gespeichert. Das äußere Schema prüft lediglich einen String; anders als C++ ruft dieser Leser anschließend keinen DTO-Validator auf. Damit passieren beispielsweise fehlende Parameter oder eine unbekannte DTO-Version die Prüfung. Lesender Nachweis: `rg -n 'dto::pruefe|jcs.clone' broker/src/coordinator/liveness.rs`. Vor der Übernahme den vorhandenen exakten DTO-Weg verwenden und einen Bericht mit korrektem Hash, aber ungültigem DTO testen."
Quellencheck: `broker/src/dto.rs:312` `pub fn pruefe(roh: &[u8]) -> Result<(), Grund>` existiert (Vertrag nakama-parameter-v2.json) und wird von liveness.rs nicht aufgerufen; C++ `berichtDtoPruefen` (NakamaParameter.cpp:407-424) prüft Hash zuerst, dann `ausDtoText`; der Rust-Test befehl.rs:1144-1186 kennt keinen Fall „Hash stimmt, DTO ungültig".
REGEL: Der Rust-Leser hält dieselbe Reihenfolge wie C++: Hash zuerst, dann `crate::dto::pruefe(jcs.as_bytes())`; bei `Err` wird der Bericht ganz abgewiesen (kein Teilzustand, `client.dsp_jcs` bleibt unverändert). Rust-Test: korrekter Hash über einer DTO-ungültigen Zeichenkette (z. B. `{}`) → abgewiesen; Rotbeweis: Validatoraufruf entfernt → Test fällt. Beide Sprachen klassifizieren dasselbe Fixture identisch (A8-Korpus: ein Negativfixture „hash passt, dto ungültig" in gueltig/ungueltig-Zuordnung, von C++ B3c und Rust A4 gleich klassifiziert).

B-03 — [P2] Erhalte unbekannte Eigenschaften bei leerem DSP-Zustand — eq-copilot/plugin/state/NakamaState.cpp:1039-1041
„DEFEKT (M-91/R1): Ein gültiger Layout-v2-Stand mit einem `Dsp`-Kind, Revision 0, freien Slots, leeren Zonen/Undo und einer unbekannten Eigenschaft lädt schreibbar. Weil `dspTraegtEtwas` ausschließlich bekannte Inhalte berücksichtigt, entfernt das anschließende Speichern jedoch das gesamte Kind einschließlich der unbekannten Eigenschaft. Lesender Nachweis: `rg -n 'dspTraegtEtwas|removeChild \(dsp' eq-copilot/plugin/state/NakamaState.cpp`. Die Weglassregel muss vorhandene unbekannte Daten erhalten, statt sie als leeren Zustand zu behandeln."
Quellencheck: `dspTraegtEtwas` (NakamaState.cpp:720-728) prüft nur Revision, Zonen, Undo-Ring, occupied; `:1005` und `:1039-1041` entfernen das Kind sonst aus der gehaltenen Kopie (die unbekannte Eigenschaft eingeschlossen); der Fall ist erreichbar (`:1265-1274`: nur state_revision Pflicht, 0 zulässig); der Test StateMigrationTestMain.cpp:1177-1180 setzt Revision 3 und deckt ihn nicht.
REGEL: Das Kind `Dsp` wird nur dann weggelassen, wenn `dspTraegtEtwas(z)` falsch ist UND der gehaltene `Dsp`-Knoten keine Eigenschaft außerhalb der bekannten Menge trägt; sonst wird es mit den bekannten Werten zurückgeschrieben und die unbekannten Eigenschaften bleiben erhalten (M-91 „erhält sie beim Speichern", ohne Einschränkung). B2-Fall: Revision 0, freie Slots, leere Zonen/Undo, eine unbekannte Eigenschaft → nach Save/Load ist die Eigenschaft noch da; Rotbeweis: alte Weglassbedingung → Eigenschaft weg. nakama-state-v2.md:61-64 („Wann Dsp überhaupt geschrieben wird") wird um diesen Satz ergänzt.

B-04 — [P2] Verwende einen gültigen Alternativwert im Hash-Negativfixture — tools/eq-copilot/erzeuge_v3_fixtures.py:1461-1462
„DEFEKT (M-105): Das Ausgangs-DTO enthält `width=1.25`; dieses Präfix-Replacement erzeugt `2.25` und verletzt zusätzlich die Obergrenze 2.0. Deshalb lehnt C++ das Fixture auch ohne Hashprüfung weiterhin ab: `cat docs/beweise/roh/SONDE-015-rot-M-105.txt` zeigt `[bereich v1.global.width]`. Der Rotlauf misst damit den geänderten Fehlergrund, nicht die geforderte Annahme eines manipulierten Berichts bei ausgeschaltetem Hash-Riegel. Einen weiterhin gültigen DTO-Wert einsetzen und dessen DTO-Gültigkeit separat absichern, damit das Negativfixture ausschließlich die Hashprüfung misst."
REGEL: Das Fixture `state-report-dsp-hash-passt-nicht` entsteht aus einem gezielten Wertaustausch innerhalb der Vertragsgrenzen (z. B. width 1.25 → 1.5), und der Erzeuger sichert zu, dass die mutierte Zeichenkette den DTO-Weg passiert; der Rotbeweis M-105 (Hashvergleich abgeschaltet) zeigt dann „angenommen", nicht einen anderen Grund. Korpus vom Writer neu erzeugen (A8), C++ B3c und Rust A4 klassifizieren gleich.

B-05 — [P2] Prüfe für M-89 die Array-Struktur statt nur die Knotenzahl — eq-copilot/plugin/tests/StateMigrationTestMain.cpp:1407-1408
„DEFEKT (M-89): `knoten(v) <= 8` bleibt wahr, wenn `Dsp` statt der flachen Array-Eigenschaft einen zusätzlichen `Occupied`-Kindknoten enthält. Genau diese Mutation dokumentiert `cat docs/beweise/roh/SONDE-015-rot-M-89.txt`; die neue M-89-Zusicherung fällt dort nicht, sondern Bytegleichheits- und Belegungsprüfungen. Damit fehlt der verlangte Rotbeweis an „flache Arrays, keine Kindknoten". Array-Typen und fehlende Kindknoten direkt prüfen und die entsprechende Zusicherung gezielt rot belegen."
REGEL: Die M-89-Zusicherung wird strukturell gefasst: `Dsp` hat null Kindknoten, und `occupied_v1`, `zonen`, `undo_ring` sind Array-Eigenschaften der erwarteten Form; die Knotenobergrenze bleibt als zweite Zusicherung daneben. Rotbeweis: `occupied` als Kindknoten → genau die Zeile „Dsp nutzt flache Arrays" fällt (Rohdatei SONDE-015-rot-M-89-n1.txt).
