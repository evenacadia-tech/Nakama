NAK-181 Etappe 1, Matrixnacharbeit 2 — Zusatzauftrag des Dirigenten (2026-09-05).

Matrixprüfung 2 (Codex gpt-6-astra, max, Thread 01a072b9-9c1b-7ce3-957f-eaeeb0f076d4, Fixdiff 55ef086...2f88e45) ist NEEDS_WORK: MP1-1 und MP1-2 bleiben offen, dazu ein neuer Bruch von E4. Urteil wörtlich: docs/beweise/roh/NAK-181-matrixpruefung-2-2f88e45.txt; Einordnung des Dirigenten an der Quelle: docs/beweise/NAK-181.md §5.2. Geschlossen und NICHT neu zu öffnen: MP1-3 bis MP1-9. Du bearbeitest ausschließlich die drei Befunde unten in §2 (E3, E4, N-18, N-18b, N-41, neue Zeile N-18c), §3, §4 (B5, B9, §4.2, §4.4) von docs/beweise/NAK-181.md — KEIN Bau, kein Produktcode, keine Nachfrage an den User. Jede Änderung an einer bestehenden Zeile bleibt an ihrer Kennung. Git wie bisher: nur docs/beweise/NAK-181.md mit Pathspec committen, `git pull --rebase` bei sauberem Worktree, pushen.

DEFEKTE (Befund des Prüfers wörtlich, dann die Regel des Dirigenten):

MP2-1 — „Verschiebe den größten Float in die Ablehnungsfälle — DEFEKT, MP1-1/R4a bleibt offen: Der größte normale `float` ist eine Ganzzahl weit oberhalb von `2^53−1`. Auch seine auf 15 Stellen gerundete Darstellung `3.40282346638529e38` wird von `broker/src/vertrag.rs:190–217` und `tools/eq-copilot/pruefe_v3_vertrag.py:213–230` abgelehnt. Damit sind N-18s Annahmezusage und derselbe Positivfall in §4.2:1120–1121 unerfüllbar. Dieser Wert gehört nach N-18b beziehungsweise als Ersatzform in die Fixture, nicht unter die akzeptierten Zahlen."
Regel R4c: Der größte normale `float` wandert von N-18 nach N-18b (Verweigerung, `wireZahl` liefert `false`) und in §4.2 in die Ersatzform (Band als `0` ohne Bit). N-18 behält nur Werte, die alle vier Riegelregeln bestehen — Endlichkeit, Dezimalgrenze `±1e308` (`vertrag.rs:179-185`, DEZ_GRENZE), höchstens 15 signifikante Stellen für nichtganzzahlige Werte (`:219-228`), Ganzzahlen (auch in Exponentialschreibweise, `:187-217`) höchstens `2^53−1` — und E4 rechnet jeden N-18-Wert einzeln am Riegel nach (Tabelle: Wert, Text, welche Regel, Ergebnis). Der kleinste normale `float` (`1.17549435e-38`) ist dabei ebenfalls nachzurechnen (15 Stellen, Exponent −38: zulässig?) — nur, was besteht, bleibt in N-18.

MP2-2 — „Unterscheide unbestätigte von noch nicht persistierten P0s — DEFEKT, MP1-2/R3a bleibt offen: Wenn der Broker den Befehl bereits persistiert hat und nur das ACK verloren ging, erfüllt er ebenfalls N-41s Ausgangszustand. `experiment_verdrahtung.rs:53–55` ruft jedoch vor der Zielprüfung `bekannter_befehl` auf; `befehl.rs:66–79` antwortet dann `idempotent_wiederholt`, nicht `abgelehnt/unknown_target`. Der Client zählt entsprechend einen Erfolg (`ControlClient.cpp:1201–1205`). Die Matrix muss diesen Fall vom noch nicht persistierten Retry trennen und jeweils unveränderten Brokerzustand verlangen, statt zur Erfüllung ihrer Zusage die bestehende Idempotenz zu brechen."
Regel R3b: N-41 trennt zwei Ausgangszustände desselben Ereignisses: (i) der Befehl ist NICHT persistiert (Wire-Write ohne Ankunft, oder Broker hat ihn nie gesehen) → nach dem Reconnect reist er mit der alten Adresse, der Broker antwortet `abgelehnt`/`unknown_target` (`befehl.rs:210-217`, `:292-293`), kein Store-Eintrag, kein Experimentzustand; (ii) der Befehl IST persistiert und nur das `command_ack` ging verloren → der Retry trägt denselben Text, `bekannter_befehl` (`befehl.rs:66-79`) antwortet `idempotent_wiederholt`, und der Brokerzustand ist **unverändert** gegenüber dem Stand vor dem Retry (kein zweiter Store-Eintrag, keine zweite Wirkung, der Versuch steht wie vor dem Reload unter der alten Bindung — M-50 „offener Zustand bleibt offen"). Die Zusage von N-41 lautet ab hier „keine Wirkung, die es vor dem Reload nicht schon gab", nicht „kein angenommenes Byte"; E3 Teil 4 und §2.4 werden entsprechend berichtigt. Beide Fälle werden im Broker-Test getrennt gemessen (`sonde013_experiment.rs`: zwei Fälle, je mit Store-Zählung vor und nach dem Retry); die Gen-Seite (B23 `unbestaetigter_versuchsbefehl_traegt_die_alte_adresse`) bleibt. Die bestehende Idempotenz wird NICHT gebrochen und nicht umgangen.

MP2-3 — „Berücksichtige abgewiesene Zahlen auch bei den PPQ-Grenzen — DEFEKT, neuer Bruch von E4/R4a: Für beispielsweise `cycle_start_ppq=1e-308` und `cycle_end_ppq=4` lassen `FeatureEngine.h:3704–3716` und `NakamaEvidenz.cpp:192–198` die endlichen Werte bis zu `zahlJson` durch. Der neue Helfer muss den ersten Wert wegen `DEZ_GRENZE` ablehnen (`vertrag.rs:183–185`); der hier angeblich unerreichbare `null`-Pfad wird damit erreichbar. Das Schema verlangt für beide PPQ-Felder Zahlen (`eq-ipc-v3.schema.json:234–235`), sodass der gesamte Snapshot ungültig würde. B5/N-18b müssen auch diesen Aufrufer mit einer vertragsgültigen Fehlerbehandlung berücksichtigen, nicht ausschließlich die Stereofelder."
Regel R4b: E4 inventarisiert JEDEN Aufrufer von `zahlJson` in `eq-copilot/plugin/vertrag/NakamaEvidenz.cpp` und von `zahl` in `eq-copilot/plugin/core/ipc/ControlClient.cpp` (mit `rg` am HEAD, Tabelle: Zeile, Feld, Schematyp, Pflicht oder optional, umgebendes Objekt) und legt je Aufrufer fest, was bei `wireZahl == false` reist: ein Wert in einem OPTIONALEN Objekt (wie `cycle`) → das ganze Objekt entfällt, genau wie heute bei Nicht-Endlichem (`NakamaEvidenz.cpp:192-193`; die Vorprüfung wird von `isfinite` auf „wireZahl-tauglich" erweitert); ein Pflichtskalar → die bestehende Ersatzzahl mit ihrer Bedeutung; ein Bandsatzwert → `0` ohne Bit. `"null"` bleibt als letzter Riegel nur dort stehen, wo die Tabelle belegt, dass kein Aufrufer ihn erreicht — sonst wird der Aufrufer in B5 geändert. Eine neue Matrixzeile N-18c misst das PPQ-Paar `1e-308`/`4` und ein Paar mit `1e309`-Überlauf: der Snapshot reist OHNE `cycle`, `validity`/`cycle_bounds` bleibt ehrlich (kein `cycle_bounds_valid` ohne Werte), A5 und `textriegel_bytes` nehmen ihn an; Rotbeweis: Vorprüfung nur `isfinite` → Snapshot ungültig. Dasselbe für jeden anderen Aufrufer, den die Tabelle als erreichbar ausweist (mindestens ein Test je Klasse: optionales Objekt, Pflichtskalar, Bandsatz).

ABSCHLUSS: Im Manifest einen Abschnitt „### 5.3 Matrixnacharbeit 2 — was eingearbeitet wurde (2026-09-05, Worker, Basis 2f88e45)" mit Tabelle Ort | Änderung | Befund, dazu die Kopftabelle (Zeile „Phase": Stand voranstellen, Verlauf bleibt; Zeilenzahl der Matrix) nachziehen. Commit nur docs/beweise/NAK-181.md mit Pathspec, pushen, dann den Turn mit der letzten Zeile „MATRIXNACHARBEIT 2 ABGESCHLOSSEN <sha>" beenden.
