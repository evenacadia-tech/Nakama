PHASE 03 — VERTRAGSTREUE (K3): v2- und v3-Verträge, beide Sprachen, beide Richtungen. Kurzname: vertrag.

FRAGE: Welches Feld liest der C++-Leser mit einer anderen Grenze als der Rust-Leser (oder umgekehrt), welches Feld hat kein Negativfixture, wo wird konvertiert, bevor geprüft wird, und welche Nachricht kann eine Seite bauen, die die andere Seite fail-open annimmt?

POPULATION: jedes Feld jeder Nachricht in eq-copilot/schemas/v3/** (JSON-Schemas, FlatBuffers-IDL, README) und eq-copilot/schemas/*.json (v2), der C++-Leser und -Schreiber eq-copilot/plugin/vertrag/** (NakamaVertrag, NakamaTelemetrie, NakamaEvidenz, NakamaUtf8, generiert/ nur als Bezug), core/ipc/WireEnvelope.*, WireZahl.h, controlclient/Vertrag.cpp, controlclient/Nachrichten.cpp, TelemetryClient, der Rust-Leser und -Schreiber broker/src/vertrag.rs, broker/src/dto.rs, broker/src/transport/v3.rs, broker/src/transport/server_v3/** (Umschlag, Frames, Familie), broker/src/generiert nur als Bezug; die Fixtures eq-copilot/fixtures/v3/** (positiv und negativ), die Prüfer tools/eq-copilot/pruefe_v3_vertrag.py, pruefe_flatc_drift.py, erzeuge_fb_fixtures.py, broker/tests/contract_cross_language.rs, eq-copilot/plugin/tests/SchemaTestMain.cpp, tests/IpcTestMain.cpp; die Capability-Datei eq-copilot/identity/host-capabilities-fl-v1.json gegen ihre Leser.

PRÜFFRAGEN:
1. Grenze beidseitig: je Feld Typ, Bereich, Länge (Zeichen vs. Bytes benannt?), Pflicht/optional, additionalProperties — steht dieselbe Grenze im Schema, im C++-Leser, im Rust-Leser und im Negativfixture? Liste jede Abweichung als Zeile.
2. Reihenfolge: Discriminator, Familie, Version, Zieladresse vor dem Inhalt; Bereichsprüfung auf dem Rohwert vor der Konvertierung (nach NAK-283 D8 und NAK-289 EnumCastOutOfRange in WireEnvelope.cpp); Längenprüfung vor dem Lesen; CRC nur bei bekanntem Encoding.
3. Fail-closed: welcher Leser nimmt bei unbekanntem Feld, unbekannter Familie, doppelter Eigenschaft, leerem Objekt, leerer Liste, Null statt Objekt, Zahl statt Text, -0.0, NaN/Inf, Überlänge, führenden Nullen, Surrogaten, überlangen UTF-8-Sequenzen still einen Default an?
4. Nicht additiv: v3-Discriminator, Zieladresse, Revision, Capability — welche Stelle könnte eine additive Erweiterung annehmen, ohne dass ein Test reißt? Fassungsschritt (Minor/Major) je Schema und die Leserregel dazu.
5. Symmetrie: Schreiber und Leser derselben Seite (C++ schreibt, was es selbst nicht lesen kann? Rust ebenso?), Roundtrip-Tests je Nachricht, Wireform-Vergleich (NAK-283 Etappe 3: Wiedervergleich über Wireform) — wo werden Doubles über Textform verglichen, wo binär?
6. Nahtstelle Broker↔Plugin: Hello/Welcome, Session-Token, Revision, Capability-Verhandlung, Fehlercodes — gleiche Menge auf beiden Seiten? Ein Fehlercode, den nur eine Seite kennt, ist ein Befund.
7. Abdeckungskarte A-2: Schema::pruefe (C++-Generalprüfung) nur in SchemaTestMain — was prüft der Produkt-Client stattdessen an derselben Kante, und welches Feld fällt dadurch durch?
