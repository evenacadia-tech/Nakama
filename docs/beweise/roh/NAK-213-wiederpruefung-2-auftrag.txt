Wiederprüfung G5-Nacharbeit 2 / Ticket NAK-213, Runde 2 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff d9effdbcc24f29eca69760ee2fe9ac3de1170e56...c8831443b69022a6f8a0b1f8b4a5d5be2cd87426 -- broker/tests/sonde014_gegenbeispiele.rs broker/tests/sonde014_verdrahtung.rs`
(ZIEL = c8831443b69022a6f8a0b1f8b4a5d5be2cd87426 = HEAD, vor und nach deinem Lauf identisch; VORHER = d9effdb, der Stand vor der Runde — gegenüber dem in der Wiederprüfung 1 geprüften Stand 91ad6f2 nur drei Rohdateien unter docs/beweise/roh/, kein Codeunterschied). Diese Runde ändert ausschließlich zwei Testdateien; broker/src, eq-copilot/ und tools/ sind an VORHER und ZIEL identisch — bestätige das. Der übrige Ticketbereich wurde in der Erstprüfung (Thread 01a07fb5-4029-7602-a198-e1a0d3561661, Stand 0491c6a) und der Wiederprüfung 1 (Thread 01a07fff-7d05-7512-9cfd-088e11e041d1, Stand 91ad6f2: Befunde 1 und 2 geschlossen, nichts gebrochen) bereits geprüft und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); keine Prozesse des Produkts, kein cargo, kein Python-Interpreter; du schreibst nichts.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus dem vorigen Urteil, docs/beweise/roh/NAK-213-wiederpruefung-1-91ad6f2.txt; je Befund die Regel des Dirigenten aus docs/beweise/roh/NAK-213-nacharbeit-2-auftrag.txt):
1. „[P2] Weise in K-49 die tatsächliche Zustellung nach — broker/tests/sonde014_verdrahtung.rs:2056-2059. Im neuen Store-Aufbau wird kein `SessionPush` registriert; `Coordinator::mit_store` initialisiert ihn mit `None`, sodass `invalidierung_zustellen` keine Nachricht an den Abonnenten schreiben kann. Die hier erwartete offene Outbox-Schuld belegt nur Einreihung, nicht die in Auftrag Nr. 3 verlangte Zusage ‚persistiert und zugestellt'. Befund 3 bleibt teilweise offen: Der Test muss zusätzlich den empfangenen `evidence_invalidate`-Payload mit den acht IDs und `messpunkt_wechsel` prüfen." Regel: `kanalwechsel_ueber_den_heartbeat_befehl` registriert einen `SessionPush`-Abonnenten für die Sitzung und prüft den EMPFANGENEN `evidence_invalidate`-Payload: genau die acht Belege, Grund `messpunkt_wechsel`, Umfang `Ids` der Sonde; die Outbox-Prüfung bleibt zusätzlich; K-49 nennt „persistiert (event_ord) und zugestellt (Payload beim Abonnenten)"; Rotbeweis: ohne Zustellung fällt der Test.
2. „[P2] Fahre die F4-Lagen über den zugesagten Heartbeat-Ingress — broker/tests/sonde014_gegenbeispiele.rs:1179-1182. Auch die neu ergänzte Lage 2 führt den Kanalwechsel über `Buehne::deskriptor` aus, das direkt `descriptor_setzen` aufruft; ein Heartbeat mit `runtime` wird nicht gesendet. Damit fehlt weiterhin die in K-44 und Befund 4 verlangte Prüferfolge über den produktiven Ingress. Die neuen Aussagen zu Cs Ausschluss und As Konkurrenzlage sind vorhanden, prüfen aber ausschließlich den Setterpfad. Beide Lagen müssen den Wechsel über den Heartbeat auslösen." Regel: `f4_kanalwechsel` löst den Wechsel in beiden Lagen über einen echten Heartbeat mit `runtime`-Block (`host_mixer_index` 9) über den Befehlsweg aus (Muster `sonde012_sources_slice.rs::heartbeat_mit_runtime`); `Buehne::deskriptor` bleibt für die Anlage der Ausgangslage erlaubt, nicht für den gemessenen Wechsel; alle bestehenden Aussagen (12 Belege `messpunkt_wechsel`, Entfernung, Cs Ausschluss in Lage 2, As Konkurrenzlage je Lage) laufen über diesen Pfad; Rotbeweis wie in K-49.

GATE-TEXT, wörtlich (docs/bauaufteilung-sonden.md:395-400): „**Gate:** Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — „G5 ist das ungewöhnlichste Gate: Der Prüfer soll **eine falsche starke Ursachenbehauptung provozieren** — korrelierter Distraktor, Parent-Duplikat, verschobene Passage. Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer." Regeln R1 bis R8 wie in der Erstprüfung; Matrix docs/beweise/NAK-213.md §2 (K-01 bis K-55) ist Referenz; laut Manifest §5 „Nacharbeit 2": A4 GRÜN 712 Tests über 29 Binaries, A31 17/0, `sonde014_verdrahtung` 35/0 (gelesen, nicht nachgemessen). Der Worker meldet einen Nebenbefund NB-9 (die SONDE-012-Testbühne fährt das Routing beim Abonnieren fail-closed; K-49 umgeht das über vertragsgültige Heartbeats) — außerhalb der Ticketgrenze, kein Prüfgegenstand, aber prüfe, ob die Umgehung die Aussage von K-49 schwächt.

Du beantwortest genau zwei Fragen:
1. Ist jeder der zwei Befunde am Zielstand geschlossen (je Befund: ja/nein, mit Beleg Datei:Zeile)? Bei 1: registriert der Test einen echten Abonnenten und prüft den empfangenen Payload (acht IDs, `messpunkt_wechsel`, Umfang `Ids`), nicht nur die Outbox? Bei 2: laufen BEIDE Lagen über einen echten Heartbeat mit `runtime` über den Befehlsweg (`befehl.rs` → `heartbeat_kontakt`), und misst der Test danach dieselben Aussagen wie zuvor?
2. Hat der Fixdiff etwas gebrochen — eine Matrixzeile (K-44, K-49 und die von ihnen genannten Aussagen), einen Entscheid, einen Satz des Gate-Textes, eine Regel R1 bis R8, eine unverändert geltende Referenzzeile — oder eine Aussage abgeschwächt (eine frühere Prüfung entfernt, bedingt gemacht oder auf einen anderen Pfad verlegt, ohne dass die Matrixzeile das sagt)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen und Ausschlüsse wie in der Erstprüfung (docs/** und CLAUDE.md ausgeschlossen; Härtungen NAK-191 bis NAK-211 und NAK-215 bis NAK-222; NAK-214 und S31b ticketfremd; Prüfliste keine Anforderung; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung).

URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
