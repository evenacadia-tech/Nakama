# NAK-312 — S25k: Tiefenaudit 3 abarbeiten, Teil 4: Hostkante und Plugin-Lebenslauf (W02, W05, W06, W09, W22, W23, W24)

| Merkmal | Wert |
|---|---|
| Ticket | NAK-312 (Register `docs/offene-punkte.md`), Planschritt **S25k** (`docs/plan/plan.json`), Prüfstufe **T2**. Quelle: Tiefenaudit 3 (`docs/audits/2026-09-15-tiefenaudit/BEFUNDE.md`: Phasen 01, 02, 04, 05, 07, 10, 14, 16; Arbeitsübersicht Phase 16, Zeilen W02, W05, W06, W09, W22, W23, W24). Muster NAK-309 und NAK-311: Quellvalidierung durch lesende Opus-Agenten, Einordnung durch den Dirigenten, Verhaltensmatrix vor Code, Rotbeweis an der Zusagezeile, Erstprüfung je Etappe. |
| Gate | Gate-Text S25k (`docs/plan/plan.json`), wörtlich: „INHALT: 14 Befunde an der Hostkante und im Plugin-Lebenslauf — T3-05-01 und T3-05-02 (alte Hostmailbox überschreibt geladenen Klang, read-only verarbeitet Hostparameter), T3-01-02, T3-01-03, T3-01-05 (JUCE/APVTS-Locks vor der RT-Wache, State-Lock beim Offlineflag, Automation am Wandzeittakt), T3-01-04, T3-01-09, T3-01-10 (erstes Offline-Audio, geerbter Hostbypass, Gen-Reset bei aktiver Hörmarkierung), T3-04-01 und T3-04-03 (Provider nach 2-s-Detach, Messpunktpanel überlebt Prozessor), T3-02-06 und T3-07-02 (Rollenwechsel Main/Legacy: Daten und wirksame Handgriffe), T3-14-01 (Label des vorigen Ziels beim Bestätigen), T3-07-05 (Quellen ab 21 nicht erreichbar). Produktentscheide als Karten: Bypass- und Reset-Historie, MainProject-Erhalt im Legacy-Zustand, Sicht- oder Annahmekapazität. GRENZE: Audio-Thread ohne Sperren und Allokationen; State bleibt verlustfrei, fremde Bytes bytegleich; Designteil bleibt geparkt (keine neue Oberfläche, nur nötiger Handgriff); keine Legacy-Umbenennung. BEWEIS: docs/beweise/NAK-312.md mit Matrix, Rotbeweisen an B2, B6, B7, B14, B15, RT-Wache vor Parameterverarbeitung, vollem Kanon GRÜN und Laufzeit-Arm.“ |
| Klasse | **14 von 14 Befunden an der Quelle bestätigt oder präzisiert, keiner widerlegt** (§1; `docs/beweise/roh/NAK-312-quellvalidierung-teil-A.md` und `docs/beweise/roh/NAK-312-quellvalidierung-teil-B.md`, je ein lesender Opus-Agent, Effort max, 20.09.2026): **neun DEFEKT** (T3-05-01, T3-05-02, T3-01-02, T3-01-03, T3-01-05, T3-01-04, T3-04-01, T3-07-02, T3-14-01), **fünf LÜCKEN** (T3-04-03 → R-312-2 mit technischer Regel; T3-01-09 → U48, T3-01-10 → R-312-5 und U56, T3-02-06 → U49, T3-07-05 → R-312-6 und U51 mit Produktentscheid). |
| Etappe | **Etappe 0 von 8 abgeschlossen 21.09.2026** (Übernahme, Quellvalidierung, Einordnung, Regeln R-312-1 bis R-312-10, Karten U48, U49, U51 berichtigt, U56 neu). **Etappe 1 von 8 abgeschlossen 21.09.2026** (Zusagen wörtlich §5, Verhaltensmatrix M-01 bis M-77 §6, Bauplan der Etappen 2 bis 6 §7, Prüfliste §8; kein Code, kein Lauf). Matrixprüfung 1: Rohurteil NEEDS_WORK (§10.1); die vier Karten U48, U49, U51, U56 sind am 21.09.2026 beantwortet (§10.2). Validierung und Einordnung 21.09.2026 (§11): Entscheide E-312-5 bis E-312-10, Regeln R-312-11 und R-312-12, R-312-8 und R-312-10 neu gefasst; T3-01-05 geteilt, Teil b ist Folgeticket NAK-340 (Planschritt S25p). **Matrixnacharbeit 1 abgeschlossen 21.09.2026** (§12: 22 berichtigte Matrixzeilen und neun neue M-78 bis M-86, Matrix damit M-01 bis M-86; §5 bis §8 nachgezogen; kein Code, kein Lauf). Matrixprüfung 2: Rohurteil NEEDS_WORK, validiert und eingeordnet 21.09.2026 (§14): Regeln R-312-13 und R-312-14, Entscheide E-312-11 und E-312-12. **Matrixnacharbeit 2 abgeschlossen 21.09.2026** (§15: 35 geänderte Matrixzeilen, davon sechs mit Sachänderung, und 51 Präfixstellen; §6-Kopf, §6.8, §7.0, §7.2, §7.3, §7.6 und §8 nachgezogen; kein Code, kein Lauf). Gemessen in §16. **Matrixprüfung 3: PASS 21.09.2026** (§17; Lücke L-B mit Regel R-312-15, Härtung H-B im Register NAK-344). **Etappe 1 von 8 abgeschlossen (Matrix geprüft).** Etappe 2 gebaut 21.09.2026 (§18: M-01 bis M-09 grün, neun Rotbeweise, neues Bein B31; Kanon nachher GRUEN 70/70, §18.7). Gemessen, Laufzeit-Arm gefahren (§19). **Erstprüfung Etappe 2: PASS 21.09.2026** (§20; zwei Härtungen im Register NAK-345). **Etappe 2 von 8 abgeschlossen.** Etappe 3a (W02) gebaut 21.09.2026 (§21.1 bis §21.4: M-10 bis M-19 und M-78 grün, elf Rotbeweise, eine Gegenprobe). Etappe 3b (Blockbindung) gebaut 21.09.2026 (§21.5 bis §21.11: M-20 bis M-29 und M-79 bis M-82 grün, vierzehn Rotbeweise, eine Gegenprobe, Kosten gemessen). Kanon nachher GRUEN 70/70 (§21.12). Gemessen, Laufzeit-Arm gefahren (§22). Erstprüfung Etappe 3: Rohurteil PASS, validiert und eingeordnet 21.09.2026 (§23): ein Defekt (L-1, read-only-Fenster am Blockrand; E-312-13, R-312-16, neue Matrixzeile M-87), eine Lücke mit Regel R-312-17 (Register NAK-347), R-312-18. Nacharbeit 1 gebaut 21.09.2026 (§24: M-87 grün nach roter Gegenprobe, zwei Rotbeweise, eigener Rotlauf für A-19, N-1 bis N-4, Einzelbeine grün). Gemessen, Laufzeit-Arm gefahren (§25). **Wiederprüfung 1 der Etappe 3: PASS 21.09.2026** (§26). **Etappe 3 von 8 abgeschlossen.** Etappe 4 gebaut 21.09.2026 (§27: M-30 bis M-39 und der Zusatz 312/NAK-345 grün, elf Rotbeweise, eine Gegenprobe; Kanon vorher GRUEN 70/70 am Basis-SHA, Kanon nachher GRUEN 70/70 an `553b70ae`, §27.10). Gemessen, Laufzeit-Arm gefahren (§28). Erstprüfung Etappe 4: Rohurteil PASS, validiert und eingeordnet 21.09.2026 (§29): kein Defekt, L-1 ist Lücke mit Produktentscheid (Karte U57, E-312-14), H-1 und H-2 Härtung (Register NAK-348; die Behauptungszeile von B31 berichtigt Etappe 5, E-312-15). **Etappe 4 von 8 abgeschlossen.** Etappe 5 gebaut 21.09.2026 (§30: Offline-Übergang `19ddf5d3` und Parameteranbindung `be37d10a`, Parameteranbindung UMGEBAUT; M-04, M-40 bis M-54 und M-83 grün, 16 Rotbeweise, zwei Gegenproben; Kanon vorher geerbt, Kanon nachher GRUEN 70/70 an `be37d10a`, §30.15). Gemessen, Laufzeit-Arm zweimal gefahren (§31): **Befund LZ-1 — das Diagnose-FL verschwindet am Stand der Etappe 5 nach 105 Sekunden Wiedergabe**, Ursache nicht gemessen; die Erstprüfung wartet. Nacharbeit 1 der Etappe 5 gebaut und gemessen 21.09.2026 (§32): LZ-1 tritt auch am Produktstand der Etappe 4 auf — FLs reguläres Programmende (Exitcode 0, eigene Schließroutine, gleiche Spur wie `WM_CLOSE`), kein Pluginweg dorthin, nicht die Etappe 5; wer das Ende auslöst, ist nicht gemessen. Neues Laufzeit-Szenario `schleife-dauerlauf` und Kanonteil 312/M-88 (`2c138b2d`), kein Produktcode; Kanon nachher GRUEN 70/70 an `2c138b2d`, Szenario am Endstand zweimal grün (§32.9, §32.10). Nacharbeit 1 gemessen und §31.3 berichtigt (§33): **LZ-1 ist FLs reguläres Programmende, tritt auch am Stand der Etappe 4 auf und ist kein Defekt der Etappe 5**; M-88 ist Regressionswache, Auslöser im Register NAK-352. Erstprüfung Etappe 5: Rohurteil PASS, validiert und eingeordnet 21.09.2026 (§34): kein Defekt, zwei Lücken mit Regeln R-312-20 bis R-312-22 (neue Matrixzeilen M-89 und M-90, M-40 um den Allokationszähler erweitert — Zusätze der Etappe 6, nur Tests), Härtungen im Register NAK-355, JUCE-eigene Parametersperre als NAK-356. **Etappe 5 von 8 abgeschlossen.** Quellvalidierung NAK-349 am 21.09.2026 bestätigt (§35): DEFEKT gegen R-312-2, Matrixzeilen M-91 und M-92, Regel R-312-23, gebaut in 6b. Die Etappe 6 ist geteilt (§35.3): 6a Hörmarkierung und Test-Zusätze (Einzelbeine), 6b Gen-Fläche, NAK-349, voller Kanon und Erstprüfung. **Etappe 6 von 8: Auftrag 6a liegt, Bauer startet** Etappe 6a, Änderungssatz A gebaut 21.09.2026 (§36.1 bis §36.9: Rücknahme im Klassifikationswechsel, drei Netze in jeder Fläche, ehrliche Meldung in Legacy; M-55 bis M-58 und M-84 grün, fünf Rotbeweise, eine Gegenprobe; Einzelbeine A3, B15, B14, A1 grün). **Etappe 6 von 8: 6a Änderungssatz A gebaut, B folgt** Etappe 6a, Änderungssatz A ist `ba32bc98`; Änderungssatz B gebaut 21.09.2026 (§36.10 bis §36.17: reset und releaseResources schließen das Intervall sperrfrei über den RT-Ring, Nummernprüfung im Abschluss von prepareToPlay; M-59 bis M-65 und M-86 grün, acht Rotbeweisdateien, eine Gegenprobe; Einzelbeine A3, B14, A1, A14 grün). **Etappe 6 von 8: 6a Änderungssätze A und B gebaut, C folgt** Etappe 6a, Änderungssatz B ist `9aa776da`; Änderungssatz C gebaut 21.09.2026 (§36.18 bis §36.24: 312/M-89, 312/M-90 und der Allokationszähler in 312/M-40 grün, H-3 rot gefahren und zurück, H-4 im Textriegel; kein Produktcode; Einzelbeine B7, B2, B6, A16 grün; kein voller Kanon). Etappe 6a gemessen 21.09.2026 (§37: drei Commits bis `5f530041`, 20 Rohdateien, A-78 und A-81 für die Erstprüfung vorgemerkt, Nebenfund im Register NAK-357). **Etappe 6 von 8: 6a gemessen, Auftrag 6b liegt, Bauer startet** Etappe 6b, Änderungssatz A gebaut 21.09.2026 (§38.1 bis §38.9: Labelentwurf mit Entwurfskennung, Bestätigung beim Auswahlwechsel, Escape verwirft; M-66 bis M-72 grün, sieben Rotbeweisdateien, eine Gegenprobe; Einzelbeine B15, B14, B13, A1 grün). **Etappe 6 von 8: 6b Änderungssatz A gebaut, B folgt** Etappe 6b, Änderungssatz A ist `db682244`; Änderungssatz B gebaut 21.09.2026 (§38.10 bis §38.17: Ersatzziel nach der Anzeigeordnung, Aktionssteuerung nur auf gezeichneten Zeilen, Entwurf vor dem Leeren bestätigt, ehrlicher Status; M-73 bis M-76 und M-85 grün, fünf Rotbeweisdateien, eine Gegenprobe; Einzelbeine B15, B14, B13, A1 grün). **Etappe 6 von 8: 6b Änderungssätze A und B gebaut, C folgt** Etappe 6b, Änderungssatz B ist `53754c66`; Änderungssatz C gebaut 21.09.2026 (§38.18 bis §38.25: der Konfliktrückruf fängt einen SafePointer und prüft ihn vor jedem Zugriff; M-91 und M-92 grün, zwei Rotbeweisdateien, eine Gegenprobe nach R-312-23; Einzelbeine B15, A1 grün). **Etappe 6 von 8: 6b A bis C gebaut, Kanon folgt** Etappe 6b, Änderungssatz C ist `6fbe396a`, die A3-Behauptungszeile `86c31f46` (§38.26); Kanon nachher der ganzen Etappe 6 **GRUEN 70/70** an `86c31f46` (§38.27). **Etappe 6 von 8: 6b gebaut, Kanon grün, Erstprüfung folgt** Etappe 6b gemessen 22.09.2026 (§39: fünf Commits bis `230d4bc4`, Kanon der ganzen Etappe 6 GRUEN 70/70 am Stand `86c31f46`; Laufzeit-Arm und Erstprüfung stehen aus; Sparmodus, Übergabe §39.3). Laufzeit-Arm der Etappe 6 gefahren 22.09.2026 (§40.1: VORAUSSETZUNG nur an U43, kein Befund); User-Entscheid NAK-358 (§40.2). Sparmodus aufgehoben, Bauer Opus 5.5 max (NAK-359, §41.1). Erstprüfung Etappe 6: Rohurteil PASS 22.09.2026 (§41.2: 0 Defekte, 6 Lücken, 6 Härtungen), Validierung läuft (§41.3). **Etappe 6 von 8: Erstprüfung PASS (roh), Validierung läuft** |
| Urteil | Kein Ticketurteil. Matrixprüfung 1: Rohurteil NEEDS_WORK 21.09.2026 (frischer, lesender Opus-Thread `97cc511f`, Effort max, HEAD `b7d82137` vor und nach dem Lauf gleich; neun Defekte, drei Lücken, drei Härtungen; §10.1); an der Quelle validiert (Opus-Thread `47dddc88`, Effort max: 8 bestätigt, 7 präzisiert, 0 widerlegt) und eingeordnet in §11: 13 Defekte für die Matrixnacharbeit 1, eine Lücke mit Regel R-312-12, eine Härtung (Register NAK-342). Matrixprüfung 2: Rohurteil NEEDS_WORK 21.09.2026 (frischer, lesender Opus-Thread `09302388`, Effort max, HEAD `41651542` vor und nach dem Lauf gleich; alle dreizehn Befunde der Runde 1 geschlossen, drei neue Defekte D-A bis D-C, eine Lücke, eine Härtung; §14.1); validiert (Opus-Thread `d61dbe9b`, Effort max: 2 bestätigt, 3 präzisiert, 0 widerlegt) und eingeordnet in §14.3: drei Defekte und eine Lücke mit Regel für die Matrixnacharbeit 2. Matrixprüfung 3: **PASS** 21.09.2026 (frischer, lesender Opus-Thread `54163af1`, Effort max, HEAD `75b66066` vor und nach dem Lauf gleich; 0 Defekte, eine Lücke mit Regel R-312-15, eine Härtung im Register NAK-344; §17). Erstprüfung Etappe 2: **PASS** 21.09.2026 (frischer, lesender Opus-Thread `bb00661f`, Effort max, T2, HEAD `4949f023` vor und nach dem Lauf gleich; 0 Defekte, 0 Lücken, 2 Härtungen im Register NAK-345; §20). Erstprüfung Etappe 3: Rohurteil PASS 21.09.2026 (Opus-Thread `a347e0fa`, Effort max, T2, HEAD `6dc9d708` vor und nach dem Lauf gleich); validiert (Opus-Thread `eb952162`, Effort max: 1 bestätigt, 3 präzisiert, 0 widerlegt) und eingeordnet in §23.3: L-1 ist DEFEKT und geht in die Nacharbeit 1 der Etappe 3, L-2 ist Lücke mit Regel R-312-17. Wiederprüfung 1 der Etappe 3: **PASS** 21.09.2026 (frischer, lesender Opus-Thread `35638327`, Effort max, HEAD `d1b20683` vor und nach dem Lauf gleich; 0 offene Befunde, 0 Brüche; §26). Erstprüfung Etappe 4: **PASS** 21.09.2026 (frischer, lesender Opus-Thread `7a3e2efe`, Effort max, T2, HEAD `9e01ecac` vor und nach dem Lauf gleich; 0 Defekte, eine Lücke mit Produktentscheid (Karte U57), zwei Härtungen im Register NAK-348; validiert von Opus-Thread `9abee352`, Effort max: 4 bestätigt, 3 präzisiert, 0 widerlegt; §29). Erstprüfung Etappe 5: **PASS** 21.09.2026 (frischer, lesender Opus-Thread `45f8c291`, Effort max, T2, HEAD `6ffc8578` vor und nach dem Lauf gleich; 0 Defekte, zwei Lücken mit Regeln R-312-20 bis R-312-22, vier Härtungen im Register NAK-355; validiert von Opus-Thread `048ab14b`, Effort max: 3 bestätigt, 5 präzisiert, 0 widerlegt; §34). Erstprüfung Etappe 6: Rohurteil **PASS** 22.09.2026 (frischer, lesender Opus-5.5-Thread `23eb5a93`, Effort max, T2, HEAD `87e8af15` vor und nach dem Lauf gleich; 0 Defekte, 6 Lücken, 6 Härtungen; Validierung §41.3). Noch kein Ticketurteil. |
| Prüfstufe | **T2 gefordert** (Muster NAK-309, NAK-311). |
| Prüfmodell | Codex `gpt-6-astra`, Effort max, lesend, für Matrixprüfung, Erstprüfung je Etappe und Abschlussprüfung. Solange die Codex-Woche über 90 % steht (20.09.2026, 23:31 Uhr: 94 %, Reset 22.09.2026, 22:19 Uhr), prüft ein frischer, lesender Opus-Thread, Effort max (Skill §3.6, Muster NAK-309 und NAK-311); nie der Bauer. Bauer: frischer Opus-5.5-Worker (`--model opus`), Effort max (User 22.09.2026, NAK-359; löst xhigh aus NAK-335 ab). |
| Aufsicht | **ENG** (Audio, State, Nebenläufigkeit, Lebenslauf). |
| Basis-SHA | Etappe 0: `090d0fa10ce44fa8c6c9ac795982584c6ebcefb7` (= `git rev-parse HEAD` = `origin/master` beim Start, 20.09.2026, 23:30 Uhr, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die fremden untracked Ordner `briefing-hub/` und `nimbalyst-local/`; `claude agents --json` ohne aktiven Worker). Die Auditmessungen der Phase 16 liefen an `aff2d818`; beide Validierer haben jede Zeilennummer am HEAD `090d0fa1` selbst nachgelesen. Etappe 1: `945ebfb1a0a2d3ac5e97e937997c0f0532ef16f8` (= `git rev-parse HEAD` beim Start, 21.09.2026; `git status --short` zeigt nur die zwei fremden untracked Ordner `briefing-hub/` und `nimbalyst-local/`; `git diff --stat 090d0fa1 HEAD -- eq-copilot broker tools` ist **leer**, gemessen — die Zeilennummern beider Quellvalidierungen gelten am HEAD unverändert, und jede Zeilenangabe in §5 bis §8 ist dort selbst nachgelesen). Matrixnacharbeit 1: `abb64275a66bdae989495a2c9ae20b6b315f00e6` (= `git rev-parse HEAD` beim Start, 21.09.2026; `git status --short` zeigt nur die zwei fremden untracked Ordner `briefing-hub/` und `nimbalyst-local/`; `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` ist **leer**, gemessen — die Zeilenangaben in §5 bis §8 gelten damit unverändert, und jede in der Nacharbeit geschriebene oder geänderte Angabe ist am HEAD `abb64275` selbst nachgelesen). Matrixnacharbeit 2: `801cb75a9ca68cbd3569f1d1bd7d617cc844e096` (= `git rev-parse HEAD` beim Start, 21.09.2026; `git status --short` zeigt nur die zwei fremden untracked Ordner `briefing-hub/` und `nimbalyst-local/`; `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` und `git diff --stat abb64275 HEAD -- eq-copilot broker tools` sind **leer**, gemessen — die Zeilenangaben in §5 bis §8 gelten weiter zum Stand `945ebfb1`, und jede in dieser Nacharbeit geschriebene oder geänderte Angabe ist am HEAD `801cb75a` selbst nachgelesen). Etappe 2: `f96bdd4a3834ef4469e110d2a286df0b86f4ff5d` (= `git rev-parse HEAD` beim Start, 21.09.2026, 04:04:43, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` ist **leer**, gemessen — der Bauplan gilt am Basis-SHA zeilengleich; §18.1). Etappe 3: `cb473944a12410e17f3205e231b32f35f92805ac` (= `git rev-parse HEAD` beim Start, 21.09.2026, 06:02:44, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; die Zeilenangaben des Bauplans sind am Basis-SHA nachgelesen, §21.1). Etappe 4: `a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c` (= `git rev-parse HEAD` beim Start, 21.09.2026, 10:35:47, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; die Zeilenangaben des Bauplans sind am Basis-SHA nachgelesen, §27.1). Etappe 5: `12300f1e429c38354f9d216d7ade395b71f71b70` (= `git rev-parse HEAD` beim Start, 21.09.2026, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; die Zeilenangaben des Bauplans sind am Basis-SHA nachgelesen, §30.1). Etappe 6a: `fd287d3c8ea277814c80e67365ea782e6af9994d` (= `git rev-parse HEAD` beim Start, 21.09.2026, 20:49:32, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; die Zeilenangaben des Bauplans sind am Basis-SHA nachgelesen, §36.1). Etappe 6b: `ed1722d992f64567e131aaa7fa5ae887bdef96bf` (= `git rev-parse HEAD` beim Start, 21.09.2026, 22:19:25, nach `git pull --ff-only` ohne Änderung; `git status --short` zeigt nur die zwei fremden untracked Ordner; die Zeilenangaben des Bauplans sind am Basis-SHA nachgelesen, §38.1). |
| Kanon vorher | Geerbt, solange kein Produktpfad wandert: NAK-311 Endstand `2d6519e8`, 20.09.2026, 22:35 bis 23:18 Uhr, GRUEN 69/69 (`docs/beweise/roh/NAK-311-2d6519e-dirty.md`). Der Bauer der ersten Codeetappe misst `git diff --stat 2d6519e8 HEAD -- eq-copilot broker tools`; ist es nicht leer, fährt er den Kanon vorher selbst. Etappe 2: **geerbt** — der Unterschied zu `2d6519e8` ist allein `eq-copilot/install/nakama-installer-v1.json` (zwei Bundle-Hashes und `hashes_erzeugt_am`, 3 Zeilen), kein Pfad unter `eq-copilot/plugin`, `broker` oder `tools` (§18.1). Etappe 3: **geerbt** — der Unterschied zu `89432468` (Kanon nachher der Etappe 2, GRUEN 70/70) ist allein `eq-copilot/install/nakama-installer-v1.json` (3 Zeilen, Nachhashen des Laufzeit-Arms, §19), kein Pfad unter `eq-copilot/plugin`, `broker` oder `tools` (§21.1). Etappe 4: **selbst gefahren**, weil die Nacharbeit 1 der Etappe 3 (`32813efd`) Produktcode änderte und nur Einzelbeine fuhr: **GRUEN 70/70**, Exitcode 0, am Basis-SHA `a80f0603`, 21.09.2026, 10:36:26 bis 11:24:06 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 4 vorher", Rohausgabe `docs/beweise/roh/NAK-312-a80f060-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend, A33 OK; §27.1). Etappe 5: **geerbt** — der Unterschied zu `553b70ae` (Kanon nachher der Etappe 4, GRUEN 70/70) ist allein `eq-copilot/install/nakama-installer-v1.json` (3 Zeilen, Nachhashen des Laufzeit-Arms, §28), kein Pfad unter `eq-copilot/plugin`, `broker` oder `tools` (§30.1). Etappe 6: für den Stand vor der Etappe 6 gilt der Kanon nachher der Nacharbeit 1 der Etappe 5 (GRUEN 70/70 an `2c138b2d`); 6a hat Produktcode geändert und nur Einzelbeine gefahren (§36), `git diff --stat 2c138b2d ed1722d9 -- eq-copilot broker tools` zeigt elf Dateien — der Kanon am Ende von 6b misst die ganze Etappe 6 (§38.1). |
| Kanon nachher | Etappe 2: **GRUEN 70/70**, Exitcode 0, Commit `89432468`, 21.09.2026, 04:42:20 bis 05:27:17 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 2", Rohausgabe `docs/beweise/roh/NAK-312-8943246-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend, A33 OK; §18.7). Etappe 3: **GRUEN 70/70**, Exitcode 0, Commit `ad18d984`, 21.09.2026, 07:21:13 bis 07:59:43 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 3", Rohausgabe `docs/beweise/roh/NAK-312-ad18d98-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend mit denselben Zählwerten wie in Etappe 2, A33 OK; §21.12). Etappe 4: **GRUEN 70/70**, Exitcode 0, Commit `553b70ae`, 21.09.2026, 12:17:28 bis 12:57:04 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 4", Rohausgabe `docs/beweise/roh/NAK-312-553b70a-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend mit denselben Zählwerten wie der Kanon vorher, A33 OK; §27.10). Etappe 5: **GRUEN 70/70**, Exitcode 0, Commit `be37d10a`, 21.09.2026, 15:10:46 bis 15:54:18 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 5", Rohausgabe `docs/beweise/roh/NAK-312-be37d10-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend mit denselben Zählwerten wie die Kanons der Etappe 4, A33 OK; §30.15). Etappe 5, Nacharbeit 1: **GRUEN 70/70**, Exitcode 0, Commit `2c138b2d`, 21.09.2026, 17:44:58 bis 18:29:51 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 5 Nacharbeit 1", Rohausgabe `docs/beweise/roh/NAK-312-2c138b2-dirty.md`; „dirty" sind die zwei fremden untracked Ordner und fünf Rohdateien der Nacharbeit; A32 HINWEIS nicht blockierend mit denselben Zählwerten, A33 OK mit 58; §32.9). Etappe 6 (6a und 6b): **GRUEN 70/70**, Exitcode 0, Commit `86c31f46`, 21.09.2026, 23:30:21 bis 22.09.2026, 00:20:49 (Runnerblock „Kanon-Lauf - NAK-312 Etappe 6", Rohausgabe `docs/beweise/roh/NAK-312-86c31f4-dirty.md`; „dirty" sind allein die zwei fremden untracked Ordner; A32 HINWEIS nicht blockierend mit denselben Zählwerten wie die Kanons der Etappen 4 und 5, A33 OK mit 58 von 58; §38.27). |
| Laufzeit-Arm | Etappe 2, §19: `LAUFZEIT NAK-312 aa074e42 VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`, Exit 3 allein wegen Karte U43, **Nulltest im Host BITIDENTISCH**, Rohdaten `docs/beweise/roh/NAK-312-laufzeit-aa074e42.md`. Etappe 3, §22: `LAUFZEIT NAK-312 dc2aaf1c VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`, Exit 3 allein wegen Karte U43, **Nulltest im Host BITIDENTISCH**, Rohdaten `docs/beweise/roh/NAK-312-laufzeit-dc2aaf1c.md`. Etappe 3 Nacharbeit 1, §25: `LAUFZEIT NAK-312 32813efd VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`, Exit 3 allein wegen Karte U43, **Nulltest im Host BITIDENTISCH**, Rohdaten `docs/beweise/roh/NAK-312-laufzeit-32813efd.md`. Etappe 4, §28: `LAUFZEIT NAK-312 dc2665ba VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`, Exit 3 allein wegen Karte U43, **Nulltest im Host BITIDENTISCH**, Rohdaten `docs/beweise/roh/NAK-312-laufzeit-dc2665ba.md`. Etappe 5, §31: `LAUFZEIT NAK-312 64ece9e1 VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=2 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=3]` in zwei von zwei Läufen, Exit 3 — **NICHT allein wegen U43: das Diagnose-FL verschwindet während der Wiedergabe (LZ-1)**; Nulltest im Host BITIDENTISCH; Rohdaten `docs/beweise/roh/NAK-312-laufzeit-64ece9e1-lauf1.md`, `docs/beweise/roh/NAK-312-laufzeit-64ece9e1.md`, `docs/beweise/roh/NAK-312-etappe5-flwache.txt`. Etappe 5, Nacharbeit 1, §32.10: am Endstand zweimal nacheinander `LAUFZEIT NAK-312 2c138b2d VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=6 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,schleife-dauerlauf.json=0,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`, Exit 3 allein wegen Karte U43, **`schleife-dauerlauf` (M-88) grün**, Nulltest im Host BITIDENTISCH; Rohdaten `docs/beweise/roh/NAK-312-laufzeit-2c138b2d-lauf1.md`, `docs/beweise/roh/NAK-312-laufzeit-2c138b2d.md`, `docs/beweise/roh/NAK-312-n1-flwache.txt`; LZ-1 am Produktstand der Etappe 4 gemessen (§32.3). |
| Ticketpfade | Vorläufig, präzisiert im Bauplan der Etappe 1: `eq-copilot/plugin/sonde/SondeProcessor.cpp`, `eq-copilot/plugin/sonde/SondeProcessor.h`; `eq-copilot/plugin/src/PluginProcessor.h`, `eq-copilot/plugin/src/PluginEditor.cpp`, `eq-copilot/plugin/src/prozessor/State.cpp`, `eq-copilot/plugin/src/prozessor/Hostbruecke.cpp`; die Hörmarkierung und das Quellenmodell von Gen; `eq-copilot/plugin/state/` nur für Ladestart, `nurLesen` und Transaktionskern; `eq-copilot/plugin/dsp/` nur für den Bereich der RT-Wache und den Offline-Übergang; `eq-copilot/plugin/tests/**` für Regressionen; `tools/beweise.ps1` nur Behauptungszeilen und, falls die Matrix ein neues Bein verlangt, dessen Deklaration; `docs/plugin-wissen.md`; `docs/beweise/NAK-312.md`, `docs/beweise/roh/NAK-312-*`. |
| Grenze | Grundgesetz „nichts Ungefragtes“: ausgeschaltet bitidentisch, sonst Passthrough sampleidentisch; im Audio-Thread keine Sperre, Allokation, kein Datei-, Pipe- oder Netzzugriff, kein Logging. State bleibt verlustfrei, fremde Bytes bytegleich, read-only unverändert. Keine neue Stateversion und keine v3-Vertragsänderung ohne Matrixzeile und Vertrag; Identität eingefroren (NAK-30: Parameter-IDs, Reihenfolge, Bereiche, Class-IDs); Legacy-Bezeichner bleiben; Design geparkt — keine neue Oberfläche, nur der nötige Handgriff oder die ehrliche Zustandsmeldung. Sicherheitsbefunde T3-12-* gehören zu S25i. Der gepinnte JUCE-VST3-Wrapperpatch (Entwurf §44.3) gehört nicht in dieses Ticket. **Kein Produktentscheid wird vor der Antwort des Users gebaut** (U48, U49, U51, U56). Kein Produktcode, bevor die Verhaltensmatrix (Etappe 1) geprüft ist. |

**Alles unter diesem Kopf ist append-only Verlauf.** Der lebende Kopf wird beim
Abschluss nachgezogen; Positionen im Kopf stehen als Symbol oder Anker, im
Verlauf gilt eine Zeilennummer zum Stand des Abschnitts, der sie trägt
(`tools/dirigent/pruefliste.md`, Abschnitt E).

---

## 0. Übernahme (Dirigent, 20.09.2026, 23:30 Uhr)

**Reihenfolge im Plan.** „Als Nächstes“ nennt S25i (NAK-310, Pipe-Sicherheit);
dessen Gate-Text stellt es selbst hinter den Codex-Wochenreset (Prüfer
`gpt-5.6-sol` max; Codex-Woche 94 %, Reset 22.09.2026, 22:19 Uhr) und lässt
S25j ff. vorlaufen. S25j (NAK-311) ist seit 20.09.2026 T2 PASS. S25g (NAK-289,
Etappen 2 und 3 der clang-tidy-Ratsche) bleibt hinter den Audit-Tickets, weil
beide Etappen dieselben State- und Prozessordateien anfassen wie S25k und S25l
(Begründung aus `docs/beweise/NAK-311.md` §0, unverändert).

**Gegenstand.** Die 14 Befund-IDs des Gates.

**Quellvalidierung (Etappe 0).** Zwei lesende Opus-Agenten, Effort max, am HEAD
`090d0fa1`, 20.09.2026, 23:36 Uhr bis 21.09.2026, 00:1x Uhr: Teil A (W02, W05,
W06: T3-05-01, T3-05-02, T3-01-02, T3-01-03, T3-01-05, T3-01-04, T3-01-09,
T3-01-10; Karte U48) nach `docs/beweise/roh/NAK-312-quellvalidierung-teil-A.md`,
Teil B (W09, W22, W23, W24: T3-04-01, T3-04-03, T3-02-06, T3-07-02, T3-14-01,
T3-07-05; Karten U49 und U51) nach
`docs/beweise/roh/NAK-312-quellvalidierung-teil-B.md`. Aufträge:
`docs/beweise/roh/NAK-312-quellvalidierung-teil-A-auftrag.txt` und
`docs/beweise/roh/NAK-312-quellvalidierung-teil-B-auftrag.txt`. Je Befund:
Quellkette zitiert, Zusage wörtlich, heutige Abdeckung, Urteil,
Kategorievorschlag, kleinster Fix, Ort des Rotbeweises; dazu gemeinsame
Ursachen, Kartenprüfung, Reihenfolge, Nichtgeprüftes. Ausschließlich
Quellenlektüre: kein Bau, kein Lauf, kein Host. HEAD blieb während beider Läufe
`090d0fa1`; `git status --short` zeigte danach nur die zwei Berichte, die zwei
Aufträge und die fremden Ordner.

## 1. Einordnung je Befund (Dirigent, 21.09.2026, 00:3x Uhr)

Grundlage: beide Validierungen. Die Einordnung folgt dem Gate-Satz, den
Invarianten in `CLAUDE.md` und den zitierten Zusagen; Schwere und Kategorie
des Audits sind Hinweis, nicht Maß. Stichproben des Dirigenten an der Quelle
(HEAD `090d0fa1`): `eq-copilot/plugin/sonde/SondeProcessor.h:523` (`controlV3`)
steht vor `:537` (`transaktion`), der Transaktionskern stirbt also zuerst, und
`eq-copilot/plugin/sonde/SondeProcessor.cpp:1143` liest `transaktion->revision()`
im Providerweg; `SondeProcessor.cpp:1321-1328` nimmt in `setNonRealtime` bei
jedem Aufruf mit `offline == true` das `zustandSchloss`;
`eq-copilot/plugin/src/prozessor/State.cpp:321-322` leert
`mainProjectMitglieder` beim Klassenwechsel ohne Speichern;
`eq-copilot/plugin/src/PluginEditor.cpp:958-959` klemmt die Zeilenhöhe auf 18
bis 34 Pixel, ohne die Zeilenzahl zu begrenzen.

| ID | Paket | Urteil der Validierung | Einordnung | Schwere | Rotbeweis fällt an | Folge |
|---|---|---|---|---|---|---|
| T3-05-01 | W02 | BESTÄTIGT, präzisiert: die Hashhälfte von M-84 hält, die Audiohälfte bricht; `prepareToPlay` heilt nicht | **DEFEKT** (SONDE-015 M-84, State-Invariante) | high | B7 `TransactionTestMain.cpp`, M-84-Block, Ladestart in dieselbe Instanz | Etappe 3, ein Änderungssatz mit T3-05-02 |
| T3-05-02 | W02 | BESTÄTIGT, präzisiert: Originalbytes bleiben, die Neutralitätszusage bricht | **DEFEKT** (SONDE-015 M-92) | high | B7 `TransactionTestMain.cpp`, M-92-Block, Hostparameter nach dem Laden | Etappe 3 |
| T3-01-05 | W05 | BESTÄTIGT, Schwere gehalten: im Offline-Render fällt der größte Teil der Hostwerte aus | **DEFEKT** (SONDE-015 M-120; Entwurf §44.3 Rückfallsatz „letzter Blockwert“) | high | B7, neuer Fall am echten Prozessor | Etappe 3, eigener Änderungssatz, R-312-10 |
| T3-01-03 | W05 | BESTÄTIGT, präzisiert: nur offline, aber in jedem Offlineblock | **DEFEKT** (Grundgesetz: keine Sperre im Audio-Thread) | medium | B7, M-120-Block, Sperrenzähler um `setNonRealtime (true)` | Etappe 5, ein Änderungssatz mit T3-01-04 |
| T3-01-04 | W06 | PRÄZISIERT: Quellkette trägt; Erreichbarkeit hängt an einer ungemessenen FL-Reihenfolge | **DEFEKT** (SONDE-015 M-120), R-312-3 | medium | B7, M-120-Block, ohne die acht verworfenen Blöcke | Etappe 5 |
| T3-01-02 | W05 | BESTÄTIGT, präzisiert: zwei Sperrfamilien in JUCE, Gegenspieler `hostParameterAbgleichen`; die Wache ist bauartbedingt eng | **DEFEKT** (Grundgesetz), dazu zu weite Behauptungszeile; R-312-1 und R-312-4 | medium | B6 `DspGoldenTestMain.cpp`, M-47-Sperrenzähler, Messung oberhalb `DspKern::verarbeite` | Etappe 2 (Messgerät), Etappe 5 (Fix) |
| T3-01-09 | W06 | BESTÄTIGT als LÜCKE, präzisiert: Probeeq misst im Hostbypass nicht mehr; auch der interne Bypass erhält den Filterzustand nicht (E-8) | **LÜCKE → Produktentscheid U48** | medium | A16 `SondeNullTestMain.cpp` (Hostbypass-Eintritt), Gegenfall B6 | Etappe 7 |
| T3-01-10 | W06 | BESTÄTIGT als LÜCKE, präzisiert: der Verlust ist das offene Interventionsintervall; `releaseResources` hat dieselbe Lücke | **LÜCKE → Regel R-312-5** (Technik, sicherer Teil) und **Produktentscheid U56** (Auftrag nach dem Anhalten) | medium | A3 `MarkierungTestMain.cpp`, neuer Fall aktive Markierung → `reset()` | Etappe 6 (R-312-5), Etappe 7 (U56) |
| T3-04-01 | W09 | BESTÄTIGT, präzisiert: das zuerst sterbende Mitglied ist bestimmbar; kein Bein kann den Pfad heute bauen | **DEFEKT** (R-D2, `docs/beweise/NAK-246.md`) | high | neues Sonde-Lebenslaufbein; vorher Testzugänge der Sonde (R-312-7) | Etappe 2 (Testzugänge), Etappe 4 (Fix) |
| T3-04-03 | W09 | BESTÄTIGT, präzisiert: der Kommentar am Übergabepunkt behauptet einen Schutz, den es nicht gibt | **LÜCKE → Regel R-312-2** (Technik) | high | B15 oder B14 mit modalem Abbau | Etappe 4 |
| T3-07-02 | W22 | BESTÄTIGT, präzisiert: drei trennbare Defekte; Fensterschließen bleibt als einziger Rückweg | **DEFEKT** (`CLAUDE.md`, keine toten UI-Elemente, kein unsichtbar bewaffneter Handgriff) | high | A3 `MarkierungTestMain.cpp` | Etappe 6, mit R-312-5 geplant, R-312-8 |
| T3-14-01 | W23 | PRÄZISIERT: U02 ist nicht gebrochen, gebrochen ist das Gesetz vom 24.08.2026 | **DEFEKT** | medium | B13 oder B15 neben `Sonde012SourcesModelTest.cpp` | Etappe 6, R-312-9 |
| T3-07-05 | W24 | BESTÄTIGT als LÜCKE, zwei Präzisierungen: Quellen ab 21 sind ungezeichnet; das Ersatz-Hauptziel kann auf eine ungezeichnete Quelle fallen | **LÜCKE → Regel R-312-6** (Technik, Ersatzziel) und **Produktentscheid U51** (Kapazität) | medium | B15 `EqCopShot` mit 20, 21, 32, 64 Quellen | Etappe 6 (R-312-6), Etappe 7 (U51) |
| T3-02-06 | W22 | PRÄZISIERT: der Live-Rückweg erhält die Daten nicht vollständig; die bestätigten Quellen sind schon beim Rollenwechsel weg | **LÜCKE → Produktentscheid U49** | medium | B2 `StateMigrationTestMain.cpp`, Gegenwache B14 | Etappe 7, nach W02 |

**Abweichungen von den Vorschlägen der Agenten.**

- Teil A schlägt die Hebung der RT-Wache als eigenes Ticket nach Muster S19b
  vor. Der Gate-Satz verlangt wörtlich „RT-Wache vor Parameterverarbeitung“ als
  Beweis dieses Tickets; sie ist deshalb Etappe 2 hier (R-312-1).
- Teil A stellt die Einordnung von T3-01-04 hinter eine Hostmessung (ruft FL
  vor einem Offline-Render `prepareToPlay`?). M-120 gibt die Zusage ohne
  Hostvorbehalt; die Einordnung DEFEKT steht, die Hostmessung ist Beobachtung,
  kein Bauvorbehalt (R-312-3).
- Teil B ordnet T3-04-03 als LÜCKE ohne Regelvorschlag mit Namen ein; der
  Dirigent entscheidet die Besitzregel als Technik (R-312-2), weil sie nur
  eine sinnvolle Richtung hat: ein Rückruf fasst nichts Totes an.
- Teil A beschreibt für T3-01-05 einen Mechanismus, der den Verlust nur
  verringert. Maß ist die Zusage, nicht der Mechanismus (R-312-10); der Bauplan
  der Etappe 1 legt den Mechanismus vor.

**Gemeinsame Ursachen (aus beiden Validierungen, vom Dirigenten übernommen).**
W02: der Ladestart kennt seine Nachbarn nicht (Mailbox, `nurLesen` im
Automationsweg) — ein Änderungssatz, eine Regression. W05: gleiche Datei und
gleicher Thread, drei verschiedene Verträge — getrennte Änderungssätze, nie
eine pauschale „Hostkante lockfrei“-Änderung. T3-01-03 und T3-01-04 sind ein
Fix in `setNonRealtime`; nur die Sperre zu entfernen verschärft T3-01-04.
T3-01-09 und T3-01-10 teilen die Produktfrage, nicht den Code. T3-04-01 und
T3-04-03 teilen die Prüfform (Callback festhalten, Eigentümer abbauen,
freigeben, Marke lesen), nicht die Reparatur (Schleuse gegen Lebendprüfung).
T3-02-06 und T3-07-02 hängen an `setzeBindung`; T3-07-02 und T3-01-10 bewegen
dieselben Rücknahmewege der Hörmarkierung und gehören in eine Etappe.
T3-14-01 und T3-07-05 setzen beide in `aktualisiereSourcesSteuerung` an.

**Irreführende Kommentare** (gehören in den Änderungssatz, der die Stelle
repariert): `eq-copilot/plugin/src/PluginEditor.cpp:731-733` (behaupteter
SafePointer-Schutz für den Prozessorzugriff), `PluginEditor.cpp:268-269` (drei
Sicherheitsnetze, die in der Main-Fläche nicht ticken),
`eq-copilot/plugin/sonde/SondeProcessor.cpp:153` (Taktbezug der Automation).

## 2. Regeln des Dirigenten (21.09.2026)

- **R-312-1 (Messgerät zuerst; T3-01-02, T3-01-03).** Die RT-Wache deckt den
  Audio-Callback der Sonde ab seinem Eintritt (`SondeProcessor::processBlock`),
  nicht erst ab `DspKern::verarbeite`. Die Behauptungszeilen der Beine, die
  sich auf die Wache stützen, sagen genau, was der Zähler sieht; Sperren, die
  JUCE vor dem Plugincode nimmt, kann die Wache nicht sehen, und die Zeile
  nennt diese Grenze. Verhaltensneutral; steht vor jedem Sperren-Rotbeweis.
- **R-312-2 (T3-04-03).** Ein asynchroner Rückruf des Editors (modales Panel,
  verzögerter Aufruf) fasst Prozessor und Editor nur nach einer Lebendprüfung
  an; nach dem Ende seines Eigentümers ist er wirkungslos: kein Zugriff, keine
  Mutation, keine Dirty-Meldung. Der Handgriff bleibt derselbe.
- **R-312-3 (T3-01-04, mit T3-01-03).** Die ersten Samples eines Offline-Renders
  tragen den bestätigten Zustand, nie die Vorschau (M-120), unabhängig davon,
  ob der Host vorher `prepareToPlay` ruft. T3-01-03 und T3-01-04 sind ein
  Änderungssatz. Ob FL vor einem Offline-Render deaktiviert, wird im
  Laufzeit-Arm beobachtet, wenn das ohne neue Infrastruktur geht; sonst
  datierte Registerzeile. Kein Bauvorbehalt.
- **R-312-4 (T3-01-02).** Der vermeidbare Teil (Adaptermutex der
  Parameteranbindung) wird nur entfernt, wenn Parameter-IDs, Reihenfolge,
  Bereiche, Hostsicht, Automation und State bytegleich bleiben (Identität
  NAK-30, `eq-copilot/identity/`); die Matrix der Etappe 1 belegt diese
  Neutralität mit einem Beweis oder begründet den Verzicht. Die JUCE-eigene
  Sperre vor dem Plugincode ist ohne den gepinnten Wrapperpatch (Entwurf §44.3)
  nicht entfernbar und wird beim Abschluss als datierte Registerzeile geführt,
  nie wegdeklariert.
- **R-312-5 (T3-01-10, sicherer Teil; starten↔stoppen).** `reset()` und
  `releaseResources()` von Gen beenden den Markierungsklang und schließen das
  offene Interventionsintervall mit genau einem `end` je `begin`; `reset()`
  allokiert nicht; der Oversize-Riegel bleibt bis zum nächsten `prepareToPlay`;
  der Überlaufpfad bleibt fail-closed. Der Auftrag bleibt bestehen — das ist
  der Zustand, den `prepareToPlay` heute herstellt, keine neue Semantik. Ob das
  Anhalten auch den Auftrag beendet, entscheidet U56; bei Antwort (2) folgt ein
  kleiner Nachzug in Etappe 7.
- **R-312-6 (T3-07-05, Teil ohne Antwort).** Ersatz-Hauptziel und jede
  Aktionssteuerung wählen nur aus der gezeichneten Menge, in Anzeigeordnung;
  kein scharfer Knopf steht auf einer ungezeichneten Quelle. Gespeicherte
  Quellen werden nie gelöscht. Die Kapazitätsgrenze wartet auf U51.
- **R-312-7 (T3-04-01).** Die Provider-Rückrufe der Sonde laufen durch dieselbe
  Besitzschleuse wie Gens IPC-Rückrufe (R-D2): nach dem Schließen kein Zugriff
  auf den Prozessor, der Abbau wartet begrenzt und gibt dann nichts frei, was
  ein abgelöster Rückruf noch anfassen kann. Dafür bekommt die Sonde
  Testzugänge wie Gen (Testkonstruktor, Starthaken, Probe-Pipename; nie die
  Produktions-Pipe); der Rotbeweis nutzt eine eigene Marke, keine Nullprüfung.
- **R-312-8 (T3-07-02).** Ein Rollenwechsel weg von Main beendet eine aktive
  Hörmarkierung (Solo, Pulse) mit Intervallende, bevor die Fläche wechselt; die
  drei Netze (Kill, Sampleratenwache, Totmann) ticken unabhängig von der
  gezeigten Fläche; kein Handgriff wirkt, dessen Bedienelement nicht sichtbar
  ist. Keine neue Oberfläche.
- **R-312-9 (T3-14-01).** Ein Labelentwurf gehört der Quellidentität, mit der
  er begann. Beim Auswahlwechsel gilt für ihn dieselbe Regel wie beim heutigen
  Fokusverlust des Editors (am Code abzulesen: bestätigen oder verwerfen),
  angewandt auf die Startquelle; auf die neue Auswahl wird ohne neuen Edit nie
  geschrieben; ein leerer Entwurf löscht keinen fremden Namen; existiert die
  Startquelle nicht mehr, verfällt der Entwurf ohne Mutation.
- **R-312-10 (T3-01-05, mit W02).** Die Übernahme von Hostparameterwerten ist
  an den Audioblock gebunden, nicht an die Wanduhr: derselbe Event- und
  Blockverlauf liefert offline und in Echtzeit dieselbe Ausgabe innerhalb der
  Toleranz von M-120; kontinuierliche Werte rampen vom vorigen zum letzten
  Blockwert (Entwurf §44.3, Rückfallsatz). Im Echtzeitfall nimmt der
  Audio-Callback dafür keine Sperre und wartet nie. Den Mechanismus legt der
  Bauplan der Etappe 1 vor (mit Kosten, Ruhegrenze M-81 und Epochenfolge);
  Samplegenauigkeit innerhalb des Blocks wird nicht zugesagt. Für W02: ein
  Ladestart macht Hostwerte wirkungslos, die vor ihm in der Mailbox lagen; ein
  neuer Hostgestus nach erfolgreichem Recall wirkt; bei `nurLesen` wirkt kein
  Hostparameter auf Audio, die Originalbytes bleiben, kein Dirty.

## 3. Produktkarten (21.09.2026)

Text berichtigt in `docs/plan/fragen.json`; gestellt wird im Zeitfenster 9 bis
23 Uhr über `/fragen`; gebaut wird je Karte erst nach der Antwort. Der Vorgriff
„Ohne Antwort baut der Dirigent Weg 1“ ist in allen vier Karten gestrichen.

| Karte | Befund | Stand |
|---|---|---|
| U48 | T3-01-09 | Text berichtigt: die Prämisse „Nakamas Schalter behält den inneren Zustand“ war falsch (E-8: Filter starten kalt); drei Wege statt zwei; die Folge „Probeeq misst im Hostbypass nicht“ ergänzt; T3-01-10 herausgenommen |
| U56 | T3-01-10 | neu: gilt der Markierungsauftrag nach dem Anhalten weiter; der sichere Teil (R-312-5) ist nicht Teil der Frage; wann FL den Anhaltemoment auslöst, ist nicht gemessen und steht so in der Karte |
| U49 | T3-02-06 | Text berichtigt: die Messpunktliste geht schon beim Wechsel verloren, nicht erst nach Speichern und Neuladen. Für den Planschritt: Weg 1 braucht ein neues, versioniertes Kind im Zustandsbaum (Schema, Leser, Schreiber, Fixtures, Migration in einem Satz) |
| U51 | T3-07-05 | Text berichtigt: die 21. Quelle ist ungezeichnet, nicht nur unbedienbar; Weg 2 ist vor der Designarbeit nicht baubar; die Empfehlung für Weg 1 steht gegen den Default des Auditentwurfs PF11-17 und sagt das |

## 4. Etappenplan

| Etappe | Inhalt | Befunde | Prüfung |
|---|---|---|---|
| 0 | Übernahme, Quellvalidierung, Einordnung, Regeln, Karten | alle | Dirigent an der Quelle |
| 1 | Verhaltensmatrix und Bauplan der Etappen 2 bis 6 (Spezifikation vor Code) | die technischen Gegenstände | Matrixprüfung, lesend |
| 2 | Messgeräte: RT-Wache ab Callback-Eintritt mit berichtigten Behauptungszeilen (R-312-1); Testzugänge der Sonde (R-312-7); verhaltensneutral | Voraussetzung für T3-01-02, T3-01-03, T3-04-01 | Erstprüfung 2 |
| 3 | W02 Ladestart (ein Änderungssatz); danach T3-01-05 Blockbindung (eigener Änderungssatz, R-312-10) | T3-05-01, T3-05-02; T3-01-05 | Erstprüfung 3 |
| 4 | W09 Besitz: Schleuse der Sonde (R-312-7), Lebendprüfung des Panels (R-312-2) | T3-04-01, T3-04-03 | Erstprüfung 4 |
| 5 | Offline-Übergang (ein Änderungssatz, R-312-3); danach Parameteranbindung (R-312-4) | T3-01-03, T3-01-04; T3-01-02 | Erstprüfung 5 |
| 6 | Gen-Fläche und Hörmarkierung: Rollenwechsel (R-312-8) mit Reset und Release (R-312-5); Labelentwurf (R-312-9); Ersatzziel (R-312-6) | T3-07-02, T3-01-10 sicherer Teil; T3-14-01; T3-07-05 Teil a | Erstprüfung 6 |
| 7 | Produktabhängiges nach Antwort, mit eigenem Matrixnachtrag; T3-02-06 nach W02 | T3-01-09 (U48), T3-01-10 Auftrag (U56), T3-02-06 (U49), T3-07-05 Kapazität (U51) | Erstprüfung 7 |
| Abschluss | Abschlussprüfung über Basis...HEAD, voller Kanon, Laufzeit-Arm | – | T2 |

Reihenfolge: Messgeräte zuerst, weil ein zu enger Zähler jede folgende
Reparatur beglaubigen würde (Muster NAK-311 R-311-2). W02 vor T3-01-02, weil
der Umbau der Parameteranbindung den Ladeweg berührt. W02 vor T3-02-06, weil
beide Save und Load berühren. T3-07-02 und der sichere Teil von T3-01-10 in
einer Etappe, weil beide dieselben Rücknahmewege bewegen.

## 5. Zusagen wörtlich (Etappe 1, Bauer, 21.09.2026)

Je technischem Gegenstand der Etappen 2 bis 6 die Zusage, gegen die er steht,
im Wortlaut ihrer Quelle, dazu die Regel `R-312-n` wörtlich aus §2, wo §1 eine
nennt. Alle Zeilennummern in §5 bis §8 gelten zum Stand `945ebfb1`; sie sind
dort selbst nachgelesen. `git diff --stat 090d0fa1 HEAD -- eq-copilot broker
tools` ist **leer** (gemessen), die Zeilennummern beider Quellvalidierungen
gelten damit unverändert. Auslassungen stehen als `…`, Zellgrenzen einer
zitierten Tabellenzeile als `·`, Zeilenumbrüche eines Kommentars als ` / `; die
Wörter sind unverändert. Die produktabhängigen Gegenstände der Etappe 7
(T3-01-09, der Auftragsteil von T3-01-10, T3-02-06, die Kapazitätsgrenze von
T3-07-05) stehen hier nicht — über sie entscheidet der User.

**Nachgezogen in der Matrixnacharbeit 1 (21.09.2026).** Wo §11.3 eine Regel neu
fasst, gilt sie hier in der Fassung von §11.3 und geht §2 vor: R-312-10 in der
Fassung von E-312-5 (§5.4) und R-312-8 in der Fassung von E-312-8 (§5.9). Die
übrigen Zitate sind unverändert. Alle Zeilenangaben in §5 bis §8 gelten weiter
zum Stand `945ebfb1`; `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools`
ist am 21.09.2026 **leer** (gemessen), jede in der Nacharbeit geschriebene oder
geänderte Angabe ist am HEAD `abb64275` selbst nachgelesen.

### 5.0 Für alle Gegenstände

- **Gate S25k** (`docs/plan/plan.json:361`), GRENZE: „Audio-Thread ohne Sperren
  und Allokationen; State bleibt verlustfrei, fremde Bytes bytegleich;
  Designteil bleibt geparkt (keine neue Oberfläche, nur nötiger Handgriff);
  keine Legacy-Umbenennung." · BEWEIS: „docs/beweise/NAK-312.md mit Matrix,
  Rotbeweisen an B2, B6, B7, B14, B15, RT-Wache vor Parameterverarbeitung,
  vollem Kanon GRÜN und Laufzeit-Arm."
- `CLAUDE.md:29-33`: „**Grundgesetz: nichts Ungefragtes.** Verarbeitung nur,
  wenn der User sie einschaltet. Ausgeschaltet ist der Pfad im Nulltest
  bitidentisch; sonst Passthrough sampleidentisch, ohne Latenz oder Tail. Der
  Advisor schlägt nur vor. Audio-Thread: keine Sperren, Allokationen, Datei-,
  Pipe- oder Netzzugriffe, kein Logging; Überlast verwirft Analyseframes, nie
  Audio."
- `CLAUDE.md:84-86`: „Beziehungen mitprüfen: speichern↔laden, starten↔stoppen,
  öffnen↔schließen, verbinden↔trennen, aktivieren↔abklingen, installieren↔
  Rückweg gehören in denselben Änderungssatz."
- `CLAUDE.md:201-203`: „**State bleibt verlustfrei.** Unbekannte
  Major-Versionen oder unzulässige Matrizen werden read-only mit Originalbytes
  gehalten; jede persistente Änderung meldet dem Host Dirty-State."
- `CLAUDE.md:204-205`: „**Identität bleibt eingefroren** bis NAK-30: Class-IDs,
  Hersteller-/Plugin-Codes und Replace-V2-Verhalten nie beiläufig ändern."

### 5.1 Etappe 2 — RT-Wache ab Callback-Eintritt (T3-01-02, T3-01-03; R-312-1)

- SONDE-015 M-47 (`docs/beweise/SONDE-015.md:1100`): „Im Callback gibt es
  **keine Sperre, keine Allokation, keinen Datei-, Pipe- oder Netzzugriff, kein
  Logging und keine geworfene Ausnahme**. Gemessen wird mit einem
  thread-lokalen Allokations- und einem Sperrzähler über mindestens 4000 Blöcke
  wechselnder Größe mit Transportkanten und Programmwechseln." · Rotbeweisspalte:
  „Der Zähler steht auf 1; oder der Zähler ist global statt thread-lokal und
  kann Worker und Audiothread nicht trennen"
- Behauptung B6 (`tools/beweise.ps1:723`): „… im Callback zaehlt der
  thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen
  und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT
  sieht."
- Behauptung B7 (`tools/beweise.ps1:728`): „Im 32-kHz-Fenster zaehlt der
  thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die
  Echtzeitwache des Kerns 0 gemeldete Sperren, getLatencySamples bleibt 0 und
  getTailLengthSeconds 0,0."
- Kopf der Wache (`eq-copilot/plugin/dsp/DspRtWache.h:9-15`): „WIE DIE WACHE
  EHRLICH BLEIBT: sie zaehlt nicht sich selbst. Der Kern / setzt beim Betreten
  des Audiopfads ein THREAD-LOKALES Flag; die / Zaehlfunktionen erhoehen nur,
  solange dieses Flag steht. …"
- Entwurf (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3066-3067`):
  „State-Tree-Copy/Replace und / Parametergesten laufen nie im Audiothread."
- **R-312-1**, wörtlich aus §2: „**R-312-1 (Messgerät zuerst; T3-01-02,
  T3-01-03).** Die RT-Wache deckt den Audio-Callback der Sonde ab seinem
  Eintritt (`SondeProcessor::processBlock`), nicht erst ab `DspKern::verarbeite`.
  Die Behauptungszeilen der Beine, die sich auf die Wache stützen, sagen genau,
  was der Zähler sieht; Sperren, die JUCE vor dem Plugincode nimmt, kann die
  Wache nicht sehen, und die Zeile nennt diese Grenze. Verhaltensneutral; steht
  vor jedem Sperren-Rotbeweis."

### 5.2 Etappe 2 — Testzugänge der Sonde (T3-04-01; R-312-7)

- NAK-246 R-D2 (`docs/beweise/NAK-246.md:1008-1016`): „R-D2: Kein
  Produkt-Callback greift nach Beginn der Prozessorzerstörung auf /
  Prozessorzustand zu. Die Callbacks der Clients fangen den Prozessor nicht /
  mehr als rohes this, sondern laufen durch eine Schleuse mit Besitz: ein /
  Callback, der nach dem Schließen der Schleuse beginnt, wird abgewiesen /
  (zählbar); ein Callback, der beim Schließen bereits läuft, wird vom /
  Destruktor zu Ende gewartet … Die / 2000-ms-Stoppfrist des Clients (B-CC-12)
  und die Ablösung des Threads / bleiben; die abgelöste Laufzeit hält dann nur
  noch Callbacks, die niemanden / mehr berühren."
- `CLAUDE.md:198-200`: „**Begriffe nicht vermischen:** Probe-Pipe ist nie
  Produktions-Pipe; `ltasReferenzDb` ist keine globale Sollkurve; Paint-FPS ist
  nicht Datenkadenz; Demo-Daten sind keine Plugin-Daten; FL zeigt MIDI 60 als
  C5."
- Muster Gen (`eq-copilot/plugin/src/PluginProcessor.h:95-99`): „Fail-closed:
  liegt `probePipename` nicht im Probe-Namensraum / (`istProbePipename`),
  bekommen beide Clients einen LEEREN Pipenamen, / mit dem sich nie eine
  Verbindung oeffnen laesst - die Produktionspipe / wird aus einem Bein heraus
  nie zum Ziel. `v3PipeNameFuerTest()` zeigt, / was uebernommen wurde."
- **R-312-7**, wörtlich aus §2: „**R-312-7 (T3-04-01).** Die Provider-Rückrufe
  der Sonde laufen durch dieselbe Besitzschleuse wie Gens IPC-Rückrufe (R-D2):
  nach dem Schließen kein Zugriff auf den Prozessor, der Abbau wartet begrenzt
  und gibt dann nichts frei, was ein abgelöster Rückruf noch anfassen kann.
  Dafür bekommt die Sonde Testzugänge wie Gen (Testkonstruktor, Starthaken,
  Probe-Pipename; nie die Produktions-Pipe); der Rotbeweis nutzt eine eigene
  Marke, keine Nullprüfung."

### 5.3 Etappe 3, erster Änderungssatz — W02 (T3-05-01, T3-05-02)

- SONDE-015 M-84 (`docs/beweise/SONDE-015.md:1162`): „Der Reload rekonstruiert
  **denselben `state_hash`** und innerhalb numerischer Toleranz denselben
  Audioausgang. Reihenfolge: State lesen → validieren → Committed setzen →
  Hostparameter synchronisieren → erster Block."
- SONDE-015 M-92 (`docs/beweise/SONDE-015.md:1175`): „Er bleibt **read-only mit
  Originalbytes** wie heute: audio-neutral, keine Bindung, `getStateInformation`
  liefert die Bytes unverändert zurück. Der bestehende Zweig in
  `NakamaState.cpp` bleibt in seiner Wirkung unverändert; nur die Bedingung
  „`Dsp` vorhanden" fällt weg."
- `CLAUDE.md:201-203` wie in §5.0 (die Bytehälfte hält heute und muss halten).
- Absichtskommentar, der stehen bleibt
  (`eq-copilot/plugin/sonde/SondeProcessor.cpp:334-337`): „Programm, Parameter,
  bestaetigter Zustand und / `publikationOffen` bleiben, ebenso die
  Hostwert-Mailbox (`hostWert`, / `hostEreignis`): sie traegt Hostwerte, keine
  Audiohistorie."
- **R-312-10**, zweiter Teil, wörtlich aus §2: „Für W02: ein Ladestart macht
  Hostwerte wirkungslos, die vor ihm in der Mailbox lagen; ein neuer Hostgestus
  nach erfolgreichem Recall wirkt; bei `nurLesen` wirkt kein Hostparameter auf
  Audio, die Originalbytes bleiben, kein Dirty."

### 5.4 Etappe 3, zweiter Änderungssatz — Blockbindung (T3-01-05; R-312-10)

- SONDE-015 M-120 (`docs/beweise/SONDE-015.md:1224`): „Beide erzeugen
  **denselben Parameterverlauf** und damit innerhalb numerischer Toleranz
  denselben Ausgang, bei gleichem Event- und Blockverlauf. Offline-Render läuft
  mit dem bestätigten Zustand, nie mit einer Vorschau." · Rotbeweisspalte: „Ein
  Offline-Render liefert einen anderen Verlauf, weil die Rampe an der Wanduhr
  statt an Samples hängt"
- Entwurf, Rückfallsatz (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3084-3086`):
  „Scheitert dieser Spike, meldet die Probe `sample_accurate_automation=false`:
  kontinuierliche Werte / werden ehrlich nur vom vorigen zum letzten Blockwert
  gerampt, Topologieautomation wird / deaktiviert, und
  Realtime/Offline-Gleichheit wird nur für denselben Event-/Blockverlauf
  behauptet."
- Entwurf (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3070-3071`): „Eine zweite /
  freie Glättung darf die Hostautomation nicht zeitlich verschieben. Realtime-
  und Offline-Render / müssen denselben Parameterverlauf erzeugen."
- SONDE-015 M-81 (`docs/beweise/SONDE-015.md:1159`), die Grenze, die der Fix
  nicht verschieben darf: „Sie wirkt im flüchtigen **`AutomationOverlay`** mit
  `automation_epoch` und zählt **keine** Revision — auch nicht pro
  Automationspunkt. Die Epoche wechselt einmal zu Beginn und einmal am Ende
  einer Hostgeste beziehungsweise nach einer definierten Ruhegrenze."
- Irreführender Kommentar, der in denselben Änderungssatz gehört
  (`eq-copilot/plugin/sonde/SondeProcessor.cpp:151-154`): „[SONDE-015] M-119: FL
  meldet `sample_accurate_automation` = unsupported / … / Pfad ist nicht
  gebaut. Die Sonde rampt blockweise, und / Topologieautomation wirkt nicht
  (§44.3 letzter Absatz)."
- **R-312-10**, wörtlich aus §2: „**R-312-10 (T3-01-05, mit W02).** Die
  Übernahme von Hostparameterwerten ist an den Audioblock gebunden, nicht an die
  Wanduhr: derselbe Event- und Blockverlauf liefert offline und in Echtzeit
  dieselbe Ausgabe innerhalb der Toleranz von [SONDE-015] M-120; kontinuierliche
  Werte rampen vom vorigen zum letzten Blockwert (Entwurf §44.3, Rückfallsatz).
  Im Echtzeitfall nimmt der Audio-Callback dafür keine Sperre und wartet nie. Den
  Mechanismus legt der Bauplan der Etappe 1 vor (mit Kosten, Ruhegrenze
  [SONDE-015] M-81 und Epochenfolge); Samplegenauigkeit innerhalb des Blocks
  wird nicht zugesagt."
- **R-312-10 in der Fassung für dieses Ticket**, wörtlich aus E-312-5 (§11.3);
  sie geht dem Absatz darüber vor, soweit sie ihn ersetzt: „**R-312-10 lautet
  für dieses Ticket:** für diese vier liefert derselbe Event- und Blockverlauf
  offline und in Echtzeit dieselbe Ausgabe innerhalb der Toleranz von
  [SONDE-015] M-120; der
  Blockrand rechnet dieselbe Zelle wie `zelleAusHost` einschließlich ihrer
  Ausnahme, wofür die bestätigten Werte der vier Parameter dem Callback ohne
  Sperre vorliegen müssen. Eine Grenzzeile der Matrix hält fest, dass die 64
  Bandwerte, `mono_bass_hz`, die elf booleschen Parameter und das Auto-Gain-Ziel
  taktgebunden bleiben. Das ist **keine dauerhafte Verengung der Zusage**:
  [SONDE-015] M-120 und der Entwurfssatz bleiben für Bandwerte gebrochen und
  offen geführt; den
  Rest schließt das Folgeticket NAK-340 (Planschritt S25p, Blockrandentwurf der
  Bandwerte im Kern mit eigener Besitzregel, eigener Matrix und eigener
  T2-Prüfung). T3-01-05 wird geteilt: Teil a hier, Teil b in NAK-340."
- Die vier Parameter, selbst nachgelesen
  (`eq-copilot/plugin/state/NakamaParameter.cpp:50-52` und `:80`):
  `v1.global.input_trim_db`, `v1.global.output_trim_db`, `v1.global.width`,
  `v2.global.mix`. Sie sind genau die kontinuierlichen Hostparameter, die der
  Kern heute schon als Rampenziel führt (`eq-copilot/plugin/dsp/DspKern.cpp:620-623`,
  gespeist aus `eq-copilot/plugin/dsp/DspProgramm.cpp:259-264` und `:271-272`).
  Das fünfte Rampenziel `autoGain` (`DspKern.cpp:626`) ist abgeleitet
  (`DspProgramm.cpp:367`), kein Hostparameter, und bleibt beim Worker: SONDE-015
  M-39 (`docs/beweise/SONDE-015.md:1087`) verlangt die Ableitung ausdrücklich
  außerhalb des Audiothreads.

### 5.5 Etappe 4 — Besitzschleuse der Sonde (T3-04-01; R-312-7)

- R-D2 (`docs/beweise/NAK-246.md:1008-1016`) wörtlich wie in §5.2. Der Satz ist
  ohne Einschränkung auf Gen formuliert.
- Kopf der Schleuse
  (`eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:17-23`): „* Der Besitzer
  SCHLIESST sie in seinem Destruktor, NACH `stop()` seiner / Clients: ein
  Callback, der danach beginnt, wird ABGEWIESEN und gezaehlt / (`abgewiesen`);
  ein Callback, der beim Schliessen bereits laeuft, wird / zu Ende GEWARTET -
  der Destruktor kehrt erst zurueck, wenn `aktiv == 0`. / Die Wartezeit ist
  durch die Callback-Dauer begrenzt und steht danach in / `gewartetMs` …"
- Sperrenordnung, die der Fix nicht verschieben darf
  (`eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:30-33`): „Im
  Callback-Pfad (`betreten`, `Zug::~Zug`) gibt es KEINE Sperre, nur / Atomics: …"
- **R-312-7** wörtlich wie in §5.2.

### 5.6 Etappe 4 — Lebendprüfung des Panels (T3-04-03; R-312-2)

- **Keine Zusage gebrochen.** Teil B hat Entwurf, Blueprint und
  `docs/plugin-wissen.md` nach einer Lebensdauerregel für ein ausgelagertes
  modales Panel durchsucht und keine gefunden; R-D2 regelt ausdrücklich
  Client-Callbacks. Die Regel fehlt, nicht ihre Erfüllung — deshalb LÜCKE mit
  technischer Regel.
- Irreführender Kommentar am Übergabepunkt, der in denselben Änderungssatz
  gehört (`eq-copilot/plugin/src/PluginEditor.cpp:731-733`): „SafePointer statt
  rohem this: der Panel-Destruktor ruft uebernehmen() / auch dann, wenn FL das
  Editorfenster MIT offenem Popover schließt — / der Editor ist dann schon im
  Abbau (Component nullt SafePointer sofort)."
- **R-312-2**, wörtlich aus §2: „**R-312-2 (T3-04-03).** Ein asynchroner Rückruf
  des Editors (modales Panel, verzögerter Aufruf) fasst Prozessor und Editor nur
  nach einer Lebendprüfung an; nach dem Ende seines Eigentümers ist er
  wirkungslos: kein Zugriff, keine Mutation, keine Dirty-Meldung. Der Handgriff
  bleibt derselbe."

### 5.7 Etappe 5, erster Änderungssatz — Offline-Übergang (T3-01-03, T3-01-04; R-312-3)

- SONDE-015 M-47 (`docs/beweise/SONDE-015.md:1100`) und `CLAUDE.md:29-33` wie in
  §5.0 und §5.1. Eine Offline-Ausnahme nennt keine der beiden Stellen.
- SONDE-015 M-120 (`docs/beweise/SONDE-015.md:1224`), zweiter Satz:
  „Offline-Render läuft mit dem bestätigten Zustand, nie mit einer Vorschau."
- Entwurf (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2137`): „Preview-Zustand
  wird nie im Projekt gespeichert und nie in einen Offline-Render übernommen."
- Entwurf (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2748`): „Preview, Focus und
  Delta sind im Offline-Render neutral."
- Irreführende Kommentare, die in denselben Änderungssatz gehören:
  `eq-copilot/plugin/sonde/SondeProcessor.h:442` „`juce::CriticalSection
  zustandSchloss;   ///< nur Nachrichten-/Hostthread, nie processBlock`" ·
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:1181-1183` „Der Control-Thread
  liest ihn / deshalb unter JUCEs Callback-Lock; **der Audiothread nimmt nie
  unseren / Runtime-/State-Lock.**" ·
  `eq-copilot/plugin/sonde/SondeProcessor.h:176-179` „Offline-Render laeuft mit
  dem bestaetigten Zustand, nie mit einer / Vorschau (§44.4, §49.2 Gate 3,
  [SONDE-015] M-120): der Wechsel in den Offline-Betrieb beendet die Preview
  und / stellt die Hoermatrix auf Processed."
- **R-312-3**, wörtlich aus §2: „**R-312-3 (T3-01-04, mit T3-01-03).** Die
  ersten Samples eines Offline-Renders tragen den bestätigten Zustand, nie die
  Vorschau ([SONDE-015] M-120), unabhängig davon, ob der Host vorher
  `prepareToPlay` ruft.
  T3-01-03 und T3-01-04 sind ein Änderungssatz. Ob FL vor einem Offline-Render
  deaktiviert, wird im Laufzeit-Arm beobachtet, wenn das ohne neue
  Infrastruktur geht; sonst datierte Registerzeile. Kein Bauvorbehalt."

### 5.8 Etappe 5, zweiter Änderungssatz — Parameteranbindung (T3-01-02; R-312-4)

- `CLAUDE.md:29-33` und SONDE-015 M-47 (`docs/beweise/SONDE-015.md:1100`) wie in
  §5.0 und §5.1. „Sie spricht vom **Thread**, nicht vom Autor" (Teil A §3c).
- `CLAUDE.md:204-205` (Identität eingefroren) wie in §5.0.
- Behauptung B7 (`tools/beweise.ps1:728`), die Identitätshälfte, die halten
  muss: „Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge
  ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je
  Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus
  ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch
  Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang."
- Entwurf §44.3, der gepinnte Wrapperpatch
  (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3082-3083`): „Quellkontext den Build
  ab; `_deps` wird nie manuell editiert. Wrapper-Diff und /
  Host-Conformance-Golden werden bei jedem JUCE-Update neu geprüft." — er
  gehört nicht in dieses Ticket (Kopf, Grenze).
- **R-312-4**, wörtlich aus §2: „**R-312-4 (T3-01-02).** Der vermeidbare Teil
  (Adaptermutex der Parameteranbindung) wird nur entfernt, wenn Parameter-IDs,
  Reihenfolge, Bereiche, Hostsicht, Automation und State bytegleich bleiben
  (Identität NAK-30, `eq-copilot/identity/`); die Matrix der Etappe 1 belegt
  diese Neutralität mit einem Beweis oder begründet den Verzicht. Die
  JUCE-eigene Sperre vor dem Plugincode ist ohne den gepinnten Wrapperpatch
  (Entwurf §44.3) nicht entfernbar und wird beim Abschluss als datierte
  Registerzeile geführt, nie wegdeklariert."

### 5.9 Etappe 6 — Rollenwechsel und Hörmarkierung (T3-07-02; R-312-8)

- `CLAUDE.md:51-52`: „**Keine toten UI-Elemente** (User 24.08.2026): jedes
  sichtbare Element führt einen Handgriff aus oder meldet ehrlich einen
  Zustand."
- Irreführender Kommentar, der in denselben Änderungssatz gehört
  (`eq-copilot/plugin/src/PluginEditor.cpp:268-269`): „Hör-Markierung:
  Sicherheitsnetz-Ticks (Konzept v2 §4/N2) — Freilauf- / Meldung löst den Latch
  sichtbar, Samplerate-Wechsel und Totmann ebenso."
- Behauptung A3 (`tools/beweise.ps1:509`): „Hoer-Markierung bleibt verriegelt:
  Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der
  U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein
  Sample (T11, mit Gegenprobe bei laufendem Transport). …" — über
  Klassifikationswechsel und Bedienelemente steht darin nichts.
- **R-312-8**, wörtlich aus §2: „**R-312-8 (T3-07-02).** Ein Rollenwechsel weg
  von Main beendet eine aktive Hörmarkierung (Solo, Pulse) mit Intervallende,
  bevor die Fläche wechselt; die drei Netze (Kill, Sampleratenwache, Totmann)
  ticken unabhängig von der gezeigten Fläche; kein Handgriff wirkt, dessen
  Bedienelement nicht sichtbar ist. Keine neue Oberfläche."
- **R-312-8 in der Fassung für dieses Ticket**, wörtlich aus E-312-8 (§11.3);
  sie ersetzt den Absatz darüber, weil dieser nur die Richtung weg von Main
  nennt, die gefährliche Richtung aber zu Main führt: „Jeder Wechsel der
  Klassifikation von Gen — zu Main und weg von Main, über `setzeBindung` oder
  `setStateInformation` — nimmt einen eingereichten Markierungsauftrag zurück,
  bevor die neue Klassifikation im Audiothread wirkt. Ein hörbarer Marker endet
  über den weichen Ausfade mit genau einem `end` zu seinem `begin`; ein nie
  hörbarer erzeugt kein Ereignis und färbt kein Sample; danach wird kein Marker
  ohne neuen Handgriff wieder wirksam. Die drei Netze (Kill, Sampleratenwache,
  Totmann) ticken in jeder Fläche; kein Handgriff wirkt, dessen Bedienelement
  nicht sichtbar ist. Keine neue Oberfläche." Dazu aus demselben Entscheid der
  Satz, der den harten Schnitt ausschließt: „Der harte Schnitt bleibt verboten
  (NAK-47): zugesagt wird „Rücknahme vor dem Flächenwechsel, der Ausfade läuft
  weich zu Ende, `end` danach", nicht „Intervallende vor dem Flächenwechsel"."
- Der weiche Ausfade, den diese Zusage nicht in einen Schnitt verwandeln darf
  (`eq-copilot/plugin/src/HoerMarkierung.h:420-429`): „🔑 NAK-47 (SONDE-013
  M-34): bei `!erlaubt` wird NICHT mehr geschnitten. / `hartAus()` setzte /
  `fade = 0` sofort, und das Signal sprang im naechsten / Block abrupt vom
  gefilterten auf den Originalpfad — ein hoerbarer Klick / und ein Bruch des
  Startbudgets §49.3 („A/B-Zustandswechsel: kein / Klick"). Statt dessen laeuft
  die vorhandene Rampe zu Ende; ERST danach / ist der Pfad wieder
  bit-transparent ([SONDE-013] M-35)."

### 5.10 Etappe 6 — reset und releaseResources (T3-01-10, sicherer Teil; R-312-5)

- **Keine Gen-Reset-Zusage gefunden** (Teil A §8c): gesucht in
  `docs/beweise/SONDE-013.md`, `docs/beweise/SONDE-015.md`, im Entwurf und in
  `docs/offene-punkte.md`. Die Regel fehlt; tragend ist deshalb
  `CLAUDE.md:84-86` (starten↔stoppen) und die im Code bereits gelöste
  Nachbarstelle.
- Begründung im Code, die die Lücke benennt
  (`eq-copilot/plugin/src/prozessor/Hostbruecke.cpp:88-96`): „🔑 NAK-180 N-10:
  der Marker ist hart aus, sein `end` kommt NIE. / … / `markierung.vorbereiten()`
  und `setzeSamplerate()` setzen `warHoerbar` / zurueck und loeschen den Fade,
  ohne den faelligen Uebergang zu melden — / der Audiothread erzeugt fuer dieses
  Intervall also kein `endete` mehr. / Ohne diese Markierung bliebe das Begin
  beim Broker fuer immer offen und / die Sitzung dauerhaft gesperrt. …"
- Der Oversize-Riegel, der bleiben muss — SONDE-013 M-36
  (`docs/beweise/SONDE-013.md:521`): „Entschieden ist ein **erzwungener Ausfade
  innerhalb der Kapazität, danach Riegel bis `prepareToPlay`** …" ·
  `eq-copilot/plugin/src/HoerMarkierung.h:308-312`: „§7.1 E-01: der
  Oversize-Riegel gilt "bis zum naechsten / prepareToPlay". Der ruft beide
  Vorbereiter, also loesen ihn auch / beide — sonst haengte die Verriegelung
  daran, welchen von zweien / der Prozessor zuerst ruft."
- **R-312-5**, wörtlich aus §2: „**R-312-5 (T3-01-10, sicherer Teil;
  starten↔stoppen).** `reset()` und `releaseResources()` von Gen beenden den
  Markierungsklang und schließen das offene Interventionsintervall mit genau
  einem `end` je `begin`; `reset()` allokiert nicht; der Oversize-Riegel bleibt
  bis zum nächsten `prepareToPlay`; der Überlaufpfad bleibt fail-closed. Der
  Auftrag bleibt bestehen — das ist der Zustand, den `prepareToPlay` heute
  herstellt, keine neue Semantik. Ob das Anhalten auch den Auftrag beendet,
  entscheidet U56; bei Antwort (2) folgt ein kleiner Nachzug in Etappe 7."

### 5.11 Etappe 6 — Labelentwurf (T3-14-01; R-312-9)

- `CLAUDE.md:51-52` wie in §5.9 — das ist die gebrochene Zusage.
- SONDE-012 U02 (`docs/beweise/SONDE-012.md:231`), die **nicht** gebrochen ist:
  „Zu jedem sichtbaren Modellstand gibt es genau **ein Hauptziel** der
  Detailfläche; alle anderen Quellen sind klar als Referenzen benannt. Bei null
  Quellen gibt es kein erfundenes Ziel. Auswahl oder Wegfall wird als ein
  Modellwechsel sichtbar; die UI zeigt nie zwei Hauptziele oder eine Referenz
  als Besitzer."
- SONDE-012 U03 (`docs/beweise/SONDE-012.md:232`), die ebenfalls hält: „Jede
  ausführbare Aktion adressiert ausschließlich die `instance_id` des beim
  Auslösen aktuellen Hauptziels. Reihenfolge: aktuelles Hauptziel lesen → vor
  Dispatch prüfen, dass es weiterhin Hauptziel ist → nur dorthin senden; …"
- Vertrag (`eq-copilot/schemas/state/nakama-state-v2.md:107-108`): „Jedes Paar
  besteht aus einer eindeutigen hex32-`instance_id` / und dem zugehörigen
  User-Label (String, auch leer, höchstens 120 Unicode-Codepoints)." — der leere
  String ist erlaubt, deshalb ist der Löschfall kein Vertragsbruch, sondern ein
  Bedienfehler.
- **R-312-9**, wörtlich aus §2: „**R-312-9 (T3-14-01).** Ein Labelentwurf gehört
  der Quellidentität, mit der er begann. Beim Auswahlwechsel gilt für ihn
  dieselbe Regel wie beim heutigen Fokusverlust des Editors (am Code abzulesen:
  bestätigen oder verwerfen), angewandt auf die Startquelle; auf die neue
  Auswahl wird ohne neuen Edit nie geschrieben; ein leerer Entwurf löscht keinen
  fremden Namen; existiert die Startquelle nicht mehr, verfällt der Entwurf ohne
  Mutation."

### 5.12 Etappe 6 — Ersatzziel (T3-07-05 Teil a; R-312-6)

- `CLAUDE.md:51-52` wie in §5.9 — sie trägt den Teil, der ohne Antwort baubar
  ist: ein sichtbarer, scharfer Knopf auf einer ungezeichneten Quelle meldet
  keinen ehrlichen Zustand.
- Die Sichtzusage, die **erfüllt** ist und die Kapazitätsfrage zur Produktkarte
  macht — Entwurf (`docs/FL-Nakama-Sonden-Design-Entwurf.md:1590`): „normale
  Projektgröße · UX für 1 Main + 16 sichtbare Sonden, Verträge und Lasttests bis
  32." und (`docs/FL-Nakama-Sonden-Design-Entwurf.md:4277`): „Live-Landkarte für
  1 Main + 16 sichtbare Probes, Lastvertrag bis 32;"
- Behauptung B15 (`tools/beweise.ps1:746`): „EqCopShot rendert den deklarierten
  SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, … genau ein Hauptziel
  mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main."
- Vertrag, der den Bestand schützt
  (`eq-copilot/schemas/state/nakama-state-v2.md:104`): „Ein Kind mit unbekanntem
  Namen ⇒ read-only (ein neues Kind ist eine Root-Versionierung, keine
  Minor-Erweiterung). Eine **unbekannte Eigenschaft** in einem bekannten Kind
  desselben Majors ⇒ additiv, wird erhalten."
- **R-312-6**, wörtlich aus §2: „**R-312-6 (T3-07-05, Teil ohne Antwort).**
  Ersatz-Hauptziel und jede Aktionssteuerung wählen nur aus der gezeichneten
  Menge, in Anzeigeordnung; kein scharfer Knopf steht auf einer ungezeichneten
  Quelle. Gespeicherte Quellen werden nie gelöscht. Die Kapazitätsgrenze wartet
  auf U51."

## 6. Verhaltensmatrix M-01 bis M-86 (Etappe 1, Bauer, 21.09.2026; Nacharbeit 1, 21.09.2026)

Eigene IDs. **Fremde Matrixzeilen (SONDE-015 M-nn, SONDE-012 U-nn, SONDE-013
M-nn, NAK-246 M-nn) stehen nie als eigene Zeile**; eine Zeile hier heißt immer
M-01 bis M-86 dieses Manifests. **Jede Nennung einer fremden Zeile trägt in §5
bis §8 ihr Präfix, gleich in welcher Spalte sie steht** (R-312-13, §14.4,
nachgezogen in der Matrixnacharbeit 2); ein bloßes `M-nn` bezeichnet
ausnahmslos eine eigene Zeile dieses Manifests. In wörtlichen Zitaten,
Codezitaten und bestehenden Testnamen bleibt der Wortlaut, und das Präfix steht
als Zusatz in eckigen Klammern davor: `[SONDE-015] M-81`. Die nachgestellte
Form „M-55 von SONDE-015" ist ebenso eindeutig und bleibt, wo sie steht. Die
Zeilen M-78 bis M-86
sind in der Matrixnacharbeit 1 entstanden und stehen in dem Abschnitt, zu dessen
Etappe sie gehören; §12 führt sie mit ihrem Grund. Neue Testfälle heißen
`312/M-nn <name>`; bestehende Fälle heißen nach ihrem Prüfnamen im Bein.

**Spalten.** *Befund* nennt die ID und die Regel oder Beziehung; *Zustand und
Vorbedingung* den Stand, den der Fall herstellt; *Ereignis* den einen Auslöser;
*Zusage* einen prüfbaren Satz mit Maß und Einheit; *Reihenfolge und Frist* den
Blockrand, das Sample, den Thread, die Callback-Reihenfolge oder den Aufruf, an
dem die Zusage hängt; *Test* Datei, Testname (**NEU** = existiert am Basis-SHA
nicht) und Bein aus `tools/beweise.ps1`; *Rotbeweis* das Etikett und die
Mutation an der Zeile, die die Zusage trägt; *Quelle* die Zusage aus §5 oder die
Regel aus §2; *Etappe* die Bauetappe aus §4. Zeilenangaben gelten zum Basis-SHA
`945ebfb1`; der Bau verschiebt sie, der Rotbeweis nennt dann die gebaute Zeile.

**Etiketten der Rotbeweisspalte — vier.** **heute rot**: am Basis-SHA
widerspricht das Verhalten der Zusage nachweislich; die Zeile nennt die
Gegenprobe und die Mutation, die nach dem Bau an der neuen Zusagezeile rot
macht. **heute nicht messbar**: der Mechanismus oder der Zähler entsteht erst im
Bau; die Zeile nennt die Mutation nach dem Bau. **Regressionswache**: heute grün
und hält, was der Bau nicht ändern darf; sie wird einmal absichtlich gebrochen
und ist kein Beleg (`tools/dirigent/pruefliste.md`, Abschnitt E). **zwei
Hälften**: die Zeile trägt zwei Etiketten und sagt je Hälfte, welches gilt.

**Regeln dieser Matrix.**

- Ein Rotbeweis fällt an der Zeile, die die Zusage trägt, nie an einem
  Nebeneffekt. Keine Zeile hängt an Wandzeit oder Zufall: Ladestart, Blockrand,
  Kontrolltakt und Destruktorfolge sind deterministisch ansteuerbar
  (`kontrollTaktFuerTest`, `eq-copilot/plugin/sonde/SondeProcessor.h:263`;
  Schranke, `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:469`).
- **Nichts Ungefragtes** (`CLAUDE.md:29-33`): ausgeschaltet bitidentisch, sonst
  Passthrough sampleidentisch, ohne Latenz oder Tail. Keine Zeile verlangt im
  Audio-Thread eine Sperre, eine Allokation, einen Datei-, Pipe- oder
  Netzzugriff oder Logging; die Echtzeitzeilen M-09, M-26, M-38, M-40, M-53 und
  M-60 messen es je Änderungssatz. Keine neue Stateversion, keine
  v3-Vertragsänderung, Identität eingefroren (NAK-30), Legacy-Bezeichner
  bleiben, keine neue Oberfläche (M-48 bis M-50, M-77).
- **Beziehungen im selben Zeilensatz** (`CLAUDE.md:84-86`): speichern↔laden
  M-10 bis M-19, M-51, M-72, M-76, M-78; starten↔stoppen M-34, M-59 bis M-66,
  M-84, M-86; öffnen↔schließen M-35 bis M-37, M-46, M-59, M-62;
  verbinden↔trennen M-33, M-52; aktivieren↔abklingen M-24, M-43 bis M-45,
  M-55 bis M-58, M-83, M-84.
- Zahlen aus Phase 16 und den beiden Quellvalidierungen sind als „gemessen" oder
  „nachgerechnet" gekennzeichnet; Zahlen, die erst die Etappe misst, stehen als
  Schranke, nie als Ergebnis.

### 6.1 Etappe 2 — Messgeräte (R-312-1, R-312-7), verhaltensneutral

**Testplayhead** heißt in dieser Etappe: ein `juce::AudioPlayHead` des Beins,
das der Prozessor über `setPlayHead` bekommt und das `processBlock` bei
`eq-copilot/plugin/sonde/SondeProcessor.cpp:385-387` liest — die einzige
Produktstelle zwischen Callback-Eintritt und `dspKern->verarbeite` (`:432`), an
der Testcode im echten Pfad läuft. Er nimmt in `getPosition()` eine
`RtWache::GemeldeteSperre` über einen eigenen `std::mutex` und macht den Bereich
damit lebendig messbar, ohne Produktcode zu verbiegen.

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-01 | T3-01-02, T3-01-03 · R-312-1 | echter `SondeProcessor`, 48 kHz, Block 256, Testplayhead gesetzt, `RtWache::zuruecksetzen()` | 200 Blöcke `processBlock` | **`RtWache::sperren()` = 200, also genau eine gemeldete Sperre je Block, gezählt an einer Stelle VOR `dspKern->verarbeite`; `RtWache::allokationen()` = 0 — dieser Zähler ist in B7 ab DIESER Etappe 2 messbar, weil derselbe Änderungssatz B7s `operator new` und `operator new[]` (`eq-copilot/plugin/tests/TransactionTestMain.cpp:76-91`) zusätzlich an `RtWache::meldeAllokation()` bindet (§7.2, E-312-11)** | `RtWache::Bereich` als erste Anweisung von `processBlock` (`SondeProcessor.cpp:371`), vor der Stempelbildung `:379-425` | B7 **NEU** `312/M-01 wache_beginnt_am_callback_eintritt` | **heute rot** — Gegenprobe: der einzige `RtWache::Bereich` des Produkts steht in `eq-copilot/plugin/dsp/DspKern.cpp:1059`, der Playhead läuft davor, Zähler bleibt 0. Nach dem Bau: `Bereich` aus `processBlock` entfernen oder hinter `:432` schieben → Zähler 0 → rot | R-312-1; SONDE-015 M-47; E-312-11; `CLAUDE.md:29-33` | 2 |
| M-02 | T3-01-02 · Grenze der Wache | wie M-01, zusätzlich ein Workerzug mit Programmbau (`kontrollTaktFuerTest`) außerhalb des Blocks | Workerzug zwischen zwei Blöcken | **Der Zähler sieht die Allokationen und Sperren des Workers NICHT: `allokationen()` und `sperren()` steigen im Workerzug um 0; eine Sperre, die der Testplayhead außerhalb eines Blocks nimmt, zählt 0** — `RtWache::allokationen()` ist in B7 ab DIESER Etappe 2 messbar (Bindung von B7s `operator new`, §7.2, E-312-11); der Grundsatz selbst hält heute schon, gemessen in B6 | thread-lokale Tiefe (`eq-copilot/plugin/dsp/DspRtWache.h:36-38`) | B7 **NEU** Teilfall von `312/M-01`; Muster B6 `der_zaehler_trennt_worker_und_audiothread ([SONDE-015] M-47)` (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:6672-6674`) | **Regressionswache** (sie gilt ab dem Bau dieser Etappe, in der Testplayhead und Allokationsbindung entstehen); einmal gebrochen: `Bereich` zusätzlich in den Konstruktor oder in `setNonRealtime` gesetzt → der Nachrichtenthread gilt als Audiopfad → Workerzug zählt → rot | R-312-1 (Satz 2 und 3); E-312-11; `DspRtWache.h:9-15` | 2 |
| M-03 | T3-01-02 · Verhaltensneutralität | A16-Nulltest und B6-Goldens am Basisstand der Etappe | voller Kanon | **Der Nulltest bleibt bitgleich und die RBJ-Goldens bytegleich; kein Ausgangssample ändert sich durch die Hebung des Bereichs** | Bereich ist ein thread-lokaler Zähler, kein Audioweg | A16 bestehend `eq_an_bypass_aus_alles_neutral_ist_bitidentisch`, B6 bestehende Goldenabschnitte; voller Kanon | **Regressionswache**; einmal gebrochen: im `Bereich`-Konstruktor ein `std::vector` anlegen → Allokationszähler steigt und die Etappe ist nicht mehr neutral → rot | Gate GRENZE; `CLAUDE.md:29-33` | 2 |
| M-04 | T3-01-03 · R-312-1 · Messgerät · E-312-10 | echter `SondeProcessor`, Testplayhead aus, Bein öffnet den Bereich wie der Wrapper um das Paar `setNonRealtime(true)` und `processBlock` | 200 Blöcke, vor jedem `setNonRealtime (true)` | **Bis Etappe 4: `RtWache::sperren()` = 200 — jede Nahme von `zustandSchloss` in `setNonRealtime` ist über den gemeldeten Adapter sichtbar; ohne den Wechsel nach offline bleibt sie 0. Ab Etappe 5: derselbe Lauf ergibt `RtWache::sperren()` = 0 und fällt mit M-40 zusammen** | Wrapperfolge: `setNonRealtime` liegt VOR `processBlock` im selben Callback (`juce_audio_plugin_client_VST3.cpp:3890-3892` und `:3907`, Bauartefakt) | B7 **NEU** `312/M-04 offlineflag_nimmt_eine_gemeldete_sperre_je_block`; ab Etappe 5 im selben Änderungssatz wie `312/M-40` auf 0 umgestellt (§7.5) | **zwei Hälften.** Etappen 2 bis 4: **heute nicht messbar** (heute `juce::ScopedLock` bei `SondeProcessor.cpp:1325`, für den Zähler unsichtbar); nach dem Bau der Etappe 2 den gemeldeten Adapter wieder durch `juce::ScopedLock` ersetzen → Zähler 0 → rot. Ab Etappe 5: die Zeile misst 0 und trägt keine eigene Instrumentaussage mehr; dass der Bereich eine Sperre in `setNonRealtime` überhaupt sähe, trägt ab dann der Rotbeweis von M-40 (dort die Sperre im Wechselpfad wieder über den gemeldeten Adapter nehmen → 200 → rot). Die Zeile wird nie rot, weil Etappe 5 den Messgegenstand absichtlich entfernt | R-312-1; SONDE-015 M-47; E-312-10 | 2, umgestellt in 5 |
| M-05 | T3-04-01 · R-312-7 · Testzugang | neuer Testkonstruktor der Sonde mit Probe-Pipename und Servererwartung, unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` | Konstruktion | **`v3PipeNameFuerTest()` liefert genau den übergebenen Probe-Namen; ein Name außerhalb des Probe-Namensraums ergibt in BEIDEN Clients den leeren Pipenamen, mit dem keine Verbindung zustande kommt (fail-closed)** | Pipename wird im Konstruktor gesetzt, vor jedem `start()` (Muster `eq-copilot/plugin/src/PluginProcessor.h:95-101`) | neues Sonde-Lebenslaufbein **NEU** `312/M-05 testkonstruktor_ist_fail_closed` | **heute nicht messbar** (die Sonde hat keinen Testkonstruktor, `grep FuerTest` über `eq-copilot/plugin/sonde/SondeProcessor.h` findet nur `briefkastenStartenFuerTest`, `:361`). Nach dem Bau: `istProbePipename`-Prüfung entfernen → ein Produktionsname wird übernommen → rot | R-312-7; `CLAUDE.md:198-200` | 2 |
| M-06 | T3-04-01 · R-312-7 · Starthaken | Sonde mit Testkonstruktor gegen einen Testserver im Probe-Namensraum | `v3StartFuerTest()`, danach `v3TelemetrieStartFuerTest()` | **Der ControlClient meldet binnen 8 s Status verbunden; ohne die Haken bleibt er getrennt, weil `controlV3.start()` im Produktzweig `SondeProcessor.cpp:191-200` unter dem Testdefine nicht läuft** | Telemetrie koppelt erst nach dem `welcome` des Controls (Muster `PluginProcessor.h:764-766`) | neues Sonde-Lebenslaufbein **NEU** `312/M-06 starthaken_verbinden_ueber_die_probe_pipe` | **heute nicht messbar**. Nach dem Bau: `v3StartFuerTest` auf einen leeren Rumpf setzen → nie verbunden → rot | R-312-7 | 2 |
| M-07 | T3-04-01 · Bauvoraussetzung | neues Konsolenziel, das `eq-copilot/plugin/sonde/SondeProcessor.cpp` übersetzt; Prozessor auf dem Heap (`std::unique_ptr`, MSVC-Stack 1 MiB, NAK-175) | Bau und Lauf des Beins | **Das Bein baut eine Sonde mit laufenden v3-Clients, zerstört sie und endet mit Exitcode 0; der Kanon fährt es als eigenes Bein mit eigener Behauptungszeile** | Zieldeklaration nach dem Muster `EqCopSonde012ProjectReloadTest` (`eq-copilot/plugin/CMakeLists.txt:562-578`) samt der Definitionsschleife `:1323-1330` | neues Sonde-Lebenslaufbein **NEU**, Kürzel beim Bau aus `tools/beweise.ps1` gelesen | **heute nicht messbar** (kein Bein baut heute eine Sonde mit laufenden v3-Clients). Nach dem Bau: Prozessor auf den Stack legen → Stapelüberlauf im vollen Kanon → rot (NAK-175) | R-312-7; `tools/dirigent/pruefliste.md` D | 2 |
| M-08 | T3-04-01 · R-312-7 Schluss | alle Beine, die die Sonde bauen | voller Kanon | **Kein Bein öffnet die Produktions-Pipe: jeder in einem Bein gesetzte Pipename liegt im Probe-Namensraum, geprüft an `nakama::ipc::istProbePipename` vor dem Aufbau** | vor jedem `start()` | neues Sonde-Lebenslaufbein **NEU** Teilfall; Muster `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:443-445` | **Regressionswache**; einmal gebrochen: dem Bein einen Produktionsnamen geben → die Wache des Beins fällt → rot | `CLAUDE.md:198-200`; R-312-7 | 2 |
| M-09 | Etappe 2 · Grundgesetz | Etappe-2-Endstand gegen den Basisstand der Etappe | voller Kanon und `git diff` | **Kein Produktverhalten ändert sich: Nulltest bitidentisch, Goldens bytegleich, 0 Allokationen und 0 Sperren in allen bestehenden Echtzeitfällen; die Änderungen sind Zähler, Testzugänge und Behauptungszeilen** | Etappe 2 steht vor jedem Sperren-Rotbeweis (R-312-1) | voller Kanon; A16, B6, B7 einzeln | **Regressionswache**; einmal gebrochen: in `setNonRealtime` zusätzlich zum gemeldeten Wrapper die Reihenfolge der zwei Anweisungen tauschen → die Vorschau endet erst nach der Hörmatrix → M-41 rot | Gate GRENZE; R-312-1 | 2 |

### 6.2 Etappe 3, erster Änderungssatz — W02 Ladestart (T3-05-01, T3-05-02)

Gemeinsamer Aufbau, wo nicht anders genannt: echter `SondeProcessor` auf dem
Heap, 48 kHz, Blockgröße 256, `hostSchreibt`
(`eq-copilot/plugin/tests/TransactionTestMain.cpp:1795`) als Hosteingang und
`kontrollTaktFuerTest` (`eq-copilot/plugin/sonde/SondeProcessor.h:263`) als
deterministische Barriere statt einer Schlafheuristik.

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-10 | T3-05-01 · R-312-10 (W02) | `eq_enabled` an und Output-Trim +3,0 dB über eine echte Transaktion committet, Audio gefahren, `getStateInformation`; danach über `hostSchreibt` −9,0 dB, OHNE Kontrolltakt | dieselben Bytes über `setStateInformation` in DIESELBE Instanz laden, dann ein Kontrolltakt | **`wirksamerZustand()` trägt Output-Trim exakt +3,0 dB, und Ausgang/Eingang liegt über 512 Samples nach dem Einschwingen innerhalb 1e-6 von 10^(3/20); der vor dem Ladestart eingetroffene Hostwert wirkt nicht** | Quittierung der Mailbox INNERHALB des Zustandsschlosses von `setStateInformation` (`SondeProcessor.cpp:1067-1098`) und VOR `hostParameterAbgleichen` (`:1103`) | B7 **NEU** `312/M-10 ladestart_macht_ausstehende_hostwerte_wirkungslos` neben dem [SONDE-015] M-84-Block (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2180-2200`) | **heute rot** — Gegenprobe Phase 16 C: bestätigt +3 dB, wirksam −8,99999944679 dB (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-c-persistenz.md:15`, gemessen). Die Zusagezeile ist `SondeProcessor.cpp:1462` (`automationSchreiben` aus `hostWert`); nach dem Bau: Quittierungsschleife entfernen → der alte Wert wird wieder geschrieben → rot | SONDE-015 M-84; R-312-10 zweiter Teil; `CLAUDE.md:201-203` | 3 |
| M-11 | T3-05-01 · Gegenfall neuer Gestus | Stand aus M-10 geladen, Mailbox quittiert | nach dem Laden über `hostSchreibt` −6,0 dB, dann Kontrolltakt | **`wirksamerZustand()` trägt exakt −6,0 dB: ein Hostgestus NACH dem erfolgreichen Recall wirkt unverändert** | Zählervergleich auf Gleichheit in `SondeProcessor.cpp:1460`, nicht auf Ordnung | B7 **NEU** Teilfall von `312/M-10` | **heute nicht messbar** als eigener Fall (heute wirkt der Wert ohnehin, aber ohne die Quittierung davor). Nach dem Bau: Quittierung auf `hostEreignisGesehen[i] = 0xFFFFFFFF` statt auf den gelesenen Stand setzen → der neue Gestus wird geschluckt → rot | R-312-10 zweiter Teil | 3 |
| M-12 | T3-05-01 · Gegenfall `prepareToPlay` | wie M-10 | zwischen Laden und Kontrolltakt ein `prepareToPlay (48000, 256)` | **Derselbe Ausgang wie in M-10: exakt +3,0 dB; der Prepare publiziert den geladenen Stand, nicht das Overlay aus der alten Mailbox** | `prepareToPlay` publiziert `transaktion->wirksam()` (`SondeProcessor.cpp:284`) | B7 **NEU** Teilfall von `312/M-10` | **heute rot** — Gegenprobe: `:284` publiziert einschließlich des inzwischen geschriebenen Overlays (Teil A §1b, eigene Ergänzung). Nach dem Bau: dieselbe Mutation wie M-10 → rot | SONDE-015 M-84; R-312-10 zweiter Teil | 3 |
| M-13 | T3-05-01 · Gegenfall `reset` | Output-Trim +3,0 dB committet, über `hostSchreibt` −9,0 dB geschrieben, KEIN Ladestart | `reset()` zwischen Schreiben und Kontrolltakt | **`wirksamerZustand()` trägt danach −9,0 dB: `reset` bewahrt die Hostwert-Mailbox absichtlich, der Fix darf diese Entscheidung nicht mitnehmen** | `reset` nimmt erst `getCallbackLock()`, dann `zustandSchloss` (`SondeProcessor.cpp:340-342`) und fasst die Mailbox nicht an | B7 **NEU** Teilfall von `312/M-10` | **Regressionswache**; einmal gebrochen: die Quittierung zusätzlich in `reset` einziehen → der Hostwert verschwindet → rot | Absichtskommentar `SondeProcessor.cpp:334-337`; `CLAUDE.md:84-86` | 3 |
| M-14 | T3-05-01 · Hashhälfte von SONDE-015 M-84 | wie M-10 | nach dem Kontrolltakt messen | **`bestaetigterZustand()` trägt weiter +3,0 dB und `stateHashText()` ist gleich dem der Quelle; der Undo-Ring ist unverändert** | Ladestart setzt `r = r0 = revision` (`eq-copilot/plugin/state/NakamaTransaktion.cpp:255-256`); `automationSchreiben` erzeugt keine Revision | B7 bestehend `reload_rekonstruiert_denselben_audioausgang`, um den Hashvergleich in derselben Instanz ergänzt | **Regressionswache** (die Hashhälfte hält heute); einmal gebrochen: die Quittierung vor `ladestart` statt danach setzen und dabei die Revision mitziehen → Hash weicht ab → rot | SONDE-015 M-84 (Hashhälfte) | 3 |
| M-15 | T3-05-02 · R-312-10 (W02) | `eq-copilot/fixtures/state/schema2/fremdes-major-3.bin` geladen, `nurLesen` = true, Ausgang vor der Automation als Referenz genommen | über `hostSchreibt` `v2.global.eq_enabled` = 1 und `v1.global.output_trim_db` = +6,0 dB, dann Kontrolltakt | **Der Ausgang bleibt über 512 Samples BITGLEICH zum Eingang (0 abweichende Samples), `zustandLesen().nurLesen` bleibt true, `getStateInformation` bytegleich zur Fixture, und kein Host-Dirty wird gemeldet** | `nurLesen` ist Eigenschaft des Standes und wirkt dort, wo der wirksame Satz entsteht (`NakamaTransaktion.cpp:340-348`, Bedingung in `:345`) | B7 **NEU** `312/M-15 read_only_bleibt_audio_neutral` hinter dem [SONDE-015] M-92-Block (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2017-2036`) | **heute rot** — Gegenprobe Phase 16 C: +6,00000017258 dB bei erhaltenen Originalbytes (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-c-persistenz.md:25`, gemessen). Zusagezeile `NakamaTransaktion.cpp:345`; Mutation: `nurLesen`-Bedingung dort entfernen → rot | SONDE-015 M-92; `CLAUDE.md:29-33` | 3 |
| M-16 | T3-05-02 · Hostanzeige | wie M-15 | nach dem Kontrolltakt den Hostparameter lesen | **Der Hostregler steht wieder auf seinem neutralen Wert: kein Bedienelement spiegelt einen Zustand vor, den es nicht gibt** | `hostParameterAbgleichen` mit dem neutralen Satz (`SondeProcessor.cpp:1422-1432`), derselbe Weg wie beim abgewiesenen Gestus (`:1381-1391`) | B7 **NEU** Teilfall von `312/M-15` | **heute rot** — heute steht der Regler auf +6 dB, während nichts passiert. Nach dem Bau: den Abgleich aus dem read-only-Zweig entfernen → Regler bleibt stehen → rot | `CLAUDE.md:51-52` (sinngemäß für den Hostregler); SONDE-015 M-92 | 3 |
| M-17 | T3-05-02 · Gegenfall gültiger Stand | ein gültiger eigener Stand mit `eq_enabled` an geladen, `nurLesen` = false | dieselbe Hostautomation wie M-15 | **Die Automation wirkt weiterhin: Ausgang/Eingang innerhalb 1e-6 von 10^(6/20); der Rückweg aus read-only heraus ist offen** | `ladestart` setzt das Flag in beide Richtungen | B7 **NEU** Teilfall von `312/M-15` | **Regressionswache**; einmal gebrochen: das `nurLesen`-Flag im Kern nur setzen, nie löschen → nach einem read-only-Stand bleibt jede Automation tot → rot | SONDE-015 M-92; `CLAUDE.md:84-86` (aktivieren↔abklingen) | 3 |
| M-18 | W02 · Zahlenrand NaN und ±Inf | wie M-10 und M-15 | ein nicht endlicher Hostwert (NaN, +Inf, −Inf) trifft vor und nach dem Ladestart ein | **Kein Ausgangssample wird nicht endlich; der nicht endliche Wert wird wie heute behandelt (`zelleAusHost` gibt den bestätigten Wert zurück, `SondeProcessor.cpp:1394-1420`), und die neue Quittierung ändert dieses Urteil nicht** | Prüfung vor dem Schreiben ins Overlay | B7 **NEU** Teilfälle von `312/M-10` und `312/M-15` | **Regressionswache** (die Endlichkeitsregel hält heute); einmal gebrochen: die Endlichkeitsprüfung in `zelleAusHost` entfernen → ein NaN erreicht das Overlay → rot | `CLAUDE.md:29-33`; NaN-Ehrlichkeit | 3 |
| M-78 | T3-05-01 · R-312-11 · Hostwert „während des Ladens" | wie M-10; ein Haken des Beins hält `setStateInformation` an einer Schranke INNERHALB des Zustandsschlosses an, nach der Quittierung (`SondeProcessor.cpp:1067-1098`) und vor `hostParameterAbgleichen` (`:1103`) | aus einem zweiten Thread über `hostSchreibt` −6,0 dB auf `v1.global.output_trim_db`, dann Schranke freigeben, dann ein Kontrolltakt | **Der Wert wirkt, und Regler und Klang zeigen denselben: `wirksamerZustand()` trägt exakt −6,0 dB, `hostParameter[i]->getValue()` liest nach dem Abgleich genau diesen Wert in Hostgenauigkeit zurück, und kein Sample klingt nach dem geladenen +3,0 dB** | der Abgleich überschreibt keinen Parameter, dessen Zähler seit der Quittierung gestiegen ist, und prüft das nach seiner Schleife (`:1425-1431`) ein zweites Mal — beide Prüfungen auf **Gleichheit** des Zählerstands, nie auf Ordnung | B7 **NEU** `312/M-78 hostwert_nach_der_quittierung_wirkt_und_wird_gezeigt` neben `312/M-10` | **heute nicht messbar** (weder Quittierung noch Schranke existieren; ohne Quittierung ist „während" nicht definiert). Nach dem Bau: die Zählerprüfung im Abgleich entfernen → `setValueNotifyingHost` (`:1430`) schreibt den geladenen Wert zurück, der Regler steht auf +3,0 dB und der Klang auf −6,0 dB → rot | R-312-11; `CLAUDE.md:51-52` (sinngemäß für den Hostregler, wie M-16); SONDE-015 M-84 | 3 |
| M-19 | W02 · Zahlenrand Zählerüberlauf | Hostereigniszähler und Quittierungsstand über einen Testsetter dicht unter `0xFFFFFFFF` gesetzt | drei Hostereignisse über den Überlauf hinweg, dazwischen ein Ladestart | **Die Quittierung vergleicht auf Gleichheit, nicht auf Ordnung: nach dem Überlauf wirkt das nächste Ereignis, und kein Ereignis wird doppelt verbraucht** | `hostEreignis` ist `std::uint32_t` (`eq-copilot/plugin/sonde/SondeProcessor.h:542`), `hostEreignisGesehen` dasselbe (`:543`); Vergleich `SondeProcessor.cpp:1460` | B7 **NEU** `312/M-19 quittierung_ist_ueberlaufsicher` | **heute nicht messbar** (kein Testsetter). Nach dem Bau: `==` bei `:1460` durch `<` ersetzen → das erste Ereignis nach dem Überlauf verschwindet → rot | R-312-10 zweiter Teil; `CLAUDE.md` NaN-Ehrlichkeit (Zahlenränder) | 3 |

### 6.3 Etappe 3, zweiter Änderungssatz — Blockbindung (T3-01-05, R-312-10)

**Referenzlauf** heißt hier: ein zweiter Prozessor mit identischem Stand, der
`kontrollTaktFuerTest()` nach JEDEM Block bekommt — die Ausgabe, die der
Entwurfssatz „vom vorigen zum letzten Blockwert" beschreibt.

**Taktsperre** heißt in diesem Abschnitt der Testzugang, der die Zeilen von der
Wanduhr löst (Matrixregel §6, in der Nacharbeit 1 ergänzt). Der Worker tickt
frei nach Wanduhr (`SondeProcessor.cpp:575-578`, bei leerer Analysequeue
zusätzlich bis 20 ms `:738-740`); ohne Gegenmittel hinge „A hat den Wert nicht
bekommen" an einem Wettlauf mit ihm. `dspKontrollTakt` nimmt als erste Anweisung
`zustandSchloss` (`:1449`). Der Prüfling A fährt seine Blöcke deshalb innerhalb
eines Testzugangs, der genau dieses Schloss hält, solange die übergebene
Funktion läuft — wörtlich nach dem Muster von `mitAngehaltenerAnalyseFuerTest`
(`eq-copilot/plugin/sonde/SondeProcessor.h:316-326`), das dasselbe für den
Analyseworker tut. `processBlock` nimmt `zustandSchloss` nie (das ist die Zusage
von M-01 und M-26), der Aufruf ist also erlaubt und der Worker kann in diesem
Fenster **beweisbar** nicht ticken. Damit sind „A ohne Takt" und „B mit Takt
nach jedem Block" beide deterministisch, und der Lauf gegen den Basisstand ist
ein Beleg statt einer Wahrscheinlichkeit.

**Die abgedeckte Menge, namentlich (E-312-5).** Blockgebunden werden in diesem
Ticket genau vier Hostparameter: `v1.global.input_trim_db`,
`v1.global.output_trim_db`, `v1.global.width` und `v2.global.mix`
(`eq-copilot/plugin/state/NakamaParameter.cpp:50-52`, `:80`). Sie sind genau die
kontinuierlichen Hostparameter, für die der Kern schon heute ein Rampenziel
führt (`eq-copilot/plugin/dsp/DspKern.cpp:620-623`, gespeist aus
`eq-copilot/plugin/dsp/DspProgramm.cpp:259-264` und `:271-272`). **Nicht
abgedeckt und taktgebunden bleiben** die 64 Bandwerte
(`NakamaParameter.cpp:64-66`, `:69-73` je Slot, wirksam nur über ein Programm:
`DspProgramm.cpp:281-356`), `mono_bass_hz` (`NakamaParameter.cpp:53`,
`DspProgramm.cpp:262`, `:277-279`), die elf booleschen Hostparameter
(`NakamaParameter.cpp:49`, `:62` achtmal, `:79`, `:81`) und das abgeleitete
Auto-Gain-Ziel (`DspKern.cpp:626`, `DspProgramm.cpp:367`; SONDE-015 M-39
verlangt die Ableitung außerhalb des Audiothreads). M-79 misst die Abdeckung,
M-80 und M-81 messen den taktgebundenen Rest ehrlich als heutigen und
bleibenden Zustand dieses Tickets — für ihn sagt keine Zeile Gleichheit zwischen
Echtzeit und Offline zu. Der Rest ist Teil b von T3-01-05 und geht an NAK-340
(Planschritt S25p).

**Die Schiedsregel zwischen Blockrand und Worker (E-312-6).** Der Blockrand
liest in **jedem** Block die vier Ereigniszähler (`SondeProcessor.h:542`) und
übernimmt einen Wert genau dann, wenn der Zähler **ungleich** dem zuletzt am
Blockrand gesehenen Stand ist; verglichen wird nie auf Ordnung, weil der Zähler
überläuft (M-19, M-82). `hostEreignisOffen` (`:544`) behält seinen einzigen
Verbraucher, den Worker (`SondeProcessor.cpp:1455`); der Blockrand fasst es
nicht an. Jede Workerpublikation trägt je abgedecktem Parameter den Zählerstand,
aus dem ihr Wert stammt, und setzt ein Rampenziel nur bei Gleichheit mit dem
Blockrandstand (§7.3).

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-20 | T3-01-05 · R-312-10 (E-312-5) | zwei Prozessoren mit identischem Stand, 48 kHz, Blockgröße 64 (1,33 ms, weit unter dem Workertakt von 5 bis 20 ms, `SondeProcessor.cpp:575-578` und `:738-740`); Prüfling A fährt seine Blöcke unter der Taktsperre (der Worker kann dort beweisbar nicht ticken), B ist der Referenzlauf mit Takt nach jedem Block | je Parameter der abgedeckten Menge (`input_trim_db`, `output_trim_db`, `width`, `v2.global.mix`) dieselbe Eventfolge: Block 10 = oberer Wert, 11 = Standardwert, 12 = oberer Wert, 13 = Standardwert | **Für jeden der VIER abgedeckten Parameter ist der Ausgang von A über den ganzen Lauf BITGLEICH zu B (0 abweichende Samples); der wirksame Block hängt am Blockrand, nicht an der Wanduhr. Über andere Hostparameter sagt die Zeile nichts (M-79 bis M-81)** | Blockrandlesung im Kern VOR der Programmübernahme (`eq-copilot/plugin/dsp/DspKern.cpp:531`), im selben Callback vor `dspKern->verarbeite` (`SondeProcessor.cpp:432`); gerechnet wird dieselbe Zelle wie `zelleAusHost` (`:1394-1420`) einschließlich der Ausnahme `:1403-1404` | B7 **NEU** `312/M-20 hostwerte_sind_blockgebunden` neben `eq-copilot/plugin/tests/TransactionTestMain.cpp:2042-2060` | **heute rot** — Gegenprobe: `SondeProcessor.cpp:575-578` zieht am Wanduhrtakt, `:1460` springt auf den aktuellen Zählerstand, Zwischenwerte sind dauerhaft verloren (Teil A §5b). Zusagezeile ist der neue Blockrandzugriff; Mutation: ihn entfernen und wieder allein auf den Workerzug stellen → rot | SONDE-015 M-120; Entwurf `:3084-3086`; R-312-10 in der Fassung von E-312-5 (§5.4) | 3 |
| M-21 | T3-01-05 · Offline | wie M-20 | derselbe Lauf mit `setNonRealtime (true)` und mindestens zehnfacher Geschwindigkeit (kein Warten zwischen den Blöcken) | **Für die vier abgedeckten Parameter ist der Offlineausgang bitgleich zum Echtzeitausgang desselben Event- und Blockverlaufs** | Blockrand, nicht Wanduhr; der Worker darf beliebig spät ziehen | B7 **NEU** Teilfall von `312/M-20` | **heute rot** — nachgerechnet aus `SondeProcessor.cpp:575-579` und der gemessenen Offlinegeschwindigkeit 3,00394 s Audio in 0,21441 s (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-e-last-lebenslauf.md:41`, gemessen): rund jeder zehnte Wert überlebt. Mutation wie M-20 | SONDE-015 M-120; R-312-10 | 3 |
| M-22 | T3-01-05 · Zahlenrand Blockgröße | wie M-20, Blockgrößen 1, 64, 240, 256, 512 und die von FL gemeldete Hostblockgröße (`eq-copilot/identity/host-capabilities-fl-v1.json:54`) | dieselbe Eventfolge je Blockgröße | **Bei jeder dieser Blockgrößen ist A für die vier abgedeckten Parameter bitgleich zu B; bei Blockgröße 1 bindet jeder Block genau einen Wert** | 240 Samples sind bei 48 kHz 5 ms, 512 sind 10,7 ms — beide liegen innerhalb des Workertakts von 5 bis 20 ms (`SondeProcessor.cpp:575-578`, `:738-740`), deshalb bindet heute keine dieser Blockgrößen zuverlässig | B7 **NEU** Teilfall von `312/M-20` | **heute rot bei jeder gefahrenen Blockgröße.** Berichtigt in der Nacharbeit 1: die frühere Einstufung „512 und darüber ist Regressionswache, heute schon bitgleich" trifft nicht zu — der Takt ist keine feste 5-ms-Grenze, sondern 5 bis 20 ms, und ein Bein ohne Echtzeittaktung fährt ohnehin schneller als jeder Takt. Zusagezeile und Mutation wie M-20 | SONDE-015 M-120; Entwurf `:3084-3086`; Randbeobachtung der Validierung (Teil 2.1) | 3 |
| M-23 | T3-01-05 · bewegte Automation und Endwert | wie M-20, aber eine Rampe auf `v1.global.output_trim_db` über 200 Blöcke von −12 dB nach +12 dB und ein Halt auf dem Endwert | Automationsfahrt mit einem Punkt je Block | **A und B sind bitgleich, und nach dem Ende der Bewegung tragen beide exakt den Endwert +12,0 dB; der Schaden ist der Weg, nie das Ziel** | Endwert steht in der Mailbox, bis ein neuer kommt | B7 **NEU** Teilfall von `312/M-20` | **heute rot** für den Weg, **Regressionswache** für den Endwert; Mutation wie M-20 | SONDE-015 M-120; Entwurf `:3070-3071` | 3 |
| M-24 | T3-01-05 · SONDE-015 M-81 · aktivieren↔abklingen | wie M-23 | eine vollständige Hostgeste mit Ruhe danach | **Die Automationsepoche wechselt genau ZWEIMAL je Geste, und die Ruhegrenze zählt weiter in verarbeiteten Audiosamples (0,25 s), nicht in Workerzügen** | Ruhegrenze `SondeProcessor.cpp:1468-1473` mit `kAutomationsRuheSekunden` (`eq-copilot/plugin/state/NakamaTransaktion.h:62`) | B7 bestehend `epoche_wechselt_genau_zweimal_je_geste` (`tools/beweise.ps1:728`), erweitert um den blockgebundenen Weg | **Regressionswache**; einmal gebrochen: die Ruhegrenze an den Blockzähler statt an verarbeitete Samples hängen → bei Blockgröße 1 wechselt die Epoche zu früh → rot | SONDE-015 M-81 | 3 |
| M-25 | T3-01-05 · speichern↔laden | wie M-23 | Automationsfahrt, danach `getStateInformation` | **Das Overlay erzeugt KEINE Revision und keinen Host-Dirty; die Statebytes sind bytegleich zu denen vor der Fahrt** | `automationSchreiben` (`eq-copilot/plugin/state/NakamaTransaktion.cpp:316-324`) setzt nur das Overlaybit | B7 bestehend `automation_erzeugt_keine_revision` | **Regressionswache**; einmal gebrochen: den Blockrandzugriff über `fuehreTransaktionAus` statt über das Overlay führen → jede Automationsfahrt erzeugt Revisionen → rot | SONDE-015 M-81; `CLAUDE.md:201-203` | 3 |
| M-26 | T3-01-05 · Grundgesetz | echter `SondeProcessor`, Testplayhead aus, `RtWache::zuruecksetzen()` | 4000 Blöcke wechselnder Größe mit laufender Hostautomation auf allen vier abgedeckten Parametern | **`RtWache::sperren()` = 0 und `RtWache::allokationen()` = 0 über den ganzen Lauf, gemessen ab Callback-Eintritt (M-01) — `RtWache::allokationen()` ist in B7 seit Etappe 2 messbar (E-312-11), diese Zeile der Etappe 3 setzt sie voraus; der Blockrandzugriff nimmt keine Sperre, allokiert nicht und wartet nie — er liest je Parameter einen Ereigniszähler, einen Hostwert und den vom Worker veröffentlichten bestätigten Vergleichswert, alles relaxed aus Atomics** | Bereich ab `processBlock` (`SondeProcessor.cpp:371`); die Ausnahme von `zelleAusHost` (`:1403-1404`) braucht den bestätigten Wert, der heute nur unter dem Schloss steht (`:1398`) — der Worker veröffentlicht ihn deshalb für die vier Parameter als Atomic (§7.3) | B7 **NEU** `312/M-26 blockbindung_ist_echtzeitfest` | **heute nicht messbar** (der Mechanismus entsteht im Bau). Nach dem Bau: im Blockrandzugriff `transaktion->bestaetigt()` unter einer gemeldeten Sperre auf `zustandSchloss` lesen statt das veröffentlichte Atomic → Zähler steigt → rot | `CLAUDE.md:29-33`; SONDE-015 M-47; E-312-11; R-312-10 (Satz 3); E-312-5 | 3 |
| M-27 | T3-01-05 · Gegenfall große Blöcke | wie M-20, Blockgröße 4096 (85 ms, über jedem Workertakt) | dieselbe Eventfolge | **A und B sind für die vier abgedeckten Parameter bitgleich, und der Wert wirkt in DEMSELBEN Block, an dessen Rand er gelesen wurde — nicht im folgenden** | 4096 Samples sind 85 ms bei 48 kHz und liegen über dem Takt von 5 bis 20 ms; die Zeile grenzt den Übernahmemoment ein, nicht die Taktfrage | B7 **NEU** Teilfall von `312/M-20` | **heute rot.** Berichtigt in der Nacharbeit 1: die frühere Einstufung „Regressionswache, heute schon bitgleich" trifft nicht zu — auch bei 4096 fährt ein Bein ohne Echtzeittaktung schneller als der Workertakt, und der Referenzlauf B taktet nach jedem Block. Nach dem Bau: den Blockrandzugriff so bauen, dass er den Wert erst im FOLGENDEN Block nimmt → bei 4096 weicht A um einen ganzen Block ab → rot | SONDE-015 M-120; Randbeobachtung der Validierung (Teil 2.1) | 3 |
| M-28 | T3-01-05 · Gegenfall ohne Ereignisse | wie M-20, aber keine Hostereignisse | 200 Blöcke | **A und B sind bitgleich, und der Blockrandzugriff setzt kein Rampenziel: alle vier Zähler sind gleich dem zuletzt am Blockrand gesehenen Stand, und `DspKern::uebernahmen()` sowie die gefahrenen Rampenziele sind gleich denen eines Laufs ohne den Mechanismus** | Gleichheitsvergleich je Parameter am Blockrand; `hostEreignisOffen` wird dabei NICHT gelesen und nicht getauscht — es behält seinen einzigen Verbraucher, den Worker (`SondeProcessor.cpp:1455`) | B7 **NEU** Teilfall von `312/M-20` | **Regressionswache**; einmal gebrochen: den Blockrandzugriff das Rampenziel unbedingt setzen lassen, statt nur bei ungleichem Zähler → er überschreibt die Ziele des Programms bei jedem Block → rot. Zweite Gegenprobe im selben Fall: den Blockrand `hostEreignisOffen.exchange` rufen lassen → der Worker verliert die Meldung, Overlay, Epoche und Ruhegrenze (`:1457-1473`) laufen nicht mehr → M-24 und M-25 rot | SONDE-015 M-120; Grundgesetz; E-312-6 | 3 |
| M-29 | T3-01-05 mit W02 · Beziehung | Stand mit +3 dB committet, Automationsfahrt läuft | mitten in der Fahrt ein Ladestart in dieselbe Instanz, danach weitere Blöcke mit neuen Hostwerten | **Werte vor dem Ladestart wirken nicht, Werte danach wirken am nächsten Blockrand; der bestätigte Zustand ist der geladene, und der Hash bleibt der der Quelle** | Quittierung im Ladestart (M-10) vor dem nächsten Blockrandzugriff | B7 **NEU** `312/M-29 ladestart_und_blockbindung_greifen_ineinander` | **heute rot** (beide Hälften brechen heute einzeln, M-10 und M-20). Nach dem Bau: die Quittierung hinter den ersten Blockrand nach dem Laden schieben → ein alter Wert wirkt einen Block lang → rot | R-312-10 (beide Teile); SONDE-015 M-84 | 3 |
| M-79 | T3-01-05 · E-312-5 · Abdeckung | wie M-20; das Bein liest die Abdeckungstabelle des Blockrands über einen Testzugang des Kerns | je ein Hostereignis auf jedem der 112 Hostparameter, ohne Kontrolltakt dazwischen | **Die Abdeckung ist genau vier Parameter groß und namentlich festgelegt: die Tabelle trägt exakt `v1.global.input_trim_db`, `v1.global.output_trim_db`, `v1.global.width`, `v2.global.mix`; für diese vier ist A bitgleich zu B, und für jeden der übrigen 108 setzt der Blockrandzugriff kein Rampenziel** | die vier sind genau die kontinuierlichen Hostparameter mit einem Rampenziel im Kern (`eq-copilot/plugin/dsp/DspKern.cpp:620-623`); `autoGain` (`:626`) ist abgeleitet (`eq-copilot/plugin/dsp/DspProgramm.cpp:367`), kein Hostparameter | B7 **NEU** `312/M-79 genau_vier_parameter_sind_blockgebunden` | **heute nicht messbar** (es gibt weder Tabelle noch Blockrandzugriff). Nach dem Bau: `v1.global.width` aus der Tabelle nehmen → die Mengenprüfung fällt UND die Bitgleichheit für `width` fällt → rot; umgekehrt `v1.global.mono_bass_hz` aufnehmen → die Mengenprüfung fällt → rot | E-312-5; R-312-10 in der Fassung von E-312-5 | 3 |
| M-80 | T3-01-05 Teil b · E-312-5 · Grenzzeile Bandwert | wie M-20, aber die Eventfolge läuft auf `v1.band.5.gain_db` (freier Slot, Muster `eq-copilot/plugin/tests/TransactionTestMain.cpp:2050-2053`) | dieselbe Eventfolge zweimal: einmal unter der **Taktsperre** (der Worker kann beweisbar nicht ticken), einmal mit `kontrollTaktFuerTest()` nach JEDEM Block | **Der Bandwert bleibt taktgebunden, und die Zeile sagt das ehrlich: er steht NICHT in der Abdeckungstabelle des Blockrands (M-79); unter der Taktsperre weichen A und B in mindestens einem Sample ab, mit Kontrolltakt nach jedem Block sind sie bitgleich; der Endwert ist in beiden Läufen exakt der letzte gesetzte. Für Bandwerte sagt KEINE Zeile dieses Tickets Gleichheit zwischen Echtzeit und Offline zu — das bleibt der Zustand nach diesem Ticket und geht als Teil b von T3-01-05 an NAK-340 (Planschritt S25p)** | Bandwerte wirken nur über ein Programm (`DspProgramm.cpp:281-356`), das der Worker unter dem Zustandsschloss baut (`SondeProcessor.cpp:1476-1477`); der Blockrand hat für sie kein Ziel | B7 **NEU** `312/M-80 bandwert_bleibt_taktgebunden` | **Regressionswache** — sie hält heute und muss nach dem Bau weiter halten, weil die Abdeckung sich in diesem Ticket nicht ausdehnen darf; einmal gebrochen: `gain_db` in die Abdeckungstabelle von M-79 aufnehmen → die erste Hälfte (ohne Takt abweichend) fällt, und der Kern schriebe Bandwerte ohne die eigene Besitzregel, die NAK-340 erst baut → rot | E-312-5; SONDE-015 M-120 (für Bandwerte weiterhin gebrochen, offen geführt) | 3 |
| M-81 | T3-01-05 Teil b · E-312-5 · Grenzzeile boolescher Parameter | wie M-80, aber die Eventfolge läuft auf `v2.global.eq_enabled` (`eq-copilot/plugin/state/NakamaParameter.cpp:79`) | Umschalten an/aus je Block, einmal unter der **Taktsperre** und einmal mit Kontrolltakt nach jedem Block | **Auch der boolesche Parameter bleibt taktgebunden, in zwei getrennt geprüften Hälften. MENGE: `v2.global.eq_enabled` steht NICHT in der Abdeckungstabelle des Blockrands (M-79) — der Fall trägt dafür eine eigene Behauptung. VERHALTEN: unter der Taktsperre liegt der Umschaltblock von A hinter dem von B (mindestens ein abweichendes Sample), mit Kontrolltakt sind beide bitgleich; der Endzustand stimmt in beiden Läufen. Keine Gleichheitszusage für die elf booleschen Hostparameter** | `eq_enabled` wirkt über `aus.eqEngagiert` im Programm (`DspProgramm.cpp:263`), nie über ein Rampenziel (`eq-copilot/plugin/dsp/DspKern.cpp:620-623` setzt genau vier Ziele, `:626` das abgeleitete `autoGain`) | B7 **NEU** Teilfall von `312/M-80`, mit eigener Mengenbehauptung über die Abdeckungstabelle | **zwei Hälften.** Mengenhälfte: **Regressionswache**; einmal gebrochen: `eq_enabled` in die Abdeckungstabelle aufnehmen → die eigene Mengenbehauptung dieses Falls fällt → rot. Die wahre Folge dieser Mutation steht hier, statt einer geliehenen: der Blockrand schreibt allein `blockrandStand[i]` (§7.3 Punkt 1) und fasst `hostEreignisOffen` nicht an (Punkt 5), der Worker quittiert weiter selbst (`SondeProcessor.cpp:1455`, `:1460-1462`), und der Schalter wirkt weiter über `aus.eqEngagiert` (`DspProgramm.cpp:263`) — der frühere Satz „der Wert verschwindet für den Worker → der Schalter wirkt gar nicht mehr" war an der Quelle falsch und gehörte M-28. Verhaltenshälfte: **messend, ohne eigene Mutation** (E-312-12) — ohne Rampenziel ändert die Tabellenmitgliedschaft am Laufverhalten nichts, A bleibt hinter B; eine Mutation, die zusätzlich einen Schreibweg auf `aus.eqEngagiert` gäbe, baute im Rotlauf genau den Blockrandweg, den E-312-5 nach NAK-340 verschiebt, und ist deshalb verworfen | E-312-5; E-312-12; `CLAUDE.md:29-33` | 3 |
| M-82 | T3-01-05 · E-312-6 · Zahlenrand der Schiedsregel | wie M-20; Ereigniszähler (`SondeProcessor.h:542`) und Blockrandstand im Kern über einen Testsetter dicht unter `0xFFFFFFFF` gesetzt | drei Hostereignisse auf `v1.global.output_trim_db` über den Überlauf hinweg, dazwischen je ein Block | **Der Blockrand vergleicht auf UNGLEICHHEIT, nie auf Ordnung: nach dem Überlauf wirkt das nächste Ereignis am nächsten Blockrand, kein Ereignis wird doppelt übernommen, und A bleibt bitgleich zu B** | derselbe Gleichheitsbegriff wie die Quittierung in `SondeProcessor.cpp:1460` (M-19); der Blockrandstand ist ein `std::uint32_t`, der dem Audiothread allein gehört | B7 **NEU** `312/M-82 schiedsregel_ist_ueberlaufsicher` neben `312/M-19` | **heute nicht messbar** (weder Blockrandstand noch Testsetter). Nach dem Bau: `!=` am Blockrand durch `>` ersetzen → das erste Ereignis nach dem Überlauf fehlt → rot | E-312-6; M-19 (bleibt wörtlich); `CLAUDE.md` NaN-Ehrlichkeit (Zahlenränder) | 3 |

### 6.4 Etappe 4 — Besitz (T3-04-01, T3-04-03; R-312-7, R-312-2)

**Kanarienvogel** heißt in beiden Bündeln dieselbe Marke: ein
`std::atomic<bool>`, das dem Bein gehört, den Prozessor überlebt und dessen
Destruktor es auf false setzt; der Rückruf zählt, ob er nach `false` noch
gelaufen ist. Eine Nullprüfung genügt nicht — `~unique_ptr` nullt seinen Zeiger
nicht (R-312-7 letzter Satz, NAK-246 R-D2 Rotbeweis).

**Schranke und Marke sind zwei Dinge (R-312-12).** Die **Schranke** hält den
Rückruf an; wo sie sitzt, sagt jede Zeile ausdrücklich (vor oder hinter
`betreten()`), weil davon abhängt, welcher Schleusenzähler steigt. Die **Marke**
sitzt an der Zugriffsstelle selbst — unmittelbar vor der ersten Zeile, die
Besitzerzustand anfasst: sie liest das `std::atomic<bool> lebt` des Beins, und
bei `lebt == false` erhöht sie `nachEndeErreicht` und lässt den Rückruf
**zurückkehren, ohne den Prozessor anzufassen**; danach läuft dieser Rückruf nie
wieder an. Damit berührt weder der Lauf gegen den Basisstand noch eine Mutation
freigegebenen Speicher: der Beleg „heute rot" ist die **gezählte Marke**, nie
ein Absturz. Ohne diese Marke wäre der Basisstandlauf von M-30 und M-31 ein
echtes Use-after-free — `transaktion` (`eq-copilot/plugin/sonde/SondeProcessor.h:537`)
stirbt vor `controlV3` (`:523`), und der Providerweg liest
`transaktion->revision()` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1143`).
Nach dem Bau erreicht ein abgewiesener Zug die Marke gar nicht erst; im
Basisstand, wo es keine Schleuse gibt, erreicht er sie und wird gezählt.

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-30 | T3-04-01 · R-312-7 · R-312-12 | Sonde mit Testkonstruktor und Starthaken (M-05, M-06) gegen einen Testserver im Probe-Namensraum; die Schranke des Beins sitzt in `v3Status()` HINTER `betreten()`, die Marke unmittelbar vor `transaktion->revision()` (`SondeProcessor.cpp:1143`) | Prozessor aus dem Testthread zerstören (der Destruktor setzt `lebt` auf false), danach die Schranke aus einem dritten Thread freigeben, sobald `stand().geschlossen` wahr ist, dann Zähler und Schleusenstand lesen | **Nach dem Ende des Eigentümers erreicht kein Provider den Besitzerzugriff: `nachEndeErreicht` = 0; der Stand der Schleuse (`eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:172-181`) zeigt bei dieser Hakenlage `gewartetMs` ≥ 1 und `abgewiesen` = 0, und der Destruktor ist erst danach zurückgekehrt** | Schleuse schließt im Destruktor NACH `telemetryV3.stop()` und `controlV3.stop()` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:228-229`) und VOR der Zerstörung der Mitglieder; der Zähler wird am ENDE des Destruktorrumpfs gelesen, nicht an seinem Anfang — sonst zählte ein zulässiger Zugriff während des Wartens als „nach dem Ende" | neues Sonde-Lebenslaufbein **NEU** `312/M-30 provider_fasst_nach_dem_schliessen_nichts_mehr_an`; Muster `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:456-475` | **heute rot** (UB-frei belegt) — der Lauf gegen den Basisstand misst `nachEndeErreicht` = 1, weil der Haken vor `SondeProcessor.cpp:1143` (`transaktion->revision()`) zählt und zurückkehrt; er stürzt nicht ab, und der Beleg ist die gezählte Marke (R-312-12). Zusagezeile ist das `schliessen()` im Destruktor; Mutation nach dem Bau: es entfernen → der Rückruf wird weder abgewiesen noch abgewartet → Marke zählt → rot | R-D2 (`docs/beweise/NAK-246.md:1008-1016`); R-312-7; R-312-12 | 4 |
| M-31 | T3-04-01 · Wiederholung · R-312-12 | wie M-30, derselbe zählende und anhaltende Haken in jedem Zyklus | 20 Gegenzyklen aus Aufbau, gehaltenem Rückruf, Abbau und Freigabe | **In jedem Zyklus `nachEndeErreicht` = 0; Handles und Threads des Prozesses stehen nach dem letzten Zyklus nicht höher als nach dem ersten** | je Zyklus eine eigene Schleuse; sie gehört dem Prozessor, nicht dem Bein | neues Sonde-Lebenslaufbein **NEU** Teilfall von `312/M-30`; Muster Phase 16 E (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-e-last-lebenslauf.md:67-69`) | **heute rot** (UB-frei belegt) (jeder der 20 Zyklen zählt an der Marke statt freigegebenen Speicher zu berühren, R-312-12). Nach dem Bau: die Schleuse als Mitglied statt als `shared_ptr` halten → der abgelöste Rückruf hält eine tote Schleuse; der Haken zählt, bevor er sie anfasst → rot ohne Absturz | R-D2; R-312-7; R-312-12 | 4 |
| M-32 | T3-04-01 · starten↔stoppen · Frist | wie M-30, Rückruf hält länger als die Stoppfrist | `stop()` der beiden Clients | **`stop()` kehrt zurück, der Zähler `stopFristUeberschritten` steigt um 1, und der Thread wird abgelöst; der Destruktor wartet danach höchstens die Restdauer des laufenden Rückrufs, nie unbegrenzt** | `kStopFristMs` = 2000 (`eq-copilot/plugin/core/ipc/controlclient/Intern.h:59` und `eq-copilot/plugin/core/ipc/TelemetryClient.cpp:30`), Zähler `ControlClient.cpp:129`, Ablösung `:132` (in der Nacharbeit 1 am Code nachgemessen und berichtigt) | neues Sonde-Lebenslaufbein **NEU** Teilfall von `312/M-30` | **Regressionswache** (die Frist hält heute); einmal gebrochen: `schliessen()` VOR `stop()` rufen → der Destruktor wartet auf einen Rückruf, den niemand mehr abbricht → Laufzeit reißt die Beinfrist → rot | R-D2 (Satz 4); `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:17-23` | 4 |
| M-33 | T3-04-01 · verbinden↔trennen | Sonde mit laufenden Clients, Schleuse offen | `setStateInformation` mit gültigem Stand, das `controlV3.reconnect()` und `telemetryV3.reconnect()` auslöst | **Ein Reconnect schließt eine offene Schleuse nicht und öffnet eine geschlossene nicht: `istGeschlossen()` ist vor und nach dem Reconnect gleich, und `betreten` steigt weiter** | Reconnect steht am Ende des Ladens (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1104-1105`), außerhalb des Zustandsschlosses | neues Sonde-Lebenslaufbein **NEU** `312/M-33 reconnect_ruehrt_die_schleuse_nicht_an` | **heute nicht messbar** (keine Schleuse). Nach dem Bau: `reconnect` die Schleuse neu anlegen lassen → ein abgelöster Rückruf sieht eine offene Schleuse → Marke zählt → rot | R-312-7; `CLAUDE.md:84-86` | 4 |
| M-34 | T3-04-01 · starten↔stoppen | Sonde mit Testkonstruktor | `start()` beider Clients, dann Destruktor | **Reihenfolge im Destruktor wie bei Gen: Briefkasten stoppen (`:216`), Listener abmelden (`:221-222`), Worker joinen (`:224-227`), `telemetryV3.stop()` (`:228`), `controlV3.stop()` (`:229`), danach Schleuse schließen — und erst dann enden die Mitglieder** | `SondeProcessor.cpp:212-230`; Vorbild `eq-copilot/plugin/src/PluginProcessor.cpp:500` | neues Sonde-Lebenslaufbein **NEU** Teilfall von `312/M-30` | **heute nicht messbar**. Nach dem Bau: das `schliessen()` an den Anfang des Destruktors ziehen → laufende Rückrufe werden abgewiesen, bevor `stop()` sie beenden konnte, und `abgewiesen` steigt, wo `gewartetMs` stehen müsste → rot | R-D2; `CLAUDE.md:84-86` | 4 |
| M-35 | T3-04-03 · R-312-2 | Gen in Legacy-Rolle, Editor offen, Messpunkt-Popover steht (`eq-copilot/plugin/src/PluginEditor.cpp:734`), im Panel eine echte Änderung gesetzt | Editor zerstören, danach Prozessor zerstören, DANN der Nachrichtenschleife einen Durchlauf geben | **Der Panel-Destruktor fasst weder Prozessor noch Editor an: die Marke zählt 0 Zugriffe nach dem Ende, keine Mutation an der Bindung, 0 Host-Dirty-Meldungen** | Lebendprüfung VOR dem Prozessorzugriff in `uebernehmen()` (`PluginEditor.cpp:700-709`, Zugriff heute bei `:707`) | B15 **NEU** `312/M-35 panel_abbau_nach_prozessorende_ist_wirkungslos`, alternativ ein Fall der B14-Familie | **heute rot** — Gegenprobe: `proz` ist eine rohe Referenz (`PluginEditor.cpp:627`), `~MesspunktPanel` ruft `uebernehmen()` unbedingt (`:688`), und `setzeBindung` nimmt sofort `bindungMutex` (`eq-copilot/plugin/src/prozessor/State.cpp:304`). Zusagezeile ist die neue Lebendprüfung; Mutation: sie entfernen → Marke zählt → rot | R-312-2; Teil B §2 | 4 |
| M-36 | T3-04-03 · öffnen↔schließen | wie M-35 | Panel schließen, DANN Editor, DANN Prozessor zerstören | **Der normale Handgriff bleibt unverändert: die Bindung wird übernommen, und genau EINE Host-Dirty-Meldung entsteht, weil genau eine persistente Änderung vorliegt** | `uebernehmen()` läuft im Destruktor des Panels, solange der Editor lebt | B15 **NEU** Teilfall von `312/M-35` | **Regressionswache**; einmal gebrochen: die Lebendprüfung so bauen, dass sie den Editor im Abbau bereits als tot führt, bevor das Panel schließt → der normale Handgriff verliert die Änderung → rot | R-312-2 (letzter Satz) | 4 |
| M-37 | T3-04-03 · Gegenfall ohne Änderung | wie M-36, aber im Panel nichts geändert | Panel schließen, Editor und Prozessor zerstören | **Keine Mutation und keine Dirty-Meldung: der Frühausstieg wegen fehlender Änderung bleibt, wo er ist** | `State.cpp:318-319` liegt INNERHALB der Sperre und bleibt dort | B15 **NEU** Teilfall von `312/M-35` | **Regressionswache**; einmal gebrochen: den Frühausstieg vor die Sperre ziehen → die Dirty-Zählung ändert sich → rot | `CLAUDE.md:201-203`; R-312-2 | 4 |
| M-38 | W09 · Grundgesetz | Sonde mit Schleuse, Gen mit Lebendprüfung | 4000 Blöcke `processBlock` mit laufenden v3-Clients | **Weder Schleuse noch Lebendprüfung liegen im Audiopfad: `RtWache::sperren()` = 0 und `RtWache::allokationen()` = 0 — dieser Allokationszähler ist in B7 seit Etappe 2 messbar (Bindung von B7s `operator new`, §7.2, E-312-11); der Callbackpfad der Schleuse arbeitet nur mit Atomics** | `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:30-37`: `warteMutex` gehört ausschließlich dem Schließpfad | B7 bestehend (`EqCopTransactionTest`), Zyklus um laufende v3-Clients ergänzt; Messung wie M-01 | **zwei Hälften.** Zählerhälfte: **Regressionswache**; einmal gebrochen an der Zeile, die die Zusage trägt: den Provider `v3Status()` aus `processBlock` rufen → seine Zeichenketten (`SondeProcessor.cpp:1145`, `:1172`, beide `toStdString()`) allokieren im Bereich → `RtWache::allokationen()` steigt → rot. Diese Zeile behauptet dabei **keinen** Sperrenanstieg: die drei Sperren auf dem Weg `v3Status()` — `zustandSchloss` (`:1139`), `getCallbackLock()` (`:1184`), `hostKontextSchloss` (`:1192`) — sind `juce::ScopedLock` und damit für `RtWache::sperren()` unsichtbar, das allein über `RtWache::GemeldeteSperre` steigt (`eq-copilot/plugin/dsp/DspRtWache.h:50-53`, `:84`); ebenso wenig fällt sie über `betreten()`, das auf Clientthreads läuft (`SondeProcessor.cpp:139-142`), nie in `processBlock` (Wache zählt nur bei Tiefe größer 0 des EIGENEN Threads, `DspRtWache.h:36-38`). Schleusenhälfte: **ausdrücklich ohne eigene Mutation** (E-312-11 Punkt 3), weil eine solche einen Haken in `Schleuse.h:121-139` bräuchte, den E-312-10 ausschließt; sie bleibt eine messende Zusage. Berichtigt in der Nacharbeit 2 (D-B): der frühere Satz „beide Zähler steigen" behauptete einen Sperrenzähler, den `juce::ScopedLock` nicht bewegt | `CLAUDE.md:29-33`; SONDE-015 M-47; E-312-11; E-312-10; H3 der Matrixprüfung 1 | 4 |
| M-39 | W09 · Zahlenrand Frist 2 s · E-312-10 · R-312-12 | wie M-30, in drei Teilfällen mit ausdrücklicher Hakenlage; die Marke sitzt stets an der Zugriffsstelle | **(1)** Schranke HINTER `betreten()`, Freigabe sobald `stand().geschlossen` wahr ist. **(2)** Schranke VOR `betreten()`, Freigabe nach Rückkehr des Destruktors (Join des zerstörenden Threads). **(3)** Freigabe vor Beginn der Zerstörung | **Geordnet wird über beobachtbare Schleusenzustände, nie über die Wanduhr: in allen drei Fällen ist `nachEndeErreicht` = 0, und der Destruktor kehrt nie zurück, während `stand().aktiv` größer 0 ist. Zulässig sind (`abgewiesen`, `gewartetMs`) = (0, ≥ 1) in Fall 1, (≥ 1, 0) in Fall 2 und (0, 0) in Fall 3 — auch (0, 0) ist korrektes Verhalten. Verboten ist allein eine Marke größer 0 oder ein Destruktor, der bei laufendem Rückruf zurückkehrt** | `kStopFristMs` = 2000 (`eq-copilot/plugin/core/ipc/controlclient/Intern.h:59`, `eq-copilot/plugin/core/ipc/TelemetryClient.cpp:30`) ist die Ablösefrist von `stop()` (`eq-copilot/plugin/core/ipc/ControlClient.cpp:121-133`), KEINE Verhaltensgrenze der Schleuse: weder `betreten` (`Schleuse.h:121-139`) noch `schliessen` (`:145-165`) hängen an ihr. Die Frist ist durch Fall 1 und Fall 2 auf beiden Seiten gedeckt | neues Sonde-Lebenslaufbein **NEU** `312/M-39 schleuse_ist_ueber_zustaende_geordnet`, Teilfälle von `312/M-30` | **heute nicht messbar** (keine Schleuse). Nach dem Bau, an der Zeile, die die Zusage trägt: `schliessen()` im Destruktor entfernen → Fall 1 kehrt zurück, bevor der Rückruf fertig ist, Fall 2 weist ihn nicht ab; beide erreichen danach die Marke → `nachEndeErreicht` = 1 → rot, UB-frei (R-312-12). Berichtigt in der Nacharbeit 1: die frühere Mutation „zweites Lesen in `betreten()` entfernen" entfällt — sie fiele nur in einem Fenster von wenigen Befehlen und bräuchte einen Haken in `Schleuse.h`, den §7.4 ausdrücklich nicht anfasst | R-D2; E-312-10; `Schleuse.h:172-181` | 4 |

### 6.5 Etappe 5, erster Änderungssatz — Offline-Übergang (T3-01-03, T3-01-04; R-312-3)

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-40 | T3-01-03 · R-312-3 | echter `SondeProcessor` mit gesetzter Vorschau und Hörmatrix Candidate; das Bein öffnet den Bereich wie der Wrapper (M-04) | 200 Blöcke, vor jedem `setNonRealtime (true)` | **`RtWache::sperren()` = 0 über die 200 Blöcke: das Offlineflag nimmt in keinem Block eine Sperre im Callback** | Wechselerkennung per Atomic vor jeder Sperre; `setNonRealtime` kehrt bei gleichem Wert sofort zurück | B7 **NEU** `312/M-40 offlineflag_nimmt_keine_sperre` neben dem [SONDE-015] M-120-Block (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2216-2234`) | **heute rot** — Gegenprobe: `SondeProcessor.cpp:1325` nimmt `zustandSchloss` bei JEDEM Aufruf mit `offline == true`; mit dem gemeldeten Wrapper aus M-04 zählt der Zähler 200. Zusagezeile ist `:1325`; Mutation: die Sperre wieder unbedingt machen → rot | SONDE-015 M-47; `CLAUDE.md:29-33`; R-312-3 | 5 |
| M-41 | T3-01-03 · aktivieren↔abklingen · E-312-7 (Hörmatrixhälfte) | wie M-40 | der ERSTE Aufruf mit `offline == true` | **Er stellt die Hörmatrix sofort um: `gewuenschteHoermatrix()` ist unmittelbar nach der Rückkehr Processed, und der erste danach gefahrene Block ist ab Sample 0 bitgleich zum bestätigten Lauf (M-42). Über `previewAktiv()` sagt diese Zeile NICHTS — die Buchhaltung trägt M-83** | `setzeHoermatrix` schreibt ein Atomic (`eq-copilot/plugin/dsp/DspKern.h:244`) und wirkt im selben Callback; dazu ein Offline-Riegel, unter dem jeder Hörwunsch als Processed wirkt, solange er steht | B7 **NEU** Teilfall von `312/M-40` | **Regressionswache** für die Hörmatrixhälfte (sie hält heute); einmal gebrochen: die Wechselerkennung so bauen, dass sie auch den ersten Aufruf schluckt → der Candidate bleibt hörbar → rot. Berichtigt in der Nacharbeit 1: die frühere Zusage „`previewAktiv()` ist spätestens nach dem ersten Block false" widersprach der eigenen Fristspalte und hätte nach Etappe 5 am Workertakt gehangen | SONDE-015 M-120; R-312-3; E-312-7 | 5 |
| M-83 | T3-01-03 · E-312-7 (Buchhaltungshälfte) | wie M-40 | der erste Aufruf mit `offline == true`, danach ein ausdrücklicher `kontrollTaktFuerTest()` | **Die Buchhaltung folgt im nächsten Kontrolltakt, nicht im Block: NACH dem ausdrücklichen Takt ist `previewAktiv()` false, und nach weiteren `ceil (kFadeSamples / Blockgröße) + 1` Blöcken und einem zweiten Takt ist die Candidate-Bank freigegeben. VOR dem ausdrücklichen Takt sagt die Zeile nichts über `previewAktiv()`** | das Atomic „Vorschauende angefordert" setzt der Callback, verbraucht wird es in `dspKontrollTakt` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1447-1478`) unter dem Schloss, das der Takt ohnehin hält; Merkzettel, Poolpublikation und Bankfreigabe bleiben beim Worker (`eq-copilot/plugin/dsp/DspKern.h:157`, `:599-602`; `eq-copilot/plugin/dsp/DspBankPool.h:37-39`, `:154`). Der Worker läuft im Bein mit (`SondeProcessor.cpp:188-189`) und darf früher ziehen; `beendePreview` ist idempotent (`eq-copilot/plugin/state/NakamaTransaktion.cpp:311`), das Ergebnis nach dem Takt damit fest | B7 **NEU** `312/M-83 vorschauende_wird_im_kontrolltakt_verbucht`; im selben Änderungssatz wird der bestehende Fall `offline_render_nutzt_den_bestaetigten_zustand` auf einen ausdrücklichen Takt vor der `previewAktiv()`-Prüfung umgestellt (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2231`, §7.5) | **heute nicht messbar** (das Atomic entsteht im Bau; heute läuft `beendePreview` synchron unter dem Schloss im Callback, `SondeProcessor.cpp:1326`). Nach dem Bau: das Atomic im Kontrolltakt nicht verbrauchen → `previewAktiv()` bleibt auch nach dem ausdrücklichen Takt true → rot | E-312-7; SONDE-015 M-120; `CLAUDE.md:29-33` | 5 |
| M-42 | T3-01-04 · R-312-3 | zwei Prozessoren mit unterschiedlichem bestätigtem und Candidate-Satz, Candidate hörbar eingeschwungen | `setNonRealtime (true)`, danach SOFORT Audio lesen | **Ab Sample 0 tragen die ersten Samples den bestätigten Zustand: `ya` und `yb` sind bitgleich ab Sample 0, und `max abs(ya[i] − yb[i])` über die ersten 256 Samples ist 0** | Hörmatrixwechsel hart am ersten Sample nach dem Flag, kein Fade | B7 bestehend `offline_render_nutzt_den_bestaetigten_zustand` (`TransactionTestMain.cpp:2216-2234`), die Verwurfszeile `:2228` gestrichen; die `previewAktiv()`-Prüfung `:2231` wandert hinter einen ausdrücklichen `kontrollTaktFuerTest()` und gehört damit zu M-83, nicht mehr zu diesem Fall (§7.5) | **heute rot** — Gegenprobe: `eq-copilot/plugin/dsp/DspKern.cpp:1239` setzt `hoerFadeRest = kFadeSamples` (256, `eq-copilot/plugin/dsp/DspProgramm.h:87`), und bei `i == 0` ist das Gewicht der Vorschau exakt 1 (`DspKern.cpp:1349`). Zusagezeile `:1239`; Mutation: dort wieder `kFadeSamples` statt 0 im Offlinefall → rot | SONDE-015 M-120; Entwurf `:2137`, `:2748`; R-312-3 | 5 |
| M-43 | T3-01-04 · Gegenfall Rückweg | wie M-42, danach Offline aus | `setNonRealtime (false)` und Rückkehr zu Candidate | **Der Rückweg blendet weiterhin WEICH: der Nachbarsprung am Umschaltsample liegt innerhalb der E-31-Schranke; die harte Umstellung gilt nur für die Offlinerichtung** | das harte Schalten ist ein eigenes Atomic, das nur beim Wechsel nach offline gesetzt wird | B7 **NEU** Teilfall von `312/M-42` | **Regressionswache**; einmal gebrochen: das harte Schalten unbedingt machen → jeder Hörmatrixwechsel wird ein Schnitt → M-55 von SONDE-015 (`docs/beweise/SONDE-015.md:1114`) rot | SONDE-015 E-31 (`docs/beweise/SONDE-015.md:3425`); SONDE-015 M-55 (`docs/beweise/SONDE-015.md:1114`) | 5 |
| M-44 | T3-01-04 · Offline hin und zurück | Prozessor mit Vorschau, Offline an, Offline aus | zweiter Wechsel nach offline | **`setNonRealtime (false)` belebt die Vorschau NICHT wieder; ein zweiter Wechsel nach offline findet keine Vorschau mehr vor und ändert am Ausgang nichts** | Vorschauende ist ein einmaliger Übergang, kein Zustand, der zurückkippt | B7 **NEU** Teilfall von `312/M-42` | **Regressionswache** (hält heute); einmal gebrochen: die Wechselerkennung beim Rückweg zurücksetzen und die Vorschau neu setzen → rot | SONDE-015 M-120; `CLAUDE.md:84-86` | 5 |
| M-45 | T3-01-04 · Gegenfall ohne Vorschau | derselbe Ablauf ohne aktive Vorschau | `setNonRealtime (true)` | **Ab Sample 0 bitgleich — das gilt schon heute und grenzt die Regel ein** | — | B7 **NEU** Teilfall von `312/M-42` | **Regressionswache**; einmal gebrochen: das harte Schalten auch ohne laufenden Fade einen Mischstand stehen lassen → rot | SONDE-015 M-120 | 5 |
| M-46 | T3-01-04 · öffnen↔schließen · E-312-7 | wie M-42 | nach dem Wechsel ein ausdrücklicher `kontrollTaktFuerTest()`, dann `ceil (kFadeSamples / Blockgröße) + 1` Blöcke, dann ein zweiter Takt | **Es bleibt keine Bank hängen: nach dieser Folge ist die Candidate-Bank wieder frei, und der Hörhalt ist beendet, auch wenn nie ein Fade lief. Die Freigabe hängt am Takt, nicht am Block — gemessen wird sie deshalb NACH einem ausdrücklichen Takt, nie an einer Blockzahl allein** | `beendeHoerHalt` (`eq-copilot/plugin/dsp/DspKern.cpp:500-517`) hängt an `hoermatrixMischtCandidate()` (`:494-498`), das bei `hoerFadeRest == 0` sofort falsch wird, und läuft am Ende jedes Stücks (`:1389`); die Bank gibt der Worker frei — `pflege()` im Kontrolltakt (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1452`), nachdem `beendePreview` → `beendeCandidate` (`eq-copilot/plugin/state/NakamaTransaktion.cpp:762`) die ENDE-Marke gesetzt hat | B7 **NEU** Teilfall von `312/M-42`, gemeinsam mit `312/M-83` gefahren | **heute nicht messbar** (die taktgebundene Freigabe entsteht im Bau). Nach dem Bau, an der Zeile, die die Zusage trägt: das Atomic „Vorschauende angefordert" im Kontrolltakt nicht verbrauchen → die ENDE-Marke kommt nie, die Candidate-Bank bleibt belegt → rot. Berichtigt in der Nacharbeit 1: die frühere Mutation „`beendeCandidate` aus dem Callbackpfad entfernen" kann nicht fallen, weil `beendeCandidate` nach E-312-7 gar nicht im Callbackpfad steht | SONDE-015 E-33; R-312-3; E-312-7 | 5 |
| M-47 | T3-01-04 · Zahlenrand Blockgröße 1 | wie M-42, Blockgrößen 1, 64 und 256 | `setNonRealtime (true)` | **Bei jeder Blockgröße ist das ERSTE Sample nach dem Flag bestätigt und das LETZTE Sample davor unverändert; bei Blockgröße 1 gilt das für genau ein Sample** | Sampleindex, nicht Blockindex | B7 **NEU** Teilfall von `312/M-42` | **heute rot** bei jeder Blockgröße (der Fade ist blockgrößenunabhängig 256 Samples lang). Mutation wie M-42 | SONDE-015 M-120; `CLAUDE.md` Zahlenränder | 5 |

### 6.6 Etappe 5, zweiter Änderungssatz — Parameteranbindung (T3-01-02; R-312-4)

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-48 | T3-01-02 · R-312-4 · Identität | Sonde am Basis-SHA der Etappe und am Endstand | Parameterliste auslesen | **Zahl, Reihenfolge, IDs, Bereiche und Defaults der Hostparameter sind gegenüber einem am Basis-SHA der Etappe erzeugten Golden bytegleich; die Hostsicht (`convertTo0to1` und `convertFrom0to1`) liefert an je drei Stützstellen je Parameter denselben float** | Golden entsteht über den unveränderten Prozessor am Basis-SHA, nie von Hand | B7 bestehend (112 Host-Parameter in Vertragsreihenfolge, `tools/beweise.ps1:728`), um Bereiche, Defaults und die Hostsicht erweitert | **Regressionswache**; einmal gebrochen: einen Parameter in der Layoutliste verschieben → Golden weicht ab → rot. Ist die Bytegleichheit nicht herstellbar, gilt der begründete Verzicht nach R-312-4 und der Umbau entfällt (§7.5) | R-312-4; `CLAUDE.md:204-205` | 5 |
| M-49 | T3-01-02 · R-312-4 · State | Stand mit belegten Slots und laufender Automation | speichern, laden, speichern | **Die Statebytes sind über den Umbau hinweg bytegleich zu denen des Basis-SHA, und eine Automationsfahrt erzeugt dieselbe Epochenfolge wie vorher** | `getStateInformation` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1024-1028`) unverändert | B2 und B7 bestehend, um den Vergleich gegen das Golden des Basis-SHA ergänzt | **Regressionswache**; einmal gebrochen: die Parameter ohne den bisherigen Namensraum anlegen → State weicht ab → rot | R-312-4; `CLAUDE.md:201-203` | 5 |
| M-50 | T3-01-02 · Strukturbeweis | Quelltext unter `eq-copilot/plugin/sonde/` am Endstand der Etappe | Textriegel über das Verzeichnis | **Kein Quelltext unter `eq-copilot/plugin/sonde/` nennt `AudioProcessorValueTreeState`: die zweite Sperrfamilie (der APVTS-Adapter) existiert nicht mehr; fail-closed, weniger Dateien als der Bestand ist rot** | Muster `keine_sperre_am_wrapper_vorbei_im_kern` (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:6629-6660`) | B7 **NEU** `312/M-50 kein_apvts_mehr_in_der_sonde` | **heute rot** — Gegenprobe: `SondeProcessor.cpp:143` legt `parameterBaum (*this, nullptr, "NakamaProbeeqParameter", baueParameterLayout())` an. Zusagezeile ist der entfernte APVTS; Mutation: ihn wieder einführen → Riegel rot | R-312-4; Teil A §3g | 5 |
| M-51 | T3-01-02 · speichern↔laden | Stand mit +3 dB, in dieselbe Instanz geladen | `setStateInformation` | **`hostParameterAbgleichen` spiegelt den geladenen Stand weiter zum Host: nach dem Laden trägt jeder Hostparameter den Wert des geladenen Standes in Hostgenauigkeit** | Reihenfolge nach SONDE-015 M-84: State lesen, validieren, Committed setzen, Hostparameter synchronisieren, erster Block (`SondeProcessor.cpp:1100-1103`) | B7 bestehend `reload_rekonstruiert_denselben_audioausgang`, um den Hostparametervergleich ergänzt | **Regressionswache**; einmal gebrochen: `hostParameterAbgleichen` aus dem Ladeweg entfernen → der Host zeigt den alten Stand → rot | SONDE-015 M-84; R-312-4 | 5 |
| M-52 | T3-01-02 · verbinden↔trennen | Sonde konstruiert und zerstört | Konstruktor und Destruktor | **`addListener` und `removeListener` bleiben paarweise: nach dem Destruktor hält kein Parameter mehr einen Listener auf den Prozessor; die Zahl der angemeldeten Listener ist vor der Konstruktion und nach der Zerstörung gleich** | Anmeldung `SondeProcessor.cpp:157-164`, Abmeldung im Destruktor `:221-222` | B7 **NEU** Teilfall von `312/M-50` | **Regressionswache**; einmal gebrochen: die Abmeldeschleife im Destruktor entfernen → ein Parameter hält einen toten Listener → rot | `CLAUDE.md:84-86`; R-312-4 | 5 |
| M-53 | T3-01-02 · Grundgesetz und Grenze | echter `SondeProcessor`, Bereich ab Callback-Eintritt (M-01) | 4000 Blöcke mit laufender Hostautomation aus einem zweiten Thread | **`RtWache::sperren()` = 0 und `RtWache::allokationen()` = 0 — dieser Allokationszähler ist in B7 seit Etappe 2 messbar (Bindung von B7s `operator new`, §7.2, E-312-11). Die Zeile sagt ausdrücklich: gezählt wird, was der Prozessor ab `processBlock` tut; die Sperre, die JUCE vor dem Plugincode nimmt (`AudioProcessorParameter::sendValueChangedMessageToListeners`), sieht der Zähler bauartbedingt nicht — sie bleibt als datierte Registerzeile** | Bereich ab `SondeProcessor.cpp:371`; die JUCE-Sperre liegt davor | B7 bestehend, Behauptungszeile um die Grenze ergänzt; Registerzeile beim Abschluss in `docs/offene-punkte.md` | **Regressionswache**; einmal gebrochen: im Parameterweg des Prozessors eine gemeldete Sperre nehmen → Zähler steigt → rot | R-312-4 (letzter Satz); SONDE-015 M-47; E-312-11; `CLAUDE.md:29-33` | 5 |
| M-54 | T3-01-02 · Zahlenrand Parameterindex | Sonde mit 112 Hostparametern | `parameterValueChanged` mit Index −1, 0, 111 und 112 | **Nur 0 bis 111 erreichen die Mailbox; −1 und 112 kehren ohne Wirkung zurück, und `abgleichTiefe > 0` unterdrückt die eigene Rückmeldung** | Wache `SondeProcessor.cpp:1333`, thread-lokaler Zähler `:76-82` | B7 **NEU** Teilfall von `312/M-50` | **Regressionswache**; einmal gebrochen: die Indexwache entfernen → Zugriff außerhalb des Feldes → rot | `CLAUDE.md` Zahlenränder; R-312-4 | 5 |

### 6.7 Etappe 6 — Gen-Fläche und Hörmarkierung (T3-07-02, T3-01-10 sicherer Teil, T3-14-01, T3-07-05 Teil a)

Gemeinsamer Aufbau, wo nicht anders genannt: echter `EqCopilotProcessor` auf
dem Heap (MSVC-Stack, NAK-175), laufender Transport, Editor offen. Die Fälle
messen **tatsächlichen Ausgang und UI-Zustand gemeinsam**, nie nur einen von
beiden.

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-55 | T3-07-02 · R-312-8 in der Fassung von E-312-8 · Szenario S1 „zu Main" | Gen auf dem Heap, `prepareToPlay`, `testForciereEchtzeit (true)`, laufender Transport über die Brücke, `setzeEditorOffen (true)` (Muster `eq-copilot/plugin/tests/MarkierungTestMain.cpp:451-458`); Rolle `sensor`, Marker SOLO oder PULS über `markierungEinreichen` eingereicht, 40 Blöcke gefahren (Ausgang bitgleich, Ring leer) | `setzeBindung ("hub", …)`, SOFORT danach 40 Blöcke, ohne Timertick | **In der Richtung zu Main entsteht nichts: der Ausgang ist über alle 40 Blöcke nach dem Rollenwechsel bitgleich zum Eingang (0 abweichende Samples), und im Interventionsring steht weder ein `begin` noch ein `end`. Der in Legacy eingereichte Auftrag ist zurückgenommen, bevor `istMainKlassifiziert` kippt** | Rücknahme (`markierung.reicheAus()`, `eq-copilot/plugin/src/HoerMarkierung.h:411-414`) synchron auf dem Nachrichtenthread VOR dem Store in `spiegleKlassifikation` (`eq-copilot/plugin/src/prozessor/State.cpp:272`); der Audiothread liest `istMainKlassifiziert` dafür mit `acquire` statt `relaxed` (`eq-copilot/plugin/src/PluginProcessor.cpp:874`), damit ein Block, der die neue Klassifikation sieht, auch die Rücknahme sieht (Briefkasten `acq_rel`, `HoerMarkierung.h:360-361`) | A3 **NEU** `312/M-55 rollenwechsel_zu_main_entwaffnet_den_marker` neben `eq-copilot/plugin/tests/MarkierungTestMain.cpp:179-184` | **heute rot** — Gegenprobe: in Legacy ist `istMainKlassifiziert` false (`State.cpp:272`, `eq-copilot/plugin/state/NakamaLebenslauf.h:137`), damit `erlaubt` false (`PluginProcessor.cpp:874-881`) und `zielAn` false (`HoerMarkierung.h:493-499`); `setzeBindung` schärft synchron (`State.cpp:343-344`), der Marker blendet ab dem nächsten Block ein (`HoerMarkierung.h:586-596`) und meldet `begin` (`:619-623`). Zusagezeile ist die neue Rücknahme vor dem Store; Mutation: sie entfernen → Einblenden ab dem ersten Block, ein `begin` im Ring → rot. Berichtigt in der Nacharbeit 1: die frühere Zusage verlangte „genau EIN `end` zu dem offenen `begin`" in einem Szenario, in dem nie ein `begin` entsteht — beide Hälften schlossen einander aus; und der Fix im Timertick ließ ein Fenster von bis zu 33,3 ms offen (`PluginEditor.cpp:230`) | R-312-8 in der Fassung von E-312-8 (§5.9); `CLAUDE.md:51-52` | 6 |
| M-56 | T3-07-02 · drei Netze | Marker in der MAIN-Fläche aktiv | (a) Kill-/Freilaufmeldung, (b) Sampleratenwechsel, (c) 10 Minuten ohne Bedienung | **Alle drei Netze greifen auch in der Main-Fläche: die Markierung endet in jedem der drei Fälle, der Ausgang wird bitgleich zum Eingang, und der Grund steht im Status** | der Block `PluginEditor.cpp:270-278` liegt vor der Abzweigung `:245`, nicht hinter dem Rücksprung `:257` | A3 **NEU** `312/M-56 die_drei_netze_ticken_in_jeder_flaeche` | **heute rot** — Gegenprobe: `timerCallback` kehrt bei `:257` zurück, bevor `:270-278` erreicht ist. Zusagezeile ist `:257`; Mutation: das `return` wieder vor den Block ziehen → rot | R-312-8; irreführender Kommentar `PluginEditor.cpp:268-269` | 6 |
| M-57 | T3-07-02 · Rückwechsel · E-312-8 | Marker in Legacy eingereicht, Rollenwechsel nach `hub` (M-55), dann zurück nach `sensor` | Rückwechsel, danach 40 Blöcke | **Nach dem Rückwechsel ist kein Marker scharf, und kein Bedienelement behauptet das Gegenteil: `markModus` ist aus, `markierungAusKnopf` ist deshalb NICHT sichtbar, der Ausgang bleibt über alle 40 Blöcke bitgleich zum Eingang, und es entsteht kein neues `begin`. Erst ein neuer Handgriff macht wieder etwas wirksam** | `markierungAusKnopf.setVisible` (`eq-copilot/plugin/src/PluginEditor.cpp:279`) folgt `markModus`; nach der Rücknahme in `spiegleKlassifikation` ist `markModus` aus, der Knopf verschwindet und keine Fläche zeigt einen Handgriff ohne Wirkung | A3 **NEU** Teilfall von `312/M-55` | **heute rot** — heute überlebt der Auftrag beide Wechsel (`ausGewuenscht` setzt nur ein ausdrücklicher Aus-Auftrag, `eq-copilot/plugin/src/HoerMarkierung.h:484-491`) und wird beim zweiten Wechsel zu Main ohne Handgriff wieder hörbar. Mutation wie M-55. Berichtigt in der Nacharbeit 1: die frühere Zusage „der Aus-Knopf ist wieder sichtbar, trägt aber keinen scharfen Auftrag" hätte selbst ein totes Element verlangt (`CLAUDE.md:51-52`) | R-312-8 in der Fassung von E-312-8; `CLAUDE.md:51-52` | 6 |
| M-84 | T3-07-02 · L3 · E-312-8 · Szenario S2 „weg von Main" | Aufbau wie M-55, aber Rolle zuerst `hub`: `setzeBindung ("hub", …)`, dann `markierungEinreichen`, dann Blöcke fahren, bis `markierungHoerbar()` wahr ist (Muster `eq-copilot/plugin/tests/MarkierungTestMain.cpp:470-478`) — hier entsteht ein `begin` wirklich | `setzeBindung ("sensor", …)`, danach Blöcke über die volle Fadelänge, danach `setzeBindung ("hub", …)` und 40 weitere Blöcke | **Genau ein `begin` und genau ein `end` dazu, mit der gezählten Hördauer; der Ausfade läuft WEICH zu Ende (der Nachbarsprung am Umschaltsample liegt innerhalb der E-31-Schranke, kein Schnitt), und nach dem zweiten Wechsel zu Main entsteht ohne neuen Handgriff KEIN neues `begin`, der Ausgang bleibt über 40 Blöcke bitgleich zum Eingang** | Rücknahme im Klassifikationswechsel vor dem Store (`eq-copilot/plugin/src/prozessor/State.cpp:272`), danach fällt `erlaubt` (`eq-copilot/plugin/src/PluginProcessor.cpp:874-881`), `zielAn` wird false (`HoerMarkierung.h:493-499`), die vorhandene Rampe läuft zu Ende (NAK-47, `HoerMarkierung.h:420-429`) und das `end` entsteht am Fadeende (`:626-641`). Zugesagt ist „Rücknahme vor dem Flächenwechsel", NICHT „Intervallende vor dem Flächenwechsel" | A3 **NEU** `312/M-84 rollenwechsel_weg_von_main_schliesst_das_intervall` neben `312/M-55` | **zwei Hälften.** `begin`/`end`-Paar und weicher Ausfade: **Regressionswache** (halten heute); einmal gebrochen: die Rücknahme als `hartAus()` statt als Aus-Auftrag bauen → der Ausfade wird ein Schnitt → SONDE-015 M-55 (`docs/beweise/SONDE-015.md:1114`) rot. Zweiter Wechsel ohne neues `begin`: **heute rot** (der Auftrag bleibt heute scharf, `HoerMarkierung.h:484-491`); Zusagezeile ist die Rücknahme vor dem Store, Mutation: sie entfernen → nach dem zweiten Wechsel zu Main ein neues `begin` → rot. Neu in der Nacharbeit 1: das Pflichtgegenbeispiel „Solo oder Pulse aktiv beim Rollenwechsel weg von Main" hatte bisher keine Zeile (L3) | R-312-8 in der Fassung von E-312-8 (§5.9); SONDE-015 E-31 (`docs/beweise/SONDE-015.md:3425`); `CLAUDE.md:84-86` | 6 |
| M-58 | T3-07-02 · ehrliche Meldung | Gen in Legacy-Rolle, Editor offen | SOLO auslösen | **Es wird nichts eingereicht, und der Statustext behauptet nicht „nur dieser Bereich spielt": er nennt die Lage, die wirklich eintritt** | Klassifikationsabfrage VOR `processor.markierungEinreichen` (`PluginEditor.cpp:800`) | A3 **NEU** Teilfall von `312/M-55`; die Statusprüfung liest den Editorzustand, nicht nur den Prozessor | **heute rot** — Gegenprobe: `schalteMarkierung` (`PluginEditor.cpp:754-815`) fragt die Klassifikation nicht, und `:807-812` meldet Engagement. Zusagezeile ist die neue Abfrage vor `:800`; Mutation: sie entfernen → rot | `CLAUDE.md:51-52`; R-312-8 | 6 |
| M-59 | T3-01-10 · R-312-5 · starten↔stoppen · H1 | Gen mit `prepareToPlay`, laufendem Transport und eingereichtem hörbarem Auftrag, Blöcke gefahren bis `markierungHoerbar()` wahr ist; **danach wird der Transport angehalten** — das ist die Lage, in der ein Host `reset()` ruft, und sie steht hier ausdrücklich, weil mit laufendem Transport schon der erste Folgeblock wieder erlaubt wäre (M-63) | `reset()` ohne neues `prepareToPlay`, danach 40 Blöcke bei angehaltenem Transport | **Der Klang endet und das Intervall schließt: alle 40 Blöcke nach `reset()` sind bitgleich zum Eingang, und im Interventionsring steht genau EIN `end` zu dem offenen `begin`, mit `project_sample_end` null und der gezählten Hördauer; ein zweites `end` entsteht nicht (M-86)** | derselbe Abschlussweg wie `prepareToPlay` (`eq-copilot/plugin/src/prozessor/Hostbruecke.cpp:97-142`), aufgerufen aus der neuen `reset`-Override | A3 **NEU** `312/M-59 reset_beendet_klang_und_intervall` neben dem [SONDE-013] M-36-Fall (`eq-copilot/plugin/tests/MarkierungTestMain.cpp:449-547`) | **heute rot** — Gegenprobe: `eq-copilot/plugin/src/PluginProcessor.h:111-114` hat keine `reset`-Override, gerufen wird die leere JUCE-Basis. Die Zusagezeile ist die neue Override; vor dem Bau fällt der Rotbeweis an der fehlenden Zeile, gemessen an der Wirkung | R-312-5; `CLAUDE.md:84-86`; `Hostbruecke.cpp:88-96` | 6 |
| M-60 | T3-01-10 · R-312-5 · Grundgesetz | wie M-59, Allokationszähler des Beins scharf | `reset()` | **`reset()` allokiert nicht: der Allokationszähler steigt über den Aufruf um 0; weder `wet.calloc` noch `versuchTrocken.assign` laufen** | `vorbereiten` allokiert (`eq-copilot/plugin/src/HoerMarkierung.h:305-306`), der neue Abbruchpfad nicht | A3 **NEU** Teilfall von `312/M-59` | **heute nicht messbar** (es gibt keinen `reset`-Pfad). Nach dem Bau: in der Override `markierung.vorbereiten (maxBlock)` statt des allokationsfreien Abbruchpfads rufen → Zähler steigt → rot | R-312-5; `CLAUDE.md:29-33` | 6 |
| M-61 | T3-01-10 · Oversize-Riegel | Marker aktiv, ein Block über `wetKapazitaet` hat den Riegel gesetzt | `reset()`, danach Blöcke, danach `prepareToPlay` | **Der Riegel bleibt über `reset()` gesetzt und löst sich erst beim nächsten `prepareToPlay`** | `eq-copilot/plugin/src/HoerMarkierung.h:308-312` und `:444` | A3 bestehend [SONDE-013] „M-36: nach prepareToPlay ist sie wieder moeglich" (`eq-copilot/plugin/tests/MarkierungTestMain.cpp:529-546`), um den `reset`-Weg ergänzt | **Regressionswache**; einmal gebrochen: in der neuen Override `oversizeRiegel = false` setzen → der Riegel löst sich zu früh → rot | SONDE-013 M-36 (`docs/beweise/SONDE-013.md:521`); R-312-5 | 6 |
| M-62 | T3-01-10 · öffnen↔schließen · fail-closed | zwei offene Intervalle erzwungen (Marker aktiv, ein Übergang wartet bereits) | `reset()` | **Der Überlaufpfad bleibt fail-closed: `interventionsRingUeberlauf` wird true, und es wird KEINE Sequenznummer gezogen, die nicht reist** | `Hostbruecke.cpp:104-115`: die Sequenz wird nur gezogen, wenn sie auch reist | A3 **NEU** Teilfall von `312/M-59` | **Regressionswache** (der Pfad hält heute für `prepareToPlay`); einmal gebrochen: in der Override die Sequenz unbedingt ziehen → eine Lücke beim Broker → rot | R-312-5; Entwurf §34.2 | 6 |
| M-63 | T3-01-10 · Auftrag bleibt · H1 | wie M-59, aber der Transport läuft über `reset()` hinweg WEITER | `reset()`, danach Blöcke mit erlaubter Lage | **Der Auftrag bleibt eingereicht und beginnt beim nächsten erlaubten Block neu: es entsteht ein NEUES `begin` — kein zweites `end` zum alten Intervall —, und `markierungHoerbar()` wird wieder wahr. Das ist derselbe Zustand, den `prepareToPlay` heute herstellt; ob das Anhalten den Auftrag beendet, ist mit U56 am 21.09.2026 beantwortet („Auftrag bleibt bestehen") und wird hier nicht zusätzlich gebaut** | `reicheEin` und `briefkasten` bleiben unberührt (`eq-copilot/plugin/src/HoerMarkierung.h:472`); das neue `begin` entsteht regulär über `:619-623`, weil der Abbruchpfad `warHoerbar` zurückgesetzt hat | A3 **NEU** Teilfall von `312/M-59` | **Regressionswache**; einmal gebrochen: in der Override `ausGewuenscht` setzen → der Auftrag ist weg, und U56 wäre entgegen der Antwort vorentschieden → rot. Berichtigt in der Nacharbeit 1: M-59 und M-63 trugen dieselbe Vorbedingung „laufender Transport" und schlossen einander damit aus — M-59 verlangte Bitgleichheit im ersten Folgeblock, M-63 den Neubeginn genau dort | R-312-5 (Satz 4 und 5); Karte U56 (§10.2) | 6 |
| M-86 | T3-01-10 · R-312-5 · H1 · begin↔end über die ganze Folge | wie M-59; der Interventionsring wird über die ganze Folge gelesen und jedes Ereignis gezählt | `reset()`, danach 40 Blöcke OHNE Erlaubnis (Transport angehalten), danach 40 Blöcke MIT Erlaubnis, danach `prepareToPlay (48000, 256)` | **Über die ganze Folge hat jedes `begin` genau ein `end`, und kein gefärbter Block liegt ohne offenes `begin`: das `end` aus `reset()` wird in den Blöcken ohne Erlaubnis NICHT ein zweites Mal gemeldet; der erste Block mit Erlaubnis meldet ein neues `begin`, bevor er färbt; das abschließende `prepareToPlay` schließt genau dieses eine offene Intervall. Ereigniszahl am Ende: zwei `begin` und zwei `end`** | der Abbruchpfad der `reset`-Override setzt `warHoerbar` und `hoerbareSamples` zurück wie `vorbereiten` (`eq-copilot/plugin/src/HoerMarkierung.h:313-314`) und `setzeSamplerate` (`:678-679`); bleibt `warHoerbar` stehen, bildet schon der nächste Block ohne Erlaubnis ein zweites `end` (`:626-641`), und ein Block mit Erlaubnis blendet ein, ohne `begin` zu melden (`:619` verlangt `! warHoerbar`) | A3 **NEU** `312/M-86 jedes_begin_hat_genau_ein_end_ueber_reset` neben `312/M-59` | **heute nicht messbar** (es gibt keine `reset`-Override, `eq-copilot/plugin/src/PluginProcessor.h:111-114`). Nach dem Bau, an der Zeile, die die Zusage trägt: im Abbruchpfad `warHoerbar` nicht zurücksetzen → der nächste Block ohne Erlaubnis meldet ein zweites `end`, und der erste Block mit Erlaubnis färbt ohne `begin` → rot | R-312-5 („genau ein `end` je `begin`"); `eq-copilot/plugin/src/PluginProcessor.cpp:978-979`; H1 der Matrixprüfung 1 | 6 |
| M-64 | T3-01-10 · releaseResources und prepareToPlay | wie M-59 | (a) `releaseResources()` statt `reset()`, (b) `prepareToPlay` statt `reset()` | **(a) erzeugt dasselbe `end` wie `reset()` und gibt zusätzlich die Blockpuffer frei; (b) erzeugt dasselbe `end` wie heute — der Fix verwendet den vorhandenen Weg wieder, statt einen zweiten zu bauen** | dasselbe Trio wie bei Probeeq (`eq-copilot/plugin/sonde/SondeProcessor.cpp:299-343`) | A3 **NEU** Teilfälle von `312/M-59` | **zwei Hälften.** (a) **heute rot** (`eq-copilot/plugin/src/PluginProcessor.h:112` ist ein leerer Rumpf); (b) **Regressionswache**, hält heute. Mutation: in der Override einen zweiten, eigenen Abschlussweg bauen → (b) und (a) liefern verschiedene Ereignisse → rot | R-312-5; `CLAUDE.md:84-86` | 6 |
| M-65 | T3-01-10 · Gegenfall ohne Markierung | Gen ohne eingereichten Auftrag | `reset()` und `releaseResources()` | **Es entsteht KEIN Ereignis, die Statebytes bleiben bytegleich, und es wird kein Host-Dirty gemeldet** | `markerAbgebrochen` bleibt false (`Hostbruecke.cpp:76`) | A3 **NEU** Teilfall von `312/M-59` | **Regressionswache**; einmal gebrochen: in der Override das `end` unbedingt bilden → jedes Stop erzeugt ein Ereignis ohne `begin` → rot | R-312-5; `CLAUDE.md:201-203` | 6 |
| M-66 | T3-14-01 · R-312-9 | führender Main, zwei bestätigte Quellen A und B mit VERSCHIEDENEN Labels, Hauptziel A, Feld fokussiert und geändert | B in der gezeichneten Liste anklicken (`eq-copilot/plugin/src/PluginEditor.cpp:984`), einen Timertick fahren, dann Enter | **Bs Label ist unverändert, As Label trägt den Entwurf, und die Zahl der Host-Dirty-Meldungen ist gleich der Zahl der wirklich geänderten Namen (hier 1)** | Entwurfskennung wird gesetzt, wo der Text geladen wird (`PluginEditor.cpp:934-936`), und beim Commit mit dem Aktionsziel verglichen (`:878`) | B15 **NEU** `312/M-66 labelentwurf_gehoert_seiner_startquelle`, alternativ ein Editorfall der B13/B14-Familie | **heute rot** — Gegenprobe: `:878` nimmt das Ziel B, `:885` den Text von A, und der No-op-Riegel `eq-copilot/plugin/src/prozessor/Ipc.cpp:1222-1223` greift bei verschiedenen Namen nicht. Zusagezeile ist `PluginEditor.cpp:934` (`! sourcesLabelFeld.hasKeyboardFocus (true)`), nach dem Bau der Kennungsvergleich; Mutation: den Vergleich entfernen → rot | R-312-9; `CLAUDE.md:51-52` | 6 |
| M-67 | T3-14-01 · leerer Entwurf | A OHNE Label, B MIT Label, sonst wie M-66 | dieselbe Folge | **Bs Name wird NICHT gelöscht: ein leerer Entwurf der Startquelle A schreibt nie auf B** | Kappung `:885` begrenzt nach oben, nicht nach unten; der Vertrag erlaubt den leeren String (`eq-copilot/schemas/state/nakama-state-v2.md:107-108`) | B15 **NEU** Teilfall von `312/M-66` | **heute rot** — heute wird Bs Name gelöscht (Teil B §5e, Präzisierung 2). Mutation wie M-66 | R-312-9; `CLAUDE.md:51-52` | 6 |
| M-68 | T3-14-01 · Escape und Fokusverlust | wie M-66 | (a) Escape statt Enter, (b) Fokusverlust durch Klick außerhalb der Liste | **(a) verwirft den Entwurf ohne Mutation; (b) folgt der am Code abgelesenen heutigen Regel — `onFocusLost` ruft denselben Commit wie `onReturnKey`, also BESTÄTIGEN, angewandt auf die Startquelle** | beide Eingänge sind verdrahtet (`PluginEditor.cpp:209-210`) und müssen dieselbe Regel sehen | B15 **NEU** Teilfälle von `312/M-66` | **zwei Hälften.** (a) **Regressionswache** (Escape schreibt heute nichts); (b) **heute rot** (der Commit landet heute auf dem neuen Ziel). Mutation wie M-66 | R-312-9 (Satz 2); `CLAUDE.md:84-86` | 6 |
| M-69 | T3-14-01 · Startquelle verschwindet | Entwurf für A begonnen | A fällt vor dem Bestätigen weg oder bekommt eine neue Runtime-Nonce | **Der Entwurf verfällt ohne Mutation: weder A noch B werden geschrieben, und es entsteht kein Host-Dirty** | Kennungsvergleich schlägt fehl, bevor `benenneSourcesHauptziel` gerufen wird (`PluginEditor.cpp:888`) | B15 **NEU** Teilfall von `312/M-66` | **heute rot** — heute schreibt der Commit auf das aktuelle Hauptziel, gleich was mit A geschah. Mutation wie M-66 | R-312-9 (letzter Satz) | 6 |
| M-70 | T3-14-01 · Gegenfall unveränderter Text | Hauptziel A, Text unverändert | Enter | **Keine Mutation und kein Host-Dirty** | No-op-Riegel `eq-copilot/plugin/src/prozessor/Ipc.cpp:1222-1223` und der frühe Rückweg `PluginEditor.cpp:886-887` | B15 **NEU** Teilfall von `312/M-66` | **Regressionswache**; einmal gebrochen: den No-op-Riegel entfernen → jede Bestätigung meldet Dirty → rot | `CLAUDE.md:201-203`; R-312-9 | 6 |
| M-71 | T3-14-01 · Zahlenrand Label | Labels mit 0, 120 und 121 Unicode-Codepoints, darunter Zeichen außerhalb der BMP | Bestätigen | **0 und 120 werden angenommen und zweimal gleich beschnitten (`PluginEditor.cpp:198` als Eingabebeschränkung, `:885` als Kappung); 121 wird abgewiesen (`Ipc.cpp:1208`), und der gespeicherte Name bleibt unverändert** | Zählung in Codepoints, nicht in Bytes (Vertrag `nakama-state-v2.md:107-108`) | B15 **NEU** Teilfall von `312/M-66` | **Regressionswache** (die Kappung hält heute); einmal gebrochen: `:885` auf 121 stellen → der Vertrag wird gerissen → rot | Vertrag `nakama-state-v2.md:107-108`; `CLAUDE.md` Zahlenränder | 6 |
| M-72 | T3-14-01 · speichern↔laden | Namen aus M-66 geschrieben | speichern, in eine frische Instanz laden | **Beide Namen kommen unverändert zurück; die Zuordnung Name zu `instance_id` ist dieselbe wie vor dem Speichern** | Publikation `Ipc.cpp:1240-1241`, danach Save | B14 bestehend (`tools/beweise.ps1:745`), um den Fall aus M-66 ergänzt | **Regressionswache**; einmal gebrochen: den Entwurf statt des bestätigten Labels speichern → der geladene Name weicht ab → rot | `CLAUDE.md:84-86`, `:201-203` | 6 |
| M-73 | T3-07-05 · R-312-6 | Main-Fläche bei exakt 760x430 mit 16, 20, 21, 32 und 64 bestätigten Quellen | Ersatz-Hauptziel entsteht über `stelleZielSicher` | **Das Ersatzziel ist immer die erste ANGEZEIGTE Quelle in Anzeigeordnung, also eine der Zeilen, die `sourcesZeilen()` wirklich herstellt — bei 21, 32 und 64 Quellen sind das genau 20** | Ersatzwahl nach derselben Ordnung, in der gezeichnet wird (`eq-copilot/plugin/src/SourcesModel.cpp:1651-1663` gegen `:1772-1777`) | B15 **NEU** `312/M-73 ersatzziel_folgt_der_anzeigeordnung`, Shots `sicht (20)`, `sicht (21)`, `sicht (32)`, `sicht (64)` neben `eq-copilot/plugin/tests/ShotTestMain.cpp:115-118` | **heute rot** — Gegenprobe: `eintraege` ist nach `instanceId` geordnet, die Anzeige nach Mixerindex, dann Name, dann `instanceId`; das Ersatzziel kann jenseits der zwanzigsten Position liegen. Zusagezeile ist `SourcesModel.cpp:1776`; Mutation: dort wieder `eintraege.begin()` nehmen → rot | R-312-6; `CLAUDE.md:51-52` | 6 |
| M-74 | T3-07-05 · scharfer Knopf · E-312-9 · Umordnungsfall | Main-Fläche bei exakt 760x430 mit 25 bestätigten Quellen über `setzeFixtureFuerTest` (`eq-copilot/plugin/src/SourcesModel.cpp:1748`); Hauptziel per `waehleHauptziel` (`:1670`) auf die Quelle in Zeile 5 gesetzt — es EXISTIERT, `stelleZielSicher` greift also nicht (`:1774-1775`) | eine Quelle mit kleinerem Sortierschlüssel tritt bei (oder Quellen davor werden umbenannt), bis das Hauptziel hinter Zeile 20 liegt; danach ein Tick über den Testzugang und den Zustand der Aktionssteuerung lesen | **Kein sichtbarer, scharfer Knopf steht auf einer ungezeichneten Quelle: `sourcesAktionKnopf` und `sourcesLabelFeld` sind nicht sichtbar, `sourcesAktionsZiel` ist leer, das Hauptziel im Modell ist UNVERÄNDERT (kein automatischer Zielwechsel, SONDE-012 U02), und es geht kein Schreibversuch an das Modell** | `aktualisiereSourcesSteuerung` (`eq-copilot/plugin/src/PluginEditor.cpp:902-944`) sucht das Hauptziel nur in `sourcesAnzeige.quellen[0 … sourcesZeilen().size())` — dieselbe Rechnung wie Zeichnen (`:1135`) und Klick (`:980`); bei 25 Quellen und 760x430 sind das genau 20 Zeilen (`:958-959`: `jlimit (18, 34, 366 / 25)` = 18, 366 / 18 = 20, nachgerechnet) | B15 **NEU** `312/M-74 aktionssteuerung_bindet_nur_an_gezeichnete_zeilen`, Teilfall von `312/M-73` | **heute rot** — Gegenprobe: `:905-907` sucht über die GANZE `sourcesAnzeige.quellen`, `:919` und `:933` schalten danach sichtbar; die Sortierung (`SourcesModel.cpp:1651-1664`) ordnet nach Mixerindex, sichtbarem Namen und `instanceId`, und der sichtbare Name folgt bei fehlendem Hostbusnamen dem Labelfeld (`:1781-1789`). Zusagezeile ist die neue, auf die gezeichneten Zeilen beschränkte Suche `:905-907`; Mutation: wieder über die ganze Liste suchen → Knopf und Labelfeld bleiben auf einer ungezeichneten Quelle sichtbar → rot. Berichtigt in der Nacharbeit 1: das frühere Szenario „Hauptziel per `stelleZielSicher` vergeben" heilte schon durch §7.6 und konnte die zweite Hälfte von R-312-6 nicht messen | R-312-6; E-312-9; `CLAUDE.md:51-52`; SONDE-012 U02 (`docs/beweise/SONDE-012.md:231`) | 6 |
| M-85 | T3-07-05 mit T3-14-01 · E-312-9 mit R-312-9 | wie M-74, zusätzlich ein offener Labelentwurf: das Feld trägt für die Startquelle A einen geänderten Text und hat den Tastaturfokus | dieselbe Umordnung, die A hinter Zeile 20 schiebt, danach ein Tick | **Der offene Entwurf wird nach R-312-9 abgeschlossen, BEVOR `sourcesAktionsZiel` geleert wird: As Label trägt den Entwurf, keine fremde Quelle wird geschrieben, und die Zahl der Host-Dirty-Meldungen ist gleich der Zahl der wirklich geänderten Namen (hier 1). Danach sind Knopf und Labelfeld unsichtbar (M-74)** | das Verschwinden des Feldes löst `onFocusLost` aus (`eq-copilot/plugin/src/PluginEditor.cpp:210`), und die heutige Regel dort ist BESTÄTIGEN (`:209-210` rufen beide `uebernehmeSourcesLabel`); der Abschluss muss vor dem Leeren des Ziels laufen, weil `uebernehmeSourcesLabel` bei leerem Ziel still zurückkehrt (`:876-877`) | B15 **NEU** Teilfall von `312/M-74`, gemeinsam mit `312/M-66` gefahren | **heute rot** — heute wird das Ziel in `aktualisiereSourcesSteuerung` (`:908-909`) gesetzt, ohne den offenen Entwurf zu behandeln, und ein späterer Fokusverlust fällt in den stillen Rückweg `:876-877`: der Entwurf verschwindet ohne Mutation und ohne Meldung. Zusagezeile ist der Abschluss des Entwurfs vor `:908`; Mutation: ihn hinter das Leeren des Ziels schieben → der Entwurf verfällt still → rot | E-312-9 (letzter Satz); R-312-9; `CLAUDE.md:51-52` | 6 |
| M-75 | T3-07-05 · Zahlenrand Quellenzahl | 0, 1 und 16 Quellen bei exakt 760x430 | Zeichnen und Aktionssteuerung | **0 Quellen: kein Ersatzziel, keine sichtbare Aktionssteuerung. 1 und 16 Quellen: jede Quelle ist gezeichnet und erreichbar — wie heute** | `sourcesZeilen()` liefert bei leerer Liste nichts (`PluginEditor.cpp:953-955`) | B15 bestehend `sicht (0)`, `sicht (1)`, `sicht (16)` (`eq-copilot/plugin/tests/ShotTestMain.cpp:115-118`) | **Regressionswache**; einmal gebrochen: die Ersatzwahl auf die gezeichnete Menge stellen, ohne den leeren Fall abzufangen → Zugriff auf eine leere Liste → rot | R-312-6; Entwurf `:1590`, `:4277` | 6 |
| M-76 | T3-07-05 · Gegenwache State | geladener Stand mit 40 bestätigten Mitgliedern | speichern und laden über die ganze Folge aus M-73 | **Der State-Bestand bleibt unverändert: 40 Mitglieder vorher und nachher, kein Mitglied wird zur Anpassung an eine Sichtgrenze gelöscht; eine unbekannte additive Eigenschaft bleibt bytegleich erhalten** | Deckel `maxMainProjectMitglieder` = 64 (`eq-copilot/plugin/state/NakamaState.h:77`) bleibt der Speicherdeckel | B14 bestehend (`tools/beweise.ps1:745`), um den Bestandsvergleich ergänzt | **Regressionswache**; einmal gebrochen: die Sichtgrenze im Modell auf den Bestand anwenden → Mitglieder verschwinden → rot | R-312-6 (Satz 2); `CLAUDE.md:201-203`; Vertrag `nakama-state-v2.md:104` | 6 |
| M-77 | Etappe 6 · Grundgesetz und Grenze | Endstand der Etappe 6 gegen ihren Basisstand | voller Kanon und `git diff` | **Keine neue Oberfläche, keine neue Stateversion, keine v3-Vertragsänderung, Identität eingefroren, Legacy-Bezeichner unverändert; im Audio-Thread keine neue Sperre und keine Allokation; der Nulltest bleibt bitidentisch** | Etappe 6 ändert Ablaufreihenfolgen, Bedingungen und einen Statustext, keine Fläche | voller Kanon; B1, B2, B3c, A14, A16 und A3 einzeln | **Regressionswache**; einmal gebrochen: für den Statustext ein neues Bedienelement anlegen → B15-Sichtsatz weicht ab und der Designteil wäre angefasst → rot | Gate GRENZE; `CLAUDE.md:29-33`, `:204-205`; Designteil geparkt | 6 |

### 6.8 Zählung

86 Zeilen, am Text gezählt (77 aus der Etappe 1, neun aus der Matrixnacharbeit 1):
Etappe 2 neun (M-01 bis M-09), Etappe 3 fünfundzwanzig (M-10 bis M-29 und M-78
bis M-82, davon elf W02 — M-10 bis M-19 und M-78 — und vierzehn Blockbindung —
M-20 bis M-29 und M-79 bis M-82), Etappe 4 zehn (M-30 bis M-39), Etappe 5
sechzehn (M-40 bis M-54 und M-83, davon neun Offline-Übergang — M-40 bis M-47
und M-83 — und sieben Parameteranbindung), Etappe 6 sechsundzwanzig (M-55 bis
M-77 und M-84 bis M-86). Summe: 9 + 25 + 10 + 16 + 26 = 86. Jede Regel hat ihre
Zeilen: R-312-1 M-01 bis M-04 und M-09; R-312-2 M-35
bis M-37; R-312-3 M-40 bis M-47 und M-83; R-312-4 M-48 bis M-54; R-312-5 M-59
bis M-65 und M-86; R-312-6 M-73 bis M-76 und M-85; R-312-7 M-05 bis M-08, M-30
bis M-34, M-39; R-312-8 (Fassung E-312-8) M-55 bis M-58 und M-84; R-312-9 M-66
bis M-72 und M-85; R-312-10 (Fassung E-312-5) M-10 bis M-29 und M-78 bis M-82;
R-312-11 M-78; R-312-12 M-30, M-31, M-39. Jeder Entscheid der Einordnung hat
seine Zeilen: E-312-5 M-20 bis M-29 und M-79 bis M-81; E-312-6 M-19, M-28,
M-82; E-312-7 M-41, M-46, M-83; E-312-8 M-55, M-57, M-84; E-312-9 M-74, M-85;
E-312-10 M-04, M-39; E-312-11 M-01, M-02, M-26, M-38, M-53; E-312-12 M-81.
R-312-13 (Präfix jeder fremden Zeile) und R-312-14 (Kommentar im selben
Änderungssatz) binden Schreibweise und Änderungssatz, nicht ein Verhalten, und
haben deshalb keine eigene Matrixzeile: R-312-13 steht im Kopf von §6, R-312-14
in §7.6 Punkt 2 und §8 Zeile E. Die Matrixnacharbeit 2 hat keine Zeile angelegt
und keine entfernt; die Zählung bleibt 86. Jeder technische Gegenstand hat
mindestens eine Zeile:
T3-01-02 M-01, M-02, M-48 bis M-54; T3-01-03 M-04, M-40, M-41, M-83; T3-01-04
M-42 bis M-47; T3-01-05 Teil a M-20 bis M-29, M-79, M-82, Grenze von Teil b
M-80 und M-81 (Rest NAK-340); T3-04-01 M-05 bis M-08, M-30 bis M-34, M-39;
T3-04-03 M-35 bis M-37; T3-05-01 M-10 bis M-14, M-18, M-19, M-78; T3-05-02
M-15 bis M-18; T3-07-02 M-55 bis M-58 und M-84; T3-07-05 Teil a M-73 bis M-76
und M-85; T3-14-01 M-66 bis M-72 und M-85; T3-01-10 sicherer Teil M-59 bis
M-65 und M-86.

## 7. Bauplan der Etappen 2 bis 6 (Etappe 1, Bauer, 21.09.2026)

Kein Code, nur Sätze je Datei. Die Reihenfolge aus §4 gilt **ohne Abweichung**:
Etappe 2 (Messgeräte), Etappe 3 (erst W02, dann T3-01-05, je ein eigener
Änderungssatz mit eigenem Commit), Etappe 4, Etappe 5 (erst Offline-Übergang,
dann Parameteranbindung), Etappe 6. Zeilennummern gelten zum Stand `945ebfb1`.
Jede Etappe beginnt mit `git pull --ff-only`, `git rev-parse HEAD` und
`git status --short` (nur die zwei fremden Ordner) und endet mit Commits per
Pathspec; die Erstprüfung je Etappe liegt beim Dirigenten (§4). **Kein
Produktcode, bevor die Matrixprüfung dieser Etappe 1 bestanden ist** (Kopf,
Grenze).

### 7.0 Gemeinsame Regeln, Ticketpfade und neue Pfade

- **Ticketpfade, präzisiert.** Etappe 2:
  `eq-copilot/plugin/sonde/SondeProcessor.cpp` (`processBlock`, `setNonRealtime`),
  `eq-copilot/plugin/sonde/SondeProcessor.h` (Testkonstruktor, Starthaken,
  Schlossadapter), `eq-copilot/plugin/CMakeLists.txt` (neues Ziel),
  `tools/beweise.ps1` (Deklaration und Behauptungszeilen),
  `eq-copilot/plugin/tests/TransactionTestMain.cpp` (B7: `operator new` und
  `operator new[]` zusätzlich an `RtWache::meldeAllokation()` binden, E-312-11 —
  in der Nacharbeit 2 ergänzt). Etappe 3:
  `SondeProcessor.cpp` (`setStateInformation`, `dspKontrollTakt`,
  `parameterValueChanged`, `processBlock`), `SondeProcessor.h` (Mailboxfelder,
  veröffentlichte bestätigte Werte der vier Parameter, Testsetter für den
  Zählerrand, Taktsperre-Testzugang), `eq-copilot/plugin/state/NakamaTransaktion.cpp` und
  `eq-copilot/plugin/state/NakamaTransaktion.h` (`nurLesen` im Kern),
  `eq-copilot/plugin/dsp/DspKern.cpp` und `eq-copilot/plugin/dsp/DspKern.h`
  (Blockrandübernahme), `TransactionTestMain.cpp`. Etappe 4:
  `SondeProcessor.h` und `SondeProcessor.cpp` (Schleusenmitglied, Lambdas,
  Destruktor), `eq-copilot/plugin/src/PluginEditor.cpp` (`MesspunktPanel`,
  `uebernehmen`, Übergabepunkt, Kommentar), das neue Sonde-Lebenslaufbein,
  `eq-copilot/plugin/tests/TransactionTestMain.cpp` (M-38 liegt in B7; in der
  Nacharbeit 1 ergänzt, H3), `eq-copilot/plugin/tests/ShotTestMain.cpp`.
  Etappe 5: `SondeProcessor.cpp` und
  `SondeProcessor.h` (`setNonRealtime`, Parameteranbindung, Kommentare),
  `DspKern.cpp` und `DspKern.h` (hartes Schalten, `beendeCandidate`),
  `TransactionTestMain.cpp`. Etappe 6:
  `eq-copilot/plugin/src/PluginProcessor.h` (neue `reset`-Override,
  `releaseResources`), `eq-copilot/plugin/src/prozessor/Hostbruecke.cpp`
  (Abschlussweg), `eq-copilot/plugin/src/HoerMarkierung.h` (öffentlicher
  Abbruchpfad), `PluginEditor.cpp` (`timerCallback`, `schalteMarkierung`,
  `wechsleFlaecheWennNoetig`, `uebernehmeSourcesLabel`,
  `aktualisiereSourcesSteuerung`, Kommentare),
  `eq-copilot/plugin/src/prozessor/State.cpp` (Rücknahme vor dem Store in
  `spiegleKlassifikation`, `:262-273`; E-312-8 — in der Nacharbeit 1 ergänzt),
  `eq-copilot/plugin/src/PluginProcessor.cpp` (`istMainKlassifiziert` wird im
  Audiothread `acquire` gelesen statt `relaxed`, `:874` — in der Nacharbeit 1
  ergänzt), `eq-copilot/plugin/src/PluginEditor.h` (Tick-Testzugang nach dem
  Muster der vorhandenen Testzugänge `:60-68`; ohne ihn hängen M-55 bis M-58 am
  Nachrichtenloop und damit an der Wanduhr, weil der Editor `juce::Timer`
  privat erbt, `:50-51` und `:71` — in der Nacharbeit 1 ergänzt),
  `eq-copilot/plugin/src/SourcesModel.cpp` (`stelleZielSicher`),
  `eq-copilot/plugin/tests/MarkierungTestMain.cpp`, `ShotTestMain.cpp`. Dazu in
  jeder Etappe `docs/beweise/NAK-312.md` und `docs/beweise/roh/NAK-312-*`, am
  Ende `docs/plugin-wissen.md`.
- **Kanon vorher und nachher.** Etappe 2 misst
  `git diff --stat 2d6519e8 HEAD -- eq-copilot broker tools`; ist es nicht leer
  (am 21.09.2026 gemessen: eine Zeile in
  `eq-copilot/install/nakama-installer-v1.json`, kein Produktpfad), entscheidet
  der Bauer nach der Regel des Kopfes, ob der Kanon vorher neu läuft. Die
  Etappen 3 bis 6 übernehmen den Kanon nachher der Vorgängeretappe als Kanon
  vorher, wenn `git diff --stat <dessen SHA> HEAD -- eq-copilot broker tools`
  leer ist, und fahren ihn sonst neu. Am Ende jeder Etappe läuft der volle Kanon
  grün
  (`pwsh -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312 Etappe N'`).
  Vor jedem Kanon prüfen, dass im sichtbaren Checkout kein zweiter Bau- oder
  Testprozess läuft (NAK-290); bleibt das Log nach „Baue:" stehen, `vctip.exe`
  beenden (NAK-300); enden zwei `flatc`-Beine mit „Voraussetzung fehlt", einmal
  wiederholen (NAK-280).
- **Einzelbeine während des Baus.** Der Runner hat keinen Einzelbeinschalter.
  C++: alle betroffenen Ziele in EINEM Aufruf aus pwsh bauen
  (`cmake --build eq-copilot/build --config Release --target …`; cmake liegt
  nicht im Bash-`PATH`), dann das Binary direkt fahren und seinen Zeitstempel
  gegen die jüngste Quelle prüfen (NAK-230, `Copy-Item` überträgt
  `LastWriteTime`).
- **Welche Beine eine Änderung trifft.** `eq-copilot/plugin/sonde/SondeProcessor.cpp`
  geht in A16 `EqCopProbeeqNullTest`, B7 `EqCopTransactionTest`, die
  Sonde012-Ziele und das Bundle (B1); `eq-copilot/plugin/dsp/` liegt in der
  Kernbibliothek und trifft B6, B7, A16, A1 und A14;
  `eq-copilot/plugin/src/PluginEditor.cpp` und die Gen-Prozessorquellen treffen
  A1, A3, B2, B14 und B15; `eq-copilot/plugin/state/` trifft B2, B7 und A16. Die
  verbindliche Liste liest der Bau aus `eq-copilot/plugin/CMakeLists.txt`, nicht
  aus diesem Absatz.
- **Neue Pfade — Zielpfade, noch nicht angelegt.** Je Rotbeweis eine Rohdatei
  `docs/beweise/roh/NAK-312-rot-M-NN.txt`; die Kostenmessung der Etappe 3 nach
  `docs/beweise/roh/NAK-312-etappe3-blockbindung-kosten.txt`; das neue Bein
  nach `eq-copilot/plugin/tests/SondeLebenslaufTestMain.cpp` mit dem Ziel
  `EqCopSondeLebenslaufTest`. Die Rohausgaben der Kanonläufe benennt der Runner
  selbst. Kein weiterer neuer Quelltextpfad: alle übrigen Fälle liegen in den
  bestehenden Testdateien.

### 7.1 Rotbeweis-Ablauf, Golden-, Fixture- und Zeilenendenregel

Je Matrixzeile mit Rotbeweis eine Rohdatei
`docs/beweise/roh/NAK-312-rot-M-NN.txt` im Aufbau von SONDE-015 §11.3:

1. Kopf: Matrixzeile, Zusage, Bein, Datei und Zeile der Mutation.
2. SHA-256 der unveränderten Datei (`Get-FileHash -Algorithm SHA256`) und eine
   Sicherungskopie außerhalb des Repos.
3. Mutation genau an der Zeile, die die Zusage trägt (die Rotbeweisspalte nennt
   sie); vorher und nachher wörtlich in der Rohdatei.
4. Nur die betroffenen Ziele bauen und fahren; vorher
   `(Get-Item <datei>).LastWriteTime = Get-Date`, danach den Zeitstempel des
   Binaries gegen die Quelle prüfen. In die Rohdatei: Exitcode, gefallene
   Prüfungen, „Die Zusage fällt an der erwarteten Prüfung: JA", mitgefallene
   Prüfungen mit Grund und die Prüfungen, die unter derselben Mutation grün
   bleiben.
5. Rücknahme **bytegleich** aus der Sicherung, SHA-256 gleich dem Wert aus
   Schritt 2 (in der Rohdatei), `LastWriteTime` auf jetzt, neu bauen,
   Zeitstempel prüfen, Bein grün. Zwei Mutationen derselben Datei werden
   rückwärts zurückgenommen.
6. `git diff --stat` der Etappe zeigt keine mutierte Datei außerhalb der
   Ticketpfade.

Für „heute rot" steht zusätzlich der Lauf des neuen Tests gegen den
unveränderten Basisstand in der Rohdatei: der Test wird vor der Reparatur
geschrieben und einmal rot gefahren. Für „heute nicht messbar" und
„Regressionswache" ist die Mutation nach dem Bau der Beleg.

**Kein Rotlauf in undefiniertem Verhalten (R-312-12, nachgezogen in der
Matrixnacharbeit 1).** Wo ein Rotlauf einen Rückruf nach dem Ende seines
Eigentümers führen würde, trennt das Bein **Schranke** und **Marke**: die
Schranke hält den Rückruf an (ihre Lage — vor oder hinter `betreten()` — nennt
die Zeile, weil davon abhängt, welcher Schleusenzähler steigt); die Marke sitzt
an der Zugriffsstelle selbst, unmittelbar vor der ersten Zeile, die
Besitzerzustand anfasst, liest das `std::atomic<bool>` des Beins und lässt den
Rückruf bei `lebt == false` zurückkehren, **nachdem** sie gezählt hat und
**ohne** den Prozessor anzufassen. Der Beleg ist damit die gezählte Marke, nie
ein Absturz. Das gilt für den Lauf gegen den Basisstand und für jede Mutation
von M-30, M-31 und M-39. Ohne diese Trennung wäre der Basisstandlauf ein echtes
Use-after-free: `transaktion` (`eq-copilot/plugin/sonde/SondeProcessor.h:537`)
stirbt vor `controlV3` (`:523`), und der Providerweg liest
`transaktion->revision()` (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1143`).

**Golden- und Fixture-Regel.** Goldens und Fixtures entstehen nur über ihren
Erzeuger: das Parametergolden von M-48 und M-49 gibt der unveränderte Prozessor
am Basis-SHA der Etappe 5 aus; die Shots von M-73 bis M-75 erzeugt `EqCopShot`
selbst; `eq-copilot/fixtures/state/schema2/fremdes-major-3.bin` bleibt
unverändert. Von Hand ausgeschrieben wird nur, was selbst eine Formel ist
(10^(3/20), 10^(6/20)); kein Test ruft die Produktfunktion als Orakel. Für
Fixtures unter `eq-copilot/fixtures/state/**` gilt `-text` aus
`.gitattributes`: sie werden bytegleich geschrieben.

**Zeilenenden**, am 21.09.2026 mit `git ls-files --eol` gemessen und vor jedem
Schreiben erneut zu lesen (`CLAUDE.md`, Maschinen-Landminen): Arbeitskopie CRLF
bei `eq-copilot/plugin/sonde/SondeProcessor.cpp`, `SondeProcessor.h`,
`eq-copilot/plugin/src/PluginProcessor.h`,
`eq-copilot/plugin/src/prozessor/State.cpp`,
`eq-copilot/plugin/src/prozessor/Hostbruecke.cpp`,
`eq-copilot/plugin/src/HoerMarkierung.h`, `eq-copilot/plugin/dsp/DspKern.cpp`,
`eq-copilot/plugin/tests/TransactionTestMain.cpp`,
`eq-copilot/plugin/tests/MarkierungTestMain.cpp`,
`eq-copilot/plugin/tests/SondeNullTestMain.cpp`,
`eq-copilot/plugin/tests/DspGoldenTestMain.cpp`,
`eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp`, `tools/beweise.ps1` und
`docs/plugin-wissen.md`; Arbeitskopie LF bei
`eq-copilot/plugin/dsp/DspRtWache.h`,
`eq-copilot/plugin/state/NakamaTransaktion.cpp`,
`eq-copilot/plugin/src/PluginEditor.cpp`,
`eq-copilot/plugin/tests/ShotTestMain.cpp`,
`eq-copilot/plugin/CMakeLists.txt` und `docs/beweise/NAK-312.md`. Im Index
liegen alle als LF.

### 7.2 Etappe 2 — Messgeräte (R-312-1, R-312-7), verhaltensneutral

**Wo der Bereich beginnt und endet.** `RtWache::Bereich wache;` wird die erste
Anweisung von `SondeProcessor::processBlock`
(`eq-copilot/plugin/sonde/SondeProcessor.cpp:369-371`), vor
`juce::ScopedNoDenormals` oder unmittelbar danach; er endet mit der Funktion,
also nach der Analysepublikation (`:466`, `:475`) und nach der
JUCE-Sicherheitsschleife (`:493-494`). Der Bereich des Kerns
(`eq-copilot/plugin/dsp/DspKern.cpp:1059`) bleibt, wo er ist: `RtWache` zählt
eine **Tiefe** (`eq-copilot/plugin/dsp/DspRtWache.h:36-38`), Verschachtelung ist
vorgesehen und ändert nichts.

**Was er kostet.** Je Block ein Inkrement und ein Dekrement eines
`thread_local int` — kein Atomic, keine Sperre, keine Allokation. Der Zähler
selbst wird nur berührt, wenn eine gemeldete Sperre oder eine gemeldete
Allokation auftritt.

**Warum er verhaltensneutral ist.** Der Bereich schreibt kein Sample und ändert
keinen Programmzustand. Beleg: A16-Nulltest bitidentisch, B6-Goldens bytegleich,
voller Kanon grün (M-03, M-09).

**Zählbare Sperre im Callbackpfad.** `RtWache::GemeldeteSperre`
(`DspRtWache.h:78-93`) ruft `l.lock()` und `l.unlock()`; `juce::CriticalSection`
kennt nur `enter()` und `exit()`. Die Sonde bekommt deshalb einen
zwei Zeilen langen Adapter in ihrem eigenen Namensraum (`lock()` ruft `enter()`,
`unlock()` ruft `exit()`), und `setNonRealtime` nimmt `zustandSchloss` über
`RtWache::GemeldeteSperre<Adapter>` statt über `juce::ScopedLock`
(`SondeProcessor.cpp:1325`). Dieselbe Sperre, dieselbe Sperrenordnung, ein
zusätzlicher thread-lokaler Test. `eq-copilot/plugin/dsp/DspRtWache.h` wird
**nicht** angefasst; der Textriegel von B6 liest nur
`eq-copilot/plugin/dsp/` (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:6629-6660`)
und bleibt unberührt.

**Zählbare Allokation in B7 (E-312-11, nachgezogen in der Matrixnacharbeit 2).**
B7 zählt heute in einen **eigenen** thread-lokalen Zähler: `operator new` und
`operator new[]` (`eq-copilot/plugin/tests/TransactionTestMain.cpp:76-91`)
erhöhen `allokationen` (`:72-73`, Inkremente `:78` und `:87`), rufen aber
`RtWache::meldeAllokation()` **nicht**; der vorhandene Fall liest allein
`dsp::RtWache::sperren()` (`:3368`) und prüft `imCallback == 0` gegen den
eigenen Zähler (`:3374-3376`). Die Zusagen von M-01, M-02, M-26, M-38 und M-53
nennen dagegen `RtWache::allokationen()` — sonst misst das Bein eine andere
Größe, als die Zeile sagt. Diese Etappe bindet deshalb beide Operatoren
**zusätzlich** an `RtWache::meldeAllokation()`, wörtlich nach dem Muster von B6
(`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:100` und `:109`:
`if (zaehleAllokationen) { ++allokationen; RtWache::meldeAllokation(); }`). Der
eigene Zähler von B7 und alle bestehenden Prüfungen bleiben unverändert;
`meldeAllokation()` erhöht ohnehin nur im Audiopfad
(`eq-copilot/plugin/dsp/DspRtWache.h:41-44`), also allein innerhalb des
Bereichs. Ab dieser Etappe ist `RtWache::allokationen()` in B7 damit messbar;
die fünf Zeilen sagen das in ihrer Zusage.

**Wie die Behauptungszeilen lauten werden — je Bein nur der Bereich, den es
wirklich öffnet (D5, berichtigt in der Matrixnacharbeit 1).** B6
`EqCopDspGoldenTest` übersetzt genau eine Quelle
(`eq-copilot/plugin/CMakeLists.txt:1148-1154`:
`target_sources(EqCopDspGoldenTest PRIVATE tests/DspGoldenTestMain.cpp)` plus
`nakama_kern_anbinden`); weder `sonde/SondeProcessor.cpp` noch
`NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` kommen darin vor, B6 kann also keinen
`SondeProcessor` anlegen und misst den Bereich in `DspKern::verarbeite`
(`eq-copilot/plugin/dsp/DspKern.cpp:1059`, die einzige `RtWache::Bereich`-Stelle
im Produkt). **B6 bekommt deshalb KEINEN Satz über den Callback-Eintritt der
Sonde**, sondern höchstens die Klarstellung, dass sein Zähler ab
`DspKern::verarbeite` misst. Den Satz über den Bereich **ab dem Eintritt in
`SondeProcessor::processBlock`** bekommen genau die Beine, die eine Sonde bauen:
B7 (`tools/beweise.ps1:728`), A16 (`:688`) und das neue Sonde-Lebenslaufbein.
Dazu je Satz die Grenze — Sperren, die JUCE vor dem Plugincode nimmt, sieht der
Zähler bauartbedingt nicht — und der Hinweis, dass das Offlineflag gemessen
wird, indem das Bein den Bereich wie der Wrapper um `setNonRealtime` und
`processBlock` legt. Die Behauptung sagt damit nicht mehr, als ihr Test misst
(`tools/dirigent/pruefliste.md` E, R-312-1 Satz 2). M-01, M-02 und M-04 liegen
sämtlich in B7 — die Matrix war hier schon richtig, nur der Bauplan nicht.

**Testzugänge der Sonde, Muster Gen.** Unter
`#if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` — dem Define, das alle
Konsolenziele über `eq-copilot/plugin/CMakeLists.txt:1323-1330` und die
Sondenfunktionen `:407-409`, `:437-439` tragen — bekommt
`eq-copilot/plugin/sonde/SondeProcessor.h` (a) einen Konstruktor
`SondeProcessor (const std::string& probePipename, nakama::ipc::ServerErwartung)`
nach dem Muster `eq-copilot/plugin/src/PluginProcessor.h:100-101`, der beide
v3-Clients bei der Konstruktion auf den Probenamen setzt und fail-closed den
leeren Namen übernimmt, wenn `nakama::ipc::istProbePipename` false sagt; (b) die
Starthaken `v3StartFuerTest()` und `v3TelemetrieStartFuerTest()` nach dem Muster
`PluginProcessor.h:763-766`; (c) `v3PipeNameFuerTest()` als Sicht auf das, was
übernommen wurde. Der Produktkonstruktor delegiert an denselben privaten
Konstruktor und reicht den Produktnamen durch — der Produktpfad ändert sich
nicht, und der Produktzweig `SondeProcessor.cpp:191-200` bleibt, wie er ist.
**Kein Test berührt die Produktions-Pipe**: das Bein prüft vor dem Aufbau
`istProbePipename` (Muster
`eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp:443-445`), und der
fail-closed-Zweig macht einen Produktionsnamen unbrauchbar.

**Wie ein neues Lebenslaufbein deklariert wird.** Ein Konsolenziel
`EqCopSondeLebenslaufTest` in `eq-copilot/plugin/CMakeLists.txt` nach dem Muster
der Sondenfunktion `:427-445`: Quelle
`eq-copilot/plugin/tests/SondeLebenslaufTestMain.cpp` (Zielpfad),
`sonde/SondeProcessor.cpp`, `src/AnalyseEngine.cpp`, `src/DiagnoseAntwort.cpp`,
`core/ipc/PipeToken.cpp`, Definitionen `NAKAMA_SONDE_AKTIV=1` und
`NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3=1`, `nakama_kern_anbinden`. Im Bein liegen
Prozessor und Testserver auf dem **Heap** (`std::unique_ptr`), weil
`FeatureEngine`-Objekte den MSVC-Standardstack von 1 MiB sprengen und der Riss
erst im vollen Kanon sichtbar wird (NAK-175). Die Zeile in `tools/beweise.ps1`
bekommt ein freies Kürzel — welches, liest der Bau aus der Datei — und eine
Behauptung, die genau den Lebenslauffall nennt: Aufbau über die Probe-Pipe,
gehaltener Provider über die Stoppfrist, Marke nach dem Eigentümerende.

**Risiken und Grenze.** Nicht angefasst: `DspRtWache.h`, der Produktzweig der
v3-Clients, jede Audiorechnung, jede Behauptung außerhalb von A16, B6, B7 und
dem neuen Bein. Risiko: ein zweiter `Bereich` an einer Stelle, die auch vom
Nachrichtenthread läuft, würde den Zähler lügen lassen — M-02 misst genau das.

### 7.3 Etappe 3 — W02 und die Blockbindung (R-312-10)

**Erster Änderungssatz: W02.**

*Wie die Ladegeneration gebildet wird.* Nicht als neue Zahl, sondern als
**Quittierung** des vorhandenen Ereigniszählers: innerhalb des
Zustandsschlosses, das `setStateInformation` ohnehin hält
(`eq-copilot/plugin/sonde/SondeProcessor.cpp:1067-1098`), wird für alle
Hostparameter `hostEreignisGesehen[i] = hostEreignis[i]` gesetzt und
`hostEreignisOffen` auf false gelegt. Das sind Schreibzugriffe auf vorhandene
Felder (`SondeProcessor.h:541-543`) unter einem Schloss, das gehalten wird:
keine Allokation, kein neues Feld im State, kein Vertragswechsel.

*Wer sie liest.* `dspKontrollTakt` (`SondeProcessor.cpp:1455-1465`) und — nach
dem zweiten Änderungssatz — der Blockrand des Kerns. Beide vergleichen auf
**Gleichheit** (`:1460`), nicht auf Ordnung; die Quittierung ist damit
überlaufsicher (M-19).

*Reihenfolge.* Die Quittierung steht **vor** `hostParameterAbgleichen`
(`:1103`). Stünde sie danach, quittierte sie die Ereignisse, die dieser Abgleich
selbst auslöst, und der Befund verschöbe sich nur.

*Was mit Werten geschieht, die während des Ladens eintreffen (R-312-11,
berichtigt in der Matrixnacharbeit 1).* `parameterValueChanged` (`:1330-1338`)
nimmt sie weiter an; ihr Zähler steigt über den quittierten Stand hinaus, und
sie wirken beim nächsten Takt. **Der frühere Satz „wird mitquittiert" war
falsch** und ist an der Quelle widerlegt: die Herkunftssperre `AbgleichHerkunft`
(`:76-82`) ist **thread-lokal** (`:76` `thread_local int abgleichTiefe = 0;`)
und unterdrückt nur Rückmeldungen auf dem Abgleichthread. Ein Hostwert aus dem
Prozessaufruf erhöht `hostEreignis` (`:1336`) nach der Quittierung und wird
deshalb **nicht** mitquittiert; der nächste Takt schreibt ihn ins Overlay
(`:1459-1462`). Ohne weitere Regel entstünde genau dadurch der Riss, den M-16
für den read-only-Fall schon verbietet: der Abgleich setzt den Regler auf den
geladenen Wert zurück (`:1428-1430`), die Mailbox behält den Hostwert — der
Klang folgt dem Hostwert, der Regler zeigt den geladenen, bis zum nächsten
Hostereignis auf diesem Parameter. **Gebaut wird deshalb R-312-11:** ein
Hostwert nach der Quittierung ist ein neuer Hostgestus und wirkt; der Abgleich
überschreibt keinen Parameter, dessen Zähler seit der Quittierung gestiegen ist,
und prüft das nach seiner Schleife (`:1425-1431`) ein zweites Mal — beide
Prüfungen auf **Gleichheit** des Zählerstands, nie auf Ordnung. Danach zeigen
Regler und Klang für jeden Parameter denselben Wert. SONDE-015 M-84
(`docs/beweise/SONDE-015.md:1162`) verlangt weiter den
geladenen Stand, M-11 misst den Gegenfall „neuer Gestus nach dem Recall", und
M-78 misst den Fall „während".

*read-only.* Der Transaktionskern bekommt ein `nurLesen`-Flag, das `ladestart`
aus dem geladenen Stand mitbekommt; `Transaktionskern::wirksam`
(`eq-copilot/plugin/state/NakamaTransaktion.cpp:340-348`) legt dann kein Overlay
über den bestätigten Satz (zusätzliche Bedingung in `:345`). Im selben Zug ruft
der Kontrolltakt `hostParameterAbgleichen` mit dem neutralen Satz, damit der
Hostregler sichtbar zurückspringt (M-16) — derselbe Weg, den
`gestusAbschliessen` (`SondeProcessor.cpp:1381-1391`) für den abgewiesenen
Gestus schon benutzt. Das Flag wird in **beide** Richtungen gesetzt, sonst
bleibt die Automation nach einem read-only-Stand tot (M-17).

*Beine, die einzeln zu fahren sind:* B7 `EqCopTransactionTest`, danach B2
`EqCopStateMigrationTest` und A16 `EqCopProbeeqNullTest` als Gegenwachen.

*Risiken und Grenze.* Nicht angefasst: `getStateInformation`
(`SondeProcessor.cpp:1024-1028`), die Mailbox in `reset` (`:334-337`), die
Revisions- und Undo-Wege, der Statevertrag. Risiko: eine Quittierung, die auch
`reset` erfasst, nimmt eine bewusste Entscheidung mit — M-13 wacht darüber.

**Zweiter Änderungssatz: die Blockbindung. Mechanismus, zwei geprüfte Wege.**

*Weg 1 — blockgebundenes Wecken des Workers.* `processBlock` erhöht vor
`dspKern->verarbeite` (`SondeProcessor.cpp:432`) einen
`std::atomic<std::uint64_t> blockZaehler` (relaxed, kein Wecken, keine Sperre);
der Worker (`:569-579`) zieht, sobald dieser Zähler gewachsen ist und
`hostEreignisOffen` steht, und verkürzt seine Wartezeit (`:739`), solange das
Flag steht. Die 5-ms-Frist bleibt Obergrenze für ACK-Ernte und Ruhegrenze.
**Kosten:** ein `fetch_add` je Block; der Worker pollt häufiger. **Geprüft an:**
`:569-579`, `:739`, `:1455-1465`. **Ergebnis: erfüllt R-312-10 nicht.** Der
Zeitpunkt bleibt der Zug eines anderen Threads; im Offline-Render mit rund
14-facher Geschwindigkeit (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-e-last-lebenslauf.md:41`,
gemessen) liegen weiterhin Blöcke ohne Zug, Echtzeit und Offline bleiben
ungleich. Maß ist die Zusage, nicht der Mechanismus.

*Weg 2 — Blockrandübernahme im Kern aus der Hostmailbox, begrenzt auf vier
Parameter. **Empfehlung**; das ist Weg (C) der Validierung, und **E-312-5
ersetzt E-312-1 im Umfang**.*

*Was abgedeckt ist.* Genau vier Hostparameter: `v1.global.input_trim_db`,
`v1.global.output_trim_db`, `v1.global.width`, `v2.global.mix`
(`eq-copilot/plugin/state/NakamaParameter.cpp:50-52`, `:80`). Für sie setzt der
Kern am Blockrand dieselben Rampenziele, die er heute aus dem Programm setzt
(`eq-copilot/plugin/dsp/DspKern.cpp:620-623`, gespeist aus
`eq-copilot/plugin/dsp/DspProgramm.cpp:259-264` und `:271-272`); `setzeZiel`
(`eq-copilot/plugin/dsp/DspKern.h:412-418`) erzeugt bei gleichem Wert keine
neue Rampe (`:414`).

*Was NICHT abgedeckt ist — und warum der frühere Satz falsch war (D1).* Der
Satz „Abgedeckt sind genau die Parameter, für die das Overlay heute schon
durchlässt" ist an der Quelle widerlegt:
`eq-copilot/plugin/state/NakamaTransaktion.cpp:345` lässt jeden
nichttopologischen Parameter durch, und `gleitkomma`
(`NakamaParameter.cpp:25-33`, `:30`) setzt `topologisch` unbedingt auf false —
das Overlay lässt **80** Hostparameter durch (69 kontinuierliche und 11
boolesche), der Blockrand erreicht davon **vier**. Alles andere wirkt nur über
ein **Programm**, das der Worker unter dem Zustandsschloss baut
(`eq-copilot/plugin/sonde/SondeProcessor.cpp:1449`, `:1476-1477`): die 64
Bandwerte (`DspProgramm.cpp:281-356`), `mono_bass_hz` (`:262`, `:277-279`), die
elf Booleschen (`:258`, `:263`, `:265`) und das abgeleitete Auto-Gain-Ziel
(`DspKern.cpp:626`, `DspProgramm.cpp:367`). Das Auto-Gain-Ziel bleibt
ausdrücklich beim Worker: SONDE-015 M-39 (`docs/beweise/SONDE-015.md:1087`) sagt
die Ableitung **außerhalb des Audiothreads** zu, und das ist gezählt, nicht
behauptet (`RtWache::meldeAbleitung`, `eq-copilot/plugin/dsp/DspRtWache.h:55-63`,
gerufen in `DspProgramm.cpp:89`). Topologieautomation bleibt deaktiviert
(SONDE-015 M-119, `docs/beweise/SONDE-015.md:1223`;
`SondeProcessor.cpp:151-154`; der Kommentar wird im selben Änderungssatz
berichtigt). **Der Rest wird nicht wegdeklariert:** SONDE-015 M-120 und der
Entwurfssatz bleiben für Bandwerte gebrochen, M-80 und M-81 messen genau diesen
Zustand als heutigen und bleibenden Stand dieses Tickets, und T3-01-05 Teil b
geht als Folgeticket **NAK-340** (Planschritt S25p) an den Blockrandentwurf im
Kern mit eigener Besitzregel, eigener Matrix und eigener T2-Prüfung. Die
Registerzeile beim Abschluss zeigt auf NAK-340, **nicht** auf den
Wrapperpatch — der liefert Sampleoffsets vor `processBlock` und entwirft keine
Bandkoeffizienten im Audiothread.

*Wie die bestätigten Werte dem Callback ohne Sperre vorliegen (E-312-5,
Schlusssatz).* Bitgleichheit in M-20 verlangt, dass der Blockrand dieselbe Zelle
rechnet wie `zelleAusHost` (`SondeProcessor.cpp:1394-1420`) **einschließlich**
der Ausnahme `:1403-1404`: gleicht der Hostwert dem bestätigten Wert in
Hostgenauigkeit, ist es genau der bestätigte Wert. Diese Ausnahme liest heute
`transaktion->bestaetigt()` (`:1398`) — verfügbar nur unter dem Schloss. Der
Prozessor veröffentlicht deshalb je abgedecktem Parameter zwei Atomics, beide
**vom Worker unter dem Zustandsschloss geschrieben** und vom Callback relaxed
gelesen: `bestaetigtNormiert[4]` (`float`, der Vergleichswert der Ausnahme, also
`p->convertTo0to1 (hostWertAus (i, bestaetigt))`) und `bestaetigtZahl[4]`
(`double`, der bestätigte Wert selbst, den die Ausnahme zurückgibt). Geschrieben
werden sie dort, wo der bestätigte Zustand sich ändert — Commit, Ladestart,
Undo —, also im Kontrolltakt (`:1447-1478`) und in `setStateInformation`
(`:1067-1098`), beide unter dem Schloss. Der Callback rechnet damit ohne jeden
Zugriff auf den Transaktionskern: Zähler ungleich → Hostwert laden → gleicht er
`bestaetigtNormiert[i]`, ist die Zelle `bestaetigtZahl[i]`; sonst
`p->convertFrom0to1` plus `jlimit` wie `:1411-1416`. `convertFrom0to1` ist bei
allen vier Parametern die reine Arithmetik einer
`juce::NormalisableRange<float>`: keine Sperre, keine Allokation. Ein nicht
endlicher Hostwert zählt wie heute als unverändert (`:1408-1409`, M-18).

*Die Schiedsregel zwischen Blockrand und Worker (E-312-6; D2 berichtigt).* Der
frühere Satz legte einen **Ordnungsvergleich** („nicht älter als") auf
`hostEreignis` — denselben Zähler, für den M-19 den Ordnungsvergleich zur
brechenden Mutation erklärt, und er läuft über (`SondeProcessor.h:542`,
`std::uint32_t`). Gebaut wird stattdessen:

1. Der Blockrand liest in **jedem** Block die vier Zähler (vier relaxed Loads)
   und übernimmt einen Wert genau dann, wenn
   `hostEreignis[i] != blockrandStand[i]`; danach `blockrandStand[i] = n`.
   Derselbe Gleichheitsbegriff wie die Quittierung in `SondeProcessor.cpp:1460`
   — überlaufsicher, M-19 bleibt wörtlich, M-82 misst den Rand.
2. Die Mailboxlesung steht im selben Callback **vor** der Programmübernahme
   (`DspKern.cpp:531`).
3. Jede Workerpublikation trägt je abgedecktem Parameter den Zählerstand, aus
   dem ihr Wert stammt; der Worker setzt ihn aus `hostEreignisGesehen`
   (`SondeProcessor.cpp:1461`), Commit und Ladestart quittieren (Stand =
   aktueller Zähler).
4. Bei der Übernahme setzt das Programm ein Rampenziel nur, wenn
   `publiziert[i] == blockrandStand[i]`; sonst bleibt das Blockrandziel stehen.
   Weil der Blockrand vorher gelesen hat, ist eine ungleiche Publikation immer
   die ältere — die Regel braucht nur diese eine Richtung.
5. `hostEreignisOffen` (`SondeProcessor.h:544`) behält **genau einen**
   Verbraucher, den Worker (`SondeProcessor.cpp:1455`). Der Blockrand liest und
   tauscht es nicht. Täte er es, verlöre der Worker die Meldung, und Overlay,
   Epoche und Ruhegrenze (`:1457-1473`) liefen für diese Ereignisse nicht —
   SONDE-015 M-81 wäre gebrochen. M-28 wacht darüber.

**Kosten, statt „kein Speicherzuwachs" (D2 berichtigt).** Je Blockrand vier
relaxed Loads der Zähler; im Ereignisfall je betroffenem Parameter zwei weitere
Loads und eine Zellrechnung. Kein `exchange`, keine Sperre, keine Allokation,
kein Warten (M-26). Speicher, benannt statt bestritten: im Kern vier
`std::uint32_t` (Audiothread-eigen, nicht atomar) = 16 Byte; je Programm vier
`std::uint32_t` = 16 Byte, übergeben mit der Bank über dieselbe
Release-/Acquire-Übergabe wie die Kennungen — bei vier Bänken
(`eq-copilot/plugin/dsp/DspBankPool.h:128`) und zwei Vorbauplätzen
(`eq-copilot/plugin/dsp/DspKern.h:596`) also 96 Byte; im Prozessor vier
`std::atomic<float>` und vier `std::atomic<double>` = 48 Byte. Klein, aber nicht
null.

*Was die Zeilen von der Wanduhr löst (Matrixregel §6).* Der Worker tickt frei
(`SondeProcessor.cpp:575-578`, plus bis 20 ms Schlaf `:738-740`); ein Bein, das
„A hat den Wert nicht bekommen" misst, hinge sonst an einem Wettlauf mit ihm.
`SondeProcessor.h` bekommt deshalb unter demselben Testdefine wie die übrigen
Zugänge eine **Taktsperre**: ein Template, das `zustandSchloss` hält, solange die
übergebene Funktion läuft — wörtlich nach dem Muster von
`mitAngehaltenerAnalyseFuerTest` (`SondeProcessor.h:316-326`), das dasselbe für
den Analyseworker tut. `dspKontrollTakt` nimmt dieses Schloss als erste
Anweisung (`SondeProcessor.cpp:1449`) und blockiert dort; `processBlock` nimmt es
nie (M-01, M-26), die Blöcke laufen also weiter. Damit sind M-20 bis M-29 und
die Grenzzeilen M-80 und M-81 deterministisch, und der Lauf gegen den Basisstand
ist ein Beleg statt einer Wahrscheinlichkeit.

**Wirkung:** für die vier abgedeckten Parameter entscheidet der Audiothread den
wirksamen Block am Blockrand; gleicher Event- und Blockverlauf liefert in
Echtzeit und offline dieselbe Ausgabe, und der Entwurfssatz „vom vorigen zum
letzten Blockwert" (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3084-3086`) ist für
sie wörtlich erfüllt. Samplegenauigkeit innerhalb des Blocks wird nicht
zugesagt; für Bandwerte, `mono_bass_hz`, die Booleschen und das Auto-Gain-Ziel
wird gar keine Gleichheit zugesagt. **Risiken:** (a) zwei Schreiber desselben
Rampenziels — geregelt über Punkt 1 bis 4; (b) `dspBericht` und die Taps müssen
denselben Stand lesen, sonst meldet der Bericht kurz einen anderen Gain als der
Klang (SONDE-015 M-57 bleibt Maß); (c) die Ruhegrenze zählt weiter in
verarbeiteten Audiosamples (`SondeProcessor.cpp:1468-1473`) und bleibt unberührt
(M-24).

*Was ein vorallokierter Ereignisring kosten würde.* Ein SPSC-Ring je Parameter
mit Blockindex und Wert bewahrt die **Reihenfolge** mehrerer Werte innerhalb
eines Blocks. FL liefert höchstens einen Punkt je Block und meldet
`sample_accurate_automation` als `unsupported`
(`eq-copilot/identity/host-capabilities-fl-v1.json:27`, `:54`). Ohne den
gepinnten Wrapperpatch (`docs/FL-Nakama-Sonden-Design-Entwurf.md:3072-3086`) ist
der Ring deshalb Speicher und Codepfad ohne Wirkung — und der Wrapperpatch
gehört ausdrücklich nicht in dieses Ticket (Kopf, Grenze). **Ohne den
Wrapperpatch ergibt er keinen Sinn.**

*Begrenztes Warten des Callbacks im Offline-Render — Für und Wider, Entscheidung
beim Dirigenten in der Matrixprüfung; der Bauer baut sie nicht voraus.*
**Dafür:** offline gibt es keine Gerätefrist, der Wrapper hält ohnehin das
Callback-Schloss (`juce_audio_plugin_client_VST3.cpp:3890`, Bauartefakt), der
Host meldet den Zustand über `setNonRealtime`, Paket E hat offline **0**
Dienstzeitüberschreitungen gemessen
(`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-e-last-lebenslauf.md:35`), und
ein begrenztes Warten machte Echtzeit und Offline trivial gleich, ohne den Kern
anzufassen. **Dagegen:** das Grundgesetz nennt den **Thread**, nicht die Frist
(`CLAUDE.md:32-33`), und weder SONDE-015 M-47 noch der Entwurf kennen eine
Offline-Ausnahme (Teil A §4c); die Gegenspieler unter demselben Schloss haben
**unbegrenzte** Haltedauer (Serialisierung in
`SondeProcessor.cpp:1024-1028`, Programmbau in `:1447-1478`); ein Warten machte
T3-01-03 zur Bauart statt zum Befund und koppelte die Exportdauer an die
Arbeitsverteilung des Workers.

*Beine, die einzeln zu fahren sind:* B7, danach B6 `EqCopDspGoldenTest`
(SONDE-015 M-120 Kernhälfte,
`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:8182-8348`) und A16.

*Risiken und Grenze.* Nicht angefasst: der Wrapper, `DspKern::verarbeite` als
Rechenweg, der Transaktionskern als Wahrheit des bestätigten Zustands, jede
Topologieautomation. Risiko: ein Blockrandzugriff, der unbedingt läuft,
überschreibt die Rampenziele des Programms — M-28 wacht darüber.

### 7.4 Etappe 4 — Besitz (R-312-7, R-312-2)

**Deklarationsposition der Schleuse.** `eq-copilot/plugin/sonde/SondeProcessor.h`
bekommt ein `std::shared_ptr<nakama::ipc::CallbackSchleuse> callbackSchleuse`,
deklariert **vor** `controlV3` (`:523`) und `telemetryV3` (`:524`) — C++
zerstört Mitglieder in umgekehrter Deklarationsreihenfolge, das Mitglied muss
also die Clients überleben. Es steht damit auch vor den drei Heap-Mitgliedern
`:535-537`, die der Befund trifft.

**Die drei Lambdas.** `SondeProcessor.cpp:139-142` fängt die Schleuse als
`shared_ptr` mit und betritt sie **vor** dem ersten Besitzerzugriff, wörtlich in
der Form von `eq-copilot/plugin/src/PluginProcessor.cpp:188-196`. Ein
abgewiesener Zug liefert das leere Default-Objekt zurück, wie bei Gen. Der
scharfe Zugriff, den das schützt, ist `SondeProcessor.cpp:1143`
(`transaktion->revision()`); der Zug liegt davor, nicht darin.

**Schließpunkt.** `~SondeProcessor()` (`:212-230`) schließt sie **nach**
`telemetryV3.stop()` (`:228`) und `controlV3.stop()` (`:229`) und vor dem Ende
des Rumpfes, also vor jeder Mitgliederzerstörung — die Stelle, die
`PluginProcessor.cpp:500` bei Gen einnimmt.

**Verhalten bei gerissener Frist.** `stop()` wartet höchstens `kStopFristMs`
(2000 ms, `eq-copilot/plugin/core/ipc/controlclient/Intern.h:59` und
`eq-copilot/plugin/core/ipc/TelemetryClient.cpp:30`), zählt
`stopFristUeberschritten` (`eq-copilot/plugin/core/ipc/ControlClient.cpp:130`)
und löst den Thread ab (`:133`). Danach schließt die Schleuse: ein Rückruf, der
noch läuft, wird zu Ende gewartet (`gewartetMs` steigt), ein später
beginnender wird abgewiesen (`abgewiesen` steigt). Die abgelöste Laufzeit hält
danach eine geschlossene Schleuse und berührt niemanden mehr.

**Warum keine neue Sperre im Callbackpfad entsteht.** `betreten` und
`Zug::~Zug` arbeiten ausschließlich mit Atomics; `warteMutex` und `warte`
gehören allein dem Schließpfad
(`eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:28-37`). Der Callbackpfad
der Provider läuft ohnehin nicht im Audio-Thread; M-38 misst trotzdem, dass die
RT-Wache über 4000 Blöcke bei 0 bleibt.

**Form der Lebendprüfung des Panels.** `MesspunktPanel`
(`eq-copilot/plugin/src/PluginEditor.cpp:625-627`) hält statt
`EqCopilotProcessor& proz` einen
`juce::Component::SafePointer<EqCopilotEditor>` — denselben, den der
Änderungs-Callback schon führt (`:737`). `uebernehmen()` (`:700-709`) holt den
Prozessor über den Editor und kehrt **ohne jeden Zugriff** zurück, wenn der
Editor weg ist; der Aufruf bei `:707` liegt dann hinter der Prüfung. Der
Übergabepunkt `:734` übergibt entsprechend den SafePointer statt `processor`.
Der JUCE-Vertrag trägt das: der Editor wird vor seinem Prozessor zerstört
(`editorBeingDeleted`), also ist ein lebender Editor ein lebender Prozessor.

**Der berichtigte Kommentar.** `PluginEditor.cpp:731-733` sagt heute, der
SafePointer schütze den Destruktorpfad; geschützt ist nur das Neuzeichnen. Der
neue Text sagt, was dann gilt: der Panel-Destruktor prüft den Editor, bevor er
den Prozessor anfasst, und der SafePointer im Änderungs-Callback bleibt für das
Neuzeichnen. Der Kommentar gehört in denselben Änderungssatz wie der Fix (§1,
Irreführende Kommentare).

**Wo die Schranke des Beins sitzt und wo die Marke (R-312-12).** Die Marke sitzt
in den Provider-Lambdas unmittelbar vor `transaktion->revision()`
(`SondeProcessor.cpp:1143`), also vor der ersten Zeile, die Besitzerzustand
anfasst; sie zählt und kehrt zurück, ohne den Prozessor zu berühren. Die
Schranke ist je Fall benannt: **hinter** `betreten()` in M-30 und M-39 Fall 1
(dann wartet der Destruktor, `gewartetMs` steigt), **vor** `betreten()` in M-39
Fall 2 (dann wird der Zug abgewiesen, `abgewiesen` steigt). Freigegeben wird an
beobachtbaren Zuständen — `stand().geschlossen` beziehungsweise der Rückkehr des
Destruktors —, nie an einer Zeitmarke. **`Schleuse.h` bekommt keinen Haken**:
die einzige Mutation, die einen bräuchte, war „zweites Lesen in `betreten()`
entfernen" (`Schleuse.h:131-136`), und sie entfällt (E-312-10), weil sie nur in
einem Fenster von wenigen Befehlen fiele.

**Beine, die einzeln zu fahren sind:** das neue Sonde-Lebenslaufbein, danach
B7 `EqCopTransactionTest` (M-38 liegt dort, H3), B15 `EqCopShot` und B14
`EqCopSonde012ProjectReloadTest`.

**Risiken und Grenze.** Nicht angefasst: `Schleuse.h` selbst, Gens Schleuse,
die Stoppfristen, der Briefkasten (`SondeProcessor.cpp:214-216`), das
Quellenmodell. Risiko: ein `reconnect` im Ladeweg (`:1104-1105`), der die
Schleuse neu anlegt, hebt den Schutz auf — M-33 wacht darüber.

### 7.5 Etappe 5 — Offline-Übergang und Parameteranbindung

**Der kombinierte Fix in `setNonRealtime`** (`SondeProcessor.cpp:1321-1328`).
Grundsatz nach E-312-7: **beim Offline-Wechsel berührt der Callback nur
Atomics, die ihm gehören oder ausdrücklich geteilt sind.** Merkzettel,
Poolpublikation und Bankfreigabe bleiben beim Worker — die Besitzregeln sagen
das wörtlich: `eq-copilot/plugin/dsp/DspKern.h:599-602` („Merkzettel und Zaehler
gehoeren ALLEIN dem Worker - kein Atomic, keine Sperre, nichts davon liest der
Audiothread"), `DspKern.h:157` (`beendeCandidate`, `:205`, steht unter
„Control-Worker-Seite - laeuft NIE im Audiothread"),
`eq-copilot/plugin/dsp/DspBankPool.h:154` (dieselbe Überschrift für
`publiziereEnde`) und `DspBankPool.h:37-39` („WARUM DER AUDIOTHREAD NIE
FREIGIBT: … `frei` macht ausschliesslich der Worker").

1. *Wechselerkennung.* Ein `std::atomic<bool> offlineGesehen` hält den zuletzt
   gesehenen Wert. Ist er gleich, kehrt die Funktion sofort zurück — das ist der
   Normalfall in nahezu jedem Offlineblock, und dort fällt dann keine Sperre an
   (M-40).
2. *Hörmatrix, sofort im Callback.* Beim Wechsel nach offline setzt sie
   `setzeHoermatrix (processed)` (`eq-copilot/plugin/dsp/DspKern.h:244`, ein
   Atomic) **und** ein neues Atomic „hart schalten". Dieses verbraucht der
   **Audiothread selbst** in `verarbeiteStueck` vor `DspKern.cpp:1233` und setzt
   `hoerVorher = hoerLaufend = wirksam; hoerFadeRest = 0` statt `kFadeSamples`
   (`:1237-1239`) — Zustand, der ihm allein gehört (`DspKern.h:609-613`: „Nur
   der Audiothread liest und schreibt ihn"). Ein laufender Fade in die andere
   Richtung wird dabei mit beendet, sonst bleibt ein Mischstand stehen.
3. *Offline-Riegel statt `beendeCandidate` im Callback (D3 berichtigt).* Der
   frühere Schritt 3 rief `beendeCandidate` (`DspKern.cpp:434-443`) aus dem
   Callback und begründete das mit „arbeitet nur mit Atomics und einer
   Publikation". Das ist an der Quelle widerlegt: `:441` schreibt
   `merkzettel[(size_t) Pfad::candidate].gueltig = false;` — nicht atomar, dem
   Worker allein gehörend —, und `:442` `publiziereEnde` ruft
   `verdraengeBereiten` (`eq-copilot/plugin/dsp/DspBankPool.cpp:157-162`), das
   einen publizierten Slot per CAS freigibt; aus dem Callback hieße das, der
   Audiothread gibt eine Bank frei. Heute ist das durch genau das
   Zustandsschloss serialisiert, das T3-01-03 beanstandet (`:1325`) — erst der
   Wegfall des Schlosses erzeugte das Rennen. **Gebaut wird stattdessen ein
   Offline-Riegel:** ein Atomic, unter dem jeder Hörwunsch als Processed wirkt,
   solange es steht, und das `setNonRealtime (false)` löst. Es schließt das
   Fenster, in dem `candidateAktiv` (`DspKern.h:614`) noch steht und ein
   `setzeHoermatrix (candidate)` von außen (öffentlich,
   `eq-copilot/plugin/sonde/SondeProcessor.h:224`) den Candidate offline hörbar
   machte (`DspKern.cpp:1113-1115`). Gedeckt ist der Riegel vom Entwurf
   (`docs/FL-Nakama-Sonden-Design-Entwurf.md:2748`: „Preview, Focus und Delta
   sind im Offline-Render neutral").
4. *Buchhaltung beim nächsten Takt.* Ein zweites Atomic „Vorschauende
   angefordert" wird im Callback gesetzt; `dspKontrollTakt` (`:1447-1478`)
   verbraucht es unter dem Schloss, das es ohnehin hält, und ruft
   `transaktion->beendePreview()`
   (`eq-copilot/plugin/state/NakamaTransaktion.cpp:309-314`) → `beendeCandidate`
   (`:762`) wie heute. Am nächsten Blockrand nimmt der Kern die ENDE-Marke
   (`DspKern.cpp:537-557`); weil die Hörmatrix keinen Candidate mehr mischt
   (`:494-498`), blendet der Pfad in die Ruhe statt in den Hörhalt und dient per
   ACK aus (`:1040-1046`); der folgende Takt gibt die Bank frei (`pflege`,
   `SondeProcessor.cpp:1452`). Stand beim Schalten schon ein Hörhalt, beendet
   `beendeHoerHalt` ihn am Ende desselben Stücks (`DspKern.cpp:1389`,
   `:500-517`). Keine Bank bleibt hängen — M-46 misst das nach einem
   ausdrücklichen Takt, nicht nach einer Blockzahl.

**Warum das T3-01-04 nicht verschärft.** Für den **Klang** reicht Schritt 2
allein: ab Sample 0 des ersten Offlineblocks ist `wirksam = processed`
(`DspKern.cpp:1113-1116`), und der Ausgang kommt aus dem Committed-Pfad
(`:1263`, `:1297`). Der Candidate-Pfad rechnet weiter, ist aber nicht hörbar und
geht nicht in die Analyse (`SondeProcessor.cpp:438`, Tap `postCommitted`). R-312-3
hält damit unabhängig davon, wann `beendePreview` läuft. M-41 misst die sofortige
Hörmatrix, M-42 misst Sample 0, M-83 misst die Buchhaltung nach dem Takt.

**Was Etappe 5 an bestehenden Prüfungen ausdrücklich umstellt.**

- *Der B7-Fall `offline_render_nutzt_den_bestaetigten_zustand`*
  (`eq-copilot/plugin/tests/TransactionTestMain.cpp:2216-2234`) wird in DIESEM
  Änderungssatz umgebaut (E-312-7): die acht Verwurfsblöcke `:2228` fallen weg,
  nach `setNonRealtime (true)` (`:2227`) wird sofort ein Block gefahren und
  `gewuenschteHoermatrix() == processed` samt Bitgleichheit ab Sample 0 geprüft
  (M-42); die Prüfung `! a->previewAktiv()` (`:2231`) wandert **hinter** ein
  ausdrückliches `a->kontrollTaktFuerTest()` und gehört damit zu M-83. Grund:
  `fahreAudio` (`:1751-1768`) ruft keinen Kontrolltakt, der Worker zieht an der
  Wanduhr (`SondeProcessor.cpp:575-578`, bei leerer Analysequeue zusätzlich bis
  20 ms `:738-740`) — ohne die Umstellung hinge der bestehende, heute grüne Fall
  nach Etappe 5 an einem Wettlauf mit dem Workertakt, und die Matrixregel „keine
  Zeile hängt an Wandzeit" wäre gebrochen. `beendePreview` ist idempotent
  (`NakamaTransaktion.cpp:311`), das Ergebnis nach dem ausdrücklichen Takt
  also fest.
- *Die Matrixzeile M-04* (Etappe 2) misst ein Instrument, dem dieser
  Änderungssatz den Messgegenstand nimmt (E-312-10): `setNonRealtime` nimmt
  danach weder im Normalfall (Schritt 1) noch im Wechselfall (Schritt 4) das
  Zustandsschloss, derselbe Lauf zählt statt 200 Sperren 0. M-04 wird deshalb
  **im selben Änderungssatz** auf 0 umgestellt und fällt mit M-40 zusammen; die
  Instrumentaussage — dass der Bereich eine Sperre in `setNonRealtime`
  überhaupt sähe — trägt ab dann der Rotbeweis von M-40. Ohne diese Umstellung
  würde M-04 im Kanon rot, ohne dass ein Produktfehler vorliegt.

**Die drei irreführenden Kommentare** gehören in denselben Änderungssatz:
`SondeProcessor.h:442` (das Zustandsschloss wird sehr wohl aus dem Callback
genommen — nach dem Fix nicht mehr, und der Text sagt, was gilt),
`SondeProcessor.cpp:1181-1183` (die Aussage begründet dort eine Entscheidung mit
einer falschen Tatsache) und `SondeProcessor.h:176-179` (der Wechsel ist heute
ein 256-Sample-Fade, nach dem Fix ein Schnitt — der Text sagt es dann richtig).

**T3-01-02: der Identitätsbeweis oder der Verzicht.** Der Umbau entfernt die
zweite Sperrfamilie, indem die 112 Hostparameter statt über
`AudioProcessorValueTreeState::ParameterLayout` als schlichte
`juce::RangedAudioParameter` direkt am Prozessor hängen; damit entfällt der
`ParameterAdapter` samt seinem Hörer-Mutex. Der Beweis ist dreiteilig und läuft
**vor** dem Umbau als Golden am Basis-SHA der Etappe: (a) Zahl, Reihenfolge,
IDs, Bereiche und Defaults der Hostparameter bytegleich; (b) die Hostsicht
(`convertTo0to1`, `convertFrom0to1`) an je drei Stützstellen je Parameter
bitgleich; (c) Statebytes über speichern, laden, speichern bytegleich, und eine
Automationsfahrt mit derselben Epochenfolge (M-48, M-49). B1 misst zusätzlich
das neu gebaute Bundle (Identität NAK-30). **Der Verzicht ist begründet, wenn
einer der drei Teile nicht bytegleich herstellbar ist**: dann bleibt der APVTS,
die Etappe endet nach dem Offline-Übergang, und beide Sperrfamilien stehen in
der Registerzeile. Der Verzicht wird im Manifest festgehalten, nicht still
gebaut.

**Wortlaut der Registerzeile für die JUCE-eigene Sperre** (beim Abschluss nach
`docs/offene-punkte.md`, datiert):

> **[Härtung/Struktur · Planschritt S25k]** **Die JUCE-eigene Parametersperre
> vor dem Plugincode bleibt.** `AudioProcessorParameter::setValueNotifyingHost`
> nimmt `listenerLock`, bevor Nakamas atomarer Listener läuft; die RT-Wache
> sieht sie bauartbedingt nicht, weil sie erst mit `SondeProcessor::processBlock`
> beginnt. Ohne den gepinnten VST3-Wrapperpatch (Entwurf §44.3) ist sie nicht
> entfernbar; er ist ein eigener, identitätsnaher Bau und gehört nicht in eine
> Audit-Nacharbeit. NAK-312 Etappe 5 hat die zweite Familie (der APVTS-Adapter)
> entfernt und mit M-50 gegen Rückkehr gesichert; diese eine bleibt offen und
> wird nie wegdeklariert.

**Beine, die einzeln zu fahren sind:** erster Änderungssatz B7, dann B6 und A16;
zweiter Änderungssatz B7, B2, A16 und B1.

**Risiken und Grenze.** Nicht angefasst: der Wrapper, die Hörmatrix in der
Echtzeitrichtung, `prepareToPlay` und `releaseResources` der Sonde, die
Parameter-IDs und ihre Reihenfolge. Risiko: eine pauschale Reparatur „nie faden"
bricht M-55 von SONDE-015 — M-43 wacht darüber.

### 7.6 Etappe 6 — Gen-Fläche und Hörmarkierung

**Wo die Rücknahme sitzt: im Klassifikationswechsel, nicht im Timertick (D6 und
L3 berichtigt, E-312-8).** Der frühere Plan setzte `markierungBeenden` in
`wechsleFlaecheWennNoetig` (`eq-copilot/plugin/src/PluginEditor.cpp:833-849`).
Das trägt nicht: diese Funktion läuft aus `timerCallback` (`:244`) mit
`startTimerHz (30)` (`:230`), während `setzeBindung` die Audioseite **synchron**
auf dem Nachrichtenthread schärft (`eq-copilot/plugin/src/prozessor/State.cpp:344`
über `spiegleKlassifikation`, `:262-273`). Dazwischen liegen bis zu 33,3 ms, in
denen der Marker scharf ist und färbt; und der Flächenwechsel selbst läuft im
selben Funktionsaufruf (`PluginEditor.cpp:845-846`), das Intervallende erst über
spätere Audioblöcke — der Satz „Erst danach wechselt die Fläche" war falsch.

**Gebaut wird stattdessen:** in `spiegleKlassifikation` (`State.cpp:262-273`),
**vor** dem Store `istMainKlassifiziert.store (…)` (`:272`), prüft der Prozessor,
ob `lebenslauf.audioAusnahmeErlaubt()`
(`eq-copilot/plugin/state/NakamaLebenslauf.h:137`) sich gegenüber dem
gespiegelten Wert ändert; tut sie das, ruft er zuerst `markierung.reicheAus()`
(`eq-copilot/plugin/src/HoerMarkierung.h:411-414`) und dann erst den Store. Das
deckt **alle** Wechselwege, weil `spiegleKlassifikation` von `setzeBindung`
(`State.cpp:344`) und von beiden Zweigen von `setStateInformation` (`:192`,
`:215`) gerufen wird, und es gilt in **beide** Richtungen (E-312-8).
Randbedingungen:

1. `reicheEin` hat genau einen Publisher, den Nachrichtenthread
   (`HoerMarkierung.h:345`, `schreibIndex` ist nicht atomar). `State.cpp:346-348`
   hält fest, dass Rollenwahl und `setStateInformation` heute beide auf dem
   Message-Thread laufen; der Aufrufer hält dabei `bindungMutex` (`:264`).
   `reicheAus` allokiert nicht und nimmt keine Sperre — es schreibt einen Puffer
   und tauscht einmal (`:352-361`).
2. Damit ein Block, der die neue Klassifikation sieht, **sicher auch** die
   Rücknahme sieht, wird das Lesen von `istMainKlassifiziert` im Audiothread von
   `relaxed` auf `acquire` gehoben (`eq-copilot/plugin/src/PluginProcessor.cpp:874`);
   der Briefkasten überträgt schon mit `acq_rel` (`HoerMarkierung.h:360-361`).
   Der Store bleibt `seq_cst` wie heute. **Derselbe Änderungssatz berichtigt den
   Kommentar, den er selbst falsch macht** (R-312-14, §14.4):
   `eq-copilot/plugin/src/prozessor/State.cpp:270-271` sagt heute „Gelesen /
   wird im processBlock relaxed - dort haengt kein anderer Wert daran."; nach
   dem Bau ist beides falsch — gelesen wird mit `acquire`, und es hängt sehr
   wohl ein anderer Wert daran, nämlich die Rücknahme. Der neue Text sagt, was
   dann gilt; der übrige Kommentarblock `:264-269` und der Store `:272`
   (`seq_cst`) bleiben, wie sie sind.
3. Im Editor räumt `wechsleFlaecheWennNoetig` danach nur noch seinen
   Anzeigezustand (`markModus`, Statustext); `markierungBeenden` (`:817-826`) ist
   idempotent und bleibt als Weg für die drei Netze. Der Aus-Knopf
   (`:279`) folgt `markModus` und ist nach der Rücknahme unsichtbar — ein
   sichtbarer Aus-Knopf ohne Auftrag wäre selbst ein totes Element
   (`CLAUDE.md:51-52`, M-57 berichtigt).

**Was zugesagt wird und was nicht (NAK-47).** „Intervallende vor dem
Flächenwechsel" ist mit weichem Ausfade (80 ms, `HoerMarkierung.h:569-570`) und
Tick (33,3 ms) nicht herstellbar, ohne die Fläche zu verzögern oder hart zu
schneiden; den Schnitt verbietet NAK-47 (`:420-429`). Zugesagt wird deshalb:
**Rücknahme vor dem Flächenwechsel, der Ausfade läuft weich zu Ende, das `end`
kommt danach.** In der Richtung zu Main entsteht gar kein Ereignis (M-55), in
der Richtung weg von Main genau ein `end` zum offenen `begin` (M-84,
`:626-641`). Vor dem Rollenwechsel meldet `schalteMarkierung` (`:754-815`)
ehrlich: es fragt vor `:800` denselben Term, den das Audio fragt
(`holeKlassifikation() == Klassifikation::main`, gespiegelt in `State.cpp:272`),
reicht sonst nichts ein und sagt im Status, was los ist (M-58).

**Deterministischer Tick für die Editorhälfte.** M-55 bis M-58 messen Ausgang
und UI-Zustand gemeinsam; der Editor erbt `juce::Timer` aber **privat**
(`eq-copilot/plugin/src/PluginEditor.h:50-51`, `:71`), ein Bein kann den Tick
also nicht auslösen und hinge sonst am Nachrichtenloop und damit an der Wanduhr.
`PluginEditor.h` bekommt deshalb unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` einen
Tick-Testzugang nach dem Muster der schon vorhandenen Testzugänge (`:60-68`).
`PluginEditor.h` wird damit Ticketpfad der Etappe 6 (§7.0).

**Wo die drei Netze ticken.** Der Block `PluginEditor.cpp:270-278` (Kill- und
Freilaufmeldung, Sampleratenwache, 10-Minuten-Totmann) wandert vor die
Abzweigung `:245`, oder das `return` bei `:257` weicht einem `else`. Danach
ticken alle drei in beiden Flächen, und der Kommentar `:268-269` ist wieder
wahr — er wird im selben Satz auf den neuen Ort gezogen.

**Reset und Release (R-312-5).** `eq-copilot/plugin/src/PluginProcessor.h` bekommt
`void reset() override;` neben `:111-114`, implementiert in
`eq-copilot/plugin/src/prozessor/Hostbruecke.cpp` neben `prepareToPlay` (`:33`).
Die Override ruft einen neuen öffentlichen Einstieg der Markierung, der
`hartAus()` (`eq-copilot/plugin/src/HoerMarkierung.h:760-765`) ausführt und den
fälligen Übergang wie `vorbereiten` (`:297-320`) zurückgibt — **ohne**
`wetKapazitaet`, `wet.calloc` (`:305-306`) und `versuchTrocken.assign`
(`Hostbruecke.cpp:81`): `reset` allokiert nicht (M-60). **Ausdrücklich (H1):**
dieser Einstieg setzt `warHoerbar` und `hoerbareSamples` zurück, genau wie
`vorbereiten` (`:313-314`) und `setzeSamplerate` (`:678-679`). Bleibt
`warHoerbar` stehen, meldet schon der nächste Block ohne Erlaubnis ein zweites
`end` zum selben `begin` (`:626-641`, erreicht über `:501` und `:511-517`), und
ein Block mit Erlaubnis blendet ein, ohne `begin` zu melden (`:619` verlangt
`! warHoerbar`) — gefärbtes Audio ohne offenes Intervall, also genau das, was
`eq-copilot/plugin/src/PluginProcessor.cpp:978-979` ausschließt („Ein verlorenes
Begin darf niemals / eine scheinbar saubere Baseline erzeugen (§34.2)"). R-312-5
sagt **genau ein `end` je `begin`** zu; M-86 misst das über `reset()`,
Folgeblöcke mit und ohne Erlaubnis und ein späteres `prepareToPlay`. Danach läuft genau der
Block `Hostbruecke.cpp:97-142`, der das `end` mit `project_sample_end` null
bildet, die Sequenz nur zieht, wenn sie reist, und beim zweiten offenen
Intervall fail-closed den Überlauf meldet. Der Oversize-Riegel (`:444`,
`:308-312`) wird **nicht** gelöst — er gilt bis zum nächsten `prepareToPlay`
(M-61). `releaseResources` (`PluginProcessor.h:112`) bekommt denselben Aufruf
und darf zusätzlich seine Blockpuffer freigeben (M-64). Der Auftrag bleibt
eingereicht: `reicheEin` und der Briefkasten bleiben unberührt
(`HoerMarkierung.h:472`), und U56 ist am 21.09.2026 mit „Auftrag bleibt
bestehen" beantwortet (§10.2) — es gibt hier nichts nachzuziehen. Die zwei
Zeilen dazu fahren deshalb **verschiedene Transportlagen**, sonst schlössen sie
einander aus (H1): M-59 hält den Transport vor dem `reset()` an und misst
Bitgleichheit plus genau ein `end`; M-63 lässt ihn weiterlaufen und misst, dass
der Auftrag beim nächsten erlaubten Block ein **neues `begin`** erzeugt.

**Die Entwurfskennung des Labels.** Der Editor führt neben `sourcesAktionsZiel`
(gesetzt bei `PluginEditor.cpp:908`) eine zweite Kennung: die Quelle, für die
der aktuelle Feldinhalt gilt. Sie wird gesetzt, wo der Text geladen wird
(`:934-936`). `uebernehmeSourcesLabel` (`:874-899`) vergleicht sie bei `:878`
mit dem Aktionsziel und schreibt nur bei Gleichheit.
`aktualisiereSourcesSteuerung` (`:902-944`) entscheidet beim Zielwechsel
ausdrücklich über den alten Entwurf.

**Die am Code abgelesene Fokusverlust-Regel.** `onReturnKey` und `onFocusLost`
(`:209-210`) rufen beide `uebernehmeSourcesLabel` — die heutige Regel ist
**bestätigen**, nicht verwerfen. R-312-9 wendet genau diese Regel auf die
Startquelle an: beim Zielwechsel wird der Entwurf noch unter **A** abgeschlossen
und danach Bs Label geladen. Damit verliert der User nichts, und nichts landet
falsch. Die Bedingung bei `:934` scheitert danach nicht mehr am Fokus, sondern
am Kennungsvergleich (M-66 bis M-69).

**Die Anzeigeordnung als Wahrheit der sichtbaren Aktion.** `stelleZielSicher`
(`eq-copilot/plugin/src/SourcesModel.cpp:1772-1777`) wählt seinen Ersatz nach
derselben Ordnung, in der die Fläche zeichnet (`:1651-1663`), und nur unter den
Zeilen, die `sourcesZeilen()` (`PluginEditor.cpp:951-967`) wirklich herstellt.
Die Zeilenzahl wird dabei **aus der Geometrie gerechnet**, nie als 20
hingeschrieben: bei der in `CLAUDE.md` genannten Standardgröße wären es
mehr, und eine hart geschriebene Zahl verbaute jede spätere Fläche (M-73 bis
M-75). Gespeicherte Quellen werden nie gelöscht (M-76). Der Kommentar
`PluginEditor.cpp:853-854` bleibt inhaltlich richtig und wird nicht angefasst.

**Die Aktionssteuerung bindet nur an eine gezeichnete Zeile (D7 berichtigt,
E-312-9).** `stelleZielSicher` allein trägt R-312-6 nicht: es greift **nur**,
wenn das Hauptziel fehlt (`SourcesModel.cpp:1774-1775`). Existiert es und rückt
durch Umordnung hinter die letzte gezeichnete Zeile — eine Quelle mit kleinerem
Sortierschlüssel tritt bei, oder ein Hostbusname ändert sich; der sichtbare Name
folgt dem Labelfeld nur, wenn kein gültiger Hostbusname gemeldet ist
(`:1781-1789`) —, dann sucht `aktualisiereSourcesSteuerung` es heute weiter über
die **ganze** Liste (`PluginEditor.cpp:905-907`) und schaltet
`sourcesAktionKnopf` (`:919`) und `sourcesLabelFeld` (`:933`) sichtbar: ein
scharfer Knopf auf einer ungezeichneten Quelle. **Gebaut wird die kleinste
Regel:** die Suche läuft nur über
`sourcesAnzeige.quellen[0 … sourcesZeilen().size())` — dieselbe Rechnung wie
Zeichnen (`:1135`) und Klick (`:980`). Liegt das Ziel dahinter, gilt
`hatZiel = false`, Knopf und Labelfeld sind unsichtbar, `sourcesAktionsZiel` ist
leer, und **das Modell bleibt unverändert**: kein automatischer Zielwechsel auf
die erste gezeichnete Zeile, denn das wäre ein Modellwechsel ohne Handgriff
(SONDE-012 U02, `docs/beweise/SONDE-012.md:231`: „Auswahl oder Wegfall wird als
ein Modellwechsel sichtbar"). Ein Klick auf eine gezeichnete Zeile wählt wie
heute ein neues Hauptziel (`:984`). Keine neue Oberfläche. **Zusammenspiel mit
R-312-9:** verschwindet das Labelfeld mit einem offenen Entwurf, löst das
`onFocusLost` aus (`:210`), und die heutige Regel dort ist bestätigen (`:209-210`
rufen beide `uebernehmeSourcesLabel`). Der Entwurf muss deshalb **nach R-312-9
abgeschlossen werden, bevor `sourcesAktionsZiel` geleert wird** (`:908-909`) —
sonst fällt er in den stillen Rückweg bei leerem Ziel (`:876-877`) und verfällt
ohne Mutation und ohne Meldung. M-74 misst den Umordnungsfall mit 25 Quellen,
M-85 den offenen Entwurf darin.

**Beine, die einzeln zu fahren sind:** A3 `EqCopMarkierungTest`, danach B15
`EqCopShot`, B14 und B13.

**Risiken und Grenze.** Nicht angefasst: die Fläche selbst (Designteil geparkt),
der weiche Ausfade der Markierung, der Interventionsvertrag, der Statevertrag,
das Quellenmodell jenseits der Ersatzwahl; in `State.cpp` nur die Rücknahme vor
dem Store in `spiegleKlassifikation` (`:262-273`), nicht die Löschstelle
`:321-322` (das ist T3-02-06, Etappe 7). Risiko: die Rücknahme
darf den weichen Ausfade nicht in einen harten Schnitt verwandeln
(`HoerMarkierung.h:420-429`, NAK-47) — M-55 misst den Ausgang, nicht nur den
UI-Zustand.

### 7.7 Wirkung auf Etappe 7: wo die Etappen 2 bis 6 den Weg offen halten

- **W02 vor T3-02-06 (U49).** Die Quittierung sitzt im **Ladepfad**
  (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1051-1106`), T3-02-06 im
  Schreib- und Rollenpfad (`eq-copilot/plugin/src/prozessor/State.cpp:320-323`,
  `eq-copilot/plugin/state/NakamaState.cpp:1009-1012`). Beide Antworten auf U49
  bleiben baubar: Weg 1 braucht ein neues, versioniertes Kind im Zustandsbaum
  (`eq-copilot/schemas/state/nakama-state-v2.md:104`) und setzt auf denselben
  Leser auf; Weg 2 erweitert nur die Löschstelle. **Verbaut würde der Weg**,
  wenn W02 den Ladestart mit einer neuen Stateversion löste statt mit einer
  Quittierung vorhandener Felder — deshalb tut er das nicht.
- **R-312-5 vor U56.** Etappe 6 beendet Klang und Intervall und lässt den
  **Auftrag** ausdrücklich stehen (M-63). Antwort (1) von U56 braucht danach gar
  nichts mehr; Antwort (2) ist ein Nachzug im selben `reset`-Pfad. **Verbaut
  würde der Weg**, wenn Etappe 6 `ausGewuenscht` setzte — das wäre Antwort (2)
  ohne Frage.
- **R-312-6 vor U51.** Das Ersatzziel folgt der Anzeigeordnung, und die
  Zeilenzahl wird gerechnet, nicht geschrieben. Damit bleiben Weg 1 (Annahme
  begrenzen, die Grenze liest die Geometrie) und Weg 2 (blätterbare Liste, erst
  nach der Designarbeit baubar) offen. **Verbaut würde der Weg** durch eine
  feste 20 im Modell.
- **Die Override-Stelle des Hostbypass für U48.** Die Etappen 2 bis 6 fassen
  `getBypassParameter` und `processBlockBypassed` **nicht** an; beide bleiben in
  `eq-copilot/plugin/sonde/SondeProcessor.h` und
  `eq-copilot/plugin/src/PluginProcessor.h` unüberschrieben, und die
  RT-Wache misst den Bypasspfad nicht, weil `processBlock` dort nicht läuft —
  das ist die Lage, die U48 entscheidet, kein Riss. Zwei Stellen könnten sie
  vorentscheiden und tun es deshalb nicht: die Parameteranbindung der Etappe 5
  zeigt `getBypassParameter()` **nicht** auf `v1.global.bypass` (das schriebe
  FLs Slotknopf in den `state_hash` und meldete Host-Dirty), und die harte
  Hörmatrixumstellung der Etappe 5 wird **nicht** an `processBlockBypassed`
  gekoppelt.
- **T3-02-06 selbst** bleibt außerhalb dieser Etappen. Berichtigt in der
  Matrixnacharbeit 1: Etappe 6 ändert `State.cpp` jetzt sehr wohl — sie setzt
  die Rücknahme des Markierungsauftrags vor den Store in
  `spiegleKlassifikation` (`:262-273`, E-312-8) und macht die Datei damit zum
  Ticketpfad (§7.0). **Nicht angefasst bleibt die Löschstelle** `:321-322`
  (`zustand.mainProjectMitglieder.clear()` beim Klassenwechsel), und genau sie
  ist der Gegenstand von T3-02-06. Beide Antworten auf U49 bleiben baubar: die
  Rücknahme liegt vor dem Store und berührt weder Bestand noch Zustandsbaum.

## 8. Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Etappe 1

Etappe 1 baut nichts; Punkte, die auf Code zielen, sind **nicht zutreffend**.
Wo die Matrix sie für die Bauetappen anlegt, steht die Zeile dabei.

| Abschnitt | Punkt | Etappe 1 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Schlüssel, Lesepfad, Zähler, beide Sprachen | nicht zutreffend; kein Gegenstand berührt eine Queue. Die Analysequeue wird nur gelesen (`eq-copilot/plugin/sonde/SondeProcessor.cpp:466`, `:475`, `:487`) und von keiner Zeile verändert |
| B Lebenszyklus | Reihenfolge beim Verbinden und Trennen, Joins mit Frist, Stopp-Fenster, Close-Flag vor Inhalt | nicht zutreffend (kein Code); vollständig angelegt in M-30 bis M-39: Reihenfolge im Destruktor M-34, Frist M-32, Registrierung im Stopp-Fenster M-30, Reconnect M-33; M-39 ordnet die Frist seit der Nacharbeit 1 über beobachtbare Schleusenzustände statt über die Wanduhr (E-312-10), und M-30, M-31 und M-39 laufen ihre Rotbeweise UB-frei über Schranke und Marke (R-312-12). Dazu das Trio starten↔stoppen bei Gen M-59 bis M-66 sowie M-84 (Rollenwechsel weg von Main) und M-86 (genau ein `end` je `begin` über `reset()`) |
| C Verträge und Längen | Grenzwerte an jeder Zahl über den Draht, NaN und Inf, exakte Feldmenge | nicht zutreffend (kein Code und keine Vertragsänderung); die Zahlenränder sind als eigene Zeilen angelegt: NaN und ±Inf M-18, Zählerüberlauf der Quittierung M-19 und der Schiedsregel M-82, Blockgrößen M-22 und M-47, Frist 2 s M-39 (über Zustände geordnet), Parameterindex M-54, Labellänge M-71, Quellenzahl M-73 und M-75 |
| D Bau- und Prüfriegel | fail-closed ohne Rohtextheuristik, Unbekanntes ist ROT | angewandt: der Testkonstruktor der Sonde ist fail-closed (M-05), der Überlaufpfad der Markierung bleibt fail-closed (M-62), der Textriegel von M-50 ist rot, wenn das Verzeichnis fehlt oder Dateien fehlen |
| D Bau- und Prüfriegel | Frische der Eingaben | angewandt: Binaryzeitstempel gegen die jüngste Quelle bei jedem Einzelbein und jedem Rotbeweis (§7.0, §7.1; NAK-230) |
| D Bau- und Prüfriegel | was der Kanon nicht baut, bezeugt er nicht als frisch | angewandt: jeder neue Fall liegt in einem Ziel, das der Kanon baut und fährt; das neue Sonde-Lebenslaufbein bekommt eine eigene Zeile in `tools/beweise.ps1` (§7.2), nie einen stillen Zusatzlauf |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test, Runnerbehauptung kurz | angewandt, in der Nacharbeit 1 berichtigt (D5): den Satz über den Bereich ab dem Eintritt in `SondeProcessor::processBlock` bekommen nur die Beine, die eine Sonde bauen — B7, A16 und das neue Sonde-Lebenslaufbein. **B6 bekommt ihn nicht**: sein Ziel übersetzt allein `tests/DspGoldenTestMain.cpp` (`eq-copilot/plugin/CMakeLists.txt:1148-1154`), es kann keinen `SondeProcessor` anlegen, und sein Zähler misst ab `DspKern::verarbeite` (§7.2). Das neue Bein bekommt eine Behauptung über genau seinen Lebenslauffall; R-312-1 verlangt die Grenze ausdrücklich. In der Nacharbeit 2 berichtigt (D-B, E-312-11): eine Zeile nennt als Zähler nur, was ihr Bein wirklich liest — B7 bindet dafür in Etappe 2 seinen `operator new` an `RtWache::meldeAllokation()` (§7.2), und keine Rotbeweisspalte behauptet einen Anstieg von `RtWache::sperren()` für eine Sperre, die nicht über `RtWache::GemeldeteSperre` genommen wird |
| E Behauptung ≤ Messung | Zahlen gemessen, nicht abgeschrieben | angewandt: −8,99999944679 dB und +6,00000017258 dB als „gemessen" aus Phase 16 C gekennzeichnet, die Offlinerechnung als „nachgerechnet"; Zeilenenden am 21.09.2026 mit `git ls-files --eol` gemessen (§7.1). In der Nacharbeit 1 an der Quelle nachgezählt statt geschätzt: das Overlay lässt **80** Hostparameter durch (69 kontinuierliche, 11 boolesche; `eq-copilot/plugin/state/NakamaParameter.cpp:49-81`), der Blockrand erreicht davon **vier**, und der Workertakt ist **5 bis 20 ms**, nicht fest 5 ms (`eq-copilot/plugin/sonde/SondeProcessor.cpp:575-578` und `:738-740`) — daran hingen die Etiketten von M-22 und M-27. Die Zahl der kontinuierlichen Hostparameter wird nicht mehr offengelassen, sondern die abgedeckte Menge namentlich genannt (§6.3, §7.3) |
| E Behauptung ≤ Messung | lebender Kopf beim Abschluss, Verlauf append-only | angewandt: §5 bis §8 angehängt; im Kopf nur die Zeilen Etappe und Basis-SHA nachgezogen |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen; eine Probe, die ohne den Fix rot ist, heißt Regressionswache | angewandt: vier Etiketten (§6), Ablauf §7.1; jede Zeile nennt die Mutation an der Zeile, die die Zusage trägt. In der Nacharbeit 1 nachgezogen: kein Rotlauf in undefiniertem Verhalten (R-312-12, §7.1 — Schranke und Marke getrennt, M-30, M-31, M-39); der Rotbeweis von M-38 fällt jetzt an der Zusagezeile statt an einer für den Zähler unsichtbaren Stelle (H3); der von M-46 misst die Bankfreigabe am Kontrolltakt statt an einem Callbackpfad, den E-312-7 gar nicht baut; M-04 ist an Etappen gebunden, M-22 und M-27 tragen das Etikett, das am Code stimmt. In der Nacharbeit 2 nachgezogen: M-38 fällt über `RtWache::allokationen()` und behauptet keinen Sperrenanstieg mehr, es trägt „zwei Hälften" mit ausdrücklich mutationsfreier Schleusenhälfte (D-B, E-312-11, H-A); M-81 nennt als Folge seiner Mutation nur, was der Bauplan hergibt, und trägt ebenfalls „zwei Hälften" (D-C, E-312-12) |
| E Behauptung ≤ Messung | Writer-Fixtures statt Handschrift | angewandt: das Parametergolden erzeugt der unveränderte Prozessor am Basis-SHA, die Shots erzeugt `EqCopShot`, die Fremd-Major-Fixture bleibt unverändert (§7.1) |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: je Etappe Runnerbehauptung, Kopfkommentar der betroffenen Quelle und Manifestkopf (§7.2 bis §7.6); die drei irreführenden Kommentare aus §1 sind je ihrem Änderungssatz zugeordnet. In der Nacharbeit 2 nachgezogen (L-A, R-312-14): die Regel gilt nicht nur für diese drei, sondern für **jeden** Kommentar, den ein Änderungssatz selbst falsch macht — der erste Fall ist `eq-copilot/plugin/src/prozessor/State.cpp:270-271`, den das `acquire`-Lesen der Etappe 6 falsch macht; §7.6 Punkt 2 verlangt seine Berichtigung im selben Änderungssatz |
| F Änderungssatz | Beziehungen im selben Commit; Writer, Reader und Fixtures eines Vertrags zusammen | angewandt: Beziehungen in §6 gruppiert; W02 ein Satz, T3-01-05 ein Satz, T3-01-03 mit T3-01-04 ein Satz, T3-07-02 mit dem sicheren Teil von T3-01-10 eine Etappe (§4, §7.3, §7.5, §7.6). In der Nacharbeit 1 ergänzt: die Umstellung von M-04 und des bestehenden B7-Falls liegt im selben Änderungssatz wie der Offline-Fix (§7.5), und die Rücknahme in `spiegleKlassifikation` samt dem `acquire`-Lesen im Audiothread liegt im selben Änderungssatz wie der Flächenwechsel (§7.6) — Publisher und Leser einer Ordnungsregel gehören zusammen. Kein Vertrag wird geändert, deshalb kein Fixture-Satz |

### 8.1 Selbstaudit und eigene Messungen

- **Gelesen** in der Reihenfolge des Auftrags: dieses Manifest §0 bis §4;
  `docs/beweise/roh/NAK-312-quellvalidierung-teil-A.md` und
  `docs/beweise/roh/NAK-312-quellvalidierung-teil-B.md` vollständig; aus
  `docs/audits/2026-09-15-tiefenaudit/BEFUNDE.md` die zwölf Befundzeilen der
  technischen Gegenstände, ihre Skeptikerzeilen der Phase 10 und die Zeilen W02,
  W05, W06, W09, W22, W23, W24 der Arbeitsübersicht; die Rohberichte
  `docs/audits/2026-09-15-tiefenaudit/roh/phase-16-c-persistenz.md`,
  `docs/audits/2026-09-15-tiefenaudit/roh/phase-16-e-last-lebenslauf.md`,
  `docs/audits/2026-09-15-tiefenaudit/roh/phase-16-f-gegenpruefung-c.md` und
  `docs/audits/2026-09-15-tiefenaudit/roh/phase-16-abdeckung.md`; die
  Anforderungsquellen aus §5 an ihrer Stelle; `docs/gesundheit/KONZEPT.md` §3;
  `docs/plugin-wissen.md` und `docs/offene-punkte.md` (NAK-312, NAK-179,
  NAK-246); der Code an den in §5 bis §7 genannten Stellen; die Beine in
  `tools/beweise.ps1`; `tools/dirigent/pruefliste.md` als Arbeitsliste;
  `tools/plan/dokuriegel.py` für seine Regeln.
- **Eigene Messungen, lesend:** `git rev-parse HEAD` (`945ebfb1`),
  `git status --short` (nur `briefing-hub/` und `nimbalyst-local/`),
  `git diff --stat 090d0fa1 HEAD -- eq-copilot broker tools` (leer),
  `git diff --stat 2d6519e8 HEAD -- eq-copilot broker tools` (eine Zeile in
  `eq-copilot/install/nakama-installer-v1.json`, kein Produktpfad),
  `git ls-files --eol` der Dateien aus §7.1, Grep und Lesen an den Quellen.
- **Kein Bau, kein Test, keine Repro, kein FL Studio.** Das einzige gefahrene
  Skript ist `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` vor dem
  Commit.
- **Selbstaudit, im Lauf berichtigt.** Vier Zeilenangaben aus den
  Quellvalidierungen habe ich an der Quelle anders vorgefunden und hier
  berichtigt: die Abmeldeschleife im Sondendestruktor steht bei
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:221-222`, nicht bei `:216-217`;
  die Längen- und Hauptzielwache von `benenneSourcesHauptziel` steht bei
  `eq-copilot/plugin/src/prozessor/Ipc.cpp:1208` und `:1217`, der No-op-Riegel
  bei `:1222-1223` (Teil B nennt `:1207` und `:1221`); die additive
  Erhaltungsregel des Statevertrags steht bei
  `eq-copilot/schemas/state/nakama-state-v2.md:104`, nicht bei `:103`. Dazu eine
  sachliche Berichtigung am Bauplan: `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp`
  bekommt sein `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` aus der Schleife
  `eq-copilot/plugin/CMakeLists.txt:1323-1330`, nicht aus seinem eigenen
  Zielblock — der Testkonstruktor der Sonde gehört deshalb unter dasselbe Define
  wie bei Gen und nicht in einen Bau ohne Define. Jede Matrixzeile trägt elf
  Trenner und keinen ungeschützten senkrechten Strich; jeder neue Pfad ist als
  Zielpfad benannt; §6.8 ist am Text gezählt.

## 9. Messung der Etappe 1 und Entscheide (Dirigent, 21.09.2026, 01:0x Uhr)

### 9.1 Messung

Worker `8cbfe878` (Opus, Effort xhigh, `dontAsk`), 00:12 bis 00:5x Uhr, Basis
`945ebfb1a0a2d3ac5e97e937997c0f0532ef16f8`. Ergebnis: ein Commit
`2deb5e3dfe3273dc1e5b8abcbea1ad79f7862081` = `origin/master`, Baum sauber (nur
die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD,
`git diff --stat 945ebfb1..2deb5e3d`: eine Datei, `docs/beweise/NAK-312.md`,
+1210/−2. Rundenbilanz `945ebfb1..2deb5e3d`: Doku 1 Datei +1210/−2, Produkt und
Tests 0 Zeilen — Matrixrunde, zählt für das Rundenbudget null.

### 9.2 Entscheide zu den Vorlagen des Bauplans

- **E-312-1 (R-312-10, Mechanismus).** Gebaut wird Weg 2 aus §7.3: die
  Blockrandübernahme im Kern aus der Hostmailbox, nur wenn `hostEreignisOffen`
  steht, nur für die nichttopologischen kontinuierlichen Hostparameter, mit dem
  monotonen Ereigniszähler als Schiedsrichter zwischen Blockrand und
  Workerpublikation. Weg 1 erfüllt die Zusage nicht. Ein Ereignisring wird nicht
  gebaut (ohne den Wrapperpatch wirkungslos).
- **E-312-2 (Warten im Offline-Render).** Abgelehnt. Das Grundgesetz nennt den
  Thread, nicht die Frist; Weg 2 macht das Warten unnötig; keine Matrixzeile
  darf es voraussetzen.
- **E-312-3 (neue Pfade).** Genehmigt mit Grenze:
  ein neues Bein mit der Testquelle `SondeLebenslaufTestMain.cpp` unter
  `eq-copilot/plugin/tests/`, dem Ziel `EqCopSondeLebenslaufTest` in
  `eq-copilot/plugin/CMakeLists.txt` und seiner Deklaration in
  `tools/beweise.ps1`; der Testkonstruktor der Sonde liegt unter demselben
  Define wie bei Gen; kein Test berührt die Produktions-Pipe. Je Rotbeweis eine
  Rohdatei unter `docs/beweise/roh/`. Kein weiterer neuer Quelltextpfad.
- **E-312-4 (T3-01-02).** Der dreiteilige Identitätsbeweis aus §7.5 ist die
  Bedingung des Umbaus; ist ein Teil nicht bytegleich herstellbar, gilt der
  Verzicht, im Manifest begründet. Der Wortlaut der Registerzeile für die
  JUCE-eigene Sperre ist angenommen und wird beim Abschluss datiert eingetragen,
  angepasst an das, was Etappe 5 tatsächlich gebaut hat.

### 9.3 Auftrag Matrixprüfung 1

Frischer, lesender Opus-Thread, Effort max (Codex-Woche 94 %, Kopfzeile
Prüfmodell); Auftrag `docs/beweise/roh/NAK-312-matrixpruefung-1-auftrag.txt`,
Bericht nach `docs/beweise/roh/NAK-312-matrixpruefung-1-urteil.md` (Zielpfad,
noch nicht angelegt). Prüfbereich:
`git diff 945ebfb1...2deb5e3d -- docs/beweise/NAK-312.md` (§5 bis §8), Gate-Bezug
§0 bis §4 und §9.2.

## 10. Matrixprüfung 1 und Antworten des Users (Dirigent, 21.09.2026, 01:4x Uhr)

### 10.1 Matrixprüfung 1: NEEDS_WORK (Rohurteil)

Frischer, lesender Opus-Thread `97cc511f`, Effort max, 00:49 bis 01:2x Uhr, HEAD
`b7d82137633a28e9ac42099b6568a0b44bd12b0f` vor und nach dem Lauf gleich (vom
Prüfer und vom Dirigenten gemessen); geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-matrixpruefung-1-urteil.md`. Rohurteil: **NEEDS_WORK —
neun Defekte (D1 bis D9), drei Lücken (L1 bis L3), drei Härtungen (H1 bis H3).**
Kurzfassung der Defekte: D1 die Blockrandübernahme (Weg 2, E-312-1) deckt nur
die fünf globalen Rampenziele, nicht die Bandwerte, die nur über ein Programm
wirken — R-312-10 bliebe für Bandautomation gebrochen; D2 der Schiedsrichter von
Weg 2 legt einen Ordnungsvergleich auf einen überlaufenden Zähler, den M-19 an
derselben Zeile für rot erklärt; D3 `beendeCandidate` aus dem Callback berührt
den Merkzettel, der allein dem Worker gehört; D4 M-41 ist als Regressionswache
geführt, obwohl der Bau genau diese Eigenschaft verschiebt; D5 B6 bekäme eine
Behauptung über einen Prozessor, den B6 nicht baut; D6 M-55 verlangt ein `end`
zu einem `begin`, das in ihrem Szenario nie entsteht, und der Fix im Timertick
lässt ein Fenster; D7 M-74 verlangt eine Änderung an der Aktionssteuerung, die
der Bauplan nicht baut; D8 M-04 und M-40 behaupten für dieselbe Messung 200 und
0; D9 M-39 hängt an einer Wanduhrkoinzidenz. Das Rohurteil ist noch nicht
eingeordnet: jeden Befund validiert ein frischer, lesender Opus-Thread, Effort
max, an der Quelle (Auftrag
`docs/beweise/roh/NAK-312-matrixpruefung-1-validierung-auftrag.txt`, Bericht
nach `docs/beweise/roh/NAK-312-matrixpruefung-1-validierung.md`, Zielpfad, noch
nicht angelegt); Teil 2 des Auftrags legt dem Dirigenten die Wege für D1 bis D9
mit Belegen vor. Die Einordnung und die Regeln folgen in §11.

### 10.2 Antworten des Users auf die vier Karten (21.09.2026)

Der User hat die Karten in der Nacht selbst angefordert. Gestellt während der
laufenden Matrixprüfung, eingetragen nach ihrem Ende (kein Commit während des
Prüflaufs); je Karte ein Commit.

| Karte | Befund | User-Wort, wörtlich gewählt | Folge | Abnahme |
|---|---|---|---|---|
| U48 | T3-01-09 | „Weich + Messung läuft (Empfohlen)“, 01:01 Uhr | FLs Slot-Bypass blendet weich wie Nakamas Schalter, Probeeq misst im Hostbypass weiter; Etappe 7 | `design/abnahmen/2026-09-21-fl-bypass-weich-messung-laeuft-u48.md` |
| U49 | T3-02-06 | „Alles bleibt erhalten (Empfohlen)“, 01:01 Uhr | Bestände überstehen den Rollenwechsel live und über Save und Load; neues versioniertes Kind im Zustandsbaum; Etappe 7 nach W02 | `design/abnahmen/2026-09-21-rollenwechsel-daten-bleiben-erhalten-u49.md` |
| U51 | T3-07-05 | „Auf 20 begrenzen (Empfohlen)“, 01:02 Uhr | Gen nimmt höchstens 20 Probeeq an, die 21. meldet ehrlich „Liste voll“; gespeicherte Quellen werden nie gelöscht; Etappe 7 | `design/abnahmen/2026-09-21-quellenannahme-auf-20-begrenzt-u51.md` |
| U56 | T3-01-10 | „Auftrag bleibt bestehen (Empfohlen)“, 01:02 Uhr | R-312-5 deckt es in Etappe 6; der Nachzug in Etappe 7 entfällt | `design/abnahmen/2026-09-21-markierungsauftrag-bleibt-nach-anhalten-u56.md` |

Damit hängt kein Gegenstand dieses Tickets mehr an einer offenen Antwort.
Etappe 7 bekommt ihren eigenen Matrixnachtrag, sobald die Matrix der Etappen 2
bis 6 geprüft ist.

## 11. Einordnung der Matrixprüfung 1 und Regeln (Dirigent, 21.09.2026, 02:3x Uhr)

### 11.1 Validierung an der Quelle

Frischer, lesender Opus-Thread `47dddc88`, Effort max, 01:20 bis 02:2x Uhr, HEAD
`bc32f3c993bdf10e0b03a7610dcd5ade2fb3c471` vor und nach dem Lauf gleich, nur der
Bericht `docs/beweise/roh/NAK-312-matrixpruefung-1-validierung.md` geschrieben:
**8 bestätigt, 7 präzisiert, 0 widerlegt.** Stichprobe des Dirigenten:
`eq-copilot/plugin/src/PluginEditor.cpp:840-846` führt `hinweisKnopf` und
`markierungAusKnopf` in der Legacy-Liste, die in der Main-Fläche unsichtbar ist.

### 11.2 Einordnung

| Befund | Urteil der Validierung | Einordnung des Dirigenten | Folge |
|---|---|---|---|
| D1 | BESTÄTIGT (80 statt 69 durchgelassene Parameter) | **DEFEKT** gegen R-312-10 und E-312-1 | E-312-5, R-312-10 neu gefasst, Folgeticket NAK-340 |
| D2 | BESTÄTIGT | **DEFEKT** gegen 312/M-19 | E-312-6 |
| D3 | PRÄZISIERT (zweite Besitzregel im Pool) | **DEFEKT** gegen die Besitzregel in `eq-copilot/plugin/dsp/DspKern.h` und `DspBankPool.h` | E-312-7 |
| D4 | PRÄZISIERT | **DEFEKT** (Etikett und Wanduhrabhängigkeit) | E-312-7 |
| D5 | BESTÄTIGT | **DEFEKT** gegen R-312-1 | jede Behauptungszeile nennt nur den Bereich, den ihr Bein öffnet |
| D6 | BESTÄTIGT | **DEFEKT** | E-312-8, R-312-8 neu gefasst |
| D7 | PRÄZISIERT | **DEFEKT** gegen R-312-6 | E-312-9 |
| D8 | BESTÄTIGT | **DEFEKT** (zwei Etappen, eine Funktion) | E-312-10 |
| D9 | PRÄZISIERT | **DEFEKT** gegen die Matrixregel „keine Zeile hängt an Wandzeit" | E-312-10 |
| L1 | PRÄZISIERT, umgestuft | **DEFEKT** (Bauplansatz widerspricht dem Code) und Lücke → **R-312-11** | R-312-11 |
| L2 | BESTÄTIGT | **LÜCKE → R-312-12** | R-312-12 |
| L3 | PRÄZISIERT, umgestuft | **DEFEKT** (Pflichtgegenbeispiel fehlt) | E-312-8 |
| H1 | PRÄZISIERT | **DEFEKT** — R-312-5 sagt „genau ein `end` je `begin`" zu; ein zweites `end` im nächsten Block bricht das | in die Nacharbeit |
| H2 | BESTÄTIGT | **HÄRTUNG** → Register NAK-342 | kein Auftrag |
| H3 | BESTÄTIGT | **DEFEKT** in der Sache der Randbeobachtung: der Rotbeweis von 312/M-38 kann nicht fallen (Regel „Rotbeweis an der Zusagezeile"); die fehlende Nennung von B7 allein wäre Kosmetik | in die Nacharbeit |

Randbeobachtungen der Validierung, die der Dirigent als Defekte derselben Klasse
in die Nacharbeit gibt, weil sie Rotbeweise oder Etiketten betreffen: die
Etiketten von 312/M-22 und 312/M-27 („Regressionswache, heute schon bitgleich"
trifft nicht zu), der Rotbeweis von 312/M-46 fällt nicht, 312/M-59 gegen
312/M-63, die Zusage von 312/M-57 (ein sichtbarer Aus-Knopf ohne Auftrag wäre
selbst ein totes Element), die fehlenden Ticketpfade
`eq-copilot/plugin/src/prozessor/State.cpp` und
`eq-copilot/plugin/src/PluginEditor.h` in §7.0.

### 11.3 Entscheide und Regeln

- **E-312-5 (D1; ersetzt E-312-1 im Umfang).** Gebaut wird Weg (C) der
  Validierung: die Blockrandübernahme gilt in diesem Ticket für genau vier
  namentlich genannte Hostparameter — `input_trim_db`, `output_trim_db`, `width`,
  `v2.global.mix` —, deren Rampenziele der Kern am Blockrand selbst setzt.
  **R-312-10 lautet für dieses Ticket:** für diese vier liefert derselbe Event-
  und Blockverlauf offline und in Echtzeit dieselbe Ausgabe innerhalb der
  Toleranz von M-120; der Blockrand rechnet dieselbe Zelle wie `zelleAusHost`
  einschließlich ihrer Ausnahme, wofür die bestätigten Werte der vier Parameter
  dem Callback ohne Sperre vorliegen müssen. Eine Grenzzeile der Matrix hält
  fest, dass die 64 Bandwerte, `mono_bass_hz`, die elf booleschen Parameter und
  das Auto-Gain-Ziel taktgebunden bleiben. Das ist **keine dauerhafte
  Verengung der Zusage**: M-120 und der Entwurfssatz bleiben für Bandwerte
  gebrochen und offen geführt; den Rest schließt das Folgeticket NAK-340
  (Planschritt S25p, Blockrandentwurf der Bandwerte im Kern mit eigener
  Besitzregel, eigener Matrix und eigener T2-Prüfung). T3-01-05 wird geteilt:
  Teil a hier, Teil b in NAK-340. Weg (B) wird verworfen (Gleichheit ohne
  Warten nicht zusagbar); E-312-2 (kein Warten) bleibt.
- **E-312-6 (D2).** Schiedsregel nach Validierung Teil 2 Punkt 2: Vergleich auf
  Gleichheit, nie auf Ordnung (312/M-19 bleibt wörtlich); der Blockrand liest
  die Zähler der vier Parameter in jedem Block und vor der Programmübernahme;
  jede Publikation trägt je abgedecktem Parameter den Zählerstand, aus dem ihr
  Wert stammt, und setzt ein Rampenziel nur bei Gleichheit mit dem
  Blockrandstand; `hostEreignisOffen` bleibt allein beim Worker; der Speicher
  (je vier 32-Bit-Stände im Kern und je Programm) wird im Bauplan genannt statt
  „kein Speicherzuwachs"; eine Zählerrand-Zeile dicht unter dem Überlauf.
- **E-312-7 (D3, D4).** Beim Offline-Wechsel berührt der Callback nur eigene
  oder geteilte Atomics: hartes Schalten der Hörmatrix, das der Audiothread im
  eigenen Zustand verbraucht, ein Offline-Riegel, unter dem jeder Hörwunsch als
  Processed wirkt (Entwurf: Preview im Offline-Render neutral), und ein Atomic
  „Vorschauende angefordert". Merkzettel, Publikation, `beendeCandidate` und die
  Bankfreigabe bleiben beim Worker im nächsten Kontrolltakt. 312/M-41 trennt die
  sofortige Hörmatrix (ab Sample 0) von der Buchhaltung; `previewAktiv()` wird
  nur nach `kontrollTaktFuerTest()` geprüft; der bestehende B7-Fall wird in
  Etappe 5 ausdrücklich so umgestellt; der Rotbeweis von 312/M-46 misst die
  Freigabe am Takt.
- **E-312-8 (D6, L3; R-312-8 neu gefasst, beide Richtungen).** „Jeder Wechsel
  der Klassifikation von Gen — zu Main und weg von Main, über `setzeBindung`
  oder `setStateInformation` — nimmt einen eingereichten Markierungsauftrag
  zurück, bevor die neue Klassifikation im Audiothread wirkt. Ein hörbarer
  Marker endet über den weichen Ausfade mit genau einem `end` zu seinem `begin`;
  ein nie hörbarer erzeugt kein Ereignis und färbt kein Sample; danach wird kein
  Marker ohne neuen Handgriff wieder wirksam. Die drei Netze (Kill,
  Sampleratenwache, Totmann) ticken in jeder Fläche; kein Handgriff wirkt,
  dessen Bedienelement nicht sichtbar ist. Keine neue Oberfläche." Ort: im
  Klassifikationswechsel auf dem Nachrichtenthread vor dem Store in
  `spiegleKlassifikation`; das Lesen der Klassifikation im Audiothread wird so
  geordnet, dass ein Block, der die neue Klassifikation sieht, auch die
  Rücknahme sieht. Der harte Schnitt bleibt verboten (NAK-47): zugesagt wird
  „Rücknahme vor dem Flächenwechsel, der Ausfade läuft weich zu Ende, `end`
  danach", nicht „Intervallende vor dem Flächenwechsel". Szenarien S1 (zu Main)
  und S2 (weg von Main, dort entsteht das `begin` wirklich) aus der Validierung
  Teil 2 Punkt 4. `State.cpp` und ein Tick-Testzugang in `PluginEditor.h` (Muster
  der vorhandenen Testzugänge) werden Ticketpfade der Etappe 6. Produktwirkung
  der Regel: Register NAK-341.
- **E-312-9 (D7).** Die Aktionssteuerung bindet nur an ein Hauptziel innerhalb
  der gezeichneten Zeilen (dieselbe Rechnung wie Zeichnen und Klick); liegt es
  dahinter, sind Knopf und Labelfeld unsichtbar, das Modell bleibt unverändert,
  kein automatischer Zielwechsel (SONDE-012 U02). Ein offener Labelentwurf wird
  nach R-312-9 behandelt, bevor das Aktionsziel geleert ist. Messende Zeile im
  Umordnungsfall mit 25 Quellen.
- **E-312-10 (D8, D9).** 312/M-04 bekommt zwei Hälften mit ausdrücklichem
  Etappenwechsel (bis Etappe 4: 200; ab Etappe 5 im selben Änderungssatz wie
  312/M-40 umgestellt auf 0, die Instrumentaussage trägt dann der Rotbeweis von
  312/M-40); §7.5 nennt die Umstellung. 312/M-39 ordnet über Haken und
  beobachtbare Schleusenzustände statt über die Frist; zulässig sind
  (`abgewiesen`, `gewartetMs`) = (0, 0), (≥ 1, 0), (0, ≥ 1); verboten sind eine
  Marke größer 0 und ein Destruktor, der zurückkehrt, während die Schleuse
  aktive Rückrufe zählt. `Schleuse.h` bekommt keinen Haken; die Mutation
  „zweites Lesen in `betreten()` entfernen" entfällt als Rotbeweis.
- **R-312-11 (L1).** Ein Hostwert, der nach der Quittierung des Ladestarts
  eintrifft, ist ein neuer Hostgestus und wirkt. Der Abgleich zum Host
  überschreibt keinen Parameter, dessen Zähler seit der Quittierung gestiegen
  ist, und prüft das nach seinem Schreiben erneut; nach dem Abgleich zeigen
  Regler und Klang für jeden Parameter denselben Wert. Messende Zeile für
  „während"; der Bauplansatz „mitquittiert" wird berichtigt.
- **R-312-12 (L2).** Ein Rotbeweis läuft nie in undefiniertem Verhalten: der
  Haken zählt den Zugriff an der Marke, bevor der Rückruf Prozessorzustand
  berührt, und hält den Rückruf danach dauerhaft an. Das gilt für den Lauf gegen
  den Basisstand und für jede Mutation von 312/M-30, 312/M-31 und 312/M-39; der
  Beleg „heute rot" ist die gezählte Marke, nicht ein Absturz.

Rundenbilanz bisher: Matrixrunde (§9.1), zählt null. Nacharbeit: ein frischer
Opus-Worker, Effort xhigh, mit genau diesen Defekten und Regeln (Auftrag
`docs/beweise/roh/NAK-312-matrixnacharbeit-1-auftrag.txt`); danach
Matrixprüfung 2 nur über den Diff der Runde.

## 12. Matrixnacharbeit 1 (Bauer, 21.09.2026)

Grundlage: das Rohurteil der Matrixprüfung 1 (§10.1), die Validierung an der
Quelle (§11.1) und die Einordnung mit ihren Entscheiden und Regeln (§11.2,
§11.3). §11.3 geht §2 und §9.2 vor, wo es sie ersetzt. Geändert wurde genau eine
Datei, `docs/beweise/NAK-312.md`, und darin §5 bis §8 sowie die Kopfzeilen
Etappe und Basis-SHA. Kein Produktcode, kein Test, kein Schema, kein Werkzeug,
keine andere Doku; kein Bau, kein Lauf, kein FL Studio.

### 12.1 Geänderte Matrixzeilen (22)

| ID | Was vorher stand | Was jetzt gilt | Verlangt von |
|---|---|---|---|
| M-04 | ein Etikett, `RtWache::sperren()` = 200 dauerhaft | zwei Hälften mit ausdrücklichem Etappenwechsel: 200 bis Etappe 4, 0 ab Etappe 5, Instrumentaussage ab dann bei M-40 | D8, E-312-10 |
| M-20 | Eventfolge nur auf `output_trim_db`, Frist „5 ms" | die vier abgedeckten Parameter namentlich, Workertakt 5 bis 20 ms, Blockrandlesung vor der Programmübernahme, Zelle wie `zelleAusHost` samt Ausnahme | D1, E-312-5, E-312-6 |
| M-21 | Gleichheit ohne genannte Menge | Gleichheit ausdrücklich nur für die vier abgedeckten Parameter | D1, E-312-5 |
| M-22 | „512 und darüber: Regressionswache, heute schon bitgleich" | heute rot bei jeder gefahrenen Blockgröße; Begründung am gemessenen Takt 5 bis 20 ms | Randbeobachtung der Validierung (Teil 2.1) |
| M-23 | Rampe ohne genannten Parameter | Rampe ausdrücklich auf `v1.global.output_trim_db` | D1 |
| M-26 | „der Blockrandzugriff nimmt keine Sperre" ohne Mechanismus | nennt die drei relaxed Loads je Parameter samt veröffentlichtem bestätigtem Vergleichswert; Mutation trifft die neue Lesestelle | D1, E-312-5 Schlusssatz |
| M-27 | „Regressionswache, heute schon bitgleich" bei 4096 | heute rot; die Zeile grenzt den Übernahmemoment ein, nicht die Taktfrage | Randbeobachtung der Validierung (Teil 2.1) |
| M-28 | Zusage über das Flag `hostEreignisOffen` am Blockrand | Zusage über den Gleichheitsvergleich der vier Zähler; das Flag behält den Worker als einzigen Verbraucher, zweite Gegenprobe dafür | D2, E-312-6 |
| M-29 | unverändert in der Sache, Bezug auf die alte Abdeckung | Bezug auf die vier abgedeckten Parameter | D1 |
| M-30 | Haken „VOR dem ersten Besitzerzugriff", Lage offen; Rotlauf im Use-after-free | Schranke (hinter `betreten()`) und Marke (an der Zugriffsstelle) getrennt und benannt; `nachEndeErreicht` = 0; Lesepunkt am Ende des Destruktorrumpfs | L2, D9, R-312-12, E-312-10 |
| M-31 | „20 Zyklen", Rotlauf im Use-after-free | dieselbe Trennung je Zyklus; Beleg ist die gezählte Marke, kein Absturz | L2, R-312-12 |
| M-32 | `stopFristUeberschritten` bei `:130`, Ablösung bei `:133` | am Code nachgemessen: Zähler `:129`, Ablösung `:132` | Anforderung „jede Zeilenangabe selbst nachlesen" |
| M-38 | Mutation „in `betreten()` eine Sperre nehmen" | Mutation „Provider `v3Status()` aus `processBlock` rufen" — die frühere kann der Zähler bauartbedingt nicht sehen; Bein B7 ausgeschrieben | H3 |
| M-39 | Ereignis „Schranke exakt bei `kStopFristMs`", Zusage „nie beides 0" | drei Teilfälle mit ausdrücklicher Hakenlage, geordnet über beobachtbare Schleusenzustände; (0, 0) ist zulässig; Mutation „zweites Lesen entfernen" entfällt, kein Haken in `Schleuse.h` | D9, E-312-10 |
| M-41 | „`previewAktiv()` false spätestens nach dem ersten Block", Regressionswache | nur noch die Hörmatrixhälfte (Regressionswache); die Buchhaltung trägt M-83 | D4, E-312-7 |
| M-42 | Testspalte ohne die Umstellung der `previewAktiv()`-Prüfung | die Prüfung `:2231` wandert hinter einen ausdrücklichen Kontrolltakt und gehört zu M-83 | D4, E-312-7 |
| M-46 | Mutation „`beendeCandidate` aus dem Callbackpfad entfernen" | Freigabe wird nach einem ausdrücklichen Kontrolltakt gemessen; Mutation trifft das Verbrauchen des Atomics im Takt | D3, E-312-7, Randbeobachtung (Teil 2.3) |
| M-55 | Szenario `sensor → hub` mit Zusage „genau EIN `end` zu dem offenen `begin`"; Fix im Timertick | Szenario S1 „zu Main": kein `begin`, kein `end`, Ausgang bitgleich; Rücknahme synchron vor dem Store in `spiegleKlassifikation`, `acquire`-Lesen im Audiothread | D6, E-312-8 |
| M-57 | „der Aus-Knopf ist wieder sichtbar, trägt aber keinen scharfen Auftrag" | der Aus-Knopf ist NICHT sichtbar, weil `markModus` aus ist; ein sichtbarer Knopf ohne Auftrag wäre selbst ein totes Element | Randbeobachtung (Teil 2.4), `CLAUDE.md:51-52` |
| M-59 | „laufender Transport", erster Block nach `reset()` bitgleich | Transport wird vor dem `reset()` angehalten; 40 Blöcke bitgleich, genau ein `end`, kein zweites | H1 |
| M-63 | „wie M-59", Auftrag beginnt neu — bei derselben Vorbedingung wie M-59 | Transport läuft ausdrücklich weiter; gemessen wird ein NEUES `begin`, nicht ein zweites `end` | H1 |
| M-74 | Szenario „Hauptziel per `stelleZielSicher` vergeben" | Umordnungsfall mit 25 Quellen und existierendem Hauptziel; Zusage über Unsichtbarkeit, unverändertes Modell und keinen automatischen Zielwechsel | D7, E-312-9 |

### 12.2 Neue Matrixzeilen (9)

| ID | Etappe | Was sie misst | Verlangt von |
|---|---|---|---|
| M-78 | 3 (W02) | Ein Hostwert, der NACH der Quittierung und vor dem Abgleich eintrifft, wirkt, und Regler und Klang zeigen danach denselben Wert | L1, R-312-11 |
| M-79 | 3 (Blockbindung) | Die Abdeckung ist genau vier namentlich festgelegte Parameter groß; für die übrigen 108 setzt der Blockrand kein Rampenziel | D1, E-312-5 |
| M-80 | 3 (Blockbindung) | Ein Bandwert bleibt taktgebunden — ehrlich gemessen, ohne Gleichheitszusage; Grenze zu NAK-340 | D1, E-312-5 |
| M-81 | 3 (Blockbindung) | Ein boolescher Hostparameter (`v2.global.eq_enabled`) bleibt taktgebunden, ebenso ohne Gleichheitszusage | D1, E-312-5 |
| M-82 | 3 (Blockbindung) | Der Zählerrand der Schiedsregel: Vergleich auf Ungleichheit, nie auf Ordnung, über den Überlauf hinweg | D2, E-312-6 |
| M-83 | 5 (Offline) | Die Buchhaltungshälfte des Vorschauendes: `previewAktiv()` erst nach einem ausdrücklichen Kontrolltakt, Bankfreigabe danach | D3, D4, E-312-7 |
| M-84 | 6 | Szenario S2 „weg von Main": genau ein `end` zum `begin`, weicher Ausfade, kein neues `begin` beim Rückwechsel | L3, E-312-8 |
| M-85 | 6 | Ein offener Labelentwurf wird nach R-312-9 abgeschlossen, bevor das Aktionsziel geleert wird | D7, E-312-9 |
| M-86 | 6 | Über `reset()`, Folgeblöcke mit und ohne Erlaubnis und ein späteres `prepareToPlay`: jedes `begin` hat genau ein `end`, kein gefärbter Block ohne `begin` | H1 |

### 12.3 Geänderte Bauplanabschnitte

| Abschnitt | Was vorher stand | Was jetzt gilt | Verlangt von |
|---|---|---|---|
| §7.0 Ticketpfade | Etappe 4 ohne `TransactionTestMain.cpp`; Etappe 6 ohne `State.cpp`, `PluginProcessor.cpp` und `PluginEditor.h` | alle vier ergänzt, je mit Grund | H3; E-312-8 |
| §7.1 Rotbeweis-Ablauf | nichts über Rotläufe in undefiniertem Verhalten | neuer Absatz: Schranke und Marke getrennt, Beleg ist die gezählte Marke | L2, R-312-12 |
| §7.2 Behauptungszeilen | „B6 und B7 bekommen je einen Satz" über den Callback-Eintritt der Sonde | B6 bekommt ihn NICHT (baut keinen `SondeProcessor`); den Satz bekommen B7, A16 und das neue Bein | D5 |
| §7.3 W02, „während des Ladens" | „wird mitquittiert" | an der Quelle widerlegt (`abgleichTiefe` ist thread-lokal); gebaut wird R-312-11 mit zweiter Prüfung nach dem Abgleich | L1, R-312-11 |
| §7.3 Weg 2, Abdeckung | „abgedeckt sind genau die Parameter, für die das Overlay durchlässt" (80) | vier namentlich genannte Parameter; der taktgebundene Rest steht mit Grund da und geht an NAK-340; Registerzeile zeigt auf NAK-340, nicht auf den Wrapperpatch | D1, E-312-5 |
| §7.3 Weg 2, Callback-Rechnung | nichts darüber, wie die Ausnahme von `zelleAusHost` ohne Schloss rechnet | zwei vom Worker unter dem Schloss veröffentlichte Atomics je Parameter, vom Callback relaxed gelesen | E-312-5 Schlusssatz |
| §6.3 und §7.3, Taktsperre | M-20 bis M-29 verglichen „A mit dem echten Worker" gegen den Referenzlauf — der Worker tickt aber nach Wanduhr, die Aussage „A hat den Wert nicht bekommen" war damit ein Wettlauf | neuer Testzugang, der `zustandSchloss` hält, solange die Blöcke laufen (Muster `mitAngehaltenerAnalyseFuerTest`); A fährt darunter, der Worker kann beweisbar nicht ticken | Matrixregel §6 „keine Zeile hängt an Wandzeit oder Zufall" |
| §7.3 Weg 2, Schiedsregel | Ordnungsvergleich („nicht älter als") auf dem überlaufenden Zähler; „kein Speicherzuwachs" | fünf Punkte mit Gleichheitsvergleich, Lesen vor der Programmübernahme, Zählerstand je Publikation, `hostEreignisOffen` beim Worker; Speicher in Byte benannt | D2, E-312-6 |
| §7.5 Schritt 3 | `beendeCandidate` im Callback, „arbeitet nur mit Atomics" | Offline-Riegel statt `beendeCandidate`; Merkzettel, Publikation und Freigabe bleiben beim Worker, mit den Besitzregeln im Wortlaut | D3, E-312-7 |
| §7.5 Schritt 4 und Umstellungen | keine Aussage über bestehende Prüfungen | neuer Abschnitt: der B7-Fall wird umgebaut (Takt vor `previewAktiv()`), und M-04 wird im selben Änderungssatz auf 0 umgestellt | D4, D8, E-312-7, E-312-10 |
| §7.6 Ort der Rücknahme | `markierungBeenden` im Timertick, „erst danach wechselt die Fläche" | Rücknahme synchron vor dem Store in `spiegleKlassifikation`, `acquire`-Lesen im Audiothread, drei Randbedingungen; zugesagt wird „Rücknahme vor dem Flächenwechsel", nicht „Intervallende davor" | D6, L3, E-312-8 |
| §7.6 Tick-Testzugang | keine Aussage | `PluginEditor.h` bekommt einen Tick-Testzugang, sonst hängen M-55 bis M-58 an der Wanduhr | E-312-8, Matrixregel §6 |
| §7.6 Reset und Release | „gibt den fälligen Übergang wie `vorbereiten` zurück" | ausdrücklich: `warHoerbar` und `hoerbareSamples` werden zurückgesetzt, mit dem Schaden bei Unterlassung | H1 |
| §7.6 Auftrag bleibt | „ob das Anhalten ihn beendet, entscheidet U56" | U56 ist beantwortet; M-59 und M-63 fahren verschiedene Transportlagen | H1, §10.2 |
| §7.6 Anzeigeordnung | nur `stelleZielSicher` | neuer Abschnitt: `aktualisiereSourcesSteuerung` sucht nur in den gezeichneten Zeilen, kein automatischer Zielwechsel, Zusammenspiel mit R-312-9 | D7, E-312-9 |
| §7.7 T3-02-06 | „`State.cpp` wird in den Etappen 2 bis 6 nicht angefasst" | berichtigt: Etappe 6 ändert `spiegleKlassifikation`; nicht angefasst bleibt die Löschstelle `:321-322`, und beide U49-Wege bleiben baubar | E-312-8 |
| §8 Prüfliste | Zeilen B, C, E (drei Punkte) und F auf dem Stand der Etappe 1 | nachgezogen auf die neuen Zeilen, die berichtigten Etiketten, R-312-12 und die nachgemessenen Zahlen | Folge der obigen |

### 12.4 Nicht Gegenstand dieser Nacharbeit

H2 (Hostsicht der Parameter nur am Konsolenprozessor gemessen) bleibt Härtung
und ist als Register NAK-342 geführt — hier steht dazu kein Satz. Die Etappe 7,
das Folgeticket NAK-340 und jede Umformulierung ohne Defektbezug sind ebenfalls
nicht angefasst.

### 12.5 Selbstaudit der Nacharbeit

- **Gelesen:** dieses Manifest vollständig, besonders §10.1, §11.2 und §11.3;
  `docs/beweise/roh/NAK-312-matrixpruefung-1-urteil.md` und
  `docs/beweise/roh/NAK-312-matrixpruefung-1-validierung.md` je vollständig;
  `tools/dirigent/pruefliste.md` als Arbeitsliste; der Code an jeder Stelle, die
  in §5 bis §8 neu steht oder geändert wurde.
- **Eigene Messungen, lesend:** `git rev-parse HEAD` (`abb64275`),
  `git status --short` (nur `briefing-hub/` und `nimbalyst-local/`),
  `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` (leer),
  `git diff --stat 090d0fa1 HEAD -- eq-copilot broker tools` (leer),
  `git ls-files --eol docs/beweise/NAK-312.md` (LF im Index und in der
  Arbeitskopie, vor dem Schreiben gelesen und so zurückgeschrieben).
- **An der Quelle nachgelesen** (Auswahl der tragenden Stellen):
  `eq-copilot/plugin/state/NakamaParameter.cpp:15-33` und `:49-82` (die vier
  globalen Parameter, 64 Bandwerte, 11 Boolesche, 32 topologische);
  `eq-copilot/plugin/dsp/DspKern.cpp:520-532`, `:600-627` (fünf Rampenziele),
  `:434-443`, `:494-517`, `:1059`, `:1110-1116`, `:1228-1242`, `:1385-1390`;
  `eq-copilot/plugin/dsp/DspKern.h:157`, `:205`, `:244`, `:404-418`, `:593-614`;
  `eq-copilot/plugin/dsp/DspProgramm.cpp:244-282`, `:294-317`, `:348-356`,
  `:364-369`; `eq-copilot/plugin/dsp/DspBankPool.h:8-14`, `:35-42`, `:128`,
  `:152-158` und `DspBankPool.cpp:155-163`;
  `eq-copilot/plugin/dsp/DspRtWache.h:30-64`, `:76-96`, `:112-124`;
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:136-144`, `:186-232`, `:366-390`,
  `:428-439`, `:566-582`, `:735-742`, `:1136-1150`, `:1168-1175`, `:1318-1340`,
  `:1392-1432`, `:1447-1480` und `SondeProcessor.h:224`, `:255-268`,
  `:316-326`, `:528-548`;
  `eq-copilot/plugin/state/NakamaTransaktion.cpp:306-320`, `:343-347`,
  `:760-766`; `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h:10-45`,
  `:106-205`, `Intern.h:57-60`, `ControlClient.cpp:118-136`,
  `TelemetryClient.cpp:28-32`; `eq-copilot/plugin/src/PluginEditor.cpp:195-212`,
  `:242-285`, `:795-850`, `:872-945`, `:946-990`, `:1130-1140` und
  `PluginEditor.h:45-75`; `eq-copilot/plugin/src/prozessor/State.cpp:258-278`,
  `:318-326`, `:338-352`; `eq-copilot/plugin/src/prozessor/Hostbruecke.cpp:30-50`,
  `:70-145`; `eq-copilot/plugin/src/HoerMarkierung.h:295-322`, `:340-365`,
  `:405-432`, `:478-520`, `:586-596`, `:616-644`, `:660-685`, `:755-770`;
  `eq-copilot/plugin/src/PluginProcessor.cpp:974-984`, `PluginProcessor.h:109-116`;
  `eq-copilot/plugin/src/SourcesModel.cpp:1645-1670`, `:1735-1795`;
  `eq-copilot/plugin/state/NakamaLebenslauf.h:135-139`;
  `eq-copilot/plugin/CMakeLists.txt:1146-1156`;
  `eq-copilot/plugin/tests/TransactionTestMain.cpp:2046-2056`, `:2214-2236`,
  `MarkierungTestMain.cpp:177-187`, `:449-460`, `:468-478`;
  `tools/beweise.ps1:509`, `:688`, `:691`, `:693`, `:723`, `:728`, `:744-746`;
  `docs/beweise/SONDE-015.md:1087`, `:1114`, `:3425`.
- **Im Lauf berichtigt.** Zwei Zeilenangaben aus der Etappe 1 stimmten nicht mit
  dem Code überein und sind hier berichtigt: `stopFristUeberschritten` steht bei
  `eq-copilot/plugin/core/ipc/ControlClient.cpp:129` (nicht `:130`), die
  Ablösung bei `:132` (nicht `:133`) — M-32 und §7.4. `prepareToPlay` in
  `Hostbruecke.cpp` beginnt bei `:33` (die `34` war die öffnende Klammer).
  `alsMainKlassifizieren` in `MarkierungTestMain.cpp` endet bei `:184`, nicht
  `:185`. `DspKernAusfuehrung::beendePreview` ruft `beendeCandidate` bei
  `NakamaTransaktion.cpp:762`. `kBaenke = 4` steht in `DspBankPool.h:128`.
- **Kein Bau, kein Test, keine Repro, kein FL Studio.** Das einzige gefahrene
  Skript ist `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` vor dem
  Commit. Git nur lesend außer dem einen Commit und Push am Ende. Die fremden
  Ordner `briefing-hub/` und `nimbalyst-local/` sind nicht angefasst.

## 13. Messung der Matrixnacharbeit 1, Berichtigung der Uhrzeiten, Auftrag Matrixprüfung 2 (Dirigent, 21.09.2026, 02:3x Uhr)

### 13.1 Berichtigung: geschätzte Uhrzeiten in §0, §1 und §9 bis §11

Der Dirigent hat in diesen Abschnitten mehrere Uhrzeiten geschätzt statt
gemessen. Maßgeblich sind die Commitzeiten (`git log --date`) und die in der
Session gelesenen Uhrzeiten (`Get-Date`); die Abschnitte selbst bleiben als
Verlauf stehen:

| Stelle | dort steht | gemessen |
|---|---|---|
| §0, Ende der Quellvalidierung | „00:1x Uhr" | Teil B endete 20.09.2026, 23:56 Uhr (Abschlusszeile seines Laufs), Teil A vor dem Commit `e530913f` um 00:09 Uhr |
| §1, Überschrift | „00:3x Uhr" | vor 00:09 Uhr geschrieben (Commit `e530913f`) |
| §9, Überschrift und Workerlauf | „01:0x Uhr", „00:12 bis 00:5x Uhr" | Worker `8cbfe878` von 00:12 Uhr bis zu seinem Commit `2deb5e3d` um 00:45 Uhr; §9 committet 00:48 Uhr (`b7d82137`) |
| §10, Überschrift und Prüferlauf | „01:4x Uhr", „00:49 bis 01:2x Uhr" | Prüfer `97cc511f` ab 00:49 Uhr, um 01:05 Uhr noch arbeitend, beendet vor dem Commit `74fbae75` um 01:16 Uhr; §10 committet 01:20 Uhr (`075fb1b6`) |
| §11, Überschrift und Validiererlauf | „02:3x Uhr", „01:20 bis 02:2x Uhr" | Validierer `47dddc88` ab 01:20 Uhr, um 01:51 Uhr noch arbeitend, beendet vor dem Commit `211a6347` um 01:58 Uhr, mit dem §11 committet ist |

### 13.2 Messung der Matrixnacharbeit 1

Worker `98d64cb0` (Opus, Effort xhigh, `dontAsk`), 01:59 bis 02:32 Uhr, Basis
`abb64275a66bdae989495a2c9ae20b6b315f00e6`. Ergebnis: ein Commit
`d5e3e33a8b4e48fa9da68129928308b44a816515` (02:31 Uhr) = `origin/master`, Baum
sauber (nur die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD,
`git diff --stat abb64275..d5e3e33a`: eine Datei, `docs/beweise/NAK-312.md`,
+728/−147. Vom Dirigenten gemessen: §0 bis §4 und §9 bis §11 sind zwischen
beiden Ständen zeichengleich; im Kopf sind nur die Zeilen Etappe und Basis-SHA
geändert. Die Matrix führt jetzt M-01 bis M-86; §12 listet die geänderten und
neuen Zeilen. Rundenbilanz `abb64275..d5e3e33a`: Doku 1 Datei +728/−147,
Produkt und Tests 0 Zeilen — Matrixrunde, zählt für das Rundenbudget null.

### 13.3 Auftrag Matrixprüfung 2 (Wiederprüfung)

Frischer, lesender Opus-Thread, Effort max (Codex-Woche 94 %); Auftrag
`docs/beweise/roh/NAK-312-matrixpruefung-2-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-matrixpruefung-2-urteil.md` (Zielpfad, noch nicht
angelegt). Prüfbereich schrumpft: nur
`git diff abb64275...d5e3e33a -- docs/beweise/NAK-312.md`, die Befundliste aus
§11.2 und das Gate; Urteil nur: geschlossen, und nichts gebrochen.

### 13.4 Übergabe an die frische Dirigentensession (21.09.2026, 02:3x Uhr)

Die Session `90fe90ab` endet im sauberen Fenster wegen des Kontextstands (49 %
um 02:32 Uhr gemessen, Skill §5): kein Worker, kein Kanon, kein Beobachter, kein
Cron-Job, Baum sauber, alles gepusht. Die Matrixprüfung 2 ist **noch nicht
gestartet**. Die nächste Session:

1. startet die Matrixprüfung 2 als frischen, lesenden Opus-Thread, Effort max,
   `dontAsk`, mit dem Auftrag aus §13.3 (nur Lese- und lesende Git-Werkzeuge
   plus Schreiben der einen Berichtsdatei), misst HEAD vor und nach dem Lauf und
   lässt jeden Befund von einem weiteren lesenden Opus-Thread an der Quelle
   validieren, bevor sie einordnet. Rundenbudget: bisher eine Matrixnacharbeit;
   Matrixrunden zählen für das Budget null.
2. Bei PASS: Auftrag Etappe 2 (Messgeräte) an einen frischen Opus-Worker, Effort
   xhigh, Workername mit der Endung `-bau` (Register NAK-343); danach der
   Matrixnachtrag für Etappe 7 — alle vier Karten sind beantwortet (§10.2).
3. Stellt im Zeitfenster 9 bis 23 Uhr die Karte U55 (Zahlen:
   `docs/beweise/roh/NAK-335-u55-zahlen.md`).

Dem User in dieser Session gemeldet: die Teilung von T3-01-05 mit dem
Folgeticket NAK-340 (Planschritt S25p) und die Produktwirkung von R-312-8 auf
die Hörmarkierung (Register NAK-341).

## 14. Matrixprüfung 2, Validierung, Einordnung und Regeln (Dirigent, 21.09.2026, 03:21 Uhr)

Uhrzeiten aus `Get-Date` und den Läufen selbst. Dirigentensession `1a45e086`
(Start 02:38 Uhr, HEAD `41651542`, Baum sauber, kein aktiver Worker, kein
Cron-Job).

### 14.1 Matrixprüfung 2: NEEDS_WORK (Rohurteil)

Frischer, lesender Opus-Thread `09302388`, Effort max, `dontAsk`, 02:39 bis vor
02:59 Uhr, HEAD `416515424f91e965922d19a48a612c31b1a252c5` vor und nach dem Lauf
gleich (vom Prüfer und vom Dirigenten gemessen); geschrieben wurde nur der
Bericht `docs/beweise/roh/NAK-312-matrixpruefung-2-urteil.md` (Commit
`a4494fad`). Rohurteil: **NEEDS_WORK — drei Defekte (D-A, D-B, D-C), eine Lücke
(L-A), eine Härtung (H-A).** Alle dreizehn Befunde der Liste aus §11.2 sind
geschlossen (H3 mit dem Vorbehalt D-B); die drei Defekte sind neue Brüche der
Nacharbeit 1: D-A die neuen Zeilen M-81 und M-84 kollidieren mit bloß
geschriebenen SONDE-015-Zeilen gleicher Nummer; D-B der Rotbeweis von M-38
behauptet einen Sperrenzähler, den `juce::ScopedLock` nicht bewegt; D-C die
Mutationskette von M-81 widerspricht §7.3 Punkt 1 und 5 derselben Runde.

### 14.2 Validierung an der Quelle

Frischer, lesender Opus-Thread `d61dbe9b`, Effort max, `dontAsk`, 03:00 bis
03:15 Uhr (Abschlusszeile seines Laufs), HEAD
`a4494fad0a65a56eb1c1eb09a6a3a6596414ed17` vor und nach dem Lauf gleich, nur der
Bericht `docs/beweise/roh/NAK-312-matrixpruefung-2-validierung.md` geschrieben
(Auftrag `docs/beweise/roh/NAK-312-matrixpruefung-2-validierung-auftrag.txt`):
**2 bestätigt, 3 präzisiert, 0 widerlegt.** Stichproben des Dirigenten an der
Quelle: `eq-copilot/plugin/sonde/SondeProcessor.cpp:1139` ist
`const juce::ScopedLock l (zustandSchloss);`, `RtWache::meldeSperre()` hat als
einzigen Aufrufer `GemeldeteSperre` (`eq-copilot/plugin/dsp/DspRtWache.h:84`);
`eq-copilot/plugin/tests/TransactionTestMain.cpp:76-91` zählt in einen eigenen
thread-lokalen Zähler und ruft `RtWache::meldeAllokation()` nicht; im Manifest
kommt `meldeAllokation` nicht vor (`git grep`, 0 Treffer).

### 14.3 Einordnung

| Befund | Urteil der Validierung | Einordnung des Dirigenten | Folge |
|---|---|---|---|
| D-A | PRÄZISIERT (enger: nur vier Stellen im Diff; breiter: neun weitere Stellen mit Kollisionen über M-47, M-55, M-36, die schon vor der Runde bestanden) | **DEFEKT** gegen den Kopfsatz von §6 („eine Zeile hier heißt immer M-01 bis M-86 dieses Manifests"); an vier Stellen liest ein Bauer eine falsche Zusage | R-312-13; gemeinsame Ursache, deshalb das ganze Manifest §5 bis §8, nicht nur die Stellen im Diff |
| D-B | PRÄZISIERT (zweite Hälfte: auch `RtWache::allokationen()` steigt in B7 nicht, betroffen M-01, M-02, M-26, M-38, M-53) | **DEFEKT** gegen die Matrixregel „Rotbeweis an der Zusagezeile" und gegen M-04 und §7.2 | E-312-11 |
| D-C | PRÄZISIERT (der Rotbeweis fällt an der eigenen ersten Zusagehälfte; falsch ist die geschriebene Kette, sie gehört M-28) | **DEFEKT** gegen §7.3 Punkt 1 und 5 | E-312-12 |
| L-A | BESTÄTIGT | **LÜCKE → R-312-14** | R-312-14, in die Nacharbeit |
| H-A | BESTÄTIGT | **HÄRTUNG**; eine eigene Mutation für die Schleusenhälfte bleibt durch E-312-10 ausgeschlossen. Weil E-312-11 die Zeile M-38 ohnehin neu fasst, trägt sie dort das vorhandene Etikett „zwei Hälften" — kein eigener Auftrag, kein Registerpunkt | Etikett in M-38 (E-312-11) |

### 14.4 Entscheide und Regeln

- **R-312-13 (D-A).** Im ganzen Manifest §5 bis §8 trägt jede Nennung einer
  fremden Matrixzeile ihr Präfix (`SONDE-015 M-nn`, `SONDE-013 M-nn`,
  `SONDE-012 U-nn`, `NAK-246 M-nn`); ein bloßes `M-nn` bezeichnet ausnahmslos
  eine eigene Zeile M-01 bis M-86. Das gilt für die Liste A **und** die Liste B
  der Validierung Teil 2.1 (auch Nummern über 86: SONDE-015 M-92, M-119, M-120)
  und für jede weitere Stelle, die der Bauer mit `git grep` findet. In
  wörtlichen Zitaten (R-312-10 aus §2 in §5.4, Codezitate, bestehende Testnamen
  wie `der_zaehler_trennt_worker_und_audiothread (M-47)`) bleibt der Wortlaut,
  das Präfix steht als Zusatz in eckigen Klammern davor: `[SONDE-015] M-81`.
  Der Kopfsatz von §6 sagt das künftig selbst. §0 bis §4 und §9 bis §13 sind
  Verlauf und bleiben unberührt. Codekommentare mit bloßer SONDE-015-Nummer
  (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1454`) sind nicht Gegenstand: im
  Code gibt es keinen Namensraum dieses Manifests.
- **E-312-11 (D-B).** Eine Zeile nennt als Zähler nur, was ihr Bein wirklich
  liest, und behauptet eine Sperre nur dort, wo sie über
  `RtWache::GemeldeteSperre` genommen wird. (1) Etappe 2 (Messgeräte) bindet
  `operator new` und `operator new[]` von B7
  (`eq-copilot/plugin/tests/TransactionTestMain.cpp:76-91`) zusätzlich an
  `RtWache::meldeAllokation()` nach dem Muster von B6
  (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:100`, `:109`); der eigene
  thread-lokale Zähler von B7 und seine bestehenden Prüfungen bleiben. §7.2
  nennt den Schritt, die Ticketpfade der Etappe 2 tragen die Datei, und die
  Zusagen von M-01, M-02, M-26, M-38 und M-53 („`RtWache::allokationen()` = 0"
  in B7) werden damit messbar; ihr Etikett sagt, ab welcher Etappe. (2) Der
  Rotbeweis von M-38 lautet: `v3Status()` aus `processBlock` rufen → seine
  Zeichenketten (`SondeProcessor.cpp:1145`, `:1172`) allokieren im Bereich →
  `RtWache::allokationen()` steigt → rot. Die Zeile sagt ausdrücklich, dass die
  drei Sperren auf diesem Weg (`:1139`, `:1184`, `:1192`) `juce::ScopedLock` und
  für `RtWache::sperren()` unsichtbar sind; sie behauptet keinen Sperrenanstieg.
  (3) M-38 trägt das Etikett „zwei Hälften": die Zählerhälfte mit dem Rotbeweis
  aus (2), die Schleusenhälfte („der Callbackpfad der Schleuse arbeitet nur mit
  Atomics") ausdrücklich ohne eigene Mutation, weil E-312-10 den Haken in
  `Schleuse.h` ausschließt.
- **E-312-12 (D-C).** Die Mutation einer Zeile nennt nur die Folge, die der
  Bauplan für sie hergibt; eine Kette, die einer anderen Zeile gehört (hier
  M-28), wird nicht übernommen. M-81 ist eine Grenzzeile ohne eigenen Wirkpfad
  und trägt „zwei Hälften": Die Mengenhälfte („`eq_enabled` steht nicht in der
  Abdeckungstabelle") prüft der Test von M-81 mit eigener Behauptung; Mutation:
  `eq_enabled` in die Abdeckungstabelle aufnehmen → diese Behauptung fällt →
  rot. Die Zeile sagt dazu wahr: der Blockrand schreibt nur `blockrandStand`,
  der Worker sieht den Wert weiter, der Schalter wirkt weiter über
  `aus.eqEngagiert` (`eq-copilot/plugin/dsp/DspProgramm.cpp:263`). Die
  Verhaltenshälfte (unter der Taktsperre liegt der Umschaltblock von A hinter
  dem von B, keine Gleichheitszusage, Grenze nach NAK-340) ist messend und
  trägt keine eigene Mutation: ohne Rampenziel ändert die
  Tabellenmitgliedschaft das Laufverhalten nicht. Die von der Validierung
  genannte Mutation „Tabelleneintrag plus Schreibweg auf `aus.eqEngagiert`"
  wird verworfen: sie baute im Rotlauf genau den Blockrandweg, den E-312-5 nach
  NAK-340 verschiebt.
- **R-312-14 (L-A).** Ein Änderungssatz berichtigt jeden Kommentar, den er
  selbst falsch macht, im selben Änderungssatz — nicht nur die drei aus §1.
  §7.6 Punkt 2 nennt dafür
  `eq-copilot/plugin/src/prozessor/State.cpp:270-271` („Gelesen wird im
  processBlock relaxed - dort haengt kein anderer Wert daran"); §8 Zeile E
  führt die Regel.

### 14.5 Auftrag Matrixnacharbeit 2

Frischer Opus-Worker, Effort xhigh, `dontAsk`; Auftrag
`docs/beweise/roh/NAK-312-matrixnacharbeit-2-auftrag.txt`. Gegenstand: nur
`docs/beweise/NAK-312.md` §5 bis §8 und ein neuer §15 mit der Liste der
geänderten Stellen; kein Code, kein Lauf. Rundenbudget: Matrixrunden zählen
null. Danach Matrixprüfung 3 als Wiederprüfung über den Diff dieser Runde.

## 15. Matrixnacharbeit 2 (Bauer, 21.09.2026)

Grundlage: das Rohurteil der Matrixprüfung 2 (§14.1), die Validierung an der
Quelle (§14.2) und die Einordnung mit ihren Entscheiden und Regeln (§14.3,
§14.4). §14.4 geht §2 und §11.3 vor, wo es sie ergänzt. Geändert wurde genau
eine Datei, `docs/beweise/NAK-312.md`, und darin §5 bis §8 sowie die Kopfzeilen
Etappe und Basis-SHA. Kein Produktcode, kein Test, kein Schema, kein Werkzeug,
keine andere Doku; kein Bau, kein Lauf, kein FL Studio. Zeilennummern in diesem
Abschnitt gelten zum Stand nach dieser Nacharbeit; die Zeilenangaben in §5 bis
§8 gelten weiter zum Stand `945ebfb1`, weil
`git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` am 21.09.2026
**leer** ist (gemessen), und jede in dieser Nacharbeit geschriebene oder
geänderte Angabe ist am HEAD `801cb75a` selbst nachgelesen.

### 15.1 Matrixzeilen mit Sachänderung (6)

| ID | Was vorher stand | Was jetzt gilt | Verlangt von |
|---|---|---|---|
| M-01 | Zusage „`RtWache::allokationen()` = 0" ohne Angabe, ob B7 diesen Zähler überhaupt liest | die Zusage sagt, dass der Zähler in B7 ab DIESER Etappe 2 messbar ist, weil derselbe Änderungssatz B7s `operator new` und `operator new[]` (`eq-copilot/plugin/tests/TransactionTestMain.cpp:76-91`) an `RtWache::meldeAllokation()` bindet | D-B, E-312-11 (1) |
| M-02 | dieselbe Zusage, Etikett „Regressionswache" ohne Messbarkeitsgrenze | Messbarkeit ab Etappe 2 benannt; das Etikett sagt, dass die Wache ab dem Bau dieser Etappe gilt und der Grundsatz heute schon in B6 gemessen wird | D-B, E-312-11 (1) |
| M-26 | Zusage „`RtWache::allokationen()` = 0" für eine Etappe-3-Zeile | die Zeile sagt ausdrücklich, dass sie die Bindung aus Etappe 2 voraussetzt | D-B, E-312-11 (1) |
| M-38 | Etikett „Regressionswache"; Mutation „`v3Status()` aus `processBlock` rufen → er nimmt dort `zustandSchloss` (`:1139`) … → beide Zähler steigen → rot" | Etikett „zwei Hälften": die Zählerhälfte fällt über `RtWache::allokationen()` an den Zeichenketten `:1145` und `:1172`, und die Zeile hält fest, dass die drei Sperren dieses Weges (`:1139`, `:1184`, `:1192`) `juce::ScopedLock` und für `RtWache::sperren()` unsichtbar sind — kein Sperrenanstieg wird mehr behauptet; die Schleusenhälfte wird ausdrücklich ohne eigene Mutation geführt (E-312-10) | D-B und H-A, E-312-11 (2) und (3) |
| M-53 | dieselbe Allokationszusage ohne Messbarkeitsgrenze | Messbarkeit seit Etappe 2 benannt | D-B, E-312-11 (1) |
| M-81 | Etikett „Regressionswache"; Mutationskette „der Blockrand quittiert den Zähler trotzdem und der Wert verschwindet für den Worker → der Schalter wirkt gar nicht mehr" | Etikett „zwei Hälften": die Mengenhälfte trägt eine eigene Behauptung über die Abdeckungstabelle, die Mutation „`eq_enabled` aufnehmen" lässt genau diese fallen, und die wahre Folge steht in der Zeile (der Blockrand schreibt allein `blockrandStand[i]`, der Worker quittiert weiter selbst, `eq_enabled` wirkt weiter über `aus.eqEngagiert`, `eq-copilot/plugin/dsp/DspProgramm.cpp:263`); die Verhaltenshälfte ist messend und trägt keine eigene Mutation | D-C, E-312-12 |

### 15.2 Geänderte Abschnitte außerhalb der Matrixzeilen

| Stelle | Was vorher stand | Was jetzt gilt | Verlangt von |
|---|---|---|---|
| Kopf von §6 | „Fremde Matrixzeilen … stehen nur in der Quellspalte, nie als eigene Zeile; eine Zeile hier heißt immer M-01 bis M-86 dieses Manifests." | derselbe Satz ohne die Spaltenbindung, dazu die Präfixregel im Wortlaut: jede Nennung einer fremden Zeile trägt in §5 bis §8 ihr Präfix, gleich in welcher Spalte; ein bloßes `M-nn` ist ausnahmslos eine eigene Zeile; in Zitaten, Codezitaten und Testnamen steht das Präfix in eckigen Klammern davor | D-A, R-312-13 |
| §6.8 (Zählung) | Entscheidliste bis E-312-10, Regelliste bis R-312-12 | E-312-11 (M-01, M-02, M-26, M-38, M-53) und E-312-12 (M-81) ergänzt; R-312-13 und R-312-14 als Regeln ohne eigene Matrixzeile benannt, mit ihrem Ort; festgehalten, dass diese Runde keine Zeile angelegt oder entfernt hat und die Zählung 86 bleibt | D-B, D-C, L-A |
| §7.0, Ticketpfade Etappe 2 | `eq-copilot/plugin/tests/TransactionTestMain.cpp` ohne Zweck | derselbe Pfad mit dem Zweck: B7s `operator new` und `operator new[]` an `RtWache::meldeAllokation()` binden | D-B, E-312-11 (1) |
| §7.2 (Messgeräte) | kein Satz über B7s Allokationszähler | neuer Absatz „Zählbare Allokation in B7": B7 zählt heute in einen eigenen thread-lokalen Zähler (`:72-73`, Inkremente `:78` und `:87`) und ruft `RtWache::meldeAllokation()` nicht; der vorhandene Fall liest allein `dsp::RtWache::sperren()` (`:3368`) und prüft `imCallback == 0` (`:3374-3376`). Die Etappe bindet beide Operatoren zusätzlich an `meldeAllokation()` nach dem Muster von B6 (`eq-copilot/plugin/tests/DspGoldenTestMain.cpp:100`, `:109`); eigener Zähler und bestehende Prüfungen bleiben, `DspRtWache.h` bleibt unangetastet | D-B, E-312-11 (1) |
| §7.3, Satz zu R-312-11 | „M-84 verlangt weiter den geladenen Stand" — als geschriebener Satz falsch, weil die eigene Zeile M-84 vom Rollenwechsel handelt | „SONDE-015 M-84 (`docs/beweise/SONDE-015.md:1162`) verlangt weiter den geladenen Stand" | D-A, R-312-13 |
| §7.6 Punkt 2 | das Heben von `relaxed` auf `acquire` ohne Zuordnung des Kommentars, den es falsch macht | derselbe Punkt verlangt im selben Änderungssatz die Berichtigung von `eq-copilot/plugin/src/prozessor/State.cpp:270-271` („Gelesen / wird im processBlock relaxed - dort haengt kein anderer Wert daran."), weil nach dem Bau beide Halbsätze falsch sind; Kommentarblock `:264-269` und Store `:272` bleiben | L-A, R-312-14 |
| §8, Zeile E „Behauptung nicht weiter als der Test" | nur der B6-Nachzug aus der Nacharbeit 1 | zusätzlich: eine Zeile nennt als Zähler nur, was ihr Bein wirklich liest, und keine Rotbeweisspalte behauptet `RtWache::sperren()` für eine Sperre außerhalb von `RtWache::GemeldeteSperre` | D-B, E-312-11 |
| §8, Zeile E „jede neue Prüfung einmal gebrochen" | „der Rotbeweis von M-38 fällt jetzt an der Zusagezeile" ohne den Zähler | zusätzlich: M-38 fällt über `RtWache::allokationen()` und trägt „zwei Hälften"; M-81 nennt nur die Folge, die der Bauplan hergibt, und trägt ebenfalls „zwei Hälften" | D-B, D-C, H-A |
| §8, Zeile E „geänderte Zusage an drei Stellen" | „die drei irreführenden Kommentare aus §1 sind je ihrem Änderungssatz zugeordnet" | zusätzlich die Regel R-312-14: sie gilt für jeden Kommentar, den ein Änderungssatz selbst falsch macht; erster Fall ist `State.cpp:270-271` | L-A, R-312-14 |

### 15.3 Präfixstellen nach R-312-13 (51)

Gesucht mit `git grep -n -oE 'M-[0-9]+'` über §5 bis §8, je Treffer am Kontext
entschieden; Grundlage sind Liste A und Liste B der Validierung (Teil 2.1),
ergänzt um die selbst gefundenen Stellen. Nicht angefasst sind die schon
präfixierten Stellen der Liste C, auch die nachgestellte Form „M-55 von
SONDE-015" (eindeutig, kein Befund), und Codekommentare im Produkt
(R-312-13 letzter Satz).

| Zeile | vorher | jetzt |
|---|---|---|
| `:370` §5.4, Codezitat `SondeProcessor.cpp:151-154` | „M-119: FL meldet …" | „[SONDE-015] M-119: FL meldet …" |
| `:377` §5.4, Zitat R-312-10 | „Toleranz von M-120" | „Toleranz von [SONDE-015] M-120" |
| `:381` §5.4, Zitat R-312-10 | „Ruhegrenze M-81" | „Ruhegrenze [SONDE-015] M-81" |
| `:387` §5.4, Zitat E-312-5 | „Toleranz von M-120" | „Toleranz von [SONDE-015] M-120" |
| `:393` §5.4, Zitat E-312-5 | „M-120 und der Entwurfssatz" | „[SONDE-015] M-120 und der Entwurfssatz" |
| `:461` §5.7, Codezitat `SondeProcessor.h:176-179` | „(§44.4, §49.2 Gate 3, M-120)" | „(§44.4, §49.2 Gate 3, [SONDE-015] M-120)" |
| `:465` §5.7, Zitat R-312-3 | „nie die Vorschau (M-120)" | „nie die Vorschau ([SONDE-015] M-120)" |
| `:536` §5.9, Codezitat `HoerMarkierung.h:420-429` | „bit-transparent (M-35)" | „bit-transparent ([SONDE-013] M-35)" |
| `:688` M-01, Quelle | „M-47" | „SONDE-015 M-47", dazu E-312-11 |
| `:689` M-02, Testname aus B6 | „…worker_und_audiothread (M-47)" | „…worker_und_audiothread ([SONDE-015] M-47)" |
| `:691` M-04, Quelle | „M-47" | „SONDE-015 M-47" |
| `:708` M-10, Test und Quelle | „neben dem M-84-Block"; Quelle „M-84" | „neben dem [SONDE-015] M-84-Block"; Quelle „SONDE-015 M-84" |
| `:710` M-12, Quelle | „M-84" | „SONDE-015 M-84" |
| `:712` M-14, Befund und Quelle | „Hashhälfte M-84"; Quelle „M-84 (Hashhälfte)" | „Hashhälfte von SONDE-015 M-84"; Quelle „SONDE-015 M-84 (Hashhälfte)" |
| `:713` M-15, Test und Quelle | „hinter dem M-92-Block"; Quelle „M-92" | „hinter dem [SONDE-015] M-92-Block"; Quelle „SONDE-015 M-92" |
| `:714` M-16, Quelle | „M-92" | „SONDE-015 M-92" |
| `:715` M-17, Quelle | „M-92" | „SONDE-015 M-92" |
| `:717` M-78, Quelle | „M-84" | „SONDE-015 M-84" |
| `:772` M-20, Quelle | „M-120" | „SONDE-015 M-120" |
| `:773` M-21, Quelle | „M-120" | „SONDE-015 M-120" |
| `:774` M-22, Quelle | „M-120" | „SONDE-015 M-120" |
| `:775` M-23, Quelle | „M-120" | „SONDE-015 M-120" |
| `:776` M-24, Befund und Quelle | „T3-01-05 · M-81 · aktivieren↔abklingen"; Quelle „M-81" | „T3-01-05 · SONDE-015 M-81 · aktivieren↔abklingen"; Quelle „SONDE-015 M-81" |
| `:777` M-25, Quelle | „M-81" | „SONDE-015 M-81" |
| `:778` M-26, Quelle | „M-47" | „SONDE-015 M-47", dazu E-312-11 |
| `:779` M-27, Quelle | „M-120" | „SONDE-015 M-120" |
| `:780` M-28, Quelle | „M-120" | „SONDE-015 M-120" |
| `:781` M-29, Quelle | „M-84" | „SONDE-015 M-84" |
| `:783` M-80, Quelle | „M-120 (für Bandwerte weiterhin gebrochen)" | „SONDE-015 M-120 (für Bandwerte weiterhin gebrochen)" |
| `:821` M-38, Quelle | „M-47" | „SONDE-015 M-47", dazu E-312-11 und E-312-10 |
| `:828` M-40, Test und Quelle | „neben dem M-120-Block"; Quelle „M-47" | „neben dem [SONDE-015] M-120-Block"; Quelle „SONDE-015 M-47" |
| `:829` M-41, Quelle | „M-120" | „SONDE-015 M-120" |
| `:830` M-83, Quelle | „M-120" | „SONDE-015 M-120" |
| `:831` M-42, Quelle | „M-120" | „SONDE-015 M-120" |
| `:832` M-43, Quelle | „M-55" | „SONDE-015 M-55 (`docs/beweise/SONDE-015.md:1114`)" |
| `:833` M-44, Quelle | „M-120" | „SONDE-015 M-120" |
| `:834` M-45, Quelle | „M-120" | „SONDE-015 M-120" |
| `:836` M-47, Quelle | „M-120" | „SONDE-015 M-120" |
| `:845` M-51, Frist und Quelle | „Reihenfolge M-84: State lesen …"; Quelle „M-84" | „Reihenfolge nach SONDE-015 M-84: State lesen …"; Quelle „SONDE-015 M-84" |
| `:847` M-53, Quelle | „M-47" | „SONDE-015 M-47", dazu E-312-11 |
| `:864` M-59, Test | „neben dem M-36-Fall" | „neben dem [SONDE-013] M-36-Fall" |
| `:866` M-61, Test | A3 bestehend „M-36: nach prepareToPlay …" | A3 bestehend [SONDE-013] „M-36: nach prepareToPlay …" |
| `:1232` §7.3 | „M-84 verlangt weiter den geladenen Stand" | „SONDE-015 M-84 (`docs/beweise/SONDE-015.md:1162`) verlangt weiter den geladenen Stand" |
| `:1300` §7.3 | „Topologieautomation bleibt deaktiviert (M-119, …)" | „… (SONDE-015 M-119, `docs/beweise/SONDE-015.md:1223`, …)" |
| `:1424` §7.3, Beineliste | „B6 `EqCopDspGoldenTest` (M-120 Kernhälfte, …)" | „B6 `EqCopDspGoldenTest` (SONDE-015 M-120 Kernhälfte, …)" |

Gezählt: 51 Stellen in 45 Zeilen (sechs Zeilen tragen zwei Stellen). Vorher
trugen 17 Stellen in §5 bis §8 ein Präfix, danach 68 — gemessen mit
`grep -oE '(\[)?SONDE-01[235](\])? M-[0-9]+'` über den Abschnittsbereich vor und
nach der Änderung. Drei dieser Stellen brauchten neben dem Präfix ein Wort: die
Befundspalte von M-14 heißt jetzt „Hashhälfte **von** SONDE-015 M-84", die
Fristspalte von M-51 „Reihenfolge **nach** SONDE-015 M-84", und die Quellspalte
von M-43 trägt zur fremden Zeile ihre Fundstelle
(`docs/beweise/SONDE-015.md:1114`). Sonst ist jede nur präfixierte Matrixzeile
außerhalb ihres Präfixes zeichengleich geblieben (gemessen: nach Entfernen der
Präfixe weichen genau die sechs Zeilen aus §15.1 sowie M-14, M-43 und M-51 ab).

### 15.4 Geprüft und nicht geändert

- **M-80** (Grenzzeile Bandwert), ausdrücklich auf dieselbe Ursache wie M-81
  geprüft: ihre Mutation („`gain_db` in die Abdeckungstabelle aufnehmen → die
  erste Hälfte fällt, und der Kern schriebe Bandwerte ohne die Besitzregel, die
  NAK-340 erst baut") behauptet weder eine Quittierung durch den Blockrand noch
  einen Verlust beim Worker und widerspricht damit §7.3 Punkt 1 bis 5 nicht.
  Sie bleibt unberührt (Auftrag der Nacharbeit 2, E-312-12 letzter Satz).
- Die nachgestellt präfixierten Stellen „M-55 von SONDE-015" in §6.5 und §7.5
  sind eindeutig und bleiben im Wortlaut (Validierung Teil 2.1, Liste C).
- Codekommentare mit bloßer SONDE-015-Nummer
  (`eq-copilot/plugin/sonde/SondeProcessor.cpp:1454`) sind nicht Gegenstand:
  im Code gibt es keinen Namensraum dieses Manifests (R-312-13 letzter Satz).
- Nicht Gegenstand dieser Runde: H-A als eigene Mutation (durch E-312-10
  ausgeschlossen, erledigt durch das Etikett „zwei Hälften" in M-38), H2
  (Register NAK-342), die Etappe 7, NAK-340, jede Umformulierung ohne
  Defektbezug, §0 bis §4 und §9 bis §14.

### 15.5 Selbstaudit der Nacharbeit

- **Gelesen:** dieses Manifest §0 bis §14 (§14 verbindlich),
  `docs/beweise/roh/NAK-312-matrixpruefung-2-urteil.md` und
  `docs/beweise/roh/NAK-312-matrixpruefung-2-validierung.md` vollständig,
  `tools/dirigent/pruefliste.md` als Arbeitsliste sowie der Code an jeder
  Stelle, die diese Runde schreibt oder ändert.
- **Eigene Messungen, lesend:** `git rev-parse HEAD` (`801cb75a`),
  `git status --short` (nur `briefing-hub/` und `nimbalyst-local/`),
  `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` und
  `git diff --stat abb64275 HEAD -- eq-copilot broker tools` (beide leer),
  `git ls-files --eol docs/beweise/NAK-312.md` (LF vor und nach dem Schreiben),
  `git grep -n -oE 'M-[0-9]+'` über §5 bis §8 vor und nach der Änderung.
- **Am Code am HEAD `801cb75a` selbst nachgelesen:**
  `eq-copilot/plugin/tests/TransactionTestMain.cpp:72-91` (eigener Zähler, kein
  `RtWache::meldeAllokation()`), `:3368` und `:3374-3376`;
  `eq-copilot/plugin/tests/DspGoldenTestMain.cpp:100` und `:109`;
  `eq-copilot/plugin/dsp/DspRtWache.h:36-38`, `:41-44`, `:50-53`, `:78-93`
  (einziger Aufrufer von `meldeSperre` ist `GemeldeteSperre`, `:84`);
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:1139`, `:1145`, `:1172`, `:1184`,
  `:1192`, `:1325`, `:1455`, `:1460-1462`;
  `eq-copilot/plugin/dsp/DspProgramm.cpp:263`;
  `eq-copilot/plugin/dsp/DspKern.cpp:620-623` und `:626`;
  `eq-copilot/plugin/state/NakamaParameter.cpp:79`;
  `eq-copilot/plugin/src/prozessor/State.cpp:262-273`;
  `eq-copilot/plugin/src/PluginProcessor.cpp:874`;
  `docs/beweise/SONDE-015.md:1087`, `:1100`, `:1114`, `:1159`, `:1162`,
  `:1175`, `:1223`, `:1224`; `docs/beweise/SONDE-013.md:519-521`.
- **Selbstaudit gegen die Anforderungen des Auftrags:** nach der Änderung
  liefert `git grep` für jedes bloße `M-nn` in §5 bis §8 eine eigene Zeile M-01
  bis M-86; jede fremde Nennung trägt ihr Präfix. Alle Zeilen, die
  `RtWache::sperren()` nennen (M-01, M-02, M-04, M-26, M-38, M-40, M-53),
  wurden einzeln gelesen: keine behauptet einen Anstieg für eine Sperre
  außerhalb von `RtWache::GemeldeteSperre` — M-04 und M-40 sprechen über
  `:1325`, die Etappe 2 ausdrücklich über den gemeldeten Adapter nimmt (§7.2),
  und M-38 behauptet nach dieser Runde keinen Sperrenanstieg mehr. Keine Zeile
  verlangt im Audio-Thread eine Sperre, Allokation, ein Warten oder Logging.
  Jede geänderte Matrixzeile trägt elf Trenner und keinen ungeschützten
  senkrechten Strich; §6.8 ist nachgezogen, die Zählung bleibt 86.
- **Kein Bau, kein Test, kein Kanon, kein FL Studio.** Das einzige gefahrene
  Skript ist `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-312.md` vor
  dem Commit.

## 16. Messung der Matrixnacharbeit 2, Auftrag Matrixprüfung 3 (Dirigent, 21.09.2026, 03:50 Uhr)

Worker `bbcebe29` (Opus, Effort xhigh, `dontAsk`), 03:24 bis 03:46 Uhr
(Commitzeit), Basis `801cb75a9ca68cbd3569f1d1bd7d617cc844e096`. Ergebnis: ein
Commit `b5e60e2cf821d521fc771866b5b9b491037cbf07` = `origin/master`, Baum sauber
(nur die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD,
`git diff --stat 801cb75a..b5e60e2c`: eine Datei, `docs/beweise/NAK-312.md`,
+280/-65. Vom Dirigenten an den Hunkköpfen gemessen: berührt sind nur die
Kopfzeilen Etappe und Basis-SHA, §5 bis §8 und der neue §15; §0 bis §4 und §9
bis §14 sind zwischen beiden Ständen unberührt. Rundenbilanz
`801cb75a..b5e60e2c`: Doku 1 Datei +280/-65, Produkt und Tests 0 Zeilen —
Matrixrunde, zählt für das Rundenbudget null.

Auftrag Matrixprüfung 3 (Wiederprüfung): frischer, lesender Opus-Thread, Effort
max (Codex-Woche 94 %); Auftrag
`docs/beweise/roh/NAK-312-matrixpruefung-3-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-matrixpruefung-3-urteil.md` (Zielpfad, noch nicht
angelegt). Prüfbereich schrumpft: nur
`git diff 801cb75a...b5e60e2c -- docs/beweise/NAK-312.md`, die Befundliste aus
§14.3 und das Gate; Urteil nur: geschlossen, und nichts gebrochen.

## 17. Matrixprüfung 3: PASS, Abschluss der Etappe 1, Auftrag Etappe 2 (Dirigent, 21.09.2026, 04:03 Uhr)

Berichtigung zu §16: die Überschrift nennt „03:50 Uhr"; gemessen ist, dass §16
vor dem Prüferstart um 03:49 Uhr (`Get-Date`) geschrieben und committet war
(Commit `75b66066`).

**Matrixprüfung 3: PASS.** Frischer, lesender Opus-Thread `54163af1`, Effort
max, `dontAsk`, 03:49 bis vor 04:02 Uhr, HEAD
`75b66066e6762067c1b6fa6697337f94faaf6659` vor und nach dem Lauf gleich (vom
Prüfer und vom Dirigenten gemessen); geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-matrixpruefung-3-urteil.md`. D-A, D-B, D-C und L-A
sind geschlossen, kein geänderter Satz bricht einen anderen; benannt sind eine
Lücke (L-B) und eine Härtung (H-B), beide ohne Defekt. Ein Lauf ohne Defekt ist
PASS; eine Validierung entfällt, weil kein Befund Nacharbeit auslöst. Die
Verhaltensmatrix M-01 bis M-86 und der Bauplan der Etappen 2 bis 6 sind damit
geprüft; die Grenze „kein Produktcode, bevor die Verhaltensmatrix geprüft ist"
ist erfüllt.

**Einordnung.**

- **L-B → R-312-15 (Technik, Dirigent).** Für M-80 gilt dieselbe Form wie für
  M-81 nach E-312-12: die Mutation „`gain_db` in die Abdeckungstabelle
  aufnehmen" fällt an der eigenen Mengenbehauptung der Zeile („`gain_db` steht
  nicht in der Abdeckungstabelle"); der Halbsatz „der Kern schriebe Bandwerte
  ohne die eigene Besitzregel, die NAK-340 erst baut" ist Warnung, keine Stufe
  des Rotlaufs; die Verhaltenshälfte ist messend und trägt keine eigene
  Mutation. Der Bauer der Etappe 3 baut M-80 in dieser Lesart und hält sie in
  seinem Etappenabschnitt fest; eine eigene Matrixrunde löst die Lücke nicht
  aus.
- **H-B → Register NAK-344** (Prüfform für R-312-13), kein Auftrag.

**Rundenbilanz der Etappe 1, kumuliert.** Zwei Matrixnacharbeiten
(`abb64275..d5e3e33a` Doku +728/-147; `801cb75a..b5e60e2c` Doku +280/-65),
Produkt und Tests je 0 Zeilen — Matrixrunden zählen für das Rundenbudget null.

**Auftrag Etappe 2 (Messgeräte).** Frischer Opus-Worker, Effort xhigh,
`dontAsk`, Workername mit der Endung `-bau` (Register NAK-343); Auftrag
`docs/beweise/roh/NAK-312-etappe-2-auftrag.txt`. Aufsicht ENG. Danach
Erstprüfung der Etappe durch einen frischen, lesenden Prüfer (Codex-Woche 94 %,
deshalb Opus max, Kopfzeile Prüfmodell).

## 18. Etappe 2 — Messgeräte (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-2-auftrag.txt`; Spezifikation §5.1,
§5.2, §6.1, §7.0 bis §7.2 und E-312-11 (§14.4). Uhrzeiten aus `Get-Date` oder
Commitzeit. Zeilenangaben in diesem Abschnitt gelten zum Stand der Etappe 2.

### 18.1 Start, Basis-SHA, Kanon vorher

- Start 21.09.2026, 04:04:43 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `f96bdd4a3834ef4469e110d2a286df0b86f4ff5d` (Basis-SHA
  der Etappe 2); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`.
- `git diff --stat 945ebfb1 HEAD -- eq-copilot broker tools` ist **leer**
  (gemessen): die Zeilenangaben des Bauplans gelten am Basis-SHA unverändert.
- **Kanon vorher: geerbt.** `git diff --stat 2d6519e8 HEAD -- eq-copilot broker
  tools` zeigt genau eine Datei, `eq-copilot/install/nakama-installer-v1.json`,
  3 Zeilen geändert: die zwei `sha256`-Werte der Bundles und
  `hashes_erzeugt_am` (Nachhashen nach dem Kanon von NAK-311). Keine Datei unter
  `eq-copilot/plugin`, `broker` oder `tools` hat sich bewegt. Nach der Regel des
  Kopfes gilt der Kanon des NAK-311-Endstands `2d6519e8`, GRUEN 69/69
  (`docs/beweise/roh/NAK-311-2d6519e-dirty.md`).

### 18.2 Gebaut

- `eq-copilot/plugin/sonde/SondeProcessor.cpp`: `RtWache::Bereich` ist die
  erste Anweisung von `processBlock` (R-312-1); `setNonRealtime` nimmt
  `zustandSchloss` über `RtWache::GemeldeteSperre<GemeldetesSchloss>` statt
  über `juce::ScopedLock` — dieselbe Sperre in derselben Ordnung, nur gemeldet.
  Produkt- und Testkonstruktor delegieren an einen privaten Konstruktor mit
  `V3Verdrahtung` (Muster Gen, NAK-246 D2); der Produktkonstruktor liefert SID,
  `pipeNameV3 (SID)` und die Installbindung wie bisher, der Produktzweig mit
  `controlV3.start()` ist unverändert.
- `eq-copilot/plugin/sonde/SondeProcessor.h`: der Schlossadapter
  `GemeldetesSchloss` (`lock()` ruft `enter()`, `unlock()` ruft `exit()`), der
  Testkonstruktor `SondeProcessor (const std::string&, ServerErwartung)`
  (fail-closed über `istProbePipename`), die Starthaken `v3StartFuerTest()`
  und `v3TelemetrieStartFuerTest()` und die Sicht `v3PipeNameFuerTest()` —
  alle Testzugänge unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`.
- `eq-copilot/plugin/tests/TransactionTestMain.cpp` (B7): `operator new` und
  `operator new[]` melden zusätzlich an `RtWache::meldeAllokation()`
  (E-312-11, wörtlich nach B6); Abschnitt W mit Testplayhead und den Fällen
  312/M-01, 312/M-02, 312/M-04; Kopfkommentar nachgezogen.
- `eq-copilot/plugin/tests/SondeLebenslaufTestMain.cpp` (neu, Ziel
  `EqCopSondeLebenslaufTest`, Bein **B31**): 312/M-05 bis 312/M-08 gegen den
  geteilten v3-Testserver `eq-copilot/plugin/tests/V3TestServer.h`, Prozessoren
  auf dem Heap.
- `eq-copilot/plugin/CMakeLists.txt`: das Ziel über `nakama_sonde012_test`
  (Probeeq-Quelle, `NAKAMA_SONDE_AKTIV=1`, `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3=1`,
  `nakama_kern_anbinden`), dazu in der `/utf-8`-Liste.
- `tools/beweise.ps1`: B31 deklariert (`AbPhase='jetzt'`); die B7-Behauptung
  um die Messgeräte ergänzt und ihr 32-kHz-Satz berichtigt („die
  Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock)"
  statt „die Echtzeitwache des Kerns"); die B6-Behauptung klargestellt (B6
  baut keine Sonde, sein Zähler misst ab `DspKern::verarbeite`).
- Nicht angefasst: `eq-copilot/plugin/dsp/DspRtWache.h` (nur vorübergehend für
  den Rotbeweis von M-03, bytegleich zurückgenommen), der Produktzweig der
  v3-Clients, jede Audiorechnung, Schemas, Broker, Installer, Identität,
  `design/`, `docs/plugin-wissen.md`.

### 18.3 Matrixzeilen der Etappe 2

Einzelläufe am Inhalt des Endstands (B7, B31, A16 und B6 zuletzt nach der
bytegleichen Rücknahme ihres letzten Rotbeweises, B11 und B12 vor den
Rotbeweisen, B30 danach):
B7 195 geprüft, 0 Fehler; B31 8 geprüft, 0 Fehler; A16 131 Prüfungen ok;
B11 21 bestanden; B12 4 bestanden; B30 168 bestanden; nach dem Rotbeweis M-03
B6 389 geprüft, 0 Fehler. Der volle Kanon steht in §18.7.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-01 | `312/M-01 wache_beginnt_am_callback_eintritt`, B7 | grün: 200 gemeldete Sperren bei 200 Playhead-Aufrufen, `RtWache::allokationen()` 0, eigener Zähler 0 | heute rot am Basisstand (0 Sperren); Mutation: `Bereich` aus `processBlock` entfernt → 0 → rot; `docs/beweise/roh/NAK-312-rot-M-01.txt` |
| M-02 | `312/M-02 der_zaehler_sieht_den_workerzug_nicht` (Teilfall von 312/M-01), B7 | grün: Workerzug mit 1137 eigenen Allokationen, beide Wachenzähler +0, Playhead-Sperre außerhalb +0, der Block danach +1 | Regressionswache; `Bereich` als Mitglied mit Konstruktorlebensdauer → der Workerzug zählt 1137 → rot; `docs/beweise/roh/NAK-312-rot-M-02.txt` |
| M-03 | A16 bestehend (131 Prüfungen, darunter `eq_an_bypass_aus_alles_neutral_ist_bitidentisch`), B6 bestehende Goldenabschnitte, voller Kanon | grün: A16 131/131, B6 389/389; Kanon §18.7 | Regressionswache; `std::vector` im `Bereich`-Konstruktor → die Allokationszähler von B6 und B7 fallen, A16 und die Goldens bleiben erwartungsgemäß grün; `docs/beweise/roh/NAK-312-rot-M-03.txt` |
| M-04 | `312/M-04 offlineflag_nimmt_eine_gemeldete_sperre_je_block`, B7 | grün: offline 200, ohne Wechsel 0 (Hälfte der Etappen 2 bis 4) | heute nicht messbar (Basisstand 0); Adapter zurück auf `juce::ScopedLock` → 0 → rot; `docs/beweise/roh/NAK-312-rot-M-04.txt` |
| M-05 | `312/M-05 testkonstruktor_ist_fail_closed`, B31 | grün: Probe-Name übernommen, fremder Name leer übernommen, kein Verbindungsversuch | heute nicht messbar; `istProbePipename`-Prüfung entfernt → der fremde Name wird übernommen → rot; `docs/beweise/roh/NAK-312-rot-M-05.txt` |
| M-06 | `312/M-06 starthaken_verbinden_ueber_die_probe_pipe` und `312/M-06 ohne_starthaken_bleiben_beide_clients_getrennt`, B31 | grün: Control und Telemetrie verbunden (Einzellauf 6 ms und 5 ms); ohne Haken 0 Versuche, 0 Serververbindungen | heute nicht messbar; `v3StartFuerTest` leer → Control nach 8 s getrennt → rot; `docs/beweise/roh/NAK-312-rot-M-06.txt` |
| M-07 | `312/M-07 sonde_mit_laufenden_clients_wird_abgebaut` und Exitcode von B31 | grün: Abbau mit beiden verbundenen Clients, Exit 0 | Matrixmutation (Stack) bleibt grün, Abweichung A-4; Ersatz: Worker im Destruktor nicht gejoint → Exit 0xC0000409 → rot; `docs/beweise/roh/NAK-312-rot-M-07.txt` |
| M-08 | drei Wachen `312/M-08 kein_bein_oeffnet_die_produktions_pipe`, B31 | grün: Name des Testservers und Startname im Probe-Namensraum | Regressionswache; Produktionsname im Bein → die Wache fällt vor dem Aufbau → rot, nichts geöffnet; `docs/beweise/roh/NAK-312-rot-M-08.txt` |
| M-09 | A16, B6, B7 einzeln; voller Kanon | grün (einzeln); Kanon §18.7 | Regressionswache; Matrixmutation (Tausch) bleibt grün, Abweichung A-5; Ersatz: `beendePreview` unter der gemeldeten Sperre entfernt → [SONDE-015] M-120 rot; `docs/beweise/roh/NAK-312-rot-M-09.txt` |

### 18.4 Abweichungen vom Bauplan, begründet

- **A-1 — A16 bekommt keinen Satz über den Bereich.** §7.2 nennt A16 unter den
  Beinen, die den Satz über den Bereich ab dem Eintritt in
  `SondeProcessor::processBlock` bekommen. A16
  (`eq-copilot/plugin/tests/SondeNullTestMain.cpp`) liest keinen Zähler der
  Wache — weder `RtWache` noch einen eigenen `operator new`; der Satz sagte
  dort mehr, als der Test misst (Prüfliste E), und R-312-1 bindet die Zeilen
  der Beine, „die sich auf die Wache stützen". Den Satz trägt B7, in dem M-01,
  M-02 und M-04 liegen; B6 bekommt die Klarstellung. Aus demselben Grund trägt
  B31 keinen Zählersatz. Die A16-Zeile ist unverändert.
- **A-2 — Behauptung von B31.** §7.2 nennt für das neue Bein „Aufbau über die
  Probe-Pipe, gehaltener Provider über die Stoppfrist, Marke nach dem
  Eigentümerende". Die zwei letzten misst erst Etappe 4 (M-30 ff.); die
  Behauptung nennt deshalb nur, was Etappe 2 misst (M-05 bis M-08), und der
  Kommentar über der Deklaration sagt, dass Etappe 4 sie erweitert.
- **A-3 — Form der Mutation von M-02.** Die Matrix nennt „Bereich zusätzlich
  in den Konstruktor oder in `setNonRealtime` gesetzt". Ein `Bereich` als
  lokale Variable im Konstruktorrumpf endete mit dem Rumpf und wirkte nicht;
  gebrochen wurde deshalb mit einem `Bereich` als Mitglied, das auf dem
  konstruierenden Thread entsteht und bis zum Destruktor lebt — genau das
  Risiko aus §7.2. Die Variante `setNonRealtime` träfe 312/M-02 nicht, weil der
  Fall `setNonRealtime` nicht ruft.
- **A-4 — M-07: die Matrixmutation ist kein Rotbeweis.** „Prozessor auf den
  Stack legen → Stapelüberlauf" setzt FeatureEngine-Objekte von rund 0,5 MB im
  Prozessor voraus. Gemessen: `sizeof (SondeProcessor)` = 81 664 Byte (von B31
  ausgegeben); zwei Sonden im Rahmen laufen grün (Rohdatei, Teil a). Die
  Heapregel bleibt im Bein (Projektregel, NAK-175), belegt ist sie hier nicht.
  Die Zusage der Zeile bleibt: der Ersatz bricht den Abbau selbst (Destruktor
  joint den Worker nicht) und lässt B31 mit 0xC0000409 enden — an der
  Exitcode-Hälfte der Zusage; die Prüfzeile 312/M-07 wird unter dieser
  Mutation nicht erreicht, weil schon die erste abgebaute Sonde (Ende von
  312/M-05) den Prozess beendet.
- **A-5 — M-09: die Matrixmutation ist kein Rotbeweis.** Die Zeile erwartet
  „312/M-41 rot"; 312/M-41 entsteht erst in Etappe 5 (§6.5). Unter derselben
  Sperre und aus einem Thread ist die Reihenfolge von `beendePreview` und
  `setzeHoermatrix` für keinen bestehenden Fall beobachtbar (Tausch gemessen:
  B7 195/195 grün). Ersatz an der Stelle, die Etappe 2 im Produktweg umbaut:
  `beendePreview` unter der gemeldeten Sperre entfernt → der bestehende Fall
  `offline_render_nutzt_den_bestaetigten_zustand` ([SONDE-015] M-120) fällt.
- **A-6 — M-03 mutiert vorübergehend `eq-copilot/plugin/dsp/DspRtWache.h`.**
  Die Rotbeweisspalte nennt den `Bereich`-Konstruktor; die Datei ist nicht
  Ticketpfad. Die Mutation ist bytegleich zurückgenommen (SHA-256
  `B6B882D9…` vorher und nachher, gleich HEAD); `git diff` der Etappe
  enthält die Datei nicht.
- **A-7 — Zieldeklaration.** Die Zeile M-07 nennt das Muster
  `EqCopSonde012ProjectReloadTest` samt Definitionsschleife, §7.2 das Muster
  der Sondenfunktion. Gebaut ist der Aufruf von `nakama_sonde012_test`: die
  Funktion setzt `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` selbst, ein Eintrag in der
  Definitionsschleife wäre doppelt. Das Kürzel B31 ist die erste freie
  B-Nummer in `tools/beweise.ps1`.
- **A-8 — „in BEIDEN Clients" (M-05).** Gemessen über `v3PipeNameFuerTest()`:
  beide Clients entstehen in der Initialisierungsliste aus diesem einen
  Mitglied (`controlV3 (…, v3PipeName, …)`, `telemetryV3 (…, v3PipeName, …)`);
  `ControlClient` und `TelemetryClient` zeigen ihren Namen nicht. Dass die
  Telemetrie den Probe-Namen trägt, belegt ihre Verbindung zum Testserver in
  M-06. Derselbe Maßstab wie bei Gen (NAK-246).
- **A-9 — „jeder in einem Bein gesetzte Pipename" (M-08).** Der fail-closed-Fall
  von M-05 übergibt absichtlich einen Namen außerhalb des Probe-Namensraums
  (einen synthetischen, nicht den Produktionsnamen) und startet keinen Client.
  Die Wache gilt für jeden Namen, der in einen Server oder einen Start geht,
  und steht vor deren Aufbau.
- **A-10 — der Workerzug von M-02.** Im Messfenster laufen eine Transaktion
  (Programmbau auf demselben Thread), ein Hostereignis und
  `kontrollTaktFuerTest()`. So hängt der Anstieg des eigenen Zählers nicht
  daran, ob der Hintergrundworker das Hostereignis zuerst verbraucht (Regel
  der Matrix: keine Zeile hängt an Wandzeit oder Zufall).

### 18.5 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Etappe 2

| Abschnitt | Punkt | Etappe 2 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge beim Verbinden, Trennen, Joins mit Frist | angewandt: Control verbindet über die Starthaken, die Telemetrie koppelt danach (M-06); Abbau mit beiden laufenden Clients (M-07); Destruktor und Fristen unverändert; Stopp-Fenster und Schleuse sind Etappe 4 |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | nicht zutreffend: kein Draht-, Schema- oder Statefeld berührt |
| D Bau- und Prüfriegel | fail-closed, Frische, was der Kanon nicht baut | angewandt: Testkonstruktor fail-closed (M-05), Probe-Wache vor jedem Server und Start (M-08); Binaryzeitstempel gegen die Quelle bei jedem Einzelbein und jedem Rotbeweis; B31 ist ein eigenes Kanonbein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: A-1 und A-2; B7 nennt genau die Messungen von M-01, M-02, M-04, B31 genau die von M-05 bis M-08, B6 nur die Klarstellung |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerzeilen B6, B7, B31; Kopfkommentare von B7 und B31 und Kommentare an `processBlock`, `setNonRealtime` und den Testzugängen; Manifestkopf (Etappe, Basis-SHA, Kanon). `git grep -n "Echtzeitwache des Kerns" -- tools` ist leer; in B7 trägt nur noch der NAK-311-Fall 311/M-139 den Wortlaut (wahr, enger als gemessen, nicht Etappe 2) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: neun Rohdateien `docs/beweise/roh/NAK-312-rot-M-01.txt` bis `docs/beweise/roh/NAK-312-rot-M-09.txt`, zwei Ersatzmutationen begründet (A-4, A-5) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Zähler, Größen und Zeiten aus Läufen und `Get-Date` |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: Starthaken und Abbau im selben Bein, Messgerät, Behauptungszeilen und Tests in einem Commit |

### 18.6 Selbstaudit

- **Zahlenränder.** Alle neuen Vergleiche sind ganzzahlig und exakt
  (`std::uint64_t`-Zähler, `int`-Aufrufzahlen); die Zähler werden vor jeder
  Messung zurückgesetzt; kein Gleitkommavergleich, NaN und ±Inf kommen in den
  neuen Vergleichen nicht vor. Die Millisekunden in M-06 und M-07 sind Detail,
  keine Schranke.
- **Audio-Thread.** Der `Bereich` ist ein Inkrement und ein Dekrement eines
  `thread_local int` — kein Atomic, keine Sperre, keine Allokation. In
  `setNonRealtime` kommt vor derselben Sperre `meldeSperre()` hinzu: ein
  thread-lokales Lesen und nur im Bereich ein `relaxed`-Inkrement; im Produkt
  ruft der Wrapper `setNonRealtime` vor dem Bereich von `processBlock`, dort
  zählt nichts. Belegt: A16 131/131 bitgleich, B6 389/389, B7 195/195 samt
  311/M-139 (0 Allokationen, 0 Sperren).
- **Produktpfad des Konstruktors.** Dieselben Werte in derselben Reihenfolge
  der Mitglieder; `brokerServerErwartung()` entsteht einmal und geht als Kopie
  an beide Clients statt zweimal (Konstanten der Installbindung, inhaltsgleich).
- **Save/Load.** Kein Statefeld berührt; B31 lädt seinen Stand über den
  Schreiber `nakama::state::speichere`, nicht aus Handschrift.
- **verbinden↔trennen, starten↔stoppen.** M-06 und M-07 im selben Bein; die
  Starthaken gibt es nur im Testbau; der Destruktor ist unverändert.
- **Kommentare (R-312-14).** Geprüft mit `git grep`:
  `eq-copilot/plugin/dsp/DspRtWache.h:9-11` nennt nur den Kern als Setzer des
  thread-lokalen Flags; seit Etappe 2 setzt es auch `SondeProcessor::processBlock`.
  Der Satz ist unvollständig, nicht falsch, und die Datei liegt außerhalb der
  Ticketpfade dieser Etappe — Hinweis an den Dirigenten.
  `eq-copilot/plugin/sonde/SondeProcessor.h:495` (`zustandSchloss` „nie
  processBlock") macht Etappe 2 nicht falsch; die Nahme im Audio-Callback über
  `setNonRealtime` ist T3-01-03 (Etappe 5).
- **Gesundheit.** A32 (Einzellauf vor dem Kanon): `SondeProcessor.cpp` wächst
  von 1480 auf 1532 Zeilen und liegt damit über dem Ziel 1500 (Maß ohne
  Grenze); die drei gerissenen Grenzen (Dateien über 2000 Zeilen, Funktionen
  über 200 Zeilen, Kommentar-Bezeichner ohne Code) sind dieselben wie im
  geerbten NAK-311-Kanon, keine Fundstelle liegt in einer Datei der Etappe 2.
  A33 (Einzellauf vor dem Kanon): 58 Fundstellen von höchstens 58, Grenze
  gehalten.
- **Rotbeweise.** Neun Rohdateien, jede mit SHA-256 vor und nach der Mutation
  (bytegleich), Zeitstempelprüfung Binary gegen Quelle und grünem Lauf danach.
  `git status --short` nach dem letzten Rotbeweis: nur die Dateien der
  Etappe und die zwei fremden Ordner.

---

## Kanon-Lauf - NAK-312 Etappe 2

**Lauf:** 2026-09-21 04:42 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-8943246-dirty.md](roh/NAK-312-8943246-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 04:42:20 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 89432468 NAK-312 Etappe 2: Messgeraete - RT-Wache ab Callback-Eintritt, Testzugaenge der Sonde, Bein B31 |
| Commit (voll) | 894324685da5ed4c694dad52020110980fe676a2 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,11 s | [A1](roh/NAK-312-8943246-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,74 s | [A2](roh/NAK-312-8943246-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,71 s | [A3](roh/NAK-312-8943246-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.134,14 s | [A4](roh/NAK-312-8943246-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,80 s | [A4-SI](roh/NAK-312-8943246-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,75 s | [A4b](roh/NAK-312-8943246-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,89 s | [A5](roh/NAK-312-8943246-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-312-8943246-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-312-8943246-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,38 s | [A8](roh/NAK-312-8943246-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,39 s | [A9](roh/NAK-312-8943246-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,02 s | [A10](roh/NAK-312-8943246-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,24 s | [A11](roh/NAK-312-8943246-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A12](roh/NAK-312-8943246-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,34 s | [A13](roh/NAK-312-8943246-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 8,94 s | [A14](roh/NAK-312-8943246-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,29 s | [A17](roh/NAK-312-8943246-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,69 s | [A18](roh/NAK-312-8943246-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-312-8943246-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,32 s | [A20](roh/NAK-312-8943246-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,34 s | [A21](roh/NAK-312-8943246-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,02 s | [A22](roh/NAK-312-8943246-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,68 s | [A24](roh/NAK-312-8943246-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,16 s | [A34](roh/NAK-312-8943246-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 33,40 s | [A35](roh/NAK-312-8943246-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 5,99 s | [A36](roh/NAK-312-8943246-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,13 s | [A28](roh/NAK-312-8943246-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,14 s | [A29](roh/NAK-312-8943246-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A30](roh/NAK-312-8943246-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 96,98 s | [A31](roh/NAK-312-8943246-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-312-8943246-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,15 s | [A26](roh/NAK-312-8943246-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,14 s | [A27](roh/NAK-312-8943246-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,75 s | [A23](roh/NAK-312-8943246-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,68 s | [A16](roh/NAK-312-8943246-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,25 s | [B1](roh/NAK-312-8943246-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,31 s | [B2](roh/NAK-312-8943246-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,07 s | [B3](roh/NAK-312-8943246-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,11 s | [B3b](roh/NAK-312-8943246-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,41 s | [B3c](roh/NAK-312-8943246-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,42 s | [B4](roh/NAK-312-8943246-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,16 s | [B9](roh/NAK-312-8943246-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,37 s | [B5](roh/NAK-312-8943246-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,16 s | [B6](roh/NAK-312-8943246-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, nimmt jeder Wechsel nach offline genau eine gemeldete Sperre, ohne Wechsel keine. Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 1,09 s | [B7](roh/NAK-312-8943246-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,68 s | [B10](roh/NAK-312-8943246-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,12 s | [B11](roh/NAK-312-8943246-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,64 s | [B12](roh/NAK-312-8943246-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 0,13 s | [B31](roh/NAK-312-8943246-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,13 s | [B13](roh/NAK-312-8943246-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,35 s | [B14](roh/NAK-312-8943246-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,25 s | [B15](roh/NAK-312-8943246-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,06 s | [B20](roh/NAK-312-8943246-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,24 s | [B22](roh/NAK-312-8943246-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,66 s | [B23](roh/NAK-312-8943246-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,01 s | [B24](roh/NAK-312-8943246-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,06 s | [B25](roh/NAK-312-8943246-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-312-8943246-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-312-8943246-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,96 s | [B19](roh/NAK-312-8943246-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,35 s | [B17](roh/NAK-312-8943246-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 5,67 s | [B18](roh/NAK-312-8943246-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,71 s | [B16](roh/NAK-312-8943246-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,41 s | [B27](roh/NAK-312-8943246-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,87 s | [B29](roh/NAK-312-8943246-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,42 s | [B30](roh/NAK-312-8943246-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,15 s | [B28](roh/NAK-312-8943246-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,28 s | [B8](roh/NAK-312-8943246-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 12,75 s | [A32](roh/NAK-312-8943246-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 107,27 s | [A33](roh/NAK-312-8943246-dirty.md#a33) |


### 18.7 Kanon nachher (Bauer, 21.09.2026)

Voller Kanon abgekoppelt nach dem Auftrag, Befehl `pwsh -NoProfile -File
tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312
Etappe 2'`, auf dem Commit `894324685da5ed4c694dad52020110980fe676a2`; Start
04:42:20 (`Get-Date`), Ende 05:27:17 (Zeitstempel der Logdatei, letzte Zeile
`EXIT=0`). Kopf und Übersicht stehen im Runnerblock „Kanon-Lauf - NAK-312
Etappe 2" unmittelbar über diesem Abschnitt, die Rohausgabe unter
`docs/beweise/roh/NAK-312-8943246-dirty.md`. Kopfzeile des Runners, wörtlich:

```
**Lauf:** 2026-09-21 04:42 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0
```

- **GRÜN 70/70**, Exitcode 0; stillgelegt bleibt A15. Die Zahl steigt gegen
  den geerbten Kanon (69/69) um genau das neue Bein B31
  (`EqCopSondeLebenslaufTest`, [OK] Exit 0). A16, B6, B7, B11, B12 und B30,
  die Beine der Etappe, sind grün; A32 endet mit [HINWEIS] Exit 4, nicht
  blockierend (dieselben drei Grenzen wie im geerbten Kanon, §18.6), A33 mit
  [OK] Exit 0.
- **„dirty"** im Namen der Rohausgabe: die Kopfzeile Arbeitsbaum meldet zwei
  unbestätigte Dateien — die fremden untracked Ordner `briefing-hub/` und
  `nimbalyst-local/`; `git status --short` zeigte vor und nach dem Lauf sonst
  nichts außer den Schreibzielen des Runners.
- **Lauf ohne Eingriff in Quellen, zwei Stillstände erklärt.** Vor dem Start
  lief kein Bau- oder Testprozess (NAK-290); ein liegengebliebenes
  `vctip.exe` aus meinem Einzelbau war vorher beendet. Nach „Baue:" stand das
  Log rund zehn Minuten: kein Compiler aktiv, nur ein neues `vctip.exe`
  (Start 04:42:28), um 04:52:56 beendet (NAK-300); danach lief der Runner
  weiter. Von 04:53 bis nach 05:08 stand das Log im Bein A4 (`cargo test` des
  Brokers, Testbinary mit steigender CPU-Zeit; im NAK-311-Kanon dauerte A4
  1 213,79 s).
- **Installer-Hashes.** Der Runner pinnt den Release-Broker ins
  Installer-Manifest; `eq-copilot/install/nakama-installer-v1.json` blieb
  unverändert. Die Bundlebytes von Nakama Probeeq haben sich mit dieser
  Etappe geändert; vor einer Installation (Laufzeit-Arm) ist nach der Regel in
  `CLAUDE.md` `--hashen` fällig — nicht Ticketpfad dieser Etappe, Hinweis an
  den Dirigenten.

## 19. Messung der Etappe 2, Laufzeit-Arm, Auftrag Erstprüfung (Dirigent, 21.09.2026, 05:37 Uhr)

**Messung.** Worker `56137fde` (Opus, Effort xhigh, `dontAsk`), 04:04 bis
05:28 Uhr (Commitzeit des zweiten Commits), Basis
`f96bdd4a3834ef4469e110d2a286df0b86f4ff5d`. Ergebnis: zwei Commits,
`89432468` (04:42 Uhr, Bau, Tests, Rotbeweise, §18) und `aa074e42` (05:28 Uhr,
Kanon nachher, Manifestkopf) = `origin/master`; Baum sauber (nur die zwei
fremden untracked Ordner), Basis ist Vorfahr von HEAD. Produkt- und Testdiff
`git diff --stat f96bdd4a..aa074e42 -- eq-copilot broker tools`: sechs Dateien,
+587/-12, alle in den Ticketpfaden der Etappe 2 (§7.0); `DspRtWache.h` ist
nicht im Diff (A-6). Rundenbilanz `f96bdd4a..aa074e42`: Produkt 3 Dateien
+120/-6, Tests 2 Dateien +459/-4, Prüfwerkzeug 1 Datei +8/-2, Doku 11 Dateien
+11161/-4. Messabdeckung: M-01 bis M-09 tragen je Test, Ergebnis und
Rotbeweis-Rohdatei (§18.3); zwei Matrixmutationen waren kein Rotbeweis und sind
begründet ersetzt (A-4 zu M-07, A-5 zu M-09). Kanon nachher vom Dirigenten am
Log gelesen: `GRUEN - 70/70`, `EXIT=0`, keine rote Zeile. Produkthunks vom
Dirigenten gelesen (`SondeProcessor.cpp`, `SondeProcessor.h`): der
Produktkonstruktor delegiert an den privaten Konstruktor mit unveränderter SID,
`pipeNameV3` und Installbindung; `verdrahtung.erwartung` wird zweimal kopiert
und nicht verschoben; der Testkonstruktor steht unter
`NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` und ist fail-closed.

**Laufzeit-Arm** (05:29 bis 05:36 Uhr, Stand `aa074e42`):
`LAUFZEIT NAK-312 aa074e42 VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`,
Exit 3 allein wegen Karte U43 (Referenzprojekt
`Nakama-Diagnose-Verarbeitung.flp` fehlt, K-286-1; Muster NAK-309 und
NAK-311), kein Szenario verfehlt im Sinn von Exit 4. **Nulltest im Host am
Auslieferungszustand BITIDENTISCH** (v = 0, g = 1,0, 0 Abweichungen); Vergleich
`ohne_slots` GLEICH. Rohdaten `docs/beweise/roh/NAK-312-laufzeit-aa074e42.md`;
die nachgezogenen Hashes in `eq-copilot/install/nakama-installer-v1.json`
gehören zu diesem Commit.

**Auftrag Erstprüfung Etappe 2.** Frischer, lesender Opus-Thread, Effort max
(Codex-Woche 94 %, Kopfzeile Prüfmodell), Vorlage A aus
`tools/dirigent/pruefauftrag-vorlage.md`; Auftrag
`docs/beweise/roh/NAK-312-etappe-2-erstpruefung-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-etappe-2-erstpruefung-urteil.md` (Zielpfad, noch
nicht angelegt). Prüfbereich: `git diff f96bdd4a...<Commit dieses Abschnitts>`
über die sechs Produkt-, Test- und Werkzeugpfade der Etappe 2; `docs/**` ist
ausgeschlossen.

## 20. Erstprüfung der Etappe 2: PASS, Abschluss der Etappe 2 (Dirigent, 21.09.2026, 06:00 Uhr)

**Erstprüfung Etappe 2: PASS.** Frischer, lesender Opus-Thread `bb00661f`,
Effort max, `dontAsk`, Prüfstufe T2, Vorlage A, 05:37 bis vor 05:59 Uhr, HEAD
`4949f0239db3fcac301b2280953df091e047e1cb` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Prüfbereich
`git diff f96bdd4a...5a049913` über die sechs Produkt-, Test- und
Werkzeugpfade der Etappe 2 (+587/-12), vollständig gelesen; geschrieben wurde
nur der Bericht `docs/beweise/roh/NAK-312-etappe-2-erstpruefung-urteil.md`.
0 Defekte, 0 Lücken, 2 Härtungen (H-1 Wortlaut der B7-Behauptung zum
Offlineflag, H-2 Produktverdrahtung des Konstruktors ungemessen) → Register
NAK-345, kein Auftrag. Ein Lauf ohne Defekt ist PASS; eine Validierung
entfällt. Rundenbudget der Etappe 2: keine Nacharbeitsrunde.

**Hygiene im Abschlussfenster der Etappe** (per Kommando gemessen während des Prüferlaufs, zwischen 05:37 und 05:59 Uhr):
`MEMORY.md` 5 813 Bytes, Root-`CLAUDE.md` 20 229 Bytes (Grenze 20 480),
Dirigenten-Skill 24 568 Bytes (Grenze 24 576), keine Indexzeile über 250
Zeichen, keine Memory-Datei ohne Indexlink; `gesundheit.py` im Kanon der
Etappe Exit 4 mit den bekannten Codebase-Maßen (Register NAK-292, NAK-343),
kein neuer Riss.

**Nächster Schritt: Etappe 3** (erst W02 Ladestart, dann Blockbindung, je ein
eigener Änderungssatz mit eigenem Commit; §7.3), Auftrag
`docs/beweise/roh/NAK-312-etappe-3-auftrag.txt`. R-312-15 (M-80) gilt.

## 21. Etappe 3 — W02 Ladestart und Blockbindung (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-3-auftrag.txt`; Spezifikation §5.3,
§5.4, §6.2, §6.3, §7.0, §7.1, §7.3, E-312-5, E-312-6, R-312-11 und R-312-12
(§11.3), R-312-13, E-312-11, E-312-12 und R-312-14 (§14.4), R-312-15 (§17).
Uhrzeiten aus `Get-Date` oder Commitzeit. Zeilenangaben in §21.1 bis §21.3
gelten zum Stand des Commits der Etappe 3a, danach zum Stand der Etappe 3b.
Fremde Matrixzeilen tragen ihr Präfix (R-312-13).

### 21.1 Start, Basis-SHA, Kanon vorher

- Start 21.09.2026, 06:02:44 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `cb473944a12410e17f3205e231b32f35f92805ac` (Basis-SHA
  der Etappe 3); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`.
- Der Bauplan gilt zum Stand `945ebfb1`; `git diff --stat 945ebfb1 HEAD --
  eq-copilot/plugin` zeigt die fünf Dateien der Etappe 2. Jede Zeilenangabe ist
  am Basis-SHA nachgelesen: `setStateInformation` beginnt dort bei
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:1098` (Bauplan `:1051`),
  `zelleAusHost` bei `:1446`, `hostParameterAbgleichen` bei `:1474`,
  `dspKontrollTakt` bei `:1499`, `reset` bei `:355`, `processBlock` bei `:409`.
- **Kanon vorher: geerbt.** `git diff --stat 89432468 HEAD -- eq-copilot broker
  tools` zeigt genau eine Datei, `eq-copilot/install/nakama-installer-v1.json`,
  3 Zeilen: die zwei `sha256`-Werte der Bundles und `hashes_erzeugt_am` (das
  Nachhashen des Laufzeit-Arms, §19). Keine Datei unter `eq-copilot/plugin`,
  `broker` oder `tools` hat sich bewegt; nach der Regel aus §7.0 gilt der Kanon
  nachher der Etappe 2, GRUEN 70/70 am Stand `89432468`
  (`docs/beweise/roh/NAK-312-8943246-dirty.md`, §18.7).

### 21.2 Gebaut — erster Änderungssatz (W02; T3-05-01, T3-05-02)

- `eq-copilot/plugin/sonde/SondeProcessor.cpp`, `setStateInformation`: der
  Ladestart gibt das read-only des Standes an den Kern
  (`ladestart (…, zustand.nurLesen)`). Danach, im selben Zustandsschloss und
  **vor** `hostParameterAbgleichen`, die **Quittierung** der Hostwert-Mailbox:
  erst `hostEreignisOffen.exchange (false)`, dann je Hostparameter
  `hostEreignisGesehen[i] = hostEreignis[i]` und dieselbe Zahl in den lokalen
  Stand `quittiert` (`:1153-1159`). Vorhandene Felder, keine Allokation, kein
  Statefeld, kein Vertragswechsel. Im Testbau läuft danach der Ladehaken
  (312/M-78).
- `hostParameterAbgleichen (werte, quittiert)` (R-312-11, `:1504-1543`): mit dem
  Stand der Quittierung lässt der Abgleich jeden Parameter stehen, dessen
  Zähler nicht mehr **gleich** dem quittierten ist (nie ein Ordnungsvergleich),
  und stellt nach seiner Schleife in einer zweiten Prüfung einen Regler, den ein
  endlicher Hostwert während des Schreibens erreicht hat, auf diesen Hostwert.
  Alle übrigen Aufrufer rufen ohne Stand wie bisher.
- `dspKontrollTakt` (`:1558-1606`): verbraucht der Takt ein Hostereignis, während
  der geladene Stand read-only ist, ruft er nach dem Loslassen des Schlosses
  `hostParameterAbgleichen` mit dem bestätigten (neutralen) Satz — derselbe Weg
  wie beim abgewiesenen Gestus; der Hostregler springt zurück (312/M-16).
- `eq-copilot/plugin/state/NakamaTransaktion.h` und `.cpp`: `ladestart` bekommt
  `nurLesen` (Vorgabe `false`, alle übrigen Aufrufer unverändert) und setzt
  `nurLesenStand` bei Erfolg in **beide** Richtungen (`:272`);
  `Transaktionskern::wirksam` legt bei `nurLesenStand` kein Overlay über den
  bestätigten Satz (`:349`); `nurLesen()` liest das Flag.
- `eq-copilot/plugin/sonde/SondeProcessor.h`: unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` die **Taktsperre**
  `mitAngehaltenemTaktFuerTest` (hält `zustandSchloss`, wörtlich nach dem Muster
  `mitAngehaltenerAnalyseFuerTest`), `setzeLadeHakenFuerTest` und
  `setzeHostZaehlerFuerTest`; Doku zu Takt und Abgleich.
- `eq-copilot/plugin/tests/TransactionTestMain.cpp` (B7): Abschnitt X mit
  312/M-10 bis 312/M-13, 312/M-15 bis 312/M-19 und 312/M-78; 312/M-14 als
  Teilfall des bestehenden `reload_rekonstruiert_denselben_audioausgang` in
  Abschnitt O; Kopfkommentar nachgezogen.
- `tools/beweise.ps1`: die B7-Behauptung um genau die gemessenen W02-Fälle
  ergänzt.
- Nicht angefasst: `getStateInformation`, die Mailbox in `reset` (312/M-13),
  Revisions- und Undo-Wege, Schemas, Fixtures, Identität, Broker, Installer.

### 21.3 Matrixzeilen des ersten Änderungssatzes

Einzelläufe am Endstand 3a (Binaries jünger als jede Quelle): B7 206 geprüft,
0 Fehler; A16 131 Prüfungen ok; B2 291; B3c 162 bestanden; B6 389 geprüft,
0 Fehler; B31 8; B11 21; B12 4; B30 168; A1 `NULLTEST OK`. Gegenprobe am
Basisstand mit der Endfassung der Tests:
`docs/beweise/roh/NAK-312-etappe3a-gegenprobe.txt` (B7 Exit 1, 206 geprüft,
7 Fehler: 312/M-10, M-11, M-12, M-15, M-16, M-19, M-78; grün 312/M-13, M-14,
M-17, M-18).

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-10 | `312/M-10 ladestart_macht_ausstehende_hostwerte_wirkungslos`, B7 | grün: wirksam +3 dB nach dem Takt und nach der Wiederöffnung der Mailbox, Regler +3 dB, Ausgang/Eingang 8,0e-8 von 10^(3/20) über 512 Samples | heute rot (Basisstand −9 dB, wie Phase 16 C); Quittierungszeile entfernt → der alte Wert lebt auf → rot; `docs/beweise/roh/NAK-312-rot-M-10.txt` |
| M-11 | `312/M-11 neuer_hostgestus_nach_dem_recall_wirkt`, B7 | grün: geladen +3 dB, danach −6 dB, Klang folgt | heute nicht messbar als eigener Fall; Matrixmutation `0xFFFFFFFF` → rot (Teil a), zusätzlich `n + 1` → Gestus geschluckt → rot (Teil b); `docs/beweise/roh/NAK-312-rot-M-11.txt` |
| M-12 | `312/M-12 prepare_nach_dem_laden_publiziert_den_geladenen_stand`, B7 | grün: +3 dB, 8,1e-8 | heute rot (−9 dB); Mutation wie M-10 → rot; `docs/beweise/roh/NAK-312-rot-M-12.txt` |
| M-13 | `312/M-13 reset_bewahrt_die_hostmailbox`, B7 | grün: −9 dB, 2,0e-8 | Regressionswache; Quittierung in `reset` → +3 dB → rot; `docs/beweise/roh/NAK-312-rot-M-13.txt` |
| M-14 | `312/M-14 ladestart_in_dieselbe_instanz_haelt_den_hash` (Teilfall von `reload_rekonstruiert_denselben_audioausgang`), B7 | grün: bestätigter Zustand, Hash, Ring, Cursor, Revision gleich der Quelle | Regressionswache; Matrixmutation bleibt grün (A-12), Ersatz: Mailboxwert in den geladenen Stand → Hash weicht ab → rot; `docs/beweise/roh/NAK-312-rot-M-14.txt` |
| M-15 | `312/M-15 read_only_bleibt_audio_neutral`, B7 | grün: 0 von 1024 Werten abweichend, nurLesen, Fixture bytegleich, Dirty 0 | heute rot (1022 abweichend); `! nurLesenStand` entfernt → rot; `docs/beweise/roh/NAK-312-rot-M-15.txt` |
| M-16 | `312/M-16 read_only_regler_springt_auf_neutral`, B7 | grün: eq_enabled 0, Output-Trim 0 dB | heute rot (1 und +6 dB); Abgleichanforderung im Takt entfernt → rot; `docs/beweise/roh/NAK-312-rot-M-16.txt` |
| M-17 | `312/M-17 nach_read_only_wirkt_automation_wieder`, B7 | grün: +6 dB, 1,1e-7 | Regressionswache; Flag nur setzen → 0 dB → rot; `docs/beweise/roh/NAK-312-rot-M-17.txt` |
| M-18 | `312/M-18 nicht_endlicher_hostwert_bleibt_wirkungslos`, B7 | grün: bestätigte Werte, alle Samples endlich, read-only 0 abweichend | Regressionswache; Endlichkeitsprüfung entfernt → NaN im Overlay → rot; `docs/beweise/roh/NAK-312-rot-M-18.txt` |
| M-19 | `312/M-19 quittierung_ist_ueberlaufsicher`, B7 | grün: −6 / −9 / +3 / −3 dB, Epoche steht | heute nicht messbar; `==` durch `<` → −9 dB nach dem Überlauf fehlt → rot; `docs/beweise/roh/NAK-312-rot-M-19.txt` |
| M-78 | `312/M-78 hostwert_nach_der_quittierung_wirkt_und_wird_gezeigt`, B7 | grün: wirksam −6 dB, Regler −6 dB, Klang am Soll | heute nicht messbar (am Basisstand gemessen: Regler +3 dB, Klang −6 dB); Zählerprüfung im Abgleich entfernt → rot; `docs/beweise/roh/NAK-312-rot-M-78.txt` |

### 21.4 Abweichungen im ersten Änderungssatz, begründet

- **A-11 — die Taktsperre entsteht schon im ersten Änderungssatz.** §7.0 und
  §7.3 führen sie bei der Blockbindung. 312/M-10 bis 312/M-19 und 312/M-78
  brauchen sie bereits: ohne sie tickt der freie Worker zwischen `hostSchreibt`
  und dem Ladestart und verbraucht den alten Hostwert vorher, dann hinge
  „heute rot" am Wettlauf (Matrixregel §6). Sie ist ein Testzugang unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` und ändert kein Produktverhalten.
- **A-12 — 312/M-14: die Matrixmutation ist kein Rotbeweis.** „Die Quittierung
  vor `ladestart` statt danach setzen und dabei die Revision mitziehen" ist im
  baubaren Teil gefahren (Quittierung vor den Ladestart verschoben): B7 bleibt
  206/206 grün — die Quittierung berührt allein die Mailbox, der Ladestart allein
  den Transaktionskern, und eine Revision kennt die Quittierung nicht. Ersatz an
  derselben Stelle: der ausstehende Hostwert wird vor dem Ladestart in den
  geladenen Stand übernommen, statt ihn zu quittieren → Hash und bestätigter
  Zustand weichen ab → rot. Der Fall vergleicht im bestehenden Block dessen
  Stand (zwei Bänder, Width 1,3, Auto-Gain, eine Zone) über den **ganzen**
  bestätigten `DspSatz` statt über „+3,0 dB", dazu Ring, Cursor und Revision.
- **A-13 — Wiederöffnungsschritt in 312/M-10, 312/M-12 und 312/M-19.** Der erste
  Rotlauf von 312/M-10 blieb unter der Matrixmutation grün: das Zurücksetzen von
  `hostEreignisOffen` verdeckt einen alten Wert allein — bis irgendein anderes
  Hostereignis die Mailbox wieder öffnet. Die drei Fälle schreiben deshalb nach
  dem Laden ein Hostereignis auf einen freien Slot (klanglos, [SONDE-015] M-63)
  und prüfen danach erneut. Die Gegenprobe ist mit dieser Endfassung gefahren.
- **A-14 — 312/M-11 in einer eigenen Instanz.** Hinter dem Wiederöffnungsschritt
  von 312/M-10 hätte der erste Takt den verschobenen Quittierungsstand wieder
  glattgezogen. Die Matrixmutation (`0xFFFFFFFF`) ist Rotbeweis (Teil a: der
  Klang folgt dem Gestus nicht, weil der erste Takt alle 112 Mailboxwerte
  wiederbelebt); Teil b (`n + 1`) zeigt zusätzlich das Schlucken selbst.
- **A-15 — das Flag fällt per Tausch.** §7.3 sagt „auf false gelegt". Eine bloße
  Speicherung darf die CPU hinter das folgende Lesen der Zähler ziehen; ein
  gleichzeitig eintreffender Hostwert bliebe dann bis zum nächsten Ereignis
  liegen. `exchange` ordnet sich davor, wie im Takt.
- **A-16 — R-312-11, die zweite Prüfung.** Gebaut wie verlangt (nach der
  Schleife, auf Gleichheit, nur mit endlichem Hostwert). Ihr eigenes Zeitfenster
  — ein Hostwert WÄHREND des Schreibens der Schleife — misst keine Zeile; 312/M-78
  misst die erste Prüfung, und unter seiner Mutation entfallen beide.
- **A-17 — der Rücksprung des Reglers im read-only-Stand läuft im Worker.**
  §7.3 legt ihn in den Kontrolltakt, und der läuft im Produkt auf dem
  Workerthread. `setValueNotifyingHost` von dort landet im VST3-Wrapper im
  Parametercache (`paramChanged` → `setParameterValue`, Bauartefakt
  `juce_audio_plugin_client_VST3.cpp:1469-1484`), nicht direkt beim Host. Ob FL
  einen solchen Rücksprung während laufender Automation als Eingriff wertet,
  ist nicht gemessen — Hinweis an den Dirigenten für den Laufzeit-Arm.

### 21.5 Gebaut — zweiter Änderungssatz (Blockbindung; T3-01-05 Teil a)

Zeilenangaben zum Stand des Commits der Etappe 3b.

- `eq-copilot/plugin/dsp/DspKern.h` und `.cpp` (Blockrandübernahme):
  - die **Abdeckungstabelle** `DspKern::kBlockrandParameter` (`DspKern.h:232`):
    `v1.global.input_trim_db`, `v1.global.output_trim_db`, `v1.global.width`,
    `v2.global.mix` — dieselben Indizes wie `baueProgramm`.
  - `verarbeite (…, const BlockrandHostwerte* hostwerte = nullptr)`: die
    Mailboxlesung `uebernimmBlockrandHostwerte` steht vor `blockrand
    (committed)` und vor dessen frühem Rücksprung bei laufendem Übergang
    (`DspKern.cpp:1149`, E-312-6 Punkt 2). Je neuem Stand setzt sie
    `blockStand`; bei einem Hostgestus rechnet sie das Rampenziel mit derselben
    Formel wie `baueProgramm` (Trims: 0 dB ergibt 1,0, sonst `dbInLinear`; Width
    und Mix als Wert) und setzt es, wenn der Committed-Pfad eine aktive Bank hat
    — ein ruhender oder ausblendender Pfad nimmt kein Ziel (NAK-311 T3-15-05),
    das Ziel bleibt gemerkt (`DspKern.cpp:661-694`). Ohne `hostwerte` (alle
    übrigen Aufrufer, B6) arbeitet der Kern wie bisher.
  - der **Publikationsstand** reist je Bank (`bankStand`, geschrieben in
    `publiziereVorbau` vor der Übergabe, `DspKern.cpp:431`); bei der
    Programmübernahme setzt `setzeProgrammziel` (`:696-707`) das Ziel eines
    abgedeckten Parameters des Committed-Pfades nur, wenn der Stand des
    Programms **gleich** dem Blockrandstand ist, sonst gilt das Blockrandziel
    (E-312-6 Punkt 4). Candidate-Pfad unverändert.
  - die **Neutralprüfung** (NAK-311) schreibt bis zum Ende der längsten
    laufenden Parameterrampe (`cRampenEnde`, `:1241`, `:1437`) — Befund im
    Bau, A-19.
  - Testzugänge: `blockrandStand`, `setzeBlockrandStandFuerTest`,
    `blockrandZiele`, `setzePublikationsStand`.
- `eq-copilot/plugin/sonde/SondeProcessor.cpp` und `.h`:
  - `processBlock` (`:494-524`): je Platz der Tabelle der Ereigniszähler mit
    `acquire`; nur bei **ungleichem** Stand die Quittung des Ladestarts
    (`blockrandQuittung`) und sonst Hostwert und Zelle; `hostEreignisOffen`
    liest und tauscht der Blockrand nicht (E-312-6 Punkt 5).
  - `zelleAmBlockrand` (`:1566-1582`): dieselbe Zelle wie `zelleAusHost`
    (Ausnahme „gleich dem bestätigten Wert in Hostgenauigkeit",
    Endlichkeitsregel, Bereich) ohne Transaktionskern, aus dem veröffentlichten
    bestätigten Wert `bestaetigtBlock`.
  - `bestaetigteBlockwerteVeroeffentlichen` unter dem Zustandsschloss in
    Konstruktor (`:221`), Ladestart (`:1213`) und Commit (`:1387`);
    `blockrandQuittung` im Ladestart (`:1211`, Vorgabe `kKeineQuittung`);
    `publikationsStandSetzen` vor jeder Publikation des Committed-Pfades:
    `prepareToPlay` (`:342`), Ladestart (`:1220`) und Takt (`:1703`) mit
    `hostEreignisGesehen`, Commit (`:1378`) mit dem aktuellen Zähler (der Commit
    quittiert).
  - `parameterValueChanged` erhöht den Zähler mit `release` (`:1480`), Blockrand
    und Takt lesen ihn mit `acquire` (A-21).
  - der M-119-Kommentar im Konstruktor (§1, irreführend) ist berichtigt
    (`:191-199`), der Klassenkopf nennt die Blockbindung;
    `setzeHostZaehlerFuerTest` setzt auch den Blockrandstand (312/M-82).
- `eq-copilot/plugin/tests/TransactionTestMain.cpp` (B7): Abschnitt Y
  (312/M-20 bis M-23, M-26 bis M-29, M-79 bis M-82, der Teilfall
  `nicht_endliche_hostwerte_am_blockrand`, der Messfall `312/Kosten`), 312/M-24
  und 312/M-25 als Teilfälle in Abschnitt O; der Schalter
  `NAK312_GEGENPROBE_OHNE_BLOCKRAND` blendet allein im Gegenprobelauf die Fälle
  aus, die Testzugänge des Mechanismus lesen; Kopfkommentar nachgezogen.
- `tools/beweise.ps1`: die B7-Behauptung um genau die gemessenen
  Blockbindungsfälle ergänzt.
- Nicht angefasst: Bandwerte, `mono_bass_hz`, die Schalter und das
  Auto-Gain-Ziel (kein Schreibweg des Blockrands, Teil b, NAK-340),
  `hostEreignisOffen` als Verbraucher, der Transaktionskern als Wahrheit des
  bestätigten Zustands, `DspProgramm`, der Wrapper, Schemas, Fixtures,
  Identität.

### 21.6 Matrixzeilen des zweiten Änderungssatzes

Einzelläufe am Endstand 3b (Binaries jünger als jede Quelle): B7 222 geprüft,
0 Fehler; A16 131; B2 291; B3c 162; B6 389 geprüft, 0 Fehler (Goldens und
Neutralfälle unverändert trotz A-19); B31 8; B11 21; B12 4; B30 168; A1
`NULLTEST OK`. Gegenprobe am Basisstand (Commit 3a) mit der Endfassung der
Tests: `docs/beweise/roh/NAK-312-etappe3b-gegenprobe.txt` (B7 Exit 1, 220
geprüft, 6 Fehler: 312/M-20, M-21, M-22, M-23, M-27, M-29 — genau die
„heute rot"-Zeilen; grün 312/M-24, M-25, M-26, M-28, M-80, M-81 und der
Teilfall `nicht_endliche_hostwerte_am_blockrand`).

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-20 | `312/M-20 hostwerte_sind_blockgebunden`, B7; Teilfall `nicht_endliche_hostwerte_am_blockrand` | grün: in beiden Ständen je Parameter A/B 0; B/C 894 bis 1551, Mix im neutralen Stand bauartbedingt 0; Teilfall: Ausgang endlich, 0 abweichend, 12 Ziele | heute rot (Basisstand A/B 1022 bis 1742); Blockrandzugriff entfernt → rot (Teil a); Endlichkeitsregel entfernt → Teilfall rot (Teil b); `docs/beweise/roh/NAK-312-rot-M-20.txt` |
| M-21 | `312/M-21 offline_gleich_echtzeit_fuer_die_vier`, B7 | grün: offline A/B 0 in beiden Ständen, 17,7- bis 19,2-fach Echtzeit gemessen | heute rot; Mutation wie M-20 → rot; `docs/beweise/roh/NAK-312-rot-M-21.txt` |
| M-22 | `312/M-22 blockbindung_haelt_bei_jeder_blockgroesse`, B7 | grün: Blockgröße 1, 64, 185, 240, 256, 512 je 0 von 8 Läufen abweichend; Blockgröße 1: 32 Ziele für 32 Ereignisse | heute rot bei jeder Blockgröße (7 von 8); Mutation wie M-20 → rot; `docs/beweise/roh/NAK-312-rot-M-22.txt` |
| M-23 | `312/M-23 bewegte_automation_bitgleich_und_endwert_exakt`, B7 | grün: A/B 0, Endwert beider 2,2e-7 von 10^(12/20) | heute rot für den Weg (A/B 30 592), Endwert grün; Mutation wie M-20 → Weg rot; `docs/beweise/roh/NAK-312-rot-M-23.txt` |
| M-24 | `312/M-24 epoche_wechselt_genau_zweimal_auch_blockgebunden` (Teilfall von `epoche_wechselt_genau_zweimal_je_geste`), B7 | grün: Epoche 0 → 1 → 1 → 2 | Regressionswache; Blockzähler statt Samples → Ruhegrenze nie erreicht → rot (A-24); `docs/beweise/roh/NAK-312-rot-M-24.txt` |
| M-25 | `312/M-25 blockgebundene_automation_erzeugt_keine_revision` (Teilfall von `automation_erzeugt_keine_revision`), B7 | grün: Revision 1 → 1, Dirty 0, Statebytes gleich | Regressionswache; Blockrandwert als Transaktion → Revision 51, Dirty 50 → rot; `docs/beweise/roh/NAK-312-rot-M-25.txt` |
| M-26 | `312/M-26 blockbindung_ist_echtzeitfest`, B7 | grün: 4000 Blöcke, Sperren 0, Allokationen 0, eigener Zähler 0 | heute nicht messbar; bestätigter Wert unter gemeldeter Sperre → 16 000 Sperren → rot; `docs/beweise/roh/NAK-312-rot-M-26.txt` |
| M-27 | `312/M-27 wert_wirkt_im_block_seines_randes`, B7 | grün: A/B 0, Ende Block 9 1,0, Ende Block 10 15,849 | heute rot (Ende Block 10 1,0); Mailboxlesung hinter den Block → rot; `docs/beweise/roh/NAK-312-rot-M-27.txt` |
| M-28 | `312/M-28 ohne_ereignis_setzt_der_blockrand_kein_ziel`, B7 | grün: A/B 0, Übernahmen +0/+0, Ziele +0/+0 | Regressionswache; Ziel unbedingt → rot (Teil a); Blockrand tauscht `hostEreignisOffen` → 312/M-24 rot (Teil b, A-25); `docs/beweise/roh/NAK-312-rot-M-28.txt` |
| M-29 | `312/M-29 ladestart_und_blockbindung_greifen_ineinander`, B7 | grün: nach dem Laden 0 abweichend, am Ende 10^(6/20), geladen und Hash der Quelle | heute rot (am Ende 10^(3/20)); Quittung am Blockrand entfernt → 1150 abweichend → rot; `docs/beweise/roh/NAK-312-rot-M-29.txt` |
| M-79 | `312/M-79 genau_vier_parameter_sind_blockgebunden`, B7 | grün: die vier Namen, 4 Ziele nach 112 Ereignissen, 312/M-20 grün | heute nicht messbar; `width` entfernt → Menge und Bitgleichheit rot (Teil a); `mono_bass_hz` aufgenommen → Menge rot (Teil b); `docs/beweise/roh/NAK-312-rot-M-79.txt` |
| M-80 | `312/M-80 bandwert_bleibt_taktgebunden`, B7 | grün: gain_db nicht in der Tabelle; ohne Takt 4480 abweichend, mit Takt 0; Endwert −6 dB beide | Regressionswache in der Lesart R-312-15 (§21.9): `gain_db` aufgenommen → Mengenbehauptung rot, Verhalten unverändert; `docs/beweise/roh/NAK-312-rot-M-80.txt` |
| M-81 | `312/M-81 Mengenhaelfte …` und `312/M-81 Verhaltenshaelfte …`, B7 | grün: eq_enabled nicht in der Tabelle; ohne Takt 1187 abweichend, mit Takt 0, Endzustand an | zwei Hälften: `eq_enabled` aufgenommen → Mengenhälfte rot, Verhaltenshälfte grün (E-312-12); `docs/beweise/roh/NAK-312-rot-M-81.txt` |
| M-82 | `312/M-82 schiedsregel_ist_ueberlaufsicher`, B7 | grün: A/B 0, 3 Ziele, nach dem Überlauf wirksam, am Ende 10^(12/20) | heute nicht messbar; Ordnung statt Ungleichheit → 1 Ziel, am Ende +6 dB → rot; `docs/beweise/roh/NAK-312-rot-M-82.txt` |

### 21.7 Kosten der Blockbindung

Gemessen nach `docs/beweise/roh/NAK-312-etappe3-blockbindung-kosten.txt`
(Messfall `312/Kosten` in B7, am Stand 3a in der Gegenprobe und am Stand 3b):

- **Speicher:** `dsp::DspKern` 25 296 → 25 440 Byte (+144), `SondeProcessor`
  81 728 → 81 856 Byte (+128); dazu 96 Byte Stapel je Block. Der Bauplan nannte
  160 Byte (16 im Kern, 96 je Programm, 48 im Prozessor); gebaut sind 272 Byte,
  aufgeschlüsselt in der Rohdatei (Stand je Bank statt je Programm, gemerktes
  Blockrandziel, ein Atomic statt zwei für den bestätigten Wert, die Quittung
  des Ladestarts, gecachte Bereichsgrenzen).
- **Arbeit je Block:** ohne Ereignis vier `acquire`-Loads und vier Vergleiche;
  je Ereignis zwei weitere Loads, die Zellrechnung und ein Rampenziel. Kein
  `exchange`, keine Sperre, keine Allokation, kein Warten (312/M-26).
- **Zeit in `processBlock`** (Block 64, Minimum aus drei Durchgängen): ohne
  Ereignis 927 ns (Stand 3a) gegen 955 ns (Stand 3b); mit je einem Ereignis auf
  den vier Parametern 961 ns gegen 1039 bis 1098 ns (zwei Läufe). Die
  Blockbindung kostet damit rund 27 ns je Block ohne Ereignis und 50 bis 110 ns
  mit vier Ereignissen — höchstens rund 0,01 Prozent der Blockzeit von 1,33 ms.
  Einzelmessungen, keine Schranke.

### 21.8 Abweichungen im zweiten Änderungssatz, begründet

- **A-18 — ein Atomic statt zwei für den bestätigten Wert.** §7.3 sieht je
  Parameter `bestaetigtNormiert` (float) und `bestaetigtZahl` (double) vor. Zwei
  Atomics kann der Callback aus zwei verschiedenen Publikationen lesen
  (Vergleichswert des neuen, Wert des alten Stands). Gebaut ist ein
  `atomic<double>` je Parameter; der Vergleichswert entsteht am Blockrand aus
  ihm mit genau dem Ausdruck von `zelleAusHost`
  (`convertTo0to1 (hostWertAus (…))`, reine NormalisableRange-Arithmetik). Die
  Zusage (dieselbe Zelle einschließlich der Ausnahme, ohne Sperre) hält; M-26
  mutiert die Lesestelle dieses einen Atomics.
- **A-19 — Befund im Bau: die Neutralprüfung kannte keine Rampe ohne
  Übergang.** Der erste Lauf von 312/M-20 zeigte 126 abweichende Samples: A
  sprang im letzten Block einer Rampe (Block 16) auf 1,0, B rampte zu Ende. Die
  Neutralprüfung (`verarbeiteStueck`, NAK-311) wertet die Rampen am Stückende aus
  und begrenzt das Schreibende nur mit dem Programmübergang — das setzte voraus,
  dass Parameterrampen nur zusammen mit einem Übergang laufen (beide beginnen am
  selben Blockrand, `kRampeSamples` gleich `kFadeSamples`). Die Blockbindung setzt
  Rampenziele ohne Übergang; ohne Korrektur verlöre der letzte Rampenblock sein
  Schreiben, hörbar als Sprung. Das Schreibende liegt jetzt frühestens am Ende
  der längsten laufenden Rampe (`cRampenEnde`); mit Übergang ist das dasselbe
  Sample, B6 bleibt 389/389 einschließlich aller Neutral- und Schreibendefälle.
  Gemessen wird die Korrektur von 312/M-20 bis M-22 im neutralen Stand (Rotbeweis
  über dieselbe Mutation: ohne Blockrand fällt der Fall ebenso).
- **A-20 — die Quittung des Ladestarts erreicht den Blockrand.** §7.3 sagt: die
  Quittierung lesen „`dspKontrollTakt` und — nach dem zweiten Änderungssatz —
  der Blockrand". Die Quittierung selbst liegt unter dem Zustandsschloss; der
  Blockrand liest deshalb eine eigene Kopie `blockrandQuittung` (Vorgabe
  `kKeineQuittung`, ein 64-Bit-Wert, den kein 32-Bit-Zähler trifft). Ohne sie
  wirkte ein alter Mailboxwert, solange ein laufender Übergang die Übernahme des
  geladenen Programms aufschiebt — genau das misst 312/M-29 (Rotbeweis: 1150
  abweichende Samples).
- **A-21 — Speicherordnung.** §7.3 und M-26 sagen „relaxed". Damit ein Zähler
  und sein Hostwert zusammenpassen („der Zählerstand, aus dem ihr Wert stammt",
  E-312-6 Punkt 3), erhöht der Listener mit `release`, Blockrand und Takt lesen
  mit `acquire`; auf x64 dieselben Befehle wie `relaxed` (die Erhöhung ist ohnehin
  ein gesperrter Befehl). Kein Warten, keine Sperre.
- **A-22 — zwei Stände in 312/M-20 bis M-22.** Im neutralen Stand (eq an, keine
  Bänder) ist Mix bauartbedingt ohne Wirkung (nass gleich trocken). Die Fälle
  fahren deshalb zusätzlich einen Stand mit Band (Bell 1 kHz +6 dB); der neutrale
  Stand bleibt, weil nur er die Neutralprüfung mitmisst (A-19). Mix verlangt im
  neutralen Stand nur A gleich B.
- **A-23 — 312/M-80 mit belegtem Slot 5, 312/M-81 mit Band.** Die Matrix nennt
  für M-80 den freien Slot 5; auf einem freien Slot ändert `gain_db` den Klang
  nicht ([SONDE-015] M-63), die Verhaltenshälfte wäre leer. Der Stand belegt Slot
  5 (Bell 1 kHz 0 dB). 312/M-81 fährt mit Band, weil An und Aus im neutralen
  Stand gleich klingen.
- **A-24 — 312/M-24 fällt anders als vorhergesagt.** Die Matrix sagt für die
  Mutation „Ruhegrenze an den Blockzähler" „bei Blockgröße 1 zu früh". Der Fall
  fährt Blöcke zu 480 Samples (wie der bestehende); dort erreicht der Blockzähler
  die Ruhegrenze nie, die Epoche bleibt stehen (0 → 1 → 1 → 1). Rot an der
  Zusage (genau zwei Wechsel), in der anderen Richtung.
- **A-25 — die zweite Gegenprobe von 312/M-28 färbt nur 312/M-24.** Die Matrix
  nennt „M-24 und M-25 rot". Tauscht der Blockrand `hostEreignisOffen`, laufen
  Overlay und Epoche nicht (312/M-24 rot); eine Revision entsteht ohne Overlay
  ebenso wenig, 312/M-25 bleibt grün.
- **A-26 — 312/M-22: die FL-Blockgröße.** `eq-copilot/identity/host-capabilities-fl-v1.json:54`
  belegt Blöcke von 1 bis 4096 Samples und „~180-190 Samples je Block"; gefahren
  ist 185, 4096 trägt 312/M-27.
- **A-27 — 312/M-21: die zehnfache Geschwindigkeit ist gemessen, keine
  Schranke.** Der Lauf wartet zwischen den Blöcken nie; die gemessene
  Geschwindigkeit (17,7- bis 19,2-fach) steht im Detail, sie ist keine
  Bestehensbedingung, weil eine Zeitgrenze den Fall an die Rechnerlast hängte.
- **A-28 — Teilfall `nicht_endliche_hostwerte_am_blockrand` ohne eigene
  Matrixzeile.** Der Auftrag verlangt im Selbstaudit NaN und ±Inf an jedem
  Hostwert, der den Blockrand erreicht; der Teilfall misst es und ist einmal
  gebrochen (Rohdatei M-20, Teil b).
- **A-29 — 312/M-26 liest den Allokationszähler von B7 selbst.** Der Fall
  schaltet den eigenen `operator new`-Zähler nur um `processBlock` ein; die
  Hostereignisse davor laufen außerhalb des Bereichs der Echtzeitwache, wie im
  Wrapper (Listener vor `processBlock`).

### 21.9 Lesart R-312-15 zu 312/M-80

Gebaut und gefahren in der Lesart des Dirigenten (§17): die Mutation „`gain_db`
in die Abdeckungstabelle aufnehmen" fällt an der **eigenen Mengenbehauptung**
der Zeile („`v1.band.5.gain_db` steht nicht in der Abdeckungstabelle"). Der
Halbsatz „der Kern schriebe Bandwerte ohne die eigene Besitzregel, die NAK-340
erst baut" ist **Warnung, keine Stufe des Rotlaufs**: gemessen bleibt unter der
Mutation die Verhaltenshälfte unverändert (ohne Takt 4480 abweichend, mit Takt 0,
Endwert −6 dB), weil der Blockrand für einen Parameter ohne Rampenziel allein
seinen Stand schreibt. Die Verhaltenshälfte ist messend und trägt keine eigene
Mutation. Dieselbe Form gilt nach E-312-12 für 312/M-81.

### 21.10 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Etappe 3

| Abschnitt | Punkt | Etappe 3 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt; die Analysequeue läuft unter der Taktsperre voll und verwirft Analyse, nie Audio (bestehende Politik) |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: Ladestart quittiert vor dem Abgleich, Ladehaken zwischen beiden (312/M-78); `reset` bewahrt die Mailbox (312/M-13); `prepareToPlay` nach dem Laden publiziert den geladenen Stand (312/M-12); der Blockrand nimmt in Ruhe und Ausblenden kein Ziel |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | angewandt: NaN und ±Inf an Ladestart (312/M-18) und Blockrand (Teilfall zu 312/M-20); Zählerüberlauf der Quittierung (312/M-19) und der Schiedsregel (312/M-82); Blockgröße 1 und 4096; kein Draht-, Schema- oder Statefeld berührt |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel gegen die Quelle bei jedem Einzelbein, jedem Rotbeweis und jeder Gegenprobe (`LastWriteTime` auf jetzt); Testzugänge nur unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`; kein neues Bein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die B7-Behauptung nennt genau die gemessenen Fälle; die Kosten sind Messung ohne Schranke; A-16 nennt, was ungemessen bleibt |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerbehauptung B7, Kopfkommentar von B7, Kommentare an Ladestart, Abgleich, Takt, Blockrand, Neutralprüfung (DspKern.h Zusage 3), Klassenkopf der Sonde und der M-119-Kommentar (§1); Manifestkopf (Etappe, Basis-SHA, Kanon) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: 25 Matrixzeilen mit Rohdatei `docs/beweise/roh/NAK-312-rot-M-NN.txt`, dazu der Teilfall zu 312/M-20; zwei Gegenproben am Basisstand; Ersatz- und Abweichungsfälle begründet (A-12, A-24, A-25) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Werte aus Läufen, Uhrzeiten aus `Get-Date` und Binaryzeitstempeln, Größen per `sizeof` |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: speichern↔laden (Ladestart, Hash, Statebytes in 312/M-14, M-15, M-25, M-29), aktivieren↔abklingen (Epoche und Ruhegrenze 312/M-24, read-only und zurück 312/M-17), W02 und Blockbindung je ein Commit |

### 21.11 Selbstaudit

- **Zahlenränder.** Hostereigniszähler `uint32`: Quittierung, Takt und Blockrand
  vergleichen nur auf Gleichheit (312/M-19, 312/M-82, gemessen über den
  Überlauf). Die Blockrand-Quittung ist `uint64` mit der Vorgabe `~0`, die kein
  32-Bit-Stand trifft; ein Stand trifft eine alte Quittung erst nach genau 2^32
  Ereignissen auf demselben Parameter ohne neuen Ladestart (bei FL mit höchstens
  einem Punkt je Block und Block 64: über 60 Tage Dauerautomation) — dann wäre
  genau ein Ereignis wirkungslos; benannt, nicht gebaut. Die Ladegeneration ist
  keine neue Zahl, sondern die Quittierung der vorhandenen Zähler. Blockgröße 1
  in 312/M-22 (32 Ziele für 32 Ereignisse). NaN und ±Inf: jeder neue Vergleich
  (`zelleAmBlockrand`: Ausnahme vor Endlichkeit wie `zelleAusHost`; zweite
  Prüfung des Abgleichs nur mit endlichem Hostwert) und jeder Hostwert, der
  Ladestart oder Blockrand erreicht (312/M-18, Teilfall zu 312/M-20).
- **Echtzeit.** Der Blockrand liest Atomics (`acquire` für Zähler und Quittung,
  `relaxed` für Hostwert und bestätigten Wert), rechnet NormalisableRange-
  Arithmetik und `std::pow` wie `baueProgramm`, schreibt nur audiothread-eigenen
  Zustand und einen relaxed-Zähler; `hostEreignisOffen` behält den Worker als
  einzigen Verbraucher. Gemessen: 312/M-26 0 Sperren, 0 Allokationen über 4000
  Blöcke; B7-Fälle 312/M-01, M-02, M-04 und 311/M-139 grün.
- **Stale Closures.** Die Testlambdas fangen Prozessoren per Referenz innerhalb
  ihres Blocks; der Ladehaken wird nach 312/M-78 geleert. Im Produkt fängt
  `neuerGestus` `this` und den Zeiger auf den lokalen Zählerstand, beide leben
  über den Aufruf.
- **Save/Load-Symmetrie.** Kein Statefeld berührt; `getStateInformation` liefert
  read-only die Fixture bytegleich (312/M-15), der Hash eines Ladestarts in
  dieselbe Instanz bleibt der der Quelle (312/M-14, M-29), eine blockgebundene
  Fahrt lässt die Statebytes bytegleich (312/M-25). B2 291/291.
- **Beziehungen.** aktivieren↔abklingen: Epoche genau zweimal je Geste auch
  blockgebunden (312/M-24); read-only und zurück (312/M-17); Ruhepfad nimmt kein
  Ziel, Einschalten aus der Ruhe folgt der Schiedsregel. speichern↔laden wie
  oben. starten↔stoppen: `reset` bewahrt die Mailbox (312/M-13).
- **Behauptungszeile nicht weiter als der Test.** Die B7-Behauptung nennt je
  Satz einen gemessenen Fall; die zweite Prüfung aus R-312-11 und der Rücksprung
  des Reglers im Host (A-16, A-17) stehen nicht darin.
- **Kommentare (R-312-14).** Berichtigt im jeweiligen Änderungssatz: der
  M-119-Kommentar im Konstruktor (§1, „Die Sonde rampt blockweise"), die Zusage 3
  und der Neutralprüfungskommentar im Kern, der Klassenkopf der Sonde
  (`processBlock` liest jetzt die Mailbox), die Dokumentation von `ladestart`,
  `wirksam`, `dspKontrollTakt`, `hostParameterAbgleichen` und `verarbeite`.
  `git grep -n "Die Sonde rampt blockweise"` ist leer. Der Kommentar an
  `zustandSchloss` („nie processBlock") bleibt wahr: der Blockrand nimmt es nicht.
- **Rotbeweise.** 25 Matrixzeilen, 25 Rohdateien, jede mit SHA-256 vor und nach
  der Mutation (bytegleich), Zeitstempelprüfung und grünem Lauf danach; zwei
  Gegenproben, beide bytegleich zurückgenommen. `git status --short` nach dem
  letzten Rotbeweis: nur die Dateien der Etappe und die zwei fremden Ordner.

---

## Kanon-Lauf - NAK-312 Etappe 3

**Lauf:** 2026-09-21 07:21 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-ad18d98-dirty.md](roh/NAK-312-ad18d98-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 07:21:14 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | ad18d984 NAK-312 Etappe 3b: Blockbindung der Hostwerte - Trims, Width und Mix am Blockrand (T3-01-05 Teil a) |
| Commit (voll) | ad18d984c80a4bf0987537ef40fa218c69627f33 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,10 s | [A1](roh/NAK-312-ad18d98-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,72 s | [A2](roh/NAK-312-ad18d98-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,76 s | [A3](roh/NAK-312-ad18d98-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.138,42 s | [A4](roh/NAK-312-ad18d98-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,38 s | [A4-SI](roh/NAK-312-ad18d98-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,54 s | [A4b](roh/NAK-312-ad18d98-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,98 s | [A5](roh/NAK-312-ad18d98-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,16 s | [A6](roh/NAK-312-ad18d98-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,15 s | [A7](roh/NAK-312-ad18d98-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,38 s | [A8](roh/NAK-312-ad18d98-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,39 s | [A9](roh/NAK-312-ad18d98-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,92 s | [A10](roh/NAK-312-ad18d98-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,28 s | [A11](roh/NAK-312-ad18d98-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,34 s | [A12](roh/NAK-312-ad18d98-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,24 s | [A13](roh/NAK-312-ad18d98-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 9,70 s | [A14](roh/NAK-312-ad18d98-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,30 s | [A17](roh/NAK-312-ad18d98-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,91 s | [A18](roh/NAK-312-ad18d98-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,15 s | [A19](roh/NAK-312-ad18d98-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,34 s | [A20](roh/NAK-312-ad18d98-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,35 s | [A21](roh/NAK-312-ad18d98-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,10 s | [A22](roh/NAK-312-ad18d98-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,78 s | [A24](roh/NAK-312-ad18d98-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,17 s | [A34](roh/NAK-312-ad18d98-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 33,42 s | [A35](roh/NAK-312-ad18d98-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 6,15 s | [A36](roh/NAK-312-ad18d98-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,16 s | [A28](roh/NAK-312-ad18d98-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,16 s | [A29](roh/NAK-312-ad18d98-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,17 s | [A30](roh/NAK-312-ad18d98-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 97,15 s | [A31](roh/NAK-312-ad18d98-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,27 s | [A25](roh/NAK-312-ad18d98-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,15 s | [A26](roh/NAK-312-ad18d98-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,15 s | [A27](roh/NAK-312-ad18d98-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,64 s | [A23](roh/NAK-312-ad18d98-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,66 s | [A16](roh/NAK-312-ad18d98-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,28 s | [B1](roh/NAK-312-ad18d98-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,29 s | [B2](roh/NAK-312-ad18d98-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,06 s | [B3](roh/NAK-312-ad18d98-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,08 s | [B3b](roh/NAK-312-ad18d98-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,64 s | [B3c](roh/NAK-312-ad18d98-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,43 s | [B4](roh/NAK-312-ad18d98-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-312-ad18d98-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,63 s | [B5](roh/NAK-312-ad18d98-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,32 s | [B6](roh/NAK-312-ad18d98-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, nimmt jeder Wechsel nach offline genau eine gemeldete Sperre, ohne Wechsel keine. Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist fuer diese vier bitgleich zu einem mit Takt nach jedem Block - im neutralen Stand und mit Band, offline wie in Echtzeit, bei Blockgroesse 1, 64, 185, 240, 256, 512 und 4096 (dort wirkt der Wert im Block seines Randes), bei einer Rampe ueber 200 Bloecke und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 2,33 s | [B7](roh/NAK-312-ad18d98-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,24 s | [B10](roh/NAK-312-ad18d98-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,01 s | [B11](roh/NAK-312-ad18d98-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,63 s | [B12](roh/NAK-312-ad18d98-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 0,14 s | [B31](roh/NAK-312-ad18d98-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,12 s | [B13](roh/NAK-312-ad18d98-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,41 s | [B14](roh/NAK-312-ad18d98-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,29 s | [B15](roh/NAK-312-ad18d98-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,06 s | [B20](roh/NAK-312-ad18d98-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,29 s | [B22](roh/NAK-312-ad18d98-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,68 s | [B23](roh/NAK-312-ad18d98-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,03 s | [B24](roh/NAK-312-ad18d98-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,16 s | [B25](roh/NAK-312-ad18d98-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,49 s | [B26](roh/NAK-312-ad18d98-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-312-ad18d98-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,91 s | [B19](roh/NAK-312-ad18d98-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,33 s | [B17](roh/NAK-312-ad18d98-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 5,72 s | [B18](roh/NAK-312-ad18d98-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,74 s | [B16](roh/NAK-312-ad18d98-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,43 s | [B27](roh/NAK-312-ad18d98-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,87 s | [B29](roh/NAK-312-ad18d98-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,61 s | [B30](roh/NAK-312-ad18d98-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,15 s | [B28](roh/NAK-312-ad18d98-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,28 s | [B8](roh/NAK-312-ad18d98-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 31,47 s | [A32](roh/NAK-312-ad18d98-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 111,01 s | [A33](roh/NAK-312-ad18d98-dirty.md#a33) |


### 21.12 Kanon nachher (Bauer, 21.09.2026)

Voller Kanon abgekoppelt nach dem Auftrag, Befehl `pwsh -NoProfile -File
tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312
Etappe 3'`, auf dem Commit `ad18d984c80a4bf0987537ef40fa218c69627f33`; Start
07:21:13 (`Get-Date`), Ende 07:59:43 (Zeitstempel der Logdatei, letzte Zeile
`EXIT=0`). Kopf und Übersicht stehen im Runnerblock „Kanon-Lauf - NAK-312
Etappe 3" unmittelbar über diesem Abschnitt, die Rohausgabe unter
`docs/beweise/roh/NAK-312-ad18d98-dirty.md`. Kopfzeile des Runners, wörtlich:

```
**Lauf:** 2026-09-21 07:21 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0
```

- **GRÜN 70/70**, Exitcode 0; stillgelegt bleibt A15. Die Zahl ist die des
  Kanons der Etappe 2 (§18.7); Etappe 3 bringt kein neues Bein. Die Beine der
  Etappe sind grün: B7 (222 geprüft, 0 Fehler), A1, A16, B2, B3c, B6, B11,
  B12, B30 und B31.
- **A32** endet mit [HINWEIS] Exit 4, nicht blockierend, mit denselben
  Zählwerten wie der Kanon der Etappe 2 (`docs/beweise/roh/NAK-312-8943246-dirty.md`):
  Quelldateien über 2000 Zeilen 2 von höchstens 0, über dem Ziel von 1500
  Zeilen 10, Funktionen über 200 Zeilen 34 von höchstens 28,
  Kommentar-Bezeichner ohne Code 36 von höchstens 30. Die Etappe erhöht keinen
  dieser Werte. **A33** [OK] Exit 0, clang-tidy-Fundstellen 58 von höchstens 58.
- **„dirty"** im Namen der Rohausgabe: die Kopfzeile Arbeitsbaum meldet zwei
  unbestätigte Dateien — die fremden untracked Ordner `briefing-hub/` und
  `nimbalyst-local/`; `git status --short` zeigte nach dem Lauf sonst nur die
  Schreibziele des Runners (dieses Manifest und die Rohausgabe).
- **Lauf ohne Eingriff in Quellen, ein Stillstand erklärt.** Nach „Baue:" stand
  das Log ohne aktiven Compiler; allein `vctip.exe` (PID 22280) lebte als
  Nachkomme des Bauschritts. Um 07:24:47 beendet (NAK-300); danach lief der
  Runner weiter. Das längste Bein war A4 (`cargo test` des Brokers, 1 138,42 s).
- **Installer-Hashes.** Der Runner pinnt den Release-Broker ins
  Installer-Manifest; `eq-copilot/install/nakama-installer-v1.json` blieb
  unverändert (`git status --short`). Die Bundlebytes haben sich mit dieser
  Etappe geändert; vor einer Installation (Laufzeit-Arm) ist nach der Regel in
  `CLAUDE.md` `--hashen` fällig — nicht Ticketpfad dieser Etappe, Hinweis an
  den Dirigenten.

## 22. Messung der Etappe 3, Laufzeit-Arm, Auftrag Erstprüfung (Dirigent, 21.09.2026, 08:12 Uhr)

**Messung.** Worker `9592aeef` (Opus, Effort xhigh, `dontAsk`), 06:02 bis
08:04 Uhr (Commitzeit des dritten Commits), Basis
`cb473944a12410e17f3205e231b32f35f92805ac`. Ergebnis: drei Commits,
`c94b8dd9` (06:44 Uhr, Etappe 3a, W02 Ladestart), `ad18d984` (07:20 Uhr, Etappe
3b, Blockbindung) und `dc2aaf1c` (08:04 Uhr, Kanon nachher, Manifestkopf) =
`origin/master`; Baum sauber (nur die zwei fremden untracked Ordner), Basis ist
Vorfahr von HEAD. Produkt- und Testdiff
`git diff --stat cb473944..dc2aaf1c -- eq-copilot broker tools`: acht Dateien,
+1650/-60, alle in den Ticketpfaden der Etappe 3 (§7.0). Rundenbilanz
`cb473944..dc2aaf1c`: Produkt 6 Dateien +525/-58, Tests 1 Datei +1124/-1,
Prüfwerkzeug 1 Datei +1/-1, Doku 30 Dateien +13772/-4. Messabdeckung: M-10 bis
M-29 und M-78 bis M-82 tragen je Test, Ergebnis und Rotbeweis-Rohdatei (§21.3,
§21.6); beide Gegenproben am Basisstand färben genau die „heute rot"-Zeilen.
Kanon nachher vom Dirigenten am Log gelesen: `GRUEN - 70/70`, `EXIT=0`
(07:59 Uhr), keine rote Zeile. Vom Dirigenten für die Erstprüfung
hervorgehoben: die Abweichung A-19 (Schreibende der Neutralprüfung in
`eq-copilot/plugin/dsp/DspKern.cpp` frühestens am Ende der längsten laufenden
Rampe) berührt die Nulltestkante und ist eigene Prüffrage; der Hinweis A-17
(Rücksprung des Reglers im read-only-Stand aus dem Workerthread, Wirkung in FL
ungemessen) steht im Register NAK-346.

**Laufzeit-Arm** (08:05 bis 08:11 Uhr, Stand `dc2aaf1c`):
`LAUFZEIT NAK-312 dc2aaf1c VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`,
Exit 3 allein wegen Karte U43 (Referenzprojekt
`Nakama-Diagnose-Verarbeitung.flp` fehlt, K-286-1), kein Szenario verfehlt im
Sinn von Exit 4. **Nulltest im Host am Auslieferungszustand BITIDENTISCH**
(v = 0, g = 1,0, 0 Abweichungen); Vergleich `ohne_slots` GLEICH. Rohdaten
`docs/beweise/roh/NAK-312-laufzeit-dc2aaf1c.md`; die nachgezogenen Hashes in
`eq-copilot/install/nakama-installer-v1.json` gehören zu diesem Commit.

**Auftrag Erstprüfung Etappe 3.** Frischer, lesender Opus-Thread, Effort max
(Codex-Woche 94 %, Kopfzeile Prüfmodell), Vorlage A; Auftrag
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-urteil.md` (Zielpfad, noch
nicht angelegt). Prüfbereich: `git diff cb473944...<Commit dieses Abschnitts>`
über die acht Produkt-, Test- und Werkzeugpfade der Etappe 3; `docs/**` ist
ausgeschlossen.

## 23. Erstprüfung der Etappe 3, Validierung, Einordnung und Regeln (Dirigent, 21.09.2026, 09:28 Uhr)

### 23.1 Erstprüfung Etappe 3: Rohurteil PASS

Frischer, lesender Opus-Thread `a347e0fa`, Effort max, `dontAsk`, Prüfstufe T2,
Vorlage A, 08:12 bis vor 08:47 Uhr, HEAD
`6dc9d708ffe48d134ab0281c80db113d2e3d37cd` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Prüfbereich `git diff cb473944...1a91d0d2` über die acht
Pfade der Etappe 3; geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-urteil.md`. Rohurteil PASS: 0
Defekte, zwei Lücken (L-1, L-2), zwei Härtungen (H-1, H-2). Wegen der Tragweite
von L-1 hat der Dirigent trotz PASS an der Quelle validieren lassen.

### 23.2 Validierung an der Quelle

Frischer, lesender Opus-Thread `eb952162`, Effort max, `dontAsk`, 08:49 bis vor
09:26 Uhr, HEAD `e79922328c53ff4dc582d927de172b8bcbbd0f2a` vor und nach dem Lauf
gleich, nur der Bericht
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-validierung.md` geschrieben
(Auftrag
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-validierung-auftrag.txt`):
**1 bestätigt, 3 präzisiert, 0 widerlegt.** L-1 ist Schritt für Schritt am Code
nachgerechnet (Spitze +13,71 dB bei Fadesample 124; Fenster 256 Samples, bei
einem beim Laden laufenden Übergang bis 4352 Samples bei Blockgröße 4096) und
vom Validierer zum DEFEKT hochgestuft.

### 23.3 Einordnung

| Befund | Urteil der Validierung | Einordnung des Dirigenten | Folge |
|---|---|---|---|
| L-1 | PRÄZISIERT, umgestuft | **DEFEKT** — neu durch Etappe 3b, gegen R-312-10 zweiter Teil („bei `nurLesen` wirkt kein Hostparameter auf Audio"), [SONDE-015] M-92 („keine Bindung") und den Gatepunkt T3-05-02; die Matrix hatte keine Zeile dafür | E-312-13, neue Matrixzeile M-87, Nacharbeit 1 der Etappe 3 |
| L-2 | PRÄZISIERT | **LÜCKE → R-312-17**; Bestand aus NAK-311, nicht durch dieses Ticket verursacht | R-312-17, Register NAK-347 zum Folgeticket NAK-340, keine Nacharbeit |
| H-1 | BESTÄTIGT | **HÄRTUNG** in der Sache; der Satz in `tools/beweise.ps1` reicht aber weiter als die Messung und widerspricht §21.10 („die B7-Behauptung nennt genau die gemessenen Fälle"). Weil die Nacharbeit 1 dieselbe Behauptungszeile um M-87 ergänzt, wird der Wortlaut dort berichtigt | in die Nacharbeit 1 (nur Wortlaut) |
| H-2 | PRÄZISIERT | gegen **R-312-14**: der Kommentar stammt aus Etappe 3a und ist in zwei von drei Zeitpunkten falsch | in die Nacharbeit 1 (nur Kommentar) |

Dazu aus der Validierung, gegen R-312-14: drei Kommentare (`DspKern.h` zum
Ausblenden mit den Gains der ausblendenden Bank, `DspKern.cpp` an der
ENDE-Übernahme und der neue Kommentar an der Blockrandübernahme) sind seit 3b im
ersten Block einer ENDE-Blende falsch, in dem der Blockrand einen Hostwert nimmt.
Im schreibbaren Stand ist dieses Verhalten kein Defekt (die Automation ist der
Wunsch des Users; M-11 und M-78: ein Gestus nach dem Recall wirkt); die
Kommentare werden berichtigt. Und gegen die Matrixregel „eine Regressionswache
wird einmal absichtlich gebrochen": die Korrektur A-19 an der Nulltestkante hat
keinen eigenen Rotlauf.

### 23.4 Entscheide und Regeln

- **E-312-13 (L-1).** Gebaut wird Weg A der Validierung: ein
  `std::atomic<bool>` für den Blockrand neben `blockrandQuittung`, das der
  Ladestart bei jedem erfolgreichen Laden in beide Richtungen aus
  `transaktion->nurLesen()` speichert — **vor** dem Tausch von
  `hostEreignisOffen` und vor der Zählerlesung der Quittierung, mit `seq_cst`.
  `processBlock` liest es im Ereignisfall **nach** dem Zähler und übergibt bei
  gesetztem Flag keinen Wert (`neu` ohne `wert`; der Blockrandstand zieht
  nach, kein Ziel). `DspKern` bleibt im Verhalten unberührt. Kein Schloss,
  keine Allokation, kein Warten. Weg B (Marker in der Quittung) wird verworfen:
  er lässt ein Fenster im Speicherpuffer, bis die Speicherung ohnehin vor den
  Tausch wandert. Variante C (kein Ziel in keiner ENDE-Blende) wird verworfen:
  sie änderte das Verhalten im schreibbaren Stand, das keine Zusage verbietet,
  und greift in die Neutralprüfung aus NAK-311 ein.
- **R-312-16 (L-1, schließende Regel).** Nach einem Ladestart mit `nurLesen`
  setzt der Blockrand ab dem ersten Block aus keinem Hostwert ein Rampenziel —
  auch nicht für das Ausblenden des vorigen Standes und nicht während eines
  beim Laden laufenden Übergangs —, bis ein schreibbarer Stand geladen ist;
  danach wirkt Automation am Blockrand wieder.
- **Matrixzeile M-87 (neu, Etappe 3, R-312-16).** Zustand: zwei Instanzen A und
  C, je eq an und Output-Trim +3 dB committet und eingeschwungen, Taktsperre.
  Ereignis: in beiden `setStateInformation` mit dem Fixture des fremden
  Majors; nur in A danach Hostwerte auf die vier blockgebundenen Parameter
  (Input-Trim +24 dB, Output-Trim +24 dB, Width 2, Mix 0); dann dieselben
  Blöcke mit fester Saat über mindestens 512 Samples. Zusage: **A ist ab dem
  ersten Sample des ersten Blocks nach dem Laden bitgleich zu C (0 abweichende
  Samples), und die Zahl der Blockrandziele von A steigt um 0.** Teilfall (i):
  beim Laden läuft ein Übergang (vorher ein Commit Output-Trim +6 dB und genau
  ein Block zu 64), danach Blöcke zu 185 und zu 512 — dieselbe Zusage.
  Teilfall (ii), Rückweg: fremdes Major, dann eigener schreibbarer Stand mit
  eq an, unter der Taktsperre **ohne** Kontrolltakt Output-Trim +6 dB — A ist
  bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang bei
  10^(6/20). Test: `312/M-87 read_only_ladestart_nimmt_kein_blockrandziel` mit
  seinen Teilfällen, B7 `EqCopTransactionTest`. Rotbeweis: **heute rot**
  (Gegenprobe am Stand vor der Nacharbeit: 256 Fadesamples je Kanal weichen ab,
  Spitze rund +13,7 dB, Ziele +4); nach dem Bau: Flaglesung in `processBlock`
  entfernt → Hauptfall und (i) rot; Flag nur gesetzt, nie gelöscht → (ii) rot.
  Quelle: R-312-16, R-312-10 zweiter Teil, [SONDE-015] M-92, Gate T3-05-02.
- **R-312-17 (L-2).** Die Gleichheitszusage aus E-312-5 („dieselbe Ausgabe
  offline und in Echtzeit") gilt in diesem Ticket für **endliche
  Eingangssamples**; für nicht endliche und subnormale Eingangssamples
  verriegelt und zählt der Kern in beiden Läufen (NaN-Ehrlichkeit hält), welche
  Blöcke er schreibt, hängt aber weiter an Übergängen und Neutralmerkmalen aus
  dem Takt. Das ist Bestand aus NAK-311 und wird im Folgeticket NAK-340
  geschlossen (Register NAK-347); eine Änderung an der Neutralprüfung und an
  `DspProgramm.*` liegt außerhalb der Ticketpfade der Etappe 3 und bräuchte
  eigene Matrixzeilen und Nulltests.
- **R-312-18 (A-19).** Eine Änderung an der Nulltestkante trägt ihren eigenen
  Rotlauf: die Rücknahme von `cRampenEnde` im Schreibende macht nach der
  Rechnung der Validierung 312/M-20, M-21, M-22, M-27 und M-79 rot; die
  Nacharbeit 1 fährt ihn und legt ihn als Rohdatei ab.

### 23.5 Auftrag Nacharbeit 1 der Etappe 3

Frischer Opus-Worker, Effort xhigh, `dontAsk`; Auftrag
`docs/beweise/roh/NAK-312-etappe-3-nacharbeit-1-auftrag.txt`. Nur betroffene
Beine; der volle Kanon läuft wieder am Ende der nächsten Etappe. Rundenbudget
der Etappe 3: Runde 1 von 3. Danach Wiederprüfung nur über den Fixdiff
(Vorlage B).

## 24. Etappe 3 — Nacharbeit 1 (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-3-nacharbeit-1-auftrag.txt`;
Spezifikation §23.3 und §23.4 (E-312-13, R-312-16, Matrixzeile M-87, R-312-17,
R-312-18), §5.3, §6 Kopf, §6.2 (M-15 bis M-17), §7.1, §14.4 (R-312-14) und §21;
Befunde und Nachmessung aus
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-urteil.md` und
`docs/beweise/roh/NAK-312-etappe-3-erstpruefung-validierung.md`. Uhrzeiten aus
`Get-Date`, Rohdateien oder Binaryzeitstempeln. Zeilenangaben gelten zum
Endstand dieser Nacharbeit. Fremde Matrixzeilen tragen ihr Präfix (R-312-13).
Die Matrixzeile M-87 steht verbindlich in §23.4; §6 ist nicht nachgezogen
(Auftrag).

### 24.1 Start und Basis-SHA

- Start 21.09.2026, 09:30:31 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `529b824200e49db95a3ea24560b270f7b710c1a0` (Basis-SHA
  der Nacharbeit 1); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`.
- Vor dem ersten Bau lief kein zweiter Bau- oder Testprozess: keine Prozesse
  `cmake`, `MSBuild`, `cl` oder `link`; die laufenden `pwsh`-Prozesse sind
  Dirigent, Plan-Tab und Cockpit (NAK-290).
- Kein voller Kanon in dieser Nacharbeit (Auftrag); die betroffenen Beine
  einzeln in §24.7.

### 24.2 Gegenprobe am Stand vor dem Fix (heute rot)

Rohdatei `docs/beweise/roh/NAK-312-etappe3-nacharbeit1-gegenprobe.txt`, Lauf
09:42:45. Produktcode unverändert auf HEAD `529b8242` (SHA-256 je Produktdatei
in der Rohdatei, jede „gleich HEAD"); allein die Testdatei trägt 312/M-87, in
derselben Fassung wie am Endstand (SHA-256
`2047FA9792B46E6A9CABEACB8C0684B9F8ABF18BC708F32B95D20FCA9F97045A`, am Endstand
nachgemessen). Binary 09:42:38 jünger als die Quelle 09:41:51. B7 Exit 1, 225
geprüft, 2 Fehler — genau der Hauptfall und Teilfall (i) von 312/M-87:

- Hauptfall: A gegen C links 256, rechts 256 abweichend, Samples 0 bis 255 nach
  dem Laden, danach bitgleich; die Blockrandziele von A steigen um 4. Die
  Messung ohne Bedingung mit Output-Trim allein ergibt als größtes
  Ausgang/Eingang +13,71 dB gegen +3,00 dB ohne Hostwert — die gerechnete
  Spitze der Validierung (+13,71 dB bei Fadesample 124) ist damit gemessen.
- Teilfall (i): bei Blöcken zu 185 weichen 626, bei 512 768 Samples je Kanal ab
  (Samples 0 bis 625 und 0 bis 767), je Ziele +4 — die Fensterlängen der
  Validierung (⌈192/B⌉·B + 256).
- Teilfall (ii) ist am Basisstand grün: ohne Flag gibt es keinen Rückweg, der
  hängen könnte. Er wacht über den Rückweg und fällt unter Teil b des
  Rotbeweises (§24.4).
- Alle übrigen 223 Prüfungen von B7 sind grün.

Der Befund L-1 ist damit bestätigt, nicht widerlegt; gebaut wurde erst danach.

### 24.3 Gebaut — E-312-13 (Weg A der Validierung)

- `eq-copilot/plugin/sonde/SondeProcessor.h:690`:
  `std::atomic<bool> blockrandNurLesen { false };` neben `blockrandQuittung`
  (`:681`), mit Kommentar. Der Klassenkopf nennt die Lesung im Audiothread
  (`:42-43`) und die read-only-Ausnahme der Blockbindung (`:56-59`).
- `eq-copilot/plugin/sonde/SondeProcessor.cpp:1209`, `setStateInformation`:
  `blockrandNurLesen.store (transaktion->nurLesen(), std::memory_order_seq_cst);`
  — unter dem Zustandsschloss, nach beiden Ladestartzweigen (`:1188-1197`),
  **vor** dem Tausch von `hostEreignisOffen` (`:1222`) und vor der Zählerlesung
  der Quittierung (`:1225`). Gespeichert wird das Flag des Kerns: `nurLesen()`
  wechselt nur mit einem erfolgreichen `ladestart`
  (`eq-copilot/plugin/state/NakamaTransaktion.cpp:272`), also wechselt
  `blockrandNurLesen` nur bei erfolgreichem Laden, in beide Richtungen.
- `SondeProcessor.cpp:509` und `:524`, `processBlock`: der Ereigniszähler wird
  mit `seq_cst` gelesen (bisher `acquire`); im Ereignisfall liest der Blockrand
  **nach** dem Zähler und nach der Quittungsprüfung (`:517`) das Flag und
  übergibt bei gesetztem Flag `neu` ohne `wert`. Der Kern setzt dann nur
  `blockStand` und `blockZielGueltig = false`
  (`eq-copilot/plugin/dsp/DspKern.cpp:673-676`) — der Blockrandstand zieht
  nach, kein Ziel. Das gilt auch im ersten Block einer ENDE-Blende und während
  eines beim Laden laufenden Übergangs, weil die Mailboxlesung vor `blockrand`
  steht (`DspKern.cpp:1159-1165`).
- `DspKern` bleibt im Verhalten unberührt (N-1: nur Kommentare). Im Audiothread
  kein Schloss, keine Allokation, kein Warten: je Ereignisplatz eine
  Atomic-Lesung mehr.
- **Speicherordnung, begründet.** Im Ladestart ist die Speicherung S des Flags
  vor der Zählerlesung R der Quittierung sequenziert. Ein Hostwert nach der
  Quittierung erhöht den Zähler über den gelesenen Stand q (Listener
  `fetch_add` mit `release`, `SondeProcessor.cpp:1505`). Liest der Blockrand
  diesen neueren Stand (Lesung Z) und danach das Flag (Lesung F), dann ist R
  kohärenzgeordnet vor Z (R liest q, Z einen späteren Wert derselben
  Änderungsordnung). Sind R und Z `seq_cst`, steht R in der Gesamtordnung vor
  Z; mit S vor R und Z vor F folgt S vor F, und F liest das gespeicherte Flag
  oder ein späteres ([atomics.order] Absatz 3 und 4). Der Listener darf
  `release` bleiben. Mit `acquire` an Z und F hielte das nur auf x64 (TSO),
  nicht im Speichermodell — deshalb `seq_cst` an allen vier Zugriffen, nicht nur
  an der Speicherung (A-30). Kosten auf dem Zielsystem, nachgelesen in der
  installierten MSVC-STL 14.44.35207 (`include/atomic`, `include/xatomic.h`):
  die Speicherung ist `_InterlockedExchange8`, ein gesperrter Befehl im
  Ladestart unter dem Zustandsschloss; eine `seq_cst`-Lesung ist auf x64 eine
  gewöhnliche Lesung mit Compilerbarriere (`_Compiler_or_memory_barrier` =
  `_Compiler_barrier`) — im Audiothread dieselben Befehle wie mit `acquire`.

### 24.4 Matrixzeile M-87

Aufbau: A und C über `blockPruefling` auf dem Heap (NAK-175), eq an und
Output-Trim +3 dB committet und eingeschwungen. Jeder Lauf steht ganz unter
EINER Taktsperre ohne Kontrolltakt (das Schloss ist rekursiv; `fahreFolge`
läuft darin), mit fester Saat; in (i) liegen Commit +6 dB und der eine Block zu
64 in derselben Taktsperre. (ii) lädt vor Block 0 erst das fremde Major, dann
einen eigenen Stand mit eq an und schreibt vor Block 10 Output-Trim +6 dB, 24
Blöcke zu 64; der Referenzlauf bekommt nach jedem Block einen Kontrolltakt.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-87 | `312/M-87 read_only_ladestart_nimmt_kein_blockrandziel` mit den Teilfällen `312/M-87 Teilfall (i) uebergang_beim_laden` und `312/M-87 Teilfall (ii) rueckweg_ohne_kontrolltakt`, B7 (`eq-copilot/plugin/tests/TransactionTestMain.cpp:4749-4902`) | Hauptfall grün: Blöcke zu 256, A gegen C links 0 und rechts 0 abweichend (1024 Samples je Kanal), Ziele +0, A und C read-only; die Messung mit Output-Trim allein ergibt +3,00 dB wie C. (i) grün: Blöcke zu 185 und zu 512, Übergang beim Laden lief, je 0 und 0 abweichend, Ziele +0. (ii) grün: zwischendurch read-only, danach schreibbar, A/B 0 abweichend, am Ende 1,99526225 = 10^(6/20) | heute rot (§24.2). Teil a: Flaglesung in `processBlock` entfernt (`SondeProcessor.cpp:524`) → Hauptfall und (i) rot mit den Zahlen der Gegenprobe, (ii) grün. Teil b: Flag nur gesetzt, nie gelöscht (`:1209`) → allein (ii) rot (A/B 1664, am Ende 1,0), Hauptfall und (i) grün. Beide Male keine weitere Prüfung mitgefallen, Rücknahme bytegleich (SHA-256 `12C93624…EFFA` vorher und nachher); `docs/beweise/roh/NAK-312-rot-M-87.txt` |

### 24.5 Nebenpunkte N-1 bis N-4

- **N-1 (R-312-14)** — berichtigt, nur Kommentare:
  `eq-copilot/plugin/dsp/DspKern.h:67-74` (Ausblenden mit den Gains der
  ausblendenden Bank), `eq-copilot/plugin/dsp/DspKern.cpp:550-555`
  (ENDE-Übernahme) und `:681-690` (Blockrandübernahme; „ein ruhender oder
  ausblendender Pfad nimmt kein Ziel" gilt jetzt ausdrücklich für den Pfad,
  dessen Ausblenden in einem früheren Block begonnen hat). Jede Stelle sagt: im
  schreibbaren Stand kann der Blockrand im Block, in dem eine ENDE-Blende
  beginnt, noch ein Ziel setzen, das die ausblendende Bank formt; im
  read-only-Stand setzt er keines (R-312-16). Dazu weitere Kommentarstellen
  (A-31). `git diff` über `DspKern.*` enthält außer Kommentarzeilen nur
  `DspKern.cpp:676`, deren Code (`continue;`) gleich bleibt; geändert ist dort
  allein der Zeilenkommentar.
- **N-2 (R-312-14, H-2)** — `eq-copilot/plugin/sonde/SondeProcessor.cpp:1654-1663`:
  der Kommentar der zweiten Prüfung nennt je Zeitpunkt, was gilt — trifft ein
  nicht endlicher Hostwert vor der Prüfung der ersten Schleife oder nach ihrem
  Schreiben ein, zeigt der Regler den Hostwert und der Klang den bestätigten
  Wert; nur zwischen Prüfung und Schreiben bleibt der geschriebene bestätigte
  Wert. Nur Kommentar.
- **N-3 (H-1, Prüfliste E)** — `tools/beweise.ps1:728`, nur der Wortlaut der
  B7-Behauptung: „fuer alle vier in beiden Staenden (neutral und mit Band) bei
  Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline;
  fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (…), ueber eine
  Rampe von 200 Bloecken und ueber den Zaehlerueberlauf". Dazu ein Satz zu
  312/M-87, der nur nennt, was die drei Fälle messen: Bitgleichheit ab dem
  ersten Sample nach dem Laden bei Blöcken zu 256 und bei laufendem Übergang zu
  185 und 512, Blockrandziele +0, der Rückweg ohne Kontrolltakt bitgleich zum
  Lauf mit Takt und am Ende bei 10^(6/20). Die Datei parst fehlerfrei
  (PowerShell-Parser, 0 Fehler); Zeilenenden CRLF wie zuvor.
- **N-4 (R-312-18)** — Rotlauf der Korrektur A-19, Rohdatei
  `docs/beweise/roh/NAK-312-rot-A-19.txt`, 09:47:52 bis 09:48:23: `cRampenEnde`
  im Schreibende der Neutralprüfung zurückgenommen
  (`eq-copilot/plugin/dsp/DspKern.cpp:1448`; die Zeile wird wortgleich die des
  Stands 3a, `git show c94b8dd9`, Zeile 1336). B7 Exit 1, 225 geprüft,
  5 Fehler — genau die fünf der Rechnung der Validierung: 312/M-20 (im
  neutralen Stand je 126 abweichende Werte für Input-Trim, Output-Trim und
  Width, deckungsgleich mit A-19; Mix im neutralen Stand und der Stand mit Band
  A/B 0), 312/M-21 (dieselben 126 offline), 312/M-22 (Blockgrößen 64 bis 512
  mit 3 bis 4 von 8 Läufen abweichend; Blockgröße 1 bleibt 0 von 8, wie
  gerechnet), 312/M-27 (A/B 1020) und 312/M-79 (über seine Klausel aus
  312/M-20). Grün unter derselben Mutation: 312/M-23, M-26, M-28, M-29, M-80,
  M-81, M-82, der Teilfall `nicht_endliche_hostwerte_am_blockrand`, 312/M-87
  mit beiden Teilfällen und das mitgefahrene B6 mit 389 von 389. SHA-256
  vorher und nach der Rücknahme gleich
  (`E7A53DDC311A0C0F25A0018E12BA15BD78AB50A5A7CC621767373D59AD36BC42`);
  Neubau danach B7 225/0, B6 389/0.

### 24.6 Abweichungen, begründet

- **A-30 — `seq_cst` an drei Lesungen zusätzlich zur Speicherung.** E-312-13
  nennt `seq_cst` an der Speicherung. Im Speichermodell ist die Zusage erst mit
  `seq_cst` an der Speicherung, an der Zählerlesung der Quittierung, an der
  Zählerlesung am Blockrand und an der Flaglesung gedeckt (§24.3; Validierung
  Teil 2 Punkt 1). Auf x64 sind die drei Lesungen dieselben Befehle wie vorher.
- **A-31 — weitere Kommentarstellen (R-312-14).** Über die drei aus N-1 hinaus:
  die vierte Stelle derselben Aussage (`DspKern.cpp:1136-1142`, „Ausgeblendet
  hat die Bank mit IHREN Gains"); zwei Stellen, die dieser Änderungssatz selbst
  falsch gemacht hätte — die Doku von `BlockrandHostwert` (`DspKern.h:241-247`,
  „`wert`, wenn das Ereignis ein Hostgestus ist") und der Zeilenkommentar
  `DspKern.cpp:676` („vom Ladestart quittiert"); in der Sonde der Klassenkopf
  (`SondeProcessor.h:42-43`, `:56-59`), der Kommentar der Blockrandlesung
  (`SondeProcessor.cpp:500-501`) und der des Listeners (`:1500-1503`, „mit
  `acquire` liest" wird „mit `acquire` oder stärker"). Alle nur Kommentar.
- **A-32 — eine Messung ohne Bedingung im Hauptfall.** Die Zeile M-87 nennt als
  Gegenprobe „Spitze rund +13,7 dB". Mit vier Parametern (Width, Mix) ist
  Ausgang/Eingang kein reiner Faktor; eine dritte Instanz mit Output-Trim
  +24 dB allein misst die Spitze. Sie ist keine Bestehensbedingung.
- **A-33 — Aufbauprüfungen im Fall.** Jeder Fall prüft zusätzlich seinen
  Aufbau: Fixture gelesen, A und C read-only; in (i) beide Commits angenommen
  und beim Laden ein Übergang gelaufen (`gefahreneSlots`: Quellbank ≥ 0); in
  (ii) zwischendurch read-only, danach schreibbar. So wird die Zusage nicht an
  einem leeren Aufbau grün.
- **A-34 — M-87 hinter dem Gegenprobeschalter der Etappe 3b.** Der Fall steht
  in Abschnitt Y wie 312/M-79 und M-82 unter
  `#if ! defined (NAK312_GEGENPROBE_OHNE_BLOCKRAND)`, weil er den Zielzähler des
  Mechanismus liest. Die Gegenprobe dieser Nacharbeit brauchte den Schalter
  nicht: der Basisstand hat den Mechanismus.
- **A-35 — Rotbeweis M-87 zweimal gefahren.** Ein erster Lauf (Binaries
  09:46:03 bis 09:46:41) lief am Stand vor einer letzten Wortkorrektur im
  Kommentar der Flaglesung (eine Zeile weniger); die Rohdatei trägt den zweiten
  Lauf, 10:00:08 bis 10:00:44, am Endstand (Zeilen 524 und 1209, SHA-256 der
  Endfassung), mit demselben Ergebnis.
- **A-36 — Matrixzeile M-27 unverändert.** Die schließende Regel zu H-1 nennt
  „Behauptung und Matrixzeile M-27"; N-3 und die Ticketgrenze umfassen nur die
  Behauptungszeile, §6 ist nicht Auftrag. M-27 sagt weiter „für die vier
  abgedeckten Parameter", gemessen wird Output-Trim im neutralen Stand — offen
  für den Dirigenten.

### 24.7 Einzelbeine

Bau aller betroffenen Ziele in einem `cmake`-Aufruf aus pwsh, 09:56:21 bis
09:57:50, `BAU EXIT=0`; vorher `LastWriteTime` aller geänderten Quellen auf
jetzt (NAK-230). Die Liste stammt aus `eq-copilot/plugin/CMakeLists.txt`: jedes
Ziel, das `sonde/SondeProcessor.cpp` übersetzt (`NakamaProbeeq_VST3`,
`EqCopProbeeqNullTest`, `EqCopSonde012HostChannelContextTest`,
`EqCopSonde012LoudnessSourceTest`, `EqCopSondeLebenslaufTest`,
`EqCopBriefkastenTest`, `EqCopSessionSoak`, `EqCopTransactionTest`), die
übrigen Sonde012-Ziele und die Kernbeine A1, B6 und B2. Lauf 09:57:57 bis
09:59:31, jedes Binary jünger als die jüngste Quelle (09:56:21):

| Bein | Ziel | Ergebnis |
|---|---|---|
| B7 | `EqCopTransactionTest` | Exit 0, 225 geprüft, 0 Fehler (vorher 222; neu die drei Prüfungen von 312/M-87) |
| A16 | `EqCopProbeeqNullTest` | Exit 0, 131 Prüfungen ok, 0 Fehler |
| A1 | `EqCopNullTest` | Exit 0, `NULLTEST OK` |
| B6 | `EqCopDspGoldenTest` | Exit 0, 389 geprüft, 0 Fehler; Goldens unberührt |
| B2 | `EqCopStateMigrationTest` | Exit 0, 291 Prüfungen ok, 0 Fehler |
| B31 | `EqCopSondeLebenslaufTest` | Exit 0, 8 geprüft, 0 Fehler |
| B11 | `EqCopSonde012HostChannelContextTest` | Exit 0, 21 bestanden, 0 fehlgeschlagen |
| B12 | `EqCopSonde012LoudnessSourceTest` | Exit 0, 4 bestanden, 0 fehlgeschlagen |
| B13 | `EqCopSonde012SourcesModelTest` | Exit 0, 88/88 grün |
| B14 | `EqCopSonde012ProjectReloadTest` | Exit 0, 231/231 grün |
| B30 | `EqCopBriefkastenTest` | Exit 0, 168 bestanden, 0 fehlgeschlagen |
| Bundle | `NakamaProbeeq_VST3` | gebaut, `Nakama Probeeq.vst3` 09:57:50; nicht installiert (Laufzeit-Arm beim Dirigenten) |

Gebaut, nicht einzeln gefahren: `EqCopSonde012SourcesLatencyTest` (läuft nur
in A23 über den Python-Runner) und `EqCopSessionSoak` (A24). Zusätzlich
gemessen: die clang-tidy-Ratsche `py -3.13 tools/plan/tidy.py` (Ende 09:55:16,
109 s): „Grenze gehalten - 58 Fundstellen von hoechstens 58", keine Fundstelle
in den geänderten Dateien — gemessen vor der letzten Wortkorrektur im
Kommentar der Flaglesung. Nach den Rotläufen um 10:00 ist B7 erneut gebaut und
grün (225/0, Rohdatei M-87); die Quellbytes sind die des Beinlaufs (Rücknahme
bytegleich). Nicht gefahren: voller Kanon und Laufzeit-Arm (Auftrag).

### 24.8 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für die Nacharbeit 1

| Abschnitt | Punkt | Nacharbeit 1 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: Flag vor der Quittierung, Lesung am Blockrand nach dem Zähler; laden in beide Richtungen (read-only und zurück, 312/M-87 (ii), 312/M-17); `reset` und `prepareToPlay` lassen das Flag, es gehört zum geladenen Stand |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | angewandt: kein Draht-, Schema- oder Statefeld; das Flag vergleicht keinen Zähler (312/M-19, M-82 grün); die Messfunktion lässt Eingang 0 und NaN aus |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel gegen die Quelle bei Gegenprobe, jedem Rotlauf und jedem Bein; kein neuer Testzugang (312/M-87 nutzt Taktsperre, `blockrandZiele` und `gefahreneSlots`) |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: B7-Behauptung je Bedingung genau das Gemessene (N-3), der Satz zu 312/M-87 nur was die drei Fälle messen; offen: Matrixzeile M-27 (A-36) |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerbehauptung B7, Kopfkommentar von B7, Manifestkopf (Etappe) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: 312/M-87 Hauptfall und (i) (Teil a), (ii) (Teil b), Gegenprobe am Stand vor dem Fix; A-19 mit eigenem Rotlauf (N-4) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Werte aus Läufen, Uhrzeiten aus `Get-Date`, Rohdateien und Binaryzeitstempeln |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: read-only laden und zurück zum schreibbaren Stand samt Test und Rotbeweisen in einem Commit |

### 24.9 Selbstaudit

- **Speicherordnung gegen A-15 und A-21.** A-15 bleibt: der Tausch von
  `hostEreignisOffen` (Standard `seq_cst`) ordnet sich weiter vor die
  Zählerlesungen der Quittierung, die neue Speicherung steht davor. A-21 bleibt:
  eine `seq_cst`-Lesung ist auch eine acquire-Lesung, die Paarung mit dem
  `release` des Listeners gilt am Blockrand weiter; der Takt liest unverändert
  `acquire`. Nicht geschlossen und wie in §23.2 benannt bleibt das Restfenster
  der Validierung: ein ganzer Ladestart zwischen Zählerlesung und
  `uebernehmeBereiten` desselben Blocks, betroffen sind Werte von vor dem Laden;
  es verlangt einen angehaltenen Audiothread.
- **Rückweg M-17 und Teilfall (ii).** 312/M-17 bleibt unter beiden Mutationen
  grün, weil sein Wert über den Kontrolltakt kommt, nicht über den Blockrand;
  deshalb trägt (ii) den Rückweg ohne Kontrolltakt, und Teil b färbt allein
  (ii).
- **Zählerüberlauf.** Das Flag vergleicht keinen Zähler; Quittung,
  Blockrandstand und Takt vergleichen weiter nur auf Gleichheit. Im
  read-only-Stand zieht `blockStand` nach (`neu` ohne `wert`), sodass nach dem
  Rückweg kein alter Stand als neu gilt; 312/M-19 und M-82 grün.
- **Blockgröße 1.** Mit read-only nicht eigens gefahren: der Weg ist
  blockgrößenunabhängig (eine Lesung je Ereignisplatz vor `verarbeite`,
  `SondeProcessor.cpp:502-536`), die Fälle decken 185, 256 und 512 ab, und
  312/M-22 bindet bei Blockgröße 1 im schreibbaren Stand weiter 32 Ziele für
  32 Ereignisse.
- **Fehlgeschlagener Ladestart.** Das Flag wechselt nur bei erfolgreichem
  Laden: bei `LadeErgebnis::ignoriert` kehrt `setStateInformation` vor dem
  Schloss zurück (`:1166-1167`), das Flag bleibt; scheitert der eigene
  Ladestart, lädt der neutrale Zweig mit `zustand.nurLesen` (für einen eigenen
  Stand falsch), und das Flag folgt dem Kern; schlüge auch der neutrale fehl
  (`jassert`, nach B2 ausgeschlossen), blieben `nurLesen()` und damit das Flag
  beim vorigen Stand.
- **Save/Load-Symmetrie.** Das Flag ist Laufzeit, kein Statefeld;
  `getStateInformation` ist unberührt, der read-only-Stand liefert die Fixture
  weiter bytegleich (312/M-15 grün), B2 291/291.
- **Behauptungszeile nicht weiter als der Test.** Jede Bedingung der
  B7-Behauptung hat ihren Fall (N-3); der Satz zu 312/M-87 nennt die drei
  gefahrenen Fälle, ihre Blockgrößen und ihre Maße, sonst nichts.
- **Audiothread.** Neu im Callback ist allein die Lesung eines lockfreien
  `std::atomic<bool>`; 312/M-26 (4000 Blöcke, 0 Sperren, 0 Allokationen) und
  A16 131/131 grün.
- **Stale Closures.** Die Lambdas des Falls fangen `fremd`, `plusDrei` und
  `eigen` per Referenz innerhalb ihres Blocks; die Merker von (ii) gehen als
  Zeiger auf Lokale, die den Lauf überleben.
- **Git.** `git status --short` nach dem letzten Rotlauf: die sechs geänderten
  Ticketdateien, die drei neuen Rohdateien und die zwei fremden Ordner.

## 25. Messung der Nacharbeit 1 der Etappe 3, Laufzeit-Arm, Auftrag Wiederprüfung 1 (Dirigent, 21.09.2026, 10:14 Uhr)

**Messung.** Worker `4e1e2c6e` (Opus, Effort xhigh, `dontAsk`), 09:30 bis 10:05
Uhr (Commitzeit), Basis `529b824200e49db95a3ea24560b270f7b710c1a0`. Ergebnis: ein
Commit `32813efd6d5683e9eaa01b140199805f9bdb0d02` = `origin/master`, Baum sauber
(nur die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD.
Rundenbilanz `529b8242..32813efd`: Produkt 4 Dateien +76/-20, Tests 1 Datei
+212/-1, Prüfwerkzeug 1 Datei +1/-1, Doku 4 Dateien +619/-1. Vom Dirigenten am
Diff gemessen: `eq-copilot/plugin/dsp/DspKern.cpp` und `DspKern.h` ändern nur
Kommentare (keine Codezeile im Diff außerhalb von Kommentaren); in
`eq-copilot/plugin/sonde/SondeProcessor.cpp` steht die Speicherung von
`blockrandNurLesen` (`seq_cst`) vor dem Tausch von `hostEreignisOffen` und vor
der Zählerlesung der Quittierung, die Flaglesung in `processBlock` nach dem
Zähler. Gegenprobe am Stand vor dem Fix vom Dirigenten gelesen
(`docs/beweise/roh/NAK-312-etappe3-nacharbeit1-gegenprobe.txt`, Lauf 09:42 Uhr):
B7 Exit 1, 225 geprüft, 2 Fehler — 312/M-87 Hauptfall (alle 256 Fadesamples
beider Kanäle weichen ab) und Teilfall (i) rot, Teilfall (ii) grün. **L-1 ist
damit gemessen bestätigt.**

**Laufzeit-Arm** (10:06 bis 10:13 Uhr, Stand `32813efd`):
`LAUFZEIT NAK-312 32813efd VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`,
Exit 3 allein wegen Karte U43, kein Szenario verfehlt im Sinn von Exit 4.
**Nulltest im Host am Auslieferungszustand BITIDENTISCH** (v = 0, g = 1,0,
0 Abweichungen); Vergleich `ohne_slots` GLEICH. Rohdaten
`docs/beweise/roh/NAK-312-laufzeit-32813efd.md`; die nachgezogenen Hashes in
`eq-copilot/install/nakama-installer-v1.json` gehören zu diesem Commit.

**Auftrag Wiederprüfung 1 der Etappe 3.** Frischer, lesender Opus-Thread, Effort
max (Codex-Woche 94 %), Vorlage B; Auftrag
`docs/beweise/roh/NAK-312-etappe-3-wiederpruefung-1-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-etappe-3-wiederpruefung-1-urteil.md` (Zielpfad, noch
nicht angelegt). Prüfbereich schrumpft: nur
`git diff 529b8242...<Commit dieses Abschnitts>` über die sechs Produkt-, Test-
und Werkzeugpfade der Runde, die Befundliste aus §23.3 und das Gate.

## 26. Wiederprüfung 1 der Etappe 3: PASS, Abschluss der Etappe 3, Übergabe (Dirigent, 21.09.2026, 10:31 Uhr)

**Wiederprüfung 1 der Etappe 3: PASS.** Frischer, lesender Opus-Thread
`35638327`, Effort max, `dontAsk`, Vorlage B, 10:14 bis vor 10:30 Uhr, HEAD
`d1b206830a97ee7a14c74087483df023bdf459cc` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Prüfbereich `git diff 529b8242...bda63629` über die sechs
Pfade der Runde; geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-etappe-3-wiederpruefung-1-urteil.md`. L-1 und N-1 bis
N-4 geschlossen, 0 offene Befunde, 0 Brüche. **Etappe 3 ist damit
abgeschlossen**: Erstprüfung Rohurteil PASS (§23.1), ein durch Validierung
hochgestufter Defekt (L-1), eine Nacharbeitsrunde, Wiederprüfung PASS.

**Rundenbilanz der Etappe 3, kumuliert**
(`py -3.13 tools/dirigent/rundenbilanz.py --runden cb473944 dc2aaf1c 32813efd`,
gemessen): Bau `cb473944..dc2aaf1c` Produkt 6 Dateien +525/-58, Tests 1 Datei
+1124/-1, Prüfwerkzeug 1 Datei +1/-1; Runde 1 `dc2aaf1c..32813efd` Produkt 5
Dateien +79/-23 (darin die Installer-Hashzeilen des Laufzeit-Arms), Tests 1
Datei +212/-1, Prüfwerkzeug 1 Datei +1/-1; Werkzeugurteil: kein
Konvergenz-Signal. Rundenbudget der Etappe 3: eine von drei Runden verbraucht.

**Stand der Etappen.** 0 bis 3 abgeschlossen (Etappe 1: Matrix PASS nach zwei
Matrixnacharbeiten; Etappe 2: Erstprüfung PASS ohne Runde; Etappe 3:
Wiederprüfung PASS nach einer Runde). Offen: Etappe 4 (Besitz), 5
(Offline-Übergang, Parameteranbindung), 6 (Gen-Fläche und Hörmarkierung), 7
(Produktabhängiges nach den Antworten auf U48, U49, U51, U56 mit eigenem
Matrixnachtrag), Abschlussprüfung T2 über Basis...HEAD mit vollem Kanon und
Laufzeit-Arm.

**Übergabe an die frische Dirigentensession.** Die Session `1a45e086` endet im
sauberen Fenster wegen des Kontextstands (51 % gemessen aus dem
Telemetrie-Cache, Skill §5: ab 500k keine neue Arbeitsphase): kein Worker, kein
Kanon, kein Beobachter, kein Cron-Job, Baum sauber, alles gepusht. Die nächste
Session:

1. startet den Bauer der Etappe 4 als frischen Opus-Worker, Effort xhigh
   (User-Entscheid 21.09.2026, Karte U55: bleibt unbefristet), `dontAsk`,
   Workername mit der Endung `-bau`, Aufsicht ENG, mit dem Auftrag
   `docs/beweise/roh/NAK-312-etappe-4-auftrag.txt` (liegt; der Bauer fährt den
   Kanon vorher selbst, weil die Nacharbeit 1 nur Einzelbeine fuhr; der Zusatz
   aus Register NAK-345 Punkt 2 ist darin entschieden);
2. misst danach wie in §19 und §22 (Diff, Rundenbilanz, Messabdeckung,
   Laufzeit-Arm mit `-Basis <Basis der Etappe>`) und lässt die Erstprüfung
   über Vorlage A von einem frischen, lesenden Prüfer fahren — ab dem
   Codex-Wochenreset (22.09.2026, 22:19 Uhr) wieder `gpt-6-astra` max, bis
   dahin Opus max; Rohurteile mit Tragweite lässt sie wie in §23 an der Quelle
   validieren, auch bei PASS;
3. gibt dem Bauer der Etappe 5 mit: die B7-Behauptung zum Offlineflag auf
   „Aufruf" festlegen (Register NAK-345 Punkt 1);
4. hat fünf offene Karten beim User (U11, U13, U26, U50, U52); U55 ist seit
   21.09.2026, 09:05 Uhr beantwortet.

Arbeitsmuster dieser Session, das sich bewährt hat: Prüfaufträge mit
Platzhalter für den Ziel-SHA vorbereiten, während der Laufzeit-Arm läuft; der
Ziel-SHA ist der Commit der Messung, der Auftrag liegt als eigener Commit
darüber; Wiederprüfung und Validierung schreiben genau eine Berichtsdatei, der
Dirigent committet erst nach ihrem Ende.

---

## Kanon-Lauf - NAK-312 Etappe 4 vorher

**Lauf:** 2026-09-21 10:36 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-a80f060-dirty.md](roh/NAK-312-a80f060-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 10:36:26 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | a80f0603 Planstand gerechnet |
| Commit (voll) | a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,06 s | [A1](roh/NAK-312-a80f060-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 10,00 s | [A2](roh/NAK-312-a80f060-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,72 s | [A3](roh/NAK-312-a80f060-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.123,25 s | [A4](roh/NAK-312-a80f060-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,56 s | [A4-SI](roh/NAK-312-a80f060-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,75 s | [A4b](roh/NAK-312-a80f060-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 4,06 s | [A5](roh/NAK-312-a80f060-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-312-a80f060-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-312-a80f060-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,37 s | [A8](roh/NAK-312-a80f060-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,36 s | [A9](roh/NAK-312-a80f060-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,96 s | [A10](roh/NAK-312-a80f060-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,42 s | [A11](roh/NAK-312-a80f060-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A12](roh/NAK-312-a80f060-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,23 s | [A13](roh/NAK-312-a80f060-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 9,11 s | [A14](roh/NAK-312-a80f060-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,44 s | [A17](roh/NAK-312-a80f060-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 36,99 s | [A18](roh/NAK-312-a80f060-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-312-a80f060-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,38 s | [A20](roh/NAK-312-a80f060-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,36 s | [A21](roh/NAK-312-a80f060-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,57 s | [A22](roh/NAK-312-a80f060-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,61 s | [A24](roh/NAK-312-a80f060-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,16 s | [A34](roh/NAK-312-a80f060-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 33,42 s | [A35](roh/NAK-312-a80f060-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 6,32 s | [A36](roh/NAK-312-a80f060-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A28](roh/NAK-312-a80f060-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,14 s | [A29](roh/NAK-312-a80f060-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,14 s | [A30](roh/NAK-312-a80f060-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 97,43 s | [A31](roh/NAK-312-a80f060-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,15 s | [A25](roh/NAK-312-a80f060-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,14 s | [A26](roh/NAK-312-a80f060-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-312-a80f060-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,65 s | [A23](roh/NAK-312-a80f060-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,65 s | [A16](roh/NAK-312-a80f060-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,13 s | [B1](roh/NAK-312-a80f060-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,29 s | [B2](roh/NAK-312-a80f060-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,05 s | [B3](roh/NAK-312-a80f060-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,09 s | [B3b](roh/NAK-312-a80f060-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,50 s | [B3c](roh/NAK-312-a80f060-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,45 s | [B4](roh/NAK-312-a80f060-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-312-a80f060-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,63 s | [B5](roh/NAK-312-a80f060-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,34 s | [B6](roh/NAK-312-a80f060-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, nimmt jeder Wechsel nach offline genau eine gemeldete Sperre, ohne Wechsel keine. Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist bitgleich zu einem mit Takt nach jedem Block - fuer alle vier in beiden Staenden (neutral und mit Band) bei Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline; fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (dort wirkt der Wert im Block seines Randes), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. Seit der Nacharbeit 1 der Etappe 3 (L-1, R-312-16) nimmt der Blockrand nach einem read-only-Ladestart keinen Hostwert: laedt ein Stand mit eq an und Output-Trim +3 dB das fremde Major und kommen danach Input-Trim +24 dB, Output-Trim +24 dB, Width 2 und Mix 0 als Hostwerte, ist der Ausgang ab dem ersten Sample nach dem Laden bitgleich zu dem ohne diese Hostwerte, und die Blockrandziele steigen um 0 - bei Bloecken zu 256 und, wenn beim Laden ein Uebergang laeuft, bei 185 und 512; ein eigener schreibbarer Stand danach laesst Output-Trim +6 dB ohne Kontrolltakt wieder am Blockrand wirken (bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang am Ende bei 10^(6/20)). | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 2,29 s | [B7](roh/NAK-312-a80f060-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,04 s | [B10](roh/NAK-312-a80f060-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,04 s | [B11](roh/NAK-312-a80f060-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,77 s | [B12](roh/NAK-312-a80f060-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 0,13 s | [B31](roh/NAK-312-a80f060-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,08 s | [B13](roh/NAK-312-a80f060-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,37 s | [B14](roh/NAK-312-a80f060-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,35 s | [B15](roh/NAK-312-a80f060-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-312-a80f060-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,23 s | [B22](roh/NAK-312-a80f060-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,70 s | [B23](roh/NAK-312-a80f060-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,19 s | [B24](roh/NAK-312-a80f060-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-312-a80f060-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-312-a80f060-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,20 s | [B21](roh/NAK-312-a80f060-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,90 s | [B19](roh/NAK-312-a80f060-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,33 s | [B17](roh/NAK-312-a80f060-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 5,98 s | [B18](roh/NAK-312-a80f060-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,74 s | [B16](roh/NAK-312-a80f060-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,54 s | [B27](roh/NAK-312-a80f060-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,87 s | [B29](roh/NAK-312-a80f060-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,74 s | [B30](roh/NAK-312-a80f060-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,15 s | [B28](roh/NAK-312-a80f060-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,29 s | [B8](roh/NAK-312-a80f060-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 35,62 s | [A32](roh/NAK-312-a80f060-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 118,65 s | [A33](roh/NAK-312-a80f060-dirty.md#a33) |


## 27. Etappe 4 — Besitz (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-4-auftrag.txt`; Spezifikation §5.5,
§5.6, §6 Kopf, §6.4 (M-30 bis M-39), §7.0, §7.1, §7.4, E-312-10 und R-312-12
(§11.3), E-312-11, R-312-13 und R-312-14 (§14.4), §18 (A-2), dazu der Zusatz
aus dem Register NAK-345 Punkt 2. Uhrzeiten aus `Get-Date`, Logdateien oder
Binaryzeitstempeln. Zeilenangaben gelten zum Endstand dieser Etappe, soweit
nicht „am Basis-SHA" dabeisteht. Fremde Matrixzeilen tragen ihr Präfix
(R-312-13).

### 27.1 Start, Basis-SHA, Kanon vorher

- Start 21.09.2026, 10:35:47 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c` (Basis-SHA
  der Etappe 4); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`.
- Der Bauplan gilt zum Stand `945ebfb1`. `git diff --stat 945ebfb1 a80f0603`
  zeigt für die Pfade der Etappe vier Dateien (`SondeProcessor.cpp`,
  `SondeProcessor.h`, `SondeLebenslaufTestMain.cpp`, `TransactionTestMain.cpp`);
  `eq-copilot/plugin/src/PluginEditor.cpp` und `ShotTestMain.cpp` sind seitdem
  unverändert, dort gelten die Zeilen des Bauplans. Am Basis-SHA nachgelesen:
  die drei Provider-Lambdas bei `eq-copilot/plugin/sonde/SondeProcessor.cpp:179-182`
  (Bauplan `:139-142`), der Destruktor bei `:270-288` mit den zwei `stop()` bei
  `:286-287` (Bauplan `:212-230`, `:228-229`), `controlV3` und `telemetryV3` bei
  `eq-copilot/plugin/sonde/SondeProcessor.h:650-651` (Bauplan `:523-524`),
  `transaktion` bei `:664` (Bauplan `:537`), `transaktion->revision()` im
  Statusprovider bei `SondeProcessor.cpp:1299` (Bauplan `:1143`), der Reconnect
  am Ende des Ladens bei `:1260-1261` (Bauplan `:1104-1105`).
- **Kanon vorher: selbst gefahren**, weil die Nacharbeit 1 der Etappe 3
  (`32813efd`) Produktcode geändert und nur Einzelbeine gefahren hat
  (`git diff --stat ad18d984 a80f0603 -- eq-copilot broker tools`: sieben
  Dateien, darunter `DspKern.*` und `SondeProcessor.*`). Befehl nach dem
  Auftrag, abgekoppelt, Titel `NAK-312 Etappe 4 vorher`, auf `a80f0603` vor
  jeder Quelländerung; Start 10:36:26, Ende 11:24:06 (Zeitstempel der
  Logdatei, letzte Zeile `EXIT=0`). **GRÜN 70/70**, Exitcode 0, stillgelegt
  bleibt A15. Kopf und Übersicht stehen im Runnerblock „Kanon-Lauf - NAK-312
  Etappe 4 vorher" unmittelbar über diesem Abschnitt, die Rohausgabe unter
  `docs/beweise/roh/NAK-312-a80f060-dirty.md` („dirty" sind allein die zwei
  fremden untracked Ordner). A32 [HINWEIS] Exit 4, nicht blockierend:
  Quelldateien über 2000 Zeilen 2, über dem Ziel 1500 Zeilen 11, Funktionen
  über 200 Zeilen 34, Kommentar-Bezeichner ohne Code 39 — gegen den Kanon
  der Etappe 3 (2, 10, 34, 36) stammt der Anstieg aus der Nacharbeit 1, nicht
  aus dieser Etappe. A33 [OK], 58 Fundstellen von höchstens 58. Ein Stillstand
  erklärt: nach „Baue:" stand das Log ohne Compiler, allein `vctip.exe`
  (Start 10:37:14) lebte; um 10:49:11 beendet (NAK-300), danach lief der
  Runner weiter. Das längste Bein war A4 (1 123,25 s). Vor dem Start lief kein
  Bau- oder Testprozess (NAK-290).

### 27.2 Gebaut

- `eq-copilot/plugin/sonde/SondeProcessor.h`: das Mitglied
  `std::shared_ptr<nakama::ipc::CallbackSchleuse> callbackSchleuse`, deklariert
  **vor** `controlV3` und `telemetryV3` (`:700`, die Clients bei `:706-707`);
  der Klassenkopf nennt den Besitz (`:61-67`). Testzugänge unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`: `callbackSchleuseFuerTest()` (als
  `shared_ptr`, Muster Gen), `ProviderHakenFuerTest` mit
  `setzeProviderHakenFuerTest` (Schranke vor und hinter `betreten()`, Marke vor
  dem ersten Besitzerzugriff; die Haken liegen in einem eigenen `shared_ptr`
  `providerHaken`, ebenfalls vor `controlV3`) und
  `setzeAbbauEndeHakenFuerTest` (am Ende des Destruktorrumpfs).
- `eq-copilot/plugin/sonde/SondeProcessor.cpp`: die drei Provider (Hello,
  Status, Telemetrie-Hello) fangen `this` und die Schleuse und rufen den
  Prozessor nur innerhalb eines Zugs, wörtlich in der Form von Gen
  (`eq-copilot/plugin/src/PluginProcessor.cpp:188-235`); ein abgewiesener Zug
  liefert das leere Default-Objekt (`:179-228`). `~SondeProcessor` schließt die
  Schleuse **nach** `telemetryV3.stop()` und `controlV3.stop()` und vor jeder
  Mitgliederzerstörung (`:333-343`), im Testbau danach der Abbauende-Haken
  (`:344-347`). Im Produktbau gibt es weder Haken noch Haltepunkte; der
  Produktzweig mit `controlV3.start()` ist unverändert.
- `eq-copilot/plugin/src/PluginEditor.cpp`: `MesspunktPanel` hält statt
  `EqCopilotProcessor& proz` den `juce::Component::SafePointer<EqCopilotEditor>`
  (`:643-647`), den derselbe Übergabepunkt auch dem Änderungs-Callback gibt
  (`:773-782`). `uebernehmen()` prüft ihn als Erstes und kehrt ohne Zugriff
  zurück, wenn der Editor weg ist (`:730-732`); den Prozessor erreicht es nur
  über `ed->processor` (`:742`). Der irreführende Kommentar am Übergabepunkt
  (§1, `:731-733` am Basis-SHA) ist berichtigt (`:766-772`). Testzugang unter
  demselben Define: die Marke `testzugang::messpunktMarkeFuerTest()`
  (`:611-624`), gefragt unmittelbar vor dem Prozessorzugriff (`:738-741`).
- `eq-copilot/plugin/tests/SondeLebenslaufTestMain.cpp` (B31): der
  Kanarienvogel, der gehaltene Abbau `gehaltenerAbbau` mit Schranke, Marke,
  Abbauende-Haken und Freigabe an beobachtbaren Zuständen; die Fälle
  312/M-30, M-31, M-32, M-33, M-34, M-39 und 312/NAK-345; ein optionales
  Argument wählt Fälle nach ihrer Kennung (A-40); Kopfkommentar nachgezogen.
- `eq-copilot/plugin/tests/ShotTestMain.cpp` (B15): das Messpunkt-Panel am
  echten Editor (312/M-35 bis M-37) mit der Nachrichtenschleife nach dem
  Muster von B30 und dem Bild `nak312-messpunkt-panel.png` im Ausgabeordner
  des Beins; `--sonde012-suite` fährt es nach dem Sichtsatz.
- `eq-copilot/plugin/tests/TransactionTestMain.cpp` (B7): Abschnitt Z,
  312/M-38 gegen den geteilten Testserver; Kopfkommentar nachgezogen.
- `tools/beweise.ps1`: die Behauptungen von B7, B15 und B31 um genau die
  gemessenen Fälle ergänzt (BOM und CRLF erhalten, drei Zeilen).
- Nicht angefasst: `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h`
  (E-312-10), Gens Schleuse, die Stoppfristen, der Briefkasten, das
  Quellenmodell, `eq-copilot/plugin/dsp/`, Schemas, Fixtures, Broker,
  Installer, Identität, `design/`, `docs/plugin-wissen.md`, die Dateien der
  Etappen 5 und 6. `eq-copilot/plugin/src/prozessor/State.cpp` ist allein für
  den Rotbeweis von M-37 vorübergehend mutiert und bytegleich zurückgenommen.

### 27.3 Gegenprobe am Stand ohne Schleuse (heute rot)

`docs/beweise/roh/NAK-312-etappe4-gegenprobe.txt`, Lauf 11:26:29 bis 11:28:13.
Die Produktdateien stehen auf der **Basisvariante**: `a80f0603` plus allein die
Testzugänge (Haken, Abbauende-Haken, Panel-Marke) — ohne Schleuse und ohne
Lebendprüfung, SHA-256 je Datei in der Rohdatei. Die Tests sind die
Endfassung; B31 mit vorangestelltem `#define NAK312_GEGENPROBE_OHNE_SCHLEUSE 1`
(blendet nur die Lesestellen der Schleuse aus, Muster Etappe 3b). Ergebnis,
jeweils über die **gezählte Marke** und ohne Absturz (R-312-12): 312/M-30 und
M-39 Fall 1 nachEndeErreicht 1, M-39 Fall 2 1, M-33 1, M-31 in 20 von 20
Zyklen je 1, 312/M-35 Marke 1; beide Prozesse enden regulär mit Exit 1.
Grün wie erwartet: 312/M-32, M-36, M-37, 312/NAK-345, 312/M-05 bis M-08. Das
Bild des geöffneten Panels ist in der Gegenprobe und am Endstand bytegleich
(SHA-256 `A1F6432A…9986`): Handgriff und Oberfläche sind unverändert.

### 27.4 Matrixzeilen der Etappe 4

Einzelläufe am Endstand (Binaries jünger als jede Quelle) in §27.5. Test,
Ergebnis und Rotbeweis je Zeile; jede Rohdatei trägt Kopf, SHA-256 vor und
nach der Mutation (bytegleich), Zeitstempelprüfung, beide Läufe und eine
Auswertung.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-30 | `312/M-30 provider_fasst_nach_dem_schliessen_nichts_mehr_an`, B31 | grün: nachEndeErreicht 0; im Destruktor geschlossen, aktiv 0, gewartetMs 3, abgewiesen 0; Stempel geschlossen < Freigabe < Marke lebend < Abbauende < Rückkehr | heute rot (Gegenprobe: Marke 1); `schliessen()` entfernt → Marke 1, im Destruktor aktiv 1 → rot; `docs/beweise/roh/NAK-312-rot-M-30.txt` |
| M-31 | `312/M-31 zwanzig_gegenzyklen_ohne_zugriff_nach_dem_ende`, B31 | grün: 20 Zyklen, nachEndeErreicht Summe 0; Threads nach Zyklus 1 / 20: 4 / 1, Handles 160 / 160 | heute rot (Gegenprobe: 20 von 20); Schleuse als Mitglied → 20 von 20 an der Marke → rot, ohne Absturz (A-40); `docs/beweise/roh/NAK-312-rot-M-31.txt` |
| M-32 | `312/M-32 stoppfrist_loest_ab_und_der_destruktor_wartet_nur_den_rest`, B31 | grün: stopFristUeberschritten Control 1, Telemetrie 0; Destruktor 2007 ms; gewartetMs 3 innerhalb der Restdauer | Regressionswache; `schliessen()` vor `stop()` → Frist 0 → rot (A-41); `docs/beweise/roh/NAK-312-rot-M-32.txt` |
| M-33 | `312/M-33 reconnect_ruehrt_die_schleuse_nicht_an`, B31 | grün: Reconnect verbunden, dieselbe Schleuse, geschlossen vor/nach nein/nein, betreten 6 → 10, danach Abbau mit nachEndeErreicht 0 | heute nicht messbar; Reconnect legt die Schleuse neu an → andere Schleuse, Marke 1 → rot; `docs/beweise/roh/NAK-312-rot-M-33.txt` |
| M-34 | `312/M-34 destruktor_stoppt_vor_dem_schliessen`, B31 | grün: Frist 1 beim Schliessen, danach gewartet (gewartetMs 3) statt abgewiesen, im Abbauende-Haken zu und aktiv 0 | heute nicht messbar; `schliessen()` an den Anfang → Frist 0, und in 312/M-39 Fall 3 abgewiesen 1 → rot (A-42); `docs/beweise/roh/NAK-312-rot-M-34.txt` |
| M-35 | `312/M-35 panel_abbau_nach_prozessorende_ist_wirkungslos`, B15 | grün: Panel abgebaut, Marke 0, Host-Dirty 0, Bindung bis zum Prozessorende unverändert | heute rot (Gegenprobe: Marke 1); Lebendprüfung entfernt → Marke 1 → rot; `docs/beweise/roh/NAK-312-rot-M-35.txt` |
| M-36 | `312/M-36 normaler_handgriff_bleibt`, B15 | grün: Bindung übernommen, genau 1 Host-Dirty | Regressionswache; Prüfung verlangt zusätzlich `isShowing()` → Änderung verloren, Dirty 0 → rot (A-44); `docs/beweise/roh/NAK-312-rot-M-36.txt` |
| M-37 | `312/M-37 ohne_aenderung_keine_mutation`, B15 | grün: keine Mutation, 0 Host-Dirty | Regressionswache; Matrixmutation bleibt grün (A-43), Ersatz: Frühausstieg entfernt → Dirty 1 → rot; `docs/beweise/roh/NAK-312-rot-M-37.txt` |
| M-38 | `312/M-38 besitzschleuse_liegt_nicht_im_audiopfad`, B7 | grün: 4000 Blöcke, Schleuse betreten 4 → 6 im Messfenster, `RtWache::sperren()` 0, `RtWache::allokationen()` 0, eigener Zähler 0 | zwei Hälften: Zählerhälfte `v3Status()` aus `processBlock` → `RtWache::allokationen()` 8000, `RtWache::sperren()` 0 → rot; Schleusenhälfte ohne eigene Mutation (E-312-11, E-312-10); `docs/beweise/roh/NAK-312-rot-M-38.txt` |
| M-39 | `312/M-39 Fall 1` (im Lauf von 312/M-30), `Fall 2`, `Fall 3`, B31 | grün: (0, 3), (1, 0), (0, 0); nachEndeErreicht je 0; im Abbauende-Haken je aktiv 0 | heute nicht messbar; `schliessen()` entfernt → Fall 1 kehrt bei aktiv 1 zurück, Fall 2 nicht abgewiesen, beide Marke 1 → rot; Fall 3 bleibt grün; `docs/beweise/roh/NAK-312-rot-M-39.txt` |
| NAK-345 | `312/NAK-345 produktverdrahtung_traegt_den_produktnamen`, B31 | grün: gleich ja, Länge 56 / 56, kein Client gestartet | Regressionswache; anderer Name im Produktweg → gleich NEIN → rot; `docs/beweise/roh/NAK-312-rot-NAK-345.txt` |

Zeile **312/NAK-345 produktverdrahtung_traegt_den_produktnamen** (Register
NAK-345 Punkt 2, Härtung H-2 der Erstprüfung der Etappe 2): eine Sonde aus dem
Produktkonstruktor im Testbau trägt genau `pipeNameV3 (aktuelleLogonSid())`;
es startet kein Client und öffnet sich keine Pipe (im Testbau läuft der
Startzweig des Produktkonstruktors nicht). Der Name selbst trägt die SID und
steht in keiner Ausgabe, nur Vergleich und Längen.

### 27.5 Einzelbeine

Bau aller betroffenen Ziele in einem `cmake`-Aufruf aus pwsh, 11:33:31 bis
11:37:02, `BAU EXIT=0`, vorher `LastWriteTime` der geänderten Quellen auf
jetzt (NAK-230). Die Liste stammt aus `eq-copilot/plugin/CMakeLists.txt`: jedes
Ziel, das `sonde/SondeProcessor.cpp` übersetzt (`NakamaProbeeq_VST3`,
`EqCopProbeeqNullTest`, `EqCopSonde012HostChannelContextTest`,
`EqCopSonde012LoudnessSourceTest`, `EqCopSondeLebenslaufTest`,
`EqCopBriefkastenTest`, `EqCopSessionSoak`, `EqCopTransactionTest`), jedes, das
`src/PluginEditor.cpp` übersetzt (`EqCopilot_VST3`, `EqCopNullTest`,
`EqCopMarkierungTest`, `EqCopShot`, `EqCopPaintBench`, `EqCopIdentityTest`,
`EqCopStateMigrationTest`, `EqCopSonde013PassageStateTest`,
`EqCopSonde014IntentTest`, `EqCopSonde014AssistentTest`,
`EqCopLebenslaufTest`, `EqCopQueueStressTest`, `EqCopAnalysisGoldenTest`,
`EqCopSonde012ProjectReloadTest`, `EqCopBriefkastenTest`), dazu
`EqCopSonde012SourcesLatencyTest` und `EqCopSonde012SourcesModelTest`. Lauf
11:37:23 bis 11:40:34, jedes Binary jünger als die jüngste Quelle:

| Bein | Ziel | Ergebnis |
|---|---|---|
| A1 | `EqCopNullTest` | Exit 0, `NULLTEST OK` |
| A3 | `EqCopMarkierungTest` | Exit 0, `MARKIERUNGSTEST OK` |
| A16 | `EqCopProbeeqNullTest` | Exit 0 |
| B1 | `EqCopIdentityTest` | Exit 0, 120 Prüfungen ok, 0 Fehler |
| B2 | `EqCopStateMigrationTest` | Exit 0, 291 Prüfungen ok |
| B4 | `EqCopQueueStressTest` | Exit 0, 121 Prüfungen, 0 Fehler |
| B5 | `EqCopAnalysisGoldenTest` | Exit 0, 248 bestanden, 0 Fehler |
| B7 | `EqCopTransactionTest` | Exit 0, 226 geprüft, 0 Fehler (vorher 225; neu 312/M-38) |
| B8 | `EqCopLebenslaufTest` | Exit 0 |
| B11 | `EqCopSonde012HostChannelContextTest` | Exit 0, 21 bestanden |
| B12 | `EqCopSonde012LoudnessSourceTest` | Exit 0, 4 bestanden |
| B13 | `EqCopSonde012SourcesModelTest` | Exit 0 |
| B14 | `EqCopSonde012ProjectReloadTest` | Exit 0, 231/231 grün |
| B15 | `EqCopShot --sonde012-suite` | Exit 0, 25/25 Shots, Panel 3 geprüft, 0 Fehler |
| B23 | `EqCopSonde013PassageStateTest` | Exit 0 |
| B27 | `EqCopSonde014IntentTest` | Exit 0, 270 Prüfungen ok |
| B29 | `EqCopSonde014AssistentTest` | Exit 0, 193/193 grün |
| B30 | `EqCopBriefkastenTest` | Exit 0 (71,6 s) |
| B31 | `EqCopSondeLebenslaufTest` | Exit 0, 17 geprüft, 0 Fehler (vorher 8), 71,2 s |

Zusätzlich gemessen: `py -3.13 tools/plan/gesundheit.py` (11:32:58) mit
denselben Zählwerten wie der Kanon vorher (2, 11, 34, 39) — die Etappe erhöht
keinen; `py -3.13 tools/plan/tidy.py` zuerst mit 60 Fundstellen (12:09:36):
zwei neue in `PluginEditor.cpp` (`performance-unnecessary-value-param` und
`performance-move-const-arg` am SafePointer-Parameter des Panels), behoben
(der Parameter ist jetzt eine konstante Referenz); zweiter Lauf 12:09:53 bis
12:11:48: **58 Fundstellen von höchstens 58**. Danach 312/M-35 und M-36 am
neuen Stand von `PluginEditor.cpp` erneut rot und grün gefahren (die zwei
Rohdateien tragen diesen Lauf, 12:11:59 bis 12:12:35; B15 danach grün, 25/25
Shots und 3/3 Panelfälle). Die übrigen Ziele, die `PluginEditor.cpp` übersetzen,
baut und fährt der Kanon nachher am Endstand. Nicht gefahren: der
Laufzeit-Arm (Auftrag).

### 27.6 Abweichungen vom Bauplan, begründet

- **A-37 — Ort der Marke.** §6.4 und §7.4 setzen sie „unmittelbar vor
  `transaktion->revision()`". Der erste Zugriff auf Besitzerzustand im
  Statusweg ist aber der Aufruf von `v3Status()` selbst, dessen erste Zeile
  das Zustandsschloss des Prozessors nimmt; eine Marke hinter dieser Sperre
  läge im Lauf gegen den Stand ohne Schleuse hinter dem Zugriff auf ein
  zerstörtes Schloss. Nach der Definition derselben Zeilen („unmittelbar vor
  der ersten Zeile, die Besitzerzustand anfasst", R-312-12) steht die Marke
  deshalb im Provider-Lambda unmittelbar vor `v3Status()`, im Panel
  unmittelbar vor `ed->processor.setzeBindung` (am Basis-SHA vor
  `proz.setzeBindung`).
- **A-38 — Wo die Haken wohnen.** Gens Haken ist ein Mitglied des Prozessors
  und liegt im Zug, dort lebt der Prozessor noch. Die Schranke vor
  `betreten()` und die Marke müssen dagegen aus einer abgelösten
  Client-Laufzeit nach dem Ende des Prozessors erreichbar sein; sie liegen
  deshalb in einem eigenen `shared_ptr`, den das Statuslambda fängt (nur im
  Testbau). Das Kanarienvogel-Flag setzt ein Haken am Ende des
  Destruktorrumpfs (Spalte „Reihenfolge und Frist" von M-30). Die Panel-Marke
  ist eine funktionsstatische Größe in `PluginEditor.cpp`; der Test deklariert
  ihren Zugang selbst, weil `PluginEditor.h` kein Ticketpfad dieser Etappe ist.
- **A-39 — Freigaberegel der Schranke in M-30, M-33 und M-39 Fall 1.** Statt
  „sobald `stand().geschlossen` wahr ist" gibt das Bein frei, sobald die
  Schleuse geschlossen ist **und** den gehaltenen Provider als laufend führt
  (`aktiv ≥ 1`), oder sobald der Abbauende-Haken `lebt` auf false gesetzt hat.
  Schlösse eine Schleuse ohne diesen Zug (die Mutante von M-33 oder der Stand
  ohne Schleuse), gäbe die reine Regel den Provider frei, während die
  Mitglieder sterben — ein Rotlauf in undefiniertem Verhalten. Beides sind
  beobachtbare Zustände, keine Wanduhr (E-312-10).
- **A-40 — M-31 mit der Schranke vor `betreten()` und ein Fallschalter.** Die
  Matrixmutation (Schleuse als Mitglied) fällt ohne undefiniertes Verhalten
  nur, wenn der Provider nach dem Ende des Eigentümers beginnt (Geometrie von
  M-39 Fall 2); in der Geometrie von M-30 wartet der Destruktor auf ihn, und
  die Mutation träfe allein ein Rennen im Verlassen des Zugs innerhalb von
  `Schleuse.h`, das ohne Haken dort nicht messbar ist (E-312-10). Die 20
  Zyklen halten deshalb vor `betreten()` und geben nach dem Destruktor frei.
  Der Rotlauf von M-31 fährt mit dem Argument `312/M-31` allein diesen Fall:
  die übrigen Fälle mit Schranke hinter `betreten()` liefen im Mutantenbau in
  dasselbe Rennen. B31 bekommt dafür einen Fallschalter (Argument = Teil der
  Kennung); der Kanon fährt ohne Argument alles. Ebenso gefiltert laufen die
  Rotläufe von M-39 (`312/M-39`) und 312/NAK-345.
- **A-41 — M-32 fällt an der Zählerhälfte.** Die Matrix erwartet unter
  „`schliessen()` vor `stop()`" einen Destruktor, der die Beinfrist reißt. Weil
  das Bein beim Schließen freigibt (A-39), endet der Provider dann vor `stop()`,
  und `stop()` joint ihn: `stopFristUeberschritten` bleibt 0 → rot an der
  Zusage „steigt um 1". Gebaut als zusätzlicher, früherer Aufruf; der spätere
  bleibt und ist laut `Schleuse.h` idempotent — die Wirkung ist genau das
  Vorziehen. Dasselbe gilt für M-34.
- **A-42 — was M-34 misst.** Gemessen wird die Kette, die die Etappe baut:
  beide `stop()` vor dem Schließen (der gehaltene Provider war beim Schließen
  bereits abgelöst, Frist 1, und wurde danach gewartet statt abgewiesen) und
  das Schließen vor dem Ende der Mitglieder (Abbauende-Haken: zu, aktiv 0).
  Briefkasten, Listener und Workerjoin davor sind unveränderte Zeilen. Die
  Matrixfolge „abgewiesen steigt, wo gewartetMs stehen müsste" zeigt der
  Rotlauf in 312/M-39 Fall 3 (abgewiesen 1, gewartetMs 0).
- **A-43 — M-37: die Matrixmutation ist kein Rotbeweis.** „Den Frühausstieg
  vor die Sperre ziehen" ist gebaut und bleibt grün (Teil a der Rohdatei): das
  Bein läuft auf einem Thread, zwischen Vergleich und Sperre schreibt niemand.
  Ersatz an der Zeile, die die Zusage trägt: der Frühausstieg entfernt → Dirty
  1 → rot (Teil b). `State.cpp` ist nicht Ticketpfad dieser Etappe; beide
  Mutationen sind bytegleich zurückgenommen (Muster A-6).
- **A-44 — Form der Mutation von M-36.** „Den Editor im Abbau bereits als tot
  führen" ist als zusätzliche Bedingung `ed->isShowing()` gebaut: ein Editor
  ohne sichtbares Fenster — im Abbau des Hostfensters wie im Bein ohne
  Desktop — gilt dann als tot.
- **A-45 — M-38 misst die Sonde, nicht Gen.** B7 baut keinen Gen; die
  Lebendprüfung liegt in `MesspunktPanel::uebernehmen` auf dem
  Nachrichtenthread, Gens `processBlock` ist in dieser Etappe unverändert, und
  A1 und A3 sind grün. „4000 Blöcke" ist als Untergrenze gebaut: das Bein fährt
  weiter, bis ein Provider im Messfenster durch die Schleuse gelaufen ist
  (Heartbeat-Takt 1 s; 10 s nur als Obergrenze gegen einen hängenden Lauf).
- **A-46 — was M-35 an der Bindung misst.** Die Bindung ist bis zum Ende des
  Prozessors lesbar und dort unverändert, die Dirty-Zählung läuft bis dahin;
  danach belegt allein die Marke (0), dass der Panel-Destruktor nichts mehr
  anfasst.
- **A-47 — ein Lauf, vier Zusagen.** M-30, M-32, M-34 und M-39 Fall 1 haben
  dieselbe Hakenlage (die Matrix führt sie als Teilfälle von M-30); ein
  gehaltener Abbau trägt alle vier Prüfungen. M-39 Fall 2 und Fall 3 sind je
  ein eigener Lauf.

### 27.7 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Etappe 4

| Abschnitt | Punkt | Etappe 4 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge beim Trennen, Joins mit Frist, Stopp-Fenster, Close-Flag vor Inhalt | angewandt: erst `stop()` beider Clients (Frist 2 s, danach Ablösung), dann Schließen, dann Mitglieder (312/M-34, M-32); ein Rückruf nach dem Schließen wird abgewiesen, einer davor gewartet (312/M-39); das Schließflag prüft `betreten()` vor jedem Besitzerzugriff; Reconnect lässt die Schleuse stehen (312/M-33); 20 Gegenzyklen ohne Anstieg von Handles und Threads (312/M-31) |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | nicht zutreffend: kein Draht-, Schema- oder Statefeld; der Zahlenrand der Etappe ist die Frist (312/M-32: Destruktor ≥ 2000 ms, `gewartetMs` innerhalb der Restdauer) |
| D Bau- und Prüfriegel | fail-closed, Frische, was der Kanon nicht baut | angewandt: Binaryzeitstempel gegen die Quelle bei Gegenprobe, jedem Rotlauf und jedem Bein; Testzugänge nur unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`; jeder neue Fall liegt in einem Kanonbein (B7, B15, B31); der Fallschalter von B31 greift nur mit Argument |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: B31 nennt genau die gemessenen Zustände (Marke, gewartet, abgewiesen, Frist, Reconnect, Zyklen, Produktname), B15 die drei Panelfälle, B7 die 4000 Blöcke mit laufenden Clients und die zwei Zähler — ohne Sperrenbehauptung jenseits von `RtWache::GemeldeteSperre` |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerbehauptungen B7, B15, B31; Kopfkommentare von B7, B15 (ShotTestMain) und B31, Klassenkopf der Sonde, Kommentare am Destruktor, an den Lambdas und am Übergabepunkt; Manifestkopf (Etappe, Basis-SHA, Kanon) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: elf Rohdateien `docs/beweise/roh/NAK-312-rot-M-30.txt` bis `NAK-312-rot-M-39.txt` und `NAK-312-rot-NAK-345.txt`, dazu die Gegenprobe; Ersatz und Abweichungen begründet (A-40 bis A-44) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Werte aus Läufen, Uhrzeiten aus `Get-Date`, Logdateien und Binaryzeitstempeln |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: verbinden↔trennen (Provider betreten, Schleuse schließen, `addListener` und Destruktor unverändert paarweise, Reconnect 312/M-33), öffnen↔schließen (Panel öffnen und abbauen, 312/M-35 bis M-37), starten↔stoppen (312/M-32, M-34) — Bau, Tests, Rotbeweise und Behauptungen in einem Commit |

### 27.8 Selbstaudit

- **Destruktorfolge und Mitgliederreihenfolge.** `callbackSchleuse` ist vor
  `controlV3` und `telemetryV3` deklariert und stirbt nach ihnen; die Lambdas
  kopieren den Zeiger bei der Konstruktion, das Mitglied wird nie neu
  zugewiesen (312/M-33). Der Destruktor schließt nach beiden `stop()` und vor
  jeder Mitgliederzerstörung; `transaktion` (`SondeProcessor.h:720`), das der
  Statusprovider liest, stirbt danach und vor `controlV3`. Der Abbauende-Haken
  (nur Testbau) läuft nach dem Schließen, der Prozessor lebt dort.
- **Frist genau 2 s und gerissene Frist.** `kStopFristMs` = 2000 bleibt
  unverändert. Gemessen: Destruktor 2005 bis 2007 ms bei gehaltenem Provider,
  `stopFristUeberschritten` 1 für den gehaltenen ControlClient, 0 für die
  Telemetrie; danach wartet `schliessen()` 1 bis 3 ms (die Restdauer des
  Providers). Ohne gehaltenen Provider (312/M-39 Fall 3, M-07) endet der Abbau
  in 5 bis 7 ms.
- **Wiederholte Gegenzyklen.** 20 Zyklen mit je eigener Sonde, Schleuse und
  eigenem Testserver; das Bein wartet je Zyklus, bis die abgelöste Laufzeit
  die Haken losgelassen hat und ihr Thread beendet ist, bevor es zählt.
- **Stale Closures.** Produkt: jedes Lambda fängt `this` und eine Kopie des
  Schleusenzeigers, nie eine Referenz auf ein Mitglied. Test: die Haken fangen
  den Kanarienvogel als `shared_ptr`; der Abbauende-Haken fängt `&l` und den
  rohen Prozessorzeiger und läuft nur innerhalb von `gehaltenerAbbau`; der
  Freigabethread wird vor der Rückkehr gejoint; die Panel-Marke fängt ihren
  Zähler als `shared_ptr` und wird nach jedem Fall geleert.
- **Rückruf nach dem Abbau wirkungslos.** Sonde: ein Provider nach dem
  Schließen liefert das leere Default-Objekt, der Client bricht danach ab
  (`sollAbbrechen`), die Laufzeit hält nur noch eine geschlossene Schleuse.
  Panel: `getComponent()` castet dynamisch und ist null, sobald der Editor
  seinen eigenen Destruktor verlassen hat; danach fasst `uebernehmen()` nichts
  an (312/M-35). Der normale Handgriff (312/M-36, M-37) und das Bild des
  Panels sind unverändert.
- **Beziehungen.** verbinden↔trennen: jeder der drei Provider betritt, der
  Destruktor schließt; `addListener` im Konstruktor und `removeListener` im
  Destruktor bleiben paarweise; ein Reconnect öffnet und schließt nichts
  (312/M-33). öffnen↔schließen: Panel öffnen über den Handgriff, schließen
  über `dismiss()` oder über das Ende des Editors (312/M-35 bis M-37).
- **Echtzeit.** `processBlock` ist unverändert; die Schleuse arbeitet im
  Callbackpfad nur mit Atomics und liegt nicht im Audiopfad (312/M-38: 0/0 über
  4000 Blöcke bei laufenden Clients). Keine neue Sperre, keine Allokation im
  Audiothread; der Aufbau der Schleuse ist eine Allokation im Konstruktor.
- **Behauptungszeile nicht weiter als der Test.** Jeder Satz der ergänzten
  Behauptungen hat seinen Fall; der Satz über Handles und Threads nennt genau
  „nicht höher als nach dem ersten Zyklus".
- **Kommentare (R-312-14).** Berichtigt: der Übergabepunkt des Panels (§1,
  „Component nullt SafePointer sofort" — `git grep` findet den Satz nicht
  mehr), der Kopf von B31 („misst dieses Bein erst mit Etappe 4" ist jetzt
  gemessen) und der erste Destruktorschritt der Sonde („Schleuse zu" meinte
  den Briefkasten; seit es eine zweite Schleuse gibt, sagt der Kommentar das).
  Die Zeile über B31 in `tools/beweise.ps1` („misst es erst ab Etappe 4")
  bleibt wahr.
- **Git.** `git status --short` nach dem letzten Rotlauf: die acht geänderten
  Dateien der Etappe, das Manifest, die Rohausgabe des Kanons vorher, die
  Gegenprobe, elf Rot-Rohdateien und die zwei fremden Ordner; `State.cpp` ist
  nicht darunter.

### 27.9 Hinweise an den Dirigenten

- **Produktwirkung von R-312-2.** Ein im Namens- oder Paarfeld des Popovers
  getippter, noch nicht bestätigter Text (weder Enter noch Fokuswechsel) wird
  nicht mehr übernommen, wenn das Editorfenster vor dem Popover schließt;
  bisher übernahm ihn der Panel-Destruktor im nächsten Durchlauf der
  Nachrichtenschleife, solange der Prozessor noch lebte. Die Rollenwahl
  übernimmt weiter sofort. Ob FL beim Schließen des Fensters den Fokus vorher
  abgibt (dann übernähme `textEditorFocusLost` bei lebendem Editor), ist nicht
  gemessen.
- **Beobachtung, nicht untersucht:** in jedem gehaltenen Abbau zählt die
  Schleuse rund 185 Eintritte in etwa zwei Sekunden Verbindung; welcher
  Provider so oft gerufen wird, liegt außerhalb dieser Etappe.
- **Installer-Hashes.** Die Bundlebytes beider Plugins ändern sich mit dieser
  Etappe; vor dem Laufzeit-Arm ist nach `CLAUDE.md` `--hashen` fällig.
- **Laufzeit von B31** steigt von unter einer Sekunde auf rund 71 s (22
  gehaltene Abbauten zu je gut 2 s Stoppfrist).

---

## Kanon-Lauf - NAK-312 Etappe 4

**Lauf:** 2026-09-21 12:17 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-553b70a-dirty.md](roh/NAK-312-553b70a-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 12:17:29 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 553b70ae NAK-312 Etappe 4: Besitzschleuse der Sonde, Lebendpruefung des Messpunkt-Panels |
| Commit (voll) | 553b70aec3f3126d2794fe77b55918c2a62f99e0 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,21 s | [A1](roh/NAK-312-553b70a-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,98 s | [A2](roh/NAK-312-553b70a-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,83 s | [A3](roh/NAK-312-553b70a-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.131,70 s | [A4](roh/NAK-312-553b70a-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,51 s | [A4-SI](roh/NAK-312-553b70a-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,76 s | [A4b](roh/NAK-312-553b70a-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,20 s | [A5](roh/NAK-312-553b70a-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,15 s | [A6](roh/NAK-312-553b70a-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,15 s | [A7](roh/NAK-312-553b70a-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,43 s | [A8](roh/NAK-312-553b70a-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,69 s | [A9](roh/NAK-312-553b70a-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,11 s | [A10](roh/NAK-312-553b70a-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,26 s | [A11](roh/NAK-312-553b70a-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A12](roh/NAK-312-553b70a-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,21 s | [A13](roh/NAK-312-553b70a-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 9,38 s | [A14](roh/NAK-312-553b70a-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,32 s | [A17](roh/NAK-312-553b70a-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 37,77 s | [A18](roh/NAK-312-553b70a-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-312-553b70a-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,32 s | [A20](roh/NAK-312-553b70a-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,60 s | [A21](roh/NAK-312-553b70a-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,34 s | [A22](roh/NAK-312-553b70a-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,59 s | [A24](roh/NAK-312-553b70a-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,17 s | [A34](roh/NAK-312-553b70a-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 30,17 s | [A35](roh/NAK-312-553b70a-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 6,79 s | [A36](roh/NAK-312-553b70a-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,28 s | [A28](roh/NAK-312-553b70a-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,55 s | [A29](roh/NAK-312-553b70a-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,31 s | [A30](roh/NAK-312-553b70a-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 97,78 s | [A31](roh/NAK-312-553b70a-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,16 s | [A25](roh/NAK-312-553b70a-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,14 s | [A26](roh/NAK-312-553b70a-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-312-553b70a-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,78 s | [A23](roh/NAK-312-553b70a-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,68 s | [A16](roh/NAK-312-553b70a-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,14 s | [B1](roh/NAK-312-553b70a-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,46 s | [B2](roh/NAK-312-553b70a-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,05 s | [B3](roh/NAK-312-553b70a-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-312-553b70a-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,45 s | [B3c](roh/NAK-312-553b70a-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,37 s | [B4](roh/NAK-312-553b70a-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-312-553b70a-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,65 s | [B5](roh/NAK-312-553b70a-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,47 s | [B6](roh/NAK-312-553b70a-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, nimmt jeder Wechsel nach offline genau eine gemeldete Sperre, ohne Wechsel keine. Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist bitgleich zu einem mit Takt nach jedem Block - fuer alle vier in beiden Staenden (neutral und mit Band) bei Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline; fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (dort wirkt der Wert im Block seines Randes), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. Seit der Nacharbeit 1 der Etappe 3 (L-1, R-312-16) nimmt der Blockrand nach einem read-only-Ladestart keinen Hostwert: laedt ein Stand mit eq an und Output-Trim +3 dB das fremde Major und kommen danach Input-Trim +24 dB, Output-Trim +24 dB, Width 2 und Mix 0 als Hostwerte, ist der Ausgang ab dem ersten Sample nach dem Laden bitgleich zu dem ohne diese Hostwerte, und die Blockrandziele steigen um 0 - bei Bloecken zu 256 und, wenn beim Laden ein Uebergang laeuft, bei 185 und 512; ein eigener schreibbarer Stand danach laesst Output-Trim +6 dB ohne Kontrolltakt wieder am Blockrand wirken (bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang am Ende bei 10^(6/20)). Seit NAK-312 Etappe 4 (312/M-38): ueber mindestens 4000 Bloecke wechselnder Groesse, waehrend beide v3-Clients der Sonde ueber einen Testserver auf der Probe-Pipe verbunden sind und ihre Provider nachweislich durch die Besitzschleuse laufen, bleiben RtWache::sperren() und RtWache::allokationen() ab Callback-Eintritt 0. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 2,29 s | [B7](roh/NAK-312-553b70a-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,30 s | [B10](roh/NAK-312-553b70a-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,03 s | [B11](roh/NAK-312-553b70a-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,61 s | [B12](roh/NAK-312-553b70a-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. Seit Etappe 4 (R-312-7, R-312-12) die Besitzschleuse der Sonde, gemessen an einem gehaltenen Statusprovider und einer gezaehlten Marke vor dem ersten Besitzerzugriff: nach dem Ende des Eigentuemers erreicht ihn kein Provider (0 in jedem Fall und in 20 Gegenzyklen, nach denen Handles und Threads nicht hoeher stehen als nach dem ersten); gehalten hinter dem Betreten wird er zu Ende gewartet, gehalten davor und nach dem Destruktor freigegeben abgewiesen, vor dem Abbau freigegeben laeuft er durch; stop() loest den gehaltenen ControlClient nach der 2-s-Frist ab, und beide stop() liegen vor dem Schliessen; ein Reconnect laesst dieselbe offene Schleuse stehen. Eine Sonde aus dem Produktkonstruktor traegt im Testbau genau pipeNameV3 (aktuelleLogonSid()), ohne dass ein Client startet. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 71,21 s | [B31](roh/NAK-312-553b70a-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,09 s | [B13](roh/NAK-312-553b70a-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,39 s | [B14](roh/NAK-312-553b70a-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. Seit NAK-312 Etappe 4 (R-312-2) faehrt dasselbe Bein das Messpunkt-Popover am echten Editor: baut die Nachrichtenschleife es erst nach Editor und Prozessor ab, fasst sein Destruktor keinen von beiden an (gezaehlte Marke 0, Bindung bis zum Prozessorende unveraendert, 0 Host-Dirty); schliesst es, solange der Editor lebt, wird eine Aenderung mit genau einer Host-Dirty-Meldung uebernommen, ohne Aenderung mit keiner. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,79 s | [B15](roh/NAK-312-553b70a-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-312-553b70a-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,22 s | [B22](roh/NAK-312-553b70a-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,69 s | [B23](roh/NAK-312-553b70a-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,10 s | [B24](roh/NAK-312-553b70a-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-312-553b70a-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,49 s | [B26](roh/NAK-312-553b70a-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-312-553b70a-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,99 s | [B19](roh/NAK-312-553b70a-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,34 s | [B17](roh/NAK-312-553b70a-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 5,93 s | [B18](roh/NAK-312-553b70a-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,92 s | [B16](roh/NAK-312-553b70a-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,43 s | [B27](roh/NAK-312-553b70a-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,87 s | [B29](roh/NAK-312-553b70a-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,72 s | [B30](roh/NAK-312-553b70a-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,17 s | [B28](roh/NAK-312-553b70a-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,30 s | [B8](roh/NAK-312-553b70a-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 13,11 s | [A32](roh/NAK-312-553b70a-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 124,71 s | [A33](roh/NAK-312-553b70a-dirty.md#a33) |


### 27.10 Kanon nachher (Bauer, 21.09.2026)

Voller Kanon abgekoppelt nach dem Auftrag, Befehl `pwsh -NoProfile -File
tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312
Etappe 4'`, auf dem Commit `553b70aec3f3126d2794fe77b55918c2a62f99e0`; Start
12:17:28 (`Get-Date`), Ende 12:57:04 (Zeitstempel der Logdatei, letzte Zeile
`EXIT=0`). Kopf und Übersicht stehen im Runnerblock „Kanon-Lauf - NAK-312
Etappe 4" unmittelbar über diesem Abschnitt, die Rohausgabe unter
`docs/beweise/roh/NAK-312-553b70a-dirty.md`. Kopfzeile des Runners, wörtlich:

```
**Lauf:** 2026-09-21 12:17 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0
```

- **GRÜN 70/70**, Exitcode 0; stillgelegt bleibt A15. Die Zahl ist die des
  Kanons vorher; Etappe 4 bringt kein neues Bein. Die Beine der Etappe sind
  grün: B7 (`EqCopTransactionTest`, 2,29 s), B15 (`EqCopShot`, 2,79 s), B31
  (`EqCopSondeLebenslaufTest`, 71,21 s), dazu A1, A3, A16, B2, B14 und B30.
- **A32** endet mit [HINWEIS] Exit 4, nicht blockierend, mit denselben
  Zählwerten wie der Kanon vorher: Quelldateien über 2000 Zeilen 2, über dem
  Ziel von 1500 Zeilen 11, Funktionen über 200 Zeilen 34, Kommentar-Bezeichner
  ohne Code 39. Die Etappe erhöht keinen dieser Werte. **A33** [OK] Exit 0,
  clang-tidy-Fundstellen 58 von höchstens 58.
- **„dirty"** im Namen der Rohausgabe: die zwei fremden untracked Ordner
  `briefing-hub/` und `nimbalyst-local/`; `git status --short` zeigte nach dem
  Lauf sonst nur die Schreibziele des Runners (dieses Manifest und die
  Rohausgabe). `eq-copilot/install/nakama-installer-v1.json` blieb unverändert.
- **Lauf ohne Eingriff in Quellen, ein Stillstand erklärt.** Nach „Baue:" stand
  das Log ab 12:21:00 ohne aktiven Compiler; allein `vctip.exe` (Start
  12:17:36) lebte. Um 12:21:09 beendet (NAK-300); danach lief der Runner
  weiter. Das längste Bein war A4 (1 131,70 s). Vor dem Start lief kein Bau-
  oder Testprozess (NAK-290).

## 28. Messung der Etappe 4, Laufzeit-Arm, Auftrag Erstprüfung (Dirigent, 21.09.2026, 13:10 Uhr)

**Messung.** Worker `8fbc6039` (Opus, Effort xhigh, `dontAsk`, Aufsicht ENG),
10:35 bis 12:58 Uhr (Startbeleg und `claude agents --json` mit `done`, beide
gemessen), Basis `a80f0603cad1a40ce4ceb5d47cb55a0011e33d7c`. Ergebnis: zwei
Commits, `553b70aec3f3126d2794fe77b55918c2a62f99e0` (12:17:08, Produkt, Tests,
Behauptungszeilen, Rotbeweise) und `dc2665baf4a8f6e12dee003a437fd6b98903cb5c`
(12:57:43, Kanon nachher und Manifestkopf) = `origin/master`, Baum sauber (nur
die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD. Rundenbilanz
`a80f0603..dc2665ba` (`py -3.13 tools/dirigent/rundenbilanz.py`, gemessen):
Produkt 3 Dateien +171/-15, Tests 3 Dateien +908/-9, Prüfwerkzeug 1 Datei
+3/-3, Doku 15 Dateien +22173/-4. Außerhalb von `docs/beweise/` berührt der
Diff genau sieben Pfade, alle Ticketpfade der Etappe:
`eq-copilot/plugin/sonde/SondeProcessor.cpp`, `SondeProcessor.h`,
`eq-copilot/plugin/src/PluginEditor.cpp`, die drei Testdateien
`ShotTestMain.cpp`, `SondeLebenslaufTestMain.cpp`, `TransactionTestMain.cpp`
und `tools/beweise.ps1`; `eq-copilot/plugin/core/ipc/controlclient/Schleuse.h`
(E-312-10) und `eq-copilot/plugin/src/prozessor/State.cpp` (Mutation von
A-43, bytegleich zurückgenommen) stehen nicht im Diff.

Vom Dirigenten am Produktdiff gelesen: `callbackSchleuse` ist unmittelbar vor
`controlV3` und `telemetryV3` deklariert; alle drei Provider betreten die
Schleuse vor `v3Hello()`, `v3Status()` und `v3TelemetryHello()` und liefern
abgewiesen das leere Default-Objekt; `callbackSchleuse->schliessen()` steht im
Destruktor nach `telemetryV3.stop()` und `controlV3.stop()`; das Panel hält
einen `SafePointer` auf den Editor, und die Lebendprüfung ist die erste
Anweisung von `uebernehmen()`. Die neuen Testzugänge der Sonde liegen im
bestehenden Block `#if defined (NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` von
`SondeProcessor.h`, die Panel-Marke in einem eigenen solchen Block von
`PluginEditor.cpp`.

**Messabdeckung.** Kanon nachher am Stand `553b70ae`: `EXIT=0`, GRUEN 70/70
(Log des Runners vom Dirigenten gelesen); B7 `EqCopTransactionTest`, B31
`EqCopSondeLebenslaufTest` (71,21 s) und B15 `EqCopShot` je Exit 0, A33
`tidy.py` Exit 0, A32 HINWEIS Exit 4 nicht blockierend (Register NAK-292). In
der Rohausgabe `docs/beweise/roh/NAK-312-553b70a-dirty.md` stehen als `ok`
gelaufen: 312/M-30, M-31, M-32, M-33, M-34, M-35, M-36, M-37, M-38, M-39 Fall
1 bis 3 und 312/NAK-345. Elf Rotbeweisdateien und die Gegenprobe liegen
(§27.3, §27.4).

**Laufzeit-Arm** (12:59 bis 13:06 Uhr, Stand `dc2665ba`):
`LAUFZEIT NAK-312 dc2665ba VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0]`,
Exit 3 allein wegen Karte U43 (Referenzprojekt
`Nakama-Diagnose-Verarbeitung.flp` fehlt, Schritt 8 VORAUSSETZUNG), kein
Szenario verfehlt im Sinn von Exit 4. **Nulltest im Host am
Auslieferungszustand BITIDENTISCH** (v = 0, g = 1,0, 0 Abweichungen über
2 015 193 Frames); Vergleich `ohne_slots` GLEICH. Rohdaten
`docs/beweise/roh/NAK-312-laufzeit-dc2665ba.md`; die nachgezogenen Hashes in
`eq-copilot/install/nakama-installer-v1.json` gehören zu diesem Commit.

**Hinweis des Bauers mit Produktwirkung (§27.9), noch nicht eingeordnet.** Ein
im Namens- oder Paarfeld des Popovers getippter, noch nicht bestätigter Text
wird nicht mehr übernommen, wenn das Editorfenster vor dem Popover schließt und
der Prozessor noch lebt. Der Prüfer ordnet das gegen den letzten Satz von
R-312-2 und M-36 ein; der Dirigent entscheidet danach.

**Auftrag Erstprüfung der Etappe 4.** Frischer, lesender Opus-Thread, Effort
max (Codex-Woche 94 %, Reset 22.09.2026, 22:19 Uhr), T2, Vorlage A; Auftrag
`docs/beweise/roh/NAK-312-etappe-4-erstpruefung-auftrag.txt`, Bericht nach
`docs/beweise/roh/NAK-312-etappe-4-erstpruefung-urteil.md` (Zielpfad, noch
nicht angelegt). Prüfbereich: `git diff a80f0603...<Commit dieses Abschnitts>`
über die sieben Produkt-, Test- und Werkzeugpfade der Etappe.

## 29. Erstprüfung der Etappe 4: PASS, Validierung, Einordnung, Abschluss der Etappe 4 (Dirigent, 21.09.2026, 14:13 Uhr)

### 29.1 Rohurteil

**Erstprüfung der Etappe 4: Rohurteil PASS.** Frischer, lesender Opus-Thread
`7a3e2efe`, Effort max, `dontAsk`, Vorlage A, T2, 13:11 bis 13:35 Uhr
(Startbeleg und `done` vom Dirigenten gemessen), HEAD
`9e01ecac12b7fd7da65d71a8a241c67ed4494f6e` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Prüfbereich `git diff a80f0603...33ef53fc` über die
sieben Pfade der Etappe; geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-etappe-4-erstpruefung-urteil.md`. 0 Defekte, eine
Lücke (L-1), zwei Härtungen (H-1, H-2).

### 29.2 Validierung an der Quelle

Frischer, lesender Opus-Thread `9abee352`, Effort max, `dontAsk`, 13:37 bis
14:09 Uhr (gemessen), HEAD `94f54e4bde01b37dee423ccf5adbf4bc9bf48296` vor und
nach dem Lauf gleich (vom Dirigenten gemessen); Auftrag
`docs/beweise/roh/NAK-312-etappe-4-erstpruefung-validierung-auftrag.txt`,
Bericht `docs/beweise/roh/NAK-312-etappe-4-erstpruefung-validierung.md`:
**4 bestätigt** (V-1 Mechanik von L-1, V-2 Stand am Basis-SHA, V-5 H-1, V-7
tragende Entlastungen), **3 präzisiert** (V-3 Zusagenlage, V-4 Baubarkeit,
V-6 H-2), **0 widerlegt**.

### 29.3 Einordnung (Dirigent)

- **L-1 — LÜCKE mit Produktentscheid, Karte U57 (E-312-14).** Ein im
  Messpunkt-Popover getippter, weder mit Enter noch mit Fokuswechsel
  bestätigter Name oder Paarname wird verworfen, wenn der Editor vor dem
  Popover abgebaut wird und der Prozessor noch lebt; am Basis-SHA übernahm ihn
  der Panel-Destruktor (V-2, gelesen, nicht gemessen). Keine Matrixzeile führt
  den Fall als Zusage (V-3); R-312-2 ist für ihn in sich gespannt — Satz 2 und
  der Bauplan §7.4 verwerfen, Satz 3 („Der Handgriff bleibt derselbe") erhält,
  und die Prämisse seiner Herkunft („es geht um Speicherlebensdauer, nicht um
  Verhalten") trifft für genau diesen Fall nicht zu. Ob unbestätigter Text beim
  Schließen des Fensters gilt, ist Bedienlogik und damit Sache des Users:
  Karte **U57** in `docs/plan/fragen.json`, Empfehlung Weg 1 (gebauter Stand).
  Gründe der Empfehlung: das Sources-Labelfeld desselben Editors verwirft
  unbestätigten Text beim Editorabbau am Basis-SHA wie am Zielstand (V-3
  letzter Punkt); eine erhaltende Übernahme liefe als Host-Rückruf
  (`setDirty`, `restartComponent`) verschachtelt im `IPlugView::removed()` des
  Hosts und griffe bei jedem Editorabbau, auch beim Entfernen des Plugins und
  am Projektende (V-4 Punkt 6); ob FL beim Fensterschluss die Ansicht abbaut
  oder den Fokus vorher abgibt, ist nicht gemessen. **Berichtigung:** die vom
  Dirigenten erwogene Mechanik, das Popover im Editorabbau synchron zu
  zerstören, ist unzulässig — Box und Panel sind Unterobjekte des
  `CallOutBoxCallback`, den der Modal-Manager besitzt (V-4 Punkt 2). Wählt der
  User Weg 2, ist der baubare Weg eine synchrone Übernahme im Rumpf von
  `~EqCopilotEditor` vor `setzeEditorOffen (false)`; M-35 und B15 wären dafür
  neu zu fassen (V-4 Punkte 3 und 6), gebaut in Etappe 7 mit eigenem
  Matrixnachtrag. Keine Nacharbeit in dieser Etappe.
- **H-1 — zwei Teile (E-312-15).** Bestätigt (V-5): 312/M-34 misst nur
  `controlV3.stop()` sicher vor dem Schließen; die B31-Behauptungszeile in
  `tools/beweise.ps1` („beide stop() liegen vor dem Schliessen") sagt an der
  Telemetriehälfte mehr, als das Bein misst. Am Code ist M-34 erfüllt
  (Reihenfolge `telemetryV3.stop()`, `controlV3.stop()`, `schliessen()`,
  gelesen von Prüfer und Validierer), der Rotbeweis der Matrixzeile fällt,
  keine Zusage von Gate oder Matrix bricht: kein DEFEKT im Sinn des
  Rundenbudgets. Die überziehende Behauptung ist trotzdem ein Fehler im
  Auftrag und wird behoben: **der Bauer der Etappe 5 fasst die Zeile als
  Pflichtzusatz auf das Gemessene** („`controlV3.stop()` gemessen vor dem
  Schließen, `telemetryV3.stop()` nach Zeilenfolge"). Die Telemetriehälfte
  messbar zu machen, ist HÄRTUNG (Register NAK-348).
- **H-2 — HÄRTUNG (Register NAK-348).** Präzisiert (V-6): die Zusage von M-30
  („kein Provider") reicht dem Wortlaut nach weiter als ihre festgelegte
  Messung (Schranke und Marke nur im Statusprovider); die Matrix legt die
  Messung ausdrücklich dorthin, R-312-7 verlangt keine Messung je Provider.
  Alle drei Provider betreten die Schleuse vor dem ersten Besitzerzugriff
  (gelesen).
- **Nebenfund der Validierung (V-4 Punkt 5), außerhalb des Prüfbereichs:** das
  Kennungskonflikt-Panel ruft den Editor über rohes `this`. Gelesen, nicht
  gemessen, nicht bewertet → Register **NAK-349** mit Quellvalidierung vor dem
  Start der Etappe 6.
- **Werkzeug:** das Cockpit erkennt einen Startbeleg nur für Workernamen mit
  der Endung `-bau` oder `-fort`; Prüfer und Validierer meldeten deshalb
  „Startbeleg fehlt" (Anzeige, kein Zustand des Laufs) → Register **NAK-350**.

### 29.4 Urteil und Stand

**Erstprüfung der Etappe 4: PASS.** Kein Defekt, keine Nacharbeitsrunde;
Rundenbudget der Etappe 4: null von drei Runden verbraucht. Rundenbilanz der
Etappe (eine Baurunde, §28): Produkt 3 Dateien +171/-15, Tests 3 Dateien
+908/-9, Prüfwerkzeug 1 Datei +3/-3; Werkzeugurteil: kein Konvergenz-Signal.
**Etappe 4 ist damit abgeschlossen.**

**Stand der Etappen.** 0 bis 4 abgeschlossen. Offen: Etappe 5
(Offline-Übergang, Parameteranbindung), 6 (Gen-Fläche und Hörmarkierung), 7
(Produktabhängiges nach den Antworten auf U48, U49, U51, U56 und
gegebenenfalls U57, mit eigenem Matrixnachtrag), Abschlussprüfung T2 über
Basis...HEAD mit vollem Kanon und Laufzeit-Arm.

**Zusätze für den Bauer der Etappe 5:** (1) die B7-Behauptung zum Offlineflag
auf „Aufruf" festlegen (Register NAK-345 Punkt 1, aus §26); (2) die
B31-Behauptungszeile nach E-312-15 auf das Gemessene fassen.

---

## Kanon-Lauf - NAK-312 Etappe 5

**Lauf:** 2026-09-21 15:10 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-be37d10-dirty.md](roh/NAK-312-be37d10-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 15:10:47 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | be37d10a NAK-312 Etappe 5: Parameteranbindung ohne APVTS (T3-01-02) |
| Commit (voll) | be37d10a5d8b2525fbe39e84e2d7c9a4fb403fd2 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,11 s | [A1](roh/NAK-312-be37d10-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 10,87 s | [A2](roh/NAK-312-be37d10-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,69 s | [A3](roh/NAK-312-be37d10-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.236,35 s | [A4](roh/NAK-312-be37d10-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 13,29 s | [A4-SI](roh/NAK-312-be37d10-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,72 s | [A4b](roh/NAK-312-be37d10-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 4,17 s | [A5](roh/NAK-312-be37d10-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,18 s | [A6](roh/NAK-312-be37d10-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,30 s | [A7](roh/NAK-312-be37d10-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,48 s | [A8](roh/NAK-312-be37d10-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,60 s | [A9](roh/NAK-312-be37d10-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,94 s | [A10](roh/NAK-312-be37d10-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,31 s | [A11](roh/NAK-312-be37d10-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,36 s | [A12](roh/NAK-312-be37d10-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,25 s | [A13](roh/NAK-312-be37d10-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 11,42 s | [A14](roh/NAK-312-be37d10-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,55 s | [A17](roh/NAK-312-be37d10-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 40,52 s | [A18](roh/NAK-312-be37d10-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,17 s | [A19](roh/NAK-312-be37d10-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,46 s | [A20](roh/NAK-312-be37d10-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,65 s | [A21](roh/NAK-312-be37d10-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,19 s | [A22](roh/NAK-312-be37d10-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,72 s | [A24](roh/NAK-312-be37d10-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,18 s | [A34](roh/NAK-312-be37d10-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 36,78 s | [A35](roh/NAK-312-be37d10-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 7,50 s | [A36](roh/NAK-312-be37d10-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,17 s | [A28](roh/NAK-312-be37d10-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,16 s | [A29](roh/NAK-312-be37d10-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,15 s | [A30](roh/NAK-312-be37d10-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 107,19 s | [A31](roh/NAK-312-be37d10-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,25 s | [A25](roh/NAK-312-be37d10-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,15 s | [A26](roh/NAK-312-be37d10-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,15 s | [A27](roh/NAK-312-be37d10-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,69 s | [A23](roh/NAK-312-be37d10-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,62 s | [A16](roh/NAK-312-be37d10-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,09 s | [B1](roh/NAK-312-be37d10-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. Seit NAK-312 Etappe 5 (312/M-49): die eingefrorenen Statebytes des Parametergoldens vom Basis-SHA 12300f1e passen zu ihrer Zeile im Golden (Laenge und SHA-256), laden normal und kommen aus dem Schreiber bytegleich zurueck. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,53 s | [B2](roh/NAK-312-be37d10-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,09 s | [B3](roh/NAK-312-be37d10-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,12 s | [B3b](roh/NAK-312-be37d10-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,52 s | [B3c](roh/NAK-312-be37d10-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,44 s | [B4](roh/NAK-312-be37d10-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,25 s | [B9](roh/NAK-312-be37d10-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,95 s | [B5](roh/NAK-312-be37d10-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,58 s | [B6](roh/NAK-312-be37d10-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, zaehlt sie seit Etappe 5 ueber 200 Bloecke je Aufruf setNonRealtime (true) wie je Aufruf setNonRealtime (false) 0 gemeldete Sperren (bis Etappe 4 je Aufruf setNonRealtime (true) genau eine). Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist bitgleich zu einem mit Takt nach jedem Block - fuer alle vier in beiden Staenden (neutral und mit Band) bei Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline; fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (dort wirkt der Wert im Block seines Randes), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. Seit der Nacharbeit 1 der Etappe 3 (L-1, R-312-16) nimmt der Blockrand nach einem read-only-Ladestart keinen Hostwert: laedt ein Stand mit eq an und Output-Trim +3 dB das fremde Major und kommen danach Input-Trim +24 dB, Output-Trim +24 dB, Width 2 und Mix 0 als Hostwerte, ist der Ausgang ab dem ersten Sample nach dem Laden bitgleich zu dem ohne diese Hostwerte, und die Blockrandziele steigen um 0 - bei Bloecken zu 256 und, wenn beim Laden ein Uebergang laeuft, bei 185 und 512; ein eigener schreibbarer Stand danach laesst Output-Trim +6 dB ohne Kontrolltakt wieder am Blockrand wirken (bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang am Ende bei 10^(6/20)). Seit NAK-312 Etappe 4 (312/M-38): ueber mindestens 4000 Bloecke wechselnder Groesse, waehrend beide v3-Clients der Sonde ueber einen Testserver auf der Probe-Pipe verbunden sind und ihre Provider nachweislich durch die Besitzschleuse laufen, bleiben RtWache::sperren() und RtWache::allokationen() ab Callback-Eintritt 0. Seit NAK-312 Etappe 5 (T3-01-03, T3-01-04, R-312-3) der Offline-Uebergang: mit hoerbarer Vorschau zaehlt die Wache im Bereich wie der Wrapper ueber 200 Bloecke 0 Sperren; unmittelbar nach dem ersten Aufruf setNonRealtime (true) ist die gewuenschte Hoermatrix Processed, und der Ausgang ist ab Sample 0 bitgleich zum Lauf ohne Vorschau - bei Blockgroesse 1, 64 und 256 und auch, wenn beim Wechsel ein Hoermatrix-Fade in Gegenrichtung oder ein Hoerhalt laeuft; nach einem ausdruecklichen Kontrolltakt ist keine Vorschau mehr aktiv, nach ceil(kFadeSamples/Blockgroesse)+1 Bloecken und einem zweiten Takt die Candidate-Bank frei; der Rueckweg in die Echtzeit blendet weich (Nachbarsprung hoechstens Fadeschritt plus 2^-23) und belebt die Vorschau nicht, und ohne Vorschau aendert das Offlineflag am Ausgang nichts. Seit dem zweiten Aenderungssatz der Etappe 5 (T3-01-02, R-312-4) haengen die 112 Hostparameter ohne APVTS direkt am Prozessor: Zahl, Reihenfolge, IDs, Namen, Klassen, Bereiche, Defaults und Hostsicht (convertTo0to1 an Minimum, Default und Maximum; convertFrom0to1, getText und getValueForText an 0, 0,37 und 1), die Statebytes ueber speichern, laden, speichern und die Epochenfolge einer Automationsfahrt sind gleich dem Golden des Basis-SHA 12300f1e; kein Quelltext unter eq-copilot/plugin/sonde/ nennt die APVTS-Klasse; nach dem Laden traegt jeder Hostparameter den geladenen Wert; am Ende des Destruktorrumpfs erreicht ein Hostwert den Prozessor nicht mehr; Index -1 und 112 bleiben ohne Wirkung, und der eigene Abgleich meldet kein Hostereignis; ueber 4000 Bloecke mit Hostautomation aus einem zweiten Thread zaehlt die Wache ab processBlock 0 Sperren und 0 Allokationen - die Sperre, die JUCE vor dem Plugincode in sendValueChangedMessageToListeners nimmt, sieht sie bauartbedingt nicht. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 1,86 s | [B7](roh/NAK-312-be37d10-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,76 s | [B10](roh/NAK-312-be37d10-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,00 s | [B11](roh/NAK-312-be37d10-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,62 s | [B12](roh/NAK-312-be37d10-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. Seit Etappe 4 (R-312-7, R-312-12) die Besitzschleuse der Sonde, gemessen an einem gehaltenen Statusprovider und einer gezaehlten Marke vor dem ersten Besitzerzugriff: nach dem Ende des Eigentuemers erreicht ihn kein Provider (0 in jedem Fall und in 20 Gegenzyklen, nach denen Handles und Threads nicht hoeher stehen als nach dem ersten); gehalten hinter dem Betreten wird er zu Ende gewartet, gehalten davor und nach dem Destruktor freigegeben abgewiesen, vor dem Abbau freigegeben laeuft er durch; stop() loest den gehaltenen ControlClient nach der 2-s-Frist ab, controlV3.stop() gemessen vor dem Schliessen, telemetryV3.stop() nach Zeilenfolge; ein Reconnect laesst dieselbe offene Schleuse stehen. Eine Sonde aus dem Produktkonstruktor traegt im Testbau genau pipeNameV3 (aktuelleLogonSid()), ohne dass ein Client startet. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 71,16 s | [B31](roh/NAK-312-be37d10-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,22 s | [B13](roh/NAK-312-be37d10-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,44 s | [B14](roh/NAK-312-be37d10-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. Seit NAK-312 Etappe 4 (R-312-2) faehrt dasselbe Bein das Messpunkt-Popover am echten Editor: baut die Nachrichtenschleife es erst nach Editor und Prozessor ab, fasst sein Destruktor keinen von beiden an (gezaehlte Marke 0, Bindung bis zum Prozessorende unveraendert, 0 Host-Dirty); schliesst es, solange der Editor lebt, wird eine Aenderung mit genau einer Host-Dirty-Meldung uebernommen, ohne Aenderung mit keiner. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,94 s | [B15](roh/NAK-312-be37d10-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-312-be37d10-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,46 s | [B22](roh/NAK-312-be37d10-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,73 s | [B23](roh/NAK-312-be37d10-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,12 s | [B24](roh/NAK-312-be37d10-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-312-be37d10-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-312-be37d10-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,08 s | [B21](roh/NAK-312-be37d10-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,99 s | [B19](roh/NAK-312-be37d10-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,38 s | [B17](roh/NAK-312-be37d10-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 6,16 s | [B18](roh/NAK-312-be37d10-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,95 s | [B16](roh/NAK-312-be37d10-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,49 s | [B27](roh/NAK-312-be37d10-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,88 s | [B29](roh/NAK-312-be37d10-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,76 s | [B30](roh/NAK-312-be37d10-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,16 s | [B28](roh/NAK-312-be37d10-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,31 s | [B8](roh/NAK-312-be37d10-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 37,62 s | [A32](roh/NAK-312-be37d10-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 122,77 s | [A33](roh/NAK-312-be37d10-dirty.md#a33) |


## 30. Etappe 5 — Offline-Übergang und Parameteranbindung (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-5-auftrag.txt`; Spezifikation §1
(T3-01-02, T3-01-03, T3-01-04, W05), §2 (R-312-1, R-312-3, R-312-4), §5.0, §5.7,
§5.8, §6 Kopf, §6.1 (M-04 in der Fassung von E-312-10), §6.5, §6.6, §7.0, §7.1,
§7.5, §7.7, §9.2 (E-312-3, E-312-4), §11.3 (E-312-7, E-312-10, R-312-12), §14.4
(E-312-11, R-312-13, R-312-14), §29.3 (E-312-15). Uhrzeiten aus `Get-Date`,
Binaryzeitstempeln oder Commitzeit. Zeilenangaben gelten zum Endstand `be37d10a`,
soweit nicht „am Basis-SHA" dabeisteht. Fremde Matrixzeilen tragen ihr Präfix
(R-312-13).

### 30.1 Start, Basis-SHA, Kanon vorher

- Start nach dem Auftragscommit `12300f1e` (14:17:59): `git pull --ff-only` ohne
  Änderung; `git rev-parse HEAD` = `12300f1e429c38354f9d216d7ade395b71f71b70`
  (Basis-SHA der Etappe 5); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`. Erste gemessene Uhrzeit 14:29:37 (`Get-Date`).
- Der Bauplan gilt zum Stand `945ebfb1`; jede Zeilenangabe aus §6.5, §6.6 und
  §7.5 ist am Basis-SHA nachgelesen: `setNonRealtime` stand dort bei
  `eq-copilot/plugin/sonde/SondeProcessor.cpp:1541-1553` (Bauplan `:1321-1328`),
  `dspKontrollTakt` bei `:1748` (Bauplan `:1447`), die Abmeldeschleife bei
  `:326-327` (Bauplan `:221-222`), der Fadebeginn der Hörmatrix bei
  `eq-copilot/plugin/dsp/DspKern.cpp:1343-1350` (Bauplan `:1233-1239`), der
  B7-Fall `offline_render_nutzt_den_bestaetigten_zustand` bei
  `eq-copilot/plugin/tests/TransactionTestMain.cpp:2337-2355` (Bauplan
  `:2216-2234`), 312/M-04 bei `:3692-3726`.
- **Kanon vorher: geerbt.** `git diff --stat 553b70ae HEAD -- eq-copilot broker
  tools` zeigt allein `eq-copilot/install/nakama-installer-v1.json` (3 Zeilen,
  Nachhashen des Laufzeit-Arms, §28), keinen Pfad unter `eq-copilot/plugin`,
  `broker` oder `tools`. Nach der Regel aus §7.0 gilt der Kanon nachher der
  Etappe 4, GRUEN 70/70 am Stand `553b70ae`
  (`docs/beweise/roh/NAK-312-553b70a-dirty.md`, §27.10).

### 30.2 Das Parametergolden am Basis-SHA (vor jeder Produktänderung)

Rohdatei `docs/beweise/roh/NAK-312-parameter-golden-erzeugung.txt`. Der
Erzeuger (`bitsVon`, `sha256Von`, `parameterGoldenText`, `zustandsGolden`,
`parameterGolden` und das Argument `--parameter-golden <text> <bin>` in `main`
von B7) wurde zuerst allein in die Testdatei geschrieben, B7 gegen die
unveränderten Produktquellen von `12300f1e` gebaut (Bau 14:33:55 bis 14:34:52,
SHA-256 aller vier Produktdateien gleich HEAD in der Rohdatei) und um 14:34:59
gefahren. Danach ging die Testdatei bytegleich auf HEAD zurück (SHA-256
`18F63AA9…93A3` vorher und nachher); der zweite Änderungssatz hat denselben
Erzeuger wortgleich wieder eingesetzt (Diff der eingesetzten Zeilen gegen den
Diff des Erzeugungslaufs: identisch, gemessen). Ergebnis:
`docs/beweise/roh/NAK-312-parameter-golden.txt` (29 665 Byte) und
`docs/beweise/roh/NAK-312-parameter-golden-s1.bin` (5 745 Byte); dreimal neu
erzeugt, jedes Mal bytegleich (Text SHA-256 `98D29988…7BEF`, Bin `4EA7C41E…7810`).

Inhalt: (a) Zahl 112, Gruppen 0, je Parameter Index, `getParameterIndex`,
`paramID`, Name, Klasse samt Auswahlliste, Versionshinweis, `NormalisableRange`
(start, end, interval, skew, symmetricSkew), Default normiert und denormiert,
Wert nach der Konstruktion, Schrittzahl, diskret, boolesch, automatisierbar,
Meta, Kategorie, Einheit; (b) die Hostsicht — `convertTo0to1` an Minimum,
Default und Maximum, `convertFrom0to1`, `getText` und `getValueForText` an 0,
0,37 und 1 —, jede Zahl als Bitmuster; (c) ein Stand mit festem `instance_id`
aus dem Schreiber, zwei Commits (Slots 0, 2, 5 belegt, Output-Trim −1,5 dB),
eine Automationsfahrt unter der Taktsperre (20 Hostpunkte auf Band-0-Gain und
Output-Trim, 30 Ruheblöcke, jeder Takt ausdrücklich), Laden in eine zweite
Instanz: s0, s1, s1 nach der Fahrt und s2 mit Länge und SHA-256, Revision und
`state_hash` beider Instanzen, Host-Dirty 2, Epochenfolge 0, 1 (44-mal), 2
(sechsmal). Am Basis-SHA: s1 = s1 nach der Fahrt = s2.

**Ort, begründet (A-48):** `docs/beweise/roh/` ist der einzige Ticketpfad, den
§7.1 und E-312-3 für ein Golden decken; `eq-copilot/fixtures/state/` ist nach
dem Auftrag nicht anzufassen. Die Textdatei trägt LF, die Tests lesen sie
zeilenendenfest (ein anderer Rechner darf sie mit CRLF auschecken); die Bin-Datei
fällt unter `*.bin binary`.

### 30.3 Gebaut — erster Änderungssatz (Offline-Übergang; T3-01-03, T3-01-04)

Commit `19ddf5d3` (14:54:14), gepusht.

- `eq-copilot/plugin/sonde/SondeProcessor.cpp:1560-1594`, `setNonRealtime`: nach
  `juce::AudioProcessor::setNonRealtime` die **Wechselerkennung**
  (`offlineGesehen`, erst eine Lesung, dann der Tausch — genau ein Aufruf führt
  den Wechsel aus); bei gleichem Wert sofortige Rückkehr, keine Sperre. Der
  **Wechsel nach offline** setzt den Hörwunsch auf Processed, dann über
  `dspKern->setzeOfflineRiegel (true)` Riegel und Anforderung des harten
  Schaltens, zuletzt `vorschauEndeAngefordert`. Der **Rückweg** löst den Riegel
  und nimmt eine noch nicht verbrauchte Anforderung zurück. Kein Schloss, keine
  Allokation, kein `beendeCandidate` im Callback (D3 berichtigt, E-312-7).
- `SondeProcessor.cpp:1801-1810`, `dspKontrollTakt`: nach `pflege()` verbraucht
  der Takt `vorschauEndeAngefordert` unter dem Zustandsschloss, das er ohnehin
  hält, und ruft `transaktion->beendePreview()` → `beendeCandidate` wie bisher.
- `eq-copilot/plugin/dsp/DspKern.h:340-352` und `:742-743`:
  `setzeOfflineRiegel (bool)` und die zwei ausdrücklich geteilten Atomics
  `offlineRiegelAn` und `hartSchalten`; `DspKern.cpp:1211-1236`, in
  `verarbeiteStueck`: die Anforderung wird zuerst gelesen (acquire) und vom
  Audiothread verbraucht, der Riegel macht jeden Hörwunsch zu Processed, und bei
  verbrauchter Anforderung steht die Hörmatrix ab dem ersten Sample des Stücks
  ohne Fade auf dem wirksamen Stand (`hoerVorher = hoerLaufend = wirksam`,
  `hoerFadeRest = 0`) — ein laufender Fade in jede Richtung endet mit, ein
  Hörhalt endet am Ende desselben Stücks über `beendeHoerHalt`.
- Die drei irreführenden Kommentare (§1, §7.5) berichtigt:
  `SondeProcessor.h:620-623` (`zustandSchloss`: Nachrichten-, Host- und
  Workerthread, nie im Audio-Callback — weder `processBlock` noch
  `setNonRealtime`), `SondeProcessor.cpp:1412-1418` (die Ordnung der zwei
  Sperren statt der falschen Tatsache) und `SondeProcessor.h:214-227` (der
  Wechsel ist ein Schnitt, die Buchhaltung folgt im Takt). Dazu nach R-312-14:
  Klassenkopf der Sonde (`setNonRealtime` fasst nur Atomics an), die Doku zu
  `setzeHoermatrix`, `dspKontrollTakt`, `wirksameHoermatrix` und der
  M-55-Kommentar in `verarbeiteStueck`.
- Der Schlossadapter `GemeldetesSchloss` (Etappe 2) ist entfernt: nach dem Fix
  benutzt ihn keine Zeile mehr (A-54).
- `eq-copilot/plugin/tests/TransactionTestMain.cpp`: der Fall
  `offline_render_nutzt_den_bestaetigten_zustand` (Abschnitt O, `:2540-2573`)
  nach §7.5 umgebaut — die acht Verwurfsblöcke fallen weg, geprüft werden die
  sofortige Hörmatrix und Bitgleichheit ab Sample 0, `previewAktiv()` erst nach
  einem ausdrücklichen `kontrollTaktFuerTest()` (M-83-Teil); 312/M-04 auf 0
  umgestellt (`:3909-3949`); neuer Abschnitt ZA `nak312OfflineUebergang`
  (`:5390-5639`, Hilfen davor) mit 312/M-40, M-41, M-83, M-46 samt Teilfall Hörhalt, dem
  M-42-Teilfall Fade in Gegenrichtung, M-43, M-44, M-45 und M-47; Kopfkommentar
  nachgezogen.
- `tools/beweise.ps1`: B7 zum Offlineflag je Aufruf (Zusatz 1) und um den
  Offline-Übergang ergänzt; B31 nach E-312-15 (Zusatz 2, §30.10).
- Nicht angefasst: der Wrapper und sein Patch, `prepareToPlay` und
  `releaseResources` der Sonde, die Hörmatrix in der Echtzeitrichtung,
  Parameter, Schemas, Fixtures, Identität, `design/`, `docs/offene-punkte.md`,
  `docs/plugin-wissen.md`.

### 30.4 Gegenprobe am Basisstand (heute rot) — erster Änderungssatz

`docs/beweise/roh/NAK-312-etappe5a-gegenprobe.txt`, Lauf 14:39:25 bis 14:39:28.
Produktquellen unverändert auf `12300f1e` (SHA-256 je Datei, „gleich HEAD"),
die Tests in ihrer Endfassung (SHA-256 `04CA4559…B8B1`, am Commit `19ddf5d3`
unverändert). B7 Exit 1, 240 geprüft, **9 Fehler — genau die „heute
rot"-Zeilen**: der umgebaute M-120-Fall (312/M-42, größte Abweichung der ersten
256 Samples 0,323985), 312/M-04 zweite Hälfte (200 Sperren), 312/M-40 (200
Sperren), 312/M-42 im Bereich, der M-42-Teilfall Fade in Gegenrichtung, der
Ausgang des M-46-Teilfalls Hörhalt und 312/M-47 bei 1, 64 und 256 (das erste
Sample nach dem Flag ist das des Zwillings ohne Flag). Grün, wie ihre Etiketten
sagen: 312/M-41, M-43, M-44, M-45 (Regressionswachen) und M-83, M-46 Hauptfall
(heute nicht messbar — die synchrone Vorschaubeendigung erfüllt sie heute auch).

### 30.5 Matrixzeilen des ersten Änderungssatzes

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-04 | `312/M-04 offlineflag_nimmt_keine_gemeldete_sperre_mehr`, B7 | grün: 0 Sperren je Aufruf `setNonRealtime (true)` wie `(false)` über 200 Blöcke | zwei Hälften nach E-312-10: die Etappe-5-Hälfte hat keine eigene Mutation; am Basisstand rot (200, Gegenprobe), unter der Mutation von M-40 rot (Teil a 200, Teil b 1); `docs/beweise/roh/NAK-312-rot-M-40.txt` |
| M-40 | `312/M-40 offlineflag_nimmt_keine_sperre`, B7 | grün: Candidate vorher hörbar, `RtWache::sperren()` 0 über 200 Blöcke im Bereich wie der Wrapper | heute rot (200); Teil a Sperre unbedingt → 200 → rot, Teil b Sperre im Wechselpfad → 1 → rot (A-51); `docs/beweise/roh/NAK-312-rot-M-40.txt` |
| M-41 | `312/M-41 der_erste_aufruf_stellt_die_hoermatrix_sofort_um` (Teilfall von 312/M-40), B7 | grün: gewünscht sofort Processed, wirksam nach dem ersten Block Processed | Regressionswache; Wechselerkennung schluckt den ersten Aufruf (Anfangswert `true`) → rot; `docs/beweise/roh/NAK-312-rot-M-41.txt` |
| M-83 | `312/M-83 vorschauende_wird_im_kontrolltakt_verbucht`, B7; dazu der M-83-Teil im umgebauten M-120-Fall | grün: nach dem ersten ausdrücklichen Takt `previewAktiv()` false, nach 2 Blöcken und dem zweiten Takt Slot 1 frei | heute nicht messbar; Atomic im Takt nicht verbraucht → `previewAktiv()` true, Slot belegt → rot; `docs/beweise/roh/NAK-312-rot-M-83.txt` |
| M-42 | `offline_render_nutzt_den_bestaetigten_zustand` (umgebaut, Abschnitt O), dazu `312/M-42 im Bereich wie der Wrapper` und der Teilfall `fade_in_der_gegenrichtung`, B7 | grün: sofort Processed, 100 Blöcke bitgleich ab Sample 0, max. Abweichung der ersten 256 Samples 0; im Bereich 200 Blöcke bitgleich; laufender Fade Processed → Candidate endet hart | heute rot (0,323985); `kFadeSamples` statt 0 im harten Zweig → rot; `docs/beweise/roh/NAK-312-rot-M-42.txt` |
| M-43 | `312/M-43 der_rueckweg_blendet_weich`, B7; [SONDE-015] M-55 in B6 | grün: erster Sprung 0,00116631389, größter 0,00116634369 bei Fadeschritt 0,00116632303 + 2^−23, Ende 0,598578691 | Regressionswache; hartes Schalten unbedingt → Sprung 0,29857868 → rot, in B6 `hoermatrixwechsel_ist_klickfrei` und sieben Hörmatrixfälle rot; `docs/beweise/roh/NAK-312-rot-M-43.txt` |
| M-44 | `312/M-44 offline_hin_und_zurueck_belebt_die_vorschau_nicht`, B7 | grün: nach Offline an, Takt, aus, Takt keine Vorschau, Processed, Echtzeit bitgleich; zweites Offline bitgleich | Regressionswache; Vorschau im Takt gemerkt und beim Rückweg neu gesetzt → rot; `docs/beweise/roh/NAK-312-rot-M-44.txt` |
| M-45 | `312/M-45 ohne_vorschau_ab_sample_0_bitgleich`, B7 | grün: ab Sample 0 bitgleich zum Lauf ohne Offlineflag | Regressionswache; harter Zweig lässt einen Mischstand mit Dry stehen → rot; `docs/beweise/roh/NAK-312-rot-M-45.txt` |
| M-46 | `312/M-46 keine_bank_bleibt_haengen` und Teilfall `hoerhalt_beim_schalten`, B7 | grün: Slot frei, Candidate aktiv/Quelle −1/−1; mit laufendem Hörhalt (64 von 256 Samples) ab Sample 0 bitgleich, Bank nach Takt frei | heute nicht messbar; Mutation wie M-83 → Slot belegt → rot; `docs/beweise/roh/NAK-312-rot-M-46.txt` |
| M-47 | `312/M-47 blockgroesse_1`, `_64`, `_256`, B7 | grün je Blockgröße: letztes Sample davor gleich dem Zwilling und noch Candidate, erstes danach bestätigt und ungleich dem Zwilling | heute rot bei jeder Blockgröße; Mutation wie M-42 → rot; `docs/beweise/roh/NAK-312-rot-M-47.txt` |

### 30.6 Gebaut — zweiter Änderungssatz (Parameteranbindung; T3-01-02)

Commit `be37d10a` (15:10:09), gepusht. **Parameteranbindung: UMGEBAUT** — der
Identitätsbeweis ist in allen drei Teilen bytegleich (§30.8).

- `SondeProcessor.cpp:84-126`, `hostParameterAnlegen`: dieselbe Schleife über
  `parameter::tabelle()` wie das frühere `baueParameterLayout`, dieselben
  Klassen (`AudioParameterBool`, `AudioParameterFloat` mit denselben
  `NormalisableRange` samt Skew, `AudioParameterChoice`), dieselben
  `ParameterID { b.id, 1 }`, Namen und Defaults, in derselben Reihenfolge —
  aber über `addParameter` direkt am Prozessor. Aufgerufen als erste Anweisung
  des Konstruktorrumpfs (`:239-243`); die APVTS legte sie zuvor in der
  Initialisierungsliste an, dazwischen liegt nur die Vorgabeinitialisierung der
  übrigen Mitglieder. Das Mitglied `parameterBaum` ist entfernt; damit entfällt
  der Adapter je Parameter mit seiner Hörer-Sperre und der 10-Hz-Timer der
  APVTS, der Werte in einen nie gespeicherten Baum spiegelte.
- `SondeProcessor.h`: Klassenkopf und Mitgliedskommentar nachgezogen (kein
  Quelltext unter `eq-copilot/plugin/sonde/` nennt die APVTS-Klasse mehr);
  Testzugänge `hostEreignisFuerTest`, `hostEreignisOffenFuerTest`,
  `parameterValueChangedFuerTest` unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`
  (`:450-457`).
- B7: der Erzeuger aus §30.2 wortgleich; Abschnitt ZB `nak312Parameteranbindung`
  (`:5692-5890`) mit 312/M-48, M-49, M-50, M-52, M-53, M-54; 312/M-51 als
  Teilfall von `reload_rekonstruiert_denselben_audioausgang` (Abschnitt O);
  der Prüfname `apvts_fuehrt_112_parameter_in_vertragsreihenfolge` heißt jetzt
  `prozessor_fuehrt_112_parameter_in_vertragsreihenfolge` (A-58). B2
  (`eq-copilot/plugin/tests/StateMigrationTestMain.cpp`): Abschnitt 312/M-49
  über die eingefrorenen Statebytes; Kopfkommentare beider Beine nachgezogen.
- `tools/beweise.ps1`: B7 und B2 um genau diese Messungen ergänzt; die
  B7-Zeile nennt die Grenze (die Sperre, die JUCE vor dem Plugincode in
  `sendValueChangedMessageToListeners` nimmt, sieht der Zähler nicht).
- Nicht angefasst: Parameter-IDs, Reihenfolge, Bereiche, Defaults, Class-IDs,
  `getBypassParameter` (bleibt unüberschrieben, §7.7), `getStateInformation`,
  der Wrapper, `eq-copilot/plugin/core/`, Schemas, Fixtures, Broker, Installer.

### 30.7 Gegenprobe vor dem Umbau (heute rot) — zweiter Änderungssatz

`docs/beweise/roh/NAK-312-etappe5b-gegenprobe.txt`, Lauf 14:59:31 bis 14:59:35:
Produkt = `19ddf5d3` plus allein die drei Testzugänge, Tests in der Endfassung.
B7 Exit 1, 247 geprüft, **1 Fehler — genau 312/M-50** (SondeProcessor.cpp und
SondeProcessor.h nennen die APVTS-Klasse). Grün: 312/M-48 und M-49 — das Golden
des Basis-SHA gilt auch nach dem ersten Änderungssatz, der die Parameter nicht
berührt —, 312/M-51 bis M-54. B2 Exit 0, 294 Prüfungen.

### 30.8 Der Identitätsbeweis und die Matrixzeilen des zweiten Änderungssatzes

Am Endstand, gemessen: **(a)** die 114 Parameterzeilen des Goldens (Zahl,
Gruppen, 112 Parameter) sind Zeile für Zeile und Bit für Bit gleich; **(b)**
dieselben Zeilen tragen die Hostsicht an je drei Stützstellen (`convertTo0to1`
an Minimum, Default, Maximum; `convertFrom0to1`, `getText`, `getValueForText`
an 0, 0,37, 1) — gleich; **(c)** die 9 Zustandszeilen (s0, s1, s1 nach der
Fahrt, s2, Commits, Revision, `state_hash`, Dirty, Epochenfolge) sind gleich,
und s1 ist bytegleich zur eingefrorenen Bin-Datei; B2 lädt diese Bytes normal
und schreibt sie bytegleich zurück. B1 misst das neu gebaute Bundle: 120/0.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-48 | `312/M-48 hostparameter_gleich_dem_golden_des_basis_sha`, B7 | grün: 114 Zeilen gegen 114 im Golden, gleich | Regressionswache; Platz 1 und 2 vertauscht → Zeile 4 weicht ab → rot; `docs/beweise/roh/NAK-312-rot-M-48.txt` |
| M-49 | `312/M-49 statebytes_und_epochenfolge_gleich_dem_golden`, B7; `312/M-49 (B2)` und `312/M-49 statebytes_des_basis_sha_reisen_bytegleich`, B2 | grün: 9 Zeilen gleich, s1 5 745 Byte bytegleich zur Datei; B2 lädt normal und schreibt bytegleich | Regressionswache in vier Teilen (A-59): a Matrixmutation → M-49 grün, M-48 rot; b ohne Herkunftstag → M-49 grün, 312/M-54 rot; c `getStateInformation` ohne Kernhälfte → s1 3 537 Byte → rot; d ohne `addListener` → Epochen 0 durchgehend → rot; `docs/beweise/roh/NAK-312-rot-M-49.txt` |
| M-50 | `312/M-50 kein_apvts_mehr_in_der_sonde`, B7 | grün: 3 Dateien, kein Treffer | heute rot (Gegenprobe); Typalias auf die APVTS-Klasse in SondeProcessor.h → Treffer → rot; `docs/beweise/roh/NAK-312-rot-M-50.txt` |
| M-51 | `312/M-51 hostparameter_spiegeln_den_geladenen_stand` (Teilfall von `reload_rekonstruiert_denselben_audioausgang`), B7 | grün: 112 von 112 gleich, 9 nicht auf dem Default | Regressionswache; Abgleich im Ladeweg entfernt → 103 von 112 → rot; `docs/beweise/roh/NAK-312-rot-M-51.txt` |
| M-52 | `312/M-52 listener_paarweise_an_und_ab` (Teilfall von 312/M-50), B7 | grün: vorher erreicht ja, am Ende des Destruktorrumpfs Zähler 112, Mailbox unverändert, nicht offen | Regressionswache; Abmeldeschleife entfernt → Mailbox 1 → 113 → rot; `docs/beweise/roh/NAK-312-rot-M-52.txt` |
| M-53 | `312/M-53 parameteranbindung_ist_echtzeitfest`, B7 | grün: 0 Sperren, 0 Allokationen, eigener Zähler 0 über 4000 Blöcke bei 195 723 Hostschreibrunden eines zweiten Threads (Einzellauf 15:09:49) | Regressionswache; gemeldete Sperre im Parameterweg am Blockrand → 16 000 → rot; `docs/beweise/roh/NAK-312-rot-M-53.txt` |
| M-54 | `312/M-54 parameterindex_rand_und_abgleichtiefe` (Teilfall von 312/M-50), B7 | grün: −1 und 112 ohne Wirkung, 0 und 111 je +1, Abgleich ohne Ereignis, Regler folgt | Regressionswache; Indexwache faltet statt abzuweisen (ohne UB, A-60) → rot; `docs/beweise/roh/NAK-312-rot-M-54.txt` |

### 30.9 Einzelbeine

Je Änderungssatz, Binaries jünger als jede ihrer Quellen (NAK-230):

- Erster Änderungssatz (nach dem letzten Rotbeweis neu gebaut, 14:53:28 bis
  14:53:46): B7 240 geprüft, 0 Fehler; B6 389 geprüft, 0 Fehler; A16 131
  Prüfungen ok. Dazu B11 21 bestanden, B12 4 bestanden; das Bundle
  `NakamaProbeeq_VST3`, B31, `EqCopBriefkastenTest` und `EqCopSessionSoak`
  gebaut (Exit 0). Clang-tidy-Teilmessung (`tools/plan/tidy.py --nur`):
  `SondeProcessor` 14 Fundstellen, alle in `eq-copilot/plugin/core/analysis/`
  (keine in einer Datei der Etappe), `dsp/DspKern` 0.
- Zweiter Änderungssatz (15:09:06 bis 15:09:51): B7 247 geprüft, 0 Fehler; B2
  294 Prüfungen ok; A16 131 Prüfungen ok; B1 120 Prüfungen ok — B1 und B2
  übersetzen `SondeProcessor.cpp` nicht, ihre Binaries (15:01:44, 14:59:25) sind
  jünger als ihre eigenen Quellen; B1 misst das neu gebaute Bundle (Bau
  15:09:06 bis 15:09:49). Clang-tidy-Teilmessung `SondeProcessor` unverändert
  14 Fundstellen außerhalb der Etappe.

### 30.10 Die zwei Zusätze des Dirigenten

1. **Register NAK-345 Punkt 1:** die B7-Behauptung zum Offlineflag sagt jetzt
   „zählt sie seit Etappe 5 über 200 Blöcke je Aufruf setNonRealtime (true) wie
   je Aufruf setNonRealtime (false) 0 gemeldete Sperren (bis Etappe 4 je Aufruf
   setNonRealtime (true) genau eine)" — genau das, was 312/M-04 und 312/M-40
   messen.
2. **E-312-15 (§29.3):** die B31-Behauptung sagt statt „beide stop() liegen vor
   dem Schliessen" jetzt „controlV3.stop() gemessen vor dem Schliessen,
   telemetryV3.stop() nach Zeilenfolge"; der Test ist unverändert.

### 30.11 Abweichungen vom Bauplan, begründet

- **A-48 — Ort des Goldens.** §30.2.
- **A-49 — Ort der Fälle.** M-40 steht mit den Teilfällen M-41, M-43 bis M-47
  und M-83 im neuen Abschnitt ZA statt unmittelbar neben dem M-120-Block in
  Abschnitt O; dort steht der umgebaute Fall selbst (M-42). Die Fälle brauchen
  eigene Aufbauten (Zwilling, Gleichanteil, Blockgrößen) und eine gemeinsame
  Hilfe; ein Abschnitt hält sie zusammen. Die Bitgleichheit ab Sample 0, die
  M-41 der Zeile M-42 zuweist, misst der Fall 312/M-40 zusätzlich unter dem
  Namen `312/M-42 im Bereich wie der Wrapper`.
- **A-50 — Aufbau von M-43.** Statt der Band-Vorschau von M-42 fährt M-43
  einen Gleichanteil 0,3 mit neutralem bestätigtem Satz und einer Vorschau
  Output-Trim +6 dB: so sind beide Seiten konstant, und der Nachbarsprung nach
  [SONDE-015] E-31 ist exakt gegen die Fadeschrittweite prüfbar. Die „Rückkehr
  zu Candidate" braucht eine neue Vorschau, weil die erste mit dem Wechsel nach
  offline endet (M-44).
- **A-51 — M-40 in zwei Teilen.** Die Zeile M-40 sagt „die Sperre wieder
  unbedingt machen" (Teil a, 200), die Zeile M-04 „die Sperre im Wechselpfad"
  (Teil b, 1); beide gefahren, beide rot.
- **A-52 — M-44, Form der Mutation.** „Die Wechselerkennung beim Rückweg
  zurücksetzen und die Vorschau neu setzen": die Wechselerkennung setzt sich im
  gebauten Code beim Rückweg ohnehin zurück (`offlineGesehen` wird false); die
  Mutation merkt sich deshalb die Vorschau im Takt vor `beendePreview` und setzt
  sie beim Rückweg mit Hörwunsch Candidate neu (drei Stellen derselben Datei).
- **A-53 — Riegel und Anforderung in einer Kernfunktion.** §7.5 lässt
  `setNonRealtime` die zwei Atomics setzen; gebaut ist
  `DspKern::setzeOfflineRiegel (bool)`, die beide in fester Ordnung schreibt
  (Riegel vor Anforderung; beim Rückweg Anforderung vor Riegel), weil der
  Audiothread die Anforderung zuerst liest und dann Riegel und Wunsch sehen muss.
  Zusätzlich nimmt der Rückweg eine nicht verbrauchte Anforderung zurück — sonst
  schnitte der erste Echtzeitblock nach einem Offlinefenster ohne Block hart
  (gegen [SONDE-015] M-55). Die Anforderung verbraucht allein der Audiothread,
  wie §7.5 verlangt.
- **A-54 — der Schlossadapter der Etappe 2 ist entfernt.** Nach dem Fix nimmt
  keine Zeile mehr `zustandSchloss` über `GemeldetesSchloss`; ein unbenutzter
  Adapter wäre toter Code. Die Mutationen von M-40 bringen ihn als lokalen
  Adapter mit.
- **A-55 — M-04, Prüfname.** Der Name
  `offlineflag_nimmt_eine_gemeldete_sperre_je_block` hätte nach der Umstellung
  das Gegenteil dessen gesagt, was der Fall prüft; er heißt jetzt
  `offlineflag_nimmt_keine_gemeldete_sperre_mehr` und nennt den alten Namen in
  Klammern. Kennung 312/M-04 unverändert.
- **A-56 — zwei Rohdateien aus einem Lauf.** M-83 und M-46 teilen die Mutation
  („das Atomic im Kontrolltakt nicht verbrauchen"), M-42 und M-47 ebenso
  („Mutation wie M-42"): je ein Lauf, je Zeile eine Rohdatei mit demselben
  Protokoll. Die Bewertung jeder Rohdatei ist nach dem Lauf auf das Gemessene
  gefasst (Zeile „Bewertung (nach dem Lauf, gemessen)").
- **A-57 — das Golden entstand vor dem ersten Änderungssatz.** §7.1 verlangt es
  „am Basis-SHA der Etappe 5"; deshalb ist es vor jeder Produktänderung erzeugt
  und erst mit dem zweiten Änderungssatz committet (§30.2).
- **A-58 — Prüfname M-88.** `apvts_fuehrt_112_parameter_in_vertragsreihenfolge`
  wäre nach dem Umbau falsch (R-312-14); er heißt
  `prozessor_fuehrt_112_parameter_in_vertragsreihenfolge` und deutet den alten
  Namen in Klammern an („apvts_fuehrt_…"). Kein Runnertext und kein Test hängt am alten Namen (gemessen mit
  `git grep`); historische Rohausgaben (NAK-246), die Testspalte von
  [SONDE-015] M-88 und der Tiefenaudit zitieren ihn als Verlauf.
- **A-59 — M-49: die Matrixmutation ist kein Rotbeweis.** Gemessen (Teil a):
  Parameter ohne das Präfix v1./v2. lassen M-49 grün — die Statebytes schreibt
  `getStateInformation` aus dem bestätigten Zustand des Transaktionskerns
  (Kennungen aus `parameter::tabelle()`), und die Epochenfolge hängt an
  Hostereignis und Ruhegrenze, nicht an der Kennung; es fallen M-48 und die
  Kennungsfälle. Teil b (Herkunftstag entfernt) lässt M-49 ebenfalls grün: das
  Echo des eigenen Abgleichs ist der bestätigte Wert und ändert die Epochen der
  Fahrt nicht. Den Rotbeweis tragen deshalb die Zeilen der zwei Hälften: Teil c
  an `getStateInformation` (der Zeile, die die Reihenfolgespalte nennt) und
  Teil d an der Anmeldung `addListener` (die Anbindung, über die eine
  Automationsfahrt überhaupt Epochen erzeugt). Die Zusage der Zeile hält; B2
  misst den Schreiber und bleibt unter jedem Teil grün.
- **A-60 — M-54 ohne undefiniertes Verhalten.** „Die Indexwache entfernen"
  schriebe mit −1 und 112 außerhalb von `std::array` — ein Lauf in
  undefiniertem Verhalten (Grundsatz aus R-312-12 und dem Auftrag). Die Mutation
  faltet die Indizes in den Bereich (−1 → 111, 112 → 0): ungültige Indizes
  erreichen die Mailbox, ohne außerhalb des Feldes zu schreiben — rot an
  derselben Zusage.
- **A-61 — M-52, die Zählung.** „Die Zahl der angemeldeten Listener ist vor der
  Konstruktion und nach der Zerstörung gleich" ist so nicht messbar: JUCE hat
  keinen Zugang zur Listenerzahl, und die Parameter sterben mit dem Prozessor.
  Gemessen wird am Ende des Destruktorrumpfs (Haken aus Etappe 4, der Prozessor
  lebt, der Worker ist gejoint): ein Wert an jedem der 112 Parameter erreicht
  einen Zähler des Beins (112), aber keinen Listener des Prozessors (Mailbox
  unverändert, nicht offen); die Gegenprobe davor zeigt, dass derselbe Weg den
  lebenden Prozessor erreicht.
- **A-62 — M-53 ist ein neuer Fall.** Die Zeile nennt „B7 bestehend"; der
  nächste bestehende Fall 312/M-26 schreibt die Hostwerte auf demselben Thread.
  M-53 verlangt einen zweiten Thread; der Fall ist neu, die Behauptungszeile
  nennt die Grenze.
- **A-63 — M-51, das Soll.** Das erwartete Hostparameterbild rechnet der Test
  aus der Vertragszelle und der Hostsicht des Parameters selbst
  (`convertTo0to1`), nicht über die Produktfunktion `hostWertAus` (§7.1: kein
  Orakel aus dem Produkt).
- **A-64 — M-50, Form der Mutation.** Statt die APVTS samt Konstruktion
  zurückzubauen (das legte die Parameter doppelt an), holt ein Typalias die
  Klasse in den Sondenquelltext zurück; der Riegel ist textuell und misst genau
  diese Rückkehr.
- **A-65 — Git.** Der Push von `be37d10a` lief im selben Shellbefehl
  unmittelbar nach `git pull --rebase` („Current branch master is up to date"),
  nicht als eigener Schritt.

### 30.12 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Etappe 5

| Abschnitt | Punkt | Etappe 5 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: Offline an → Takt → aus → an (312/M-44), Rückweg ohne Wiederbelebung, Hörhalt beim Schalten endet und gibt die Bank frei (312/M-46); Listener am Ende des Destruktorrumpfs abgemeldet (312/M-52); Konstruktor legt die Parameter vor jeder Nutzung an |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | angewandt: Blockgröße 1, 64, 256 (312/M-47); Parameterindex −1, 0, 111, 112 (312/M-54); Hostsicht an 0, 0,37, 1 und an Minimum, Default, Maximum (312/M-48); kein Draht-, Schema- oder Statefeld berührt (s1 bytegleich) |
| D Bau- und Prüfriegel | fail-closed, Frische, was der Kanon nicht baut | angewandt: Textriegel M-50 fail-closed (Ordner fehlt oder weniger als 3 Dateien → rot); Golden fehlt → M-48 und M-49 rot; Binaryzeitstempel gegen die Quelle bei jedem Einzelbein, jedem Rotbeweis und beiden Gegenproben; kein neues Bein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: B7 nennt je Satz einen gemessenen Fall und die JUCE-Grenze; B2 genau M-49; B31 auf das Gemessene gefasst (Zusatz 2); die Offlineflag-Zeile „je Aufruf" (Zusatz 1) |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerbehauptungen B7, B2, B31; Kopfkommentare von B7, B2, der Sonde und des Kerns, die drei Kommentare aus §1 und die aus R-312-14; Manifestkopf (Etappe, Basis-SHA, Kanon) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: 16 Rohdateien `docs/beweise/roh/NAK-312-rot-M-40.txt` bis `NAK-312-rot-M-54.txt` und `NAK-312-rot-M-83.txt`, zwei Gegenproben; Ersatz und Abweichungen begründet (A-51, A-52, A-59, A-60, A-64) |
| E Behauptung ≤ Messung | Writer-Fixtures statt Handschrift | angewandt: das Golden gibt der Prozessor am Basis-SHA aus, dreimal bytegleich wiederholt; die Anfangsbytes s0 schreibt der Schreiber |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Zähler, Sprünge, Byteanzahlen und Uhrzeiten aus Läufen, `Get-Date` und Commitzeit |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: aktivieren↔abklingen (Offline an und aus, Vorschau und ihr Ende, Hörhalt und Bankfreigabe) in `19ddf5d3`; speichern↔laden (312/M-49, M-51) und verbinden↔trennen (312/M-52) in `be37d10a`; M-04 und der B7-Fall im selben Änderungssatz wie der Offline-Fix |

### 30.13 Selbstaudit

- **Welcher Thread welches Atomic.** `offlineGesehen`: nur `setNonRealtime`
  (VST3: Audio-Callback unter JUCEs Callback-Lock oder `setupProcessing` auf dem
  Hostthread), Lesung acquire, Tausch acq_rel. `offlineRiegelAn`: schreibt
  `setNonRealtime` (release), liest der Audiothread (acquire).
  `hartSchalten`: schreibt `setNonRealtime` (release, true beim Wechsel, false
  beim Rückweg), verbraucht allein der Audiothread (Lesung acquire, Tausch
  acq_rel). `hoerwunsch`: wie bisher (release/acquire).
  `vorschauEndeAngefordert`: schreibt der Wechsel im Callback (release),
  verbraucht der Takt unter dem Zustandsschloss (acquire, Tausch acq_rel).
  Ordnung: der Wechsel schreibt Wunsch, Riegel, Anforderung in dieser Folge; der
  Audiothread liest die Anforderung zuerst — wer sie sieht, sieht Wunsch und
  Riegel. **Der Audiothread gibt keine Bank frei und fasst den Merkzettel nicht
  an**: `beendeCandidate` läuft nur in `dspKontrollTakt` auf dem Worker; im
  Audiothread bleibt es bei `meldeAusgedient` in `beendeHoerHalt` wie bisher.
- **Offline an, aus, wieder an.** 312/M-44; ein Rückweg vor dem ersten
  Offlineblock nimmt die Anforderung zurück (A-53), ein angefordertes
  Vorschauende bleibt angefordert.
- **Ein laufender Fade in die andere Richtung beim harten Schalten.** Teilfall
  `fade_in_der_gegenrichtung` (Processed → Candidate, 64 von 256) und Teilfall
  `hoerhalt_beim_schalten` (Candidate → Processed mit Hörhalt): beide ab Sample 0
  bitgleich zum bestätigten Lauf.
- **Blockgröße 1.** 312/M-47 bei 1, 64, 256; der Kern verbraucht die Anforderung
  im ersten Stück, auch in einem stückelnden Überblock. Ein freigegebener Kern
  (`dryL` leer) verbraucht sie nicht; unter dem Riegel ist das spätere harte
  Schalten dann ein Schalten von Processed auf Processed.
- **NaN und ±Inf in der Hostsicht.** Keine neue Rechnung mit Hostwerten; die
  Parameterklassen sind dieselben (312/M-48 bitgleich), nicht endliche Hostwerte
  behandeln `zelleAusHost` und `zelleAmBlockrand` wie bisher (312/M-18, Teilfall
  zu 312/M-20, grün).
- **Parameterindex −1 und 112.** 312/M-54; der Testzugang
  `hostEreignisFuerTest` liefert außerhalb 0 bis 111 die 0 statt zu lesen.
- **Save/Load-Symmetrie und Host-Dirty.** s1 = s2 bytegleich zum Basis-SHA,
  Dirty 2 wie am Basis-SHA — die zwei Commits; das Laden von s0 in derselben
  Instanz meldet keines (312/M-49); nach dem Laden zeigt jeder Hostparameter
  den geladenen Wert (312/M-51).
- **Stale Closures in Parameter-Listenern.** Der Listener ist der Prozessor
  selbst; seine Abmeldung steht im Destruktor vor dem Workerjoin (312/M-52). Die
  Test-Lambdas fangen den rohen Zeiger nur bis zum Ende des Destruktorrumpfs;
  der Zähler des Beins lebt länger als die Parameter.
- **Behauptungszeile nicht weiter als der Test.** B7: jeder neue Satz ein
  gemessener Fall; „ab processBlock" und die JUCE-Grenze stehen darin. B2: nur
  M-49. Kein Satz behauptet einen Rotbeweis.
- **Kommentare, die der Änderungssatz falsch macht (R-312-14).** Berichtigt:
  die drei aus §1, Klassenkopf, `setzeHoermatrix`, `dspKontrollTakt`,
  `wirksameHoermatrix`, M-55-Kommentar, Mitgliedskommentar der Parameter,
  Kopfkommentare von B7 und B2, die Prüfnamen von M-04 und M-88. Außerhalb der
  Ticketpfade und deshalb Hinweis (§30.14): `eq-copilot/plugin/state/NakamaParameter.h:9-10`
  („die APVTS-Konstruktion (SONDE-015) braucht sie zur Bauzeit") und `:97`
  („ein APVTS-/FL-Automationsparameter").
- **Gesundheit.** A32 und A33 im Kanon (§30.15); Teilmessungen in §30.9.

### 30.14 Hinweise an den Dirigenten

- **Produktwirkung 1 — Vorschau während eines laufenden Offline-Renders.**
  Bisher beendete jeder Offlineblock eine Vorschau (die Sperre lief bei jedem
  Aufruf). Jetzt beendet nur der Wechsel nach offline die Vorschau. Setzt Gen
  WÄHREND eines laufenden Renders eine neue Vorschau, bleibt sie als
  Buchhaltung stehen, ist im Render nicht hörbar (Riegel) und wird nach dem
  Render hörbar, wenn der Hörwunsch dann Candidate ist (weicher Fade). Ob Gen
  das im Render überhaupt anbietet, ist nicht gemessen.
- **Produktwirkung 2 — der erste Offlineblock schneidet.** War beim Wechsel
  eine Vorschau hörbar (oder Dry oder Delta), springt der Ausgang am ersten
  Sample des Renders ohne Fade auf den bestätigten Zustand — so verlangen es
  R-312-3 und M-42; in Echtzeit blendet die Hörmatrix weiter weich.
- **Produktwirkung 3 — keine.** Unter dem Riegel wirkt jeder Hörwunsch als
  Processed; das war schon bisher so, weil jeder Offlineblock die Hörmatrix auf
  Processed setzte. Die Parameteranbindung ändert für Host und User nichts
  (Golden bytegleich); entfallen ist allein ein interner 10-Hz-Timer.
- **Registerzeile beim Abschluss:** es gilt der **Umbau**; der Wortlaut aus
  §7.5 trifft unverändert zu („NAK-312 Etappe 5 hat die zweite Familie (der
  APVTS-Adapter) entfernt und mit M-50 gegen Rückkehr gesichert").
- **Kommentar außerhalb der Ticketpfade:** `eq-copilot/plugin/state/NakamaParameter.h`
  nennt die APVTS-Konstruktion der Sonde (Zeilen 9-10) und
  „APVTS-/FL-Automationsparameter" (Zeile 97); seit dem Umbau gibt es in der
  Sonde keine APVTS mehr. Nicht falsch im Sinn der Tabelle, aber veraltet —
  Pflege nach Ticketende.
- **`docs/plugin-wissen.md` am Ticketende:** Offline-Übergang (Wechselerkennung,
  Riegel, hartes Schalten, Buchhaltung im Takt) und die Parameteranbindung ohne
  APVTS nachziehen.
- **Laufzeit-Arm (R-312-3):** der VST3-Wrapper ruft `setNonRealtime` in
  `setupProcessing` (Hostthread, vor dem Render) und vor jedem Block; welchen
  Weg FL beim Render nimmt, ist nicht gemessen — beobachtbar, ob der erste
  Renderblock den Wechsel sieht.

### 30.15 Kanon nachher (Bauer, 21.09.2026)

Voller Kanon abgekoppelt nach dem Auftrag, Befehl `pwsh -NoProfile -File
tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312
Etappe 5'`, auf dem Commit `be37d10a5d8b2525fbe39e84e2d7c9a4fb403fd2`; Start
15:10:46 (`Get-Date`), Ende 15:54:18 (Zeitstempel der Logdatei, letzte Zeile
`EXIT=0`). Kopf und Übersicht stehen im Runnerblock „Kanon-Lauf - NAK-312
Etappe 5" unmittelbar über diesem Abschnitt, die Rohausgabe unter
`docs/beweise/roh/NAK-312-be37d10-dirty.md`. Kopfzeile des Runners, wörtlich:

```
**Lauf:** 2026-09-21 15:10 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0
```

- **GRÜN 70/70**, Exitcode 0; stillgelegt bleibt A15. Die Etappe bringt kein
  neues Bein. Die Beine der Etappe sind grün: B7 (`EqCopTransactionTest`,
  1,86 s), B6 (`EqCopDspGoldenTest`, 6,58 s), A16 (`EqCopProbeeqNullTest`,
  0,62 s), B2 (`EqCopStateMigrationTest`, 1,53 s), B1 (`EqCopIdentityTest`,
  0,09 s, gegen das neu gebaute Bundle), B31 (`EqCopSondeLebenslaufTest`,
  71,16 s), dazu B11, B12 und A14.
- **A32** endet mit [HINWEIS] Exit 4, nicht blockierend, mit denselben
  Zählwerten wie die Kanons der Etappe 4: Quelldateien über 2000 Zeilen 2, über
  dem Ziel von 1500 Zeilen 11, Funktionen über 200 Zeilen 34,
  Kommentar-Bezeichner ohne Code 39. Die Etappe erhöht keinen dieser Werte;
  `SondeProcessor.cpp` (1857 Zeilen), `DspKern.cpp` (1529 Zeilen) und
  `DspKern::verarbeiteStueck` (327 Zeilen) lagen schon am Basis-SHA über dem
  Ziel beziehungsweise über 200 Zeilen und wachsen um die Zeilen der Etappe.
  **A33** [OK] Exit 0, clang-tidy-Fundstellen 58 von höchstens 58.
- **„dirty"** im Namen der Rohausgabe: die zwei fremden untracked Ordner
  `briefing-hub/` und `nimbalyst-local/`; `git status --short` zeigte nach dem
  Lauf sonst nur die Schreibziele des Runners (dieses Manifest und die
  Rohausgabe). `eq-copilot/install/nakama-installer-v1.json` blieb unverändert.
- **Lauf ohne Eingriff in Quellen, ein Stillstand erklärt.** Nach „Baue:" stand
  das Log ohne aktiven Compiler (gesehen 15:15:40); allein `vctip.exe` (Start
  15:10:51) lebte. Um 15:15:46 beendet (NAK-300); danach lief der Runner
  weiter. Das längste Bein war A4 (1 236,35 s). Vor dem Start lief kein Bau-
  oder Testprozess außer untätigen MSBuild-Knoten und einem `vctip.exe` aus den
  eigenen Einzelbauten dieser Etappe; beide um 15:10 beendet (NAK-290). Eine
  Hintergrundwache der Session auf das Logende wurde vom Werkzeug wegen
  Speicherknappheit beendet; der Kanon selbst lief davon unberührt weiter, das
  Ende ist an der Logdatei gemessen.

## 31. Messung der Etappe 5, Laufzeit-Arm mit Befund LZ-1, Auftrag Nacharbeit 1 (Dirigent, 21.09.2026, 16:17 Uhr)

### 31.1 Messung

Worker `c594801d` (Opus, Effort xhigh, `dontAsk`, Aufsicht ENG), 14:18 bis
15:56 Uhr (Startbeleg und `done` gemessen), Basis
`12300f1e429c38354f9d216d7ade395b71f71b70`. Ergebnis: drei Commits —
`19ddf5d397867cab315da20df5ef9dc4e0050c2b` (14:54:14, Offline-Übergang),
`be37d10a5d8b2525fbe39e84e2d7c9a4fb403fd2` (15:10:09, Parameteranbindung ohne
APVTS) und `64ece9e12341050e696ceb04d87b7718407e314c` (15:55:52, Manifest §30,
Kanon nachher, Kopf) = `origin/master`, Baum sauber (nur die zwei fremden
untracked Ordner), Basis ist Vorfahr von HEAD. Rundenbilanz `12300f1e..64ece9e1`
(`py -3.13 tools/dirigent/rundenbilanz.py`, gemessen): Produkt 4 Dateien
+195/-44, Tests 2 Dateien +882/-18, Prüfwerkzeug 1 Datei +3/-3, Doku 23 Dateien
+13320/-4. Außerhalb von `docs/beweise/` berührt der Diff genau sieben Pfade,
alle Ticketpfade der Etappe (`eq-copilot/plugin/dsp/DspKern.cpp`, `DspKern.h`,
`eq-copilot/plugin/sonde/SondeProcessor.cpp`, `SondeProcessor.h`,
`eq-copilot/plugin/tests/TransactionTestMain.cpp`,
`eq-copilot/plugin/tests/StateMigrationTestMain.cpp`, `tools/beweise.ps1`).
Kanon nachher am Stand `be37d10a`: `EXIT=0`, GRUEN 70/70 (Log des Runners vom
Dirigenten gelesen).

Vom Dirigenten am Produktdiff gelesen: `setNonRealtime` arbeitet nur mit
Atomics (Wechselerkennung `offlineGesehen` per `load` und `exchange`,
`setzeHoermatrix`, `setzeOfflineRiegel`, Anforderung
`vorschauEndeAngefordert`); `DspKern::verarbeiteStueck` verbraucht
`hartSchalten` per `exchange` und setzt unter dem Riegel `wirksam` auf
Processed; `dspKontrollTakt` verbraucht die Anforderung des Vorschauendes.

### 31.2 Laufzeit-Arm: Befund LZ-1 — FL verschwindet während der Wiedergabe

**Lauf 1** (15:56 bis 16:03 Uhr, Stand `64ece9e1`), Kopfzeile:
`LAUFZEIT NAK-312 64ece9e1 VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=5 verfehlt=2 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,snapshot-runde01.json=0,u40-aktivitaetsgate.json=3]`,
Exit 3. Neu gegenüber §19, §22, §25 und §28: `u40-aktivitaetsgate.json=3` —
das Szenario bricht am ersten Schritt ab („Timeout waiting for FL Studio
response after 2.0s", einmal wiederholt), vier Sekunden nach dem Ende von
`snapshot-runde01`; um 16:03:50 existierte kein `FL64.exe` mehr, und der Runner
hat kein Beenden protokolliert. Rohdaten
`docs/beweise/roh/NAK-312-laufzeit-64ece9e1-lauf1.md`.

**Lauf 2** (16:05 bis 16:11 Uhr, derselbe Stand) mit einer Prozesswache des
Dirigenten im 1-s-Takt (`docs/beweise/roh/NAK-312-etappe5-flwache.txt`):
dieselbe Kopfzeile, derselbe Abbruch von `u40-aktivitaetsgate`. Das Diagnose-FL
des Szenarios `snapshot-runde01` startete um 16:06:25 (PID 316548, Runner-Log)
und **existierte ab 16:08:10 nicht mehr — 105 Sekunden nach dem Start, bei
laufender Wiedergabe im Songloop (Songlänge 45 696 ms), ohne Zutun des
Runners**; das Szenario lief lokal weiter bis 16:11:19 und meldete Exit 0.
Rohdaten `docs/beweise/roh/NAK-312-laufzeit-64ece9e1.md`.

**Gemessen ist:** in zwei von zwei Läufen am Stand der Etappe 5 verschwindet
das Diagnose-FL während der Wiedergabe. Im Lauf der Etappe 4 (Stand `dc2665ba`,
§28) lief das Diagnose-FL desselben Szenarios (PID 78568, Start 12:59:57) um
13:07 Uhr noch (vom Dirigenten gemessen, gut sieben Minuten nach dem Start), und
`u40-aktivitaetsgate` endete mit Exit 0; das Diagnose-FL des Laufs am Stand
`32813efd` (§25, PID 154952) lebte um 10:34 Uhr noch, gut zwanzig Minuten nach
seinem Start. **Einschränkung, gemessen:** auch diese zwei FLs verschwanden
später ohne Runner — der Lauf der Etappe 4 fand um 12:59:16 den Besitzeintrag
154952 tot vor, der Lauf 1 der Etappe 5 um 15:56:57 den Eintrag 78568; wann und
warum, ist nicht gemessen (Absturz, Fremdbeendigung oder ein geschlossenes
Fenster sind gleich möglich). Gesichert ist deshalb nur: am Stand der Etappe 5
endet der Host nach 105 Sekunden, an den früheren Ständen überlebte er
mindestens sieben und zwanzig Minuten. **Nicht gemessen ist die Ursache.** Keine Absturzspur: kein
Eintrag „Application Error", „Application Hang" oder „Windows Error Reporting"
für `FL64.exe` zwischen 15:50 und 16:05 Uhr, kein Dump unter
`%LOCALAPPDATA%\CrashDumps`, kein FL-Crashlog unter
`Dokumente\Image-Line\FL Studio\Support\Logs` (nur Startup-Logs). Ob Änderungssatz
A (`19ddf5d3`), Änderungssatz B (`be37d10a`) oder etwas außerhalb des Plugins
den Host beendet, ist offen; die Gegenprobe am Stand der Etappe 4 auf dem
heutigen Rechnerstand steht aus.

**Unverändert gut:** Nulltest im Host am Auslieferungszustand **BITIDENTISCH**
(v = 0, g = 1,0, 0 Abweichungen über 2 015 193 Frames), Vergleich `ohne_slots`
GLEICH; die zwei Offline-Render des Runners endeten regulär. Der dritte
Vergleich fehlt weiter wegen Karte U43.

**Beobachtungen am Werkzeug (nicht bewertet, in den Auftrag gegeben):** (1)
`snapshot-runde01` meldet Exit 0, obwohl sein Host während des Szenarios
verschwand; (2) die Rohdatei des Laufs 2 führt in den Briefkasten-Details 25
Erwartungen mit der PID des Laufs 1 (381456) neben 24 mit der eigenen (316548);
(3) der Runner wertet „kein Ping" als VORAUSSETZUNG (Exit 3), nicht als
verfehlt (Exit 4).

### 31.3 Einordnung und Folge

**LZ-1 ist DEFEKT-Kandidat der Schwere critical** (der Host endet bei normaler
Wiedergabe; Gate-Satz „vollem Kanon GRÜN und Laufzeit-Arm", Skill §3.3:
verfehlter Laufzeit-Arm ist Nacharbeit wie ein Prüferdefekt). Der Kanon sieht
ihn nicht (GRUEN 70/70). Die Erstprüfung der Etappe 5 startet **nicht** auf
diesem Stand: ein lesender Prüfer urteilte über Code, der im Host nicht trägt.
**Regel R-312-19 (LZ-1).** Das Plugin beendet oder blockiert seinen Host in
keinem Betriebszustand. Gemessen wird das im Laufzeit-Arm über mindestens fünf
Songumläufe Echtzeit-Wiedergabe des Diagnoseprojekts; verschwindet der Host
oder antwortet er nicht mehr, ist das Szenario verfehlt (Exit 4). Sobald die
Ursache bekannt ist, bekommt sie zusätzlich einen Fall im Kanon mit Rotbeweis
an der Zeile, die die Zusage trägt. **Neue Matrixzeile M-88** (Etappe 5,
Nacharbeit 1): Zustand Diagnoseprojekt mit beiden Plugins am Endstand, Ereignis
Wiedergabe im Songloop über fünf Umläufe, Zusage „der Host lebt und antwortet
nach jedem Umlauf, und beide Rollen beantworten den Briefkasten", Test neues
Laufzeit-Szenario plus der Kanonfall an der Ursache, Rotbeweis „heute rot"
(Stand `64ece9e1`: Host verschwindet nach 105 Sekunden), Quelle R-312-19 und
Gate-Satz „Laufzeit-Arm".

Folge: **Nacharbeit 1 der Etappe 5 mit der Ursache als Auftrag**
(`docs/beweise/roh/NAK-312-etappe-5-nacharbeit-1-auftrag.txt`): reproduzieren,
zwischen `dc2665ba`, `19ddf5d3` und `be37d10a` eingrenzen (mit Gegenprobe am
Stand der Etappe 4), Ursache an der Quelle belegen, beheben, und den
Laufzeit-Arm um ein Szenario ergänzen, das das Überleben des Hosts über mehrere
Songumläufe misst und bei verschwundenem Host als verfehlt endet. Die
Erstprüfung geht danach über `12300f1e...HEAD` der ganzen Etappe. Die
nachgezogenen Hashes in `eq-copilot/install/nakama-installer-v1.json` gehören
zu diesem Commit.

---

## Kanon-Lauf - NAK-312 Etappe 5 Nacharbeit 1

**Lauf:** 2026-09-21 17:44 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-2c138b2-dirty.md](roh/NAK-312-2c138b2-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 17:44:59 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 2c138b2d NAK-312 Etappe 5 Nacharbeit 1: Laufzeit-Szenario schleife-dauerlauf und Kanonteil 312/M-88 (R-312-19) |
| Commit (voll) | 2c138b2d5b03f731526f7eba31797efeae166449 |
| Arbeitsbaum | 7 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,15 s | [A1](roh/NAK-312-2c138b2-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 12,41 s | [A2](roh/NAK-312-2c138b2-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,72 s | [A3](roh/NAK-312-2c138b2-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.259,63 s | [A4](roh/NAK-312-2c138b2-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 24,19 s | [A4-SI](roh/NAK-312-2c138b2-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,77 s | [A4b](roh/NAK-312-2c138b2-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 4,23 s | [A5](roh/NAK-312-2c138b2-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,18 s | [A6](roh/NAK-312-2c138b2-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,18 s | [A7](roh/NAK-312-2c138b2-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,45 s | [A8](roh/NAK-312-2c138b2-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,48 s | [A9](roh/NAK-312-2c138b2-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,96 s | [A10](roh/NAK-312-2c138b2-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,36 s | [A11](roh/NAK-312-2c138b2-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,43 s | [A12](roh/NAK-312-2c138b2-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,29 s | [A13](roh/NAK-312-2c138b2-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 11,79 s | [A14](roh/NAK-312-2c138b2-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,78 s | [A17](roh/NAK-312-2c138b2-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 43,61 s | [A18](roh/NAK-312-2c138b2-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,17 s | [A19](roh/NAK-312-2c138b2-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,37 s | [A20](roh/NAK-312-2c138b2-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,38 s | [A21](roh/NAK-312-2c138b2-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,11 s | [A22](roh/NAK-312-2c138b2-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,72 s | [A24](roh/NAK-312-2c138b2-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,20 s | [A34](roh/NAK-312-2c138b2-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 32,93 s | [A35](roh/NAK-312-2c138b2-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 7,71 s | [A36](roh/NAK-312-2c138b2-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,34 s | [A28](roh/NAK-312-2c138b2-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,21 s | [A29](roh/NAK-312-2c138b2-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,26 s | [A30](roh/NAK-312-2c138b2-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 109,45 s | [A31](roh/NAK-312-2c138b2-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,29 s | [A25](roh/NAK-312-2c138b2-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,18 s | [A26](roh/NAK-312-2c138b2-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,16 s | [A27](roh/NAK-312-2c138b2-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,83 s | [A23](roh/NAK-312-2c138b2-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,79 s | [A16](roh/NAK-312-2c138b2-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,17 s | [B1](roh/NAK-312-2c138b2-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. Seit NAK-312 Etappe 5 (312/M-49): die eingefrorenen Statebytes des Parametergoldens vom Basis-SHA 12300f1e passen zu ihrer Zeile im Golden (Laenge und SHA-256), laden normal und kommen aus dem Schreiber bytegleich zurueck. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,49 s | [B2](roh/NAK-312-2c138b2-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,08 s | [B3](roh/NAK-312-2c138b2-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,13 s | [B3b](roh/NAK-312-2c138b2-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,52 s | [B3c](roh/NAK-312-2c138b2-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,47 s | [B4](roh/NAK-312-2c138b2-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,18 s | [B9](roh/NAK-312-2c138b2-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 6,15 s | [B5](roh/NAK-312-2c138b2-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,86 s | [B6](roh/NAK-312-2c138b2-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, zaehlt sie seit Etappe 5 ueber 200 Bloecke je Aufruf setNonRealtime (true) wie je Aufruf setNonRealtime (false) 0 gemeldete Sperren (bis Etappe 4 je Aufruf setNonRealtime (true) genau eine). Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist bitgleich zu einem mit Takt nach jedem Block - fuer alle vier in beiden Staenden (neutral und mit Band) bei Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline; fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (dort wirkt der Wert im Block seines Randes), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. Seit der Nacharbeit 1 der Etappe 3 (L-1, R-312-16) nimmt der Blockrand nach einem read-only-Ladestart keinen Hostwert: laedt ein Stand mit eq an und Output-Trim +3 dB das fremde Major und kommen danach Input-Trim +24 dB, Output-Trim +24 dB, Width 2 und Mix 0 als Hostwerte, ist der Ausgang ab dem ersten Sample nach dem Laden bitgleich zu dem ohne diese Hostwerte, und die Blockrandziele steigen um 0 - bei Bloecken zu 256 und, wenn beim Laden ein Uebergang laeuft, bei 185 und 512; ein eigener schreibbarer Stand danach laesst Output-Trim +6 dB ohne Kontrolltakt wieder am Blockrand wirken (bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang am Ende bei 10^(6/20)). Seit NAK-312 Etappe 4 (312/M-38): ueber mindestens 4000 Bloecke wechselnder Groesse, waehrend beide v3-Clients der Sonde ueber einen Testserver auf der Probe-Pipe verbunden sind und ihre Provider nachweislich durch die Besitzschleuse laufen, bleiben RtWache::sperren() und RtWache::allokationen() ab Callback-Eintritt 0. Seit NAK-312 Etappe 5 (T3-01-03, T3-01-04, R-312-3) der Offline-Uebergang: mit hoerbarer Vorschau zaehlt die Wache im Bereich wie der Wrapper ueber 200 Bloecke 0 Sperren; unmittelbar nach dem ersten Aufruf setNonRealtime (true) ist die gewuenschte Hoermatrix Processed, und der Ausgang ist ab Sample 0 bitgleich zum Lauf ohne Vorschau - bei Blockgroesse 1, 64 und 256 und auch, wenn beim Wechsel ein Hoermatrix-Fade in Gegenrichtung oder ein Hoerhalt laeuft; nach einem ausdruecklichen Kontrolltakt ist keine Vorschau mehr aktiv, nach ceil(kFadeSamples/Blockgroesse)+1 Bloecken und einem zweiten Takt die Candidate-Bank frei; der Rueckweg in die Echtzeit blendet weich (Nachbarsprung hoechstens Fadeschritt plus 2^-23) und belebt die Vorschau nicht, und ohne Vorschau aendert das Offlineflag am Ausgang nichts. Seit dem zweiten Aenderungssatz der Etappe 5 (T3-01-02, R-312-4) haengen die 112 Hostparameter ohne APVTS direkt am Prozessor: Zahl, Reihenfolge, IDs, Namen, Klassen, Bereiche, Defaults und Hostsicht (convertTo0to1 an Minimum, Default und Maximum; convertFrom0to1, getText und getValueForText an 0, 0,37 und 1), die Statebytes ueber speichern, laden, speichern und die Epochenfolge einer Automationsfahrt sind gleich dem Golden des Basis-SHA 12300f1e; kein Quelltext unter eq-copilot/plugin/sonde/ nennt die APVTS-Klasse; nach dem Laden traegt jeder Hostparameter den geladenen Wert; am Ende des Destruktorrumpfs erreicht ein Hostwert den Prozessor nicht mehr; Index -1 und 112 bleiben ohne Wirkung, und der eigene Abgleich meldet kein Hostereignis; ueber 4000 Bloecke mit Hostautomation aus einem zweiten Thread zaehlt die Wache ab processBlock 0 Sperren und 0 Allokationen - die Sperre, die JUCE vor dem Plugincode in sendValueChangedMessageToListeners nimmt, sieht sie bauartbedingt nicht. Seit der Nacharbeit 1 der Etappe 5 (R-312-19, Kanonteil von 312/M-88) ruft kein Quelltext unter src, sonde, dsp, state, vertrag, core und hostbridge von eq-copilot/plugin ExitProcess, TerminateProcess, PostQuitMessage, FatalAppExit, exit, _exit, quick_exit, _Exit, Process::terminate oder systemRequestedQuit und keiner nennt WM_CLOSE, WM_QUIT, SC_CLOSE oder JUCEApplicationBase (Textriegel); ob der Host im Betrieb lebt, misst das nicht - das misst der Laufzeit-Arm. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 2,00 s | [B7](roh/NAK-312-2c138b2-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,29 s | [B10](roh/NAK-312-2c138b2-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,17 s | [B11](roh/NAK-312-2c138b2-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,70 s | [B12](roh/NAK-312-2c138b2-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. Seit Etappe 4 (R-312-7, R-312-12) die Besitzschleuse der Sonde, gemessen an einem gehaltenen Statusprovider und einer gezaehlten Marke vor dem ersten Besitzerzugriff: nach dem Ende des Eigentuemers erreicht ihn kein Provider (0 in jedem Fall und in 20 Gegenzyklen, nach denen Handles und Threads nicht hoeher stehen als nach dem ersten); gehalten hinter dem Betreten wird er zu Ende gewartet, gehalten davor und nach dem Destruktor freigegeben abgewiesen, vor dem Abbau freigegeben laeuft er durch; stop() loest den gehaltenen ControlClient nach der 2-s-Frist ab, controlV3.stop() gemessen vor dem Schliessen, telemetryV3.stop() nach Zeilenfolge; ein Reconnect laesst dieselbe offene Schleuse stehen. Eine Sonde aus dem Produktkonstruktor traegt im Testbau genau pipeNameV3 (aktuelleLogonSid()), ohne dass ein Client startet. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 71,11 s | [B31](roh/NAK-312-2c138b2-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,14 s | [B13](roh/NAK-312-2c138b2-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,49 s | [B14](roh/NAK-312-2c138b2-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. Seit NAK-312 Etappe 4 (R-312-2) faehrt dasselbe Bein das Messpunkt-Popover am echten Editor: baut die Nachrichtenschleife es erst nach Editor und Prozessor ab, fasst sein Destruktor keinen von beiden an (gezaehlte Marke 0, Bindung bis zum Prozessorende unveraendert, 0 Host-Dirty); schliesst es, solange der Editor lebt, wird eine Aenderung mit genau einer Host-Dirty-Meldung uebernommen, ohne Aenderung mit keiner. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 3,34 s | [B15](roh/NAK-312-2c138b2-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,08 s | [B20](roh/NAK-312-2c138b2-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,48 s | [B22](roh/NAK-312-2c138b2-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,77 s | [B23](roh/NAK-312-2c138b2-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,16 s | [B24](roh/NAK-312-2c138b2-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,09 s | [B25](roh/NAK-312-2c138b2-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,49 s | [B26](roh/NAK-312-2c138b2-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,08 s | [B21](roh/NAK-312-2c138b2-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 2,35 s | [B19](roh/NAK-312-2c138b2-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,36 s | [B17](roh/NAK-312-2c138b2-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 6,31 s | [B18](roh/NAK-312-2c138b2-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 2,03 s | [B16](roh/NAK-312-2c138b2-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,50 s | [B27](roh/NAK-312-2c138b2-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,90 s | [B29](roh/NAK-312-2c138b2-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 72,38 s | [B30](roh/NAK-312-2c138b2-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,20 s | [B28](roh/NAK-312-2c138b2-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,34 s | [B8](roh/NAK-312-2c138b2-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 45,08 s | [A32](roh/NAK-312-2c138b2-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 141,34 s | [A33](roh/NAK-312-2c138b2-dirty.md#a33) |


## 32. Etappe 5 — Nacharbeit 1: Ursache von LZ-1 (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-5-nacharbeit-1-auftrag.txt`; Befund
§31.2, Regel R-312-19 und Matrixzeile M-88 §31.3. Uhrzeiten aus `Get-Date`,
Ereignisprotokoll (UserModePowerService, Id 12), Dateizeitstempeln, den
Prozesswachen des Bauers und Commitzeit. „Gerechnet" steht, wo eine Zeit aus
Szenariostart, Rechnungsdauer und Szenarioende folgt. Fremde Matrixzeilen
tragen ihr Präfix (R-312-13).

### 32.1 Start und Basis-SHA

- Erste gemessene Uhrzeit 16:19:20 (`Get-Date`). `git pull --ff-only`: „Already
  up to date"; `git rev-parse HEAD` = `5f1ca5a69cba08de08c95e2f2349f57c4f87b510`
  (Basis-SHA der Nacharbeit); `git status --short` zeigt nur `briefing-hub/` und
  `nimbalyst-local/`.
- Das Produkt am Basis-SHA ist das von `64ece9e1` und `be37d10a`:
  `git diff --stat be37d10a 64ece9e1` und `git diff --stat 64ece9e1 5f1ca5a6`,
  je `-- eq-copilot/plugin broker/src eq-copilot/schemas`, sind leer.
- Werkzeuge: kein Debugger und kein ProcDump auf dem Rechner (gesucht, §32.4).
  Gemessen wird mit eigenen Prozesswachen im 1-s-Takt (Get-Process, nur lesen):
  Wache 1 wie `docs/beweise/roh/NAK-312-etappe5-flwache.txt` plus Speicher,
  Handles, Threads, CPU; Wache 2 hält beim ersten Sehen ein Prozesshandle und
  liest nach dem Ende **Exitcode und Endzeit**; Wache 3 dazu die Leerlaufzeit
  der Sitzung (`GetLastInputInfo`) und ob das Vordergrundfenster einer
  FL-Instanz gehört (nur ja/nein). Rohdatei `docs/beweise/roh/NAK-312-n1-flwache.txt`.

### 32.2 Reproduktion am HEAD — nicht reproduziert

Drei Läufe des Laufzeit-Arms am Produktstand der Etappe 5, Rohdateien
`docs/beweise/roh/NAK-312-n1-repro-head.md`, `NAK-312-n1-repro-head-voll.md`,
`NAK-312-n1-head-voll2.md`:

| Lauf | Szenarien | Ergebnis | Diagnose-FL des Snapshots |
|---|---|---|---|
| 16:26-16:31 | nur `schleife-dauerlauf` | Exit 0 (GEMESSEN), fünf Wraps (Position nach jedem Umlauf 321, 2826, 5420, 7924, 10473 ms) | — (FL 231228 lebte 16:26:59 bis zum Beenden durch den nächsten Runner 16:32:20, Exit −1) |
| 16:32-16:43 | voller Satz (sechs) | alle Szenarien 0 außer `nulltest-host` 5 (U43) | 75884, Start 16:37:15, lebte bis zum Beenden durch den nächsten Runner 17:05:15,999 (Exit −1) |
| 17:28-17:39 | voller Satz, nach der Rücknahme neu gebaut (§32.3) | alle 0 außer `nulltest-host` 5 | 139208, Start 17:32:53, lebte bis 17:44:46 (vom Bauer vor dem Kanon beendet, Exit −1) |

Die zwei roten Läufe des Dirigenten (§31.2) und meine zwei ersten Läufe
installierten dasselbe Bündel (Installer-Hash `active-probe` =
`E5881DAF42BBA3384EF61C9A4274896DA241E8A83AEDB53E4E01FD3F1115BAEE` in beiden
Runnerlogs). **Gleiche Binaries: zweimal rot (15:57, 16:06), dreimal grün
(16:26, 16:32, 17:28, der dritte nach bytegleichem Neubau).** LZ-1 hängt damit
nicht allein am Produktstand.

**Zeitpunkte gegen Wiedergabe und Songposition** (aus den Szenariomarken
`<!-- szenario.py … -->`, der Dauer der Rechnung aus F-28 und dem Ende des
Szenarios, gerechnet; das Ende gemessen am Energie-Ereignis):

| Lauf | Stand | Start `snapshot-runde01` | Wrap, danach Rechnung F-28 | FL-Ende | Lage des Endes |
|---|---|---|---|---|---|
| 15.09., NAK-286 | `e11dcfcb` | 14:02:47 | ab ≈ 14:03:36 (219,4 s) | 14:06:03,783 | ≈ 148 s in der Rechnung |
| Lauf 1 Dirigent | `64ece9e1` | 15:57:48 | ab ≈ 15:58:39 (266,3 s) | 16:00:47,873 | ≈ 129 s in der Rechnung |
| Lauf 2 Dirigent | `64ece9e1` | 16:06:37 | ab ≈ 16:07:25 (233,6 s) | 16:08:08,838 | ≈ 43 s in der Rechnung |
| Gegenprobe Bauer | Produkt `dc2665ba` | 17:10:23 | ab ≈ 17:11:12 (290,9 s) | 17:15:07,810 (Wache) | ≈ 236 s in der Rechnung |

Gemeinsam ist gemessen: **jedes der vier Enden fiel in die Rechnung aus F-28**
von `snapshot-runde01` — FL spielt im Songloop weiter, der Runner stellt keine
Anfrage, `py -3.13 szenario.py --rechne` rechnet. Die Songposition im Moment des
Endes ist nicht gemessen (in dieser Phase fragt niemand den Briefkasten; die
letzte Antwort des Laufs 2 stammt von 16:07:25,4 bei Projektsample 1 990 656,
also am Wrap).

### 32.3 Eingrenzung je Stand

**Produktstand der Etappe 4 (`dc2665ba`, gleich `12300f1e`), heute gemessen —
LZ-1 tritt auf.** Die vier Produktdateien
(`SondeProcessor.cpp`, `SondeProcessor.h`, `DspKern.cpp`, `DspKern.h`) mit
`git checkout dc2665ba -- <pfade>` geholt, `LastWriteTime` auf jetzt, beide
Bündel gebaut (17:02:59 bis 17:05:01, Binaries jünger als die Quellen), über
den Laufzeit-Arm installiert (`\Nakama\installieren`, `pruefen` 3 Artefakte
„aktuell") und gemessen. Kopfzeile:
`LAUFZEIT NAK-312 5f1ca5a6 VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=6 verfehlt=2 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,schleife-dauerlauf.json=0,snapshot-runde01.json=0,u40-aktivitaetsgate.json=3] fl=Producer Edition v26.1.4 [build 5589]`
(der Runner nennt den HEAD, installiert war der Arbeitsbaum). Das Diagnose-FL
des Snapshots (PID 176680, Start 17:10:11) **endete 17:15:07,810 selbst mit
Exitcode 0**; `u40-aktivitaetsgate` brach am ersten Ping ab (Exit 3) — das
Bild aus §31.2. `schleife-dauerlauf` lief im selben Lauf davor grün. Danach
`git checkout HEAD -- <pfade>`: SHA-256 der vier Dateien gleich den Werten vor
dem Tausch (`07EF9E6B…`, `73E35EB3…`, `E6693B24…`, `7CA7267E…`), neu gebaut
(17:18:47 bis 17:20:44). Rohdateien
`docs/beweise/roh/NAK-312-n1-gegenprobe-dc2665ba.md`,
`docs/beweise/roh/NAK-312-n1-eingrenzung.txt`.

**Weiter zurück, aus dem Systemprotokoll** (Rohdatei
`docs/beweise/roh/NAK-312-n1-fl-historie.txt`: jedes vom Runner gestartete
Diagnose-FL aus `laufzeit.log` gegen die Energie-Ereignisse): selbst geendet
sind Runner-FLs schon lange vor Etappe 5 und vor NAK-312 — `e11dcfcb`
(NAK-286, 15.09.) nach 207,3 s **während `snapshot-runde01`, danach
`u40-aktivitaetsgate` Exit 3** (das ist NAK-307), `d6382571` (15.09.) nach
550 s, `c8286582` (NAK-289) nach 85 605 s, `ea2a9bec` (12.09.) nach 4 038 s,
heute `dc2aaf1c` nach 1 302,6 s, `32813efd` nach 10 069,6 s, `dc2665ba` nach
1 302,4 s, `64ece9e1` nach 190,7 s und 102,8 s.

**`19ddf5d3` gegen `be37d10a`: nicht gemessen** (A-68). Die Eingrenzung setzt
einen Stand ohne den Befund voraus; der Produktstand der Etappe 4 zeigt ihn,
der der Etappe 5 lief dreimal ohne ihn. **Die Ursache liegt nicht in der
Etappe 5.**

### 32.4 Spuren

Rohdatei `docs/beweise/roh/NAK-312-n1-spuren.txt`. Kurz:

- **Kein Absturz:** kein WER-Eintrag, kein Dump, kein FL-Crashlog, kein
  Ressourcenmangel (Id 2004). FL-Abstürze werden hier protokolliert (16.08.2026
  APPCRASH mit Kontakt 8, 18.08.2026 AppHang); LZ-1 hinterließ keinen.
- **Kein Abschuss von außen:** jedes FL erzeugt beim Start ein Energie-Ereignis
  (Id 12); ein zweites beim Ende nur, wenn FL selbst endet. Gemessen: elf per
  Stop-Process beendete FLs (zehn vom Runner, eines vom Bauer; Exit −1) ohne
  Ende-Ereignis, sechs Renders (Exit 0) und ein `WM_CLOSE` (Exit 0) mit
  Ende-Ereignis. Die LZ-1-Enden 16:00:47,873,
  16:08:08,838 und 17:15:07,661 tragen eines.
- **FLs eigene Schließroutine lief:** beim regulären Ende schreibt FL
  `Settings\…\Recent files.scr`, `Tab_*.tabconfig`, `Auto saved.ncp`,
  `Vel curve *.env`; beim Abschuss nicht (Runner beendete 75884 um 17:05:15,999,
  `Recent files.scr` blieb auf 16:32:35). Beim Selbstende 176680 geschrieben
  17:15:06,446 bis 17:15:07,676.
- **Kein Leck:** PID 176680 hielt privaten Speicher 321,3 bis 321,9 MB, Handles
  6123 bis 6134, Threads 64 bis 73; 17:15:04 +22 Handles und +1 Thread, danach
  das Herunterfahren (17:15:06 48 Threads, 17:15:07 einer). Was 17:15:04
  auslöste, ist nicht gemessen.
- **Briefkasten und Diagnose beider Rollen unmittelbar vor dem Ende: nicht
  gemessen** — in der Rechnung aus F-28 fragt kein Schritt. Die letzten
  Antworten vor einem Ende (Lauf 2, 16:07:25) zeigen beide Rollen normal
  (`spielt` true, `nicht_endlich_rahmen` 0).
- Ereignisprotokolle (alle aktivierten) je 10 s um die Enden: nur das
  Energie-Ereignis und Hintergrund.
- Umgebung (Beobachtung): beide Enden des Dirigenten fielen in eine
  Spielsitzung auf dem Rechner (Spiellogs 15:54:38 bis 16:10:02), mein grüner
  Lauf 16:26 in eine weitere (16:14:34 bis 16:32:49), das Selbstende 17:15:07 in
  keine. Wache 3 (17:18:34 bis 18:55:57) maß eine Leerlaufzeit der Sitzung von
  0 bis 509 s — der Rechner wurde zeitweise bedient; jedes Runner-FL lag rund
  2 s nach seinem Start kurz im Vordergrund; in dieser Zeit endete kein FL
  selbst.

### 32.5 Der Mechanismus

**Gemessen:** LZ-1 ist **FLs reguläres Programmende**, keine Beendigung durch
das Plugin, kein Absturz und kein Abschuss. Beleg an der Spur: dasselbe Bild
wie ein `WM_CLOSE` an das Hauptfenster, das der Bauer 17:23:26,399 an ein
runner-eigenes FL schickte (§32.7) — Exitcode 0, Einstellungsdateien
17:23:26,435 bis 17:23:26,893, Energie-Ereignis 17:23:26,884, kein WER — gegen
das Selbstende am Etappe-4-Stand: Exitcode 0, Einstellungsdateien 17:15:06,446
bis 17:15:07,676, Energie-Ereignis 17:15:07,661, kein WER.

**Das Plugin hat keinen Weg dorthin, belegt an der Quelle:** kein Quelltext der
zwei ausgelieferten Bündel ruft ExitProcess, TerminateProcess, PostQuitMessage,
FatalAppExit, `exit`, `_exit`, `quick_exit`, `_Exit`, `Process::terminate` oder
`systemRequestedQuit` oder nennt `WM_CLOSE`, `WM_QUIT`, `SC_CLOSE` (Textriegel
`312/M-88 kein_weg_zum_hostende_im_pluginquelltext`,
`eq-copilot/plugin/tests/TransactionTestMain.cpp:5895-5972` am Stand `2c138b2d`, 103 Dateien ohne
Treffer); in JUCE 8.0.9 ruft allein `Process::terminate`
(`eq-copilot/build/_deps/juce-src/modules/juce_core/native/juce_Threads_windows.cpp:292-299`)
ExitProcess, und das nur aus dem Linux-Pfad
(`juce_gui_basics/native/juce_XWindowSystem_linux.cpp:1484`); ein Abbruch über
`std::terminate` endet mit `__fastfail` und WER-Eintrag, nicht mit FLs
Schließroutine. FL bindet sich an keine Konsole (FL64.exe GUI-Subsystem, weder
FL64.exe noch FLEngine_x64.dll importieren Konsolenfunktionen). Das
Controller-Skript des FL-MCP beendet FL nicht, und kein Werkzeug des
Laufzeit-Arms sendet FL eine Fenster- oder Quit-Nachricht.

**Nicht gemessen ist, wer oder was FLs Programmende auslöst.** Möglich sind
nach der Spur ein Schließen über das Fenster (durch eine Person oder einen
anderen Prozess) oder ein Entschluss von FL selbst; keiner davon ist gemessen.
Beobachtungen ohne Deutung: FL startet sichtbar und spielt das Diagnoseprojekt
hörbar; der Rechner wurde während der Messungen zeitweise bedient; alle vier Enden fielen
in die Rechnung aus F-28; zwei Runner-FLs (`dc2aaf1c`, `dc2665ba`) endeten
1 302,6 s und 1 302,4 s nach ihrem Start — ein fester Abstand, den keine Messung
erklärt.

### 32.6 Kein Fix im Plugin

Die Ursache liegt nicht in den Ticketpfaden der Etappe 5 und nicht im Plugin.
Nach dem Auftrag wird nichts umgebaut; am Produktcode ändert die Nacharbeit
nichts. Den kleinsten Schritt außerhalb nennt §32.14 (Messen des Auslösers).

### 32.7 M-88 und das neue Szenario

**Szenario `docs/gesundheit/szenarien/schleife-dauerlauf.json`** (Commit
`2c138b2d`; Format nach `tools/fl/szenario.py`, Tabelle in
`docs/gesundheit/szenarien/LIES-MICH.md`): `frischer_start`, Ping, beide
Plugins am Platz, Songlänge höchstens 46 000 ms, Songloop, Stopp, Position 0,
Briefkasten beider Rollen, Start; dann fünfmal 46 s Warten und danach
`transport.getStatus` (spielt, Songloop), `transport.getPosition` und
`lokal.briefkasten` gen und probeeq mit `frame.spielt` true; am Ende Stopp. 230 s
reine Wartezeit, fünf Wraps bei 45 696 ms Songlänge (gemessen: nach jedem Wrap
eine Position von 321 bis 10 473 ms). Ein verschwundener oder nicht antwortender
Host fällt an der Prüfung nach dem Umlauf als VERFEHLT (Szenario-Exit 4): die
Prüfungen sind keine Pings, ein Ping-Ausfall wäre Exit 3. Der Name sortiert das
Szenario nach `nulltest-host` und vor `snapshot-runde01`; beide Nachbarn laufen
wie bisher, und die Folge bricht an ihm nie ab.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-88 | `schleife-dauerlauf.json`, Laufzeit-Arm; Kanonteil `312/M-88 kein_weg_zum_hostende_im_pluginquelltext`, B7 | Szenario grün in fünf Läufen am Produkt der Etappe 5 (16:26, 16:32, 17:28 und die zwei Läufe am Endstand, §32.10) und einmal am Produkt der Etappe 4; Kanonteil grün, 103 Dateien ohne Treffer | „heute rot" am Stand `64ece9e1` **nicht gemessen** — LZ-1 trat in meinen Läufen dort nicht auf (A-67); Rotbeweis des Szenarios an der Zusagezeile: das runner-eigene FL 90 s nach dem Start per `WM_CLOSE` regulär geschlossen → Schritt 15 (Status nach dem zweiten Umlauf) und folgende VERFEHLT, Szenario-Exit 4, Kopfzeile `VERFEHLT … [schleife-dauerlauf.json=4]`; Kanonteil: `if (offline && getTotalNumInputChannels() < 0) std::exit (0);` in `SondeProcessor::setNonRealtime` → genau 312/M-88 rot, nichts mit; `docs/beweise/roh/NAK-312-rot-M-88.txt` |

### 32.8 Einzelbeine

- B7 `EqCopTransactionTest` nach dem Bau 17:40:59 bis 17:41:10 (Binary 17:41:10,
  jünger als `TransactionTestMain.cpp` 17:40:55): **248 geprüft, 0 Fehler**,
  Exit 0 (vorher 247; neu der Kanonteil). Unter der Mutation 248/1, nach der
  Rücknahme wieder 248/0 (Bau 17:42:14 bis 17:42:21).
- A35 `tools/fl/selbsttest.py` (liest jede Szenariodatei, 309/M-48 Echo in jeder
  Szenariodatei): **GRUEN**, `laufzeit.ps1` 37 ok, 0 rot (17:42:34 bis 17:43:09).
- Die zwei Bündel für die Gegenprobe und die Rücknahme gebaut (§32.3); B1 misst
  das im Kanon neu gebaute Bündel.

### 32.9 Kanon nachher (Bauer, 21.09.2026)

Voller Kanon abgekoppelt nach dem Auftrag, Befehl `pwsh -NoProfile -File
tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-312.md -Anhaengen -Titel 'NAK-312
Etappe 5 Nacharbeit 1'`, auf dem Commit
`2c138b2d5b03f731526f7eba31797efeae166449`; Start 17:44:58 (`Get-Date`), Ende
18:29:51 (Zeitstempel der Logdatei, letzte Zeile `EXIT=0`). Kopf und Übersicht
stehen im Runnerblock „Kanon-Lauf - NAK-312 Etappe 5 Nacharbeit 1" unmittelbar
über diesem Abschnitt, die Rohausgabe unter
`docs/beweise/roh/NAK-312-2c138b2-dirty.md`. Kopfzeile des Runners, wörtlich:

```
**Lauf:** 2026-09-21 17:44 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-2c138b2-dirty.md](roh/NAK-312-2c138b2-dirty.md)
```

- **GRÜN 70/70**, Exitcode 0; stillgelegt bleibt A15. Kein neues Bein. B7
  (`EqCopTransactionTest`, 2,00 s) **248 geprüft, 0 Fehler** samt dem Kanonteil
  312/M-88; B1 (0,17 s, gegen das neu gebaute Bündel), B2 (1,49 s), B6 (6,86 s),
  A16 (0,79 s), B31 (71,11 s) und A35 (32,93 s, liest die neue Szenariodatei)
  grün.
- **A32** [HINWEIS] Exit 4, nicht blockierend, mit denselben Zählwerten wie die
  Kanons der Etappen 4 und 5 (Quelldateien über 2000 Zeilen 2, über 1500 Zeilen
  11, Funktionen über 200 Zeilen 34, Kommentar-Bezeichner ohne Code 39). **A33**
  [OK] Exit 0, clang-tidy-Fundstellen **58 von höchstens 58**.
- **„dirty"**: die zwei fremden Ordner und die fünf bis dahin ungetrackten
  Rohdateien der Nacharbeit (`NAK-312-n1-*.md`); der Runner nennt 7
  unbestätigte Dateien. Kein Produkt- oder Testpfad war ungetrackt oder
  geändert; `eq-copilot/install/nakama-installer-v1.json` lag vor dem Start
  wieder auf HEAD und blieb unverändert.
- **Ein Stillstand, erklärt (NAK-300, A-72).** Nach „Baue:" stand das Log ohne
  Compiler, Linker oder MSBuild (gesehen 17:50:01); allein `vctip.exe`
  (Start 17:45:49, Kind des Bauschritts) lebte. Zwischen 17:50:01 und 17:50:25
  beendet (das nächste Bein stand danach im Log); danach lief
  der Runner weiter. Das längste Bein war A4 (1 259,63 s). Vor dem Start lief kein
  FL (das letzte runner-eigene FL 139208 um 17:44:46 beendet) und kein Bau- oder
  Testprozess (NAK-290).

### 32.10 Laufzeit-Arm am Endstand (Bauer, 21.09.2026)

Zwei aufeinanderfolgende Läufe nach dem grünen Kanon, am Commit `2c138b2d`
(Produkt unverändert, Bündel aus dem Kanon-Bau), voller Szenariosatz samt
`schleife-dauerlauf`, gestartet 18:33:29 in einem abgekoppelten Befehl, Wache 3
dazu (`docs/beweise/roh/NAK-312-n1-flwache.txt`, Abschnitt Endstand). Vor dem
Start lief kein FL und kein Bau- oder Testprozess. Kopfzeilen, wörtlich:

- Lauf 1 (18:33 bis 18:44), Rohdatei
  `docs/beweise/roh/NAK-312-laufzeit-2c138b2d-lauf1.md`:
  `LAUFZEIT NAK-312 2c138b2d VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=6 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,schleife-dauerlauf.json=0,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0] fl=Producer Edition v26.1.4 [build 5589]`,
  Exit 3.
- Lauf 2 (18:44 bis 18:55), Rohdatei
  `docs/beweise/roh/NAK-312-laufzeit-2c138b2d.md`:
  `LAUFZEIT NAK-312 2c138b2d VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=6 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,schleife-dauerlauf.json=0,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0] fl=Producer Edition v26.1.4 [build 5589]`,
  Exit 3.

**`schleife-dauerlauf` in beiden Läufen grün** (fünf Umläufe, beide Rollen
antworten nach jedem), ebenso `snapshot-runde01` und `u40-aktivitaetsgate`;
Exit 3 allein wegen Karte U43 (`nulltest-host` 5, Referenzprojekt
`Nakama-Diagnose-Verarbeitung.flp` fehlt). **Nulltest im Host am
Auslieferungszustand BITIDENTISCH** in beiden Läufen (v = 0, g = 1,0,
0 Abweichungen über 2 015 193 Frames), `ohne_slots` GLEICH. Kein Diagnose-FL
endete selbst: jedes wurde vom nächsten Schritt des Runners beendet (Exit −1),
das Snapshot-FL des Laufs 2 (PID 218184) bleibt nach dem Lauf offen wie bisher.
Beide Läufe zogen dieselben Hashes des Kanon-Baus in
`eq-copilot/install/nakama-installer-v1.json` nach (`main` 723AB44F…,
`active-probe` EC153663…, `hashes_erzeugt_am` aus Lauf 2); sie gehören zum
Commit dieses Abschnitts.

### 32.11 Abweichungen, begründet

- **A-66 — der Kanonteil von M-88 ist ein Riegel der Pluginseite, kein Fall
  „an der Ursache".** Die Ursache liegt außerhalb des Plugins und ist ohne Host
  nicht messbar; ohne Host messbar ist der Ausschluss, auf dem das Urteil
  ruht (kein Pluginweg zum Hostende). Er steht als Textriegel in B7 (Muster
  312/M-50), fail-closed (fehlt ein Ordner oder sind es weniger als 100 Dateien,
  ist er rot). `std::terminate` bleibt erlaubt (NAK-289, Abbruch mit WER-Spur);
  Mitgliedsaufrufe wie `schloss.exit()` zählen nicht.
- **A-67 — Rotbeweis des Szenarios an der Umgebung statt „heute rot".** Am
  Produkt `64ece9e1` blieb das Szenario in allen Läufen grün; der Befund lässt
  sich nicht auf Abruf erzeugen. Belegt ist deshalb die Zusagezeile: ein
  regulär geschlossener Host (dieselbe Art Ende wie LZ-1, gleiche Spur) fällt an
  der Prüfung nach dem Umlauf. Beendet wurde nur das FL, das der Runner für
  genau diesen Lauf gestartet hatte (PID aus dem Runner-Log).
- **A-68 — keine Eingrenzung `19ddf5d3` gegen `be37d10a`** (§32.3).
- **A-69 — Rohausgabe des ersten Laufs im MCP-Repo.** Mit relativem `-Roh`
  schrieb `szenario.py` (läuft über `uv run --directory <MCP-Repo>`) seinen Teil
  nach `C:\Users\phili\Projekte\fl-studio-mcp\docs\beweise\roh\`. Den Teil an
  `docs/beweise/roh/NAK-312-n1-repro-head.md` angehängt, meinen dort angelegten
  Ordner entfernt; `git status --short` im MCP-Repo danach leer. Alle weiteren
  Läufe mit absolutem Pfad. Am Runner nichts geändert (Hinweis §32.14).
- **A-70 — Installer-Manifest.** Die Läufe der Eingrenzung zogen die Hashes der
  jeweils gebauten Bündel nach; vor dem Kanon auf HEAD zurückgenommen, committet
  sind allein die Hashes der Läufe am Endstand.
- **A-71 — Rohausgaben unter eigenem Namen.** Die Läufe laufen alle auf HEAD
  `5f1ca5a6`; damit keine Rohdatei eine andere überschreibt, trägt jede einen
  eigenen Namen `NAK-312-n1-<lauf>.md` (`-Roh`), die Läufe am Endstand
  `NAK-312-laufzeit-<sha>-lauf1.md` und `NAK-312-laufzeit-<sha>.md`.
- **A-72 — ein Stillstand im Kanon.** Siehe §32.9.

### 32.12 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für die Nacharbeit 1

| Abschnitt | Punkt | Nacharbeit 1 |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt als Messgegenstand: Start und Ende jeder FL-Instanz mit Exitcode; kein Lebenszykluscode geändert |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | angewandt: Songlänge als Bereich 1 bis 46 000 ms; Wartezeit 46 s gegen 45 696 ms Songlänge; kein Draht-, Schema- oder Statefeld berührt |
| D Bau- und Prüfriegel | fail-closed, Frische, was der Kanon nicht baut | angewandt: Kanonteil fail-closed (Ordner, Dateizahl); Binaryzeitstempel bei jedem Bau; das Szenario läuft nur im Laufzeit-Arm, nicht im Kanon (A35 liest es) |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die B7-Zeile sagt, dass der Riegel das Leben des Hosts nicht misst; „nicht gemessen" für Auslöser, Songposition und Briefkasten vor dem Ende |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: B7-Kopfkommentar, B7-Behauptungszeile, Szenariotabelle; Manifestkopf |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: Szenario (WM_CLOSE → Exit 4) und Kanonteil (Mutation → genau M-88 rot); `docs/beweise/roh/NAK-312-rot-M-88.txt` |
| E Behauptung ≤ Messung | Writer-Fixtures statt Handschrift | nicht zutreffend: keine Fixture |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Zeiten aus Ereignisprotokoll, Wachen und `Get-Date`; gerechnete Zeiten als gerechnet markiert |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: Szenario, Szenariotabelle, Kanonteil und Behauptungszeile in `2c138b2d`; starten↔stoppen im Szenario (Start und Stopp der Wiedergabe) |

### 32.13 Selbstaudit

- **Hält jede Zusage der Etappe 5?** Am Produktcode ändert die Nacharbeit nichts;
  B7 248/0 mit allen Fällen aus §30.5 und §30.8, der Identitätsbeweis
  (312/M-48, M-49) grün, B1 im Kanon grün (§32.9).
- **Ist die Gegenprobe die Ursache und nicht ein Nebeneffekt?** Die Gegenprobe
  am Etappe-4-Stand zeigt denselben Befund mit derselben Spur; das schließt die
  Etappe 5 als Ursache aus. Sie belegt nicht, wer das Programmende auslöst.
  Der Rotbeweis des Szenarios fällt an der Zusagezeile (Prüfung nach dem
  Umlauf), nicht an einem Nebeneffekt: die Schritte vor dem Schließen bleiben
  grün, die Ping-Regel (Exit 3) greift nicht.
- **Kann das Szenario falsch grün sein?** Ein Host, der lebt, aber nicht
  spielt, fällt an `is_playing` und `frame.spielt`; ein Host, der nach dem
  letzten Umlauf endet, fällt am abschließenden Stopp (Timeout, Exit 4). Ein
  Host, der zwischen zwei Prüfungen endet und neu startet, ist ausgeschlossen:
  nur der Runner startet FL, und der Briefkasten erwartet die Diagnose-PID.
- **Kann der Kanonteil falsch rot sein?** Ein Wort in einem Kommentar zählt mit
  (Absicht, fail-closed); ein Mitgliedsaufruf `x.exit()` nicht.
- **Was bleibt ungemessen?** Der Auslöser des regulären Programmendes; die
  Songposition beim Ende; Briefkasten und Diagnose beider Rollen unmittelbar
  vor dem Ende; was 17:15:04 die +22 Handles auslöste; warum zwei FLs nach
  gleich 1 302,5 s endeten.

### 32.14 Hinweise an den Dirigenten

- **Produktwirkung: keine.** Kein Produktcode geändert; der Kanonteil und das
  Szenario messen nur.
- **LZ-1 ist nicht die Etappe 5** und derselbe Befund wie NAK-307 (15.09.,
  `e11dcfcb`): FL beendet sich regulär, während `snapshot-runde01` rechnet. Die
  Erstprüfung der Etappe 5 hängt nicht an LZ-1.
- **Kleinster nächster Schritt zum Auslöser:** eine Wache mit Leerlaufzeit und
  Vordergrundfenster (Wache 3 dieser Nacharbeit) bei jedem Laufzeitlauf; endet
  FL, zeigt sie, ob unmittelbar davor eine Eingabe kam und FL vorn lag.
- **Laufzeit-Arm (nicht geändert, Beobachtungen):** (1) ein relativer
  `-Roh`-Pfad schreibt den Szenarioteil ins MCP-Repo (A-69); (2) die drei
  Beobachtungen aus §31.2 bleiben; (3) das Diagnose-FL bleibt nach dem Lauf
  offen, bis der nächste Runner es beendet.
- **Außerhalb des Auftrags, nicht untersucht:** `eqcop-broker-v3probe.exe`
  endete heute fünfmal mit `0xc0000409` (Dumps unter
  `%LOCALAPPDATA%\CrashDumps`, 04:54 bis 15:17) — Testbroker der Kanonbeine.

## 33. Messung der Nacharbeit 1 der Etappe 5, Berichtigung von §31.3, Übergabe (Dirigent, 21.09.2026, 19:03 Uhr)

### 33.1 Messung

Worker `2b0c2f22` (Opus, Effort xhigh, `dontAsk`, Aufsicht ENG), Start 16:19
Uhr, Basis `5f1ca5a69cba08de08c95e2f2349f57c4f87b510`. Um 18:30 Uhr meldete der
Beobachter ihn als blockiert: der Kanon war seit 18:29:51 fertig (`EXIT=0`), der
Worker wartete auf eine eigene Hintergrundbenachrichtigung, die nicht ankam —
keine Frage, kein Berechtigungsprompt (Logende vom Dirigenten gelesen). Der
Dirigent hat ihn angehalten und nach Skill §3.2 mit Zusatz fortgesetzt; die
Fortsetzung mit Flags startete eine **Kopie** der Session unter der Kennung
`5f1c145b` (Meldung der CLI: „keeps its own saved options, so the flags you
passed started a copy"), die den Verlauf trug und 18:33 bis 19:00 Uhr zu Ende
arbeitete; unter dem Workernamen lief zu jeder Zeit genau ein Schreiber
(gemessen). Ergebnis: zwei Commits —
`2c138b2d5b03f731526f7eba31797efeae166449` (17:44:13, Szenario
`schleife-dauerlauf` und Kanonteil 312/M-88) und
`8bd47ff3d6cb72e50333a072557375a150272968` (18:59:32, Manifest §32, Kanon
nachher, Laufzeit-Arm, Installer-Hashes) = `origin/master`, Baum sauber (nur die
zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD. Rundenbilanz
`5f1ca5a6..8bd47ff3` (gemessen): Produkt 1 Datei +3/-3 (allein die
Installer-Hashes), Tests 1 Datei +86/-1, Prüfwerkzeug 1 Datei +1/-1, Doku 16
Dateien +23053/-3. **Kein Produktcode geändert.** Außerhalb von
`docs/beweise/`: `docs/gesundheit/szenarien/schleife-dauerlauf.json` und die
Tabellenzeile dazu in `docs/gesundheit/szenarien/LIES-MICH.md` (gehört zum
Szenario), `eq-copilot/plugin/tests/TransactionTestMain.cpp`,
`tools/beweise.ps1` (eine Behauptungszeile),
`eq-copilot/install/nakama-installer-v1.json`. Kanon nachher am Stand
`2c138b2d`: `EXIT=0`, GRUEN 70/70 (Log des Runners vom Dirigenten gelesen).
Laufzeit-Arm am Endstand, zwei Läufe des Bauers (§32.10): beide Exit 3 **allein
wegen Karte U43**, `schleife-dauerlauf`, `snapshot-runde01` und
`u40-aktivitaetsgate` je Exit 0, **Nulltest im Host BITIDENTISCH**.

### 33.2 Berichtigung von §31.3 — LZ-1 ist kein Defekt der Etappe 5

Die Einordnung des Dirigenten in §31.3 („DEFEKT-Kandidat der Schwere critical",
„Befund gegen den Stand der Etappe 5") ist **durch Messung widerlegt** (§32):

- LZ-1 ist **FLs reguläres Programmende**: Exitcode 0, FLs Schließroutine
  schreibt ihre Einstellungsdateien, Energie-Ereignis wie bei einem `WM_CLOSE`,
  kein Absturz, kein Abschuss (§32.4, §32.5).
- Es tritt **am Produktstand der Etappe 4 ebenso auf** (Gegenprobe des Bauers,
  Selbstende 17:15:07 mit demselben Folgebild) und ist seit dem 15.09.2026
  belegt (Register NAK-307); mit **denselben Binaries** der Etappe 5 liefen zwei
  Läufe rot (Dirigent, 15:57 und 16:06) und fünf grün (Bauer).
- Das Plugin hat an der Quelle keinen Weg zu einem Hostende (Kanonteil
  312/M-88, 103 Dateien ohne Treffer).
- **Nicht gemessen ist der Auslöser.** Beobachtungen des Bauers ohne Deutung:
  der Rechner wurde zeitweise bedient, beide roten Läufe des Dirigenten fielen in
  eine Spielsitzung auf dem Rechner, FL startet sichtbar und spielt hörbar; zwei
  Runner-FLs endeten 1 302,6 s und 1 302,4 s nach ihrem Start.

Folgen: (1) Der Schluss des Dirigenten aus „zwei Läufe gesund, zwei Läufe
krank" trug nicht; die Ursachensuche mit Pflicht-Gegenprobe hat ihn korrigiert,
bevor Code geändert wurde. (2) **M-88 wird berichtigt:** das Etikett „heute
rot (Stand `64ece9e1`)" aus §31.3 entfällt — am Stand `64ece9e1` ist das
Szenario in den Läufen des Bauers grün (A-67); M-88 ist **Regressionswache**,
Rotbeweis wie in §32.7 (reguläres Schließen des runner-eigenen FL → Szenario
Exit 4; `std::exit` im Pluginquelltext → Kanonteil rot). R-312-19 bleibt. (3)
Der Auslöser geht ins Register (NAK-352), nicht in dieses Ticket. (4) **Die
Erstprüfung der Etappe 5 hängt nicht mehr an LZ-1** und geht über
`12300f1e...<Commit dieses Abschnitts>`: Änderungssätze `19ddf5d3` und
`be37d10a` der Etappe und der Kanonteil der Nacharbeit 1 aus `2c138b2d`.

### 33.3 Übergabe an die frische Dirigentensession

Die Session `bbee07a1` endet im sauberen Fenster wegen des Kontextstands (über
50 % gemessen aus dem Telemetrie-Cache, Skill §5): kein Worker, kein Kanon, kein
Beobachter, kein Cron-Job, Baum sauber, alles gepusht. Die nächste Session:

1. startet die **Erstprüfung der Etappe 5** mit dem liegenden Auftrag
   `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-auftrag.txt` (Vorlage A, T2;
   frischer, lesender Prüfer — bis zum Codex-Wochenreset am 22.09.2026, 22:19
   Uhr Opus max, danach `gpt-6-astra` max); Rohurteile mit Tragweite lässt sie
   wie in §23 und §29 an der Quelle validieren, auch bei PASS. Dem Prüfer ist
   die Produktwirkung 1 aus §30.14 zur Einordnung aufgegeben;
2. trägt beim Abschluss der Etappe 5 die Registerzeile zur JUCE-eigenen
   Parametersperre ein (Wortlaut §7.5; es gilt der **Umbau**, §30.14);
3. lässt vor dem Start der Etappe 6 den Nebenfund NAK-349
   (Kennungskonflikt-Panel) an der Quelle validieren;
4. nennt lesende Prüfer und Validierer weiter nach Belieben — das Cockpit meldet
   für sie „Startbeleg fehlt" (NAK-350, nur Anzeige);
5. hat sechs offene Karten beim User (U11, U13, U26, U50, U52, U57).

Arbeitsmuster, das sich bewährt hat: die Szenariozahlen der Laufzeit-Kopfzeile
gegen den Vorlauf lesen (ein toter Host erscheint sonst als VORAUSSETZUNG wie
U43, NAK-351); einen Laufzeit-Befund erst als Ursache beauftragen und nie als
Fix, mit Pflicht-Gegenprobe am Vorstand; bei einem blockierten Worker zuerst
das Logende lesen. **Versäumt und nachzuholen als Regel:** vor der Einordnung
eines Laufzeit-Befunds das Register nach dem Symptom durchsuchen — NAK-307
(15.09.2026) beschrieb dasselbe Bild (`u40-aktivitaetsgate` Exit 3 nach
`snapshot-runde01`), der Dirigent hat es vor §31.3 nicht gesucht und LZ-1
deshalb zu hoch und gegen den falschen Stand eingeordnet.

## 34. Erstprüfung der Etappe 5: PASS, Validierung, Einordnung, Abschluss der Etappe 5 (Dirigent, 21.09.2026, 20:15 Uhr)

### 34.1 Rohurteil

**Erstprüfung der Etappe 5: Rohurteil PASS.** Frischer, lesender Opus-Thread
`45f8c291`, Effort max, `dontAsk`, Vorlage A, T2, Start 19:08 Uhr, `done`
gemessen 19:39 Uhr (Startbeleg und Ende vom Dirigenten gemessen), HEAD
`6ffc8578269a5cc0b4e7a2d08256d449131a7b95` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Prüfbereich `git diff 12300f1e...0014d5f7` über die zehn
Pfade des Auftrags; geschrieben wurde nur der Bericht
`docs/beweise/roh/NAK-312-etappe-5-erstpruefung-urteil.md`. 0 Defekte, zwei
Lücken (L-1, L-2), vier Härtungen (H-1 bis H-4).

### 34.2 Validierung an der Quelle

Frischer, lesender Opus-Thread `048ab14b`, Effort max, `dontAsk`, Start 19:41
Uhr, `done` gemessen 20:12 Uhr, HEAD
`cf410c58c46f320e53b5ee324ee912da369548d9` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Auftrag
`docs/beweise/roh/NAK-312-etappe-5-erstpruefung-validierung-auftrag.txt`,
Bericht `docs/beweise/roh/NAK-312-etappe-5-erstpruefung-validierung.md`:
**3 bestätigt** (V-1 Mechanik von H-1, V-3 kein Produktaufrufer von Vorschau
und Hörmatrix, V-8 tragende Entlastungen), **5 präzisiert** (V-2 Erreichbarkeit
von H-1, V-4 Zusagenlage zu L-1, V-5 Fenster von L-2, V-6 Matrixkopf gegen
M-40, V-7 H-3 und H-4), **0 widerlegt**.

### 34.3 Einordnung (Dirigent, E-312-16)

Vor der Einordnung im Register nach dem Symptom gesucht (Lehre aus §33.3):
`setupProcessing` und `setNonRealtime` stehen in `docs/offene-punkte.md` nur in
NAK-345 (Behauptungszeile), kein früherer Punkt beschreibt einen der Befunde.

- **L-1 — LÜCKE mit Regel R-312-20, kein Produktentscheid.** Eine WÄHREND
  eines laufenden Renders gesetzte Vorschau oder ein dort gesetzter Hörwunsch
  ist im Render unhörbar (Riegel, `DspKern.cpp:1225`); ihr Bestand nach dem
  Render ist von Gate und Matrix nicht geregelt (V-4). Der Satz aus §30.14
  „wird nach dem Render hörbar" beschreibt keinen Automatismus: der Wechsel
  stellt Processed, `bereiteVor` und `freigeben` ebenso; hörbar würde sie nur
  durch einen neuen Candidate-Wunsch (Prüfer (d), V-4). Eine Karte an den User
  entfällt: der Entwurf entscheidet den Weg über Gen vor (§33.4 Lease höchstens
  400 ms ohne Erneuerung; §41.3 hörbare Vorschau nur im Realtime-Modus bei
  laufendem Transport, Rückkehr bei Stop, Lease-Ablauf und Epochenwechsel), und
  heute hat die Sonde keinen Produktaufrufer von `setzePreview` (V-3); der erste
  entsteht in S29–31. **R-312-20:** Ein während des Offline-Renders gesetzter
  Hörwunsch (Dry, Delta, Candidate) wirkt als Processed (Entwurf `:2748`,
  E-312-7); der Bestand einer im Render gesetzten Vorschau nach dem Render
  gehört zu den Lease-Regeln von S29–31, nicht zu diesem Ticket. Die
  Riegelzeile bekommt ihren messenden Fall (neue Matrixzeile **M-89**), weil
  eine Mutation, die sie entfernt, heute grün bliebe (H-2 Punkt 1, V-6 a).
- **L-2 — LÜCKE mit Regel R-312-21, keine Nacharbeit.** Ein beim Rückweg noch
  nicht verbuchtes Vorschauende beendet die im nächsten Kontrolltakt aktive
  Vorschau, auch eine nach dem Rückweg oder im ersten Taktintervall des Renders
  neu gesetzte (V-5; Verhaltensänderung der Etappe, klickfrei). **R-312-21:**
  Ein anstehendes Vorschauende darf eine im selben Taktfenster neu gesetzte
  Vorschau beenden; das Ende einer Vorschau ist die sichere Richtung (Rückkehr
  zum gespeicherten Zustand, Entwurf §41.3 fail-closed). Die Wiederaufnahme
  einer gehaltenen Vorschau regelt der Renew-Weg von S29–31. Kein Fall, kein
  Umbau in diesem Ticket.
- **H-1 — HÄRTUNG (Register NAK-355), mit Auslöser vor S29–31.** Bestätigt
  (V-1): bei GLEICHZEITIGEN, GEGENSÄTZLICHEN `setNonRealtime`-Aufrufen aus zwei
  Threads können `offlineGesehen` und Riegel auseinanderlaufen. Präzisiert
  (V-2): der Ausschluss trägt nur über den VST3-Vertrag
  (`ivstaudioprocessor.h:307`), nicht über den Wrapper — `setupProcessing` nimmt
  weder den Callback-Lock noch JUCEs FL-Sperre; für FL ist er Annahme, nicht
  Messung. Liefe `setupProcessing` neben `process`, liefe schon der Wrapper in
  einen Datenwettlauf (`bufferMapper`), unabhängig von der Sonde. Gerechnet vom
  Dirigenten, nicht gemessen: weil der Wrapper `setNonRealtime` vor JEDEM Block
  mit dem Modus des Blocks ruft, heilt der nächste Block, dessen Modus von
  `offlineGesehen` abweicht, beide Verschränkungen; stehen bleibt der Zustand
  nur, wenn der Host einen Moduswechsel ankündigt und danach Blöcke im alten
  Modus liefert. Heute ist keiner der Fälle hörbar (kein Produktpfad setzt
  einen anderen Hörwunsch als Processed, V-3). Keine Zusage von Gate oder
  Matrix spricht über gegensätzliche Aufrufe aus zwei Threads; der Kommentar
  `SondeProcessor.cpp:1571-1572` trägt nur für gleiche Werte. Die von R-312-3
  verlangte datierte Registerzeile („Ob FL vor einem Offline-Render
  deaktiviert …") fehlte am HEAD (V-2) und steht jetzt in NAK-355: vor S29–31
  wird FLs Renderweg gemessen oder der Übergang auf EIN Atomic mit
  Vergleichstausch gestellt.
- **H-2 — LÜCKE mit Regel R-312-22 (Messung), nur Tests.** Bestätigt und
  präzisiert (V-6): kein Fall setzt einen Hörwunsch unter dem Riegel (→ M-89,
  R-312-20); kein B7-Fall fährt den Rückweg ohne dazwischenliegenden Block, die
  Rücknahme der Anforderung (`DspKern.h:349`) ist damit ungemessen; der
  Matrixkopf (§6, „die Echtzeitzeilen M-09, M-26, M-38, M-40, M-53 und M-60
  messen es je Änderungssatz") weist M-40 als Echtzeitzeile des
  Offline-Änderungssatzes aus, Zusage und Fall von M-40 messen aber nur
  gemeldete Sperren — die Allokationshälfte der Gate-GRENZE ist für den
  Übergang gelesen (nur Atomics, V-8 a), nicht gemessen. Am Produktcode bricht
  nichts. **R-312-22:** Auch ein Hin- und Rückweg OHNE dazwischenliegenden Block
  hinterlässt für den Echtzeitbetrieb kein hartes Schalten (neue Matrixzeile
  **M-90**, dient M-43 und [SONDE-015] M-55); 312/M-40 misst zusätzlich
  `RtWache::allokationen()` = 0 über `setNonRealtime` und die 200 Blöcke
  (Zusage von M-40 um diesen Halbsatz erweitert). Gebaut als **Zusatz der
  Etappe 6** (nur `eq-copilot/plugin/tests/TransactionTestMain.cpp` und die
  B7-Behauptungszeile), je Zeile mit Rotbeweis an der Zeile, die die Zusage
  trägt.
- **H-3 — HÄRTUNG (Register NAK-355), ein Rotlauf als Zusatz.** Präzisiert
  (V-7 a): die B2-Hälfte von 312/M-49 ist nie rot gefahren; die Matrixzeile
  verlangt einen Bruch je Zeile, nicht je Bein. Weil der Gate-Satz „Rotbeweisen
  an B2" nennt und bis Etappe 5 keine Rohdatei einen roten B2-Lauf zeigt, fährt
  der Bauer der Etappe 6 ihn einmal (ein verändertes Byte der Bin-Kopie, nur
  lokal, nicht committet) und legt die Rohdatei ab.
- **H-4 — HÄRTUNG (Register NAK-355), Behauptungszeile als Pflichtzusatz.**
  Präzisiert (V-7 b): der Textriegel 312/M-88 liest `*.cpp;*.h`, die
  B7-Behauptung sagt „kein Quelltext unter … core und hostbridge" und reicht
  damit um die zwei getrackten `.h.in`-Vorlagen weiter, als das Bein misst
  (heute wahr, von Prüfer und Validierer nachgesucht). Wie bei E-312-15 gilt
  der Auftragssatz „keine Zeile behauptet mehr, als ihr Bein misst": der Bauer
  der Etappe 6 nimmt `*.in` ins Muster. Schwelle 100 gegen Bestand 103 und
  weitere Wörter der Liste bleiben Härtung im Register.

Neue Matrixzeilen (Spalten wie §6.5):

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-89 | Erstprüfung Etappe 5 L-1, H-2 · R-312-20 | echter `SondeProcessor` mit gesetzter Vorschau, `setNonRealtime (true)` ist gelaufen | im Render nacheinander `setzeHoermatrix` Dry, Delta, Candidate, je Blöcke der Größen 1, 64, 256 | **Jeder Offlineblock ist ab Sample 0 bitgleich zum bestätigten Lauf, gleich welcher Hörwunsch im Render gesetzt wird** | der Riegel wirkt im Audiothread vor der Wahl des Hörstands | B7 **NEU** `312/M-89` | die Riegelzeile (`DspKern.cpp`, `offlineRiegelAn` → Processed) entfernen → rot | Entwurf `:2748`; E-312-7; [SONDE-015] M-120 | 6 (Zusatz) |
| M-90 | Erstprüfung Etappe 5 H-2 · R-312-22 | echter `SondeProcessor`, Hörmatrix Processed | `setNonRealtime (true)`, sofort `setNonRealtime (false)` ohne Block dazwischen, danach Echtzeitblöcke mit Wechsel auf Candidate | **Der erste Echtzeitblock nach einem Offline-Fenster ohne Block schaltet WEICH: der Nachbarsprung am Umschaltsample liegt innerhalb der E-31-Schranke** | der Rückweg nimmt die Anforderung zurück, bevor er den Riegel löst (A-53) | B7 **NEU** `312/M-90` | die Rücknahme der Anforderung im Rückweg (`DspKern.h`, `setzeOfflineRiegel (false)`) entfernen → rot | M-43; [SONDE-015] M-55, E-31 | 6 (Zusatz) |

### 34.4 Urteil und Stand

**Erstprüfung der Etappe 5: PASS.** Kein Defekt. Rundenbudget der Etappe 5:
eine von drei Runden verbraucht (Nacharbeit 1, Ursachensuche zu LZ-1, ohne
Produktcode). Rundenbilanz (gemessen, `tools/dirigent/rundenbilanz.py --runden
12300f1e 64ece9e1 0014d5f7`): Baurunde Produkt 4 Dateien +195/-44, Tests 2
Dateien +882/-18, Prüfwerkzeug 1 Datei +3/-3; Nacharbeit 1 Produkt 1 Datei
+3/-3 (Installer-Hashes), Tests 1 Datei +86/-1, Prüfwerkzeug 1 Datei +1/-1;
Werkzeugurteil: kein Konvergenz-Signal. **Etappe 5 ist damit abgeschlossen.**
Die Registerzeile zur JUCE-eigenen Parametersperre steht mit dem Wortlaut aus
§7.5 (es gilt der Umbau, §30.14) als NAK-356 in `docs/offene-punkte.md`.

**Stand der Etappen.** 0 bis 5 abgeschlossen. Offen: Quellvalidierung des
Nebenfunds NAK-349 vor dem Start der Etappe 6, Etappe 6 (Gen-Fläche und
Hörmarkierung), 7 (Produktabhängiges nach U48, U49, U51, U56 und
gegebenenfalls U57, mit eigenem Matrixnachtrag), Abschlussprüfung T2 über
Basis...HEAD mit vollem Kanon und Laufzeit-Arm.

**Zusätze für den Bauer der Etappe 6** (nur Tests und Behauptungszeilen, kein
Produktcode): (1) M-89 nach R-312-20; (2) M-90 und der Allokationszähler in
312/M-40 nach R-312-22; (3) ein roter Lauf der B2-Hälfte von 312/M-49 (H-3);
(4) `*.in` im Muster des Textriegels 312/M-88 (H-4).

**Betrieb (User 21.09.2026, 20:0x Uhr):** Ab 90 % Claude-Woche (gemessen 20:12
Uhr: 84 %) startet kein Opus-Lauf mehr; nach dem Codex-Wochenreset (22.09.2026,
22:19 Uhr) baut Codex `gpt-5.6-sol` xhigh und prüft `gpt-6-astra` high, Claude
läuft nur noch als Dirigent (Dirigenten-Skill §3.6).

## 35. Quellvalidierung NAK-349, Teilung der Etappe 6 in 6a und 6b, Auftrag 6a (Dirigent, 21.09.2026, 20:47 Uhr)

### 35.1 Quellvalidierung NAK-349 (Kennungskonflikt-Panel)

Frischer, lesender Opus-Thread `75dd0645`, Effort max, `dontAsk`, Start 20:21
Uhr, `done` gemessen 20:47 Uhr, HEAD
`9f6c0f074a8fa05365ecf6dab91807e366a95d7e` vor und nach dem Lauf gleich (vom
Dirigenten gemessen); Auftrag
`docs/beweise/roh/NAK-349-quellvalidierung-auftrag.txt`, Bericht
`docs/beweise/roh/NAK-349-quellvalidierung.md`: **3 bestätigt, 3 präzisiert,
0 widerlegt.** Der Bericht vermerkt selbst eine Abweichung vom Auftrag (ein
versehentliches `python3 --version`, ohne Datei und ohne Anteil an einem
Urteil).

- **Der Weg ist bestätigt (V-1, V-2).** Der Rückruf in `zeigeKonflikt()`
  (`eq-copilot/plugin/src/PluginEditor.cpp`) fängt rohes `this`; ein per Enter
  oder Barrierefreiheit ausgelöster Klick ist eine gepostete Nachricht, und der
  Fokus liegt nach dem Öffnen schon auf dem Knopf. Baut der Host den Editor
  zwischen Einstellen und Zustellung ab, steht die Aufräumnachricht des
  Modal-Managers HINTER dem Klick: der Rückruf läuft bei lebender Box auf totem
  Editor. Präzisiert: zwei Nutzergesten „Enter, dann Fenster schließen" öffnen
  den Weg bei ungefilterter Pumpe nicht; offen ist er nur bei bestimmtem
  Hostverhalten (Abbau in der Behandlung derselben Tastennachricht, aus einer
  gesendeten Nachricht, aus gefilterter Pumpe oder aus einem früheren
  JUCE-Rückruf derselben Runde) — nicht gemessen. Ein Mausklick beginnt immer
  bei lebendem Editor.
- **Die Folge ist nicht harmlos (V-3).** Der erste Zugriff liest
  `this->processor` aus freigegebenem Speicher, danach `neueSensorId()` (neue
  `instanceId`, Host-Dirty, Reconnect beider Clients) und zwei Schreibzugriffe
  in den toten Editor: Zugriff auf freigegebenen Speicher, undefiniertes
  Verhalten; der Prozessor lebt in diesem Moment nicht sicher.
- **Einziger verbliebener Weg (V-4).** Hinweisliste und Messpunktpanel fangen
  einen `SafePointer`; das Konfliktpanel ist der einzige CallOutBox- oder
  Dialogweg des Editors mit rohem `this`.
- **Zusagenlage (V-5).** R-312-2 deckt den Fall im Wortlaut („modales Panel,
  verzögerter Aufruf … nach dem Ende seines Eigentümers ist er wirkungslos: kein
  Zugriff, keine Mutation, keine Dirty-Meldung. Der Handgriff bleibt derselbe.");
  Gate und Matrix führen nur das Messpunktpanel.
- **Keine Berührung mit den Änderungssätzen der Etappe 6 (V-6);** die Sicherung
  teilt mit ihr Ticketpfade (`PluginEditor.cpp`, `ShotTestMain.cpp`) und das
  Bein B15 und steht dort als eigener Änderungssatz.

### 35.2 Einordnung (Dirigent, E-312-17)

**DEFEKT gegen R-312-2, gebaut in Etappe 6b als eigener Änderungssatz.** Die
Regel gilt seit Etappe 0 für jeden asynchronen Rückruf des Editors; das
Konfliktpanel erfüllt sie nicht. Der Fix ist der der Hinweisliste
(`SafePointer` im Rückruf, Lebendprüfung vor jedem Zugriff), keine neue
Oberfläche, der Handgriff bleibt derselbe. **R-312-23 (Marke im toten
Editor):** Liegt der Prozessorverweis im Editor selbst, steht die gezählte
Marke nach R-312-12 VOR jeder Dereferenzierung von `this` — im Basislauf als
erste Anweisung des Rückrufs ohne Editorzustand, nach dem Bau hinter der
Lebendprüfung; sonst liefe schon der Rotlauf in undefiniertem Verhalten (V-5
Präzisierung). Der Kanarienvogel folgt der Lesart von B15 (`lebt = false`
nach `editor.reset()`).

Neue Matrixzeilen (Spalten wie §6.4):

| ID | Befund | Zustand und Vorbedingung | Ereignis | Zusage | Reihenfolge und Frist | Test | Rotbeweis | Quelle | Etappe |
|---|---|---|---|---|---|---|---|---|---|
| M-91 | NAK-349 · R-312-2 · R-312-23 | Gen in Legacy-Rolle auf dem Heap, Editor offen, Kennungskonflikt-Popover steht | `neuKnopf.triggerClick()` (der gepostete Weg von Enter), danach Editor zerstören, in der zweiten Variante danach auch den Prozessor, DANN die Nachrichtenschleife pumpen | **Der Rückruf ist nach dem Ende des Editors wirkungslos: die Marke zählt 0 Zugriffe nach dem Ende, die `instanceId` ist unverändert, 0 Host-Dirty-Meldungen** | Lebendprüfung über `SafePointer` VOR jedem Zugriff auf Editor und Prozessor | B15 **NEU** `312/M-91 konfliktrueckruf_nach_editorende_ist_wirkungslos`, zwei Varianten | **heute rot** — Gegenprobe am Basisstand: die Marke als erste Anweisung des Rückrufs zählt 1 und hält den Rückruf an (R-312-23), nie ein Lauf in undefiniertem Verhalten | R-312-2; `docs/beweise/roh/NAK-349-quellvalidierung.md` V-1 bis V-3 | 6b |
| M-92 | NAK-349 · öffnen↔schließen · Handgriff bleibt | wie M-91, Editor lebt | `neuKnopf.triggerClick()`, Nachrichtenschleife pumpen, danach Panel, Editor und Prozessor zerstören | **Der normale Handgriff bleibt derselbe: die Instanz bekommt eine neue Kennung, genau EINE Host-Dirty-Meldung, die Statusmeldung steht, die Box schließt** | der Klick läuft bei lebendem Editor wie bisher | B15 **NEU** Teilfall von `312/M-91` | **Regressionswache**; einmal gebrochen: die Lebendprüfung unbedingt scheitern lassen → keine neue Kennung → rot | R-312-2 (letzter Satz); `CLAUDE.md` (keine toten UI-Elemente) | 6b |

### 35.3 Teilung der Etappe 6 und Auftrag 6a

Die Etappe 6 wird wie die Etappe 3 in zwei Teilen gebaut, mit EINEM vollen
Kanon und EINER Erstprüfung am Ende von 6b:

- **6a — Hörmarkierung und Test-Zusätze:** die A3-Zeilen M-55 bis M-65, M-84
  und M-86 in zwei Änderungssätzen (Rollenwechsel und drei Netze; reset und
  releaseResources), dazu als dritter Änderungssatz die vier Test-Zusätze aus
  §34.4 (M-89, M-90, Allokationszähler in 312/M-40, roter B2-Lauf von
  312/M-49, `*.in` im Textriegel 312/M-88). Einzelbeine, kein voller Kanon.
  Auftrag `docs/beweise/roh/NAK-312-etappe-6a-auftrag.txt`. Bauer: frischer
  Opus-Worker, Effort xhigh; jeder Änderungssatz endet an einer sauberen,
  gepushten Grenze, damit der Stopp bei 90 % Claude-Woche (§34.4, Skill §3.6)
  keinen halben Stand hinterlässt.
- **6b — Gen-Fläche:** die B15- und B14-Zeilen M-66 bis M-76 und M-85
  (Labelentwurf, Ersatzziel), der Änderungssatz NAK-349 (M-91, M-92), M-77 und
  der volle Kanon; danach Laufzeit-Arm und Erstprüfung der ganzen Etappe 6.
  Bauer nach dem Kontingentstand: Opus xhigh oder, nach dem Codex-Wochenreset,
  Codex `gpt-5.6-sol` xhigh.

## 36. Etappe 6a — Hörmarkierung und Test-Zusätze (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-6a-auftrag.txt`; Spezifikation §1
(T3-07-02, T3-01-10), §2 (R-312-5, R-312-8, R-312-12, R-312-14), §5.0, §5.9,
§5.10, §6 Kopf, §6.7 (M-55 bis M-65, M-84, M-86), §7.0, §7.1, §7.6, §7.7, §8,
§10.2 (U56), §11.3 (E-312-8, R-312-12), §14.4 (R-312-13, R-312-14), §34
(R-312-20, R-312-22, M-89, M-90, M-40, H-3, H-4). Drei Änderungssätze mit je
eigenem Commit; jeder Unterabschnitt gilt zum Endstand seines Änderungssatzes.
Uhrzeiten aus `Get-Date`, Binaryzeitstempeln oder Commitzeit. Fremde
Matrixzeilen tragen ihr Präfix (R-312-13).

### 36.1 Start, Basis-SHA, Kanon vorher

- Start 20:49:32 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `fd287d3c8ea277814c80e67365ea782e6af9994d`
  (Basis-SHA der Etappe 6a); `git status --short` zeigt nur `briefing-hub/`
  und `nimbalyst-local/`. Vor jedem Bau lief kein zweiter Bau- oder
  Testprozess (NAK-290); das laufende `FL64` gehört dem User und blieb
  unberührt.
- **Kanon vorher: geerbt.** `git diff --stat 2c138b2d HEAD -- eq-copilot broker
  tools` zeigt allein `eq-copilot/install/nakama-installer-v1.json` (3 Zeilen).
  Nach dem Auftrag gilt der Kanon nachher der Nacharbeit 1 der Etappe 5, GRUEN
  70/70 am Stand `2c138b2d` (`docs/beweise/roh/NAK-312-2c138b2-dirty.md`,
  §32.9).
- Bauplanzeilen am Basis-SHA nachgelesen: `git diff --stat 945ebfb1 fd287d3c`
  über die acht Dateien der Ticketgrenze zeigt allein
  `eq-copilot/plugin/src/PluginEditor.cpp` (+49/−9, alles hinter Zeile 700).
  Damit gelten `State.cpp:262-273` (Store `:272`), `PluginProcessor.cpp:874`,
  `PluginProcessor.h:111-114`, `Hostbruecke.cpp:34-147`, `HoerMarkierung.h`
  (`:297-320`, `:411-414`, `:420-429`, `:472`, `:484-491`, `:619-641`,
  `:665-685`, `:760-765`) und `PluginEditor.h:50-51`, `:60-68`, `:71` wie im
  Bauplan; in `PluginEditor.cpp` stehen `timerCallback` (`:242`, Abzweigung
  `:245`, Rücksprung `:257`, Netze `:268-278`, Aus-Knopf `:279`) wie im Bauplan,
  `schalteMarkierung` aber bei `:794-855` (Einreichen `:840`, Bauplan `:800`),
  `markierungBeenden` bei `:857-866`, `wechsleFlaecheWennNoetig` bei `:873-912`.

### 36.2 Gebaut — Änderungssatz A (Rollenwechsel und drei Netze; T3-07-02)

Commit: dieser Änderungssatz (Betreff „NAK-312 Etappe 6a: Änderungssatz A …");
sein SHA steht im Abschnitt des Änderungssatzes B. Zeilen zum Endstand A.

- `eq-copilot/plugin/src/prozessor/State.cpp:262-291`, `spiegleKlassifikation`:
  **vor** dem Store prüft der Prozessor, ob `lebenslauf.audioAusnahmeErlaubt()`
  vom gespiegelten Wert abweicht; tut sie das und steht beim Publisher ein
  Auftrag (`markierung.zielGesetzt()`), ruft er `markierung.reicheAus()`, dann
  erst den Store (`:290`, weiter `seq_cst`). Das deckt `setzeBindung` und beide
  Zweige von `setStateInformation`, in beide Richtungen (E-312-8). Der
  Kommentar, den der Änderungssatz falsch machte („Gelesen wird im processBlock
  relaxed - dort haengt kein anderer Wert daran"), ist ersetzt (R-312-14);
  `:264-270` bleiben.
- `eq-copilot/plugin/src/PluginProcessor.cpp:874-877`: `istMainKlassifiziert`
  wird im Audiothread mit `acquire` gelesen.
- `eq-copilot/plugin/src/PluginEditor.cpp`: `timerCallback` (`:242-346`) — die
  drei Netze (Freilauf, Samplerate, Totmann) stehen jetzt **vor** der
  Abzweigung in die Main-Fläche (`:246-259`, Abzweigung `:261`), ihr Kommentar
  ist mitgezogen; der Aus-Knopf folgt `markModus` in der Legacy-Fläche
  (`:284-285`). `schalteMarkierung` (`:800-874`) fragt vor dem Einreichen
  (`:859`) denselben Term wie das Audio (`holeKlassifikation() ==
  Klassifikation::main`, `:846-857`) und meldet sonst „Markierung nicht möglich —
  nur ein Main färbt hörbar, diese Instanz bleibt neutral." (M-58).
  `wechsleFlaecheWennNoetig` (`:892-945`, Räumung `:894-906`) räumt nur Anzeigezustand: ein Latch,
  dessen Klassifikation nicht mehr Main ist, wird ausgeschaltet, der Status sagt
  „Markierung beendet — diese Instanz ist nicht mehr Main."; einen Auftrag nimmt
  der Editor hier nicht zurück (A-73).
- `eq-copilot/plugin/src/PluginEditor.h`: Testzugänge unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` (`:68-85`: `timerTickFuerTest`,
  `schalteMarkierungFuerTest`, Leser von Latch, Aus-Knopf, Status und Fläche,
  `letzteBedienungVorFuerTest`, A-71); der Kommentar zur Latch-Liste (`:109-112`)
  nennt jetzt den Klassifikationswechsel und die Main-Bedingung (R-312-14).
- `eq-copilot/plugin/src/PluginProcessor.h:662-668`: Testzugänge
  `meldeFreilaufFuerTest` und `markierungZielGesetztFuerTest` unter demselben
  Define (A-70).
- `eq-copilot/plugin/tests/MarkierungTestMain.cpp`: Kopfkommentar ergänzt;
  Namensraum `nak312` (`Stand` mit Heap-Prozessor, Transport über die Brücke,
  bestätigter Senderpause, Gleichanteil 0,3, Blockzähler, synchroner Ernte des
  Senders) und die Fälle `nak312M55` (mit M-58 und M-57), `nak312M57Editor`,
  `nak312M56`, `nak312M84`, `nak312M84ImAusfade` (`:1421-1849`), aufgerufen aus
  `main` (`:2011-2015`).
- Nicht angefasst: `Hostbruecke.cpp`, `HoerMarkierung.h` (nur für die Mutation
  von M-84, bytegleich zurück), der weiche Ausfade, die Löschstelle
  `State.cpp` `mainProjectMitglieder` (T3-02-06), `uebernehmeSourcesLabel`,
  `aktualisiereSourcesSteuerung`, `sourcesZeilen`, `SourcesModel.cpp`,
  `ShotTestMain.cpp`, `tools/beweise.ps1`, Schemas, Fixtures, Identität,
  `design/`.

### 36.3 Gegenprobe am Basisstand (heute rot) — Änderungssatz A

`docs/beweise/roh/NAK-312-etappe6a-gegenprobe-A.txt`, Bau 21:24:56 bis
21:25:03, Lauf 21:25:03 bis 21:25:11: die drei Produktdateien des
Änderungssatzes bytegleich auf `fd287d3c` (SHA-256 je Datei, `git diff HEAD`
leer), dazu die Testzugänge und die Tests in der Endfassung. A3 Exit 1,
**14 Fehler — genau die „heute rot"-Prüfungen**: M-58 (drei Prüfungen: Auftrag
eingereicht, Latch SOLO mit Aus-Knopf, Status „Einfärben: SOLO — nur dieser
Bereich spielt."), M-55 (40 abweichend, 40 hörbar, 1 im Ring; der Sender findet
1 begin), M-57 (8 abweichende Blöcke nach dem Rückwechsel, 1 im Ring; Auftrag
und Latch stehen; Editorhälfte Latch SOLO), M-56 (a, b, c: Latch bleibt, Status
unverändert, a und c färben bis Block 39), M-84 zweite Hälfte (40 abweichend,
ein neues begin; 44 544 statt 24 064 Samples gehört) und der Teilfall im
laufenden Ausfade. Grün, wie das Etikett sagt: die erste Hälfte von M-84
(Nachbarsprung 0,0000781250 ≤ 0,0000782442, Paar begin/end im Ausfade) als
Regressionswache. Ein erster Gegenprobelauf um 21:05 mit einer früheren
Testfassung ist verworfen: dort räumte die echte Freilauferkennung
(`lebenszeichen`, Wanduhr) den Latch in M-57 zufällig ab; seither verwirft der
Tick-Helfer das Freilaufsignal vor jedem Tick, und nur 312/M-56 (a) setzt es
gezielt (A-75).

### 36.4 Matrixzeilen des Änderungssatzes A

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-55 | `312/M-55 rollenwechsel_zu_main_entwaffnet_den_marker`, A3 | grün: in Legacy 40 Blöcke bitgleich, Ring leer; nach dem Wechsel zu Main, ohne Tick, 40 Blöcke 0 abweichend, 0 hörbar, 0 im Ring; der Sender findet 0 begin, 0 end | heute rot (Gegenprobe); Rücknahme vor dem Store entfernt → 40 abweichend, 1 im Ring, 1 begin → rot; `docs/beweise/roh/NAK-312-rot-M-55.txt` |
| M-56 | `312/M-56 die_drei_netze_ticken_in_jeder_flaeche` (a Freilauf, b Samplerate, c 10 Minuten), A3 | grün: in der Main-Fläche je Latch aus und Auftrag zurückgenommen, Grund im Status, der Ausgang ab Block 8 (a, c) bzw. sofort (b) bitgleich, danach still | heute rot (Gegenprobe); der Main-Zweig samt `return` wieder vor den Netzen → a, b, c rot; `docs/beweise/roh/NAK-312-rot-M-56.txt` |
| M-57 | `312/M-57 rueckwechsel_hinterlaesst_keinen_scharfen_marker` (Teilfall von 312/M-55) und `312/M-57 editorhaelfte_kein_aus_knopf_ohne_auftrag`, A3 | grün: nach sensor, hub, sensor 40 Blöcke bitgleich, 0 im Ring, kein Auftrag, kein Latch, Aus-Knopf unsichtbar; Editorhälfte: nach dem Wechsel weg von Main kein Latch, Aus-Knopf unsichtbar, Status „diese Instanz ist nicht mehr Main" | heute rot (Gegenprobe); Mutation wie M-55 → rot; Editorhälfte: Anzeigeräumung entfernt → rot (A-73); `docs/beweise/roh/NAK-312-rot-M-57.txt` |
| M-84 | `312/M-84 rollenwechsel_weg_von_main_schliesst_das_intervall` und Teilfall `teilfall_rueckwechsel_im_laufenden_ausfade`, A3 | grün: 1 begin und 1 end mit derselben `intervention_id`, `tail_samples` 52 928 = 2 · 24 064 + 4 800; Nachbarsprung am Umschaltsample 0,0000781250 ≤ 0,0000782442, größter Sprung 0,0000781417; nach dem zweiten Wechsel zu Main 40 Blöcke bitgleich, kein neues begin; Rückwechsel mitten im Ausfade: kein Wiedereinblenden, 1 begin, 1 end | zwei Hälften: Paar und weicher Ausfade Regressionswache — Rücknahme als Schnitt → Sprung 0,3000000119 → rot (A-72); zweiter Wechsel heute rot (Gegenprobe), Mutation wie M-55 → rot; `docs/beweise/roh/NAK-312-rot-M-84.txt` |
| M-58 | `312/M-58 legacy_solo_reicht_nichts_ein` (Teilfall von 312/M-55), A3 | grün: kein Auftrag beim Publisher, kein Latch, kein Aus-Knopf, Status „Markierung nicht möglich — nur ein Main färbt hörbar, diese Instanz bleibt neutral." | heute rot (Gegenprobe); Abfrage entfernt → Auftrag eingereicht, Status falsch → rot (A-76); `docs/beweise/roh/NAK-312-rot-M-58.txt` |

### 36.5 Einzelbeine — Änderungssatz A

Alle 15 Ziele, die nach `eq-copilot/plugin/CMakeLists.txt` `PluginEditor.cpp`
und die Gen-Prozessorquellen übersetzen (`EqCopilot_VST3`, `EqCopNullTest`,
`EqCopMarkierungTest`, `EqCopShot`, `EqCopPaintBench`, `EqCopIdentityTest`,
`EqCopStateMigrationTest`, `EqCopSonde013PassageStateTest`,
`EqCopLebenslaufTest`, `EqCopQueueStressTest`, `EqCopAnalysisGoldenTest`,
`EqCopSonde012ProjectReloadTest`, `EqCopSonde014IntentTest`,
`EqCopSonde014AssistentTest`, `EqCopBriefkastenTest`), in einem cmake-Aufruf
gebaut, 21:30:10 bis 21:32:57, Exit 0; jedes Binary jünger als die jüngste
Quelle (21:28:45, die letzte Rücknahme). Gefahren 21:33:04 bis 21:33:35:

- A3 `EqCopMarkierungTest`: Exit 0, 139 Prüfungen ok, „MARKIERUNGSTEST OK".
- B15 `EqCopShot --sonde012-suite`: Exit 0, 25 Shots bei 760x430, „NAK-312
  PANEL 3 geprueft, 0 Fehler".
- B14 `EqCopSonde012ProjectReloadTest`: Exit 0, 231/231.
- A1 `EqCopNullTest`: Exit 0, „NULLTEST OK".
- Zusätzlich, weil sie Klassifikation und Marker fahren: B8
  `EqCopLebenslaufTest` Exit 0, 82 Prüfungen; B23
  `EqCopSonde013PassageStateTest` Exit 0, 409 bestanden.
- clang-tidy-Teilmessung (`tools/plan/tidy.py --nur`): `src/PluginEditor` 15,
  `prozessor/State` 14, `src/PluginProcessor` 15 Fundstellen; die Treffer in
  `src/` (`PluginEditor.cpp` Initialisierung von `k` in `anzeigeKurve`,
  `PluginProcessor.cpp` Breitenumwandlung in der Pegelabfrage) stehen am
  Basis-SHA an `:1089` und `:913`, die übrigen in
  `eq-copilot/plugin/core/analysis/`; keine neue Fundstelle.

### 36.6 Abweichungen vom Bauplan, begründet — Änderungssatz A

- **A-70 — zwei Testzugänge in `PluginProcessor.h`.** Die Ticketgrenze nennt
  die Datei für reset und releaseResources. `meldeFreilaufFuerTest` braucht
  M-56 (a): das Freilaufsignal setzt im Produkt allein `lebenszeichen` an der
  Wanduhr, und die Matrixregel verbietet Zeilen, die an Wandzeit hängen.
  `markierungZielGesetztFuerTest` braucht M-58 und M-57: „es wird nichts
  eingereicht" ist nur am Publisher messbar, im Audio nicht (in Legacy färbt
  ein Auftrag ohnehin nicht). Beide stehen unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`; der Produktpfad ist unverändert.
- **A-71 — der Editor-Testzugang ist mehr als der Tick.** M-57 und M-58 messen
  UI-Zustand; der Editor hält Latch, Aus-Knopf und Status privat, und den
  Handgriff SOLO erreicht nur `schalteMarkierung`. Dazu der Totmann-Setter für
  M-56 (c). Alles unter dem Define, nach dem Muster der vorhandenen Zugänge.
- **A-72 — M-84, Form der Mutation.** „Die Rücknahme als `hartAus()` bauen":
  `hartAus()` ist Zustand des Audiothreads und vom Nachrichtenthread aus nur mit
  Datenrennen erreichbar. Gebaut ist der Schnitt dort, wo der Audiothread den
  Aus-Auftrag übernimmt (`HoerMarkierung.h`, Zweig in `verarbeite`); damit ist
  jeder Aus-Auftrag ein Schnitt, und T2 und T5 fallen mit (Grund in der
  Rohdatei). Der Rotbeweis fällt an der Schranke von [SONDE-015] E-31, die der
  Test von M-84 selbst misst.
- **A-73 — die Anzeigeräumung fragt die Klassifikation, nicht die Fläche.**
  §7.6 lässt `wechsleFlaecheWennNoetig` den Anzeigezustand räumen. Gebaut ist
  die Räumung als erster Schritt dieser Funktion, aber geknüpft an denselben
  Term wie Audio und Einschalten (Klassifikation ≠ Main), nicht an den
  Flächenwechsel: zwei Rollenwechsel zwischen zwei Ticks lassen die Fläche
  stehen, und ein geladener Stand kann die Klassifikation ohne Flächenwechsel
  verlassen (Rolle `hub`, Klassifikation nicht Main); in beiden Fällen stünde
  sonst ein Latch ohne Auftrag. Weil der Editor nach M-58 in Legacy gar keinen
  Latch mehr setzen kann, misst die Matrixzeile M-57 den Editor nur trivial;
  der zusätzliche Teilfall `312/M-57 editorhaelfte_kein_aus_knopf_ohne_auftrag`
  (Latch über den Editor in Main, dann weg von Main) trägt die Editorhälfte,
  mit eigener Mutation.
- **A-74 — Gleichanteil statt Sinus.** Die Fälle fahren 0,3 konstant: die Zone
  120 bis 300 Hz sperrt ihn, beide Seiten des Ausfades sind konstant, und der
  Nachbarsprung ist exakt gegen die Fadeschrittweite prüfbar (Muster A-50).
- **A-75 — „im Interventionsring" und „gezählte Hördauer", gemessen.** Der
  Sender des Workers ruht bestätigt (`senderAnhaltenFuerTest`,
  `warteAufSenderPauseFuerTest`), der Ring behält jedes Ereignis;
  gemessen werden sein Füllstand und die synchrone Ernte des echten Senders
  (Sendezug, Wire-Commit ohne Draht, Mitschnitt) mit Typ, `intervention_id` und
  `tail_samples`. Der Wiretext trägt keine Dauer; die Hördauer steht in
  `tail_samples` = 2 · Dauer + ein Zehntel der Rate (NAK-180 R5, im Test als
  Formel). Der Tick-Helfer verwirft das Freilaufsignal vor jedem Tick (§36.3).
- **A-76 — M-58 unter seiner Mutation.** Ohne die Abfrage fallen „nichts
  eingereicht" und der Status; die Prüfung „kein Latch, kein Aus-Knopf" bleibt
  grün, weil die Anzeigeräumung (A-73) den Latch beim nächsten Tick abräumt.
  Die Zusage fällt an ihrer Zeile.
- **A-77 — ein zusätzlicher Teilfall zu M-84.** `teilfall_rueckwechsel_im_laufenden_ausfade`
  misst den Punkt „Rollenwechsel während eines laufenden Ausfades" des
  Selbstaudits (am Basisstand rot, unter der Mutation von M-55 und unter dem
  Schnitt von M-84 ebenfalls).

### 36.7 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz A

| Abschnitt | Punkt | Änderungssatz A |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt; der Briefkasten der Markierung bekommt eine Publikation mehr nur, wenn ein Auftrag steht |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: Rollenwechsel hin, zurück, wieder hin in beiden Richtungen (312/M-55 mit M-57; 312/M-84), Wechsel im laufenden Ausfade (Teilfall), Editor vor dem Prozessor abgebaut |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | nicht zutreffend: kein Draht-, Schema- oder Statefeld; der Wiretext ist unverändert, gemessen über den echten Sender |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel gegen die jüngste Quelle bei jedem Bau, jedem Rotbeweis und der Gegenprobe; kein neues Bein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die A3-Behauptungszeile ist unverändert und sagt weniger, als das Bein jetzt misst (`tools/beweise.ps1` gehört nicht zur Ticketgrenze von A); die Statustexte sagen, was eintritt |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Kommentar `State.cpp` (R-312-14), Netzkommentar und Latch-Liste im Editor, Kopfkommentar von A3; Runnerzeile und Manifestkopf siehe §36.9 |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: fünf Rohdateien `docs/beweise/roh/NAK-312-rot-M-55.txt` bis `NAK-312-rot-M-58.txt` und `NAK-312-rot-M-84.txt`, eine Gegenprobe |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Blockzahlen, Sprünge, `tail_samples`, Prüfungszahlen und Uhrzeiten aus Läufen und `Get-Date` |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: aktivieren↔abklingen (Rücknahme im Klassifikationswechsel und der weiche Ausfade), öffnen↔schließen der Fläche mit der Anzeigeräumung, Publisher (`spiegleKlassifikation`) und Leser (`acquire` im Audiothread) derselben Ordnungsregel |

### 36.8 Selbstaudit — Änderungssatz A

- **Welcher Thread welches Atomic.** `istMainKlassifiziert`: geschrieben allein
  in `spiegleKlassifikation` unter `bindungMutex` (Nachrichten- oder Hostthread,
  `seq_cst`); dort selbst mit `relaxed` gelesen — derselbe Schreiber unter
  derselben Sperre sieht seinen letzten Store; im Audiothread mit `acquire`
  gelesen. `briefkasten`: der Publisher tauscht `acq_rel`, der Audiothread liest
  `acquire` und tauscht `acq_rel`. Kette: `reicheAus` (Tausch) ist vor dem Store
  sequenziert; ein Block, dessen `acquire`-Lesung den neuen Wert sieht, sieht
  danach den Briefkasten mit gesetztem Neu-Bit (Kohärenz: sein Lesen liegt nach
  dem Tausch). `zielGesetztAtomic`: Publisher schreibt und liest `relaxed`
  (derselbe Thread), `lebenszeichen` liest wie bisher. `freilaufKill`: schreibt
  der Audiothread, verbraucht der Editor per Tausch — jetzt in beiden Flächen.
  Den Nutzen von `acquire` kann ein Einthread-Bein nicht zeigen; er ist aus der
  Ordnung gerechnet, nicht gemessen.
- **`reicheAus`/`reicheEin` haben genau einen Publisher.** Aufrufer: der Editor
  (Einreichen, Aus, Destruktor) und `spiegleKlassifikation` aus `setzeBindung`
  (Rollenwahl im Editor) und `setStateInformation`. Der VST3-Vertrag ruft jede
  Pluginfunktion außer `IAudioProcessor::process` und `setProcessing` im
  UI-Thread (Steinberg, VST 3 Developer Portal, API Documentation, gelesen am
  21.09.2026); das ist JUCEs Nachrichtenthread. Der Kommentar
  `State.cpp` in `setzeBindung` hält dasselbe fest.
- **Rollenwechsel hin, zurück, wieder hin.** Zu Main (M-55), zurück (M-57),
  weg von Main und zurück (M-84), zurück mitten im Ausfade (Teilfall): jeweils
  kein Wiedereinblenden ohne Handgriff, genau ein `end` zu einem `begin`.
- **Kein harter Schnitt (NAK-47).** Die Rücknahme ist ein Aus-Auftrag; M-84
  misst den Ausgang am Umschaltsample gegen E-31, und die Mutation zum Schnitt
  fällt dort.
- **Stale Closures im Editor.** Der Änderungssatz legt keine Lambdas an; die
  drei Netze rufen `markierungBeenden` direkt aus dem Tick.
- **UI-Text, der lügt.** „Markierung nicht möglich — nur ein Main färbt hörbar,
  diese Instanz bleibt neutral." gilt in Legacy und unklassifiziert (§53.5:
  audio-neutral). „Markierung beendet — diese Instanz ist nicht mehr Main."
  erscheint nur, wenn ein Latch stand und die Klassifikation Main verlassen hat —
  über Rollenwahl oder geladenen Stand.
- **Behauptungszeile nicht weiter als der Test.** Die A3-Zeile ist unverändert
  und behauptet weniger, als das Bein misst.
- **Kommentare, die der Änderungssatz falsch macht (R-312-14).** Berichtigt:
  `State.cpp` (relaxed, „kein anderer Wert"), der Netzkommentar im Editor (jetzt
  am neuen Ort und mit „in jeder Fläche"), die Latch-Liste in `PluginEditor.h`.
  Nicht vom Änderungssatz falsch gemacht und deshalb unberührt: der Kommentar im
  Editordestruktor („hart … schneiden", seit NAK-47 überholt) — Hinweis §36.9.
- **Allokation und Sperre im Audiothread.** Der Änderungssatz ändert im
  Audiothread nur die Speicherordnung einer Lesung; `reicheAus` läuft auf dem
  Nachrichtenthread und allokiert nicht (drei vorallokierte Puffer).

### 36.9 Hinweise an den Dirigenten — Änderungssatz A

- **Produktwirkung — die Hörmarkierung hat keinen hörbaren Weg mehr (Register
  NAK-341).** In der Legacy-Rolle meldet SOLO oder PULS jetzt ehrlich
  „Markierung nicht möglich — nur ein Main färbt hörbar, diese Instanz bleibt
  neutral." statt „Einfärben: SOLO — nur dieser Bereich spielt." und zeigt
  keinen Aus-Knopf; in der Main-Fläche gibt es keinen Auslöser. Der einzige
  bisher hörbare Weg (in Legacy einreichen, dann zu Main wechseln) ist
  geschlossen. Die Befundkarten zeigen SOLO und PULS weiter; sie führen einen
  Handgriff aus und melden ehrlich — ob sie in Legacy sichtbar bleiben sollen,
  ist Designfrage (geparkt).
- **Produktwirkung — Netze in der Main-Fläche.** Freilauf, Sampleratenwechsel
  und Totmann beenden einen Latch jetzt auch dort. Reichweite heute gering: in
  der Main-Fläche entsteht ein Latch nur, wenn das Hinweis-Popover über einen
  Rollenwechsel hinweg offen bleibt; dann ist der SOLO-Knopf im Popover der
  einzige sichtbare Aus-Weg, bis das Popover schließt — Restfall, nicht gebaut.
- **Runnerzeile A3.** `tools/beweise.ps1` gehört nicht zur Ticketgrenze der
  Änderungssätze A und B; die A3-Behauptung nennt Rollenwechsel, drei Netze,
  reset und releaseResources noch nicht. Sie sagt nicht mehr, als das Bein
  misst; wenn sie nachgezogen werden soll, gehört das in 6b oder den Abschluss.
- **Kommentar außerhalb des Änderungssatzes:** der Editordestruktor sagt
  „editorOffen=false lässt die Audioseite zusätzlich hart auf den Originalpfad
  schneiden" — seit NAK-47 ist das eine Rampe. Pflege nach Ticketende.
- **Für 6b:** `PluginEditor.cpp` ist gegen den Basis-SHA um rund 40 Zeilen
  verschoben (`timerCallback` `:242-346`, `schalteMarkierung` `:800-874`,
  `wechsleFlaecheWennNoetig` ab `:892`); `timerTickFuerTest` in
  `PluginEditor.h` ist der Tick-Testzugang, den M-74 und M-85 brauchen. Der
  Tick läuft jetzt: Anzeigeräumung, drei Netze, dann die Main-Fläche.

### 36.10 Gebaut — Änderungssatz B (reset und releaseResources; T3-01-10 sicherer Teil)

Änderungssatz A ist Commit `ba32bc98` (21:36:15), gepusht. Änderungssatz B:
dieser Commit (Betreff „NAK-312 Etappe 6a: Änderungssatz B …"); sein SHA steht
im Abschnitt des Änderungssatzes C. Basis des Änderungssatzes B: `ba32bc98`.
Zeilen zum Endstand B.

- `eq-copilot/plugin/src/HoerMarkierung.h:323-360`: der öffentliche
  Abbruchpfad `brichAb()` — `hartAus()`, der fällige Übergang mit der gezählten
  Hördauer, `warHoerbar` und `hoerbareSamples` zurückgesetzt wie in
  `vorbereiten` und `setzeSamplerate` (H1); keine Zuteilung, der Oversize-Riegel
  und der Auftrag bleiben (U56). `gibPufferFrei()` (`:356-360`) für
  releaseResources. Dazu der Zähler der Wet-Zuteilungen (`:307`, `:855`) mit
  zwei Lesern (`:743-744`, A-83).
- `eq-copilot/plugin/src/prozessor/Hostbruecke.cpp`: `reset()` (`:165-176`) und
  `releaseResources()` (`:178-188`) rufen den gemeinsamen Abschluss
  `markierungAbbrechen()` (`:190-224`): `brichAb()`, bei fälligem Übergang ein
  `end` mit `project_sample_end` null, der gezählten Dauer und dem Nachlauf aus
  `tailSamplesFuer`, die Eingriffsnummer des laufenden Eingriffs; die Sequenz
  wird nur gezogen, wenn der RT-Ring Platz hat, sonst steht das Überlaufbit
  (fail-closed). Kein Mutex, keine Zuteilung (A-78). `releaseResources` gibt
  danach die Trockenkopie des Vergleichspegels und den Wet-Puffer frei.
  `prepareToPlay` (`:39-163`): `anLebendes` verlangt zusätzlich, dass das offene
  Begin des Senders zum laufenden Eingriff gehört (`:110-120`, A-81). Der
  Dateikopf nennt die zwei neuen Eintritte.
- `eq-copilot/plugin/src/PluginProcessor.h`: `releaseResources()` und `reset()`
  als Overrides mit Kommentar (`:112-118`), `markierungAbbrechen()` privat
  (`:1405-1408`); Testzugänge unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`
  (`:675-686`: Überlaufbit des Prozessors, Blockpuffer, Wet-Zuteilungen; A-83).
- `eq-copilot/plugin/tests/MarkierungTestMain.cpp`: Kopfkommentar ergänzt;
  312/M-61 im bestehenden Oversize-Fall `sonde013M36` (`:535-554`); ab `:1875`
  der Abschnitt des Änderungssatzes B mit den Hilfen `klingtInMain`, `endeAus`,
  `tailFuer`, `Dirty` und den Fällen `nak312M59` (mit M-60 und M-63), `nak312M86`
  (mit dem Teilfall reset, release, prepare), `nak312M62` (a, b), `nak312M64`
  (a, b), `nak312M65`, aufgerufen aus `main` (`:2413-2417`).
- Nicht angefasst: `State.cpp`, `PluginProcessor.cpp`, `PluginEditor.cpp`,
  `PluginEditor.h`, der weiche Ausfade in `verarbeite`, der Interventionsvertrag
  (Wiretext unverändert), `Ipc.cpp` (Sender), `tools/beweise.ps1`, Schemas,
  Fixtures, Identität.

### 36.11 Gegenprobe am Stand A (heute rot) — Änderungssatz B

`docs/beweise/roh/NAK-312-etappe6a-gegenprobe-B.txt`, Bau 21:50:02 bis
21:50:11, Lauf 21:50:11 bis 21:50:19: `Hostbruecke.cpp` bytegleich auf
`ba32bc98`; `HoerMarkierung.h` und `PluginProcessor.h` auf `ba32bc98` plus
allein die Testzugänge (ohne Override, ohne Abbruchpfad, `releaseResources`
leer); Tests in der Endfassung. A3 Exit 1, **8 Fehler — genau die „heute
rot"-Prüfungen**: M-59 (8 abweichende Blöcke im Ausfade, das `end` mit
gesetzter Projektzeit), M-63 (kein `end`, kein neues begin — der Marker klingt
durch), M-86 und sein Teilfall, M-62 (a) und (b), M-64 (a) und die
Durchlaufprüfung nach releaseResources. Grün, wie ihre Etiketten sagen: M-60
(heute nicht messbar), M-61, M-64 (b) und M-65 (Regressionswachen). Danach der
Fix-Stand bytegleich zurück (SHA-256 gleich), neu gebaut, A3 grün.

### 36.12 Matrixzeilen des Änderungssatzes B

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-59 | `312/M-59 reset_beendet_klang_und_intervall`, A3 | grün: Transport angehalten, `reset()`, 40 Blöcke bitgleich, genau 1 `end` zum offenen begin, `project_sample_end` null, `tail_samples` 45 760 = 2 · 20 480 + 4 800 | heute rot (Gegenprobe); Abschluss aus der Override entfernt → rot; `docs/beweise/roh/NAK-312-rot-M-59.txt` |
| M-60 | `312/M-60 reset_allokiert_nicht` (Teilfall von 312/M-59), A3 | grün: 0 Allokationen über `operator new` und `new[]` des Aufrufthreads, Wet-Zuteilungen 1 → 1, Blockpuffer unverändert | heute nicht messbar; `vorbereiten` statt des Abbruchpfads → Zuteilungen 1 → 2 → rot (A-80); `docs/beweise/roh/NAK-312-rot-M-60.txt` |
| M-61 | `312/M-61 reset_loest_den_oversize_riegel_nicht` im Fall [SONDE-013] M-36, A3 | grün: nach `reset()` blendet ein neuer Auftrag in 60 gültigen Blöcken nicht ein; erst `prepareToPlay` löst den Riegel | Regressionswache; `oversizeRiegel = false` im Abbruchpfad → rot; `docs/beweise/roh/NAK-312-rot-M-61.txt` |
| M-62 | `312/M-62 (a) zwei_offene_intervalle_ueber_reset` und `(b) voller_ring_bleibt_fail_closed`, A3 | grün: (a) wartender Übergang aus `prepareToPlay` plus zweites offenes Intervall — begin@1 end@2 begin@3 end@4, kein Überlauf; (b) voller Ring — Überlaufbit des Prozessors vorher nein, nachher ja, das nächste begin trägt 2 nach 1 | Regressionswache; Sequenz unbedingt ziehen → begin trägt 3 → rot (A-79); `docs/beweise/roh/NAK-312-rot-M-62.txt` |
| M-63 | `312/M-63 auftrag_bleibt_ueber_reset` (Teilfall von 312/M-59), A3 | grün: Transport läuft weiter, 1 `end` (null) zum alten, 1 neues begin mit neuer `intervention_id`, hörbar, Auftrag steht | Regressionswache; `ausGewuenscht` im Abbruchpfad gesetzt → kein neues begin → rot; `docs/beweise/roh/NAK-312-rot-M-63.txt` |
| M-86 | `312/M-86 jedes_begin_hat_genau_ein_end_ueber_reset` und `teilfall_reset_release_prepare`, A3 | grün: auf der Leitung `end:A begin:B end:B`; nach 40 Blöcken ohne Erlaubnis 1 Ereignis im Ring, nach dem ersten Block mit Erlaubnis 2 (sein begin im selben Block, der färbt); reset, release, prepare: genau 1 `end` | heute nicht messbar (Gegenprobe rot); `warHoerbar` stehen lassen → zweites `end` → rot; Nummernprüfung entfernt → `end:A end:A begin:B` → rot (A-81); `docs/beweise/roh/NAK-312-rot-M-86.txt` |
| M-64 | `312/M-64 (a) release_schliesst_wie_reset` und `(b) prepare_schliesst_wie_bisher`, A3 | grün: reset, release und prepare je 1 `end`, `project_sample_end` null, `tail_samples` 45 760; Blockpuffer nach reset 1 536, nach release 0; 5 Blöcke ohne neues `prepareToPlay` bitgleich | (a) heute rot, (b) Regressionswache; ein zweiter, eigener Abschlussweg in releaseResources → anderes `end` → rot; `docs/beweise/roh/NAK-312-rot-M-64.txt` |
| M-65 | `312/M-65 ohne_auftrag_kein_ereignis` (Teilfall von 312/M-59), A3 | grün: 0 im Ring, 0 Ereignisse, Statebytes 251/251 gleich, Host-Dirty 0 | Regressionswache; `end` unbedingt bilden → 2 Ereignisse → rot; `docs/beweise/roh/NAK-312-rot-M-65.txt` |

### 36.13 Einzelbeine — Änderungssatz B

Dieselben 15 Ziele wie in §36.5 in einem cmake-Aufruf, 21:56:39 bis 21:59:21,
Exit 0, jedes Binary jünger als die jüngste Quelle (`Hostbruecke.cpp` 21:56:07,
die letzte Rücknahme). Gefahren 21:59:32 bis 22:00:13:

- A3 `EqCopMarkierungTest`: Exit 0, 152 Prüfungen ok.
- B14 `EqCopSonde012ProjectReloadTest`: Exit 0, 231/231.
- A1 `EqCopNullTest`: Exit 0, „NULLTEST OK".
- A14 `pruefe_kern_identitaetsfrei.py`: Exit 0, 25 ok, 0 Fehler.
- Zusätzlich: B15 Exit 0 (25 Shots, Panel 3/0), B8 Exit 0 (82), B23 Exit 0
  (409).
- clang-tidy-Teilmessung `prozessor/Hostbruecke`: 14 Fundstellen, alle in
  `eq-copilot/plugin/core/analysis/`, keine in `src/`.

### 36.14 Abweichungen vom Bauplan, begründet — Änderungssatz B

- **A-78 — der Abschluss aus reset() und releaseResources() geht über den
  RT-Ring, nicht durch den Block aus `prepareToPlay`.** §7.6 lässt nach
  `brichAb()` „genau den Block `Hostbruecke.cpp:97-142`" laufen. Zwei Gründe
  dagegen, beide gemessen oder gelesen: (1) Der Block nimmt
  `sendeZustandMutex`. Der Wrapper ruft `reset()` aus `setProcessing (false)`
  (`juce_audio_plugin_client_VST3.cpp`), und Steinberg erlaubt genau diesen
  Aufruf im Audio-Thread („could be called in an Audio Thread, avoid any memory
  allocation", VST 3 Developer Portal, API Documentation, gelesen am
  21.09.2026); der Auftrag verlangt im Selbstaudit „reset ohne Allokation und
  ohne Sperre". (2) Der Block legt sein totes Ende in den Sendezustand; der
  Sender nimmt es nur mit, wenn der Ring etwas enthält oder ein Linkaufbau
  ansteht (`interventionenSenden`, frühe Rückkehr). `prepareToPlay` stößt diesen
  Aufbau mit `controlV3.reconnect()` selbst an, `reset()` hat keinen Anstoß — das
  `end` bliebe liegen, bis irgendein anderes Ereignis kommt (gemessen: M-64 (b)
  liefert sein `end` im Bein erst nach einem ausdrücklichen Linkaufbau, A-82).
  Der Ring ist der Weg jedes anderen `end`: sperrfrei, in Sequenzordnung, vom
  Worker im nächsten Durchlauf abgeholt. Die Zusagen stehen: M-59 wörtlich („im
  Interventionsring steht genau EIN end"), M-60, M-64 (dasselbe `end` auf der
  Leitung), M-86. Annahme wie bei `prepareToPlay`: `reset()` und
  `releaseResources()` laufen nie gleichzeitig mit `processBlock`; der Host
  ordnet seinen letzten Block davor.
- **A-79 — M-62 in der Form des Rings.** Die Vorbedingung der Matrix (ein
  wartender Übergang aus `prepareToPlay` plus ein zweites offenes Intervall)
  führt mit dem Ring nicht mehr zum Überlauf: das `end` aus `reset()` reiht sich
  in Sequenzordnung hinter den wartenden Übergang, und beide Intervalle
  schließen lückenlos (gemessen, M-62 (a)). Fail-closed bleibt der Fall, in dem
  das `end` nicht unterzubringen ist — der volle Ring: Überlaufbit, keine
  Sequenz (M-62 (b)). Das Überlaufbit des Prozessors liest ein Testzugang, weil
  `v3Status()` es mit dem des Rings verodert und das Füllen des Rings dessen Bit
  schon setzt.
- **A-80 — M-60 zählt zusätzlich die Wet-Zuteilungen.** `juce::HeapBlock` teilt
  mit `std::calloc` zu; der Zähler des Beins hängt an `operator new` und sähe
  `wet.calloc` nicht. Die Mutation der Matrix („`vorbereiten` in der Override")
  bliebe mit ihm allein grün. Die Markierung zählt deshalb ihre Zuteilungen in
  `vorbereiten`.
- **A-81 — `prepareToPlay` prüft die Eingriffsnummer.** Folgt `prepareToPlay`
  auf `reset()`, bevor der Worker das `end` aus dem Ring geholt hat (der Host
  ruft `setProcessing (false)`, `setActive (false)`, `setupProcessing`,
  `setActive (true)` kurz nacheinander), hält der Sender das alte Begin noch für
  lebend; der Block hängte sein totes Ende daran, und das neue Intervall bliebe
  offen (gemessen unter der Mutation: `end:A end:A begin:B`). `anLebendes`
  verlangt deshalb dieselbe Eingriffsnummer wie der laufende Eingriff. Das
  schließt dieselbe Fehlzuordnung für ein `end`, das ein Block geschrieben und
  der Worker noch nicht geholt hat; in allen anderen Lagen entscheidet der Block
  wie bisher (M-64 (b), M-62 (a) grün).
- **A-82 — M-64 (b) fährt den Linkaufbau selbst.** Das tote Ende aus
  `prepareToPlay` reist im Produkt mit dem Linkaufbau aus `reconnect()`; ohne
  Pipe ruft das Bein `v3LinkFuerTest (true)`. Gemessen wird nur das `end`; ein
  Replay-Begin desselben Eingriffs zählt dort nicht.
- **A-83 — Testzugänge.** In `HoerMarkierung.h` ein Zähler der
  Wet-Zuteilungen (ein Inkrement in `vorbereiten`, Vorbereitungskontext) und
  zwei Leser; in `PluginProcessor.h` unter dem Define die Leser für das
  Überlaufbit des Prozessors, die Blockpuffer und die Zuteilungen.
- **A-84 — welche Blockpuffer releaseResources freigibt.** Die zwei, die
  `prepareToPlay` nach der Blockgröße zuteilt: die Trockenkopie des
  Vergleichspegels und den Wet-Puffer der Markierung. Analysequeue und Engines
  haben feste Größe und bleiben. Ein Block ohne neues `prepareToPlay` läuft
  danach unberührt durch (gemessen in M-64 (a)).

### 36.15 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz B

| Abschnitt | Punkt | Änderungssatz B |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | angewandt: voller RT-Ring bei `reset()` → Überlaufbit, keine Sequenz (312/M-62 (b)); der Abflussweg ist der Worker ohne Reconnect (A-78) |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: reset → Blöcke ohne und mit Erlaubnis → prepare (312/M-86), reset → release → prepare (Teilfall), prepare mit wartendem Übergang → reset (312/M-62 (a)); je genau ein `end` je begin |
| C Verträge und Längen | Grenzwerte, NaN/Inf, Feldmenge | angewandt: `project_sample_end` null, `tail_samples` als Formel geprüft; der Wiretext ist unverändert |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel bei jedem Bau, Rotbeweis und der Gegenprobe; kein neues Bein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die Code-Behauptung „ein Block ohne neues prepareToPlay läuft unberührt durch" ist gemessen (M-64 (a)); „ohne Sperre" ist am Code gelesen, nicht gemessen (§36.16) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: acht Rohdateien `docs/beweise/roh/NAK-312-rot-M-59.txt` bis `NAK-312-rot-M-65.txt` und `NAK-312-rot-M-86.txt` (zwei Teile), eine Gegenprobe |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Blockzahlen, Sequenzen, `tail_samples`, Byteanzahlen und Uhrzeiten aus Läufen und `Get-Date` |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: starten↔stoppen (prepareToPlay, reset, releaseResources teilen die Buchführung der Intervalle; die Nummernprüfung liegt im selben Änderungssatz wie der Ring-Abschluss) |

### 36.16 Selbstaudit — Änderungssatz B

- **Welcher Thread welches Atomic.** `interventionsSequenz`: `fetch_add` im
  Audiothread (Block), in `markierungAbbrechen` (Aufrufer von reset oder
  releaseResources) und in `prepareToPlay` unter `sendeZustandMutex`; der
  Linkaufbau setzt sie nur bei Neutralität auf 0, und neutral ist ein Prozessor
  mit hörbarem Marker nicht. `interventionsNummer`: geschrieben nur im Block,
  in reset, release und prepare gelesen, während kein Block läuft.
  RT-Ring: `kopf` schreibt der Aufrufer von `schreibe` (Block oder
  reset/release, nie gleichzeitig), `schwanz` der Worker; `fuellstand` vor dem
  Schreiben ist eine sichere Platzzusage, weil der Worker nur leert.
  `interventionsRingUeberlauf`: `relaxed` wie an den übrigen Stellen.
- **reset ohne Allokation und ohne Sperre.** Allokation gemessen (M-60: 0 über
  `operator new` und `new[]`, keine Wet-Zuteilung). Sperre am Code gelesen:
  `markierungAbbrechen` fasst die Markierung, drei Atomics, eine reine Funktion
  und den Ring an, keinen Mutex; Gen hat keinen Sperrenzähler wie die RT-Wache
  der Sonde, deshalb nicht gemessen.
- **Nie gleichzeitig mit `processBlock`.** `brichAb()` fasst Zustand des
  Audiothreads an, `markierungAbbrechen` schreibt in den Ring. `releaseResources`
  ruft der Wrapper aus `setActive (false)` und `terminate()`; `setActive` und
  `process` sperrt JUCE für FL gegeneinander
  (`FLStudioDIYSpecificationEnforcementLock`, weil FLs Patcher `process` vor und
  während `setActive` ruft). `reset()` kommt aus `setProcessing (false)`, das
  JUCE nicht sperrt; VST3 sagt dazu: `setProcessing (false)` kommt nach den
  `process`-Aufrufen. Dieselbe Annahme trägt der Bauplan (`hartAus()` in der
  Override) und `prepareToPlay` für `vorbereiten`. Nicht gemessen, wie FL es
  hält (Hinweis §36.17).
- **Genau ein `end` je `begin` über jede Folge.** reset allein (M-59), reset
  bei laufendem Transport (M-63), reset → Blöcke → prepare (M-86), reset →
  release → prepare (Teilfall), release allein und prepare allein (M-64),
  zweimal reset hintereinander (der zweite findet `warHoerbar` false).
- **`project_sample_end` null** in M-59, M-63, M-64 gemessen.
- **Sequenzüberlauf fail-closed** in M-62 (b) gemessen.
- **Der harte Schnitt.** `brichAb()` schneidet wie `vorbereiten` — so verlangt
  es §7.6 für den Klang, und `reset()` kommt, wenn der Host die Verarbeitung
  anhält. Läuft der Transport weiter (M-63), blendet der Marker danach neu ein;
  der Sprung am Schnitt ist dann hörbar (Hinweis §36.17).
- **Kommentare, die der Änderungssatz falsch macht (R-312-14).** Dateikopf von
  `Hostbruecke.cpp` (zwei neue Eintritte), `anLebendes` (die Nummer),
  `PluginProcessor.h` (`releaseResources` war ein leerer Rumpf ohne Wort).
  Unverändert richtig: der N-10-Kommentar in `prepareToPlay` (er beschreibt den
  Weg von `prepareToPlay`).
- **Stale Closures, UI-Text:** der Änderungssatz legt keine Lambdas und keinen
  Text an.

### 36.17 Hinweise an den Dirigenten — Änderungssatz B

- **Produktwirkung — Anhalten beendet die Hörmarkierung.** Hält der Host an
  (`setProcessing (false)` → `reset()`) oder deaktiviert die Instanz
  (`releaseResources()`), verstummt eine hörbare Markierung, und ihr Intervall
  schließt beim Broker mit einem `end` ohne Endzeit. Der Auftrag bleibt (U56):
  spielt der Transport wieder, klingt sie mit neuem begin. Ruft ein Host
  `reset()` bei weiterlaufendem Transport, springt der Ausgang am Schnitt von
  gefärbt auf trocken und blendet danach wieder ein — so verlangt es §7.6, wie
  bei `prepareToPlay`. Ob FL `setProcessing (false)` beim Stoppen ruft, auf
  welchem Thread und ob noch ein `process` danach oder gleichzeitig kommt, ist
  nicht gemessen; der Laufzeit-Arm kann es beobachten.
- **Befund in der Sonde (außerhalb des Tickets):** `SondeProcessor::reset()`
  nimmt `getCallbackLock()` und `zustandSchloss` (NAK-283). Nach Steinberg darf
  `setProcessing`, aus dem der Wrapper `reset()` ruft, im Audio-Thread laufen —
  dieselbe Klasse wie A-78. Vorschlag: datierte Registerzeile.
- **Nummernprüfung in `prepareToPlay` (A-81):** schließt auch eine
  Fehlzuordnung, die es vor diesem Ticket schon gab (ein `end` eines Blocks im
  Ring, danach ein neues begin, danach `prepareToPlay`, alles innerhalb eines
  Workertakts). Das war nicht Gegenstand eines Befunds; gemessen ist es nur für
  den reset-Fall.
- **Für 6b:** keine Berührung mit Labelentwurf und Ersatzziel; die Hilfen in
  `MarkierungTestMain.cpp` (`nak312::Stand`, `ernte`) stehen für weitere
  Markierungsfälle bereit.

### 36.18 Gebaut — Änderungssatz C (Test-Zusätze aus §34; kein Produktcode)

Änderungssatz B ist Commit `9aa776da` (22:05:11), gepusht. Änderungssatz C:
dieser Commit (Betreff „NAK-312 Etappe 6a: Änderungssatz C …"). Basis:
`9aa776da`. Geändert sind allein `eq-copilot/plugin/tests/TransactionTestMain.cpp`
und die B7-Behauptungszeile in `tools/beweise.ps1`; die Produktdateien von Sonde
und Kern (`DspKern.cpp`, `DspKern.h`, `SondeProcessor.cpp`), die Golden-Bin und
die Vorlage `BrokerInstallBinding.h.in` sind nur für die Rotläufe angefasst und
bytegleich zurück (SHA-256 je Rohdatei, `git status` danach ohne sie).

- `TransactionTestMain.cpp:5417-5459`, 312/M-40: der thread-lokale Zähler ist
  über `setNonRealtime (true)` und `processBlock` im Bereich wie der Wrapper
  scharf und meldet an die Echtzeitwache; geprüft werden
  `RtWache::allokationen()` = 0 und der eigene Zähler = 0 neben den Sperren
  (R-312-22, H-2).
- `:5659-5751`, im Abschnitt ZA: 312/M-89 je Blockgröße 1, 64, 256 — nach
  `setNonRealtime (true)` nacheinander Hörwunsch Dry, Delta, Candidate, je
  512 Samples, gegen den bestätigten Zwilling ohne Vorschau; 312/M-90 — Aufbau
  wie 312/M-43, Offline an und sofort aus ohne Block, dann Candidate. Beide
  unter der Taktsperre `mitAngehaltenemTaktFuerTest` (A-85).
- `:6060-6063`, Textriegel 312/M-88: Muster `*.cpp;*.h;*.in` (H-4).
- Kopfkommentar (`:77-83`) und Abschnittstitel ZA nachgezogen.
- `tools/beweise.ps1:728`, B7: der Offline-Satz nennt jetzt „0 Sperren und
  (seit Etappe 6a gemessen) 0 Allokationen"; ein neuer Satz nennt 312/M-89 und
  312/M-90 mit genau ihren Bedingungen (Taktsperre, stehende Vorschau,
  Blockgrößen, Schranke). Der Satz zum Textriegel bleibt, wie er ist — er ist
  mit `*.in` wahr (H-4). Die B2-Zeile bleibt unverändert: sie behauptet für
  312/M-49 genau, was B2 misst (A-88).

### 36.19 Matrixzeilen und Zusätze des Änderungssatzes C

Keine Gegenprobe: M-89 und M-90 messen Verhalten, das seit Etappe 5 besteht;
§34.3 nennt als Rotbeweis je die Mutation.

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-89 | `312/M-89 hoerwunsch_im_render_wirkt_als_processed_blockgroesse_1`, `_64`, `_256`, B7 | grün: Dry, Delta und Candidate je bitgleich zum bestätigten Lauf ab Sample 0 (512, 8 und 2 Blöcke je Wunsch), Vorschau vorher hörbar und am Ende noch gesetzt | Riegelzeile in `DspKern.cpp` entfernt → alle drei Wünsche verschieden → rot; `docs/beweise/roh/NAK-312-rot-M-89.txt` |
| M-90 | `312/M-90 hin_und_zurueck_ohne_block_schaltet_nicht_hart`, B7 | grün: größter Nachbarsprung 0,00116634369 bei Fadeschritt 0,00116632303 + 2⁻²³, vorher 0,300000012, Ende 0,598578691 | Rücknahme der Anforderung in `DspKern.h` entfernt → erster Sprung 0,29857868 → rot (A-86); `docs/beweise/roh/NAK-312-rot-M-90.txt` |
| M-40 | `312/M-40 offlineflag_nimmt_keine_sperre`, B7, Allokationshälfte | grün: RtWache::sperren 0, RtWache::allokationen 0, eigener Zähler 0 über 200 Blöcke | eine Allokation in `setNonRealtime` der Sonde → 200 → rot; `docs/beweise/roh/NAK-312-rot-M-40-allokation.txt` |
| M-49 | `312/M-49 (B2)` und `312/M-49 statebytes_des_basis_sha_reisen_bytegleich`, B2 (H-3) | grün: 294 Prüfungen, 0 Fehler | ein Byte der Golden-Bin (Offset 2000, XOR 0x01) → SHA-256 ungleich der Zeile, Laden nicht normal → 3 Fehler → rot; Datei bytegleich zurück, `git status` leer; `docs/beweise/roh/NAK-312-rot-M-49-b2.txt` |
| M-88 | `312/M-88 kein_weg_zum_hostende_im_pluginquelltext`, B7 (H-4) | grün: 105 Dateien, kein Treffer (vorher 103 laut §34.3) | `WM_CLOSE` in `BrokerInstallBinding.h.in` → Treffer → rot (A-87); `docs/beweise/roh/NAK-312-rot-M-88-in.txt` |

### 36.20 Einzelbeine — Änderungssatz C

B7, B2, B6 und A16 in einem cmake-Aufruf gebaut, 22:10:42 bis 22:10:58, Exit 0,
jedes Binary jünger als die jüngste Quelle (`SondeProcessor.cpp` 22:10:11, die
letzte Rücknahme). Gefahren 22:12:49 bis 22:13:00:

- B7 `EqCopTransactionTest`: Exit 0, 252 geprüft, 0 Fehler.
- B2 `EqCopStateMigrationTest`: Exit 0, 294 Prüfungen, 0 Fehler.
- B6 `EqCopDspGoldenTest`: Exit 0, 389 geprüft, 0 Fehler.
- A16 `EqCopProbeeqNullTest`: Exit 0, 131 Prüfungen, 0 Fehler.
- `tools/beweise.ps1` parst fehlerfrei (PowerShell-Parser).

### 36.21 Abweichungen, begründet — Änderungssatz C

- **A-85 — M-89 und M-90 unter der Taktsperre.** Der Wechsel nach offline
  fordert das Vorschauende an; der Worker verbucht es im nächsten Kontrolltakt,
  zu einer Zeit, die die Wanduhr bestimmt. Ohne Sperre hinge in M-89 die
  Candidate-Phase an dieser Zeit, und in M-90 nähme ein verbuchtes Ende den
  Candidate weg — die Mutation könnte dann nicht fallen. Unter der Sperre steht
  die Vorschau (gemessen: am Ende von M-89 noch gesetzt).
- **A-86 — M-90 setzt die Vorschau vor dem Offline-Fenster.** Ein Candidate,
  der aus der Ruhe einblendet, gilt für die Hörmatrix noch nicht als vorhanden
  ([SONDE-015] E-34); eine liegengebliebene Anforderung schaltete dann
  Processed auf Processed, und der Rotbeweis fiele nicht. Deshalb liegt die
  Vorschau schon eingeschwungen bei Hörwunsch Processed, bevor das Fenster
  kommt (Zustand der Matrixzeile: „Hörmatrix Processed").
- **A-87 — H-4 einmal gebrochen.** §34 verlangt nur das Muster; Prüfliste E
  verlangt, dass jede neue Prüfung einmal bricht. Der Rotlauf zeigt, dass der
  Riegel die Vorlage wirklich liest.
- **A-88 — B2-Zeile unverändert.** H-3 ist ein Rotlauf ohne neue Prüfung; die
  Zeile sagt für 312/M-49 genau das Gemessene.

### 36.22 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz C

| Abschnitt | Punkt | Änderungssatz C |
|---|---|---|
| A Rückstau | – | nicht zutreffend |
| B Lebenszyklus | Offline an und aus | angewandt: Hin und Rückweg ohne Block (312/M-90) |
| C Verträge und Längen | Zahlenränder | angewandt: Blockgröße 1, 64, 256 (312/M-89) |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: der Textriegel liest alle Quelltextformen der Ordner (`*.in`), weiter fail-closed; Binaryzeitstempel bei jedem Bau und Rotbeweis |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die B7-Zeile nennt je Satz die gemessene Bedingung; „kein Quelltext" ist mit `*.in` wahr |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: fünf Rohdateien (§36.19) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Sprünge, Zähler, Dateizahl und Uhrzeiten aus Läufen und `Get-Date`; „103" steht als Zitat aus §34.3 |
| F Änderungssatz | Beziehungen | angewandt: Hin- und Rückweg des Offline-Riegels in einem Fall |

### 36.23 Selbstaudit — Änderungssatz C

- **Kein Produktcode.** `git diff --stat 9aa776da` vor dem Commit zeigt allein
  `TransactionTestMain.cpp`, `tools/beweise.ps1`, das Manifest und die
  Rohdateien; `DspKern.cpp`, `DspKern.h`, `SondeProcessor.cpp`, die Golden-Bin
  und die Vorlage stehen bytegleich (SHA-256 in den Rohdateien).
- **Taktsperre ohne Warten.** In den Lambdas läuft nur, was die Sperre erlaubt
  (Blöcke, `setNonRealtime`, `setzeHoermatrix`, `previewAktiv` auf demselben
  Thread, rekursives Schloss); kein Warten auf einen Thread, der dasselbe
  Schloss nimmt.
- **Behauptungszeile nicht weiter als der Test.** Geprüft Satz für Satz gegen
  die Prüfnamen von B7.
- **Stale Closures:** die Lambdas fangen per Referenz und laufen synchron
  innerhalb von `mitAngehaltenemTaktFuerTest`.

### 36.24 Hinweise an den Dirigenten — Änderungssatz C und Übergabe an 6b

- **Produktwirkung: keine** (nur Tests und eine Behauptungszeile).
- **Register NAK-355:** H-3 ist gefahren (rot, zurück, grün), H-4 gebaut und
  gebrochen; die übrigen Härtungen dort (Schwelle 100 gegen Bestand 105,
  weitere Wörter) bleiben offen.
- **Für 6b:** Etappe 6a hat drei Commits (`ba32bc98`, `9aa776da` und dieser);
  der volle Kanon ist nicht gelaufen. Der Kanon am Ende von 6b misst zum ersten
  Mal alle drei zusammen, dazu das neu gebaute Bundle (`EqCopilot_VST3`, zuletzt
  21:57 gebaut) gegen B1; `--hashen` des Installers folgt erst nach grünem
  Kanon. Der Tick-Testzugang und die Leser des Anzeigezustands in
  `PluginEditor.h` stehen für M-74 und M-85 bereit; `PluginEditor.cpp` ist
  gegen den Basis-SHA von 6a um rund 40 Zeilen verschoben. Die A3-Runnerzeile
  nennt die Fälle der Etappe 6a noch nicht (§36.9).
- **Zur Prüfung vorgemerkt:** A-78 (der Abschluss aus reset und
  releaseResources über den Ring statt durch den Block aus `prepareToPlay`) und
  A-81 (Nummernprüfung in `prepareToPlay`) sind Bauplanabweichungen mit
  Produktcode; beide sind gemessen (M-59, M-64, M-86 samt Teil 2 der
  Rohdatei).

## 37. Messung der Etappe 6a, Auftrag 6b (Dirigent, 21.09.2026, 22:20 Uhr)

### 37.1 Messung

Worker `b1dc89c2` (Opus, Effort xhigh, `dontAsk`, Aufsicht ENG), Start 20:49
Uhr, `done` gemessen 22:16 Uhr, Basis
`fd287d3c8ea277814c80e67365ea782e6af9994d`. Drei Commits —
`ba32bc9868ab1b19357c216c4d6e4472fe0e7739` (Änderungssatz A),
`9aa776da3fc8da375b07aa1229884b3bbb3a0aeb` (Änderungssatz B),
`5f5300413e32a62cba022f0bc55a94b4ef5a224b` (Änderungssatz C) = `origin/master`,
Baum sauber (nur die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD,
kein fremder Commit dazwischen (Beobachter: drei HEAD-Wechsel, alle vom Bauer).
Rundenbilanz `fd287d3c..5f530041` (gemessen): Produkt 7 Dateien +248/-19, Tests
2 Dateien +962/-8, Prüfwerkzeug 1 Datei +1/-1, Doku 21 Dateien +2660/-2.
Geänderte Pfade außerhalb von `docs/`: `HoerMarkierung.h`, `PluginEditor.cpp`,
`PluginEditor.h`, `PluginProcessor.cpp`, `PluginProcessor.h`,
`prozessor/Hostbruecke.cpp`, `prozessor/State.cpp`,
`tests/MarkierungTestMain.cpp`, `tests/TransactionTestMain.cpp`,
`tools/beweise.ps1` — alle innerhalb der Ticketgrenze des Auftrags.
Messabdeckung: je eine Rohdatei für M-55 bis M-65, M-84, M-86, M-89, M-90, den
Allokationszähler von M-40, die B2-Hälfte von M-49 und das Muster `*.in` von
M-88, dazu die Gegenproben A und B (20 Rohdateien, gezählt). Meldung des
Bauers: 16 Matrixzeilen grün, 18 Rotbeweise, Einzelbeine A3, B15, B14, A1, A14,
B7, B2, B6, A16, dazu B8 und B23 grün. **Der volle Kanon und der Laufzeit-Arm
sind wie beauftragt nicht gelaufen**; sie folgen am Ende von 6b.

### 37.2 Für die Erstprüfung der Etappe 6 vorgemerkt

- **A-78 (Produktcode, Bauplanabweichung):** `reset()` und
  `releaseResources()` schließen das offene Markierungsintervall über den
  RT-Ring statt über den Abschlussblock von `prepareToPlay`, weil der Host
  `setProcessing (false)` im Audio-Thread rufen darf und der Abschlussblock eine
  Sperre nimmt (§36.14). Technische Entscheidung im Sinn der Gate-GRENZE;
  der Prüfer ordnet sie gegen M-59, M-64 und M-86 ein.
- **A-81 (Produktcode, Bauplanabweichung):** `prepareToPlay` prüft die
  Eingriffsnummer, damit es sein `end` nicht an ein schon geschlossenes
  Intervall hängt (§36.14, §36.17).
- **Produktwirkung, bekannt:** die Hörmarkierung hat nach R-312-8 keinen
  hörbaren Weg mehr, bis die Main-Fläche einen Auslöser bekommt (Register
  NAK-341, Design geparkt); Anhalten des Hosts beendet eine hörbare
  Markierung, der Auftrag bleibt (U56 beantwortet, §10.2).
- **Nebenfund außerhalb des Tickets:** `SondeProcessor::reset()` nimmt
  `getCallbackLock()` und `zustandSchloss`, obwohl der Host `setProcessing` im
  Audio-Thread rufen darf → Register **NAK-357**, Quellvalidierung vor einer
  Einordnung.
- Die A3-Behauptungszeile nennt die Fälle der Etappe 6a noch nicht; der Bauer
  von 6b zieht sie nach.

### 37.3 Auftrag 6b

`docs/beweise/roh/NAK-312-etappe-6b-auftrag.txt`: drei Änderungssätze
(Labelentwurf M-66 bis M-72; Ersatzziel und Aktionssteuerung M-73 bis M-76 und
M-85; Kennungskonflikt-Panel M-91 und M-92 nach R-312-23), die
A3-Behauptungszeile, der volle Kanon der ganzen Etappe 6 (M-77). Bauer:
frischer Opus-Worker, Effort xhigh; Claude-Woche gemessen 22:16 Uhr: 86 %.

## 38. Etappe 6b — Gen-Fläche, Kennungskonflikt-Panel, Kanon der Etappe 6 (Bauer, 21.09.2026)

Auftrag `docs/beweise/roh/NAK-312-etappe-6b-auftrag.txt`; Spezifikation §1
(T3-14-01, T3-07-05), §2 (R-312-2, R-312-6, R-312-9, R-312-12, R-312-14),
§5.0, §5.11, §5.12, §6 Kopf, §6.7 (M-66 bis M-77, M-85), §7.0, §7.1, §7.6,
§7.7, §8, §11.3 (E-312-9, R-312-12), §14.4 (R-312-13, R-312-14), §27, §35
(M-91, M-92, R-312-23) mit `docs/beweise/roh/NAK-349-quellvalidierung.md`, §36.
Drei Änderungssätze mit je eigenem Commit; jeder Unterabschnitt gilt zum
Endstand seines Änderungssatzes. Uhrzeiten aus `Get-Date`, Binaryzeitstempeln
oder Commitzeit. Fremde Matrixzeilen tragen ihr Präfix (R-312-13). Die
Abweichungen setzen die Zählung aus §36 fort (A-89 ff.).

### 38.1 Start, Basis-SHA, Kanon vorher

- Start 22:19:25 (`Get-Date`): `git pull --ff-only` ohne Änderung;
  `git rev-parse HEAD` = `ed1722d992f64567e131aaa7fa5ae887bdef96bf`
  (Basis-SHA der Etappe 6b); `git status --short` zeigt nur `briefing-hub/`
  und `nimbalyst-local/`. Vor jedem Bau lief kein zweiter Bau- oder
  Testprozess (NAK-290); das laufende `FL64` gehört dem User und blieb
  unberührt.
- **Kanon vorher.** Der Kanon nachher der Nacharbeit 1 der Etappe 5 (GRUEN
  70/70 an `2c138b2d`, §32.9) gilt für den Stand vor der Etappe 6. Etappe 6a
  hat Produktcode geändert und nur Einzelbeine gefahren (§36):
  `git diff --stat 2c138b2d ed1722d9 -- eq-copilot broker tools` zeigt elf
  Dateien (die drei Commits `ba32bc98`, `9aa776da`, `5f530041` und das
  Installermanifest). Der volle Kanon am Ende von 6b misst die ganze Etappe 6
  zum ersten Mal (M-77).
- Bauplanzeilen am Basis-SHA nachgelesen: `git diff --stat 945ebfb1 ed1722d9`
  über die fünf Dateien der Ticketgrenze zeigt `PluginEditor.cpp`,
  `PluginEditor.h` und `ShotTestMain.cpp`; `SourcesModel.cpp` und
  `Sonde012ProjectReloadTest.cpp` sind unverändert (`stelleZielSicher`
  `SourcesModel.cpp:1772-1777`, Sortierung `:1651-1664` wie im Bauplan). In
  `PluginEditor.cpp` am Basis-SHA: `uebernehmeSourcesLabel` `:947-973`
  (Bauplan `:874-899`), `aktualisiereSourcesSteuerung` `:975-1017` (Bauplan
  `:902-944`), das Laden des Textes `:1007-1009` (Bauplan `:934-936`),
  `sourcesZeilen` `:1024-1040` (Bauplan `:951-967`), `mouseDown` `:1047-1061`
  mit der Zielwahl `:1057` (Bauplan `:984`), das Zeichnen der Zeilen `:1208`
  (Bauplan `:1135`), `zeigeKonflikt` `:544-605` mit dem Rückruf `:595-601`.

### 38.2 Gebaut — Änderungssatz A (Labelentwurf; T3-14-01)

Commit: dieser Änderungssatz (Betreff „NAK-312 Etappe 6b: Änderungssatz A …");
sein SHA steht im Abschnitt des Änderungssatzes B. Zeilen zum Endstand A.

- `eq-copilot/plugin/src/PluginEditor.h`: die Entwurfskennung
  `sourcesLabelFuerId` und `sourcesLabelFuerNonce` neben `sourcesAktionsZiel`
  (`:187-194`: instance_id und Runtime-Nonce der Quelle, für die der
  Feldinhalt gilt); der private Leser `sourcesLabelHatFokus()` (`:136`); unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` die Testzugänge des Feldes (`:86-117`:
  Tippen, Einfügen, Enter, Escape, Fokusverlust, Feldtext, Aktionsziel,
  gezeichnete Zeilen) und der Fokusschalter `sourcesLabelFokusFuerTest`
  (`:193`, A-91).
- `eq-copilot/plugin/src/PluginEditor.cpp`:
  - `aktualisiereSourcesSteuerung` (`:993-1051`): dort, wo der Text geladen
    wird, entsteht die Kennung (`:1025-1043`). Gehört der Feldinhalt einer
    anderen Quelle als dem Ziel oder derselben Quelle mit neuer Runtime-Nonce,
    lädt das Feld das gespeicherte Label der Quelle, die es jetzt bedient —
    auch mit Fokus; für dieselbe Quelle schützt der Fokus den Entwurf wie
    bisher. Ohne Ziel ist die Kennung leer.
  - `uebernehmeSourcesLabel` (`:957-991`): schreibt nur, wenn die Kennung das
    Aktionsziel ist (`:967`), und prüft Hauptziel, Runtime-Nonce und
    Mitgliedschaft gegen die Sicht des Modells von jetzt (`:969-975`, A-92),
    nicht gegen die Anzeige des letzten Ticks.
  - `mouseDown` (`:1092-1114`): ein Klick auf eine andere gezeichnete Zeile
    bestätigt einen offenen Entwurf unter seiner Startquelle, bevor
    `waehleSourcesHauptziel` das Ziel wechselt (`:1102-1110`, A-89); ein Klick
    auf das eigene Ziel bestätigt nichts.
  - Konstruktor (`:211-220`): `onEscapeKey` verwirft den Entwurf ohne Mutation
    — die Kennung verfällt, das Feld lädt das gespeicherte Label (A-90).
  - `sourcesLabelHatFokus()` (`:1053-1062`): im Produktbau
    `sourcesLabelFeld.hasKeyboardFocus (true)`, im Testbau zusätzlich der
    Fokusschalter; er ersetzt die eine Fokusabfrage an der Ladestelle (A-91).
- `eq-copilot/plugin/tests/ShotTestMain.cpp` (B15): Kopfkommentar ergänzt; der
  Abschnitt `nak312Labelentwurf` (`:458-775`) mit der `LabelBuehne` (echter
  Main auf dem Heap, Worker-Drain aus, zwei bestätigte Mitglieder A und B über
  den ACK-Weg, Namen im Prozessorzustand, Editor offen und getickt), dem Klick
  `klicke` (zweimal `mouseDown` wie JUCE, A-95) und den Fällen 312/M-66 (mit
  Gegenrichtung und eigenem Ziel), M-67, M-68 (a1, a2, b), M-69 (a, b, c),
  M-70, M-71 (a bis d); aufgerufen aus `main`.
- `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp` (B14): Namensraum
  `nak312` mit `labelentwurf_ueberlebt_speichern_und_laden` (312/M-72),
  aufgerufen aus `main`.
- `tools/beweise.ps1`: die Behauptungen von B15 und B14 um genau die
  gemessenen Fälle ergänzt (BOM und CRLF erhalten, zwei Zeilen, der
  PowerShell-Parser meldet 0 Fehler).
- Nicht angefasst: `SourcesModel.cpp`, `sourcesZeilen`, `zeigeKonflikt`, das
  Quellenmodell, die Dateien der Etappe 6a, Schemas, Fixtures, Identität,
  `design/`. `eq-copilot/plugin/src/prozessor/Ipc.cpp` und
  `eq-copilot/plugin/state/NakamaState.cpp` sind allein für die Rotbeweise von
  M-70 und M-72 mutiert und bytegleich zurückgenommen (SHA-256 in den
  Rohdateien, `git status` danach ohne sie).

### 38.3 Gegenprobe am Basisstand (heute rot) — Änderungssatz A

`docs/beweise/roh/NAK-312-etappe6b-gegenprobe-A.txt`, Bau 22:32:49 bis
22:33:03, Lauf 22:33:41 bis 22:33:53. Die Produktdateien stehen auf der
**Basisvariante**: `ed1722d9` plus allein die Testzugänge in `PluginEditor.h`
und die produktneutrale Fokus-Naht (A-91), SHA-256 je Datei in der Rohdatei;
die Tests in der Endfassung bis auf die zwei Selbstauditprüfungen von
312/M-66, die danach hinzukamen. B15 Exit 1, **7 Fehler — genau die „heute
rot"-Prüfungen**: 312/M-66 (der Entwurf von A landet auf B), M-67 (Bs Name
gelöscht), M-68 (a2) (der mit Escape verlassene Entwurf landet beim nächsten
Enter auf B), M-68 (b) (Fokusverlust schreibt auf B), M-69 (a) (auf B), (b)
und (c) (auf A trotz neuer Runtime-Nonce). Grün, wie ihre Etiketten sagen:
M-68 (a1) „Escape schreibt nichts", M-70, M-71 (a) bis (d). B14 Exit 1: allein
die Vorbedingung von 312/M-72 fällt (sie ist der Fall 312/M-66); die
Save/Load-Hälfte ist grün — die Namen, die der Basisstand geschrieben hat,
reisen bytegleich.

### 38.4 Matrixzeilen des Änderungssatzes A

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-66 | `312/M-66 labelentwurf_gehoert_seiner_startquelle` mit den Selbstauditprüfungen `(Gegenrichtung)` und `(eigenes Ziel)`, B15 | grün: A trägt den Entwurf, B unverändert, genau 1 Host-Dirty, das Feld zeigt Bs Label, Aktionsziel B; Gegenrichtung: der Entwurf auf B bleibt auf B, 1 Host-Dirty mehr; ein Klick auf das eigene Ziel schreibt nichts | heute rot (Gegenprobe); Kennungsvergleich entfernt → B trägt den Entwurf, 2 Host-Dirty → rot; Bedingung „anderes Ziel" entfernt → das eigene Ziel wird bestätigt → rot; `docs/beweise/roh/NAK-312-rot-M-66.txt` |
| M-67 | `312/M-67 leerer_entwurf_loescht_keinen_fremden_namen`, B15 | grün: Bs Name bleibt, 0 Host-Dirty | heute rot (Gegenprobe); Mutation wie M-66 → Bs Name leer → rot; `docs/beweise/roh/NAK-312-rot-M-67.txt` |
| M-68 | `312/M-68 (a1) escape_schreibt_nichts`, `(a2) escape_verwirft_den_entwurf`, `(b) fokusverlust_bestaetigt_auf_der_startquelle`, B15 | grün: (a1) keine Mutation, 0 Host-Dirty; (a2) das Feld zeigt nach Escape wieder As Label, Klick auf B, Tick und Enter schreiben nichts; (b) A trägt den Entwurf, B unverändert, 1 Host-Dirty | zwei Hälften: (a1) Regressionswache, (a2) und (b) heute rot (Gegenprobe); Mutation wie M-66 → (a2), (b) rot, (a1) grün; Bestätigung im Auswahlwechsel entfernt → (b) rot; Escape-Verdrahtung entfernt → (a2) rot (A-90); `docs/beweise/roh/NAK-312-rot-M-68.txt` |
| M-69 | `312/M-69 (a) startquelle_faellt_weg`, `(b) neue_runtime_nonce_mit_tick`, `(c) neue_runtime_nonce_ohne_tick`, B15 | grün: je weder A noch B geschrieben, 0 Host-Dirty; (a) Aktionsziel B mit Bs Label, (b) das Feld zeigt As gespeichertes Label | heute rot (Gegenprobe); Mutation wie M-66 → (a) auf B, (b) und (c) auf A → rot; `docs/beweise/roh/NAK-312-rot-M-69.txt` |
| M-70 | `312/M-70 unveraenderter_text_mutiert_nicht`, B15 | grün: keine Mutation, 0 Host-Dirty, keine Fehlermeldung | Regressionswache; No-op-Riegel in `Ipc.cpp` allein entfernt → grün (A-93); zusammen mit dem frühen Rückweg im Editor → 1 Host-Dirty → rot; `docs/beweise/roh/NAK-312-rot-M-70.txt` |
| M-71 | `312/M-71 (a) eingabebeschraenkung_120`, `(b) kappung_120`, `(c) null_und_120_werden_angenommen`, `(d) 121_wird_abgewiesen`, B15 | grün: 121 Codepoints U+1F3B9 eingefügt → das Feld hält 120; 121 bestätigt → 120 gespeichert; 0 und 120 angenommen, je 1 Host-Dirty; 121 am Prozessor abgewiesen, Name unverändert | Regressionswache; Kappung auf 121 → der Prozessor weist ab, der Name bleibt → rot (A-96); `docs/beweise/roh/NAK-312-rot-M-71.txt` |
| M-72 | `312/M-72 labelentwurf_ueberlebt_speichern_und_laden`, B14 | grün: nach dem Fall 312/M-66 lädt die frische Instanz A mit dem Entwurf und B mit seinem Namen, dieselbe Zuordnung, 0 Host-Dirty beim Laden | Regressionswache; der Schreiber legt für jedes Mitglied das Label des ersten ab → B lädt As Namen → rot (A-94); `docs/beweise/roh/NAK-312-rot-M-72.txt` |

### 38.5 Einzelbeine — Änderungssatz A

Alle 15 Ziele, die nach `eq-copilot/plugin/CMakeLists.txt` `PluginEditor.cpp`
und die Gen-Prozessorquellen übersetzen (Liste wie §36.5), dazu
`EqCopSonde012SourcesModelTest`, in einem cmake-Aufruf gebaut, 22:50:50 bis
22:52:44, Exit 0; jedes Binary jünger als die jüngste seiner Quellen
(`PluginEditor.cpp` 22:49:33, die letzte Rücknahme; B13 übersetzt sie nicht,
seine jüngste Eingabe ist `state/NakamaState.cpp` 22:41:17, Binary 22:44:39).
Gefahren 22:52:44 bis 22:52:56:

- B15 `EqCopShot --sonde012-suite`: Exit 0, 25 Shots bei 760x430, „NAK-312
  PANEL 3 geprueft, 0 Fehler", „NAK-312 LABEL 15 geprueft, 0 Fehler".
- B14 `EqCopSonde012ProjectReloadTest`: Exit 0, 234/234 (vorher 231; neu
  zwei Prüfungen von 312/M-72 und die Aufbauprüfung aus `mainAnlegen`).
- B13 `EqCopSonde012SourcesModelTest`: Exit 0, 88/88.
- A1 `EqCopNullTest`: Exit 0, „NULLTEST OK".
- Die 25 Bilder des Sichtsatzes und das Panelbild sind in der Gegenprobe und
  am Endstand A bytegleich (26 von 26 SHA-256 gleich): die Fläche ist
  unverändert.
- clang-tidy-Teilmessung (`tools/plan/tidy.py --nur src/PluginEditor`,
  22:45:08 bis 22:45:55): 15 Fundstellen wie in §36.5, davon in `src/` allein
  die vorhandene Initialisierung von `k` in `anzeigeKurve`; keine neue.

### 38.6 Abweichungen vom Bauplan, begründet — Änderungssatz A

- **A-89 — die Bestätigung beim Auswahlwechsel sitzt im Klick, nicht in
  `aktualisiereSourcesSteuerung`.** §7.6 lässt `aktualisiereSourcesSteuerung`
  beim Zielwechsel den Entwurf „noch unter A" abschließen. Das ist nicht
  baubar: der Klick ruft `waehleSourcesHauptziel` sofort, das Modell führt
  danach B als Hauptziel, und `benenneSourcesHauptziel` schreibt nur auf das
  aktuelle Hauptziel (`eq-copilot/plugin/src/prozessor/Ipc.cpp:1208`, `:1217`;
  SONDE-012 U03). Im nächsten Tick wiese der Prozessor jeden Schreibversuch
  auf A ab. Gebaut ist deshalb: `mouseDown` bestätigt den Entwurf, solange A
  noch Hauptziel ist, und wählt erst dann B; `aktualisiereSourcesSteuerung`
  lädt danach Bs Label, weil die Kennung nicht mehr passt. Der Klick in die
  Liste nimmt dem Feld den Fokus nicht (JUCE gibt ihn nur an eine
  Komponente ab, die ihn will; `juce_Component.cpp:2722-2723` behält ihn bei
  einem fokussierten Kind) — ohne diese Bestätigung bliebe der Entwurf also
  offen, genau das ist T3-14-01. Ein Zielwechsel ohne Handgriff entsteht nur,
  wenn A wegfällt (`stelleZielSicher`); dort verfällt der Entwurf (R-312-9
  letzter Satz, M-69).
- **A-90 — Escape verwirft wirklich; M-68 (a) in zwei Hälften.** Die
  Zusage „(a) verwirft den Entwurf ohne Mutation" hält am Basisstand nur
  halb: Escape schreibt nichts, lässt den Text aber im Feld, und ein späteres
  Enter oder — nach A-89 — der nächste Auswahlwechsel bestätigt ihn. Gebaut
  ist `onEscapeKey`: die Kennung verfällt, das Feld lädt das gespeicherte
  Label. Gemessen als (a1) „Escape schreibt nichts" (Regressionswache, wie
  das Etikett) und (a2) „Escape verwirft" (am Basisstand rot). Kein neues
  Element, kein neuer Text.
- **A-91 — Fokus im Bein ohne Fenster.** JUCE gibt einer Komponente den
  Tastaturfokus nur, wenn ihr Fenster den Systemfokus bekommt
  (`juce_Component.cpp:2679-2685`); EqCopShot rendert ohne Fenster. Der
  Zustand „Feld fokussiert und geändert" entsteht deshalb über einen
  Testzugang: `sourcesLabelHatFokus()` liefert im Testbau zusätzlich den
  Fokusschalter, im Produktbau genau `hasKeyboardFocus (true)`. Enter,
  Escape und Fokusverlust rufen die verdrahteten Rückrufe des Feldes direkt
  (der echte `TextEditor` stellt sie über eine gepostete Nachricht zu; das
  Bein pumpt hier keine Schleife und hängt damit nicht an der Wanduhr).
- **A-92 — der Kennungsvergleich fällt gegen das Modell von jetzt, und die
  Kennung trägt die Runtime-Nonce.** M-69 nennt „eine neue Runtime-Nonce" als
  Fall der verschwundenen Startquelle; die Kennung ist deshalb instance_id
  und Nonce. `uebernehmeSourcesLabel` liest dafür die Sicht des Modells beim
  Bestätigen (`processor.sourcesSicht()`), nicht die Anzeige des letzten
  Ticks: sonst schriebe ein Enter zwischen Modellwechsel und Tick den
  Entwurf noch auf die alte Adresse (312/M-69 (c), am Basisstand rot).
- **A-93 — M-70: die Matrixmutation allein ist kein Rotbeweis.** Den
  No-op-Riegel in `Ipc.cpp` allein zu entfernen bleibt grün: der frühe
  Rückweg in `uebernehmeSourcesLabel` ruft den Prozessor bei unverändertem
  Text gar nicht erst. Rot wird die Zeile, wenn beide Riegel fallen (Teil b
  der Rohdatei); beide Dateien sind bytegleich zurück.
- **A-94 — M-72: Form der Mutation.** „Den Entwurf statt des bestätigten
  Labels speichern" hat im Code keinen Weg: der Entwurf lebt nur im Editor,
  gespeichert wird der Prozessorzustand. Gebrochen ist deshalb der
  Speicherweg selbst: der Schreiber der Mitgliederliste
  (`eq-copilot/plugin/state/NakamaState.cpp`, vorübergehend, bytegleich
  zurück, Muster A-43) legt für jedes Mitglied das Label des ersten ab — der
  geladene Name weicht vom bestätigten ab.
- **A-95 — der Klick im Bein.** Das Bein ruft `mouseDown` des echten Editors
  zweimal mit einem Ereignis in der Mitte der Zeile: JUCE stellt einen Klick
  auf die Editorfläche der Komponente selbst zu und danach ihren
  Mauslisteners, und der Editor hört sich über `addMouseListener (this,
  true)` selbst zu (`juce_Component.cpp:2269`, `:2276`, `:180-181`). Der
  zweite Aufruf findet A nicht mehr als Hauptziel und schreibt nichts
  (gemessen: 1 Host-Dirty).
- **A-96 — M-71: 121 erreicht den Prozessor über den Editor nie.** Der Editor
  kappt vor dem Aufruf auf 120; „121 wird abgewiesen (`Ipc.cpp:1208`)" ist
  deshalb am Prozessor direkt gemessen (d), die Kappung im Editor als (b), die
  Eingabebeschränkung als (a).

### 38.7 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz A

| Abschnitt | Punkt | Änderungssatz A |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge, Joins, Stopp-Fenster | angewandt: Reihenfolge „bestätigen, dann Ziel wechseln" im Klick (A-89); der Entwurf überlebt keinen Wechsel der Quellidentität (instance_id oder Nonce, 312/M-69); das Bein baut den Editor vor dem Prozessor ab |
| C Verträge und Längen | Textfeld mit Schema-Länge, Zeichen gegen Bytes | angewandt: 0, 120, 121 Codepoints mit U+1F3B9 außerhalb der BMP; Eingabebeschränkung, Kappung und Längenwache je einzeln (312/M-71) |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel bei jedem Bau, jeder Gegenprobe und jedem Rotbeweis; kein neues Bein; die Fokus-Naht wirkt nur unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: B15 und B14 nennen je Satz einen gemessenen Fall; „Fokusverlust" nennt den gemessenen Ablauf (nach dem Klick schreibt er nicht auf B) |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerzeilen B15 und B14, Kopfkommentar von `ShotTestMain.cpp` und die Kommentare an Kennung, Ladestelle, Commit, Klick und Escape; Manifestkopf (Etappe, Basis-SHA, Kanon vorher) |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: sieben Rohdateien `docs/beweise/roh/NAK-312-rot-M-66.txt` bis `NAK-312-rot-M-72.txt`, eine Gegenprobe; auch die zwei Selbstauditprüfungen sind gebrochen (Rohdatei M-66) |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Host-Dirty-Zähler, Codepointlängen, Prüfungszahlen und Uhrzeiten aus Läufen und `Get-Date` |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: speichern↔laden (312/M-72 im selben Commit wie der Fix), öffnen↔schließen (Editor auf und ab je Fall), Laden des Textes und Kennung an derselben Stelle |

### 38.8 Selbstaudit — Änderungssatz A

- **Zielwechsel mit offenem Entwurf in jede Richtung.** A nach B (312/M-66),
  B nach A (Gegenrichtung), Klick auf das eigene Ziel (keine Bestätigung),
  Wechsel ohne Handgriff durch Wegfall (M-69 (a)), neue Nonce mit und ohne
  Tick (M-69 (b), (c)). Der zweite `mouseDown` desselben Klicks schreibt
  nichts (A-95).
- **Leerer Entwurf.** Löscht nie einen fremden Namen (312/M-67); ein leerer
  Entwurf der eigenen Quelle ist erlaubt und wird angenommen (312/M-71 (c),
  Vertrag `nakama-state-v2.md:107-108`).
- **Startquelle verschwindet.** Der Entwurf verfällt ohne Mutation und ohne
  Dirty; das Feld zeigt danach das Label des Ziels, das es bedient.
- **Zahlenrand Label.** 0, 120, 121 Codepoints (312/M-71). Der Zahlenrand
  der Quellenzahl gehört Änderungssatz B.
- **Stale Closures.** `onEscapeKey` fängt `this` und lebt im Feld, einem
  Mitglied des Editors; der `TextEditor` stellt Escape über eine gepostete
  Nachricht mit schwacher Referenz auf sich selbst zu und verwirft sie nach
  seinem Ende. `mouseDown` kopiert die gewählte instance_id, bevor es den
  Prozessor ruft.
- **Save/Load und Host-Dirty genau einmal.** Je geändertem Namen genau eine
  Meldung (312/M-66: 1, Gegenrichtung: 2, M-71: 1, 2, 3); unveränderter Text
  keine (M-70); speichern und laden erhält beide Namen und ihre Zuordnung
  (M-72).
- **UI-Text, der lügt.** Kein neuer Text. „Stored fallback label saved." nach
  der Bestätigung im Klick ist wahr (A wurde gespeichert) und verschwindet im
  nächsten Tick, weil das neue Ziel bestätigt und schreibbar ist.
- **Tasten ändern nie ihre Maße.** Keine Geometrie geändert; 26 Bilder
  bytegleich zur Gegenprobe.
- **Behauptungszeile nicht weiter als der Test.** Die B15- und B14-Sätze sind
  Satz für Satz gegen die Prüfnamen gelesen.
- **Kommentare (R-312-14).** Neu: an der Kennung, der Ladestelle, dem Commit,
  dem Klick und Escape. Geprüft und wahr geblieben: „Genau ein gemeinsamer
  Aktionssatz fuer genau das aktuelle Hauptziel" (`PluginEditor.h:180-181`)
  und der Kopf von `Sonde012ProjectReloadTest.cpp`.

### 38.9 Hinweise an den Dirigenten — Änderungssatz A

- **Produktwirkung — ein Klick auf eine andere Quelle speichert den offenen
  Namen.** Wer im Namensfeld tippt und dann eine andere Zeile anklickt,
  speichert den Text jetzt für die Quelle, die er bearbeitete (wie bisher
  beim Verlassen des Feldes), und das Feld zeigt danach den Namen der neuen
  Quelle. Bisher landete der Text beim nächsten Enter auf der neuen Quelle,
  und ein leerer Text löschte deren Namen.
- **Produktwirkung — Escape nimmt den Entwurf zurück.** Escape im Namensfeld
  stellt den gespeicherten Namen wieder her; bisher blieb der Text stehen
  und wurde später gespeichert.
- **Produktwirkung — ein Entwurf verfällt still,** wenn die Quelle vor dem
  Bestätigen verschwindet oder mit neuer Runtime-Nonce zurückkommt (die
  Sonde hat neu gestartet): das Feld zeigt den gespeicherten Namen, ohne
  Meldung. Ob dafür eine Meldung nötig ist, ist eine Designfrage (geparkt).
- **Beobachtung:** JUCE stellt einen Klick auf die Editorfläche zweimal an
  `mouseDown` zu (A-95); das galt schon vorher für `waehleSourcesHauptziel`
  (der zweite Aufruf ist dort ein No-op) und ist für die Bestätigung gemessen
  harmlos.

### 38.10 Gebaut — Änderungssatz B (Ersatzziel und Aktionssteuerung; T3-07-05 Teil a)

Änderungssatz A ist Commit `db682244` (22:56:25), gepusht. Änderungssatz B:
dieser Commit (Betreff „NAK-312 Etappe 6b: Änderungssatz B …"); sein SHA steht
im Abschnitt des Änderungssatzes C. Basis des Änderungssatzes B: `db682244`.
Zeilen zum Endstand B.

- `eq-copilot/plugin/src/SourcesModel.cpp`: die Anzeigeordnung als eine
  Funktion `vorInAnzeige` im dateilokalen Namensraum (`:347-366`: Mixerindex,
  sichtbarer Name über den Sortierschlüssel, Codepunkte, instance_id); `sicht()`
  sortiert damit (`:1672-1675`), und `stelleZielSicher` (`:1783-1799`) wählt
  den Ersatz als Minimum derselben Ordnung statt `eintraege.begin()` — die
  erste Quelle in Anzeigeordnung, also die erste gezeichnete Zeile (A-97).
  Ohne Eintrag bleibt das Ziel leer. Der Kopf `SourcesModel.h` ist unberührt.
- `eq-copilot/plugin/src/PluginEditor.cpp`:
  - `aktualisiereSourcesSteuerung` (`:993-1068`): sucht das Hauptziel nur in
    `sourcesAnzeige.quellen[0 … min (sourcesZeilen().size(), quellen.size()))`
    (`:996-1008`), dieselbe Rechnung wie Zeichnen und Klick. Liegt es dahinter:
    `hatZiel = false`, Knopf und Labelfeld unsichtbar, `sourcesAktionsZiel`
    leer, das Modell unberührt. Ein offener Entwurf wird davor über
    `uebernehmeSourcesLabel` abgeschlossen (`:1009-1015`, M-85), solange das
    Aktionsziel noch die Startquelle nennt. Der Status sagt in diesem Fall
    „Main target is outside the drawn list - select a drawn source."
    (`:1029-1031`, A-99).
  - `paintMainFlaeche` (`:1467-1477`): die Beschriftung „Stored label fallback
    (untrusted user text)" steht nur, wenn das Labelfeld sichtbar ist (A-100).
- `eq-copilot/plugin/src/PluginEditor.h`: zwei Testzugänge unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` (`:118-121`: Sichtbarkeit von Knopf und
  Labelfeld).
- `eq-copilot/plugin/tests/ShotTestMain.cpp` (B15): `ersatzSicht` (`:95-110`,
  kleinste instance_id in der letzten Anzeigezeile) und vier neue Bilder des
  Sichtsatzes mit 20, 21, 32 und 64 Quellen (`:156-160`, A-102); der Abschnitt
  `nak312Ersatzziel` (`:802-1056`) mit `ZielBuehne`, `umordnungsSicht` und den
  Fällen 312/M-73 (16, 20, 21, 32, 64), M-74 (Beitritt, Hostbusnamen; mit
  Status, Pixelprüfung der Feldbeschriftung und dem Bild
  `nak312-hauptziel-ausserhalb.png`), M-85 und M-75 (0, 1, 16); aufgerufen aus
  `main`; Kopfkommentar ergänzt.
- `eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp` (B14):
  `bestand_ueberlebt_die_sichtgrenze` (`:2634-2727`, 312/M-76 mit Vorbedingung
  und Zusage).
- `tools/beweise.ps1`: die Behauptungen von B15 und B14 um genau die
  gemessenen Fälle ergänzt (BOM und CRLF erhalten, Parser 0 Fehler).
- Nicht angefasst: `SourcesModel.h`, `sourcesZeilen`, der Klick, das
  Quellenmodell jenseits der Ersatzwahl, die Kapazität (U51), die Dateien der
  Etappe 6a, Schemas, Fixtures, Identität, `design/`.

### 38.11 Gegenprobe am Stand A (heute rot) — Änderungssatz B

`docs/beweise/roh/NAK-312-etappe6b-gegenprobe-B.txt`, Bau 23:05:50 bis
23:06:04, Lauf 23:06:04 bis 23:06:17: `PluginEditor.cpp` und
`SourcesModel.cpp` bytegleich auf `db682244`, `PluginEditor.h` mit den zwei
Testzugängen, die Tests in der Endfassung; SHA-256 je Datei in der Rohdatei;
danach der Fix-Stand bytegleich zurück (SHA-256 gleich), neu gebaut, B15 und
B14 grün. B15 Exit 1, **8 Fehler — genau die „heute rot"-Prüfungen**: 312/M-73
in allen fünf Größen (das Ersatzziel ist die kleinste instance_id und steht in
der letzten Anzeigezeile; ab 21 Quellen ungezeichnet, mit sichtbarem, scharfem
Knopf), M-74 in beiden Umordnungen (Knopf und Labelfeld auf dem Hauptziel in
Anzeigezeile 21 sichtbar, die Feldbeschriftung gezeichnet: 604 Pixel), M-85
(das Ziel bleibt gesetzt, der Entwurf unbestätigt). B14 Exit 1: allein die
Vorbedingung von 312/M-76 (sie ist die Folge aus M-73). Grün, wie ihre
Etiketten sagen: 312/M-75 in allen drei Größen und die Bestandshälfte von M-76.

### 38.12 Matrixzeilen des Änderungssatzes B

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-73 | `312/M-73 ersatzziel_folgt_der_anzeigeordnung` mit 16, 20, 21, 32 und 64 Quellen, dazu vier Bilder `sonde012-{20,21,32,64}-sources-ersatzziel.png`, B15 | grün: das Ersatz-Hauptziel steht je in Anzeigezeile 1; gezeichnet 16, 20, 20, 20, 20 Zeilen; Aktionsziel, Knopf und Labelfeld auf ihm | heute rot (Gegenprobe); `eintraege.begin()` statt der Anzeigeordnung → das Ersatzziel in der letzten Anzeigezeile → rot; `docs/beweise/roh/NAK-312-rot-M-73.txt` |
| M-74 | `312/M-74 aktionssteuerung_bindet_nur_an_gezeichnete_zeilen` (Umordnung durch Beitritt; durch Hostbusnamen), B15 | grün: Hauptziel per Handgriff in Zeile 5, nach der Umordnung in Anzeigezeile 21; Knopf und Labelfeld unsichtbar, Aktionsziel leer, Hauptziel im Modell unverändert, Modellrevision und Host-Dirty gleich, Status „Main target is outside the drawn list", 0 Pixel der Feldbeschriftung | heute rot (Gegenprobe); Suche über die ganze Liste → Knopf und Labelfeld sichtbar → rot (mit M-85); Beschriftung ohne Bedingung → rot; Statuszweig entfernt → rot; `docs/beweise/roh/NAK-312-rot-M-74.txt` |
| M-85 | `312/M-85 offener_entwurf_wird_vor_dem_leeren_des_ziels_bestaetigt`, B15 | grün: 25 bestätigte Mitglieder; der Entwurf steht auf der Startquelle, 0 fremde Labels geschrieben, genau 1 Host-Dirty; danach Knopf und Labelfeld unsichtbar, Hauptziel unverändert | heute rot (Gegenprobe); Abschluss hinter das Leeren des Ziels → der Entwurf verfällt still → rot; `docs/beweise/roh/NAK-312-rot-M-85.txt` |
| M-75 | `312/M-75 (0 Quellen)`, `(1 Quelle)`, `(16 Quellen)`, B15; die Bilder `sonde012-0-sources.png`, `sonde012-1-source.png`, `sonde012-16-sources.png` bleiben bytegleich | grün: 0 Quellen ohne Hauptziel und ohne Aktionssteuerung; bei 1 und 16 ist jede Zeile gezeichnet und per Klick mit Knopf und Labelfeld erreichbar (1 von 1, 16 von 16) | Regressionswache; gezeichnete Menge um eins zu kurz → 1 und 16 rot (A-98); `docs/beweise/roh/NAK-312-rot-M-75.txt` |
| M-76 | `312/M-76 Vorbedingung (Folge aus 312/M-73)` und `312/M-76 bestand_ueberlebt_die_sichtgrenze`, B14 | grün: 20 Zeilen gezeichnet, das Ersatzziel in Zeile 1 mit Aktionssteuerung; 40 Mitglieder geladen, im Modell nach der Folge, gespeichert und frisch geladen; die unbekannte Eigenschaft reist gleich; 0 Host-Dirty | Regressionswache; `projektReload` übernimmt höchstens 20 → das Modell trägt 20 → rot; `docs/beweise/roh/NAK-312-rot-M-76.txt` |

### 38.13 Einzelbeine — Änderungssatz B

Die 15 Ziele aus §38.5, dazu `EqCopSonde012SourcesModelTest` und
`EqCopSonde012SourcesLatencyTest` (beide übersetzen `SourcesModel.cpp`), in
einem cmake-Aufruf gebaut, 23:11:29 bis 23:13:33, Exit 0; jedes Binary jünger
als die jüngste Quelle (`SourcesModel.cpp` 23:09:40, die letzte Rücknahme).
Gefahren 23:13:33 bis 23:13:46:

- B15 `EqCopShot --sonde012-suite`: Exit 0, „SONDE-012 SHOTS 29/29 gruen"
  (vorher 25; neu die vier Ersatzbilder), „NAK-312 PANEL 3", „NAK-312 LABEL
  15", „NAK-312 ZIEL 11 geprueft, 0 Fehler".
- B14 `EqCopSonde012ProjectReloadTest`: Exit 0, 237/237 (vorher 234; neu die
  zwei Prüfungen von 312/M-76 und die Aufbauprüfung aus `mainAnlegen`).
- B13 `EqCopSonde012SourcesModelTest`: Exit 0, 88/88.
- A1 `EqCopNullTest`: Exit 0, „NULLTEST OK".
- `EqCopSonde012SourcesLatencyTest` ist gebaut, aber nicht einzeln gefahren:
  er verlangt einen Pipenamen aus dem Probe-Namensraum und läuft im Kanon
  (Exit 3 ohne ihn, „VERWEIGERT: genau ein Pipename aus dem Probe-Namensraum
  ist Pflicht").
- Die 25 Bilder des bisherigen Sichtsatzes und das Panelbild sind in der
  Gegenprobe und am Endstand B bytegleich; die vier neuen zeigen am Stand A
  das Hauptziel in der letzten Anzeigezeile (bei 21, 32 und 64 außerhalb der
  Liste, bei 20 in Zeile 20) mit scharfem Knopf, am Endstand „Host Bus 1
  MAIN" in Zeile 1. Das
  Bild `nak312-hauptziel-ausserhalb.png` zeigt den Zustand von M-74: rechts
  Name und Status, kein Knopf, kein Feld, keine Feldbeschriftung.
- clang-tidy-Teilmessung (`--nur src/PluginEditor`, `--nur src/SourcesModel`,
  23:14:04 bis 23:15:20): 15 wie in §38.5 und 1 in
  `eq-copilot/plugin/vertrag/NakamaVertrag.h:29` (unberührt); keine neue.

### 38.14 Abweichungen vom Bauplan, begründet — Änderungssatz B

- **A-97 — der Ersatz ist das Minimum der Anzeigeordnung; die Zeilenzahl ist
  keine Eingabe.** §7.6 lässt `stelleZielSicher` „nur unter den Zeilen, die
  `sourcesZeilen()` wirklich herstellt" wählen. Das Modell kennt die Geometrie
  des Editors nicht, und sie gehört ihm nicht (die Zeilenzahl darf nie als 20
  im Modell stehen, §7.7). Die erste Quelle in Anzeigeordnung steht aber immer
  in der ersten gezeichneten Zeile, sobald die Fläche überhaupt eine Zeile
  herstellt — bei 760x430 immer (366 Pixel ≥ 18, nachgerechnet). Die Wahl ist
  damit ohne Zahl richtig; M-73 misst sie gegen `sourcesZeilen()`. Die
  Ordnung ist eine Funktion für Sortierung und Wahl, damit beide nie
  auseinanderlaufen; `SourcesModel.h` bleibt unberührt (Ticketgrenze).
- **A-98 — M-75: die Matrixmutation liefe in undefiniertes Verhalten.** „Die
  Ersatzwahl ohne den leeren Fall" dereferenzierte bei leerer Liste `end()`
  einer `std::map`. Gebrochen ist statt dessen die Rechnung der gezeichneten
  Menge (um eins zu kurz): genau der Fehler, vor dem die Zeile wacht.
- **A-99 — ehrlicher Status statt eines falschen.** Liegt das Hauptziel hinter
  der letzten gezeichneten Zeile, sagte der Status nach der neuen Regel „Bind
  the source before editing its stored label." — für eine bestätigte Quelle
  falsch (`CLAUDE.md`, keine toten Elemente, ehrliche Zustandsmeldung). Er
  sagt jetzt „Main target is outside the drawn list - select a drawn
  source." in der vorhandenen Statuszeile; kein neues Element, keine
  Geometrie. Gemessen in M-74.
- **A-100 — die Beschriftung des Labelfelds folgt dem Feld.** Das Bild des
  Zustands von M-74 zeigte die Beschriftung „Stored label fallback (untrusted
  user text)" ohne Feld — eine tote Beschriftung, die erst dieser
  Änderungssatz erzeugte. `paintMainFlaeche` zeichnet sie nur bei sichtbarem
  Feld (eine Bedingung, keine Geometrie; `paintMainFlaeche` steht nicht in
  der Ticketgrenze, die Stelle ist die Folge dieses Änderungssatzes). Gemessen
  an den Pixeln ihrer Zeile in M-74; die 25 bisherigen Bilder bleiben
  bytegleich, weil dort ein Ziel gezeichnet ist.
- **A-101 — M-74: Umordnung über die Fixture.** `setzeFixtureFuerTest` setzt
  das Hauptziel aus den Markierungen der Zeilen neu; die neue Sicht markiert
  deshalb dieselbe Quelle, die zuvor per Handgriff gewählt war. Geprüft ist,
  dass es danach dieselbe ist und der Tick weder die Modellrevision noch den
  Host-Dirty-Zähler bewegt („kein Schreibversuch").
- **A-102 — die vier Bilder von M-73 mit der Ersatzsicht.** `sicht (n)`
  markiert Zeile 1 schon als Hauptziel; die Bilder tragen deshalb die Sicht
  ohne Hauptziel mit der kleinsten instance_id in der letzten Anzeigezeile,
  und das Modell wählt den Ersatz.
- **A-103 — M-76 in zwei Prüfungen.** Die Vorbedingung (die Folge aus M-73
  lief: 20 Zeilen, Ersatz in Zeile 1) und die Zusage (Bestand) sind getrennt,
  damit die Regressionswache nicht an M-73 hängt (in der Gegenprobe fällt nur
  die Vorbedingung).
- **A-104 — M-85 mit echten Mitgliedern.** `benenneSourcesHauptziel` schreibt
  nur in ein Mitglied des Prozessorzustands; die 25 Quellen sind deshalb über
  den ACK-Weg bestätigte Mitglieder.

### 38.15 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz B

| Abschnitt | Punkt | Änderungssatz B |
|---|---|---|
| A Rückstau | Politik bei voll, Abflussweg, Zähler | nicht zutreffend: keine Queue berührt |
| B Lebenszyklus | Reihenfolge | angewandt: Abschluss des Entwurfs vor dem Leeren des Ziels (312/M-85); Editor vor Prozessor abgebaut |
| C Verträge und Längen | Zahlenrand | angewandt: 0, 1, 16, 20, 21, 25, 32, 40, 41, 64 Quellen (M-73 bis M-76; 41 im Beitrittsfall von M-74) |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel bei jedem Bau, der Gegenprobe und jedem Rotbeweis; kein neues Bein |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: B15 und B14 nennen je Satz einen gemessenen Fall; die Beschriftung ist an Pixeln gemessen, nicht behauptet |
| E Behauptung ≤ Messung | geänderte Zusage an drei Stellen | angewandt: Runnerzeilen B15 und B14, Kopfkommentar von `ShotTestMain.cpp`, Kommentare an Ordnung, Ersatzwahl, Suche, Abschluss und Beschriftung; Manifestkopf |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: fünf Rohdateien `docs/beweise/roh/NAK-312-rot-M-73.txt` bis `NAK-312-rot-M-76.txt` und `NAK-312-rot-M-85.txt` (M-74 in drei Teilen), eine Gegenprobe |
| E Behauptung ≤ Messung | Zahlen gemessen | angewandt: Anzeigezeilen, Zeilenzahlen, Pixelzahlen, Mitgliederzahlen und Uhrzeiten aus Läufen |
| F Änderungssatz | Beziehungen im selben Commit | angewandt: speichern↔laden (312/M-76), Sortierung und Ersatzwahl aus einer Ordnung, Suche und Abschluss des Entwurfs in einem Satz |

### 38.16 Selbstaudit — Änderungssatz B

- **Zahlenrand Quellenzahl.** 0 (kein Ziel, keine Zeile), 1 und 16 (alle
  gezeichnet und erreichbar), 20 (alle 20 gezeichnet, Ersatz in Zeile 1), 21,
  32, 64 (20 gezeichnet), 25 und 41 (Umordnung), 40 (Bestand). Über 64 nimmt
  der Speicher nicht an (`maxMainProjectMitglieder`, unverändert).
- **Umordnung durch Hostbusname.** Gemessen (M-74, zweite Umordnung, und
  M-85): 16 Quellen dahinter bekommen einen Namen, der davor sortiert.
- **Startquelle verschwindet.** Der Ersatz ist die erste gezeichnete Zeile
  (M-73); das Hauptziel wechselt nur, wenn es fehlt (`stelleZielSicher`
  unverändert in seiner Bedingung).
- **Kein automatischer Zielwechsel.** Liegt das Hauptziel hinter Zeile 20,
  bleibt es Hauptziel; Modellrevision und Host-Dirty bleiben (M-74). Ein
  Klick auf eine gezeichnete Zeile wählt wie bisher.
- **Stale Closures.** Keine neuen Lambdas mit Fang; `vorInAnzeige` ist eine
  reine Funktion.
- **Save/Load und Host-Dirty.** Der Bestand bleibt (M-76), der Abschluss des
  Entwurfs meldet genau einmal (M-85), die Umordnung selbst nicht (M-74).
- **UI-Text, der lügt.** Gefunden und berichtigt: „Bind the source …" für eine
  bestätigte, ungezeichnete Quelle (A-99). Die rechte Seite zeigt weiter
  Name und Zustand des Hauptziels — das ist wahr, es ist Hauptziel.
- **Tote Elemente.** Gefunden und berichtigt: die Beschriftung ohne Feld
  (A-100).
- **Tasten ändern nie ihre Maße.** Keine Geometrie geändert; die bisherigen
  Bilder sind bytegleich.
- **Behauptungszeile nicht weiter als der Test.** Gegen die Prüfnamen gelesen.
- **Kommentare (R-312-14).** Neu an Ordnung, Ersatzwahl, Suche, Abschluss und
  Beschriftung. Geprüft und wahr geblieben: der Kommentar über
  `sourcesAktionKnopf` („Genau ein gemeinsamer Aktionssatz …") und
  `PluginEditor.cpp` an der Main-Fläche („Die abgenommene funktionale
  Pruefflaeche hat genau dieses Mass").
- **Ein fehlgeschlagener Abschluss im Umordnungsfall.** Wiese
  `benenneSourcesHauptziel` den Entwurf dort ab (Schreibrecht verloren),
  verdrängte der Status „outside the drawn list" die Fehlermeldung, weil das
  Aktionsziel im selben Durchlauf wechselt. Das Feld ist ohne Schreibrecht
  gesperrt; der Fall ist nicht gemessen (Hinweis §38.17).

### 38.17 Hinweise an den Dirigenten — Änderungssatz B

- **Produktwirkung — das Ersatz-Hauptziel ist die erste Zeile der Liste.**
  Verschwindet das Hauptziel, wird die oberste gezeichnete Quelle das neue;
  bisher war es die Quelle mit der kleinsten inneren Kennung, bei mehr als 20
  Quellen oft eine ungezeichnete mit scharfem Knopf.
- **Produktwirkung — ein Hauptziel hinter Zeile 20 hat keine Handgriffe.**
  Rückt das Hauptziel durch neue Quellen oder umbenannte Hostbusse hinter die
  letzte Zeile, verschwinden „Remove/Bind source" und das Namensfeld samt
  Beschriftung; die Statuszeile sagt „Main target is outside the drawn list -
  select a drawn source.", rechts stehen weiter Name und Zustand des
  Hauptziels. Ein offener Name wird vorher gespeichert. Das Hauptziel wechselt
  nicht von selbst; der User wählt eine gezeichnete Zeile. Ob die Liste mehr
  Quellen zeigen soll, bleibt U51 (Kapazität).
- **Nicht gemessen:** der Abschluss eines Entwurfs, den der Prozessor im
  Umordnungsfall abweist; seine Fehlermeldung würde vom neuen Status verdrängt
  (§38.16). Vorschlag: Registerzeile, wenn der Dirigent den Fall für
  erreichbar hält.

### 38.18 Gebaut — Änderungssatz C (Kennungskonflikt-Panel; NAK-349)

Änderungssatz B ist Commit `53754c66` (23:18:40), gepusht. Änderungssatz C:
dieser Commit (Betreff „NAK-312 Etappe 6b: Änderungssatz C …"). Basis:
`53754c66`. Zeilen zum Endstand C.

- `eq-copilot/plugin/src/PluginEditor.cpp`, `zeigeKonflikt` (`:571-648`): der
  Rückruf des Panels fängt `juce::Component::SafePointer<EqCopilotEditor>`
  statt rohem `this` (`:621`, `:631`) und prüft ihn als Erstes (`:633-635`);
  ist der Editor fort, kehrt er ohne Zugriff zurück. Den Prozessor erreicht er
  nur als `ed->processor`; der Handgriff bei lebendem Editor ist derselbe
  (neue Kennung, Statusmeldung, `dismiss()` im Panel). Kommentar am
  Übergabepunkt (`:613-620`). Testzugang unter
  `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`: die Marke
  `testzugang::konfliktMarkeFuerTest()` (`:551-565`), gefragt hinter der
  Lebendprüfung und vor dem ersten Zugriff (`:636-639`) — am Stand B für die
  Gegenprobe als erste Anweisung des Rückrufs (R-312-23).
- `eq-copilot/plugin/tests/ShotTestMain.cpp` (B15): Deklaration der Marke; der
  Abschnitt `nak312Konfliktpanel` (`:1062-1203`) mit `konfliktLauf`
  (Kanarienvogel, Legacy-Gen auf dem Heap, Popover über den normalen
  Handgriff, `neuKnopf->triggerClick()`, `editor.reset()`, wahlweise
  `proz.reset()`, `pumpe`, `PanelDirty`) und den Fällen 312/M-91 (a) und (b)
  sowie 312/M-92; aufgerufen aus `main`; Kopfkommentar ergänzt.
- `tools/beweise.ps1`: die Behauptung von B15 um genau diese Fälle ergänzt.
- Nicht angefasst: das Messpunkt- und das Hinweispanel, der Konfliktknopf und
  seine Sichtbarkeit, `neueSensorId`, die Dateien der Etappe 6a und der
  Änderungssätze A und B jenseits dieser Stelle.

### 38.19 Gegenprobe am Stand B (heute rot, R-312-23) — Änderungssatz C

`docs/beweise/roh/NAK-312-etappe6b-gegenprobe-C.txt`, Bau 23:20:48 bis
23:20:55, Lauf 23:20:55 bis 23:21:00. `PluginEditor.cpp` auf `53754c66` plus
allein der Testzugang: die Markenfunktion und die Marke als **erste**
Anweisung des Rückrufs, ohne Editorzustand (R-312-23); der Rückruf fängt
weiter rohes `this`. B15 Exit 1, der Prozess endet regulär: **312/M-91 (a)
und (b) Marke 1** — der gepostete Klick wird nach dem Ende des Editors
zugestellt und erreicht den Rückruf, der ohne die Marke `this->processor` aus
freigegebenem Speicher läse (`docs/beweise/roh/NAK-349-quellvalidierung.md`
V-3). Grün: 312/M-92 (Regressionswache) und alle übrigen Fälle.

### 38.20 Matrixzeilen des Änderungssatzes C

| ID | Test (Name, Bein) | Ergebnis | Rotbeweis |
|---|---|---|---|
| M-91 | `312/M-91 (a) konfliktrueckruf_nach_editorende_ist_wirkungslos` (Prozessor lebt) und `(b)` (Prozessor danach zerstört), B15 | grün: je Popover geöffnet, `triggerClick`, Editor zerstört, (b) auch der Prozessor, dann gepumpt bis das Panel abgebaut ist; Marke 0, Kennung unverändert, 0 Host-Dirty | heute rot (Gegenprobe: Marke 1 in beiden Varianten); Lebendprüfung entfernt → Marke 1 → rot, ohne Lauf in undefiniertem Verhalten; `docs/beweise/roh/NAK-312-rot-M-91.txt` |
| M-92 | `312/M-92 normaler_handgriff_bleibt`, B15 | grün: der Klick läuft bei lebendem Editor; neue Kennung, genau 1 Host-Dirty, Status „Neue Kennung vergeben — dieser Messpunkt meldet sich frisch an.", die Box schließt | Regressionswache; Lebendprüfung scheitert unbedingt → keine neue Kennung, 0 Host-Dirty → rot; `docs/beweise/roh/NAK-312-rot-M-92.txt` |

### 38.21 Einzelbeine — Änderungssatz C

Die 15 Ziele aus §38.5 in einem cmake-Aufruf gebaut, 23:23:29 bis 23:25:21,
Exit 0; jedes Binary jünger als `PluginEditor.cpp` (23:22:34, die letzte
Rücknahme). Gefahren 23:25:21 bis 23:25:34:

- B15 `EqCopShot --sonde012-suite`: Exit 0, 29/29 Shots, „NAK-312 PANEL 3",
  „LABEL 15", „ZIEL 11", „KONFLIKT 3 geprueft, 0 Fehler".
- A1 `EqCopNullTest`: Exit 0, „NULLTEST OK".
- Zusätzlich B14 `EqCopSonde012ProjectReloadTest`: Exit 0, 237/237.
- Alle 30 Bilder des Beins sind in der Gegenprobe und am Endstand C
  bytegleich.
- clang-tidy-Teilmessung `--nur src/PluginEditor` (nach dem Bau): 15 wie in
  §38.5, keine neue — der `SafePointer` wird als Wert gefangen, nicht als
  Parameter übergeben (der Befund der Etappe 4, §27.5, entsteht nicht).
- Danach ist allein der Doxygen-Kommentar der Marke umbrochen (gleiche
  Zeilenzahl); `EqCopShot` und `EqCopNullTest` neu gebaut 23:28:10 bis
  23:28:23 (Binaries jünger als die Quelle), B15 und A1 erneut Exit 0 mit
  denselben Summen, gefahren bis 23:28:29.

### 38.22 Abweichungen vom Bauplan, begründet — Änderungssatz C

- **A-105 — M-91 prüft die Kennung in (b) bis zum Prozessorende.** Nach dem
  Ende des Prozessors ist seine Kennung nicht mehr lesbar; wie bei 312/M-35
  (A-46) wird sie unmittelbar vor `proz.reset()` gelesen, danach belegt allein
  die Marke (0), dass der Rückruf nichts mehr anfasst.
- **A-106 — M-92 prüft die Kennung auf Wechsel, nicht auf einen Wert.**
  `neueSensorId` zieht eine zufällige Uuid; gemessen ist „ungleich der
  vorigen und nicht leer".

### 38.23 Prüfliste (`tools/dirigent/pruefliste.md`), abgehakt für Änderungssatz C

| Abschnitt | Punkt | Änderungssatz C |
|---|---|---|
| A Rückstau | – | nicht zutreffend |
| B Lebenszyklus | Reihenfolge beim Abbau, Rückruf nach dem Ende | angewandt: Klick einstellen, Editor abbauen, (b) Prozessor abbauen, dann zustellen (312/M-91); Klick bei lebendem Editor, danach Abbau (312/M-92) |
| C Verträge und Längen | – | nicht zutreffend: kein Draht-, Schema- oder Statefeld |
| D Bau- und Prüfriegel | fail-closed, Frische | angewandt: Binaryzeitstempel bei Bau, Gegenprobe und Rotbeweis; die Marke nur unter `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3` |
| E Behauptung ≤ Messung | Behauptung nicht weiter als der Test | angewandt: die B15-Zeile nennt den geposteten Klick, die zwei Varianten und den Handgriff — genau die drei Fälle |
| E Behauptung ≤ Messung | jede neue Prüfung einmal gebrochen | angewandt: `docs/beweise/roh/NAK-312-rot-M-91.txt`, `docs/beweise/roh/NAK-312-rot-M-92.txt`, eine Gegenprobe |
| F Änderungssatz | öffnen↔schließen | angewandt: Popover öffnen, Klick, Box schließt (M-92); Abbau vor der Zustellung (M-91) |

### 38.24 Selbstaudit — Änderungssatz C

- **Der Konfliktrückruf bei totem Editor mit lebendem und mit totem
  Prozessor.** Beide gemessen (312/M-91 (a), (b)): die Lebendprüfung fällt vor
  jedem Zugriff, die Marke bleibt 0. `getComponent()` liest nur die schwache
  Referenz, nie den Editor; `ed->processor` wird erst nach der Prüfung gelesen.
- **Stale Closures und SafePointer im Editor.** Der Rückruf fängt eine Kopie
  des `SafePointer` (konstant, als Wert); das Panel fängt `this` des Panels
  für `dismiss()`, das mit dem Panel stirbt. Kein Fang einer Referenz auf ein
  Editormitglied.
- **Mausklick.** Ein Mausklick beginnt immer bei lebendem Editor (V-2); die
  Prüfung ändert dort nichts (312/M-92 fährt denselben Rückruf).
- **Ein Abbau innerhalb des Rückrufs** (Host baut in `restartComponent` ab,
  V-2) bleibt ungemessen: danach schriebe `ed->statusMeldung` in einen toten
  Editor. Die Lebendprüfung steht am Eintritt, nicht nach `neueSensorId` —
  genau das Muster der Hinweisliste und des Messpunktpanels; Hinweis §38.25.
- **UI-Text, Tasten, Fläche.** Kein neuer Text, keine Geometrie; 30 Bilder
  bytegleich.
- **Kommentare (R-312-14).** Neu am Übergabepunkt und an der Marke. Geprüft
  und wahr geblieben: der Kopf von `zeigeKonflikt` („die sichtbare
  Entscheidung beim Kennungs-Konflikt") und der Kommentar der Hinweisliste
  („SafePointer: die CallOutBox kann den Editor überleben").

### 38.25 Hinweise an den Dirigenten — Änderungssatz C

- **Produktwirkung: keine sichtbare.** Der Knopf im Kennungskonflikt-Popover
  wirkt wie bisher. Neu ist nur, dass ein Klick, den der Host erst nach dem
  Schließen des Editorfensters zustellt, nichts mehr tut — bisher lief er auf
  freigegebenem Speicher (undefiniertes Verhalten, mit möglicher neuer
  Kennung und Host-Dirty). Ob FL einen solchen Klick je zustellt, ist nicht
  gemessen (V-2).
- **Restfall, nicht gebaut:** baut der Host den Editor innerhalb des Rückrufs
  ab (während `neueSensorId` den Host benachrichtigt), träfe die folgende
  Statuszeile einen toten Editor. Die Quelle gibt dafür keinen Anhalt (V-2);
  eine zweite Prüfung nach `neueSensorId` wäre der kleinste Nachzug.
  Vorschlag: Registerzeile.

### 38.26 Abschluss der Etappe 6: die A3-Behauptungszeile

Änderungssatz C ist Commit `6fbe396a` (23:28:48), gepusht. Die
A3-Behauptungszeile in `tools/beweise.ps1` nennt jetzt die Fälle der Etappe
6a — 312/M-55 bis M-58 und M-84 (Rollenwechsel, Legacy, drei Netze) sowie
312/M-59 bis M-65 und M-86 (reset und releaseResources) — mit genau den in
§36.4 und §36.12 gemessenen Werten und Bedingungen (§36.9, §36.24): Rücknahme
vor der Klassifikation, 40 bitgleiche Blöcke ohne Ereignis, SOLO in Legacy
ohne Auftrag und ohne Aus-Knopf, genau ein begin und ein end im weichen
Ausfade innerhalb der E-31-Schranke, kein Wiedereinblenden, die drei Netze in
der Main-Fläche; reset bei angehaltenem Transport mit 40 bitgleichen Blöcken,
genau einem end ohne Projektzeit, 0 Allokationen, stehendem Oversize-Riegel
und stehendem Auftrag (neues begin bei laufendem Transport), je begin genau
ein end über reset, Blöcke und prepareToPlay, fail-closed bei vollem Ring,
releaseResources wie reset mit freigegebenen Blockpuffern, prepareToPlay wie
bisher, ohne Auftrag kein Ereignis und kein Host-Dirty. Eigener Commit
(Betreff „NAK-312 Etappe 6b: A3-Behauptungszeile …"), BOM und CRLF erhalten,
der PowerShell-Parser meldet 0 Fehler. Danach der volle Kanon der Etappe 6
(M-77), abgekoppelt; sein Ergebnis steht im nächsten Abschnitt.

---

## Kanon-Lauf - NAK-312 Etappe 6

**Lauf:** 2026-09-21 23:30 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 70/70 Kanon-Laeufe bestanden | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-312-86c31f4-dirty.md](roh/NAK-312-86c31f4-dirty.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-21 23:30:21 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 86c31f46 NAK-312 Etappe 6b: A3-Behauptungszeile nennt die Fälle der Etappe 6a |
| Commit (voll) | 86c31f4673b84398c2a8c1ca4063dd5e2d757de8 |
| Arbeitsbaum | 2 unbestaetigte Datei(en) - dieser Lauf beweist NICHT allein den Commit |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.6 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,07 s | [A1](roh/NAK-312-86c31f4-dirty.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,98 s | [A2](roh/NAK-312-86c31f4-dirty.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). Seit NAK-283 (F12) erzeugt ein endlicher Eingang keinen nicht endlichen Wet-Ausgang: im Puls auf 1 kHz mit einem Ton der Amplitude 0,8 x FLT_MAX ist jedes Ausgangssample endlich, und nach dem Einblenden steigt der Wet-Zaehler um genau die verriegelten Samples. Seit NAK-312 Etappe 6a (R-312-8): ein Rollenwechsel zu Main nimmt einen in Legacy eingereichten Auftrag zurueck, bevor die Klassifikation wirkt - 40 Bloecke danach bitgleich ohne Ereignis im Ring, ebenso nach dem Rueckwechsel; in Legacy reicht SOLO nichts ein, zeigt keinen Aus-Knopf, und der Status nennt die Lage; ein Wechsel weg von Main schliesst das Intervall mit genau einem begin und einem end ueber einen weichen Ausfade (Nachbarsprung innerhalb der E-31-Schranke) und blendet beim zweiten Wechsel nicht wieder ein, auch mitten im Ausfade; Freilaufmeldung, Sampleratenwechsel und 10-Minuten-Totmann beenden die Markierung auch in der Main-Flaeche. Seit Etappe 6a (R-312-5): reset() bei angehaltenem Transport beendet Klang und Intervall (40 Bloecke bitgleich, genau ein end ohne Projektzeit, 0 Allokationen), laesst den Oversize-Riegel bis zum naechsten prepareToPlay stehen und den Auftrag bestehen (laeuft der Transport weiter, ein neues begin); ueber reset, Bloecke ohne und mit Erlaubnis und prepareToPlay hat jedes begin genau ein end; bei vollem Ring bleibt reset fail-closed (Ueberlaufbit, keine Sequenz); releaseResources schliesst wie reset und gibt die Blockpuffer frei, prepareToPlay wie bisher; ohne Auftrag entsteht kein Ereignis und kein Host-Dirty. | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 8,30 s | [A3](roh/NAK-312-86c31f4-dirty.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. Fehlt einem Test eine Voraussetzung (das Junction-Recht fuer store_weist_reparse_punkt_im_pfad_ab), meldet er NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.132,32 s | [A4](roh/NAK-312-86c31f4-dirty.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. Fehlt das Junction-Recht fuer volumenentscheidung_haengt_am_sqlite_handle_nicht_am_namen, meldet der Test NOT RUN mit Grund: das Bein steht dann als [NOT RUN] in der Uebersicht und der Lauf ist UNVOLLSTAENDIG, nie gruen (NAK-309 R-309-4). | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,76 s | [A4-SI](roh/NAK-312-86c31f4-dirty.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. Seit NAK-309 (M-18 bis M-28) die Regeln des Probewerkzeugs: ohne Argument waehlt es den v2-Probenamen, ein leeres Argument bleibt leer; der Produktionsname (auch gross oder gemischt geschrieben), Namen mit \ oder / hinter \\.\pipe\, fremde Namen und ungueltige v2-Suffixe werden abgelehnt, v2- und v3-Probenamen zugelassen; ein PipeClient ohne Namen startet keinen Verbindungsversuch; ein Konfliktende gilt nur mit einem neuen Broker-ACK ohne Konflikt auf derselben stehenden Verbindung, nicht nach Abbau oder Neuaufbau. Quelltextwachen: PipeProbeMain entscheidet den Namen vor Servererwartung und erstem Client (abgelehnt Exit 64) und wartet auf dieses Konfliktende-Praedikat; das Produkt nennt seine v2-Pipe ausdruecklich. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,76 s | [A4b](roh/NAK-312-86c31f4-dirty.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,87 s | [A5](roh/NAK-312-86c31f4-dirty.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-312-86c31f4-dirty.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,14 s | [A7](roh/NAK-312-86c31f4-dirty.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,37 s | [A8](roh/NAK-312-86c31f4-dirty.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID, keine ist verbrannt oder wiederverwendet, und jedes Offsetfeld steht im Rust-Strukturriegel. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,57 s | [A9](roh/NAK-312-86c31f4-dirty.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen, integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) und seit SONDE-015 band_dynamic_gain_db ID 22 (acht Werte, leerer Vektor, Altsender ohne Feld, vier falsche Laengen, NaN und Inf) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,99 s | [A10](roh/NAK-312-86c31f4-dirty.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,27 s | [A11](roh/NAK-312-86c31f4-dirty.md#a11) |
| A12 | Parameterbestand Layout v2 (120 Kennungen, 112 Host-Parameter als Praefix) haelt den Vertrag, und die 109 v1-Kennungen sind aus der eingefrorenen v1-Datei woertlich abgeleitet; Zonen-, Preset- und Ablehnungsregeln stimmen mit dem Vertrag ueberein; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State- und Presetkorpus samt MANIFEST bytegleich zur Neuerzeugung, keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,41 s | [A12](roh/NAK-312-86c31f4-dirty.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,39 s | [A13](roh/NAK-312-86c31f4-dirty.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 8,78 s | [A14](roh/NAK-312-86c31f4-dirty.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,31 s | [A17](roh/NAK-312-86c31f4-dirty.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,72 s | [A18](roh/NAK-312-86c31f4-dirty.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,15 s | [A19](roh/NAK-312-86c31f4-dirty.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,46 s | [A20](roh/NAK-312-86c31f4-dirty.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,34 s | [A21](roh/NAK-312-86c31f4-dirty.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,06 s | [A22](roh/NAK-312-86c31f4-dirty.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. Eine fehlende Messung ist kein PASS (NAK-283 F13): jeder Speicherpunkt traegt das Gueltigkeitsmerkmal seiner Windows-Abfrage, und fehlt ausserhalb der Neustartfenster eine gueltige Messung, endet S07 mit dem eigenen Status MESSUNG FEHLT (Exit 3) statt im Budget. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,97 s | [A24](roh/NAK-312-86c31f4-dirty.md#a24) |
| A34 | Der Selbsttest des Soak-Orakels (NAK-283 F13, Muster A27). Er laeuft OHNE Repo-Fixture und ohne Lauf: vier Faelle bauen ihre Berichte im Speicher und laufen durch dieselben Funktionen wie A24 - Messstelle, Speicherpunkt, Urteil. Eine fehlende RSS-Messung endet mit MESSUNG FEHLT und Exit 3, nie im Budget; ein nicht abfragbarer Prozess und ein Prozess mit 0 Bytes Working Set sind im Bericht verschieden; ein Fehlerpunkt direkt vor oder nach dem Neustartfenster faellt, obwohl das Fenster die Kurve filtert; ein Altbericht ohne Gueltigkeitsmerkmal bleibt ueber --bericht auswertbar, derselbe Bericht als Livelauf ist rot. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --selbsttest` | [OK] Exit 0 | 0,16 s | [A34](roh/NAK-312-86c31f4-dirty.md#a34) |
| A35 | Der Selbsttest des Laufzeit-Arms (Plan S25e, NAK-286, NAK-309) laeuft ohne FL, ohne Installation und ohne MCP-Repo gegen Attrappen: der Runner haelt Exitcodes, Urteilswoerter und Kopfzeile der ersten Fassung, beendet nie ein fremdes FL (eigen ist nur ein selbst gestarteter Prozess, dessen PID, Startzeit und Befehlszeile in der Besitzliste stehen, auch bei gleichem Fenstertitel), faehrt nur den gepinnten MCP-Stand, startet keinen FL-Lauf gegen einen nicht aktuellen Bau und meldet ein veraendertes Diagnoseprojekt; eine Szenario-Voraussetzung (Exit 5) laesst die Folge weiterlaufen, nur ABWEICHUNG loest den Rueckweg aus. Eine MCP-Antwort misst nur mit der Anforderungskennung ihres Versuchs, jede andere heisst UNGEMESSEN; dass der Importweg fl_trigger nie laedt, ist ueber ein Attrappenpaket gemessen. Antworten des Briefkastens werden an Name und Groesse ausgewaehlt, bevor eine Datei geoeffnet wird; der Nulltest trennt Format, Versatz beider Vorzeichen, Kettenfaktor, Abweichung und Nullvorzeichen und urteilt BITIDENTISCH nur bei gleichen Bytes des Bereichs (NAK-311: +0 gegen -0 heisst NULLVORZEICHEN, Szenario-Exit 5); Baender entstehen nur mit Rechnung aus F-28. Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\fl\selbsttest.py` | [OK] Exit 0 | 33,89 s | [A35](roh/NAK-312-86c31f4-dirty.md#a35) |
| A36 | Die Frischepruefung des Runners leitet je Pruefbinary den Frischebaum aus den MSBuild-Tracking-Logs, den Kernbibliotheken und dem Konfigurationsstand ab: an einem Attrappen-Baubaum verweigert eine geaenderte DSP-Quelle ohne Bau die Beglaubigung, ein frischer Bau wird angenommen, Gleichstand ist frisch, ein nicht ableitbarer Baum ist nie gruen, und jedes gebaute gebundene Kernziel ist gedeckt. Dasselbe Urteil trifft jedes Ziel aus $gemesseneZiele, das der Runner baut (NAK-309 R-309-9; die Uebergabe misst eine Quelltextwache): eine juengere Eingabe nur des gemessenen Ziels verweigert die Beglaubigung, auch wenn jedes Pruefbinary frisch ist, und ein nicht ableitbares oder im CMake-Export fehlendes Ziel ist nie gruen. Verglichen werden Zeitstempel, keine Inhalte. Die NOT-RUN-Meldung der cargo-Beine (R-309-4): ein leerer Meldeordner heisst gelaufen, eine Marke NOT RUN mit Test und Grund, ein unlesbarer Ordner nie leer; im Urteilsblock des Runners geht ROT der fehlenden Voraussetzung vor, und NOT RUN zaehlt nie als gruen (Quelltextwache). Jede Erwartung laeuft mit ihrem Gegenteil. | `py -3.13 tools\eq-copilot\pruefe_beweisrunner.py --selbsttest` | [OK] Exit 0 | 6,01 s | [A36](roh/NAK-312-86c31f4-dirty.md#a36) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,16 s | [A28](roh/NAK-312-86c31f4-dirty.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,15 s | [A29](roh/NAK-312-86c31f4-dirty.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A30](roh/NAK-312-86c31f4-dirty.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 96,90 s | [A31](roh/NAK-312-86c31f4-dirty.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,22 s | [A25](roh/NAK-312-86c31f4-dirty.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,15 s | [A26](roh/NAK-312-86c31f4-dirty.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-312-86c31f4-dirty.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,77 s | [A23](roh/NAK-312-86c31f4-dirty.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc) mit dem aktiven DSP-Kern: im Default (eq_enabled aus) bitgleich ueber 1000 Bloecke von 1 bis 2048 Samples bei 44,1 / 48 / 96 / 192 kHz ohne belegte Bank; eq_enabled an mit bypass aus und sonst neutralen Defaults bitgleich; eq_enabled an mit bypass an bitgleich trotz hoerbarem Band dahinter; nach eq_enabled aus nach dem Fade wieder bitgleich; Mix 0 mit Output-Trim 0 dB bitgleich; der Passthrough sanitisiert kein NaN/Inf; 0 Samples Latenz und kein Tail im Passthrough und im Hard-Bypass; speichern-laden-speichern bytegleich im Layout v2 mit Kind Dsp. Seit NAK-283 (F04, F05) bleibt der Nulltest bitgleich, wenn reset() vor jedem dritten Block laeuft - ausgeschaltet bei 44,1 / 48 / 96 / 192 kHz, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen, und im Hard-Bypass auch mit NaN, +Inf und -Inf zwischen zwei reset(); Mono und Stereo mit gleichem Ein- und Ausgang werden angenommen (im Monobus bleibt der Passthrough bitgleich, und das Band rechnet endlich), Quadrophonie, 5.1, vier diskrete Kanaele, Mono->Stereo, Stereo->Mono und jeder deaktivierte Hauptbus bekommen ein Nein, setBusesLayout auf Quadrophonie scheitert, und der Prozessor bleibt bei zwei Kanaelen. Das Bundle meldet 112 Host-Parameter, und sein Bundlevertrag laesst nur active_probe zu. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der Kern engagiert-neutral (eq_enabled an, sonst Default) keinen Sample: ab Sample 512 kommen das Bitmuster aus Subnormals, +-0 und kleinsten Normalen, die Wachmarke 0x7F800001 (signalisierender NaN), ein ruhiger NaN und +-Inf bytegleich heraus, bei 44,1 / 48 / 96 kHz und im Monobus; der Riegel zaehlt die nicht endlichen Werte (6, im Monobus 12), der Tap post_committed traegt dort 0,0 und an jedem Rauschsample ab Sample 0 den Eingang als double. Bei Mix 0 mit Output-Trim 0 dB kommt dasselbe Material in den Bloecken ab Sample 512 bytegleich heraus, und der Tap traegt ab Sample 512 den Eingang als double. Ausgeschaltet und im Hard-Bypass bleibt das Material bytegleich ohne Zaehler; mit hoerbarem Band schreibt der Kern und gibt die Wachmarke nie aus. Die Messung 311/M-01 gibt die Bits unter FTZ/DAZ aus und prueft nur, dass alle 18 Kombinationen liefen. Seit NAK-311 Etappe 4 (R-311-3) rechnet der Ausgleich nur mit dem ausgegebenen Kanal: im Monobus melden Kern UND dspBericht fuer ein Band im Modus right exakt +0,0 dB, und der Ausgang mit Auto-Gain ist bytegleich zum Lauf ohne; nach dem Layoutwechsel auf Stereo tragen beide den Zweikanalwert, bitgleich zu einem Stereokern mit demselben Zustand, zurueck in Mono wieder +0,0 - waehrend channel_mode `right` im bestaetigten Zustand und in den Statebytes stehen bleibt, ohne Revision und ohne Host-Dirty. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, Abnahme U47) ist der EQ unter 44,1 kHz nicht unterstuetzt: bei 32 und 22,05 kHz bereitet der Kern nichts vor, und ueber 1000 Bloecke von 1 bis 2048 Samples kommen Rauschen, Bitmuster, Wachmarke, NaN und +-Inf im Stereo- wie im Monobus bytegleich heraus, ohne dass die Zaehler fuer nicht endliche Eingaenge, geheilte Filterzustaende und verworfene Analyseframes steigen; der Kern meldet die abgelehnte Rate, exakt +0,0 dB Ausgleich und die Kanalzahl 2, und die Blockgroessen 1, 255, 256, 257, 2048 und 4096 kommen ebenso bytegleich heraus, ohne gefuellten Tap, mit 0 Samples Latenz und Tail 0,0 s. Genau 44 100 Hz und der naechste double darueber bleiben unterstuetzt - das Band ist hoerbar, und der Fingerabdruck des Ausgangs ist bei 44,1 / 48 / 96 / 192 kHz gleich dem Golden, das dieselbe Messung am unveraenderten Kern des Basisstands der Etappe (5ee8318c) erzeugt hat -, waehrend 44 100 minus 1 ULP, 44 099, 32 000, 22 050, 8 000, 0, negativ, NaN und +-Inf abgelehnt werden. Rate 0, nicht endlich, 1e300 und maxBlock 0 bleiben das verriegelte Fenster: die letzte Vorbereitung steht, der dspBericht meldet sie weiter, und ABGELEHNT ist dort nichts. v3Samplerate, v3BlockSize und v3Channels tragen im abgelehnten Fenster weiter die gemessenen Werte des Hosts. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,69 s | [A16](roh/NAK-312-86c31f4-dirty.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,15 s | [B1](roh/NAK-312-86c31f4-dirty.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. Seit SONDE-015 zusaetzlich das Kind Dsp: 120 Werte bit-exakt durch den Roundtrip, flache Arrays statt Kindknoten, voller Undo-Ring aus 32 Schnappschuessen bytegleich und unter 16 MiB, der 33. Eintrag read-only; die Layoutmigration v1 nach v2 setzt occupied bitgenau aus enabled und den Werten; das Preset traegt zwei getrennte Versionen, weist jedes der sechs verbotenen Identitaetsfelder ab, ignoriert unbekannte Top-Level-Felder und laesst eq_enabled beim Laden unberuehrt. Seit NAK-312 Etappe 5 (312/M-49): die eingefrorenen Statebytes des Parametergoldens vom Basis-SHA 12300f1e passen zu ihrer Zeile im Golden (Laenge und SHA-256), laden normal und kommen aus dem Schreiber bytegleich zurueck. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,30 s | [B2](roh/NAK-312-86c31f4-dirty.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,08 s | [B3](roh/NAK-312-86c31f4-dirty.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,13 s | [B3b](roh/NAK-312-86c31f4-dirty.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. Seit SONDE-015 zusaetzlich der Fassungsschritt P1 5: der aus der committeten Fassung zurueckgebaute Leser der Fassung 4 lehnt state_report.dsp ab, ein Bericht ohne dsp bleibt in beiden Fassungen gueltig, und der Empfaenger rechnet SHA-256(dsp.jcs) gegen state_hash nach - ein schemagueltiger Bericht mit abweichendem Hash wird GANZ abgewiesen. Seit NAK-311 Etappe 4 Teil b (R-311-5) klemmt der Bericht `auto_gain_db` auf die Vertragsgrenze, statt sie zu reissen: acht Low-Shelves 1 kHz +12 dB Q 8 sind abgeleitet -199,77 dB, gemeldet werden exakt -120, und der aus dem echten Transaktionskern gebaute state_report ist gueltig; acht Shelves Q 0,707 bleiben mit -92,29 dB bitgleich zum abgeleiteten Wert. Die Klemmgrenze im C++ ist dieselbe Zahl wie minimum und maximum des GELADENEN Schemas; +/-120 kommen unveraendert zurueck, der naechste double darueber und darunter sowie 120,5 werden geklemmt, NaN wird +0,0 (nicht -0,0) und +/-Inf werden +/-120. Geklemmt wird NUR der Bericht: DspKern::autoGainDb() und der lineare Faktor des gefahrenen Programms bleiben beim ungeklemmten Wert, und klemmungen bleibt leer. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) meldet der Bericht bei einer Rate unter 44,1 kHz wie ohne Samplerate: bei 32 kHz haelt der Transaktionskern fs = 0, auto_gain_db ist exakt +0,0 und klemmungen leer, waehrend derselbe Zustand bei 48 kHz -92,29 dB meldet; Kern und Bericht tragen die abgelehnte Rate, und der daraus gebaute state_report bleibt gueltig gegen $defs/dsp_bericht. Der Grund bleibt C++-lokal: dasselbe Dokument MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen, ohne das Feld ist es gueltig - der v3-Vertrag bleibt unberuehrt. Seit NAK-311 Etappe 5 Satz B (R-311-14, Karte U54) melden Bericht und Kern DENSELBEN gedeckelten Wert: acht High-Cuts 20 Hz Q 0,15 sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() lesbar, gefahren und gemeldet werden bitgleich kAutoGainDeckelDb, weil der Deckel vor baueBericht sitzt; die Berichtsklemmung laesst den Wert unveraendert durch, und der Zustand steht in Kern und Bericht. Mit ausgeschaltetem Auto-Gain ist er in beiden falsch, auf der Absenkungsseite ebenso (-199,77 dB abgeleitet, exakt -120 gemeldet, klemmungen leer). Der Zustand geht nicht auf den Draht: derselbe Bericht MIT dem Feld im dsp-Block wird von additionalProperties: false abgewiesen. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,45 s | [B3c](roh/NAK-312-86c31f4-dirty.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,34 s | [B4](roh/NAK-312-86c31f4-dirty.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,16 s | [B9](roh/NAK-312-86c31f4-dirty.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,38 s | [B5](roh/NAK-312-86c31f4-dirty.md#b5) |
| B6 | Aktiver DSP-Kern (plugin/dsp/) als Bibliothek: alle sechs RBJ-Filtertypen liegen ueber einem 1/24-Oktav-Gitter von 20 Hz bis min(20 kHz, 0,45 fs) bei 44,1 / 48 / 96 / 192 kHz innerhalb 0,05 dB typisch und 0,1 dB an den Raendern an der analytischen Antwort - gemessen als Impulsantwort AM TAP post_committed, also durch den Audiopfad, gegen eine im Test eigenstaendig ausgeschriebene Formel. Ausgeschaltet und hard-bypassed wird bei jeder Hoermatrix-Auswahl kein Sample geschrieben (bitgleich); bei Mix 0 mit Output-Trim 0 dB ist der Ausgang wertgleich zum Eingang; nach dem Ausschaltfade ist der Passthrough wieder bitgleich, und er sanitisiert kein NaN. Die acht Slots wirken als Kaskade von Slot 0 nach 7 gegen ein unabhaengig gerechnetes geordnetes Golden, das ein Rueckwaertslauf reisst. Auto-Gain ergibt bei flacher Kurve exakt 0 dB und bei einem +6-dB-Shelf ueber dem Gitter -6 dB, ein identisches Mid/Side-Paar faellt exakt auf den Stereo-Fall zusammen, und die Dynamik bewegt ihn nicht. Der Bankpool faehrt jeden der sechs Ownership-Uebergaenge einzeln, gibt busy_retry statt eine aktive Bank zu verdraengen, wird erst nach dem Audio-ACK frei und haelt seine Invarianten unter zwei echten Threads ueber tausende Bloecke; im Callback zaehlt der thread-lokale Zaehler ueber 4000 Bloecke wechselnder Groesse 0 Allokationen und 0 Sperren, waehrend derselbe Zaehler die Allokationen des Workers NICHT sieht; B6 baut keine Sonde, sein Zaehler misst ab DspKern::verarbeite (NAK-312 R-312-1). Ein Block ueber maxBlock (1024 Samples bei 256) verwirft den Analyse-Tap und gleicht auf beiden Kanaelen sample-exakt dem Lauf ohne Ueberlast in 4 x 256. Seit NAK-283 (F12) die float-Kante des Ausgangs: ein endlicher double ueber FLT_MAX (Output-Trim +6 dB auf 0,75 und 0,25 x FLT_MAX) kommt auf 0 verriegelt und im Eingangszaehler gezaehlt heraus, und kein Ausgangswert ist nicht endlich; auf endlichem Material in +/-1,0 (Bell +9 dB und Output-Trim +6 dB, 204 800 Werte, auch mit Betrag >= 1) ist jeder float bitgleich die Verengung des double-Taps, und der Zaehler bleibt stehen. Seit NAK-311 Etappe 2 (Neutralpruefung) schreibt der engagiert-neutrale Kern (Merkmal neutral, alle fuenf Rampen in Ruhe auf 1,0) keinen Sample: die Wachmarke 0x7F800001, NaN, +-Inf und das Bitmuster kommen bytegleich heraus, der Riegel zaehlt, der Tap post_committed traegt dort 0,0. Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096 (maxBlock 512), und das Schreiben setzt am ersten Sample eines neuen Uebergangs wieder ein; bei Delta und Candidate, bei Mix 1e-9 und mit einem 0-dB-Bell schreibt der Kern. Wo er nicht mehr schreibt, misst der Tap post_committed die Rechnung: am Ende einer Width-Rampe auf 1,0 und bei Mix 0 exakt den Eingang als double. Der Allokationslauf mit neutralem Schritt bleibt bei 0 Allokationen. Seit NAK-311 Etappe 3 (W01) schaltet ein Pfad aus der Ruhe wie ein frischer Kern ein: nach Ausschalten, Ausblenden und einer Aenderung in der Ruhe (Output- oder Input-Trim +24 dB, Mix 0, Width 2,0, Auto-Gain; ebenso nach beendeAudiohistorie im Ausblenden und fuer den Candidate nach Ende und Hoerhalt am Tap post_candidate) ist der Ausgang ab dem Umschaltblock bytegleich zu einem frischen Kern mit demselben Programm, bei Blockgroessen 1 bis 4096 und 44,1 / 48 / 96 kHz; mit Ziel 0 dB bleibt Ausgang/Eingang an jedem Sample 1,0. Seit NAK-311 Etappe 3 (W03) traegt jeder aktive Slot eine Lebenszykluskennung: ein Slot, dessen Belegung und Topologie ueber jede Publikation seit dem gefahrenen Programm gleich blieben, behaelt beim Wechsel an einem ANDEREN Slot seinen Filter-, Detektor- und Huellkurvenzustand - sein Perioden-RMS (volle Periode des Prueftons, beide Kanaele, 100 Perioden ab dem Umschaltblock) liegt hoechstens 0,5 dB neben einem Referenzkern mit gleicher Vorgeschichte ohne Wechsel, bei 44,1 / 48 / 96 kHz und Blockgroessen 1 bis 4096 ueber maxBlock, am Candidate-Pfad ebenso; ein geaenderter Slot, ein zwischendurch entfernter und neu belegter und nach einem globalen Wechsel (Samplerate, Mono-Bass-Stufe) jeder Slot startet kalt und meldet 0,0, waehrend acht Slots zugleich ihre Auslenkungen bitgleich behalten oder alle acht kalt starten. Aendert der uebertragene Slot in derselben Publikation seinen Wert, bleibt der Quadraturbetrag am Umschaltsample innerhalb der Fadeschrittweite, ueber 100 Perioden hoechstens 0,5 dB ueber der groesseren eingeschwungenen Spitze und nach Fade-Ende plus Einschwingzeit hoechstens 0,5 dB neben dem Rampenlauf ohne Fremdslotwechsel; der Mono-Bass-Zustand wandert unter gleicher Pfadkennung mit, und der Allokationslauf bleibt mit einem Committed-Fremdslotwechsel im Zyklus bei 0 Allokationen. Seit NAK-311 Etappe 4 (R-311-3) rechnet die Auto-Gain-Ableitung im Monobus (Kanalzahl 1) die geordnete Kaskade in Slotreihenfolge und wertet nur den ausgegebenen Kanal aus: ein Band im Modus right oder side ergibt exakt +0,0 dB (kein -0,0), und der Ausgang ist ueber 48 000 Samples bytegleich zum Lauf ohne Auto-Gain; left und mid kompensieren voll wie derselbe Kern im Modus stereo; zwei vertauschte Slotreihenfolgen ergeben verschiedene Werte, und zwei und acht Baender treffen die im Test eigenstaendig ausgeschriebene Formel auf 1e-9 dB. Kanalzahl 0, 2 und 3 rechnen weiter die Zweikanalformel, deren vierzehn Werte bitgleich (memcmp) zum Basisstand der Etappe bleiben, und auch im Monokern laeuft die Ableitung nie im Audiopfad. Seit NAK-311 Etappe 4 Teil b (T3-15-11) ist die Steuerrate gemessen statt begruendet: gegen einen Referenzkern mit Range 0 (derselbe SVF-Weg) bleibt der Tap post_committed nach dem Einsatz eines Quadraturtons bis einschliesslich des BEZUGSPUNKTS bitgleich und weicht genau 1 Sample danach ab; Bezugspunkt ist der erste Steuerschritt mit einer Auslenkung ungleich 0, im Test aus schrittRest und der Auslenkung gelesen statt aus dem Toneinsatz, und er liegt im Raster auf der Phase d = (8 - p) mod 8 zum Toneinsatz; genau 8 Samples nach ihm liegt wieder ein Steuerschritt, an dem der dort entworfene Satz mit Gewicht 1 wirkt - dort traegt svfVon genau den am Bezugspunkt entworfenen Satz -, in jeder gefahrenen Steuerphase und bei jeder Rate. Die Spanne 1 bis 8 Samples bis zur ersten und 8 bis 15 Samples bis zur vollen Wirkung des ersten Entwurfs ist dagegen die RASTERZAEHLUNG 1 + d und 8 + d ueber die vier gefahrenen Steuerphasen, gezaehlt ab dem Sample Bezugspunkt - d und nicht als Abstand zum Bezugspunkt; in Zeit sind die 8 bis 15 Samples der vollen Wirkung 0,167 bis 0,3125 ms bei 48 kHz, 0,181 bis 0,340 ms bei 44,1 kHz und 0,083 bis 0,156 ms bei 96 kHz, also bei 48 und 44,1 kHz GROEBER als die kuerzeste Attack von 0,1 ms. Seit dem festgelegten Pegelbegriff (Satz D, R-311-15) liegt der Bezugspunkt bei manchen Steuerphasen einen ganzen Rasterschritt nach dem Toneinsatz; dieser Rasterschritt ist das Einschwingen des Pegelbegriffs und steckt nicht in den Zahlen 1 bis 8 und 8 bis 15. Das Verhalten aendert sich dabei nicht, kDynamikSchritt bleibt 8. Seit NAK-311 Etappe 5 Satz B (T3-15-09 Teil b, R-311-14, Karte U54) ist der ANGEWANDTE Auto-Gain nach oben einseitig gedeckelt: acht High-Cuts 20 Hz Q 0,15 bei 48 kHz sind abgeleitet +150,46 dB und ueber DspKern::autoGainRohDb() weiter lesbar, angewandt wird bitgenau kAutoGainDeckelDb = 24,0 dB, und am Tap post_committed liegt der Ausgang damit um den Faktor 15,849 ueber demselben Lauf ohne Auto-Gain statt um 3,3e7 - der Zustand ist dabei gesetzt. Die Deckelfunktion laesst den naechsten double unter der Grenze und die Grenze selbst bitgleich durch und setzt erst den naechsten darueber auf die Grenze; -0,0, +0,0 und das kleinste Subnormal kommen bitgleich zurueck. Die Absenkungsseite bleibt ungedeckelt (-199,77 und -92,29 dB bitgleich, Zustand falsch), Mono- und Zweikanalzweig deckeln an ihrer eigenen Rueckgabezeile auf denselben Wert, bei ausgeschaltetem Auto-Gain wird nichts angewandt und nichts gemeldet, und nach einem Programm unter dem Deckel faellt der Zustand zurueck, waehrend der angewandte Faktor ueber kRampeSamples auf das neue Ziel laeuft statt zu springen. Alle vierzehn Hexgolden von 311/M-61 liegen zwischen -6,00 und +0,23 dB, also auf der ungedeckelten Seite, und bleiben bitgleich. Seit NAK-311 Etappe 5 Satz C (T3-15-07, R-311-13, Karte U44) kennt die Kennungsvergabe auch WERTE: reisst ein Slot bei sonst gleicher Topologie eines der drei Kriterien - Frequenzverhaeltnis 2,0, Gueteverhaeltnis 4,0, Gaindifferenz 20,0 dB, richtungsfrei und strikt groesser -, bekommt NUR dieser Slot eine neue Lebenszykluskennung, der Blockrand blendet ueber statt zu rampen, und nur dieser Slot startet kalt. Gemessen am Mass von R-311-13 (groesster Ausgangsbetrag im Fenster von 0,5 s ab dem Umschaltsample gegen die groessere der beiden eingeschwungenen Spitzen, hoechstens 1 dB): der Sprung freq_hz 5000 -> 50 Hz an einem Low-Shelf +6 dB Q 0,707 faellt von +17,41 auf -0,22 dB, derselbe Sprung am Low-Cut 2000 -> 20 Hz von +22,20 auf +0,13 dB, die Gegenrichtung 50 -> 5000 Hz von +1,91 auf 0,00 dB, und ein dynamisches Band mit Range 0 haelt das Mass ebenso. Dieselbe Strecke in zwanzig Stufen zu je 1,2589 bleibt dagegen auf dem Rampenweg, behaelt ihre Kennung und ist ueber alle 40 960 Samples bitgleich zum Basisstand. Strikt groesser entscheidet an allen sechs Kanten je Kriterium (eine ULP darunter, genau auf der Grenze, eine ULP darueber, in beiden Richtungen), nicht endliche und nicht positive Werte erzwingen IMMER eine neue Kennung, acht gleichzeitig springende Slots ergeben genau EINEN Crossfade, ein zweiter Sprung im laufenden Fade wird erst am Blockrand nach dessen Ende genommen, und der Fade dauert bei Blockgroesse 1, 255, 256, 257 und 4096 genau kFadeSamples SAMPLES. Im Callback bleibt der Zaehler dabei bei 0 Allokationen und 0 Sperren, auch wenn eine Publikation ueber dem Kriterium im selben Zyklus liegt. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15, Karte U45) vergleicht die Dynamikschwelle einen FESTGELEGTEN Pegelbegriff mit dem Threshold: zwischen Detektor und Huellkurve liegt ein symmetrisches Ein-Pol-Leistungsmittel mit dem festen Fenster kPegelFensterMs = 10 ms (durchschnitt); Fenster 0 (spitze) schliesst die Stufe kurz. Aktiv ist durchschnitt - baueProgramm setzt ihn unbedingt ohne Eingabe aus dem Vertrag, im Produktcode steht genau eine Setzstelle, und Pegelbegriff::spitze kommt dort nur in der Abbildung auf die Fensterlaenge vor. Gemessen bei 48 kHz, Bell Q 0,707, Range -12 dB, Threshold im Knie: drei Materialien gleicher Leistung (Quadraturton, L = R-Sinus, Rauschen) liegen um hoechstens 0,02 dB bei 1 kHz, 0,06 dB bei 341 Hz, 0,21 dB bei 100 Hz und 0,90 dB bei 20 Hz auseinander, das Rauschen um 0,32 dB - vorher 2,12 bis 4,01 dB. Neun Einstellungen aus Attack 0,1/10/500 ms mal Hold 0/30/500 ms spannen je Bandmitte 0,05 / 0,15 / 0,50 / 2,20 dB statt 5,78 dB; die Restwelligkeit eines FESTEN Fensters waechst mit fallender Bandmitte und steht als Zahl in der Schranke. Mit Fenster 0 ist der dynamische Weg ueber 76 096 Samples bitgleich zum Basisstand des Aenderungssatzes, und der Kurzschluss gibt den Eingang auch bei nicht endlichem oder denormalem Zustand bitgleich zurueck. Der Pegelzustand liegt im BandZustand: er wandert am Blockrand mit dem uebrigen Bandzustand, wird mit dem abgeschalteten Detektor gliedweise genullt, am Blockrand geheilt und denormalgeriegelt, ist bei Stille bitgenau +0,0 und ergibt bei 44,1 / 48 / 96 / 192 kHz dieselbe Zeit. Der Pegelbegriff ist topologisch: rampenKompatibel und die Kennungsvergabe tragen ihn, zwischen zwei Begriffen wird nie interpoliert. Die Sprungantwort misst gegen eine im Test ausgeschriebene Referenz der GANZEN Kette (Bandpass, Pegelbegriff, Huellkurve, Steuerraster): bei Attack 20 ms und Fenster 10 ms liegt der 63-%-Punkt bei 32,04 bis 32,17 ms statt bei 20 ms, die Toleranz von 1 ms je Stufe bleibt. Im Callback bleiben die Zaehler mit acht dynamischen Baendern bei 0 Allokationen und 0 Sperren; im Quelltext von verarbeiteBand steht der Aufruf der Pegelstufe genau einmal, und huellkurvePol, huellkurveEntwurf, std::exp, std::pow, std::log10, new und malloc stehen dort buchstaeblich nicht - der Pol entsteht im Worker (baueProgramm). Die Wortsuche liest nur den Text dieser einen Funktion und folgt keinem Aufruf; ueber den Steuerratenschritt, der alle kDynamikSchritt Samples Kennlinie und SVF-Satz neu rechnet (leistungInDb mit log10, svfEntwurf mit pow), sagt sie nichts. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [OK] Exit 0 | 6,14 s | [B6](roh/NAK-312-86c31f4-dirty.md#b6) |
| B7 | Lokaler Transaktionskern der aktiven Sonde mit dem echten DSP-Kern: die Falltabelle T1-T17 aus Manifest SONDE-015 §5.11.4 laeuft tabellengetrieben mit den Invarianten I1, I2 und I4 als Wachen nach jeder Eingabe; ein Fehler in jeder Stufe S1-S7 laesst bestaetigten Zustand, Hash, Undo-Ring und Register unveraendert, und weder der Nachschlag S0 noch der Commit-Punkt allozieren; Apply, Revert, Neutralisieren, Remove, Undo, Redo und Preset-Laden erzeugen je genau eine Revision, 10.000 doppelte, vertauschte und veraltete Eingaben einer Transaktion hoechstens eine. Belegung, Remove und Undo als ein Objekt, Schutz-Zonen mit Verletzungsmeldung, Undo-Ring der Tiefe 32 und Preset halten ihre Matrixzeilen. Am echten SondeProcessor: 112 Host-Parameter in Vertragsreihenfolge ohne occupied; Hostautomation ohne Revision mit zwei Epochenwechseln je Geste, auf einem freien Slot bitgleich im Klang; ein abgeschlossener Gestus ist eine Revision; Host-Dirty nur bei einem Commit; das Kind Dsp reist durch Speichern und Laden, und ein Reload rekonstruiert Hash und Ausgang. Seit NAK-283 (F05, F09, F12) am echten SondeProcessor: reset() beendet die Audiohistorie - nach einem Impuls in den Resonator (1 kHz, +12 dB, Q 10) ist der erste Block Stille auf beiden Kanaelen exakt 0, ohne reset() klingt er nach - und laesst getStateInformation bytegleich, die Revision, das bestaetigte eq_enabled und die engagierte Bank im Kern stehen (1 kHz danach mehr als 6 dB lauter); releaseResources->prepareToPlay, reset() und prepareToPlay allein enden in derselben Audiohistorie (Stille exakt 0, untereinander bitgleich). Ein Block ueber maxBlock bei rechnendem Kern hinterlaesst in der Analysequeue eine Luecke (der naechste Block beginnt bei 192 mit kFlagLueckeDavor, Segment +1), und Kern und Queue beschreiben dieselbe verworfene Zeitspanne (ein verworfener Tap, ein Block ohne Audio mit 128 Frames, oversize und Ueberlauf +0); ruht der Committed-Pfad im Hard-Bypass, wird derselbe Block ohne Luecke angenommen. Endliche double ueber FLT_MAX am Tap kommen in der Analysekopie als endliche floats an, und der Kernzaehler steigt um genau diese 1024 Werte. Seit NAK-311 Etappe 3 (W01): reset() im laufenden Ausblenden (Output-Trim +24 dB, ein Block nach eq aus), danach Output-Trim 0 dB in der Ruhe und eq an - Ausgang/Eingang bleibt an jedem Sample des Wiedereinschaltens hoechstens 1 + 1e-6. Seit W03: Remove und Neubelegung desselben Slots mit derselben Konfiguration ohne Audioblock dazwischen - beide committet, die zweite Publikation verdraengt die erste - starten kalt; der Uebergang ist ein Crossfade, die gemeldete Auslenkung ist ab dem ersten Block exakt 0,0 und der Ausgang bei Stille ab kFadeSamples exakt 0,0. Seit NAK-311 Etappe 4 Teil b (F12, R-311-4) hat die Toleranz von M-84 ihre Zahl samt Referenzbeginn und Geltungsbereich: t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz, der Vergleichskern laeuft ununterbrochen weiter und bekommt ab dort denselben Eingang. Fuer ein dynamisches Bell (Kanalmodus stereo, Sidechain internal) mit Quadraturton auf der Bandmitte im Teilraum Q >= 1, Q*A_min >= 0,5 und f0 bis min(20 kHz, fs/4) weicht der Quadraturbetrag je Sample ab t_E = max(kFadeSamples, 5*tau_a*fs + 10*Q*A_max*fs/(pi*f0) + 16) hoechstens 0,1 dB ab - gemessen hoechstens 0,03 dB ueber drei Prueflinge (Q 2 bei Attack 0,1 / 10 / 500 ms, Q 1,0, f0 = fs/4 mit Q 24) je in Knie und Plateau -, davor im Betrag hoechstens \|g0\| + \|Range\| + 0,1 dB; dasselbe gilt fuer prepareToPlay auf der laufenden Instanz. Der Same-Instance-Ladestart bleibt dagegen in Ausgang UND Tap bitgleich zum ununterbrochenen Kern, und zwei Sekunden eingeschwungenes Audio lassen die Statebytes unveraendert: die Huellkurvenleistung ist Audiohistorie, kein persistenter Wert. Seit NAK-311 Etappe 5 Satz A (F08, R-311-16, R-311-20) nimmt ein Ratenwechsel den Zustand zurueck: ueber 48 -> 32 -> 48 kHz ist der Ausgang im 32-kHz-Fenster bytegleich zum Eingang und der Kern meldet 32 000 als abgelehnte Rate, nach dem dritten prepareToPlay ist der Melder wieder exakt +0,0, das Band hoerbar und der Ausgang bitgleich zu einem Kern, der nie abgelehnt hat - waehrend eq_enabled, Statebytes, Revision und Host-Dirty ueber die ganze Folge unveraendert bleiben. Mit einem High-Cut 20 Hz Q 0,707 und Auto-Gain an sind Kern UND Bericht im Fenster exakt +0,0 dB, klemmungen leer, die Zaehler des alten Fensters genullt und die Kanalzahl auf 2, beide tragen die abgelehnte Rate, jcs, Revision und Undo-Tiefe bleiben die des bestaetigten Zustands, und nach dem Rueckweg tragen beide wieder bitgleich denselben abgeleiteten Wert. Im 32-kHz-Fenster zaehlt der thread-lokale Zaehler ueber 200 Bloecke 0 Allokationen im Callback, die Echtzeitwache (seit NAK-312 ab dem Eintritt in SondeProcessor::processBlock) 0 gemeldete Sperren, getLatencySamples bleibt 0 und getTailLengthSeconds 0,0. Seit NAK-311 Etappe 5 Satz D (T3-15-06, R-311-15) traegt t_E den festgelegten Pegelbegriff: der Huellkurventerm rechnet 5*(tau_a + kPegelFensterMs)*fs statt 5*tau_a*fs, also hoechstens 5*kPegelFensterMs*fs Samples mehr, und die drei Prueflinge weichen ab t_E in Knie und Plateau hoechstens 0,027 dB ab - die Toleranz von 0,1 dB bleibt unveraendert. Zwei Sekunden eingeschwungenes Audio bewegen den Pegelzustand nachweislich auf einen endlichen Wert ueber 0, der Slot faehrt dabei den Begriff durchschnitt, und die Statebytes bleiben bytegleich: der Pegelbegriff ist Laufzeit wie die Kanalzahl und steht in keinem Statefeld. Seit NAK-312 Etappe 2 (R-312-1, E-312-11) misst die Echtzeitwache ab dem Eintritt in SondeProcessor::processBlock, und B7 meldet jede eingeschaltete Allokation zusaetzlich an sie: mit einem Testplayhead, der je Aufruf eine gemeldete Sperre nimmt, zaehlt sie ueber 200 Bloecke genau 200 Sperren vor dspKern->verarbeite und 0 Allokationen; ein Workerzug mit Programmbau zwischen zwei Bloecken bewegt beide Zaehler nicht; legt das Bein den Bereich wie der Wrapper um setNonRealtime und processBlock, zaehlt sie seit Etappe 5 ueber 200 Bloecke je Aufruf setNonRealtime (true) wie je Aufruf setNonRealtime (false) 0 gemeldete Sperren (bis Etappe 4 je Aufruf setNonRealtime (true) genau eine). Sperren, die JUCE vor dem Plugincode nimmt, sieht der Zaehler bauartbedingt nicht. Seit NAK-312 Etappe 3a (W02) quittiert ein Ladestart die Hostwert-Mailbox: ein Hostwert, der vor dem Laden derselben Bytes in dieselbe Instanz eintraf, bleibt wirkungslos - auch ueber prepareToPlay, eine spaetere Wiederoeffnung der Mailbox und den Zaehlerueberlauf hinweg -, der Hash bleibt der der Quelle, und ein Hostwert nach der Quittierung wirkt, auch einer zwischen Quittierung und Abgleich, dessen Regler danach denselben Wert zeigt; reset() bewahrt die Mailbox. Ein read-only geladener Stand bleibt unter Hostautomation (eq_enabled und Output-Trim) ueber 512 Samples bitgleich zum Eingang, seine Regler springen auf neutral, die Originalbytes bleiben, kein Dirty, und ein gueltiger Stand danach laesst die Automation wieder wirken; NaN und +-Inf bleiben wirkungslos. Seit NAK-312 Etappe 3b (T3-01-05 Teil a, E-312-5, E-312-6) uebernimmt der Kern Input-Trim, Output-Trim, Width und Mix am Blockrand: ein Lauf unter der Taktsperre ohne Kontrolltakt ist bitgleich zu einem mit Takt nach jedem Block - fuer alle vier in beiden Staenden (neutral und mit Band) bei Blockgroesse 1, 64, 185, 240, 256 und 512 in Echtzeit und bei 64 offline; fuer Output-Trim im neutralen Stand zusaetzlich bei 4096 (dort wirkt der Wert im Block seines Randes), ueber eine Rampe von 200 Bloecken und ueber den Zaehlerueberlauf; ohne Ereignis setzt der Blockrand kein Ziel, NaN und +-Inf erreichen ihn als bestaetigter Wert, ein Ladestart mitten in der Fahrt macht einen alten Mailboxwert auch am Blockrand wirkungslos, und die Abdeckung ist genau diese vier. Epoche und Ruhegrenze bleiben beim Worker (genau zwei Epochenwechsel je Geste, Ruhegrenze in verarbeiteten Samples), und eine blockgebundene Fahrt erzeugt keine Revision, kein Dirty und bytegleiche Statebytes; ueber 4000 Bloecke wechselnder Groesse mit Automation zaehlt die Echtzeitwache 0 Sperren und 0 Allokationen. Ein Bandwert und eq_enabled bleiben taktgebunden: ohne Takt weicht der Lauf ab, mit Takt ist er bitgleich. Seit der Nacharbeit 1 der Etappe 3 (L-1, R-312-16) nimmt der Blockrand nach einem read-only-Ladestart keinen Hostwert: laedt ein Stand mit eq an und Output-Trim +3 dB das fremde Major und kommen danach Input-Trim +24 dB, Output-Trim +24 dB, Width 2 und Mix 0 als Hostwerte, ist der Ausgang ab dem ersten Sample nach dem Laden bitgleich zu dem ohne diese Hostwerte, und die Blockrandziele steigen um 0 - bei Bloecken zu 256 und, wenn beim Laden ein Uebergang laeuft, bei 185 und 512; ein eigener schreibbarer Stand danach laesst Output-Trim +6 dB ohne Kontrolltakt wieder am Blockrand wirken (bitgleich zu einem Lauf mit Takt nach jedem Block, Ausgang/Eingang am Ende bei 10^(6/20)). Seit NAK-312 Etappe 4 (312/M-38): ueber mindestens 4000 Bloecke wechselnder Groesse, waehrend beide v3-Clients der Sonde ueber einen Testserver auf der Probe-Pipe verbunden sind und ihre Provider nachweislich durch die Besitzschleuse laufen, bleiben RtWache::sperren() und RtWache::allokationen() ab Callback-Eintritt 0. Seit NAK-312 Etappe 5 (T3-01-03, T3-01-04, R-312-3) der Offline-Uebergang: mit hoerbarer Vorschau zaehlt die Wache im Bereich wie der Wrapper ueber 200 Bloecke 0 Sperren und (seit Etappe 6a gemessen) 0 Allokationen; unmittelbar nach dem ersten Aufruf setNonRealtime (true) ist die gewuenschte Hoermatrix Processed, und der Ausgang ist ab Sample 0 bitgleich zum Lauf ohne Vorschau - bei Blockgroesse 1, 64 und 256 und auch, wenn beim Wechsel ein Hoermatrix-Fade in Gegenrichtung oder ein Hoerhalt laeuft; nach einem ausdruecklichen Kontrolltakt ist keine Vorschau mehr aktiv, nach ceil(kFadeSamples/Blockgroesse)+1 Bloecken und einem zweiten Takt die Candidate-Bank frei; der Rueckweg in die Echtzeit blendet weich (Nachbarsprung hoechstens Fadeschritt plus 2^-23) und belebt die Vorschau nicht, und ohne Vorschau aendert das Offlineflag am Ausgang nichts. Seit NAK-312 Etappe 6a (R-312-20, R-312-22), unter der Taktsperre bei stehender Vorschau: ein im Render gesetzter Hoerwunsch Dry, Delta oder Candidate wirkt als Processed - jeder Offlineblock ist bei Blockgroesse 1, 64 und 256 ab Sample 0 bitgleich zum bestaetigten Lauf; nach setNonRealtime (true) und sofort setNonRealtime (false) ohne Block dazwischen schaltet der erste Echtzeitblock beim Wechsel auf Candidate nicht hart (Nachbarsprung hoechstens Fadeschritt plus 2^-23). Seit dem zweiten Aenderungssatz der Etappe 5 (T3-01-02, R-312-4) haengen die 112 Hostparameter ohne APVTS direkt am Prozessor: Zahl, Reihenfolge, IDs, Namen, Klassen, Bereiche, Defaults und Hostsicht (convertTo0to1 an Minimum, Default und Maximum; convertFrom0to1, getText und getValueForText an 0, 0,37 und 1), die Statebytes ueber speichern, laden, speichern und die Epochenfolge einer Automationsfahrt sind gleich dem Golden des Basis-SHA 12300f1e; kein Quelltext unter eq-copilot/plugin/sonde/ nennt die APVTS-Klasse; nach dem Laden traegt jeder Hostparameter den geladenen Wert; am Ende des Destruktorrumpfs erreicht ein Hostwert den Prozessor nicht mehr; Index -1 und 112 bleiben ohne Wirkung, und der eigene Abgleich meldet kein Hostereignis; ueber 4000 Bloecke mit Hostautomation aus einem zweiten Thread zaehlt die Wache ab processBlock 0 Sperren und 0 Allokationen - die Sperre, die JUCE vor dem Plugincode in sendValueChangedMessageToListeners nimmt, sieht sie bauartbedingt nicht. Seit der Nacharbeit 1 der Etappe 5 (R-312-19, Kanonteil von 312/M-88) ruft kein Quelltext unter src, sonde, dsp, state, vertrag, core und hostbridge von eq-copilot/plugin ExitProcess, TerminateProcess, PostQuitMessage, FatalAppExit, exit, _exit, quick_exit, _Exit, Process::terminate oder systemRequestedQuit und keiner nennt WM_CLOSE, WM_QUIT, SC_CLOSE oder JUCEApplicationBase (Textriegel); ob der Host im Betrieb lebt, misst das nicht - das misst der Laufzeit-Arm. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [OK] Exit 0 | 1,97 s | [B7](roh/NAK-312-86c31f4-dirty.md#b7) |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 221,18 s | [B10](roh/NAK-312-86c31f4-dirty.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 0,99 s | [B11](roh/NAK-312-86c31f4-dirty.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,62 s | [B12](roh/NAK-312-86c31f4-dirty.md#b12) |
| B31 | Sonde-Lebenslaufbein (NAK-312 R-312-7): der Testkonstruktor der Sonde uebernimmt einen Probe-Pipenamen unveraendert und einen Namen ausserhalb des Probe-Namensraums fail-closed als leeren Pipenamen; ohne die Starthaken macht im Testbau kein v3-Client einen Verbindungsversuch, mit ihnen meldet der ControlClient binnen 8 s verbunden ueber die Probe-Pipe eines Testservers, und die Telemetrie koppelt nach dessen welcome; die Sonde wird mit beiden laufenden Clients auf dem Heap abgebaut. Jeder Name, mit dem ein Client startet, liegt vor dem Start im Probe-Namensraum. Seit Etappe 4 (R-312-7, R-312-12) die Besitzschleuse der Sonde, gemessen an einem gehaltenen Statusprovider und einer gezaehlten Marke vor dem ersten Besitzerzugriff: nach dem Ende des Eigentuemers erreicht ihn kein Provider (0 in jedem Fall und in 20 Gegenzyklen, nach denen Handles und Threads nicht hoeher stehen als nach dem ersten); gehalten hinter dem Betreten wird er zu Ende gewartet, gehalten davor und nach dem Destruktor freigegeben abgewiesen, vor dem Abbau freigegeben laeuft er durch; stop() loest den gehaltenen ControlClient nach der 2-s-Frist ab, controlV3.stop() gemessen vor dem Schliessen, telemetryV3.stop() nach Zeilenfolge; ein Reconnect laesst dieselbe offene Schleuse stehen. Eine Sonde aus dem Produktkonstruktor traegt im Testbau genau pipeNameV3 (aktuelleLogonSid()), ohne dass ein Client startet. | `eq-copilot\build\plugin\EqCopSondeLebenslaufTest_artefacts\Release\EqCopSondeLebenslaufTest.exe` | [OK] Exit 0 | 71,14 s | [B31](roh/NAK-312-86c31f4-dirty.md#b31) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,08 s | [B13](roh/NAK-312-86c31f4-dirty.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. Seit NAK-312 Etappe 6b (312/M-72) reisen die Namen nach dem Labelentwurf-Fall am echten Editor (Entwurf der Startquelle A beim Klick auf B bestaetigt, danach Tick und Enter) durch Speichern und Laden in eine frische Instanz unveraendert und mit derselben Zuordnung Name zu instance_id; das Laden meldet kein Dirty. Seit Etappe 6b (312/M-76) behaelt ein geladener Stand mit 40 bestaetigten Mitgliedern und einer unbekannten additiven MainProject-Eigenschaft ueber die Folge mit 20 gezeichneten Zeilen und Ersatzziel alle 40 Mitglieder im State, im Modell und nach dem Laden in eine frische Instanz; die Eigenschaft reist unveraendert, kein Host-Dirty. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 8,54 s | [B14](roh/NAK-312-86c31f4-dirty.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. Seit NAK-312 Etappe 4 (R-312-2) faehrt dasselbe Bein das Messpunkt-Popover am echten Editor: baut die Nachrichtenschleife es erst nach Editor und Prozessor ab, fasst sein Destruktor keinen von beiden an (gezaehlte Marke 0, Bindung bis zum Prozessorende unveraendert, 0 Host-Dirty); schliesst es, solange der Editor lebt, wird eine Aenderung mit genau einer Host-Dirty-Meldung uebernommen, ohne Aenderung mit keiner. Seit NAK-312 Etappe 6b (R-312-9) dazu der Labelentwurf der Main-Flaeche am echten Editor auf einem echten Main (Namen im Prozessorzustand): ein Entwurf der Startquelle A wird beim Klick auf die Zeile B auf A bestaetigt, und weder Enter noch Fokusverlust danach schreiben ihn auf B; ein leerer Entwurf loescht Bs Namen nicht; nach Escape zeigt das Feld wieder As Label, und der naechste Auswahlwechsel schreibt nichts; faellt A weg oder kommt A mit neuer Runtime-Nonce zurueck, verfaellt der Entwurf ohne Mutation, auch mit Enter vor dem naechsten Tick; unveraenderter Text schreibt nichts; im Feld werden 121 Codepoints ausserhalb der BMP auf 120 beschraenkt und beim Bestaetigen auf 120 gekappt, 0 und 120 werden angenommen, 121 am Prozessor abgewiesen; Host-Dirty je geaendertem Namen genau einmal. Seit Etappe 6b (R-312-6) dazu Ersatzziel und Aktionssteuerung: mit 16, 20, 21, 32 und 64 Quellen ohne Hauptziel ist das Ersatz-Hauptziel die erste angezeigte Quelle (vier Bilder 20/21/32/64 im Sichtsatz), ab 20 Quellen zeichnet die Flaeche 20 Zeilen; rueckt ein per Handgriff gewaehltes Hauptziel durch Beitritt oder Hostbusnamen hinter Zeile 20, sind Knopf, Labelfeld und dessen Beschriftung unsichtbar, das Aktionsziel ist leer, das Hauptziel im Modell unveraendert ohne Schreibversuch, und der Status sagt es; ein offener Entwurf wird davor auf der Startquelle bestaetigt (genau 1 Host-Dirty); ohne Quelle gibt es keine Aktionssteuerung, bei 1 und 16 Quellen ist jede Zeile gezeichnet und per Klick erreichbar. Seit Etappe 6b (NAK-349, R-312-2) dazu das Kennungskonflikt-Panel: stellt die Nachrichtenschleife einen geposteten Klick (triggerClick, der Weg von Enter) erst nach dem Ende des Editors zu, mit lebendem und mit danach zerstoertem Prozessor, fasst der Rueckruf weder Editor noch Prozessor an (gezaehlte Marke 0, Kennung unveraendert, 0 Host-Dirty); bei lebendem Editor vergibt derselbe Klick eine neue Kennung mit genau einer Host-Dirty-Meldung und Statusmeldung, und die Box schliesst. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 4,50 s | [B15](roh/NAK-312-86c31f4-dirty.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-312-86c31f4-dirty.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und der Fingerprint ist unter Vorzeichenumkehr des Signals bytegleich. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 1,25 s | [B22](roh/NAK-312-86c31f4-dirty.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,68 s | [B23](roh/NAK-312-86c31f4-dirty.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,03 s | [B24](roh/NAK-312-86c31f4-dirty.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-312-86c31f4-dirty.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-312-86c31f4-dirty.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-312-86c31f4-dirty.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 2,04 s | [B19](roh/NAK-312-86c31f4-dirty.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht fuer 15 bis 19 keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt auf 1e-9 genau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. Seit NAK-283 (N01) zusaetzlich die vier transienten Faelle 20 bis 23 aus EBU Tech 3341 (Versatz 0 bis 3): jeder trifft eine im Test gerechnete, vorher an 15 bis 19 auf +/-0,01 dB validierte 64-fach-Referenz innerhalb +/-0,1 dB und liegt zusaetzlich in der Normtoleranz 0,0 +0,2/-0,4 dBTP. Insgesamt 43 Pruefungen. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,36 s | [B17](roh/NAK-312-86c31f4-dirty.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 6,21 s | [B18](roh/NAK-312-86c31f4-dirty.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 1,69 s | [B16](roh/NAK-312-86c31f4-dirty.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,40 s | [B27](roh/NAK-312-86c31f4-dirty.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,89 s | [B29](roh/NAK-312-86c31f4-dirty.md#b29) |
| B30 | Diagnose-Briefkasten im Plugin (Plan S25e (3), R-286-1 und R-286-2): ohne anfrage.json nur eine Existenzpruefung je Takt (hoechstens 1 Hz, Message-Thread) und nichts Messbares - Audio, State, Parameter und der Snapshot des Knopfwegs bleiben bytegleich. Eine gueltige Anfrage beantworten Gen und Probeeq je Kennung genau einmal, solange sie im Ring der letzten 256 steht, ueber den eigenen Schreibweg (exklusive Temp-Datei, Groessenpruefung, Umbenennen ohne Ersetzen, nie Ordneranlage oder Loeschung) mit einem Umschlag nach nakama.diagnose.antwort.v1; fremde oder zu grosse Anfragen und solche hinter einer Junction bleiben ohne Antwort, eine beim Lesen gesperrte wird im naechsten Takt beantwortet. | `eq-copilot\build\plugin\EqCopBriefkastenTest_artefacts\Release\EqCopBriefkastenTest.exe` | [OK] Exit 0 | 71,41 s | [B30](roh/NAK-312-86c31f4-dirty.md#b30) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,15 s | [B28](roh/NAK-312-86c31f4-dirty.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. Seit NAK-309 (M-74) zuerst, vor dem ersten Prozessor: im Testbau nennt der Prozessor seinem v2-Client einen Probe-Namen, nie die Produktions-Pipe, nach Produkt- und Testkonstruktor denselben, und jedes Ziel mit den Prozessorquellen ausser dem Produkt traegt den Testschalter. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,27 s | [B8](roh/NAK-312-86c31f4-dirty.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook. Der Quellumfang kommt seit NAK-283 (M-69) aus einem pruefbaren Inventar: jede Datei mit Endung .rs, .cpp, .h oder .hpp unter broker/src und eq-copilot/plugin samt allen Unterbaeumen, mit Grund ausgenommen nur der flatc-Codegen (broker/src/generiert, eq-copilot/plugin/vertrag/generiert) und der Testbaum eq-copilot/plugin/tests; der Bericht druckt das Inventar, und eine Quelldatei, die weder gemessen noch ausgenommen ist, beendet den Lauf als WERKZEUGFEHLER (Inventarriegel, Exit 2). Gemessen werden Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Grenzen - Zeilen je Datei, Funktionen ueber 200 Zeilen, Kommentar-Bezeichner - werden ehrlich gemeldet, auch wenn sie reissen (Pflegeticket NAK-292). Jeder neue Treffer ueber der Zeilengrenze meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 129 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung; das Inventar mit neuem Unterbaum, Codegen, Tests und einer Ableitung, die still einen Unterbaum verliert). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 heisst gerissen und ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest, eine fehlende Inventarwurzel oder der Inventarriegel, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen, und ein ungemessener Unterbaum machte jedes andere Mass falsch. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 34,31 s | [A32](roh/NAK-312-86c31f4-dirty.md#a32) |
| A33 | clang-tidy ueber alle Uebersetzungseinheiten von eq-copilot/plugin ohne tests/ und ohne den flatc-Codegen vertrag/generiert/ (Bein A9 haelt ihn bytegleich; Regelsatz eq-copilot/plugin/.clang-tidy: bugprone, clang-analyzer, concurrency, performance sowie eine cert-, cppcoreguidelines- und misc-Auswahl; keine Stilregeln, Ausnahmen dort mit Grund) in einem eigenen Ninja-Baum eq-copilot/build-tidy mit denselben cl.exe-Schaltern wie der Produktbau. Gezaehlt werden eindeutige Fundstellen (Datei, Zeile, Spalte, Check) gegen die Ratsche in tools/plan/tidy.py: Grenze = Iststand der Erstmessung NAK-288, Ziel 0, nur gesenkt, nie erhoeht; eine Aenderung des Regelsatzes zieht die Ratsche im selben Commit nach. Vorangestellt der Selbsttest (Einordnung in den Quellort, Datenbankfilter, Diagnoseparser, Deduplizierung, Ratschenrand, Umgebungsleser). Nicht blockierend: Exit 4 ist ein Hinweis. ROT ist Exit 2: eine Uebersetzungseinheit mit error-Diagnose oder ein abgestuerzter clang-tidy heisst NICHT gemessen, denn eine halb geparste Einheit meldet weniger Funde und saehe wie Fortschritt aus. FEHLT (Exit 3) ohne Visual Studio 2022 mit C++-Werkzeugen und Clang-Komponente, cmake oder ninja. | `py -3.13 tools\plan\tidy.py --mit-selbsttest` | [OK] Exit 0 | 108,74 s | [A33](roh/NAK-312-86c31f4-dirty.md#a33) |


### 38.27 Kanon nachher der Etappe 6 (Bauer, 22.09.2026)

- Vor dem Start lief kein Bau- oder Testprozess (NAK-290); ein verwaister
  `vctip.exe` aus dem ersten Einzelbau (Start 22:32:54) ist vorher beendet.
- Befehl nach dem Auftrag, abgekoppelt, Titel `NAK-312 Etappe 6`, auf
  `86c31f46`; Start 21.09.2026, 23:30:21 (Kopf des Runners), Ende 22.09.2026,
  00:20:49 (Zeitstempel der Logdatei, letzte Zeile `EXIT=0`). **GRÜN 70/70**,
  Exitcode 0, stillgelegt bleibt A15. Kopf und Übersicht stehen im Runnerblock
  „Kanon-Lauf - NAK-312 Etappe 6" unmittelbar über diesem Abschnitt, die
  Rohausgabe unter `docs/beweise/roh/NAK-312-86c31f4-dirty.md` („dirty" sind
  allein die zwei fremden untracked Ordner: „2 unbestaetigte Datei(en)").
- Ein Stillstand erklärt: nach „Baue:" stand das Log ohne Compiler, allein
  `vctip.exe` (Start 23:30:41) lebte; um 23:44:46 beendet (NAK-300), danach lief
  der Runner weiter. A4 lief 1 132,32 s ohne Logzeile; sein Testprozess
  `sonde014_proposal` verbrauchte dabei CPU-Zeit (84,8 s auf 87,5 s in 15 s,
  gemessen 23:59). Die vierte Runde des Hintergrundwächters hat das System
  wegen Speicherknappheit beendet (nicht den Kanon); danach ist im Vordergrund
  gewartet.
- **A32** [HINWEIS] Exit 4, nicht blockierend: Quelldateien über 2000 Zeilen 2,
  über dem Ziel 1500 Zeilen 11, Funktionen über 200 Zeilen 34,
  Kommentar-Bezeichner ohne Code 39 — dieselben Zählwerte wie die Kanons der
  Etappen 4 und 5; die Etappe 6 erhöht keinen. **A33** [OK]: 58 Fundstellen von
  höchstens 58 — die Etappe 6 (6a und 6b) fügt keine hinzu.
- **M-77 am Endstand.** Die Beine der Zeile einzeln: B1, B2, B3c, A14, A16 und
  A3 je [OK], dazu A1, B13, B14, B15 [OK]. `git diff --stat fd287d3c 86c31f46`
  über `eq-copilot/schemas`, `eq-copilot/fixtures`, `eq-copilot/identity`,
  `design`, `broker`, `eq-copilot/install`, `eq-copilot/plugin/dsp`,
  `eq-copilot/plugin/state`, `eq-copilot/plugin/core` und
  `eq-copilot/plugin/sonde` ist **leer**: keine neue Stateversion, keine
  v3-Vertragsänderung, Identität und Legacy-Bezeichner unverändert. Keine neue
  Oberfläche: die 25 bisherigen Bilder des Sichtsatzes sind über alle drei
  Änderungssätze bytegleich (§38.5, §38.13, §38.21); 6b ändert keinen
  Audiopfad, der Nulltest bleibt bitidentisch (A1, A16).
- Nicht gefahren: der Laufzeit-Arm (Auftrag). Die Bundlebytes beider Plugins
  haben sich mit der Etappe 6 geändert; vor dem Laufzeit-Arm ist `--hashen`
  des Installers fällig.

### 38.28 Hinweise an den Dirigenten — Abschluss der Etappe 6b

- **Commits der Etappe 6b:** `db682244` (A), `53754c66` (B), `6fbe396a` (C),
  `86c31f46` (A3-Behauptungszeile) und der Commit dieses Abschnitts.
  Rundenbilanz `ed1722d9..86c31f46` (gemessen): Produkt 3 Dateien
  (`PluginEditor.cpp`, `PluginEditor.h`, `SourcesModel.cpp`), Tests 2 Dateien,
  Prüfwerkzeug 1 Datei (drei Behauptungszeilen), Doku 18 Dateien.
- **Produktwirkungen** stehen je Änderungssatz in §38.9, §38.17 und §38.25; die
  sichtbaren: ein Klick auf eine andere Quelle speichert den offenen Namen,
  Escape nimmt ihn zurück, das Ersatz-Hauptziel ist die oberste Zeile, ein
  Hauptziel hinter Zeile 20 hat keine Handgriffe und einen ehrlichen Status.
- **Für die Erstprüfung vorgemerkt:** A-89 (Bestätigung beim Auswahlwechsel
  im Klick statt in `aktualisiereSourcesSteuerung`), A-90 (Escape verwirft;
  M-68 (a) in zwei Hälften), A-92 (Kennung mit Runtime-Nonce, Vergleich gegen
  die Modellsicht von jetzt), A-97 (Ersatz ohne Zeilenzahl im Modell), A-99
  und A-100 (Statuszeile und Feldbeschriftung; `paintMainFlaeche` ist nicht in
  der Funktionsliste der Ticketgrenze, die Stelle ist Folge von B).
- **Registervorschläge, nicht gebaut:** (1) ein im Umordnungsfall vom
  Prozessor abgewiesener Entwurf verliert seine Fehlermeldung an den neuen
  Status (§38.16); (2) ein Abbau des Editors innerhalb des Konfliktrückrufs
  träfe die folgende Statuszeile (§38.24, §38.25); (3) ob ein still
  verfallender Entwurf eine Meldung braucht, ist eine Designfrage (geparkt,
  §38.9).

## 39. Messung der Etappe 6b, Sparmodus, Übergabe an die frische Dirigentensession (Dirigent, 22.09.2026, 00:25 Uhr)

### 39.1 Messung

Worker `b9272cb2` (Opus, Effort xhigh, `dontAsk`, Aufsicht ENG), Start
21.09.2026, 22:19 Uhr, `done` gemessen 22.09.2026, 00:24 Uhr, Basis
`ed1722d992f64567e131aaa7fa5ae887bdef96bf`. Fünf Commits — `db682244`
(Änderungssatz A, Labelentwurf), `53754c66` (Änderungssatz B, Ersatzziel und
Aktionssteuerung), `6fbe396a` (Änderungssatz C, Kennungskonflikt-Rückruf,
NAK-349), `86c31f46` (A3-Behauptungszeile), `230d4bc4` (Manifest §38.27 und
§38.28) = `origin/master` = `230d4bc44cf3bafdab2780ffb6cb497048fe920d`, Baum
sauber (nur die zwei fremden untracked Ordner), Basis ist Vorfahr von HEAD.
Rundenbilanz `ed1722d9..230d4bc4` (gemessen): Produkt 3 Dateien +209/-33, Tests
2 Dateien +976/-2, Prüfwerkzeug 1 Datei +3/-3, Doku 19 Dateien +12323/-4.
**Kanon nachher der ganzen Etappe 6: GRUEN 70/70**, Exitcode 0, am Stand
`86c31f46`, Lauf 21.09.2026, 23:30 Uhr (Kopfzeile des Runners und `EXIT=0` im
Log vom Dirigenten gelesen; Rohausgabe `docs/beweise/roh/NAK-312-86c31f4-dirty.md`).
**Nicht gelesen vom Dirigenten** (Sparmodus): die Hinweise des Bauers in §38.9,
§38.17, §38.25 und §38.28 und die Abweichungsliste von 6b; **nicht gefahren:**
der Laufzeit-Arm der Etappe 6.

### 39.2 Sparmodus (User 21.09.2026, 23:5x Uhr)

Bis zum Claude-Wochenreset (gemessen: 25.09.2026, 15:00 Uhr; Stand 22.09.2026,
00:24 Uhr: 88 %) gilt: **kein Opus-Lauf mehr, keine Fragen an den User, so wenig
Dirigenten-Turns wie möglich.** Keine Kontrollticks im Halbstundentakt — der
Hintergrundbeobachter (`cockpit.ps1 -WatchWorker` als Hintergrundkommando)
meldet das Workerende, dazu höchstens ein seltener Rückfall-Cron; Kommandos
bündeln; `claude logs` nie ungekürzt lesen; vom Manifest nur Kopf und letzte
Abschnitte. Bau und Prüfung nach Skill §3.6: Bauer Codex `gpt-5.6-sol` xhigh
(`workspace-write`), Prüfer und Validierer `gpt-6-astra` high, nie max. Die
Codex-Woche setzt am 22.09.2026 um 22:19 Uhr zurück (gemessen); vorher läuft
nichts über Codex (94 %).

### 39.3 Übergabe

Diese Session endet im sauberen Fenster (kein Worker, kein Kanon, kein
Beobachter, kein Cron-Job, alles gepusht) und startet sich über den Marker
frisch, weil jeder Turn hier rund 380k Tokens Kontext liest. Die nächste
Session:

1. fährt den Laufzeit-Arm der Etappe 6, wenn kein fremdes FL-Projekt offen ist
   (`pwsh -NoProfile -File tools/fl/laufzeit.ps1 -Ticket NAK-312 -Basis fd287d3c`;
   am 21.09.2026 lief ein FL64 des Users), Kopfzeile ins Manifest, nachgezogene
   Installer-Hashes mit Pathspec committen;
2. setzt einen One-Shot-Cron kurz nach dem Codex-Reset (22.09.2026, 22:25 Uhr)
   und tut bis dahin nichts;
3. liest dann §38.9, §38.17, §38.25, §38.28 und die Abweichungen von 6a und 6b
   (A-78 und A-81 sind Produktcode abseits des Bauplans, §37.2) und startet die
   **Erstprüfung der ganzen Etappe 6** über `tools/dirigent/codex-lauf.ps1`,
   Vorlage A, T2, `gpt-6-astra` high, Prüfbereich `git diff fd287d3c...<HEAD>`
   über die Pfade der Etappe 6 (§7.0 und die Aufträge
   `docs/beweise/roh/NAK-312-etappe-6a-auftrag.txt`,
   `docs/beweise/roh/NAK-312-etappe-6b-auftrag.txt`), Matrixzeilen M-55 bis
   M-77, M-84 bis M-86, M-89 bis M-92 und die Erweiterung von M-40; Rohurteil
   an der Quelle validieren lassen (Astra high, frischer Thread);
4. lässt danach die Etappe 7 (Produktabhängiges nach U48, U49, U51, U56, U57 —
   alle beantwortet außer U57, dort gilt ohne Antwort Weg 1) mit eigenem
   Matrixnachtrag von Codex Sol xhigh bauen und committet dessen Stand nach
   eigenem Kanonlauf (Skill §3.6, §3.1);
5. hat offen im Register: NAK-357 (Quellvalidierung `SondeProcessor::reset()`),
   NAK-355 (FLs Renderweg vor S29–31).

## 40. Laufzeit-Arm der Etappe 6, User-Entscheid NAK-358 (Dirigent, 22.09.2026, 11:34 Uhr)

### 40.1 Laufzeit-Arm

Aufruf 22.09.2026, 11:22 Uhr, am Stand `c2d4f8be` (= `origin/master`;
Produktstand `86c31f46` plus Doku), Basis `fd287d3c`, abgekoppelt, Ende
11:33 Uhr, `EXIT=3`. Kopfzeile:

`LAUFZEIT NAK-312 c2d4f8be VORAUSSETZUNG installation=installiert; pruefen Exit 0, 3 Artefakt(e) aktuell szenarien=6 verfehlt=1 [bereitschaft.json=0,fenster.json=0,nulltest-host.json=5,schleife-dauerlauf.json=0,snapshot-runde01.json=0,u40-aktivitaetsgate.json=0] fl=Producer Edition v26.1.4 [build 5589]`

Rohdatei `docs/beweise/roh/NAK-312-laufzeit-c2d4f8be.md`. Gleiches Bild wie
alle Läufe seit der Etappe 2 (§19, §22, §25, §28, §31, §32): fünf Szenarien
grün, `nulltest-host` Exit 5 nur an Schritt 8 (`verarbeitung_ein`:
Referenzprojekt fehlt, Karte U43, K-286-1); Auslieferungsrender bitidentisch
(SHA-256 `FAE3676F…`), Diagnose- und Referenzprojekt am Ende bytegleich zum
Repo. Die nachgezogenen Installer-Hashes in
`eq-copilot/install/nakama-installer-v1.json` (3 Artefakte) gehören zu diesem
Commit. Kein Befund gegen die Etappe 6.

### 40.2 User-Entscheid NAK-358: FL ist in der Bauphase nie des Users, wird immer beendet

Der Dirigent hatte um 00:25 Uhr das runner-eigene Diagnose-FL (PID 218184,
seit 21.09.2026, 18:49 Uhr, Besitzeintrag in `eigene-prozesse.json`
vorhanden) ohne Blick in die Besitzliste als fremd gewertet, den Arm
ausgelassen und den User um das Schließen gebeten (Verstoß gegen Skill §6).
User-Wort 22.09.2026 wörtlich im Register NAK-358; Folge in Skill §3.3 und
§4, CLAUDE.md „Bauen und beweisen" und `tools/fl/LIES-MICH.md`: jeder Aufruf
des Arms trägt `-Beenden`, ein vorgefundenes FL beendet der Runner (eigen)
oder der Dirigent per `Stop-Process`. Dieser Lauf ging noch ohne `-Beenden`;
der Runner beendete seine Instanzen zwischen den Szenarien selbst (PID
218184, 89272, 162060), die letzte (PID 326088, eigen, Diagnoseprojekt in der
Befehlszeile) beendete der Dirigent um 11:34 Uhr per `Stop-Process`; danach
kein FL64-Prozess. Kontextfläche nach dem Umbau gemessen: Skill 24 567 Bytes
(Grenze 24 576), CLAUDE.md 20 300 (20 480), MEMORY.md 6 311 (22 528);
Dokuriegel auf CLAUDE.md, Skill, Register und LIES-MICH: 0 Befunde. Offen
als Werkzeugpunkt in NAK-358: Standard des Runners auf Beenden drehen.

### 40.3 Nächster Schritt

Unverändert §39.3 Punkte 2 bis 5: One-Shot-Cron kurz nach dem Codex-Reset
(22.09.2026, 22:27 Uhr), dann Erstprüfung der ganzen Etappe 6 (Vorlage A, T2,
`gpt-6-astra` high), Validierung, Etappe 7.

## 41. Sparmodus aufgehoben, Erstprüfung der Etappe 6 (Dirigent, 22.09.2026, 20:0x Uhr)

### 41.1 User-Entscheid NAK-359

User 22.09.2026, 19:2x Uhr: „OPUS 5.5 ist draussen und ich hab einen wochenreset,
den ich manuell anwenden kann. das hießt es geht jetzt weiter, aber mit opus max
als bauer" (Register NAK-359, wörtlich). Gemessen: `claude -p … --model opus`
antwortet `claude-opus-5-5` (CLI 2.1.280). Folge: der Sparmodus aus §39.2 ist
aufgehoben; Bauer und alle Opus-Worker `--model opus --effort max`; Prüfer und
Validierer bleiben frische, lesende Opus-Threads mit max, solange Codex über
90 % steht (gemessen 19:29 Uhr: 94 %, Reset 22:19 Uhr); danach Astra nach Skill
§3.4. Skill §3.2/§3.6 und Memory nachgezogen; der One-Shot-Cron auf den
Codex-Reset ist gelöscht.

### 41.2 Erstprüfung der Etappe 6 — Rohurteil

Auftrag `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-auftrag.txt` (Vorlage
A, T2, BASIS `fd287d3c`, ZIEL `86c31f46`, 13 Pfade, Matrixzeilen M-55 bis M-77,
M-84 bis M-86, M-89 bis M-92 und die Erweiterung von M-40; Ausschlüsse
NAK-340 bis NAK-359, NAK-292, NAK-307, Etappe 7, Designteil). Prüfer: frischer,
lesender Opus-5.5-Thread `23eb5a93` (`nakama-nak312-86c31f4-e6pruef`), Effort
max, Start 19:35 Uhr, `done` 20:01 Uhr; HEAD `87e8af15` vor und nach dem Lauf
gleich; Beobachter (Aufsicht NORMAL) und 15-Minuten-Cron liefen und sind
abgeräumt. Bericht `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-urteil.md`
(23 147 Bytes): **URTEIL: PASS — 0 Defekte, 6 Lücken, 6 Härtungen.**

Rohbefunde (Wortlaut im Bericht): L-1 `reset()` gleichzeitig zu `process()`
(Hostvertrag, für FL ungemessen; JUCEs FL-Sperre umschließt `setProcessing`
nicht); L-2 A-78/A-79 gegen den Wortlaut von M-59, M-62, M-64 (Bauplanweg
ersetzt, Zusagen halten, drei Matrixstellen beschreiben den alten Weg); L-3
M-84 unter Produkttakt: das end nach „weg von Main" verwirft der Sender
(`Ipc.cpp:139-147`, seit BASIS unverändert; heute nur über den Restfall
NAK-341 erreichbar); L-4 abgewiesener Entwurf im Umordnungsfall verliert die
Meldung (§38.28 (1)); L-5 Editorabbau innerhalb des Konfliktrückrufs, SafePointer
nur am Eintritt geprüft (§38.28 (2)); L-6 M-56 „der Grund steht im Status" ist
in der Main-Fläche nicht gezeichnet (Produktlücke wie NAK-341). H-1
Allokationszähler sieht nur `operator new`/`new[]`; H-2 Testzähler
`zuteilungen` im Produktpfad ohne Makro; H-3 M-68 (b) fährt nicht das Ereignis
der Zeile; H-4 Anzeigeräumung fragt die Klassifikation nur zum Tick; H-5
Meldung für still verfallenden Entwurf ist Design (geparkt); H-6 Publisher-Aufruf
in `spiegleKlassifikation` hängt am VST3-Threadmodell. A-78 und A-81:
technische Entscheidungen innerhalb der GRENZE, nachgerechnet, kein
Zusagebruch.

### 41.3 Validierung und Einordnung

Rohurteil an der Quelle validieren lassen (frischer, lesender Opus-5.5-Thread,
Effort max; Auftrag `docs/beweise/roh/NAK-312-etappe-6-erstpruefung-validierung-auftrag.txt`,
Bericht `…-validierung.md`); Einordnung durch den Dirigenten folgt in §41.4.

### 41.4 Abschlussaufgabe aus dem Kontexthygiene-Volllauf (22.09.2026)

`docs/plugin-wissen.md` kennt NAK-312 nicht (0 Treffer; §1.7 endet bei
NAK-311). Der Abschluss dieses Tickets zieht einen Abschnitt §1.8 „Sonde und
Messpunkte (NAK-312, Etappen 2–6)" nach: Besitzschleuse, Messpunkt- und
Kennungskonflikt-Panel, RT-Wache ab Callback-Eintritt, Blockbindung der
Hostwerte, read-only-Ladestart, Hörmarkierungs-Intervall über `reset()` und
`releaseResources()` (Register NAK-360, Punkt 1).
