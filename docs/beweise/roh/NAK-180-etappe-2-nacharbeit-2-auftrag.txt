NAK-180 — Etappe 2, Nacharbeit 2 (Runde 2 von 3). Auftrag des Dirigenten (2026-09-05).

DU BIST ein frischer Opus-Bauer (max) im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen, Commits nur mit explizitem Pathspec, Push nach `git pull --rebase` bei sauberem Worktree. Keine Neben-Refactors, keine Legacy-Umbenennung, keine Schemaänderung (v3-Verträge eingefroren; keine neuen Wire-Felder). Landminen: MSVC-Stack 1 MiB (Engines/Prozessoren in Tests auf den Heap); der Kanon läuft abgekoppelt (Befehl unten), Logausgabe mit *> (überschreibt, kein Löschen nötig); Test-Pipes nur im Probe-Namensraum.

QUELLEN, die du zuerst liest:
1. docs/beweise/NAK-180.md — Kopftabelle, §2 (Verhaltensmatrix E1 bis E11, N-01 bis N-37, §2.5 Nachträge), §7 (Erstprüfung: EP-01 bis EP-21 mit Regeln), §7.1 (was Nacharbeit 1 einarbeitete), **§7.2 (Wiederprüfung 1: die acht Defekte WN-01 bis WN-08 mit je der Regel, die sie schließt — das ist dein Auftrag)**.
2. docs/beweise/roh/NAK-180-etappe-2-wiederpruefung-1-829158d.txt — die Befunde wörtlich (Datei:Zeile, Begründung).
3. docs/beweise/roh/NAK-180-auftrag.txt — Ticketgrenze, Regeln R1 bis R6 (R3 (a)/(b) für WN-07/WN-08), Beine, Kanonbefehl, Selbstaudit.
4. tools/dirigent/pruefliste.md — vor jedem Commit abhaken, im Manifest je Zeile den Messort nennen.

AUFTRAG: Schließe genau die acht Defekte aus §7.2 mit den dort genannten Regeln — nicht weniger, nicht mehr. Drei sind Produkt (WN-01 Neutralitätsprädikat mit wartendem End, WN-02 Zustellgeneration = Wire-Commit-Generation, WN-03 Nachlauffrist als Maximum ohne 3600-s-Deckel), fünf sind Testform (WN-04, WN-05, WN-06: die Tests halten den ECHTEN Produkt-Callback bzw. Aufbauzug per Haken fest und erzwingen die Lage deterministisch — Latch/Barrier/Condition, kein Sleep; WN-07: Rust-E2E über Wire-JSON und eingespeiste Evidenz; WN-08: r01Resync über den laufenden ControlClient gegen einen Probe-Server). Je Regel ein Rotbeweis (docs/beweise/roh/NAK-180-rot-wn0x.txt), der genau die Regel zurückdreht und dabei wirklich fällt — ein Rotbeweis, der aus einem anderen Grund fällt (WN-04 heute), zählt nicht.

REIHENFOLGE (je Schritt eigener Commit mit Pathspec, betroffene Beine grün vor dem Commit):
1. Produkt: WN-01, WN-02, WN-03 (PluginProcessor.cpp/.h, ControlClient.cpp/.h) mit je einem neuen Testfall in EqCopSonde013PassageStateTest bzw. EqCopIpcTest.
2. Tests: WN-04, WN-05, WN-06 (Haken vor CAS / vor Löschungen / vor dem Aufbauzug-Lock; die Haken sind Test-Only-Einhängepunkte im Produkt, nie im Audiothread).
3. Rust-E2E WN-07 (broker/tests/sonde013_verdrahtung.rs).
4. WN-08: r01Resync über den laufenden ControlClient gegen einen Probe-Server (vorhandene Testdoubles zuerst suchen: EqCopIpcTest, EqCopLebenslaufTest, tools/eq-copilot Probe-Broker; nie der Produktions-Pipename).
5. Manifest: §2.5 Nachträge, wo eine Regel eine Zeile präzisiert (N-05 Frist als Maximum, N-27 Wire-Commit-Generation, N-36 wartendes End); §6.3 Messbilanz; neuer Abschnitt §7.3 „Nacharbeit 2 — was eingearbeitet wurde" (Tabelle WN-xx → Stelle → Test → Rotbeweis); §6.7 Prüfliste; Kopfzeile Phase „Etappe 2: Nacharbeit 2 abgeschlossen, Kanon <GRÜN n/n> auf <sha>, Wiederprüfung 2 offen".
6. Voller Kanon abgekoppelt:
   $log = Join-Path $env:TEMP 'nakama-nak180-kanon.log'
   $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-180.md -Anhaengen -Titel NAK-180 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
   Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile','-Command',$befehl
   Fertig, wenn die letzte Logzeile mit EXIT= beginnt (höchstens alle 15 Minuten `Get-Content $log -Tail 3`); kein Stand mit NOT RUN.
7. Commit (Manifest, Rohausgaben) mit Pathspec, `git pull --rebase`, Push. Kein Anfassen von docs/beweise/G4.md, docs/offene-punkte.md, docs/PLAN-STAND.md, docs/plan/. Turn mit der letzten Zeile „ETAPPE 2 NACHARBEIT 2 ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.

SELBSTAUDIT vor jedem Commit (adversarial): Fällt jeder Rotbeweis aus GENAU dem Grund, den seine Regel nennt? Gibt es noch einen Sleep als einzige Ordnung? Liest ein Rückruf oder Callback eine andere Generation als die, die zu seiner Wirkung gehört? Audiothread ohne Sperre/Allokation, Nulltest bitidentisch, keine Schemaänderung.