URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).

# NAK-311 Erstprüfung 2 (S25j, Etappe 2, W04) — Bericht des lesenden Prüfers

Stand 19.09.2026. Prüfbereich: `git diff 99307112a6ab0210079e45d04a360a149b170c5c...f43a65c0844b255cff6eb88f91e75d5aead6883d` über `tools/fl/nulltest.py`, `tools/beweise.ps1`, `eq-copilot/plugin/dsp/DspKern.cpp`, `DspKern.h`, `DspProgramm.cpp`, `DspProgramm.h`, `eq-copilot/plugin/tests/DspGoldenTestMain.cpp`, `SondeNullTestMain.cpp` (8 Dateien, +1152/-44). Der Code im Arbeitsbaum ist der Code des ZIEL: `git diff --stat f43a65c0 HEAD -- eq-copilot tools broker` ist leer, HEAD trägt darüber nur `docs/PLAN-STAND.md`, `docs/beweise/NAK-311.md` und den Prüfauftrag. Zeilenangaben gelten zum ZIEL.

## Prüffragen

**(a) Messgerät (T3-09-02, R-311-2, M-02 bis M-09).** `vergleiche` (`tools/fl/nulltest.py:205-283`) zählt wie bisher die Wertunterschiede (`ungleich = r != q`, :221) und vergleicht dieselben ausgerichteten Bereiche zusätzlich in ihrer Float-32-Bitdarstellung (:224); `nullvorzeichen` sind die wertgleichen, aber bitverschiedenen Stellen (:225-226), bei Float 32 genau ±0, weil NaN nie wertgleich ist. Mit Wertunterschieden entscheidet der unveränderte Weg KETTE oder ABWEICHUNG (:265-283), die Nullvorzeichen stehen als Zahl daneben; ohne Wertunterschiede gilt die Regeltabelle :253-256 in der Rangfolge VERSATZ (v ≠ 0), NULLVORZEICHEN (v = 0, nullvorzeichen > 0), BITIDENTISCH (v = 0, nullvorzeichen = 0 und gleiche SHA-256 des Bereichs), sonst fail-closed ABWEICHUNG. BITIDENTISCH setzt damit gleiche Bytes des Bereichs voraus: 0 Abweichungen und 0 Nullvorzeichen heißen, dass `bitverschieden` überall falsch ist; die SHA-Gleichheit prüft es ein zweites Mal. `EXIT_JE_URTEIL` (:286-289) führt NULLVORZEICHEN mit Exit 5 und ABWEICHUNG mit Exit 4. Weg R2 ist im Code unverändert: `bewerte_verarbeitung` (:388-406) entscheidet an `abweichungen` und urteilt bei reinem Nullvorzeichen VERFEHLT, Exit 4 (neu ist nur das Befundwort NULLVORZEICHEN), `lies_auslieferung` verlangt weiter BITIDENTISCH (:420), `abweichungen_gegen_auslieferung` vergleicht weiter bitweise (:454); Urteilswörter und Exits von R2 bleiben. `tools/fl/laufzeit.ps1` und `tools/fl/szenario.py` sind zwischen BASIS und ZIEL unverändert; der Rückweg fährt nur bei `(Nulltest-Urteil) -eq 'ABWEICHUNG'` (laufzeit.ps1:976), ein Szenario-Exit 5 endet VORAUSSETZUNG mit Exit 3 (:1084), und `szenario.py` reicht den Exit 5 von `nulltest.py` durch (:993-997). Gemessen: A35 `py -3.13 tools/fl/selbsttest.py` GRÜN, Exit 0 (nulltest.py 12 ok, darunter 311/M-03, 311/M-04, 311/M-05, 311/M-07, 311/M-08, M-62, M-75, beide M-64; laufzeit.ps1 37 ok, darunter M-65 `rueckweg_nur_bei_abweichung`; szenario.py 39 ok). Zusätzlich über `fahre` in einem Scratch-Skript außerhalb des Repos: ein bytegleicher IEEE-Float-32-Render (der Zustand der Matrixzeile M-02) urteilt BITIDENTISCH, Exit 0, nullvorzeichen 0, SHA-256 gleich; ein Render mit Faktor 0,5 und zwei Nullvorzeichen an Quellnullen bleibt KETTE, Exit 5, nullvorzeichen 2; ein signalisierender NaN 0x7F800001 an einer Quellnull endet ABWEICHUNG, Exit 4, nullvorzeichen 0; zwei reine Nullvorzeichen enden NULLVORZEICHEN, Exit 5, mit erster und letzter Stelle in Ergebnis und Rohzeile.

**(b) Neutralprüfung (T3-01-01, §7.2 Punkt 3, M-10 bis M-21).** Das Merkmal `neutral` (`DspProgramm.cpp:274-278`) verlangt engagiert, keinen Hard-Bypass, je aktivem Band einen statischen Einheitsbiquad ohne SVF, die M/S-Stufe aus (:190, width 1,0 und mono_bass nicht über 0) und beide Trims bei 0 dB. Der Kern (`DspKern.cpp:1095-1108`) lässt das Schreiben nur, wenn nach dem Stück der Committed-Pfad ohne Übergang auf einer engagierten, nicht hard-bypassten Bank steht (`pfadSteht`), `hoerLaufend` Processed oder Dry ist und entweder `neutral` mit allen fünf Rampen `ruhtBei (1.0)` gilt oder Mix `ruhtBei (0.0)` mit Output `ruhtBei (1.0)` und den übrigen Rampen in Ruhe. Beides ist bauartbedingt die Identität: im ersten Fall überspringt `verarbeiteBank` jede Stufe (:651, :668, :709-731; Bänder :483), im zweiten setzt :726 den rohen Dry-Wert, und der ruhende Output-Trim multipliziert nicht. Die Grenze `max (zc.rest am Stückbeginn, hoerFadeRest)` (:914, :1107) liegt auf dem Sample: Rampen starten nur in `blockrand` zusammen mit einem Übergang (:410-424), `kRampeSamples` und `kFadeSamples` sind beide 256 (`DspProgramm.h:53`, `:57`), beide zählen je Sample, und `Rampe::tick` setzt am letzten Sample exakt das Ziel (`DspKern.h:340-349`); ruhen die Rampen nach dem Stück und lief darin kein Übergang, ruhten sie das ganze Stück. Das Schreiben setzt am ersten Sample eines Übergangs wieder ein, weil `blockrand` nur am Beginn eines äußeren Aufrufs übernimmt und der neue Übergang das erste Stück dann schreibend macht (pfadSteht falsch oder Grenze mindestens 256); ein Hörmatrixwechsel setzt `hoerFadeRest` vor der Grenzrechnung (:1004-1026). Gegenrichtung an der Quelle durchgegangen: wirksames Band (Merkmal falsch, der M-33-Zweig nur bei ruhendem Mix 0), laufende Rampe, Crossfade und Hörmatrix-Fade (in der Grenze, auch nach der W-5-Umkehr), Delta und Candidate (`hoertPfad` falsch), Rampe durch 0,99999994 (`ruhtBei` fragt `rest`, `aktuell` und `ziel`), Mix 1e-9 (ruht weder auf 0 noch auf 1), M-33 mit wirksamer M/S-Stufe oder Auto-Gain (der Wet-Zweig geht bei Mix 0 nicht ein), wartender dritter Hörmatrixwechsel (bis zum Fadeende hört `hoerLaufend` Processed oder Dry, der Wechsel beginnt im nächsten Stück wie vor der Etappe), Mono (dryR = dryL, geschrieben wird nur Kanal 0), Host-Reset (`beendeAudiohistorie` beendet Übergang und Rampen gemeinsam): in keinem erreichbaren Zustand greift die Prüfung, obwohl der Ausgang nicht der Eingang wäre. Der F-4-Zweig (:916-936) ist über den Vertrag nicht erreichbar, nachgerechnet an allen sechs Entwürfen (`DspFilter.h:116-218`) gegen `Biquad::istEinheit` (:50-53): Bell und Shelves tragen b1 oder a1 ungleich 0, weil cos(w0) in double nie exakt 0 ist, Notch und Cuts tragen b2 ungleich 0 oder b0 ungleich 1, nicht endliche Parameter ergeben NaN; `neutral` heißt damit praktisch „kein aktives Band“, und §22.5 Punkt 4 bleibt ohne Befund. Riegel, beide Pfade, Taps und Zähler laufen vor dem Schreibende unverändert (:938-989); im nicht mehr geschriebenen Bereich hätte vor der Etappe weder der F12-Zähler (:1136, :1141) gegriffen noch der Hörmatrix-Zustand anders geendet; der W-1-Rücksetzblock hängt an `passthroughErreicht` (:1079, :1146) und entspricht der alten Bedingung, weil W-1 (`nachPass`) und Neutralprüfung (`pfadSteht`) sich ausschließen; die Sonde misst weiter am Tap `post_committed`, nie am Hostpuffer (`SondeProcessor.cpp:401-433`). Gemessen: B6 Exit 0, „279 geprueft, 0 Fehler“, darunter 311/M-12, 311/M-13 bei den Blockgrößen 1, 255, 256, 257, 180 und 4096, 311/M-14, 311/M-18 (Processed, Dry, Delta, Candidate, Hörmatrix-Fade) und beide Selbstauditprüfungen; A16 Exit 0, „110 Pruefungen ok, 0 Fehler“, darunter 311/M-10, 311/M-11 mit M-15, M-16 und M-17, 311/M-19, 311/M-20, 311/M-92, 311/M-93 bei 44,1, 48 und 96 kHz und 311/M-01 mit MXCSR 0x00009FE0 (FTZ 1, DAZ 1) und „a 0, b 0, c 0“ veränderten Mustern.

**(c) Echtzeit (M-22, CLAUDE.md).** Die Neutralprüfung liest je Stück eine Handvoll Felder (Programm, Rampenzustand, Übergangsrest, `hoerFadeRest`) und läuft im neutralen Programm über höchstens acht Bandprogramme (`DspKern.cpp:926-935`); `programmVon` und `baenke.bank` liefern Referenzen auf vorallokierte Bänke. Keine Allokation, keine Sperre, kein Logging, kein Datei-, Pipe- oder Netzzugriff, keine Arbeit je Sample. Gemessen im eigenen B6-Lauf: `null_allokationen_im_callback_samt_programmwechseln` mit dem neutralen Schritt bei Block 370 (Testzähler 0, Kernzähler 0, 81 Übernahmen) und `null_sperren_im_callback` (Sperrenzähler 0) über denselben Lauf (`DspGoldenTestMain.cpp:3176-3243`). Dass der Lauf die Prüfung tatsächlich erreicht, belegt die Rohdatei M-22 (666 Allokationen unter der Mutation in der Prüfung).

**(d) Rotbeweise und Messkraft.** Alle Rohdateien M-02 bis M-22, M-82, M-83 und M-90 bis M-93 gelesen, jede Mutationsstelle am ZIEL nachgemessen; jede trägt dort wörtlich den „vorher“-Text der Rohdatei und ist die Zeile, die die Zusage trägt: `nulltest.py:221` (M-06, M-07), :224 (M-02, M-03 Mutation A), :224 mit der Regel BITIDENTISCH :255 (M-03 Mutation B), :248 (M-04), :253 (M-05 VERSATZ), :537 (M-05 Rohzeile), :400 (M-08); `laufzeit.ps1:976` (M-09); `DspProgramm.cpp:184` (M-10 Rauschen, M-93); `DspKern.cpp:1107` (M-10 Bitmuster, M-11, M-12, M-19, M-21), dieselbe Zeile als „nur am Stückbeginn“ (M-13), :914 (M-14), :991 (M-15, M-16); `DspProgramm.cpp:277` (M-17); `DspKern.cpp:1097` (M-18), :1103 (M-20), :1106 (M-22); `DspFilter.h:120` (M-82); `DspProgramm.cpp:125` (M-83); `DspKern.cpp:678` (M-90), :726 (M-91, M-92). Der Endstand der Mutationsläufe stimmt mit dem ZIEL überein: `DspKern.cpp` SHA-256 C909BCC38159B72B1D438640791330686D323010A676C5F04D2F3EF7533B6D9D und `SondeNullTestMain.cpp` C51A47859C939BA11F1D95110D5EF06A0EA2189C4FD1F76A13EDE48A10BA5E42 wie in den Rohdateien, `nulltest.py` 61DC53AB37C3505B96042FB2081502274296921BD82FCC6EA7CDBAD4002CD9EE wie im Teil-a-Stand. Je Zeile fällt die Zusage an der erwarteten Prüfung; Mitgefallene sind mit Grund deklariert und hängen an derselben Zusage (etwa `delta_bei_gleichheit_ist_exakt_null` bei M-18, der Ausgangsvergleich über Sample 255 bei M-90). Die angenommenen Abweichungen M-14 (Fall am Teilfall selbst, weil die Ideallinienprüfung aus Abschnitt E von neutral weg rampt) und M-82 (Zeilenvergleich statt 0,05-dB-Schranke) sind an der Quelle stimmig. Die vier Tapvergleiche nach R-311-6 holen die Messkraft zurück: unter den SONDE-015-Mutationen bleibt der Ausgangsvergleich im nicht geschriebenen Zustand grün (M-91, M-92) oder fällt nur noch über Sample 255 (M-90), der Tap fällt jeweils (250 von 514, 8191 von 8192, 905634 von 905634 Stellen); bei M-93 fällt unter M-02-e4 der Tap (906646 von 906648) zusammen mit dem Ausgangsvergleich, weil 1,000001 die Rampenruhe auf 1,0 bricht.

**(e) Bitidentität, Goldens, Verträge.** Ausgeschaltet und Hard-Bypass: A16 311/M-11 Teilfälle M-15 und M-16 grün (Rauschen, Bitmuster, Wachmarke, NaN, ±Inf bytegleich, Zähler 0), dazu die bestehenden Default-Fälle über 1000 Blöcke bei 44,1 bis 192 kHz; der frühe Rückweg bei `committedRuht` (`DspKern.cpp:991-1000`) ist unverändert. RBJ- und Stereo-Goldens: die Abschnitte C, G und H meines B6-Laufs am ZIEL zeilenweise gegen die B6-Ausgabe im Kanon-Rohlauf an der Etappenbasis (`docs/beweise/roh/NAK-311-9930711-dirty.md`) und gegen die Basiszeilen in `NAK-311-etappe2-b6-zeilenvergleich.txt` verglichen: C 78 von 78, G 11 von 11, H 12 von 12 Basiszeilen bytegleich in Reihenfolge; neu sind nur 311/M-90 und 311/M-91 in G (M-82, M-83). Keine neue Stateversion, keine v3-Vertragsänderung, Identität eingefroren: `git diff --stat 99307112 f43a65c0` über `eq-copilot/identity`, `eq-copilot/schemas`, `eq-copilot/plugin/state`, `eq-copilot/plugin/sonde`, `eq-copilot/fixtures`, `eq-copilot/install`, `broker`, `DspFilter.h`, `DspBankPool.*`, `tools/fl/laufzeit.ps1` und `tools/fl/szenario.py` ist leer; das neue Feld `DspProgramm::neutral` ist Laufzeitzustand, wird nicht persistiert und nirgends bytegenau verglichen. Am ZIEL gefahren und grün: A1 `EqCopNullTest` („NULLTEST OK“), B1 `EqCopIdentityTest` (120 ok), B3c `EqCopSchemaTest` (152 bestanden), B7 `EqCopTransactionTest` („TRANSAKTION OK“). Seit BASIS ist am Produktverhalten nichts gebrochen: der Passthrough und der Hard-Bypass laufen wie vorher, der engagiert-neutrale und der M-33-Zustand schreiben ab dem Schreibende nicht mehr und geben den Eingang bytegleich aus, jeder andere Zustand schreibt wie vorher.

## Befunde nach Klassen

**DEFEKT:** keiner.

**LÜCKE:** keine.

**HÄRTUNG** (keine Nacharbeit in dieser Etappe):

- **H-1 (M-09).** `M-65 rueckweg_nur_bei_abweichung` fährt nur KETTE und VERSATZ (`tools/fl/laufzeit.ps1:1911`). Dass NULLVORZEICHEN keinen Rückweg auslöst, folgt aus dem Stringvergleich an :976 (gelesen) und dem Exit 5 aus `EXIT_JE_URTEIL` (gemessen). Ein dritter Schleifenwert `'NULLVORZEICHEN'` würde das neue Urteil direkt bewachen; die Matrix verlangt ihn nicht, und `laufzeit.ps1` bleibt nach §9 F-1 in diesem Ticket unverändert.
- **H-2 (M-02).** `M-62 identisch_null_abweichungen` fährt den bytegleichen Render als PCM 24 (`tools/fl/nulltest.py:658`); die Matrixzeile M-02 beschreibt einen IEEE-Float-32-Render. Selbst nachgemessen: der Float-32-Fall urteilt BITIDENTISCH, Exit 0, nullvorzeichen 0, SHA-256 gleich. Ein Float-32-Teilfall würde das einzige Format unter die Wache stellen, in dem −0 vorkommen kann.
- **H-3 (Neutralprüfung).** Nicht eigens gemessen, an der Quelle korrekt: wartender dritter Hörmatrixwechsel während eines Fades, M-33 mit laufender M/S-Stufe oder eingeschaltetem Auto-Gain, wirksam → neutral per Crossfade, Host-Reset im neutralen Zustand. Je ein Test wäre Härtung.
- **H-4 (Behauptung B6, `tools/beweise.ps1:723`).** „Das Schreibende liegt exakt am ersten Sample nach Crossfade, Rampe und Hoermatrix-Fade, auch mitten im Teilstueck, bei Blockgroessen 1, 180, 255, 256, 257 und 4096“: die Blockgrößenreihe misst 311/M-13 nur für den Crossfade aus der Ruhe; Rampe (311/M-14) und Hörmatrix-Fade (311/M-18) laufen je mit einem Block zu 512. Wortlaut, keine falsche Aussage über das Verhalten.

## Geprüft und nicht geprüft

**Selbst nachgemessen.** HEAD vor dem Lauf; Code des Arbeitsbaums gleich ZIEL (`git diff --stat f43a65c0 HEAD -- eq-copilot tools broker` leer); Binaries `EqCopDspGoldenTest` 21:24:43 und `EqCopProbeeqNullTest` 21:24:34 jünger als jede Quelle unter `eq-copilot` außerhalb der Bauordner (jüngste `DspGoldenTestMain.cpp` 21:09:51); SHA-256 der acht Prüfbereichsdateien und Abgleich mit den Rohdateien; A35 (Exit 0), B6 (Exit 0, 279/0), A16 (Exit 0, 110/0), A1, B1, B3c, B7 (je Exit 0); der Zeilenvergleich C, G und H gegen den Kanon-Rohlauf an 99307112 und gegen die Rohdatei; vier Kanten des Messgeräts über `fahre` und `rohzeile` in einem Scratch-Skript; `git diff --stat` über Identität, Schemas, State, Sonde, Fixtures, Installer, Broker, `laufzeit.ps1` und `szenario.py`; jede Mutationsstelle der 27 Rotbeweiszeilen am ZIEL-Code. Die Scratch-Skripte und ihre WAV-Attrappen lagen nur im Jobordner außerhalb des Repos; `git status --short` zeigte vor und nach allen Läufen nur `briefing-hub/` und `nimbalyst-local/`.

**Nur gelesen, nicht nachgemessen.** Die Mutationsläufe selbst (Bauen ist dem Prüfer nicht erlaubt): Rotläufe, Rücknahmen und Grünläufe stehen nur als Rohdatei. Der volle Kanon GRÜN 69/69 auf 3599ca48 und die Nachfahrt des Dirigenten (§23). Die Messung M-01 vorher (Ausgang (a): in den Zuständen a und b je 24 von 48 Mustern verändert, genau die vier Subnormals, Zustand c 0); nachgemessen ist nur der Stand nachher („a 0, b 0, c 0“). Das Verhalten des Laufzeit-Arms bei NULLVORZEICHEN (aus `laufzeit.ps1:976`, `:1084` und `szenario.py:993-997`; kein Selbsttestfall, H-1). Die Unerreichbarkeit des F-4-Zweigs und die Zustände aus H-3 (Quellenrechnung, kein Lauf). Nicht geprüft, weil nicht Prüfgegenstand: „Nulltest im Host bitidentisch“, der Laufzeit-Arm, die Etappen 3 bis 5 und `docs/**`.

HEAD zu Beginn: fa053d9b7dbb5ba6fdea88bb8d7a44a6dadb058f. HEAD am Ende: fa053d9b7dbb5ba6fdea88bb8d7a44a6dadb058f.
