URTEIL: NEEDS_WORK — fünf Defekte betreffen die zugesagte Messabdeckung, nicht eine nachgewiesene Produktregression.

Geprüft: gebundener Diff, Matrix, zwölf Rotbelege, Runner und delegierende Testauskunft; A26/A27/A25/A8 unter Python 3.14.2 jeweils Exit 0, zusätzliche A26-In-Memory-Gegenproben korrekt, HEAD und Worktree vor/nach unverändert auf 054883faa9679c1ec2caf1310aa15452e4d56bd3.

Nicht geprüft: Compiler, Cargo, Test-Exes und neuer Kanonlauf gemäß Auftrag; Python 3.13 scheiterte beim Interpreterstart, C++-/Rust-Ergebnisse und A5-grün wurden ausschließlich aus Quellen beziehungsweise dem bestehenden Kanonbeleg gelesen, A5 selbst blieb unverändert.

Full review comments:

- [P2] Miss Gain und Leiter am tatsächlich serialisierten Evidenzpfad — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013EventWireTest.cpp:1658-1662
  DEFEKT gegen R4/E4b und N-21: Die Gainzeile liest über `perzentilDb` unmittelbar `FeatureFrame`-Arrays; PRE und POST werden hier weder serialisiert noch aus eigenem Wire-Text zurückgelesen. Auch die Leiter ruft nur `quantisiere16` auf Testzahlen auf, statt die Auflösung erzeugter Snapshots zu messen. Damit fehlt die zugesagte C++-Wirehälfte der gekoppelten Kette; frühere Schemaprüfungen ersetzen diesen Zahlenvergleich nicht. Lesende Reproduktion: `rg -n 'perzentilDb|quantisiere16' eq-copilot/plugin/tests/Sonde013EventWireTest.cpp`. Beide Erwartungen müssen am tatsächlichen Snapshot-Ausgang messen ([E4b](docs/beweise/NAK-182.md#L426-L431), [AGENTS.md:51–52](AGENTS.md#L51-L52)).

- [P2] Prüfe im Laufzeit-Sweep alle drei zugesagten Träger — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013StereoGoldenTest.cpp:1179-1183
  DEFEKT gegen N-18: Der Sweep speist zwar 300, 900 und 2000 Hz ein, wertet aber ausschließlich das 900-Hz-Band aus. Die [Matrixzeile](docs/beweise/NAK-182.md#L990) verlangt drei Trägerfrequenzen je Blockgröße; §6.4 begründet nur die Toleranz von 0,25 rad, nicht das Weglassen zweier Träger. Das beeinflusst auch den vorhandenen Rotbeweis: Ein Sample Versatz ergibt bei 2000 Hz 0,2618 rad und überschreitet diese Toleranz, während die ausgewerteten 900 Hz grün bleiben. Lesende Reproduktion: `rg -n 'phasenfehler' eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp`. Werte alle drei Träger innerhalb des Sweeps aus.

- [P2] Prüfe die ausbleibende Empfehlung auch im Stereo-Sweep — C:\Users\phili\Projekte\Nakama\eq-copilot\plugin\tests\Sonde013StereoGoldenTest.cpp:1201-1204
  DEFEKT gegen [N-19](docs/beweise/NAK-182.md#L991): Für unkorrelierte Kanäle verlangt die Matrix zusätzlich „keine Lag- oder Polaritätsempfehlung“. Das neue Prädikat prüft ausschließlich Basis, Korrelationsbit und Korrelation; die bereits erhobenen Kohärenz- und Phasenfelder beeinflussen das Ergebnis nicht. Eine fälschlich vorhandene Phase würde deshalb bei niedriger Korrelation als korrekte Antwort bestehen. Der bestehende Einzeltest bei 512 deckt diese Zusage für die übrigen Blockgrößen nicht ab. Lesende Reproduktion: `rg -n -A11 -F 'klasse ("sweep_uncorrelated_channels"' eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp`. Ergänze die fehlende Prüfung je Sweepgröße.

- [P2] Führe S-07 durch die tatsächliche Wahrheitsvalidierung — C:\Users\phili\Projekte\Nakama\tools\eq-copilot\pruefe_p4_korpus.py:500-504
  DEFEKT gegen [S-07](docs/beweise/NAK-182.md#L1237): Diese Erwartungen prüfen lediglich die Mitgliedschaft zweier Literale in einer lokalen Testmenge. Sie erzeugen keinen ungültigen Korpusfall und erreichen die tatsächliche Ablehnung in `main` nicht. Reproduktion: `python -B tools/eq-copilot/pruefe_p4_korpus.py --selbsttest` meldet beide S-07-Zeilen grün; der lesende Lauftrace bestätigt, dass der Validierungsweg unbetreten bleibt. A26 weist unbekannte Wahrheiten aktuell korrekt ab, aber der zugesagte Selbsttest misst diese Eigenschaft nicht. Prüfe den ungültigen Fall und sein gültiges Gegenteil über dieselbe Validierung wie A26.

- [P2] Hinterlege das vereinbarte Material in der gemeinsamen Fixture — C:\Users\phili\Projekte\Nakama\tools\eq-copilot\erzeuge_v3_fixtures.py:3002-3008
  DEFEKT gegen [E4b/Form B](docs/beweise/NAK-182.md#L416-L418): Die generierte Fixture enthält Pegel und Auflösung, aber keine deklarierte Signalfrequenz oder Materialdauer. B16 legt stattdessen Amplitude, 1000 Hz, 48000 Hz und die Laufbegrenzung lokal fest; damit ist das zugesagte Material nicht über die gemeinsame Datei gebunden. Lesende Reproduktion: `(Get-Content -Raw eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json | ConvertFrom-Json).PSObject.Properties.Name` sowie die Konstanten in B16:1629–1637. Ergänze die Materialzahlen im Erzeuger und verwende sie im Verbraucher, entsprechend der gemeinsamen Fixturebindung ([AGENTS.md:51–52](AGENTS.md#L51-L52)).