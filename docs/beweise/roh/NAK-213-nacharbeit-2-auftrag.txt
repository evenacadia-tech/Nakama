NAK-213 — Nacharbeit 2 (nach der Wiederprüfung 1). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in docs/beweise/NAK-213.md §5 Verlauf (Eintrag „Nacharbeit 2") ein. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors. tools/dirigent/pruefliste.md ist deine Arbeitsliste. Ticketgrenze und Regeln R1 bis R8 wie in den Aufträgen unter docs/beweise/roh/NAK-213-*.txt; die Matrix docs/beweise/NAK-213.md §2 bleibt die Spezifikation. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad.

LAGE: Die Wiederprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a07fff-7d05-7512-9cfd-088e11e041d1, Stand 91ad6f2; Urteil wörtlich in docs/beweise/roh/NAK-213-wiederpruefung-1-91ad6f2.txt) bestätigt die Befunde 1 und 2 der Erstprüfung als geschlossen (Produktcode) und nichts Gebrochenes. Zwei Prüfzusagen bleiben teilweise offen — beides Tests, kein Produktcode. Der Dirigent hat beide an der Quelle bestätigt. Du schließt genau diese zwei, sonst nichts.

DIE ZWEI BEFUNDE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT sonde014_verdrahtung.rs:2056–2059 (K-49, Rest von Erstprüfungsbefund 3). Prüfer wörtlich: „Im neuen Store-Aufbau wird kein `SessionPush` registriert; `Coordinator::mit_store` initialisiert ihn mit `None`, sodass `invalidierung_zustellen` keine Nachricht an den Abonnenten schreiben kann. Die hier erwartete offene Outbox-Schuld belegt nur Einreihung, nicht die in Auftrag Nr. 3 verlangte Zusage ‚persistiert und zugestellt'. Der Test muss zusätzlich den empfangenen `evidence_invalidate`-Payload mit den acht IDs und `messpunkt_wechsel` prüfen." Bestätigt: der Test liest die Outbox (:2047–2062), registriert aber keinen Abonnenten (`session_push_setzen`, mod.rs:331). REGEL (K-49): `kanalwechsel_ueber_den_heartbeat_befehl` registriert einen `SessionPush`-Abonnenten für die Sitzung (bestehendes Testmuster: `session_push_setzen` in sonde013_verdrahtung.rs oder store_crash_matrix.rs) und prüft den EMPFANGENEN `evidence_invalidate`-Payload: genau die acht Belege, Grund `messpunkt_wechsel`, Umfang `Ids` der Sonde; die Outbox-Prüfung bleibt zusätzlich. K-49 nennt danach „persistiert (event_ord) und zugestellt (Payload beim Abonnenten)" als das, was gemessen ist; Rotbeweis: ohne Zustellung (Abonnent registriert, Zustellung unterdrückt) fällt der Test.

2. DEFEKT sonde014_gegenbeispiele.rs:1179–1182 (K-44, Rest von Erstprüfungsbefund 4). Prüfer wörtlich: „Auch die neu ergänzte Lage 2 führt den Kanalwechsel über `Buehne::deskriptor` aus, das direkt `descriptor_setzen` aufruft; ein Heartbeat mit `runtime` wird nicht gesendet. Damit fehlt weiterhin die in K-44 und Befund 4 verlangte Prüferfolge über den produktiven Ingress. Die neuen Aussagen zu Cs Ausschluss und As Konkurrenzlage sind vorhanden, prüfen aber ausschließlich den Setterpfad. Beide Lagen müssen den Wechsel über den Heartbeat auslösen." Bestätigt: K-44 Ereignisspalte lautet „Heartbeat mit `runtime` (produktiver Ingress, E8), dann Evidenz"; `f4_kanalwechsel` (:1179–1182) ruft `b.deskriptor(…)` → `descriptor_setzen`. REGEL (K-44, E8): `f4_kanalwechsel` löst den Wechsel in beiden Lagen über einen echten Heartbeat mit `runtime`-Block (`host_mixer_index` 9) über den Befehlsweg aus (Muster `sonde012_sources_slice.rs::heartbeat_mit_runtime`:80 und K-49); die Bühne bekommt dafür einen Heartbeat-Helfer, `Buehne::deskriptor` bleibt für die Anlage der Ausgangslage erlaubt, nicht für den gemessenen Wechsel. Alle bestehenden Aussagen (12 Belege `messpunkt_wechsel`, Entfernung, Cs Ausschluss in Lage 2, As Konkurrenzlage je Lage) bleiben und laufen über diesen Pfad; Rotbeweis wie in K-49 (eine Fassung, die den Vergleich nur im Setter hat, lässt den Lauf rot werden).

BEINE: A4 (`cargo test --manifest-path broker/Cargo.toml`), darin A31 und `sonde014_verdrahtung`, grün; kein Produktcode, kein C++-Bein, kein Korpus, kein voller Kanon in dieser Runde. Rotbeweise unter docs/beweise/roh/NAK-213-rot-K-49-nacharbeit-2.txt und NAK-213-rot-K-44-heartbeat.txt.

MANIFEST: §5 Verlauf, append-only: Eintrag „Wiederprüfung 1 — NEEDS_WORK (08.09.2026, Stand 91ad6f2)" mit Thread, Modell, Effort, Rohdatei, den zwei geschlossenen und zwei offenen Befunden, darunter „Nacharbeit 2" mit Basis, geänderten Dateien, nachgezogenen Zeilen K-44 und K-49, gemessenen Beinen mit Zahlen, Rotbeweisen. Kopftabelle: „Phase" auf „Nacharbeit 2 nach Wiederprüfung 1 (NEEDS_WORK, zwei Restbefunde in Tests)", „Änderungssatz" um die Commits. Prüfstufe bleibt T1 PASS bis der Dirigent T2/T3 setzt.

SELBSTAUDIT vor dem Commit: keine Änderung an broker/src; die Tests messen, was K-44 und K-49 sagen, und die Zeilen sagen nicht mehr, als die Tests messen; Heartbeat-Sequenzen fortlaufend, Abonnent je Sitzung genau einmal registriert; `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-213.md` grün; Commits mit Pathspec (Betreff „NAK-213 Nacharbeit 2: …"), pull --rebase, push, STOPP. Die Wiederprüfung 2 setzt der Dirigent.
