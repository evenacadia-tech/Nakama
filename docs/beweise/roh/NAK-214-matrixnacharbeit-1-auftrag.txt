NAK-214 — Matrixnacharbeit 1 (Etappe 1, Runde 1). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (docs/beweise/NAK-214.md); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Kein Produktcode, kein Test, keine Fixture — diese Runde ändert ausschließlich die Verhaltensmatrix in docs/beweise/NAK-214.md. tools/dirigent/pruefliste.md ist deine Arbeitsliste. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks.

LAGE: Matrixprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a080a4-8e6d-7fe0-9fc6-53bfce5d210e, Stand d0f2f304) ergab NEEDS_WORK — Urteil wörtlich in docs/beweise/roh/NAK-214-matrixpruefung-1-d0f2f30.txt, Prüfauftrag in docs/beweise/roh/NAK-214-matrixpruefung-1-auftrag.txt. Der Dirigent hat alle acht Befunde an der Quelle bestätigt (HEAD d0f2f304): sieben sind DEFEKTE der Matrix, einer (Befund 2) ist zur Hälfte Defekt und zur Hälfte LÜCKE, für die der Dirigent unten die Regel entschieden hat. Du schließt genau diese acht, sonst nichts. Für jeden Befund gilt: die betroffenen Entscheide in §2.0 und Zeilen in §2.1–§2.9 werden so umgeschrieben, dass die Zusage am Code an HEAD und am gebauten Vertrag wahr ist; je Zeile weiter Test (Datei::Fall) und Rotbeweis oder ausdrückliche Kennzeichnung als Kontrollfall/Regressionswache. Zeilen-IDs V-01 bis V-44 bleiben stabil; neue Zeilen bekommen die nächste freie Nummer (ab V-45). Nichts, was der Prüfer nicht erhoben hat, kommt dazu. Die vier vom Dirigenten getragenen Lesarten (E2 Rangschwelle, E6 clear() beim Sitzungswechsel, E7 unknown_target, E9 offenes Experiment tauglich / unvergleichbar ist nicht unbekannt) bleiben stehen; die Befunde unten präzisieren sie, sie kippen sie nicht.

DIE ACHT BEFUNDE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT E3/V-14 — das Nullpaar. Prüfer: „Auch vollständig identische PRE/POST-Signale erfüllen das Prädikat: gültige Bänder, ausgerichtetes Delta null, `match_gain_db = 0` und `wirkung = Some("Kette wirkt breitbandig gleichmäßig")` (`prepost.rs:824–843, 883–884`). Hier existiert weder eine Spektral- noch eine Pegelveränderung. Trotzdem verlangt V-14 `wirkungsbeleg` und `effektkette_pre_post`, entgegen R2 und der ‚reproduzierbaren Veränderung' aus SONDE-014 M-17 bzw. Entwurf §36.1." Bestätigt: `relation_db` (:637–660) liefert für identische Signale exakt 0.0 je Frame, `mittlere_relation` (:772–790) markiert alle Bänder gültig, der Median (:826–836) ist `Some(0.0)`, `Kettenbefund::Stationaer` (:752–758) führt auf `beschreibe_wirkung` (:842–843), und die 1,0-dB-Spannweite (:883) ist ein Wortwahl-Literal, keine Klassenschwelle. E3s Begründung („trotzdem ein gemessener Pegeleffekt") trägt genau dann nicht, wenn auch `match_gain_db` null ist. REGEL R6 (Dirigent, 08.09.2026): Das Strukturprädikat aus E3 bekommt einen vierten Punkt, schwellenfrei und ohne geratene Zahl (M-31): mindestens ein gültiges Band des ausgerichteten Deltas ist von exakt 0.0 verschieden ODER `match_gain_db` ist von exakt 0.0 verschieden. Ein Paar, dessen gültige Bänder und dessen `match_gain_db` sämtlich exakt null sind, trägt keine gemessene Wirkung: `prepost_wirkungsbeleg = false`, Klasse 1, `next_test = PrePostPaarMessen` (E5 unverändert; NB-2 wächst um diesen Fall). Kein Textvergleich, keine dB-Schwelle, keine Bindung an das Befundband (NB-1 bleibt). V-14 wird geteilt: der Kontrollfall „flaches Spektrum, `match_gain_db ≠ 0`" bleibt Wirkungsbeleg (bestehende Zeile), das exakt-null-Paar wird eine neue Zeile mit Test und Rotbeweis (Rotbeweis: vierter Punkt entfernt → das Nullpaar trägt `wirkungsbeleg`). E3 und §2.7 M-17-Nachtrag werden entsprechend berichtigt; NaN bleibt wie bisher über „kein gültiges Band" (V-15) abgedeckt.

2. DEFEKT (Nachlauf) und LÜCKE (`unknown`) E9/V-39 — Taint nach dem Interventionsende. Prüfer: „Nach einem Interventionsende wird die zugehörige ID entfernt, während `tail_samples_offen` weiterhin positiv sein kann (`intervention.rs:230–236`). Der geplante Filter würde die Experimentpassage dann wieder zulassen, obwohl `Taintstand::erlaubt()` weiterhin falsch ist; dasselbe gilt bei `unknown` ohne aktive ID (`zustand.rs:194–197`). R5 verlangt ein nicht getaintetes Experiment, SONDE-013 M-58/M-61 verlangen Nachlaufende bzw. bestätigten Resync." Bestätigt: `intervention.rs:230–236` entfernt die Intervention und zieht `tail_samples_offen` hoch; `Taintstand` (`zustand.rs:169–192`) trägt außerhalb von `interventionen` keine `experiment_id` — nach dem `remove` ist nicht mehr ableitbar, welches Experiment den Nachlauf verursacht hat; `taint_intervalle_des_experiments_schliessen` (`intervention.rs:446–459`, gerufen aus `experiment_verdrahtung.rs:637–641`) entfernt nur Einträge und erzeugt keinen Nachlauf. REGEL R7 (Dirigent, 08.09.2026), Nachlaufhälfte: „getaintet" im Sinn von R5 umfasst den Nachlauf der Intervention dieses Experiments (M-58: „das Ende allein genügt nicht"). Weil die Zuordnung heute beim Interventionsende verloren geht, wird sie an genau dieser Stelle gerettet: der Taintstand merkt sich beim Ende einer Intervention mit `experiment_id` diese ID als „Nachlauf offen für Experiment" (internes Zustandsfeld am `Taintstand`, kein Leitungsfeld, kein Vertrag, kein Fassungsschritt); die Menge wird geleert, sobald `tail_samples_offen` auf 0 fällt oder `neutral_resync` den Taint leert (M-61). Der Passagenfilter aus E9 liest je Taintstand die offenen Interventionen UND diese Nachlaufmenge, über alle Taintstände des Stands. Damit werden `broker/src/coordinator/intervention.rs` (nur das Retten und Leeren der Zuordnung) und `broker/src/coordinator/zustand.rs` (nur das Feld am `Taintstand`) Ticketpfade; Kopftabelle „Ticketpfade"/„Nicht angefasst" und §2.9 Nr. 11 werden entsprechend präzisiert („keine Änderung am Taintmechanismus außer der Zuordnung Nachlauf→Experiment"). V-39 bekommt den Nachlauffall als eigene Zeile mit Test und Rotbeweis (Rotbeweis: nur offene Interventionen gelesen → die Passage wird während des Nachlaufs gewählt). `unknown`-Hälfte, Entscheid des Dirigenten zur Lücke: `unknown` ist sitzungsweit ohne Experimentbezug (Resync-Verlust) und kein Kriterium der Passagenwahl; das Material der Sitzung ist währenddessen über den bestehenden Taint gesperrt (M-61/M-62), die Passage bleibt ein Zeitrahmen. Eine neue Kontrollzeile misst: Taintstand `unknown = true` ohne offene Intervention und ohne Nachlauf → die Passagenwahl bleibt unverändert, Fenster dieser Sitzung liefern währenddessen keine Evidenz (bestehender Weg, nur genutzt).

3. DEFEKT E1/V-02, V-05–V-10 — die Rotbeweise der zwei Riegel. Prüfer: „Der beschriebene gemeinsame Rotbeweis kann nicht entstehen: Ohne Schlussriegel stoppen weiterhin die neue Zielvorbedingung und die bestehende Passagenvorbedingung (`proposal.rs:656`); umgekehrt bleibt V-02 beim alleinigen Entfernen der Zielvorbedingung am Schlussriegel hängen. Außerdem lässt sich `stop_if` nicht durch eine handgebaute Lage leeren, weil `proposal()` es selbst erzeugt (`proposal.rs:665, 732, 855`); Eingriffsgrenzen werden ebenfalls intern gebildet." Bestätigt, Herkunft der sechs Felder an HEAD: `target` von außen (`lage.ziel_instanz`, :719), `passage_id` von außen, aber von WN-04 :656 vorher gefangen, `listen_for` von außen (`befund.listen_for`, :731) — der einzige Teilfall, der den Schlussriegel über den Erzeuger erreicht —, `allowed_bounds` intern (`eingriff()`, :779–792, bei Eingriff immer drei `Some`), `stop_if` intern (:855 immer `GuardrailAbdeckung`), `revert` intern und konstant wahr (:132–136, :372). REGEL (Dirigent, 08.09.2026): EIN Riegel, nicht zwei. Die in E1 zusätzlich vorgesehene Zielvorbedingung „ohne Ziel kein Objekt" entfällt; R1 wird wörtlich durch den Schlussriegel `gate_felder_vollstaendig` am Ende von `proposal()` erfüllt, und der bestehende Rückgabewert `(feld, false)` ist der Grund. V-02 misst über den Schlussriegel (Rotbeweis: Schlussriegel entfernt → ein Proposal mit `target: ""` erscheint in `vorschlaege_sicht` und im Store); V-05 (`target`) ist damit über den Erzeuger erreichbar. Die Sechserprüfung wird an zwei Stellen gemessen und die Matrix sagt je Feld ehrlich, wo: (a) `gate_felder_vollstaendig` als Funktion am `Proposal`-Objekt, sechs Teilfälle direkt am Objekt gebaut (Rotbeweis: Funktion liefert konstant wahr → alle sechs Teilfälle rot); (b) der produktive Aufruf über den Erzeuger mit den zwei erreichbaren Feldern `target` (V-05) und `listen_for` (V-08), Rotbeweis: Aufruf entfernt → Objekt entsteht. V-06 (`passage_id`): WN-04-Vorbedingung bestehend, Teilfall nur am Objekt. V-07, V-09, V-10: über den Erzeuger strukturell nicht erreichbar — gemessen nur an der Funktion (a); ihre Zeilen sagen das statt „die Lage wird von Hand gebaut". E1, §2.7 M-46-Nachtrag („das None bei fehlendem Ziel") und die Aufrufkettentabelle werden auf den einen Riegel umgeschrieben.

4. DEFEKT V-12 — der Kontrollfall ist nicht herstellbar. Prüfer: „Innerhalb einer Sitzung kann nicht genau einem Befund das Proposal-Ziel fehlen. `vorschlaege_bilden` ermittelt eine gemeinsame `Proposallage` und übergibt sie jedem Befund (`proposal_verdrahtung.rs:38–60`); jedes Proposal übernimmt dieselbe `lage.ziel_instanz` (`proposal.rs:719`)." Bestätigt (:45 eine Lage je Sitzung, :58–61 dieselbe Lage in `filter_map`). REGEL: V-12 wird auf ein befundeigenes Gate-Feld umgestellt: Sitzung mit mehreren Befunden, genau einer mit leerem `listen_for` (von Hand gebaut, am Produktpfad nicht erreichbar, siehe Befund 3) → die übrigen Vorschläge entstehen, der eine nicht, `paare` bleibt paarweise ausgerichtet. Kontrollfall ohne eigenen Rotbeweis, wie bisher gekennzeichnet.

5. DEFEKT E4/V-17 — der tragende Rotbeweis wäre ein No-Op. Prüfer: „E4 erhält `prepost_paar` ausdrücklich mit seiner bisherigen Bedeutung `ergebnis.is_some()` (`hypothese_verdrahtung.rs:243–253`). Genau diese Zuweisung ‚wieder' herzustellen verändert nach dem vorgesehenen Bau nichts; Klassenwahl und `next_test` lesen dann ausschließlich `prepost_wirkungsbeleg`. Der Rotbeweis bliebe grün." Bestätigt. REGEL: Der Rotbeweis V-17 setzt `prepost_wirkungsbeleg` aus `ergebnis.is_some()` statt aus dem Strukturprädikat (E3/R6) — dann trägt der `Probable`-Befund `wirkungsbeleg`. Die Zeile nennt das so; `prepost_paar` bleibt unberührt (E4).

6. DEFEKT E6/V-23, V-24 — der Befundzähler. Prüfer: „Für die neuen lokalen Zustandswechsel fehlt die gekoppelte Aktualisierung von `findingsOffen`. Der Zähler wird nur beim Snapshot neu berechnet und schließt `stale` aus (`SourcesModel.cpp:1260, 1588–1605`). Nach dem bloßen Zustandswechsel bleiben z. B. zwei offene Befunde gezählt; `PluginEditor.cpp:1318–1321` zeigt weiterhin ‚Findings: 2 open'." Bestätigt: `zaehleOffeneFindings()` hat genau einen Aufrufer (:1260), überspringt `stale` (:1599), und `beginneSubscription` (:456–463) hält die Kopplung heute selbst als Regel fest (M-84: „Die abgeleitete Zahl faellt MIT ihrer Quelle"). REGEL R8 (Dirigent, 08.09.2026): Jeder lokale Übergang auf `stale` (`controlEnde`, Rücknahme ohne Folge-Snapshot) rechnet `findingsOffen` über `zaehleOffeneFindings()` neu; unmittelbar danach ist `findingsOffen == 0`. V-23 und V-24 messen den Zähler mit (Rotbeweis: Neuberechnung entfernt → der Zähler bleibt bei 2, die Anzeige behauptet offene Arbeit); §2.7 bekommt eine Zeile M-84 („unverändert und angewendet").

7. DEFEKT E6/V-25, V-27 — Reconnect derselben Sitzung. Prüfer: „Auch beim Reconnect derselben Sitzung ruft der Produktpfad `beginneSubscription` auf (`PluginProcessor.cpp:2408–2410`). Diese Funktion leert `befunde` unbedingt, ohne Epochenvergleich (`SourcesModel.cpp:439–455`). Bleibt der Snapshot aus, existiert eine leere Liste statt der zugesagten erhaltenen stale-Befunde." Bestätigt: `v3ControlLink(true)` ruft `beginneSubscription` bei jedem positiven Link-Callback (:2406–2410, einzige Bedingung `pluginKind == "main"` und gültige Adresse), die Epoche kommt unverändert aus `v3SessionEpoch`, ein Epochenvergleich existiert weder davor noch in der Funktion. REGEL: Der Produktpfad bleibt, wie er ist — kein Epochenvergleich wird hinzugefügt. E6 und die Zeilen sagen ehrlich, was gilt: zwischen Verbindungsende und Reconnect sind die Befunde `stale` und sichtbar (V-23); jeder Reconnect ruft `beginneSubscription` und leert `befunde`, `experimente`, `paare`, `findingsOffen` — bei neuer wie bei gleicher `session_epoch` (V-25 wird auf beide Fälle erweitert); ohne Folgesnapshot bleibt die Liste leer und `findingsOffen` 0, nichts ist handelbar (V-27 wird so umgeschrieben und misst den Produktpfad: `beginneSubscription` mit derselben Epoche, kein Snapshot); mit Folgesnapshot füllt sich die Liste vom Broker (V-26 unverändert). Der E6-Satz „Die Liste bleibt stehen — sie war zuletzt wahr" gilt nur bis zum Reconnect und wird so eingegrenzt.

8. DEFEKT V-44 — Terminal schließt `experiment_abort` ein. Prüfer: „‚Terminal' umfasst auch `experiment_abort`: Dieser setzt `Terminal::Abgebrochen` (`experiment.rs:1210–1223`) und schließt die zugehörigen Taintintervalle (`experiment_verdrahtung.rs:637–641`). Genau dieser Zustand muss nach R5, E9 und V-38 ausgeschlossen bleiben, während V-44 seine Passage wieder freigibt." Bestätigt (`experiment_verdrahtung.rs:338–339`: jede Art außer `experiment_begin` schließt den Taint des Experiments). REGEL: V-44 wird nach Terminalart geteilt: (a) `neutral_resync` auf einem offenen Versuch oder Ablauf des Nachlaufs (R7) → die Passage ist wieder wählbar, sofern die übrigen Eignungsbedingungen weiter gelten; (b) `Terminal::Ergebnis` mit bekannter Vergleichbarkeit → wählbar (V-41), mit `vergleichbarkeit == None` → nicht (V-40); (c) `Terminal::Abgebrochen` → nie wieder wählbar (V-38, R5 „nicht zurückgenommen"). Die Zeile nennt je Fall den Test; (c) ist Kontrollfall zu V-38.

VERLAUF: Trage im Manifest §5 (Verlauf, append-only) einen Eintrag „Matrixprüfung 1 — NEEDS_WORK (08.09.2026, Stand d0f2f304)" mit Thread, Modell, Effort, Rohdatei und der Zuordnung Defekt/Lücke der acht Befunde ein, und darunter „Matrixnacharbeit 1" mit der Liste der geänderten Entscheide und Zeilen. Kopftabelle: Zeile „Phase" auf „Etappe 1 — Matrixnacharbeit 1 nach Matrixprüfung 1 (NEEDS_WORK)"; Regeln-Zeile um R6, R7, R8 (Quelle: diese Auftragsdatei docs/beweise/roh/NAK-214-matrixnacharbeit-1-auftrag.txt); Ticketpfade um `intervention.rs` und `zustand.rs` (nur R7-Zuordnung). Der lebende Kopf und §1–§2 werden umgeschrieben, §5 nur angehängt. §2.8 bleibt „kein Fassungsschritt" — prüfe und bestätige, dass R6 bis R8 kein Leitungsfeld berühren.

SELBSTAUDIT vor dem Commit: Jede geänderte Zeile gegen den Code an HEAD lesen (Zahlenränder: exakt 0.0 gegen 1e-9, NaN-Band neben Nullband, Nachlauf 0 gegen 1 Sample, `unknown` mit und ohne Nachlauf, zwei Befunde mit einem leeren `listen_for`, Zähler 0/1/2, Reconnect mit gleicher gegen neue Epoche, drei Terminalarten); keine Zusage ohne Test und Rotbeweis oder ehrliche Kennzeichnung; keine Manifestsätze, die mehr behaupten als der genannte Test misst; keine Widersprüche zwischen zwei Zeilen (V-02↔V-05, V-25↔V-27, V-38↔V-44 waren genau das). Dann `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-214.md` (Tabellen- und Verweisriegel) grün, Commit mit Pathspec docs/beweise/NAK-214.md, Betreff „NAK-214 Matrixnacharbeit 1: …", pull --rebase, push, STOPP. Die Matrixprüfung 2 setzt der Dirigent.
