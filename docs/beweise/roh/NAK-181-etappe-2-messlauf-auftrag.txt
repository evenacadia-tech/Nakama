NAK-181 Etappe 2 — Messlauf. Zusatzauftrag des Dirigenten (2026-09-05).

Du bist der Bauer dieses Tickets (Session-Fortsetzung). Etappe 2 ist gebaut (Kanon GRÜN 53/53 auf 8b01a1f), aber die Messbilanz §6.4 übergibt 39 von 46 Matrixzeilen — der Etappe-2-Auftrag verlangte „keine Zeile gebaut, nicht gemessen". Eine Matrixzeile ist eine Zusage mit messendem Test; „Wache auf Bestand" ist genau das, was die Zeile verlangt (N-11 sagt wörtlich „erstmals gemessen"), und ein Test auf unverändertem Code ist der Beleg, dass die Zusage heute hält. Präzedenz: NAK-180 Messlauf (docs/beweise/NAK-180.md §6.3/§6.6) — dort fand der erzwungene Messlauf fünf Defekte im eigenen Bau. Basis = `git rev-parse HEAD` beim Start (e78c816). Git-Regeln wie bisher (Pathspec, kein `git add -A`, kein `--amend`, `git pull --rebase` bei sauberem Worktree, Push). Lösch-Cmdlet nie verwenden; für selbst angelegte Pfade [System.IO.File]::Delete.

ZU MESSEN — jede Zeile mit dem Test, den die Matrix nennt (Datei::Fall), Ergebnis und Rohdatei; Rotbeweis dort, wo die Matrix einen nennt:

1. N-11 — `broker/tests/sonde013_experiment.rs::fremdes_projekt_erreicht_den_versuch_nicht`: Versuch offen unter Projektbindung A; `experiment_candidate` derselben `experiment_id` mit Zieladresse der Projektbindung B → `abgelehnt` mit Grund `revision_conflict`; der Versuch bleibt offen unter A mit Baseline, Match-Gain und gebundener Reihenfolge (Riegel `experiment_verdrahtung.rs:74-93`, erstmals gemessen).
2. N-25 — `broker/tests/sonde013_verdrahtung.rs::bandsatz_ohne_bit_ist_fail_closed`: ein Bandsatz mit gelöschtem Bit an Position i liest sich als `(0.0, false)` — nie als Wert des Vorgängerbands, nie als gemessene 0; über den gemeinsamen Dekodierweg (`evidenz.rs`), für einen Bandsatz, den der Broker heute konsumiert.
3. N-34 — `sonde013_verdrahtung.rs::drop_invalidiert_als_sequenzluecke`: zwei aufeinanderfolgende Evidenzsnapshots derselben Quelle mit den Stempeln aus N-31 (Drop: `continuity_segment + 1`) → `invalidierung_aus_transportbruch` meldet `Sequenzluecke`; mit den Stempeln aus N-32 (Seek: `transport_epoch + 1`) → `Epochwechsel`; ein Drop trägt nie `Epochwechsel`. Die Stempel kommen aus derselben Zahlenlage, die der Plugin-Test N-31/N-32 gemessen hat (Werte aus der Rohdatei übernehmen, nicht erfinden).
4. N-41 — `sonde013_experiment.rs::retry_an_fremde_sitzung_wird_abgewiesen`: nicht persistierter Retry mit alter Zieladresse nach Reconnect → `abgelehnt`/`unknown_target` (`befehl.rs:210-217`, `:292-293`), Store-Zählung vor und nach dem Retry gleich, kein Experimentzustand.
5. N-41b — `sonde013_experiment.rs::<Fall für den persistierten Retry>`: Befehl persistiert, `command_ack` verloren, derselbe Text erneut → `idempotent_wiederholt` (`bekannter_befehl`, `befehl.rs:66-89`), Store-Zählung vor und nach dem Retry gleich, Versuch unverändert unter der alten Bindung.
6. N-42 — Kanal- oder Tap-Wechsel bei gleicher `startFolge` im echten `SondeProcessor` (Plugin-Hälfte in `EqCopSonde012HostChannelContextTest`, wie N-31 bis N-33): der nächste Snapshot trägt `continuity_segment + 1` (Weg `schliesstAn` verwirft → `grenzeZwischen` `lokaleLuecke`); Broker-Hälfte in `sonde013_verdrahtung.rs`: `Sequenzluecke`. Kein Umbau von `grenzeZwischen` (R7a).
7. N-18, Rust- und A5-Hälfte — die Matrix nennt `broker/tests/contract_cross_language.rs::wirezahl_texte_halten_den_textriegel` und A5 über dieselbe Liste; gemessen ist bisher nur B3c (C++). Vehikel ist die in §4.2 vorgesehene Byteinstanz `eq-copilot/fixtures/v3/evidenz-zahlen-wire-v1.json` aus dem Erzeuger `tools/eq-copilot/erzeuge_v3_fixtures.py` (Zahlklassen aus §4.2: ganzzahlig, ein Zehntel, ein Drittel gedeckelt, `-0`, kleinster normaler `float`, `1e-307`, Nyquist-gekappter Bandwert, Ersatzform für verweigerte Klassen): A8 `--pruefen` hält sie bytegleich, A5 nimmt sie an, der Rust-Test liest sie durch `textriegel_bytes` und `serde_json` und vergleicht die Zahlwerte mit den erwarteten `f64`; der C++-Test B3c prüft `evidenceSnapshotAlsJson` bytegleich gegen dieselbe Fixture (N-16 Fixturehälfte). Damit ist der Verweis in §4.2 kein Zielpfad mehr, sondern eine Datei.

DANACH: die betroffenen Beine fahren (`cargo test` für `sonde013_experiment`, `sonde013_verdrahtung`, `contract_cross_language`; `EqCopSonde012HostChannelContextTest`; `EqCopSchemaTest`; A5 `pruefe_v3_vertrag.py`; A8 `erzeuge_v3_fixtures.py --pruefen`), Rohausgaben nach docs/beweise/roh/NAK-181-<sha>.txt; dann der volle Kanon abgekoppelt auf dem sauberen Endstand (Befehl mit File.Delete-Ersatzweg aus docs/beweise/roh/NAK-181-etappe-2-zusatz-kanon.txt; warten bis EXIT=; kein NOT RUN). Manifest: §6.4 auf 46/46 (oder ehrliche Zahl mit Grund je Zeile), neuer Abschnitt „### 6.9 Messlauf (2026-09-05, Worker, Basis e78c816)" mit Tabelle Zeile | Test | Ergebnis | Rohdatei und den dabei gefundenen Defekten im eigenen Bau (jeder wird im selben Lauf behoben und mit Rotbeweis belegt); Kopfzeile Phase: „Etappe 2 gebaut und gemessen 46/46, Kanon <GRÜN n/n> auf <sha>, Erstprüfung offen". Commits mit Pathspec (Tests, Fixtures, Erzeuger, docs/beweise/NAK-181.md, docs/beweise/roh/NAK-181-*), Push. Kein Anfassen von docs/beweise/G4.md, NAK-180.md, offene-punkte.md, PLAN-STAND.md, docs/plan/, eq-copilot/schemas/. Turn mit der letzten Zeile „MESSLAUF ABGESCHLOSSEN <end-sha>" beenden. Keine Nachfrage an den User.
