NAK-213 — Nacharbeit 1 (nach der Erstprüfung). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in docs/beweise/NAK-213.md §5 Verlauf (Eintrag „Nacharbeit 1") ein. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor dem Commit abhaken und im Manifest nennen, wo jede Zeile gemessen ist. Ticketgrenze und Regeln R1 bis R8 wie in docs/beweise/roh/NAK-213-auftrag.txt, NAK-213-etappe-2-auftrag.txt und den beiden Matrixnacharbeits-Aufträgen; die Matrix docs/beweise/NAK-213.md §2 bleibt die Spezifikation.

LAGE: Die Erstprüfung (Codex gpt-6-astra max, lesend, Thread 01a07fb5-4029-7602-a198-e1a0d3561661, Stand 0491c6a; Urteil wörtlich in docs/beweise/roh/NAK-213-erstpruefung-1-0491c6a.txt, Auftrag in NAK-213-erstpruefung-auftrag.txt) ergab NEEDS_WORK mit vier Befunden. Der Dirigent hat alle vier an der Quelle bestätigt: zwei Produktdefekte (Broker) und zwei nicht wie zugesagt gemessene Matrixzeilen (Tests). Du schließt genau diese vier, sonst nichts. Ursachen, nicht Punktkorrekturen — wo zwei Befunde dieselbe Ursache haben, ist die Ursache der Auftrag.

DIE VIER BEFUNDE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT hypothese_verdrahtung.rs:282–284 — Main-Belege verschwinden ohne eindeutige Führung. Prüfer wörtlich: „Vollständigkeitsmarke setzen, Main A gültige Fenster liefern lassen, anschließend ein zweites Main derselben Sitzung automatisch beitreten lassen und A erneut messen lassen; Sonden liefern noch nichts. Bei `fuehrendes_main = None` werden hier sämtliche Main-Profile verworfen. `enthaltung_ohne_ort` sammelt anschließend nur Kandidaten- und Masterfenster und liefert trotz gültiger Sitzungsbelege `None`: Die Sitzung schweigt." Bestätigt: `aufnahmen_sammeln` legt ein Main-Profil nur ab, wenn es das führende ist (:282–284); `enthaltung_ohne_ort` (hypothese.rs:1916–1925) liest nur `kandidaten` und `master`. REGEL (E7, R4, R8): Die Aufnahme trägt die Profile ALLER Main-Clients der Sitzung (führend oder nicht), und die Enthaltung ohne Ort sammelt ihre `evidence_ids` über alle Quellen der Sitzung einschließlich aller Mains; nur ohne jeden gültigen Beleg der Sitzung bleibt es bei R8 (kein Befund). Eine Zeile misst genau die Eingabefolge des Prüfers (Main A mit Belegen, zweites Main tritt bei, Sonden stumm → Enthaltung ohne Ort mit `KeineEindeutigeFuehrung` und As Belegen als `evidence_ids`, kein Schweigen) mit Rotbeweis; K-31/K-35/K-38 bleiben grün.

2. DEFEKT liveness.rs:513–518 — Kanalwechsel bei verweigertem Store-Append inkonsistent. Prüfer wörtlich: „Main und Sonde liefern zwölf gedeckte, ausgerichtete Fenster mit positivem Zusammenhang, die Sonde zunächst ohne Mixerindex; danach Store stoppen und einen Heartbeat mit Kanal 9 senden. Der Deskriptor ist bereits ersetzt, wenn `invalidierung_anwenden` scheitert. Dessen Fehlerzweig nimmt die Evidenzausschlüsse zurück und berechnet sofort neue Befunde: Die alten Fenster zählen nun unter Kanal 9, bis hin zu `hoch`. Das anschließende Setzen von `link.trennen` verhindert diese Neuberechnung nicht." Bestätigt: der Deskriptorersatz (:431) liegt vor der Invalidierung (:513–519); bei `Err` läuft der B16-Fehlerzweig (Ausschlüsse zurück, Neuberechnung) mit dem bereits ersetzten Kanal. REGEL (R5, E8 — Rückweg): Ein Kanalwechsel wird erst wirksam, wenn seine Invalidierung angenommen ist. Scheitert der Append, wird der Deskriptorersatz für den Kanal zurückgenommen (der alte `host_mixer_index` steht wieder im Deskriptor) BEVOR der Fehlerzweig neu rechnet, und der Link gilt wie bisher als storeverweigert; kein Fenster von vor dem Wechsel zählt je unter dem neuen Kanal. Der Positionswechsel (`measurement_position`, SONDE-013 R24) bleibt unverändert (§2.9 Nr. 4); ist er von derselben Reihenfolge betroffen, als Nebenbefund nummerieren, nicht beheben. Eine Zeile misst die Eingabefolge des Prüfers mit einem verweigernden Store (Muster der bestehenden B16-Tests) und den Rotbeweis (ohne Rückweg erscheint ein Befund mit den alten Fenstern unter Kanal 9).

3. DEFEKT sonde014_verdrahtung.rs:1805–1808 — K-49 läuft ohne Store. Prüfer wörtlich: „`coordinator()` erzeugt `Coordinator::mit_uhr` ohne Store. Dieser Test erreicht deshalb ausschließlich den `Ok(None)`-Zweig der Invalidierung, nicht deren Persistierung und Zustellung über den Store. K-49 verlangt ausdrücklich ‚Der Broker läuft mit Store'." Bestätigt (`coordinator()` :64–66; K-49 wörtlich „Der Broker läuft mit Store"). REGEL: `kanalwechsel_ueber_den_heartbeat_befehl` läuft mit Store (bestehendes Muster eines Store-gebundenen Coordinators in den Tests) und bestätigt die Invalidierung der acht Belege mit `messpunkt_wechsel` über den Store-Weg (persistiert und zugestellt), zusätzlich zum Zähler; die Matrixzeile K-49 nennt danach genau das, was gemessen ist.

4. DEFEKT sonde014_gegenbeispiele.rs:1231–1235 — K-44 Lage 2 fehlt. Prüfer wörtlich: „Die Kanalwechselfolge sendet anschließend immer auch ein neues Fenster von C. Der ausdrücklich verlangte zweite Ablauf fehlt: C wechselt per Heartbeat von Kanal 7 auf 9, danach messen nur Main und A weiter. Die Prüfung `stark(&nachher) == 0` ist bereits wegen der konstanten Pegel durch NAK-212 R1 erfüllt und belegt die zusätzliche Konkurrenzregel nicht." Bestätigt (K-44 verlangt „die zweite Lage: derselbe Aufbau, aber C misst nach dem Wechsel nicht mehr" und „der Test misst welchen [Grund]"). REGEL (K-44, K-14): Der Test fährt beide Lagen. In Lage 2 ist C nach dem Wechsel ohne gültiges Fenster: der führende Befund trägt C als `(…, evidenz_zurueckgenommen)` sichtbar, A trägt höchstens `mittel` mit `Konkurrenzlage::Messausschluss` als gemessenem Grund (nicht nur `stark == 0`); in Lage 1 wird As Grund ebenso gemessen (NAK-212 R1, `uplift = None`). Die bedingte Prüfung `if let Some(f) = c_befund` wird zu einer Aussage, die misst (in Lage 1: C trägt höchstens `mittel`; in Lage 2: C trägt keinen eigenen Befund).

BEINE: nach jedem Schritt A4 (`cargo test --manifest-path broker/Cargo.toml`), A31 und die Modultests der berührten Dateien grün; A30 (Korpus) falls die Kette berührt ist. Kein C++-Bein nötig, sofern eq-copilot/ unberührt bleibt (Vertrag und Fixtures ändern sich nicht). Der volle Kanon läuft beim Abschluss des Tickets, nicht in dieser Runde. Rotbeweise je neuer oder geänderter Zeile unter docs/beweise/roh/NAK-213-rot-<zeile>.txt (neue Zeilen ab K-54).

MANIFEST: §5 Verlauf, append-only: Eintrag „Erstprüfung — NEEDS_WORK (08.09.2026, Stand 0491c6a)" mit Thread, Modell, Effort, Rohdatei und den vier Befunden, darunter „Nacharbeit 1" mit Basis, geänderten Dateien, neuen/geänderten Matrixzeilen (§2 nachziehen, Zeilen-IDs stabil), gemessenen Beinen mit Zahlen, Rotbeweisen. Kopftabelle: „Phase" auf „Nacharbeit 1 nach Erstprüfung (NEEDS_WORK, vier Befunde)", „Änderungssatz" um die Nacharbeits-Commits. Prüfstufe bleibt T1 PASS bis der Dirigent T2/T3 setzt.

SELBSTAUDIT vor jedem Commit: Diff adversarial lesen — Zahlenränder (zwei Mains gleichzeitig, kein Main, Main ohne Beleg, leere `evidence_ids`, Store verweigert beim ersten und beim wiederholten Heartbeat, Kanal unverändert), Determinismus M-25, keine irreführenden Kommentare oder Manifestsätze, Beziehungen: Deskriptorersatz ↔ Rücknahme, Führung ↔ Belegsammlung, Store ↔ Zustellung. Dann `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-213.md` grün, Commits mit Pathspec (Betreff „NAK-213 Nacharbeit 1: …"), pull --rebase, push, STOPP. Die Wiederprüfung setzt der Dirigent.
