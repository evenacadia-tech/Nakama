Wiederprüfung NAK-180 / G4-Nacharbeit 1, Matrixprüfung 2 (Runde 1 der Etappe 1) — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff 3684f76840b704e9f222c201a51b451a5316acc6...f364d5cd96d5a11de9690271a359d10527f4ded3 -- docs/beweise/NAK-180.md`
(ZIEL = f364d5cd96d5a11de9690271a359d10527f4ded3 = HEAD, vor und nach deinem Lauf identisch). Der übrige Matrixbereich wurde in Matrixprüfung 1 geprüft und ist hier nicht Prüfgegenstand. Es gibt weiterhin keinen Produktcode in diesem Ticket; geprüft wird die Spezifikation (docs/beweise/NAK-180.md §2 bis §4, §5.1).

BEFUNDE, die diese Runde schließen sollte (wörtlich aus Matrixprüfung 1, docs/beweise/roh/NAK-180-matrixpruefung-1-b05dc32.txt; Regeln des Dirigenten in docs/beweise/NAK-180.md §5, Tabelle MP1-1 bis MP2-4):
1. [P1] „Binde eingereihte P0-Nachrichten an die Linkgeneration" — `sendeP0` reiht nur ein (`ControlClient.cpp:799-832`); ein gescheiterter Wire-Write legt dieselbe Nachricht in die verbindungsübergreifende P0-Queue zurück (`:1454-1463`); das Aufbau-Flag ist dann verbraucht; auf dem nächsten Link kann ein altes `false` den R1-Zweig auslösen oder ein altes Begin mit dem Replay dieselbe ID doppeln (R2/N-23, M-39). Regel R7: „gesendet" heißt Wire-Commit; Heartbeats werden nie zurückgelegt; Aufbau-Flags gelten erst mit dem Wire-Commit als verbraucht; Berichtsnachrichten alter Generation werden beim Linkende aus der P0-Queue entfernt; Begin/End bleiben und reisen vor dem Replay; Replay nur für Begins, die weder auf dem Draht noch in der Queue sind; Test mit Abbruch zwischen Enqueue und Wire-Commit.
2. [P1] „Decke ein abgewiesenes Marker-Begin im R2-Weg ab" — N-05 setzte ein gesendetes Begin voraus; bei `sendeP0 == false` bleibt nur das Sticky-Bit (`PluginProcessor.cpp:1077-1087`), kein offenes gesendetes Begin, kein Replay, das End trifft auf kein Begin (`intervention.rs:214-219`), der reguläre Pfad nullt nicht. Regel R8: Replay gilt jedem lokal offenen Begin; ein End reist nur nach einem auf demselben Link zugestellten Begin; eigene Zeile mit Rotbeweis.
3. [P1] „Sperre Evidenz bereits zwischen Hello und erstem Heartbeat" — `control_hello_registrieren` legt Link und Sitzung ohne Taint an (`link.rs:238-278`), `evidence_dispatch_locked` behandelt eine Sitzung ohne Eintrag als erlaubt (`intervention.rs:577-586`); N-07 injizierte erst nach dem Heartbeat. Regel R9: Main-Hello setzt `taint.unknown = true`; der erste Heartbeat löst mit `false` oder bestätigt mit `true`; Sonden-Hellos ändern den Taint nicht; Rust-Test mit Sondenevidenz zwischen Hello und erstem Heartbeat.
4. [P2] „Vergleiche den vollständigen Heartbeat bytegleich" — §4.2 beschränkte den Bytevergleich auf ein Feldsegment; R3a verlangt den vollständigen `heartbeatAlsJson`-Text bytegleich zur Fixture (ControlStatus, sequence, Zähler, Runtime auf Fixturewerte); §4.2 berichtigen, keine Abweichung in §4.4.

GATE-BEZUG, wörtlich (docs/FL-Nakama-Sonden-Design-Entwurf.md:4289): „Hörmarker- und Experimentende schließen alle Taintintervalle; der reguläre Pfad nullt danach wieder." Dazu Entwurf §34.2 (Resync als ausdrückliche, bestätigte Lebenszyklusoperation), SONDE-013-Matrix M-33 bis M-39 und M-58 bis M-63 (docs/beweise/SONDE-013.md:460-653), Invarianten aus CLAUDE.md (Audio-Thread ohne Lock/Allokation, Nulltest bitidentisch, NaN-Ehrlichkeit, Schemas eingefroren).

Du beantwortest genau zwei Fragen:
1. Ist jeder der vier Befunde am Zielstand in der Matrix geschlossen (je Befund: ja/nein, mit Zeilen-IDs N-xx/E-x, messendem Test und Rotbeweis als Beleg)? Prüfe dabei an der Quelle (ControlClient.cpp, PluginProcessor.cpp, link.rs, befehl.rs, intervention.rs), ob die neuen Zeilen mit dem gemessenen Code zusammenpassen — insbesondere: der Aufbaufilter für Berichtsnachrichten alter `wireGeneration` (N-26) gegen die heutige P0-Queue (`IpcQueues.h`, `p0.zuruecklegen`), die Reihenfolge Queue-Rest → Replay (N-27) gegen doppelte `intervention_id` beim Broker, das End-ohne-Begin-Vorziehen (N-29) gegen `intervention.rs:214-219`, und ob R9 (N-30 bis N-32) den Same-Broker-Fall (`control_ende` hat `unknown` gesetzt, neues Main-Hello, erster Heartbeat `false` löst) und den Fall „Main-Hello ohne jeden Heartbeat (Link stirbt vorher)" fail-closed hält.
2. Hat der Fixdiff etwas gebrochen — eine ältere Matrixzeile (N-01 bis N-24), einen Entscheid E1 bis E5, eine Zeile der SONDE-013-Matrix, einen Satz des Gate-Bezugs, eine Invariante, einen Vertrag (keine neuen Wire-Felder)?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1 (DEFEKT / LÜCKE / HÄRTUNG); ausgeschlossen: docs/** außerhalb von NAK-180.md §2–§4/§5.1, Registerpunkte NAK-155 bis NAK-190, tools/dirigent/pruefliste.md als Anforderungsquelle, Ticketfremdes (NAK-181/182, `IpcVerbindung::oeffnen`-Fristlogik, G6/G7-Punkte NAK-183 bis NAK-185), Kosmetik.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — alle vier Befunde geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID, Quelle Datei:Zeile, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
