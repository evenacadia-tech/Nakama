URTEIL: PASS

## HEAD und Arbeitsbaum

| | HEAD Nakama | `git status --short` Nakama |
|---|---|---|
| vor dem Lauf | `7d3794aac773147c1d5a2ffd18c4e74b37d82d6d` | `?? briefing-hub/`, `?? nimbalyst-local/` |
| nach dem Lauf | `7d3794aac773147c1d5a2ffd18c4e74b37d82d6d` | `?? briefing-hub/`, `?? nimbalyst-local/` |

HEAD identisch, keine neue Änderung. Nachbarrepo `C:\Users\phili\Projekte\fl-studio-mcp`: HEAD `15b3b1a133ad37aa75e63e2d98a7aac0b74a62ae`, `git status --short` leer (vor und nach dem Lauf gemessen). In keinem der beiden Repos habe ich geschrieben, gebaut, ein Bein, einen Selbsttest oder ein Skript gefahren, FL berührt oder eine Pipe geöffnet. Zwei `git diff`-Ausgaben habe ich zum Lesen nach `%TEMP%` geschrieben (`nak309-laufzeit.diff`, `nak309-szenario.diff`) — außerhalb beider Repos, nichts im Arbeitsbaum.

Selbst nachgemessen: `git diff --stat 48fc736a..252f8c73 -- eq-copilot broker tools` ist **leer** — der Code am ZIEL ist derselbe, an dem die Rotbeweise und die Einzelbeine liefen. `git diff --stat 7a51070…252f8c73 -- eq-copilot broker tools` enthält außer den 18 Auftragspfaden nur `tools/fl/LIES-MICH.md` (im Auftrag als Prosa ausgenommen) — kein Code- oder Werkzeugpfad außerhalb des Prüfbereichs.

---

## (a) T3-13-01 / T3-13-04 (R-309-5, R-309-3)

**Ohne Argument der Probename.** `eq-copilot/plugin/src/ProbePipeRegel.h:59` (`probePipeName`) gibt ohne `argv[1]` `kV2ProbePipeName` (`:37`, `\\.\pipe\evenacadia.eq-copilot.m2probe`) zurück und nennt `kPipeName` nie; `eq-copilot/plugin/probe/PipeProbeMain.cpp:131` ruft genau diese Funktion. Gemessen von A4b `probe_pipe_ohne_argument_waehlt_probe_namen` (`eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp:1522-1541`), inkl. Gegenprobe „mit Argument gilt das Argument, leer bleibt leer".

**Ablehnung vor dem ersten Client.** `PipeProbeMain.cpp:131-138`: Name → Urteil → `if (urteil != zugelassen)` → Meldung → `return kExitPipename` (64, `:124`). Erst danach `serverErwartungFuer` (`:142`) und `baueClient` (`:157`). Die Quelltextwache `probe_main_prueft_pipenamen_vor_dem_client` (`PipeClientLifecycleTestMain.cpp:1615-1638`) prüft die Reihenfolge Name < Urteil < Bedingung < Exit 64 < Servererwartung < `baueClient`, „Exit 64 genau einmal", die Meldung und „kein `kPipeName` in main" — jeder fehlende Anker ist rot (fail-closed).

**Produktionsname in jeder Schreibweise.** `ProbePipeRegel.h:67` vergleicht mit `equalsIgnoreCase`; zusätzlich ist die Erlaubnisliste (`:70-85`) groß-/kleinschreibungsgenau, `istProbePipename` (`eq-copilot/plugin/core/ipc/PipeToken.h:59-70`, unverändert) ebenso. Ergebnis: **zwei unabhängige Schichten** lehnen `\\.\PIPE\EVENACADIA.EQ-COPILOT.V1` und `\\.\Pipe\Evenacadia.EQ-Copilot.v1` ab — kein einzelner Eingriff lässt einen Produktionsnamen durch. Gemessen A4b `probe_pipe_lehnt_produktionsnamen_ab` mit 14 Namen (`:1566-1588`), darunter leer (`m21_leer`), v3-Produktionsform, drei Traversalen (`\..\` und `/../`), `…v1.`, ohne Pipepfad und drei ungültige v2-Suffixe.

**Trennerregel (E3-1).** `ProbePipeRegel.h:71` macht jeden Namen mit `\` oder `/` hinter `\\.\pipe\` fremd. Ich habe nachgelesen, dass das wirklich nötig ist: `istProbePipename` prüft nur den Präfix und den nicht-leeren Rest — ohne die Trennerregel wäre `\\.\pipe\evenacadia.nakama.v3.probe.x\..\evenacadia.eq-copilot.v1` `zugelassen`. Der Rotbeweis M-22 (2) bricht genau diese Zeile und genau dieser Fall fällt. Die Regel ist strenger als §6.4 und im Sinn von R-309-5; `istProbePipename` bleibt bytegleich (nicht im Diff).

**`PipeClient` mit leerem Namen.** `eq-copilot/plugin/src/PipeClient.cpp:224` trägt den Namen, wie er kommt (Rückfall entfallen); `:238-244` verlässt `start()` vor Thread und Versuch, Status `getrennt`, `letzterFehler` „kein Pipename". Sperrenordnung geprüft: `lebenslaufMutex` → `zustandMutex` wie in `stop()` (`:262`, `:277`); `zustandMutex` wird nirgends vor `lebenslaufMutex` genommen — kein neuer Deadlock. `pipeName` wird nur im Konstruktor gesetzt und sonst nur gelesen (`:238`, `:386`, `:400`), der neue Leseweg `pipeNameFuerTest()` (`PipeClient.h:140`) ist damit rennfrei. Alle drei Konstruktionsstellen von `PipeClient` im Baum (`PipeProbeMain.cpp:93`, `PluginProcessor.cpp:177`, `PipeClientLifecycleTestMain.cpp:159`/`:597`) nennen den Namen ausdrücklich.

**Produkt trägt weiter `kPipeName` (M-24).** `PluginProcessor.cpp:93-101` wählt zur Übersetzungszeit; Produktzweig `return juce::String (juce::CharPointer_UTF16 (kPipeName));` (`:99`), Testzweig `…m2probe.testbau` (`:96`). `produktVerdrahtung()` (`:115`) und der Testkonstruktor (`:138`) setzen beide `v2PipeName`; der v2-Client bekommt `verdrahtung.v2PipeName` (`:177`) statt `{}`. Gemessen als Quelltextwache `produkt_nennt_v2_pipe_ausdruecklich` (`PipeClientLifecycleTestMain.cpp:1680-1706`) — sie prüft den Produktzweig, dass zwischen `#else` und dem `kPipeName`-`return` kein anderes `return` steht, die Zuweisung in `produktVerdrahtung` vor `return v;` und das Argument am Client. **Zur Prüffrage aus NAK-318:** M-24 misst die Namensgleichheit **textuell und exakt**, nicht zur Laufzeit — A4b übersetzt den Prozessor nicht, und B8 misst nur den Testbau-Namen. Kein Bein baut den Produktzweig; das deckt sich mit M-24s eigener Testspalte und mit NAK-318 (datierte Härtung), ist also kein Befund.

**M-74 und die Frage nach `SondeProcessor`.** Selbst nachgelesen: **Probeeq hat keinen v2-Client.** `eq-copilot/plugin/sonde/SondeProcessor.h:523-524` hält nur `nakama::ipc::ControlClient controlV3` und `TelemetryClient telemetryV3`; `SondeProcessor.cpp:137-142` verdrahtet beide auf den v3-Namen, und `:191-200` startet sie nur außerhalb von `NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3`. Kein `PipeClient` in `plugin/sonde/` (gegrept). Im ganzen Baum trägt genau ein Prozessor einen v2-Client (`EqCopilotProcessor::pipe`, `PluginProcessor.h:1919`), und `pipe.start()` (`PluginProcessor.cpp:463`) läuft auch im Testbau — dort auf dem Probenamen. **M-74 deckt damit jeden Prozessor mit v2-Client.** Die Zielmenge habe ich selbst gezählt: 16 Vorkommen von `NAKAMA_PROZESSOR_QUELLEN` in `eq-copilot/plugin/CMakeLists.txt` (`:111` Definition, `:135` Produkt `EqCopilot`, dazu 14 Testziele); die Testschalter-Schleife `:1323-1330` enthält genau diese 14, `EqCopilot` nicht. Zwei weitere Ziele übersetzen `src/PipeClient.cpp` ohne den Prozessor — `EqCopPipeProbe` (`:599`, Name aus `argv` über `probePipeUrteil`) und `EqCopPipeClientTest` (`:614`, Namen je Fall PID-basiert); beide erreichen die Produktions-Pipe nicht. Die Wache B8 `m74_…/cmake` (`eq-copilot/plugin/tests/LebenslaufTestMain.cpp:258-306`, `:352-378`) liest `CMakeLists.txt` über `__FILE__` und ist fail-closed: fehlender Anker, unzuordenbare Quellnennung, fehlendes `EqCopilot`, `EqCopilot` in der Schleife oder ein Ziel ohne Schalter → rot. Teil 0 steht vor dem ersten Prozessor und bricht bei (a) mit `return 1` ab (`:333-336`) — ohne einen Pipezugriff.

**Konfliktende nur mit neuem ACK.** `ProbePipeRegel.h:92-99`: `verbunden` ∧ `protokollVersion == 2` ∧ gleiche `verbindungsVersuche` ∧ `heartbeatsBestaetigt >` vorher ∧ `! konflikt`. `PipeProbeMain.cpp:213-216` zieht `vorher` unmittelbar nach `b->stop()` und wartet auf das Prädikat; Fehlermeldung „Konflikt-Ende ohne bestaetigtes ACK" mit Exit 1 (`:218-227`). Sechs Snapshotpaare in A4b (`:1717-1774`) trennen Abbau (v0), Status-allein (v2 + getrennt), Neuaufbau (Versuche 1→2 bei ACK 3→5) und erstes ACK mit Konflikt; die Wache `probe_main_wartet_auf_konfliktende_praedikat` (`:1780-1797`) hält Reihenfolge, „Stand direkt nach dem Stopp" (kein `;` dazwischen) und „altes Flagprädikat entfernt".

## (b) T3-13-02 (R-309-1')

**Ein einziger Ausgang zum Beenden.** `$script:U.BeendeProzess` wird im Produktivcode genau einmal aufgerufen: `tools/fl/laufzeit.ps1:533`, in `Beende-Eigenen` (`:528-535`), das vorher `Besitz-Urteil` fragt. Alle Beendenwege gehen darüber: `Beende-Diagnose-FL` (`:549`), `Beende-Restprozesse` (`:556`), Render-Hänger (`:780`). Die Fremdprüfung steht damit **am** Beenden, nicht bloß davor — stärker als die Frage verlangt. `Ist-Diagnose` (der Titel-Teilstringtest des Basis-SHA) existiert nicht mehr (gegrept, kein Treffer in `tools/fl/`).

**Besitz.** `Besitz-Urteil` (`:498-511`): `ProzessInfo` (Win32_Process, `:277-283`) → ohne Prozess, ohne Startzeit oder ohne Befehlszeile ist nichts eigen; Eintrag muss in **PID, Startzeit und Befehlszeile** gleichen (`:503`); die Befehlszeile muss ein Projekt der Arbeitskopie dieses Laufs nennen (`:506-508`, `Projekt-Der-Befehlszeile` `:487-490` parst den gequoteten `.flp`-Pfad am Ende); bei Zweck `fl` mit Fenster und Titel muss der Titel mit `<Projektdatei> - ` beginnen (`:509-511`, zweite Sicherheit, E3-8 auch in `Warte-Ping` `:885`). Render gilt eigen, wenn die Befehlszeile ein `.flp` **im** `ProjektOrdner` nennt (`:507`) — die Referenzrenders liegen dort (`:826-828`), das deckt A-11/R-309-1'.

**Fail-closed geprüft, je an der Zeile:** unlesbare oder fremdformatige Liste → `Lies-Besitz` (`:440-459`) meldet einmal je Lauf und liefert `@()`, nie „alles eigen"; unbrauchbare Einträge werden je Eintrag verworfen (`:452-457`); toter Eintrag und wiederverwendete PID → `Bereinige-Besitz` (`:473-484`) vergleicht Startzeit **und** Befehlszeile und protokolliert den Grund; verweigerte Befehlszeile → `:502` „Befehlszeile nicht lesbar", fremd. Atomar geschrieben über Temp + `[IO.File]::Move(..., $true)` (`:420-431`).

**Fensterlos und titelgleich.** `Fremde-FL` (`:517`) filtert **ohne** Fensterbedingung, `Fahre-Lauf:1061-1062` endet vor `Beende-Restprozesse` mit Exit 0 `UEBERSPRUNGEN`; `Fremd-Text` (`:519-526`) nennt PID, Titel und Grund und unterscheidet „fremder FL-Prozess ohne Fenster", „eigenes FL zeigt ein fremdes Projekt" und „fremdes FL-Projekt offen". Dieselbe Prüfung vor Installation (`:597-598`), vor der Controller-Neuinstallation (`:637-638`), vor dem Render (`:758-759`), vor `frischer_start` (`:900-901`) und beim Ping (`:883-884`). `Fahre-Rueckweg` (`:915-926`) und der `-Beenden`-Zweig (`:1076`) haben keine eigene Vorprüfung, brauchen sie aber nicht: `Beende-Eigenen` trägt sie; der Rückweg verweigert danach mit PID und Titel, wenn noch irgendein FL läuft (`:921-926`). NAK-297 bleibt gewahrt: ein leerer Titel überspringt die Titelprüfung (`:509`), macht also weder fremd noch eigen.

**Grenze (kein Befund, benannt):** die Fremdprüfung ist ein Tor an benannten Stellen, kein Dauerwächter. Ein Titel-Doppel, das nach dem Ping erscheint, wird nicht mehr erkannt; der Lauf misst daneben weiter und endet mit seinem Szenarienurteil — genau das verlangt M-31 (a) („Exit 0 GEMESSEN"), und nur das Beenden bleibt geschützt.

## (c) T3-13-03 (R-309-2, A-7)

**Nakama-Seite.** `tools/fl/szenario.py:699-712` (`_sende`): je Versuch `umg.mcp_kennung()` (32 Hex, `:375-377`), Übergabe als `request_id`; eigene `RuntimeError`-Antworten tragen die Kennung (`:704`); `_zuordnen` (`:715-724`) gibt die Antwort nur bei `erhalten == kennung` zurück, sonst `_ungemessen` samt Zählung in `lauf.verworfen`. Die Wiederholung läuft nur, wenn die Antwort **zugeordnet** war und ein Timeout ist (`:708`) — eine verspätete Antwort löst keine Wiederholung aus und die Wiederholung trägt eine neue Kennung. `fahre` (`:2089-2099`): bei `_ungemessen` wird der Schritt nie bewertet, `lauf.letzte[aktion]` bleibt unberührt (`:2101`, nur bei `success`) — das ist der Kern von „keine Kette"; Exit 5, beim Ping Exit 3 mit Abbruch. Fehlende, fremde, doppelte (dieselbe Kennung eines früheren Versuchs) und verspätete Kennung sind damit derselbe Weg.

**Vorrang 3 vor 5 vor 4** bestätigt an `szenario.py:79-80` (`_RANG`) und `:121-123` (`schlechter`); `EXIT_VORAUSSETZUNG` wird beim Ping direkt gesetzt und ist der höchste Rang.

**Sicher mit altem Controller oder alter Bibliothek.** Bibliothek ohne `request_id`: `_nimmt_kennung` (`:301-309`) → `Umgebung.verbindung` liefert `(None, "MCP-Bibliothek ohne Anforderungskennung …")` (`:314-317`); `fahre:2055-2060` endet mit Exit 3, `ping:2157-2161` ebenfalls. Controller ohne Kennung: Antwort ohne `request_id` → `UNGEMESSEN`, beim Ping Exit 3 mit Abbruch (`:2094-2099`, Fall `309/M-42` Teilfall Ping prüft, dass nach dem Ping kein Aufruf mehr kommt). **Keine stille Fehlzuordnung möglich** — eine Antwort wird nur bei Zeichengleichheit der Kennung übernommen. E3-6 ist strenger als M-42: eine lokale Aktion, die intern eine Antwort verwirft, heißt UNGEMESSEN, auch wenn sie ok meldet (`:2097-2104`).

**Nachbarrepo (A-7).** `src/fl_studio_mcp/utils/midi_connection.py`: Kennung in die Auftragsdatei (`:238-239` des Zielstands), eigene Fehler mit Kennung (`_local_error`, `:81-88`), `_wait_for_response` übergeht fremde Kennungen **ohne die Datei zu löschen**, zählt je Kennung einmal und hält `deadline = start_time + timeout` absolut. `fl_controller/device_FLStudioMCP.py` kopiert die Kennung in Erfolg und Fehler (`request_id` vor `write_response`), `SCRIPT_VERSION` steigt auf `2026-09-18`. `tests/test_request_id.py` misst beide Hälften; die Controllerhälfte über den AST-Lader am echten Skript, die Frist über eine Uhr-Attrappe mit Obergrenze (E3-12: ohne diese Obergrenze hing der Fall unter der M-47-Mutation endlos — das ist eine echte Verbesserung des Tests, keine Abschwächung). Antworten ohne Kennung reicht der Client unverändert durch (`test_antwort_ohne_kennung_unveraendert`), ohne eigene Kennung verhält er sich wie bisher (`test_ohne_kennung_wie_bisher`) — genau die Grenze von E3-5.

**Echo.** Die vier Szenariodateien fragen jetzt `index`, `mode` und `track` mit `gleich` auf den gesendeten Wert ab. Selbst im Nachbarrepo nachgelesen, dass der Controller diese Felder wirklich zurückgibt: `channels.getInfo` → `"index": index` aus `params`, `transport.setLoopMode` → `{"mode": mode}`, `mixer.getPeaks` → `"track"`. Die neuen Erwartungen sind im echten Lauf erfüllbar (R-286-5 gewahrt). Dass beide Echos den *angeforderten* Wert spiegeln, macht sie zum Zuordnungsbeleg, nicht zum Wirkungsbeleg — R-309-2 verlangt nur die Zuordnung, und `transport.getStatus` prüft `loop_mode` separat.

## (d) T3-13-05, T3-13-06

**Importwache, beide Wege.** `szenario.py:263-278` (`lade_mcp_modul`): Stellvertreter für `fl_studio_mcp.utils` mit echtem `__path__` **vor** dem Import, danach `piano_roll_module()` über `sys.modules` — das ist der echte Importgraph nach dem Import, nicht eine statische Annahme; ein Treffer (auch von fremder Hand geladen) wirft `ImportgraphFehler` → Exit 3 mit Modulnamen. Zweiter Weg: `FENSTER_SKRIPT` (`:107-111`) lädt `lade_mcp_modul` aus derselben Datei und damit dieselbe Wache in den Unterprozess. Ich habe geprüft, dass `szenario.py` importsicher ist: nur Stdlib auf Modulebene (`:62-77`), `numpy` erst in Funktionen, `if __name__ == "__main__"` am Ende. Der echte Graph des Nachbarrepos stützt das: nur `utils/__init__.py:4` lädt `fl_trigger`; `connection.py` zieht `midi_connection` → `fl_paths`, `fenster.py` nur Stdlib, `fl_studio_mcp/__init__.py` ist leer, `pynput` steckt allein in `fl_trigger`. Die Ping-Zeile trägt den Graphen (`szenario.py:2159`, `:2165`) und `laufzeit.ps1:887` schreibt ihn ins Protokoll.

**Pin.** `Pruefe-McpStand` (`laufzeit.ps1:993-1027`) läuft in `Fahre-Lauf:1039` — nach `Lohnt-Es`, **vor** `Installiere` (`:1044`), vor `Pruefe-Controller`/`setup-local.ps1` (`:1053`) und vor jedem `uv run`. Exit 3 `VORAUSSETZUNG` bei: fehlender Pin-Datei, kein JSON-Objekt, fremdem `format`, unvollständiger Datei (Revision oder Blob-Kennung nicht 40 Hex), scheiterndem `git`, abweichender Revision, geänderter getrackter Datei, fehlendem Pfad im Commit, abweichender Blob-Kennung. `uv.lock` wird protokolliert, nicht verglichen (im Nachbarrepo ungetrackt). Der Runner schreibt die Pin-Datei nie (kein Schreibweg in der Funktion; `309/M-56` prüft den SHA-256 vor und nach dem Lauf).

**Pin gegen den Nachbarstand selbst nachgemessen** — `git -C …fl-studio-mcp rev-parse 15b3b1a1:<pfad>` für alle zehn Einträge: alle zehn Blob-Kennungen stimmen mit `tools/fl/mcp-stand.json` überein, `revision` = HEAD des Nachbarrepos = `origin/evenacadia-local`, Arbeitskopie sauber.

## (e) Rotbeweise an der Zusagezeile

Selbst nachgemessen: 39 Rohdateien `docs/beweise/roh/NAK-309-rot-M-NN.txt` für M-18 bis M-28, M-30 bis M-56 und M-74, jede mit „Die Zusage faellt an der erwarteten Pruefung: **JA**", keine mit NEIN (49 Teile). Für 14 Zeilen habe ich die zitierte Datei:Zeile gegen den Zielstand gelesen — `laufzeit.ps1:480`, `:503`, `:504`, `:517`, `:556`, `:883-885`, `:884`, `:1012`, `:1034-1035`, `:1038-1039`, `:1045`; `szenario.py:108-109`, `:267-268`, `:700`, `:708`, `:718`; `PluginProcessor.cpp:96`; `CMakeLists.txt:1327`; `ProbePipeRegel.h:59`, `:67`, `:71`, `:74-83`, `:94`, `:96`, `:97`, `:98`; `PipeClient.cpp:224`, `:238-244`; `PipeProbeMain.cpp:131`, `:137`, `:216`. **Jede „vorher"-Zeile entspricht wörtlich dem Code am Zielstand, und jede mutierte Zeile ist die Zeile, die die Zusage ihrer Matrixzeile trägt** — nicht ein Nebeneffekt. Die Wahl ist an den Stellen mit zwei Schichten sauber begründet (E3-10): M-22 (2) bricht die Trennerregel und **genau** `m22_traversal_v3` fällt, weil nur der v3-Zweig ohne sie durchlässt (im Code nachvollzogen); M-34 bricht Schutz und Aufrufstelle einzeln; M-38 (e) mutiert das Besitzurteil, weil eine synthetische „alles eigen"-Liste am zweiten Merkmal scheitern würde. Die Rohdateien führen je Lauf die nicht gefallenen Prüfungen als Beleg gegen den Nebeneffekt (M-19 (1): 80 weiter grün, „Mitgefallen: keine").

**Sonderfall M-19 (1).** Ich habe die Mutation im Code durchgerechnet: mit `==` statt `equalsIgnoreCase` fällt der Großschreibungsname durch die **groß-/kleinschreibungsgenaue Präfixprüfung** (`:70`) auf `fremd` — er bleibt abgelehnt, mit Exit 64, ohne `PipeClient`. Damit gilt: die Mutation bricht **das Urteil und den Meldungstext** der Zusage („`PROBE ABGELEHNT (Pipename produktion)`"), nicht die Ablehnung selbst. Nach dem Wortlaut der Zeile M-19 — die Meldung ist Teil der fett gesetzten Zusage — ist das ein Rotbeweis an der Zusagezeile, und die Mutation sitzt an der genannten Zeile, nicht an einem Nebeneffekt. Was **nicht** eintritt, ist der in der Rotbeweisspalte vorhergesagte Weg „Großschreibung zugelassen": dazu müssten zwei Schichten gleichzeitig fallen. Der Bauer hat das in E3-10 und in der Rohdatei ausdrücklich so hingeschrieben; ich lese es genauso. Der Code ist damit **strenger** als die Zeile behauptet — das ist eine Ungenauigkeit der Vorhersage im Manifest (docs/**, nach Ausschluss 1 kein NEEDS_WORK-Grund), kein Loch in der Messung. Geführt als L-1.

## Produktverhalten seit BASIS

Selbst nachgemessen (Diff vollständig gelesen, Zielstand an den Urteilsstellen nachgelesen):

- **Identität eingefroren:** `eq-copilot/identity/**` und `eq-copilot/schemas/**` sind nicht im Änderungssatz; keine Class-ID, kein Hersteller-/Plugin-Code, kein Replace-V2-Schalter berührt.
- **Nulltest/Passthrough:** kein DSP-, `processBlock`- oder Latenzcode im Diff. Die Produktänderungen sind drei: Rückfall in `PipeClient.cpp:224` entfällt, `PluginProcessor.cpp:177` nennt den Namen ausdrücklich (im Produktbau derselbe `kPipeName`), `PipeClient.h:140` bekommt einen lesenden Zugriff. Beide neuen Testzugriffe im Header (`:775`, `:778`) liegen im bestehenden Block `#if defined(NAKAMA_PHASE_B_TEST_NO_PRODUCT_V3)` (`:533`–`:1045`), die Definition von `v2PipeNameDesBausFuerTest` ebenso (`PluginProcessor.cpp:125`–`:149`); der Produktbau sieht nur das zusätzliche Feld `v2PipeName` in der privaten `V3Verdrahtung`.
- **Audio echtzeitfest:** `v2PipeNameDesBaus()` läuft zur Übersetzungszeit/Konstruktionszeit, nie im Audiopfad; der neue Leerzweig in `start()` nimmt `zustandMutex` in der bestehenden Ordnung und `start()` wird aus dem Konstruktor gerufen (`PluginProcessor.cpp:463`), nicht aus `processBlock`.
- **State verlustfrei:** kein State-, Schema- oder Migrationscode im Diff.
- **Nur gelesen, nicht nachgemessen:** die grünen Beine A1 (`NULLTEST OK`), B14 (231/231), B8, A4b (82 Prüfungen), A35 und der Kanon GRUEN 69/69 auf `ec419a71` — ich habe nichts davon gefahren. Dass `git diff --stat ec419a71..252f8c73 -- eq-copilot broker tools` leer ist, habe ich selbst gemessen (über `48fc736a..252f8c73`, leer), der Kanon deckt also den Endstand des Codes.

---

## Befundliste

Kein DEFEKT.

**L-1 · LÜCKE — `docs/beweise/NAK-309.md:535` (M-19, Rotbeweisspalte) gegen `eq-copilot/plugin/src/ProbePipeRegel.h:67`, `:70`.** Die Zeile sagt „Vergleich groß- und kleinschreibungsgenau → Großschreibung zugelassen → rot". Zugelassen wird der Name unter dieser Mutation nie; er fällt auf `fremd` und wird weiter mit Exit 64 abgelehnt. Die Matrix unterscheidet an dieser Stelle nicht zwischen dem Sicherheitskern der Zusage (Ablehnung, Exit 64, kein Client) und ihrem Etikett/Meldungstext; nach ihrem Wortlaut ist die Mutation ein gültiger Rotbeweis, ihre Wirkungsvorhersage ist falsch. Keine Nacharbeit: der Wortlaut ist docs/** (Ausschluss 1), der Code ist strenger als die Zeile.

**L-2 · LÜCKE — `eq-copilot/plugin/tests/PipeClientLifecycleTestMain.cpp:1680-1706` (M-24).** Die Gleichheit „Produkt-v2-Client trägt `kPipeName`" ist nur als Quelltextaussage gemessen; kein Ziel übersetzt den Produktzweig von `v2PipeNameDesBaus()` (`PluginProcessor.cpp:99`), B8 misst nur den Testbau-Namen. Deckt sich mit NAK-318 und mit M-24s eigener Testspalte; benannt, keine Nacharbeit.

**L-3 · LÜCKE — `tools/fl/laufzeit.ps1:487-490` (`Projekt-Der-Befehlszeile`).** Der Besitz hängt daran, dass Win32_Process die Befehlszeile als `… "<pfad>.flp"` am Ende liefert; gemessen ist das nur gegen die Attrappe `T-Starte` (`:1234`), die dieses Format nachbaut. §5 sagt selbst, dass Host-Zeilen erst mit der Rohdatei des Abschlusslaufs als gemessen gelten. Die Fehlrichtung ist fail-closed (nichts eigen → Exit 0 `UEBERSPRUNGEN`, kein Beenden), also kein Sicherheitsloch, aber der erste echte Laufzeit-Arm-Lauf kann daran übersprungen enden.

**L-4 · LÜCKE — `tools/fl/laufzeit.ps1:1061-1067` gegen M-34 („neben einer fremden FL-Sitzung wird nicht gemessen").** Die Fremdprüfung ist ein Tor an benannten Stellen (Installation, Controller, Render, FL-Start, Ping, `frischer_start`), kein Dauerwächter; ein Titel-Doppel nach dem Ping bleibt unentdeckt und der Lauf misst daneben weiter. M-31 (a) verlangt genau dieses Verhalten (Exit 0 `GEMESSEN`); die beiden Zeilen widersprechen sich nur im Satzbau, nicht in der Sache. Benannt, damit der Satz aus M-34 nicht als Dauerzusage gelesen wird.

**L-5 · LÜCKE — `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:269-303` (M-74 (c)).** Die Wache erfasst Ziele über `${NAKAMA_PROZESSOR_QUELLEN}`. Ein künftiges Ziel, das auf anderem Weg einen v2-Client bekäme (eigener Prozessor, eigenes `main` plus `src/PipeClient.cpp`), stünde außerhalb. Am Zielstand ist das leer: nur `EqCopilotProcessor` trägt einen v2-Client, `SondeProcessor` keinen.

**H-1 · HÄRTUNG — `tools/fl/laufzeit.ps1:778-782`.** Das Protokoll sagt „genau PID X beenden", bevor `Beende-Eigenen` gefragt ist; findet `FL-Instanzen` den Prozess nicht (`$inst` leer), passiert nichts und es folgt keine zweite Zeile. Die Behauptung im Log ist dann größer als die Handlung (Prüfliste E).

**H-2 · HÄRTUNG — `tools/fl/laufzeit.ps1:494-496` (`Trage-Ein` → `Schreibe-Besitz`).** Ein Schreibfehler der Besitzliste wird nicht gefangen; die Ausnahme endet den Lauf mit Exit 3, das gestartete FL bleibt ohne Eintrag und gilt in jedem Folgelauf als fremd (Übersprung bis es geschlossen wird). Fail-closed, aber ohne eigene Meldung.

**H-3 · HÄRTUNG — `tools/fl/szenario.py:301-309` (`_nimmt_kennung`).** Ein `send_command(..., **kwargs)`, das `request_id` verschluckt, passiert das Tor; erst die Antwort ohne Kennung führt zu UNGEMESSEN/Exit 3. Wirkung bleibt fail-closed, die Meldung nennt dann aber die falsche Ursache.

**H-4 · HÄRTUNG — `fl_controller/device_FLStudioMCP.py` (Zielstand, `if isinstance(request_id, str)` vor `write_response`).** Eine nicht-String-Kennung wird nicht zurückkopiert; die Antwort wäre auf der Nakama-Seite UNGEMESSEN. Der Client sendet nur 32-Hex-Strings, der Fall ist heute unerreichbar.

**H-5 · HÄRTUNG — `eq-copilot/plugin/tests/LebenslaufTestMain.cpp:280-289`.** Die Zuordnung Zielname ↔ Quellnennung geht über `rfind("target_sources(")`; ein `${NAKAMA_PROZESSOR_QUELLEN}` außerhalb eines `target_sources`-Aufrufs würde dem letzten vorangehenden Ziel zugeschrieben. Setzt einen Umbau der CMake-Datei voraus, nicht eine versehentliche Regression.

---

## geprüft

**Selbst nachgemessen (lesende Kommandos).** `git rev-parse HEAD` und `git status --short` in beiden Repos vor und nach dem Lauf. `git diff 7a510701…252f8c73` über alle 18 Auftragspfade vollständig gelesen; `git diff --stat 7a510701..252f8c73 -- eq-copilot broker tools` (keine Code-/Werkzeugdatei außerhalb des Prüfbereichs außer der ausgenommenen Prosa `tools/fl/LIES-MICH.md`); `git diff --stat 48fc736a..252f8c73 -- eq-copilot broker tools` leer (Code am ZIEL = Stand der Rotbeweise und Einzelbeine). Nachbarrepo: `git diff 01f72386…15b3b1a1` über die vier Pfade vollständig, `git show 15b3b1a1:tests/test_request_id.py` vollständig, `git rev-parse 15b3b1a1:<pfad>` für alle zehn Pin-Einträge (alle gleich), Importe aller sieben Paketmodule am Zielstand. Zielstand gelesen: `ProbePipeRegel.h` vollständig, `PipeProbeMain.cpp:118-200`, `PipeClient.cpp` (Konstruktor, `start`, Sperrenordnung, jede `pipeName`-Stelle), `PipeClient.h:119-160`, `PluginProcessor.cpp:83-180`, `:440-509`, `PluginProcessor.h` (Guard-Grenzen 533/1045, neue Zugriffe), `EqCopilotIds.h:18`, `PipeToken.h:45-70`, `sonde/SondeProcessor.h`/`.cpp` (v2-Client-Frage), `CMakeLists.txt` (`:111-141`, `:1300-1344`, alle 17 `src/PipeClient.cpp`-Nennungen, alle 16 `NAKAMA_PROZESSOR_QUELLEN`-Nennungen), `PipeClientLifecycleTestMain.cpp` (die 11 neuen Fälle und Wachen), `LebenslaufTestMain.cpp` (Teil 0 und die CMake-Wache), `ShotTestMain.cpp` (Kopfkommentar), `laufzeit.ps1` (Besitzblock `:383-563`, `Warte-Ping`, `Fahre-Rueckweg`, `Pruefe-McpStand`, `Fahre-Lauf`, `Rendere`, `Rendere-Referenzprojekte`, alle Beendenstellen, die neuen Selbsttestfälle), `szenario.py` (`lade_mcp_modul`, `_nimmt_kennung`, `Umgebung.verbindung`, `_sende`, `_zuordnen`, `_antwort_zelle`, `fahre`, `ping`, `FENSTER_SKRIPT`, Exitranglisten, die neuen Fälle, `BEREITSCHAFT_13_09`), `tools/beweise.ps1` (Behauptungen A35 und B8), `tools/fl/mcp-stand.json`, die vier Szenariodateien, `eq-copilot/README.md:57-66`. Rotbeweise: Werkzeug `NAK-309-etappe3-rot.ps1` (Specs M-18 bis M-28, M-30 bis M-56, M-74), Rohdatei M-19 vollständig, Mutationsblöcke von M-30, M-34, M-35, M-37, M-38, M-40, M-42, M-44, M-46, M-50, M-51, M-52, M-56, M-74, JA/NEIN-Zählung über alle 39 Dateien. Manifest als Referenz: §2, §3, §5 (Regeln), §5.3–§5.6, §9, §10, §14.2, §14.3, §14.6–§14.13, §15.

**Nur gelesen, nicht nachgemessen.** Jeder Laufbeleg: Kanon GRUEN 69/69 auf `ec419a71`, die Einzelbeine A4b/A35/A1/B14/B8 und die Pytest-Suite (54 passed), der Handlauf M-29 (`NAK-309-etappe3-pipeprobe.txt`, Lauf 1 Exit 0, Lauf 2 Exit 1 mit `dateiidentitaetFalsch`), die Rotbeweis-Läufe selbst (Bau-, Lauf- und Rücknahmeblöcke der Rohdateien), die SHA-256-Werte und Zeitstempel in den Rohdateien, `A33`-Zahl 58/58, die Mitgefallen-Listen. Auftragsgemäß habe ich nichts davon gefahren; ich habe nur geprüft, dass die Mutationen an den Zusagezeilen sitzen und dass der Code, an dem sie liefen, dem Zielstand entspricht.

## nicht geprüft

**Bewusst außerhalb.** Alles nach Ausschluss 1 bis 3: Prosa und Zahlen in `docs/**` (einschließlich der Manifesttexte, `tools/fl/LIES-MICH.md`, `docs/plugin-wissen.md`, `docs/beweise/NAK-286.md`), die Rohdatei-Wortlaute, `tools/dirigent/pruefliste.md`; die datierten Härtungen NAK-133 (Kanon baut `EqCopPipeProbe` nicht), NAK-290 (geteilte Probe-Pipenamen, damit auch der feste Name `…m2probe.testbau`), NAK-292 (A32 misst `laufzeit.ps1` mit 2 276 und `szenario.py` mit 3 935 Zeilen nicht), NAK-300, NAK-307 samt F-23, NAK-318; die Schwäche von `istProbePipename` selbst (S25i/NAK-310 — ich habe nur geprüft, dass `probePipeUrteil` sie für das Probewerkzeug über die Trennerregel schließt); die A4b-Behauptungszeile (E3-11); Etappe 2, Etappe 4, der volle Kanon am Ticketende und der Laufzeit-Arm.

**Nicht klärbar ohne Ausführung — ehrlich offen.** (1) Ob der Laufzeit-Arm am echten FL grün fährt: das gesamte Besitz- und Pin-Verhalten ist gegen Attrappen gemessen; das echte Format von `Win32_Process.CommandLine` (L-3), die echte Boot-Marke der Controller-Version `2026-09-18` und der erste Pinlauf gegen das Nachbarrepo sind ungemessen. (2) Ob die Beine wirklich in den berichteten Zahlen grün sind — ich habe kein Binary gestartet. (3) Ob unter den Rotmutationen genau die berichteten Fälle fielen — ich habe die Rohdateien gegen Werkzeug und Code geprüft, aber keinen Lauf wiederholt. (4) Die Aussage aus E3-1, Win32 löse `..` in `\\.\`-Pfaden auf, habe ich nicht gemessen; die Trennerregel ist unabhängig davon richtig, weil `istProbePipename` nur den Präfix prüft. (5) Ob ein gleichzeitig laufender Probe-Broker je den Namen `…m2probe.testbau` belegen könnte (NAK-290, ausgeschlossen).
