NAK-181 Etappe 1, Matrixnacharbeit 3 — Zusatzauftrag des Dirigenten (2026-09-05). LETZTE Runde im Budget der Etappe 1.

Matrixprüfung 3 (Codex gpt-6-astra, max, Thread 01a072d8-a390-78b0-add9-10c3bde915cf, Fixdiff a4a9295...20fc256) ist NEEDS_WORK: MP2-1 und MP2-2 sind geschlossen, MP2-3 bleibt mit zwei Restdefekten derselben Regel offen. Urteil wörtlich: docs/beweise/roh/NAK-181-matrixpruefung-3-20fc256.txt; Einordnung des Dirigenten: docs/beweise/NAK-181.md §5.4. Du bearbeitest ausschließlich die zwei Befunde unten in §2 (E4, N-18, N-18b, neue Zeile N-18d, §2.3, §2.4), §3.3 und §4 (B5, §4.2, §4.4) von docs/beweise/NAK-181.md — KEIN Bau, kein Produktcode, keine Nachfrage an den User; nichts Geschlossenes neu öffnen. Git wie bisher: nur docs/beweise/NAK-181.md mit Pathspec committen, `git pull --rebase` bei sauberem Worktree, pushen.

DEFEKTE (Befund des Prüfers wörtlich, dann die Regel des Dirigenten):

MP3-1 — „Beziehe beide Sampleraten in den R4b-Fehlerweg ein — DEFEKT, MP2-3/R4b bleibt offen: Bei `sample_rate = 1e-308` beziehungsweise `hello.samplerate = 1e-308` lassen `NakamaEvidenz.cpp:162–184` und `ControlClient.cpp:464–472,1667,1740` den endlichen Wert durch; `broker/src/vertrag.rs:183–185` und `tools/eq-copilot/pruefe_v3_vertrag.py:203–205` lehnen ihn dennoch ab. Die behauptete Unerreichbarkeit stimmt deshalb für beide Sampleratenzeilen nicht: Nach dem vorgesehenen Helferwechsel bliebe `null` in Pflicht-Zahlenfeldern erreichbar, wodurch Snapshot beziehungsweise Hello schemaungültig würden (`eq-ipc-v3.schema.json:159,358`). B5 muss auch diese Vorprüfungen erweitern und Snapshot beziehungsweise Handschlag bei Verweigerung abbrechen."
Regel R4d: Beide Sampleratenzeilen der Aufrufertabelle in E4 werden als ERREICHBAR geführt (Vorprüfung `(0, 768000]` schließt Werte unter der Riegel-Untergrenze nicht aus). B5 erweitert die Vorprüfungen an der Quelle: `transportstempelJson`/`evidenceSnapshotAlsJson` verwirft den Snapshot, wenn `wireZahl(t.sample_rate)` verweigert — derselbe Ausgang wie heute bei nicht endlich, `≤ 0` oder `> 768000` (`NakamaEvidenz.cpp:162-164`); `audioGueltig` (`ControlClient.cpp:464-472`) lässt keinen Handschlag zu, dessen Samplerate der Riegel nicht trägt (kein Hello, Zustand wie heute bei ungültiger Audio-Lage, `:1667-1669`). Eine neue Matrixzeile N-18d misst beides: ein Snapshot mit `sample_rate = 1e-308` entsteht nicht (Rückgabe `false`, kein Byte), ein Hello mit `samplerate = 1e-308` verbindet nicht — es reist kein `null` in ein Pflicht-`number`-Feld; Rotbeweis: Vorprüfung ohne `wireZahl` → `null` im Feld → A5 lehnt ab. Prüfe die übrigen als „unerreichbar" geführten Aufrufer der Tabelle noch einmal mit derselben Frage („besteht jeder Wert, den die heutige Vorprüfung durchlässt, alle vier Riegelregeln — auch Werte mit Betrag unter 1e-307?") und berichtige jede Zeile, bei der das nicht gilt.

MP3-2 — „Formuliere die Untergrenze anhand des Dezimalexponenten — DEFEKT, neuer Bruch von E4/R4c: Der zugesagte Bereich enthält beispielsweise `2e-308`, obwohl beide Riegel dafür `dez = -308` berechnen und ablehnen (`broker/src/vertrag.rs:181–185`, `tools/eq-copilot/pruefe_v3_vertrag.py:202–205`). Die Bedingung `dez > -308` bedeutet für nichtnullige Dezimalliterale einen Betrag von mindestens `1e-307`, nicht lediglich mehr als `1e-308`. Damit verspricht diese neue Bereichsbeschreibung die Übertragung von Zahlen, die der bestehende Leser verweigert. Übernimm hier die tatsächliche lexikalische `dez`-Bedingung des gerundeten Wiretexts."
Regel R4e: E4 formuliert die Untergrenze am Dezimalexponenten des GERUNDETEN Wiretexts, wie der Riegel sie rechnet (`dez = (ganz.len() − fuehrende − 1) + exp`, `vertrag.rs:181-185`; Ablehnung bei `dez ≤ −308` und `dez ≥ 308`): zulässig ist `dez > −308`, das heißt für nichtnullige Werte ein Betrag von mindestens `1e-307` (Mantisse in [1, 10)); Werte mit kleinerem Betrag liefern `false` und nehmen den Ersatzweg ihres Aufrufers. Die Bereichsliste (:397-400) und die Randwerttabelle werden berichtigt: N-18b bekommt `2e-308` und `9.99e-308` als verweigerte Randwerte (mit nachgerechnetem `dez`), N-18 behält `1e-300` und bekommt `1e-307` als kleinsten angenommenen Wert; jeder Wert der Randwerttabelle trägt seinen `dez`-Wert. Prüfe zugleich die Obergrenze mit derselben Rechnung (`dez ≥ 308` → abgelehnt), damit die Beschreibung an beiden Enden die lexikalische Bedingung trägt.

ABSCHLUSS: Im Manifest einen Abschnitt „### 5.5 Matrixnacharbeit 3 — was eingearbeitet wurde (2026-09-05, Worker, Basis 20fc256)" mit Tabelle Ort | Änderung | Befund, dazu die Kopftabelle (Zeile „Phase": Stand voranstellen, Verlauf bleibt; Zeilenzahl der Matrix) nachziehen. Commit nur docs/beweise/NAK-181.md mit Pathspec, pushen, dann den Turn mit der letzten Zeile „MATRIXNACHARBEIT 3 ABGESCHLOSSEN <sha>" beenden.
