NAK-121 / Planschritt S19b — ETAPPE 2, NACHARBEIT RUNDE 1: sechzehn Defekte der Abschlusspruefung schliessen.

ROLLE UND ORT
Du bist der Nacharbeits-Worker (Opus) im sichtbaren Checkout C:/Users/phili/Projekte/Nakama, Branch master, Basis-SHA {{BASIS}}. Der Dirigent (Fable) misst nur am Repo. Lies zuerst CLAUDE.md (Repo-Root), dann docs/beweise/NAK-121.md: Paragraph 1 (Gate-Text mit der GRENZE: kein Wire-, State- oder Testvertrag), Paragraph 3 (Befundtabelle), Paragraph 4 (Verhaltensmatrix H-01 bis H-22), Paragraph 11 und 12 (Konvergenzentscheid, Bau-Verlauf). Die Abschlusspruefung (Codex gpt-5.6-sol, max, Thread 01a06727-def6-7ab3-835a-4db9bba4d951, Rohurteil docs/beweise/roh/NAK-121-abschlusspruefung-1-4288ac2.txt) ergab NEEDS_WORK. Der Dirigent hat sechzehn Befunde an der Quelle als Defekt bestaetigt und je Befund die Regel festgelegt; einen Befund (H-03/C-06, senke.rs:226-228) hat er als Haertung eingeordnet — den fasst du NICHT an. Du setzt genau die sechzehn Regeln um, nicht mehr. Bestehende Tests behalten ihre Aussage; erlaubt sind neue Tests, Erweiterungen des vorhandenen Testhakens `CoordinatorFlushTestHaken` und Aenderungen an den in dieser Etappe neu geschriebenen Tests. Keine neue uneingeschraenkte `pub`-Signatur, die ein anderer Verbraucher liest; `pub(crate)`/`pub(super)` sind frei. Jede geaenderte H-Zeile bekommt einen frischen Rotbeweis unter docs/beweise/roh/NAK-121-rot-<ID>-r1.txt.

DIE SECHZEHN DEFEKTE — je Befund woertlich (Codex), dann die Regel des Dirigenten (03.09.2026)

D1 — G2-MSRV-001, broker/Cargo.toml:12. Codex: „Mit der deklarierten Toolchain und dem eingecheckten Lockfile bricht `cargo +1.82 check --manifest-path broker/Cargo.toml --locked` vor dem Kompilieren ab, weil `uuid 1.24.1` und `getrandom 0.4.3` Rust 1.85 verlangen. Damit ist 1.82 nicht der zugesagte tatsaechlich noetige Compilerboden; der neue Test vergleicht nur den Manifestwert und erkennt diesen Widerspruch nicht." (Dirigent: Registry-Cache bestaetigt rust-version 1.85.0 fuer uuid 1.24.1 und 1.85 fuer getrandom 0.4.3.)
REGEL: `rust-version` = das Maximum der `rust-version`-Werte aller im Lockfile gesperrten Abhaengigkeiten und des eigenen Codes (mindestens 1.85). Der Zwillingstest liest `cargo metadata --format-version 1 --manifest-path broker/Cargo.toml --locked` (oder die Registry-Manifeste der gesperrten Pakete) und prueft, dass kein Paket einen hoeheren Boden verlangt als das Manifest deklariert; die Pruefung laeuft im bestehenden Bein `broker` (cargo test). Kommentar in Cargo.toml nachziehen.

D2 — H-11, broker/src/transport/warteschlange.rs:32. Codex: „Jeder zugelassene Frame ist bereits hoechstens `MAX_PAYLOAD_BYTES` gross und die Queue hat hoechstens 256 Slots. Bei 255 Maximalframes bleibt sie unter diesem Budget, der 256. erreicht es exakt und wird angenommen, danach greift stets zuerst die Slotgrenze; die Bytegrenze kann im Produktionskonstruktor daher nie unabhaengig ausloesen und die urspruenglichen rund 64 MiB pro Verbindung bleiben moeglich."
REGEL: `CAP_INGRESS_BYTES` wird eine benannte Konstante deutlich unter `CAP_INGRESS * MAX_PAYLOAD_BYTES`, so gewaehlt, dass die Slotpolitik aus C-09 (`CAP_INGRESS = 256`, Grenztests bei CAP/CAP+1) fuer die realen Telemetrie- und Befehlsframes nie vorweggenommen wird (Wert am gemessenen Maximum der realen Frames begruenden, z. B. ein Vielfaches davon mal 256, aber hoechstens ein Achtel des heutigen Produkts). Der Test misst am Budget und am Budget plus eins mit maximal grossen Frames, dass zuerst P2 faellt und bei P0/P1 getrennt wird, und dass 256 kleine Frames weiterhin erst an der Slotgrenze scheitern. Der Kommentar in warteschlange.rs, der weniger als 64 MiB als „zweite, strengere Politik" bezeichnet, entfaellt; H-11 in Paragraph 4 bekommt den Wert in der Grenzspalte.

D3 — H-10, broker/src/coordinator/link.rs:281-285. Codex: „Bei einem Reconnect mit identischer Nonce teilen alter und neuer Link denselben Aliasbesitzer, weshalb die Registrierung den Alias zunaechst absichtlich stehen laesst. Sobald der alte Link anschliessend `control_ende` erreicht, entfernt dieser bedingungslose Aufruf jedoch genau die gemeinsame Zuordnung; danach faellt Dispatch fuer den ueberlebenden Link fail-closed aus. Der neue Test prueft den Alias nur vor diesem alten Cleanup." (Dirigent: die bedingungslose Entfernung ist Bestand, aber H-10 erzwingt den Fall neu, weil jetzt auch eine identische Nonce verdraengt — Regression durch H-10.)
REGEL: Beim Abbau eines Links wird der Alias nur entfernt, wenn kein anderer Link im Stand denselben Aliasbesitzer (und Adressraum) traegt; sonst bleibt die Zuordnung fuer den ueberlebenden Link stehen. Test in coordinator_model: Reconnect mit identischer Nonce → `control_ende`/`control_schliesst` des alten Links → Dispatch und ein P0-Befehl an den neuen Link funktionieren (nicht fail-closed); Mutante ohne die Besitzerpruefung ist rot.

D4 — G2-FLOATEDGE-001, broker/src/coordinator/sicht.rs:158. Codex: „Eine positive subnormale `sample_rate` passiert die vorhandene Eingangspruefung; bei einer von null verschiedenen Samplezahl ueberlaeuft diese Division zu `inf`. Statt des zugesagten `None` samt Zaehlung liefert die Sicht damit weiterhin einen nicht-endlichen abgeleiteten Wert, und der benannte Grenzwerttest fehlt."
REGEL: `fenster_ms` ist `None`, wenn `sample_rate` nicht `is_normal()` und positiv ist oder das Ergebnis nicht endlich ist; der Fall wird in einem Zaehler der Sicht/Statistik gezaehlt (NaN-Ehrlichkeit aus CLAUDE.md). Test `subnormale_samplerate_ergibt_kein_fenster` in coordinator_model mit subnormal, 0, negativ, +inf, NaN und einem normalen Wert.

D5 — H-17, broker/src/coordinator/liveness.rs:423-425. Codex: „Ein direkter Aufrufer kann einen Wert nur mit gueltiger `adresse` und `aussageklasse = beobachtend` uebergeben; fehlende Pflichtfelder wie `plugin_kind`, `measurement_position`, `betrieb`, `capabilities` und `frische` werden nicht geprueft, dennoch wird der Deskriptor gespeichert und `true` geliefert. H-17 verlangt hier die vollstaendige v3-Vertragspruefung, nicht nur Klasse und Adresse."
REGEL: `descriptor_setzen` und `descriptor_aus_heartbeat` nutzen dieselbe Prueffunktion (plugin_kind, messpunkt/betrieb, capabilities, Pflichtfelder des v3-Deskriptors, Aussageklasse); ein Deskriptor, den der Heartbeat-Weg ablehnt, lehnt auch der Setter ab. Test: je fehlendem Pflichtfeld `false` und kein Eintrag in Snapshot/Store.

D6 — H-14, broker/src/coordinator/link.rs:270. Codex: „Erreicht das Aliasregister seinen Deckel, liefert es `Registrierung::Ungueltig`; behandelt wird hier jedoch nur die Kollisionsvariante, danach werden Client und Link angelegt und bedingungslos als angenommen zurueckgegeben. Auch ein Fehler am persistenten Guarddeckel schaltet nur Routing ab. Der Peer erhaelt somit ein Welcome statt der von H-14 zugesagten Ablehnung."
REGEL: `Registrierung::Ungueltig` (Deckel erreicht) fuehrt zu `ControlRegistrierung::abgewiesen(grund)` — kein Client, kein Link, kein Welcome; ein Deckelfehler der persistenten Riegeltabelle ebenso. Test: am Deckel wird das Hello abgewiesen (kein Welcome, Zaehler), Deckel minus eins wird angenommen.

D7 — H-14, broker/src/instance_alias.rs:161-162. Codex: „Nach Erreichen von 1024 Eintraegen kann eine explizite Aufloesung die Menge auf 1023 reduzieren. Die naechste Kollision passiert diese Pruefung und fuegt anschliessend zwei Besitzer ein, sodass die zugesagte Obergrenze mit 1025 Eintraegen ueberschritten wird; der Test erreicht den Deckel nur in Zweierschritten und deckt diesen Zustand nicht ab."
REGEL: Die Pruefung reserviert so viele Plaetze, wie die Kollision einfuegt (`len + 2 > MAX_QUARANTAENE` → Ungueltig); der Test faehrt den Deckel auch aus einem ungeraden Stand (1023) an und misst, dass die Menge nie ueber 1024 waechst.

D8 — H-14, broker/src/coordinator/flush.rs:177-182. Codex: „Trennen sich beide kollidierenden Links vor der expliziten Aufloesung, enthaelt `stand.links` keinen passenden Besitzer mehr und diese Liste bleibt leer. Store- und Coordinator-Guard werden dennoch entfernt und die Methode meldet Erfolg, waehrend der Aliasbesitzer quarantaenisiert bleibt und seine erneute Registrierung weiter scheitert."
REGEL: Die Aufloesung findet den Adressraum nicht ueber lebende Links, sondern ueber das Quarantaeneregister selbst (der Eintrag kennt seinen Besitzer; `quarantaene_aufloesen` ueber alle Adressraeume dieses Besitzers) — Riegel und Alias laufen nie auseinander. Test: beide Links trennen, Riegel aufloesen, Alias erneut registrieren gelingt.

D9 — H-21, broker/src/store/writer.rs:665-666. Codex: „Bei einem persistenten Guard mit grossgeschriebener SID und einer Aufloesung ueber dieselbe SID in Kleinschreibung loescht dieses SQLite-Praedikat wegen der standardmaessig binaeren Kollation keine Zeile und prueft die betroffene Zeilenzahl nicht. Der gefaltete Speicherindex wird trotzdem geleert und die Methode meldet Erfolg; beim naechsten Start wird der vermeintlich geloeschte Guard erneut geladen."
REGEL: Loeschen und Lesen persistenter Riegel gehen ueber dieselbe Faltung wie der Speicherindex — entweder loescht der Writer mit der aus dem Index stammenden Originalschreibweise oder das Praedikat faltet beide Seiten (`lower(effective_address) = lower(?1)`; kein Schema, keine gespeicherten Bytes geaendert); die betroffene Zeilenzahl wird geprueft und eine Null als Fehler gemeldet. Test in store_crash_matrix: Guard mit grossgeschriebener SID persistieren, mit Kleinschreibung aufloesen, Store neu oeffnen → Guard ist weg.

D10 — G2-TOCTOU-002, broker/src/store/pfad.rs:112-113. Codex: „Die Funktion sucht weiterhin per `exists()` den naechsten vorhandenen Vorfahren und klassifiziert anschliessend dessen Pfadnamen. Beim ersten Start mit fehlenden Komponenten oder bei einem Austausch zwischen Pruefung und spaeterem `create_dir_all` beziehungsweise SQLite-Open wird daher ein anderes Objekt geoeffnet als geprueft; die A-Zusage verlangt stattdessen komponentenweise Handles und die Volumenpruefung am geoeffneten Datenbankobjekt."
REGEL: Die Volumenklassifikation (remote/lokal) laeuft nach dem Oeffnen auf dem Handle der Datenbankdatei (z. B. `GetFinalPathNameByHandleW` auf dem geoeffneten Handle, dann `GetVolumePathNameW`/`GetDriveTypeW` auf diesem finalen Pfad; windows-sys `Win32_Storage_FileSystem` ist schon eingebunden) und weist die Datenbank ab, bevor der Store sie nutzt; die Vorfahrenklassifikation darf als Vorpruefung bleiben, ist aber nicht mehr die Entscheidung. Test: `store_weist_reparse_punkt_im_pfad_ab` bleibt; neuer Test zeigt, dass die Entscheidung am geoeffneten Objekt faellt (Mutante: Handle-Klassifikation entfernen → rot).

D11 — H-01, broker/src/transport/server_v3/win_handles.rs:581-582. Codex: „Serverstopp und regulaerer Verbindungsabbau verwenden `alle_io_abbrechen` beziehungsweise `io_abbrechen`, die den Zaehler hier als `None` weiterreichen. Liefert `CancelIoEx` dort einen unerwarteten Fehler statt `ERROR_NOT_FOUND`, wird er weiterhin verschluckt; gezaehlt wird ausschliesslich im Wachhundpfad, obwohl H-01 dies auch fuer Ende und Stopp zusagt."
REGEL: Jeder Abbruchpfad (Wachhund, Verbindungsende, Serverstopp) reicht die Statistik durch und zaehlt unerwartete `CancelIoEx`-Fehler; die `None`-Variante entfaellt. Test: Rotbeweis ueber den Stopp-Pfad.

D12 — H-04-Test, broker/tests/coordinator_model.rs:1165-1166. Codex: „`modell_sicht` gibt ausschliesslich kopierte Werte zurueck und laesst seinen internen Mutex-Guard vor der Rueckkehr fallen; die Panik in der naechsten Anweisung laeuft deshalb nicht unter dem Lock. Der Mutex bleibt gesund und alle nachfolgenden Assertions pruefen lediglich normale Zugriffe, nicht die zugesagte Weiterarbeit nach einer tatsaechlichen Vergiftung."
REGEL: Der Test vergiftet den Standlock wirklich (Panik in einem Thread, waehrend der Lock gehalten wird — ueber den vorhandenen `CoordinatorFlushTestHaken` oder einen `pub(crate)`/Testhaken-Weg, keine neue Verbraucher-Signatur) und beweist die Vergiftung (ein anschliessender Zugriff laeuft ueber `PoisonError::into_inner`, beobachtbar ueber einen Zaehler oder den Haken); danach misst er die Weiterarbeit. Mutante: ein `expect` auf dem Lock → rot.

D13 — H-10-Test, broker/tests/coordinator_model.rs:1329-1333. Codex: „Der als P0-Zieltest bezeichnete Lauf gibt nur zwei Trennflags und die Subscriptionzahl zurueck; er sendet keinen P0-Befehl und vergleicht weder Zielwirkung noch persistierten Zustand. Eine Ruecknahme der neuen Zielaufloesung in `befehl.rs` bliebe daher gruen, solange die separate Identisch-Nonce-Verdraengung erhalten bleibt."
REGEL: Der Test sendet einen persistenten P0-Befehl bei zwei Links derselben Adresse und misst Ziel (Link-Identitaet des Zielclients), Wirkung und persistierten Zustand; die Mutante „erste Adressuebereinstimmung" ist rot.

D14 — H-02-Test, broker/src/transport/server_v3/verbindung.rs:512-515. Codex: „Die Testnaht beendet den Schreiber bereits, sobald dessen ID in `abgeloest` erscheint. Dieser Marker wird vor dem naechsten Wachhundtick gesetzt, sodass der Thread sein Handle selbst freigibt, ohne dass der Wachhund `CancelIoEx` ausgefuehrt haben muss; selbst ein Entfernen des zugesagten wiederholten Wachhundabbruchs liesse den Test bestehen."
REGEL: Die Testnaht beendet den Schreiber erst, nachdem der Wachhund fuer die abgeloeste ID tatsaechlich abgebrochen hat (Zaehler oder Ereignis des Wachhund-Abbruchs, nicht die Markierung); die Mutante ohne Wachhund-Abbruch ist rot.

D15 — H-07-Test, broker/tests/security_vectors.rs:1044-1046. Codex: „Der Test verbindet nur einen Client und schliesst ihn sofort, ohne Hello, Snapshot oder sonstigen Broker-Write. Damit ist kein Ausgabepuffer abzufliessen; geprueft werden lediglich der Spurmarker und ein weiterhin nuller Timeoutzaehler. Weder die Zustellung gepufferter Daten noch die zugesagte positive Zaehlung bei einem nicht lesenden Peer wird so gemessen."
REGEL: Der Test erzeugt einen ausstehenden Broker-Write (Hello/Welcome oder Snapshot) und misst beides: bei lesendem Peer kommt der gepufferte Inhalt vor dem Schliessen an; bei nicht lesendem Peer zaehlt der Timeoutzaehler positiv und das Schliessen dauert hoechstens die Frist. Mutante ohne Abfluss → rot.

D16 — G2-LOSSYSTR-001-Test, broker/tests/store_crash_matrix.rs:2551-2555. Codex: „Auf einem gewoehnlichen, wohlgeformten LocalAppData-Pfad besteht diese Pruefung auch nach einer Regression zu `String::from_utf16_lossy`. Enthielte der Pfad tatsaechlich ein ungepaartes Surrogat, wuerde zudem selbst die korrekte `OsString`-Variante beim anschliessenden `to_string_lossy` als U+FFFD dargestellt. Der Test speist die relevante UTF-16-Kante somit nicht ein und kann die zugesagte verlustfreie Wandlung nicht unterscheiden."
REGEL: Der Pfad bleibt von der Wandlung bis zum Oeffnen ein `OsString`/`PathBuf` (kein `to_string_lossy` auf dem Oeffnungspfad; nur Anzeige darf lossy sein). Der Test speist einen Pfad mit ungepaartem Surrogat ein (ueber die Pfadquelle, die `store_pfad` liest — Umgebungsvariable oder Registry-Wert im Testrahmen — oder ueber einen `pub(crate)`-Einstieg in die Wandlung) und prueft, dass der geoeffnete Pfad byteweise (`OsStrExt::encode_wide`) dem Original entspricht; Mutante `from_utf16_lossy` → rot.

FORM
- Ein Commit je Defekt oder je Modulgruppe, Betreff „NAK-121 Nacharbeit 1 D<n>: <Kurz>"; nach jeder Gruppe cargo test gruen, Pathspec-Commit, Push (bei sauberem Worktree `git pull --rebase` vorher).
- Am Ende ein Kanon der betroffenen Beine (mindestens `broker`, A21, A22, A4-SI, B10) ueber `pwsh -NoProfile -File tools/beweise.ps1` — lies im Skript, wie einzelne Beine gewaehlt werden; fehlt eine Auswahl, laeuft der volle Kanon abgekoppelt wie in Etappe 2 (Log nakama-nak121-kanon-nacharbeit1.log). Rohausgabe committen.
- Manifest docs/beweise/NAK-121.md: Kopf (Phase: „Etappe 2, Nacharbeit Runde 1 nach Abschlusspruefung 1"; Kanon-Zeile aktualisieren, wenn ein voller Lauf gefahren wurde), Paragraph 4 nur dort aendern, wo eine Regel eine H-Zeile praezisiert (H-11 Wert, H-14 Reservierung/Aufloesungsweg, H-17 gemeinsame Pruefung, H-10 Aliasbesitz), neuer Abschnitt „## 13. Nacharbeit Runde 1 (Abschlusspruefung 1)" append-only: je Defekt Regel woertlich, Commit, Test, Rotbeweis. Dokuriegel fahren.
- Git: git status --short vor jedem Commit; nur explizite Pathspecs; nie git add -A, nie --amend, nie reset --hard; fremde uncommittete Dateien nie anfassen. Ende: Worktree sauber, HEAD auf origin/master; kein Stand mit ungelaufenen Tests. Wird dein Kontext knapp, committe und pushe den erreichten Stand mit Hinweis im Manifest, welche D-Nummern offen sind.
- Keine Rueckfragen an den User; technische Entscheidungen triffst du selbst und begruendest sie im Manifest.
