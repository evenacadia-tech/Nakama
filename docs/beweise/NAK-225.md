<!-- NAKAMA-URTEIL: T2 PASS 2026-09-09 -->

# NAK-225 — S25d: Plugin-Kern verhaltensneutral aufteilen

| Merkmal | Wert |
|---|---|
| Ticket | NAK-225 (Register `docs/offene-punkte.md` Zeile 239), Planschritt **S25d** (`docs/plan/plan.json`), Prüfstufe **T2**. Pflegeschritt 2 nach G5, Muster S25c (NAK-224) und S19b (NAK-121). |
| Phase | **Abgeschlossen 09.09.2026 (Dirigent): T2 PASS — Erstprüfung PASS ohne Defekt (Codex Astra max, Thread `01a087a1`); der Abschlusskanon des Erbauers GRÜN 62/62 auf `f4d497b3` gilt als Kanon des Endstands, weil alle Commits danach nur `docs/` berühren (End-SHA `004b1099`). Register nachgezogen (NAK-225 geschlossen; NAK-236, NAK-238 umgeschrieben; NAK-242 bis NAK-244 neu).** Davor: **T1 PASS 2026-09-09 — gebaut in 20 Commits plus diesem Abschluss-Commit.** Der Prozessor ist in vier Fachbereiche geteilt, `FeatureEngine.h` in Vertrag plus sechs Merkmalsfamilien, der `ControlClient` in drei Fachbereiche; keine Quelldatei liegt mehr über 2 000 Zeilen, kein neues Modul über dem Ziel 1 500. Riegelkarte §3 vollständig mit beiden Werten gemessen, Nebenbefunde §5 nummeriert, Verlauf §6. **Ein roter Zwischenlauf** (§6 h2) hat einen echten Fund geliefert und ist behoben; er bleibt als Rohausgabe stehen. T2 setzt der Dirigent nach der Codex-Prüfung. |
| Prüfstufe | **T2 PASS 2026-09-09** (Codex `gpt-6-astra` max, Erstprüfung, Thread `01a087a1-5006-7670-9087-d944dfb0c60e`, HEAD vor und nach dem Lauf `004b1099`; Kanon GRÜN 62/62 auf `f4d497b3`, Rohausgabe `docs/beweise/roh/NAK-225-f4d497b.md`). Davor: **T1 PASS 2026-09-09** (Selbstaudit des Erbauers je Commit; Bau und Beine je Commit; Riegelkarte R1–R10 vollständig; Abschlusskanon GRÜN 62/62). **Was T1 nicht sagt:** kein FL-Lauf; keine Aussage über Laufzeitverhalten jenseits der Beine; die Nebenbefunde §5 sind ausdrücklich nicht behoben. T2 setzt der Dirigent nach der Codex-Prüfung. |
| Basis-SHA | `0ca392b52b484bc3d7463586719d111c3f8bcda9` (= `git rev-parse HEAD` beim Start; `git status --short` leer, HEAD = `origin/master`) |
| Kanon vorher | `41f4984` **GRÜN 62/62, Exit 0** (Abschlusskanon NAK-224, Rohausgabe `docs/beweise/roh/NAK-224-41f4984.md`). `git diff --stat 41f4984..0ca392b5 -- eq-copilot broker tools` ist **leer** (in dieser Session gemessen, §3 R1) — der Lauf steht auf demselben Codestand wie der Basis-SHA. |
| Kanon nachher | **`f4d497b3` GRÜN — 62/62 Kanon-Läufe bestanden, Exit 0**, Lauf 2026-09-09, abgekoppelt auf sauberem Baum, Rohausgabe `docs/beweise/roh/NAK-225-f4d497b.md`. **Gleiche Beinzahl wie vorher (62)**, und je Bein dieselbe Prüfungsmenge mit einer einzigen Abweichung: A32 116 → 121 (die fünf neuen Selbsttestfälle aus Schritt g). Nebenmeldungen wörtlich wie in jedem grünen Kanon: „2 geplante Prüfung(en) noch nicht gebaut", „1 stillgelegte(s) Bein(e)". **Davor ein roter Lauf** auf `d9e2b6db` (1 von 62, Bein A5) — echter Fund, behoben, §6 h2, Rohausgabe `roh/NAK-225-d9e2b6d.md`. |
| Testbestand | **Zielmenge unverändert: 120 vcxproj-Ziele im Baum, 40 Kanon-Bauziele** (`roh/NAK-225-ziele-vorher.txt` gegen `-nachher.txt`, §3 R4). Prüfungszahlen je Bein `roh/NAK-225-beinzahlen.txt` (§3 R1b). Keine Prüfung fällt weg: A5 613 vorher wie nachher, B10 381 vorher wie nachher, A14 25, B1 120, B2 180, B8 79, A16 87, B3b 85. |
| Ticketpfade | `eq-copilot/plugin/src/PluginProcessor.cpp` (+ neu `src/prozessor/`), `eq-copilot/plugin/core/analysis/FeatureEngine.h` (+ neu `core/analysis/featureengine/`), `eq-copilot/plugin/core/ipc/ControlClient.cpp` (+ neu `core/ipc/controlclient/`), `eq-copilot/plugin/CMakeLists.txt` (nur Quelllisten), `tools/plan/gesundheit.py`, `tools/beweise.ps1` (nur der Behauptungstext von A32), `docs/beweise/NAK-225.md`, `docs/beweise/roh/NAK-225-*`. **Erzwungen dazu, je Zeile in §2.6 begründet:** `tools/eq-copilot/pruefe_kern_identitaetsfrei.py` (handgeführte Objektliste des Kern-Riegels A14 — sie steht in derselben Beziehung zu `NAKAMA_KERN_QUELLEN` wie Quellliste ↔ Ziel) und `eq-copilot/plugin/tests/IpcTestMain.cpp` (zwei Zeilen: eine Regressionswache, die **Quelltext liest**, muss dem Pfad ihrer Quelle folgen). **Nicht erzwungen:** `PluginProcessor.h` und `ControlClient.h` haben **0 geänderte Zeilen** — der Compiler hat keine gefordert. |
| Nicht angefasst (Grenze) | `eq-copilot/identity/` 0, `eq-copilot/schemas/` 0, `eq-copilot/fixtures/` 0, `eq-copilot/plugin/hostbridge/` 0, `third_party/` 0, `broker/` 0, `docs/offene-punkte.md` 0, `docs/plan/*` 0, `docs/PLAN-STAND.md` 0, `CLAUDE.md` 0, `.claude/` 0, `design/` 0 — alle in §3 R5 gemessen. Ebenfalls nicht: **NAK-235** (keine lange Funktion zerlegt; die sechs genannten sind nach dem Schnitt gleich lang oder kürzer, §3 R9), **NAK-236** (Kommentar-Bezeichner außerhalb der drei Dateien), **NAK-237** (SAFETY-Arbeit im Broker), **NAK-238** für `SourcesModel.cpp` (1 864), `PluginEditor.cpp` (1 665), `PluginProcessor.h` (1 524) — unter der Grenze 2 000, späterer Pflegeschritt. |
| Änderungssatz | Ein verhaltensneutraler Änderungssatz in **20 Commits** plus dem Abschluss-Commit, Reihenfolge nach Ablauf (a)–(h) des Ticketauftrags: Manifest und Schnittplan (a), CMake-Quellliste (b), vier Fachbereiche des Prozessors (c1–c4), Vertrag plus sechs Merkmalsfamilien der Engine (d0–d6), drei Fachbereiche des ControlClient (e1–e3), Kommentare (f), `gesundheit.py` samt A32-Behauptung (g), Riegelkarte und Kanonbefund (h1–h2). Beziehungen im selben Satz: Quellliste ↔ Teil ↔ Ziel ↔ A14-Objektliste; Ratsche ↔ Iststand ↔ Pflegeticket-Liste ↔ A32-Behauptung. |
| Prüfmodell | T1 durch den Erbauer (Selbstaudit je Commit, voller Kanon am Ende). Prüfer aller Urteilsläufe: Codex `gpt-6-astra`, Effort max, lesend (User-Wort 05.09.2026); Sol nur Gegenprüfer. |
| Erstprüfung | Codex `gpt-6-astra` (max), lesend, Thread `01a087a1-5006-7670-9087-d944dfb0c60e`, HEAD vor und nach dem Lauf `004b1099`, Lauf 09.09.2026 21:24–21:37, Prüfbereich `0ca392b5...004b1099 -- eq-copilot/plugin/CMakeLists.txt eq-copilot/plugin/core/analysis/FeatureEngine.h eq-copilot/plugin/core/analysis/featureengine/ eq-copilot/plugin/core/ipc/ControlClient.cpp eq-copilot/plugin/core/ipc/controlclient/ eq-copilot/plugin/src/PluginProcessor.cpp eq-copilot/plugin/src/prozessor/ eq-copilot/plugin/tests/IpcTestMain.cpp tools/beweise.ps1 tools/eq-copilot/pruefe_kern_identitaetsfrei.py tools/plan/gesundheit.py` (Auftrag `docs/beweise/roh/NAK-225-erstpruefung-auftrag.txt`) — **URTEIL PASS, kein Defekt.** Geprüft laut Prüfer: gebundener Diff, Funktionsgleichheit, Bindungen, Deklarationsreihenfolge, Audio/State, CMake-Zielmengen und Testwachen; A5-Metrikriegel 6/6, `gesundheit.py` Exit 0, A14-Artefaktprüfung 24/24, 25 Shot-Hashes unverändert. Nicht erneut ausgeführt: Bauten, voller Kanon, C++-Tests, Rendering (Sandbox ohne Schreibrecht; Kanon 62/62 und Selbsttest 121/121 aus den Rohbelegen gelesen; A14 ohne Neubau Exit 3 ohne Frischeurteil). Volltext `docs/beweise/roh/NAK-225-erstpruefung-004b109.txt` |
| Urteil | **T2 PASS — Erstprüfung, 09.09.2026** (Codex `gpt-6-astra`, Effort max, lesend, Thread `01a087a1-5006-7670-9087-d944dfb0c60e`, HEAD vor und nach dem Lauf `004b1099`). Prüffolge: Erstprüfung über den ganzen Ticketbereich `0ca392b5...004b1099`, PASS — keine Nacharbeitsrunde. Basis-SHA `0ca392b52b484bc3d7463586719d111c3f8bcda9`, End-SHA `004b10997d9eaef716aed611838ed8830301fc84` (Codestand unter `eq-copilot/` und `tools/` identisch mit `f4d497b3`; Kanon GRÜN 62/62 auf `f4d497b3`, Rohausgabe `docs/beweise/roh/NAK-225-f4d497b.md`; davor ROT 1/62 auf `d9e2b6db`, Bein A5, behoben in §6 h2). Kumulierte Rundenbilanz (`py -3.13 tools/dirigent/rundenbilanz.py --runden 0ca392b5 004b1099`): Bau `0ca392b5..004b1099` Produkt 21 Dateien +9778/−8866, Tests 1 Datei +8/−2, Prüfwerkzeug 3 Dateien +75/−15 — eine Runde, kein Konvergenz-Signal. Register-Nachzüge im Abschluss-Commit: NAK-225 geschlossen; NAK-236 (Ratsche 30, NB-2 und NB-4 als Material), NAK-238 (drei Plugin-Dateien bleiben, ohne Schrittzuordnung) umgeschrieben; NB-1 → NAK-242 (Prüfliste: vor einem Dateischnitt Register und quelltextlesende Wachen suchen), NB-5 → NAK-243 (Messkante `gesundheit.py`), NB-6 → NAK-244 (C4458 in `stereoSchritt`); NAK-235 unverändert (28) |

---

## 1. Ticket- und Registerbezug, wörtlich

**Ticket** (`docs/plan/plan.json`, Schritt S25d, Ticket NAK-225, Stufe T2),
wörtlich:

> ANGELEGT 08.09.2026 (Pflegeschritt 2 nach G5, Register NAK-225, vor S26–28,
> damit der EQ-Bau auf geteilten Dateien beginnt). Gemessen am 08.09.2026:
> `eq-copilot/plugin/src/PluginProcessor.cpp` 4 161 Zeilen,
> `core/analysis/FeatureEngine.h` 3 969, `core/ipc/ControlClient.cpp` 2 307,
> `src/SourcesModel.cpp` 1 797. INHALT: je Datei nach Fachbereich teilen
> (Prozessor: Hostbrücke, State, IPC-Anbindung, Analyse-Anstoß; FeatureEngine:
> Messkern je Merkmalsfamilie; ControlClient: Verbindung, Nachrichten,
> Fehlercodes), Identität, Audio-Thread und State-Pfade unberührt. BEWEIS:
> Nulltest bitidentisch, Passthrough sampleidentisch, Kanon vorher = nachher
> grün, `EqCopShot`-Sichtprüfung unverändert, Beweismanifest mit den
> Beinzahlen. GRENZE: keine Legacy-Umbenennung (NAK-30), keine neue Optik,
> keine Funktionsänderung.

**Register** (`docs/offene-punkte.md`), die Zeile, die den Umfang trägt:

> **NAK-225** | 09-08, Pflegeschritt 2 nach G5, vor S26–28 (Messung
> 08.09.2026) | **[Planarbeit · S25d]** **Plugin-Kern verhaltensneutral
> aufteilen:** `PluginProcessor.cpp` (4 161 Zeilen), `FeatureEngine.h` (3 969),
> `ControlClient.cpp` (2 307), ggf. `SourcesModel.cpp` (1 797) nach
> Fachbereich; Identität, Audio-Thread, State unberührt; Beweis Nulltest
> bitidentisch, Passthrough sampleidentisch, Kanon vorher = nachher,
> `EqCopShot`-Sicht unverändert. Vor dem EQ-Bau, damit S26–28 auf geteilten
> Dateien beginnt. Wortlaut in `docs/plan/plan.json` S25d.

**Ausdrücklich nicht Teil dieses Tickets** (Entscheid des Dirigenten, Technik):

> **NAK-235** | **[Lücke · Pflege · S25c/S25d]** **28 Funktionen über 200
> Zeilen, ohne Pflegeticket** (Ratsche 28 in `gesundheit.py`, Ziel 0). […]
> NAK-224/-225/-234 decken die Dateien, nicht diese Funktionen […] Beim
> Aufteilen in S25c/S25d die Ratsche im selben Ticket senken

> **NAK-236** | **[Härtung · Pflege · Kommentare]** **32 Kommentar-Bezeichner
> ohne Entsprechung im Code** (Ratsche 32 seit NAK-224, davor 35; Ziel 0) […]
> Rund ein Drittel sind Fremdnamen (Heuristik-Fehlerklasse, Manifest NAK-223
> §2.5) und bleiben in der Zahl. Lügende Kommentare gehören in den jeweiligen
> Pflegeschritt der Datei

> **NAK-237** | **[Härtung · Rust · unsafe · S25c]** **undocumented_unsafe_blocks
> = deny in `broker/Cargo.toml` wird von keinem Bein gefahren.** […]
> Sicherheitsnah — Bearbeitung nur delegiert (Worker oder `/rust-review`), nie
> im Dirigentenkontext (User-Wort 01.09.2026)

> **NAK-238** | **[Lücke · Pflege · S25d]** **Sechs Dateien über dem Ziel 1 500
> Zeilen** (Werkzeugzählung nach NAK-224): `experiment_verdrahtung.rs` 1 892,
> `SourcesModel.cpp` 1 864, `broker/src/server.rs` 1 841, `experiment.rs`
> 1 727, `PluginEditor.cpp` 1 665, `PluginProcessor.h` 1 524. […] Die drei
> Plugin-Dateien gehören zu S25d (NAK-225)

Von NAK-238 fallen unter dieses Ticket **nur die Dateien über der Grenze
2 000**. `SourcesModel.cpp` (1 864), `PluginEditor.cpp` (1 665) und
`PluginProcessor.h` (1 524) liegen unter der Grenze und bleiben in NAK-238 für
einen späteren Pflegeschritt. NAK-235 wird nur insoweit berührt, als die
Ratsche nach dem Schnitt auf den neuen Iststand gesenkt wird.

---

## 2. Schnittplan

Vor dem ersten Schnitt geschrieben, aus dem Code am Basis-SHA abgeleitet.

### 2.1 Iststand am Basis-SHA

Gemessen in dieser Session mit `py -3.13 tools/plan/gesundheit.py`
(Rohausgabe `docs/beweise/roh/NAK-225-gesundheit-vorher.txt`) sowie mit
`git grep` und `wc -l`.

| Datei | Zeilen | Struktur |
|---|---|---|
| `eq-copilot/plugin/src/PluginProcessor.cpp` | 4 389 | `namespace eqcop`; drei anonyme Namensräume (21–79, 1846–1857, 2946–2984) mit zehn Helfern; 88 Memberdefinitionen von `EqCopilotProcessor` |
| `eq-copilot/plugin/core/analysis/FeatureEngine.h` | 4 390 | Header-only. Zeit-, Event- und Bandvertrag 85–921; `class FeatureEngine` 922–4390 mit `public:` 924–1591, `private:` ab 1592, verschachtelt `Stufe` 1595, `Support` 1676, `Akku`/`Breite` 4186, `StereoAkku` 4209, `Passagenfenster` 4293 |
| `eq-copilot/plugin/core/ipc/ControlClient.cpp` | 2 475 | `namespace nakama::ipc`; anonymer Namensraum 19–399 mit 21 Helfern und Konstanten; freie Vertragsfunktionen 401–665; Pimpl `struct ControlClient::Laufzeit` 666–856; Memberdefinitionen 858–2470 |

Einbindung am Basis-SHA: `src/PluginProcessor.cpp` steht **14-mal wörtlich**
in `eq-copilot/plugin/CMakeLists.txt` (Hauptziel Zeile 107 und 13 Testziele),
`core/ipc/ControlClient.cpp` **einmal** in `NAKAMA_KERN_QUELLEN` (Zeile 174),
`FeatureEngine.h` wird von **16 Dateien** direkt eingebunden.

### 2.2 Regel, nach der geschnitten wurde

1. **Die Wurzeldatei behält Namen und Pfad.** Kein `git mv`; die Historie des
   Audio-Pfads bleibt auf `PluginProcessor.cpp`, die des Verbindungsaufbaus
   auf `ControlClient.cpp`, die des Vertrags auf `FeatureEngine.h`.
2. **Ein Item wandert dorthin, wo sein Fachbereich liegt** — nicht dorthin, wo
   es zufällig steht. Maßstab ist der Fachbereich aus dem Ticket, nicht die
   Zeilennummer.
3. **Ein Helfer wandert mit seinem einzigen Nutzer.** Nur ein Helfer, den
   mehrere Teile brauchen, geht in den internen Kopf — genau einmal, `inline`,
   in einem eigenen Namensraum. Nichts wird kopiert.
4. **Funktionskörper sind textgleich.** Verschoben werden Zeilen, nicht Logik;
   geändert werden nur `#include`-Zeilen, Sichtbarkeit und Bindung (§2.6
   einzeln) und Dateiköpfe.
5. **Kein Teil über dem Ziel 1 500 Zeilen.** Wo eine Familie darüber läge,
   wird sie geteilt; die Begründung steht in der jeweiligen Tabelle.

### 2.3 `PluginProcessor.cpp` — vier Fachbereiche

Der Audio-Pfad und der Aufbau bleiben in der Wurzel. Herausgelöst werden vier
Übersetzungseinheiten unter `eq-copilot/plugin/src/prozessor/` (Zielpfad, beim
Schreiben dieses Plans noch nicht angelegt).

| Item alt (Zeilen am Basis-SHA) | Zieldatei |
|---|---|
| Includes 1–17, `namespace eqcop` | Wurzel `src/PluginProcessor.cpp` |
| anon `projektEnde` 23–29, `projektAbstandGroesserAls64` 31–39 | Wurzel (nur von `processBlock` gebraucht) |
| anon `brokerStartMutexName` 66–71, `brokerServerErwartung` 73–79 | Wurzel (nur vom Konstruktor gebraucht) |
| anon `uuidHex32` 41–51 | `src/prozessor/Intern.h` — **einziger geteilter Helfer** (Wurzel 87, Ipc 2034/2052/4087, State 2768, Analyse 3390/3391/3442/3481/3522) |
| Konstruktor 81–316, Destruktor 318–363 | Wurzel |
| `nachlaufFristSetzen` 364–404, `nachlaufAbgelaufen` 406–421, `mitschnittZustellen` 422–438, `mitschnittVerwerfen` 440–453 | Wurzel (Quarantäne-Nachlauf des Audio-Pfads) |
| `processBlock` 581–986, `nakamaBlockEmpfangen` 987–1045, `lebenszeichen` 1046–1107 | Wurzel (Audio-Thread) |
| `prepareToPlay` 455–568, `isBusesLayoutSupported` 570–579, `createEditor` 4384–4389, `meldeHostDirty` 2586–2594, `setzeEditorOffen` 2728–2738 | `src/prozessor/Hostbruecke.cpp` |
| `neueSensorId` 2571–2584, `getStateInformation` 2595–2599, `setStateInformation` 2601–2664, `holeSensorId`/`holeRolle`/`holeLabel`/`holePaarId` 2666–2669, `stateNurLesen` 2671–2675, `holeStateHerkunft` 2677–2681, `holeStateGrund` 2683–2687, `holeStateFremdesMajor` 2689–2693, `holeZustandKopie` 2695–2701, `spiegleKlassifikation` 2703–2714, `holeKlassifikation` 2716–2720, `darfBrokerStarten` 2722–2726, `setzeBindung` 2740–2796, `merkeManuellePassage` 2866–2902, `vergissManuellePassage` 2904–2944, `manuellePassagen` 3607–3640, `setzeQuellenrolle` 3837–3864, `entferneQuellenrolle` 3866–3890, `schuetzeQuelle` 3892–3918, `hebeQuellenschutzAuf` 3920–3946, `setzeQuellenbeziehung` 3948–3975, `speichereQuellenGleichrangigkeit` 3977–3982, `entferneQuellenbeziehung` 3984–4008, `sourceIntents` 4010–4014, `intentSchutzangaben` 4016–4020, `intentBeziehungen` 4022–4026, `intentBestandRevision` 4028–4032, `wirkendeQuellenrolle` 4034–4044, `entmaskierungErlaubt` 4046–4052 | `src/prozessor/State.cpp` |
| anon `alsHex32` 53–64, anon `wireZahl` 1846–1857; `interventionsWireJson` 1372–1414, `interventionenSenden` 1416–1756, `v3Hello` 1758–1780, `v3Status` 1782–1816, `v3TelemetryHello` 1818–1826, `v3SubscribeJson` 1828–1844, `v3IntentUpdateJson` 1859–1964, `v3AssistantStepJson` 1965–2004, `v3UserVerdictJson` 2005–2086, `urteilMitFrischemKopf` 2087–2115, `sendeIntentVollbestand` 2116–2132, `sendeIntentFortschreibung` 2134–2156, `v3ControlLink` 2224–2434, `v3Antwort` 2436–2474, `v3Frame` 2476–2482, `sourcesTick` 2484–2491, `reconnectSources` 2493–2496, `statsSnapshot` 2498–2521, `messKompakt` 2522–2570, `bindeSourcesHauptziel` 2798–2801, `benenneSourcesHauptziel` 2803–2827, `entferneSourcesHauptziel` 2829–2864, `sendeSourcesCommand` 4054–4114, `wendeBestaetigteSourcesCommandsAn` 4116–4161, `ausstehenderSourcesCommandFuerTest` 4162–4172 | `src/prozessor/Ipc.cpp` |
| anon `fingerprintJson`/`zahl`/`jsonText` 2946–2984; `workerLauf` 1109–1371, `setzeAssistentSchritt` 2158–2206, `assistentSchrittKopie` 2208–2222, `bindePassagenfenster` 2986–3005, `bindePassagenfensterMitEpoche` 3007–3040, `passagenfensterWunschFuerTest` 3042–3049, `passagenfensterFuehrt` 3051–3063, `versuchAufgenommeneBloecke` 3065–3068, `vergleichszustandLeeren` 3070–3118, `loesePassagenfenster` 3120–3137, `engineabzugLesen` 3139–3154, `passagenfensterInEngine` 3156–3167, `versuchReferenzJson` 3169–3242, `versuchKopfJson` 3244–3269, `sendeVersuchP0` 3271–3278, `beginneVersuch` 3280–3422, `erfasseKandidat` 3424–3453, `urteileVersuch` 3455–3511, `brichVersuchAb` 3513–3536, `laufenderVersuch` 3538–3542, `versuchLautheitAbgeglichen` 3544–3565, `versuchMatchGainDb` 3567–3577, `versuchLautheitAbgeglichenLebendFuerTest` 3579–3582, `vergleichspegelZaehlerstand` 3584–3600, `versuchNichtEndlicheSamples` 3602–3605, `assistentAenderungMelden` 3641–3660, `assistentStarten` 3662–3676, `assistentWeiter` 3678–3695, `assistentZurueck` 3697–3709, `assistentUeberspringen` 3711–3723, `assistentAbbrechen` 3725–3747, `assistentAntwort` 3749–3793, `assistentAusState` 3795–3799, `assistentFortsetzen` 3801–3805, `assistentVersuchStarten` 3807–3835, `schreibeSnapshotDatei` 4173–4382 | `src/prozessor/Analyse.cpp` |

### 2.4 `FeatureEngine.h` — Vertrag plus Merkmalsfamilien

`FeatureEngine.h` bleibt der einzige Kopf, den die 16 Einbinder kennen; keine
`#include`-Zeile in `tests/`, `sonde/` oder `src/` ändert sich. Die
Klassendeklaration bleibt in `FeatureEngine.h`; die Definitionen wandern als
`inline`-Definitionen außerhalb der Klasse in Teilköpfe unter
`eq-copilot/plugin/core/analysis/featureengine/` (Zielpfad, beim Schreiben
dieses Plans noch nicht angelegt), die `FeatureEngine.h` einbindet. Keine Definition wandert in eine `.cpp`.

| Item alt (Zeilen am Basis-SHA) | Zieldatei | Einbindestelle |
|---|---|---|
| Dateikopf und Includes 1–84 | `FeatureEngine.h` | — |
| Vertrag 85–921: `Grenzgrund`, die `kG`-Konstanten, `Zeitbasis`, `Herleitung`, `Transportstempel`, `nak29Verstoss`, `Frameversuch`, `frameversuch`, `Ereignis`, `Fingerprint`, `kFingerprintMindestFenster`, `fingerprintAehnlichkeit`, `StereoBandwert`, `StereoSkalare`, `Bandsatz`, `LiveBaender`, `EvidenzBaender`, `FeatureFrame`, `kFeatureMetricsVersion` und die Schwellenkonstanten, `VerteilungsRing` | `featureengine/Vertrag.h` | **vor** der Klasse |
| `class FeatureEngine`: alle Deklarationen, die verschachtelten Typen `Stufe`, `Support`, `Akku`, `Breite`, `StereoAkku`, `Passagenfenster` und alle Datenfelder | `FeatureEngine.h` | — |
| Definitionen des `public`-Teils, soweit mehrzeilig: `vorbereiten`, `zuruecksetzen`, `nimmBlock`, `stereoHatInhalt`, `setzePassagenfenster`, `loeschePassagenfenster` und die übrigen mehrzeiligen Zugriffsfunktionen | `featureengine/Lebenszyklus.h` | nach der Klasse |
| Definitionen 1686–2249: `projektVorwaerts`, `supportVereinen`, `supportMerken`, `trennIndex`, `zuordnung`, `auskunftBestimmen`, `grenzeZwischen`, `istLoopWrap`, `ppqProSampleVon`, `moeglicherStraddleIn`, `grenzeZiehen`, `blockProjektSpanneGueltig`, `blockContinuousSpanneGueltig`, `evidenzContinuousBelegen`, `rahmenZeitBelegen` | `featureengine/Zeit.h` | nach der Klasse |
| Definitionen 2251–2895: `verarbeiteSamples`, `schiebeStufe`, `rechneFenster`, `summeBereich`, `flussSchritt`, `medianDerHistorie`, `ereignisAblegen` | `featureengine/Spektrum.h` | nach der Klasse |
| Definitionen 2896–3187: `stereoSchritt`, `stereoSample`, `stereoAuswerten`, `stereoLeeren` | `featureengine/Stereo.h` | nach der Klasse |
| Definitionen 3188–3337: `fingerprintSchritt`, `fingerprintJetzt`, `fingerprintLeeren` | `featureengine/Fingerprint.h` | nach der Klasse |
| Definitionen 3339–3548: `zelleSchliessen`, `lraBin`, `kurzLufs`, `momentanLufs`, `kurzTruePeak`, `crestKurz`, `lraLu`, `lraBinMitte`, `lraPerzentil` | `featureengine/Lautheit.h` | nach der Klasse |
| Definitionen 3550–4184: `baueFrame`, `evidenzLeeren`, `rahmenLeeren`, `baueStempel`, `fuelleLive`, `fuelleEvidenz`, `ringInZeitfolge`, `perzentil`, `fuelleVerteilung`, `fuelleAbdeckungUndKonvergenz`, `fuelleSkalare` | `featureengine/Frame.h` | nach der Klasse |

Drei Regeln, die bei einer header-only-Klasse ABI- und Overload-relevant sind
und deshalb ausdrücklich gelten:

- **Die Reihenfolge der Deklarationen in der Klasse bleibt unverändert.** Eine
  Definition außerhalb der Klasse ändert die Reihenfolge nicht; sie steht
  hinter der vollständigen Klassendeklaration.
- **Im Klassenkörper definierte Memberfunktionen sind implizit `inline`.**
  Wandert die Definition nach außen, muss sie das Schlüsselwort `inline`
  ausdrücklich tragen, sonst bricht das Linken über 16 Übersetzungseinheiten.
- **`static` gehört nur in die Deklaration**, nie in die Out-of-class-Definition;
  Standardargumente ebenfalls nur in die Deklaration. `noexcept` und
  `constexpr` bleiben an beiden Stellen wörtlich wie im Bestand.

### 2.5 `ControlClient.cpp` — drei Fachbereiche

| Item alt (Zeilen am Basis-SHA) | Zieldatei |
|---|---|
| Kopf und Includes 1–16, `namespace nakama::ipc` | Wurzel `core/ipc/ControlClient.cpp` |
| Anonymer Namensraum 19–399 vollständig (`kSchluesselStateReport`, `kLeseTaktMs`, `kJsonSafeModulus`, `kStopFristMs`, `CommandAckArt`, `istHex64`, `nichtnegativeJsonGanzzahl`, `fehlercodeHaeltVertrag`, `jsonStringToken`, `commandIdAusAuftrag`, `commandAckArtLesen`, `jsonString`, `jsonStringUntrusted`, `utf8CodepointsBis`, `jsonSafe`, `boolJson`, `capabilitiesJson`, `stateHashJson`, `runtimeJson`, `stateReportJson`, `zahl`) | `core/ipc/controlclient/Intern.h` — die Helfer werden von **allen vier** Teilen gebraucht (Messung: `kStopFristMs` in Wurzel und `Laufzeit`; `CommandAckArt`, `commandIdAusAuftrag`, `commandAckArtLesen` in Vertrag und Nachrichten; `kSchluesselStateReport`, `kLeseTaktMs`, `kJsonSafeModulus`, `stateReportJson` in Verbindung) |
| `struct ControlClient::Laufzeit` 666–856 | `core/ipc/controlclient/Laufzeit.h` — die drei Teile definieren `ControlClient::Laufzeit::`-Member und brauchen die vollständige Deklaration |
| `ControlClient::ControlClient` 858–874, `~ControlClient` 876–879, `start` 881–896, `stop` 898–953, `reconnect` 955–971, `snapshot` 973, `statusProviderGesetzt` 975–978, `setzeKonfliktWiederholungHook` 990–996, `setzeAuftragAbgeschlossenHook` 998–1004, `setzeP0Rueckmeldung` 1006–1016, `setzeReplayBeginHook` 1018–1022, `setzeAufbauZugHakenFuerTest` 1024–1027, `setzeProbeGegenstelleFuerTest` 1029–1059 | Wurzel `core/ipc/ControlClient.cpp` |
| Freie Vertragsfunktionen 401–665: `commandAckHaeltVertrag`, `auftragMitBasisRevision`, `helloAlsJson`, `heartbeatAlsJson`, `welcomeHaeltVertrag`, `rejectHaeltVertrag`, `audioGueltig`, `istHex32`, `adresseAlsJson`, `adresseGueltig`, `instanceAdresseAusState`, `wireAdresseAusState`, `instanceAliasZielPasst` | `core/ipc/controlclient/Vertrag.cpp` (Fehlercodes, Vertragsprüfung, Wire-Form) |
| `Laufzeit::sendeP0` 1217–1256, `Laufzeit::sendePersistenzP0` 1258–1330, `Laufzeit::inFlightNachReconnect` 1332–1356, `Laufzeit::inFlightNachWireWrite` 1358–1369, `Laufzeit::inFlightAck` 1371–1474, `Laufzeit::sendeP1` 1476–1495, `Laufzeit::snapshotIntern` 1497–1501, `ControlClient::sendeP0` 980–983, `gemeldeteStateRevision` 985–988, `sendePersistenzP0` 1197–1200, `sendeP1` 1202–1205, `interventionsZug` 1147–1195, `fuelleP0QueueFuerTest` 1753–1761, `leereP0QueueFuerTest` 1763–1769, `zustelleAllesFuerTest` 1771–1797 | `core/ipc/controlclient/Nachrichten.cpp` |
| `Laufzeit::sollAbbrechen` 1212–1215, `Laufzeit::kopplung` 1503–1520, `ControlClient::kopplung` 1207–1210, `Laufzeit::aufbauAussageZurueckstellen` 1521–1571, `Laufzeit::heartbeatSchritt` 1572–1626, `Laufzeit::aufbauZug` 1628–1684, `ControlClient::aufbauZug` 1686, `tlAufbauGeneration` 1060, `meldeAufbauUrteil` 1062–1113, `loescheAufbauUrteil` 1115–1123, `wireGenerationJetzt` 1125–1139, `tlLinkEndeGeneration` 1140, `sterbendeGenerationJetzt` 1142–1145, `linkAufbauFuerTest` 1688–1702, `linkEndeFuerTest` 1704–1717, `heartbeatSchrittFuerTest` 1719–1726, `heartbeatTextFuerTest` 1728–1751, `Laufzeit::meldeLinkStatus` 1799–1833, `Laufzeit::threadLauf` 1835–1888, `Laufzeit::eineVerbindung` 1890–2470 | `core/ipc/controlclient/Verbindung.cpp` |

Der K1-Riegel (`NakamaKernRiegel.h` am Anfang **und** am Ende jeder
Kern-Übersetzungseinheit) gilt für jede neue `controlclient/`-Quelle; die
Teilköpfe sind keine Übersetzungseinheiten und tragen ihn nicht.

### 2.6 Sichtbarkeits- und Bindungsänderungen, einzeln

Jede Zeile nennt, was sich an Sichtbarkeit oder Bindung ändert, und warum sie
ohne diese Änderung nicht ginge. Alles andere ist reine Verschiebung.

| Was | Vorher | Nachher | Grund |
|---|---|---|---|
| `uuidHex32` (Prozessor) | anonymer Namensraum in `PluginProcessor.cpp`, interne Bindung | `inline` in `namespace eqcop::prozessor_intern` (`src/prozessor/Intern.h`), externe Bindung, **eine** Entität | Vier Übersetzungseinheiten brauchen ihn (Wurzel, Ipc, State, Analyse). Ein anonymer Namensraum im Kopf hätte je Einheit eine eigene Kopie erzeugt. Die Nutzer holen ihn mit einer **using-Deklaration** einzeln nach `eqcop` — kein `using namespace`, damit jede Teildatei im Kopf zeigt, was sie von dort nimmt. |
| `projektEnde`, `projektAbstandGroesserAls64`, `brokerStartMutexName`, `brokerServerErwartung` | anonymer Namensraum der Wurzel | unverändert anonym, in der Wurzel | Nur die Wurzel nutzt sie (gemessen). |
| `alsHex32`, `wireZahl` | anonymer Namensraum der Wurzel | unverändert anonym, jetzt in `src/prozessor/Ipc.cpp` | Nur die IPC-Anbindung nutzt sie (gemessen). |
| `fingerprintJson`, `zahl`, `jsonText` | anonymer Namensraum der Wurzel | unverändert anonym, jetzt in `src/prozessor/Analyse.cpp` | Nur der Analyse-Anstoß nutzt sie (gemessen). |
| Die 49 privaten Memberfunktionen von `FeatureEngine` | im Klassenkörper definiert, damit **implizit** `inline` | Deklaration in der Klasse, Definition außerhalb mit **ausdrücklichem** `inline` | Ohne `inline` bräche das Linken über 16 Übersetzungseinheiten. `static` steht nur noch in der Deklaration — eine Out-of-class-Definition darf es nicht wiederholen. |
| `FeatureEngine::lraBinMitte` | `static constexpr` im Klassenkörper | Deklaration `static constexpr`, Definition `constexpr` ohne `inline` | `constexpr` schließt `inline` ein; ein zusätzliches `inline` wäre redundant. |
| Der Vertrag von `FeatureEngine.h` (Zeilen 85–920 des Urstands) | im selben Kopf wie die Klasse | `core/analysis/featureengine/Vertrag.h`, eingebunden **vor** der Klasse | Die Klasse führt seine Typen in Feldern und Signaturen; er muss vor ihr stehen. Der Teilkopf verweigert die eigenständige Einbindung. |
| Die 21 Helfer und Konstanten des anonymen Namensraums von `ControlClient.cpp` | anonymer Namensraum, interne Bindung; die Konstanten `constexpr` (damit ebenfalls intern) | `inline` beziehungsweise `inline constexpr` in `namespace nakama::ipc::controlclient_intern` (`core/ipc/controlclient/Intern.h`) | Alle vier Teile lesen aus ihnen (gemessen). `inline constexpr` macht aus je einer Kopie pro Einheit **eine** Entität. Die Teilquellen holen sie mit `using namespace controlclient_intern;` **innerhalb der `.cpp`** — die Aufrufstellen bleiben damit wörtlich wie im Urstand. |
| `ControlClient::Laufzeit` | im Rumpf von `ControlClient.cpp` | `core/ipc/controlclient/Laufzeit.h` | Drei Übersetzungseinheiten definieren `Laufzeit::`-Member und brauchen die vollständige Deklaration. Der Kopf ist intern; die Regel aus `ControlClient.h` (der Client hält nur den `shared_ptr`) bleibt unverändert. |
| `kStopFristMs` in `Laufzeit.h` | anonymer Namensraum | `using controlclient_intern::kStopFristMs;` **innerhalb** von `namespace nakama::ipc` in `Laufzeit.h` | Der Rumpf misst die Stoppfrist an zwei Stellen. Eine using-**Deklaration** statt einer Direktive, damit ein interner Kopf nicht den ganzen Helfersatz in `nakama::ipc` schiebt. |
| `tlAufbauGeneration`, `tlLinkEndeGeneration` | `thread_local` auf Namensraumebene in `ControlClient.cpp` | unverändert `thread_local`, jetzt in `core/ipc/controlclient/Verbindung.cpp` | Alle Verwendungsstellen liegen in dieser Übersetzungseinheit (gemessen: 1078, 1133, 1144, 1691, 1700, 1711–1715, 1814–1832, 2123–2125 des Urstands). |
| `#include`-Zeilen | je Wurzeldatei die Vollmenge | je Übersetzungseinheit der gemessene Bedarf | Kein Kopf, der nicht gebraucht wird. Der Bau ist der Beweis: ein fehlender Kopf ist ein Übersetzungsfehler. |

**Nicht geändert:** `eq-copilot/plugin/src/PluginProcessor.h` und
`eq-copilot/plugin/core/ipc/ControlClient.h` haben **0 geänderte Zeilen**. Der
Compiler hat keine erzwungen: die Aufteilung kommt ohne eine einzige Zeile in
den öffentlichen Köpfen aus.

### 2.7 CMake und der A14-Kernriegel

- `src/PluginProcessor.cpp` steht 14-mal wörtlich im `CMakeLists.txt`. Diese
  14 Stellen werden durch **eine** Quellliste `NAKAMA_PROZESSOR_QUELLEN`
  ersetzt, im ersten Codecommit des Tickets. Danach kommt jeder weitere Teil
  an genau einer Stelle hinzu; jedes Ziel, das vorher die Wurzel übersetzte,
  übersetzt danach dieselbe Menge Teile.
- Die neuen `controlclient/`-Teile kommen in `NAKAMA_KERN_QUELLEN`. **Damit
  ändert sich die handgeführte Objektliste `ERWARTETE_OBJEKTE` in
  `tools/eq-copilot/pruefe_kern_identitaetsfrei.py`** — Bein A14 prüft in [S6],
  dass `NAKAMA_KERN_QUELLEN` und `ERWARTETE_OBJEKTE` sich decken, und in [3],
  dass das Archiv genau diese Objekte enthält. Quellliste und Objektliste
  gehören deshalb in denselben Änderungssatz; das ist dieselbe Beziehung wie
  Quellliste ↔ Ziel und der Grund, warum diese Datei in den Ticketpfaden steht.
- Die Zielmenge bleibt gleich: `docs/beweise/roh/NAK-225-ziele-vorher.txt`
  gegen `-nachher.txt`, §3 R4.

---

## 3. Riegelkarte R1–R10

Jede Zeile mit Befehl, Wert am Basis-SHA und Wert am Endstand; jeder Wert aus
einem Lauf dieser Session, jeder Rohbeleg unter `docs/beweise/roh/`.

| Riegel | Befehl | Vorher (Basis-SHA `0ca392b5`) | Nachher (Endstand) |
|---|---|---|---|
| R1 Kanon | `pwsh -File tools/beweise.ps1 -Bauen …` | **GRÜN 62/62, Exit 0** auf `41f4984` (NAK-224, `roh/NAK-224-41f4984.md`); `git diff --stat 41f4984..0ca392b5 -- eq-copilot broker tools` **leer** | **GRÜN 62/62, Exit 0** auf `f4d497b3` (`roh/NAK-225-f4d497b.md`), abgekoppelt, sauberer Baum. Gleiche Beinzahl (62). |
| R1b Beinzahlen | Prüfungszahlen je Bein aus der Rohausgabe | 62 Beine, 2 575 ok-Zeilen, Summen je Bein — `roh/NAK-225-beinzahlen.txt` | 62 Beine, **2 580 ok-Zeilen**. `diff` der beiden Tabellen: **eine einzige Zeile** — A32 116 → 121, die fünf neuen Selbsttestfälle. Jedes andere Bein meldet exakt dieselbe Prüfungsmenge (A5 613, B10 381, B1 120, B2 180, B8 79, A16 87, B3b 85, A14 25). `roh/NAK-225-beinzahlen.txt`. |
| R2 Nulltest/Passthrough | A1 `EqCopNullTest`, A16 `EqCopProbeeqNullTest`, B3b `EqCopHostProbeTest` | A1 Exit 0 (10 Prüfungen, NULLTEST OK), A16 Exit 0 (SONDE-NULLTEST OK - 87 Pruefungen ok, 0 Fehler), B3b Exit 0 (HOSTPROBE OK - 85/85 Pruefungen bestanden) | Im Abschlusskanon je Exit 0: A1 (0,06 s), A16 (0,07 s), B3b (0,07 s) — dieselben Prüfungszahlen wie vorher. B3b misst dabei 0 Allokationen im Audio-Thread. |
| R3 Sicht | `EqCopShot --sonde012-suite …` | 25 Bilder, SHA-256 in `roh/NAK-225-shots-vorher.txt`; **zwei Läufe am Basis-SHA byteweise gleich** (`-vorher-b.txt`) — das Rendern ist deterministisch, die Gleichheit unten ist also eine Aussage über den Schnitt | **25 Bilder, byteweise gleich dem Basisstand** (`roh/NAK-225-shots-nachher.txt`, `diff` leer). Zusätzlich nach c4 und nach e3 einzeln gemessen, jedes Mal gleich. |
| R4 Testbestand | Vcxproj-Ziele im Baum; Kanon-Bauziele | 120 Vcxproj-Ziele, 40 Kanon-Bauziele — `roh/NAK-225-ziele-vorher.txt` | **120 und 40, unverändert** (`roh/NAK-225-ziele-nachher.txt`, `Compare-Object` leer). Jedes Testziel baut; keine Prüfung fällt weg (R1b). |
| R5 Diff-Riegel | `git diff --stat <Basis>..HEAD` je Bereich | — | **`roh/NAK-225-diffriegel.txt`:** `identity` 0, `schemas` 0, `fixtures` 0, `hostbridge` 0, `third_party` 0, `broker` 0, `docs/plan` 0, `offene-punkte.md` 0, `PLAN-STAND.md` 0, `CLAUDE.md` 0, `design` 0, `.claude` 0. **Eine Abweichung:** `eq-copilot/plugin/tests/IpcTestMain.cpp` +8/−2 — begründet in §2.6 und im Commit zu e3. |
| R6 Historie | `git log --follow`, `git log --oneline <Basis>..HEAD` | — | **`roh/NAK-225-historie.txt`:** 0 Umbenennungen im Ticketbereich für alle drei Wurzeldateien, `git log --follow` bleibt trivial (41, 22, 31 Commits vor dem Basis-SHA). 20 Commits zum Messzeitpunkt, je herausgelöstem Teil einer; der Abschluss-Commit mit diesem Manifest kommt danach. |
| R7 Dateiköpfe | Kopf gegen Inhalt, dreistufig | — | **`roh/NAK-225-dateikoepfe.txt`: 20 Dateien, 0 Bezeichner ohne Entsprechung.** Je Kopf: HIER (Name steht im Rumpf), VERWEIS (Name steht in einer anderen Datei des Schnitts — die Zieldatei wird genannt), LEER (Name existiert nirgends). LEER = 0. |
| R8 Audio-Thread | Text je Funktion gegen den Basisstand | — | **`roh/NAK-225-audiopfad.txt`: 11 Funktionen, 0 Abweichungen.** `processBlock` (406), `nakamaBlockEmpfangen` (59), `lebenszeichen` (62), der Quarantäne-Nachlauf (4 Funktionen) und die beiden anonymen Rechenhelfer liegen byteweise unverändert in der Wurzel; `prepareToPlay` (114) und `isBusesLayoutSupported` (10) byteweise unverändert in `prozessor/Hostbruecke.cpp`. Dateireihenfolge erhalten. Keine neue Indirektion, kein virtueller Aufruf, keine Allokation — B3b misst 0 Allokationen im Audio-Thread. |
| R9 `gesundheit.py` | `py -3.13 tools/plan/gesundheit.py --mit-selbsttest` | Exit 4: über 2 000 = **3**, über Ziel 1 500 = **6**, Funktionen über 200 = **28**, Kommentar-Bezeichner = **32**, `dead_code` = 0, Kontextflächen OK, Selbsttest 116/116 — `roh/NAK-225-gesundheit-vorher.txt` | **Exit 0: über 2 000 = 0**, über Ziel 1 500 = 6 (dieselben sechs Dateien, alle ohne Anspruch aus S25d), Funktionen über 200 = **28** (gleich; die sechs genannten sind gleich lang oder kürzer), Kommentar-Bezeichner = **30**, Selbsttest **121/121** — `roh/NAK-225-gesundheit-nachher.txt`. Ratschen nur gesenkt (32 → 30), Pflegeticket-Liste leer und gegen stilles Veralten gesichert (Rotbeweis `roh/NAK-225-rotbeweis-pflegeticket.txt`), A32-Behauptung im Runner nachgezogen, `--clippy` unberührt (Broker 0 Zeilen). Kein neues Modul über 1 500. |
| R10 Bau je Commit | `cmake --build … --config Release --target …` | Neubau der drei Wurzeldateien und aller Abhängigen: **Exit 0, 72 `warning C`** — `roh/NAK-225-warnungen-vorher.txt` | **Jeder der 18 Codecommits gebaut, Exit 0.** Gleich gemessener Vergleichslauf am Endstand: **Exit 0, 124 `warning C`** — dieselben vier Warncodes, dieselben 30 (Ziel, Datei, Warncode)-Paare und **genau eine eigene Warnstelle in beiden Läufen** (dieselbe Codezeile in `stereoSchritt`, vorher `FeatureEngine.h(2914)`, jetzt `featureengine/Stereo.h(70)`). MSVC meldet eine Kopfwarnung einmal je Übersetzungseinheit; der Prozessor ist seit S25d fünf statt einer — daher die höhere Rohzahl ohne neue Warnung. `roh/NAK-225-warnungen-nachher.txt`. |

### 3.1 Was aus den drei Dateien geworden ist

| Wurzel | vorher | nachher | Teile |
|---|---:|---:|---|
| `src/PluginProcessor.cpp` | 4 389 | **993** | `prozessor/Hostbruecke.cpp` 187, `prozessor/State.cpp` 586, `prozessor/Ipc.cpp` 1 386, `prozessor/Analyse.cpp` 1 449, `prozessor/Intern.h` 49 |
| `core/analysis/FeatureEngine.h` | 4 390 | **1 314** | `featureengine/Vertrag.h` 760, `Zeit.h` 601, `Spektrum.h` 650, `Stereo.h` 318, `Fingerprint.h` 199, `Lautheit.h` 239, `Frame.h` 668 |
| `core/ipc/ControlClient.cpp` | 2 475 | **247** | `controlclient/Vertrag.cpp` 312, `Nachrichten.cpp` 452, `Verbindung.cpp` 1 080, `Intern.h` 420, `Laufzeit.h` 225 |

Keine Datei über 2 000, keine neue über dem Ziel 1 500. Die größte neue Datei
ist `prozessor/Analyse.cpp` mit 1 449 Zeilen.

---

## 4. Prüfliste (`tools/dirigent/pruefliste.md`)

Vor jedem Commit abgehakt; hier steht je Zeile, **wo** sie gemessen ist.

| Klasse der Prüfliste | Trifft zu? | Wo gemessen |
|---|---|---|
| A. Rückstau und Prioritätsklassen (IPC, Queues) | nein (nur Verschiebung) | Keine Politik, kein Puffer, kein Zähler geändert. Die Klassen P0/P1/P2 stehen textgleich in `controlclient/Nachrichten.cpp` — Pruefskript 71 Blöcke, 0 Fehler. Gemessen: B10 `EqCopIpcTest` 381 Prüfungen vorher wie nachher, A22 Lastbein im Kanon (§3 R1). |
| B. Lebenszyklus (verbinden/trennen, starten/stoppen) | nein (nur Verschiebung) | `start`/`stop`/`reconnect` bleiben in der Wurzel, `threadLauf`/`eineVerbindung`/`meldeLinkStatus` wandern textgleich nach `controlclient/Verbindung.cpp`. Die Wache `abbruchsignal_gehoert_der_generation/control` liest Quelltext und ist mitgezogen (§2.6, §5 NB-3) — Prüfname und Prüfungszahl unverändert. |
| C. Verträge und Längen | **ja, gefunden** | Kein Schema und kein Fixture geändert (§3 R5, je 0 Zeilen). **Der Kanon hat trotzdem einen Vertragsbruch gefunden:** `metriken-v1.json` bindet je Schwelle die DATEI, in der die Zahl steht; der erste Vertragsschnitt nahm sie mit. Behoben am Schnitt, nicht am Register — §6 h2, §5 NB-1. |
| D. Bau- und Prüfriegel (CMake, Runner) | **ja** | Quelllisten §2.7; A14-Objektliste im selben Änderungssatz (A14 grün, 25 Prüfungen); Zielmenge vorher = nachher (§3 R4); K1-Riegel am Anfang und Ende jeder neuen Kernquelle (§3 R7, A14 [3]). Die neue Wache in `gesundheit.py` ist fail-closed und einmal absichtlich gebrochen (`roh/NAK-225-rotbeweis-pflegeticket.txt`). |
| E. Behauptung ≤ Messung | **ja** | Jede Zahl in diesem Manifest stammt aus einem Lauf dieser Session; je Riegel ein Rohbeleg unter `docs/beweise/roh/`. Geänderte Zusage → drei Stellen (§6 g): A32-Behauptung im Runner, Skriptkopf `gesundheit.py`, dieser Manifestkopf. **Eine Korrektur:** die Commitnachricht zu d0 nennt „<40 Kanonziele>"; gebaut wurde die R10-Zielmenge (12 Ziele). Ab d1 nennt jede Nachricht die richtige Menge; `--amend` ist im Projekt verboten, deshalb steht die Korrektur hier. |
| F. Änderungssatz | **ja** | Quellliste ↔ Teil ↔ Ziel ↔ A14-Objektliste je in einem Commit (b, c1–c4, e1–e3). Ratsche ↔ Iststand ↔ Pflegeticket-Liste ↔ A32-Behauptung in einem Commit (g). Speichern ↔ Laden in derselben Datei (`prozessor/State.cpp`), aktivieren ↔ abklingen in derselben Datei (`featureengine/Stereo.h`), verbinden ↔ trennen in derselben Datei (`controlclient/Verbindung.cpp`). |

---

## 5. Nebenbefunde

**NB-1 — Ein Register kann einen ORT binden, nicht nur einen Wert.**
`eq-copilot/schemas/v3/metriken-v1.json` führt je Schwelle die Datei, in der
die Zahl steht; Bein A5 hält beide Seiten zusammen (M-06). Der erste
Vertragsschnitt nahm `kFeatureMetricsVersion` und sechs Schwellen mit nach
`featureengine/Vertrag.h` — Compiler, Textprüfung, Nulltest und Sichtsatz
blieben grün, der Kanon fiel. Behoben am Schnitt (§6 h2). **Für den Plan:** wer
künftig eine Datei teilt, prüft vorher, ob ein Register oder eine
quelltextlesende Wache auf sie zeigt; ein `git grep` nach dem Dateinamen in
`tools/` und `tests/` hätte beide Fälle dieses Tickets vorher gefunden.

**NB-2 — `STATUS_STACK_OVERFLOW` bleibt in der Kommentar-Ratsche.**
`core/analysis/FeatureEngine.h` nennt den Windows-SEH-Code in der Begründung,
warum `headroomRing` im Heap liegt. Das ist ein Fremdname, kein
Repo-Bezeichner — die bekannte Heuristik-Fehlerklasse (Manifest NAK-223 §2.5,
Register NAK-236). Er ist nicht geändert und bleibt in der Zahl 30.

**NB-3 — Zwei Zeilen in `tests/IpcTestMain.cpp`.** Die Regressionswache
`abbruchsignal_gehoert_der_generation` liest Quelltext und musste dem Pfad
ihrer Quelle folgen. Ohne die Änderung läse sie eine Datei, in der ihr Muster
gar nicht vorkommt — still grün oder still rot. Prüfname und Prüfungszahl
sind unverändert (381 vorher wie nachher). Begründet im Commit zu e3.

**NB-4 — Kommentare in anderen Dateien zeigen auf verschobene Zeilen.**
`sonde/SondeProcessor.cpp`:354, `sonde/SondeProcessor.h`:177,
`core/StampedAudioQueue.h`:5 und `tests/IpcTestMain.cpp`:1607/1689 nennen
`PluginProcessor.cpp:<Zeile>` beziehungsweise `ControlClient.cpp:<Zeile>`. Sie
sind Verlauf, kein Ticketpfad, und bleiben unberührt (Ticketauftrag Punkt 5).
Material für NAK-236.

**NB-5 — `gesundheit.py` misst `schreibeSnapshotDatei` jetzt mit 210 statt 215
Zeilen.** Der Text der Funktion ist byteweise identisch (Prüfskript, 112
Blöcke). Der Unterschied ist eine Messkante: im Urstand zählte das Werkzeug
fünf vorangehende Kommentarzeilen mit, in `prozessor/Analyse.cpp` trennt sie
eine Leerzeile vom Signaturbeginn. Keine Funktion ist länger geworden — die
Zusage aus NAK-235 hält.

**NB-6 — Der Prozessor erzeugt dieselbe Kopfwarnung jetzt fünfmal statt
einmal.** MSVC meldet C4458 einmal je Übersetzungseinheit, die den Kopf
einbindet. Die Rohzahl steigt dadurch von 72 auf 124, ohne dass eine neue
Warnung entstünde (§3 R10). Wer die Zahl senken will, behebt die eine
Warnstelle in `stereoSchritt` — das wäre eine Verhaltensfrage und gehört nicht
in dieses Ticket.

---

## 6. Verlauf

Append-only. Je Schritt: was gemacht wurde und woran es gemessen ist.

**(a) 09.09.2026 — Iststand und Schnittplan** (`9684760b`). Basis-SHA
`0ca392b5`, Worktree sauber. Gemessen: `gesundheit.py` (Exit 4, drei Dateien
über 2 000), Beinzahlen aus dem NAK-224-Abschlusskanon (62 Beine, 2 575
ok-Zeilen), `EqCopShot`-Hashes **zweimal** am Basisstand (25 Bilder, beide
Läufe byteweise gleich), Zielmenge (120 vcxproj, 40 Kanon-Bauziele),
MSVC-Warnungen beim Neubau der drei Wurzeldateien und aller Abhängigen (72,
Exit 0). Schnittplan §2 vor dem ersten Schnitt geschrieben.

**(b) CMake-Quellliste** (`ac76a8fe`). `src/PluginProcessor.cpp` stand an
vierzehn Stellen wörtlich; jetzt steht die Menge einmal in
`set(NAKAMA_PROZESSOR_QUELLEN …)`. Zielmenge unverändert (120), Bau Exit 0,
zwölf Beine grün.

**(c1–c4) Der Prozessor, vier Fachbereiche** (`c59a9187`, `048dae92`,
`7e2780aa`, `84226d46`). Hostbrücke (187), State (586), Ipc (1 386), Analyse
(1 449); die Wurzel behält Aufbau, Abbau und den Audio-Pfad und schrumpft von
4 389 auf 993 Zeilen. Nur **ein** Helfer wird geteilt (`uuidHex32`, gemessen);
alle anderen wandern mit ihrem einzigen Nutzer und bleiben anonym. Je Commit
Bau Exit 0 und Beine grün; ein Prüfskript belegt je Commit 112 Blöcke wörtlich
identisch. Nach c4 zusätzlich: 12/12 Beine grün, `EqCopShot`-Sichtsatz
byteweise gleich dem Basisstand.

**Ein Fund unterwegs:** die erste Fassung von c3 baute nicht — die berechnete
Include-Liste ließ `Diagnose.h` in der Wurzel weg, weil das Erkennungsmuster
`kDiagnoseVersion` und `diagnose (…)` nicht traf. Der Bau hat es sofort
gemeldet, das Muster ist geschärft, c3 ist mit Exit 0 committet. Ein fehlender
Kopf ist ein Übersetzungsfehler und kann nicht still bleiben — genau deshalb
wird der Bedarf gemessen und nicht die Vollmenge kopiert.

**(d0–d6) Die Engine, Vertrag plus sechs Merkmalsfamilien** (`baa63898`,
`1412ae60`, `08bb29b2`, `1bc89a7c`, `ccd0eee9`, `94f46e8f`, `3683d716`).
`FeatureEngine.h` bleibt der einzige Kopf, den die 16 Einbinder kennen: keine
`#include`-Zeile in `tests/`, `sonde/` oder `src/` ändert sich. Die 49
privaten Memberdefinitionen wandern als `inline`-Definitionen außerhalb der
Klasse; die Deklaration bleibt an ihrer Stelle, die Reihenfolge unverändert.
Je Commit prüft ein Skript für **jedes** Mitglied dreierlei: Deklaration =
Urstand-Signatur plus Semikolon, Rumpf ausgerückt byteweise = Urstand-Rumpf,
Kommentarvorspann wörtlich — und für jedes noch nicht herausgelöste, dass es
unverändert in der Klasse steht. 49 Mitglieder plus Vertrag, 0 Abweichungen in
jedem der sieben Läufe. Nach d6: voller Bau Exit 0, 12/12 Beine grün,
Sichtsatz gleich.

**(e1–e3) Der ControlClient, drei Fachbereiche** (`053ff7f8`, `c447fc4e`,
`2faf21a5`). Vertrag (312), Nachrichten (452), Verbindung (1 080); dazu die
beiden internen Köpfe `Intern.h` (420) und `Laufzeit.h` (225). Die Wurzel
schrumpft von 2 475 auf 247 Zeilen. Prüfskript je Commit 71 Blöcke, 0 Fehler;
A14 je Commit grün (25 Prüfungen) — die Objektliste des Kern-Riegels ist im
selben Änderungssatz mitgezogen.

**Zwei Funde in e3.** Erstens fiel B1 (`EqCopIdentityTest`), weil die
R10-Zielmenge `NakamaProbeeq_VST3` nicht enthielt und dessen `moduleinfo.json`
damit älter war als das geänderte `CMakeLists.txt` — die Frischewache des
Beins bei der Arbeit, kein Defekt; die Zielmenge ist ergänzt. Zweitens fiel
B10 (`EqCopIpcTest`) mit einer Prüfung: die Regressionswache
`abbruchsignal_gehoert_der_generation/control` **liest Quelltext** und suchte
in `ControlClient.cpp` nach einem Muster, das nach `controlclient/Verbindung.cpp`
gewandert war. Zwei Zeilen in `tests/IpcTestMain.cpp` ziehen den Pfad nach;
Prüfname und Prüfungszahl bleiben (381 vorher wie nachher). §5 NB-3.

**(f) Kommentare wahr gemacht** (`3dba7f38`). Zwei Bezeichner in
`prozessor/Ipc.cpp` (`assistentSchritt`, `commandIdVorgabe`) nannten
entfallene Namen ohne Historienmarker im eigenen Umfeld und tragen ihn jetzt.
`STATUS_STACK_OVERFLOW` bleibt: Fremdname, §5 NB-2. Ratsche 32 → 30.

**(g) `gesundheit.py` und die A32-Behauptung** (`b6433705`). Die
Pflegeticket-Zuordnung ist leer, weil keine Datei die Grenze mehr reißt — und
sie kann nicht still veralten: eine neue Wache meldet jeden Eintrag, dessen
Datei die Grenze nicht mehr reißt, als **Werkzeugfehler** (Exit 2), nicht als
Befund. Einmal absichtlich gebrochen, Rotbeweis
`roh/NAK-225-rotbeweis-pflegeticket.txt`. Fünf neue Selbsttestfälle (116 →
121). Geänderte Zusage an drei Stellen nachgezogen.

**(h1) Riegelkarte am Endstand gemessen** (`d9e2b6db`). Sieben Rohbelege:
Sichtsatz, Zielmenge, Diff-Riegel, Historie, Dateiköpfe, Audio-Pfad,
Warnungen. Kein Code geändert.

**(h2) Roter Kanon, echter Fund, behoben** (`f4d497b3`). Der erste
Abschlusskanon auf `d9e2b6db` war **ROT: 1 von 62**, Bein A5
(`pruefe_v3_vertrag.py`), Exit 2. `eq-copilot/schemas/v3/metriken-v1.json`
bindet je Schwelle die **Datei**, in der die Zahl steht; der Vertragsschnitt
hatte `kFeatureMetricsVersion` und sechs Schwellen mitgenommen. Behoben am
Schnitt, nicht am Register: der Vertragsteil endet jetzt vor der Versionszeile
(Urstand-Zeile 778 statt 920), `FeatureEngine.h` behält die Konstanten und
`VerteilungsRing`. A5 danach Exit 0 mit 613 Prüfungen — dieselbe Zahl wie
vorher. Der rote Lauf bleibt als Rohausgabe `roh/NAK-225-d9e2b6d.md` und als
Kanonabschnitt unten stehen. §5 NB-1.

**(h3) Abschlusskanon** (`f4d497b3`). **GRÜN — 62/62 Kanon-Läufe bestanden,
Exit 0**, abgekoppelt auf sauberem Baum, Rohausgabe
`roh/NAK-225-f4d497b.md`. Nebenmeldungen wörtlich wie in jedem grünen Kanon:
„2 geplante Prüfung(en) noch nicht gebaut", „1 stillgelegte(s) Bein(e)".
**Prüfungszahlen je Bein vorher gegen nachher: eine einzige Abweichung** —
A32 116 → 121 ok-Zeilen, das sind exakt die fünf Selbsttestfälle aus Schritt
(g). Jedes andere Bein meldet dieselbe Menge; 2 575 → 2 580 ok-Zeilen,
Differenz 5, alle in A32.

**Rundenbilanz** (`py -3.13 tools/dirigent/rundenbilanz.py 0ca392b5..HEAD`):
Produkt 21 Dateien +9 778/−8 866, Tests 1 Datei +8/−2, Prüfwerkzeug 3 Dateien
+75/−15, Doku 17 Dateien +8 879/−0. Ein Bau, keine Nacharbeitsrunde nach
einer Prüferrückgabe; die beiden Funde in (e3) und (h2) sind innerhalb des
Baus gefunden und geschlossen worden.

**Übergabe an den Dirigenten.** Zwei Nachträge außerhalb der Ticketpfade:
NAK-236 in `docs/offene-punkte.md` nennt weiterhin Ratsche 32 (jetzt 30), und
NAK-238 kann um die drei Plugin-Dateien gekürzt werden, die nicht mehr über
der Grenze liegen. `docs/PLAN-STAND.md` ist mit
`py -3.13 tools/plan/planstand.py` neu zu rechnen.

---

## Kanon-Lauf - NAK-225

**Lauf:** 2026-09-09 20:07 | **Runner:** `tools/beweise.ps1` | **Urteil:** ROT - 1 von 62 Kanon-Laeufen fehlgeschlagen | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 2 | **Rohausgabe:** [roh/NAK-225-d9e2b6d.md](roh/NAK-225-d9e2b6d.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-09 20:07:54 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | d9e2b6db NAK-225 (S25d) Schritt h1: Riegelkarte am Endstand gemessen |
| Commit (voll) | d9e2b6db3172935a47c49ce5a4e6cba97180a488 |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,10 s | [A1](roh/NAK-225-d9e2b6d.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,55 s | [A2](roh/NAK-225-d9e2b6d.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,80 s | [A3](roh/NAK-225-d9e2b6d.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.198,67 s | [A4](roh/NAK-225-d9e2b6d.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 13,07 s | [A4-SI](roh/NAK-225-d9e2b6d.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 66,23 s | [A4b](roh/NAK-225-d9e2b6d.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [ROT] Exit 2 | 4,11 s | [A5](roh/NAK-225-d9e2b6d.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,21 s | [A6](roh/NAK-225-d9e2b6d.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,14 s | [A7](roh/NAK-225-d9e2b6d.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,26 s | [A8](roh/NAK-225-d9e2b6d.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,41 s | [A9](roh/NAK-225-d9e2b6d.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,94 s | [A10](roh/NAK-225-d9e2b6d.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,25 s | [A11](roh/NAK-225-d9e2b6d.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A12](roh/NAK-225-d9e2b6d.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,22 s | [A13](roh/NAK-225-d9e2b6d.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 7,59 s | [A14](roh/NAK-225-d9e2b6d.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,13 s | [A17](roh/NAK-225-d9e2b6d.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,03 s | [A18](roh/NAK-225-d9e2b6d.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-225-d9e2b6d.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A20](roh/NAK-225-d9e2b6d.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,33 s | [A21](roh/NAK-225-d9e2b6d.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,03 s | [A22](roh/NAK-225-d9e2b6d.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,72 s | [A24](roh/NAK-225-d9e2b6d.md#a24) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A28](roh/NAK-225-d9e2b6d.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,13 s | [A29](roh/NAK-225-d9e2b6d.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A30](roh/NAK-225-d9e2b6d.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 104,90 s | [A31](roh/NAK-225-d9e2b6d.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-225-d9e2b6d.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,14 s | [A26](roh/NAK-225-d9e2b6d.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,27 s | [A27](roh/NAK-225-d9e2b6d.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,54 s | [A23](roh/NAK-225-d9e2b6d.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,21 s | [A16](roh/NAK-225-d9e2b6d.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,20 s | [B1](roh/NAK-225-d9e2b6d.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,18 s | [B2](roh/NAK-225-d9e2b6d.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,21 s | [B3](roh/NAK-225-d9e2b6d.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-225-d9e2b6d.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,39 s | [B3c](roh/NAK-225-d9e2b6d.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,47 s | [B4](roh/NAK-225-d9e2b6d.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,15 s | [B9](roh/NAK-225-d9e2b6d.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,30 s | [B5](roh/NAK-225-d9e2b6d.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 216,36 s | [B10](roh/NAK-225-d9e2b6d.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 0,99 s | [B11](roh/NAK-225-d9e2b6d.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,60 s | [B12](roh/NAK-225-d9e2b6d.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,11 s | [B13](roh/NAK-225-d9e2b6d.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,21 s | [B14](roh/NAK-225-d9e2b6d.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,19 s | [B15](roh/NAK-225-d9e2b6d.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-225-d9e2b6d.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,91 s | [B22](roh/NAK-225-d9e2b6d.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,57 s | [B23](roh/NAK-225-d9e2b6d.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 0,97 s | [B24](roh/NAK-225-d9e2b6d.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,06 s | [B25](roh/NAK-225-d9e2b6d.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,47 s | [B26](roh/NAK-225-d9e2b6d.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-225-d9e2b6d.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,82 s | [B19](roh/NAK-225-d9e2b6d.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,11 s | [B17](roh/NAK-225-d9e2b6d.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,01 s | [B18](roh/NAK-225-d9e2b6d.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,52 s | [B16](roh/NAK-225-d9e2b6d.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,32 s | [B27](roh/NAK-225-d9e2b6d.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,23 s | [B29](roh/NAK-225-d9e2b6d.md#b29) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,16 s | [B28](roh/NAK-225-d9e2b6d.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,29 s | [B8](roh/NAK-225-d9e2b6d.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook: Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500, `broker/src/generiert/` ausgenommen), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Zeilengrenze HAELT seit NAK-225 (S25d): keine Quelldatei liegt mehr ueber 2 000 Zeilen, und die Pflegeticket-Zuordnung ist leer. Jeder neue Treffer meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 121 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest oder ein fehlender Messort, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [OK] Exit 0 | 4,87 s | [A32](roh/NAK-225-d9e2b6d.md#a32) |


---

## Kanon-Lauf - NAK-225

**Lauf:** 2026-09-09 20:45 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 62/62 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-225-f4d497b.md](roh/NAK-225-f4d497b.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-09 20:45:38 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | f4d497b3 NAK-225 (S25d) Schritt h2: Vertragsschnitt endet vor der Metrikversion (Kanonbefund) |
| Commit (voll) | f4d497b360dd12d03f98ab5e1d8688b66404d1e3 |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,06 s | [A1](roh/NAK-225-f4d497b.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,65 s | [A2](roh/NAK-225-f4d497b.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,79 s | [A3](roh/NAK-225-f4d497b.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.199,25 s | [A4](roh/NAK-225-f4d497b.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,50 s | [A4-SI](roh/NAK-225-f4d497b.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,53 s | [A4b](roh/NAK-225-f4d497b.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,19 s | [A5](roh/NAK-225-f4d497b.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,19 s | [A6](roh/NAK-225-f4d497b.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-225-f4d497b.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,25 s | [A8](roh/NAK-225-f4d497b.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,50 s | [A9](roh/NAK-225-f4d497b.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,72 s | [A10](roh/NAK-225-f4d497b.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,42 s | [A11](roh/NAK-225-f4d497b.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,26 s | [A12](roh/NAK-225-f4d497b.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,22 s | [A13](roh/NAK-225-f4d497b.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 7,76 s | [A14](roh/NAK-225-f4d497b.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,04 s | [A17](roh/NAK-225-f4d497b.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,50 s | [A18](roh/NAK-225-f4d497b.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,28 s | [A19](roh/NAK-225-f4d497b.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,31 s | [A20](roh/NAK-225-f4d497b.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,36 s | [A21](roh/NAK-225-f4d497b.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,56 s | [A22](roh/NAK-225-f4d497b.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,68 s | [A24](roh/NAK-225-f4d497b.md#a24) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,15 s | [A28](roh/NAK-225-f4d497b.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. Die Frischepruefung zaehlt das Rechenmodul broker/src/coordinator/hypothese/ zur LAUFZEIT auf (rekursiv), und eine genannte, aber fehlende Quelle ist ebenfalls Exit 3 mit Nennung des Pfads - kein stilles Ueberspringen (NAK-224 D1). | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,13 s | [A29](roh/NAK-225-f4d497b.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,27 s | [A30](roh/NAK-225-f4d497b.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 105,34 s | [A31](roh/NAK-225-f4d497b.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,16 s | [A25](roh/NAK-225-f4d497b.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,15 s | [A26](roh/NAK-225-f4d497b.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-225-f4d497b.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,65 s | [A23](roh/NAK-225-f4d497b.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,07 s | [A16](roh/NAK-225-f4d497b.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,15 s | [B1](roh/NAK-225-f4d497b.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,20 s | [B2](roh/NAK-225-f4d497b.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,05 s | [B3](roh/NAK-225-f4d497b.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-225-f4d497b.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,39 s | [B3c](roh/NAK-225-f4d497b.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,45 s | [B4](roh/NAK-225-f4d497b.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-225-f4d497b.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,53 s | [B5](roh/NAK-225-f4d497b.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 216,51 s | [B10](roh/NAK-225-f4d497b.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,00 s | [B11](roh/NAK-225-f4d497b.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,62 s | [B12](roh/NAK-225-f4d497b.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,12 s | [B13](roh/NAK-225-f4d497b.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,24 s | [B14](roh/NAK-225-f4d497b.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,22 s | [B15](roh/NAK-225-f4d497b.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,06 s | [B20](roh/NAK-225-f4d497b.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,91 s | [B22](roh/NAK-225-f4d497b.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,61 s | [B23](roh/NAK-225-f4d497b.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,10 s | [B24](roh/NAK-225-f4d497b.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-225-f4d497b.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-225-f4d497b.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-225-f4d497b.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 2,00 s | [B19](roh/NAK-225-f4d497b.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,12 s | [B17](roh/NAK-225-f4d497b.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,25 s | [B18](roh/NAK-225-f4d497b.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,72 s | [B16](roh/NAK-225-f4d497b.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,35 s | [B27](roh/NAK-225-f4d497b.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,24 s | [B29](roh/NAK-225-f4d497b.md#b29) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,14 s | [B28](roh/NAK-225-f4d497b.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,26 s | [B8](roh/NAK-225-f4d497b.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook: Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500, `broker/src/generiert/` ausgenommen), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 30, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-225 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Zeilengrenze HAELT seit NAK-225 (S25d): keine Quelldatei liegt mehr ueber 2 000 Zeilen, und die Pflegeticket-Zuordnung ist leer. Jeder neue Treffer meldet sich als OHNE PFLEGETICKET; ein Zuordnungseintrag, dessen Datei die Grenze nicht mehr reisst, beendet den Lauf als WERKZEUGFEHLER (Exit 2), damit die Liste nicht still veraltet. Vorangestellt laeuft der Selbsttest: 121 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest oder ein fehlender Messort, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [OK] Exit 0 | 5,33 s | [A32](roh/NAK-225-f4d497b.md#a32) |

