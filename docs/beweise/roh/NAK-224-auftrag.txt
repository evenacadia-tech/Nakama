NAK-224 — S25c: Ursachenkern des Brokers verhaltensneutral aufteilen (`broker/src/coordinator/hypothese.rs`, `broker/src/transport/server_v3/mod.rs`, `broker/src/lib.rs`; Register NAK-224, NAK-234, NAK-238, NAK-233). Ticketauftrag des Dirigenten (2026-09-09, Pflegeschritt 1 nach G5; Muster S19b/NAK-121).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start; trage ihn in die Kopftabelle von docs/beweise/NAK-224.md ein (Manifest neu anlegen; Muster ist die Kopftabelle von docs/beweise/NAK-121.md und docs/beweise/NAK-223.md: Ticket, Phase, Prüfstufe, Basis-SHA, Kanon vorher, Kanon nachher, Testanzahl, Ticketpfade, Nicht angefasst, Änderungssatz, Prüfmodell — darunter §1 Ticket- und Registerbezug wörtlich, §2 Schnittplan, §3 Riegelkarte, §4 Prüfliste, §5 Nebenbefunde, §6 Verlauf; ein lebender Kopf, darunter append-only). Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (neue Dateien vorher `git add -- <pfad>`, Verschiebungen mit `git mv`); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie Backticks in `-m`. Keine Neben-Refactors, keine Legacy-Umbenennung, kein neues Verhalten. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §4 nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits, Manifest und Modulköpfe Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder dem rekursiven Unix-Löschbefehl (Ask-Regel, der Auto-Modus hält an); selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. `cmake` liegt nicht im Bash-PATH; C++-Beine nur über den Runner aus pwsh. Beende deinen Turn nicht, solange ein von dir gestarteter Lauf (Kanon) läuft; die letzte Logzeile `EXIT=` ist das Ende.

TICKET, wörtlich (docs/plan/plan.json, Schritt S25c, Ticket NAK-224, Stufe T2; für §1 des Manifests direkt aus plan.json kopieren, nicht aus diesem Auftrag): „ANGELEGT 08.09.2026 (Pflegeschritt 1 nach G5, Register NAK-224). `broker/src/coordinator/hypothese.rs` trug am 08.09.2026 3 762 Zeilen, weil NAK-212 und NAK-213 dort gebaut haben. INHALT, ein einziger verhaltensneutraler Änderungssatz: die Datei nach Fachbereich teilen (Screening und Deckel, Zusammenhang und Rangkomponenten, Klassenwahl und Konkurrenzlage, Enthaltungen, Befundbau und Sicht), Historie erhalten (`git log --follow`), Modulköpfe wahr. BEWEIS: Kanon vorher = Kanon nachher grün mit gleicher Beinzahl, P5-Korpus bytegleich (A28/A29), Determinismus M-25 über hundert Läufe, `git diff --stat` ohne Zeile in Tests oder Fixtures außer Pfadänderungen. Muster: S19b (NAK-121). GRENZE: keine Regeländerung, keine neue Schwelle, kein Fassungsschritt; Nebenbefunde nummeriert ins Manifest, nie stumm behoben. NACHTRAG 08.09.2026 (Messung beim G5-Abschluss, Register NAK-234): `broker/src/transport/server_v3/mod.rs` liegt mit 2 007 Zeilen ebenfalls über der Grenze und gehört in denselben Änderungssatz; generierte Dateien unter `broker/src/generiert/` sind keine Pflegefläche (A9 hält sie bytegleich zur Neuerzeugung). Dazu NAK-233 (toter Parameter `veraltet` in `zustand_aus_sicherheit`), zu schließen in derselben Aufteilung, sofern die Zustandsabbildung M-29 unberührt bleibt.“

REGISTERZEILEN, die den Umfang tragen (docs/offene-punkte.md, wörtlich dort nachlesen und in §1 zitieren): NAK-224 (die Aufteilung selbst), NAK-234 (`server_v3/mod.rs`), NAK-238 (`broker/src/lib.rs` 1 998 Zeilen, „in S25c mitnehmen“), NAK-233 (toter Parameter `veraltet`). NAK-235 (28 Funktionen über 200 Zeilen), NAK-236 (Kommentar-Bezeichner außerhalb der drei Dateien) und NAK-237 (`// SAFETY:`-Begründungen) sind NICHT Teil dieses Tickets — Entscheid des Dirigenten, Begründung unten.

ISTSTAND am Basis-SHA, gemessen vom Dirigenten am 09.09.2026 mit `py -3.13 tools/plan/gesundheit.py` (du misst ihn selbst erneut und trägst die Tabelle in §2 ein): `hypothese.rs` 3 986 Zeilen (Produktcode bis Zeile 2 593, Inline-Testmodul `mod tests` ab 2 594), `server_v3/mod.rs` 2 169 Zeilen (Produktcode bis Zeile 203, Inline-Testmodul ab 204), `lib.rs` 1 998 Zeilen (Produktcode bis Zeile 1 290, Inline-Tests ab 1 291). Kanon vorher: GRÜN 62/62 auf `a75bcee7` (docs/beweise/roh/NAK-223-a75bcee.md); `git diff --stat a75bcee7..HEAD -- broker eq-copilot tools` ist leer, der Lauf steht also auf demselben Codestand wie dein Basis-SHA — das in der Kopftabelle als „Kanon vorher“ mit genau dieser Messung belegen (Muster NAK-121 Kopftabelle). Ratschen in gesundheit.py: Funktionen über 200 Zeilen 28, Kommentar-Bezeichner 35, clippy 91. Telemetrie und Testzahl misst du selbst.

SCHNITT — Technik, entschieden vom Dirigenten; die Feinheit leitest du aus dem Code ab und schreibst sie VOR dem ersten Schnitt als Schnittplan in §2 (Tabelle: jedes Item alt — Typ, Funktion, Konstante, Testfunktion — mit Zielmodul; die Regel, nach der du geschnitten hast; die Sichtbarkeiten):
(1) `hypothese.rs` wird ein Verzeichnismodul: erster Commit `git mv broker/src/coordinator/hypothese.rs broker/src/coordinator/hypothese/mod.rs` ohne Inhaltsänderung (reine Umbenennung = 100 % Ähnlichkeit, `git log --follow` reicht damit bis SONDE-014 Etappe C zurück; NAK-121 §5 und §12). Danach in eigenen Commits je Modul AUS `mod.rs` herausgelöst — die Reihenfolge der Commits belegt die Herkunft: Fachbereiche aus dem Plan (Screening und Deckel; Zusammenhang und Rangkomponenten; Klassenwahl und Konkurrenzlage; Enthaltungen; Befundbau und Sicht), dazu die Vertragstypen (`Ursachenklasse`, `Aussageklasse`, `Ausschlussgrund`, `CauseHypothesis` usw.) dort, wo sie fachlich hingehören — du entscheidest, ob sie in `mod.rs` bleiben oder ein eigenes Modul bilden, und begründest es im Schnittplan. `mod.rs` behält den wahren Modulkopf (die drei harten Regeln M-20/M-17/M-25 und M-87 gehören dorthin, wo die Rechnung steht, die sie bindet — verteile die Kopfzeilen mit, statt sie zu kopieren) und exportiert mit `pub use` alles, was heute unter `crate::coordinator::hypothese::…` oder `eqcop_broker::coordinator::hypothese::…` erreichbar ist, unter demselben Pfad. Das Inline-Testmodul (ab Zeile 2 594) wandert mit: jede Testfunktion in das `#[cfg(test)] mod tests` des Moduls, dessen Funktion sie misst; Testkörper textgleich bis auf `use`-Zeilen; Konstanten wie `stufe_b_*`-Zähler bleiben bei ihrem Fachbereich.
(2) `server_v3/mod.rs`: die 203 Zeilen Produktcode bleiben in `mod.rs` (Konstanten, Schema-Minor-Prüfung, `pub use`); das Inline-Testmodul (Zeilen 204 bis 2 169) wird nach Fachbereich in Testdateien innerhalb des Modulverzeichnisses geteilt (etwa `server_v3/tests_<bereich>.rs`, eingebunden über `#[cfg(test)] mod tests_<bereich>;`, oder ein `#[cfg(test)] mod tests { mod <bereich>; … }` mit Dateien unter `server_v3/tests/`; du wählst die Form, die private Items ohne neue `pub`-Sichtbarkeit erreicht — `pub(super)`/`pub(crate)` nur wo nötig, jede Sichtbarkeitsänderung im Schnittplan). Hier gibt es keine reine Umbenennung; Herkunft belegt die Commitfolge aus `mod.rs` heraus.
(3) `lib.rs` bleibt Crate-Wurzel und wird dünn: Moduldeklarationen, `pub use`, Crate-Doku. Die Fachbereiche der Wurzel (Register/`SensorEintrag`, Sitzungen/`sessions_bilden`, Paare/`paare_auswerten`, Brokerstatus/Lebenslauf `broker_starten`…`broker_status`, Probe/Profil/Aggregat) gehen je in ein eigenes Modul unter `broker/src/` (Name aus dem Fachbereich, nicht `util`), die Inline-Tests mit ihren Funktionen; jeder heute unter `eqcop_broker::<Name>` erreichbare Pfad bleibt über `pub use` bestehen — Ziel 0 geänderte Zeilen unter `broker/src/bin/`, `broker/src/server.rs` und `broker/tests/`.
(4) NAK-233: `zustand_aus_sicherheit(klasse, veraltet)` — der Produktpfad übergibt an allen vier Stellen `false`, aber `broker/tests/sonde014_befund.rs:259` misst den Zweig `veraltet = true` als Teil von M-29. Eine Signaturänderung bräuchte damit eine Testzeile und ist nach dem BEWEIS-Satz des Tickets („ohne Zeile in Tests … außer Pfadänderungen“) hier nicht zulässig. Entscheid: die Signatur bleibt; du machst die Doku der Funktion wahr (der Weg zu `stale` läuft produktiv über `befunde_veralten_locked` in hypothese_verdrahtung.rs und den Teilverlust der Evidenzrücknahme, nie über diesen Parameter; Aufrufstellen im Doku-Kommentar nennen) und trägst in §5 als Nebenbefund NB-1 den Befund mit Zeilenbeleg ein, dass die Signaturänderung einen eigenen Schritt mit Teständerung braucht. Der Dirigent zieht NAK-233 im Register nach.
(5) Lügende Kommentare in den drei Dateien (A32-Liste: hypothese.rs:65 `geschlossene_mengen_des_produzenten_decken_sich_mit_dem_vertrag`, :266 `ausschlussgruende_sind_index_treu`, :664 `eigen4`, :1475 `eigen3`; server_v3/mod.rs:520 `kBlockFrist`): je Treffer an der Quelle prüfen — nennt der Kommentar einen Bezeichner, der anders heißt oder nicht mehr existiert, wird er auf den wahren Namen gesetzt; ist es ein Fremdname oder ein Heuristik-Fehler (Manifest NAK-223 §2.5), bleibt er und steht mit Begründung in §5. Kommentare außerhalb der drei Dateien bleiben unberührt (NAK-236).
(6) `tools/plan/gesundheit.py`: die Pflegeticket-Liste (Zeilen 212 bis 216) muss nach dem Schnitt wahr sein — Einträge für Dateien, die nicht mehr über 2 000 Zeilen liegen, entfallen; die Ratschen Funktionen/Kommentar-Bezeichner/clippy werden auf den gemessenen neuen Iststand GESENKT, nie erhöht (Ratsche = Iststand, Kommentar im Werkzeug erklärt es); der Selbsttest (`--mit-selbsttest`) bleibt grün. Erwartung nach dem Schnitt: „Quelldateien über 2000 Zeilen“ = 3 (nur die drei NAK-225-Dateien), kein neues Modul über dem Ziel 1 500 Zeilen — liegt eines darüber, steht die Begründung im Schnittplan und der Dirigent entscheidet. A32 endet weiterhin mit Exit 4 [HINWEIS] wegen NAK-225; das ist erwartet und nicht blockierend.

NICHT TEIL DIESES TICKETS (Dirigent, Technik): kein Zerlegen langer Funktionen (NAK-235: `hypothesen` mit 267 Zeilen bleibt eine Funktion; keine Funktion wird länger); keine `// SAFETY:`-Arbeit (NAK-237, sicherheitsnah, eigener delegierter Schritt); keine Änderung an `experiment_verdrahtung.rs`, `server.rs`, `experiment.rs` (über Ziel, unter Grenze — NAK-238 nennt sie, die Grenze reißt nur `lib.rs`); keine Änderung an Schemas, Fixtures, `eq-copilot/`, `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md`, `.claude/`. Ticketpfade sind ausschließlich: `broker/src/coordinator/hypothese.rs` → `broker/src/coordinator/hypothese/`, `broker/src/transport/server_v3/`, `broker/src/lib.rs` plus die daraus herausgelösten neuen Module unter `broker/src/`, `tools/plan/gesundheit.py`, `docs/beweise/NAK-224.md`, `docs/beweise/roh/NAK-224-*`. Muss ein anderer Pfad angefasst werden, damit es kompiliert (z. B. eine `use`-Zeile in einem Nachbarmodul), ist das eine Pfadänderung: erlaubt, aber im Manifest je Datei und Zeile genannt.

RIEGELKARTE (§3 des Manifests; je Zeile Befehl, Wert VORHER am Basis-SHA und NACHHER am Endstand, aus dieser Session):
R1 Kanon: vorher GRÜN 62/62 auf `a75bcee7` mit leerem Codediff zum Basis-SHA (siehe oben); nachher voller abgekoppelter Kanon auf dem Endstand, GRÜN mit 62 Beinen, gleiche Beinzahl.
R2 P5-Korpus bytegleich: A28 (`erzeuge_p5_korpus.py --pruefen`) und A29 (`pruefe_p5_korpus.py`) grün; zusätzlich SHA-256 der Ergebnisdatei `eq-copilot/build/p5-korpus-ergebnis.json` (geschrieben von broker/tests/sonde014_p5_korpus.rs) vorher und nachher gleich — beide Hashes im Manifest.
R3 Determinismus M-25: `cargo test --manifest-path broker/Cargo.toml --test sonde014_hypothese ranking_ist_bytegleich_ueber_hundert_laeufe` und `screening_ist_deterministisch` grün vorher und nachher (Ausgabe zitieren).
R4 Testbestand: `cargo test --manifest-path broker/Cargo.toml -- --list` vorher und nachher — gleiche Anzahl; nach Abzug der Modulpräfixe gleiche Namensmenge (Differenz als Datei unter docs/beweise/roh/NAK-224-testliste-diff.txt, erwartet leer bis auf Präfixe). 0 Compilerwarnungen in `cargo build` und `cargo test`.
R5 Diff-Riegel: `git diff --stat <Basis>..HEAD` — `broker/tests/` 0 Zeilen (Ziel), `eq-copilot/` 0, `broker/src/generiert/` 0, Fixtures und Schemas 0; jede Abweichung unter `broker/tests/` ist eine Pfadänderung und steht je Zeile mit Begründung im Manifest.
R6 Historie: `git log --follow --oneline -- broker/src/coordinator/hypothese/mod.rs` reicht bis zum Commit zurück, der `hypothese.rs` angelegt hat (SHA nennen); `git log --oneline <Basis>..HEAD` zeigt je herausgelöstem Modul einen eigenen Commit.
R7 Modulköpfe wahr: jedes neue Modul trägt einen `//!`-Kopf, der sagt, was darin steht und welche Regel es bindet; kein Kopf behauptet etwas, das in einem anderen Modul liegt; du prüfst jeden Kopf gegen den Inhalt und listest sie in §3.
R8 NAK-233: Signatur unverändert; Doku wahr; `broker/tests/sonde014_befund.rs` unverändert und grün (M-29).
R9 gesundheit.py: Tabelle vorher/nachher; Pflegeticket-Liste wahr; kein neues Modul über 1 500 Zeilen; Ratschen nur gesenkt; `--clippy` vorher und nachher gemessen (Zahl darf nicht steigen).
R10 Rundenbilanz-Lesbarkeit: jeder Commit kompiliert und `cargo test` (mindestens die Testziele, die das geschnittene Modul messen: sonde014_hypothese, sonde014_befund, sonde014_verdrahtung, coordinator_model, security_vectors, transport_fuzz, broker_idle; beim letzten Commit je Datei der volle `cargo test`) ist grün — kein Zwischencommit mit rotem Bau.

ABLAUF: (a) Iststand messen (R1 bis R4, R9 vorher) und ins Manifest, Manifest mit Schnittplan committen und pushen (erster Commit, nur docs/beweise/NAK-224.md und docs/beweise/roh/NAK-224-*). (b) hypothese.rs: `git mv`-Commit, dann je Modul ein Commit; nach dem letzten voller `cargo test`. (c) server_v3/mod.rs analog. (d) lib.rs analog. (e) Kommentare (5) in einem Commit. (f) gesundheit.py (6) in einem Commit. (g) Riegelkarte nachher messen, dann den vollen Kanon abgekoppelt auf sauberem Baum:
  $log = Join-Path $env:TEMP 'nakama-nak224-kanon.log'
  $befehl = "pwsh -NoProfile -File tools/beweise.ps1 -Bauen -Ziel docs/beweise/NAK-224.md -Anhaengen -Titel NAK-224 *> $log; Add-Content $log ('EXIT=' + `$LASTEXITCODE)"
  Start-Process pwsh -WindowStyle Hidden -WorkingDirectory (Get-Location) -ArgumentList '-NoProfile', '-Command', $befehl
(einfache Anführungszeichen um EXIT= — CLAUDE.md-Landmine vom 08.09.2026). Warten, bis die letzte Logzeile mit `EXIT=` beginnt; höchstens alle 15 Minuten `Get-Content $log -Tail 3`. Ein einmaliges „Voraussetzung fehlt“ an zwei flatc-Beinen direkt nach einem frischen Bau ist bekannt: einmal wiederholen, bevor du einen Befund erhebst. Ergebnis (Kopfzeile, Beinzahl, Rohausgabe docs/beweise/roh/NAK-224-<sha>.md) ins Manifest; Urteilsmarke `<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->` erst nach grünem Kanon mit 62 Beinen; T2 setzt der Dirigent nach der Codex-Prüfung. Kumulierte Rundenbilanz: `py -3.13 tools/dirigent/rundenbilanz.py <Basis>..HEAD` ins Manifest.

SELBSTAUDIT vor jedem Commit (adversarial): kein Verhalten geändert (Diff je Modul gegen den Ursprungstext lesen — nur Verschiebung, `use`-Zeilen, Sichtbarkeit, Modulkopf), keine Konstante umbenannt oder umgewertet, kein `pub` breiter als vorher außer den im Schnittplan genannten `pub(crate)`/`pub(super)`, Tie-Break- und Quantisierungskonstanten von M-25 unverändert am Ort ihres Fachbereichs, Testkörper textgleich, kein Test still weggefallen (R4), kein Modul mit leerem oder kopiertem Kopf, gesundheit.py-Ausgabe sortiert und deterministisch, keine Behauptung im Manifest ohne Lauf aus dieser Session. Beziehungen: `git mv` ↔ Herauslösen ↔ `pub use` in einem Änderungssatz; Ratsche ↔ Iststand ↔ Pflegeticket-Liste gemeinsam.

ABSCHLUSS: Manifest vollständig (Kopftabelle mit Basis- und Endstand, Schnittplan, Riegelkarte R1–R10 mit beiden Werten, Prüfliste, Nebenbefunde nummeriert, Kanon-Kopfzeile, Rundenbilanz), alles committet und gepusht, Worktree sauber. Melde dich erst, wenn `git status --short` leer ist und `git rev-parse HEAD` gleich `git rev-parse origin/master` ist.
