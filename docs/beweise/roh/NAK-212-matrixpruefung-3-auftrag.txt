Matrixprüfung 3 NAK-212 / G5-Nacharbeit 1, Runde 2 — gebundener Prüfauftrag des Dirigenten (2026-09-07)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); keine Prozesse des Produkts, kein cargo, kein Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Matrixnacharbeit 2:
`git diff 66f578e4ec5cb9622af242f4feb5b13aa878bc54...740de80171a94e4693173d0428d171c717281f3f -- docs/beweise/NAK-212.md`
(ZIEL = 740de80171a94e4693173d0428d171c717281f3f = HEAD, vor und nach deinem Lauf identisch). Die übrige Matrix wurde in den Matrixprüfungen 1 und 2 geprüft und ist hier nicht Prüfgegenstand; du liest sie als Kontext. Es gibt weiterhin keinen Produktcode: der Codestand unter broker/, eq-copilot/, tools/ ist identisch mit dem Kanonstand 4df8855.

BEFUND, den diese Runde schließen sollte (wörtlich aus dem vorigen Urteil, docs/beweise/roh/NAK-212-matrixpruefung-2-66f578e.txt — lies die Datei vollständig):
D7 — [P2] Sichere den quantisierten Rangabstand im Aufbau von N-17: Ein positiver Uplift garantiert keinen verschiedenen Gesamtrang: bei Bandpassung 0,1, Intent-Relevanz 0,5, Routingqualität 1 und sonst null ergeben `uplift = Some(0,000001)` beziehungsweise `None` beide den Rang 0,266667, weil broker/src/coordinator/hypothese.rs:535-540 nach der Mittelung erneut quantisiert. N-17 verlangt dafür weiterhin einen starken Befund, während die neue E6-Regel beide Kandidaten als ungetrennt einstuft. Der positive Kontrollfall muss ausdrücklich unterschiedliche quantisierte Gesamtränge voraussetzen und diesen Abstand im genannten Test prüfen.

Die Regel des Dirigenten steht wörtlich in docs/beweise/roh/NAK-212-matrixnacharbeit-2-auftrag.txt: N-17 nennt als Vorbedingung einen um mindestens ein Quantum verschiedenen quantisierten Gesamtrang UND eine verschiedene Zusammenhangskomponente, der Kontrollfall bewegt den Rang sichtbar (Größenordnung 0,1), der Test prüft den Rangabstand explizit; dazu eine Grenzfallzeile mit Rotbeweis (Uplift unterhalb des Quantums ist kein Unterschied — beide `mittel`, jeder Alternative des anderen).

GATE-BEZUG, wörtlich: Gate-Text G5 (docs/bauaufteilung-sonden.md:395-400): „Enthaltung („mehr Daten nötig") ist das gewünschte Ergebnis, nicht ein Treffer." Unverändert geltende Referenzzeilen: docs/beweise/SONDE-014.md §3 M-15, M-25, M-26.

Du beantwortest genau zwei Fragen:
1. Ist D7 am Zielstand geschlossen — ja/nein mit Beleg (neuer Wortlaut von N-17, die Grenzfallzeile, der messende Test mit Datei::Fall oder NEU, der Rotbeweis)? Prüfe an hypothese.rs:535-540 nach, ob der Kontrollfall mit den genannten Komponentenwerten tatsächlich zwei verschiedene quantisierte Gesamtränge ergibt, und ob der Grenzfall (0,000001 gegen None) tatsächlich denselben quantisierten Rang ergibt.
2. Hat der Fixdiff etwas gebrochen — eine bestehende Matrixzeile (insbesondere E6, N-41, N-42), eine Regel R1–R6, eine der 27 erwarteten Ausgaben in §2.6, eine Referenzzeile aus SONDE-014 §3, eine Invariante aus CLAUDE.md?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen und Ausschlüsse wie in der Matrixprüfung 1 (docs/beweise/roh/NAK-212-matrixpruefung-1-auftrag.txt).

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — D7 geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID oder Manifestzeile, Quelle Datei:Zeile, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
