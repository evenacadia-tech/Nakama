Wiederprüfung S25b / Ticket NAK-223, Runde 2 — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff da730200ab644606567f786c621d72ba03ee8462...cf94c4b4edc23c93faf5820fd699533b0c4b9c8f -- tools/plan/gesundheit.py`
(ZIEL = cf94c4b4edc23c93faf5820fd699533b0c4b9c8f, das Ende der Nacharbeit 2; HEAD beim Lauf ist ein Nachfolger von ZIEL, der gegenüber ZIEL ausschließlich diesen Prüfauftrag unter docs/beweise/roh/ trägt — `git diff --stat cf94c4b4..HEAD -- tools .claude broker eq-copilot` ist leer — und vor wie nach deinem Lauf identisch bleibt; VORHER = da730200, der Stand vor der Runde, er enthält gegenüber dem geprüften Stand afc2d699 nur Rohdateien unter docs/beweise/roh/, keinen Code). Der übrige Ticketbereich wurde in der Erstprüfung (Thread 01a08530) und der Wiederprüfung 1 (Thread 01a08551-373e-7792-bd0e-9983ad9e9ebc, docs/beweise/roh/NAK-223-wiederpruefung-1-9cef44b.txt) bereits geprüft und ist hier nicht Prüfgegenstand. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat; Python-Aufrufe von `finde_dead_code` und der Selbsttest sind lesende Läufe und erlaubt — startet Python 3.13 bei dir nicht, nenne das unter „nicht geprüft“); kein cargo, kein cmake, kein Kanon; du schreibst nichts.

DER BEFUND, den diese Runde schließen sollte — wörtlich aus dem Urteil der Wiederprüfung 1, mit der Regel des Dirigenten (docs/beweise/roh/NAK-223-nacharbeit-2-auftrag.txt):
DEFEKT: „Überspringe gültige Elementnamen nicht als Qualifizierer — tools/plan/gesundheit.py:572-575. Beim gültigen Rust-Typnamen `union` überspringt die Wiederholung den tatsächlichen Namen und erfasst stattdessen `where`. Reproduktion: Für `s = '#[allow(dead_code)]\nstruct union where u8: Copy;\nfn x<T>() where T: Copy {}\n'` liefert `finde_dead_code([('t.rs', None)], {'t.rs': s})` am Zielstand `[]`, vorher einen Treffer `union`: Die fremde `where`-Klausel wird als Verwendung gewertet. Bei sonst gehaltenen Grenzen kippt der virtuelle Gesamtlauf dadurch von Exit 4 auf 0. Das verletzt den Namensvertrag aus R-D1 und das Gate-Maß für aufruferlose Helfer; übersprungen werden dürfen nur tatsächliche Qualifizierer.“
Regel R-D1b: Qualifizierer werden ausschließlich VOR dem Elementschlüsselwort übersprungen, und nur dann, wenn ein Elementschlüsselwort folgt — Grammatik `[Qualifizierer]* Schlüsselwort [mut]? Name` mit Qualifizierer ∈ {`pub`, `pub(...)`, `const`, `unsafe`, `async`, `default`, `extern "…"`}, Schlüsselwort ∈ {`fn`, `struct`, `enum`, `trait`, `type`, `const`, `static`, `union`, `mod`}, `mut` nur unmittelbar nach `static`; das erste Wort nach dem Schlüsselwort ist ausnahmslos der Name (auch `union`, `default`, `where`); `const` ist Qualifizierer, wenn ein Schlüsselwort folgt, sonst selbst Schlüsselwort. Messung: die Reproduktion des Prüfers liefert genau einen Treffer `union`; Selbsttestfälle je ohne und mit Verwendung für `struct union where u8: Copy;` → `union`, `fn default() -> u8 { 1 }` → `default`, `default fn helfer() {}` → `helfer`, `union Foo { a: u8 }` → `Foo`, `static mut ZAEHLER: u8 = 0;` → `ZAEHLER`, `const fn name() {}` → `name`, `const NAME: u8 = 1;` → `NAME`, `pub const extern "C" fn helfer() {}` → `helfer`; die Fälle der Nacharbeit 1 (`pub const fn`, `pub(crate) unsafe fn`, `pub async fn`, `unsafe fn`, `pub unsafe trait`) bleiben grün; Rotbeweis `NAK-223-rot-deadcode.txt` um einen dritten Abschnitt erweitert (Mutation: Wiederholung wieder hinter dem Schlüsselwort → Selbsttest rot; Rücknahme → grün).

GATE-TEXT, wörtlich (docs/plan/plan.json S25b): „`tools/plan/gesundheit.py` misst die Codebase (… aufruferlose `#[allow(dead_code)]`-Helfer; …) … druckt eine Tabelle und gibt Exit 4, sobald eine Schwelle gerissen ist.“ Exit-Semantik des Werkzeugs: 0 = keine Grenze gerissen, 2 = Werkzeug kaputt, 4 = Befund.

Du beantwortest genau zwei Fragen:
1. Ist der Befund am Zielstand geschlossen — ja/nein, mit Beleg (Datei:Zeile der Regex `ELEMENT`/`QUALIFIZIERER`, die Selbsttestfälle mit Namen, der Rotbeweis-Abschnitt; die Reproduktion des Prüfers am Zielstand nachgefahren)? Hält die Regex die Grammatik aus R-D1b auch für `pub(in crate::a) fn helfer`, `const NAME: u8` gegen `const fn name`, `static mut NAME` und `extern "C" fn`?
2. Hat der Fixdiff etwas gebrochen — die Fälle der Erstprüfung und der Nacharbeit 1 (`pub const fn helfer`, `pub(crate) unsafe fn`, `pub async fn`, `unsafe fn`, `pub unsafe trait Helfer`), die übrigen Maße, die Exit-Semantik 0/2/4, den Clippy-Pfad (unberührt zu lassen), die Tabellenform, den Determinismus, die Schwellen (28/91/35; 2 000/1 500/200; Kontextgrenzen) oder den bestehenden Selbsttest?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen (DEFEKT / LÜCKE / HÄRTUNG) und Ausschlüsse wie in der Erstprüfung: docs/** kein Prüfgegenstand; Register NAK-215 bis NAK-222, NAK-224, NAK-225, NAK-228 bis NAK-234; die Nebenbefunde N-1 bis N-4 aus docs/beweise/NAK-223.md §4; Heuristik-Fehlerklassen aus §2.5 sind erklärte Grenzen (Makros, Lambdas, Strings mit Klammern, Fremdnamen in Kommentaren; Elemente in Makro-Aufrufen oder hinter `#[cfg_attr]` gehören dazu); Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; tools/dirigent/pruefliste.md keine Anforderungsquelle; Kosmetik, Stil: nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — der Befund ist geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Regel bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
