Matrixprüfung 1 NAK-214 / G5-Nacharbeit 3 — gebundener Prüfauftrag des Dirigenten (2026-09-08)

Du bist der frische, lesende Prüfer. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, kein cargo und keinen Python-Interpreter; du schreibst nichts. Prüfgegenstand ist ausschließlich die Verhaltensmatrix der Etappe 1 dieses Tickets:
`git diff aa584e90ef43ea7edc010c8fe30396bf8f73dc97...d0f2f304cfc8464680113df85069d948b685c4b6 -- docs/beweise/NAK-214.md`
mit BASIS = aa584e90ef43ea7edc010c8fe30396bf8f73dc97 und ZIEL = d0f2f304cfc8464680113df85069d948b685c4b6 (HEAD; er muss vor und nach deinem Lauf identisch sein). Der Codestand unter broker/, eq-copilot/, tools/ ist an BASIS und ZIEL identisch mit dem Kanonstand d3c6709 (Abschlusskanon NAK-213, GRÜN 61/61, docs/beweise/roh/NAK-213-d3c6709.md).
In dieser Etappe gibt es keinen Produktcode: das Ticket macht `gate_felder_vollstaendig` zum produktiven Riegel vor Persistenz und Zustellung (kein Proposal ohne führendes Main), bindet `claim_class = wirkungsbeleg` an ein PRE/POST-Paar mit gemessener Wirkung, setzt in Gen bei Verbindungsende und bei einer Rücknahme ohne Folge-Snapshot alle Befunde auf `stale`, weist `user_verdict` mit nicht existenter `finding_id` ab und filtert die Passagenwahl der Aufnahme — deshalb entstand zuerst die Matrix (docs/beweise/NAK-214.md §2: die neun Entscheide E1 bis E9 in §2.0, die Zeilen V-01 bis V-44 in §2.1 bis §2.5, die drei Lückenstellen mit erwarteter Ausgabe in §2.6, die Einordnung gegen SONDE-014 §3, NAK-212 §2 und NAK-213 §2 in §2.7, der Vertragsentscheid in §2.8, die Nicht-Zusagen in §2.9). Prüfstufe T3-Nacharbeit.

GATE-BEZUG, wörtlich: P5-Exit-Gate (Entwurf §59, docs/FL-Nakama-Sonden-Design-Entwurf.md:4322-4324): „Jede sichtbare Behauptung referenziert existente Evidenz-IDs, jedes Proposal nennt Ziel, Passage, Grenzen, Hörziel, Stopbedingung und Rückweg. Gegenbeispiele erzwingen nachweislich Enthaltung." Entwurf §36.1 (Zeilen 2354-2365), die drei Aussageklassen: „1. Zusammenhang: Quelle und Masterproblem treten im selben Bereich und Zeitfenster auf. 2. Wirkungsbeleg: Ein PRE/POST-Paar zeigt eine reproduzierbare Veränderung durch eine Kette. 3. Kontrollierter Ursachenbeleg … Aus paralleler Telemetrie allein entsteht nie Klasse 2 oder 3." SONDE-014 M-17 (docs/beweise/SONDE-014.md §3): „Aus paralleler Telemetrie allein entsteht nie Klasse 2 oder 3 … 2. Wirkungsbeleg (PRE/POST-Paar mit reproduzierbarer Veränderung)." Gate-Text G5 (docs/bauaufteilung-sonden.md:395-400): „Codex + adversariale Gegenbeispiele. Falsifikation: Gate 6, 7, 8" — hartes Gate 6 (Entwurf §49.2, Zeile 3571): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." Entwurf Zeile 1669: „genau ein führendes Main pro aktiver Sitzung."

BEFUNDE, die die Matrix schließen muss (an der Quelle bestätigt, docs/beweise/G5.md §1, §4, §6, §8, §11 Nr. 3): E-D5 = A6 (bei zwei Mains `fuehrendes_main = None`, `unwrap_or_default()` liefert `target: ""`, das Proposal wird persistiert; `gate_felder_vollstaendig` hat keinen Produktivaufrufer), E-D6 (`prepost_paar` aus `ergebnis.is_some()`, `beurteile_paar` liefert `Some` auch für `Probable`, der Befund trägt `claim_class = wirkungsbeleg` ohne ausgerichtetes Delta und ohne Wirkung), E-L2 (`juengste_passage_im_projekt` wählt die jüngste Passage unabhängig von Vergleichbarkeit, Taint oder Abschlusszustand), E-L5 (`user_verdict_p0` persistiert ohne Existenzprüfung der `finding_id`), A5 (Gen behält nach einer Rücknahme ohne Folge-Snapshot einen READY-Befund). Rohbelege: docs/beweise/roh/G5-eigener-lauf-bf92891.md (D5 §B7 und „DEFEKT D5", D6, L2, L5, B6 Weg 8 und 10, B7 Weg 8), docs/beweise/roh/G5-codex-astra-falsifikation-bf92891.txt (A5, A6). Die Zeilenangaben in G5.md beziehen sich auf bf92891 und sind seit NAK-212/213 verschoben; lies die aktuellen Zeilen an ZIEL.
REGELN R1 bis R5 des Dirigenten, wörtlich: docs/beweise/roh/NAK-214-auftrag.txt (Abschnitt REGELN) und docs/beweise/G5.md §11 Nr. 3; sie stehen in §1 des Manifests noch einmal. Die Matrix darf von den Regeln nur mit benannter Begründung in §2.0 abweichen; eine Abweichung ohne Begründung oder gegen den Wortlaut ist DEFEKT.

VIER LESARTEN, DIE DER DIRIGENT TRÄGT (Technikentscheid 08.09.2026, vor deinem Lauf an der Quelle geprüft). Sie sind als Wortlautabweichung kein Defekt; du prüfst sie ausschließlich auf innere Konsistenz, Determinismus und darauf, ob sie einen zugeordneten Befund offen lassen oder eine Invariante brechen — dann ist das ein Defekt der Lesart:
(a) E2 — „Klasse AudioAligned" in R2 ist die Rangschwelle `>= Alignmentklasse::AudioAligned`. Quelle an ZIEL: broker/src/coordinator/prepost.rs:44 („Die Reihenfolge ist die Rangfolge"), :55 `AudioAligned = 2`, :57 `FeatureAligned = 3`; `AudioAligned` wird im Broker nirgends gesetzt. „Ein Paar darunter" liegt damit bei `Probable` und `Unclear`. Prüfe: ist `FeatureAligned` mit ausgerichtetem Delta und benannter Wirkung ein Wirkungsbeleg im Sinn von M-17 („mit reproduzierbarer Veränderung") — und wenn nicht, welcher Satz bricht?
(b) E6 — `beginneSubscription` behält beim Sitzungswechsel `befunde.clear()`; das ist strenger als `stale` und erfüllt R3 („nicht handelbar"). Prüfe nur, ob Rückweg (V-26) und fail-closed (V-27) vollständig sind und ob ein `stale`-Befund nach `controlEnde` mit §59 Satz 1 verträglich ist.
(c) E7 — R4 antwortet mit dem bestehenden Fehlercode `unknown_target` (Fassung 1), kein Fassungsschritt. Prüfe: trägt der Schemakommentar zu `unknown_target` in eq-copilot/schemas/v3/eq-ipc-v3.schema.json ($defs/fehlercode) die Bedeutung „das adressierte Domänenobjekt existiert nicht", und ist die Abweisung (`ergebnis = "abgelehnt"`, kein Domänenereignis, kein Append) als Zeile mit Test und Rotbeweis gedeckt?
(d) E9 — ein offenes Experiment ohne Ergebnis ist als Passage tauglich; „Vergleichbarkeit unbekannt" trifft nur ein Experiment mit `Terminal::Ergebnis` und `achsen.vergleichbarkeit == None`; ein Ergebnis `unvergleichbar` bleibt wählbar, weil NAK-212 N-43 diesen Fall danach mit `passage_unvergleichbar` trifft. Prüfe: kann nach dieser Lesart eine untaugliche Passage gewählt werden, oder kann derselbe Zustand zwei verschiedene Ausgaben erzeugen (M-25)?

Referenzmatrizen: docs/beweise/SONDE-014.md §3 (M-17, M-25, M-29, M-30, M-31, M-33 bis M-35, M-42, M-43, M-46, M-52, M-55, M-77, M-87), docs/beweise/NAK-212.md §2 (N-37, N-43, E5), docs/beweise/NAK-213.md §2 (E7/R4 führendes Main, R8 Enthaltung ohne Ort setzt Beleg voraus, E3/E9 Fassung 4), docs/beweise/SONDE-013.md (PRE/POST-Klassen, Experimentlebenszyklus, Taint M-62, WN-04 Passage im Proposal). Quellen an ZIEL: broker/src/coordinator/proposal.rs (`proposal()`, `gate_felder_vollstaendig`, `stopbedingungen`, `passage_id.as_ref()?`), proposal_verdrahtung.rs (`proposallage_locked`, `vorschlaege_bilden`, `vorschlag_persistieren`, `darf_draft_offer`, `draft_offer_ziele`, `draft_offer_zustellen`, `messbare_guardrails`, `capability_vorhanden`), hypothese.rs (`Quellprofil.prepost_paar`, Wahl von `claim_class`, `ursachenklasse`, `next_test`, `pre_post`, `listen_for` um :2072 bis :2209; die zwei Achsen :2096-2104), hypothese_verdrahtung.rs (Paarnaht :243-260, Passagenwahl :336-338), prepost.rs (`Alignmentklasse`, `beurteile_paar`, `dreifachergebnis`, `beschreibe_wirkung`), befehl.rs (`persistenz_p0_intern` :148-178, `user_verdict_p0` :545-568, `befunde_gegen_store_haerten` :185), experiment.rs (`juengste_passage_im_projekt` :919-923, `Terminal`, `Achsenrechnung.vergleichbarkeit`), zustand.rs (`Taintstand`, `Stand::taint`), experiment_verdrahtung.rs (:120, :128, :133 `unknown_target`), mitgliedschaft.rs (`fuehrendes_main`), eq-copilot/plugin/src/SourcesModel.cpp und SourcesModel.h (`controlEnde`, `beginneSubscription`, `uebernehmeEvidenzruecknahme`, `uebernehmeSnapshot`, `darfAudition`/`darfDraft` :187-189), eq-copilot/plugin/core/ipc/ControlClient.cpp (`fehlercodeHaeltVertrag` :74-81), eq-copilot/schemas/v3/eq-ipc-v3.schema.json ($defs/proposal, session_finding, user_verdict, command_ack, fehlercode, aussageklasse, next_test), broker/tests/sonde014_gegenbeispiele.rs (Modulkopf :30, `luecke` :465-483), broker/tests/sonde014_proposal.rs, sonde014_befund.rs, sonde014_verdrahtung.rs, sonde013_prepost.rs, sonde013_experiment.rs, eq-copilot/plugin/tests/Sonde014BefundTest.cpp, Sonde012SourcesModelTest.cpp, tools/eq-copilot/pruefe_p5_korpus.py (`offene_luecken`), eq-copilot/fixtures/p5-korpus/MANIFEST.json und sitzungen.json, tools/beweise.ps1:594 (A31-Behauptung).

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: eine Matrixzeile V-01 bis V-44 oder ein Entscheid E1 bis E9 widerspricht einem Satz des Gate-Bezugs oben, dem Entwurf §36.1 oder Zeile 1669, einer unverändert geltenden Zeile der SONDE-014-Matrix (insbesondere M-17, M-25 bytegleiches Ranking, M-30 Sperre am Zustand, M-46 vollständiges Objekt oder keines, M-77 dieselbe Regel in allen Sprachen), der NAK-212-Matrix (N-43) oder der NAK-213-Matrix (E7/R4, R8), einem bestehenden Vertrag, dem gemessenen Code an ZIEL oder einer Invariante aus CLAUDE.md („Schemas sind Verträge": neue persistente Felder zuerst versionieren, Altstände laden, unbekannte Felder dürfen alte Consumer nicht zerstören; „Keine toten UI-Elemente"; „Begriffe nicht vermischen"; „State bleibt verlustfrei"; Audio-Thread ohne Sperren); ODER eine Regel R1 bis R5 ist von keiner Zeile mit messendem Test (Datei::Fall) und Rotbeweis gedeckt; ODER einer der fünf zugeordneten Befunde (E-D5, E-D6, E-L2, E-L5, A5) bleibt nach der Matrix offen; ODER eine der drei Lückenstellen in §2.6 hat eine erwartete Ausgabe, die dem Gate-Text widerspricht (eine falsche starke Behauptung oder ein Proposal ohne Ziel als „zulässig"); ODER die Matrix lässt einen Fall aus, in dem nach ihren eigenen Regeln zwei verschiedene Ausgaben möglich wären (Nichtdeterminismus, M-25); ODER ein Entscheid weicht vom Wortlaut einer Regel ab, ohne dass §2.0 die Abweichung benennt und trägt (die vier Lesarten oben sind getragen).
- LÜCKE: Gate, Matrix, Regeln und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, kein NEEDS_WORK-Grund):
1. Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände in docs/**; der Manifestkopf; §3 (Prüfliste), §4 (Nebenbefunde NB-1 bis NB-5) und §5 (Verlauf) von docs/beweise/NAK-214.md; docs/beweise/G5.md; docs/beweise/NAK-212.md; docs/beweise/NAK-213.md; docs/offene-punkte.md. Nur §1 und §2 sind Prüfgegenstand; §4 liest du als Kontext (siehe Frage 12).
2. Im Register datierte Härtungen und Lücken NAK-191 bis NAK-211 und NAK-215 bis NAK-222. Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. Ticketfremdes — NAK-212 (Zusammenhangsgröße, Sicherheitsklasse, Alignment, Passage, Trennung) und NAK-213 (Screening, Deckel, Konkurrenzlage, Master-Wahl, Kanaltafel, Kanalwechsel; beides nur genutzt), die Anzeige-Gestaltung in Gen (S31b), der P6-Lieferumfang — ist nur insoweit Prüfgegenstand, als die Matrix dorthin abgrenzt: dann prüfst du, ob die Abgrenzung stimmt. Kosmetik, Stil: nein.

WAS DU TUST:
1. Lies das Manifest §1 und §2 vollständig, dann die Regeln R1 bis R5, dann die zitierten Quellen an ZIEL und die Referenzmatrizen.
2. Prüfe je Regel R1 bis R5: Welche Zeilen V-nn decken sie, welcher Test misst jede Zeile (bestehend mit Datei::Fall oder NEU mit Bein), und gibt es je Zeile einen Rotbeweis oder eine ausdrückliche Kennzeichnung als Kontrollfall bzw. Regressionswache?
3. Prüfe E1 (R1, V-01 bis V-13): Deckt der Schlussriegel am Ende von `proposal()` zusammen mit der Vorbedingung „ohne Ziel kein Objekt" alle drei Wirkungen — Zustandshaltung, Persistenz, Zustellung — über die an ZIEL gemessene Aufrufkette (`vorschlaege_bilden` → `filter_map`, `vorschlag_persistieren` über `neue`, `darf_draft_offer`/`draft_offer_ziele` über `paare`)? Gibt es einen weiteren Weg, auf dem ein Proposal-Objekt entsteht, persistiert oder zugestellt wird (Store-Replay, Projektion, Undo, Sicht, Tests-Helfer im Produktpfad), den der Riegel nicht passiert? Deckt die Matrix: eindeutiges Main, zwei Mains (`fuehrendes_main = None` → Enthaltung ohne Ort → kein Proposal), Main-Rückzug zwischen Befund und Proposal, jedes der sechs Gate-Felder einzeln fehlend (V-05 bis V-10) — und kann ein Test je Feld einen Rotbeweis erzeugen, ohne den Erzeuger zu umgehen? Ist „der Riegel fällt stumm" mit §59 Satz 1 und „Keine toten UI-Elemente" vereinbar: bleibt die Enthaltung ohne Ort mit `next_test` und `listen_for` sichtbar, und behauptet Gen an keiner Stelle ein Proposal? Bleibt `darf_draft_offer` (M-52) unverändert (V-13)?
4. Prüfe E2 bis E5 (R2, V-14 bis V-22): Ist das Strukturprädikat aus E3 (Klasse `>= AudioAligned`, `ausgerichtet_db` `Some` mit mindestens einem Band `ausgerichtet_gueltig = true`, `wirkung` `Some`) an `dreifachergebnis` (:811-852) genau die Stelle, an der das ausgerichtete Delta entsteht — kann ein Paar Punkt 1 erfüllen und Punkt 2 nicht (alle Bänder nicht-endlich), und welche Zeile misst das? Ist V-14 („Paar mit Wirkung 0" / „breitbandig gleichmäßig" bleibt Wirkungsbeleg) mit M-17 „reproduzierbare Veränderung" vereinbar, oder braucht die Matrix hier eine Abgrenzung? Zweiteilung `prepost_paar`/`prepost_wirkungsbeleg` (E4): sind alle Leser von `prepost_paar` an ZIEL aufgezählt (um :2072, :2079, :2158, :2185 — gibt es weitere in hypothese.rs, sicht.rs oder den Tests?), und ist die Beibehaltung von `pre_post = "post"` aus `prepost_paar` mit „Begriffe nicht vermischen" verträglich? Bleibt die `next_test`-Kette (E5) in Reihenfolge und Werten unverändert, mit genau einer geänderten Bedingung? Bleibt der Vertrag unberührt (Werte von `aussageklasse`, `ursachenklasse`, `next_test`; Fassung 4)? Deckt die Matrix: Paar `>= AudioAligned` mit Wirkung, `>= AudioAligned` ohne gemessene Wirkung, `Probable` mit Ergebnis (V-17), ohne Paar (bestehender Fall `parallele_telemetrie_bleibt_klasse_eins`), `Unclear`? Ist die Klassenwahl deterministisch (M-25)?
5. Prüfe E6 (R3, V-23 bis V-29): `controlEnde` → alle Befunde `stale`; Rücknahme ohne Folge-Snapshot → alle `stale` (fail-closed); Sitzungswechsel `clear()`. Ist der Ort der Änderung (der `zustand`-String, nicht `darfAudition`/`darfDraft`, SourcesModel.h:187-189) M-30-konform? Rückweg: `uebernehmeSnapshot` ersetzt die Liste als Ganzes (V-26); kein Snapshot → bleibt `stale` (V-27); Reconnect mit Snapshot derselben Sitzung gegen Reconnect mit neuer `session_epoch` — deckt die Matrix beide, und `findingsOffen`/`paare`/`experimente` mit? Welche C++-Tests messen die Zeilen (Sonde014BefundTest, Sonde012SourcesModelTest; Beine B13/B28), und sind sie ohne FL fahrbar? Hat `uebernehmeEvidenzruecknahme` an ZIEL einen Pfad, auf dem nach der Rücknahme doch ein READY-Befund stehen bleibt (der Befund A5)?
6. Prüfe E7 und E8 (R4, V-30 bis V-36): Riegel hinter dem Idempotenzcheck — deckt die Matrix mit Test und Rotbeweis: `finding_id` existent → angenommen und persistiert; nie existent (32 Hexzeichen, unbekannt) → `abgelehnt`/`unknown_target`, kein Append, kein Domänenereignis; angenommen, dann Befund zurückgenommen, dann Wiederholung desselben `command_id` → `idempotent_wiederholt`; `stale`- und `more_data`-Befund → angenommen; Wiederholung eines abgelehnten Befehls → erneut abgelehnt (nie committet, also nicht idempotent)? Ist der Existenzcheck gegen `stand.befunde` deterministisch gegenüber dem Snapshot-Timing — ein Befund, den der User im Snapshot sah, der aber inzwischen aus `stand.befunde` gefallen ist (Rücknahme, Invalidierung, Sitzungsende): welche Antwort, und ist das mit „verlorene Userarbeit" (Modulkopf befehl.rs) vereinbar? Die Session wird aus `kopf.ziel` gebildet: kann eine `finding_id` einer anderen Session desselben Projekts durchrutschen oder fälschlich abgewiesen werden?
7. Prüfe E9 (R5, V-37 bis V-44): die drei Ausschlussgründe an der Quelle (`Terminal::Abgebrochen` mit beiden Gründen; offene Intervention im `Taintstand`; `Terminal::Ergebnis` mit `vergleichbarkeit == None`); Taintmenge über alle Taintstände des Stands; Filter vor `max_by_key(folge)`; „sonst rechnet die Aufnahme ohne Passage" — was heißt das an ZIEL (hypothese_verdrahtung.rs:336-338: `passage_id = None` → welche Kette, welche Enthaltung, N-43-Abgrenzung)? Deckt die Matrix: jüngste gültig, jüngste zurückgenommen mit älterer gültig (V-38), jüngste getaintet, Vergleichbarkeit unbekannt, `unvergleichbar` (V-41), keine taugliche Passage, gleiche `folge` (V-42 Kontrollfall), Experiment einer anderen Sitzung desselben Projekts, Taint einer anderen Sitzung? Bleibt NAK-212 E5/N-43 (Master-Belege vor der Passage) unverändert und eindeutig abgegrenzt?
8. Prüfe §2.6: Stimmt die Messung an ZIEL (kein `luecke`-Aufrufer in sonde014_gegenbeispiele.rs, kein `luecke`-Feld in sitzungen.json, kein `nicht_gemessen` im P5-MANIFEST, tools/beweise.ps1:594)? Sind die drei Fälle mit erwarteter Ausgabe gegen den Gate-Text richtig (keine falsche starke Behauptung, kein Proposal ohne Ziel als „zulässig")? Ist Fall 3 (`Buehne::mit_store`, abgebrochenes jüngstes Experiment, älteres tauglich) deterministisch spezifiziert? Bleibt die gedruckte Zusage in Runnerkopf, Modulkopf und Manifestkopf nach dem Bau wahr?
9. Prüfe §2.7: Ist jede Zeile, die als „unverändert" steht, wirklich unberührt, und trägt jede „präzisiert"/„Nachtrag"-Zeile ihren Nachtrag ohne Widerspruch zum Wortlaut der Ursprungszeile?
10. Prüfe §2.8: Ist „kein Fassungsschritt" an allen fünf Wegen haltbar — insbesondere: ist `Quellprofil.prepost_wirkungsbeleg` rein intern und kein Leitungsfeld; trägt `command_ack` mit `unknown_target` auf `user_verdict` keine neue Kombination, die pruefe_v3_vertrag.py, erzeuge_v3_fixtures.py oder ein Fixture erfassen müsste; bleibt A5/A8 damit unverändert grün?
11. Prüfe §2.9 (Nicht-Zusagen): Enthält es etwas, das eine Regel R1 bis R5 verlangt?
12. Lies §4 (NB-1 bis NB-5) nur als Kontext: Ist ein Nebenbefund in Wahrheit ein Defekt der Matrix gegen die Regeln — insbesondere NB-4 (`pre_post` aus der Paarexistenz) gegen E4 und NB-5 (bereits persistierte schemaungültige Proposals) gegen R1 „wird persistiert" und „State bleibt verlustfrei"?

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Zeilen-ID (V-nn oder E-n) oder Manifestzeile, Quelle Datei:Zeile, welcher Satz von Gate, Regel, Entwurf, Vertrag, Referenzmatrix oder Invariante bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
