NAK-214 — Nacharbeit 1 nach der Erstprüfung (Etappe 2, Runde 1). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec; vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Keine Neben-Refactors, keine Legacy-Umbenennung. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor jedem Commit abhaken und im Manifest §3 nennen, wo jede Zeile gemessen ist. Sprache: Docs, Commits, Manifest Deutsch; Code-Bezeichner wie im Bestand. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks. Typografische Anführungszeichen in Rust-Strings schließen mit U+201C, nie mit ASCII-`"`.

LAGE: Die Erstprüfung nach dem Bau (Codex gpt-6-astra max, lesend, Thread 01a081dd-842a-7f93-8c48-194b95d95887, Stand 184fe645) meldet NEEDS_WORK mit genau zwei Defekten — beide in der Testabdeckung von R4, kein Produktdefekt; Urteil wörtlich in docs/beweise/roh/NAK-214-erstpruefung-184fe64.txt, Prüfauftrag in docs/beweise/roh/NAK-214-erstpruefung-auftrag.txt. Der Dirigent hat beide an der Quelle bestätigt. Du schließt genau diese zwei, sonst nichts. Kein Produktcode wird angefasst, sofern die Tests nicht zeigen, dass der Riegel selbst falsch liegt — dann Ursache im Manifest benennen, beheben, und nur dann ist Produktcode Teil dieser Runde.

DIE ZWEI DEFEKTE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT V-32 — der `stale`-Kontrollfall ist ungemessen. Prüfer: „Die Bühne liefert frisch berechnete Befunde über `zustand_aus_sicherheit(..., false)`; dieser Filter erfasst daher nur `MoreData`, niemals `Stale`. Damit bleibt der vorgeschriebene Kontrollfall V-32 ungemessen: Ein versehentliches Zurückweisen ausschließlich veralteter Befunde würde nicht auffallen. Der Zustand ist produktiv erreichbar: Befund F erzeugen → wirksames `intent_update` ohne Folge-Evidenz → `Stale` ausdrücklich prüfen → `user_verdict` mit neuer `command_id` senden und Annahme samt Persistenz prüfen. Der bestehende Test `rollenaenderung_macht_den_vorschlag_stale` zeigt bereits diesen Zustandswechsel; die Abweichungsbegründung trägt deshalb nicht." Bestätigt: `broker/tests/sonde014_verdrahtung.rs:3408–3425` (`user_verdict_auf_more_data_und_stale_wird_angenommen`) filtert `zustand != ReadyToSend` auf frisch gerechneten Befunden; `zustand_aus_sicherheit` (hypothese.rs) liefert `Stale` nur bei `veraltet = true`, und die Bühne setzt das nie. Die Manifestzeile 1006 („Präzisierung gegenüber der Matrix … die Bühne trägt beide Klassen") behauptet damit mehr, als der Test misst. REGEL: V-32 wird als eigener Teilfall (oder eigener Test `user_verdict_auf_stale_wird_angenommen`, wie die Matrix ihn nennt) über den produktiven Pfad gemessen: ein existenter Befund F wird durch den bestehenden Weg veraltet (`intent_update` ohne Folge-Evidenz, Muster `rollenaenderung_macht_den_vorschlag_stale`), der Test prüft ausdrücklich `zustand == Befundzustand::Stale`, sendet dann `user_verdict` mit neuer `command_id` auf F und prüft `ergebnis = "angewandt"`, das persistierte Domänenereignis und `state_hash`. Rotbeweis `NAK-214-rot-V-32.txt`: der Existenzriegel schließt `Stale` aus (Zustandsbedingung eingezogen) → der Test wird rot; mit dem gebauten Riegel grün. Manifest: Zeile V-32 nennt den messenden Test; die Präzisierung in §6 (Zeile 1006 ff.) wird auf die Wahrheit umgeschrieben, dass V-31 aus der frischen Bühne und V-32 aus dem veralteten Befund kommt.

2. DEFEKT V-34 — der Teilfall „Sitzung ohne Befunde" fehlt. Prüfer: „Dieser Negativtest setzt ausdrücklich vorhandene Befunde voraus und prüft anschließend nur eine darin unbekannte ID. Der in V-34 verlangte zweite Teilfall mit fehlendem Sitzungseintrag fehlt. V-35 ersetzt ihn nicht: Dort kehrt ein bereits angenommener Befehl vor dem Existenzriegel idempotent zurück. Die fehlende Eingabefolge lautet: Main anmelden und State melden, ohne Befunde zu erzeugen → neue `command_id` mit wohlgeformter unbekannter `finding_id` senden → `abgelehnt/unknown_target` und keinerlei Persistenz prüfen." Bestätigt: `sonde014_verdrahtung.rs:3339–3344` (`user_verdict_ohne_befund_wird_abgewiesen`) baut `buehne_mit_passage(&c, 1, 12)` und verlangt `!befunde.is_empty()`; der `None`-Zweig von `stand.befunde.get(&session)` wird nirgends gefahren; Manifestzeile 1016 („derselbe Zweig") ist Behauptung ohne Messung. REGEL: V-34 bekommt den zweiten Teilfall über den produktiven Pfad — ein Main meldet sich an und meldet State, ohne dass Evidenz oder Befunde entstehen (`stand.befunde` ohne Eintrag für die Sitzung; im Test ausdrücklich prüfen), dann `user_verdict` mit neuer `command_id` und wohlgeformter unbekannter `finding_id` (32 Hexzeichen) → `command_ack` mit `ergebnis = "abgelehnt"`, `code = "unknown_target"`, `store.command_event_lesen(command_id)` bleibt `None`, kein `user_verdict`-Domänenereignis, kein `internal_p0_command`. Rotbeweis `NAK-214-rot-V-34.txt`: der `None`-Zweig behandelt eine fehlende Sitzung als „existent" (oder überspringt den Riegel) → der Test wird rot. Manifest: Zeile V-34 nennt den Teilfall; §6 Zeile 1016 wird auf die Messung umgeschrieben.

BEINE: Nur A4 ist betroffen (`cargo test --manifest-path broker/Cargo.toml`, gezielt die Datei `sonde014_verdrahtung` und danach das ganze Broker-Bein); kein C++-Bein, kein Vertrag, kein Korpus, kein neues Bein, kein voller Kanon in dieser Runde (den Abschlusskanon setzt der Dirigent nach der Wiederprüfung). Die Rotbeweise als Datei unter docs/beweise/roh/ ablegen, wie die bestehenden 22 (Mutation benennen, rote Ausgabe, Rücknahme, grüne Ausgabe; Rücknahme am Testergebnis verifizieren, nicht am Kopiervorgang — `mtime`-Landmine in CLAUDE.md).

MANIFEST docs/beweise/NAK-214.md: §5 Verlauf (append-only) bekommt „Erstprüfung — NEEDS_WORK (08.09.2026, Stand 184fe645)" mit Thread, Modell, Effort, Rohdatei und der Einordnung (zwei Defekte, beide Testabdeckung R4) sowie „Nacharbeit 1" mit den geänderten Tests, Rotbeweisen und Manifestzeilen; Kopftabelle: Zeile „Phase" auf „Nacharbeit 1 nach der Erstprüfung (NEEDS_WORK, zwei Testdefekte) — 08.09.2026", neue Zeile „Änderungssatz — Nacharbeit 1" wie in docs/beweise/NAK-213.md; Prüfliste §3 nachziehen (Zeile E „Behauptung ≤ Messung" trägt jetzt V-32 und V-34 als gemessen); die Urteilsmarke T1 PASS bleibt stehen. Nebenbefunde nummeriert ab NB-8 (oder der nächsten freien Nummer) ins Manifest, nie ins Register.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen — `Stale` wirklich über den produktiven Weg und nicht per Konstruktor gesetzt; `command_id`s neu und ohne Kollision mit V-30/V-33/V-35/V-36; der Teilfall ohne Befunde prüft den fehlenden Sitzungseintrag, nicht eine leere Liste nach Rücknahme; kein Produktcode geändert (oder Ursache benannt); keine Manifestsätze, die mehr behaupten als der Test misst; `#[cfg(windows)]` wie bei den Store-Fällen im Bestand. Dann Commit mit Pathspec (broker/tests/sonde014_verdrahtung.rs, docs/beweise/NAK-214.md, docs/beweise/roh/NAK-214-rot-V-32.txt, docs/beweise/roh/NAK-214-rot-V-34.txt), Betreff „NAK-214 Nacharbeit 1: …", pull --rebase, push, STOPP. Die Wiederprüfung 1 setzt der Dirigent.
