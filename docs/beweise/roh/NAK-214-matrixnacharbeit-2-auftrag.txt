NAK-214 — Matrixnacharbeit 2 (Etappe 1, Runde 2). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (docs/beweise/NAK-214.md); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Kein Produktcode, kein Test, keine Fixture — diese Runde ändert ausschließlich die Verhaltensmatrix in docs/beweise/NAK-214.md. tools/dirigent/pruefliste.md ist deine Arbeitsliste. Lösche nie mit dem PowerShell-Lösch-Cmdlet; selbst angelegte Temp-Dateien nur über [System.IO.File]::Delete auf den exakten Pfad. Mehrzeilige Commitnachrichten über `git commit -F <datei>`, nie über `-m` mit Backticks.

LAGE: Matrixprüfung 2 (Codex gpt-6-astra max, lesend, Thread 01a080d5-38de-7961-84fc-5aa8f028602a, Stand b204bbcb) bestätigt sechs der acht Befunde der Matrixprüfung 1 als geschlossen und meldet nichts Gebrochenes; zwei bleiben teilweise offen — Urteil wörtlich in docs/beweise/roh/NAK-214-matrixpruefung-2-b204bbc.txt, Prüfauftrag in docs/beweise/roh/NAK-214-matrixpruefung-2-auftrag.txt. Der Dirigent hat beide an der Quelle bestätigt. Du schließt genau diese zwei, sonst nichts. Zeilen-IDs V-01 bis V-48 bleiben stabil; neue Zeilen bekommen die nächste freie Nummer (ab V-49). Nichts, was der Prüfer nicht erhoben hat, kommt dazu.

DIE ZWEI BEFUNDE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT E9/V-44/V-46 — der Terminalübergang bei laufendem Nachlauf ist ungemessen. Prüfer: „Für einen Experimentabschluss bei bereits positivem Restnachlauf fehlt die ausdrücklich verlangte messende Terminalzeile. `intervention.rs:446–459` entfernt aktive Intervalle, lässt den Nachlauf aber bestehen; nach R7 muss deshalb auch dessen gerettete Experimentzuordnung bestehen bleiben. V-46 fährt nur das Interventionsende, V-44 schließt Terminals ausdrücklich aus, und V-48 kann einen Zuordnungsverlust nicht erkennen, weil `Abgebrochen` unabhängig davon ausgeschlossen bleibt. Ein vorzeitiges Löschen der Zuordnung bei `Terminal::Ergebnis` bliebe damit ungemessen und gäbe die Passage entgegen R7/M-58 frei." Bestätigt: die E9-Tabelle zum Leeren der Nachlaufmenge nennt Nachlauffortschritt (Samples, Zeit) und den bestätigten Nachbericht-Abschluss; der Weg über `taint_intervalle_des_experiments_schliessen` (jede Art außer `experiment_begin`, `experiment_verdrahtung.rs:338–339`) steht dort nicht, und V-41 („`Terminal::Ergebnis` mit bekannter Vergleichbarkeit → wählbar") nennt keine Nachlaufbedingung — für ein Experiment, das mitten im Nachlauf sein Ergebnis bekommt, lässt die Matrix damit zwei Ausgaben zu (M-25). REGEL (Dirigent, 08.09.2026, Präzisierung von R7): Ein Terminal — `Ergebnis` wie `Abgebrochen` — lässt die gerettete Nachlaufzuordnung unberührt; sie wird ausschließlich durch Nachlaufende (`tail_samples_offen` auf 0) oder bestätigten Resync geleert (R7 wörtlich). Daraus folgt für die Passagenwahl: `Terminal::Ergebnis` mit bekannter Vergleichbarkeit bei `tail_samples_offen > 0` ist NICHT wählbar, solange der Nachlauf läuft, und wird mit dem Nachlaufende wählbar (dann greift V-41); `Terminal::Abgebrochen` bleibt unabhängig davon dauerhaft ausgeschlossen (V-48). Neue Zeile(n) ab V-49 mit Test (Datei::Fall) und Rotbeweis: das Terminal leert die Zuordnung → die Passage wird mitten im Nachlauf gewählt; die Freigabe nach Nachlaufende wird im selben Fall gemessen (aktivieren↔abklingen). Die E9-Tabelle „wann die Menge geleert wird" bekommt die Zeile „Terminal des Experiments: Menge unverändert" mit Quelle `intervention.rs:446–459`; V-41 und V-44 verweisen auf die neue Zeile, damit kein Zustand zwei Ausgaben hat.

2. DEFEKT V-12 — die Paarzuordnung im produktiven Pfad ist nicht mehr gemessen. Prüfer: „Das leere `listen_for` macht den selektiven Fall herstellbar, aber die Verlagerung auf einzelne `proposal()`-Aufrufe verliert die verlangte Messung von `paare`. `proposal.rs:722` kopiert lediglich die eigene `finding_id`; die tatsächlichen Befund-/Proposal-Paare entstehen erst in `proposal_verdrahtung.rs:58–61` und werden bei `:86–89` gemeinsam ausgewertet. Eine nach dem Herausfiltern verschobene Paarzuordnung ließe sämtliche jetzt beschriebenen Einzelaufrufe grün." Bestätigt: `vorschlaege_bilden` bildet `paare` per `filter_map` über alle Befunde der Sitzung (:58–61), leitet `neue` daraus ab (:62) und wählt `angebot` aus denselben Paaren (:86–89); V-12 nennt als Test `sonde014_proposal.rs::riegel_trifft_nur_den_unvollstaendigen`, der den Erzeuger einzeln ruft und diesen Pfad nicht erreicht. REGEL: V-12 fährt den produktiven Pfad: mehrere Befunde einer Sitzung liegen in `stand.befunde`, genau einer mit leerem `listen_for` (direkt im Stand gesetzt, weil am Produktpfad nicht erreichbar, V-08); `vorschlaege_bilden` läuft über den Coordinator; gemessen wird am Bestand — `stand.vorschlaege` und `vorschlaege_sicht` enthalten genau die Vorschläge der übrigen Befunde, jeder mit der `finding_id` seines Befunds, kein Vorschlag ohne Befund, und `angebot` (sofern eines entsteht) gehört zu einem Befund, der einen Vorschlag hat. Test in `sonde014_verdrahtung.rs` (Datei::Fall benennen); Kontrollfall wie bisher, ohne eigenen Rotbeweis — oder mit Rotbeweis, wenn eine Mutation der Paarbildung (`zip` statt `filter_map`-Paar) den Fall rot macht; dann sag, welche.

VERLAUF: Trage im Manifest §5 (append-only) „Matrixprüfung 2 — NEEDS_WORK (08.09.2026, Stand b204bbcb)" mit Thread, Modell, Effort, Rohdatei und der Zuordnung (sechs geschlossen, zwei Defekte) ein, und darunter „Matrixnacharbeit 2" mit den geänderten Zeilen. Kopftabelle: Zeile „Phase" auf „Etappe 1 — Matrixnacharbeit 2 nach Matrixprüfung 2 (NEEDS_WORK, zwei Restbefunde)"; Regeln-Zeile um die R7-Präzisierung (Quelle: diese Auftragsdatei docs/beweise/roh/NAK-214-matrixnacharbeit-2-auftrag.txt). Der lebende Kopf und §2 werden umgeschrieben, §5 nur angehängt.

SELBSTAUDIT vor dem Commit: die geänderten Zeilen gegen den Code an HEAD lesen (Nachlauf 0 gegen 1 Sample beim Terminal, `Ergebnis` gegen `Abgebrochen`, Resync während Terminal, zwei Befunde gegen drei mit einem leeren `listen_for`, `angebot` mit und ohne handelbaren Vorschlag); keine Zusage ohne Test und Rotbeweis oder ehrliche Kennzeichnung; keine zwei Zeilen mit zwei Ausgaben für denselben Zustand (V-41↔neue Zeile, V-44↔V-48). Dann `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-214.md` grün, Commit mit Pathspec docs/beweise/NAK-214.md, Betreff „NAK-214 Matrixnacharbeit 2: …", pull --rebase, push, STOPP. Die Matrixprüfung 3 setzt der Dirigent.
