Erstprüfung 1 SONDE-014 — Codex gpt-6-astra, Effort max, lesend, Thread 01a079da-2aab-7540-8173-c6733c7739ca; HEAD vor und nach dem Lauf dce6970c7428bd25f24df9fe953bddb43204f9c6; Start 2026-09-07T05:12:15, Ende 05:43:25 (31 min), EXIT=0. Auftrag: docs/beweise/roh/SONDE-014-erstpruefung-1-auftrag.txt. Rohurteil unverändert:

URTEIL: NEEDS_WORK — mehrere konkrete Gate- und Matrixverletzungen.

Geprüft: begrenzter Diff, 61 Rust-Testfälle, vorhandene C++-Binaries einschließlich Nulltest, Identität, State, Queue-Stress und IPC sowie A5/A8/A28–30 grün; lesende Gegenproben wie beschrieben, HEAD vor/nach unverändert auf dce6970c7428bd25f24df9fe953bddb43204f9c6 und keine eigenen Dateiänderungen.

Nicht geprüft: Neubau, vollständiger Kanon, schreibende Store-/Killreproduktionen und Wiederholung aller 103 Rotbeweise; A12 unter Python 3.14 wegen fehlendem rfc8785 blockiert, Python 3.13 nicht startbar, A29 nutzte das vorhandene Ergebnisartefakt und die übrigen Produktpfadbefunde beruhen auf Codeinspektion.

Full review comments:

- [P1] Sperre die Rechnung auch bei vollständig fehlendem Intent — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese_verdrahtung.rs:139-142
  DEFEKT gegen M-86: Bei `intent == None` ist diese Bedingung falsch; ohne jede Vollständigkeitsmeldung entstehen daher bereits Findings und Proposals. Derselbe Fehler steht in `hypothesen()`. Der ausgeführte Test `broker/target/debug/deps/sonde014_hypothese-3c6b314470c00640.exe --exact causehypothesis_traegt_die_zehn_felder` demonstriert den Pfad: Sein Aufbau sendet keinen Intent und erwartet trotzdem einen Befund. Der vorhandene Sperrtest prüft dagegen nur einen vorhandenen, unvollständigen Bestand beziehungsweise die nicht verwendete Hilfsfunktion `darf_rechnen()`. Damit kann ein noch nicht übertragenes Schutzveto fehlen.

- [P1] Erhalte Teilupdates anderer Koaleszierungsschlüssel — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/intent.rs:404-405
  DEFEKT gegen M-85: Der Sender koalesziert je Quelle/Scope, während diese Ablehnung die globale Bestandsrevision verwendet. Reprofolge: Teilupdate A/Revision 1, B/Revision 2, anschließend A/Revision 3 vor dem Abfluss einreihen. `P1Warteschlange::einreihen()` ersetzt A an seiner bisherigen Position, sodass A/3 vor B/2 ankommt; B/2 wird hier vollständig verworfen. Damit kann beispielsweise die geschützte Rolle einer anderen Quelle dauerhaft im Broker fehlen. Die positionsbewahrende Koaleszierung wurde mit `EqCopIpcTest.exe` nachgemessen; die resultierende Ablehnung ist am Empfangspfad nachvollzogen.

- [P1] Prüfe Rechenergebnisse vor der Veröffentlichung erneut — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese_verdrahtung.rs:297-302
  DEFEKT gegen M-10/M-24: `hypothesen_bilden()` kopiert den Eingang unter dem Lock, rechnet außerhalb und trägt anschließend bedingungslos ein. Repro-Interleaving: Aufnahme mit Intent-Revision 1 sammeln, während der Rechnung Revision 2 empfangen und den Befund invalidieren, danach das alte Ergebnis hier eintragen. Dadurch überschreibt ein alter READY-Befund den bereits auf STALE gesetzten Bestand; `session_snapshot_json()` prüft die Revision ebenfalls nicht erneut. Entsprechend können zwischenzeitlich zurückgenommene Evidenz-IDs wieder veröffentlicht werden. Dieser Nebenläufigkeitspfad wurde gelesen, nicht mittels einer neuen Barrieretest-Instrumentierung ausgeführt.

- [P1] Verifiziere Evidenz-Referenzen gegen den Store — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/sicht.rs:635-640
  DEFEKT gegen M-28: Der sichtbare Snapshot serialisiert ausschließlich den Cache; auch die Erzeugung filtert lediglich dessen `ausschlussgrund`, ohne die referenzierten Store-Zeilen zu prüfen. Leserepro für einen isolierten Storetest: Befund erzeugen, mit `DELETE FROM evidence` dessen Belege entfernen und anschließend `session_snapshot_json()` aufrufen. Der gespeicherte Befund wird unverändert einschließlich seiner fehlenden IDs ausgegeben. Der vorhandene M-28-Test verwendet keinen Store und prüft nur eine Teilinvalidierung im Speicher; er misst die ausdrücklich zugesagte Prüfung gegen SQLite nicht.

- [P1] Prüfe die Mindestfensterzahl auch für den Master — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1328-1336
  DEFEKT gegen M-23: `fenster` zählt ausschließlich die Kandidatenfenster; auch das vorgelagerte Passagengate prüft die Mindestzahl nur beim Kandidaten. Repro-Eingang für `hypothesen()`: vollständiger Intent, bekannte Route, eine Passage mit acht unabhängigen Kandidatenfenstern, aber nur einem überlappenden Masterfenster. Das relative Alignment besteht wegen der Normierung auf das kürzere Intervall, und ein einzelner Kandidat erreicht hier `hoch`/READY. Verlangt sind dagegen mindestens acht unabhängige Fenster je beteiligter Quelle innerhalb der Passage, andernfalls MORE DATA mit `passage_zu_kurz`.

- [P2] Behalte das Anomalieband im zurückgerechneten Intervall — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1229-1230
  DEFEKT im Frequenzdatenweg von M-18/M-19: Die Rückabbildung passt nicht zur Gruppierung `band * 64 / 221`. Nachgemessene Reproduktion: `python -B -c "b=100;g=b*64//221;print(g*221//64,(g+1)*221//64)"` liefert `96 100`. Eine Anomalie in Band 100 wird damit dem halboffenen Intervall `[96,100)` zugeordnet, das dieses Band gerade ausschließt. Die Beobachtung stammt weiterhin aus Band 100, während Rangrechnung, Maskierung und Proposal-Zielbereich andere Bänder lesen. Das betrifft die erzeugten Daten, nicht die ausgeschlossene Oberflächengestaltung.

- [P2] Verlange die Passage auch für NoChange- und MoreData-Proposals — C:/Users/phili/Projekte/Nakama/eq-copilot/schemas/v3/eq-ipc-v3.schema.json:1989-1993
  DEFEKT gegen das Exit-Gate und M-43/M-46: `passage_id` fehlt in `required`. Die lesende Gegenprobe hat das Feld jeweils aus `draft_offer`, `draft-offer-mehr-daten` und `draft-offer-keine-aenderung` entfernt; `Draft202012Validator.is_valid()` blieb bei allen drei Objekten wahr. Auch der Broker erzeugt vor einer benannten Passage entsprechende unvollständige Objekte; sein Sechs-Felder-Test überspringt die Passage ausdrücklich. Dass anschließend kein Draft versendet wird, erfüllt nicht die Zusage vollständiger NoChange-/MoreData-Proposals. Writer, Reader und Fixtures müssen gemeinsam korrigiert werden ([AGENTS.md:51–52](AGENTS.md#L51-L52)).

- [P1] Sende den Assistentenschritt aus dem restaurierten Main-State — C:/Users/phili/Projekte/Nakama/eq-copilot/plugin/src/PluginProcessor.cpp:1941-1946
  DEFEKT gegen M-59/M-88/M-89: Der Sender liest den separaten Schatten `assistentSchritt`, aber `setStateInformation()` restauriert ausschließlich `zustand.assistent`. Reprofolge im vorhandenen B29-Aufbau: `getStateInformation()` → neuer Prozessor → `setStateInformation()` → `v3AssistantStepFuerTest()`. Der persistente Schritt ist vorhanden, die Wire-Nachricht bleibt jedoch leer; bei Reload im selben Prozessor kann stattdessen der vorherige Schritt gesendet werden. Zudem zählt der Schatten seine Revision unabhängig vom gespeicherten Zustand. Der bestehende Recall-Test prüft nur Main-State und Speicherbytes, nicht diese Sendeseite ([AGENTS.md:48–52](AGENTS.md#L48-L52)).

- [P1] Erhalte das Experimentziel über den Brokerneustart — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/experiment_verdrahtung.rs:1732-1732
  DEFEKT gegen M-48 und die gekoppelte Save/Load-Zusage: `experiment_json()` speichert das neue Ziel nicht, und die Wiederherstellung setzt es hier ausdrücklich auf `None`. Reprofolge: `experiment_begin` mit Ziel- und Schutzbändern → Brokerneustart auf demselben Store → manueller Experimentabschluss. Der weiterhin abschließbare Versuch benutzt anschließend die Heuristik statt der übergebenen Bereiche; damit kann sich auch die Guardrail-Auswertung ändern. `ziel_geraten` kennzeichnet den Verlust lediglich, erhält aber nicht den zugesagten Mit-Ziel-Pfad ([AGENTS.md:48–52](AGENTS.md#L48-L52)).

- [P1] Verdrahte user_verdict mit dem persistierenden Produktpfad — C:/Users/phili/Projekte/Nakama/eq-copilot/plugin/tests/IpcTestMain.cpp:5637-5644
  DEFEKT gegen M-73: Dieser grüne Test bekommt seinen ACK vom Testserver, nicht vom Coordinator. Repro am tatsächlichen Empfänger: das gültige `user_verdict.json` als P0 an `Coordinator::p0_mit_minor()` übergeben. Der Schema-Leser akzeptiert die neue Familie, aber `p0_json_mit_minor()` in `befehl.rs` hat keinen entsprechenden Zweig und endet in `_ => None`; weder Persistenz noch ACK entstehen. Auch `assistentAntwort()` sendet kein `user_verdict`. Somit belegt der ausgeführte B10-Test nur den generischen Transport, nicht das zugesagte dauerhaft wiederholbare Userurteil ([AGENTS.md:51–52](AGENTS.md#L51-L52)).

- [P2] Schreibe auch den zugesagten SQLite-Spiegel des Intents — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/intent.rs:486-493
  DEFEKT gegen M-71: Angenommene `intent_update`-Nachrichten ändern ausschließlich `stand.intent`, invalidieren gegebenenfalls Befunde und kehren erfolgreich zurück. Anders als beim AssistantStep gibt es für diesen Bestand keinen Store-Append. Nach einem Vollbericht beziehungsweise einer Rollenänderung enthält SQLite daher keinen versionierten SourceIntent-Spiegel für Suche und Crashdiagnose. Lesender Nachweis: `rg -n 'store|append|spiegeln' broker/src/coordinator/intent.rs` zeigt keinen Persistenzpfad. Der erhaltene autoritative Main-State ersetzt den zusätzlich zugesagten Spiegel nicht.

- [P1] Bewerte bei der Schwellensuche die neue Handelbarkeit — C:/Users/phili/Projekte/Nakama/tools/eq-copilot/pruefe_p5_korpus.py:220-225
  DEFEKT gegen M-31/M-66: Nach dem Absenken der Schwelle bewertet `_passt()` weiterhin die ursprüngliche Sicherheitsklasse statt der nun zugelassenen Handlung. Ein falscher Distraktor mit `mittel` gilt dadurch auch dann als richtig, wenn die Kandidatenschwelle ihn handelbar macht. Nachgemessen mit `python -B tools/eq-copilot/pruefe_p5_korpus.py`: Schon `mittel` erklärt sechs Befunde einschließlich Distraktor und Parent-Duplikat für handelbar, dennoch steht `davon_falsch=0`; `unklar` lässt sogar alle acht passieren. Damit ist die ausgegebene Schwelle keine Kalibrierung der zugesagten Handelbarkeit.

- [P2] Berechne Klassenmetriken gegen unabhängige Sollbefunde — C:/Users/phili/Projekte/Nakama/tools/eq-copilot/pruefe_p5_korpus.py:354-361
  DEFEKT gegen M-64: Die Ursachenklasse wird ungeprüft aus der Produktausgabe übernommen; als Wahrheit zählt lediglich die Quell-ID beziehungsweise eine Enthaltung. Reproduktion ausschließlich im RAM: Ergebnis über `_voraussetzung()` laden, beim `wahrer_kandidat` die erlaubte Klasse `quelle_resonanz` durch `peak_aus_transient` ersetzen und dieselben Auswertefunktionen aufrufen. A29 blieb in dieser Session grün und meldete auch für die falsche Klasse Precision/Recall 1,000. Zusätzlich entsteht der Recall-Nenner nur aus ausgegebenen Befunden, sodass ausgelassene Sollbefunde verschwinden. Ohne unabhängige Klassenlabels und Sollmengen messen diese Zahlen nicht die versprochene Ursachenklassifikation.

- [P2] Löse Alternativ-IDs zur tatsächlichen Distraktorquelle auf — C:/Users/phili/Projekte/Nakama/tools/eq-copilot/pruefe_p5_korpus.py:377-384
  DEFEKT gegen M-65/NAK-190: Gezählt wird nur die Länge beliebiger `alternatives`-Listen. Ob eine ID einen existenten, anderen Befund bezeichnet und dieser tatsächlich zur bekannten Distraktorquelle gehört, bleibt ungeprüft. Reproduktion ausschließlich im RAM: In der geladenen Ausgabe von `korrelierter_distraktor` sämtliche Alternativverweise durch `'f' * 32` ersetzen und `_faelle_bilden()` beziehungsweise A29 ausführen. Nachgemessen: keine Probleme, die Sitzung gilt weiterhin als messender NAK-190-Nachweis und A29 endet grün. Eine gewöhnliche Regression der Referenzbildung würde den geforderten Gegenbeweis daher nicht auslösen.