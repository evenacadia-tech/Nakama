VALIDIERUNG: D-1a PRÄZISIERT · D-1b BESTÄTIGT · D-1c BESTÄTIGT · D-1d BESTÄTIGT · D-1e BESTÄTIGT · D-1f/M-51 BESTÄTIGT · D-1f/M-47b BESTÄTIGT · D-2 PRÄZISIERT · D-3 BESTÄTIGT

# NAK-311 — Validierung der Matrixprüfung 1 (frischer lesender Validierer, 19.09.2026)

Gegenstand: die Behauptungen D-1a bis D-3 und die Faktenfragen L-1 bis L-4 und H-2 aus `docs/beweise/roh/NAK-311-matrixpruefung-1-urteil.md`, gemessen an `docs/beweise/NAK-311.md` und am Code; Auftrag `docs/beweise/roh/NAK-311-matrixpruefung-1-validierung-auftrag.txt`. Zeilenangaben `NAK-311.md:n` gelten am HEAD (Arbeitskopie CRLF, Index LF, gleiche Zeilen). Der Code unter `eq-copilot/` ist an `ac62c734` und am HEAD gleich, der Arbeitsbaum gleich HEAD (siehe „Selbst nachgemessen“). Nur lesend: kein Bau, kein Testlauf, kein Python, kein FL. Jede Aussage über Rot und Grün unten ist aus Quelle und Bauplantext hergeleitet, keine ist gemessen. Die Einordnung Defekt, Lücke oder Härtung trifft der Dirigent.

## D-1a — M-32 und M-34: `beendeAudiohistorie` in der Ruhe

**1. Zitat.**
- M-32 (`NAK-311.md:611`): Zustand „wie M-23 bis zur Änderung in der Ruhe (0 dB), dann `beendeAudiohistorie()`“; Zusage „**Ausgang bitgleich zum frischen Kern, Spitze höchstens 1 + 1e−6 — die Audiohistorie friert kein veraltetes Ziel ein**“; Frist „ein ruhender Pfad steht nach `beendeAudiohistorie` auf 1,0 (`eq-copilot/plugin/dsp/DspKern.cpp:182-185`)“; Test „B6 **NEU** Teilfall `…_nach_beende_audiohistorie`“; Rotbeweis „**heute rot** — Phase 16 gemessen: dieselbe Spitze 4,6832 mit `beendeAudiohistorie` im ausgeschalteten Zustand (Teil A §1.1 Station 6); Rotbeweis: für ruhende Pfade wieder `setzeSofort (r->ziel)` → rot“.
- M-34 (`:613`): Zustand „echter `SondeProcessor` (B7): Transaktionen eq an mit Output-Trim +24 dB, Audio, eq aus, Audio über das Ausblenden, Output-Trim 0 dB, `reset()`“; Ereignis „eq an, Audio“; Zusage „**Spitze am Ausgang höchstens 1 + 1e−6; der bestehende B7-Fall zu `reset()` bleibt grün; der Kommentar `eq-copilot/plugin/sonde/SondeProcessor.cpp:299-300` sagt, was geschieht**“; Test „B7 **NEU** `311/M-34 reset_in_der_ruhe_friert_kein_ziel_ein` in `eq-copilot/plugin/tests/TransactionTestMain.cpp`; …“; Rotbeweis „**heute rot** (Mechanik wie M-32 über den Prozessorweg, hergeleitet; …); Rotbeweis wie M-32“.
- Bezugszeilen: M-23 (`:602`), Zustand „eq an mit Output-Trim +24 dB, 1024 Samples eingeschwungen; eq aus, Ausblenden und 512 Samples Ruhe; in der Ruhe Output-Trim 0 dB publiziert (ENDE-Marke)“, Rotbeweis „nach dem Bau: die Ruhezuweisung am Übergangsende entfernt → rot“. M-33 (`:612`), Zustand „eq an mit +24 dB, eq aus, `beendeAudiohistorie()` 64 Samples nach Beginn des Ausblendens (der Übergang endet dort, der Pfad ruht)“, Rotbeweis „Ruhezuweisung nach dem Übergangsende in `beendeAudiohistorie` entfernt → rot“.
- Bauplan §7.3, W01 (`:984-989`): „Endet in `verarbeitePfad` ein Übergang in die Ruhe (`:816-824` mit `z.aktiv < 0`), setzt der Kern die fünf Pfadrampen auf die Ruhewerte 1,0 — dieselbe Zuweisung, die `PfadZustand::ruhe` (`:85-93`) in `bereiteVor` macht. `beendeAudiohistorie` (`:182-185`) setzt einen danach ruhenden Pfad ebenso auf 1,0 und einen engagierten wie heute auf sein Ziel.“

**2. Code** (alle Zeilenangaben des Prüfers stimmen).
- `DspKern.cpp:91` in `PfadZustand::ruhe` (`:85-93`): `rampen.setzeSofort (1.0);` mit `DspKern.h:315`: `void setzeSofort (double z) noexcept { aktuell = ziel = z; schritt = 0.0; rest = 0; }` — also aktuell = ziel = 1,0.
- `DspKern.cpp:818-824` (Übergangsende in `verarbeitePfad`): `if (z.uebergang != Uebergang::keiner && ! hoerHalt && z.rest <= 0) { if (z.quelle >= 0) baenke.meldeAusgedient (z.quelle); z.quelle = -1; z.uebergang = Uebergang::keiner; z.rest = 0; }` — hierher kommt die W01-Ruhezuweisung.
- `DspKern.cpp:174-180` (Übergangsende in `beendeAudiohistorie`) und `:184-185`: `for (auto* r : { &z.rampen.input, &z.rampen.output, &z.rampen.mix, &z.rampen.width, &z.rampen.autoGain }) r->setzeSofort (r->ziel);`
- Die Änderung in der Ruhe erreicht die Rampen nicht: `DspKern.cpp:379` `if (alt < 0) return;` (ENDE-Marke bei ruhendem Pfad); ein ruhender Pfad tickt nur (`:786`), `Rampe::tick` ändert bei `rest == 0` nichts (`DspKern.h:325-334`).
- Wiedereinschalten: `DspKern.cpp:418-424` `setzeZiel …`, darin `DspKern.h:319` `if (z == ziel) return;`.
- M-34: `SondeProcessor.cpp:306-308`: `const juce::ScopedLock callback (getCallbackLock()); const juce::ScopedLock l (zustandSchloss); dspKern->beendeAudiohistorie();`

**3. Herleitung.** Nach dem W01-Bau endet in M-32 das Ausblenden in `verarbeitePfad`, dort werden die fünf Rampen auf aktuell = ziel = 1,0 gesetzt. Die ENDE-Marke mit 0 dB kehrt an `:379` zurück. `beendeAudiohistorie` findet einen ruhenden Pfad mit Zielen 1,0; die Mutation „`setzeSofort (r->ziel)`“ setzt 1,0 auf 1,0. Beim Wiedereinschalten kehrt `setzeZiel (1.0)` an `DspKern.h:319` zurück, es läuft keine Rampe, der Crossfade aus Dry mischt zwei gleiche Signale → Spitze 1 → grün. M-34 läuft über `reset()` in denselben Zustand (Testname „…_in_der_ruhe_…“, „Mechanik wie M-32“: das Ausblenden ist vor `reset()` beendet) → grün. Umgekehrt bleibt M-32 auch unter der M-23-Mutation („Ruhezuweisung am Übergangsende entfernt“) grün, weil dann der ruhende Zweig von `beendeAudiohistorie` auf 1,0 setzt. Der Zustand ist nach dem Bau doppelt gesichert: keine Einzelmutation an einer der beiden Zeilen macht M-32 oder M-34 rot. Den ruhenden Zweig mit veraltetem Ziel erreicht nur der M-33-Aufbau (Übergangsende in `beendeAudiohistorie` selbst, `:174-180`). Am Basisstand fehlen beide Zuweisungen; `:184-185` friert 15,849 ein (so auch §1, `NAK-311.md:96-98`). Mit nur einer der beiden Reparaturen wäre M-32 grün — der Basisrot ist weder allein `beendeAudiohistorie` noch allein dem Ausblendende zuzuordnen.

**4. Gegenargument gesucht.** (a) Lesart „Audio über das Ausblenden“ = Audio endet im Ausblenden: dann wäre M-34 der M-33-Fall und die Mutation wirkte; dagegen stehen Testname („in_der_ruhe“) und „Mechanik wie M-32“. (b) Ein anderer Weg in einen ruhenden Pfad mit Rampen ungleich 1,0 nach dem Bau: `bereiteVor` (`:110`), Ende in `verarbeitePfad` (W01), Ende in `beendeAudiohistorie` (W01), `beendeHoerHalt` (W01), ENDE-Marke in der Ruhe (`:379`, ändert nichts) — keiner. (c) Kein Satz in §7, §9.1 oder §10.2 nennt eine andere Brechung; §7.3 (`:1010-1011`) verweist auf „die Mutationen aus §6.2“.

**5. Urteil: PRÄZISIERT.** Kern trägt: die genannte Mutation lässt M-32 und M-34 nach dem Bau grün, den ruhenden Zweig trägt allein M-33. Einzelheit anders: „Auch ihr Basislauf ist nur deshalb rot, weil die Ruhezuweisung am Ausblendende fehlt …, nicht wegen `beendeAudiohistorie`“ — beide fehlenden Zuweisungen sind notwendige Bedingungen des Basisrots. Schärfer als behauptet: nach dem Bau gibt es für M-32 keine wirksame Einzelmutation.

**6. Gebrochener Satz und kleinste Korrektur.**
- §6 (`:529-530`): „Ein Rotbeweis fällt an der Zeile, die die Zusage trägt, nie an einem Nebeneffekt.“ Etikett (`:512-514`): „**heute rot**: am Basis-SHA widerspricht das Verhalten der Zusage nachweislich; die Zeile nennt die Gegenprobe (Datei:Zeile oder Rohbeleg) und die Mutation, die nach dem Bau an der neuen Zusagezeile rot macht.“
- Vorschlag M-32: als eigene Rotbeweiszeile streichen, mit Verweis auf M-33 (ruhender Zweig von `beendeAudiohistorie`) und M-23 (Übergangsende). Soll der Auftragssatz „beendeAudiohistorie nach einer Änderung in der Ruhe friert kein veraltetes Ziel ein“ (`docs/beweise/roh/NAK-311-etappe-1-auftrag.txt:22`) wörtlich getragen sein, M-33 um „Output-Trim 0 dB als ENDE-Marke nach `beendeAudiohistorie`, dann eq an“ ergänzen; die ENDE-Marke ändert die Rampen nicht (`:379`), der Rotbeweis von M-33 bleibt wirksam.
- Vorschlag M-34: Aufbau auf das M-33-Muster: „Audio nur bis in das Ausblenden (weniger als `kFadeSamples` nach eq aus, etwa ein Block zu 64 Samples), Output-Trim 0 dB, `reset()`“, Testname entsprechend; Rotbeweis „wie M-33“. Rot, weil `beendeAudiohistorie` dann den Übergang selbst beendet (`:174-180`) und ohne die Ruhezuweisung der heutige Zweig `:184-185` die Ziele 15,849 einfriert; das Wiedereinschalten rampt von 15,849 (`DspKern.h:317-323`) und überschwingt wie M-23 → Spitze über 1 + 1e−6. Deterministisch über die feste Blockgröße des B7-Helfers `prozessor (rate, block)` (`TransactionTestMain.cpp:1713-1719`).

## D-1b — M-42: Regressionswache der bestehenden M-121-Fälle

**1. Zitat.** M-42 (`NAK-311.md:636`): Zustand „bestehende Unterfälle (a) Remove und (b) Neubelegung mit einem statischen Notch, jeweils mit Audio dazwischen“; Zusage „**Auslenkung 0,0 nach Remove und nach Neubelegung, wie heute**“; Test „B6 bestehend `remove_verwirft_die_alte_auslenkung`, `remove_mit_neubelegung_verwirft_die_alte_auslenkung` (`:4031-4058`)“; Rotbeweis „**Regressionswache**; einmal gebrochen: der geänderte Slot bekommt den Zustand der alten Bank („immer kopieren") → rot“. Bauplan §7.3 (`:1045-1050`): „Im Crossfade mit alter Bank (`alt >= 0`) übernimmt die neue Bank vor dem ersten Sample den `BandZustand` jedes Slots, der in beiden Programmen aktiv ist und dieselbe Kennung trägt, wenn die Pfadkennung gleich ist; … Alles andere bleibt kalt, wie `publiziereVorbau` die Bank vorbereitet hat (E-8 für geänderte Slots).“

**2. Code** (Angaben stimmen).
- Test `DspGoldenTestMain.cpp:4014-4015`: `belege (dyn, 2, Filtertyp::bell, 1000.0, 1.0, 0.0); machDynamisch (dyn, 2, -9.0, -50.0, 1.0, 0.0, 5.0);`, 100 Blöcke 0,9-Sinus. (a) `:4035-4038`: `auto entfernt = machSatz (true); belege (entfernt, 0, Filtertyp::bell, 500.0, 1.0, 3.0); k->uebernehmeZustand (entfernt); fahreStille (*k, kFadeSamples + 2048, 512);`, Prüfung `:4042` `std::abs (vorher[2]) > 1.0 && nachRemove[2] == 0.0`. (b) `:4049-4052`: `auto neu = machSatz (true); belege (neu, 2, Filtertyp::notch, 3000.0, 4.0, 0.0); k->uebernehmeZustand (neu); fahreStille (*k, kFadeSamples + 2048, 512);`, Prüfung `:4056` `nachher[2] == 0.0`.
- `DspKern.cpp:463` `if (! b.aktiv) return;`; `:695-700`: `for (int i = 0; i < kSlots; ++i) { const auto& bp = bank.programm.baender[(size_t) i]; if (! (bp.aktiv && bp.nutztSvf)) z.auslenkungen[(size_t) i].store (0.0, std::memory_order_relaxed); }`; ein statisches Band meldet danach `z.auslenkungDb` (`:618-624`).
- Kalter Vorgänger: `DspKern.cpp:266` `bank.zustaendeNullen();`, `DspBankPool.cpp:17` `auslenkungDb = 0.0;`. Die Bank für „entfernt“ ist frisch reserviert (`DspBankPool.cpp:109-121`), Slot 2 ist dort frei und wird nie gerechnet. `fahreStille` ruft kein `pflege()` (`DspGoldenTestMain.cpp:281-294`).

**3. Herleitung.** (a) Nach dem Bau ist Slot 2 im neuen Programm frei: `:695-700` schreibt 0,0, `verarbeiteBand` kehrt an `:463` zurück, ein kopierter Zustand in Slot 2 wird nie gelesen → grün unter „immer kopieren“. (b) Vorgänger von Slot 2 ist die „entfernt“-Bank mit genulltem, nie gerechnetem Slot 2; „immer kopieren“ kopiert `auslenkungDb` 0 → der Notch meldet 0,0 (`:699`, `:624`) → grün. Die Zeile hat keinen wirksamen Rotbeweis.

**4. Gegenargument gesucht.** Kopie aus der älteren dyn-Bank: die Übertragung liest nach §7.3 nur die alte Bank des Blockrands (`:1041-1042`), dyn ist dann ausgeblendet. Mutation „neue Bank nicht genullt“: „neu“ bekommt die dritte, nie benutzte Bank (dyn bleibt ohne `pflege()` `ausgedient`), also ebenfalls null. Kein Satz in §7.3, F-7 oder §10.2 nennt eine andere Brechung.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett (`:516-518`): „**Regressionswache**: heute grün und hält, was der Bau nicht ändern darf; sie wird einmal absichtlich gebrochen und ist kein Beleg“, dazu §6 (`:529-530`). Vorschlag (a): Mutation an der M-121-Zeile „Nullmeldung `DspKern.cpp:695-700` entfernt → (a) rot“. Rot, weil im Crossfade die ausblendende dyn-Bank je Block zuerst rechnet (`:752-761`) und `pz.auslenkungen[2]` mit ihrem Wert beschreibt (Release 5 ms: nach 512 Samples Stille liegt der Pegel noch weit über −50 dB, also −9 dB); die neue Bank schreibt Slot 2 nie, der Wert bleibt nach dem Fade stehen → `nachRemove[2] != 0`. Vorschlag (b): unter keiner Übertragungsmutation rot zu machen (kalter Vorgänger); den eigenen Rotbeweis für (b) streichen mit Verweis auf M-47 (b), wo der statische Nachfolger eines geänderten Slots unter „auch geänderte Slots übertragen“ rot wird (siehe D-1f/M-47b). Wer (b) behalten will, baut ihn als Teilfall ohne Zwischen-Remove (Notch direkt auf die dynamische Belegung, Audio dazwischen); dann meldet der Notch unter derselben Mutation die kopierte `auslenkungDb` (`:618-624`) → rot.

## D-1c — M-45: Regressionswache „heute schon innerhalb“

**1. Zitat.** M-45 (`NAK-311.md:639`): Zustand „wie M-43 mit Bell 1 kHz Q 2 +12 dB und 1-kHz-Sinus“; Ereignis „Slot 5 schaltet ein“; Zusage „**Maß höchstens 0,5 dB (heute −0,21 dB, Altbasis `docs/audits/2026-09-15-tiefenaudit/BEFUNDE.md:634`)**“; Test „B6 **NEU** Teilfall“; Rotbeweis „**Regressionswache** (grenzt die Regel auf tieffrequente, hochgütige Bänder ein); einmal gebrochen: der Übergang mit Übertragung ohne Crossfade (Rest 0) → Sprung auf die neue Bank → Maß über 0,5 dB → rot“. M-43 (`:637`), Zustand: „… Slot 5 belegt, `gain_db` 0, ausgeschaltet; Referenzkern mit identischer Vorgeschichte ohne Wechsel“; Ereignis „nur `v1.band.5.enabled` wird wahr“. Bauplan wie D-1b (`:1045-1048`).

**2. Code** (Angaben stimmen; die Methode heißt `BiquadZustand::tick`, nicht `Biquad::tick`).
- `DspFilter.h:118` `const double A = std::pow (10.0, gainDb / 40.0);` → 1,0 bei 0 dB; `:122` `const double a0 = 1.0 + alpha / A;`; `:125-129` `f.b0 = (1.0 + alpha * A) / a0;` (exakt 1), `f.b1 = (-2.0 * c) / a0;` gleich `f.a1 = (-2.0 * c) / a0;`, `f.b2 = (1.0 - alpha * A) / a0;` gleich `f.a2 = (1.0 - alpha / A) / a0;`. Vorgabetyp Bell (`eq-copilot/schemas/state/nakama-parameter-v2.json:146`).
- `DspFilter.h:92-94`: `const double y = f.b0 * x + z1; z1 = f.b1 * x - f.a1 * y + z2; z2 = f.b2 * x - f.a2 * y;` → bei Zustand 0: y = x, z1 = z2 = 0.
- Rest 0: `DspKern.cpp:744` `const bool crossfade = z.uebergang == Uebergang::crossfade && z.rest > 0;` → kein Quelllauf, `:808-809` Ausgang = aktive Bank, `:818-824` sofortiges Übergangsende.

**3. Herleitung.** Nach dem Bau trägt die neue Bank Slot 0 mit übertragenem Zustand und unveränderten Koeffizienten, Slot 5 als Bell 0 dB mit Zustand 0 = Einheit. Unter „Rest 0“ rechnet ab dem Umschaltsample nur die neue Bank; sie liefert, was die alte geliefert hätte, und der Referenzkern dasselbe → Maß ≈ 0 dB → grün. Beide Wächter halten M-45 je allein: nur Crossfade = heute −0,21 dB, nur Übertragung ≈ 0 dB.

**4. Gegenargument gesucht.** Rundung (etwa eine FMA-Zusammenziehung in `:93`) änderte das Ergebnis um Größenordnung 1e−16 relativ, das Maß bliebe ≪ 0,5 dB. Kanalfrage: der Bell ist `stereo`, beide Komponenten gleich. Kein Satz in §7.3, F-9 oder §10.2 fängt es auf.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett Regressionswache (`:516-518`), §6 (`:529-530`). Vorschlag: Rotbeweis „Übergang ohne Übertragung und ohne Crossfade (Rest 0, Slot 0 kalt) → rot“ (Vorschlag des Prüfers); beide Eingriffe liegen in derselben Crossfade-Verzweigung von `blockrand`, die das Maß trägt. Rot, weil Slot 0 dann ohne Blende kalt einsetzt: für Bell 1 kHz Q 2 +12 dB ist α = sin(2π·1000/48000)/4 ≈ 0,0326, A ≈ 1,995, a2 = (1 − α/A)/(1 + α/A) ≈ 0,968, Polradius ≈ 0,984, Zeitkonstante ≈ 61 Samples; der Ausgang wächst von b0·x ≈ 1,05·x auf 3,98·x, der Perioden-RMS der ersten Periode liegt abgeschätzt rund 6 dB unter dem Referenzkern → Maß über 0,5 dB. Jede der beiden Einzelmutationen bliebe grün.

## D-1d — M-52: deterministische Hälfte

**1. Zitat.** M-52 (`NAK-311.md:646`): Zustand „Worker und Audiothread echt parallel (M-122-Lauf, …); dazu deterministisch: eine Publikation zwischen `publikation` und `uebernehme` verdrängt (E-24)“; Zusage „**Die Kennungen stehen in der Bank, bevor sie publiziert wird (Release), und werden am Blockrand nach dem Acquire gelesen; der Stresslauf hält seine Invarianten; eine zwischen Lesen und Übernahme verdrängte Belegung bricht die Übertragung**“; Test „B6 bestehend `bankpool_parallel_ohne_rennen (M-122)` (`:4170`) plus **NEU** Teilfall von `311/M-41` mit getrennten Zugängen“; Rotbeweis „… deterministischer Teil heute rot (Rotbeweis: Kennung erst nach `publiziere` geschrieben → der Teilfall liest die alte Kennung → rot)“. Bauplan §7.3 Vergabe (`:1030-1031`): „`publiziereVorbau` (`DspKern.cpp:237-276`) vergibt nach `bank.programm = prog`: …“; (`:1036-1040`): „Weil der Worker gegen die zuletzt *publizierte* und nicht gegen die vom Audiothread *genommene* Belegung vergleicht, tragen die Kennungen jeden Wechsel einer verdrängten Zwischenpublikation (M-41) …“; (`:1061-1062`): „Die Kennungen erreichen ihn über dieselbe Release-/Acquire-Übergabe wie das übrige Programm (M-52).“

**2. Code** (Angaben stimmen).
- Worker, ein Aufruf: `DspKern.cpp:259-260` `bank.programm = prog; bank.programm.generation = baenke.naechsteGeneration();`, `:273` `baenke.publiziere (p, slot);`; Release in `DspBankPool.cpp:148-149` `veroeffentlicht[(size_t) p].exchange (publikationsWort (g, slot), std::memory_order_acq_rel);`.
- Audiothread: `DspKern.cpp:368` `const int neu = baenke.uebernehmeBereiten (p);` = `uebernehme (p, publikation (p))` (`DspBankPool.h:209`). Getrennte Zugänge `DspBankPool.h:196-206`; `DspBankPool.cpp:230-231` `if (! wechsle (slot, zustandsWort (g, BankZustand::bereit), zustandsWort (g, BankZustand::audioAktiv))) return -1;` — bei verdrängter Beobachtung −1, die Bank wird nicht gelesen.

**3. Herleitung.** Einfädig kehrt `publiziereVorbau` vollständig zurück, bevor der Test `verarbeite` ruft; die Kennung steht in der Bank, gleich wo in der Funktion sie geschrieben wird. `blockrand` liest sie erst nach `uebernehmeBereiten`. Ein getrenntes `uebernehme` mit alter Beobachtung liefert −1 ohne Banklesezugriff; der nächste Blockrand nimmt die neueste Publikation mit fertiger Kennung. Die Mutation wird nur als Speicherordnungsfehler unter echter Nebenläufigkeit sichtbar, also allenfalls im Stresslauf.

**4. Gegenargument gesucht.** Ein Haken zwischen Worker-Schritten existiert nicht; der einzige Haken liegt zwischen Teilstücken (`DspKern.h:174-182`), und übernommen wird einmal je äußerem Aufruf (`DspKern.cpp:834-838`). Kein Satz fängt es auf.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** §6 (`:529-534`): „Ein Rotbeweis fällt an der Zeile, die die Zusage trägt, nie an einem Nebeneffekt. Abläufe mit Publikation und Blockrand sind deterministisch: … verdrängte Publikationen entstehen durch zwei Publikationen ohne Aufruf dazwischen oder durch die getrennten Zugänge `publikation`/`uebernehme` (E-24); keine Zeile hängt an Wandzeit oder Zufall.“ Dazu das Etikett heute rot (`:512-514`). Vorschlag: im deterministischen Teil die Mutation von M-41 einsetzen: „der Worker vergleicht mit der vom Audiothread genommenen statt mit der zuletzt publizierten Belegung“. Rot, deterministisch: A genommen, „leer“ publiziert, Test liest `publikation`, B publiziert — unter der Mutation vergleicht der Worker B mit dem genommenen A, gleiche Topologie, B behält die Kennung von A; `uebernehme` mit alter Beobachtung liefert −1, der nächste Blockrand nimmt B mit gleicher Kennung, überträgt, B erbt die Auslenkung von A → rot. Den Release-Satz der Zusage kennzeichnen als „durch Konstruktion (Vergabe vor `publiziere`, `acq_rel`-Tausch `DspBankPool.cpp:148-149`) und Stresslauf M-122, ohne deterministischen Rotbeweis“. Gleichwertig kleiner: die deterministische Hälfte streichen mit Verweis auf M-41.

## D-1e — M-80: Hörmatrix im Hash

**1. Zitat.** M-80 (`NAK-311.md:685`): Zustand „gleicher kalter Beginn beider Instanzen, statische Bänder (bestehender Fall)“; Ereignis „Reload“; Zusage „**Hash gleich, Ausgang bitgleich (strenger als M-84)**“; Test „B7 bestehend `reload_rekonstruiert_denselben_audioausgang (M-84)`“; Rotbeweis „**Regressionswache**; einmal gebrochen: die Hörmatrix-Auswahl in den Hash genommen → Hash weicht ab → rot“. Bauplan §7.4 (`:1212-1213`): „M-80 bleibt bestehend.“ Herkunft: SONDE-015 M-84, Rotbeweisspalte (`docs/beweise/SONDE-015.md:1162`): „Der Hash nach dem Reload unterscheidet sich, weil ein transienter Wert mitgehasht wurde“.

**2. Code** (Angaben stimmen).
- `TransactionTestMain.cpp:1713-1719` `prozessor`: `setRateAndBufferSizeDetails`, `prepareToPlay` — kein Hörmatrix-Aufruf. Fall `:2171-2191`: `auto a = prozessor (48000.0, 256); … setze (*a, z); … a->getStateInformation (bytes); const auto ya = fahreAudio (*a, 300, 256, 42); auto b = std::make_unique<Prozessor>(); b->setStateInformation (bytes.getData(), (int) bytes.getSize()); … b->prepareToPlay (48000.0, 256); const auto yb = fahreAudio (*b, 300, 256, 42); pruefe (b->stateHashText() == a->stateHashText() && bitgleich (ya, yb), …` — kein `setzeHoermatrix`.
- `DspKern.h:424` `std::atomic<Hoermatrix> hoerwunsch  { Hoermatrix::processed };`, `DspKern.cpp:112` in `bereiteVor` `hoerwunsch .store (Hoermatrix::processed, …);`, `SondeProcessor.cpp:1057-1058` `// R10: nach dem Laden steht die Hoermatrix IMMER auf Processed (M-52).` `dspKern->setzeHoermatrix (nakama::dsp::Hoermatrix::processed);`. Hash: `SondeProcessor.cpp:1263-1267` `return nakama::transaktion::alsText (transaktion->hash());`.

**3. Herleitung.** Beide Instanzen stehen beim Hashvergleich auf Processed. Ein Hash, der die Auswahl einschlösse, trüge in A und B denselben Wert → grün.

**4. Gegenargument gesucht.** Der Fall setzt die Hörmatrix weder direkt noch über eine Pipe; kein Satz fängt es auf.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett Regressionswache (`:516-518`). Vorschlag: Mutation am Ladeweg, der die Zusage trägt: „`setStateInformation` übernimmt den geladenen Satz nicht (`SondeProcessor.cpp:1047-1049` stets falsch, also neutraler Ladestart `:1050-1055`)“. Rot, deterministisch: B trägt den Hash des neutralen Zustands (A den von `z` mit zwei Bändern, Width 1,3, Auto-Gain, Zone) und gibt als Passthrough den Eingang aus, A den bearbeiteten → beide Teilprüfungen fallen.

## D-1f/M-51 — neuer Fall ohne Mutation

**1. Zitat.** M-51 (`NAK-311.md:645`): Zustand „bestehender B-5-Fall (`:1038-1087`) und ein Crossfade mit Übertragung: Slot 0 eingeschwungen, Slot 1 wechselt den Typ“; Zusage „**Sprung am Umschaltsample höchstens Fadeschrittweite + 2^−23; Wache über den Lauf höchstens `kaltSchranke` + 2^−23; die bestehenden Fälle bleiben grün**“; Test „B6 bestehend `zweiter_wechsel_im_laufenden_fade_springt_nicht`, `wache_der_kalt_startenden_bank_ueber_den_ganzen_lauf` plus **NEU** `311/M-51 crossfade_mit_uebertragung_haelt_e31`“; Rotbeweis „**zwei Hälften:** bestehende Fälle Regressionswache (einmal gebrochen: ein Typwechsel läuft als Rampe → Sprung → rot); neuer Fall heute nicht messbar“. Etikett (`:514-516`): „**heute nicht messbar**: der Mechanismus oder das Urteil entsteht erst im Bau; die Zeile nennt die Mutation nach dem Bau.“ §7.1 (`:826-828`): „Für „heute nicht messbar", „Regressionswache", „erlaubtes Verhalten gemessen" und „Regel gemessen" ist die Mutation nach dem Bau der Beleg“. §7.3 Beine (`:1088-1091`) führt M-51 nur mit der Wachhälfte: „… die Wachhälften aus M-46, M-47, M-49, M-51, M-52 je einmal gebrochen.“

**2. Code** (für die Korrektur). Im Crossfade rechnet die Quellbank aus ihrem eigenen Zustand (`DspKern.cpp:754-760`: `const Rampen gesichert = z.rampen; … verarbeiteBank (z, z.quelle, -1, 0, vonL, vonR, numSamples); z.rampen = gesichert;`); am ersten Fadesample ist t = 0 (`:800-801`: `const double t = 1.0 - (double) rest / (double) kFadeSamples; ausL[i] = vonL[i] * (1.0 - t) + nachL[i] * t;`). E-31-Prüfung am Umschaltsample: `DspGoldenTestMain.cpp:1068-1071`.

**3. Herleitung.** Die neue Hälfte trägt das Etikett „heute nicht messbar“, nennt aber keine Mutation; §7.3 listet sie weder bei den Rotbeweisen noch bei „je einmal gebrochen“. Sie hat weder Basisrot noch Nachbau-Mutation.

**4. Gegenargument gesucht.** Die Mutation der Wachhälfte („ein Typwechsel läuft als Rampe“) ist auf die bestehenden Fälle bezogen; ob sie den neuen Fall rot machte, sagt die Zeile nicht (H-3 bezweifelt sie schon für B-5; nicht mein Gegenstand). Kein Satz fängt die Lücke auf.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett „heute nicht messbar“ (`:514-516`) und §7.1 (`:826-828`). Vorschlag: für den neuen Fall die Mutation „Übertragung mit vertauschter Richtung (die ausblendende Quellbank bekommt den kalten Zustand der neuen Bank)“ nennen, Aufbau mit DC-Eingang und einem Slot 0, dessen DC-Verstärkung ungleich 1 ist (etwa Low-Shelf 8 kHz Q 0,707 +9 dB wie `DspGoldenTestMain.cpp:1047`). Rot, deterministisch: am Umschaltsample ist der Ausgang die Quellbank allein; mit genulltem Slot 0 liefert sie b0·x statt des eingeschwungenen Werts (nachgerechnet b0 ≈ 1,431, also 0,43 statt 0,3·10^(9/20) ≈ 0,85) → Sprung von rund 0,4 (Slot 1 mit DC-Verstärkung nahe 1, etwa ein Bell) gegen eine Fadeschrittweite der Größenordnung 1e−3 (im B-5-Aufbau 2,1e−3) → E-31-Prüfung rot. Der DC-Eingang macht zugleich `kaltSchranke` anwendbar (siehe L-3, Beifang).

## D-1f/M-47b — `dynamic_enabled` aus ohne Mutation

**1. Zitat.** M-47 (`NAK-311.md:641`): Ereignis „(a) Remove an Slot 0; (b) `dynamic_enabled` aus an Slot 0“; Zusage „**Slot 0 meldet ab dem ersten Block 0,0 (M-121); Slot 1 behält seine Historie (Maß M-43 höchstens 0,5 dB)**“; Test „B6 **NEU** Teilfall“; Rotbeweis „**zwei Hälften:** Meldung 0 Regressionswache (für Remove bestehend, `:4042`); Slot 1 heute rot“. Vergleich M-46 (`:640`): „Slot 0 kalt Regressionswache (einmal gebrochen: auch geänderte Slots übertragen → Slot 0 meldet den alten Wert → rot)“.

**2. Code.** `DspGoldenTestMain.cpp:4042` ist die Prüfzeile des bestehenden Remove-Falls, keine Mutation. Für (b): `DspProgramm.cpp:217-218` `b.statisch = entwurf (…); b.statischIstEinheit = b.statisch.istEinheit();`, `:221-222` `b.dynamisch = dynAn; b.nutztSvf = dynAn && typHatGain (b.typ);` → statisch. `DspFilter.h:50-53` verlangt für `istEinheit` `b1 == 0.0 … && a1 == 0.0 …`; ein Bell hat b1 = a1 = −2·cos(w0)/a0 ≠ 0, ist also nie Einheit → kein Rücksprung an `DspKern.cpp:483`; `:699` schreibt 0,0, danach `:618-624` `double gemeldet = z.auslenkungDb; … pz.auslenkungen[(size_t) slotIndex].store (gemeldet, …);`.

**3. Herleitung.** Die Wachhälfte „Meldung 0“ nennt weder für (a) noch für (b) eine Brechung; der Verweis `:4042` zeigt auf eine bestehende Prüfung. (b) hat damit weder Basisrot (Regressionswache) noch Rotbeweis nach dem Bau. Den Vorschlag des Prüfers nachgeprüft: unter „auch geänderte Slots übertragen“ bekommt der statische Slot 0 den kopierten `BandZustand` samt `auslenkungDb` ≈ −12 dB, erreicht `:618-624` und meldet −12 dB nach der Nullung an `:699` → „ab dem ersten Block 0,0“ fällt → wirksam.

**4. Gegenargument gesucht.** §7.3 (`:1090-1091`) verspricht das Brechen der Wachhälfte, nennt aber keine Mutation; die Mutation steht in M-46, nicht in M-47. Nichts fängt es auf.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett Regressionswache (`:516-518`), §7.1 (`:826-828`). Vorschlag: Wachhälfte von M-47 ergänzen: „(b) einmal gebrochen: auch geänderte Slots übertragen → der statische Slot 0 meldet die kopierte Auslenkung → rot; (a) einmal gebrochen: Nullmeldung `DspKern.cpp:695-700` entfernt → Slot 0 meldet den Wert der ausblendenden Quellbank → rot“. (b) rot wie oben; (a) rot, weil der freie Slot 0 an `:463` zurückkehrt und ohne `:695-700` der Wert stehen bleibt, den die Quellbank im Crossfade zuerst schreibt (`:754-760`, `:618-624`; Hold 500 ms hält ihren Pegel).

## D-2 — F-12, M-77, M-78: Toleranz ohne den Einblend-Crossfade aus Dry

**1. Zitat.**
- M-77 (`NAK-311.md:682`): Zustand „wie M-76; Instanz B neu: `setStateInformation` mit den Bytes von A und `prepareToPlay`. Referenzbeginn t = 0 ist das erste Sample des ersten Blocks von B; A läuft ununterbrochen weiter und bekommt ab t = 0 denselben Eingang“; Zusage „**Die Abweichung des Quadraturbetrags je Sample, 20·log10(B/A), ist ab t_E = 5·τ_a + 10·Q/(π·f0) + 16 Samples höchstens 0,1 dB, davor höchstens Betrag(Range) + 0,1 dB; bei Attack 0,1, 10 und 500 ms (t_E rund 7,2 ms, 56,7 ms und 2,507 s bei 48 kHz), im Knie (Threshold −15 dB bei Detektorleistung −9,03 dB, Auslenkung −5,97 dB) und im Plateau (Threshold −40 dB)**“; Frist „Hüllkurve startet kalt (`bereiteVor`) und steigt bei konstanter Detektorleistung im Attackzweig (`eq-copilot/plugin/dsp/DspFilter.h:312-318`)“; Rotbeweis „**Regel gemessen** — … Rotbeweis: Attackpol in `huellkurveEntwurf` (`eq-copilot/plugin/dsp/DspFilter.h:284`) mit fünffachem τ → nach t_E über 0,1 dB → rot“. Prüfling M-76 (`:681`): „dynamisches Bell 1 kHz Q 2, Grundgain 0 dB, `stereo`, Range −12 dB, …“.
- M-78 (`:683`): Zustand „Instanz A selbst: `prepareToPlay` bei laufendem Zustand; Vergleich gegen eine ununterbrochen laufende Instanz C mit identischer Vorgeschichte“; Zusage „**wie M-77**“; Rotbeweis „**Regel gemessen**; Rotbeweis wie M-77“.
- F-12 (`:1414-1433`), tragende Sätze: „Referenzbeginn t = 0 ist das erste Sample des ersten Blocks der geladenen oder neu vorbereiteten Instanz; … Toleranz: ab t_E = 5·τ_a + 10·Q/(π·f0) + 16 Samples weicht der Quadraturbetrag je Sample höchstens 0,1 dB ab, davor höchstens Betrag(Range) + 0,1 dB. Herleitung aus der Hüllkurvenmechanik: …“ und „Detektorbandpass und Bandfilter starten ebenfalls kalt; … Die Steuerrate fügt höchstens 16 Samples hinzu … Vor t_E kann der kalte Start höchstens die ganze Range auslassen, weil der Grundgain des Prüflings 0 dB ist und ein kalter Detektor nur weniger absenkt.“ §7.4 (`:1215-1216`): „Der Nachtrag in SONDE-015 nennt bei M-84 die Zahl und verweist auf NAK-311 M-77.“

**2. Code** (alle Angaben des Prüfers stimmen).
- `DspKern.cpp:109-110` in `bereiteVor`: `baenke.zuruecksetzen(); for (auto& z : pfade) z.ruhe();` → `aktiv = -1` (`:87`). `SondeProcessor.cpp:256` `dspKern->bereiteVor (sichereRate, maxBlock);`, `:261` `publikationOffen = ! dspAusfuehrung->publiziereWirksam (transaktion->wirksam(), true);`; beim Laden wird nur mit Samplerate publiziert (`:1062-1063`).
- `DspKern.cpp:372` `const int alt = z.aktiv;` (−1), `:398` `const bool nurRampen = alt >= 0 && …` (falsch), `:413-414` `z.uebergang = nurRampen ? Uebergang::rampe : Uebergang::crossfade; z.rest = nurRampen ? kRampeSamples : kFadeSamples;` mit `kFadeSamples = 256` (`DspProgramm.h:53`).
- `DspKern.cpp:762-768`: „Aus der Ruhe oder aus einem Passthrough: vom unveraenderten, NICHT verriegelten Eingang“ → `std::memcpy (vonL, dryL.data(), bytes);`. `:800-801` `t = 1.0 - (double) rest / (double) kFadeSamples; ausL[i] = vonL[i] * (1.0 - t) + nachL[i] * t;` → t = n/256 für n = 0 bis 255, ab n = 256 `ausL[i] = nachL[i];` (`:808`).
- Der SVF-Bell ist an f0 reell: `DspSvf.h:108-110` `c.m0 = 1.0; c.m1 = k * (A * A - 1.0); c.m2 = 0.0;` mit Bandpass 1/k bei Ω = 1 (`DspProgramm.cpp:46-53`) → H(f0) = A² = G; Dry- und Nassanteil addieren sich im Quadraturbetrag phasengleich.

**3. Herleitung, selbst gerechnet.**
- Band 1 kHz, Q 0,707, Attack 0,1 ms, 48 kHz: 5·τ_a = 0,5 ms = 24 Samples; 10·Q/(π·f0) = 2,2505 ms = 108,0 Samples; + 16 → t_E = 148,0 Samples = 3,08 ms < 256. Der Nassanteil von B ist nach F-12s eigener Herleitung bis t_E auf 0,03 dB eingeschwungen; übrig bleibt die Dry-Beimischung: B/A = ((1 − n/256) + (n/256)·G)/G mit G = 10^(−12/20) = 0,251189. Bei n = 148: (0,421875 + 0,578125·0,251189)/0,251189 = 2,2576 → +7,07 dB; beim ersten Sample ≥ t_E (n = 149): +7,03 dB; bei n = 255: +0,10 dB (1,01164); ab n = 256: 0. Die Zusage „ab t_E höchstens 0,1 dB“ reißt für n = 149 bis 255. Im Knie (G = 10^(−5,97/20) ≈ 0,503) bei n = 149 rund +3,0 dB.
- Prüfling der Phase 16 (Q 2): 10·Q/(π·f0) = 6,366 ms = 305,6 Samples, t_E = 7,2 ms = 345,6 Samples > 256 → die Beimischung endet vor t_E, M-77 kann es nicht zeigen. Allgemein reißt es bei 48 kHz und Attack 0,1 ms für Q < 1,414·f0/1 kHz, etwa schon beim Vorgabe-Q 1,0 bei 1 kHz (t_E = 192,8 Samples); Q-Bereich 0,15 bis 24, Attack ab 0,1 ms (`nakama-parameter-v2.json:170-171`, `:249`).
- Recall-Aufbau: M-77 (neue Instanz, `setStateInformation` vor `prepareToPlay`) und M-78 (`prepareToPlay` bei laufender Instanz) laufen beide über `bereiteVor` → Ruhe → Übernahme mit `alt < 0` → Einblend-Crossfade aus Dry über 256 Samples; der Aufbau umgeht ihn nicht. Nur M-76 (Ladestart ohne `prepareToPlay`) läuft als Rampenübergang mit Zustandsübernahme (`DspKern.cpp:398-408`).
- Abgleich Phase 16: gemessen wird `db(rms(cc.l[0..480))/rms(wc.l[0..480)))` des linken Kanals (`docs/audits/2026-09-15-tiefenaudit/roh/phase-16-nachweise/d/dsp_probe.cpp:95`, `cold=core()` = frisches `bereiteVor`, Bell 1 kHz Q 2, Quadraturton 0,5, Phase 0 am Messbeginn). Modell „nur Crossfade“ (Nassanteil von B ab Sample 0 gleich G·x), diskret über 480 Samples: Σ_{n<256}(1 − (1 − G)·n/256)²·sin²(2πn/48) = 56,108, G²·Σ_{256..479} sin² = 6,987, G²·Σ_{0..479} sin² = 15,143 → Verhältnis 4,1666 → +6,198 dB. Gemessen +6,240277 dB; Rest +0,042 dB aus dem Einschwingen von Detektor und Band (bei Attack 10 ms gemessen +6,247436 dB, Rest +0,049 dB, gleiche Richtung). Die Zahl des Prüfers (0,2625 gegen 0,0625 → +6,23 dB) rechnet mit G = 0,25 und dem kontinuierlichen Integral.

**4. Gegenargument gesucht.** (a) F-12 legt den Referenzbeginn auf das erste Sample der neu vorbereiteten Instanz, schließt die Beimischung also ein. (b) Der Test mit dem Q-2-Prüfling bliebe grün, die Zahl aber gilt nach §7.4 als M-84-Nachtrag allgemein. (c) Kein Satz in F-12, §7.4 oder §10.2 nennt `kFadeSamples`. Nichts fängt es auf.

**5. Urteil: PRÄZISIERT.** Kern trägt: F-12 enthält die Dry-Beimischung nicht; für ein vertragsgültiges Band reißt die Toleranz um rund +7 dB; M-77 und M-78 laufen durch den Crossfade; der Q-2-Prüfling verdeckt es. Einzelheit anders: „quantitativ genau dieser Crossfade … +6,23 dB“ — mit G = 0,251189 erklärt der Crossfade allein +6,20 dB, die restlichen +0,04 dB stammen aus dem kalten Einschwingen; die +6,23 dB entstehen aus G ≈ 0,25. Ebenso nennt der Prüfer n = 148, das erste Sample ab t_E ist n = 149 (+7,03 statt +7,07 dB).

**6. Gebrochener Satz und kleinste Korrektur.** R-311-4 (`:129-132`): „Die numerische Toleranz von M-84 für den Recall eingeschwungener dynamischer Bänder bekommt eine Zahl. Der Bauer der Etappe 1 schlägt sie aus der Hüllkurvenmechanik vor und belegt sie in Etappe 4 mit einer Messung“; Etikett (`:521-523`): „**Regel gemessen** (nur R-311-4): die Zahl einer Regel für Verhalten, das es heute schon gibt; der Test ist ihr Beleg, läuft am Basis-SHA und nach dem Bau und wird einmal gebrochen.“ Die Zahl widerspricht dem Code für einen erreichbaren Teil des Parameterraums, der Beleg deckt ihn nicht. Vorschlag: in F-12 und M-77 t_E = max(`kFadeSamples`, 5·τ_a·fs + 10·Q·fs/(π·f0) + 16) Samples (die Beimischung endet exakt mit Sample 256; die Fassung des Prüfers max(256, …) + 16 genügt ebenfalls) und ein zweiter Prüfling, bei dem der Crossfade überwiegt (Bell 1 kHz Q 0,707, Attack 0,1 ms, Plateau; alte t_E 148 Samples). Rotbeweis für den neuen Term: „Einblenden aus der Ruhe mit doppelter Länge (`DspKern.cpp:414`, Rest 2·`kFadeSamples` bei `alt < 0`)“ → bei n = 256 noch 50 % Dry, B/A = (0,5 + 0,5·0,2512)/0,2512 = +7,9 dB → rot, deterministisch.

## D-3 — M-17: „die Wachmarke kommt als 0,0 heraus“

**1. Zitat.** M-17 (`NAK-311.md:584`): Zustand „eq an, +12-dB-Bell bei 1 kHz auf Slot 0, sonst Default“; Ereignis „`processBlock`“; Zusage „**Die Neutralprüfung greift nicht: der Kern schreibt (das Band ist hörbar), die Wachmarke kommt als 0,0 heraus und wird gezählt (M-49)**“; Test „A16 **NEU** Teilfall“; Rotbeweis „**Regressionswache**; einmal gebrochen: die Bedingung „kein aktives Band" aus der Neutralprüfung genommen → das Band wirkt nicht → rot“. §7.2 (`:943-944`): „A16: `311/M-01`, `311/M-11` mit den Teilfällen M-15, M-16, M-17, M-19 und M-21“. Material: M-11 (`:578`) „wie M-10; im ersten Block nach Fade und Rampen liegen an drei Stellen die Wachmarke, dazu ein ruhiger NaN, +Inf und −Inf“; M-10 (`:577`) „… `fahreNull` mit Rauschen wie heute, dazu in jedem Block ab dem Ende von Engagier-Fade und Rampen das Bitmuster“.

**2. Code** (Angaben stimmen). Riegel `DspKern.cpp:921-924`: `if (! std::isfinite (l)) { l = 0.0; zaehlerEingaenge.fetch_add (1, std::memory_order_relaxed); } … eingL[i] = l;`. `DspFilter.h:92` `const double y = f.b0 * x + z1;` → bei x = 0: y = z1. Rauschen `SondeNullTestMain.cpp:82` `wuerfel.nextFloat() * 1.8f - 0.9f`, `fahreNull` `:153-177`; hörbares Band `setzeHoerbaresBand` `:127-134` (+12 dB, 1 kHz, Q-Vorgabe 1,0). Schreiben `DspKern.cpp:1077-1079` `float fl = (float) l; … kanaele[0][i] = fl;`.

**3. Herleitung am vorgeschriebenen Material.** Vor der Wachmarke liegt Signal (Rauschen durch den Engagier-Fade und im ersten Block nach Fade und Rampen, dazu das Bitmuster). Ein Band ist aktiv, die Neutralprüfung greift nicht, der Kern schreibt. An der Wachmarke gibt der Riegel 0,0 in die Kette; der statische +12-dB-Bell liefert y = z1, z1 trägt den Verlauf des Rauschens (Polzeitkonstante bei Q 1 rund 30 Samples; auch acht Bitmustersamples davor lassen z1 weit von 0). Heraus kommt (float) z1, endlich und nicht 0,0; gezählt wird richtig. Am Basisstand gilt dasselbe → die als Regressionswache etikettierte Zeile wäre heute rot.

**4. Gegenargument gesucht.** „kommt als 0,0 heraus“ als Aussage über den Riegelausgang (SONDE-015 M-49: „vor jedem Filterzustand durch 0,0 ersetzt“): kein Beobachtungspunkt zeigt dort 0,0 — `pre_nakama` trägt den Rohwert (`DspKern.cpp:953`), `post_committed` z1 (`:954`), der Ausgang (float) z1. Ein stiller Vorlauf steht weder in M-17 noch in M-11 oder M-10.

**5. Urteil: BESTÄTIGT.**

**6. Gebrochener Satz und kleinste Korrektur.** Etikett (`:516`): „**Regressionswache**: heute grün und hält, was der Bau nicht ändern darf“. Vorschlag: Zusage „… die Wachmarke ist überschrieben (Ausgang dort nicht bytegleich `0x7F800001`, endlich) und gezählt (M-49)“. Heute grün (der Kern schreibt (float) z1, der Riegel zählt). Unter der genannten Mutation gilt das Programm als neutral, der Kern schreibt nicht, die Wachmarke kommt bytegleich heraus → rot, deterministisch. Einen stillen Vorlauf empfehle ich nicht: z1 wird nach Rauschen erst über den Denormal-Riegel (`DspFilter.h:64`) oder FTZ exakt 0.

## L-1 — Neutralprüfung und Ausgangsvergleiche bestehender Fälle (Fakten)

Bauplan §7.2 Punkt 3 (`NAK-311.md:924-932`): „`verarbeiteStueck` rechnet wie heute weiter (Riegel, beide Pfade, Taps, Zähler) und bestimmt das Schreibende neu: der Puffer bleibt ab dem ersten Sample unberührt, ab dem der Ausgang bauartbedingt der Eingang ist — … (M-02-Zustand) Programm neutral mit allen Rampen auf 1,0 oder (M-33-Zustand) Mix-Rampe ruht auf 0,0 und Output-Rampe auf 1,0; dazu Hörmatrix Processed oder Dry ohne laufenden Hörmatrix-Fade. Die Grenze liegt wie bei E-32 auf dem Sample …“. Der Tap `post_committed` bleibt gefüllt (`DspKern.cpp:954` kopiert `cAusL/R`), solange der Block `maxBlock` nicht übersteigt (`:889`); das gilt in allen Fällen unten.

- **B6 `ms_stufe_ist_bei_width_eins_bitgenau_reversibel (M-30, B-19)`**, `DspGoldenTestMain.cpp:2180-2215` (vom Prüfer genannt, Zeilen stimmen). Width 1,5 eingeschwungen, dann `machSatz (true)` → Rampenübergang 256 Samples, ein Block zu 512. Vergleichsfenster `:2207-2210` Samples 255 bis 511; Vorbedingungen `:2198-2203` `floatWaereAnders` über 255 bis 511 und `:2211` `msStufenLaeufe() > laeufeVorher`. Heute: die M/S-Stufe rechnet den ganzen Block mit w = 1,0 ab Sample 255, geschrieben und bitgleich — belegt die Reversibilität in `double`. Nach dem Bau: das Übergangsende liegt nach Sample 255, 256 bis 511 werden nicht geschrieben; verglichen wird effektiv nur Sample 255, und ob gerade dieses zu den `floatWaereAnders`-Samples gehört, prüft der Fall nicht. Der Tap hielte die Trennschärfe: `post_committed` trägt die M/S-Rechnung für alle 512 Samples in `double`, ein Vergleich mit `(double) aK[i]` über 255 bis 511 wiese Float-Rechnung an den empfindlichen Samples aus.
- **B6 `mix_null_ist_bitidentisch (M-33)`**, `:2298-2327` (vom Prüfer genannt, stimmt). Mix 0, Input-Trim +9 dB, Bell +12 dB, `kRampeSamples + 4096` Stille, acht Blöcke verglichen, keine Vorbedingung, keine Gegenprobe. Heute: der Ausgang ist `dL[i]` (`DspKern.cpp:726`), belegt „Dry = `pre_nakama`“. Nach dem Bau: M-33-Zustand, kein Sample geschrieben; die SONDE-015-Rotbeweise („Bei Mix 0 bleibt ein Rest des Wet-Zweigs hörbar; oder der Dry-Zweig wird hinter dem Input-Trim abgegriffen …“, `docs/beweise/SONDE-015.md:1081`) würden hier nicht mehr rot. Der Tap hielte sie: `post_committed` = `dL` im M-33-Zustand.
- **A16 `mix_null_ist_bitidentisch`**, `SondeNullTestMain.cpp:420-445` (vom Prüfer genannt, stimmt). Zählt ab `einschwingen = kFadeSamples + kRampeSamples` = 512 (`:432`, `:436`); Vorbedingung Gegenprobe Mix 1 mit Abweichungen (`:437-441`). Heute wie B6. Nach dem Bau: Crossfade und Rampen enden bei 256, danach M-33-Zustand; das gezählte Fenster ist vollständig ungeschrieben, die Gegenprobe belegt nur noch „das Band ist bei Mix 1 hörbar“. Tap über `dspKernFuerTest().tap (…)` hielte es wie in B6.
- **A16 `eq_an_bypass_aus_alles_neutral_ist_bitidentisch`**, `:356-373`. 44,1 und 96 kHz, 1000 Blöcke ab Sample 0 (`:365`, `fahreNull` ohne `zaehlenAb`), Vorbedingung `rechnet` (`:368-369`). Heute: jeder Sample durchläuft die engagierte Kette und den Float-Rücklauf, bitgleich für Rauschen. Nach dem Bau: nur die 256 Samples des Engagier-Fades werden geschrieben (Dry und neutrale Bank gleich, exakt), danach nichts. Der Tap hielte „die neutrale Kette rechnet die Identität“ (`post_committed` = `(double)` Eingang); den Float-Rücklauf gibt es danach nicht mehr.
- **B6 `unity_kurzschluss_statt_multiplikation (M-02)`**, `DspGoldenTestMain.cpp:637-677`. Trims +6 dB, dann 0 dB mit Rampe, `kRampeSamples + 2048` Stille, 40 Blöcke verglichen, keine Vorbedingung. Heute: Ausgang bitgleich durch die volle Kette nach einer Rampe. Nach dem Bau: nichts geschrieben; der SONDE-015-Rotbeweis „die Rampe endet auf `0.99999994f` statt exakt `1.0f`“ (`SONDE-015.md:1035`) bliebe wirksam, weil die Neutralprüfung `Rampe::ruhtBei (1.0)` verlangt und dann geschrieben würde. Eine Multiplikation mit exakt 1,0 statt des Kurzschlusses ist in `double` heute wie nach dem Bau unsichtbar, auch am Tap.
- **B6 `width_eins_ist_bitidentisch (M-30)`**, `:2143-2171`. Zehn Blöcke nach 2048 Stille. Heute schon ohne Trennschärfe für den Kurzschluss (Kommentar `:2167-2169`: „Die Bitgleichheit haelt in `double` auch ohne ihn“); getragen wird er vom Zähler `msStufenLaeufe() == 0` (`:2170`), der nach dem Bau unverändert misst.
- **B6 `identische_zustaende_nullen_beim_wechsel (M-55)`**, `:3528-3553`. Neutral, Hörmatrix Processed → Dry, vier Blöcke zu 256, Toleranz 1e−6. Nach dem Bau wird Block 1 (Hörmatrix-Fade) weiter geschrieben, Blöcke 2 bis 4 nicht; das Fenster, in dem die Zusage liegt, bleibt gemessen.
- Geprüft und nicht betroffen: `delta_bei_gleichheit_ist_exakt_null (M-54)` `:3445-3461` (Delta schreibt immer, F-4), `hoermatrix_dry_ist_bitidentisch (M-53)` `:3420-3442` (wirksames Programm, F-4), alle ausgeschalteten und Hard-Bypass-Fälle (A16 `:219-354`, `:375-418`; B6 `:572-635`, `:679-709`, `:838-1036`), `mono_und_stereo_bleiben_angenommen` (`SondeNullTestMain.cpp:830-865`, Passthrough ausgeschaltet), M-48 und M-120 (wirksame Bänder).

## L-2 — Übergangsfenster in den neutralen Zustand (Fakten)

- F-4 (`NAK-311.md:1354-1363`): „Die Neutralprüfung bestimmt nur, bis zu welchem Sample der Kern schreibt; gerechnet wird weiter (Taps, Zähler, Auslenkungen). Die Grenze ist das späteste von Crossfade-Ende, Rampenende und Hörmatrix-Fade-Ende im Stück. Nach einer Bandrampe auf 0 dB trägt ein Einheitsbiquad noch zwei Samples Rest (`DspKern.cpp:474-483`); ist beim Übergangsende ein Bandzustand nicht leer, schreibt der Kern bis zum Stückende weiter, und die Ruhe beginnt am nächsten Stück — konservativ, weil in diesem Stück ein Übergang lief und M-02 die Ruhe meint. Delta und Candidate schreiben immer (M-18). …“
- Fenster je Art: Engagier-Fade aus der Ruhe und jeder Crossfade in ein neutrales Programm `kFadeSamples` = 256 (`DspKern.cpp:414`); Rampenübergang auf die Ruhewerte `kRampeSamples` = 256 (`DspProgramm.h:57`); Hörmatrix-Fade Processed ↔ Dry `kFadeSamples` = 256 (`DspKern.cpp:982`); nach einer Bandrampe mit nicht leerem Bandzustand bis zum Ende des Stücks, in dem der Übergang endet — mindestens 256, höchstens 256 + `maxBlock` − 1 Samples. Die Angabe des Prüfers „bis zu 256 Samples“ gilt damit für die ersten drei Arten, nicht für den Bandrampenfall. Delta und Candidate sind kein Fenster, sie schreiben dauernd. In jedem geschriebenen Sample liegt die Hin- und Rückwandlung `DspKern.cpp:893-895` und `:1077-1084`.
- Zählbeginn: der bestehende A16-Fall `eq_an_bypass_aus_alles_neutral_ist_bitidentisch` zählt ab Sample 0 (`SondeNullTestMain.cpp:365` ohne `zaehlenAb`, `:169`), also samt Engagier-Fade, aber mit Rauschen ohne Subnormals. M-10 (`:577`): Rauschen wie heute ab Sample 0, das Bitmuster erst „in jedem Block ab dem Ende von Engagier-Fade und Rampen“ (ohne Zahl); M-01 (`:568`): „Sample ab `kFadeSamples` + `kRampeSamples`“. Kein Fall legt Subnormals, ±0 oder kleinste Normale in ein Übergangsfenster.
- R-311-2 wörtlich (`NAK-311.md:114-124`): „**R-311-2 (W04, Reihenfolge).** Das Messgerät zuerst: `tools/fl/nulltest.py` urteilt auf dem Weg R1 nur dann BITIDENTISCH, wenn die Bytes des Bereichs gleich sind (SHA-256 oder Bytevergleich); ein Unterschied nur im Nullvorzeichen ist eine Abweichung mit eigener Kennzeichnung. Danach T3-01-01; ihr erster Schritt ist die Messung, ob die Konversionen Float nach Double und zurück unter DAZ und FTZ Subnormals und −0 verändern (A16 mit Material aus Subnormals, ±0, kleinsten Normalen). Das Ergebnis entscheidet DEFEKT oder LÜCKE; Fix und Rotbeweis sind in beiden Fällen dieselben. Wird der Laufzeit-Arm nach der Reparatur des Messgeräts aus Gründen außerhalb des Plugins rot (Nullvorzeichen aus Host oder Renderweg), ist das ein eigener Befund, keine Regression dieses Tickets.“ — schweigt zum Fenster.
- SONDE-015 M-02 wörtlich (`docs/beweise/SONDE-015.md:1035`), Zustand „`eq_enabled` **true**, `bypass` false, alle 120 Werte sonst auf Default“; Zusage „Auch **ohne** Hard-Bypass ist der engagierte, aber neutrale Kern bitidentisch: 0 dB Trim läuft **nicht** als Multiplikation durch die Samples, sondern über einen Unity-Kurzschluss — einschliesslich der Rampe, die auf dem Weg zu 1,0 durch `0.99999994f` laufen kann. Dasselbe gilt für `width` = 1,0, `mono_bass_hz` = 0, `mix` = 1,0 und `auto_gain` = false: der vollständige Weg aus §3.0 ist durchlaufen und schreibt trotzdem keinen Sample.“ — schweigt zu Engagier-Fade und Hörmatrix-Fade; der Rampensatz meint laut Rotbeweisspalte das Rampenende („oder die Rampe endet auf `0.99999994f` statt exakt `1.0f`“), nicht die Samples während der Rampe. Die Lesart „M-02 meint die Ruhe“ steht nur in F-4 (`:1361-1362`), nicht im Wortlaut von M-02.

## L-3 — Crossfade mit Übertragung und gleichzeitiger Wertänderung (Fakten)

- Bestätigt am Code: im Crossfade ruft `verarbeitePfad` die aktive Bank ohne Quellbank (`DspKern.cpp:780` `verarbeiteBank (z, klingend, rampe ? z.quelle : -1, rampe ? z.rest : 0, …)`), `:644` setzt `quelle` nur bei `rampeRest > 0`, `:470-472` `qb` nur mit Quellbank und `rampeRest > 0`; ohne `qb` ist t = 1,0 (`:493-495`) und `mitte` falsch (`:502`). Damit rechnet die neue Bank ab dem ersten Sample mit ihren eigenen Koeffizienten: statischer Biquad `b.statisch` (`:588`), Detektor `b.detektor` (`:535`), Hüllkurvenpole `b.huelle` (`:552`), Entwurfswerte `b.grundG`, `b.q`, `b.gainDb` (`:559`); ebenso der Mono-Bass-Hochpass (Mischung nur mit Quelle, `:683-685`). Koeffizienteninterpolation über `kRampeSamples` gibt es nur im Rampenübergang.
- Präzisiert: ein dynamisches Band trägt im `BandZustand` auch `schrittRest`, `svfVon` und `svfNach` (`DspBankPool.h:65-70`). Wird er übertragen, laufen bis zu 8 Samples mit dem Entwurf des alten Programms, danach mischt die Steuerrate über 8 Samples zum neuen Entwurf (`DspKern.cpp:557-581`). Für statische Bänder, Detektor und Hüllkurve wirkt der neue Satz sofort.
- `kaltSchranke` (`DspGoldenTestMain.cpp:486-517`, E-31 `docs/beweise/SONDE-015.md:3425`): „hergeleitet aus dem Einschwingen der KALT startenden Zielbank. Die Quellbank steht eingeschwungen auf `quelle`; die Zielbank startet mit Zustand 0 auf dem konstanten Eingang `x` und liefert y(n) - gerechnet mit der eigenstaendigen RBJ-Referenz dieses Tests. Mit t = n/K ist der Ausgang o(n) = quelle*(1 - t) + y(n)*t, also o(n) - o(n-1) = (y(n-1) - quelle)/K + t*(y(n) - y(n-1)) fuer n <= K; o(n) - o(n-1) = y(n) - y(n-1) danach.“ Vorbedingung: ein Band (Typ, fs, f0, Q, Gain) ab Zustand 0, konstanter Eingang, eingeschwungene Quelle mit konstantem Ausgang; B-5 nimmt das Maximum zweier solcher Schranken bei DC 0,3 (`:1077-1079`). Für eine Zielbank mit übertragenem, von 0 verschiedenem Zustand oder mit neuen Koeffizienten auf warmem Zustand ist sie nicht hergeleitet.

## L-4 — übertragener `monoBassZustand` (Fakten)

- Bestätigt: unter M-01 bis M-83 (`NAK-311.md:568-693`) nennt nur M-48 (`:642`) den Mono-Bass, und zwar den Wechsel an/aus: „(a) Mono-Bass 0 → 120 Hz (Crossfade, E-20)“, Zusage „… bei (a) wird auch der Mono-Bass-Zustand nicht übertragen“, danach Stille. Keine Zeile fährt beiderseits aktive Mono-Bass-Stufe bei gleicher Pfadkennung, den Fall aus §7.3 (`:1047-1048`) und F-8 (`:1389-1392`).
- Sichtbar wird der Seitenhochpass nur über eine Seitenkomponente: `DspKern.cpp:679` `double s = (L[i] - R[i]) * 0.5;`, `:686` `s = bank.monoBassZustand.tick (hp, s);`. Ein Signal mit Anteil L ≠ R um oder unter `mono_bass_hz` zeigt ihn, etwa ein reines Seitensignal (L = a·sin, R = −L, wie M-26 für Width) oder ein Quadraturpaar (L sin, R cos); ein Monoton (L = R) ergibt s = 0 und verbirgt ihn ganz. Die übrigen §6.3-Zeilen fahren einen 50-Hz-Sinus (M-43, M-46 Slot 1) und einen 1-kHz-Quadraturton (M-46 Slot 0) bei Mono-Bass 0; M-48 (a) schaltet ihn ein und fährt danach Stille.

## H-2 — Nachbau-Mutation bei Hälften „heute rot“ (Fakten)

| Zeile | Hälfte | Wortlaut der Rotbeweisspalte | Nachbau-Mutation |
|---|---|---|---|
| M-28 (`:607`) | Bitgleichheit zum frischen Kern | „Bitgleichheit zum frischen Kern heute rot (heute steht die Rampe in der Ruhe auf 15,849)“ | keine, nur Basislauf |
| M-29 (`:608`) | Wiedereinschalten | „Wiedereinschalten heute rot (die Rampe steht nach dem Ausblenden auf +24 dB)“ | keine, nur Basislauf |
| M-31 (`:610`) | zweiter Candidate | „zweiter Candidate heute rot (seine Rampen starten beim alten Ziel)“ | keine, nur Basislauf |
| M-46 (`:640`) | Slot 1 warm | „Slot 1 warm heute rot“ | keine, auch kein Basisbeleg |
| M-47 (`:641`) | Slot 1 | „Slot 1 heute rot“ | keine, auch kein Basisbeleg |
| M-49 (`:643`) | (b) | „(b) heute rot“ | keine, auch kein Basisbeleg |
| M-65 (`:670`) | (b) | „(b) heute rot (Zweikanalformel)“ | keine, nur Basislauf |
| M-10 (`:577`) | Ausgang (b) | „bei (b) Regressionswache; in beiden Fällen nach dem Bau zusätzlich gebrochen: Neutralprüfung entfernt → … bei (b) rot nur über M-11“ | genannt, fällt aber an M-11, nicht an M-10 |
| M-19 (`:586`) | Wachmarke; Bitmuster | „Wachmarke heute rot (wie M-11); Bitmuster nach M-01“ | keine eigene; „wie M-11“ steht am Basisbefund, nicht als „Rotbeweis wie“ |
| M-21 (`:588`) | beide | „wie M-10 und M-11 je Rate (Wachmarke heute rot, Bitmuster nach M-01)“ | keine eigene; Verweis auf M-10 und M-11, erbt die Lücke von M-10 (b) |

Die übrigen Zeilen mit „heute rot“ nennen eine Nachbau-Mutation oder „Rotbeweis wie M-nn“: M-03, M-11 bis M-14, M-20, M-23 bis M-27, M-32 bis M-38, M-40, M-41, M-43, M-44, M-52 (deterministischer Teil), M-53 bis M-55, M-57 bis M-60, M-62, M-67. Ob sie wirken, ist nur für M-32, M-34 (D-1a) und M-52 (D-1d) geprüft.

## Selbst nachgemessen

- `git rev-parse HEAD` zu Beginn `5114e9b320957ba6233bd51c8e4bbc0b9972b1a8`; `git status --short` zeigte nur `briefing-hub/`, `nimbalyst-local/` und die Auftragsdatei.
- `git diff --stat ac62c734c1b6448767f9a37c537a99fd7257437f HEAD -- eq-copilot broker tools`: nur `tools/pruefung/tiefenaudit_karte.py` (+446), außerhalb der geprüften Quellen. `git diff --stat HEAD -- eq-copilot broker tools docs/beweise/NAK-311.md`: leer. `git ls-files --eol docs/beweise/NAK-311.md`: `i/lf w/crlf`.
- Am HEAD gelesen: `DspKern.cpp` vollständig, `DspKern.h` vollständig, `DspProgramm.cpp` vollständig, `DspProgramm.h:48-72`, `DspFilter.h` vollständig, `DspSvf.h:80-166`, `DspBankPool.h` vollständig, `DspBankPool.cpp:8-56`, `:109-158`, `:210-238`; `SondeProcessor.cpp:225-433`, `:1025-1072`, `:1255-1279`; `DspGoldenTestMain.cpp:281-294`, `:440-709`, `:980-1128`, `:2138-2337`, `:3040-3094`, `:3320-3564`, `:3960-4185`; `SondeNullTestMain.cpp:60-194`, `:218-457`, `:794-883`; `TransactionTestMain.cpp:118-137`, `:1700-1739`, `:2140-2209`; `nakama-parameter-v2.json:136-172`, `:233-249`; `dsp_probe.cpp:1-56`, `:89-96`.
- Gerechnet: t_E und B/A für Q 0,707 (n = 148, 149, 255) und für Q 1,0; Knie bei n = 149; t_E des Q-2-Prüflings; das diskrete Crossfade-Modell des Phase-16-Werts (Summen C0 = −2,539058, C1 = −800,01, C2 = −202 827,6 über cos(πn/12), n = 0 bis 255); die Koeffizientengleichheit des Bell 0 dB; Polradius und Zeitkonstante des +12-dB-Bells bei Q 2 und Q 1; b0 des Low-Shelf 8 kHz Q 0,707 +9 dB (1,4314); B/A bei doppelter Fadelänge.

## Nur gelesen / nicht geprüft

- Kein Bau, kein Lauf, kein Kanon: alle Aussagen über Rot und Grün vor und nach dem Bau sind aus Quelle und Bauplantext hergeleitet. Die −6 dB für die Doppelmutation in M-45 sind eine Hüllkurvenabschätzung; die +0,042 dB Einschwinganteil in Phase 16 sind die Differenz Messung minus Crossfade-Modell, nicht eigens gerechnet.
- Nicht gelesen: die Hashfunktion des Transaktionskerns (D-1e hängt nicht daran), `DspBankPool.cpp` außerhalb der genannten Zeilen, die Quellvalidierungen Teil A und B, `BEFUNDE.md`, SONDE-015 außer `:1035`, `:1081`, `:1102`, `:1103`, `:1162`, `:3425`, der Etappe-1-Auftrag außer `:22`, der Prüfauftrag außer `:15-16`, `:28`.
- Nicht geprüft: ob MSVC `b1*x - a1*y + z2` zu einer FMA zusammenzieht (D-1c hängt nicht an Bitgleichheit); die Knie- und Attack-10/500-Werte der Phase 16 außer als Richtung; ob die Wachmutation von M-51 den neuen Fall trifft.
- Nicht Gegenstand: H-1, H-3 bis H-8, U44 bis U47, U54, Etappe 5, T3-12-*, Legacy-Bezeichner, Design.

## Beifang

- M-77/M-78: „davor höchstens Betrag(Range) + 0,1 dB“ hält mit der Dry-Beimischung nur bei Grundgain ≥ 0 dB, wie F-12 (`NAK-311.md:1430-1432`) für den Prüfling sagt; als allgemeine M-84-Zahl (§7.4 `:1215-1216`) risse es etwa bei Grundgain −6 dB und Range −12 dB schon am ersten Sample (B/A = 10^(18/20), +18 dB).
- M-51, neuer Fall: `kaltSchranke` gilt für ein einzelnes kalt startendes Band auf konstantem Eingang (L-3); mit übertragenem Slot 0 passt sie nur bei DC-Eingang mit x = eingeschwungener Ausgang von Slot 0. Die Zeile legt das nicht fest.

HEAD zu Beginn: `5114e9b320957ba6233bd51c8e4bbc0b9972b1a8` · HEAD am Ende: `5114e9b320957ba6233bd51c8e4bbc0b9972b1a8`
