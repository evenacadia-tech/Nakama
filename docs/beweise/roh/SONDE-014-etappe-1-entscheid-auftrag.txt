SONDE-014 — Etappe 1b, Entscheid des Dirigenten zu E-01 bis E-09 und zur Lücke E-10 (2026-09-06). Zusatzauftrag an denselben Opus-Bauer.

Deine Etappe 1 (Commit 1f126a4) ist gemessen: Worktree sauber, HEAD auf origin, Rundenbilanz Doku +1331/-0 wie für eine Spezifikationsetappe erwartet. Jeder Vorschlag in §4 ist an der Quelle nachgeprüft (reservierte-nachrichten-v1.json: draft_offer und user_verdict gehören SONDE-014, finding/intent/assistant_step sind frei; session_snapshot trägt experimente und paare seit Wire-Envelope-schema_minor 2 als optionale Felder; keine Experimentfamilie trägt ein Zielfeld; FeatureEngine.h kEvidenzIntervallMinS 0,25 s, WorkerCadence.h 50/250 ms). Es gilt Folgendes. Arbeite es in docs/beweise/SONDE-014.md ein: je Entscheid ein klar markierter Block „Etappe 1b (Entscheid des Dirigenten, 06.09.2026)" unter dem jeweiligen §4-Abschnitt (der alte Vorschlagstext bleibt stehen); die betroffenen Matrixzeilen in §3 werden in Zusage, Belegklasse, Test und Rotbeweis nachgezogen (die Matrix ist bis zur Matrixprüfung Spezifikation im Entstehen; der §4-Block hält den vorigen Wortlaut als Historie); §3.10 neu zählen; §5.1, §5.2 und §5.4 nachziehen; im Kopf eine Zeile „Entscheide | E-01 bis E-10 wörtlich in docs/beweise/roh/SONDE-014-etappe-1-entscheid-auftrag.txt". Dann py -3.13 tools/plan/dokuriegel.py docs/beweise/SONDE-014.md, Commit nur mit Pathspec (docs/beweise/SONDE-014.md docs/beweise/roh/SONDE-014-etappe-1-entscheid-auftrag.txt — diesen Auftrag unverändert), git pull --rebase bei sauberem Worktree, push, und beende deinen Turn mit der letzten Zeile „ETAPPE 1B ABGESCHLOSSEN". Kein Bau, kein Produktcode, kein Schema, keine Nachfrage an den User, kein Löschbefehl.

E-01 (M-02) — ANGENOMMEN (A) mit Präzisierung E-01a. `rolle` ist ein geschlossenes Feld mit genau fünf Werten; eine Quelle trägt je Scope (global oder Passage) genau eine Rolle oder keine. Ableitung nach deiner Tabelle für führt (foreground, lead), trägt (middle, foundation), begleitet (background, texture). Für `geschützt` und `bewusst verschmolzen` ist `prominence` NICHT „unverändert", sondern abgeleitet auf den festen Neutralwert `middle` mit leerem Funktionstag und einem Veto-Kennzeichen (`schutz` beziehungsweise `verschmolzen`) als Teil der Belegung — nur so ist die Abbildung total und injektiv (M-02: Rückweg eindeutig, keine zweite Wahrheit nach §33.5). Orthogonal und von der Rolle unabhängig bleiben die geschützten Bereiche und Eigenschaften (Attack, Breite, Ausklang, Bandintervalle) sowie die paarweisen Beziehungen aus §37.1 — das sind die Schutzangabe und die Verschmelzungs-Erlaubnis, über die U22 Impuls und Raum abbildet (M-03, M-04). Produktwirkung, die daraus folgt und keine neue Produktfrage ist: eine Quelle mit Rolle `geschützt` trägt keine Prominenzposition; wer Position und Schutz zugleich will, setzt die Positionsrolle und dazu Schutzbereiche. Test: Roundtrip über alle fünf Rollen plus die Probe, dass zwei verschiedene Belegungen nie auf dieselbe Rolle zurücklesen.

E-02 (M-04) — ANGENOMMEN (A). Rolle `bewusst verschmolzen` = globales Veto gegen Entmaskierungsempfehlungen mit Beteiligung dieser Quelle; die paarweise Erlaubnis beziehungsweise gerichtete Beziehung ist das spezifischere Werkzeug und gewinnt, wo sie gesetzt ist — eine explizite Beziehung „A führt vor B" hebt das globale Veto nur für dieses Paar auf. Beide auf Stufe 2 der Konfliktregeln, spezifisch vor global (analog Stufe 3). Test: drei Fälle (nur Rolle, nur Beziehung, beides im Widerspruch).

E-03 (M-37) — ANGENOMMEN (A) mit Präzisierung der Zusage. Der Wert entsteht im Broker-Coordinator bei jedem Evidenz-Eingang einer der beiden Quellen und reist über den Rückweg aus E-04. Die Zusage M-37 lautet: bei jedem schweren Editortakt (250 ms) liegt der Wert so aktuell vor wie die jüngste Evidenz beider Quellen; im Normalbetrieb (kEvidenzIntervallMinS = 0,25 s) ist er höchstens ein Evidenzintervall plus Transportlatenz alt; bei P1-Rückstau folgt er der bestehenden Kadenzreduktion bis 1 Hz und trägt dann ein Kennzeichen `herabgesetzt` (Feld im Wert, kein interpolierter Zwischenwert). Der Test misst das Alter des Werts relativ zur jüngsten Evidenz, nicht eine Wallclock-Rate; der Rotbeweis lässt den Wert nur beim Neuberechnen eines Befunds entstehen.

E-04 (M-40) — ENTSCHIEDEN: JA, ein eigenes v3-Feld, aber am Rückweg, nie an der Sondenmessung. Kein Feld am `evidence_snapshot`. `session_snapshot` bekommt als Wire-Envelope-schema_minor 3 (Fassungsschritt wie Fassung 2 in SONDE-013) genau EIN neues optionales Feld `findings`: die Liste der Befunde mit Zustand (READY TO SEND / MORE DATA / STALE), den Datenfeldern aus M-1x/M-2x (sechs Teile, drei Anzeigezeilen, evidence_ids, Intent-Revision) und je Befund einem optionalen Objekt `maskierung` (`quelle_a`, `quelle_b`, `band_von`, `band_bis`, `wert_db`, `gueltig`, `herabgesetzt`). Die Maskierung hängt am Finding, nicht in einer zweiten Liste — der Blueprint nennt sie „zweite Darstellung desselben Finding-Datenwegs" (M-41), und zwei Listen könnten auseinanderlaufen. Der Name lautet `maskierung`, nicht `ueberdeckung` (Kollision mit der Zeitüberdeckung, §2.12). Der Plantext-Satz „der Name gehört in die v3-Vertragsrunde von S16–17" wird so erfüllt: S16–17 ist abgenommen; der Name wird in diesem Ticket als belegtes Feld in eq-copilot/schemas/v3/reservierte-nachrichten-v1.json eingetragen, im v3-README als Fassungsschritt geführt, mit Cross-Language-Fixtures (C++ und Rust), Negativfixtures und Fassungsleiter von A5 bewacht — genau der Weg, den SONDE-013 für `experimente` und `paare` gegangen ist. Ein Leser der Fassung 2 sieht das Feld nicht (optional: Abwesenheit heißt „nichts", nie null). Damit darf Etappe 2 den Rückweg bauen; die Sperre aus §5.4 Zeile 1 entfällt. Der Dirigent zieht beim Abschluss die Plantexte (Überdeckung → Maskierung) nach; das ist nicht dein Auftrag.

E-05 (M-48, NAK-168) — ANGENOMMEN (A) mit `proposal_id` als Rückverweis. `experiment_begin` bekommt in derselben Fassung 3 ein optionales Objekt `ziel` (`band_von`, `band_bis`, `geschuetzte_baender` als Liste von Bandintervallen, `proposal_id`). Ohne `ziel` (manueller Versuch ohne Vorschlag, SONDE-013-Pfad) bleibt die heutige Heuristik und das Resultat trägt das Kennzeichen `ziel_geraten`; mit `ziel` liest der Guardrail-Rechner Ziel- und Schutzbereiche und rät nichts. Beide Pfade werden gemessen, je mit Rotbeweis (Heuristik trotz `ziel`; fehlendes Kennzeichen ohne `ziel`).

E-06 (M-43) — ANGENOMMEN. Feld `revert` mit geschlossener Menge `experiment_abort`, `manual_only`, `none_needed`; `dsp_revert` ist P7 (revert_transaction, SONDE-016) und in P5 ein Vertragsbruch wie `execution = previewable` (M-45). Test: jeder der drei Werte je einem Proposal-Typ zugeordnet, vierter Wert rot in C++ und Rust (M-77).

E-07 (M-55) — ANGENOMMEN. `Preview` bleibt in der Zustandsmenge, die P5-Übergangstabelle führt keine Kante dorthin; ein gespeicherter `AssistantStep` mit `Preview` ist in P5 ein Lesefehler, kein stiller Sprung.

E-08 (M-71) — ANGENOMMEN mit Präzisierung der Autoritäten nach §33.5. Die autoritative Validierung des Intents (Rollenmenge, Zyklusprüfung, Konfliktauflösung beim Setzen, M-05/M-06/M-13) und die Zustandsmaschine des `AssistantStep` (Übergänge, Abbruch, Zurück, Überspringen, Resume, Rekonstruktion, M-5x) laufen im Main, also im Gen-Plugin in C++, persistent im `MainProjectState`. `intent.rs` und `assistent.rs` im Broker-Coordinator sind Spiegel und Vertragsvalidierung beim Empfang (M-77: dieselbe Rollenmenge, dieselben Enumgrenzen beidseitig) sowie die Auswertung der Konfliktregeln beim Rechnen — keine zweite Zustandsmaschine, keine zweite Wahrheit. `hypothese.rs`, `proposal.rs` und `maskierung.rs` rechnen im Coordinator, weil dort Evidenzbestand und Store liegen. Je Modul eine Verdrahtungsdatei nach dem SONDE-013-Muster; die Produktpfade werden in der Verdrahtung geschlossen, Tests rufen die Verdrahtung, nicht das Modul.

E-09 (M-73) — ANGENOMMEN. `draft_offer` (reserviert, wird belegt) reist als P1 mit Schlüssel `proposal:<proposal_id>`; `user_verdict` (reserviert, wird belegt) als persistenzpflichtiger P0 über `sendePersistenzP0` mit Wiederholung unter derselben `command_id` über einen Brokerkill hinweg (A4-SI-Muster); Findings und Maskierungen reisen über `session_snapshot.findings` (E-04) und erben dessen Koaleszierung.

E-10 (LÜCKE, neu, zwei Matrixzeilen M-85 und M-86) — Transport des Intents vom Main zum Broker. Deine Matrix legt den Intent in den Main-State (M-09, M-71) und lässt den Broker damit rechnen (M-12, M-4x, E-08), nennt aber keine Nachricht dafür; `intent` ist im Register frei. Regel: Der Intent reist vom Main zum Broker über eine eigene Familie, die dieses Ticket in reservierte-nachrichten-v1.json reserviert und belegt (Namensvorschlag `intent_update`; endgültigen Namen entscheidet die Matrix mit Begründung, im Muster der bestehenden Familiennamen). Klasse P1 mit Schlüssel `intent:<quelle_id>:<passage_id oder global>`; Koaleszierung ist zulässig, weil nur die jüngste Revision wirkt (M-10). Nach Verbindungsaufbau und nach jedem Brokerneustart meldet Main seinen vollständigen Intent-Bestand (auch einen leeren) mit einer Vollständigkeitsmarke samt Revision, BEVOR der Broker eine Hypothese oder ein Proposal rechnet (Reihenfolge nach M-75/M-76); bis diese Marke vorliegt, entstehen keine Proposals — der Zustand ist MORE DATA. Grund: ein Veto (geschützt, bewusst verschmolzen) darf durch Transportverlust nie verschwinden. M-85: Transport, Schlüssel, Koaleszierung und Revisionsordnung (Test A4-Verdrahtungsfall plus B10; Rotbeweis: ältere Revision überschreibt jüngere nach Koaleszierung). M-86: Vollständigkeitsmarke vor jeder Rechnung (Test A4 Neustartfall; Rotbeweis: Proposal entsteht vor der Marke). Beide Zeilen tragen Belegklasse BAULÜCKE mit Entscheid.

Angenommen ohne Änderung: §4.9 P-01 bis P-04 (zieht der Dirigent ins Register beziehungsweise als Karten nach docs/plan/fragen.json), §4.10 N-01 bis N-05 (Register beim Abschluss; N-02 und N-05 zieht Etappe 2 mit nach, wie von dir vorgeschlagen), §4.11, §5.5. Die Fassung 3 (E-04, E-05, E-09, E-10) ist EIN Fassungsschritt des Wire-Envelopes in einer Bauetappe, nicht vier — §5.1 entsprechend ordnen.
