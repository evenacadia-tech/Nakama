NAK-312 — Validierung der Matrixprüfung 1 an der Quelle. Auftrag des Dirigenten, 21.09.2026.

ROLLE. Du bist ein frischer, lesender Validierer (nicht der Bauer der Matrix, nicht der Prüfer). Repo C:\Users\phili\Projekte\Nakama, Branch master, sichtbarer Checkout. Du prüfst die Befunde eines Prüfberichts an der Quelle und lieferst dem Dirigenten die Entscheidungsgrundlage. Du reparierst nichts und änderst die Matrix nicht.

GRENZE. Deine einzige Schreibaktion ist die neue Datei docs/beweise/roh/NAK-312-matrixpruefung-1-validierung.md (LF-Zeilenenden). Kein Bau, kein Testlauf, kein Kanon, kein Skript, kein FL Studio. Git nur lesend (status, rev-parse, log, show, diff, blame, grep, ls-files); kein add, kein commit, kein push. Fremde Dateien fasst du nie an (briefing-hub/ und nimbalyst-local/ sind fremd). Miss git rev-parse HEAD und git status --short zu Beginn und vor dem Schreiben und nenne beides im Kopf.

GEGENSTAND. Der Prüfbericht docs/beweise/roh/NAK-312-matrixpruefung-1-urteil.md (URTEIL NEEDS_WORK: neun DEFEKTE D1 bis D9, drei LÜCKEN L1 bis L3, drei HÄRTUNGEN H1 bis H3) über die Verhaltensmatrix und den Bauplan in docs/beweise/NAK-312.md §5 bis §8 (Stand 2deb5e3d; §0 bis §4 und §9 sind Gate-Bezug). Prüfauftrag: docs/beweise/roh/NAK-312-matrixpruefung-1-auftrag.txt. Der Produktcode ist seit 090d0fa1 unverändert.

TEIL 1 — je Befund D1 bis D9, L1 bis L3, H1 bis H3:
a) Der beanstandete Satz oder die Matrixzeile, wörtlich mit Zeile in docs/beweise/NAK-312.md.
b) Nachmessung an der Quelle: jede Codezeile, auf die sich der Befund stützt, selbst lesen und wörtlich mit Datei:Zeile zitieren. Stimmt die Reproduktion? Stimmt sie nur teilweise, sag genau, was anders ist.
c) Welcher Satz bricht wirklich (Regel R-312-n aus §2, Entscheid E-312-n aus §9.2, Zusage aus §5, Matrixregel aus §6, CLAUDE.md-Invariante, Kommentar mit Besitzregel im Code) — wörtlich mit Quelle. Bricht keiner, steht das da.
d) Urteil: BESTÄTIGT, PRÄZISIERT oder WIDERLEGT, und deine Einordnung: DEFEKT (verletzt Matrix, Gate, Regel, Entwurf oder Invariante), LÜCKE (Quellen schweigen; der Dirigent entscheidet die Regel) oder HÄRTUNG (von keiner Zusage verlangt). Prüfe ausdrücklich, ob der Befund unter einen Ausschluss des Prüfauftrags fällt.
e) Die schließende Regel in einem Satz: was muss in Matrix oder Bauplan danach gelten, damit der Befund geschlossen ist (kein Text der Matrix, nur die Regel).

TEIL 2 — Entscheidungsgrundlagen, an der Quelle belegt (der Dirigent entscheidet, du legst vor):
1. Zu D1 (Blockbindung der Bandwerte, R-312-10). Fakten: welche Hostparameter sind kontinuierlich und nichttopologisch (NakamaParameter.cpp), welche wirken über Rampenziele am Blockrand, welche nur über ein Programm (DspProgramm.cpp: baueProgramm, entwurf, svfEntwurf, leiteAutoGainAb, mono_bass_hz; DspKern.cpp: blockrand, nurRampen, rampenKompatibel, die Bänke und der Pool in DspBankPool.*). Prüfe dann jeden dieser Wege auf Machbarkeit OHNE Sperre, Allokation und Warten im Audio-Callback (E-312-2 gilt: kein Warten, auch nicht offline) und mit gleichem Ergebnis in Echtzeit und offline für denselben Event- und Blockverlauf: (A) der Audio-Callback rechnet am Blockrand für den Rampenfall (gleiche Topologie) die betroffenen Koeffizienten selbst aus den Mailbox-Atomics in eine vorallokierte Bank — was liest baueProgramm heute, das nicht atomar vorliegt; was kostet der Entwurf je Band und die Auto-Gain-Ableitung je Block in Rechenschritten; welche Zustände (Merkzettel, Kennungen, Bericht, Epoche) gehören allein dem Worker und dürften nicht berührt werden; (B) der Worker bleibt Erbauer, aber die Übernahme wird deterministisch an Blöcke gebunden (Blockzähler, feste Verzögerung in Blöcken, vorallokierter Ring Callback→Worker und Bank Worker→Callback) — was geschieht, wenn der Worker offline bei rund 14-facher Geschwindigkeit nicht nachkommt, ohne dass der Callback wartet; ist Gleichheit dann noch zusagbar; (C) die Zusage wird auf das Erfüllbare begrenzt: globale Gains blockgebunden (Weg 2), Bandwerte weiter über den Worker, M-120 und R-312-10 entsprechend enger gefasst und der Rest als datierte Registerzeile zum Wrapperpatch aus Entwurf §44.3 — was bleibt dann hörbar im Export falsch, in Zahlen aus den Quellen (Blockgröße, Workertakt 5 ms, Offlinegeschwindigkeit). Je Weg: Machbarkeit am Code mit Datei:Zeile, Umfang (welche Dateien und Funktionen), Risiko für Nulltest, Goldens, M-47, M-81, und ob er in ein Audit-Nacharbeitsticket passt. Gib eine begründete Empfehlung.
2. Zu D2: eine überlaufsichere Schiedsregel zwischen Blockrand und Workerpublikation, die M-19 (Gleichheit statt Ordnung bei der Quittierung) nicht widerspricht — welche Zählerbreite, welche Differenzbildung, welcher Speicher je Parameter, und was davon heute schon existiert.
3. Zu D3 und D4: was darf der Callback beim Offline-Wechsel tun, ohne Workerbesitz (Merkzettel, DspKern.h:599-603) zu berühren — reicht hartes Schalten der Hörmatrix über ein Atomic im Kern, und wer lässt die Candidate-Bank wann los; wie bleibt „erste Offline-Samples tragen den bestätigten Zustand" (R-312-3) wahr, wenn beendePreview erst im Kontrolltakt läuft; wie wird der bestehende B7-Fall (TransactionTestMain.cpp um :2214-2236) deterministisch statt wanduhrabhängig geprüft (kontrollTaktFuerTest).
4. Zu D6 und L3: wo muss das Beenden des Markers sitzen, damit zwischen setzeBindung und dem nächsten Timertick kein Fenster bleibt (synchron im Rollenwechsel auf dem Nachrichtenthread, State.cpp setzeBindung oder spiegleKlassifikation, statt in wechsleFlaecheWennNoetig) — und welche Richtung ist am Code die gefährliche: sensor→hub (Marker wird scharf, ohne dass sein Bedienelement sichtbar war) oder hub→sensor (hörbarer Marker verliert die Erlaubnis und fadet aus); zitiere PluginProcessor.cpp um :874-881 und :980-1020, HoerMarkierung.h um :415-450 und :493-499, PluginEditor.cpp um :225-285 und :833-849. Schlage die korrekte Fassung von R-312-8 als eine Regel in beiden Richtungen vor und das deterministische Szenario, in dem ein begin wirklich entsteht.
5. Zu D7: was aktualisiereSourcesSteuerung tun muss, damit kein sichtbarer scharfer Knopf auf einer ungezeichneten Quelle steht, wenn das Hauptziel existiert, aber hinter Zeile 20 fällt (Umbenennen ändert die Ordnung) — kleinste Regel ohne neue Oberfläche.
6. Zu D8 und D9: wie M-04 formuliert sein muss, damit sie Etappe 2 rot/grün beweist und nach Etappe 5 nicht falsch rot wird; wie M-39 ohne Wanduhrkoinzidenz deterministisch wird (Haken statt Frist) und welche Ausgänge zulässig sind.

LIEFERUNG. Berichtskopf (Ticket, HEAD, Datum, Modell, Gegenstand, Prüfform, Arbeitsbaum), dann Teil 1 je Befund in der Reihenfolge D1 bis D9, L1 bis L3, H1 bis H3, dann Teil 2 Punkt 1 bis 6, dann eine Übersichtstabelle (Befund | Urteil | Einordnung | schließende Regel in einem Satz), dann „Nicht geprüft". Ein Urteil ohne zitierte Zeile gilt nicht; schreibe „nicht gefunden", statt zu raten. Beende dich mit einer Zeile: FERTIG Validierung Matrixprüfung 1, <Zahl> bestätigt, <Zahl> präzisiert, <Zahl> widerlegt.
