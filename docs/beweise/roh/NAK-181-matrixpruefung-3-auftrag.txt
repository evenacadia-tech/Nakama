Wiederprüfung Etappe 1 (Matrixprüfung 3) / Ticket NAK-181 (G4-Nacharbeit 2), Runde 2 — gebundener Prüfauftrag des Dirigenten (2026-09-05)

Du bist der frische, lesende Prüfer dieser Runde. Du liest und fährst nur lesende Kommandos (rg, git diff, git show, cat); du startest keine Prozesse des Produkts, keinen Compiler, keinen Cargo-Lauf und keinen Python-Interpreter; du schreibst nichts. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff a4a9295951643bd4b23332dbd5735f9128ea4377...20fc25642d8723ab1fe9dfb61c87e7ded2bbed03 -- docs/beweise/NAK-181.md`
(ZIEL = 20fc25642d8723ab1fe9dfb61c87e7ded2bbed03 = HEAD, vor und nach deinem Lauf identisch; eine uncommittete Datei docs/beweise/roh/NAK-181-matrixpruefung-3-auftrag.txt im Worktree ist dieser Auftrag selbst). Der übrige Ticketbereich — die Matrix vom Stand 2f88e45 — wurde in Matrixprüfung 1 und 2 bereits geprüft und ist hier nicht Prüfgegenstand. In dieser Etappe gibt es keinen Produktcode: Prüfgegenstand ist die Verhaltensmatrix in docs/beweise/NAK-181.md §2 (E1 bis E9, N-01 bis N-42 samt N-18b, N-18c, N-41b), §3 und §4 (§4.4 Nr. 1 bis 16); der Änderungsbericht des Workers steht in §5.3. Prüfstufe T3-Nacharbeit zum Gate G4.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus Matrixprüfung 2, docs/beweise/roh/NAK-181-matrixpruefung-2-2f88e45.txt; Einordnung des Dirigenten mit der Regel je Befund in docs/beweise/NAK-181.md §5.2 und im Nacharbeitsauftrag docs/beweise/roh/NAK-181-matrixnacharbeit-2-auftrag.txt):
MP2-1 [P1] „Verschiebe den größten Float in die Ablehnungsfälle — DEFEKT, MP1-1/R4a bleibt offen: Der größte normale `float` ist eine Ganzzahl weit oberhalb von `2^53−1`. Auch seine auf 15 Stellen gerundete Darstellung `3.40282346638529e38` wird von `broker/src/vertrag.rs:190–217` und `tools/eq-copilot/pruefe_v3_vertrag.py:213–230` abgelehnt. Damit sind N-18s Annahmezusage und derselbe Positivfall in §4.2:1120–1121 unerfüllbar. Dieser Wert gehört nach N-18b beziehungsweise als Ersatzform in die Fixture, nicht unter die akzeptierten Zahlen." — Regel R4c: größter normaler `float` nach N-18b und in die Ersatzform; N-18 behält nur Werte, die alle vier Riegelregeln bestehen, je Wert am Riegel nachgerechnet.
MP2-2 [P1] „Unterscheide unbestätigte von noch nicht persistierten P0s — DEFEKT, MP1-2/R3a bleibt offen: Wenn der Broker den Befehl bereits persistiert hat und nur das ACK verloren ging, erfüllt er ebenfalls N-41s Ausgangszustand. `experiment_verdrahtung.rs:53–55` ruft jedoch vor der Zielprüfung `bekannter_befehl` auf; `befehl.rs:66–79` antwortet dann `idempotent_wiederholt`, nicht `abgelehnt/unknown_target`. Der Client zählt entsprechend einen Erfolg (`ControlClient.cpp:1201–1205`). Die Matrix muss diesen Fall vom noch nicht persistierten Retry trennen und jeweils unveränderten Brokerzustand verlangen, statt zur Erfüllung ihrer Zusage die bestehende Idempotenz zu brechen." — Regel R3b: N-41 (nicht persistiert → `unknown_target`, kein Store-Eintrag) und N-41b (persistiert, ACK verloren → `idempotent_wiederholt`, Brokerzustand unverändert), Zusage „keine Wirkung, die es vor dem Reload nicht schon gab", beide Fälle mit Store-Zählung gemessen; Idempotenz bleibt.
MP2-3 [P2] „Berücksichtige abgewiesene Zahlen auch bei den PPQ-Grenzen — DEFEKT, neuer Bruch von E4/R4a: Für beispielsweise `cycle_start_ppq=1e-308` und `cycle_end_ppq=4` lassen `FeatureEngine.h:3704–3716` und `NakamaEvidenz.cpp:192–198` die endlichen Werte bis zu `zahlJson` durch. Der neue Helfer muss den ersten Wert wegen `DEZ_GRENZE` ablehnen (`vertrag.rs:183–185`); der hier angeblich unerreichbare `null`-Pfad wird damit erreichbar. Das Schema verlangt für beide PPQ-Felder Zahlen (`eq-ipc-v3.schema.json:234–235`), sodass der gesamte Snapshot ungültig würde. B5/N-18b müssen auch diesen Aufrufer mit einer vertragsgültigen Fehlerbehandlung berücksichtigen, nicht ausschließlich die Stereofelder." — Regel R4b: E4 inventarisiert jeden Aufrufer von `zahlJson` (NakamaEvidenz.cpp) und `zahl` (ControlClient.cpp) mit Klassenregel je Aufrufer (optionales Objekt entfällt ganz; Pflichtskalar mit bestehender Ersatzzahl; Bandsatz `0` ohne Bit); `"null"` nur, wo belegt unerreichbar; N-18c misst das PPQ-Paar.

GATE-BEZUG, wörtlich: P4-Exit-Gate (docs/FL-Nakama-Sonden-Design-Entwurf.md:4287-4289) Satz 2: „Kein unbekannter Zeitpfad erzeugt eine starke Cross-Probe- oder PRE/POST-Aussage." §49.2 Gate 6 (:3557): „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." Referenzmatrix docs/beweise/SONDE-013.md M-07, M-08, M-11 (Zeilen 476-480), M-18, M-22, M-23 (493-498), M-43, M-46, M-50 (533-540), M-53 (548). Invarianten aus CLAUDE.md: Schemas sind Verträge (v3 eingefroren; Writer, Reader, Fixtures, Cross-Language-Verbraucher im selben Änderungssatz); Audio-Thread ohne Sperren, Allokationen, Datei-, Pipe-, Netzzugriff und Logging; Nulltest bitidentisch; NaN-Ehrlichkeit; State verlustfrei.

Du beantwortest genau zwei Fragen:
1. Ist jeder der drei Befunde am Zielstand geschlossen — je Befund: ja/nein, mit Beleg (Zeilen-ID, Manifestzeile, Quelle Datei:Zeile am ZIEL)? Prüfe an der Quelle: R4c gegen `broker/src/vertrag.rs` (alle vier Regeln, DEZ_GRENZE) und `tools/eq-copilot/pruefe_v3_vertrag.py` — rechne jeden Wert der Randwerttabelle in E4 selbst nach (insbesondere den kleinsten normalen `float`, `2^53−1`, `1e-300`, `1.0/3.0` gedeckelt); R3b gegen `broker/src/coordinator/befehl.rs` (`bekannter_befehl` :60-89, `persistenz_p0_intern` :206-220, `unknown_target` :292-293), `experiment_verdrahtung.rs:48-60`, `ControlClient.cpp` (`inFlightNachReconnect`, `inFlightAck`) und den Store-Pfad (`command_event_lesen`); R4b gegen `NakamaEvidenz.cpp` und `ControlClient.cpp` — zähle die Aufrufer von `zahlJson` und `zahl` selbst mit `rg` und vergleiche mit der Zwölfertabelle in E4; prüfe für jeden als „unerreichbar" geführten Aufrufer die genannte Vorprüfung an der Quelle; prüfe die Klassenregel „optionales Objekt entfällt ganz samt `validity.cycle_bounds`" gegen das Schema (`$defs/cycle`, `validity`) und `FeatureEngine.h:3704-3716`.
2. Hat der Fixdiff etwas gebrochen — eine bereits geprüfte Matrixzeile, einen Entscheid E1 bis E9, eine Zeile der SONDE-013-Referenzmatrix, eine Zusage aus NAK-180, einen bestehenden Vertrag, einen Satz des Gate-Bezugs oder eine Invariante aus CLAUDE.md? Besonders: Der Worker hat beim Inventarisieren einen Nebenfund aufgenommen (§5.3 letzte Zeile, §4.7 Nr. 3, B5): `$defs/cycle` verlange `active` und `bounds_valid`, der Serialisierer schreibe beide nie (`eq-ipc-v3.schema.json:204-208`, `NakamaEvidenz.cpp:195-200`) — prüfe das an der Quelle; ist es wahr, gehört die Behebung in B5 zur vertragsgültigen Fehlerbehandlung von R4b (dann kein Bruch); ist es falsch, ist B5 zu berichtigen. Ist die Aussage „die wirksame Obergrenze des Drahtes ist `2^53`, weil jeder `double` darüber ganzzahlig ist" mit dem Riegel und mit den bestehenden Fixtures/State-Hashes (N-19) verträglich? Ändert N-41b etwas am `inFlight`-Verhalten, das NAK-180 (docs/beweise/NAK-180.md §2, Zustellgeneration, bestätigte Zustellung) zugesagt hat?

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen wie in Matrixprüfung 1 und 2: DEFEKT (bricht Gate, Matrix, Regel, Entwurf, Vertrag, Code am ZIEL oder Invariante — je Defekt Zeilen-ID, Datei:Zeile, welcher Satz bricht), LÜCKE, HÄRTUNG. Ausschlüsse: docs/** außerhalb von §2 bis §5.3 dieses Manifests (Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände; docs/beweise/G4.md, NAK-180.md, offene-punkte.md); im Register datierte Härtungen und Lücken NAK-155 bis NAK-179 und NAK-183 bis NAK-192; Nebenbefund §4.7 Nr. 1 (`seitenanteil_db`, M-45) und die `epochwechsel`-Benennung des Kanalwechsels (NAK-194) liegen außerhalb; tools/dirigent/pruefliste.md ist keine Anforderungsquelle; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung; Ticketfremdes (NAK-180, NAK-182, Anzeigefläche, v2-Telemetriebaum) nein; Kosmetik, Stil nein.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Zeilen-ID oder Manifestzeile, Quelle Datei:Zeile, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
