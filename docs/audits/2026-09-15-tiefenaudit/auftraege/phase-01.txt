PHASE 01 — ECHTZEIT (K6): der Audio-Pfad von Gen und Probeeq. Kurzname: echtzeit.

FRAGE: Welcher Aufruf auf dem Audio-Thread sperrt, alloziert, loggt, wartet, liest eine Uhr, greift auf Datei, Pipe oder Netz zu oder kann werfen — und welche Zusage (Nulltest bitidentisch, Passthrough sampleidentisch, Überlast verwirft Frames, nie Audio) hält nur, weil es niemand versucht hat?

POPULATION (statischer Aufrufgraph, vollständig): jede Funktion, die von processBlock, processBlockBypassed, prepareToPlay, releaseResources, reset, setStateInformation/getStateInformation (soweit sie den Audio-Thread berühren) und den Analyse-Einstiegen erreichbar ist, in eq-copilot/plugin/src/PluginProcessor.cpp und .h, src/prozessor/**, sonde/** (SondeProcessor), dsp/** (DspKern, DspRtWache, Biquad/SVF), core/StampedAudioQueue.h, core/ipc/IpcQueues.h und InterventionsRing.h (Audio-seitige Kanten), core/analysis/** und core/analysis/featureengine/** (Einstiege tick/push, Fensterlogik), spike/** (AuxSpike), hostbridge/**. JUCE-Interna nur dort, wo Nakama sie vom Audio-Thread ruft (juce::String, juce::var, juce::Array-Wachstum, Logger, AudioProcessorValueTreeState-Listener, MessageManager, CriticalSection).

PRÜFFRAGEN (je Frage die Population, dann die Ausreißer):
1. Sperren: jede CriticalSection, std::mutex, SpinLock, getCallbackLock und jeder atomic-Wartelauf auf dem Audio-Pfad; wo hält der Message-Thread dieselbe Sperre während Datei-, Pipe- oder Allokationsarbeit (Prioritätsinversion)?
2. Allokation: std::vector/std::string/juce::Array/HeapBlock-Wachstum, std::function-Zuweisung, Lambda-Capture mit Heap, shared_ptr-Erzeugung, juce::var, dynamic_cast/typeid, Ausnahmen (try/catch/throw) und noexcept-Grenzen (nach NAK-289 Etappe 1: TruePeak.h vorbereiten, IpcQueues.h bestaetigen) auf dem Audio-Pfad; wo ist die Vorab-Reservierung (prepareToPlay) und wo fehlt die Wache?
3. Zeit: Wanduhr, Time::getMillisecondCounter, steady_clock, Sleep, Thread::yield auf dem Audio-Pfad; wo hängt eine musikalische Größe (Zonen-Ticks, Passage, Fenster) an Wanduhrzeit statt an Samples?
4. Nulltest und Passthrough: jeder Pfad, der bei ausgeschalteter Verarbeitung ein Sample anfasst (Denormal-Flush, Kopie, Gain 1,0 als Multiplikation, Latenzmeldung ≠ 0, Tail); Bypass-Umschaltung ohne Klick; Sampleraten- und Blockgrößenwechsel (1 bis 16384, Blockgröße 0, Kanalzahl 1/2/4, Bus-Layout mit Sidechain) mitten im Lauf.
5. Überlast: jeder Ring und jede Queue, die der Audio-Thread füllt (Sample- und Deskriptor-Ring, Interventionsring, Tap zur Sonde): Politik bei voll ausdrücklich, Zähler, kein Blockieren, kein Verwerfen von Audio; Rückkanal Control→Audio ohne Sperre.
6. NaN/Inf: jede Stelle, an der ein nicht-endlicher Wert aus dem Host, aus Parametern oder aus der Rechnung in den Filterzustand, in Zähler oder auf den Ausgang gelangt; Denormals; float-Verengung am Ausgang (nach NAK-283 F12); Verriegelung und Zählung.
7. Wachen: welche der obigen Zusagen misst heute ein Test (DspRtWache im EQ-Kern, Allokationszähler in QueueStress und DspGolden, Nulltest-Beine) und welcher Pfad liegt außerhalb jeder Wache (Abdeckungskarte A-4, A-5 kennen: RtWache nicht am Messpfad, Allokationszähler fängt nur operator new)?
8. Thread-Konvention: welche Funktion trägt eine Konvention nur als Kommentar („nur Message-Thread", „nur Audio-Thread") ohne jassert, ohne Test; wo ruft der Editor oder ein Timer direkt in Audio-Zustand?
