<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->

# NAK-224 — S25c: Ursachenkern des Brokers verhaltensneutral aufteilen

| Merkmal | Wert |
|---|---|
| Ticket | NAK-224 (Register `docs/offene-punkte.md` Zeile 237), Planschritt **S25c** (`docs/plan/plan.json`), Prüfstufe **T2**. Pflegeschritt 1 nach G5, Muster S19b (NAK-121). Mitgeführt: **NAK-234** (`server_v3/mod.rs`), **NAK-238** (`broker/src/lib.rs`), **NAK-233** (toter Parameter `veraltet`). |
| Phase | **T1 PASS 2026-09-09 — gebaut in 29 Commits, Abschlusskanon GRÜN 62/62.** `hypothese.rs` (3 986 → `mod.rs` 72 plus elf Fachmodule), `server_v3/mod.rs` (2 169 → 212 plus sechs Testdateien) und `broker/src/lib.rs` (1 998 → 58 plus sieben Module) sind aufgeteilt; NAK-233 ist über die Doku geschlossen (NB-1), die vier Kommentare aus Punkt 5 sind wahr gemacht, `gesundheit.py` ist nachgezogen. Riegelkarte §3 vollständig gemessen; Verlauf §6. T2 setzt der Dirigent nach der Codex-Prüfung. |
| Prüfstufe | **T1 PASS 2026-09-09** (Selbstaudit des Erbauers je Commit, Riegelkarte R1–R10 vollständig, Abschlusskanon GRÜN 62/62 auf `3b902e4`). **Was T1 nicht sagt:** kein FL-Lauf; die Nebenbefunde NB-2 und NB-3 sind Testbefunde außerhalb des geänderten Verhaltens und ausdrücklich nicht behoben. T2 setzt der Dirigent nach der Codex-Prüfung. |
| Basis-SHA | `090954fa5814c1503a6f352e80c577cbeee0a246` (= `git rev-parse HEAD` beim Start, Worktree sauber, `git status --short` leer, HEAD = `origin/master`) |
| Kanon vorher | `a75bcee7` **GRÜN 62/62, Exit 0** (Abschlusskanon NAK-223, Rohausgabe `docs/beweise/roh/NAK-223-a75bcee.md`). `git diff --stat a75bcee7..090954fa -- broker eq-copilot tools` ist **leer** (in dieser Session gemessen, §3 R1) — der Lauf steht auf demselben Codestand wie der Basis-SHA. |
| Kanon nachher | `3b902e4` **GRÜN — 62/62 Kanonläufe bestanden, Exit 0** (abgekoppelt, sauberer Baum, Rohausgabe `docs/beweise/roh/NAK-224-3b902e4.md`). **Gleiche Beinzahl wie vorher (62).** A4 allein Exit 0 in 1.177,14 s, A4-SI Exit 0 in 12,82 s, A32 `[HINWEIS] Exit 4 – nicht blockierend` in 5,13 s. Nebenmeldungen wörtlich wie in jedem grünen Kanon: „2 geplante Prüfung(en) noch nicht gebaut“, „1 stillgelegte(s) Bein(e)“. **Zwei Läufe:** der erste auf `a87a5d9` war bereits GRÜN 62/62 (`docs/beweise/roh/NAK-224-a87a5d9.md`); danach wurde die A32-Behauptung im Runner nachgezogen (NB-6), und der zweite Lauf beglaubigt den Endstand samt dieser Zeile. |
| Testanzahl | **774 vorher wie nachher** (`cargo test --manifest-path broker/Cargo.toml -- --list`, 28 Testbinaries plus Doc-tests). Nach Abzug der Modulpräfixe ist die Namensmenge identisch — `docs/beweise/roh/NAK-224-testliste-diff.txt` ist leer. 0 Compilerwarnungen in `cargo build` und `cargo test --lib`; die 6 Warnungen zweier Testbinaries liegen in Dateien mit 0 geänderten Zeilen (§3 R4). |
| Ticketpfade | `broker/src/coordinator/hypothese.rs` (Verlauf: seit dem `git mv` das Verzeichnismodul `broker/src/coordinator/hypothese/`); `broker/src/transport/server_v3/`; `broker/src/lib.rs` plus die daraus herausgelösten neuen Module unter `broker/src/`; `tools/plan/gesundheit.py`; `docs/beweise/NAK-224.md`; `docs/beweise/roh/NAK-224-*` |
| Nicht angefasst (Grenze) | `broker/src/generiert/` (A9 hält es bytegleich zur Neuerzeugung), `broker/src/bin/`, `broker/src/server.rs`, `broker/tests/`, `eq-copilot/` vollständig, Schemas und Fixtures, `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md`, `.claude/`, `design/`. Ebenfalls nicht: **NAK-235** (keine lange Funktion wird zerlegt, `hypothesen` bleibt eine Funktion), **NAK-236** (Kommentar-Bezeichner außerhalb der drei Dateien), **NAK-237** (`// SAFETY:`-Arbeit, sicherheitsnah, eigener delegierter Schritt), `experiment_verdrahtung.rs`, `experiment.rs` (über Ziel, unter Grenze). |
| Änderungssatz | Ein einziger verhaltensneutraler Änderungssatz in **27 Commits**: (1) `git mv` von `hypothese.rs` nach `hypothese/mod.rs`, (2) je Fachbereich ein Herauslöse-Commit, (3) dasselbe für `server_v3/mod.rs` und `lib.rs`, (4) die Kommentarkorrekturen, (5) `gesundheit.py` (Pflegeticket-Liste und Ratschen auf den neuen Iststand). Beziehung `git mv` ↔ Herauslösen ↔ `pub use` in einem Satz; Ratsche ↔ Iststand ↔ Pflegeticket-Liste gemeinsam. |
| Prüfmodell | T1 durch den Erbauer (Selbstaudit je Commit, voller Kanon am Ende); Prüfung durch Codex (read-only) setzt der Dirigent an. |

---

## 1. Ticket- und Registerbezug, wörtlich

**Ticket** (`docs/plan/plan.json`, Schritt S25c, Ticket NAK-224, Stufe T2),
wörtlich:

> ANGELEGT 08.09.2026 (Pflegeschritt 1 nach G5, Register NAK-224).
> `broker/src/coordinator/hypothese.rs` trug am 08.09.2026 3 762 Zeilen, weil
> NAK-212 und NAK-213 dort gebaut haben. INHALT, ein einziger
> verhaltensneutraler Änderungssatz: die Datei nach Fachbereich teilen
> (Screening und Deckel, Zusammenhang und Rangkomponenten, Klassenwahl und
> Konkurrenzlage, Enthaltungen, Befundbau und Sicht), Historie erhalten
> (`git log --follow`), Modulköpfe wahr. BEWEIS: Kanon vorher = Kanon nachher
> grün mit gleicher Beinzahl, P5-Korpus bytegleich (A28/A29), Determinismus
> M-25 über hundert Läufe, `git diff --stat` ohne Zeile in Tests oder Fixtures
> außer Pfadänderungen. Muster: S19b (NAK-121). GRENZE: keine Regeländerung,
> keine neue Schwelle, kein Fassungsschritt; Nebenbefunde nummeriert ins
> Manifest, nie stumm behoben. NACHTRAG 08.09.2026 (Messung beim G5-Abschluss,
> Register NAK-234): `broker/src/transport/server_v3/mod.rs` liegt mit 2 007
> Zeilen ebenfalls über der Grenze und gehört in denselben Änderungssatz;
> generierte Dateien unter `broker/src/generiert/` sind keine Pflegefläche (A9
> hält sie bytegleich zur Neuerzeugung). Dazu NAK-233 (toter Parameter
> `veraltet` in `zustand_aus_sicherheit`), zu schließen in derselben
> Aufteilung, sofern die Zustandsabbildung M-29 unberührt bleibt.

**Register** (`docs/offene-punkte.md`), die vier Zeilen, die den Umfang tragen:

> **NAK-224** | 09-08, Pflegeschritt 1 nach G5 (Messung 08.09.2026) |
> **[Planarbeit · S25c]** **`broker/src/coordinator/hypothese.rs` (3 762
> Zeilen) verhaltensneutral nach Fachbereich aufteilen** — Screening und
> Deckel, Zusammenhang und Rangkomponenten, Klassenwahl und Konkurrenzlage,
> Enthaltungen, Befundbau und Sicht; Historie erhalten; Beweis Kanon vorher =
> nachher, Korpus bytegleich, M-25. Muster NAK-121/S19b. Wortlaut des Schritts
> in `docs/plan/plan.json` S25c.

> **NAK-233** | 09-08, NAK-214-Abschluss (NB-7, `docs/beweise/NAK-214.md` §4,
> gefunden in der Nacharbeit 1; Dirigent) | **[Härtung · Pflege · S25c]** **Der
> Parameter `veraltet` von `zustand_aus_sicherheit` ist tot.** `hypothese.rs`:786
> bildet `stale`, wenn `veraltet` wahr ist; alle vier Aufrufstellen (`:1776`,
> `:2011`, `:2056`, `:2179`) übergeben `false`, und `stale` entsteht produktiv
> nur in `befunde_veralten_locked` (`hypothese_verdrahtung.rs`:1020) und beim
> Teilverlust der Evidenzrücknahme (`:718`). Die Doku liest sich, als führe der
> Weg über den Parameter — diese Lesart trug den Testdefekt V-32. Eine
> Signaturänderung berührt die Zustandsabbildung M-29; gehört in die Aufteilung
> von `hypothese.rs` (NAK-224). Wortlaut NB-7

> **NAK-234** | 09-08, G5-Abschluss (Codebase-Messung des Dirigenten im
> Abschlussfenster, Skill §3.5, von Hand bis NAK-223 gebaut ist) |
> **[Planarbeit · S25c]** **`broker/src/transport/server_v3/mod.rs` liegt mit
> 2 007 Zeilen über der 2 000er-Grenze** und fehlt im Iststand von NAK-223
> (dort nur `hypothese.rs` und vier Plugin-Dateien). Gehört in den
> Broker-Pflegeschritt S25c neben `hypothese.rs` (3 762); Nachtrag im
> Schritttext `docs/plan/plan.json` S25c. Ausgenommen bleibt generierter Code:
> `broker/src/generiert/nakama_telemetry_v1_generated.rs` (2 325) ist Codegen
> aus dem .fbs und wird von A9 bytegleich zur Neuerzeugung gehalten (Nachtrag
> S25b). […]

> **NAK-238** | 09-09, NAK-223-Abschluss (N-4, §4; Dirigent) | **[Lücke ·
> Pflege · S25c/S25d]** **Sieben Dateien über dem Ziel 1 500 Zeilen**
> (Werkzeugzählung): `broker/src/lib.rs` 1 998 (zwei Zeilen unter der Grenze
> 2 000), `experiment_verdrahtung.rs` 1 892, `SourcesModel.cpp` 1 864,
> `broker/src/server.rs` 1 841, `experiment.rs` 1 727, `PluginEditor.cpp`
> 1 665, `PluginProcessor.h` 1 524. `lib.rs` reißt mit dem nächsten Zuwachs die
> Grenze und hätte dann kein Pflegeticket — **in S25c mitnehmen**

**Nicht Teil dieses Tickets** (Entscheid des Dirigenten, Technik, 09.09.2026):
**NAK-235** (28 Funktionen über 200 Zeilen — kein Zerlegen langer Funktionen,
`hypothesen` bleibt mit 267 Zeilen eine Funktion, keine Funktion wird länger),
**NAK-236** (Kommentar-Bezeichner außerhalb der drei Dateien) und **NAK-237**
(`// SAFETY:`-Begründungen — sicherheitsnah, eigener delegierter Schritt nach
der CLAUDE.md-Regel „Sicherheitsarbeit wird nie im Gesprächs- oder
Dirigentenkontext selbst ausgeführt").

---

## 2. Schnittplan

### 2.1 Iststand am Basis-SHA, selbst gemessen

`py -3.13 tools/plan/gesundheit.py` am Basis-SHA `090954fa`, Rohausgabe
`docs/beweise/roh/NAK-224-gesundheit-vorher.txt`:

| Maß | Ist | Ziel | Grenze | Status |
|---|---|---|---|---|
| Quelldateien über 2000 Zeilen | 5 | 0 | 0 | GRENZE |
| Quelldateien über Ziel 1500 Zeilen | 7 | 0 | — | ZIEL |
| Funktionen über 200 Zeilen | 28 | 0 | 28 | ZIEL |
| Clippy-Fundstellen (broker) | 91 | 0 | 91 | ZIEL |
| allow(dead_code) ohne Aufrufer | 0 | 0 | 0 | OK |
| Kommentar-Bezeichner ohne Code | 35 | 0 | 35 | ZIEL |

Die drei Ticketdateien, `wc -l` in dieser Session:

| Datei | Zeilen gesamt | Produktcode | Inline-Testmodul |
|---|---|---|---|
| `broker/src/coordinator/hypothese.rs` | 3 986 | 1–2 593 | `mod tests` ab 2 594 |
| `broker/src/transport/server_v3/mod.rs` | 2 169 | 1–203 | `mod tests` ab 204 |
| `broker/src/lib.rs` | 1 998 | 1–1 290 | `mod register_tests` ab 1 291 |

### 2.2 Die Regel, nach der geschnitten wurde

**Ein Modul trägt genau die Rechnung eines Fachbereichs und die Regel, die
diese Rechnung bindet.** Daraus folgen drei Entscheidungen:

1. **Die Kopfzeilen werden verteilt, nicht kopiert.** Die drei harten Regeln
   des Ursprungskopfs stehen dort, wo die Rechnung liegt, die sie bindet:
   **M-20** (Gate vor Gewichtung) in `screening.rs`, **M-17** (aus paralleler
   Telemetrie nie Klasse 2 oder 3) in `befund.rs` bei `baue_befund`, **M-25**
   (gleicher Eingang, gleiche Reihenfolge) in `determinismus.rs` bei den vier
   benannten Größen, **M-87** (jeder Ausgeschiedene trägt einen Grund) in
   `vertrag.rs` bei `Ausschlussgrund`. `mod.rs` behält nur den Satz, der für
   das **ganze** Modul gilt: E-08 (wer hier rechnet), die Reinheit der Rechnung
   und das Risiko **R1** (ein Test, der nur dieses Modul ruft, kann nicht
   fallen, wenn die Verdrahtung fehlt).
2. **Die Vertragstypen bilden ein eigenes Modul** (`vertrag.rs`) und bleiben
   nicht in `mod.rs`. Begründung: die sechs geschlossenen Mengen
   (`Ursachenklasse`, `Aussageklasse`, `Zielmetrik`, `Sicherheitsklasse`,
   `NaechsterTest`, `Ausschlussgrund`) sind ein eigener Fachbereich mit eigener
   Zusage — sie sind die Rust-Hälfte des eingefrorenen Schemas (M-77), und
   genau zwei Tests messen sie beidseitig. In `mod.rs` stünden sie neben
   Moduldeklarationen und `pub use` und hätten dort keinen Kopf, der sie
   bindet. Die **Ausgangsformen** dagegen gehen zu der Rechnung, die sie füllt:
   `Rangkomponenten` und `Zusammenhangsbeleg` nach `zusammenhang.rs`,
   `Befundkonfidenz`/`Befundzustand` nach `klassenwahl.rs`,
   `CauseHypothesis`/`Bandintervall`/`Beobachtung`/`Ausschluss` nach
   `befund.rs`.
3. **`mod.rs` exportiert je Untermodul mit einem Glob-`pub use`.** Ein
   Glob-Re-Export ist hier die einzige Form, die die Zusage „jeder heute unter
   `crate::coordinator::hypothese::…` erreichbare Pfad bleibt unter demselben
   Pfad erreichbar" **vollständig** hält, ohne eine handgeführte Liste, die
   beim nächsten `pub fn` still veraltet. Er hebt außerdem keine Sichtbarkeit
   an: ein `pub(super)`-Item wird durch ihn nicht öffentlich (Rust kappt die
   Re-Export-Sichtbarkeit auf das Minimum aus Item und `use`).

### 2.3 `hypothese.rs` → `hypothese/` — Zielmodule

| Zielmodul | Fachbereich | Modulkopf bindet |
|---|---|---|
| `mod.rs` | Moduldeklarationen, Glob-`pub use`, Ort der Rechnung | E-08, Reinheit, R1 |
| `vertrag.rs` | Die geschlossenen Mengen des Produzenten | M-14, M-77, M-87 |
| `determinismus.rs` | Die Größen der `metrics_version` und `quantisiert` | M-25, §36.4, E-12 |
| `eingang.rs` | Was der Rechner über eine Quelle weiß | M-07, M-86, NAK-213 E7/R4 |
| `messung.rs` | Kleines, reines, einzeln messbares Rechenwerkzeug | M-07, M-19 |
| `screening.rs` | Stufe A: Screening, Deckel und die harten Gates | M-18, M-20, M-24, K-01/K-07 |
| `zusammenhang.rs` | Stufe B: die sechs Rangkomponenten und der Beleg | M-19, M-25, NAK-212 R1/R2/E6 |
| `klassenwahl.rs` | Konkurrenzlage, Sicherheitsklasse → Zustand | M-15, M-29, M-30, M-31, U21 |
| `befund.rs` | Befundbau und Sicht: `CauseHypothesis`, `baue_befund` | M-16, M-17, M-25 (`finding_id`) |
| `enthaltung.rs` | Die Enthaltung mit und ohne Ort | M-27, R4, R8 |
| `rechnung.rs` | Der Ablauf: `hypothesen`, `masteranomalie` | M-18, M-86, NAK-213 E7 |
| `testhilfe.rs` | `#[cfg(test)]` — die geteilten Testbauer | — |

### 2.4 `hypothese.rs` — Item für Item

**Produktcode.** (P = bleibt privat, P→S = Sichtbarkeit geändert, siehe §2.7)

| Item alt | Typ | Zielmodul |
|---|---|---|
| `Ursachenklasse`, `URSACHENKLASSEN`, `impl` | Enum, Konstante | `vertrag.rs` |
| `Aussageklasse`, `AUSSAGEKLASSEN`, `impl` | Enum, Konstante | `vertrag.rs` |
| `Zielmetrik`, `ZIELMETRIKEN`, `impl` | Enum, Konstante | `vertrag.rs` |
| `Sicherheitsklasse`, `SICHERHEITSKLASSEN`, `impl` | Enum, Konstante | `vertrag.rs` |
| `NaechsterTest`, `NAECHSTE_TESTS`, `impl` | Enum, Konstante | `vertrag.rs` |
| `Ausschlussgrund`, `AUSSCHLUSSGRUENDE`, `impl` | Enum, Konstante | `vertrag.rs` |
| `BOOTSTRAP_SAAT`, `RANG_QUANTUM`, `TIE_BREAK_KEY`, `RANGKOMPONENTEN` | Konstante | `determinismus.rs` |
| `ROLLENPRIORITAET`, `PRIORITAET_OHNE_ROLLE`, `KANDIDATEN_DECKEL` | Konstante | `determinismus.rs` |
| `BAENDER_FEIN`, `GRUPPEN_LIVE` | Konstante | `determinismus.rs` |
| `quantisiert` | Funktion | `determinismus.rs` |
| `Evidenzfenster` + `impl band` | Struct | `eingang.rs` |
| `Quellprofil`, `Passagenfenster`, `Aufnahme` | Struct | `eingang.rs` |
| `mittel`, `median`, `spannweite`, `bandmittel` (P→S) | Funktion | `messung.rs` |
| `spanne`, `ueberlappt`, `paarueberdeckung`, `zeitueberdeckung` (P→S) | Funktion | `messung.rs` |
| `fenster_ganz_in_passage`, `gemeinsame_reihen`, `korrelation_gerichtet` (P→S) | Funktion | `messung.rs` |
| `fenster_in_passage`, `passagenmaterial`, `unabhaengige_fenster` | Funktion (pub) | `messung.rs` |
| `Gateurteil` | Enum | `screening.rs` |
| `gruppe_von_band`, `bandintervall_der_gruppe` | Funktion (pub) | `screening.rs` |
| `Screeningrang` + `impl`, `screeningrang`, `screening_quantisiert` | Struct, Funktion | `screening.rs` |
| `STUFE_B_AUFRUFE`, `STUFE_B_MAX` (thread_local) | Statik | `screening.rs` |
| `stufe_b_zaehler_zuruecksetzen`, `stufe_b_max_je_rechnung`, `stufe_b_aufrufe` | Funktion (pub) | `screening.rs` |
| `gate` | Funktion (pub) | `screening.rs` |
| `Rangkomponenten` + `impl` (`werte`, `rang`) | Struct | `zusammenhang.rs` |
| `Zusammenhangsbeleg` + `impl`, `zusammenhang_verschieden` | Struct, Funktion | `zusammenhang.rs` |
| `rangkomponenten`, `rang_und_beleg`, `zusammenhang` | Funktion (pub) | `zusammenhang.rs` |
| `bandpassung`, `koinzidenz_gerichtet`, `uplift_gerichtet`, `upliftreihe` (P) | Funktion | `zusammenhang.rs` |
| `intent_relevanz`, `wiederholbarkeit`, `routingqualitaet` (P) | Funktion | `zusammenhang.rs` |
| `getrennt`, `rang_quantisiert` | Funktion (pub) | `zusammenhang.rs` |
| `Befundkonfidenz` | Struct | `klassenwahl.rs` |
| `Befundzustand`, `BEFUNDZUSTAENDE`, `impl`, `zustand_aus_sicherheit` | Enum, Funktion | `klassenwahl.rs` |
| `Konkurrenzlage`, `konkurrenzlage` | Enum, Funktion | `klassenwahl.rs` |
| `Bandintervall` + `impl`, `Beobachtung`, `Ausschluss` | Struct | `befund.rs` |
| `CauseHypothesis`, `ERGEBNISTEILE`, `impl` (`sechs_teile`, `tie_break`) | Struct | `befund.rs` |
| `baue_befund` (P→S), `kurz` (P→S) | Funktion | `befund.rs` |
| `finding_id` | Funktion (pub) | `befund.rs` |
| `Enthaltungsgrund` + `impl` | Enum | `enthaltung.rs` |
| `enthaltung_ohne_ort`, `enthaltung` (P→S) | Funktion | `enthaltung.rs` |
| `Rechenergebnis`, `hypothesen` | Struct, Funktion (pub) | `rechnung.rs` |
| `masteranomalie` | Funktion (pub) | `rechnung.rs` |

**Testmodul** (ab Zeile 2 594). Jede Testfunktion geht in das
`#[cfg(test)] mod tests` des Moduls, dessen Funktion sie misst; die Körper
bleiben textgleich bis auf `use`-Zeilen.

| Testfunktion | misst | Zielmodul |
|---|---|---|
| `quantisierung_klemmt_rastet_und_ist_nan_ehrlich` | `quantisiert` | `determinismus.rs` |
| `geschlossene_mengen_sind_rund` | die sechs Mengen | `vertrag.rs` |
| `band_ohne_bit_traegt_keinen_wert` | `Evidenzfenster::band` | `eingang.rs` |
| `korrelation_ist_streng` | `korrelation_gerichtet` | `messung.rs` |
| `korrelation_gerichtet_trennt_gemessene_null_von_nicht_messbar` | `korrelation_gerichtet` | `messung.rs` |
| `spanne_saettigt_an_den_raendern` | `spanne` | `messung.rs` |
| `alignment_misst_paarweise_ueberlappung` | `paarueberdeckung`, `zeitueberdeckung` | `messung.rs` |
| `passagenmaterial_zaehlt_nur_ganz_innenliegende_fenster` | `passagenmaterial` | `messung.rs` |
| `unabhaengige_fenster_verdichten_wiederholungen` | `unabhaengige_fenster` | `messung.rs` |
| `screeningrang_misst_auf_der_gruppe` | `screeningrang` | `screening.rs` |
| `screeningrang_traegt_die_koinzidenz` | `screeningrang` | `screening.rs` |
| `stufe_b_laeuft_hoechstens_fuenfmal` | Deckel und Zähler (K-01/K-07) | `screening.rs` |
| `bandraender_fallen_von_beiden_seiten` | `gruppe_von_band`, `bandintervall_der_gruppe` | `screening.rs` |
| `gate_faellt_in_der_zugesagten_reihenfolge` | `gate` | `screening.rs` |
| `passagengates_trennen_verschoben_anders_und_zu_kurz` | `gate` | `screening.rs` |
| `getrennt_verlangt_rang_und_zusammenhang` | `getrennt` | `zusammenhang.rs` |
| `belegpraedikate_lesen_vorzeichen` | `Zusammenhangsbeleg` | `zusammenhang.rs` |
| `gemessene_null_trennt_vom_nicht_gemessenen` | `zusammenhang_verschieden` | `zusammenhang.rs` |
| `bootstrap_einseitig_trennt_die_richtung` | `wiederholbarkeit` | `zusammenhang.rs` |
| `rang_ist_das_quantisierte_mittel_der_sechs` | `Rangkomponenten::rang` | `zusammenhang.rs` |
| `messgruende_sind_genau_die_fuenf_aus_r2` | `ist_messgrund` und `konkurrenzlage` | `klassenwahl.rs` |
| `finding_id_ist_deterministisch_und_trennt` | `finding_id` | `befund.rs` |
| `sechs_teile_sind_sechs_und_benannt` | `sechs_teile` | `befund.rs` |
| `einzelueberlebender_neben_passagenausschluss` | `hypothesen` | `rechnung.rs` |
| `ohne_vollstaendigkeitsmarke_rechnet_das_modul_nicht` | `hypothesen` (M-86) | `rechnung.rs` |

**Geteilte Testbauer** → `testhilfe.rs` (`#[cfg(test)]`, `pub(super)`):
`fenster`, `rang`, `beleg`, `quelle`, `quelle_ab`, `aufnahme_mit`,
`masterprofil`. Sie wandern **wörtlich**; nur ihre Sichtbarkeit wird
`pub(super)`, damit die Tests der Geschwistermodule sie erreichen, ohne dass
ein Körper sich ändert. Konstanten und Zähler wie `stufe_b_*` bleiben bei ihrem
Fachbereich (`screening.rs`).

### 2.5 `server_v3/mod.rs`

Die 203 Zeilen Produktcode bleiben unverändert in `mod.rs` (Konstanten
`MAX_VERBINDUNGEN`, `PIPE_INSTANZEN`, `BOOTSTRAP_FRIST`, `RATE_PRO_SEKUNDE`,
`CAP_WRITER`, `SENKE_FRIST`, `WACHHUND_TAKT`, `FLUSH_FRIST`, die drei
`*_SCHEMA_MINOR`, `schema_minor_bekannt`, die Moduldeklarationen und die
`pub use`-Blöcke). Geteilt wird ausschließlich das Inline-Testmodul.

**Form:** Testdateien **direkt im Modulverzeichnis** als
`server_v3/tests_<bereich>.rs`, eingebunden mit
`#[cfg(test)] mod tests_<bereich>;`. Begründung, aus dem Code abgeleitet: der
Test `bootstrap_und_senkenfrist_minus_eins_und_exakt_sind_inklusive` liest mit
`include_str!("listener.rs")` und `include_str!("griff.rs")` Nachbardateien,
und `broker_p2_push_…` liest mit `include_str!` zwei Schemadateien über einen
relativen Pfad. `include_str!` löst **relativ zur enthaltenden Datei** auf —
eine Testdatei in einem Unterverzeichnis `server_v3/tests/` würde jeden dieser
Pfade um ein `../` verändern und damit einen Testkörper anfassen. Die flache
Form hält alle Pfade byte-gleich.

| Zieldatei | Fachbereich |
|---|---|
| `tests_hilfe.rs` | `FOLGE`, `probe_pipe`, `Testclient` + `impl` + `Drop`, `praefix`, `adresse_json`, `control_hello`, `test_adresse`, `control_hello_adresse`, `control_hello_fach`, `subscribe`, `heartbeat_fuer_adresse`, `vollstaendiger_heartbeat`, `telemetry_hello`, `telemetry_hello_adresse`, `welcome_lesen`, `p0`, `p1`, `p2`, `warte_auf`, `frame_json_lesen`, `frame_roh_lesen` |
| `tests_fristen.rs` | Fristen und Fassungen: `bootstrap_und_senkenfrist_…`, `transportfassung_und_json_leser_stimmen_ueberein`, `unbekannter_schema_minor_wird_vor_der_senke_abgewiesen`, `p0_auf_der_telemetriepipe_wird_abgewiesen`, `p2_auf_der_controlpipe_wird_abgewiesen` |
| `tests_rueckstau.rs` | Rückstau und Prioritätsklassen: `BlockSenke`, `P1BlockSenke`, `blockierende_senke_haelt_den_leser_nicht_auf`, `p0_ueberlauf_trennt_die_verbindung`, `p0_wird_beantwortet_waehrend_p1_die_senke_blockiert`, `geschlossener_eingang_liefert_nichts_mehr` |
| `tests_lebenszyklus.rs` | Start, Stopp, Ernte: `LebenszyklusBlockSenke`, `control_ende_beendet_die_telemetrie`, `acceptor_ueberlebt_die_verbindungsgrenze`, `beendete_verbindungen_werden_geerntet`, `stop_im_fenster_vor_der_bedienung_haengt_nicht`, `stoppen_endet_auch_bei_haengender_senke`, `stoppen_endet_auch_bei_haengendem_lebenszyklusaufruf`, `kopplung_faellt_mit_dem_leserende_nicht_erst_nach_den_joins` |
| `tests_kopplung.rs` | Kopplung und Trennreihenfolge: `ReihenfolgeSenke`, `welcome_folgt_dem_abgeschlossenen_control_verbunden`, `connect_callbacks_je_kopplung_genau_einmal`, `trennreihenfolge_je_callback_genau_einmal`, `abgeloestes_telemetrie_getrennt_haelt_control_getrennt_nicht_auf`, `telemetrie_endet_zuerst_control_getrennt_folgt_trotzdem`, `trennreihenfolge_haelt_im_kopplungs_callback_fenster`, `abgeloestes_telemetrie_gekoppelt_laesst_control_getrennt_nicht_warten` |
| `tests_abonnement.rs` | Abonnement und Übergabe an den Coordinator: `subscription_ist_an_eigenen_control_link_gebunden`, `join_kandidat_laeuft_ueber_probe_pipe_ohne_join_candidate_nachrichtenfamilie`, `io_worker_uebergibt_validierte_bytes_ausschliesslich_an_den_coordinator`, `broker_p2_push_nutzt_subscriber_telemetriepipe_und_aktive_minors`, `subscription_cleanup_vor_weiterem_push` |

Die Testdateien sind Geschwister von `mod.rs` innerhalb `server_v3` und sehen
dessen private Items unverändert über `use super::*`. Herkunft belegt die
Commitfolge aus `mod.rs` heraus; eine reine Umbenennung gibt es hier nicht.

### 2.6 `broker/src/lib.rs`

`lib.rs` bleibt Crate-Wurzel und wird dünn: Crate-Doku, `pub mod`- und
`mod`-Deklarationen, `pub use`. Jeder heute unter `eqcop_broker::<Name>`
erreichbare Pfad bleibt über `pub use` bestehen — Ziel **0 geänderte Zeilen**
unter `broker/src/bin/`, `broker/src/server.rs` und `broker/tests/`.

| Zielmodul | Fachbereich |
|---|---|
| `broker/src/zeit.rs` | `jetzt_ms`, `MONOTONER_START`, `monoton_ms`, `Zeitstempel` + `impl` |
| `broker/src/register.rs` | `STALE_MS`, `MAX_MARKIERUNGS_NONCES_PRO_SENSOR`, `VerbindungsMetadaten`, `SensorEintrag`, `hello_metadaten_uebernehmen`, `Register` + `impl` |
| `broker/src/sitzung.rs` | `SessionInfo`, `sessions_bilden` |
| `broker/src/paar.rs` | `PaarStatus`, `hoermarkierungs_sperrgrund`, `aggregat_sperrgrund`, `paare_auswerten` |
| `broker/src/lebenslauf.rs` | `PIPE_NAME`, `BROKER_PRO_USER_MAX`, `BROKER_IDLE_ENDE_MS`, `SENSOR_ID_RUECKGABE_MS`, `BrokerStatus`, `BrokerSupervisor`, `BrokerLauf`, `BROKER`, `broker_version`, `broker_starten`, `broker_geordnet_stoppen`, `broker_idle_ende_erreicht`, `broker_idle_aktualisieren`, `broker_soll_idle_enden`, `broker_hat_fatalen_v3_listenerfehler`, `broker_store_sicht`, `broker_status`, `status_ohne_lauf` |
| `broker/src/probe.rs` | `probe_lauf`, `profil_binden`, `aggregat_schreiben` |
| `broker/src/testhilfe.rs` | `#[cfg(test)]` — `hello`, `messbereit`, `fenster` |

Das Inline-Testmodul `register_tests` (ab 1 291) wandert mit seinen
Funktionen: die Registertests nach `register.rs`,
`zwei_fl_prozesse_bleiben_getrennte_sessions` nach `sitzung.rs`, die acht
`paar_*`-Tests samt `paar_basis` nach `paar.rs`.
`stale_nutzt_monotone_zeit_und_ignoriert_wallclock_spruenge` misst das Register
über die monotone Uhr und bleibt bei `register.rs`.

### 2.7 Sichtbarkeiten — jede Änderung einzeln

Kein `pub` wird breiter. Geändert wird ausschließlich von **modulprivat** auf
**`pub(super)`** oder **`pub(crate)`**, weil die Rechnung, die den Helfer ruft,
nach dem Schnitt in einem Geschwistermodul liegt. `pub(super)` in
`hypothese::<untermodul>` bedeutet „sichtbar in `hypothese` und allen
Nachkommen" — also genau der Umfang, den der Helfer vorher als dateiprivates
Item hatte.

| Item | vorher | nachher | warum |
|---|---|---|---|
| `mittel`, `median`, `spannweite`, `bandmittel`, `spanne`, `ueberlappt`, `paarueberdeckung`, `zeitueberdeckung`, `fenster_ganz_in_passage`, `gemeinsame_reihen`, `korrelation_gerichtet` | privat | `pub(super)` | Ruf aus `screening.rs`, `zusammenhang.rs`, `rechnung.rs`, `befund.rs` |
| `baue_befund`, `kurz` | privat | `pub(super)` | Ruf aus `rechnung.rs` bzw. `enthaltung.rs` |
| `enthaltung_ohne_ort`, `enthaltung` | privat | `pub(super)` | Ruf aus `rechnung.rs` |
| `Zusammenhangsbeleg::vergleichsform` | privat | bleibt privat | Ruf nur aus `zusammenhang_verschieden`, gleiches Modul |
| geteilte Testbauer in `hypothese/testhilfe.rs` und `broker/src/testhilfe.rs` | privat | `pub(super)` / `pub(crate)`, `#[cfg(test)]` | Ruf aus den Testmodulen der Geschwister |
| geteilte Testbauer in `server_v3/tests_hilfe.rs` | privat | `pub(super)`, `#[cfg(test)]` | Ruf aus den Geschwister-Testdateien |
| `lib.rs`-Helfer `hoermarkierungs_sperrgrund`, `aggregat_sperrgrund`, `monoton_ms`, `hello_metadaten_uebernehmen`, `broker_version`, `status_ohne_lauf`, `MONOTONER_START`, `BROKER`, `Zeitstempel`, `VerbindungsMetadaten`, `BrokerSupervisor`, `BrokerLauf`, `MAX_MARKIERUNGS_NONCES_PRO_SENSOR` | privat (Crate-Wurzel) | `pub(crate)` | dieselbe Reichweite: ein privates Item der Crate-Wurzel war schon bisher aus keinem anderen Crate erreichbar |

**Gemessen am Endstand, nicht behauptet.** Zwei Abgleiche über alle
`broker/src/**/*.rs` zwischen Basis-SHA und Endstand:

* **Die öffentliche Fläche ist unverändert.** Die Menge aller `pub fn`,
  `pub struct`, `pub enum`, `pub const`, `pub static`, `pub type`, `pub trait`
  und `pub mod` zählt vorher wie nachher **1 098** Einträge, und die Differenz
  in **beide** Richtungen ist leer — kein Item ist öffentlich geworden, keines
  verschwunden. Rohlisten `docs/beweise/roh/NAK-224-pub-vorher.txt` und
  `-nachher.txt`.
* **Neu eingeschränkt sichtbar sind genau 68 Items**, davon 11 `pub(crate)` und
  57 `pub(super)`; die Liste steht in
  `docs/beweise/roh/NAK-224-sichtbarkeiten.txt`. Der weit überwiegende Teil sind
  die Testbauer aus den drei `testhilfe`-Modulen, die nur unter `cfg(test)`
  gebaut werden. Jeder Produktivfall ist oben in der Tabelle genannt; hinzu
  kamen beim Bauen `Zielmetrik::braucht`, die zwei `STUFE_B_*`-Zähler und die
  drei Stufe-B-Helfer (§6).

Die tatsächlich gesetzten Sichtbarkeiten sind damit gegen diese Tabelle geprüft;
jede Abweichung steht in §6 mit Begründung.

### 2.8 Erwartete Größen

Kein neues Modul soll das Ziel **1 500 Zeilen** reißen. Erwartung nach dem
Schnitt: „Quelldateien über 2000 Zeilen" = **3** (nur die drei
NAK-225-Dateien). Die gemessenen Größen stehen in §3 R9.

---

## 3. Riegelkarte

| Riegel | Befehl | Vorher (Basis `090954fa`) | Nachher (Endstand) |
|---|---|---|---|
| **R1** Kanon | `tools/beweise.ps1` | GRÜN 62/62 auf `a75bcee7`; `git diff --stat a75bcee7..090954fa -- broker eq-copilot tools` **leer** (gemessen 09.09.2026) | **GRÜN 62/62, Exit 0** auf `3b902e4`, abgekoppelt, sauberer Baum — **gleiche Beinzahl**. Davor derselbe Stand ohne die Runner-Korrektur: GRÜN 62/62 auf `a87a5d9` |
| **R2** P5-Korpus | A28 `erzeuge_p5_korpus.py --pruefen`, A29 `pruefe_p5_korpus.py`, SHA-256 von `eq-copilot/build/p5-korpus-ergebnis.json` | Ergebnisdatei frisch aus `cargo test --test sonde014_p5_korpus` (1 passed, 32,36 s), SHA-256 `a013da9a8806cb5fcd9dfa40ed34d3a8bf023ee000b1d0aa9b00f1555aa6d056` | Ergebnisdatei erneut frisch erzeugt (1 passed, 30,73 s), SHA-256 **`a013da9a8806cb5fcd9dfa40ed34d3a8bf023ee000b1d0aa9b00f1555aa6d056` — bytegleich**. A28 meldet „P5-Korpus: 2 Dateien, 27 Sitzungen“ und „bytegleich“, Exit 0; A29 meldet „P5-Korpus: gruen“, Exit 0, darin M-69 („auf jedem Gegenbeispiel unsicher HAELT“) und der unveränderte `GATE_MINDEST_FENSTER`-Startwert 8 |
| **R3** Determinismus M-25 | `cargo test --test sonde014_hypothese ranking_ist_bytegleich_ueber_hundert_laeufe` und `screening_ist_deterministisch` | beide **ok** — je „1 passed; 0 failed“, 40,72 s bzw. 156,51 s | beide **ok** — je „1 passed; 0 failed“, 37,25 s bzw. 139,40 s |
| **R4** Testbestand | `cargo test --manifest-path broker/Cargo.toml -- --list` | **774** Tests über 28 Binaries; Rohliste `docs/beweise/roh/NAK-224-testliste-vorher.txt` | **774**, gleiche Zahl; Rohliste `docs/beweise/roh/NAK-224-testliste-nachher.txt`. Nach Abzug der Modulpräfixe ist die Namensmenge **identisch**: `docs/beweise/roh/NAK-224-testliste-diff.txt` ist **leer (0 Bytes)**. `cargo build` und `cargo test --lib`: **0 Warnungen**; die 6 Warnungen der Testbinaries `sonde013_verdrahtung` und `sonde014_gegenbeispiele` liegen in Dateien mit **0 geänderten Zeilen** in diesem Ticket, sind also unverändert vorbestehend |
| **R5** Diff-Riegel | `git diff --stat 090954fa..HEAD` | — | `eq-copilot/` **0**, `broker/src/generiert/` **0**, Schemas und Fixtures **0**. `broker/tests/` **1 Zeile**: `broker_idle.rs:189` `include_str!("../src/lib.rs")` → `include_str!("../src/lebenslauf.rs")` — eine Pfadänderung, weil der dort gemessene Produkttext (`broker_geordnet_stoppen`) nach dem Schnitt in `lebenslauf.rs` steht; die Assertions selbst sind unverändert. Gesamt 34 Dateien, +9 939/−7 939 |
| **R6** Historie | `git log --follow --oneline -- broker/src/coordinator/hypothese/mod.rs` | — | reicht bis **`5ceff037`** „SONDE-014 Etappe C, Teil 1: Ursachenhypothese im Broker“ — mit `--diff-filter=A` genau der Commit, der `hypothese.rs` angelegt hat. Der `git mv` steht als eigener Commit `32d67a38` mit **100 % Ähnlichkeit** (`rename broker/src/coordinator/{hypothese.rs => hypothese/mod.rs} (100%)`). `git log --oneline 090954fa..HEAD` zeigt **27 Commits**, je herausgelöstem Modul einen |
| **R7** Modulköpfe | Sichtprüfung je Modul gegen den Inhalt | — | **25 neue Dateien, 25 `//!`-Köpfe**, keiner leer und keiner kopiert (Liste §6). Die Regelverteilung ist geprüft: M-20 und M-87/NAK-213 in `screening.rs`, M-17 und M-16 in `befund.rs`, M-25 in `determinismus.rs`, M-77/M-87 in `vertrag.rs`, M-07/M-86 in `eingang.rs`, M-15/M-29/M-30 in `klassenwahl.rs`, M-27/R4/R8 in `enthaltung.rs`, M-18/M-86 in `rechnung.rs`; `hypothese/mod.rs` behält E-08 und Risiko R1 und verweist für jede Regel auf ihr Modul |
| **R8** NAK-233 | `broker/tests/sonde014_befund.rs` unverändert und grün (M-29) | Zweig `veraltet = true` gemessen in `sonde014_befund.rs:259` | Signatur `zustand_aus_sicherheit(klasse, veraltet)` **unverändert**; `git diff` über `broker/tests/sonde014_befund.rs` ist **leer**; `cargo test --test sonde014_befund` **7 passed**. Die Doku nennt jetzt die zwei produktiven `stale`-Wege (`befunde_invalidieren_locked`, `befunde_veralten_locked`) und dass alle vier Aufrufe `false` übergeben — NB-1 |
| **R9** gesundheit.py | `py -3.13 tools/plan/gesundheit.py [--clippy]` | über 2000: **5**; über 1500: **7**; Funktionen über 200: **28**; Clippy: **91**; Kommentar-Bezeichner: **35**; dead_code: **0** | über 2000: **3** (nur die drei NAK-225-Dateien, Erwartung erfüllt); über 1500: **6** (`lib.rs` ist fort); Funktionen über 200: **28**; Clippy: **91**; Kommentar-Bezeichner: **32**; dead_code: **0**. Pflegeticket-Liste wahr (NAK-224 und NAK-234 entfernt), Ratsche `KOMMENTAR_GRENZE` 35 → **32 gesenkt**, keine erhöht. Selbsttest **116/116**. Zwei Läufe hintereinander byteidentisch. Größtes neues Modul **996 Zeilen** (`register.rs`) — keines über dem Ziel 1 500 |
| **R10** Rundenbilanz-Lesbarkeit | je Commit `cargo build` und gezielte `cargo test` | — | **26 Bau-Commits, jeder grün.** Je Commit: `cargo build` ohne Warnung, `cargo test --lib` 245 passed und — für die `hypothese`-Commits — `sonde014_hypothese` 35 passed (die zwei Determinismusläufe übersprungen, sie liefen am Ende in voller Länge) sowie `sonde014_befund` 7. Am Ende jeder der drei Dateien der volle Satz: `sonde014_hypothese` 37, `sonde014_befund` 7, `sonde014_verdrahtung` 57, `coordinator_model` 51, `broker_idle` 5, `security_vectors` 23, `transport_fuzz` 12. Rundenbilanz `090954fa..HEAD`: Produkt 29 Dateien +8 527/−7 931, Tests 1 Datei +1/−1, Prüfwerkzeug 1 Datei +17/−7, Doku 3 Dateien +1 394/−0 |

---

## 4. Prüfliste (`tools/dirigent/pruefliste.md`)

Die Liste ist für diesen Schritt in weiten Teilen **nicht anwendbar**: NAK-224
verschiebt Code und ändert kein Verhalten, keinen Vertrag und keine Zusage. Wo
eine Zeile trotzdem greift, steht hier, wo sie gemessen ist.

| Zeile | Stand | Wo gemessen |
|---|---|---|
| **A. Rückstau und Prioritätsklassen** | unberührt | Die Politik je Klasse steht unverändert in `queues.rs` und `verbindung.rs`; die Fälle, die sie messen, sind wörtlich nach `server_v3/tests_rueckstau.rs` verschoben und dort grün (§3 R10). `git diff` über `queues.rs` und `verbindung.rs` ist leer. |
| **B. Lebenszyklus** | unberührt | Dieselbe Lage: die Fälle liegen jetzt in `server_v3/tests_lebenszyklus.rs` und `tests_kopplung.rs`, die Reihenfolge- und Fristenlogik in `trennung.rs`, `griff.rs`, `listener.rs` ist nicht angefasst. `broker_idle` 5 passed, `security_vectors` 23 passed. |
| **C. Verträge und Längen** | unberührt | Kein Schema, kein Fixture, keine Wireform: §3 R5 misst 0 geänderte Zeilen unter `eq-copilot/schemas/`, `eq-copilot/fixtures/` und `broker/src/generiert/`. Die geschlossenen Mengen sind wörtlich verschoben; `geschlossene_mengen_sind_rund` und `geschlossene_mengen_der_fassung_3_stehen_je_einmal_im_vertrag` sind beide grün. |
| **D. Bau- und Prüfriegel** | greift | `tools/plan/gesundheit.py` ist angefasst — aber nur Ratsche und Pflegeticket-Liste, keine Messlogik. Der Selbsttest bleibt bei **116/116**; die Ratsche wurde **gesenkt**, nie erhöht, und ein Treffer ohne Ticket bleibt sichtbar (§3 R9). |
| **E. Behauptung ≤ Messung** | greift | Jede Zahl in diesem Manifest stammt aus einem Lauf dieser Session; die Rohausgaben liegen unter `docs/beweise/roh/NAK-224-*`. Der Nebenbefund NB-1 ist **nicht stumm behoben**, sondern mit Zeilenbeleg geführt. Drei Kommentare, die mehr behaupteten als der Code hergibt, sind auf den wahren Namen gesetzt (NB-3). |
| **F. Änderungssatz** | greift | Die Beziehung `git mv` ↔ Herauslösen ↔ `pub use` liegt in **einem** Änderungssatz (27 Commits, ein Ticket). Ratsche ↔ Iststand ↔ Pflegeticket-Liste sind **gemeinsam** in einem Commit nachgezogen. Jede Testverschiebung reist mit der Funktion, die sie misst. |
| **E, Zeile „jede neue Prüfung einmal gebrochen"** | nicht anwendbar | Dieses Ticket legt **keine** neue Prüfung an. Der Beweis ist die Gleichheit vorher/nachher: gleiche Testzahl, gleiche Namensmenge, bytegleicher Korpus (§3 R2 bis R4). |

---

## 5. Nebenbefunde

**NB-1 — NAK-233: der Parameter `veraltet` ist im Produktpfad tot, die
Signatur bleibt trotzdem.** `zustand_aus_sicherheit(klasse, veraltet)` bildet
`Befundzustand::Stale`, wenn `veraltet` wahr ist. Alle vier Produktivaufrufe
übergeben `false`. Produktiv entsteht `stale` auf zwei anderen Wegen:
`befunde_veralten_locked` in `hypothese_verdrahtung.rs` und der Teilverlust der
Evidenzrücknahme in derselben Datei. **Der Zweig `veraltet = true` ist aber
gemessen:** `broker/tests/sonde014_befund.rs:259` prüft ihn als Teil der
Zustandsabbildung M-29. Eine Signaturänderung bräuchte damit eine Änderung an
einer Testzeile, und der BEWEIS-Satz dieses Tickets schließt das aus („`git
diff --stat` ohne Zeile in Tests oder Fixtures außer Pfadänderungen").
**Entscheid:** die Signatur bleibt unverändert; die Doku der Funktion wird wahr
gemacht (der produktive Weg zu `stale` steht dort mit seinen Aufrufstellen).
Die Signaturänderung braucht einen eigenen Schritt mit Teständerung; der
Dirigent zieht NAK-233 im Register nach.

**NB-2 — `acceptor_ueberlebt_die_verbindungsgrenze` flackerte zweimal, ohne
sich als Fehler dieses Tickets nachweisen zu lassen.** Der Fall liegt in
`server_v3/tests_lebenszyklus.rs` (wörtlich verschoben, Rumpf unverändert) und
prüft nach der Abweisung des 97. Clients `bewaffnete_listener == 2`. In den
ersten Läufen nach dem `tests_kopplung`-Schnitt fiel er zweimal mit `left: 1,
right: 2`. **Gemessen zur Einordnung, jeweils voller `cargo test --lib`:**

| Stand | Läufe | Ergebnis |
|---|---|---|
| Basis `090954fa` (lokale Arbeitskopie `C:/nk224`) | 8 + 20 einzeln, 20 unter doppelter Last | **48/48 grün** |
| Endstand dieses Tickets | 30 einzeln, 20 unter doppelter Last | **50/50 grün** (nach den zwei frühen Rotläufen) |

Der Rumpf ist unverändert; was sich geändert hat, ist der **Testname** und
damit die Reihenfolge, in der die Harness ihn neben andere Pipefälle stellt.
Im Code selbst liegt ein echtes Zeitfenster: `warte_auf` wartet auf
`am_worker_cap_abgewiesen > 0`, das **Nachrüsten** des Ersatzlisteners
geschieht danach, und die Zusicherung liest die Zahl sofort. Eine Härtung wäre
ein `warte_auf` um die letzte Zusicherung — das ist eine **Änderung an einem
Testkörper** und damit nach dem BEWEIS-Satz dieses Tickets ausgeschlossen. Der
Befund gehört deshalb hierher und in ein eigenes Ticket, nicht in diesen
Änderungssatz.

**NB-3 — `server::tests::connection_und_pipe_instanzzahl_bleiben_hart_begrenzt`
hing 45 Minuten.** Aufgetreten unter einer künstlich harten Schleife
(`touch` + Neubau + sofortiger Testlauf, zehnmal hintereinander). Der Prozess
stand mit **3 Threads, alle im Kernel-Wait, 0,59 s CPU**; offen war die Pipe
`evenacadia.eq-copilot.test.<pid>.connection-cap`, also die des v2-Servers aus
`broker/src/server.rs`. Der Test endet mit `griff.stoppen()` **ohne Frist** —
anders als sein Nachbar `stoppen_bricht_stille_connection_ohne_client_weckhilfe_ab`,
der genau dafür ein `recv_timeout(2 s)` benutzt. `broker/src/server.rs` ist in
diesem Ticket **nicht angefasst** (`git diff` dort zeigt nur die eine
Pfadänderung in `include_str!`), und der hängende Pipename gehört zum
v2-Server, nicht zu einer der drei geschnittenen Dateien. Der Befund ist damit
**vorbestehend und außerhalb des Ticketbereichs**; er gehört als eigene Zeile
ins Register.

**NB-4 — zwei Kommentar-Bezeichner bleiben stehen, und das ist richtig so.**
`hypothese/zusammenhang.rs` nennt `eigen4` (Zeile 81) und `eigen3` (Zeile 344).
Beide sind **Kennungen von Codex-Befunden aus dem G5-Lauf**, nicht Bezeichner
dieses Codes — belegt in `docs/beweise/NAK-212.md` und der Rohausgabe
`docs/beweise/roh/G5-gegenbeispiele-bf92891/`. Die A32-Heuristik kann einen
Fremdnamen nicht von einem toten Bezeichner unterscheiden; das ist der
Ausnahmefall aus Manifest NAK-223 §2.5. Ebenso bleibt
`g5_unbeteiligte_quelle` (Zeile 81): das ist ein **Fall des P5-Korpus**
(`eq-copilot/fixtures/p5-korpus/sitzungen.json`), kein Test.

**NB-6 — die A32-Behauptung im Runner nannte drei Zahlen, die nicht mehr
stimmten.** Zwei davon hat dieses Ticket selbst bewegt: die Ratsche für
Kommentar-Bezeichner (35 → 32) und die Zahl der Dateien über der 2 000er-Grenze
(fünf mit NAK-224/225/234 → drei mit NAK-225). Die dritte war **schon vorher
falsch**: der Selbsttest von `gesundheit.py` steht seit den zwei Nacharbeiten
von NAK-223 bei **116** Fällen, die Runner-Behauptung sagte weiterhin 62.
Gemessen im Abschlusskanon dieses Tickets: „Selbsttest: 116/116 bestanden“.
Alle drei sind nachgezogen — nach der Prüflisten-Regel „geänderte Zusage, drei
Stellen“ (Runnerkopf, Skriptkopf, lebender Manifestkopf). Angefasst ist
ausschließlich der Behauptungstext von A32; kein Bein, kein Riegel, keine
Baulogik. Deshalb der **zweite Kanonlauf**: die Übersichtstabelle, die der
Runner ans Manifest hängt, trägt diesen Text, und ein beglaubigtes Manifest
soll nicht eine Zahl behaupten, die im selben Änderungssatz korrigiert wurde.

**NB-5 — ein Doc-Kommentar stand seit NAK-121 am falschen Item.** In
`server_v3/mod.rs` beschrieb der Kopf von `MAX_VERBINDUNGEN` („Höchstens so
viele gleichzeitige Verbindungen …") das Modul `auth`. Gefunden beim Ordnen der
Moduldeklarationen, in derselben Datei und damit im Ticketbereich; er steht
jetzt wieder an seiner Konstanten. Nicht Teil der A32-Liste, weil die Heuristik
nur auf **Bezeichner** in Kommentaren sieht, nicht auf ihre Position.

---

## 6. Verlauf

**09.09.2026 — Iststand und Schnittplan.** Basis-SHA `090954fa`, Worktree
sauber. R1 bis R4 und R9 vorher gemessen (§3). Schnittplan §2 aus dem Code
abgeleitet und vor dem ersten Schnitt festgeschrieben.

**09.09.2026 — `hypothese.rs` aufgeteilt, elf Commits.** Zuerst die reine
Umbenennung (`git mv`, 100 % Ähnlichkeit, `2e...` → siehe R6), danach je
Fachbereich ein Commit in dieser Reihenfolge: `vertrag`, `determinismus`,
`testhilfe`, `eingang`, `messung`, `screening`, `zusammenhang`, `klassenwahl`
(darin NAK-233), `befund`, `enthaltung`, `rechnung`. Jeder Commit baute ohne
Warnung und lief mit `cargo test --lib` sowie `sonde014_hypothese` (ohne die
zwei Determinismusläufe, die am Ende in voller Länge liefen) grün.

**Abweichungen vom Schnittplan §2, alle beim Bauen entstanden und hier
genannt:**

| Abweichung | Warum |
|---|---|
| `Zielmetrik::braucht` privat → `pub(super)` | steht in §2.7 nicht; der einzige Aufrufer `gate` liegt nach dem Schnitt in `screening.rs` |
| `STUFE_B_AUFRUFE`, `STUFE_B_MAX` privat → `pub(super)` | ihre Schreiber `rang_und_beleg` und `hypothesen` liegen in Geschwistermodulen |
| `bandpassung`, `koinzidenz_gerichtet`, `intent_relevanz` privat → `pub(super)` | vom Screening gerufen; in §2.7 nur pauschal als „Ruf aus Geschwistermodul" geführt |
| drei `super::experiment::`-Pfade in `wiederholbarkeit` → `crate::coordinator::experiment::` | `super` ist nach dem Schnitt `hypothese`, nicht mehr `coordinator` |
| zwei `super::maskierung::`-Pfade in `baue_befund` und `CauseHypothesis` → `crate::coordinator::maskierung::` | dieselbe Ursache |
| ein `super::intent::`-Pfad in `hypothesen` → `crate::coordinator::intent::` | dieselbe Ursache |
| Testbauer in `testhilfe.rs` um vier Leerzeichen ausgerückt | ein Modul auf oberster Ebene statt eines Items im Testmodul; maschinell geprüft: null Abweichungen gegen den Ursprungstext |
| `passagenmaterial_zaehlt_nur_ganz_innenliegende_fenster` liegt in `messung.rs`, misst aber auch `masteranomalie` | der Test misst zuerst `fenster_in_passage`; er bleibt bei dieser Funktion und importiert `masteranomalie` aus dem Elternmodul, der Rumpf ist unverändert |

**Größen nach dem Schnitt** (`wc -l`): `mod.rs` 72, `befund` 484,
`determinismus` 120, `eingang` 179, `enthaltung` 203, `klassenwahl` 240,
`messung` 556, `rechnung` 522, `screening` 813, `testhilfe` 132, `vertrag`
349, `zusammenhang` 630 — Summe 4 300 gegenüber 3 986 vorher. Der Zuwachs
sind elf Modulköpfe und die `use`-Blöcke; kein Modul liegt über dem Ziel
1 500.

**Prüfung am Ende des `hypothese`-Schnitts, alle grün und ohne Warnung:**
`cargo test --lib` 245; `sonde014_hypothese` 37 (mit
`ranking_ist_bytegleich_ueber_hundert_laeufe` und
`screening_ist_deterministisch`, 141,86 s); `sonde014_befund` 7;
`sonde014_verdrahtung` 57; `coordinator_model` 51; `broker_idle` 5;
`security_vectors` 23; `transport_fuzz` 12.

**09.09.2026 — `server_v3/mod.rs` aufgeteilt, sechs Commits.** Die 203 Zeilen
Produktcode blieben unangetastet; geteilt wurde nur das Inline-Testmodul, in
`tests_hilfe`, `tests_fristen`, `tests_rueckstau`, `tests_lebenszyklus`,
`tests_kopplung`, `tests_abonnement`. Die Dateien liegen **flach im
Modulverzeichnis**, damit die drei `include_str!`-Pfade byte-gleich bleiben
(§2.5). `mod.rs` fällt von 2 169 auf **212** Zeilen; das größte Testmodul ist
`tests_kopplung.rs` mit 498.

Abweichungen vom Schnittplan, beim Bauen entstanden:

| Abweichung | Warum |
|---|---|
| `BlockSenke` liegt in `tests_hilfe.rs`, nicht in `tests_rueckstau.rs` | zwei Lebenszyklusfälle brauchen sie ebenfalls; ein geteiltes Bauteil gehört zu den geteilten Bauteilen |
| `Testclient` samt vier Methoden, `BlockSenke` samt drei Feldern und alle Rahmenbauer werden `pub(super)` | Ruf aus den Geschwister-Testdateien; das Feld `Testclient::h` bleibt privat, weil nur die `impl`-Blöcke es lesen |
| Testbauer um vier Leerzeichen ausgerückt | dieselbe Ursache wie bei `hypothese/testhilfe.rs` |

**09.09.2026 — `broker/src/lib.rs` aufgeteilt, sechs Commits.** `zeit`,
`testhilfe`, `register`, `sitzung`, `paar`, `lebenslauf` und `probe`. `lib.rs`
fällt von 1 998 auf **58** Zeilen und trägt nur noch Crate-Doku, die
Moduldeklarationen und die Glob-Reexporte. Größen: `register` 996,
`lebenslauf` 391, `paar` 433, `probe` 113, `sitzung` 71, `zeit` 41,
`testhilfe` 61 — keines über dem Ziel 1 500.

Abweichungen vom Schnittplan, beim Bauen entstanden:

| Abweichung | Warum |
|---|---|
| **Zwei `include_str!`-Pfade nachgezogen** — `broker/src/server.rs:1332` und `broker/tests/broker_idle.rs:189` lesen jetzt `lebenslauf.rs` statt `lib.rs` | Beide Tests messen **Produktquelltext**: die v2-Verdrahtung des Coordinators und die Reihenfolge der vier Entnahmen im geordneten Stopp. Dieser Text steht nach dem Schnitt in `lebenslauf.rs`. Ohne die Anpassung sind beide Tests rot, obwohl sich am gemessenen Verhalten nichts geändert hat. Je ein Token; die Assertions selbst sind unverändert. Das ist die einzige Zeile unter `broker/tests/` (§3 R5) |
| `paar_basis` liegt in `broker/src/testhilfe.rs`, nicht in `paar.rs` | die Registertests brauchen ihn ebenfalls |
| `hoermarkierungs_sperrgrund` und `aggregat_sperrgrund` werden `pub(crate)` | die Registertests messen sie, und `probe.rs` ruft den zweiten |
| sieben Felder von `SensorEintrag`, `VerbindungsMetadaten`, vier Felder von `BrokerLauf`, `BrokerSupervisor`, `BROKER`, `broker_version`, `status_ohne_lauf` werden `pub(crate)` | dieselbe Reichweite wie vorher: private Items der Crate-Wurzel waren aus keinem anderen Crate erreichbar |
| Eine Doc-Zeile von `v2_sensorpool_gibt_getrennte_ids_zurueck` blieb beim Schnitt in `lib.rs` zurück und wurde von Hand nachgezogen | Werkzeugfehler des Schnittskripts an der letzten Blockgrenze des Testmoduls; sofort bemerkt, weil `cargo build` „expected item after doc comment" meldete |

**Maschineller Wörtlichkeitsabgleich.** Für jede aus `lib.rs` herausgelöste
Datei wurde geprüft, dass **jede Codezeile** im Ursprungstext des Basis-SHA
vorkommt — modulo `pub(crate)`/`pub(super)` und den bekannten Pfadersetzungen.
Ergebnis: `zeit.rs`, `testhilfe.rs`, `sitzung.rs`, `paar.rs`, `lebenslauf.rs`,
`probe.rs` je **0 Abweichungen**; `register.rs` **0** bis auf die
Fortsetzungszeile eines mehrzeiligen `use`-Blocks, die das Prüfskript nicht als
`use`-Zeile erkennt.

**Die 25 neuen Dateien und ihre Modulköpfe (R7).**

| Datei | Kopfzeile |
|---|---|
| `hypothese/vertrag.rs` | Die geschlossenen Mengen des Produzenten (SONDE-014 Etappe C) |
| `hypothese/determinismus.rs` | Determinismus (M-25): die Größen, die zur `metrics_version` gehören |
| `hypothese/eingang.rs` | Die Eingangsformen: was der Rechner über eine Quelle weiß |
| `hypothese/messung.rs` | Rechenwerkzeug — klein, rein und einzeln messbar |
| `hypothese/screening.rs` | Stufe A — das günstige Screening, der Deckel und die harten Gates |
| `hypothese/zusammenhang.rs` | Stufe B — die fokussierte Evidenz (M-19, M-20) |
| `hypothese/klassenwahl.rs` | Klassenwahl und Konkurrenzlage: von der Sicherheit zum Zustand |
| `hypothese/befund.rs` | Befundbau und Sicht: die `CauseHypothesis` und was sie trägt |
| `hypothese/enthaltung.rs` | Die Enthaltung — mit Ort und ohne |
| `hypothese/rechnung.rs` | Der Ablauf: von der Aufnahme zur Hypothese |
| `hypothese/testhilfe.rs` | Die geteilten Testbauer der Ursachenrechnung |
| `server_v3/tests_hilfe.rs` | Die geteilten Bauteile der v3-Listenertests |
| `server_v3/tests_fristen.rs` | Fristen und Fassungen: was der Worker prüft, bevor die Senke etwas sieht |
| `server_v3/tests_rueckstau.rs` | Rückstau und Prioritätsklassen: der Leser hängt nie hinter der Senke |
| `server_v3/tests_lebenszyklus.rs` | Lebenszyklus: starten, stoppen, ernten — und nie in sich selbst joinen |
| `server_v3/tests_kopplung.rs` | Kopplung und Trennreihenfolge — die Zeilen der Verhaltensmatrix |
| `server_v3/tests_abonnement.rs` | Abonnement und Übergabe an den Coordinator |
| `broker/src/zeit.rs` | Zeitmessung des Brokers: Wanduhr für Ausgaben, Monotonie für Fristen |
| `broker/src/register.rs` | Das Sensorregister: wer meldet sich, wem gehört eine Sensor-ID, was ist noch frisch |
| `broker/src/sitzung.rs` | Sitzungen: welche Sensoren gehören zu demselben Hostprozess |
| `broker/src/paar.rs` | PRE/POST-Paare: wie stark die Aussage über ein Paar sein darf |
| `broker/src/lebenslauf.rs` | Brokerlebenslauf: starten, laufen, geordnet enden |
| `broker/src/probe.rs` | Die drei Aufrufe, die von außen in den Broker hineinreichen |
| `broker/src/testhilfe.rs` | Die geteilten Testbauer der Brokerwurzel |
| (dazu die beiden neu gefassten Wurzeln `hypothese/mod.rs` und `broker/src/lib.rs`) | — |

---

**09.09.2026 — Abschluss.** Riegelkarte R1 bis R10 vollständig gemessen (§3),
Manifest nachgezogen, dann der volle abgekoppelte Kanon auf sauberem Baum:
**GRÜN 62/62, Exit 0** auf `a87a5d9`. Danach die Runner-Korrektur aus NB-6 und
ein **zweiter** Lauf auf `3b902e4`, ebenfalls **GRÜN 62/62, Exit 0** — er
beglaubigt den Endstand samt der berichtigten A32-Behauptung.

Kumulierte Rundenbilanz (`py -3.13 tools/dirigent/rundenbilanz.py 090954fa..HEAD`):
Produkt 29 Dateien +8 527/−7 931, Tests 1 Datei +1/−1, Prüfwerkzeug 2 Dateien
+18/−8, Doku 7 Dateien +10 148/−0. Der Produktzuwachs von rund 600 Zeilen bei
gleichem Verhalten sind die 25 Modulköpfe und die `use`-Blöcke, die eine
einzelne große Datei nicht braucht; die Dokuzeilen sind zum größten Teil die
zwei Kanon-Rohausgaben und die zwei Testlisten.

**Was dieses Ticket ausdrücklich NICHT getan hat:** keine Funktion zerlegt
(NAK-235 — `hypothesen` hat unverändert 267 Zeilen, keine Funktion ist länger
geworden), keine `// SAFETY:`-Arbeit (NAK-237), keinen Kommentar außerhalb der
drei Ticketdateien angefasst (NAK-236), keine Regel, keine Schwelle und keinen
Fassungsschritt geändert. Die zwei Testbefunde NB-2 und NB-3 sind bewusst nicht
behoben: beide bräuchten eine Änderung an einem Testkörper, und das schließt der
BEWEIS-Satz dieses Tickets aus.

## Kanon-Lauf - NAK-224

**Lauf:** 2026-09-09 14:41 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 62/62 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-224-a87a5d9.md](roh/NAK-224-a87a5d9.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-09 14:41:06 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | a87a5d9d NAK-224 (S25c): Manifest nachgezogen — Riegelkarte, Pruefliste, fuenf Nebenbefunde |
| Commit (voll) | a87a5d9d7e841e897afdd03003f08667ac42cf69 |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,10 s | [A1](roh/NAK-224-a87a5d9.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 10,16 s | [A2](roh/NAK-224-a87a5d9.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,68 s | [A3](roh/NAK-224-a87a5d9.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.177,14 s | [A4](roh/NAK-224-a87a5d9.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,82 s | [A4-SI](roh/NAK-224-a87a5d9.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,71 s | [A4b](roh/NAK-224-a87a5d9.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 4,14 s | [A5](roh/NAK-224-a87a5d9.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,13 s | [A6](roh/NAK-224-a87a5d9.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-224-a87a5d9.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,25 s | [A8](roh/NAK-224-a87a5d9.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,39 s | [A9](roh/NAK-224-a87a5d9.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 4,01 s | [A10](roh/NAK-224-a87a5d9.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,25 s | [A11](roh/NAK-224-a87a5d9.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,28 s | [A12](roh/NAK-224-a87a5d9.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,20 s | [A13](roh/NAK-224-a87a5d9.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 7,01 s | [A14](roh/NAK-224-a87a5d9.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,27 s | [A17](roh/NAK-224-a87a5d9.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,57 s | [A18](roh/NAK-224-a87a5d9.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-224-a87a5d9.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A20](roh/NAK-224-a87a5d9.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,34 s | [A21](roh/NAK-224-a87a5d9.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,03 s | [A22](roh/NAK-224-a87a5d9.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,71 s | [A24](roh/NAK-224-a87a5d9.md#a24) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A28](roh/NAK-224-a87a5d9.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,13 s | [A29](roh/NAK-224-a87a5d9.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A30](roh/NAK-224-a87a5d9.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 105,28 s | [A31](roh/NAK-224-a87a5d9.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-224-a87a5d9.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,14 s | [A26](roh/NAK-224-a87a5d9.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-224-a87a5d9.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,64 s | [A23](roh/NAK-224-a87a5d9.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,11 s | [A16](roh/NAK-224-a87a5d9.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,12 s | [B1](roh/NAK-224-a87a5d9.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,22 s | [B2](roh/NAK-224-a87a5d9.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,07 s | [B3](roh/NAK-224-a87a5d9.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,11 s | [B3b](roh/NAK-224-a87a5d9.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,60 s | [B3c](roh/NAK-224-a87a5d9.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,41 s | [B4](roh/NAK-224-a87a5d9.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,15 s | [B9](roh/NAK-224-a87a5d9.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,44 s | [B5](roh/NAK-224-a87a5d9.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 215,82 s | [B10](roh/NAK-224-a87a5d9.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,00 s | [B11](roh/NAK-224-a87a5d9.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,61 s | [B12](roh/NAK-224-a87a5d9.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,12 s | [B13](roh/NAK-224-a87a5d9.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,23 s | [B14](roh/NAK-224-a87a5d9.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,28 s | [B15](roh/NAK-224-a87a5d9.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-224-a87a5d9.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,95 s | [B22](roh/NAK-224-a87a5d9.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,51 s | [B23](roh/NAK-224-a87a5d9.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,03 s | [B24](roh/NAK-224-a87a5d9.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-224-a87a5d9.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-224-a87a5d9.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-224-a87a5d9.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,94 s | [B19](roh/NAK-224-a87a5d9.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,11 s | [B17](roh/NAK-224-a87a5d9.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,09 s | [B18](roh/NAK-224-a87a5d9.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,52 s | [B16](roh/NAK-224-a87a5d9.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,33 s | [B27](roh/NAK-224-a87a5d9.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,22 s | [B29](roh/NAK-224-a87a5d9.md#b29) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,15 s | [B28](roh/NAK-224-a87a5d9.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,29 s | [B8](roh/NAK-224-a87a5d9.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook: Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500, `broker/src/generiert/` ausgenommen), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 35, clippy 91) stehen auf dem Iststand vom 09.09.2026 und reissen bei der ersten Verschlechterung; die Zeilengrenze reisst heute bei genau den fuenf Dateien mit Pflegeticket NAK-224/225/234 und meldet jeden weiteren Treffer als OHNE PFLEGETICKET. Vorangestellt laeuft der Selbsttest: 62 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest oder ein fehlender Messort, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 4,98 s | [A32](roh/NAK-224-a87a5d9.md#a32) |


---

## Kanon-Lauf - NAK-224

**Lauf:** 2026-09-09 15:15 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 62/62 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-224-3b902e4.md](roh/NAK-224-3b902e4.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-09 15:15:56 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 3b902e48 NAK-224 (S25c): A32-Behauptung im Runner nachgezogen (Pruefliste E) |
| Commit (voll) | 3b902e48e180441638d6bde29af76f233f4c314d |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,09 s | [A1](roh/NAK-224-3b902e4.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,87 s | [A2](roh/NAK-224-3b902e4.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,62 s | [A3](roh/NAK-224-3b902e4.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.185,60 s | [A4](roh/NAK-224-3b902e4.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 12,74 s | [A4-SI](roh/NAK-224-3b902e4.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 65,74 s | [A4b](roh/NAK-224-3b902e4.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 3,20 s | [A5](roh/NAK-224-3b902e4.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-224-3b902e4.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,13 s | [A7](roh/NAK-224-3b902e4.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,38 s | [A8](roh/NAK-224-3b902e4.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,26 s | [A9](roh/NAK-224-3b902e4.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 3,15 s | [A10](roh/NAK-224-3b902e4.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,25 s | [A11](roh/NAK-224-3b902e4.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,24 s | [A12](roh/NAK-224-3b902e4.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,20 s | [A13](roh/NAK-224-3b902e4.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 6,99 s | [A14](roh/NAK-224-3b902e4.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,17 s | [A17](roh/NAK-224-3b902e4.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 35,47 s | [A18](roh/NAK-224-3b902e4.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,14 s | [A19](roh/NAK-224-3b902e4.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,44 s | [A20](roh/NAK-224-3b902e4.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,34 s | [A21](roh/NAK-224-3b902e4.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,07 s | [A22](roh/NAK-224-3b902e4.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,77 s | [A24](roh/NAK-224-3b902e4.md#a24) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,13 s | [A28](roh/NAK-224-3b902e4.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,13 s | [A29](roh/NAK-224-3b902e4.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,26 s | [A30](roh/NAK-224-3b902e4.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 105,43 s | [A31](roh/NAK-224-3b902e4.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-224-3b902e4.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,14 s | [A26](roh/NAK-224-3b902e4.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-224-3b902e4.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,54 s | [A23](roh/NAK-224-3b902e4.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,10 s | [A16](roh/NAK-224-3b902e4.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,12 s | [B1](roh/NAK-224-3b902e4.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,21 s | [B2](roh/NAK-224-3b902e4.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,14 s | [B3](roh/NAK-224-3b902e4.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,07 s | [B3b](roh/NAK-224-3b902e4.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,40 s | [B3c](roh/NAK-224-3b902e4.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,43 s | [B4](roh/NAK-224-3b902e4.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,14 s | [B9](roh/NAK-224-3b902e4.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,44 s | [B5](roh/NAK-224-3b902e4.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 215,53 s | [B10](roh/NAK-224-3b902e4.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 0,98 s | [B11](roh/NAK-224-3b902e4.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,60 s | [B12](roh/NAK-224-3b902e4.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,12 s | [B13](roh/NAK-224-3b902e4.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,20 s | [B14](roh/NAK-224-3b902e4.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,45 s | [B15](roh/NAK-224-3b902e4.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,06 s | [B20](roh/NAK-224-3b902e4.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,94 s | [B22](roh/NAK-224-3b902e4.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,60 s | [B23](roh/NAK-224-3b902e4.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,00 s | [B24](roh/NAK-224-3b902e4.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,08 s | [B25](roh/NAK-224-3b902e4.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,48 s | [B26](roh/NAK-224-3b902e4.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,08 s | [B21](roh/NAK-224-3b902e4.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 2,11 s | [B19](roh/NAK-224-3b902e4.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,12 s | [B17](roh/NAK-224-3b902e4.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,11 s | [B18](roh/NAK-224-3b902e4.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,56 s | [B16](roh/NAK-224-3b902e4.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,40 s | [B27](roh/NAK-224-3b902e4.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,22 s | [B29](roh/NAK-224-3b902e4.md#b29) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,14 s | [B28](roh/NAK-224-3b902e4.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,28 s | [B8](roh/NAK-224-3b902e4.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook: Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500, `broker/src/generiert/` ausgenommen), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 32, clippy 91) stehen auf dem Iststand vom 09.09.2026 nach NAK-224 und reissen bei der ersten Verschlechterung; sie werden nur gesenkt, nie erhoeht. Die Zeilengrenze reisst heute bei genau den drei Dateien mit Pflegeticket NAK-225 und meldet jeden weiteren Treffer als OHNE PFLEGETICKET. Vorangestellt laeuft der Selbsttest: 116 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest oder ein fehlender Messort, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 5,13 s | [A32](roh/NAK-224-3b902e4.md#a32) |
