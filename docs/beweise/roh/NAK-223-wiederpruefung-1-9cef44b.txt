URTEIL: NEEDS_WORK — beide ursprünglichen Fehlerbilder sind behoben, aber der Fixdiff erzeugt die beschriebene Regex-Regression.

Geprüft: Befund 1 geschlossen: ja, Originalreproduktion unter Python 3.14 liefert genau [('t.rs', 1, 'helfer')] (gesundheit.py:574–575); Selbsttestfälle für pub const fn, pub(crate) unsafe fn, pub async fn, pub const HELFER und unsafe fn mit den Namen „ohne Verwendung ist ein Befund“, „meldet den Namen“ und „mit Verwendung ist kein Befund“ stehen in Zeilen 1104–1120, Rotbeweis NAK-223-rot-deadcode.txt §3/Mutation A; Befund 2 geschlossen: ja, Originalreproduktion liefert RuntimeError ohne Zahl, der Hauptlauf mit Prozessersatz Exit 2 (gesundheit.py:522–525, 1341–1343), Fälle „Abbruch MIT/OHNE Diagnosen ist ein Werkzeugfehler“, „sauberer Lauf mit 3 Warnungen zaehlt 3“ und „sauberer Lauf ohne Diagnosen zaehlt 0“ in Zeilen 1156–1172, Rotbeweis NAK-223-rot-clippy.txt §3/Mutation B; virtueller vollständiger Selbsttest mit Speicheradapter vorher 62/62, Ziel 96/96; übrige Messwerte und Tabellen alt/neu identisch, zwei reale Messläufe ohne Clippy bytegleich mit Exit 4, Exitpfad 0 zusätzlich über markierte Einspeisung bestätigt, Schwellen 28/91/35 und 2000/1500/200 einschließlich Kontextgrenzen unverändert, A32-Verdrahtung --mit-selbsttest und Exit 4 → HINWEIS unverändert (tools/beweise.ps1:732, 1136–1139), HEAD und Worktree-Status vor/nach identisch, nichts geschrieben.

Nicht geprüft: native Python-3.13-Läufe einschließlich Selbsttest/A32 wegen Launcher-Exit 101 („Anmeldesitzung nicht vorhanden“); echter Clippy-Lauf, Cargo, CMake und Kanon auftragsgemäß nicht ausgeführt.

Review comment:

- [P2] Überspringe gültige Elementnamen nicht als Qualifizierer — C:\Users\phili\Projekte\Nakama\tools\plan\gesundheit.py:572-575
  DEFEKT: Beim gültigen Rust-Typnamen `union` überspringt die Wiederholung den tatsächlichen Namen und erfasst stattdessen `where`. Reproduktion: Für `s = '#[allow(dead_code)]\nstruct union where u8: Copy;\nfn x<T>() where T: Copy {}\n'` liefert `finde_dead_code([('t.rs', None)], {'t.rs': s})` am Zielstand `[]`, vorher einen Treffer `union`: Die fremde `where`-Klausel wird als Verwendung gewertet. Bei sonst gehaltenen Grenzen kippt der virtuelle Gesamtlauf dadurch von Exit 4 auf 0. Das verletzt den Namensvertrag aus [R-D1](docs/beweise/roh/NAK-223-nacharbeit-1-auftrag.txt#L10) und das Gate-Maß für aufruferlose Helfer; übersprungen werden dürfen nur tatsächliche Qualifizierer.