SONDE-014 / S23–25 — Etappe 2, Nacharbeit 2 (Runde 2 von 3). Auftrag des Dirigenten (2026-09-07) an einen frischen Opus-Bauer (max).

LAGE
Repo C:/Users/phili/Projekte/Nakama, Branch master. Basis = `git rev-parse HEAD` beim Start (der Dirigent hat den Stand nach f30ac47 gemessen: Worktree sauber, Nacharbeit 1 abgeschlossen, betroffene Beine grün, docs/beweise/roh/SONDE-014-nacharbeit-1-25460cd.txt). Ticket-Basis bleibt f90abf5. Aufsicht ENG. Es gelten unverändert die Bauregeln aus docs/beweise/roh/SONDE-014-etappe-2-auftrag.txt und die Rahmenregeln aus docs/beweise/roh/SONDE-014-nacharbeit-1-auftrag.txt (Git nur mit Pathspec, nie `git add -A`, nie `--amend`, Commit-Nachricht über Temp-Datei und `git commit -F`, `git pull --rebase` bei sauberem Worktree vor dem Push; kein Löschbefehl und kein Lösch-Cmdlet; keine Änderung an docs/offene-punkte.md, docs/PLAN-STAND.md, docs/NEXT-SESSION.md, docs/plan/, design/, Audio-/Sonden-/Hostbridge-/Identity-/Installer-Pfaden; Engines in Tests auf den Heap; jeder Rotbeweis fällt an genau der Zeile, die die Zusage trägt; keine Nachfrage an den User; beende den Turn nie mit uncommittetem Stand oder laufendem Bein).

REFERENZ
Verhaltensmatrix docs/beweise/SONDE-014.md §3 mit den Nachträgen aus §8.6; Entscheide E-01 bis E-13; Regeln NR-01 bis NR-14 (Nacharbeit 1, §8). Die Wiederprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a07a94-57c2-7282-bdc5-30f03182c866, HEAD f30ac47 vor und nach dem Lauf, 20 min, Fixdiff a319b2d...f30ac47) steht wörtlich in docs/beweise/roh/SONDE-014-wiederpruefung-1-f30ac47.txt, ihr Auftrag in docs/beweise/roh/SONDE-014-wiederpruefung-1-auftrag.txt. Urteil: NEEDS_WORK, fünf Befunde WP1-1 bis WP1-5. Der Dirigent hat jeden an der Quelle geprüft: vier DEFEKTE (WP1-1, WP1-3, WP1-4, WP1-5) und eine LÜCKE (WP1-2), die er in dieser Runde entscheidet (E-14). Lies zuerst diesen Auftrag, dann das Rohurteil vollständig, dann §8 des Manifests und die genannten Quellstellen.

BEFUNDE (Kernsatz wörtlich aus dem Urteil; Datei:Zeile zum Stand f30ac47) und die REGEL, die jeden schließt:

WP1-1 — „Wenn `assistentAntwort()` ein anderes Ergebnis setzt, erhöht `assistentAenderungMelden()` zunächst `v3StateRevision`; der anschließend erzeugte Urteilskopf übernimmt diese neue Revision. Der Broker kennt bis zum nächsten `state_report` jedoch noch den alten Stand. … P0 überholt den P1-Bericht, `befehl.rs:259–260` antwortet mit `revision_conflict`, und `inFlightAck()` entfernt das unpersistierte Urteil endgültig. Damit bleibt EP-10/M-73 offen; der neue B29-Abschnitt prüft nur den JSON-Erzeuger, nicht diesen gekoppelten Versand." (eq-copilot/plugin/src/PluginProcessor.cpp:3670-3685, 3173 versuchKopfJson, 3553; broker/src/coordinator/befehl.rs:257-260; eq-copilot/plugin/core/ipc/ControlClient.cpp:1222-1238; state_report reist als P1 im 1-Hz-Takt und wird vom P0 strukturell überholt — der Konflikt ist der Regelfall, nicht ein Zeitfenster)
  Regel WN-01: Der Kopf eines `user_verdict` trägt als `base_revision` die zuletzt AN DEN BROKER GEMELDETE State-Revision (die Revision des zuletzt gesendeten `state_report`; im Plugin als eigener Wert geführt, gesetzt beim Versand des Reports), nie den lokalen Zähler, der soeben erhöht wurde. Zusätzlich ist ein `konflikt`-ACK auf ein `user_verdict` kein endgültiger Verlust: das Plugin baut den Kopf mit der dann gemeldeten Revision neu und wiederholt das Urteil unter derselben `command_id` (idempotent, NR-10); erst `angewandt` schließt es ab. Gemessen mit einem A4-Fall (`state_report` mit Revision n → Zustandsänderung im Kopf simuliert → `user_verdict` mit `base_revision = n` wird `angewandt`, mit n+1 `konflikt`) und einem B29/B10-Fall, der `assistentAntwort(..., urteil)` ruft (nicht den Erzeuger) und am Testserver prüft, dass der gesendete Kopf die gemeldete Revision trägt und nach einem `konflikt`-ACK eine Wiederholung mit frischem Kopf folgt. Rotbeweis: Kopf wieder aus `v3StateRevision` → Fall fällt.

WP1-2 — LÜCKE, in dieser Runde entschieden — „Nach Vollbestand/0 → Teilupdate A/3 → Befundbildung für B → verspätetem B/2 mit `rolle=geschuetzt` wird das Schutzveto zwar übernommen, die Sitzungsrevision bleibt aber 3. Dieser Zweig markiert den bisherigen READY-Befund nicht als STALE. Trifft B/2 während der Rechnung ein, akzeptiert auch die neue Eintragungsprüfung das veraltete Ergebnis, weil dessen numerische Intent-Revision weiterhin übereinstimmt." (broker/src/coordinator/intent.rs:579-596; hypothese_verdrahtung.rs:694-701). M-10 bindet STALE wörtlich an eine gestiegene Revision; die Lücke ist durch NR-02 entstanden.
  Entscheid E-14 (Dirigent, 07.09.2026): Maßgeblich für STALE und für die Eintragungsprüfung ist nicht die vom Sender gezählte Revision, sondern JEDE im Broker wirksam übernommene Bestandsänderung (Voll- oder Teilbericht). Der Broker führt je Sitzung eine monoton steigende Bestandsgeneration, die bei jeder Übernahme steigt; Befunde und laufende Aufnahmen tragen die Generation, mit der sie gerechnet wurden. Steigt die Generation, gehen abhängige Befunde sichtbar in STALE (ohne Nachrechnen), und ein Rechenergebnis einer älteren Generation wird nicht eingetragen (NR-03). Die Sender-Revision bleibt allein die Ordnung der Übernahme (NR-02). M-10 erhält diesen Satz als Nachtrag „E-14 (Nacharbeit 2, 07.09.2026)".
  Regel WN-02: `befunde_veralten_locked` und die Prüfung in `befunde_eintragen` arbeiten auf der Bestandsgeneration; der verspätete Teilbericht mit hartem Veto entwertet den READY-Befund. Neuer A4-Fall `verspaeteter_teilbericht_entwertet_abhaengige_befunde`: Vollbestand/0 → A/3 → Befund READY für B → B/2 `geschuetzt` → Befund STALE, kein Draft; zweiter Abschnitt über den Testhaken aus NR-03: B/2 trifft während der Rechnung ein → altes Ergebnis wird nicht eingetragen. Rotbeweis: Generation aus der Prüfung nehmen → Fall fällt.

WP1-3 — „EP-04 bleibt für erneut abonnierende Clients offen: Nach Befundbildung und `DELETE FROM evidence` im isolierten Storetest genügt ein weiteres `subscribe_session` ohne zwischenzeitlichen Flush. `resubscribe_snapshot_push()` in `subscription.rs:75–164` umgeht diesen neuen Riegel, liest die gespeicherte Sessionprojektion und versendet deren `findings` unverändert." (broker/src/coordinator/subscription.rs:74-96, 115; flush.rs:51; die NR-04-Härtung sitzt nur in hypothese_verdrahtung.rs:348, flush.rs:51, sicht.rs:354)
  Regel WN-03: Es gibt KEINEN Sendepfad für `findings` ohne Store-Existenzprüfung: `resubscribe_snapshot_push` ruft `befunde_gegen_store_haerten(session)` vor dem Standlock UND hält eine aus `session_state_lesen` gelesene Projektion gegen dieselbe Existenzabfrage (fehlende ID → Befund fällt aus den gesendeten `findings`, Teilverlust → STALE), bevor Bytes das Haus verlassen. Ein Inventar aller Stellen, die `findings` serialisieren oder gespeicherte Projektionen senden, steht in §9 mit der Härtungsstelle je Pfad. Neuer A4-Storefall `re_subscribe_haelt_die_belege_gegen_den_store`: Befund bilden → Flush → `DELETE FROM evidence` → erneutes `subscribe_session` ohne Flush → der gepushte Snapshot trägt den Befund nicht mehr. Rotbeweis: Härtung im Re-Subscribe entfernen → Fall fällt.

WP1-4 — „Bei einer Sitzung ohne benannte Passage erzeugt der Broker weiterhin unvollständige Proposals. Der ausgeführte Test bestätigt jetzt ausdrücklich, dass `proposal_json_fuer_test()` das neue Pflichtfeld nicht liefert, und überspringt anschließend dessen Prüfung. Dieselbe Serialisierung wird von `vorschlag_persistieren()` gespeichert. … Der Erzeuger muss ohne Passage auf das Proposal verzichten, statt die Vertragsabweichung im Test festzuschreiben." (broker/src/coordinator/proposal.rs:694-698, 899; proposal_verdrahtung.rs:76-88, 139-157, 302-303; broker/tests/sonde014_proposal.rs:211-217; Doc-Kommentar proposal.rs:350 behauptet noch „im Vertrag optional")
  Regel WN-04: Ohne benannte Passage entsteht KEIN Proposal-Objekt: der Erzeuger liefert `Option<Proposal>` und gibt ohne `befund.passage_id` `None` zurück — kein Eintrag in `stand.vorschlaege`, kein `vorschlag_persistieren`, kein `draft_offer`; `no_change` und `more_data` bleiben vollständige Objekte mit Passage (M-46). Der Sprung `if *feld == "passage_id" { … continue; }` in `sonde014_proposal.rs` entfällt; die Bühne führt eine benannte Passage oder der Fall misst „ohne Passage kein Objekt". Der Doc-Kommentar in proposal.rs wird berichtigt. Neuer A4-Fall `ohne_passage_entsteht_kein_proposal`: Bühne ohne Passage → `vorschlaege_sicht` leer und keine `event_type='proposal'`-Zeile im Store; Gegenprobe mit Passage → vollständiges Sechs-Felder-Objekt (bestehender Fall `sechs_gate_felder_sind_pflicht_mit_passage`). Rotbeweis: `None`-Zweig entfernen → Fall fällt.

WP1-5 — „Bei voller 64er-P0-Queue liefert `sendePersistenzP0()` false und entfernt den neuen Auftrag wieder aus `inFlight` (`ControlClient.cpp:1151–1165`). Dieser neue Aufrufer ignoriert das Ergebnis und gibt weiterhin `gemeldet` zurück. … die Methode meldet Erfolg, das Urteil wird aber weder persistiert noch beim Reconnect wiederholt. Das verletzt M-73 ausdrücklich." (eq-copilot/plugin/src/PluginProcessor.cpp:3679-3685; eq-copilot/plugin/core/ipc/ControlClient.cpp:1148-1166; `beiP0Verworfen` wird auf diesem Weg nicht gerufen)
  Regel WN-05: Kein Rückgabewert der Queue-Politik wird ignoriert (M-73 Rotbeweis wörtlich): `assistentAntwort` gibt nur `true` zurück, wenn das Urteil eingereiht wurde; ein bei voller P0-Queue abgewiesenes `user_verdict` wird NICHT stillschweigend gelöscht, sondern geht an den `beiP0Verworfen`-Pfad (Verbindung verworfen, Eintrag gemeldet — M-73 P0-Politik wörtlich) und bleibt im Plugin zur Wiederholung nach dem Reconnect erhalten (dieselbe `command_id`). Neuer B10/B29-Fall: P0-Queue mit 64 Einträgen füllen → `assistentAntwort(..., urteil)` → Rückgabe `false`, `beiP0Verworfen` gerufen, nach Reconnect wird das Urteil unter derselben `command_id` gesendet und `angewandt`. Rotbeweis: Rückgabewert wieder verwerfen → Fall fällt.

GEMEINSAME URSACHEN: (1) das Userurteil auf der Plugin-Sendeseite ist nicht an den tatsächlichen Verbindungszustand gekoppelt (WN-01, WN-05 — gemeldete Revision, Queue-Politik, Wiederholung: ein Änderungssatz); (2) ein zweiter Sendepfad ohne den Riegel (WN-03); (3) Entwertung hängt an der Sender-Zahl statt an der Übernahme (WN-02); (4) der Erzeuger erzeugt, was der Vertrag verbietet (WN-04). Baue je Ursache einen logischen Commit.

BETROFFENE BEINE (nur diese; kein voller Kanon — der läuft beim Abschluss): A4 `cargo test --manifest-path broker/Cargo.toml` (sonde014_nacharbeit1, sonde014_verdrahtung, sonde014_proposal, sonde014_hypothese, Store-Tests), A4-SI (`store_crash_matrix -- --ignored`), B10 `EqCopIpcTest`, B29 `EqCopSonde014AssistentTest`, B27 `EqCopSonde014IntentTest`, B2 `EqCopStateMigrationTest` und B14, falls NakamaState berührt ist; dazu jedes weitere Bein, dessen Quelle dein Diff berührt (aus `tools/beweise.ps1` ablesen). Jede Regel hat ihren Rotbeweis `docs/beweise/roh/SONDE-014-rot-WN-nn.txt`; der Beinlauf auf dem Endstand wird als `docs/beweise/roh/SONDE-014-nacharbeit-2-<sha>.txt` belegt.

MANIFEST docs/beweise/SONDE-014.md: neuer Abschnitt „§9 Wiederprüfung 1 und Nacharbeit 2" (append-only unter §8.8): Kopf der Wiederprüfung 1 (Modell, Effort, Thread, HEAD, Dauer, Urteil, Rohdatei, Auftrag), Einordnung des Dirigenten (4 Defekte, 1 Lücke → E-14), Zuordnung WP1-1 bis WP1-5 → WN-01 bis WN-05 → geänderte Stelle → Rotbeweis, das Sendepfad-Inventar aus WN-03, gemessene Beinläufe mit Zahlen, Matrixnachträge (M-10 E-14; M-28, M-46, M-73 „Nacharbeit 2"), Prüflisten-Abhakung; lebender Kopf: „Phase" auf „Etappe 2: Nacharbeit 2 abgeschlossen, Wiederprüfung 2 offen", „Urteil" um „Wiederprüfung 1 (Astra max): NEEDS_WORK, 5 Befunde — 4 Defekte, 1 Lücke (E-14)" ergänzt, Kurztabelle der Runden fortgeschrieben, Ticketpfade um neue Dateien ergänzt; Nebenbefunde in §7.11 fortschreiben. `py -3.13 tools/plan/dokuriegel.py docs/beweise/SONDE-014.md` grün vor dem Commit.

SELBSTAUDIT vor jedem Commit: Kopf des Urteils wirklich aus der gemeldeten Revision, Wiederholung nach Konflikt wirklich gefahren, Generation wirklich in Veralten UND Eintragung, kein Sendepfad ohne Riegel (Inventar gegen `rg 'findings' broker/src`), ohne Passage wirklich kein Objekt und keine Store-Zeile, Rückgabewert wirklich ausgewertet und Verworfenes gemeldet; keine Umbenennung, kein Neben-Refactor, keine Änderung an Identität, Audio-Pfad oder Installer. Commits mit Pathspec je Ursache, pushen. Letzte Zeile deiner Ausgabe: „NACHARBEIT 2 ABGESCHLOSSEN <end-sha>".