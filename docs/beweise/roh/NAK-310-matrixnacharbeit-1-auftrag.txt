Matrixnacharbeit 1 / Ticket NAK-310, Planschritt S25i (Tiefenaudit 3 abarbeiten, Teil 2: Pipe-Sicherheit; Etappe 1 „Spezifikation vor Code") — Auftrag des Dirigenten an einen frischen Opus-Worker (2026-09-23)

ARBEITSORT. Du arbeitest ausschließlich in der Arbeitskopie C:\Users\phili\nk-doku (Branch master, Stand beim Start = origin/master; miss `git rev-parse HEAD` zu Beginn und nenne ihn in deinem Bericht). Der sichtbare Checkout C:\Users\phili\Projekte\Nakama ist tabu: dort läuft die Abschlussprüfung eines anderen Tickets mit HEAD-Regel. Du änderst genau zwei Dateien: docs/beweise/NAK-310.md (Abschnitte §5 bis §8, dazu am Ende ein neuer Abschnitt §12 mit deinem Bericht) und diese Auftragsdatei bleibt unverändert; keine Datei unter broker/, eq-copilot/, tools/, docs/plan/, docs/offene-punkte.md. Kein Code, kein Bau, kein Test, kein cargo, kein FL Studio, kein Python-Interpreter, keine Named Pipe, kein Angriffscode (auch nicht als Beispiel). Fremde Dateien (briefing-hub/, nimbalyst-local/) fasst du nie an.

GIT-REGELN (Skill §6, CLAUDE.md): Commit nur mit explizitem Pathspec (`git add -- docs/beweise/NAK-310.md docs/beweise/roh/NAK-310-matrixnacharbeit-1-auftrag.txt`), nie `git add -A`, nie `--amend`, nie `git reset --hard`, nie fremde uncommittete Dateien anfassen. Mehrzeilige Commitnachricht über `git commit -F <datei>` (Backticks in `-m` werden von der Shell ausgewertet); die Nachricht endet mit der Zeile `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Vor dem Push `git pull --rebase` (der Worktree ist dann sauber), dann `git push`; der Push ist ein eigener Schritt. Ein Verweis ins Leere ist ein Befund: vor dem Commit `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-310.md` (das ist das einzige erlaubte Python-Kommando; Exit 0 oder nur „benannt (historisch oder Zielpfad)" ist sauber; ein Verweis auf eine noch nicht angelegte Datei trägt im selben Satz „Zielpfad, noch nicht angelegt"). Zeilenenden: docs/beweise/NAK-310.md hat im Worktree CRLF (`git ls-files --eol`), schreibe dieselben zurück. In Markdown-Tabellenzellen ein Pipe-Zeichen als \| schreiben, Zeilenbereiche mit ASCII-Bindestrich.

WARTEN UND WÄCHTER (NAK-354): Du fährst keinen Kanon und keinen Lauf; falls du dennoch auf etwas wartest, setze einen abgelaufenen Wächter neu und lies bei jeder Rückkehr zuerst das Log. Melde dich nie mit „ich warte auf die Benachrichtigung" und steh dann.

QUELLEN, verbindlich: docs/beweise/NAK-310.md — Kopf (Zeile „Gate", wörtlich), §0, §1 (Einordnung je Befund), §2 (R-310-1 bis R-310-6), §3 (U59 ist seit 23.09.2026, 12:12 Uhr beantwortet: „Nur Automatik-Sitzungen (Empfohlen)"; §10.3), §4 Etappenplan, §5 Zusagen, §6 Matrix M-01 bis M-93, §7 Bauplan, §8 Prüfliste, §10 (Rohurteil der Matrixprüfung 1, drei Befunde V-1 bis V-3), **§11 (Einordnung und die Regeln R-310-7, R-310-8, R-310-9 — sie sind dein Auftrag, wörtlich zu halten)**; docs/beweise/roh/NAK-310-matrixpruefung-1-urteil.md (Rohurteil), docs/beweise/roh/NAK-310-matrixpruefung-1-validierung.md (Validierung mit Zeilen, Ablauf, Gegenfällen, „Gemeinsame Ursache"); docs/beweise/roh/NAK-310-quellvalidierung-teil-A.md und -teil-B.md; CLAUDE.md „Arbeitsregeln" (Beziehungen mitprüfen: speichern↔laden, starten↔stoppen, öffnen↔schließen, verbinden↔trennen, aktivieren↔abklingen) und „Tragende technische Invarianten"; tools/dirigent/pruefliste.md; der Code am Stand (nur lesen): broker/src/lebenslauf.rs, broker/src/briefkasten.rs, broker/src/main.rs, broker/src/register.rs, broker/src/server.rs, eq-copilot/plugin/core/ipc/BrokerLifecycle.cpp, eq-copilot/plugin/core/diagnose/Briefkasten.cpp, tools/fl/laufzeit.ps1, tools/beweise.ps1 (A4-Behauptungszeile um Zeile 518, Beine A4, A4-SI, A4b, A17, A18, A22, A35, B10, B30). Alle Zeilenangaben des Prüfers und des Validierers gelten am Stand ac69a9bd (`git show ac69a9bd:docs/beweise/NAK-310.md`); im heutigen Manifest liegen §10 und §11 darunter.

DIE URSACHE IST DER AUFTRAG. Der Validierer hat für alle drei Befunde dieselbe Ursache benannt: §7 übernimmt Sätze und Forderungen aus den Quellvalidierungen, ohne jeden Satz einer Matrixzeile zuzuordnen; je Etappe fehlen die Gegenstücke — zur Aktivierung die Abklingzeile, zur neuen Zusage die Behauptungszeile, zu einer Übersichtsbehauptung die tragende Zeile. Du behebst die drei Befunde und die Ursache:

1. **V-1, Regel R-310-7 (Marke bindet an die markierende Sitzung).** Ergänze in §6 Matrixzeilen (fortlaufend ab M-94, Präfix und Spalten wie die bestehenden Zeilen) für: den Startpfad — der Broker erhält beim markierten Start die Kennung des startenden Prozesses als Startargument (kein v2- oder v3-Vertragsfeld; Weg in BrokerLifecycle.cpp, Zeilen nennen); die Prozesswache — endet der markierende Prozess, schließt der Broker den Briefkasten binnen höchstens 1 s dauerhaft für diesen Lauf; die Gegenfälle (a) markierender Prozess endet → Anfrage danach unbeantwortet, mit Rotbeweis an einem Brokertest, der einen Hilfsprozess des Tests als Sitzungsprozess nutzt (ohne FL, ohne Produktions-Pipe, nur Probe-Pipe), (b) markierender Prozess lebt, zweite Verbindung → Antwort wie bisher, (c) Marke ohne Kennung oder mit toter Kennung → kein Briefkasten (fail-closed), (d) `stoppe()` des Plugins und die 60-s-Leerlaufgrenze unverändert; die Runnererwartung (tools/fl/laufzeit.ps1, Szenario): nach dem Ende des Diagnose-FL keine Antwort mehr, ehrliche Meldung bei fail-closed. Berichtige den Notiztext von M-85 (b) („Broker läuft ohne Marke" ist für einen markierten Broker eines früheren Laufs falsch). Ergänze die Beziehungsliste des Briefkastens in §6 um starten↔stoppen und aktivieren↔abklingen. Zieh §7.4 (Etappe 5) nach: Startpfad, Prozesswache, Testvektor, Runnererwartung, die Restlücke „parallele User-Sitzung während eines Laufs" bleibt unter Restlücke benannt (Register NAK-310, §7.4). Je Zeile: Zustand, Ereignis, Zusage, Test (Datei, Name, Bein), Rotbeweis an der Zusage. Nenne, was am Code heute nicht bereitsteht (etwa eine Prozesswache im Broker) als Bauinhalt der Etappe 5, nicht als Zusage über vorhandenen Code.
2. **V-2, Regel R-310-8.** Berichtige den Satz in §7.0 (an ac69a9bd Zeilen 822-825; im heutigen Manifest per Textfund): Etappe 4 begrenzt nicht, wer die Hörmarkierung über v2 setzt. Nenne die Restlücke (Teil B `:380-388`, Register NAK-377) in §7.3 unter „Restlücke im Threat-Model". Keine neue Matrixzeile, keine Zusage über eine Setzerbegrenzung.
3. **V-3, Regel R-310-9.** Ergänze in §7.2 (Änderungssatz A: v2/v3-Entkopplung) und §7.3 (Commit 1: Bindung am Tor; Commit 2: v2-Übernahme) je einen Satz mit dem Edit an der A4-Behauptungszeile in tools/beweise.ps1 (Zeile nennen, Textfund vorher wörtlich zitieren, nachher als Vorschlag, der nicht mehr sagt, als der Test misst). Ordne in §8 die Prüflistenregel „drei Stellen" jeder Zeile mit Kanonbein zu, nicht nur M-25 und M-32.
4. **Ursache: Durchsicht der Etappen 2 bis 5.** Gehe §7.1 bis §7.5 Satz für Satz durch: Jeder Satz, der ein Verhalten verspricht, trägt eine Matrixzeile (nenne sie) oder wird als Bauinhalt ohne Zusage gekennzeichnet oder gestrichen. Prüfe je Etappe die Gegenstücke starten↔stoppen, verbinden↔trennen, öffnen↔schließen, aktivieren↔abklingen und Zusage↔Behauptungszeile; je fehlendes Gegenstück eine Zeile mit Test und Rotbeweis. Trage die Zuordnung als Tabelle in §12 ein (Bauplansatz · Matrixzeile · Kennzeichnung).
5. **Nachziehen.** §5 (Zusagen wörtlich), §7, §8 (Prüfliste; abgehakt für diese Nacharbeit) und die Beziehungsliste in §6 konsistent; der Kopf des Manifests bleibt Sache des Dirigenten (nicht anfassen). Schreibe am Ende einen Abschnitt „## 12. Matrixnacharbeit 1 (Bauer, 23.09.2026)" mit: HEAD zu Beginn, je Befund was geändert wurde (Zeilen M-nn, §-Absätze, Textfund vorher/nachher), die Zuordnungstabelle aus Punkt 4, Abweichungen von diesem Auftrag mit Begründung, Selbstaudit gegen tools/dirigent/pruefliste.md, Hinweise an den Dirigenten. Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

GRENZEN (R-310-6, Gate): kein Angriffscode; Identität eingefroren (NAK-30); Installer-Rückweg bytegleich; Probe-Pipe nie Produktions-Pipe; v2-Vertrag und v2-Pipename unverändert; Token je Broker-Lauf unverändert; keine neuen v3-Fehlercodes; kein neues Feld in einem v2- oder v3-Vertrag (die Prozesskennung ist ein Startargument des Brokerprozesses); Legacy-Bezeichner bleiben; Design geparkt. Weg 2 von U59 ist entfallen (Produktentscheid); Zeilen „nach U59" sind bindend. Entscheide des Dirigenten (§1 bis §4, §11) hältst du; hältst du einen für falsch, schreibst du es unter Abweichungen und baust trotzdem nach der Regel.

ABSCHLUSS: `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-310.md` sauber; Commit mit Pathspec (Betreff „NAK-310 Matrixnacharbeit 1: R-310-7 Sitzungsbindung der Marke (V-1), §7.0 berichtigt (V-2, R-310-8), A4-Behauptungszeilen (V-3, R-310-9), Durchsicht der Etappen 2 bis 5"), `git pull --rebase`, `git push`; nenne in der Schlussmeldung HEAD vorher und nachher und die Zahl der neuen und geänderten Matrixzeilen. Beende dich mit einer Zeile: FERTIG Matrixnacharbeit 1 NAK-310, <n> neue Zeilen, <n> geänderte Zeilen, HEAD <sha>.
