Die statisch nachvollzogenen Gegenbeispiele erzeugen falsche starke Befunde und verletzen die Evidenz- sowie Proposal-Verträge. Es wurden ausschließlich Quellen gelesen, keine Tests oder Produktprozesse ausgeführt. HEAD blieb bf92891b17b0b6f216dcf1b10a50ac2d9b756dc4; die Produkt-Diffs blieben leer.

Full review comments:

- [P1] Trenne korrelierte Kandidaten nicht allein durch Intent-Priorität — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1212-1216
  DEFEKT (B3): Im Aufbau `korrelierter_distraktor` mit zwölf identischen Fenstern pro Quelle und bekannten unterschiedlichen Mixerkanälen genügt ein vorheriges vollständiges `intent_update` mit `rolle=fuehrt` für den Distraktor. Seine höhere Intent-Relevanz erhöht den Gesamtrang; dieser Vergleich erklärt dadurch die messtechnisch identischen Kandidaten für getrennt. Der Distraktor erhält `hoch/ready_to_send`, die wahre Quelle wird seine Alternative. Damit erzeugt eine musikalische Priorität die nach G5/M-21 verbotene starke Ursachenbehauptung, obwohl keine zusätzliche unterscheidende Evidenz vorliegt.

- [P1] Berücksichtige ungemessene aktive Quellen vor einer Hochstufung — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese_verdrahtung.rs:165-168
  DEFEKT (B3/B4): Beide Quellen des Distraktorkorpus aktiv anmelden, die vollständige Intent-Marke setzen und zwölf Main-Fenster liefern; anschließend zuerst acht Distraktor-Fenster und erst später die Fenster der wahren Quelle zustellen. Weil diese Schleife ausschließlich Evidenzhistorien betrachtet, fehlt die bereits registrierte wahre Quelle vollständig. Der Distraktor gilt als einziger Kandidat automatisch als getrennt und wird `hoch/ready_to_send`; erst spätere Evidenz senkt ihn wieder ab. Das verletzt G5/M-21. Der Korpustest liest nur den abschließenden Snapshot und erfasst diese veröffentlichte Zwischenbehauptung nicht.

- [P1] Berücksichtige den Main bei der Parent-Duplikaterkennung — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1134-1138
  DEFEKT (B4): Main und passive Insertprobe auf Mixerkanal 0 mit vollständigem Intent und jeweils zwölf deckungsgleichen Fenstern anmelden. Die Verdrahtung setzt korrekt die Main-ID als Parent der Probe. `ids` enthält jedoch ausschließlich Nicht-Main-Kandidaten, und auch `ist_parent` durchsucht nur diese Kandidaten. Deshalb bleibt `duplikat=false`, und die Probe erhält allein `hoch/ready_to_send`, obwohl sie das Main-Signal dupliziert. Der Parent-Gegenbeispielvertrag aus G5/M-65 wird damit verletzt; der vorhandene Parent-Test verwendet ausschließlich zwei Nicht-Main-Kandidaten.

- [P1] Beschränke die Befundrechnung auf die benannte Passage — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1245-1246
  DEFEKT (B5): Eine Passage `[T,T+4096)` anlegen und Main sowie Probe jeweils acht 512-Sample-Fenster darin liefern lassen. Liefert anschließend nur der Main ein neuntes Fenster ab `T+4096` mit einer neuen Anomalie in einem anderen Band, liest diese Funktion genau dieses Außenfenster. Die Gates bestehen weiterhin: acht Fenster liegen je Teilnehmer innerhalb der Passage, und die auf die kürzere Spanne normierte Überdeckung bleibt 1. Der veröffentlichte Befund nennt die alte Passage, behauptet aber deren fremde Anomalie mit `hoch/ready_to_send`. Das verletzt M-23; die Passagenbegrenzung muss auch die tatsächlich verwendeten Messwerte begrenzen.

- [P2] Invalidiere Befunde beim Empfang einer Evidenzrücknahme — C:/Users/phili/Projekte/Nakama/eq-copilot/plugin/src/SourcesModel.cpp:1517-1517
  DEFEKT (B6): Nach einem Snapshot mit READY-Befund und Evidenz E invalidiert eine empfangene Rücknahme von E ausschließlich `eintraege[*].zeile.messung` (`uebernehmeEvidenzruecknahme`), nicht die neue Befundliste. Bleibt der nachfolgende Snapshot durch einen Pipe-Abbruch aus, gibt diese Sicht weiterhin den READY-Befund aus; auch dessen `darfAudition()` und `darfDraft()` bleiben wahr. Damit überlebt eine starke Behauptung ihre ausdrücklich zurückgenommenen Belege entgegen M-24/M-28 und der ehrlichen Zustandsanzeige aus [CLAUDE.md:75–76](C:/Users/phili/Projekte/Nakama/CLAUDE.md#L75-L76). Rücknahme und Verbindungsende müssen auch den neuen Befundzustand berücksichtigen.

- [P2] Verhindere Proposals mit leerem Ziel bei fehlender Führung — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/proposal_verdrahtung.rs:115-119
  DEFEKT (B7): Mit einem automatisch führenden Main vollständigen Intent, eine gültige Passage und Evidenz anlegen; anschließend ein zweites Main derselben Sitzung mit eigener ID und neutralem Heartbeat anmelden. Ohne ausdrückliche Führungswahl wird `fuehrendes_main=None`. Weitere Probe-Evidenz erzeugt weiterhin Vorschläge, deren Ziel hier zu `""` wird. `proposal()` und `vorschlag_persistieren()` übernehmen dieses `more_data`-Proposal ungeprüft in Modell und Store. Es fehlt somit das verlangte Ziel trotz vorhandenem Feld; das verletzt Exit-Gate Satz 2, M-42/M-43/M-46 und den persistenten Vertrag gemäß [AGENTS.md:51–52](C:/Users/phili/Projekte/Nakama/AGENTS.md#L51-L52).

- [P2] Begrenze Kandidaten vor der fokussierten Evidenzrechnung — C:/Users/phili/Projekte/Nakama/broker/src/coordinator/hypothese.rs:1139-1140
  DEFEKT (B8/M-18): Bei sieben gate-fähigen Quellen wird `rangkomponenten` hier siebenmal aufgerufen. Darin liegen bereits die 221-Band-Auswertung, der bedingte Uplift und gegebenenfalls der Block-Bootstrap. Erst nach diesen Rechnungen begrenzt `truncate(KANDIDATEN_DECKEL)` die Ergebnisliste auf fünf. Damit erreichen mehr als fünf Kandidaten die ausdrücklich begrenzte Stufe B, und deren Rechenaufwand wächst weiterhin mit sämtlichen Quellen. `screening_reicht_hoechstens_fuenf_weiter` prüft lediglich die abschließende Befundzahl und misst diese M-18-Zusage nicht.