Wiederprüfung S25c / Ticket NAK-224, Runde 1 — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer dieser Runde. Prüfbereich ist ausschließlich der Fixdiff der Runde:
`git diff c892648f3a11cac0d25971bb2f93da06c5748e0d...079c1604700ddc6f2c5ba0f01262427d90637524 -- broker/src/lebenslauf.rs broker/src/probe.rs tools/eq-copilot/pruefe_p5_korpus.py tools/beweise.ps1`
(ZIEL = HEAD = 079c1604700ddc6f2c5ba0f01262427d90637524, vor und nach deinem Lauf identisch). Der übrige Ticketbereich wurde bereits geprüft (Erstprüfung, Thread 01a08674-5ab8-7841-8469-eb52429fef6d) und ist hier nicht Prüfgegenstand. Rotbeweise der Runde als Kontext: docs/beweise/roh/NAK-224-rot-d1.txt, docs/beweise/roh/NAK-224-rot-d2.txt; Manifest docs/beweise/NAK-224.md §6 „Nacharbeit Runde 1“.

BEFUNDE, die diese Runde schließen sollte (wörtlich aus dem vorigen Urteil):
D1 — „[P2] Ziehe A29s Quellliste beim Modulschnitt mit — broker/src/coordinator/hypothese/mod.rs:50-53. DEFEKT gegen R2/A29: Nach einer gewöhnlichen Änderung an einem neuen Hypothesenmodul akzeptiert A29 weiterhin alte P5-Ergebnisse. `tools/eq-copilot/pruefe_p5_korpus.py::QUELLEN` nennt noch die gelöschte `hypothese.rs`; `_voraussetzung()` überspringt sie mittels `is_file()`. Die lesende Zeitstempel-Gegenprobe bestätigt: Eine neuere ursprüngliche Datei wird abgewiesen, eine neuere `hypothese/befund.rs` nicht. Reproduktion der verlorenen Abhängigkeit: `python -B -c "import runpy; m=runpy.run_path('tools/eq-copilot/pruefe_p5_korpus.py'); assert all(p.is_file() for p in m['QUELLEN'])"`. Die Quellenliste muss alle neuen Rechenmodule berücksichtigen, damit der Nachweis weiterhin den aktuellen Stand betrifft.“
Regel des Dirigenten zu D1: `QUELLEN` zählt alle Rust-Quellen unter `broker/src/coordinator/hypothese/` zur Laufzeit auf (rekursiv, sortiert) plus `hypothese_verdrahtung.rs`; eine genannte, aber fehlende Quelle ist eine fehlende Voraussetzung (Exit 3 mit Pfad), kein stilles Überspringen; die Frischeprüfung selbst bleibt unverändert.
D2 — „[P2] Schütze die neuen server-Imports mit cfg(windows) — broker/src/lebenslauf.rs:21-21. DEFEKT gegen den verhaltensneutralen Schnitt: Auf Nicht-Windows-Zielen wird jetzt bedingungslos `crate::server` importiert, obwohl `lib.rs` dieses Modul ausschließlich unter `#[cfg(windows)]` deklariert. Derselbe Fehler steht in `probe.rs:14`. Damit scheitert bereits der Bibliotheksbau mit E0432, statt den vorhandenen Nicht-Windows-Rückweg von `broker_starten` bereitzustellen. Reproduktion mit installiertem Linux-Target: `cargo check --manifest-path broker/Cargo.toml --lib --target x86_64-unknown-linux-gnu`. Beide `server`-Imports benötigen dieselbe Windows-Bedingung wie zuvor ihre Verwendungen; der Fehler wurde hier statisch geprüft, nicht cross-kompiliert.“
Regel des Dirigenten zu D2: beide `use crate::server`-Zeilen tragen `#[cfg(windows)]`, sonst nichts. Das Linux-Target `x86_64-unknown-linux-gnu` ist seit dieser Runde installiert; `cargo check --lib --target x86_64-unknown-linux-gnu` darfst du lesend fahren (es schreibt nur nach `broker/target/`).

GATE-TEXT, wörtlich (docs/plan/plan.json, Schritt S25c, Ticket NAK-224): „ANGELEGT 08.09.2026 (Pflegeschritt 1 nach G5, Register NAK-224). `broker/src/coordinator/hypothese.rs` trug am 08.09.2026 3 762 Zeilen, weil NAK-212 und NAK-213 dort gebaut haben. INHALT, ein einziger verhaltensneutraler Änderungssatz: die Datei nach Fachbereich teilen (Screening und Deckel, Zusammenhang und Rangkomponenten, Klassenwahl und Konkurrenzlage, Enthaltungen, Befundbau und Sicht), Historie erhalten (`git log --follow`), Modulköpfe wahr. BEWEIS: Kanon vorher = Kanon nachher grün mit gleicher Beinzahl, P5-Korpus bytegleich (A28/A29), Determinismus M-25 über hundert Läufe, `git diff --stat` ohne Zeile in Tests oder Fixtures außer Pfadänderungen. Muster: S19b (NAK-121). GRENZE: keine Regeländerung, keine neue Schwelle, kein Fassungsschritt; Nebenbefunde nummeriert ins Manifest, nie stumm behoben. NACHTRAG 08.09.2026 (Messung beim G5-Abschluss, Register NAK-234): `broker/src/transport/server_v3/mod.rs` liegt mit 2 007 Zeilen ebenfalls über der Grenze und gehört in denselben Änderungssatz; generierte Dateien unter `broker/src/generiert/` sind keine Pflegefläche (A9 hält sie bytegleich zur Neuerzeugung). Dazu NAK-233 (toter Parameter `veraltet` in `zustand_aus_sicherheit`), zu schließen in derselben Aufteilung, sofern die Zustandsabbildung M-29 unberührt bleibt.“

Du beantwortest genau zwei Fragen:
1. Ist jeder der genannten Befunde am Zielstand geschlossen (je Befund: ja/nein, mit Beleg)? Für D1: greift die Frischeprüfung jetzt auf jeder Datei unter `hypothese/` (lies `QUELLEN` und `_voraussetzung()`), und wird eine fehlende genannte Quelle als Voraussetzung gemeldet statt übersprungen? Für D2: tragen beide Importe `#[cfg(windows)]`, und gibt es im Fixdiff oder in den neuen Modulwurzeln dieses Tickets keinen weiteren unbedingten Import eines `cfg`-gebundenen Items?
2. Hat der Fixdiff etwas gebrochen — einen Test, eine Riegelzeile (R1 bis R10 in docs/beweise/NAK-224.md §3), einen Satz des Gate-Textes, eine Invariante aus CLAUDE.md? Insbesondere: A29 bleibt auf einer frischen Ergebnisdatei grün (Exit 0), die Windows-Bibliothek baut warnungsfrei, die geänderte A29-Behauptung in `tools/beweise.ps1` beschreibt, was das Skript misst.

Neue Befunde außerhalb dieser zwei Fragen erhebst du nicht. Befundklassen und Ausschlüsse wie in Variante A (docs/** ausgeschlossen; Härtungen NAK-235, NAK-236, NAK-237 und die Nebenbefunde NB-2/NB-3 des Manifests; Prüfliste keine Anforderung; Sabotage-Szenarien gegen Prüfwerkzeuge = Härtung).

URTEIL: PASS — alle Befunde geschlossen, nichts gebrochen.
URTEIL: NEEDS_WORK — je offenem Befund oder Bruch: Datei:Zeile, Reproduktion, welcher Satz bricht.
URTEIL: BLOCKED — Prüfbereich nicht lesbar.
Danach: geprüft / nicht geprüft, je ein Absatz.
