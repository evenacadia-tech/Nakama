Erstprüfung G4-Nacharbeit 3 / Ticket NAK-182 — gebundener Prüfauftrag des Dirigenten (2026-09-06)

Du bist der frische, lesende Prüfer. Prüfbereich ist ausschließlich der Änderungssatz dieses Tickets:
`git diff c4290fb15ebea9f30ca19772aeb6ed80a68ad2c7...054883faa9679c1ec2caf1310aa15452e4d56bd3 -- tools/eq-copilot/pruefe_p4_korpus.py tools/eq-copilot/erzeuge_p4_korpus.py tools/eq-copilot/erzeuge_v3_fixtures.py eq-copilot/fixtures/p4-korpus eq-copilot/fixtures/v3/evidenz-0p01-paar-wire-v1.json eq-copilot/plugin/tests/Sonde013StereoGoldenTest.cpp eq-copilot/plugin/tests/AnalysisGoldenTestMain.cpp eq-copilot/plugin/tests/Sonde013EventWireTest.cpp eq-copilot/plugin/tests/Sonde013TruePeakGoldenTest.cpp eq-copilot/plugin/tests/LoudnessGoldenTestMain.cpp eq-copilot/plugin/tests/QueueStressTestMain.cpp eq-copilot/plugin/tests/SondeNullTestMain.cpp broker/tests/sonde013_verdrahtung.rs broker/src/coordinator/experiment_verdrahtung.rs tools/beweise.ps1`
mit BASIS = c4290fb15ebea9f30ca19772aeb6ed80a68ad2c7 und ZIEL = 054883faa9679c1ec2caf1310aa15452e4d56bd3 (HEAD; er muss vor und nach deinem Lauf identisch sein). Alles andere, was seit BASIS im Repo geändert wurde, gehört zu anderen Tickets und ist hier nicht Prüfgegenstand. Eine uncommittete Datei docs/beweise/roh/NAK-182-erstpruefung-auftrag.txt im Worktree ist dieser Auftrag selbst, kein Prüfgegenstand.
Prüfstufe T3-Nacharbeit zum Phasengate G4 (docs/beweise/G4.md §11 Nr. 3), verlangt vom Plan für den Schritt „Klangberechnung und Vergleichslogik unabhängig prüfen".

GATE-TEXT, wörtlich und vollständig (docs/FL-Nakama-Sonden-Design-Entwurf.md:4287, P4-Exit-Gate Satz 1):
„Referenzkorpus, Loop-/Seek-/PDC-Goldens und adversariale Vergleichsfixtures bestehen."
Dazu die Falsifikationsziele des Gates §49.2 Gate 6 (:3560) „Ein nicht vergleichbares Experiment erhält ein starkes Siegerurteil." und Gate 7 (:3561) „Eine Standard-Insertprobe wird als exakter Mastersummenbeitrag bezeichnet." — beide müssen im Prüfwerkzeug A26 als falsche Behauptung erkannt werden.

Mehr verlangt das Gate nicht. Die Verhaltensmatrix steht in docs/beweise/NAK-182.md, Abschnitt §2 (bindend sind die jeweils jüngsten Fassungen aus Etappe 1b, Matrixnacharbeit 1 und 2 — Stand 7a5755c, Matrixprüfung 3 PASS, Thread 01a075a7-7f27-7882-9c7b-529d4ebe41a4), mit den Zusagen M-79 bis M-85 aus docs/beweise/SONDE-013.md:621-627 als Referenz; sie ist Referenz. Die Regeln des Dirigenten: R1 bis R7 (docs/beweise/roh/NAK-182-auftrag.txt), R1a (docs/beweise/roh/NAK-182-etappe-1-entscheid-auftrag.txt), MN1-1 bis MN1-7 (docs/beweise/roh/NAK-182-matrixnacharbeit-1-auftrag.txt), MN2-1 und MN2-2 (docs/beweise/roh/NAK-182-matrixnacharbeit-2-auftrag.txt). Der Bau ist in §6 des Manifests beschrieben; §6.4 nennt zehn Abweichungen vom Bauplan mit Begründung — eine Abweichung ist kein Defekt, solange die Zusage der Zeile weiter gemessen wird.
Die Riegel/Tests, die es messen: A26 `tools/eq-copilot/pruefe_p4_korpus.py`, A27 `pruefe_p4_korpus.py --selbsttest` (neu), A25 `erzeuge_p4_korpus.py --pruefen`, A8 `erzeuge_v3_fixtures.py --pruefen`, B19 `Sonde013StereoGoldenTest.cpp` (Sweep über 18 Blockgrößen), B5 `AnalysisGoldenTestMain.cpp` (`impulse_time_is_stable_over_block_sizes_and_sample_rates`), B16 `Sonde013EventWireTest.cpp` (`focused_evidence_wire_carries_0p01_db_steps`), A4 `broker/tests/sonde013_verdrahtung.rs` (`dekodierte_evidenz_findet_gain_mit_intervall`), Rotbeweise docs/beweise/roh/NAK-182-rot-*.txt (zwölf), Beinausgaben docs/beweise/roh/NAK-182-beine.txt. Jüngster Kanon: GRÜN 54/54 auf 3ca500f (docs/beweise/roh/NAK-182-3ca500f.md); 3ca500f..054883f ändert nur docs/**.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: verletzt einen Satz des Gate-Textes oben, eine Zeile der Verhaltensmatrix (jüngste Fassung), eine Regel R/R1a/MN ohne benannte Begründung in §4.4 oder §6.4, einen Test oder eine Invariante aus CLAUDE.md (Identität eingefroren, Schemas sind Verträge — v3-Encodings und Discriminatoren unangetastet, bytegleiche Fixtures, Audio echtzeitfest, Nulltest bitidentisch, State verlustfrei, NaN-Ehrlichkeit). Reproduzierbar, mit Kommando; je Defekt: Datei:Zeile und der Satz, der bricht. Ein Test, der seine Matrixzeile nicht misst (falsche Toleranz, falscher Bezugswert, Erwartung trivial erfüllbar, Rotbeweis fällt an einem Nebeneffekt statt an der gewerteten Zeile), ist ein Defekt.
- LÜCKE: Gate, Matrix und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, keine Erwähnung als NEEDS_WORK-Grund):
1. docs/** — Manifeste, Register, Planstand, Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände, Kommentar-Wortlaute. Manifeste sind Kontext, kein Prüfgegenstand; die Rotbeweis- und Beindateien unter docs/beweise/roh/ liest du als Beleg, erhebst aber keine Befunde über ihre Form.
2. Im Register datierte Härtungen und Lücken: NAK-155 bis NAK-179, NAK-183 bis NAK-197, NAK-190 (M-85 Ziel 1, korrelierter nicht kausaler Distraktor, ist P5-Lieferumfang — seine Abwesenheit ist kein Defekt; der Korpus druckt die Lücke). Die Nebenbefunde §4.7 Nr. 1 bis 6 und §6.7 Nr. 1 bis 5 des Manifests (kein Intervall im Dreifachergebnis; M-81 „Standard-Testmaterial" für LUFS-I nicht erfüllt; Klassen-Brier; `bisEvidenz` zählt Blöcke; alte Harnischfixture mit `q_db_0p1_i16`; adaptive Flussschwelle; `guardrail_loudness_db`/`guardrail_peak_db` hart None; `SondeProcessor` auf dem Stack in SondeNullTestMain) zieht der Dirigent ins Register — keine Befunde dieses Tickets. Ein Befund gegen ein Prüfskript, einen Riegel oder den Runner, der absichtliche Sabotage in repo-eigenen Quellen voraussetzt, ist HÄRTUNG — das Bedrohungsmodell der Riegel ist die versehentliche Regression, nicht der Angreifer mit Schreibrecht.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. §2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lückenlos") gilt für Gate-Audits, nicht für diesen Ticketreview. Kosmetik, Stil, optionale Härtung, theoretische Randfälle, Ticketfremdes (NAK-180, NAK-181, ein externes Loudness-Testset, ein Intervallfeld im Paarurteil, Umbau der alten Harnischfixture): nein.

WAS DU TUST:
1. Lies den Diff im Prüfbereich.
2. Prüfe am Zielstand, ob Tests und Riegel messen, was Gate und Matrix verlangen. Du darfst lesen und lesende Kommandos fahren (rg, git, cat; die Python-Prüfskripte A26, A27, A25 --pruefen und A8 --pruefen sind lesend und dürfen laufen); Compiler, Cargo und Test-Exes startest du nicht; du schreibst nichts. Die Fragen:
   (a) R1a/R2/R6 im Prüfskript: Sind `KEINE_BEHAUPTUNG` und `KEINE_STARKE` eine einzige Quelle (Erzeuger → MANIFEST → Prüfer, keine Kopie), lesen `_passt` und `moegliche_starke` sie, ist jede Wahrheit außerhalb `ERLAUBTE_WAHRHEITEN` rot, riegelt `_riegel` `falsche_starke`, `falsche_schwache`, `precision <= 1`, `recall <= 1` und das Brier-Gate, und misst A27 jeden Riegel mit Gegenteil (Korpusweg für `_passt`, Kennzahlweg für die Quoten)? Erkennt A26 die Gate-6- und Gate-7-Verletzung im Korpus (stark auf `unvergleichbar`/`nicht_exakt`/`nicht_kausal`, schwach auf `unbekannt`/`unvergleichbar`/`zeitvariabel`) — belegen die Rotbeweise M-79-unvergleichbar, M-85-gate7, M-85-verteilt, M-83-schwach das an der gewerteten Zeile? Steht jeder der 25 Korpusfälle mit `quelle` und wörtlichem Bezeichner in der genannten Datei, ist die Suche wörtlich, und macht ein entfernter Bezeichner A26 rot (Rotbeweise M-79-fallname, M-81-bezeichner)? Ist die Distraktor-Lücke im MANIFEST (`nicht_gemessen`) und im A26-Druck, und misst die Wache der fünf Schemamengen (kausale Bezeichner) am echten Schema?
   (b) M-82 in B19: Steht die Blockgrößenmenge genau einmal, stimmt sie mit der belegten Definition (1 bis 16384; 18 Werte aus QueueStressTestMain.cpp:666-667), misst der Sweep je Größe die analytische Antwort der fünf Klassen plus Folddown mit Präsenzbits UND vergleichbare Framezahlen (Nebenbefund `bisEvidenz` umgerechnet), nennt die Bein-Behauptung die 18 Größen und nie „über alle"? Fällt der Rotbeweis M-82 (Kanalversatz um ein Sample bei Blockgröße ≠ 512) an den Zeilen, die er treffen soll — §6.4 Nr. 7 sagt, `sweep_known_delay` bleibt dabei grün, weil ein Sample bei 900 Hz innerhalb der Phasentoleranz liegt: ist die Zusage „bekannte Laufzeit über die Blockgrößen" damit noch gemessen, oder ist ihre Toleranz so weit, dass der Test nichts unterscheidet?
   (c) M-84 in B5: Ist die Ereigniszuordnung t0-frei (§6.4 Nr. 4: stärkstes Flussereignis statt erstes — hält das MN2-1, und ist die Erwartung „kein Ereignis vor t0 − 4096" oder ein Äquivalent gemessen)? Ist `stromSample` bei jeder der 5 Sampleraten über die 5 Blockgrößen exakt gleich, gilt `0 <= t0 - stromSample < 4096`, und fällt der Rotbeweis M-84 (+4096 bei Blockgröße ≠ 512) an der Gleichheitszeile?
   (d) M-83 Satz 2, die gekoppelte Kette: Trägt `evidenz-0p01-paar-wire-v1.json` Material, Pegel, G und Auflösung als Zahlen (Form B), ist sie im Erzeuger registriert und von A8 bytegleich gehalten, und lesen beide Hälften dieselbe Datei? C++-Hälfte (B16): erzeugt die echte FeatureEngine den Snapshot mit `q_db_0p01_i16`, wird G je Band mit Bit innerhalb ±0,1 dB aus dem eigenen Wire-Text zurückgewonnen, und misst die Leiter 20 Schritte zu genau 1? Rust-Hälfte (A4): geht der Snapshot durch den echten Wirepfad (Beginn, Kandidat, Terminal), liest der Test `band_delta_db` je Band mit Bit ±0,1 dB über `resultatmessung_fuer_test` (delegiert die Auskunft ausschließlich, kein Feld, keine Verhaltensänderung, kein Schema?), und enthält `intervall` G mit beiden Grenzen ±0,1 dB? Fallen die Rotbeweise M-83-skala (Zehnteln der Werte) an der bandweisen Zeile und M-83-aufloesung an der Leiterzeile — und ist die in §6.3 verworfene Variante (Encoding-Umschaltung trifft die Bereichsprüfung) korrekt als Nebeneffekt erkannt?
   (e) R7: Sagen die geänderten Bein-Behauptungen (A26, A27, B19, B5, B16, ggf. B9/A4) nur, was gemessen wird, und ist A27 wirklich im Kanon-Runner geführt (Kuerzel, Name, Art, AbPhase)?
3. Prüfe, ob der Änderungssatz seit BASIS etwas am Produktverhalten gebrochen hat: der einzige Produktcode ist `resultatmessung_fuer_test` (broker/src/coordinator/experiment_verdrahtung.rs) — delegiert er ausschließlich, ohne Seiteneffekt, ohne neues Feld? Sind bestehende Testbezeichner, Testziele und `fn`-Namen unverändert (nur hinzugefügt)? Bleibt A5 `pruefe_v3_vertrag.py` unberührt und grün (Kanon)? Nenne, was du selbst nachgemessen und was du nur gelesen hast.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Matrix bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
