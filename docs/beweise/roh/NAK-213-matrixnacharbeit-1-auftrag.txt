NAK-213 — Matrixnacharbeit 1 (Etappe 1, Runde 1). Ticketauftrag des Dirigenten (2026-09-08).

DU BIST der frische Opus-Bauer (max). Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start. Aufsicht ENG. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (docs/beweise/NAK-213.md); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`. Kein Produktcode, kein Test, keine Fixture — diese Runde ändert ausschließlich die Verhaltensmatrix in docs/beweise/NAK-213.md. tools/dirigent/pruefliste.md ist deine Arbeitsliste.

LAGE: Matrixprüfung 1 (Codex gpt-6-astra max, lesend, Thread 01a07e85-64c7-7ab2-9e51-da2565fb0a61, Stand 8e6ce55) ergab NEEDS_WORK — Urteil wörtlich in docs/beweise/roh/NAK-213-matrixpruefung-1-8e6ce55.txt, Prüfauftrag in docs/beweise/roh/NAK-213-matrixpruefung-1-auftrag.txt. Der Dirigent hat alle neun Befunde an der Quelle bestätigt: sieben sind DEFEKTE der Matrix, zwei sind LÜCKEN, für die der Dirigent unten die Regel entschieden hat (R6, R8). Du schließt genau diese neun, sonst nichts. Für jeden Befund gilt: die betroffenen Entscheide in §2.0 und Zeilen in §2.1–§2.9 werden so umgeschrieben, dass die Zusage am Code an HEAD und am gebauten Vertrag wahr ist; je Zeile weiter Test (Datei::Fall) und Rotbeweis oder ausdrückliche Kennzeichnung als Kontrollfall/Regressionswache. Zeilen-IDs K-01 bis K-48 bleiben stabil; neue Zeilen bekommen die nächste freie Nummer (ab K-49). Nichts, was der Prüfer nicht erhoben hat, kommt dazu.

DIE NEUN BEFUNDE UND JE DIE REGEL, DIE IHN SCHLIESST:

1. DEFEKT E8/K-40–K-44 — produktiver Heartbeat-Pfad fehlt. Prüfer: „Der bezeichnete Ingress ist `descriptor_setzen`, der unter `broker/src` keinen Produktaufrufer hat. Produktive Kanaländerungen erreichen dagegen über `befehl.rs:669` den getrennten Pfad `heartbeat_kontakt`, der den Deskriptor direkt ersetzt (`liveness.rs:405–431`)." Bestätigt: liveness.rs:676 sagt selbst, dass `descriptor_setzen` nur aus einer Integrationstestdatei aufgerufen wird; `heartbeat_kontakt` vergleicht bei `runtime_gemeldet` nur `measurement_position` (:406–420) und ersetzt den Deskriptor (:431). REGEL: E8 nennt `heartbeat_kontakt` als den produktiven Ingress des Kanalwechsels (den Deskriptorersatz bei `runtime_gemeldet`); `descriptor_setzen` bleibt höchstens Testfläche. Mindestens eine Zeile misst den Kanalwechsel über einen echten Heartbeat (`befehl.rs`-Weg) mit Rotbeweis; K-41/K-42 (Erstwert gesetzt/weggelassen) gelten für denselben Pfad.

2. LÜCKE E1/K-07 — mehr als 32 Ausschlüsse. Prüfer: „Ein Main mit 38 gate-fähigen Sonden liegt innerhalb von `SESSION_CLIENT_CAP = 64`. Der neue Schnitt erzeugt dafür 33 verschiedene Ausschlüsse am führenden Befund. `session_finding.ausschluesse` erlaubt aber höchstens 32 (`eq-ipc-v3.schema.json:1900–1904`); `SourcesModel.cpp:950–953` verwirft andernfalls den Snapshot." Bestätigt. REGEL R6 (Dirigent, 08.09.2026): Jeder ausgeschiedene Kandidat trägt seinen Grund (M-87) — die Ausschlussliste wird nie gekappt. Deshalb steigt `ausschluesse.maxItems` in derselben Fassung 4 (E3/E9) auf `SESSION_CLIENT_CAP` (64), gespiegelt in Vertrag, Rust-Prüfung, C++ (`SourcesModel.cpp:950`) und Python (`pruefe_v3_vertrag.py`), mit Grenztest: ein Befund mit 33 Ausschlüssen ist in Fassung 4 gültig und in Fassung 3 ungültig (Rückbauprobe, Muster K-46), ein Befund mit 65 Ausschlüssen ist ungültig. Der Rust-Writer erzeugt nie mehr Ausschlüsse als Quellen in der Sitzung; eine Zeile misst das am Rand (63 Sonden plus Main). Die Zeile K-07 wird um diesen Rand ergänzt oder es entsteht eine eigene Zeile.

3. DEFEKT E1 — Onset-/Peak-Koinzidenz aus dem Screening gestrichen. Prüfer: „Entwurf §36.2 (`:2371–2373`) und M-18 (`SONDE-014.md:737`) verlangen ausdrücklich, diese Koinzidenz bereits im Screening einzubeziehen. Ihr vollständiges Weglassen ist keine widerspruchsfreie Präzisierung. Die angeführte technische Begründung trägt nicht: `hypothese.rs:1040–1042` berechnet die Onset-Korrelation ohne Uplift oder Bootstrap." Bestätigt: `koinzidenz_gerichtet` liest gemeinsame Reihen und rechnet eine gerichtete Korrelation, ohne Uplift und ohne Bootstrap. REGEL: Die Screeningordnung (E1) verbindet Gruppenenergie (64er-Gitter), paarweise Überlappung, Onset-/Peak-Koinzidenz (dieselbe Funktion `koinzidenz_gerichtet`, dieselben Reihen) und musikalische Priorität — wie Entwurf §36.2 und M-18 sie nennen. Sie bleibt eine eigene Ordnung (keine siebte Rangkomponente, M-25 unberührt), deterministisch quantisiert mit demselben Tie-Break. Der Nachtrag zu M-18 in §2.8 wird entsprechend berichtigt. Eine Zeile misst, dass bei gleicher Energie, Überlappung und Priorität die Koinzidenz den Schnitt entscheidet — nicht die ID.

4. DEFEKT E6/K-24 — `host_mixer_index = 0` ist vertragswidrig. Prüfer: „`host_mixer_index = 0` ist im bestehenden Vertrag ausdrücklich ungültig: Der rohe Hostindex beginnt bei 1 (`eq-ipc-v3.schema.json:731–735`). Auch `SourcesModel.cpp:310–314` weist 0 ab." Bestätigt (`minimum: 1`; „Außerhalb gilt der Hostwert als nicht geliefert"). REGEL: Alle NAK-213-Aufbauten verwenden nur vertragsgültige Indizes (≥ 1). K-24 wird zu „Kanal 1 ist ein Kanal; kein Kanal ist das Fehlen des Feldes; ein Wert < 1 gilt laut Vertrag als nicht geliefert, also als Fehlen" — mit Test und Rotbeweis wie bisher. „Master auf 0" in K-44 und in §2.7 (Korpussitzungen, die dieses Ticket ohnehin ändert: Fälle 1–4) wird auf einen gültigen Index umgestellt, soweit der Korpuserzeuger diese Sitzungen in Etappe 2 anfasst; der Rest des NAK-212-Korpus bleibt unberührt (Grenze).

5. DEFEKT K-33 — Rückzug des zweiten Mains stellt die Führung nicht her. Prüfer: „In der übernommenen Ausgangslage K-31 sind beide Mains unbestätigt. `beitritt_aufheben` setzt ausschließlich die Mitgliedschaft des zurückgezogenen Clients zurück (`mitgliedschaft.rs:168–175`); es bestätigt das verbleibende Main nicht. Die Führungswahl berücksichtigt aber nur bestätigte Mains (`:74–87`), sodass `fuehrendes_main` weiterhin `None` bleibt." Bestätigt. REGEL: K-33 nennt als Ereignisfolge den Rückzug des zweiten Mains UND die Bestätigung des verbleibenden Mains über den bestehenden Bestätigungsweg (welcher, steht an der Quelle: mitgliedschaft.rs); erst danach ist die Führung eindeutig und die nächste Rechnung liefert Befunde. `mitgliedschaft.rs` bleibt unverändert (§2.9 Nr. 9). Falls K-31 in Wahrheit einen anderen Ausgangszustand hat (ein bestätigtes Main, dann Beitritt eines zweiten), beschreibe beide Lagen je als eigene Zeile mit dem Code als Beleg.

6. DEFEKT E9/K-46 — der C++-Leser wählt die Fassung nicht. Prüfer: „Werden ausschließlich die Enum-Liste und die maximale Fassungszahl erweitert, akzeptiert der neue C++-Leser die beiden neuen Gründe auch in einem ausdrücklich als Fassung 3 eingehenden Snapshot: `SourcesModel.cpp:532–535` prüft nur die Obergrenze, die Liste in `:963–969` ist fassungsunabhängig. Der vorgesehene Rust-Rückbau würde denselben Snapshot abweisen. Das verletzt die Fassungswahl aus `reservierte-nachrichten-v1.json:11` und M-77." Bestätigt. REGEL R7 (Dirigent, 08.09.2026): Der C++-Leser akzeptiert `screening_ueberboten` und `master_duplikat` nur in Snapshots mit `schema_minor >= 4`; ein Snapshot der Fassung 3 mit einem der beiden Gründe ist ungültig — dieselbe Regel wie im Rust-Rückbau (M-77). E9 nennt damit drei Berührungen in SourcesModel.cpp (Enum-Liste, Fassungsbindung der neuen Werte, Ausschluss-Obergrenze aus R6) plus `WireEnvelope.h`; K-46 bekommt die C++-Gegenprobe (Bein B13, Fassung-3-Snapshot mit neuem Grund → ungültig) mit Rotbeweis.

7. DEFEKT K-47 — der Rotbeweis für `kJsonSchemaMinor` tritt nicht ein. Prüfer: „Dessen Aufnahmehelfer (`Sonde012SourcesModelTest.cpp:205–209`) verwendet den Overload, der dieselbe Konstante als eingehende Fassung einsetzt (`SourcesModel.h:235–240`). Beim Rücksetzen auf 3 sinken daher Eingangsminor und Leserobergrenze gemeinsam; der Vergleich in `SourcesModel.cpp:532` entdeckt die Abweichung zu Fassung 4 nicht." Bestätigt. REGEL: K-47 erhält einen messenden Rotbeweis für `kJsonSchemaMinor`: ein B13-Fall, der den Snapshot mit explizit vorgegebenem `schema_minor = 4` über den Overload mit Fassungsparameter einliest (bleibt `kJsonSchemaMinor` auf 3, fällt er an `:532`), und/oder ein Textriegel in `pruefe_v3_vertrag.py`, der `kJsonSchemaMinor` gegen `aktuell` im Register liest (Muster `metrics_version_bindet_schwellen`). Mindestens einer der beiden; beide sind zulässig. Damit ist NB-3 innerhalb der Ticketgrenze, soweit er die fünf Fassungszahlen betrifft.

8. DEFEKT K-44(a) — vollständige Rücknahme entfernt den Befund, sie macht ihn nicht `stale`. Prüfer: „Im genannten Aufbau referenziert Cs Befund ausschließlich Cs zwölf Fenster. Werden sämtliche IDs zurückgenommen, entfernt der unverändert zugesagte Invalidierungsweg diesen Befund vollständig; `Stale` wird nur gesetzt, wenn gültige IDs verbleiben (`hypothese_verdrahtung.rs:579–588`)." Bestätigt (M-28: „Unsichtbar heißt wirklich fort"). REGEL: K-44(a) erwartet für einen Befund, dessen Belege vollständig zurückgenommen sind, die Entfernung (M-24/M-28-Weg unverändert); `stale` nur für Befunde mit verbleibenden gültigen IDs. Die Zeile unterscheidet beide Lagen und misst beide, mit Rotbeweis wie bisher.

9. LÜCKE E7/K-38 — Sitzung ohne verbliebene gültige Evidenz. Prüfer: „Nach vollständiger Evidenzrücknahme bleiben die Sitzungsschlüssel erhalten; eine später durch eine andere Sitzung angestoßene Rechnung sammelt auch diese Sitzung erneut (`hypothese_verdrahtung.rs:72–80,135–137`). K-19 greift nicht, denn früher wurden Belege geliefert. E7 verlangt nun eine Enthaltung ohne Masterbeleg, kann deren Pflichtliste aber nicht gültig füllen: Zurückgenommene IDs werden beim Eintragen abgewiesen (`:844–860`, M-28), eine leere Liste verletzt `minItems: 1`." Bestätigt. REGEL R8 (Dirigent, 08.09.2026): Eine Enthaltung ohne Ort setzt mindestens einen gültigen (nicht zurückgenommenen) Beleg der Sitzung voraus. Ohne einen solchen entsteht kein Befund — K-19 wird präzisiert auf „keine gültige Messung" statt „nie gesendet" —, und die bestehenden Befunde der Sitzung laufen über M-24. Eine neue Zeile misst den Fall: alle Belege einer Sitzung zurückgenommen, Rechnung durch eine andere Sitzung angestoßen → kein Befund, kein Vertragsbruch; Rotbeweis: ohne die Regel entstünde eine Enthaltung mit leerer oder ungültiger `evidence_ids`.

VERLAUF: Trage im Manifest §5 (Verlauf, append-only; lege §5 an, falls es fehlt) einen Eintrag „Matrixprüfung 1 — NEEDS_WORK (08.09.2026, Stand 8e6ce55)" mit Thread, Modell, Effort, Rohdatei und der Zuordnung Defekt/Lücke der neun Befunde ein, und darunter „Matrixnacharbeit 1" mit der Liste der geänderten Entscheide und Zeilen. Kopftabelle: Zeile „Phase" auf „Etappe 1 — Matrixnacharbeit 1 nach Matrixprüfung 1 (NEEDS_WORK)"; Regeln-Zeile um R6, R7, R8 (Quelle: diese Auftragsdatei docs/beweise/roh/NAK-213-matrixnacharbeit-1-auftrag.txt). Der lebende Kopf und §1–§2 werden umgeschrieben, §5 nur angehängt.

SELBSTAUDIT vor dem Commit: Jede geänderte Zeile gegen den Code an HEAD und den Vertrag lesen (Zahlenränder 32/33/64/65, Index 1, Fassung 3/4, leere ID-Menge, vollständige gegen partielle Rücknahme); keine Zusage ohne Test und Rotbeweis; keine Manifestsätze, die mehr behaupten als der genannte Test misst. Dann `py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-213.md` (Tabellen- und Verweisriegel) grün, Commit mit Pathspec docs/beweise/NAK-213.md, Betreff „NAK-213 Matrixnacharbeit 1: …", pull --rebase, push, STOPP. Die Matrixprüfung 2 setzt der Dirigent.
