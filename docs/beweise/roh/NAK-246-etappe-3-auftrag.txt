NAK-246 Etappe 3 — D2: Besitz laufender Callbacks (M-06 bis M-09). Fortsetzungsauftrag des Dirigenten (2026-09-11) zum Ticketauftrag docs/beweise/roh/NAK-246-auftrag.txt (Abschnitt „ETAPPEN 2 BIS 6").

STAND. Etappe 1 (Matrix) ist mit PASS abgeschlossen auf 73d2374d (§7.8), Etappe 2 (D1) mit PASS auf d7325800 (§8.9). §3 (Matrix), §4 (Bauplan) und §5 (Entscheide) sind die Referenz für Bauer und Prüfer; du änderst sie nicht, du löst sie ein. Diese Etappe baut ausschließlich D2 (Manifest §1.5, §2.4, §3.2 M-06 bis M-09, §4.2, §5.2 mit Feinheiten 1 bis 8; Regel R-D2 im Ticketauftrag wörtlich; Abweichung 1 in §5.10: die Schleuse schließt NACH den drei stop()-Aufrufen des Destruktors, der synchrone negative Link-Callback läuft noch durch — so festgelegt und geprüft).

MODELL DES BAUERS (Dirigenten-Vermerk 11.09.2026): claude-fable-5-1[1m] mit --effort xhigh bis zum Wochenreset (User-Wort 11.09.2026: „doch bei 95 einfach weitermachen … schlimmstenfalls bricht es eben ab und wir müssen nach dem reset weitermachen wo wir aufgehört haben"); --permission-mode dontAsk. Bricht dein Lauf ab, gilt der committete Stand als Zwischenstand; deshalb committest du in kleinen, kompilierten, gemessenen Schritten (nie einen Stand mit NOT RUN) und pushst jeden Commit.

DU BIST der frische Worker im sichtbaren Checkout C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = git rev-parse HEAD beim Start (trage ihn in die Kopfzeile „Basis-SHA" als „Etappe 3: <sha>" ein); git status --short muss leer sein, sonst nichts anfassen und als letzte Zeile „NAK-246 HALT <grund>" melden. Aufsicht ENG. Keine Rückfrage an den User; technische Entscheidungen triffst du selbst an der Quelle und begründest sie im Manifest. Lies zuerst vollständig: den Ticketauftrag (Regeln, Ticketgrenze, Git), Manifest §1.5, §2.4, §3 (Einleitung und §3.2), §4.0, §4.2, §4.6, §4.8, §5.2, §5.10 Zeile 1, §8 (Muster eines Etappenabschnitts) und tools/dirigent/pruefliste.md; dazu die Quellen eq-copilot/plugin/src/PluginProcessor.cpp (alle zehn Callback-Registrierungen: :112-121, :164, :225, :304; Destruktor :324-337), eq-copilot/plugin/core/ipc/controlclient/Verbindung.cpp und Nachrichten.cpp (Aufrufstellen im ablösbaren Thread), ControlClient.cpp:119-135 (Detach), Sonde013PassageStateTest.cpp:713-744 (bestehender Produktclient-Aufbau gegen TestServer, nachnutzen).

GRUNDSATZ, wörtlich in jeder Zeile: Je Matrixzeile fällt der Rotbeweis an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt. Deckel und Zahlen kommen nur aus der Quelle.

WAS DU BAUST (Reihenfolge nach §4.2): (1) Die Schleuse als eigener Typ eq-copilot/plugin/core/ipc/controlclient/Schleuse.h (neu) nach §5.2: Besitz ohne Mutex im Callback-Pfad (nur Atomics), Zähler betreten/abgewiesen/gewartetMs, Schließen mit Warten auf laufende Callbacks (Condvar nur im Destruktor-/Schließpfad), Kanarienvogel-Muster für die Tests. B10-Fälle M-06 und M-07 an der Besitzerattrappe mit derselben Schleuse; Rotbeweis mit der Attrappe in Alt-Verdrahtung (rohes this, kein Warten): docs/beweise/roh/NAK-246-rot-M-06.txt, -M-07.txt (Kanarienvogel verändert bzw. Destruktor kehrt zurück, während der Callback gehalten wird). (2) Prozessor verdrahten: alle ZEHN Lambdas (sechs Konstruktor-Lambdas :112-121; beiP0Zugestellt und beiP0Verworfen :164; hookReplayBegin :225; hookKonfliktWiederholung :304) laufen durch die Schleuse, Destruktor-Reihenfolge nach M-06 (brokerLifecycle.stop() → telemetryV3.stop() → controlV3.stop() → Schleuse schließen → Mitglieder zerstören), Schleuse als shared_ptr-Mitglied; Testkonstruktor mit Pipe und Servererwartung per Delegation (der Produktkonstruktor bleibt unverändert, §5.2 Feinheit 5) und setzeV3AntwortHakenFuerTest. (3) B14-Fälle am echten Prozessor gegen den Testserver, unter Nachnutzung des B23-Aufbaus (v3ProbeGegenstelleFuerTest/v3StartFuerTest): M-06 abgeloester_produkt_callback_beruehrt_den_zerstoerten_prozessor_nicht (Haken an beiP0Verworfen oder einem anderen der vier nachregistrierten, wie §3.2 nach R-M1-1 festlegt), M-07 prozessorabbau_ueber_die_frist_wartet_den_produkt_callback_zu_ende (Prozessor auf dem Heap, reset() aus dem Testthread, Freigabe aus einem dritten Thread), M-09 alle_produkt_callbacks_laufen_durch_die_schleuse (betreten ≥ Summe der ausgelösten Ereignisse einschließlich P0-Rückmeldung und Replay-Begin, so wie M-09 es festlegt). Rotbeweise docs/beweise/roh/NAK-246-rot-M-09.txt (keine Schleuse: betreten existiert nicht bzw. Summe zu klein) und, wo §3.2 es verlangt, die B14-Varianten von M-06/M-07 gegen den Alt-Stand. (4) M-08: bestehende B10-Fälle 2, 2b, 2c (IpcTestMain.cpp:4395 ff.) bleiben grün, plus die neue Messung abgewiesen >= 1 nach Ablösung + Zerstörung + Freigabe (Regressionswache; Rohausgabe genügt). Vertragstext in ControlClient.h und TelemetryClient.h zu B-CC-10/B-TC-07 um den Besitzerteil ergänzen. Fix und Tests im selben Änderungssatz.

BEINE, einzeln aus pwsh mit den Kürzeln aus tools/beweise.ps1 (nie aus Bash — cmake liegt dort nicht im PATH): B10 EqCopIpcTest, B14 EqCopSonde012ProjectReloadTest, B8 EqCopLebenslaufTest, A22 pruefe_ipc_last.py, A1 EqCopNullTest, A16 EqCopProbeeqNullTest; dazu der Bau von EqCopilot_VST3 ohne neue „warning C". Rohausgabe gesammelt in docs/beweise/roh/NAK-246-etappe3-beine.txt (Kürzel, Befehl, Exit, Zeitstempel des Binaries gegen die Quelle; Landmine NAK-230 Copy-Item/LastWriteTime). Kein voller Kanon in dieser Etappe (§4.6). Landminen: NAK-175 (Prozessorobjekte in Tests auf den Heap, MSVC-Stack 1 MiB); Sperrenordnung der Schleuse (kein Mutex im Callback-Pfad); ein Destruktor, der auf einen Callback wartet, der seinerseits auf den Destruktor wartet, ist ein Deadlock — Abweichung 1 in §5.10 (synchroner Link-Callback aus stop() läuft VOR dem Schließen) ist genau dafür festgelegt; Testpipe-Namen eindeutig je Fall; A1/A16 bitidentisch. NAK-184 Weckungs-Teil (ControlClient.cpp:1023-1027, notify ohne Lock) bleibt außerhalb dieser Etappe — nicht anfassen, außer die Matrix verlangt es an einer Zeile (dann begründet in 9.6).

MANIFEST (append-only, neuer Abschnitt „## 9. Etappe 3 — D2: Besitz laufender Callbacks" nach §8.9, Aufbau wie §8): 9.1 Was gebaut wurde (Schleuse: Zustände, Zähler, Schließ- und Warteweg; die zehn Verdrahtungen mit Zeile; Destruktor-Reihenfolge; Testkonstruktor); 9.2 Entscheide und Feinheiten über §5.2 hinaus (mit Begründung; keine neue Regel ohne Quelle); 9.3 Messabdeckung als EINE Tabelle je Matrixzeile M-06 bis M-09 (Test Datei:Zeile, Rotbeweis Rohdatei mit fallender Zusage und Exit rot/grün, Belegklasse); 9.4 Beine mit Rohausgabe; 9.5 Prüfliste abgehakt mit Fundstelle; 9.6 Abweichungen vom Bauplan §4.2 oder von §5.2; 9.7 Nebenbefunde (nicht ins Register); 9.8 Selbstaudit (stale Zeiger, Lebensdauer, Sperrenordnung, Zahlenränder von Zählern und Fristen, gewartetMs-Messung, irreführende Vertragstexte). Kopfzeilen, die du anfasst: nur „Basis-SHA" (Etappe 3) und „Kanon nachher" (Beine der Etappe 3); die Zeile „Etappe" schreibt der Dirigent.

TICKETGRENZE dieser Etappe: eq-copilot/plugin/core/ipc/controlclient/Schleuse.h (neu), eq-copilot/plugin/core/ipc/ControlClient.h und TelemetryClient.h (nur der Vertragstext), eq-copilot/plugin/src/PluginProcessor.h und .cpp (Schleuse, zehn Lambdas, Destruktor, Testkonstruktor, Testhaken), eq-copilot/plugin/tests/IpcTestMain.cpp, eq-copilot/plugin/tests/Sonde012ProjectReloadTest.cpp, eq-copilot/plugin/CMakeLists.txt nur, falls eine neue Quelle oder ein Test-Define nötig ist (begründet), tools/beweise.ps1 nur für einen begründeten neuen Bein-Eintrag, docs/beweise/NAK-246.md, docs/beweise/roh/NAK-246-rot-M-0[6-9].txt, docs/beweise/roh/NAK-246-etappe3-beine.txt. Alles andere aus „NICHT ANFASSEN" des Ticketauftrags bleibt unberührt; nichts aus den Etappen 4 bis 6 (Ipc.cpp-Drain, Nachrichten.cpp-Dreiwert, Broker, SourcesModel, Vergleichspegel); keine Legacy-Umbenennung, keine Neben-Refactors.

GIT. Nie git add -A, nie --amend, fremde uncommittete Dateien nie anfassen; git status --short zuerst lesen. Commits nur mit explizitem Pathspec; Commitnachrichten über git commit -F <datei>, nie -m mit Backticks; Push nach git pull --rebase bei sauberem Worktree als eigener Schritt; nach jedem Commit pushen. Vor jedem Commit: py -3.13 tools/plan/dokuriegel.py docs/beweise/NAK-246.md ohne Befund (Pipe-Zeichen in Tabellenzellen als \|; Zeilenbereiche mit ASCII-Bindestrich). Kleine Werkzeugaufrufe (je Edit rund 250 Zeilen). Selbstaudit vor jedem Commit.

ENDE: Letzte Zeile deines Turns „NAK-246 ETAPPE 3 ABGESCHLOSSEN <voller sha>" (bei langem Thread „NAK-246 ETAPPE 3 TEILSTAND <sha>" mit dem, was gemessen ist und was fehlt). Keine Rückfrage an den User.
