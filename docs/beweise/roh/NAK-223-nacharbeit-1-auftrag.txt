NAK-223 (S25b) — Nacharbeit 1 nach der Erstprüfung (NEEDS_WORK, zwei Defekte). Auftrag des Dirigenten, 2026-09-09.

DU BIST der frische Opus-Bauer (max) für diese Nacharbeitsrunde. Repo C:\Users\phili\Projekte\Nakama, Branch master. Basis-SHA = `git rev-parse HEAD` beim Start (Worktree sauber). Aufsicht NORMAL. Git-Regeln: nie `git add -A`, nie `--amend`, nie fremde uncommittete Dateien anfassen; committen nur mit explizitem Pathspec (neue Dateien vorher `git add -- <pfad>`); vor dem Push bei sauberem Worktree `git pull --rebase`, dann `git push origin master`; mehrzeilige Commitnachrichten über `git commit -F <datei>`. Kein Produktcode, keine Neben-Refactors. Lösche nie mit dem PowerShell-Lösch-Cmdlet oder `rm -rf`; selbst angelegte Dateien nur mit `[System.IO.File]::Delete` auf exakte Pfade. tools/dirigent/pruefliste.md ist deine Arbeitsliste: vor dem Commit abhaken und im Manifest §3 nachziehen. Der Rotbeweis fällt je Zeile an der Zeile, die die Zusage trägt, nicht an einem Nebeneffekt.

TICKETGRENZE dieser Runde: nur `tools/plan/gesundheit.py`, die Rotbeweise `docs/beweise/roh/NAK-223-rot-deadcode.txt` und `NAK-223-rot-clippy.txt` (jeweils um einen Abschnitt erweitert, nichts Altes umgeschrieben) und das Manifest `docs/beweise/NAK-223.md` (lebender Kopf: Phase-Zeile, Änderungssatz „Nacharbeit 1"; §2.5 Heuristik-Text, wo er die Namenswahl oder die Clippy-Fehlerbehandlung beschreibt; §5 Verlauf: neuer Abschnitt „Erstprüfung — NEEDS_WORK (09.09.2026, Stand `d6a0bfda`)" mit Thread `01a08530-2bfd-7162-a0c8-d3ff058cc551`, Urteil wörtlich aus `docs/beweise/roh/NAK-223-erstpruefung-a1722a2.txt`, dann „Nacharbeit 1"). Nichts sonst: kein Runner, kein Skill, kein Playbook, kein Register, kein Plan, kein Produktcode.

DIE ZWEI DEFEKTE, wörtlich aus dem Urteil der Erstprüfung (Codex `gpt-6-astra`, max, read-only, Thread 01a08530, Stand d6a0bfda; `docs/beweise/roh/NAK-223-erstpruefung-a1722a2.txt`), je Defekt die Regel des Dirigenten, die ihn schließt (beide an der Quelle bestätigt: `gesundheit.py`:532–533 und :522):

DEFEKT 1 — „Erfasse bei const fn den tatsächlichen Helfernamen — tools/plan/gesundheit.py:532-533. DEFEKT: Bei einem unbenutzten `pub const fn helfer()` erfasst `ELEMENT` das Schlüsselwort `fn` als Namen. Jede andere Funktionsdefinition gilt anschließend als Verwendung; der aufruferlose Helfer bleibt ungemeldet. Lesende Reproduktion: `py -3.14 -B -c "import runpy; g=runpy.run_path('tools/plan/gesundheit.py'); print(g['finde_dead_code']([('t.rs',None)], {'t.rs':'#[allow(dead_code)]\npub const fn helfer() {}\nfn main() {}\n'}))"` liefert `[]` statt eines Treffers. Auch der virtuelle Gesamtmesslauf liefert damit 0/OK und Exit 0 statt 4. Das verletzt das Gate-Maß „aufruferlose `#[allow(dead_code)]`-Helfer“ und unterdrückt die vorgesehene Pflegemeldung. Funktionsqualifizierer müssen vor der Namensauswahl berücksichtigt werden."
REGEL R-D1: Die Namenswahl für das Element unter `#[allow(dead_code)]` überspringt alle Sichtbarkeits- und Funktionsqualifizierer vor dem Schlüsselwort — `pub`, `pub(crate)`, `pub(super)`, `pub(in …)`, `const`, `unsafe`, `async`, `extern "…"`, `default` — und liefert den Bezeichner unmittelbar nach dem Elementschlüsselwort; `const NAME: T` (Konstante, Name `NAME`) bleibt von `const fn name` (Funktion, Name `name`) unterscheidbar, ebenso `unsafe impl`/`unsafe trait`. Messung: Selbsttestfälle im Speicher für `pub const fn helfer`, `pub(crate) unsafe fn helfer`, `pub async fn helfer`, `pub const HELFER: u8 = 1;` und `unsafe fn helfer` — je mit und ohne Verwendung, die Erwartung mit ihrem Gegenteil; Rotbeweis `NAK-223-rot-deadcode.txt` um den Fall erweitert (Mutation: Qualifizierer-Überspringen zurückgenommen → der Selbsttest wird rot; Rücknahme → grün). Die Reproduktion des Prüfers muss danach genau einen Treffer `helfer` liefern.

DEFEKT 2 — „Verwirf Clippy-Ergebnisse eines fehlgeschlagenen Prozesses — tools/plan/gesundheit.py:522-526. DEFEKT: Bricht Clippy beispielsweise mit Compilerfehler E0308 und Exit 101 ab, steht die Fehlerdiagnose bereits in `fund`; dadurch überspringt `not fund` die Fehlerbehandlung. Die unvollständige Messung erscheint als gültige Warnungszahl. Reproduktion ohne Cargo-Ausführung: `py -3.14 -B -c "import runpy,pathlib,subprocess,shutil,json; g=runpy.run_path('tools/plan/gesundheit.py'); shutil.which=lambda _: 'cargo'; subprocess.run=lambda *a,**k: subprocess.CompletedProcess([],101,json.dumps(dict(reason='compiler-message',message=dict(level='error',code=dict(code='E0308')))),''); print(g['miss_clippy'](pathlib.Path.cwd()))"` liefert `(1, ['?:0:0 E0308'], '')`; der virtuelle Gesamtmesslauf endet damit sogar mit Exit 0. Das verletzt die zugesagte Messung der „cargo clippy-Warnungen“, auf der die Pflegeentscheidung beruht. Ein Prozessfehler muss unabhängig von vorhandenen Diagnosen als Messfehler weitergegeben werden."
REGEL R-D2: Ein Exitcode ≠ 0 des gestarteten `cargo clippy`-Prozesses ist ein Werkzeugfehler, unabhängig davon, ob Diagnosen gelesen wurden: `miss_clippy` liefert dann keine Zahl, sondern den Fehler (Exitcode und letzte stderr-Zeile), und der Gesamtlauf mit `--clippy` endet auf demselben Weg wie beim fehlenden Messort (Commit 092db4de) mit Exit 2 „Werkzeugfehler: cargo clippy Exit N — …“, nie mit 0 oder 4; „NICHT MESSBAR“ bleibt ausschließlich dem Lauf ohne `--clippy` und dem Fall „cargo nicht gefunden“ vorbehalten (Umgebung, kein Werkzeugfehler). Messung: Selbsttestfälle ohne Cargo-Ausführung (Prozessersatz wie in der Reproduktion des Prüfers): Exit 101 mit Diagnosen → Fehler; Exit 101 ohne Diagnosen → Fehler; Exit 0 mit 3 Warnungen → 3; Exit 0 ohne Diagnosen → 0; Rotbeweis `NAK-223-rot-clippy.txt` um den Fall erweitert (Mutation: Bedingung zurück auf `not fund and …` → Selbsttest rot; Rücknahme → grün). Die Reproduktion des Prüfers muss danach keine Zahl mehr liefern.

BEINE dieser Runde: `py -3.13 tools/plan/gesundheit.py --selbsttest` (gezielt und vollständig, Fallzahl im Manifest — sie muss um die neuen Fälle steigen), `py -3.13 tools/plan/gesundheit.py` (Messlauf ohne clippy: Tabelle unverändert gegenüber §2.1 außer den Werten, die dieses Ticket selbst bewegt hat; zwei Läufe bytegleich), `py -3.13 tools/plan/gesundheit.py --clippy` einmal echt (Zahl gegen die Ratsche 91 — sie darf nicht reißen; Laufzeit im Manifest), danach das Bein A32 genau so, wie der Runner es aufruft (`py -3.13 tools/plan/gesundheit.py --mit-selbsttest` aus dem Repo-Root; erwarteter Exit 4 wegen der fünf bekannten Dateien über der Grenze, alles andere unverändert; der Runner hat keinen Einzelbein-Schalter, kein voller Kanon — den setzt der Dirigent). Kein C++-Bein, kein Vertrag.

SELBSTAUDIT vor dem Commit: Diff adversarial lesen — Determinismus (zwei Läufe bytegleich), keine Änderung an Schwellen oder Tabellenform, Exit-Semantik 0/2/4 in der Docstring-Beschreibung und im Skill-Satz weiterhin wahr (der Skill wird in dieser Runde nicht angefasst; prüfe nur, dass sein Satz wahr bleibt), keine irreführenden Kommentare, nichts außerhalb der Ticketgrenze berührt (`git diff --stat` zeigt nur die genannten Dateien).

ABSCHLUSS: ein Commit (Pathspec), gepusht, Worktree sauber; melde dich erst, wenn `git status --short` leer ist und HEAD auf origin/master liegt.
