# Offene Punkte — durables Offen-Set

> Regeln: Offene Einträge werden beim Auffrischen NIE gelöscht. Schließen
> nur mit ID + Commit-SHA (oder expliziter User-Entscheidung). Neue
> Out-of-scope-Funde kommen als datierte Zeile mit ID dazu.
> Nur Nakama-/eq-copilot-Punkte — FL-Studio-Workspace-Themen (MIDI,
> Vorlagen, Plugin-Hub-GUI) bleiben dort.
> Seit 21.08.2026 gibt es eine dritte Tabelle **Geparkt**: Punkte der
> Prisma-Studie, die der User geparkt hat — weder Arbeit noch erledigt.

## Offen (Stand 2026-08-23)

| ID | Seit | Punkt |
|---|---|---|
| NAK-64 | 08-23 | **Umschnitt auf den Entscheid „Gen wird EQ-Zentrale"** (Register 23.08.; Erratum (n); Wortlaut `design/abnahmen/2026-08-23-gen-eq-zentrale.md`). In EINEM sauberen Änderungssatz, nicht nebenbei: (1) `docs/bauaufteilung-sonden.md` und die Schritt-Texte von `docs/plan/plan.json` P6–P7 umschneiden — EQ-Bedien-UI einmal (Gen Seite 2) statt je App, Suna-Ziel stilllegen (das gebaute `Nakama Suna.vst3` bleibt Verlauf in den S9-Manifesten; Code `NkPr` bleibt reserviert), Rückfallfläche der Sonde als eigene Zeile; (2) Cap-Semantik §44.2 vom User entscheiden lassen (Vorschlag im Erratum: ±3 dB bindet den Advisor, User-Edits über Gen = manueller Weg ±12 dB) — steht als **U14** in `docs/plan/fragen.json`; (3) klären, wie die Suna-spezifischen Kanon-Beine (A15 `EqCopSunaNullTest`) im Merge weiterleben — die NullTest-Pflicht selbst wächst (EQ aus = bitidentisch gilt künftig auch für Gen mit Master-EQ und die Sonde mit EQ aus); (4) **Nachtrag 23.08., Interview struktur R2:** Mix/Dry-Wet je Sonde als versionierte Parameter-Erweiterung (neue ID — `nakama-parameter-v1.json` kennt global nur Bypass/Trims/Width/Mono-Bass), User-Wort „und natürlich mix also wieviel sie auf den mix anwendet des eq (das gehört aber auch noch in den Gen ganz wichtig)" (`design/abnahmen/2026-08-23-interview-struktur.md`). Bis dahin: Erratum (n) hat Vorrang, plan.json S26–31 beschreibt den alten Schnitt. |
| NAK-65 | 08-23 | **Figma-Flächen zum EQ-Zentrale-Entscheid — liegt beim User:** (1) Gen Seite 2 (EQ-Zentrale; seine Design-Vorgaben, Register 23.08.: Sonden-Durchschalter AUF der Seite, zwei EQ-Spuren in EINEM Graph farblich unterschieden); (2) Minimal-Rückfallfläche der Sonde (Vorschlag, Zuschnitt offen). Der Probeeq-Stand `design/assets/figma/2026-08-22-probeeq.png` ist durch den Entscheid Verlauf; ob seine EQ-Optik die Basis der Gen-Seite wird, entscheidet der User in Figma. Größenfrage offen: die abgenommenen 760×430 galten der Übersicht (20.08.); ob Seite 2 mehr Fläche braucht, klärt sein Entwurf. |
| NAK-59 | 08-23, S12–13 | **Band-Stereo hat keinen Platz im v3-Binärvertrag.** Entwurf §56 verlangt von der FeatureEngine ausdrücklich „64-Live- und 221-Evidenzbänder, Gültigkeitsbitmap, **Band-Stereo** und Ereignisse". `SONDE-009` berechnet es (Seitenanteil S/(M+S) je Live-Band, mit eigener Bitmap — `FeatureFrame::liveBreite`/`liveBreiteBitmap`, Bein B5 misst es an einem L==R-Signal gegen 0). **Auf die Leitung kommt es heute nicht:** `table Frame` in `eq-copilot/schemas/v3/flatbuffers/nakama_telemetry_v1.fbs` trägt genau EINEN `Bandwerte`-Satz je Frame und `breite` nur als Skalar (id 8); ein zweiter bandaufgelöster Satz ist dort nicht vorgesehen. Bewusst **nicht** nebenbei geändert: eine `.fbs`-Änderung heißt neues Feld mit eigener id, `flatc`-Codegen, Drift-Test und beide handgeschriebenen Leser (C++ und Rust) nachziehen — das ist der Schnitt, den SONDE-005b beschreibt, und er gehört zu `SONDE-010`, wo der Telemetry-Client der erste echte Verbraucher wird. Wer es angeht: entweder ein `baender_stereo:Bandwerte`-Feld mit neuer id (dann tragen beide Leser die „genau ein Träger gesetzt"-Regel auch dafür), oder im Entwurf festhalten, dass Band-Stereo eine rein lokale Größe bleibt — dann gehört dieser Satz in §56 und nicht in eine Sitzungsnotiz. Kein Produktcode hängt heute daran. |
|---|---|---|
| NAK-07 | 08-14 | Dauerbetrieb: Loudness inkrementell, Pipe-Deadlines. **Umgewidmet 08-21 (Kontext-Interview):** der Rest des alten Änderungssatzes 2/3 lebt in der Sondenlinie weiter — fixed-memory Loudness ist `SONDE-008` (Entwurf §65), Backpressure/Deadlines der Pipe sind `SONDE-010`. **Erste Hälfte geschlossen 23.08. mit `SONDE-008`** (`7fa1cf5`): `kZellen` und der 4×/s neu allozierte Zweitdurchgang sind weg, der `LoudnessAccumulator` braucht 120 252 Byte unabhängig von der Laufzeit — eine Million Zellen mit **0 Allokationen** vorgeführt, Gegenprobe der alten Rechnung 36 (Kanon-Bein B9). **Offen bleibt** Backpressure/Deadlines der Pipe (`SONDE-010`). |
| NAK-10 | 08-17 | Broker liest das `hoermarkierung`-Heartbeat-Flag noch nicht (serde verwirft still; `protokoll.rs` MessStand ohne Feld) — Aggregat soll laut `eq-ipc.schema.json` während Markierung pausieren/markieren. Bewusst nach dem Harness-Lauf, s. HOER-MARKIERUNG-KONZEPT. Entwurf §31.2 führt es als offenen Vertrag; Zielphase P2 (§56). |
| NAK-13 | 08-18 | Broker-Autostart fehlt: Früher startete die Hub-App den Broker automatisch; jetzt muss `eqcop-broker.exe` von Hand laufen, sonst misst das Plugin nur lokal (verbindet per Backoff, sobald der Broker da ist — kein Datenverlust, aber keine Paare/Aggregate). Klären: Autostart-Eintrag, Task-Scheduler oder Start auf Zuruf. Konzeptantwort im Entwurf §48.3 (Start nur durch klassifiziertes Main mit offenem Editor, `SONDE-007b`) — ungebaut. |
| NAK-25 | 08-21 | **Der Baustand-Riegel von `tools/beweise.ps1` ist zu grob geworden** (Fund beim Abschluss von S3b, out of scope). Er bildet EINEN globalen „neueste Quelle“-Zeitstempel über alle Quellorte und vergleicht ihn gegen JEDE Prüfbinärdatei (`beweise.ps1`, Schleife um `$neuesteQuelle`). Seit `hostprobe/` dazugekommen ist, gilt damit z. B. `EqCopNullTest` (Bau 18.08.) als veraltet, obwohl seine eigenen Quellen unberührt sind — ein Lauf OHNE `-Bauen` endet dadurch immer auf Exitcode 4. Die Richtung stimmt (er beglaubigt nie fälschlich, er verweigert nur zu oft), und mit `-Bauen` schlägt das Urteil des Buildsystems den Zeitstempel — der Normalfall bleibt also grün. **Bewusst NICHT jetzt gefixt:** eine Zuordnung Quelle→Ziel wäre echte Arbeit und würde einen Riegel anfassen, der sich in dieser Session zweimal bewährt hat. Wer es angeht: je Ziel die eigenen Quellorte führen, nicht global vergleichen. |
| NAK-24 | 08-21 | **`hatTransport` ist im VST3-Pfad eine Tautologie** (Fund aus `SONDE-003`, out of scope dieses Tickets). `PluginProcessor.cpp:140` (seit 22.08.; vorher `:135`) setzt `hatTransport = true`, sobald `getPosition()` irgendein `PositionInfo` liefert — und JUCEs VST3-Playhead gibt **nie** `nullopt` zurück (`juce_audio_plugin_client_VST3.cpp`, `getPosition()` endet auf `return info;`). Ein fehlender `ProcessContext` sieht damit aus wie „Transport vorhanden, Position 0". Ebenso wird `projektZeitSamples` nie gelöscht, wenn der Context wegfällt. Die Hostbrücke liefert seit `1e91d54` das ehrliche Bit (`processContextPresent`); der Umbau des Processors darauf gehört zu `SONDE-008/009` (P2) und wird dort mit den zeitgestempelten Featureframes zusammen gemacht — nicht nebenbei. **GESCHLOSSEN 23.08. mit `SONDE-008`** (Commits `7fa1cf5` + Folgecommit): der Prozessor ist eine `Senke` der Hostbruecke; `hatTransport` heisst jetzt „Transport ist BEKANNT" und kommt aus `processContextPresent && playing.gueltig`, nicht mehr aus „irgendein PositionInfo kam". Zweite Haelfte ebenfalls zu: die Projektzeit traegt ein eigenes Gueltigkeitsbit (`StatsSnapshot::projectTimeValid`), der Heartbeat sendet `project_time_samples` ohne Beweis als **null** statt als alten Wert. Manifest `docs/beweise/SONDE-008.md` §4.5/§5. |
| NAK-26 | 08-21 | **`pluginval.exe` liegt nur unter `%TEMP%`** (`C:\Users\phili\AppData\Local\Temp\pluginval.exe`, Fund beim Abschluss von SONDE-005a, out of scope). Der Beweiskanon zitiert es als Host-Härtungsschritt (`--strictness-level 8`), und die Manifeste von SONDE-003/003b/005a stützen sich darauf — aber Windows räumt `%TEMP%` auf, und auf dem Zweitrechner existiert es gar nicht. Wer es angeht: an einen dauerhaften Ort legen (z. B. `tools/extern/`, nicht ins Repo — Fremdbinärdatei) und im Beweis-Runner als eigener Kanon-Eintrag mit „Voraussetzung fehlt"-Zweig führen, damit sein Fehlen sichtbar ist statt stillschweigend übersprungen zu werden. **Nachtrag 23.08. (S9):** Die Kopie liegt weiterhin dort und hat den Lauf am geänderten Main-Bundle getragen (`docs/beweise/SONDE-007b.md` B13, SUCCESS). Der Punkt bleibt damit **unverändert offen** — er beschreibt nicht, dass die Datei fehlt, sondern dass sie flüchtig ist. **Kostenbeleg für die Dringlichkeit:** beim Selbstaudit desselben Tickets habe ich sie nicht gefunden (gesucht unter `Program Files`, `C:\Users\phili`, `C:\tools`, `C:\bin` — nicht unter `%TEMP%`), daraufhin fälschlich „nicht auffindbar" ins Manifest geschrieben und einen doppelten offenen Punkt NAK-53 angelegt. Beides ist zurückgenommen und in B13 offen protokolliert. Ein Werkzeug, dessen Ort nur in einer Zeile dieses Dokuments steht, wird genau so wieder verlorengehen. |
| NAK-27 | 08-21 | **`offline_render_detection` ist keine Capability aus §53.6** (Fund in T2-Runde 1 zu SONDE-005a). Das v3-Schema hatte sie selbst erfunden; sie ist entfernt, weil der Capabilityvertrag §53.6 abschliessend ist und das Objekt strikt. Die Faehigkeit selbst wird von `EqCopHostProbe` (S3b) real gemessen. Wer es angeht: entscheiden, ob §53.6 im Entwurf um eine elfte Capability erweitert wird (dann Schema, Fixtures und alle drei Beine nachziehen) — oder ob Offline-Render dauerhaft nur eine Messgroesse ohne Capabilitybit bleibt. Kein Produktcode haengt heute daran. |
| NAK-28 | 08-21 | **`subscribe_session` hat keinen Gegenpfad** (Fund in T2-Runde 1 zu SONDE-005a). Alle anderen Paare sind vollstaendig (`preview_begin`↔`preview_end`, `audible_intervention_begin`↔`end`, `apply`↔`revert`, `experiment_begin`↔`abort`), aber die 25er-Liste aus Entwurf §33.3 kennt kein `unsubscribe_session`. Die CLAUDE.md-Invariante zu Gegenpfaden und §67 Punkt 2 (Unsubscribe) fragen es woertlich ab. Weil die 25 geschlossen ist und ein unbekannter Discriminator abgelehnt wird, ist das spaeter KEIN additiver Nachtrag. Wer es angeht: entweder §33.3 auf 26 erweitern, oder im Entwurf festhalten, dass das Verbindungsende der Gegenpfad ist — dann gehoert dieser Satz in §33.3 und nicht in eine Sitzungsnotiz. |
| NAK-29 | 08-21 | **Bedingte Feldpflichten im Transportstempel sind nicht durchgesetzt** (Fund in T2-Runde 1 zu SONDE-005a, gemessen: alle vier Faelle sind heute gueltig). `time_basis=project_samples` + `validity.project_time=true` OHNE `project_sample_start`; `time_basis=local_monotonic` MIT `validity.project_time=true` (direkter Widerspruch zu §32.3); `cycle.bounds_valid=true` ohne `start_ppq`/`end_ppq`; `cycle.bounds_valid=false` mit `derivation=validated_block_mapping`. Die Mechanik dafuer existiert (diskriminierte Union, in dieser Runde zweimal neu angewandt), aber der Schnitt ist groesser als ein Fix: `transportstempel` muesste ueber `time_basis` diskriminieren und `cycle` ueber `bounds_valid`. Gehoert zu `SONDE-009` (FeatureEngine v2, Zeit- und Validity-Vertraege), wo der Erzeuger dieser Felder entsteht. **GESCHLOSSEN 23.08. mit `SONDE-009`** (Commit `357786e`): `nak29Verstoss()` in `plugin/core/analysis/FeatureEngine.h` setzt alle vier genannten Faelle durch — und **zwei weitere**, die aus demselben §32.3-Satz folgen („mit eigenen Gueltigkeitsbits gespeichert") und sonst dieselbe Luecke an anderer Zeile waeren: ein gesetztes `cycle_bounds`-Bit ohne die PPQ-Werte, ein gesetztes `continuous_time`-Bit ohne den Wert. 🔑 **Geschlossen im ERZEUGER, nicht im Empfaenger:** ein Frame mit verletztem Stempel wird gar nicht erst veroeffentlicht (Zaehler `nak29Abgelehnt()`), der Empfaenger sieht die Luecke an der springenden `sequence`. Ein Riegel im Empfaenger faenge den kaputten Stempel erst, nachdem er ueber die Leitung ging, und muesste in JEDEM Empfaenger stehen. Rueckgabe ist eine **Nummer** statt eines bool — ein Riegel, der nur „nein" sagt, laesst offen, ob er aus dem richtigen Grund nein sagt; Bein B5 §L3 misst, dass sechs Verletzungen sechs verschiedene Nummern ergeben. Der v3-SCHEMA-Schnitt (diskriminierte Union in `eq-ipc-v3.schema.json`) bleibt davon unberuehrt und ist weiterhin offen — er gehoert zu `SONDE-010`, wo der Envelope-Parser entsteht; bis dahin kann kein Nakama-Erzeuger einen solchen Stempel bauen. Belege `docs/beweise/SONDE-009.md` §4.5, Bein B5 Abschnitt H. |
| NAK-30 | 08-21 | **Umbenennung zu „Nakama Studio" / Gen / Probeeq / Suna ist eine Identitätsänderung** (User 21.08.: „Nakama Gen = Main app · aktive sonde = Nakama Probeeq · passive sonde = Nakama Suna · Bundle = Nakama Studio"). `eq-copilot/identity/plugin-identities-v1.json` friert neben den CIDs auch `bundle` (`EQ-Copilot.vst3`) und `produktname` (`EQ-Copilot`) ein; die CIDs leiten sich laut Manifest aus `Evna`+`Eqcp`/`NkPr`/`NkAc` ab, nicht aus Namen. Zu klären, nicht entschieden: Braucht die Umbenennung ein `plugin-identities-v2.json`, und bleiben die CIDs dabei unverändert (dann laden alte FL-Projekte weiter), oder ändert sich mit dem Bundle-Dateinamen etwas, das FL als neues Plugin sieht? Erst messen (Scan-Fixture, `EqCopIdentityTest`), dann umbenennen — nie als Nebenbei-Refactor der `EqCop*`-Namen. |
| NAK-31 | 08-21 | **`tools/analyze-track.py` (Erzeuger der Golden-Referenz) liegt nur im FL-Studio-Repo** (`C:\Users\phili\FL-Studio\tools\analyze-track.py`, dazu `match-reference.py`; im Nakama-Repo 0 Treffer). `erzeuge_fixtures.py` sucht es erst unter `tools/`, dann per dokumentiertem Fallback unter `~/FL-Studio/tools/` (`lade_analyze()`, Z. 144–158); Kalibrier-Protokoll und M1-/CS1-Befund referenzieren es als `tools/…`; der GoldenTest misst gegen seine Achse. Ins Nakama-Repo holen (mit Abhängigkeiten), sonst ist die Golden-Referenz auf dem Zweitrechner nicht regenerierbar. |
| ~~NAK-32~~ | 08-21 | **GESCHLOSSEN 23.08. mit `ff0e0b8`** (S9/SONDE-007b Abschnitt 3): `eq-copilot/.gitignore` trägt jetzt `install/*` mit zwei Ausnahmen — versioniert sind `Install-Nakama.ps1` (manifestgetrieben, mit Rückweg-Hälfte) und `nakama-installer-v1.json`; Bundles, Rollbacks und `install-ergebnis.json` bleiben Maschinenartefakte. ⚠️ `install/*` statt `install/`: mit Schrägstrich steigt Git nicht in den Ordner hinab und die Ausnahmen bleiben wirkungslos. Das alte `Install-EQ-Copilot.ps1` bleibt unangetastet und weiterhin ignoriert — es installiert den 16.08.-Stand mit drei Hash-Literalen und ist durch den neuen Weg ersetzt, nicht gelöscht. Belege: `docs/beweise/SONDE-007b.md` B12. Der ursprüngliche Text: **`eq-copilot/install/` ist gitignoriert** (`eq-copilot/.gitignore:2`): `Install-EQ-Copilot.ps1`, Staging-Bundle, vier Rollback-Bundles und `install-result.txt` existieren nur auf dem Desktop; CLAUDE.md nennt das Skript als den einzigen Installationsweg. Das Skript versionieren (ohne Bundles und Rollbacks — die bleiben Maschinenartefakte). |
| NAK-33 | 08-21 | **Parameterzahl je Band: 12 (Figma) gegen 13 (Entwurf §53.8).** Der Figma-Stand Probeeq (21.08.) zeigt BAND · TYPE · FREQ · GAIN · Q · MODE \| DYN · THRESH · RANGE · ATTACK · HOLD · RELEASE; §53.8 definiert je Slot zusätzlich `sidechain_source` (`none\|internal\|priority_sidechain`), und `enabled` erscheint im Figma-Stand nur als Griff-Zustand auf der Kurve. Offen, nicht entschieden: ist `sidechain_source` ein sichtbarer Parameter, ein Untermenü von DYN, oder entfällt er im ersten Active-Release? Entwurf-Errata (h). **Nachtrag 08-22 (SONDE-006):** der State trägt alle **13** IDs (`eq-copilot/schemas/state/nakama-parameter-v1.json`) — die mechanisch sichere Obermenge: ein nicht sichtbarer Parameter bleibt auf Default `none` und kostet nichts, ein später nachgeschobener hätte eine neue Layout-Version erzwungen (IDs werden nie umbenannt, §53.8). Offen bleibt damit nur die UI-Frage (sichtbar / Untermenü / unsichtbar) für den Figma-Stand — Entscheid beim User. |
| NAK-34 | 08-21 | **`EqCopHostProbeTest` zählt 89 nur mit PNG-Argument, der Kanon misst 85.** Mit Zielpfad laufen vier zusätzliche Bildbeweis-Prüfungen (`HostProbeTestMain.cpp:727-766`); das zweite Bild (Messzustand) entsteht per `replace("leerzustand","messzustand")` am Dateinamen (`:755-756`) — heißt die Datei nicht „…leerzustand…", überschreibt das Messbild das Leerbild still, und beide Prüfungen bleiben grün. Kanon-Eintrag B3b läuft ohne Argument (85/85 in allen Manifesten seit SONDE-005a). Wer es angeht: zwei feste Zielnamen statt Namensersetzung, und den Bildbeweis in den Kanon holen oder die 89 nirgends mehr als Kanonzahl führen. |
| NAK-35 | 08-21 | **Hör-Markierungs-Verriegelung: Code und bisherige Doku sagen Verschiedenes.** `PluginProcessor.cpp:202-207` (seit 22.08.; vorher `:197-202`): `(echtzeitOk ∨ testEchtzeit) ∧ (spielt ∨ ¬hatTransport) ∧ ¬isNonRealtime ∧ (editorOffen ∨ testEchtzeit)`. Die CLAUDE.md bis 21.08. (`e2b553e^`) beschrieb vier UND-Bedingungen („bewiesene Echtzeit ∧ Editor offen ∧ Transport ∧ !isNonRealtime"); Code ist Wahrheit, die Doku ist nachgezogen. Offen bleibt die Produktfrage: ist `¬hatTransport ⇒ erlaubt` (fail-open ohne Transportinfo) gewollt? Entwurf §0.1 führt den Zweig als „formal fail-open, praktisch tot" (weil `hatTransport` im VST3-Pfad immer wahr wird — NAK-24) und verlangt für die Zielmigration gültiges `playing=true`. Zusammen mit NAK-24 in `SONDE-008/009` entscheiden; dazu der User-Hörtest in FL (vormals NAK-03 §18), sobald Gen seine Oberfläche hat. **GESCHLOSSEN 23.08. mit `SONDE-008`** (Commits `7fa1cf5` + Folgecommit): die Produktfrage hat der User am 22.08. im Hub (`U10`) beantwortet — „Nein, nur mit Signal". Der Term `(spielt ∨ ¬hatTransport)` ist gefallen und heisst jetzt `spieltGueltig ∧ spielt`. In FL aendert das nichts (der Zweig war dort tot); headless faerbt ohne Playhead nichts mehr, weshalb Markierungs- und Lebenslauftest einen tragen. Der User-Hoertest in FL bleibt davon unberuehrt und gehoert weiter zu Gens Oberflaeche. Manifest §5. **Nachtrag T2 23.08.:** die Schließung ist sachlich bestätigt (Positivprobe: ohne gültiges „spielt" faerbt kein einziges Sample), **aber kein Kanon-Bein deckt den Term** — das fail-open laesst sich zurueckbauen und alle vier Audio-Beine bleiben gruen. Der fehlende Riegel ist als **T2-2 in NAK-58** gefuehrt; diese Zeile bleibt geschlossen, weil sie die Produktfrage betraf, nicht die Absicherung. |
| NAK-36 | 08-21 | **Broker-Release-Binaries sind älter als die Quellen.** `broker/target/release/eqcop-broker.exe` und `eqcop-broker-probe.exe` vom 18.08. 11:43; Quellen zuletzt 21.08. (`Cargo.toml` 17:50, `telemetrie.rs` / `contract_cross_language.rs` 18:26). `cargo test` baut nur `debug`. Vor dem nächsten Betrieb oder Pipe-Ende-zu-Ende-Lauf `cargo build --release --manifest-path broker/Cargo.toml`. |
| NAK-37 | 08-21 | **`EqCopAuxSpikeTest` (41 Prüfungen, `SONDE-004a`) läuft nicht im Kanon** — `tools/beweise.ps1` kennt es weder als „jetzt" noch als „geplant" (grep 0 Treffer), während das Schwester-Messgerät `EqCopHostProbeTest` Kanon-Eintrag B3b ist. Das Gerät trägt die Messung mit dem höchsten Erkenntnisrisiko (Termin A). Entweder als Kanon-Eintrag führen, bis S4 das Gerät entsorgt, oder die Asymmetrie im Runner begründen. |
| NAK-38 | 08-21 | **UI-Rückfluss-Liste Design ↔ Entwurf** (Errata (h)): Rollen 5 (Design-Spez.) vs. 7 (§9) · Suna-Kachel mit Name/Rolle-Bedienung (Design) vs. „null Hostparameter, Bedienung über Main" (§0.4) · Messzustände 5 vs. 2 · Arbeitsschritte 3 vs. 5 · Main-Übersicht: Figma-Stand 21.08. zeigt ein Quellen×Band-Gitter, Abnahme 20.08. sagte Overview+Detail ohne Heatmap — ob das Gitter die Heatmap aus §35.1 ist, klärt die Übersetzung. Rückfluss laut §0.4 Punkt 2 vor dem P3-Gate; Entscheid je Punkt beim User (Figma-Stand = neues User-Wort). | offen |
| NAK-39 | 08-21 | **„12 von 20 Kernfunktionen gewählt" (Entwurf §0.2/§22) ist nicht rekonstruierbar** — weder die Zwanziger-Liste noch die Auswahl existiert in einer Datei (Kontext-Inventur). Gilt als Codex-Annahme der Fassung 0.1 (Errata (m)). Frage an den User, wenn der Kernfunktionen-Satz je beschnitten oder erweitert werden soll. | offen |
| NAK-40 | 08-22 | **`instance_id` im State ist bytegleich zur Schema-1-`sensor_id`, die v3-Adresse verlangt hex32** (Fund in SONDE-006). §32.2 verlangt „`sensor_id` bleibt bytegleich als `instance_id` erhalten"; reale IDs sind `juce::Uuid` (32 Hex-Kleinbuchstaben) und passen, aber `adresse.instance_id` in `schemas/v3/eq-ipc-v3.schema.json` ist `$defs/hex32` (`^[0-9a-f]{32}$`), und die eingefrorenen Schema-1-Goldens tragen eine UUID **mit Bindestrichen**. Der State bleibt bytegleich (Vertrag `nakama-state-v2.md` §3); wer `SONDE-010` baut, legt fest, wie eine nicht-hex32-`instance_id` auf die v3-Adresse kommt (ableiten + im State mitführen, oder den Altstand sichtbar neu kennzeichnen) — nie still. |
| NAK-44 | 08-22 | **Termin A2 — PDC-Golden, Bus-Identität, Kanalreihenfolge, FL-Version** (T2-Befunde zu SONDE-004). Termin A hat einen getrennten, recall-stabilen Sidechain-Weg mit Versatz 0 gemessen, aber **kein Plugin im Aufbau meldete Latenz** — FLs PDC hatte nichts zu kompensieren, der Fall aus Entwurf §32.2 („Fehlreihenfolgen bei latenzbehafteten Instanzen") ist ungemessen; deshalb steht `aux_priority_sidechain` **unsupported** (§53.6 „PDC-synchroner echter Sidechain"). Ebenso unentscheidbar: welche Quelle auf welchem Aux ankam und die L/R-Reihenfolge (dieselbe Impulsdatei, L = R, auf allen drei Spuren); die FL-Version von Termin A ist nicht protokolliert (installiert 25.2.5.5319 und 26.1.4.5589). Termin A2: auf der Sidechain-Quellspur ein latenzmeldendes Plugin, je Spur ein anderer Impulszeitpunkt, einmal L-only/R-only, FL-Version notieren (Stand-Hinweis in `eq-copilot/docs/FL-TERMIN-A-AUX-PDC.md`). Hebt bei Erfolg `aux_priority_sidechain` und `aux_compare_pre` (T2-Runde 2: derselbe Maßstab — §32.2 „Topologie, Kanalreihenfolge und PDC" — gilt für beide Aux-Bits); zusammen mit einem Impulsgolden je Bus (NAK-43) auch `presentation_latency`. |
| NAK-43 | 08-22 | **`presentation_latency`: ein verworfener Wertwechsel ohne Protokoll** (Termin B, `host-probe-20260822-132644.json` `presentation_latency.verworfene_wertwechsel = 1`). Die Host-Probe hält je Richtung den ERSTEN gemeldeten Wert (Eingang 3 924, Ausgang 4 410) und zählt spätere andere Werte nur — welcher Wert wann kam (Render? Projektwechsel?), steht nicht im Bericht. Bevor `SONDE-009` die Presentation-Latency für die Cross-Probe-Ausrichtung nutzt: die Probe um eine kurze Werthistorie (Block, Richtung, alt → neu) erweitern und den Fall in FL reproduzieren. Bis dahin steht das Bit `presentation_latency` auf `unsupported` (T2-Runde 1 zu SONDE-004; `host-capabilities-fl-v1.json` `offen`). |
| NAK-42 | 08-22 | **Rust klassifiziert die ungültigen DTO-Fixtures nicht** (T2-Befund zu SONDE-006). `broker/tests/contract_cross_language.rs` kanonisiert und hasht nur `dto_gueltig`; die 15 `dto/ungueltig/` mit Grund liest nur C++ (`NakamaParameter::ausDtoText`) und Python (`erzeuge_state_fixtures.py`). §65 braucht es für SONDE-006 nicht, aber §55 („C++ und Rust klassifizieren jedes gültige und ungültige Fixture identisch") wird es am P1-Exit (G1) verlangen. Eigentümer: SONDE-016 (der Broker verifiziert dort `apply_transaction`-DTOs) — spätestens vor G1 ein Rust-DTO-Validator gegen `nakama-parameter-v1.json` mit denselben Grundwörtern. |
| NAK-41 | 08-22 | **Schema-2-Stand in einem alten Build (≤ 0.3.0 vom 16.08.)**: der alte `setStateInformation` verlangt den Baumtyp `EqCopilotState` und ignoriert `NakamaState` still — die Instanz bekäme eine frische zufällige `sensor_id` und Rolle `sensor` (kein Absturz, aber stiller Identitätsverlust). §55 „Rollback" meint den Weiterbetrieb des alten Builds mit seinen eigenen Projekten, nicht das Rückladen neuer Projekte. Folge für die Installation: der 0.3.0-Stand mit Schema 2 wird erst installiert, wenn der User das weiß (NAK-32-Skript, UAC-Klick); ein Rollback auf das 16.08.-Bundle nach dem Speichern eines Projekts mit dem neuen Build verliert dessen Messpunkt-Identität. Kein Code-Fix möglich (der alte Build ist ausgeliefert); Gegenmittel wäre ein Legacy-Spiegel im State, den §33.5 („keine zwei stillen Wahrheiten") verbietet. |
| NAK-45 | 08-22 | **Der OpenWiki-Lauf ist nicht abgeschlossen und liegt vollständig uncommittet** (Fund beim Hook-Audit, out of scope). `openwiki/_plan.md` liegt noch im Baum — laut Vertrag des OpenWiki-Servers entfernt erst die Finalisierung diese temporäre Datei; ein liegengebliebenes `_plan.md` heißt also: unterbrochen oder nie finalisiert. Untracked sind außerdem `openwiki/` (sechs Seitenordner), `AGENTS.md` und `.github/workflows/openwiki-update.yml` — letzterer ein täglicher `schedule`-Lauf (08:00 UTC), der nie feuert, solange er nicht im Remote liegt. `CLAUDE.md` trägt dazu einen uncommitteten OpenWiki-Abschnitt, der auf das ebenfalls uncommittete `AGENTS.md` verweist: die Verfassung verweist auf eine Datei, die es im Repo noch nicht gibt. Wer es angeht: den Lauf zu Ende führen oder die Reste entfernen — das ist eine Entscheidung über eine ZWEITE Wissensquelle neben `docs/` und gehört nicht nebenbei gemacht. Die Dateien stammen aus einer fremden Session; bis zur Klärung nicht anfassen. |
| NAK-46 | 08-22 | **Nimbalyst legt Workspace-Stümpfe im Repo-Wurzelverzeichnis an** (Fund beim Hook-Audit). `nakama/`, `Project Nakama/`, `Untitled Workspace/` — je nur ein `README.md` mit „Welcome to your new workspace!", angelegt 22.08. zwischen 17:31 und 17:36 — liegen untracked neben `eq-copilot/` und `design/`. Sie verrauschen `git status` und damit die Uncommitted-Warnung des `nakama-primer`, die vor parallelen Sessions schützt. Nicht gelöscht, weil Löschen unumkehrbar ist und die Ordner vom User stammen können: sobald sein Wort vorliegt, entweder weg oder in `.gitignore`. **Nachtrag 23.08. (Hygiene-Session, gemessen):** von den dreien existiert nur noch `Untitled Workspace/`; `nakama/` und `Project Nakama/` sind weg — von wem, ist nicht protokolliert. Der Punkt bleibt offen, aber er betrifft nur noch **einen** Ordner. |
| NAK-47 | 08-22 | **Die Hör-Markierung schneidet hart, statt auszublenden** (Fund aus G0, aus Codex' widerlegtem Gate-1-Pfad verifiziert geborgen; `docs/beweise/G0.md` §3.1). `hartAus()` (`eq-copilot/plugin/src/HoerMarkierung.h:419-424`) setzt `fade = 0.0` sofort. Fällt `erlaubt` weg, während die Markierung hörbar ist (Editor schließt, Transport stoppt, Echtzeitbeweis verfällt), springt das Signal im nächsten Block abrupt vom gefilterten auf den Originalpfad — hörbarer Klick. Kein Bruch von Gate 1 (das Audio wird ja gerade **nicht** mehr verändert), aber ein Bruch des Startbudgets §49.3 „A/B-Zustandswechsel: kein Klick; identischer Zustand nullt innerhalb numerischer Toleranz". Dieselbe harte Kante steht in `HoerMarkierung.h:281` (`n > wetKapazitaet` ⇒ `hartAus()`). Fix wäre ein kurzer erzwungener Ausfade statt des Sofortschnitts — verlangt aber Sorgfalt, weil der Neutralpfad danach **bit-transparent** bleiben muss (MarkierungTest). Eigentümer offen; spätestens wenn die Markierung aus dem Provisorium in die Figma-UI wandert. |
| NAK-48 | 08-22 | **Der gevendorte JUCE-VST3-Wrapper nimmt eine `CriticalSection` im Audiocallback** (Nebenbefund von Codex in G0, **nicht** eigenständig verifiziert — vor jeder Handlung an der Quelle prüfen). Behauptet: `_deps/juce-src/modules/juce_audio_plugin_client/juce_audio_plugin_client_VST3.cpp:3714-3716, 3815-3823, 4050` sperrt im Callback, während `setActive()` (`:2809-2812`) denselben Mutex von einem anderen Hostthread hält; der Audiothread wartet dann über `juce_ScopedLock.h:79` / `juce_Threads_windows.cpp:62`. Das ist fremder Code außerhalb `eq-copilot/plugin` und damit außerhalb des G0-Scopes, zielt aber genau auf **Gate 2** („ein IPC-, Broker-, Datenbank- oder UI-Fehler blockiert den Audiothread"), das ab G2 scharf ist. Vor G2 prüfen: gilt das für unsere Bus-Konfiguration, und ändert der Bridge-Patch etwas daran? Falls bestätigt, ist es ein Grundgesetz-Bruch, den wir nicht selbst geschrieben haben — Umgang (Patch erweitern oder Bit dokumentieren) gehört dann entschieden, nicht stillschweigend hingenommen. |
| NAK-49 | 08-22 | **22 bestätigte C++-Befunde aus dem G0-Lauf, keiner auf der P0-Fläche** (`/c-review`, 13 Cluster, zwei Judges; Manifest `docs/beweise/G0.md` §4, Rohdaten regenerierbar unter `.c-review-results/`, gitignoriert). Verteilung: **2 HIGH** — `ACCESS-001` (`src/PipeClient.cpp:186`: `CreateFileW` ohne `SECURITY_SQOS_PRESENT \| SECURITY_IDENTIFICATION`, ein lokaler Pipe-Namensbesetzer kann den DAW-Token impersonieren) und `STR-002` (`:336`: `juce::String::fromUTF8` auf Peer-Bytes ohne UTF-8-Prüfung, JUCE validiert nur per `jassert`); beide selbst an der Quelle nachgemessen. **8 MEDIUM** in `PipeClient.cpp` (Handle-Race und verworfener Rückgabewert bei `CancelSynchronousIo` `:130`, unbegrenzte JSON-Rekursion `:211`, kein Lesetimeout `:316`, `:274`), `PluginProcessor.cpp` (kein Exception-Riegel in `setStateInformation` `:427`, UTF-8-Lauf über ein `.flp`-Label `:715`) und `state/NakamaState.cpp:352` (unbegrenzte Allokation/Rekursion in `ValueTree::readFromStream`). **12 LOW**, darunter drei Build-Härtungspunkte (`CMakeLists.txt`: kein CFG, keine gehärtete STL, `/GL /LTCG` nur im Shipping-Target) und vier `jassert`-Prüfungen, die im Release verschwinden. Sachliche Zuordnung: die Pipe-Befunde gehören zu **SONDE-010/011**, wo der IPC-Weg ohnehin neu gebaut wird (§48.4 fordert dort Impersonation + SID-Prüfung, siehe NAK-21); die State-Parserbefunde zu SONDE-006-Nacharbeit; die Build-Härtung zu SONDE-007b (Installer-/Zielmanifest). Nichts davon blockiert G0 oder S8 — aber keiner der 22 darf still verschwinden (Bauaufteilung §6.6). |
| ~~NAK-50~~ | 08-22 | **GESCHLOSSEN 23.08.** mit `fa46e98` (Entscheid ins Register) · `3e78bff` (Urteilsmarken) · `1d1b247` (Rechner) · `01581e3` (Automatik) · `56de4fb` (Skill `/fragen`). Der User hat die Präferenz von damals („wäre mir lieber") zur Entscheidung gemacht — Wortlaut 23.08.: „*wir brauchen eine andere möglichkeit als den hub. eie gibt keine automatische aktualisation , das heißt er wird driften und somit drifted plan stand auch*", Wahl: „*saeite ganz weg, alles im repo + Skill bauen, der automatisch die offenen fragen nach und nach an mich stellt und gleich einarbeitet in den plan*". Alle drei damals benannten Leistungen sind ersetzt: **(a) anzeigen** → `docs/PLAN-STAND.md`, jetzt **gerechnet** statt gepflegt (Manifest da = gebaut, Urteilsmarke mit PASS = abgenommen) und vom Hook `tools/hooks/planstand.sh` frisch gehalten; **(b) asynchron antworten** → Skill `.claude/skills/fragen/`, der die Karten aus `docs/plan/fragen.json` einzeln im Chat stellt und die Antwort wörtlich einarbeitet; **(c) Bilder** → `docs/plan/bilder/`, im Editor direkt sichtbar. 🔑 **Die Lehre, die den Punkt ausgelöst hat, ist damit strukturell erledigt:** die Kette riss, weil jedes Glied Handarbeit war — jetzt ist der einzige von Hand geschriebene Teil das Urteil selbst, und das ist fail-closed (fehlt es, gilt „nicht abgenommen"). — *Ursprünglicher Text:* **Der User will den Hub im Nimbalyst-GUI statt auf der Briefing-Seite** — Wortlaut 22.08. abends: „EIGENTLICH ist die seite unnötig noise, es wäre mir lieber alles hier im nimbalyst GUI zu haben". Das berührt den Entscheid von heute Vormittag („das ist der neue und einzige hub", Register-Zeile 22.08.) und muss vor dem Umbau als Register-Eintrag präzisiert werden — **noch nicht ins Register übernommen**, weil der Wortlaut eine Präferenz („wäre mir lieber") ist und nicht klärt, ob die Seite abgeschaltet oder nur entlastet wird. **Auslöser** war die Erkenntnis, dass nichts die Seite automatisch aktuell hält: Stand der Seite 9/34, lokal 11/34, real 12/34 bei 13 Commits Drift; die Kette `hub.json` → `hub_sync.py senden` → Seite hängt daran, dass eine Session daran denkt, und riss heute, weil `hub.json` uncommittet bei einer parallelen Session lag. **Was die Seite heute leistet und was ersetzt werden muss** (User-Worte 22.08.): (a) anzeigen — Plan, „bei dir", Figma-Stand *mit Bild* („ich muss sehen können um was es geht"), ersetzt durch `docs/PLAN-STAND.md` (erzeugt, seit `2e8141a`) plus ein Bilder-Blatt; (b) **asynchron antworten** — „baue eine antwortfunktion für mich ein, dass ich zu jedem punkt stellung nehmen kann, dann musst du nur noch die seite anschauen um infos zu bekommen": das ist die tragende Funktion, sie darf beim Umbau nicht verlorengehen; Nimbalyst-Ersatz wäre ein Antwort-Blatt im Repo, das der User direkt im Editor beschreibt und das der SessionStart-Hook vorliest — ohne Netz, ohne Deploy, versioniert in git; (c) Bilder hochladen — im Repo trivial, Dateien danebenlegen. **Vorteil des Umbaus:** eine Wahrheit weniger, kein Deploy für neue Fragen oder Bilder, keine Sync-Kette die reißen kann. **Kosten:** `briefing-hub/` (Subtree, Codex-Vorarbeit) wird tot, und CLAUDE.md-Abschnitt „Hub" plus drei SessionStart-Hooks müssen umgeschrieben werden. Nicht nebenbei machen. |
| ~~NAK-51~~ | 08-22 | **VERWORFEN 23.08. auf User-Wort** — „*upodate das openwiki und verwerfe das mit dem API key wir machen das manuell*". Der Punkt löst sich nicht dadurch, dass der Schlüssel gesetzt wird, sondern dadurch, dass niemand ihn mehr braucht: `.github/workflows/openwiki-update.yml` ist gelöscht, die Auffrischung läuft von Hand im Lauf einer Session über die OpenWiki-Werkzeuge (erste solche Auffrischung am 23.08., Lauf `4670889c`). Damit fällt auch der stille Fehlschlag weg, der den Punkt überhaupt erzeugt hat: ein Workflow, der täglich startet und im Actions-Bereich unbemerkt abbricht. Die beiden optionalen LangSmith-Geheimnisse sind mit ihm gegenstandslos. ⚠️ **Was bleibt:** die Auffrischung passiert jetzt nur, wenn jemand sie anstößt — sie ist damit dieselbe Art von Handarbeit, die bei NAK-50 zur Drift geführt hat. Der Unterschied ist, dass eine veraltete Wiki-Seite nichts behauptet, was ein Beweis widerlegen würde: `CLAUDE.md` und der Code schlagen die Wiki-Seite, und das steht dort auch. — *Ursprünglicher Text:* **OpenWiki: die taegliche Auffrischung laeuft ins Leere, weil `OPENAI_API_KEY` als Repo-Geheimnis fehlt.** Gemessen 22.08. abends: `gh secret list` ist leer, `gh run list` zeigt keinen Lauf. Der Workflow `.github/workflows/openwiki-update.yml` liegt seit `ba6d6c8` im Hauptzweig und ist damit scharf (`schedule: 0 8 * * *`, taeglich 08:00 UTC, dazu `workflow_dispatch`); er startet also, installiert `openwiki@0.3.3` und bricht beim Schritt „Run OpenWiki" ab, weil `OPENWIKI_PROVIDER: openai` mit `OPENWIKI_MODEL_ID: gpt-5.6-terra` einen Schluessel braucht. **Still** — sichtbar nur im Actions-Bereich auf GitHub. Setzen kann das nur der User: `gh secret set OPENAI_API_KEY` (fragt den Wert interaktiv ab, damit er nirgends im Klartext landet). Zwei weitere Geheimnisse sind optional und betreffen nur die LangSmith-Ablaufverfolgung (`OPENWIKI_LANGSMITH_API_KEY`, `LANGSMITH_API_KEY`) — ohne sie laeuft der Rest. **Zustand sonst gesund:** `openwiki/_plan.md` und `_skeleton.md` sind nicht mehr im Baum, der Initiallauf ist also finalisiert (berichtigt damit NAK-45, das den Lauf noch als unabgeschlossen fuehrte); die Seiten liegen unter `openwiki/` in fuenf Bereichen, `AGENTS.md` und der CLAUDE.md-Verweis sind seit `ba6d6c8` committet. Der Workflow committet nicht direkt, sondern legt einen Pull Request auf Zweig `openwiki/update` an — der Merge bleibt eine bewusste Handlung. **Klarstellung zum Modellvertrauen (User 22.08. abends, Wortlaut):** „gpt IST WAS anderes. das galt nur für gemini. gpt vertraue ich." Der Fremdmodell-Riegel (Register 22.08.) bindet **nur Gemini**; GPT schreibend im OpenWiki-Lauf ist ausdruecklich gewollt. Ebenfalls sein Wort: „das workspace hier baut auf openwiki als kontextsystem auf" — OpenWiki ist damit Grundlage, nicht Beiwerk, und wird nicht als zusaetzliche Wissensquelle gegen `docs/` abgewogen. |
| ~~NAK-52~~ | 08-22, S8 | **GESCHLOSSEN 23.08. mit `8e32baf`** (S9/SONDE-007b Abschnitt 1): `cmake/NakamaIdentitaet.cmake` liest den Manifesteintrag, das Bauskript traegt kein Identitaetsliteral mehr, und `EqCopIdentityTest` ist im selben Aenderungssatz umgestellt — der Quellfrost misst jetzt „steht keine zweite Wahrheit mehr da", nicht „steht das Literal noch da". Beide Class-IDs bytegleich, 93 Pruefungen gruen (`docs/beweise/SONDE-007b.md` B1/B2/B5). Der urspruengliche Text: **Die Identitaet steht als Literal im CMake, nicht aus `plugin-identities-v1.json` gelesen.** Entwurf §53.4 sagt: „Identitaet kommt ausschliesslich aus `plugin-identities-v1.json` ueber die duennen Target-Schichten." S8 hat die eine Haelfte dieses Satzes gebaut und bewiesen — der Kern sieht keine Identitaet (`docs/beweise/SONDE-007a.md`). Die andere Haelfte steht aus: `juce_add_plugin(EqCopilot …)` traegt `COMPANY_NAME "evenacadia"`, `PLUGIN_MANUFACTURER_CODE Evna`, `PLUGIN_CODE Eqcp`, `PRODUCT_NAME "EQ-Copilot"` als Literale in `eq-copilot/plugin/CMakeLists.txt`. **Bewusst nicht in S8 geaendert**, aus zwei Gruenden: (a) „duenne Target-Schichten" (Plural) entstehen erst mit den drei Zielen in `SONDE-007b` — bei genau einem Ziel ist die JSON-Lesung Zeremonie ohne Nutzen; (b) `EqCopIdentityTest` friert die CMake-**Quelle** heute in genau dieser Textform ein (`tests/IdentityTestMain.cpp:257-263`: `zielBlock.contains("PLUGIN_CODE " + …)`) — eine JSON-Lesung wuerde dieses Bein brechen und muss es im selben Aenderungssatz auf den neuen Weg umstellen (dann: liest CMake die JSON, und stimmt das gebaute `moduleinfo.json` weiter mit ihr ueberein). **Eigentuemer: `SONDE-007b` / S9.** Bis dahin ist die eingefrorene Identitaet doppelt niedergelegt (JSON + CMake-Literal) und von B1 gegeneinander gemessen — kein Risiko, aber auch nicht, was §53.4 verlangt. |
| ~~NAK-54~~ | 08-23 | **GESCHLOSSEN 23.08. mit `09c5899`**: beide Entscheide stehen mit Datum und Wortlaut im Register der `CLAUDE.md`, der zweite ausdrücklich **mit** seiner Kehrseite (an die Stelle der Regel tritt die Belegpflicht: Stütze UND was ungeprüft blieb) — ein Entscheid „keine harte Regel" ohne diese Zeile liest sich als Freibrief. Die Sperre fiel, weil die Datei nur noch in den Zeilenenden von HEAD abwich (497 CRLF, Inhalt bytegleich, `git diff` leer); es war also nie fremde Arbeit darin, die ein `git add` hätte mitnehmen können. — *Ursprünglicher Text:* **Zwei User-Entscheide vom 23.08. fehlen im Register in `CLAUDE.md`.** Beide fielen beim Bau der Dirigenten-Rolle und liegen wörtlich vor: (a) **Autonomiegrad** — gewählt „**Durchlaufen bis der Plan leer ist**" (Runner arbeitet Ticket für Ticket ohne Zwischenhalt, bis Plan leer / Gate / Bruch); (b) **Gate-Strenge** — Wortlaut: „*ich will nicht dass es an einer harten regel scheitert. ich denke das kann variieren. die regel ist einfach das individuell zu entscheiden mit bestem wissen*". Beide sind in `.claude/skills/dirigent/SKILL.md` und `docs/dirigent/protokoll.md` mit Datum und Wortlaut festgehalten, aber der **kanonische Ort ist das Register in `CLAUDE.md`** (Projektregel: „Ein Entscheid existiert nur mit Datum + Zitat des Users"). **Warum offen statt erledigt:** `CLAUDE.md` lag beim Eintragen uncommittet in der Hand einer laufenden OpenWiki-Session (Codex, `0cd8ca40`); ein `git add CLAUDE.md` hätte deren Arbeit mitgenommen — genau die Landmine, die am 21.08. elf fremde Dateien in einen Commit gezogen hat. Nachzutragen, sobald die Datei frei ist. ⚠️ Bis dahin gilt (b) **nicht** als Freibrief: die Skill setzt an die Stelle der Regel eine **Belegpflicht** (jedes Weiterschalten mit Stütze UND mit dem, was ungeprüft blieb). |
| ~~NAK-55~~ | 08-23 | **VOLLSTÄNDIG GESCHLOSSEN 23.08. abends mit `09c5899`** — die letzte offene Zeile war die `CLAUDE.md`-Nachliste; sie ist eingetragen, und zwar **nachgemessen statt nachgetragen**: „12 Verbraucher" ist nicht 13 (wie hier notiert), sondern **14** — gezählt an den `nakama_kern_anbinden()`-Aufrufen in `eq-copilot/plugin/CMakeLists.txt` (10 wörtliche plus zweimal zwei über die Sonden-Hilfsfunktionen `:219`/`:240`); zwischen dem Notieren und dem Eintragen kam `EqCopQueueStressTest` dazu. „Kanon 23 Beine" ist nicht 24, sondern **26** — gezählt in der Tabelle von `tools/beweise.ps1` und unabhängig bestätigt durch drei Rohblöcke `26/26` in `docs/beweise/SONDE-008.md`; zwischen dem Notieren und dem Eintragen kamen B4 und B9 dazu. 🔑 **Damit ist die Lehre dieses Punktes ein zweites Mal belegt, an sich selbst:** die Korrektur einer abgeschriebenen Zahl altert genauso schnell wie die Zahl. Deshalb steht in `CLAUDE.md` jetzt **neben** beiden Zahlen, wo sie entstehen (`:156` sammelt in eine GLOBAL-Property, `:627` zählt, der Configure-Lauf meldet sie · die Kanon-Tabelle in `tools/beweise.ps1`) — wer sie das nächste Mal braucht, zählt nach, statt zu glauben. Unverändert offen, aber **nicht** Teil dieses Punktes: ein PASS auf den nachgebesserten S9-Stand darf nur ein frischer Prüfer geben (§6.7). — *Ursprünglicher Text:* **NACHTRAG 23.08. nachmittags — alle vier Befunde GESCHLOSSEN**, Belege `docs/beweise/SONDE-007b.md` **§6**, Commits `d93d536` (T2-2, T2-3) · `9573546` (T2-4, T2-5) · der Dokumentationscommit dieses Abschnitts. Jeder Befund wurde erst an der Quelldatei nachgemessen (alle vier bestätigten sich, keiner war ein Fehlalarm), dann geschlossen, und jeder neue oder erweiterte Riegel **beim Fallen vorgeführt** — Rohausgabe je Befund in §6.1–§6.5. Fünf weitere Funde kamen dabei heraus und sind mitgeschlossen: die Null-Schleife des Identitätslesers verglich gegen Rückgabewerte, die CMake nie liefert (ein *gelöschter* `produktname` konfigurierte grün durch, `JucePlugin_Name="produktname-NOTFOUND"`), `codeAlsZahl()` las über das Stringende, `foreach(RANGE -1)` läuft über `0;-1` statt null Mal — und **zwei echte Fehler im Rückweg, die erst das AUSFÜHREN zeigte** (`$hashtable[$null]` wirft in PowerShell, der Rückweg starb ab der zweiten Installation; ein verweigerter Rückweg überschrieb `install-ergebnis.json` mit `status: ERROR` und mauerte damit den `-Erzwingen`-Ausweg zu, den er selbst anbietet). Daraus ist **Kanon-Bein A18** geworden (`tools/eq-copilot/pruefe_installer_gegenpfad.py`, Kanon 23 → 24): der Gegenpfad läuft ab jetzt bei jedem Beweislauf mit, in einer Sandbox unter `%TEMP%` — es wird nichts installiert. **T1 ist gefahren** (§6.6, feste Sechserliste). ⚠️ **Der Punkt bleibt bis auf eine Zeile geschlossen — offen ist nur die `CLAUDE.md`-Nachliste** (unten, jetzt ZWEI Zahlen: „12 Verbraucher" → 13 und „Kanon (23 Beine)" → 24). Ebenfalls unverändert offen, aber **nicht** Teil dieses Punktes: ein PASS auf den nachgebesserten Stand darf nur ein frischer Prüfer geben (§6.7). — *Ursprünglicher Text:* **S9/SONDE-007b: vier offene T2-Befunde aus Teil B** (Sammelzeile — der Text steht dort, wo er gemessen wurde: `docs/beweise/SONDE-007b.md` §5.7, nicht hier abgeschrieben). **T2-2** `cmake/NakamaIdentitaet.cmake`: die Null-Schleife `:86` deckt `produktname`/`bundle`/`plugin_code`, aber **nicht** `hersteller.name`/`hersteller.code` — und die Vier-Zeichen-Prüfung `:99` gilt nur dem Plugincode. Gemessen: `hersteller.code: null` konfiguriert grün durch und JUCE setzt seinen Vorgabewert `Manu` (`JucePlugin_ManufacturerCode=0x4d616e75`), `hersteller.name: null` ergibt `"yourcompany"`. Beide Class-IDs aller drei Bundles hängen daran; der Dateikopf `:19-21` sagt zugleich zu, dass genau das nicht passiert. **T2-3** `tests/IdentityTestMain.cpp`: der umgedrehte Quellfrost prüft nur den Block `juce_add_plugin(EqCopilot …)` — für `NakamaSuna`/`NakamaProbeeq` prüft nichts, dass keine zweite Wahrheit danebensteht (per Mutationsprobe vorgeführt: Literal im Main-Block ⇒ Exit 1, dasselbe Literal in `nakama_sonde_ziel` ⇒ grün). Ein einziger zusätzlicher Blockfrost auf die geteilte Funktion deckt beide Ziele. **T2-4** `pruefe_installer_manifest.py`: ein `art` außerhalb `vst3\|broker` fällt durch alle zwölf Regeln (gemessen: 0 von 12 sehen es) und landet in `Install-Nakama.ps1:107` im Broker-Zweig — also im geschützten Spawn-Pfad. **T2-5** Installer-Vertrag: Auslieferungseinheit ist die innere Binärdatei, nicht das Bundle; eine Erstinstallation erzeugt ein Bundle ohne `moduleinfo.json`, das Main-Bundle behielte das vom 16.08. (`"Version": "0.1.0"` neben 0.3.0), und `-Rueckweg` lässt die selbst angelegten Ordner als leeres `.vst3`-Gehäuse stehen. **T2-2 und T2-3 gehören vor ein PASS auf S9 geschlossen**, T2-4/T2-5 sind Vertragsarbeit und können mit `SONDE-010` laufen. **Mitzunehmen, sobald `CLAUDE.md` frei ist** (dieselbe Sperre wie NAK-54, parallele Session): dort steht „K2b/K2c … gegen **jeden** Verbraucher des Kerns (12)" — gemessen sind es **13**, seit `EqCopLebenslaufTest` in Abschnitt 3 dazukam. **Zweite Zeile seit 23.08. nachmittags:** „**Kanon (23 Beine**, Tabelle in `tools/beweise.ps1`)" — es sind **24**, seit A18 (`pruefe_installer_gegenpfad.py`) dazukam; im selben Absatz ist die Aufzählung um A18 zu ergänzen und der Verweis „zuletzt `SONDE-007b.md`: 23/23 grün" auf den Stand des jüngsten Manifests zu ziehen. 🔑 **Beide Zeilen haben dieselbe Ursache:** eine gemessene Zahl, die in eine Zusammenfassung abgeschrieben wurde, altert dort — hier zweimal innerhalb desselben Tickets. |
| NAK-56 | 08-23, S10–11 | **Ungemessen: schreibt FL die Projektzeit über die Teilstücke eines zerteilten Puffers fort?** Der Capabilityreport S4 hält fest, dass FL Puffer bis auf **1 Sample** zerteilt, um Automationspunkte zu setzen (`docs/beweise/termin-b/`, „Automation nie >1 Punkt je Block bei Offset 0"). Ob die Teilstücke dabei dieselbe `projectTimeSamples` tragen oder sie fortschreiben, steht in den Rohdaten **nicht** — die Messung war auf Automationspunkte gerichtet, nicht auf die Zeit der Teilstücke. `SONDE-008` deckt beide Fälle ab, ohne zu raten: fortschreitende Zeit ⇒ normale Fortsetzung, **stehende** Zeit ⇒ keine Aussage (der lokale monotone Strom trägt, §53.6) — die Alternative wäre gewesen, an jeder Teilungsgrenze still einen Analyseblock zu verlieren. Sichtbar wird der reale Fall am Zähler `analyseKontinuitaetsbrueche()`: läuft er in FL bei automationsreichem Material hoch, stimmt die Annahme nicht. **Beim nächsten FL-Termin ablesen** (Gen hat dafür noch keine Anzeige — NAK-57). Quelle: `plugin/core/StampedAudioQueue.h`, `schliesstAn()`; Manifest `docs/beweise/SONDE-008.md` §4.3. ⚠️ **Nachtrag 23.08. (T2-4-Nacharbeit):** derselbe Zähler steigt seither **auch** bei einem Seek mit bekannt gestopptem Transport. Wer ihn in FL abliest, muss die beiden Ursachen trennen — sonst wird ein Playhead-Zug beim Vorhören als Beleg gegen die Teilstück-Annahme gelesen. Beim Termin also **ohne** Playhead-Züge messen, oder Gen zeigt die Ursachen getrennt (NAK-57). ✅ **Werkbankhälfte geschlossen 23.08. mit `SONDE-009`** (Commit `133526e`): der T2-Bericht zu SONDE-008 §8.9 Punkt 2 nannte die Brückenhälfte des Zeitstempels „die größte ungedeckte Fläche dieses Tickets" — `nakamaBlockEmpfangen()` war gelesen, nie gefahren. Bein **B5 Abschnitt K** fährt sie jetzt am ECHTEN `EqCopilotProcessor` mit einem konstruierten `Blockbefund` und misst, dass die Brückenwerte bis in den Transportstempel des `FeatureFrame` durchkommen. Die Gegenprobe macht die Aussage erst zu einer: über die Brücke stehen **alle sieben** Gültigkeitsbits (0x7f), über den JUCE-Playhead-Rückfallweg nur **zwei** (0x3) — insbesondere kennt der Playhead kein Gültigkeitsbit für `recording`. Damit ist gemessen, dass die Brücke wirklich der Träger ist und nicht irgendein Weg. ⚠️ **Zwei Dinge bleiben offen und sind NICHT Teil dieser Schließung:** (a) die **FL-Hälfte** — schreibt FL die Projektzeit über Teilstücke fort? Das war ausdrücklich nicht Auftrag von SONDE-009 (User-Karte `U13`, Installation ist ein User-Klick), und die FeatureEngine deckt beide Fälle ohne zu raten (Bein B5 §G10 misst, dass stehende Projektzeit über Teilstücke KEINE Grenze ist); (b) der **gepatchte VST3-Wrapper** ist weiterhin nicht gefahren — Abschnitt K ruft die Senke direkt, misst also die Abbildung Brücke→Stempel→Frame vollständig, aber nicht, ob der Wrapper diesen Befund im echten Host so erzeugt. |
| NAK-58 | 08-23, S10–11 | **NACHTRAG 23.08. abends — alle vier Befunde GESCHLOSSEN**, Belege `docs/beweise/SONDE-008.md` **§9**, Commits `f88f8c4` (T2-1) · `90c387a` (T2-3, T2-4) · `a563d4c` (T2-2) · der Dokumentationscommit dieses Abschnitts. Jeder Befund erst an der Quelldatei nachgemessen — **alle vier bestätigten sich, keiner war ein Fehlalarm** — und jeder neue oder erweiterte Riegel **beim Fallen vorgeführt**, mit echtem Bau und byteweiser Rückstellung (SHA-256 vorher = nachher). **T2-1:** nicht den Eimer melden, sondern die Voraussetzung reparieren — über dem Feingitter liegt jetzt ein **Oberband** aus 3070 Bins à 1 LU bis +3100,01 LUFS, also über `lautheit(DBL_MAX)` = +3081,86; damit hat jede endliche Blocklautheit einen Bin **endlicher Breite**, Γ_r durchschneidet per Konstruktion genau einen, und der Satz „unsicher ist allein die Zugehörigkeit eines einzigen Bins" ist keine Zusage mehr, sondern eine Folge. Der Korpus des Prüfers liefert seither **d = 0,000000000 LU** statt 2,917909. Bein B9 sieht den Fall (neuer Abschnitt G, **108** statt 66 Prüfungen). **T2-2:** `EqCopMarkierungTest` **T11** — ohne Playhead bei sonst voller Erlaubnis kein Sample, mit Gegenprobe; die Prüfer-Mutation macht das Bein jetzt rot. **T2-3:** der Anlauf steigt **sofort** in `neustartAnfordern()`, und `workerLauf()` liest ihn je Block. **T2-4:** eine Zeit, die bei **bekannt gestopptem** Transport springt, ist eine Grenze (§32.3 „ein Sprung"). B4 **81** statt 69 Prüfungen (Abschnitte O und P). ⚠️ **Nachtrag `bbe4b3f`, Selbstaudit an der eigenen Nacharbeit:** die erste Fassung von T2-4 („bei Stopp ist **jede** Zeitbewegung ein Bruch") lag näher am Wortlaut und war trotzdem zu scharf — ein Host, der „gestoppt" meldet und die Projektzeit dennoch lückenlos fortschreibt, hätte damit **jeden** Block verloren. Die zweite Fassung fragt bei bekanntem Transport, laufend wie stehend, dieselbe eine Sache: *bewegt sich die Zeit, dann bitte lückenlos* — weniger Code als die erste, plus ein Testfall genau dafür. 🔑 **Drei Lehren, die nicht im Code stehen:** (a) *ehrlich-und-falsch ist schlechter als richtig* — die billige Lösung (Eimerbeitrag melden) hätte den Wert 2,9 LU daneben gelassen, 29× die §49-Toleranz; das Oberband kostet 36 KB und macht den Fehler weg. (b) *ein Bein kann am richtigen Ort stehen und trotzdem blind sein* — Abschnitt N fährt genau den `prepareToPlay`-Gegenpfad von T2-3, sah ihn aber nie, weil dort zwischen Neuanlauf und Drain **immer** ein Audioblock läuft; der Fehler saß exakt in dem Fenster, das N nicht fährt (der Zähler stand auch vor dem Fix schon auf 40). (c) *ein Fix, der näher am **Wortlaut** der Vorgabe liegt, ist nicht automatisch näher an ihrer **Absicht*** — §32.3 nennt „einen Sprung" als Grenze, nicht „eine Bewegung"; siehe den Nachtrag zu T2-4 oben. ⚠️ **Ein PASS auf S10–11 darf weiterhin nur ein frischer Prüfer geben** — diese Nacharbeit erklärt keines. — *Ursprünglicher Text:* **S10–11/SONDE-008: vier offene T2-Befunde** (Sammelzeile — der Text steht dort, wo er gemessen wurde: `docs/beweise/SONDE-008.md` **§8**, nicht hier abgeschrieben). Urteil des frischen Prüfers: **NEEDS_WORK**. Der Umbau selbst hält und ist unter härterer Last als sein eigenes Bein geprüft (60 000 Blöcke mit gemischtem Überlauf/Oversize, 4 000 000 Frames in echtem Zweithread-Betrieb: kein zerrissener Block, keine Ordnungsverletzung, Frame- und Blockbilanz exakt). **T2-1** (mittel–hoch) `plugin/core/analysis/LoudnessAccumulator.h`: der Eimer über dem Histogramm-Gitter entscheidet **den ganzen Eimer** an seinem Mittelwert. Enthält er zwei Pegel und fällt Γ_r dazwischen, ist das Ergebnis um **2,918 LU** verschoben, während `unsicherheitLu()` **0,000000000 LU** meldet — der Kopfkommentar (`:254-257`, `:279-280`) nennt die Schranke „vollständig" und „alles andere ist exakt", das Manifest §4.4 „die Zugehörigkeit **eines einzigen** Bins". Der Golden B9 §F kann es nicht sehen, weil er den Eimer nur mit **konstantem** Material füllt (200× z=1e6) — mit einem Pegel kann er konstruktiv nicht scheitern. Reproduktion (vier Zeilen) in §8.9. Wer es schließt, berichtigt im selben Änderungssatz den Kopfkommentar und ergänzt B9 um einen **Zwei-Pegel**-Korpus über dem Gitter. **T2-2** (mittel) `PluginProcessor.cpp`: der U10-Riegel (`∧ spieltGültig ∧ spielt`) ist **wirksam** — Positivprobe: ohne Playhead färbt bei sonst voller Erlaubnis kein einziges Sample — aber von **keinem** Kanon-Bein gedeckt. Mutationsprobe: fail-open `(spielt ∨ ¬hatTransport)` zurückgebaut ⇒ `EqCopNullTest`, `EqCopMarkierungTest`, `EqCopLebenslaufTest`, `EqCopQueueStressTest` **alle vier weiter Exit 0**. Ursache strukturell: die Beine wurden dem neuen Term angepasst (überall ein laufender Playhead ⇒ `hatTransport` wahr ⇒ fail-open-Zweig wieder tot), nicht gegen seine Rücknahme gehärtet. Es fehlt genau eine Prüfung: Main klassifiziert, Editor offen, `testForciereEchtzeit(true)`, aktiver Auftrag, **kein Playhead** ⇒ bitgleich. **T2-3** (niedrig–mittel) `plugin/core/StampedAudioQueue.h:230-234`: die Zusage „der Consument erkennt Blöcke aus dem alten Anlauf an ihrer kleineren `startFolge`" gilt erst ab dem **ersten Audioblock nach** `prepareToPlay` — gemessen 0 von 3 Altblöcken erkennbar. Weil `workerLauf()` am Kopf derselben Runde `engine.vorbereiten(neueSr)` ruft und danach den Ring leert, werden Blöcke der **alten** Samplerate mit der neuen analysiert (Fenster ≈ eine Worker-Periode, ~50 ms; Engine ist dabei frisch zurückgesetzt). Zweite Hälfte: `aktuellerAnlauf()` wird einmal vor der Schleife und `relaxed` gelesen — kippt der Anlauf während der Schleife, zählt der Worker **frische** Blöcke als `veralteteBloecke`; der Zähler vermischt damit zwei Sachverhalte. **T2-4** (niedrig) `StampedAudioQueue.h:591-594`: ein Seek bei **gestopptem** Transport ist keine Kontinuitätsgrenze (gemessen: Sprung um 10 s ⇒ 0 Brüche), obwohl §32.3 Seek ausdrücklich als Epochengrenze führt. Die Entscheidung dahinter steht ehrlich im Manifest §4.3 Punkt 2 (stehende Zeit bei Stopp darf kein Bruch sein), ihre Kehrseite nicht: eine Zeit, die bei Stopp *springt*, ist etwas anderes als eine, die *steht*, und der Unterschied ist an den vorhandenen Bits unterscheidbar. Eigentümer `SONDE-009`, das die Fensterbuchhaltung auf dieser Grenze aufbaut. ⚠️ **Ein PASS auf S10–11 darf erst nach der Nacharbeit und nur von einem frischen Prüfer kommen** (dieselbe Regel wie `SONDE-007b.md` §6.7). Was der Prüfer **nicht** geprüft hat, steht vollständig in §8.9 — vor allem: kein Lauf in FL, und die **Brückenhälfte des Zeitstempels (`nakamaBlockEmpfangen`) hat kein Bein**. |
| NAK-57 | 08-23, S10–11 | **Die neue Analyse-Telemetrie ist auslesbar, aber nirgends sichtbar.** `SONDE-008` liefert Ganzblockdrops (getrennt nach Überlauf und Oversize), verworfene Frames, High-Water-Mark der Hostblockgröße, versiegelte und in der Quarantäne verworfene Blöcke, Kontinuitätsbrüche und `LoudnessAccumulator::unsicherheitLu()`. Der Editor zeigt davon **nur** `framesDropped` (unverändert). Entwurf §0.4/§1.4 der Bauaufteilung verlangt in jeder UI-Fassung sichtbar: Frische/stale · Konfidenz · Capability-Degradation — `unsicherheitLu()` ist genau eine Konfidenzangabe, die Drop- und Bruchzähler sind Degradation. **Bewusst nicht in diesem Ticket gebaut:** die Oberfläche kommt aus Figma über `design/` (Register 21./22.08.), die heutige Material-Kit-Front ist ein Provisorium, an dem keine Arbeit mehr stattfindet. Gehört in die Gen-Übersetzung; bis dahin lesen die Beine B4/B9 die Zahlen. **Nachtrag 23.08. (S12–13/SONDE-009): die Liste ist länger geworden, der Punkt bleibt unverändert offen.** Dazugekommen sind aus der Fensterbuchhaltung: `getrennteFenster()`, `epochenwechsel()`, `segmentwechsel()` (die beiden sind §32.3 zufolge **verschiedene** Dinge und dürfen in keiner Anzeige verschmelzen — Segment heißt „unsere Analyse hat eine Lücke", Epoche heißt „der Host ist gesprungen"), `straddleVerworfen()`, `nak29Abgelehnt()`, `verworfeneFensterSamples()`, `ereignisseVerworfen()` und `grenzenMitGrund(Grenzgrund)` für alle neun Ursachen einzeln. Letzteres ist der Punkt, den NAK-56 für den FL-Termin braucht: **die Ursachen getrennt zeigen**, sonst wird ein Playhead-Zug beim Vorhören als Beleg gegen die Teilstück-Annahme gelesen. Gelesen werden sie heute nur von Bein B5. |
| NAK-62 | 08-23 | **NACHTRAG 23.08. abends — GESCHLOSSEN**, Commit `94c5ab0`: der User hat entschieden („*ASSET KIT is alt und längst verworfen*", 23.08.), die Löschung ist committet und **alle sechs Verweise sind im selben Änderungssatz nachgezogen** (README, zwei Archiv-LIES-MICHs, Prisma-Statusblatt, NEXT-SESSION, Freigabe-Marker); die Kette `tokens.json` → `LeitstandTokens.h` bleibt unberührt lebendig. ⚠️ **ID berichtigt:** diese Zeile hieß NAK-59, aber NAK-59 war bereits an Band-Stereo (`SONDE-010`, Manifest SONDE-009) vergeben — zwei parallele Sessions zogen dieselbe Nummer; hier umnummeriert zu **NAK-62**. — *Ursprünglicher Text:* **Zwei verwaiste Löschungen unter `eq-copilot/design/` liegen uncommittet — bewusst nicht mitgenommen.** Eine beendete Session hat `ASSET-KIT.md` und `eq-copilot-material-preview.png` von der Platte entfernt; beide sind in HEAD noch vorhanden, im Arbeitsbaum als `D` sichtbar. **Warum nicht committet:** der Aufräum-Entscheid vom 21.08. („Inspirationen + regenerierbare Renders raus") deckt das **PNG** (regenerierbar per `EqCopShot.exe <ziel.png>`), aber nicht das **Ist-Dokument**. `ASSET-KIT.md` hat sechs lebende Verweise — `docs/NEXT-SESSION.md:314`, `eq-copilot/README.md:78`, `eq-copilot/design/archive/LIES-MICH.md:45`, `eq-copilot/design/archive/nakama-geraetefront/referenz/LIES-MICH.md:20`, `eq-copilot/design/prisma-studie/docs/design-stand.md:450` und `.claude/kreativ-freigabe.md:12` — und beschreibt die Kette `tokens.json` → `LeitstandTokens.h`, die `CLAUDE.md` ausdrücklich „technisch lebendig" nennt. Die Löschung allein zu committen hieße: sechs tote Verweise stehenlassen. **Auch das PNG allein geht nicht**, weil `ASSET-KIT.md` es einbettet (`![Echter JUCE-Render …](eq-copilot-material-preview.png)`) — dann trüge das committete Dokument einen kaputten Bildverweis. Wer es angeht, macht beides in EINEM Änderungssatz: löschen und die sechs Verweise nachziehen (oder das Dokument nach `eq-copilot/design/archive/` verschieben, wo ein Ist-Dokument des Provisoriums hingehört). Bis dahin nichts anfassen — insbesondere **nicht** per `git checkout --` wiederherstellen, das wäre das Verwerfen fremder Arbeit. |
| NAK-60 | 08-23 | **NACHTRAG 23.08. abends — GESCHLOSSEN**, Commit `7020f06`: der User hat von sich aus im Chat entschieden („*motion spezifikation ist UX ausarbeitung und gehört commited*", 23.08.) — das Blatt plus seine zwei Querverweise sind in EINEM Zug committet. Der unten vorgeschlagene Weg über die Hub-Seite war damit überholt (die Regel verbietet, Fragen in den Chat zu tragen — nicht dem User, dort zu antworten). Das Blatt bleibt **Arbeitsvorgabe**: seine Einzelwerte (z. B. ~140 ms Übergangsdauer, Figma-Seite „Motion / Interactions") sind damit NICHT abgenommen. — *Ursprünglicher Text:* **Verwaiste Interaktions- und Motion-Spezifikation im Design-Ordner — liegt untracked, bewusst nicht committet.** `design/docs/interaktions-und-motion-spezifikation.md` (151 Zeilen, Dateizeit 23.08. 00:12) plus zwei Querverweis-Edits in `design/docs/arbeitsplan.md` und `design/docs/oberflaechen-spezifikation.md`, die auf sie zeigen. Inhaltlich ist das Blatt sauber gearbeitet: es erfindet keine Optik, verweist auf die Figma-Stände, hält NAK-33 offen und nennt sich selbst „Arbeitsvorgabe", nicht Abnahme. **Warum trotzdem nicht committet:** sein Kopf sagt „auf ausdrücklichen Auftrag", nennt aber keinen Urheber, und es gibt **keinen Beleg** dafür — in `docs/hub/hub.json` und `design/abnahmen/` steht zu Motion, Gesten oder Interaktion nichts; die einzigen Fundstellen im Repo sind `?? design/docs/…`-Zeilen in den `git status`-Blöcken der Manifeste SONDE-007a/007b/008, es liegt also seit mehreren Sessions unbeansprucht da. Es setzt zugleich Dinge fest, die Entscheide wären (rund 140 ms Übergangsdauer, eine zusätzliche Figma-Seite „Motion / Interactions" als Aufgabe an den User) — und `design/LIES-MICH.md` Regel 3/4 verlangt dafür sein Wort. In `design/docs/` committet würde es zur Projektquelle („vollständig, nichts von außen dazuholen"). Alles-oder-nichts: die zwei Querverweise ohne die Datei ergäben tote Links, deshalb liegen alle drei. **Richtiger Weg:** als Frage auf die Hub-Seite (dort antwortet der User, nicht im Chat — Register 22.08.); sagt er ja, wandert das Blatt mitsamt seinen zwei Querverweisen in einem Zug ins Repo. |
| ~~NAK-61~~ | 08-23 | **GEGENSTANDSLOS 23.08.** — die Seite ist abgeschafft (NAK-50), `briefing-hub/` liegt nur noch als Verlauf im Baum und wird nicht mehr gebaut oder deployt. Der Fehler bleibt in der Datei stehen; wer den Ordner je wiederbelebt, findet ihn hier beschrieben. — *Ursprünglicher Text:* **Bestehender Typfehler in der Briefing-Seite** (Fund beim Prüfen der verwaisten Hub-Arbeit, out of scope). `briefing-hub/app/briefing-app.tsx:963` annotiert den `.then()`-Rückgabewert von `response.json()`; unter TypeScript 5.9 liefert der `unknown`, und `tsc --noEmit` bricht mit **TS2345** ab (einziger Fehler im Projekt). Die Zeile ist in HEAD bytegleich, der Fehler ist also **nicht** von der geborgenen Arbeit (`6e67662`) — er stand vorher da. Folgenlos für den Betrieb, weil `vinext build` nicht typprüft; die Seite läuft. Wer es angeht: Antwort erst als `unknown` annehmen und schmal validieren, statt sie zu behaupten — die Seite liest hier Fremddaten aus `/api/hub`. Hängt am Schicksal von `briefing-hub/` (NAK-50). |
| ~~NAK-63~~ | 08-23 | **GESCHLOSSEN 23.08.** im OpenWiki-Lauf `4670889c` (Modus `update`, Sprache unverändert `en`). `briefing-sync.md` und `briefing-application.md` sind gelöscht — die begriffliche Grenze ist wirklich weg, nicht nur veraltet; an ihre Stelle tritt `collaboration/plan-status.md` (gerechneter Status, Urteilsmarke, Schleifenfreiheit des Stempels, Fragen im Chat). Nachgezogen: `session-automation.md` (zwei Hooks weg, zwei dazu), `quickstart.md`, `delivery/build-and-proof.md`. Dabei kam heraus, dass das Wiki **S10–11 und S12–13 komplett fehlten** — `StampedAudioQueue`, `LoudnessAccumulator` und `FeatureEngine` kamen auf keiner Seite vor; deshalb zusätzlich `plugin/audio-runtime.md` überarbeitet (Ganzblock-Queue statt Teilblock-FIFO, Hostbrücke verdrahtet, Markierung ohne fail-open) und `plugin/measurement-core.md` neu angelegt. Beleg: `openwiki_finish` meldet `complete` ohne Warnungen; ein erster Lauf hatte für zwei Seiten noch Belegschuld gemeldet und deren Claims-Sidecar **nicht** geschrieben — nachgezogen im zweiten Lauf. — *Ursprünglicher Text:* **Die OpenWiki-Seiten beschreiben den abgeschafften Briefing-Sync als lebendes System.** |
| ~~NAK-66~~ | 08-23 | **GESCHLOSSEN 23.08. mit `e6f42b1`** — die parallele Session hat committet und beide Hälften dabei mitgenommen; meine Registerzeile und die drei Kanon-Berichtigungen sind darin enthalten (nachgeprüft an `git show e6f42b1`). Der Punkt hat sich also nicht durch Trennen erledigt, sondern dadurch, dass die andere Seite zuerst fertig war. 🔑 **Die Lehre bleibt und ist allgemeiner als der Fall:** ob fremde Arbeit trennbar ist, entscheidet nicht die Datei, sondern der Hunk. In `docs/offene-punkte.md` standen unsere Zeilen nebeneinander — dort ließ sich gezielt stapeln (zwei von drei Hunks, an Index und Arbeitsbaum gegengeprüft). In `CLAUDE.md` **verlängerte ihre Änderung meinen eigenen Satz**; dieselbe Trennung hätte dort einen in sich widersprüchlichen Stand committet. Erst schauen, wo die Grenze verläuft, dann entscheiden. — *Ursprünglicher Text:* **Zwei Zeilen von mir liegen uncommittet in `CLAUDE.md`, vermischt mit der Arbeit einer parallelen Session — bewusst nicht committet.** Meine Hälfte: die Registerzeile zum verworfenen OpenWiki-Schlüssel (23.08.) und die Berichtigung der Kanonzahlen (dort stand „26 Beine" und „Drei Beine geplant (B5, B6, B7)"; gemessen sind es **30 deklarierte Einträge, 28 lauffähig**, und B5 `EqCopAnalysisGoldenTest` ist seit SONDE-009 gebaut — die 28 decken sich mit dem `28/28` in `docs/beweise/SONDE-009.md`). Fremde Hälfte: eine **Produktentscheidung** vom selben Tag — „Suna ist in Probeeq aufgegangen", Nakama hat nur noch **zwei** Apps, Gens Seite 2 wird EQ-Zentrale. Zusammen 34 eingefügte und 21 gelöschte Zeilen in EINER Datei. **Warum nicht committet:** dieselbe Landmine wie NAK-54 — ein `git add CLAUDE.md` nähme die fremde Entscheidung mit in meinen Commit, und die gehört mit ihrem eigenen User-Zitat und ihrer eigenen Begründung in einen eigenen Änderungssatz. Wer es aufräumt, committet **zuerst** die Produktentscheidung mit Wortlaut, danach die zwei kleinen Berichtigungen. ⚠️ Bis dahin ist der Wahrheitskern in `CLAUDE.md` an einer Stelle uneinheitlich: der Kopf sagt „zwei Apps", der eingerahmte Wahrheitskern-Block darunter noch „drei". |
| NAK-67 | 08-23 | **`openwiki_begin` könnte den gelöschten Zeitplan-Workflow wieder anlegen.** Die Skill-Regel sagt ausdrücklich: „`openwiki_begin` owns their setup" für die verwalteten Blöcke in `AGENTS.md`/`CLAUDE.md` **und den generierten Scheduled-Update-Workflow". Gelöscht wurde `.github/workflows/openwiki-update.yml` NACH `openwiki_finish` (User-Wort 23.08.: „wir machen das manuell") — im nächsten `openwiki_begin` kann er also wieder auftauchen, still, und dann täglich erneut ins Leere laufen. **Beim nächsten OpenWiki-Lauf prüfen:** liegt die Datei danach wieder da, gehört sie erneut gelöscht — und wenn das zweimal passiert, braucht es eine dauerhafte Lösung (z. B. Eintrag in `.openwikiignore`, die es heute nicht gibt). Ebenfalls betroffen: `AGENTS.md` Zeile 10 behauptet weiter „The scheduled OpenWiki GitHub Actions workflow refreshes the repository wiki" — die Zeile steht **im verwalteten Block** und wurde deshalb nicht von Hand berichtigt. Betroffen sind mindestens `openwiki/collaboration/briefing-sync.md`, `briefing-application.md`, `session-automation.md` und `quickstart.md` samt ihrer Claims-Sidecars — sie führen `docs/hub/hub.json`, `tools/hub/hub_sync.py holen\|senden` und die Seite `nakama-briefing.philipld.chatgpt.site` als geltenden Weg. Seit dem 23.08. stimmt keine dieser Aussagen mehr (NAK-50). **Bewusst nicht von Hand berichtigt:** OpenWiki-eigene Seiten, Claims, Indizes und Provenance werden laut Serveranweisung nicht direkt editiert — die Korrektur gehört in einen OpenWiki-Lauf (`/openwiki`, Modus „reconcile documentation after source changes"). Vorher ist NAK-51 zu lösen (der Workflow läuft ins Leere, weil `OPENAI_API_KEY` als Repo-Geheimnis fehlt — das kann nur der User setzen). Bis dahin gilt: **Code und `CLAUDE.md` schlagen die Wiki-Seite**, wie immer. |

## Geparkt mit der Prisma-Studie (User 21.08.2026: „Familie; Prisma nur Studie")

Weder offen im Sinne von Arbeit noch erledigt. Statusblatt:
`eq-copilot/design/prisma-studie/STATUS.md`. Wird nur wieder bewegt, wenn der
User die Studie ausdrücklich reaktiviert.

| ID | Seit | Punkt |
|---|---|---|
| NAK-11 | 08-17 | Unicorn-Studio-Weg: Offline-/Selbsthosting der Szenen-JSON braucht den „Legend"-Plan; SDK-Lizenz („nur für legitime Unicorn-Studio-Projekte") vor einem JUCE-WebView-Einsatz im ausgelieferten Plugin klären. Design-Phase (CDN-Embed) ist unkritisch. **Geparkt 08-21:** die Unicorn-Lichtwelt gehört zur Prisma-Studie; ohne Studie kein WebView-Einsatz, ohne Einsatz keine Lizenzfrage. |
| NAK-14 | 08-18 | Lesbarkeits-Test der Befund-Verkörperung ist bisher nur an EINEM Befund gedacht: Kein Entwurf aus dem /diverge-duo-Lauf (12 Optionen) wurde gegen den Alltagsfall MEHRERE gleichzeitige Befunde (3–4 nebeneinander, ggf. auf verschiedenen Flächen) geprüft — genau der Fall, für den der 2-Sekunden-Massstab des Profis gilt. Ebenso ungenutzt als Träger: die drei Flächen/das Drehen und der Bündel-Modus. Vor dem Bauen einer Verkörperung mitprüfen. **Geparkt 08-21:** Befund-Verkörperung im Glas ist Prisma-Studie; die Plugin-UI kommt aus Figma. |
| NAK-16 | 08-19 | **ST-Map-Live-Refraktion** (User-Freigabe 19.08., ersetzt den NAK-15-Ansatz nach dessen Verwurf). **Pipeline recherche-gehärtet** — verbindlich ist `eq-copilot/design/prisma-studie/docs/2026-08-19-stmap-live-refraktion.md` (seit 21.08.; vorher docs/research/) (5-Worker-Deep-Research, Primärquellen, Blender-Fakten am installierten 5.1.2 verifiziert): Refraction BSDF statt Glass · 32f-EXR statt PNG-Hi/Lo · Box-Filter 0,01 + Denoise aus · Offset-Form → RG16F (LINEAR = WebGL2-Kern) · R/B-Normalisierung (B = Gewicht + TIR-Maske) · Lerp-Composite mit Glanz-Pass · Dispersion per 3-Tap. Erst EIN Probe-Still mit Probe-Checkliste beweisen (Maßstab: Glasinneres ändert sich mit dem Hintergrund — Ähnlichkeit zum alten Frame ist als Metrik VERBOTEN), dann 72er-Sequenz. Einstieg: `docs/NEXT-SESSION.md`. **Stand 19.08. (cba6bd6, 602b20a): Probe-Still GEBAUT und BESTANDEN** — 74,2 % der Glaspixel ändern sich mit dem Hintergrund (Messwerte `renders/stmap/messung.json`, Checkliste komplett beantwortet: B nicht Fresnel-gewichtet, 64 Samples genügen, Half-Float trägt, RGBA16F ok). Offen bleibt: User-Sichtung des lebenden Blatts (`stmap-probe.html`, Optik-Abnahme inkl. Dispersion-Stärke + Glanz-Licht-Rig), dann 72er-Sequenz (~18 min: ST-Maps 4 s + Glanz 11 s je Frame) und Integration in Prototyp/Schlieren-Blatt (dort den getContext-Patch für preserveDrawingBuffer mitnehmen). **Geparkt 08-21:** Pfade meinen den Stand vor dem Umzug nach `eq-copilot/design/prisma-studie/`; die regenerierbaren ST-Map-Daten wurden am 21.08. entfernt (Regenerierung: STATUS.md). |
| NAK-17 | 08-19 | **Prisma-Mesh: Seitenflächen-Normalen zeigen nach INNEN** (Winding in `prisma-material-still.py` `from_pydata`; am evaluierten Depsgraph gemessen). Cycles rendert Glas trotzdem korrekt (Shading-Normalen werden geflippt) — alle abgenommenen Renders entstanden SO. Nicht nebenbei „fixen": ein Flip ändert potenziell MNEE-/Fasen-Nuancen aller Folge-Renders gegen den abgenommenen Stand. Nur zusammen mit einer bewussten Neu-Abnahme drehen. Bis dahin: Spiegelwinkel-/Normalen-Mathe immer am evaluierten Mesh messen, nie aus der Eckenliste herleiten. **Geparkt 08-21** mit der Studie. |
| NAK-18 | 08-19 | **Boden-Kontakt-Ebene widerspricht dem Zielvertrag:** `renders/dreh-boden/` (Taste B im Blatt) ist aus der alten Dark-Field-Studiowelt gebacken; `eq-copilot/design/prisma-studie/docs/visuelles-zielbild-hoerkompass.md` (seit 21.08.; vorher docs/) §6 erlaubt Kontaktlicht/Bodenwirkung nur aus DERSELBEN Lichtwelt wie der lebende Hintergrund. Bei der NAK-16-Integration entscheiden: Kontakt-Ebene aus der lebenden Welt neu herleiten (z. B. gespiegeltes Live-Sampling analog zur ST-Map) oder ersatzlos aus dem Begleiterbild nehmen. Gleiche Prüfung gilt fürs Glanz-Licht-Rig (aktuell statisches Kanten-Paar, kühl-blau — nah an der Nexus-Palette, aber nicht AUS ihr). **Geparkt 08-21:** der Zielvertrag selbst ist „Alles nur Studie" (User 21.08.). |

## Geschlossen

| ID | Geschlossen | Punkt |
|---|---|---|
| NAK-01 | 08-21, Kontext-Interview (User: „alle alten sind alt"; Aufräum-Freigabe Frage 20: alte Design-Richtungen ins Archiv) | **Geschlossen ohne Umsetzung:** das Tiefenfeld-Musterblatt liegt im Archiv (`eq-copilot/design/archive/alt-2026-08/nakama-spectral-field-claude.html`), ein Design-Freeze findet nicht mehr statt — die Plugin-UI kommt aus Figma. Ursprungstext: Design-Freeze ausstehend: User kalibriert das Tiefenfeld-Musterblatt im Browser; „Werte kopieren"-JSON in den Chat = Freeze → JUCE-Voreinstellung. |
| NAK-02 | 08-21, Kontext-Interview (User: „alle alten sind alt"; Archiv) | **Geschlossen ohne Umsetzung:** Bauplan 2.0 liegt im Archiv (`docs/archiv/NAKAMA-SPECTRAL-FIELD-BAUPLAN.md`); der JUCE-Umbau der Hauptansicht nach seinem §13 ist kein Ziel mehr; Gen bekommt seine Oberfläche aus Figma über `design/`. Ursprungstext: JUCE-Umbau der Hauptansicht nach Bauplan 2.0 §13 (12 Schritte) — startet erst NACH NAK-01. |
| NAK-03 | 08-21, Kontext-Interview (Material-Kit-Front: „Nie abgenommen – bleibt Provisorium"; Hub-App seit 18.08. kein Produktteil — CLAUDE.md „Was NICHT mehr gilt") | **Geschlossen:** §4/§11–15 der Testanleitung setzen die gestrichene Hub-App und ihren 🎚-Knopf voraus (Stand-Hinweis in `eq-copilot/docs/FL-TESTANLEITUNG.md`); §16–17 prüfen das Material-Kit-Provisorium, an dem keine Arbeit mehr stattfindet. Die formalen FL-Messungen der Sondenfamilie sind Termin A/B. Rest ohne Nachfolger: der Hörtest der Hör-Markierung (§18) — bei NAK-35 mitgeführt. Ursprungstext: Beim User: FL-TESTANLEITUNG §11–17 (M2-Roundtrip · M3-Karten · Graph/„KURVE STEHT") + §18 Hör-Markierung (Puls/Solo hören, Render-Beweis). Plugin 0.3.0 ist installiert. |
| NAK-04 | 08-21, Kontext-Interview (User: „Termine bald; bis dahin S7") | **Ersetzt** durch die formalen FL-Termine A (`eq-copilot/docs/FL-TERMIN-A-AUX-PDC.md`, Messgerät `EqCopAuxSpike`) und B (`…-B-HOSTZEIT.md`, Messgerät `EqCopHostProbe`) — mit Klickliste, Messgerät und JSON-Bericht statt formloser Meldung. Ursprungstext: 16-Instanzen-/PDC-/Offline-Render-Verhalten = Alltagsbeobachtung, User meldet formlos. |
| NAK-05 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-Roadmap M3b; die Sondenlinie ist seit 20.08. beschlossen: „okay dann fangen wir damit nächste session an") | **Geschlossen:** M3b gehört zur M-Roadmap des alten EQ-Copilot (Benchmark-Studie, Abschnitt „M3b — Lernfenster sichtbar + Korridor"); die Sondenlinie setzt Kalibrierung neu auf — Entwurf §34.3 Konfidenz, §49.4 Evaluationskorpus, P4/P5 (`SONDE-013/014`). Ein „Korridor" als globale Zielkurve widerspricht außerdem der Invariante `ltasReferenzDb ≠ Sollkurve`. Ursprungstext: M3b: Korridor-Kalibration aus even-Bounces (Benchmark-Studie, nächste Stufe). |
| NAK-06 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-Roadmap M3c; Sondenlinie seit 20.08. beschlossen) | **Geschlossen:** die Inhalte sind in der Sondenlinie neu definiert — PRE/POST-Kettenprüfung = Kernfunktion 5 (Entwurf §10, §38), Passagen = §32.4, Masking = Roadmap 3 (§47.1); die M3c-Fassung auf dem alten Plugin wird nicht gebaut. Ursprungstext: M3c: PRE/POST-Karte, Hover-Inspekt, Passagen, Masking (Benchmark-Studie). |
| NAK-08 | 08-21, Kontext-Interview (User: „Nein – raus aus dem Produkt") | **Geschlossen:** keine KI-/Claude-Erklärschicht im Produkt; der Advisor ist regelbasiert (CLAUDE.md Wahrheitskern; Entwurf-Errata (e)). Ursprungstext: Claude-Anbindung: `--bare` scheitert an OAuth → Agent-SDK-/API-Adapter nötig (M4-Thema; Grundgesetz: Aufruf nur auf bewussten Klick). |
| NAK-09 | 08-21, Befund nach Kontext-Interview (alte EQ-Copilot-M3-Linie; Sondenlinie seit 20.08. beschlossen) | **Geschlossen:** das Kalibrier-Protokoll kalibriert die Befundkarten-Schwellen des alten Plugins („kommt mit M3b-Profil", `eq-copilot/kalibration/KALIBRIER-PROTOKOLL.md`); der Messkern wird als FeatureEngine v2 (`SONDE-009`) mit eigenen Goldens neu gebaut, die Evidenzqualität folgt §49.4. Das Teststück und `verify_testsong.py` bleiben als Fixture-Quelle; `analyze-track.py` ist NAK-31. Ursprungstext: Kalibrier-Protokoll Runde 2 (helle Instrumente ODER EQ-Fehler-Blindtest), `eq-copilot/kalibration/`. |
| NAK-12 | 08-21, Befund nach Kontext-Interview (Hub-App seit 18.08. kein Produktteil — CLAUDE.md „Was NICHT mehr gilt") | **Geschlossen:** der Entwurf kennt die Komponente nicht (grep `Sensorübersicht\|Svelte` 0 Treffer); Profilbindung und Sensor-Status sind in der Sondenlinie Aufgabe von Gen (Entwurf §3.1, §35.1 Quellenübersicht, `SONDE-012`). Die Referenzkopie `broker/sensoruebersicht-referenz/Sensoruebersicht.svelte` bleibt als Verlauf liegen. Ursprungstext: Sensorübersicht heimatlos: Das Svelte-Panel (Profilbindung, Sensor-Status) flog mit der Broker-Herauslösung aus der Hub-App; Referenzkopie in `broker/sensoruebersicht-referenz/`. Braucht ein neues Zuhause (eigene Mini-UI, CLI oder Plugin-Editor) — bis dahin Bindungen nur per JSON-Datei + Broker-Neustart. |
| NAK-15 | 08-19, `7de964d` | **Prisma in der App ohne starren Hintergrund** (User-Anforderung 19.08.). Umgesetzt in der Nacht 19.08.: 72 RGBA-Drehframes `renders/dreh-frei/` (film_transparent, Plate+Boden kameraunsichtbar, Kamera identisch zur bewiesenen JS-Projektion) + 72 Boden-Kontaktframes `renders/dreh-boden/` (Prisma lichtwirksam aber unsichtbar, offline Alpha aus Helligkeit — Shadow Catcher war gemessen der falsche Weg, die Pfützen sind Glanzreflexe); Blatt komponiert transparent (clearRect, `?frames=dreh` Vergleich, Taste B/`?boden=0`), Pixel-Beweis: neben der Silhouette exakt Body-Farbe, Ebene 0 scheint durch. Alte Frames bleiben als Archiv/Prototyp-Quelle. **Nachtrag 19.08.: Ergebnis vom User VERWORFEN** („spiegelung des alten hintergrundes eingebacken … beleuchtung komplett falsch … billig zusammengeklebt") — die Silhouetten-Freistellung stimmt, aber das GLASINNERE trägt weiter die alte Konserve. Nachfolger: **NAK-16** (ST-Map-Live-Refraktion). |
| NAK-19 | 08-20, `ab80522` | Aufgeloest durch **Produktentscheid 20.08.** (Entwurf §0.3): Hoerkompass-Zielvertrag gilt der eigenstaendigen Prisma-App, das Master-Plugin bekommt eine konventionelle Arbeits-UI — Landkarte und Zielvertrag leben in zwei Oberflaechen. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. **Nachtrag 21.08. (Kontext-Interview):** gegenstandslos — der Hörkompass ist „Alles nur Studie" (User), die Plugin-UI kommt aus Figma; die Kollision existiert nicht mehr, auch der Auflösungsgrund „zwei Oberflächen" ist damit Studie (Entwurf-Errata (c)). |
| NAK-20 | 08-20, `ab80522` | In Fassung 0.4 uebernommen: `JUCE_VST3_CAN_REPLACE_VST2=0` ist Teil des eingefrorenen Identitaetsmanifests (§31.2/§44.1/§53.5, `SONDE-001`); die vier abgeleiteten CIDs stehen wortgleich in §31.2. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-21 | 08-20, `ab80522` | In Fassung 0.4 korrigiert: §48.4 traegt die verbindliche Reihenfolge (Connect → Hello lesen → impersonieren → SID pruefen → RevertToSelf → annehmen/ablehnen) samt `FALSE`-Regel; `security_vectors` (§66.2) hat den Negativtest. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-22 | 08-20, `ab80522` | Uebernommen: `SONDE-004` ist die erste Welle (Entwurf §65 + NEXT-SESSION). Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. |
| NAK-23 | 08-20, `ab80522` | In Fassung 0.4 erledigt: (a) der widerspruechliche Buendelungssatz ist entfernt, (b) §53.4 haelt fest, dass der gemeinsame Kern keine `JucePlugin_*`-Konstanten sieht, (c) `experiment_abort` ist im Nachrichtenvertrag. Befund aus `docs/pruefbericht-sondenentwurf-2026-08-20.md`. **Nachtrag 21.08.:** `docs/bauaufteilung-sonden.md` §1.3 trug bis zum 21.08. die Gegenposition („1 Batch = N Frames EINER Quelle + Wrapper") als „Festlegung bleibt gültig" weiter; am 21.08. auf Entwurf §33.1 und das gebaute `.fbs` berichtigt (eine Liste von `(source, frame)`, höchstens ein Frame je Quelle, Regel `quelle_doppelt`). |
