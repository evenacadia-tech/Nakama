Erstprüfung S25c / Ticket NAK-224 — gebundener Prüfauftrag des Dirigenten (2026-09-09)

Du bist der frische, lesende Prüfer. Prüfbereich ist ausschließlich der Änderungssatz dieses Tickets:
`git diff 090954fa5814c1503a6f352e80c577cbeee0a246...61c990b3824dd802cb51cb3a2370d9fbe543861d -- broker/src/coordinator/hypothese.rs broker/src/coordinator/hypothese/ broker/src/lebenslauf.rs broker/src/lib.rs broker/src/paar.rs broker/src/probe.rs broker/src/register.rs broker/src/server.rs broker/src/sitzung.rs broker/src/testhilfe.rs broker/src/transport/server_v3/ broker/src/zeit.rs broker/tests/broker_idle.rs tools/beweise.ps1 tools/plan/gesundheit.py`
mit BASIS = 090954fa5814c1503a6f352e80c577cbeee0a246 und ZIEL = 61c990b3824dd802cb51cb3a2370d9fbe543861d (HEAD; er muss vor und nach deinem Lauf identisch sein).
Alles andere, was seit BASIS im Repo geändert wurde, gehört zu anderen Tickets und ist hier nicht Prüfgegenstand.
Prüfstufe T2, verlangt vom Plan für S25c.

GATE-TEXT, wörtlich und vollständig (docs/plan/plan.json, Schritt S25c, Ticket NAK-224):
„ANGELEGT 08.09.2026 (Pflegeschritt 1 nach G5, Register NAK-224). `broker/src/coordinator/hypothese.rs` trug am 08.09.2026 3 762 Zeilen, weil NAK-212 und NAK-213 dort gebaut haben. INHALT, ein einziger verhaltensneutraler Änderungssatz: die Datei nach Fachbereich teilen (Screening und Deckel, Zusammenhang und Rangkomponenten, Klassenwahl und Konkurrenzlage, Enthaltungen, Befundbau und Sicht), Historie erhalten (`git log --follow`), Modulköpfe wahr. BEWEIS: Kanon vorher = Kanon nachher grün mit gleicher Beinzahl, P5-Korpus bytegleich (A28/A29), Determinismus M-25 über hundert Läufe, `git diff --stat` ohne Zeile in Tests oder Fixtures außer Pfadänderungen. Muster: S19b (NAK-121). GRENZE: keine Regeländerung, keine neue Schwelle, kein Fassungsschritt; Nebenbefunde nummeriert ins Manifest, nie stumm behoben. NACHTRAG 08.09.2026 (Messung beim G5-Abschluss, Register NAK-234): `broker/src/transport/server_v3/mod.rs` liegt mit 2 007 Zeilen ebenfalls über der Grenze und gehört in denselben Änderungssatz; generierte Dateien unter `broker/src/generiert/` sind keine Pflegefläche (A9 hält sie bytegleich zur Neuerzeugung). Dazu NAK-233 (toter Parameter `veraltet` in `zustand_aus_sicherheit`), zu schließen in derselben Aufteilung, sofern die Zustandsabbildung M-29 unberührt bleibt.“

Mehr verlangt das Gate nicht. Das Ticket hat keine Verhaltensmatrix; die Riegelkarte in docs/beweise/NAK-224.md §3 (R1 bis R10) ist die Referenz der Zusagen, der Schnittplan in §2 (Item-für-Item-Tabelle §2.4, Sichtbarkeiten §2.7) die Referenz dessen, was wohin verschoben wurde. Entscheid des Dirigenten im Ticketauftrag (docs/beweise/roh/NAK-224-auftrag.txt, Punkt 4): NAK-233 wird NICHT über eine Signaturänderung geschlossen, weil `broker/tests/sonde014_befund.rs:259` den Zweig `veraltet = true` als Teil von M-29 misst und der BEWEIS-Satz Testzeilen ausschließt; die Signatur bleibt, die Doku wird wahr. Das ist keine Lücke des Bauers, sondern die Regel. Ebenso Entscheid: `broker/src/lib.rs` (1 998 Zeilen, Register NAK-238) gehört zum Änderungssatz; NAK-235 (lange Funktionen), NAK-236 (Kommentar-Bezeichner außerhalb der drei Dateien) und NAK-237 (`// SAFETY:`) gehören nicht dazu.
Die Riegel/Tests, die es messen: R1 Kanon `tools/beweise.ps1` (62 Beine vorher wie nachher); R2 A28 `tools/…/erzeuge_p5_korpus.py --pruefen`, A29 `pruefe_p5_korpus.py`, SHA-256 von `eq-copilot/build/p5-korpus-ergebnis.json` aus `broker/tests/sonde014_p5_korpus.rs`; R3 `broker/tests/sonde014_hypothese.rs` (`ranking_ist_bytegleich_ueber_hundert_laeufe`, `screening_ist_deterministisch`); R4 `cargo test -- --list` (774 vorher wie nachher, Rohlisten docs/beweise/roh/NAK-224-testliste-*.txt); R5 `git diff --stat` über broker/tests, eq-copilot, generiert; R6 `git log --follow` auf `broker/src/coordinator/hypothese/mod.rs`; R7 Modulköpfe; R8 `broker/tests/sonde014_befund.rs` (M-29); R9 `tools/plan/gesundheit.py` (Pflegeticket-Liste, Ratschen 28/32/91, Selbsttest); R10 je Commit grüner Bau. Öffentliche Fläche: docs/beweise/roh/NAK-224-pub-vorher.txt und -nachher.txt (1 098 Einträge, Differenz leer), neue `pub(crate)`/`pub(super)` in docs/beweise/roh/NAK-224-sichtbarkeiten.txt. Jüngster Kanon: GRÜN 62/62, Exit 0, auf `3b902e4` (docs/beweise/roh/NAK-224-3b902e4.md); die Commits danach berühren nur docs/.

BEFUNDKLASSEN — du ordnest jeden Befund ein, der Dirigent prüft die Einordnung an der Quelle:
- DEFEKT: verletzt einen Satz des Gate-Textes oben, eine Zeile der Riegelkarte, einen Test oder eine Invariante aus CLAUDE.md (Identität eingefroren, Audio echtzeitfest, Nulltest bitidentisch, State verlustfrei). Reproduzierbar, mit Kommando; je Defekt: Datei:Zeile und der Satz, der bricht. Für dieses Ticket heißt DEFEKT vor allem: eine Verschiebung, die Verhalten ändert (anderer Wert, andere Reihenfolge, andere Sichtbarkeit nach außen, verlorener oder veränderter Test), eine Regel- oder Schwellenänderung, ein Modulkopf, der lügt, oder eine gebrochene Historie.
- LÜCKE: Gate, Riegelkarte und Entwurf sagen zu dem Fall nichts — benennen, keine Nacharbeit.
- HÄRTUNG: wünschenswert, von keiner Zusage verlangt — benennen, keine Nacharbeit.

AUSDRÜCKLICH AUSGESCHLOSSEN (kein Befund, keine Erwähnung als NEEDS_WORK-Grund):
1. docs/** — Manifeste, Register, Planstand, Prosa, Zeilenverweise, Trefferzahlen, Abschnittsstände, Kommentar-Wortlaute. Manifeste sind Kontext, kein Prüfgegenstand.
2. Im Register datierte Härtungen und ausgegliederte Punkte: NAK-235 (28 Funktionen über 200 Zeilen — kein Zerlegen langer Funktionen in diesem Ticket), NAK-236 (Kommentar-Bezeichner außerhalb der drei Ticketdateien), NAK-237 (undokumentierte `unsafe`-Blöcke). Die Nebenbefunde NB-2 (flackernder Test `acceptor_ueberlebt_die_verbindungsgrenze`, Testkörper unverändert verschoben) und NB-3 (hängender v2-Servertest in `broker/src/server.rs`, dort nur eine Pfadänderung) stehen in docs/beweise/NAK-224.md §5 und gehen ins Register — sie sind nur dann ein Befund dieses Tickets, wenn du zeigst, dass der Schnitt sie verursacht (Prüffrage 5). Ein Befund gegen ein Prüfskript, einen Riegel oder den Runner, der absichtliche Sabotage in repo-eigenen Quellen voraussetzt, ist HÄRTUNG — das Bedrohungsmodell der Riegel ist die versehentliche Regression, nicht der Angreifer mit Schreibrecht.
3. tools/dirigent/pruefliste.md ist eine Arbeitsliste des Workers, keine Anforderungsquelle. §2.4 des Codex-Skills sondenplan-audit („Wiederholung bis lückenlos“) gilt für Gate-Audits, nicht für diesen Ticketreview. Kosmetik, Stil, optionale Härtung, theoretische Randfälle, Ticketfremdes: nein.

WAS DU TUST:
1. Lies den Diff im Prüfbereich. Für die Verschiebungen ist `git diff -M --color-moved=plain 090954fa...61c990b3 -- <pfad>` oder ein Vergleich alter und neuer Funktionstexte (z. B. `git show 090954fa:broker/src/coordinator/hypothese.rs` gegen die neuen Module) das Werkzeug; der Schnittplan §2.4 sagt, welches Item wohin gehört.
2. Prüfe am Zielstand, ob Tests und Riegel messen, was Gate und Riegelkarte verlangen: (1) Ist jede Verschiebung verhaltensneutral — Funktionskörper, Konstanten (insbesondere die M-25-Größen in `hypothese/determinismus.rs`: Bootstrap-Saat, Reduktionsreihenfolge, Quantisierung, Tie-Break-Key; die Screening-Deckel aus M-18), Reihenfolgen und Enum-Varianten textgleich bis auf `use`-Zeilen, Sichtbarkeit und Modulkopf? Nenne jedes Item, das sich inhaltlich unterscheidet. (2) Öffentliche Fläche: kein Item ist breiter sichtbar als vorher; jeder bisher gültige Pfad `eqcop_broker::…` und `crate::coordinator::hypothese::…` bleibt über `pub use` erreichbar; die neuen `pub(super)`/`pub(crate)` reichen nicht weiter als die frühere dateiprivate Reichweite; testonly Bauer stehen unter `#[cfg(test)]`. (3) Testbestand: 774 Tests vorher wie nachher, Namen bis auf Modulpräfix gleich, Inline-Testkörper textgleich, kein Test still weggefallen oder mit `#[ignore]` versehen; die eine Zeile in `broker/tests/broker_idle.rs` ist eine reine Pfadänderung. (4) `tools/plan/gesundheit.py` und die A32-Zeile in `tools/beweise.ps1`: nur Pflegeticket-Liste und Ratschen (35 → 32 gesenkt, keine erhöht), keine neue Schwelle, keine geänderte Messlogik, Selbsttest weiter grün; die Runner-Behauptung nennt Zahlen, die der Lauf misst. (5) NB-2 und NB-3: vorbestehendes Zeitfenster im unveränderten Testkörper beziehungsweise außerhalb des Ticketbereichs — oder durch den Schnitt eingeführt? Du darfst lesen und lesende Kommandos fahren (`git`, `cargo test -- --list`, `py -3.13 tools/plan/gesundheit.py`); du schreibst nichts.
3. Prüfe, ob der Änderungssatz seit BASIS etwas am Produktverhalten gebrochen hat (Identität, Passthrough/Nulltest, State, Audio-Thread; hier vor allem: Hypothesenrechnung, Broker-Lebenslauf `broker_starten`/`broker_geordnet_stoppen`, v3-Transportkonstanten) — nenne, was du selbst nachgemessen und was du nur gelesen hast.

URTEIL — genau eine der drei Formen, als erste Zeile:
URTEIL: PASS — kein DEFEKT (Lücken und Härtungen dürfen benannt sein).
URTEIL: NEEDS_WORK — mindestens ein DEFEKT; je Defekt: Datei:Zeile, Reproduktion, welcher Satz von Gate oder Riegelkarte bricht.
URTEIL: BLOCKED — du konntest den Prüfbereich nicht lesen.
Danach: geprüft / nicht geprüft, je ein Absatz.
