<!-- NAKAMA-URTEIL: T1 PASS 2026-09-09 -->

# NAK-223 — S25b: Gesundheitsmaß für Codebase und Kontext mit Schwellen

| Merkmal | Wert |
|---|---|
| Ticket | NAK-223 (Register `docs/offene-punkte.md`), Planschritt **S25b** (`docs/plan/plan.json`), Prüfstufe **T2**. Erstes Pflegeticket des Plans, angelegt 08.09.2026 nach dem User-Wort „Codebase-Gesundheit ist Planbestandteil, nicht Nebentätigkeit". |
| Phase | **Nachgearbeitet — Nacharbeit 2 nach der Wiederprüfung 1.** Werkzeug `tools/plan/gesundheit.py` gebaut, als nicht blockierendes Bein **A32** im Kanon verdrahtet, in Dirigenten-Skill §3.5 und Kontext-Hygiene-Playbook angebunden. Die Erstprüfung (Codex `gpt-6-astra`, Effort max, read-only, 09.09.2026, Stand `d6a0bfda`) urteilte **NEEDS_WORK** mit zwei reproduzierten Defekten; beide sind in Nacharbeit 1 behoben (§5.5, §5.6). Die **Wiederprüfung 1** (derselbe Prüfer, Stand `afc2d699`) bestätigt beide als geschlossen und urteilt erneut **NEEDS_WORK** wegen **einer Regression der Namenswahl**: ein gültiger Elementname, der zugleich Schlüsselwort ist (`struct union where`), wurde als Qualifizierer übersprungen. Behoben in Nacharbeit 2 nach Regel **R-D1b** — Qualifizierer nur noch **vor** dem Schlüsselwort, das erste Wort danach ist ausnahmslos der Name (§5.7, §5.8). Jede Runde mit Mutationsbeweis an der zusagetragenden Zeile und mit der Reproduktion des Prüfers auf dem neuen Stand. Selbsttest **116/116** (62 → 96 → 116), 12 Rotbeweise, einer davon um zwei Abschnitte und einer um einen erweitert. T2 setzt der Dirigent nach der Nachprüfung. |
| Prüfstufe | **T1 PASS 2026-09-09** — Selbstaudit, Selbsttest 62/62, 12 Rotbeweise und Abschlusskanon **GRÜN 62/62** auf `092db4de` (Exitcode 0, Arbeitsbaum sauber), alles aus dieser Session. **Was T1 nicht sagt:** keine fremde Prüfung, kein FL-Lauf. T2 setzt der Dirigent nach der Codex-Prüfung. |
| Basis-SHA | `274d3ff8371aa85b0132f996b7b759c8dd8fc6da` (= `git rev-parse HEAD` beim Start, Worktree sauber) |
| Kanon vorher | `89c97f0` **GRÜN 61/61** (Abschlusskanon NAK-214, Rohausgabe `docs/beweise/roh/NAK-214-89c97f0.md`). Zwischen `89c97f0` und dem Basis-SHA liegen nur `docs/`- und CLAUDE.md-Commits; der beglaubigte Codestand gilt unverändert. |
| Kanon nachher | `092db4de` **GRÜN — 62/62 Kanonläufe bestanden, Exitcode 0** (abgekoppelt, Lauf 09.09.2026 09:23–09:56, Arbeitsbaum sauber, Rohausgabe `docs/beweise/roh/NAK-223-092db4d.md`; über alle Cargo-Beine 806 passed, 0 failed). Die Beinzahl steigt von 61 auf **62** — der Zuwachs ist **A32**, und es steht darin als `[HINWEIS] Exit 4 - Befund, nicht blockierend`, ohne Urteil oder Exitcode zu bewegen (§5.4). Die Nebenmeldungen „2 geplante Prüfung(en)" und „1 stillgelegte(s) Bein(e)" stehen wörtlich wie in jedem grünen Kanon seit NAK-213. |
| Ticketpfade | `tools/plan/gesundheit.py` (neu); `tools/beweise.ps1` (nur: zwei Hilfsfunktionen `Feldwert`/`Ist-Hinweisexit`, ein `Ordner`-Feld im Python-Zweig, der `[HINWEIS]`-Zweig, ein Term in `$gruen`, der Eintrag **A32**); `.claude/skills/dirigent/SKILL.md` (nur die Klammer in §3.5); `docs/context-hygiene-playbook.md` (nur ein Absatz im Budget-Abschnitt); `docs/beweise/NAK-223.md` und `docs/beweise/roh/NAK-223-*.txt` |
| Nicht angefasst (Grenze) | **Produktcode** (`broker/src`, `eq-copilot/plugin/**`) — `git diff` dort ist leer, gemessen. Tests, Fixtures, Verträge (`eq-copilot/schemas/**`), `docs/offene-punkte.md`, `docs/plan/*`, `docs/PLAN-STAND.md`, `CLAUDE.md`, `design/`. **Im Runner:** kein bestehendes Bein, kein Riegel, keine Baulogik; der Urteilsblock nur um den `[HINWEIS]`-Term erweitert (Begründung §2.6). |
| Änderungssatz | Bau: Werkzeug, Runner-Bein, Skill-Aufruf und Playbook-Satz in **einem** Änderungssatz (Prüfliste F). **Nacharbeit 1:** ausschließlich `tools/plan/gesundheit.py`, die beiden um je einen Abschnitt erweiterten Rotbeweise (`roh/NAK-223-rot-deadcode.txt`, `roh/NAK-223-rot-clippy.txt`) und dieses Manifest; Runner, Skill, Playbook, Register und Plan bleiben unberührt — das ist die Ticketgrenze der Runde und in §5.6 mit `git diff --stat` belegt. **Nacharbeit 2:** ausschließlich die Namenswahl in `tools/plan/gesundheit.py` samt ihren Selbsttestfällen, `roh/NAK-223-rot-deadcode.txt` (ein angehängter Abschnitt §4, nichts Altes umgeschrieben) und dieses Manifest — drei Dateien, in §5.8 mit `git diff --stat` belegt. |
| Prüfmodell | T1 durch den Erbauer; Prüfung durch Codex `gpt-6-astra` (Effort max, read-only) setzt der Dirigent an. |

---

## 1. Ticket- und Regelbezug, wörtlich

**User-Wort** (Register `docs/offene-punkte.md`, Zeile NAK-223, 08.09.2026), das
den Schritt begründet:

> „ich möchte dass die codebase selbst regelmäßig auf solche probleme überprüft
> und verbessert wird. das projekt muss nachhaltig funktionieren und wartbar
> bleiben. das geht nur wenn die codebase regelmäßig in perfektem shape bleibt.
> nicht immer nur dann wenn hart an der grenze ist. … ich will nicht dass deine
> eine kleine nebentätigkeit ist, sondern bestandteil vom plan . da ich kein
> coder bin, muss ich den code für di KI so wartbar wie möglich halten"

**Ticket** (`docs/plan/plan.json`, Schritt S25b, Ticket NAK-223, Stufe T2),
wörtlich: „INHALT: `tools/plan/gesundheit.py` misst die Codebase (Zeilen je
Quelldatei unter broker/src, eq-copilot/plugin/src und eq-copilot/plugin/core
mit Grenze 2 000 und Ziel 1 500; Funktionen über 200 Zeilen; `cargo
clippy`-Warnungen; aufruferlose `#[allow(dead_code)]`-Helfer; Kommentare, die
auf nicht existente Bezeichner verweisen) und den Kontext (Bytes von MEMORY.md,
Root-CLAUDE.md und Dirigenten-Skill gegen die Grenzen des Playbooks
`docs/context-hygiene-playbook.md`; Index-Zeilen über 250 Zeichen;
Memory-Dateien ohne Index-Link), druckt eine Tabelle und gibt Exit 4, sobald
eine Schwelle gerissen ist. Angebunden in der Dirigenten-Skill §3.5
(Abschlussfenster) und als nicht blockierendes Bein in `tools/beweise.ps1`. Die
Schwellen entscheidet der Bauer aus der Messung des Iststands am Basis-SHA, nie
geraten; der Iststand steht im Manifest. GRENZE: kein Produktcode, kein Umbau
bestehender Beine."

**Ticketnachtrag** (Dirigent, Technik, 08.09.2026): „generierte Dateien unter
`broker/src/generiert/` (Codegen aus dem .fbs, Bein A9) sind von der
Zeilenmessung ausgenommen; die Handmessung beim G5-Abschluss (Register NAK-234)
zählt sie nicht."

**CLAUDE.md-Invariante**, die die Bauform bestimmt: „**Keine toten
UI-Elemente.** Jedes sichtbare Element führt einen Handgriff aus oder meldet
ehrlich einen Zustand." Übertragen auf ein Messwerkzeug: ein Maß, das nichts
misst oder strukturell immer grün ist, wäre ein totes Element. Daraus folgen die
Ratschen in §2.4 und der Selbsttest in §2.5.

**CLAUDE.md-Regel für den Pflegeschritt** (08.09.2026): „Je Phase steht ein
eigener Pflegeschritt im Plan (S25b–S25d, S31c, S35b), dazu Schwellen, die
sofort ein Pflegeticket auslösen, gemessen mit `py -3.13
tools/plan/gesundheit.py` (Ticket NAK-223; bis es gebaut ist, von Hand: keine
Quelldatei über 2 000 Zeilen)."

---

## 2. Iststand am Basis-SHA und die daraus gesetzten Schwellen

### 2.1 Iststand, gemessen am 09.09.2026 auf `274d3ff8`

Alle Zahlen aus dem Werkzeug selbst (`py -3.13 tools/plan/gesundheit.py
--clippy`), Worktree beim Messen sauber. Zwei Läufe hintereinander sind
bytegleich (§3, Determinismus).

| Maß | Ist | Ziel | Grenze | Status |
|---|---|---|---|---|
| Quelldateien über 2 000 Zeilen | 5 | 0 | 0 | **GRENZE** |
| Quelldateien über Ziel 1 500 Zeilen | 7 | 0 | — | ZIEL |
| Funktionen über 200 Zeilen | 28 | 0 | 28 | ZIEL |
| Clippy-Fundstellen (broker) | 91 | 0 | 91 | ZIEL |
| allow(dead_code) ohne Aufrufer | 0 | 0 | 0 | OK |
| Kommentar-Bezeichner ohne Code | 35 | 0 | 35 | ZIEL |
| MEMORY.md (Bytes) | 3 251 | 22 528 | 24 986 | OK |
| CLAUDE.md (Bytes) | 22 858 | — | 24 576 | OK |
| Dirigenten-Skill (Bytes) | 35 685 | — | 36 864 | OK |
| MEMORY.md: Zeilen über 250 Zeichen | 0 | 0 | 0 | OK |
| Memory-Dateien ohne Index-Link | 0 | 0 | 0 | OK |

**Deckungsgleich mit der Referenzmessung des Dirigenten** (Register NAK-223,
NAK-234, 08./09.09.2026): sechs Dateien über 2 000 Zeilen, eine davon generiert
— das Werkzeug zählt die generierte nicht mit und kommt auf 5. Zwölf über 1 500
— das Werkzeug zeigt 5 + 7 = 12, weil es die beiden Schwellen als zwei Zeilen
führt und eine Datei nur einmal zählt. MEMORY.md 3,4 KB, CLAUDE.md 22,9 KB,
Skill 35,7 KB, 0 Index-Zeilen über 250. Keine Abweichung.

Die Tabelle zeigt den Stand **am Basis-SHA**. Zwei Werte hat dieses Ticket
selbst bewegt: der Dirigenten-Skill schrumpft von 35 685 auf **35 654** Bytes
(§2.6), und das Playbook wächst um einen Absatz — es zählt zu keinem Maß.
Ein Lauf auf dem Endstand zeigt deshalb 35 654; alle übrigen Zahlen stehen
unverändert.

### 2.2 Die fünf Treffer über der Zeilengrenze

| Datei | Zeilen | Pflegeticket |
|---|---|---|
| `eq-copilot/plugin/core/analysis/FeatureEngine.h` | 4 390 | NAK-225 |
| `eq-copilot/plugin/src/PluginProcessor.cpp` | 4 389 | NAK-225 |
| `broker/src/coordinator/hypothese.rs` | 3 986 | NAK-224 |
| `eq-copilot/plugin/core/ipc/ControlClient.cpp` | 2 475 | NAK-225 |
| `broker/src/transport/server_v3/mod.rs` | 2 169 | NAK-234 |

Jeder Treffer hat ein Pflegeticket. Ein sechster trüge in der Trefferliste
`[OHNE PFLEGETICKET]` und löste zusätzlich die Hinweiszeile „N Datei(en) über
der Grenze OHNE Pflegeticket — neuer Registerpunkt nötig" aus. Die Zuordnung
steht als Konstante `PFLEGETICKETS` im Werkzeug; sie macht den Unterschied
zwischen bekannter Schuld und neuer sichtbar, statt beides zu einer Zahl zu
verrühren.

`broker/src/generiert/nakama_telemetry_v1_generated.rs` (2 446 Zeilen) ist
ausgenommen. Bein **A9** hält ihn bytegleich zur Neuerzeugung aus dem `.fbs` und
lief in dieser Session grün (§5), also ist er kein Wartungsaufwand.

### 2.3 Woher jede Schwelle kommt

| Schwelle | Wert | Herkunft |
|---|---|---|
| Zeilen je Quelldatei — Grenze | 2 000 | Plan S25b und CLAUDE.md, wörtlich. **Gesetzt, nicht gemessen.** |
| Zeilen je Quelldatei — Ziel | 1 500 | Plan S25b, wörtlich. Gesetzt. |
| Funktion — Maßstab | 200 Zeilen | Ticket NAK-223, wörtlich. Gesetzt. |
| Funktionen über 200 — Anzahl | 28 | **Ratsche = Iststand.** |
| Clippy-Fundstellen | 91 | **Ratsche = Iststand.** |
| allow(dead_code) ohne Aufrufer | 0 | Iststand ist 0; die Grenze ist dieselbe Zahl. |
| Kommentar-Bezeichner ohne Code | 35 | **Ratsche = Iststand.** |
| MEMORY.md Bytes | Ziel 22 528 / Grenze 24 986 | `docs/context-hygiene-playbook.md`, wörtlich („Arbeitsziel ≤ 22 KB", „~24,4 KB (24 986 Bytes)"). |
| CLAUDE.md Bytes | 24 576 | Playbook und Skill §3.5, wörtlich („≤ 24 KB"). |
| Dirigenten-Skill Bytes | 36 864 | Playbook und Skill §3.5, wörtlich („≤ 36 KB"). |
| Index-Zeile Zeichen | 250 | Playbook Phase C, wörtlich. |
| Memory-Dateien ohne Index-Link | 0 | Playbook Phase F, wörtlich („0 echte Waisen"). |

### 2.4 Warum drei Schwellen Ratschen sind

Der Ticketauftrag verlangt, die Grenze so zu setzen, „dass sie heute genau dort
reißt, wo ein Pflegeticket bereits angelegt ist, und sonst nicht". Für die
Zeilengrenze ist das ohne Zutun erfüllt: sie reißt heute bei genau den fünf
Dateien aus §2.2, alle mit Ticket. Für die drei übrigen Maße gibt es kein
Pflegeticket — also darf keines davon heute reißen, und der Iststand ist die
Grenze.

Eine Ratsche ist kein Freibrief, sondern das schärfere Werkzeug: sie hält heute
und reißt bei der **ersten** Verschlechterung. Eine 0 stünde dauerhaft rot, sähe
nach Normalzustand aus und würde nach kurzer Zeit überlesen — genau der Zustand,
den der User mit „nicht immer nur dann wenn hart an der Grenze ist" ausschließt.
Das Ziel bleibt bei allen dreien 0 und steht in jeder Tabelle daneben; sinkt
eine Zahl durch ein Pflegeticket, wird die Ratsche im selben Ticket nachgezogen,
sonst deckt sie stillschweigend wieder Platz für neue Funde.

Damit ist heute **genau ein Maß rot**, und es ist das, für das Pflegetickets
offenstehen. Jede weitere Rotmeldung ist echte Verschlechterung.

### 2.5 Die Heuristiken und ihre Fehlerklassen, ehrlich benannt

**Zeilenzählung.** Zeilen = Anzahl `\n` plus eins, wenn die Datei nicht mit
einem Umbruch endet; die leere Datei hat 0. CRLF und LF zählen gleich, ein
UTF-8-BOM wird verworfen statt mitgezählt. Alle fünf Fälle im Selbsttest, jeder
mit seinem Gegenteil. Gegenprobe: Bein A9 meldet für die generierte Rust-Datei
2 446 Zeilen, das Werkzeug ebenfalls.

**Funktionen über 200 Zeilen.** Klammertiefe auf einem Text, in dem Kommentare,
Strings, Rohstrings (`r#"…"#`, `R"x(…)x"`) und Zeichenliterale durch Leerzeichen
ersetzt sind — Zeilenstruktur und jede Position bleiben erhalten. Ein Block
`{…}` gilt als Funktion, wenn der Text zwischen dem letzten `;`/`{`/`}` und der
öffnenden Klammer eine Signatur ist (Rust: enthält `fn NAME` und eine
geschlossene Parameterliste; C++: endet auf `)` plus erlaubte Qualifikatoren und
beginnt nicht mit `if`/`for`/`while`/`switch`/`catch`/`else`/`do`).

*Fehlerklassen, nicht behoben:* C++-Lambdas werden erkannt und **bewusst nicht**
gezählt — ihre Zeilen zählen zur umgebenden Funktion, und die Zahl der
übersprungenen Lambdas steht im Bericht. Ein funktionsartiges Makro `MAKRO(x)
{ … }` ist von einer Definition textuell nicht unterscheidbar und wird gezählt.
Rust-Closures (`|x| { … }`) und `macro_rules!`-Rümpfe sind keine Kandidaten
(kein `fn`). Ein Präprozessorzweig mit unbalancierten Klammern verschöbe die
Tiefenzählung; im gemessenen Bestand kommt keiner vor.

*Gegenprüfung an drei Stellen von Hand:* `verbindung.rs` Zeile 147 → 890 = 744
Zeilen, `SourcesModel.cpp` 538 → 1281 = 744, `PluginProcessor.cpp` 581 → 974 =
394. Alle drei exakt wie gemeldet.

*Gefundener und behobener Fehler beim Bauen:* Die erste Fassung erkannte
Zeichenliterale über ein Zeichenfenster („Schlusszeichen in den nächsten 12
Zeichen"). In `struct S<'a> { x: &'a str }` fand dieses Fenster das **zweite**
`'a` und maskierte die öffnende Klammer dazwischen weg — die Tiefenzählung wäre
in jeder Rust-Datei mit Lebensdauern still danebengelegen. Ersetzt durch die
exakte Form eines Literals (`ZEICHENLITERAL`); trifft sie nicht, ist das `'`
gewöhnlicher Text. Derselbe Fix deckt den C++-Ziffernstrenner `1'000` ab. Beide
Fälle stehen als Selbsttestfälle.

**Clippy-Fundstellen.** `cargo clippy --manifest-path broker/Cargo.toml
--all-targets --message-format=json -- --cap-lints warn`, gezählt werden
**eindeutige** Fundstellen über (Datei, Zeile, Spalte, Lint-Code).

`--cap-lints warn` ist nötig, nicht Bequemlichkeit: `broker/Cargo.toml` setzt
`undocumented_unsafe_blocks = "deny"`, der Lauf bricht ohne den Schalter nach 26
Fehlern ab und meldet nur einen Teil der Funde. Gemessen am 09.09.2026: **36
gemeldete Warnungen im abgebrochenen Lauf gegen 91 eindeutige Fundstellen im
vollständigen.** Ein Textparser auf der Konsolenausgabe hätte hier still die
kleinere Zahl beglaubigt.

Die Dedup ist ebenso nötig: ohne sie zählt dieselbe Diagnose für `lib` und `lib
test` doppelt (roh 153 Diagnosen zu 91 Fundstellen).

*Abgebrochener Prozess (Nacharbeit 1, 09.09.2026).* Ein Exitcode ≠ 0 des
gestarteten `cargo clippy` ist ein **Werkzeugfehler** und wird geprüft, **bevor**
eine einzige Diagnose gelesen wird — unabhängig davon, ob welche im Strom
standen. Die erste Fassung fragte `not fund and lauf.returncode != 0` und
übersprang die Fehlerbehandlung damit genau dann, wenn der Fehler selbst eine
Diagnose geschrieben hatte: ein Compilerfehler E0308 mit Exit 101 erschien als
gültige Warnungszahl 1, und der Gesamtlauf endete mit 0 (Erstprüfung, Defekt 2,
§5.5). Jetzt wirft `miss_clippy` `RuntimeError` und der Lauf endet auf demselben
Weg wie bei einem fehlenden Messort mit **Exit 2** und `Werkzeugfehler: cargo
clippy Exit N - <letzte stderr-Zeile>`. „Nicht messbar" bleibt allein dem Lauf
**ohne** `--clippy` und der Umgebung ohne cargo vorbehalten; ein fehlendes
`broker/Cargo.toml` ist seither ebenfalls Werkzeugfehler, nicht „nicht messbar".
Der echte Messlauf ist davon unberührt: `--cap-lints warn` hält den
Prozess-Exitcode bei 0 (gemessen, §5.6).

*Laufzeit:* der Schalter `--clippy` baut und läuft deshalb nur bewusst. Gemessen
mit warmem Cargo-Cache 4,9 s; das Kanon-Bein fährt ihn **nicht** und meldet das
Maß als „nicht messbar (Schalter --clippy)".

**allow(dead_code) ohne Aufrufer.** Zum Attribut wird der erste folgende
Bezeichner gesucht (auf derselben oder einer der nächsten Zeilen, Attribute und
Kommentare übersprungen); Befund, wenn der Name außerhalb seiner Definitionszeile
nirgends im Crate vorkommt. **Bezeichner heißt: das erste Wort hinter dem
Elementschlüsselwort** (Nacharbeit 1 und 2, 09.09.2026). Die Grammatik ist
`[Qualifizierer]* Schlüsselwort [mut]? Name`: Sichtbarkeits- und
Funktionsqualifizierer — `pub`, `pub(crate)`, `pub(in …)`, `const`, `unsafe`,
`async`, `default`, `extern "C"` — stehen nur **vor** dem Schlüsselwort und
werden nur übersprungen, wenn ein Schlüsselwort folgt; `mut` gilt nur
unmittelbar nach `static`. Das Wort **nach** dem Schlüsselwort ist ausnahmslos
der Name, auch wenn es `union`, `default` oder `where` heißt — die
Wiederholung hinter dem Schlüsselwort übersprang bei `struct union where` den
echten Namen und las die fremde `where`-Klausel (Wiederprüfung 1, Regel
R-D1b, §5.7/§5.8). `const fn name` gegen `const NAME: T` und `static mut NAME`
gegen `static NAME` bleiben unterschieden; beim ersten Paar erledigt das der
Rückfall des Ausdrucks (Backtracking), beim zweiten die eigene Alternative
`static mut` vor der Schlüsselwortliste. Iststand 0: das einzige Attribut im
Bestand (`griff.rs`:419,
`pub struct WorkerPlatzProbe(#[allow(dead_code)] WorkerPlatz);`) steht inline
vor einem Typnamen, der anderswo verwendet wird.

*Gefundener und behobener Fehler beim Bauen:* Die erste Fassung nahm bei der
Verwendungssuche die **Attribut**zeile aus, nicht die **Definitions**zeile — der
Name steht aber eine Zeile tiefer, wurde dort gefunden und das Maß hätte
strukturell nie einen Befund gemeldet. Der Selbsttestfall „dead_code ohne
Aufrufer ist ein Befund" hat ihn gefangen.

*Fehlerklassen:* ein in `#[cfg_attr(…, allow(dead_code))]` verpacktes Attribut
wird nicht gesehen — die Form beginnt nicht mit `#[allow`. Ein Struct, das nur
noch in `impl Foo` vorkommt, gilt als verwendet. Beide stehen im Skriptkopf.

**Fehlender Messort.** Fehlt einer der drei Orte, ist das ein **Werkzeugfehler**
(Exit 2), kein leeres Ergebnis. Das Kontext-Hygiene-Playbook nennt die Klasse
beim Namen: „Ein Prüfkommando auf einen verschobenen Pfad liefert still 0 und
lässt Veraltetes ‚verifiziert' aussehen." Ohne diese Wache fiele nach einer
Umbenennung von `eq-copilot/plugin/core` ein Drittel der Codebase unbemerkt aus
der Messung — und das Werkzeug meldete grün. Rotbeweis
`roh/NAK-223-rot-messort.txt`: 0 → **2** → 0, und die 2 ist im Runner `[ROT]`,
weil `HinweisExits` nur die 4 abdeckt.

**Kommentar-Bezeichner ohne Code.** Kandidat ist eine Backtick-Spanne in einem
Kommentar der Messorte, die genau wie ein Bezeichner aussieht (mindestens 4
Zeichen), kein Schlüsselwort und kein gewöhnliches Wort aus der Stoppliste ist.
Befund, wenn der Name in keiner Quelle und keinem Vertrag des Repos außerhalb von
Kommentaren vorkommt.

Diese Heuristik hat beim Bauen **zweimal** deutlich nachgeschärft werden müssen,
jedes Mal mit einer benannten Ursache:

1. **409 → 107.** Die Nachweisbasis kam anfangs aus dem *maskierten* Text — und
   Maskierung löscht String-Literale. Ein Protokoll- oder Schemafeld
   (`event_log`, `assistant_step_update`, `intent_update`) existiert im Code
   aber genau als String. Für die Klammerzählung müssen Strings weg, für die
   Existenzprüfung müssen sie bleiben: dasselbe Rohmaterial, zwei Sichten
   (`ohne_kommentare`).
2. **107 → 49.** Die Basis umfasste nur Rust und C++. Ein Kommentar darf auf ein
   Schemafeld (`.json`, `.fbs`), ein Bau- oder Testziel (`CMakeLists.txt`,
   `Cargo.toml`, ein Rust-Testziel heißt wie seine Datei) oder ein
   Werkzeugskript verweisen. Erweitert auf Quellen **und Verträge** des Repos
   plus die Dateinamen selbst. `docs/` und `.md` bleiben **bewusst draußen**:
   Manifeste zitieren Bezeichner fortlaufend, und ein Kommentarverweis, den nur
   noch ein altes Manifest kennt, ist der Fund, den dieses Maß sucht — nicht
   sein Gegenbeweis.
3. **49 → 35.** Eine Stichprobe von zwölf Treffern zeigte, dass die Mehrheit
   **absichtliche Historienverweise** sind: „`TAIL_SCHRITT_JE_TICK` ist
   ENTFALLEN", „hier stand bis zur Runde 3 ein `baseRevision`-Parameter", „Die
   Runde 2 hielt genau EINEN `Urteilmitschnitt`". Diese Kommentarform ist im
   Nakama-Kanon erwünscht. Übernommen wurde deshalb die Umfeldausnahme aus
   `tools/plan/dokuriegel.py`: 200 Zeichen davor bis 200 danach im selben
   Kommentar, Marker `entfallen`/`gestrichen`/`umbenannt`/`Runde N`/`hätte`/…
   Solche Treffer werden **gezählt und benannt** (12 am Basis-SHA), aber nicht
   als Befund geführt.

*Fehlerrate der verbleibenden 35, aus derselben Stichprobe geschätzt:* rund ein
Drittel sind Fremdnamen, die es im Repo nicht geben kann und soll —
Windows-Konstanten (`STATUS_STACK_OVERFLOW`, `NMPWAIT_USE_DEFAULT_WAIT`,
`nDefaultTimeOut`), ein Assemblerbefehl (`cvttsd2si`), externe Bibliotheken
(`eigen3`, `eigen4`), ein Git-SHA (`ed9bbf7`). Der Rest sind echte tote
Verweise: `uebernehmeSnapshot` (`SourcesModel.cpp`:492, :1636) nennt eine
Methode, die es nicht gibt, `kBlockFrist` (`server_v3/mod.rs`:520) eine
Konstante, die anders heißt. Beide stehen in §4 als Nebenbefund.

**Kontextmaße.** Bytes von `MEMORY.md`, `CLAUDE.md` und dem Dirigenten-Skill;
Zeilen über 250 Zeichen in `MEMORY.md`; Memory-Dateien ohne `(datei.md)`-Link
oder `[[slug]]` im Index. Der Memory-Ordner ist **rechnerlokal**
(`~/.claude/projects/<slug>/memory/`, Slug aus dem absoluten Repo-Pfad, hier
`C--Users-phili-Projekte-Nakama`). Fehlt er — anderer Rechner, andere
Installation —, melden diese Maße „nicht messbar" und sind **nie rot**. Ein
Werkzeug, das auf dem Laptop rot wird, weil dort ein Ordner anders heißt, misst
den Rechner und nicht das Projekt.

### 2.6 Warum das Runner-Bein so gebaut ist

Der Ticketauftrag: „ein Befund von gesundheit.py darf weder das Kanonurteil noch
den Exitcode des Runners ändern, muss aber in Übersicht und Rohausgabe sichtbar
sein".

Gebaut als ein Feld `HinweisExits=@(4)` am Kanoneintrag, gelesen über
`Ist-Hinweisexit` — mit demselben `PSObject.Properties`-Zugriff wie das
bestehende `Ist-Stillgelegt`, weil `Set-StrictMode -Version Latest` beim
Punktzugriff auf ein fehlendes Feld wirft. Exit 4 wird zu `[HINWEIS]` statt
`[ROT]`, jeder andere Exitcode ungleich 0 bleibt `[ROT]`: ein abgestürztes oder
fehlendes Messwerkzeug ist ein echter Befund.

**Der eine Eingriff in den Urteilsblock ist nötig, nicht bequem.** `$gruen`
zählte bisher nur `ExitCode -eq 0`. Ein `[HINWEIS]`-Bein fiele damit heraus, und
die Urteilszeile lautete `GRUEN - 61/62` statt `GRUEN - 62/62` — der Befund
hätte das Urteil bewegt, genau das, was der Auftrag verbietet. Deshalb zählt
`[HINWEIS]` in `$gruen` mit. Das ist keine Schönung: die Übersichtstabelle jedes
Manifests trägt das Symbol `[HINWEIS]` samt Statustext „Befund, nicht
blockierend (siehe Rohausgabe)", und die volle Trefferliste steht in der
Rohausgabe des Beins — beide Sichtbarkeitsforderungen ohne weitere Änderung am
Runner erfüllt, weil beide Tabellen `$e.Symbol` und `$e.StdOut` ohnehin rendern.

Der **Selbsttest läuft im selben Aufruf voran** (`--mit-selbsttest`). Ein Maß,
dessen Werkzeug nicht mehr misst, ist gefährlicher als gar keines: es meldete
jede Verschlechterung als „alles in Ordnung". Rot im Selbsttest heißt Exit 2 →
`[ROT]` im Kanon, und es wird gar nicht erst gemessen.

Zweite Änderung am Runner: das optionale Feld `Ordner` im Python-Zweig
(Standard bleibt `tools\eq-copilot`), weil dieses Werkzeug in `tools\plan` liegt.

*Laufzeit des Beins* (Selbsttest plus Messung, ohne clippy): 4,64 / 4,68 / 4,77 s
in drei Läufen — die Ticketvorgabe ist „unter 60 s".

---

## 3. Prüfliste `tools/dirigent/pruefliste.md` — wo jede Zeile gemessen ist

| Zeile der Prüfliste | Wo gemessen |
|---|---|
| **A** Rückstau, Prioritätsklassen | Nicht anwendbar: kein IPC, keine Queue, kein Puffer in diesem Ticket. |
| **B** Lebenszyklus | Nicht anwendbar: kein Verbinden/Trennen, kein Thread, kein Join. |
| **C** Verträge und Längen — Zahlenränder | Selbsttest: 2 000/2 001 Zeilen, 200/201 Zeilen je Funktion, 250/251 Zeichen je Indexzeile, Grenze exakt getroffen hält, leere Datei, fehlender Schlussumbruch, CRLF, BOM. Jeder Rand mit seinem Gegenteil. Rotbeweise `NAK-223-rot-dateizeilen.txt`, `-funktionen.txt`, `-indexzeile.txt`, `-memory-bytes.txt`, `-claudemd.txt`, `-skill.txt` fahren dieselben Ränder als echte Verletzung samt Rücknahme. |
| **D** Riegel ist fail-closed, Unbekanntes ist ROT | Exit 2 bei rotem Selbsttest, bei fehlendem Messort und — seit Nacharbeit 1 — bei jedem Exitcode ≠ 0 des `cargo clippy`-Prozesses (alle drei `RuntimeError` → „Werkzeugfehler"), Exit 3 nur bei `--clippy` ohne cargo. Im Runner bleibt jeder Exitcode außer 0 und 4 `[ROT]`. Rotbeweis `roh/NAK-223-rot-messort.txt` misst den verschobenen Messort: **2**, nicht 0 und nicht 4; `roh/NAK-223-rot-clippy.txt` §3 misst den abgebrochenen Prozess, samt Selbsttestfall „Gesamtlauf erbt den Werkzeugfehler". |
| **D** Was der Kanon nicht baut, darf er nicht als frisch bezeugen | Das Bein misst Quelltext, kein Artefakt; es kann nichts Veraltetes beglaubigen. Der clippy-Teil baut und ist deshalb **nicht** Teil des Beins — er meldet sich als „nicht messbar (Schalter --clippy)" statt still 0. |
| **E** Behauptung ≤ Messung | Die A32-Behauptung im Runner nennt genau die elf Maße, die das Werkzeug führt, die drei Ratschenwerte und dass clippy nicht mitläuft. Jede Zahl in §2 stammt aus einem Lauf dieser Session, keine aus einer anderen Datei abgeschrieben. Die Referenzmessung des Dirigenten ist in §2.1 **gegengeprüft**, nicht übernommen. |
| **E** Jede neue Prüfung wurde einmal absichtlich gebrochen | 12 Rotbeweise, §5.2. Jeder mit Verletzung **und** Rücknahme in derselben Datei; elf gegen Exit 4, der Messort-Beweis gegen Exit 2. **Nacharbeit 1** erweitert zwei davon um je einen Abschnitt (§5.6): die Mutation sitzt an der Zeile, die die Zusage trägt — `ELEMENT` bzw. die Exitcodeprüfung in `miss_clippy` —, ihr Text ist wörtlich der Stand vor der Nacharbeit (aus `git show HEAD` gegengeprüft), der Selbsttest wird 90/96 bzw. 91/96 rot und nach der Rücknahme wieder 96/96. **Nacharbeit 2** erweitert `roh/NAK-223-rot-deadcode.txt` um §4 (Mutation C, §5.8): dieselbe Zeile `ELEMENT`, aber chirurgisch — nur die Wiederholung **hinter** dem Schlüsselwort zurückgesetzt, die Qualifizierer davor unangetastet. Deshalb bleiben alle acht Formen der Vorrunde grün und es kippen genau die vier Zusagen dieser Runde: 112/116 rot, nach der Rücknahme 116/116, Quelle bytegleich. |
| **E** Geänderte Zusage — drei Stellen | Bau: neue Zusage, keine geänderte; die drei Stellen tragen sie widerspruchsfrei — Skriptkopf (`gesundheit.py` Docstring), Runnerkopf (A32-Behauptung), Manifestkopf (dieses §2). **Nacharbeit 1 ändert eine Zusage** (clippy: Prozessabbruch ist Werkzeugfehler statt „nicht messbar"); nachgezogen sind alle drei Stellen, an denen sie steht: Skriptkopf (Exitcode-Zeile **und** Maß (3)), `miss_clippy`-Docstring und §2.5 dieses Manifests. Die A32-Behauptung im Runner nennt clippy nur als „läuft NICHT mit" — das bleibt wahr; sie trägt aber die **Fallzahl 62**, und der Selbsttest führt jetzt 116 Fälle. Der Runner liegt außerhalb der Ticketgrenze beider Nacharbeitsrunden; die Zahl ist als Übergabe in §5.6 und §5.8 vermerkt. **Nacharbeit 2 ändert keine Zusage**, sondern präzisiert die aus Nacharbeit 1 (Regel R-D1b). Sie steht an zwei Stellen — im Kommentar über `ELEMENT` in `gesundheit.py` und in §2.5 dieses Manifests —, beide sind nachgezogen; Skriptkopf, Runnerkopf, Skill und Playbook tragen die Namenswahl nicht: `git grep -n ELEMENTschluesselwort -- tools/ .claude docs/` trifft ausschließlich `tools/plan/gesundheit.py` (Zeilen 560 und 1105). |
| **F** Änderungssatz — zusammengehörige Paare | Werkzeug ↔ Skill-Aufruf ↔ Runner-Bein ↔ Playbook-Satz in **einem** Änderungssatz. Das Gegenstück zur Messung ist die Reaktion darauf: sie steht im Skill §3.5 („ein gerissener Wert wird ein datierter Registerpunkt … nie stilles Nachbessern"). |
| **Determinismus** (Selbstaudit-Vorgabe) | Zwei Läufe hintereinander, `diff` bytegleich. Jede Trefferliste ist sortiert; die Dateisammlung läuft über `sorted()`. |
| **Pfade ohne Rechnerbezug** (Selbstaudit-Vorgabe) | Repo-Wurzel aus dem Skriptpfad (`parents[2]`, wie `planstand.py`); der Memory-Slug wird aus der Wurzel gerechnet, nicht eingetragen; fehlt der Ordner, ist das Maß „nicht messbar", nie rot. |
| **Keine irreführenden Ausgabetexte** (Selbstaudit-Vorgabe) | Die Tabelle trennt `Ziel` (Wunsch, kein Exit) von `Grenze` (reißt, Exit 4). Status `ZIEL` und `GRENZE` sind zwei Wörter; das Urteil unten nennt beides getrennt. Selbsttestfälle „Ziel verfehlt heisst ZIEL", „Grenze exakt getroffen reisst nicht". Ein per `--einspeisen` gesetzter Wert wird im **Kopf** angesagt („dieser Bericht ist KEINE vollständige Messung"), nicht nur an seiner Zeile — sonst spräche das Urteil von „allen Grenzen" über eine Messung, die es nicht gab (§5.1b Nr. 2, zwei Selbsttestfälle). |
| **Runner-Urteil unbewegt** (Selbstaudit-Vorgabe) | `NAK-223-rot-runnerurteil.txt`: die **echte** Urteilslogik aus `tools/beweise.ps1` (extrahiert zwischen ihren Kopfzeilen) mit 61 grünen Beinen plus A32, einmal `[OK]`/Exit 0 und einmal `[HINWEIS]`/Exit 4 — beide Male zeichengleich `GRUEN - 62/62 Kanon-Laeufe bestanden`, Exitcode 0. |

---

## 4. Nebenbefunde — gemessen, nicht behoben

Alle vier liegen außerhalb der Ticketgrenze („kein Produktcode"). Sie gehören
datiert ins Register, nicht in dieses Ticket.

**N-1 — 28 Funktionen über 200 Zeilen, ohne Pflegeticket.** Die längsten:
`server_v3/verbindung.rs`:147 `verbindung_bedienen` 744 Zeilen;
`SourcesModel.cpp`:538 `uebernehmeSessionSnapshot` 744;
`ControlClient.cpp`:1890 `ControlClient::Laufzeit::eineVerbindung` 580;
`server_v3/listener.rs`:265 `v3_server_starten_intern` 439;
`experiment_verdrahtung.rs`:860 `resultatmessung` 394;
`PluginProcessor.cpp`:581 `processBlock` 394. Vollständige Liste in der
Rohausgabe jedes Kanonlaufs. Die drei bestehenden Pflegetickets (NAK-224, -225,
-234) decken die *Dateien*, nicht diese Funktionen; drei der sechs längsten
liegen in Dateien **ohne** Ticket.

**N-2 — 35 Kommentar-Bezeichner ohne Entsprechung.** Darunter mindestens zwei
echte tote Verweise: `uebernehmeSnapshot` (`SourcesModel.cpp`:492 und :1636)
nennt eine Methode, die es im Repo nicht gibt; `kBlockFrist`
(`server_v3/mod.rs`:520) eine Konstante, die anders heißt. Rund ein Drittel der
35 sind Fremdnamen (§2.5) und bleiben dauerhaft in der Zahl.

**N-3 — `undocumented_unsafe_blocks` steht auf `deny`, wird aber von keinem Bein
gefahren.** `broker/Cargo.toml` erklärt die Regel in `[lints.clippy]` als
`deny`; der Kanon fährt `cargo test`, wo Clippy-Lints wirkungslos sind. Ergebnis:
30 undokumentierte `unsafe`-Blöcke (nach Dedup; 56 Rohdiagnosen) im Bestand, und
`cargo clippy` bricht deshalb ohne `--cap-lints warn` mit 26 Fehlern ab. Eine
Regel, die niemand misst, ist keine. Ab jetzt zählt A32 sie mit — allerdings als
Teil der Ratsche 91 und nicht als eigenes Maß.

**N-4 — sieben weitere Dateien über dem Ziel 1 500 Zeilen:** `broker/src/lib.rs`
1 998, `experiment_verdrahtung.rs` 1 892, `SourcesModel.cpp` 1 864,
`broker/src/server.rs` 1 841, `experiment.rs` 1 727, `PluginEditor.cpp` 1 665,
`PluginProcessor.h` 1 524. `lib.rs` steht 2 Zeilen unter der Grenze 2 000.

---

## 5. Verlauf

### 5.1 Bau — 09.09.2026

Gebaut in einem Zug: Werkzeug, Selbsttest, Rotbeweise, Runner-Bein, Skill,
Playbook. Drei Fehler fielen beim Bauen auf und sind behoben — jeder von einem
Selbsttestfall oder einem Rotbeweis gefunden, keiner durch Lesen:

1. **Zeichenliteral-Fenster gegen Rust-Lebensdauern** (§2.5). Gefunden vom
   Selbsttestfall „Lebensdauer bricht die Maskierung nicht".
2. **dead_code-Suche nahm die falsche Zeile aus** (§2.5). Gefunden vom
   Selbsttestfall „dead_code ohne Aufrufer ist ein Befund".
3. **Kommentar-Maß dreimal nachgeschärft**, 409 → 107 → 49 → 35 (§2.5). Gefunden
   durch Lesen der Trefferliste und eine Stichprobe von zwölf Treffern gegen
   `git grep`.

Dazu zwei Fehler im **Beweisaufbau**, die das Werkzeug korrekt gemeldet hat und
die die Isolationsanforderung an einen Rotbeweis zeigen: 28 Funktionen à 201
Zeilen ergeben eine Datei mit 5 656 Zeilen und rissen die Dateizeilen-Grenze
mit; 24 986 `x` ohne Umbruch sind *eine* Zeile mit 24 986 Zeichen und rissen das
250-Zeichen-Maß mit. Beide Rotbeweise verteilen jetzt auf mehrere Dateien
beziehungsweise Zeilen. Ein dritter: `pathlib.write_text` übersetzt `\n` auf
Windows zu `\r\n` und machte jede byte-genaue Verletzung um die Zeilenzahl zu
groß — behoben mit `newline=""`.

**Selbsttest:** 62 Fälle, 62 bestanden. Fälle entstehen im Speicher und laufen
durch dieselben Funktionen wie die Messung (Muster A27/A30); jede Erwartung
läuft mit ihrem Gegenteil.

### 5.1b Selbstaudit des eigenen Diffs — vier Nacharbeiten, 09.09.2026

Das adversariale Lesen des committeten Diffs (`c242e97a`) fand vier Punkte. Der
erste ist ein echter Befund gegen das Werkzeug; der laufende Kanon wurde dafür
in seiner Bauphase **abgebrochen** und nach dem Fix neu gestartet — ein Kanon
auf einem Stand, der gleich darauf geändert wird, beweist nichts.

1. **Fehlender Messort wurde still übersprungen** (`if not basis.is_dir():
   continue`). Behoben: `sammle_quellen` wirft jetzt, jeder fehlende Ort ist
   Exit 2. Zwei neue Selbsttestfälle und der zwölfte Rotbeweis.
2. **Die Urteilszeile log bei `--einspeisen`.** Ein gesetzter Wert trug zwar
   seine Marke in der Zeile, aber unten stand „alle Grenzen gehalten" über eine
   Messung, die es nicht gab. Behoben: eine Kopfzeile „dieser Bericht ist KEINE
   vollständige Messung" mit den Namen der gesetzten Maße. Zwei neue
   Selbsttestfälle (mit und ohne Einspeisung).
3. **`#[cfg_attr(…, allow(dead_code))]` wird nicht gesehen** — die Form beginnt
   nicht mit `#[allow`. Als Fehlerklasse im Skriptkopf und in §2.5 benannt, nicht
   behoben: sie kommt im Bestand nicht vor, und eine zweite Attributform wäre
   Heuristik ohne Anlass.
4. **Der Skill-Byte-Wert im Manifest** war der Stand am Basis-SHA, während der
   Endstand 35 654 zeigt. Als Satz in §2.1 klargestellt.

### 5.2 Rotbeweise — 12, alle bestanden

Jede Datei enthält beide Läufe roh: mit synthetischer Verletzung (Exit 4, mit der
richtigen Zeile) und nach der Rücknahme (Exit 0).

| Maß | Datei | Verletzung → Rücknahme |
|---|---|---|
| Quelldatei über 2 000 Zeilen | `roh/NAK-223-rot-dateizeilen.txt` | 2 001 → 2 000 |
| Funktionen über 200, Ratsche 28 | `roh/NAK-223-rot-funktionen.txt` | 29 → 28 |
| Clippy-Ratsche 91 | `roh/NAK-223-rot-clippy.txt` | 92 → 91 (Istwert eingespeist, siehe unten) |
| allow(dead_code) ohne Aufrufer | `roh/NAK-223-rot-deadcode.txt` | ohne Aufrufer → mit Aufrufer |
| Kommentar-Bezeichner, Ratsche 35 | `roh/NAK-223-rot-kommentare.txt` | 36 → 35 |
| MEMORY.md Bytes | `roh/NAK-223-rot-memory-bytes.txt` | 24 987 → 24 986 |
| MEMORY.md Zeile über 250 Zeichen | `roh/NAK-223-rot-indexzeile.txt` | 251 → 250 |
| Memory-Datei ohne Index-Link | `roh/NAK-223-rot-waise.txt` | unverlinkt → verlinkt |
| CLAUDE.md Bytes | `roh/NAK-223-rot-claudemd.txt` | 24 577 → 24 576 |
| Dirigenten-Skill Bytes | `roh/NAK-223-rot-skill.txt` | 36 865 → 36 864 |
| Fehlender Messort | `roh/NAK-223-rot-messort.txt` | Ort umbenannt → zurück; **0 → 2 → 0** |
| Runner-Urteil unbewegt | `roh/NAK-223-rot-runnerurteil.txt` | `[OK]` gegen `[HINWEIS]`, zeichengleich |

Der Messort-Beweis ist der einzige, der **nicht** auf Exit 4 zielt: ein
verschobener Messort ist ein Werkzeugfehler (Exit 2, im Runner `[ROT]`), kein
Codebase-Befund. Ein Beweis, der auch dort die 4 erwartete, hätte die
Unterscheidung verwischt, um die es geht.

Zehn der zwölf laufen gegen einen **synthetischen Minimalbaum** in `%TEMP%`
(`--wurzel`), damit kein anderes gerissenes Maß den Beweis verdeckt; im
Repo-Lauf reißt die Zeilengrenze ohnehin. Der Clippy-Beweis nutzt den
Prüfschalter `--einspeisen`, weil eine echte 92. Fundstelle nur durch eine
Änderung am Produktcode entstünde — die ist in diesem Ticket verboten. Jede so
entstandene Zeile trägt in Tabelle und JSON die Marke `[EINGESPEIST]` und ist
nie eine Messung; dass die Zahl selbst richtig **gemessen** wird, zeigt der Lauf
`--clippy` in §2.1 (91, deckungsgleich mit der Handmessung über `cargo clippy
--message-format=json` plus Dedup).

### 5.3 Bein A9, gefahren

`py -3.13 tools/eq-copilot/pruefe_flatc_drift.py` → **Exit 0**, „Drift: 0
Dateien". `broker/src/generiert/nakama_telemetry_v1_generated.rs` mit 2 446
Zeilen unverändert, `flatc` 25.12.19 gepinnt, Feld-IDs 0 rot. Der Beweis, dass
die Ausnahme von der Zeilenmessung nichts verdeckt, sondern eine Datei betrifft,
die ein anderes Bein bytegleich hält.

### 5.4 Abschlusskanon — GRÜN 62/62 auf `092db4de`

**`GRUEN - 62/62 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht
gebaut | 1 stillgelegte(s) Bein(e)`, Exitcode 0.** Lauf 09.09.2026 09:23–09:56,
abgekoppelt gestartet, Arbeitsbaum beim Lauf **sauber**, Rohausgabe
`docs/beweise/roh/NAK-223-092db4d.md`. Über alle Cargo-Beine 806 passed, 0
failed. Die beiden Nebenmeldungen stehen wörtlich so in jedem grünen Kanon seit
NAK-213.

**Die Beinzahl steigt von 61 auf 62** — das neue A32 ist der Zuwachs. Es steht
in der Übersicht als:

> `| A32 | … | py -3.13 tools\plan\gesundheit.py --mit-selbsttest | [HINWEIS]
> Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 4,64 s | [↓ A32] |`

Damit ist die Zusage aus §2.6 **am echten Lauf** gemessen und nicht nur an der
isolierten Urteilsprobe: das Bein endet mit Exit 4, die Urteilszeile lautet
`GRUEN - 62/62` und der Runner gibt Exitcode 0. Der Befund selbst ist zweifach
sichtbar — als `[HINWEIS]` in der Übersichtstabelle und als vollständige
Tabelle samt Trefferliste im Rohausgabe-Abschnitt `#### A32` (dort endet der
Bericht mit `URTEIL: GRENZE GERISSEN - 1 Maass(e): Quelldateien ueber 2000
Zeilen`, den fünf Dateien aus §2.2 und ihren Pflegetickets).

**Vorlauf:** ein erster Kanonlauf wurde in seiner Bauphase abgebrochen, weil das
Selbstaudit des Diffs den Messort-Befund fand (§5.1b Nr. 1) — ein Kanon auf
einem Stand, der gleich darauf geändert wird, beweist nichts. Der hier
berichtete Lauf ist der einzige, der zu Ende lief.

**Bein A9 im Kanon:** grün, wie im gezielten Lauf aus §5.3 — die Ausnahme der
generierten Datei von der Zeilenmessung bleibt gedeckt.

### 5.5 Erstprüfung — NEEDS_WORK (09.09.2026, Stand `d6a0bfda`)

Prüfer Codex `gpt-6-astra`, Effort **max**, read-only, Thread
`01a08530-2bfd-7162-a0c8-d3ff058cc551`. Auftrag (Vorlage A, Ticketbereich
`274d3ff8`…`d6a0bfda`) in `roh/NAK-223-erstpruefung-auftrag.txt`, Urteil roh in
`roh/NAK-223-erstpruefung-a1722a2.txt`. Urteil wörtlich:

> URTEIL: NEEDS_WORK — zwei reproduzierte DEFEKTE.
>
> Geprüft: ZIEL-Diff und Schwellen, zwölf Rotbelege lesend, virtuelle
> Grenzproben, 58/58 reine Selbsttestfälle unter Python 3.14, isolierte
> Runner-Auswertung (0/4 wortgleich; Fehlercodes 1/2/5/42/124 → ROT), Skill
> −31 Bytes, leerer Produktdiff sowie zwei bytegleiche Messläufe in 4,77/4,24
> Sekunden; HEAD und Arbeitsbaum blieben unverändert.
>
> Nicht geprüft: echte Clippy-, Kanon- und Produktläufe; der vollständige
> Selbsttest war umgebungsbedingt nicht ausführbar (Python 3.13 startet nicht,
> Python 3.14 scheitert am gesperrten Temp-Dateisystem).

Die beiden Defekte, je mit ihrer Kopfzeile aus dem Urteil (voller Wortlaut
samt Reproduktionsbefehl im Rohbeleg):

1. **[P2] Erfasse bei const fn den tatsächlichen Helfernamen** —
   `tools/plan/gesundheit.py:532-533`. „Bei einem unbenutzten `pub const fn
   helfer()` erfasst `ELEMENT` das Schlüsselwort `fn` als Namen. Jede andere
   Funktionsdefinition gilt anschließend als Verwendung; der aufruferlose
   Helfer bleibt ungemeldet." Die lesende Reproduktion liefert `[]` statt eines
   Treffers, der virtuelle Gesamtmesslauf 0/OK und Exit 0 statt 4.
2. **[P2] Verwirf Clippy-Ergebnisse eines fehlgeschlagenen Prozesses** —
   `tools/plan/gesundheit.py:522-526`. „Bricht Clippy beispielsweise mit
   Compilerfehler E0308 und Exit 101 ab, steht die Fehlerdiagnose bereits in
   `fund`; dadurch überspringt `not fund` die Fehlerbehandlung. Die
   unvollständige Messung erscheint als gültige Warnungszahl." Die Reproduktion
   ohne Cargo-Ausführung liefert `(1, ['?:0:0 E0308'], '')`.

Beide sind an der Quelle bestätigt worden, bevor gebaut wurde, und beide sind
dieselbe Fehlerklasse: **eine Messung, die im Fehlerfall eine Zahl liefert statt
eines Fehlers.** Bei Defekt 1 war der falsche Name kein leeres Ergebnis, sondern
das Wort `fn`, das in jeder Rust-Datei vorkommt — das Maß konnte strukturell nie
melden. Bei Defekt 2 machte `not fund` die Fehlerbehandlung genau dann
unerreichbar, wenn der Fehler selbst eine Diagnose geschrieben hatte.

### 5.6 Nacharbeit 1 — 09.09.2026

**Defekt 1.** Die Namenswahl überspringt jetzt Qualifizierer. `pub`,
`pub(crate)`, `pub(in …)`, `unsafe`, `async`, `extern "…"` und `default` stehen
vor dem Schlüsselwort und sind selbst keins — die Suche geht ohnehin über sie
hinweg; `const fn name`, `const extern "C" fn name` und `static mut NAME` tragen
ein weiteres Schlüsselwort **zwischen** Schlüsselwort und Bezeichner, und genau
das erfasst der neue Ausdruck. `const NAME: T` bleibt unterschieden, weil dort
kein zweites Schlüsselwort folgt; der Rückfall des Ausdrucks (Backtracking)
hält auch einen Bezeichner wie `fnord` oder eine Funktion namens `default`
korrekt. Belegt mit acht Quellformen im Selbsttest, jede einmal ohne und einmal
mit Verwendung, dazu der gemeldete Name.

**Defekt 2.** Der Exitcode wird vor dem Lesen der Diagnosen und ohne jede
Bedingung geprüft; `miss_clippy` wirft dann `RuntimeError` statt eine Zahl zu
liefern, und der Lauf endet über denselben Weg wie ein fehlender Messort mit
**Exit 2** und `Werkzeugfehler: cargo clippy Exit N - <letzte stderr-Zeile>`.
Rückgabewert und Ausnahme trennen seither zwei Bedeutungen: die Rückgabe trägt
nur noch den Umgebungsgrund „cargo nicht gefunden" (NICHT MESSBAR), jeder
Werkzeugfehler wirft — ein Aufrufer kann eine Ausnahme nicht versehentlich
übergehen, und genau das war die Falle. Ein fehlendes `broker/Cargo.toml` zählt
dabei zum Werkzeugfehler, weil „nicht messbar" nach dieser Regel allein dem Lauf
ohne `--clippy` und der Umgebung ohne cargo bleibt.

**Die eine Annahme, die gemessen werden musste:** eine strikte
Exitcode-Prüfung wäre ein Selbstschuss, wenn der echte Clippy-Lauf ≠ 0 liefert.
Er liefert 0 — `--cap-lints warn` stuft auch das in `broker/Cargo.toml`
gesetzte `deny`-Lint auf Warnung herab. Gemessen im Lauf unten.

**Beine dieser Runde** (alle 09.09.2026, Worktree beim Messen sauber):

| Bein | Ergebnis |
|---|---|
| `py -3.13 tools/plan/gesundheit.py --selbsttest` | **96/96 bestanden**, Exit 0. Vorher 62 — der Zuwachs sind 24 dead_code-Fälle (acht Quellformen × ohne Verwendung / Name / mit Verwendung) und 10 Clippy-Fälle (Abbruch mit und ohne Diagnosen, sauberer Lauf mit 3 und mit 0 Warnungen, Fehlertext, Gesamtlauf erbt den Fehler, Lauf ohne Schalter, fehlender Messort, fehlendes cargo). |
| `py -3.13 tools/plan/gesundheit.py` (ohne clippy) | Exit 4 wegen der fünf bekannten Dateien über der Zeilengrenze. **Zwei Läufe bytegleich** (SHA256 `34DBC969…0FB323`). Tabelle deckungsgleich mit §2.1: 5 / 7 / 28 / — / 0 / 35 / 3 251 / 22 858 / 35 654 / 0 / 0. Kein Wert hat sich durch diese Runde bewegt. |
| `py -3.13 tools/plan/gesundheit.py --clippy` | **91 Fundstellen**, Status ZIEL — die Ratsche 91 hält exakt. Laufzeit **5,02 s**. Der Prozess selbst endet mit 0; hätte er das nicht, wäre der Lauf jetzt Exit 2 statt einer Zahl. |
| `py -3.13 tools/plan/gesundheit.py --mit-selbsttest` (Form des Runner-Beins A32, aus dem Repo-Root) | Selbsttest 96/96 vorangestellt, danach die Messung; Exit **4**, Laufzeit 4,84 s. Unverändert der erwartete Zustand des nicht blockierenden Beins. |

**Mutationsbeweise**, je an der Zeile, die die Zusage trägt — der Text der
Mutation ist wörtlich der Stand vor der Nacharbeit, gegen `git show HEAD`
geprüft, und die Quelle wird bytegleich zurückgesetzt:

- `roh/NAK-223-rot-deadcode.txt` §3: `ELEMENT` zurückgenommen → **90/96**, sechs
  ROT-Zeilen an `const fn`, `const extern "C" fn` und `static mut`. Die vier
  Formen, die die alte Namenswahl zufällig richtig traf, bleiben grün — die
  Mutation ist auf den Defekt gerichtet, nicht auf das Maß. Rücknahme → 96/96.
- `roh/NAK-223-rot-clippy.txt` §3: Bedingung zurück auf `not fund and
  lauf.returncode != 0` hinter den Diagnosen → **91/96**, fünf ROT-Zeilen,
  darunter „Gesamtlauf erbt den Werkzeugfehler". Die beiden Fälle des sauberen
  Laufs bleiben grün. Rücknahme → 96/96.

**Die Reproduktionen des Prüfers** stehen in denselben beiden Belegen auf dem
neuen Stand: Defekt 1 liefert `[('t.rs', 1, 'helfer')]` statt `[]`, Defekt 2
keine Zahl mehr, sondern `RuntimeError: cargo clippy Exit 101 - ohne Ausgabe auf
stderr`.

**Geänderte Zusage, Nachweis per `git grep` (Prüfliste E, 30.08.2026):**
`git grep -n "cargo clippy" -- tools/ docs/context-hygiene-playbook.md .claude`
(ohne `docs/beweise/roh/`) trifft **ausschließlich `tools/plan/gesundheit.py`**:
Zeile 9 (Exitcode-Liste), Zeile 96 (Maß (3)), die Fehlerzeile in `miss_clippy`
und die Selbsttestfälle. Runnerkopf, Dirigenten-Skill und Playbook tragen die
Zusage über die Clippy-Fehlerbehandlung nicht — die A32-Behauptung sagt zu
clippy nur „laeuft NICHT mit … und meldet sich als nicht messbar", und das
bleibt für das Bein wahr, weil es ohne `--clippy` läuft. Nachzuziehen war
deshalb nichts außerhalb von Skript und Manifestkopf.

**Ticketgrenze gemessen:** `git diff --stat` zeigt genau vier Dateien —
`tools/plan/gesundheit.py`, die beiden Rotbelege und dieses Manifest. Kein
Produktcode, kein Runner, kein Skill, kein Playbook, kein Register, kein Plan.

**Übergabe an den Dirigenten (außerhalb dieser Ticketgrenze):** die
A32-Behauptung in `tools/beweise.ps1` nennt „Vorangestellt laeuft der
Selbsttest: **62 Faelle**". Es sind jetzt 96. Die Aussage unterbietet die
Messung, ist also keine Überbehauptung, aber die Zahl ist veraltet und gehört in
denselben Schritt, in dem der Kanon neu läuft.

### 5.7 Wiederprüfung 1 — NEEDS_WORK (09.09.2026, Stand `afc2d699`)

Prüfer Codex `gpt-6-astra`, Effort **max**, read-only, Thread
`01a08551-373e-7792-bd0e-9983ad9e9ebc`. Auftrag (Vorlage B, Fixdiff
`1c93d0fc`…`afc2d699`) in `roh/NAK-223-wiederpruefung-1-auftrag.txt`, Urteil roh
in `roh/NAK-223-wiederpruefung-1-9cef44b.txt`. Urteil wörtlich:

> URTEIL: NEEDS_WORK — beide ursprünglichen Fehlerbilder sind behoben, aber der
> Fixdiff erzeugt die beschriebene Regex-Regression.
>
> Geprüft: Befund 1 geschlossen: ja, Originalreproduktion unter Python 3.14
> liefert genau [('t.rs', 1, 'helfer')] (gesundheit.py:574–575); Selbsttestfälle
> für pub const fn, pub(crate) unsafe fn, pub async fn, pub const HELFER und
> unsafe fn mit den Namen „ohne Verwendung ist ein Befund", „meldet den Namen"
> und „mit Verwendung ist kein Befund" stehen in Zeilen 1104–1120, Rotbeweis
> NAK-223-rot-deadcode.txt §3/Mutation A; Befund 2 geschlossen: ja,
> Originalreproduktion liefert RuntimeError ohne Zahl, der Hauptlauf mit
> Prozessersatz Exit 2 (gesundheit.py:522–525, 1341–1343), Fälle „Abbruch
> MIT/OHNE Diagnosen ist ein Werkzeugfehler", „sauberer Lauf mit 3 Warnungen
> zaehlt 3" und „sauberer Lauf ohne Diagnosen zaehlt 0" in Zeilen 1156–1172,
> Rotbeweis NAK-223-rot-clippy.txt §3/Mutation B; virtueller vollständiger
> Selbsttest mit Speicheradapter vorher 62/62, Ziel 96/96; übrige Messwerte und
> Tabellen alt/neu identisch, zwei reale Messläufe ohne Clippy bytegleich mit
> Exit 4, Exitpfad 0 zusätzlich über markierte Einspeisung bestätigt, Schwellen
> 28/91/35 und 2000/1500/200 einschließlich Kontextgrenzen unverändert,
> A32-Verdrahtung --mit-selbsttest und Exit 4 → HINWEIS unverändert
> (tools/beweise.ps1:732, 1136–1139), HEAD und Worktree-Status vor/nach
> identisch, nichts geschrieben.
>
> Nicht geprüft: native Python-3.13-Läufe einschließlich Selbsttest/A32 wegen
> Launcher-Exit 101 („Anmeldesitzung nicht vorhanden"); echter Clippy-Lauf,
> Cargo, CMake und Kanon auftragsgemäß nicht ausgeführt.

Der eine Restbefund, wörtlich (voller Wortlaut samt Reproduktionsbefehl im
Rohbeleg):

> [P2] Überspringe gültige Elementnamen nicht als Qualifizierer —
> `tools/plan/gesundheit.py:572-575`. „DEFEKT: Beim gültigen Rust-Typnamen
> `union` überspringt die Wiederholung den tatsächlichen Namen und erfasst
> stattdessen `where`. … Bei sonst gehaltenen Grenzen kippt der virtuelle
> Gesamtlauf dadurch von Exit 4 auf 0. Das verletzt den Namensvertrag aus R-D1
> und das Gate-Maß für aufruferlose Helfer; übersprungen werden dürfen nur
> tatsächliche Qualifizierer."

An der Quelle bestätigt, bevor gebaut wurde: `finde_dead_code` liefert für
`struct union where u8: Copy;` neben einer fremden `where`-Klausel `[]`. Es ist
**dieselbe Fehlerklasse wie die beiden Erstbefunde** — eine Messung, die im
Fehlerfall eine Zahl liefert statt eines Fehlers —, nur an der anderen Kante
derselben Zeile: Defekt 1 las **zu früh** (das Schlüsselwort als Namen), die
Regression las **zu spät** (das Wort hinter dem Namen). Gegengemessen:
`fn default() -> u8` lieferte korrekt `default`, `union Foo` korrekt `Foo` — die
Regression traf genau die Lage „ein Wort, das zugleich Schlüsselwort und
gültiger Bezeichner ist, steht an Namensposition und wird von einem weiteren
Wort gefolgt".

### 5.8 Nacharbeit 2 — 09.09.2026

**Regel R-D1b** (Präzisierung von R-D1, Technikentscheid des Dirigenten):
Qualifizierer werden ausschließlich **vor** dem Elementschlüsselwort
übersprungen, und nur dann, wenn auf sie ein Elementschlüsselwort folgt. Die
Grammatik der Namenswahl lautet `[Qualifizierer]* Schlüsselwort [mut]? Name` mit
Qualifizierer aus `pub`, `pub(…)`, `const`, `unsafe`, `async`, `default` und
`extern "…"`, Schlüsselwort aus `fn`, `struct`, `enum`, `trait`, `type`,
`const`, `static`, `union`, `mod`, und `mut` nur unmittelbar nach `static`. Das
erste Wort **nach** dem Schlüsselwort ist der Name, auch wenn es `union`,
`default`, `where` oder ein anderes Wort ist, das anderswo Schlüsselwort wäre.

Umgesetzt ist das als Umkehr der Wiederholung: die Konstante `ZWISCHENWORT`
(hinter dem Schlüsselwort) entfällt und wird durch `QUALIFIZIERER` (davor)
ersetzt; `static mut` ist die einzige Zweiwortform und steht als eigene
Alternative **vor** der Schlüsselwortliste, damit sie zuerst greift. `const`
steht in beiden Rollen — als Qualifizierer und als Schlüsselwort —, und die
Unterscheidung `const fn name` gegen `const NAME: T` erledigt das Backtracking
des Ausdrucks selbst: die gierige Qualifiziererschleife schluckt erst `const`,
findet danach kein Schlüsselwort und fällt auf null Wiederholungen zurück. Keine
Sonderfallbehandlung im Code.

**Beine dieser Runde** (alle 09.09.2026, Worktree beim Messen sauber):

| Bein | Ergebnis |
|---|---|
| `py -3.13 tools/plan/gesundheit.py --selbsttest` | **116/116 bestanden**, Exit 0. Vorher 96 — der Zuwachs sind 18 Fälle aus sechs weiteren Quellformen (`struct union where`, `union Foo`, `fn default()`, `default fn helfer`, `const fn name`, `const NAME: u8`; je ohne Verwendung, Name, mit Verwendung) und 2 Fälle, die die Reproduktion des Prüfers wörtlich führen: Befund und Name bei einer fremden `where`-Klausel in derselben Crate-Wortmenge. |
| `py -3.13 tools/plan/gesundheit.py` (ohne clippy) | Exit 4 wegen der fünf bekannten Dateien über der Zeilengrenze. **Zwei Läufe bytegleich**, und der Hash ist **derselbe wie in Nacharbeit 1** (SHA256 `34DBC969…0FB323`, 6 289 Bytes): 5 / 7 / 28 / — / 0 / 35 / 3 251 / 22 858 / 35 654 / 0 / 0. Gegengeprüft nicht nur an der Zahl, sondern am Stand: `git show HEAD:tools/plan/gesundheit.py` gegen dieselbe Wurzel und denselben Memory-Ordner gefahren, Ausgabe **bytegleich** zum neuen Stand. `allow(dead_code) ohne Aufrufer` bleibt 0. |
| `py -3.13 tools/plan/gesundheit.py --mit-selbsttest` (Form des Runner-Beins A32) | Selbsttest 116/116 vorangestellt, danach die Messung; Exit **4**, Laufzeit 4,6 s. Unverändert der erwartete Zustand des nicht blockierenden Beins. |

Ein `--clippy`-Lauf war nicht nötig und ist nicht gefahren: der Clippy-Pfad ist
in dieser Runde unberührt, `git diff` zeigt dort keine Zeile.

**Mutationsbeweis**, an derselben Zeile wie in Nacharbeit 1 —
`roh/NAK-223-rot-deadcode.txt` §4, Mutation C: die alte Wiederholung wieder
**hinter** dem Schlüsselwort eingesetzt, die Qualifizierer davor unangetastet.
Ergebnis **112/116**, vier ROT-Zeilen, alle vier an `struct union where`; alle
acht Formen der Nacharbeit 1 bleiben grün. Das ist der Punkt dieser Mutation:
sie ist auf die **neue** Zusage gerichtet und kann nicht durch einen
Nebeneffekt der alten grün oder rot werden. Rücknahme → 116/116, Quelle
bytegleich (SHA256 gegen eine Kopie geprüft).

**Die Reproduktion des Prüfers** steht in demselben Beleg auf dem neuen Stand:
`finde_dead_code` liefert für `struct union where u8: Copy;` neben
`fn x<T>() where T: Copy {}` genau `[('t.rs', 1, 'union')]` statt `[]`.

**Selbstaudit des Diffs** (Prüfliste E, adversarial gelesen):

- Die neue Regex an 21 Formen von Hand gemessen, darunter alle acht der
  Nacharbeit 1, alle sechs neuen, `pub(in crate::a) fn helfer`,
  `pub async unsafe fn`, `const unsafe extern "C" fn helfer`, `static ZAEHLER`
  ohne `mut`, `pub mod`, `type` und `enum`. Alle 21 liefern den erwarteten
  Namen.
- **Kein falscher Treffer bei `impl`:** `impl` ist bewusst kein Schlüsselwort
  dieses Maßes. `unsafe impl Trait for X`, `impl Trait for X` und
  `impl<T> Helfer for T` ergeben deshalb **gar kein** Match — nicht etwa den
  falschen Namen `Trait`. Direkt an `ELEMENT` gemessen.
- Determinismus: zwei Messläufe bytegleich, Hash identisch mit dem der
  Vorrunde.
- Unberührt: Schwellen (2000/1500/200/28/91/35 und die Kontextgrenzen),
  Tabellenform, Exit-Semantik (0/2/3/4), Clippy-Pfad, Messortwache und
  Einspeisungsmarke.

**Ticketgrenze gemessen:** `git diff --stat` zeigt genau drei Dateien —
`tools/plan/gesundheit.py` (51 eingefügte, 20 entfernte Zeilen, alle in der
Namenswahl und ihren Selbsttestfällen), `roh/NAK-223-rot-deadcode.txt` (ein
angehängter Abschnitt, nichts Altes umgeschrieben) und dieses Manifest. Kein
Produktcode, kein Runner, kein Skill, kein Playbook, kein Register, kein Plan.

**Übergabe an den Dirigenten (außerhalb dieser Ticketgrenze):** die
A32-Behauptung in `tools/beweise.ps1` nennt „Vorangestellt laeuft der
Selbsttest: **62 Faelle**". Es sind jetzt **116**. Die Aussage unterbietet die
Messung, ist also keine Überbehauptung, aber die Zahl ist veraltet und gehört in
denselben Schritt, in dem der Kanon neu läuft.

---

## Kanon-Lauf - NAK-223

**Lauf:** 2026-09-09 09:23 | **Runner:** `tools/beweise.ps1` | **Urteil:** GRUEN - 62/62 Kanon-Laeufe bestanden | 2 geplante Pruefung(en) noch nicht gebaut | 1 stillgelegte(s) Bein(e), siehe Uebersicht | **Exitcode:** 0 | **Rohausgabe:** [roh/NAK-223-092db4d.md](roh/NAK-223-092db4d.md)

### Kopf - woran gemessen wurde

| Feld | Wert |
|---|---|
| Zeitpunkt | 2026-09-09 09:23:46 +02:00 |
| Rechner | SCHUBBINATOR200 \| Windows 10.0.26200.0 |
| Zweig | master |
| Commit | 092db4de NAK-223 Selbstaudit: fehlender Messort ist ein Werkzeugfehler, kein stilles Null |
| Commit (voll) | 092db4de0628448472616735917155a568189cad |
| Arbeitsbaum | sauber |
| JUCE gepinnt | 8.0.9 |
| JUCE auf Platte | 8.0.9-dirty |
| FL Studio | FL Studio 2025 25.2.5.5319 \| FL Studio 2026 26.1.4.5589 |
| cargo | cargo 1.93.1 (083ac5135 2025-12-15) |
| rustc | rustc 1.93.1 (01f6ddf75 2026-02-11) |
| PowerShell | 7.6.5 |
| cmake | cmake version 3.31.6-msvc6 |

### Uebersicht

| # | Behauptung | Befehl | Ergebnis | Dauer | Rohausgabe |
|---|---|---|---|---|---|
| A1 | Passthrough ist bitgleich; 0 Samples Latenz, 0 Tail; NaN/Inf werden gezaehlt, aber nicht veraendert. | `eq-copilot\build\plugin\EqCopNullTest_artefacts\Release\EqCopNullTest.exe` | [OK] Exit 0 | 0,17 s | [A1](roh/NAK-223-092db4d.md#a1) |
| A2 | AnalyseEngine deckt sich mit der eingefrorenen Offline-Referenz (Fixture-SHA-256 als Determinismus-Riegel). | `eq-copilot\build\plugin\EqCopGoldenTest_artefacts\Release\EqCopGoldenTest.exe eq-copilot\fixtures` | [OK] Exit 0 | 9,61 s | [A2](roh/NAK-223-092db4d.md#a2) |
| A3 | Hoer-Markierung bleibt verriegelt: Render/Freilauf bitgleich, Analyse-Abgriff sitzt vor der Faerbung, und der U10-Term ist gedeckt - OHNE Playhead faerbt bei sonst voller Erlaubnis kein Sample (T11, mit Gegenprobe bei laufendem Transport). | `eq-copilot\build\plugin\EqCopMarkierungTest_artefacts\Release\EqCopMarkierungTest.exe` | [OK] Exit 0 | 7,87 s | [A3](roh/NAK-223-092db4d.md#a3) |
| A4 | Broker-Vertragstests: eingefrorene v2/v3-Vertraege sowie SONDE-011 Phase B mit Coordinator/Session, monotoner Liveness/Eviction, SQLite-Migration 1, Single-Writer, Projektionen, Snapshot-Outbox, dauerhaften Konfliktriegeln, produktiver v2+v3-Verdrahtung und der nicht isolationspflichtigen Killmatrix. Seit SONDE-013 zusaetzlich die Fassungsleiter (jede Minorfassung wird aus der committeten zurueckgebaut, der Leser der Fassung 1 lehnt jede Neuerung der Fassung 2 ab, Fassung 0 erbt den Rueckbau) und der Empfaenger des evidence_snapshot: fremde Adresse verworfen, offene Intervention sperrt statt abzuschwaechen, nach Ende und Nachlauf nimmt er wieder an. Seit SONDE-014 zusaetzlich der Intent- und Assistentenspiegel (Koaleszierung je Quelle/Scope, keine Rechnung vor der Vollstaendigkeitsmarke) und der URSACHENPFAD: aus paralleler Telemetrie entsteht nie Aussageklasse 2 oder 3, das Screening reicht hoechstens fuenf Kandidaten weiter, fehlende Coverage und falsches Alignment sind ein GATE vor der Gewichtung und tragen einen Grund aus der geschlossenen Achtermenge, eine Passage unter GATE_MINDEST_FENSTER traegt keine starke Aussage (mit Gegenprobe an der Kante 7/8), eine Ruecknahme invalidiert die abhaengigen Hypothesen deterministisch, und hundert Laeufe ueber dieselben Bytes liefern bytegleich dieselbe Rangfolge. Seit Etappe F dazu die Proposal-Policy: ein Vorschlag entsteht MIT seinem Befund und traegt die fuenfzehn Felder aus 42.1 plus revert, der Rueckweg ist ein FELD mit drei Werten (dsp_revert faellt), in P5 ist jede Aktion manual, keine Aenderung und mehr Daten sind vollstaendige Objekte, der Zielbereich kommt aus dem Band des Befunds statt aus dem groessten Banddelta, ein geschuetztes Band ist eine HARTE Constraint mit Gegenprobe, ein stop_if auf einem nicht messbaren Guardrail meldet MORE DATA, und ueber fuenfhundert zufaellige Eingaenge wird kein Hard Cap und keine engere Usergrenze ueberschritten. Der Guardrail-Rechner LIEST seit E-05 den Zielbereich aus experiment_begin.ziel; ohne ihn bleibt die Heuristik und das Resultat traegt ziel_geraten - beide Pfade mit verschiedener Zahl gemessen. | `cargo test --manifest-path broker/Cargo.toml --color never` | [OK] Exit 0 | 1.225,54 s | [A4](roh/NAK-223-092db4d.md#a4) |
| A4-SI | SONDE-011 Phase-B-Systemintegration auf Probe-Pipenamen: echter C++-ControlClient wiederholt persistenzpflichtige Befehle ueber Brokerkills mit derselben command_id; Store/Coordinator liefern nur absolute session_snapshot-Pushes, koaleszieren Snapshot-Schuld, halten Locks aus externer Arbeit heraus und bereinigen Eviction/Nonce vor spaeterem Push. | `cargo test --manifest-path broker/Cargo.toml --color never --test store_crash_matrix -- --ignored --test-threads=1` | [OK] Exit 0 | 14,07 s | [A4-SI](roh/NAK-223-092db4d.md#a4-si) |
| A4b | C++-PipeClient: stop/reconnect und ganze Frames sind zeitlich begrenzt, SQOS ist Identification, Peerbytes sind UTF-8/NUL-sauber, ACK-Sequenzen streng und u64-Zaehler wire-sicher. | `eq-copilot\build\plugin\EqCopPipeClientTest_artefacts\Release\EqCopPipeClientTest.exe` | [OK] Exit 0 | 66,22 s | [A4b](roh/NAK-223-092db4d.md#a4b) |
| A5 | Referenzbein (jsonschema, draft 2020-12): Schema haelt die Engine-Teilmenge ein, Textriegel deckt jede gemessene Kante, jedes Fixture wird wie im Manifest klassifiziert, jede Definition hat ein Negativfixture. Seit SONDE-013 zusaetzlich der Fassungsschritt: der aus dem Register zurueckgebaute Leser der Fassung 1 LEHNT jede Neuerung der Fassung 2 ab (drei Experimentfamilien, evidence_snapshot.ereignisse/.stereo, zwei neue Invalidierungsgruende), und die Summe der registrierten Familien stimmt mit dem oneOf ueberein. | `py -3.13 tools\eq-copilot\pruefe_v3_vertrag.py --abdeckung` | [OK] Exit 0 | 4,04 s | [A5](roh/NAK-223-092db4d.md#a5) |
| A6 | Beide Bandgitter sind bytegleich zur Neuerzeugung; 221 Baender, 64 Gruppen als exakte Partition. | `py -3.13 tools\eq-copilot\erzeuge_bandgitter.py --pruefen` | [OK] Exit 0 | 0,14 s | [A6](roh/NAK-223-092db4d.md#a6) |
| A7 | Quantisierungsvertrag bytegleich zur Neuerzeugung; Rundung, Saettigung und Nichtendliches als Testvektoren. | `py -3.13 tools\eq-copilot\erzeuge_quantisierung.py --pruefen` | [OK] Exit 0 | 0,26 s | [A7](roh/NAK-223-092db4d.md#a7) |
| A8 | Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei. | `py -3.13 tools\eq-copilot\erzeuge_v3_fixtures.py --pruefen` | [OK] Exit 0 | 0,42 s | [A8](roh/NAK-223-092db4d.md#a8) |
| A9 | Codegen-Drift ist 0: die Neugenerierung aus dem .fbs ist bytegleich zum committeten C++- und Rust-Code; flatc, C++-Header und Rust-Crate tragen dieselbe gepinnte Version; jedes Tabellenfeld traegt eine explizite Feld-ID. | `py -3.13 tools\eq-copilot\pruefe_flatc_drift.py` | [OK] Exit 0 | 0,26 s | [A9](roh/NAK-223-092db4d.md#a9) |
| A10 | Binaerer Fixture-Korpus und sein MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; sechs NAK-29-Transportrelationen, band_stereo ID 10 samt saturated-/Grenzfaellen und integration_samples ID 14 (voller Rahmen, duenner Rahmen, gesendete 0 als Senderfehler) sind in beiden Lesern gedeckt. | `py -3.13 tools\eq-copilot\erzeuge_fb_fixtures.py --pruefen` | [OK] Exit 0 | 2,63 s | [A10](roh/NAK-223-092db4d.md#a10) |
| A11 | Die fuenf v2-Vertraege (ipc v2, measurement v1, report v1, snapshot v3, aggregat v1) sind gueltiges JSON und gueltige JSON-Schemas; ihre $id-Familie ist eingefroren. | `py -3.13 tools\eq-copilot\pruefe_v2_schemas.py` | [OK] Exit 0 | 0,24 s | [A11](roh/NAK-223-092db4d.md#a11) |
| A12 | Parameterbestand (109 IDs, §53.8) haelt den Vertrag; RFC-8785-Zahlenvektoren tragen den RFC-Text und werden von rfc8785 bestaetigt; State-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung. | `py -3.13 tools\eq-copilot\erzeuge_state_fixtures.py --pruefen` | [OK] Exit 0 | 0,29 s | [A12](roh/NAK-223-092db4d.md#a12) |
| A13 | Capabilityreport FL: die zehn Bits aus §53.6 entsprechen der v3-Vertragsform und stehen so, wie die Rohdaten der Termine A und B sie tragen; jedes supported hat einen Termin, jedes unsupported seinen festen Fallback. | `py -3.13 tools\eq-copilot\pruefe_host_capabilities.py` | [OK] Exit 0 | 0,21 s | [A13](roh/NAK-223-092db4d.md#a13) |
| A14 | Der gemeinsame Kern traegt keine Bundle-Identitaet (Entwurf §53.4): NakamaKern.lib wird vor der Messung geloescht und von MSBuild neu erzeugt (jede Kernquelle uebersetzt, Objekte und Lib juenger als der Zeitanker, Objektzahl = Quellzahl), enthaelt danach keinen eingefrorenen Identitaetswert aus plugin-identities-v1.json (Text in ASCII/UTF-16LE, Viercodes als Integer beider Byteordnungen, CIDs roh und COM-vertauscht) und genau die erwarteten Kernobjekte ohne JUCE-Modulobjekt; die Gegenprobe findet die Pflichtnadeln im gebauten Gen-Bundle, sonst waere das Schweigen ueber den Kern wertlos. Fehlende Voraussetzung (kein Neubau, Artefakt nicht auffindbar, --nur-messen) endet mit Exit 3, mit registriertem Befund mit 2, nie mit 0. Seit NAK-100 (30.08.2026) ohne K1b-Textscan, Ortsriegel, JUCE-Baum-Riegel und Frischewachen (Haertung NAK-103). | `py -3.13 tools\eq-copilot\pruefe_kern_identitaetsfrei.py` | [OK] Exit 0 | 6,30 s | [A14](roh/NAK-223-092db4d.md#a14) |
| A17 | Installer-Manifest: keine zweite Identitaet (kein Viercode, keine Class-ID, kein Produkt- oder Bundlename ausser im Pfad), jeder Quellpfad ist der aus Ziel + Identitaetsdatei nachgerechnete Bundle-Ordner, `art` ist eine geschlossene Menge, jedes aktive Ziel genau einmal und jedes stillgelegte benannt mit lesbarer Marke, Broker aus dem Crate-Namen, Zielverzeichnisse geschuetzt, Signaturzeile ehrlich, Rueckweg samt NAK-41-Riegel und hash_art vollstaendig; jede der 15 Regeln faellt an einem verdorbenen Manifest, die konkreten Umgehungen fallen einzeln. [4] vergleicht Artefakthashes im Kanon weich und mit --release hart; [4b] berichtet den installierten Stand; [5] kreuzprueft den Ordner-Hash; [6] faehrt die produktive C++-WinVerifyTrust-/Thumbprint-Kette mit dem OS-vertrauenswuerdigen Signerzertifikat einer signierten Windows-Fixture fuer gueltig und falsch sowie einer unsigned Temp-Kopie fuer fehlend, ohne den Zertifikatsspeicher zu aendern. Jede gelesene JSON-Datei wird vor dem Zugriff strukturell geprueft; unerwartete Ausnahmen enden kontrolliert. | `py -3.13 tools\eq-copilot\pruefe_installer_manifest.py` | [OK] Exit 0 | 1,08 s | [A17](roh/NAK-223-092db4d.md#a17) |
| A18 | Gegenpfad installieren<->Rueckweg gefahren (Sandbox, nichts installiert): Erstinstallation traegt moduleinfo.json, ein Tausch ersetzt statt zu mischen, -Pruefen sieht den ganzen Ordner, der Rueckweg stellt den Vorzustand bytegleich her und laesst KEIN leeres .vst3-Gehaeuse stehen, selbst angelegte Ordner verschwinden nur leer, der NAK-41-Riegel greift und zerstoert seine eigene Wiederholungsquelle nicht, eine dritte Artefaktsorte bricht ab. | `py -3.13 tools\eq-copilot\pruefe_installer_gegenpfad.py` | [OK] Exit 0 | 34,09 s | [A18](roh/NAK-223-092db4d.md#a18) |
| A19 | BandGridZahlen.h ist bytegleich aus den zwei eingefrorenen Gitterfixturen erzeugt; die 64 Live-Gruppen partitionieren die 221 Feinbaender lueckenlos und ueberschneidungsfrei, und die groben Kanten sind bitgleiche Kopien feiner Kanten (kein zweites Filterbank-Gitter). | `py -3.13 tools\eq-copilot\erzeuge_bandgitter_header.py --pruefen` | [OK] Exit 0 | 0,13 s | [A19](roh/NAK-223-092db4d.md#a19) |
| A20 | Envelope-Fixture-Korpus und MANIFEST bytegleich zur Neuerzeugung; keine verwaiste Datei; jede der 14 Envelope-Regeln hat mindestens ein Negativfixture. | `py -3.13 tools\eq-copilot\erzeuge_envelope_fixtures.py --pruefen` | [OK] Exit 0 | 0,30 s | [A20](roh/NAK-223-092db4d.md#a20) |
| A21 | v3-Envelope unter Zufall: 20 000 Zufallspuffer bringen den Pruefer nie aus dem Tritt und JEDER angenommene Frame erfuellt jede Kopfregel; 3000 gekippte P2-Payloadbits fallen einzeln an der CRC; feindliche Laengen (0, 15, ueber der Grenze, 0xFFFFFFFF) und die u32-Grenze von 16+payload_len enden in der erwarteten Fehlerklasse, ohne Absturz und ohne Eingabeallokation in Groesse der behaupteten Laenge; 300 Runden Fragmentierung (byteweise und in Zufallshaeppchen) liefern exakt dieselben Frames; ein kaputter Frame beendet den Strom statt zu resynchronisieren; die Ratengrenze haelt unter Flut. | `cargo test --manifest-path broker/Cargo.toml --test transport_fuzz --color never` | [OK] Exit 0 | 0,47 s | [A21](roh/NAK-223-092db4d.md#a21) |
| A22 | Ende-zu-Ende ueber die PROBE-Pipe, zwei Sprachen ein Draht: 32 echte C++-Sondenpaare koppeln sich am echten Rust-Listener (32 Control + 32 Telemetry, jede Telemetrieverbindung ueber link_id + challenge + gleiche runtime_nonce), fluten P2 bis die Schleuse mit Cap 2 nachweislich ersetzt, und WAEHRENDDESSEN geht kein einziger P0-Frame verloren; die P0-Antwortlatenz bleibt unter der Schranke. Keine Verbindung wird wegen Envelope, Rate oder P0-Ueberlauf geschlossen. | `py -3.13 tools\eq-copilot\pruefe_ipc_last.py` | [OK] Exit 0 | 10,52 s | [A22](roh/NAK-223-092db4d.md#a22) |
| A24 | G3-SOAK Dauerlauf ueber eine ausschliessliche Probe-Pipe: 1 Main mit subscribe_session und echtem SourcesModel plus 16 Sondenpaare und 16 echte SondeProcessor am Ganzblockpfad laufen gegen den echten Rust-Coordinator, waehrend ein Viertel der Sonden langsam liest und der Broker mitten im Lauf getoetet und neu gestartet wird. Gemessen: Mitgliedschaft und fuehrendes_main driften nie, kein P0 geht ausserhalb der Neustartfenster verloren und seine ACK-p95 bleibt unter 1.000 ms, kein Mitglied wird ausserhalb der Fenster stale oder evicted, der Snapshot ist nach jedem Neustart binnen 60 s wieder vollstaendig und die alte broker_epoch kommt nie wieder, Working Set beider Prozesse waechst je Generation um hoechstens max(10 %, 16 MiB), und der Sondenpfad verliert keinen Ganzblock. Beide Programme verweigern Produktions- und Golden-Pipename mit Exit 3, gefahren vor dem Lauf. Seit SONDE-013 laeuft dabei der Evidenzpfad mit: jede Sonde baut oberhalb einer kleinen P1-Fuellstandsschwelle GAR KEINEN evidence_snapshot mehr und zaehlt den verworfenen Analyseframe, statt die in EINTRAEGEN gedeckelte P1-Queue mit 10-KiB-Nachrichten zu fuellen. | `py -3.13 tools\eq-copilot\pruefe_session_soak.py --sonden 16 --minuten 2 --neustarts 1` | [OK] Exit 0 | 148,97 s | [A24](roh/NAK-223-092db4d.md#a24) |
| A28 | Der P5-Evaluationskorpus ist reproduzierbar (Muster A25): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt SHA-256 im Manifest, und eine verwaiste Datei faellt. Zusaetzlich der Hygieneriegel aus NAK-182 R2: der Bezeichner des Kettenbeins steht WOERTLICH in seiner Datei, sonst waere der Name ein Etikett. | `py -3.13 tools\eq-copilot\erzeuge_p5_korpus.py --pruefen` | [OK] Exit 0 | 0,13 s | [A28](roh/NAK-223-092db4d.md#a28) |
| A29 | Das Sammelbein des P5-Exit-Gates (59 Punkt 6, 36.4, M-64 bis M-70, R2). Es ist das dritte Glied einer KETTE: der Korpus traegt die Wahrheit, broker/tests/sonde014_p5_korpus.rs faehrt jede Sitzung durch p1 und schreibt die TATSAECHLICH ausgegebene Hypothese, und dieses Bein haelt beides gegeneinander - eine falsche starke Produktbehauptung aendert den Korpus nicht, sie faellt am Vergleich. Precision und Recall stehen JE URSACHENKLASSE, dazu Brier, Kalibrierung, Coverage und Enthaltungsrate; die vier Riegel (falsche_starke, falsche_schwache, precision und recall in [0,1]) laufen je Klasse und ueber die Gesamtmenge. Die Schwelle aus M-31 ist AUSGABE: gesucht wird die niedrigste Sicherheitsstufe, deren Riegel halten. Der Startwert von GATE_MINDEST_FENSTER wird an den zwei Passagensitzungen kalibriert. Ohne frische Ergebnisdatei meldet das Bein Voraussetzung-fehlt (Exit 3) statt gruen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py` | [OK] Exit 0 | 0,13 s | [A29](roh/NAK-223-092db4d.md#a29) |
| A30 | Der Selbsttest des P5-Sammelbeins (M-68, Muster A27). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch DIESELBEN Funktionen, die das Sammelbein fuehrt. Jede Erwartung laeuft mit ihrem GEGENTEIL - eine falsche starke Behauptung und die richtige daneben, eine Enthaltung und dieselbe Menge ohne sie, ein leerer Satz und ein voller. Die vier Riegel werden synthetisch gefuettert und muessen fallen: precision > 1, recall > 1 und brier > 1 sind ueber den Korpusweg strukturell unerreichbar, und eine Wache ohne ausfuehrbaren Negativtest ist keine gemessene Zusage. Zuletzt die Schwellensuche in beide Richtungen. | `py -3.13 tools\eq-copilot\pruefe_p5_korpus.py --selbsttest` | [OK] Exit 0 | 0,14 s | [A30](roh/NAK-223-092db4d.md#a30) |
| A31 | Die adversarialen Gegenbeispiele des Phasengates G5 am PRODUKTPFAD - genau die Faelle, die das Korpusformat nicht ausdruecken kann. Eine Intent-Rolle trennt zwei messtechnisch identische Kandidaten NICHT, auch nicht bei rolle=fuehrt, und bei drei sichtbaren Kandidaten wird der Fuehrende gegen JEDEN geprueft, nicht nur gegen den Zweitplatzierten. Eine Quelle, die ihre Energie in einem ganz anderen Band traegt als der Masterbefund, erreicht keine starke Aussage. Zwei Kandidaten, deren Raenge sich nur durch ein Band WEIT ausserhalb des Befundintervalls unterscheiden, sind ungetrennt. Umklammernde und disjunkte Fensterspannen fallen am Alignment mit Grund, obwohl die Ueberdeckung ihrer SPANNEN 1,0 meldet. Und die Passage laeuft ueber den echten Produktpfad samt Store: zwoelf und acht Fensterlaengen tragen die Aussage, sieben nicht, und eine Passage ohne ein einziges Masterfenster ergibt eine ENTHALTUNG mit ungueltiger Beobachtung statt Schweigen (M-27). Seit der Nacharbeit 1 (07.09.2026) faehrt das Bein ALLE 15 vereinbarten Ketteneingaben durch denselben Produktpfad und DRUCKT je Eingabe ihr Ergebnis: vier Passagenrandwerte (12, 11, 8 und 7 Fensterlaengen), den zweiten experiment_begin mit gleicher passage_id, zwei Master, den Master ohne Fenster, den Kanalwechsel vor und nach dem Deskriptorwechsel, drei Sonden auf einem Kanal, die antikorrelierte Quelle allein und neben der korrelierten, dazu die Masteranomalie in einem Fenster HINTER der Passage. Seit NAK-213 (08.09.2026) traegt KEINE der sieben frueher gedruckten NAK-213-Luecken mehr eine Luecke: zwei Master ergeben eine Enthaltung OHNE ORT statt eines stillen Gewinners, ein Master ohne Fenster ebenfalls statt Schweigen, der Kanalwechsel nimmt die Belege GENAU DER wechselnden Quelle zurueck und entfernt ihren Befund, drei Sonden auf einem Kanal werden ueber ALLE Quellen der Sitzung erkannt - eine vierte, stumme Sonde desselben Kanals setzt die Duplikatmarke mit. Seit NAK-214 (08.09.2026) fahren drei Faelle dieses Tickets mit: zwei bestaetigte Mains erzeugen KEINEN Vorschlag (Vorschlaege 0, keine proposal-Zeile im Store), ein PRE/POST-Paar unterhalb der Alignmentschwelle bleibt Aussageklasse 1 mit next_test prepost_paar_messen, und eine zurueckgenommene juengste Passage laesst die AELTERE rechnen - jeder druckt sein Ergebnis. Keine Luecke bleibt gedruckt, und der aufruferlose Helfer `luecke()` ist mit seinem letzten Aufrufer gegangen. | `cargo test --manifest-path broker/Cargo.toml --test sonde014_gegenbeispiele --color never -- --nocapture --test-threads=1` | [OK] Exit 0 | 104,52 s | [A31](roh/NAK-223-092db4d.md#a31) |
| A25 | Der P4-Referenzkorpus ist reproduzierbar (M-80): der Erzeuger baut jede Datei BYTEGLEICH neu, --pruefen vergleicht den committeten Bestand gegen die Neuerzeugung samt MANIFEST und meldet jede verwaiste Datei. Dasselbe Muster wie A6, A7, A8, A10, A12 und A20; die gitattributes-Regel haelt die Zeilenenden. Seit NAK-182 weist der Erzeuger ausserdem ab, was der Korpus nicht tragen darf: eine Wahrheit ausserhalb der geschlossenen Menge, eine fehlende `quelle` und einen Bezeichner, der in der genannten Datei nicht woertlich steht. Die zwei Mengen KEINE_BEHAUPTUNG und KEINE_STARKE sowie die Zeile `nicht_gemessen` reisen im MANIFEST mit, damit der Pruefer sie von dort liest statt aus einer zweiten Kopie. | `py -3.13 tools\eq-copilot\erzeuge_p4_korpus.py --pruefen` | [OK] Exit 0 | 0,14 s | [A25](roh/NAK-223-092db4d.md#a25) |
| A26 | Das Sammelbein des P4-Exit-Gates (§58, §49.4, M-79/M-81 bis M-85): vier Klassen - Referenzkorpus, Loop-/Seek-/PDC-Goldens, adversariale Vergleichsfixtures und Stereo-Goldens -, alle muessen bestehen, und jedes genannte Bein steht wirklich im Kanon-Runner. Neben Precision und Recall zaehlen Kalibrierung, Brier Score, Coverage, Enthaltungsrate und die Zahl FALSCHER Behauptungen; die muss null sein. Eine Enthaltung ist dabei kein Fehler - §49.4 woertlich: ein konservatives unsicher ist besser als eine ueberzeugende falsche Ursache. Seit NAK-182 liest das Bein zwei geschlossene Wahrheitsmengen AUS DEM MANIFEST: auf KEINE_BEHAUPTUNG (unbekannt, unvergleichbar, zeitvariabel) ist jede Aussage falsch, auch die schwache; auf den zwei Werten, die nur in KEINE_STARKE stehen (nicht_kausal, nicht_exakt), ist es die starke. Zaehler und Nenner von Recall lesen dieselbe Menge, Precision und Recall sind als Quoten geriegelt, jeder Korpusfall nennt eine Quelldatei samt woertlichem Bezeichner und wird dort gesucht, die fuenf geschlossenen Mengen des v3-Vertrags werden auf kausale Bezeichner geprueft (Entscheid G4 §8), und die in P4 NICHT gemessene Zusage M-85 Ziel 1 wird im Kopf gedruckt statt still zu fehlen. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py` | [OK] Exit 0 | 0,13 s | [A26](roh/NAK-223-092db4d.md#a26) |
| A27 | Der Selbsttest des Exit-Gate-Beins (NAK-182 R1, MP1-6). Er laeuft OHNE Repo-Fixture: die Faelle entstehen im Speicher und laufen durch dieselben Funktionen, die A26 fuehrt. Auf dem Korpusweg fallen eine STARKE Aussage auf jedem Wert aus KEINE_STARKE und eine SCHWACHE auf jedem Wert aus KEINE_BEHAUPTUNG; eine schwache Aussage auf nicht_kausal oder nicht_exakt bleibt gruen - genau das trennt die zwei Mengen. Auf dem Kennzahlweg bekommt die Riegelfunktion synthetische Kennzahlen direkt eingespeist, weil Recall und Precision ueber 1 nach der Zweimengenlogik strukturell unerreichbar sind und die Wachen trotzdem einen Weg zu fallen brauchen. Dazu ein erfundener Bezeichner, ein Pfad ins Leere, eine fehlende Vertragsmenge und ein kausaler Enumwert. Seit Nacharbeit 1 laeuft auch S-07 durch die echte Validierung: ein synthetischer Fall mit einer Wahrheit ausserhalb ERLAUBTE_WAHRHEITEN geht durch dieselbe Funktion _pruefe_wahrheiten, die A26 auf den geladenen Korpus anwendet, und muss genau eine rote Zeile mit dem Fallnamen ergeben; sein Gegenteil keine. Jede Erwartung laeuft MIT ihrem Gegenteil - sonst waere der Selbsttest auch dann gruen, wenn ein Riegel immer rot meldete. | `py -3.13 tools\eq-copilot\pruefe_p4_korpus.py --selbsttest` | [OK] Exit 0 | 0,13 s | [A27](roh/NAK-223-092db4d.md#a27) |
| A23 | SONDE-012 E-L06 integriert ueber eine ausschliessliche Probe-Pipe: synthetische C++-Quellen senden vollstaendige Messfenster durch den echten Rust-Coordinator an das echte Main-SourcesModel. Der Endpunkt ist dessen Revision samt Anzeige-Invalidierung. Bei 16 und 32 Quellen liegt p95 fuer 2048/4096 Samples unter 300 ms und fuer 16384 Samples unter 750 ms, jeweils ab dem ersten Sample des Fensters. | `py -3.13 tools\eq-copilot\pruefe_sonde012_sources_latency.py` | [OK] Exit 0 | 4,64 s | [A23](roh/NAK-223-092db4d.md#a23) |
| A15 | STILLGELEGT - mass bis 28.08.2026 Nakama Suna (NkPr): Passthrough bitgleich ueber drei Samplerates und fuenf Blockgroessen, 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur passive_probe zu; speichern-laden-speichern bytegleich. | `(nicht gefahren)` | [STILLGELEGT] seit 2026-08-28 (S9b/SONDE-007c): das Ziel NakamaSuna ist stillgelegt - Suna ist in Nakama Probeeq aufgegangen (design/abnahmen/2026-08-28-suna-stilllegung-vorgezogen.md). Weder gebaut noch gefahren; die Zeile bleibt sichtbar, damit die Kanonzahl nicht still sinkt. | - | - |
| A16 | Nakama Probeeq (NkAc): heute ebenfalls Passthrough bitgleich (die EQ-DSP kommt in P6), 0 Samples Latenz, kein Tail, kein Hostparameter; Bundlevertrag laesst nur active_probe zu; speichern-laden-speichern bytegleich. Seit der T3-Nacharbeit 29.08.2026 (G1 §4.2) faehrt dasselbe Bein die Gate-7-Kette AM GEBAUTEN BUNDLE Nakama Probeeq - Klasse active_probe, denn die Sondenschale traegt genau EINE Klasse je Uebersetzung: ein sonst gueltiger Stand mit measurement_position=post_fader_contribution kommt ueber setStateInformation read-only zurueck, die Instanz bleibt neutral, die Originalbytes gehen unveraendert an den Host zurueck und ein erneutes Laden waescht nichts; auch eine bereits klassifizierte Instanz faellt beim Nachreichen desselben Standes auf neutral zurueck; Gegenprobe mit insert laedt normal. Zusaetzlich misst dasselbe Bein direkt an der oeffentlichen positionErlaubt, dass post_fader_contribution fuer alle vier Klassen abgelehnt wird - das gemeinsame Ergebnis von Capability-Vorpruefung und Klassenmatrix, ohne die beiden Haelften zu trennen. Die vollstaendige 16er-Matrix samt Bundlevertraegen misst B2. | `eq-copilot\build\plugin\EqCopProbeeqNullTest_artefacts\Release\EqCopProbeeqNullTest.exe` | [OK] Exit 0 | 0,10 s | [A16](roh/NAK-223-092db4d.md#a16) |
| B1 | Bundle-Identitaet (CIDs, JUCE_VST3_CAN_REPLACE_VST2=0) eingefroren. | `eq-copilot\build\plugin\EqCopIdentityTest_artefacts\Release\EqCopIdentityTest.exe` | [OK] Exit 0 | 0,13 s | [B1](roh/NAK-223-092db4d.md#b1) |
| B2 | State-Schema 2: Roundtrip bytegleich, Schema-1-Migration rein und golden, unbekanntes Major read-only mit Originalbytes, Duplicate erkennbar und aufloesbar, Host-Dirty; alle vier Klassen sperren post_fader_contribution; NAK-40-Aliasvektoren adressieren die bytegleich erhaltene Legacy-instance_id ohne Alias im State; Parametertabelle und RFC-8785-state_hash sprachuebergreifend. | `eq-copilot\build\plugin\EqCopStateMigrationTest_artefacts\Release\EqCopStateMigrationTest.exe` | [OK] Exit 0 | 1,21 s | [B2](roh/NAK-223-092db4d.md#b2) |
| B3 | Hostkontext (Anwesenheit, Parameterpunkte, Buslatenz) wird gemessen, nicht geraten; Quellhash-Gate des JUCE-Patches gruen. | `eq-copilot\build\plugin\EqCopHostContextTest_artefacts\Release\EqCopHostContextTest.exe` | [OK] Exit 0 | 0,08 s | [B3](roh/NAK-223-092db4d.md#b3) |
| B3b | Termin-B-Messgeraet: Passthrough bitgleich, Sprung-/Automations-/Latenzmessung inkl. Fehlalarm-Riegel, Bericht-Rueckweg, 0 Allokationen. | `eq-copilot\build\plugin\EqCopHostProbeTest_artefacts\Release\EqCopHostProbeTest.exe` | [OK] Exit 0 | 0,11 s | [B3b](roh/NAK-223-092db4d.md#b3b) |
| B3c | v3-Vertrag: C++ klassifiziert JSON- und FlatBuffers-Korpus wie das Manifest (Urteil UND Verletzungsmenge), einschliesslich Boolean/RFC-6901-Discriminatoren, NAK-29 in beiden Darstellungen und band_stereo ID 10; Bandgitter und Quantisierung bitgleich. Seit SONDE-013 misst dasselbe Bein integration_samples ID 14 ueber Encoder und Leser: gesetzt kommt Bit UND Wert zurueck, Abwesenheit bleibt gueltig und ist KEINE 0, und eine gesendete 0 faellt als Senderfehler. | `eq-copilot\build\plugin\EqCopSchemaTest_artefacts\Release\EqCopSchemaTest.exe` | [OK] Exit 0 | 0,38 s | [B3c](roh/NAK-223-092db4d.md#b3c) |
| B4 | StampedAudioQueue und Ein-Block-Quarantaene: Ganzblockaufnahme bitgleich ueber jeden Ringumlauf, Ueberlauf BEIDER Ringe verwirft den ganzen Block und nie eine Teilmenge, Oversize ueber der Slotkapazitaet faellt fuer die Analyse und laesst Audio unberuehrt, Flush (numSamples 0) ist kein Verlust, Mono dupliziert L, Mehrfach-Tap-Layout traegt; Quarantaene versiegelt erst mit bewiesener Fortsetzung, Seek und Transportkante verwerfen genau EINEN Block, stehende Projektzeit (FL-Teilpuffer) ist kein Bruch, Projektzeit-Ueberlauf und negative Zeit sind behandelt; Worker-Publikation folgt monotonen 50-/250-ms-Deadlines statt Batchzahl, holt nach Pausen nicht auf und laesst wartende Reset-/Frame-Aufrufer vor; verdrahtet: Passthrough bitgleich ueber 18 Blockgroessen von 1 bis 16384, 0 Samples Latenz, kein Tail, 0 Allokationen im Audiothread ueber 4000 Bloecke wechselnder Groesse mit Transportkanten UND ueber 2000 weitere mit wechselnder Hoer-Markierung, Ein- und Ausfade und Interventionsring (SONDE-013 M-74), und die Engine bekommt den Strom bis auf den Block in Quarantaene. | `eq-copilot\build\plugin\EqCopQueueStressTest_artefacts\Release\EqCopQueueStressTest.exe` | [OK] Exit 0 | 3,51 s | [B4](roh/NAK-223-092db4d.md#b4) |
| B9 | Fixed-memory Loudness (§48.1): der LoudnessAccumulator deckt sich mit der ausgebauten Vektorrechnung innerhalb ±0,1 LU (Entwurf §49) ueber konstante, rampende, zufaellige und einstuendige Korpora sowie ueber Stille unter dem absoluten Gate; Kurz-LUFS ist BITGLEICH; ein adversarialer Sweep legt 1000 Bloecke in den Grenzbin des relativen Gates und die selbstgemeldete Schranke unsicherheitLu() deckt jeden Lauf; eine Million Zellen laufen mit 0 Allokationen durch, waehrend die Gegenprobe (alte Rechnung) allozert; NaN/Inf-Zellen sind gezaehlt statt still als 0 verbucht und l_j == -70,0 exakt bleibt wertgleich; ueber dem Feingitter traegt ein OBERBAND aus Bins von 1 LU bis ueber lautheit(DBL_MAX) - mit ZWEI Pegeln darin (Korpus des T2-Pruefers), einem adversarialen Sweep im Oberband-Grenzbin, beiden Richtungen der Naht zwischen den Aufloesungen und der Gegenprobe, dass kein Block durch das Raster faellt. | `eq-copilot\build\plugin\EqCopLoudnessGoldenTest_artefacts\Release\EqCopLoudnessGoldenTest.exe` | [OK] Exit 0 | 0,17 s | [B9](roh/NAK-223-092db4d.md#b9) |
| B5 | FeatureEngine v2 haelt Zeit-, Validity-, Event- und Bandvertraege: Bandgitter und alle Quantisierungsvektoren bitgleich zum v3-Vertrag, Bitmap LSB-first mit Fuellbits 0, FFT gegen Parseval und einen Sinus auf der Binmitte, K-Gewichtung ueber 20 Hz..20 kHz unter 0,1 dB an der BS.1770-Referenzkette; Drop/Seek(laufend UND gestoppt)/Loop-Wrap/moeglicher Straddle/Transportkante/Sampleratewechsel/Neuanlauf/Beweislagewechsel trennen JEDES offene Fenster - auch den K-Filterzustand, bitgleich gemessen -, waehrend FL-Teilstuecke mit stehender Projektzeit lokal weiterlaufen, und ein Drop zaehlt als Segment, nicht als Epoche. Seit NAK-182 misst ein Sweep zusaetzlich die ZEITLAGE: ein bekannter Impuls bei Stromsample 206336 laeuft ueber die fuenf Blockgroessen 1, 333, 512, 2048 und 16384 mal die fuenf Sampleraten 44,1 / 48 / 88,2 / 96 und 192 kHz, und der gemeldete stromSample ist je Samplerate ueber alle fuenf Blockgroessen IDENTISCH und liegt innerhalb einer Fensterlaenge (4096 Samples) vor dem Impuls. Die Auswahl des Ereignisses kennt die Sollzeit NICHT - gemessen wird das staerkste Flussereignis des Laufs, und dass es unzweifelhaft das staerkste ist, ist eine eigene Zusage. | `eq-copilot\build\plugin\EqCopAnalysisGoldenTest_artefacts\Release\EqCopAnalysisGoldenTest.exe` | [OK] Exit 0 | 5,30 s | [B5](roh/NAK-223-092db4d.md#b5) |
| B6 | Aktiver DSP-Kern liefert die eingefrorene Referenzantwort. | `eq-copilot\build\plugin\EqCopDspGoldenTest_artefacts\Release\EqCopDspGoldenTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B7 | Apply/Revert ist transaktional - kein halber Zustand ueberlebt. | `eq-copilot\build\plugin\EqCopTransactionTest_artefacts\Release\EqCopTransactionTest.exe` | [GEPLANT] geplant (ab P6) | - | - |
| B10 | C++-v3-Vertrag und SONDE-011 Phase B: Envelope/CRC/Pipetoken und begrenzte P0/P1/P2-Politiken bleiben gruen; das In-Flight-Register gibt persistente Befehle erst bei angewandt, idempotent_wiederholt oder endgueltigem Fehler frei und reiht vor ACK dieselbe command_id wieder ein; Autostart verbindet zuerst, prueft Manifest-SHA-256 und bei gesetztem Thumbprint WinVerifyTrust plus Signer, spawnt verborgen und mutex-idempotent, haelt Backoff/Timeout/Cooldown-Grenzen und beendet keinen Brokerprozess. Seit SONDE-013 nimmt die C++-Vertragsengine die drei Experimentfamilien aus den committeten Fixtures an, lehnt einen fremden execution_mode an derselben Engine ab, und ein experiment_manual_result laeuft als persistenzpflichtiger P0-Befehl durch das In-Flight-Register wie jede andere steuernde Nachricht. | `eq-copilot\build\plugin\EqCopIpcTest_artefacts\Release\EqCopIpcTest.exe` | [OK] Exit 0 | 217,24 s | [B10](roh/NAK-223-092db4d.md#b10) |
| B11 | Probeeq uebernimmt JUCE updateTrackProperties ausschliesslich auf dem Message-Thread; gueltige Hostnamen reisen codepointgetreu in heartbeat.runtime, leer/Whitespace/121 Codepoints/C0/C1 gelten als Fehlen; Hostvorrang veraendert das gespeicherte User-Label nicht, Clear stellt dessen Rueckfall her. Persistierte Messpunkte insert/pre/post und Betrieb active/suspended/offline reisen ohne Synthese; der lokale C++-Pfad behauptet nicht, dass FL den Callback liefert. | `eq-copilot\build\plugin\EqCopSonde012HostChannelContextTest_artefacts\Release\EqCopSonde012HostChannelContextTest.exe` | [OK] Exit 0 | 1,01 s | [B11](roh/NAK-223-092db4d.md#b11) |
| B12 | Probeeq fuehrt Audio ueber die vorallokierte Ganzblockqueue zum Analyseworker und P2: LUFS-I reist nur atomar mit endlicher unsicherheitLu und ohne Status; collecting/gated reisen ohne Zahlenpaar; halbe, nichtendliche oder statusbehaftete Paare werden nicht serialisiert. Seek, Loop, Oversize-Drop und Kontinuitaetsbruch starten eine leere Reihe; der gemessene Audiopfad alloziert dabei 0-mal und verwirft bei Ueberlast nur Analyse. | `eq-copilot\build\plugin\EqCopSonde012LoudnessSourceTest_artefacts\Release\EqCopSonde012LoudnessSourceTest.exe` | [OK] Exit 0 | 1,61 s | [B12](roh/NAK-223-092db4d.md#b12) |
| B13 | Gen/Main ersetzt sein Quellenmodell durch absolute session_snapshot-Sichten und haelt Mitgliedschaft, Control, Messung, Namensherkunft und Capability-Evidenz getrennt; Frischegrenzen, Messpunkte, Betrieb, Fehlerkanal, Lautheitspaar, Hostvorrang, Sortierung und Hauptziel werden einzeln gemessen. Die integrierte 16-/32-Quellen-Zeitmessung gehoert A23. | `eq-copilot\build\plugin\EqCopSonde012SourcesModelTest_artefacts\Release\EqCopSonde012SourcesModelTest.exe` | [OK] Exit 0 | 0,12 s | [B13](roh/NAK-223-092db4d.md#b13) |
| B14 | MainProject Save/Load erhaelt bestaetigte Quellidentitaet und User-Label, aber weder Control-Liveness noch Runtime-Nonce oder Messframe; Join, Benennen und Unbind melden je echte persistente Aenderung Host-Dirty, No-op, Save und Load nicht; Reload baut einen frischen subscribe_session-Auftrag. | `eq-copilot\build\plugin\EqCopSonde012ProjectReloadTest_artefacts\Release\EqCopSonde012ProjectReloadTest.exe` | [OK] Exit 0 | 0,22 s | [B14](roh/NAK-223-092db4d.md#b14) |
| B15 | EqCopShot rendert den deklarierten SONDE-012-Sichtsatz bei exakt 760x430: 0/1/16 Quellen, fresh/partial/stale/disconnected/invalid, Namens- und Lautheitsgegenpfade, alle Diagnosezustaende, Label-/Unnamed-Rueckfall, Findings, genau ein Hauptziel mit Referenzen sowie das deaktivierte Label-Feld eines nicht fuehrenden Main. | `eq-copilot\build\plugin\EqCopShot_artefacts\Release\EqCopShot.exe --sonde012-suite eq-copilot/build/sonde012-shots` | [OK] Exit 0 | 2,24 s | [B15](roh/NAK-223-092db4d.md#b15) |
| B20 | Gesamtklasse nach §34.3 (M-06): die vier Klassen sind geordnet, und `deckeln` ist ein Minimum, kein Mittelwert. Jeder der vier harten Maengel deckelt EINZELN auf schwach, ohne gegen die drei guten Nachbarquellen verrechnet zu werden; zwei Maengel ergeben in allen zwoelf Paarungen unbrauchbar. Was ein Erzeuger nicht beurteilen kann, deckelt ebenso auf mittel - eine Sonde mit PERFEKTER Abdeckung kommt deshalb nur auf mittel, weil sie Session, Passage und Alignment nicht sieht; das ist die Regel hinter dem Literal, das Etappe B an einer Stelle gesetzt hatte. Die zwei Abdeckungsschwellen und die Fensterschwelle fallen je an ihrer Kante und knapp darunter (Muster C-09). Die tragende Zusage ist eine INVARIANTE ueber den ganzen Raum: in 1536 Uebergaengen ueber alle 256 Bitkombinationen in drei Basislagen hebt ein zusaetzlicher harter Mangel die Klasse NIE - und alle vier Klassen kommen darin wirklich vor, die Invariante ist also nicht trivial erfuellt. Ein Mittelwert koennte diese Zusage nicht halten. | `eq-copilot\build\plugin\EqCopSonde013QualityClassTest_artefacts\Release\EqCopSonde013QualityClassTest.exe` | [OK] Exit 0 | 0,07 s | [B20](roh/NAK-223-092db4d.md#b20) |
| B22 | Content-Fingerprint einer Passage (§32.4, M-26/M-27/M-31): er entsteht erst ab genug Material und traegt die Zahl seiner Fenster mit; dasselbe Material zweimal ergibt BITGLEICH denselben Fingerprint, derselbe Akkord in zwei Lautstaerken bleibt ueber 0,95 aehnlich (jeder Verlauf ist auf sein eigenes Maximum normiert), ein anderer Akkord ist messbar unaehnlicher und Rauschen deutlich. Die adversariale Rueckrechenprobe zeigt nicht, dass eine Rueckrechnung schwer waere, sondern dass die Information nicht da ist: 76 Byte fuer 204800 Samples, und ein Sinus und ein Dreieck derselben Grundfrequenz sind sich AEHNLICH, obwohl sie voellig anders klingen - der Fingerprint ist bewusst nicht injektiv, und alle drei Verlaeufe sind Energien ohne Phase. Das Fingerprintfenster ueberbrueckt keine Epochengrenze: nach einer Stromluecke traegt die Engine den Fingerprint des NEUEN Materials (Aehnlichkeit 1,00 zur neuen Referenz, 0,00 zur alten), und der Fensterzaehler faellt mit - bei einer Ueberbrueckung waeren es doppelt so viele. Die Aehnlichkeit selbst ist ein MINIMUM ueber die drei Verlaeufe und kein Mittelwert; zwei Fingerprints ohne Bit sind nicht aehnlich, sondern gar nichts. | `eq-copilot\build\plugin\EqCopSonde013FingerprintGoldenTest_artefacts\Release\EqCopSonde013FingerprintGoldenTest.exe` | [OK] Exit 0 | 0,90 s | [B22](roh/NAK-223-092db4d.md#b22) |
| B23 | Die manuelle Passage als Projektintent im MainProjectState (§33.5, M-25/M-69). Das neue persistente Feld `manual_passages_v1` traegt seine Fassung im Namen; ein Altstand ohne das Feld laedt normal, ein unbekanntes Zusatzfeld aus einer spaeteren Fassung zerstoert den Leser nicht und steht unveraendert im naechsten Save, und Save/Load ist ueber zwei Runden bytegleich - auch am oberen int64-Rand und mit leerer Liste (die Eigenschaft entfaellt dann, statt als leeres Array zu reisen). Jede persistente Aenderung meldet GENAU einmal Host-Dirty, das Vergessen wie das Merken; ein read-only-Stand verweigert beides und gibt seine Originalbytes zurueck. Was hier NICHT reist, ist Absicht: Fingerprint, Quellenset, Abdeckung und Epoche sind Messergebnisse und bleiben im Store, sonst behauptete ein Projekt nach dem Loeschen der Datenbank weiter, es gebe Evidenz. Jede Grenze wird von beiden Seiten gefahren - was die Produkt-API ablehnt, lehnt auch der Leser ab, und zwar fail-closed als read-only statt still korrigiert. | `eq-copilot\build\plugin\EqCopSonde013PassageStateTest_artefacts\Release\EqCopSonde013PassageStateTest.exe` | [OK] Exit 0 | 11,52 s | [B23](roh/NAK-223-092db4d.md#b23) |
| B24 | PRE/POST auf der Audioseite (§38.3/§38.4, M-18/M-20/M-24). Die Magnitude-Squared Coherence ist 1 fuer JEDE lineare zeitinvariante Kette und faellt genau dann, wenn die Kette nichtlinear oder zeitvariabel ist: identische Kette, reiner Gain von +15 dB und ein linearer Tiefpass bleiben fast ueberall ueber der 0,8-Schwelle aus §38.3 - Kompression, Modulation, Saturation und wechselnde Latenz fallen darunter, und die beiden Gruppen ueberlappen nicht. Deshalb traegt keine der vier adversarialen Ketten eine statische EQ-Behauptung. Der Vergleichspegel wird vorab gemessen und eingefroren: sechs Sekunden mit +18 dB und zwei Sekunden Stille aendern ihn um kein Tausendstel, denn eine mitlaufende Regelung wuerde selbst zum hoerbaren Prozessor; ohne genug Material friert er gar nicht erst ein, und NaN/Inf verriegeln statt ihn zu vergiften. Hoerbares Delta ist ohne nachgewiesenes Compare-Routing gesperrt - der Raum aus drei binaeren Nachweisen wird VOLLSTAENDIG abgefahren, genau eine der acht Kombinationen ist frei, und in P4 gibt es kein Compare-Routing. | `eq-copilot\build\plugin\EqCopSonde013PrePostGoldenTest_artefacts\Release\EqCopSonde013PrePostGoldenTest.exe` | [OK] Exit 0 | 1,02 s | [B24](roh/NAK-223-092db4d.md#b24) |
| B25 | Unsicherheit und Mehrfachtestung (§43.2, M-45). Der Block-Bootstrap ist deterministisch, enthaelt den Mittelwert und liefert bei bloeckigem Material ein BREITERES Intervall als ein Bootstrap ueber Einzelwerte - benachbarte Deltas sind korreliert, und wer Unabhaengigkeit annimmt, macht aus Rauschen eine belastbare Aenderung. Leere oder unmoegliche Eingaben ergeben KEIN Intervall statt eines um null. Bei 221 gleichverteilten p-Werten meldet der naive Scan zweistellig viele Baender und die FDR-Korrektur kein einziges, waehrend 20 klare Treffer durchkommen; die Grenze p_(k) = k/m*q faellt inklusiv an ihrem Wert. Cluster verlangen zusammenhaengende Baender - drei verstreute ergeben keinen, vier benachbarte schon, und auch am linken und rechten Rand. | `eq-copilot\build\plugin\EqCopSonde013ExperimentGoldenTest_artefacts\Release\EqCopSonde013ExperimentGoldenTest.exe` | [OK] Exit 0 | 0,07 s | [B25](roh/NAK-223-092db4d.md#b25) |
| B26 | Die zwei Kanten des Blindvergleichs (§43.1, §15, M-43/M-44). Ohne eingefrorenen Vergleichspegel wird KEIN Klangurteil angenommen - lauter klingt besser, und ein A/B ohne Pegelabgleich misst genau das; ein noch laufender Pegel zaehlt nicht als Abgleich. Die Blindreihenfolge wird vor dem Urteil gebunden und laesst sich nicht drehen; vor dem Urteil gibt der Typ sie GAR NICHT heraus, und der Ausgabeparameter bleibt unberuehrt statt still einen lesbaren Wert zu tragen. Erst das Urteil deckt genau die gebundene Reihenfolge auf, in beide Richtungen geprueft. Ein zweites Urteil und ein spaeterer Pegel aendern nichts mehr; jede Sperre traegt ein Wort, und der Passagenwechsel raeumt Urteil, Abgleich und Bindung gemeinsam ab. | `eq-copilot\build\plugin\EqCopSonde013ExperimentUiTest_artefacts\Release\EqCopSonde013ExperimentUiTest.exe` | [OK] Exit 0 | 0,50 s | [B26](roh/NAK-223-092db4d.md#b26) |
| B21 | RT→Control-Ring fuer hoerbare Eingriffe (M-37 bis M-39, §34.2): ein Begin steht SOFORT zur Abholung bereit, ohne Kadenzfenster - der Unterschied zum 1-Hz-Heartbeat-Bit, das ein 380-Sample-Ereignispaar nie gesehen haette. Ein Ende traegt einen Nachlauf, der mindestens so lang ist wie der Eingriff selbst, UND dessen Dauer, damit ein Empfaenger die Konservativitaet pruefen statt sie glauben kann. Der Ueberlauf ist die schaerfste Zusage: dieser Ring verwendet KEIN drop-oldest, das aelteste Ereignis steht unveraendert an erster Stelle, der Ueberlauf ist sticky gemeldet und gezaehlt, der Rest kommt lueckenlos aufsteigend heraus, und das Sticky-Bit heilt nicht von selbst - nur resync() loescht es. 200 Runden Schreiben, Lesen und Abfragen erzeugen NULL Allokationen, mit Zaehler gemessen statt behauptet (M-74). Sequenzen am u64-Rand kommen unveraendert zurueck, und zuruecksetzen() leert Ring, Sticky-Bit und Zaehler gemeinsam. | `eq-copilot\build\plugin\EqCopSonde013InterventionRingTest_artefacts\Release\EqCopSonde013InterventionRingTest.exe` | [OK] Exit 0 | 0,07 s | [B21](roh/NAK-223-092db4d.md#b21) |
| B19 | Bandweise Stereoevidenz (§40.1, §40.3, M-08/M-10/M-11/M-12): die fuenf Klassen aus §40.3 treffen ihre ANALYTISCH bekannte Antwort - Mono ergibt Korrelation 1, Kohaerenz 1, Phase 0 und Folddown 0 dB innerhalb der 0,25 dB aus §40.3; Polaritaetsinvertierung ergibt Korrelation -1 bei Kohaerenz 1 (die Kohaerenz misst den Zusammenhang, nicht das Vorzeichen) und eine Monosumme, die an die Vertragsgrenze laeuft statt zu schweigen; eine bekannte Laufzeit ergibt eine Phase, die der Formel +2*pi*f*tau folgt, an drei Traegerfrequenzen gemessen; unkorrelierte Kanaele fallen in Korrelation und Kohaerenz, und bei niedriger Kohaerenz entsteht keine Lag- oder Polaritaetsempfehlung. Seit NAK-182 faehrt ein Sweep dieselben fuenf Klassen und den Folddown ueber die 18 Blockgroessen 1, 2, 3, 7, 15, 16, 31, 64, 127, 128, 333, 512, 1024, 2048, 4096, 8192, 12345 und 16384 - je Blockgroesse dieselbe Antwort, je Blockgroesse ein wirklich entstandenes Evidenzfenster und vergleichbar viele gemittelte Fenster (Freiheitsgrade innerhalb 20 Prozent des Medians). Das ist eine AUSWAHL aus 1 bis 16384 und keine Erschoepfung: die Obergrenze ist die Slotkapazitaet des Layouts, 256 zum Beispiel bleibt ungemessen. Seit Nacharbeit 1 wertet der Sweep bei bekannter Laufzeit ALLE DREI Traeger aus - 300, 900 und 2000 Hz, je Blockgroesse mit Praesenzbit und Phasenfehler gegen +2*pi*f*tau innerhalb 0,25 rad -, und der Unkorreliert-Fall misst je Blockgroesse zusaetzlich, dass KEIN Band mit Basis eine Phase traegt; das sind dieselben Felder, die der 512er-Abschnitt fuer M-12 nutzt. | `eq-copilot\build\plugin\EqCopSonde013StereoGoldenTest_artefacts\Release\EqCopSonde013StereoGoldenTest.exe` | [OK] Exit 0 | 1,80 s | [B19](roh/NAK-223-092db4d.md#b19) |
| B17 | True Peak nach ITU-R BS.1770-5 (M-02): der 8-fach-Polyphaseninterpolator trifft die fuenf True-Peak-Testfaelle aus EBU Tech 3341 (15 bis 19) innerhalb der SCHAERFEREN Toleranz aus §49.3, also +/-0,1 dB statt der +0,2/-0,4 der Norm; gemessen wird gegen die ANALYTISCHE Referenz - die Signale sind Sinus mit definierter Frequenz, Amplitude und Phase, und der wahre Scheitel eines Sinus IST seine Amplitude, also steht keine zweite eigene Implementierung als Vergleich dahinter. Der Gegenbeleg zeigt, dass ein Detektor ohne Ueberabtastung bei Testfall 16 um 3,01 dB zu optimistisch waere. Der Sampleraten-Sweep ueber 44,1/48/88,2/96/192 kHz haelt dieselbe Zusage, weil die EBU-Frequenzen an fs gebunden sind. Die 8-fach-Entscheidung wird als ZAHL getroffen: die geschlossene Fehlerschranke der Ueberabtastung haelt bei fs/4 mit Faktor 8 (-0,042 dB) und reisst mit Faktor 4 (-0,169 dB). Raender: Stille ergibt exakt 0, ein NaN bleibt nicht im Filterzustand, ein Gleichanteil bleibt bitgenau er selbst (jede der acht Phasen hat DC-Verstaerkung 1, nicht nur die Gesamtsumme 8), und ohne vorbereiten() misst der Detektor gar nicht, statt zu raten. | `eq-copilot\build\plugin\EqCopSonde013TruePeakGoldenTest_artefacts\Release\EqCopSonde013TruePeakGoldenTest.exe` | [OK] Exit 0 | 0,11 s | [B17](roh/NAK-223-092db4d.md#b17) |
| B18 | Loudnessfenster, Headroom und Dynamik (M-01, M-03, M-04, M-07, M-09): Momentary (400 ms) und Short-term (3 s) sind wirklich zwei Fenster - bei stehendem Pegel gleich, 0,8 s nach einem Pegelsprung mehr als 5 LU auseinander, danach wieder beieinander; nach 0,6 s traegt der Frame Momentary und KEIN Short-term. integration_samples passt im Dauerbetrieb zur 10-Hz-Kadenz und faellt beim ersten Rahmen nach einer Grenze messbar kleiner aus. PSR rechnet gegen das True-Peak-Maximum DESSELBEN 3-s-Fensters, nicht gegen den Sample-Peak des 100-ms-Rahmens; die Engine setzt selbst kein PLR, weil LUFS-I erst im Sondenprozessor zugemischt wird. Headroom ist eine Verteilung: zwei Signale mit gleichem Maximum trennen sich um mehr als 10 dB im P50. Crest steht in zwei Fenstern und trennt dichte kleine Spitzen von einer einzelnen grossen. LRA gibt es nach 30 s NICHT, nach 75 s mit passender Spanne, bei Material ohne Dynamik nahe null und nach 90 s Stille gar nicht - die 60-s-Schwelle zaehlt gegatetes Material, keine Wanduhr. NaN und Inf im Eingang erzeugen in keiner der neun neuen Metrikstellen einen nichtendlichen Wert. Ein verworfener Analyseblock schliesst auch die neuen Fenster, und keines von ihnen ueberbrueckt eine Transportgrenze. | `eq-copilot\build\plugin\EqCopSonde013DynamicsTest_artefacts\Release\EqCopSonde013DynamicsTest.exe` | [OK] Exit 0 | 4,18 s | [B18](roh/NAK-223-092db4d.md#b18) |
| B16 | Evidenzpfad (§33.2): der aus einem echten FeatureFrame erzeugte evidence_snapshot passiert Textriegel und dieselbe Vertragsengine wie B3c; P10/P50/P95 trennen ein springendes von einem ruhigen Signal gleicher mittlerer Lautheit (Spanne P95-P10 um mehr als 6 dB verschieden) und halten in JEDEM Band mit Bit die Ordnung P10<=P50<=P95; die Abdeckung faellt messbar, sobald ganze Analysefenster in Stille liegen, und die Konvergenz faellt bei wanderndem Pegel; der Ereignisstrom traegt Fluss- und Peakbit getrennt samt Verlustzaehler, ein Ereignis fremder Epoche oder ohne Anker entsteht gar nicht. Seit NAK-182 misst dasselbe Bein die C++-Haelfte der fokussierten 0,01-dB-Evidenz (M-83 Satz 2), seit Nacharbeit 1 vollstaendig am SERIALISIERTEN Snapshot: PRE und POST laufen durch den echten Serialisierer, und jede Zahl kommt aus verteilung.p50 des eigenen Wire-Textes - Encoding-Wort, Ganzzahlen und Praesenzbits. Daraus kommt ein bekannter Gain von 3,00 dB je Band mit Bit innerhalb 0,1 dB zurueck, und eine Leiter aus 20 Pegeln im Abstand 0,01 dB - je ein eigener Enginelauf samt Serialisierung - ergibt 20 streng monotone Ganzzahlen, deren Nachbarn sich um genau 1 unterscheiden. Material (Frequenz, Samplerate, Amplitude, Blockgroesse, Laufbegrenzung), Pegel, Gain und Aufloesung kommen aus den ZAHLEN der Fixture evidenz-0p01-paar-wire-v1.json, nicht aus ihren Bytes und nicht aus lokalen Konstanten. | `eq-copilot\build\plugin\EqCopSonde013EventWireTest_artefacts\Release\EqCopSonde013EventWireTest.exe` | [OK] Exit 0 | 0,51 s | [B16](roh/NAK-223-092db4d.md#b16) |
| B27 | Der musikalische Intent als Projektintent im MainProjectState (§37.1, U22, M-01 bis M-09/M-11). Die Rollenmenge ist GESCHLOSSEN und hat genau fuenf Werte; ein sechster faellt in der Produkt-API UND im Leser, statt still auf einen bekannten Zweig abgebildet zu werden. Die §37.1-Belegung ist ABGELEITET und damit total und injektiv: alle fuenf Rollen kommen ueber den Roundtrip zurueck, keine zwei teilen sich eine Belegung (alle zwanzig geordneten Paare), und eine fremde Belegung gehoert zu keiner Rolle. Schutzangaben sind ORTHOGONAL zur Rolle - eine Quelle ohne Rolle kann Attack geschuetzt haben, und keiner der fuenf Rollenwechsel loescht einen Schutzbereich. Die Rolle bewusst-verschmolzen ist ein globales Veto gegen Entmaskierung; eine ausdrueckliche gerichtete Beziehung hebt es NUR fuer dieses Paar auf, nicht fuer ein drittes. Die Konfliktregeln sind eine geordnete Liste, kein Score: alle zehn Paarungen der fuenf Stufen fallen in beiden Leserichtungen, hundert Vermutungen schlagen die Schutzgrenze nie, und innerhalb Stufe 2 gewinnt das Spezifischere. Ein Zyklus im fuehrt_vor-Graphen erreicht die Persistenz NIE unmarkiert - die Pruefung laeuft beim Speichern, der Leser weist einen von Hand gebauten Zyklus als read-only ab, und derselbe Schluss als gleichrangig gespeichert laedt und kommt als gleichrangig zurueck. Eine abgeleitete Vermutung ueberschreibt einen Userwert nie, der umgekehrte Weg gilt. Derselbe Bus traegt verschiedene Rollen in zwei Passagen ohne Datenverlust, passagespezifisch vor global. Dazu die fuenf Teile der State-Invariante: Fassung im Namen, Altstand ohne die Felder laedt bytegleich, eine unbekannte Eigenschaft ueberlebt, Save/Load ueber zwei Runden bytegleich, und jede echte Aenderung meldet GENAU einmal Host-Dirty, waehrend No-op, abgewiesener Wert, Laden und read-only schweigen. Jede Grenze faellt von BEIDEN Seiten (17 Leserfaelle samt NaN/Inf-Konfidenz, Revision 0, Bandrand 0/221 und Selbstbeziehung), und die volle 256er-Liste, die die API erzeugt, laedt der eigene Leser. Zuletzt M-11 am Produktpfad: zwei echte Prozessoren mit demselben Strom, einer mit vollem Intent, liefern bitgleiche Evidenzbaender, Perzentile, Livebaender, metrics_version und NaN-Zaehler - mit Gegenprobe, dass die Baender wirklich Werte tragen. | `eq-copilot\build\plugin\EqCopSonde014IntentTest_artefacts\Release\EqCopSonde014IntentTest.exe` | [OK] Exit 0 | 0,37 s | [B27](roh/NAK-223-092db4d.md#b27) |
| B29 | Die Zustandsmaschine des AssistantStep im Main (46.1, E-07/E-08, M-55 bis M-62). Die Zustandsmenge hat ACHT Werte, jeder kommt ueber seinen eigenen Rueckweg zurueck, ein neunter faellt. Nach preview fuehrt KEINE Kante - weder hin noch zurueck -, ein gespeicherter preview-Schritt ist ein Lesefehler statt eines stillen Sprungs, und die Produkt-API setzt ihn nicht; die Gegenprobe mit proposal laedt normal. Jeder der acht Zustaende traegt fuenf Angaben (Eintritt, Evidenz, Useraktion, Timeout ueber 0, sichere Rueckkante), auch preview - der erste Zustand ist seine eigene Rueckkante. Der Deckel ist STRUKTURELL: ein zweiter Startversuch bei offenem Schritt wird abgewiesen, nicht eingereiht, und erst nach dem terminalen Abbruch beginnt ein neuer. Die vier Gegenpfade laufen einzeln: Zurueck geht auf die Rueckkante, Ueberspringen auf den naechsten Zustand, Resume ist eine FRAGE ohne Revisionssprung, und Verwerfen ist terminal statt geloescht - danach gibt es kein Resume mehr. Die drei benannten Ergebnisse aus 46.2 sind eigene Ergebnisse mit Objekt; dasselbe zweimal hebt keine Revision. Die harten Gates greifen VOR der Gewichtung: drei Kandidaten mit perfektem Rang und je einem gerissenen Gate verlassen die Liste, ein bescheidener bleibt. Verdrahtet am echten Prozessor mit Host-Dirty genau einmal je echter Aenderung, Rekonstruktion aus dem gespeicherten MainProject und bytegleichem Save/Load; jede Grenze faellt von beiden Seiten, und ein Altstand ohne die Eigenschaft laedt normal und schreibt sie nicht. | `eq-copilot\build\plugin\EqCopSonde014AssistentTest_artefacts\Release\EqCopSonde014AssistentTest.exe` | [OK] Exit 0 | 0,21 s | [B29](roh/NAK-223-092db4d.md#b29) |
| B28 | Die Befundzustaende auf der Gen-Seite (Abnahme U21, M-29 bis M-35). Die drei Sicherheitsstufen sind auf GENAU DREI Zustaende abgebildet, und die Abbildung ist ein FELD: ein vierter Wert macht den ganzen Snapshot ungueltig, statt still auf einen bekannten zu fallen. Nur READY TO SEND erlaubt HOLD TO AUDITION und SEND DRAFT - die Sperre liegt am Befund im Modell, nicht an einer ausgegrauten Schaltflaeche. Messqualitaet der Passage und Sicherheit des Befunds bleiben zwei Felder aus zwei Quellen: ein Befund mit confidence.class hoch, dessen Zustand more_data sagt, bleibt nicht handelbar. Alternativen sind EIGENE Befunde mit eigenem Zustand und reisen als IDs; ein Freitext faellt. Der Beleg ist die markierte Zone - Bandrand 0 und 221 gueltig, leeres Intervall und 222 nicht -, und ein Belegtextfeld gibt es nicht. Die drei Anzeigezeilen sind drei eigene Felder; eine vierte oder eine leere faellt. Die acht Ausschlussgruende kommen mit ihrem Grund an, ein neunter faellt. Jede Zahl faellt an ihrer Grenze und nicht erst danach. Zuletzt die Fassungsleiter: ein Leser der Fassung 2 lehnt findings ab, Abwesenheit heisst keine Befunde, und ein Sitzungswechsel raeumt sie ab. Seit Etappe E dazu der Maskierungswert AM Befund: er benennt Frequenzbereich und beide Quellen, gueltig und herabgesetzt sind zwei eigene Bits, Abwesenheit ist etwas anderes als ein ungueltiger Wert, und ein Objekt beliebiger Form faellt - sechs Formen einzeln geprueft. Eine Zeichenanweisung (Achse, Zoom, Farbe) faellt am Vertrag, auch im Maskierungswert. | `eq-copilot\build\plugin\EqCopSonde014BefundTest_artefacts\Release\EqCopSonde014BefundTest.exe` | [OK] Exit 0 | 0,14 s | [B28](roh/NAK-223-092db4d.md#b28) |
| B8 | Lifecycle-Klassifikation §53.5 bleibt erhalten; SONDE-011 startet den Broker nur ueber state::Lebenslauf::darfBrokerStarten() bei Main plus offenem Editor. Alle Negativzustaende lassen den Launcher unberuehrt, und die instrumentierte Gegenprobe misst null Broker-Lifecycle-Aufrufe aus processBlock beziehungsweise dem Audiothread. | `eq-copilot\build\plugin\EqCopLebenslaufTest_artefacts\Release\EqCopLebenslaufTest.exe` | [OK] Exit 0 | 0,28 s | [B8](roh/NAK-223-092db4d.md#b8) |
| A32 | Codebase- und Kontextgesundheit gegen die Schwellen aus Plan S25b und dem Kontext-Hygiene-Playbook: Zeilen je Quelldatei (Grenze 2 000, Ziel 1 500, `broker/src/generiert/` ausgenommen), Anzahl Funktionen ueber 200 Zeilen, aufruferlose allow(dead_code)-Helfer, Backtick-Bezeichner in Kommentaren ohne Entsprechung im Repo, sowie Bytes von MEMORY.md, CLAUDE.md und Dirigenten-Skill, Index-Zeilen ueber 250 Zeichen und Memory-Dateien ohne Index-Link. Die Ratschen (Funktionen 28, Kommentar-Bezeichner 35, clippy 91) stehen auf dem Iststand vom 09.09.2026 und reissen bei der ersten Verschlechterung; die Zeilengrenze reisst heute bei genau den fuenf Dateien mit Pflegeticket NAK-224/225/234 und meldet jeden weiteren Treffer als OHNE PFLEGETICKET. Vorangestellt laeuft der Selbsttest: 62 Faelle im Speicher durch dieselben Funktionen, jede Erwartung mit ihrem Gegenteil (Zahlenraender 2 000/2 001, 200/201, 250/251, CRLF, BOM, fehlender Schlussumbruch, Rust-Lebensdauer und C++-Ziffernstrenner gegen die Literalmaskierung). Der clippy-Teil laeuft NICHT mit (Schalter --clippy, er baut) und meldet sich als nicht messbar. Nicht blockierend: Exit 4 ist ein Hinweis. Exit 2 ist ROT und heisst, dass das WERKZEUG nicht mehr misst - roter Selbsttest oder ein fehlender Messort, denn ein verschobener Pfad wuerde sonst still 0 messen und die halbe Codebase unbemerkt aus dem Mass nehmen. | `py -3.13 tools\plan\gesundheit.py --mit-selbsttest` | [HINWEIS] Exit 4 - Befund, nicht blockierend (siehe Rohausgabe) | 4,64 s | [A32](roh/NAK-223-092db4d.md#a32) |

